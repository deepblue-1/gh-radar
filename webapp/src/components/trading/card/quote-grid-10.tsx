"use client";

import type { RelayQuote } from "@gh-radar/shared";

export interface QuoteGrid10Props {
  quote: RelayQuote | null;
  basePrice?: number;
  upperLimit?: number;
  lowerLimit?: number;
  currentPrice?: number;
}

/** RED 골격 — Task 1 GREEN 에서 채운다. */
export function QuoteGrid10(_props: QuoteGrid10Props) {
  return null;
}
