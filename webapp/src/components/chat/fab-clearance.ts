/**
 * AI FAB 과 겹치지 않기 위한 **하단 고정 바의 오른쪽 여백** (18-10).
 *
 * FAB(`chat-fab.tsx`)은 종목상세 본문(`/stocks/{code}`)에서 `fixed right-6 bottom-6` 로 뜨고,
 * 폭은 종목명 라벨(「AI · {종목명} 분석」) 길이에 따라 달라진다 — 고정 숫자 여백으로는 이름이
 * 긴 종목에서 다시 겹친다(18-10 브라우저 실측: 「삼성전자」 에서 FAB 폭 134px).
 * 그래서 FAB 이 자기 실측 폭을 `--chat-fab-w` 로 문서 루트에 싣고, 같은 화면의 하단 고정 바는
 * 이 클래스로 **오른쪽 끝을** `우측 오프셋 24 + FAB 폭 + 간격 8` 만큼 당긴다.
 * ★ 안쪽 여백(`pr`)이 아니라 바의 **오른쪽 끝(`right`)** 이다 — 여백만 주면 버튼은 비켜 가도
 *   바의 배경·블러가 FAB 밑까지 깔리고, 둘 다 `z-40` 이라 나중에 붙는 포털(바)이 FAB 을 덮어
 *   채팅 진입점이 흐려진다(18-10 브라우저 실측). 바가 FAB 자리까지 오지 않으면 겹칠 면적이 없다.
 *
 * ★ 이 파일은 **상수만** 둔다 — 카드 본문(작업대에도 쓰인다)이 FAB 컴포넌트 모듈 전체를
 *   끌어오지 않게 한다.
 * ★ FAB 이 없는 화면에서는 변수가 없어 `0px` 로 떨어진다 — 이 클래스는 FAB 표면(호가 탭)에서만
 *   붙인다(`dirty-action-bar.tsx` ⑥ⓒⓙ). z-index 로 FAB 을 덮지 않는다.
 */

/** FAB 이 문서 루트(`<html>`)에 싣는 자기 폭(px) CSS 변수 이름. */
export const CHAT_FAB_WIDTH_VAR = "--chat-fab-w";

/** 하단 고정 바의 오른쪽 끝 — 24(우측 오프셋) + FAB 폭 + 8(간격). `inset-x-0` 의 오른쪽을 덮어쓴다. */
export const CHAT_FAB_CLEARANCE_CLASS = "right-[calc(var(--chat-fab-w,0px)_+_32px)]";
