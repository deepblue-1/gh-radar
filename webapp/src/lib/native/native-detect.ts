// RED 스텁 — 구현은 GREEN 커밋에서 채운다.
export const NATIVE_DETECT_SCRIPT = '';
export function isNativeApp(): boolean {
  return false;
}
export function nativePlatform(): 'ios' | 'android' | null {
  return null;
}
