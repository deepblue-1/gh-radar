---
phase: 13-home-surge-themes
plan: 04
type: execute
wave: 3
depends_on: [03]
autonomous: false
files_modified:
  - webapp/src/lib/home-api.ts
  - webapp/src/components/home/home-header.tsx
  - webapp/src/components/home/theme-card.tsx
  - webapp/src/components/home/solo-card.tsx
  - webapp/src/components/home/news-block.tsx
  - webapp/src/components/home/home-empty.tsx
  - webapp/src/components/home/home-skeleton.tsx
  - webapp/src/components/home/home-client.tsx
  - webapp/src/hooks/use-home-query.ts
requirements: [HOME-01]
user_setup: []

must_haves:
  truths:
    - "Home fetches /api/home via apiFetch and renders themes (2+ stocks) + singles (1 stock) sections"
    - "Date nav (prev/next + 오늘 reset) and time-slot pill row (:30 slots) both work (D-02)"
    - "Each theme card shows name, reason, avg change% (RED --up), member mini-rows (top 4 + overflow), 근거 뉴스 1-2건 (verbatim title/source + ↗)"
    - "Empty state ('오늘은 +20% 급등 종목이 없습니다') renders when snapshot null or themes+singles empty"
    - "Error state = stale-but-visible + inline card (scanner-error pattern); loading = skeleton"
  artifacts:
    - path: "webapp/src/components/home/home-client.tsx"
      provides: "home page client — state machine (loading/populated/empty/error) + nav"
      min_lines: 60
    - path: "webapp/src/lib/home-api.ts"
      provides: "apiFetch contract for /api/home"
      exports: ["fetchHome"]
    - path: "webapp/src/components/home/theme-card.tsx"
      provides: "theme card per UI-SPEC (mini-row list, news block)"
    - path: "webapp/src/components/home/home-empty.tsx"
      provides: "dashed empty state + 스캐너로 이동 CTA"
  key_links:
    - from: "webapp/src/lib/home-api.ts"
      to: "/api/home"
      via: "apiFetch"
      pattern: "api/home"
    - from: "webapp/src/components/home/home-client.tsx"
      to: "webapp/src/hooks/use-home-query.ts"
      via: "useHomeQuery"
      pattern: "useHomeQuery"
---

<objective>
Build the read-only home page components per 13-UI-SPEC.md (user-approved mockup): home header (title + date nav + time-slot pill row), theme cards (mini-row member list + 근거 뉴스 block), solo (개별 급등) cards, empty/skeleton/error states, and the fetch layer (home-api + useHomeQuery). This plan produces the components and client state machine; Plan 05 mounts them at `/` and swaps the sidebar.

Purpose: HOME-01's user-facing value. The UI-SPEC is the visual contract (Toss-style globals tokens, EXACTLY 4 sizes {12,14,18,24} + 2 weights {400,800}, RED --up for all change%, no hardcoded colors). This plan implements it faithfully with a human-verify checkpoint.

Output: `webapp/src/components/home/*` + `webapp/src/lib/home-api.ts` + `webapp/src/hooks/use-home-query.ts`, verified visually.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-home-surge-themes/13-UI-SPEC.md
@.planning/phases/13-home-surge-themes/13-RESEARCH.md

<interfaces>
<!-- Visual contract = 13-UI-SPEC.md (READ IT FULLY). Reuse existing patterns. -->

Fetch layer (webapp/src/lib/api.ts, VERIFIED): apiFetch, ApiClientError, X-Request-Id capture, 8s timeout, envelope parse.

Response contract (from Plan 03, packages/shared HomeSnapshotResponse):
{ snapshot: { tradeDate, capturedAt, themeCount, stockCount, isCarried, payload: { threshold, marketStatus, themes:[{name,reason,stocks:[{code,name,changeRate}],news:[{title,url,source}]}], singles:[{code,name,changeRate,reason,news}] } } | null, index: [{tradeDate,capturedAt,themeCount,stockCount,isCarried}] }

UI-SPEC key facts (do NOT re-derive — 13-UI-SPEC.md is source of truth):
- Tokens: globals.css only. 국내 색상 LOCKED: 상승 = RED (--up). All change% = --up + .mono (tabular-nums slashed-zero) + weight 800.
- Typography: EXACTLY 4 sizes {12,14,18,24}px, EXACTLY 2 weights {400,800}. Page title --t-h2 24/800. Section headers + theme name + avg change% + solo change% = --t-h4 18/800. Body/reason/news title/stock name/change% = --t-sm 14. Codes/source/count-badge = --t-caption 12.
- Content max-width ~820px (max-w-5xl), gap --s-4.
- Card = .card-shadow + border + rounded --r-lg, hover border --primary tint.
- Date nav: prev/next icon-btn (‹ › 32×32 --r) + mono date label (YYYY-MM-DD 14/800) + 오늘 pill. next disabled at latest slot. aria-label 필수 (prev "이전 날짜", next "다음 날짜").
- Slot pills: SEGMENTED PILL row (variant A — NOT dropdown), overflow-x auto mobile. selected = .slot.on (--primary fill, 800). current/close slot = --up dot. label HH:MM ; close slot = "HH:MM · 마감".
- Theme card: header (name 18/800 + reason muted 14/400 | avg change% 18/800 mono --up + "평균 등락" cap) → member mini-rows (grid 1fr auto: [name 14/800 + code mono caption] | [change% mono 800 --up]) change% desc, top 4 + "+N개 종목 더" → 근거 뉴스 block (border-subtle divider + "근거 뉴스" label 12/800 + 1-2 anchors: --flat dot + [title 14/400 / source 12 muted + ↗], hover title → --primary, target=_blank rel=noopener).
- Solo card: header (name 14/800 + code mono | change% --t-lg=18 mono 800 --up) → news 1건 (no label).
- Section labels: "주도 테마" / "개별 급등" (--t-h4 800) + mono count-badge (muted pill).
- Empty: dashed-border card (--primary 30% tint) + circular --accent icon (lucide Sparkles/Search) + heading "오늘은 +20% 급등 종목이 없습니다" + body "장중 매시 30분에 갱신됩니다. 상단 날짜를 바꿔 지난 급등 테마를 확인해 보세요." + secondary CTA "스캐너로 이동" (→ /scanner). role=status.
- Error: scanner-error pattern (Phase 09.2/12 precedent) — stale-but-visible + inline card, heading "불러오지 못했습니다" / body "잠시 후 다시 시도해 주세요." / button "다시 불러오기". error.message NOT shown; console.error separated.
- is_carried / marketStatus badges = executor discretion (optional), muted 12, 400/800 only.

Skeleton reuse: scanner/themes skeleton family (.skeleton-list stagger + prefers-reduced-motion). Header skeleton + 2-3 card skeletons.
Empty reuse: themes-empty pattern.
Error reuse: scanner-error / Phase 09.2 chart error pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: home-api + useHomeQuery + card presentation components</name>
  <read_first>
    - .planning/phases/13-home-surge-themes/13-UI-SPEC.md (FULL — visual contract)
    - .planning/phases/13-home-surge-themes/mockups/home-surge-themes.html (approved visual source of truth)
    - webapp/src/lib/api.ts (apiFetch/ApiClientError contract)
    - webapp/src/lib/theme-api.ts or scanner-api (apiFetch usage precedent)
    - webapp/src/styles/globals.css (token names: --up, --t-h2/h4/sm/caption, --s-*, --r-lg, --accent, --primary, --flat, --border-subtle, .mono, .card-shadow)
    - webapp/src/components/themes/* + scanner/* (card/mini-row + count-badge precedent)
  </read_first>
  <action>
    - webapp/src/lib/home-api.ts: export fetchHome(params?: { date?: string; capturedAt?: string }): Promise<HomeSnapshotResponse> using apiFetch(`/api/home?...`) with query params. Import HomeSnapshotResponse from @gh-radar/shared.
    - webapp/src/hooks/use-home-query.ts: useHomeQuery({ date?, capturedAt? }) — fetch state { data, loading, error }, stale-but-visible on error (keep last data), refetch on param change. No polling (read-only history; 60s optional but not required). console.error separated from UI error.
    - webapp/src/components/home/news-block.tsx: NewsBlock({ news, showLabel }) — border-subtle divider + optional "근거 뉴스" label (12/800) + up to 2 anchors (--flat dot, title 14/400, source 12 muted + ↗, target=_blank rel=noopener, hover title --primary). Verbatim title/url/source (no transform).
    - webapp/src/components/home/theme-card.tsx: ThemeCard({ theme }) per UI-SPEC — header (name --t-h4 800 + reason muted 14/400 | avg change% --t-h4 800 mono --up computed = avg of stocks.changeRate + "평균 등락" cap), member mini-rows (grid 1fr auto, change% desc, top 4 + "+N개 종목 더"), NewsBlock showLabel. All change% = .mono --up 800. No hardcoded colors.
    - webapp/src/components/home/solo-card.tsx: SoloCard({ single }) — header (name 14/800 + code mono caption | change% --t-lg 18 mono 800 --up) + NewsBlock showLabel=false. reason muted below header if present.
  </action>
  <verify>
    <automated>pnpm --filter webapp build && grep -q "api/home" webapp/src/lib/home-api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "apiFetch" webapp/src/lib/home-api.ts` matches
    - `grep -rq "var(--up)" webapp/src/components/home/theme-card.tsx` matches (RED change%)
    - `grep -q "rel=\"noopener\"" webapp/src/components/home/news-block.tsx` matches (or rel with noopener)
    - `grep -rqi "#[0-9a-f]\{6\}\|oklch(" webapp/src/components/home/theme-card.tsx webapp/src/components/home/solo-card.tsx` returns NO match (no hardcoded colors)
    - `grep -q "+N개 종목 더\|개 종목 더" webapp/src/components/home/theme-card.tsx` matches (overflow line)
    - `pnpm --filter webapp build` exits 0
  </acceptance_criteria>
  <done>Fetch layer + hook + theme/solo/news card components render per UI-SPEC with globals tokens only, RED change%, verbatim news anchors. Webapp builds.</done>
</task>

<task type="auto">
  <name>Task 2: home header (date nav + slot pills) + empty/skeleton/error + home-client</name>
  <read_first>
    - .planning/phases/13-home-surge-themes/13-UI-SPEC.md (Component & Interaction Inventory + a11y + States to spec)
    - webapp/src/components/scanner/scanner-skeleton.tsx (skeleton family + prefers-reduced-motion)
    - webapp/src/components/themes/*empty* (themes-empty pattern)
    - webapp/src/components/scanner/*error* or Phase 09.2 chart error (scanner-error stale-but-visible pattern)
    - webapp/src/components/home/theme-card.tsx + solo-card.tsx (Task 1)
  </read_first>
  <action>
    - webapp/src/components/home/home-header.tsx: HomeHeader({ snapshot, index, selected, onSelectDate, onSelectSlot, onToday }) — title "오늘의 급등 테마" (--t-h2 24/800) + date nav (prev/next icon-btn 32×32 --r with aria-label "이전 날짜"/"다음 날짜", mono date label 14/800, "오늘" reset pill; next disabled at latest slot opacity 0.4, prev disabled if no earlier date) + slot pill row (segmented, derived from index filtered to selected date; .slot.on for selected --primary fill 800; --up dot for current/close slot; label HH:MM, close slot "HH:MM · 마감"; overflow-x auto). All dates/times .mono.
    - webapp/src/components/home/home-empty.tsx: dashed-border card (--primary 30% tint via color-mix) + circular --accent icon (lucide Sparkles) + heading (--t-h4 800) + body (muted 14/400 max-w 44ch) + secondary CTA link "스캐너로 이동" → /scanner. role=status.
    - webapp/src/components/home/home-skeleton.tsx: header skeleton + 2-3 card skeletons (scanner skeleton family, prefers-reduced-motion static).
    - webapp/src/components/home/home-client.tsx ('use client'): useHomeQuery + state machine. loading → HomeSkeleton. error (no prior data) → inline error card (heading "불러오지 못했습니다" / body "잠시 후 다시 시도해 주세요." / button "다시 불러오기"; stale-but-visible if prior data exists; console.error only). populated → HomeHeader + section "주도 테마" (count-badge) mapping ThemeCard + section "개별 급등" (count-badge) mapping SoloCard. empty (snapshot null OR payload.themes empty AND singles empty) → HomeEmpty. Nav state: selected {date, capturedAt}; onSelectDate/onSelectSlot/onToday update state → useHomeQuery refetch. is_carried/marketStatus badge optional (muted, 400/800 only).
  </action>
  <verify>
    <automated>pnpm --filter webapp build && grep -q "오늘의 급등 테마" webapp/src/components/home/home-header.tsx && grep -q "급등 종목이 없습니다" webapp/src/components/home/home-empty.tsx && grep -q 'aria-label' webapp/src/components/home/home-header.tsx && grep -q 'role="status"' webapp/src/components/home/home-empty.tsx && grep -q 'rel="noopener' webapp/src/components/home/news-block.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "이전 날짜" webapp/src/components/home/home-header.tsx` and `grep -q "다음 날짜" ...` (aria-labels, a11y)
    - `grep -q "· 마감" webapp/src/components/home/home-header.tsx` matches (close slot label)
    - `grep -q "스캐너로 이동" webapp/src/components/home/home-empty.tsx` matches
    - `grep -q "role=\"status\"" webapp/src/components/home/home-empty.tsx` matches
    - `grep -q "다시 불러오기" webapp/src/components/home/home-client.tsx` matches (error retry)
    - `grep -q "useHomeQuery" webapp/src/components/home/home-client.tsx` matches
    - `grep -q "주도 테마" webapp/src/components/home/home-client.tsx` and `grep -q "개별 급등" ...`
    - `pnpm --filter webapp build` exits 0
  </acceptance_criteria>
  <done>Header (date nav + slot pills w/ a11y labels), empty (CTA + role=status), skeleton, and home-client state machine (loading/populated/empty/error) all build and follow UI-SPEC.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of home components against UI-SPEC</name>
  <what-built>
    Full home page component set (header/date-nav/slot-pills, theme cards, solo cards, news blocks, empty/skeleton/error states) per 13-UI-SPEC.md. Not yet mounted at / (Plan 05 does that) — verify via a temporary route or Storybook-less local render, OR defer visual check to Plan 05 production if executor mounts a preview.
  </what-built>
  <how-to-verify>
    Executor renders home-client with mock HomeSnapshotResponse data (populated + empty variants) at a temp local route (e.g. /home-preview) or via the existing /design catalog, then user checks in browser (light + dark):
    1. Theme cards: RED (--up) change%, mono numbers, name/reason/avg layout, member mini-rows (top 4 + "+N개 종목 더"), 근거 뉴스 1-2건 with source + ↗ external link.
    2. Date nav prev/next + 오늘 pill; slot pill row (segmented, selected --primary, close slot "· 마감", --up dot).
    3. Empty state (dashed border, --accent icon, "오늘은 +20% 급등 종목이 없습니다", 스캐너로 이동 CTA).
    4. Light AND dark theme both correct (tokens auto-branch).
    5. Typography: only 4 sizes + 2 weights visible; no hardcoded colors.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues (spacing, color, typography, layout).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/home response → DOM | AI-derived content (news title/url) rendered as links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11 | Tampering (reverse tabnabbing) | news-block external anchors | mitigate | target=_blank rel="noopener noreferrer" on all news links |
| T-13-09 | Info Disclosure (error internals) | home-client error state | mitigate | generic copy "잠시 후 다시 시도해 주세요."; error.message not rendered; console.error separated (scanner-error pattern) |
</threat_model>

<verification>
- `pnpm --filter webapp build` exits 0.
- Visual checkpoint (Task 3) approved for populated + empty, light + dark.
- No hardcoded colors; exactly 4 sizes / 2 weights; RED --up change%.
</verification>

<success_criteria>
- Home components render themes + singles per UI-SPEC with globals tokens only.
- Date + slot navigation (D-02), empty/skeleton/error states all present.
- News anchors verbatim + rel=noopener; visual checkpoint approved.
</success_criteria>

<output>
After completion, create `.planning/phases/13-home-surge-themes/13-04-SUMMARY.md`.
</output>
