---
phase: 13-home-surge-themes
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - webapp/src/app/page.tsx
  - webapp/src/components/layout/app-sidebar.tsx
  - webapp/e2e/specs/home.spec.ts
requirements: [HOME-01]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Root / renders the home page (no longer redirects to /scanner)"
    - "Sidebar NAV order = 홈(1st, active at /) · 스캐너(2nd) · 테마(3rd) · 관심종목(4th)"
    - "/scanner direct access still works (regression)"
    - "E2E covers home render + date/slot nav + empty state + scanner regression"
  artifacts:
    - path: "webapp/src/app/page.tsx"
      provides: "home page mount (AppShell + Suspense + HomeClient)"
      contains: "HomeClient"
    - path: "webapp/src/components/layout/app-sidebar.tsx"
      provides: "NAV with 홈 first, 스캐너 second"
      contains: "홈"
    - path: "webapp/e2e/specs/home.spec.ts"
      provides: "home E2E (render/nav/empty) + scanner regression"
  key_links:
    - from: "webapp/src/app/page.tsx"
      to: "webapp/src/components/home/home-client.tsx"
      via: "import HomeClient"
      pattern: "HomeClient"
    - from: "webapp/src/components/layout/app-sidebar.tsx"
      to: "/"
      via: "NAV href /"
      pattern: "href: \"/\""
---

<objective>
Promote home to the app root: replace `page.tsx`'s `redirect('/scanner')` with the home page (AppShell + Suspense + HomeClient from Plan 04), reorder the sidebar NAV (홈 1st active-at-`/`, 스캐너 2nd, 테마 3rd, 관심종목 4th), and add E2E coverage for home render + date/slot nav + empty state plus a `/scanner` regression check.

Purpose: This is the "홈 = 루트(/) 승격" decision. It's the smallest change surface but has a regression risk (root was a server redirect) — E2E locks both the new home behavior and continued /scanner access.

Output: home mounted at `/`, reordered sidebar, `webapp/e2e/specs/home.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-home-surge-themes/13-UI-SPEC.md
@.planning/phases/13-home-surge-themes/13-RESEARCH.md

<interfaces>
<!-- Current state (VERIFIED) — exact swap points. -->

webapp/src/app/page.tsx CURRENT (redirect — to be replaced):
```ts
import { redirect } from 'next/navigation';
export default function HomePage() { redirect('/scanner'); }
```

webapp/src/app/scanner/page.tsx (TEMPLATE for new home page — AppShell + Suspense + Skeleton):
```tsx
export const dynamic = 'force-dynamic';
export default function ScannerPage() {
  return (<AppShell sidebar={<AppSidebar />}>
    <div className="mx-auto flex w-full max-w-5xl flex-col gap-4 md:gap-6">
      <Suspense fallback={<ScannerSkeleton />}><ScannerClient /></Suspense>
    </div></AppShell>);
}
```

app-sidebar.tsx CURRENT NAV (VERIFIED — reorder + add 홈):
```ts
import { Activity, Layers, Star } from "lucide-react";
const NAV = [
  { href: "/scanner", label: "스캐너", icon: Activity },
  { href: "/themes", label: "테마", icon: Layers },
  { href: "/watchlist", label: "관심종목", icon: Star },
];
const active = pathname === href;   // pathname === "/" activates 홈
```
Target NAV: prepend { href: "/", label: "홈", icon: Home } (import Home from lucide-react). Order = 홈, 스캐너, 테마, 관심종목. active weight 600 is INHERITED from existing component (do NOT touch styles — UI-SPEC Typography note).

Regression note (RESEARCH Runtime State): /scanner route/bookmarks stay working; only / changes. E2E must assert /scanner still renders.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount home at / + reorder sidebar NAV</name>
  <read_first>
    - webapp/src/app/page.tsx (current redirect — replace)
    - webapp/src/app/scanner/page.tsx (structure template: AppShell + Suspense + skeleton + force-dynamic)
    - webapp/src/components/layout/app-sidebar.tsx (NAV array + active logic — from interfaces)
    - webapp/src/components/home/home-client.tsx + home-skeleton.tsx (Plan 04 — mount targets)
  </read_first>
  <action>
    - webapp/src/app/page.tsx: replace redirect with home page mirroring scanner/page.tsx structure — `export const dynamic = 'force-dynamic'` + AppShell sidebar={<AppSidebar />} + max-w-5xl container gap --s-4 + Suspense fallback={<HomeSkeleton />} wrapping <HomeClient />. Import HomeClient + HomeSkeleton from @/components/home/*.
    - webapp/src/components/layout/app-sidebar.tsx: import Home from lucide-react; NAV = [{ href:"/", label:"홈", icon:Home }, { href:"/scanner", label:"스캐너", icon:Activity }, { href:"/themes", label:"테마", icon:Layers }, { href:"/watchlist", label:"관심종목", icon:Star }]. Do NOT change active-item style code (600 inherited). pathname==="/" naturally activates 홈. Keep aria-current="page" + data-nav-item.
  </action>
  <verify>
    <automated>pnpm --filter webapp build && grep -q "HomeClient" webapp/src/app/page.tsx && grep -q 'href: "/"' webapp/src/components/layout/app-sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "redirect" webapp/src/app/page.tsx` returns NO match (redirect removed)
    - `grep -q "HomeClient" webapp/src/app/page.tsx` matches
    - `grep -q "force-dynamic" webapp/src/app/page.tsx` matches
    - app-sidebar NAV first entry label is "홈" and href "/" (홈 before 스캐너)
    - `grep -q "Home" webapp/src/components/layout/app-sidebar.tsx` matches (lucide Home import)
    - `pnpm --filter webapp build` exits 0
  </acceptance_criteria>
  <done>Root / renders home (no redirect); sidebar shows 홈 first (active at /), then 스캐너/테마/관심종목. Webapp builds.</done>
</task>

<task type="auto">
  <name>Task 2: E2E — home render + nav + empty + scanner regression</name>
  <read_first>
    - webapp/e2e/specs/*.spec.ts (existing Playwright spec + auth fixture/storageState pattern)
    - webapp/playwright.config.ts (baseURL, PORT — dev.sh PORT=3100 per memory)
    - webapp/e2e/specs/a11y.spec.ts (contains `page.goto('/scanner')` — mirror its scanner-render assertion for regression)
    - .planning/phases/13-home-surge-themes/13-UI-SPEC.md (copy strings for selectors)
  </read_first>
  <action>
    - webapp/e2e/specs/home.spec.ts (Playwright, reuse auth fixture/storageState):
      (1) `/` renders home — page title "오늘의 급등 테마" visible; either theme cards OR empty state present.
      (2) date nav: prev/next icon buttons present with aria-label "이전 날짜"/"다음 날짜"; "오늘" reset pill present.
      (3) slot pill row present when snapshot exists (or gracefully absent on empty) — assert slot pills OR empty state (data-dependent, tolerant).
      (4) empty-state-tolerant: if no surges today, assert "오늘은 +20% 급등 종목이 없습니다" + "스캐너로 이동" CTA link to /scanner.
      (5) REGRESSION: navigate to /scanner directly → scanner UI still renders (mirror scanner.spec assertion). / no longer 302-redirects to /scanner.
      (6) sidebar: 홈 nav item present, active (aria-current="page") at /.
    Use tolerant assertions (data-dependent: surges vary by day) — assert (cards present) OR (empty state present), never hard-fail on live data absence.
  </action>
  <verify>
    <automated>pnpm --filter webapp build && pnpm --filter @gh-radar/webapp exec playwright test home.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "오늘의 급등 테마" webapp/e2e/specs/home.spec.ts` matches
    - `grep -q "/scanner" webapp/e2e/specs/home.spec.ts` matches (regression nav)
    - `grep -q "이전 날짜\|다음 날짜" webapp/e2e/specs/home.spec.ts` matches (date nav)
    - `grep -q "aria-current" webapp/e2e/specs/home.spec.ts` matches (홈 active)
    - spec uses tolerant OR-assertion for cards vs empty (grep -q "급등 종목이 없습니다" webapp/e2e/specs/home.spec.ts matches)
    - `pnpm --filter @gh-radar/webapp exec playwright test home.spec.ts` exits 0 (playwright webServer auto-boots PORT=3100 dev, reuseExistingServer outside CI — runs home.spec locally, not deferred)
    - `pnpm --filter webapp build` exits 0
  </acceptance_criteria>
  <done>home.spec.ts covers home render, date/slot nav, empty tolerance, and /scanner regression. Webapp builds AND `pnpm --filter @gh-radar/webapp exec playwright test home.spec.ts` passes green locally (playwright auto-boots dev server). Plan 06 re-runs the same spec against the deployed preview as the release gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → / | public read-only home (no auth-gated content beyond existing middleware) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-12 | Availability (root redirect regression) | page.tsx swap | mitigate | E2E asserts / renders home AND /scanner still works (Runtime State regression note) |
</threat_model>

<verification>
- `pnpm --filter webapp build` exits 0.
- page.tsx no longer redirects; sidebar NAV reordered (홈 first).
- `pnpm --filter @gh-radar/webapp exec playwright test home.spec.ts` green locally (render/nav/empty/regression); Plan 06 re-runs it against the deployed preview as the release gate.
</verification>

<success_criteria>
- Root / renders home page; /scanner regression-safe.
- Sidebar NAV: 홈 · 스캐너 · 테마 · 관심종목, 홈 active at /.
- E2E spec (home.spec) written AND passing locally via `playwright test home.spec.ts`.
</success_criteria>

<output>
After completion, create `.planning/phases/13-home-surge-themes/13-05-SUMMARY.md`.
</output>
