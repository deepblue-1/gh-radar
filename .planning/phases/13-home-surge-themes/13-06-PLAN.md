---
phase: 13-home-surge-themes
plan: 06
type: execute
wave: 5
depends_on: [02, 03, 05]
autonomous: false
files_modified:
  - scripts/setup-home-sync-iam.sh
  - scripts/deploy-home-sync.sh
  - scripts/smoke-home-sync.sh
requirements: [HOME-01]
user_setup:
  - service: gcp-cloud-run
    why: "home-sync Cloud Run Job + Scheduler deploy + server/webapp redeploy"
    dashboard_config:
      - task: "Approve GCP-authenticated deploy (Deployer SA) — setup IAM, deploy job, POC run, smoke, server/webapp redeploy"
        location: "gh-radar GCP project (asia-northeast3)"

must_haves:
  truths:
    - "home-sync SA + Secret accessor bindings exist (anthropic + supabase-service-role reused, 신규 0)"
    - "Cloud Run Job gh-radar-home-sync + Scheduler gh-radar-home-sync-cron (30 9-15 * * 1-5, OAuth invoker) deployed"
    - "One POC slot run produces plausible KR surge themes + measured token cost within ~$3.1/월 ceiling"
    - "server redeployed with /api/home; webapp deployed with home at /; E2E + smoke green"
  artifacts:
    - path: "scripts/deploy-home-sync.sh"
      provides: "Cloud Run Job + Scheduler deploy (theme-sync clone, no VPC, OAuth)"
    - path: "scripts/setup-home-sync-iam.sh"
      provides: "SA + Secret accessor + scheduler invoker binding"
    - path: "scripts/smoke-home-sync.sh"
      provides: "job execute + snapshot row assertion"
  key_links:
    - from: "scripts/deploy-home-sync.sh"
      to: "gh-radar-home-sync-cron"
      via: "gcloud scheduler --schedule + --oauth-service-account-email"
      pattern: "30 9-15"
---

<objective>
Deploy the home-sync pipeline to production and gate it on a Claude POC (cost + clustering accuracy), mirroring the theme-sync deploy pattern (no VPC, OAuth invoker, reused Secrets). Then redeploy server (for `/api/home`) and webapp (for home at `/`), run the smoke + Playwright E2E, and verify the production home page renders.

Purpose: This is the [BLOCKING] production activation of HOME-01. The POC gate (theme-sync 10-06 precedent) validates that bottom-up clustering produces real KR themes at acceptable cost before wiring the scheduler to run 7×/day.

Output: 3 deploy scripts, deployed Job + Scheduler, POC evidence, redeployed server + webapp, green smoke + E2E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-home-surge-themes/13-RESEARCH.md
@.planning/phases/13-home-surge-themes/13-VALIDATION.md

<interfaces>
<!-- Deploy pattern = theme-sync (VERIFIED). Reuse Secrets, no VPC. -->

deploy-theme-sync.sh precedent (VERIFIED):
- guard: active gcloud config + SA (gh-radar-scheduler-sa + gh-radar-theme-sync-sa) + Secrets exist
- Cloud Run Job: gh-radar-theme-sync, --memory=512Mi, --task-timeout=600s, --max-retries=1, service-account gh-radar-theme-sync-sa
- --set-secrets="SUPABASE_SERVICE_ROLE_KEY=gh-radar-supabase-service-role:latest,ANTHROPIC_API_KEY=gh-radar-anthropic-api-key:latest"
- add-iam-policy-binding run.invoker for scheduler SA
- Scheduler: --schedule + --time-zone="Asia/Seoul" + --oauth-service-account-email (OIDC 금지, Pitfall 5)

home-sync deltas (RESEARCH §Pattern 5):
- Job gh-radar-home-sync, --memory=512Mi, --task-timeout=120s (Claude 1x, shorter than theme-sync 600s), --max-retries=1
- NO VPC/--network (Supabase + Anthropic only — do NOT clone intraday-sync VPC stack)
- Secrets REUSED (신규 0): gh-radar-supabase-service-role, gh-radar-anthropic-api-key
- --set-env-vars: SUPABASE_URL, HOME_SYNC_SURGE_THRESHOLD=20, HOME_SYNC_NEWS_PER_STOCK=5, HOME_SYNC_SURGE_MAX=80, LOG_LEVEL=info, APP_VERSION=$SHA (NO brightdata/alpha/scrape vars)
- Scheduler gh-radar-home-sync-cron, --schedule="30 9-15 * * 1-5", --time-zone="Asia/Seoul", --oauth-service-account-email=gh-radar-scheduler-sa@... (7 slots, 15:30 마감 slot included, §Pattern 5)
- SA: gh-radar-home-sync-sa (new, minimal — anthropic + supabase-service-role accessor 2 bindings only)

setup-home-sync-iam.sh precedent = setup-theme-sync-iam.sh (SA create + secretAccessor binding for 2 secrets + scheduler invoker). Reuse gh-radar-scheduler-sa.
smoke-home-sync.sh precedent = smoke-theme-sync.sh (gcloud run jobs execute --wait + Cloud Logging msg check [use jsonPayload.msg for Job logs per Phase 10 lesson] + Supabase snapshot row count > 0 assertion). DI-02 CR guard: pipe header parsing through `tr -d '\r'`.

Deploy creds (MEMORY): Deployer SA key ~/.config/gcloud/gh-radar-deployer.json, GOOGLE_APPLICATION_CREDENTIALS + CLOUDSDK_CORE_PROJECT=gh-radar. Do NOT re-ask for creds.
server redeploy: scripts/deploy-server.sh (HEAD image). webapp deploy: repo root vercel pull→build→deploy --prebuilt (MEMORY reference_vercel_frontend_deploy — ignoreCommand skips docs pushes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write setup-home-sync-iam.sh + deploy-home-sync.sh + smoke-home-sync.sh (static)</name>
  <read_first>
    - scripts/setup-theme-sync-iam.sh (SA + secretAccessor + scheduler invoker binding template)
    - scripts/deploy-theme-sync.sh (Job + Scheduler OAuth deploy template — FULL)
    - scripts/smoke-theme-sync.sh (job execute + row-count assertion + tr -d '\r' guard)
    - .planning/phases/13-home-surge-themes/13-RESEARCH.md §Pattern 5 (home-sync deltas)
  </read_first>
  <action>
    Clone the 3 theme-sync scripts with home-sync identifiers + §Pattern 5 deltas:
    - scripts/setup-home-sync-iam.sh: create SA gh-radar-home-sync-sa; bind secretAccessor on gh-radar-supabase-service-role + gh-radar-anthropic-api-key; bind gh-radar-scheduler-sa run.invoker (deferred to deploy if job-not-yet-created, mirror theme-sync ordering). NO brightdata secret binding.
    - scripts/deploy-home-sync.sh: guard (config + SA gh-radar-scheduler-sa/gh-radar-home-sync-sa + Secrets supabase-service-role/anthropic-api-key) → docker build+push image home-sync:$SHA (+latest) → gcloud run jobs deploy gh-radar-home-sync (512Mi, task-timeout=120s, max-retries=1, service-account gh-radar-home-sync-sa, --set-secrets 2 reused, --set-env-vars home-sync tuning NO scrape vars, NO --network) → add run.invoker for scheduler SA → gcloud scheduler jobs create/update http gh-radar-home-sync-cron --schedule="30 9-15 * * 1-5" --time-zone="Asia/Seoul" --oauth-service-account-email (OIDC 금지).
    - scripts/smoke-home-sync.sh: gcloud run jobs execute gh-radar-home-sync --wait → check Cloud Logging (jsonPayload.msg, retry for ingestion lag) → Supabase REST count home_theme_snapshots where trade_date=today → assert >= 1 row (or plausible empty on no-surge day, logged). tr -d '\r' on any curl header parse (DI-02 guard).
    Mark scripts executable (chmod +x).
  </action>
  <verify>
    <automated>bash -n scripts/setup-home-sync-iam.sh && bash -n scripts/deploy-home-sync.sh && bash -n scripts/smoke-home-sync.sh && grep -q "30 9-15 \* \* 1-5" scripts/deploy-home-sync.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` (syntax check) passes for all 3 scripts
    - `grep -q "gh-radar-home-sync-cron" scripts/deploy-home-sync.sh` matches
    - `grep -q "oauth-service-account-email" scripts/deploy-home-sync.sh` matches (OIDC 금지)
    - `grep -q "task-timeout=120s" scripts/deploy-home-sync.sh` matches
    - `grep -q "network\|--network\|vpc" scripts/deploy-home-sync.sh` returns NO match (no VPC)
    - `grep -q "brightdata" scripts/deploy-home-sync.sh` returns NO match (Secrets reused, 신규 0)
    - `grep -q "gh-radar-anthropic-api-key" scripts/deploy-home-sync.sh` and `grep -q "gh-radar-supabase-service-role" ...` (reused secrets)
    - `grep -q "home_theme_snapshots" scripts/smoke-home-sync.sh` matches
  </acceptance_criteria>
  <done>3 deploy scripts pass syntax check, use OAuth invoker + reused secrets + no VPC + 30 9-15 cron. Static-verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] GCP deploy + Claude POC gate + server/webapp redeploy + smoke/E2E</name>
  <what-built>
    home-sync worker (Plans 01-02), /api/home route (Plan 03), home UI + root swap (Plans 04-05), and deploy scripts (Task 1). This checkpoint executes the actual production activation + the POC cost/accuracy gate (theme-sync 10-06 precedent). Build/type/unit all pass WITHOUT deploy — production activation is required for HOME-01 to be end-to-end live.
  </what-built>
  <how-to-verify>
    Orchestrator runs after user approval (GCP creds: Deployer SA per MEMORY, do NOT re-ask):
    1. `bash scripts/setup-home-sync-iam.sh` — SA + secret accessor bindings (exit 0).
    2. `bash scripts/deploy-home-sync.sh` — build/push image + Job + Scheduler (OAuth) (exit 0).
    3. POC GATE: `gcloud run jobs execute gh-radar-home-sync --wait` → inspect Cloud Logging summary (claudeCalled, themeCount, stockCount) + the written home_theme_snapshots row payload. VERIFY: (a) themes correspond to real KR surge themes (육안, theme-sync POC precedent), (b) news refs resolved to verbatim titles (no hallucinated URLs), (c) measure input/output tokens → estimate monthly cost within ~$3.1/월 ceiling. If accuracy poor or cost >2× ceiling → STOP, tune prompt/newsPerStock/surgeMax before proceeding.
    4. `bash scripts/deploy-server.sh` (HEAD image) → curl `/api/home` → 200 with { snapshot, index } object contract (or snapshot=null on no-surge). server smoke green.
    5. webapp deploy (repo root: vercel pull → build → deploy --prebuilt per MEMORY) → visit `/` → home page renders (오늘의 급등 테마 + cards OR empty state), /scanner still works.
    6. `bash scripts/smoke-home-sync.sh` → snapshot row assertion.
    7. `pnpm --filter webapp e2e -- home.spec` → home E2E + scanner regression green.
  </how-to-verify>
  <resume-signal>Type "approved" after: deploy exit 0, POC themes plausible + cost within ceiling, /api/home 200, production / renders home, /scanner regression OK, smoke + home E2E green. If POC fails, describe tuning needed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP/STATE/REQUIREMENTS + SUMMARY</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 13 section + Progress table)
    - .planning/REQUIREMENTS.md (HOME-01 Traceability status)
    - .planning/STATE.md (Roadmap Evolution + progress counters)
    - .planning/phases/13-home-surge-themes/13-*-SUMMARY.md (prior plan summaries for evidence)
  </read_first>
  <action>
    After Task 2 approval (production live):
    - ROADMAP.md: mark Phase 13 plans [x], add completion date + image SHA + POC evidence (themeCount/cost), update Progress table row (Phase 13 6/6 Complete).
    - REQUIREMENTS.md: HOME-01 Traceability Pending → Complete with evidence (production home live, POC themeCount + cost, home E2E green). Update Last updated line.
    - STATE.md: update progress counters (completed_plans + percent), add Phase 13 completion note to Roadmap Evolution (production deploy details, Secret reuse 신규 0, cron 30 9-15, POC result), update Session Continuity.
    - Only edit if Task 2 approved (production verified). If POC required tuning, record the tuning in STATE decisions.
  </action>
  <verify>
    <automated>grep -q "Phase 13" .planning/ROADMAP.md && grep -q "HOME-01" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 13 plans marked [x] with completion evidence
    - REQUIREMENTS.md HOME-01 status = Complete (post-approval)
    - STATE.md progress counters updated + Phase 13 evolution note added
  </acceptance_criteria>
  <done>Planning docs reflect Phase 13 production completion with POC evidence and deploy details.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scheduler → Cloud Run Job | OAuth invoker (not OIDC) |
| Job runtime → Secrets | service_role + anthropic via Secret Manager accessor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-13 | Spoofing/EoP (scheduler auth) | Scheduler → Job | mitigate | --oauth-service-account-email only (OIDC 금지, Pitfall 5); gh-radar-scheduler-sa run.invoker scoped to job |
| T-13-14 | EoP (over-privileged SA) | gh-radar-home-sync-sa | mitigate | minimal — only supabase-service-role + anthropic accessor (no brightdata, no VPC) |
| T-13-15 | Cost overrun (Claude runaway) | POC gate | mitigate | POC token measurement gate before scheduler-driven 7×/day activation (theme-sync 10-06 precedent) |
</threat_model>

<verification>
- 3 deploy scripts syntax-valid + OAuth/no-VPC/reused-secrets/30 9-15 cron.
- [BLOCKING] deploy exit 0; POC themes plausible + cost within ceiling; /api/home 200; production / renders home; /scanner regression OK; smoke + home E2E green.
- ROADMAP/STATE/REQUIREMENTS updated post-approval.
</verification>

<success_criteria>
- home-sync Job + Scheduler (30 9-15, OAuth, no VPC, reused Secrets) live.
- POC validates clustering accuracy + cost within ~$3.1/월.
- server /api/home + production home page live; /scanner regression-safe; smoke + E2E green.
- HOME-01 marked Complete.
</success_criteria>

<output>
After completion, create `.planning/phases/13-home-surge-themes/13-06-SUMMARY.md`.
</output>
