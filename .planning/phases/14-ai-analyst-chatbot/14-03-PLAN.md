---
phase: 14-ai-analyst-chatbot
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - server/src/middleware/require-auth.ts
  - server/src/middleware/__tests__/require-auth.test.ts
  - server/src/schemas/chat.ts
  - server/src/services/chat-history.ts
  - server/src/services/__tests__/chat-history.test.ts
autonomous: true
requirements: [CHAT-01]
must_haves:
  truths:
    - "토큰 없음/무효 요청은 401(SSE 헤더 쓰기 전)로 거부되고, 유효 토큰은 req.userId 를 설정한다"
    - "서버가 서비스롤로 conversations/messages 를 WHERE user_id 명시 필터로 read/write 한다"
    - "타 사용자 conversationId 로 접근 시 소유권 검증 실패로 거부된다"
  artifacts:
    - path: "server/src/middleware/require-auth.ts"
      provides: "requireAuth() RequestHandler — supabase.auth.getUser(jwt) 검증"
      exports: ["requireAuth"]
    - path: "server/src/services/chat-history.ts"
      provides: "대화/메시지 CRUD (서비스롤, WHERE user_id)"
      exports: ["loadConversation", "listConversations", "createConversation", "appendMessage", "deleteConversation", "assertConversationOwner"]
    - path: "server/src/schemas/chat.ts"
      provides: "zod — POST body, 목록 쿼리 검증"
  key_links:
    - from: "chat-history.assertConversationOwner"
      to: "conversations.user_id"
      via: "WHERE id AND user_id 명시 필터"
      pattern: "\\.eq\\(\"user_id\""
---

<objective>
gh-radar 서버 최초의 사용자 인증(D-02)과 대화 영속화 read/write 계층을 만든다. `supabase.auth.getUser(jwt)` JWT 검증 미들웨어 + zod 입력 검증 + 서비스롤 히스토리 CRUD(소유권 명시 필터).

Purpose: SSE 라우트(P06)와 대화관리 라우트가 소비할 인증·데이터 서비스. RLS 는 defense-in-depth 이고, 서버 경로의 실제 방어선은 `WHERE user_id = req.userId` 명시 필터(RESEARCH Pattern 3).
Output: 미들웨어 1 + 스키마 1 + 히스토리 서비스 1 + 유닛 테스트 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md
@.planning/phases/14-ai-analyst-chatbot/14-CONTEXT.md

<interfaces>
From server/src/errors.ts (에러 팩토리 — 재사용):
```typescript
export class ApiError extends Error { constructor(public status: number, public code: string, message: string) }
export const ValidationFailed = (msg: string) => new ApiError(400, "VALIDATION_FAILED", msg);
```
From server/src/schemas/home.ts (zod 스키마 스타일 — safeParse + issue.path):
```typescript
export const HomeQuery = z.object({ /* ... */ });
```
From packages/shared (P02): `ConversationRow`, `MessageRow`, `MessageBlock`, `ChatRole`
From RESEARCH Pattern 3 (require-auth — verbatim 골격):
```typescript
const { data, error } = await supabase.auth.getUser(token);
if (error || !data.user) return res.status(401).json({ error: { code: "UNAUTHENTICATED", message: "..." } });
(req as any).userId = data.user.id;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: requireAuth JWT 미들웨어 + 유닛 테스트</name>
  <files>server/src/middleware/require-auth.ts, server/src/middleware/__tests__/require-auth.test.ts</files>
  <read_first>
    - server/src/middleware/rate-limit.ts (RequestHandler 반환 팩토리 패턴)
    - server/src/app.ts (app.locals.supabase 접근 방식 — `req.app.locals.supabase`)
    - server/src/errors.ts (에러 코드/envelope)
    - .planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md (Pattern 3 — 401 은 SSE 헤더 전, 일반 JSON)
  </read_first>
  <behavior>
    - Test 1: Authorization 헤더 없음 → 401 `{error:{code:"UNAUTHENTICATED"}}`, next() 미호출
    - Test 2: `Bearer` prefix 없는 헤더 → 401
    - Test 3: `supabase.auth.getUser` 가 error 반환 → 401 "세션이 만료되었습니다."
    - Test 4: 유효 토큰 → `req.userId = data.user.id` 설정 + next() 호출
  </behavior>
  <action>
    `requireAuth(): RequestHandler` 팩토리 작성 (RESEARCH Pattern 3 verbatim):
    - `req.header("authorization")` → `Bearer ` prefix slice(7). 없으면 `res.status(401).json({ error: { code: "UNAUTHENTICATED", message: "로그인이 필요합니다." } })` + return.
    - `const supabase = req.app.locals.supabase as SupabaseClient` → `await supabase.auth.getUser(token)`.
    - error 또는 !data.user → `res.status(401).json({ error: { code: "UNAUTHENTICATED", message: "세션이 만료되었습니다." } })` + return.
    - 성공 → `(req as { userId?: string }).userId = data.user.id` → `next()`.
    - **주석**: "SSE 라우트는 res.writeHead 이후 상태코드 변경 불가 → JWT 검증은 반드시 헤더 쓰기 전(미들웨어). 401 은 일반 JSON." (RESEARCH Pitfall/Anti-pattern)
    - 타입 안전을 위해 `declare global { namespace Express { interface Request { userId?: string } } }` 또는 로컬 타입 확장 사용 (기존 request-id 미들웨어의 확장 방식 따를 것).

    테스트: `supabase.auth.getUser` 를 vi.fn mock 으로 주입한 fake `req.app.locals.supabase`. res 는 status/json spy. Behavior 4케이스.
  </action>
  <verify>
    <automated>pnpm --filter server test require-auth</automated>
  </verify>
  <acceptance_criteria>
    - `grep "supabase.auth.getUser" server/src/middleware/require-auth.ts` 매칭
    - `grep "401" server/src/middleware/require-auth.ts` 매칭 (2 케이스)
    - `grep "UNAUTHENTICATED" server/src/middleware/require-auth.ts` 매칭
    - `pnpm --filter server test require-auth` — 4 tests pass
  </acceptance_criteria>
  <done>requireAuth 미들웨어 + 4 유닛테스트 green.</done>
</task>

<task type="auto">
  <name>Task 2: chat zod 스키마</name>
  <files>server/src/schemas/chat.ts</files>
  <read_first>
    - server/src/schemas/home.ts (z.object + coerce + 정규식 패턴)
    - server/src/schemas/search.ts (종목코드 정규식 선례)
    - .planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md (V5 Input Validation — message ≤1,000자, conversationId uuid, stockCode 정규식)
  </read_first>
  <action>
    zod 스키마 export:
    - `ChatPostBody = z.object({ message: z.string().min(1).max(1000), conversationId: z.string().uuid().optional(), stockCode: z.string().regex(/^\d{6}$/).optional() })` — message 1~1,000자(D Claude's Discretion), conversationId 선택(없으면 새 대화), stockCode 6자리 숫자(종목상세 컨텍스트, D-03).
    - `ConversationListQuery = z.object({ stockCode: z.string().regex(/^\d{6}$/).optional() })` — 종목별 필터(D-13).
    기존 schemas/* 의 safeParse + issue 처리 스타일과 정합. 6자리 정규식은 search.ts 선례 확인 후 동일 패턴 사용.
  </action>
  <verify>
    <automated>pnpm --filter server exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "max(1000)" server/src/schemas/chat.ts` 매칭
    - `grep "uuid()" server/src/schemas/chat.ts` 매칭
    - `grep "ChatPostBody\|ConversationListQuery" server/src/schemas/chat.ts` 2 export
    - server typecheck exit 0
  </acceptance_criteria>
  <done>ChatPostBody + ConversationListQuery zod 스키마 export, typecheck 통과.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: chat-history 서비스 (서비스롤 CRUD + 소유권 검증) + 유닛 테스트</name>
  <files>server/src/services/chat-history.ts, server/src/services/__tests__/chat-history.test.ts</files>
  <read_first>
    - server/src/services/discussion-classify.ts (supabase.from().update().eq() 패턴 + SupabaseClient 타입)
    - server/src/mappers/home.ts (row → 도메인 매핑 스타일, snake→camel)
    - .planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md (Pattern 3 — WHERE user_id 명시 필터, Pitfall 3 — tool 원본 미저장 텍스트 스냅샷만)
  </read_first>
  <behavior>
    - Test 1: `assertConversationOwner(supabase, convId, userId)` — 소유자 일치 시 통과, 불일치/미존재 시 ApiError 403/404 throw (타 사용자 대화 차단, T-14-01)
    - Test 2: `createConversation` — user_id/stock_code/title(첫 메시지 30자 truncate) INSERT 후 row 반환
    - Test 3: `appendMessage` — role/content/blocks INSERT + conversations.updated_at = now() UPDATE
    - Test 4: `listConversations(userId, stockCode?)` — WHERE user_id [+ stock_code] ORDER BY updated_at DESC
    - Test 5: `loadConversation` — 소유권 검증 후 messages ORDER BY created_at ASC
  </behavior>
  <action>
    서비스롤 SupabaseClient 를 인자로 받는 순수 함수 모듈. 모든 read/write 는 `.eq("user_id", userId)` 명시 필터(RLS 는 방어선, 서버 경로 필터가 실제 방어 — RESEARCH Pattern 3).

    export 함수:
    - `assertConversationOwner(supabase, conversationId, userId): Promise<ConversationRow>` — `select().eq("id",convId).eq("user_id",userId).maybeSingle()`. null → `ApiError(404,"CONVERSATION_NOT_FOUND",...)`. (user_id 불일치도 여기서 not-found 로 흡수 → 존재 여부 누설 방지, T-14-01).
    - `listConversations(supabase, userId, stockCode?)` — stockCode 있으면 `.eq("stock_code",stockCode)`, 없으면 전체. `order("updated_at",{ascending:false})`. ConversationRow[] 매핑.
    - `createConversation(supabase, userId, opts:{stockCode?:string|null; firstUserMessage:string})` — title = `firstUserMessage.slice(0,30)` (제목 자동생성, 추가 LLM 콜 없음 — RESEARCH Open Q3). insert + select single 반환.
    - `appendMessage(supabase, conversationId, msg:{role:ChatRole; content:string; blocks?:MessageBlock[]|null})` — messages insert + `conversations.update({updated_at:now}).eq("id",conversationId)`.
    - `loadConversation(supabase, conversationId, userId)` — assertConversationOwner 먼저 → messages select `.eq("conversation_id",convId).order("created_at")` → `{conversation, messages}`.
    - `deleteConversation(supabase, conversationId, userId)` — assertConversationOwner 먼저 → `.delete().eq("id",convId).eq("user_id",userId)` (messages 는 FK CASCADE).

    row → 도메인 매핑은 mappers/home.ts snake→camel 스타일. 에러는 ApiError throw(라우트가 next 위임).
    테스트: supabase mock (from().select().eq()... thenable) — discussion-classify.test 나 mappers/home mock 스타일 재사용. Behavior 5케이스.
  </action>
  <verify>
    <automated>pnpm --filter server test chat-history</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function\|export function" server/src/services/chat-history.ts` >= 6
    - `grep "\.eq(\"user_id\"" server/src/services/chat-history.ts` 매칭 (명시 소유권 필터)
    - `grep "slice(0, 30)\|slice(0,30)" server/src/services/chat-history.ts` 매칭 (제목 truncate)
    - `grep "CONVERSATION_NOT_FOUND" server/src/services/chat-history.ts` 매칭
    - `pnpm --filter server test chat-history` — 5 tests pass
  </acceptance_criteria>
  <done>6 CRUD 함수 + 소유권 검증 + 5 유닛테스트 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/chat* | Authorization: Bearer JWT 가 유일한 인증 근거 (untrusted) |
| conversationId(client) → 히스토리 서비스 | 사용자가 임의 UUID 제출 가능 → 소유권 검증 필요 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Spoofing | requireAuth | mitigate | `supabase.auth.getUser(jwt)` 서명·만료 검증. 실패 시 401(SSE 헤더 전, 일반 JSON) |
| T-14-01 | Info Disclosure / Tampering (IDOR) | assertConversationOwner | mitigate | 모든 read/write `.eq("user_id", req.userId)` 명시 필터. 미소유 conversationId → 404(존재여부 누설 회피) |
| T-14-05a | 입력 검증 | chat zod 스키마 | mitigate | message ≤1,000자 / conversationId uuid / stockCode 6자리 정규식 — 프롬프트 인젝션 표면 축소(V5) |
</threat_model>

<verification>
- require-auth 4 유닛테스트
- chat-history 5 유닛테스트
- server typecheck
</verification>

<success_criteria>
- requireAuth 가 무효 토큰 401 + 유효 토큰 req.userId
- chat-history 6 CRUD 함수 소유권 명시 필터
- zod 스키마 입력 검증
</success_criteria>

<output>
After completion, create `.planning/phases/14-ai-analyst-chatbot/14-03-SUMMARY.md`
</output>
