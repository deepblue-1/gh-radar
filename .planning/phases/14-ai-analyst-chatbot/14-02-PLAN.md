---
phase: 14-ai-analyst-chatbot
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/chat.ts
  - packages/shared/src/index.ts
  - server/src/config.ts
  - server/src/services/__tests__/anthropic-mock.ts
autonomous: true
requirements: [CHAT-01]
must_haves:
  truths:
    - "SSE 이벤트 계약(ChatSSEEventMap)과 전문가 ID가 서버·웹앱 공통 타입으로 존재한다"
    - "챗 모델/kill-switch/윈도잉 파라미터가 config 키로 존재하고 env override 가능하다"
    - "Anthropic SDK 를 스트리밍/tool_use 형태로 mock 하는 공용 테스트 픽스처가 존재한다"
  artifacts:
    - path: "packages/shared/src/chat.ts"
      provides: "SpecialistId, ChatSSEEventMap, ConversationRow, MessageRow, MessageBlock, ChatRole"
      exports: ["SpecialistId", "SPECIALIST_LABELS", "ChatSSEEventMap"]
    - path: "server/src/config.ts"
      provides: "chatEnabled, chatLeadModel, chatSpecialistModel, chatWebSearchModel, chatMaxToolRounds, chatMaxHistoryMessages"
      contains: "chatLeadModel"
    - path: "server/src/services/__tests__/anthropic-mock.ts"
      provides: "makeStreamMock / makeToolUseFinalMessage 픽스처 유틸"
  key_links:
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/chat.ts"
      via: "re-export"
      pattern: "from \"./chat\""
---

<objective>
서버와 웹앱이 공유하는 SSE 이벤트 계약·전문가 ID 를 `packages/shared/src/chat.ts` 에 정의하고, 챗 모델/윈도잉/kill-switch config 키를 추가하며, 다운스트림 서버 테스트가 재사용할 Anthropic SDK mock 픽스처를 만든다.

Purpose: 인터페이스-우선 — 이후 서버(P03~P06)와 웹앱(P07~P10)이 이 계약에 대해 병렬 구현. SSEEventMap 은 SSE 프로토콜의 단일 진실 소스(RESEARCH Pattern 6).
Output: 공유 타입 1파일 + config 확장 + mock 픽스처.

모델 결정 (Claude's Discretion, RESEARCH A1/A2 해소):
- `chatLeadModel` default = `claude-sonnet-4-6` (CONTEXT "Sonnet" — discussion-classify 세대 정합, $3/$15). env `CHAT_LEAD_MODEL` override.
- `chatSpecialistModel` default = `claude-haiku-4-5` (CONTEXT "전문가 Haiku"). env `CHAT_SPECIALIST_MODEL`.
- `chatWebSearchModel` default = `claude-haiku-4-5` (CONTEXT "전문가 Haiku" 문자 그대로 존중). RESEARCH Pitfall 1/A2 — Haiku 가 web_search 미지원일 수 있음 → P11 POC 게이트에서 실측, 실패 시 env `CHAT_WEBSEARCH_MODEL=claude-sonnet-4-6` 으로 1줄 폴백. 별도 키로 분리해 두는 이유가 이것.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md
@.planning/phases/14-ai-analyst-chatbot/14-CONTEXT.md

<interfaces>
From packages/shared/src/index.ts (기존 re-export 패턴 — 여기 추가):
```typescript
export type { NewsArticle } from "./news";
export type { Discussion, DiscussionListResponse } from "./discussion";
```
From server/src/config.ts (기존 챗 관련 키 — 재사용):
```typescript
anthropicApiKey: process.env.ANTHROPIC_API_KEY ?? null,
classifyModel: process.env.DISCUSSION_CLASSIFY_MODEL ?? "claude-haiku-4-5",
classifyEnabled: (process.env.DISCUSSION_CLASSIFY_ENABLED ?? "true") !== "false",
```
From RESEARCH Pattern 6 (ChatSSEEventMap 권장 — verbatim 채택):
```typescript
interface ChatSSEEventMap {
  session: { conversationId: string };
  text: { text: string };
  text_clear: Record<string, never>;
  agent_start: { agent: SpecialistId; label: string };
  agent_end: { agent: SpecialistId };
  stock_card: { code: string; name: string; price: number; changeRate: number };
  chart: { code: string };
  citation: { title: string; source?: string; url: string; kind: "news" | "web" };
  response_complete: Record<string, never>;
  error: { error?: string; message?: string };
  done: Record<string, never>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: packages/shared/src/chat.ts 공유 계약 작성 + index re-export</name>
  <files>packages/shared/src/chat.ts, packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/index.ts (re-export 패턴 — 기존 export 문 스타일 그대로 따를 것)
    - packages/shared/src/home.ts (타입 파일 주석/구조 스타일 참고)
    - .planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md (Pattern 6 ChatSSEEventMap, Pattern 4 스키마)
  </read_first>
  <action>
    `packages/shared/src/chat.ts` 생성. 아래 정의를 정확히 포함:

    ```typescript
    export type SpecialistId = "quote" | "theme" | "news" | "limitup" | "websearch";

    /** 팀장 tool 이름 → SpecialistId 매핑용 tool 이름 상수. */
    export const SPECIALIST_TOOL_NAMES: Record<SpecialistId, string> = {
      quote: "consult_quote_specialist",
      theme: "consult_theme_specialist",
      news: "consult_news_specialist",
      limitup: "consult_limitup_specialist",
      websearch: "consult_websearch_specialist",
    };

    /** 진행 스텝퍼(D-04/C5) 한글 라벨. */
    export const SPECIALIST_LABELS: Record<SpecialistId, string> = {
      quote: "시세·수급 전문가",
      theme: "테마 전문가",
      news: "뉴스·심리 전문가",
      limitup: "상한가 패턴 전문가",
      websearch: "실시간 검색 전문가",
    };

    export type ChatRole = "user" | "assistant";

    /** messages.blocks jsonb 부가물 (D-07/08/10). content 텍스트와 분리. */
    export type MessageBlock =
      | { type: "stock_card"; code: string; name: string; price: number; changeRate: number }
      | { type: "chart"; code: string }
      | { type: "citation"; title: string; source?: string; url: string; kind: "news" | "web" };

    export interface ConversationRow {
      id: string;
      userId: string;
      stockCode: string | null;
      title: string | null;
      createdAt: string;
      updatedAt: string;
    }

    export interface MessageRow {
      id: string;
      conversationId: string;
      role: ChatRole;
      content: string;
      blocks: MessageBlock[] | null;
      createdAt: string;
    }

    export interface ChatSSEEventMap {
      session: { conversationId: string };
      text: { text: string };
      text_clear: Record<string, never>;
      agent_start: { agent: SpecialistId; label: string };
      agent_end: { agent: SpecialistId };
      stock_card: { code: string; name: string; price: number; changeRate: number };
      chart: { code: string };
      citation: { title: string; source?: string; url: string; kind: "news" | "web" };
      response_complete: Record<string, never>;
      error: { error?: string; message?: string };
      done: Record<string, never>;
    }

    export type ChatSSEEventType = keyof ChatSSEEventMap;
    ```

    `packages/shared/src/index.ts` 에 re-export 추가 (기존 스타일):
    ```typescript
    export type { SpecialistId, ChatRole, MessageBlock, ConversationRow, MessageRow, ChatSSEEventMap, ChatSSEEventType } from "./chat";
    export { SPECIALIST_TOOL_NAMES, SPECIALIST_LABELS } from "./chat";
    ```
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/shared build 2>/dev/null || pnpm --filter @gh-radar/shared exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "consult_websearch_specialist" packages/shared/src/chat.ts` 매칭 (5 tool 이름)
    - `grep "실시간 검색 전문가" packages/shared/src/chat.ts` 매칭 (5 한글 라벨 — UI-SPEC Copywriting 일치)
    - `grep "from \"./chat\"" packages/shared/src/index.ts` 매칭
    - typecheck exit 0
  </acceptance_criteria>
  <done>chat.ts 존재 + index 재수출 + shared 패키지 typecheck 통과.</done>
</task>

<task type="auto">
  <name>Task 2: server config 챗 키 추가</name>
  <files>server/src/config.ts</files>
  <read_first>
    - server/src/config.ts (AppConfig 타입 + loadConfig 반환 — 기존 classifyModel/anthropicApiKey/classifyEnabled 패턴 mirror)
  </read_first>
  <action>
    `AppConfig` 타입에 아래 필드 추가:
    ```typescript
    // Phase 14 — AI 애널리스트 챗봇 (D-11/D-12, RESEARCH A1/A2)
    chatEnabled: boolean;          // CHAT_DISABLED kill-switch (ww-bot 패턴)
    chatLeadModel: string;         // 팀장 Sonnet
    chatSpecialistModel: string;   // 전문가 Haiku
    chatWebSearchModel: string;    // 웹서치 — Haiku 미지원 시 Sonnet 폴백 (별도 키)
    chatMaxToolRounds: number;     // 팀장 tool-use 루프 상한
    chatMaxHistoryMessages: number;// pruneHistory 슬라이딩 윈도우
    ```
    `loadConfig()` 반환 객체에 추가 (기존 `anthropicApiKey`/`classifyModel` 아래):
    ```typescript
    chatEnabled: (process.env.CHAT_DISABLED ?? "false") !== "true",
    chatLeadModel: process.env.CHAT_LEAD_MODEL ?? "claude-sonnet-4-6",
    chatSpecialistModel: process.env.CHAT_SPECIALIST_MODEL ?? "claude-haiku-4-5",
    chatWebSearchModel: process.env.CHAT_WEBSEARCH_MODEL ?? "claude-haiku-4-5",
    chatMaxToolRounds: Number(process.env.CHAT_MAX_TOOL_ROUNDS ?? "5"),
    chatMaxHistoryMessages: Number(process.env.CHAT_MAX_HISTORY_MESSAGES ?? "30"),
    ```
    주석에 "chatWebSearchModel 기본 Haiku — P11 POC 에서 web_search 미지원 확인 시 CHAT_WEBSEARCH_MODEL=claude-sonnet-4-6 env 폴백 (RESEARCH A2)" 명시.
    기존 `anthropicApiKey` 키는 재사용(신규 생성 금지) — 챗도 동일 키 사용.
  </action>
  <verify>
    <automated>pnpm --filter server exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "chatLeadModel\|chatSpecialistModel\|chatWebSearchModel\|chatEnabled\|chatMaxToolRounds\|chatMaxHistoryMessages" server/src/config.ts` >= 12 (타입 6 + loadConfig 6)
    - `grep "CHAT_DISABLED" server/src/config.ts` 매칭
    - `grep "claude-sonnet-4-6" server/src/config.ts` 매칭 (chatLeadModel default)
    - server typecheck exit 0
  </acceptance_criteria>
  <done>6 챗 config 키가 AppConfig 타입 + loadConfig 양쪽에 존재, env override 가능, typecheck 통과.</done>
</task>

<task type="auto">
  <name>Task 3: Anthropic SDK mock 공용 픽스처</name>
  <files>server/src/services/__tests__/anthropic-mock.ts</files>
  <read_first>
    - server/src/services/discussion-classify.ts (`__resetAnthropicClientForTests` + SDK mock 접근 선례)
    - .planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md (Code Examples — stream/finalMessage/tool_use 형태)
  </read_first>
  <action>
    다운스트림 서버 테스트(P05 orchestrator, P06 chat-service)가 재사용할 순수 픽스처 유틸을 만든다. Anthropic SDK 의 `messages.stream()` 반환(async iterable + `.finalMessage()`)과 `messages.create()` 반환을 흉내내는 팩토리. 실제 네트워크 호출 없음.

    export 함수:
    - `makeStreamMock(opts: { textDeltas: string[]; finalMessage: Anthropic.Message })` → `{ [Symbol.asyncIterator], finalMessage: () => Promise<Anthropic.Message> }` 형태. `content_block_delta`(text_delta) 이벤트를 textDeltas 로 방출 후 finalMessage 반환.
    - `makeToolUseFinalMessage(calls: Array<{ id: string; name: string; input: unknown }>)` → `stop_reason: "tool_use"` + content 에 `tool_use` 블록 배열.
    - `makeEndTurnFinalMessage(text: string)` → `stop_reason: "end_turn"` + content 에 text 블록.
    - `makeCreateResponse(text: string)` → 전문가 Haiku `messages.create()` 반환 형태 (content text 블록 1개).

    타입은 `@anthropic-ai/sdk` 의 `Anthropic.Message` / `Anthropic.MessageParam` 사용. 이 파일은 테스트 헬퍼이므로 vitest 가 실행하는 test 파일이 아님(`.test.ts` 아님) — describe/it 없음.
  </action>
  <verify>
    <automated>pnpm --filter server exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function make" server/src/services/__tests__/anthropic-mock.ts` >= 4
    - `grep "finalMessage" server/src/services/__tests__/anthropic-mock.ts` 매칭
    - `grep "stop_reason" server/src/services/__tests__/anthropic-mock.ts` 매칭 (tool_use + end_turn)
    - `grep -c "describe\|it(" server/src/services/__tests__/anthropic-mock.ts` == 0 (테스트 아님, 헬퍼)
    - server typecheck exit 0
  </acceptance_criteria>
  <done>4개 mock 팩토리 export, describe/it 없음, typecheck 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → config | 운영자가 모델 ID/kill-switch 를 env 로 주입 (신뢰) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04a | DoS(비용) | chatEnabled kill-switch | mitigate | `CHAT_DISABLED=true` 로 챗 전체 즉시 차단 (ww-bot 패턴). 남용/비용급증 시 운영 정지 수단 |
| T-14-14 | (설정 오류) | chatWebSearchModel 분리 | mitigate | 웹서치 모델을 별도 키로 분리 — Haiku web_search 미지원 시 코드 변경 없이 env 폴백 (RESEARCH A2) |
</threat_model>

<verification>
- shared 패키지 typecheck (SSE 계약 컴파일)
- server typecheck (config 확장)
- mock 픽스처 typecheck
</verification>

<success_criteria>
- ChatSSEEventMap + SpecialistId + 라벨/tool 이름 매핑이 shared 에서 export
- 6 챗 config 키 존재 + env override
- Anthropic SDK mock 픽스처 4팩토리 존재
</success_criteria>

<output>
After completion, create `.planning/phases/14-ai-analyst-chatbot/14-02-SUMMARY.md`
</output>
