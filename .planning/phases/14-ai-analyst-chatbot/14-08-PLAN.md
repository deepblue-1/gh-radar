---
phase: 14-ai-analyst-chatbot
plan: 08
type: execute
wave: 3
depends_on: [07]
files_modified:
  - webapp/src/components/chat/chat-fab.tsx
  - webapp/src/components/chat/chat-sheet.tsx
  - webapp/src/components/chat/composer.tsx
  - webapp/src/components/chat/chat-states.tsx
  - webapp/src/components/chat/__tests__/chat-fab.test.tsx
  - webapp/src/components/chat/__tests__/chat-sheet.test.tsx
  - webapp/src/components/stock/stock-detail-client.tsx
  - webapp/src/app/layout.tsx
  - webapp/src/components/layout/app-sidebar.tsx
autonomous: true
requirements: [CHAT-01]
must_haves:
  truths:
    - "전역 FAB 이 모든 페이지 우하단에 뜨고, 비로그인 클릭 시 로그인 유도 상태가 표시된다"
    - "종목상세(/stocks/[code])에서 FAB 라벨이 provider 의 stockContext(종목명)를 반영해 `AI 애널리스트 · {종목명} 분석` 으로 표시된다"
    - "종목 컨텍스트로 챗 시트를 열면 해당 종목의 최근 대화가 자동 로드되고(D-03), 없으면 빈 상태다. `＋ 새 대화` 로 초기화된다"
    - "챗 시트가 열리고 composer 로 메시지를 보내면 SSE 스트리밍 텍스트가 시트에 append 된다"
    - "스트리밍 중 정지 버튼이 뜨고, 새 메시지 전송 시 이전 응답이 abort 된다"
    - "사이드바에 /chat 메뉴가 추가된다"
  artifacts:
    - path: "webapp/src/components/chat/chat-fab.tsx"
      provides: "전역 FAB — 로그인 게이트 + 종목 컨텍스트(usePathname)"
      exports: ["ChatFab"]
    - path: "webapp/src/components/chat/chat-sheet.tsx"
      provides: "챗 시트 — 헤더/thread/composer + SSE 스트리밍"
      exports: ["ChatSheet"]
    - path: "webapp/src/components/chat/composer.tsx"
      provides: "textarea + 전송/정지 토글 (Enter 전송)"
  key_links:
    - from: "chat-fab / chat-sheet"
      to: "useChat (P07 provider)"
      via: "전역 상태"
      pattern: "useChat"
    - from: "composer 전송"
      to: "streamChat (P07)"
      via: "SSE 요청"
      pattern: "streamChat"
    - from: "layout.tsx"
      to: "ChatProvider + ChatFab + ChatSheet"
      via: "전역 마운트"
      pattern: "ChatProvider"
---

<objective>
챗 UI 쉘을 구성한다: 전역 FAB(C1, 로그인 게이트 D-01 + 종목 컨텍스트 D-03) + 챗 시트(C2, shadcn Sheet right/bottom) + composer(C9, 전송/정지 토글 D-06) + 상태 박스(C11, 로그인필요/빈/에러). layout.tsx 전역 마운트 + 사이드바 /chat nav.

Purpose: 챗 진입점과 스트리밍 셸. 메시지 렌더링 세부(마크다운/진행스텝퍼/카드)는 P09 가 채움 — 이 plan 은 텍스트 append 수준의 최소 thread + composer/SSE 배선까지.
Output: FAB + 시트 + composer + 상태 + layout/nav 배선 + FAB 컴포넌트 테스트. 채택 목업 `14-chat-mockup.html` 기준.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ai-analyst-chatbot/14-UI-SPEC.md
@.planning/phases/14-ai-analyst-chatbot/14-CONTEXT.md
@.planning/phases/14-ai-analyst-chatbot/14-chat-mockup.html

<interfaces>
From webapp/src/components/chat/chat-provider.tsx (P07): `useChat()` → `{open, openChat, closeChat, stockContext, setStockContext}` (stockContext = `{code,name}|null`, 종목명은 이 채널로 공급 — usePathname 은 code 만)
From webapp/src/lib/chat-api.ts (P07): `listConversations(stockCode?)` → `ConversationRow[]`, `getConversation(id)` → `{conversation, messages}` (D-03 자동 이어가기 데이터 소스)
From webapp/src/lib/chat-sse.ts (P07): `streamChat(params, onEvent, signal)`
From webapp/src/components/stock/stock-detail-client.tsx (기존, P08 이 setStockContext 발행 배선 추가): `stock.name` 보유
From webapp/src/lib/auth-context.tsx: `useAuth()` → 로그인 상태
From webapp/src/components/ui/sheet.tsx / textarea.tsx / button.tsx (기존 shadcn — 신규 생성 금지)
From webapp/src/app/layout.tsx (현재 provider 트리 — ChatProvider 추가 + FAB/Sheet 마운트):
```tsx
<ThemeProvider><AuthProvider><WatchlistSetProvider>{children}</WatchlistSetProvider></AuthProvider></ThemeProvider>
```
From webapp/src/components/layout/app-sidebar.tsx (NAV 배열):
```typescript
const NAV = [
  { href: "/", label: "홈", icon: Home },
  { href: "/scanner", label: "스캐너", icon: Activity },
  { href: "/themes", label: "테마", icon: Layers },
  { href: "/watchlist", label: "관심종목", icon: Star },
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChatFab (로그인 게이트 + 종목 컨텍스트) + 컴포넌트 테스트</name>
  <files>webapp/src/components/chat/chat-fab.tsx, webapp/src/components/chat/chat-states.tsx, webapp/src/components/stock/stock-detail-client.tsx, webapp/src/components/chat/__tests__/chat-fab.test.tsx</files>
  <read_first>
    - webapp/src/components/chat/chat-provider.tsx (P07 — useChat + setStockContext)
    - webapp/src/components/stock/stock-detail-client.tsx (기존 — stock.name 보유, setStockContext 발행 지점)
    - webapp/src/lib/auth-context.tsx (useAuth 로그인 상태)
    - .planning/phases/14-ai-analyst-chatbot/14-UI-SPEC.md (C1 FAB / C11 상태박스 / Copywriting / Color — --primary FAB, 56px, 9999px)
    - .planning/phases/14-ai-analyst-chatbot/14-chat-mockup.html (FAB 시각 계약)
  </read_first>
  <behavior>
    - Test 1: 비로그인(useAuth 미인증) + FAB 클릭 → 로그인 필요 상태 표시(체험 모드 없음, D-01). streamChat 미호출
    - Test 2: 로그인 상태 + FAB 클릭 → openChat 호출(시트 open)
    - Test 3: `useChat().stockContext = {code:"005930", name:"삼성전자"}` (provider mock) → FAB aria-label `AI 애널리스트 · 삼성전자 분석`, 클릭 시 openChat 에 해당 컨텍스트 전달
    - Test 4: `stockContext = null` → 라벨 `AI 애널리스트`, 컨텍스트 없음
  </behavior>
  <action>
    - `ChatFab` — `'use client'`. 우하단 `fixed right-6 bottom-6`, 56px, `--primary` 배경, `9999px`, lucide `MessageSquare`. z-index 는 Sheet scrim 아래(UI-SPEC C1). aria-label `AI 애널리스트`(종목상세: `AI 애널리스트 · {종목명} 분석`).
    - **종목명 데이터 출처 결정(Warning 해소):** FAB 라벨의 `{종목명}` 은 `useChat().stockContext.name` 에서 읽는다. usePathname 은 code 만 제공하므로 이름은 provider 채널로 공급 — 종목상세 페이지(`stock-detail-client.tsx`)가 `stock.name` 로드 후 `setStockContext({code, name})` 발행, 언마운트 시 `setStockContext(null)`. 이렇게 하면 이미 fetch 한 stock 데이터를 재사용(추가 조회 없음). FAB 는 stockContext 있으면 `AI 애널리스트 · {name} 분석`, 없으면 `AI 애널리스트`. (전역 종목명 context 신설 대신 기존 provider 확장 + 기존 stock fetch 재사용 = 최소 변경.)
    - **stock-detail-client.tsx 배선:** 기존 `stock` state 로드 완료 시 `useChat().setStockContext({code, name: stock.name})`, 컴포넌트 언마운트 cleanup 에서 `setStockContext(null)`. 기존 로직/렌더 불변 — useEffect 1개 추가만.
    - 클릭 시: `useAuth` 미인증 → 로그인 필요 상태 박스(C11) 표시(모달/시트 내 상태). 인증 → `openChat({code, name})`.
    - `chat-states.tsx` — 상태 박스 3종(빈/로그인필요/에러) export. Copywriting verbatim: 로그인 필요 heading `로그인이 필요해요` / body `AI 애널리스트는 로그인 후 이용할 수 있어요. 대화 기록은 계정에 안전하게 저장됩니다.` + `Google로 로그인` 버튼. 빈 상태 `무엇이든 물어보세요` + 예시 프롬프트 칩. 에러 `답변을 불러오지 못했어요` + `다시 시도`.

    테스트: usePathname/useAuth/useChat mock. Behavior 4케이스.
  </action>
  <verify>
    <automated>pnpm --filter webapp test chat-fab</automated>
  </verify>
  <acceptance_criteria>
    - `grep "stockContext" webapp/src/components/chat/chat-fab.tsx` 매칭 (종목명 라벨 소스 — provider)
    - `grep "setStockContext" webapp/src/components/stock/stock-detail-client.tsx` 매칭 (종목명 발행 배선)
    - `grep "useAuth" webapp/src/components/chat/chat-fab.tsx` 매칭 (로그인 게이트)
    - `grep "로그인이 필요해요" webapp/src/components/chat/chat-states.tsx` 매칭 (Copywriting verbatim)
    - `grep "AI 애널리스트" webapp/src/components/chat/chat-fab.tsx` 매칭
    - `pnpm --filter webapp test chat-fab` — 4 tests pass
  </acceptance_criteria>
  <done>ChatFab 로그인 게이트 + 종목명 라벨(provider stockContext) + stock-detail-client 발행 배선 + 상태 박스 3종, 4 컴포넌트 테스트 green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ChatSheet + composer (D-03 자동 이어가기 + SSE 스트리밍 + 정지/abort)</name>
  <files>webapp/src/components/chat/chat-sheet.tsx, webapp/src/components/chat/composer.tsx, webapp/src/components/chat/__tests__/chat-sheet.test.tsx</files>
  <read_first>
    - webapp/src/components/ui/sheet.tsx (side right/bottom + 애니메이션 220/160ms)
    - webapp/src/components/ui/textarea.tsx + button.tsx
    - webapp/src/lib/chat-sse.ts (P07 — streamChat/onEvent/signal)
    - webapp/src/lib/chat-api.ts (P07 — listConversations/getConversation, D-03 자동 이어가기 데이터)
    - webapp/src/components/chat/chat-provider.tsx (P07 — useChat + stockContext)
    - .planning/phases/14-ai-analyst-chatbot/14-CONTEXT.md (D-03 종목상세 챗 = 최근 대화 자동 이어가기 + 새 대화)
    - .planning/phases/14-ai-analyst-chatbot/14-UI-SPEC.md (라인 143 종목 컨텍스트: 최근 대화 자동 이어가기, 없으면 새 대화, ＋새 대화로 초기화)
    - .planning/phases/14-ai-analyst-chatbot/14-UI-SPEC.md (C2 시트 440px/85dvh, C9 composer auto-grow 40→120 + 전송/정지 + 면책 + Enter/Shift+Enter)
    - .planning/phases/14-ai-analyst-chatbot/14-chat-mockup.html
  </read_first>
  <behavior>
    - Test 1: 종목 컨텍스트로 시트 open(`stockContext={code:"005930",name:"삼성전자"}`) → `listConversations("005930")` 호출 → 반환 최신 1건의 id 로 `getConversation(id)` 호출 → 해당 messages 가 thread 에 프리로드 + conversationId 세팅 (D-03 자동 이어가기)
    - Test 2: 종목 컨텍스트로 open 했으나 `listConversations` 가 빈 배열 → getConversation 미호출, thread 빈 상태, conversationId=null (새 대화)
    - Test 3: `＋ 새 대화` 클릭 → messages 초기화 + conversationId=null (stockContext 는 유지 — 같은 종목 새 대화)
    - Test 4: 일반(비종목) 컨텍스트로 open(`stockContext=null`) → listConversations 미호출(자동 로드 안 함), 빈 상태
  </behavior>
  <action>
    - `ChatSheet` — `'use client'`. shadcn Sheet(desktop side=right 440px / mobile bottom 85dvh, UI-SPEC 반응형). `useChat().open` 으로 제어. 헤더(AI 아바타 + "팀장 애널리스트" 제목 + 종목 서브 + `＋ 새 대화` + 닫기). thread 스크롤 영역 + composer. 현재 대화만(목록 없음, D-13).
    - **D-03 자동 이어가기(BLOCKER 해소):** 시트가 `open===true` 로 전환되고 `useChat().stockContext` 가 존재하면 useEffect 에서 `listConversations(stockContext.code)` 호출 → 결과가 있으면 최신(첫) 대화의 `getConversation(id)` 로 messages 프리로드 + `conversationId` 세팅, 없으면 빈 상태(새 대화). stockContext 가 null(일반 챗)이면 자동 로드 안 함(빈 상태로 새 대화 시작 — 목록/이어가기는 /chat P10 담당, D-13). 로드 중복 방지: 같은 (open,code) 조합 1회만(가드 ref 또는 deps). 로드 실패 시 에러 상태 박스(C11) 표시.
    - **`＋ 새 대화` 버튼:** messages=[] + conversationId=null 로 초기화(stockContext 는 유지 — 같은 종목으로 새 대화). 진행 중 스트리밍이 있으면 abort.
    - 스트리밍 상태 관리: `messages` state(user/assistant) + `streamingText` + `abortRef`(AbortController). 전송 시:
      - `abortRef.current?.abort()` (새 질문 시 이전 자동 abort, D-06) → 새 AbortController.
      - user 메시지 append → `streamChat({message, conversationId, stockCode}, onEvent, controller.signal)`.
      - onEvent switch: `session`→conversationId 저장, `text`→streamingText append(레이아웃 shift 최소, Phase 13 교훈), `text_clear`→리셋, `agent_start/agent_end/stock_card/citation/chart`→P09 렌더 훅에 위임(이 plan 은 최소 placeholder, P09 확장), `response_complete`→assistant 확정, `error`→에러 상태, `done`→종료.
      - **시트를 닫아도(closeChat) 진행 중 fetch 는 abort 하지 않음**(서버가 완료 후 저장, D-06) — closeChat 은 open=false 만, abort 는 새 전송/명시 정지 버튼만.
    - `composer` — textarea auto-grow(40→120px, UI-SPEC C9) + 전송 버튼(`--primary`, paper-plane, aria-label `전송`). 스트리밍 중 = 정지 버튼(square, outline, aria-label `중단`)로 전환 → 클릭 시 abortRef.abort. Enter 전송 / Shift+Enter 줄바꿈. 하단 면책 문구 `AI 답변은 참고용이며 투자자문이 아닙니다`(상시, UI-SPEC). placeholder `상한가 종목·주도 테마·익절 판단을 물어보세요…`.
    - 접근성: 진행표시 `aria-live="polite"`, 스트리밍 텍스트 영역 `aria-busy` 토글, Sheet Radix 포커스트랩/ESC 상속.
  </action>
  <verify>
    <automated>pnpm --filter webapp test chat-sheet &amp;&amp; pnpm --filter webapp exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "listConversations" webapp/src/components/chat/chat-sheet.tsx` 매칭 (D-03 자동 이어가기)
    - `grep "getConversation" webapp/src/components/chat/chat-sheet.tsx` 매칭 (최근 대화 메시지 프리로드)
    - `grep "streamChat" webapp/src/components/chat/chat-sheet.tsx` 매칭 (SSE 배선)
    - `grep "abort" webapp/src/components/chat/chat-sheet.tsx` 매칭 (D-06 새 질문 자동 중단)
    - `grep "투자자문이 아닙니다" webapp/src/components/chat/composer.tsx` 매칭 (면책 상시)
    - `grep "aria-label" webapp/src/components/chat/composer.tsx` 매칭 (전송/중단)
    - `grep "＋ 새 대화\|새 대화" webapp/src/components/chat/chat-sheet.tsx` 매칭
    - `pnpm --filter webapp test chat-sheet` — 4 tests pass (자동 이어가기/빈/새 대화/일반)
    - webapp typecheck exit 0 + 기존 테스트 green
  </acceptance_criteria>
  <done>ChatSheet D-03 자동 이어가기 + SSE 스트리밍 + composer 전송/정지 토글 + 면책 + 새 대화 초기화, chat-sheet 4테스트 + typecheck 통과.</done>
</task>

<task type="auto">
  <name>Task 3: layout.tsx 전역 마운트 + 사이드바 /chat nav</name>
  <files>webapp/src/app/layout.tsx, webapp/src/components/layout/app-sidebar.tsx</files>
  <read_first>
    - webapp/src/app/layout.tsx (provider 트리 — ChatProvider 추가 위치)
    - webapp/src/components/layout/app-sidebar.tsx (NAV 배열 + lucide 아이콘 import)
    - webapp/src/components/chat/chat-provider.tsx / chat-fab.tsx / chat-sheet.tsx (P07/이 plan)
  </read_first>
  <action>
    - layout.tsx: provider 트리에 `ChatProvider` 추가(WatchlistSetProvider 안쪽 또는 바깥 — useChat 이 useAuth 접근 필요하므로 AuthProvider 안쪽). `{children}` 뒤에 `<ChatFab />` + `<ChatSheet />` 전역 마운트(모든 페이지에서 접근). server component 인 layout 이 client 컴포넌트를 마운트하는 것은 정상.
    - app-sidebar.tsx: NAV 배열에 `{ href: "/chat", label: "AI 애널리스트", icon: MessageSquare }` 추가(lucide MessageSquare import). 위치는 홈/스캐너/테마/관심종목 흐름에 맞게(관심종목 아래 또는 테마 위 — UI 판단). **라벨 확정(Warning 해소):** 사이드바 메뉴명 = `AI 애널리스트` (FAB primary CTA / UI-SPEC Copywriting 과 통일). ROADMAP 초안의 "AI챗봇" 서술은 폐기 — 전 표면 라벨을 `AI 애널리스트` 로 단일화.
  </action>
  <verify>
    <automated>pnpm --filter webapp exec tsc --noEmit &amp;&amp; pnpm --filter webapp build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ChatProvider" webapp/src/app/layout.tsx` 매칭
    - `grep "ChatFab\|ChatSheet" webapp/src/app/layout.tsx` 매칭 (전역 마운트)
    - `grep "/chat" webapp/src/components/layout/app-sidebar.tsx` 매칭 (nav)
    - `grep "AI 애널리스트" webapp/src/components/layout/app-sidebar.tsx` 매칭 (한글 라벨)
    - webapp typecheck + build exit 0
  </acceptance_criteria>
  <done>ChatProvider/FAB/Sheet 전역 마운트 + /chat nav, build 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 UI → 챗 API | 비로그인 사용자의 FAB 접근 시도 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02b | Spoofing (클라 게이트 우회) | ChatFab 로그인 게이트 | accept | 클라 게이트는 UX — 실제 방어는 서버 requireAuth(P03/P06). 비로그인 요청은 서버 401 |
| T-14-09 | (UX 안정성) 스트리밍 abort 경합 | ChatSheet abortRef | mitigate | 새 전송 시 이전 abort(D-06). 시트 닫힘은 abort 안 함(서버 완료 저장) — 명확 분리 |
</threat_model>

<verification>
- chat-fab 4 컴포넌트 테스트
- webapp typecheck + build (시트/composer/layout/nav)
</verification>

<success_criteria>
- FAB 전역 + 로그인 게이트 + 종목 컨텍스트
- 시트 SSE 스트리밍 + composer 전송/정지 + 면책
- layout 마운트 + /chat nav
</success_criteria>

<output>
After completion, create `.planning/phases/14-ai-analyst-chatbot/14-08-SUMMARY.md`
</output>
