---
phase: 14-ai-analyst-chatbot
plan: 09
type: execute
wave: 4
depends_on: [08]
files_modified:
  - webapp/src/components/chat/message-assistant.tsx
  - webapp/src/components/chat/message-user.tsx
  - webapp/src/components/chat/agent-progress.tsx
  - webapp/src/components/chat/mini-stock-card.tsx
  - webapp/src/components/chat/citation.tsx
  - webapp/src/components/chat/mini-chart.tsx
  - webapp/src/components/chat/chat-thread.tsx
  - webapp/src/components/chat/chat-sheet.tsx
  - webapp/src/components/chat/__tests__/message-render.test.tsx
autonomous: true
requirements: [CHAT-01]
must_haves:
  truths:
    - "assistant 답변이 풀 마크다운(표/리스트/헤딩/강조/코드)으로 렌더된다"
    - "스트리밍 중 전문가별 진행 스텝퍼(done✓/active●/wait○)가 표시된다"
    - "답변 내 종목 언급이 미니 종목카드(국내색상)로, 근거가 출처 인용 블록으로 렌더된다"
    - "가격흐름 설명 시 기존 lightweight-charts 미니 일봉차트가 삽입된다"
  artifacts:
    - path: "webapp/src/components/chat/message-assistant.tsx"
      provides: "react-markdown + remark-gfm 답변 렌더 + blocks(카드/차트/citation)"
      exports: ["MessageAssistant"]
    - path: "webapp/src/components/chat/agent-progress.tsx"
      provides: "진행 스텝퍼 (Variant B, 5 전문가)"
      exports: ["AgentProgress"]
    - path: "webapp/src/components/chat/mini-stock-card.tsx"
      provides: "인라인 종목카드 → /stocks/[code] (국내색상 up/down)"
  key_links:
    - from: "mini-chart.tsx"
      to: "Phase 09.2 lightweight-charts + chart-colors.ts"
      via: "재사용 (oklch 직접주입 금지)"
      pattern: "chart-colors"
    - from: "agent-progress.tsx"
      to: "SPECIALIST_LABELS (shared)"
      via: "전문가 라벨"
      pattern: "SPECIALIST_LABELS"
---

<objective>
챗 답변 렌더링 세부를 구현한다: assistant 마크다운(C4, D-09) + user 버블(C3) + 진행 스텝퍼(C5, D-04 Variant B) + 미니 종목카드(C6, D-07 국내색상) + 출처 인용(C7, D-08) + 미니 일봉차트(C8, D-10 lightweight-charts 재사용) + thread 컨테이너. P08 시트의 최소 append 를 이 컴포넌트들로 교체.

Purpose: 답변 콘텐츠 표현 계약(D-07/08/09/10) 충족. SSE 이벤트(agent_start/agent_end/stock_card/citation/chart)를 시각 요소로 매핑.
Output: 6 렌더 컴포넌트 + thread + 렌더 유닛테스트. 채택 목업 기준.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ai-analyst-chatbot/14-UI-SPEC.md
@.planning/phases/14-ai-analyst-chatbot/14-CONTEXT.md
@.planning/phases/14-ai-analyst-chatbot/14-chat-mockup.html

<interfaces>
From packages/shared (P02): `SpecialistId`, `SPECIALIST_LABELS`, `MessageBlock`, `ChatSSEEventMap`
From webapp/src/lib/chart-colors.ts (Phase 09.2): oklch → hex/rgb 변환 (미니차트 색상 주입)
From webapp/src/components/chart/* (Phase 09.2 StockDailyChart — 미니차트 재사용 원본, lightweight-charts 5.2.0)
From webapp/src/components/chat/chat-sheet.tsx (P08 — onEvent 훅 위치, 이 plan 이 렌더 컴포넌트로 채움)
UI-SPEC 색상: 상승=빨강(`--up`/`--up-bg`), 하락=파랑(`--down`/`--down-bg`) — 국내 관례 필수(Pitfall 5)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MessageAssistant (마크다운) + MessageUser + chat-thread</name>
  <files>webapp/src/components/chat/message-assistant.tsx, webapp/src/components/chat/message-user.tsx, webapp/src/components/chat/chat-thread.tsx, webapp/src/components/chat/__tests__/message-render.test.tsx</files>
  <read_first>
    - .planning/phases/14-ai-analyst-chatbot/14-UI-SPEC.md (C3 user 버블 radius 12 12 4 12, C4 assistant full-width + 아바타 + "팀장 애널리스트", Typography 14px/1.6)
    - .planning/phases/14-ai-analyst-chatbot/14-chat-mockup.html
    - webapp/src/components/chat/chat-sheet.tsx (P08 — messages state 형태)
  </read_first>
  <behavior>
    - Test 1: MessageAssistant — 마크다운 표(`| a | b |`)가 `<table>` 로 렌더(remark-gfm)
    - Test 2: MessageAssistant — 헤딩/리스트/강조 렌더 + 답변 말미 축약 면책 `※ ... 투자자문이 아닙니다.`
    - Test 3: MessageUser — 우측 정렬 버블, content 텍스트 표시
    - Test 4: chat-thread — user/assistant 메시지 순서대로 렌더 + 스트리밍 중 마지막 assistant 에 streamingText
  </behavior>
  <action>
    - `MessageAssistant` — `react-markdown` + `remark-gfm`(D-09) 로 content 렌더. 좌측 full-width, AI 아바타(`--primary`) + "팀장 애널리스트" 라벨(caption). 표/리스트/헤딩/강조/코드 지원. XSS 방어는 react-markdown 기본(HTML 비활성). blocks(stock_card/chart/citation) 를 본문 아래/인라인 렌더(각 하위 컴포넌트에 위임). 답변 말미 축약 면책 문구(UI-SPEC).
    - `MessageUser` — 우측 정렬, `--accent` 버블, radius `12px 12px 4px 12px`(UI-SPEC C3), 14px.
    - `chat-thread` — 메시지 배열 map 렌더 + 스트리밍 중 진행 스텝퍼(P09 Task 2) + 자동 스크롤(하단). 레이아웃 shift 최소(append, Phase 13 교훈).

    테스트: @testing-library/react. Behavior 4케이스(표 렌더 확인이 핵심 — remark-gfm 동작 검증).
  </action>
  <verify>
    <automated>pnpm --filter webapp test message-render</automated>
  </verify>
  <acceptance_criteria>
    - `grep "react-markdown" webapp/src/components/chat/message-assistant.tsx` 매칭
    - `grep "remark-gfm\|remarkGfm" webapp/src/components/chat/message-assistant.tsx` 매칭 (표 지원 D-09)
    - `grep "팀장 애널리스트" webapp/src/components/chat/message-assistant.tsx` 매칭
    - `grep "투자자문이 아닙니다" webapp/src/components/chat/message-assistant.tsx` 매칭 (축약 면책)
    - `pnpm --filter webapp test message-render` — 4 tests pass (표 렌더 포함)
  </acceptance_criteria>
  <done>assistant 마크다운(표 포함) + user 버블 + thread, 4 렌더 테스트 green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AgentProgress 스텝퍼 (Variant B) + MiniStockCard + Citation</name>
  <files>webapp/src/components/chat/agent-progress.tsx, webapp/src/components/chat/mini-stock-card.tsx, webapp/src/components/chat/citation.tsx, webapp/src/components/chat/__tests__/message-render.test.tsx</files>
  <read_first>
    - .planning/phases/14-ai-analyst-chatbot/14-UI-SPEC.md (C5 진행 스텝퍼 채택=Variant B done✓/active●/wait○, C6 미니카드, C7 출처인용, Color 국내관례, Copywriting `{전문가} 분석 중…`)
    - packages/shared/src/chat.ts (SPECIALIST_LABELS)
    - .planning/phases/14-ai-analyst-chatbot/14-chat-mockup.html (진행표시 B 변형)
    - webapp/src/components/chat/chat-sheet.tsx (P08 — agent_start/agent_end/stock_card/citation onEvent 상태)
  </read_first>
  <behavior>
    - Test 1: AgentProgress — 진행 상태 map({quote:"done", theme:"active", news:"wait"}) → done✓/active●/wait○ 렌더 + SPECIALIST_LABELS 한글 라벨
    - Test 2: MiniStockCard — 상승(changeRate>0) → `--up` 색상 배지(국내 빨강), 링크 href `/stocks/{code}`
    - Test 3: MiniStockCard — 하락(changeRate<0) → `--down` 색상(파랑)
    - Test 4: Citation — 뉴스(kind:"news") 제목+출처+URL verbatim 링크, 웹(kind:"web") citations 렌더
  </behavior>
  <action>
    - `AgentProgress` — Variant B 세로 스텝퍼(UI-SPEC 채택). props = 전문가별 상태 `Record<SpecialistId,"done"|"active"|"wait">`. done ✓(`--up-bg`), active ●(`--primary`, blink 1.2s), wait ○. 라벨 = `SPECIALIST_LABELS[id]`(한글). `aria-live="polite"`. Copywriting `{전문가} 분석 중…`. agent_start→active, agent_end→done SSE 매핑(P08 이 상태 관리, 이 컴포넌트는 표시). `prefers-reduced-motion` 존중.
    - `MiniStockCard` — `<a href={/stocks/${code}}>`. `--card`+border, 종목명+코드+`.mono` 가격 + 등락 배지. **국내색상**: changeRate>0 `--up`(빨강), <0 `--down`(파랑) (D-07/Pitfall 5). hover `--muted`.
    - `Citation` — `<a>` 좌측 border-left 3px 블록, `--muted` 배경. 뉴스=제목+출처+URL verbatim(D-08), 웹=citations. "근거 뉴스" 라벨 그룹.

    테스트: message-render.test.tsx 확장. Behavior 4케이스(국내색상 up/down 검증이 핵심).
  </action>
  <verify>
    <automated>pnpm --filter webapp test message-render</automated>
  </verify>
  <acceptance_criteria>
    - `grep "SPECIALIST_LABELS" webapp/src/components/chat/agent-progress.tsx` 매칭
    - `grep "분석 중" webapp/src/components/chat/agent-progress.tsx` 매칭 (Copywriting)
    - `grep -- "--up\|--down" webapp/src/components/chat/mini-stock-card.tsx` 매칭 (국내색상)
    - `grep "/stocks/" webapp/src/components/chat/mini-stock-card.tsx` 매칭 (링크)
    - `grep "verbatim\|source\|url" webapp/src/components/chat/citation.tsx` 매칭 (D-08)
    - `pnpm --filter webapp test message-render` — 진행+카드+인용 tests green
  </acceptance_criteria>
  <done>진행 스텝퍼(B) + 국내색상 미니카드 + 출처인용, 렌더 테스트 green.</done>
</task>

<task type="auto">
  <name>Task 3: MiniChart (lightweight-charts 재사용) + 시트 배선 교체</name>
  <files>webapp/src/components/chat/mini-chart.tsx, webapp/src/components/chat/chat-sheet.tsx</files>
  <read_first>
    - webapp/src/components/chart/* (Phase 09.2 StockDailyChart — lightweight-charts 5.2.0 재사용 원본)
    - webapp/src/lib/chart-colors.ts (oklch → hex/rgb 변환 — memory lesson feedback_lightweight_charts_oklch)
    - webapp/src/components/chat/chat-sheet.tsx (P08 — onEvent switch 에 렌더 컴포넌트 연결)
    - .planning/phases/14-ai-analyst-chatbot/14-UI-SPEC.md (C8 미니차트 120px, 한국식 봉색)
  </read_first>
  <action>
    - `MiniChart` — Phase 09.2 lightweight-charts 재사용(신규 차트 코드 금지, D-10). 높이 120px. 데이터는 웹앱이 Supabase 직접조회(chart SSE 는 code 만 트리거, RESEARCH Pattern 6) — 기존 daily-ohlcv-api 재사용. **oklch 직접 주입 금지** → `chart-colors.ts` 변환 후 inject(memory lesson). 국내식 봉색(상승 빨강 `--up`, 하락 파랑 `--down`).
    - chat-sheet.tsx 배선 교체: P08 의 최소 텍스트 append 를 chat-thread + MessageAssistant/MessageUser 로 교체. onEvent switch 를 실제 컴포넌트 상태에 연결:
      - `agent_start`→진행상태[agent]="active", `agent_end`→"done" (AgentProgress)
      - `stock_card`→현재 assistant blocks 에 stock_card push (MiniStockCard)
      - `citation`→blocks 에 citation push (Citation)
      - `chart`→blocks 에 chart push (MiniChart)
      - `text`→streamingText append
    - 완료 시 blocks 포함 메시지 확정(서버가 messages.blocks 로 저장, P06/P01).
  </action>
  <verify>
    <automated>pnpm --filter webapp exec tsc --noEmit &amp;&amp; pnpm --filter webapp build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "chart-colors\|chartColors" webapp/src/components/chat/mini-chart.tsx` 매칭 (oklch 회피 lesson)
    - `grep "lightweight-charts\|createChart\|StockDailyChart" webapp/src/components/chat/mini-chart.tsx` 매칭 (재사용)
    - `grep "MessageAssistant\|chat-thread\|ChatThread" webapp/src/components/chat/chat-sheet.tsx` 매칭 (배선 교체)
    - `grep "agent_start\|stock_card\|citation\|chart" webapp/src/components/chat/chat-sheet.tsx` 매칭 (onEvent 연결)
    - webapp typecheck + build exit 0
  </acceptance_criteria>
  <done>미니차트(lightweight-charts 재사용, oklch 회피) + 시트 렌더 배선 교체, build 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assistant content(LLM) → 마크다운 렌더 | LLM 생성 마크다운을 DOM 렌더 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | Tampering (XSS via 마크다운) | MessageAssistant | mitigate | react-markdown 기본(HTML 비활성, raw HTML 미허용) — 스크립트 주입 차단. 링크는 종목/출처 href 만 |
| T-14-05c | (신뢰성) 국내색상 역전 | MiniStockCard / MiniChart | mitigate | 상승=--up(빨강)/하락=--down(파랑) 강제(Pitfall 5). 차트 chart-colors.ts 변환(oklch 회피 lesson) |
</threat_model>

<verification>
- message-render 유닛테스트 (마크다운 표 + 진행 + 카드 + 인용)
- webapp typecheck + build
</verification>

<success_criteria>
- 마크다운 답변(표/리스트/헤딩)
- 진행 스텝퍼 Variant B 5전문가
- 국내색상 미니카드 + 출처인용 + 미니차트(재사용)
</success_criteria>

<output>
After completion, create `.planning/phases/14-ai-analyst-chatbot/14-09-SUMMARY.md`
</output>
