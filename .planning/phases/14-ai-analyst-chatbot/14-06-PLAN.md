---
phase: 14-ai-analyst-chatbot
plan: 06
type: execute
wave: 4
depends_on: [01, 03, 05]
files_modified:
  - server/src/services/chat-service.ts
  - server/src/services/__tests__/chat-service.test.ts
  - server/src/routes/chat.ts
  - server/src/routes/__tests__/chat.route.test.ts
  - server/src/app.ts
  - server/src/services/supabase.ts
autonomous: true
requirements: [CHAT-01]
must_haves:
  truths:
    - "POST /api/chat 가 JWT 인증 후 SSE 스트림으로 팀장 답변을 토큰 단위로 흘려보낸다"
    - "팀장 tool_use 는 agent_start/agent_end SSE 로, 종목 언급은 stock_card SSE 로 전파된다"
    - "히스토리는 DB 복원 후 sanitizeMessages 통과, 응답 완료 시 user+assistant 메시지가 저장된다"
    - "새 요청이 이전 요청을 abort 하고(D-06), 시트를 닫아도 서버는 완료 후 저장한다"
    - "GET /api/chat/conversations 목록 + DELETE /api/chat/conversations/:id 가 소유자만 동작한다"
  artifacts:
    - path: "server/src/services/chat-service.ts"
      provides: "handleChatStream — 팀장 tool-use 루프 + sanitize/prune/retry + SSE emit"
      exports: ["handleChatStream"]
    - path: "server/src/routes/chat.ts"
      provides: "POST / (SSE) + GET/DELETE conversations"
      exports: ["chatRouter"]
    - path: "server/src/app.ts"
      provides: "app.use('/api/chat', chatRouter) 결선"
      contains: "/api/chat"
  key_links:
    - from: "routes/chat.ts POST /"
      to: "handleChatStream"
      via: "SSE 헤더 후 호출"
      pattern: "handleChatStream"
    - from: "chat-service"
      to: "chat-orchestrator SPECIALIST_TOOLS/runSpecialist"
      via: "팀장 루프"
      pattern: "SPECIALIST_TOOLS\\|runSpecialist"
---

<objective>
ww-bot `chat-service.ts`/`chat.ts` 를 gh-radar 서버로 이식한다. 검증된 SSE 헤더·15s keepalive·interrupt/busy 가드·sanitizeMessages·pruneHistory·429/529 retry·프롬프트 캐싱을 그대로 옮기고, "tool" 자리에 P05 의 5 전문가 tool 을 꽂아 팀장(Sonnet) 루프를 구성한다. SSE 라우트 + 대화관리(GET/DELETE) + app.ts 결선까지.

Purpose: 서버 챗 기능의 완성. 이 phase 위험은 새 알고리즘이 아니라 **이식 누락**(RESEARCH Don't Hand-Roll Key insight) — 고아 tool_result·연속 role·overloaded retry·close race 를 재발명하지 말고 그대로.
Output: chat-service + routes + app.ts 결선 + 유닛/라우트 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md
@.planning/phases/14-ai-analyst-chatbot/14-CONTEXT.md

<interfaces>
From server/src/services/chat-orchestrator.ts (P05): `SPECIALIST_TOOLS`, `runSpecialist`, `extractStockRefs`, `toolNameToSpecialistId`
From server/src/services/chat-history.ts (P03): `loadConversation`, `createConversation`, `appendMessage`, `listConversations`, `deleteConversation`, `assertConversationOwner`
From server/src/services/chat-prompts.ts (P04): `LEAD_PROMPT`
From server/src/middleware/require-auth.ts (P03): `requireAuth`
From server/src/schemas/chat.ts (P03): `ChatPostBody`, `ConversationListQuery`
From server/src/config.ts (P02): `chatEnabled`, `chatLeadModel`, `chatMaxToolRounds`, `chatMaxHistoryMessages`, `anthropicApiKey`
From packages/shared (P02): `ChatSSEEventMap`, `SPECIALIST_LABELS`, `MessageBlock`
From server/src/middleware/rate-limit.ts: rate limiter 팩토리 패턴
From server/src/app.ts (현재 결선 — 추가 지점):
```typescript
app.use("/api/home", homeRouter);   // ← 이 아래에 app.use("/api/chat", chatRouter) 추가
```
</interfaces>

<canonical_refs>
**반드시 읽고 이식** (외부 레포):
- ../weekly-wine-bot/server/src/services/chat-service.ts — sanitizeMessages/pruneHistory/isRetryableError/sendSSE/handleChatStream/세션 Map/interrupt/busy/MAX_* 상수 (906줄, 이식 기준)
- ../weekly-wine-bot/server/src/routes/chat.ts — SSE 헤더/15s keepalive/req.close→abort/route rate-limit/CHAT_DISABLED/done 보장 (142줄)
</canonical_refs>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: chat-service 이식 (팀장 tool-use 루프 + sanitize/prune/retry + SSE)</name>
  <files>server/src/services/chat-service.ts, server/src/services/__tests__/chat-service.test.ts</files>
  <read_first>
    - ../weekly-wine-bot/server/src/services/chat-service.ts (이식 원본 — sanitizeMessages/pruneHistory/isRetryableError/sendSSE/handleChatStream/세션Map/interrupt/MAX_* 전부)
    - server/src/services/chat-orchestrator.ts (P05 — SPECIALIST_TOOLS/runSpecialist/extractStockRefs)
    - server/src/services/chat-history.ts (P03 — 복원/저장 함수)
    - server/src/services/chat-prompts.ts (P04 — LEAD_PROMPT)
    - server/src/services/discussion-classify.ts (Anthropic client 싱글톤)
    - .planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md (Pattern 1 이식 포인트 + Code Examples 팀장 루프 골격 + Pitfall 3 히스토리 복원)
  </read_first>
  <behavior>
    - Test 1: sanitizeMessages — 고아 tool_result 제거 + 연속 role 병합 + 첫 메시지 user 보장 (ww-bot 원본 테스트 이식)
    - Test 2: pruneHistory — MAX_HISTORY_MESSAGES 초과 시 슬라이딩 윈도우, 첫 메시지 user 유지
    - Test 3: isRetryableError — overloaded_error/rate_limit_error/429/529 만 true
    - Test 4: handleChatStream — 팀장 mock 이 end_turn → text SSE + response_complete + user/assistant appendMessage 호출
    - Test 5: handleChatStream — 팀장 mock 이 tool_use(2개) → agent_start×2 병렬(Promise.all) + runSpecialist×2 + tool_result 후 재호출 → 최종 end_turn
    - Test 6: 새 요청이 interruptController.abort 로 이전 abort (D-06 busy 가드)
    - Test 7: extractStockRefs 로 감지된 종목 → stock_quotes 조회 → stock_card SSE emit (D-07)
  </behavior>
  <action>
    ww-bot `chat-service.ts` 를 이식하되 gh-radar 도메인으로 치환:
    - **그대로 이식**: `sanitizeMessages`, `pruneHistory`, `isRetryableError`, retry(MAX_RETRIES=2), `sendSSE`, 세션 Map + busy/interrupt(`AbortController` + `AbortSignal.any([clientAbort, interrupt])`), MAX_* 상수(config 로 대체: chatMaxToolRounds/chatMaxHistoryMessages). SYSTEM_PROMPT → `LEAD_PROMPT`(P04). MODEL → `cfg.chatLeadModel`.
    - **tool 치환**: `WINE_TOOLS` → `SPECIALIST_TOOLS`(P05), `executeTool` → `runSpecialist`. `generateToolLabel` → `SPECIALIST_LABELS[toolNameToSpecialistId(name)]` ("시세·수급 전문가 분석 중…" 라벨 D-04).
    - **팀장 루프**(RESEARCH Code Examples verbatim 골격): `for round < cfg.chatMaxToolRounds` → `lead.messages.stream({ model, max_tokens:4096, system:[{type:"text", text:LEAD_PROMPT, cache_control:{type:"ephemeral"}}], tools: SPECIALIST_TOOLS(마지막 원소 cache_control ephemeral), messages: workingMessages }, {signal})`. text_delta → `sendSSE(res,"text",...)`. finalMessage.stop_reason==="tool_use" → tool_use 블록들 `Promise.all(runSpecialist)` 병렬 + 각 앞뒤 `agent_start`/`agent_end` SSE + tool_result push. end_turn → break.
    - **SSE 이벤트**: session(conversationId) / text / agent_start / agent_end / stock_card / citation / response_complete. 웹서치 citations → citation SSE(D-08). extractStockRefs → stock_quotes 조회 후 stock_card SSE(D-07).
    - **히스토리(Pitfall 3)**: 시작 시 `loadConversation`(있으면) → user/assistant 텍스트 턴만 workingMessages 복원(tool 원본 미저장) → `sanitizeMessages` 필수 → `pruneHistory`. conversationId 없으면 `createConversation`(title=첫 메시지 30자) 후 session SSE. 완료 시 `appendMessage`(user, assistant+blocks). **시트 닫아도(clientAbort) 서버는 finalMessage 까지 완료 후 저장**(D-06) — clientAbort 는 스트림 전송만 멈추고 저장 로직은 진행되게(ww-bot interrupt 와 client close 분리 처리 확인).
    - **비용 로깅**: ww-bot `[chat] usage input/output tokens` 패턴 pino 로 기록(Pitfall 4, 메모리 lesson project_claude_haiku_cost_classify).
    - handleChatStream 시그니처: `(res, supabase, abortSignal, { userId, conversationId?, message, stockCode? })`.

    테스트: P02 anthropic-mock(makeStreamMock/makeToolUseFinalMessage/makeEndTurnFinalMessage) + supabase mock + res spy. Behavior 7케이스.
  </action>
  <verify>
    <automated>pnpm --filter server test chat-service</automated>
  </verify>
  <acceptance_criteria>
    - `grep "sanitizeMessages\|pruneHistory\|isRetryableError" server/src/services/chat-service.ts` 3개 매칭 (이식 완료)
    - `grep "cache_control" server/src/services/chat-service.ts` 매칭 (프롬프트 캐싱, Pitfall 4)
    - `grep "SPECIALIST_TOOLS\|runSpecialist" server/src/services/chat-service.ts` 매칭
    - `grep "Promise.all" server/src/services/chat-service.ts` 매칭 (병렬 전문가 호출 D-04)
    - `grep "agent_start\|agent_end\|stock_card" server/src/services/chat-service.ts` 매칭
    - `grep "AbortSignal.any" server/src/services/chat-service.ts` 매칭 (interrupt D-06)
    - `pnpm --filter server test chat-service` — 7 tests pass
  </acceptance_criteria>
  <done>chat-service 이식 완료 — sanitize/prune/retry/캐싱/interrupt + 팀장 tool 루프 + SSE emit + 히스토리 저장, 7 유닛테스트 green.</done>
</task>

<task type="auto">
  <name>Task 2: routes/chat.ts (SSE POST + GET/DELETE 대화관리) + supabase 서비스롤 확인</name>
  <files>server/src/routes/chat.ts, server/src/routes/__tests__/chat.route.test.ts, server/src/services/supabase.ts</files>
  <read_first>
    - ../weekly-wine-bot/server/src/routes/chat.ts (SSE 헤더/keepalive/close→abort/done 보장/route rate-limit/CHAT_DISABLED 이식 원본)
    - server/src/routes/home.ts (Router + app.locals.supabase + next(e) 위임 스타일)
    - server/src/middleware/require-auth.ts (P03), server/src/schemas/chat.ts (P03), server/src/services/chat-history.ts (P03)
    - server/src/services/supabase.ts (서비스롤 클라 생성 — JWT 검증도 이 클라의 auth.getUser 사용)
    - .planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md (Code Examples SSE 라우트 + Pattern 5 + Pitfall 2 Cloud Run)
  </read_first>
  <action>
    `chatRouter = Router()`:
    - `router.post("/", chatRateLimit, requireAuth(), handler)` — RESEARCH Code Examples verbatim:
      - `ChatPostBody.safeParse(req.body)` 실패 → 400(ValidationFailed). 성공 후 `{message, conversationId, stockCode}`.
      - `if (!cfg.chatEnabled) return res.status(503).json({error:{code:"CHAT_DISABLED",...}})` (kill-switch, 헤더 전).
      - `clientAbort = new AbortController(); req.on("close", () => clientAbort.abort())`.
      - `res.writeHead(200, {"Content-Type":"text/event-stream","Cache-Control":"no-cache",Connection:"keep-alive","X-Accel-Buffering":"no"})` (Cloud Run, Pitfall 2).
      - `keepalive = setInterval(()=>{ if(!res.writableEnded) res.write(": keepalive\n\n") }, 15_000)` (Pitfall 2).
      - `try { await handleChatStream(res, supabase, clientAbort.signal, {userId:req.userId, conversationId, message, stockCode}) } finally { clearInterval(keepalive); if(!res.writableEnded){ res.write("event: done\ndata: {}\n\n"); res.end() } }` (done 보장).
    - `router.get("/conversations", requireAuth(), handler)` — `ConversationListQuery.safeParse(req.query)` → `listConversations(supabase, req.userId, stockCode?)` → `{data: ConversationRow[]}` JSON. (D-13 종목 필터)
    - `router.get("/conversations/:id", requireAuth(), handler)` — `loadConversation(supabase, id, req.userId)` → `{conversation, messages}`. 미소유 → 404.
    - `router.delete("/conversations/:id", requireAuth(), handler)` — `deleteConversation(supabase, id, req.userId)` → 204.
    - `chatRateLimit` — middleware/rate-limit 팩토리 재사용 또는 라우트 전용(ww-bot route rate-limit 패턴). 에러는 `next(e)` 위임(error.message 미노출, V7).
    - supabase.ts 는 이미 서비스롤 클라 export — JWT 검증(`auth.getUser`)이 서비스롤 클라로 동작함을 주석 확인(변경 불요면 no-op, `app.locals.supabase` 가 auth.getUser 지원 확인만).

    라우트 테스트(supertest): 401(무토큰) / 400(빈 message) / 503(CHAT_DISABLED) / GET conversations 목록 / DELETE 소유권. SSE 스트림 자체는 chat-service 유닛이 커버 — 라우트 테스트는 헤더/상태코드/JSON 경로 중심.
  </action>
  <verify>
    <automated>pnpm --filter server test chat.route</automated>
  </verify>
  <acceptance_criteria>
    - `grep "X-Accel-Buffering" server/src/routes/chat.ts` 매칭 (Cloud Run Pitfall 2)
    - `grep "keepalive" server/src/routes/chat.ts` 매칭 (15s)
    - `grep "requireAuth()" server/src/routes/chat.ts` 4회 (POST + GET×2 + DELETE)
    - `grep "CHAT_DISABLED\|chatEnabled" server/src/routes/chat.ts` 매칭 (kill-switch)
    - `grep "event: done" server/src/routes/chat.ts` 매칭 (done 보장)
    - `pnpm --filter server test chat.route` — 라우트 테스트 green (401/400/503/목록/삭제)
  </acceptance_criteria>
  <done>chat 라우트 — SSE POST + GET/DELETE + 인증/kill-switch/keepalive/done 보장, supertest green.</done>
</task>

<task type="auto">
  <name>Task 3: app.ts 결선</name>
  <files>server/src/app.ts</files>
  <read_first>
    - server/src/app.ts (라우터 등록 순서 + express.json 16kb + app.locals DI)
    - server/src/routes/chat.ts (P06 Task 2 — chatRouter export)
  </read_first>
  <action>
    `import { chatRouter } from "./routes/chat.js"` 추가. `app.use("/api/home", homeRouter)` 다음 줄에 `app.use("/api/chat", chatRouter)` 추가.
    글로벌 `express.json({ limit: "16kb" })` 는 이미 존재 — 챗 body(~1,000자)에 충분(RESEARCH Pattern 5 A5, 라우트별 파서 불요). `/api` rate-limit(200req/60s)이 챗에도 적용됨(D-11) — 그대로 유지.
    SSE 라우트가 helmet/cors 미들웨어 뒤에 위치함 확인(순서 변경 금지).
  </action>
  <verify>
    <automated>pnpm --filter server exec tsc --noEmit &amp;&amp; pnpm --filter server test</automated>
  </verify>
  <acceptance_criteria>
    - `grep "app.use(\"/api/chat\", chatRouter)" server/src/app.ts` 매칭
    - `grep "chatRouter" server/src/app.ts` import + use 2회
    - server 전체 test green + typecheck exit 0
  </acceptance_criteria>
  <done>chatRouter 결선, server 전체 테스트+typecheck 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/chat (SSE) | 인증·검증 통과한 스트리밍 요청 |
| Cloud Run proxy → SSE 응답 | 프록시 버퍼링/타임아웃 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Spoofing | requireAuth 게이트 | mitigate | 모든 챗 라우트 requireAuth() — SSE 헤더 쓰기 전 401(Pattern 3) |
| T-14-01 | IDOR | GET/DELETE conversations | mitigate | assertConversationOwner / WHERE user_id 명시 필터. 미소유 404 |
| T-14-04 | DoS(비용) | chatEnabled + rate-limit + MAX_TOOL_ROUNDS | mitigate | CHAT_DISABLED 503 kill-switch + /api rate-limit(200/60s) + route rate-limit + chatMaxToolRounds 상한 + 토큰 usage 로깅 |
| T-14-07 | SSE 버퍼/스머글링 | SSE 헤더 | mitigate | X-Accel-Buffering:no + 15s keepalive(Cloud Run Pitfall 2) + helmet 기적용 + CORS allow-list |
| T-14-06 | Info Disclosure (로그 유출) | pino 로깅 | mitigate | error.message 미노출(next 위임, V7) + pino redact ANTHROPIC_API_KEY 기포함 + Authorization 헤더 redact 확인. 대화 본문 로그 미기록 |
</threat_model>

<verification>
- chat-service 7 유닛 + chat.route supertest + server 전체 test + typecheck
</verification>

<success_criteria>
- POST /api/chat SSE 팀장 답변 스트리밍 + agent/stock_card/citation 이벤트
- 히스토리 복원 sanitize + 완료 저장, 새 요청 interrupt
- GET/DELETE 대화관리 소유자 한정
- app.ts 결선
</success_criteria>

<output>
After completion, create `.planning/phases/14-ai-analyst-chatbot/14-06-SUMMARY.md`
</output>
