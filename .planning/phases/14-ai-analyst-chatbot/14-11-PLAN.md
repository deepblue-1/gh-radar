---
phase: 14-ai-analyst-chatbot
plan: 11
type: execute
wave: 6
depends_on: [06, 10]
files_modified:
  - webapp/e2e/specs/chat.spec.ts
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [CHAT-01]
user_setup:
  - service: anthropic
    why: "웹서치 전문가(D-12) — web_search 서버 tool 은 조직 관리자가 Claude Console 에서 활성화 필요"
    dashboard_config:
      - task: "Claude Console › Privacy 에서 web search 활성화 (미활성 시 웹서치 전문가만 비활성, 나머지 4 전문가는 정상)"
        location: "Claude Console (platform.claude.com)"
must_haves:
  truths:
    - "production POST /api/chat 가 JWT 인증 후 SSE 첫 토큰을 수신하고, 미인증은 401 이다"
    - "Playwright E2E 로 로그인→질문→스트리밍→히스토리 저장 플로우가 검증된다"
    - "CHAT-01 요구사항이 REQUIREMENTS.md + Traceability 에 Complete 로 기록된다"
    - "웹서치 전문가 모델(Haiku vs Sonnet)이 POC 실측으로 확정된다"
  artifacts:
    - path: "webapp/e2e/specs/chat.spec.ts"
      provides: "챗 E2E — 로그인 게이트 + 스트리밍 + 히스토리"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CHAT-01 정의 + Traceability 행"
      contains: "CHAT-01"
  key_links:
    - from: "webapp/e2e/specs/chat.spec.ts"
      to: "production /api/chat SSE"
      via: "로그인 fixture + 스트리밍 assert"
      pattern: "chat"
---

<objective>
서버 재배포 + webapp Vercel 배포 + production smoke + Playwright E2E + web_search 콘솔 활성화 확인/웹서치 모델 POC + REQUIREMENTS/ROADMAP/STATE 갱신으로 phase 를 마감한다.

Purpose: end-to-end 프로덕션 검증 + 요구사항 추적성 완결(HOME-01/LIMIT-01 선례). ANTHROPIC_API_KEY 는 서버에 이미 바인딩(`gh-radar-anthropic-api-key:latest`) — 신규 secret 불요.
Output: E2E spec + production 배포 + POC 게이트 + 요구사항 문서.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md

<interfaces>
From scripts/deploy-server.sh: ANTHROPIC_API_KEY 이미 `--update-secrets` 에 바인딩. 재배포 시 새 코드(챗 라우트) 반영. CHAT_* env 는 코드 default 로 동작(선택적 추가).
From webapp/e2e/auth.setup.ts + e2e/fixtures: 로그인 storageState fixture(Phase 06.2) — 챗 E2E 재사용
From webapp/e2e/specs/home.spec.ts: E2E spec 구조 참고 (PORT=3100 dev.sh 기준, memory feedback_check_dev_sh_first)
From .planning/REQUIREMENTS.md: v1 섹션 + Traceability 표 + Coverage 카운트 (CHAT-01 추가 대상)
Memory: reference_vercel_frontend_deploy (repo root vercel pull→build→deploy --prebuilt, ignoreCommand docs-tip skip 함정), reference_gh_radar_deployer_sa (GCP 인증)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright chat E2E spec + REQUIREMENTS/ROADMAP/STATE 갱신</name>
  <files>webapp/e2e/specs/chat.spec.ts, .planning/REQUIREMENTS.md</files>
  <read_first>
    - webapp/e2e/specs/home.spec.ts (E2E spec 구조 — PORT 3100, 로그인 fixture 사용)
    - webapp/e2e/auth.setup.ts (storageState 로그인 fixture)
    - .planning/REQUIREMENTS.md (v1 섹션 + Traceability + Coverage 카운트 — CHAT-01 추가)
    - .planning/ROADMAP.md (Phase 14 엔트리 — Plans 목록/Requirements 갱신)
  </read_first>
  <action>
    E2E `chat.spec.ts` (Playwright, PORT=3100 dev.sh 기준 — memory feedback_check_dev_sh_first):
    - Test 1: 비로그인 상태에서 FAB 클릭 → "로그인이 필요해요" 상태 표시(D-01, 스트리밍 미발생).
    - Test 2: 로그인(storageState) 후 FAB → 시트 open → 질문 전송 → SSE text 스트리밍 수신(assistant 메시지 나타남) → 면책 문구 표시.
    - Test 3: 종목상세(/stocks/[code])에서 FAB 라벨에 종목명 컨텍스트 표시(D-03).
    - Test 4: /chat 페이지 대화목록 렌더 + 삭제 다이얼로그 open/취소.
    - 스트리밍은 실서버 또는 SSE mock 라우트로(로컬 dev 서버가 챗 라우트 제공). 네트워크 불안정 대비 assert 는 첫 토큰/시트 상태 중심.

    REQUIREMENTS.md 갱신:
    - v1 Requirements 에 새 섹션 `### Chat` + `- [x] **CHAT-01**: 팀장(Sonnet)+전문가 5(Haiku: 시세·수급/테마/뉴스·심리/상한가 패턴/실시간 웹서치) 멀티에이전트 AI 애널리스트 챗봇 — 로그인 사용자별·종목별 히스토리(conversations/messages, RLS), SSE 스트리밍 POST /api/chat + JWT 검증, 미니 종목카드/출처 인용/미니 일봉차트/진행 스텝퍼, react-markdown 답변, 전역 FAB + /chat 페이지 — Phase 14`
    - Traceability 표에 `| CHAT-01 | Phase 14 | Complete |` 행 추가.
    - Coverage 카운트 36→37 갱신 + Last updated 주석.
    - ROADMAP.md Phase 14 엔트리: Requirements CHAT-01 명시 + Plans 11개 목록 체크 + Goal 확정(placeholder 있으면).
  </action>
  <verify>
    <automated>grep -q "CHAT-01" .planning/REQUIREMENTS.md &amp;&amp; grep -q "CHAT-01 | Phase 14" .planning/REQUIREMENTS.md &amp;&amp; test -f webapp/e2e/specs/chat.spec.ts &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "CHAT-01" .planning/REQUIREMENTS.md` v1 정의 + Traceability 2회 이상 매칭
    - `grep "CHAT-01 | Phase 14 | Complete" .planning/REQUIREMENTS.md` 매칭
    - `grep -c "test(\|test.describe" webapp/e2e/specs/chat.spec.ts` >= 4
    - `grep "3100\|baseURL" webapp/e2e/specs/chat.spec.ts` (dev.sh 포트 정합) 또는 config 상속 확인
  </acceptance_criteria>
  <done>chat E2E 4시나리오 + REQUIREMENTS CHAT-01/Traceability Complete + ROADMAP 갱신.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] 서버 재배포 + webapp 배포 + web_search POC + production smoke</name>
  <action>모든 구현 plan(P01~P10) 완료 후 서버 재배포 + webapp Vercel 배포 + web_search 모델 POC 실측 + production smoke 를 수행한다. ANTHROPIC_API_KEY 는 서버에 이미 바인딩(gh-radar-anthropic-api-key:latest) — 신규 secret 불요. 아래 how-to-verify 6단계대로 수행.</action>
  <what-built>챗 기능 전체(서버 SSE 라우트 + webapp FAB/시트/페이지)를 production 에 배포하고 web_search 활성화·웹서치 모델을 실측 확정.</what-built>
  <how-to-verify>
    1. **서버 재배포** (memory reference_gh_radar_deployer_sa GCP 인증 사용, 신규 secret 불요 — ANTHROPIC_API_KEY 기바인딩): `scripts/deploy-server.sh` 로 새 이미지 배포. 필요 시 `CHAT_LEAD_MODEL`/`CHAT_SPECIALIST_MODEL`/`CHAT_WEBSEARCH_MODEL` env 는 코드 default(claude-sonnet-4-6 / claude-haiku-4-5 / claude-haiku-4-5)로 동작하므로 추가 불필요.
    2. **web_search 콘솔 활성화 확인** (RESEARCH Environment Availability): Claude Console › Privacy 에서 web search 활성화 여부 확인. 미활성 → 활성화하거나, 웹서치 전문가 비활성 상태로 나머지 4 전문가만 운영(챗 완전 동작).
    3. **웹서치 모델 POC** (RESEARCH A2/Pitfall 1): 웹서치 유발 질문("오늘 장중 속보 알려줘")으로 실측. `web_search_tool_result_error` / 미실행 관측 시 → server env `CHAT_WEBSEARCH_MODEL=claude-sonnet-4-6` 로 폴백 재배포. 정상 동작 시 Haiku 유지. 결과를 SUMMARY 에 기록.
    4. **webapp 배포** (memory reference_vercel_frontend_deploy — repo root vercel pull→build→deploy --prebuilt, ignoreCommand docs-tip skip 함정 주의): react-markdown 번들 포함 배포.
    5. **production smoke**:
       - `curl -X POST {SERVER_URL}/api/chat` (토큰 없이) → 401 `UNAUTHENTICATED`.
       - 유효 access token 으로 SSE POST → 첫 토큰(text 이벤트) 수신 확인 + 히스토리 저장(conversations 1행 증가).
       - 프로덕션 웹앱 로그인 후 FAB → 질문 → 스트리밍 + 미니카드/면책 렌더 육안 확인.
       - Vercel env `NEXT_PUBLIC_API_BASE_URL` trailing newline 검증(memory feedback_vercel_env_paste_newline).
    6. **비용 로그 확인**: Cloud Logging 에서 `[chat] usage` 토큰 로깅 관측(Pitfall 4, 비용 경계 추적 lesson).
  </how-to-verify>
  <resume-signal>배포 완료 + 401/SSE 첫토큰 smoke + web_search 모델 확정(Haiku 유지 or Sonnet 폴백) 후 결과 기술, 또는 이슈 보고</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production client → /api/chat | 실사용자 JWT + SSE |
| Cloud Run → Anthropic (web_search) | 외부 웹 검색 활성화 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02d | Spoofing | production 401 smoke | mitigate | 무토큰 POST → 401 검증. requireAuth production 동작 확인 |
| T-14-04d | DoS(비용) | production 토큰 로깅 | mitigate | `[chat] usage` 로그 관측(조기 비용 감지). web_search max_uses:3 + D-12 억제. 남용 시 CHAT_DISABLED |
| T-14-12 | (배포 회귀) 웹서치 미지원 | web_search POC | mitigate | Haiku web_search 실측 → 미지원 시 env 폴백(코드 변경 0). 미활성 콘솔 시 웹서치만 descope |
| T-14-06b | Info Disclosure | Vercel env newline | mitigate | NEXT_PUBLIC_API_BASE_URL trailing newline 검증(memory lesson) |
</threat_model>

<verification>
- chat E2E 4시나리오
- production smoke (401 + SSE 첫토큰 + 히스토리 저장)
- web_search 모델 POC 확정
- REQUIREMENTS/ROADMAP/STATE 갱신
</verification>

<success_criteria>
- 서버+webapp production 배포, 챗 end-to-end 동작
- 미인증 401 + 인증 SSE 스트리밍 + 히스토리 저장
- web_search 모델 실측 확정
- CHAT-01 Complete 추적성
</success_criteria>

<output>
After completion, create `.planning/phases/14-ai-analyst-chatbot/14-11-SUMMARY.md`
</output>
