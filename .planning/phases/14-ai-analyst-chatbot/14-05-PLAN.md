---
phase: 14-ai-analyst-chatbot
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified:
  - server/src/services/chat-orchestrator.ts
  - server/src/services/__tests__/chat-orchestrator.test.ts
autonomous: true
requirements: [CHAT-01]
must_haves:
  truths:
    - "팀장에 노출할 5개 전문가 tool 정의(Anthropic.Tool[])가 존재한다"
    - "팀장이 고른 전문가들이 tool_use id 기준으로 dispatch 되어 tool_result 로 조립된다"
    - "한 turn 에 여러 tool_use 가 오면 Promise.all 로 병렬 실행된다 (선택적 병렬 호출)"
  artifacts:
    - path: "server/src/services/chat-orchestrator.ts"
      provides: "SPECIALIST_TOOLS, runSpecialist dispatch, emitInlineStockCards"
      exports: ["SPECIALIST_TOOLS", "runSpecialist", "extractStockRefs"]
  key_links:
    - from: "runSpecialist(tool name)"
      to: "consult{X}Specialist"
      via: "이름 기반 dispatch"
      pattern: "consult(Quote|Theme|News|Limitup|WebSearch)Specialist"
---

<objective>
팀장(Sonnet)에 노출할 5개 전문가 tool 정의와, tool_use → 전문가 실행 → tool_result 조립 dispatch 를 구현한다(RESEARCH Pattern 2 agent-as-tool). 병렬 호출(Promise.all)과 답변 텍스트에서 종목 참조를 뽑는 인라인 카드 유틸(D-07)도 포함.

Purpose: 멀티에이전트 오케스트레이션 코어. chat-service(P06) 의 팀장 루프가 `SPECIALIST_TOOLS` 를 팀장에 넘기고, tool_use 블록마다 `runSpecialist` 를 호출한다.
Output: 오케스트레이터 1 + 유닛 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md
@.planning/phases/14-ai-analyst-chatbot/14-CONTEXT.md

<interfaces>
From server/src/services/specialists/* (P04):
```typescript
consultQuoteSpecialist(supabase, input): Promise<string>
consultThemeSpecialist(supabase, input): Promise<string>
consultNewsSpecialist(supabase, input): Promise<string>
consultLimitupSpecialist(supabase, input): Promise<string>
consultWebSearchSpecialist(input): Promise<{ text: string; citations: Array<{title:string; url:string}> }>
```
From packages/shared (P02): `SpecialistId`, `SPECIALIST_TOOL_NAMES`, `SPECIALIST_LABELS`
From RESEARCH Pattern 2 (SPECIALIST_TOOLS 정의 — verbatim 채택):
```typescript
const SPECIALIST_TOOLS: Anthropic.Tool[] = [
  { name: "consult_quote_specialist", description: "특정 종목의 현재가·등락률·거래대금·시가총액·최근 일봉 흐름·수급 관점 분석이 필요할 때", input_schema: { type:"object", properties:{ code:{type:"string"}, question:{type:"string"} }, required:["question"] } },
  ...
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SPECIALIST_TOOLS 정의 + runSpecialist dispatch</name>
  <files>server/src/services/chat-orchestrator.ts, server/src/services/__tests__/chat-orchestrator.test.ts</files>
  <read_first>
    - server/src/services/specialists/*.ts (P04 — 시그니처 확인)
    - .planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md (Pattern 2 tool 정의 5종 description + Code Examples 병렬 dispatch)
    - packages/shared/src/chat.ts (SPECIALIST_TOOL_NAMES / SPECIALIST_LABELS)
  </read_first>
  <behavior>
    - Test 1: SPECIALIST_TOOLS 는 길이 5, 각 name 이 SPECIALIST_TOOL_NAMES 값과 일치, input_schema.type=="object"
    - Test 2: runSpecialist("consult_quote_specialist", {code,question}, supabase) → consultQuoteSpecialist 호출 결과(문자열) 반환
    - Test 3: runSpecialist("consult_websearch_specialist", {question}) → 웹서치 결과 text 를 tool_result content 로, citations 는 반환 객체로 분리 노출 (팀장 tool_result 엔 text, D-08 citation 은 SSE 로 별도 전파)
    - Test 4: 알 수 없는 tool 이름 → 안내 텍스트(throw 안 함, 팀장이 계속 진행)
    - Test 5: runSpecialist("consult_quote_specialist", {question}) — code 미지정 → consultQuoteSpecialist 미호출 + graceful skip 텍스트("종목이 특정되지 않아…") 반환. limitup 도 동일 (code 의존 전문가는 무데이터 호출 차단, D-08 환각방지)
  </behavior>
  <action>
    - `SPECIALIST_TOOLS: Anthropic.Tool[]` — RESEARCH Pattern 2 의 5개 tool description verbatim. name 은 `SPECIALIST_TOOL_NAMES` 값 사용. input_schema properties `code`(string, optional), `question`(string, required). 웹서치 tool description 에 "비용 큼 — 꼭 필요할 때만"(D-12). **code 의존 전문가(quote/limitup) tool description 에는 "종목 코드가 특정되지 않으면 호출하지 말 것" 명시**(D-08 환각방지 — 무데이터 시세/상한가 분석 유발 차단). `code` 를 required 로 올리지 않는 이유: 팀장이 종목 없는 일반 질문에서 tool 스키마 위반 에러를 내지 않고 자연스럽게 미호출하도록 두고, 실제 방어는 아래 runSpecialist graceful guard 로 강제.
    - **runSpecialist code guard(D-08):** name 이 quote/limitup 인데 `input.code` 가 없으면 consult 함수를 호출하지 않고 `{text:"종목이 특정되지 않아 시세/상한가 분석을 건너뜁니다"}` 즉시 반환(무데이터 DB 조회·환각 원천 차단). news/theme/websearch 는 code 없이도 유효(테마/뉴스/웹은 코드 무관 질의 가능)하므로 guard 미적용.
    - 팀장 프롬프트 캐싱을 위해 tools 배열은 그대로 export(P06 에서 마지막 원소에 cache_control 부착).
    - `runSpecialist(name: string, input: {code?:string; question:string}, supabase): Promise<{ text: string; citations?: Array<{title:string;url:string}> }>` — name → SpecialistId 역매핑 후 해당 consult 함수 dispatch. 데이터 전문가는 `{text}`, 웹서치는 `{text, citations}`. 미지의 name 은 `{text:"해당 전문가를 찾을 수 없습니다"}`.
    - `toolNameToSpecialistId(name): SpecialistId | null` 헬퍼(SSE agent_start 라벨용, P06 재사용).
    - `extractStockRefs(text: string): Array<{code:string}>` — 답변 텍스트에서 6자리 종목코드 패턴(정규식 `\((\d{6})\)` 또는 종목코드 표기)을 추출(D-07 미니 카드 트리거용). 실제 카드 데이터(가격/등락률) 조회는 P06 이 stock_quotes 로 수행.

    테스트: P02 anthropic-mock + specialists 를 vi.mock 으로 스텁. Behavior 4케이스.
  </action>
  <verify>
    <automated>pnpm --filter server test chat-orchestrator</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "consult_" server/src/services/chat-orchestrator.ts` >= 5 (5 tool)
    - `grep "input_schema" server/src/services/chat-orchestrator.ts` 매칭
    - `grep "runSpecialist\|extractStockRefs\|SPECIALIST_TOOLS" server/src/services/chat-orchestrator.ts` 3 export
    - `grep "종목이 특정되지 않아" server/src/services/chat-orchestrator.ts` 매칭 (quote/limitup code guard, D-08)
    - `pnpm --filter server test chat-orchestrator` — 5 tests pass
  </acceptance_criteria>
  <done>5 tool 정의(quote/limitup code 의존 명시) + runSpecialist dispatch + code guard(D-08) + extractStockRefs + 5 유닛테스트 green. 병렬 실행(Promise.all)은 P06 팀장 루프가 수행 — 여기선 단일 dispatch 검증.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 팀장 tool_use.input → 전문가 | LLM 이 생성한 tool 인자(code/question)가 전문가로 전달 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03b | Tampering (tool 인자 오염) | runSpecialist input | mitigate | code 는 전문가 내부 조회 시 파라미터 바인딩(SQL injection 무관 — Supabase 클라). question 은 프롬프트 주입만, 시스템 프롬프트가 역할 고정. **quote/limitup code 미지정 시 graceful skip(무데이터 조회 차단, D-08)** |
| T-14-04c | DoS(비용) | 미지 tool graceful | accept | 미지 tool 이름은 안내 텍스트 반환(throw 안 함) — 팀장 루프 MAX_TOOL_ROUNDS(P06)가 무한 방지 |
</threat_model>

<verification>
- chat-orchestrator 4 유닛테스트
</verification>

<success_criteria>
- SPECIALIST_TOOLS 5종 정의
- runSpecialist 이름 기반 dispatch
- extractStockRefs 종목 참조 추출
</success_criteria>

<output>
After completion, create `.planning/phases/14-ai-analyst-chatbot/14-05-SUMMARY.md`
</output>
