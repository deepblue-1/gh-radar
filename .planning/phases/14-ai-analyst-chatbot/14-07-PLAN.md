---
phase: 14-ai-analyst-chatbot
plan: 07
type: execute
wave: 2
depends_on: [02]
files_modified:
  - webapp/package.json
  - webapp/src/lib/chat-sse.ts
  - webapp/src/lib/__tests__/chat-sse.test.ts
  - webapp/src/lib/chat-api.ts
  - webapp/src/components/chat/chat-provider.tsx
autonomous: true
requirements: [CHAT-01]
must_haves:
  truths:
    - "웹앱이 raw fetch + getReader 로 SSE 를 소비하고 이벤트를 콜백으로 파싱한다 (apiFetch 미사용)"
    - "챗 요청에 Supabase access token 이 Authorization: Bearer 로 부착된다"
    - "대화 목록/삭제는 JSON(apiFetch) 로, 전역 챗 상태(open/context)는 provider 로 관리된다"
    - "종목상세 페이지가 setStockContext({code,name})로 종목 컨텍스트를 provider 에 발행하고, 이탈 시 null 로 해제한다"
  artifacts:
    - path: "webapp/src/lib/chat-sse.ts"
      provides: "streamChat — raw fetch + parseSSEStream 포팅 + Bearer 부착"
      exports: ["streamChat", "parseSSEStream"]
    - path: "webapp/src/lib/chat-api.ts"
      provides: "listConversations / getConversation / deleteConversation (JSON)"
    - path: "webapp/src/components/chat/chat-provider.tsx"
      provides: "ChatProvider + useChat — open/stockContext 전역 상태 + setStockContext 발행"
      exports: ["ChatProvider", "useChat"]
      exports: ["ChatProvider", "useChat"]
  key_links:
    - from: "chat-sse.streamChat"
      to: "Supabase getSession().access_token"
      via: "Authorization: Bearer"
      pattern: "Bearer"
---

<objective>
프론트 챗 기반 계층을 만든다. `apiFetch` 는 8s 타임아웃 JSON 전용이라 SSE 불가 — raw `fetch` + `response.body.getReader()` + `parseSSEStream`(somi-chat-core 포팅)으로 스트림 소비. 대화관리는 JSON(apiFetch), 전역 상태는 provider.

Purpose: SSE 계약(P02 ChatSSEEventMap)의 클라이언트 소비 계층. UI 컴포넌트(P08/P09)와 /chat 페이지(P10)가 소비. 서버 런타임 없이 SSE 계약만으로 구현 가능(병렬).
Output: markdown 의존성 설치 + chat-sse + chat-api + chat-provider + SSE 파서 유닛테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md
@.planning/phases/14-ai-analyst-chatbot/14-CONTEXT.md
@.planning/phases/14-ai-analyst-chatbot/14-UI-SPEC.md

<interfaces>
From ../weekly-wine-bot/packages/somi-chat-core/src/sse-parser.ts (포팅 원본 — verbatim 채택, 타입만 ChatSSEEventMap 으로):
```typescript
export async function parseSSEStream(reader, onEvent): Promise<void> {
  const decoder = new TextDecoder(); let buffer = ""; let eventType = "";
  // event: / data: 라인 파싱 + 버퍼 경계 flush
}
```
From webapp/src/lib/supabase/client.ts: `createClient()` → `createBrowserClient` → `.auth.getSession()` → `session.access_token`
From webapp/src/lib/api.ts: `resolveBaseUrl()`, `apiFetch`(JSON 전용, SSE 불가), `ApiClientError`
From packages/shared (P02): `ChatSSEEventMap`, `ChatSSEEventType`, `ConversationRow`, `MessageRow`, `SpecialistId`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: react-markdown + remark-gfm 설치</name>
  <files>webapp/package.json</files>
  <read_first>
    - webapp/package.json (dependencies 섹션 — lightweight-charts 등 기존 런타임 deps 위치)
    - .planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md (Standard Stack — react-markdown 10.1.0 / remark-gfm 4.0.1 verified)
  </read_first>
  <action>
    `cd webapp && pnpm add react-markdown@10 remark-gfm@4` 실행 (RESEARCH verified 버전: react-markdown 10.1.0, remark-gfm 4.0.1). D-09 풀 마크다운(표/리스트/헤딩/코드). somi-chat 라이트 변환은 폐기.
  </action>
  <verify>
    <automated>grep -q "react-markdown" webapp/package.json &amp;&amp; grep -q "remark-gfm" webapp/package.json &amp;&amp; pnpm --filter webapp exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "react-markdown" webapp/package.json` 매칭 (^10)
    - `grep "remark-gfm" webapp/package.json` 매칭 (^4)
    - webapp typecheck exit 0
  </acceptance_criteria>
  <done>react-markdown@10 + remark-gfm@4 설치, typecheck 통과.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chat-sse.ts (SSE 소비 + Bearer) + parseSSEStream 유닛테스트</name>
  <files>webapp/src/lib/chat-sse.ts, webapp/src/lib/__tests__/chat-sse.test.ts</files>
  <read_first>
    - ../weekly-wine-bot/packages/somi-chat-core/src/sse-parser.ts (parseSSEStream 포팅 원본)
    - webapp/src/lib/supabase/client.ts (getSession().access_token 취득)
    - webapp/src/lib/api.ts (resolveBaseUrl — base URL 재사용)
    - .planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md (Code Examples 클라 SSE + Anti-pattern apiFetch 불가)
  </read_first>
  <behavior>
    - Test 1: parseSSEStream — `event: text\ndata: {"text":"안"}\n\n` 스트림 → onEvent("text",{text:"안"}) 호출
    - Test 2: parseSSEStream — 청크 경계로 잘린 이벤트(버퍼 flush) 정상 파싱
    - Test 3: parseSSEStream — 잘못된 JSON data 라인은 무시(throw 안 함)
    - Test 4: parseSSEStream — 여러 이벤트 타입(agent_start/stock_card/done) 순차 콜백
  </behavior>
  <action>
    - `parseSSEStream(reader, onEvent)` — somi-chat-core sse-parser.ts verbatim 포팅. 타입만 `ChatSSEEventType`/`ChatSSEEventMap`(P02) 사용. TextDecoder stream 경계·버퍼 flush 처리 그대로.
    - `streamChat(params: { message: string; conversationId?: string; stockCode?: string }, onEvent, signal): Promise<void>` — RESEARCH Code Examples verbatim:
      - `const { data:{ session } } = await createClient().auth.getSession()`. session 없으면 로그인 필요 에러 throw(D-01 — UI 가 로그인 게이트 표시).
      - `fetch(\`${resolveBaseUrl()}/api/chat\`, { method:"POST", headers:{ "Content-Type":"application/json", Authorization:\`Bearer ${session.access_token}\` }, body: JSON.stringify(params), signal })`.
      - 401 응답 → 세션만료 에러 throw. 503 → CHAT_DISABLED 에러. non-ok 이면서 SSE 아님 → 에러.
      - `await parseSSEStream(resp.body!.getReader(), onEvent)`.
      - **apiFetch 사용 금지** 주석(8s 타임아웃이 스트림 끊음, RESEARCH Anti-pattern).

    테스트: ReadableStream mock(Uint8Array 청크 push) + onEvent spy. Behavior 4케이스. streamChat 은 fetch mock 으로 최소 검증(Bearer 헤더 부착).
  </action>
  <verify>
    <automated>pnpm --filter webapp test chat-sse</automated>
  </verify>
  <acceptance_criteria>
    - `grep "parseSSEStream\|streamChat" webapp/src/lib/chat-sse.ts` 2 export
    - `grep "getReader" webapp/src/lib/chat-sse.ts` 매칭 (raw fetch stream)
    - `grep "Bearer" webapp/src/lib/chat-sse.ts` 매칭 (access_token 부착)
    - `grep "apiFetch" webapp/src/lib/chat-sse.ts` == 결과 없음 (SSE 는 raw fetch)
    - `pnpm --filter webapp test chat-sse` — 4 tests pass
  </acceptance_criteria>
  <done>chat-sse — parseSSEStream 포팅 + streamChat(Bearer) + 4 유닛테스트 green.</done>
</task>

<task type="auto">
  <name>Task 3: chat-api.ts (대화관리 JSON) + chat-provider.tsx (전역 상태)</name>
  <files>webapp/src/lib/chat-api.ts, webapp/src/components/chat/chat-provider.tsx</files>
  <read_first>
    - webapp/src/lib/api.ts (apiFetch 시그니처 + ApiClientError — JSON 경로는 apiFetch 사용 가능)
    - webapp/src/lib/supabase/client.ts (getSession — apiFetch 에 Bearer 부착 필요 시)
    - webapp/src/lib/auth-context.tsx (useAuth 패턴 — provider context 스타일)
    - .planning/phases/14-ai-analyst-chatbot/14-CONTEXT.md (D-13 목록/필터 /chat 만, D-03 종목 컨텍스트)
  </read_first>
  <action>
    `chat-api.ts` — 대화관리 JSON (apiFetch 사용 가능, SSE 아님). Bearer 토큰 부착 필요(챗 라우트는 requireAuth) → apiFetch 가 Authorization 헤더 옵션 지원하는지 확인하고, 미지원이면 getSession 후 헤더 주입하는 얇은 래퍼. export:
    - `listConversations(stockCode?)` → `GET /api/chat/conversations[?stockCode=]` → `ConversationRow[]`.
    - `getConversation(id)` → `GET /api/chat/conversations/:id` → `{conversation, messages}`.
    - `deleteConversation(id)` → `DELETE /api/chat/conversations/:id`.

    `chat-provider.tsx` — `'use client'`. 전역 챗 상태:
    - `ChatProvider` context: `{ open: boolean; openChat(ctx?: {code:string; name:string}): void; closeChat(): void; stockContext: {code:string; name:string} | null; setStockContext(ctx: {code:string; name:string} | null): void }`.
    - `setStockContext` — 종목상세 페이지(P08 이 StockDetailClient 배선)가 `stock.name` 로드 후 `{code,name}` 발행, 언마운트 시 `null` 로 해제. FAB 라벨/openChat 이 이 값을 읽어 `{종목명}` 을 채움(D-03, usePathname 은 code 만 주므로 name 은 이 채널로 공급). `openChat(ctx)` 호출 시에도 stockContext 갱신.
    - `useChat()` 훅. layout.tsx(P08) 가 마운트, FAB/시트가 소비.
    - 상태만 관리(SSE 스트리밍 자체는 시트 컴포넌트 P08 가 chat-sse 로 수행) — provider 는 open/context 만.
  </action>
  <verify>
    <automated>pnpm --filter webapp exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "listConversations\|getConversation\|deleteConversation" webapp/src/lib/chat-api.ts` 3 export
    - `grep "'use client'" webapp/src/components/chat/chat-provider.tsx` 매칭
    - `grep "ChatProvider\|useChat" webapp/src/components/chat/chat-provider.tsx` 2 export
    - `grep "stockContext\|openChat" webapp/src/components/chat/chat-provider.tsx` 매칭
    - `grep "setStockContext" webapp/src/components/chat/chat-provider.tsx` 매칭 (종목명 발행 채널, D-03/FAB 라벨)
    - webapp typecheck exit 0
  </acceptance_criteria>
  <done>chat-api 3 함수 + chat-provider(open/context) export, typecheck 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 서버 챗 API | access_token 을 Authorization 헤더로 전달 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02c | Spoofing | streamChat Bearer | mitigate | Supabase getSession().access_token 을 서버가 검증(P03). 클라는 토큰 저장/조작 안 함(Supabase SDK 관리) |
| T-14-08 | (신뢰성) SSE 파싱 견고성 | parseSSEStream | mitigate | 검증된 somi-chat-core 파서 포팅 — 버퍼 경계 flush + JSON 파싱 실패 무시(throw 안 함) |
</threat_model>

<verification>
- chat-sse 4 유닛테스트
- webapp typecheck (chat-api/provider)
</verification>

<success_criteria>
- streamChat raw fetch + Bearer + parseSSEStream
- chat-api 대화관리 JSON
- chat-provider 전역 open/context
</success_criteria>

<output>
After completion, create `.planning/phases/14-ai-analyst-chatbot/14-07-SUMMARY.md`
</output>
