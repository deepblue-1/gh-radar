---
phase: 14-ai-analyst-chatbot
plan: 10
type: execute
wave: 5
depends_on: [07, 08, 09]
files_modified:
  - webapp/src/app/chat/page.tsx
  - webapp/src/components/chat/conversation-list.tsx
  - webapp/src/components/chat/delete-conversation-dialog.tsx
  - webapp/src/components/chat/__tests__/conversation-list.test.tsx
autonomous: true
requirements: [CHAT-01]
must_haves:
  truths:
    - "/chat 페이지가 2-col(대화목록 + thread)로 대화 관리를 제공한다 (모바일 1-col)"
    - "대화목록을 종목별로 필터링하고 대화를 선택해 이어갈 수 있다 (D-13)"
    - "대화 삭제는 확인 다이얼로그 후 소유자만 삭제된다 (되돌릴 수 없음 고지)"
  artifacts:
    - path: "webapp/src/app/chat/page.tsx"
      provides: "/chat 2-col 페이지 (D-13)"
    - path: "webapp/src/components/chat/conversation-list.tsx"
      provides: "대화목록 + 종목 필터 select + 새 대화"
    - path: "webapp/src/components/chat/delete-conversation-dialog.tsx"
      provides: "삭제 확인 다이얼로그 (destructive)"
  key_links:
    - from: "/chat page"
      to: "chat-api (P07) listConversations/getConversation/deleteConversation"
      via: "대화 관리"
      pattern: "listConversations\\|deleteConversation"
---

<objective>
사이드바 `/chat` 전용 페이지(D-13)를 만든다: 2-col(좌 280px 대화목록 + 종목 필터 + 새 대화 / 우 thread). 대화 선택·이어가기·삭제(확인 다이얼로그). FAB 시트는 목록 없이 가볍게 유지(D-13) — 목록/필터는 이 페이지만.

Purpose: 대화 관리 표면(D-13). 로그인 사용자의 종목별 히스토리 탐색. thread/메시지 렌더는 P09 컴포넌트 재사용.
Output: /chat 페이지 + 대화목록 + 삭제 다이얼로그 + 유닛테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-ai-analyst-chatbot/14-UI-SPEC.md
@.planning/phases/14-ai-analyst-chatbot/14-CONTEXT.md

<interfaces>
From webapp/src/lib/chat-api.ts (P07): `listConversations(stockCode?)`, `getConversation(id)`, `deleteConversation(id)`
From webapp/src/lib/chat-sse.ts (P07): `streamChat`
From webapp/src/components/chat/chat-thread.tsx + message-*.tsx (P09 — thread/메시지 재사용)
From webapp/src/components/chat/composer.tsx (P08 — composer 재사용)
From webapp/src/components/chat/chat-states.tsx (P08 — 로그인필요/빈 상태)
From webapp/src/components/ui/dialog.tsx (삭제 확인) + webapp middleware (route guard — /chat 로그인 필수, Phase 06.2 패턴)
UI-SPEC C10: 좌 280px 대화목록(종목 필터 select + 새 대화) / 우 thread, active 대화 `--accent` + `aria-current`, 종목 배지 pill, 모바일 1-col(<640px)
UI-SPEC C11 삭제: 트리거 🗑(aria-label="대화 삭제") → 다이얼로그 `이 대화를 삭제할까요?` / `삭제한 대화는 되돌릴 수 없어요.` / `삭제`(destructive)·`취소`(outline)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConversationList + DeleteConversationDialog + 유닛테스트</name>
  <files>webapp/src/components/chat/conversation-list.tsx, webapp/src/components/chat/delete-conversation-dialog.tsx, webapp/src/components/chat/__tests__/conversation-list.test.tsx</files>
  <read_first>
    - webapp/src/lib/chat-api.ts (P07 — list/delete)
    - webapp/src/components/ui/dialog.tsx (다이얼로그) + button.tsx (destructive/outline variant)
    - .planning/phases/14-ai-analyst-chatbot/14-UI-SPEC.md (C10 목록 + C11 삭제 + Copywriting verbatim + Color destructive)
  </read_first>
  <behavior>
    - Test 1: ConversationList — listConversations 결과를 updated_at desc 로 렌더, active 대화 aria-current
    - Test 2: ConversationList — 종목 필터 select 변경 → listConversations(stockCode) 재조회
    - Test 3: ConversationList — 🗑 클릭 → DeleteConversationDialog open (aria-label "대화 삭제")
    - Test 4: DeleteConversationDialog — `삭제` 클릭 → deleteConversation(id) 호출 + onDeleted 콜백. `취소` → 닫힘, 미삭제
  </behavior>
  <action>
    - `ConversationList` — `'use client'`. 대화 배열 렌더(제목 + 종목 배지 pill + 타임스탬프 caption). active 대화 `--accent` 배경 + `aria-current="true"`. 상단: 종목 필터 `select`(전체 / 종목별, D-13) + `＋ 새 대화` 버튼. 필터 변경 시 `listConversations(stockCode)` 재조회. 🗑 아이콘(`aria-label="대화 삭제"`) → 삭제 다이얼로그.
    - `DeleteConversationDialog` — shadcn Dialog. Copywriting verbatim: `이 대화를 삭제할까요?` / `삭제한 대화는 되돌릴 수 없어요.` / `삭제`(destructive `--destructive`) · `취소`(outline). 확인 시 `deleteConversation(id)` + onDeleted 콜백(목록 갱신).

    테스트: chat-api mock. Behavior 4케이스.
  </action>
  <verify>
    <automated>pnpm --filter webapp test conversation-list</automated>
  </verify>
  <acceptance_criteria>
    - `grep "listConversations" webapp/src/components/chat/conversation-list.tsx` 매칭
    - `grep "aria-current" webapp/src/components/chat/conversation-list.tsx` 매칭 (active 대화)
    - `grep "대화 삭제" webapp/src/components/chat/conversation-list.tsx` 매칭 (aria-label)
    - `grep "되돌릴 수 없어요" webapp/src/components/chat/delete-conversation-dialog.tsx` 매칭 (Copywriting)
    - `grep "deleteConversation" webapp/src/components/chat/delete-conversation-dialog.tsx` 매칭
    - `pnpm --filter webapp test conversation-list` — 4 tests pass
  </acceptance_criteria>
  <done>대화목록(필터/active/삭제 트리거) + 삭제 다이얼로그(destructive), 4 유닛테스트 green.</done>
</task>

<task type="auto">
  <name>Task 2: /chat 페이지 (2-col + thread 재사용) + route guard 확인</name>
  <files>webapp/src/app/chat/page.tsx</files>
  <read_first>
    - webapp/src/components/chat/conversation-list.tsx (이 plan Task 1)
    - webapp/src/components/chat/chat-thread.tsx + composer.tsx (P08/P09 재사용)
    - webapp/src/components/chat/chat-states.tsx (P08 — 로그인 필요/빈 상태)
    - webapp/src/lib/chat-api.ts + chat-sse.ts (P07)
    - webapp/src/app/watchlist/page.tsx (Phase 06.2 로그인 필수 페이지 패턴 — middleware route guard 대상)
    - .planning/phases/14-ai-analyst-chatbot/14-UI-SPEC.md (C10 2-col grid → 1-col <640px, Interaction 반응형)
  </read_first>
  <action>
    - `/chat` 페이지 — 2-col grid(좌 280px `ConversationList` / 우 thread + composer). `<640px` 1-col(모바일, D-13). 대화 선택 → `getConversation(id)` 로 messages 로드 → `chat-thread`(P09) 렌더. composer(P08) 로 이어가기 → `streamChat({conversationId})`. `＋ 새 대화` → conversationId 리셋.
    - 비로그인 → `chat-states` 로그인 필요(D-01). middleware route guard 가 `/chat` 를 로그인 필수 경로에 포함하는지 확인(Phase 06.2 matcher — 미포함 시 추가). 빈 상태(대화 없음) → `무엇이든 물어보세요` + 예시 프롬프트 칩.
    - FAB 시트와 컴포넌트 재사용(thread/message/composer 공유) — 코드 중복 최소화.
    - 종목 컨텍스트 없는 일반 대화(stockCode NULL)가 이 페이지의 기본. 종목 필터로 종목별 대화 탐색(D-13).
  </action>
  <verify>
    <automated>pnpm --filter webapp exec tsc --noEmit &amp;&amp; pnpm --filter webapp build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f webapp/src/app/chat/page.tsx` 존재
    - `grep "ConversationList" webapp/src/app/chat/page.tsx` 매칭
    - `grep "getConversation\|streamChat" webapp/src/app/chat/page.tsx` 매칭 (이어가기)
    - `grep "ChatThread\|chat-thread" webapp/src/app/chat/page.tsx` 매칭 (재사용)
    - webapp typecheck + build exit 0
  </acceptance_criteria>
  <done>/chat 2-col 페이지 + thread/composer 재사용 + route guard, build 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /chat 페이지 → 대화 API | 로그인 사용자의 대화 목록/삭제 요청 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01b | IDOR | deleteConversation / getConversation | mitigate | 서버 assertConversationOwner(P03/P06) — 클라 목록은 본인 것만(서버 WHERE user_id). middleware route guard /chat 로그인 필수 |
| T-14-11 | (데이터 손실) 실수 삭제 | DeleteConversationDialog | mitigate | 확인 다이얼로그 + "되돌릴 수 없어요" 고지(hard delete, messages FK CASCADE) |
</threat_model>

<verification>
- conversation-list 4 유닛테스트
- webapp typecheck + build (/chat 페이지)
</verification>

<success_criteria>
- /chat 2-col 대화 관리(목록/필터/이어가기)
- 삭제 확인 다이얼로그(destructive)
- 모바일 1-col + 로그인 게이트
</success_criteria>

<output>
After completion, create `.planning/phases/14-ai-analyst-chatbot/14-10-SUMMARY.md`
</output>
