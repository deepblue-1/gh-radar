---
phase: 14-ai-analyst-chatbot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260702170000_chat_conversations.sql
autonomous: false
requirements: [CHAT-01]
must_haves:
  truths:
    - "로그인 사용자별 대화(conversations)와 메시지(messages)가 Supabase에 영속 저장된다"
    - "타 사용자의 대화/메시지는 RLS로 읽기·쓰기 모두 차단된다"
    - "conversations는 stock_code로 종목별 필터가 가능하다 (NULL=일반 대화)"
  artifacts:
    - path: "supabase/migrations/20260702170000_chat_conversations.sql"
      provides: "conversations + messages 테이블 + RLS 8정책 + 인덱스"
      contains: "CREATE TABLE conversations"
  key_links:
    - from: "messages.conversation_id"
      to: "conversations.id"
      via: "FK ON DELETE CASCADE"
      pattern: "REFERENCES conversations\\(id\\) ON DELETE CASCADE"
    - from: "RLS messages 정책"
      to: "conversations.user_id"
      via: "EXISTS 서브쿼리"
      pattern: "EXISTS \\(SELECT 1 FROM conversations"
---

<objective>
로그인 사용자별·종목별 대화 영속화의 데이터 기반을 만든다. `conversations`/`messages` 두 테이블을 watchlists RLS 패턴(TO authenticated, `auth.uid() = user_id`)을 mirror 하여 생성하고, production DB에 push 한다.

Purpose: D-02(히스토리 서버 전담) + D-03(종목별 이어가기)의 저장 계층. 서비스롤은 RLS bypass 라 서버가 write 전담하지만, RLS는 defense-in-depth 방어선(직접 PostgREST 접근 시 IDOR 차단).
Output: 마이그레이션 1개 + production 적용.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ai-analyst-chatbot/14-CONTEXT.md
@.planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md

<interfaces>
<!-- watchlists RLS 4정책 패턴 (mirror 대상). home_theme_snapshots 는 공개 테이블이라 TO anon,authenticated 였으나 conversations/messages 는 비공개 → TO authenticated 만. -->
From supabase/migrations/20260416120000_watchlists.sql:
```sql
ALTER TABLE watchlists ENABLE ROW LEVEL SECURITY;
CREATE POLICY "auth_select_own_watchlists" ON watchlists FOR SELECT TO authenticated USING (auth.uid() = user_id);
CREATE POLICY "auth_insert_own_watchlists" ON watchlists FOR INSERT TO authenticated WITH CHECK (auth.uid() = user_id);
CREATE POLICY "auth_update_own_watchlists" ON watchlists FOR UPDATE TO authenticated USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);
CREATE POLICY "auth_delete_own_watchlists" ON watchlists FOR DELETE TO authenticated USING (auth.uid() = user_id);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: conversations + messages 마이그레이션 SQL 작성</name>
  <files>supabase/migrations/20260702170000_chat_conversations.sql</files>
  <read_first>
    - supabase/migrations/20260416120000_watchlists.sql (RLS 4정책 + FK CASCADE 패턴 — mirror 원본)
    - supabase/migrations/20260701123000_home_theme_snapshots.sql (RLS lesson 주석 스타일 참고)
    - .planning/phases/14-ai-analyst-chatbot/14-RESEARCH.md (Pattern 4 스키마 — verbatim 채택)
  </read_first>
  <action>
    아래 SQL 을 정확히 이 내용으로 작성한다 (RESEARCH Pattern 4 확정 스키마). `BEGIN;`/`COMMIT;` 로 감싼다.

    ```sql
    BEGIN;

    -- conversations: 로그인 사용자별 대화. stock_code NULL=일반(/chat), 값=종목상세 컨텍스트(D-03).
    CREATE TABLE conversations (
      id          uuid PRIMARY KEY DEFAULT gen_random_uuid(),
      user_id     uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
      stock_code  text REFERENCES stocks(code) ON DELETE SET NULL,
      title       text,
      created_at  timestamptz NOT NULL DEFAULT now(),
      updated_at  timestamptz NOT NULL DEFAULT now()
    );
    CREATE INDEX idx_conversations_user_stock ON conversations (user_id, stock_code, updated_at DESC);

    -- messages: 대화 내 메시지. content=렌더용 마크다운 텍스트, blocks=미니카드/차트/citation 부가물(D-07/08/10).
    -- tool_use/tool_result 원본은 저장하지 않음 (RESEARCH Pitfall 3 — 텍스트 스냅샷만).
    CREATE TABLE messages (
      id              uuid PRIMARY KEY DEFAULT gen_random_uuid(),
      conversation_id uuid NOT NULL REFERENCES conversations(id) ON DELETE CASCADE,
      role            text NOT NULL CHECK (role IN ('user','assistant')),
      content         text NOT NULL,
      blocks          jsonb,
      created_at      timestamptz NOT NULL DEFAULT now()
    );
    CREATE INDEX idx_messages_conversation ON messages (conversation_id, created_at);

    ALTER TABLE conversations ENABLE ROW LEVEL SECURITY;
    ALTER TABLE messages ENABLE ROW LEVEL SECURITY;

    -- conversations: watchlists mirror — TO authenticated, auth.uid() = user_id (비공개, anon 접근 불가가 의도)
    CREATE POLICY "auth_select_own_conversations" ON conversations FOR SELECT TO authenticated USING (auth.uid() = user_id);
    CREATE POLICY "auth_insert_own_conversations" ON conversations FOR INSERT TO authenticated WITH CHECK (auth.uid() = user_id);
    CREATE POLICY "auth_update_own_conversations" ON conversations FOR UPDATE TO authenticated USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);
    CREATE POLICY "auth_delete_own_conversations" ON conversations FOR DELETE TO authenticated USING (auth.uid() = user_id);

    -- messages: user_id 없음 → conversation 소유권을 EXISTS 서브쿼리로 (4정책 모두 동일 패턴)
    CREATE POLICY "auth_select_own_messages" ON messages FOR SELECT TO authenticated
      USING (EXISTS (SELECT 1 FROM conversations c WHERE c.id = conversation_id AND c.user_id = auth.uid()));
    CREATE POLICY "auth_insert_own_messages" ON messages FOR INSERT TO authenticated
      WITH CHECK (EXISTS (SELECT 1 FROM conversations c WHERE c.id = conversation_id AND c.user_id = auth.uid()));
    CREATE POLICY "auth_update_own_messages" ON messages FOR UPDATE TO authenticated
      USING (EXISTS (SELECT 1 FROM conversations c WHERE c.id = conversation_id AND c.user_id = auth.uid()));
    CREATE POLICY "auth_delete_own_messages" ON messages FOR DELETE TO authenticated
      USING (EXISTS (SELECT 1 FROM conversations c WHERE c.id = conversation_id AND c.user_id = auth.uid()));

    COMMIT;
    ```

    파일 상단에 Phase 14 / D-02 / watchlists mirror / RESEARCH Pattern 4 근거 주석을 단다.
    RPC 없음(plain table read/write) → REVOKE 불요 (home_theme_snapshots 선례, RESEARCH Pattern 4 명시). anon 은 정책 미부여 = default-deny (비공개 테이블 의도).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260702170000_chat_conversations.sql &amp;&amp; grep -c "CREATE POLICY" supabase/migrations/20260702170000_chat_conversations.sql | grep -q "^8$" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE POLICY" supabase/migrations/20260702170000_chat_conversations.sql` == 8
    - `grep -c "TO authenticated" ...` == 8 (모든 정책이 authenticated role 명시)
    - `grep "TO anon" ...` == 결과 없음 (비공개 테이블 — anon 정책 금지)
    - `grep "EXISTS (SELECT 1 FROM conversations" ...` 4회 매칭 (messages 4정책)
    - `grep "ON DELETE CASCADE" ...` conversations.user_id + messages.conversation_id 2회
    - `grep "ON DELETE SET NULL" ...` conversations.stock_code 1회
  </acceptance_criteria>
  <done>SQL 파일 존재, 8 RLS 정책(TO authenticated), FK CASCADE/SET NULL, 2 인덱스 포함.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] supabase db push</name>
  <action>Task 1 의 마이그레이션 파일 완성 후 production Supabase 에 `supabase db push` 로 적용한다. push 없이는 build/type check 가 통과해도 실제 DB 에 테이블이 없어 verification 이 false-positive 가 되므로 [BLOCKING]. 아래 how-to-verify 절차대로 수행.</action>
  <what-built>conversations/messages 마이그레이션을 production Supabase에 적용.</what-built>
  <how-to-verify>
    1. `SUPABASE_ACCESS_TOKEN` 환경변수 확인 (비대화형 push). 프로젝트 메모리 `reference_krx_auth_key`/기존 phase 기록에서 토큰 위치 먼저 확인 — 사용자에게 재요청 금지.
    2. `supabase db push` 실행 (repo root). 마이그레이션 `20260702170000_chat_conversations.sql` 적용 확인.
    3. 검증: Supabase SQL editor 또는 `supabase db remote` 로 `SELECT count(*) FROM conversations;` 가 0 반환(테이블 존재), `SELECT tablename, policyname FROM pg_policies WHERE tablename IN ('conversations','messages');` 가 8행 반환.
    4. build/type check 는 이 push 없이도 통과하므로(타입은 config 기반), push 완료가 verification 의 선결 조건임 — false-positive 방지.
  </how-to-verify>
  <resume-signal>push 완료 + pg_policies 8행 확인 후 "applied" 입력, 또는 이슈 기술</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgREST(anon/authenticated) → conversations/messages | 로그인 사용자가 브라우저에서 직접 Supabase 접근 시 RLS 통과 필요 |
| 서버(service_role) → 테이블 | RLS bypass — 서버가 `WHERE user_id` 명시 필터로 전담 write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure / Tampering (IDOR) | conversations/messages RLS | mitigate | `TO authenticated` + `auth.uid()=user_id` (conversations) / EXISTS 서브쿼리(messages). 직접 PostgREST 접근 시 타인 대화 차단 (defense-in-depth — 서버는 별도 명시 필터) |
| T-14-13 | Elevation (default-deny 회피 실수) | RLS role 명시 | mitigate | 모든 정책 `TO authenticated` 명시. anon 정책 미부여 = 비로그인 접근 불가 (비공개 테이블 의도). watchlists 선례와 동일 |
</threat_model>

<verification>
- 마이그레이션 파일 8 RLS 정책 + FK + 인덱스 (grep)
- production push 후 pg_policies 8행 (checkpoint)
</verification>

<success_criteria>
- conversations/messages 테이블 production 존재
- RLS 8정책 (conversations 4 + messages 4) 적용, 모두 TO authenticated
- messages FK ON DELETE CASCADE, conversations.stock_code SET NULL
</success_criteria>

<output>
After completion, create `.planning/phases/14-ai-analyst-chatbot/14-01-SUMMARY.md`
</output>
