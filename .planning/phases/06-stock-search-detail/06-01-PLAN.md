---
phase: 06-stock-search-detail
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - webapp/package.json
  - webapp/playwright.config.ts
  - webapp/tests/setup.ts
  - webapp/vitest.config.ts
  - webapp/e2e/fixtures/stocks.ts
  - webapp/src/components/ui/command.tsx
  - webapp/src/components/ui/dialog.tsx
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03]
must_haves:
  truths:
    - "vitest 가 jsdom + RTL cleanup + matchMedia/ResizeObserver 모크를 로드한다"
    - "playwright test --list 가 성공한다 (chromium 프로젝트 감지)"
    - "shadcn command 블록이 webapp/src/components/ui/command.tsx 로 설치되어 있다"
    - "e2e 픽스처가 정상 종목(005930) · 잘못된 코드(INVALID) · null 가격 케이스를 제공한다"
  artifacts:
    - path: "webapp/src/components/ui/command.tsx"
      provides: "shadcn Command/CommandDialog export"
    - path: "webapp/src/components/ui/dialog.tsx"
      provides: "radix Dialog wrapper (CommandDialog 의존성)"
    - path: "webapp/playwright.config.ts"
      provides: "chromium 프로젝트 + baseURL + webServer=next dev"
    - path: "webapp/tests/setup.ts"
      provides: "matchMedia + ResizeObserver mock, RTL cleanup"
    - path: "webapp/e2e/fixtures/stocks.ts"
      provides: "e2e 픽스처 (samsung/INVALID/null-price)"
  key_links:
    - from: "webapp/vitest.config.ts"
      to: "webapp/tests/setup.ts"
      via: "test.setupFiles"
      pattern: "setupFiles"
    - from: "webapp/package.json"
      to: "cmdk"
      via: "dependency 추가"
      pattern: "\"cmdk\""
---

<objective>
Phase 6 Wave 0 — 테스트 인프라 (vitest setup, playwright, axe) 와 shadcn `command` 블록 설치를 선행한다. 이후 모든 plan 의 `<automated>` 검증이 실행 가능해진다.

Purpose: 06-VALIDATION.md Wave 0 Requirements 6항목을 1:1 커버. Nyquist 규약상 테스트 파일이 없는 상태에서 다음 wave 를 시작할 수 없다.
Output: 테스트 러너 · 픽스처 · shadcn command 컴포넌트 · dev deps 전부 설치 완료.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-stock-search-detail/06-CONTEXT.md
@.planning/phases/06-stock-search-detail/06-RESEARCH.md
@.planning/phases/06-stock-search-detail/06-VALIDATION.md
@webapp/vitest.config.ts
@webapp/package.json

<interfaces>
<!-- Phase 5 에서 이미 확정된 테스트 패턴 (일관성 유지 필수) -->

From webapp/vitest.config.ts (현재 값):
```ts
// environment: 'jsdom', globals: true, include: ['src/**/*.test.{ts,tsx}']
// alias: { '@': path.resolve(__dirname, './src') }
// ❗ setupFiles 현재 미설정 — 본 plan 에서 추가
```

From webapp/src/hooks/use-polling.test.ts (Phase 5 모킹 패턴):
```ts
// vi.fn() 으로 fetcher 직접 모킹 → msw 아님. Phase 6도 vi.mock('@/lib/stock-api') 채택.
```

From webapp/src/components/ui/badge.tsx:
```ts
// variant: 'default' | 'secondary' | 'outline' | 'up' | 'down' | 'flat'
// KOSPI/KOSDAQ 전용 variant 없음 → 'outline' + 텍스트 "KOSPI"/"KOSDAQ" 로 표기
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn `command` 블록 설치 + cmdk dependency</name>
  <read_first>
    - webapp/package.json (기존 deps 확인, cmdk 부재 확인)
    - webapp/components.json (radix-nova preset + cssVariables=true 유지)
    - .planning/phases/06-stock-search-detail/06-RESEARCH.md §"Standard Stack > Supporting" 및 §"설치 명령"
    - .planning/phases/06-stock-search-detail/06-UI-SPEC.md §"Design System" (cmdk shadcn 블록 + lucide-react 아이콘)
  </read_first>
  <action>
    1) `cd /Users/alex/repos/gh-radar/webapp && pnpm dlx shadcn@latest add command`
       - 프롬프트에서 `dialog` 블록 덮어쓰기 질문 시 **"yes"** (CommandDialog 의존성)
    2) 산출물 확인:
       - `webapp/src/components/ui/command.tsx` 생성됨 (Command, CommandDialog, CommandInput, CommandList, CommandItem, CommandEmpty, CommandLoading, CommandGroup, CommandSeparator export)
       - `webapp/src/components/ui/dialog.tsx` 생성됨 (radix Dialog wrapper)
       - `webapp/package.json` dependencies 에 `cmdk` 추가됨 (버전 `^1.1.1` 또는 그 이상 — latest)
    3) 금지: CommandDialog 를 커스텀 래핑하지 말 것. shadcn 기본 export 를 GlobalSearch 에서 직접 import.
    4) pnpm install 이 shadcn CLI 에 의해 자동 수행되지 않으면 `pnpm install --filter @gh-radar/webapp` 로 보완.
    5) 타입 체크: `pnpm --filter @gh-radar/webapp exec tsc --noEmit` 이 에러 0 이어야 한다.
  </action>
  <verify>
    <automated>test -f webapp/src/components/ui/command.tsx &amp;&amp; test -f webapp/src/components/ui/dialog.tsx &amp;&amp; grep -q '"cmdk"' webapp/package.json &amp;&amp; pnpm --filter @gh-radar/webapp exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/src/components/ui/command.tsx` 파일 존재
    - `webapp/src/components/ui/dialog.tsx` 파일 존재
    - `grep -q '"cmdk"' webapp/package.json` → exit 0
    - `grep -E "export .* CommandDialog" webapp/src/components/ui/command.tsx` → 매치 1건 이상
    - `pnpm --filter @gh-radar/webapp exec tsc --noEmit` → exit 0
  </acceptance_criteria>
  <done>cmdk 설치 + command/dialog 컴포넌트 파일 존재 + tsc 통과</done>
</task>

<task type="auto">
  <name>Task 2: vitest setup 파일 + e2e 픽스처 추가</name>
  <read_first>
    - webapp/vitest.config.ts (현재: setupFiles 미설정)
    - webapp/src/hooks/use-polling.test.ts (Phase 5 fakeTimers 패턴 참고)
    - .planning/phases/06-stock-search-detail/06-VALIDATION.md §"Wave 0 Requirements"
  </read_first>
  <action>
    1) 신규 `webapp/tests/setup.ts` 생성:
       ```ts
       import '@testing-library/jest-dom/vitest';
       import { afterEach } from 'vitest';
       import { cleanup } from '@testing-library/react';

       afterEach(() => cleanup());

       // cmdk CommandDialog 는 radix Dialog 내부에서 ResizeObserver/matchMedia 를 사용.
       if (typeof window !== 'undefined') {
         if (!window.matchMedia) {
           // @ts-expect-error — jsdom polyfill
           window.matchMedia = (query: string) => ({
             matches: false,
             media: query,
             onchange: null,
             addListener: () => {},
             removeListener: () => {},
             addEventListener: () => {},
             removeEventListener: () => {},
             dispatchEvent: () => false,
           });
         }
         if (!('ResizeObserver' in window)) {
           // @ts-expect-error — jsdom polyfill
           window.ResizeObserver = class {
             observe() {}
             unobserve() {}
             disconnect() {}
           };
         }
         // cmdk scroll-into-view 호출 대응
         if (!Element.prototype.scrollIntoView) {
           Element.prototype.scrollIntoView = () => {};
         }
       }
       ```
    2) `webapp/vitest.config.ts` 수정 (Edit tool 권장) — test block 에 `setupFiles: ['./tests/setup.ts']` 추가:
       ```ts
       test: {
         environment: 'jsdom',
         globals: true,
         setupFiles: ['./tests/setup.ts'],
         include: ['src/**/*.test.{ts,tsx}'],
       }
       ```
    3) dev deps 설치 (workspace root):
       `pnpm --filter @gh-radar/webapp add -D @testing-library/jest-dom@^6.6.3 @testing-library/user-event@^14.5.2`
    4) 신규 `webapp/e2e/fixtures/stocks.ts` 생성 — 아래 정확 구조:
       ```ts
       import type { Stock } from '@gh-radar/shared';

       export const FIXTURE_SAMSUNG: Stock = {
         code: '005930', name: '삼성전자', market: 'KOSPI',
         price: 58700, changeAmount: 1200, changeRate: 2.09,
         volume: 15_324_000, tradeAmount: 900_000_000_000,
         open: 57500, high: 59000, low: 57200,
         marketCap: 350_400_000_000_000,
         upperLimit: 74750, lowerLimit: 40250,
         updatedAt: '2026-04-15T05:30:00.000Z',
       };

       export const FIXTURE_NULL_PRICE: Stock = {
         ...FIXTURE_SAMSUNG,
         code: '999999', name: '가상거래정지종목',
         price: 0, open: 0, high: 0, low: 0, marketCap: 0,
         upperLimit: 0, lowerLimit: 0,
       };

       export const INVALID_CODE = 'INVALID'; // 서버 regex 통과하지만 404
       export const MALFORMED_CODE = '!!!'; // 클라 regex 에서 notFound()
       ```
    5) 검증: `pnpm --filter @gh-radar/webapp test -- --run` exit 0 (no tests 에서 --passWithNoTests 가 이미 설정됨).
  </action>
  <verify>
    <automated>test -f webapp/tests/setup.ts &amp;&amp; test -f webapp/e2e/fixtures/stocks.ts &amp;&amp; grep -q "setupFiles" webapp/vitest.config.ts &amp;&amp; pnpm --filter @gh-radar/webapp test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "setupFiles: \['./tests/setup.ts'\]" webapp/vitest.config.ts` → 매치
    - `grep -q "matchMedia" webapp/tests/setup.ts` → 매치
    - `grep -q "ResizeObserver" webapp/tests/setup.ts` → 매치
    - `grep -q "FIXTURE_SAMSUNG" webapp/e2e/fixtures/stocks.ts` → 매치
    - `grep -q '"@testing-library/jest-dom"' webapp/package.json` → 매치 (devDependencies)
    - `pnpm --filter @gh-radar/webapp test -- --run` → exit 0
  </acceptance_criteria>
  <done>vitest setup 활성 + 픽스처 파일 생성 + jest-dom/user-event 설치</done>
</task>

<task type="auto">
  <name>Task 3: playwright config + axe 설치</name>
  <read_first>
    - webapp/package.json (scripts 섹션 — test 스크립트 확인)
    - webapp/next.config.ts (dev 서버 포트 기본 3000)
    - .planning/phases/06-stock-search-detail/06-VALIDATION.md §"Test Infrastructure" 및 §"Wave 0 Requirements"
  </read_first>
  <action>
    1) dev deps 설치 (workspace root):
       `pnpm --filter @gh-radar/webapp add -D @playwright/test@^1.49.0 @axe-core/playwright@^4.10.0`
    2) 브라우저 설치: `pnpm --filter @gh-radar/webapp exec playwright install chromium`
    3) 신규 `webapp/playwright.config.ts` 생성 — 아래 정확 내용:
       ```ts
       import { defineConfig, devices } from '@playwright/test';

       /**
        * Phase 6 Wave 0 — E2E 설정.
        * - baseURL: http://localhost:3000 (Next dev)
        * - webServer: `pnpm dev` 자동 기동 (reuseExistingServer CI 외 true)
        * - project: chromium 단일
        * - testDir: e2e/specs
        */
       export default defineConfig({
         testDir: './e2e/specs',
         fullyParallel: true,
         forbidOnly: !!process.env.CI,
         retries: process.env.CI ? 1 : 0,
         workers: process.env.CI ? 1 : undefined,
         reporter: [['list']],
         use: {
           baseURL: 'http://localhost:3000',
           trace: 'retain-on-failure',
         },
         projects: [
           { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
         ],
         webServer: {
           command: 'pnpm dev',
           url: 'http://localhost:3000',
           reuseExistingServer: !process.env.CI,
           timeout: 120_000,
         },
       });
       ```
    4) `webapp/e2e/specs/` 디렉토리 생성 + `.gitkeep` 추가 (Wave 3 에서 실제 spec 작성).
    5) `webapp/package.json` scripts 에 추가 (Edit):
       ```json
       "test:e2e": "playwright test"
       ```
    6) 검증: `pnpm --filter @gh-radar/webapp exec playwright test --list` → exit 0 (spec 0건이어도 --list 는 성공).
  </action>
  <verify>
    <automated>test -f webapp/playwright.config.ts &amp;&amp; test -d webapp/e2e/specs &amp;&amp; grep -q '"@playwright/test"' webapp/package.json &amp;&amp; grep -q '"@axe-core/playwright"' webapp/package.json &amp;&amp; pnpm --filter @gh-radar/webapp exec playwright test --list</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/playwright.config.ts` 존재
    - `grep -q "testDir: './e2e/specs'" webapp/playwright.config.ts` → 매치
    - `grep -q "baseURL: 'http://localhost:3000'" webapp/playwright.config.ts` → 매치
    - `grep -q "chromium" webapp/playwright.config.ts` → 매치
    - `grep -q '"@playwright/test"' webapp/package.json` → 매치
    - `grep -q '"@axe-core/playwright"' webapp/package.json` → 매치
    - `pnpm --filter @gh-radar/webapp exec playwright test --list` → exit 0
  </acceptance_criteria>
  <done>playwright chromium 설치 + config 작성 + axe-core 설치 + --list 통과</done>
</task>

</tasks>

<threat_model>
이번 phase 는 조회 전용 UI + 이미 배포된 백엔드 API 소비. 신규 trust boundary 없음. ASVS L1 기준으로 mitigate 요건 없음. `security_enforcement: false` 취급.
</threat_model>

<verification>
1. `pnpm --filter @gh-radar/webapp test -- --run` → PASS (0 tests allowed via --passWithNoTests)
2. `pnpm --filter @gh-radar/webapp exec playwright test --list` → PASS
3. `pnpm --filter @gh-radar/webapp exec tsc --noEmit` → PASS
4. webapp/package.json diff 에 cmdk · @playwright/test · @axe-core/playwright · @testing-library/jest-dom · @testing-library/user-event 추가됨
</verification>

<success_criteria>
- 06-VALIDATION.md §"Wave 0 Requirements" 6항목 모두 ✅
- Wave 1+ 의 `<automated>` 명령이 실행 가능한 환경 구성 완료
- 어떤 새 production 코드도 작성하지 않음 (인프라 전용)
</success_criteria>

<output>
`.planning/phases/06-stock-search-detail/06-01-SUMMARY.md` 생성
</output>
