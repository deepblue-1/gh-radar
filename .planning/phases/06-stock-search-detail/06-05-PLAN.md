---
phase: 06-stock-search-detail
plan: 05
type: execute
wave: 3
depends_on: [06-04]
files_modified:
  - webapp/src/app/stocks/[code]/page.tsx
  - webapp/src/app/stocks/[code]/not-found.tsx
  - webapp/src/app/stocks/[code]/error.tsx
  - webapp/src/app/stocks/[code]/__tests__/not-found.test.tsx
  - webapp/src/app/stocks/[code]/__tests__/error.test.tsx
autonomous: true
requirements: [SRCH-03]
must_haves:
  truths:
    - "/stocks/[code] 라우트가 동작하며 code 는 ^[A-Za-z0-9]{1,10}$ regex 통과 시에만 StockDetailClient 마운트"
    - "regex 실패 시 즉시 notFound() → app/stocks/[code]/not-found.tsx 렌더"
    - "not-found.tsx 가 UI-SPEC 404 카피 + '스캐너로 돌아가기' 버튼(→ /scanner) 노출"
    - "error.tsx 가 ApiClientError.message + '다시 시도' 버튼(Next reset()) 노출"
    - "page.tsx 는 'use client' + React.use(params) 로 Next 15 Promise params 처리"
  artifacts:
    - path: "webapp/src/app/stocks/[code]/page.tsx"
      provides: "동적 라우트 엔트리 + StockDetailClient 마운트"
    - path: "webapp/src/app/stocks/[code]/not-found.tsx"
      provides: "404 전용 UI (UI-SPEC Copywriting)"
    - path: "webapp/src/app/stocks/[code]/error.tsx"
      provides: "error boundary UI (reset 버튼)"
  key_links:
    - from: "webapp/src/app/stocks/[code]/page.tsx"
      to: "webapp/src/components/stock/stock-detail-client.tsx"
      via: "<StockDetailClient code={code} />"
      pattern: "<StockDetailClient"
    - from: "webapp/src/app/stocks/[code]/not-found.tsx"
      to: "/scanner"
      via: "next/link"
      pattern: "href=\"/scanner\""
---

<objective>
Plan 04 의 StockDetailClient 를 Next.js 15 동적 라우트 `/stocks/[code]` 에 배선 + 전용 `not-found.tsx` · `error.tsx` 를 작성.

Purpose: `use(params)` 패턴으로 Next 15 Promise params 처리 + code regex 클라 검증 + 404/5xx 분리.
Output: 3개 라우트 파일 + 2개 단위 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-stock-search-detail/06-CONTEXT.md
@.planning/phases/06-stock-search-detail/06-RESEARCH.md
@.planning/phases/06-stock-search-detail/06-UI-SPEC.md
@webapp/src/app/not-found.tsx
@webapp/src/app/error.tsx
@webapp/src/components/layout/app-shell.tsx
@webapp/src/components/stock/stock-detail-client.tsx

<interfaces>
From webapp/src/components/stock/stock-detail-client.tsx (Plan 04):
```ts
export function StockDetailClient({ code }: { code: string }): JSX.Element;
```

From webapp/src/components/layout/app-shell.tsx:
```tsx
<AppShell hideSidebar>{children}</AppShell>
// hideSidebar=true → 헤더 + 단일 main 컬럼 (Phase 4 v1 기본)
```

From Next.js 15 App Router:
```ts
// page.tsx props: { params: Promise<{ code: string }> }
// 'use client' 에서: const { code } = use(params);
// error.tsx props: { error: Error & { digest?: string }, reset: () => void }
// not-found.tsx: props 없음
```

Code regex (서버 검증과 동일):
```ts
const CODE_RE = /^[A-Za-z0-9]{1,10}$/;
```
</interfaces>

<ui_spec_tokens>
<!-- 06-UI-SPEC.md 정확 카피 -->

404 (not-found.tsx):
- 제목: `종목을 찾을 수 없습니다`
- 본문: `종목코드를 다시 확인해 주세요. (영문/숫자 1~10자, 예: 005930)`
- CTA: `스캐너로 돌아가기` (Primary Button variant="default", → /scanner)

Error (error.tsx):
- 제목: `데이터를 불러오지 못했습니다`
- 본문: `{error.message} 잠시 후 다시 시도해 주세요.`
- CTA: `다시 시도` (onClick=reset)

Typography:
- 제목: `text-[length:var(--t-h2)] font-semibold` (Heading 24)
- 본문: `text-[length:var(--t-sm)] text-[var(--muted-fg)]`
- 버튼 표준 (shadcn Button)

Spacing: `space-y-4` 섹션 내부, `py-12` 페이지 상하 여백
</ui_spec_tokens>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx + not-found.tsx + error.tsx 라우트 작성</name>
  <read_first>
    - webapp/src/app/not-found.tsx (기존 루트 404 — 패턴 참고)
    - webapp/src/app/error.tsx (기존 루트 error boundary — 패턴 참고)
    - .planning/phases/06-stock-search-detail/06-RESEARCH.md §"Pattern 4: 동적 라우트 Next 15 params Promise"
    - .planning/phases/06-stock-search-detail/06-CONTEXT.md §D6
  </read_first>
  <action>
    1) 신규 `webapp/src/app/stocks/[code]/page.tsx` — 정확 구조:
       ```tsx
       'use client';
       import { use } from 'react';
       import { notFound } from 'next/navigation';
       import { AppShell } from '@/components/layout/app-shell';
       import { CenterShell } from '@/components/layout/center-shell';
       import { StockDetailClient } from '@/components/stock/stock-detail-client';

       const CODE_RE = /^[A-Za-z0-9]{1,10}$/;

       /**
        * Stock Detail Route — Phase 6 SRCH-03.
        * - 'use client' + React.use(params) (Next 15 Promise params, R4)
        * - code regex 클라 1차 검증 실패 시 즉시 notFound() → not-found.tsx
        * - StockDetailClient 가 실제 fetch/refresh/404/error 분기 처리
        */
       export default function StockPage({
         params,
       }: {
         params: Promise<{ code: string }>;
       }) {
         const { code } = use(params);
         if (!CODE_RE.test(code)) notFound();
         return (
           <AppShell hideSidebar>
             <CenterShell>
               <StockDetailClient code={code} />
             </CenterShell>
           </AppShell>
         );
       }
       ```
       **주의**: `webapp/src/components/layout/center-shell.tsx` 가 존재하지 않으면 `<main className="mx-auto w-full max-w-5xl px-6 py-8">` 로 대체 (executor 는 먼저 파일 존재 여부 `ls webapp/src/components/layout/` 로 확인 후 결정). CenterShell 이 존재하면 그대로 사용.

    2) 신규 `webapp/src/app/stocks/[code]/not-found.tsx`:
       ```tsx
       import Link from 'next/link';
       import { AppShell } from '@/components/layout/app-shell';
       import { Button } from '@/components/ui/button';

       /**
        * /stocks/[code] 전용 404 (Phase 6 D6).
        * - 잘못된 형식 code (regex 실패) 또는 서버 404 (미수집 종목) 진입 시 렌더
        */
       export default function StockNotFound() {
         return (
           <AppShell hideSidebar>
             <main className="mx-auto w-full max-w-md px-6 py-12">
               <section className="space-y-4 text-center">
                 <h1 className="text-[length:var(--t-h2)] font-semibold text-[var(--fg)]">
                   종목을 찾을 수 없습니다
                 </h1>
                 <p className="text-[length:var(--t-sm)] text-[var(--muted-fg)]">
                   종목코드를 다시 확인해 주세요. (영문/숫자 1~10자, 예: 005930)
                 </p>
                 <Button asChild>
                   <Link href="/scanner">스캐너로 돌아가기</Link>
                 </Button>
               </section>
             </main>
           </AppShell>
         );
       }
       ```
       **주의**: `Button asChild` 가 기존 `webapp/src/components/ui/button.tsx` 에서 지원되는지 확인. 미지원 시 `<Link href="/scanner" className="{button 클래스 문자열}">` 대체 또는 `<Button onClick={() => router.push('/scanner')}>` (이 경우 'use client' 추가).

    3) 신규 `webapp/src/app/stocks/[code]/error.tsx`:
       ```tsx
       'use client';
       import { useEffect } from 'react';
       import { AppShell } from '@/components/layout/app-shell';
       import { Button } from '@/components/ui/button';

       /**
        * /stocks/[code] error boundary (Phase 6 D6).
        * - StockDetailClient 에서 setState 대신 throw 된 에러만 여기로 도달 (Pitfall 5)
        * - 주로 렌더 중 unexpected error — fetch 에러는 DetailClient 에서 인라인 처리
        */
       export default function StockError({
         error,
         reset,
       }: {
         error: Error & { digest?: string };
         reset: () => void;
       }) {
         useEffect(() => {
           // eslint-disable-next-line no-console
           console.error('[gh-radar] stock detail error:', error);
         }, [error]);

         return (
           <AppShell hideSidebar>
             <main className="mx-auto w-full max-w-md px-6 py-12" role="alert">
               <section className="space-y-4 text-center">
                 <h1 className="text-[length:var(--t-h2)] font-semibold text-[var(--fg)]">
                   데이터를 불러오지 못했습니다
                 </h1>
                 <p className="text-[length:var(--t-sm)] text-[var(--muted-fg)]">
                   {error.message} 잠시 후 다시 시도해 주세요.
                 </p>
                 <Button onClick={reset}>다시 시도</Button>
               </section>
             </main>
           </AppShell>
         );
       }
       ```

    4) tsc + 전체 vitest 회귀 통과.
  </action>
  <verify>
    <automated>test -f webapp/src/app/stocks/\[code\]/page.tsx &amp;&amp; test -f webapp/src/app/stocks/\[code\]/not-found.tsx &amp;&amp; test -f webapp/src/app/stocks/\[code\]/error.tsx &amp;&amp; pnpm --filter @gh-radar/webapp exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 3개 파일 존재
    - `grep -q "'use client'" webapp/src/app/stocks/\\[code\\]/page.tsx` → 매치
    - `grep -q "use(params)" webapp/src/app/stocks/\\[code\\]/page.tsx` → 매치
    - `grep -q "CODE_RE" webapp/src/app/stocks/\\[code\\]/page.tsx` → 매치
    - `grep -q "^\\[A-Za-z0-9\\]{1,10}\\$" webapp/src/app/stocks/\\[code\\]/page.tsx` → 매치 (정규식 리터럴)
    - `grep -q "notFound()" webapp/src/app/stocks/\\[code\\]/page.tsx` → 매치
    - `grep -q "<StockDetailClient" webapp/src/app/stocks/\\[code\\]/page.tsx` → 매치
    - `grep -q "종목을 찾을 수 없습니다" webapp/src/app/stocks/\\[code\\]/not-found.tsx` → 매치
    - `grep -q "영문/숫자 1~10자" webapp/src/app/stocks/\\[code\\]/not-found.tsx` → 매치
    - `grep -q "스캐너로 돌아가기" webapp/src/app/stocks/\\[code\\]/not-found.tsx` → 매치
    - `grep -q "href=\"/scanner\"" webapp/src/app/stocks/\\[code\\]/not-found.tsx` → 매치
    - `grep -q "데이터를 불러오지 못했습니다" webapp/src/app/stocks/\\[code\\]/error.tsx` → 매치
    - `grep -q "다시 시도" webapp/src/app/stocks/\\[code\\]/error.tsx` → 매치
    - `grep -q "onClick={reset}" webapp/src/app/stocks/\\[code\\]/error.tsx` → 매치
    - tsc 통과
  </acceptance_criteria>
  <done>3개 라우트 파일 작성 + tsc 통과</done>
</task>

<task type="auto">
  <name>Task 2: not-found / error 단위 테스트 (RTL 렌더)</name>
  <read_first>
    - webapp/src/app/stocks/\[code\]/not-found.tsx (Task 1 산출물)
    - webapp/src/app/stocks/\[code\]/error.tsx (Task 1 산출물)
  </read_first>
  <action>
    1) 신규 `webapp/src/app/stocks/[code]/__tests__/not-found.test.tsx`:
       ```tsx
       import { describe, it, expect } from 'vitest';
       import { render, screen } from '@testing-library/react';
       import StockNotFound from '../not-found';

       // AppShell 은 next-themes Provider · 사이드바 등 의존 → 단위 테스트에서 mock
       vi.mock('@/components/layout/app-shell', () => ({
         AppShell: ({ children }: { children: React.ReactNode }) => <>{children}</>,
       }));

       describe('StockNotFound (/stocks/[code]/not-found.tsx)', () => {
         it('제목/본문/CTA 카피 정확 노출', () => {
           render(<StockNotFound />);
           expect(screen.getByRole('heading', { name: '종목을 찾을 수 없습니다' })).toBeInTheDocument();
           expect(screen.getByText(/영문\/숫자 1~10자, 예: 005930/)).toBeInTheDocument();
           const cta = screen.getByRole('link', { name: '스캐너로 돌아가기' });
           expect(cta).toHaveAttribute('href', '/scanner');
         });
       });
       ```

    2) 신규 `webapp/src/app/stocks/[code]/__tests__/error.test.tsx`:
       ```tsx
       import { describe, it, expect, vi } from 'vitest';
       import { render, screen } from '@testing-library/react';
       import userEvent from '@testing-library/user-event';
       import StockError from '../error';

       vi.mock('@/components/layout/app-shell', () => ({
         AppShell: ({ children }: { children: React.ReactNode }) => <>{children}</>,
       }));

       describe('StockError (/stocks/[code]/error.tsx)', () => {
         it('error.message + 재시도 버튼', async () => {
           const reset = vi.fn();
           const err = new Error('백엔드 서버 장애') as Error & { digest?: string };
           render(<StockError error={err} reset={reset} />);
           expect(screen.getByRole('heading', { name: '데이터를 불러오지 못했습니다' })).toBeInTheDocument();
           expect(screen.getByText(/백엔드 서버 장애/)).toBeInTheDocument();
           const btn = screen.getByRole('button', { name: '다시 시도' });
           await userEvent.click(btn);
           expect(reset).toHaveBeenCalledTimes(1);
         });
       });
       ```

    3) 실행: `pnpm --filter @gh-radar/webapp test -- --run src/app/stocks` → 2 tests pass.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp test -- --run src/app/stocks</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/src/app/stocks/\[code\]/__tests__/not-found.test.tsx` 존재
    - `webapp/src/app/stocks/\[code\]/__tests__/error.test.tsx` 존재
    - 2 tests 전부 pass
  </acceptance_criteria>
  <done>not-found / error 2 tests 통과</done>
</task>

</tasks>

<threat_model>
N/A (조회 전용 UI). code 는 클라 regex + 서버 regex 이중 검증.
</threat_model>

<verification>
- `pnpm --filter @gh-radar/webapp test -- --run` 전체 suite → PASS
- `pnpm --filter @gh-radar/webapp exec tsc --noEmit` → PASS
- `pnpm --filter @gh-radar/webapp build` → next build 성공 (라우트 등록 확인)
</verification>

<success_criteria>
- /stocks/005930 경로 빌드 타임 등록 확인 (`.next/server/app/stocks/[code]/page.js` 생성)
- /stocks/!!! (regex 실패) → notFound 브랜치
- /stocks/INVALID (regex 통과, 서버 404) → StockDetailClient 내 notFound() → not-found.tsx (Plan 04 테스트 Test 5 에서 이미 증명)
- UI-SPEC Copywriting 카피 문자열 정확 일치
</success_criteria>

<output>
`.planning/phases/06-stock-search-detail/06-05-SUMMARY.md` 생성
</output>
