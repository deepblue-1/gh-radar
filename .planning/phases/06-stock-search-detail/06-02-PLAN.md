---
phase: 06-stock-search-detail
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - webapp/src/lib/stock-api.ts
  - webapp/src/lib/__tests__/stock-api.test.ts
  - webapp/src/hooks/use-debounced-search.ts
  - webapp/src/hooks/use-debounced-search.test.ts
  - webapp/src/hooks/use-cmdk-shortcut.ts
  - webapp/src/hooks/use-cmdk-shortcut.test.ts
autonomous: true
requirements: [SRCH-01, SRCH-02]
must_haves:
  truths:
    - "searchStocks(q, signal) 이 GET /api/stocks/search?q=... 을 호출하고 Stock[] 을 반환한다"
    - "fetchStockDetail(code, signal) 이 GET /api/stocks/:code 를 호출하고 Stock 을 반환한다"
    - "useDebouncedSearch 가 300ms 후에만 fetcher 를 호출한다 (그 전 입력 변경은 취소)"
    - "useDebouncedSearch 가 새 입력 발생 시 in-flight request 를 abort 한다"
    - "useCmdKShortcut 이 mod+k keydown 시 toggle callback 을 실행한다 (Mac metaKey / Win ctrlKey 양측)"
  artifacts:
    - path: "webapp/src/lib/stock-api.ts"
      provides: "searchStocks, fetchStockDetail"
      exports: ["searchStocks", "fetchStockDetail"]
    - path: "webapp/src/hooks/use-debounced-search.ts"
      provides: "useDebouncedSearch hook"
      exports: ["useDebouncedSearch"]
    - path: "webapp/src/hooks/use-cmdk-shortcut.ts"
      provides: "mod+k 전역 단축키"
      exports: ["useCmdKShortcut"]
  key_links:
    - from: "webapp/src/lib/stock-api.ts"
      to: "webapp/src/lib/api.ts"
      via: "apiFetch 재사용"
      pattern: "import.*apiFetch.*from.*['\"]\\.\\/api['\"]"
    - from: "webapp/src/hooks/use-debounced-search.ts"
      to: "webapp/src/lib/stock-api.ts"
      via: "searchStocks 호출"
      pattern: "searchStocks\\("
---

<objective>
Phase 6 자동완성/상세 페이지에 공통으로 사용되는 **데이터 레이어 + 훅 2종** 을 구현한다. 이후 Plan 03 (GlobalSearch) 과 Plan 04 (StockDetailClient) 가 이 모듈을 import 한다.

Purpose: API 호출과 debounce/단축키 로직을 UI 컴포넌트와 분리하여 단위 테스트 가능한 pure 모듈로 고립.
Output: 3개 소스 + 3개 테스트 파일. 모두 vitest fakeTimers 로 결정론적으로 검증.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-stock-search-detail/06-CONTEXT.md
@.planning/phases/06-stock-search-detail/06-RESEARCH.md
@.planning/phases/06-stock-search-detail/06-VALIDATION.md
@webapp/src/lib/api.ts
@webapp/src/hooks/use-polling.ts
@webapp/src/hooks/use-polling.test.ts
@packages/shared/src/stock.ts

<interfaces>
<!-- 기존 인프라 — 그대로 사용 -->

From webapp/src/lib/api.ts:
```ts
export function apiFetch<T>(path: string, init?: ApiFetchInit): Promise<T>;
export class ApiClientError extends Error {
  readonly code: string; readonly status: number; readonly requestId?: string;
}
export interface ApiFetchInit extends Omit<RequestInit, 'signal'> {
  timeoutMs?: number;
  signal?: AbortSignal; // 외부 signal 과 8s 타임아웃이 합성됨
}
```

From @gh-radar/shared (packages/shared/src/stock.ts):
```ts
export type Market = 'KOSPI' | 'KOSDAQ';
export type Stock = {
  code: string; name: string; market: Market;
  price: number; changeAmount: number; changeRate: number;
  volume: number; tradeAmount: number;
  open: number; high: number; low: number;
  marketCap: number; upperLimit: number; lowerLimit: number;
  updatedAt: string;
};
```

From webapp/src/hooks/use-polling.ts (Phase 5 AbortController 패턴 — 참조만):
```ts
// controllerRef = useRef<AbortController|null>(null);
// 각 새 fetch 전 controllerRef.current?.abort(); 새 controller 생성.
```

From server/src/routes/stocks.ts (수정 금지 — 계약):
- GET /api/stocks/search?q=... → Stock[] (limit 20, name asc)
- GET /api/stocks/:code → Stock (404 → ApiClientError.status === 404)
- code regex: ^[A-Za-z0-9]{1,10}$
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: stock-api.ts + 단위 테스트 (vi.mock fetch)</name>
  <read_first>
    - webapp/src/lib/api.ts (apiFetch 시그니처, ApiClientError)
    - webapp/src/lib/scanner-api.ts (Phase 5 패턴 — import 경로·주석 스타일 참고)
    - .planning/phases/06-stock-search-detail/06-RESEARCH.md §"Code Examples > 예시 2: stock-api.ts"
  </read_first>
  <behavior>
    - Test 1: `searchStocks('삼성', signal)` 은 `apiFetch` 를 정확히 `'/api/stocks/search?q=삼성'` path 로 호출한다
    - Test 2: `searchStocks` 가 한글·공백·특수문자 (`&`, `%`) 를 URLSearchParams 로 정확히 인코딩한다
    - Test 3: `fetchStockDetail('005930', signal)` 은 `apiFetch` 를 `'/api/stocks/005930'` path 로 호출한다
    - Test 4: `fetchStockDetail` 은 code 를 `encodeURIComponent` 처리한다 (예: `encodeURIComponent('abc/x')` → `'abc%2Fx'`)
    - Test 5: signal 이 그대로 apiFetch init 에 전달된다
  </behavior>
  <action>
    1) 신규 `webapp/src/lib/stock-api.ts` — 정확한 구조:
       ```ts
       /**
        * Stock API wrapper (Phase 6 SRCH-01/02/03).
        *
        * - apiFetch 재사용: envelope 파싱 + X-Request-Id + 8s 타임아웃 + ApiClientError.
        * - searchStocks: GET /api/stocks/search?q=... (서버 정렬 name asc, limit 20 — 수정 금지)
        * - fetchStockDetail: GET /api/stocks/:code (404 시 ApiClientError.status === 404)
        */
       import type { Stock } from '@gh-radar/shared';
       import { apiFetch } from './api';

       export function searchStocks(q: string, signal: AbortSignal): Promise<Stock[]> {
         const params = new URLSearchParams({ q });
         return apiFetch<Stock[]>(`/api/stocks/search?${params.toString()}`, { signal });
       }

       export function fetchStockDetail(code: string, signal: AbortSignal): Promise<Stock> {
         return apiFetch<Stock>(`/api/stocks/${encodeURIComponent(code)}`, { signal });
       }
       ```
    2) 신규 `webapp/src/lib/__tests__/stock-api.test.ts` — vitest + vi.mock:
       ```ts
       import { describe, it, expect, vi, beforeEach } from 'vitest';

       vi.mock('../api', () => ({
         apiFetch: vi.fn(),
         ApiClientError: class extends Error {
           constructor(public code: string, message: string, public status: number) { super(message); }
         },
       }));

       import { apiFetch } from '../api';
       import { searchStocks, fetchStockDetail } from '../stock-api';

       const mockFetch = apiFetch as unknown as ReturnType<typeof vi.fn>;

       describe('searchStocks', () => {
         beforeEach(() => mockFetch.mockReset().mockResolvedValue([]));

         it('한글 쿼리를 URLSearchParams 로 인코딩', async () => {
           const signal = new AbortController().signal;
           await searchStocks('삼성', signal);
           expect(mockFetch).toHaveBeenCalledWith(
             expect.stringMatching(/^\/api\/stocks\/search\?q=/),
             { signal },
           );
           const [path] = mockFetch.mock.calls[0];
           expect(path).toContain('q=' + encodeURIComponent('삼성'));
         });

         it('특수문자 & 를 인코딩', async () => {
           const signal = new AbortController().signal;
           await searchStocks('A&B', signal);
           expect(mockFetch.mock.calls[0][0]).toContain('q=A%26B');
         });
       });

       describe('fetchStockDetail', () => {
         beforeEach(() => mockFetch.mockReset().mockResolvedValue({}));

         it('code 를 경로에 삽입', async () => {
           const signal = new AbortController().signal;
           await fetchStockDetail('005930', signal);
           expect(mockFetch).toHaveBeenCalledWith('/api/stocks/005930', { signal });
         });

         it('code 에 encodeURIComponent 적용', async () => {
           const signal = new AbortController().signal;
           await fetchStockDetail('abc/x', signal);
           expect(mockFetch.mock.calls[0][0]).toBe('/api/stocks/abc%2Fx');
         });
       });
       ```
    3) 실행: `pnpm --filter @gh-radar/webapp test -- --run src/lib/__tests__/stock-api.test.ts` → 4 tests pass.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp test -- --run src/lib/__tests__/stock-api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/src/lib/stock-api.ts` 존재, `export function searchStocks`, `export function fetchStockDetail` 정확히 존재
    - `grep -q "import.*apiFetch.*from '\\./api'" webapp/src/lib/stock-api.ts` → 매치
    - `grep -q "encodeURIComponent(code)" webapp/src/lib/stock-api.ts` → 매치
    - test 파일 4 tests 전부 pass
  </acceptance_criteria>
  <done>stock-api 함수 2개 + 4개 단위 테스트 통과</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useDebouncedSearch + 단위 테스트 (fake timers + AbortController)</name>
  <read_first>
    - webapp/src/hooks/use-polling.ts · use-polling.test.ts (fakeTimers 패턴, controllerRef 패턴)
    - .planning/phases/06-stock-search-detail/06-RESEARCH.md §"Pattern 3: Debounced 검색 + AbortController"
    - .planning/phases/06-stock-search-detail/06-RESEARCH.md §"Pitfall 3: AbortController 미사용 시 race condition"
  </read_first>
  <behavior>
    - Test 1: query='' 이면 results=[] · loading=false · error=undefined, fetcher 미호출
    - Test 2: query='삼' 입력 후 300ms 이내에는 fetcher 미호출
    - Test 3: query='삼' 입력 후 300ms 경과 시 fetcher 1회 호출, results 업데이트
    - Test 4: 300ms 내 연속 입력 '삼' → '삼성' → '삼성전' 후 300ms 대기 시 fetcher 는 마지막 쿼리로 1회만 호출
    - Test 5: 이전 호출이 in-flight 인 상태에서 새 입력 → controller.abort() 호출 (이전 결과 버림)
    - Test 6: fetcher 가 reject → error 에 Error 인스턴스, loading=false, results=[]
    - Test 7: fetcher reject 가 AbortError (controller.signal.aborted) 인 경우 error/results 건드리지 않음
  </behavior>
  <action>
    1) 신규 `webapp/src/hooks/use-debounced-search.ts`:
       ```ts
       'use client';
       import { useEffect, useRef, useState } from 'react';
       import { searchStocks } from '@/lib/stock-api';
       import type { Stock } from '@gh-radar/shared';

       export interface UseDebouncedSearchResult {
         results: Stock[];
         loading: boolean;
         error: Error | undefined;
       }

       /**
        * useDebouncedSearch — 입력 후 `delayMs` 침묵 시 `searchStocks` 호출.
        * - 새 입력 발생 시 in-flight 요청을 AbortController 로 취소 (race condition 방지, Pitfall 3)
        * - query.trim().length === 0 이면 즉시 빈 결과 + 이전 요청 abort
        * - AbortError 는 사용자 에러로 노출하지 않음
        */
       export function useDebouncedSearch(query: string, delayMs = 300): UseDebouncedSearchResult {
         const [results, setResults] = useState<Stock[]>([]);
         const [loading, setLoading] = useState(false);
         const [error, setError] = useState<Error | undefined>(undefined);
         const controllerRef = useRef<AbortController | null>(null);

         useEffect(() => {
           const trimmed = query.trim();
           if (trimmed.length === 0) {
             controllerRef.current?.abort();
             controllerRef.current = null;
             setResults([]);
             setError(undefined);
             setLoading(false);
             return;
           }

           const timer = setTimeout(() => {
             controllerRef.current?.abort();
             const controller = new AbortController();
             controllerRef.current = controller;
             setLoading(true);
             setError(undefined);
             searchStocks(trimmed, controller.signal)
               .then((data) => {
                 if (controller.signal.aborted) return;
                 setResults(data);
                 setLoading(false);
               })
               .catch((err: unknown) => {
                 if (controller.signal.aborted) return;
                 setError(err instanceof Error ? err : new Error(String(err)));
                 setResults([]);
                 setLoading(false);
               });
           }, delayMs);

           return () => clearTimeout(timer);
         }, [query, delayMs]);

         useEffect(() => () => controllerRef.current?.abort(), []);

         return { results, loading, error };
       }
       ```
    2) 신규 `webapp/src/hooks/use-debounced-search.test.ts`:
       - `vi.mock('@/lib/stock-api', () => ({ searchStocks: vi.fn() }))`
       - `vi.useFakeTimers({ shouldAdvanceTime: true })` · `renderHook` · `act(vi.advanceTimersByTimeAsync)`
       - 7 tests 위 behavior 전부 커버
       - AbortController mock: `searchStocks` 가 받은 signal 의 aborted 를 확인하려면 구현에서 signal 을 controllerRef 에 저장 후 `mockImplementation((q, signal) => new Promise((_, reject) => { signal.addEventListener('abort', () => reject(new DOMException('abort','AbortError'))); }))` 패턴 사용
    3) 실행: `pnpm --filter @gh-radar/webapp test -- --run src/hooks/use-debounced-search.test.ts` → 7 tests pass.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp test -- --run src/hooks/use-debounced-search.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/src/hooks/use-debounced-search.ts` 파일 존재
    - `grep -q "'use client'" webapp/src/hooks/use-debounced-search.ts` → 매치
    - `grep -q "new AbortController()" webapp/src/hooks/use-debounced-search.ts` → 매치
    - `grep -q "setTimeout" webapp/src/hooks/use-debounced-search.ts` → 매치
    - `grep -q "controllerRef.current?.abort()" webapp/src/hooks/use-debounced-search.ts` → 매치
    - `grep -q "query.trim()" webapp/src/hooks/use-debounced-search.ts` → 매치
    - 7 tests 전부 pass
  </acceptance_criteria>
  <done>useDebouncedSearch 훅 + 7 tests 통과 (debounce + abort + empty + error 모두 커버)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: useCmdKShortcut + 단위 테스트 (keydown 시뮬레이션)</name>
  <read_first>
    - .planning/phases/06-stock-search-detail/06-RESEARCH.md §"Pattern 1: ⌘K 전역 단축키 (R1)"
    - .planning/phases/06-stock-search-detail/06-RESEARCH.md §"Pitfall 4: ⌘K 단축키가 form input 에서 발화 안됨" · §"Pitfall 6"
  </read_first>
  <behavior>
    - Test 1: 훅 마운트 후 `metaKey + k` keydown → toggle 1회 호출
    - Test 2: 훅 마운트 후 `ctrlKey + k` keydown → toggle 1회 호출 (Win/Linux)
    - Test 3: 다른 키 (`metaKey + j`) → toggle 미호출
    - Test 4: modifier 없이 `k` 만 → toggle 미호출
    - Test 5: 훅 마운트 시 event.preventDefault() 호출됨 (event spy)
    - Test 6: unmount 후 keydown → toggle 미호출 (listener 해제 확인)
  </behavior>
  <action>
    1) 신규 `webapp/src/hooks/use-cmdk-shortcut.ts` — 정확 구현:
       ```ts
       'use client';
       import { useEffect } from 'react';

       /**
        * useCmdKShortcut — document keydown 에 mod+k 단축키 등록.
        * - metaKey (Mac Cmd) OR ctrlKey (Win/Linux) + k
        * - event.preventDefault() 로 OS 기본 단축키 억제 (Pitfall 6)
        * - document 에 바인딩 (input focus 중에도 발화, Pitfall 4)
        */
       export function useCmdKShortcut(toggle: () => void): void {
         useEffect(() => {
           const onKeyDown = (e: KeyboardEvent) => {
             if (e.key === 'k' && (e.metaKey || e.ctrlKey)) {
               e.preventDefault();
               toggle();
             }
           };
           document.addEventListener('keydown', onKeyDown);
           return () => document.removeEventListener('keydown', onKeyDown);
         }, [toggle]);
       }
       ```
    2) 신규 `webapp/src/hooks/use-cmdk-shortcut.test.ts`:
       - `renderHook` + `useCallback` toggle 스파이
       - KeyboardEvent dispatch: `document.dispatchEvent(new KeyboardEvent('keydown', { key: 'k', metaKey: true, cancelable: true }))`
       - Test 5 preventDefault 검증: `defaultPrevented` 플래그 체크 전에 `cancelable: true` 필수
       - Test 6 unmount: `const { unmount } = renderHook(...)` → `unmount()` → dispatch 후 spy 미호출
    3) 실행: `pnpm --filter @gh-radar/webapp test -- --run src/hooks/use-cmdk-shortcut.test.ts` → 6 tests pass.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp test -- --run src/hooks/use-cmdk-shortcut.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/src/hooks/use-cmdk-shortcut.ts` 존재
    - `grep -q "document.addEventListener('keydown'" webapp/src/hooks/use-cmdk-shortcut.ts` → 매치
    - `grep -q "e.metaKey || e.ctrlKey" webapp/src/hooks/use-cmdk-shortcut.ts` → 매치
    - `grep -q "e.preventDefault()" webapp/src/hooks/use-cmdk-shortcut.ts` → 매치
    - 6 tests 전부 pass
  </acceptance_criteria>
  <done>useCmdKShortcut 훅 + 6 tests 통과</done>
</task>

</tasks>

<verification>
- `pnpm --filter @gh-radar/webapp test -- --run` 전체 suite → PASS
- `pnpm --filter @gh-radar/webapp exec tsc --noEmit` → PASS
- 17개 테스트(4 + 7 + 6) 전부 green
</verification>

<success_criteria>
- stock-api / use-debounced-search / use-cmdk-shortcut 3 모듈 + 3 테스트 파일 commit
- AbortController race 방지 로직이 테스트로 증명됨 (Pitfall 3)
- mod+k 가 Mac/Win 양측 호환 (Pitfall 4, 6)
- Plan 03/04 가 이 모듈을 import 할 수 있는 상태
</success_criteria>

<output>
`.planning/phases/06-stock-search-detail/06-02-SUMMARY.md` 생성
</output>
