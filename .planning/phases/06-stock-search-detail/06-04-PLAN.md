---
phase: 06-stock-search-detail
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - webapp/src/components/stock/stock-hero.tsx
  - webapp/src/components/stock/stock-stats-grid.tsx
  - webapp/src/components/stock/coming-soon-card.tsx
  - webapp/src/components/stock/stock-detail-client.tsx
  - webapp/src/components/stock/stock-detail-skeleton.tsx
  - webapp/src/components/stock/__tests__/stock-hero.test.tsx
  - webapp/src/components/stock/__tests__/stock-stats-grid.test.tsx
  - webapp/src/components/stock/__tests__/stock-detail-client.test.tsx
autonomous: true
requirements: [SRCH-03]
must_haves:
  truths:
    - "StockHero 가 종목명·코드·마켓배지·현재가(Display 30/24 반응형)·등락액·등락률(up/down/flat 색상) 을 표시한다"
    - "StockStatsGrid 가 8필드(시가·고가·저가·거래량·거래대금·시총·상한가·하한가) 를 2열/md:3열 Card 로 렌더한다"
    - "Stats 값이 `value <= 0 || !Number.isFinite(value)` 이면 em-dash(—) 로 표시된다 (시가/고가/저가/시총/상한가/하한가). 거래량/거래대금/등락액/등락률은 분기 미적용"
    - "ComingSoonCard 가 Phase 7/8 placeholder (제목·본문) 를 정확한 UI-SPEC 카피로 렌더한다"
    - "StockDetailClient 가 mount 시 fetchStockDetail 호출 + refresh 버튼 + ApiClientError.status===404 시 notFound() 호출"
    - "초기 로딩 시 StockDetailSkeleton 노출, 재조회 시 기존 데이터 유지 (stale-but-visible)"
  artifacts:
    - path: "webapp/src/components/stock/stock-hero.tsx"
      provides: "Hero 섹션"
      exports: ["StockHero"]
    - path: "webapp/src/components/stock/stock-stats-grid.tsx"
      provides: "8필드 Card grid + em-dash 정책"
      exports: ["StockStatsGrid"]
    - path: "webapp/src/components/stock/coming-soon-card.tsx"
      provides: "Phase 7/8 placeholder Card"
      exports: ["ComingSoonCard"]
    - path: "webapp/src/components/stock/stock-detail-client.tsx"
      provides: "fetch + refresh + 404 분기 오케스트레이션"
      exports: ["StockDetailClient"]
  key_links:
    - from: "webapp/src/components/stock/stock-detail-client.tsx"
      to: "webapp/src/lib/stock-api.ts"
      via: "fetchStockDetail 호출"
      pattern: "fetchStockDetail\\("
    - from: "webapp/src/components/stock/stock-detail-client.tsx"
      to: "next/navigation"
      via: "notFound() 호출"
      pattern: "notFound\\(\\)"
    - from: "webapp/src/components/stock/stock-stats-grid.tsx"
      to: "webapp/src/components/ui/number.tsx"
      via: "<Number> 렌더링"
      pattern: "<Number "
---

<objective>
Phase 6 종목 상세 페이지의 **뷰 컴포넌트 + fetch 오케스트레이션 클라이언트**. Plan 05 의 `app/stocks/[code]/page.tsx` 가 이 모듈을 마운트한다.

Purpose: Hero / StatsGrid / ComingSoon / DetailClient 4개 컴포넌트로 SRCH-03 (상세 페이지 현재가·등락률·거래량 등) 완결. em-dash 정책(Pitfall 1) 은 StatsGrid 내부에서 처리.
Output: 5개 소스 + 3개 테스트 파일.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-stock-search-detail/06-CONTEXT.md
@.planning/phases/06-stock-search-detail/06-RESEARCH.md
@.planning/phases/06-stock-search-detail/06-UI-SPEC.md
@webapp/src/components/ui/number.tsx
@webapp/src/components/ui/card.tsx
@webapp/src/components/ui/badge.tsx
@webapp/src/components/ui/skeleton.tsx
@webapp/src/components/ui/button.tsx
@webapp/src/lib/stock-api.ts
@webapp/src/lib/api.ts
@webapp/src/components/scanner/scanner-client.tsx
@packages/shared/src/stock.ts

<interfaces>
From webapp/src/components/ui/number.tsx:
```ts
// format: 'price' | 'percent' | 'volume' | 'marketCap' | 'trade-amount' | 'plain'
// <Number value={58700} format="price" /> → "58,700"
// <Number value={0.0325} format="percent" showSign withColor /> → "+3.25%" (red)
// <Number value={3.504e14} format="marketCap" /> → "350.4 조원"
// ❗ percent 는 소수 (0.0325 = 3.25%). 서버 changeRate 는 **정수 %** (예: 2.09 = 2.09%) → 변환 필요!
// 실제: scanner-client.tsx 에서 changeRate 값을 어떻게 처리하는지 확인 (Phase 5 에서 확정)
```

From webapp/src/lib/stock-api.ts (Plan 02):
```ts
export function fetchStockDetail(code: string, signal: AbortSignal): Promise<Stock>;
```

From webapp/src/lib/api.ts:
```ts
export class ApiClientError extends Error {
  readonly code: string; readonly status: number; readonly requestId?: string;
}
```

From @gh-radar/shared Stock 타입:
```ts
// price/open/high/low/marketCap/upperLimit/lowerLimit: number (nullable 필드는 서버 mapper 가 0 으로 강제)
// changeAmount: number (음수 정상)
// changeRate: number — Phase 5 실측: 정수 % 스케일 (2.09 = 2.09%)
// volume: number · tradeAmount: number (0 정상값 — formatTradeAmount 가 0 → '-' 자동)
// updatedAt: string (ISO)
```

From next/navigation:
```ts
export function notFound(): never;
// try/catch 내부에서 호출 가능 (내부 NEXT_NOT_FOUND 심볼)
```
</interfaces>

<ui_spec_tokens>
<!-- 06-UI-SPEC.md 정확 값 -->

Typography:
- Hero 종목명: `text-[length:var(--t-h2)] font-semibold` (Heading 24)
- Hero 현재가: `text-[length:var(--t-h2)] md:text-[length:var(--t-h1)] font-semibold mono` (24px → 30px, primary focal point)
- Hero 등락액/등락률: `text-[length:var(--t-sm)] mono` + `withColor`
- Stats Card 라벨: `text-[length:var(--t-caption)] font-semibold` (Caption 12 weight 600)
- Stats Card 값: `text-[length:var(--t-sm)] font-semibold mono` (Body 14 weight 600)
- 섹션 헤더(관련 뉴스/종목토론방 상위 라벨): `text-[length:var(--t-h2)] font-semibold`
- ComingSoon 카드 제목: `text-[length:var(--t-sm)] font-semibold`
- 갱신시각: `text-[length:var(--t-caption)] text-[var(--muted-fg)] mono`

Spacing:
- Hero 내부 블록 간격: `gap-6` (s-5 = 24px)
- 현재가 ↔ 등락: `gap-3` (s-3 = 12px)
- Stats grid: `grid grid-cols-2 md:grid-cols-3 gap-4 md:gap-6`
- 섹션 간: `space-y-8` (s-6 = 32px)
- Card padding: `p-4` (s-4 = 16px)

Color:
- 등락 up (양수): Number `withColor` → `text-[var(--up)]`
- 등락 down (음수): `text-[var(--down)]`
- 등락 flat (0): `text-[var(--flat)]`
- 라벨 muted: `text-[var(--muted-fg)]`
- em-dash: `text-[var(--muted-fg)] mono`

Copywriting (정확 문자열):
- Stats 라벨: `시가` / `고가` / `저가` / `거래량` / `거래대금` / `시가총액` / `상한가` / `하한가`
- Refresh 버튼: `새로고침` (+ `lucide RefreshCw` 아이콘)
- 갱신시각 prefix: `갱신 ` (trailing space)
- Null 표기: `—` (em-dash U+2014)
- Phase 7 Card 제목: `관련 뉴스` / 본문: `Phase 7 로드맵에서 제공됩니다.`
- Phase 8 Card 제목: `종목토론방` / 본문: `Phase 8 로드맵에서 제공됩니다.`

갱신시각 포맷:
```ts
new Intl.DateTimeFormat('ko-KR', {
  timeZone: 'Asia/Seoul', hour12: false,
  hour: '2-digit', minute: '2-digit', second: '2-digit',
}).format(new Date(stock.updatedAt)) + ' KST'
// 결과: "갱신 14:32:05 KST"
```
</ui_spec_tokens>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StockHero + StockStatsGrid + ComingSoonCard + 스냅샷/em-dash 테스트</name>
  <read_first>
    - webapp/src/components/ui/number.tsx (format 종류 + withColor)
    - webapp/src/components/ui/badge.tsx (variant: up/down/flat + outline 사용)
    - webapp/src/components/ui/card.tsx (Card/CardHeader/CardContent API)
    - webapp/src/components/scanner/scanner-table.tsx (changeRate 를 어떻게 Number 에 전달하는지 확인 — percent 스케일 결정)
    - .planning/phases/06-stock-search-detail/06-UI-SPEC.md 전체 (Typography / Color / Copywriting 섹션)
    - .planning/phases/06-stock-search-detail/06-RESEARCH.md §"Pitfall 1" + §"코드 예시 5 StatValue"
    - .planning/phases/06-stock-search-detail/06-RESEARCH.md §"R3 Stats grid 열 수"
    - webapp/e2e/fixtures/stocks.ts (FIXTURE_SAMSUNG, FIXTURE_NULL_PRICE)
  </read_first>
  <behavior>
    StockHero:
    - Test 1: 삼성전자 픽스처 렌더 → 종목명 `삼성전자`, 코드 `005930`, 마켓 배지 `KOSPI`, 현재가 `58,700`, 등락액 `+1,200`, 등락률 `+2.09%` 노출
    - Test 2: price=0 (정지/폐지) → 현재가 자리에 em-dash `—` 노출, 등락액·등락률은 em-dash 적용 안 됨 (정상 렌더)
    - Test 3: Hero 현재가 요소에 `text-[length:var(--t-h2)]` 와 `md:text-[length:var(--t-h1)]` 클래스 존재

    StockStatsGrid:
    - Test 4: 8개 라벨(시가/고가/저가/거래량/거래대금/시가총액/상한가/하한가) 모두 노출
    - Test 5: FIXTURE_NULL_PRICE (open/high/low/marketCap/upperLimit/lowerLimit 모두 0) → 6개 필드 모두 em-dash `—` 노출
    - Test 6: 거래량·거래대금 은 0 값이어도 em-dash 대신 Number 포맷 (volume: 0 → `0`, tradeAmount: 0 → `-` via formatTradeAmount — 이는 formatTradeAmount 기본 동작)
    - Test 7: grid container 에 `grid-cols-2 md:grid-cols-3` 클래스 존재

    ComingSoonCard:
    - Test 8: `title="관련 뉴스"` `body="Phase 7 로드맵에서 제공됩니다."` 렌더 → 두 문자열 노출
  </behavior>
  <action>
    1) 신규 `webapp/src/components/stock/stock-hero.tsx`:
       ```tsx
       import type { Stock } from '@gh-radar/shared';
       import { Badge } from '@/components/ui/badge';
       import { Number } from '@/components/ui/number';

       export interface StockHeroProps {
         stock: Stock;
       }

       /**
        * StockHero — Phase 6 D4 Hero 섹션 (UI-SPEC primary focal point).
        * - 종목명(Heading 24) · 코드 · 마켓배지
        * - 현재가(Display 30 → 모바일 24, 반응형 breakpoint)
        * - 등락액 + 등락률 (up/down/flat 색상)
        * - price <= 0 → em-dash (정지/폐지 종목)
        */
       export function StockHero({ stock }: StockHeroProps) {
         const priceValid = Number.isFinite(stock.price) && stock.price > 0;
         // changeRate 서버 스케일: 정수 % (Phase 5 실측, 2.09 = 2.09%) → Number format=percent 는 소수 기대 → /100
         const changeRateDecimal = stock.changeRate / 100;

         return (
           <section className="space-y-6" aria-label="종목 개요">
             <div className="flex flex-wrap items-center gap-3">
               <h1 className="text-[length:var(--t-h2)] font-semibold tracking-[-0.01em] text-[var(--fg)]">
                 {stock.name}
               </h1>
               <span className="mono text-[length:var(--t-sm)] text-[var(--muted-fg)]">
                 {stock.code}
               </span>
               <Badge variant="outline">{stock.market}</Badge>
             </div>

             <div className="flex flex-wrap items-baseline gap-3">
               {priceValid ? (
                 <span className="mono text-[length:var(--t-h2)] md:text-[length:var(--t-h1)] font-semibold text-[var(--fg)]">
                   <Number value={stock.price} format="price" />
                 </span>
               ) : (
                 <span className="mono text-[length:var(--t-h2)] md:text-[length:var(--t-h1)] font-semibold text-[var(--muted-fg)]">
                   —
                 </span>
               )}
               <span className="mono text-[length:var(--t-sm)]">
                 <Number value={stock.changeAmount} format="price" showSign withColor />
               </span>
               <span className="mono text-[length:var(--t-sm)]">
                 <Number value={changeRateDecimal} format="percent" showSign withColor />
               </span>
             </div>
           </section>
         );
       }
       ```
    2) 신규 `webapp/src/components/stock/stock-stats-grid.tsx`:
       ```tsx
       import type { Stock } from '@gh-radar/shared';
       import { Card } from '@/components/ui/card';
       import { Number, type NumberFormat } from '@/components/ui/number';

       interface StatCell {
         label: string;
         value: number;
         format: NumberFormat;
         /** true 면 value <= 0 또는 NaN 시 em-dash (Pitfall 1 대응) */
         nullAsEmDash: boolean;
       }

       /**
        * StockStatsGrid — 8필드 Card grid (Phase 6 D4).
        * - grid-cols-2 md:grid-cols-3 (R3 권고: lg 4열 거절)
        * - em-dash 정책: 시가·고가·저가·시총·상한가·하한가 는 value<=0 일 때 `—`
        * - 거래량·거래대금 은 0 정상값 → formatTradeAmount/Number 기본 동작 유지
        */
       export function StockStatsGrid({ stock }: { stock: Stock }) {
         const cells: StatCell[] = [
           { label: '시가', value: stock.open, format: 'price', nullAsEmDash: true },
           { label: '고가', value: stock.high, format: 'price', nullAsEmDash: true },
           { label: '저가', value: stock.low, format: 'price', nullAsEmDash: true },
           { label: '거래량', value: stock.volume, format: 'volume', nullAsEmDash: false },
           { label: '거래대금', value: stock.tradeAmount, format: 'trade-amount', nullAsEmDash: false },
           { label: '시가총액', value: stock.marketCap, format: 'marketCap', nullAsEmDash: true },
           { label: '상한가', value: stock.upperLimit, format: 'price', nullAsEmDash: true },
           { label: '하한가', value: stock.lowerLimit, format: 'price', nullAsEmDash: true },
         ];

         return (
           <div className="grid grid-cols-2 md:grid-cols-3 gap-4 md:gap-6">
             {cells.map((c) => (
               <Card key={c.label} className="p-4">
                 <div className="space-y-2">
                   <div className="text-[length:var(--t-caption)] font-semibold text-[var(--muted-fg)]">
                     {c.label}
                   </div>
                   <div className="text-[length:var(--t-sm)] font-semibold">
                     {c.nullAsEmDash && (!Number.isFinite(c.value) || c.value <= 0) ? (
                       <span className="mono text-[var(--muted-fg)]">—</span>
                     ) : (
                       <Number value={c.value} format={c.format} />
                     )}
                   </div>
                 </div>
               </Card>
             ))}
           </div>
         );
       }
       ```
    3) 신규 `webapp/src/components/stock/coming-soon-card.tsx`:
       ```tsx
       import { Card } from '@/components/ui/card';

       export interface ComingSoonCardProps {
         title: string;
         body: string;
       }

       /**
        * ComingSoonCard — Phase 7/8 placeholder (UI-SPEC Copywriting).
        * - 제목: Body 14 weight 600
        * - 본문: Body 14 regular muted
        */
       export function ComingSoonCard({ title, body }: ComingSoonCardProps) {
         return (
           <Card className="p-4">
             <div className="space-y-2">
               <h3 className="text-[length:var(--t-sm)] font-semibold text-[var(--fg)]">{title}</h3>
               <p className="text-[length:var(--t-sm)] text-[var(--muted-fg)]">{body}</p>
             </div>
           </Card>
         );
       }
       ```
    4) 테스트 파일 2개 작성:
       - `webapp/src/components/stock/__tests__/stock-hero.test.tsx` — 3 tests
       - `webapp/src/components/stock/__tests__/stock-stats-grid.test.tsx` — 5 tests (8 라벨 · null 6개 · 0 정상 · grid-cols 클래스)
       - FIXTURE_SAMSUNG / FIXTURE_NULL_PRICE 는 `@/../e2e/fixtures/stocks` 경로 대신 테스트 내 inline 정의 (단위 테스트 격리) — 또는 `webapp/src/__tests__/fixtures/stocks.ts` 공통 파일 작성 후 import
    5) 실행: `pnpm --filter @gh-radar/webapp test -- --run src/components/stock` → 8 tests pass.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp test -- --run src/components/stock/__tests__/stock-hero.test.tsx src/components/stock/__tests__/stock-stats-grid.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/src/components/stock/stock-hero.tsx` 존재, `grep -q "md:text-\\[length:var(--t-h1)\\]"` 매치
    - `webapp/src/components/stock/stock-stats-grid.tsx` 존재
    - `grep -q "grid-cols-2 md:grid-cols-3" webapp/src/components/stock/stock-stats-grid.tsx` → 매치
    - `grep -q "c.value <= 0" webapp/src/components/stock/stock-stats-grid.tsx` → 매치 (em-dash 분기)
    - `grep -q "!Number.isFinite" webapp/src/components/stock/stock-stats-grid.tsx` → 매치
    - 8개 라벨 문자열 모두 grep 매치: 시가·고가·저가·거래량·거래대금·시가총액·상한가·하한가
    - `grep -q "관련 뉴스\\|Phase 7 로드맵에서 제공됩니다" webapp/src/components/stock/coming-soon-card.tsx` → coming-soon 은 props 수신이므로 실제 카피는 Plan 05 page.tsx 에서 주입됨 (이 파일은 일반 컴포넌트)
    - 8 tests 전부 pass
  </acceptance_criteria>
  <done>Hero/StatsGrid/ComingSoon 3 컴포넌트 + 8 tests 통과 (em-dash 정책 증명)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StockDetailClient + StockDetailSkeleton + 단위 테스트 (fetch + refresh + 404 → notFound)</name>
  <read_first>
    - .planning/phases/06-stock-search-detail/06-RESEARCH.md §"Code Examples > 예시 4: useStockDetail"
    - .planning/phases/06-stock-search-detail/06-RESEARCH.md §"Pitfall 5: error.tsx 가 not-found 가로챔"
    - webapp/src/components/scanner/scanner-client.tsx (refresh 버튼 패턴, RefreshCw + isRefreshing spinner)
    - webapp/src/hooks/use-polling.ts (controllerRef 패턴 복제)
    - webapp/src/components/ui/skeleton.tsx (Skeleton API)
  </read_first>
  <behavior>
    - Test 1: mount 시 fetchStockDetail 이 (code, signal) 로 1회 호출됨
    - Test 2: fetch resolve 후 Hero/StatsGrid/ComingSoon 렌더 (삼성전자 텍스트 노출)
    - Test 3: 초기 로딩 중에는 StockDetailSkeleton 노출 (Hero 없음)
    - Test 4: refresh 버튼 클릭 → fetchStockDetail 재호출, 기존 data 유지하면서 `aria-busy=true` 또는 spinner 노출
    - Test 5: ApiClientError.status === 404 → notFound() 호출됨 (notFound 를 `vi.mock('next/navigation')` 으로 스파이)
    - Test 6: ApiClientError.status === 500 → error state 로 throw (Next error boundary 가 catch 하도록 error 를 state 에 두고 throw 하거나 `throw err` — 실제 패턴: error 를 state 에 저장 후 `useEffect(() => { if (error && !stock) throw error; })` 로 escalate → 또는 단순 `setError` 후 인라인 렌더 + 재시도 버튼). **채택: state 유지 + 인라인 에러 카드 + 재시도 버튼** (데이터 보존 목적). `error.tsx` 는 throw 계열(notFound 외)만 잡음.
    - Test 7: 갱신시각 `갱신 HH:MM:SS KST` 포맷 노출 (Intl.DateTimeFormat Asia/Seoul)
  </behavior>
  <action>
    1) 신규 `webapp/src/components/stock/stock-detail-skeleton.tsx`:
       ```tsx
       import { Skeleton } from '@/components/ui/skeleton';

       /** Phase 6 초기 로딩 skeleton — Hero + Stats grid 형태 유지 (CLS 최소화) */
       export function StockDetailSkeleton() {
         return (
           <div className="space-y-8" aria-busy="true" aria-label="종목 정보 로딩 중">
             <section className="space-y-6">
               <div className="flex items-center gap-3">
                 <Skeleton className="h-6 w-40" />
                 <Skeleton className="h-4 w-16" />
                 <Skeleton className="h-5 w-14 rounded-full" />
               </div>
               <div className="flex items-baseline gap-3">
                 <Skeleton className="h-8 w-32 md:h-10 md:w-40" />
                 <Skeleton className="h-4 w-16" />
                 <Skeleton className="h-4 w-16" />
               </div>
             </section>
             <div className="grid grid-cols-2 md:grid-cols-3 gap-4 md:gap-6">
               {Array.from({ length: 8 }).map((_, i) => (
                 <Skeleton key={i} className="h-20 w-full" />
               ))}
             </div>
           </div>
         );
       }
       ```
    2) 신규 `webapp/src/components/stock/stock-detail-client.tsx`:
       ```tsx
       'use client';
       import { useCallback, useEffect, useMemo, useRef, useState } from 'react';
       import { notFound } from 'next/navigation';
       import { RefreshCw } from 'lucide-react';
       import type { Stock } from '@gh-radar/shared';
       import { ApiClientError } from '@/lib/api';
       import { fetchStockDetail } from '@/lib/stock-api';
       import { Button } from '@/components/ui/button';
       import { StockHero } from './stock-hero';
       import { StockStatsGrid } from './stock-stats-grid';
       import { ComingSoonCard } from './coming-soon-card';
       import { StockDetailSkeleton } from './stock-detail-skeleton';

       const KST_TIME_FMT = new Intl.DateTimeFormat('ko-KR', {
         timeZone: 'Asia/Seoul',
         hour12: false,
         hour: '2-digit',
         minute: '2-digit',
         second: '2-digit',
       });

       export interface StockDetailClientProps {
         code: string;
       }

       /**
        * StockDetailClient — Phase 6 상세 페이지 fetch + refresh 오케스트레이션.
        * - mount 시 fetchStockDetail 호출
        * - 수동 refresh 버튼 (자동 폴링 없음 — D5)
        * - 404 → notFound() → app/stocks/[code]/not-found.tsx (Pitfall 5)
        * - 기타 에러 → 인라인 에러 카드 + 재시도 (error.tsx 가 아닌 state 로 관리)
        * - 재조회 시 기존 stock 유지 (stale-but-visible)
        */
       export function StockDetailClient({ code }: StockDetailClientProps) {
         const [stock, setStock] = useState<Stock | undefined>(undefined);
         const [error, setError] = useState<Error | undefined>(undefined);
         const [isRefreshing, setIsRefreshing] = useState(false);
         const [isInitialLoading, setIsInitialLoading] = useState(true);
         const controllerRef = useRef<AbortController | null>(null);

         const load = useCallback(async () => {
           controllerRef.current?.abort();
           const controller = new AbortController();
           controllerRef.current = controller;
           setIsRefreshing(true);
           try {
             const data = await fetchStockDetail(code, controller.signal);
             if (controller.signal.aborted) return;
             setStock(data);
             setError(undefined);
           } catch (err) {
             if (controller.signal.aborted) return;
             if (err instanceof ApiClientError && err.status === 404) {
               notFound();
               return;
             }
             setError(err instanceof Error ? err : new Error(String(err)));
           } finally {
             if (!controller.signal.aborted) {
               setIsRefreshing(false);
               setIsInitialLoading(false);
             }
           }
         }, [code]);

         useEffect(() => {
           void load();
           return () => controllerRef.current?.abort();
         }, [load]);

         const updatedAtLabel = useMemo(() => {
           if (!stock) return null;
           const d = new Date(stock.updatedAt);
           return Number.isFinite(d.getTime()) ? `갱신 ${KST_TIME_FMT.format(d)} KST` : null;
         }, [stock]);

         if (isInitialLoading && !stock) return <StockDetailSkeleton />;

         if (!stock && error) {
           return (
             <section className="space-y-4" role="alert">
               <h2 className="text-[length:var(--t-h2)] font-semibold">데이터를 불러오지 못했습니다</h2>
               <p className="text-[length:var(--t-sm)] text-[var(--muted-fg)]">
                 {error.message}
               </p>
               <Button onClick={() => void load()}>다시 시도</Button>
             </section>
           );
         }

         if (!stock) return <StockDetailSkeleton />;

         return (
           <div className="space-y-8">
             <StockHero stock={stock} />
             <div className="flex items-center justify-between gap-3">
               {updatedAtLabel && (
                 <span className="text-[length:var(--t-caption)] text-[var(--muted-fg)] mono">
                   {updatedAtLabel}
                 </span>
               )}
               <Button
                 onClick={() => void load()}
                 disabled={isRefreshing}
                 variant="outline"
                 aria-label="새로고침"
               >
                 <RefreshCw className={`mr-2 h-4 w-4 ${isRefreshing ? 'animate-spin' : ''}`} aria-hidden="true" />
                 새로고침
               </Button>
             </div>
             <StockStatsGrid stock={stock} />
             {error && (
               <p className="text-[length:var(--t-caption)] text-[var(--destructive)]">
                 최근 갱신 실패: {error.message}
               </p>
             )}
             <div className="grid grid-cols-1 md:grid-cols-2 gap-4 md:gap-6">
               <ComingSoonCard title="관련 뉴스" body="Phase 7 로드맵에서 제공됩니다." />
               <ComingSoonCard title="종목토론방" body="Phase 8 로드맵에서 제공됩니다." />
             </div>
           </div>
         );
       }
       ```
    3) 신규 `webapp/src/components/stock/__tests__/stock-detail-client.test.tsx`:
       - `vi.mock('next/navigation', () => ({ notFound: vi.fn() }))`
       - `vi.mock('@/lib/stock-api', () => ({ fetchStockDetail: vi.fn() }))`
       - 7 tests 위 behavior 전부
       - `ApiClientError` 는 실제 클래스 사용 (mock 금지 — `instanceof` 검증 필요) — `webapp/src/lib/api.ts` 에서 직접 import
    4) 실행: `pnpm --filter @gh-radar/webapp test -- --run src/components/stock/__tests__/stock-detail-client.test.tsx` → 7 tests pass.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp test -- --run src/components/stock/__tests__/stock-detail-client.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/src/components/stock/stock-detail-client.tsx` 존재
    - `grep -q "'use client'" webapp/src/components/stock/stock-detail-client.tsx` → 매치
    - `grep -q "fetchStockDetail" webapp/src/components/stock/stock-detail-client.tsx` → 매치
    - `grep -q "err.status === 404" webapp/src/components/stock/stock-detail-client.tsx` → 매치
    - `grep -q "notFound()" webapp/src/components/stock/stock-detail-client.tsx` → 매치
    - `grep -q "관련 뉴스" webapp/src/components/stock/stock-detail-client.tsx` → 매치
    - `grep -q "Phase 7 로드맵에서 제공됩니다." webapp/src/components/stock/stock-detail-client.tsx` → 매치
    - `grep -q "Phase 8 로드맵에서 제공됩니다." webapp/src/components/stock/stock-detail-client.tsx` → 매치
    - `grep -q "새로고침" webapp/src/components/stock/stock-detail-client.tsx` → 매치
    - `grep -q "Asia/Seoul" webapp/src/components/stock/stock-detail-client.tsx` → 매치
    - 7 tests 전부 pass
  </acceptance_criteria>
  <done>StockDetailClient + Skeleton + 7 tests 통과 (fetch/refresh/404 notFound/에러 보존)</done>
</task>

</tasks>

<threat_model>
N/A (조회 전용). code 파라미터는 클라 + 서버 양측 `^[A-Za-z0-9]{1,10}$` regex 검증. encodeURIComponent 적용 (Plan 02).
</threat_model>

<verification>
- `pnpm --filter @gh-radar/webapp test -- --run` 전체 → PASS (이전 + 본 plan 15 tests 추가)
- `pnpm --filter @gh-radar/webapp exec tsc --noEmit` → PASS
</verification>

<success_criteria>
- SRCH-03 뷰 계층 완료 (Hero + Stats 8필드 + Placeholder 2개 + Skeleton)
- em-dash 정책으로 Pitfall 1 (mapper null→0) 방어
- 404 분기로 Pitfall 5 (error.tsx 가로챔) 방어
- Plan 05 가 이 컴포넌트를 page.tsx 에서 마운트 가능한 상태
</success_criteria>

<output>
`.planning/phases/06-stock-search-detail/06-04-SUMMARY.md` 생성
</output>
