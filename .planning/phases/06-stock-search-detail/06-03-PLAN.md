---
phase: 06-stock-search-detail
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - webapp/src/components/search/global-search.tsx
  - webapp/src/components/search/search-trigger.tsx
  - webapp/src/components/search/__tests__/global-search.test.tsx
  - webapp/src/components/layout/app-header.tsx
autonomous: true
requirements: [SRCH-01, SRCH-02]
must_haves:
  truths:
    - "헤더 '종목 검색' input 을 클릭하면 ⌘K Dialog 가 열린다"
    - "mod+k 단축키로 Dialog 오픈/닫기 토글된다"
    - "Dialog 입력 시 300ms debounce 후 자동완성이 노출된다"
    - "자동완성 항목 선택 시 /stocks/:code 로 이동한다 (router.push) + dialog 닫히고 query 초기화"
    - "로딩/빈 결과/에러/초기 각 상태에 UI-SPEC Copywriting 섹션의 한글 카피가 정확히 노출된다"
    - "Command 는 shouldFilter={false}, CommandItem value={stock.code} 로 서버 결과를 그대로 표시 (Pitfall 2 방지)"
  artifacts:
    - path: "webapp/src/components/search/global-search.tsx"
      provides: "⌘K CommandDialog + debounce + 결과 리스트"
      exports: ["GlobalSearch"]
    - path: "webapp/src/components/search/search-trigger.tsx"
      provides: "헤더 readonly 검색 input 트리거"
      exports: ["SearchTrigger"]
    - path: "webapp/src/components/layout/app-header.tsx"
      provides: "<GlobalSearch /> 마운트 (nav slot)"
  key_links:
    - from: "webapp/src/components/search/global-search.tsx"
      to: "webapp/src/hooks/use-debounced-search.ts"
      via: "useDebouncedSearch 호출"
      pattern: "useDebouncedSearch\\("
    - from: "webapp/src/components/search/global-search.tsx"
      to: "webapp/src/hooks/use-cmdk-shortcut.ts"
      via: "useCmdKShortcut 호출"
      pattern: "useCmdKShortcut\\("
    - from: "webapp/src/components/layout/app-header.tsx"
      to: "webapp/src/components/search/global-search.tsx"
      via: "GlobalSearch 렌더"
      pattern: "<GlobalSearch"
---

<objective>
Phase 6 전역 검색 UX — ⌘K CommandDialog + 헤더 readonly 트리거 + 자동완성 결과 리스트. SRCH-01, SRCH-02 완결.

Purpose: 사용자가 어떤 페이지에서든 ⌘K 혹은 헤더 클릭으로 검색 → 300ms debounce → 서버 결과 → ↑↓ Enter 로 상세 이동.
Output: GlobalSearch 컴포넌트 + SearchTrigger + AppHeader 배선 + integration 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-stock-search-detail/06-CONTEXT.md
@.planning/phases/06-stock-search-detail/06-RESEARCH.md
@.planning/phases/06-stock-search-detail/06-UI-SPEC.md
@webapp/src/components/ui/command.tsx
@webapp/src/components/ui/badge.tsx
@webapp/src/components/layout/app-header.tsx
@webapp/src/hooks/use-debounced-search.ts
@webapp/src/hooks/use-cmdk-shortcut.ts

<interfaces>
From webapp/src/components/ui/command.tsx (shadcn, Wave 0 설치 후):
```ts
// export: Command, CommandDialog, CommandInput, CommandList, CommandEmpty,
//         CommandGroup, CommandItem, CommandShortcut, CommandSeparator, CommandLoading
// CommandDialog props: { open: boolean, onOpenChange: (v:boolean)=>void, children: ReactNode }
// Command props: 표준 cmdk — { shouldFilter?: boolean }
// CommandItem props: { value: string, onSelect: (value:string)=>void }
```

From webapp/src/components/ui/badge.tsx:
```ts
// variant: 'default' | 'secondary' | 'outline' | 'up' | 'down' | 'flat'
// ❗ KOSPI/KOSDAQ variant 없음 → variant="outline" + 텍스트 "KOSPI"/"KOSDAQ" 사용
```

From webapp/src/hooks/use-debounced-search.ts (Plan 02):
```ts
export function useDebouncedSearch(query: string, delayMs?: number): {
  results: Stock[]; loading: boolean; error: Error | undefined;
};
```

From webapp/src/hooks/use-cmdk-shortcut.ts (Plan 02):
```ts
export function useCmdKShortcut(toggle: () => void): void;
```

From next/navigation:
```ts
useRouter(): { push(href: string): void; ... }
```
</interfaces>

<ui_spec_tokens>
<!-- 06-UI-SPEC.md 에서 인용 — 정확한 값 그대로 사용 -->

Copywriting (정확한 문자열 — 변경 금지):
- AppHeader 검색 input placeholder: `종목명 또는 코드 검색  ⌘K`
- Dialog input placeholder: `종목명 또는 종목코드를 입력하세요`
- 로딩: `검색 중…`
- 초기 상태 (query 0): `검색어를 입력하면 결과가 표시됩니다`
- 빈 결과 (query 있음): `"{q}" 에 해당하는 종목이 없습니다`
- 에러: `검색에 실패했습니다. 잠시 후 다시 시도해 주세요.`

Typography (Body 14 weight 400):
- CommandItem 종목명: 기본 (Body 14 regular)
- 종목코드: `text-[length:var(--t-caption)] text-[var(--muted-fg)] mono` (12px muted mono)

Spacing:
- CommandItem padding: `py-2 px-3` (row-h 36px)
- item 내부 gap: `gap-3`

Color:
- highlighted 항목: cmdk `data-[selected=true]:bg-accent` (자동)
- error 텍스트: `text-[var(--down)]` 또는 `text-[var(--destructive)]`
- muted 텍스트: `text-[var(--muted-fg)]`
</ui_spec_tokens>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SearchTrigger — 헤더 readonly 검색 input</name>
  <read_first>
    - webapp/src/components/layout/app-header.tsx (기존 Input disabled placeholder 교체 대상)
    - .planning/phases/06-stock-search-detail/06-UI-SPEC.md §"Copywriting Contract" (헤더 placeholder)
    - .planning/phases/06-stock-search-detail/06-RESEARCH.md §"R1 구현 메모" (readonly + onClick 패턴)
  </read_first>
  <action>
    1) 신규 `webapp/src/components/search/search-trigger.tsx` — 정확 구조:
       ```tsx
       'use client';
       import { Search } from 'lucide-react';

       export interface SearchTriggerProps {
         onClick: () => void;
       }

       /**
        * SearchTrigger — AppHeader 중앙 nav slot 에 마운트되는 readonly input.
        * - 클릭/포커스 시 GlobalSearch Dialog 토글
        * - 우측 키캡 `⌘K` 힌트 (UI-SPEC Copywriting)
        * - 모바일(<lg)에는 input 숨기고 아이콘 버튼만 노출 (터치 44×44 보장)
        */
       export function SearchTrigger({ onClick }: SearchTriggerProps) {
         return (
           <>
             {/* Desktop: readonly input (lg+) */}
             <button
               type="button"
               onClick={onClick}
               aria-label="종목 검색 열기"
               className="hidden h-9 w-full max-w-sm items-center gap-2 rounded-md border border-[var(--border)] bg-[var(--muted)] px-3 text-left text-[length:var(--t-sm)] text-[var(--muted-fg)] transition-colors hover:bg-[var(--muted)]/80 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--ring)] lg:flex"
             >
               <Search className="h-4 w-4" aria-hidden="true" />
               <span className="flex-1">종목명 또는 코드 검색</span>
               <kbd className="mono rounded border border-[var(--border)] bg-[var(--bg)] px-1.5 py-0.5 text-[11px] text-[var(--muted-fg)]">⌘K</kbd>
             </button>

             {/* Mobile: 아이콘 버튼 (lg 미만) */}
             <button
               type="button"
               onClick={onClick}
               aria-label="종목 검색 열기"
               className="inline-flex h-11 w-11 items-center justify-center rounded-md text-[var(--fg)] transition-colors hover:bg-[var(--muted)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--ring)] lg:hidden"
             >
               <Search className="h-5 w-5" aria-hidden="true" />
             </button>
           </>
         );
       }
       ```
    2) `webapp/src/components/layout/app-header.tsx` 수정:
       - 기존 `<Input disabled ...>` 블록 삭제
       - `AppHeader` 의 기본 `nav` 가 없을 때 **렌더 안 함** (GlobalSearch 가 nav prop 으로 주입됨)
       - `nav ?? null` 로 교체 (placeholder Input 완전 제거)
       - `aria-label="종목 검색 (Phase 6 활성)"` 설명 주석은 삭제 (더 이상 유효하지 않음)
    3) tsc 통과 확인.
  </action>
  <verify>
    <automated>test -f webapp/src/components/search/search-trigger.tsx &amp;&amp; ! grep -q 'placeholder="종목 검색 (Phase 6)"' webapp/src/components/layout/app-header.tsx &amp;&amp; pnpm --filter @gh-radar/webapp exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/src/components/search/search-trigger.tsx` 존재
    - `grep -q "aria-label=\"종목 검색 열기\"" webapp/src/components/search/search-trigger.tsx` → 매치
    - `grep -q "⌘K" webapp/src/components/search/search-trigger.tsx` → 매치
    - `grep -q "종목명 또는 코드 검색" webapp/src/components/search/search-trigger.tsx` → 매치
    - app-header.tsx 에서 기존 `placeholder="종목 검색 (Phase 6)"` 문자열 **제거됨**
    - app-header.tsx 에서 `nav ?? null` 또는 `{nav}` 로 교체됨 (placeholder 없음)
    - tsc 통과
  </acceptance_criteria>
  <done>SearchTrigger 컴포넌트 + app-header placeholder 제거</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GlobalSearch — CommandDialog + useDebouncedSearch + useCmdKShortcut + integration 테스트</name>
  <read_first>
    - .planning/phases/06-stock-search-detail/06-RESEARCH.md §"Pattern 2: cmdk shouldFilter 비활성"
    - .planning/phases/06-stock-search-detail/06-RESEARCH.md §"Code Examples > 예시 3: GlobalSearch"
    - .planning/phases/06-stock-search-detail/06-RESEARCH.md §"Pitfall 2, 5" (cmdk value + router 이동 후 dialog 닫기)
    - .planning/phases/06-stock-search-detail/06-UI-SPEC.md §"Copywriting Contract"
    - webapp/src/components/ui/command.tsx (Wave 0 설치 산출물 — export 목록 확인)
    - webapp/src/components/ui/badge.tsx (variant 목록 — outline 사용)
  </read_first>
  <behavior>
    - Test 1: 초기 렌더 시 Dialog 닫혀있음 (role="dialog" 없음)
    - Test 2: trigger 프로퍼티로 받은 함수가 toggle 역할 — 외부에서 open=true 주면 Dialog 열림 (controlled)
    - Test 3: query='삼성' 입력 → fake advance 300ms → results 렌더 (삼성전자 CommandItem 노출)
    - Test 4: CommandItem 클릭(또는 Enter) → router.push('/stocks/005930') 호출 + open=false 로 변경 콜백
    - Test 5: query='' 상태에서 `검색어를 입력하면 결과가 표시됩니다` 카피 노출
    - Test 6: loading 상태에서 `검색 중…` 카피 노출
    - Test 7: results=[] + query='xyz' + !loading → `"xyz" 에 해당하는 종목이 없습니다` 카피 노출
    - Test 8: error 상태 → `검색에 실패했습니다. 잠시 후 다시 시도해 주세요.` 카피 노출 (text-[var(--destructive)] 또는 --down)
    - Test 9: Command 요소에 `shouldFilter={false}` prop 전달 확인 (`data-slot` 또는 렌더 props 검증)
  </behavior>
  <action>
    1) 신규 `webapp/src/components/search/global-search.tsx` — 정확 구조:
       ```tsx
       'use client';
       import { useCallback, useState } from 'react';
       import { useRouter } from 'next/navigation';
       import {
         CommandDialog,
         CommandInput,
         CommandList,
         CommandEmpty,
         CommandLoading,
         CommandItem,
         CommandGroup,
       } from '@/components/ui/command';
       import { Badge } from '@/components/ui/badge';
       import { useCmdKShortcut } from '@/hooks/use-cmdk-shortcut';
       import { useDebouncedSearch } from '@/hooks/use-debounced-search';
       import { SearchTrigger } from './search-trigger';

       /**
        * GlobalSearch — ⌘K CommandDialog (Phase 6 SRCH-01/02).
        * - SearchTrigger (헤더 readonly input) + CommandDialog 통합 컴포넌트
        * - shouldFilter={false} — 서버 `name.ilike OR code.ilike` 결과를 그대로 표시 (Pitfall 2)
        * - CommandItem value={stock.code} — cmdk 내부 key 충돌 방지 (Pitfall 2)
        * - 선택 시 router.push + setOpen(false) + setQuery('')
        */
       export function GlobalSearch() {
         const [open, setOpen] = useState(false);
         const [query, setQuery] = useState('');
         const router = useRouter();
         const { results, loading, error } = useDebouncedSearch(query, 300);

         const toggle = useCallback(() => setOpen((v) => !v), []);
         useCmdKShortcut(toggle);

         const handleSelect = useCallback(
           (code: string) => {
             setOpen(false);
             setQuery('');
             router.push(`/stocks/${code}`);
           },
           [router],
         );

         const showEmpty = !loading && !error && query.trim().length > 0 && results.length === 0;
         const showInitial = query.trim().length === 0;

         return (
           <>
             <SearchTrigger onClick={toggle} />
             <CommandDialog
               open={open}
               onOpenChange={setOpen}
               shouldFilter={false}
             >
               <CommandInput
                 value={query}
                 onValueChange={setQuery}
                 placeholder="종목명 또는 종목코드를 입력하세요"
               />
               <CommandList>
                 {showInitial && (
                   <CommandEmpty>검색어를 입력하면 결과가 표시됩니다</CommandEmpty>
                 )}
                 {loading && <CommandLoading>검색 중…</CommandLoading>}
                 {error && (
                   <div className="px-3 py-2 text-[length:var(--t-sm)] text-[var(--destructive)]">
                     검색에 실패했습니다. 잠시 후 다시 시도해 주세요.
                   </div>
                 )}
                 {showEmpty && (
                   <CommandEmpty>&quot;{query}&quot; 에 해당하는 종목이 없습니다</CommandEmpty>
                 )}
                 {results.length > 0 && (
                   <CommandGroup>
                     {results.map((s) => (
                       <CommandItem
                         key={s.code}
                         value={s.code}
                         onSelect={() => handleSelect(s.code)}
                         className="flex items-center gap-3 py-2 px-3"
                       >
                         <span className="flex-1 text-[length:var(--t-sm)]">{s.name}</span>
                         <span className="mono text-[length:var(--t-caption)] text-[var(--muted-fg)]">
                           {s.code}
                         </span>
                         <Badge variant="outline">{s.market}</Badge>
                       </CommandItem>
                     ))}
                   </CommandGroup>
                 )}
               </CommandList>
             </CommandDialog>
           </>
         );
       }
       ```
       **주의 — CommandDialog 가 `shouldFilter` 를 top-level prop 으로 받지 않을 수 있음**: shadcn 산출물에서 CommandDialog 구현 확인 후 필요 시 내부 `<Command shouldFilter={false}>` 로 래핑 (shadcn 공식 예제 참조). 반드시 `shouldFilter={false}` 가 Command 요소에 적용되어야 함.
    2) 신규 `webapp/src/components/search/__tests__/global-search.test.tsx`:
       - `vi.mock('next/navigation', () => ({ useRouter: () => ({ push: pushSpy }) }))`
       - `vi.mock('@/hooks/use-debounced-search', () => ({ useDebouncedSearch: vi.fn() }))` — 상태별 return 제어
       - `vi.mock('@/hooks/use-cmdk-shortcut', () => ({ useCmdKShortcut: vi.fn() }))`
       - 9 tests (위 behavior)
       - Dialog 는 portal 렌더링 → `screen.findByRole('dialog')` 또는 `within(document.body).findByText(...)` 로 검색
       - `shouldFilter={false}` 검증은 Command mock 또는 snapshot 으로 확인 (`vi.mock('@/components/ui/command', () => ({...Command with spy}))` 또는 실제 dom inspect)
    3) `webapp/src/components/layout/app-header.tsx` 는 수정 **안 함** (Task 1 에서 이미 nav slot 만 렌더하도록 변경됨). Plan 05 에서 `AppShell` 루트에 GlobalSearch 를 마운트. 단, `AppShell` 이 이미 `nav` prop 을 받으므로 본 plan Task 3 에서 layout 통합.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp test -- --run src/components/search/__tests__/global-search.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/src/components/search/global-search.tsx` 존재
    - `grep -q "'use client'" webapp/src/components/search/global-search.tsx` → 매치
    - `grep -q "useDebouncedSearch(query, 300)" webapp/src/components/search/global-search.tsx` → 매치
    - `grep -q "useCmdKShortcut(toggle)" webapp/src/components/search/global-search.tsx` → 매치
    - `grep -q "shouldFilter={false}" webapp/src/components/search/global-search.tsx` → 매치
    - `grep -q "value={s.code}" webapp/src/components/search/global-search.tsx` → 매치
    - `grep -q "router.push(\`/stocks/\${code}\`)" webapp/src/components/search/global-search.tsx` → 매치
    - `grep -q "종목명 또는 종목코드를 입력하세요" webapp/src/components/search/global-search.tsx` → 매치
    - `grep -q "검색 중…" webapp/src/components/search/global-search.tsx` → 매치
    - `grep -q "검색어를 입력하면 결과가 표시됩니다" webapp/src/components/search/global-search.tsx` → 매치
    - `grep -q "검색에 실패했습니다" webapp/src/components/search/global-search.tsx` → 매치
    - test 파일 9 tests 전부 pass
  </acceptance_criteria>
  <done>GlobalSearch 컴포넌트 + 9 integration tests 통과 (cmdk shouldFilter, value=code, 상태별 카피)</done>
</task>

<task type="auto">
  <name>Task 3: AppShell/AppHeader 에 GlobalSearch 마운트</name>
  <read_first>
    - webapp/src/components/layout/app-shell.tsx (nav prop 존재 확인)
    - webapp/src/components/layout/app-header.tsx (Task 1 수정본)
    - webapp/src/app/layout.tsx 또는 webapp/src/app/scanner/page.tsx (AppShell 호출 위치 확인)
  </read_first>
  <action>
    1) `webapp/src/components/layout/app-shell.tsx` 수정:
       - 기본 `nav` prop 이 미지정일 때 `<GlobalSearch />` 를 자동 마운트 하도록 기본값 설정
       - 정확히: `nav = <GlobalSearch />` 가 기본값. 명시적으로 `nav={null}` 을 주면 비활성.
       - import 추가: `import { GlobalSearch } from '@/components/search/global-search';`
    2) 변경 후 `/scanner` 와 향후 `/stocks/:code` 모두에서 헤더에 GlobalSearch 가 자동 노출되어야 함.
    3) tsc + 단위 테스트(use-polling.test.ts 등) 회귀 확인.
  </action>
  <verify>
    <automated>grep -q "import { GlobalSearch }" webapp/src/components/layout/app-shell.tsx &amp;&amp; grep -q "<GlobalSearch" webapp/src/components/layout/app-shell.tsx &amp;&amp; pnpm --filter @gh-radar/webapp test -- --run &amp;&amp; pnpm --filter @gh-radar/webapp exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { GlobalSearch }" webapp/src/components/layout/app-shell.tsx` → 매치
    - `grep -q "<GlobalSearch" webapp/src/components/layout/app-shell.tsx` → 매치
    - 전체 vitest suite 회귀 없음 (이전 통과한 test 모두 green 유지)
    - tsc 통과
  </acceptance_criteria>
  <done>AppShell 기본 nav 에 GlobalSearch 자동 마운트</done>
</task>

</tasks>

<threat_model>
N/A (조회 전용 UI). `q` 쿼리 파라미터는 서버 zod `SearchQuery` 에서 이미 검증/sanitize 됨 (`server/src/schemas/search.ts`). 클라이언트는 에스케이프된 URLSearchParams 로 전달.
</threat_model>

<verification>
- `pnpm --filter @gh-radar/webapp test -- --run` → PASS (global-search test 9개 포함 전체)
- `pnpm --filter @gh-radar/webapp exec tsc --noEmit` → PASS
- 로컬 수동 스모크(선택): `pnpm --filter @gh-radar/webapp dev` → localhost:3000/scanner → ⌘K → "삼성" → 자동완성 노출
</verification>

<success_criteria>
- SRCH-01 (종목명/코드 검색) + SRCH-02 (자동완성 드롭다운) UI 완성
- 헤더 placeholder 완전 제거 → readonly 트리거 + ⌘K 키캡
- cmdk shouldFilter=false + value=code 로 서버 검색 결과 100% 표시
- debounce 300ms + AbortController 로 race condition 방지
</success_criteria>

<output>
`.planning/phases/06-stock-search-detail/06-03-SUMMARY.md` 생성
</output>
