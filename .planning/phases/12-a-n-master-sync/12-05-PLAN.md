---
phase: 12-a-n-master-sync
plan: 05
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - webapp/src/lib/limit-up-api.ts
  - webapp/src/lib/limit-up-format.ts
  - webapp/src/lib/limit-up-format.test.ts
  - webapp/src/components/stock/stock-limit-up-section.tsx
  - webapp/src/components/stock/stock-detail-client.tsx
autonomous: false
requirements: [LIMIT-01]
nyquist_compliant: true

must_haves:
  truths:
    - "종목상세(/stocks/[code]) 에 상한가 다음날 이력 섹션이 ②안(데이터 대시보드) 레이아웃으로 렌더된다"
    - "KPI 4그리드(시초가 익절%·평균 시초가·최악 저가·분포 spark) + 전체 OHLC 이벤트 표 + 테마 가로 풀링 바"
    - "N(resolvedEvents)<3 이면 큰 익절% 숨기고 카운트만, 이벤트 0회면 빈 상태 카피"
  artifacts:
    - path: "webapp/src/components/stock/stock-limit-up-section.tsx"
      provides: "②안 섹션 컴포넌트 (KPI 그리드 + OHLC 표 + 테마 풀링 바 + 면책)"
      min_lines: 120
    - path: "webapp/src/lib/limit-up-api.ts"
      provides: "fetchStockLimitUp apiFetch<LimitUpResponse> wrapper"
      contains: "apiFetch"
    - path: "webapp/src/lib/limit-up-format.ts"
      provides: "N≥3 게이팅 + spark 버킷 색 매핑 순수함수 (테스트 가능 분리)"
      contains: "sparkBucketTone"
    - path: "webapp/src/lib/limit-up-format.test.ts"
      provides: "게이팅 경계(N=2/3) + spark 버킷 색(index 2 = pos) 단위 테스트"
      contains: "sparkBucketTone"
  key_links:
    - from: "webapp/src/components/stock/stock-detail-client.tsx"
      to: "StockLimitUpSection"
      via: "co-movement 섹션 인근 마운트"
      pattern: "StockLimitUpSection"
    - from: "webapp/src/components/stock/stock-limit-up-section.tsx"
      to: "/api/stocks/:code/limit-up"
      via: "fetchStockLimitUp"
      pattern: "fetchStockLimitUp"
---

<objective>
Phase 12 (LIMIT-01) webapp 표시 — `StockLimitUpSection` 컴포넌트를 목업 **②안(데이터 대시보드)** 레이아웃으로 작성하고, Phase 11 `stock-comovement-section` 의 fetch/quiet-fallback/마운트 패턴을 미러해 종목상세에 렌더한다. Vercel 배포 + 시각 검증.

Purpose: 본 phase 에서 **새로 작성하는 두 번째 축인 webapp 표시 컴포넌트**. 사전계산 `{ hero, events, themes }` 를 KPI 4그리드 + 전체 OHLC 표 + 테마 가로 풀링 바로 표현(목업 ② 섹션 시각 기준). 국내 색상(수익=빨강 --up/손실=파랑 --down), N≥3 게이팅, 빈 상태.
Output: limit-up-api.ts + stock-limit-up-section.tsx + stock-detail-client 마운트 + Vercel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-a-n-master-sync/12-CONTEXT.md
@.planning/phases/12-a-n-master-sync/12-RESEARCH.md
@.planning/phases/12-a-n-master-sync/12-limit-up-mockup.html

<interfaces>
<!-- Plan 01 산출 공유 타입 + Plan 03 라우트 응답 (apiFetch<LimitUpResponse> 객체 계약) -->
```typescript
interface LimitUpResponse { hero: LimitUpStockStats; events: LimitUpEvent[]; themes: LimitUpThemeStat[]; }
interface LimitUpEvent { date; isJeomsang; nextOpenRet|null; nextHighRet|null; nextLowRet|null; nextCloseRet|null; tradeAmount; turnover|null; }
interface LimitUpStockStats { totalEvents; resolvedEvents; winCount; winRate|null; avgOpenRet|null; worstLowRet|null; recentWins; recentLosses; histogram: number[]; }
interface LimitUpThemeStat { themeId; themeName; sampleN; winRate|null; avgOpenRet|null; }
```

<!-- 복제 원본 webapp/src/lib/comovement-api.ts -->
```typescript
export function fetchStockComovement(code, k=8, signal?) {
  return apiFetch<CoMovementResponse>(`/api/stocks/${encodeURIComponent(code)}/co-movement?k=${k}`, { signal });
}
```

<!-- 복제 원본 webapp/.../stock-comovement-section.tsx 패턴: 'use client', mount fetch + AbortController,
     loaded/hasError state, quiet fallback(에러 시 null), 종목 간 내비게이션 state 리셋, em-dash, oklch 토큰만. -->

<!-- 목업 ② 섹션 시각 기준 (12-limit-up-mockup.html, ② 데이터 대시보드):
     - .kpi-grid (repeat(4,1fr)): 시초가 익절 '71% 5/7' / 평균 시초가 '+2.8%' / 최악 저가 '−7.1%' / 분포 .spark(5버킷)
     - .tbl-wrap table: 상한가일·구분(tag-jeom/tag-norm)·시·고·저·종·거래대금·회전율. faded(오래된 건).
     - 두번째 .card: .pool-bar 리스트 (테마명·track 진행바·익절률%·"N=… · 평균 ±x%")
     - .legend: 점상 정의·회전율=거래량/상장주식수. .disc 면책. -->
색 토큰: --up(빨강 수익) / --down(파랑 손실) / --flat(보합) / --up-bg(점상 태그 배경) / --muted(일반 태그). globals.css oklch 직접 사용(차트 아님 → chart-colors.ts 변환 불필요, D-13).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/limit-up-api.ts (apiFetch wrapper)</name>
  <files>webapp/src/lib/limit-up-api.ts</files>
  <read_first>
    - webapp/src/lib/comovement-api.ts (apiFetch<T> wrapper 복제 원본)
    - webapp/src/lib/api.ts (apiFetch — envelope 파싱 + X-Request-Id + 8s 타임아웃 + ApiClientError)
    - packages/shared/src/limitUp.ts (LimitUpResponse 계약)
  </read_first>
  <action>
    `webapp/src/lib/limit-up-api.ts`: comovement-api 복제 + 교체. `export function fetchStockLimitUp(code: string, signal?: AbortSignal): Promise<LimitUpResponse> { return apiFetch<LimitUpResponse>(`/api/stocks/${encodeURIComponent(code)}/limit-up`, { signal }); }`. import `{ LimitUpResponse } from '@gh-radar/shared'`. (k 파라미터 없음 — 단일 종목 전체 이력 반환, 페이지네이션은 클라 더보기.)
  </action>
  <verify>
    <automated>pnpm -F @gh-radar/webapp typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "apiFetch<LimitUpResponse>" webapp/src/lib/limit-up-api.ts` == 1
    - `grep -c "/limit-up" webapp/src/lib/limit-up-api.ts` == 1 (경로 일치)
    - `pnpm -F @gh-radar/webapp typecheck` exit 0
  </acceptance_criteria>
  <done>fetchStockLimitUp 가 apiFetch<LimitUpResponse> 로 /api/stocks/:code/limit-up 호출, typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/limit-up-format.ts 순수함수 + 단위 테스트 (게이팅 + spark 색 매핑)</name>
  <files>webapp/src/lib/limit-up-format.ts, webapp/src/lib/limit-up-format.test.ts</files>
  <read_first>
    - packages/shared/src/limitUp.ts (LimitUpStockStats — resolvedEvents/winRate/histogram 필드)
    - webapp/src/lib/format.ts (기존 포맷 유틸 톤 — 거래대금 억 단위 등 재사용 여부)
    - .planning/phases/12-a-n-master-sync/12-CONTEXT.md D-09(N≥3 게이팅) / D-11(분포 5버킷)
  </read_first>
  <behavior>
    - shouldShowWinRate({ resolvedEvents: 3 }) === true; shouldShowWinRate({ resolvedEvents: 2 }) === false (N≥3 게이팅 경계, D-09)
    - sparkBucketTone(0) === 'down'; sparkBucketTone(1) === 'down'; sparkBucketTone(2) === 'up'; sparkBucketTone(3) === 'up'; sparkBucketTone(4) === 'up' (off-by-one 박제 — index 2 = 0~+5% 는 양수 → up, BLOCKER 3 회귀 차단)
    - fmtRet(null) === '—'; fmtRet(0) 보합; fmtRet(2.8) === '+2.8%'; fmtRet(-7.1) === '−7.1%' (방향 부호)
    - fmtTurnover(null) === '—'; fmtTurnover(0.18) === '18%' (회전율 NULL → em-dash, D-07)
  </behavior>
  <action>
    `webapp/src/lib/limit-up-format.ts` 신규 순수함수 모듈 (컴포넌트에서 분리해 테스트 가능하게):
    - `shouldShowWinRate(stats: Pick<LimitUpStockStats,'resolvedEvents'>): boolean` = `stats.resolvedEvents >= 3` (D-09 게이팅).
    - `sparkBucketTone(index: number): 'up' | 'down'` = `index >= 2 ? 'up' : 'down'` (5버킷: 0/1=음수→down, 2/3/4=0이상→up. BLOCKER 3 off-by-one 정확 매핑).
    - `fmtRet(v: number | null): string` = null → '—', else 부호 접두(`v>0`→'+', `v<0`→'−', 0→'')와 `v.toFixed(1)` 조합 → 예 '+2.8%' / '−7.1%' / '0.0%' (방향 부호, 보합 0.0%). 색은 컴포넌트가 별도 tone 으로 적용(포맷과 분리).
    - `fmtTurnover(v: number | null): string` = null → '—', else `${Math.round(v*100)}%`.
    `webapp/src/lib/limit-up-format.test.ts`: behavior 4종 전부 — 특히 `sparkBucketTone(2) === 'up'` 으로 spark off-by-one 회귀 박제.
  </action>
  <verify>
    <automated>pnpm -F @gh-radar/webapp test -- limit-up-format && pnpm -F @gh-radar/webapp typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sparkBucketTone\|shouldShowWinRate\|fmtRet\|fmtTurnover" webapp/src/lib/limit-up-format.ts` ≥ 4
    - `grep -c "sparkBucketTone(2)" webapp/src/lib/limit-up-format.test.ts` ≥ 1 (index 2 = up 박제 — BLOCKER 3 회귀 가드)
    - `grep -c "resolvedEvents: 2\|resolvedEvents: 3\|>= 3" webapp/src/lib/limit-up-format.test.ts` ≥ 1 (N≥3 경계)
    - `pnpm -F @gh-radar/webapp test -- limit-up-format` green
  </acceptance_criteria>
  <done>게이팅/spark색/포맷 순수함수가 분리되고 단위 테스트가 spark index 2 = up(BLOCKER 3 회귀) + N≥3 경계를 박제, 테스트 green.</done>
</task>

<task type="auto">
  <name>Task 3: stock-limit-up-section.tsx (②안 데이터 대시보드)</name>
  <files>webapp/src/components/stock/stock-limit-up-section.tsx</files>
  <read_first>
    - webapp/src/components/stock/stock-comovement-section.tsx ('use client' fetch/AbortController/loaded·hasError state/quiet fallback/state 리셋/em-dash/oklch 토큰 패턴 복제 원본)
    - .planning/phases/12-a-n-master-sync/12-limit-up-mockup.html ② 섹션 (kpi-grid / tbl-wrap table 8컬럼 / pool-bar / legend / disc — **시각 기준**)
    - .planning/phases/12-a-n-master-sync/12-CONTEXT.md D-08~D-18 (KPI 히어로·N≥3 게이팅·최근3회·분포 spark·이벤트표·테마 풀링 바·면책)
    - webapp/src/lib/format.ts (거래대금 한글 단위 '억' 포맷 — formatTradeAmount 류 재사용 가능 여부 확인)
    - packages/shared/src/limitUp.ts (LimitUpEvent/LimitUpStockStats/LimitUpThemeStat 필드)
  </read_first>
  <action>
    `webapp/src/components/stock/stock-limit-up-section.tsx` 신규 ('use client'):
    **순수 로직은 Task 2 의 `limit-up-format.ts`(shouldShowWinRate/sparkBucketTone/fmtRet/fmtTurnover) 를 import 해 사용** — 컴포넌트에 게이팅·spark 색·포맷 로직을 인라인 재구현하지 말 것(테스트 박제된 단일 출처).
    상태/패턴 = comovement-section 미러: mount fetch(fetchStockLimitUp + AbortController), `loaded`/`hasError` state, 종목 간 내비게이션 시 state 리셋, 에러 시 quiet fallback(`return null` — error.message 미노출, console.error 만, T-12-05-01), `!loaded` 시 null(레이아웃 점프 방지).

    렌더 (목업 ② 레이아웃):
    1. **빈 상태** (hero.totalEvents === 0 또는 events.length === 0): co-movement 빈 박스 미러 — "아직 마감상한가 이력이 없습니다" + 면책 톤. (대형주 정상 빈 상태.)
    2. **헤더**: 섹션 h2 "상한가 다음날 이력" + 서브 "상한가 종가 매수 → 다음 영업일 수익률 · 출처 KRX" + 우측 nbadge "최근 24개월 · {totalEvents}회".
    3. **KPI 4그리드** (.kpi-grid repeat(4,1fr)):
       - ① 시초가 익절: `resolvedEvents >= 3` 이면 `{Math.round(winRate*100)}% <small>{winCount}/{resolvedEvents}</small>` (--up); **N<3 이면 큰 % 숨기고 "{winCount}/{resolvedEvents}회"만** (D-09 게이팅).
       - ② 평균 시초가: `avgOpenRet` 방향색(+면 --up, −면 --down), null → "—".
       - ③ 최악 저가: `worstLowRet` (--down), null → "—".
       - ④ 분포 spark: histogram 5버킷 → `.spark .b`. **버킷 index → 색 매핑(정확히): bucket[0] −10~−5 = neg(--down), bucket[1] −5~0 = neg(--down), bucket[2] 0~+5 = pos(--up), bucket[3] +5~+10 = pos(--up), bucket[4] +10%+ = pos(--up)** (목업 ②: neg/neg/pos/pos/pos). 즉 index ≤ 1 만 neg, index ≥ 2 는 pos. 각 막대 height = 버킷카운트/max*100%.
       서브: "최근 3회 {recentWins}승 {recentLosses}패" (D-10) 를 표 상단 캡션 또는 KPI 보조줄(Claude 재량 — 목업 톤 유지).
    4. **OHLC 이벤트 표** (.tbl-wrap, events DESC): 컬럼 상한가일·구분·시·고·저·종·거래대금·회전율. 구분 = isJeomsang ? tag-jeom("점상", --up-bg/--up) : tag-norm("일반", --muted). 시/고/저/종 = next*Ret 방향색(+빨강/−파랑/0 보합), null → "—". 거래대금 = 억 단위 포맷. 회전율 = turnover null → "—" else `{Math.round(turnover*100)}%` (D-07 근사 — legend 또는 헤더 툴팁에 "현재 상장주식수 기준 근사"). 오래된 이벤트 faded(opacity 0.5) — 상위 N(예: 4) 이후 또는 기준일 이전(Claude 재량). 길면 상위 N + "더보기"(useState expanded, 추가 fetch 없음 — 전체 events 보유).
    5. **테마 풀링 바** (별도 card, themes sample_n DESC): `.pool-bar` 각 — 테마명 + .track 진행바(width=winRate%) + 익절률%(winRate null → "—", --up if >0.5) + "N={sampleN} · 평균 {avgOpenRet>=0?'+':''}{avgOpenRet}%". themes 비어있으면 카드 생략. 많으면 상위 일부 + 더보기(Claude 재량).
    6. **legend** (점상 정의 + 회전율=거래량/상장주식수) + **면책 .disc** ("표본 {totalEvents}회로 적음. 과거 통계이며 미래 수익을 보장하지 않습니다. 출처 KRX.", D-14).

    색: globals.css oklch 토큰만 (--up/--down/--flat/--up-bg/--muted/--accent-fg). 신규 토큰·하드코딩 0. 차트 아님 → chart-colors.ts 변환 불필요.
  </action>
  <verify>
    <automated>pnpm -F @gh-radar/webapp typecheck && pnpm -F @gh-radar/webapp build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fetchStockLimitUp" webapp/src/components/stock/stock-limit-up-section.tsx` ≥ 1
    - `grep -c "sparkBucketTone\|shouldShowWinRate" webapp/src/components/stock/stock-limit-up-section.tsx` ≥ 1 (Task 2 순수함수 사용 — 인라인 재구현 금지)
    - `grep -c "resolvedEvents\|winRate" ...` ≥ 1 + N≥3 게이팅 분기 존재 (`>= 3` 또는 `< 3` 비교)
    - `grep -c "isJeomsang\|점상" ...` ≥ 1 (점상 태그)
    - `grep -c "var(--up)\|var(--down)\|var(--up-bg)\|var(--flat)" ...` ≥ 3 (국내 색상 토큰)
    - spark 버킷 색 매핑이 off-by-one 없음 — index ≥ 2(0~+5% 이상)는 pos(--up), index ≤ 1 만 neg(--down). 코드에 `i >= 2`(또는 `index < 2`/`bucketIndex >= 2`) 형태의 경계 분기가 존재하고 음수 버킷에 0~+5% 가 포함되지 않음 (grep 으로 경계 상수 2 확인 + 코드 리뷰)
    - `grep -c "turnover" ...` ≥ 1 + null → "—" 처리
    - `grep -c "미래 수익을 보장하지 않습니다\|출처 KRX" ...` ≥ 1 (면책)
    - `grep -c "chart-colors\|#[0-9a-fA-F]\{6\}\|oklch(" ...` == 0 (하드코딩/변환 0)
    - `grep -c "return null" ...` ≥ 1 (quiet fallback)
    - `pnpm -F @gh-radar/webapp build` exit 0
  </acceptance_criteria>
  <done>②안 섹션이 KPI 4그리드+분포 spark / OHLC 8컬럼 표(점상 태그·faded·더보기) / 테마 풀링 바 / 면책 으로 렌더, N≥3 게이팅·빈 상태·국내 색상·quiet fallback, 빌드 green.</done>
</task>

<task type="auto">
  <name>Task 4: stock-detail-client.tsx 마운트</name>
  <files>webapp/src/components/stock/stock-detail-client.tsx</files>
  <read_first>
    - webapp/src/components/stock/stock-detail-client.tsx (마운트 위치 — `<StockComovementSection stockCode={stock.code} />` 인근, D-21)
  </read_first>
  <action>
    `webapp/src/components/stock/stock-detail-client.tsx`: import `{ StockLimitUpSection } from './stock-limit-up-section';` 추가 + `<StockComovementSection stockCode={stock.code} />` **바로 다음**에 `<StockLimitUpSection stockCode={stock.code} />` 추가 (co-movement 섹션 인근, CONTEXT D-21). 기존 StockThemeChips/StockComovementSection/news/discussion 순서 유지.
  </action>
  <verify>
    <automated>pnpm -F @gh-radar/webapp typecheck && pnpm -F @gh-radar/webapp build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "StockLimitUpSection" webapp/src/components/stock/stock-detail-client.tsx` == 2 (import + 마운트)
    - 마운트 라인이 `StockComovementSection` 라인 인근 (행 번호 근접)
    - `pnpm -F @gh-radar/webapp build` exit 0
  </acceptance_criteria>
  <done>StockLimitUpSection 이 종목상세 co-movement 섹션 다음에 마운트, 빌드 green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: [BLOCKING] Vercel 배포 + 종목상세 시각 검증</name>
  <files>webapp/src/components/stock/stock-limit-up-section.tsx, webapp/src/components/stock/stock-detail-client.tsx</files>
  <action>오케스트레이터가 Vercel production 배포(repo root pull→build→deploy --prebuilt) 후 사용자 시각 확인: /stocks/000440(②안 목업 대조) / 표본<3 종목(큰 % 숨김) / 005930(빈 상태) / 다크·라이트 색상. 상세는 how-to-verify 참조.</action>
  <what-built>
    상한가 다음날 이력 섹션(②안 데이터 대시보드)이 종목상세에 마운트. Plan 03(server 라우트) + Plan 04(워커가 채운 테이블)가 prod 에 있어야 실데이터 표시.
  </what-built>
  <how-to-verify>
    오케스트레이터가 Vercel 배포 후 사용자 시각 확인:
    1. Vercel production 배포 (repo root pull→build→deploy --prebuilt, 메모리 reference_vercel_frontend_deploy — ignoreCommand stuck 회피).
    2. N≥3 종목(황금 케이스 000440 등 이벤트 다수) `/stocks/000440` 진입 → ②안 목업과 대조: KPI 4그리드(시초가 익절% + M/N) + 분포 spark + OHLC 8컬럼 표(점상 태그·시고저종 빨강/파랑·거래대금억·회전율%·오래된 건 faded) + 테마 풀링 바(N desc) + 면책.
    3. 표본<3 종목 → 큰 익절% 숨김, 카운트만 노출 확인.
    4. 이벤트 0회 대형주(005930) → 빈 상태 카피("아직 마감상한가 이력이 없습니다").
    5. 색상: 수익=빨강(--up)/손실=파랑(--down) 국내 관행. 다크/라이트 둘 다 깨짐 없음.
  </how-to-verify>
  <read_first>
    - .planning/phases/12-a-n-master-sync/12-limit-up-mockup.html ② 섹션 (대조 기준)
    - 메모리 reference_vercel_frontend_deploy (수동 배포 절차)
    - 메모리 feedback_vercel_env_paste_newline (env 검증)
  </read_first>
  <acceptance_criteria>
    - Vercel 배포 성공 + 새 production URL 반영
    - /stocks/000440 ②안 레이아웃(KPI 4그리드+spark / OHLC 8컬럼 / 테마 풀링 바 / 면책) 목업과 일치
    - N<3 종목 큰 % 숨김 + 카운트만 / 이벤트 0회 빈 상태 / 색상 국내 관행 / 다크·라이트 정상
  </acceptance_criteria>
  <resume-signal>Vercel 배포 + 000440 ②안 시각 일치 + N<3 게이팅 + 빈 상태 + 색상 확인 후 "approved" 또는 이슈 보고</resume-signal>
  <verify>Vercel 배포 성공 + /stocks/000440 ②안 레이아웃(KPI 4그리드/OHLC 8컬럼/테마 풀링 바/면책) 목업 일치 + N<3 게이팅 + 빈 상태 + 국내 색상 + 다크·라이트 정상</verify>
  <done>상한가 다음날 이력 섹션이 ②안으로 종목상세에 렌더, 실데이터 표시(000440), N게이팅·빈상태·색상 시각 검증 완료</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server /api/stocks/:code/limit-up | apiFetch (envelope + 타임아웃) — 미인증 공개 read |
| server 에러 → webapp 표시 | PostgREST/RLS 내부정보가 사용자에게 노출되면 안 됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | Information Disclosure | stock-limit-up-section 에러 | mitigate | 에러 시 섹션 quiet fallback(return null) + console.error 만 — error.message 사용자 미노출 (stock-comovement-section 선례) |
| T-12-05-02 | Tampering (UX 오도) | 표본 부족 통계 | mitigate | N(resolvedEvents)<3 시 큰 익절% 숨기고 카운트만 (D-09) + 면책 항상 노출 — 가짜정밀도/과신 방지 |
| T-12-05-03 | Information Disclosure | 회전율 근사 | accept | turnover 는 현재 listing_shares 근사 → legend/툴팁 표기로 처리(생략 아님, D-07) — 공개 시장 데이터라 위험 낮음 |
</threat_model>

<verification>
- Task 1~3: typecheck + build green, grep 검증(게이팅·색상·면책·quiet fallback·마운트)
- Task 4 [BLOCKING]: Vercel 배포 + ②안 시각 일치 + N게이팅 + 빈 상태 + 색상
</verification>

<success_criteria>
- 종목상세에 상한가 다음날 이력 섹션이 ②안(데이터 대시보드) 레이아웃으로 렌더
- KPI 4그리드(시초가 익절%·평균·최악·분포 spark) + OHLC 8컬럼 표(점상·faded·더보기) + 테마 가로 풀링 바(N desc) + 면책
- N(resolvedEvents)<3 게이팅(큰 % 숨김) + 이벤트 0회 빈 상태 + 국내 색상(--up/--down) + quiet fallback
- 신규 토큰/하드코딩 색 0 (globals.css oklch 토큰 직접, 차트 아님)
</success_criteria>

<output>
After completion, create `.planning/phases/12-a-n-master-sync/12-05-SUMMARY.md`
</output>
