---
phase: 12-a-n-master-sync
plan: 04
type: execute
wave: 4
depends_on: ["02", "03"]
files_modified:
  - scripts/setup-limit-up-sync-iam.sh
  - scripts/deploy-limit-up-sync.sh
  - scripts/smoke-limit-up-sync.sh
autonomous: false
requirements: [LIMIT-01]
user_setup: []

must_haves:
  truths:
    - "limit-up-sync 워커가 Cloud Run Job + Scheduler 로 야간 1회 (candle-sync EOD 이후) 실행되어 rebuild_limit_up RPC 를 호출한다"
    - "smoke 가 Job execute exit 0 + limit_up_events 행수 > 0 + Scheduler ENABLED 를 검증한다"
  artifacts:
    - path: "scripts/deploy-limit-up-sync.sh"
      provides: "Cloud Run Job gh-radar-limit-up-sync + Scheduler gh-radar-limit-up-sync-nightly 배포 (OAuth invoker)"
      contains: "limit-up-sync"
    - path: "scripts/setup-limit-up-sync-iam.sh"
      provides: "SA gh-radar-limit-up-sync-sa + supabase-service-role accessor 1개 (외부 키 0)"
      contains: "gh-radar-limit-up-sync-sa"
    - path: "scripts/smoke-limit-up-sync.sh"
      provides: "INV-1~5 (Job exit 0, complete 로그, failed 0, limit_up_events count>0, Scheduler ENABLED)"
      contains: "limit_up_events"
  key_links:
    - from: "scripts/deploy-limit-up-sync.sh"
      to: "workers/limit-up-sync/Dockerfile"
      via: "docker build -f"
      pattern: "workers/limit-up-sync/Dockerfile"
    - from: "Cloud Scheduler gh-radar-limit-up-sync-nightly"
      to: "Cloud Run Job gh-radar-limit-up-sync"
      via: "OAuth invoker (OIDC 금지)"
      pattern: "oauth-service-account-email"
---

<objective>
Phase 12 (LIMIT-01) 워커 배포 — `limit-up-sync` 의 IAM/deploy/smoke 스크립트를 Phase 11 `co-movement-sync` 1:1 복제로 작성하고, Cloud Run Job + Scheduler(야간 1회, candle-sync EOD 이후)를 배포해 `rebuild_limit_up` 야간 재계산을 활성화한다.

Purpose: 사전계산 갱신 자동화(D-19/D-20). 외부 API 키 0 — supabase-service-role accessor 1개만(자체 DB 집계). co-movement-sync 배포 패턴(OAuth invoker, OIDC 금지, 리소스 단위 바인딩) 그대로 미러.
Output: 3 스크립트 + GCP 리소스(SA + Job + Scheduler) + smoke 통과.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-a-n-master-sync/12-CONTEXT.md
@.planning/phases/12-a-n-master-sync/12-RESEARCH.md
@.planning/phases/12-a-n-master-sync/12-VALIDATION.md

<interfaces>
<!-- 복제 원본 3 스크립트 — read_first 로 읽고 식별자만 교체.
     comovement-sync → limit-up-sync, theme_comovement/cosurge_edges → limit_up_events, rebuild_comovement → rebuild_limit_up. -->

deploy-comovement-sync.sh 핵심:
  IMAGE="${REGISTRY}/co-movement-sync:${SHA}"        → limit-up-sync:${SHA}
  docker build -f workers/co-movement-sync/Dockerfile → workers/limit-up-sync/Dockerfile
  RUNTIME_SA=gh-radar-comovement-sync-sa             → gh-radar-limit-up-sync-sa
  deploy_job "gh-radar-comovement-sync" "180s" "512Mi" → "gh-radar-limit-up-sync"
  Scheduler "gh-radar-comovement-sync-nightly" cron "0 2 * * 2-6" → "gh-radar-limit-up-sync-nightly"
  COMMON_SECRETS="SUPABASE_SERVICE_ROLE_KEY=gh-radar-supabase-service-role:latest" (동일)
  Scheduler 인증 = --oauth-service-account-email (OIDC 금지)

setup-comovement-sync-iam.sh 핵심:
  신규 SA gh-radar-comovement-sync-sa → gh-radar-limit-up-sync-sa
  재사용 SA gh-radar-scheduler-sa (Phase 05.1)
  secret accessor = gh-radar-supabase-service-role 1개만 (외부 API 키 0)

smoke-comovement-sync.sh 핵심:
  INV-4 = theme_comovement/cosurge_edges count>0 → limit_up_events count>0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: setup/deploy/smoke 스크립트 3종 (co-movement-sync 1:1 복제 + 식별자 교체)</name>
  <files>scripts/setup-limit-up-sync-iam.sh, scripts/deploy-limit-up-sync.sh, scripts/smoke-limit-up-sync.sh</files>
  <read_first>
    - scripts/setup-comovement-sync-iam.sh (SA + secret accessor 원본)
    - scripts/deploy-comovement-sync.sh (Job + Scheduler 배포 원본 — IMAGE/Dockerfile 경로/SA/Job명/Scheduler명/cron/OAuth)
    - scripts/smoke-comovement-sync.sh (INV-1~5 원본 — INV-4 테이블 count)
    - workers/limit-up-sync/Dockerfile (Plan 01 산출 — 빌드 경로 확인. 이미지 태그 limit-up-sync)
    - .planning/phases/12-a-n-master-sync/12-CONTEXT.md D-20 (스케줄 = candle-sync EOD 이후 야간 1회)
  </read_first>
  <action>
    `scripts/setup-limit-up-sync-iam.sh` = `setup-comovement-sync-iam.sh` 복제 + 교체:
    - 신규 SA `gh-radar-comovement-sync-sa` → `gh-radar-limit-up-sync-sa` (전 occurrence: 변수명/describe/create/display-name/accessor 바인딩).
    - display-name: "gh-radar limit-up-sync (rebuild_limit_up RPC, Phase 12 LIMIT-01)".
    - 재사용 SA `gh-radar-scheduler-sa` + secret `gh-radar-supabase-service-role` 동일(외부 키 0).
    - 주석 "co-movement"/"COMV-01"/"rebuild_comovement" → "limit-up"/"LIMIT-01"/"rebuild_limit_up".

    `scripts/deploy-limit-up-sync.sh` = `deploy-comovement-sync.sh` 복제 + 교체:
    - IMAGE: `co-movement-sync:${SHA}` → `limit-up-sync:${SHA}` (+ :latest).
    - docker build `-f workers/co-movement-sync/Dockerfile` → `-f workers/limit-up-sync/Dockerfile`.
    - RUNTIME_SA / SA 가드 루프 `gh-radar-comovement-sync-sa` → `gh-radar-limit-up-sync-sa`.
    - `deploy_job "gh-radar-comovement-sync" "180s" "512Mi"` → `deploy_job "gh-radar-limit-up-sync" "180s" "512Mi"` (task-timeout 180s 유지 — 1.4M행 24m, comovement ~2.5M행 선례 마진 충분).
    - Scheduler `gh-radar-comovement-sync-nightly` → `gh-radar-limit-up-sync-nightly`, cron `"0 2 * * 2-6"` 유지 (D-20 — EOD candle-sync 이후 새벽, 화~토). `--oauth-service-account-email` 유지(OIDC 금지).
    - COMMON_ENV `LOOKBACK_MONTHS=24` + COMMON_SECRETS `SUPABASE_SERVICE_ROLE_KEY=gh-radar-supabase-service-role:latest` 동일.
    - 완료 echo 문구 limit-up-sync 로 교체.

    `scripts/smoke-limit-up-sync.sh` = `smoke-comovement-sync.sh` 복제 + 교체:
    - JOB `gh-radar-comovement-sync` → `gh-radar-limit-up-sync`, SCHED `...-nightly` → `gh-radar-limit-up-sync-nightly`, cron 동일.
    - INV-2/3 로그 메시지 `"co-movement-sync complete"`/`"rebuild_comovement complete"` → `"limit-up-sync complete"`/`"rebuild_limit_up complete"`, `"co-movement-sync failed"` → `"limit-up-sync failed"`.
    - INV-4: `theme_comovement` count>0 + `cosurge_edges` count>0 두 체크 → `limit_up_events` count>0 + `limit_up_stock_stats` count>0 두 체크 (REST count=exact 패턴 동일, `tr -d '\r'` 유지 — DI-02 CR 버그 회피).
  </action>
  <verify>
    <automated>bash -n scripts/setup-limit-up-sync-iam.sh && bash -n scripts/deploy-limit-up-sync.sh && bash -n scripts/smoke-limit-up-sync.sh</automated>
  </verify>
  <acceptance_criteria>
    - 3 스크립트 `bash -n` (구문 검사) exit 0
    - `grep -rc "co-movement-sync\|comovement\|rebuild_comovement\|theme_comovement\|cosurge" scripts/setup-limit-up-sync-iam.sh scripts/deploy-limit-up-sync.sh scripts/smoke-limit-up-sync.sh` 합계 == 0 (식별자 누락 복제 0)
    - `grep -c "workers/limit-up-sync/Dockerfile" scripts/deploy-limit-up-sync.sh` == 1
    - `grep -c "oauth-service-account-email" scripts/deploy-limit-up-sync.sh` ≥ 1 (OAuth, OIDC 금지)
    - `grep -c "gh-radar-limit-up-sync-sa" scripts/setup-limit-up-sync-iam.sh` ≥ 1
    - `grep -c "limit_up_events" scripts/smoke-limit-up-sync.sh` ≥ 1 (INV-4 교체)
    - `grep -c "gh-radar-supabase-service-role" scripts/setup-limit-up-sync-iam.sh` == 1 + 외부 API 키 secret 바인딩 0
  </acceptance_criteria>
  <done>3 스크립트가 co-movement-sync 복제로 식별자 전량 교체(잔존 0), bash 구문 통과, OAuth/외부키0/limit_up_events count 확인.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] GCP 배포 (IAM → deploy → smoke)</name>
  <files>scripts/setup-limit-up-sync-iam.sh, scripts/deploy-limit-up-sync.sh, scripts/smoke-limit-up-sync.sh</files>
  <action>오케스트레이터가 사용자 GCP 인증(Deployer SA, GCP_PROJECT_ID=gh-radar) 후 실행: `bash scripts/setup-limit-up-sync-iam.sh` → `bash scripts/deploy-limit-up-sync.sh` → `bash scripts/smoke-limit-up-sync.sh`(INV-1~5). 상세는 how-to-verify 참조.</action>
  <what-built>
    setup/deploy/smoke 스크립트. GCP 리소스(SA + Cloud Run Job + Scheduler)는 git 미반영 — 스크립트 실행으로 생성. Plan 02 마이그레이션이 production 적용된 후에만 RPC 가 존재.
    **순차화(WARNING 4):** 본 plan 은 wave 4 — Plan 03 Task 3(server 재배포) 이후 진행. 두 [BLOCKING] 배포 게이트(server 재배포 / GCP 워커 배포)가 동일 wave 에서 동시 실행되지 않도록 server 라우트 prod curl 검증이 끝난 뒤 워커를 배포한다.
  </what-built>
  <how-to-verify>
    오케스트레이터가 사용자 GCP 인증(Deployer SA, GOOGLE_APPLICATION_CREDENTIALS + CLOUDSDK_CORE_PROJECT=gh-radar) 후 실행:
    1. `export GCP_PROJECT_ID=gh-radar` + `source` deploy env(SUPABASE_URL).
    2. `bash scripts/setup-limit-up-sync-iam.sh` — SA 생성 + supabase-service-role accessor 바인딩 (exit 0).
    3. `bash scripts/deploy-limit-up-sync.sh` — 이미지 빌드/push + Cloud Run Job + Scheduler 배포 (exit 0).
    4. `bash scripts/smoke-limit-up-sync.sh` — INV-1(Job execute exit 0) + INV-2(limit-up-sync complete 로그) + INV-3(failed 0) + INV-4(limit_up_events + limit_up_stock_stats count>0) + INV-5(Scheduler ENABLED cron '0 2 * * 2-6').
  </how-to-verify>
  <read_first>
    - scripts/setup-limit-up-sync-iam.sh + deploy-limit-up-sync.sh + smoke-limit-up-sync.sh (Task 1 산출)
    - 메모리 reference_gh_radar_deployer_sa (GCP 영구 인증)
    - 메모리 feedback_parallel_wave_worktree (병렬 wave worktree — 단 본 plan 은 배포라 순차)
    - tasks/lessons.md (정량 주장 실측)
  </read_first>
  <acceptance_criteria>
    - setup-limit-up-sync-iam.sh exit 0 + SA gh-radar-limit-up-sync-sa describe 성공
    - deploy-limit-up-sync.sh exit 0 + Cloud Run Job gh-radar-limit-up-sync + Scheduler gh-radar-limit-up-sync-nightly 존재
    - smoke INV-1~5 전부 PASS (limit_up_events count>0, Scheduler ENABLED cron '0 2 * * 2-6')
  </acceptance_criteria>
  <resume-signal>setup/deploy exit 0 + smoke INV-1~5 PASS (limit_up_events>0, Scheduler ENABLED) 후 "approved" 또는 이슈 보고</resume-signal>
  <verify>setup/deploy exit 0 + smoke INV-1~5 PASS (Job exit 0, limit_up_events count>0, Scheduler ENABLED cron 0 2 * * 2-6)</verify>
  <done>Cloud Run Job gh-radar-limit-up-sync + Scheduler gh-radar-limit-up-sync-nightly 배포, 워커가 rebuild_limit_up 호출해 사전계산 테이블 채움(smoke count>0)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud Scheduler → Cloud Run Job | OAuth invoker (gh-radar-scheduler-sa) — 리소스 단위 바인딩 |
| Cloud Run Job → Supabase | service_role secret 으로 rebuild_limit_up RPC 호출 (외부 입력 없음) |
| Secret Manager → Job | supabase-service-role 만 accessor (외부 API 키 0) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Elevation of Privilege | Scheduler → Job 인증 | mitigate | --oauth-service-account-email (OIDC 금지) + 리소스 단위 run.invoker 바인딩 (프로젝트 단위 금지) — co-movement-sync 패턴 |
| T-12-04-02 | Information Disclosure | service_role 키 | mitigate | Secret Manager → Cloud Run secret 주입, 이미지/코드 하드코딩 0. 최소권한 SA(supabase-service-role accessor 1개, 외부 키 미바인딩) |
| T-12-04-03 | Tampering | 배포 대상 프로젝트 | mitigate | deploy 스크립트 gcloud config 가드 (active config/project == gh-radar 불일치 시 exit 1) |
</threat_model>

<verification>
- Task 1: bash -n 구문 + 식별자 교체 grep 검증 (co-movement-sync 잔존 0)
- Task 2 [BLOCKING]: setup/deploy exit 0 + smoke INV-1~5 PASS
</verification>

<success_criteria>
- limit-up-sync Cloud Run Job + Scheduler(0 2 * * 2-6 KST, OAuth) 가 GCP 에 ENABLED
- 워커가 rebuild_limit_up RPC 호출 → limit_up_events/stock_stats/theme_stats 채움 (smoke count>0)
- 외부 API 키 0 (supabase-service-role accessor 1개만) — 최소권한
- 스케줄 = candle-sync EOD 이후 야간 (전 영업일 OHLCV 확정 후 재집계, D-20)
</success_criteria>

<output>
After completion, create `.planning/phases/12-a-n-master-sync/12-04-SUMMARY.md`
</output>
