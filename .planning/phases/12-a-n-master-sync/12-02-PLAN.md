---
phase: 12-a-n-master-sync
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - supabase/migrations/20260628120000_limit_up_tables.sql
  - .planning/phases/12-a-n-master-sync/fixtures/limit_up_golden.sql
autonomous: false
requirements: [LIMIT-01]
nyquist_compliant: true

must_haves:
  truths:
    - "limit_up_events / limit_up_stock_stats / limit_up_theme_stats 3 테이블이 production Supabase 에 존재한다"
    - "rebuild_limit_up() RPC 가 호가단위 백테스트로 마감상한가 이벤트를 도출해 3 테이블에 TRUNCATE+INSERT 한다"
    - "RPC 실행 후 황금 케이스 종목(000390 4회·000440 4회/점상1)이 limit_up_events 에 정확히 재현된다"
  artifacts:
    - path: "supabase/migrations/20260628120000_limit_up_tables.sql"
      provides: "3 사전계산 테이블 + limit_up_price() IMMUTABLE 함수 + rebuild_limit_up() RPC + RLS + REVOKE/GRANT"
      contains: "rebuild_limit_up"
    - path: ".planning/phases/12-a-n-master-sync/fixtures/limit_up_golden.sql"
      provides: "황금 케이스 검증 쿼리 (이벤트 카운트·점상·다음날 수익률 대조)"
      contains: "000440"
  key_links:
    - from: "rebuild_limit_up()"
      to: "stock_daily_ohlcv"
      via: "LAG(close)/LEAD(open) 윈도우 + close = limit_up_price(prev_close) 필터"
      pattern: "limit_up_price"
    - from: "limit_up_theme_stats"
      to: "theme_stocks (is_system, effective_to IS NULL)"
      via: "active 멤버 풀링 집계"
      pattern: "effective_to IS NULL"
---

<objective>
Phase 12 (LIMIT-01) 사전계산 마이그레이션 — `limit_up_*` 3 테이블 + 호가단위 `limit_up_price()` 함수 + `rebuild_limit_up()` plpgsql RPC 를 Phase 11 `comovement_tables.sql` 패턴으로 작성하고, [BLOCKING] production push 후 황금 케이스로 정확성을 검증한다.

Purpose: 본 phase 에서 **새로 작성하는 코드의 한 축인 RPC 본문(백테스트 SQL)** 을 구현한다. RESEARCH §1 검증된 호가단위 CASE + §2 백테스트 CTE 를 그대로 사용해 마감상한가 이벤트·다음날 수익률·테마 풀링을 in-DB 단일 패스로 집계한다.
Output: 마이그레이션 1개(3 테이블 + 2 함수 + RLS + REVOKE/GRANT), 황금 케이스 fixture, production 적용.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-a-n-master-sync/12-CONTEXT.md
@.planning/phases/12-a-n-master-sync/12-RESEARCH.md
@.planning/phases/12-a-n-master-sync/12-VALIDATION.md

<interfaces>
<!-- 복제 원본 supabase/migrations/20260611120000_comovement_tables.sql 을 read_first 로 읽어
     BEGIN/COMMIT · RLS TO anon,authenticated · REVOKE 3줄 · SECURITY DEFINER search_path · TRUNCATE+INSERT 골격을 그대로 미러. -->

호가단위 함수 (RESEARCH §1, IMMUTABLE 권장):
```sql
CREATE FUNCTION public.limit_up_price(prev_close numeric) RETURNS numeric
LANGUAGE sql IMMUTABLE AS $$
  SELECT CASE
    WHEN prev_close * 1.3 < 2000   THEN floor(prev_close*1.3 / 1)    * 1
    WHEN prev_close * 1.3 < 5000   THEN floor(prev_close*1.3 / 5)    * 5
    WHEN prev_close * 1.3 < 20000  THEN floor(prev_close*1.3 / 10)   * 10
    WHEN prev_close * 1.3 < 50000  THEN floor(prev_close*1.3 / 50)   * 50
    WHEN prev_close * 1.3 < 200000 THEN floor(prev_close*1.3 / 100)  * 100
    WHEN prev_close * 1.3 < 500000 THEN floor(prev_close*1.3 / 500)  * 500
    ELSE                                floor(prev_close*1.3 / 1000) * 1000
  END;
$$;
```

백테스트 CTE (RESEARCH §2, 실측 검증):
```sql
WITH ordered AS (
  SELECT o.code, o.date, o.open, o.high, o.low, o.close, o.volume, o.trade_amount, o.change_rate,
    LAG(o.close)  OVER (PARTITION BY o.code ORDER BY o.date) AS prev_close,
    LEAD(o.open)  OVER (PARTITION BY o.code ORDER BY o.date) AS next_open,
    LEAD(o.high)  OVER (PARTITION BY o.code ORDER BY o.date) AS next_high,
    LEAD(o.low)   OVER (PARTITION BY o.code ORDER BY o.date) AS next_low,
    LEAD(o.close) OVER (PARTITION BY o.code ORDER BY o.date) AS next_close
  FROM stock_daily_ohlcv o WHERE o.date >= v_since
),
events AS (
  SELECT * FROM ordered
  WHERE prev_close IS NOT NULL
    AND change_rate <= 31                          -- 신규상장/증자 아티팩트 배제
    AND close = limit_up_price(prev_close)         -- 마감상한가 (정수 정확 비교)
)
-- 점상 = open=high=low=close. next_open IS NOT NULL 인 이벤트만 통계.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 마이그레이션 — 3 테이블 + limit_up_price() + rebuild_limit_up() RPC</name>
  <files>supabase/migrations/20260628120000_limit_up_tables.sql</files>
  <read_first>
    - supabase/migrations/20260611120000_comovement_tables.sql (BEGIN/COMMIT·RLS·REVOKE 3줄·SECURITY DEFINER search_path·TRUNCATE+INSERT·GET DIAGNOSTICS·jsonb_build_object 골격 원본 — **이 골격을 그대로 미러**)
    - supabase/migrations/20260512120000_create_stock_daily_ohlcv.sql (백테스트 source 스키마: PK(code,date), close numeric(20,2), volume/trade_amount bigint, change_rate nullable)
    - supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql (stocks.listing_shares bigint — 회전율 분모)
    - supabase/migrations/20260609120000_theme_tables.sql (theme_stocks PK(theme_id,stock_code), is_system, effective_to IS NULL=active, idx_theme_stocks_active)
    - supabase/migrations/20260610130000_theme_admin_overrides.sql (themes.hidden — 테마 풀링 시 hidden=false 필터)
    - .planning/phases/12-a-n-master-sync/12-RESEARCH.md §1 호가단위 / §2 백테스트 CTE / §3 회전율 / §4 테마 풀링 / "Code Examples" RPC 골격 / "Common Pitfalls" 1~7
    - .planning/phases/12-a-n-master-sync/12-CONTEXT.md D-01~D-18 (이벤트 판별·점상·회전율·테마 풀링 결정값)
  </read_first>
  <action>
    `supabase/migrations/20260628120000_limit_up_tables.sql` 신규 작성. `comovement_tables.sql` 골격 미러 + 본 phase 내용:

    BEGIN; ... COMMIT; 래핑.

    **1) `limit_up_price(prev_close numeric) RETURNS numeric LANGUAGE sql IMMUTABLE`** — interfaces 의 7-tier CASE (target=prev_close×1.3 기준 floor 절사). SECURITY DEFINER 아님(순수 산술, 권한 불요). REVOKE 불필요(읽기 전용 산술).

    **2) 3 테이블 (snake_case):**
    - `limit_up_events` — PK(code, date). 컬럼: code text REFERENCES stocks(code) ON DELETE CASCADE, date date, is_jeomsang boolean, next_open_ret numeric(8,2), next_high_ret numeric(8,2), next_low_ret numeric(8,2), next_close_ret numeric(8,2), trade_amount bigint, turnover numeric(8,4) (NULL 허용 — listing_shares 미보유), computed_at timestamptz DEFAULT now(). 인덱스 `idx_limit_up_events_code ON limit_up_events(code)`.
    - `limit_up_stock_stats` — PK(code). 컬럼: code text REFERENCES stocks(code) ON DELETE CASCADE, total_events int, resolved_events int (next_open 있는 이벤트), win_count int (next_open_ret > 0), win_rate numeric(5,4) (NULL — 적재는 항상, N≥3 게이팅은 읽기 시), avg_open_ret numeric(8,2), worst_low_ret numeric(8,2), recent_wins int, recent_losses int (최근 3회 D-10), bucket_n10_n5 int, bucket_n5_0 int, bucket_0_p5 int, bucket_p5_p10 int, bucket_p10 int (히스토그램 5버킷 D-11), computed_at timestamptz DEFAULT now().
    - `limit_up_theme_stats` — PK(theme_id). 컬럼: theme_id uuid REFERENCES themes(id) ON DELETE CASCADE, sample_n int (멤버 풀 누적 마감상한가 이벤트 수 D-17), win_count int, win_rate numeric(5,4), avg_open_ret numeric(8,2), computed_at timestamptz DEFAULT now(). 인덱스 불필요(theme_id PK).

    **3) RLS — 공개 read (TO anon, authenticated 둘 다, Pitfall 7):**
    각 3 테이블 ENABLE ROW LEVEL SECURITY + `CREATE POLICY "read_limit_up_events" ON limit_up_events FOR SELECT TO anon, authenticated USING (true);` (3 테이블 각각).

    **4) `rebuild_limit_up(p_lookback_months int DEFAULT 24) RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path = public, pg_temp`:**
    - v_since := (current_date - (p_lookback_months||' months')::interval)::date.
    - STEP A — `TRUNCATE limit_up_events; INSERT INTO limit_up_events (...)` RESEARCH §2 CTE: ordered(LAG/LEAD 윈도우) → events(prev_close NOT NULL AND change_rate <= 31 AND close = limit_up_price(prev_close)). SELECT 컬럼: is_jeomsang = (open=high AND high=low AND low=close), next_open_ret = (next_open-close)/close*100, next_high/low/close_ret 동일식, trade_amount, turnover = `CASE WHEN s.listing_shares IS NULL OR s.listing_shares=0 THEN NULL ELSE volume::numeric/s.listing_shares END` (stocks JOIN). **WHERE next_open IS NOT NULL** (가장 최근 이벤트 다음날 부재 제외, Pitfall 4). float8 캐스팅 금지(Pitfall 3 — ::numeric 유지). GET DIAGNOSTICS v_event_rows.
    - STEP B — `TRUNCATE limit_up_stock_stats; INSERT ...` limit_up_events GROUP BY code: total_events=count(*), resolved_events=count(*) (events 가 이미 next_open NOT NULL), win_count=count(*) FILTER(next_open_ret>0), win_rate=win_count::numeric/NULLIF(count(*),0), avg_open_ret=avg(next_open_ret), worst_low_ret=min(next_low_ret). 히스토그램 버킷 = count FILTER(next_open_ret < -5), FILTER(>=-5 AND <0), FILTER(>=0 AND <5), FILTER(>=5 AND <10), FILTER(>=10). recent_wins/recent_losses = 최근 3 이벤트(date DESC LIMIT 3) 의 next_open_ret>0 / <=0 카운트 (lateral 또는 윈도우 row_number<=3 서브쿼리). GET DIAGNOSTICS v_stat_rows.
    - STEP C — `TRUNCATE limit_up_theme_stats; INSERT ...` 테마 풀링 (§4, D-15~D-17): active 시스템 테마 멤버(theme_stocks JOIN themes ON is_system=true AND hidden=false WHERE effective_to IS NULL) 의 stock_code 들이 limit_up_events 에 매칭되는 이벤트 풀 → theme_id GROUP BY: sample_n=count(*), win_count=count FILTER(next_open_ret>0), win_rate=win_count/NULLIF(sample_n,0), avg_open_ret=avg(next_open_ret). HAVING count(*) >= 1 (모든 풀 적재; 노출 정렬은 읽기 시 N desc). GET DIAGNOSTICS v_theme_rows.
    - RETURN jsonb_build_object('lookback_since', v_since, 'event_rows', v_event_rows, 'stock_stat_rows', v_stat_rows, 'theme_stat_rows', v_theme_rows, 'rebuilt_at', now()).

    **5) REVOKE/GRANT (rebuild RPC 만, Pitfall 7):**
    `REVOKE EXECUTE ON FUNCTION public.rebuild_limit_up(int) FROM PUBLIC;`
    `REVOKE EXECUTE ON FUNCTION public.rebuild_limit_up(int) FROM anon, authenticated;`
    `GRANT  EXECUTE ON FUNCTION public.rebuild_limit_up(int) TO service_role;`
    (limit_up_price 는 REVOKE 없음 — 순수 산술.)
  </action>
  <verify>
    <automated>supabase db lint --schema public 2>&1 | grep -qiv "error" && test "$(grep -cE 'rebuild_limit_up|limit_up_price|read_limit_up|REVOKE EXECUTE ON FUNCTION public.rebuild_limit_up' supabase/migrations/20260628120000_limit_up_tables.sql)" -ge 9</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db lint --schema public` 이 마이그레이션 SQL 구문 오류 0 (error 미출력) — grep 카운트만으로는 SQL 유효성을 보장하지 못하므로 구문 파싱 게이트 필수
    - `grep -cE "rebuild_limit_up|limit_up_price|read_limit_up|REVOKE EXECUTE ON FUNCTION public.rebuild_limit_up" supabase/migrations/20260628120000_limit_up_tables.sql` ≥ 9 (핵심 식별자 실재 — 실질 체크 임계)
    - `grep -c "REVOKE EXECUTE ON FUNCTION public.rebuild_limit_up" supabase/migrations/20260628120000_limit_up_tables.sql` ≥ 2 (FROM PUBLIC + FROM anon, authenticated)
    - `grep -c "GRANT  EXECUTE ON FUNCTION public.rebuild_limit_up(int) TO service_role" ...` == 1
    - `grep -c "SET search_path = public, pg_temp" ...` ≥ 1
    - `grep -c "TO anon, authenticated USING (true)" ...` == 3 (3 테이블 read 정책)
    - `grep -c "close = limit_up_price(prev_close)" ...` ≥ 1 (가격 매칭 판별 — 비율 임계 아님, D-01)
    - `grep -c "change_rate <= 31" ...` ≥ 1 (아티팩트 게이트)
    - `grep -c "effective_to IS NULL" ...` ≥ 1 (테마 active 멤버)
    - `grep -c "next_open IS NOT NULL" ...` ≥ 1 (Pitfall 4)
    - `grep -c "TRUNCATE limit_up" ...` == 3 (3 테이블 full-rebuild)
  </acceptance_criteria>
  <done>마이그레이션이 3 테이블 + limit_up_price() + rebuild_limit_up()(3 STEP TRUNCATE+INSERT) + RLS 3 정책 + REVOKE/GRANT 를 포함, grep 검증 통과.</done>
</task>

<task type="auto">
  <name>Task 2: 황금 케이스 fixture SQL</name>
  <files>.planning/phases/12-a-n-master-sync/fixtures/limit_up_golden.sql</files>
  <read_first>
    - .planning/phases/12-a-n-master-sync/12-RESEARCH.md §Validation Architecture (000390 4회 / 000440 4회·점상1) + §1 황금 케이스 가격(95500→124100 등)
    - .planning/phases/12-a-n-master-sync/12-VALIDATION.md "Wave 0 Requirements" (limit_up_golden.sql 명세)
  </read_first>
  <action>
    `.planning/phases/12-a-n-master-sync/fixtures/limit_up_golden.sql` 신규 작성 — RPC rebuild 후 정확성 대조 쿼리 모음 (psql/Supabase SQL editor 실행):
    1. **호가단위 함수 검증:** `SELECT limit_up_price(95500)=124100 AS c1, limit_up_price(297000)=386000 AS c2, limit_up_price(386000)=501000 AS c3, limit_up_price(876000)=1138000 AS c4, limit_up_price(60000)=78000 AS c5;` — 전부 true 기대.
    2. **이벤트 카운트 대조:** `SELECT code, count(*) AS events, count(*) FILTER (WHERE is_jeomsang) AS jeom FROM limit_up_events WHERE code IN ('000390','000440') GROUP BY code ORDER BY code;` — 기대: 000390 events≥4, 000440 events≥4 · jeom≥1 (RESEARCH 실측. lookback 시점에 따라 ±1 — 주석에 "실측 시점 24m 기준, 재실행일 변동 가능" 명시).
    3. **다음날 수익률 sanity:** `SELECT code, date, is_jeomsang, next_open_ret, next_high_ret, next_low_ret, next_close_ret FROM limit_up_events WHERE code='000440' ORDER BY date DESC;` — next_high_ret >= next_open_ret AND next_low_ret <= next_close_ret 류 부등식이 모든 행에서 성립(고가 ≥ 시·종, 저가 ≤ 시·종) 주석 명시.
    4. **stats 일관성:** `SELECT s.code, s.total_events, s.win_rate, (SELECT count(*) FROM limit_up_events e WHERE e.code=s.code) AS event_table_count FROM limit_up_stock_stats s WHERE s.code IN ('000390','000440');` — total_events == event_table_count.
    헤더 주석에 기대값·실행법(`psql "$SUPABASE_DB_URL" -f .../limit_up_golden.sql` 또는 Supabase SQL editor) 기록.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-a-n-master-sync/fixtures/limit_up_golden.sql && grep -c "limit_up_price\|000440\|limit_up_events" .planning/phases/12-a-n-master-sync/fixtures/limit_up_golden.sql</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재 + `grep -c "limit_up_price(95500)=124100" fixtures/limit_up_golden.sql` ≥ 1
    - `grep -c "000390\|000440" fixtures/limit_up_golden.sql` ≥ 2
    - 4개 검증 블록(호가단위/이벤트 카운트/수익률 sanity/stats 일관성) 모두 존재
  </acceptance_criteria>
  <done>황금 케이스 fixture 가 호가단위·이벤트 카운트·수익률·stats 일관성 4 블록으로 작성됨.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] supabase db push + rebuild_limit_up 실행 + 황금 케이스 검증</name>
  <files>supabase/migrations/20260628120000_limit_up_tables.sql, .planning/phases/12-a-n-master-sync/fixtures/limit_up_golden.sql</files>
  <action>오케스트레이터가 사용자 승인 후 실행: `supabase db push`(SUPABASE_ACCESS_TOKEN env, non-TTY) → `select rebuild_limit_up(24)` → 황금 케이스 fixture 대조 → anon RLS/REVOKE 라이브 확인. 상세 절차는 아래 how-to-verify 참조.</action>
  <what-built>
    마이그레이션 `20260628120000_limit_up_tables.sql`(3 테이블 + 2 함수 + RLS + REVOKE/GRANT)과 황금 케이스 fixture. 빌드/타입은 통과하나 **라이브 DB 에 RPC/테이블이 없으면 false-positive 검증 상태**가 되므로 production push 가 필수.
  </what-built>
  <how-to-verify>
    오케스트레이터가 사용자 승인 후 실행 (env: `SUPABASE_ACCESS_TOKEN` 설정, non-TTY):
    1. `supabase db push` — 마이그레이션 적용 (exit 0). 재실행 시 "Remote database is up to date".
    2. RPC 실행 (service_role REST 또는 SQL editor): `select rebuild_limit_up(24);` → jsonb `{event_rows, stock_stat_rows, theme_stat_rows, ...}` 반환, event_rows > 0.
    3. 황금 케이스: `psql "$SUPABASE_DB_URL" -f .planning/phases/12-a-n-master-sync/fixtures/limit_up_golden.sql` (또는 SQL editor 붙여넣기) → 호가단위 5 케이스 전부 true + 000390/000440 이벤트 카운트 기대치(≥4, 000440 점상≥1) + stats 일관성 통과.
    4. RLS/권한: anon REST `GET /rest/v1/limit_up_events?code=eq.000440` 200 (default-deny 아님), anon `POST /rest/v1/rpc/rebuild_limit_up` 403/4xx (REVOKE 동작 확인).
  </how-to-verify>
  <read_first>
    - tasks/lessons.md (db push non-TTY, 정량 주장 실측)
    - .planning/phases/12-a-n-master-sync/fixtures/limit_up_golden.sql (검증 쿼리)
  </read_first>
  <acceptance_criteria>
    - `supabase db push` exit 0 + 재실행 "up to date"
    - `select rebuild_limit_up(24)` 가 event_rows > 0 반환
    - 황금 케이스 호가단위 5 케이스 전부 true, 000390/000440 이벤트 카운트 ≥ 기대치
    - anon REST GET limit_up_events 200, anon RPC rebuild_limit_up 4xx(REVOKE)
  </acceptance_criteria>
  <resume-signal>db push exit 0 + rebuild_limit_up event_rows>0 + 황금 케이스 통과 + RLS/REVOKE 확인 후 "approved" 또는 이슈 보고</resume-signal>
  <verify>db push exit 0 + `select rebuild_limit_up(24)` event_rows>0 + 황금 케이스 fixture 전 블록 통과 + anon RPC 4xx</verify>
  <done>마이그레이션 production 적용, rebuild_limit_up 이 event_rows>0 반환, 황금 케이스(000390/000440) 재현, RLS read 200 + RPC REVOKE 4xx 라이브 확인</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/authenticated → limit_up_* 테이블 | 공개 read (시장 통계, PII 없음) — RLS SELECT TO anon, authenticated |
| anon/authenticated → rebuild_limit_up RPC | 차단 대상 — 재계산은 워커(service_role) 전용 |
| SECURITY DEFINER 함수 → 스키마 | search_path 격리로 하이재킹 방지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Elevation of Privilege | rebuild_limit_up() | mitigate | REVOKE EXECUTE FROM PUBLIC + anon, authenticated; GRANT service_role only (3줄) — anon 실행 차단 |
| T-12-02-02 | Elevation of Privilege | rebuild_limit_up() SECURITY DEFINER | mitigate | SET search_path = public, pg_temp — 검색경로 하이재킹 방지 |
| T-12-02-03 | DoS (self) | limit_up_* read | mitigate | RLS SELECT TO anon, authenticated 둘 다 — 로그인 사용자 default-deny 빈 응답 회피 |
| T-12-02-04 | Tampering | limit_up_events 데이터 | mitigate | 워커만 service_role 로 RPC 호출 → INSERT, 사용자 쓰기 경로 없음(read-only RLS) |
</threat_model>

<verification>
- Task 1/2 grep 검증 통과 (마이그레이션 + fixture 정적 검증)
- Task 3 [BLOCKING] 후: rebuild_limit_up event_rows > 0, 황금 케이스 통과, RLS/REVOKE 라이브 확인
</verification>

<success_criteria>
- 3 사전계산 테이블 + limit_up_price() + rebuild_limit_up() RPC 가 production 에 존재
- RPC 가 호가단위 가격 매칭(close=limit_up_price)으로 마감상한가 이벤트를 도출 (비율 임계 아님, D-01)
- 황금 케이스 종목이 정확 재현 (000390/000440 이벤트 카운트·점상·수익률 sanity)
- RPC REVOKE anon/authenticated + GRANT service_role, read RLS TO anon+authenticated 둘 다 라이브 확인
</success_criteria>

<output>
After completion, create `.planning/phases/12-a-n-master-sync/12-02-SUMMARY.md`
</output>
