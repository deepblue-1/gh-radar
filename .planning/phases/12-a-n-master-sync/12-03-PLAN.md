---
phase: 12-a-n-master-sync
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - server/src/schemas/limitUp.ts
  - server/src/mappers/limitUp.ts
  - server/src/routes/limitUp.ts
  - server/src/routes/stocks.ts
  - server/src/routes/__tests__/limitUp.test.ts
autonomous: false
requirements: [LIMIT-01]
nyquist_compliant: true

must_haves:
  truths:
    - "GET /api/stocks/:code/limit-up 가 { hero, events, themes } 객체를 반환한다 (배열 아님)"
    - "라우트가 :code 를 regex 검증하고 limit_up_* 테이블만 읽는다 (on-demand 재계산 트리거 없음)"
    - "server 가 production 에 재배포되어 prod curl 이 알려진 상한가 종목에서 200 + 객체 형태를 반환한다"
  artifacts:
    - path: "server/src/routes/limitUp.ts"
      provides: "GET /api/stocks/:code/limit-up 읽기 라우트 (limit_up_stock_stats + limit_up_events + limit_up_theme_stats 조회)"
      exports: ["limitUpRouter"]
    - path: "server/src/schemas/limitUp.ts"
      provides: ":code zod regex 검증"
      contains: "LimitUpParams"
    - path: "server/src/mappers/limitUp.ts"
      provides: "snake_case DB row → camelCase LimitUpResponse 매핑 + numeric(text) 정규화"
      contains: "toNum"
  key_links:
    - from: "server/src/routes/stocks.ts"
      to: "limitUpRouter"
      via: "stocksRouter.use('/:code/limit-up', limitUpRouter) — /:code 핸들러보다 먼저 등록"
      pattern: "/:code/limit-up"
    - from: "server/src/routes/limitUp.ts"
      to: "limit_up_stock_stats / limit_up_events / limit_up_theme_stats"
      via: "supabase.from(...).select"
      pattern: "limit_up_"
---

<objective>
Phase 12 (LIMIT-01) server 읽기 경로 — `GET /api/stocks/:code/limit-up` 라우트를 Phase 11 `comovement.ts` 패턴으로 작성하되 **시세 실시간 조인을 제거**(정적 이력)하고, `{ hero, events, themes }` 객체 계약으로 반환한다. server production 재배포 + prod curl 검증.

Purpose: 사전계산된 `limit_up_*` 테이블을 종목상세에 노출할 읽기 API. on-demand 재계산 금지(D-22) — 워커가 채운 테이블만 SELECT. 새 라우트는 server 재배포까지 해야 prod 404 를 피한다(Pitfall 6, lesson).
Output: schemas/mappers/routes/limitUp.ts + stocks.ts 등록 + 단위 테스트 + server 재배포 + prod curl 200.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-a-n-master-sync/12-CONTEXT.md
@.planning/phases/12-a-n-master-sync/12-RESEARCH.md
@.planning/phases/12-a-n-master-sync/12-VALIDATION.md

<interfaces>
<!-- Plan 01 산출 공유 타입 (packages/shared/src/limitUp.ts) -->
```typescript
interface LimitUpResponse { hero: LimitUpStockStats; events: LimitUpEvent[]; themes: LimitUpThemeStat[]; }
interface LimitUpEvent { date; isJeomsang; nextOpenRet; nextHighRet; nextLowRet; nextCloseRet; tradeAmount; turnover; }
interface LimitUpStockStats { totalEvents; resolvedEvents; winCount; winRate; avgOpenRet; worstLowRet; recentWins; recentLosses; histogram; }
interface LimitUpThemeStat { themeId; themeName; sampleN; winRate; avgOpenRet; }
```

<!-- 복제 원본 server/src/routes/comovement.ts — mergeParams, 객체 satisfies 반환, ApiError 분기.
     단, comovement 의 stock_quotes/co-surge/computeComovement 는 본 라우트에 불필요(정적 이력, 시세 조인 제거 — RESEARCH Anti-Pattern). -->

복제 원본 server/src/schemas/comovement.ts:
```typescript
export const CoMovementParams = z.object({ code: z.string().regex(/^[A-Za-z0-9]{1,10}$/) });
```

복제 원본 server/src/mappers/comovement.ts:
```typescript
export function toNum(v: string|number|null|undefined, fallback=0): number { ... } // PostgREST numeric(text) 정규화
```

복제 원본 server/src/routes/stocks.ts 등록부:
```typescript
stocksRouter.use("/:code/co-movement", comovementRouter); // /:code 핸들러보다 먼저
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: schemas + mappers/limitUp.ts (zod 검증 + DB row 타입 + numeric 정규화)</name>
  <files>server/src/schemas/limitUp.ts, server/src/mappers/limitUp.ts</files>
  <read_first>
    - server/src/schemas/comovement.ts (CoMovementParams regex 복제 원본)
    - server/src/mappers/comovement.ts (toNum numeric 정규화 + row 타입 톤 복제 원본)
    - packages/shared/src/limitUp.ts (Plan 01 — LimitUpResponse/LimitUpEvent/LimitUpStockStats/LimitUpThemeStat 계약)
    - supabase/migrations/20260628120000_limit_up_tables.sql (Plan 02 — 3 테이블 컬럼명 snake_case 확인)
  </read_first>
  <action>
    `server/src/schemas/limitUp.ts`: `export const LimitUpParams = z.object({ code: z.string().regex(/^[A-Za-z0-9]{1,10}$/) });` (comovement 복제) + `export type LimitUpParamsT = z.infer<...>`.
    `server/src/mappers/limitUp.ts`: comovement 의 `toNum` 복제 + 3 row 타입 정의:
    - `LimitUpEventRow` = { code; date; is_jeomsang; next_open_ret; next_high_ret; next_low_ret; next_close_ret; trade_amount; turnover } (numeric → string|number|null).
    - `LimitUpStockStatsRow` = { code; total_events; resolved_events; win_count; win_rate; avg_open_ret; worst_low_ret; recent_wins; recent_losses; bucket_n10_n5; bucket_n5_0; bucket_0_p5; bucket_p5_p10; bucket_p10 }.
    - `LimitUpThemeStatRow` = { theme_id; sample_n; win_count; win_rate; avg_open_ret }.
    매핑 헬퍼 3종 (row → camelCase 계약 타입): `mapEvent(row): LimitUpEvent` (turnover NULL 보존 → null, 나머지 toNum), `mapStats(row): LimitUpStockStats` (histogram = [bucket_n10_n5, bucket_n5_0, bucket_0_p5, bucket_p5_p10, bucket_p10].map(toNum), win_rate NULL 보존 → null — N≥3 게이팅은 마이그레이션이 항상 적재하므로 여기선 그대로 통과, webapp 이 resolvedEvents<3 시 큰 % 숨김), `mapTheme(row, name): LimitUpThemeStat`.
  </action>
  <verify>
    <automated>pnpm -F @gh-radar/server typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LimitUpParams" server/src/schemas/limitUp.ts` ≥ 1 + regex `/^[A-Za-z0-9]{1,10}$/` 포함
    - `grep -c "mapEvent\|mapStats\|mapTheme\|toNum" server/src/mappers/limitUp.ts` ≥ 4
    - `grep -c "turnover" server/src/mappers/limitUp.ts` ≥ 1 (NULL 보존 매핑)
    - `pnpm -F @gh-radar/server typecheck` exit 0
  </acceptance_criteria>
  <done>schemas/limitUp.ts zod regex + mappers/limitUp.ts 3 row 타입 + 매핑 헬퍼, typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: routes/limitUp.ts + stocks.ts 등록 + 단위 테스트</name>
  <files>server/src/routes/limitUp.ts, server/src/routes/stocks.ts, server/src/routes/__tests__/limitUp.test.ts</files>
  <read_first>
    - server/src/routes/comovement.ts (mergeParams, ApiError safeParse 분기, 객체 satisfies 반환, Cache-Control no-store 복제 원본 — 단 stock_quotes/co-surge/computeComovement/fetchChunked 제거)
    - server/src/routes/stocks.ts (등록 순서 — /:code 핸들러 앞 + import 추가 위치)
    - server/src/lib/computeComovement.test.ts (server 단위 테스트 위치 src/**/*.test.ts 패턴 — vitest include)
    - server/vitest.config.ts (include: tests/**/*.test.ts + src/**/*.test.ts, setupFiles tests/setup.ts)
    - server/src/app.ts (app.locals.supabase 주입 — req.app.locals.supabase 접근)
    - packages/shared/src/limitUp.ts (LimitUpResponse 반환 계약)
  </read_first>
  <behavior>
    - 유효 :code + 데이터 있음 → 200 + { hero, events, themes } 객체 (배열 아님). events 는 date DESC.
    - 이벤트 0회 종목 → 200 + hero(totalEvents:0 등 zero stats) + events:[] + themes:[] (빈 상태는 webapp 처리)
    - 잘못된 :code (특수문자/길이초과) → 400 INVALID_QUERY_PARAM
    - Supabase 에러 → next(e) 전파 (error.message webapp 미노출은 섹션 책임)
  </behavior>
  <action>
    `server/src/routes/limitUp.ts`: `Router({ mergeParams: true })` (comovement 미러). GET "/" 핸들러:
    1. `LimitUpParams.safeParse(req.params)` 실패 → ApiError(400, "INVALID_QUERY_PARAM", ...).
    2. supabase = req.app.locals.supabase.
    3. `limit_up_stock_stats` WHERE code=:code maybeSingle → hero (없으면 zero stats 객체: totalEvents 0 등 — 빈 상태).
    4. `limit_up_events` WHERE code=:code ORDER BY date DESC (행 수 작아 페이지네이션 불필요 — RESEARCH "Don't Hand-Roll" 명시) → events.map(mapEvent).
    5. 테마: 앵커의 active 시스템 테마 = `theme_stocks` WHERE stock_code=:code AND effective_to IS NULL → theme_id 들 → `limit_up_theme_stats` IN(theme_id들) + `themes` IN(...) name 조인(hidden=false) → mapTheme, **sample_n DESC 정렬** (D-17). 다중 테마라도 행 수 작음(.in 단일 호출 충분).
    6. `res.setHeader("Cache-Control", "no-store"); res.json({ hero, events, themes } satisfies LimitUpResponse);`
    7. try/catch → next(e).
    **시세(stock_quotes) 조인·computeComovement·fetchChunked 전부 미사용** (정적 이력, RESEARCH Anti-Pattern "co-movement 의 stock_quotes 실시간 조인 복제 금지").
    `server/src/routes/stocks.ts`: import `{ limitUpRouter } from "./limitUp.js";` 추가 + `stocksRouter.use("/:code/limit-up", limitUpRouter);` 를 **`stocksRouter.get("/:code", ...)` 보다 먼저** (co-movement 등록 라인 인근, Pitfall 5).
    `server/src/routes/__tests__/limitUp.test.ts`: behavior 4 케이스 — supabase mock(maybeSingle/select/order/in 체이닝) + express app 또는 핸들러 직접 호출. 200 객체 형태(hero/events/themes 키) + events DESC + 400 regex 거부 검증.
  </action>
  <verify>
    <automated>pnpm -F @gh-radar/server test && pnpm -F @gh-radar/server typecheck && pnpm -F @gh-radar/server build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'stocksRouter.use("/:code/limit-up"' server/src/routes/stocks.ts` == 1
    - stocks.ts 에서 `/:code/limit-up` use 등록이 `stocksRouter.get("/:code"` 라인보다 앞 (행 번호 비교: `grep -n` 으로 use 라인 < get 라인)
    - `grep -c "satisfies LimitUpResponse" server/src/routes/limitUp.ts` ≥ 1 (객체 계약)
    - `grep -c "stock_quotes\|computeComovement\|fetchQuotesChunked" server/src/routes/limitUp.ts` == 0 (시세 조인 제거 확인)
    - `grep -c "sample_n\|order.*sample_n\|ascending: false" server/src/routes/limitUp.ts` ≥ 1 (테마 N desc)
    - `pnpm -F @gh-radar/server test` green (limitUp.test.ts 4 케이스)
  </acceptance_criteria>
  <done>limitUpRouter 가 객체 반환·:code 검증·정적 읽기(시세 조인 0), stocks.ts 에 /:code 앞 등록, 테스트·빌드 green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] server production 재배포 + prod curl 검증</name>
  <files>scripts/deploy-server.sh</files>
  <action>오케스트레이터가 사용자 GCP 인증(Deployer SA) 후 실행: `bash scripts/deploy-server.sh`(Cloud Run service 재배포) → prod curl /api/stocks/000440/limit-up(객체·events>0) / 005930(빈 상태) / !!!(400) + 호출 전후 limit_up_events count 불변(read-only). 상세는 how-to-verify 참조.</action>
  <what-built>
    `GET /api/stocks/:code/limit-up` 라우트 + stocks.ts 등록. **새 server 라우트는 재배포 전까지 prod 404** (Pitfall 6, Phase 10 lesson) — server 재배포 + prod curl 필수.
  </what-built>
  <how-to-verify>
    오케스트레이터가 사용자 GCP 인증(Deployer SA) 후 실행:
    1. `bash scripts/deploy-server.sh` — Cloud Run service 재배포 (새 revision).
    2. prod curl: `curl -s https://gh-radar-server-1023658565518.asia-northeast3.run.app/api/stocks/000440/limit-up` → 200 + JSON **객체** `{ "hero": {...}, "events": [...], "themes": [...] }` (배열 아님). events 비어있지 않음(000440 황금 케이스 종목).
    3. 이벤트 0회 종목: `curl .../api/stocks/005930/limit-up` → 200 + hero.totalEvents 0 류 + events:[] (대형주 정상 빈 상태).
    4. 잘못된 코드: `curl .../api/stocks/!!!/limit-up` → 400.
    5. on-demand 재계산 없음 확인: 라우트 호출이 limit_up_events 행 수를 변경하지 않음 (호출 전후 service_role count 동일).
  </how-to-verify>
  <read_first>
    - scripts/deploy-server.sh (Cloud Run service 재배포 스크립트)
    - tasks/lessons.md (새 라우트 server 재배포 + prod curl, --remove-secrets 누적 주의)
    - 메모리 reference_gh_radar_deployer_sa (GCP 인증)
  </read_first>
  <acceptance_criteria>
    - deploy-server.sh exit 0 + 새 revision 활성
    - `curl .../api/stocks/000440/limit-up` 200 + 응답이 `{` 로 시작하는 객체 + `hero`/`events`/`themes` 키 존재 + events.length > 0
    - `curl .../api/stocks/005930/limit-up` 200 + events 빈 배열 (빈 상태)
    - `curl .../api/stocks/!!!/limit-up` 400
    - 라우트 호출 전후 limit_up_events count 동일 (read-only, D-22)
  </acceptance_criteria>
  <resume-signal>server 재배포 exit 0 + prod curl 000440 200 객체(events>0) + 005930 빈 상태 + 400 거부 확인 후 "approved" 또는 이슈 보고</resume-signal>
  <verify>deploy-server.sh exit 0 + `curl .../api/stocks/000440/limit-up` 200 객체(events>0) + 005930 빈 + !!! 400 + 호출 전후 limit_up_events count 동일</verify>
  <done>server 재배포 완료, prod curl 이 객체 계약(hero/events/themes) 200 반환, read-only(재계산 0) 확인</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/stocks/:code/limit-up | 미인증 :code 입력이 PostgREST 바인딩 전 통과 |
| 라우트 → limit_up_* 테이블 | service_role(app.locals.supabase) 읽기 전용 — 쓰기/재계산 경로 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Tampering | :code 파라미터 | mitigate | LimitUpParams zod regex `/^[A-Za-z0-9]{1,10}$/` — PostgREST 바인딩 전 형식 차단 (SQL injection 은 supabase client 파라미터화로 2차 방어) |
| T-12-03-02 | Information Disclosure | Supabase 에러 | mitigate | next(e) 전파 → 글로벌 에러 핸들러 (PostgREST/RLS 내부정보 webapp 미노출은 Plan 05 섹션 quiet fallback) |
| T-12-03-03 | DoS | 응답 크기 | accept | 단일 종목 이벤트 행 수 작음(24m 마감상한가, 수~수십 행) — k 클램프 불요. 테마도 종목당 소수 |
| T-12-03-04 | Elevation (간접) | on-demand 재계산 | mitigate | 라우트는 SELECT 만 — rebuild_limit_up RPC 미호출 (D-22). 호출 전후 행수 불변으로 검증 |
</threat_model>

<verification>
- Task 1/2: typecheck + build + 단위 테스트 green, 등록 순서 grep 검증
- Task 3 [BLOCKING]: server 재배포 + prod curl 000440(객체·events>0)/005930(빈)/!!!(400) + read-only 확인
</verification>

<success_criteria>
- GET /api/stocks/:code/limit-up 가 { hero, events, themes } 객체 반환 (배열 아님 — 계약 드리프트 회피)
- 라우트가 :code regex 검증 + limit_up_* SELECT 만 (시세 조인 0, 재계산 0)
- /:code/limit-up 가 /:code 핸들러보다 먼저 등록 (shadowing 회피)
- server production 재배포 + prod curl 200 (Pitfall 6 회피)
</success_criteria>

<output>
After completion, create `.planning/phases/12-a-n-master-sync/12-03-SUMMARY.md`
</output>
