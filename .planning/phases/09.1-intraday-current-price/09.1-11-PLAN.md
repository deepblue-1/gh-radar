---
phase: 09.1
plan: 11
type: execute
wave: 4
depends_on: [01, 09, 10]
files_modified:
  - workers/ingestion
  - server/src/kis
  - packages/shared/src/kis.ts
  - packages/shared/src/index.ts
  - server/src/services/kis-runtime.ts
  - server/src/config.ts
  - scripts/deploy-ingestion.sh
  - scripts/setup-ingestion-iam.sh
  - scripts/smoke-ingestion.sh
  - scripts/deploy-server.sh
  - .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [DATA-02]
requirements_addressed:
  - DATA-02 (Wave 4 cutover #3 — KIS ingestion 완전 폐기 (RESEARCH §12 11-step) + kis_tokens DROP migration push + 코드/스크립트 git deletion + STATE/REQUIREMENTS/ROADMAP 갱신. SC #7 (KIS 폐기 완전화) 충족.)
must_haves:
  truths:
    - "RESEARCH §12 의 11-step 순서 준수 — Scheduler PAUSE → 데이터 정합 검증 → Job 삭제 → Scheduler 삭제 → SA 삭제 → Secret 삭제 → kis_tokens DROP migration push → 코드 deletion → git commit"
    - "Cloud Run Job gh-radar-ingestion + Scheduler gh-radar-ingestion-scheduler + SA gh-radar-ingestion-sa + Secrets gh-radar-kis-app-key/gh-radar-kis-app-secret 모두 GCP 에서 삭제"
    - "supabase/migrations/20260514120000_drop_kis_tokens.sql production 적용"
    - "workers/ingestion/ + server/src/kis/ + packages/shared/src/kis.ts + packages/shared/src/index.ts (kis export 라인) + server/src/services/kis-runtime.ts + scripts/deploy-ingestion.sh / setup-ingestion-iam.sh / smoke-ingestion.sh git history 보존하며 deletion (git rm)"
    - "deploy-server.sh + server/src/config.ts 의 KIS env/secret 라인 제거"
    - "STATE/REQUIREMENTS/ROADMAP 갱신 — DATA-02 status Pending → ✅ + Phase 09.1 complete"
    - "최종 상태: 시스템 전체에서 KIS 호출 0 (CONTEXT D-01 충족)"
  artifacts:
    - path: .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md
      provides: "cutover #3 (KIS 폐기) 결과 추가"
    - path: .planning/STATE.md
      provides: "갱신 — Phase 09.1 complete, last_updated, progress 갱신"
    - path: .planning/REQUIREMENTS.md
      provides: "DATA-02 status: Complete + Traceability 갱신"
    - path: .planning/ROADMAP.md
      provides: "Phase 09.1 status: Complete + completion date"
  key_links:
    - from: "kis_tokens DROP migration"
      to: "Supabase production"
      via: "supabase db push 또는 SQL Editor — KIS Secret 삭제 후"
      pattern: "DROP TABLE IF EXISTS public.kis_tokens"
---

<objective>
**[BLOCKING]** Wave 4 의 마지막 cutover — RESEARCH §12 의 11-step 순서로 KIS ingestion 완전 폐기. Cloud Scheduler PAUSE → 데이터 정합 검증 → Job/SA/Secret 삭제 → kis_tokens DROP migration push → workers/ingestion 디렉터리 + server/src/kis + packages/shared/src/kis.ts + 관련 스크립트 git rm + commit + STATE/REQUIREMENTS/ROADMAP 갱신.

Purpose: Phase 09.1 의 최종 상태 — 시스템 전체에서 KIS 호출 0 (CONTEXT D-01). git history 보존 (D-01 사용자 명시) — 향후 KIS 가 키움보다 우월한 신규 endpoint 출시 시 git checkout 으로 부분 복원 가능.

Output: GCP KIS 리소스 0, 코드/스크립트 deletion commit, planning 문서 갱신, DEPLOY-LOG 최종 기록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md
@.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md
@.planning/phases/09.1-intraday-current-price/09.1-09-PLAN.md
@.planning/phases/09.1-intraday-current-price/09.1-10-PLAN.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@supabase/migrations/20260514120000_drop_kis_tokens.sql

<read_first>
- `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §12.1 (11-step 순서), §12.2 (디렉터리/파일 삭제 목록), §12.3 (GCP cleanup gcloud), §12.4 (git history 보존)
- `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-01 (KIS 사용처 0)
- 기존 .planning/STATE.md (현재 progress + 갱신 필요 필드)
- 기존 .planning/REQUIREMENTS.md (DATA-02 Pending → Complete)
- 기존 .planning/ROADMAP.md (Phase 09.1 status + plans complete)
- Plan 09 의 DEPLOY-LOG.md (이어서 작성)
</read_first>

<interfaces>
<!-- RESEARCH §12 의 11-step 순서 (rollback 가능한 절차) -->
| Step | Action | Rollback 가능? |
|---|---|---|
| 1 | intraday-sync production live 검증 (Plan 09 + 10 완료) | — (이미 production) |
| 2 | Supabase query 로 stock_quotes 신선도 검증 — 모든 활성 종목 updated_at ≥ 5분 이내 | — |
| 3 | top_movers query 로 등락률 상위 60 정상 갱신 확인 | — |
| 4 | Cloud Scheduler `gh-radar-ingestion-scheduler` PAUSE | ✅ RESUME |
| 5 | 10분 대기 후 데이터 정합 재확인 | ✅ Step 4 RESUME |
| 6 | Cloud Run Job `gh-radar-ingestion` 삭제 | ⚠️ git checkout + redeploy |
| 7 | Cloud Scheduler `gh-radar-ingestion-scheduler` 삭제 | ⚠️ |
| 8 | SA `gh-radar-ingestion-sa` 삭제 | ⚠️ |
| 9 | Secrets `gh-radar-kis-app-key`, `gh-radar-kis-app-secret` 삭제 | ⚠️ |
| 10 | Supabase migration `20260514120000_drop_kis_tokens.sql` push | ⚠️ |
| 11 | git rm 코드/스크립트 + commit + push | ✅ git history 보존 |
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RESEARCH §12 step order → production 안정성 | 순서 위반 시 데이터 공백 위험 (T-09.1-37). Step 1~3 검증 + Step 4 PAUSE (rollback 가능) → Step 5 대기 후 점진 destruction. |
| git rm → 코드 history | --cached 또는 일반 rm 로 working tree 제거 + commit. git log 로 history 영구 보존 (D-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-47 | Availability | KIS PAUSE 후 intraday-sync 가 stock_quotes 갱신 미보장 → 데이터 공백 | mitigate | Step 2 + 3 의 검증 (5분 신선도 + top_movers 60 row) 가 성공해야 step 4 PAUSE. Step 5 의 10분 대기 + 재검증으로 PAUSE 후 stale 종목 식별 가능 (있으면 RESUME 으로 즉시 복귀). |
| T-09.1-48 | Tampering | git rm 시 referenced import 잔존 → build break | mitigate | git rm 전 `pnpm typecheck` 먼저 실행. server/src/kis 에 대한 import 가 모두 server/src/kiwoom 로 교체되었는지 (Plan 07 완료) 검증. workers/ingestion 은 자체 워크스페이스 — deletion 시 다른 워크스페이스 영향 없음 (단, pnpm-lock.yaml 갱신 필요). |
| T-09.1-49 | Configuration | KIS Secret 삭제 후 server old revision 이 active 로 남아 KIS env 참조 시 부팅 실패 | accept | Plan 10 완료 후 server 새 revision active. Cloud Run 의 traffic 100% 새 revision. 옛 revision 은 destroy 안 됨 (rollback 보존) — 사용자가 명시적으로 rollback 시 KIS Secret 미존재로 부팅 실패. 이는 의도된 destructive action. |
| T-09.1-50 | Information Disclosure | git rm 후 history 의 KIS APP_KEY 평문 잔존 | accept | KIS APP_KEY 는 코드에 직접 평문 포함 안 됨 (Secret Manager + env). git history 에는 코드만 — 자격증명 노출 없음. 단, 사용자가 .env 파일을 commit 한 적 있으면 별도 history 정리 필요 (`git filter-repo`) — 본 plan 범위 밖. |
| T-09.1-51 | Availability | kis_tokens DROP 후 server old revision rollback 시 부팅 실패 | accept | 위와 동일. server config.ts 의 KIS env optional 처리 (Plan 07) 로 KIS 부재 시 부팅 통과. 단 KIS_APP_KEY env optional + kis_tokens 부재 시 createKisRuntime 가 throw — server.ts 가 try/catch 로 폴백 — server 부팅 자체는 통과. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Step 1~3 — intraday-sync 데이터 정합 검증 (PAUSE 전 안전 확인)</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §12.1 step 2~3
  </read_first>
  <action>
    SQL 검증 — Supabase production:

    ```sql
    -- Step 2: 활성 종목 stock_quotes 신선도 (5분 이내)
    SELECT COUNT(*) FROM stock_quotes
    WHERE updated_at > now() - interval '5 minutes';
    -- 예상: ≥ 1500 (활성 1898 중 90% 이상 5분 안에 갱신)

    -- Step 3: top_movers 정상 60 row + 갱신 5분 이내
    SELECT COUNT(*), MAX(updated_at) FROM top_movers;
    -- 예상: 60 row + updated_at 5분 이내
    ```

    또는 REST API:
    ```bash
    THRESHOLD=$(date -u -v-5M '+%Y-%m-%dT%H:%M:%S.000Z' 2>/dev/null || date -u -d '5 minutes ago' '+%Y-%m-%dT%H:%M:%S.000Z')
    # stock_quotes 신선도
    curl -fsS -I "${SUPABASE_URL}/rest/v1/stock_quotes?updated_at=gte.${THRESHOLD}&select=code" \
      -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" \
      -H "Prefer: count=exact" -H "Range: 0-0" 2>&1 | grep -i "content-range"
    # top_movers
    curl -fsS -I "${SUPABASE_URL}/rest/v1/top_movers?select=code" \
      -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" \
      -H "Prefer: count=exact" -H "Range: 0-0" 2>&1 | grep -i "content-range"
    ```

    결과 캡처 — DEPLOY-LOG 에 기록.

    **장 시간 외 (15:30 이후 또는 휴장일):** 검증 SKIP — 다음 평일 장 시간 첫 cycle 직후 본 task 재실행. Step 4~ 진행 보류.
  </action>
  <verify>
    <automated>: "${SUPABASE_URL:?}" "${SUPABASE_SERVICE_ROLE_KEY:?}"; THRESHOLD=$(date -u -v-5M '+%Y-%m-%dT%H:%M:%S.000Z' 2>/dev/null || date -u -d '5 minutes ago' '+%Y-%m-%dT%H:%M:%S.000Z'); RANGE=$(curl -fsS -I "${SUPABASE_URL}/rest/v1/stock_quotes?updated_at=gte.${THRESHOLD}&select=code" -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" -H "Prefer: count=exact" -H "Range: 0-0" 2>/dev/null | grep -i 'content-range'); COUNT=$(echo "$RANGE" | grep -oE '[0-9]+$'); [ -n "$COUNT" ] &amp;&amp; [ "$COUNT" -ge 1500 ]</automated>
  </verify>
  <done>
    - stock_quotes 신선도 검증 PASS (≥1500 5분 이내)
    - top_movers 60 row 갱신 확인
    - 장 시간 외라면 SKIP + 다음 평일 재실행 명시
  </done>
</task>

<task type="auto">
  <name>Task 2: Step 4~5 — KIS Cloud Scheduler PAUSE + 10분 대기 + 재검증</name>
  <files>(GCP — Cloud Scheduler state)</files>
  <read_first>
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §12.1 step 4~5, §12.3
  </read_first>
  <action>
    Step 4: KIS Scheduler PAUSE (rollback 가능):
    ```bash
    gcloud scheduler jobs pause gh-radar-ingestion-scheduler --location=asia-northeast3
    gcloud scheduler jobs describe gh-radar-ingestion-scheduler --location=asia-northeast3 --format='value(state)'
    # 예상: PAUSED
    ```

    Step 5: 10분 대기 후 stock_quotes 신선도 재검증 (intraday-sync 가 단독으로 갱신 정상):
    ```bash
    sleep 600
    # Task 1 의 stock_quotes 신선도 query 재실행
    THRESHOLD=$(date -u -v-5M '+%Y-%m-%dT%H:%M:%S.000Z' 2>/dev/null || date -u -d '5 minutes ago' '+%Y-%m-%dT%H:%M:%S.000Z')
    curl -fsS -I "${SUPABASE_URL}/rest/v1/stock_quotes?updated_at=gte.${THRESHOLD}&select=code" \
      -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" \
      -H "Prefer: count=exact" -H "Range: 0-0" 2>&1 | grep -i "content-range"
    # 예상: ≥ 1500 — intraday-sync 단독으로 충분 (KIS 인계 성공)
    ```

    **검증 실패 시 (count < 1500 또는 stale):**
    ```bash
    # Rollback — KIS Scheduler RESUME 으로 복구
    gcloud scheduler jobs resume gh-radar-ingestion-scheduler --location=asia-northeast3
    # 본 plan 중단 + intraday-sync 디버그 (logging, retry config 등)
    ```

    검증 PASS 시 다음 task 진행.
  </action>
  <verify>
    <automated>STATE=$(gcloud scheduler jobs describe gh-radar-ingestion-scheduler --location=asia-northeast3 --format='value(state)' 2>/dev/null); [ "$STATE" = "PAUSED" ]</automated>
  </verify>
  <done>
    - KIS Scheduler PAUSED
    - 10분 대기 + intraday-sync 단독 갱신 stock_quotes ≥1500 확인
    - rollback 절차 명시 (RESUME 명령)
  </done>
</task>

<task type="auto">
  <name>Task 3: Step 6~9 — GCP KIS 리소스 삭제 (Job + Scheduler + SA + Secrets)</name>
  <files>(GCP cleanup)</files>
  <read_first>
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §12.3 (gcloud delete 명령 전체)
  </read_first>
  <action>
    순차 실행 (rollback 어려움 — 각 step 후 확인):

    ```bash
    # Step 6: Cloud Run Job 삭제
    gcloud run jobs delete gh-radar-ingestion --region=asia-northeast3 --quiet

    # Step 7: Cloud Scheduler 삭제
    gcloud scheduler jobs delete gh-radar-ingestion-scheduler --location=asia-northeast3 --quiet

    # Step 8: SA 삭제 (IAM bindings 자동 정리)
    gcloud iam service-accounts delete gh-radar-ingestion-sa@gh-radar.iam.gserviceaccount.com --quiet

    # Step 9: Secrets 삭제
    gcloud secrets delete gh-radar-kis-app-key --quiet
    gcloud secrets delete gh-radar-kis-app-secret --quiet

    # 검증
    gcloud run jobs describe gh-radar-ingestion --region=asia-northeast3 2>&1 | head -3 # NOT_FOUND 예상
    gcloud secrets describe gh-radar-kis-app-key 2>&1 | head -3                         # NOT_FOUND
    gcloud iam service-accounts describe gh-radar-ingestion-sa@gh-radar.iam.gserviceaccount.com 2>&1 | head -3  # NOT_FOUND
    ```

    Alert policy 도 정리 (선택):
    ```bash
    POLICY=$(gcloud alpha monitoring policies list --filter='displayName="gh-radar-ingestion-failure"' --format='value(name)')
    [ -n "$POLICY" ] && gcloud alpha monitoring policies delete "$POLICY" --quiet
    ```
  </action>
  <verify>
    <automated>! gcloud run jobs describe gh-radar-ingestion --region=asia-northeast3 >/dev/null 2>&amp;1 &amp;&amp; ! gcloud secrets describe gh-radar-kis-app-key >/dev/null 2>&amp;1 &amp;&amp; ! gcloud secrets describe gh-radar-kis-app-secret >/dev/null 2>&amp;1 &amp;&amp; ! gcloud iam service-accounts describe gh-radar-ingestion-sa@gh-radar.iam.gserviceaccount.com >/dev/null 2>&amp;1</automated>
  </verify>
  <done>
    - 4 GCP 리소스 모두 NOT_FOUND
    - Alert policy 삭제 (선택)
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Step 10 — kis_tokens DROP migration push (.deferred 복원 후 사용자 확인)</name>
  <what-built>
    Plan 01 이 작성한 `supabase/migrations/20260514120000_drop_kis_tokens.sql` — 본 task 가 production 적용.

    **사전 조건:** Plan 09 Task 1 의 race 가드로 `20260514120000_drop_kis_tokens.sql` 이 `.sql.deferred` 확장자로 rename 되어있어야 함. 본 task 의 옵션 A 절차 첫 step 에서 원본 확장자로 복원한다.
  </what-built>
  <action>본 task 의 사용자 액션은 <how-to-verify> 블록 참조. 자동화 가능한 부분 없음 — 사용자 confirm 필요.</action>
  <how-to-verify>
    사용자가 다음 중 한 방법으로 production DB 에 적용:

    **옵션 A — supabase CLI (Plan 09 의 .deferred 가드 복원 + push):**

    Step 1 — .deferred 확장자 복원:
    ```bash
    cd supabase/migrations
    if [ -f 20260514120000_drop_kis_tokens.sql.deferred ]; then
      mv 20260514120000_drop_kis_tokens.sql.deferred 20260514120000_drop_kis_tokens.sql
    fi
    ls 20260514120000_drop_kis_tokens.sql  # 원본 확장자 복원 확인
    ! ls 20260514120000_drop_kis_tokens.sql.deferred 2>/dev/null  # .deferred 부재 확인
    cd -
    ```

    Step 2 — supabase db push (이번엔 kis_tokens DROP 만 미적용 상태이므로 단일 파일 push):
    ```bash
    export SUPABASE_ACCESS_TOKEN=<사용자 토큰>
    supabase db push
    # 본 명령은 미적용 .sql (= 20260514120000_drop_kis_tokens.sql) 1개만 push
    ```

    Step 3 — 검증:
    ```sql
    SELECT * FROM pg_tables WHERE tablename = 'kis_tokens';
    -- 결과: 0 row (삭제 성공)
    SELECT * FROM pg_tables WHERE tablename = 'kiwoom_tokens';
    -- 결과: 1 row (kiwoom_tokens 잔존 — Plan 09 에서 적용된 상태 그대로)
    ```

    **옵션 B — SQL Editor (.deferred 복원 불필요, supabase CLI 미사용):**
    1. supabase/migrations/20260514120000_drop_kis_tokens.sql (또는 .sql.deferred) 내용 복사
    2. Supabase Dashboard SQL Editor 에서 실행
    3. 옵션 A 의 Step 3 검증 SQL 실행
    4. 옵션 B 채택 시에도 git tracking 정합성을 위해 `.deferred` 확장자는 원본 `.sql` 로 복원 권장 (`mv ...sql.deferred ...sql`)

    실패 시 (FK references 등): error message 캡처 + Plan 중단 + 디버그.
  </how-to-verify>
  <resume-signal>"kis_tokens dropped" 또는 issue</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Step 11 — git rm + commit (코드 + 스크립트 + dead config 정리)</name>
  <files>workers/ingestion, server/src/kis, packages/shared/src/kis.ts, packages/shared/src/index.ts, server/src/services/kis-runtime.ts, server/src/config.ts, scripts/deploy-ingestion.sh, scripts/setup-ingestion-iam.sh, scripts/smoke-ingestion.sh, scripts/deploy-server.sh, ops/alert-ingestion-failure.yaml</files>
  <read_first>
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §12.2 (deletion 목록 전문), §12.4 (git history 보존)
  </read_first>
  <action>
    Step 11.1: git rm 디렉터리 + 파일:
    ```bash
    # 워크스페이스 통째 삭제
    git rm -r workers/ingestion/

    # server/src/kis 디렉터리 (Plan 07 에서 import 모두 kiwoom 으로 교체됨)
    git rm -r server/src/kis/

    # server/src/services/kis-runtime.ts (kis-runtime 호출은 Plan 07 의 server.ts 갱신에서 제거됨)
    git rm server/src/services/kis-runtime.ts

    # packages/shared/src/kis.ts
    git rm packages/shared/src/kis.ts

    # 스크립트
    git rm scripts/deploy-ingestion.sh
    git rm scripts/setup-ingestion-iam.sh
    git rm scripts/smoke-ingestion.sh

    # 기존 KIS alert YAML (있는 경우)
    [ -f ops/alert-ingestion-failure.yaml ] && git rm ops/alert-ingestion-failure.yaml

    # 기존 server tests (KIS 관련)
    [ -d server/tests/kis ] && git rm -r server/tests/kis
    ```

    Step 11.2: 코드 수정 — KIS 잔존 import / config 제거:

    `packages/shared/src/index.ts` — KIS export 라인 제거:
    ```typescript
    // 본 라인 삭제:
    // export type { KisRankingRow, KisTokenResponse } from "./kis.js";
    ```

    `server/src/config.ts` — KIS env 라인 제거 + 타입 필드 제거:
    ```typescript
    // AppConfig type 에서 다음 3 라인 삭제:
    // kisBaseUrl: string;
    // kisAppKey: string;
    // kisAppSecret: string;

    // loadConfig() 안에서 다음 3 라인 삭제:
    // kisBaseUrl: process.env.KIS_BASE_URL ?? "...",
    // kisAppKey: optional("KIS_APP_KEY") ?? "",
    // kisAppSecret: optional("KIS_APP_SECRET") ?? "",
    ```

    `scripts/deploy-server.sh` — KIS 관련 라인 제거:
    - Section 2.5 의 KIS secret accessor 바인딩 (`gh-radar-kis-app-key/secret` for loop) 삭제
    - Section 5 의 `--set-env-vars` 에서 `KIS_BASE_URL=...` 삭제
    - Section 5 의 `--update-secrets` 에서 `KIS_APP_KEY=gh-radar-kis-app-key:latest, KIS_APP_SECRET=gh-radar-kis-app-secret:latest` 삭제

    Step 11.3: 빌드 검증 — 모든 워크스페이스:
    ```bash
    pnpm install --frozen-lockfile=false  # workers/ingestion 제거 반영
    pnpm -r typecheck
    pnpm -r build
    pnpm -r test
    ```

    실패 시: 잔존 import 또는 dead config 잔존 — grep 으로 추적:
    ```bash
    grep -rn "from \"@gh-radar/shared\"" --include="*.ts" | grep -i "Kis"
    grep -rn "kis_tokens\|KIS_APP_KEY\|kisBaseUrl" server/ workers/ packages/
    grep -rn "from \".*\\/kis\\/" server/ workers/ packages/
    ```

    Step 11.4: server 재배포 (KIS env/secret 제거된 새 image):
    ```bash
    bash scripts/deploy-server.sh
    # 새 revision active. KIS env 제거 + KIS secret 제거 → 더 깨끗한 config.
    ```

    Step 11.5: git commit (한글 메시지, 사용자 확인 후):
    ```bash
    git add -A
    git status  # 검토
    # 사용자 확인 후:
    git commit -m "feat(09.1): KIS ingestion 완전 폐기 — workers/ingestion, server/src/kis, packages/shared/src/kis.ts 디렉터리/파일 삭제 + GCP Job/Scheduler/SA/Secrets 정리 + kis_tokens DROP migration 적용. 키움 단일 source 로 전환 완료 (D-01)."
    git push
    ```
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar &amp;&amp; pnpm -r typecheck &amp;&amp; pnpm -r build &amp;&amp; ! test -d workers/ingestion &amp;&amp; ! test -d server/src/kis &amp;&amp; ! test -f packages/shared/src/kis.ts &amp;&amp; ! grep -rq "KIS_APP_KEY\|kisBaseUrl\|from \"@gh-radar/shared\".*KisRankingRow" server/src packages/shared/src</automated>
  </verify>
  <done>
    - workers/ingestion + server/src/kis + packages/shared/src/kis.ts + server/src/services/kis-runtime.ts deletion
    - 3 KIS scripts deletion
    - server/src/config.ts + scripts/deploy-server.sh 의 KIS 라인 제거
    - packages/shared/src/index.ts 의 KIS export 라인 제거
    - 모든 워크스페이스 typecheck/build/test green
    - server 재배포 (KIS env/secret 제거된 새 revision)
    - git commit (한글 메시지) + push
  </done>
</task>

<task type="auto">
  <name>Task 6: STATE.md / REQUIREMENTS.md / ROADMAP.md 갱신 + DEPLOY-LOG.md 최종 기록</name>
  <files>.planning/STATE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md</files>
  <read_first>
    - `.planning/STATE.md` (현재 — Phase 09.1 planning complete 상태)
    - `.planning/REQUIREMENTS.md` (DATA-02 Pending)
    - `.planning/ROADMAP.md` (Phase 09.1 plans TBD)
    - 기존 DEPLOY-LOG.md (Plan 09 + 10 작성)
  </read_first>
  <action>
    Step 6.1: `.planning/STATE.md` 갱신 — frontmatter + Roadmap Evolution + Performance Metrics:
    - frontmatter `status` → `executing` 유지 (Phase 10 다음)
    - frontmatter `stopped_at` → `Phase 09.1 complete — KIS → 키움 완전 대체 + 코드 deletion + DEPLOY-LOG`
    - frontmatter `last_updated` → 본 task 실행 시각 ISO
    - frontmatter `last_activity` → `{date} -- Phase 09.1 complete`
    - frontmatter `progress.completed_plans` += 11
    - frontmatter `progress.completed_phases` += 1
    - frontmatter `progress.percent` 재계산
    - "Current Position" 섹션 — Phase 10 으로 갱신
    - "Roadmap Evolution" 섹션 — Phase 09.1 complete 한 줄 추가:
      ```
      - Phase 09.1 complete {date}: KIS ingestion 완전 폐기 + 키움 REST API (ka10027 페이지네이션 + ka10001 hot set) 단일 source 전환. workers/intraday-sync 신설 (Cloud Run Job + Scheduler `* 9-15 * * 1-5` Asia/Seoul, VPC + Static IP). server/src/kis → server/src/kiwoom 교체 + Cloud Run service VPC connector 재배포. SC #1~9 모두 충족. trade_amount 정책 정확값 → volume×close 근사값 전환 (D-23). git history 보존 (workers/ingestion + server/src/kis + packages/shared/src/kis.ts).
      ```
    - "Performance Metrics > By Phase" 테이블 — Phase 09.1 row 추가 (11 plans, duration)

    Step 6.2: `.planning/REQUIREMENTS.md` 갱신:
    - DATA-02 항목 — 본문 끝에 `**Status:** ✅ Complete (Phase 09.1 — {date})` 추가
    - Traceability 표 — `| DATA-02 | Phase 09.1 | Pending |` → `| DATA-02 | Phase 09.1 | Complete |`
    - "Last updated" 푸터 갱신

    Step 6.3: `.planning/ROADMAP.md` 갱신:
    - "Phase 09.1: intraday-current-price (RE-SCOPED 2026-05-14 — KIS → 키움 완전 대체)" section
      - `**Plans:** TBD` → `**Plans:** 11 plans complete`
      - `**Status:** ✅ Complete ({date})` 추가
      - "Plans:" 라인 다음에 11 PLAN 의 체크리스트 추가:
        ```
        Plans:
        - [x] 09.1-01-PLAN.md — Wave 0 마이그레이션 SQL 4종 (kiwoom_tokens + 2 RPC + kis_tokens DROP)
        - [x] 09.1-02-PLAN.md — Wave 0 packages/shared/src/kiwoom.ts 4 타입
        - [x] 09.1-03-PLAN.md — Wave 0 workers/intraday-sync 워크스페이스 스캐폴드
        - [x] 09.1-04-PLAN.md — Wave 1 STEP1 client + token store + ka10027 + signed mapper
        - [x] 09.1-05-PLAN.md — Wave 1 STEP2 ka10001 hot set + mapOhlc + 5 fixture
        - [x] 09.1-06-PLAN.md — Wave 1 pipeline 통합 (bootstrap + topMovers + 2 RPC caller + stock_quotes 양단계 + runIntradayCycle)
        - [x] 09.1-07-PLAN.md — Wave 2 server kiwoom 교체 + mapper 갱신 + routes/stocks 전환
        - [x] 09.1-08-PLAN.md — Wave 3 setup/deploy/smoke 스크립트 + alert YAML
        - [x] 09.1-09-PLAN.md — Wave 4 cutover #1 worker production + IAM/VPC/IP whitelist
        - [x] 09.1-10-PLAN.md — Wave 4 cutover #2 server VPC 재배포
        - [x] 09.1-11-PLAN.md — Wave 4 cutover #3 KIS 폐기 + git deletion + STATE/REQUIREMENTS/ROADMAP 갱신
        ```
    - "Progress > By Phase" 테이블 — Phase 09.1 row: `Complete | {date}`
    - 상단 Phases 목록 — Phase 09.1 항목 [x] 마킹

    Step 6.4: DEPLOY-LOG.md 최종 section 추가:
    ```markdown
    ---

    ## Wave 4 cutover #3 — KIS deprecation (RESEARCH §12 11-step)

    **Date:** {today}
    **Trigger:** Plan 11

    ### Pre-deprecation verification (Step 1~3)

    | Check | Result |
    |---|---|
    | stock_quotes 5분 이내 갱신 row >= 1500 (intraday-sync 단독) | ✅ {COUNT} row |
    | top_movers 60 row + updated_at 5분 이내 | ✅ |

    ### Cleanup steps (Step 4~10)

    | Step | Action | Result |
    |---|---|---|
    | 4 | Scheduler PAUSE | ✅ PAUSED |
    | 5 | 10분 대기 + 재검증 | ✅ stock_quotes ≥1500 (intraday-sync 단독 갱신) |
    | 6 | Cloud Run Job 삭제 | ✅ NOT_FOUND |
    | 7 | Scheduler 삭제 | ✅ NOT_FOUND |
    | 8 | SA 삭제 | ✅ NOT_FOUND |
    | 9 | Secrets 삭제 (kis-app-key + kis-app-secret) | ✅ NOT_FOUND × 2 |
    | 10 | kis_tokens DROP migration | ✅ Applied |

    ### Code deletion (Step 11)

    | Path | Status |
    |---|---|
    | workers/ingestion/ | ✅ git rm |
    | server/src/kis/ | ✅ git rm |
    | server/src/services/kis-runtime.ts | ✅ git rm |
    | packages/shared/src/kis.ts | ✅ git rm |
    | packages/shared/src/index.ts (KIS export 라인) | ✅ 수정 |
    | server/src/config.ts (KIS env 필드) | ✅ 수정 |
    | scripts/deploy-server.sh (KIS env/secret 라인) | ✅ 수정 |
    | scripts/deploy-ingestion.sh / setup-ingestion-iam.sh / smoke-ingestion.sh | ✅ git rm |
    | ops/alert-ingestion-failure.yaml | ✅ git rm |

    ### Build verification

    | Workspace | typecheck | build | test |
    |---|---|---|---|
    | @gh-radar/shared | ✅ | ✅ | ✅ |
    | @gh-radar/intraday-sync | ✅ | ✅ | ✅ |
    | server | ✅ | ✅ | ✅ |
    | webapp | ✅ | ✅ | ✅ |
    | workers/master-sync, candle-sync, news-sync, discussion-sync | ✅ | ✅ | ✅ |

    ### Server redeploy (KIS env/secret 제거된 새 image)

    - 새 revision: gh-radar-server-{revision}
    - SHA: `{git SHA}`

    ### Git commit

    - SHA: `{commit hash}`
    - Message: "feat(09.1): KIS ingestion 완전 폐기 — ..."

    ### Final State

    - **시스템 전체 KIS 호출**: **0** (CONTEXT D-01 충족) ✅
    - **intraday-sync**: production active (cron `* 9-15 * * 1-5` Asia/Seoul)
    - **server**: Kiwoom only + VPC connector
    - **stocks/stock_quotes/stock_daily_ohlcv 갱신**: intraday-sync (분 단위) + candle-sync EOD (17:30) + master-sync (08:10)
    - **git history**: 폐기 코드 보존 — 향후 KIS 우월 endpoint 출시 시 `git checkout {commit}^ -- workers/ingestion/` 부분 복원 가능

    ### Phase 09.1 Success Criteria 검증 (RE-SCOPED, 9개)

    | # | 기준 | 상태 | 증거 |
    |---|---|---|---|
    | 1 | workers/intraday-sync STEP1+STEP2 매분 cycle | ✅ | Cloud Logging "intraday cycle complete" 매분 |
    | 2 | kis_tokens DROP + kiwoom_tokens CREATE + 2 RPC production 적용 + REVOKE 명시 | ✅ | Step 10 + Plan 09 |
    | 3 | Cloud Run Job + Scheduler ENABLED | ✅ | gcloud describe |
    | 4 | VPC + Subnet + Router + NAT + Static IP + worker + service VPC connector | ✅ | Plan 09 + Plan 10 |
    | 5 | server/src/kis → server/src/kiwoom + ka10001 동기 호출 | ✅ | Plan 07 + Plan 10 |
    | 6 | 키움 IP 화이트리스트 + 첫 cycle ka10027 ≥1500 + ka10001 hot set | ✅ | Plan 09 + smoke INV-4/5 |
    | 7 | KIS ingestion 완전 폐기 (Job/Scheduler/SA/Secrets/kis_tokens) | ✅ | 본 plan Step 4~10 |
    | 8 | candle-sync EOD 17:30 와 시간/컬럼 충돌 없음 | ✅ | RPC 컬럼 분리 (close/STEP1 vs ohlc/STEP2 + EOD overlay) |
    | 9 | trade_amount 근사값 정책 + 코드 주석 명시 | ✅ | workers/intraday-sync/src/pipeline/map.ts:25 |
    ```

    Step 6.5: git commit (위 4 파일):
    ```bash
    git add .planning/STATE.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md
    # 사용자 확인 후:
    git commit -m "docs(09.1): Phase 09.1 complete — STATE/REQUIREMENTS/ROADMAP/DEPLOY-LOG 갱신. DATA-02 ✅. KIS → 키움 완전 대체 + worker production + server VPC + KIS 폐기 + git deletion."
    git push
    ```
  </action>
  <verify>
    <automated>grep -q "DATA-02 | Phase 09.1 | Complete" .planning/REQUIREMENTS.md &amp;&amp; grep -q "Phase 09.1 complete" .planning/STATE.md &amp;&amp; grep -q "Wave 4 cutover #3" .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md &amp;&amp; grep -q "11 plans complete" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    - STATE.md frontmatter + Current Position + Roadmap Evolution + Performance Metrics 갱신
    - REQUIREMENTS.md DATA-02 status Complete + Traceability 갱신
    - ROADMAP.md Phase 09.1 status Complete + 11 plans 체크리스트
    - DEPLOY-LOG.md 최종 cutover #3 + 9 SC 검증 표
    - git commit (한글) + push
  </done>
</task>

</tasks>

<verification>
- 시스템 전체 KIS 호출 0 (CONTEXT D-01 최종 충족)
- pnpm -r typecheck/build/test 모두 green
- git history 에 폐기 코드 보존 (`git log --all -- workers/ingestion/` 보임)
- planning 문서 (STATE/REQUIREMENTS/ROADMAP/DEPLOY-LOG) 모두 갱신
- DATA-02 ✅ Complete
</verification>

<success_criteria>
- Phase 09.1 의 9 SC 모두 충족 (DEPLOY-LOG 의 9 SC 검증 표 ✅)
- KIS 호출 0 (D-01)
- 키움 단일 source, intraday-sync + server (kiwoom) 모두 production active
- 운영 안정성: alert + smoke + retry policy 활성
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-intraday-current-price/09.1-11-SUMMARY.md`
</output>
