---
phase: 09.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260514120000_drop_kis_tokens.sql
  - supabase/migrations/20260514120100_create_kiwoom_tokens.sql
  - supabase/migrations/20260514120200_intraday_upsert_close_rpc.sql
  - supabase/migrations/20260514120300_intraday_upsert_ohlc_rpc.sql
autonomous: true
requirements: [DATA-02]
requirements_addressed:
  - DATA-02 (마이그레이션 SQL 파일 생성 — kis_tokens DROP + kiwoom_tokens CREATE + intraday_upsert_close/ohlc RPC. push 는 Plan 09 [BLOCKING])
must_haves:
  truths:
    - "supabase/migrations 디렉터리에 4개 신규 SQL 파일 존재"
    - "kiwoom_tokens 테이블 schema = (token_type PK, access_token, expires_at, fetched_at)"
    - "intraday_upsert_close 가 stock_daily_ohlcv 의 close/change/volume 갱신 + INSERT 시 open=high=low=close 임시 초기화"
    - "intraday_upsert_ohlc 가 hot set 의 open/high/low 만 UPDATE (close/volume omit)"
    - "모든 신규 RPC 와 kiwoom_tokens 가 anon/authenticated REVOKE + service_role only GRANT 명시"
  artifacts:
    - path: supabase/migrations/20260514120000_drop_kis_tokens.sql
      provides: "kis_tokens DROP (Wave 4 cutover 의 마지막에 push)"
      contains: "DROP TABLE IF EXISTS public.kis_tokens"
    - path: supabase/migrations/20260514120100_create_kiwoom_tokens.sql
      provides: "kiwoom_tokens CREATE + RLS"
      contains: "CREATE TABLE public.kiwoom_tokens"
    - path: supabase/migrations/20260514120200_intraday_upsert_close_rpc.sql
      provides: "RPC #1 intraday_upsert_close(jsonb)"
      contains: "CREATE OR REPLACE FUNCTION public.intraday_upsert_close"
    - path: supabase/migrations/20260514120300_intraday_upsert_ohlc_rpc.sql
      provides: "RPC #2 intraday_upsert_ohlc(jsonb)"
      contains: "CREATE OR REPLACE FUNCTION public.intraday_upsert_ohlc"
  key_links:
    - from: "intraday_upsert_close"
      to: "stock_daily_ohlcv"
      via: "INSERT ... ON CONFLICT (code, date) DO UPDATE SET close, high=GREATEST, low=LEAST, volume, trade_amount, change_amount, change_rate"
      pattern: "ON CONFLICT \\(code, date\\) DO UPDATE"
    - from: "intraday_upsert_ohlc"
      to: "stock_daily_ohlcv"
      via: "ON CONFLICT (code, date) DO UPDATE SET open=EXCLUDED.open, high=EXCLUDED.high, low=EXCLUDED.low (close/volume 의도적 omit)"
      pattern: "open  = EXCLUDED.open"
---

<objective>
Wave 0 의 DB 마이그레이션 SQL 4종을 작성한다 — `kis_tokens` DROP, `kiwoom_tokens` CREATE + RLS, `intraday_upsert_close(jsonb)` RPC, `intraday_upsert_ohlc(jsonb)` RPC.

Purpose: intraday-sync worker 가 의존하는 DB 객체 (token cache + 2개 RPC) 를 Wave 0 에 정의해 Wave 1 워커 코드가 컴파일·테스트 가능하게 한다. Production push 는 Wave 4 의 [BLOCKING] cutover 에서 실행 (`supabase db push`). kis_tokens DROP 파일은 본 Wave 에 작성하지만 Wave 4 의 KIS 폐기 마지막 step 에서만 push 된다.

Output: 4개 SQL 파일 + REVOKE/GRANT 명시 (MEMORY feedback_supabase_rpc_revoke 룰 준수).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md
@.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md
@supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql
@supabase/migrations/20260512120000_create_stock_daily_ohlcv.sql

<read_first>
- `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-26~D-28 (kiwoom_tokens schema), D-33~D-35 (RPC 정책)
- `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §3.1 (RPC #1 SQL 본문), §3.2 (RPC #2 SQL 본문), §5.2 (kis_tokens DROP + kiwoom_tokens CREATE)
- `supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql` (RLS REVOKE/GRANT 패턴 참조)
- `supabase/migrations/20260512120000_create_stock_daily_ohlcv.sql` (FK NOT VALID + 컬럼 타입 numeric(20,2) / numeric(10,4))
- MEMORY: `feedback_supabase_rpc_revoke` (REVOKE FROM PUBLIC + REVOKE FROM anon, authenticated 둘 다 명시 필수)
</read_first>

<interfaces>
<!-- 기존 stock_daily_ohlcv 컬럼 (Phase 9 + hotfix) — 본 plan 의 RPC 가 이 schema 에 INSERT/UPDATE -->
```sql
-- supabase/migrations/20260512120000_create_stock_daily_ohlcv.sql
CREATE TABLE public.stock_daily_ohlcv (
  code          text NOT NULL,
  date          date NOT NULL,
  open          numeric(20,2),
  high          numeric(20,2),
  low           numeric(20,2),
  close         numeric(20,2),
  volume        bigint,
  trade_amount  bigint,
  change_amount numeric(20,2),
  change_rate   numeric(10,4),  -- hotfix 20260512123000
  inserted_at   timestamptz NOT NULL DEFAULT now(),
  PRIMARY KEY (code, date)
);
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase REST → Postgres | service_role JWT 만 RPC 호출 가능. anon/authenticated 는 REVOKE 명시. |
| migration 파일 → production DB | 본 Plan 은 SQL 파일만 작성. 실제 push 는 Wave 4 [BLOCKING] task. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-01 | Elevation of Privilege | intraday_upsert_close / intraday_upsert_ohlc RPC | mitigate | `REVOKE EXECUTE ON FUNCTION ... FROM PUBLIC; REVOKE EXECUTE ... FROM anon, authenticated; GRANT EXECUTE ... TO service_role` 3줄 명시 (MEMORY feedback_supabase_rpc_revoke). 단일 REVOKE 만으로 platform auto-grant 회귀 위험. |
| T-09.1-02 | Information Disclosure | kiwoom_tokens 테이블 (access_token 평문 저장) | mitigate | `ALTER TABLE ... ENABLE ROW LEVEL SECURITY; REVOKE ALL FROM PUBLIC; REVOKE ALL FROM anon, authenticated; GRANT SELECT, INSERT, UPDATE, DELETE TO service_role` — anon/authenticated 가 token 을 SELECT 못 함. token 자체는 24h 짧은 lifetime + refresh 가능. |
| T-09.1-03 | Tampering | RPC #1 의 OHL 무단 overwrite | accept | RPC #1 의 ON CONFLICT UPDATE branch 에서 `open` 의도적 omit (첫 INSERT 만 set). high=GREATEST, low=LEAST 단조. RPC #2 가 hot set 의 정확값으로 추후 덮어쓰기. EOD 17:30 candle-sync 가 모든 OHLCV 컬럼을 공식값으로 최종 overlay. |
| T-09.1-04 | Availability | kis_tokens DROP 시점 race | mitigate | DROP migration 파일은 본 Plan 에 작성하지만 Wave 4 의 KIS 폐기 마지막 step 에서만 push. Wave 0 push 시 KIS ingestion worker 가 토큰 발급 실패 → 본 Plan 09 의 push 순서 지정. (Plan 02 의 kiwoom_tokens 만 Wave 4 초반에 push) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: kis_tokens DROP 마이그레이션 작성</name>
  <files>supabase/migrations/20260514120000_drop_kis_tokens.sql</files>
  <read_first>
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §5.2 Migration 1
    - `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-01 (KIS 사용처 0)
  </read_first>
  <action>
    `supabase/migrations/20260514120000_drop_kis_tokens.sql` 신규 생성. 본문:

    ```sql
    -- 09.1 Plan 01 — kis_tokens DROP (Wave 4 KIS 폐기 cutover 마지막 step 에서 push)
    -- 본 파일은 Wave 0 에서 작성하지만, Wave 4 의 12.1 step 10 (KIS Job/Scheduler/SA/Secret 정리 후) 에서만 production 에 적용.
    -- Wave 4 이전에 push 하면 KIS ingestion worker 가 토큰 발급 실패 (kis_tokens 의존).

    BEGIN;

    DROP TABLE IF EXISTS public.kis_tokens CASCADE;

    COMMIT;
    ```

    주의: CASCADE — kis_tokens 에 FK 가 걸린 객체는 없지만 안전망. PUBLIC SCHEMA 만 drop.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260514120000_drop_kis_tokens.sql &amp;&amp; grep -q "DROP TABLE IF EXISTS public.kis_tokens" supabase/migrations/20260514120000_drop_kis_tokens.sql</automated>
  </verify>
  <done>
    - 파일 존재 + DROP TABLE 문 포함
    - BEGIN/COMMIT transaction 래핑
    - 주석으로 Wave 4 push 시점 명시
  </done>
</task>

<task type="auto">
  <name>Task 2: kiwoom_tokens CREATE 마이그레이션 작성 (RLS REVOKE/GRANT 명시)</name>
  <files>supabase/migrations/20260514120100_create_kiwoom_tokens.sql</files>
  <read_first>
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §5.2 Migration 2
    - `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-26 (token_type PK), D-27 (24h TTL refresh)
    - MEMORY `feedback_supabase_rpc_revoke` — REVOKE FROM PUBLIC + REVOKE FROM anon, authenticated 둘 다 필수
  </read_first>
  <action>
    `supabase/migrations/20260514120100_create_kiwoom_tokens.sql` 신규 생성. 본문:

    ```sql
    -- 09.1 Plan 01 — kiwoom_tokens CREATE
    -- D-26: schema = (token_type PK, access_token, expires_at, fetched_at)
    -- D-27: 24h TTL — worker/server 가 만료 5분 전 refresh
    -- D-35 / MEMORY feedback_supabase_rpc_revoke: REVOKE FROM PUBLIC + REVOKE FROM anon, authenticated 명시
    -- 본 파일은 Wave 4 cutover 의 첫 push (worker production live 직전).

    BEGIN;

    CREATE TABLE public.kiwoom_tokens (
      token_type   text PRIMARY KEY,                    -- "live" / "mock"
      access_token text NOT NULL,
      expires_at   timestamptz NOT NULL,
      fetched_at   timestamptz NOT NULL DEFAULT now()
    );

    -- RLS: service_role only (worker + server 양쪽 SELECT/UPSERT)
    ALTER TABLE public.kiwoom_tokens ENABLE ROW LEVEL SECURITY;

    REVOKE ALL ON public.kiwoom_tokens FROM PUBLIC;
    REVOKE ALL ON public.kiwoom_tokens FROM anon, authenticated;
    GRANT SELECT, INSERT, UPDATE, DELETE ON public.kiwoom_tokens TO service_role;

    COMMIT;
    ```
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260514120100_create_kiwoom_tokens.sql &amp;&amp; grep -q "CREATE TABLE public.kiwoom_tokens" supabase/migrations/20260514120100_create_kiwoom_tokens.sql &amp;&amp; grep -q "REVOKE ALL ON public.kiwoom_tokens FROM anon, authenticated" supabase/migrations/20260514120100_create_kiwoom_tokens.sql &amp;&amp; grep -q "GRANT SELECT, INSERT, UPDATE, DELETE ON public.kiwoom_tokens TO service_role" supabase/migrations/20260514120100_create_kiwoom_tokens.sql</automated>
  </verify>
  <done>
    - 파일 존재
    - CREATE TABLE 4 컬럼 (token_type PK, access_token NOT NULL, expires_at NOT NULL, fetched_at DEFAULT now())
    - ENABLE ROW LEVEL SECURITY
    - REVOKE FROM PUBLIC + REVOKE FROM anon, authenticated (둘 다 명시)
    - GRANT TO service_role
  </done>
</task>

<task type="auto">
  <name>Task 3: intraday_upsert_close RPC 마이그레이션 작성 (D-33)</name>
  <files>supabase/migrations/20260514120200_intraday_upsert_close_rpc.sql</files>
  <read_first>
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §3.1 (SQL 전문 그대로 복사)
    - `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-33 (INSERT 시 open=high=low=close 임시 초기화 + UPDATE 시 high=GREATEST/low=LEAST/close=cur_prc/volume=now_trde_qty)
    - MEMORY `feedback_supabase_rpc_revoke`
  </read_first>
  <action>
    `supabase/migrations/20260514120200_intraday_upsert_close_rpc.sql` 신규 생성. RESEARCH §3.1 의 SQL 본문을 그대로 복사:

    ```sql
    -- 09.1 Plan 01 — intraday_upsert_close(jsonb) RPC (STEP1)
    -- D-33: STEP1 의 1,898 종목 close/change/volume + 09:00 첫 cycle INSERT 시 OHL=close 임시 초기화
    -- ON CONFLICT UPDATE branch: high=GREATEST, low=LEAST 단조 갱신, open 의도적 omit (STEP2 가 정확값으로 덮어쓰기)
    -- D-35: REVOKE PUBLIC + REVOKE anon, authenticated + GRANT service_role only

    BEGIN;

    CREATE OR REPLACE FUNCTION public.intraday_upsert_close(
      p_rows jsonb
    )
    RETURNS integer
    LANGUAGE plpgsql
    SECURITY DEFINER
    AS $$
    DECLARE
      r jsonb;
      affected integer := 0;
    BEGIN
      FOR r IN SELECT * FROM jsonb_array_elements(p_rows)
      LOOP
        INSERT INTO public.stock_daily_ohlcv
          (code, date, open, high, low, close, volume, trade_amount, change_amount, change_rate, inserted_at)
        VALUES (
          r->>'code',
          (r->>'date')::date,
          (r->>'close')::numeric(20,2),    -- INSERT 시: open = cur_prc 임시
          (r->>'close')::numeric(20,2),    -- INSERT 시: high = cur_prc 임시
          (r->>'close')::numeric(20,2),    -- INSERT 시: low  = cur_prc 임시
          (r->>'close')::numeric(20,2),
          (r->>'volume')::bigint,
          (r->>'trade_amount')::bigint,    -- D-22, D-23: volume × close 근사값
          NULLIF(r->>'change_amount', '')::numeric(20,2),
          NULLIF(r->>'change_rate', '')::numeric(10,4),
          now()
        )
        ON CONFLICT (code, date) DO UPDATE
          SET close         = EXCLUDED.close,
              high          = GREATEST(stock_daily_ohlcv.high, EXCLUDED.close),
              low           = LEAST(stock_daily_ohlcv.low, EXCLUDED.close),
              volume        = EXCLUDED.volume,
              trade_amount  = EXCLUDED.trade_amount,
              change_amount = EXCLUDED.change_amount,
              change_rate   = EXCLUDED.change_rate,
              inserted_at   = now();
              -- open: 의도적 omit (첫 INSERT 만 set, STEP2 의 intraday_upsert_ohlc 가 정확값 덮어쓰기)
        affected := affected + 1;
      END LOOP;
      RETURN affected;
    END;
    $$;

    REVOKE EXECUTE ON FUNCTION public.intraday_upsert_close(jsonb) FROM PUBLIC;
    REVOKE EXECUTE ON FUNCTION public.intraday_upsert_close(jsonb) FROM anon, authenticated;
    GRANT EXECUTE ON FUNCTION public.intraday_upsert_close(jsonb) TO service_role;

    COMMIT;
    ```
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260514120200_intraday_upsert_close_rpc.sql &amp;&amp; grep -q "CREATE OR REPLACE FUNCTION public.intraday_upsert_close" supabase/migrations/20260514120200_intraday_upsert_close_rpc.sql &amp;&amp; grep -q "high          = GREATEST" supabase/migrations/20260514120200_intraday_upsert_close_rpc.sql &amp;&amp; grep -q "REVOKE EXECUTE ON FUNCTION public.intraday_upsert_close(jsonb) FROM anon, authenticated" supabase/migrations/20260514120200_intraday_upsert_close_rpc.sql &amp;&amp; grep -q "GRANT EXECUTE ON FUNCTION public.intraday_upsert_close(jsonb) TO service_role" supabase/migrations/20260514120200_intraday_upsert_close_rpc.sql</automated>
  </verify>
  <done>
    - 파일 존재
    - SECURITY DEFINER + LANGUAGE plpgsql
    - INSERT branch: open=high=low=close (임시값)
    - ON CONFLICT DO UPDATE: high=GREATEST, low=LEAST, close=EXCLUDED.close, volume=EXCLUDED.volume, trade_amount + change_amount + change_rate
    - open 컬럼이 ON CONFLICT 의 SET 절에 없음 (의도적 omit)
    - REVOKE FROM PUBLIC + REVOKE FROM anon, authenticated 두 줄
    - GRANT EXECUTE TO service_role
  </done>
</task>

<task type="auto">
  <name>Task 4: intraday_upsert_ohlc RPC 마이그레이션 작성 (D-34, 신규)</name>
  <files>supabase/migrations/20260514120300_intraday_upsert_ohlc_rpc.sql</files>
  <read_first>
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §3.2 (SQL 전문 그대로 복사)
    - `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-34 (hot set OHLC + 상한가/하한가/시가총액 — STEP1 임시값 덮어쓰기), D-14 (hot set 외 ~1,700 종목은 STEP1 임시값 유지)
    - MEMORY `feedback_supabase_rpc_revoke`
  </read_first>
  <action>
    `supabase/migrations/20260514120300_intraday_upsert_ohlc_rpc.sql` 신규 생성. RESEARCH §3.2 그대로 복사:

    ```sql
    -- 09.1 Plan 01 — intraday_upsert_ohlc(jsonb) RPC (STEP2 신규)
    -- D-34: STEP2 의 hot set ~250 종목 정확 OHLC. RPC #1 의 임시값 (open=high=low=close) 위에 정확값 덮어쓰기
    -- 호출 측 보장: 항상 RPC #1 → RPC #2 순서 (RPC #2 INSERT branch 는 거의 발생 안 함, 안전상 명시)
    -- D-35: REVOKE PUBLIC + REVOKE anon, authenticated + GRANT service_role only

    BEGIN;

    CREATE OR REPLACE FUNCTION public.intraday_upsert_ohlc(
      p_rows jsonb
    )
    RETURNS integer
    LANGUAGE plpgsql
    SECURITY DEFINER
    AS $$
    DECLARE
      r jsonb;
      affected integer := 0;
    BEGIN
      FOR r IN SELECT * FROM jsonb_array_elements(p_rows)
      LOOP
        INSERT INTO public.stock_daily_ohlcv
          (code, date, open, high, low, close, volume, trade_amount, change_amount, change_rate, inserted_at)
        VALUES (
          r->>'code',
          (r->>'date')::date,
          (r->>'open')::numeric(20,2),
          (r->>'high')::numeric(20,2),
          (r->>'low')::numeric(20,2),
          (r->>'open')::numeric(20,2),     -- INSERT-only branch (희귀): close 폴백 = open
          0,                                -- volume — STEP1 RPC 가 매분 갱신
          0,                                -- trade_amount — STEP1 RPC 가 매분 갱신
          NULL,
          NULL,
          now()
        )
        ON CONFLICT (code, date) DO UPDATE
          SET open  = EXCLUDED.open,        -- ka10001 의 정확 시가로 덮어쓰기 (D-14, D-34)
              high  = EXCLUDED.high,        -- ka10001 의 정확 고가
              low   = EXCLUDED.low,         -- ka10001 의 정확 저가
              inserted_at = now();
              -- close, volume, trade_amount, change_amount, change_rate: 의도적 omit
              -- STEP1 의 intraday_upsert_close 가 매분 갱신하는 컬럼 — STEP2 가 덮어쓰면 안 됨
        affected := affected + 1;
      END LOOP;
      RETURN affected;
    END;
    $$;

    REVOKE EXECUTE ON FUNCTION public.intraday_upsert_ohlc(jsonb) FROM PUBLIC;
    REVOKE EXECUTE ON FUNCTION public.intraday_upsert_ohlc(jsonb) FROM anon, authenticated;
    GRANT EXECUTE ON FUNCTION public.intraday_upsert_ohlc(jsonb) TO service_role;

    COMMIT;
    ```
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260514120300_intraday_upsert_ohlc_rpc.sql &amp;&amp; grep -q "CREATE OR REPLACE FUNCTION public.intraday_upsert_ohlc" supabase/migrations/20260514120300_intraday_upsert_ohlc_rpc.sql &amp;&amp; grep -q "open  = EXCLUDED.open" supabase/migrations/20260514120300_intraday_upsert_ohlc_rpc.sql &amp;&amp; grep -q "REVOKE EXECUTE ON FUNCTION public.intraday_upsert_ohlc(jsonb) FROM anon, authenticated" supabase/migrations/20260514120300_intraday_upsert_ohlc_rpc.sql &amp;&amp; grep -q "GRANT EXECUTE ON FUNCTION public.intraday_upsert_ohlc(jsonb) TO service_role" supabase/migrations/20260514120300_intraday_upsert_ohlc_rpc.sql</automated>
  </verify>
  <done>
    - 파일 존재
    - SECURITY DEFINER + LANGUAGE plpgsql
    - ON CONFLICT DO UPDATE: open=EXCLUDED.open, high=EXCLUDED.high, low=EXCLUDED.low (3개 컬럼만)
    - close / volume / trade_amount / change_amount / change_rate 가 ON CONFLICT 의 SET 절에 없음 (의도적 omit)
    - REVOKE FROM PUBLIC + REVOKE FROM anon, authenticated 두 줄
    - GRANT EXECUTE TO service_role
  </done>
</task>

</tasks>

<verification>
- 4개 SQL 파일 모두 존재 (`ls supabase/migrations/20260514*.sql | wc -l` = 4)
- 각 파일이 BEGIN/COMMIT 으로 transaction 래핑
- 모든 RPC + 신규 테이블에 `REVOKE ... anon, authenticated` 명시
- production push 는 본 plan 범위 밖 — Plan 09 [BLOCKING] 에서 처리
</verification>

<success_criteria>
- 4개 SQL 파일 syntax 정확 (BEGIN/COMMIT, CREATE TABLE / FUNCTION 문법)
- REVOKE/GRANT 패턴 MEMORY rule 준수 (PUBLIC + anon/authenticated 둘 다)
- Wave 1 의 워커 코드가 본 plan 의 RPC signature (`intraday_upsert_close(jsonb)`, `intraday_upsert_ohlc(jsonb)`) 와 kiwoom_tokens schema 를 그대로 호출 가능
- kis_tokens DROP 파일은 Wave 0 에 존재하지만 push 는 Wave 4 cutover 마지막 step 에서만
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-intraday-current-price/09.1-01-SUMMARY.md`
</output>
