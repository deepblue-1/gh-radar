---
phase: 09.1-intraday-current-price
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - workers/intraday-sync/package.json
  - workers/intraday-sync/tsconfig.json
  - workers/intraday-sync/vitest.config.ts
  - workers/intraday-sync/Dockerfile
  - workers/intraday-sync/src/config.ts
  - workers/intraday-sync/src/logger.ts
  - workers/intraday-sync/src/retry.ts
  - workers/intraday-sync/src/services/supabase.ts
  - workers/intraday-sync/src/index.ts
  - pnpm-workspace.yaml
autonomous: true
requirements:
  - DATA-02
requirements_addressed:
  - DATA-02

must_haves:
  truths:
    - "workers/intraday-sync/ 워크스페이스가 pnpm-workspace.yaml 의 workers/* 패턴으로 자동 등록되어 `pnpm -F @gh-radar/intraday-sync ...` 호출 가능 — D-13"
    - "package.json 이 candle-sync 와 동일 의존성 (axios/@supabase/supabase-js/pino/dotenv) + devDeps (tsx/typescript/vitest) — 단일 mode 라 MODE 분리 X"
    - "tsconfig.json 이 candle-sync 패턴 그대로 (commonjs/node, extends ../../tsconfig.base.json)"
    - "Dockerfile 이 candle-sync 멀티스테이지 (builder + production) + GIT_SHA build-arg + alpine 22 + pnpm 10"
    - "config.ts 가 SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY/KIWOOM_APPKEY/KIWOOM_SECRETKEY 모두 필수 검증 + KIWOOM_BASE_URL/MIN_EXPECTED_ROWS/PAGINATION_HARD_CAP env 노출 — RESEARCH §9.2"
    - "config.ts 에 MODE 필드 없음 — intraday 단일 mode (candle-sync 의 backfill/daily/recover 분기 없음, D-13)"
    - "logger.ts 가 pino + redact (kiwoomAppkey/kiwoomSecretkey/supabaseServiceRoleKey + headers.authorization + *.token) — RESEARCH §9.3 (T-1 mitigation)"
    - "retry.ts 가 withRetry 3회 exponential backoff (200·400ms) — candle-sync 동일 시그니처"
    - "services/supabase.ts 가 createSupabaseClient(config): SupabaseClient 단일 함수 export"
    - "src/index.ts 는 placeholder — runIntradayCycle entry 골격만 (실제 cycle 구현은 Plan 04)"
    - "pnpm -F @gh-radar/intraday-sync typecheck + test --run + build 3종 모두 PASS"
  artifacts:
    - path: "workers/intraday-sync/package.json"
      provides: "@gh-radar/intraday-sync 워크스페이스 정의"
      contains: "@gh-radar/intraday-sync"
    - path: "workers/intraday-sync/Dockerfile"
      provides: "멀티스테이지 빌드 + GIT_SHA build-arg + alpine 22"
      contains: "FROM node:22-alpine AS builder"
    - path: "workers/intraday-sync/src/config.ts"
      provides: "loadConfig() — env 로딩 + 필수 검증 + 키움 env 노출"
      contains: "kiwoomAppkey"
    - path: "workers/intraday-sync/src/logger.ts"
      provides: "logger pino + 키움 secret redact"
      contains: "kiwoomAppkey"
    - path: "workers/intraday-sync/src/retry.ts"
      provides: "withRetry — 3회 exponential backoff"
      contains: "withRetry"
  key_links:
    - from: "workers/intraday-sync/package.json"
      to: "pnpm-workspace.yaml"
      via: "workers/* 패턴 자동 등록"
      pattern: "workers/\\*"
    - from: "workers/intraday-sync/src/config.ts"
      to: "Plan 03/04 kiwoom/* + pipeline/* + index.ts"
      via: "Config 타입 + loadConfig() import"
      pattern: "loadConfig"
---

<objective>
intraday-sync 워커의 스캐폴드 — candle-sync 의 워크스페이스 구조를 1:1 mirror 하여 신규 `workers/intraday-sync/` 디렉터리 생성. Plan 03 (키움 OAuth + ka10027 페이지네이션) 과 Plan 04 (pipeline + runCycle entry) 가 본 plan 의 산출물 위에 구현된다.

Purpose:
- DATA-02 SC #1 (워커 워크스페이스 1:1 mirror) — D-13 (워크스페이스 mirror)
- D-15 (KIWOOM Secret 2개 env 노출)
- T-1 mitigation (logger.ts 의 secret redact paths 확장)
- RESEARCH §9.1 (워크스페이스 구조), §9.2 (config.ts), §9.3 (logger redact)

Phase-level decisions honored implicitly by 본 plan 의 워크스페이스 구조:
- D-04: ka10027 미반환 ~2,400 종목은 갱신 X — 워커가 "응답 받은 종목만 처리" 패턴으로 자연 충족 (Plan 04 가 구현)
- D-11: EOD candle-sync 17:30 이 final overlay — 본 워커는 close/change/volume 4 컬럼만 갱신 (Plan 04 mapper + Plan 01 RPC SET 절)
- D-17 / D-18 / D-19: 테이블/시간대/API 분리 — 본 워커가 신규 workspace (intraday-sync) + 키움 base URL + Scheduler `* 9-15` 로 KIS ingestion (`stocks`/`stock_quotes`/`top_movers`) 무변경 보장
- R1 mitigation: CONTEXT D-12 의 가설 ("Direct VPC Egress + Static IP, Cloud NAT 대체") 은 D-12 REVISED 로 정정 → Plan 05 의 setup script 가 Cloud NAT 정공법 구현

Mirror 대상 (candle-sync → intraday-sync):
- `workers/candle-sync/package.json` → `workers/intraday-sync/package.json` (이름만 변경)
- `workers/candle-sync/Dockerfile` → `workers/intraday-sync/Dockerfile` (--filter 만 변경)
- `workers/candle-sync/tsconfig.json` → 동일
- `workers/candle-sync/vitest.config.ts` → 동일
- `workers/candle-sync/src/{logger,retry,services/supabase}.ts` → mirror 후 logger redact 확장
- `workers/candle-sync/src/config.ts` → KRX/MODE 환경변수 제거 + 키움 환경변수 추가

변경점 (candle-sync 대비):
- 패키지명: `@gh-radar/candle-sync` → `@gh-radar/intraday-sync`
- config: KRX_AUTH_KEY/MODE/BACKFILL_*/RECOVER_* 제거, KIWOOM_BASE_URL/KIWOOM_APPKEY/KIWOOM_SECRETKEY/MIN_EXPECTED_ROWS/PAGINATION_HARD_CAP 추가
- logger: redact paths 에 키움 secret + Bearer token 추가
- Dockerfile `--filter=@gh-radar/intraday-sync`
- src/index.ts 는 placeholder (Plan 04 의 runIntradayCycle 이 채움)

Output:
- workers/intraday-sync/ 9 파일 + pnpm-workspace.yaml 확인 (workers/* 이미 존재 — 무수정)
- `pnpm install` 성공 + `pnpm -F @gh-radar/intraday-sync typecheck` 성공
- `pnpm -F @gh-radar/intraday-sync test --run` 성공 (zero test 라도)
- `pnpm -F @gh-radar/intraday-sync build` 성공 (dist/index.js 생성)
- docker build 는 본 plan 범위 외 (Plan 06 까지 deferred)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md
@.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md

# Mirror 대상 — candle-sync 패턴 (가장 최근)
@workers/candle-sync/package.json
@workers/candle-sync/tsconfig.json
@workers/candle-sync/vitest.config.ts
@workers/candle-sync/Dockerfile
@workers/candle-sync/src/config.ts
@workers/candle-sync/src/logger.ts
@workers/candle-sync/src/retry.ts
@workers/candle-sync/src/services/supabase.ts

# Workspace 등록 확인
@pnpm-workspace.yaml

<interfaces>
<!-- intraday-sync 의 Config (candle-sync 와 차이 — MODE 없음, KIWOOM env 추가) -->
```typescript
export type Config = {
  // 공통 (candle-sync 동일)
  supabaseUrl: string;
  supabaseServiceRoleKey: string;
  logLevel: string;
  appVersion: string;

  // intraday-sync 신규 (RESEARCH §9.2)
  kiwoomBaseUrl: string;          // KIWOOM_BASE_URL env, default "https://api.kiwoom.com"
  kiwoomAppkey: string;           // KIWOOM_APPKEY env (secret)
  kiwoomSecretkey: string;        // KIWOOM_SECRETKEY env (secret)
  minExpectedRows: number;        // MIN_EXPECTED_ROWS env, default 1500 (CONTEXT D-01 "~1,898" 보수)
  paginationHardCap: number;      // PAGINATION_HARD_CAP env, default 5000 (페이지네이션 무한루프 가드)
};
```

<!-- runIntradayCycle (Plan 04 가 채움) — 본 plan 의 src/index.ts 는 placeholder -->
```typescript
export async function runIntradayCycle(config: Config): Promise<{ inserted: number; duration_ms: number }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: package.json + tsconfig.json + vitest.config.ts 생성 + pnpm install</name>
  <files>
    workers/intraday-sync/package.json,
    workers/intraday-sync/tsconfig.json,
    workers/intraday-sync/vitest.config.ts,
    pnpm-workspace.yaml
  </files>

  <read_first>
    - workers/candle-sync/package.json (mirror 대상 — 의존성 버전 + scripts)
    - workers/candle-sync/tsconfig.json (mirror — extends ../../tsconfig.base.json)
    - workers/candle-sync/vitest.config.ts (mirror — globals + passWithNoTests)
    - pnpm-workspace.yaml (workers/* 패턴 존재 확인 — 이미 있어야 함, Phase 9 등록)
  </read_first>

  <action>
1. **`workers/intraday-sync/package.json`** 생성 (candle-sync 와 동일, 이름만 변경):
```json
{
  "name": "@gh-radar/intraday-sync",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx -r dotenv/config src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@gh-radar/shared": "workspace:*",
    "@supabase/supabase-js": "^2.49.0",
    "axios": "^1.7.0",
    "dotenv": "^16.4.0",
    "pino": "^9.0.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  }
}
```

2. **`workers/intraday-sync/tsconfig.json`** 생성 (candle-sync 와 동일):
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "commonjs",
    "moduleResolution": "node",
    "outDir": "./dist",
    "rootDir": "./src",
    "paths": {}
  },
  "include": ["src"]
}
```

3. **`workers/intraday-sync/vitest.config.ts`** 생성 (candle-sync 와 동일, passWithNoTests:true 추가):
```typescript
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    globals: true,
    passWithNoTests: true,
  },
});
```

4. **pnpm-workspace.yaml** 확인:
- `grep -E "workers/\\*" pnpm-workspace.yaml` 결과 1줄 (이미 등록됨 — Phase 9 시 등록)
- 만약 없으면 (예외 case) `workers/*` 패턴 추가

5. **`pnpm install`** 실행 — node_modules 생성 + 워크스페이스 graph 갱신:
```bash
pnpm install
```
exit 0 + `node_modules/@gh-radar/intraday-sync` symlink 존재 확인.
  </action>

  <verify>
    <automated>test -f workers/intraday-sync/package.json && test -f workers/intraday-sync/tsconfig.json && test -f workers/intraday-sync/vitest.config.ts && grep -q "@gh-radar/intraday-sync" workers/intraday-sync/package.json && grep -q "workers/\*" pnpm-workspace.yaml && pnpm install --frozen-lockfile=false && pnpm -F @gh-radar/intraday-sync exec node -e "console.log('workspace OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `test -f workers/intraday-sync/package.json` exit 0
    - `grep -c '"name": "@gh-radar/intraday-sync"' workers/intraday-sync/package.json` = 1
    - dependencies 에 `@gh-radar/shared`, `@supabase/supabase-js`, `axios`, `dotenv`, `pino` 모두 존재
    - scripts 에 `dev`, `build`, `typecheck`, `test` 모두 존재
    - tsconfig 가 `../../tsconfig.base.json` extends
    - vitest.config.ts 에 `passWithNoTests: true` 매치 (Wave 0 placeholder 보호)
    - `grep -q "workers/\\*" pnpm-workspace.yaml` exit 0 (이미 등록)
    - `pnpm install` exit 0
    - `pnpm -F @gh-radar/intraday-sync exec node -e "console.log(1)"` exit 0 (워크스페이스 인식)
  </acceptance_criteria>

  <done>workspace 등록 + 의존성 설치 + 빈 워크스페이스 인식. src 파일은 Task 2 가 작성.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: src/{config,logger,retry,services/supabase,index}.ts + Dockerfile 생성</name>
  <files>
    workers/intraday-sync/src/config.ts,
    workers/intraday-sync/src/logger.ts,
    workers/intraday-sync/src/retry.ts,
    workers/intraday-sync/src/services/supabase.ts,
    workers/intraday-sync/src/index.ts,
    workers/intraday-sync/Dockerfile
  </files>

  <read_first>
    - workers/candle-sync/src/config.ts (mirror — Config 타입 + loadConfig 구조, parseNumberEnv 패턴)
    - workers/candle-sync/src/logger.ts (mirror — pino + redact paths)
    - workers/candle-sync/src/retry.ts (mirror — withRetry 그대로)
    - workers/candle-sync/src/services/supabase.ts (mirror — createSupabaseClient 그대로)
    - workers/candle-sync/Dockerfile (mirror — 멀티스테이지 + GIT_SHA + --filter 만 변경)
    - workers/candle-sync/src/index.ts (참고용 — 본 plan 의 index.ts 는 placeholder)
    - .planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md §9.2 (config 필드) §9.3 (logger redact)
  </read_first>

  <action>
1. **`workers/intraday-sync/src/config.ts`** — candle-sync 패턴 + 키움 env 교체:
```typescript
export type Config = {
  // 공통 (candle-sync 동일)
  supabaseUrl: string;
  supabaseServiceRoleKey: string;
  logLevel: string;
  appVersion: string;

  // intraday-sync 신규 — 키움 REST API
  kiwoomBaseUrl: string;          // KIWOOM_BASE_URL env, default "https://api.kiwoom.com"
  kiwoomAppkey: string;           // KIWOOM_APPKEY env (Cloud Run --set-secrets)
  kiwoomSecretkey: string;        // KIWOOM_SECRETKEY env (Cloud Run --set-secrets)

  // 가드/페이지네이션
  minExpectedRows: number;        // MIN_EXPECTED_ROWS env, default 1500 — RESEARCH §6 가드
  paginationHardCap: number;      // PAGINATION_HARD_CAP env, default 5000 — 무한루프 안전상한
};

function parseNumberEnv(raw: string | undefined, fallback: number): number {
  if (!raw) return fallback;
  const n = Number(raw);
  if (!Number.isFinite(n)) {
    throw new Error(`Invalid numeric env value: "${raw}"`);
  }
  return n;
}

export function loadConfig(): Config {
  const supabaseUrl = process.env.SUPABASE_URL;
  const supabaseServiceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY;
  const kiwoomAppkey = process.env.KIWOOM_APPKEY;
  const kiwoomSecretkey = process.env.KIWOOM_SECRETKEY;

  if (!supabaseUrl || !supabaseServiceRoleKey) {
    throw new Error("SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY must be set");
  }
  if (!kiwoomAppkey || !kiwoomSecretkey) {
    throw new Error("KIWOOM_APPKEY and KIWOOM_SECRETKEY must be set");
  }

  return {
    supabaseUrl,
    supabaseServiceRoleKey,
    logLevel: process.env.LOG_LEVEL ?? "info",
    appVersion: process.env.APP_VERSION ?? "0.0.0",
    kiwoomBaseUrl: process.env.KIWOOM_BASE_URL ?? "https://api.kiwoom.com",
    kiwoomAppkey,
    kiwoomSecretkey,
    minExpectedRows: parseNumberEnv(process.env.MIN_EXPECTED_ROWS, 1500),
    paginationHardCap: parseNumberEnv(process.env.PAGINATION_HARD_CAP, 5000),
  };
}
```

2. **`workers/intraday-sync/src/logger.ts`** — candle-sync redact paths 확장 (RESEARCH §9.3 — T-1 mitigation):
```typescript
import pino from "pino";

export const logger = pino({
  level: process.env.LOG_LEVEL ?? "info",
  redact: {
    paths: [
      "*.supabaseServiceRoleKey",
      "*.kiwoomAppkey",
      "*.kiwoomSecretkey",
      "headers.authorization",       // Bearer token redact (axios response/request)
      "*.token",                     // OAuth token field redact
    ],
    censor: "[REDACTED]",
  },
});
```

3. **`workers/intraday-sync/src/retry.ts`** — candle-sync 그대로 (동일 시그니처):
```typescript
import { logger } from "./logger";

export async function withRetry<T>(
  fn: () => Promise<T>,
  label: string,
  attempts = 3,
): Promise<T> {
  let lastErr: unknown;
  for (let i = 1; i <= attempts; i++) {
    try {
      return await fn();
    } catch (err) {
      lastErr = err;
      if (i === attempts) break;
      const waitMs = 200 * Math.pow(2, i - 1);
      logger.warn({ label, attempt: i, waitMs, err: (err as Error).message }, "retry");
      await new Promise((r) => setTimeout(r, waitMs));
    }
  }
  throw lastErr;
}
```

4. **`workers/intraday-sync/src/services/supabase.ts`** — candle-sync 그대로:
```typescript
import { createClient, type SupabaseClient } from "@supabase/supabase-js";
import type { Config } from "../config";

export function createSupabaseClient(config: Config): SupabaseClient {
  return createClient(config.supabaseUrl, config.supabaseServiceRoleKey);
}
```

5. **`workers/intraday-sync/src/index.ts`** — placeholder (Plan 04 의 runIntradayCycle 구현 전):
```typescript
import "dotenv/config";
import { loadConfig } from "./config";
import { logger } from "./logger";

// NOTE: runIntradayCycle (Plan 04) 가 실제 cycle 구현.
// 본 placeholder 는 Plan 02 가 typecheck/test/build 통과를 위해 작성.

async function main(): Promise<void> {
  const config = loadConfig();
  const log = logger.child({ app: "intraday-sync", version: config.appVersion });
  log.info("intraday-sync placeholder — Plan 04 에서 runIntradayCycle 구현 예정");
  process.exit(0);
}

// CLI 진입점 (vitest import 시에는 실행 안 함) — candle-sync 패턴
if (process.argv[1] && process.argv[1].endsWith("index.js")) {
  main().catch((err) => {
    logger.error({ err }, "intraday-sync placeholder failed");
    process.exit(1);
  });
}
```

6. **`workers/intraday-sync/Dockerfile`** — candle-sync 와 동일 구조, `--filter=@gh-radar/intraday-sync` 만 다름:
```dockerfile
# === Builder Stage ===
FROM node:22-alpine AS builder
RUN corepack enable && corepack prepare pnpm@10 --activate
WORKDIR /app

COPY package.json pnpm-workspace.yaml pnpm-lock.yaml tsconfig.base.json ./
COPY packages/shared/package.json ./packages/shared/
COPY workers/intraday-sync/package.json ./workers/intraday-sync/

RUN pnpm install --frozen-lockfile

COPY packages/shared/ ./packages/shared/
COPY workers/intraday-sync/ ./workers/intraday-sync/

RUN pnpm -F @gh-radar/shared build
RUN pnpm -F @gh-radar/intraday-sync build

# pnpm deploy: isolated prod node_modules
RUN pnpm --filter=@gh-radar/intraday-sync --prod --legacy deploy /out
RUN cp -r /app/workers/intraday-sync/dist /out/dist

# === Production Image ===
FROM node:22-alpine
WORKDIR /app

RUN addgroup -S app && adduser -S app -G app

COPY --from=builder /out/dist ./dist
COPY --from=builder /out/package.json ./
COPY --from=builder /out/node_modules ./node_modules
COPY --from=builder /app/packages/shared/dist ./node_modules/@gh-radar/shared/dist

ARG GIT_SHA=dev
ENV APP_VERSION=${GIT_SHA}

USER app
CMD ["node", "dist/index.js"]
```

7. 검증:
```bash
pnpm -F @gh-radar/intraday-sync typecheck   # exit 0
pnpm -F @gh-radar/intraday-sync test --run  # exit 0 (passWithNoTests:true 라 zero test OK)
pnpm -F @gh-radar/intraday-sync build       # exit 0 — dist/index.js 생성
```
  </action>

  <verify>
    <automated>test -f workers/intraday-sync/src/config.ts && test -f workers/intraday-sync/src/logger.ts && test -f workers/intraday-sync/src/retry.ts && test -f workers/intraday-sync/src/services/supabase.ts && test -f workers/intraday-sync/src/index.ts && test -f workers/intraday-sync/Dockerfile && grep -q "kiwoomAppkey" workers/intraday-sync/src/config.ts && grep -q "kiwoomSecretkey" workers/intraday-sync/src/config.ts && grep -q "KIWOOM_BASE_URL" workers/intraday-sync/src/config.ts && grep -q "kiwoomAppkey" workers/intraday-sync/src/logger.ts && grep -q "headers.authorization" workers/intraday-sync/src/logger.ts && grep -q "withRetry" workers/intraday-sync/src/retry.ts && grep -q "FROM node:22-alpine AS builder" workers/intraday-sync/Dockerfile && grep -q "@gh-radar/intraday-sync" workers/intraday-sync/Dockerfile && pnpm -F @gh-radar/intraday-sync typecheck && pnpm -F @gh-radar/intraday-sync test --run && pnpm -F @gh-radar/intraday-sync build</automated>
  </verify>

  <acceptance_criteria>
    - `workers/intraday-sync/src/config.ts` 가 `loadConfig()` export
    - config 필드: `supabaseUrl`, `supabaseServiceRoleKey`, `kiwoomBaseUrl`, `kiwoomAppkey`, `kiwoomSecretkey`, `minExpectedRows`, `paginationHardCap`, `logLevel`, `appVersion` 모두 존재
    - config 의 **MODE 필드 없음** (candle-sync 의 mode/backfillFrom/backfillTo/recover* 가 본 워커에 없음 — intraday 단일 mode)
    - config default: `kiwoomBaseUrl="https://api.kiwoom.com"`, `minExpectedRows=1500`, `paginationHardCap=5000`
    - `KIWOOM_APPKEY`/`KIWOOM_SECRETKEY` 미지정 시 명확한 에러 throw
    - `workers/intraday-sync/src/logger.ts` 가 pino + redact paths 에 `*.kiwoomAppkey`, `*.kiwoomSecretkey`, `headers.authorization`, `*.token` 모두 포함 — T-1 mitigation
    - `workers/intraday-sync/src/retry.ts` 가 `export async function withRetry<T>(...)` 시그니처
    - `workers/intraday-sync/src/services/supabase.ts` 가 `createSupabaseClient(config: Config): SupabaseClient` export
    - `workers/intraday-sync/src/index.ts` 가 placeholder — `loadConfig()` 호출 + log + exit 0
    - `workers/intraday-sync/Dockerfile` 멀티스테이지 (`FROM node:22-alpine AS builder` + 두 번째 `FROM node:22-alpine`)
    - Dockerfile 에 `pnpm -F @gh-radar/intraday-sync build` + `--filter=@gh-radar/intraday-sync --prod --legacy deploy /out` 매치
    - `ARG GIT_SHA=dev` + `ENV APP_VERSION=${GIT_SHA}` 매치
    - `pnpm -F @gh-radar/intraday-sync typecheck` exit 0
    - `pnpm -F @gh-radar/intraday-sync test --run` exit 0
    - `pnpm -F @gh-radar/intraday-sync build` exit 0 — `workers/intraday-sync/dist/index.js` 생성
  </acceptance_criteria>

  <done>intraday-sync 워크스페이스 + 인프라 5 파일 + Dockerfile + placeholder entry. Plan 03/04 가 본 산출물 위에 구현.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.env → Config | KIWOOM_APPKEY/SECRETKEY + SUPABASE_SERVICE_ROLE_KEY 시크릿 origin |
| Logger output → Cloud Logging | redact 누락 시 시크릿 평문 노출 |
| Dockerfile build context → Production image | base image 에 시크릿/dev 파일 leak 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-SCAF-01 | INFORMATION DISCLOSURE | logger.ts redact 누락 (T-1) | mitigate | logger.ts redact paths 에 `*.kiwoomAppkey`, `*.kiwoomSecretkey`, `*.supabaseServiceRoleKey`, `headers.authorization`, `*.token` 명시. RESEARCH §9.3 권장 준수. |
| T-09.1-SCAF-02 | TAMPERING (env missing) | KIWOOM_APPKEY/SECRETKEY 미설정 | mitigate | loadConfig() 가 두 env 부재 시 즉시 명확한 에러 throw. Cloud Run Job 시작 시점 fail-fast. |
| T-09.1-SCAF-03 | INFORMATION DISCLOSURE | Dockerfile 빌드 시 시크릿 leak | mitigate | Dockerfile 에 env/secret 정의 없음. .env 는 .dockerignore (candle-sync 패턴 그대로). Cloud Run Job 가 deploy 시점에 secret 마운트 (Plan 05). |
</threat_model>

<verification>
- `workers/intraday-sync/` 디렉터리 + 9 파일 생성
- `pnpm -F @gh-radar/intraday-sync typecheck` PASS
- `pnpm -F @gh-radar/intraday-sync test --run` PASS (zero test 라도)
- `pnpm -F @gh-radar/intraday-sync build` PASS (dist/index.js)
- pnpm-workspace.yaml workers/* 자동 등록 (변경 없음)
- Plan 03 (키움 client) + Plan 04 (pipeline + entry) 가 src/{config,logger,retry,services/supabase}.ts import 가능
</verification>

<success_criteria>
- intraday-sync 워크스페이스가 candle-sync 1:1 mirror 구조로 생성
- typecheck + test + build 3종 모두 PASS
- Dockerfile 멀티스테이지 + GIT_SHA build-arg
- logger redact paths 가 키움 secret + Bearer token 포함 (T-1)
- Plan 03/04 의 src 파일이 본 인프라 4종을 import 가능
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-intraday-current-price/09.1-02-SUMMARY.md`
</output>
</content>
