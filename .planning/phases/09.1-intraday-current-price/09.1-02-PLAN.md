---
phase: 09.1
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/shared/src/kiwoom.ts
  - packages/shared/src/index.ts
  - packages/shared/src/__tests__/kiwoom.test.ts
autonomous: true
requirements: [DATA-02]
requirements_addressed:
  - DATA-02 (KiwoomKa10027Row / KiwoomKa10001Row / IntradayCloseUpdate / IntradayOhlcUpdate 타입 4종 — Wave 1 worker + Wave 2 server 가 import)
must_haves:
  truths:
    - "packages/shared/src/kiwoom.ts 에 4개 타입 export — KiwoomKa10027Row, KiwoomKa10001Row, IntradayCloseUpdate, IntradayOhlcUpdate"
    - "packages/shared 의 index.ts 에 신규 타입들이 re-export 되어 @gh-radar/shared 에서 import 가능"
    - "기존 kis.ts (KisRankingRow / KisTokenResponse) 는 본 plan 에서 변경 없음 — Wave 4 cleanup 까지 유지"
    - "@gh-radar/shared 의 build/typecheck/test 가 green"
  artifacts:
    - path: packages/shared/src/kiwoom.ts
      provides: "4 타입 정의 + 한글 주석"
      min_lines: 50
    - path: packages/shared/src/index.ts
      provides: "kiwoom 타입 re-export"
      contains: "from \"./kiwoom"
    - path: packages/shared/src/__tests__/kiwoom.test.ts
      provides: "타입 컴파일 sanity test (객체 리터럴 타입 검사)"
  key_links:
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/kiwoom.ts"
      via: "export type { ... } from \"./kiwoom.js\""
      pattern: "from \"./kiwoom"
---

<objective>
`packages/shared` 에 키움 응답 raw 타입 (KiwoomKa10027Row, KiwoomKa10001Row) + intraday cycle update 타입 (IntradayCloseUpdate, IntradayOhlcUpdate) 4종을 추가하여 Wave 1 의 worker pipeline 과 Wave 2 의 server kiwoom client 가 동일 타입을 공유한다.

Purpose: type contracts 를 Wave 0 에 정의하여 downstream wave 가 codebase 탐색 없이 직접 import 가능하게 한다. 기존 `kis.ts` 는 본 plan 에서 건드리지 않고 Wave 4 cleanup 단계에서 제거.

Output: `kiwoom.ts` 1개 신규 + `index.ts` 갱신 + 컴파일 sanity 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md
@.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md
@packages/shared/src/index.ts
@packages/shared/src/kis.ts
@packages/shared/src/stock.ts

<read_first>
- `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §2.1 (4 타입 전문)
- `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-09 (signed price 부호 prefix), D-10 (_AL strip), D-15 (mac 단위 가설), D-23 (trade_amount 근사값)
- `packages/shared/src/index.ts` (re-export 패턴 — `.js` 확장자 사용)
- `packages/shared/src/kis.ts` (기존 KIS 타입 — 변경 안 함)
- `packages/shared/src/stock.ts` (StockDailyOhlcv 참조 타입)
</read_first>

<interfaces>
<!-- packages/shared/src/index.ts 현재 export 패턴 -->
```typescript
export type { Stock, Market, SecurityType, StockMaster, StockQuote, StockWithQuote, BdydTrdRow, StockDailyOhlcv } from "./stock.js";
export type { KisRankingRow, KisTokenResponse } from "./kis.js";
// ... (기타 export)
```

<!-- 본 plan 추가 라인: -->
```typescript
export type { KiwoomKa10027Row, KiwoomKa10001Row, IntradayCloseUpdate, IntradayOhlcUpdate } from "./kiwoom.js";
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 키움 API → mapper | 응답 raw 타입은 모두 string. mapper 가 number 변환 + 검증 (Wave 1 Plan 04 책임). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-05 | Tampering | KiwoomKa10001Row 의 mac 단위 가설 (R2) | accept | 본 plan 은 type 만 정의 (단위 책임 X). Wave 1 Plan 04 의 fixture 캡처 task 가 단위 확정 + parseMac 곱셈 상수 조정. type 변경 불필요. |
| T-09.1-06 | Configuration | shared package 빌드/번들 누락 | mitigate | `pnpm -F @gh-radar/shared build` 검증 + `pnpm -F @gh-radar/shared typecheck` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: kiwoom.ts 신규 작성 — 4개 타입 정의</name>
  <files>packages/shared/src/kiwoom.ts</files>
  <read_first>
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §2.1 (KiwoomKa10027Row, KiwoomKa10001Row, IntradayCloseUpdate, IntradayOhlcUpdate 전문)
    - `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-09 (cur_prc/pred_pre/flu_rt 부호 prefix), D-10 (stk_cd 의 _AL 접미사)
  </read_first>
  <action>
    `packages/shared/src/kiwoom.ts` 신규 생성. RESEARCH §2.1 기준 4 타입 + 한글 주석 (필드별 부호/단위/매핑 대상 명시):

    ```typescript
    // packages/shared/src/kiwoom.ts
    //
    // 키움 REST API 응답 raw 타입 + intraday-sync cycle update 타입.
    // Phase 09.1 (DATA-02) — Wave 0 Plan 02.
    //
    // RESEARCH §2.1 기준. mac 단위 가설 = 억원 (Plan 04 fixture 캡처로 확정 — R2).

    // ─────────────────────────────────────────────────────────────
    // STEP 1 — ka10027 (등락률 순위) raw row
    //
    // 응답 패턴 (사용자 실측 2026-05-13):
    //   - cur_prc: signed string "+6760" / "-274250" — 첫 글자가 부호 (절댓값 사용)
    //   - pred_pre, flu_rt: signed string (부호 유지하여 DB 저장)
    //   - stk_cd: 6자 단축코드 + "_AL" suffix (예: "007460_AL") — mapper 에서 strip
    //   - now_trde_qty: 누적 거래량 가설 (R1, Plan 04 fixture 로 확정)
    // ─────────────────────────────────────────────────────────────
    export type KiwoomKa10027Row = {
      stk_cd: string;           // "007460_AL" — strip 후 6자
      stk_nm?: string;          // 종목명 (참고용 — stocks 마스터에 존재)
      cur_prc: string;          // "+6760" / "-274250" — signed (절댓값 사용)
      pred_pre?: string;        // "+100" / "-100" — signed (change_amount, 부호 유지)
      flu_rt?: string;          // "+1.50" / "-1.50" — signed (change_rate, 부호 유지)
      now_trde_qty?: string;    // "1234567" — 누적 거래량 가설 (R1)
      pred_pre_sig?: string;    // 전일대비 sign code
      sel_req?: string;
      buy_req?: string;
      cntr_str?: string;
      cnt?: string;
    };

    // ─────────────────────────────────────────────────────────────
    // STEP 2 — ka10001 (주식기본정보) raw row
    //
    // 키움 ka10001 응답은 45 필드 (KiwoomRestApi.Net wrapper 참조).
    // 본 phase 가 사용하는 7개 필드만 명시 (나머지 38개는 [key:string] 인덱서 미사용 — 명시적 타입 우선).
    //
    // mac 단위 가설: 억원 (Plan 04 fixture 캡처로 확정 — R2).
    //   잘못된 단위 시 mapper 의 parseMac 곱셈 상수 1줄 변경으로 해결.
    // ─────────────────────────────────────────────────────────────
    export type KiwoomKa10001Row = {
      stk_cd: string;           // 6자 단축코드 (suffix 없음, STEP1 strip 결과)
      stk_nm?: string;          // 참고용
      cur_prc: string;          // signed (검증 가드 용 — STEP1 cur_prc 와 일치해야)
      pred_pre?: string;        // signed (server inquirePrice 폴백 용)
      flu_rt?: string;          // signed (server inquirePrice 폴백 용)
      open_pric: string;        // signed (절댓값) — 오늘자 시가
      high_pric: string;        // signed (절댓값) — 오늘자 고가
      low_pric: string;         // signed (절댓값) — 오늘자 저가
      upl_pric?: string;        // 상한가 (보통 무부호)
      lst_pric?: string;        // 하한가
      mac?: string;             // 시가총액 — 가설 단위 = 억원 (R2, Plan 04 확정)
      // 나머지 38개 필드 (per_rt, eps, bps, trde_qty 등): 본 phase 미사용
      // R3 (Plan 04 fixture 캡처): 누적 거래량/거래대금 동등 필드 존재 시 본 타입 확장
    };

    // ─────────────────────────────────────────────────────────────
    // STEP 1 출력 — pipeline mapper 가 ka10027 row 를 변환한 결과.
    // intraday_upsert_close RPC + stock_quotes 양쪽에 입력으로 전달.
    //
    // D-23: tradeAmount = volume × price 근사값 (트레이딩 시그널 용도).
    // ─────────────────────────────────────────────────────────────
    export type IntradayCloseUpdate = {
      code: string;                       // 6자 (stk_cd 의 _AL strip 결과)
      date: string;                       // ISO YYYY-MM-DD (KST today)
      name?: string;                      // stock_quotes UPSERT 시 사용 (D-20)
      market?: "KOSPI" | "KOSDAQ";        // stocks 마스터에서 join
      price: number;                      // close = cur_prc absolute value
      changeAmount: number | null;
      changeRate: number | null;
      volume: number;                     // now_trde_qty
      tradeAmount: number;                // volume × price 근사값 (D-23)
    };

    // ─────────────────────────────────────────────────────────────
    // STEP 2 출력 — pipeline mapper 가 ka10001 row 를 변환한 결과.
    // intraday_upsert_ohlc RPC + stock_quotes (hot set 만) 양쪽 입력.
    //
    // D-14: hot set 외 ~1,700 종목은 STEP1 의 임시값 (open=high=low=close) 유지.
    //       EOD 17:30 candle-sync 가 모든 OHLCV 컬럼 공식값 overlay.
    // ─────────────────────────────────────────────────────────────
    export type IntradayOhlcUpdate = {
      code: string;                       // 6자
      date: string;                       // ISO YYYY-MM-DD (KST today)
      open: number;                       // open_pric absolute
      high: number;                       // high_pric absolute
      low: number;                        // low_pric absolute
      upperLimit: number | null;          // upl_pric (가능하면 절댓값)
      lowerLimit: number | null;          // lst_pric
      marketCap: number | null;           // mac × 10^8 (단위 가설 — R2 확정 후 조정)
    };
    ```

    주의:
    - `[key:string]: string` 인덱서 사용 안 함 (kis.ts 와 의도적 차이 — 명시 필드 + 타입 안전성 우선)
    - 모든 옵셔널 필드 (`?:`) 는 키움 응답에서 누락 가능성을 의미
    - cur_prc, open_pric, high_pric, low_pric 는 필수 (mapper 가 throw 할 정당한 사유 보유)
  </action>
  <verify>
    <automated>test -f packages/shared/src/kiwoom.ts &amp;&amp; grep -q "export type KiwoomKa10027Row" packages/shared/src/kiwoom.ts &amp;&amp; grep -q "export type KiwoomKa10001Row" packages/shared/src/kiwoom.ts &amp;&amp; grep -q "export type IntradayCloseUpdate" packages/shared/src/kiwoom.ts &amp;&amp; grep -q "export type IntradayOhlcUpdate" packages/shared/src/kiwoom.ts</automated>
  </verify>
  <done>
    - 파일 존재 + 4개 type export
    - 각 타입에 한글 주석 (필드 의미 명시)
    - mac, now_trde_qty 의 가설 (R1/R2) 주석으로 명시
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: index.ts 갱신 + kiwoom 타입 sanity test</name>
  <files>packages/shared/src/index.ts, packages/shared/src/__tests__/kiwoom.test.ts</files>
  <read_first>
    - `packages/shared/src/index.ts` (현재 export 패턴 — `.js` 확장자 명시)
    - `packages/shared/src/__tests__/` (기존 테스트 디렉터리 패턴 확인)
  </read_first>
  <behavior>
    - Test 1: `KiwoomKa10027Row` 객체 리터럴이 컴파일 — 필수 필드 (stk_cd, cur_prc) 만 채운 객체 + 모든 필드 채운 객체 둘 다 통과
    - Test 2: `KiwoomKa10001Row` 객체 리터럴이 컴파일 — open_pric/high_pric/low_pric 필수
    - Test 3: `IntradayCloseUpdate` 의 changeAmount, changeRate 가 number | null 허용
    - Test 4: `IntradayOhlcUpdate` 의 upperLimit, lowerLimit, marketCap 이 number | null 허용
  </behavior>
  <action>
    Step 1: `packages/shared/src/index.ts` 갱신 — 기존 export 라인 다음에 한 줄 추가:

    ```typescript
    export type { KiwoomKa10027Row, KiwoomKa10001Row, IntradayCloseUpdate, IntradayOhlcUpdate } from "./kiwoom.js";
    ```

    Step 2: `packages/shared/src/__tests__/kiwoom.test.ts` 신규 작성 — 타입 컴파일 sanity (런타임 assertion 없이 type check 만):

    ```typescript
    import { describe, it, expectTypeOf } from "vitest";
    import type {
      KiwoomKa10027Row,
      KiwoomKa10001Row,
      IntradayCloseUpdate,
      IntradayOhlcUpdate,
    } from "../kiwoom.js";

    describe("kiwoom shared types", () => {
      it("KiwoomKa10027Row 필수 필드만 채워서 컴파일", () => {
        const row: KiwoomKa10027Row = {
          stk_cd: "007460_AL",
          cur_prc: "+6760",
        };
        expectTypeOf(row).toEqualTypeOf<KiwoomKa10027Row>();
      });

      it("KiwoomKa10001Row 필수 필드 (stk_cd + cur_prc + open/high/low_pric)", () => {
        const row: KiwoomKa10001Row = {
          stk_cd: "005930",
          cur_prc: "+70500",
          open_pric: "+70000",
          high_pric: "+71000",
          low_pric: "+69500",
        };
        expectTypeOf(row).toEqualTypeOf<KiwoomKa10001Row>();
      });

      it("IntradayCloseUpdate.changeAmount/changeRate 가 null 허용", () => {
        const u: IntradayCloseUpdate = {
          code: "005930",
          date: "2026-05-14",
          price: 70500,
          changeAmount: null,
          changeRate: null,
          volume: 0,
          tradeAmount: 0,
        };
        expectTypeOf(u.changeAmount).toEqualTypeOf<number | null>();
        expectTypeOf(u.changeRate).toEqualTypeOf<number | null>();
      });

      it("IntradayOhlcUpdate.upperLimit/lowerLimit/marketCap 가 null 허용", () => {
        const u: IntradayOhlcUpdate = {
          code: "005930",
          date: "2026-05-14",
          open: 70000,
          high: 71000,
          low: 69500,
          upperLimit: null,
          lowerLimit: null,
          marketCap: null,
        };
        expectTypeOf(u.upperLimit).toEqualTypeOf<number | null>();
        expectTypeOf(u.lowerLimit).toEqualTypeOf<number | null>();
        expectTypeOf(u.marketCap).toEqualTypeOf<number | null>();
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar &amp;&amp; pnpm -F @gh-radar/shared typecheck &amp;&amp; pnpm -F @gh-radar/shared test --run &amp;&amp; pnpm -F @gh-radar/shared build</automated>
  </verify>
  <done>
    - `index.ts` 에 kiwoom 4 타입 re-export 라인 추가
    - 4개 타입 sanity test 작성 + green
    - `pnpm -F @gh-radar/shared typecheck` exit 0
    - `pnpm -F @gh-radar/shared build` exit 0 (dist/index.js + dist/index.d.ts 갱신)
    - Wave 1 의 worker / Wave 2 의 server 가 `import type { ... } from "@gh-radar/shared"` 로 4 타입 모두 import 가능
  </done>
</task>

</tasks>

<verification>
- `pnpm -F @gh-radar/shared typecheck && pnpm -F @gh-radar/shared test --run && pnpm -F @gh-radar/shared build` exit 0
- `grep -r "KiwoomKa10027Row\|KiwoomKa10001Row\|IntradayCloseUpdate\|IntradayOhlcUpdate" packages/shared/dist/ | head -5` — dist 에 타입 빌드 확인
- 기존 kis.ts export 무변경 (Wave 4 cleanup 까지)
</verification>

<success_criteria>
- packages/shared 의 build/typecheck/test green
- Wave 1 의 worker pipeline 이 `import type { KiwoomKa10027Row, IntradayCloseUpdate } from "@gh-radar/shared"` 가능
- Wave 2 의 server kiwoom client 가 `import type { KiwoomKa10001Row } from "@gh-radar/shared"` 가능
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-intraday-current-price/09.1-02-SUMMARY.md`
</output>
