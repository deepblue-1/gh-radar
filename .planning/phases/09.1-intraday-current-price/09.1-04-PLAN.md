---
phase: 09.1
plan: 04
type: execute
wave: 1
depends_on: [02, 03]
files_modified:
  - workers/intraday-sync/src/kiwoom/client.ts
  - workers/intraday-sync/src/kiwoom/rateLimiter.ts
  - workers/intraday-sync/src/kiwoom/tokenStore.ts
  - workers/intraday-sync/src/kiwoom/fetchRanking.ts
  - workers/intraday-sync/src/pipeline/map.ts
  - workers/intraday-sync/tests/fixtures/ka10027-page1.json
  - workers/intraday-sync/tests/fixtures/oauth-token.json
  - workers/intraday-sync/tests/parseSigned.test.ts
  - workers/intraday-sync/tests/map.test.ts
  - workers/intraday-sync/tests/tokenStore.test.ts
  - workers/intraday-sync/tests/fetchRanking.test.ts
autonomous: true
requirements: [DATA-02]
requirements_addressed:
  - DATA-02 (STEP1 — 키움 OAuth client + Supabase kiwoom_tokens cache + ka10027 페이지네이션 + parseSignedPrice/parseOptionalSignedNumber + _AL strip + ka10027Row→IntradayCloseUpdate mapper)
must_haves:
  truths:
    - "키움 OAuth2 token 발급 + Supabase kiwoom_tokens cache (만료 5분 전 refresh, race idempotent)"
    - "ka10027 페이지네이션 loop — cont-yn=Y 동안 next-key 헤더로 추적, hard cap 5000 초과 시 throw, 401/429 분류 throw"
    - "parseSignedPrice 가 +/- prefix 분리 + 절댓값 + direction 반환, 빈 문자열/NaN throw"
    - "stripAlSuffix 가 \"007460_AL\" → \"007460\" 변환 + 6자 검증"
    - "ka10027RowToCloseUpdate 가 1 row → IntradayCloseUpdate 매핑 (tradeAmount = volume × price 근사값)"
    - "token-bucket rate limiter 가 24 req/s 제어 (STEP2 의 ka10001 호출용)"
  artifacts:
    - path: workers/intraday-sync/src/kiwoom/client.ts
      provides: "createKiwoomClient(baseUrl) — axios baseURL + timeout 10s"
    - path: workers/intraday-sync/src/kiwoom/rateLimiter.ts
      provides: "acquireKiwoomRateToken() — 24 req/s token bucket (configurable)"
    - path: workers/intraday-sync/src/kiwoom/tokenStore.ts
      provides: "getKiwoomToken(supabase, config) — Supabase kiwoom_tokens 캐시 + 만료 5분 전 refresh"
      exports: ["getKiwoomToken", "parseKiwoomExpiresDt"]
    - path: workers/intraday-sync/src/kiwoom/fetchRanking.ts
      provides: "fetchKa10027(client, token) — 페이지네이션 loop + 응답 헤더 cont-yn/next-key"
      exports: ["fetchKa10027"]
    - path: workers/intraday-sync/src/pipeline/map.ts
      provides: "parseSignedPrice / parseOptionalSignedNumber / parseOptionalNumber / stripAlSuffix / ka10027RowToCloseUpdate"
  key_links:
    - from: "fetchKa10027"
      to: "Supabase kiwoom_tokens"
      via: "getKiwoomToken(supabase, config) 으로 토큰 가져온 뒤 client 호출"
      pattern: "getKiwoomToken"
    - from: "ka10027RowToCloseUpdate"
      to: "@gh-radar/shared IntradayCloseUpdate"
      via: "return type IntradayCloseUpdate, tradeAmount = volume × price 근사값"
      pattern: "tradeAmount"
---

<objective>
STEP1 의 키움 API client + ka10027 페이지네이션 + 토큰 캐시 + signed-prefix mapper 를 구현하고, Plan 05 (STEP2 hot set) 와 Plan 06 (pipeline 통합) 가 의존할 수 있도록 fixture + 단위 테스트로 RED→GREEN 검증한다.

Purpose: STEP1 의 "1,898 종목 × close/change/volume" 확보. 키움 OAuth2 OAuth2 토큰을 Supabase `kiwoom_tokens` 에 캐시 (worker + server 공유). 부호 prefix 와 `_AL` 접미사를 정확히 mapper 에서 처리하여 downstream pipeline 이 깨끗한 데이터만 받게 한다.

Output: 5 src + 2 fixture + 4 test. 모든 unit test green. fixture 는 사용자 실측 (2026-05-13) 기반 placeholder — Plan 06 의 production smoke 에서 실데이터로 교체.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md
@.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md
@.planning/phases/09.1-intraday-current-price/09.1-01-PLAN.md
@.planning/phases/09.1-intraday-current-price/09.1-02-PLAN.md
@.planning/phases/09.1-intraday-current-price/09.1-03-PLAN.md
@workers/ingestion/src/kis/tokenStore.ts
@workers/ingestion/src/kis/client.ts
@workers/ingestion/src/kis/rateLimiter.ts

<read_first>
- `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §1.1~1.4 (ka10027 endpoint + request body + 응답 + 부호 prefix), §1.7 (rate limit), §1.9 (_AL suffix), §2.2 (mapper), §5.3 (token store 패턴), Pattern 1 (페이지네이션 코드 전문)
- `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-06~D-10, D-26~D-27
- `workers/ingestion/src/kis/tokenStore.ts` (kis_tokens cache 패턴 — table 이름만 교체)
- `workers/ingestion/src/kis/client.ts` (axios + timeout 10s 패턴)
- `workers/ingestion/src/kis/rateLimiter.ts` (token bucket 패턴)
</read_first>

<interfaces>
<!-- Plan 02 가 만든 타입 (packages/shared/src/kiwoom.ts) -->
```typescript
export type KiwoomKa10027Row = {
  stk_cd: string;
  stk_nm?: string;
  cur_prc: string;
  pred_pre?: string;
  flu_rt?: string;
  now_trde_qty?: string;
  // ...
};

export type IntradayCloseUpdate = {
  code: string;
  date: string;
  name?: string;
  market?: "KOSPI" | "KOSDAQ";
  price: number;
  changeAmount: number | null;
  changeRate: number | null;
  volume: number;
  tradeAmount: number;
};
```

<!-- Plan 03 가 만든 config 의 사용 가능 필드 -->
```typescript
config.kiwoomBaseUrl       // "https://api.kiwoom.com"
config.kiwoomAppkey
config.kiwoomSecretkey
config.kiwoomTokenType     // "live"
config.paginationHardCap   // 5000
config.ka10001RateLimitPerSec  // 24
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process → Kiwoom API | Bearer token via headers.authorization. token-bucket rate limiter. |
| Process → Supabase kiwoom_tokens | service-role 권한, RLS bypass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-10 | Information Disclosure | access_token 로깅 | mitigate | Plan 03 의 logger redact paths 가 `*.access_token, *.accessToken, *.token, headers.authorization` 처리. fetchKa10027 호출 시 token 을 객체 필드로 전달 시 redact 적용. |
| T-09.1-11 | DoS | 키움 429 rate-limit IP block | mitigate | rateLimiter.ts 의 token-bucket 24 req/s + withRetry exponential backoff. 429 응답 시 fetchRanking.ts throw "키움 429 — rate limit" (분류된 에러로 caller 가 retry decision). |
| T-09.1-12 | Tampering | 키움 401 (만료 토큰) silent retry | mitigate | fetchKa10027 가 401 응답 시 throw "키움 401 — token/credential 실패". tokenStore.getKiwoomToken 의 만료 5분 전 refresh 가 정상 동작 가정 — 401 시는 systemic 문제로 즉시 throw. |
| T-09.1-13 | Tampering | race condition — worker/server 동시 token refresh | accept | RESEARCH §5.4 — Supabase upsert idempotent (마지막 INSERT 승). 양쪽 모두 정상 토큰 보유. 별도 lock 불필요. |
| T-09.1-14 | Availability | 페이지네이션 무한 loop | mitigate | fetchKa10027 의 hard cap 5000 + cont-yn=Y AND next-key 가 둘 다 있어야 진행. cap 초과 시 throw. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseSignedPrice + stripAlSuffix + parseOptionalSignedNumber + parseOptionalNumber + 단위 테스트 (RED→GREEN)</name>
  <files>workers/intraday-sync/src/pipeline/map.ts, workers/intraday-sync/tests/parseSigned.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §2.2 (parseSignedPrice 코드), §1.4 (부호 prefix 의미), §1.9 (_AL strip)
    - `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-09, D-10
  </read_first>
  <behavior>
    - `parseSignedPrice("+6760")` → `{ value: 6760, direction: "up" }`
    - `parseSignedPrice("-274250")` → `{ value: 274250, direction: "down" }`
    - `parseSignedPrice("6760")` → `{ value: 6760, direction: "flat" }` (부호 없음)
    - `parseSignedPrice("0")` → `{ value: 0, direction: "flat" }` (보합)
    - `parseSignedPrice("+1,234,567")` → `{ value: 1234567, direction: "up" }` (천단위 콤마)
    - `parseSignedPrice("")` → throws "missing signed price"
    - `parseSignedPrice(undefined)` → throws "missing signed price"
    - `parseSignedPrice("abc")` → throws (invalid)
    - `parseOptionalSignedNumber("+1.50")` → 1.5
    - `parseOptionalSignedNumber("-1.50")` → -1.5
    - `parseOptionalSignedNumber("")` → null
    - `parseOptionalSignedNumber(undefined)` → null
    - `parseOptionalNumber("1234567")` → 1234567
    - `parseOptionalNumber("")` → null
    - `stripAlSuffix("007460_AL")` → "007460"
    - `stripAlSuffix("005930")` → "005930" (no suffix → 그대로)
  </behavior>
  <action>
    Step 1: `workers/intraday-sync/tests/parseSigned.test.ts` 작성 (위 behavior 의 expectations 를 vitest `describe + it` 으로). 먼저 작성하여 RED 확인 (map.ts 미존재).

    Step 2: `workers/intraday-sync/src/pipeline/map.ts` 작성 — RESEARCH §2.2 의 4 함수 + stripAlSuffix:

    ```typescript
    // workers/intraday-sync/src/pipeline/map.ts
    //
    // STEP 1 mapper — ka10027 응답 row 를 IntradayCloseUpdate 로 변환.
    // RESEARCH §2.2 기준. CONTEXT D-09 (부호 prefix), D-10 (_AL strip), D-23 (tradeAmount 근사값).

    import type { KiwoomKa10027Row, IntradayCloseUpdate } from "@gh-radar/shared";

    export function parseSignedPrice(s: string | undefined): { value: number; direction: "up" | "down" | "flat" } {
      if (!s) throw new Error("missing signed price");
      const trimmed = s.trim();
      if (trimmed === "") throw new Error("missing signed price");
      const sign = trimmed[0];
      const rest = (sign === "+" || sign === "-") ? trimmed.slice(1) : trimmed;
      const abs = Number(rest.replace(/,/g, ""));
      if (!Number.isFinite(abs)) throw new Error(`invalid signed price: "${s}"`);
      return {
        value: abs,
        direction: sign === "+" ? "up" : sign === "-" ? "down" : "flat",
      };
    }

    export function parseOptionalSignedNumber(s: string | undefined): number | null {
      if (!s || s.trim() === "") return null;
      const n = Number(s.replace(/,/g, ""));
      return Number.isFinite(n) ? n : null;
    }

    export function parseOptionalNumber(s: string | undefined): number | null {
      if (!s || s.trim() === "") return null;
      const n = Number(s.replace(/,/g, ""));
      return Number.isFinite(n) ? n : null;
    }

    export function stripAlSuffix(stkCd: string): string {
      return stkCd.replace(/_AL$/, "");
    }

    // ka10027 1 row → IntradayCloseUpdate
    // D-23: tradeAmount = volume × price 근사값 (트레이딩 시그널 용도, 회계 아님).
    // 기존 workers/ingestion/src/pipeline/map.ts:5 의 "근사값은 허용하지 않음" 정책 반전 —
    // ka10027/ka10001 응답에 KIS acml_tr_pbmn 동등 필드 부재로 근사값 채택.
    export function ka10027RowToCloseUpdate(
      row: KiwoomKa10027Row,
      dateIso: string,
    ): IntradayCloseUpdate {
      const code = stripAlSuffix(row.stk_cd);
      if (!/^\d{6}$/.test(code)) {
        throw new Error(`Invalid stk_cd after strip: "${row.stk_cd}" → "${code}"`);
      }

      const price = parseSignedPrice(row.cur_prc).value;
      const changeAmount = parseOptionalSignedNumber(row.pred_pre);
      const changeRate = parseOptionalSignedNumber(row.flu_rt);
      const volume = parseOptionalNumber(row.now_trde_qty) ?? 0;

      // D-23: tradeAmount 근사값 (volume × close)
      const tradeAmount = Math.round(volume * price);

      return {
        code,
        date: dateIso,
        name: row.stk_nm,
        price,
        changeAmount,
        changeRate,
        volume,
        tradeAmount,
      };
    }
    ```

    Step 3: `parseSigned.test.ts` 의 4 함수 + ka10027RowToCloseUpdate 통합 테스트 모두 GREEN 확인.

    Step 4: `tests/map.test.ts` 신규 — ka10027RowToCloseUpdate 별도 파일:

    ```typescript
    import { describe, it, expect } from "vitest";
    import { ka10027RowToCloseUpdate } from "../src/pipeline/map";

    describe("ka10027RowToCloseUpdate", () => {
      it("정상 row + tradeAmount = volume × price 근사값", () => {
        const row = {
          stk_cd: "007460_AL",
          stk_nm: "에이프로젠",
          cur_prc: "+6760",
          pred_pre: "+100",
          flu_rt: "+1.50",
          now_trde_qty: "1234567",
        };
        const result = ka10027RowToCloseUpdate(row, "2026-05-14");
        expect(result.code).toBe("007460");
        expect(result.price).toBe(6760);
        expect(result.changeAmount).toBe(100);
        expect(result.changeRate).toBe(1.5);
        expect(result.volume).toBe(1234567);
        expect(result.tradeAmount).toBe(Math.round(1234567 * 6760));
        expect(result.name).toBe("에이프로젠");
      });

      it("음수 변동 (하락 종목)", () => {
        const row = {
          stk_cd: "005930_AL",
          cur_prc: "-274250",
          pred_pre: "-5750",
          flu_rt: "-2.05",
          now_trde_qty: "100000",
        };
        const result = ka10027RowToCloseUpdate(row, "2026-05-14");
        expect(result.price).toBe(274250); // 절댓값
        expect(result.changeAmount).toBe(-5750); // 부호 유지
        expect(result.changeRate).toBe(-2.05);
      });

      it("now_trde_qty 누락 → volume=0, tradeAmount=0", () => {
        const row = { stk_cd: "005930_AL", cur_prc: "+70500" };
        const result = ka10027RowToCloseUpdate(row, "2026-05-14");
        expect(result.volume).toBe(0);
        expect(result.tradeAmount).toBe(0);
      });

      it("stk_cd 가 6자 아니면 throw", () => {
        expect(() => ka10027RowToCloseUpdate({ stk_cd: "12345_AL", cur_prc: "+100" }, "2026-05-14")).toThrow(/Invalid stk_cd/);
        expect(() => ka10027RowToCloseUpdate({ stk_cd: "ABCDEF_AL", cur_prc: "+100" }, "2026-05-14")).toThrow(/Invalid stk_cd/);
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar &amp;&amp; pnpm -F @gh-radar/intraday-sync test -- parseSigned.test.ts map.test.ts</automated>
  </verify>
  <done>
    - parseSignedPrice + 3 보조 함수 + stripAlSuffix + ka10027RowToCloseUpdate 5 export
    - 양 테스트 파일 green
    - 음수/양수/0/빈문자열/콤마/잘못된 코드 모든 케이스 커버
    - tradeAmount = Math.round(volume * price) 명시
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: client.ts + rateLimiter.ts + tokenStore.ts + tokenStore 단위 테스트</name>
  <files>workers/intraday-sync/src/kiwoom/client.ts, workers/intraday-sync/src/kiwoom/rateLimiter.ts, workers/intraday-sync/src/kiwoom/tokenStore.ts, workers/intraday-sync/tests/tokenStore.test.ts, workers/intraday-sync/tests/fixtures/oauth-token.json</files>
  <read_first>
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §5.3 (tokenStore 전문), §5.4 (race idempotent), §1.5.1 (token endpoint), Pattern 2 (rate limiter)
    - `workers/ingestion/src/kis/tokenStore.ts` (kis_tokens 패턴)
    - `workers/ingestion/src/kis/rateLimiter.ts` (token bucket 패턴)
    - `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-26, D-27
  </read_first>
  <behavior>
    - `acquireKiwoomRateToken` 호출 24회 연속 → 1초 안에 모두 resolve
    - 25번째 호출은 ~40ms 대기 (refill 시간)
    - `getKiwoomToken` — 캐시에 만료 5분+ 남은 token 있으면 axios 미호출, 캐시값 반환
    - `getKiwoomToken` — 캐시 만료 임박 시 axios POST `/oauth2/token` 호출 + upsert
    - `getKiwoomToken` — 캐시 부재 시 axios 호출
    - `parseKiwoomExpiresDt("20260513093013")` → `Date` (KST → UTC 변환)
    - `parseKiwoomExpiresDt("invalid")` → throws
  </behavior>
  <action>
    Step 1: `tests/fixtures/oauth-token.json` 신규 (RESEARCH §1.1 응답 패턴):
    ```json
    {
      "token": "FAKE_TOKEN_FOR_TEST_abc123",
      "token_type": "bearer",
      "expires_dt": "20260515093013",
      "return_code": 0,
      "return_msg": "정상적으로 처리되었습니다"
    }
    ```

    Step 2: `src/kiwoom/client.ts` 신규:
    ```typescript
    import axios, { type AxiosInstance } from "axios";

    /**
     * 키움 REST API axios client.
     * RESEARCH §1.1 — baseURL = api.kiwoom.com (mock 은 mockapi.kiwoom.com).
     *
     * 각 호출 시 caller 가 headers (authorization Bearer, api-id, cont-yn, next-key) 를 지정.
     * client 자체에는 Bearer 미주입 — token 이 매 호출 동적이고 expires 5분 전 refresh.
     */
    export function createKiwoomClient(baseUrl: string): AxiosInstance {
      return axios.create({
        baseURL: baseUrl,
        timeout: 10_000,
        headers: { "content-type": "application/json;charset=utf-8" },
      });
    }
    ```

    Step 3: `src/kiwoom/rateLimiter.ts` 신규 (RESEARCH §13.2 + Pattern 2 mirror, configurable rate):
    ```typescript
    /**
     * 키움 ka10001 token-bucket rate limiter.
     * RESEARCH §1.7 — 24 req/s (사용자 실측 2026-05-13).
     * worker (STEP2 hot set) + server (on-demand) 가 동일 Static IP 공유 시
     * IP-단위 통합 bucket 가설 — 양쪽 24 req/s 적용 (보수적 overprovision).
     */

    const BUCKET_CAPACITY_DEFAULT = 24;
    const REFILL_RATE_PER_SEC_DEFAULT = 24;

    type Bucket = { available: number; lastRefill: number; capacity: number; refillRate: number };

    const bucket: Bucket = {
      available: BUCKET_CAPACITY_DEFAULT,
      lastRefill: Date.now(),
      capacity: BUCKET_CAPACITY_DEFAULT,
      refillRate: REFILL_RATE_PER_SEC_DEFAULT,
    };

    export function configureKiwoomRateLimiter(opts: { capacity: number; refillRatePerSec: number }): void {
      bucket.capacity = opts.capacity;
      bucket.refillRate = opts.refillRatePerSec;
      bucket.available = Math.min(bucket.available, opts.capacity);
    }

    export async function acquireKiwoomRateToken(): Promise<void> {
      return new Promise((resolve) => {
        const tick = () => {
          const now = Date.now();
          const elapsedSec = (now - bucket.lastRefill) / 1000;
          bucket.available = Math.min(bucket.capacity, bucket.available + elapsedSec * bucket.refillRate);
          bucket.lastRefill = now;

          if (bucket.available >= 1) {
            bucket.available -= 1;
            resolve();
          } else {
            setTimeout(tick, 50);
          }
        };
        tick();
      });
    }

    export function resetKiwoomRateLimiter(): void {
      bucket.available = bucket.capacity;
      bucket.lastRefill = Date.now();
    }
    ```

    Step 4: `src/kiwoom/tokenStore.ts` 신규 — RESEARCH §5.3 전문 + 한글 주석 + parseKiwoomExpiresDt export:
    ```typescript
    import axios from "axios";
    import type { SupabaseClient } from "@supabase/supabase-js";
    import type { Config } from "../config";
    import { logger } from "../logger";

    const TOKEN_REFRESH_THRESHOLD_MS = 5 * 60 * 1000; // 만료 5분 전 갱신 (D-27)

    export type KiwoomToken = {
      accessToken: string;
      expiresAt: Date;
    };

    /**
     * 키움 OAuth2 token 발급 + Supabase kiwoom_tokens cache.
     * RESEARCH §5.3 + D-26/D-27.
     * race condition idempotent (D-27, RESEARCH §5.4) — upsert onConflict token_type DO UPDATE.
     * worker + server 가 동일 row 공유.
     */
    export async function getKiwoomToken(
      supabase: SupabaseClient,
      config: Config,
    ): Promise<KiwoomToken> {
      const tokenType = config.kiwoomTokenType;

      // 1. 캐시 확인
      const { data: cached } = await supabase
        .from("kiwoom_tokens")
        .select("access_token, expires_at")
        .eq("token_type", tokenType)
        .maybeSingle();

      if (cached) {
        const expiresAt = new Date(cached.expires_at);
        const remainMs = expiresAt.getTime() - Date.now();
        if (remainMs > TOKEN_REFRESH_THRESHOLD_MS) {
          logger.info({ remainMs }, "reusing cached Kiwoom token");
          return { accessToken: cached.access_token, expiresAt };
        }
        logger.info({ remainMs }, "Kiwoom token expiring soon — refreshing");
      } else {
        logger.info("Kiwoom token cache empty — issuing new token");
      }

      // 2. 만료 임박 또는 캐시 부재 → 새 토큰 발급
      const res = await axios.post(
        `${config.kiwoomBaseUrl}/oauth2/token`,
        {
          grant_type: "client_credentials",
          appkey: config.kiwoomAppkey,
          secretkey: config.kiwoomSecretkey,
        },
        {
          timeout: 10_000,
          headers: { "content-type": "application/json;charset=utf-8" },
        },
      );

      if (res.data.return_code !== 0) {
        throw new Error(`Kiwoom OAuth issue failed: ${res.data.return_msg}`);
      }

      const accessToken = res.data.token;
      const expiresAt = parseKiwoomExpiresDt(res.data.expires_dt);

      // 3. UPSERT (race condition idempotent — 마지막 INSERT 승)
      const { error } = await supabase
        .from("kiwoom_tokens")
        .upsert(
          {
            token_type: tokenType,
            access_token: accessToken,
            expires_at: expiresAt.toISOString(),
            fetched_at: new Date().toISOString(),
          },
          { onConflict: "token_type" },
        );

      if (error) {
        logger.warn({ error }, "kiwoom_tokens upsert failed, continuing with fresh token");
      }

      return { accessToken, expiresAt };
    }

    /**
     * 키움 expires_dt 형식 (RESEARCH §5.3) — "YYYYMMDDhhmmss" KST → Date (UTC).
     * 예: "20260513093013" → 2026-05-13 09:30:13 KST → 2026-05-13 00:30:13 UTC
     */
    export function parseKiwoomExpiresDt(s: string): Date {
      if (!/^\d{14}$/.test(s)) throw new Error(`Invalid expires_dt: ${s}`);
      const y = Number(s.slice(0, 4));
      const mo = Number(s.slice(4, 6)) - 1;
      const d = Number(s.slice(6, 8));
      const h = Number(s.slice(8, 10));
      const mi = Number(s.slice(10, 12));
      const se = Number(s.slice(12, 14));
      return new Date(Date.UTC(y, mo, d, h - 9, mi, se));
    }
    ```

    Step 5: `tests/tokenStore.test.ts` — axios + supabase mock:
    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest";
    import axios from "axios";
    import { getKiwoomToken, parseKiwoomExpiresDt } from "../src/kiwoom/tokenStore";
    import oauthFixture from "./fixtures/oauth-token.json";

    vi.mock("axios");

    function mockSupabaseWithToken(cached: { access_token: string; expires_at: string } | null) {
      const single = vi.fn().mockResolvedValue({ data: cached, error: null });
      const eq = vi.fn().mockReturnValue({ maybeSingle: single });
      const select = vi.fn().mockReturnValue({ eq });
      const upsert = vi.fn().mockResolvedValue({ error: null });
      const from = vi.fn().mockReturnValue({ select, upsert });
      return { from, _single: single, _upsert: upsert } as any;
    }

    const baseConfig = {
      kiwoomBaseUrl: "https://api.kiwoom.com",
      kiwoomAppkey: "appkey",
      kiwoomSecretkey: "secretkey",
      kiwoomTokenType: "live",
    } as any;

    describe("parseKiwoomExpiresDt", () => {
      it("YYYYMMDDhhmmss KST → UTC Date", () => {
        const d = parseKiwoomExpiresDt("20260515093013");
        expect(d.toISOString()).toBe("2026-05-15T00:30:13.000Z");
      });
      it("형식 다르면 throw", () => {
        expect(() => parseKiwoomExpiresDt("invalid")).toThrow();
        expect(() => parseKiwoomExpiresDt("2026-05-15")).toThrow();
      });
    });

    describe("getKiwoomToken", () => {
      beforeEach(() => {
        vi.mocked(axios.post).mockReset();
      });

      it("캐시에 만료 1시간 남은 token 있으면 axios 미호출 + cached 반환", async () => {
        const future = new Date(Date.now() + 60 * 60 * 1000);
        const supabase = mockSupabaseWithToken({
          access_token: "CACHED",
          expires_at: future.toISOString(),
        });
        const tok = await getKiwoomToken(supabase, baseConfig);
        expect(tok.accessToken).toBe("CACHED");
        expect(axios.post).not.toHaveBeenCalled();
      });

      it("캐시 부재 시 axios POST + upsert + 새 토큰 반환", async () => {
        const supabase = mockSupabaseWithToken(null);
        vi.mocked(axios.post).mockResolvedValue({ data: oauthFixture } as any);
        const tok = await getKiwoomToken(supabase, baseConfig);
        expect(tok.accessToken).toBe(oauthFixture.token);
        expect(axios.post).toHaveBeenCalledWith(
          "https://api.kiwoom.com/oauth2/token",
          expect.objectContaining({ grant_type: "client_credentials", appkey: "appkey", secretkey: "secretkey" }),
          expect.any(Object),
        );
        expect(supabase._upsert).toHaveBeenCalledOnce();
      });

      it("캐시 만료 임박 (4분 남음) 시 새 토큰 발급", async () => {
        const soon = new Date(Date.now() + 4 * 60 * 1000);
        const supabase = mockSupabaseWithToken({
          access_token: "EXPIRING",
          expires_at: soon.toISOString(),
        });
        vi.mocked(axios.post).mockResolvedValue({ data: oauthFixture } as any);
        const tok = await getKiwoomToken(supabase, baseConfig);
        expect(tok.accessToken).toBe(oauthFixture.token);
        expect(axios.post).toHaveBeenCalledOnce();
      });

      it("키움 return_code != 0 시 throw", async () => {
        const supabase = mockSupabaseWithToken(null);
        vi.mocked(axios.post).mockResolvedValue({ data: { return_code: 1, return_msg: "권한 없음" } } as any);
        await expect(getKiwoomToken(supabase, baseConfig)).rejects.toThrow(/권한 없음/);
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar &amp;&amp; pnpm -F @gh-radar/intraday-sync test -- tokenStore.test.ts</automated>
  </verify>
  <done>
    - 3개 src 파일 + 1 fixture + 1 test 파일
    - getKiwoomToken 의 4 시나리오 (캐시 hit / 부재 / 만료임박 / 키움 에러) green
    - parseKiwoomExpiresDt KST→UTC 변환 검증
    - axios.post 가 정확한 endpoint + body 로 호출됨 확인
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: fetchRanking.ts (ka10027 페이지네이션) + fixture + 단위 테스트</name>
  <files>workers/intraday-sync/src/kiwoom/fetchRanking.ts, workers/intraday-sync/tests/fixtures/ka10027-page1.json, workers/intraday-sync/tests/fetchRanking.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §1.2 (request body), §1.3 (응답 구조), Pattern 1 (페이지네이션 전문)
    - `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-06 (mrkt_tp=000, sort_tp=1), D-07 (updown_incls=1)
  </read_first>
  <behavior>
    - `fetchKa10027(client, token)` — cont-yn=N 단일 페이지 응답 시 1회 호출 + 모든 row 반환
    - 다중 페이지: cont-yn=Y 응답 + next-key 헤더 → 다음 호출 시 cont-yn=Y, next-key 헤더 echo
    - 401 응답 → throw "키움 401 — token/credential 실패"
    - 429 응답 → throw "키움 429 — rate limit"
    - 응답 row 수 > paginationHardCap → throw
    - request body 가 D-06/D-07 의 정확한 8 필드 (mrkt_tp=000, sort_tp=1, ...) 로 호출됨
  </behavior>
  <action>
    Step 1: `tests/fixtures/ka10027-page1.json` 신규 (RESEARCH §1.3 응답 패턴 — 사용자 실측 기반 placeholder):
    ```json
    {
      "return_code": 0,
      "return_msg": "정상적으로 처리되었습니다",
      "pred_pre_flu_rt_upper": [
        {
          "stk_cd": "007460_AL",
          "stk_nm": "에이프로젠",
          "cur_prc": "+6760",
          "pred_pre_sig": "2",
          "pred_pre": "+100",
          "flu_rt": "+1.50",
          "sel_req": "1000",
          "buy_req": "2000",
          "now_trde_qty": "1234567",
          "cntr_str": "",
          "cnt": "1"
        },
        {
          "stk_cd": "005930_AL",
          "stk_nm": "삼성전자",
          "cur_prc": "+70500",
          "pred_pre_sig": "2",
          "pred_pre": "+500",
          "flu_rt": "+0.71",
          "sel_req": "5000",
          "buy_req": "8000",
          "now_trde_qty": "10000000",
          "cntr_str": "",
          "cnt": "2"
        }
      ]
    }
    ```

    Step 2: `src/kiwoom/fetchRanking.ts` 신규 (RESEARCH Pattern 1):
    ```typescript
    import type { AxiosInstance } from "axios";
    import type { KiwoomKa10027Row } from "@gh-radar/shared";

    /**
     * ka10027 등락률 순위 페이지네이션 loop.
     * RESEARCH §1.2 + Pattern 1.
     *
     * D-06: mrkt_tp=000 (KOSPI+KOSDAQ 통합), sort_tp=1 (상승률 내림차순)
     * D-07: updown_incls=1 (등락 미발생 포함)
     *
     * 페이지네이션: 응답 헤더 cont-yn=Y/N + next-key 로 추적.
     * Hard cap 으로 무한 loop 방지 (T-09.1-14).
     */
    export async function fetchKa10027(
      client: AxiosInstance,
      token: string,
      hardCap = 5000,
    ): Promise<KiwoomKa10027Row[]> {
      const body = {
        mrkt_tp: "000",
        sort_tp: "1",
        trde_qty_cnd: "0000",
        stk_cnd: "0",
        crd_cnd: "0",
        updown_incls: "1",
        pric_cnd: "0",
        trde_prica_cnd: "0",
      };

      const all: KiwoomKa10027Row[] = [];
      let contYn: "Y" | "N" = "N";
      let nextKey: string | undefined;
      let pageCount = 0;

      do {
        const headers: Record<string, string> = {
          authorization: `Bearer ${token}`,
          "api-id": "ka10027",
          "content-type": "application/json;charset=utf-8",
          "cont-yn": contYn,
        };
        if (nextKey) headers["next-key"] = nextKey;

        let res;
        try {
          res = await client.post<{ pred_pre_flu_rt_upper?: KiwoomKa10027Row[]; return_code?: number; return_msg?: string }>(
            "/api/dostk/rkinfo",
            body,
            { headers },
          );
        } catch (err: any) {
          if (err?.response?.status === 401) throw new Error("키움 401 — token/credential 실패");
          if (err?.response?.status === 429) throw new Error("키움 429 — rate limit");
          throw err;
        }

        if (res.data.return_code !== undefined && res.data.return_code !== 0) {
          throw new Error(`ka10027 return_code=${res.data.return_code}: ${res.data.return_msg}`);
        }

        all.push(...(res.data.pred_pre_flu_rt_upper ?? []));
        contYn = (res.headers["cont-yn"] === "Y") ? "Y" : "N";
        nextKey = res.headers["next-key"];
        pageCount += 1;

        if (all.length >= hardCap) throw new Error(`pagination hard cap ${hardCap} exceeded (page ${pageCount})`);
      } while (contYn === "Y" && nextKey);

      return all;
    }
    ```

    Step 3: `tests/fetchRanking.test.ts`:
    ```typescript
    import { describe, it, expect, vi } from "vitest";
    import { fetchKa10027 } from "../src/kiwoom/fetchRanking";
    import page1 from "./fixtures/ka10027-page1.json";

    function makeClient(responses: Array<{ data: any; headers: any } | { error: any }>) {
      const post = vi.fn();
      for (const r of responses) {
        if ("error" in r) {
          post.mockRejectedValueOnce(r.error);
        } else {
          post.mockResolvedValueOnce(r);
        }
      }
      return { post } as any;
    }

    describe("fetchKa10027", () => {
      it("단일 페이지 (cont-yn=N) 응답 → 모든 row 반환", async () => {
        const client = makeClient([{ data: page1, headers: { "cont-yn": "N" } }]);
        const rows = await fetchKa10027(client, "TOKEN");
        expect(rows).toHaveLength(2);
        expect(rows[0].stk_cd).toBe("007460_AL");
      });

      it("request body 가 D-06/D-07 의 정확한 필드 포함", async () => {
        const client = makeClient([{ data: page1, headers: { "cont-yn": "N" } }]);
        await fetchKa10027(client, "TOKEN");
        expect(client.post).toHaveBeenCalledWith(
          "/api/dostk/rkinfo",
          expect.objectContaining({
            mrkt_tp: "000",
            sort_tp: "1",
            updown_incls: "1",
          }),
          expect.objectContaining({
            headers: expect.objectContaining({
              authorization: "Bearer TOKEN",
              "api-id": "ka10027",
              "cont-yn": "N",
            }),
          }),
        );
      });

      it("다중 페이지 (cont-yn=Y) → next-key 헤더로 다음 호출", async () => {
        const client = makeClient([
          { data: page1, headers: { "cont-yn": "Y", "next-key": "PAGE2_KEY" } },
          { data: page1, headers: { "cont-yn": "N" } },
        ]);
        const rows = await fetchKa10027(client, "TOKEN");
        expect(rows).toHaveLength(4); // 2 + 2
        expect(client.post).toHaveBeenCalledTimes(2);
        // 2번째 호출에 next-key 헤더 포함 확인
        expect(client.post).toHaveBeenNthCalledWith(
          2,
          "/api/dostk/rkinfo",
          expect.any(Object),
          expect.objectContaining({
            headers: expect.objectContaining({ "next-key": "PAGE2_KEY", "cont-yn": "Y" }),
          }),
        );
      });

      it("401 응답 → throw '키움 401'", async () => {
        const client = makeClient([{ error: { response: { status: 401 } } }]);
        await expect(fetchKa10027(client, "BAD")).rejects.toThrow(/키움 401/);
      });

      it("429 응답 → throw '키움 429'", async () => {
        const client = makeClient([{ error: { response: { status: 429 } } }]);
        await expect(fetchKa10027(client, "TOKEN")).rejects.toThrow(/키움 429/);
      });

      it("return_code != 0 응답 → throw with return_msg", async () => {
        const client = makeClient([{ data: { return_code: 99, return_msg: "권한 없음" }, headers: { "cont-yn": "N" } }]);
        await expect(fetchKa10027(client, "TOKEN")).rejects.toThrow(/return_code=99.*권한 없음/);
      });

      it("hardCap 초과 시 throw", async () => {
        // page1 = 2 row. hardCap=2 면 첫 페이지 다음에 더 진행 불가
        const client = makeClient([
          { data: page1, headers: { "cont-yn": "Y", "next-key": "PAGE2" } },
        ]);
        await expect(fetchKa10027(client, "TOKEN", 2)).rejects.toThrow(/hard cap 2 exceeded/);
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar &amp;&amp; pnpm -F @gh-radar/intraday-sync test -- fetchRanking.test.ts</automated>
  </verify>
  <done>
    - fetchKa10027 + fixture + 6 테스트 case green
    - 페이지네이션 cont-yn / next-key 헤더 정확
    - 401/429/return_code 에러 분류 throw
    - hard cap 가드
  </done>
</task>

</tasks>

<verification>
- `pnpm -F @gh-radar/intraday-sync typecheck && pnpm -F @gh-radar/intraday-sync test --run` 모두 exit 0
- 4 test 파일 모두 green (parseSigned + map + tokenStore + fetchRanking)
- 5개 src 파일 (client, rateLimiter, tokenStore, fetchRanking, pipeline/map) 정상 빌드
</verification>

<success_criteria>
- STEP1 의 키움 호출 chain (token 발급 → ka10027 페이지네이션 → ka10027Row → IntradayCloseUpdate) 모두 단위 테스트 PASS
- Plan 05 (ka10001 hot set) + Plan 06 (pipeline 통합) 가 본 plan 의 export 를 import 가능
- redact 패턴이 logger 출력에서 access_token / kiwoomAppkey / kiwoomSecretkey 가리는지 본 plan 의 test 가 검증 (간접적으로 — redact 자체는 logger config)
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-intraday-current-price/09.1-04-SUMMARY.md`
</output>
