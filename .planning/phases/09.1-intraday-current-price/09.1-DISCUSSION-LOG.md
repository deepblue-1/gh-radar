# Phase 09.1: Intraday Current Price (KIS → 키움 완전 대체) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-14
**Phase:** 09.1-intraday-current-price
**Areas discussed:** Hot set 정의, server on-demand 종목 상세, stock_quotes 누적 정책, trade_amount UI 처리, 키움 OAuth token 캐시, server outbound 인프라
**Scope re-definition:** 이전 CONTEXT (2026-05-13) 의 "키움 추가 + KIS 무변경" 가정이 사용자 의사결정과 충돌. 본 discuss 에서 "KIS → 키움 완전 대체" 로 재정의.

---

## Pre-discussion 컨텍스트 검증 (대화 단계)

사용자: "phase 9.1을 구현하고나면, KIS API는 더이상 사용하는게 없는건가?"
→ Claude 가 기존 CONTEXT/PLAN 6개 검토 후 "9.1 은 KIS 무변경 + 키움 추가" 로 보고.

사용자: "이상하네, 9.1이 KIS → 키움으로 완전 변경으로 알고 있는데?"
→ Claude 가 재검증 → 스코프 OUT 에 "KIS 완전 대체 — 후속 phase" 명시 확인. 사용자 인식과 명확히 다름.

사용자: "지금 plan이 잘못되었어. discuss부터 다시해야될거 같아."
→ (a) 기존 artifact 덮어쓰기 (git history 보존) 선택.
→ KIS 완전 대체 가능성 subagent 검증: "조건부 100% 대체 가능, 기술 blocker 없음".

3차 iteration 거쳐 최종 구조 합의:
- STEP 1 = 키움 ka10027 (KIS ranking 폐기)
- STEP 2 = 키움 ka10001 hot set (옵션 B)
- STEP 3 = 폐기 (trade_amount 보완 endpoint 없음)

사용자: "응 진행해보자" → /gsd-discuss-phase 09.1 진입.

---

## Q1. Hot set 정의 — ka10001 매분 호출 대상

| Option | Description | Selected |
|--------|-------------|----------|
| (a) top 200 ∪ watchlist (~250 종목, ~10.5초) | 60초 cycle 매우 여유, 정확도 충분 — ⭐ 추천 | ✓ |
| (b) top 400 ∪ watchlist (~450 종목, ~18.8초) | top 200-400 OHLC 정확도 ↑, 비용 ↑ | |
| (c) top N 동적 (종목 페이지 진입 시 임시 추가) | 복잡도 ↑, 캐시 invalidation 설계 필요 | |

**User's choice:** (a)
**Notes:** STEP1 ka10027 응답이 이미 등락률 내림차순 정렬이라 `slice(0, 200)` + watchlist unique 합집합으로 산출. watchlists 종목수 추정 ~50-200 (user당 50 limit).

---

## Q2. server on-demand 종목 상세 처리

| Option | Description | Selected |
|--------|-------------|----------|
| (a) 키움 client 동기 호출 교체 | 실시간성 ↑, server static IP 필요 → 인프라 복잡도 ↑ | ✓ |
| (b) DB 캐시(stock_quotes)만 읽기 | 최대 ~60s stale, server static IP 불필요 — ⭐ 추천 | |
| (c) hybrid (hot set 은 DB, 외는 키움) | 두 경로 유지 비용 | |

**User's choice:** (a)
**Notes:** 사용자가 실시간성 우선 선택. Q6 (server static IP) 도 필요해짐. server/src/kis/* → server/src/kiwoom/* 교체 + Cloud Run service 도 VPC/NAT 경유 outbound 재배포.

---

## Q3. stock_quotes 누적 캐시 정책

| Option | Description | Selected |
|--------|-------------|----------|
| (a) 매분 활성 1,898 종목 누적 — 비활성은 마지막값 유지 | UPSERT 단순, UI 가 활성/비활성 구분 안 함 — ⭐ 추천 | ✓ |
| (b) ka10027 응답 빠진 종목 stale 제거 | 비활성 거래정지 종목이 DB 에서 사라짐 — 종목 상세 영향 | |

**User's choice:** (a)
**Notes:** 영구 누적, stale cleanup 없음. updated_at 컬럼으로 신선도 판단.

---

## Q4. trade_amount UI 처리

| Option | Description | Selected |
|--------|-------------|----------|
| (a) 컬럼 NULL 허용 + UI "-" 표시 (기존 fallback) | schema 변경 0, 향후 정확값 endpoint 도입 시 무손실 복원 — ⭐ 추천 | |
| (b) 컬럼 자체 schema 에서 삭제 | 향후 복원 비용, migration 위험 | |
| (c) volume × price 근사값 | 정확도 손실 ~3-7%, 현 코드 정책 반대 | ✓ |

**User's choice:** (c) — 트레이딩 시그널 용도
**Notes:** Claude 의 "현 코드 주석과 충돌" 지적에 사용자가 사용처(대량거래 식별)를 명시하며 정책 반전 확정. `workers/ingestion/src/pipeline/map.ts:5` 의 "근사값은 허용하지 않음" 주석을 새 정책으로 교체. 정확한 거래대금이 필요한 회계/리포팅 용도가 아님. 향후 정확값 필요 시 Phase 09.5 후보 (ka10003/ka10032 도입) 로 deferred.

---

## Q5. 키움 OAuth token 캐시 위치

| Option | Description | Selected |
|--------|-------------|----------|
| (a) Supabase kiwoom_tokens 테이블 (kis_tokens 패턴 mirror) | 영구화 ↑, schema 추가 | ✓ |
| (b) in-memory cache (만료 시 refresh) | 단일 워커라 영구화 불필요 — ⭐ 추천 | |

**User's choice:** (a)
**Notes:** Q2=a (server 도 키움 호출) 와 자연스럽게 어울림 — worker + server 가 동일 토큰 row 공유. 24h TTL refresh, 동시 race 는 idempotent (마지막 INSERT 승). kis_tokens drop + kiwoom_tokens create 통합 마이그레이션.

---

## Q6. server (Cloud Run service) 의 키움 outbound 인프라

| Option | Description | Selected |
|--------|-------------|----------|
| (a) server 와 intraday-sync 가 동일 VPC/NAT/Static IP 공유 (IP 1개 등록) | 인프라 자원 최소, IP 변경 시 한 곳만 갱신 — ⭐ 추천 | ✓ |
| (b) server 와 worker 각각 별도 VPC + NAT + Static IP (IP 2개 등록) | 격리 ↑, 자원 2배 | |

**User's choice:** (a)
**Notes:** server 도 `--vpc-egress=all-traffic --network=gh-radar-vpc --subnet=...` 옵션으로 재배포. 키움 IP 화이트리스트에 Static IP 1개만 등록 (사용자 액션, Phase 09.1-06-PLAN [BLOCKING]).

---

## Claude's Discretion (사용자가 위임한 영역)

- 키움 client 구현 패턴 (axios + interceptors / fetch / SDK)
- 부호 prefix 파싱 정규식
- 에러 분류 (401 / 429 / 5xx) per retry policy 세부
- 로깅 필드 schema (pino, redact 확장)
- ka10027/ka10001 응답 fixture 구조 (vitest)
- hot set 산출 알고리즘 (STEP1 결과 slice + watchlist union)
- workers/ingestion 폐기 순서 (Cloud Scheduler PAUSE → Job 삭제 → Secret 정리 → migration)
- workers/intraday-sync vs server 의 키움 client 공유 방식 (packages/shared 추출 vs 각자 보유)

---

## Deferred Ideas (본 phase 스코프 밖, 보존)

### Phase 09.2 후보 (Intraday Full OHLC for All Active Stocks)
hot set 외 ~1,698 종목의 OHLC 도 5분 cycle 로 갱신 — 신규 Cloud Run Job 분리.

### Phase 09.3 후보 (WebSocket 실시간 tick)
키움 WebSocket FID stream + SSE → 브라우저 fan-out.

### Phase 09.4 후보 (분봉 차트 backfill)
ka10080 또는 동등 endpoint + 신규 `stock_minute_ohlcv` 테이블.

### Phase 09.5 후보 (거래대금 정확값 복원)
ka10003/ka10032 도입 → trade_amount 정확값 정책 복원, 본 phase 의 D-23/D-24 폐기.

### Phase 09.6 후보 (호가 10단계 depth)
ka10004 또는 동등 endpoint + UI 호가창.

### EODHD 글로벌 vendor 대안 (idle)
한국 시장 지원 시 키움 대체 가능. €29.99/월. v2 단순화 시 재검토.

---

## Discussion Flow Summary

1. **Pre-discuss 단계** (대화) — 사용자 인식과 기존 9.1 plan 의 충돌 발견, KIS 완전 대체 가능성 subagent 검증, 구조 제안 3회 iteration.
2. **/gsd-discuss-phase 09.1 진입** — 사용자 OK.
3. **Q1~Q5 5개 묶음 질문** — 사용자 답변 (a, a, a, c, a).
4. **Q6 (Q2 결과 의존) + Q4 보조 재확인** — 사용자 답변 (a, c 확정).
5. **CONTEXT.md / DISCUSSION-LOG.md 작성** — 본 단계.
6. **Next:** /gsd-plan-phase 09.1 진입. 기존 6 PLAN 갈아엎고 STEP2 ka10001 hot set + server 변경 반영해 재작성. ROADMAP §"Phase 09.1" SC + REQUIREMENTS DATA-02 갱신도 plan-phase 또는 별도 step.

---

*Generated: 2026-05-14*
*Discuss mode: interactive (text mode in auto session)*
