---
phase: 09.1
plan: 07
type: execute
wave: 2
depends_on: [02, 04, 05]
files_modified:
  - server/src/kiwoom/client.ts
  - server/src/kiwoom/rateLimiter.ts
  - server/src/kiwoom/tokenStore.ts
  - server/src/kiwoom/inquirePrice.ts
  - server/src/mappers/stock.ts
  - server/src/services/kiwoom-runtime.ts
  - server/src/services/kis-runtime.ts
  - server/src/server.ts
  - server/src/app.ts
  - server/src/config.ts
  - server/src/routes/stocks.ts
  - server/tests/kiwoom/inquirePrice.test.ts
  - server/tests/kiwoom/tokenStore.test.ts
  - server/tests/mappers/stock.test.ts
  - server/tests/routes/stock-detail.test.ts
autonomous: true
requirements: [DATA-02]
requirements_addressed:
  - DATA-02 (D-17/D-18 — server/src/kis → server/src/kiwoom 교체. server 종목 상세 페이지의 on-demand inquirePrice 가 키움 ka10001 동기 호출. inquirePriceToQuoteRow 가 KiwoomKa10001Row → StockQuoteRow 매핑.)
must_haves:
  truths:
    - "server/src/kiwoom/ 디렉터리에 client.ts/rateLimiter.ts/tokenStore.ts/inquirePrice.ts 4 파일 존재"
    - "server/src/mappers/stock.ts 의 inquirePriceToQuoteRow 가 KiwoomKa10001Row 입력 + StockQuoteRowUpsert (volume/trade_amount omit) 반환 — D-22 충돌 해소"
    - "server/src/routes/stocks.ts 의 import 가 ../kis/inquirePrice → ../kiwoom/inquirePrice 로 교체"
    - "server/src/services/kiwoom-runtime.ts 가 createKiwoomRuntime(config, supabase) → AxiosInstance 반환 (kis-runtime mirror)"
    - "server/src/server.ts 가 kisClient → kiwoomClient 로 교체, app.ts deps 도 동일 변경"
    - "server/src/config.ts 의 KIS_APP_KEY/KIS_APP_SECRET → KIWOOM_APPKEY/KIWOOM_SECRETKEY 로 env 교체"
    - "기존 server/src/kis/ 디렉터리는 본 plan 에서 건드리지 않음 — Wave 4 cleanup 에서 삭제 (transition risk T-11)"
    - "server tests 모두 green (kiwoom inquirePrice + mapper + stock-detail route)"
  artifacts:
    - path: server/src/kiwoom/inquirePrice.ts
      provides: "fetchInquirePrice(client, token, code): Promise<KiwoomKa10001Row> — 토큰 + rate limiter"
    - path: server/src/mappers/stock.ts
      provides: "inquirePriceToQuoteRow(code, ka10001Row): StockQuoteRowUpsert — volume/trade_amount omit (D-22 충돌 해소)"
    - path: server/src/services/kiwoom-runtime.ts
      provides: "createKiwoomRuntime(config, supabase): Promise<AxiosInstance>"
  key_links:
    - from: "server/src/routes/stocks.ts"
      to: "server/src/kiwoom/inquirePrice.ts"
      via: "import { fetchInquirePrice } from \"../kiwoom/inquirePrice.js\""
      pattern: "from \"../kiwoom/inquirePrice"
    - from: "createKiwoomRuntime"
      to: "Supabase kiwoom_tokens"
      via: "getKiwoomToken (worker 와 동일 row 공유)"
      pattern: "getKiwoomToken"
---

<objective>
server 측 KIS 사용처를 키움 client 로 완전 교체한다 — `server/src/kis/*` → `server/src/kiwoom/*` 신설 (worker 의 token store + rate limiter 패턴 mirror, Supabase `kiwoom_tokens` row 공유). 종목 상세 페이지의 on-demand `inquirePrice` 호출이 ka10001 동기 호출로 전환되며, `inquirePriceToQuoteRow` mapper 가 KIS 필드 매핑에서 ka10001 필드 매핑으로 재작성된다.

Purpose: D-17/D-18 의 server 측 키움 통합. server 도 키움 호출 → Static IP 화이트리스트 통과 (Wave 3 server 재배포 후 활성). 기존 `server/src/kis/` 는 본 plan 에서 건드리지 않음 — Wave 4 cleanup 의 안전한 deletion 을 위해 transition 기간 동안 dead code 로 잔존.

Output: 7 server src + 4 server test 갱신/신규. `pnpm -F server test typecheck build` 모두 exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md
@.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md
@.planning/phases/09.1-intraday-current-price/09.1-02-PLAN.md
@.planning/phases/09.1-intraday-current-price/09.1-04-PLAN.md
@.planning/phases/09.1-intraday-current-price/09.1-05-PLAN.md
@server/src/kis/inquirePrice.ts
@server/src/kis/client.ts
@server/src/kis/tokenStore.ts
@server/src/services/kis-runtime.ts
@server/src/mappers/stock.ts
@server/src/routes/stocks.ts
@server/src/app.ts
@server/src/server.ts
@server/src/config.ts

<read_first>
- `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §13 (server kiwoom client 전문), §1.5 (ka10001 응답), §2.4 (server mapper)
- `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-17 (server 키움 동기 호출), D-18 (server static IP 공유), D-19 (server 토큰 전략 — Supabase 공유)
- `server/src/kis/*` 전체 (mirror 원본)
- `server/src/services/kis-runtime.ts` (createKisRuntime 패턴)
- `server/src/routes/stocks.ts` (현재 `inquirePriceToQuoteRow(code, price)` 호출 시그니처)
- `server/src/mappers/stock.ts` (StockQuoteRow 타입 + 현재 inquirePriceToQuoteRow)
- Wave 1 Plan 04/05 의 worker kiwoom 패턴 — server 는 worker 와 별도 모듈이지만 동일 패턴 mirror
</read_first>

<interfaces>
<!-- worker 측 동일 함수 (Plan 04 + 05) — server 는 별도 모듈로 재구현 (cross-workspace dependency 회피) -->
```typescript
// Plan 04 의 worker tokenStore signature
export async function getKiwoomToken(
  supabase: SupabaseClient,
  config: { kiwoomBaseUrl: string; kiwoomAppkey: string; kiwoomSecretkey: string; kiwoomTokenType?: string },
): Promise<{ accessToken: string; expiresAt: Date }>;
```

<!-- server StockQuoteRow (mappers/stock.ts) 본 plan 의 inquirePriceToQuoteRow 출력 타입 -->
```typescript
export type StockQuoteRow = {
  code: string;
  price: string;          // numeric string (Supabase 컬럼 타입)
  change_amount: string;
  change_rate: string;
  volume: number;
  trade_amount: number;
  open: string | null;
  high: string | null;
  low: string | null;
  market_cap: number | null;
  upper_limit: string;
  lower_limit: string;
  updated_at: string;
};
```

<!-- routes/stocks.ts 현재 호출 (Wave 2 전환 대상) -->
```typescript
import { fetchInquirePrice } from "../kis/inquirePrice.js";  // ← "../kiwoom/inquirePrice.js" 로 교체
const price = await fetchInquirePrice(kisClient, code);      // 시그니처 (client, code) → (client, token, code)
freshQuote = inquirePriceToQuoteRow(code, price);             // 입력 타입 KisInquirePriceRow → KiwoomKa10001Row
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud Run service → Kiwoom API | server 도 Static IP (Wave 3 재배포 후) 경유. Bearer token via headers.authorization. |
| server → Supabase kiwoom_tokens | service-role 권한, worker 와 동일 row 공유 (race idempotent). |
| dead code (server/src/kis/) | 본 plan 에서 남아있음 — Wave 4 cleanup 에서 삭제. import 가 없으므로 빌드/typecheck 에 영향 없음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-26 | Availability | server ka10001 호출이 Wave 3 server VPC 재배포 전 → IP whitelist 통과 실패 | mitigate | Wave 3 가 server VPC 재배포 책임. 본 plan 의 server 코드는 deploy 시까지 production 영향 없음 (현 production server 이미지는 git push 까지 변경 안 됨, deploy 명시 액션). 단, 로컬 dev 환경에서 server 가 키움 호출 시 실패 가능 — try/catch + fallback cached quote 유지 (기존 routes/stocks.ts 패턴). |
| T-09.1-27 | DoS | server ka10001 burst (사용자 다수 동시 종목 상세) | mitigate | server/src/kiwoom/rateLimiter.ts 의 token bucket 24 req/s (worker 와 별도 모듈 인스턴스, 단 IP 공유 시 통합 bucket — RESEARCH §1.7). request burst 시 acquireKiwoomRateToken 가 자연 직렬화. |
| T-09.1-28 | Information Disclosure | KIWOOM_APPKEY/SECRETKEY 가 server log 노출 | mitigate | server logger (pino) redact paths 갱신 — KIS 와 동일 패턴. Wave 3 deploy-server.sh 에서 secret 명시 + .env.deploy gitignore. |
| T-09.1-29 | Tampering | mapper 가 server on-demand 호출 시 worker STEP1 의 정확한 trade_amount/volume 을 0 으로 덮어씀 | mitigate | inquirePriceToQuoteRow 가 StockQuoteRowUpsert (volume/trade_amount omit) 반환 → Supabase upsert 가 두 컬럼 미터치 → STEP1 ka10027 의 매분 정확값 (D-22) 보존. 단위 테스트 가 omit 검증. |
| T-09.1-30 | Availability | server ka10001 호출 timeout (10초) | accept | axios timeout 10s 명시. 실패 시 try/catch + cached quote fallback (기존 routes/stocks.ts 패턴 유지). |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: server/src/kiwoom/ 4파일 신설 (client + rateLimiter + tokenStore + inquirePrice) + 단위 테스트</name>
  <files>server/src/kiwoom/client.ts, server/src/kiwoom/rateLimiter.ts, server/src/kiwoom/tokenStore.ts, server/src/kiwoom/inquirePrice.ts, server/tests/kiwoom/tokenStore.test.ts, server/tests/kiwoom/inquirePrice.test.ts</files>
  <read_first>
    - `server/src/kis/client.ts` (axios pattern)
    - `server/src/kis/tokenStore.ts` (Supabase token cache — table 이름과 endpoint 만 다름)
    - Plan 04 의 worker tokenStore.ts (Supabase kiwoom_tokens row 공유)
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §13.2 (server client + rate limiter), §13.3 (server inquirePrice), §13.4 (server token store), §5.3 (token store 공통 패턴)
  </read_first>
  <behavior>
    - `createKiwoomClient(baseUrl)` → axios baseURL + timeout 10s (worker 패턴 mirror)
    - `acquireKiwoomRateToken()` → 24 req/s token bucket (worker 와 별도 모듈 인스턴스, 본 server 프로세스 내부 직렬화)
    - `getKiwoomToken(supabase, config)` → Supabase kiwoom_tokens 캐시 (worker 와 동일 row 공유 — race idempotent)
    - `fetchInquirePrice(client, token, code)` → ka10001 호출 + return_code 검증 + KiwoomKa10001Row 반환
  </behavior>
  <action>
    `server/src/kiwoom/client.ts` (worker Plan 04 패턴):
    ```typescript
    import axios, { type AxiosInstance } from "axios";

    export function createKiwoomClient(baseUrl: string): AxiosInstance {
      return axios.create({
        baseURL: baseUrl,
        timeout: 10_000,
        headers: { "content-type": "application/json;charset=utf-8" },
      });
    }
    ```

    `server/src/kiwoom/rateLimiter.ts` (worker Plan 04 패턴 mirror):
    ```typescript
    /**
     * server 측 키움 ka10001 token-bucket rate limiter. 24 req/s default.
     * worker 와 별도 모듈 인스턴스 — IP 공유 시 키움 측 통합 bucket 으로 자연 직렬화 가정 (RESEARCH §1.7, A15).
     */
    const BUCKET_CAPACITY_DEFAULT = 24;
    const REFILL_RATE_PER_SEC_DEFAULT = 24;

    const bucket = {
      available: BUCKET_CAPACITY_DEFAULT,
      lastRefill: Date.now(),
      capacity: BUCKET_CAPACITY_DEFAULT,
      refillRate: REFILL_RATE_PER_SEC_DEFAULT,
    };

    export function configureKiwoomRateLimiter(opts: { capacity: number; refillRatePerSec: number }): void {
      bucket.capacity = opts.capacity;
      bucket.refillRate = opts.refillRatePerSec;
      bucket.available = Math.min(bucket.available, opts.capacity);
    }

    export async function acquireKiwoomRateToken(): Promise<void> {
      return new Promise((resolve) => {
        const tick = () => {
          const now = Date.now();
          const elapsedSec = (now - bucket.lastRefill) / 1000;
          bucket.available = Math.min(bucket.capacity, bucket.available + elapsedSec * bucket.refillRate);
          bucket.lastRefill = now;
          if (bucket.available >= 1) {
            bucket.available -= 1;
            resolve();
          } else {
            setTimeout(tick, 50);
          }
        };
        tick();
      });
    }

    export function resetKiwoomRateLimiter(): void {
      bucket.available = bucket.capacity;
      bucket.lastRefill = Date.now();
    }
    ```

    `server/src/kiwoom/tokenStore.ts` (worker Plan 04 § 5.3 mirror — server import 경로 `.js` 확장자):
    ```typescript
    import axios from "axios";
    import type { SupabaseClient } from "@supabase/supabase-js";
    import { logger } from "../logger.js";

    const TOKEN_REFRESH_THRESHOLD_MS = 5 * 60 * 1000;

    export type KiwoomToken = { accessToken: string; expiresAt: Date };

    export type KiwoomTokenConfig = {
      kiwoomBaseUrl: string;
      kiwoomAppkey: string;
      kiwoomSecretkey: string;
      kiwoomTokenType?: string;
    };

    /**
     * Supabase kiwoom_tokens cache. worker (Plan 04) 와 동일 row 공유 (RESEARCH §5.3, D-19).
     * race condition idempotent (마지막 INSERT 승).
     */
    export async function getKiwoomToken(
      supabase: SupabaseClient,
      config: KiwoomTokenConfig,
    ): Promise<KiwoomToken> {
      const tokenType = config.kiwoomTokenType ?? "live";

      const { data: cached } = await supabase
        .from("kiwoom_tokens")
        .select("access_token, expires_at")
        .eq("token_type", tokenType)
        .maybeSingle();

      if (cached) {
        const expiresAt = new Date(cached.expires_at);
        const remainMs = expiresAt.getTime() - Date.now();
        if (remainMs > TOKEN_REFRESH_THRESHOLD_MS) {
          return { accessToken: cached.access_token, expiresAt };
        }
        logger.info({ remainMs }, "Kiwoom token expiring soon — refreshing");
      } else {
        logger.info("Kiwoom token cache empty — issuing");
      }

      const res = await axios.post(
        `${config.kiwoomBaseUrl}/oauth2/token`,
        {
          grant_type: "client_credentials",
          appkey: config.kiwoomAppkey,
          secretkey: config.kiwoomSecretkey,
        },
        { timeout: 10_000, headers: { "content-type": "application/json;charset=utf-8" } },
      );

      if (res.data.return_code !== 0) {
        throw new Error(`Kiwoom OAuth issue failed: ${res.data.return_msg}`);
      }

      const accessToken = res.data.token;
      const expiresAt = parseKiwoomExpiresDt(res.data.expires_dt);

      const { error } = await supabase
        .from("kiwoom_tokens")
        .upsert(
          {
            token_type: tokenType,
            access_token: accessToken,
            expires_at: expiresAt.toISOString(),
            fetched_at: new Date().toISOString(),
          },
          { onConflict: "token_type" },
        );

      if (error) {
        logger.warn({ error }, "kiwoom_tokens upsert failed, continuing");
      }

      return { accessToken, expiresAt };
    }

    export function parseKiwoomExpiresDt(s: string): Date {
      if (!/^\d{14}$/.test(s)) throw new Error(`Invalid expires_dt: ${s}`);
      const y = Number(s.slice(0, 4));
      const mo = Number(s.slice(4, 6)) - 1;
      const d = Number(s.slice(6, 8));
      const h = Number(s.slice(8, 10));
      const mi = Number(s.slice(10, 12));
      const se = Number(s.slice(12, 14));
      return new Date(Date.UTC(y, mo, d, h - 9, mi, se));
    }
    ```

    `server/src/kiwoom/inquirePrice.ts` (RESEARCH §13.3 — server 측 ka10001 호출):
    ```typescript
    import type { AxiosInstance } from "axios";
    import type { KiwoomKa10001Row } from "@gh-radar/shared";
    import { acquireKiwoomRateToken } from "./rateLimiter.js";

    /**
     * server 의 종목 상세 페이지 on-demand 호출 (D-17).
     * worker 의 fetchKa10001ForHotSet 와 같은 endpoint + headers + body.
     * server 는 단일 종목 1회 호출 (사용자 요청 trigger).
     */
    export async function fetchInquirePrice(
      client: AxiosInstance,
      token: string,
      code: string,
    ): Promise<KiwoomKa10001Row> {
      await acquireKiwoomRateToken();

      const headers = {
        authorization: `Bearer ${token}`,
        "api-id": "ka10001",
        "content-type": "application/json;charset=utf-8",
      };

      const res = await client.post("/api/dostk/stkinfo", { stk_cd: code }, { headers });

      if (res.data.return_code !== 0) {
        throw new Error(`ka10001 ${code} return_code=${res.data.return_code}: ${res.data.return_msg}`);
      }

      return res.data as KiwoomKa10001Row;
    }
    ```

    `server/tests/kiwoom/tokenStore.test.ts` (worker tokenStore.test.ts 패턴 mirror):
    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest";
    import axios from "axios";
    import { getKiwoomToken, parseKiwoomExpiresDt } from "../../src/kiwoom/tokenStore.js";

    vi.mock("axios");

    function mockSupabase(cached: { access_token: string; expires_at: string } | null) {
      const single = vi.fn().mockResolvedValue({ data: cached, error: null });
      const eq = vi.fn().mockReturnValue({ maybeSingle: single });
      const select = vi.fn().mockReturnValue({ eq });
      const upsert = vi.fn().mockResolvedValue({ error: null });
      return { from: vi.fn().mockReturnValue({ select, upsert }) } as any;
    }

    const config = {
      kiwoomBaseUrl: "https://api.kiwoom.com",
      kiwoomAppkey: "k",
      kiwoomSecretkey: "s",
    };

    describe("parseKiwoomExpiresDt", () => {
      it("YYYYMMDDhhmmss → UTC Date", () => {
        const d = parseKiwoomExpiresDt("20260515093013");
        expect(d.toISOString()).toBe("2026-05-15T00:30:13.000Z");
      });
    });

    describe("getKiwoomToken", () => {
      beforeEach(() => vi.mocked(axios.post).mockReset());

      it("캐시 valid → axios 미호출", async () => {
        const future = new Date(Date.now() + 60 * 60 * 1000);
        const supabase = mockSupabase({ access_token: "CACHED", expires_at: future.toISOString() });
        const t = await getKiwoomToken(supabase, config);
        expect(t.accessToken).toBe("CACHED");
        expect(axios.post).not.toHaveBeenCalled();
      });

      it("캐시 부재 → 새 토큰 발급", async () => {
        const supabase = mockSupabase(null);
        vi.mocked(axios.post).mockResolvedValue({
          data: { return_code: 0, token: "NEW_TOKEN", expires_dt: "20260515093013" },
        } as any);
        const t = await getKiwoomToken(supabase, config);
        expect(t.accessToken).toBe("NEW_TOKEN");
      });
    });
    ```

    `server/tests/kiwoom/inquirePrice.test.ts`:
    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest";
    import { fetchInquirePrice } from "../../src/kiwoom/inquirePrice.js";
    import { configureKiwoomRateLimiter, resetKiwoomRateLimiter } from "../../src/kiwoom/rateLimiter.js";

    describe("fetchInquirePrice (server)", () => {
      beforeEach(() => {
        configureKiwoomRateLimiter({ capacity: 1000, refillRatePerSec: 1000 });
        resetKiwoomRateLimiter();
      });

      it("return_code=0 응답 → KiwoomKa10001Row 반환", async () => {
        const post = vi.fn().mockResolvedValue({
          data: {
            return_code: 0,
            return_msg: "정상",
            stk_cd: "005930",
            cur_prc: "+70500",
            open_pric: "+70000",
            high_pric: "+71000",
            low_pric: "+69500",
            upl_pric: "91000",
            lst_pric: "49000",
            mac: "4209000",
          },
        });
        const client = { post } as any;
        const row = await fetchInquirePrice(client, "TOKEN", "005930");
        expect(row.stk_cd).toBe("005930");
        expect(row.open_pric).toBe("+70000");
        expect(post).toHaveBeenCalledWith(
          "/api/dostk/stkinfo",
          { stk_cd: "005930" },
          expect.objectContaining({
            headers: expect.objectContaining({
              authorization: "Bearer TOKEN",
              "api-id": "ka10001",
            }),
          }),
        );
      });

      it("return_code != 0 → throw", async () => {
        const post = vi.fn().mockResolvedValue({ data: { return_code: 1700, return_msg: "rate" } });
        const client = { post } as any;
        await expect(fetchInquirePrice(client, "T", "005930")).rejects.toThrow(/1700.*rate/);
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar &amp;&amp; pnpm -F server test -- kiwoom/inquirePrice.test.ts kiwoom/tokenStore.test.ts</automated>
  </verify>
  <done>
    - 4 server kiwoom 모듈 + 2 test 파일 green
    - server 가 worker 와 동일 kiwoom_tokens row 공유 가능 (D-19)
    - inquirePrice 가 ka10001 endpoint + 올바른 headers
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: mappers/stock.ts 갱신 + stock.test.ts 갱신 — inquirePriceToQuoteRow 가 KiwoomKa10001Row 입력</name>
  <files>server/src/mappers/stock.ts, server/tests/mappers/stock.test.ts</files>
  <read_first>
    - `server/src/mappers/stock.ts` (기존 KIS 매핑 — 본 plan 에서 갱신)
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §2.4 (server mapper 신규 코드)
    - `server/tests/mappers/stock.test.ts` (rowToStock 테스트 — 본 plan 에서 inquirePriceToQuoteRow 케이스 추가)
  </read_first>
  <behavior>
    - `inquirePriceToQuoteRow(code, ka10001Row)` — KiwoomKa10001Row 입력 + **StockQuoteRowUpsert 반환 (volume/trade_amount 키 omit, D-22 충돌 해소)**
    - price = parseSignedPrice(cur_prc) 절댓값
    - open/high/low = parseSignedPrice 절댓값 (string 타입)
    - market_cap = parseMac 결과 (× 10^8)
    - upper_limit / lower_limit = parseOptionalSignedNumber 절댓값
    - **volume / trade_amount 키 미포함** — STEP1 ka10027 가 매분 갱신하는 정확값을 server on-demand 호출이 0 으로 덮어쓰지 않도록 (D-22 STEP1 1898 종목 매분 trade_amount = volume × close UPSERT 보존)
    - routes/stocks.ts 의 supabase.upsert(freshQuote, { onConflict: "code" }) 가 부분 컬럼 UPDATE — Supabase JS client 가 row 의 명시 키만 SET, 미언급 컬럼은 기존 값 유지
    - 기존 rowToStock 테스트는 영향 없음 — 별도 함수
  </behavior>
  <action>
    `server/src/mappers/stock.ts` 갱신 — `KisInquirePriceRow` import 를 `KiwoomKa10001Row` 로 교체 + `inquirePriceToQuoteRow` 본문 재작성. 기존 `rowToStock`, `mergeMasterAndQuote` 는 무변경:

    파일 전체 내용 (기존 코드 + 본 plan 의 변경):

    ```typescript
    import type { Stock, Market, KiwoomKa10001Row } from "@gh-radar/shared";

    // signedPrice / mac parser — worker 와 별도 모듈 (cross-workspace import 회피)
    // Plan 04 의 packages/shared 가 패턴 제공하나 server 는 자체 인스턴스 유지.
    function parseSignedPrice(s: string | undefined): number {
      if (!s) throw new Error("missing signed price");
      const trimmed = s.trim();
      if (trimmed === "") throw new Error("missing signed price");
      const sign = trimmed[0];
      const rest = (sign === "+" || sign === "-") ? trimmed.slice(1) : trimmed;
      const abs = Number(rest.replace(/,/g, ""));
      if (!Number.isFinite(abs)) throw new Error(`invalid signed price: "${s}"`);
      return abs;
    }

    function parseOptionalSignedNumber(s: string | undefined): number | null {
      if (!s || s.trim() === "") return null;
      const n = Number(s.replace(/,/g, ""));
      return Number.isFinite(n) ? n : null;
    }

    function parseMac(s: string | undefined): number | null {
      if (!s || s.trim() === "") return null;
      const n = Number(s.replace(/,/g, ""));
      if (!Number.isFinite(n)) return null;
      // 가설 단위 = 억원 (Plan 04/05 의 fixture R2 캡처로 확정 — 변경 시 1줄)
      return Math.round(n * 100_000_000);
    }

    // === 레거시 — Plan 05 전환 전 stocks 테이블 row (보존) ===
    export type StockRow = {
      code: string;
      name: string;
      market: string;
      price: string;
      change_amount: string;
      change_rate: string;
      volume: number;
      trade_amount: number;
      open: string | null;
      high: string | null;
      low: string | null;
      market_cap: number | null;
      upper_limit: string;
      lower_limit: string;
      updated_at: string;
    };

    export type StockWithProximity = Stock & { upperLimitProximity: number };

    export function rowToStock(r: StockRow): StockWithProximity {
      const upper = Number(r.upper_limit);
      const price = Number(r.price);
      return {
        code: r.code,
        name: r.name,
        market: r.market as Market,
        price,
        changeAmount: Number(r.change_amount),
        changeRate: Number(r.change_rate),
        volume: r.volume,
        tradeAmount: r.trade_amount,
        open: Number(r.open ?? 0),
        high: Number(r.high ?? 0),
        low: Number(r.low ?? 0),
        marketCap: Number(r.market_cap ?? 0),
        upperLimit: upper,
        lowerLimit: Number(r.lower_limit),
        updatedAt: r.updated_at,
        upperLimitProximity: upper > 0 ? price / upper : 0,
      };
    }

    // === 신규 3-테이블 구조 (stocks 마스터 + stock_quotes) ===

    export type StockMasterRow = {
      code: string;
      name: string;
      market: string;
      sector: string | null;
      security_type: string;
      listing_date: string | null;
      is_delisted: boolean;
      updated_at: string;
    };

    export type StockQuoteRow = {
      code: string;
      price: string;
      change_amount: string;
      change_rate: string;
      volume: number;
      trade_amount: number;
      open: string | null;
      high: string | null;
      low: string | null;
      market_cap: number | null;
      upper_limit: string;
      lower_limit: string;
      updated_at: string;
    };

    export type StockWithProximityResponse = Stock & { upperLimitProximity: number };

    export function mergeMasterAndQuote(
      master: StockMasterRow,
      quote: StockQuoteRow | null,
    ): StockWithProximityResponse {
      const price = quote ? Number(quote.price) : 0;
      const upper = quote ? Number(quote.upper_limit) : 0;
      return {
        code: master.code,
        name: master.name,
        market: master.market as Market,
        price,
        changeAmount: quote ? Number(quote.change_amount) : 0,
        changeRate: quote ? Number(quote.change_rate) : 0,
        volume: quote ? quote.volume : 0,
        tradeAmount: quote ? quote.trade_amount : 0,
        open: quote ? Number(quote.open ?? 0) : 0,
        high: quote ? Number(quote.high ?? 0) : 0,
        low: quote ? Number(quote.low ?? 0) : 0,
        marketCap: quote ? Number(quote.market_cap ?? 0) : 0,
        upperLimit: upper,
        lowerLimit: quote ? Number(quote.lower_limit) : 0,
        updatedAt: quote ? quote.updated_at : master.updated_at,
        upperLimitProximity: upper > 0 ? price / upper : 0,
      };
    }

    /**
     * stock_quotes UPSERT 시 명시 컬럼만 갱신하는 partial row 타입.
     * Phase 09.1 D-22 충돌 해소 (R3 RESOLVED) — server on-demand 호출이 worker 의
     * STEP1 매분 trade_amount/volume UPSERT 결과를 덮어쓰지 않도록 두 컬럼을 omit.
     */
    export type StockQuoteRowUpsert = Omit<StockQuoteRow, "volume" | "trade_amount">;

    /**
     * 키움 ka10001 응답 → stock_quotes UPSERT (부분 컬럼) row.
     * Phase 09.1 D-17/D-18 — server 도 키움 동기 호출.
     * RESEARCH §2.4 + Open Q R3 RESOLVED.
     *
     * volume / trade_amount 키 omit (D-22 충돌 해소):
     *   - STEP1 ka10027 가 매분 1898 종목의 정확값 (volume × close 근사) UPSERT
     *   - server on-demand 호출은 가격/등락/OHLC/limits/market_cap 만 갱신
     *   - Supabase upsert({ onConflict: "code" }) 가 row 의 명시 키만 SET — 미언급 컬럼 (volume, trade_amount) 은 기존 값 유지
     */
    export function inquirePriceToQuoteRow(
      code: string,
      ka10001: KiwoomKa10001Row,
    ): StockQuoteRowUpsert {
      const price = parseSignedPrice(ka10001.cur_prc);
      const open = parseSignedPrice(ka10001.open_pric);
      const high = parseSignedPrice(ka10001.high_pric);
      const low = parseSignedPrice(ka10001.low_pric);
      const changeAmount = parseOptionalSignedNumber(ka10001.pred_pre);
      const changeRate = parseOptionalSignedNumber(ka10001.flu_rt);
      const upperLimit = parseOptionalSignedNumber(ka10001.upl_pric);
      const lowerLimit = parseOptionalSignedNumber(ka10001.lst_pric);
      const marketCap = parseMac(ka10001.mac);

      // 명시 키만 — volume / trade_amount 는 의도적으로 omit (D-22)
      return {
        code,
        price: price.toString(),
        change_amount: (changeAmount ?? 0).toString(),
        change_rate: (changeRate ?? 0).toString(),
        open: open.toString(),
        high: high.toString(),
        low: low.toString(),
        market_cap: marketCap,
        upper_limit: (upperLimit !== null ? Math.abs(upperLimit) : 0).toString(),
        lower_limit: (lowerLimit !== null ? Math.abs(lowerLimit) : 0).toString(),
        updated_at: new Date().toISOString(),
      };
    }
    ```

    `server/tests/mappers/stock.test.ts` 갱신 — 기존 rowToStock 테스트 유지 + `inquirePriceToQuoteRow` 테스트 추가:

    ```typescript
    import { describe, it, expect } from "vitest";
    import { rowToStock, inquirePriceToQuoteRow } from "../../src/mappers/stock";
    import { samsungRow } from "../fixtures/stocks";
    import type { KiwoomKa10001Row } from "@gh-radar/shared";

    describe("rowToStock", () => {
      // 기존 5 테스트 유지
      it("converts string numerics to number", () => {
        const s = rowToStock(samsungRow);
        expect(typeof s.price).toBe("number");
        expect(s.price).toBe(70000);
        expect(s.changeRate).toBeCloseTo(1.45);
      });
      it("computes upperLimitProximity = price/upperLimit", () => {
        const s = rowToStock(samsungRow);
        expect(s.upperLimitProximity).toBeCloseTo(70000 / 91000);
      });
      it("returns 0 for upperLimitProximity when upper_limit is 0", () => {
        const s = rowToStock({ ...samsungRow, upper_limit: "0" });
        expect(s.upperLimitProximity).toBe(0);
      });
      it("handles null market_cap as 0", () => {
        const s = rowToStock({ ...samsungRow, market_cap: null });
        expect(s.marketCap).toBe(0);
      });
      it("preserves market as 'KOSPI' | 'KOSDAQ'", () => {
        expect(rowToStock(samsungRow).market).toBe("KOSPI");
      });
    });

    describe("inquirePriceToQuoteRow (Phase 09.1 D-17 — 키움 ka10001)", () => {
      const ka10001: KiwoomKa10001Row = {
        stk_cd: "005930",
        cur_prc: "+70500",
        pred_pre: "+500",
        flu_rt: "+0.71",
        open_pric: "+70000",
        high_pric: "+71000",
        low_pric: "+69500",
        upl_pric: "91000",
        lst_pric: "49000",
        mac: "4209000",
      };

      it("ka10001 응답 → StockQuoteRow 매핑 (절댓값 + market_cap × 10^8)", () => {
        const row = inquirePriceToQuoteRow("005930", ka10001);
        expect(row.code).toBe("005930");
        expect(row.price).toBe("70500");
        expect(row.open).toBe("70000");
        expect(row.high).toBe("71000");
        expect(row.low).toBe("69500");
        expect(row.upper_limit).toBe("91000");
        expect(row.lower_limit).toBe("49000");
        expect(row.market_cap).toBe(4209000 * 100_000_000);
        expect(row.change_amount).toBe("500");
        expect(row.change_rate).toBe("0.71");
      });

      it("volume / trade_amount 키 omit (D-22 충돌 해소 — R3 RESOLVED)", () => {
        const row = inquirePriceToQuoteRow("005930", ka10001);
        expect(row).not.toHaveProperty("volume");
        expect(row).not.toHaveProperty("trade_amount");
      });

      it("upl_pric / lst_pric / mac 빈 문자열 → null (ETF 패턴)", () => {
        const etf: KiwoomKa10001Row = {
          ...ka10001,
          upl_pric: "",
          lst_pric: "",
          mac: "",
        };
        const row = inquirePriceToQuoteRow("069500", etf);
        expect(row.upper_limit).toBe("0");
        expect(row.lower_limit).toBe("0");
        expect(row.market_cap).toBeNull();
      });

      it("음수 cur_prc → 절댓값 (변동성 종목)", () => {
        const jeil: KiwoomKa10001Row = {
          ...ka10001,
          cur_prc: "-1500",
          pred_pre: "-150",
          flu_rt: "-9.09",
          open_pric: "+1650",
          high_pric: "+1700",
          low_pric: "+1450",
        };
        const row = inquirePriceToQuoteRow("052670", jeil);
        expect(row.price).toBe("1500"); // 절댓값
        expect(row.change_amount).toBe("-150"); // 부호 유지
        expect(row.change_rate).toBe("-9.09");
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar &amp;&amp; pnpm -F server test -- mappers/stock.test.ts</automated>
  </verify>
  <done>
    - mappers/stock.ts 의 inquirePriceToQuoteRow 가 KiwoomKa10001Row 입력 받음
    - 기존 rowToStock + mergeMasterAndQuote 무변경
    - 4 신규 테스트 케이스 green (정상 매핑 / volume·trade_amount omit / ETF 빈 문자열 / 음수)
    - 기존 5 rowToStock 테스트 green 유지
  </done>
</task>

<task type="auto">
  <name>Task 3: routes/stocks.ts + app.ts + server.ts + config.ts + kiwoom-runtime.ts 갱신 + stock-detail route test 갱신</name>
  <files>server/src/routes/stocks.ts, server/src/app.ts, server/src/server.ts, server/src/config.ts, server/src/services/kiwoom-runtime.ts, server/src/services/kis-runtime.ts, server/tests/routes/stock-detail.test.ts</files>
  <read_first>
    - `server/src/routes/stocks.ts` (import + GET /:code 의 fetchInquirePrice 호출 시그니처)
    - `server/src/app.ts` (deps.kisClient + req.app.locals.kisClient 패턴)
    - `server/src/server.ts` (createKisRuntime 호출)
    - `server/src/config.ts` (KIS env 정의)
    - `server/src/services/kis-runtime.ts` (mirror)
    - `server/tests/routes/stock-detail.test.ts` (기존 KIS 테스트 시그니처)
  </read_first>
  <action>
    Step 1: `server/src/services/kiwoom-runtime.ts` 신규 — kis-runtime.ts mirror:
    ```typescript
    import type { AxiosInstance } from "axios";
    import type { SupabaseClient } from "@supabase/supabase-js";
    import type { AppConfig } from "../config.js";
    import { createKiwoomClient } from "../kiwoom/client.js";
    import { getKiwoomToken } from "../kiwoom/tokenStore.js";
    import { logger } from "../logger.js";

    /**
     * server 측 키움 runtime — Phase 09.1 D-17/D-18.
     * server 부팅 시 1회 토큰 발급 + axios client 반환.
     * 토큰 만료 5분 전 자동 refresh 는 매 요청 시 getKiwoomToken 재호출 (server.ts 가 client 만 보유, 매 inquirePrice 호출 시 token 재조회 — Plan 04/05 의 stateless pattern).
     */
    export async function createKiwoomRuntime(
      config: AppConfig,
      supabase: SupabaseClient,
    ): Promise<{ client: AxiosInstance; getToken: () => Promise<string> }> {
      // 부팅 시 sanity check — 토큰 발급 시도
      const initial = await getKiwoomToken(supabase, config);
      logger.info({ tokenLen: initial.accessToken.length, expiresAt: initial.expiresAt.toISOString() }, "Kiwoom runtime ready");

      const client = createKiwoomClient(config.kiwoomBaseUrl);

      // 매 요청 시 토큰 재조회 (캐시 hit 시 axios 미호출, 만료 임박 시 refresh)
      const getToken = async (): Promise<string> => {
        const t = await getKiwoomToken(supabase, config);
        return t.accessToken;
      };

      return { client, getToken };
    }
    ```

    Step 2: `server/src/config.ts` 갱신 — KIS env 유지 (Wave 4 cleanup 까지) + KIWOOM env 추가:
    ```typescript
    export type AppConfig = {
      // ... 기존 필드 모두 유지
      nodeEnv: "development" | "test" | "production";
      port: number;
      logLevel: string;
      appVersion: string;
      supabaseUrl: string;
      supabaseServiceRoleKey: string;
      corsAllowedOrigins: string;

      // KIS — Wave 4 cleanup 까지 유지 (런타임 미사용, dead config)
      kisBaseUrl: string;
      kisAppKey: string;
      kisAppSecret: string;

      // Kiwoom — Phase 09.1 신규 (D-17 server 키움 통합)
      kiwoomBaseUrl: string;
      kiwoomAppkey: string;
      kiwoomSecretkey: string;
      kiwoomTokenType: string;

      naverClientId: string | undefined;
      naverClientSecret: string | undefined;
      naverBaseUrl: string;
      naverDailyBudget: number;
      brightdataApiKey: string | undefined;
      brightdataZone: string;
      brightdataUrl: string;
      discussionDailyBudget: number;
      discussionRefreshCooldownSeconds: number;
      anthropicApiKey: string | null;
      classifyConcurrency: number;
      classifyModel: string;
    };

    export function loadConfig(): AppConfig {
      const get = (k: string): string => {
        const v = process.env[k];
        if (!v) throw new Error(`${k} must be set`);
        return v;
      };
      const optional = (k: string): string | undefined => process.env[k];

      return {
        nodeEnv: (process.env.NODE_ENV ?? "development") as AppConfig["nodeEnv"],
        port: Number(process.env.PORT ?? 8080),
        logLevel: process.env.LOG_LEVEL ?? "info",
        appVersion: process.env.APP_VERSION ?? "dev",
        supabaseUrl: get("SUPABASE_URL"),
        supabaseServiceRoleKey: get("SUPABASE_SERVICE_ROLE_KEY"),
        corsAllowedOrigins: process.env.CORS_ALLOWED_ORIGINS ?? "",
        // KIS — dead config (Wave 4 cleanup 까지 startup throw 회피 위해 optional 화)
        kisBaseUrl: process.env.KIS_BASE_URL ?? "https://openapi.koreainvestment.com:9443",
        kisAppKey: optional("KIS_APP_KEY") ?? "",
        kisAppSecret: optional("KIS_APP_SECRET") ?? "",
        // Kiwoom — Phase 09.1 신규
        kiwoomBaseUrl: process.env.KIWOOM_BASE_URL ?? "https://api.kiwoom.com",
        kiwoomAppkey: get("KIWOOM_APPKEY"),
        kiwoomSecretkey: get("KIWOOM_SECRETKEY"),
        kiwoomTokenType: process.env.KIWOOM_TOKEN_TYPE ?? "live",
        // 기타 (무변경)
        naverClientId: process.env.NAVER_CLIENT_ID,
        naverClientSecret: process.env.NAVER_CLIENT_SECRET,
        naverBaseUrl: process.env.NAVER_BASE_URL ?? "https://openapi.naver.com",
        naverDailyBudget: Number(process.env.NAVER_DAILY_BUDGET ?? "24500"),
        brightdataApiKey: process.env.BRIGHTDATA_API_KEY,
        brightdataZone: process.env.BRIGHTDATA_ZONE ?? "gh_radar_naver",
        brightdataUrl: process.env.BRIGHTDATA_URL ?? "https://api.brightdata.com/request",
        discussionDailyBudget: Number(process.env.DISCUSSION_DAILY_BUDGET ?? "5000"),
        discussionRefreshCooldownSeconds: Number(process.env.DISCUSSION_REFRESH_COOLDOWN_SECONDS ?? "30"),
        anthropicApiKey: process.env.ANTHROPIC_API_KEY ?? null,
        classifyConcurrency: Number(process.env.DISCUSSION_CLASSIFY_CONCURRENCY ?? "5"),
        classifyModel: process.env.DISCUSSION_CLASSIFY_MODEL ?? "claude-haiku-4-5",
      };
    }
    ```

    Step 3: `server/src/services/kis-runtime.ts` — 본 plan 에서 그대로 두되 server.ts 가 호출 안 함 (dead code). Wave 4 cleanup 에서 삭제. 변경 없음.

    Step 4: `server/src/app.ts` 갱신 — `kisClient` → `kiwoomRuntime` 으로 deps 교체. 기존 `naverClient`, `brightdataClient` 등은 무변경:
    ```typescript
    import express, { type Express } from "express";
    import helmet from "helmet";
    import cors from "cors";
    import type { SupabaseClient } from "@supabase/supabase-js";
    import type { AxiosInstance } from "axios";
    import { requestId } from "./middleware/request-id.js";
    import { apiRateLimiter } from "./middleware/rate-limit.js";
    import { httpLogger } from "./middleware/pino-http.js";
    import { errorHandler } from "./middleware/error-handler.js";
    import { notFoundHandler } from "./middleware/not-found.js";
    import { corsOptions } from "./services/cors-config.js";
    import { healthRouter } from "./routes/health.js";
    import { scannerRouter } from "./routes/scanner.js";
    import { stocksRouter } from "./routes/stocks.js";

    export type KiwoomRuntime = {
      client: AxiosInstance;
      getToken: () => Promise<string>;
    };

    export type AppDeps = {
      supabase: SupabaseClient;
      // Phase 09.1: kisClient → kiwoomRuntime 으로 교체 (D-17)
      kiwoomRuntime?: KiwoomRuntime;
      naverClient?: AxiosInstance;
      brightdataClient?: AxiosInstance;
      brightdataApiKey?: string;
      brightdataZone?: string;
    };

    export function createApp(deps: AppDeps): Express {
      const app = express();
      app.set("trust proxy", 1);

      app.locals.supabase = deps.supabase;
      app.locals.kiwoomRuntime = deps.kiwoomRuntime;
      app.locals.naverClient = deps.naverClient;
      app.locals.brightdataClient = deps.brightdataClient;
      app.locals.brightdataApiKey = deps.brightdataApiKey;
      app.locals.brightdataZone = deps.brightdataZone;

      app.use(requestId());
      app.use(httpLogger());
      app.use(helmet());
      app.use(cors(corsOptions()));
      app.use(express.json({ limit: "16kb" }));
      app.use("/api", apiRateLimiter());

      app.use("/api/health", healthRouter);
      app.use("/api/scanner", scannerRouter);
      app.use("/api/stocks", stocksRouter);

      app.use(notFoundHandler);
      app.use(errorHandler);

      return app;
    }
    ```

    Step 5: `server/src/server.ts` 갱신 — createKisRuntime 호출 제거 + createKiwoomRuntime 호출 추가:
    ```typescript
    import axios from "axios";
    import { createApp, type KiwoomRuntime } from "./app.js";
    import { supabase } from "./services/supabase.js";
    import { logger } from "./logger.js";
    import { loadConfig } from "./config.js";
    import { createKiwoomRuntime } from "./services/kiwoom-runtime.js";

    const config = loadConfig();

    let kiwoomRuntime: KiwoomRuntime | undefined = undefined;
    try {
      kiwoomRuntime = await createKiwoomRuntime(config, supabase);
    } catch (err) {
      logger.error(
        { err },
        "Kiwoom runtime init failed — /api/stocks/:code 폴백 모드 (cached only)",
      );
    }

    const naverClient =
      config.naverClientId && config.naverClientSecret
        ? axios.create({
            baseURL: config.naverBaseUrl,
            timeout: 15000,
            headers: {
              "X-Naver-Client-Id": config.naverClientId,
              "X-Naver-Client-Secret": config.naverClientSecret,
              Accept: "application/json",
            },
          })
        : undefined;
    if (!naverClient) {
      logger.warn("NAVER_CLIENT_ID/SECRET not set — POST /news/refresh will return 503");
    }

    let brightdataClient = undefined;
    if (config.brightdataApiKey) {
      if (!config.brightdataUrl.startsWith("https://")) {
        throw new Error(`BRIGHTDATA_URL must be https (got: ${config.brightdataUrl})`);
      }
      brightdataClient = axios.create({
        baseURL: config.brightdataUrl,
        timeout: 30_000,
        headers: { "User-Agent": `gh-radar-server/${config.appVersion}` },
      });
    } else {
      logger.warn("BRIGHTDATA_API_KEY not set — POST /discussions/refresh will return 503");
    }

    const app = createApp({
      supabase,
      kiwoomRuntime,
      naverClient,
      brightdataClient,
      brightdataApiKey: config.brightdataApiKey,
      brightdataZone: config.brightdataZone,
    });

    app.listen(config.port, () => {
      logger.info({ port: config.port, env: config.nodeEnv, version: config.appVersion }, "gh-radar-server listening");
    });

    for (const sig of ["SIGTERM", "SIGINT"] as const) {
      process.on(sig, () => {
        logger.info({ signal: sig }, "shutting down");
        process.exit(0);
      });
    }
    ```

    Step 6: `server/src/routes/stocks.ts` 갱신 — GET /:code 의 kisClient → kiwoomRuntime 으로 교체:
    ```typescript
    // 기존 import:
    // import { fetchInquirePrice } from "../kis/inquirePrice.js";
    // 본 plan 변경:
    import { fetchInquirePrice } from "../kiwoom/inquirePrice.js";
    import type { KiwoomRuntime } from "../app.js";
    import type { StockQuoteRowUpsert } from "../mappers/stock.js";  // Phase 09.1 D-22

    // GET /:code 핸들러 안 (기존 코드의 kisClient 부분만 교체):
    // 기존: const kisClient = req.app.locals.kisClient as AxiosInstance | undefined;
    //       if (kisClient) { const price = await fetchInquirePrice(kisClient, code); ... }
    // 변경:
    const kiwoomRuntime = req.app.locals.kiwoomRuntime as KiwoomRuntime | undefined;

    // Phase 09.1 D-22 — partial upsert (volume/trade_amount 키 미포함, STEP1 worker 갱신값 보존)
    let freshQuote: StockQuoteRowUpsert | null = null;
    if (kiwoomRuntime) {
      try {
        const token = await kiwoomRuntime.getToken();
        const ka10001Row = await fetchInquirePrice(kiwoomRuntime.client, token, code);
        freshQuote = inquirePriceToQuoteRow(code, ka10001Row);
        // upsert (실패해도 응답 우선) — Supabase 가 명시 컬럼만 SET (volume/trade_amount 미터치)
        const { error: upErr } = await supabase
          .from("stock_quotes")
          .upsert(freshQuote, { onConflict: "code" });
        if (upErr) {
          logger.warn(
            { code, err: upErr },
            "stock_quotes upsert failed (continuing with fresh quote)",
          );
        }
      } catch (err) {
        logger.warn(
          { code, err: (err as Error).message },
          "kiwoom inquirePrice failed — fall back to cached quote",
        );
      }
    }
    ```

    실제 파일은 GET /:code 핸들러 안의 위 블록만 정확히 교체. 나머지 (search, news, discussion 라우터 마운트, master/quote merge) 무변경.

    Step 7: `server/tests/routes/stock-detail.test.ts` 갱신 — `kisClient` mock 을 `kiwoomRuntime` mock 으로 교체.

    기존 테스트가 `kisClient: { get: ... }` 패턴으로 mock 한다면, 본 plan 에서 다음으로 교체:
    ```typescript
    const kiwoomRuntime = {
      client: { post: vi.fn().mockResolvedValue({
        data: { return_code: 0, stk_cd: "005930", cur_prc: "+70500", open_pric: "+70000", high_pric: "+71000", low_pric: "+69500", upl_pric: "91000", lst_pric: "49000", mac: "4209000" },
      }) },
      getToken: vi.fn().mockResolvedValue("TEST_TOKEN"),
    };
    const app = createApp({ supabase, kiwoomRuntime: kiwoomRuntime as any });
    ```

    핵심 테스트 case 갱신 (kiwoomRuntime call verification):
    - "GET /api/stocks/005930 → 200 + fresh kiwoom quote"
    - "kiwoom 호출 실패 → cached quote fallback"
    - "마스터 없음 → 404"

    실제 작업: 기존 stock-detail.test.ts 파일 read + `kisClient` 문자열 → `kiwoomRuntime` 객체 교체 + mock 응답 패턴 KiwoomKa10001Row 로 변경. 기존 테스트 시나리오 의도 유지.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar &amp;&amp; pnpm -F server typecheck &amp;&amp; pnpm -F server test &amp;&amp; pnpm -F server build</automated>
  </verify>
  <done>
    - app.ts / server.ts / config.ts / routes/stocks.ts / kiwoom-runtime.ts 갱신
    - 기존 server/src/kis/* 파일 무변경 (Wave 4 cleanup 대상)
    - server tests 모두 green
    - typecheck + build exit 0
    - GET /api/stocks/:code 가 kiwoomRuntime 미주입 시에도 cached fallback 으로 200 응답 (기존 패턴 유지)
  </done>
</task>

</tasks>

<verification>
- `pnpm -F server typecheck && pnpm -F server test && pnpm -F server build` 모두 exit 0
- `grep -r "from \"../kis/" server/src/ | wc -l` — 모든 production import 가 kiwoom 으로 교체됨 (dead file 잔존)
- `grep -r "kiwoomRuntime\|fetchInquirePrice" server/src/routes/stocks.ts` 가 키움 사용 확인
- 기존 KIS 테스트 파일 (`server/tests/kis/*`) 은 본 plan 에서 건드리지 않음 — Wave 4 cleanup 에서 삭제
</verification>

<success_criteria>
- server 가 production deploy 시 KIS_APP_KEY/KIS_APP_SECRET 없어도 부팅 (kis env optional 화 — Wave 4 cleanup 까지 transition)
- KIWOOM_APPKEY/KIWOOM_SECRETKEY 가 deploy env 에 주입되면 server 가 kiwoomRuntime 부팅
- /api/stocks/:code 가 kiwoom 호출 → KiwoomKa10001Row → StockQuoteRow → UI 의 종목 상세 페이지 즉시 응답
- Wave 3 의 server VPC 재배포 + Wave 4 의 KIS env 정리 후 dead code 완전 제거 (Plan 11)
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-intraday-current-price/09.1-07-SUMMARY.md`
</output>
