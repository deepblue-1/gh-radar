---
phase: 09.1-intraday-current-price
plan: 06
type: execute
wave: 3
depends_on:
  - 09.1-03
  - 09.1-05
files_modified:
  - .planning/phases/09.1-intraday-current-price/09.1-01-MIGRATION-VERIFY.md
  - .planning/phases/09.1-intraday-current-price/09.1-03-API-VERIFICATION.md
  - .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements:
  - DATA-02
requirements_addressed:
  - DATA-02

must_haves:
  truths:
    - "[BLOCKING] Plan 01 마이그레이션 (intraday_upsert_close RPC) 가 production Supabase 에 push — DATA-02 SC #2"
    - "[BLOCKING] Plan 05 setup-intraday-sync-iam.sh 실행 — VPC/NAT/Static IP + runtime SA + KIWOOM secret 신설 + secret accessor + compute.networkUser — DATA-02 SC #4"
    - "[BLOCKING] 사용자 액션 — KIWOOM secret 2개 version 추가 (echo -n 'VALUE' | gcloud secrets versions add ... --data-file=-)"
    - "[BLOCKING] 사용자 액션 — 키움 openapi.kiwoom.com 화이트리스트에 Static IP 등록 — DATA-02 SC #5"
    - "[BLOCKING] Plan 05 deploy-intraday-sync.sh 실행 — Docker build/push + Cloud Run Job + Cloud Scheduler + run.invoker — DATA-02 SC #3"
    - "[BLOCKING] alert policy 등록 — `gcloud alpha monitoring policies create --policy-from-file=ops/alert-intraday-sync-failure.yaml` (notification channel ID sed 치환 후)"
    - "[BLOCKING] smoke INV-1~6 PASS — Cloud Run Job + Scheduler ENABLED + Static IP + Subnet + NAT + alert policy 모두 존재"
    - "[BLOCKING] 첫 cycle manual execute (`gcloud run jobs execute gh-radar-intraday-sync --wait`) + 응답 캡처 — R2/R3 검증 (now_trde_qty 누적 + token expires 기록) + Supabase rows_affected ≥ 1500 검증 — DATA-02 SC #5"
    - "MIGRATION-VERIFY.md + API-VERIFICATION.md 실측값 paste — Plan 01/03 templates 갱신"
    - "DEPLOY-LOG.md 작성 — image SHA + Job/Scheduler/alert policy ID + Static IP + 첫 cycle 결과 (rows_affected, duration_ms, sample 005930 row)"
    - "REQUIREMENTS.md DATA-02 status: Pending → Complete"
    - "ROADMAP.md Phase 09.1 plans 6개 모두 체크 + Completed 날짜 기입"
    - "STATE.md 갱신 — Phase 09.1 Complete + Phase 10 으로 focus 이동"
  artifacts:
    - path: ".planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md"
      provides: "production 배포 + 첫 cycle 결과 + 사용자 액션 확인 기록"
      contains: "DATA-02"
    - path: ".planning/phases/09.1-intraday-current-price/09.1-01-MIGRATION-VERIFY.md"
      provides: "RPC production push 검증 실값"
      contains: "service_role"
    - path: ".planning/phases/09.1-intraday-current-price/09.1-03-API-VERIFICATION.md"
      provides: "R2 (now_trde_qty) + R3 (token expires) 실측 결과"
      contains: "expires_dt"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DATA-02 status Complete"
      contains: "DATA-02"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 09.1 plans 6/6 + Completed"
      contains: "09.1-06-PLAN"
  key_links:
    - from: "[BLOCKING] supabase db push"
      to: "RPC intraday_upsert_close production 적용"
      via: "supabase db push (Plan 01 마이그레이션)"
      pattern: "intraday_upsert_close"
    - from: "[BLOCKING] 사용자 IP 화이트리스트 등록"
      to: "키움 ka10027 401 회피"
      via: "Static IP echo → 키움 마이페이지 등록"
      pattern: "Static IP"
    - from: "[BLOCKING] 첫 cycle manual execute"
      to: "stock_daily_ohlcv 오늘자 row UPDATE"
      via: "gcloud run jobs execute gh-radar-intraday-sync --wait"
      pattern: "rows_affected"
---

<objective>
Phase 09.1 의 production 배포 + 사용자 액션 + 첫 cycle 검증 + 모든 문서 갱신. 본 plan 은 **모든 BLOCKING checkpoint** 가 포함되어 `autonomous: false` — 사용자 confirm 후 진행.

본 plan 의 task 시퀀스:
1. **[BLOCKING] Supabase 마이그레이션 production push** (autonomous:false — production DB 변경)
2. **[BLOCKING] IAM setup 스크립트 실행** (VPC/NAT/Static IP 생성)
3. **[BLOCKING:USER-ACTION] KIWOOM secret version 추가 + 키움 화이트리스트에 Static IP 등록** (외부 시스템)
4. **[BLOCKING] deploy 스크립트 실행 + alert policy 등록** (Docker build + Cloud Run Job + Scheduler)
5. **[BLOCKING] smoke + 첫 cycle manual execute + R2/R3 검증**
6. **DEPLOY-LOG + MIGRATION-VERIFY + API-VERIFICATION 실값 paste**
7. **REQUIREMENTS/ROADMAP/STATE 갱신** — DATA-02 Complete + Phase 09.1 close

Purpose:
- DATA-02 5개 SC 모두 production 검증 (SC #1~#6)
- R2 mitigation (now_trde_qty 누적 검증)
- R3 mitigation (token expires_dt 측정)
- R4 mitigation (RPC INSERT 분기 자연 검증 — 첫 cycle 오늘자 row 부재 시 INSERT 동작 확인)
- Phase 09.1 완료 sign-off

Output:
- DEPLOY-LOG.md (신규)
- MIGRATION-VERIFY.md + API-VERIFICATION.md (Plan 01/03 templates 갱신)
- REQUIREMENTS.md (DATA-02 Pending → Complete)
- ROADMAP.md (Phase 09.1 plans 6/6 + Completed 날짜)
- STATE.md (current focus → Phase 10)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md
@.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md

# Plan 01~05 산출물 (의존)
@supabase/migrations/20260514120000_intraday_upsert_close_rpc.sql
@.planning/phases/09.1-intraday-current-price/09.1-01-MIGRATION-VERIFY.md
@.planning/phases/09.1-intraday-current-price/09.1-03-API-VERIFICATION.md
@workers/intraday-sync/Dockerfile
@workers/intraday-sync/src/index.ts
@scripts/setup-intraday-sync-iam.sh
@scripts/deploy-intraday-sync.sh
@scripts/smoke-intraday-sync.sh
@ops/alert-intraday-sync-failure.yaml

# Mirror — Phase 9 DEPLOY-LOG 패턴
@.planning/phases/09-daily-candle-data/09-DEPLOY-LOG.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Supabase 마이그레이션 production push — intraday_upsert_close RPC</name>
  <files>
    .planning/phases/09.1-intraday-current-price/09.1-01-MIGRATION-VERIFY.md
  </files>

  <read_first>
    - supabase/migrations/20260514120000_intraday_upsert_close_rpc.sql (Plan 01 산출)
    - .planning/phases/09.1-intraday-current-price/09.1-01-MIGRATION-VERIFY.md (Plan 01 템플릿)
    - Phase 9 의 09-DEPLOY-LOG.md (production push 패턴)
  </read_first>

  <action>
**What's being built / executed:**

Plan 01 의 마이그레이션 SQL — `intraday_upsert_close(jsonb)` RPC + REVOKE/GRANT 명시.
    본 task 가 production Supabase 에 푸시 + 4 검증 (시그니처 / SECURITY DEFINER / role_routine_grants / INSERT-UPDATE 분기 smoke) 후 09.1-01-MIGRATION-VERIFY.md 에 실값 paste.

**Verification steps (executor performs):**

1. **production push:**
   ```bash
   # supabase CLI (link 된 상태 가정 — Phase 9 setup 승계)
   supabase db push
   ```
   또는 직접 psql:
   ```bash
   psql "$SUPABASE_DB_URL" -f supabase/migrations/20260514120000_intraday_upsert_close_rpc.sql
   ```
   exit 0 확인.

2. **RPC 시그니처 확인:**
   ```sql
   SELECT pg_get_functiondef('public.intraday_upsert_close(jsonb)'::regprocedure);
   ```
   → SECURITY DEFINER + SET search_path = public + LANGUAGE plpgsql 모두 확인.

3. **권한 확인 (T-6 — 가장 중요):**
   ```sql
   SELECT grantee, privilege_type
   FROM information_schema.role_routine_grants
   WHERE routine_schema = 'public' AND routine_name = 'intraday_upsert_close'
   ORDER BY grantee;
   ```
   기대: `service_role | EXECUTE` 1행 만. anon / authenticated / PUBLIC 행 **없음**.
   → 행이 1개 초과면 즉시 ROLLBACK + 디버그.

4. **INSERT 분기 smoke (오늘자 row 부재 시):**
   ```sql
   SELECT public.intraday_upsert_close('[{"code":"999999","date":"2099-01-01","close":1000,"volume":100,"change_amount":50,"change_rate":5.0}]'::jsonb);
   SELECT open, high, low, close, volume, trade_amount, change_amount, change_rate
     FROM public.stock_daily_ohlcv WHERE code='999999' AND date='2099-01-01';
   -- 기대: open=1000.00, high=1000.00, low=1000.00, close=1000.00, trade_amount=0, change_amount=50, change_rate=5.0
   DELETE FROM public.stock_daily_ohlcv WHERE code='999999';
   ```

5. **UPDATE 분기 smoke (high/low 단조):**
   ```sql
   -- close=1500 → high=1500
   SELECT public.intraday_upsert_close('[{"code":"999999","date":"2099-01-01","close":1000,"volume":100,"change_amount":0,"change_rate":0}]'::jsonb);
   SELECT public.intraday_upsert_close('[{"code":"999999","date":"2099-01-01","close":1500,"volume":200,"change_amount":500,"change_rate":50}]'::jsonb);
   SELECT high, low, close FROM public.stock_daily_ohlcv WHERE code='999999' AND date='2099-01-01';
   -- 기대: high=1500, low=1000, close=1500
   -- close=800 → low=800 (high 1500 보존)
   SELECT public.intraday_upsert_close('[{"code":"999999","date":"2099-01-01","close":800,"volume":300,"change_amount":-200,"change_rate":-20}]'::jsonb);
   SELECT high, low, close FROM public.stock_daily_ohlcv WHERE code='999999' AND date='2099-01-01';
   -- 기대: high=1500, low=800, close=800
   DELETE FROM public.stock_daily_ohlcv WHERE code='999999';
   ```

6. **MIGRATION-VERIFY.md 갱신** — Plan 01 의 템플릿에 위 4건 결과 paste + Sign-off 6 항목 체크.
  </action>

  <verify>
    <automated># Checkpoint — manual verification gate. Executor performs steps in <action>, captures evidence into DEPLOY-LOG / VERIFY artifacts, then user issues resume-signal. No standalone automated command — see <action> Step 검증 for the executable evidence.
echo "MANUAL_GATE — checkpoint:human-verify / checkpoint:human-action"</automated>
  </verify>

  <resume-signal>모든 검증 PASS + MIGRATION-VERIFY.md 갱신 완료. 사용자가 "approved" 입력 시 Task 2 진행. 검증 실패 시 ROLLBACK + 사용자 보고.</resume-signal>

  <acceptance_criteria>
    - `supabase db push` 또는 `psql -f` exit 0
    - role_routine_grants 결과: `service_role | EXECUTE` 단일 entry (T-6 — Supabase auto-grant 회귀 확인)
    - INSERT 분기 smoke: open=high=low=close (D-20 옵션 D)
    - UPDATE 분기 smoke: high GREATEST + low LEAST 단조 갱신
    - 09.1-01-MIGRATION-VERIFY.md Sign-off 7항목 모두 체크됨
    - Status: Draft → Verified (또는 동등 표기)
  </acceptance_criteria>

  <done>RPC production 적용 완료 + 4 검증 PASS + MIGRATION-VERIFY.md 실값 paste.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] setup-intraday-sync-iam.sh 실행 + KIWOOM secret version 추가 + 키움 화이트리스트 등록</name>
  <files>
    .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md
  </files>

  <read_first>
    - scripts/setup-intraday-sync-iam.sh (Plan 05 산출 — 실행 대상)
    - .planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md §4.7 (사용자 confirm 항목)
    - $HOME/.claude/projects/-Users-alex-repos-gh-radar/memory/reference_gh_radar_deployer_sa.md (영구 GCP 인증 키)
    - $HOME/.claude/projects/-Users-alex-repos-gh-radar/memory/feedback_dont_ask_existing_creds.md (KIWOOM_APPKEY 위치 — 사용자 보유 명시)
  </read_first>

  <action>
**Step 1: IAM setup 스크립트 실행**
```bash
export GCP_PROJECT_ID=gh-radar
export GOOGLE_APPLICATION_CREDENTIALS=~/.config/gcloud/gh-radar-deployer.json
export CLOUDSDK_CORE_PROJECT=gh-radar
bash scripts/setup-intraday-sync-iam.sh
```

**기대 종료 메시지:**
- VPC stack ready. Static IP for Kiwoom whitelist: `XX.XX.XX.XX`
- USER ACTIONS REQUIRED 안내 메시지 출력

**Step 2: 사용자 — KIWOOM secret version 추가**
- 키움 openapi.kiwoom.com 마이페이지에서 APPKEY/SECRETKEY 확보 (사용자가 이미 보유 — feedback_dont_ask_existing_creds 룰)
- 다음 명령 실행:
  ```bash
  echo -n '<KIWOOM_APPKEY>' | gcloud secrets versions add gh-radar-kiwoom-appkey --data-file=-
  echo -n '<KIWOOM_SECRETKEY>' | gcloud secrets versions add gh-radar-kiwoom-secretkey --data-file=-
  ```
- 검증:
  ```bash
  gcloud secrets versions list gh-radar-kiwoom-appkey --limit=1
  gcloud secrets versions list gh-radar-kiwoom-secretkey --limit=1
  ```
  state=ENABLED 행 1건 확인.

**Step 3: 사용자 — 키움 IP 화이트리스트 등록 (외부 시스템, CLI/API 없음 — 진정한 human-action)**
1. 브라우저로 https://openapi.kiwoom.com/ 접속
2. 로그인 → 마이페이지 → IP 등록 페이지
3. Step 1 의 setup-script 가 출력한 Static IP (예: `XX.XX.XX.XX`) 를 등록
4. 등록 완료 후 키움 시스템에 반영되는데 통상 즉시~수분 소요

**Step 4: DEPLOY-LOG.md 신규 생성 — Step 1~3 기록**

파일 `.planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md` 신규 생성 (Phase 9 09-DEPLOY-LOG.md mirror):

```markdown
# Phase 09.1: Intraday Current Price — Deploy Log

**Phase:** 09.1-intraday-current-price (INSERTED)
**Requirement:** DATA-02
**Deployed (started):** TBD
**Deployed (completed):** TBD

## 0. Pre-deployment Summary

- **Workspace:** `workers/intraday-sync/`
- **Image:** `asia-northeast3-docker.pkg.dev/gh-radar/gh-radar/intraday-sync:<GIT_SHA>`
- **Cloud Run Job:** `gh-radar-intraday-sync` (region=asia-northeast3)
- **Cloud Scheduler:** `gh-radar-intraday-sync` (cron `* 9-15 * * 1-5` Asia/Seoul)
- **Static IP (Cloud NAT):** `XX.XX.XX.XX`
- **Migration:** `supabase/migrations/20260514120000_intraday_upsert_close_rpc.sql`

## 1. Migration Push (Task 1)

| Item | Result |
|------|--------|
| Push command | `supabase db push` |
| Exit code | 0 |
| RPC signature OK | yes |
| role_routine_grants | service_role EXECUTE 단일 entry (T-6 PASS) |
| INSERT smoke | PASS — open=high=low=close=1000 |
| UPDATE smoke | PASS — high GREATEST 1500, low LEAST 800 |

## 2. IAM + VPC + NAT + Secret Setup (Task 2)

| Resource | Name | Status | Notes |
|----------|------|--------|-------|
| VPC | `gh-radar-vpc` | created/exists | custom mode |
| Subnet | `gh-radar-subnet-an3` | created/exists | 10.10.0.0/26 asia-northeast3 |
| Static IP | `gh-radar-intraday-static-ip` | reserved | `XX.XX.XX.XX` |
| Cloud Router | `gh-radar-router-an3` | created/exists | asia-northeast3 |
| Cloud NAT | `gh-radar-nat-an3` | created/exists | Static IP 바인딩 |
| Runtime SA | `gh-radar-intraday-sync-sa@...` | created | min-permission |
| Secret KIWOOM_APPKEY | `gh-radar-kiwoom-appkey` | created + version added | 사용자 액션 |
| Secret KIWOOM_SECRETKEY | `gh-radar-kiwoom-secretkey` | created + version added | 사용자 액션 |
| Secret Supabase | `gh-radar-supabase-service-role` | reused (Phase 05.1) | accessor 추가 |
| compute.networkUser | Cloud Run Service Agent + runtime SA | granted | RESEARCH §4.5 |

## 3. Kiwoom Whitelist Registration (Task 2 — USER ACTION)

| Item | Value |
|------|-------|
| Static IP | `XX.XX.XX.XX` |
| 등록 시각 (KST) | TBD |
| 등록 확인 방법 | 첫 cycle 401 없이 통과 (Task 5) |

## 4. Cloud Run Job + Scheduler Deploy (Task 3)

(Task 3 채움)

## 5. Alert Policy (Task 3)

(Task 3 채움)

## 6. First Cycle Manual Execute (Task 4)

(Task 4 채움)

## 7. R2 / R3 검증 결과 (Task 4)

(Task 4 채움)
```

USER ACTION 완료 후 본 plan 의 다음 task 로 진행.
  </action>

  <verify>
    <automated># Checkpoint:human-action — manual gate. Executor performs setup script + USER actions (secret version add + 키움 IP whitelist), captures Static IP into DEPLOY-LOG sections 1-3. Resume-signal gates progression.
echo "MANUAL_GATE — checkpoint:human-action (USER must register Static IP at openapi.kiwoom.com)"</automated>
  </verify>

  <resume-signal>(a) `bash scripts/setup-intraday-sync-iam.sh` exit 0 + Static IP echo 캡처, (b) `gcloud secrets versions list` 가 두 secret 모두 ENABLED 1건 보고, (c) 사용자가 키움 마이페이지에 Static IP 등록 완료 후 "approved" 입력 시 Task 3 진행.</resume-signal>

  <acceptance_criteria>
    - setup-intraday-sync-iam.sh exit 0
    - VPC/Subnet/Static IP/Router/NAT/Runtime SA 모두 GCP 에 생성 (smoke INV-3/4/5 가 사후 검증)
    - KIWOOM secret 2개 version 추가 완료 (gcloud secrets versions list 가 ENABLED 1건)
    - 사용자가 키움 화이트리스트에 Static IP 등록 (외부 시스템 — 명시적 사용자 confirm 필수)
    - 09.1-DEPLOY-LOG.md 신규 생성 + section 1, 2, 3 채움
  </acceptance_criteria>

  <done>IAM + VPC + NAT + Static IP 인프라 + KIWOOM secret + 키움 화이트리스트 등록 완료.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] deploy-intraday-sync.sh 실행 + alert policy 등록</name>
  <files>
    .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md
  </files>

  <read_first>
    - scripts/deploy-intraday-sync.sh (Plan 05 산출 — 실행 대상)
    - ops/alert-intraday-sync-failure.yaml (Plan 05 산출 — alert policy)
    - $HOME/.claude/projects/-Users-alex-repos-gh-radar/memory/reference_gh_radar_deployer_sa.md
  </read_first>

  <action>
**What's being built / executed:**

Plan 05 의 deploy 스크립트 — Docker build + push + Cloud Run Job 배포 + run.invoker 바인딩 + Cloud Scheduler 생성. 본 task 가 production 에 실행 + alert policy 등록 + DEPLOY-LOG section 4/5 갱신.

**Verification steps (executor performs):**

**Step 1: deploy 스크립트 실행**
```bash
export GCP_PROJECT_ID=gh-radar
export GOOGLE_APPLICATION_CREDENTIALS=~/.config/gcloud/gh-radar-deployer.json
export CLOUDSDK_CORE_PROJECT=gh-radar
export SUPABASE_URL="<production supabase url — Phase 9 env 와 동일>"
bash scripts/deploy-intraday-sync.sh
```

**기대 결과:**
- Docker build/push 성공 → `asia-northeast3-docker.pkg.dev/gh-radar/gh-radar/intraday-sync:<GIT_SHA>`
- Cloud Run Job `gh-radar-intraday-sync` deployed
- run.invoker bound
- Cloud Scheduler `gh-radar-intraday-sync` ENABLED (cron `* 9-15 * * 1-5` Asia/Seoul)

**Step 2: alert policy 등록** (notification channel placeholder 치환 후)
```bash
# 1) Phase 05.1 의 alex@jx1.io 채널 ID 조회
CHANNEL_ID=$(gcloud alpha monitoring channels list --filter="labels.email_address=alex@jx1.io" --format='value(name.basename())' | head -1)
echo "channel: $CHANNEL_ID"

# 2) YAML 치환 + apply (idempotent: describe → update, 없으면 create)
sed -e "s/\${PROJECT_ID}/$GCP_PROJECT_ID/g" -e "s/\${NOTIFICATION_CHANNEL_ID}/$CHANNEL_ID/g" \
  ops/alert-intraday-sync-failure.yaml > /tmp/intraday-alert.yaml

POLICY_ID=$(gcloud alpha monitoring policies list --filter="displayName='gh-radar-intraday-sync-failure'" --format='value(name)' | head -1)
if [[ -z "$POLICY_ID" ]]; then
  gcloud alpha monitoring policies create --policy-from-file=/tmp/intraday-alert.yaml
else
  gcloud alpha monitoring policies update "$POLICY_ID" --policy-from-file=/tmp/intraday-alert.yaml
fi
```

**Step 3: smoke INV-1~6 실행**
```bash
bash scripts/smoke-intraday-sync.sh
```
exit 0 + `PASS=6, FAIL=0` 출력 확인.

**Step 4: DEPLOY-LOG.md section 4/5 갱신** — image SHA, Job/Scheduler 리소스 이름, alert policy ID, smoke 결과 paste.
  </action>

  <verify>
    <automated># Checkpoint — manual verification gate. Executor performs steps in <action>, captures evidence into DEPLOY-LOG / VERIFY artifacts, then user issues resume-signal. No standalone automated command — see <action> Step 검증 for the executable evidence.
echo "MANUAL_GATE — checkpoint:human-verify / checkpoint:human-action"</automated>
  </verify>

  <resume-signal>`smoke-intraday-sync.sh` 가 `PASS=6, FAIL=0` 출력 + DEPLOY-LOG section 4/5 갱신 완료 시 "approved". 실패 시 디버그 후 재실행.</resume-signal>

  <acceptance_criteria>
    - Docker image push 성공 (`gcloud artifacts docker images list` 가 신규 image 보고)
    - Cloud Run Job `gh-radar-intraday-sync` describe 성공 (image SHA == 방금 push 한 것)
    - Cloud Scheduler state=ENABLED + cron `* 9-15 * * 1-5` + timezone Asia/Seoul
    - run.invoker 정책에 `gh-radar-scheduler-sa` 포함
    - Cloud Run Job spec 의 vpcAccess egress=ALL_TRAFFIC + network=gh-radar-vpc + subnetwork=gh-radar-subnet-an3
    - alert policy `gh-radar-intraday-sync-failure` ENABLED + notification channel bound
    - `smoke-intraday-sync.sh` exit 0 + PASS=6 FAIL=0
    - DEPLOY-LOG section 4 (Cloud Run Job + Scheduler) + section 5 (alert policy) 실값 paste
  </acceptance_criteria>

  <done>Production 인프라 fully deployed + smoke 6/6 PASS + alert policy active.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] 첫 cycle manual execute + R2/R3 검증 + Supabase rows_affected 확인</name>
  <files>
    .planning/phases/09.1-intraday-current-price/09.1-03-API-VERIFICATION.md,
    .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md
  </files>

  <read_first>
    - scripts/smoke-intraday-sync.sh (--check-cycle 플래그)
    - .planning/phases/09.1-intraday-current-price/09.1-03-API-VERIFICATION.md (Plan 03 템플릿 — R2/R3)
    - .planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md §1.6 (R2 검증), §5.3 (R3 검증)
  </read_first>

  <action>
**What's being built / executed:**

production Cloud Run Job 의 첫 manual cycle — Plan 04 의 runIntradayCycle 이 키움 ka10027 호출 + stock_daily_ohlcv 갱신.
    본 task 가 첫 cycle 의 logs 캡처 + R2/R3 검증 + 09.1-03-API-VERIFICATION.md 실값 paste + DEPLOY-LOG section 6/7 갱신.

**Verification steps (executor performs):**

**Step 1: 첫 cycle 시각 결정**
- 평일 09:00~15:30 KST 안에서 실행 권장 (장중 — 의미있는 응답)
- 장 외 시간 실행 시 ka10027 응답이 0 row → cycle 정상 종료지만 R2/R3 의 실측 검증은 불가 → 평일 장중 권장

**Step 2: 첫 cycle manual execute**
```bash
bash scripts/smoke-intraday-sync.sh --check-cycle
# 또는
gcloud run jobs execute gh-radar-intraday-sync --region=asia-northeast3 --wait
```

**Step 3: cycle logs 캡처**
```bash
gcloud logging read 'resource.type=cloud_run_job AND resource.labels.job_name=gh-radar-intraday-sync' \
  --limit=100 --format=json --freshness=10m > /tmp/intraday-cycle-1.json
# 필드 추출: fetched_rows, mapped_rows, skipped_rows, rpc_calls, rows_affected, duration_ms
```

**Step 4: Supabase 검증**
```sql
-- 1. 오늘자 row 수 (활성 종목 갱신 = 매핑된 rows)
SELECT COUNT(*) FROM stock_daily_ohlcv WHERE date = CURRENT_DATE;
-- 기대: ≥ 1500 (MIN_EXPECTED_ROWS)

-- 2. 005930 (삼성전자) 의 오늘자 row
SELECT code, date, open, high, low, close, volume, trade_amount, change_amount, change_rate, inserted_at
FROM stock_daily_ohlcv WHERE code='005930' AND date = CURRENT_DATE;
-- 기대 (장중): open=high=low=close=<현재가> (D-20 INSERT 분기), volume>0, trade_amount=0 (EOD 가 채움), inserted_at 최근

-- 3. inserted_at 분포 — 모든 row 가 cycle 시점에 갱신됐는지
SELECT inserted_at::date AS d, COUNT(*) FROM stock_daily_ohlcv
WHERE date = CURRENT_DATE GROUP BY d;
```

**Step 5: R3 검증 — token expires_dt 측정**
- cycle logs 의 `"kiwoom token issued"` 항목에서 `expiresAt` 값 캡처
- cycle 시작 시각 (timestamp) 과 expiresAt 차이 측정 (분 단위)
- 결과를 09.1-03-API-VERIFICATION.md R3 섹션에 paste — 30분 / 24h / 기타 확정

**Step 6: R2 검증 — now_trde_qty 누적 여부**
1. 첫 cycle 의 005930 응답 캡처 → V1
2. 1~2분 대기 후 manual execute 한 번 더 실행 OR Cloud Scheduler 의 다음 cron 발화 기다림
3. 두 번째 cycle 의 005930 응답 캡처 → V2
4. V2 ≥ V1 단조 증가 확인 → 누적 거래량 확정 (D-06 가설 OK)
5. 09.1-03-API-VERIFICATION.md R2 섹션 paste

**Step 7: 09.1-DEPLOY-LOG.md section 6/7 갱신**
- section 6: 첫 cycle 결과 (fetched_rows, rpc_calls, rows_affected, duration_ms, 005930 sample row)
- section 7: R2/R3 검증 요약
  </action>

  <verify>
    <automated># Checkpoint — manual verification gate. Executor performs steps in <action>, captures evidence into DEPLOY-LOG / VERIFY artifacts, then user issues resume-signal. No standalone automated command — see <action> Step 검증 for the executable evidence.
echo "MANUAL_GATE — checkpoint:human-verify / checkpoint:human-action"</automated>
  </verify>

  <resume-signal>(a) cycle exit 0 + rows_affected ≥ 1500 + 005930 row 갱신 확인, (b) R3 expires_dt 분 단위 측정값 paste, (c) R2 V2 ≥ V1 확인 (장중 시 가능, 장 외라면 deferred 표기), (d) DEPLOY-LOG section 6/7 갱신 → "approved" 시 Task 5 진행.</resume-signal>

  <acceptance_criteria>
    - 첫 cycle exit 0 (Cloud Run Job execution succeeded)
    - logs 의 `intraday-sync OK` + `fetched_rows`, `mapped_rows`, `rpc_calls`, `rows_affected` 캡처
    - Supabase: `stock_daily_ohlcv` 의 오늘자 row 수 ≥ 1500
    - Supabase: 005930 의 오늘자 row 갱신 확인 (close > 0, inserted_at 최근)
    - 401 / 429 / partial response 에러 없음 (IP 화이트리스트 + secret 정상 작동 확인)
    - 09.1-03-API-VERIFICATION.md R3 (expires_dt 분 단위) paste
    - 09.1-03-API-VERIFICATION.md R2 (now_trde_qty V1/V2) paste — 장중이면 PASS, 장외면 "deferred to next trading day" 표기 허용
    - 09.1-DEPLOY-LOG.md section 6 (cycle 결과) + section 7 (R2/R3) 갱신
  </acceptance_criteria>

  <done>첫 production cycle 검증 + R2/R3 mitigation 완료 + DEPLOY-LOG section 6/7.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: REQUIREMENTS.md + ROADMAP.md + STATE.md 갱신 (Phase 09.1 Complete sign-off)</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>

  <read_first>
    - .planning/REQUIREMENTS.md (DATA-02 status 행)
    - .planning/ROADMAP.md (Phase 09.1 Plans 체크박스 + Completed 마커)
    - .planning/STATE.md (current focus + progress 갱신)
    - .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md (Task 2/3/4 가 채운 실값)
  </read_first>

  <action>
1. **`.planning/REQUIREMENTS.md`** — DATA-02 행 갱신:
   - Line `| DATA-02 | Phase 09.1 | Pending |` → `| DATA-02 | Phase 09.1 | Complete |`
   - 본문의 DATA-02 정의 라인은 그대로 유지

2. **`.planning/ROADMAP.md`** — Phase 09.1 섹션 갱신:
   - 6 plan 모두 체크박스 `[ ]` → `[x]`
   - Plans 헤더 아래 `**Completed:** YYYY-MM-DD` 추가 (Task 4 의 cycle 성공일 기준)
   - 본문 변경 없음

3. **`.planning/STATE.md`** — current focus / progress 갱신:
   - `progress.total_plans: 70` → `76` (Phase 09.1 6 plans 추가)
   - `progress.completed_plans: 56` → `62`
   - `progress.percent` 재계산
   - `stopped_at` → "Phase 09.1 Complete — Ready for Phase 10 (AI Summarization)"
   - `last_updated` → 오늘 ISO
   - `Current focus` → "Phase 10 — ai-summarization"
   - `Phase 1 Success Criteria 검증` 아래 또는 `Roadmap Evolution` 항목에 1줄 추가:
     ```
     - Phase 09.1 complete YYYY-MM-DD: intraday-sync production live. Cloud Run Job gh-radar-intraday-sync + Cloud Scheduler `* 9-15 * * 1-5` Asia/Seoul. Direct VPC Egress + Cloud NAT + Static IP <XX.XX.XX.XX> 키움 화이트리스트 등록. RPC intraday_upsert_close service_role 만 EXECUTE. 첫 cycle rows_affected=<N>.
     ```

4. 변경 후 grep 검증:
```bash
grep "DATA-02 | Phase 09.1 | Complete" .planning/REQUIREMENTS.md
grep -c "\[x\] 09.1-" .planning/ROADMAP.md  # 6
grep "Phase 09.1 Complete" .planning/STATE.md
```
  </action>

  <verify>
    <automated>grep -q "DATA-02 | Phase 09.1 | Complete" .planning/REQUIREMENTS.md && [ "$(grep -c '\[x\] 09.1-' .planning/ROADMAP.md)" -eq 6 ] && grep -q "Phase 09.1 Complete" .planning/STATE.md</automated>
  </verify>

  <acceptance_criteria>
    - REQUIREMENTS.md: DATA-02 row Pending → Complete
    - ROADMAP.md: Phase 09.1 의 6 plans 모두 `[x]` 체크 + Completed 날짜 기록
    - STATE.md: completed_plans + percent 갱신, current focus → Phase 10, stopped_at → Phase 09.1 Complete, Roadmap Evolution 에 Phase 09.1 완료 한 줄 추가
    - 3 파일 모두 grep 검증 PASS
  </acceptance_criteria>

  <done>Phase 09.1 Complete sign-off — 3 핵심 planning 파일 갱신 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → production Supabase | supabase db push (Task 1) |
| developer → production GCP | setup/deploy scripts (Task 2/3) |
| user → 키움 openapi.kiwoom.com | IP whitelist 등록 (Task 2 — 외부 시스템) |
| Cloud Scheduler → Cloud Run Job | OAuth scheduler-sa run.invoker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-PROD-01 | TAMPERING (마이그레이션 실패) | RPC INSERT 후 ROLLBACK | mitigate | Plan 01 의 단일 BEGIN/COMMIT 트랜잭션 — 실패 시 자동 ROLLBACK. Task 1 의 4 검증 시퀀스가 사후 확인. |
| T-09.1-PROD-02 | ELEVATION OF PRIVILEGE | anon/authenticated 가 RPC 호출 가능 (DI-01 회귀) | mitigate | Task 1 step 3 의 information_schema.role_routine_grants 검증이 핵심 — service_role 단일 entry 만 허용. |
| T-09.1-PROD-03 | SPOOFING | 잘못된 IP 가 키움에 등록 | mitigate | Task 2 의 사용자 액션 — setup script 가 echo 한 Static IP 를 그대로 복사 등록. Cloud NAT 의 natIps 가 IAM 가드. |
| T-09.1-PROD-04 | INFORMATION DISCLOSURE | DEPLOY-LOG 에 secret 값 노출 | mitigate | DEPLOY-LOG 는 secret **이름** + **GIT_SHA** + **rows_affected** 만 기록. KIWOOM_APPKEY/SECRETKEY 값은 절대 paste 금지. |
| T-09.1-PROD-05 | DENIAL OF SERVICE | 첫 cycle 의 Direct VPC Egress 시작 지연 (T-10) | accept | RESEARCH §4.6: 첫 cycle ~1분+ 지연 가능 — 두 번째 cycle 부터 안정. Task 4 가 manual execute 로 검증 → Cloud Scheduler 의 정시 발화 전 디버그 가능. |
| T-09.1-PROD-06 | INFORMATION DISCLOSURE | Cloud Logging 에 token leak | mitigate | Plan 02 의 logger redact paths (`headers.authorization`, `*.token`, `*.kiwoomAppkey`, `*.kiwoomSecretkey`) 가 적용. Task 4 의 logs 캡처 시 redact 확인. |
| T-09.1-PROD-07 | AVAILABILITY | 알림 channel 미연결 | mitigate | Task 3 step 2 의 channel ID sed 치환 + apply. Phase 05.1 의 alex@jx1.io 채널 재사용. |
</threat_model>

<verification>
- 5개 task 모두 완료 (4개 BLOCKING + 1개 sign-off)
- Supabase RPC production 적용 + 권한 검증 PASS
- GCP VPC/NAT/Static IP/Cloud Run Job/Scheduler/Alert policy 모두 ENABLED
- 사용자가 키움 화이트리스트에 Static IP 등록 완료
- 첫 cycle 검증: rows_affected ≥ 1500, 401/429 없음
- R2 (now_trde_qty 누적) + R3 (token expires) 실측 paste
- REQUIREMENTS/ROADMAP/STATE 모두 갱신
- DEPLOY-LOG.md 모든 section 채움
</verification>

<success_criteria>
- DATA-02 모든 SC #1~#6 production 검증
- Phase 09.1 Complete sign-off
- R2/R3 mitigation 실측 paste
- 알림 채널 active — 다음 평일 09:00 부터 Cloud Scheduler 정시 발화
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-intraday-current-price/09.1-06-SUMMARY.md`
</output>
</content>
