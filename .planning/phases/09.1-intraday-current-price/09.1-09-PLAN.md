---
phase: 09.1
plan: 09
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06, 07, 08]
files_modified:
  - .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md
autonomous: false
requirements: [DATA-02]
requirements_addressed:
  - DATA-02 (Wave 4 cutover #1 — Supabase migration push (kiwoom_tokens + 2 RPC) + worker IAM/VPC/Static IP 실행 + KIWOOM secret 값 사용자 등록 + worker production 배포 + 첫 cycle 검증. KIS ingestion 은 아직 ACTIVE — 본 plan 종료 시점에 키움 worker production live 검증된 후 Plan 10 이 server 재배포 + KIS pause)
must_haves:
  truths:
    - "Supabase production DB 에 kiwoom_tokens 테이블 + intraday_upsert_close + intraday_upsert_ohlc RPC 가 적용 (psql 또는 supabase db push)"
    - "GCP 에 VPC stack (gh-radar-vpc + subnet + router + nat + static-ip) 생성"
    - "Cloud Run Job gh-radar-intraday-sync 배포 + Cloud Scheduler gh-radar-intraday-sync-cron 활성화"
    - "키움 KIWOOM_APPKEY/SECRETKEY 가 GCP Secret 에 사용자 등록"
    - "Static IP 가 키움 openapi.kiwoom.com IP 화이트리스트에 사용자 등록"
    - "intraday-sync 의 첫 production cycle 이 ka10027 응답 ≥1,500 row 확인"
    - "DEPLOY-LOG.md 에 실배포 결과 (이미지 SHA, Static IP, Scheduler 상태, smoke 결과) 기록"
    - "본 plan 종료 시점에 KIS ingestion 은 ACTIVE 유지 (Plan 11 cleanup 까지) — stocks/stock_quotes/top_movers 가 두 워커 동시 갱신 (intraday-sync 가 인계 시작)"
  artifacts:
    - path: .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md
      provides: "실배포 명령/이미지 SHA/Static IP/Scheduler/INV 결과 기록"
  key_links:
    - from: "사용자 액션 (키움 portal IP 등록)"
      to: "키움 API 응답 200"
      via: "Static IP + KIWOOM_APPKEY/SECRETKEY 둘 다 등록되어야 첫 cycle 성공"
      pattern: "Static IP whitelist"
---

<objective>
**[BLOCKING]** Wave 4 의 첫 cutover task — Supabase migration push (kiwoom_tokens + RPC 2종) + GCP VPC/IAM/Job/Scheduler 배포 + 사용자가 KIWOOM 인증 정보 + Static IP 등록 + 첫 production cycle 검증 + DEPLOY-LOG 기록. 본 plan 종료 시 intraday-sync 는 production 에서 매분 cycle 실행 중이지만, KIS ingestion 은 아직 ACTIVE (Plan 10 server 재배포 + Plan 11 KIS pause/cleanup 까지).

Purpose: 사용자 액션 (KIWOOM secret value 등록 + 키움 portal IP 화이트리스트) 가 필요하므로 `autonomous: false` 명시. cutover 의 안전 순서: migration 먼저 → infra setup → secret value 등록 → job 배포 → smoke 검증.

Output: production DB + production GCP 리소스 active. DEPLOY-LOG.md 1 파일.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md
@.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md
@.planning/phases/09.1-intraday-current-price/09.1-01-PLAN.md
@.planning/phases/09.1-intraday-current-price/09.1-08-PLAN.md
@scripts/setup-intraday-sync-iam.sh
@scripts/deploy-intraday-sync.sh
@scripts/smoke-intraday-sync.sh
@supabase/migrations/20260514120100_create_kiwoom_tokens.sql
@supabase/migrations/20260514120200_intraday_upsert_close_rpc.sql
@supabase/migrations/20260514120300_intraday_upsert_ohlc_rpc.sql

<read_first>
- `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §1.8 (IP whitelist), §4.3 (architecture), §8 (Environment Availability)
- `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-31 (사용자 액션 — IP 등록)
- MEMORY `reference_gh_radar_deployer_sa` (영구 GCP 인증, `~/.config/gcloud/gh-radar-deployer.json` + `GOOGLE_APPLICATION_CREDENTIALS` + `CLOUDSDK_CORE_PROJECT=gh-radar`)
- MEMORY `feedback_dont_ask_existing_creds` (KIWOOM_APPKEY/SECRETKEY 가 .env 또는 사용자가 보유 — 본 plan 의 task 1 시점에 사용자에게 한번만 요청)
- Plan 01 의 마이그레이션 4 파일 (kis_tokens DROP 은 Plan 11 에서 push)
- Plan 03 의 KIWOOM_BASE_URL default + KIWOOM_TOKEN_TYPE default
</read_first>

<interfaces>
<!-- 본 plan 의 cutover 순서 (의존성 그래프) -->
1. kiwoom_tokens migration push (kis_tokens DROP 은 Plan 11 까지 대기)
2. intraday_upsert_close RPC migration push
3. intraday_upsert_ohlc RPC migration push
4. bash scripts/setup-intraday-sync-iam.sh (VPC + SA + Secrets + IAM)
5. 사용자 액션: KIWOOM_APPKEY / KIWOOM_SECRETKEY 값을 GCP Secret 에 추가
6. 사용자 액션: Static IP 를 키움 portal 에 등록
7. bash scripts/deploy-intraday-sync.sh (Job + Scheduler + Alert)
8. bash scripts/smoke-intraday-sync.sh (첫 cycle 검증)
9. DEPLOY-LOG 기록
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 → GCP Secret Manager | KIWOOM_APPKEY/SECRETKEY 평문을 gcloud CLI 로 등록. 평문 노출 zone = 사용자 터미널 history. |
| 사용자 → 키움 openapi.kiwoom.com | Static IP 등록 후 키움이 IP 화이트리스트에 매칭. |
| migration push → production DB | service_role 권한으로 schema 변경. 롤백 — DROP 가능. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-37 | Availability | KIS ingestion 과 intraday-sync 동시 실행 → stock_quotes 동시 UPSERT race | accept | 두 워커 모두 onConflict: code 의 idempotent UPSERT. KIS ingestion 의 stock_quotes 갱신 빈도 1분 + intraday-sync 1분 → 60s 윈도우 안에 양쪽 1회씩, 마지막 쓰기 승. 데이터 정합 영향 < 1분 stale. Plan 11 가 KIS pause → 완전 해소. |
| T-09.1-38 | Information Disclosure | KIWOOM_APPKEY/SECRETKEY 등록 시 사용자 터미널 history 노출 | mitigate | `gcloud secrets versions add ... --data-file=-` + stdin 사용 → shell history 에 평문 미저장. `echo -n '<KEY>' \| gcloud ...` 패턴 사용 시 history 저장 — task action 에 `read -s` 사용 명시. |
| T-09.1-39 | Availability | 첫 cycle 실패 (휴장일 또는 비-장 시간) | mitigate | smoke 의 INV-2 가 "intraday cycle complete" OR "휴장일" 둘 다 PASS. 장 시간 외 실행 시 manual execute 로 verify (Job execute --wait + 로그 검증). |
| T-09.1-40 | Tampering | migration push 가 production 에 잘못 적용 | mitigate | supabase db push 전 staging 검증 권장. 본 plan 의 task 1 은 사용자가 `psql` 또는 Supabase SQL Editor 로 수동 적용 — 자동 push 회피 (사용자 확인 step). |
| T-09.1-41 | Configuration | Static IP 등록 누락 → 첫 cycle 401/key denied | mitigate | task 6 의 사용자 액션 + smoke 의 INV-3 ("키움 401" 0건 검증) 가 catch. 실패 시 즉시 rollback 가능 (Scheduler PAUSE → 등록 후 RESUME). |
| T-09.1-42 | Availability | Cloud Run Job 첫 부팅 Direct VPC Egress 1분+ cold-start | accept | RESEARCH §4.8 + T-10. 첫 cycle 지연 → 두 번째 cycle 부터 정상. INV-1 의 --wait 가 60s task-timeout 안에 완료 가능 — 만약 cold-start 가 60s 넘으면 manual execute 한 번 더. |
</threat_model>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Supabase migration push — kiwoom_tokens + 2 RPC (kis_tokens DROP race 가드 포함)</name>
  <what-built>
    Plan 01 이 작성한 3개 migration 파일 (이번 wave 에 적용 대상):
    - supabase/migrations/20260514120100_create_kiwoom_tokens.sql
    - supabase/migrations/20260514120200_intraday_upsert_close_rpc.sql
    - supabase/migrations/20260514120300_intraday_upsert_ohlc_rpc.sql

    Plan 01 이 함께 작성했지만 **본 wave 에서 push 금지** (Plan 11 의 마지막 step 에서 적용):
    - supabase/migrations/20260514120000_drop_kis_tokens.sql
  </what-built>
  <action>본 task 의 사용자 액션은 <how-to-verify> 블록 참조. 자동화 가능한 부분 없음 — 사용자 confirm 필요. **race condition 가드 step (rename) 필수 — supabase db push 가 디렉터리 안 모든 미적용 파일을 한꺼번에 적용하므로 kis_tokens DROP 이 의도치 않게 같은 push 에 포함되는 사고를 방지한다 (T-09.1-04 Availability mitigation).**</action>
  <how-to-verify>
    사용자가 다음 순서로 Supabase production DB 에 마이그레이션 적용:

    **옵션 A — supabase CLI (권장, race 가드 포함):**

    Step 1 — kis_tokens DROP migration 임시 비활성화 (rename 으로 push 대상에서 제외):
    ```bash
    cd supabase/migrations
    mv 20260514120000_drop_kis_tokens.sql 20260514120000_drop_kis_tokens.sql.deferred
    ls 20260514120000_drop_kis_tokens.sql.deferred  # 존재 확인
    ! ls 20260514120000_drop_kis_tokens.sql 2>/dev/null  # 원본 부재 확인
    cd -
    ```

    Step 2 — supabase db push (kiwoom_tokens + 2 RPC 만 적용):
    ```bash
    export SUPABASE_ACCESS_TOKEN=<사용자 토큰>
    supabase db push
    # 본 명령은 supabase/migrations/ 의 신규 .sql 파일만 push.
    # .sql.deferred 는 supabase CLI 가 인식하지 않음 → kis_tokens DROP 제외 보장.
    ```

    Step 3 — 검증 SQL (Supabase Dashboard SQL Editor 또는 psql):
    ```sql
    SELECT tablename FROM pg_tables WHERE tablename IN ('kis_tokens', 'kiwoom_tokens') ORDER BY tablename;
    -- 기대 결과 (2 row):
    --   kis_tokens     ← 아직 존재 (Plan 11 까지 유지)
    --   kiwoom_tokens  ← 신규 생성

    SELECT proname FROM pg_proc WHERE proname IN ('intraday_upsert_close', 'intraday_upsert_ohlc') ORDER BY proname;
    -- 기대 결과 (2 row):
    --   intraday_upsert_close
    --   intraday_upsert_ohlc
    ```

    Step 4 — 본 wave 동안 .deferred 상태 유지 (Plan 11 Task 4 직전에 복원). 즉, 본 plan 종료 시점부터 Plan 11 의 kis_tokens DROP push step 직전까지 .deferred 확장자 유지.

    **옵션 B — Supabase Dashboard SQL Editor (수동, race 위험 없음):**
    1. 20260514120100_create_kiwoom_tokens.sql 내용 복사 → SQL Editor 에서 실행
    2. 20260514120200_intraday_upsert_close_rpc.sql → 실행
    3. 20260514120300_intraday_upsert_ohlc_rpc.sql → 실행
    4. 20260514120000_drop_kis_tokens.sql 은 **실행 금지** (Plan 11 Task 4 까지 보류)
    5. Step 3 의 검증 SQL 실행

    검증 후 사용자가 다음 메시지로 confirm: "kiwoom_tokens + 2 RPC migration applied (kis_tokens still exists)"
  </how-to-verify>
  <resume-signal>"migration applied (kis_tokens still exists)" 또는 issue 보고</resume-signal>
</task>

<task type="auto">
  <name>Task 1.5: 자동 검증 — kiwoom_tokens 테이블 + 2 RPC 존재 확인 (Task 1 결과 검증)</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - Plan 01 의 마이그레이션 SQL 3종 (kiwoom_tokens + 2 RPC) 의 명명 규약
  </read_first>
  <action>
    Task 1 의 사용자 액션 (마이그레이션 push) 직후, REST API + psql 로 production DB 상태를 자동 검증한다 — race 가드가 정상 동작했는지 (`kis_tokens` 잔존 + `kiwoom_tokens` 신규) 확인.

    환경 변수 (.env.deploy 또는 사용자 export):
    ```bash
    export SUPABASE_URL=https://<project>.supabase.co
    export SUPABASE_SERVICE_ROLE_KEY=<service-role>
    export SUPABASE_DB_URL="postgresql://postgres.<project>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres"
    ```

    검증 1 — REST API 로 kiwoom_tokens 테이블 존재 확인 (count=0 OK, 핵심은 4xx 가 아닌 200 응답):
    ```bash
    curl -fsS "${SUPABASE_URL}/rest/v1/kiwoom_tokens?select=count" \
      -H "apikey: ${SUPABASE_SERVICE_ROLE_KEY}" \
      -H "Authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}" \
      | jq -e '.[0].count >= 0'
    # exit 0 = 테이블 존재 + REST API 접근 가능
    ```

    검증 2 — RPC 2종 존재 확인 (psql):
    ```bash
    psql "$SUPABASE_DB_URL" -t -c "SELECT proname FROM pg_proc WHERE proname IN ('intraday_upsert_close', 'intraday_upsert_ohlc') ORDER BY proname" \
      | tr -d ' ' | grep -v '^$' | wc -l | grep -q '^2$'
    # 2 → 두 RPC 모두 존재
    ```

    검증 3 — kis_tokens 잔존 확인 (race 가드 동작) + kiwoom_tokens 동시 존재:
    ```bash
    psql "$SUPABASE_DB_URL" -t -c "SELECT tablename FROM pg_tables WHERE tablename IN ('kis_tokens', 'kiwoom_tokens') ORDER BY tablename" \
      | tr -d ' ' | grep -v '^$' | sort | tr '\n' ',' | grep -q '^kis_tokens,kiwoom_tokens,$'
    # 두 테이블 모두 존재 (race 가드 정상)
    ```

    실패 시:
    - 검증 1 fail → kiwoom_tokens migration 미적용. Task 1 의 옵션 A/B 중 한 방법으로 재적용.
    - 검증 2 fail (RPC 0 또는 1 row) → 누락 RPC 의 SQL 파일 수동 적용.
    - 검증 3 fail with `kiwoom_tokens` only (kis_tokens 부재) → **race 가드 실패 — kis_tokens 가 의도치 않게 DROP 됨**. 즉시 STOP + 사용자에게 보고 + Phase 9 candle-sync 의 다른 KIS 사용처 영향 확인.
  </action>
  <verify>
    <automated>curl -fsS "${SUPABASE_URL}/rest/v1/kiwoom_tokens?select=count" -H "apikey: ${SUPABASE_SERVICE_ROLE_KEY}" -H "Authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}" | jq -e '.[0].count >= 0' &amp;&amp; psql "$SUPABASE_DB_URL" -t -c "SELECT proname FROM pg_proc WHERE proname IN ('intraday_upsert_close', 'intraday_upsert_ohlc') ORDER BY proname" | tr -d ' ' | grep -v '^$' | wc -l | grep -q '^2$' &amp;&amp; psql "$SUPABASE_DB_URL" -t -c "SELECT tablename FROM pg_tables WHERE tablename IN ('kis_tokens', 'kiwoom_tokens') ORDER BY tablename" | tr -d ' ' | grep -v '^$' | sort | tr '\n' ',' | grep -q '^kis_tokens,kiwoom_tokens,$'</automated>
  </verify>
  <done>
    - kiwoom_tokens 테이블 REST API 응답 200
    - intraday_upsert_close + intraday_upsert_ohlc RPC 둘 다 존재
    - kis_tokens 잔존 (race 가드 정상 — Plan 11 까지 유지)
  </done>
</task>

<task type="auto">
  <name>Task 2: setup-intraday-sync-iam.sh 실행 (VPC + SA + 빈 Secrets + IAM)</name>
  <files>(GCP resources — no local files modified)</files>
  <read_first>
    - `scripts/setup-intraday-sync-iam.sh` (Plan 08 작성)
    - MEMORY `reference_gh_radar_deployer_sa` (env 설정)
  </read_first>
  <action>
    GCP 환경 변수 설정 후 setup 스크립트 실행:

    ```bash
    export GOOGLE_APPLICATION_CREDENTIALS=~/.config/gcloud/gh-radar-deployer.json
    export CLOUDSDK_CORE_PROJECT=gh-radar
    export GCP_PROJECT_ID=gh-radar
    gcloud config configurations activate gh-radar

    bash scripts/setup-intraday-sync-iam.sh
    ```

    결과:
    - VPC stack (gh-radar-vpc + subnet + router + nat + static-ip) 생성
    - SA gh-radar-intraday-sync-sa 생성 + IAM 바인딩
    - Secrets gh-radar-kiwoom-appkey, gh-radar-kiwoom-secretkey (빈 secret — 값은 task 3 에서 사용자 등록)
    - Cloud Run Service Agent + 2 SA 에 compute.networkUser 부여
    - 스크립트 출력의 "📌 Static IP: X.X.X.X" 라인을 DEPLOY-LOG 에 기록 (Static IP 주소)
  </action>
  <verify>
    <automated>gcloud compute addresses describe gh-radar-static-ip --region=asia-northeast3 --format='value(address)' &amp;&amp; gcloud iam service-accounts describe gh-radar-intraday-sync-sa@gh-radar.iam.gserviceaccount.com &amp;&amp; gcloud compute networks describe gh-radar-vpc &amp;&amp; gcloud secrets describe gh-radar-kiwoom-appkey &amp;&amp; gcloud secrets describe gh-radar-kiwoom-secretkey</automated>
  </verify>
  <done>
    - 모든 GCP 리소스 (VPC + Subnet + Router + NAT + Static IP + SA + 2 Secrets) 존재 확인
    - Static IP 주소 캡처
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: 사용자 액션 — KIWOOM_APPKEY/SECRETKEY 값을 GCP Secret 에 등록</name>
  <what-built>
    Task 2 가 빈 Secret 2종 (`gh-radar-kiwoom-appkey`, `gh-radar-kiwoom-secretkey`) 만 생성. 본 task 는 사용자가 값을 등록.
  </what-built>
  <action>본 task 의 사용자 액션은 <how-to-verify> 블록 참조. 자동화 가능한 부분 없음 — 사용자 confirm 필요.</action>
  <how-to-verify>
    사용자가 다음 명령으로 값 추가 (stdin 사용 — shell history 보호):

    ```bash
    # APPKEY 값 등록 (-s 로 입력 숨김)
    read -s -p "KIWOOM_APPKEY: " KIWOOM_APPKEY; echo
    echo -n "$KIWOOM_APPKEY" | gcloud secrets versions add gh-radar-kiwoom-appkey --data-file=-
    unset KIWOOM_APPKEY

    # SECRETKEY 값 등록
    read -s -p "KIWOOM_SECRETKEY: " KIWOOM_SECRETKEY; echo
    echo -n "$KIWOOM_SECRETKEY" | gcloud secrets versions add gh-radar-kiwoom-secretkey --data-file=-
    unset KIWOOM_SECRETKEY
    ```

    검증:
    ```bash
    gcloud secrets versions list gh-radar-kiwoom-appkey --filter='state=ENABLED' --format='value(name)'
    # → 1 (version 1 enabled)
    gcloud secrets versions list gh-radar-kiwoom-secretkey --filter='state=ENABLED' --format='value(name)'
    # → 1
    ```

    사용자가 보유한 KIWOOM_APPKEY/SECRETKEY 가 .env 파일 또는 키움 portal 의 application 페이지에 있는 자격증명을 그대로 사용 (MEMORY: feedback_dont_ask_existing_creds — 사용자에게 한 번만 요청).
  </how-to-verify>
  <resume-signal>"KIWOOM secrets registered" 또는 issue</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: 사용자 액션 — 키움 OpenAPI portal 에 Static IP 등록 (D-31)</name>
  <what-built>
    Task 2 가 Static IP (`gh-radar-static-ip`) 예약. 본 task 는 사용자가 키움 portal 에 등록.
  </what-built>
  <action>본 task 의 사용자 액션은 <how-to-verify> 블록 참조. 자동화 가능한 부분 없음 — 사용자 confirm 필요.</action>
  <how-to-verify>
    1. Task 2 의 출력에서 Static IP 주소 확인 (또는 `gcloud compute addresses describe gh-radar-static-ip --region=asia-northeast3 --format='value(address)'`)
    2. https://openapi.kiwoom.com → 사용자 계정 로그인 → 앱 관리 / IP 등록 페이지
    3. 위에서 확인한 Static IP 1개 추가 (예: `34.64.X.X`)
    4. 저장 + 적용 확인

    검증: Task 7 의 smoke INV-1 (Job execute --wait) 가 success → IP 화이트리스트 통과 확인.

    Rollback 가능: 추후 Static IP 변경 시 키움 portal 에서 기존 IP 삭제 + 신규 IP 추가.
  </how-to-verify>
  <resume-signal>"Static IP registered to Kiwoom portal" 또는 issue</resume-signal>
</task>

<task type="auto">
  <name>Task 5: deploy-intraday-sync.sh 실행 (Cloud Run Job + Scheduler + Alert)</name>
  <files>(GCP resources)</files>
  <read_first>
    - `scripts/deploy-intraday-sync.sh` (Plan 08)
  </read_first>
  <action>
    배포 환경 변수 설정 후 실행:

    ```bash
    export GCP_PROJECT_ID=gh-radar
    export SUPABASE_URL=https://<project>.supabase.co  # 또는 .env.deploy 에서 source
    export NOTIFICATION_CHANNEL_ID=<email channel id from gcloud alpha monitoring channels list>

    bash scripts/deploy-intraday-sync.sh
    ```

    결과:
    - intraday-sync:${SHA} Artifact Registry 푸시
    - Cloud Run Job gh-radar-intraday-sync 배포 (VPC + min/max 옵션 + KIWOOM secrets)
    - Scheduler gh-radar-intraday-sync-cron 활성화 (cron '* 9-15 * * 1-5' Asia/Seoul)
    - Alert policy gh-radar-intraday-sync-failure 활성화
  </action>
  <verify>
    <automated>gcloud run jobs describe gh-radar-intraday-sync --region=asia-northeast3 --format='value(name)' &amp;&amp; gcloud scheduler jobs describe gh-radar-intraday-sync-cron --location=asia-northeast3 --format='value(state)' | grep -q ENABLED</automated>
  </verify>
  <done>
    - Cloud Run Job 배포 완료 (이미지 SHA 캡처)
    - Scheduler ENABLED + cron + Asia/Seoul
    - Alert policy active
  </done>
</task>

<task type="auto">
  <name>Task 6: smoke-intraday-sync.sh 실행 (INV-1~6 + --check-scheduler + --check-static-ip)</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - `scripts/smoke-intraday-sync.sh` (Plan 08)
  </read_first>
  <action>
    smoke 검증 + 결과 캡처:

    ```bash
    # INV-1~6 (장 시간 09:00~15:30 KST 권장)
    bash scripts/smoke-intraday-sync.sh

    # 추가 검증
    bash scripts/smoke-intraday-sync.sh --check-scheduler
    bash scripts/smoke-intraday-sync.sh --check-static-ip
    ```

    각 INV 결과를 DEPLOY-LOG 에 기록 (PASS/FAIL + 상세 메시지).

    **장 시간 외 실행 시:** INV-2 가 "휴장일" 메시지로 PASS 처리. INV-4/INV-5 (stock_quotes/ohlcv 5분 이내 row) 는 휴장일 SKIP — 다음 평일 장 시간 첫 cycle 직후 재실행.

    **장 시간 실행 시:** 모든 INV 가 PASS 되어야 본 plan 종료. FAIL 시:
    - INV-1 FAIL → Cloud Logging 에서 에러 메시지 확인 (키움 401 = IP 등록 누락, 키움 429 = rate limit, partial response = MIN_EXPECTED 가드)
    - INV-4 FAIL (count < 1500) → ka10027 응답 row 수 부족 → 키움 portal 사용량/권한 재확인
  </action>
  <verify>
    <automated>bash scripts/smoke-intraday-sync.sh --check-scheduler</automated>
  </verify>
  <done>
    - INV-1~6 결과 캡처 (PASS/FAIL/SKIP)
    - 장 시간 실행 시 INV-4 (stock_quotes 1500+ row) 확인 — 첫 cycle 의 ka10027 응답 정상 (성공 시 DATA-02 SC #6 충족)
    - Cloud Logging 의 "intraday cycle complete" 메시지 확인
  </done>
</task>

<task type="auto">
  <name>Task 7: DEPLOY-LOG.md 작성 (Wave 4 cutover #1 결과)</name>
  <files>.planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md</files>
  <read_first>
    - `.planning/phases/02-backend-api/02-DEPLOY-LOG.md` (template 참조)
    - `.planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md`
    - `.planning/phases/09-daily-candle-data/09-06-SUMMARY.md` (Phase 9 deploy log 형식)
  </read_first>
  <action>
    `.planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md` 신규 작성:

    ```markdown
    # Phase 09.1 — DEPLOY-LOG

    **Phase:** 09.1-intraday-current-price (KIS → 키움 완전 대체)
    **Wave 4 cutover #1 — Worker production live**
    **Date:** {today YYYY-MM-DD KST}

    ## Migrations applied (Supabase production)

    | Migration | Applied At | Method |
    |---|---|---|
    | 20260514120100_create_kiwoom_tokens.sql | {timestamp} | supabase db push OR SQL Editor |
    | 20260514120200_intraday_upsert_close_rpc.sql | {timestamp} | (위와 동일) |
    | 20260514120300_intraday_upsert_ohlc_rpc.sql | {timestamp} | (위와 동일) |
    | 20260514120000_drop_kis_tokens.sql | **NOT YET** | Plan 11 cleanup 의 마지막 step 에서 push |

    ## GCP resources created

    | Resource | Name | Region |
    |---|---|---|
    | VPC | gh-radar-vpc | global |
    | Subnet | gh-radar-subnet-an3 (10.10.0.0/26) | asia-northeast3 |
    | Cloud Router | gh-radar-router-an3 | asia-northeast3 |
    | Cloud NAT | gh-radar-nat-an3 | asia-northeast3 |
    | Static IP | gh-radar-static-ip → **{Static IP 주소}** | asia-northeast3 |
    | SA | gh-radar-intraday-sync-sa@gh-radar.iam.gserviceaccount.com | — |
    | Secret | gh-radar-kiwoom-appkey | global |
    | Secret | gh-radar-kiwoom-secretkey | global |
    | Cloud Run Job | gh-radar-intraday-sync | asia-northeast3 |
    | Scheduler | gh-radar-intraday-sync-cron (cron `* 9-15 * * 1-5` Asia/Seoul) | asia-northeast3 |
    | Alert policy | gh-radar-intraday-sync-failure | global |

    ## User actions completed

    | Action | Status |
    |---|---|
    | KIWOOM_APPKEY 값 등록 (gh-radar-kiwoom-appkey:1) | ✅ |
    | KIWOOM_SECRETKEY 값 등록 (gh-radar-kiwoom-secretkey:1) | ✅ |
    | 키움 portal IP 화이트리스트 등록 ({Static IP}) | ✅ |

    ## Image SHA

    | Image | SHA |
    |---|---|
    | intraday-sync | `{git SHA}` |

    ## Smoke results

    | INV | Result | Note |
    |---|---|---|
    | INV-1 Job execute --wait exit 0 | {PASS / FAIL} | |
    | INV-2 logs: cycle complete OR 휴장일 | {PASS} | |
    | INV-3 logs: no 키움 401 / 429 / cycle failed | {PASS} | |
    | INV-4 stock_quotes 5분 이내 row >= 1500 | {PASS / SKIP if 휴장일} | |
    | INV-5 stock_daily_ohlcv 오늘 row >= 1500 | {PASS / SKIP} | |
    | INV-6 Scheduler ENABLED | PASS | |
    | --check-scheduler | PASS | cron `* 9-15 * * 1-5` Asia/Seoul 확인 |
    | --check-static-ip | PASS | Job 이 VPC connector 연결 + Static IP 일치 |

    ## Current State

    - **intraday-sync**: production active (Cloud Scheduler 가 매분 fire)
    - **KIS ingestion (gh-radar-ingestion)**: **STILL ACTIVE** — Plan 11 cleanup 까지 유지
    - **server (gh-radar-server)**: **OLD image** — VPC connector 옵션 미적용. Plan 10 가 재배포
    - **stock_quotes / stock_daily_ohlcv 갱신**: 양쪽 워커 (KIS ingestion + intraday-sync) 동시 갱신 중. 마지막 쓰기 승 (idempotent UPSERT)

    ## Next steps

    - Plan 10: server 재배포 (VPC connector + KIWOOM secret) → 종목 상세 페이지 키움 동기 호출
    - Plan 11: KIS ingestion 폐기 (Scheduler PAUSE → Job 삭제 → SA 삭제 → Secrets 삭제 → kis_tokens migration push → 코드 deletion)

    ## Rollback procedure (필요 시)

    1. Cloud Scheduler PAUSE: `gcloud scheduler jobs pause gh-radar-intraday-sync-cron --location=asia-northeast3`
    2. Cloud Run Job 삭제 (선택): `gcloud run jobs delete gh-radar-intraday-sync --region=asia-northeast3 --quiet`
    3. Supabase migration revert (선택): kiwoom_tokens + 2 RPC 수동 DROP
    4. KIS ingestion 만 active 유지 → 기존 상태 복원
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md &amp;&amp; grep -q "Wave 4 cutover #1" .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md &amp;&amp; grep -q "STILL ACTIVE" .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md</automated>
  </verify>
  <done>
    - DEPLOY-LOG.md 생성
    - migration / GCP 리소스 / 사용자 액션 / smoke 결과 모두 기록
    - Plan 10/11 의 다음 단계 명시
    - rollback 절차 명시
  </done>
</task>

</tasks>

<verification>
- Supabase production: kiwoom_tokens + 2 RPC active
- GCP: Cloud Run Job + Scheduler + Alert + VPC + Static IP + SA + Secrets 모두 active
- 첫 cycle (장 시간 시) 성공 — stock_quotes 1500+ row 갱신 + stock_daily_ohlcv 오늘 row 1500+
- DEPLOY-LOG 기록 + 현재 상태 명시 (KIS ingestion 아직 ACTIVE, server old image)
</verification>

<success_criteria>
- DATA-02 SC #1~6 모두 충족 (worker 측):
  - SC#1: workers/intraday-sync 워크스페이스 존재 + 매분 cycle 실행
  - SC#2: kiwoom_tokens + 2 RPC production 적용
  - SC#3: Cloud Run Job + Scheduler ENABLED
  - SC#4: VPC + Subnet + Router + NAT + Static IP 생성
  - SC#6: 첫 cycle ka10027 ≥1,500 row + stock_quotes 1898 row 갱신
- SC#5 (server 측) + SC#7 (KIS 폐기) 는 Plan 10 + 11 에서
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-intraday-current-price/09.1-09-SUMMARY.md`
</output>
