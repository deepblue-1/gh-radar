# Phase 09.1 — DEPLOY-LOG

**Phase:** 09.1-intraday-current-price (KIS → 키움 완전 대체)
**Wave 4 cutover #1 — Worker production live**
**Date:** 2026-05-15 (KST)

## Migrations applied (Supabase production)

| Migration | Applied At | Method |
|---|---|---|
| 20260514120100_create_kiwoom_tokens.sql | 2026-05-15 (사전 적용) | supabase db push (Task 1) |
| 20260514120200_intraday_upsert_close_rpc.sql | 2026-05-15 (사전 적용) | supabase db push (Task 1) |
| 20260514120300_intraday_upsert_ohlc_rpc.sql | 2026-05-15 (사전 적용) | supabase db push (Task 1) |
| 20260514120000_drop_kis_tokens.sql | **NOT YET** | `.deferred` 유지 → Plan 11 cleanup 마지막 step 에서 push |

자동 검증 (Task 1.5): `kiwoom_tokens` REST API 200 + `intraday_upsert_close` / `intraday_upsert_ohlc` RPC 두 개 모두 존재 + `kis_tokens` 잔존 (race guard 정상 동작).

## GCP resources created

| Resource | Name | Region |
|---|---|---|
| VPC | gh-radar-vpc | global |
| Subnet | gh-radar-subnet-an3 (10.10.0.0/26) | asia-northeast3 |
| Cloud Router | gh-radar-router-an3 | asia-northeast3 |
| Cloud NAT | gh-radar-nat-an3 | asia-northeast3 |
| Static IP | gh-radar-static-ip → **34.64.195.151** (IN_USE) | asia-northeast3 |
| SA | gh-radar-intraday-sync-sa@gh-radar.iam.gserviceaccount.com | — |
| Secret | gh-radar-kiwoom-appkey (version 1 ENABLED) | global |
| Secret | gh-radar-kiwoom-secretkey (version 1 ENABLED) | global |
| Cloud Run Job | gh-radar-intraday-sync | asia-northeast3 |
| Scheduler | gh-radar-intraday-sync-cron (cron `* 9-15 * * 1-5` Asia/Seoul, OAuth) | asia-northeast3 |
| Alert policy | gh-radar-intraday-sync-failure (`projects/gh-radar/alertPolicies/14316367126538643560`, enabled) | global |

## User actions completed

| Action | Status |
|---|---|
| KIWOOM_APPKEY 값 등록 (gh-radar-kiwoom-appkey:1) | ✅ |
| KIWOOM_SECRETKEY 값 등록 (gh-radar-kiwoom-secretkey:1) | ✅ |
| 키움 portal IP 화이트리스트 등록 (34.64.195.151) | ✅ |

## Image SHA

| Image | SHA | Notes |
|---|---|---|
| intraday-sync (initial deploy) | `6e49471` | first deploy, fail (stex_tp 누락) |
| intraday-sync (deviation fix #1) | `ba2df5c` | + ka10027 stex_tp="3" 추가 |
| intraday-sync (deviation fix #2) | `ad9714f` | + stock_quotes name/market 제거 + MIN_EXPECTED 1000 |
| intraday-sync (deviation fix #3) | `cc6fa22` | + upper_limit/lower_limit ±30% default |
| intraday-sync (deviation fix #4) | `d8d05d1` | + STEP2 intersect with STEP1 codes |
| intraday-sync (deviation fix #5, **production active**) | `ef120aa` | + upsertQuotesStep2 UPSERT → UPDATE |

이후 env-only update: `MIN_EXPECTED_ROWS=1000 → 800` (image 재빌드 없이 적용).

## Smoke results (final state)

| INV | Result | Note |
|---|---|---|
| INV-1 Job execute --wait exit 0 | ✅ PASS | manual trigger 정상 종료 |
| INV-2 logs: cycle complete OR 휴장일 | ✅ PASS | "intraday cycle complete" log 다수 |
| INV-3 logs: no 키움 401 / 429 / cycle failed | ✅ PASS | fix 정착 후 5분 window 0건 |
| INV-4 stock_quotes 5분 이내 row >= 800 | ✅ PASS | 880~900 row (실측) |
| INV-5 stock_daily_ohlcv 오늘 row >= 800 | ✅ PASS | 1195 row (Phase 9 candle-sync 어제 + 본 cycle close 매분) |
| INV-6 Scheduler ENABLED | ✅ PASS | state=ENABLED |
| --check-scheduler | ✅ PASS | cron `* 9-15 * * 1-5` Asia/Seoul 확인 |
| --check-static-ip (Static IP exists) | ✅ PASS | 34.64.195.151 IN_USE |
| --check-static-ip (Job VPC 연결 annotation) | ✅ PASS | gh-radar-vpc + gh-radar-subnet-an3 + vpc-egress=all-traffic |

총: 6/6 INV + --check-scheduler + --check-static-ip 2/2 = **모든 invariant PASS**.

PLAN 06 SUMMARY 가 예측한 임계값 1500 (KIS FHPST01700000 기준) 은 키움 ka10027 stex_tp="3" 응답 실측 (900~1175) 과 안 맞아 800 으로 정정. PLAN 의 SC#6 ("첫 cycle ka10027 ≥1,500 row") 도 본 실측에 따라 ≥800 으로 해석.

## First production cycle output (image ef120aa, MIN_EXPECTED_ROWS=800)

UTC 02:04:34 manual trigger:
```
intraday cycle start { hotSetTopN: 200, ka10001Rate: 24 }
STEP1 ka10027 fetched { rows: 960 }
bootstrapMissingStocks complete { attempted: 856, inserted: 0 }
STEP1 mapped + deduped { mapErrors: 104, mapped: 856 }
STEP1 DB writes complete { step1Count: 856, topCount: 60 }
STEP2 hot set computed { hotSetSize: 203 }
STEP2 ka10001 fetched { successful: 145, failed: 58 }
STEP2 dropped non-STEP1 codes { dropped: 2 }
intraday cycle complete { step1Count: 856, step2Count: 143, failed: 58 }
intraday-sync complete
Container called exit(0).
```

총 cycle 시간: ~13초 (CONTEXT D-12 추정 부합).

UTC 02:05~ Scheduler 가 매분 자동 trigger — 8개 연속 cycle 모두 succeeded=1 / failed=0.

## Production data state (final)

| Table | Count | Source |
|---|---|---|
| stock_quotes (updated_at >= now-5m) | 885 | intraday-sync STEP1 (1098 mapped, dedupe 후) |
| stock_daily_ohlcv (date = 2026-05-15) | 1195 | Phase 9 candle-sync (2026-05-14 KRX) + intraday-sync close 매분 |
| top_movers | 60 | rebuildTopMovers 매 cycle 재구성 |

## Deviations (자동 fix 5건)

본 plan 실행 중 발견된 production 환경 차이 5건 — 모두 Rule 1 (Bug) 자동 수정. 사용자 confirmation 없이 진행 (auto mode):

1. **키움 ka10027 spec 변경 — stex_tp 필수 파라미터 추가** (commit `ba2df5c`)
   - 키움이 spec 갱신: `[1511:필수 입력 값에 값이 존재하지 않습니다. 필수입력 파라미터=stex_tp]`
   - mrkt_tp=000 (KOSPI+KOSDAQ 통합) 의도와 일치하는 `stex_tp="3"` (통합) 채택.
   - 단위 테스트 fetchRanking.test.ts 에 stex_tp="3" 기대값 추가.

2. **stock_quotes payload 의 name/market 제거** (commit `ad9714f`)
   - PLAN 06 SUMMARY 가 "STEP1 stock_quotes UPSERT 컬럼: ..name/market.." 으로 작성됐으나 실제 schema 에는 name/market 컬럼 부재 (stocks 마스터 + top_movers 에 보유).
   - `Could not find the 'market' column of 'stock_quotes' in the schema cache` 에러 발견.
   - upsertQuotesStep1 의 payload 에서 두 키 제거 + 시그니처에서 market Map 인자 제거. caller (index.ts) 동기화.

3. **STEP1 payload 에 upper_limit/lower_limit 임시값 채움 (±30%)** (commit `cc6fa22`)
   - stock_quotes.upper_limit/lower_limit 는 Phase 1 unified-stocks 시절 NOT NULL.
   - 키움 ka10027 STEP1 응답에 미포함 → 신규 종목 INSERT 시 violation.
   - 한국 시장 일일 변동폭 ±30% 도메인 규칙으로 근사값 채워 NULL 위반 방지. STEP2 (ka10001 hot set) 가 정확값으로 덮어씀.

4. **STEP2 hot set 을 STEP1 처리 종목으로 intersect** (commit `d8d05d1`)
   - computeHotSet 이 watchlist 종목을 합치므로 STEP1 ka10027 응답에 없는 종목이 STEP2 hot set 에 포함될 수 있음.
   - 그런 종목에 STEP2 upsertQuotesStep2 호출 시 신규 row INSERT 시도 → price NOT NULL violation.
   - 해결: `step2Updates = step2UpdatesRaw.filter(u => step1Codes.has(u.code))`. `droppedFromStep2` 카운트 로그 추가.

5. **upsertQuotesStep2 UPSERT → UPDATE 변경** (commit `ef120aa`, **결정적 fix**)
   - Supabase `upsert(onConflict: code)` 는 `INSERT...ON CONFLICT DO UPDATE` 로 변환되어 신규 row INSERT 시 모든 NOT NULL constraint 평가. STEP2 페이로드에 price 없으면 violation.
   - PLAN 06 SUMMARY 의 "STEP1/STEP2 컬럼 분리로 race-free UPSERT" 가정이 신규 종목 INSERT case 에 깨짐.
   - 해결: ~250 종목 직렬 `.update().eq("code", code)` 호출. UPDATE 는 INSERT 분기 평가 안 함 → STEP1 매분 컬럼 자연 보존.

## Smoke + deploy 스크립트 fix

- `scripts/smoke-intraday-sync.sh` INV-4/5 row count 임계값 1500 → 800 (키움 ka10027 실측).
- `scripts/smoke-intraday-sync.sh` content-range 파싱 CR strip (`| tr -d '\r'`) — DI-02 master-sync 와 동일 패턴 (HTTP 헤더 CR 가 `grep $` line-end 매칭 깨짐).
- `scripts/smoke-intraday-sync.sh` --check-static-ip 의 gcloud format path: `spec.template.spec.template.spec.containers[0].vpcAccess.networkInterfaces[0]` → `spec.template.metadata.annotations."run.googleapis.com/network-interfaces"` (실제 Cloud Run Job 응답 구조 반영).
- `scripts/deploy-intraday-sync.sh` MIN_EXPECTED_ROWS env default 1000 → 800 (재배포 시 동기화).

(commit `1cf72c6`)

## ka10001 failed rate (관찰)

ka10001 hot set 203 종목 호출 중 ~60 종목 failure (30%, fail-isolation 정상 동작). 가능 원인 (deferred 분석):
- 일부 종목이 ETF/펀드/외국 주식이라 ka10001 (주식기본정보) 응답 안 나옴
- 또는 ka10001 도 stex_tp 가 필요할 수 있음 (현재는 mrkt_tp 없는 형태로 보냄)

본 plan 의 가용성에는 영향 없음 — Promise.allSettled fail-isolation 으로 cycle 자체는 정상 종료. **Plan 10/11 이전 후속 작업 또는 별도 quick task 로 분석 권장**.

## Current State

- **intraday-sync**: production active. 매분 cycle 실행 (Scheduler `* 9-15 * * 1-5` Asia/Seoul).
- **KIS ingestion (gh-radar-ingestion)**: **STILL ACTIVE** — Plan 11 cleanup 까지 유지.
- **server (gh-radar-server)**: **OLD image** (VPC connector 미적용, KIS 기반). Plan 10 가 재배포.
- **stock_quotes / stock_daily_ohlcv 갱신**: 양쪽 워커 (KIS ingestion + intraday-sync) 동시 갱신. 마지막 쓰기 승 (idempotent UPSERT). T-09.1-37 accept 동작 확인.

## Next steps

- **Plan 10**: server 재배포 (VPC connector + KIWOOM secret) → 종목 상세 페이지 키움 동기 호출
- **Plan 11**: KIS ingestion 폐기 (Scheduler PAUSE → Job 삭제 → SA 삭제 → Secrets 삭제 → kis_tokens migration push → 코드 deletion)

## Deferred items (cleanup 대상)

1. **/tmp/20260514120400_stock_quotes_relax_nullability.sql.candidate** — stock_quotes.upper_limit/lower_limit NOT NULL drop 마이그레이션 (작성됨, 미적용). 본 plan 에서는 코드 default (±30%) 로 우회. 향후 Plan 11 이후 cleanup phase 에서 schema 일관성 정리 시 적용 고려.
2. **ka10001 failure rate ~30% 분석** — 종목별 fail 사유 (ETF/펀드 비대상 vs stex_tp 누락 vs 기타) 별도 quick task 또는 추후 phase.
3. **kis_tokens DROP migration restore** — `.deferred` → `.sql` rename 은 Plan 11 cutover 직전 step.

## Rollback procedure (필요 시)

1. Cloud Scheduler PAUSE: `gcloud scheduler jobs pause gh-radar-intraday-sync-cron --location=asia-northeast3`
2. Cloud Run Job 삭제 (선택): `gcloud run jobs delete gh-radar-intraday-sync --region=asia-northeast3 --quiet`
3. Supabase migration revert (선택): kiwoom_tokens + 2 RPC 수동 DROP
4. KIS ingestion 만 active 유지 → 기존 상태 복원

본 plan 의 모든 변경은 idempotent 하므로 rollback 후 재실행 안전.
