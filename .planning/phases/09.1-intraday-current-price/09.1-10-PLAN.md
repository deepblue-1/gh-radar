---
phase: 09.1
plan: 10
type: execute
wave: 4
depends_on: [07, 08, 09]
files_modified:
  - .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md
autonomous: false
requirements: [DATA-02]
requirements_addressed:
  - DATA-02 (Wave 4 cutover #2 — server VPC 재배포 + 종목 상세 페이지 키움 동기 호출 검증. SC #5 (server kiwoom 교체) + SC #4 의 server 부분 (VPC connector 공유) 충족)
must_haves:
  truths:
    - "deploy-server.sh (Plan 08 갱신본) 가 production 실행되어 server 가 VPC connector + KIWOOM secret 으로 재배포"
    - "server 부팅 시 createKiwoomRuntime 가 정상 토큰 발급 (Cloud Logging 의 'Kiwoom runtime ready' 메시지)"
    - "GET /api/stocks/005930 응답이 정상 200 + StockQuoteRow (kiwoom 호출 성공)"
    - "기존 server smoke (smoke-server.sh INV-1~9) 모두 PASS — KIS dead config 가 server 부팅 차단 안 함"
    - "DEPLOY-LOG.md 갱신 (Wave 4 cutover #2 section 추가)"
    - "본 plan 종료 시점에 KIS ingestion 은 여전히 ACTIVE — Plan 11 cleanup 까지 유지"
  artifacts:
    - path: .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md
      provides: "cutover #2 결과 추가 — server image SHA + outbound IP 검증 + KIWOOM ka10001 응답 sample"
  key_links:
    - from: "server 종목 상세 페이지"
      to: "Static IP → 키움 ka10001"
      via: "Cloud Run service VPC connector → Cloud NAT → Static IP → 키움 응답"
      pattern: "Kiwoom runtime ready"
---

<objective>
**[BLOCKING]** Wave 4 의 두 번째 cutover task — `scripts/deploy-server.sh` (Plan 08 갱신본) 실행하여 Cloud Run service `gh-radar-server` 를 VPC connector + KIWOOM secret 으로 재배포. 종목 상세 페이지의 on-demand `inquirePrice` 호출이 키움 ka10001 동기 호출로 전환된다.

Purpose: SC #5 (server kiwoom 교체) + SC #4 의 server 부분 (VPC connector 공유) 충족. 사용자 confirm 필요 — production server 에 1-2분 cold-start latency spike 가능 (RESEARCH §4.6 T-12).

Output: server image SHA 갱신 + DEPLOY-LOG cutover #2 section. KIS ingestion 은 여전히 ACTIVE (Plan 11 cleanup 까지).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md
@.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md
@.planning/phases/09.1-intraday-current-price/09.1-07-PLAN.md
@.planning/phases/09.1-intraday-current-price/09.1-08-PLAN.md
@.planning/phases/09.1-intraday-current-price/09.1-09-PLAN.md
@scripts/deploy-server.sh
@scripts/smoke-server.sh

<read_first>
- `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §4.6 (server VPC 옵션 + cold-start pitfall T-12), §13 (server kiwoom client)
- `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-30 (server 재배포 필요)
- `scripts/deploy-server.sh` (Plan 08 갱신본 — VPC + KIWOOM)
- `scripts/smoke-server.sh` (기존 server smoke INV)
- Plan 09 의 DEPLOY-LOG.md (현재 상태 추적)
</read_first>

<interfaces>
<!-- Plan 08 의 deploy-server.sh 갱신본 핵심 변경 -->
- VPC: --network=gh-radar-vpc --subnet=gh-radar-subnet-an3 --vpc-egress=all-traffic
- Env: KIWOOM_BASE_URL + KIWOOM_TOKEN_TYPE 추가
- Secret: KIWOOM_APPKEY + KIWOOM_SECRETKEY 추가
- KIS env/secret 유지 (Plan 11 cleanup 까지 dead config)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 → production server | 1-2분 cold-start latency spike 양해. min-instances=1 유지. |
| Cloud Run service → 키움 API | VPC connector → Cloud NAT → Static IP (worker 와 공유). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-43 | Availability | server VPC 재배포 시 1-2분 downtime | mitigate | min-instances=1 유지. 재배포 시간대 사용자 선택 (저녁/주말 권장). 사용자 confirm 후 실행. |
| T-09.1-44 | Configuration | server 가 KIWOOM secret 미존재 시 부팅 실패 | mitigate | Plan 09 의 task 3 가 KIWOOM secret 값을 GCP 에 등록. 본 plan 의 deploy 가 secret 바인딩 — 부팅 시 createKiwoomRuntime 가 성공. |
| T-09.1-45 | Tampering | 새 server image 가 STAGE 검증 없이 production 직행 | accept | Cloud Run 의 traffic split 미사용 (단일 revision). 실패 시 즉시 rollback (gcloud run services update --to-revisions={이전 revision}=100). |
| T-09.1-46 | Information Disclosure | KIWOOM_APPKEY 가 server log 출력 | mitigate | server logger (pino) redact paths — Phase 1 부터의 패턴. KIWOOM 추가 redact 는 Plan 03 worker 패턴과 동일 (server 측 logger 도 Plan 07 의 갱신 일부). |
</threat_model>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: 사용자 confirm — server 재배포 시점 (cold-start downtime 양해)</name>
  <what-built>
    Plan 08 의 deploy-server.sh 갱신본 + Plan 07 의 server 코드 (kiwoom client + KIWOOM env). 본 task 는 사용자 confirm.
  </what-built>
  <action>본 task 의 사용자 액션은 <how-to-verify> 블록 참조. 자동화 가능한 부분 없음 — 사용자 confirm 필요.</action>
  <how-to-verify>
    사용자에게 다음 정보 제공:
    - 재배포 시 1-2분 cold-start latency spike (RESEARCH §4.6 T-12)
    - production server URL: https://gh-radar-server-1023658565518.asia-northeast3.run.app
    - min-instances=1 유지 → downtime 없음 (latency spike 만)
    - 권장 시점: 저녁 (KST 19:00 이후) 또는 주말

    사용자 confirm 메시지 예: "proceed" / "wait until {time}" / "abort"
  </how-to-verify>
  <resume-signal>"proceed" 또는 다른 시점 지정</resume-signal>
</task>

<task type="auto">
  <name>Task 2: deploy-server.sh 실행 (VPC connector + KIWOOM secret)</name>
  <files>(GCP resource — Cloud Run service)</files>
  <read_first>
    - `scripts/deploy-server.sh` (Plan 08 갱신본)
  </read_first>
  <action>
    배포 환경 변수 설정 후 실행:

    ```bash
    export GOOGLE_APPLICATION_CREDENTIALS=~/.config/gcloud/gh-radar-deployer.json
    export CLOUDSDK_CORE_PROJECT=gh-radar
    export GCP_PROJECT_ID=gh-radar
    export SUPABASE_URL=https://<project>.supabase.co
    export CORS_ALLOWED_ORIGINS="https://gh-radar-webapp.vercel.app,..."
    gcloud config configurations activate gh-radar

    bash scripts/deploy-server.sh
    ```

    결과:
    - server:${SHA} Artifact Registry 푸시
    - Cloud Run service gh-radar-server 갱신 (새 revision)
    - VPC connector 활성 (--network=gh-radar-vpc + --subnet=gh-radar-subnet-an3 + --vpc-egress=all-traffic)
    - KIWOOM_APPKEY/KIWOOM_SECRETKEY secret 바인딩
    - 기존 KIS_APP_KEY/KIS_APP_SECRET 도 유지 (Plan 11 cleanup 까지)

    스크립트 마지막에 smoke-server.sh 자동 실행 (기존 INV-1~9).
  </action>
  <verify>
    <automated>NETWORK=$(gcloud run services describe gh-radar-server --region=asia-northeast3 --format='value(spec.template.spec.containers[0].vpcAccess.networkInterfaces[0].network)') &amp;&amp; [ -n "$NETWORK" ] &amp;&amp; SECRETS=$(gcloud run services describe gh-radar-server --region=asia-northeast3 --format='value(spec.template.spec.containers[0].env)') &amp;&amp; echo "$SECRETS" | grep -q KIWOOM_APPKEY</automated>
  </verify>
  <done>
    - Cloud Run service 새 revision 활성 + VPC connector 연결
    - KIWOOM_APPKEY env 주입 확인
    - smoke-server.sh INV 모두 PASS (기존 9 INV)
    - 새 image SHA 캡처
  </done>
</task>

<task type="auto">
  <name>Task 3: 종목 상세 페이지 검증 (kiwoom 호출 + Cloud Logging "Kiwoom runtime ready")</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - production server URL
    - `server/src/services/kiwoom-runtime.ts` (Plan 07)
  </read_first>
  <action>
    1. server 부팅 로그 확인 (createKiwoomRuntime 성공 메시지):
       ```bash
       gcloud logging read '
         resource.type="cloud_run_revision"
         AND resource.labels.service_name="gh-radar-server"
         AND jsonPayload.msg="Kiwoom runtime ready"
       ' --freshness=5m --limit=3 --format='value(jsonPayload.msg, jsonPayload.tokenLen)'
       # 예상: "Kiwoom runtime ready" + tokenLen ~150
       ```

    2. 종목 상세 페이지 API 호출 — 005930 (삼성전자):
       ```bash
       URL=$(gcloud run services describe gh-radar-server --region=asia-northeast3 --format='value(status.url)')
       curl -s "${URL}/api/stocks/005930" | jq .
       # 예상: 200 + { code, name, market, price, ... } StockWithProximity 응답
       ```

    3. Cloud Logging 에서 키움 호출 검증:
       ```bash
       gcloud logging read '
         resource.type="cloud_run_revision"
         AND resource.labels.service_name="gh-radar-server"
         AND (jsonPayload.msg=~"kiwoom" OR jsonPayload.msg=~"inquirePrice")
       ' --freshness=5m --limit=10 --format='value(jsonPayload.msg)'
       ```

    4. 실패 시 (kiwoom 401/key denied):
       - KIWOOM secret 값 검증: `gcloud secrets versions access latest --secret=gh-radar-kiwoom-appkey | head -c 5; echo`
       - Static IP 가 server outbound 와 일치하는지: server 가 routes/health 또는 dedicated debug endpoint 로 outbound IP echo (만약 미구현이면 Cloud Logging 에서 키움 응답 status code 확인)
       - cached fallback 동작 확인: 종목 상세 응답이 200 + cached quote 반환 (기존 stock_quotes 의 last updated_at 표시)
  </action>
  <verify>
    <automated>URL=$(gcloud run services describe gh-radar-server --region=asia-northeast3 --format='value(status.url)') &amp;&amp; curl -fsS "$URL/api/stocks/005930" | jq -e '.code == "005930"'</automated>
  </verify>
  <done>
    - server Cloud Logging 에 "Kiwoom runtime ready" 메시지 1+회
    - GET /api/stocks/005930 → 200 + 정상 응답
    - 키움 401/429 에러 0건 (또는 cached fallback 동작 확인)
  </done>
</task>

<task type="auto">
  <name>Task 4: DEPLOY-LOG.md 갱신 — Wave 4 cutover #2 section 추가</name>
  <files>.planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md</files>
  <read_first>
    - 기존 DEPLOY-LOG.md (Plan 09 작성)
  </read_first>
  <action>
    DEPLOY-LOG.md 에 다음 section 추가 (기존 내용 보존):

    ```markdown
    ---

    ## Wave 4 cutover #2 — Server VPC redeploy

    **Date:** {today YYYY-MM-DD KST}
    **Trigger:** Plan 10 — server 재배포 (D-30, SC #5)

    ### Deployment

    | Item | Value |
    |---|---|
    | Service | gh-radar-server |
    | Old revision | gh-radar-server-{old-revision} (KIS only) |
    | New revision | gh-radar-server-{new-revision} (Kiwoom + KIS dead config) |
    | Image | `asia-northeast3-docker.pkg.dev/gh-radar/gh-radar/server:{SHA}` |
    | VPC | gh-radar-vpc + gh-radar-subnet-an3 + vpc-egress=all-traffic |
    | KIWOOM secrets | KIWOOM_APPKEY + KIWOOM_SECRETKEY (latest) bound |
    | KIS secrets | 유지 (Plan 11 cleanup 까지 dead) |

    ### Verification

    | Check | Result |
    |---|---|
    | smoke-server.sh INV-1~9 | PASS (모두) |
    | Cloud Logging "Kiwoom runtime ready" | ✅ ({tokenLen}) |
    | GET /api/stocks/005930 → 200 + StockWithProximity | ✅ |
    | 종목 상세 페이지 (webapp) 키움 동기 호출 정상 | ✅ |
    | 키움 401/429 에러 0건 (5분 윈도우) | ✅ |

    ### Outbound IP 검증

    server 가 Static IP `{Static IP 주소}` 로 outbound — 키움 응답 200 (IP whitelist 통과 확인).

    ### Latency Impact

    - 재배포 직후 첫 1-2분 latency spike (RESEARCH §4.6 T-12) — min-instances=1 유지로 cold-start 완화
    - 안정화 후: 종목 상세 평균 응답 < {N}ms (Cloud Logging 또는 Cloud Run metrics)

    ### Current State

    - **server**: Kiwoom + VPC active. KIS dead config 잔존 (Plan 11 cleanup 까지)
    - **intraday-sync**: production active (Plan 09 부터)
    - **KIS ingestion (gh-radar-ingestion)**: **STILL ACTIVE** — Plan 11 cleanup 까지

    ### Next steps

    - Plan 11 [BLOCKING]: KIS ingestion 폐기 — Cloud Scheduler PAUSE → 정합 검증 → Job/SA/Secret 삭제 → kis_tokens migration push → 코드 deletion + commit + STATE/REQUIREMENTS/ROADMAP 갱신
    ```
  </action>
  <verify>
    <automated>grep -q "Wave 4 cutover #2" .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md &amp;&amp; grep -q "Server VPC redeploy" .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md</automated>
  </verify>
  <done>
    - DEPLOY-LOG.md cutover #2 section 추가
    - 새 revision + image SHA 기록
    - smoke 결과 + Cloud Logging 검증 + outbound IP 명시
  </done>
</task>

</tasks>

<verification>
- Cloud Run service gh-radar-server 새 revision active + VPC connector
- 종목 상세 페이지 API GET /api/stocks/:code → 200 (kiwoom 호출 성공 또는 cached fallback)
- DEPLOY-LOG cutover #2 기록
- KIS ingestion 은 여전히 active (Plan 11 cleanup 대상)
</verification>

<success_criteria>
- DATA-02 SC #5 충족 — server/src/kis/* → server/src/kiwoom/* 교체 + production 활성
- DATA-02 SC #4 의 server 부분 충족 — Cloud Run service 도 동일 VPC connector 로 outbound 통합
- 종목 상세 페이지 실시간성 유지 (D-17 — DB 캐시 60s stale 회피, kiwoom 동기 호출)
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-intraday-current-price/09.1-10-SUMMARY.md`
</output>
