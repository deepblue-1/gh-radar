---
phase: 09.1-intraday-current-price
plan: 10
subsystem: cutover (Cloud Run service redeploy — VPC connector + KIWOOM secret 적용으로 server on-demand 종목 상세 페이지 키움 동기 호출 전환)

tags: [gcp, cloud-run, vpc, static-ip, kiwoom-api, server, redeploy, on-demand, production-cutover]

# Dependency graph
requires:
  - phase: 09.1-intraday-current-price
    provides: "Plan 07 의 server kiwoom client 4 모듈 + createKiwoomRuntime + inquirePriceToQuoteRow (StockQuoteRowUpsert) + routes/stocks.ts 의 kiwoomRuntime 통합 — image fe96bec 가 실행하는 코드 base"
  - phase: 09.1-intraday-current-price
    provides: "Plan 08 의 deploy-server.sh 갱신본 (VPC connector 옵션 + KIWOOM env 2종 + KIWOOM secret 2종 + VPC stack 존재 게이트) — Wave 4 cutover #2 실행 스크립트"
  - phase: 09.1-intraday-current-price
    provides: "Plan 09 의 GCP 인프라 (gh-radar-vpc + subnet 10.10.0.0/26 + Cloud NAT + Static IP 34.64.195.151) + KIWOOM secret value 등록 + 키움 portal IP whitelist — server 가 즉시 공유 가능"
provides:
  - "Cloud Run service `gh-radar-server` revision `gh-radar-server-00015-zr5` production active (image `fe96bec`)"
  - "Direct VPC Egress 적용 — server outbound 가 Cloud NAT 경유 Static IP `34.64.195.151` (worker 와 공유)"
  - "종목 상세 페이지 `/api/stocks/:code` 가 키움 ka10001 동기 호출로 전환 (D-17 충족) — 005930/000660 실측 200 OK"
  - "Cloud Logging `Kiwoom runtime ready` (tokenLen=86) — server 부팅 시 createKiwoomRuntime 정상 토큰 발급 검증"
  - "smoke-server.sh INV-1~9 모두 PASS (CORS/rate-limit/X-Request-Id 등 기존 invariant 유지)"
  - ".planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md cutover #2 section 추가"
affects:
  - "production webapp 종목 상세 페이지 (`/stocks/[code]`) 가 server → 키움 → 실시간 ka10001 응답으로 갱신"
  - "Plan 11 (KIS ingestion 폐기) — server 가 KIS 무사용 상태 확정. server/src/kis/* + services/kis-runtime.ts + tests/kis/* + config.ts kisBaseUrl/AppKey/AppSecret + deploy-server.sh 의 KIS env/secret 잔존만 cleanup 대상"
  - "키움 portal IP whitelist 운영 — Static IP 1개 (34.64.195.151) 가 worker + service 양쪽 outbound 통합 (D-29 충족, 사용자 운영 부담 최소화)"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cloud Run service Direct VPC Egress — annotation `run.googleapis.com/network-interfaces` + `vpc-access-egress=all-traffic` 으로 Serverless VPC Access connector 없이 native VPC 연결. Cloud Run Job (intraday-sync) + Cloud Run service (server) 가 동일 VPC + Cloud NAT 공유 (D-29)."
    - "Idempotent redeploy 패턴 — deploy-server.sh 가 secret accessor 5종 + VPC stack 존재 게이트 + smoke 9 INV 일괄 실행. Wave 3 setup 미실행 시 즉시 exit (T-09.1-36 mitigate)."
    - "Cold-start 최소화 패턴 — min-instances=1 유지로 idle instance 항시 ready. 첫 startup ~3초 (Kiwoom runtime ready), RESEARCH §4.6 T-12 의 1-2분 예측보다 우수."
    - "BEFORE/AFTER state 캡처 — cutover 직전 old revision/image SHA 기록 + cutover 직후 new state 검증. rollback 시 명확한 target."

key-files:
  created:
    - ".planning/phases/09.1-intraday-current-price/09.1-10-SUMMARY.md"
  modified:
    - ".planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md"

key-decisions:
  - "사용자가 prompt 에서 사전 승인한 Wave 4 cutover #2 진행 — Plan 의 Task 1 (checkpoint:human-action) 을 auto mode 기준으로 skip. 사용자가 명시한 '검증 단계 실패 발견 시 즉시 중단' 조건 충족."
  - "deploy-server.sh 의 SUPABASE_URL + CORS_ALLOWED_ORIGINS 는 기존 revision (gh-radar-server-00014-4nt) 에서 추출하여 재사용. MEMORY 의 '기존 credentials 재요청 금지' 룰 준수 (다시 묻지 않음)."
  - "Cloud Logging 검색 시 pino 의 실제 필드명은 `jsonPayload.message` (msg 아님) — Plan 본문 검증 쿼리의 `jsonPayload.msg=\"Kiwoom runtime ready\"` 패턴은 미동작. 본 실행에서 `jsonPayload.message` 로 자연 정정."
  - "outbound IP echo endpoint 미구현 — Plan 본문은 debug endpoint 가능성 언급. 실제 검증은 키움 ka10001 응답이 401/403 없이 정상 200 + 가격/OHLC 반환 → IP whitelist 통과 확인의 indirect 검증으로 대체."

patterns-established:
  - "Phase 09.1 Wave 4 cutover #2 패턴 — Plan 09 의 worker live + Plan 10 의 server live 가 동일 VPC + Static IP 공유. 키움 portal IP whitelist 1개로 양쪽 outbound 통합 (사용자 specifics 충족)."
  - "service revision 검증 4단계 — (1) `gcloud run services describe` revision name/image, (2) annotation 의 VPC network-interfaces + egress, (3) env/secret 주입 확인, (4) Cloud Logging 의 부팅 메시지 + endpoint smoke. 본 plan 4 단계 모두 PASS."

requirements-completed: [DATA-02]

# Metrics
duration: 8m
completed: 2026-05-15
---

# Phase 09.1 Plan 10: Wave 4 cutover #2 (Server VPC redeploy) Summary

**`gh-radar-server` Cloud Run service 가 VPC connector (`gh-radar-vpc` + `gh-radar-subnet-an3` + `vpc-egress=all-traffic`) + KIWOOM secret (gh-radar-kiwoom-appkey/secretkey:latest) 로 재배포되어 production active. 종목 상세 페이지의 on-demand 호출이 키움 ka10001 동기 호출로 전환 (D-17). 새 revision `gh-radar-server-00015-zr5` (image `fe96bec`), 부팅 시 `Kiwoom runtime ready` (tokenLen=86) 정상, smoke-server.sh 9/9 PASS, GET /api/stocks/005930+000660 200 + 완전한 StockWithProximity 응답. 키움 401/429 에러 0건 (10분 window). KIS ingestion 은 ACTIVE 유지 (Plan 11 cleanup 까지).**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-05-15T02:20:33Z
- **Completed:** 2026-05-15T02:28:22Z
- **Tasks:** 4 (Task 1 auto-skip via prompt confirmation, Task 2/3/4 자동 실행)
- **Files modified:** 2 (DEPLOY-LOG.md + 본 SUMMARY.md)
- **Image deploys:** 1 (initial — deviation 0건, RED-GREEN cycle 불필요)

## Accomplishments

### Server cutover 완료

1. **Pre-flight 검증** — 9 secret enabled (KIWOOM 2종 latest=1) + VPC stack live + Static IP 34.64.195.151 IN_USE + default compute SA 에 KIWOOM accessor + compute.networkUser 모두 바인딩 확인.
2. **BEFORE state 캡처** — 구 revision `gh-radar-server-00014-4nt`, image `3bb72d4` (KIS only), default network (VPC 미적용).
3. **deploy-server.sh 실행** — Docker build (amd64) + push (Artifact Registry) + `gcloud run deploy` 일괄. SUPABASE_URL + CORS_ALLOWED_ORIGINS 는 기존 revision 에서 추출 재사용 (사용자 재질문 회피).
4. **New revision active** — `gh-radar-server-00015-zr5`, image `asia-northeast3-docker.pkg.dev/gh-radar/gh-radar/server:fe96bec`. annotation `network-interfaces` = `[{"network":"gh-radar-vpc","subnetwork":"gh-radar-subnet-an3"}]`, `vpc-access-egress=all-traffic`.
5. **smoke-server.sh INV-1~9 9/9 PASS** — /api/health, /api/scanner, /api/stocks/:code, /api/stocks/000000 → 404, /api/stocks/search, CORS preflight (허용/거부), rate-limit 429, X-Request-Id.
6. **Cloud Logging 검증** — `Kiwoom runtime ready` (jsonPayload.message, tokenLen=86) 1회 (02:23:45 UTC) + `reusing cached Kiwoom token` 추후 호출에서 정상.
7. **종목 상세 API smoke** — GET /api/stocks/005930 (삼성전자) → 200 + price=283500, open/high/low/marketCap/upperLimit/lowerLimit 모두 채워짐. GET /api/stocks/000660 (SK하이닉스) → 동일 패턴 정상.
8. **키움 에러 0건** — 10분 window 의 jsonPayload.message 검색: 401/429/cycle failed 패턴 0건. smoke INV-4 의 'Stock 000000 not found' 2건만 정상 404 동작.
9. **DEPLOY-LOG.md 갱신** — cutover #2 section 추가 (60 lines): deployment 표 + verification 표 + outbound IP 검증 + latency impact + current state + next steps.

### 종목 상세 실측 응답 (cutover 직후, 02:26 UTC)

```
GET /api/stocks/005930 (삼성전자)
  price=283500, change=-12500 (-4.22%), volume=13029960
  open=291000, high=296500, low=278500
  marketCap=1657420000000000 (1,657조원)
  upperLimit=384500, lowerLimit=207500
  upperLimitProximity=0.737

GET /api/stocks/000660 (SK하이닉스)
  price=1916000, change=-54000 (-2.74%), volume=4569707
  open=1950000, high=1995000, low=1874000
  marketCap=1365537700000000
  upperLimit=2561000, lowerLimit=1379000
```

D-17 (server on-demand 키움 동기 호출) + STEP1 worker 의 cached volume/trade_amount 합성 (Plan 07 의 cached-SELECT-before-upsert 패턴) 모두 정상 동작.

## Task Commits

| Task | Description | Commit |
|---|---|---|
| 1 | 사용자 confirm (prompt 사전 승인) | (no commit) |
| 2 | deploy-server.sh 실행 (Docker build + push + gcloud run deploy + smoke) | (no commit — GCP only, image SHA `fe96bec` = git HEAD `fe96bec`) |
| 3 | 종목 상세 페이지 검증 (Cloud Logging + smoke) | (no commit — verification only) |
| 4 | DEPLOY-LOG.md Wave 4 cutover #2 section 추가 | `a7b9df3` (docs) |

**Plan metadata:** _SUMMARY commit appended after this file written_

## Files Created/Modified

### 신규 (1 파일)

- `.planning/phases/09.1-intraday-current-price/09.1-10-SUMMARY.md` — 본 SUMMARY

### 갱신 (1 파일)

- `.planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md` (+60 lines)
  - `## Wave 4 cutover #2 — Server VPC redeploy` section 추가
  - Deployment 표: service / old revision / new revision / image / VPC / KIWOOM env+secrets / KIS secrets 잔존 / service URL / min-instances
  - Verification 표 11 row 모두 ✅ (revision/VPC annotation/env/secret/smoke 9 INV/Cloud Logging/종목 smoke 2종/키움 에러 0건)
  - Outbound IP 검증 section: Static IP 34.64.195.151 경유 + 키움 IP whitelist 통과 확인
  - Latency Impact section: cold-start 약 3초, smoke 9 INV timeout 없이 통과, min-instances=1 유지
  - Current State section: server live (Kiwoom+VPC), intraday-sync live, KIS ingestion STILL ACTIVE, kis_tokens DROP `.deferred` 유지
  - Next steps: Plan 11 KIS ingestion 폐기

## Decisions Made

(주요 결정 4건 — 위 key-decisions 섹션 참조)

추가 메모:
- **deploy 실패 0건**: image SHA `fe96bec` = 현재 git HEAD, Plan 07 의 server 코드 (cached-SELECT-before-upsert + StockQuoteRowUpsert partial UPSERT) 가 production schema 와 자연 호환. Plan 09 의 5건 worker deviation 패턴 (key 컬럼 부재 + NOT NULL 위반 등) 이 server 측에서는 발생 안 함 — 이유는 Plan 07 의 `inquirePriceToQuoteRow` 가 `Omit<StockQuoteRow, "volume"|"trade_amount">` 반환으로 STEP1 의 volume/trade_amount 보존 + name/market 키 미포함 (mergeMasterAndQuote 가 stocks 마스터 + stock_quotes 합성으로 응답 만듦, server 가 stock_quotes 의 name/market 컬럼에 의존 안 함).
- **deploy 시간**: Docker build + push + gcloud run deploy + smoke 9 INV 모두 합쳐 ~5분 (background 실행). server cold-start 자체는 3초.
- **Token 캐시 공유 동작 확인**: `Kiwoom runtime ready` 가 단 1회 (02:23:45) + 이후 호출에서 `reusing cached Kiwoom token` 다수 — D-19 (worker 와 server 가 kiwoom_tokens row 공유) 정상 동작. intraday-sync worker 가 이미 토큰 발급 후 row 에 저장 → server 부팅 시 cached hit.

## Deviations from Plan

**None — plan executed exactly as written.**

Plan 본문의 Task 1 (사용자 confirm) 은 사용자가 prompt 에서 사전 승인 (auto mode + Wave 4 진행 명시 승인). Task 2/3/4 는 PLAN 본문의 action 블록을 그대로 실행. 자동 수정 (Rule 1/2/3) 또는 architectural 변경 (Rule 4) 발생 0건.

소소한 검색 쿼리 차이 (PLAN 의 `jsonPayload.msg=\"Kiwoom runtime ready\"` → 실제 `jsonPayload.message=\"Kiwoom runtime ready\"` 로 자연 정정) 는 운영 명령 수준의 자연 보정이라 deviation 으로 분류하지 않음. Plan 의 의도 (server 부팅 로그 확인) 충족.

## Issues Encountered

**None during execution.**

소소한 운영 노트:
- Plan 본문의 Cloud Logging 검색 query 가 `jsonPayload.msg=` 패턴 사용했으나 pino 가 실제 사용하는 필드는 `jsonPayload.message`. 첫 검색 0건 반환 → raw JSON 으로 필드 구조 확인 후 `jsonPayload.message` 로 검색 정정 → 정상 결과 1건 발견. Plan 본문의 sample query 가 향후 phase 에서 동일 패턴 사용 시 잘못된 0건 반환 가능성. 본 SUMMARY 의 patterns-established 에 명시.

## Threat Surface Notes

본 plan 의 threat_model (T-09.1-43 ~ 46) 모두 mitigate/accept 상태로 구현 완료:

- **T-09.1-43 (Availability — server VPC 재배포 시 1-2분 downtime)**: mitigate 동작 확인 — min-instances=1 유지, cold-start 약 3초 (예측 1-2분 대비 우수). 사용자 confirm 사전 완료.
- **T-09.1-44 (Configuration — KIWOOM secret 미존재 시 부팅 실패)**: mitigate 동작 확인 — Plan 09 의 Task 3 가 KIWOOM secret value 등록 + 본 plan 의 deploy 가 secret binding. 부팅 시 `Kiwoom runtime ready` (tokenLen=86) 정상.
- **T-09.1-45 (Tampering — STAGE 검증 없이 production 직행)**: accept — Cloud Run 의 traffic split 미사용 (단일 revision 100%). 실패 시 즉시 rollback 가능 (`gcloud run services update --to-revisions=gh-radar-server-00014-4nt=100`). 본 cutover 는 smoke 9/9 + Cloud Logging + 종목 smoke 2종 모두 PASS, rollback 불필요.
- **T-09.1-46 (Information Disclosure — KIWOOM_APPKEY 로그 출력)**: mitigate 동작 확인 — pino redact paths 가 token / appkey / secretkey / authorization header 모두 redacted. Cloud Logging 200+ entry 검사 시 KIWOOM 평문 값 0건 노출.

**신규 surface (plan threat_model 에 없음)**: 없음.

## Known Stubs

없음 — 본 plan 의 모든 변경이 production 인프라 + 실데이터 ingestion (키움 ka10001 실시간 호출). mock/placeholder 없음.

## Threat Flags

없음 — 본 plan 은 production cutover 만 진행, 신규 보안 surface 도입 없음. KIWOOM secret 은 Plan 09 가 이미 도입한 surface 의 server-side 활성화. VPC connector 도 Plan 09 의 worker 와 동일 stack 재사용.

## Next Phase Readiness

- **Plan 11 (KIS ingestion 폐기) 준비 완료**:
  - server 가 키움 동기 호출로 완전 전환 — KIS ingestion 폐기 후 server 측 영향 0.
  - 본 plan 후 KIS 잔존 surface (cleanup 대상):
    - `server/src/kis/` 디렉터리 전체 (Plan 07 부터 dead code)
    - `server/src/services/kis-runtime.ts` (Plan 07 부터 dead code)
    - `server/tests/kis/` 디렉터리 전체
    - `server/src/config.ts` 의 `kisBaseUrl / kisAppKey / kisAppSecret` 필드 (optional 화 상태)
    - `server/tests/setup.ts` 의 `KIS_APP_KEY/SECRET` env 주입
    - `packages/shared/src/kis.ts` (KIS 타입 정의)
    - `workers/ingestion/` 디렉터리 전체 (KIS ingestion 워커 원본)
    - GCP: `gh-radar-ingestion` Cloud Run Job + `gh-radar-ingestion-scheduler` Cloud Scheduler + `gh-radar-ingestion-sa` SA + `gh-radar-kis-app-key/secret` Secrets
    - `scripts/deploy-server.sh` 의 KIS env (`KIS_BASE_URL`) + KIS secret 2종 (`KIS_APP_KEY/SECRET`)
    - Supabase `kis_tokens` 테이블 (DROP migration `.deferred` rename 후 push)

- **Blocker 없음** — 새 server revision 100% traffic + smoke 9/9 PASS + 종목 상세 API 정상. Plan 11 cleanup 시작 시점 자유 선택 가능.

## Self-Check: PASSED

**Files verified to exist:**
- FOUND: .planning/phases/09.1-intraday-current-price/09.1-10-SUMMARY.md
- FOUND: .planning/phases/09.1-intraday-current-price/09.1-DEPLOY-LOG.md (modified, "Wave 4 cutover #2" + "Server VPC redeploy" grep hit)

**Commits verified in git log:**
- FOUND: a7b9df3 (Task 4 — DEPLOY-LOG Wave 4 cutover #2 추가)

**Production state verified:**
- Cloud Run service gh-radar-server revision = `gh-radar-server-00015-zr5` ACTIVE
- Image = `asia-northeast3-docker.pkg.dev/gh-radar/gh-radar/server:fe96bec`
- annotation `vpc-access-egress` = `all-traffic`
- annotation `network-interfaces` = `[{"network":"gh-radar-vpc","subnetwork":"gh-radar-subnet-an3"}]`
- KIWOOM_APPKEY + KIWOOM_SECRETKEY env bound from Secret Manager (latest version=1)
- KIWOOM_BASE_URL + KIWOOM_TOKEN_TYPE env injected
- KIS_APP_KEY + KIS_APP_SECRET + KIS_BASE_URL 잔존 (Plan 11 cleanup 대상, dead config)
- Cloud Logging "Kiwoom runtime ready" tokenLen=86 (1 entry, 02:23:45 UTC)
- smoke-server.sh 9/9 PASS
- GET /api/stocks/005930 → 200 + 완전 StockWithProximity
- GET /api/stocks/000660 → 200 + 완전 StockWithProximity
- 키움 401/429 / cycle failed 에러 0건 (10분 window, smoke INV-4 의 STOCK_NOT_FOUND 2건 제외)

---
*Phase: 09.1-intraday-current-price*
*Plan: 10*
*Completed: 2026-05-15*
