---
phase: 09.1
plan: 05
type: execute
wave: 1
depends_on: [02, 03, 04]
files_modified:
  - workers/intraday-sync/src/kiwoom/fetchHotSet.ts
  - workers/intraday-sync/src/pipeline/mapOhlc.ts
  - workers/intraday-sync/src/pipeline/hotSet.ts
  - workers/intraday-sync/tests/fixtures/ka10001-005930.json
  - workers/intraday-sync/tests/fixtures/ka10001-035720.json
  - workers/intraday-sync/tests/fixtures/ka10001-052670.json
  - workers/intraday-sync/tests/fixtures/ka10001-005935.json
  - workers/intraday-sync/tests/fixtures/ka10001-069500.json
  - workers/intraday-sync/tests/mapOhlc.test.ts
  - workers/intraday-sync/tests/hotSet.test.ts
  - workers/intraday-sync/tests/fetchHotSet.test.ts
autonomous: true
requirements: [DATA-02]
requirements_addressed:
  - DATA-02 (STEP2 — ka10001 단일 종목 호출 + Promise.allSettled fail-isolation + hot set 산출 (top 200 ∪ watchlist) + ka10001Row→IntradayOhlcUpdate mapper + mac 단위 가설 처리)
must_haves:
  truths:
    - "fetchKa10001ForHotSet 가 ~250 codes 를 Promise.allSettled + token bucket 24 req/s 로 호출 (fail-isolation)"
    - "ka10001RowToOhlcUpdate 가 open/high/low/upperLimit/lowerLimit/marketCap 변환 — mac 가설 단위=억원 (×10^8) 명시"
    - "computeHotSet 가 STEP1 응답의 top N + watchlists.stock_code unique 합집합 반환"
    - "5개 fixture 종목 (보통주/KOSDAQ 대형/변동성/우선주/ETF) 으로 mapper 변종 검증"
    - "한 종목 실패가 cycle 전체를 중단시키지 않음 (성공만 successful[] 반환, failed count 별도)"
  artifacts:
    - path: workers/intraday-sync/src/kiwoom/fetchHotSet.ts
      provides: "fetchKa10001ForHotSet(client, token, codes) — Promise.allSettled + rate limiter"
      exports: ["fetchKa10001ForHotSet"]
    - path: workers/intraday-sync/src/pipeline/mapOhlc.ts
      provides: "ka10001RowToOhlcUpdate + parseMac"
    - path: workers/intraday-sync/src/pipeline/hotSet.ts
      provides: "computeHotSet(supabase, step1Updates, topN) — 합집합 산출"
      exports: ["computeHotSet"]
  key_links:
    - from: "fetchKa10001ForHotSet"
      to: "rateLimiter.acquireKiwoomRateToken"
      via: "각 호출 직전 acquire (24 req/s)"
      pattern: "acquireKiwoomRateToken"
    - from: "computeHotSet"
      to: "watchlists 테이블"
      via: "supabase.from('watchlists').select('stock_code')"
      pattern: "from\\(\"watchlists\"\\)"
---

<objective>
STEP2 의 ka10001 단일 종목 호출 + hot set 산출 + OHLC mapper 를 구현하고 5개 종목 fixture (보통주/KOSDAQ/변동성/우선주/ETF) 로 변종 검증한다.

Purpose: hot set (top 200 ∪ watchlist unique, ~250 종목) 만 매분 ka10001 호출하여 정확한 OHLC + 상한가/하한가/시가총액 확보. RESEARCH §11 의 합집합 알고리즘 + §1.5 의 ka10001 응답 매핑 + Pattern 2 의 Promise.allSettled fail-isolation.

Output: 3 src + 5 fixture + 3 test. mac 단위 가설 (억원) 은 fixture 캡처 task 에서 production smoke 시 확정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md
@.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md
@.planning/phases/09.1-intraday-current-price/09.1-02-PLAN.md
@.planning/phases/09.1-intraday-current-price/09.1-04-PLAN.md
@supabase/migrations/20260416120000_watchlists.sql
@workers/intraday-sync/src/pipeline/map.ts

<read_first>
- `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §1.5 (ka10001 endpoint + 응답 + 7 필드 + mac 단위), §1.5.4 (fixture 캡처 5종 종목), §2.3 (mapOhlc 코드), §11 (hot set 알고리즘 + 코드 전문), §2.6 (Promise.allSettled fail-isolation), Pattern 2 (fetchHotSet 코드)
- `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-11~D-16, D-23
- `supabase/migrations/20260416120000_watchlists.sql` (watchlists.stock_code 컬럼 — service-role 가 SELECT)
- Plan 04 의 export: `parseSignedPrice, parseOptionalSignedNumber` 재사용
- Plan 04 의 `acquireKiwoomRateToken` from `rateLimiter.ts`
</read_first>

<interfaces>
<!-- Plan 02 의 KiwoomKa10001Row + IntradayOhlcUpdate -->
```typescript
export type KiwoomKa10001Row = {
  stk_cd: string;
  cur_prc: string;
  open_pric: string;
  high_pric: string;
  low_pric: string;
  upl_pric?: string;
  lst_pric?: string;
  mac?: string;
  // ...
};

export type IntradayOhlcUpdate = {
  code: string;
  date: string;
  open: number;
  high: number;
  low: number;
  upperLimit: number | null;
  lowerLimit: number | null;
  marketCap: number | null;
};
```

<!-- watchlists 테이블 schema (Phase 06.2) -->
```sql
CREATE TABLE public.watchlists (
  user_id uuid REFERENCES auth.users(id) ON DELETE CASCADE,
  stock_code text REFERENCES public.stocks(code) ON DELETE CASCADE,
  created_at timestamptz NOT NULL DEFAULT now(),
  PRIMARY KEY (user_id, stock_code)
);
```

<!-- Plan 04 의 export -->
import { parseSignedPrice, parseOptionalSignedNumber } from "./map";
import { acquireKiwoomRateToken } from "../kiwoom/rateLimiter";
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → Supabase watchlists | service-role read (RLS bypass). RLS 정책 사용자별이 아니라 service-role 만 무제한. |
| Worker → 키움 ka10001 | 매분 ~250 calls. token bucket 24 req/s 강제. Promise.allSettled 로 부분 실패 격리. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-15 | Tampering (silent data corruption) | mac 단위 가설 오류 (R2) | mitigate | parseMac 의 곱셈 상수 (× 10^8) 를 한 줄로 격리. Plan 06 의 production smoke 후 fixture 캡처로 확정. 잘못된 단위 시 1줄 변경 + production 정정. 본 plan 의 단위 테스트는 "가설 단위 = 억원" 명시 + 변경 시 테스트 expectation 도 갱신 필요. |
| T-09.1-16 | DoS | 한 종목 ka10001 실패가 cycle 전체 중단 | mitigate | Promise.allSettled (Promise.all 아님) — 각 호출 독립. failed count 만 로그 + cycle 계속. Phase 07.2 의 fail-isolation 패턴 mirror. |
| T-09.1-17 | DoS | hot set 250 동시 호출이 token bucket 우회 | mitigate | fetchKa10001ForHotSet 가 각 호출 직전 `await acquireKiwoomRateToken()` 호출 → bucket 강제. Promise.allSettled 의 동시성은 bucket 이 자연 직렬화 (24 req/s). |
| T-09.1-18 | Availability | watchlist 0 rows (사용자 없음) | mitigate | computeHotSet 가 빈 watchlist 시 top N 만 반환 (정상 동작). Empty 결과 시 step2 단계 skip (hot set 0 size → ka10001 0 calls). |
| T-09.1-19 | Information Disclosure | watchlists.user_id 노출 | accept | computeHotSet 는 `stock_code` 만 SELECT — user_id 미노출. service-role 사용은 정당 (worker 환경에서만). |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ka10001RowToOhlcUpdate + parseMac + 5개 fixture + 단위 테스트</name>
  <files>workers/intraday-sync/src/pipeline/mapOhlc.ts, workers/intraday-sync/tests/fixtures/ka10001-005930.json, workers/intraday-sync/tests/fixtures/ka10001-035720.json, workers/intraday-sync/tests/fixtures/ka10001-052670.json, workers/intraday-sync/tests/fixtures/ka10001-005935.json, workers/intraday-sync/tests/fixtures/ka10001-069500.json, workers/intraday-sync/tests/mapOhlc.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §1.5.2 (7 필드 매핑 표), §1.5.3 (mac 단위 가설), §2.3 (mapOhlc 전문)
    - `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-15 (mac 단위)
  </read_first>
  <behavior>
    - `ka10001RowToOhlcUpdate(row, "2026-05-14")` 가 open/high/low/upperLimit/lowerLimit/marketCap 매핑
    - `parseMac("5034000")` → 5034000 * 10^8 = 503,400,000,000,000 (가설: 억원, 50.34조 원)
    - `parseMac("")` → null
    - `parseMac(undefined)` → null
    - `parseMac("invalid")` → null
    - 005930 (삼성전자, 보통주, KOSPI) — 정상 OHLC + 상한가 무부호 → upperLimit number 양수
    - 035720 (카카오, KOSDAQ 대형) — KOSPI 와 동일 응답 패턴
    - 052670 (제일바이오, 변동성 큼) — 음수 부호 cur_prc / 0 등 edge case
    - 005935 (삼성전자우, 우선주) — 정상 응답 (우선주 응답 패턴)
    - 069500 (KODEX 200, ETF) — ETF 의 mac 응답 패턴 (NULL 또는 작은 값)
    - upl_pric / lst_pric 빈 문자열 → null
    - cur_prc / open_pric 누락 → throw
  </behavior>
  <action>
    Step 1: 5개 fixture 파일 신규 (RESEARCH §1.5.4 placeholder — Plan 06 의 production smoke 가 실데이터로 교체):

    `tests/fixtures/ka10001-005930.json` (삼성전자):
    ```json
    {
      "return_code": 0,
      "return_msg": "정상적으로 처리되었습니다",
      "stk_cd": "005930",
      "stk_nm": "삼성전자",
      "cur_prc": "+70500",
      "pred_pre": "+500",
      "flu_rt": "+0.71",
      "open_pric": "+70000",
      "high_pric": "+71000",
      "low_pric": "+69500",
      "upl_pric": "91000",
      "lst_pric": "49000",
      "mac": "4209000"
    }
    ```

    `tests/fixtures/ka10001-035720.json` (카카오, KOSDAQ 대형):
    ```json
    {
      "return_code": 0,
      "return_msg": "정상",
      "stk_cd": "035720",
      "stk_nm": "카카오",
      "cur_prc": "+45000",
      "pred_pre": "+1000",
      "flu_rt": "+2.27",
      "open_pric": "+44500",
      "high_pric": "+45500",
      "low_pric": "+44000",
      "upl_pric": "57850",
      "lst_pric": "31150",
      "mac": "200000"
    }
    ```

    `tests/fixtures/ka10001-052670.json` (제일바이오, 변동성):
    ```json
    {
      "return_code": 0,
      "return_msg": "정상",
      "stk_cd": "052670",
      "stk_nm": "제일바이오",
      "cur_prc": "-1500",
      "pred_pre": "-150",
      "flu_rt": "-9.09",
      "open_pric": "+1650",
      "high_pric": "+1700",
      "low_pric": "+1450",
      "upl_pric": "2145",
      "lst_pric": "1155",
      "mac": "150"
    }
    ```

    `tests/fixtures/ka10001-005935.json` (삼성전자우):
    ```json
    {
      "return_code": 0,
      "return_msg": "정상",
      "stk_cd": "005935",
      "stk_nm": "삼성전자우",
      "cur_prc": "+58000",
      "pred_pre": "+400",
      "flu_rt": "+0.69",
      "open_pric": "+57700",
      "high_pric": "+58200",
      "low_pric": "+57500",
      "upl_pric": "75400",
      "lst_pric": "40600",
      "mac": "470000"
    }
    ```

    `tests/fixtures/ka10001-069500.json` (KODEX 200, ETF):
    ```json
    {
      "return_code": 0,
      "return_msg": "정상",
      "stk_cd": "069500",
      "stk_nm": "KODEX 200",
      "cur_prc": "+40000",
      "pred_pre": "+200",
      "flu_rt": "+0.50",
      "open_pric": "+39900",
      "high_pric": "+40100",
      "low_pric": "+39850",
      "upl_pric": "",
      "lst_pric": "",
      "mac": ""
    }
    ```

    Step 2: `src/pipeline/mapOhlc.ts` 신규 (RESEARCH §2.3):
    ```typescript
    // workers/intraday-sync/src/pipeline/mapOhlc.ts
    //
    // STEP 2 mapper — ka10001 응답을 IntradayOhlcUpdate 로 변환.
    // RESEARCH §1.5 + §2.3. D-15 (mac 단위 가설), D-23 트레이딩 시그널 정책.

    import type { KiwoomKa10001Row, IntradayOhlcUpdate } from "@gh-radar/shared";
    import { parseSignedPrice, parseOptionalSignedNumber } from "./map";

    export function ka10001RowToOhlcUpdate(
      row: KiwoomKa10001Row,
      dateIso: string,
    ): IntradayOhlcUpdate {
      if (!/^\d{6}$/.test(row.stk_cd)) {
        throw new Error(`Invalid ka10001 stk_cd: "${row.stk_cd}"`);
      }

      const open = parseSignedPrice(row.open_pric).value;
      const high = parseSignedPrice(row.high_pric).value;
      const low = parseSignedPrice(row.low_pric).value;
      const upperLimit = parseOptionalSignedNumber(row.upl_pric);
      const lowerLimit = parseOptionalSignedNumber(row.lst_pric);
      const marketCap = parseMac(row.mac);

      return {
        code: row.stk_cd,
        date: dateIso,
        open,
        high,
        low,
        upperLimit: upperLimit !== null ? Math.abs(upperLimit) : null,
        lowerLimit: lowerLimit !== null ? Math.abs(lowerLimit) : null,
        marketCap,
      };
    }

    /**
     * mac 단위 가설: 억원 (KIS hts_avls 컨벤션) — Plan 06 production fixture 캡처로 확정 (R2).
     * 잘못된 단위 발견 시 본 함수의 곱셈 상수만 변경.
     */
    export function parseMac(s: string | undefined): number | null {
      if (!s || s.trim() === "") return null;
      const n = Number(s.replace(/,/g, ""));
      if (!Number.isFinite(n)) return null;
      return Math.round(n * 100_000_000);
    }
    ```

    Step 3: `tests/mapOhlc.test.ts`:
    ```typescript
    import { describe, it, expect } from "vitest";
    import { ka10001RowToOhlcUpdate, parseMac } from "../src/pipeline/mapOhlc";
    import samsung from "./fixtures/ka10001-005930.json";
    import kakao from "./fixtures/ka10001-035720.json";
    import jeil from "./fixtures/ka10001-052670.json";
    import samsungPref from "./fixtures/ka10001-005935.json";
    import kodex200 from "./fixtures/ka10001-069500.json";

    describe("parseMac", () => {
      it("가설 단위 = 억원 → ×10^8", () => {
        expect(parseMac("4209000")).toBe(4209000 * 100_000_000);
      });
      it("빈 문자열 → null", () => {
        expect(parseMac("")).toBeNull();
        expect(parseMac(undefined)).toBeNull();
      });
      it("invalid → null", () => {
        expect(parseMac("abc")).toBeNull();
      });
      it("콤마 처리", () => {
        expect(parseMac("4,209,000")).toBe(4209000 * 100_000_000);
      });
    });

    describe("ka10001RowToOhlcUpdate", () => {
      it("005930 (삼성전자, 보통주 KOSPI)", () => {
        const r = ka10001RowToOhlcUpdate(samsung as any, "2026-05-14");
        expect(r.code).toBe("005930");
        expect(r.open).toBe(70000);
        expect(r.high).toBe(71000);
        expect(r.low).toBe(69500);
        expect(r.upperLimit).toBe(91000);
        expect(r.lowerLimit).toBe(49000);
        expect(r.marketCap).toBe(4209000 * 100_000_000);
      });

      it("035720 (카카오, KOSDAQ 대형) — 정상 응답", () => {
        const r = ka10001RowToOhlcUpdate(kakao as any, "2026-05-14");
        expect(r.code).toBe("035720");
        expect(r.open).toBe(44500);
        expect(r.upperLimit).toBe(57850);
      });

      it("052670 (제일바이오, 변동성) — 음수 cur_prc 환경에서도 open/high/low 양수", () => {
        const r = ka10001RowToOhlcUpdate(jeil as any, "2026-05-14");
        expect(r.code).toBe("052670");
        expect(r.open).toBe(1650);
        expect(r.high).toBe(1700);
        expect(r.low).toBe(1450);
      });

      it("005935 (삼성전자우, 우선주)", () => {
        const r = ka10001RowToOhlcUpdate(samsungPref as any, "2026-05-14");
        expect(r.code).toBe("005935");
        expect(r.open).toBe(57700);
        expect(r.upperLimit).toBe(75400);
      });

      it("069500 (KODEX 200, ETF) — upl_pric/lst_pric/mac 빈 문자열 → null", () => {
        const r = ka10001RowToOhlcUpdate(kodex200 as any, "2026-05-14");
        expect(r.code).toBe("069500");
        expect(r.upperLimit).toBeNull();
        expect(r.lowerLimit).toBeNull();
        expect(r.marketCap).toBeNull();
        expect(r.open).toBe(39900);
      });

      it("stk_cd 6자 아니면 throw", () => {
        expect(() => ka10001RowToOhlcUpdate(
          { ...samsung, stk_cd: "INVALID" } as any,
          "2026-05-14",
        )).toThrow(/Invalid ka10001 stk_cd/);
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar &amp;&amp; pnpm -F @gh-radar/intraday-sync test -- mapOhlc.test.ts</automated>
  </verify>
  <done>
    - 5 fixture + mapOhlc.ts + 1 test 파일
    - 7 테스트 case (parseMac 4 + ka10001RowToOhlcUpdate 5 + throw 1 → 통합)
    - mac 단위 가설 명시 (× 10^8) + 변경 시 1줄 격리
    - upl_pric / lst_pric 빈 문자열 → null 처리
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hotSet.ts (top 200 ∪ watchlist 합집합) + 단위 테스트</name>
  <files>workers/intraday-sync/src/pipeline/hotSet.ts, workers/intraday-sync/tests/hotSet.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §11 (computeHotSet 전문)
    - `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-11, D-13
  </read_first>
  <behavior>
    - `computeHotSet(supabase, step1Updates[300개], topN=200)` → top 200 codes + watchlist 합집합
    - watchlist 가 빈 테이블 → top 200 만 반환 (200 size)
    - watchlist 30개 unique + 그 중 5개가 top 200 와 겹침 → 200 + 25 = 225 size
    - step1Updates 가 100개만 (topN=200) → 100 + watchlist size
    - Supabase 에러 → throw
    - 반환 결과는 string[] (순서 보장 X)
  </behavior>
  <action>
    Step 1: `src/pipeline/hotSet.ts` 신규 (RESEARCH §11 전문):
    ```typescript
    // workers/intraday-sync/src/pipeline/hotSet.ts
    //
    // STEP 2 hot set 산출. RESEARCH §11 + D-11.
    //   hot set = (STEP1 top N) ∪ (watchlists.stock_code unique 전체)
    //
    // ka10027 응답이 sort_tp=1 (상승률 내림차순) 이므로 추가 정렬 불필요 → slice(0, N).

    import type { SupabaseClient } from "@supabase/supabase-js";
    import type { IntradayCloseUpdate } from "@gh-radar/shared";

    export async function computeHotSet(
      supabase: SupabaseClient,
      step1Updates: IntradayCloseUpdate[],
      topN: number,
    ): Promise<string[]> {
      // 1. STEP1 응답에서 top N (이미 등락률 내림차순)
      const topMovers = step1Updates.slice(0, topN).map((u) => u.code);

      // 2. watchlists 전체 unique stock_code
      const { data: watchlistRows, error } = await supabase
        .from("watchlists")
        .select("stock_code");

      if (error) throw error;

      const watchlistCodes = (watchlistRows ?? []).map(
        (r: { stock_code: string }) => r.stock_code,
      );

      // 3. 합집합 (Set 중복 제거)
      const union = new Set<string>([...topMovers, ...watchlistCodes]);

      return Array.from(union);
    }
    ```

    Step 2: `tests/hotSet.test.ts`:
    ```typescript
    import { describe, it, expect, vi } from "vitest";
    import { computeHotSet } from "../src/pipeline/hotSet";
    import type { IntradayCloseUpdate } from "@gh-radar/shared";

    function mockSupabase(watchlistRows: { stock_code: string }[] | null, error?: any) {
      const select = vi.fn().mockResolvedValue({ data: watchlistRows, error: error ?? null });
      const from = vi.fn().mockReturnValue({ select });
      return { from } as any;
    }

    function makeStep1(codes: string[]): IntradayCloseUpdate[] {
      return codes.map((code) => ({
        code,
        date: "2026-05-14",
        price: 1000,
        changeAmount: 0,
        changeRate: 0,
        volume: 0,
        tradeAmount: 0,
      }));
    }

    describe("computeHotSet", () => {
      it("watchlist 빈 → top N 만 반환", async () => {
        const step1 = makeStep1([...Array(300)].map((_, i) => String(i + 1).padStart(6, "0")));
        const supabase = mockSupabase([]);
        const result = await computeHotSet(supabase, step1, 200);
        expect(result).toHaveLength(200);
        expect(result[0]).toBe("000001");
        expect(result).not.toContain("000201");
      });

      it("watchlist 30 unique + top 5 가 watchlist 와 겹침 → 200 + 25 = 225", async () => {
        const step1 = makeStep1([...Array(300)].map((_, i) => String(i + 1).padStart(6, "0")));
        const watchlist = [
          { stock_code: "000001" }, // 겹침
          { stock_code: "000002" }, // 겹침
          { stock_code: "000003" }, // 겹침
          { stock_code: "000004" }, // 겹침
          { stock_code: "000005" }, // 겹침
          ...Array(25)
            .fill(null)
            .map((_, i) => ({ stock_code: String(900000 + i).padStart(6, "0") })),
        ];
        const supabase = mockSupabase(watchlist);
        const result = await computeHotSet(supabase, step1, 200);
        expect(result).toHaveLength(225);
      });

      it("step1Updates 가 topN 미만 → 모두 + watchlist", async () => {
        const step1 = makeStep1(["000001", "000002", "000003"]);
        const supabase = mockSupabase([{ stock_code: "999999" }]);
        const result = await computeHotSet(supabase, step1, 200);
        expect(new Set(result)).toEqual(new Set(["000001", "000002", "000003", "999999"]));
      });

      it("supabase 에러 → throw", async () => {
        const step1 = makeStep1([]);
        const supabase = mockSupabase(null, new Error("supabase down"));
        await expect(computeHotSet(supabase, step1, 200)).rejects.toThrow(/supabase down/);
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar &amp;&amp; pnpm -F @gh-radar/intraday-sync test -- hotSet.test.ts</automated>
  </verify>
  <done>
    - hotSet.ts + hotSet.test.ts
    - 4 테스트 case (empty / 겹침 / topN 미만 / supabase 에러) green
    - 합집합 size 가 expected (D-11)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: fetchHotSet.ts (ka10001 순차 호출 + Promise.allSettled) + 단위 테스트</name>
  <files>workers/intraday-sync/src/kiwoom/fetchHotSet.ts, workers/intraday-sync/tests/fetchHotSet.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-intraday-current-price/09.1-RESEARCH.md` §2.6 (Promise.allSettled fail-isolation), Pattern 2 (fetchHotSet 전문), §1.5.1 (ka10001 endpoint + headers)
    - `.planning/phases/09.1-intraday-current-price/09.1-CONTEXT.md` D-12 (호출 시간 예산)
  </read_first>
  <behavior>
    - 250 codes 입력 → 250개 호출 + Promise.allSettled 로 합산
    - 각 호출 직전 acquireKiwoomRateToken (token bucket)
    - 일부 호출 실패 (rejected) → successful array + failed count 반환
    - return_code != 0 응답 → 해당 종목 throw → failed count 증가
    - 정상 응답 → successful[] 에 추가
    - rate limit (24 req/s) bucket 이 호출 직렬화 검증 (test 는 simple version — rate 검증은 통합 시 확인)
  </behavior>
  <action>
    Step 1: `src/kiwoom/fetchHotSet.ts` 신규 (RESEARCH Pattern 2):
    ```typescript
    import type { AxiosInstance } from "axios";
    import type { KiwoomKa10001Row } from "@gh-radar/shared";
    import { acquireKiwoomRateToken } from "./rateLimiter";

    /**
     * ka10001 hot set 순차 호출 (token bucket rate-limited).
     * RESEARCH Pattern 2 + §2.6 — Promise.allSettled fail-isolation.
     *
     * 종목별 실패가 cycle 전체 중단 안 함. failed count 만 별도 반환.
     */
    export async function fetchKa10001ForHotSet(
      client: AxiosInstance,
      token: string,
      codes: string[],
    ): Promise<{ successful: KiwoomKa10001Row[]; failed: number; failures: Array<{ code: string; error: string }> }> {
      const results = await Promise.allSettled(
        codes.map(async (code) => {
          await acquireKiwoomRateToken();
          const headers = {
            authorization: `Bearer ${token}`,
            "api-id": "ka10001",
            "content-type": "application/json;charset=utf-8",
          };
          const res = await client.post("/api/dostk/stkinfo", { stk_cd: code }, { headers });
          if (res.data.return_code !== 0) {
            throw new Error(`ka10001 ${code} return_code=${res.data.return_code}: ${res.data.return_msg}`);
          }
          return res.data as KiwoomKa10001Row;
        }),
      );

      const successful: KiwoomKa10001Row[] = [];
      const failures: Array<{ code: string; error: string }> = [];

      results.forEach((r, idx) => {
        if (r.status === "fulfilled") {
          successful.push(r.value);
        } else {
          failures.push({ code: codes[idx], error: (r.reason as Error).message });
        }
      });

      return { successful, failed: failures.length, failures };
    }
    ```

    Step 2: `tests/fetchHotSet.test.ts` — 일부 mock 실패:
    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest";
    import { fetchKa10001ForHotSet } from "../src/kiwoom/fetchHotSet";
    import { resetKiwoomRateLimiter, configureKiwoomRateLimiter } from "../src/kiwoom/rateLimiter";
    import samsung from "./fixtures/ka10001-005930.json";
    import kakao from "./fixtures/ka10001-035720.json";

    describe("fetchKa10001ForHotSet", () => {
      beforeEach(() => {
        // rate limiter 큰 capacity 로 단위 테스트가 직렬화 대기로 느려지지 않게
        configureKiwoomRateLimiter({ capacity: 1000, refillRatePerSec: 1000 });
        resetKiwoomRateLimiter();
      });

      it("모든 호출 성공 → successful = N, failed = 0", async () => {
        const post = vi
          .fn()
          .mockResolvedValueOnce({ data: samsung })
          .mockResolvedValueOnce({ data: kakao });
        const client = { post } as any;

        const out = await fetchKa10001ForHotSet(client, "TOKEN", ["005930", "035720"]);
        expect(out.successful).toHaveLength(2);
        expect(out.failed).toBe(0);
        expect(out.failures).toHaveLength(0);

        // 각 호출이 정확한 stk_cd + headers 로 호출됨
        expect(post).toHaveBeenCalledWith(
          "/api/dostk/stkinfo",
          { stk_cd: "005930" },
          expect.objectContaining({
            headers: expect.objectContaining({
              authorization: "Bearer TOKEN",
              "api-id": "ka10001",
            }),
          }),
        );
      });

      it("일부 호출 실패 → fail-isolation (성공만 successful, 실패는 failures)", async () => {
        const post = vi
          .fn()
          .mockResolvedValueOnce({ data: samsung })
          .mockRejectedValueOnce(new Error("network error"))
          .mockResolvedValueOnce({ data: kakao });
        const client = { post } as any;

        const out = await fetchKa10001ForHotSet(client, "TOKEN", ["005930", "BAD", "035720"]);
        expect(out.successful).toHaveLength(2);
        expect(out.failed).toBe(1);
        expect(out.failures[0].code).toBe("BAD");
      });

      it("return_code != 0 응답 → 해당 종목 실패로 분류", async () => {
        const post = vi
          .fn()
          .mockResolvedValueOnce({ data: samsung })
          .mockResolvedValueOnce({ data: { return_code: 1700, return_msg: "허용된 요청 개수를 초과" } });
        const client = { post } as any;

        const out = await fetchKa10001ForHotSet(client, "TOKEN", ["005930", "035720"]);
        expect(out.successful).toHaveLength(1);
        expect(out.failed).toBe(1);
        expect(out.failures[0].code).toBe("035720");
        expect(out.failures[0].error).toMatch(/1700.*허용된 요청 개수를 초과/);
      });

      it("빈 codes 입력 → 빈 결과", async () => {
        const post = vi.fn();
        const client = { post } as any;
        const out = await fetchKa10001ForHotSet(client, "TOKEN", []);
        expect(out.successful).toHaveLength(0);
        expect(out.failed).toBe(0);
        expect(post).not.toHaveBeenCalled();
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar &amp;&amp; pnpm -F @gh-radar/intraday-sync test -- fetchHotSet.test.ts</automated>
  </verify>
  <done>
    - fetchHotSet.ts + fetchHotSet.test.ts
    - 4 테스트 case (all success / partial failure / return_code error / empty) green
    - Promise.allSettled 패턴으로 fail-isolation 검증
    - failures[] 가 code + error message 보존
  </done>
</task>

</tasks>

<verification>
- `pnpm -F @gh-radar/intraday-sync test --run` 모든 단위 테스트 green (Plan 04 + Plan 05 합쳐 7+ test 파일)
- `pnpm -F @gh-radar/intraday-sync typecheck && pnpm -F @gh-radar/intraday-sync build` exit 0
- Plan 06 (pipeline 통합) 가 본 plan 의 export 들을 import 가능
</verification>

<success_criteria>
- STEP2 의 fetchHotSet + mapOhlc + hotSet 3개 컴포넌트가 단위 테스트로 검증
- 5 fixture 가 보통주/KOSDAQ/변동성/우선주/ETF 변종 커버
- mac 단위 가설 (×10^8) 이 mapOhlc.ts:parseMac 한 줄로 격리
- Plan 06 의 cycle 통합 시 본 plan 의 함수들을 직접 호출 가능
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-intraday-current-price/09.1-05-SUMMARY.md`
</output>
