---
phase: 22-gh-trade-ios-testflight-android-play
plan: 03
type: execute
wave: 2
depends_on: ["22-02"]
files_modified:
  - webapp/src/lib/supabase/public-path.ts
  - webapp/src/lib/supabase/middleware.ts
  - webapp/src/lib/supabase/__tests__/public-path.test.ts
  - webapp/src/app/privacy/page.tsx
  - webapp/src/app/privacy/__tests__/page.test.tsx
  - webapp/e2e/specs/auth-guards.spec.ts
autonomous: true
requirements: [MOBILE-02]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-11: 비로그인 브라우저·WebView 가 `/privacy` 를 열면 리다이렉트 없이 200 과 「개인정보처리방침」 제목·13개 절이 보인다(`https://trade.jx1.io/privacy` — push 는 22-07)"
    - "D-11: 페이지 문안은 22-02 에서 승인된 `22-PRIVACY-DRAFT.md` 본문(부록 제외)과 같고 미정 표식이 0개다"
    - "공개 경로 경계(T-22-12): `isPublicPath` 는 `/privacy` · `/privacy/…` 만 열고 `/privacyx` · `/privacy-x` 는 여전히 `/login?next=` 로 보낸다 · 기존 `/login` · `/auth/…` 판정과 로그인 사용자의 `/login` → `/` 분기는 그대로다"
    - "relay 결합 없음(D-11 · 메모리 「배포는 relay 먼저·push 나중」): `/privacy` 는 정적 RSC — 데이터 요청·클라이언트 훅·relay/API 호출이 없어 push 게이트의 백엔드 diff 가 구조적으로 0 이다"
    - "범위: 이 플랜의 웹 변경은 frontmatter 의 6개 파일뿐이다 — `/privacy` 외 화면·동작 변화 없음(CONTEXT 범위 밖 「웹 화면 변경」 예외는 `/privacy` 1개)"
  artifacts:
    - path: webapp/src/lib/supabase/public-path.ts
      provides: "PUBLIC_PREFIXES · isPublicPath (next/server 의존 없는 순수 모듈)"
      exports: ["PUBLIC_PREFIXES", "isPublicPath"]
    - path: webapp/src/app/privacy/page.tsx
      provides: "한글 개인정보처리방침 RSC 페이지 · metadata"
      contains: "개인정보처리방침"
    - path: webapp/src/lib/supabase/__tests__/public-path.test.ts
      provides: "공개 경로 판정 표(통과 · 차단)"
    - path: webapp/src/app/privacy/__tests__/page.test.tsx
      provides: "13개 절 · 항목표 · metadata.title 회귀면"
  key_links:
    - from: webapp/src/lib/supabase/middleware.ts
      to: webapp/src/lib/supabase/public-path.ts
      via: "const isPublic = isPublicPath(pathname)"
      pattern: "isPublicPath\\(pathname\\)"
    - from: webapp/src/app/privacy/page.tsx
      to: webapp/src/components/layout/center-shell.tsx
      via: "<CenterShell> 셸 재사용"
      pattern: "CenterShell"
  prohibitions:
    - statement: "MUST NOT `/privacy` 공개 라우트 1개 외의 웹 화면·동작을 바꾼다(CONTEXT 범위 밖 · D-04 트레이딩 접근 무변경 포함)"
      status: resolved
      verification: test
    - statement: "MUST NOT `/privacy` 가 데이터 요청·외부 호출을 한다 — 사용자 수에 비례하는 호출을 만들지 않는 정적 문서다(CLAUDE.md 운영 기준)"
      status: resolved
      verification: test
    - statement: "MUST NOT 승인된 초안과 다른 문구(추가·삭제·완화)를 페이지에 넣는다"
      status: resolved
      verification: judgment
---

<objective>
승인된 개인정보처리방침을 로그인 없이 열리는 공개 라우트 `/privacy` 로 만든다(D-11). 미들웨어 공개 판정을 순수 함수 `isPublicPath` 로 뽑아 경계를 단위 테스트로 잠그고, 페이지는 기존 `CenterShell` 과 타이포 토큰만 쓰는 정적 RSC 로 둔다.

Purpose: 스토어·테스터가 URL 로 방침을 볼 수 있어야 한다. 공개 prefix 는 인증 벽의 구멍이므로 `/privacy-x` 같은 이웃 경로가 새지 않음을 테스트로 고정한다(T-22-12).
Output: `public-path.ts` · 미들웨어 1줄 교체 · `/privacy` 페이지 · 단위 2파일 · e2e 2케이스. push 는 하지 않는다(22-07).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-PATTERNS.md
@webapp/src/lib/supabase/middleware.ts
@webapp/src/components/layout/center-shell.tsx
@webapp/e2e/specs/auth-guards.spec.ts

작업 루트는 메인 체크아웃 `/Users/alex/repos/gh-radar` 다. 웹 단일 파일 테스트는 `pnpm --filter @gh-radar/webapp exec vitest --run <경로>`. dev 서버는 **3100** — 필요하면 `PORT=3100 pnpm --filter @gh-radar/webapp run dev` 를 백그라운드로 띄운다(`dev.sh` 금지 — 다른 세션 포트를 kill 한다). Playwright 는 baseURL `http://localhost:3100` · 기존 서버 재사용.

<interfaces>
```ts
// webapp/src/lib/supabase/public-path.ts (신규 — 순수 모듈, import 없음)
export const PUBLIC_PREFIXES: readonly string[];   // ["/login", "/auth", "/privacy"]
export function isPublicPath(pathname: string): boolean;
//   pathname === prefix || pathname.startsWith(`${prefix}/`)  — 기존 middleware.ts L55-57 판정식 그대로

// webapp/src/lib/supabase/middleware.ts (기존 · 교체 지점)
//   L8  const PUBLIC_PREFIXES = ["/login", "/auth"];      → 삭제(새 모듈에서 import)
//   L55 const isPublic = PUBLIC_PREFIXES.some(...)       → const isPublic = isPublicPath(pathname);
//   L72 if (user && pathname === "/login") { … }         → 불변

// webapp/src/app/design/page.tsx 의 metadata 형식(analog)
// export const metadata = { title: 'Design Catalog · GH Trade', description: '…' };
// 타이포 토큰: --t-h1 · --t-h2 · --t-h3 · --t-base · --t-sm · --t-caption · 색: --fg · --muted-fg · --border · --border-subtle · --card
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `isPublicPath` 순수 모듈 추출 + `/privacy` 공개 prefix + 판정 표 단위 테스트</name>
  <files>webapp/src/lib/supabase/public-path.ts, webapp/src/lib/supabase/middleware.ts, webapp/src/lib/supabase/__tests__/public-path.test.ts</files>
  <read_first>
    - webapp/src/lib/supabase/middleware.ts (전체 — L4-8 주석·상수 · L54-57 판정식 · L72 로그인 분기)
    - webapp/src/lib/__tests__/safe-path.test.ts (L1-25 — it.each 판정 표 · 「깨지면 사용자가 겪는 일」 헤더 주석 관례)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-PATTERNS.md (`middleware.ts` · `public-path.test.ts` 절)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md (Pattern 6 · Security Domain V4)
  </read_first>
  <behavior>
    - 통과: `/privacy` · `/privacy/` · `/privacy/anything` · `/login` · `/auth/callback` → true
    - 차단: `/privacyx` · `/privacy-x` · `/` · `/trading` · `/me` · `/loginx` · `/authx` → false
    - `PUBLIC_PREFIXES` 가 정확히 `["/login", "/auth", "/privacy"]` 다(순서 포함)
  </behavior>
  <action>
    ① 테스트 먼저(RED): `webapp/src/lib/supabase/__tests__/public-path.test.ts` — `import { isPublicPath, PUBLIC_PREFIXES } from '../public-path'`. 파일 머리 주석(safe-path.test.ts 관례): 「Phase 22 Plan 03 Task 1 — 공개 경로 판정(D-11 · MOBILE-02b)」 + 깨지면 사용자가 겪는 일 — 통과 표가 깨지면 스토어·테스터가 방침 URL 에서 로그인 화면으로 튕긴다 / 차단 표가 깨지면 이름이 비슷한 보호 경로가 로그인 없이 열린다(인증 벽 구멍). `it.each` 통과 표 · 차단 표 · `PUBLIC_PREFIXES` 동일성 1케이스.
    ② `webapp/src/lib/supabase/public-path.ts` 를 만든다(GREEN): import 없는 순수 모듈. JSDoc 에 D-10(`/login` 로그인 화면 · `/auth` OAuth callback)과 Phase 22 D-11(`/privacy` 개인정보처리방침 — 스토어·테스터가 로그인 없이 연다)을 적고, 판정은 「prefix 와 같거나 prefix + `/` 로 시작」 경계 비교라 이웃 이름이 열리지 않는다고 적는다. 미들웨어에서 분리한 이유(미들웨어는 `next/server`·`@supabase/ssr` 를 import 해 jsdom 단위 테스트가 무겁다)도 한 줄.
    ③ `webapp/src/lib/supabase/middleware.ts`: L4-8 의 로컬 상수와 주석을 지우고 `import { isPublicPath } from "./public-path";` 를 더한 뒤 L55-57 을 `const isPublic = isPublicPath(pathname);` 한 줄로 바꾼다. `updateSession` 머리 주석의 「3. PUBLIC_PREFIXES 공개 whitelist」 문장은 새 모듈을 가리키게 고친다. 나머지(쿠키 3단 동기화 · `getUser()` · 리다이렉트 · L72 로그인 분기)는 한 글자도 바꾸지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/supabase/__tests__/public-path.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or "public-path.test.ts" is absent from the output</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'isPublicPath(pathname)' webapp/src/lib/supabase/middleware.ts` 가 1 · `grep -c 'const PUBLIC_PREFIXES' webapp/src/lib/supabase/middleware.ts` 가 0
    - `grep -c 'if (user && pathname === "/login")' webapp/src/lib/supabase/middleware.ts` 가 1 (로그인 분기 불변)
    - `grep -c '"/privacy"' webapp/src/lib/supabase/public-path.ts` 가 1 · `grep -c '^import' webapp/src/lib/supabase/public-path.ts` 가 0
    - 두 `<automated>` 가 통과한다
  </acceptance_criteria>
  <done>공개 경로 판정이 순수 함수로 테스트되고 `/privacy` 가 경계 안전하게 열렸다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `/privacy` 정적 RSC 페이지(승인 문안) + 페이지 테스트 + e2e 2케이스(공개 · 이웃 경로 차단)</name>
  <precondition>`.planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md` frontmatter 에 `status: approved` 가 있다(22-02 Task 3)</precondition>
  <files>webapp/src/app/privacy/page.tsx, webapp/src/app/privacy/__tests__/page.test.tsx, webapp/e2e/specs/auth-guards.spec.ts</files>
  <read_first>
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md (승인 문안 — 1~13절 · frontmatter `effective_date` · `contact`)
    - webapp/src/app/design/page.tsx (L1-45 — RSC `metadata` 형식 · `CenterShell` 사용 · 타이포 토큰 클래스)
    - webapp/src/components/layout/center-shell.tsx (전체 — `AppHeader themeToggle` + `max-w-4xl` 본문)
    - webapp/src/app/login/__tests__/page.test.tsx (L1-40 — 테스트 헤더 주석 · vi.mock 관례)
    - webapp/e2e/specs/auth-guards.spec.ts (전체 — 파일 레벨 storageState 비우기 · describe 안 케이스 형식 · L73-83 루트 케이스 주석)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-PATTERNS.md (`privacy/page.tsx` · `page.test.tsx` · `auth-guards.spec.ts` 절)
  </read_first>
  <behavior>
    - `render(<PrivacyPage />)` → level 1 heading 「개인정보처리방침」 1개
    - level 2 heading 13개가 22-02 interfaces 의 절 제목과 같은 순서로 있다(「1. 개인정보의 처리 목적」 … 「13. 시행일」)
    - 2절 표에 「Google 계정」 · 「관심종목」 · 「AI 애널리스트 대화」 · 「DMA 계정 정보」 텍스트가 있다
    - 13절에 초안 `effective_date` 날짜 문자열이 있다 · 문서 어디에도 미정 표식 문자열이 없다
    - `metadata.title === '개인정보처리방침 · GH Trade'`
    - e2e: 쿠키 없는 context 가 `/privacy` 로 가면 URL 이 `/privacy` 그대로이고 제목이 보인다 · `/privacy-x` 는 `/login?next=%2Fprivacy-x` 로 간다
  </behavior>
  <action>
    ① 테스트 먼저(RED): `webapp/src/app/privacy/__tests__/page.test.tsx` — `import PrivacyPage, { metadata } from '../page'`. `CenterShell` 안 `AppHeader` 가 인증·테마 컨텍스트를 요구하면 `vi.mock('@/components/layout/app-header', () => ({ AppHeader: () => null }))`. 헤더 주석: 「Phase 22 Plan 03 Task 2 — `/privacy`(D-11 · MOBILE-02c)」 + 깨지면 사용자가 겪는 일(절이 빠지면 법정 기재 사항 누락 · 제목이 바뀌면 스토어 링크 미리보기가 틀림). behavior 의 단언을 `getByRole('heading', { level, name })` 와 `getByText` 로 쓴다. 절 제목 13개 배열은 테스트 파일 안 상수로 둔다(초안 interfaces 와 같은 문자열).
    ② `webapp/src/app/privacy/page.tsx`(GREEN) — 서버 컴포넌트로 둔다: 클라이언트 지시어·데이터 요청·React 훅을 쓰지 않는다(정적 문서 · relay 결합 없음). `export const metadata = { title: '개인정보처리방침 · GH Trade', description: 'GH Trade(웹·iOS·Android 앱)가 처리하는 개인정보 항목·목적·보유 기간·위탁·권리 행사 방법' }`. 기본 export `PrivacyPage` 는 `<CenterShell>` 안에 `<article>`: `<h1 className="text-[length:var(--t-h1)] font-bold">개인정보처리방침</h1>` → 시행일 한 줄(`text-[var(--muted-fg)]`) → 머리말 → 절 1~13 을 `<section id="section-{n}">` + `<h2 className="text-[length:var(--t-h3)] font-semibold">{n}. 제목</h2>` 로. 본문은 `text-[length:var(--t-base)] leading-relaxed`, 목록은 `list-disc pl-5`. 2절 항목표는 `<div className="overflow-x-auto">` 로 감싼 `<table>`(열: 항목 · 저장 위치 · 목적 · 보유 기간 · 셀 `border-[var(--border)]` · `text-[length:var(--t-sm)]`) — 폰 폭(320~390px) WebView 에서 표가 잘리지 않고 가로 스크롤된다. 연락처가 이메일이면 `mailto:` 링크. 문안은 승인 초안 1~13절을 **그대로** 옮긴다(초안 뒤쪽의 검토자용 코드 근거·검토자 메모는 옮기지 않고, 그 존재를 페이지 주석에도 적지 않는다 · 문구 추가·삭제·완화 금지). 색은 토큰만 — 하드코딩 색 금지.
    ③ `webapp/e2e/specs/auth-guards.spec.ts`: 기존 describe 안에 두 케이스를 더한다 — `test("public: 미인증 /privacy → 리다이렉트 없이 개인정보처리방침", …)`: `page.goto("/privacy")` → `toHaveURL(/\/privacy$/)` → `getByRole("heading", { level: 1, name: "개인정보처리방침" })` 보임. `test("middleware-guard: 미인증 /privacy-x → /login?next=%2Fprivacy-x", …)`: `toHaveURL(/\/login\?next=%2Fprivacy-x$/)`. 파일 머리 「검증:」 목록에 두 줄을 더하고, 루트 `/` 케이스 주석의 「`PUBLIC_PREFIXES` (`/login`·`/auth`) 하나뿐」 문장을 `/privacy` 를 포함하도록(그리고 판정이 `lib/supabase/public-path.ts` 로 옮겨졌음을) 고친다. 다른 케이스는 건드리지 않는다.
    ④ 시각 확인(메모리 「손보는 표면 안의 시각 결함은 바로 고치기」): dev 서버 :3100 에서 `/privacy` 를 390px·1280px 폭 × 라이트·다크로 Playwright 스크린샷(scratchpad 경로)을 찍어 표 잘림·줄바꿈 깨짐·겹침이 있으면 이 태스크 안에서 고치고, 경로를 SUMMARY 에 적는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/app/privacy/__tests__/page.test.tsx src/lib/supabase/__tests__/public-path.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or "page.test.tsx" is absent from the output</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/auth-guards.spec.ts</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or no test title containing "privacy" appears in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE "use client|fetch\(|useEffect|useState" webapp/src/app/privacy/page.tsx` 가 0 · `grep -c "CenterShell" webapp/src/app/privacy/page.tsx` 가 1 이상 · `grep -c "개인정보처리방침 · GH Trade" webapp/src/app/privacy/page.tsx` 가 1
    - `grep -c "확인 필요" webapp/src/app/privacy/page.tsx` 가 0 · `grep -c "부록" webapp/src/app/privacy/page.tsx` 가 0
    - `grep -c "privacy" webapp/e2e/specs/auth-guards.spec.ts` 가 3 이상
    - dev 서버 기동 상태에서 `curl -s -o /dev/null -w '%{http_code}' http://localhost:3100/privacy` 가 `200` (쿠키 없음) · `curl -s -o /dev/null -w '%{http_code}' http://localhost:3100/privacy-x` 가 `307` 또는 `302`
    - `git diff --name-only {이 플랜 첫 커밋}^ HEAD -- webapp/` 의 목록이 frontmatter `files_modified` 6개의 부분집합이다(범위 밖 웹 변경 0)
    - 세 `<automated>` 가 통과하고 SUMMARY 에 스크린샷 4장 경로가 있다
  </acceptance_criteria>
  <done>승인된 방침이 로그인 없이 `/privacy` 에서 보이고, 이웃 경로는 막혀 있으며, 웹의 다른 표면은 그대로다 — push 는 하지 않았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 비로그인 요청 → Next middleware 인증 벽 | 공개 prefix 가 넓어지면 보호 경로가 로그인 없이 열린다 |
| 승인 문안 → 공개 페이지 | 옮기는 과정에서 문구가 바뀌면 승인이 무의미해진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-12 | Elevation of Privilege | `isPublicPath` 공개 prefix 경계 | medium | mitigate | `=== prefix` 또는 `prefix + '/'` 경계 비교 유지 · 차단 표 단위 테스트(`/privacyx` · `/privacy-x` · `/loginx` · `/authx`) · e2e `/privacy-x` → `/login?next=` |
| T-22-08 | Repudiation (법적) | 승인 문안과 페이지 불일치 | medium | mitigate | precondition `status: approved` · 13절 제목 테스트 · 미정 표식·부록 0(acceptance grep) · 문구 변경 금지(prohibition) |
| T-22-18 | Denial of Service / Tampering | `/privacy` 가 relay·API 에 결합돼 push 게이트를 흐림 | low | mitigate | 정적 RSC — 데이터 요청·훅 0(acceptance grep) · 웹 변경 파일 6개 부분집합 |
</threat_model>

<verification>
- 단위(public-path · page) green · webapp typecheck green · auth-guards e2e green(새 2케이스 포함)
- `/privacy` 200 · `/privacy-x` 로그인 리다이렉트 · 웹 변경 파일 6개 이내
</verification>

<success_criteria>
- `/privacy` 가 로그인 없이 승인 문안을 보여 주고, 인증 벽의 다른 경계는 바뀌지 않았다. 배포는 22-07 의 push 결정 뒤다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/Gemfile` · `mobile/Gemfile.lock` (fastlane `~> 2.240`) | Ruby 의존성 | 22-01 |
| `mobile/fastlane/build_numbers.rb` — `GhTradeBuildNumbers.ios_build_number(t)` (22-01) · `GhTradeBuildNumbers.android_version_code(t)` · `ANDROID_VERSION_CODE_MAX` (22-04) | 빌드 번호 함수 | 22-01 · 22-04 |
| `mobile/fastlane/test/build_numbers_test.rb` (Ruby minitest) | 단위 테스트 | 22-04 |
| `mobile/ios/App/fastlane/Fastfile` lane `beta` (22-01) · lane `latest` (22-06) · `mobile/ios/App/fastlane/Appfile` | fastlane | 22-01 · 22-06 |
| `mobile/android/fastlane/Fastfile` lanes `build` · `beta` · `validate` · `track` · `mobile/android/fastlane/Appfile` | fastlane | 22-04 |
| `mobile/scripts/setup-release-secrets.sh` — stages `dir` · `asc` (22-01) · `keystore` · `backup` · `play-sa` (22-04) | 사용자 `!` 비밀 주입 | 22-01 · 22-04 |
| `mobile/scripts/release-ios.sh` — 인자 `beta`(기본) · `latest`(22-06) · exit 3 = 비밀 env 누락 | 래퍼 | 22-01 · 22-06 |
| `mobile/scripts/release-android.sh` — 인자 `beta`(기본) · `build` · `validate` · `track` · `check` | 래퍼 | 22-04 |
| `mobile/scripts/check-ipa.sh` (`IPA CHECK OK`) · `mobile/scripts/check-aab.sh` (`AAB CHECK OK`) · `mobile/scripts/check-release-hygiene.sh` (`RELEASE HYGIENE OK`) | 검사 스크립트 | 22-01 · 22-04 |
| npm scripts `native:release:ios` (22-01) · `native:release:android` · `native:release:android:aab` (22-04) | 스크립트 | 22-01 · 22-04 |
| Gradle property `ghtradeVersionCode` · release `signingConfigs` ← env `GHTRADE_UPLOAD_STORE_FILE` · `GHTRADE_UPLOAD_STORE_PASSWORD` · `GHTRADE_UPLOAD_KEY_ALIAS` · `GHTRADE_UPLOAD_KEY_PASSWORD` · `gradle.taskGraph.whenReady` 릴리스 가드 | Android 빌드 설정 | 22-04 |
| env 이름: `ASC_KEY_ID` · `ASC_ISSUER_ID` · `ASC_KEY_PATH` · `GHTRADE_RELEASE_OUT` (ios.env) · `GHTRADE_UPLOAD_*` · `GHTRADE_UPLOAD_SHA1` · `GOOGLE_PLAY_JSON_KEY` (android.env) · `PLAY_RELEASE_STATUS` · `GHTRADE_RELEASE_ENV` · `GHTRADE_RELEASE_DIR` · `WEEKLY_WINE_FASTLANE_DIR` (선택 override) | env 이름(값은 저장소 밖) | 22-01 · 22-04 |
| 저장소 밖 `~/.config/gh-trade/release/` (700) — `ios.env` · `AuthKey_{KEYID}.p8` · `android.env` · `ghtrade-upload.jks` · `play-service-account.json` (600) · 산출 경로 `~/Library/Developer/gh-trade-release/ios` | 비밀·산출물(저장소 밖) | 22-01 · 22-04 · 22-06 |
| GCP Secret Manager `gh-radar-ghtrade-upload-keystore` · `gh-radar-ghtrade-upload-keystore-password` · `gh-radar-ghtrade-asc-api-key` · GCP SA `gh-radar-play-publisher` · API `androidpublisher.googleapis.com` | 외부(사용자 `!`) | 22-04 · 22-06 |
| iOS `Info.plist` `ITSAppUsesNonExemptEncryption` = false | 설정 | 22-01 |
| `mobile/.gitignore` 릴리스 비밀·산출물 패턴 | 설정 | 22-01 |
| `.planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md` (승인된 한글 초안) | 계획 문서 | 22-02 |
| 웹 `webapp/src/lib/supabase/public-path.ts` — `PUBLIC_PREFIXES` · `isPublicPath` | 유틸 | **22-03** |
| 웹 라우트 `/privacy` — `webapp/src/app/privacy/page.tsx` (`metadata.title` 「개인정보처리방침 · GH Trade」) | RSC 페이지 | **22-03** |
| 테스트: `webapp/src/lib/supabase/__tests__/public-path.test.ts` · `webapp/src/app/privacy/__tests__/page.test.tsx` · `webapp/e2e/specs/auth-guards.spec.ts` `/privacy` 2케이스 | 단위 · e2e | **22-03** |
| `mobile/README.md` 「릴리스 (Phase 22)」 절 · 테스터 안내 · 「비밀 파일」 · 범위 밖 갱신 | 문서 | 22-06 |
| `webapp/src/lib/native/google-client-ids.ts` 주석 갱신(상수 값 불변) | 주석 | 22-06 |
| 콘솔: ASC 앱 레코드 · TestFlight 내부 그룹 `GH Trade 테스터`(자동 배포) · Play 앱 · 내부 테스트 트랙 · GCP Android OAuth 클라이언트(업로드 키 · Play 앱 서명 인증서마다) | 외부 상태 | 22-01 · 22-05 · 22-06 |

<output>
Create `.planning/phases/22-gh-trade-ios-testflight-android-play/22-03-SUMMARY.md` when done — 테스트 결과 · 스크린샷 4장 경로 · 웹 변경 파일 목록 · 「push 하지 않음」을 적는다.
</output>
