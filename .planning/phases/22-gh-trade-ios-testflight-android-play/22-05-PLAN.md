---
phase: 22-gh-trade-ios-testflight-android-play
plan: 05
type: execute
wave: 3
depends_on: ["22-04"]
files_modified: []
autonomous: false
requirements: [MOBILE-02]
user_setup:
  - service: google-play-console
    why: "Play 는 첫 AAB 를 콘솔에서 수동으로 올려야 하고(D-10 · supply draft-app 오류 회피), 그 순간 Play 앱 서명 키 종류가 정해진다(D-08 one-way)"
    dashboard_config:
      - task: "앱 만들기 — 앱 이름 `GH Trade` · 기본 언어 한국어 · 앱 · 무료 · 선언 2개 동의"
        location: "play.google.com/console → 모든 앱 → 앱 만들기"
      - task: "내부 테스트 테스터 이메일 목록 `GH Trade 테스터` (본인 Google 계정 포함)"
        location: "GH Trade → 테스트 및 출시 → 테스트 → 내부 테스트 → 테스터"
      - task: "내부 테스트 새 버전 — 앱 서명 키 선택(Task 1 결정) → 22-04 AAB 업로드 → 내부 테스트로 출시"
        location: "GH Trade → 내부 테스트 → 새 버전 만들기"
      - task: "앱 서명 인증서 SHA-1(표시되는 인증서 전부) · 업로드 키 인증서 SHA-1 복사"
        location: "GH Trade → 테스트 및 출시 → 설정 → 앱 무결성 → Play 앱 서명"

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-08 (one-way): Play 앱 서명 등록과 앱 서명 키 종류가 사람 결정(Task 1 · blocking-human)을 거친 뒤에만 첫 업로드에서 확정된다 — 권장은 고전 Google 생성 키(RESEARCH Pitfall 5 · 발견 4)이고 선택한 옵션의 실제 콘솔 문구가 기록된다"
    - "D-10: Play 첫 AAB 는 사용자가 콘솔에서 수동으로 올리고 **내부 테스트로 출시**까지 한다 — 올린 파일은 22-04 파이프라인 산출 AAB(업로드 키 서명 · 운영 URL)다"
    - "D-02: 내부 테스트 트랙과 테스터 이메일 목록이 있고, 외부 공개 링크·비공개/공개 테스트 트랙은 만들지 않는다"
    - "D-08: Play 앱 서명 인증서 SHA-1(하이브리드면 표시되는 전부)과 업로드 키 SHA-1 이 재개 신호로 보고돼 22-06 GCP Android OAuth 클라이언트 등록 입력이 된다"
    - "D-12: 이 콘솔 절차에 Play 데이터 보안 양식·앱 콘텐츠 선언 작성이 없다 — 콘솔이 그 양식 없이 내부 테스트 출시를 막으면 멈추고 D-12 재논의를 요청한다"
  artifacts: []
  key_links:
    - from: mobile/scripts/release-android.sh
      to: "Play Console 내부 테스트 트랙"
      via: "`release-android.sh build`(22-04) 가 만든 AAB 를 사용자가 수동 업로드(D-10) — 이후 업로드는 `release-android.sh beta`(fastlane supply · 22-06)"
      pattern: "release-android.sh"
  prohibitions:
    - statement: "MUST NOT 자체 앱 서명 키를 만들어 Play 에 올리거나(PEPK) 앱 서명 키를 저장소·로컬에 보관한다 — 앱 서명 키는 Google 이 보관한다(D-08)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT 테스터 이메일을 SUMMARY·문서에 적는다 — 명수만 기록한다"
      status: resolved
      verification: test
---

<objective>
Play 쪽 one-way 문을 사람이 확인하고 지나간다: 앱 서명 키 종류를 결정한 뒤(Task 1), 사용자가 Play 앱을 만들고 22-04 의 AAB 를 내부 테스트 트랙에 처음 올려 출시한다(Task 2 · D-08 · D-10).

Purpose: 신규 Play 앱은 기본으로 양자 대비 하이브리드 서명(키 3개)에 등록되고, 이 경우 Google 로그인 `DEVELOPER_ERROR` 미해결 사례가 있다(RESEARCH Pitfall 5 · LOW 신뢰). 첫 업로드 화면에서 고전 키를 고르면 위험이 가장 싸게 줄어든다 — 공개 테스트·프로덕션 뒤에는 앱 서명 키를 바꿀 수 없다.
Output: Play 앱 · 내부 테스트 트랙(첫 버전 출시) · 테스터 목록 · 앱 서명/업로드 인증서 SHA-1 보고. 저장소 파일 변경 없음.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-CONTEXT.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-04-SUMMARY.md

작업 루트는 메인 체크아웃 `/Users/alex/repos/gh-radar` 다. 이 플랜은 콘솔 작업만 있다 — 실행기는 체크포인트를 띄우기 전에 AAB 가 업로드 키로 서명돼 있는지 확인하고, 22-04 SUMMARY 의 versionCode · AAB 경로 · 업로드 SHA-1 을 체크포인트 본문에 붙인다.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: D-08 one-way — Play 앱 서명 키 종류 결정(첫 업로드 화면에서 고를 옵션)</name>
  <read_first>
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-CONTEXT.md (D-08 · 리서치 메모 2026-09-27)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md (발견 4 · Pitfall 5 · State of the Art 「Play App Signing」 · Assumptions A3 · A4)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-04-SUMMARY.md (versionCode · AAB 경로 · 업로드 SHA-1)
  </read_first>
  <decision>Play 첫 AAB 업로드 화면의 「앱 서명 키」 선택에서 무엇을 고를 것인가 — 이 선택이 앱 서명 키를 확정한다</decision>
  <context>
    Play 앱 서명 등록은 첫 업로드 뒤 해제할 수 없다(D-08 one-way). 내부 테스트 단계에서는 「앱 서명 키 변경」 이 아직 가능하지만 공개 테스트·프로덕션 뒤에는 불가하다(RESEARCH 발견 4) — 지금 고르는 것이 가장 싸다. 되돌리는 비용: 잘못 고르면 Android 테스터 전원이 Google 로그인에 실패하고(`[28444]`/`DEVELOPER_ERROR` — 웹에는 「로그인 처리에 실패」 만 보임), 키 변경 요청 → 새 인증서 SHA-1 재등록 → 재설치가 필요하다. iOS 는 영향 없다. 실행기는 이 체크포인트를 띄우기 전에 verify 명령으로 AAB 가 업로드 키로 서명돼 있음을 확인하고 그 출력(`AAB CHECK OK sha1=…`)을 본문에 붙인다.
  </context>
  <options>
    <option id="classic">
      <name>고전 Google 생성 앱 서명 키(권장 — 화면에 선택지가 있으면)</name>
      <pros>Google 로그인 호환 사례가 안정적 · 앱 서명 인증서 1개라 GCP 등록 SHA-1 이 단순(업로드 1 + 앱 서명 1)</pros>
      <cons>양자 대비 서명 기능은 없다(내부 테스트와 무관) · 선택지 이름·위치가 콘솔마다 다를 수 있어 사용자가 화면 문구를 보고해야 한다(A3)</cons>
    </option>
    <option id="hybrid">
      <name>기본값(양자 대비 하이브리드) 수락 + 표시되는 인증서 전부 등록</name>
      <pros>콘솔 기본 경로 그대로 · 추가 조작 없음</pros>
      <cons>인증서 3개 → SHA-1 전부를 각각 Android OAuth 클라이언트로 등록해야 한다 · 모두 등록해도 `DEVELOPER_ERROR` 가 나는 미해결 사례(2026-09-25)가 있다 — 22-06 UAT 에서 실패하면 내부 테스트 단계 안에 「앱 서명 키 변경」 으로 고전 키로 옮긴다</cons>
    </option>
    <option id="own-key">
      <name>자체 앱 서명 키 업로드(PEPK) — 비권장</name>
      <pros>키를 직접 통제</pros>
      <cons>D-08(Google 보관 · 우리는 업로드 키만) 위반 — 앱 서명 키를 우리가 보관·백업해야 한다. 이 옵션을 고르면 D-08 재논의로 멈춘다</cons>
    </option>
  </options>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash mobile/scripts/release-android.sh check</automated>
    <fails_when>non-zero exit, or the output lacks "AAB CHECK OK", or contains "AAB CHECK FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - 재개 신호가 `classic` · `hybrid` · `own-key` 중 하나이고 SUMMARY 「결정」 절에 기록된다
    - `own-key` 면 Task 2 로 넘어가지 않고 멈춰 D-08 재논의를 요청한다
    - 이 체크포인트는 auto-advance 로 넘기지 않는다(blocking-human)
  </acceptance_criteria>
  <resume-signal>`classic` / `hybrid` / `own-key` 중 하나로 답한다</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Play 앱 생성 · 내부 테스터 목록 · 첫 AAB 수동 업로드(결정한 서명 키) · 내부 테스트 출시 · 인증서 SHA-1 보고</name>
  <reversibility rating="one-way">첫 업로드가 Play 앱 서명 등록과 앱 서명 키를 확정한다 — 해제 불가, 공개 테스트·프로덕션 뒤에는 키 변경도 불가(D-08).</reversibility>
  <read_first>
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md (Pitfall 5 · 9 · 10 · Manual-only 표)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-04-SUMMARY.md (AAB 경로 · versionCode)
  </read_first>
  <action>사용자가 Play Console 에서 앱을 만들고 22-04 의 AAB 를 내부 테스트 트랙에 처음 올려 출시한다(D-10). Play 개발자 계정은 이미 있다(D-01). 데이터 보안 양식·앱 콘텐츠 선언은 채우지 않는다(D-12).</action>
  <instructions>
    Claude 가 이미 한 것: 업로드 키로 서명된 운영 설정 AAB 를 만들어 검사했다 — 경로 `mobile/android/app/build/outputs/bundle/release/app-release.aab` (22-04 SUMMARY 의 versionCode). Task 1 에서 서명 키 옵션이 정해졌다.

    1. https://play.google.com/console → 모든 앱 → 「앱 만들기」: 앱 이름 `GH Trade` · 기본 언어 **한국어(ko-KR)** · 앱 · 무료 · 개발자 프로그램 정책·미국 수출법 선언 동의 → 앱 만들기.
    2. GH Trade → 테스트 및 출시 → 테스트 → **내부 테스트** → 「테스터」 탭 → 이메일 목록 만들기 `GH Trade 테스터` → 본인 Google 계정(+ 지금 알고 있는 테스터 Google 계정) → 저장. 외부 공개 링크·비공개/공개 테스트 트랙은 만들지 않는다(D-02).
    3. 내부 테스트 → 「새 버전 만들기」 → 앱 서명 안내가 나오면 **Task 1 에서 고른 옵션**을 선택한다(`classic` 이면 「Google 생성 키」 중 하이브리드·양자 대비가 아닌 쪽 — 화면 문구를 그대로 메모) → 위 AAB 를 끌어다 놓는다 → 출시 이름은 기본값 → 저장 → 「버전 검토」 → **「내부 테스트로 출시 시작」**. 콘솔이 데이터 보안 양식 등 앱 콘텐츠 없이 내부 테스트 출시를 막으면 여기서 멈추고 알려 준다(D-12 재논의).
    4. 테스트 및 출시 → 설정 → **앱 무결성** → 「Play 앱 서명」: 「앱 서명 키 인증서」 의 SHA-1(여러 개 표시되면 전부 — 「인증서 다운로드」 로 받은 것 포함)과 「업로드 키 인증서」 SHA-1 을 복사한다. SHA-1 은 공개 지문이라 재개 신호에 붙여도 된다. 업로드 키 SHA-1 은 22-04 에서 받은 값과 같아야 한다.
    5. 내부 테스트 → 테스터 탭의 「링크 복사」(참여 링크)는 22-06 테스터 안내에 쓴다 — 재개 신호에는 넣지 않아도 된다.
  </instructions>
  <verification>재개 뒤 Claude 가 확인할 수 있는 것: 업로드 키 SHA-1 이 22-04 기록과 같은지(문자열 대조), AAB 가 그대로인지(verify). Play 트랙 상태는 SA 가 생긴 뒤 22-06 의 `release-android.sh track` 이 자동 확인한다.</verification>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash mobile/scripts/release-android.sh check</automated>
    <fails_when>non-zero exit, or the output lacks "AAB CHECK OK"</fails_when>
  </verify>
  <acceptance_criteria>
    - 재개 신호가 `done 서명="{콘솔에서 고른 옵션 문구}" 앱서명SHA1={…}[,{…}] 업로드SHA1={…} 테스터={명수}` 형태이고 모든 값이 SUMMARY 「결정」 절에 기록된다(테스터 이메일은 적지 않는다)
    - 재개 신호의 업로드 SHA-1 이 22-04 SUMMARY 의 업로드 인증서 SHA-1 과 대소문자 무시로 같다 — 다르면 멈추고 알린다(다른 키로 올린 것)
    - 앱 서명 SHA-1 이 1개 이상 보고됐다(하이브리드면 표시된 전부)
  </acceptance_criteria>
  <resume-signal>`done 서명="Google 생성 키(권장)" 앱서명SHA1=AA:…,BB:… 업로드SHA1=CC:… 테스터=1` 처럼 답한다(콘솔이 막았으면 그 화면 문구를 붙인다)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 AAB(업로드 키) → Google Play 앱 서명 | Google 이 앱 서명 키로 재서명해 테스터 기기에 배포한다 — 설치본 서명이 로컬과 다르다 |
| Play 설치본 서명 → Google OAuth(Android 클라이언트) | 설치본 SHA-1 이 GCP 에 없으면 로그인이 실패한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-05 | Denial of Service (로그인 가용성) | Play 앱 서명 인증서 × Google 로그인 | medium | mitigate | Task 1 blocking-human 결정(고전 키 권장) · Task 2 가 앱 서명 SHA-1 전부 보고 · 22-06 이 각 SHA-1 로 Android 클라이언트 등록 + Play 설치본 logcat `signingSha1` UAT · 내부 테스트 단계 안에 키 변경 가능 |
| T-22-11 | Denial of Service (가용성) | 앱 서명 키 자체 보관(PEPK) 선택 | medium | mitigate | `own-key` 는 D-08 위반으로 멈춤(Task 1 acceptance) — 앱 서명 키는 Google 보관 |
| T-22-19 | Spoofing | 다른 키로 서명된 AAB 업로드 | low | mitigate | verify `AAB CHECK OK`(서명 SHA-1 = 업로드 키) · 재개 신호 업로드 SHA-1 대조 |
</threat_model>

<verification>
- Task 1 결정 기록 · AAB CHECK OK · Task 2 재개 신호(서명 옵션 문구 · 인증서 SHA-1 · 테스터 명수) · 업로드 SHA-1 일치
</verification>

<success_criteria>
- GH Trade 가 Play 내부 테스트 트랙에 첫 버전으로 출시됐고, 앱 서명 키 종류가 사람 결정으로 확정·기록됐다. 로그인 호환 확인은 22-06 이 한다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/Gemfile` · `mobile/Gemfile.lock` (fastlane `~> 2.240`) | Ruby 의존성 | 22-01 |
| `mobile/fastlane/build_numbers.rb` — `GhTradeBuildNumbers.ios_build_number(t)` (22-01) · `GhTradeBuildNumbers.android_version_code(t)` · `ANDROID_VERSION_CODE_MAX` (22-04) | 빌드 번호 함수 | 22-01 · 22-04 |
| `mobile/fastlane/test/build_numbers_test.rb` (Ruby minitest) | 단위 테스트 | 22-04 |
| `mobile/ios/App/fastlane/Fastfile` lane `beta` (22-01) · lane `latest` (22-06) · `mobile/ios/App/fastlane/Appfile` | fastlane | 22-01 · 22-06 |
| `mobile/android/fastlane/Fastfile` lanes `build` · `beta` · `validate` · `track` · `mobile/android/fastlane/Appfile` | fastlane | 22-04 |
| `mobile/scripts/setup-release-secrets.sh` — stages `dir` · `asc` (22-01) · `keystore` · `backup` · `play-sa` (22-04) | 사용자 `!` 비밀 주입 | 22-01 · 22-04 |
| `mobile/scripts/release-ios.sh` — 인자 `beta`(기본) · `latest`(22-06) · exit 3 = 비밀 env 누락 | 래퍼 | 22-01 · 22-06 |
| `mobile/scripts/release-android.sh` — 인자 `beta`(기본) · `build` · `validate` · `track` · `check` | 래퍼 | 22-04 |
| `mobile/scripts/check-ipa.sh` (`IPA CHECK OK`) · `mobile/scripts/check-aab.sh` (`AAB CHECK OK`) · `mobile/scripts/check-release-hygiene.sh` (`RELEASE HYGIENE OK`) | 검사 스크립트 | 22-01 · 22-04 |
| npm scripts `native:release:ios` (22-01) · `native:release:android` · `native:release:android:aab` (22-04) | 스크립트 | 22-01 · 22-04 |
| Gradle property `ghtradeVersionCode` · release `signingConfigs` ← env `GHTRADE_UPLOAD_STORE_FILE` · `GHTRADE_UPLOAD_STORE_PASSWORD` · `GHTRADE_UPLOAD_KEY_ALIAS` · `GHTRADE_UPLOAD_KEY_PASSWORD` · `gradle.taskGraph.whenReady` 릴리스 가드 | Android 빌드 설정 | 22-04 |
| env 이름: `ASC_KEY_ID` · `ASC_ISSUER_ID` · `ASC_KEY_PATH` · `GHTRADE_RELEASE_OUT` (ios.env) · `GHTRADE_UPLOAD_*` · `GHTRADE_UPLOAD_SHA1` · `GOOGLE_PLAY_JSON_KEY` (android.env) · `PLAY_RELEASE_STATUS` · `GHTRADE_RELEASE_ENV` · `GHTRADE_RELEASE_DIR` · `WEEKLY_WINE_FASTLANE_DIR` (선택 override) | env 이름(값은 저장소 밖) | 22-01 · 22-04 |
| 저장소 밖 `~/.config/gh-trade/release/` (700) — `ios.env` · `AuthKey_{KEYID}.p8` · `android.env` · `ghtrade-upload.jks` · `play-service-account.json` (600) · 산출 경로 `~/Library/Developer/gh-trade-release/ios` | 비밀·산출물(저장소 밖) | 22-01 · 22-04 · 22-06 |
| GCP Secret Manager `gh-radar-ghtrade-upload-keystore` · `gh-radar-ghtrade-upload-keystore-password` · `gh-radar-ghtrade-asc-api-key` · GCP SA `gh-radar-play-publisher` · API `androidpublisher.googleapis.com` | 외부(사용자 `!`) | 22-04 · 22-06 |
| iOS `Info.plist` `ITSAppUsesNonExemptEncryption` = false | 설정 | 22-01 |
| `mobile/.gitignore` 릴리스 비밀·산출물 패턴 | 설정 | 22-01 |
| `.planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md` (승인된 한글 초안) | 계획 문서 | 22-02 |
| 웹 `webapp/src/lib/supabase/public-path.ts` — `PUBLIC_PREFIXES` · `isPublicPath` | 유틸 | 22-03 |
| 웹 라우트 `/privacy` — `webapp/src/app/privacy/page.tsx` (`metadata.title` 「개인정보처리방침 · GH Trade」) | RSC 페이지 | 22-03 |
| 테스트: `webapp/src/lib/supabase/__tests__/public-path.test.ts` · `webapp/src/app/privacy/__tests__/page.test.tsx` · `webapp/e2e/specs/auth-guards.spec.ts` `/privacy` 2케이스 | 단위 · e2e | 22-03 |
| `mobile/README.md` 「릴리스 (Phase 22)」 절 · 테스터 안내 · 「비밀 파일」 · 범위 밖 갱신 | 문서 | 22-06 |
| `webapp/src/lib/native/google-client-ids.ts` 주석 갱신(상수 값 불변) | 주석 | 22-06 |
| 콘솔: ASC 앱 레코드 · TestFlight 내부 그룹 `GH Trade 테스터`(자동 배포) · Play 앱 · 내부 테스트 트랙 · GCP Android OAuth 클라이언트(업로드 키 · Play 앱 서명 인증서마다) | 외부 상태 | 22-01 · **22-05** · 22-06 |

<output>
Create `.planning/phases/22-gh-trade-ios-testflight-android-play/22-05-SUMMARY.md` when done — 「결정」 절에 서명 옵션(재개 신호 원문 · 콘솔 문구) · 앱 서명 SHA-1 전부 · 업로드 SHA-1 일치 여부 · 첫 출시 versionCode · 테스터 명수를 적는다.
</output>
