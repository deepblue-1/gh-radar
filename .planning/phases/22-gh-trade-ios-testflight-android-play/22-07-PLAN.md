---
phase: 22-gh-trade-ios-testflight-android-play
plan: 07
type: execute
wave: 5
depends_on: ["22-03", "22-06"]
files_modified: []
autonomous: false
requirements: [MOBILE-02]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "전 자동 게이트 green — build_command · test_command · auth-guards e2e · build_numbers minitest · `RELEASE HYGIENE OK` · `PROD CONFIG OK`"
    - "D-03 · 엣지 FA-4: 두 번째 `native:release:ios` · `native:release:android` 가 각각 직전 업로드보다 **엄격히 큰** 빌드 번호·versionCode 로 올라갔고(`IPA CHECK OK` · `AAB CHECK OK` · track 목록 포함), 테스터 기기에 TestFlight 자동 배포 · Play 업데이트로 도착한다(end-of-phase UAT)"
    - "D-09 · 엣지 FA-5: 릴리스 두 번 뒤에도 `git status --porcelain mobile/` 가 빈 출력이다 — 빌드 번호·산출물·서명 파일이 저장소에 남지 않았고 생성 설정은 운영값(`PROD CONFIG OK`)이다"
    - "D-11 · 메모리 「배포는 relay 먼저·push 나중」: `git push`(=웹 프로덕션 배포)는 (a) 이번 push 가 백엔드 경로 변경을 싣지 않고(`git diff --stat origin/master HEAD -- relay/ server/ supabase/ workers/ packages/shared/` 빈 출력) (b) 배포된 relay SHA 기준 백엔드 diff 가 비었고 (c) 개인정보처리방침 초안이 `status: approved` 이며 (d) 웹 변경 파일이 허용 7개의 부분집합일 때, 사용자 결정(blocking-human) 뒤에만 한다 — push 직전 같은 게이트를 다시 확인한다"
    - "D-11: push 했다면 `https://trade.jx1.io/privacy` 가 비로그인으로 리다이렉트 없이 200 과 「개인정보처리방침」 을 돌려준다"
    - "D-04: 이 phase 전체에서 relay·server·supabase·트레이딩 접근 코드는 바뀌지 않았다 — 테스터 트레이딩 차단은 기존 `dma_credentials` allow-list 그대로"
  artifacts: []
  key_links:
    - from: .planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md
      to: "https://trade.jx1.io/privacy (Vercel 프로덕션)"
      via: "status: approved → Task 2 push 결정 → git push → Vercel 빌드(webapp/ 변경 감지)"
      pattern: "status: approved"
  prohibitions:
    - statement: "MUST NOT 비밀값(업로드 키스토어·비밀번호 · ASC .p8 · Play SA JSON · 키 ID 값)을 저장소·문서·로그·PLAN/SUMMARY 에 남긴다"
      status: resolved
      verification: test
    - statement: "MUST NOT 테스터 계정에 `dma_credentials` 를 발급하거나 트레이딩 접근 규칙을 바꾼다(D-04)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT 문안 승인·백엔드 결합 게이트·사용자 push 결정 전에 `git push` 한다 — push 직전 다른 세션의 모르는 커밋이 끼어 있으면 멈춘다"
      status: resolved
      verification: test
    - statement: "MUST NOT `/privacy` 외의 웹 화면·동작을 바꾼다(웹 변경 파일 허용 7개 부분집합)"
      status: resolved
      verification: test
    - statement: "MUST NOT 빌드 번호를 저장소 파일에 써서 커밋한다(D-09)"
      status: resolved
      verification: test
    - statement: "MUST NOT 테스터 이메일을 문서·SUMMARY 에 적는다"
      status: resolved
      verification: test
    - statement: "MUST NOT 사용자 수에 비례하는 외부 호출(on-demand 수집)을 추가한다 — 이 phase 의 웹 변경은 정적 페이지 1개다"
      status: resolved
      verification: test
---

<objective>
Phase 22 마감: 전 자동 게이트를 돌리고, 두 플랫폼에 두 번째 빌드를 올려 「빌드 번호 증가 → 명령 한 번 → 테스터 자동 업데이트」 라는 반복 절차(D-03)를 실측한 뒤, 개인정보처리방침이 들어간 웹을 push(=Vercel 프로덕션 배포)할지 사용자 결정으로 정한다(D-11).

Purpose: 반복 가능성(D-03 · 엣지 FA-4)은 두 번째 실행에서만 증명된다. push 는 곧 배포라 문안 승인·백엔드 결합 게이트를 **push 직전**에 다시 확인한다(메모리 「배포는 relay 먼저·push 나중」 · 「동시 세션 커밋 경합」).
Output: 게이트 기록 · 두 번째 업로드(iOS 빌드 번호 · Android versionCode) · push 결정 · (push 시) 운영 `/privacy` 200 확인. 코드 변경 없음(게이트 실패 수정 제외).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-CONTEXT.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-VALIDATION.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-01-SUMMARY.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-03-SUMMARY.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-06-SUMMARY.md
@.planning/phases/21-gh-trade-mobile-app/21-36-SUMMARY.md

작업 루트는 메인 체크아웃 `/Users/alex/repos/gh-radar` 다. 비밀 취급 규칙은 22-01 과 같다. 21-36 SUMMARY 의 push 게이트·운영 반영 확인 방식을 그대로 따른다. Bash timeout 600000(iOS archive).

<interfaces>
```text
프로젝트 게이트(config.json): build_command = pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/webapp run typecheck
                              test_command  = pnpm --filter @gh-radar/relay run test && pnpm --filter @gh-radar/webapp run test
릴리스: native:release:ios → IPA CHECK OK · native:release:android → AAB CHECK OK · release-android.sh track → 「internal versionCodes: …」 · release-ios.sh latest → 「latest TestFlight build …」
배포된 relay SHA: https://dma.jx1.io/healthz 의 version (없으면 STATE.md 의 최근 relay 배포 SHA — 출처를 SUMMARY 에 적는다)
웹 변경 허용 7개: webapp/src/lib/supabase/{public-path.ts,middleware.ts,__tests__/public-path.test.ts} · webapp/src/app/privacy/{page.tsx,__tests__/page.test.tsx} · webapp/e2e/specs/auth-guards.spec.ts · webapp/src/lib/native/google-client-ids.ts
scripts/vercel-ignore-build.sh: webapp/ · packages/shared/ · pnpm-lock.yaml 변경이 있을 때만 Vercel 이 빌드한다
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전 자동 게이트 + 두 번째 릴리스(iOS · Android) — 빌드 번호 엄격 증가 · 저장소 무변경 · 테스터 자동 업데이트(UAT)</name>
  <files>(없음 — 검증·업로드 전용. 게이트 실패를 고치면 고친 파일을 SUMMARY 「Deviations」 에 적는다)</files>
  <read_first>
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-VALIDATION.md (Per-Task Verification Map · Manual-Only)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-01-SUMMARY.md (첫 iOS 빌드 번호) · 22-06-SUMMARY.md (supply versionCode · latest)
    - .planning/phases/21-gh-trade-mobile-app/21-36-SUMMARY.md (게이트 기록 형식)
  </read_first>
  <action>
    ① 자동 게이트를 순서대로 돌린다(각 출력 요약을 SUMMARY 에): build_command · test_command · `pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/auth-guards.spec.ts` · `/opt/homebrew/opt/ruby/bin/ruby mobile/fastlane/test/build_numbers_test.rb` · `bash mobile/scripts/check-release-hygiene.sh`. 하나라도 실패하면 원인을 고치고(이 phase 범위의 버그면 수정 커밋 · 아니면 SUMMARY 에 기록 후 멈춤) 처음부터 다시 돌린다.
    ② 두 번째 릴리스(D-03 · 엣지 FA-4): 22-06 업로드와 최소 1분 간격을 둔 뒤 `pnpm --filter @gh-radar/mobile run native:release:ios` → `pnpm --filter @gh-radar/mobile run native:release:android` 를 **순서대로**(동시 실행 금지 — 엣지 FA-5). 새 iOS 빌드 번호(`GHTRADE_RELEASE_OUT/build_number.txt` 를 check-ipa 가 출력)가 22-01 값보다, 새 versionCode 가 22-06 값보다 큰지 확인한다. 이어 `bash mobile/scripts/release-android.sh track` 에 새 versionCode 가 있는지, `bash mobile/scripts/release-ios.sh latest` 가 새 빌드 번호를 돌려주는지(처리 대기 최대 30분) 확인한다.
    ③ 마무리 확인: `git status --porcelain mobile/` 빈 출력 · `pnpm --filter @gh-radar/mobile run native:verify-prod` → `PROD CONFIG OK`. push 는 하지 않는다(Task 2 결정).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run test && pnpm --filter @gh-radar/webapp run test</automated>
    <fails_when>non-zero exit, or a vitest summary line contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/auth-guards.spec.ts</automated>
    <fails_when>non-zero exit, or the summary contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && /opt/homebrew/opt/ruby/bin/ruby mobile/fastlane/test/build_numbers_test.rb && bash mobile/scripts/check-release-hygiene.sh</automated>
    <fails_when>non-zero exit, or output lacks "0 failures, 0 errors" or "RELEASE HYGIENE OK"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:release:ios</automated>
    <fails_when>non-zero exit, or output lacks "IPA CHECK OK", or contains "fastlane finished with errors"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:release:android && bash mobile/scripts/release-android.sh track</automated>
    <fails_when>non-zero exit, or output lacks "AAB CHECK OK" or "internal versionCodes:", or contains "fastlane finished with errors"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && test -z "$(git status --porcelain -- mobile/)" && pnpm --filter @gh-radar/mobile run native:verify-prod</automated>
    <fails_when>non-zero exit — mobile/ has uncommitted changes after two releases, or output lacks "PROD CONFIG OK"</fails_when>
    <human-check>
      <test>① 본인 iPhone 의 TestFlight 에 새 빌드(SUMMARY 의 두 번째 빌드 번호)가 자동으로 나타나고 업데이트 → 앱 실행 → 로그인 유지 · 홈. ② Android(Play 설치본)에서 Play 스토어 GH Trade 에 「업데이트」 가 나타나고 업데이트 → 실행 → 로그인 유지 · 홈. ③ (가능하면) 테스터 1명이 초대 수락 → 설치까지 README 테스터 안내만으로 마쳤는지.</test>
      <expected>두 플랫폼 모두 사용자가 아무 설정 없이 새 빌드를 받는다(D-03) · 로그인 유지 · 테스터 절차가 문서대로다(D-02)</expected>
      <why_human>스토어 배포 전파·기기 업데이트·타인 계정 수락은 자동화할 수 없다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 두 번째 iOS 빌드 번호 > 22-01 빌드 번호 · 두 번째 versionCode > 22-06 versionCode 가 숫자로 적혀 있고, track 출력에 두 번째 versionCode 가 있다
    - 일곱 `<automated>` 가 모두 통과하고 SUMMARY 에 각 게이트 결과(통과 수 등 실제 출력 요약)가 있다
  </acceptance_criteria>
  <done>게이트가 green 이고 반복 릴리스가 두 플랫폼에서 증명됐으며, 저장소는 깨끗하다 — push 는 하지 않았다.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: push(=웹 프로덕션 배포) 결정 — 백엔드 결합 게이트 · 문안 승인 · 웹 변경 범위 확인 뒤</name>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-36-SUMMARY.md (push 게이트 ①② 방식)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md (frontmatter status)
    - .planning/STATE.md (최근 relay 배포 SHA · 배포 순서 메모)
  </read_first>
  <decision>개인정보처리방침 `/privacy` 와 Phase 22 웹 변경을 지금 `git push` 로 프로덕션에 배포할지</decision>
  <context>
    이 저장소에서 `git push` 는 곧 webapp 프로덕션 배포다(Vercel — 브라우저 사용자에게도 즉시 노출 · D-11). `/privacy` 는 relay·server 결합이 없는 정적 페이지지만, push 는 그 시점 master 전체를 내보낸다. 실행기는 이 체크포인트를 띄우기 전에 아래 verify 네 명령을 돌리고 출력 전부와 `git log --oneline origin/master..HEAD` 커밋 목록(이 phase 커밋 · docs 커밋으로 분류)을 본문에 붙인다. (b) 가 비어 있지 않으면 해당 커밋이 이미 origin/master 에 있는지(`git merge-base --is-ancestor`)를 함께 보여 주고, 사용자가 명시적으로 받아들이지 않는 한 `hold` 가 기본이다(메모리 「배포는 relay 먼저·push 나중」 — 백엔드가 막히면 push 하지 않는다). 모바일 앱 배포(TestFlight · Play)는 push 와 무관하게 이미 끝났다.
  </context>
  <options>
    <option id="push">
      <name>push — 운영 `/privacy` 공개</name>
      <pros>스토어·테스터가 방침 URL 을 바로 쓴다 · 웹 변경이 앱 셸(Remote-URL)에도 즉시 반영된다</pros>
      <cons>브라우저 사용자에게도 즉시 배포된다 · Task 3 가 push 직전 게이트를 다시 확인하고, 다른 세션의 모르는 커밋이 끼어 있으면 멈춘다</cons>
    </option>
    <option id="hold">
      <name>hold — 이번에는 push 하지 않음</name>
      <pros>다른 배포 일정(예: Phase 18 relay 선배포)과 묶을 수 있다</pros>
      <cons>운영 `/privacy` 는 404 로 남는다 — 방침 URL 이 필요한 스토어 절차는 push 뒤로 미뤄진다</cons>
    </option>
  </options>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && git fetch origin && test -z "$(git diff --stat origin/master HEAD -- relay/ server/ supabase/ workers/ packages/shared/)"</automated>
    <fails_when>non-zero exit — this push would carry backend (relay/server/supabase/workers/packages/shared) changes</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && V="$(curl -sf --max-time 10 https://dma.jx1.io/healthz)" && SHA="$(printf '%s' "$V" | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const j=JSON.parse(s);process.stdout.write(String(j.version||""))})')" && test -n "$SHA" && echo "deployed relay $SHA" && test -z "$(git diff --stat "$SHA" HEAD -- relay/ server/ supabase/ workers/ packages/shared/)"</automated>
    <fails_when>non-zero exit — /healthz unreachable or has no version (fall back to the STATE.md relay SHA and say so), or backend paths changed since the deployed relay SHA</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -q "^status: approved" .planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md</automated>
    <fails_when>non-zero exit — the privacy draft is not approved (D-11 gate)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && W="$(git diff --name-only origin/master HEAD -- webapp/)" && test -z "$(printf '%s\n' "$W" | grep -vxE 'webapp/src/lib/supabase/(public-path\.ts|middleware\.ts|__tests__/public-path\.test\.ts)|webapp/src/app/privacy/(page\.tsx|__tests__/page\.test\.tsx)|webapp/e2e/specs/auth-guards\.spec\.ts|webapp/src/lib/native/google-client-ids\.ts' | grep -v '^$')"</automated>
    <fails_when>non-zero exit — a webapp file outside the allowed seven would be deployed</fails_when>
  </verify>
  <acceptance_criteria>
    - 재개 신호가 `push` 또는 `hold` 이고 SUMMARY 「결정」 절에 네 게이트 출력 요약 · 배포 relay SHA 와 그 출처 · 커밋 목록 분류와 함께 기록된다
    - 게이트 중 하나라도 실패한 상태의 `push` 는 사용자가 실패 항목을 명시적으로 받아들였다는 문장이 재개 신호에 있을 때만 유효하다
    - 이 체크포인트는 auto-advance 로 넘기지 않는다(blocking-human)
  </acceptance_criteria>
  <resume-signal>`push` 또는 `hold` 로 답한다(게이트 실패를 알고도 push 하려면 `push — (b) 실패 수용: 이유` 처럼 적는다)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: (push 결정 시) push 직전 게이트 재확인 → `git push` → 운영 `/privacy` 200 확인</name>
  <precondition>Task 2 재개 신호가 `push` 다 — `hold` 면 이 태스크는 SUMMARY 에 「push 보류」 를 적고 코드·원격 변경 없이 끝난다</precondition>
  <files>(없음 — git push 와 운영 확인만)</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-36-SUMMARY.md (push → 운영 반영 폴링 방식)
    - Task 2 체크포인트 본문(그때의 커밋 목록 · relay SHA)
  </read_first>
  <action>
    ① push 직전(다른 명령을 끼우지 않고) 다시 확인한다: `git fetch origin` → `git status -sb` 가 ahead N · behind 0 인지(behind 면 멈추고 알린다 — 사용자 결정 없이 rebase·merge 하지 않는다) → `git log --oneline origin/master..HEAD` 가 Task 2 에서 보인 목록과 같은지(다른 세션의 모르는 커밋이 끼었으면 멈추고 묻는다 — 메모리 「동시 세션 커밋 경합」) → Task 2 verify 1·4 를 다시 통과하는지.
    ② `git push origin master`. `git add -A` 같은 일괄 스테이징은 하지 않는다(이 태스크는 새 커밋을 만들지 않는다).
    ③ 운영 반영 확인(21-36 방식): 30초 간격으로 최대 10분 `curl -s -o /dev/null -w '%{http_code} %{redirect_url}' https://trade.jx1.io/privacy` 가 `200 `(리다이렉트 없음)이고 본문에 「개인정보처리방침」 이 있을 때까지. 10분 안에 반영되지 않으면 멈추고 알린다 — 수동 prebuilt 배포는 사용자 확인 뒤에만 한다(메모리 reference 「Vercel 프론트 배포」). 반영 시각 · push 범위를 SUMMARY 에 적는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && if git merge-base --is-ancestor HEAD origin/master; then test "$(curl -s -o /dev/null -w '%{http_code}' https://trade.jx1.io/privacy)" = "200" && curl -s https://trade.jx1.io/privacy | grep -q "개인정보처리방침" && echo "PRIVACY LIVE"; else echo "push 보류 — HEAD 가 origin/master 에 없음"; fi</automated>
    <fails_when>non-zero exit — HEAD was pushed but production /privacy is not 200 or lacks 「개인정보처리방침」</fails_when>
  </verify>
  <acceptance_criteria>
    - `push` 였다면 SUMMARY 에 push 범위(`{이전}..{HEAD}`) · 운영 반영 시각 · `PRIVACY LIVE` 가 있고, `git status -sb` 에 ahead 가 없다
    - `hold` 였다면 SUMMARY 에 「push 보류」 와 이유가 있고 원격은 바뀌지 않았다
    - verify 가 통과한다
  </acceptance_criteria>
  <done>사용자 결정대로 웹이 배포됐거나(운영 `/privacy` 확인) 보류가 기록됐다 — Phase 22 는 `/gsd-verify-work 22` 로 넘어간다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `git push` → Vercel 프로덕션 | 웹 변경이 전 사용자에게 즉시 배포된다 |
| 다른 세션의 로컬 커밋 → 이 push | 모르는 커밋이 함께 배포될 수 있다 |
| 로컬 릴리스 → 테스터 기기 | 두 번째 빌드가 자동으로 전파된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-09 | Denial of Service / Repudiation | 백엔드 결합 변경이 섞인 push · 검증 전 배포 | high | mitigate | Task 2 게이트 (a) 이번 push 의 백엔드 diff 0 · (b) 배포 relay SHA 기준 diff 0 · (d) 웹 변경 허용 7개 · blocking-human 결정 · Task 3 push 직전 재확인(behind · 모르는 커밋이면 멈춤) |
| T-22-08 | Repudiation (법적) | 미승인 방침 배포 | medium | mitigate | 게이트 (c) `status: approved` · 22-02 blocking-human 문안 게이트 |
| T-22-04 | Tampering / Information Disclosure | 두 번째 빌드에 dev 설정 혼입 | high | mitigate | 같은 사슬(`cap sync` 양쪽 → verify-prod → check-ipa/check-aab) · 마무리 `PROD CONFIG OK` |
| T-22-07 | Tampering (무결성) | 반복 릴리스가 저장소 파일을 바꿈 | low | mitigate | 두 번 뒤 `git status --porcelain mobile/` 빈 출력(verify 7) |
| T-22-01 | Information Disclosure | 비밀의 저장소 유입(최종 확인) | high | mitigate | `RELEASE HYGIENE OK`(추적 파일 비밀 패턴 · 권한 · README) |
</threat_model>

<verification>
- 게이트 7개 green · 두 번째 업로드 번호 엄격 증가 · 저장소 무변경 · push 결정 기록 · (push 시) 운영 `/privacy` 200
- end-of-phase UAT: 22-06 Task 2 human-check(설치본 로그인 · logcat SHA-1 · DmaGate) + 이 플랜 Task 1 human-check(자동 업데이트 · 테스터 절차)
</verification>

<success_criteria>
- MOBILE-02 가 자동 게이트 · 두 번의 실제 업로드 · 실기기 UAT 목록으로 확인 가능한 상태이고, 웹 배포 여부는 사용자 결정으로 남았다. 다음 단계는 `/gsd-verify-work 22`.
</success_criteria>

## Flagged assumptions (spec-less 엣지 프로브 5건 · unresolved 유지 — 22-01 표와 같음)

| # | 프로브 | 이 phase 해석(가정) | 이 플랜에서 드러나는 곳 |
|---|---|---|---|
| FA-1 | adjacency | 같은 분 재실행은 같은 번호 → 스토어 거절 · 연 경계 단조 | Task 1 ② 「최소 1분 간격」 · build_numbers minitest |
| FA-2 | empty | env·키 누락 → fastlane 전 exit 3 · 이름만 · 테스터 0명이면 설치 불가 | 22-01 · 22-04 verify |
| FA-3 | ordering | 콘솔 선행 → lane 순서(웨이브 · precondition) · 같은 번호 두 개의 순서는 정의 없음 | 이 플랜 depends_on |
| FA-4 | idempotency | 두 번째 실행 = 더 큰 번호 · 저장소 무변경 · 비밀 스크립트 재실행은 SKIP | Task 1 ② · verify 5·6·7 |
| FA-5 | concurrency | iOS·Android 릴리스 동시 실행 금지 · 중단돼도 저장소·생성 설정 보존 | Task 1 ② 「순서대로」 · verify 7 |

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/Gemfile` · `mobile/Gemfile.lock` (fastlane `~> 2.240`) | Ruby 의존성 | 22-01 |
| `mobile/fastlane/build_numbers.rb` — `GhTradeBuildNumbers.ios_build_number(t)` (22-01) · `GhTradeBuildNumbers.android_version_code(t)` · `ANDROID_VERSION_CODE_MAX` (22-04) | 빌드 번호 함수 | 22-01 · 22-04 |
| `mobile/fastlane/test/build_numbers_test.rb` (Ruby minitest) | 단위 테스트 | 22-04 |
| `mobile/ios/App/fastlane/Fastfile` lane `beta` (22-01) · lane `latest` (22-06) · `mobile/ios/App/fastlane/Appfile` | fastlane | 22-01 · 22-06 |
| `mobile/android/fastlane/Fastfile` lanes `build` · `beta` · `validate` · `track` · `mobile/android/fastlane/Appfile` | fastlane | 22-04 |
| `mobile/scripts/setup-release-secrets.sh` — stages `dir` · `asc` (22-01) · `keystore` · `backup` · `play-sa` (22-04) | 사용자 `!` 비밀 주입 | 22-01 · 22-04 |
| `mobile/scripts/release-ios.sh` — 인자 `beta`(기본) · `latest`(22-06) · exit 3 = 비밀 env 누락 | 래퍼 | 22-01 · 22-06 |
| `mobile/scripts/release-android.sh` — 인자 `beta`(기본) · `build` · `validate` · `track` · `check` | 래퍼 | 22-04 |
| `mobile/scripts/check-ipa.sh` (`IPA CHECK OK`) · `mobile/scripts/check-aab.sh` (`AAB CHECK OK`) · `mobile/scripts/check-release-hygiene.sh` (`RELEASE HYGIENE OK`) | 검사 스크립트 | 22-01 · 22-04 |
| npm scripts `native:release:ios` (22-01) · `native:release:android` · `native:release:android:aab` (22-04) | 스크립트 | 22-01 · 22-04 |
| Gradle property `ghtradeVersionCode` · release `signingConfigs` ← env `GHTRADE_UPLOAD_STORE_FILE` · `GHTRADE_UPLOAD_STORE_PASSWORD` · `GHTRADE_UPLOAD_KEY_ALIAS` · `GHTRADE_UPLOAD_KEY_PASSWORD` · `gradle.taskGraph.whenReady` 릴리스 가드 | Android 빌드 설정 | 22-04 |
| env 이름: `ASC_KEY_ID` · `ASC_ISSUER_ID` · `ASC_KEY_PATH` · `GHTRADE_RELEASE_OUT` (ios.env) · `GHTRADE_UPLOAD_*` · `GHTRADE_UPLOAD_SHA1` · `GOOGLE_PLAY_JSON_KEY` (android.env) · `PLAY_RELEASE_STATUS` · `GHTRADE_RELEASE_ENV` · `GHTRADE_RELEASE_DIR` · `WEEKLY_WINE_FASTLANE_DIR` (선택 override) | env 이름(값은 저장소 밖) | 22-01 · 22-04 |
| 저장소 밖 `~/.config/gh-trade/release/` (700) — `ios.env` · `AuthKey_{KEYID}.p8` · `android.env` · `ghtrade-upload.jks` · `play-service-account.json` (600) · 산출 경로 `~/Library/Developer/gh-trade-release/ios` | 비밀·산출물(저장소 밖) | 22-01 · 22-04 · 22-06 |
| GCP Secret Manager `gh-radar-ghtrade-upload-keystore` · `gh-radar-ghtrade-upload-keystore-password` · `gh-radar-ghtrade-asc-api-key` · GCP SA `gh-radar-play-publisher` · API `androidpublisher.googleapis.com` | 외부(사용자 `!`) | 22-04 · 22-06 |
| iOS `Info.plist` `ITSAppUsesNonExemptEncryption` = false | 설정 | 22-01 |
| `mobile/.gitignore` 릴리스 비밀·산출물 패턴 | 설정 | 22-01 |
| `.planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md` (승인된 한글 초안) | 계획 문서 | 22-02 |
| 웹 `webapp/src/lib/supabase/public-path.ts` — `PUBLIC_PREFIXES` · `isPublicPath` | 유틸 | 22-03 |
| 웹 라우트 `/privacy` — `webapp/src/app/privacy/page.tsx` (`metadata.title` 「개인정보처리방침 · GH Trade」) | RSC 페이지 | 22-03 |
| 테스트: `webapp/src/lib/supabase/__tests__/public-path.test.ts` · `webapp/src/app/privacy/__tests__/page.test.tsx` · `webapp/e2e/specs/auth-guards.spec.ts` `/privacy` 2케이스 | 단위 · e2e | 22-03 |
| `mobile/README.md` 「릴리스 (Phase 22)」 절 · 테스터 안내 · 「비밀 파일」 · 범위 밖 갱신 | 문서 | 22-06 |
| `webapp/src/lib/native/google-client-ids.ts` 주석 갱신(상수 값 불변) | 주석 | 22-06 |
| 콘솔: ASC 앱 레코드 · TestFlight 내부 그룹 `GH Trade 테스터`(자동 배포) · Play 앱 · 내부 테스트 트랙 · GCP Android OAuth 클라이언트(업로드 키 · Play 앱 서명 인증서마다) | 외부 상태 | 22-01 · 22-05 · 22-06 |
| 운영 `https://trade.jx1.io/privacy` (push 결정 시) | 배포 | **22-07** |

<output>
Create `.planning/phases/22-gh-trade-ios-testflight-android-play/22-07-SUMMARY.md` when done — 게이트별 실제 결과 · 두 번째 빌드 번호/versionCode · push 결정과 네 게이트 출력 · (push 시) 범위와 운영 반영 시각 · end-of-phase UAT 목록(22-06 Task 2 · 22-07 Task 1 human-check)을 적는다.
</output>
