---
phase: 22-gh-trade-ios-testflight-android-play
plan: 06
type: execute
wave: 4
depends_on: ["22-05"]
files_modified:
  - mobile/ios/App/fastlane/Fastfile
  - mobile/scripts/release-ios.sh
  - mobile/README.md
  - webapp/src/lib/native/google-client-ids.ts
autonomous: false
requirements: [MOBILE-02]
user_setup:
  - service: google-play-service-account
    why: "fastlane supply 가 Play Developer API 로 업로드하려면 Play 게시용 SA 키와 앱 단위 권한이 필요하다 — weekly-wine 에는 재사용할 SA 가 없다(D-07 리서치 메모)"
    env_vars:
      - name: GOOGLE_PLAY_JSON_KEY
        source: "`! bash mobile/scripts/setup-release-secrets.sh play-sa` → `~/.config/gh-trade/release/play-service-account.json` (600 · android.env 에 경로가 이미 있다)"
    dashboard_config:
      - task: "SA 이메일 초대 — 앱 권한 GH Trade: 「테스트 트랙에 앱 출시」 · 「앱 정보 보기(읽기 전용)」 · 계정 권한 없음"
        location: "Play Console → 사용자 및 권한 → 새 사용자 초대"
  - service: google-cloud-oauth
    why: "Play 설치본은 Google 앱 서명 키로 서명돼 있어 그 SHA-1 이 GCP Android OAuth 클라이언트에 없으면 네이티브 Google 로그인이 실패한다(D-08 · Pitfall 5)"
    dashboard_config:
      - task: "Android OAuth 클라이언트 — 패키지 `com.ghtrade.app` + 업로드 키 SHA-1 1개 + Play 앱 서명 인증서 SHA-1 마다 1개"
        location: "GCP 콘솔 → 프로젝트 gh-radar → Google Auth Platform → 클라이언트 → 클라이언트 만들기 → Android"
      - task: "OAuth 동의 화면 게시 상태 확인 — 「테스트」 면 테스터 Google 계정을 테스트 사용자로 추가(D-05)"
        location: "Google Auth Platform → 대상(Audience)"
  - service: app-store-connect-testers
    why: "TestFlight 내부 테스터는 ASC 팀 사용자여야 한다(Pitfall 6)"
    dashboard_config:
      - task: "테스터 초대 — 역할 Marketing · 앱 액세스 GH Trade 만 → 수락 뒤 내부 그룹 `GH Trade 테스터` 에 추가"
        location: "App Store Connect → 사용자 및 액세스 → +"

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-07: Play 게시용 SA(`gh-radar-play-publisher`)가 새로 만들어졌고(재사용 아님) Play Console 에서 GH Trade 앱 한정 「테스트 트랙 출시 · 앱 정보 보기」 권한만 가진다 — `release-android.sh validate` 가 통과한다"
    - "MOBILE-02m: `native:release:android` 가 fastlane supply 로 내부 테스트 트랙에 업로드했고 `release-android.sh track` 의 versionCode 목록에 이번 값(22-05 첫 수동 업로드보다 큼)이 있다"
    - "MOBILE-02l: `release-ios.sh latest` 가 돌려준 최신 TestFlight 빌드 번호가 22-01 업로드 빌드 번호와 같다(처리 완료)"
    - "D-08: GCP 에 Android OAuth 클라이언트가 업로드 키 SHA-1 1개 + Play 앱 서명 인증서 SHA-1 마다 1개 추가됐다 — id_token aud 는 여전히 웹 클라이언트라 코드·Supabase Client IDs 는 무변경이고 `google-client-ids.ts` 는 주석만 바뀐다(상수 값 · 기존 단위 테스트 불변)"
    - "D-05: OAuth 동의 화면 게시 상태가 확인·기록됐고, 「테스트」 상태면 테스터 계정이 테스트 사용자로 추가됐다 — Supabase 가입 제한은 추가하지 않았다"
    - "D-02: 테스터 초대가 ASC(Marketing · GH Trade 한정 → 내부 그룹)와 Play 내부 테스트 목록으로 나갔고, `mobile/README.md` 테스터 안내가 iOS 3단계(ASC 초대 수락 → TestFlight 초대 → 설치) · Android 2단계(참여 링크 → 설치)를 정직하게 적는다"
    - "D-03: README 가 업데이트 절차를 「빌드 번호 자동 → 명령 한 번 → TestFlight 자동 배포 · Play 자동 업데이트」 로 적고 TestFlight 빌드 90일 만료를 명시한다"
    - "D-04 · D-06: 테스터 계정에는 `dma_credentials` 를 발급하지 않았고, 업로드·검증은 Claude 가 실행했으며 비밀 주입만 사용자 `!` 로 했다"
  artifacts:
    - path: mobile/ios/App/fastlane/Fastfile
      provides: "lane latest — 최신 TestFlight 빌드 번호 조회"
      contains: "lane :latest"
    - path: mobile/README.md
      provides: "「릴리스 (TestFlight · Play 내부 테스트 — Phase 22)」 절 · 테스터 안내 · 비밀 파일 · 범위 밖 갱신"
      contains: "native:release:ios"
    - path: webapp/src/lib/native/google-client-ids.ts
      provides: "릴리스 SHA-1 등록 사실을 적은 주석(상수 불변)"
      contains: "Phase 22 D-08"
  key_links:
    - from: mobile/scripts/release-ios.sh
      to: mobile/ios/App/fastlane/Fastfile
      via: "release-ios.sh latest → bundle exec fastlane latest"
      pattern: "latest"
    - from: mobile/README.md
      to: mobile/scripts/setup-release-secrets.sh
      via: "처음 1회 절차의 `! bash …` 명령"
      pattern: "setup-release-secrets.sh"
  prohibitions:
    - statement: "MUST NOT 테스터 계정에 `dma_credentials` 를 발급하거나 트레이딩 접근 규칙·relay 를 바꾼다 — 실돈 발주는 사용자 본인만(D-04)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT 테스터 이메일·SA 키 내용·비밀번호를 README·SUMMARY·로그에 적는다 — 명수와 공개 지문만"
      status: resolved
      verification: test
---

<objective>
Android 로그인·업로드 경로를 닫고 테스터를 들인다: 사용자가 Play SA·GCP Android OAuth 클라이언트·동의 화면·테스터 초대를 한 번에 처리하고(Task 1), Claude 가 fastlane 으로 Play 내부 트랙 업로드와 TestFlight 처리 완료를 자동 확인한 뒤(Task 2 — 실기기 UAT 는 end-of-phase), 릴리스 절차·테스터 안내를 문서로 남긴다(Task 3).

Purpose: Play 설치본은 Google 의 앱 서명 키로 서명돼 있어 로컬 서명과 다르다 — SHA-1 등록이 빠지면 Android 테스터 전원의 로그인이 막힌다(D-08 · Pitfall 5). 테스터 절차는 사용자가 기대한 2단계보다 iOS 가 한 단계 길다(Pitfall 6) — 문서가 이를 숨기지 않는다.
Output: Play SA · GCP Android 클라이언트 N개 · 동의 화면 상태 · 테스터 초대 · supply 업로드 1회 · iOS lane `latest` · README 릴리스 절 · client-ids 주석.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-CONTEXT.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-01-SUMMARY.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-04-SUMMARY.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-05-SUMMARY.md
@mobile/README.md
@webapp/src/lib/native/google-client-ids.ts

작업 루트는 메인 체크아웃 `/Users/alex/repos/gh-radar` 다. 비밀 취급 규칙은 22-01 과 같다. Task 1 체크포인트 본문에는 22-04 업로드 SHA-1 과 22-05 앱 서명 SHA-1 목록(공개 지문)을 그대로 붙여 사용자가 GCP 에 복사하게 한다.

<interfaces>
```text
22-01: mobile/ios/App/fastlane/Fastfile lane :beta · release-ios.sh(exit 3 규약 · 인자 없음 → beta)
22-04: release-android.sh [beta|build|validate|track|check] · lane :validate · :track(「internal versionCodes: …」 출력)
이 플랜 추가: iOS lane :latest → 「latest TestFlight build {n}」 출력 · release-ios.sh [beta|latest] (latest 는 check-ipa 없음)
android.env 의 GOOGLE_PLAY_JSON_KEY = ~/.config/gh-trade/release/play-service-account.json (22-04 keystore 단계가 경로만 미리 써 둠)
google-client-ids.ts L12-15 주석(Android 절) — 상수 4개와 export 는 불변(기존 google-client-ids.test.ts 가 형식을 잠근다)
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Play SA 생성·초대 · GCP Android OAuth 클라이언트(업로드·앱 서명 SHA-1) · 동의 화면 상태 · 테스터 초대(ASC · Play)</name>
  <read_first>
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-CONTEXT.md (D-02 리서치 메모 · D-05 · D-07 · D-08 리서치 메모)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md (Pitfall 5 · 6 · 13 · State of the Art 「SA 초대」 · Assumptions A2 · A4 · A5)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-04-SUMMARY.md (업로드 SHA-1) · 22-05-SUMMARY.md (앱 서명 SHA-1 목록)
  </read_first>
  <action>사용자가 콘솔 네 곳에서 한 번에 처리한다. 비밀(SA 키)은 `!` 한 줄로만 만든다(D-06). Supabase 설정과 가입 제한은 바꾸지 않는다(D-05). 테스터에게 `dma_credentials` 를 발급하지 않는다(D-04).</action>
  <instructions>
    Claude 가 이미 한 것: `setup-release-secrets.sh play-sa` 단계(API 활성화 · SA 생성 · 키 파일 600 · SA 이메일만 출력) · Android lane `validate`/`track` · 업로드 키 서명 AAB 첫 출시(22-05).

    1. Claude Code 입력창에 한 줄: `! bash mobile/scripts/setup-release-secrets.sh play-sa` → 「OK play-sa」 와 SA 이메일(`gh-radar-play-publisher@…`)만 나온다.
    2. Play Console → **사용자 및 권한** → 「새 사용자 초대」 → 위 SA 이메일 → 「앱 권한」 에서 GH Trade 추가 → 권한 **「테스트 트랙에 앱 출시」 · 「앱 정보 보기(읽기 전용)」** 만 → 계정 권한은 켜지 않음 → 초대. (Cloud 프로젝트 연결은 필요 없다 — RESEARCH State of the Art.)
    3. GCP 콘솔(프로젝트 `gh-radar`) → Google Auth Platform → 클라이언트 → 「클라이언트 만들기」 → 유형 **Android** → 이름 `GH Trade Android (upload key)` · 패키지 `com.ghtrade.app` · SHA-1 = 업로드 SHA-1(아래 붙인 값) → 만들기. 같은 방법으로 Play 앱 서명 SHA-1 마다 `GH Trade Android (Play app signing {n})` 를 만든다. 기존 debug 클라이언트는 그대로 둔다. Supabase Client IDs 는 바꾸지 않는다(id_token aud 는 웹 클라이언트 — 코드 변경 없음). 같은 패키지에 SHA-1 이 다른 클라이언트를 GCP 가 거부하면(A4) 화면 문구를 알려 준다.
    4. Google Auth Platform → **대상(Audience)** → 게시 상태 확인(D-05): 「프로덕션」 이면 할 일 없음 · 「테스트」 면 「테스트 사용자」 에 테스터 Google 계정을 추가한다.
    5. App Store Connect → 사용자 및 액세스 → 「+」 → 테스터 이메일(Apple ID) · 역할 **Marketing** · 앱 액세스 **GH Trade 만** → 초대(3일 만료 · Developer 역할은 인증서 접근이 붙으므로 쓰지 않는다). 테스터가 수락하면 TestFlight → 내부 그룹 `GH Trade 테스터` → 테스터 추가. 수락이 늦어지면 이 단계는 나중에 해도 된다 — 본인 계정 UAT 는 막히지 않는다.
    6. Play 내부 테스트 → 테스터 목록에 테스터 Google 계정 추가(22-05 에서 안 했다면) → 「링크 복사」 로 참여 링크를 테스터에게 보낸다. 트레이딩 권한(`dma_credentials`)은 누구에게도 발급하지 않는다(D-04).
  </instructions>
  <verification>재개 뒤 Claude 가 SA 키 권한(600)과 Play API 연결을 확인한다. Play 권한 전파가 늦으면 `validate` 가 401/403 을 낸다 — 10분 간격으로 최대 1시간 재시도하고, 그래도 실패하면 멈추고 알린다.</verification>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash mobile/scripts/check-release-hygiene.sh && bash mobile/scripts/release-android.sh validate</automated>
    <fails_when>non-zero exit, or the output lacks "RELEASE HYGIENE OK", or contains "fastlane finished with errors" (SA JSON rejected by the Play Developer API)</fails_when>
  </verify>
  <acceptance_criteria>
    - 재개 신호가 `done Android클라이언트={N}개 동의화면={프로덕션|테스트+{N}명} ASC초대={N}명 Play테스터={N}명` 형태이고 N(클라이언트)이 「1 + 22-05 앱 서명 SHA-1 개수」 이상이다 — 값이 SUMMARY 「결정」 절에 기록된다(테스터 이메일 없음)
    - verify 가 통과한다(SA 키 600 · Play API 연결)
  </acceptance_criteria>
  <resume-signal>`done Android클라이언트=2개 동의화면=프로덕션 ASC초대=2명 Play테스터=3명` 처럼 답한다</resume-signal>
</task>

<task type="auto">
  <name>Task 2: iOS lane `latest` · fastlane supply 로 Play 내부 트랙 업로드 · TestFlight 처리 완료 확인 (+ 실기기 UAT 는 end-of-phase)</name>
  <precondition>Task 1 verify 가 통과했다(`release-android.sh validate` 성공)</precondition>
  <files>mobile/ios/App/fastlane/Fastfile, mobile/scripts/release-ios.sh</files>
  <read_first>
    - mobile/ios/App/fastlane/Fastfile · mobile/scripts/release-ios.sh (22-01 — env 검사·exit 3 규약 유지)
    - mobile/android/fastlane/Fastfile · mobile/scripts/release-android.sh (22-04 — validate · track 모드)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md (Pitfall 9 · 11 · Code Examples 「테스터 수(최종 확인)」 · Validation MOBILE-02l · m · n · o)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-01-SUMMARY.md (업로드 빌드 번호) · 22-05-SUMMARY.md (첫 versionCode · 앱 서명 SHA-1)
  </read_first>
  <action>
    ① iOS Fastfile 에 `lane :latest` 를 더한다: `beta` 와 같은 env 이름 검사 → `app_store_connect_api_key(…)` → `n = latest_testflight_build_number(api_key:, app_identifier: "com.ghtrade.app")` → `UI.message("latest TestFlight build #{n}")`. `beta` lane 은 바꾸지 않는다.
    ② `mobile/scripts/release-ios.sh` 를 `MODE="${1:-beta}"`(`beta|latest` 외에는 env 로드 **전에** 사용법 + exit 2)로 바꾸고 `latest` 는 `bundle exec fastlane latest` 만 돌린다(check-ipa 없음). env 검사·exit 3 규약·헤더 주석은 그대로.
    ③ `pnpm --filter @gh-radar/mobile run native:release:android` — 새 versionCode(lane 이 출력)로 빌드·서명·검사 후 내부 트랙에 올린다. 「Only releases with status draft may be created on draft app」 이면 `PLAY_RELEASE_STATUS=draft` 로 다시 실행하고, 인증 게이트처럼 사용자에게 「Play Console → 내부 테스트 → 버전 검토 → 출시」 를 요청한 뒤 이어간다(Pitfall 9).
    ④ `bash mobile/scripts/release-android.sh track` 출력의 versionCode 목록에 ③ 의 값이 있고 22-05 첫 versionCode 보다 큰지 확인한다(엣지 FA-4 — 두 번째 실행이 더 큰 번호).
    ⑤ `bash mobile/scripts/release-ios.sh latest` 가 22-01 업로드 빌드 번호를 돌려줄 때까지 60초 간격으로 최대 30분 반복한다(처리 대기). 처리 실패(Invalid Binary · ITMS-91053 거절 메일)가 보이면 멈추고 알린다 — `PrivacyInfo.xcprivacy` 는 Deferred 라 승인 없이 추가하지 않는다(Pitfall 11 · A6).
    ⑥ SUMMARY 에 supply 업로드 versionCode · track 출력 · latest 빌드 번호 · draft 전환 여부를 적는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:release:android</automated>
    <fails_when>non-zero exit, or the output lacks "PROD CONFIG OK" or "AAB CHECK OK", or contains "fastlane finished with errors"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && bash mobile/scripts/release-android.sh track</automated>
    <fails_when>non-zero exit, or the output lacks "internal versionCodes:"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && bash mobile/scripts/release-ios.sh latest</automated>
    <fails_when>non-zero exit, or the output lacks "latest TestFlight build"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && test -z "$(git status --porcelain -- mobile/android/app/build.gradle mobile/ios/App/App/Info.plist mobile/ios/App/App.xcodeproj/project.pbxproj)"</automated>
    <fails_when>non-zero exit — an upload rewrote build.gradle, Info.plist or pbxproj (build number leaked into repo files)</fails_when>
    <human-check>
      <test>① 본인 iPhone(mesya iPhone 16)에서 TestFlight 앱 → GH Trade 설치 → 실행 → /login → 「Google로 로그인」 → 홈 착지 · 앱 종료 후 재실행 시 로그인 유지 · 마이 → 로그아웃 → 재로그인. ② Android(에뮬레이터 `Medium_Phone_API_36.1` 에 Play 스토어 로그인 후 참여 링크로 내부 테스트 참여, 또는 실기기)에서 Play 스토어로 설치 → 로그인 → 홈 착지 · 설치본이 연결된 동안 Claude 가 `adb logcat -d -s GoogleProvider` 를 읽어 `signingSha1=` 값을 22-05 앱 서명 SHA-1 과 대조. ③ `dma_credentials` 가 없는 계정(테스터 또는 본인의 두 번째 Google 계정)으로 로그인 → 트레이딩 탭 → 안내(DmaGate)만 보이고 시세·주문 UI 가 없음(실주문 금지).</test>
      <expected>① iOS 로그인·재실행·재로그인 성공 ② Android 로그인 성공이고 logcat `signingSha1` 이 GCP 에 등록한 Play 앱 서명 SHA-1 중 하나와 같다(로컬 업로드 키 SHA-1 이 아님) ③ DmaGate 만 보인다(D-04)</expected>
      <why_human>실기기·스토어 설치 경로·Google 계정 선택 UI·타인 계정은 자동화할 수 없다(MOBILE-02n · MOBILE-02o). Android 로그인이 `[28444]`/`DEVELOPER_ERROR` 로 실패하면 logcat SHA-1 로 누락 클라이언트를 찾고, 하이브리드 서명이면 22-05 결정의 대안(내부 테스트 단계 앱 서명 키 변경)을 사용자와 정한다.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "lane :latest" mobile/ios/App/fastlane/Fastfile` 가 1 · `grep -c "latest_testflight_build_number" mobile/ios/App/fastlane/Fastfile` 가 1 · `grep -c "lane :beta" mobile/ios/App/fastlane/Fastfile` 가 1(기존 lane 유지)
    - `out="$(bash mobile/scripts/release-ios.sh no-such-mode 2>&1)"; test $? -eq 2` 가 참(알 수 없는 모드 exit 2)
    - SUMMARY 의 supply versionCode 가 track 출력에 있고 22-05 첫 versionCode 보다 크다 · SUMMARY 의 latest 빌드 번호가 22-01 업로드 빌드 번호와 같다
    - 네 `<automated>` 가 통과한다
  </acceptance_criteria>
  <done>Play 내부 트랙과 TestFlight 에 각각 처리 완료된 빌드가 있고, 설치본 로그인·트레이딩 차단 확인은 end-of-phase UAT 목록에 올라갔다.</done>
</task>

<task type="auto">
  <name>Task 3: `mobile/README.md` 「릴리스」 절 · 테스터 안내 · 비밀 파일 · 범위 밖 갱신 + `google-client-ids.ts` 주석(D-08)</name>
  <files>mobile/README.md, webapp/src/lib/native/google-client-ids.ts</files>
  <read_first>
    - mobile/README.md (전체 — 「명령」 표 · 「기기 빌드 전」 · 「비밀 파일」 L90-92 · 「범위 밖 (Phase 21 Deferred)」 L94-100 · 문체)
    - webapp/src/lib/native/google-client-ids.ts (L1-20 주석 — Android 절)
    - webapp/src/lib/native/__tests__/google-client-ids.test.ts (형식 잠금 — 상수 불변 확인)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md (Open Question 3 · 5 · Pitfall 1 · 5 · 6 · 9 · Security Domain)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-CONTEXT.md (D-02 · D-03 · D-08 · D-09a · D-12 · Deferred)
    - 22-01 · 22-04 · 22-05 SUMMARY (실제 명령 · 콘솔 문구 · 실측 시간)
  </read_first>
  <action>
    ① `mobile/README.md` 에 `## 릴리스 (TestFlight · Play 내부 테스트 — Phase 22)` 절을 「기기 빌드 전」 절 뒤에 더한다(한국어 · 기존 문체). 내용: (a) 한 줄 요약 — Claude 가 명령을 실행하고, 비밀은 사용자가 `!` 로 저장소 밖에 넣는다(D-06). (b) 명령표 — `native:release:ios` · `native:release:android` · `native:release:android:aab` 와 보조 모드 `release-ios.sh latest` · `release-android.sh validate|track|check`, 각각 성공 표시(`IPA CHECK OK` · `AAB CHECK OK`). (c) 처음 1회 순서 — Apple 번들 ID·ASC 앱 레코드·내부 그룹(자동 배포) → `! bash mobile/scripts/setup-release-secrets.sh dir asc` → `… keystore backup` → Play 앱·첫 AAB 수동 업로드(서명 키 선택은 22-05 결정)·내부 테스트 출시 → `… play-sa` + Play 사용자 및 권한 초대 → GCP Android OAuth 클라이언트(업로드 키 · Play 앱 서명 인증서마다) → OAuth 동의 화면 상태. (d) 업데이트(D-03) — 빌드 번호는 자동(iOS `YYYYMMDDHHMM` · Android `(연도−2020)·10^8 + MMDDHHmm` ≤ 2,100,000,000 · 2040년 말까지 · 저장소 파일에 쓰지 않음)이라 명령 한 번이면 TestFlight 내부 그룹에 자동 배포되고 Play 는 스토어 자동 업데이트 · **TestFlight 빌드는 90일 뒤 만료** — 그 전에 새 빌드 · 같은 분 안에 두 번 올리지 않는다(번호가 같아 거절 — 1분 뒤) · iOS·Android 명령을 동시에 돌리지 않는다(같은 생성 파일을 cap sync 가 다시 씀) · 마케팅 버전 1.0 은 올리고 싶을 때만 `MARKETING_VERSION`·`versionName` 을 손으로. (e) 비밀 파일 표 — `~/.config/gh-trade/release/`(700) 아래 파일 이름 · 용도 · 권한(600)만(값·키 ID·비밀번호는 적지 않는다) · Secret Manager 백업 이름 3개 · 업로드 키를 잃으면 Play 「업로드 키 재설정」 요청(Google 이 앱 서명 키를 보관). (f) 문제 해결 — Android 로그인 `[28444]`/`DEVELOPER_ERROR` → `adb logcat -s GoogleProvider` 의 `signingSha1` 을 GCP Android 클라이언트 목록과 대조 · Play 「draft app」 → `PLAY_RELEASE_STATUS=draft` · sigh 권한 오류 → 포털에서 App Store 프로파일 수동 생성 · ITMS-91053 경고 메일 → 기록만(Deferred). (g) **테스터 안내**(테스터에게 그대로 보낼 한국어 문구) — iPhone: ① App Store Connect 초대 메일 수락(3일 안) ② TestFlight 앱 설치 후 GH Trade 초대 수락 ③ 설치 · Android: ① 참여 링크 열어 「테스터 되기」 ② Play 스토어에서 설치 · 로그인은 Google 계정 · 트레이딩 탭은 안내 화면만 보이는 것이 정상(D-04). iPhone 이 3단계인 이유(내부 테스터는 ASC 팀 사용자여야 함)를 한 줄로.
    ② 「비밀 파일」 절: 기존 문장 뒤에 릴리스 비밀은 `~/.config/gh-trade/release/` 에만 있고 저장소에는 경로·변수 이름만 있다는 문장과 `bash scripts/check-release-hygiene.sh` 로 권한·ignore 를 확인한다는 문장을 더한다.
    ③ 「범위 밖」 절: 제목을 「범위 밖 (Phase 21 · 22 Deferred)」 로 · 릴리스 서명·릴리스 SHA-1 등록을 미룬다는 기존 항목 줄을 지우고(이 phase 가 했다) · 「App Store · Play 스토어 제출」 을 「스토어 정식 출시·심사 · TestFlight 외부 테스터 · Play 비공개/공개 테스트 · Play 데이터 보안 양식 · App Store 앱 개인정보 양식(D-12)」 으로 · 「앱 안 계정 삭제(App Store 가이드라인 5.1.1(v)) — 정식 심사 때」 를 더한다. 푸시·딥링크·Privacy manifest 줄은 유지.
    ④ 문서에 비밀 값·키 ID 값·테스터 이메일·SA 이메일 전체 주소를 적지 않는다(SA 는 이름 `gh-radar-play-publisher` 만).
    ⑤ `webapp/src/lib/native/google-client-ids.ts`: 머리 주석 Android 절의 마지막 문장(릴리스 SHA-1 이 Deferred 로 미등록이라는 문장)을 「릴리스는 업로드 키 SHA-1 과 Play 앱 서명 인증서 SHA-1(표시되는 인증서 전부)을 각각 Android OAuth 클라이언트로 추가 등록했다(Phase 22 D-08). id_token aud 는 여전히 웹 클라이언트라 코드·Supabase Client IDs 변경은 없다. SHA-1 값은 여기 적지 않고 GCP 콘솔과 logcat `signingSha1` 로 대조한다.」 로 바꾼다. 상수 4개·export 는 한 글자도 바꾸지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/native/__tests__/google-client-ids.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or "google-client-ids.test.ts" is absent from the output</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && R=mobile/README.md && for s in "## 릴리스" "native:release:ios" "native:release:android:aab" "setup-release-secrets.sh dir asc" "90일" "2,100,000,000" "signingSha1" "Marketing" "check-release-hygiene.sh"; do grep -qF "$s" "$R" || { echo "README MISSING $s"; exit 1; }; done; bash mobile/scripts/check-release-hygiene.sh</automated>
    <fails_when>non-zero exit, or output contains "README MISSING", or lacks "RELEASE HYGIENE OK"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE "PASSWORD=|BEGIN PRIVATE KEY|\"private_key\"" mobile/README.md` 가 0 · `grep -cE "[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[a-z]{2,}" mobile/README.md` 가 0 (이메일 주소 없음)
    - `grep -c "release 서명 자동화" mobile/README.md` 가 0 · `grep -c "데이터 보안 양식" mobile/README.md` 가 1 이상
    - `grep -c "Phase 22 D-08" webapp/src/lib/native/google-client-ids.ts` 가 1 · `d="$(git diff {이 태스크 커밋}^ {이 태스크 커밋} -- webapp/src/lib/native/google-client-ids.ts)" && printf '%s\n' "$d" | grep -cE "^[-+]export const"` 가 0 (상수 무변경 · git 실패는 `&&` 에서 먼저 드러난다)
    - 두 `<automated>` 가 통과한다
  </acceptance_criteria>
  <done>릴리스 절차·테스터 안내·비밀 위치가 한 문서에 정리됐고, OAuth 식별자 파일은 사실만 갱신됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Play SA 키 → Google Play Developer API | 키가 새면 앱 트랙에 임의 빌드를 올릴 수 있다 |
| ASC 팀 사용자(테스터) → App Store Connect | 과한 역할은 인증서·앱 설정 접근으로 이어진다 |
| 테스터 계정 → relay 트레이딩 | 실돈 주문 경로 |
| Play 설치본 서명 → Google OAuth | SHA-1 누락 시 로그인 실패 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-10 | Elevation of Privilege | Play 게시용 SA 권한 | medium | mitigate | Play 앱 단위 「테스트 트랙 출시 · 앱 정보 보기」 만 · 계정 권한 없음 · GCP 프로젝트 역할 없음 · 키 600 · 유출 시 GCP 에서 키 삭제 |
| T-22-14 | Elevation of Privilege | ASC 테스터 역할 | medium | mitigate | Marketing 역할 + 앱 액세스 GH Trade 한정(Developer 역할 금지 — 인증서 접근) |
| T-22-06 | Elevation of Privilege | 테스터의 실돈 주문 경로 | high | mitigate | `dma_credentials` 미발급(D-04 · prohibition) · relay 가 서버측 `unauthorized` 로 모든 구독·주문 프레임 거부(`relay/src/ws/fanout.ts` — 무변경) · UAT ③ DmaGate 확인 |
| T-22-05 | Denial of Service (로그인 가용성) | Play 앱 서명 SHA-1 미등록 | medium | mitigate | 업로드 + 앱 서명 인증서마다 Android 클라이언트(Task 1 acceptance 개수) · UAT ② logcat `signingSha1` 대조 · README 문제 해결 절 |
| T-22-15 | Spoofing | OAuth 동의 화면·가입 제한 없음 | low | accept | D-05 — 웹과 같은 공개 가입 정책을 유지(앱이 새 가입 경로를 만들지 않는다) · 실돈 경로는 T-22-06 이 막는다 |
| T-22-02 | Information Disclosure | README·SUMMARY 에 비밀·이메일 기재 | high | mitigate | README 비밀 패턴 0 · 이메일 주소 0(acceptance grep) · hygiene (7) · 테스터는 명수만 |
</threat_model>

<verification>
- SA validate · supply 업로드 · track · TestFlight latest · 저장소 파일 무변경 · client-ids 테스트 · README 필수 문구 · RELEASE HYGIENE OK
- end-of-phase UAT: iOS·Android 설치본 로그인 · logcat signingSha1 · DmaGate
</verification>

<success_criteria>
- 두 스토어의 테스트 트랙에 처리 완료된 빌드가 있고, Android 릴리스 서명으로도 Google 로그인이 가능하도록 GCP 가 등록됐으며, 테스터가 따라 할 절차가 문서에 있다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/Gemfile` · `mobile/Gemfile.lock` (fastlane `~> 2.240`) | Ruby 의존성 | 22-01 |
| `mobile/fastlane/build_numbers.rb` — `GhTradeBuildNumbers.ios_build_number(t)` (22-01) · `GhTradeBuildNumbers.android_version_code(t)` · `ANDROID_VERSION_CODE_MAX` (22-04) | 빌드 번호 함수 | 22-01 · 22-04 |
| `mobile/fastlane/test/build_numbers_test.rb` (Ruby minitest) | 단위 테스트 | 22-04 |
| `mobile/ios/App/fastlane/Fastfile` lane `beta` (22-01) · lane `latest` (22-06) · `mobile/ios/App/fastlane/Appfile` | fastlane | 22-01 · **22-06** |
| `mobile/android/fastlane/Fastfile` lanes `build` · `beta` · `validate` · `track` · `mobile/android/fastlane/Appfile` | fastlane | 22-04 |
| `mobile/scripts/setup-release-secrets.sh` — stages `dir` · `asc` (22-01) · `keystore` · `backup` · `play-sa` (22-04) | 사용자 `!` 비밀 주입 | 22-01 · 22-04 |
| `mobile/scripts/release-ios.sh` — 인자 `beta`(기본) · `latest`(22-06) · exit 3 = 비밀 env 누락 | 래퍼 | 22-01 · **22-06** |
| `mobile/scripts/release-android.sh` — 인자 `beta`(기본) · `build` · `validate` · `track` · `check` | 래퍼 | 22-04 |
| `mobile/scripts/check-ipa.sh` (`IPA CHECK OK`) · `mobile/scripts/check-aab.sh` (`AAB CHECK OK`) · `mobile/scripts/check-release-hygiene.sh` (`RELEASE HYGIENE OK`) | 검사 스크립트 | 22-01 · 22-04 |
| npm scripts `native:release:ios` (22-01) · `native:release:android` · `native:release:android:aab` (22-04) | 스크립트 | 22-01 · 22-04 |
| Gradle property `ghtradeVersionCode` · release `signingConfigs` ← env `GHTRADE_UPLOAD_STORE_FILE` · `GHTRADE_UPLOAD_STORE_PASSWORD` · `GHTRADE_UPLOAD_KEY_ALIAS` · `GHTRADE_UPLOAD_KEY_PASSWORD` · `gradle.taskGraph.whenReady` 릴리스 가드 | Android 빌드 설정 | 22-04 |
| env 이름: `ASC_KEY_ID` · `ASC_ISSUER_ID` · `ASC_KEY_PATH` · `GHTRADE_RELEASE_OUT` (ios.env) · `GHTRADE_UPLOAD_*` · `GHTRADE_UPLOAD_SHA1` · `GOOGLE_PLAY_JSON_KEY` (android.env) · `PLAY_RELEASE_STATUS` · `GHTRADE_RELEASE_ENV` · `GHTRADE_RELEASE_DIR` · `WEEKLY_WINE_FASTLANE_DIR` (선택 override) | env 이름(값은 저장소 밖) | 22-01 · 22-04 |
| 저장소 밖 `~/.config/gh-trade/release/` (700) — `ios.env` · `AuthKey_{KEYID}.p8` · `android.env` · `ghtrade-upload.jks` · `play-service-account.json` (600) · 산출 경로 `~/Library/Developer/gh-trade-release/ios` | 비밀·산출물(저장소 밖) | 22-01 · 22-04 · **22-06** |
| GCP Secret Manager `gh-radar-ghtrade-upload-keystore` · `gh-radar-ghtrade-upload-keystore-password` · `gh-radar-ghtrade-asc-api-key` · GCP SA `gh-radar-play-publisher` · API `androidpublisher.googleapis.com` | 외부(사용자 `!`) | 22-04 · **22-06** |
| iOS `Info.plist` `ITSAppUsesNonExemptEncryption` = false | 설정 | 22-01 |
| `mobile/.gitignore` 릴리스 비밀·산출물 패턴 | 설정 | 22-01 |
| `.planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md` (승인된 한글 초안) | 계획 문서 | 22-02 |
| 웹 `webapp/src/lib/supabase/public-path.ts` — `PUBLIC_PREFIXES` · `isPublicPath` | 유틸 | 22-03 |
| 웹 라우트 `/privacy` — `webapp/src/app/privacy/page.tsx` (`metadata.title` 「개인정보처리방침 · GH Trade」) | RSC 페이지 | 22-03 |
| 테스트: `webapp/src/lib/supabase/__tests__/public-path.test.ts` · `webapp/src/app/privacy/__tests__/page.test.tsx` · `webapp/e2e/specs/auth-guards.spec.ts` `/privacy` 2케이스 | 단위 · e2e | 22-03 |
| `mobile/README.md` 「릴리스 (Phase 22)」 절 · 테스터 안내 · 「비밀 파일」 · 범위 밖 갱신 | 문서 | **22-06** |
| `webapp/src/lib/native/google-client-ids.ts` 주석 갱신(상수 값 불변) | 주석 | **22-06** |
| 콘솔: ASC 앱 레코드 · TestFlight 내부 그룹 `GH Trade 테스터`(자동 배포) · Play 앱 · 내부 테스트 트랙 · GCP Android OAuth 클라이언트(업로드 키 · Play 앱 서명 인증서마다) | 외부 상태 | 22-01 · 22-05 · **22-06** |

<output>
Create `.planning/phases/22-gh-trade-ios-testflight-android-play/22-06-SUMMARY.md` when done — 「결정」 절에 Android 클라이언트 개수 · 동의 화면 상태 · 테스터 명수(이메일 없음) · supply versionCode · TestFlight latest 빌드 번호 · Play 권한 전파 대기 시간 · draft 전환 여부를 적는다.
</output>
