---
phase: 22-gh-trade-ios-testflight-android-play
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/Gemfile
  - mobile/Gemfile.lock
  - mobile/fastlane/build_numbers.rb
  - mobile/ios/App/fastlane/Fastfile
  - mobile/ios/App/fastlane/Appfile
  - mobile/ios/App/App/Info.plist
  - mobile/scripts/setup-release-secrets.sh
  - mobile/scripts/release-ios.sh
  - mobile/scripts/check-ipa.sh
  - mobile/package.json
  - mobile/.gitignore
autonomous: false
requirements: [MOBILE-02]
user_setup:
  - service: apple-developer-app-store-connect
    why: "sigh 가 App Store 프로파일을 만들려면 Explicit 번들 ID 가, pilot 이 업로드하려면 ASC 앱 레코드가 있어야 한다 (D-01 · D-10 · RESEARCH Pitfall 4)"
    dashboard_config:
      - task: "Explicit App ID `com.ghtrade.app` 등록 (Description `GH Trade`, Capabilities 추가 없음)"
        location: "developer.apple.com → Certificates, IDs & Profiles → Identifiers → +"
      - task: "신규 앱 — iOS · 이름 `GH Trade` · 기본 언어 한국어 · 번들 ID `com.ghtrade.app` · SKU `com.ghtrade.app` · 사용자 액세스 전체"
        location: "appstoreconnect.apple.com → 앱 → +"
      - task: "TestFlight 내부 그룹 `GH Trade 테스터` 생성 · 자동 배포 켬 · 본인(Account Holder) 추가"
        location: "App Store Connect → GH Trade → TestFlight → 내부 테스팅 +"
  - service: release-secrets-ios
    why: "fastlane 이 ASC API 키로 프로파일 생성·업로드를 한다 — 키는 weekly-wine 것을 재사용하고 사용자가 `!` 로 저장소 밖에 복사한다 (D-06 · D-07 · 재요청 금지)"
    env_vars:
      - name: ASC_KEY_ID
        source: "`! bash mobile/scripts/setup-release-secrets.sh dir asc` 가 weekly-wine `ios/App/fastlane/.env.default` 에서 줄만 옮긴다"
      - name: ASC_ISSUER_ID
        source: "같은 단계"
      - name: ASC_KEY_PATH
        source: "같은 단계 — `~/.config/gh-trade/release/AuthKey_{KEYID}.p8` (600)"
      - name: GHTRADE_RELEASE_OUT
        source: "같은 단계 — 저장소 밖 산출 경로 `~/Library/Developer/gh-trade-release/ios`"

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-01: 계정 개설 태스크가 없다 — Apple 팀 `954QPCS3F5` 유료 멤버십을 전제로 번들 ID·ASC 앱 레코드·내부 그룹만 사용자가 콘솔에서 만든다(Task 1)"
    - "D-02 · D-10: TestFlight 내부 그룹 `GH Trade 테스터`(자동 배포 켬)가 있고, ASC 사용자 초대 화면이 열리는지(개인 멤버십 A2) Task 1 재개 신호로 보고됐다 — 불가면 D-02 재논의로 멈춘다"
    - "트레이서: `pnpm --filter @gh-radar/mobile run native:release:ios` 한 번이 운영 sync(양 플랫폼) → `PROD CONFIG OK` → 래퍼(env 이름 검증) → fastlane lane `beta`(sigh `readonly: false` · gym Release archive 자동 서명 + App Store 프로파일 export · pilot 업로드) → `IPA CHECK OK` 까지 끝나고, 그 빌드가 App Store Connect 에 올라갔다"
    - "D-09 · D-09a: iOS `CFBundleVersion` = 빌드 시각 `YYYYMMDDHHMM`(12자리)이 lane 에서 계산돼 `xcargs CURRENT_PROJECT_VERSION=` 로만 주입된다 — Info.plist 의 `CFBundleVersion` 은 `$(CURRENT_PROJECT_VERSION)` 변수 그대로, pbxproj 는 무변경, 마케팅 버전 1.0 유지"
    - "D-07: fastlane `~> 2.240` 이 `mobile/Gemfile` 하나로 잠기고(Homebrew Ruby 4 · `Gemfile.lock` 커밋), weekly-wine iOS lane 의 결함 3건(agvtool 빌드 번호 증가 · 읽기 전용 sigh · 저장소 안 산출물)을 따르지 않는다"
    - "D-06: ASC 키 복사는 사용자 `!` 한 줄(`setup-release-secrets.sh dir asc`)로만 일어나고, 그 스크립트는 값 없이 `OK`/`SKIP` 만 출력한다 — 실행기는 비밀 파일을 열거나 출력하지 않는다"
    - "Empty 엣지: env 파일이 없거나 키가 비면 `release-ios.sh` 가 fastlane 을 부르기 전에 exit 3 으로 멈추고 **키 이름·파일 경로·주입 명령만** 출력한다"
    - "V14 · Claude 재량: `ITSAppUsesNonExemptEncryption` = false(HTTPS 만 사용) · 산출물(`.ipa`·`.dSYM.zip`·`.mobileprovision`)은 `GHTRADE_RELEASE_OUT`(저장소 밖)에만 떨어지고 `mobile/.gitignore` 가 이중 방어한다 · IPA 는 Apple Distribution(954QPCS3F5) 서명 · application-identifier `954QPCS3F5.com.ghtrade.app`(키체인 권한 유지) · get-task-allow false · CAPACITOR_DEBUG 빈 값 · 운영 URL"
  artifacts:
    - path: mobile/Gemfile
      provides: "fastlane ~> 2.240 (rubygems.org 단일 소스)"
      contains: "fastlane"
    - path: mobile/fastlane/build_numbers.rb
      provides: "GhTradeBuildNumbers.ios_build_number(t) — YYYYMMDDHHMM"
      contains: "GhTradeBuildNumbers"
    - path: mobile/ios/App/fastlane/Fastfile
      provides: "lane beta — sigh(readonly false) → gym(xcargs 빌드 번호 · 저장소 밖 출력) → pilot"
      contains: "lane :beta"
    - path: mobile/scripts/release-ios.sh
      provides: "env 로드·이름 검증(exit 3) → bundle exec fastlane beta → check-ipa"
    - path: mobile/scripts/check-ipa.sh
      provides: "IPA 서명·엔타이틀먼트·빌드 번호·수출 규정·디버그·운영 URL 검사 → IPA CHECK OK"
    - path: mobile/scripts/setup-release-secrets.sh
      provides: "사용자 `!` 비밀 주입 — stages dir · asc (22-04 가 keystore · backup · play-sa 추가)"
  key_links:
    - from: mobile/package.json
      to: mobile/scripts/release-ios.sh
      via: "native:release:ios = cap sync && node scripts/verify-prod-config.mjs && bash scripts/release-ios.sh"
      pattern: "native:release:ios"
    - from: mobile/ios/App/fastlane/Fastfile
      to: mobile/fastlane/build_numbers.rb
      via: "require (Fastfile 기준 절대 경로) → GhTradeBuildNumbers.ios_build_number"
      pattern: "build_numbers"
    - from: mobile/scripts/release-ios.sh
      to: mobile/scripts/check-ipa.sh
      via: "lane 성공 뒤 bash scripts/check-ipa.sh"
      pattern: "check-ipa.sh"
  prohibitions:
    - statement: "MUST NOT 비밀값(ASC .p8 내용 · 키 ID·발급자 ID 값 · 업로드 키스토어·비밀번호 · Play SA JSON)을 저장소 파일·문서·로그·PLAN/SUMMARY·대화 출력에 남긴다 — 비밀은 ~/.config/gh-trade/release/(700/600)와 Secret Manager 에만"
      status: resolved
      verification: test
    - statement: "MUST NOT 빌드 번호를 저장소 파일(Info.plist · pbxproj · build.gradle)에 써서 커밋한다 — lane 이 빌드 시점에 주입만 한다(D-09)"
      status: resolved
      verification: test
---

<assumption_delta_decision>
- 검출기: `gsd-tools query assumption-delta scan 22 --json` → `{"detected": false}` (2026-09-27 실행). 검출기가 발화하지 않아 이 플랜은 결정 블록을 두지 않는다 — 기록만 남긴다.
</assumption_delta_decision>

<objective>
**트레이서(Phase 22 첫 구현 슬라이스) — iOS 릴리스 한 경로를 끝까지.** 사용자 콘솔 선행(Task 1)을 받은 뒤, `native:release:ios` 한 명령이 운영 sync → `native:verify-prod` 게이트 → 비밀 env 로드(이름만 검증) → fastlane lane `beta`(sigh → gym → pilot) → 산출물 검사까지 이어져 **실제 빌드가 App Store Connect(TestFlight)에 올라간다**는 것을 증명한다(D-06 · D-07 · D-09 · D-09a · D-10).

Purpose: 이 phase 의 위험은 코드보다 **콘솔 상태 × 로컬 설정 × 비밀 경계**의 짝 맞추기에 있다(RESEARCH Key insight). 번들 ID·프로파일 생성(A9 키 권한)·업로드를 첫 커밋에서 실측하면, Android·문서·테스터 절차는 증명된 경로 위에 얹힌다. 트레이서가 실패하면 lane 구조·서명 방식은 재설계 대상이다.
Output: `mobile/Gemfile(.lock)` · `mobile/fastlane/build_numbers.rb` · iOS Fastfile/Appfile · `Info.plist` 수출 규정 키 · 비밀 주입 스크립트(`dir`·`asc`) · `release-ios.sh` · `check-ipa.sh` · `native:release:ios` · `.gitignore` 보강 · TestFlight 에 올라간 첫 빌드.

스키마 게이트: 이 phase 범위(ROADMAP·CONTEXT·RESEARCH)에서 `supabase/migrations` 등 스키마 파일이 검출되지 않았다 — DB push 태스크 없음.
트레이서 위치: 콘솔 선행(Task 1)은 저장소 코드와 무관한 사람 전용 작업이라 트레이서 앞에 둔다(21-01 선례). 비밀 주입은 스크립트가 있어야 가능하므로 트레이서 **안의 인증 게이트**(checkpoints.md 「Authentication Gates」)로 처리한다 — 아래 Task 2 ⑪.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-CONTEXT.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-PATTERNS.md
@mobile/package.json
@mobile/.gitignore
@mobile/scripts/verify-prod-config.mjs
@mobile/scripts/check-sim-entitlements.sh

작업 루트는 메인 체크아웃 `/Users/alex/repos/gh-radar` 다(worktree 아님). Ruby 는 Homebrew(`/opt/homebrew/opt/ruby/bin`, 4.0.2) · bundler 4.0.8 을 쓴다 — 시스템 Ruby 2.6 은 fastlane 최소 3.1 미달. iOS archive·업로드는 수 분이 걸린다 — Bash timeout 600000 으로 돌린다.

**비밀 취급 규칙(이 phase 전 플랜 공통):** 실행기는 `~/.config/gh-trade/release/` 아래 파일과 weekly-wine `.env.default`·`AuthKey.p8` 를 `cat`·`grep`·`source`·Read 하지 않는다(저장소에 Secret read guard 훅이 있고, 메모리 규칙상 비밀 쓰기는 사용자 `!` 실행이다). 비밀은 래퍼 스크립트(`release-*.sh`) 안에서만 `source` 되고, 실행기는 래퍼·검사 스크립트를 경로로 호출할 뿐이다. SUMMARY 에는 빌드 번호·프로파일 이름·SHA-1(공개 지문)·앱 이름처럼 공개 값만 적는다.

<interfaces>
이 플랜이 새로 만드는 계약(후속 플랜이 이 이름으로 소비한다):

```text
mobile/Gemfile                         source rubygems.org · gem "fastlane", "~> 2.240"
mobile/.bundle/config (ignore)         BUNDLE_PATH: vendor/bundle  — bundler 가 ios/App · android 하위에서도 상위 Gemfile 을 찾는다
mobile/fastlane/build_numbers.rb       module GhTradeBuildNumbers
                                         def self.ios_build_number(t = Time.now) -> String  # "%Y%m%d%H%M" 12자리
                                         (22-04 가 android_version_code(t) · ANDROID_VERSION_CODE_MAX 추가)
mobile/ios/App/fastlane/Appfile        app_identifier("com.ghtrade.app") · team_id("954QPCS3F5")
mobile/ios/App/fastlane/Fastfile       platform :ios · lane :beta  (22-06 이 lane :latest 추가)
mobile/scripts/setup-release-secrets.sh  사용: bash mobile/scripts/setup-release-secrets.sh <stage>...
                                         stages(22-01): dir · asc   (22-04: keystore · backup · play-sa)
                                         출력: 「OK <stage>」 / 「SKIP <stage> — 이미 있음」 / 공개 지문·SA 이메일(22-04 이후)만
mobile/scripts/release-ios.sh          env 파일 = ${GHTRADE_RELEASE_ENV:-$HOME/.config/gh-trade/release/ios.env}
                                         exit 3 = env 파일 없음 또는 키 비어 있음(이름만 출력) · 그 외 실패 = fastlane/검사 exit 코드
~/.config/gh-trade/release/ios.env     ASC_KEY_ID · ASC_ISSUER_ID · ASC_KEY_PATH · GHTRADE_RELEASE_OUT   (값은 저장소에 없다)
$GHTRADE_RELEASE_OUT/                  App.ipa · App.app.dSYM.zip · AppStore_com.ghtrade.app.mobileprovision · build_number.txt
mobile/scripts/check-ipa.sh            사용: bash scripts/check-ipa.sh [IPA] [EXPECTED_BUILD] — 기본값은 $GHTRADE_RELEASE_OUT 에서
                                         성공 「IPA CHECK OK build=… 」 · 실패 「IPA CHECK FAIL — …」 exit 1
npm script native:release:ios          cap sync && node scripts/verify-prod-config.mjs && bash scripts/release-ios.sh
```

기존 계약(변경 없음): `native:verify-prod` = `node scripts/verify-prod-config.mjs`(스스로 sync 하지 않음 · 양 플랫폼 생성 설정 검사 → `PROD CONFIG OK`) · pbxproj `DEVELOPMENT_TEAM = 954QPCS3F5` · `CODE_SIGN_STYLE = Automatic` · `MARKETING_VERSION = 1.0` · `CURRENT_PROJECT_VERSION = 1` · Info.plist `CFBundleVersion` = `$(CURRENT_PROJECT_VERSION)` · Release 구성에는 `debug.xcconfig`(CAPACITOR_DEBUG = true)가 연결돼 있지 않다.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apple 콘솔 선행 — Explicit 번들 ID · ASC 앱 레코드 · TestFlight 내부 그룹(자동 배포) · 사용자 초대 가능 여부</name>
  <read_first>
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-CONTEXT.md (D-01 · D-02 리서치 메모 · D-10 · Claude's Discretion 앱 레코드 생성값 · D-12)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md (Pitfall 4 · Pitfall 6 · Assumptions A1 · A2)
  </read_first>
  <action>사용자가 Apple 개발자 포털과 App Store Connect 에서 네 가지를 한다. 이 태스크는 저장소 코드와 무관하고 비밀을 다루지 않는다. 계정 개설은 하지 않는다(D-01 — 두 계정 모두 보유).</action>
  <instructions>
    Claude 가 이미 한 것: 없음(첫 태스크). 이 콘솔 상태가 없으면 Task 2 의 sigh 가 「Could not find App with App Identifier」로, pilot 이 앱 레코드 없음으로 멈춘다(Pitfall 4).

    1. https://developer.apple.com/account/resources/identifiers/list → 「+」 → App IDs → App → Description `GH Trade` · Bundle ID **Explicit** `com.ghtrade.app` · Capabilities 는 아무것도 켜지 않음 → Continue → Register. 이미 등록돼 있으면 건너뛴다.
    2. https://appstoreconnect.apple.com → 앱 → 「+」 → 신규 앱: 플랫폼 iOS · 이름 `GH Trade` · 기본 언어 **한국어** · 번들 ID `com.ghtrade.app` · SKU `com.ghtrade.app` · 사용자 액세스 「전체 액세스」 → 생성. 이름이 선점돼 있으면(A1) 다른 이름(예: `GH Trade 레이더`)을 쓰고 그 이름을 알려 준다 — 홈 화면 이름은 앱 안 `CFBundleDisplayName`(`GH Trade`)이라 영향이 없다. 스크린샷·설명·앱 개인정보 양식은 채우지 않는다(내부 테스트 불필요 · D-12). 아이콘은 빌드에 들어 있는 `native:assets` 산출물을 그대로 쓴다.
    3. 그 앱 → TestFlight → 「내부 테스팅」 옆 「+」 → 그룹 이름 `GH Trade 테스터` · **「자동 배포 활성화」 체크** → 생성 → 테스터에 본인(Account Holder)을 추가한다(D-03 자동 배포 · D-02).
    4. (A2 확인만) 사용자 및 액세스 → 「+」 를 눌러 사용자 초대 화면이 열리는지만 본다 — 초대는 22-06 에서 한다. 열리지 않으면(개인 멤버십 제한) 알려 준다: 내부 테스터 경로가 막히므로 D-02 재논의 대상이다(Pitfall 6).
  </instructions>
  <verification>실행기는 체크포인트를 띄우기 전에 키체인에 팀 `954QPCS3F5` 의 Apple Distribution 인증서(+개인키)가 있는지 확인한다(D-01 — 없으면 export 가 실패하므로 콘솔 작업 전에 알린다). 콘솔 결과 자체(번들 ID · 앱 레코드)는 Task 2 의 sigh·pilot 단계가 사후 확인한다(번들 ID 없음 → sigh 실패, 앱 레코드 없음 → 업로드 실패).</verification>
  <verify>
    <automated>security find-identity -v -p codesigning | grep -q "Apple Distribution: .*(954QPCS3F5)"</automated>
    <fails_when>non-zero exit — no valid Apple Distribution identity for team 954QPCS3F5 in the login keychain (App Store export would fail)</fails_when>
  </verify>
  <acceptance_criteria>
    - 재개 신호가 `done 앱이름={ASC 앱 이름} 사용자초대={가능|불가}` 형태이고, 두 값이 이 플랜 SUMMARY 「결정」 절에 기록된다(테스터 이메일·Apple ID 는 적지 않는다)
    - `사용자초대=불가` 면 Task 2 로 넘어가지 않고 멈춰 D-02 재논의를 요청한다
  </acceptance_criteria>
  <resume-signal>`done 앱이름=GH Trade 사용자초대=가능` 처럼 답한다(이름이 달라졌거나 초대 화면이 막혔으면 그대로 적는다)</resume-signal>
</task>

<task type="tracer">
  <name>Task 2 (tracer): `native:release:ios` → 운영 sync → verify-prod → 래퍼 → fastlane beta(sigh·gym·pilot) → IPA CHECK OK · TestFlight 업로드 (end-to-end)</name>
  <precondition>Task 1 재개 신호가 `사용자초대=가능` 이고 Explicit 번들 ID `com.ghtrade.app` 과 ASC 앱 레코드가 있다 · `security find-identity -v -p codesigning` 출력에 `Apple Distribution` 과 `(954QPCS3F5)` 가 함께 있는 줄이 있다 · `/opt/homebrew/opt/ruby/bin/ruby -v` 가 4.x 다</precondition>
  <files>mobile/Gemfile, mobile/Gemfile.lock, mobile/fastlane/build_numbers.rb, mobile/ios/App/fastlane/Fastfile, mobile/ios/App/fastlane/Appfile, mobile/ios/App/App/Info.plist, mobile/scripts/setup-release-secrets.sh, mobile/scripts/release-ios.sh, mobile/scripts/check-ipa.sh, mobile/package.json, mobile/.gitignore</files>
  <read_first>
    - /Users/alex/repos/weekly-wine-app/ios/App/fastlane/Fastfile (검증된 원형 L1-49 — 결함 3건은 따라 하지 않는다) · /Users/alex/repos/weekly-wine-app/ios/App/fastlane/Appfile
    - /Users/alex/repos/weekly-wine-app/.gitignore (L20-31 fastlane 비밀·산출물 패턴 — `*.mobileprovision` 누락은 따라 하지 않는다)
    - mobile/package.json (scripts — `build`/`typecheck`/`test`/`lint` 키 금지 규약) · mobile/.gitignore (L25-27) · mobile/ios/App/App/Info.plist (L34-35 `CFBundleVersion` · 키 순서)
    - mobile/scripts/verify-prod-config.mjs (L26 `PROD_URL` · 출력 규약) · mobile/scripts/check-sim-entitlements.sh (검사 스크립트 헤더·출력 규약 — 가장 가까운 analog)
    - scripts/dma-credentials.sh (L1-60 — env `set -a; source` · 비어 있는 **키 이름만** 출력 · bash 3.2 호환 문자열 누적)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md (Pattern 1 · Pattern 4 · Pattern 5 · Pitfall 2 · 3 · 4 · 7 · 8 · 11 · 12 · Code Examples 「IPA 검사」 · Standard Stack · Installation)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-PATTERNS.md (iOS Fastfile · Appfile · release-*.sh · check-ipa · setup-release-secrets · mobile/.gitignore 절 · Shared Patterns)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-CONTEXT.md (D-06 · D-07 리서치 메모 · D-09 · D-09a · D-10 · Claude's Discretion)
  </read_first>
  <behavior>
    - `release-ios.sh` 를 존재하지 않는 env 경로(`GHTRADE_RELEASE_ENV=/nonexistent/...`)로 실행 → exit 3 · 출력에 그 경로와 `setup-release-secrets.sh dir asc` 주입 명령이 있고 fastlane 은 시작되지 않는다(「Driving the lane」 없음)
    - env 파일에 `ASC_KEY_ID` 만 있으면 → exit 3 · 비어 있는 키 이름 `ASC_ISSUER_ID ASC_KEY_PATH GHTRADE_RELEASE_OUT` 만 출력(값 출력 0)
    - 정상 env → lane `beta` 가 `build_number.txt` 에 12자리 빌드 번호를 쓰고, `check-ipa.sh` 가 IPA 안 `CFBundleVersion` 을 그 값과 대조해 `IPA CHECK OK` 를 낸다
    - `GhTradeBuildNumbers.ios_build_number(Time.new(2026, 9, 27, 0, 49))` == `"202609270049"`
  </behavior>
  <action>
    ① `mobile/Gemfile` 을 만든다: 첫 줄 `source "https://rubygems.org"`, 다음 줄 `gem "fastlane", "~> 2.240"`(D-07 리서치 메모 — 2.238 이 Ruby 4 지원, 2.240.1 이 Ruby 4 cgi 대응 · weekly-wine 잠금본 2.232.2 를 쓰지 않는다). `mobile/` 에서 `PATH=/opt/homebrew/opt/ruby/bin:$PATH bundle config set --local path vendor/bundle` 후 `PATH=/opt/homebrew/opt/ruby/bin:$PATH bundle install` → `mobile/Gemfile.lock` 을 커밋 대상으로 둔다(공급망 고정 · T-22-SC). `mobile/.bundle/config` 와 `mobile/vendor/` 는 커밋하지 않는다.
    ② `mobile/.gitignore` 끝에 섹션 주석 「# 릴리스 비밀·산출물 — 저장소 밖이 원칙, ignore 는 이중 방어(Phase 22 · T-22-01 · T-22-03)」 아래로 다음 패턴을 더한다: `*.p8` · `*.p12` · `*.cer` · `*.mobileprovision` · `key.properties` · `play-store-key.json` · `*service-account*.json` · `*.ipa` · `*.aab` · `*.apk` · `*.dSYM.zip` · `**/fastlane/report.xml` · `**/fastlane/README.md` · `**/fastlane/Preview.html` · `**/fastlane/screenshots/` · `**/fastlane/test_output/` · `**/fastlane/.env*` · `/vendor/` · `/.bundle/`. 기존 `*.jks` · `*.keystore` 줄은 그대로 둔다. `Gemfile.lock` 은 ignore 하지 않는다.
    ③ `mobile/fastlane/build_numbers.rb`: `module GhTradeBuildNumbers` 에 `def self.ios_build_number(t = Time.now)` 하나 — `t.strftime("%Y%m%d%H%M")` 을 돌려준다(D-09 · 로컬 시각 = KST). 파일 머리 한국어 주석: 빌드 번호는 저장소에 쓰지 않고 lane 이 주입만 한다(D-09 · D-09a) · 같은 분 안의 재실행은 같은 번호가 되어 스토어가 거절한다(엣지 FA-1) · Android 공식은 22-04 가 이 모듈에 더한다. 테스트 파일은 22-04 가 두 함수를 함께 잠근다.
    ④ `mobile/ios/App/fastlane/Appfile`: `app_identifier("com.ghtrade.app")` · `team_id("954QPCS3F5")` 두 줄.
    ⑤ `mobile/ios/App/fastlane/Fastfile`(RESEARCH Pattern 1 코드가 정본): 맨 위에서 `mobile/fastlane/build_numbers.rb` 를 Fastfile 디렉터리 기준 절대 경로로 require 한다(`File.expand_path("../../../fastlane/build_numbers.rb", __dir__)` — fastlane 이 Fastfile 을 eval 하므로 상대 require 가 실패하면 이 형태). `default_platform(:ios)` · `platform :ios do` · `lane :beta do`: (a) `%w[ASC_KEY_ID ASC_ISSUER_ID ASC_KEY_PATH GHTRADE_RELEASE_OUT]` 중 빈 것이 있으면 `UI.user_error!` 에 **키 이름만** (b) `app_store_connect_api_key(key_id:, issuer_id:, key_filepath:, in_house: false)` (c) `build_number = GhTradeBuildNumbers.ios_build_number` · `out = ENV["GHTRADE_RELEASE_OUT"]` · `FileUtils.mkdir_p(out)` · `File.write(File.join(out, "build_number.txt"), build_number)` · `UI.message("CFBundleVersion #{build_number}")` (d) `get_provisioning_profile(api_key:, app_identifier: "com.ghtrade.app", team_id: "954QPCS3F5", readonly: false, output_path: out)` — 첫 실행에 「com.ghtrade.app AppStore」 를 만들고 이후 재사용(Pitfall 3) (e) `build_app(project: "App.xcodeproj", scheme: "App", configuration: "Release", export_method: "app-store", export_options: { signingStyle: "manual", teamID: "954QPCS3F5", provisioningProfiles: { "com.ghtrade.app" => lane_context[SharedValues::SIGH_NAME] } }, xcargs: "CODE_SIGN_STYLE=Automatic CURRENT_PROJECT_VERSION=#{build_number} -allowProvisioningUpdates", output_directory: out, output_name: "App", clean: true)` — `export_method` 는 `app-store` 그대로(fastlane 2.240.1 gym 이 `app-store-connect` 를 거부 · Xcode 27 은 수용) (f) `upload_to_testflight(api_key:, skip_waiting_for_build_processing: true)`. **빌드 번호를 프로젝트 파일에 쓰는 fastlane 액션(weekly-wine 결함 ① — agvtool 기반)은 코드에도 주석에도 적지 않는다** — acceptance 의 음성 grep 이 주석까지 센다. 읽기 전용 sigh 옵션도 같은 이유로 주석에 적지 않는다.
    ⑥ `mobile/ios/App/App/Info.plist`: `CFBundleVersion` 블록 다음, `LSRequiresIPhoneOS` 앞에 `ITSAppUsesNonExemptEncryption` 키와 `<false/>` 값을 탭 들여쓰기로 더한다(Claude 재량 — HTTPS 만 사용하는 면제 · 매 빌드 수출 규정 질문 생략). `CFBundleVersion` 의 `$(CURRENT_PROJECT_VERSION)` 변수 참조는 절대 리터럴로 바꾸지 않는다(Pitfall 2). `plutil -lint` 로 확인한다.
    ⑦ `mobile/scripts/setup-release-secrets.sh`(사용자 `!` 실행 전용 · RESEARCH Pattern 5 · analog `scripts/dma-credentials.sh` 헤더 규약): `#!/usr/bin/env bash` · `set -euo pipefail` · `umask 077`. 헤더 주석에 「이 스크립트는 사용자가 `! bash mobile/scripts/setup-release-secrets.sh <stage>…` 로 실행한다 · 대화형 입력 없음(`!` 셸에 TTY 가 없을 수 있다) · 값은 어떤 경로로도 출력하지 않는다 · 이미 있는 파일은 덮어쓰지 않고 SKIP 한다(재실행 안전 · 엣지 FA-4)」. `REL="${GHTRADE_RELEASE_DIR:-$HOME/.config/gh-trade/release}"`. 인자로 받은 stage 를 순서대로 실행하고, 모르는 stage 는 사용법(이 플랜 stages `dir` · `asc`, 22-04 가 `keystore` · `backup` · `play-sa` 추가)을 stderr 에 찍고 exit 2. `dir`: `mkdir -p "$REL"` · `chmod 700 "$REL"` → 「OK dir」. `asc`: `WW="${WEEKLY_WINE_FASTLANE_DIR:-$HOME/repos/weekly-wine-app/ios/App/fastlane}"` · `$REL/ios.env` 가 있으면 「SKIP asc — 이미 있음」 · 없으면 `$WW/.env.default` 에서 `^ASC_KEY_ID=` · `^ASC_ISSUER_ID=` 두 줄만 grep 해 임시 파일로 옮기고(파일 전체를 출력·복사하지 않는다) 그 파일을 source 해 키 ID 를 얻은 뒤 `$WW/AuthKey.p8` 을 `$REL/AuthKey_${ASC_KEY_ID}.p8` 로 `install -m 600` 하고, `ios.env`(600)에 두 줄 + `ASC_KEY_PATH=` 절대 경로 + `GHTRADE_RELEASE_OUT=$HOME/Library/Developer/gh-trade-release/ios` 를 쓴다 → 「OK asc」. 원본 파일이 없으면 「ERROR asc — weekly-wine ASC 키 파일 없음: 경로」만 찍고 exit 1(값 출력 없음). 셸 추적 모드를 켜지 않는다. `chmod +x`.
    ⑧ `mobile/scripts/release-ios.sh`(analog `scripts/dma-credentials.sh`): `set -euo pipefail` · `cd "$(dirname "$0")/.."`(=`mobile/`) · `ENV_FILE="${GHTRADE_RELEASE_ENV:-$HOME/.config/gh-trade/release/ios.env}"`. 파일이 없으면 stderr 에 「ERROR: 릴리스 env 파일이 없습니다: 경로」 와 「주입: ! bash mobile/scripts/setup-release-secrets.sh dir asc」 두 줄 → **exit 3**. 있으면 `set -a; source; set +a` 후 `ASC_KEY_ID` · `ASC_ISSUER_ID` · `ASC_KEY_PATH` · `GHTRADE_RELEASE_OUT` 중 빈 것을 문자열로 모아(bash 3.2 호환 — dma-credentials.sh 방식) 이름만 출력 → exit 3. `ASC_KEY_PATH` 파일이 없어도 이름·경로만 출력하고 exit 3. 통과하면 `export PATH=/opt/homebrew/opt/ruby/bin:$PATH` → `(cd ios/App && bundle exec fastlane beta)` → `bash scripts/check-ipa.sh`. 비밀을 명령줄 인자·echo 로 흘리지 않는다 · 셸 추적 모드 금지 · 헤더에 「이 래퍼는 `native:release:ios` 의 마지막 단계다 — 단독 실행 전에 `native:sync` + `native:verify-prod` 가 선행돼야 한다(Pitfall 8)」와 「두 릴리스 명령을 동시에 돌리지 않는다 — 둘 다 cap sync 로 같은 생성 파일을 다시 쓴다(엣지 FA-5)」. `chmod +x`.
    ⑨ `mobile/scripts/check-ipa.sh`(analog `check-sim-entitlements.sh` · RESEARCH Code Examples 「IPA 검사」): 인자 `IPA="${1:-$GHTRADE_RELEASE_OUT/App.ipa}"` · `EXPECTED="${2:-$(cat "$GHTRADE_RELEASE_OUT/build_number.txt")}"`(비밀 아님). `mktemp -d` + `trap 'rm -rf' EXIT` 에 `unzip -q`. 실패를 문자열로 누적해 마지막에 `IPA CHECK FAIL — …` 줄들 + exit 1, 통과면 `IPA CHECK OK build=$EXPECTED` 한 줄. 검사: `codesign -dvv` 에 `Authority=Apple Distribution:` 와 `(954QPCS3F5)` · `codesign -d --entitlements :-` 에 application-identifier `954QPCS3F5.com.ghtrade.app`(키체인 권한 — 로그인 유지) · get-task-allow 가 false · PlistBuddy `CFBundleVersion` == EXPECTED 이고 12자리 숫자 · `CFBundleShortVersionString` == `1.0` · `ITSAppUsesNonExemptEncryption` == false · `CAPACITOR_DEBUG` 가 true 아님(Release 구성엔 debug.xcconfig 가 없다 — RESEARCH 근거) · `Payload/App.app/capacitor.config.json` 의 `server.url` == `https://trade.jx1.io` 이고 `server.cleartext` 가 true 아님(verify-prod `PROD_URL` 과 같은 값). 헤더에 「왜 — 실패 시 테스터가 겪는 일」(dev URL 로드 · 로그인 키체인 오류 · 웹 인스펙터 노출). `chmod +x`.
    ⑩ `mobile/package.json` scripts 에 `"native:release:ios": "cap sync && node scripts/verify-prod-config.mjs && bash scripts/release-ios.sh"` 를 더한다. `cap sync` 는 **인자 없이** 두 플랫폼을 모두 운영값으로 되돌린다(verify-prod 가 양쪽을 검사 — Pitfall 8). `build`·`typecheck`·`test`·`lint` 키는 여전히 두지 않는다(루트 `pnpm -r run build` 가 집어간다). Ruby PATH 는 package.json 이 아니라 래퍼 안에 둔다.
    ⑪ 실행 순서: (a) Empty 엣지 — `GHTRADE_RELEASE_ENV=/nonexistent/gh-trade-missing-vars bash mobile/scripts/release-ios.sh` 가 exit 3 과 주입 명령을 내는지 본다. (b) 실제 `~/.config/gh-trade/release/ios.env` 가 없어서 `native:release:ios` 가 exit 3 으로 멈추면 **인증 게이트**다: 실행기는 `checkpoint:human-action` 을 반환해 사용자에게 **`! bash mobile/scripts/setup-release-secrets.sh dir asc`** 한 줄을 안내하고(출력은 「OK dir」「OK asc」 뿐이어야 한다), 사용자가 `done` 하면 같은 명령을 다시 돌린다. 실행기는 스크립트를 대신 실행하지 않는다(D-06 · 메모리 「비밀 주입은 사용자 `!` 실행」). (c) `pnpm --filter @gh-radar/mobile run native:release:ios`. 실패 분기: sigh 가 「Could not find App with App Identifier」 → Task 1 의 번들 ID 등록 확인을 요청하고 멈춘다 · sigh 가 권한 오류(403/Forbidden — A9 키 역할 부족) → 멈추고 사용자에게 「포털 Profiles → + → App Store Connect → com.ghtrade.app → Apple Distribution 인증서 → 이름 `com.ghtrade.app AppStore`」 수동 생성을 요청한 뒤 재실행(sigh 가 찾아 쓴다) · archive 가 계정 인증으로 실패하면 xcargs 에 `-authenticationKeyPath/-authenticationKeyID/-authenticationKeyIssuerID`(값은 env 변수 참조)를 더하고 SUMMARY 에 기록 · 업로드가 ITMS-91053/Invalid Binary 로 거절되면 멈추고 사용자에게 알린다 — `PrivacyInfo.xcprivacy` 는 Deferred 라 승인 없이 추가하지 않는다(Pitfall 11 · A6), 경고 메일만이면 SUMMARY 에 기록만. (d) 성공 후 SUMMARY 에 빌드 번호 · 프로파일 이름 · 업로드 시각 · Task 1 앱 이름을 적는다(비밀·테스터 이메일 없음).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/mobile && PATH=/opt/homebrew/opt/ruby/bin:$PATH bundle exec fastlane --version</automated>
    <fails_when>non-zero exit, or the output lacks "fastlane 2.240"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && out="$(GHTRADE_RELEASE_ENV=/nonexistent/gh-trade-missing-vars bash mobile/scripts/release-ios.sh 2>&1)"; code=$?; printf '%s\n' "$out"; test "$code" -eq 3 && printf '%s' "$out" | grep -q "setup-release-secrets.sh dir asc" && ! printf '%s' "$out" | grep -q "Driving the lane"</automated>
    <fails_when>non-zero exit — the wrapper did not stop with exit 3, did not print the `setup-release-secrets.sh dir asc` injection command, or fastlane started ("Driving the lane" in output)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:release:ios</automated>
    <fails_when>non-zero exit, or the output lacks "PROD CONFIG OK" or "IPA CHECK OK", or contains "IPA CHECK FAIL" or "fastlane finished with errors"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && git diff --quiet -- mobile/ios/App/App.xcodeproj/project.pbxproj && test "$(/usr/libexec/PlistBuddy -c 'Print :CFBundleVersion' mobile/ios/App/App/Info.plist)" = '$(CURRENT_PROJECT_VERSION)' && test "$(/usr/libexec/PlistBuddy -c 'Print :ITSAppUsesNonExemptEncryption' mobile/ios/App/App/Info.plist)" = 'false'</automated>
    <fails_when>non-zero exit — pbxproj was modified, CFBundleVersion became a literal, or ITSAppUsesNonExemptEncryption is not false</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'gem "fastlane", "~> 2.240"' mobile/Gemfile` 가 1줄 · `grep -c "fastlane (2.240" mobile/Gemfile.lock` 가 1 이상 · `git check-ignore -q mobile/Gemfile.lock` 가 exit 1(추적 대상)
    - `grep -c "increment_build_number" mobile/ios/App/fastlane/Fastfile` 가 0 · `grep -c "readonly: true" mobile/ios/App/fastlane/Fastfile` 가 0 · `grep -c "readonly: false" mobile/ios/App/fastlane/Fastfile` 가 1 · `grep -c "CURRENT_PROJECT_VERSION=" mobile/ios/App/fastlane/Fastfile` 가 1 이상 · `grep -c 'export_method: "app-store"' mobile/ios/App/fastlane/Fastfile` 가 1 · `grep -c "GHTRADE_RELEASE_OUT" mobile/ios/App/fastlane/Fastfile` 가 1 이상
    - `grep -c 'app_identifier("com.ghtrade.app")' mobile/ios/App/fastlane/Appfile` 가 1 · `grep -c "def self.ios_build_number" mobile/fastlane/build_numbers.rb` 가 1
    - `node -e "const s=require('./mobile/package.json').scripts;process.exit(s['native:release:ios']==='cap sync && node scripts/verify-prod-config.mjs && bash scripts/release-ios.sh'&&!['build','typecheck','test','lint'].some(k=>k in s)?0:1)"` 가 exit 0 (repo 루트에서)
    - `cd mobile && for p in App.ipa x.aab x.apk AuthKey_X.p8 a.p12 a.cer a.mobileprovision key.properties play-store-key.json play-service-account.json ios/App/fastlane/report.xml ios/App/fastlane/README.md android/fastlane/report.xml vendor/bundle/x .bundle/config App.app.dSYM.zip; do git check-ignore -q "$p" || echo "NOT IGNORED $p"; done` 의 출력이 빈 줄
    - `git ls-files -- 'mobile/*.p8' 'mobile/*.p12' 'mobile/*.jks' 'mobile/*.keystore' 'mobile/*.mobileprovision' 'mobile/*.ipa' 'mobile/*.aab' 'mobile/*.apk'` 의 출력이 빈 줄 (git pathspec `*` 는 하위 디렉터리까지 맞춘다)
    - `grep -c "set -x" mobile/scripts/setup-release-secrets.sh mobile/scripts/release-ios.sh mobile/scripts/check-ipa.sh` 의 각 파일 값이 0 · `grep -nE '(echo|printf).*\$(ASC_KEY_ID|ASC_ISSUER_ID)' mobile/scripts/setup-release-secrets.sh mobile/scripts/release-ios.sh` 가 0줄
    - `stat -f '%Lp' "$HOME/.config/gh-trade/release"` 가 `700` (사용자 `dir asc` 실행 뒤)
    - `test -f "$HOME/Library/Developer/gh-trade-release/ios/App.ipa"` 가 exit 0 이고 `find mobile \( -name '*.ipa' -o -name '*.mobileprovision' -o -name '*.dSYM.zip' \) -not -path '*/node_modules/*' -not -path '*/DerivedData/*'` 의 출력이 빈 줄 (산출물은 저장소 밖)
    - 네 `<automated>` 명령이 모두 통과하고, SUMMARY 에 업로드된 빌드 번호(12자리) · sigh 프로파일 이름 · Task 1 앱 이름 · 인증 게이트 발생 여부가 있다(비밀·테스터 이메일 없음)
  </acceptance_criteria>
  <done>운영 설정 게이트를 통과한 Release IPA 가 Apple Distribution 으로 서명돼 TestFlight 에 올라갔고, 빌드 번호는 저장소 파일을 건드리지 않고 주입됐으며, 비밀은 저장소 밖에만 있다 — 커밋됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 저장소 밖 비밀 파일 → 래퍼 스크립트 | `~/.config/gh-trade/release/ios.env`·`.p8` 가 래퍼 프로세스 env 로만 들어온다 |
| 로컬 빌드 → App Store Connect | 서명된 IPA 가 외부(테스터 기기)로 배포된다 — 빌드 설정 오류가 그대로 테스터에게 간다 |
| rubygems.org → `mobile/Gemfile.lock` | fastlane 과 전이 gem 이 로컬 빌드 도구로 들어온다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Information Disclosure | ASC `.p8` · env 파일이 저장소에 들어감 | high | mitigate | 비밀은 `~/.config/gh-trade/release/`(700/600)에만 · `setup-release-secrets.sh` 는 사용자 `!` 실행 · `mobile/.gitignore` 에 `*.p8`·`*.p12`·`*.mobileprovision`·`**/fastlane/.env*` · acceptance 의 `git ls-files` 비밀 패턴 0 |
| T-22-02 | Information Disclosure | 래퍼·fastlane·스크립트 로그에 비밀 출력 | high | mitigate | 누락 시 **키 이름만** 출력(exit 3) · 셸 추적 모드 금지 · echo/printf 에 키 변수 0(acceptance grep) · 실행기는 비밀 파일을 읽지 않는다(Secret read guard 훅 · 컨텍스트 규칙) |
| T-22-03 | Information Disclosure | `.ipa`·`.dSYM.zip`·`.mobileprovision` 이 작업 트리에 남음 | medium | mitigate | sigh `output_path` · gym `output_directory` = `GHTRADE_RELEASE_OUT`(저장소 밖) · gitignore 이중 방어 · acceptance 의 `git status` 산출물 0 |
| T-22-04 | Tampering / Information Disclosure | dev URL·cleartext·웹 인스펙터가 켜진 빌드 업로드 | high | mitigate | `cap sync`(양쪽) → `verify-prod`(PROD CONFIG OK) → `check-ipa.sh`(운영 URL · cleartext · `CAPACITOR_DEBUG` · get-task-allow false) |
| T-22-07 | Tampering (무결성) | 빌드 번호가 Info.plist·pbxproj 에 리터럴로 기록돼 커밋 | low | mitigate | `xcargs CURRENT_PROJECT_VERSION=` 주입만 · 파일 쓰기 액션 금지(음성 grep) · verify 4번(pbxproj 무변경 · 변수 참조 유지) |
| T-22-SC | Tampering | RubyGems 설치(fastlane 2.240.x) | high | mitigate | RESEARCH Package Legitimacy Audit — fastlane Approved(12년 · 213.8M · 공식 저장소) · `source "https://rubygems.org"` 단일 · `~> 2.240` 고정 · `Gemfile.lock` 커밋 · npm 설치 없음 |
</threat_model>

<verification>
- `bundle exec fastlane --version` = 2.240.x · Empty 엣지(exit 3 · 이름만) · `native:release:ios` = PROD CONFIG OK → 업로드 → IPA CHECK OK · pbxproj/Info.plist 빌드 번호 무기록
- gitignore 샘플 전부 ignore · 추적 파일 비밀 패턴 0 · 비밀 디렉터리 700
</verification>

<success_criteria>
- 첫 GH Trade 빌드가 TestFlight 내부 그룹(자동 배포)으로 들어갔다 — 테스터 기기 설치 확인은 22-06 UAT 가 한다.
- 이후 플랜(Android · 문서 · 두 번째 업로드)이 같은 Gemfile · 같은 비밀 디렉터리 · 같은 게이트 사슬 위에 올라간다.
</success_criteria>

## Flagged assumptions (spec-less 엣지 프로브 5건 · unresolved 유지)

엣지 프로브(`MOBILE-02`)가 분류하지 못한 5건이다. 자동 해소하지 않고 이 phase 에 맞춘 해석을 적어 둔다. 방어 가능한 부분은 해당 플랜 `must_haves.truths` 에도 넣었다(표 오른쪽).

| # | 프로브 | 이 phase 에서의 해석(가정) | 판정 근거가 되는 truth |
|---|---|---|---|
| FA-1 | adjacency — 같거나 맞닿은 값 | 같은 분 안에 두 번 릴리스하면 빌드 번호가 같아지고 스토어가 두 번째 업로드를 거절한다(병합·덮어쓰기 없음). 연말→연초 경계는 단조 증가다 | 22-04 build_numbers_test(연 경계) · README 「1분 뒤 재실행」 |
| FA-2 | empty — 빈 입력 | env 파일 없음·키 비어 있음 → 래퍼가 fastlane 전에 exit 3 · 키 이름만. 테스터 0명이어도 업로드는 되지만 아무도 설치하지 못한다 — 테스터 등록은 22-05·22-06 콘솔 절차 | 22-01 Empty 엣지 truth · 22-04 래퍼 동일 |
| FA-3 | ordering — 순서 | 콘솔 선행(번들 ID·ASC 레코드 → sigh / Play 앱·첫 수동 업로드 → supply / SA 초대 → validate)이 lane 보다 먼저다 — 웨이브·precondition 이 강제한다. 같은 빌드 번호 두 개의 순서는 정의하지 않는다(스토어가 거절) | 각 플랜 precondition |
| FA-4 | idempotency — 두 번 실행 | `native:release:*` 두 번(1분 이상 간격) → 두 번째가 더 큰 빌드 번호 · 저장소 파일 무변경. `setup-release-secrets.sh` 재실행은 기존 비밀을 덮어쓰지 않고 SKIP | 22-07 두 번째 업로드 truth · 22-04 keystore SKIP |
| FA-5 | concurrency — 중단·병렬 | 두 릴리스 명령을 동시에 돌리지 않는다(둘 다 `cap sync` 로 같은 생성 파일을 다시 쓴다). 중단돼도 저장소 파일은 바뀌지 않고 생성 설정은 운영값이다 → 재실행은 새 빌드 번호 | 22-07 `git status --porcelain mobile/` 빈 출력 truth |

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/Gemfile` · `mobile/Gemfile.lock` (fastlane `~> 2.240`) | Ruby 의존성 | **22-01** |
| `mobile/fastlane/build_numbers.rb` — `GhTradeBuildNumbers.ios_build_number(t)` (22-01) · `GhTradeBuildNumbers.android_version_code(t)` · `ANDROID_VERSION_CODE_MAX` (22-04) | 빌드 번호 함수 | **22-01** · 22-04 |
| `mobile/fastlane/test/build_numbers_test.rb` (Ruby minitest) | 단위 테스트 | 22-04 |
| `mobile/ios/App/fastlane/Fastfile` lane `beta` (22-01) · lane `latest` (22-06) · `mobile/ios/App/fastlane/Appfile` | fastlane | **22-01** · 22-06 |
| `mobile/android/fastlane/Fastfile` lanes `build` · `beta` · `validate` · `track` · `mobile/android/fastlane/Appfile` | fastlane | 22-04 |
| `mobile/scripts/setup-release-secrets.sh` — stages `dir` · `asc` (22-01) · `keystore` · `backup` · `play-sa` (22-04) | 사용자 `!` 비밀 주입 | **22-01** · 22-04 |
| `mobile/scripts/release-ios.sh` — 인자 `beta`(기본) · `latest`(22-06) · exit 3 = 비밀 env 누락 | 래퍼 | **22-01** · 22-06 |
| `mobile/scripts/release-android.sh` — 인자 `beta`(기본) · `build` · `validate` · `track` | 래퍼 | 22-04 |
| `mobile/scripts/check-ipa.sh` (`IPA CHECK OK`) · `mobile/scripts/check-aab.sh` (`AAB CHECK OK`) · `mobile/scripts/check-release-hygiene.sh` (`RELEASE HYGIENE OK`) | 검사 스크립트 | **22-01** · 22-04 |
| npm scripts `native:release:ios` (22-01) · `native:release:android` · `native:release:android:aab` (22-04) | 스크립트 | **22-01** · 22-04 |
| Gradle property `ghtradeVersionCode` · release `signingConfigs` ← env `GHTRADE_UPLOAD_STORE_FILE` · `GHTRADE_UPLOAD_STORE_PASSWORD` · `GHTRADE_UPLOAD_KEY_ALIAS` · `GHTRADE_UPLOAD_KEY_PASSWORD` · `gradle.taskGraph.whenReady` 릴리스 가드 | Android 빌드 설정 | 22-04 |
| env 이름: `ASC_KEY_ID` · `ASC_ISSUER_ID` · `ASC_KEY_PATH` · `GHTRADE_RELEASE_OUT` (ios.env) · `GHTRADE_UPLOAD_*` · `GHTRADE_UPLOAD_SHA1` · `GOOGLE_PLAY_JSON_KEY` (android.env) · `PLAY_RELEASE_STATUS` · `GHTRADE_RELEASE_ENV` · `GHTRADE_RELEASE_DIR` · `WEEKLY_WINE_FASTLANE_DIR` (선택 override) | env 이름(값은 저장소 밖) | 22-01 · 22-04 |
| 저장소 밖 `~/.config/gh-trade/release/` (700) — `ios.env` · `AuthKey_{KEYID}.p8` · `android.env` · `ghtrade-upload.jks` · `play-service-account.json` (600) · 산출 경로 `~/Library/Developer/gh-trade-release/ios` | 비밀·산출물(저장소 밖) | 22-01 · 22-04 · 22-06 |
| GCP Secret Manager `gh-radar-ghtrade-upload-keystore` · `gh-radar-ghtrade-upload-keystore-password` · `gh-radar-ghtrade-asc-api-key` · GCP SA `gh-radar-play-publisher` · API `androidpublisher.googleapis.com` | 외부(사용자 `!`) | 22-04 · 22-06 |
| iOS `Info.plist` `ITSAppUsesNonExemptEncryption` = false | 설정 | **22-01** |
| `mobile/.gitignore` 릴리스 비밀·산출물 패턴 | 설정 | **22-01** |
| `.planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md` (승인된 한글 초안) | 계획 문서 | 22-02 |
| 웹 `webapp/src/lib/supabase/public-path.ts` — `PUBLIC_PREFIXES` · `isPublicPath` | 유틸 | 22-03 |
| 웹 라우트 `/privacy` — `webapp/src/app/privacy/page.tsx` (`metadata.title` 「개인정보처리방침 · GH Trade」) | RSC 페이지 | 22-03 |
| 테스트: `webapp/src/lib/supabase/__tests__/public-path.test.ts` · `webapp/src/app/privacy/__tests__/page.test.tsx` · `webapp/e2e/specs/auth-guards.spec.ts` `/privacy` 2케이스 | 단위 · e2e | 22-03 |
| `mobile/README.md` 「릴리스 (Phase 22)」 절 · 테스터 안내 · 「비밀 파일」 · 범위 밖 갱신 | 문서 | 22-06 |
| `webapp/src/lib/native/google-client-ids.ts` 주석 갱신(상수 값 불변) | 주석 | 22-06 |
| 콘솔: ASC 앱 레코드 · TestFlight 내부 그룹 `GH Trade 테스터`(자동 배포) · Play 앱 · 내부 테스트 트랙 · GCP Android OAuth 클라이언트(업로드 키 · Play 앱 서명 인증서마다) | 외부 상태 | 22-01 · 22-05 · 22-06 |

<output>
Create `.planning/phases/22-gh-trade-ios-testflight-android-play/22-01-SUMMARY.md` when done — 「결정」 절에 ASC 앱 이름 · 사용자 초대 가능 여부(A2) · 업로드 빌드 번호 · sigh 프로파일 이름 · 인증 게이트 발생 여부 · archive 인증 방식(Xcode 계정 / API 키 플래그)을 적는다. 비밀 값·테스터 이메일은 적지 않는다.
</output>
