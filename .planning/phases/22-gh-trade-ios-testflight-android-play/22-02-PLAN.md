---
phase: 22-gh-trade-ios-testflight-android-play
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md
autonomous: false
requirements: [MOBILE-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-11: 개인정보처리방침 한글 초안이 코드 근거(수집·저장·위탁 항목 11행)를 이번 실행 시점에 다시 대조해 작성됐고, 사용자가 문안을 승인하기 전에는 웹 코드(`webapp/`)에 들어가지 않는다 — 초안은 계획 문서(`22-PRIVACY-DRAFT.md`)로만 존재한다"
    - "D-11 · RESEARCH Open Question 4: `/privacy` 는 기존 `CenterShell` + 타이포 토큰을 재사용하는 텍스트 문서라 HTML 목업 게이트 대신 **문안 검토 게이트**(Task 2, blocking-human)를 둔다 — 이 선택과 이유가 체크포인트 본문에 명시된다"
    - "D-04: 초안의 DMA 계정 정보·주문 기록 항목은 「허용 사용자(운영자 본인)만」 으로 서술되고, 테스터 계정에서는 수집되지 않음이 적혀 있다"
    - "D-12: 초안은 Play 데이터 보안 양식·App Store 앱 개인정보 양식을 대신하지 않으며 이 phase 에서 두 양식은 작성하지 않는다 — 초안 끝 검토자 메모에 「정식 출시 phase 에서 이 항목표를 재사용」 이 있다"
    - "승인 뒤 초안 frontmatter 가 `status: approved` · `approved:` 날짜 · `effective_date:` 를 갖고 본문에 「[확인 필요]」 표식이 0개다(Task 3)"
  artifacts:
    - path: .planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md
      provides: "승인된 한글 개인정보처리방침 문안(13절) + 검토자용 코드 근거 부록"
      contains: "개인정보처리방침"
  key_links:
    - from: .planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md
      to: webapp/src/app/privacy/page.tsx
      via: "22-03 Task 2 가 승인 문안을 그대로 옮긴다(부록 제외)"
      pattern: "status: approved"
  prohibitions:
    - statement: "MUST NOT 개인정보처리방침이 코드가 실제로 수집·저장·위탁하는 항목보다 적게 기재하거나, 사용자 승인 전 문안을 웹 코드로 커밋·배포한다(D-11)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT 테스터 이메일(Apple ID·Google 계정)이나 사용자 개인 연락처를 승인 신호 없이 문서·SUMMARY 에 적는다 — 연락처는 사용자가 승인 단계에서 지정한 값만 쓴다"
      status: resolved
      verification: test
---

<objective>
`/privacy`(D-11)에 들어갈 **한글 개인정보처리방침 초안**을 코드 근거로 작성하고, 사용자가 문안을 승인할 때까지 웹 코드로 옮기지 않는다.

Purpose: 방침은 법적 문구다(RESEARCH A7 · LOW 신뢰). 코드 근거 표로 「실제보다 적게 기재」를 막고, 사람 승인 게이트로 문구 오류를 막는다. 초안을 계획 문서로 먼저 두는 이유: 이 저장소는 다른 세션이 master 를 push 하면 곧 웹 프로덕션 배포라(메모리 「동시 세션 커밋 경합」), 미승인 문안이 웹 코드로 커밋되는 순간 승인 없이 배포될 수 있다. 계획 문서(`.planning/`)는 push 돼도 웹 빌드를 유발하지 않는다(`scripts/vercel-ignore-build.sh` — `webapp/`·`packages/shared/`·`pnpm-lock.yaml` 만 빌드).
Output: `22-PRIVACY-DRAFT.md`(승인 상태) — 22-03 이 페이지로 옮긴다.

D-12: Play 데이터 보안 양식·App Store 앱 개인정보 양식은 이 phase 에서 채우지 않는다. 이 초안의 항목표가 정식 출시 phase 에서 두 양식의 재료가 된다.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-CONTEXT.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md

작업 루트는 메인 체크아웃 `/Users/alex/repos/gh-radar` 다. 이 플랜은 코드를 바꾸지 않는다.

<interfaces>
초안 파일 계약(22-03 이 소비):

```text
.planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md
frontmatter: status (draft|approved) · approved (YYYY-MM-DD, 승인 뒤) · effective_date (YYYY-MM-DD) · contact (사용자가 지정한 연락 수단)
본문: # GH Trade 개인정보처리방침 → 머리말 → ## 1. ~ ## 13. (아래 13개 절 제목 고정) → ## 부록 A. 코드 근거(검토자용 — 페이지에 옮기지 않음) → ## 부록 B. 검토자 메모(페이지에 옮기지 않음)
절 제목(고정 — 22-03 페이지 테스트가 이 문자열로 찾는다):
  1. 개인정보의 처리 목적
  2. 처리하는 개인정보 항목
  3. 보유 및 이용 기간
  4. 제3자 제공
  5. 처리 위탁
  6. 국외 이전
  7. 정보주체의 권리와 행사 방법
  8. 파기 절차와 방법
  9. 안전성 확보 조치
  10. 쿠키와 기기 저장소
  11. 개인정보 보호책임자와 연락처
  12. 방침 변경 고지
  13. 시행일
미정 표식: 「[확인 필요]」 — 승인 단계에서 사용자 답으로 전부 치환한다
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 코드 근거 재확인 → 한글 개인정보처리방침 초안(13절 · 항목표 11행 · 부록) 작성</name>
  <files>.planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md</files>
  <read_first>
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md (§개인정보처리방침 초안 — 코드 근거 수집 항목 표 11행 · 처리 위탁·국외 이전 후보 · 권장 섹션 순서 · Assumptions A7 · A11)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-CONTEXT.md (D-04 · D-11 · D-12 · Deferred)
    - webapp/src/lib/auth-context.tsx (L30-45 — user_metadata 사용 필드)
    - supabase/migrations/20260416120000_watchlists.sql · 20260609120000_theme_tables.sql · 20260702170000_chat_conversations.sql · 20260905120100_dma_credentials.sql · 20260905120200_dma_orders.sql (RESEARCH 표가 인용한 행)
    - server/src/services/specialists/anthropic-client.ts (L1-20 — AI 처리 위탁 근거)
    - webapp/vercel.json (리전)
  </read_first>
  <action>
    ① 근거 재확인(RESEARCH 이후 코드가 바뀌었을 수 있다): RESEARCH 표 11행의 근거 파일이 모두 있는지 `test -f` 로 확인한다. 저장소 전체에서 새 수집 경로가 생겼는지 본다 — `grep -rn "localStorage.setItem\|sessionStorage.setItem" webapp/src` 로 기기 저장 키를 다시 모으고(RESEARCH 표 8행 목록과 대조해 빠진 키를 더한다), `grep -rlE "@vercel/analytics|@sentry|gtag\(" webapp/package.json server/package.json relay/package.json webapp/src` 가 0건인지(분석·광고 SDK 없음), `grep -rnE "deleteUser|회원 ?탈퇴" webapp/src server/src` 가 0건인지(계정 삭제 기능 없음 → 이메일 요청) 확인한다. 결과가 RESEARCH 와 다르면 초안에 반영하고 SUMMARY 에 차이를 적는다.
    ② `22-PRIVACY-DRAFT.md` 를 쓴다(한국어 · 존댓말 · 법령 문체). frontmatter `status: draft` · `contact:` 와 `effective_date:` 는 「[확인 필요]」. 머리말: 서비스 GH Trade(웹 `https://trade.jx1.io` · iOS·Android 앱 — 앱은 같은 웹을 여는 셸)와 운영자(개인 · 「[확인 필요] 운영자 표기」)가 개인정보 보호법에 따라 이 방침을 둔다는 한 문단.
    ③ 절 1~13 을 interfaces 의 제목 그대로 쓴다. 2절은 표(열: 항목 · 저장 위치 · 목적 · 보유 기간) — RESEARCH 표 11행을 사용자 언어로 옮긴다: Google 계정 이메일·이름·프로필 사진 주소 / 관심종목 / 직접 만든 테마 / AI 애널리스트 대화(질문·답변 원문) / 질문의 AI 처리(Anthropic) / DMA 계정 정보(허용 사용자 본인만 · 비밀번호는 암호화 저장 · 테스터 계정에는 없음 — D-04) / 주문 기록(허용 사용자 본인만) / 기기 저장(최근 검색어·화면 배치·알림음 설정·테마 — 서버 전송 없음) / 로그인 세션 쿠키 / 앱 설정(테마) / 오프라인 안내 화면(수집 없음). 3절: 계정 삭제 시 연쇄 삭제(CASCADE) 되는 항목과, 보관 기간 로직이 없는 대화 기록 · 법정 보관 여부가 불명확한 주문 기록은 「[확인 필요]」. 4절: 제3자 제공 없음(주문은 허용 사용자 본인의 증권사 계좌 주문 중계이며 본인 지시에 따른다 — 문구는 「[확인 필요]」 검토 대상으로 표시). 5절 처리 위탁: Supabase(데이터베이스·인증) · Vercel(웹 호스팅) · Google Cloud(API 서버·중계 서버) · Google(로그인) · Anthropic(AI 답변 생성). 6절 국외 이전: 위탁사별 이전 국가·항목·방법 — Anthropic(미국) 외 각사 리전은 「[확인 필요]」(A7). Anthropic 의 보관·학습 미사용 문구는 「[확인 필요]」(A11). 7절: 열람·정정·삭제·처리정지 요구를 연락처로 요청 · 앱 안 탈퇴 기능은 없고 이메일 요청으로 처리. 8절: 요청·목적 달성 시 지체 없이 DB 삭제. 9절: 전송 구간 HTTPS · DMA 비밀번호 AES-256-GCM 암호화 저장 · DB 행 수준 보안(RLS) · 접근 권한 최소화. 10절: 로그인 유지용 세션 쿠키와 기기 저장소(목록) · 분석·광고 도구 없음 · 브라우저/앱 삭제로 지울 수 있음. 11절: 책임자·연락처 「[확인 필요]」. 12절: 변경 시 이 페이지에 7일 전 고지. 13절: 시행일 「[확인 필요]」.
    ④ `## 부록 A. 코드 근거(검토자용 — 페이지에 옮기지 않음)`: 2절 각 행 ↔ 근거 파일·행(RESEARCH 표 그대로 + ①의 재확인 결과). `## 부록 B. 검토자 메모`: 「[확인 필요]」 목록을 번호로 모은 질문표(운영자 표기 · 연락처 · 시행일 · 대화·주문 기록 보관 기간 · 4절 문구 · 각 위탁사 리전 · Anthropic 문구) · 「앱 안 계정 삭제(App Store 5.1.1(v))는 정식 심사 phase」 · 「Play 데이터 보안 양식·App Store 앱 개인정보 양식은 D-12 로 이연 — 이 항목표를 재사용」.
    ⑤ 문서에 테스터 이메일·사용자 이메일·비밀 값을 적지 않는다. 연락처는 사용자가 Task 2 에서 지정한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && D=.planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md && for h in "## 1. 개인정보의 처리 목적" "## 2. 처리하는 개인정보 항목" "## 3. 보유 및 이용 기간" "## 4. 제3자 제공" "## 5. 처리 위탁" "## 6. 국외 이전" "## 7. 정보주체의 권리와 행사 방법" "## 8. 파기 절차와 방법" "## 9. 안전성 확보 조치" "## 10. 쿠키와 기기 저장소" "## 11. 개인정보 보호책임자와 연락처" "## 12. 방침 변경 고지" "## 13. 시행일" "## 부록 A." "## 부록 B."; do grep -qF "$h" "$D" || { echo "MISSING $h"; exit 1; }; done; grep -q "^status: draft" "$D" && echo DRAFT OK</automated>
    <fails_when>non-zero exit, or output contains "MISSING", or lacks "DRAFT OK"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && for f in webapp/src/lib/auth-context.tsx supabase/migrations/20260416120000_watchlists.sql supabase/migrations/20260609120000_theme_tables.sql supabase/migrations/20260702170000_chat_conversations.sql supabase/migrations/20260905120100_dma_credentials.sql supabase/migrations/20260905120200_dma_orders.sql server/src/services/specialists/anthropic-client.ts; do test -f "$f" || { echo "EVIDENCE MISSING $f"; exit 1; }; done; echo EVIDENCE OK</automated>
    <fails_when>non-zero exit, or output contains "EVIDENCE MISSING"</fails_when>
  </verify>
  <acceptance_criteria>
    - 13개 절 제목과 부록 A·B 가 interfaces 의 문자열 그대로 있다(verify 1)
    - `grep -c "\[확인 필요\]" .planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md` 가 1 이상(승인 전 미정 항목이 표식으로 드러나 있다)
    - 2절 표에 「Google 계정」 · 「관심종목」 · 「AI 애널리스트 대화」 · 「DMA 계정 정보」 · 「주문 기록」 · 「기기 저장」 · 「세션 쿠키」 문자열이 각각 1회 이상 있다(`grep -c` 각 1 이상)
    - `grep -cE "@[a-zA-Z0-9._-]+\.(com|net|kr|io)" .planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md` 가 0 (이메일 주소 없음 — 연락처는 승인 단계에서)
    - SUMMARY 에 ① 재확인 결과(새 저장 키 · 분석 SDK 0 · 계정 삭제 0 여부)가 있다
  </acceptance_criteria>
  <done>코드 근거로 대조한 한글 초안이 계획 문서로 있고, 미정 항목은 표식으로 드러나 있다 — 웹 코드는 건드리지 않았다.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: 문안 검토 게이트(D-11) — 개인정보처리방침 한글 초안 승인 · 미정 항목 답</name>
  <read_first>
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md (Task 1 산출 — 전문)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md (Open Question 4 · Assumptions A7 · A11)
  </read_first>
  <decision>`/privacy` 에 올릴 개인정보처리방침 문안을 이대로 승인할지, 무엇을 고칠지, 미정 항목(부록 B)을 어떻게 채울지</decision>
  <context>
    실행기는 체크포인트를 띄울 때 초안 1~13절 본문 전문과 부록 B 질문표를 **메시지 본문에 그대로** 붙인다(사용자는 도구 사이 텍스트를 못 볼 수 있다 — 메모리 「AskQuestion 설명은 질문 텍스트에 임베드」). 이 게이트는 HTML 목업 게이트가 아니라 **문안 검토 게이트**다: `/privacy` 는 기존 `CenterShell` 과 타이포 토큰을 그대로 쓰는 텍스트 문서라 새 시각 디자인이 없다(RESEARCH Open Question 4). 시각 확인은 22-03 이 dev 서버 `/privacy` 스크린샷으로 SUMMARY 에 남긴다. 승인 전에는 웹 코드에 문안이 들어가지 않으며, 승인 뒤에도 push(=웹 프로덕션 배포)는 22-07 의 별도 결정이다. 되돌리는 비용: 배포 뒤 문구 수정은 쉽지만, 부정확한 방침이 게시된 기간 자체는 되돌릴 수 없다.
  </context>
  <options>
    <option id="approve">
      <name>승인 — 부록 B 답과 함께</name>
      <pros>22-03 이 바로 페이지로 옮긴다</pros>
      <cons>부록 B 의 미정 항목(운영자 표기 · 연락처 · 시행일 · 보관 기간 · 4절 문구 · 위탁사 리전 · Anthropic 문구)을 이 답에서 모두 정해야 한다</cons>
    </option>
    <option id="revise">
      <name>수정 요청</name>
      <pros>문구를 고친 뒤 다시 본다</pros>
      <cons>실행기가 초안을 고쳐 이 체크포인트를 다시 띄운다 — 승인될 때까지 반복</cons>
    </option>
  </options>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && grep -qE "^status: (draft|approved)" .planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md && grep -q "## 부록 B." .planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md</automated>
    <fails_when>non-zero exit — the draft file or its reviewer question list is missing, so there is nothing to present</fails_when>
  </verify>
  <acceptance_criteria>
    - 재개 신호가 `approve` + 부록 B 번호별 답이거나 `revise` + 수정 목록이다
    - `revise` 면 실행기는 초안을 고치고 이 체크포인트를 다시 띄운다(Task 3 로 넘어가지 않는다)
    - 이 체크포인트는 auto-advance 로 넘기지 않는다(blocking-human)
  </acceptance_criteria>
  <resume-signal>`approve 1=… 2=… 3=…`(부록 B 번호별 답) 또는 `revise: 2절 …을 …로` 처럼 답한다</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 승인 반영 — 부록 B 답으로 「[확인 필요]」 치환 · `status: approved`</name>
  <files>.planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md</files>
  <read_first>
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md
    - Task 2 재개 신호(사용자 답 원문)
  </read_first>
  <action>Task 2 의 `approve` 답만으로 초안의 「[확인 필요]」 표식을 모두 치환한다(답이 없는 항목이 남으면 멈추고 그 번호를 물어본다 — 임의로 채우지 않는다). frontmatter 를 `status: approved` · `approved: {오늘 날짜}` · `effective_date: {사용자가 준 시행일}` · `contact: {사용자가 지정한 연락 수단}` 로 바꾼다. 문안 자체는 사용자 답 외에는 바꾸지 않는다. 부록 A·B 는 그대로 두되 부록 B 각 질문 옆에 답을 적는다.</action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && D=.planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md && grep -q "^status: approved" "$D" && grep -qE "^approved: [0-9]{4}-[0-9]{2}-[0-9]{2}" "$D" && grep -qE "^effective_date: [0-9]{4}-[0-9]{2}-[0-9]{2}" "$D" && test "$(grep -c '\[확인 필요\]' "$D")" -eq 0 && echo APPROVED OK</automated>
    <fails_when>non-zero exit, or the output lacks "APPROVED OK" (status not approved, dates missing, or a 「[확인 필요]」 marker remains)</fails_when>
  </verify>
  <acceptance_criteria>
    - verify 가 `APPROVED OK` 를 출력한다
    - `git diff {Task 1 커밋 SHA} HEAD -- .planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md` 의 변경이 「[확인 필요]」 치환 · frontmatter 4키 · 부록 B 답 기록 · (revise 라운드가 있었다면) 사용자가 요청한 문구뿐이다(SUMMARY 에 치환 목록)
  </acceptance_criteria>
  <done>승인된 문안이 확정 상태로 계획 문서에 있다 — 22-03 이 그대로 옮긴다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 코드의 실제 수집 → 공개 방침 문구 | 방침이 실제보다 적게 말하면 법적·신뢰 문제가 된다 |
| 로컬 커밋 → 다른 세션의 push → 웹 프로덕션 | 미승인 문안이 웹 코드로 커밋되면 사람 승인 없이 배포될 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-08 | Repudiation (법적) | 개인정보처리방침 부정확·미승인 게시 | medium | mitigate | 코드 근거 재확인(Task 1 ①) · 항목표 11행 필수(acceptance) · blocking-human 문안 게이트(Task 2) · 승인 전 문안은 `.planning/` 에만(웹 빌드 미유발) |
| T-22-17 | Information Disclosure | 방침·SUMMARY 에 개인 이메일 기재 | low | mitigate | 이메일 주소 0 (acceptance grep) · 연락처는 사용자가 승인 답에서 지정한 값만 |
</threat_model>

<verification>
- 초안 13절 + 부록 · 근거 파일 존재 · 승인 뒤 `APPROVED OK`
</verification>

<success_criteria>
- 사용자가 승인한 한글 개인정보처리방침 문안이 확정됐고, 미정 항목이 하나도 남지 않았다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/Gemfile` · `mobile/Gemfile.lock` (fastlane `~> 2.240`) | Ruby 의존성 | 22-01 |
| `mobile/fastlane/build_numbers.rb` — `GhTradeBuildNumbers.ios_build_number(t)` (22-01) · `GhTradeBuildNumbers.android_version_code(t)` · `ANDROID_VERSION_CODE_MAX` (22-04) | 빌드 번호 함수 | 22-01 · 22-04 |
| `mobile/fastlane/test/build_numbers_test.rb` (Ruby minitest) | 단위 테스트 | 22-04 |
| `mobile/ios/App/fastlane/Fastfile` lane `beta` (22-01) · lane `latest` (22-06) · `mobile/ios/App/fastlane/Appfile` | fastlane | 22-01 · 22-06 |
| `mobile/android/fastlane/Fastfile` lanes `build` · `beta` · `validate` · `track` · `mobile/android/fastlane/Appfile` | fastlane | 22-04 |
| `mobile/scripts/setup-release-secrets.sh` — stages `dir` · `asc` (22-01) · `keystore` · `backup` · `play-sa` (22-04) | 사용자 `!` 비밀 주입 | 22-01 · 22-04 |
| `mobile/scripts/release-ios.sh` — 인자 `beta`(기본) · `latest`(22-06) · exit 3 = 비밀 env 누락 | 래퍼 | 22-01 · 22-06 |
| `mobile/scripts/release-android.sh` — 인자 `beta`(기본) · `build` · `validate` · `track` · `check` | 래퍼 | 22-04 |
| `mobile/scripts/check-ipa.sh` (`IPA CHECK OK`) · `mobile/scripts/check-aab.sh` (`AAB CHECK OK`) · `mobile/scripts/check-release-hygiene.sh` (`RELEASE HYGIENE OK`) | 검사 스크립트 | 22-01 · 22-04 |
| npm scripts `native:release:ios` (22-01) · `native:release:android` · `native:release:android:aab` (22-04) | 스크립트 | 22-01 · 22-04 |
| Gradle property `ghtradeVersionCode` · release `signingConfigs` ← env `GHTRADE_UPLOAD_STORE_FILE` · `GHTRADE_UPLOAD_STORE_PASSWORD` · `GHTRADE_UPLOAD_KEY_ALIAS` · `GHTRADE_UPLOAD_KEY_PASSWORD` · `gradle.taskGraph.whenReady` 릴리스 가드 | Android 빌드 설정 | 22-04 |
| env 이름: `ASC_KEY_ID` · `ASC_ISSUER_ID` · `ASC_KEY_PATH` · `GHTRADE_RELEASE_OUT` (ios.env) · `GHTRADE_UPLOAD_*` · `GHTRADE_UPLOAD_SHA1` · `GOOGLE_PLAY_JSON_KEY` (android.env) · `PLAY_RELEASE_STATUS` · `GHTRADE_RELEASE_ENV` · `GHTRADE_RELEASE_DIR` · `WEEKLY_WINE_FASTLANE_DIR` (선택 override) | env 이름(값은 저장소 밖) | 22-01 · 22-04 |
| 저장소 밖 `~/.config/gh-trade/release/` (700) — `ios.env` · `AuthKey_{KEYID}.p8` · `android.env` · `ghtrade-upload.jks` · `play-service-account.json` (600) · 산출 경로 `~/Library/Developer/gh-trade-release/ios` | 비밀·산출물(저장소 밖) | 22-01 · 22-04 · 22-06 |
| GCP Secret Manager `gh-radar-ghtrade-upload-keystore` · `gh-radar-ghtrade-upload-keystore-password` · `gh-radar-ghtrade-asc-api-key` · GCP SA `gh-radar-play-publisher` · API `androidpublisher.googleapis.com` | 외부(사용자 `!`) | 22-04 · 22-06 |
| iOS `Info.plist` `ITSAppUsesNonExemptEncryption` = false | 설정 | 22-01 |
| `mobile/.gitignore` 릴리스 비밀·산출물 패턴 | 설정 | 22-01 |
| `.planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md` (승인된 한글 초안) | 계획 문서 | **22-02** |
| 웹 `webapp/src/lib/supabase/public-path.ts` — `PUBLIC_PREFIXES` · `isPublicPath` | 유틸 | 22-03 |
| 웹 라우트 `/privacy` — `webapp/src/app/privacy/page.tsx` (`metadata.title` 「개인정보처리방침 · GH Trade」) | RSC 페이지 | 22-03 |
| 테스트: `webapp/src/lib/supabase/__tests__/public-path.test.ts` · `webapp/src/app/privacy/__tests__/page.test.tsx` · `webapp/e2e/specs/auth-guards.spec.ts` `/privacy` 2케이스 | 단위 · e2e | 22-03 |
| `mobile/README.md` 「릴리스 (Phase 22)」 절 · 테스터 안내 · 「비밀 파일」 · 범위 밖 갱신 | 문서 | 22-06 |
| `webapp/src/lib/native/google-client-ids.ts` 주석 갱신(상수 값 불변) | 주석 | 22-06 |
| 콘솔: ASC 앱 레코드 · TestFlight 내부 그룹 `GH Trade 테스터`(자동 배포) · Play 앱 · 내부 테스트 트랙 · GCP Android OAuth 클라이언트(업로드 키 · Play 앱 서명 인증서마다) | 외부 상태 | 22-01 · 22-05 · 22-06 |

<output>
Create `.planning/phases/22-gh-trade-ios-testflight-android-play/22-02-SUMMARY.md` when done — 근거 재확인 결과 · 수정 라운드 수 · 부록 B 답(연락처는 사용자가 공개를 허락한 값만) · 승인 날짜를 적는다.
</output>
