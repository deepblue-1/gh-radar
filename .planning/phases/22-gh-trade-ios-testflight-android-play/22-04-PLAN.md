---
phase: 22-gh-trade-ios-testflight-android-play
plan: 04
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - mobile/fastlane/build_numbers.rb
  - mobile/fastlane/test/build_numbers_test.rb
  - mobile/android/app/build.gradle
  - mobile/scripts/setup-release-secrets.sh
  - mobile/scripts/check-release-hygiene.sh
  - mobile/android/fastlane/Fastfile
  - mobile/android/fastlane/Appfile
  - mobile/scripts/release-android.sh
  - mobile/scripts/check-aab.sh
  - mobile/package.json
autonomous: false
requirements: [MOBILE-02]
user_setup:
  - service: release-secrets-android
    why: "업로드 키스토어·비밀번호는 사용자 `!` 실행 스크립트가 생성·보관하고 Secret Manager 에 1본 백업한다 (D-06 · D-08 · 메모리 「비밀 주입은 사용자 `!` 실행」)"
    env_vars:
      - name: GHTRADE_UPLOAD_STORE_FILE
        source: "`! bash mobile/scripts/setup-release-secrets.sh keystore backup` → `~/.config/gh-trade/release/android.env` (600)"
      - name: GHTRADE_UPLOAD_STORE_PASSWORD
        source: "같은 단계 — `openssl rand -base64 32` 로 생성 · 화면 출력 없음"
      - name: GHTRADE_UPLOAD_KEY_ALIAS
        source: "같은 단계 — `ghtrade-upload`"
      - name: GHTRADE_UPLOAD_KEY_PASSWORD
        source: "같은 단계 — PKCS12 라 스토어 비밀번호와 같은 값"
      - name: GHTRADE_UPLOAD_SHA1
        source: "같은 단계 — 업로드 인증서 SHA-1(공개 지문 · 스크립트가 출력)"
    dashboard_config:
      - task: "Secret Manager 백업 확인 — `gh-radar-ghtrade-upload-keystore` · `gh-radar-ghtrade-upload-keystore-password` · `gh-radar-ghtrade-asc-api-key`"
        location: "GCP 콘솔 → 프로젝트 gh-radar → Security → Secret Manager (스크립트 `backup` 단계가 만든다)"

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-09a: Android `versionCode` = `(연도−2020)·10^8 + MMDDHHmm` — 2026-09-27 00:49 → `609270049` · 2040-12-31 23:59 → `2012312359` ≤ 2,100,000,000 · 2026-12-31 23:59(`612312359`) < 2027-01-01 00:00(`701010000`) · 상한을 넘는 시각(2041년~)은 lane 이 빌드 전에 멈춘다 — minitest 로 잠겼다"
    - "D-08 · D-07: `build.gradle` release 서명은 env `GHTRADE_UPLOAD_*` 만 읽고 비밀번호 기본값·리터럴이 없다(weekly-wine 평문 결함 제거) · env 없이 `assembleDebug`(`native:build:android`)는 통과하고 `bundleRelease` 는 가드 메시지와 함께 즉시 실패한다"
    - "D-09: `versionCode` 는 Gradle property `ghtradeVersionCode` 로만 들어오고(로컬 디버그 기본 1) `versionName` 은 `1.0` 그대로 — 릴리스 빌드 뒤 `build.gradle` 은 무변경이다"
    - "D-08 · D-06: 업로드 키스토어(PKCS12 · RSA 4096 · alias `ghtrade-upload`)는 사용자 `!` 실행으로 `~/.config/gh-trade/release/ghtrade-upload.jks`(600)에 생기고 Secret Manager 에 1본 백업된다 — `keystore` 단계는 기존 키스토어가 있으면 덮어쓰지 않고 SKIP 한다(업로드 키 분실 = Google 재설정 요청)"
    - "트레이서 확장: `pnpm --filter @gh-radar/mobile run native:release:android:aab` 가 운영 sync → `PROD CONFIG OK` → 래퍼 → fastlane lane `build` → 업로드 키로 서명된 AAB → `AAB CHECK OK`(서명 SHA-1 = `GHTRADE_UPLOAD_SHA1` · jarsigner 검증 · 운영 URL · cleartext 없음)까지 끝난다 — 이 AAB 가 22-05 의 첫 수동 업로드 대상이다(D-10 · Pitfall 10)"
    - "Empty 엣지: `release-android.sh` 도 env 파일·키 누락 시 fastlane 전에 exit 3 · 키 이름과 주입 명령만 출력한다"
    - "V14: `check-release-hygiene.sh` 가 build.gradle 평문 비밀번호 · gitignore 샘플 · 추적 파일 비밀 패턴 · 비밀 디렉터리/파일 권한(700/600 — 내용 미열람) · 스크립트 셸 추적 모드 · README 비밀 패턴을 한 번에 검사해 `RELEASE HYGIENE OK` 를 낸다"
  artifacts:
    - path: mobile/fastlane/test/build_numbers_test.rb
      provides: "iOS·Android 빌드 번호 공식 · 연 경계 단조 · 상한 초과 거부 minitest"
      contains: "609270049"
    - path: mobile/android/app/build.gradle
      provides: "env 서명 · ghtradeVersionCode · 릴리스 가드"
      contains: "GHTRADE_UPLOAD_STORE_FILE"
    - path: mobile/android/fastlane/Fastfile
      provides: "lanes build · beta · validate · track"
      contains: "lane :build"
    - path: mobile/scripts/release-android.sh
      provides: "env 로드·이름 검증(exit 3) → fastlane {beta|build|validate|track} · check 모드 → check-aab"
    - path: mobile/scripts/check-aab.sh
      provides: "AAB 서명 SHA-1 · jarsigner · 운영 URL 검사 → AAB CHECK OK"
    - path: mobile/scripts/check-release-hygiene.sh
      provides: "정적 위생 검사 → RELEASE HYGIENE OK"
  key_links:
    - from: mobile/android/fastlane/Fastfile
      to: mobile/android/app/build.gradle
      via: "gradle(task: bundle, build_type: Release, properties: { ghtradeVersionCode => vc }) — 서명은 build.gradle 이 env 로 직접 읽는다"
      pattern: "ghtradeVersionCode"
    - from: mobile/package.json
      to: mobile/scripts/release-android.sh
      via: "native:release:android(:aab) = cap sync && node scripts/verify-prod-config.mjs && bash scripts/release-android.sh [build]"
      pattern: "native:release:android"
    - from: mobile/android/fastlane/Fastfile
      to: mobile/fastlane/build_numbers.rb
      via: "GhTradeBuildNumbers.android_version_code"
      pattern: "android_version_code"
  prohibitions:
    - statement: "MUST NOT 업로드 키스토어·비밀번호·SA JSON 값을 저장소 파일(build.gradle 기본값 포함)·문서·로그·명령줄 인자에 남긴다 — gradle 은 env 를 직접 읽고 fastlane 에는 서명 속성을 넘기지 않는다"
      status: resolved
      verification: test
    - statement: "MUST NOT 기존 업로드 키스토어를 재생성·덮어쓴다 — 업로드 키가 바뀌면 Play 가 업로드를 거부하고 Google 재설정이 필요하다"
      status: resolved
      verification: test
    - statement: "MUST NOT versionCode 를 build.gradle 에 써서 커밋한다(D-09)"
      status: resolved
      verification: test
---

<objective>
Android 릴리스 경로를 22-01 트레이서와 같은 사슬로 연다: 빌드 번호 공식(D-09a)을 테스트로 잠그고, `build.gradle` 을 env 서명 + `ghtradeVersionCode` + 릴리스 가드로 바꾸고, 사용자가 `!` 로 업로드 키스토어를 만들어 백업한 뒤, `native:release:android:aab` 가 업로드 키로 서명된 AAB 를 만들어 검사까지 통과시킨다(D-07 · D-08 · D-09 · D-10).

Purpose: Play 첫 업로드는 정책상 콘솔 수동이다(D-10 · Pitfall 9) — 그 AAB 도 파이프라인으로 만들어야 키·versionCode 규칙이 흐트러지지 않는다(Pitfall 10). weekly-wine 의 Android 경로는 한 번도 실행된 적이 없고 평문 비밀번호 기본값·versionCode 고정 결함이 있으므로 뼈대만 가져온다(RESEARCH 발견 2·3).
Output: 빌드 번호 테스트 · 서명/가드 gradle · 비밀 주입 단계 3개 · 위생 검사 · Android lane 4개 · 래퍼 · AAB 검사 · npm 스크립트 2개 · 업로드 키 서명 AAB 1개.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-CONTEXT.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-PATTERNS.md
@.planning/phases/22-gh-trade-ios-testflight-android-play/22-01-SUMMARY.md
@mobile/android/app/build.gradle
@mobile/scripts/setup-release-secrets.sh
@mobile/scripts/release-ios.sh

작업 루트는 메인 체크아웃 `/Users/alex/repos/gh-radar` 다. Gradle 은 JDK 21(Android Studio JBR) · AGP 8.13 · compile/target 36 그대로다. 첫 `bundleRelease` 는 `lintVitalAnalyzeRelease` 를 새로 돌린다 — 실패하면 lint 를 끄지 말고 원인을 고친다(Pitfall 14 · A8). Bash timeout 600000.

**비밀 취급 규칙:** 22-01 과 같다 — 실행기는 `~/.config/gh-trade/release/` 파일을 열거나 출력하지 않고 래퍼·검사 스크립트를 경로로만 호출한다. 비밀 쓰기(키스토어 생성·백업)는 사용자 `!` 실행이다.

<interfaces>
22-01 이 만든 것(소비):
```text
mobile/fastlane/build_numbers.rb   module GhTradeBuildNumbers; def self.ios_build_number(t = Time.now)
mobile/scripts/setup-release-secrets.sh  stages dir · asc · REL=${GHTRADE_RELEASE_DIR:-$HOME/.config/gh-trade/release} · 출력 OK/SKIP · 모르는 stage exit 2
mobile/scripts/release-ios.sh      exit 3 규약 · ${GHTRADE_RELEASE_ENV:-…/ios.env} · PATH=/opt/homebrew/opt/ruby/bin
mobile/Gemfile(.lock)              fastlane 2.240.x — android/ 하위에서도 bundle exec 가 상위 Gemfile 을 쓴다
```

이 플랜이 새로 만드는 계약:
```text
GhTradeBuildNumbers.android_version_code(t = Time.now) -> Integer   # (t.year-2020)*100_000_000 + t.strftime("%m%d%H%M").to_i
GhTradeBuildNumbers::ANDROID_VERSION_CODE_MAX = 2_100_000_000       # 넘으면 ArgumentError(메시지에 「상한」)
build.gradle  def ghtradeUploadStoreFile = System.getenv("GHTRADE_UPLOAD_STORE_FILE")
              versionCode ← project.findProperty("ghtradeVersionCode") (없으면 1) · versionName "1.0"
              signingConfigs.release ← env 4개(파일이 있을 때만) · buildTypes.release.signingConfig(파일이 있을 때만)
              gradle.taskGraph.whenReady: env 없고 bundleRelease/assembleRelease 가 그래프에 있으면 GradleException
setup-release-secrets.sh stages 추가: keystore · backup · play-sa
~/.config/gh-trade/release/android.env   GHTRADE_UPLOAD_STORE_FILE · GHTRADE_UPLOAD_STORE_PASSWORD · GHTRADE_UPLOAD_KEY_ALIAS · GHTRADE_UPLOAD_KEY_PASSWORD · GHTRADE_UPLOAD_SHA1 · GOOGLE_PLAY_JSON_KEY
android/fastlane/Appfile   json_key_file(ENV["GOOGLE_PLAY_JSON_KEY"]) · package_name("com.ghtrade.app")
android/fastlane/Fastfile  lane :build (AAB 만) · :beta (AAB → internal) · :validate (SA JSON 확인) · :track (internal versionCodes 출력)
release-android.sh [beta|build|validate|track|check]   env ${GHTRADE_RELEASE_ENV:-…/android.env} · exit 3 = 비밀 누락 · check = AAB 검사만
check-aab.sh [AAB]          기본 android/app/build/outputs/bundle/release/app-release.aab · 성공 「AAB CHECK OK sha1=…」
check-release-hygiene.sh    성공 「RELEASE HYGIENE OK」 · 실패 「RELEASE HYGIENE FAIL — …」 exit 1
npm native:release:android = cap sync && node scripts/verify-prod-config.mjs && bash scripts/release-android.sh
npm native:release:android:aab = cap sync && node scripts/verify-prod-config.mjs && bash scripts/release-android.sh build
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 빌드 번호 공식 테스트(D-09a) · build.gradle env 서명 + ghtradeVersionCode + 릴리스 가드 · 비밀 주입 단계 3개 · 위생 검사</name>
  <files>mobile/fastlane/build_numbers.rb, mobile/fastlane/test/build_numbers_test.rb, mobile/android/app/build.gradle, mobile/scripts/setup-release-secrets.sh, mobile/scripts/check-release-hygiene.sh</files>
  <read_first>
    - mobile/fastlane/build_numbers.rb · mobile/scripts/setup-release-secrets.sh (22-01 산출 — 규약 유지)
    - mobile/android/app/build.gradle (전체 — L7-12 defaultConfig · L20-25 buildTypes · 한글 근거 주석 관례 L26·L44)
    - mobile/scripts/verify-prod-config.mjs (L40-80 — 실패 누적 → 일괄 출력 · exit 1 규약)
    - scripts/dma-credentials.sh (L60-75 — gcloud 준비 · deployer 키 자동 지정 관례)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md (Pattern 2 · Pattern 5 · Pitfall 1 · 12 · Security Domain V6·V7)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-PATTERNS.md (`build.gradle` · `check-release-hygiene.sh` · `setup-release-secrets.sh` 절)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-CONTEXT.md (D-08 · D-09 · D-09a)
  </read_first>
  <behavior>
    - `android_version_code(Time.new(2026, 9, 27, 0, 49))` == 609270049
    - `android_version_code(Time.new(2040, 12, 31, 23, 59))` == 2012312359 이고 ≤ `ANDROID_VERSION_CODE_MAX`
    - 연 경계: `android_version_code(Time.new(2026, 12, 31, 23, 59))` (612312359) < `android_version_code(Time.new(2027, 1, 1, 0, 0))` (701010000)
    - `android_version_code(Time.new(2041, 1, 1, 0, 0))` 는 ArgumentError 이고 메시지에 「상한」
    - 1분 차이는 엄격히 증가 · 같은 분은 같은 값(엣지 FA-1 — 스토어가 거절)
    - `ios_build_number(Time.new(2026, 9, 27, 0, 49))` == "202609270049" · 연 경계에서도 정수 비교로 증가
    - `setup-release-secrets.sh keystore` 는 `$GHTRADE_RELEASE_DIR/ghtrade-upload.jks` 가 이미 있으면 「SKIP keystore」 를 내고 그 파일을 바꾸지 않는다 · 모르는 stage 는 exit 2
  </behavior>
  <action>
    ① 테스트 먼저(RED): `mobile/fastlane/test/build_numbers_test.rb` — `require "minitest/autorun"` · `require_relative "../build_numbers"` · behavior 의 빌드 번호 7건을 단언한다(Homebrew Ruby 4 의 번들 minitest 6 — Gemfile 에 추가하지 않는다). 파일 머리 한국어 주석: D-09a 정정(원문 `YYMMDDHHmm` 은 2026년에 26억대라 Play 상한 초과 — RESEARCH Pitfall 1) · 깨지면 겪는 일(두 번째 업로드 거절 · 연초 첫 빌드가 연말 빌드보다 작아 거절).
    ② `mobile/fastlane/build_numbers.rb`(GREEN): `ANDROID_VERSION_CODE_MAX = 2_100_000_000` 상수와 `def self.android_version_code(t = Time.now)` — `(t.year - 2020) * 100_000_000 + t.strftime("%m%d%H%M").to_i`, 결과가 상한을 넘으면 `ArgumentError`(메시지 「versionCode 상한 2,100,000,000 초과 — 2040년 이후 공식 교체 필요」). 기존 `ios_build_number` 는 그대로.
    ③ `mobile/android/app/build.gradle`(RESEARCH Pattern 2 코드가 정본): 파일 위쪽(`apply plugin` 줄 다음)에 `def ghtradeUploadStoreFile = System.getenv("GHTRADE_UPLOAD_STORE_FILE")`. `defaultConfig` 의 `versionCode 1` 을 `versionCode((project.findProperty("ghtradeVersionCode") ?: "1") as Integer)` 로 바꾸고 한 줄 주석 「D-09 · D-09a — fastlane 이 -PghtradeVersionCode 로 넘긴다 · 로컬 디버그는 1 · 이 파일에 번호를 쓰지 않는다」. `versionName "1.0"` 은 그대로. `android { … }` 안 `buildTypes` 앞에 `signingConfigs { release { if (ghtradeUploadStoreFile) { storeFile file(ghtradeUploadStoreFile); storePassword System.getenv("GHTRADE_UPLOAD_STORE_PASSWORD"); keyAlias System.getenv("GHTRADE_UPLOAD_KEY_ALIAS"); keyPassword System.getenv("GHTRADE_UPLOAD_KEY_PASSWORD") } } }` (실제 파일에서는 여러 줄로). `buildTypes.release` 에 `if (ghtradeUploadStoreFile) signingConfig signingConfigs.release` 를 더하고 `minifyEnabled false` · `proguardFiles` 는 그대로. 파일 끝(google-services 블록 앞)에 `gradle.taskGraph.whenReady { graph -> … }` 가드 — env 가 없고 그래프에 `bundleRelease` 또는 `assembleRelease` 가 있으면 `GradleException("GHTRADE_UPLOAD_STORE_FILE 없음 — 릴리스 AAB 는 native:release:android(:aab) 로만 만든다 (D-08)")`. 비밀번호에는 어떤 기본값도 두지 않는다(엘비스·삼항·문자열 리터럴 모두 금지 — weekly-wine 결함) · 주석에 D-08 · D-07 근거 한 줄씩.
    ④ `mobile/scripts/setup-release-secrets.sh` 에 stage 3개를 더하고 사용법 문구를 5개 stage 로 고친다(22-01 규약: 대화형 입력 없음 · 값 비출력 · 기존 파일 SKIP · `umask 077`). gcloud 준비는 dma-credentials.sh 관례: `CLOUDSDK_CORE_PROJECT` 기본 `gh-radar`, `CLOUDSDK_AUTH_CREDENTIAL_FILE_OVERRIDE` 가 비어 있고 `~/.config/gcloud/gh-radar-deployer.json` 이 있으면 그 경로로 export. (a) `keystore`: `$REL/ghtrade-upload.jks` 가 있으면 「SKIP keystore — 이미 있음(업로드 키는 재생성하지 않는다)」 · jks 는 없는데 `$REL/android.env` 가 있으면 「ERROR keystore — android.env 만 있고 키스토어가 없다 — 수동 확인」 exit 1 · 둘 다 없으면 비밀번호를 `openssl rand -base64 32` 로 만들어 `GHTRADE_UPLOAD_STORE_PASSWORD`·`GHTRADE_UPLOAD_KEY_PASSWORD` 두 변수에 같은 값으로 export(PKCS12 규칙) → `keytool -genkeypair -keystore "$REL/ghtrade-upload.jks" -storetype PKCS12 -alias ghtrade-upload -keyalg RSA -keysize 4096 -validity 10000 -dname "CN=GH Trade, C=KR" -storepass:env GHTRADE_UPLOAD_STORE_PASSWORD -keypass:env GHTRADE_UPLOAD_KEY_PASSWORD -noprompt` (출력은 /dev/null · `:env` 형식이라 명령줄에 비밀 없음) → `chmod 600` → `keytool -list -v … -storepass:env …` 출력에서 `SHA1:` 줄만 파싱 → `android.env`(600)에 6개 키(`GHTRADE_UPLOAD_STORE_FILE` · `GHTRADE_UPLOAD_STORE_PASSWORD` · `GHTRADE_UPLOAD_KEY_ALIAS=ghtrade-upload` · `GHTRADE_UPLOAD_KEY_PASSWORD` · `GHTRADE_UPLOAD_SHA1` · `GOOGLE_PLAY_JSON_KEY=$REL/play-service-account.json`)를 `printf '%s=%s\n'` 로 파일에만 쓴다 → 화면에는 「OK keystore」 와 「업로드 인증서 SHA-1: {지문}」(공개 값)만. (b) `backup`: 세 비밀 `gh-radar-ghtrade-upload-keystore`(jks 파일) · `gh-radar-ghtrade-upload-keystore-password`(android.env 에서 읽은 값을 umask 077 임시 파일에 써서 `--data-file` 로 넘긴 뒤 즉시 삭제) · `gh-radar-ghtrade-asc-api-key`(`AuthKey_*.p8` 이 있으면)를 각각 `gcloud secrets describe` 로 확인해 있으면 「SKIP backup {이름}」, 없으면 `gcloud secrets create {이름} --replication-policy=automatic --data-file=…` → 「OK backup {이름}」. gcloud 실패 시 gcloud stderr 와 「권한이 없으면 `gcloud auth login` 사용자 계정으로 다시 실행」 안내만. (c) `play-sa`: `gcloud services enable androidpublisher.googleapis.com` → SA `gh-radar-play-publisher`(표시 이름 `GH Trade Play publisher`)가 없으면 만들고 → `$REL/play-service-account.json` 이 없을 때만 `gcloud iam service-accounts keys create` → `chmod 600` → 화면에는 「OK play-sa」 와 SA 이메일만. GCP 프로젝트 IAM 역할은 주지 않는다(Play 권한은 Play Console 에서 앱 단위로 준다 — 최소 권한).
    ⑤ `mobile/scripts/check-release-hygiene.sh`(analog verify-prod-config.mjs 의 실패 누적 규약 · bash 3.2 호환 문자열 누적): `cd "$(dirname "$0")/.."`. 검사 (1) build.gradle 의 `storePassword`·`keyPassword` 줄에 따옴표 리터럴이나 기본값 연산자가 없고 `System.getenv("GHTRADE_UPLOAD_STORE_PASSWORD")` 가 있다 (2) `git check-ignore -q` 샘플 목록(22-01 acceptance 의 16개와 같은 목록) (3) `git ls-files --` 비밀·산출물 pathspec 이 빈 출력 (4) `$REL` 이 있으면 권한 700, 그 안의 `ios.env` · `android.env` · `AuthKey_*.p8` · `ghtrade-upload.jks` · `play-service-account.json` 중 **있는 것만** `stat -f '%Lp'` 로 600 인지 — 내용은 열지 않는다(없는 파일은 「아직 주입 전」 안내 줄만, 실패 아님) (5) `mobile/scripts/*.sh` 에 셸 추적 모드를 켜는 줄이 없다 — 패턴은 `'^[[:space:]]*set[[:space:]]+-[a-zA-Z]*x'` 처럼 문자 클래스로 써서 이 검사 스크립트 자신이 그 문자열을 한 덩어리로 담지 않게 한다(acceptance 의 음성 grep 이 이 파일도 센다) (6) 두 Fastfile 에 프로젝트 파일에 빌드 번호를 쓰는 fastlane 액션(`increment_build_number` · `increment_version_code`)이 없다 (7) `mobile/README.md` 에 `PASSWORD=` · `BEGIN PRIVATE KEY` · `"private_key"` 가 없다. 통과면 `RELEASE HYGIENE OK`, 아니면 `RELEASE HYGIENE FAIL — …` 줄들 + exit 1. `chmod +x`.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && /opt/homebrew/opt/ruby/bin/ruby mobile/fastlane/test/build_numbers_test.rb</automated>
    <fails_when>non-zero exit, or the output lacks "0 failures, 0 errors"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:android</automated>
    <fails_when>non-zero exit, or the output lacks "BUILD SUCCESSFUL"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/mobile/android && out="$(env -u GHTRADE_UPLOAD_STORE_FILE ./gradlew bundleRelease 2>&1)"; code=$?; printf '%s\n' "$out" | tail -20; test "$code" -ne 0 && printf '%s' "$out" | grep -q "GHTRADE_UPLOAD_STORE_FILE"</automated>
    <fails_when>non-zero exit — bundleRelease succeeded without the env, or the guard message naming GHTRADE_UPLOAD_STORE_FILE is missing</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && T="$(mktemp -d)" && chmod 700 "$T" && printf 'dummy' > "$T/ghtrade-upload.jks" && before="$(shasum "$T/ghtrade-upload.jks")" && out="$(GHTRADE_RELEASE_DIR="$T" bash mobile/scripts/setup-release-secrets.sh keystore 2>&1)"; printf '%s\n' "$out"; GHTRADE_RELEASE_DIR="$T" bash mobile/scripts/setup-release-secrets.sh no-such-stage >/dev/null 2>&1; bad=$?; after="$(shasum "$T/ghtrade-upload.jks")"; rm -rf "$T"; test "$before" = "$after" && printf '%s' "$out" | grep -q "SKIP keystore" && test "$bad" -eq 2</automated>
    <fails_when>non-zero exit — the existing keystore was modified, "SKIP keystore" was not printed, or an unknown stage did not exit 2</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && bash mobile/scripts/check-release-hygiene.sh</automated>
    <fails_when>non-zero exit, or the output lacks "RELEASE HYGIENE OK", or contains "RELEASE HYGIENE FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE "(storePassword|keyPassword) +['\"]" mobile/android/app/build.gradle` 가 0 · `grep -cE "getenv\([^)]*PASSWORD[^)]*\) *\?:" mobile/android/app/build.gradle` 가 0
    - `grep -c 'System.getenv("GHTRADE_UPLOAD_STORE_PASSWORD")' mobile/android/app/build.gradle` 가 1 · `grep -c "ghtradeVersionCode" mobile/android/app/build.gradle` 가 1 이상 · `grep -c 'versionName "1.0"' mobile/android/app/build.gradle` 가 1 · `grep -c "taskGraph.whenReady" mobile/android/app/build.gradle` 가 1
    - `grep -c "ANDROID_VERSION_CODE_MAX = 2_100_000_000" mobile/fastlane/build_numbers.rb` 가 1 · `grep -c "609270049" mobile/fastlane/test/build_numbers_test.rb` 가 1 이상
    - `grep -cE "read +-s|read +-p" mobile/scripts/setup-release-secrets.sh` 가 0 · `grep -c "storepass:env" mobile/scripts/setup-release-secrets.sh` 가 1 이상 · `grep -c "set -x" mobile/scripts/setup-release-secrets.sh mobile/scripts/check-release-hygiene.sh` 의 각 파일 값이 0
    - 다섯 `<automated>` 가 모두 통과한다
  </acceptance_criteria>
  <done>빌드 번호 공식이 테스트로 잠기고, gradle 은 env 로만 서명하며 디버그는 그대로, 비밀 주입 단계와 위생 검사가 준비됐다.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: 업로드 키스토어 생성 + Secret Manager 백업 — 사용자 `!` 한 줄</name>
  <read_first>
    - mobile/scripts/setup-release-secrets.sh (Task 1 산출 — keystore · backup 단계)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md (Pattern 5 · Security Domain 「업로드 키스토어·비밀번호 유출」)
  </read_first>
  <action>사용자가 비대화형·비출력 스크립트로 업로드 키스토어를 만들고 Secret Manager 에 백업한다(D-06 · D-08 · 메모리 「비밀 주입은 사용자 `!` 실행」). 실행기는 이 명령을 대신 실행하지 않는다.</action>
  <instructions>
    Claude 가 이미 한 것: `mobile/scripts/setup-release-secrets.sh` 에 `keystore` · `backup` 단계를 만들었다(기존 키스토어가 있으면 SKIP · 값은 화면에 나오지 않음 · 비밀번호는 `openssl rand` 로 생성 · keytool 은 `:env` 형식이라 명령줄에도 없음).

    사용자가 할 일 — Claude Code 입력창에 한 줄:
    `! bash mobile/scripts/setup-release-secrets.sh keystore backup`

    기대 출력: 「OK keystore」 · 「업로드 인증서 SHA-1: AA:BB:…」 · 「OK backup gh-radar-ghtrade-upload-keystore」 · 「OK backup gh-radar-ghtrade-upload-keystore-password」 · (ASC 키가 있으면) 「OK backup gh-radar-ghtrade-asc-api-key」. SHA-1 은 공개 지문이라 재개 신호에 붙여도 된다 — 22-06 GCP Android OAuth 클라이언트 등록에 쓴다. gcloud 권한 오류가 나면 `gcloud auth login` 으로 사용자 계정을 쓴 뒤 `! bash mobile/scripts/setup-release-secrets.sh backup` 만 다시 실행한다.
  </instructions>
  <verification>재개 뒤 Claude 가 아래 두 명령으로 확인한다 — 권한(700/600)과 백업 존재(메타데이터만, 값 미열람).</verification>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash mobile/scripts/check-release-hygiene.sh && test -n "$(CLOUDSDK_CORE_PROJECT=gh-radar CLOUDSDK_AUTH_CREDENTIAL_FILE_OVERRIDE="$HOME/.config/gcloud/gh-radar-deployer.json" gcloud secrets describe gh-radar-ghtrade-upload-keystore --format='value(name)' 2>/dev/null)"</automated>
    <fails_when>non-zero exit, or the output lacks "RELEASE HYGIENE OK" (a secret file is not 600 / dir not 700), or the Secret Manager backup does not exist</fails_when>
  </verify>
  <acceptance_criteria>
    - 재개 신호가 `done sha1={업로드 인증서 SHA-1}` 형태이고 그 SHA-1 이 SUMMARY 「결정」 절에 기록된다(공개 지문 · 비밀번호는 어디에도 없음)
    - verify 가 통과한다(`RELEASE HYGIENE OK` · 백업 존재)
  </acceptance_criteria>
  <resume-signal>`done sha1=AA:BB:…` 로 답한다(스크립트 출력에 ERROR 가 있으면 그 줄을 그대로 붙인다)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Android lane(build · beta · validate · track) + 래퍼 + AAB 검사 + `native:release:android(:aab)` → 업로드 키 서명 AAB</name>
  <precondition>Task 2 재개 신호에 업로드 인증서 SHA-1 이 있다 · `bash mobile/scripts/check-release-hygiene.sh` 가 `RELEASE HYGIENE OK` 다</precondition>
  <files>mobile/android/fastlane/Fastfile, mobile/android/fastlane/Appfile, mobile/scripts/release-android.sh, mobile/scripts/check-aab.sh, mobile/package.json</files>
  <read_first>
    - /Users/alex/repos/weekly-wine-app/android/fastlane/Fastfile · Appfile (미추적 · 미검증 원형 — 뼈대만, 저장소 안 기본 SA 경로와 versionCode 미주입은 따르지 않는다)
    - mobile/ios/App/fastlane/Fastfile · mobile/scripts/release-ios.sh · mobile/scripts/check-ipa.sh (22-01 — 같은 구조·exit 3 규약·출력 규약으로 맞춘다)
    - mobile/package.json (scripts)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-RESEARCH.md (Pattern 3 · Pattern 4 · Pitfall 9 · 10 · 14 · Code Examples 「AAB 검사」)
    - .planning/phases/22-gh-trade-ios-testflight-android-play/22-PATTERNS.md (Android Fastfile · Appfile · release-*.sh · check-aab 절)
  </read_first>
  <action>
    ① `mobile/android/fastlane/Appfile`: `json_key_file(ENV["GOOGLE_PLAY_JSON_KEY"])` · `package_name("com.ghtrade.app")` — 저장소 안 기본 경로를 두지 않는다(SA JSON 은 저장소 밖에만).
    ② `mobile/android/fastlane/Fastfile`(RESEARCH Pattern 3 코드가 정본): 22-01 과 같은 방식으로 `mobile/fastlane/build_numbers.rb` 를 절대 경로 require(`File.expand_path("../../fastlane/build_numbers.rb", __dir__)`). `default_platform(:android)` · `platform :android do`. private 헬퍼 `ghtrade_build_release_aab`: `GHTRADE_UPLOAD_STORE_FILE` · `GHTRADE_UPLOAD_STORE_PASSWORD` · `GHTRADE_UPLOAD_KEY_ALIAS` · `GHTRADE_UPLOAD_KEY_PASSWORD` 중 빈 것이 있으면 `UI.user_error!` 에 이름만 → `vc = GhTradeBuildNumbers.android_version_code` → `UI.message("versionCode #{vc}")` → `gradle(project_dir: ".", task: "bundle", build_type: "Release", properties: { "ghtradeVersionCode" => vc })` — **서명 관련 gradle 속성은 넘기지 않는다**(기본 `print_command` 가 명령줄을 로그에 찍는다 · 서명은 build.gradle 이 env 로 직접 읽는다) → `lane_context[SharedValues::GRADLE_AAB_OUTPUT_PATH]` 반환. `lane :build` = 헬퍼 호출 뒤 `UI.success("AAB #{path} versionCode #{vc}")`(첫 수동 업로드용 — D-10). `lane :beta` = 헬퍼 → `upload_to_play_store(track: "internal", aab:, release_status: ENV.fetch("PLAY_RELEASE_STATUS", "completed"), skip_upload_metadata: true, skip_upload_images: true, skip_upload_screenshots: true, skip_upload_changelogs: true)` (「Only releases with status draft may be created on draft app」 이면 `PLAY_RELEASE_STATUS=draft` 로 재실행하고 콘솔에서 출시 — Pitfall 9, 주석 한 줄). `lane :validate` = `validate_play_store_json_key(json_key: ENV["GOOGLE_PLAY_JSON_KEY"])`. `lane :track` = `codes = google_play_track_version_codes(track: "internal")` → `UI.message("internal versionCodes: #{codes.join(',')}")`.
    ③ `mobile/scripts/release-android.sh`(22-01 `release-ios.sh` 와 같은 규약): `MODE="${1:-beta}"` — `beta|build|validate|track|check` 외에는 env 로드 **전에** 사용법 + exit 2. `ENV_FILE="${GHTRADE_RELEASE_ENV:-$HOME/.config/gh-trade/release/android.env}"` 없음 → 경로와 「주입: ! bash mobile/scripts/setup-release-secrets.sh keystore」 → exit 3. source 뒤 필수 키: `build`·`beta`·`check` → 업로드 4개 + `GHTRADE_UPLOAD_SHA1` · `beta`·`validate`·`track` → `GOOGLE_PLAY_JSON_KEY` 도(파일이 없으면 「주입: ! bash mobile/scripts/setup-release-secrets.sh play-sa」) — 빈 키는 이름만, exit 3. `check` 는 fastlane 없이 `bash scripts/check-aab.sh` 만. 나머지는 `export PATH=/opt/homebrew/opt/ruby/bin:$PATH` → `(cd android && bundle exec fastlane "$MODE")` → `build`·`beta` 면 `bash scripts/check-aab.sh`. 셸 추적 모드 금지 · 헤더에 「두 릴리스 명령 동시 실행 금지(엣지 FA-5)」 · 「같은 분 재실행은 같은 versionCode — 1분 뒤(엣지 FA-1)」. `chmod +x`.
    ④ `mobile/scripts/check-aab.sh`(analog check-ipa.sh · RESEARCH Code Examples 「AAB 검사」): `AAB="${1:-android/app/build/outputs/bundle/release/app-release.aab}"`. 실패 누적 → `AAB CHECK FAIL — …` + exit 1 / 통과 `AAB CHECK OK sha1={지문}`. 검사: 파일 존재 · `keytool -printcert -jarfile` 의 `SHA1:` 값이 `$GHTRADE_UPLOAD_SHA1` 과 대소문자 무시 일치(Play 는 업로드 키 서명만 받는다) · `jarsigner -verify` 통과 · `unzip -p "$AAB" base/assets/capacitor.config.json` 의 `server.url` == `https://trade.jx1.io` · `server.cleartext` 가 true 아님 · `android.webContentsDebuggingEnabled` 가 true 아님. `chmod +x`.
    ⑤ `mobile/package.json` scripts: `"native:release:android": "cap sync && node scripts/verify-prod-config.mjs && bash scripts/release-android.sh"` · `"native:release:android:aab": "cap sync && node scripts/verify-prod-config.mjs && bash scripts/release-android.sh build"`. `build`·`typecheck`·`test`·`lint` 키는 두지 않는다.
    ⑥ 실행: Empty 엣지(verify 1) → `pnpm --filter @gh-radar/mobile run native:release:android:aab`. lint 실패(Pitfall 14)는 lint 설정을 끄지 말고 원인을 고친다 — 고친 파일이 이 태스크 `files` 밖이면 SUMMARY 「Deviations」 에 적는다. 성공 뒤 SUMMARY 에 versionCode · AAB 경로 · 서명 SHA-1(공개)을 적는다 — 22-05 가 이 AAB 를 콘솔에 올린다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && out="$(GHTRADE_RELEASE_ENV=/nonexistent/gh-trade-missing-vars bash mobile/scripts/release-android.sh build 2>&1)"; code=$?; printf '%s\n' "$out"; test "$code" -eq 3 && printf '%s' "$out" | grep -q "setup-release-secrets.sh keystore" && ! printf '%s' "$out" | grep -q "Driving the lane"</automated>
    <fails_when>non-zero exit — the wrapper did not stop with exit 3 before fastlane, or the injection command is missing</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:release:android:aab</automated>
    <fails_when>non-zero exit, or the output lacks "PROD CONFIG OK", "BUILD SUCCESSFUL" or "AAB CHECK OK", or contains "AAB CHECK FAIL"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && git diff --quiet -- mobile/android/app/build.gradle mobile/ios/App/App/Info.plist mobile/ios/App/App.xcodeproj/project.pbxproj && bash mobile/scripts/check-release-hygiene.sh</automated>
    <fails_when>non-zero exit — the release build rewrote build.gradle/Info.plist/pbxproj, or the output lacks "RELEASE HYGIENE OK"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "android.injected.signing" mobile/android/fastlane/Fastfile` 가 0 · `grep -c '"ghtradeVersionCode" => vc' mobile/android/fastlane/Fastfile` 가 1 · `grep -cE "lane :(build|beta|validate|track)" mobile/android/fastlane/Fastfile` 가 4 · `grep -c 'track: "internal"' mobile/android/fastlane/Fastfile` 가 1 이상
    - `grep -c 'json_key_file(ENV\["GOOGLE_PLAY_JSON_KEY"\])' mobile/android/fastlane/Appfile` 가 1 · `grep -c "fastlane/" mobile/android/fastlane/Appfile` 가 0 (저장소 안 기본 경로 없음)
    - `node -e "const s=require('./mobile/package.json').scripts;process.exit(s['native:release:android']==='cap sync && node scripts/verify-prod-config.mjs && bash scripts/release-android.sh'&&s['native:release:android:aab']==='cap sync && node scripts/verify-prod-config.mjs && bash scripts/release-android.sh build'&&!['build','typecheck','test','lint'].some(k=>k in s)?0:1)"` 가 exit 0
    - `test -f mobile/android/app/build/outputs/bundle/release/app-release.aab` 가 exit 0 이고 `git check-ignore -q mobile/android/app/build/outputs/bundle/release/app-release.aab` 가 exit 0
    - 세 `<automated>` 가 통과하고 SUMMARY 에 versionCode(9자리 · 6억대) · AAB 경로 · 서명 SHA-1 이 있다
  </acceptance_criteria>
  <done>운영 게이트를 통과한 AAB 가 업로드 키로 서명돼 있고, 저장소 파일은 그대로다 — Play 콘솔 첫 업로드 준비 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 `!` 스크립트 → 저장소 밖 비밀 · Secret Manager | 업로드 키·비밀번호가 생성·백업된다 |
| env → Gradle 서명 · fastlane 로그 | 비밀이 명령줄·로그로 새는 경로 |
| 로컬 AAB → Play(22-05 · 22-06) | 서명·설정 오류가 테스터에게 그대로 간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Information Disclosure | 업로드 키스토어·비밀번호의 저장소 유입 | high | mitigate | 저장소 밖 600 파일 · build.gradle 기본값·리터럴 0(acceptance 음성 grep 2개 · hygiene (1)) · `*.jks`·`key.properties` ignore · 추적 파일 비밀 패턴 0(hygiene (3)) |
| T-22-02 | Information Disclosure | 비밀번호가 로그·명령줄·셸 히스토리에 남음 | high | mitigate | keytool `-storepass:env`/`-keypass:env` · 비밀번호는 `openssl rand` 로 스크립트 안에서만 생성 · fastlane 에 서명 속성 미전달(음성 grep) · 래퍼 이름만 출력 · 셸 추적 모드 0 |
| T-22-04 | Tampering / Information Disclosure | dev URL·cleartext·WebView 디버깅이 켜진 AAB | high | mitigate | `cap sync`(양쪽) → verify-prod → `check-aab.sh`(운영 URL · cleartext · webContentsDebuggingEnabled) |
| T-22-07 | Tampering (무결성) | versionCode 가 build.gradle 에 기록 | low | mitigate | Gradle property 주입만 · verify 3(build.gradle 무변경) · hygiene (6) |
| T-22-11 | Denial of Service (가용성) | 업로드 키스토어 분실·덮어쓰기 | medium | mitigate | `keystore` 단계 SKIP(verify 4 가 더미로 증명) · Secret Manager 백업(Task 2 verify) · 분실 시 Play 「업로드 키 재설정」(Google 이 앱 서명 키 보관 — D-08) |
| T-22-13 | Information Disclosure | Secret Manager 백업 접근 | low | accept | 프로젝트 IAM 은 기존 운영자·deployer SA 만 — 새 IAM 부여 없음 · 백업은 암호화 저장 |
| T-22-10 | Elevation of Privilege | Play 게시용 SA 과권한 | medium | mitigate | GCP 프로젝트 역할 0(스크립트가 부여하지 않음) · Play 앱 단위 권한은 22-06 에서 「테스트 트랙 출시 · 앱 정보 보기」만 |
</threat_model>

<verification>
- build_numbers minitest green · `native:build:android` 통과(디버그 무영향) · env 없는 `bundleRelease` 가드 실패 · keystore SKIP 더미 테스트 · `RELEASE HYGIENE OK`
- 사용자 `!` keystore·backup 뒤 권한·백업 확인 · `native:release:android:aab` → `AAB CHECK OK` · 저장소 파일 무변경
</verification>

<success_criteria>
- 업로드 키로 서명된 운영 설정 AAB 가 있고, 그 키는 저장소 밖과 Secret Manager 에만 있다 — 22-05 가 이 AAB 를 Play 에 처음 올린다.
</success_criteria>

## Flagged assumptions (엣지 프로브 — 22-01 표의 Android 쪽 적용)

- FA-1 adjacency: 같은 분 재실행은 같은 versionCode → Play 거절(Task 1 behavior 가 「같은 분은 같은 값」을 명시적으로 단언해 가정을 드러낸다). 연 경계는 단조(테스트).
- FA-2 empty: env 없음·키 비어 있음 → exit 3 · 이름만(verify 1).
- FA-4 idempotency: `keystore` 재실행은 SKIP(verify 4) · `backup`·`play-sa` 도 존재 시 SKIP.
- FA-5 concurrency: iOS·Android 릴리스 명령 동시 실행 금지(래퍼 헤더 · 22-06 README).

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/Gemfile` · `mobile/Gemfile.lock` (fastlane `~> 2.240`) | Ruby 의존성 | 22-01 |
| `mobile/fastlane/build_numbers.rb` — `GhTradeBuildNumbers.ios_build_number(t)` (22-01) · `GhTradeBuildNumbers.android_version_code(t)` · `ANDROID_VERSION_CODE_MAX` (22-04) | 빌드 번호 함수 | 22-01 · **22-04** |
| `mobile/fastlane/test/build_numbers_test.rb` (Ruby minitest) | 단위 테스트 | **22-04** |
| `mobile/ios/App/fastlane/Fastfile` lane `beta` (22-01) · lane `latest` (22-06) · `mobile/ios/App/fastlane/Appfile` | fastlane | 22-01 · 22-06 |
| `mobile/android/fastlane/Fastfile` lanes `build` · `beta` · `validate` · `track` · `mobile/android/fastlane/Appfile` | fastlane | **22-04** |
| `mobile/scripts/setup-release-secrets.sh` — stages `dir` · `asc` (22-01) · `keystore` · `backup` · `play-sa` (22-04) | 사용자 `!` 비밀 주입 | 22-01 · **22-04** |
| `mobile/scripts/release-ios.sh` — 인자 `beta`(기본) · `latest`(22-06) · exit 3 = 비밀 env 누락 | 래퍼 | 22-01 · 22-06 |
| `mobile/scripts/release-android.sh` — 인자 `beta`(기본) · `build` · `validate` · `track` · `check` | 래퍼 | **22-04** |
| `mobile/scripts/check-ipa.sh` (`IPA CHECK OK`) · `mobile/scripts/check-aab.sh` (`AAB CHECK OK`) · `mobile/scripts/check-release-hygiene.sh` (`RELEASE HYGIENE OK`) | 검사 스크립트 | 22-01 · **22-04** |
| npm scripts `native:release:ios` (22-01) · `native:release:android` · `native:release:android:aab` (22-04) | 스크립트 | 22-01 · **22-04** |
| Gradle property `ghtradeVersionCode` · release `signingConfigs` ← env `GHTRADE_UPLOAD_STORE_FILE` · `GHTRADE_UPLOAD_STORE_PASSWORD` · `GHTRADE_UPLOAD_KEY_ALIAS` · `GHTRADE_UPLOAD_KEY_PASSWORD` · `gradle.taskGraph.whenReady` 릴리스 가드 | Android 빌드 설정 | **22-04** |
| env 이름: `ASC_KEY_ID` · `ASC_ISSUER_ID` · `ASC_KEY_PATH` · `GHTRADE_RELEASE_OUT` (ios.env) · `GHTRADE_UPLOAD_*` · `GHTRADE_UPLOAD_SHA1` · `GOOGLE_PLAY_JSON_KEY` (android.env) · `PLAY_RELEASE_STATUS` · `GHTRADE_RELEASE_ENV` · `GHTRADE_RELEASE_DIR` · `WEEKLY_WINE_FASTLANE_DIR` (선택 override) | env 이름(값은 저장소 밖) | 22-01 · **22-04** |
| 저장소 밖 `~/.config/gh-trade/release/` (700) — `ios.env` · `AuthKey_{KEYID}.p8` · `android.env` · `ghtrade-upload.jks` · `play-service-account.json` (600) · 산출 경로 `~/Library/Developer/gh-trade-release/ios` | 비밀·산출물(저장소 밖) | 22-01 · **22-04** · 22-06 |
| GCP Secret Manager `gh-radar-ghtrade-upload-keystore` · `gh-radar-ghtrade-upload-keystore-password` · `gh-radar-ghtrade-asc-api-key` · GCP SA `gh-radar-play-publisher` · API `androidpublisher.googleapis.com` | 외부(사용자 `!`) | **22-04** · 22-06 |
| iOS `Info.plist` `ITSAppUsesNonExemptEncryption` = false | 설정 | 22-01 |
| `mobile/.gitignore` 릴리스 비밀·산출물 패턴 | 설정 | 22-01 |
| `.planning/phases/22-gh-trade-ios-testflight-android-play/22-PRIVACY-DRAFT.md` (승인된 한글 초안) | 계획 문서 | 22-02 |
| 웹 `webapp/src/lib/supabase/public-path.ts` — `PUBLIC_PREFIXES` · `isPublicPath` | 유틸 | 22-03 |
| 웹 라우트 `/privacy` — `webapp/src/app/privacy/page.tsx` (`metadata.title` 「개인정보처리방침 · GH Trade」) | RSC 페이지 | 22-03 |
| 테스트: `webapp/src/lib/supabase/__tests__/public-path.test.ts` · `webapp/src/app/privacy/__tests__/page.test.tsx` · `webapp/e2e/specs/auth-guards.spec.ts` `/privacy` 2케이스 | 단위 · e2e | 22-03 |
| `mobile/README.md` 「릴리스 (Phase 22)」 절 · 테스터 안내 · 「비밀 파일」 · 범위 밖 갱신 | 문서 | 22-06 |
| `webapp/src/lib/native/google-client-ids.ts` 주석 갱신(상수 값 불변) | 주석 | 22-06 |
| 콘솔: ASC 앱 레코드 · TestFlight 내부 그룹 `GH Trade 테스터`(자동 배포) · Play 앱 · 내부 테스트 트랙 · GCP Android OAuth 클라이언트(업로드 키 · Play 앱 서명 인증서마다) | 외부 상태 | 22-01 · 22-05 · 22-06 |

<output>
Create `.planning/phases/22-gh-trade-ios-testflight-android-play/22-04-SUMMARY.md` when done — 「결정」 절에 업로드 인증서 SHA-1 · 첫 AAB versionCode · AAB 경로 · lint 수정 여부 · gcloud 인증 경로(deployer / 사용자)를 적는다. 비밀번호·키 내용은 적지 않는다.
</output>
