---
phase: 09.2
plan: 03
type: execute
wave: 3
depends_on:
  - "09.2-01"
  - "09.2-02"
files_modified:
  - webapp/src/components/stock/stock-detail-client.tsx
  - webapp/src/app/mockups
  - webapp/e2e/specs/stock-detail-chart.spec.ts
  - .planning/STATE.md
autonomous: false
requirements:
  - DATA-03
user_setup: []

must_haves:
  truths:
    - "/stocks/[code] 페이지 렌더 시 Hero ↓ 직후 / StatsGrid ↑ 직전에 일봉 차트 카드가 표시된다 (D-09)"
    - "StockDetailClient 가 StockDailyChartSection 을 단일 child 로 마운트하며, code + refreshSignal={isRefreshing} 두 prop 을 전달한다 (D-19 — 새로고침 버튼 클릭이 차트 fetch 도 트리거)"
    - "RESEARCH 단계의 webapp/src/app/mockups 디렉터리가 production 코드베이스에서 완전히 제거된다 (라이브러리 선정 완료 후 보관 가치 = git history 충분)"
    - "Playwright E2E spec 가 /stocks/005930 진입 → 차트 섹션 (data-testid=stock-daily-chart-section) 노출 + 차트 캔버스 컨테이너 (data-testid=stock-daily-chart-canvas) 노출 흐름을 검증한다 (DATA-03 회귀 방지, B3)"
    - "기존 stock-detail.spec.ts 가 차트 섹션 추가 후에도 회귀 없이 PASS 한다"
    - "STATE.md 의 Pending Todos 또는 Blockers 섹션에 (a) news_articles/discussions/summaries RLS audit 권고 (b) Vercel env trailing newline 검증 follow-up 두 건이 명시 기록된다 (RESEARCH Post-Deploy 디버그 #10/#11 의 별도 phase 위임)"
    - "사용자가 production-grade 차트 섹션을 시각적으로 검증하고 다크/라이트 토글 + 모바일 캔들 폭 + range 토글 4종을 모두 승인한다 (Manual Verification — VALIDATION.md)"
  artifacts:
    - path: "webapp/src/components/stock/stock-detail-client.tsx"
      provides: "StockDailyChartSection mount — Hero ↓ refresh row ↓ StockDailyChartSection ↓ StatsGrid 순서"
      contains: "StockDailyChartSection"
    - path: "webapp/e2e/specs/stock-detail-chart.spec.ts"
      provides: "DATA-03 회귀 방지 E2E spec"
    - path: ".planning/STATE.md"
      provides: "Pitfall 10 (RLS authenticated) + Pitfall 11 (Vercel env newline) follow-up 기록"
      contains: "RLS authenticated audit"
  key_links:
    - from: "webapp/src/components/stock/stock-detail-client.tsx"
      to: "@/components/stock/stock-daily-chart-section"
      via: "import + JSX mount"
      pattern: "StockDailyChartSection"
    - from: "webapp/e2e/specs/stock-detail-chart.spec.ts"
      to: "stock-detail-section testid"
      via: "data-testid='stock-daily-chart-section'"
      pattern: "stock-daily-chart-section"
---

<objective>
Plan 02 의 자기완결 차트 섹션을 종목 상세 페이지에 **mount** 하고, RESEARCH 단계 산출물 (mockups 디렉터리) 을 정리하며, Post-Deploy 디버그에서 발견된 RLS audit / Vercel env 함정을 STATE.md 의 follow-up 으로 기록한다. 마지막으로 사용자 시각 검증 checkpoint 로 다크/라이트 토글 + 모바일 가독성 + 4종 range 토글의 production 동작을 확인한다.

Purpose: 코드 변경은 최소 (StockDetailClient 한 곳만) — 본 plan 의 가치는 (1) 사용자 가시 phase 산출물의 통합 (2) RESEARCH 임시 산출물의 정리 (3) Post-Deploy 함정의 follow-up 기록 (4) Manual Verification 으로 phase 완료 게이트.
Output: StockDetailClient 1줄 mount + mockups 디렉터리 삭제 + 새 E2E spec + STATE.md follow-up 2건 + 사용자 시각 검증 승인.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.2-stock-detail-daily-chart/09.2-CONTEXT.md
@.planning/phases/09.2-stock-detail-daily-chart/09.2-RESEARCH.md
@.planning/phases/09.2-stock-detail-daily-chart/09.2-VALIDATION.md
@.planning/phases/09.2-stock-detail-daily-chart/09.2-01-PLAN.md
@.planning/phases/09.2-stock-detail-daily-chart/09.2-02-PLAN.md

@webapp/src/components/stock/stock-detail-client.tsx
@webapp/e2e/specs/stock-detail.spec.ts
@webapp/playwright.config.ts

<interfaces>
<!-- Plan 02 산출물 — 본 plan 이 import -->

import { StockDailyChartSection } from '@/components/stock/stock-daily-chart-section';

interface StockDailyChartSectionProps {
  code: string;
}
// data-testid="stock-daily-chart-section" — E2E 가 이 selector 로 검증
// data-testid="stock-daily-chart-canvas"  — 차트 컨테이너 div selector
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StockDetailClient 에 StockDailyChartSection mount (D-09 위치 lock-in)</name>
  <files>webapp/src/components/stock/stock-detail-client.tsx</files>
  <read_first>
    - webapp/src/components/stock/stock-detail-client.tsx (전체 — 특히 113행 StockHero ↓ / 114행 refresh row / 134행 StockStatsGrid 사이가 mount 지점)
    - .planning/phases/09.2-stock-detail-daily-chart/09.2-CONTEXT.md §D-09 ("Hero ↓ / StatsGrid ↑")
    - .planning/phases/09.2-stock-detail-daily-chart/09.2-02-PLAN.md (StockDailyChartSection 시그너처)
  </read_first>
  <action>
    1. webapp/src/components/stock/stock-detail-client.tsx 의 import 블록 (1~14행) 에 한 줄 추가:
       ```typescript
       import { StockDailyChartSection } from './stock-daily-chart-section';
       ```

    2. **삽입 위치:** webapp/src/components/stock/stock-detail-client.tsx 의 `<StockStatsGrid stock={stock} />` JSX **직전** (현 시점 134행 — 본 작업 시점에 Read 로 재확인 후 정확 line 적용. 134행 위로 한 줄을 삽입하므로 이후 라인은 +1 시프트).

       **삽입 코드 (정확히 한 줄):**
       ```tsx
       <StockDailyChartSection code={stock.code} refreshSignal={isRefreshing} />
       ```
       - `refreshSignal={isRefreshing}` — D-19 lock-in. StockDetailClient 의 `isRefreshing` state (41행, 새로고침 버튼 클릭 시 true → fetchStockDetail 완료 후 false 로 토글) 가 차트의 useEffect 의존성에 들어가 동일 range 로 refetch. (대안: useState counter — 그러나 isRefreshing 이 이미 변경 시점이 정확히 새로고침 트리거와 일치하므로 추가 state 불필요. boolean → unknown 으로 전달, 차트 측은 primitive 비교만 수행.)

       **주변 JSX 변경 없음:** `<StockHero/>` (113행) → 새로고침 row (114~133행) → **신규 `<StockDailyChartSection ... />`** (135행 신규) → `<StockStatsGrid stock={stock} />` (기존 134행, 신규 136행 시프트) → error 패널 (기존 135~139행) → news/discussion 섹션 (기존 140~143행) 순서 유지.

       **단순 diff:**
       ```diff
       +      <StockDailyChartSection code={stock.code} refreshSignal={isRefreshing} />
              <StockStatsGrid stock={stock} />
       ```

    3. **D-19 새로고침 연동 (lock-in):** StockDetailClient 의 새로고침 버튼은 `isRefreshing` state 를 토글한다. 위 step 2 의 `refreshSignal={isRefreshing}` prop 으로 전달 → Plan 02 의 StockDailyChartSection 이 useEffect 의존성에 refreshSignal 을 포함 → boolean 값이 바뀌는 시점마다 동일 range 로 refetch. 결과: 사용자가 새로고침 1회 클릭 → Hero/Stats fetch + 차트 fetch 모두 트리거 (D-19 CONTEXT 마지막 문장 "StockDetailClient refresh 가 차트 fetch 도 트리거" 정확 충족). 자동 폴링은 여전히 없음 (D-19 첫 문장).

    4. 다른 라인 변경 금지 — load 콜백 / 새로고침 버튼 / notFound 처리 / Skeleton 분기 / error 분기 모두 그대로 유지. StockDailyChartSection 의 fetch 라이프사이클은 자체 보유하되 부모의 새로고침 신호만 prop 으로 받아 동기화.

    주의: TS strict + React 19 환경. import 순서는 Phase 06 패턴 (선언 순서대로 ./ relative 마지막).
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp typecheck && pnpm --filter @gh-radar/webapp test --run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @gh-radar/webapp typecheck` exit code 0
    - `pnpm --filter @gh-radar/webapp test --run` 전체 PASS — 기존 + 신규 회귀 0건
    - `grep -n "<StockDailyChartSection" webapp/src/components/stock/stock-detail-client.tsx` 결과 = 1건 (B2 — JSX mount 정확 1건)
    - `grep "StockDailyChartSection" webapp/src/components/stock/stock-detail-client.tsx` 결과 = 2건 (import 1 + JSX mount 1)
    - `grep "refreshSignal" webapp/src/components/stock/stock-detail-client.tsx` 결과 ≥ 1건 (B4 — D-19 새로고침 연동 prop 전달)
    - JSX 순서 검증: `grep -n "StockHero\|StockDailyChartSection\|StockStatsGrid" webapp/src/components/stock/stock-detail-client.tsx` 결과 = StockHero → StockDailyChartSection → StockStatsGrid 순 line number 오름차순
    - `pnpm --filter @gh-radar/webapp build` exit code 0 (production build)
  </acceptance_criteria>
  <done>
    StockDetailClient 가 StockDailyChartSection 을 Hero ↓ / StatsGrid ↑ 위치에 mount + refreshSignal={isRefreshing} 으로 D-19 새로고침 연동. 기존 동작 (refresh, error, notFound, Skeleton) 무회귀. typecheck + test + build 모두 PASS.
  </done>
</task>

<task type="auto">
  <name>Task 2: mockups 디렉터리 정리 + Playwright E2E spec 신규 (DATA-03 회귀 방지)</name>
  <files>
    webapp/src/app/mockups,
    webapp/e2e/specs/stock-detail-chart.spec.ts
  </files>
  <read_first>
    - webapp/src/app/mockups (전체 디렉터리 구조 — page.tsx + chart-recharts/ + chart-shadcn/ + chart-lightweight/ + shared/)
    - webapp/e2e/specs/stock-detail.spec.ts (기존 패턴 — mockStockApi/mockNewsApi 사용)
    - webapp/playwright.config.ts (storageState 흐름)
    - webapp/e2e/fixtures/ 디렉터리 (mockStockApi, mockNewsApi 위치)
    - .planning/phases/09.2-stock-detail-daily-chart/09.2-RESEARCH.md §"Critical follow-ups" #5 (mockup 정리 정책)
  </read_first>
  <action>
    Step A — mockups 디렉터리 삭제 (RESEARCH follow-up #5 의 옵션 c "전부 삭제" 채택):
    1. `rm -rf webapp/src/app/mockups` 실행 — page.tsx, chart-recharts/, chart-shadcn/, chart-lightweight/, shared/ 모두 제거.
    2. webapp 어딘가에 `/mockups` 링크가 남아있지 않은지 grep 확인 (`grep -rn "mockups\|/mockups" webapp/src/ 2>/dev/null`). RESEARCH 단계에서 추가한 navigation 링크가 있으면 함께 제거.
    3. webapp/package.json 의 lightweight-charts / recharts 의존은 **유지** — lightweight-charts 는 production 차트가 사용. recharts 는 본 plan 에서 더 이상 사용하지 않으므로 별도 task 로 정리할 수도 있으나, 본 plan 은 보수적으로 유지 (향후 phase 에서 recharts 다른 차트 도입 시 재사용 가능. 번들 영향 = production 코드에서 recharts import 없으면 tree-shaking 으로 0 byte).
       - **선택적 정리**: `pnpm --filter @gh-radar/webapp remove recharts shadcn` 실행 가능 — 단, shadcn CLI 가 webapp/src/components/ui/chart.tsx 를 자동 생성한 것이므로 chart.tsx 의 production 사용 여부 확인 후 결정. **본 plan 은 의존 유지 + chart.tsx 미사용 코드 정리는 별도 quick task 로 위임**.
    4. 디렉터리 정리 commit 단독 메시지: "chore(09.2): RESEARCH 단계 mockup 라우트 제거 (lightweight-charts 5.2.0 lock-in 완료)"

    Step B — E2E spec 신규:
    `webapp/e2e/specs/stock-detail-chart.spec.ts`:
    ```typescript
    import { test, expect } from '@playwright/test';
    import { mockStockApi } from '../fixtures/mock-api';
    import { mockNewsApi, buildNewsList } from '../fixtures/news';

    /**
     * Phase 09.2 — DATA-03 일봉 차트 섹션 E2E.
     *
     * 스코프:
     *   - /stocks/005930 진입 시 차트 섹션이 Hero ↓ / StatsGrid ↑ 위치에 mount 되는지
     *   - 차트 캔버스 컨테이너가 노출 (Skeleton → 데이터 전환 검증)
     *   - 기간 토글 4종 (1M/3M/6M/1Y) 가 모두 표시
     *   - aria-label="일봉 차트" 가 카드에 부착
     *
     * 비검증 (Manual Verification — VALIDATION.md):
     *   - 캔들 픽셀 정확성 / 한국식 색상 시각 / 다크모드 토글 / 모바일 가독성
     *   - 위 항목은 jsdom 미지원 (lightweight-charts 의 Canvas 측정) — 사용자 시각 검증
     */

    test.describe('Phase 09.2 — 일봉 차트 섹션 (DATA-03)', () => {
      test('/stocks/005930 — 차트 섹션 마운트 + 토글 4종 표시', async ({
        page,
      }) => {
        await mockStockApi(page);
        await mockNewsApi(page, {
          code: '005930',
          list: buildNewsList('005930', 5),
        });
        await page.goto('/stocks/005930');

        // Hero 가 먼저 보여야 후속 차트 섹션도 mount (StockDetailClient 의 isInitialLoading 분기)
        await expect(
          page.getByRole('heading', { name: '삼성전자' }),
        ).toBeVisible();

        // 차트 카드 컨테이너 (data-testid)
        const section = page.getByTestId('stock-daily-chart-section');
        await expect(section).toBeVisible();

        // B3 — 차트 캔버스 컨테이너도 노출 검증 (Plan 02 의 stock-daily-chart.tsx 가 부여한 testid)
        const canvas = page.getByTestId('stock-daily-chart-canvas');
        await expect(canvas).toBeVisible({ timeout: 10_000 });

        // aria-label
        await expect(page.getByLabel('일봉 차트')).toBeVisible();

        // 토글 4종 — role=tab + name = 1M/3M/6M/1Y
        for (const range of ['1M', '3M', '6M', '1Y']) {
          await expect(
            page.getByRole('tab', { name: range }),
          ).toBeVisible();
        }

        // 1M 이 기본 active (D-04)
        await expect(
          page.getByRole('tab', { name: '1M' }),
        ).toHaveAttribute('aria-selected', 'true');
      });

      test('차트 영역이 Hero ↓ / StatsGrid ↑ 사이에 위치 (D-09)', async ({
        page,
      }) => {
        await mockStockApi(page);
        await mockNewsApi(page, {
          code: '005930',
          list: buildNewsList('005930', 5),
        });
        await page.goto('/stocks/005930');

        const hero = page.getByTestId('stock-hero-price');
        const chart = page.getByTestId('stock-daily-chart-section');
        const statsLabel = page.getByText('시가', { exact: true }).first();

        await expect(hero).toBeVisible();
        await expect(chart).toBeVisible();
        await expect(statsLabel).toBeVisible();

        // boundingBox y 좌표로 수직 순서 검증 — Hero < Chart < Stats
        const heroBox = await hero.boundingBox();
        const chartBox = await chart.boundingBox();
        const statsBox = await statsLabel.boundingBox();

        expect(heroBox).toBeTruthy();
        expect(chartBox).toBeTruthy();
        expect(statsBox).toBeTruthy();
        if (heroBox && chartBox && statsBox) {
          expect(heroBox.y).toBeLessThan(chartBox.y);
          expect(chartBox.y).toBeLessThan(statsBox.y);
        }
      });

      test('range 토글 클릭 — 3M 클릭 시 aria-selected 전환', async ({
        page,
      }) => {
        await mockStockApi(page);
        await mockNewsApi(page, {
          code: '005930',
          list: buildNewsList('005930', 5),
        });
        await page.goto('/stocks/005930');

        const tab3m = page.getByRole('tab', { name: '3M' });
        await expect(tab3m).toBeVisible();
        await tab3m.click();

        await expect(tab3m).toHaveAttribute('aria-selected', 'true');
        await expect(
          page.getByRole('tab', { name: '1M' }),
        ).toHaveAttribute('aria-selected', 'false');
      });
    });
    ```

    주의:
    - mockStockApi / mockNewsApi 는 기존 fixture (Phase 6/7) 사용. **차트 자체 fetch (Supabase 직접 호출) 는 mock 안 함** — 본 phase 는 production Supabase 의존이라 E2E 도 실 데이터 사용 (e2e auth.setup.ts 가 로그인 + RLS 통과한 상태).
    - 005930 (삼성전자) 는 Phase 9 적재 4M 행에 포함 — production data 가 있으므로 차트 컨테이너는 정상 노출. rows.length === 0 분기는 Manual Verification 으로 별도 검증.
    - boundingBox 비교는 deterministic 하지 않을 수 있어 (CSS 변경 시) flaky 가능 — 본 phase 의 tradeoff 로 수용. flaky 면 Plan 03 verify 단계에서 retry policy 1회 적용.

    Step C — Playwright spec 실행 검증:
    `pnpm --filter @gh-radar/webapp test:e2e stock-detail-chart` — playwright config 의 storageState 의존 (auth.setup 자동 실행).
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp test:e2e -- stock-detail-chart</automated>
  </verify>
  <acceptance_criteria>
    - `ls webapp/src/app/mockups 2>/dev/null` 결과 "No such file or directory" (디렉터리 삭제 확인)
    - `grep -rn "mockups" webapp/src/ 2>/dev/null` 결과 0건 (잔존 import 없음)
    - `pnpm --filter @gh-radar/webapp build` exit 0 (mockups 라우트 제거 후에도 build PASS)
    - `pnpm --filter @gh-radar/webapp test:e2e -- stock-detail-chart` 결과 = 3 tests PASS (마운트/순서/토글)
    - 기존 stock-detail.spec.ts 도 PASS — `pnpm --filter @gh-radar/webapp test:e2e -- stock-detail.spec` exit 0
    - webapp/e2e/specs/stock-detail-chart.spec.ts 파일 존재 + describe('Phase 09.2') 블록 ≥ 1건
    - `grep "export function buildNewsList\|export.*buildNewsList" webapp/e2e/fixtures/news.ts` 결과 ≥ 1건 (B5 — 본 spec 의 import dependency 가 실제 export 됨을 사전 검증. 현 시점 webapp/e2e/fixtures/news.ts:16 에 존재 확인됨)
  </acceptance_criteria>
  <done>
    /mockups 라우트 4종 (page + 3 chart variants + shared) 모두 제거 + webapp 코드베이스에서 mockup 참조 잔존 0건. 신규 E2E spec 3건 PASS. 기존 stock-detail E2E 회귀 0건. production build 정상.
  </done>
</task>

<task type="auto">
  <name>Task 3: STATE.md 에 Pitfall 10/11 follow-up 기록 (RLS audit + Vercel env newline)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md §"Pending Todos" (193~200행 현재 5건)
    - .planning/STATE.md §"Blockers/Concerns" (203~205행)
    - .planning/phases/09.2-stock-detail-daily-chart/09.2-RESEARCH.md §"Post-Deploy 디버그 세션" §"Pitfall 10" §"Pitfall 11"
    - supabase/migrations/20260413120100_rls_policies.sql (news_articles/discussions/summaries 의 현재 RLS 정책 grep)
    - supabase/migrations/20260515163000_fix_stock_daily_ohlcv_rls_authenticated.sql (stock_daily_ohlcv 가 받은 fix 패턴 — audit 대상이 따라야 할 형태)
  </read_first>
  <action>
    .planning/STATE.md §"Pending Todos" 섹션 끝에 다음 2건 추가:

    ```markdown
    - DI-03: Phase 09.2 RESEARCH Pitfall 10 follow-up — `news_articles`, `discussions`, `summaries` 테이블의 RLS 정책이 `TO anon` 만 명시하는지 audit. supabase/migrations/20260515163000 (stock_daily_ohlcv fix 패턴) mirror 로 `TO anon, authenticated USING (true)` 갱신 필요. supabase-js 가 인증 사용자 JWT 호출 → role=`authenticated` → 정책 부재 → default-deny 함정. 본 phase 09.2 와 무관 (차트는 stock_daily_ohlcv 만 사용) 하나 로그인 사용자 페이지 (Phase 7 뉴스, Phase 8 토론, Phase 10 요약) 의 빈 응답 가능성. 별도 phase 또는 인프라 PR 권장.
    - DI-04: Phase 09.2 RESEARCH Pitfall 11 follow-up — Vercel production env 등록 시 trailing newline (`\n`) 오염 검증 절차 자동화. 증상: dev 정상이나 production 만 모든 fetch 비정상. 검증: `vercel env pull` 후 `tail -c1 .env.local | xxd -p` 가 `0a` (newline) 이면 오염. 즉시 수정: `vercel env rm` + `printf "%s" "값" | vercel env add`. CI hook 자동화 검토 (별도 인프라 PR).
    ```

    + 동일 STATE.md §"Decisions" 섹션 끝 (192행 근처) 에 본 phase 09.2 의 decision 1건 추가 (간결):

    ```markdown
    - [Phase 09.2]: 캔들스틱 차트 채택 — REQUIREMENTS.md Out of Scope 정책 반전 (사용자 명시 2026-05-15, 상세 페이지 자체 완결성 우선). 라이브러리 = lightweight-charts 5.2.0 (RESEARCH 비교 후 lock-in: 번들 +4 kB / 캔들+Volume 네이티브 / 트레이더 친숙도). 데이터 = webapp → Supabase PostgREST 직접 호출 (Phase 06.2 watchlist 패턴 mirror). Pitfall 9 (oklch parser 거부) → chart-colors.ts utility 모듈로 회귀 방지. Pitfall 6 (다크모드 자동 분기 미작동) → next-themes useTheme + chart.applyOptions effect 로 production 해결.
    ```

    주의:
    - DI-XX 번호는 STATE.md 의 기존 DI-01, DI-02 다음 sequential 번호. 실제 파일 grep 후 결정.
    - 본 task 는 STATE.md 만 변경. 마이그레이션 파일이나 인프라는 건드리지 않음 (사용자 합의 없는 production DB 변경 금지).
    - commit 메시지 한글: "docs(state): Phase 09.2 결정 + Pitfall 10/11 follow-up 기록 (DI-03/04)"
  </action>
  <verify>
    <automated>grep -c "DI-03\|DI-04" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DI-03" .planning/STATE.md` 결과 ≥ 1건
    - `grep "DI-04" .planning/STATE.md` 결과 ≥ 1건
    - `grep "Phase 09.2.*캔들스틱" .planning/STATE.md` 결과 ≥ 1건 (Decisions 추가 확인)
    - `grep "RLS authenticated\|TO anon, authenticated" .planning/STATE.md` 결과 ≥ 1건 (DI-03 본문)
    - `grep "trailing newline\|0a.*newline" .planning/STATE.md` 결과 ≥ 1건 (DI-04 본문)
  </acceptance_criteria>
  <done>
    STATE.md 의 Pending Todos 에 DI-03 (RLS authenticated audit), DI-04 (Vercel env newline 검증) 두 건이 기록됨. Decisions 에 Phase 09.2 의 라이브러리 선정 + Pitfall 9/6 해결 패턴 1줄 요약 기록. 본 phase 와 직접 코드 변경 없는 follow-up 의 추적성 확보.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: 사용자 시각 검증 — 다크/라이트 토글 + 모바일 가독성 + 4 range 토글 (VALIDATION.md Manual-Only)</name>
  <files>(no file changes — manual verification only)</files>
  <action>본 task 는 코드 변경 없음. 사용자가 production-grade 빌드를 실제 브라우저에서 시각/인터랙션 검증. how-to-verify 의 9개 항목을 순차 점검 후 승인 또는 이슈 보고.</action>
  <verify>Manual — 9개 검증 항목 (검증 1~9) 사용자 응답</verify>
  <done>사용자가 9개 검증 항목 모두 승인 ("approved" 또는 동등 신호) 또는 이슈 후 재검증으로 최종 승인.</done>
  <what-built>
    Plan 01 + 02 + 03 통합 결과:
    - Plan 01: REQUIREMENTS/PROJECT/ROADMAP 갱신 + packages/shared 타입 + chart-colors utility + daily-ohlcv-api + 16건 unit tests
    - Plan 02: StockDailyChart (lightweight-charts wrapper) + StockDailyChartSection (fetch+토글+분기) + Skeleton + 6건 unit tests
    - Plan 03 Task 1~3: StockDetailClient mount + mockups 디렉터리 삭제 + 신규 E2E 3건 + STATE.md follow-up 2건
  </what-built>
  <how-to-verify>
    Pre-flight:
    - `./dev.sh` 실행 (PORT=3100, dev.sh 룰 — CLAUDE.md memory 기준)
    - 사용자 브라우저로 http://localhost:3100/login → 로그인 → http://localhost:3100/stocks/005930 진입

    검증 1 — 차트 섹션 위치 + 기본 렌더 (D-09, D-04):
    - Hero (삼성전자 + 가격) 직후, StatsGrid (시가/고가/저가...) 직전에 차트 카드 표시
    - 차트 카드 헤더: 좌측 "일봉 차트" + (오늘자 row 포함 시) "장중" 배지 / 우측 토글 4종 (1M/3M/6M/1Y)
    - 기본 활성 토글 = 1M
    - 차트 본체에 캔들스틱 (양봉 빨강 / 음봉 파랑) + 하단 25% Volume bar (캔들 색상과 동기화)

    검증 2 — 한국식 색상 (D-02):
    - 양봉 = 빨강 계열 (`#ef4444` light / `#f87171` dark)
    - 음봉 = 파랑 계열 (`#3b82f6` light / `#60a5fa` dark)
    - 미국식 (양봉 녹/음봉 적) 이 보이면 chart-colors.ts 변환 실패 — Plan 01 Task 3 회귀

    검증 3 — Range 토글 4종 (D-05):
    - 1M 클릭 → ~22 영업일 캔들 + 명료한 캔들 폭 (≥4px)
    - 3M 클릭 → 차트 즉시 갱신, ~65 영업일
    - 6M 클릭 → ~130 영업일
    - 1Y 클릭 → ~252 영업일 (캔들 폭 ~3px 한계 — D-05 정책상 허용)
    - 토글 클릭 시 Skeleton 잠깐 → 차트 재렌더 cycle 정상

    검증 4 — 다크/라이트 토글 (D-22 / Pitfall 6):
    - AppHeader 의 ThemeToggle 클릭 → 다크 ↔ 라이트 전환
    - 차트 background / text / grid / 캔들 색상 모두 1초 내 새 토큰으로 전환 (재로딩 없이 in-place applyOptions)
    - 라이트 모드: 흰 배경 + 회색 grid + 적색/청색 캔들
    - 다크 모드: 다크 배경 + 어두운 grid + 밝은 적/청색 캔들

    검증 5 — 모바일 가독성 (D-11 / VALIDATION.md):
    - DevTools 모바일 뷰포트 (iPhone 14, 390×844) 전환
    - 1M 토글 → 캔들 폭 ≥4px (육안 가독성)
    - 토글 4종이 카드 헤더에서 wrap 또는 truncate 없이 표시
    - 차트 height 340px 가 모바일에서도 적절 (UI 잘림 없음)

    검증 6 — 접근성 (D-21):
    - 차트 카드 inspect → `aria-label="일봉 차트"` 존재
    - sr-only 텍스트 inspect → "최근 N영업일 종가 추이..." 형식

    검증 7 — Empty state (D-17):
    - 신규 상장 종목 또는 코드가 stock_daily_ohlcv 에 없는 종목으로 진입 (예: /stocks/999999 → 404 가 아닌 빈 차트 종목 선택 — supabase 에서 직접 확인 후 사용자 선택)
    - 차트 영역에 "일봉 데이터가 아직 수집되지 않았습니다" 카피 표시
    - (검증 어려우면 skip — DevTools Network 탭에서 stock_daily_ohlcv response 가 빈 배열이면 OK)

    검증 8 — Error state (D-18):
    - DevTools Network 탭에서 supabase 의 stock_daily_ohlcv POST/GET 을 throttle / block
    - 차트 영역에 "일봉 데이터를 불러오지 못했습니다" + "다시 시도" 버튼 표시
    - "다시 시도" 클릭 → block 해제 후 차트 정상 복구

    검증 9 — 회귀 (Phase 6/7/8):
    - StockHero / StockStatsGrid / StockNewsSection / StockDiscussionSection 모두 정상
    - 새로고침 버튼 클릭 시 Hero/Stats 갱신 (차트는 자체 라이프사이클 — D-19 정책)
  </how-to-verify>
  <resume-signal>
    승인: "approved" 또는 "9개 검증 모두 OK"
    이슈 발견: 구체적 검증 번호 + 증상 ("검증 4 — 다크 모드에서 캔들 색상 미반영" 등) → planner/executor 수정 후 재검증
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StockDetailClient props | code 는 StockPage (route component) 가 정규식 `/^[A-Za-z0-9]{1,10}$/` 검증 후 전달 — 본 plan 의 mount 지점은 trusted input |
| /mockups 라우트 제거 | RESEARCH 단계의 mockup 가 production deploy 에 포함되어 있다면 로그인 사용자가 mockup 페이지를 통해 005930 의 차트 미리 볼 수 있었음. 본 plan 이 제거하면 attack surface 1개 소거 |
| STATE.md 변경 | 문서. 보안 영향 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-10 | Information Disclosure | /mockups 잔존 시 production 노출 | mitigate | Task 2 가 디렉터리 삭제 — production build 후 라우트 자체 미존재. middleware PUBLIC_PREFIXES 추가 안 했으므로 로그인 사용자만 봤지만, 그래도 깔끔히 제거 |
| T-09.2-11 | Tampering | E2E spec 의 storageState 의존 | accept | Phase 06.2 가 이미 채택한 패턴. setup project 가 매 run 시 신규 세션 생성 |
| T-09.2-12 | Information Disclosure | E2E 가 production Supabase 의 005930 실 데이터 호출 | accept | 005930 (삼성전자) 는 공개 시세 — 민감 정보 아님. 비-005930 종목으로 확장 시 동일 정책 |

모든 위협 LOW (3건). HIGH 없음 — block on high 정책 통과.
</threat_model>

<verification>
- `pnpm --filter @gh-radar/webapp typecheck` exit 0
- `pnpm --filter @gh-radar/webapp test --run` 전체 PASS (Plan 01 16건 + Plan 02 6건 + 기존 회귀 0건)
- `pnpm --filter @gh-radar/webapp build` exit 0 (mockups 제거 후에도 9 → 6 라우트 정상 build)
- `pnpm --filter @gh-radar/webapp test:e2e -- stock-detail-chart` 3건 PASS
- `pnpm --filter @gh-radar/webapp test:e2e -- stock-detail.spec` 기존 3건 PASS (회귀 없음)
- `grep "DI-03\|DI-04" .planning/STATE.md` 양쪽 ≥ 1건
- 사용자 시각 검증 9개 항목 승인
</verification>

<success_criteria>
- StockDetailClient 가 Hero ↓ / StatsGrid ↑ 위치에 StockDailyChartSection mount (D-09) + refreshSignal={isRefreshing} D-19 새로고침 연동
- production build 가 lightweight-charts 5.2.0 + Phase 09.2 컴포넌트 모두 통과
- E2E spec 3건 (마운트 / 위치 / 토글) 신규 PASS + 기존 stock-detail E2E 3건 회귀 0
- /mockups 라우트 4종 모두 production 에서 제거 — attack surface 소거
- STATE.md 에 Pitfall 10/11 follow-up (DI-03/04) + Phase 09.2 decision 1줄 기록
- 사용자 시각 검증 9개 항목 승인 — 다크/라이트, 모바일, range 토글, 한국식 색상, Empty/Error, 회귀 확인
- DATA-03 요구사항 traceability 가 ROADMAP plan list + REQUIREMENTS Traceability + Phase summary 3축에서 모두 확인
</success_criteria>

<output>
After completion, create `.planning/phases/09.2-stock-detail-daily-chart/09.2-03-SUMMARY.md` with:
- StockDetailClient 의 diff (3줄: import + JSX mount)
- 삭제된 mockups 디렉터리 inventory (page.tsx + chart-recharts/* + chart-shadcn/* + chart-lightweight/* + shared/*)
- 신규 E2E 3건 PASS log + 기존 stock-detail.spec 회귀 0 확인
- STATE.md DI-03/DI-04 추가 위치 + 본문 인용
- 사용자 시각 검증 9개 항목 결과 매트릭스 (✅ / ⚠️ / ❌)
- Phase 09.2 종합 — 3 plan 통합 후 production gh-radar.vercel.app 의 /stocks/005930 가 차트 표시 (Vercel 재배포 필요 시 별도 quick task)
</output>
