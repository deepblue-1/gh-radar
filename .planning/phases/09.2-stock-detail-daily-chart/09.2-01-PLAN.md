---
phase: 09.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - packages/shared/src/dailyOhlcv.ts
  - packages/shared/src/index.ts
  - packages/shared/src/__tests__/dailyOhlcv.test.ts
  - webapp/src/lib/chart-colors.ts
  - webapp/src/lib/__tests__/chart-colors.test.ts
  - webapp/src/lib/daily-ohlcv-api.ts
  - webapp/src/lib/__tests__/daily-ohlcv-api.test.ts
autonomous: true
requirements:
  - DATA-03
user_setup: []

must_haves:
  truths:
    - "REQUIREMENTS.md Out of Scope 의 '캔들스틱 차트' 행이 삭제되고 신규 DATA-03 가 v1 Data 섹션 + Traceability 표에 등재된다 (F-01)"
    - "PROJECT.md Key Decisions 테이블에 '캔들스틱 차트 채택 (OOS 정책 반전, 2026-05-15)' 결정이 추가된다 (F-02)"
    - "packages/shared 가 DailyOhlcvRow 인터페이스 + DailyOhlcvRangeKey 유니언을 export 하여 webapp 과 향후 server 가 동일 타입을 참조한다 (D-15)"
    - "webapp/src/lib/chart-colors.ts 의 getChartPalette('light' | 'dark') 가 lightweight-charts 가 파싱 가능한 sRGB hex 팔레트를 반환한다 (Pitfall 9 회귀 방지)"
    - "webapp/src/lib/daily-ohlcv-api.ts 의 fetchDailyOhlcv(code, range, signal?) 가 Supabase 직접 호출 + AbortSignal + 시간 ASC 정렬 + 숫자 변환을 수행한다 (D-12, D-13, D-14, Pitfall 7)"
    - "rangeToFromDate('1M' | '3M' | '6M' | '1Y') 가 today − {60, 120, 220, 400} calendar day 의 ISO date 를 반환한다 (D-04, D-05)"
    - "위 4개 모듈의 단위 테스트가 vitest 에서 모두 green (Wave 0 Nyquist 충족)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "F-01 — Out of Scope '캔들스틱 차트' 행 삭제 + DATA-03 신규 등재 + Traceability 행 추가"
      contains: "DATA-03"
    - path: ".planning/PROJECT.md"
      provides: "F-02 — Key Decisions 테이블에 캔들 차트 OOS 반전 결정"
      contains: "캔들스틱 차트 채택"
    - path: "packages/shared/src/dailyOhlcv.ts"
      provides: "DailyOhlcvRow + DailyOhlcvRangeKey 공유 타입"
      exports: ["DailyOhlcvRow", "DailyOhlcvRangeKey", "DAILY_OHLCV_RANGES"]
    - path: "packages/shared/src/index.ts"
      provides: "신규 타입 re-export"
    - path: "webapp/src/lib/chart-colors.ts"
      provides: "lightweight-charts 호환 팔레트 + 다크/라이트 분기 helper (oklch → hex 매핑 utility)"
      exports: ["ChartPalette", "getChartPalette"]
    - path: "webapp/src/lib/daily-ohlcv-api.ts"
      provides: "Supabase 직접 호출 fetchDailyOhlcv + rangeToFromDate"
      exports: ["fetchDailyOhlcv", "rangeToFromDate"]
    - path: "packages/shared/src/__tests__/dailyOhlcv.test.ts"
      provides: "DailyOhlcvRow 타입 컴파일 + DAILY_OHLCV_RANGES 길이 단위 테스트"
    - path: "webapp/src/lib/__tests__/chart-colors.test.ts"
      provides: "팔레트 키 + 16진수 형식 + 다크/라이트 분기 단위 테스트"
    - path: "webapp/src/lib/__tests__/daily-ohlcv-api.test.ts"
      provides: "fetchDailyOhlcv mock 호출 검증 + rangeToFromDate 4종 분기 단위 테스트"
  key_links:
    - from: "webapp/src/lib/daily-ohlcv-api.ts"
      to: "@/lib/supabase/client"
      via: "createClient() 재사용"
      pattern: "from\\(.stock_daily_ohlcv.\\)"
    - from: "webapp/src/lib/daily-ohlcv-api.ts"
      to: "@gh-radar/shared"
      via: "DailyOhlcvRow / DailyOhlcvRangeKey import"
      pattern: "from '@gh-radar/shared'"
    - from: "webapp/src/lib/chart-colors.ts"
      to: "(static palette table)"
      via: "ChartPalette 객체 매핑"
      pattern: "up:.*#[0-9a-fA-F]{6}"
---

<objective>
Phase 09.2 의 모든 후속 plan 이 의존하는 **공통 기반**을 한 번에 구축한다. 라이브러리·UI 변경은 일절 없이, (1) 캔들 차트 채택을 프로젝트 문서에 반영하고 (F-01/F-02), (2) RESEARCH 단계의 mockup `shared/fetch-daily.ts` 를 정식 모듈 `webapp/src/lib/daily-ohlcv-api.ts` 로 promote 하면서 AbortSignal 을 추가하고, (3) RESEARCH Post-Deploy Pitfall 9 (oklch parser) 를 회귀 방지용 `chart-colors.ts` utility 로 모듈화하며, (4) `packages/shared` 에 공유 타입 `DailyOhlcvRow` + `DailyOhlcvRangeKey` 를 정의한다. 각 모듈마다 단위 테스트를 동시에 추가해 VALIDATION.md 의 Wave 0 gap 을 100% 채운다.

Purpose: lightweight-charts 차트 컴포넌트가 의존할 (a) 데이터 fetch 계약 (b) 색상 팔레트 (c) 공유 타입 (d) 정책 문서 4축을 한 commit cluster 로 정리해 후속 plan 의 코드 변경이 순수 UI 작업으로 축소되도록 만든다.
Output: 4개 신규 모듈 + 3개 테스트 파일 + 3개 문서 갱신.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09.2-stock-detail-daily-chart/09.2-CONTEXT.md
@.planning/phases/09.2-stock-detail-daily-chart/09.2-RESEARCH.md
@.planning/phases/09.2-stock-detail-daily-chart/09.2-VALIDATION.md

@webapp/src/app/mockups/shared/fetch-daily.ts
@webapp/src/app/mockups/chart-lightweight/page.tsx
@webapp/src/lib/watchlist-api.ts
@webapp/src/lib/supabase/client.ts
@packages/shared/src/index.ts
@packages/shared/src/stock.ts
@webapp/vitest.config.ts

<interfaces>
<!-- 본 plan 이 정의/사용하는 핵심 contract. 후속 plan 02/03 가 그대로 import. -->

신규 export — packages/shared/src/dailyOhlcv.ts:
```typescript
export interface DailyOhlcvRow {
  /** ISO YYYY-MM-DD (Supabase stock_daily_ohlcv.date) */
  date: string;
  open: number;
  high: number;
  low: number;
  close: number;
  volume: number;
  changeAmount: number | null;
  changeRate: number | null;
}

export type DailyOhlcvRangeKey = '1M' | '3M' | '6M' | '1Y';

export const DAILY_OHLCV_RANGES: readonly DailyOhlcvRangeKey[] =
  ['1M', '3M', '6M', '1Y'] as const;
```

신규 export — webapp/src/lib/chart-colors.ts:
```typescript
export interface ChartPalette {
  /** 양봉 (한국식 빨강) — D-02 */
  up: string;
  /** 음봉 (한국식 파랑) — D-02 */
  down: string;
  /** 축/라벨 텍스트 */
  text: string;
  /** grid 선 */
  grid: string;
  /** 차트 배경 */
  bg: string;
}

export function getChartPalette(theme: 'light' | 'dark'): ChartPalette;
```

신규 export — webapp/src/lib/daily-ohlcv-api.ts:
```typescript
import type { DailyOhlcvRow, DailyOhlcvRangeKey } from '@gh-radar/shared';

export function rangeToFromDate(
  range: DailyOhlcvRangeKey,
  today?: Date,
): string;

export async function fetchDailyOhlcv(
  code: string,
  range: DailyOhlcvRangeKey,
  signal?: AbortSignal,
): Promise<DailyOhlcvRow[]>;
```

기존 reference (수정 없음) — packages/shared/src/index.ts:
- 현재 stock/news/discussion/summary/kiwoom/marketHours/api/sanitize 7개 모듈 re-export
- 본 plan 은 dailyOhlcv 한 줄 추가만
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS / PROJECT / ROADMAP 문서 정합성 (F-01 + F-02)</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/PROJECT.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (전체 — 64행 DATA-01 패턴, 87행 Out of Scope 캔들스틱 행, 94행 Traceability 표 위치)
    - .planning/PROJECT.md (Key Decisions 테이블 위치)
    - .planning/ROADMAP.md §"Phase 09.2: 종목 상세페이지 상단 일봉차트" (336~344행, Requirements TBD 라인)
    - .planning/phases/09.2-stock-detail-daily-chart/09.2-CONTEXT.md §Follow-up F-01/F-02
  </read_first>
  <action>
    F-01 (REQUIREMENTS.md):
    1. 87행 `| 캔들스틱 차트 | TradingView/키움이 이미 지배, 차별화 없음 |` 행을 **삭제**
    2. v1 Requirements > Data 섹션 (64행 DATA-01 직후) 에 신규 항목 추가:
       ```
       - **DATA-03**: 종목 상세 페이지(`/stocks/[code]`) 상단에 해당 종목의 일봉 캔들 + Volume 차트(1M / 3M / 6M / 1Y 토글, 한국식 색상)를 표시하여 트레이더가 화면 전환 없이 가격 흐름을 즉시 파악할 수 있다 (Phase 09.2 — 2026-05-15 사용자 명시 OOS 반전).
       ```
       (DATA-02 가 이미 점유했는지 확인. 점유 중이라 가정 — 신규 ID 는 DATA-03)
    3. Traceability 표 (94행~) 끝에 행 추가:
       `| DATA-03 | Phase 09.2 | Pending |`

    F-02 (PROJECT.md):
    1. Key Decisions 테이블의 마지막 행 다음에 추가:
       `| 2026-05-15 | 캔들스틱 차트 채택 (Out of Scope 정책 반전) | 사용자 명시 — 상세 페이지 자체 완결성 우선, TradingView/키움과의 차별화보다 트레이더가 화면 전환 없이 가격 흐름을 즉시 파악하는 가치 우선. RESEARCH 비교 후 lightweight-charts 5.2.0 lock-in. | Phase 09.2 |`
       (실제 컬럼 schema 는 PROJECT.md 의 헤더에 맞춰 align — 일자/결정/사유/관련 형식 유지)

    ROADMAP.md (336~344행 갱신):
    1. 339행 `**Requirements**: TBD (Phase 6 detail page 확장)` → `**Requirements**: DATA-03`
    2. Plans 0 plans → 3 plans 로 갱신 (전체 plan 작성 완료 가정 — Plan 01/02/03)
    3. 343~344행 `Plans:` 블록 갱신:
       ```
       Plans:
       - [ ] 09.2-01-PLAN.md — Wave 1 기반 (REQUIREMENTS/PROJECT 갱신 + packages/shared 타입 + chart-colors utility + daily-ohlcv-api + Wave 0 unit tests)
       - [ ] 09.2-02-PLAN.md — Wave 2 차트 UI (StockDailyChart lightweight-charts + StockDailyChartSection + 다크모드 useTheme effect + Skeleton/Empty/Error)
       - [ ] 09.2-03-PLAN.md — Wave 3 통합 + 정리 (StockDetailClient 마운트 + mockups 디렉터리 삭제 + E2E spec + 사용자 시각 검증 + STATE 함정 기록)
       ```

    주의:
    - 본 task 는 코드 변경 0. 문서만.
    - 한 commit 으로 묶을 것. 메시지 한글 ("docs(09.2): 캔들 차트 채택 정책 반전 + DATA-03 신설" 권장).
    - DATA ID 충돌 검증: 64행 DATA-01 / 65행 DATA-02 가 이미 점유 → 신규 = DATA-03. (실제 파일 grep 후 결정)
  </action>
  <verify>
    <automated>grep -c "DATA-03" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "캔들스틱 차트" .planning/REQUIREMENTS.md` 결과 0건 (Out of Scope 행 삭제 확인)
    - `grep "DATA-03" .planning/REQUIREMENTS.md` 결과 ≥ 2건 (정의 1 + Traceability 1)
    - `grep "캔들스틱 차트 채택" .planning/PROJECT.md` 결과 ≥ 1건
    - `grep "DATA-03" .planning/ROADMAP.md` 결과 ≥ 1건 (Phase 09.2 Requirements 라인)
    - `grep "09.2-01-PLAN.md" .planning/ROADMAP.md` 결과 1건
    - (W1) `grep -c "09.2-0[123]-PLAN.md" .planning/ROADMAP.md` 결과 = 3 (Plans 목록 3건 모두 갱신 검증)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md 에 캔들스틱 차트 OOS 행이 사라지고 DATA-03 가 정의 + Traceability 양쪽에 존재. PROJECT.md Key Decisions 에 OOS 반전 결정 행이 존재. ROADMAP.md Phase 09.2 entry 의 Requirements 가 DATA-03 로 명시되고 Plans 목록이 3개 plan 파일로 갱신됨.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: packages/shared 의 DailyOhlcvRow + DailyOhlcvRangeKey 타입 신설 (D-15)</name>
  <files>
    packages/shared/src/dailyOhlcv.ts,
    packages/shared/src/index.ts,
    packages/shared/src/__tests__/dailyOhlcv.test.ts
  </files>
  <read_first>
    - packages/shared/src/index.ts (현재 7개 모듈 re-export 패턴)
    - packages/shared/src/stock.ts (StockDailyOhlcv 비교 대상 — worker 내부 타입과의 차이 확인)
    - packages/shared/src/__tests__/kiwoom.test.ts (기존 타입 테스트 패턴 참고)
    - .planning/phases/09.2-stock-detail-daily-chart/09.2-CONTEXT.md §D-13/D-15
    - webapp/src/app/mockups/shared/fetch-daily.ts (DailyOhlcvRow 시안)
  </read_first>
  <behavior>
    - DailyOhlcvRow 는 8개 필드 (date / open / high / low / close / volume / changeAmount / changeRate) 정의. changeAmount/changeRate 만 nullable.
    - DailyOhlcvRangeKey union = '1M' | '3M' | '6M' | '1Y'.
    - DAILY_OHLCV_RANGES readonly tuple = ['1M', '3M', '6M', '1Y'] 길이 4.
    - 단위 테스트 — DAILY_OHLCV_RANGES.length === 4 + 각 키 포함 확인 + DailyOhlcvRow 타입에 sample row assign 가능 (TypeScript 컴파일 검증).
  </behavior>
  <action>
    1. `packages/shared/src/dailyOhlcv.ts` 신규 파일:
       ```typescript
       /**
        * Phase 09.2 — webapp + server 가 공유하는 일봉 OHLCV row 타입.
        *
        * Supabase `stock_daily_ohlcv` (Phase 9 D-03 스키마) 의 부분 projection —
        * trade_amount 는 차트 v1 미사용으로 제외. webapp/src/lib/daily-ohlcv-api.ts 의
        * fetchDailyOhlcv 반환 + StockDailyChart 입력 타입.
        */
       export interface DailyOhlcvRow {
         /** ISO YYYY-MM-DD */
         date: string;
         open: number;
         high: number;
         low: number;
         close: number;
         volume: number;
         /** 전일대비 (절대값) — 신규 상장일 등에서 null */
         changeAmount: number | null;
         /** 등락률 % — 신규 상장일 등에서 null */
         changeRate: number | null;
       }

       /** 차트 기간 토글 키 — D-04 / D-05 */
       export type DailyOhlcvRangeKey = '1M' | '3M' | '6M' | '1Y';

       /** UI 토글 컴포넌트가 iterate 할 수 있는 readonly tuple. */
       export const DAILY_OHLCV_RANGES: readonly DailyOhlcvRangeKey[] = [
         '1M',
         '3M',
         '6M',
         '1Y',
       ] as const;
       ```
    2. `packages/shared/src/index.ts` 에 한 줄 추가 (기존 export 블록 마지막 부근, news/discussion/summary 다음):
       ```typescript
       export type { DailyOhlcvRow, DailyOhlcvRangeKey } from "./dailyOhlcv.js";
       export { DAILY_OHLCV_RANGES } from "./dailyOhlcv.js";
       ```
    3. `packages/shared/src/__tests__/dailyOhlcv.test.ts` 신규:
       ```typescript
       import { describe, expect, it } from 'vitest';
       import {
         DAILY_OHLCV_RANGES,
         type DailyOhlcvRangeKey,
         type DailyOhlcvRow,
       } from '../dailyOhlcv.js';

       describe('DailyOhlcvRow / DailyOhlcvRangeKey', () => {
         it('exposes 4 range keys in 1M → 1Y order', () => {
           expect(DAILY_OHLCV_RANGES).toEqual(['1M', '3M', '6M', '1Y']);
           expect(DAILY_OHLCV_RANGES).toHaveLength(4);
         });

         it('accepts a full row with nullable change fields', () => {
           const row: DailyOhlcvRow = {
             date: '2026-05-15',
             open: 70000,
             high: 71000,
             low: 69500,
             close: 70500,
             volume: 12_345_678,
             changeAmount: 500,
             changeRate: 0.71,
           };
           expect(row.date).toBe('2026-05-15');

           const newlyListed: DailyOhlcvRow = {
             date: '2026-05-15',
             open: 10000,
             high: 11000,
             low: 9500,
             close: 10500,
             volume: 1000,
             changeAmount: null,
             changeRate: null,
           };
           expect(newlyListed.changeAmount).toBeNull();
         });

         it('rejects out-of-domain range keys at the type level', () => {
           // @ts-expect-error 5Y 는 v1 범위 외
           const _bad: DailyOhlcvRangeKey = '5Y';
           expect(true).toBe(true);
         });
       });
       ```
    4. RED → GREEN 검증: 먼저 dailyOhlcv.ts 미생성 상태로 테스트 작성 (RED) → 모듈 + index re-export 작성 (GREEN). 본 task 는 모듈 + 테스트 동시 작성이므로 test 가 PASS 됨을 확인.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/shared test --run -- dailyOhlcv</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @gh-radar/shared test --run -- dailyOhlcv` exit code 0, 3 tests PASS
    - `pnpm --filter @gh-radar/shared typecheck` exit code 0
    - `grep "DailyOhlcvRow\|DAILY_OHLCV_RANGES" packages/shared/src/index.ts` 결과 ≥ 2건
    - `node -e "require('./packages/shared/dist/index.js')" `는 build 후만 동작 — 본 acceptance 는 source-level
  </acceptance_criteria>
  <done>
    @gh-radar/shared 패키지가 DailyOhlcvRow 인터페이스, DailyOhlcvRangeKey 유니언, DAILY_OHLCV_RANGES readonly 튜플 3개를 export. webapp/server 양쪽이 import 가능. 단위 테스트 3건 PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: chart-colors.ts utility — oklch → hex 매핑 + getChartPalette helper (Pitfall 9 회귀 방지)</name>
  <files>
    webapp/src/lib/chart-colors.ts,
    webapp/src/lib/__tests__/chart-colors.test.ts
  </files>
  <read_first>
    - webapp/src/app/mockups/chart-lightweight/page.tsx (50~65행 COLORS 객체 — promote 대상)
    - .planning/phases/09.2-stock-detail-daily-chart/09.2-RESEARCH.md §"Pitfall 9 — lightweight-charts color parser 가 oklch() 거부"
    - webapp/src/styles/globals.css (Phase 3 oklch 토큰 위치 — 매핑 근거 주석에 인용)
    - webapp/vitest.config.ts (test setup 확인)
  </read_first>
  <behavior>
    - getChartPalette('light') 가 ChartPalette 객체 반환 — 5개 키 모두 존재 (up, down, text, grid, bg).
    - getChartPalette('dark') 가 light 와 다른 5개 hex 값을 반환.
    - 모든 hex 값이 `^#[0-9a-fA-F]{6}$` 패턴.
    - light.up 과 dark.up 이 서로 다름 (다크모드 분기 입증).
    - 잘못된 theme 인자 ('system' 등) 는 TypeScript 가 거부 — 런타임 가드는 불필요.
  </behavior>
  <action>
    1. `webapp/src/lib/chart-colors.ts` 신규 파일 — mockup3 의 COLORS 객체를 promote + 주석 보강:
       ```typescript
       /**
        * Phase 09.2 — lightweight-charts 호환 차트 팔레트.
        *
        * Pitfall 9 (RESEARCH §Post-Deploy 디버그) 회귀 방지:
        *   lightweight-charts 5.2.0 의 내부 color parser 는 hex/rgb/hsl/명명색만 받고
        *   CSS Color 4 `oklch()` 를 거부 ("Failed to parse color: oklch(...)").
        *   webapp/src/styles/globals.css 의 Phase 3 토큰이 모두 oklch 이므로 var(--up)
        *   이나 getComputedStyle().getPropertyValue() 결과를 그대로 chart 옵션에 주입할
        *   수 없다. 본 모듈이 oklch → sRGB hex 근사 매핑 테이블을 단일 source 로 보유.
        *
        * 다크모드 분기는 컴포넌트 측 useTheme() 훅이 'light' | 'dark' 를 결정해 호출.
        * D-02 (한국식 색상): up = 빨강, down = 파랑.
        */

       export interface ChartPalette {
         /** 양봉 (한국식 빨강) */
         up: string;
         /** 음봉 (한국식 파랑) */
         down: string;
         /** 축 / 라벨 텍스트 */
         text: string;
         /** grid 선 */
         grid: string;
         /** 차트 배경 */
         bg: string;
       }

       const PALETTES: Record<'light' | 'dark', ChartPalette> = {
         light: {
           up: '#ef4444',   // ≈ oklch(0.66 0.20 22)
           down: '#3b82f6', // ≈ oklch(0.63 0.18 250)
           text: '#737373', // ≈ oklch(0.50 0 0)
           grid: '#e7e7e7', // ≈ oklch(0.92 0 0)
           bg: '#ffffff',
         },
         dark: {
           up: '#f87171',   // ≈ oklch(0.72 0.19 22)
           down: '#60a5fa', // ≈ oklch(0.72 0.16 250)
           text: '#a3a3a3', // ≈ oklch(0.65 0 0)
           grid: '#2e2e2e', // ≈ oklch(0.24 0 0)
           bg: '#1c1c1c',   // ≈ oklch(0.12 0 0)
         },
       } as const;

       export function getChartPalette(theme: 'light' | 'dark'): ChartPalette {
         return PALETTES[theme];
       }
       ```
    2. `webapp/src/lib/__tests__/chart-colors.test.ts` 신규:
       ```typescript
       import { describe, expect, it } from 'vitest';
       import { getChartPalette, type ChartPalette } from '../chart-colors';

       const HEX_RE = /^#[0-9a-fA-F]{6}$/;
       const KEYS: (keyof ChartPalette)[] = ['up', 'down', 'text', 'grid', 'bg'];

       describe('getChartPalette', () => {
         it.each(['light', 'dark'] as const)(
           '%s 팔레트가 모든 키를 hex 문자열로 노출 (Pitfall 9: lightweight-charts 가 oklch 거부)',
           (theme) => {
             const p = getChartPalette(theme);
             for (const key of KEYS) {
               expect(p[key]).toMatch(HEX_RE);
             }
           },
         );

         it('다크/라이트 분기가 실제로 다른 hex 값을 반환한다', () => {
           const light = getChartPalette('light');
           const dark = getChartPalette('dark');
           expect(light.up).not.toBe(dark.up);
           expect(light.down).not.toBe(dark.down);
           expect(light.bg).not.toBe(dark.bg);
         });

         it('한국식 색상 컨벤션 — up = 적색 계열, down = 청색 계열 (D-02)', () => {
           const { up, down } = getChartPalette('light');
           // 적색 = R 채널이 가장 큼
           const upR = parseInt(up.slice(1, 3), 16);
           const upG = parseInt(up.slice(3, 5), 16);
           const upB = parseInt(up.slice(5, 7), 16);
           expect(upR).toBeGreaterThan(upG);
           expect(upR).toBeGreaterThan(upB);
           // 청색 = B 채널이 가장 큼
           const dnR = parseInt(down.slice(1, 3), 16);
           const dnG = parseInt(down.slice(3, 5), 16);
           const dnB = parseInt(down.slice(5, 7), 16);
           expect(dnB).toBeGreaterThan(dnR);
           expect(dnB).toBeGreaterThan(dnG);
         });
       });
       ```
    3. RED → GREEN — 모듈 + 테스트 동시 작성, vitest 실행해 PASS 확인.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp test --run -- chart-colors</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @gh-radar/webapp test --run -- chart-colors` exit code 0, 4 tests PASS (it.each 가 2건 + 단일 2건)
    - `pnpm --filter @gh-radar/webapp typecheck` exit code 0
    - `grep -E '#[0-9a-fA-F]{6}' webapp/src/lib/chart-colors.ts | wc -l` ≥ 10 (light 5 + dark 5)
    - (W2) `grep -nE "^[[:space:]]*[^/[:space:]].*oklch" webapp/src/lib/chart-colors.ts | wc -l` 결과 = 0 (코드 라인만 매치 — 주석 reference 는 `^[[:space:]]*//` 로 시작하므로 매치 제외, false positive 회피. hex 리터럴은 PALETTES 안에 의도적으로 존재하므로 본 grep 은 oklch 만 검증)
  </acceptance_criteria>
  <done>
    chart-colors.ts 가 ChartPalette 인터페이스 + getChartPalette('light' | 'dark') 함수를 export. 모든 5개 키가 hex 문자열, light/dark 가 서로 다름. 한국식 색상 컨벤션 (up=적, down=청) 준수. 단위 테스트 4건 PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: daily-ohlcv-api.ts — fetchDailyOhlcv (AbortSignal 추가) + rangeToFromDate 정식 모듈 promote</name>
  <files>
    webapp/src/lib/daily-ohlcv-api.ts,
    webapp/src/lib/__tests__/daily-ohlcv-api.test.ts
  </files>
  <read_first>
    - webapp/src/app/mockups/shared/fetch-daily.ts (전체 — 본 task 의 mockup → production promote 대상)
    - webapp/src/lib/watchlist-api.ts (Supabase 직접 호출 + 타입 변환 패턴 mirror)
    - webapp/src/lib/supabase/client.ts (createClient 시그너처)
    - .planning/phases/09.2-stock-detail-daily-chart/09.2-CONTEXT.md §D-12, D-13, D-14
    - .planning/phases/09.2-stock-detail-daily-chart/09.2-RESEARCH.md §"Pitfall 7" (.order ascending 필수)
    - packages/shared/src/dailyOhlcv.ts (Task 2 산출물 — 본 task 의 import 원천)
  </read_first>
  <behavior>
    - rangeToFromDate('1M', new Date('2026-05-15')) === '2026-03-16' (60일 전)
    - rangeToFromDate('3M', ...) === today − 120일
    - rangeToFromDate('6M', ...) === today − 220일
    - rangeToFromDate('1Y', ...) === today − 400일
    - fetchDailyOhlcv 가 createClient → from('stock_daily_ohlcv') → select(8 컬럼) → eq('code', code) → gte('date', from) → order('date', ascending: true) → abortSignal(signal) 체인 호출
    - Supabase error 발생 시 throw
    - signal.aborted 가 이미 true 면 AbortError throw (선제 가드 — 옵션. abortSignal builder 가 처리하므로 필수 아님)
    - 응답 row 의 numeric 필드 (open/high/low/close/volume/change_amount/change_rate) 가 string 으로 와도 Number() 변환
    - change_amount/change_rate null 유지
  </behavior>
  <action>
    1. `webapp/src/lib/daily-ohlcv-api.ts` 신규 파일:
       ```typescript
       /**
        * Phase 09.2 — 일봉 OHLCV fetch (webapp → Supabase 직접 호출).
        *
        * RESEARCH 단계의 webapp/src/app/mockups/shared/fetch-daily.ts 를 정식 promote.
        * 추가/변경:
        *   - AbortSignal 파라미터 (D-14 — 이전 요청 취소, Phase 6 패턴 승계)
        *   - 공유 타입은 @gh-radar/shared 의 DailyOhlcvRow / DailyOhlcvRangeKey 사용 (D-15)
        *   - Pitfall 7 (PostgREST 정렬 미명시 시 캔들 역순) 방어를 위해 .order('date', asc) 명시
        *   - Pitfall 10 (RLS authenticated 누락) 은 본 phase 의 마이그레이션
        *     20260515163000_fix_stock_daily_ohlcv_rls_authenticated.sql 에서 fix 완료 —
        *     anon + authenticated 양쪽 모두 SELECT 허용
        */

       'use client';

       import {
         DAILY_OHLCV_RANGES,
         type DailyOhlcvRangeKey,
         type DailyOhlcvRow,
       } from '@gh-radar/shared';
       import { createClient } from '@/lib/supabase/client';

       interface RawRow {
         date: string;
         open: number | string;
         high: number | string;
         low: number | string;
         close: number | string;
         volume: number | string;
         change_amount: number | string | null;
         change_rate: number | string | null;
       }

       /** range → 시작일 (calendar day 마진 — 휴장일/주말 흡수). */
       const RANGE_TO_DAYS: Record<DailyOhlcvRangeKey, number> = {
         '1M': 60,
         '3M': 120,
         '6M': 220,
         '1Y': 400,
       };

       /**
        * range → ISO YYYY-MM-DD (today − margin days).
        * `today` 인자는 테스트에서 결정론적 검증을 위해 주입 가능.
        */
       export function rangeToFromDate(
         range: DailyOhlcvRangeKey,
         today: Date = new Date(),
       ): string {
         const days = RANGE_TO_DAYS[range];
         const d = new Date(today);
         d.setDate(d.getDate() - days);
         return d.toISOString().slice(0, 10);
       }

       /**
        * 종목 1개의 일봉 OHLCV 를 from~today 범위로 조회.
        *
        * - 시간 ASC 정렬 (lightweight-charts 가 ASC 가정 — Pitfall 7).
        * - AbortSignal 로 이전 요청 취소 가능. abort 시 Supabase 가 AbortError throw.
        */
       export async function fetchDailyOhlcv(
         code: string,
         range: DailyOhlcvRangeKey,
         signal?: AbortSignal,
       ): Promise<DailyOhlcvRow[]> {
         const supabase = createClient();
         const from = rangeToFromDate(range);

         let query = supabase
           .from('stock_daily_ohlcv')
           .select(
             'date, open, high, low, close, volume, change_amount, change_rate',
           )
           .eq('code', code)
           .gte('date', from)
           .order('date', { ascending: true });

         if (signal) {
           query = query.abortSignal(signal);
         }

         const { data, error } = await query;
         if (error) throw error;
         if (!data) return [];

         return (data as RawRow[]).map((r) => ({
           date: r.date,
           open: Number(r.open),
           high: Number(r.high),
           low: Number(r.low),
           close: Number(r.close),
           volume: Number(r.volume),
           changeAmount: r.change_amount === null ? null : Number(r.change_amount),
           changeRate: r.change_rate === null ? null : Number(r.change_rate),
         }));
       }

       /** 외부 컴포넌트에서 토글 iterate 용 — packages/shared 의 readonly tuple 재export. */
       export { DAILY_OHLCV_RANGES };
       export type { DailyOhlcvRangeKey, DailyOhlcvRow };
       ```
    2. `webapp/src/lib/__tests__/daily-ohlcv-api.test.ts` 신규 — Supabase 클라이언트는 mock:
       ```typescript
       import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';

       // ── createClient mock — fetchDailyOhlcv 호출 체인 검증 ──
       const orderMock = vi.fn();
       const abortSignalMock = vi.fn(() => ({ then: orderMock }));
       const gteMock = vi.fn(() => ({
         order: (col: string, opt: { ascending: boolean }) => {
           // chain 끝의 await 가 fetchDailyOhlcv 안에서 await query 로 풀리도록 thenable
           return {
             abortSignal: (s: AbortSignal) => {
               recordedAbortSignal = s;
               return finalThenable;
             },
             then: (resolve: (v: unknown) => void) => resolve(finalResolved),
           };
         },
       }));
       const eqMock = vi.fn(() => ({ gte: gteMock }));
       const selectMock = vi.fn(() => ({ eq: eqMock }));
       const fromMock = vi.fn(() => ({ select: selectMock }));

       let recordedAbortSignal: AbortSignal | null = null;
       let finalResolved: { data: unknown; error: unknown } = {
         data: [],
         error: null,
       };
       const finalThenable = {
         then: (resolve: (v: unknown) => void) => resolve(finalResolved),
       };

       vi.mock('@/lib/supabase/client', () => ({
         createClient: () => ({ from: fromMock }),
       }));

       import {
         fetchDailyOhlcv,
         rangeToFromDate,
       } from '../daily-ohlcv-api';

       beforeEach(() => {
         finalResolved = { data: [], error: null };
         recordedAbortSignal = null;
         fromMock.mockClear();
         selectMock.mockClear();
         eqMock.mockClear();
         gteMock.mockClear();
       });

       afterEach(() => {
         vi.restoreAllMocks();
       });

       describe('rangeToFromDate', () => {
         const today = new Date('2026-05-15T00:00:00Z');

         it.each([
           ['1M', '2026-03-16'],
           ['3M', '2026-01-15'],
           ['6M', '2025-10-07'],
           ['1Y', '2025-04-10'],
         ] as const)('range %s → today − N일 = %s', (range, expected) => {
           expect(rangeToFromDate(range, today)).toBe(expected);
         });

         it('today 인자 미주입 시 현재 시각 기준 호출 가능 (smoke)', () => {
           const result = rangeToFromDate('1M');
           expect(result).toMatch(/^\d{4}-\d{2}-\d{2}$/);
         });
       });

       describe('fetchDailyOhlcv', () => {
         it('Supabase chain 을 stock_daily_ohlcv → eq(code) → gte(date) → order(ascending) 순으로 호출', async () => {
           finalResolved = {
             data: [
               {
                 date: '2026-05-14',
                 open: '70000',
                 high: '71000',
                 low: '69500',
                 close: '70500',
                 volume: '12345',
                 change_amount: '500',
                 change_rate: '0.71',
               },
             ],
             error: null,
           };

           const rows = await fetchDailyOhlcv('005930', '1M');

           expect(fromMock).toHaveBeenCalledWith('stock_daily_ohlcv');
           expect(selectMock).toHaveBeenCalledWith(
             'date, open, high, low, close, volume, change_amount, change_rate',
           );
           expect(eqMock).toHaveBeenCalledWith('code', '005930');
           expect(gteMock).toHaveBeenCalledTimes(1);
           expect(rows).toHaveLength(1);
           expect(rows[0].open).toBe(70000); // string → number 변환
           expect(rows[0].changeRate).toBe(0.71);
         });

         it('change_amount / change_rate null 유지', async () => {
           finalResolved = {
             data: [
               {
                 date: '2026-05-14',
                 open: 1000,
                 high: 1100,
                 low: 950,
                 close: 1050,
                 volume: 100,
                 change_amount: null,
                 change_rate: null,
               },
             ],
             error: null,
           };

           const rows = await fetchDailyOhlcv('900001', '1M');
           expect(rows[0].changeAmount).toBeNull();
           expect(rows[0].changeRate).toBeNull();
         });

         it('AbortSignal 주입 시 Supabase abortSignal() 호출 chain 진입', async () => {
           const ctrl = new AbortController();
           await fetchDailyOhlcv('005930', '1M', ctrl.signal);
           expect(recordedAbortSignal).toBe(ctrl.signal);
         });

         it('Supabase error → throw', async () => {
           finalResolved = { data: null, error: new Error('PostgREST 401') };
           await expect(fetchDailyOhlcv('005930', '1M')).rejects.toThrow(
             'PostgREST 401',
           );
         });

         it('data === null 응답을 빈 배열로 정규화', async () => {
           finalResolved = { data: null, error: null };
           const rows = await fetchDailyOhlcv('005930', '1M');
           expect(rows).toEqual([]);
         });
       });
       ```

       주의: 위 mock 체인은 Supabase JS v2 의 chainable thenable 패턴을 단순화한 fake — fetchDailyOhlcv 의 실제 chain 호출 순서와 fluent API 만 검증한다. PostgREST 응답 schema 의 정확한 행동은 production smoke 와 Plan 03 E2E 가 확인.

    3. RED → GREEN 사이클: 모듈 + 테스트 동시 작성, vitest PASS 확인.

    Acceptance time horizon — 60초 이내 vitest 완료.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp test --run -- daily-ohlcv-api</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @gh-radar/webapp test --run -- daily-ohlcv-api` exit code 0, 9 tests PASS (rangeToFromDate it.each 4 + smoke 1 + fetchDailyOhlcv 4 = 9건; it.each 가 4건으로 계산되므로 총 ≥ 8건이면 OK)
    - `pnpm --filter @gh-radar/webapp typecheck` exit code 0
    - `grep "abortSignal\|AbortSignal" webapp/src/lib/daily-ohlcv-api.ts` 결과 ≥ 2건
    - `grep "ascending: true" webapp/src/lib/daily-ohlcv-api.ts` 결과 1건 (Pitfall 7 가드)
    - `grep "@gh-radar/shared" webapp/src/lib/daily-ohlcv-api.ts` 결과 1건 (D-15 통합)
  </acceptance_criteria>
  <done>
    daily-ohlcv-api.ts 가 fetchDailyOhlcv(code, range, signal?) + rangeToFromDate(range, today?) 두 함수를 export. AbortSignal 통합, 시간 ASC 정렬, numeric 변환, null 유지 모두 단위 테스트로 검증. mockup3 의 fetch-daily.ts 와 동일 동작 + production-grade 강화. 후속 plan 02 의 차트 컴포넌트가 import 가능 상태.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase PostgREST | 본 phase 의 모든 데이터 흐름. webapp 의 createClient (anon key) 가 stock_daily_ohlcv 를 직접 SELECT. RLS 가 anon + authenticated 양쪽 모두 허용 (마이그레이션 20260515163000) |
| 브라우저 DOM → 차트 라이브러리 | 본 plan 은 미해당 (Plan 02 가 다룸). 본 plan 은 utility 만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-01 | Information Disclosure | webapp/src/lib/daily-ohlcv-api.ts (Supabase 호출) | mitigate | (1) URL 화이트리스트 — `from('stock_daily_ohlcv')` literal 만 사용, 사용자 입력 테이블명 금지. (2) 필터 화이트리스트 — `eq('code', code)` + `gte('date', from)` 만, 임의 컬럼/연산자 입력 금지. (3) RLS 정책이 anon/authenticated SELECT 만 허용 — UPDATE/DELETE 자동 차단 |
| T-09.2-02 | Tampering | rangeToFromDate(range) 의 range 파라미터 | mitigate | DailyOhlcvRangeKey union 타입으로 컴파일 타임 제한. RANGE_TO_DAYS lookup 이 미정의 키에 대해 undefined 반환 → setDate(NaN) → 'Invalid Date' ISO. 본 plan 은 추가 가드 없이 타입에 위임. UI 측 (Plan 02) 도 DAILY_OHLCV_RANGES 만 iterate |
| T-09.2-03 | Denial of Service | fetchDailyOhlcv 가 7일 범위가 아닌 1Y (~252 row) 까지 fetch | accept | 1Y 범위 ≈ 1.5KB 페이로드, Supabase 인덱스 (code, date DESC) 가 자연 처리. AbortSignal 로 빠른 취소 가능. 추가 rate limiting 불필요 |
| T-09.2-04 | Information Disclosure | code 파라미터에 SQL/PostgREST injection 시도 | mitigate | Supabase JS client 가 .eq() value 를 자동 escape. 추가로 stock-detail page (StockPage) 가 이미 정규식 `/^[A-Za-z0-9]{1,10}$/` 으로 code 검증 후 props 전달 — 본 plan 의 fetchDailyOhlcv 진입 시점에는 이미 sanitized |

모든 위협 LOW (1건) ~ MEDIUM (3건). HIGH 없음 — block on high 정책 통과.
</threat_model>

<verification>
- `pnpm --filter @gh-radar/shared test --run` 전체 PASS (기존 테스트 회귀 + 신규 dailyOhlcv 3건)
- `pnpm --filter @gh-radar/webapp test --run` 전체 PASS (기존 + 신규 chart-colors 4건 + daily-ohlcv-api 9건)
- `pnpm --filter @gh-radar/shared typecheck && pnpm --filter @gh-radar/webapp typecheck` 양쪽 exit 0
- `grep "DATA-03" .planning/REQUIREMENTS.md` 결과 ≥ 2건
- `grep -n "캔들스틱 차트" .planning/REQUIREMENTS.md` 결과 0건
</verification>

<success_criteria>
- F-01 / F-02 문서 정합성 완료 — REQUIREMENTS Out of Scope 캔들 행 삭제 + DATA-03 등재 + PROJECT Key Decisions 갱신 + ROADMAP plan 목록 갱신
- packages/shared 가 DailyOhlcvRow + DailyOhlcvRangeKey + DAILY_OHLCV_RANGES 3종 export
- webapp/src/lib/chart-colors.ts 가 getChartPalette('light' | 'dark') 노출 — Pitfall 9 회귀 방지 utility 모듈화 완료
- webapp/src/lib/daily-ohlcv-api.ts 가 fetchDailyOhlcv + rangeToFromDate 노출 + AbortSignal 통합 + Pitfall 7 가드 (`ascending: true`)
- 4개 모듈에 16건 이상의 단위 테스트 PASS (Wave 0 Nyquist 충족)
- 본 plan 의 모든 변경이 후속 Plan 02 (차트 UI 컴포넌트) 의 import 만으로 동작 가능
</success_criteria>

<output>
After completion, create `.planning/phases/09.2-stock-detail-daily-chart/09.2-01-SUMMARY.md` with:
- 4개 신규 모듈의 export signature 요약
- 16+ unit tests 의 PASS 요약 (per-suite 라인 수)
- F-01 / F-02 문서 변경 위치 (line numbers + diff snippet)
- 후속 Plan 02 가 사용할 import 경로 (`@gh-radar/shared`, `@/lib/chart-colors`, `@/lib/daily-ohlcv-api`)
- Pitfall 9 회귀 방지 검증 (chart-colors.ts 에 oklch 코드 라인 0건)
</output>
