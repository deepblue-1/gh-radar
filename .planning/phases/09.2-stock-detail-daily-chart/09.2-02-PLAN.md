---
phase: 09.2
plan: 02
type: execute
wave: 2
depends_on:
  - "09.2-01"
files_modified:
  - webapp/src/components/stock/stock-daily-chart.tsx
  - webapp/src/components/stock/stock-daily-chart-section.tsx
  - webapp/src/components/stock/stock-daily-chart-skeleton.tsx
  - webapp/src/components/stock/__tests__/stock-daily-chart-section.test.tsx
autonomous: true
requirements:
  - DATA-03
user_setup: []

must_haves:
  truths:
    - "StockDailyChartSection 이 'use client' 클라이언트 컴포넌트로 종목 code + 옵셔널 refreshSignal prop 을 받아 자체 fetch 라이프사이클을 갖는다 (D-09 위치는 Plan 03 에서 mount, D-19 새로고침 연동)"
    - "마운트 시 fetchDailyOhlcv(code, '1M') 가 호출되고 결과가 lightweight-charts 차트에 그려진다 (D-04 기본 1M)"
    - "1M / 3M / 6M / 1Y 토글 버튼이 카드 헤더에 표시되고, 클릭 시 차트가 새 range 로 refetch + 재렌더된다 (D-05)"
    - "다크/라이트 테마 토글 시 (next-themes useTheme) chart.applyOptions 가 재호출되어 차트 색상이 즉시 분기된다 (Pitfall 6 fix, D-22)"
    - "데이터 로딩 중 Skeleton 이 차트 영역 위에 표시된다 (D-16, Pitfall 5: container 항상 visible)"
    - "응답 row 0건이면 'Empty state' 안내 카드가 표시된다 (D-17)"
    - "Supabase fetch 실패 시 인라인 에러 메시지 + '다시 시도' 버튼이 표시되고 차트 영역은 stale-but-visible 유지 (D-18)"
    - "오늘자 row 가 응답에 포함된 경우 '장중' 라벨이 카드 헤더에 표시된다 (D-20)"
    - "screen reader 가 읽을 수 있는 sr-only 요약 텍스트 + aria-label='일봉 차트' 가 존재한다 (D-21)"
    - "모든 캔들/Volume 색상이 chart-colors.ts 의 hex 팔레트에서 오며, oklch 값이 차트 lib 에 직접 주입되지 않는다 (Pitfall 9 회귀 방지)"
    - "AbortController 가 range 변경 시 이전 fetch 를 취소한다 (Phase 6 패턴 + D-14)"
    - "refreshSignal prop 값이 변경되면 동일 range 로 차트가 refetch 된다 (D-19 — StockDetailClient 새로고침 버튼 ↔ 차트 fetch 연동)"
    - "단위 테스트 (vitest + jsdom) 가 Skeleton → 차트 컨테이너 / Empty / Error 3가지 분기 모두 검증한다"
  artifacts:
    - path: "webapp/src/components/stock/stock-daily-chart.tsx"
      provides: "lightweight-charts 인스턴스 lifecycle 만 담당하는 presentational 차트 컴포넌트 (rows + theme prop)"
      exports: ["StockDailyChart"]
      min_lines: 90
    - path: "webapp/src/components/stock/stock-daily-chart-section.tsx"
      provides: "fetch + range 토글 + 상태 분기 + 카드 컨테이너 — StockDetailClient 가 mount 할 단일 진입점"
      exports: ["StockDailyChartSection"]
      min_lines: 120
    - path: "webapp/src/components/stock/stock-daily-chart-skeleton.tsx"
      provides: "차트 카드 영역 전용 Skeleton — 차트 height 와 동일"
      exports: ["StockDailyChartSkeleton"]
    - path: "webapp/src/components/stock/__tests__/stock-daily-chart-section.test.tsx"
      provides: "Skeleton/Empty/Error/range-toggle 단위 테스트 (lightweight-charts 는 mock)"
  key_links:
    - from: "webapp/src/components/stock/stock-daily-chart-section.tsx"
      to: "@/lib/daily-ohlcv-api"
      via: "fetchDailyOhlcv import"
      pattern: "fetchDailyOhlcv"
    - from: "webapp/src/components/stock/stock-daily-chart.tsx"
      to: "lightweight-charts"
      via: "createChart + addSeries(CandlestickSeries) + addSeries(HistogramSeries)"
      pattern: "createChart|CandlestickSeries|HistogramSeries"
    - from: "webapp/src/components/stock/stock-daily-chart.tsx"
      to: "@/lib/chart-colors"
      via: "getChartPalette(theme) 호출"
      pattern: "getChartPalette"
    - from: "webapp/src/components/stock/stock-daily-chart.tsx"
      to: "next-themes"
      via: "useTheme() 훅 + useEffect 로 theme 변경 시 chart.applyOptions"
      pattern: "useTheme"
---

<objective>
Plan 01 의 모듈 (daily-ohlcv-api / chart-colors / packages/shared 타입) 을 조합해 **자기완결 차트 섹션 컴포넌트** 를 구축한다. RESEARCH 단계의 mockup3 (`webapp/src/app/mockups/chart-lightweight/page.tsx`) 를 정식 컴포넌트 3종으로 분해하면서 Post-Deploy 디버그에서 발견된 **Pitfall 6 (다크모드 자동 분기 미작동)** 을 next-themes useTheme + chart.applyOptions effect 로 production-grade 해결한다. StockDetailClient 와의 통합은 본 plan 미포함 — Plan 03 가 mount.

Purpose: lightweight-charts 의 명령형 API + Canvas 라이프사이클이 React 컴포넌트 안에서 안전하게 동작하도록 (mount 시 1회 createChart, theme 변경 시 applyOptions, rows 변경 시 setData, unmount 시 chart.remove) 3개 useEffect 를 분리하고 Skeleton/Empty/Error 3분기를 누락 없이 처리한다.
Output: 3개 신규 컴포넌트 + 1개 테스트 파일.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.2-stock-detail-daily-chart/09.2-CONTEXT.md
@.planning/phases/09.2-stock-detail-daily-chart/09.2-RESEARCH.md
@.planning/phases/09.2-stock-detail-daily-chart/09.2-VALIDATION.md
@.planning/phases/09.2-stock-detail-daily-chart/09.2-01-PLAN.md

@webapp/src/app/mockups/chart-lightweight/page.tsx
@webapp/src/components/stock/stock-detail-client.tsx
@webapp/src/components/stock/stock-detail-skeleton.tsx
@webapp/src/components/stock/coming-soon-card.tsx
@webapp/src/components/ui/card.tsx
@webapp/src/components/ui/skeleton.tsx
@webapp/src/components/ui/button.tsx
@webapp/src/components/providers/theme-provider.tsx
@webapp/src/components/layout/theme-toggle.tsx

<interfaces>
<!-- Plan 01 산출물 — 본 plan 이 import -->

import { fetchDailyOhlcv, rangeToFromDate } from '@/lib/daily-ohlcv-api';
import { getChartPalette, type ChartPalette } from '@/lib/chart-colors';
import {
  type DailyOhlcvRow,
  type DailyOhlcvRangeKey,
  DAILY_OHLCV_RANGES,
} from '@gh-radar/shared';

// next-themes (이미 webapp 에 설치, theme-provider.tsx 사용 중)
import { useTheme } from 'next-themes';

// lightweight-charts 5.2.0 (이미 webapp/package.json 의존)
import {
  createChart,
  CandlestickSeries,
  HistogramSeries,
  type IChartApi,
  type ISeriesApi,
  type Time,
} from 'lightweight-charts';

<!-- 본 plan 이 export 하는 contract -->

export interface StockDailyChartProps {
  /** 시간 ASC 정렬된 rows. 빈 배열이면 차트 인스턴스만 생성하고 setData 안 함 */
  rows: DailyOhlcvRow[];
  /** Skeleton/Empty 오버레이 시에도 chart container 는 항상 visible (Pitfall 5) */
  height?: number; // default 340
}

export interface StockDailyChartSectionProps {
  /** 종목 단축코드 (StockPage 가 정규식 검증 후 전달) */
  code: string;
  /**
   * D-19 — 부모 (StockDetailClient) 의 새로고침 버튼 클릭 시 값을 변경하면
   * 차트가 동일 range 로 refetch 한다. number / Date.now() / counter 무엇이든
   * primitive 가 바뀌면 useEffect 의존성 발화. 미주입 시 차트는 mount + range 변경
   * 만으로 fetch (기존 동작 보존).
   */
  refreshSignal?: unknown;
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StockDailyChart — lightweight-charts 명령형 lifecycle wrapper (다크모드 effect 포함)</name>
  <files>
    webapp/src/components/stock/stock-daily-chart.tsx,
    webapp/src/components/stock/stock-daily-chart-skeleton.tsx
  </files>
  <read_first>
    - webapp/src/app/mockups/chart-lightweight/page.tsx (전체 — 본 task 의 promote source. 특히 92~159행 chart create useEffect, 162~181행 setData useEffect)
    - webapp/src/components/providers/theme-provider.tsx (next-themes 통합 패턴)
    - webapp/src/components/layout/theme-toggle.tsx (useTheme 사용 예)
    - .planning/phases/09.2-stock-detail-daily-chart/09.2-RESEARCH.md §"Pitfall 5" (display:none 금지) §"Pitfall 6" (다크모드 applyOptions) §"Pitfall 9" (oklch 거부) §Code Examples §"옵션 3"
    - webapp/src/lib/chart-colors.ts (Plan 01 산출물 — Task 본문에서 import)
  </read_first>
  <behavior>
    - mount 시 1회 createChart + addSeries(CandlestickSeries) + addSeries(HistogramSeries) + priceScale 설정 → unmount 시 chart.remove() — 매 render 재생성 금지
    - rows prop 변경 시 setData (chart 재생성 금지) — rows.length === 0 이면 setData 호출 스킵
    - useTheme() 의 resolvedTheme 변경 시 chart.applyOptions + 모든 series.applyOptions 재호출 (palette 새로 주입)
    - 차트 container 는 항상 visible — Skeleton/Empty 오버레이는 부모 컴포넌트(StockDailyChartSection) 책임 (Pitfall 5)
    - 모든 색상이 getChartPalette(theme) 에서 옴 — 컴포넌트 내부에 hex 리터럴 없음 (Pitfall 9 회귀 방지: oklch literal 도 없음)
    - Volume bar 색상은 candle 양봉/음봉과 동기화 (D-03)
    - StockDailyChartSkeleton 은 height={340} 동일 사이즈 + animate-pulse Skeleton 1개
  </behavior>
  <action>
    1. `webapp/src/components/stock/stock-daily-chart-skeleton.tsx` 신규:
       ```typescript
       import { Skeleton } from '@/components/ui/skeleton';

       export interface StockDailyChartSkeletonProps {
         height?: number;
       }

       /**
        * Phase 09.2 D-16 — 차트 카드 본문 영역 Skeleton.
        * 차트와 동일 height 로 CLS 최소화. 부모(Card) 의 padding 안에 들어간다.
        */
       export function StockDailyChartSkeleton({
         height = 340,
       }: StockDailyChartSkeletonProps) {
         return (
           <Skeleton
             aria-busy="true"
             aria-label="일봉 차트 로딩 중"
             className="w-full"
             style={{ height: `${height}px` }}
           />
         );
       }
       ```

    2. `webapp/src/components/stock/stock-daily-chart.tsx` 신규 — mockup3 의 chart effect 를 promote + 다크모드 effect 추가:
       ```typescript
       'use client';

       /**
        * Phase 09.2 — lightweight-charts 5.2.0 wrapper.
        *
        * 책임 분리:
        *   - 본 컴포넌트: chart 인스턴스 lifecycle + theme/rows 반응
        *   - 부모 (StockDailyChartSection): fetch + 토글 + Skeleton/Empty/Error 분기
        *
        * Pitfall 정리 (RESEARCH §Pitfalls):
        *   - Pitfall 5: container 는 항상 visible. Skeleton/Empty 는 부모가 absolute overlay 로.
        *   - Pitfall 6: theme 변경 시 chart.applyOptions + series.applyOptions 재호출 (next-themes useTheme)
        *   - Pitfall 8: 'use client' + 모든 lib 호출은 useEffect 안에서만
        *   - Pitfall 9: 색상은 chart-colors.ts 의 hex 만, oklch 직접 주입 금지
        */

       import { useEffect, useRef } from 'react';
       import { useTheme } from 'next-themes';
       import {
         CandlestickSeries,
         HistogramSeries,
         createChart,
         type CandlestickData,
         type HistogramData,
         type IChartApi,
         type ISeriesApi,
         type Time,
       } from 'lightweight-charts';
       import type { DailyOhlcvRow } from '@gh-radar/shared';
       import { getChartPalette, type ChartPalette } from '@/lib/chart-colors';

       export interface StockDailyChartProps {
         rows: DailyOhlcvRow[];
         height?: number;
       }

       function resolvePaletteKey(
         resolvedTheme: string | undefined,
       ): 'light' | 'dark' {
         return resolvedTheme === 'light' ? 'light' : 'dark';
       }

       function applyPaletteToChart(
         chart: IChartApi,
         candle: ISeriesApi<'Candlestick'>,
         volume: ISeriesApi<'Histogram'>,
         palette: ChartPalette,
       ): void {
         chart.applyOptions({
           layout: { background: { color: palette.bg }, textColor: palette.text },
           grid: {
             vertLines: { color: palette.grid },
             horzLines: { color: palette.grid },
           },
           rightPriceScale: { borderColor: palette.grid },
           timeScale: { borderColor: palette.grid },
         });
         candle.applyOptions({
           upColor: palette.up,
           downColor: palette.down,
           borderUpColor: palette.up,
           borderDownColor: palette.down,
           wickUpColor: palette.up,
           wickDownColor: palette.down,
         });
         // Volume bar 색상은 setData 의 per-bar color 로 분기 (rows effect 가 처리)
         void volume;
       }

       export function StockDailyChart({
         rows,
         height = 340,
       }: StockDailyChartProps) {
         const { resolvedTheme } = useTheme();

         const containerRef = useRef<HTMLDivElement | null>(null);
         const chartRef = useRef<IChartApi | null>(null);
         const candleSeriesRef = useRef<ISeriesApi<'Candlestick'> | null>(null);
         const volumeSeriesRef = useRef<ISeriesApi<'Histogram'> | null>(null);

         // 1) chart 인스턴스 mount/unmount 1회. 초기 palette 는 dark default
         //    (theme effect 가 2번째 tick 에서 정확값으로 재주입)
         useEffect(() => {
           const container = containerRef.current;
           if (!container) return;
           const palette = getChartPalette('dark');
           const chart = createChart(container, {
             autoSize: true,
             layout: {
               background: { color: palette.bg },
               textColor: palette.text,
               fontFamily:
                 'Pretendard Variable, Pretendard, system-ui, sans-serif',
             },
             grid: {
               vertLines: { color: palette.grid, style: 1 },
               horzLines: { color: palette.grid, style: 1 },
             },
             rightPriceScale: { borderColor: palette.grid },
             timeScale: { borderColor: palette.grid, timeVisible: false },
             crosshair: { mode: 1 },
           });

           // v5 breaking change — addSeries(SeriesDefinition, options) 통합 패턴 (Pitfall 4)
           const candleSeries = chart.addSeries(CandlestickSeries, {
             upColor: palette.up,
             downColor: palette.down,
             borderUpColor: palette.up,
             borderDownColor: palette.down,
             wickUpColor: palette.up,
             wickDownColor: palette.down,
           });
           const volumeSeries = chart.addSeries(HistogramSeries, {
             priceFormat: { type: 'volume' },
             priceScaleId: 'volume',
           });
           chart
             .priceScale('volume')
             .applyOptions({ scaleMargins: { top: 0.75, bottom: 0 } });
           chart
             .priceScale('right')
             .applyOptions({ scaleMargins: { top: 0.05, bottom: 0.3 } });

           chartRef.current = chart;
           candleSeriesRef.current = candleSeries;
           volumeSeriesRef.current = volumeSeries;

           return () => {
             chart.remove();
             chartRef.current = null;
             candleSeriesRef.current = null;
             volumeSeriesRef.current = null;
           };
         }, []);

         // 2) theme 변경 시 chart + series 색상 재주입 (Pitfall 6)
         useEffect(() => {
           const chart = chartRef.current;
           const candle = candleSeriesRef.current;
           const volume = volumeSeriesRef.current;
           if (!chart || !candle || !volume) return;
           const palette = getChartPalette(resolvePaletteKey(resolvedTheme));
           applyPaletteToChart(chart, candle, volume, palette);

           // Volume bar 의 per-bar color 도 현재 rows 기준 재주입 (theme 변경만으로
           //   rows effect 가 안 돌므로 직접 호출).
           if (rows.length > 0) {
             const volumeData: HistogramData<Time>[] = rows.map((r) => ({
               time: r.date as Time,
               value: r.volume,
               color: r.close >= r.open ? palette.up : palette.down,
             }));
             volume.setData(volumeData);
           }
         }, [resolvedTheme, rows]);

         // 3) rows 변경 시 candle + volume setData
         useEffect(() => {
           const candle = candleSeriesRef.current;
           const volume = volumeSeriesRef.current;
           const chart = chartRef.current;
           if (!candle || !volume || !chart) return;
           if (rows.length === 0) {
             candle.setData([]);
             volume.setData([]);
             return;
           }
           const palette = getChartPalette(resolvePaletteKey(resolvedTheme));
           const candleData: CandlestickData<Time>[] = rows.map((r) => ({
             time: r.date as Time,
             open: r.open,
             high: r.high,
             low: r.low,
             close: r.close,
           }));
           const volumeData: HistogramData<Time>[] = rows.map((r) => ({
             time: r.date as Time,
             value: r.volume,
             color: r.close >= r.open ? palette.up : palette.down,
           }));
           candle.setData(candleData);
           volume.setData(volumeData);
           chart.timeScale().fitContent();
         }, [rows, resolvedTheme]);

         return (
           <div
             ref={containerRef}
             data-testid="stock-daily-chart-canvas"
             className="h-full w-full"
             style={{ height: `${height}px` }}
           />
         );
       }
       ```

    3. 본 task 의 단위 테스트는 Task 2 의 stock-daily-chart-section.test.tsx 에서 lightweight-charts 를 mock 하면서 함께 검증 (별도 .test 파일 추가 안 함 — chart 컴포넌트는 명령형 lib wrapper 라 단위 테스트 가치가 낮고, 통합 테스트가 더 의미 있음).

    Acceptance — typecheck PASS + 코드 grep 패턴 매칭으로 검증.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @gh-radar/webapp typecheck` exit code 0
    - `grep -c "useEffect" webapp/src/components/stock/stock-daily-chart.tsx` ≥ 3 (mount/theme/rows 3개 effect)
    - `grep "useTheme" webapp/src/components/stock/stock-daily-chart.tsx` 결과 ≥ 1건
    - `grep "getChartPalette" webapp/src/components/stock/stock-daily-chart.tsx` 결과 ≥ 3건 (mount + theme effect + rows effect)
    - `grep "chart.remove" webapp/src/components/stock/stock-daily-chart.tsx` 결과 ≥ 1건 (cleanup)
    - `grep -E '#[0-9a-fA-F]{6}|oklch' webapp/src/components/stock/stock-daily-chart.tsx` 결과 0건 (모든 색상은 chart-colors 위임)
    - `grep "addSeries(CandlestickSeries" webapp/src/components/stock/stock-daily-chart.tsx` 결과 1건 (Pitfall 4 — v5 패턴)
    - `wc -l webapp/src/components/stock/stock-daily-chart.tsx` 결과 ≥ 90행
    - `grep "data-testid=\"stock-daily-chart-canvas\"" webapp/src/components/stock/stock-daily-chart.tsx` 결과 ≥ 1건 (E2E selector 매치를 위한 source-level testid 부여 — Plan 03 spec 와 동기)
  </acceptance_criteria>
  <done>
    StockDailyChart 컴포넌트가 lightweight-charts chart 인스턴스를 mount/unmount 1회 lifecycle 로 관리, theme/rows 변경에 반응, 색상은 chart-colors 위임. StockDailyChartSkeleton 가 동일 height 로 CLS 최소화. webapp typecheck PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StockDailyChartSection — fetch + 토글 + Skeleton/Empty/Error/장중 라벨/sr-only 통합</name>
  <files>
    webapp/src/components/stock/stock-daily-chart-section.tsx,
    webapp/src/components/stock/__tests__/stock-daily-chart-section.test.tsx
  </files>
  <read_first>
    - webapp/src/components/stock/stock-daily-chart.tsx (Task 1 산출물 — import 대상)
    - webapp/src/components/stock/stock-daily-chart-skeleton.tsx (Task 1 산출물)
    - webapp/src/app/mockups/chart-lightweight/page.tsx (183~256행 — 카드 헤더 + 토글 + Skeleton/Empty 오버레이 패턴 promote)
    - webapp/src/components/stock/stock-detail-client.tsx (49~73행 load 콜백 + AbortController 패턴 mirror)
    - webapp/src/components/ui/card.tsx (Card 컨테이너)
    - webapp/src/components/ui/button.tsx (재시도 버튼)
    - .planning/phases/09.2-stock-detail-daily-chart/09.2-CONTEXT.md §D-10/D-16/D-17/D-18/D-20/D-21
  </read_first>
  <behavior>
    - mount 시 fetchDailyOhlcv(code, '1M') 호출 → 응답까지 Skeleton 표시
    - 응답 도착 후 차트 container 가 항상 visible (Pitfall 5) + rows 0건이면 Empty overlay
    - range 토글 클릭 시 setRange + 새 fetch (이전 AbortController abort)
    - error 시 인라인 에러 카드 (D-18) + "다시 시도" 버튼 (현 range 로 재호출)
    - 오늘자 row (rows[rows.length-1].date === today KST) 가 포함된 경우 카드 헤더에 "장중" 배지/라벨 표시 (D-20)
    - sr-only 요약: "최근 N영업일 종가 추이. 시작 종가 X원, 현재 종가 Y원" (rows.length > 0 일 때만)
    - aria-label="일봉 차트" Card 또는 section 에 부착 (D-21)
    - 컴포넌트 unmount 시 controllerRef.abort() (Phase 6 패턴)
    - 단위 테스트:
      * Skeleton 분기: fetchDailyOhlcv pending 상태에서 Skeleton 노출
      * Data 분기: rows 응답 후 chart container (data-testid) 노출
      * Empty 분기: 빈 배열 응답 시 "일봉 데이터가 아직 수집되지 않았습니다" 카피 노출
      * Error 분기: throw 시 에러 메시지 + "다시 시도" 버튼 노출
      * Range toggle: 1M → 3M 클릭 시 fetchDailyOhlcv 가 두 번째로 '3M' 인자로 호출됨
      * 장중 라벨 (D-20): vi.useFakeTimers + setSystemTime 으로 today 강제, 마지막 row.date === today → "장중" 배지 노출
      * refreshSignal (D-19): rerender 로 prop 값 변경 → fetch 재호출
  </behavior>
  <action>
    1. `webapp/src/components/stock/stock-daily-chart-section.tsx` 신규:
       ```typescript
       'use client';

       /**
        * Phase 09.2 — 종목 상세 페이지 일봉 차트 섹션 (D-09 마운트 진입점).
        *
        * 책임:
        *   - fetchDailyOhlcv 라이프사이클 (mount + range 변경 + 재시도)
        *   - AbortController 로 이전 fetch 취소
        *   - Skeleton (D-16) / Empty (D-17) / Error (D-18) / 장중 라벨 (D-20) / sr-only (D-21) 분기
        *   - 카드 컨테이너 + 헤더 (타이틀 + 기간 토글) (D-10)
        *
        * 참고: chart 인스턴스 lifecycle 은 자식 StockDailyChart 가 책임.
        */

       import { useCallback, useEffect, useMemo, useRef, useState } from 'react';
       import { Card } from '@/components/ui/card';
       import { Button } from '@/components/ui/button';
       import {
         DAILY_OHLCV_RANGES,
         type DailyOhlcvRangeKey,
         type DailyOhlcvRow,
       } from '@gh-radar/shared';
       import { fetchDailyOhlcv } from '@/lib/daily-ohlcv-api';
       import { StockDailyChart } from './stock-daily-chart';
       import { StockDailyChartSkeleton } from './stock-daily-chart-skeleton';

       export interface StockDailyChartSectionProps {
         code: string;
         /**
          * D-19 — 부모 (StockDetailClient) 새로고침 버튼이 이 값을 변경하면
          * 동일 range 로 refetch. 미주입 시 mount + range 변경에만 반응 (기존 동작).
          */
         refreshSignal?: unknown;
       }

       function todayKstIso(now: Date = new Date()): string {
         // KST = UTC+9. ISO YYYY-MM-DD 만 사용.
         const kst = new Date(now.getTime() + 9 * 3_600_000);
         return kst.toISOString().slice(0, 10);
       }

       export function StockDailyChartSection({
         code,
         refreshSignal,
       }: StockDailyChartSectionProps) {
         const [range, setRange] = useState<DailyOhlcvRangeKey>('1M');
         const [rows, setRows] = useState<DailyOhlcvRow[] | null>(null);
         const [error, setError] = useState<Error | null>(null);
         const controllerRef = useRef<AbortController | null>(null);

         const load = useCallback(
           async (nextRange: DailyOhlcvRangeKey) => {
             controllerRef.current?.abort();
             const controller = new AbortController();
             controllerRef.current = controller;
             setRows(null);
             setError(null);
             try {
               const data = await fetchDailyOhlcv(
                 code,
                 nextRange,
                 controller.signal,
               );
               if (controller.signal.aborted) return;
               setRows(data);
             } catch (err) {
               if (controller.signal.aborted) return;
               if (err instanceof Error && err.name === 'AbortError') return;
               // T-09.2-07 mitigate (W4) — 사용자 노출은 generic, 디버그용 stack 은 console
               console.error('[StockDailyChartSection] fetch failed', err);
               setError(err instanceof Error ? err : new Error(String(err)));
             }
           },
           [code],
         );

         useEffect(() => {
           void load(range);
           return () => controllerRef.current?.abort();
         }, [load, range, refreshSignal]);

         const isIntraday = useMemo(() => {
           if (!rows || rows.length === 0) return false;
           return rows[rows.length - 1].date === todayKstIso();
         }, [rows]);

         const summary = useMemo(() => {
           if (!rows || rows.length === 0) return null;
           const first = rows[0];
           const last = rows[rows.length - 1];
           return `최근 ${rows.length}영업일 종가 추이. 시작 종가 ${first.close.toLocaleString()}원, 현재 종가 ${last.close.toLocaleString()}원.`;
         }, [rows]);

         const isLoading = rows === null && error === null;
         const isEmpty = rows !== null && rows.length === 0;

         return (
           <Card
             className="space-y-4 p-5"
             aria-label="일봉 차트"
             data-testid="stock-daily-chart-section"
           >
             <header className="flex items-center justify-between gap-3">
               <div className="flex items-center gap-2">
                 <h2 className="text-[length:var(--t-h3)] font-semibold">
                   일봉 차트
                 </h2>
                 {isIntraday && (
                   <span
                     className="rounded-[var(--r-sm)] bg-[var(--muted)] px-2 py-0.5 text-[length:var(--t-caption)] text-[var(--muted-fg)]"
                     data-testid="stock-daily-chart-intraday-badge"
                   >
                     장중
                   </span>
                 )}
               </div>
               <div
                 role="tablist"
                 aria-label="기간 선택"
                 className="inline-flex items-center gap-1 rounded-[var(--r)] border border-[var(--border)] p-1"
               >
                 {DAILY_OHLCV_RANGES.map((r) => {
                   const active = r === range;
                   return (
                     <button
                       key={r}
                       type="button"
                       role="tab"
                       aria-selected={active}
                       onClick={() => setRange(r)}
                       className={`rounded-[var(--r-sm)] px-3 py-1 text-[length:var(--t-caption)] transition-colors ${
                         active
                           ? 'bg-[var(--primary)] text-[var(--primary-fg)]'
                           : 'text-[var(--muted-fg)] hover:bg-[var(--muted)]'
                       }`}
                     >
                       {r}
                     </button>
                   );
                 })}
               </div>
             </header>

             {error && (
               <div
                 role="alert"
                 className="space-y-2"
                 data-testid="stock-daily-chart-error"
               >
                 {/* T-09.2-07 mitigate (W4) — error.message 미노출.
                     PostgREST/RLS 내부 정보 누설 방지. 디버그는 console.error. */}
                 <p className="text-[length:var(--t-sm)] text-[var(--destructive)]">
                   일봉 데이터를 불러오지 못했습니다.
                 </p>
                 <Button
                   onClick={() => void load(range)}
                   variant="outline"
                   size="sm"
                 >
                   다시 시도
                 </Button>
               </div>
             )}

             {/* Pitfall 5: container 항상 visible — Skeleton/Empty 는 absolute overlay */}
             <div className="relative h-[340px] w-full">
               {isLoading && (
                 <div className="absolute inset-0">
                   <StockDailyChartSkeleton height={340} />
                 </div>
               )}
               {isEmpty && (
                 <div
                   className="absolute inset-0 grid place-items-center text-[length:var(--t-sm)] text-[var(--muted-fg)]"
                   data-testid="stock-daily-chart-empty"
                 >
                   일봉 데이터가 아직 수집되지 않았습니다.
                 </div>
               )}
               <StockDailyChart rows={rows ?? []} height={340} />
             </div>

             {summary && <p className="sr-only">{summary}</p>}
           </Card>
         );
       }
       ```

    2. `webapp/src/components/stock/__tests__/stock-daily-chart-section.test.tsx` 신규 — vitest + jsdom + Testing Library, lightweight-charts 와 daily-ohlcv-api 양쪽 mock:

       ```typescript
       import { describe, expect, it, vi, beforeEach, afterEach } from 'vitest';
       import { render, screen, waitFor } from '@testing-library/react';
       import userEvent from '@testing-library/user-event';
       import type { DailyOhlcvRow } from '@gh-radar/shared';

       // ── lightweight-charts 전체 mock — chart 인스턴스 lifecycle 은 다른 곳에서 검증 ──
       vi.mock('lightweight-charts', () => {
         const series = {
           applyOptions: vi.fn(),
           setData: vi.fn(),
         };
         const priceScale = { applyOptions: vi.fn() };
         const chart = {
           addSeries: vi.fn(() => series),
           priceScale: vi.fn(() => priceScale),
           timeScale: vi.fn(() => ({ fitContent: vi.fn() })),
           applyOptions: vi.fn(),
           remove: vi.fn(),
         };
         return {
           createChart: vi.fn(() => chart),
           CandlestickSeries: 'CandlestickSeries',
           HistogramSeries: 'HistogramSeries',
         };
       });

       // ── next-themes mock — useTheme 만 ──
       vi.mock('next-themes', () => ({
         useTheme: () => ({ resolvedTheme: 'dark' }),
       }));

       // ── daily-ohlcv-api mock — fetchDailyOhlcv 만 ──
       const fetchDailyOhlcvMock = vi.fn();
       vi.mock('@/lib/daily-ohlcv-api', () => ({
         fetchDailyOhlcv: (...args: unknown[]) => fetchDailyOhlcvMock(...args),
       }));

       import { StockDailyChartSection } from '../stock-daily-chart-section';

       const SAMPLE_ROWS: DailyOhlcvRow[] = [
         {
           date: '2026-04-15',
           open: 70000, high: 71000, low: 69500, close: 70500,
           volume: 12345, changeAmount: 500, changeRate: 0.71,
         },
         {
           date: '2026-04-16',
           open: 70500, high: 72000, low: 70000, close: 71500,
           volume: 15000, changeAmount: 1000, changeRate: 1.42,
         },
       ];

       beforeEach(() => {
         fetchDailyOhlcvMock.mockReset();
       });

       afterEach(() => {
         vi.clearAllMocks();
       });

       describe('StockDailyChartSection', () => {
         it('마운트 시 1M 으로 fetch + Skeleton 표시 → 데이터 도착 후 차트 컨테이너 노출', async () => {
           let resolveFetch!: (rows: DailyOhlcvRow[]) => void;
           fetchDailyOhlcvMock.mockReturnValueOnce(
             new Promise<DailyOhlcvRow[]>((res) => {
               resolveFetch = res;
             }),
           );
           render(<StockDailyChartSection code="005930" />);

           // fetch 호출 검증 — code, '1M', AbortSignal
           expect(fetchDailyOhlcvMock).toHaveBeenCalledTimes(1);
           expect(fetchDailyOhlcvMock).toHaveBeenCalledWith(
             '005930',
             '1M',
             expect.any(AbortSignal),
           );

           // Skeleton 분기
           expect(
             screen.getByLabelText('일봉 차트 로딩 중'),
           ).toBeInTheDocument();

           resolveFetch(SAMPLE_ROWS);
           await waitFor(() => {
             expect(
               screen.queryByLabelText('일봉 차트 로딩 중'),
             ).not.toBeInTheDocument();
           });

           expect(
             screen.getByTestId('stock-daily-chart-canvas'),
           ).toBeInTheDocument();
         });

         it('빈 배열 응답 시 Empty state 표시', async () => {
           fetchDailyOhlcvMock.mockResolvedValueOnce([]);
           render(<StockDailyChartSection code="999999" />);
           await waitFor(() => {
             expect(
               screen.getByTestId('stock-daily-chart-empty'),
             ).toHaveTextContent('일봉 데이터가 아직 수집되지 않았습니다.');
           });
         });

         it('fetch 실패 시 generic 에러 카피 + 다시 시도 버튼 (T-09.2-07 — error.message 미노출)', async () => {
           const consoleErrorSpy = vi
             .spyOn(console, 'error')
             .mockImplementation(() => {});
           fetchDailyOhlcvMock.mockRejectedValueOnce(new Error('PostgREST 500'));
           render(<StockDailyChartSection code="005930" />);
           await waitFor(() => {
             expect(
               screen.getByTestId('stock-daily-chart-error'),
             ).toHaveTextContent('일봉 데이터를 불러오지 못했습니다');
           });
           // 사용자 화면에는 'PostgREST 500' 누설 금지
           expect(
             screen.getByTestId('stock-daily-chart-error'),
           ).not.toHaveTextContent('PostgREST 500');
           // 디버그 정보는 console.error 로만
           expect(consoleErrorSpy).toHaveBeenCalled();
           expect(
             screen.getByRole('button', { name: '다시 시도' }),
           ).toBeInTheDocument();
           consoleErrorSpy.mockRestore();
         });

         it('range 토글 클릭 시 fetch 가 새 range 로 재호출', async () => {
           fetchDailyOhlcvMock.mockResolvedValue(SAMPLE_ROWS);
           const user = userEvent.setup();
           render(<StockDailyChartSection code="005930" />);

           await waitFor(() => {
             expect(fetchDailyOhlcvMock).toHaveBeenCalledTimes(1);
           });

           await user.click(screen.getByRole('tab', { name: '3M' }));

           await waitFor(() => {
             expect(fetchDailyOhlcvMock).toHaveBeenCalledTimes(2);
           });
           expect(fetchDailyOhlcvMock).toHaveBeenLastCalledWith(
             '005930',
             '3M',
             expect.any(AbortSignal),
           );
         });

         it('다시 시도 버튼 클릭 시 같은 range 로 재호출', async () => {
           fetchDailyOhlcvMock.mockRejectedValueOnce(new Error('네트워크'));
           fetchDailyOhlcvMock.mockResolvedValueOnce(SAMPLE_ROWS);
           const user = userEvent.setup();
           render(<StockDailyChartSection code="005930" />);

           await waitFor(() => {
             expect(
               screen.getByRole('button', { name: '다시 시도' }),
             ).toBeInTheDocument();
           });

           await user.click(screen.getByRole('button', { name: '다시 시도' }));

           await waitFor(() => {
             expect(fetchDailyOhlcvMock).toHaveBeenCalledTimes(2);
           });
         });

         it('aria-label="일봉 차트" 가 카드 컨테이너에 존재 (D-21)', async () => {
           fetchDailyOhlcvMock.mockResolvedValueOnce(SAMPLE_ROWS);
           render(<StockDailyChartSection code="005930" />);
           expect(screen.getByLabelText('일봉 차트')).toBeInTheDocument();
         });

         it('마지막 row 의 date 가 today (KST) 이면 "장중" 배지 표시 (D-20)', async () => {
           // KST = UTC+9. 2026-05-15 KST = 2026-05-14T15:00:00Z 이후.
           // useFakeTimers + setSystemTime 으로 deterministic.
           vi.useFakeTimers();
           vi.setSystemTime(new Date('2026-05-15T06:00:00Z')); // 2026-05-15 15:00 KST
           try {
             const todayRow: DailyOhlcvRow = {
               date: '2026-05-15',
               open: 71500, high: 72500, low: 71000, close: 72000,
               volume: 20000, changeAmount: 500, changeRate: 0.69,
             };
             fetchDailyOhlcvMock.mockResolvedValueOnce([...SAMPLE_ROWS, todayRow]);
             render(<StockDailyChartSection code="005930" />);
             await waitFor(() => {
               expect(
                 screen.getByTestId('stock-daily-chart-intraday-badge'),
               ).toHaveTextContent('장중');
             });
           } finally {
             vi.useRealTimers();
           }
         });

         it('refreshSignal 변경 시 동일 range 로 fetch 재호출 (D-19)', async () => {
           fetchDailyOhlcvMock.mockResolvedValue(SAMPLE_ROWS);
           const { rerender } = render(
             <StockDailyChartSection code="005930" refreshSignal={1} />,
           );
           await waitFor(() => {
             expect(fetchDailyOhlcvMock).toHaveBeenCalledTimes(1);
           });
           rerender(<StockDailyChartSection code="005930" refreshSignal={2} />);
           await waitFor(() => {
             expect(fetchDailyOhlcvMock).toHaveBeenCalledTimes(2);
           });
           expect(fetchDailyOhlcvMock).toHaveBeenLastCalledWith(
             '005930',
             '1M',
             expect.any(AbortSignal),
           );
         });
       });
       ```

       주의:
       - lightweight-charts mock 은 createChart → addSeries → priceScale 의 chain 호출이 throw 안 하도록 stub 만 제공. 실제 chart rendering 은 jsdom 에서 검증 불가 — 그래서 Manual Verification (VALIDATION.md) 가 별도로 시각 확인 책임.
       - userEvent 는 webapp 의 @testing-library/user-event 14.x 사용.
       - "장중 라벨" 분기는 vi.useFakeTimers + setSystemTime 으로 today 를 강제하여 deterministic 단위 테스트로 검증 (W3 fix). 시각 정합성 (배지 위치/색)은 Manual Verification (Plan 03 checkpoint).

  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp test --run -- stock-daily-chart-section</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @gh-radar/webapp test --run -- stock-daily-chart-section` exit code 0, 8 tests PASS
    - `pnpm --filter @gh-radar/webapp typecheck` exit code 0
    - `grep "fetchDailyOhlcv" webapp/src/components/stock/stock-daily-chart-section.tsx` 결과 ≥ 1건
    - `grep "AbortController" webapp/src/components/stock/stock-daily-chart-section.tsx` 결과 ≥ 1건
    - `grep -E '아직 수집되지|다시 시도|장중' webapp/src/components/stock/stock-daily-chart-section.tsx | wc -l` ≥ 3 (Empty/Error/장중 카피 모두 존재)
    - `grep "DAILY_OHLCV_RANGES" webapp/src/components/stock/stock-daily-chart-section.tsx` 결과 ≥ 1건 (토글 4종 iterate)
    - `grep "aria-label=\"일봉 차트\"" webapp/src/components/stock/stock-daily-chart-section.tsx` 결과 ≥ 1건
    - `wc -l webapp/src/components/stock/stock-daily-chart-section.tsx` 결과 ≥ 120행
    - `grep "refreshSignal" webapp/src/components/stock/stock-daily-chart-section.tsx` 결과 ≥ 3건 (interface prop 1 + 함수 시그너처 1 + useEffect 의존성 1 — D-19 새로고침 연동)
  </acceptance_criteria>
  <done>
    StockDailyChartSection 이 mount 시 1M fetch, Skeleton/Empty/Error 분기, range 토글 4종, AbortController 취소, sr-only 요약, aria-label, 장중 라벨, 다시 시도 버튼 모두 구현. 단위 테스트 8건 PASS. Plan 03 가 import 만으로 StockDetailClient 에 mount 가능.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM ↔ lightweight-charts (Canvas) | 차트 라이브러리가 사용자 입력을 받지 않음 (read-only data viz). XSS 표면 없음 |
| props (rows from fetch) → chart.setData | rows 는 Plan 01 의 fetchDailyOhlcv 가 이미 RLS 통과 + 타입 변환된 정형 데이터 |
| useTheme (next-themes) → applyOptions | resolvedTheme 은 next-themes 가 'light' | 'dark' | undefined 만 반환 — resolvePaletteKey 가 fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.2-05 | Tampering | DailyOhlcvRow.date 의 'YYYY-MM-DD' 형식 가정 | mitigate | lightweight-charts 의 Time 캐스트는 string 자동 인식. Plan 01 의 fetchDailyOhlcv 가 Supabase date 컬럼 (Postgres date 타입) 그대로 받아 변환 안 함 — 형식 보장은 DB schema |
| T-09.2-06 | Denial of Service | rows 변경 시 매번 setData (1Y = 252 row) | accept | lightweight-charts 의 setData 는 incremental rebuild — 252 row 60fps 충분. AbortController 가 race fetch 차단 |
| T-09.2-07 | Information Disclosure | Error.message 노출 가능성 (D-18) | mitigate | (W4 fix) 에러 UI 는 generic 카피 ("일봉 데이터를 불러오지 못했습니다") 만 노출. 원본 err.stack/message 는 `console.error('[StockDailyChartSection] fetch failed', err)` 로만 기록 → DevTools 개발자만 접근. PostgREST 의 schema 내부 정보 (정책명/컬럼/SQL) 누설 표면 0. 검증: `grep "{error.message}" webapp/src/components/stock/stock-daily-chart-section.tsx` 결과 0건 |
| T-09.2-08 | Tampering | Pitfall 5 — container display:none 시 chart 0×0 | mitigate | container 항상 visible (`relative h-[340px]`) + Skeleton/Empty 는 `absolute inset-0` overlay. 본 plan 의 코드 패턴이 직접 가드 |
| T-09.2-09 | Spoofing | 다른 사용자의 watchlist 종목 차트 노출 | accept | 본 차트는 종목 단위 시세 (개인 정보 아님). RLS 가 anon SELECT 허용 — 의도된 공개 데이터 |

위협 5건 (mitigate 2건 / accept 3건). HIGH/CRITICAL 없음 — block on high 정책 통과.
</threat_model>

<verification>
- `pnpm --filter @gh-radar/webapp typecheck` exit 0 (Task 1 + Task 2 양쪽)
- `pnpm --filter @gh-radar/webapp test --run` 전체 PASS — 신규 8건 + 기존 회귀 0건
- `pnpm --filter @gh-radar/webapp build` exit 0 (production build, lightweight-charts SSR 가드 검증)
- `grep -rE "^[^/]*(#[0-9a-fA-F]{6}|oklch)" webapp/src/components/stock/stock-daily-chart*.tsx` 결과 0건 (모든 색상 chart-colors 위임 — Pitfall 9 회귀 방지. ^[^/]* 패턴으로 코드 라인만 매치, 주석 false positive 회피 — W2)
- `grep "data-testid=\"stock-daily-chart-canvas\"" webapp/src/components/stock/stock-daily-chart.tsx` ≥ 1건 (B3 — E2E selector 동기)
- `grep "{error.message}" webapp/src/components/stock/stock-daily-chart-section.tsx` 결과 0건 (W4 — T-09.2-07 mitigate, error.message 사용자 미노출)
- `grep "console.error.*fetch failed" webapp/src/components/stock/stock-daily-chart-section.tsx` ≥ 1건 (W4 — 디버그용 분리 기록)
- `grep "refreshSignal" webapp/src/components/stock/stock-daily-chart-section.tsx` ≥ 3건 (B4 — D-19 새로고침 연동)
</verification>

<success_criteria>
- StockDailyChart 가 mount/theme/rows 3개 useEffect 분리, chart cleanup 보장, addSeries(CandlestickSeries) v5 패턴
- StockDailyChartSection 이 fetch + 토글 + Skeleton/Empty/Error/장중/sr-only 모든 분기 처리
- StockDailyChartSkeleton 이 동일 height (340) 로 CLS 최소화
- 단위 테스트 8건 PASS (Skeleton/Empty/Error/range-toggle/재시도/aria-label/장중-라벨/refreshSignal)
- Pitfall 5/6/9 모두 코드 패턴으로 회귀 방지 (container 항상 visible / theme effect / chart-colors 위임)
- Plan 03 의 StockDetailClient 가 단순 `<StockDailyChartSection code={stock.code} />` mount 만으로 통합 가능
</success_criteria>

<output>
After completion, create `.planning/phases/09.2-stock-detail-daily-chart/09.2-02-SUMMARY.md` with:
- 3개 컴포넌트의 props 시그너처 + 책임 경계 요약
- 단위 테스트 8건 PASS log + 검증 분기 매트릭스 (Skeleton/Empty/Error/Toggle/재시도/a11y/장중-라벨/refreshSignal)
- 다크모드 effect 패턴 코드 인용 (Pitfall 6 회귀 방지)
- Plan 03 가 사용할 import 경로 (`@/components/stock/stock-daily-chart-section`)
- 라인 수 (chart.tsx ≥90, section.tsx ≥120, skeleton.tsx ≈15)
</output>
