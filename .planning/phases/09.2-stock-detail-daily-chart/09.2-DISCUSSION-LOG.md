# Phase 09.2: 종목 상세페이지 상단 일봉차트 — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-15
**Phase:** 09.2-stock-detail-daily-chart
**Areas discussed:** 차트 형태, 기간 UX, 차트 라이브러리, 위치, 기간 토글 정확 명세, 추가 요소, 데이터 fetch

---

## 차트 형태 (시각화 방식)

| Option | Description | Selected |
|--------|-------------|----------|
| Close 라인 차트 | 종가만 라인으로 (가장 단순). 추천 — Out of Scope 정책과 가장 잘 맞고 차별화 부담 없음 | |
| Area 차트 (close + fill) | 라인 + 영역 fill 로 시각적 무게감 추가 | |
| OHLC bar 차트 | 캔들 대신 OHLC bar (open tick 좌측, close tick 우측) | |
| 캔들스틱 (OOS 재검토) | Out of Scope 결정을 뒤집고 캔들스틱으로 가는 옵션. REQUIREMENTS Out of Scope 갱신 필요 | ✓ |

**User's choice:** 캔들스틱으로 가자.
**Notes:** REQUIREMENTS.md Out of Scope 의 "캔들스틱 차트 — TradingView/키움이 이미 지배, 차별화 없음" 정책을 사용자가 명시적으로 뒤집음. 차별화 논리 대신 상세 페이지 자체 완결성을 우선. ⇒ Follow-up F-01 (REQUIREMENTS.md Out of Scope 갱신) + F-02 (PROJECT.md Key Decisions 추가) 가 CONTEXT 에 기록됨.

---

## 기간 UX (1차)

| Option | Description | Selected |
|--------|-------------|----------|
| 기본 3M + 토글 (1M/3M/6M/1Y/3Y/All) | 추천 — 단기/장기 양쪽 커버, 토글 6종 | |
| 기본 6M + 토글 (3M/6M/1Y/All) | 더 긴 기본 기간, 토글 4종 | |
| 기본 1Y + 토글 (3M/1Y/3Y/All) | 장기 시야 우선 | |
| 고정 1Y (토글 없음) | 토글 UI 생략, 1년치만 표시 | |

**User's choice:** 일봉차트를 기본으로 할건데, 최근 30일정도면 되지 않을까? 추천해줘.
**Notes:** 사용자가 1M (30일 ≈ ~22 영업일) 기본 + 토글 추천 요청. Claude 가 follow-up 으로 "기본 1M + 토글 1M/3M/6M/1Y" 추천 → 다음 라운드에서 사용자 수용.

---

## 차트 라이브러리

| Option | Description | Selected |
|--------|-------------|----------|
| recharts (D3 기반 React 차트) | shadcn/ui Chart 컴포넌트 래퍼. 번들 ~90kb. 라인/Area/OHLC bar 지원, React 19 + Tailwind 친화. 추천 | |
| lightweight-charts (TradingView) | 캔들/OHLC 최적화 ~45kb. 매우 빠른 렌더. 명령형 API → React 통합 코스트 | |
| 수제 SVG (sparkline 확장) | 외부 의존성 zero. 축/툴팁/줌 등 직접 구현 필요 | |
| research 단계 위임 | RESEARCH 에서 번들/접근성/툴팁 등 실측 후 planner 가 최종 결정 | |

**User's choice:** recharts와 lightweight-charts 두개를 사용해서 그린 주식차트 목업을 확인해보고 싶어.
**Notes:** 사용자가 양쪽 라이브러리 mockup 을 직접 보고 선정하겠다는 의사. RESEARCH 단계의 POC mockup 비교가 D-07/D-08 의 전제. researcher 가 두 라이브러리로 동일 fixture(예: 005930 1M) 캔들 차트 + Volume 합성 mockup 스크린샷 + 비교표(번들/접근성/툴팁/모바일/SSR/테마) 생성 후 사용자에게 선정 요청. 본 CONTEXT 에서는 라이브러리 미확정.

---

## 위치 / 레이아웃

| Option | Description | Selected |
|--------|-------------|----------|
| Hero 바로 아래, StatsGrid 위 (전용 카드) | ROADMAP '상단에 출력' 의도에 가장 정확. Hero → 차트 → Stats 흐름 | ✓ |
| Hero 내부에 컴팩트 차트 통합 | Hero 우측/하단 작은 차트 (sparkline 진화형) | |
| 별도 페이지 `/stocks/[code]/chart` | News/Discussion 처럼 풀페이지 분리 | |
| 탭 UI (개요/차트/뉴스/토론방) | 상세 페이지 자체 재구성 (scope creep 우려) | |

**User's choice:** Hero 바로 아래, StatsGrid 위 (전용 카드).
**Notes:** lock-in. 신규 컴포넌트 `webapp/src/components/stock/stock-daily-chart-section.tsx` (가칭) 가 StockDetailClient 의 Hero 다음 자리에 마운트.

---

## 기간 토글 정확 명세 (2차 follow-up)

| Option | Description | Selected |
|--------|-------------|----------|
| 기본 1M + 토글 1M/3M/6M/1Y | 추천 — 단기(1M=22영업일) 기본 + 필요하면 장기 활열. 캔들 폭 가독성 최적, 토글은 4개로 단순 | ✓ |
| 기본 1M + 토글 없음 | 1개월치만 고정, 장기 맥락은 v2 | |
| 기본 1M + 토글 1M/3M/6M/1Y/3Y/All | 6개 토글 (All=2020-01-02 ~). 3Y/All 구간은 캔들 좁아져 가독성 낮음 | |
| 기본 3M + 토글 1M/3M/6M/1Y | 조금 더 긴 기본 (3M=~66 영업일). 캔들 폭이 좌아져 추세 파악은 좋으나 개별 캔들 읽기 뜬다 | |

**User's choice:** 기본 1M + 토글 1M/3M/6M/1Y.
**Notes:** 4개 토글 (segmented control / shadcn ToggleGroup or Tabs 후보 — planner 재량). 3Y/All 제외 사유: 캔들 폭 가독성 한계 (RESEARCH 단계에서 캔들 폭 ≥4px 임계 검증).

---

## 캔들 외 추가 요소

| Option | Description | Selected |
|--------|-------------|----------|
| Volume bar 동반 (대충 고정) | 추천 — 캔들 하단 거래량 막대그래프 (TradingView 패턴). volume/trade_amount 이미 stock_daily_ohlcv 에 존재 | ✓ |
| 이동평균선 (MA5, MA20) 동반 | 캔들 위에 MA5/MA20 라인 오버레이. 추가 시그널 타구 | |
| Volume + MA5/MA20 모두 | Full TradingView 패턴. 구현 복잡도 최고 | |
| 캔들만 (추가 요소 없음) | v1 최소 구성 | |

**User's choice:** Volume bar 동반 (대충 고정).
**Notes:** 캔들 패널 하단에 별도 panel (75/25 비율 권장, 정확값 planner). bar 색상은 캔들과 동기화 (양봉=빨강 음봉=파랑). MA/RSI 등 보조지표는 v1 제외 → Deferred Ideas.

---

## 데이터 fetch 패턴

| Option | Description | Selected |
|--------|-------------|----------|
| 새 server 엔드포인트 GET /api/stocks/:code/ohlcv | 기존 server/src/routes/stocks.ts 패턴 (news/discussions 하위 라우터) 재사용. RLS/CORS/cache-control 일괄 제어. apiFetch 일관성 유지 | |
| Supabase 클라이언트 직접 호출 (웹앱 → Supabase) | stock_daily_ohlcv anon SELECT 허용 (Phase 9 D-03). watchlist-api 패턴 mirror. 4M 행 read-only 라 server 경유 불필요 | ✓ |
| Next.js Server Component (초기 fetch) + 클라이언트 갱신 | SSR 으로 초기 차트, 토글은 클라이언트. 다만 Phase 6 의 전체 'use client' 정책과 불일치 | |

**User's choice:** Supabase 클라이언트 직접 호출 (웹앱 → Supabase).
**Notes:** Phase 06.2 watchlist-api.ts 패턴 mirror. 신규 `webapp/src/lib/daily-ohlcv-api.ts` + `webapp/src/lib/supabase/client.ts` 재사용. 새 server 엔드포인트 추가하지 않음.

---

## Claude's Discretion

다음 항목은 RESEARCH / planner 가 후속 결정:
- 차트 라이브러리 최종 선정 (D-07/08 POC 결과)
- 차트 카드 정확 높이 (`h-[360px]` vs aspect ratio)
- 캔들 패널 ↔ Volume 패널 비율 (75/25 권장, 정확값)
- 1M/3M/6M/1Y 의 `from` 계산 정확 알고리즘 (영업일 vs calendar day, client-side 슬라이스)
- 오늘자 row 포함 / 제외 정책 + 라벨 (D-20 권장 따르되 명문화)
- packages/shared 타입 중복 / 통합 정책
- 모바일 캔들 폭 최소 정책
- Skeleton 통합 형태 (StockDetailSkeleton 확장 vs 차트 전용)
- Empty state 카피
- 차트 카드 헤더 마이크로 UI (타이틀 + 토글 정렬 / 모바일 wrap)

## Deferred Ideas

- 이동평균선 (MA5, MA20) + RSI/MACD 등 보조지표
- 줌/팬/드래그 인터랙션
- 분/주/월봉
- 종목 비교 / 다중 종목 오버레이
- 수정주가 (adj_close)
- 차트 풀스크린 / 전용 페이지 (`/stocks/[code]/chart`)
- 인트라데이 실시간 캔들 갱신 (오늘자 row 라이브 스트리밍)
- 3Y / All 기간 토글
- 차트 fixture 로컬 캐시 (SWR / React Query)

---

## 보강 라운드 (2026-05-15 재실행)

`/gsd-discuss-phase 09.2` 재실행으로 D-07/D-08 (mockup 라이브러리 후보) 보강.

### Q1. CONTEXT.md 이미 완성. 어떻게 진행?

| Option | Description | Selected |
|--------|-------------|----------|
| Skip - 그대로 사용 | CONTEXT 충분, 다음 단계로 | |
| Update - 특정 결정 보강/수정 | 추가/변경하고 싶은 결정이 있다 | |
| View - 한 번 더 짚고 결정 | 현재 결정 요약 본 후 Skip/Update | ✓ |
| Replan - 별도 작업 | 09.2 와 무관한 새 작업 | |

**User's choice:** View - 한 번 더 짚고 결정.

---

### Q2. CONTEXT 확인 후 어떻게?

| Option | Description | Selected |
|--------|-------------|----------|
| Skip - 그대로 사용 (Recommended) | CONTEXT 충분, /gsd-research-phase 09.2 또는 /gsd-plan-phase 09.2 로 진행 | |
| Update - 특정 영역 보강 | 특정 결정 수정/추가 필요 | ✓ (Other) |
| F-01/F-02 별도 먼저 | REQUIREMENTS/PROJECT 메타 갱신 먼저 | |

**User's choice (Other):** "차트에 대한 목업디자인을 확인해보고 싶어. 기존 디자인 시스템을 따르면서, 실제 차트 라이브러리를 이용한 버전으로 html 목업을 3개 만들어볼까? 지금 단계에서 하는게 맞나?"

**Notes:** Claude 가 단계 경계를 짚어줌 — discuss-phase 는 결정 캡처, mockup 자체는 research-phase 의 산출물(D-07/D-08 에 이미 명시). 다만 후보가 2개 → 3개로 늘어났으므로 그 결정만 본 단계에서 lock-in. "HTML 목업" 보다 Next.js mockup 라우트(`webapp/src/app/_mockups/*`) 가 Phase 3 디자인 토큰 자동 적용 측면에서 더 적합함을 제안.

---

### Q3. Mockup 3개 제작 진행 방법?

| Option | Description | Selected |
|--------|-------------|----------|
| Research-phase 로 넘기기 (Recommended) | 단계 경계 준수, lib 후보 3개만 lock-in → ExitPlanMode → /gsd-research-phase 09.2 | ✓ |
| Discuss-phase 안에서 직접 제작 | 이 대화 내 mockup 작성. 단점: discuss/research 로직 섞임 | |
| Mockup 대신 UI-SPEC | /gsd-ui-phase 09.2 로 디자인 contract 먼저 → research mockup | |

**User's choice:** Research-phase 로 넘기기.

---

### Q4. 캔들 차트 라이브러리 후보 3개?

| Option | Description | Selected |
|--------|-------------|----------|
| recharts + lightweight-charts + Apache ECharts (Recommended) | 대표 3각 구도 — React 궁합 / 금융 일견 / 다용도 고성능 | |
| recharts + lightweight-charts + Klinecharts | K선 전용 경량 (한/중 트레이더 도메인 특화) | |
| recharts (raw) + recharts (shadcn 래퍼) + lightweight-charts | 동일 recharts 라이브러리 두 스타일(raw vs shadcn primitive) 비교 + lightweight-charts. shadcn 디자인 시스템 통합도 직접 확인 가능 | ✓ |
| Other - 다른 조합 | 자유 입력 | |

**User's choice:** recharts 기본 + recharts(shadcn 래퍼) + lightweight-charts.

**Notes:** shadcn `<ChartContainer>` / `<ChartTooltip>` / `<ChartLegend>` 는 recharts 를 래핑한 wrapper 로 Phase 3 디자인 토큰 자동 매핑. raw recharts 와 비교하면 "디자인 시스템 통합 부담" 의 실측치를 얻을 수 있음. lightweight-charts 는 완전히 다른 명령형 API → React 통합 코스트 + 금융 차트 네이티브 성능의 대조축.

---

### 보강 라운드 적용 변경

- **CONTEXT.md D-07:** 라이브러리 후보 2개 → 3개로 갱신 (recharts raw / recharts shadcn 래퍼 / lightweight-charts).
- **CONTEXT.md D-08:** Mockup 산출물 위치 명시 (`webapp/src/app/_mockups/chart-{recharts,shadcn,lightweight}/page.tsx`), 비교표 권장 컬럼 추가.
- **CONTEXT.md `<specifics>`:** 본 보강 라운드 사용자 발화 한 줄 추가.
- **변경하지 않은 항목:** D-01 ~ D-06, D-09 ~ D-22, F-01/F-02 그대로 유효.
