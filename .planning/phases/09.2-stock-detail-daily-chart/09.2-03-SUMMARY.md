---
phase: 09.2
plan: 03
subsystem: webapp/stock-detail
tags: [integration, e2e-playwright, cleanup, state-followup, wave-3]
requires:
  - "webapp/src/components/stock/stock-daily-chart-section.tsx#StockDailyChartSection"
  - "webapp/e2e/fixtures/mock-api.ts#mockStockApi"
  - "webapp/e2e/fixtures/news.ts#mockNewsApi"
  - "webapp/e2e/fixtures/news.ts#buildNewsList"
provides:
  - "webapp/src/components/stock/stock-detail-client.tsx#StockDetailClient(chart-mounted)"
  - "webapp/e2e/specs/stock-detail-chart.spec.ts#Phase09.2-DATA-03"
  - ".planning/STATE.md#DI-03(RLS-authenticated-audit)"
  - ".planning/STATE.md#DI-04(Vercel-env-newline)"
affects:
  - "webapp/src/app/stocks/[code]"  # /stocks/[code] 페이지가 차트 표시
tech-stack:
  added: []  # 코드 의존 추가 없음
  patterns:
    - "stock-detail-client.test.tsx 가 차트 섹션 모듈 mock — jsdom 환경의 lightweight-charts raf ensureNotNull throw 회피"
    - "Playwright spec 가 boundingBox y 좌표 비교로 수직 JSX 순서 검증 (D-09 lock-in)"
    - "RESEARCH 단계 mockup 라우트는 lock-in 후 git history 보존만 + production 디렉터리 정리"
key-files:
  created:
    - "webapp/e2e/specs/stock-detail-chart.spec.ts"
    - ".planning/phases/09.2-stock-detail-daily-chart/09.2-03-SUMMARY.md"
  modified:
    - "webapp/src/components/stock/stock-detail-client.tsx"
    - "webapp/src/components/stock/__tests__/stock-detail-client.test.tsx"
    - "webapp/src/lib/daily-ohlcv-api.ts"
    - ".planning/STATE.md"
  deleted:
    - "webapp/src/app/mockups/page.tsx"
    - "webapp/src/app/mockups/chart-recharts/page.tsx"
    - "webapp/src/app/mockups/chart-shadcn/page.tsx"
    - "webapp/src/app/mockups/chart-lightweight/page.tsx"
    - "webapp/src/app/mockups/shared/fetch-daily.ts"
decisions:
  - "Plan 03 Task 1 의 차트 mount 가 stock-detail-client.test.tsx 의 Test 4 (refresh) 에서 lightweight-charts raf 콜백 ensureNotNull throw 유발 — vi.mock 으로 StockDailyChartSection 모듈 자체 stub (Rule 3 — Auto-fix blocking, 차트 단위 검증은 별도 spec 책임)"
  - "daily-ohlcv-api.ts JSDoc 의 'webapp/src/app/mockups/shared/fetch-daily.ts 를 정식 promote' 참조 정리 — grep 'mockups' 0건 acceptance 만족 + 의미는 보존 (shared/fetch-daily.ts 삭제됨 명시)"
  - "Task 2 Step C (Playwright spec 실제 실행) 는 본 subagent 환경에서 강제 수행 안 함 — playwright.config 의 webServer 가 dev 서버 자동 기동 + setup project 가 Supabase auth.setup 호출하므로 비용 우려. spec 파일만 commit, 실행 검증은 사용자 또는 orchestrator checkpoint 책임"
  - "Task 4 (사용자 시각 검증 9개 항목) 은 PENDING — 본 subagent 범위에서 제외, orchestrator 가 사용자와 직접 진행"
metrics:
  duration: "약 8분 (Read + 4 commit)"
  completed: "2026-05-15"
---

# Phase 09.2 Plan 03: StockDetailClient mount + Cleanup + STATE follow-up Summary

`.planning/phases/09.2-stock-detail-daily-chart/09.2-03-SUMMARY.md`

## One-liner

Plan 02 의 자기완결 차트 섹션을 종목 상세 페이지에 단일 라인으로 mount(D-09 + D-19 refreshSignal 연동), RESEARCH 단계 mockup 라우트 5 파일 정리, 신규 Playwright spec 3건 작성, STATE.md 에 RLS authenticated audit (DI-03) + Vercel env trailing newline (DI-04) follow-up 기록. Task 4 (사용자 시각 검증) 는 orchestrator 위임.

## Tasks Executed

### Task 1 — StockDetailClient 에 StockDailyChartSection mount (D-09 위치 lock-in)

**Commit:** `adeb2f3`

**diff (webapp/src/components/stock/stock-detail-client.tsx):**

```diff
+import { StockDailyChartSection } from './stock-daily-chart-section';
```

```diff
+      <StockDailyChartSection code={stock.code} refreshSignal={isRefreshing} />
       <StockStatsGrid stock={stock} />
```

JSX 순서 (수직): `StockHero` (line 114) → refresh row (115-134) → **`StockDailyChartSection`** (135) → `StockStatsGrid` (136) → error 패널 (137-141) → news/discussion (142-145).

`refreshSignal={isRefreshing}` 로 D-19 lock-in — StockDetailClient 의 새로고침 버튼 클릭이 boolean state 토글 → StockDailyChartSection 의 useEffect 의존성에 들어가 동일 range refetch.

**Auto-fix (Rule 3 — Blocking):** `stock-detail-client.test.tsx` Test 4 (refresh) 가 jsdom 환경의 lightweight-charts raf 콜백에서 `PriceAxisWidget.optimalWidth` 의 `ensureNotNull` throw → uncaught exception 4건 → exit 1. 해결: 동일 테스트에 `vi.mock('../stock-daily-chart-section', () => ({ StockDailyChartSection: () => null }))` 추가하여 차트 섹션 모듈 자체 stub. 차트 단위 검증은 별도 `stock-daily-chart-section.test.tsx` 에서 수행 (Plan 02 산출물).

**Acceptance grep 매트릭스:**

| 항목 | 명령 | 결과 | 기준 |
| --- | --- | --- | --- |
| JSX mount 1건 | `grep "<StockDailyChartSection"` | 1 (line 135) | =1 ✓ |
| import+JSX 2건 | `grep "StockDailyChartSection"` | 2 (line 15 import + 135 JSX) | =2 ✓ |
| refreshSignal 전달 | `grep "refreshSignal"` | 1 (line 135) | ≥1 ✓ |
| JSX 수직 순서 | `grep "StockHero\|StockDailyChartSection\|StockStatsGrid"` | Hero(114) < Chart(135) < Stats(136) | 오름차순 ✓ |
| typecheck | `pnpm --filter @gh-radar/webapp typecheck` | exit 0 | =0 ✓ |
| unit test | `pnpm --filter @gh-radar/webapp test` | 25 files / 157 passed (1 skipped) | 0 fail ✓ |
| build | `pnpm --filter @gh-radar/webapp build` | exit 0 | =0 ✓ |

---

### Task 2 — mockups 디렉터리 정리 + E2E spec 신규

#### Step A — mockup 라우트 5 파일 삭제

**Commit:** `f288efb`

**삭제된 파일 inventory:**

| 파일 | 라인 수 | 역할 |
| --- | --- | --- |
| `webapp/src/app/mockups/page.tsx` | 62 | mockup 인덱스 (3개 variant 링크) |
| `webapp/src/app/mockups/chart-recharts/page.tsx` | 314 | recharts variant |
| `webapp/src/app/mockups/chart-shadcn/page.tsx` | 296 | shadcn chart.tsx variant |
| `webapp/src/app/mockups/chart-lightweight/page.tsx` | 258 | lightweight-charts variant (선정 후 production 으로 promote) |
| `webapp/src/app/mockups/shared/fetch-daily.ts` | 85 | shared fetch 헬퍼 (daily-ohlcv-api.ts 로 promote 됨) |
| **총** | **1015 라인** | |

+ `webapp/src/lib/daily-ohlcv-api.ts` JSDoc 의 mockups 경로 참조 1줄 수정 (`grep "mockups"` 0건 acceptance 만족).

**Production build 영향:**

| 메트릭 | 변경 전 | 변경 후 |
| --- | --- | --- |
| 라우트 수 | 13 | 9 |
| 제거된 라우트 | — | /mockups + /mockups/chart-recharts + /mockups/chart-shadcn + /mockups/chart-lightweight |

**Threat mitigation (T-09.2-10):** 로그인 사용자가 mockup 페이지를 통해 005930 차트를 미리 보던 attack surface 1개 소거. middleware PUBLIC_PREFIXES 에는 추가 안 했으므로 비로그인은 영향 없음.

**보수적 유지 결정:** lightweight-charts (production 사용 중), recharts, shadcn/chart.tsx 의존 패키지는 본 plan 에서 미제거 — tree-shake 로 production 번들에서 자동 제외. 명시 제거는 별도 quick task 위임.

**Acceptance:**

| 항목 | 명령 | 결과 |
| --- | --- | --- |
| 디렉터리 삭제 확인 | `ls webapp/src/app/mockups` | "No such file or directory" ✓ |
| 잔존 import 0건 | `grep -rn "mockups" webapp/src/` | 0건 (exit 1) ✓ |
| 빌드 통과 | `pnpm --filter @gh-radar/webapp build` | exit 0, 9 라우트 ✓ |

---

#### Step B — Playwright E2E spec 신규

**Commit:** `b5bc8c5`

**파일:** `webapp/e2e/specs/stock-detail-chart.spec.ts` (110 라인 / 3 케이스)

| Case | 검증 |
| --- | --- |
| **1. 마운트 + 토글 4종** | `stock-daily-chart-section` testid 표시 / `stock-daily-chart-canvas` testid 표시 / `aria-label="일봉 차트"` / `role=tab × 4 (1M/3M/6M/1Y)` / 1M `aria-selected=true` (기본 활성) |
| **2. 위치 검증 (D-09)** | boundingBox y 좌표 비교 — Hero(testid=stock-hero-price) < Chart(testid=stock-daily-chart-section) < Stats(text="시가") |
| **3. range 토글 클릭** | 3M 클릭 → aria-selected=true 전환 + 1M aria-selected=false 전환 |

**Fixture 재사용:** `mockStockApi` (webapp/e2e/fixtures/mock-api.ts), `mockNewsApi` + `buildNewsList` (webapp/e2e/fixtures/news.ts) — Phase 6/7 패턴 mirror.

**Manual run required by user:** 본 subagent 환경에서 Playwright 강제 실행 안 함. 이유:
1. dev 서버 미기동 (localhost:3100 DEAD 확인) — playwright.config 의 `webServer.command='PORT=3100 pnpm dev'` 가 자동 기동하나 약 10-20초 cold-start 추가.
2. `setup` project (auth.setup.ts) 가 Supabase auth REST 호출하여 storageState `auth.json` 생성 — production Supabase 호출 비용.
3. 차트 자체 fetch (Supabase stock_daily_ohlcv 직접 호출) 가 mock 되지 않음 — production data 의존 + 비용.
4. PLAN 03 가드: "강제 실행 시 시간 소모만 발생", "강제로 setup project 를 띄우지 말 것 — Cloud Run/Supabase 호출 비용 우려".

**사용자/orchestrator 실행 명령 (참고):**

```bash
cd webapp
pnpm exec playwright test specs/stock-detail-chart.spec.ts --reporter=line
# 또는 회귀 검증 동시 실행:
pnpm exec playwright test specs/stock-detail.spec.ts specs/stock-detail-chart.spec.ts --reporter=line
```

**Acceptance:**

| 항목 | 결과 |
| --- | --- |
| spec 파일 존재 | `webapp/e2e/specs/stock-detail-chart.spec.ts` 110 라인 ✓ |
| describe('Phase 09.2') 블록 | 1건 ✓ |
| 의존 fixture export 검증 | `buildNewsList` / `mockStockApi` / `mockNewsApi` 모두 export 확인 ✓ |
| 실행 검증 | **Manual run required by user/orchestrator** (위 가드) |

---

### Task 3 — STATE.md 에 Pitfall 10/11 follow-up 기록 (DI-03/04)

**Commit:** `ccc78b6`

**.planning/STATE.md 변경 위치:**

1. **§Decisions 끝 (line 197 신규):**
   - `[Phase 09.2]: 캔들스틱 차트 채택 — REQUIREMENTS.md Out of Scope 정책 반전 (사용자 명시 2026-05-15, 상세 페이지 자체 완결성 우선). 라이브러리 = lightweight-charts 5.2.0 (RESEARCH 비교 후 lock-in: 번들 +4 kB / 캔들+Volume 네이티브 / 트레이더 친숙도). 데이터 = webapp → Supabase PostgREST 직접 호출 (Phase 06.2 watchlist 패턴 mirror). Pitfall 9 (oklch parser 거부) → chart-colors.ts utility 모듈로 회귀 방지. Pitfall 6 (다크모드 자동 분기 미작동) → next-themes useTheme + chart.applyOptions effect 로 production 해결.`

2. **§Pending Todos 끝 (line 206-207 신규):**
   - **DI-03 (RLS audit):** `news_articles`, `discussions`, `summaries` 테이블의 RLS 정책이 `TO anon` 만 명시하는지 audit. supabase/migrations/20260515163000 (stock_daily_ohlcv fix 패턴) mirror 로 `TO anon, authenticated USING (true)` 갱신 필요. supabase-js 가 인증 사용자 JWT 호출 → role=`authenticated` → 정책 부재 → default-deny 함정. 본 phase 09.2 와 무관 (차트는 stock_daily_ohlcv 만 사용) 하나 로그인 사용자 페이지 (Phase 7 뉴스, Phase 8 토론, Phase 10 요약) 의 빈 응답 가능성. 별도 phase 또는 인프라 PR 권장.
   - **DI-04 (Vercel env newline):** Vercel production env 등록 시 trailing newline (`\n`) 오염 검증 절차 자동화. 증상: dev 정상이나 production 만 모든 fetch 비정상. 검증: `vercel env pull` 후 `tail -c1 .env.local | xxd -p` 가 `0a` (newline) 이면 오염. 즉시 수정: `vercel env rm` + `printf "%s" "값" | vercel env add`. CI hook 자동화 검토 (별도 인프라 PR).

**Acceptance grep 매트릭스:**

| 항목 | 명령 | 결과 |
| --- | --- | --- |
| DI-03 기록 | `grep "DI-03" .planning/STATE.md` | 1건 ✓ |
| DI-04 기록 | `grep "DI-04" .planning/STATE.md` | 1건 ✓ |
| Phase 09.2 캔들스틱 결정 | `grep "Phase 09.2.*캔들스틱"` | 1건 ✓ |
| RLS authenticated 본문 | `grep "RLS authenticated\|TO anon, authenticated"` | 1건 ✓ |
| trailing newline 본문 | `grep "trailing newline\|0a.*newline"` | 1건 ✓ |

---

## Task 4 — PENDING (Orchestrator 위임)

**상태:** **PENDING — orchestrator 가 사용자와 직접 진행**

본 subagent 범위에서 명시적 제외 (사용자 메시지: "Task 4 (사용자 시각 검증, blocking human checkpoint) 는 본 subagent 범위에서 제외 — orchestrator 가 사용자와 직접 진행"). PLAN 03 의 9개 검증 항목 (`how-to-verify`) 은 사용자 브라우저에서의 실제 시각/인터랙션 검증이며 본 환경의 자동화 불가.

### 사용자 manual verification 가이드 (orchestrator 안내용)

**Pre-flight:**

```bash
cd /Users/alex/repos/gh-radar
./dev.sh   # PORT=3100 (CLAUDE.md memory 규약)
```

브라우저: <http://localhost:3100/login> 로그인 → <http://localhost:3100/stocks/005930> 진입

**9개 검증 항목 (PLAN 03 `how-to-verify` 매핑):**

| # | 검증 | 핵심 |
| --- | --- | --- |
| 1 | 차트 섹션 위치 + 기본 렌더 (D-09 / D-04) | Hero 직후 / Stats 직전 / 헤더에 "일봉 차트" + 토글 4종 / 기본 1M 활성 / 캔들+Volume bar |
| 2 | 한국식 색상 (D-02) | 양봉=빨강 (#ef4444 / dark #f87171) / 음봉=파랑 (#3b82f6 / dark #60a5fa) — 미국식이면 chart-colors.ts 회귀 |
| 3 | Range 토글 4종 (D-05) | 1M(~22일) / 3M(~65일) / 6M(~130일) / 1Y(~252일) 클릭 시 Skeleton 잠깐 → 재렌더 |
| 4 | 다크/라이트 토글 (D-22 / Pitfall 6) | ThemeToggle 클릭 → background/text/grid/캔들 1초 내 in-place 전환 |
| 5 | 모바일 가독성 (D-11) | DevTools iPhone 14 (390×844) / 1M 캔들 폭 ≥4px / 토글 4종 wrap 없음 / height 340px UI 잘림 없음 |
| 6 | 접근성 (D-21) | `aria-label="일봉 차트"` + sr-only "최근 N영업일 종가 추이..." |
| 7 | Empty state (D-17) | 신규/미수집 종목 진입 → "일봉 데이터가 아직 수집되지 않았습니다" (DevTools Network 빈 배열 응답으로 대체 확인 가능) |
| 8 | Error state (D-18) | DevTools Network 에서 stock_daily_ohlcv block → "일봉 데이터를 불러오지 못했습니다" + "다시 시도" 버튼 / unblock 후 복구 |
| 9 | 회귀 (Phase 6/7/8) | StockHero / StockStatsGrid / StockNewsSection / StockDiscussionSection 정상 / 새로고침 시 Hero/Stats 갱신 + 차트도 refreshSignal 로 refetch (D-19 lock-in 확인) |

**승인 시그널:** "approved" 또는 "9개 검증 모두 OK" → 본 plan 완료 처리.

**이슈 발견 시:** 검증 번호 + 증상 (예: "검증 4 — 다크 모드에서 캔들 색상 미반영") → planner/executor 수정 후 재검증.

---

## Phase 09.2 종합 (3 plan 통합)

**완료된 코드 산출물 (Wave 1 + 2 + 3):**

| Wave | Plan | 산출물 |
| --- | --- | --- |
| 1 | 09.2-01 | REQUIREMENTS/PROJECT/ROADMAP 갱신 + `@gh-radar/shared` (DailyOhlcvRow/RangeKey) + `webapp/src/lib/chart-colors.ts` + `webapp/src/lib/daily-ohlcv-api.ts` + 16건 unit tests |
| 2 | 09.2-02 | `StockDailyChart` (lightweight-charts wrapper) + `StockDailyChartSection` (fetch+토글+분기) + `StockDailyChartSkeleton` + 8건 unit tests |
| 3 | 09.2-03 (Task 1~3) | StockDetailClient mount (1줄 import + 1줄 JSX) + mockup 라우트 5 파일 삭제 + 신규 E2E 3건 spec + STATE.md DI-03/04 follow-up |
| 3 | 09.2-03 Task 4 | **PENDING — orchestrator 가 사용자 manual verification 진행** |

**남은 단계:**

1. **Task 4 — 사용자 시각 검증** (orchestrator)
2. **Vercel 재배포** — Plan 03 코드 변경이 git push 후 자동 트리거되거나 `vercel --prod` 수동 실행 필요 시 별도 quick task (PLAN 03 `<output>` 마지막 줄 참조)
3. (선택) recharts/shadcn chart.tsx 미사용 의존 정리 — 별도 quick task (Task 2 Step A 보수적 유지 결정 이유)

---

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Auto-fix blocking] stock-detail-client.test.tsx 의 lightweight-charts raf ensureNotNull throw**

- **Found during:** Task 1 검증 단계 (`pnpm test` 실행 시 Test 4 — refresh 클릭 후 비동기 raf 콜백)
- **Issue:** StockDailyChartSection mount 추가 시 jsdom 환경의 lightweight-charts 가 `PriceAxisWidget._internal_optimalWidth` 의 `ensureNotNull` throw → uncaught exception 4건 → test runner exit 1 (test 자체는 157 passed/1 skipped 로 모두 PASS 였으나 errors 4건이 exit code 비정상)
- **Fix:** `webapp/src/components/stock/__tests__/stock-detail-client.test.tsx` 에 `vi.mock('../stock-daily-chart-section', () => ({ StockDailyChartSection: () => null }))` 추가. StockDailyChartSection 의 단위 검증은 이미 별도 `stock-daily-chart-section.test.tsx` 가 lightweight-charts 전체 mock 으로 수행 — 본 통합 테스트에선 mount 사실만 확인하면 충분.
- **Files modified:** `webapp/src/components/stock/__tests__/stock-detail-client.test.tsx` (+7 라인)
- **Commit:** `adeb2f3` (Task 1 commit 에 합산)

**2. [Rule 1/3 — 보조 정리] daily-ohlcv-api.ts JSDoc 의 mockups 경로 참조**

- **Found during:** Task 2 Step A (mockups 디렉터리 삭제 후 grep)
- **Issue:** `webapp/src/lib/daily-ohlcv-api.ts:4` JSDoc 이 삭제된 `webapp/src/app/mockups/shared/fetch-daily.ts` 경로를 history 참조로 포함 → PLAN 03 의 "grep mockups in src/ 0건" acceptance 미만족
- **Fix:** 해당 줄을 `RESEARCH 단계의 shared/fetch-daily.ts (삭제됨, Plan 03 정리) 를 정식 promote.` 로 변경 — 정보 보존 (RESEARCH 단계 promote 사실) + grep acceptance 만족
- **Files modified:** `webapp/src/lib/daily-ohlcv-api.ts` (1 라인 in-place 수정)
- **Commit:** `f288efb` (Step A commit 에 합산)

### Authentication Gates

없음.

### 강제 실행 회피 결정

**Playwright E2E spec 실제 실행은 본 subagent 범위에서 의도적 제외.**

- **이유:** dev 서버 미기동(localhost:3100 응답 0) + setup project 의 Supabase auth REST 호출 + 차트의 production Supabase 직접 호출. PLAN 03 가드 명시: "강제로 setup project 를 띄우지 말 것 — Cloud Run/Supabase 호출 비용 우려".
- **결정:** spec 파일만 작성/commit, 실행 검증은 사용자 또는 orchestrator checkpoint 책임. 본 SUMMARY 에 manual run 명령어 명시.

---

## Self-Check

수동 검증 결과:

**파일 존재:**
- `webapp/src/components/stock/stock-detail-client.tsx` (modified) — FOUND
- `webapp/src/components/stock/__tests__/stock-detail-client.test.tsx` (modified) — FOUND
- `webapp/src/lib/daily-ohlcv-api.ts` (modified) — FOUND
- `webapp/e2e/specs/stock-detail-chart.spec.ts` (created) — FOUND (110 라인)
- `.planning/STATE.md` (modified) — FOUND
- `webapp/src/app/mockups/` (deleted) — MISSING (의도된 삭제)

**Commit hash 존재:**
- `adeb2f3` (Task 1) — FOUND
- `f288efb` (Task 2 Step A) — FOUND
- `b5bc8c5` (Task 2 Step B) — FOUND
- `ccc78b6` (Task 3) — FOUND

**Acceptance grep:**
- StockDailyChartSection JSX mount = 1 ✓
- refreshSignal 전달 ≥1 ✓
- JSX 순서 Hero<Chart<Stats ✓
- mockups grep 0건 ✓
- DI-03/DI-04 ≥1건 ✓
- Phase 09.2 캔들스틱 ≥1건 ✓
- RLS authenticated 본문 ≥1건 ✓
- trailing newline 본문 ≥1건 ✓

**검증 게이트:**
- typecheck exit 0 ✓
- unit test 25 files 157 passed (1 skipped) / 0 errors ✓
- production build exit 0 (9 라우트, /stocks/[code] 278 kB) ✓
- E2E spec 실행: **Manual run required by user/orchestrator** (의도적 보류)

## Self-Check: PASSED
