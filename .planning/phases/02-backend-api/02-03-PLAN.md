---
phase: 02-backend-api
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
requirements: [INFR-03]
files_modified:
  - server/src/routes/health.ts
  - server/src/routes/scanner.ts
  - server/src/routes/stocks.ts
  - server/src/schemas/scanner.ts
  - server/src/schemas/search.ts
  - server/src/app.ts
  - server/src/server.ts
  - server/tests/routes/health.test.ts
  - server/tests/routes/scanner.test.ts
  - server/tests/routes/stock-detail.test.ts
  - server/tests/routes/search.test.ts
autonomous: false

must_haves:
  truths:
    - "/api/health → 200 + {status:'ok', timestamp, version} (INV-1)"
    - "/api/scanner → Stock[] 배열, 각 원소에 upperLimitProximity (INV-2, D-09)"
    - "/api/scanner?market=KOSPI&minRate=10&sort=volume_desc&limit=50 — 서버측 필터/정렬/제한 (D-07)"
    - "/api/stocks/:code 미존재 → 404 + STOCK_NOT_FOUND (INV-4, D-10)"
    - "/api/stocks/search?q= → ILIKE 부분일치, name 오름차순, limit 20 (INV-5, D-11)"
    - "Zod 스키마가 invalid query → 400 + INVALID_QUERY_PARAM (D-07, D-14)"
    - "검색 쿼리 sanitize: `,()%` 제거 후 ILIKE — PostgREST 표현식 주입 방지 (RESEARCH §6.4)"
    - "server.ts entry가 PORT env 존중하며 listen"
    - "로컬 `pnpm -F @gh-radar/server dev` + curl localhost:8080/api/health → 200 검증 (체크포인트)"
  artifacts:
    - path: "server/src/routes/health.ts"
      provides: "/api/health 라우터"
      contains: "status: 'ok'"
    - path: "server/src/routes/scanner.ts"
      provides: "/api/scanner 라우터 (필터/정렬)"
      contains: "upperLimitProximity"
    - path: "server/src/routes/stocks.ts"
      provides: "/api/stocks/:code + /api/stocks/search"
      contains: "maybeSingle"
    - path: "server/src/schemas/scanner.ts"
      provides: "ScannerQuery zod 스키마"
      contains: "z.enum"
    - path: "server/src/server.ts"
      provides: "프로덕션 엔트리 (listen)"
      contains: "app.listen"
  key_links:
    - from: "server/src/routes/*"
      to: "req.app.locals.supabase"
      via: "DI 주입"
      pattern: "app.locals.supabase"
    - from: "server/src/routes/scanner.ts"
      to: "server/src/mappers/stock.ts rowToStock"
      via: "import"
      pattern: "rowToStock"
    - from: "server/src/app.ts"
      to: "server/src/routes/{health,scanner,stocks}"
      via: "app.use 라우터 마운트"
      pattern: "app.use\\(.*/api/.*Router\\)"
---

<objective>
Phase 2 Wave 3 — 4개 엔드포인트(`/api/health`, `/api/scanner`, `/api/stocks/:code`, `/api/stocks/search`) 구현 + `server.ts` 엔트리 포인트 + 로컬 dev 서버 smoke 체크포인트.

Purpose: Express 앱이 실제 Supabase(또는 mock)에서 데이터를 읽어 JSON으로 반환하는 핵심 가치 구현.
Output: supertest 통합 테스트가 모든 엔드포인트를 mock으로 검증, 로컬 `pnpm dev` + curl로 실 Supabase 응답 확인.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-backend-api/02-CONTEXT.md
@.planning/phases/02-backend-api/02-RESEARCH.md
@.planning/phases/02-backend-api/02-VALIDATION.md
@supabase/migrations/20260413120000_init_tables.sql

<interfaces>
Wave 1 산출물 (import 대상):
```ts
// server/src/mappers/stock.ts
export type StockRow = { code, name, market, price: string, change_amount: string, ... };
export type StockWithProximity = Stock & { upperLimitProximity: number };
export function rowToStock(r: StockRow): StockWithProximity;

// server/src/errors.ts
export const StockNotFound = (code: string) => new ApiError(404, "STOCK_NOT_FOUND", ...);
export const InvalidQueryParam = (param, reason) => new ApiError(400, "INVALID_QUERY_PARAM", ...);
```

Wave 2 산출물:
```ts
// server/src/app.ts
export function createApp(deps: { supabase: SupabaseClient }): Express;
// 라우터 등록 주석 위치: `app.use("/api/scanner", ...)` 자리
```

Wave 1 mock:
```ts
// server/tests/fixtures/supabase-mock.ts
export function mockSupabase(state: { stocks: StockRow[] }): SupabaseClient;
// 지원: select/eq/or/order/limit/maybeSingle/then
```

**컬럼 상수** (RESEARCH §6.2 — `.select('*')` 금지):
```
const COLS = 'code,name,market,price,change_amount,change_rate,volume,open,high,low,market_cap,upper_limit,lower_limit,updated_at';
```

**ScannerQuery 스키마** (RESEARCH §5.7):
```ts
const ScannerQuery = z.object({
  market: z.enum(['KOSPI','KOSDAQ','ALL']).default('ALL'),
  minRate: z.coerce.number().optional(),
  sort: z.enum(['rate_desc','rate_asc','volume_desc']).default('rate_desc'),
  limit: z.coerce.number().int().min(1).max(10000).optional(),
});
```

**version 소스** (RESEARCH §Q2 권고 B): `process.env.APP_VERSION ?? 'dev'`
</interfaces>
</context>

<tasks>

<task type="auto" id="02-03-01" tdd="true">
  <name>Task 1: health + scanner 라우터 + zod 스키마</name>
  <read_first>
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-RESEARCH.md (§5.7 zod, §6.2 컬럼 상수, §6.3 매퍼, §Q2 version)
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-CONTEXT.md (D-06~D-12)
    - /Users/alex/repos/gh-radar/server/src/mappers/stock.ts
    - /Users/alex/repos/gh-radar/server/src/errors.ts
    - /Users/alex/repos/gh-radar/server/tests/fixtures/supabase-mock.ts
    - /Users/alex/repos/gh-radar/server/tests/fixtures/stocks.ts
  </read_first>
  <behavior>
    - GET /api/health → 200, body `{status:'ok', timestamp: ISO8601, version: string}`; version은 `process.env.APP_VERSION ?? 'dev'`
    - GET /api/scanner (no params) → 200, Array, 각 원소에 `code, name, market, price, upperLimitProximity` 모두 존재
    - GET /api/scanner?market=KOSPI → KOSPI만 반환
    - GET /api/scanner?minRate=2 → changeRate >= 2인 종목만
    - GET /api/scanner?sort=volume_desc → volume 내림차순 정렬
    - GET /api/scanner?sort=rate_asc&limit=1 → changeRate 오름차순 1개만
    - GET /api/scanner?market=INVALID → 400 + INVALID_QUERY_PARAM
    - GET /api/scanner?limit=abc → 400 + INVALID_QUERY_PARAM
    - GET /api/scanner?limit=99999 → 400 (max 10000)
  </behavior>
  <action>
    **파일 1: `server/src/schemas/scanner.ts` 신규:**
    ```ts
    import { z } from "zod";

    export const ScannerQuery = z.object({
      market: z.enum(["KOSPI", "KOSDAQ", "ALL"]).default("ALL"),
      minRate: z.coerce.number().optional(),
      sort: z.enum(["rate_desc", "rate_asc", "volume_desc"]).default("rate_desc"),
      limit: z.coerce.number().int().min(1).max(10000).optional(),
    });

    export type ScannerQueryT = z.infer<typeof ScannerQuery>;
    ```

    **파일 2: `server/src/routes/health.ts` 신규:**
    ```ts
    import { Router } from "express";

    export const healthRouter = Router();

    healthRouter.get("/", (_req, res) => {
      res.json({
        status: "ok",
        timestamp: new Date().toISOString(),
        version: process.env.APP_VERSION ?? "dev",
      });
    });
    ```

    **파일 3: `server/src/routes/scanner.ts` 신규** (RESEARCH §6.2 + §5.7):
    ```ts
    import { Router } from "express";
    import type { SupabaseClient } from "@supabase/supabase-js";
    import { ScannerQuery } from "../schemas/scanner";
    import { rowToStock, type StockRow } from "../mappers/stock";
    import { ApiError } from "../errors";

    export const COLS =
      "code,name,market,price,change_amount,change_rate,volume,open,high,low,market_cap,upper_limit,lower_limit,updated_at";

    export const scannerRouter = Router();

    scannerRouter.get("/", async (req, res, next) => {
      try {
        const parsed = ScannerQuery.safeParse(req.query);
        if (!parsed.success) {
          const issue = parsed.error.issues[0];
          throw new ApiError(
            400,
            "INVALID_QUERY_PARAM",
            `${issue.path.join(".")}: ${issue.message}`,
          );
        }
        const { market, minRate, sort, limit } = parsed.data;
        const supabase = req.app.locals.supabase as SupabaseClient;

        let query = supabase.from("stocks").select(COLS);

        if (market !== "ALL") query = query.eq("market", market);
        if (typeof minRate === "number") query = query.gte("change_rate", minRate);

        const sortMap = {
          rate_desc: { col: "change_rate", asc: false },
          rate_asc: { col: "change_rate", asc: true },
          volume_desc: { col: "volume", asc: false },
        } as const;
        const s = sortMap[sort];
        query = query.order(s.col, { ascending: s.asc });

        if (typeof limit === "number") query = query.limit(limit);

        const { data, error } = await query;
        if (error) throw error;
        const rows = (data ?? []) as unknown as StockRow[];
        res.setHeader("Cache-Control", "no-store"); // RESEARCH §Q1 권고 A
        res.json(rows.map(rowToStock));
      } catch (e) {
        next(e);
      }
    });
    ```

    **주의:** Wave 1 mock의 `or` 핸들러는 `filtered = state.stocks.filter(...)` 형태로 **덮어쓰기** 방식이라, 복수 필터(market=KOSPI + minRate=1.0)를 동시 적용하면 뒤 호출이 앞 호출 결과를 날린다. 누적형(`filtered = filtered.filter(...)`)으로 전환 + `gte`/`eq`를 filtered 체인에 합류시키는 리팩터가 필요하다.

    **파일 4: `server/tests/fixtures/supabase-mock.ts` 수정** — Wave 1 구현을 누적형으로 전환하고 `gte` 메서드를 추가한다. 기존 파일 (Wave 1, 02-01 Task X 산출물)을 읽은 뒤, `makeBuilder` 내부 상태/메서드를 아래 unified diff 형태 그대로 패치한다.

    **변경 요점:**
    - `eq`를 지연 평가(`eqCol`/`eqVal` 메모이제이션) → **즉시 filter 누적**으로 전환 (market 필터도 filtered 체인 합류)
    - `or`를 `filtered = state.stocks.filter(...)` **덮어쓰기** → **filtered 누적 filter**로 전환
    - `gte(col, val)` 신규 추가 — filtered 누적 filter (숫자 비교)
    - `order`, `limit`는 exec() 시점 적용 유지하되 일관성 위해 그대로 존속 (exec()에서 eqCol/eqVal 이중 적용 로직 제거 → 순수 filtered 반영)

    **Unified diff (server/tests/fixtures/supabase-mock.ts):**
    ```diff
     export function mockSupabase(state: State): SupabaseClient {
       const makeBuilder = () => {
         let filtered: StockRow[] = [...state.stocks];
         let orderCol: string | null = null;
         let orderAsc = true;
         let limitN: number | null = null;
    -    let eqCol: string | null = null;
    -    let eqVal: unknown = null;

         const exec = () => {
           let rows = [...filtered];
    -      if (eqCol) rows = rows.filter((r) => (r as any)[eqCol!] === eqVal);
           if (orderCol) {
             rows.sort((a, b) => {
               const av = Number((a as any)[orderCol!]) || 0;
               const bv = Number((b as any)[orderCol!]) || 0;
               return orderAsc ? av - bv : bv - av;
             });
           }
           if (limitN != null) rows = rows.slice(0, limitN);
           return rows;
         };

         const builder: any = {
           select: vi.fn().mockReturnThis(),
           eq: vi.fn().mockImplementation((col: string, val: unknown) => {
    -        eqCol = col; eqVal = val; return builder;
    +        filtered = filtered.filter((r) => (r as any)[col] === val);
    +        return builder;
           }),
    +      gte: vi.fn().mockImplementation((col: string, val: number) => {
    +        filtered = filtered.filter(
    +          (r) => Number((r as any)[col]) >= Number(val),
    +        );
    +        return builder;
    +      }),
           or: vi.fn().mockImplementation((expr: string) => {
             // parse "name.ilike.%q%,code.ilike.%q%"
             const parts = expr.split(",");
             const patterns = parts.map((p) => {
               const m = p.match(/^(\w+)\.ilike\.%(.*)%$/);
               return m ? { col: m[1], q: m[2].toLowerCase() } : null;
             }).filter(Boolean) as { col: string; q: string }[];
    -        filtered = state.stocks.filter((r) =>
    +        filtered = filtered.filter((r) =>
               patterns.some((p) => String((r as any)[p.col]).toLowerCase().includes(p.q))
             );
             return builder;
           }),
           order: vi.fn().mockImplementation((col: string, opts?: { ascending?: boolean }) => {
             orderCol = col; orderAsc = opts?.ascending ?? true; return builder;
           }),
           limit: vi.fn().mockImplementation((n: number) => {
             limitN = n; return builder;
           }),
           maybeSingle: vi.fn().mockImplementation(async () => {
             const rows = exec();
             return { data: rows[0] ?? null, error: null };
           }),
           then: (resolve: any) => resolve({ data: exec(), error: null }),
         };
         return builder;
       };
       return { from: vi.fn().mockImplementation(() => makeBuilder()) } as unknown as SupabaseClient;
     }
    ```

    **근거:** scanner route는 `eq("market", ...)` → `gte("change_rate", ...)` 순으로 체이닝한다. 덮어쓰기 방식에서는 `gte` 호출이 `eq` 결과를 무효화할 수 있고 `or`는 반대로 이전 `eq`를 무시한다. 누적형으로 바꾸면 호출 순서와 무관하게 AND 시맨틱이 보장된다.

    **파일 5: `server/src/app.ts` 수정** — Wave 2에서 만든 주석 자리 찾아서:
    ```
    //    app.use("/api/scanner", scannerRouter);
    //    app.use("/api/stocks", stocksRouter);
    //    app.use("/api/health", healthRouter);
    ```
    주석 제거 + 라우터 import 추가:
    ```ts
    import { healthRouter } from "./routes/health";
    import { scannerRouter } from "./routes/scanner";
    // stocksRouter는 Task 2에서 추가
    ```
    그리고 블록을:
    ```ts
    app.use("/api/health", healthRouter);
    app.use("/api/scanner", scannerRouter);
    // app.use("/api/stocks", stocksRouter);  // Task 2
    ```

    **파일 6: `server/tests/routes/health.test.ts` 신규:**
    ```ts
    import { describe, it, expect } from "vitest";
    import request from "supertest";
    import { createApp } from "../../src/app";
    import { mockSupabase } from "../fixtures/supabase-mock";

    describe("/api/health", () => {
      it("returns 200 + {status,timestamp,version}", async () => {
        process.env.APP_VERSION = "abc1234";
        const app = createApp({ supabase: mockSupabase({ stocks: [] }) });
        const r = await request(app).get("/api/health");
        expect(r.status).toBe(200);
        expect(r.body.status).toBe("ok");
        expect(typeof r.body.timestamp).toBe("string");
        expect(new Date(r.body.timestamp).toISOString()).toBe(r.body.timestamp);
        expect(r.body.version).toBe("abc1234");
      });
      it("falls back to 'dev' when APP_VERSION unset", async () => {
        delete process.env.APP_VERSION;
        const app = createApp({ supabase: mockSupabase({ stocks: [] }) });
        const r = await request(app).get("/api/health");
        expect(r.body.version).toBe("dev");
      });
    });
    ```

    **파일 7: `server/tests/routes/scanner.test.ts` 신규:**
    ```ts
    import { describe, it, expect } from "vitest";
    import request from "supertest";
    import { createApp } from "../../src/app";
    import { mockSupabase } from "../fixtures/supabase-mock";
    import { allRows, samsungRow, kakaoRow, kosdaqRow } from "../fixtures/stocks";

    const app = () => createApp({ supabase: mockSupabase({ stocks: allRows }) });

    describe("/api/scanner", () => {
      it("200 + 전체 종목 반환, upperLimitProximity 포함", async () => {
        const r = await request(app()).get("/api/scanner");
        expect(r.status).toBe(200);
        expect(Array.isArray(r.body)).toBe(true);
        expect(r.body.length).toBe(3);
        const s = r.body.find((x: any) => x.code === "005930");
        expect(s).toBeDefined();
        expect(typeof s.upperLimitProximity).toBe("number");
        expect(s.upperLimitProximity).toBeCloseTo(70000 / 91000);
      });
      it("market=KOSPI → KOSPI만", async () => {
        const r = await request(app()).get("/api/scanner?market=KOSPI");
        expect(r.body.every((s: any) => s.market === "KOSPI")).toBe(true);
        expect(r.body.length).toBe(2);
      });
      it("minRate=1.0 → changeRate>=1.0 만", async () => {
        const r = await request(app()).get("/api/scanner?minRate=1.0");
        expect(r.body.every((s: any) => s.changeRate >= 1.0)).toBe(true);
      });
      it("sort=volume_desc → volume 내림차순", async () => {
        const r = await request(app()).get("/api/scanner?sort=volume_desc");
        const vols = r.body.map((s: any) => s.volume);
        const sorted = [...vols].sort((a, b) => b - a);
        expect(vols).toEqual(sorted);
      });
      it("limit=1 → 1개만", async () => {
        const r = await request(app()).get("/api/scanner?limit=1");
        expect(r.body.length).toBe(1);
      });
      it("Cache-Control: no-store 헤더", async () => {
        const r = await request(app()).get("/api/scanner");
        expect(r.headers["cache-control"]).toBe("no-store");
      });
      it("invalid market → 400 INVALID_QUERY_PARAM", async () => {
        const r = await request(app()).get("/api/scanner?market=INVALID");
        expect(r.status).toBe(400);
        expect(r.body.error.code).toBe("INVALID_QUERY_PARAM");
      });
      it("invalid limit (non-numeric) → 400", async () => {
        const r = await request(app()).get("/api/scanner?limit=abc");
        expect(r.status).toBe(400);
        expect(r.body.error.code).toBe("INVALID_QUERY_PARAM");
      });
      it("limit > 10000 → 400", async () => {
        const r = await request(app()).get("/api/scanner?limit=99999");
        expect(r.status).toBe(400);
      });
      it("market=KOSPI와 minRate=1.0이 동시 적용돼 둘 다 만족하는 row만 반환", async () => {
        // allRows = [samsungRow(KOSPI,+1.45), kakaoRow(KOSPI,...), kosdaqRow(KOSDAQ,-1.23)]
        // 기대: market=KOSPI AND changeRate>=1.0 → samsungRow 중심, kosdaqRow는 market/rate 둘 다 탈락
        const res = await request(app()).get("/api/scanner?market=KOSPI&minRate=1.0");
        expect(res.status).toBe(200);
        expect(res.body.every((s: any) => s.market === "KOSPI")).toBe(true);
        expect(res.body.every((s: any) => s.changeRate >= 1.0)).toBe(true);
        // 반환 배열에 KOSDAQ(091990) 미포함, KOSPI 음수 rate 미포함
        expect(res.body.some((s: any) => s.code === "091990")).toBe(false);
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/server && pnpm test --run tests/routes/health.test.ts tests/routes/scanner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `server/src/schemas/scanner.ts`가 `z.enum(["KOSPI", "KOSDAQ", "ALL"])` + `min(1).max(10000)` 포함
    - `server/src/routes/health.ts`가 `status: "ok"` + `new Date().toISOString()` + `APP_VERSION ?? "dev"` 포함
    - `server/src/routes/scanner.ts`에 COLS 상수 정확히 `"code,name,market,price,change_amount,change_rate,volume,open,high,low,market_cap,upper_limit,lower_limit,updated_at"` (grep 정확 매칭)
    - `server/src/routes/scanner.ts`가 `res.setHeader("Cache-Control", "no-store")` 포함
    - `server/src/app.ts`가 `app.use("/api/health", healthRouter)` + `app.use("/api/scanner", scannerRouter)` 모두 포함
    - `server/tests/fixtures/supabase-mock.ts`에 `gte(` AND `eq(` 메서드가 **filtered 누적 filter**(`filtered = filtered.filter(`) 형태로 존재 (grep 2건 이상)
    - `server/tests/fixtures/supabase-mock.ts`의 `or` 핸들러가 `filtered = state.stocks.filter` 가 아닌 `filtered = filtered.filter` 사용 (덮어쓰기 제거 확인)
    - `server/tests/routes/scanner.test.ts`에 `"market=KOSPI&minRate"` 또는 동등한 동시 필터 케이스 `it(` 블록 존재 (grep)
    - `vitest run tests/routes/health.test.ts tests/routes/scanner.test.ts` exit 0 — 12개 테스트 케이스 통과 (동시 필터 케이스 추가로 +1)
  </acceptance_criteria>
  <done>/api/health, /api/scanner 2개 엔드포인트 + zod 스키마 + mock 확장 완성.</done>
</task>

<task type="auto" id="02-03-02" tdd="true">
  <name>Task 2: stocks 라우터 (detail + search) + 검색 sanitizer</name>
  <read_first>
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-RESEARCH.md (§6.4 ILIKE + escape, §6.5 maybeSingle)
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-CONTEXT.md (D-10, D-11)
    - /Users/alex/repos/gh-radar/server/src/routes/scanner.ts (Task 1 — COLS 상수 재사용)
    - /Users/alex/repos/gh-radar/server/src/errors.ts
  </read_first>
  <behavior>
    - GET /api/stocks/005930 → 200 + `{code:"005930", name:"삼성전자", ..., upperLimitProximity: number}`
    - GET /api/stocks/999999 → 404 + `{error:{code:"STOCK_NOT_FOUND", message:"Stock 999999 not found"}}`
    - GET /api/stocks/search?q=삼성 → 200 + 배열, 각 원소 code 또는 name에 "삼성" 포함, length ≤ 20
    - GET /api/stocks/search?q= (빈) → 400 + INVALID_QUERY_PARAM
    - GET /api/stocks/search → 400 (q 누락)
    - GET /api/stocks/search?q=삼성%) → sanitize된 "삼성" 검색; PostgREST or expr 안 깨짐
    - 검색 결과 name 오름차순 정렬
  </behavior>
  <action>
    **파일 1: `server/src/schemas/search.ts` 신규:**
    ```ts
    import { z } from "zod";

    export const SearchQuery = z.object({
      q: z.string().min(1, "required").max(64, "too long"),
    });
    export type SearchQueryT = z.infer<typeof SearchQuery>;

    export function sanitizeSearchTerm(s: string): string {
      // PostgREST `or` 표현식에서 `,()%`는 파서를 깨뜨리므로 제거 (RESEARCH §6.4)
      return s.replace(/[,()%]/g, "");
    }
    ```

    **파일 2: `server/src/routes/stocks.ts` 신규:**
    ```ts
    import { Router } from "express";
    import type { SupabaseClient } from "@supabase/supabase-js";
    import { SearchQuery, sanitizeSearchTerm } from "../schemas/search";
    import { COLS } from "./scanner";
    import { rowToStock, type StockRow } from "../mappers/stock";
    import { ApiError, StockNotFound } from "../errors";

    export const stocksRouter = Router();

    // /api/stocks/search?q=...
    // **순서 중요:** /search를 /:code보다 먼저 등록해야 '/:code'가 'search'를 흡수하지 않음
    stocksRouter.get("/search", async (req, res, next) => {
      try {
        const parsed = SearchQuery.safeParse(req.query);
        if (!parsed.success) {
          const issue = parsed.error.issues[0];
          throw new ApiError(400, "INVALID_QUERY_PARAM", `${issue.path.join(".")}: ${issue.message}`);
        }
        const q = sanitizeSearchTerm(parsed.data.q);
        if (q.length === 0) {
          throw new ApiError(400, "INVALID_QUERY_PARAM", "q: empty after sanitization");
        }
        const supabase = req.app.locals.supabase as SupabaseClient;
        const { data, error } = await supabase
          .from("stocks")
          .select(COLS)
          .or(`name.ilike.%${q}%,code.ilike.%${q}%`)
          .order("name", { ascending: true })
          .limit(20);
        if (error) throw error;
        const rows = (data ?? []) as unknown as StockRow[];
        res.json(rows.map(rowToStock));
      } catch (e) {
        next(e);
      }
    });

    // /api/stocks/:code
    stocksRouter.get("/:code", async (req, res, next) => {
      try {
        const code = req.params.code;
        // 종목코드 형식: 6자리 숫자 또는 영숫자(우선주 등). 단순히 길이 제한만.
        if (!/^[A-Za-z0-9]{1,10}$/.test(code)) {
          throw new ApiError(400, "INVALID_QUERY_PARAM", "code: invalid format");
        }
        const supabase = req.app.locals.supabase as SupabaseClient;
        const { data, error } = await supabase
          .from("stocks")
          .select(COLS)
          .eq("code", code)
          .maybeSingle();
        if (error) throw error;
        if (!data) throw StockNotFound(code);
        res.json(rowToStock(data as unknown as StockRow));
      } catch (e) {
        next(e);
      }
    });
    ```

    **파일 3: `server/src/app.ts` 수정** — Task 1에서 주석 처리한 `app.use("/api/stocks", stocksRouter)` 활성화:
    ```ts
    import { stocksRouter } from "./routes/stocks";
    // ...
    app.use("/api/stocks", stocksRouter);
    ```

    **파일 4: `server/tests/routes/stock-detail.test.ts` 신규:**
    ```ts
    import { describe, it, expect } from "vitest";
    import request from "supertest";
    import { createApp } from "../../src/app";
    import { mockSupabase } from "../fixtures/supabase-mock";
    import { allRows } from "../fixtures/stocks";

    const app = () => createApp({ supabase: mockSupabase({ stocks: allRows }) });

    describe("/api/stocks/:code", () => {
      it("기존 코드 → 200 + stock", async () => {
        const r = await request(app()).get("/api/stocks/005930");
        expect(r.status).toBe(200);
        expect(r.body.code).toBe("005930");
        expect(r.body.name).toBe("삼성전자");
        expect(typeof r.body.upperLimitProximity).toBe("number");
      });
      it("없는 코드 → 404 STOCK_NOT_FOUND", async () => {
        const r = await request(app()).get("/api/stocks/000000");
        expect(r.status).toBe(404);
        expect(r.body.error.code).toBe("STOCK_NOT_FOUND");
        expect(r.body.error.message).toContain("000000");
      });
      it("잘못된 형식 → 400 INVALID_QUERY_PARAM", async () => {
        const r = await request(app()).get("/api/stocks/!!@@");
        expect(r.status).toBe(400);
      });
    });
    ```

    **파일 5: `server/tests/routes/search.test.ts` 신규:**
    ```ts
    import { describe, it, expect } from "vitest";
    import request from "supertest";
    import { createApp } from "../../src/app";
    import { mockSupabase } from "../fixtures/supabase-mock";
    import { allRows } from "../fixtures/stocks";
    import { sanitizeSearchTerm } from "../../src/schemas/search";

    const app = () => createApp({ supabase: mockSupabase({ stocks: allRows }) });

    describe("/api/stocks/search", () => {
      it("q=삼성 → 삼성전자 매치", async () => {
        const r = await request(app()).get("/api/stocks/search?q=%EC%82%BC%EC%84%B1");
        expect(r.status).toBe(200);
        expect(Array.isArray(r.body)).toBe(true);
        expect(r.body.some((s: any) => s.name.includes("삼성"))).toBe(true);
        expect(r.body.length).toBeLessThanOrEqual(20);
      });
      it("q=005930 → code 매치", async () => {
        const r = await request(app()).get("/api/stocks/search?q=005930");
        expect(r.body.some((s: any) => s.code === "005930")).toBe(true);
      });
      it("q 누락 → 400", async () => {
        const r = await request(app()).get("/api/stocks/search");
        expect(r.status).toBe(400);
        expect(r.body.error.code).toBe("INVALID_QUERY_PARAM");
      });
      it("q 빈 문자열 → 400", async () => {
        const r = await request(app()).get("/api/stocks/search?q=");
        expect(r.status).toBe(400);
      });
    });

    describe("sanitizeSearchTerm", () => {
      it("removes ,()%", () => {
        expect(sanitizeSearchTerm("삼성,(주)%")).toBe("삼성주");
      });
      it("preserves Korean/English/digits", () => {
        expect(sanitizeSearchTerm("Samsung 005930 삼성")).toBe("Samsung 005930 삼성");
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/server && pnpm test --run tests/routes/stock-detail.test.ts tests/routes/search.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `server/src/routes/stocks.ts`가 `/search`를 `/:code`보다 먼저 등록 (라인 순서 확인 — grep -n `router.get.*search` vs `router.get.*:code`)
    - `server/src/routes/stocks.ts`가 `.maybeSingle()` 사용 (`.single()` 아님)
    - `server/src/routes/stocks.ts`의 search가 `.limit(20)` + `.order("name", { ascending: true })` 포함
    - `server/src/schemas/search.ts`의 `sanitizeSearchTerm`이 `/[,()%]/g` 정규식 포함
    - `server/src/app.ts`에 `app.use("/api/stocks", stocksRouter)` 존재
    - `vitest run tests/routes/stock-detail.test.ts tests/routes/search.test.ts` exit 0 — 8개 테스트 케이스 통과
  </acceptance_criteria>
  <done>4개 엔드포인트 전부 supertest로 검증 완료. 실 Supabase 호출은 Task 3 체크포인트에서.</done>
</task>

<task type="auto" id="02-03-03">
  <name>Task 3: server.ts 엔트리 포인트 + 전체 테스트 집계</name>
  <read_first>
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-RESEARCH.md (§7.2 server.ts 분리)
    - /Users/alex/repos/gh-radar/server/src/app.ts (Wave 2)
    - /Users/alex/repos/gh-radar/server/src/config.ts (Wave 1)
  </read_first>
  <action>
    **파일 1: `server/src/server.ts` 신규** (RESEARCH §7.2):
    ```ts
    import { createApp } from "./app";
    import { supabase } from "./services/supabase";
    import { logger } from "./logger";
    import { loadConfig } from "./config";

    const config = loadConfig();
    const app = createApp({ supabase });

    app.listen(config.port, () => {
      logger.info(
        { port: config.port, env: config.nodeEnv, version: config.appVersion },
        "gh-radar-server listening",
      );
    });

    // Graceful shutdown (Cloud Run SIGTERM)
    for (const sig of ["SIGTERM", "SIGINT"] as const) {
      process.on(sig, () => {
        logger.info({ signal: sig }, "shutting down");
        process.exit(0);
      });
    }
    ```

    **빌드 확인:** `pnpm -F @gh-radar/server build`가 `dist/server.js` 생성해야 함 (Wave 4 Docker가 참조).

    **단순성 우선 (CLAUDE.md):** graceful shutdown은 SIGTERM/SIGINT 한 줄 핸들러로 충분. `server.close()` 복잡성은 Cloud Run이 인스턴스 kill로 처리.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/server && pnpm build && test -f dist/server.js && pnpm test --run</automated>
  </verify>
  <acceptance_criteria>
    - `server/src/server.ts` 파일 존재
    - `server/src/server.ts`가 `createApp({ supabase })` 호출 + `app.listen(config.port, ...)` 포함
    - `server/src/server.ts`가 SIGTERM/SIGINT 핸들러 포함 (grep 'SIGTERM')
    - `pnpm -F @gh-radar/server build` 성공, `server/dist/server.js` 파일 존재
    - `pnpm -F @gh-radar/server test --run` exit 0 — 이 페이즈 누적 모든 테스트 통과 (request-id 3 + rate-limit 2 + error-handler 5 + cors-integration 9 + rowToStock 5 + cors-config 7 + health 2 + scanner 10 + stock-detail 3 + search 6 ≈ 52개 이상)
  </acceptance_criteria>
  <done>production 엔트리 완성. 로컬 실행 가능 (체크포인트에서 검증).</done>
</task>

<task type="checkpoint:human-verify" id="02-03-04" gate="blocking">
  <name>Task 4 [체크포인트]: 로컬 dev 서버 smoke 테스트</name>
  <action>체크포인트 — 아래 `<how-to-verify>` 절차를 자동 실행 후 사용자 승인 대기.</action>
  <verify>체크포인트 — `<how-to-verify>`의 curl 5종 모두 예상 응답 반환.</verify>
  <done>사용자 "approved" 확인 시 Wave 4로 진행.</done>
  <what-built>
    - Express app (createApp + 4 라우터 + 미들웨어 스택)
    - server.ts 엔트리 포인트
    - 52+ supertest/vitest 테스트 통과
  </what-built>
  <how-to-verify>
    **자동 검증 (Claude가 실행):**
    1. `cd /Users/alex/repos/gh-radar`
    2. 실 Supabase env를 `server/.env`에 준비 (사용자가 Phase 1에서 이미 보유):
       ```
       SUPABASE_URL=<Phase 1 사용한 URL>
       SUPABASE_SERVICE_ROLE_KEY=<service_role key>
       CORS_ALLOWED_ORIGINS=http://localhost:3000
       NODE_ENV=development
       LOG_LEVEL=debug
       PORT=8080
       APP_VERSION=local-dev
       ```
    3. `pnpm -F @gh-radar/server dev` 백그라운드 실행 (run_in_background)
    4. 5초 대기 후 아래 curl 3회 실행:
       ```bash
       curl -fsS http://localhost:8080/api/health | jq -e '.status=="ok"'
       curl -fsS "http://localhost:8080/api/scanner?market=KOSPI&limit=3" | jq -e 'type=="array" and (.[0].upperLimitProximity|type=="number")'
       curl -fsS http://localhost:8080/api/stocks/005930 | jq -e '.code=="005930"'
       curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/api/stocks/000000  # 예상: 404
       curl -fsS -D - http://localhost:8080/api/health -o /dev/null | grep -i 'x-request-id:'
       ```
    5. 서버 종료 (kill background process)

    **사용자 확인 포인트:**
    - [ ] 5개 curl 모두 예상 응답 반환 (Claude가 보여줌)
    - [ ] 콘솔에 JSON 로그 스트림 (request_id 필드 포함) 확인
    - [ ] 실 Supabase에서 삼성전자(005930) 데이터가 Phase 1 인제스션으로 존재하는지 확인

    **실패 시:** 환경변수 오타, Supabase RLS(service_role은 영향 없어야 함), CORS_ALLOWED_ORIGINS 형식 오류 순으로 진단.
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 이슈 기술. 모든 curl 통과 시 다음 wave(Docker 빌드)로 진행 가능.</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-02-04 | I (SQL/PostgREST injection) | scanner.ts + stocks.ts | mitigate | ScannerQuery/SearchQuery zod 스키마가 enum/number 강제. 악성 `market=DROP TABLE` → z.enum 거부 → 400. scanner.test.ts 악성 입력 케이스 + search.test.ts sanitizeSearchTerm 테스트로 검증 |
| T-02-04b | I (ILIKE or-expr 주입) | stocks.ts search | mitigate | `sanitizeSearchTerm`이 `,()%` 제거 — PostgREST `or` 표현식 파서 보호. search.test.ts "sanitizeSearchTerm removes ,()%" assertion |
| T-02-11 | T (Route order bug) | stocks.ts | mitigate | `/search`를 `/:code`보다 먼저 등록. acceptance_criteria에서 grep으로 라인 순서 검증 |
| T-02-12 | I (종목코드 path traversal) | stocks.ts `/:code` | mitigate | `/^[A-Za-z0-9]{1,10}$/` 정규식 검증 후 400. stock-detail.test.ts "잘못된 형식" 케이스 |
| T-02-13 | D (대용량 응답 DoS) | scanner.ts | mitigate | zod `limit.max(10000)` + D-08 "페이지네이션 없음"은 최대 10000 상한. scanner.test.ts "limit > 10000 → 400" |
</threat_model>

<verification>
로컬 실행:
1. `pnpm -F @gh-radar/server build && test -f dist/server.js`
2. `pnpm -F @gh-radar/server test --run` → 52+ 테스트 exit 0
3. **체크포인트(Task 4)**: 로컬 dev + 실 Supabase curl 5종 통과
</verification>

<success_criteria>
- [ ] 4개 엔드포인트 전부 supertest로 검증됨 (health 2, scanner 10, stock-detail 3, search 6 + sanitizer 2)
- [ ] `server/src/schemas/scanner.ts` + `server/src/schemas/search.ts` zod 스키마 존재
- [ ] `server/src/server.ts` 프로덕션 엔트리 작성됨
- [ ] `pnpm build`가 `dist/server.js` 생성
- [ ] 로컬 `pnpm dev` + curl 5종 모두 통과 (체크포인트)
- [ ] 응답 body에 항상 X-Request-Id 헤더 존재 (integration 테스트로 검증)
- [ ] /api/scanner에 `Cache-Control: no-store` 헤더 존재
</success_criteria>

<output>
완료 후: `.planning/phases/02-backend-api/02-03-SUMMARY.md` — 엔드포인트 목록, 테스트 통과 수, 로컬 smoke 결과.
</output>
