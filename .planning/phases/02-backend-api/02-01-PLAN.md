---
phase: 02-backend-api
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [INFR-03]
files_modified:
  - server/package.json
  - server/tsconfig.json
  - server/vitest.config.ts
  - server/src/config.ts
  - server/src/logger.ts
  - server/src/services/supabase.ts
  - server/src/types/express.d.ts
  - server/src/errors.ts
  - server/src/mappers/stock.ts
  - server/src/services/cors-config.ts
  - server/tests/setup.ts
  - server/tests/fixtures/stocks.ts
  - server/tests/fixtures/supabase-mock.ts
  - server/tests/mappers/stock.test.ts
  - server/tests/services/cors-config.test.ts
  - packages/shared/src/api.ts
  - packages/shared/src/index.ts
  - pnpm-lock.yaml
autonomous: true

must_haves:
  truths:
    - "server 워크스페이스가 독립 빌드/테스트/typecheck 가능"
    - "supabase 싱글턴이 SUPABASE_URL + SERVICE_ROLE_KEY 검증 후 노출"
    - "rowToStock 매퍼가 string numeric → number 변환 + upperLimitProximity 계산 (D-09)"
    - "parseAllowedOrigins가 쉼표 구분 + /regex/ 리터럴 모두 파싱 (D-18)"
    - "ApiError 단일 베이스 + 편의 팩토리(StockNotFound, InvalidQueryParam, ValidationFailed)"
    - "packages/shared에 ApiError/ApiSuccess envelope 타입 export (D-04)"
  artifacts:
    - path: "server/src/services/supabase.ts"
      provides: "Supabase 싱글턴 (service_role)"
      contains: "createClient"
    - path: "server/src/mappers/stock.ts"
      provides: "rowToStock + upperLimitProximity 계산"
      contains: "upperLimitProximity"
    - path: "server/src/services/cors-config.ts"
      provides: "parseAllowedOrigins + corsOptions"
      contains: "parseAllowedOrigins"
    - path: "server/src/errors.ts"
      provides: "ApiError 계층"
      contains: "class ApiError"
    - path: "packages/shared/src/api.ts"
      provides: "API envelope 타입"
      contains: "ApiErrorBody"
    - path: "server/vitest.config.ts"
      provides: "vitest 설정"
      contains: "defineConfig"
  key_links:
    - from: "server/src/services/supabase.ts"
      to: "process.env.SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY"
      via: "런타임 검증 throw"
      pattern: "must be set"
    - from: "server/src/mappers/stock.ts"
      to: "@gh-radar/shared (Stock, Market)"
      via: "import"
      pattern: "from ['\"]@gh-radar/shared"
---

<objective>
Phase 2 Wave 1 — Express 서버 스캐폴드 + 공용 유틸/타입/매퍼를 독립적으로 구축한다.
Express 앱 인스턴스는 만들지 **않는다** (Wave 2). 이 Plan은 미들웨어/라우터가 소비할 "순수 컴포넌트"만 구현한다.

Purpose: 이후 wave가 병렬로 진행될 수 있도록 공유 기초(타입·매퍼·CORS 파서·에러 계층·Supabase 클라이언트·테스트 인프라)를 선제 확정.
Output: `server/` 워크스페이스가 `pnpm -F @gh-radar/server build`, `pnpm -F @gh-radar/server test --run`, `pnpm -F @gh-radar/server typecheck` 모두 exit 0으로 통과.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-backend-api/02-CONTEXT.md
@.planning/phases/02-backend-api/02-RESEARCH.md
@.planning/phases/02-backend-api/02-VALIDATION.md
@packages/shared/src/stock.ts
@packages/shared/src/index.ts
@workers/ingestion/src/services/supabase.ts
@workers/ingestion/src/logger.ts
@workers/ingestion/package.json
@tsconfig.base.json
@supabase/migrations/20260413120000_init_tables.sql

<interfaces>
<!-- Extracted from existing codebase — executor MUST use these verbatim. -->

From packages/shared/src/stock.ts:
```ts
export type Market = "KOSPI" | "KOSDAQ";
export type Stock = {
  code: string; name: string; market: Market;
  price: number; changeAmount: number; changeRate: number;
  volume: number; open: number; high: number; low: number;
  marketCap: number; upperLimit: number; lowerLimit: number;
  updatedAt: string;
};
```

From supabase/migrations/20260413120000_init_tables.sql — `stocks` 컬럼:
```
code text PK, name text, market text,
price numeric(20,2), change_amount numeric(20,2), change_rate numeric(8,4),
volume bigint, open numeric(20,2), high numeric(20,2), low numeric(20,2),
market_cap bigint, upper_limit numeric(20,2), lower_limit numeric(20,2),
updated_at timestamptz
```

Postgres numeric → Supabase JS returns **string** (정밀도 보존). `Number()` 캐스팅 필수.

From workers/ingestion/src/services/supabase.ts (대칭 패턴):
```ts
const supabaseUrl = process.env.SUPABASE_URL;
const supabaseKey = process.env.SUPABASE_SERVICE_ROLE_KEY;
if (!supabaseUrl || !supabaseKey) {
  throw new Error("SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY must be set");
}
export const supabase: SupabaseClient = createClient(supabaseUrl, supabaseKey);
```

Server는 추가로 `auth: { persistSession: false, autoRefreshToken: false }` 옵션 적용 (RESEARCH §6.1).
</interfaces>
</context>

<tasks>

<task type="auto" id="02-01-01">
  <name>Task 1: server/ 워크스페이스 의존성 + 설정 파일 추가</name>
  <read_first>
    - /Users/alex/repos/gh-radar/server/package.json (현재 스텁)
    - /Users/alex/repos/gh-radar/server/tsconfig.json
    - /Users/alex/repos/gh-radar/workers/ingestion/package.json (scripts/deps 템플릿)
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-RESEARCH.md (§17 verified versions, §10.1 vitest config)
    - /Users/alex/repos/gh-radar/tsconfig.base.json
  </read_first>
  <action>
    `server/package.json`을 다음과 같이 덮어쓴다 (engines + scripts + deps 전체 포함):

    ```json
    {
      "name": "@gh-radar/server",
      "version": "0.0.0",
      "private": true,
      "type": "module",
      "engines": { "node": ">=22" },
      "scripts": {
        "dev": "tsx watch -r dotenv/config src/server.ts",
        "start": "node dist/server.js",
        "build": "tsc",
        "typecheck": "tsc --noEmit",
        "test": "vitest"
      },
      "dependencies": {
        "@gh-radar/shared": "workspace:*",
        "@supabase/supabase-js": "^2.103.0",
        "@google-cloud/pino-logging-gcp-config": "^1.3.3",
        "cors": "^2.8.6",
        "express": "^5.2.1",
        "express-rate-limit": "^8.3.2",
        "helmet": "^8.1.0",
        "pino": "^10.3.1",
        "pino-http": "^11.0.0",
        "zod": "^4.0.0"
      },
      "devDependencies": {
        "@types/cors": "^2.8.17",
        "@types/express": "^5.0.0",
        "@types/node": "^22.0.0",
        "@types/supertest": "^6.0.2",
        "dotenv": "^16.4.0",
        "supertest": "^7.2.2",
        "tsx": "^4.21.0",
        "typescript": "^5.0.0",
        "vitest": "^4.1.4"
      }
    }
    ```

    `server/tsconfig.json`을 다음으로 덮어쓴다:
    ```json
    {
      "extends": "../tsconfig.base.json",
      "compilerOptions": {
        "outDir": "./dist",
        "rootDir": "./src",
        "module": "NodeNext",
        "moduleResolution": "NodeNext",
        "types": ["node"]
      },
      "include": ["src/**/*"],
      "exclude": ["node_modules", "dist", "tests"]
    }
    ```

    `server/vitest.config.ts` 신규:
    ```ts
    import { defineConfig } from "vitest/config";
    export default defineConfig({
      test: {
        globals: false,
        environment: "node",
        include: ["tests/**/*.test.ts"],
        setupFiles: ["tests/setup.ts"],
      },
    });
    ```

    `server/tests/setup.ts` 신규 — env 격리 (테스트 시 실제 env 오염 방지):
    ```ts
    import { beforeEach } from "vitest";
    beforeEach(() => {
      process.env.SUPABASE_URL = "http://localhost:54321";
      process.env.SUPABASE_SERVICE_ROLE_KEY = "test-service-role-key";
      process.env.CORS_ALLOWED_ORIGINS = "http://localhost:3000,/^https:\\/\\/gh-radar-.*\\.vercel\\.app$/";
      process.env.NODE_ENV = "test";
    });
    ```

    루트에서 `pnpm install`로 lockfile 갱신.

    CLAUDE.md 규칙 — 단순성 우선: 이 task에서 **Express 앱을 만들지 않는다**. 스캐폴드만.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm install --frozen-lockfile=false && pnpm -F @gh-radar/server typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `server/package.json` `scripts.dev` 값이 `'tsx watch -r dotenv/config src/server.ts'` 정확히 포함
    - `server/package.json` `engines.node`가 `">=22"`
    - `server/package.json` dependencies에 `express@^5.2.1`, `helmet@^8.1.0`, `cors@^2.8.6`, `express-rate-limit@^8.3.2`, `pino-http@^11.0.0`, `@google-cloud/pino-logging-gcp-config@^1.3.3`, `zod@^4.0.0` 모두 존재
    - `server/package.json` devDependencies에 `vitest@^4.1.4`, `supertest@^7.2.2`, `@types/supertest@^6.0.2` 존재
    - `server/vitest.config.ts` 파일 존재, `include: ["tests/**/*.test.ts"]` 및 `setupFiles: ["tests/setup.ts"]` 포함
    - `server/tests/setup.ts` 파일 존재, `process.env.SUPABASE_URL` 할당 포함
    - `pnpm -F @gh-radar/server typecheck` exit 0 (아직 src가 없으면 `--noEmit` 즉시 성공)
  </acceptance_criteria>
  <done>server/ 워크스페이스가 런타임 의존성 + 테스트 런너 포함한 상태로 준비됨. 이후 wave에서 소스 추가 시 즉시 빌드 가능.</done>
</task>

<task type="auto" id="02-01-02" tdd="true">
  <name>Task 2: 공용 API 타입 + 에러 계층 + Supabase 싱글턴 + 로거</name>
  <read_first>
    - /Users/alex/repos/gh-radar/packages/shared/src/index.ts (re-export 패턴)
    - /Users/alex/repos/gh-radar/packages/shared/src/stock.ts
    - /Users/alex/repos/gh-radar/workers/ingestion/src/logger.ts (redact 패턴 승계)
    - /Users/alex/repos/gh-radar/workers/ingestion/src/services/supabase.ts (싱글턴 대칭)
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-RESEARCH.md (§5.6 ApiError, §6.1 supabase singleton, §Q4 pino GCP config)
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-CONTEXT.md (D-14, D-15, D-23, D-26)
  </read_first>
  <behavior>
    - ApiError: `new ApiError(404, 'STOCK_NOT_FOUND', 'msg')`가 `{status, code, message}` 프로퍼티 노출
    - 팩토리: `StockNotFound('005930').code === 'STOCK_NOT_FOUND'` / `.status === 404`
    - 팩토리: `InvalidQueryParam('minRate','nan').code === 'INVALID_QUERY_PARAM'` / `.status === 400`
    - Supabase 싱글턴: 환경변수 누락 시 import 시점에 throw
    - `packages/shared/src/api.ts`의 `ApiErrorBody` 타입이 `{error:{code:string,message:string}}` shape
  </behavior>
  <action>
    **파일 1: `packages/shared/src/api.ts` 신규**
    ```ts
    export type ApiErrorBody = { error: { code: string; message: string } };
    export type ApiSuccess<T> = T; // D-13: 루트에 그대로 반환 (단순성)
    ```

    **파일 2: `packages/shared/src/index.ts` 수정** — 기존 re-export 유지 + `export * from "./api";` 추가 (파일 말미).

    **파일 3: `server/src/errors.ts` 신규** (RESEARCH §5.6 verbatim):
    ```ts
    export class ApiError extends Error {
      constructor(
        public readonly status: number,
        public readonly code: string,
        message: string,
      ) {
        super(message);
        this.name = "ApiError";
      }
    }

    export const StockNotFound = (code: string) =>
      new ApiError(404, "STOCK_NOT_FOUND", `Stock ${code} not found`);
    export const InvalidQueryParam = (param: string, reason: string) =>
      new ApiError(400, "INVALID_QUERY_PARAM", `${param}: ${reason}`);
    export const ValidationFailed = (msg: string) =>
      new ApiError(400, "VALIDATION_FAILED", msg);
    export const RateLimited = () =>
      new ApiError(429, "RATE_LIMITED", "Too many requests, retry later.");
    export const NotFound = () =>
      new ApiError(404, "NOT_FOUND", "Route not found");
    export const InternalError = (msg = "Internal server error") =>
      new ApiError(500, "INTERNAL_ERROR", msg);
    ```

    **파일 4: `server/src/config.ts` 신규** — 환경변수 로더 (D-35 계약):
    ```ts
    export type AppConfig = {
      nodeEnv: "development" | "test" | "production";
      port: number;
      logLevel: string;
      appVersion: string;
      supabaseUrl: string;
      supabaseServiceRoleKey: string;
      corsAllowedOrigins: string;
    };

    export function loadConfig(): AppConfig {
      const get = (k: string): string => {
        const v = process.env[k];
        if (!v) throw new Error(`${k} must be set`);
        return v;
      };
      return {
        nodeEnv: (process.env.NODE_ENV ?? "development") as AppConfig["nodeEnv"],
        port: Number(process.env.PORT ?? 8080),
        logLevel: process.env.LOG_LEVEL ?? "info",
        appVersion: process.env.APP_VERSION ?? "dev",
        supabaseUrl: get("SUPABASE_URL"),
        supabaseServiceRoleKey: get("SUPABASE_SERVICE_ROLE_KEY"),
        corsAllowedOrigins: process.env.CORS_ALLOWED_ORIGINS ?? "",
      };
    }
    ```

    **파일 5: `server/src/services/supabase.ts` 신규** (RESEARCH §6.1):
    ```ts
    import { createClient, type SupabaseClient } from "@supabase/supabase-js";

    const url = process.env.SUPABASE_URL;
    const key = process.env.SUPABASE_SERVICE_ROLE_KEY;
    if (!url || !key) {
      throw new Error("SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY must be set");
    }

    export const supabase: SupabaseClient = createClient(url, key, {
      auth: { persistSession: false, autoRefreshToken: false },
    });

    export function createSupabase(u = url!, k = key!): SupabaseClient {
      return createClient(u, k, { auth: { persistSession: false, autoRefreshToken: false } });
    }
    ```

    **파일 6: `server/src/logger.ts` 신규** (RESEARCH §Q4 권고 B + D-26 redact):
    ```ts
    import pino from "pino";
    import { createGcpLoggingPinoConfig } from "@google-cloud/pino-logging-gcp-config";

    export const logger = pino(
      createGcpLoggingPinoConfig(
        {
          serviceContext: {
            service: "gh-radar-server",
            version: process.env.APP_VERSION ?? "dev",
          },
        },
        {
          level: process.env.LOG_LEVEL ?? "info",
          redact: {
            paths: [
              "req.headers.authorization",
              "req.headers.cookie",
              "*.supabase_service_role_key",
              "*.access_token",
              "*.refresh_token",
            ],
            censor: "[REDACTED]",
          },
        },
      ),
    );
    ```

    **파일 7: `server/src/types/express.d.ts` 신규** — request-id 타입 확장:
    ```ts
    import "express";
    declare global {
      namespace Express {
        interface Request {
          id: string;
        }
      }
    }
    export {};
    ```

    **단순성 우선 (CLAUDE.md):** ApiError는 세분화된 하위 클래스 만들지 않음. 팩토리 함수로 충분.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm -F @gh-radar/server typecheck && pnpm -F @gh-radar/shared build</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/api.ts` 존재, `export type ApiErrorBody` 포함
    - `packages/shared/src/index.ts`가 `export * from "./api"` 포함 (grep)
    - `server/src/errors.ts`가 `class ApiError extends Error` 포함
    - `server/src/errors.ts`에 `StockNotFound`, `InvalidQueryParam`, `ValidationFailed`, `RateLimited`, `NotFound`, `InternalError` 6개 export 모두 존재
    - `server/src/services/supabase.ts`에 `persistSession: false` 포함 + env 누락 시 throw 로직
    - `server/src/logger.ts`가 `createGcpLoggingPinoConfig` 사용, redact 경로 5개 포함 (`authorization`, `cookie`, `supabase_service_role_key`, `access_token`, `refresh_token`)
    - `pnpm -F @gh-radar/server typecheck` exit 0
    - `pnpm -F @gh-radar/shared build` exit 0 (ApiErrorBody가 .d.ts에 노출되는지 포함)
  </acceptance_criteria>
  <done>공용 타입/에러/설정/클라이언트/로거 확정. 후속 wave가 import해서 쓸 수 있는 상태.</done>
</task>

<task type="auto" id="02-01-03" tdd="true">
  <name>Task 3: rowToStock 매퍼 + CORS 파서 + 테스트 + 픽스처</name>
  <read_first>
    - /Users/alex/repos/gh-radar/packages/shared/src/stock.ts
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-RESEARCH.md (§6.3 매퍼, §5.3 CORS 파서, §7.3 mock 스케치)
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-CONTEXT.md (D-09 upperLimitProximity, D-18 CORS 형식)
    - /Users/alex/repos/gh-radar/supabase/migrations/20260413120000_init_tables.sql (컬럼 타입)
  </read_first>
  <behavior>
    - `rowToStock({price:"70000.00", upper_limit:"91000.00", ...})` → `{price:70000, upperLimit:91000, upperLimitProximity: 70000/91000}`
    - `rowToStock` upper_limit=0 → upperLimitProximity=0 (division by zero 회피)
    - `rowToStock` market_cap=null → marketCap=0
    - `parseAllowedOrigins("a,b,/^x$/")` → `["a", "b", RegExp("^x$")]`
    - `parseAllowedOrigins(undefined)` → `[]`
    - `parseAllowedOrigins("")` → `[]`
    - `parseAllowedOrigins("  http://localhost:3000  ,  /^https:\\/\\/gh-radar-.*\\.vercel\\.app$/  ")` 공백 trim됨
    - `corsOptions().origin(undefined, cb)` → cb(null, true) (origin 없음 허용 — 서버-서버/curl)
    - `corsOptions().origin("https://evil.com", cb)` → cb(error, false)
    - `corsOptions().origin("https://gh-radar-pr123.vercel.app", cb)` → cb(null, true) (정규식 매치)
  </behavior>
  <action>
    **파일 1: `server/src/mappers/stock.ts` 신규** (RESEARCH §6.3 verbatim, 타입 시그니처는 `Stock & { upperLimitProximity: number }`):
    ```ts
    import type { Stock, Market } from "@gh-radar/shared";

    export type StockRow = {
      code: string;
      name: string;
      market: string;
      price: string;
      change_amount: string;
      change_rate: string;
      volume: number;
      open: string | null;
      high: string | null;
      low: string | null;
      market_cap: number | null;
      upper_limit: string;
      lower_limit: string;
      updated_at: string;
    };

    export type StockWithProximity = Stock & { upperLimitProximity: number };

    export function rowToStock(r: StockRow): StockWithProximity {
      const upper = Number(r.upper_limit);
      const price = Number(r.price);
      return {
        code: r.code,
        name: r.name,
        market: r.market as Market,
        price,
        changeAmount: Number(r.change_amount),
        changeRate: Number(r.change_rate),
        volume: r.volume,
        open: Number(r.open ?? 0),
        high: Number(r.high ?? 0),
        low: Number(r.low ?? 0),
        marketCap: Number(r.market_cap ?? 0),
        upperLimit: upper,
        lowerLimit: Number(r.lower_limit),
        updatedAt: r.updated_at,
        upperLimitProximity: upper > 0 ? price / upper : 0,
      };
    }
    ```

    **파일 2: `server/src/services/cors-config.ts` 신규** (RESEARCH §5.3 verbatim):
    ```ts
    import type { CorsOptions } from "cors";

    export function parseAllowedOrigins(raw: string | undefined): Array<string | RegExp> {
      if (!raw) return [];
      return raw
        .split(",")
        .map((s) => s.trim())
        .filter(Boolean)
        .map((item) => {
          const m = item.match(/^\/(.*)\/([gimsuy]*)$/);
          return m ? new RegExp(m[1], m[2]) : item;
        });
    }

    export function corsOptions(): CorsOptions {
      const allowed = parseAllowedOrigins(process.env.CORS_ALLOWED_ORIGINS);
      return {
        origin: (origin, cb) => {
          if (!origin) return cb(null, true);
          const ok = allowed.some((rule) =>
            typeof rule === "string" ? rule === origin : rule.test(origin),
          );
          cb(ok ? null : new Error("CORS_NOT_ALLOWED"), ok);
        },
        credentials: false,
        maxAge: 600,
      };
    }
    ```

    **파일 3: `server/tests/fixtures/stocks.ts` 신규** — 005930 삼성전자 + 035720 카카오 + KOSDAQ 1종:
    ```ts
    import type { StockRow } from "../../src/mappers/stock";

    export const samsungRow: StockRow = {
      code: "005930",
      name: "삼성전자",
      market: "KOSPI",
      price: "70000.00",
      change_amount: "1000.00",
      change_rate: "1.4500",
      volume: 12345678,
      open: "69500.00",
      high: "70500.00",
      low: "69000.00",
      market_cap: 418000000000000,
      upper_limit: "91000.00",
      lower_limit: "49000.00",
      updated_at: "2026-04-13T10:00:00Z",
    };

    export const kakaoRow: StockRow = {
      ...samsungRow,
      code: "035720",
      name: "카카오",
      price: "55000.00",
      change_amount: "500.00",
      change_rate: "0.9100",
      volume: 2345678,
      upper_limit: "71500.00",
      lower_limit: "38500.00",
      market_cap: 24000000000000,
    };

    export const kosdaqRow: StockRow = {
      ...samsungRow,
      code: "091990",
      name: "셀트리온헬스케어",
      market: "KOSDAQ",
      price: "80000.00",
      change_amount: "-1000.00",
      change_rate: "-1.2300",
      volume: 987654,
      upper_limit: "104000.00",
      lower_limit: "56000.00",
      market_cap: 13000000000000,
    };

    export const allRows = [samsungRow, kakaoRow, kosdaqRow];
    ```

    **파일 4: `server/tests/fixtures/supabase-mock.ts` 신규** (RESEARCH §7.3 스케치 구현):
    ```ts
    import { vi } from "vitest";
    import type { SupabaseClient } from "@supabase/supabase-js";
    import type { StockRow } from "../../src/mappers/stock";

    type State = { stocks: StockRow[] };

    export function mockSupabase(state: State): SupabaseClient {
      const makeBuilder = () => {
        let filtered: StockRow[] = [...state.stocks];
        let orderCol: string | null = null;
        let orderAsc = true;
        let limitN: number | null = null;
        let eqCol: string | null = null;
        let eqVal: unknown = null;

        const exec = () => {
          let rows = [...filtered];
          if (eqCol) rows = rows.filter((r) => (r as any)[eqCol!] === eqVal);
          if (orderCol) {
            rows.sort((a, b) => {
              const av = Number((a as any)[orderCol!]) || 0;
              const bv = Number((b as any)[orderCol!]) || 0;
              return orderAsc ? av - bv : bv - av;
            });
          }
          if (limitN != null) rows = rows.slice(0, limitN);
          return rows;
        };

        const builder: any = {
          select: vi.fn().mockReturnThis(),
          eq: vi.fn().mockImplementation((col: string, val: unknown) => {
            eqCol = col; eqVal = val; return builder;
          }),
          or: vi.fn().mockImplementation((expr: string) => {
            // parse "name.ilike.%q%,code.ilike.%q%"
            const parts = expr.split(",");
            const patterns = parts.map((p) => {
              const m = p.match(/^(\w+)\.ilike\.%(.*)%$/);
              return m ? { col: m[1], q: m[2].toLowerCase() } : null;
            }).filter(Boolean) as { col: string; q: string }[];
            filtered = state.stocks.filter((r) =>
              patterns.some((p) => String((r as any)[p.col]).toLowerCase().includes(p.q))
            );
            return builder;
          }),
          order: vi.fn().mockImplementation((col: string, opts?: { ascending?: boolean }) => {
            orderCol = col; orderAsc = opts?.ascending ?? true; return builder;
          }),
          limit: vi.fn().mockImplementation((n: number) => {
            limitN = n; return builder;
          }),
          maybeSingle: vi.fn().mockImplementation(async () => {
            const rows = exec();
            return { data: rows[0] ?? null, error: null };
          }),
          then: (resolve: any) => resolve({ data: exec(), error: null }),
        };
        return builder;
      };

      return { from: vi.fn().mockImplementation(() => makeBuilder()) } as unknown as SupabaseClient;
    }
    ```

    **파일 5: `server/tests/mappers/stock.test.ts` 신규:**
    ```ts
    import { describe, it, expect } from "vitest";
    import { rowToStock } from "../../src/mappers/stock";
    import { samsungRow } from "../fixtures/stocks";

    describe("rowToStock", () => {
      it("converts string numerics to number", () => {
        const s = rowToStock(samsungRow);
        expect(typeof s.price).toBe("number");
        expect(s.price).toBe(70000);
        expect(s.changeRate).toBeCloseTo(1.45);
      });
      it("computes upperLimitProximity = price/upperLimit", () => {
        const s = rowToStock(samsungRow);
        expect(s.upperLimitProximity).toBeCloseTo(70000 / 91000);
      });
      it("returns 0 for upperLimitProximity when upper_limit is 0", () => {
        const s = rowToStock({ ...samsungRow, upper_limit: "0" });
        expect(s.upperLimitProximity).toBe(0);
      });
      it("handles null market_cap as 0", () => {
        const s = rowToStock({ ...samsungRow, market_cap: null });
        expect(s.marketCap).toBe(0);
      });
      it("preserves market as 'KOSPI' | 'KOSDAQ'", () => {
        expect(rowToStock(samsungRow).market).toBe("KOSPI");
      });
    });
    ```

    **파일 6: `server/tests/services/cors-config.test.ts` 신규:**
    ```ts
    import { describe, it, expect, vi } from "vitest";
    import { parseAllowedOrigins, corsOptions } from "../../src/services/cors-config";

    describe("parseAllowedOrigins", () => {
      it("returns empty array for undefined/empty", () => {
        expect(parseAllowedOrigins(undefined)).toEqual([]);
        expect(parseAllowedOrigins("")).toEqual([]);
      });
      it("parses plain URLs as strings", () => {
        const r = parseAllowedOrigins("http://localhost:3000,https://example.com");
        expect(r).toEqual(["http://localhost:3000", "https://example.com"]);
      });
      it("parses /regex/ literal to RegExp", () => {
        const r = parseAllowedOrigins("/^https:\\/\\/gh-radar-.*\\.vercel\\.app$/");
        expect(r[0]).toBeInstanceOf(RegExp);
        expect((r[0] as RegExp).test("https://gh-radar-pr123.vercel.app")).toBe(true);
      });
      it("trims whitespace", () => {
        expect(parseAllowedOrigins("  a  ,  b  ")).toEqual(["a", "b"]);
      });
    });

    describe("corsOptions().origin", () => {
      it("allows undefined origin (server-to-server)", async () => {
        process.env.CORS_ALLOWED_ORIGINS = "http://localhost:3000";
        const cb = vi.fn();
        (corsOptions().origin as any)(undefined, cb);
        expect(cb).toHaveBeenCalledWith(null, true);
      });
      it("rejects non-allowed origin", () => {
        process.env.CORS_ALLOWED_ORIGINS = "http://localhost:3000";
        const cb = vi.fn();
        (corsOptions().origin as any)("https://evil.com", cb);
        expect(cb).toHaveBeenCalledWith(expect.any(Error), false);
      });
      it("accepts regex-matched origin", () => {
        process.env.CORS_ALLOWED_ORIGINS = "/^https:\\/\\/gh-radar-.*\\.vercel\\.app$/";
        const cb = vi.fn();
        (corsOptions().origin as any)("https://gh-radar-pr123.vercel.app", cb);
        expect(cb).toHaveBeenCalledWith(null, true);
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm -F @gh-radar/server test --run</automated>
  </verify>
  <acceptance_criteria>
    - `server/src/mappers/stock.ts`가 `upperLimitProximity` 계산 포함, upper_limit=0일 때 0 반환 분기
    - `server/src/services/cors-config.ts`가 `/regex/` 리터럴 패턴 파싱 (정규식 `^\/(.*)\/([gimsuy]*)$`)
    - `server/tests/fixtures/stocks.ts`에 `samsungRow.code === "005930"` 존재
    - `server/tests/fixtures/supabase-mock.ts`가 `maybeSingle`, `or`, `eq`, `order`, `limit`, `then` 6개 메서드 지원
    - `pnpm -F @gh-radar/server test --run` exit 0
    - vitest 출력에 테스트 스위트 2개 (`rowToStock`, `parseAllowedOrigins` + `corsOptions`) 존재 및 전체 최소 10개 assertion 통과
  </acceptance_criteria>
  <done>공용 매퍼/CORS 파서가 unit test로 확정됨. Supabase mock helper가 이후 supertest 통합 테스트에서 재사용 가능.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → process | SUPABASE_SERVICE_ROLE_KEY가 Secret Manager → Cloud Run env → Node process로 전달 |
| 테스트 → 런타임 | tests/setup.ts가 fake 값 주입해 실 키 누출 방지 |

## STRIDE Threat Register

| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-02-01 | I (Info disclosure) | `server/src/logger.ts` | mitigate | redact 경로에 `*.supabase_service_role_key`, `*.access_token`, `*.refresh_token`, `req.headers.authorization`, `req.headers.cookie` 5개 모두 포함 — grep으로 검증 |
| T-02-04 | T (Tampering) | `server/src/mappers/stock.ts` | mitigate | numeric 컬럼을 `Number()` 캐스팅 — NaN 회피. upper_limit=0 분기로 division-by-zero 방지 |
| T-02-08 | I (Secret in tests) | `server/tests/setup.ts` | mitigate | 테스트 env는 `test-service-role-key` fake 문자열. 실 키 사용 금지. grep로 실키 패턴 확인 |
</threat_model>

<verification>
실행 순서:
1. `pnpm install` — lockfile 갱신 (server 의존성 추가)
2. `pnpm -F @gh-radar/shared build` — `api.ts` export 반영
3. `pnpm -F @gh-radar/server typecheck` — exit 0
4. `pnpm -F @gh-radar/server test --run` — unit 테스트 exit 0 (rowToStock 5, cors 6+ 스위트 통과)
</verification>

<success_criteria>
- [ ] `server/package.json`이 11개 runtime deps + 10개 dev deps 포함 (버전은 RESEARCH §17 verified)
- [ ] `server/tsconfig.json` `module: NodeNext`, `moduleResolution: NodeNext`
- [ ] `server/vitest.config.ts` 존재 (setupFiles 포함)
- [ ] `server/src/errors.ts` — ApiError + 6개 팩토리
- [ ] `server/src/config.ts` — loadConfig()로 env 계약 강제
- [ ] `server/src/services/supabase.ts` — 싱글턴 + `createSupabase()` 테스트용 팩토리
- [ ] `server/src/logger.ts` — pino + gcp-logging-gcp-config + redact
- [ ] `server/src/mappers/stock.ts` — rowToStock + upperLimitProximity
- [ ] `server/src/services/cors-config.ts` — parseAllowedOrigins + corsOptions
- [ ] `packages/shared/src/api.ts` — ApiErrorBody export
- [ ] `server/tests/fixtures/` 2개 + `server/tests/*.test.ts` 2개 + setup.ts
- [ ] `pnpm -F @gh-radar/server test --run` exit 0
- [ ] `pnpm -F @gh-radar/server typecheck` exit 0
</success_criteria>

<output>
완료 후: `.planning/phases/02-backend-api/02-01-SUMMARY.md` 생성 (artifact 경로, 테스트 통과 수, 남은 의존성 이슈 기록).
</output>
