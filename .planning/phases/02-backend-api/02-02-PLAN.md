---
phase: 02-backend-api
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
requirements: [INFR-03]
files_modified:
  - server/src/middleware/request-id.ts
  - server/src/middleware/rate-limit.ts
  - server/src/middleware/error-handler.ts
  - server/src/middleware/not-found.ts
  - server/src/middleware/pino-http.ts
  - server/src/app.ts
  - server/tests/middleware/request-id.test.ts
  - server/tests/middleware/rate-limit.test.ts
  - server/tests/middleware/error-handler.test.ts
  - server/tests/middleware/cors-integration.test.ts
autonomous: true

must_haves:
  truths:
    - "createApp(deps) 팩토리가 Supabase 클라이언트 주입받아 Express 인스턴스 반환 (listen 없음)"
    - "app.set('trust proxy', 1) — Cloud Run 단일 proxy 신뢰 (RESEARCH Pitfall 1)"
    - "request-id 미들웨어가 X-Request-Id 헤더 echo/생성 (D-22)"
    - "rate limit 200/분/IP — 201번째 요청 429 + RATE_LIMITED (D-20)"
    - "CORS 허용 origin 200/204, 비허용 origin은 ACAO 헤더 부재"
    - "에러 미들웨어가 ApiError → {error:{code,message}}, 500은 production에서 stack 미노출 (D-15)"
    - "404 fallback이 {error:{code:'NOT_FOUND',message}} 반환"
    - "pino-http가 request_id/route/method/status/latency_ms 바인딩 (D-25)"
  artifacts:
    - path: "server/src/app.ts"
      provides: "createApp(deps) 팩토리 + 미들웨어 조립"
      contains: "export function createApp"
    - path: "server/src/middleware/request-id.ts"
      provides: "X-Request-Id 발급/echo"
      contains: "randomUUID"
    - path: "server/src/middleware/rate-limit.ts"
      provides: "per-IP 200/60s"
      contains: "limit: 200"
    - path: "server/src/middleware/error-handler.ts"
      provides: "ApiError → JSON, stack redact"
      contains: "instanceof ApiError"
    - path: "server/src/middleware/not-found.ts"
      provides: "404 fallback"
      contains: "NOT_FOUND"
  key_links:
    - from: "server/src/app.ts"
      to: "server/src/middleware/*"
      via: "app.use 등록 순서"
      pattern: "trust proxy.*request-id.*pino.*helmet.*cors.*rate"
    - from: "server/src/middleware/rate-limit.ts"
      to: "ipKeyGenerator from express-rate-limit"
      via: "import named export"
      pattern: "ipKeyGenerator"
---

<objective>
Phase 2 Wave 2 — Express app 팩토리(`createApp`)와 미들웨어 스택 전체 구현. 라우터는 비워둔 채 미들웨어/에러/404만 결선하여 supertest로 각 동작 검증.

Purpose: 라우터(Wave 3)가 "순수 비즈니스 로직"에만 집중할 수 있도록 보안/로깅/에러 경계 선제 확립.
Output: `createApp({supabase})` 호출 시 Express 앱 반환, supertest로 미들웨어 동작 전체 unit 통과.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-backend-api/02-CONTEXT.md
@.planning/phases/02-backend-api/02-RESEARCH.md
@.planning/phases/02-backend-api/02-VALIDATION.md
@.planning/phases/02-backend-api/02-01-PLAN.md

<interfaces>
Wave 1이 제공한 것 (import해서 사용):
```ts
// server/src/services/supabase.ts
export const supabase: SupabaseClient;
export function createSupabase(u?, k?): SupabaseClient;

// server/src/services/cors-config.ts
export function corsOptions(): CorsOptions;
export function parseAllowedOrigins(raw): Array<string | RegExp>;

// server/src/errors.ts
export class ApiError extends Error { status; code; message }
export const RateLimited, NotFound, InternalError, ...;

// server/src/logger.ts
export const logger: pino.Logger;

// server/src/config.ts
export function loadConfig(): AppConfig;

// server/src/types/express.d.ts
declare global { namespace Express { interface Request { id: string } } }
```

**Middleware 등록 순서 (RESEARCH §5.1 verbatim):**
1. `app.set('trust proxy', 1)`
2. requestId()
3. pinoHttp
4. helmet()
5. cors(corsOptions())
6. express.json({ limit: '16kb' })
7. `app.use('/api', apiRateLimiter())`
8. (라우터는 Wave 3에서 추가)
9. notFound (Wave 2에서 추가)
10. errorHandler (Wave 2에서 추가)
</interfaces>
</context>

<tasks>

<task type="auto" id="02-02-01" tdd="true">
  <name>Task 1: request-id + rate-limit + pino-http 미들웨어</name>
  <read_first>
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-RESEARCH.md (§5.2 request-id, §5.4 rate-limit, §Q3 pino customLogLevel, Pitfall 1 trust proxy)
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-CONTEXT.md (D-20, D-22, D-25)
    - /Users/alex/repos/gh-radar/server/src/types/express.d.ts (Wave 1 생성된 req.id 타입)
    - /Users/alex/repos/gh-radar/server/src/logger.ts
  </read_first>
  <behavior>
    - request-id: X-Request-Id 헤더 있고 `/^[A-Za-z0-9_-]{1,128}$/` 매칭 → 그대로 사용; 없거나 invalid → UUID v4 생성
    - request-id: `req.id` 할당 + 응답 `X-Request-Id` 헤더 echo
    - rate-limit: windowMs=60000, limit=200, standardHeaders=draft-7, 429시 `{error:{code:"RATE_LIMITED", message:"Too many requests, retry later."}}`
    - rate-limit: `ipKeyGenerator(req.ip, 64)` 키 생성
    - pino-http: customLogLevel — 5xx=error, 4xx=warn, 3xx=info, 2xx=debug
    - pino-http: genReqId는 `req.id` 재사용 (request-id 미들웨어 이후 등록)
  </behavior>
  <action>
    **파일 1: `server/src/middleware/request-id.ts` 신규** (RESEARCH §5.2 verbatim):
    ```ts
    import { randomUUID } from "node:crypto";
    import type { RequestHandler } from "express";

    const VALID = /^[A-Za-z0-9_-]{1,128}$/;

    export function requestId(): RequestHandler {
      return (req, res, next) => {
        const incoming = req.header("x-request-id");
        const id = incoming && VALID.test(incoming) ? incoming : randomUUID();
        req.id = id;
        res.setHeader("X-Request-Id", id);
        next();
      };
    }
    ```

    **파일 2: `server/src/middleware/rate-limit.ts` 신규** (RESEARCH §5.4 verbatim):
    ```ts
    import rateLimit, { ipKeyGenerator } from "express-rate-limit";
    import type { RequestHandler } from "express";

    export function apiRateLimiter(): RequestHandler {
      return rateLimit({
        windowMs: 60_000,
        limit: 200,
        standardHeaders: "draft-7",
        legacyHeaders: false,
        keyGenerator: (req) => ipKeyGenerator(req.ip ?? "", 64),
        handler: (_req, res) => {
          res.status(429).json({
            error: { code: "RATE_LIMITED", message: "Too many requests, retry later." },
          });
        },
      });
    }
    ```

    **파일 3: `server/src/middleware/pino-http.ts` 신규** (RESEARCH §Q3 권고 B):
    ```ts
    import pinoHttp from "pino-http";
    import type { RequestHandler } from "express";
    import { logger } from "../logger";

    export function httpLogger(): RequestHandler {
      return pinoHttp({
        logger,
        genReqId: (req) => (req as any).id,
        customLogLevel: (_req, res, err) => {
          if (err || res.statusCode >= 500) return "error";
          if (res.statusCode >= 400) return "warn";
          if (res.statusCode >= 300) return "info";
          return "debug";
        },
        customProps: (req, res) => ({
          request_id: (req as any).id,
          route: (req as any).route?.path ?? req.url,
          latency_ms: (res as any).responseTime,
        }),
      }) as unknown as RequestHandler;
    }
    ```

    **파일 4: `server/tests/middleware/request-id.test.ts` 신규:**
    ```ts
    import { describe, it, expect } from "vitest";
    import express from "express";
    import request from "supertest";
    import { requestId } from "../../src/middleware/request-id";

    function app() {
      const a = express();
      a.use(requestId());
      a.get("/x", (req, res) => res.json({ id: (req as any).id }));
      return a;
    }

    describe("requestId middleware", () => {
      it("generates UUID when header missing", async () => {
        const res = await request(app()).get("/x");
        expect(res.status).toBe(200);
        expect(res.headers["x-request-id"]).toMatch(/^[0-9a-f-]{36}$/);
        expect(res.body.id).toBe(res.headers["x-request-id"]);
      });
      it("echoes valid incoming X-Request-Id", async () => {
        const res = await request(app()).get("/x").set("X-Request-Id", "abc_123-XYZ");
        expect(res.headers["x-request-id"]).toBe("abc_123-XYZ");
      });
      it("rejects invalid id (spaces) and generates new UUID", async () => {
        const res = await request(app()).get("/x").set("X-Request-Id", "has spaces");
        expect(res.headers["x-request-id"]).toMatch(/^[0-9a-f-]{36}$/);
      });
    });
    ```

    **파일 5: `server/tests/middleware/rate-limit.test.ts` 신규** — 201개 루프는 무겁기 때문에 **작은 limit**으로 오버라이드한 별도 factory 검증으로 대체하지 않고, RESEARCH §5.4 구현을 그대로 쓰되 테스트용으로 limit=5를 통과하는 파일을 만든다:
    ```ts
    import { describe, it, expect } from "vitest";
    import express from "express";
    import request from "supertest";
    import rateLimit, { ipKeyGenerator } from "express-rate-limit";

    // 테스트 전용 작은 limit rate limiter — 프로덕션 limit 200과 동일 옵션 세팅 검증
    function testLimiter(limit: number) {
      return rateLimit({
        windowMs: 60_000,
        limit,
        standardHeaders: "draft-7",
        legacyHeaders: false,
        keyGenerator: (req) => ipKeyGenerator(req.ip ?? "", 64),
        handler: (_req, res) => {
          res.status(429).json({
            error: { code: "RATE_LIMITED", message: "Too many requests, retry later." },
          });
        },
      });
    }

    function app(limit = 3) {
      const a = express();
      a.set("trust proxy", 1);
      a.use("/api", testLimiter(limit));
      a.get("/api/ping", (_req, res) => res.json({ ok: true }));
      return a;
    }

    describe("apiRateLimiter (contract validated with small limit)", () => {
      it("(limit+1)번째 요청은 429 + RATE_LIMITED", async () => {
        const a = app(3);
        const agent = request(a);
        for (let i = 0; i < 3; i++) {
          const r = await agent.get("/api/ping");
          expect(r.status).toBe(200);
        }
        const over = await agent.get("/api/ping");
        expect(over.status).toBe(429);
        expect(over.body.error.code).toBe("RATE_LIMITED");
      });
      it("응답에 draft-7 표준 헤더 RateLimit-* 포함", async () => {
        const r = await request(app()).get("/api/ping");
        const keys = Object.keys(r.headers).map((k) => k.toLowerCase());
        expect(keys.some((k) => k.startsWith("ratelimit"))).toBe(true);
      });
    });
    ```
    **참고:** 실제 PROD limit=200은 INV-8 smoke 테스트(Plan 05)에서 curl 루프로 검증.

    **단순성 우선:** pino-http customProps에서 body는 절대 로깅 안 함. redact는 Wave 1 logger에서 이미 처리됨.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/server && pnpm test --run tests/middleware/request-id.test.ts tests/middleware/rate-limit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `server/src/middleware/request-id.ts`가 정규식 `/^[A-Za-z0-9_-]{1,128}$/` 포함
    - `server/src/middleware/rate-limit.ts`가 `limit: 200` 하드코딩 포함 (grep으로 확인)
    - `server/src/middleware/rate-limit.ts`가 `ipKeyGenerator` named import 포함
    - `server/src/middleware/pino-http.ts`가 `customLogLevel` 함수 포함 (5xx=error, 4xx=warn, 2xx=debug)
    - `vitest run tests/middleware/request-id.test.ts tests/middleware/rate-limit.test.ts` exit 0
    - request-id 테스트 3 케이스 모두 통과
    - rate-limit 테스트 2 케이스 모두 통과 (limit+1은 429, standard 헤더 존재)
  </acceptance_criteria>
  <done>3개 미들웨어 unit 확정. createApp에서 `app.use()`로 조립만 남음.</done>
</task>

<task type="auto" id="02-02-02" tdd="true">
  <name>Task 2: error-handler + 404 fallback 미들웨어</name>
  <read_first>
    - /Users/alex/repos/gh-radar/server/src/errors.ts (Wave 1)
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-RESEARCH.md (§5.6 errorHandler, §4 404 fallback 패턴)
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-CONTEXT.md (D-14, D-15, D-16)
  </read_first>
  <behavior>
    - ApiError(404, "STOCK_NOT_FOUND", "msg") throw → 404 + `{error:{code:"STOCK_NOT_FOUND", message:"msg"}}`
    - `Error("CORS_NOT_ALLOWED")` → 403 + `{error:{code:"CORS_NOT_ALLOWED", message:"Origin not allowed"}}`
    - 일반 Error throw → 500 + NODE_ENV=production일 때 `message:"Internal server error"` (실 메시지 은닉, stack 미노출)
    - 일반 Error throw → NODE_ENV=test/development일 때 실 `message` 노출
    - 응답 body에 `.stack` 필드 절대 없음
    - 404 fallback: 등록되지 않은 경로 → `{error:{code:"NOT_FOUND", message:"Route not found"}}`
  </behavior>
  <action>
    **파일 1: `server/src/middleware/error-handler.ts` 신규** (RESEARCH §5.6 verbatim):
    ```ts
    import type { ErrorRequestHandler } from "express";
    import { ApiError } from "../errors";

    export const errorHandler: ErrorRequestHandler = (err, req, res, _next) => {
      if (err instanceof ApiError) {
        (req as any).log?.warn({ err: { code: err.code, message: err.message }, code: err.code }, "api error");
        res.status(err.status).json({
          error: { code: err.code, message: err.message },
        });
        return;
      }
      if (err?.message === "CORS_NOT_ALLOWED") {
        res.status(403).json({
          error: { code: "CORS_NOT_ALLOWED", message: "Origin not allowed" },
        });
        return;
      }
      (req as any).log?.error({ err }, "unhandled error");
      const isProd = process.env.NODE_ENV === "production";
      res.status(500).json({
        error: {
          code: "INTERNAL_ERROR",
          message: isProd ? "Internal server error" : err?.message ?? "unknown",
        },
      });
    };
    ```

    **파일 2: `server/src/middleware/not-found.ts` 신규** (RESEARCH §4 — Express 5 권장 `app.use(handler)`):
    ```ts
    import type { RequestHandler } from "express";

    export const notFoundHandler: RequestHandler = (_req, res) => {
      res.status(404).json({
        error: { code: "NOT_FOUND", message: "Route not found" },
      });
    };
    ```

    **파일 3: `server/tests/middleware/error-handler.test.ts` 신규:**
    ```ts
    import { describe, it, expect, beforeEach, afterEach } from "vitest";
    import express from "express";
    import request from "supertest";
    import { errorHandler } from "../../src/middleware/error-handler";
    import { notFoundHandler } from "../../src/middleware/not-found";
    import { ApiError, StockNotFound } from "../../src/errors";

    function app(env: string) {
      process.env.NODE_ENV = env;
      const a = express();
      a.get("/api-error", (_req, _res, next) => next(StockNotFound("000000")));
      a.get("/cors-error", (_req, _res, next) => next(new Error("CORS_NOT_ALLOWED")));
      a.get("/boom", (_req, _res, next) => next(new Error("sensitive internal detail")));
      a.use(notFoundHandler);
      a.use(errorHandler);
      return a;
    }

    describe("errorHandler", () => {
      afterEach(() => { process.env.NODE_ENV = "test"; });

      it("ApiError → status + {error:{code,message}}", async () => {
        const r = await request(app("test")).get("/api-error");
        expect(r.status).toBe(404);
        expect(r.body).toEqual({ error: { code: "STOCK_NOT_FOUND", message: "Stock 000000 not found" } });
        expect(r.body.stack).toBeUndefined();
      });

      it("CORS_NOT_ALLOWED → 403", async () => {
        const r = await request(app("test")).get("/cors-error");
        expect(r.status).toBe(403);
        expect(r.body.error.code).toBe("CORS_NOT_ALLOWED");
      });

      it("production에서 일반 Error → 500 + 'Internal server error' (실 메시지 은닉)", async () => {
        const r = await request(app("production")).get("/boom");
        expect(r.status).toBe(500);
        expect(r.body.error.code).toBe("INTERNAL_ERROR");
        expect(r.body.error.message).toBe("Internal server error");
        expect(JSON.stringify(r.body)).not.toContain("sensitive internal detail");
        expect(r.body.error.stack).toBeUndefined();
      });

      it("development에서는 실 메시지 노출", async () => {
        const r = await request(app("development")).get("/boom");
        expect(r.body.error.message).toBe("sensitive internal detail");
      });
    });

    describe("notFoundHandler", () => {
      it("등록되지 않은 라우트 → 404 NOT_FOUND", async () => {
        const r = await request(app("test")).get("/nope");
        expect(r.status).toBe(404);
        expect(r.body.error.code).toBe("NOT_FOUND");
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/server && pnpm test --run tests/middleware/error-handler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `server/src/middleware/error-handler.ts`가 `err instanceof ApiError` 분기 + `CORS_NOT_ALLOWED` 분기 + production isProd 분기 모두 포함
    - `server/src/middleware/not-found.ts`가 `status(404)` + `"NOT_FOUND"` + `"Route not found"` 포함
    - error-handler 응답 body에 `stack` 필드 부재 (테스트로 검증)
    - `vitest run tests/middleware/error-handler.test.ts` exit 0 (5 assertions 이상)
    - production 모드에서 `"sensitive internal detail"` 문자열이 응답 JSON에 **없음**
  </acceptance_criteria>
  <done>에러/404 미들웨어 확정. Wave 3 라우터가 `throw StockNotFound(code)` 또는 `next(err)`만으로 정형 응답 얻음.</done>
</task>

<task type="auto" id="02-02-03" tdd="true">
  <name>Task 3: createApp() 팩토리 — 미들웨어 조립 + CORS 통합 테스트</name>
  <read_first>
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-RESEARCH.md (§5.1 등록 순서, §7.2 createApp DI 패턴)
    - /Users/alex/repos/gh-radar/server/src/middleware/ (이 Plan 이전 task 산출물)
    - /Users/alex/repos/gh-radar/server/src/services/cors-config.ts (Wave 1)
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-CONTEXT.md (D-17~D-22)
  </read_first>
  <behavior>
    - `createApp({ supabase })` 반환값은 Express Application (listen 없음)
    - `app.locals.supabase === depsSupabase` (라우터가 `req.app.locals.supabase`로 접근)
    - CORS 허용 origin(`http://localhost:3000`) preflight → 200/204 + `Access-Control-Allow-Origin` 헤더 `http://localhost:3000`
    - CORS 비허용 origin(`https://evil.example.com`) preflight → ACAO 헤더 부재 (403 또는 헤더 없음)
    - JSON body 16kb 초과 → 413 또는 에러 미들웨어 경유 응답
    - `trust proxy` === 1 (app.get('trust proxy') 값 확인)
    - 모든 응답에 X-Request-Id 헤더 존재
    - `/api/*` 경로에 rate-limiter 적용됨 (어떤 라우트 붙이지 않아도 404 응답이 rate-limit 통과 후임을 확인 불필요 — app.use('/api', rate-limit) 등록 자체 확인은 라우터 wave에서)
  </behavior>
  <action>
    **파일 1: `server/src/app.ts` 신규** (RESEARCH §5.1 + §7.2 verbatim):
    ```ts
    import express, { type Express } from "express";
    import helmet from "helmet";
    import cors from "cors";
    import type { SupabaseClient } from "@supabase/supabase-js";
    import { requestId } from "./middleware/request-id";
    import { apiRateLimiter } from "./middleware/rate-limit";
    import { httpLogger } from "./middleware/pino-http";
    import { errorHandler } from "./middleware/error-handler";
    import { notFoundHandler } from "./middleware/not-found";
    import { corsOptions } from "./services/cors-config";

    export type AppDeps = { supabase: SupabaseClient };

    export function createApp(deps: AppDeps): Express {
      const app = express();

      // 1) Cloud Run: 단일 proxy 신뢰 (RESEARCH Pitfall 1)
      app.set("trust proxy", 1);

      // deps 주입 (라우터가 req.app.locals.supabase 로 접근)
      app.locals.supabase = deps.supabase;

      // 2) request-id (pino 바인딩 위해 가장 먼저)
      app.use(requestId());

      // 3) pino-http
      app.use(httpLogger());

      // 4) helmet (보안 헤더)
      app.use(helmet());

      // 5) CORS
      app.use(cors(corsOptions()));

      // 6) body parser (16kb)
      app.use(express.json({ limit: "16kb" }));

      // 7) rate-limit on /api
      app.use("/api", apiRateLimiter());

      // 8) 라우터는 Wave 3에서 등록. 자리 표시용 빈 블록 — 수정 시 이 주석 유지.
      //    app.use("/api/scanner", scannerRouter);
      //    app.use("/api/stocks", stocksRouter);
      //    app.use("/api/health", healthRouter);

      // 9) 404 fallback
      app.use(notFoundHandler);

      // 10) error handler (마지막)
      app.use(errorHandler);

      return app;
    }
    ```

    **파일 2: `server/tests/middleware/cors-integration.test.ts` 신규:**
    ```ts
    import { describe, it, expect, beforeEach } from "vitest";
    import request from "supertest";
    import { createApp } from "../../src/app";
    import { mockSupabase } from "../fixtures/supabase-mock";

    describe("createApp — middleware stack integration", () => {
      beforeEach(() => {
        process.env.CORS_ALLOWED_ORIGINS =
          "http://localhost:3000,/^https:\\/\\/gh-radar-.*\\.vercel\\.app$/";
      });

      it("trust proxy === 1", () => {
        const app = createApp({ supabase: mockSupabase({ stocks: [] }) });
        expect(app.get("trust proxy")).toBe(1);
      });

      it("모든 응답에 X-Request-Id 헤더 존재", async () => {
        const app = createApp({ supabase: mockSupabase({ stocks: [] }) });
        const r = await request(app).get("/some-route-that-does-not-exist");
        expect(r.headers["x-request-id"]).toMatch(/^[A-Za-z0-9_-]{1,128}$/);
      });

      it("404 fallback이 동작 — /nope → NOT_FOUND", async () => {
        const app = createApp({ supabase: mockSupabase({ stocks: [] }) });
        const r = await request(app).get("/nope");
        expect(r.status).toBe(404);
        expect(r.body.error.code).toBe("NOT_FOUND");
      });

      it("CORS preflight 허용 origin → 2xx + ACAO 헤더 echo", async () => {
        const app = createApp({ supabase: mockSupabase({ stocks: [] }) });
        const r = await request(app)
          .options("/nope")
          .set("Origin", "http://localhost:3000")
          .set("Access-Control-Request-Method", "GET");
        expect([200, 204]).toContain(r.status);
        expect(r.headers["access-control-allow-origin"]).toBe("http://localhost:3000");
      });

      it("CORS preflight Vercel preview(regex) → 허용", async () => {
        const app = createApp({ supabase: mockSupabase({ stocks: [] }) });
        const r = await request(app)
          .options("/nope")
          .set("Origin", "https://gh-radar-pr42.vercel.app")
          .set("Access-Control-Request-Method", "GET");
        expect([200, 204]).toContain(r.status);
        expect(r.headers["access-control-allow-origin"]).toBe("https://gh-radar-pr42.vercel.app");
      });

      it("CORS 비허용 origin → ACAO 헤더 부재", async () => {
        const app = createApp({ supabase: mockSupabase({ stocks: [] }) });
        const r = await request(app)
          .options("/nope")
          .set("Origin", "https://evil.example.com")
          .set("Access-Control-Request-Method", "GET");
        expect(r.headers["access-control-allow-origin"]).toBeUndefined();
      });

      it("16kb 초과 JSON body → 에러 (POST 없이도 express.json limit 적용 검증)", async () => {
        const app = createApp({ supabase: mockSupabase({ stocks: [] }) });
        app.post("/echo", (req, res) => res.json(req.body));
        const huge = "x".repeat(17 * 1024);
        const r = await request(app).post("/echo").set("Content-Type", "application/json").send({ a: huge });
        expect(r.status).toBeGreaterThanOrEqual(400);
      });

      it("app.locals.supabase === deps.supabase (DI)", () => {
        const supa = mockSupabase({ stocks: [] });
        const app = createApp({ supabase: supa });
        expect(app.locals.supabase).toBe(supa);
      });

      it("helmet 기본 보안 헤더 존재 (X-DNS-Prefetch-Control)", async () => {
        const app = createApp({ supabase: mockSupabase({ stocks: [] }) });
        const r = await request(app).get("/nope");
        expect(r.headers["x-dns-prefetch-control"]).toBeDefined();
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/server && pnpm test --run tests/middleware/cors-integration.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `server/src/app.ts`가 `app.set("trust proxy", 1)` 포함
    - `server/src/app.ts`에서 미들웨어 `app.use` 호출 순서: requestId → httpLogger → helmet → cors → express.json → apiRateLimiter (다른 순서 금지)
    - `server/src/app.ts`가 `app.locals.supabase = deps.supabase` 포함
    - `server/src/app.ts`에 `app.use(notFoundHandler)` 다음 줄이 `app.use(errorHandler)` (마지막)
    - `vitest run tests/middleware/cors-integration.test.ts` exit 0 — 9 테스트 케이스 모두 통과
    - `pnpm -F @gh-radar/server typecheck` exit 0
  </acceptance_criteria>
  <done>Express 앱 팩토리 완성. Wave 3 라우터는 `createApp` 내부 주석 자리에 `app.use()` 라인만 추가하면 됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → Express | origin 기반 CORS 검증; preflight에서 거부 |
| Express → pino/로그 | redact paths가 민감 필드 차단 |
| 클라이언트 → rate-limit | IP 기반 200/min; X-Forwarded-For를 trust proxy=1로 신뢰 |

## STRIDE Threat Register

| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-02-02 | S (Spoof) / I | cors 미들웨어 | mitigate | `CORS_ALLOWED_ORIGINS` 화이트리스트 + 정규식 파싱 (D-18). 비허용 origin은 ACAO 헤더 부재로 브라우저 차단. cors-integration.test.ts "CORS 비허용 origin → ACAO 헤더 부재"로 검증 |
| T-02-03 | D (DoS) | rate-limit | mitigate | `app.set('trust proxy', 1)` 강제 (cors-integration.test.ts가 `app.get('trust proxy')===1` 확인). `ipKeyGenerator(req.ip, 64)` IPv6 subnet 키. Pitfall 1 예방. limit=200/60s 검증은 Plan 05 INV-8 smoke |
| T-02-06 | I (Stack trace leak) | errorHandler | mitigate | NODE_ENV=production일 때 err.message 은닉. error-handler.test.ts "production 일반 Error → Internal server error" 검증 + `body.stack === undefined` assert |
| T-02-09 | D (body flood) | express.json | mitigate | `limit: '16kb'`. cors-integration.test.ts 17KB body → 4xx 검증 |
| T-02-10 | T (Header injection via X-Request-Id) | request-id | mitigate | 정규식 `/^[A-Za-z0-9_-]{1,128}$/` 매칭 실패 시 UUID 재생성. request-id.test.ts "has spaces" 케이스 검증 |
</threat_model>

<verification>
1. `pnpm -F @gh-radar/server test --run` — 모든 middleware unit + integration 통과
2. `pnpm -F @gh-radar/server typecheck` — exit 0
3. grep `app.set.*trust proxy.*1` server/src/app.ts → 존재 확인
4. grep `supabase_service_role_key` server/src/logger.ts (Wave 1) → redact 확인
</verification>

<success_criteria>
- [ ] `server/src/app.ts` — createApp(deps) 팩토리 (Wave 3 라우터 등록 공간 주석 포함)
- [ ] 미들웨어 5개 파일 (request-id, rate-limit, pino-http, error-handler, not-found) 존재
- [ ] `vitest run tests/middleware/**` 10+ 테스트 케이스 통과
- [ ] trust proxy === 1 검증됨
- [ ] CORS 허용/비허용/정규식 3 케이스 모두 검증됨
- [ ] production 에러 메시지 은닉 검증됨
- [ ] `pnpm typecheck` exit 0
</success_criteria>

<output>
완료 후: `.planning/phases/02-backend-api/02-02-SUMMARY.md` 생성 (미들웨어 목록, 테스트 수, CORS 검증 결과 기록).
</output>
