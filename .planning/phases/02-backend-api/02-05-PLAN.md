---
phase: 02-backend-api
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
requirements: [INFR-03]
files_modified:
  - .planning/phases/02-backend-api/02-DEPLOY-LOG.md
autonomous: false

user_setup:
  - service: gcp
    why: "Cloud Run + Artifact Registry + Secret Manager 실배포 — 사전 계정/프로젝트/시크릿/IAM 바인딩 필요"
    env_vars:
      - name: GCP_PROJECT_ID
        source: "gh-radar 전용 GCP 프로젝트 ID (weekly-wine-bot 아님)"
      - name: SUPABASE_URL
        source: "Phase 1에서 사용한 Supabase 프로젝트 URL"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Settings → API → service_role (주의: anon 아님)"
      - name: CORS_ALLOWED_ORIGINS
        source: "쉼표 구분: 'http://localhost:3000,/^https:\\\\/\\\\/gh-radar-.*\\\\.vercel\\\\.app$/'"
    dashboard_config:
      - task: "gcloud configuration 'gh-radar' 생성 + activate + project set + run/artifacts region=asia-northeast3"
        location: "로컬 터미널 (RESEARCH §3.1 명령 verbatim)"
      - task: "gcloud services enable run.googleapis.com artifactregistry.googleapis.com secretmanager.googleapis.com"
        location: "로컬 터미널"
      - task: "Artifact Registry 저장소 'gh-radar' 생성 (asia-northeast3)"
        location: "로컬 터미널 (RESEARCH §3.1)"
      - task: "gcloud auth configure-docker asia-northeast3-docker.pkg.dev"
        location: "로컬 터미널"
      - task: "Secret Manager 시크릿 'gh-radar-supabase-service-role' 생성 + Cloud Run runtime SA에 secretAccessor 바인딩"
        location: "로컬 터미널 (RESEARCH §3.2 verbatim)"

must_haves:
  truths:
    - "Cloud Run 서비스 'gh-radar-server'가 asia-northeast3에 배포되어 공개 URL로 접근 가능 (SC#1)"
    - "minScale == 1 annotation 확인 (SC#2, INV-10)"
    - "INV-1 ~ INV-9 9개 invariant 모두 smoke 통과"
    - "INV-11: Cloud Logging에 request_id/status/latency_ms 필드 포함 로그 존재"
    - "배포 로그에 service_role 키 문자열 0건 (T-02-01 검증)"
    - "min=1 효과 검증: 5분 대기 후 `time curl $URL/api/health` 3회 모두 <500ms (cold start 없음)"
  artifacts:
    - path: ".planning/phases/02-backend-api/02-DEPLOY-LOG.md"
      provides: "배포 실행 기록 (URL, SHA, smoke 결과, min=1 검증 스크린샷)"
  key_links:
    - from: "Cloud Run 서비스"
      to: "Secret Manager (gh-radar-supabase-service-role)"
      via: "--update-secrets IAM 바인딩"
      pattern: "secretAccessor"
    - from: "Cloud Run minScale"
      to: "SC#2 (cold start 없음)"
      via: "gcloud run services describe annotation"
      pattern: "autoscaling.knative.dev/minScale"
---

<objective>
Phase 2 Wave 5 — **실 Cloud Run 배포** + INV-1~INV-11 전체 검증 + 배포 로그 기록.

Purpose: ROADMAP Phase 2 Success Criteria 4개 모두 TRUE 증명 + Phase 2 완료 게이트.
Output: 공개 URL, smoke 9개 PASS, min=1 확인, Cloud Logging 샘플, `.planning/phases/02-backend-api/02-DEPLOY-LOG.md` 기록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-backend-api/02-CONTEXT.md
@.planning/phases/02-backend-api/02-RESEARCH.md
@.planning/phases/02-backend-api/02-VALIDATION.md
@scripts/deploy-server.sh
@scripts/smoke-server.sh

<interfaces>
**배포 스크립트 입력 (export 필요):**
- `GCP_PROJECT_ID` — gh-radar 전용 프로젝트 ID
- `SUPABASE_URL` — Phase 1과 동일
- `CORS_ALLOWED_ORIGINS` — `http://localhost:3000,/^https:\/\/gh-radar-.*\.vercel\.app$/`

**검증 명령 (RESEARCH §3.4):**
```bash
gcloud run services describe gh-radar-server \
  --region=asia-northeast3 \
  --format='value(spec.template.metadata.annotations.autoscaling\.knative\.dev/minScale)'
# 출력이 "1"이면 INV-10 PASS
```

**Cloud Logging 확인 (INV-11):**
```bash
gcloud logging read 'resource.type="cloud_run_revision" AND resource.labels.service_name="gh-radar-server"' \
  --limit=5 --format=json | jq -e '.[0] | (.jsonPayload.request_id and .jsonPayload.status and .jsonPayload.latency_ms) or (.httpRequest.requestUrl and .httpRequest.status)'
```
pino-logging-gcp-config는 `httpRequest`로도 매핑 — 둘 중 하나 조건 만족이면 OK.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" id="02-05-01" gate="blocking">
  <name>Task 1 [체크포인트]: GCP 선행 조건 완료 확인 (D-39)</name>
  <action>체크포인트 — 사용자가 `<how-to-verify>`의 gcloud 7단계를 직접 실행한 뒤 승인 대기.</action>
  <verify>체크포인트 — `gcloud config list`/`artifacts repositories describe`/`secrets describe`/`secrets get-iam-policy` 4개 모두 통과.</verify>
  <done>사용자 "approved" + `GCP_PROJECT_ID` 공유 시 Task 2 실배포 진행.</done>
  <what-built>
    Wave 4에서 배포 스크립트와 Dockerfile은 작성되었으나, GCP 계정/프로젝트/Secret Manager/Artifact Registry/IAM은 Claude가 프로비저닝할 수 없음 (사용자 계정 인증 필요).
  </what-built>
  <how-to-verify>
    **사용자가 로컬 터미널에서 직접 실행 (RESEARCH §3.1, §3.2 verbatim):**

    ```bash
    # 1) gcloud configuration 분리
    gcloud config configurations create gh-radar 2>/dev/null || true
    gcloud config configurations activate gh-radar
    gcloud auth login
    gcloud config set account <your-gh-radar-email>
    export GCP_PROJECT_ID=<your-gh-radar-project-id>
    gcloud config set project $GCP_PROJECT_ID
    gcloud config set run/region asia-northeast3
    gcloud config set artifacts/location asia-northeast3

    # 2) API 활성화
    gcloud services enable \
      run.googleapis.com \
      artifactregistry.googleapis.com \
      secretmanager.googleapis.com

    # 3) Artifact Registry 저장소 생성
    gcloud artifacts repositories create gh-radar \
      --repository-format=docker \
      --location=asia-northeast3 \
      --description="gh-radar container images"

    # 4) Docker credHelper 등록
    gcloud auth configure-docker asia-northeast3-docker.pkg.dev

    # 5) Secret Manager 시크릿 생성
    echo -n "$SUPABASE_SERVICE_ROLE_KEY" | gcloud secrets create gh-radar-supabase-service-role \
      --data-file=- \
      --replication-policy=automatic

    # 6) Cloud Run runtime SA에 secretAccessor 권한 부여
    PROJECT_NUMBER=$(gcloud projects describe $GCP_PROJECT_ID --format='value(projectNumber)')
    gcloud secrets add-iam-policy-binding gh-radar-supabase-service-role \
      --member="serviceAccount:${PROJECT_NUMBER}-compute@developer.gserviceaccount.com" \
      --role="roles/secretmanager.secretAccessor"

    # 7) 검증
    gcloud config list                                                      # config=gh-radar, project=<expected>
    gcloud artifacts repositories describe gh-radar --location=asia-northeast3  # 존재 확인
    gcloud secrets describe gh-radar-supabase-service-role                  # 존재 확인
    gcloud secrets get-iam-policy gh-radar-supabase-service-role             # secretAccessor 바인딩 확인
    ```

    **사용자 확인 포인트:**
    - [ ] 1~6번 모두 성공
    - [ ] 7번 검증 4개 모두 통과
    - [ ] `gcloud config configurations list`에서 IS_ACTIVE=True인 row가 `gh-radar`

    **한국 시각 주의:** 장 마감 후(15:30 KST 이후) 또는 주말이어도 배포는 가능. 다만 INV-2 `/api/scanner` 응답은 Phase 1 ingestion이 해당 시점에 실행되었는지에 따라 stocks 테이블 내용이 달라짐. 데이터 부재 시 INV-2는 `length>0` 실패할 수 있음 — 이 경우 장 시간 중 재실행.
  </how-to-verify>
  <resume-signal>모든 검증 통과 후 "approved" + `GCP_PROJECT_ID=<value>` 공유. 이슈 있으면 기술.</resume-signal>
</task>

<task type="auto" id="02-05-02">
  <name>Task 2: scripts/deploy-server.sh 실행 (실 Cloud Run 배포)</name>
  <read_first>
    - /Users/alex/repos/gh-radar/scripts/deploy-server.sh (Wave 4)
    - /Users/alex/repos/gh-radar/scripts/smoke-server.sh (Wave 4)
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-RESEARCH.md (§3.3, §3.4, §3.5 비용 메모)
  </read_first>
  <action>
    **env export (사용자 제공 값):**
    ```bash
    export GCP_PROJECT_ID=<from Task 1>
    export SUPABASE_URL=<from Phase 1 .env>
    export CORS_ALLOWED_ORIGINS='http://localhost:3000,/^https:\/\/gh-radar-.*\.vercel\.app$/'
    ```

    **배포 실행:**
    ```bash
    cd /Users/alex/repos/gh-radar
    bash scripts/deploy-server.sh 2>&1 | tee /tmp/deploy.log
    ```

    스크립트는 가드 → build → push → deploy → smoke 순으로 실행. 마지막 `✅ deploy-server.sh complete` 출력 확인.

    **배포 후 추가 검증 (INV-10, INV-11):**

    1. **INV-10 — minScale 확인:**
       ```bash
       MIN=$(gcloud run services describe gh-radar-server \
         --region=asia-northeast3 \
         --format='value(spec.template.metadata.annotations.autoscaling\.knative\.dev/minScale)')
       [[ "$MIN" == "1" ]] && echo "✓ INV-10 minScale=1 PASS" || (echo "✗ INV-10 FAIL: $MIN"; exit 1)
       ```

    2. **INV-11 — Cloud Logging 필드:**
       ```bash
       gcloud logging read \
         'resource.type="cloud_run_revision" AND resource.labels.service_name="gh-radar-server"' \
         --limit=5 --format=json > /tmp/logs.json
       jq -e '[.[] | select((.jsonPayload.request_id // .httpRequest.requestUrl) and (.jsonPayload.status // .httpRequest.status))] | length > 0' /tmp/logs.json \
         && echo "✓ INV-11 structured logs PASS" || echo "✗ INV-11 FAIL (확인 필요)"
       ```

    3. **min=1 실효 검증 (SC#2):** 5분 대기 후 cold start 없음 확인
       ```bash
       URL=$(gcloud run services describe gh-radar-server --region=asia-northeast3 --format='value(status.url)')
       echo "Waiting 5 minutes for idle..."
       sleep 300
       for i in 1 2 3; do
         /usr/bin/time -p curl -fsS -o /dev/null "$URL/api/health" 2>&1 | grep real
       done
       # real 값이 모두 < 0.5 이면 cold start 없음
       ```

    4. **T-02-01 시크릿 누출 검증:**
       ```bash
       # 배포 로그에 service_role 키 문자열 확인 (SUPABASE_SERVICE_ROLE_KEY 값의 마지막 10자리 등으로)
       KEY_TAIL=$(echo -n "$SUPABASE_SERVICE_ROLE_KEY" | tail -c 10)
       grep -c "$KEY_TAIL" /tmp/deploy.log && echo "✗ 키 누출 가능성" || echo "✓ deploy.log에 키 미노출"

       # Cloud Logging에서도 확인
       gcloud logging read \
         "resource.type=cloud_run_revision AND resource.labels.service_name=gh-radar-server AND textPayload:\"$KEY_TAIL\"" \
         --limit=1 --format=json | jq 'length'
       # 0이어야 함
       ```

    **실패 시 복구:**
    - push 실패 (auth) → `gcloud auth configure-docker asia-northeast3-docker.pkg.dev` 재실행
    - deploy 실패 (시크릿 권한) → Task 1 step 6 IAM 바인딩 재확인
    - 5xx 즉시 발생 → `gcloud logging read 'severity=ERROR' --limit=10` 진단
    - **5xx 폭증 / smoke 실패 시 즉시 traffic 롤백** (이전 revision으로 100%):
      ```bash
      # 이전 revision 이름 조회 → traffic 100% 전환
      PREV_REV=$(gcloud run revisions list \
        --service=gh-radar-server \
        --region=asia-northeast3 \
        --format='value(metadata.name)' \
        --sort-by=~metadata.creationTimestamp \
        --limit=2 | tail -1)
      gcloud run services update-traffic gh-radar-server \
        --region=asia-northeast3 \
        --to-revisions=$PREV_REV=100
      ```
      롤백 후 다시 `bash scripts/smoke-server.sh $URL`로 이전 revision의 INV-1~INV-9 통과 재확인.
  </action>
  <verify>
    <automated>URL=$(gcloud run services describe gh-radar-server --region=asia-northeast3 --format='value(status.url)' 2>/dev/null) && test -n "$URL" && bash /Users/alex/repos/gh-radar/scripts/smoke-server.sh "$URL"</automated>
  </verify>
  <acceptance_criteria>
    - `gcloud run services describe gh-radar-server --region=asia-northeast3` 성공 + `status.url` 값 존재 (https로 시작)
    - minScale annotation 값이 정확히 문자열 "1"
    - `bash scripts/smoke-server.sh $URL` exit 0 (9개 INV 모두 PASS)
    - `time curl` 3회 real 값 모두 < 0.5초
    - `/tmp/deploy.log`에 `$SUPABASE_SERVICE_ROLE_KEY`의 마지막 10자리 문자열 0건
    - Cloud Logging 최근 5개 로그 중 `request_id` 또는 `httpRequest.requestUrl` 필드 존재 (1건 이상)
    - 롤백 경로 문서화 완료 — action의 "실패 시 복구" 섹션에 `gcloud run services update-traffic ... --to-revisions=$PREV_REV=100` 명령이 존재 (배포 후 5xx 폭증 시 즉시 이전 revision으로 traffic 100% 전환 가능)
  </acceptance_criteria>
  <done>Cloud Run 배포 + 11개 invariant 전체 검증 완료. 롤백 경로 즉시 실행 가능 상태.</done>
</task>

<task type="checkpoint:human-verify" id="02-05-03" gate="blocking">
  <name>Task 3 [체크포인트]: 배포 결과 확인 + 비용 예상</name>
  <action>체크포인트 — `<how-to-verify>`에 따라 배포 결과를 사용자에게 제시하고 비용 확인 수령 대기.</action>
  <verify>체크포인트 — 사용자가 브라우저로 3개 URL 확인 + Billing 비용 확인 응답 제공.</verify>
  <done>사용자 "approved" + 월 비용 값 공유 시 Task 4 로그 기록 진행.</done>
  <what-built>
    - Cloud Run service `gh-radar-server` (asia-northeast3) 공개 URL
    - min=1/max=3/cpu=1/mem=512Mi 프로파일
    - INV-1~INV-11 모두 PASS
  </what-built>
  <how-to-verify>
    **Claude가 보여주는 것:**
    1. 배포 URL
    2. smoke-server.sh 전체 출력 (9/9 PASS)
    3. `gcloud run services describe` yaml 일부 (spec.template.metadata.annotations 포함)
    4. `time curl` 3회 결과 (< 500ms 확인)
    5. Cloud Logging sample entry 1건 (jsonPayload 구조)

    **사용자 확인 포인트:**
    - [ ] 브라우저에서 직접 `$URL/api/health` 방문 → `{"status":"ok",...}` JSON 확인
    - [ ] 브라우저에서 `$URL/api/scanner?limit=5` → 배열 확인 (장 중이면 데이터, 장 외에는 Phase 1 최근 스냅샷)
    - [ ] 브라우저에서 `$URL/api/stocks/005930` → 삼성전자 확인
    - [ ] **STATE.md Blocker 해소:** Cloud Run 비용 — `$GCP_PROJECT_ID` 콘솔 → Billing → Cloud Run 예상 월 금액 확인 (RESEARCH §3.5 추정 $8~12 검증)

    **기대치 상기:** ROADMAP Phase 2 Success Criteria 4개:
    1. Express 앱이 Cloud Run 공개 URL로 접근 가능 ✅
    2. min-instances=1 cold start 없음 ✅ (INV-10 + time curl)
    3. `/api/scanner` Supabase 읽기 + JSON 반환 ✅ (INV-2)
    4. `/api/stocks/:code` 개별 종목 반환 ✅ (INV-3, INV-4)
  </how-to-verify>
  <resume-signal>모두 확인 후 "approved" + 실제 월 예상 비용 공유 (STATE.md Blocker 해소). 이슈 있으면 기술.</resume-signal>
</task>

<task type="auto" id="02-05-04">
  <name>Task 4: 02-DEPLOY-LOG.md 기록 + Phase 2 종료</name>
  <read_first>
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-VALIDATION.md
  </read_first>
  <action>
    `.planning/phases/02-backend-api/02-DEPLOY-LOG.md` 신규 작성 (템플릿):

    ```markdown
    # Phase 2 Deploy Log

    **Deployed:** <YYYY-MM-DD HH:MM KST>
    **Git SHA:** <from $(git rev-parse --short HEAD)>
    **Service:** gh-radar-server
    **Region:** asia-northeast3
    **URL:** <from gcloud services describe>

    ## Profile
    - CPU: 1 / Memory: 512Mi / Concurrency: 80
    - min-instances: 1 / max-instances: 3
    - Timeout: 300s

    ## Invariant Results (INV-1 ~ INV-11)

    | INV | Description | Result | Evidence |
    |-----|-------------|--------|----------|
    | INV-1 | /api/health | ✅ PASS | curl → status=ok |
    | INV-2 | /api/scanner array + upperLimitProximity | ✅ PASS | length=<N> |
    | INV-3 | /api/stocks/005930 | ✅ PASS | code=005930 |
    | INV-4 | /api/stocks/000000 → 404 STOCK_NOT_FOUND | ✅ PASS | |
    | INV-5 | /api/stocks/search?q=삼성 ≤20 | ✅ PASS | length=<N> |
    | INV-6 | CORS 허용 origin → 2xx | ✅ PASS | |
    | INV-7 | CORS 비허용 origin → ACAO 부재 | ✅ PASS | |
    | INV-8 | 201 req → 429 RATE_LIMITED | ✅ PASS | |
    | INV-9 | X-Request-Id 헤더 | ✅ PASS | |
    | INV-10 | minScale=1 annotation | ✅ PASS | gcloud describe |
    | INV-11 | 구조화 로그 request_id | ✅ PASS | Cloud Logging sample |

    ## min=1 Cold Start Check (SC#2)
    5분 idle 후 `time curl $URL/api/health` 3회:
    - run 1: real <X>s
    - run 2: real <Y>s
    - run 3: real <Z>s
    → 모두 < 0.5s (cold start 없음)

    ## Security Audit
    - [x] T-02-01: deploy.log에 service_role 키 미노출 (grep 0건)
    - [x] Cloud Logging에 service_role 키 미노출
    - [x] IAM 최소권한: secretAccessor는 `gh-radar-supabase-service-role` 리소스에만 부여

    ## Cost Estimate
    - 예상 월 비용: <사용자 확인 값> (RESEARCH §3.5 추정 $8~12)
    - STATE.md Blocker "Cloud Run 비용 확인" → ✅ 해소

    ## Phase 2 ROADMAP Success Criteria
    - [x] SC#1: Express 앱 Cloud Run 공개 URL 접근 가능
    - [x] SC#2: min-instances=1 cold start 없음
    - [x] SC#3: /api/scanner Supabase 읽기 + JSON 반환
    - [x] SC#4: /api/stocks/:code 개별 종목 반환
    ```

    **STATE.md 업데이트**(사람이 `/gsd-verify-work` 또는 별도 스텝에서 처리 — 이 task는 로그만 작성).
  </action>
  <verify>
    <automated>test -f /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-DEPLOY-LOG.md && grep -c 'PASS' /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-DEPLOY-LOG.md | awk '{ exit ($1 < 11) }'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-backend-api/02-DEPLOY-LOG.md` 존재
    - 파일에 URL, Git SHA, Deployed 타임스탬프 포함
    - INV-1~INV-11 11개 행 모두 ✅ PASS
    - SC#1~SC#4 모두 체크됨
    - Cost Estimate 섹션에 실제 확인 금액 기록
    - T-02-01 보안 감사 체크 3개 모두 [x]
  </acceptance_criteria>
  <done>Phase 2 배포 완료 + 영구 기록 확보. /gsd-verify-work로 페이즈 완료 선언 가능.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-02-01 | I (Secret leak) | 배포 로그 + Cloud Logging | mitigate | Task 2 acceptance에서 `$SUPABASE_SERVICE_ROLE_KEY` 마지막 10자리 grep으로 누출 검증. pino redact (Plan 01 T-02-01)와 중복 방어 |
| T-02-05 | S (Wrong-account deploy) | deploy-server.sh guard | mitigate | Plan 04에서 구현한 `ACTIVE_CONFIG/ACTIVE_PROJECT` 가드가 실행 시 작동. Task 1이 `gh-radar` configuration을 명시 activate하도록 사용자 안내 |
| T-02-18 | E (Runtime SA 권한 과다) | Cloud Run runtime SA | mitigate | `secretmanager.secretAccessor`를 프로젝트 단위가 아닌 **리소스 단위** `gh-radar-supabase-service-role`에만 바인딩 (Task 1 step 6). 다른 시크릿 접근 불가 |
| T-02-19 | D (min=1 비활성화 사고) | Cloud Run service | mitigate | INV-10이 minScale annotation 확인 — 누구가 실수로 콘솔에서 0으로 돌리면 다음 배포/verify에서 즉시 감지 |
| T-02-20 | I (Cloud Logging retention 기본값으로 PII 누출 가능) | Cloud Logging | accept | v1은 공개 API, 로그에 PII 없음. pino redact로 authorization/cookie/service_role 차단 — 잔여 리스크 수용 |
</threat_model>

<verification>
1. Task 1 체크포인트 완료 (GCP 사전조건)
2. Task 2 자동 — deploy + smoke 9/9 PASS + INV-10/INV-11 확인 + 시크릿 누출 0건
3. Task 3 체크포인트 — 브라우저 3개 URL 확인 + 비용 확인
4. Task 4 자동 — DEPLOY-LOG.md 기록

Phase gate: 모든 task 완료 시 ROADMAP Phase 2 Success Criteria 4개 모두 TRUE.
</verification>

<success_criteria>
- [ ] Cloud Run 서비스 `gh-radar-server` 공개 URL 확보
- [ ] smoke-server.sh 9/9 PASS
- [ ] INV-10 minScale=1 확인
- [ ] INV-11 구조화 로그 확인
- [ ] 5분 idle 후 time curl 3회 < 0.5초 (cold start 없음)
- [ ] 배포 로그/Cloud Logging 시크릿 누출 0건
- [ ] 02-DEPLOY-LOG.md 작성 + 11 INV 표 + 비용 기록
- [ ] 사용자가 "approved" 확인
</success_criteria>

<output>
완료 후: `.planning/phases/02-backend-api/02-05-SUMMARY.md` 생성 + `.planning/phases/02-backend-api/02-DEPLOY-LOG.md` 영구 보관 + STATE.md `Phase 2 complete` 업데이트 준비(다음 `/gsd-verify-work`가 처리).
</output>
