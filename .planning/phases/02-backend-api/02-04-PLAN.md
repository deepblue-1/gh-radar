---
phase: 02-backend-api
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
requirements: [INFR-03]
files_modified:
  - server/Dockerfile
  - server/.dockerignore
  - scripts/deploy-server.sh
  - scripts/smoke-server.sh
autonomous: true

must_haves:
  truths:
    - "Dockerfile 멀티스테이지 (node:22-alpine) — Phase 1 Dockerfile 패턴 미러 (D-31)"
    - "Docker 이미지가 non-root USER로 실행"
    - "Dockerfile ARG GIT_SHA + ENV APP_VERSION 주입 (RESEARCH Q2 권고 B)"
    - "--platform=linux/amd64 강제 (macOS arm64 개발환경 대응, D-32)"
    - "scripts/deploy-server.sh가 gcloud 가드 체크 포함 (D-36, D-39) — 잘못된 프로젝트면 exit 1"
    - "scripts/deploy-server.sh가 --set-env-vars ^@^ delimiter 사용 (CORS_ALLOWED_ORIGINS 콤마 이스케이프, RESEARCH Pitfall 4)"
    - "scripts/deploy-server.sh가 --min-instances=1 --max-instances=3 --cpu=1 --memory=512Mi --concurrency=80 명시 (D-28)"
    - "scripts/smoke-server.sh가 INV-1 ~ INV-9 9개 invariant를 curl+jq로 검증"
  artifacts:
    - path: "server/Dockerfile"
      provides: "멀티스테이지 production 이미지"
      contains: "FROM node:22-alpine"
    - path: "server/.dockerignore"
      provides: "빌드 컨텍스트 축소"
      contains: "node_modules"
    - path: "scripts/deploy-server.sh"
      provides: "gcloud 가드 + build + push + deploy"
      contains: "gcloud config configurations list"
    - path: "scripts/smoke-server.sh"
      provides: "INV-1~INV-9 curl 검증"
      contains: "api/health"
  key_links:
    - from: "server/Dockerfile"
      to: "server/dist/server.js (Wave 3 빌드)"
      via: "CMD node"
      pattern: "CMD.*node.*dist/server.js"
    - from: "scripts/deploy-server.sh"
      to: "scripts/smoke-server.sh"
      via: "배포 후 호출"
      pattern: "smoke-server.sh"
    - from: "scripts/deploy-server.sh"
      to: "gh-radar-supabase-service-role Secret Manager"
      via: "--update-secrets"
      pattern: "update-secrets.*SUPABASE_SERVICE_ROLE_KEY"
---

<objective>
Phase 2 Wave 4 — 컨테이너 이미지 + 배포 자동화 스크립트 + 배포 후 smoke 검증 스크립트를 **모두 로컬에서 정적 검증**한다. 실제 Cloud Run 배포는 Wave 5에서 실행.

Purpose: 배포 아티팩트를 격리된 wave에서 작성해 실수/재시도 비용 최소화.
Output: `docker build` 로컬 통과 + `scripts/deploy-server.sh` shellcheck 통과 + `scripts/smoke-server.sh` 호출 시 URL 주입으로 검증 가능 상태.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-backend-api/02-CONTEXT.md
@.planning/phases/02-backend-api/02-RESEARCH.md
@.planning/phases/02-backend-api/02-VALIDATION.md
@workers/ingestion/Dockerfile

<interfaces>
**Phase 1 Dockerfile 패턴 (mirror 대상)** — `workers/ingestion/Dockerfile`:
```dockerfile
FROM node:22-alpine AS builder
RUN corepack enable && corepack prepare pnpm@10 --activate
WORKDIR /app
COPY package.json pnpm-workspace.yaml pnpm-lock.yaml ./
COPY packages/shared/package.json ./packages/shared/
COPY workers/ingestion/package.json ./workers/ingestion/
RUN pnpm install --frozen-lockfile
COPY packages/shared/ ./packages/shared/
COPY workers/ingestion/ ./workers/ingestion/
RUN pnpm -F @gh-radar/shared build
RUN pnpm -F @gh-radar/ingestion build

FROM node:22-alpine
WORKDIR /app
COPY --from=builder /app/workers/ingestion/dist ./dist
COPY --from=builder /app/workers/ingestion/package.json ./
COPY --from=builder /app/node_modules ./node_modules
COPY --from=builder /app/packages/shared/dist ./node_modules/@gh-radar/shared/dist
COPY --from=builder /app/packages/shared/package.json ./node_modules/@gh-radar/shared/package.json
CMD ["node", "dist/index.js"]
```

**차이점 (Server용):**
- `workers/ingestion` → `server`
- `CMD node dist/index.js` → `CMD node dist/server.js`
- **추가:** non-root USER (D-31 verbatim), EXPOSE 8080, ARG GIT_SHA + ENV APP_VERSION

**Cloud Run deploy 명령 (RESEARCH §3.3 verbatim):**
```bash
gcloud run deploy gh-radar-server \
  --image=${REGISTRY}/server:${SHA} \
  --region=asia-northeast3 \
  --platform=managed \
  --allow-unauthenticated \
  --port=8080 \
  --cpu=1 --memory=512Mi --concurrency=80 \
  --min-instances=1 --max-instances=3 \
  --timeout=300s \
  --set-env-vars="^@^NODE_ENV=production@LOG_LEVEL=info@SUPABASE_URL=${SUPABASE_URL}@CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS}@APP_VERSION=${SHA}" \
  --update-secrets="SUPABASE_SERVICE_ROLE_KEY=gh-radar-supabase-service-role:latest"
```

**INV 목록 (VALIDATION.md §10.3):** INV-1 health, INV-2 scanner, INV-3 detail, INV-4 404, INV-5 search, INV-6 CORS 허용, INV-7 CORS 거부, INV-8 rate-limit, INV-9 X-Request-Id. INV-10(minScale) + INV-11(로그)는 Wave 5에서 별도 gcloud 명령으로 검증.
</interfaces>
</context>

<tasks>

<task type="auto" id="02-04-01">
  <name>Task 1: server/Dockerfile + .dockerignore + 로컬 빌드 검증</name>
  <read_first>
    - /Users/alex/repos/gh-radar/workers/ingestion/Dockerfile (mirror 대상 — Phase 1 검증 완료)
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-RESEARCH.md (§8 Dockerfile + .dockerignore)
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-CONTEXT.md (D-31, D-32)
    - /Users/alex/repos/gh-radar/server/package.json (Wave 1) — name="@gh-radar/server"
    - /Users/alex/repos/gh-radar/server/dist/server.js (Wave 3 빌드 산출물)
  </read_first>
  <action>
    **파일 1: `server/Dockerfile` 신규** (RESEARCH §8 verbatim + non-root + ARG GIT_SHA):
    ```dockerfile
    # === Builder Stage ===
    FROM node:22-alpine AS builder
    RUN corepack enable && corepack prepare pnpm@10 --activate
    WORKDIR /app

    # 의존성 캐시 최적화 — lockfile 변경 시에만 install 재실행
    COPY package.json pnpm-workspace.yaml pnpm-lock.yaml ./
    COPY packages/shared/package.json ./packages/shared/
    COPY server/package.json ./server/

    RUN pnpm install --frozen-lockfile

    # 소스 복사 + 빌드
    COPY packages/shared/ ./packages/shared/
    COPY server/ ./server/

    RUN pnpm -F @gh-radar/shared build
    RUN pnpm -F @gh-radar/server build

    # === Production Image ===
    FROM node:22-alpine
    WORKDIR /app

    # non-root user (D-31)
    RUN addgroup -S app && adduser -S app -G app

    # 빌드 아티팩트 + 최소 runtime 만 복사
    COPY --from=builder /app/server/dist ./dist
    COPY --from=builder /app/server/package.json ./
    COPY --from=builder /app/node_modules ./node_modules
    COPY --from=builder /app/packages/shared/dist ./node_modules/@gh-radar/shared/dist
    COPY --from=builder /app/packages/shared/package.json ./node_modules/@gh-radar/shared/package.json

    # 버전 주입 (RESEARCH Q2 권고 B) — 배포 스크립트가 --build-arg GIT_SHA=$SHA 전달
    ARG GIT_SHA=dev
    ENV APP_VERSION=${GIT_SHA}

    USER app
    EXPOSE 8080
    CMD ["node", "dist/server.js"]
    ```

    **파일 2: `server/.dockerignore` 신규** (RESEARCH §8):
    ```
    node_modules
    **/node_modules
    **/dist
    .git
    .env
    .env.*
    *.md
    .planning
    tests
    **/*.test.ts
    coverage
    .vscode
    ```

    **로컬 빌드 검증:**
    ```bash
    cd /Users/alex/repos/gh-radar
    docker build --platform=linux/amd64 -f server/Dockerfile -t gh-radar-server:local --build-arg GIT_SHA=local-test .
    # 빌드 성공 후:
    docker run --rm -e SUPABASE_URL=dummy -e SUPABASE_SERVICE_ROLE_KEY=dummy -e CORS_ALLOWED_ORIGINS="" gh-radar-server:local node -e "console.log(process.env.APP_VERSION)"
    # 예상 출력: local-test
    ```

    **단순성 우선:** HEALTHCHECK는 추가하지 않음 (RESEARCH §8: Cloud Run이 자체 probe 사용, Dockerfile HEALTHCHECK는 무시됨).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && docker build --platform=linux/amd64 -f server/Dockerfile -t gh-radar-server:local --build-arg GIT_SHA=local-test . 2>&1 | tail -20 && docker run --rm gh-radar-server:local node -e 'console.log(process.env.APP_VERSION)' | grep -q 'local-test'</automated>
  </verify>
  <acceptance_criteria>
    - `server/Dockerfile` 존재, `FROM node:22-alpine` 2개 stage 포함 (grep)
    - `server/Dockerfile`이 `RUN addgroup -S app && adduser -S app -G app` 포함
    - `server/Dockerfile`이 `USER app` 포함 (대소문자 정확)
    - `server/Dockerfile`이 `ARG GIT_SHA=dev` + `ENV APP_VERSION=${GIT_SHA}` 포함
    - `server/Dockerfile`이 `CMD ["node", "dist/server.js"]` 포함
    - `server/Dockerfile`이 `EXPOSE 8080` 포함
    - `server/.dockerignore` 존재, `node_modules`, `.env`, `.planning`, `tests`, `**/*.test.ts` 모두 포함
    - `docker build --platform=linux/amd64 ...` exit 0
    - `docker run --rm gh-radar-server:local node -e 'console.log(process.env.APP_VERSION)'` 출력이 `local-test` 포함
  </acceptance_criteria>
  <done>이미지 로컬 빌드 + 버전 주입 검증 완료. Wave 5에서 push만 남음.</done>
</task>

<task type="auto" id="02-04-02">
  <name>Task 2: scripts/deploy-server.sh (gcloud 가드 + build + push + deploy)</name>
  <read_first>
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-RESEARCH.md (§9 deploy 스크립트 전문, §3.3 gcloud run deploy, §Pitfall 4 콤마 이스케이프)
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-CONTEXT.md (D-36, D-39)
  </read_first>
  <action>
    **파일 1: `scripts/deploy-server.sh` 신규** (RESEARCH §9 verbatim + APP_VERSION 주입):
    ```bash
    #!/usr/bin/env bash
    set -euo pipefail

    # ═══════════════════════════════════════════════════════════════
    # Section 1: 가드 — gcloud configuration 검증 (D-36, D-39)
    # ═══════════════════════════════════════════════════════════════
    EXPECTED_PROJECT="${GCP_PROJECT_ID:-}"
    EXPECTED_CONFIG="gh-radar"

    if [[ -z "$EXPECTED_PROJECT" ]]; then
      echo "ERROR: GCP_PROJECT_ID env var is required" >&2
      echo "Hint: export GCP_PROJECT_ID=<your-project-id>" >&2
      exit 1
    fi

    ACTIVE_CONFIG=$(gcloud config configurations list --filter='IS_ACTIVE=true' --format='value(name)')
    ACTIVE_PROJECT=$(gcloud config get-value project 2>/dev/null || true)

    if [[ "$ACTIVE_CONFIG" != "$EXPECTED_CONFIG" ]]; then
      echo "ERROR: active gcloud configuration is '$ACTIVE_CONFIG', expected '$EXPECTED_CONFIG'" >&2
      echo "Hint: gcloud config configurations activate $EXPECTED_CONFIG" >&2
      exit 1
    fi

    if [[ "$ACTIVE_PROJECT" != "$EXPECTED_PROJECT" ]]; then
      echo "ERROR: active project is '$ACTIVE_PROJECT', expected '$EXPECTED_PROJECT'" >&2
      echo "Hint: gcloud config set project $EXPECTED_PROJECT" >&2
      exit 1
    fi

    echo "✓ gcloud guard: config=$ACTIVE_CONFIG, project=$ACTIVE_PROJECT"

    # ═══════════════════════════════════════════════════════════════
    # Section 2: 변수
    # ═══════════════════════════════════════════════════════════════
    SERVICE=gh-radar-server
    REGION=asia-northeast3
    REPO=gh-radar
    SHA=$(git rev-parse --short HEAD)
    REGISTRY="${REGION}-docker.pkg.dev/${EXPECTED_PROJECT}/${REPO}"
    IMAGE="${REGISTRY}/server:${SHA}"
    IMAGE_LATEST="${REGISTRY}/server:latest"

    : "${SUPABASE_URL:?SUPABASE_URL must be set (export or .env.deploy)}"
    : "${CORS_ALLOWED_ORIGINS:?CORS_ALLOWED_ORIGINS must be set}"

    echo "✓ variables: SHA=$SHA, IMAGE=$IMAGE"

    # ═══════════════════════════════════════════════════════════════
    # Section 3: Build (amd64 강제, GIT_SHA 주입)
    # ═══════════════════════════════════════════════════════════════
    echo "▶ docker build..."
    docker build \
      --platform=linux/amd64 \
      --build-arg "GIT_SHA=${SHA}" \
      -f server/Dockerfile \
      -t "$IMAGE" \
      -t "$IMAGE_LATEST" \
      .

    # ═══════════════════════════════════════════════════════════════
    # Section 4: Push
    # ═══════════════════════════════════════════════════════════════
    echo "▶ docker push..."
    docker push "$IMAGE"
    docker push "$IMAGE_LATEST"

    # ═══════════════════════════════════════════════════════════════
    # Section 5: Deploy (D-28 프로파일 + RESEARCH Pitfall 4 delimiter)
    # ═══════════════════════════════════════════════════════════════
    echo "▶ gcloud run deploy..."
    gcloud run deploy "$SERVICE" \
      --image="$IMAGE" \
      --region="$REGION" \
      --platform=managed \
      --allow-unauthenticated \
      --port=8080 \
      --cpu=1 \
      --memory=512Mi \
      --concurrency=80 \
      --min-instances=1 \
      --max-instances=3 \
      --timeout=300s \
      --set-env-vars="^@^NODE_ENV=production@LOG_LEVEL=info@SUPABASE_URL=${SUPABASE_URL}@CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS}@APP_VERSION=${SHA}" \
      --update-secrets="SUPABASE_SERVICE_ROLE_KEY=gh-radar-supabase-service-role:latest"

    # ═══════════════════════════════════════════════════════════════
    # Section 6: Smoke
    # ═══════════════════════════════════════════════════════════════
    URL=$(gcloud run services describe "$SERVICE" --region="$REGION" --format='value(status.url)')
    echo ""
    echo "✓ Deployed: $URL"
    echo ""

    echo "▶ smoke tests..."
    bash "$(dirname "$0")/smoke-server.sh" "$URL"

    echo ""
    echo "✅ deploy-server.sh complete"
    ```

    실행 권한 부여:
    ```bash
    chmod +x /Users/alex/repos/gh-radar/scripts/deploy-server.sh
    ```

    **정적 검증 (shellcheck가 설치되어 있으면):**
    ```bash
    shellcheck scripts/deploy-server.sh || echo "shellcheck skipped (not installed)"
    ```

    **가드 단위 검증 (실제 배포 없이):**
    ```bash
    # 잘못된 프로젝트 시나리오 — guard가 exit 1 해야 함
    unset GCP_PROJECT_ID
    bash scripts/deploy-server.sh 2>&1 | grep -q "GCP_PROJECT_ID env var is required" && echo "✓ guard 1 works"
    ```
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && test -x scripts/deploy-server.sh && bash -n scripts/deploy-server.sh && (unset GCP_PROJECT_ID; bash scripts/deploy-server.sh 2>&1 | grep -q 'GCP_PROJECT_ID env var is required')</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/deploy-server.sh` 존재, 실행 권한 있음 (`test -x`)
    - `bash -n scripts/deploy-server.sh` exit 0 (문법 검증)
    - 스크립트가 `gcloud config configurations list --filter='IS_ACTIVE=true'` 포함
    - 스크립트가 `--min-instances=1 --max-instances=3 --cpu=1 --memory=512Mi --concurrency=80` 모두 포함 (grep)
    - 스크립트가 `--set-env-vars="^@^NODE_ENV=production@LOG_LEVEL=info` 포함 (delimiter 정확)
    - 스크립트가 `--update-secrets="SUPABASE_SERVICE_ROLE_KEY=gh-radar-supabase-service-role:latest"` 포함
    - 스크립트가 `--build-arg "GIT_SHA=${SHA}"` 포함
    - 스크립트가 `--platform=linux/amd64` 포함
    - `GCP_PROJECT_ID` 미설정 시 스크립트가 exit 1 + "GCP_PROJECT_ID env var is required" 메시지 출력
  </acceptance_criteria>
  <done>배포 스크립트 정적 검증 완료. Wave 5에서 실 실행.</done>
</task>

<task type="auto" id="02-04-03">
  <name>Task 3: scripts/smoke-server.sh (INV-1 ~ INV-9 curl 검증)</name>
  <read_first>
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-VALIDATION.md (§Smoke-Test Invariants)
    - /Users/alex/repos/gh-radar/.planning/phases/02-backend-api/02-RESEARCH.md (§10.3 INV-1 ~ INV-11 verbatim)
  </read_first>
  <action>
    **파일 1: `scripts/smoke-server.sh` 신규** (RESEARCH §10.3 verbatim — INV-1 ~ INV-9 번호 순서):
    ```bash
    #!/usr/bin/env bash
    set -uo pipefail
    # 주의: -e는 끄고 각 invariant를 개별 fail 추적 (모두 실행 후 집계)

    URL="${1:-${SMOKE_URL:-}}"
    if [[ -z "$URL" ]]; then
      echo "Usage: smoke-server.sh <URL>" >&2
      exit 2
    fi

    PASS=0
    FAIL=0
    declare -a FAILED_INVS

    check() {
      local name="$1"; shift
      echo -n "  $name ... "
      if "$@" >/dev/null 2>&1; then
        echo "PASS"
        PASS=$((PASS + 1))
      else
        echo "FAIL"
        FAIL=$((FAIL + 1))
        FAILED_INVS+=("$name")
      fi
    }

    echo "Smoke testing $URL"
    echo ""

    # INV-1: /api/health → 200 + {status:'ok', timestamp:string, version:string}
    check "INV-1 /api/health status=ok" bash -c "curl -fsS '$URL/api/health' | jq -e '.status==\"ok\" and (.timestamp|type==\"string\") and (.version|type==\"string\")'"

    # INV-2: /api/scanner → array, 각 원소에 upperLimitProximity:number
    check "INV-2 /api/scanner upperLimitProximity" bash -c "curl -fsS '$URL/api/scanner?limit=5' | jq -e 'type==\"array\" and length>0 and (.[0].upperLimitProximity|type==\"number\")'"

    # INV-3: /api/stocks/005930 → code="005930"
    check "INV-3 /api/stocks/005930" bash -c "curl -fsS '$URL/api/stocks/005930' | jq -e '.code==\"005930\"'"

    # INV-4: /api/stocks/000000 → 404 + STOCK_NOT_FOUND
    check "INV-4 /api/stocks/000000 → 404" bash -c "
      body=\$(curl -s -o /dev/null -w '%{http_code}' '$URL/api/stocks/000000')
      [ \"\$body\" = '404' ] && curl -s '$URL/api/stocks/000000' | jq -e '.error.code==\"STOCK_NOT_FOUND\"'
    "

    # INV-5: /api/stocks/search?q=삼성 → length<=20 and length>0
    check "INV-5 /api/stocks/search?q=삼성" bash -c "curl -fsS '$URL/api/stocks/search?q=%EC%82%BC%EC%84%B1' | jq -e 'type==\"array\" and length<=20 and length>0'"

    # INV-6: CORS preflight 허용 origin → 200/204
    check "INV-6 CORS preflight (허용)" bash -c "
      code=\$(curl -s -o /dev/null -w '%{http_code}' -X OPTIONS \\
        -H 'Origin: https://gh-radar.vercel.app' \\
        -H 'Access-Control-Request-Method: GET' \\
        '$URL/api/scanner')
      [ \"\$code\" = '200' ] || [ \"\$code\" = '204' ]
    "

    # INV-7: CORS preflight 비허용 origin → ACAO 헤더 부재
    check "INV-7 CORS preflight (거부)" bash -c "
      ! curl -s -X OPTIONS -H 'Origin: https://evil.example.com' -H 'Access-Control-Request-Method: GET' -D - -o /dev/null '$URL/api/scanner' | grep -qi '^access-control-allow-origin:'
    "

    # INV-8: rate limit — 201 req → 마지막은 429
    check "INV-8 rate limit 201 req → 429" bash -c "
      last=\$(for i in \$(seq 1 201); do curl -s -o /dev/null -w '%{http_code}\n' '$URL/api/health'; done | tail -1)
      [ \"\$last\" = '429' ]
    "

    # INV-9: X-Request-Id 헤더 항상 존재
    check "INV-9 X-Request-Id 헤더" bash -c "curl -fsS -D - '$URL/api/health' -o /dev/null | grep -qi '^x-request-id:'"

    echo ""
    echo "═══════════════════════════════════════"
    echo "PASS: $PASS  FAIL: $FAIL"
    if [[ $FAIL -gt 0 ]]; then
      echo "Failed: ${FAILED_INVS[*]}"
      exit 1
    fi
    echo "✅ All smoke invariants passed"
    ```

    실행 권한 부여:
    ```bash
    chmod +x /Users/alex/repos/gh-radar/scripts/smoke-server.sh
    ```

    **문법 검증:**
    ```bash
    bash -n scripts/smoke-server.sh
    ```

    **INV-10(minScale)과 INV-11(로그)은 Wave 5 수동 검증.** smoke-server.sh는 curl로 체크 가능한 9개만 담당.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && test -x scripts/smoke-server.sh && bash -n scripts/smoke-server.sh && bash scripts/smoke-server.sh 2>&1 | grep -q 'Usage: smoke-server.sh'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/smoke-server.sh` 존재, 실행 권한 있음
    - `bash -n scripts/smoke-server.sh` exit 0
    - 스크립트에 `INV-1`, `INV-2`, ..., `INV-9` 9개 문자열 모두 존재 (grep -c "INV-" ≥ 9)
    - 스크립트에 `jq -e '.status=="ok"'` 포함 (INV-1)
    - 스크립트에 `upperLimitProximity` 문자열 포함 (INV-2)
    - 스크립트에 `STOCK_NOT_FOUND` 문자열 포함 (INV-4)
    - 스크립트에 `for i in $(seq 1 201)` 포함 (INV-8)
    - 스크립트에 `x-request-id:` 포함 (INV-9)
    - URL 인자 없이 실행 시 `Usage: smoke-server.sh` 출력 + exit 2
  </acceptance_criteria>
  <done>smoke 스크립트 작성 완료. Wave 5 실 배포 후 `bash scripts/smoke-server.sh $URL` 으로 9개 invariant 검증 가능.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Trusted Side | Untrusted Side | Crossing Assets | Related Threats |
|----------|-------------|---------------|-----------------|------------------|
| 개발자 머신 → Artifact Registry | 로컬 Docker 빌드 (amd64 플랫폼 강제) | GCP Artifact Registry (`asia-northeast3-docker.pkg.dev/${GCP_PROJECT_ID}/gh-radar`) | 이미지 (tag=git SHA + latest), `GIT_SHA` build arg | T-02-14, T-02-16 |
| Artifact Registry → Cloud Run | GCP 내부 네트워크 | 런타임 컨테이너 프로세스 | 이미지 pull + non-root USER 전환 | T-02-14 |
| Cloud Run → Secret Manager | Cloud Run runtime SA (`${PROJECT_NUMBER}-compute@…`) | Secret Manager 리소스 `gh-radar-supabase-service-role` | `SUPABASE_SERVICE_ROLE_KEY` 주입 (메모리 전용) | T-02-05(운영자 실수) |
| Cloud Run → Supabase | Cloud Run egress | 외부 인터넷 (Supabase PostgREST) | service_role 키 헤더 + JSON API 쿼리 | T-02-15(CORS 경계 오염) |
| 개발자 → gcloud CLI | 로컬 개발자 ADC + `gh-radar` configuration | GCP Control Plane API | 배포 명령 + 프로젝트 ID + `--set-env-vars` 페이로드 | T-02-05, T-02-15 |
| deploy-server.sh stdout → 로그 파일 | 배포 스크립트 출력 | `/tmp/deploy.log` + 사용자 터미널 | 환경변수 확인 로그 (service_role 키는 stdout 금지) | T-02-17 |

## STRIDE Threat Register

| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-02-05 | S (Wrong-account deploy) | scripts/deploy-server.sh Section 1 | mitigate | gcloud configuration 가드 — `ACTIVE_CONFIG`와 `EXPECTED_CONFIG='gh-radar'` 불일치 시 exit 1, `ACTIVE_PROJECT`와 `EXPECTED_PROJECT` 불일치 시 exit 1. D-39 weekly-wine-bot 오배포 방지 2차 방어선 |
| T-02-14 | E (Container escalation) | server/Dockerfile | mitigate | `USER app` non-root 실행. addgroup/adduser -S (system user, no login shell) |
| T-02-15 | T (CORS_ALLOWED_ORIGINS 파싱 깨짐) | deploy-server.sh --set-env-vars | mitigate | `^@^` delimiter 사용해 콤마 포함 값이 gcloud 파서에 의해 쪼개지지 않음 (RESEARCH Pitfall 4). acceptance에서 grep으로 delimiter 검증 |
| T-02-16 | I (이미지에 .env 포함) | server/.dockerignore | mitigate | `.env`, `.env.*`, `node_modules` 모두 제외. `docker history gh-radar-server:local`로 수동 검증 |
| T-02-17 | I (stack trace in smoke 실패 로그) | scripts/smoke-server.sh | accept | smoke 로그는 curl stderr만; 이미 production 에러 핸들러가 stack을 은닉 (Plan 02 T-02-06) |
</threat_model>

<verification>
1. `docker build --platform=linux/amd64 ...` exit 0 + `docker run`에서 APP_VERSION 주입 확인
2. `bash -n scripts/deploy-server.sh` + `bash -n scripts/smoke-server.sh` 모두 exit 0
3. `GCP_PROJECT_ID` 미설정 시 deploy 스크립트 가드 작동
4. smoke 스크립트 URL 없이 실행 시 Usage 메시지
</verification>

<success_criteria>
- [ ] `server/Dockerfile` — 멀티스테이지 + non-root + ARG GIT_SHA
- [ ] `server/.dockerignore` — .env, tests, node_modules 제외
- [ ] 로컬 docker build 통과 (amd64, APP_VERSION=local-test 주입 확인)
- [ ] `scripts/deploy-server.sh` — 가드 + build + push + deploy + smoke 호출
- [ ] `scripts/smoke-server.sh` — INV-1 ~ INV-9 curl+jq 검증
- [ ] 두 스크립트 모두 chmod +x + bash -n 통과
</success_criteria>

<output>
완료 후: `.planning/phases/02-backend-api/02-04-SUMMARY.md` 작성 (이미지 크기, 스크립트 파일 경로, shellcheck 결과 기록).
</output>
