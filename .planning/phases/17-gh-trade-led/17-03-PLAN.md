---
phase: 17-gh-trade-led
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - relay/src/dma/msg-type.ts
  - relay/src/dma/envelope.ts
  - relay/src/hub/subscription-hub.ts
  - relay/src/ws/fanout.ts
  - relay/tests/helpers/frames.ts
  - relay/tests/helpers/fake-gateway.ts
  - relay/tests/strategy-hub.test.ts
  - webapp/src/lib/use-relay-socket.ts
autonomous: true
requirements: [TRADE-04]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "게이트웨이가 보내는 76·77·78 프레임이 `unknown-msg-type` 경고 없이 파싱되고 브라우저까지 내려간다 (D-02 · D-03)"
    - "브라우저가 wss 인증을 끝내면 요청 없이 `rate.cross.snap` 1프레임과 (받은 적 있으면) `queued.window` 1프레임을 받는다 (D-03)"
    - "세션 Ready 전에 도착한 76 은 캐시에만 들어가고 팬아웃되지 않는다 (D-03)"
    - "hub switch 의 `default:` 로 조용히 떨어지는 프레임이 여전히 0 이다 (PC-12)"
  artifacts:
    - relay/tests/helpers/frames.ts
    - relay/tests/helpers/fake-gateway.ts
  key_links:
    - "INBOUND_MSG_TYPES(76/77/78) ↔ subscription-hub.ts switch 명시 case 3개 — 둘은 한 커밋에서만 함께 바뀐다"
    - "hub 세션 캐시 → fanout 인증 스냅샷 → webapp applyFrame 보관"
---

<objective>
gh-trade 서버가 이미 보내고 있으나 relay 가 warn 으로 버리던 신규 푸시 3종(76 `RateCrossAlert` · 77 `QueuedWindowState` · 78 `RateCrossSnapshot`)을 파싱해 세션 캐시에 보관하고 브라우저 프레임으로 내린다. **웹 UI 는 만들지 않는다**(Phase 18) — 이 plan 의 목표는 드롭 경고 0 과 브라우저 상태 보관이다.

Purpose: 76 은 로그인 전 연결에도 KRX 20% 돌파마다 온다. 지금은 매 돌파마다 `unknown-msg-type` warn 이 쌓여 진짜 이상 신호를 덮는다. 또한 Phase 18 의 돌파감지 UI 는 relay 가 above 집합을 이미 들고 있어야 첫 프레임부터 그릴 수 있다.
Output: 파서 3종, hub 명시 case 3개 + 세션 캐시 + getter, 인증 스냅샷 2프레임, webapp 리듀서 보관, fake-gateway 빌더.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gh-trade-led/17-CONTEXT.md
@.planning/phases/17-gh-trade-led/17-RESEARCH.md
@.planning/phases/17-gh-trade-led/17-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 76 RateCrossAlert — 화이트리스트에서 브라우저 상태까지 한 경로</name>
  <files>relay/src/dma/msg-type.ts, relay/src/dma/envelope.ts, relay/src/hub/subscription-hub.ts, webapp/src/lib/use-relay-socket.ts, relay/tests/helpers/frames.ts, relay/tests/strategy-hub.test.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/relay/src/dma/msg-type.ts` 전체 (`INBOUND_MSG_TYPES` 주석의 PC-12 규율 — 「넓힌 만큼 명시 case 로 받는 것이 조건」)
    - `/Users/alex/repos/gh-radar/relay/src/dma/envelope.ts` 433-462 (`tryParseEnvelope` 드롭 2갈래) · 471-520 (`parseQuoteState` — 파서의 형식 검증·`dropField` 관례)
    - `/Users/alex/repos/gh-radar/relay/src/hub/subscription-hub.ts` 545-645 (`#onFrame` switch · `default:` 주석) · 464-491 (getter 3종 형식) · 261 부근 (`#viTriggers` 캐시 선언 형식)
    - `/Users/alex/repos/gh-radar/relay/src/ws/fanout.ts` 550-572 (인증 직후 스냅샷 전송부)
    - `/Users/alex/repos/gh-radar/webapp/src/lib/use-relay-socket.ts` 348-430 (`applyFrame` switch)
    - `/Users/alex/repos/gh-radar/relay/tests/helpers/frames.ts` 81-146 · `/Users/alex/repos/gh-radar/relay/tests/helpers/fake-gateway.ts` 440-480 (`sendGarbage`/`UNKNOWN_MSG_TYPE`)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §1-1 76행 · §1-2 `RateCrossAlert` 본문행 · §4 Pitfall 1
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-02 · D-03
  </read_first>
  <behavior>
    - `isin` 12자 · `exchange` "KRX" 인 76 프레임 → `parseRateCrossAlert` 가 `RelayRateCrossItem` 을 돌려준다
    - `isin` 이 12자가 아닌 76 프레임 → `null` (기존 `dropField("bad-isin", ...)` 형식)
    - 세션이 Ready 인 사용자에게 76 이 오면 브라우저 연결로 `{t:"rate.cross", item}` 1프레임이 나간다
    - 세션이 아직 Ready 가 아닐 때 온 76 은 캐시에만 들어가고 팬아웃되지 않는다
    - 같은 `isin`+`exchange` 로 76 이 두 번 오면 캐시 원소는 1개이고 **뒤 값으로 덮인다**
    - 76 수신에 `unknown-msg-type` warn 이 0건이다
  </behavior>
  <action>
① `relay/src/dma/msg-type.ts` 의 `INBOUND_MSG_TYPES` 에 `MSG.RateCrossAlert` · `MSG.QueuedWindowState` · `MSG.RateCrossSnapshot` 을 더해 **22종**으로 만든다. 상단 「유입 집합」 주석에 이 3종의 하류 처리 책임(명시 case + 세션 캐시)을 한 문단으로 적는다. 화이트리스트를 넓히는 것과 hub 명시 `case` 를 더하는 것은 **같은 커밋**이어야 한다 — 따로 두면 그 사이 커밋에서 프레임이 `default:` 로 조용히 떨어져 PC-12 불변식이 깨진다(Pitfall 1).

② `relay/src/dma/envelope.ts` 에 `parseRateCrossAlert(env: Envelope): RelayRateCrossItem | null` 을 추가한다. 슬롯은 `env.rateCrossAlert()`. 형식 검증은 기존 파서와 동형으로 `isValidIsin` · `isValidExchange` 를 쓰고 실패는 `dropField` 로 사유·카운터를 남긴다. `lastPrice`/`basePrice` 는 long 일 수 있으므로 `toNum` 을 통과시킨다(16-RESEARCH Pitfall 3). `changeRate` 는 double % 이므로 그대로. `exchangeTime`(12자) · `serverTime`("HH:MM:SS") 는 **해석하지 않고 원문**으로 넘긴다.

③ `relay/src/hub/subscription-hub.ts` 에 명시 `case MSG.RateCrossAlert:` 를 더한다. 처리: 파싱 → 세션 캐시 upsert → Ready 인 세션에만 `#fanout(userId, { t: "rate.cross", item })`. 캐시는 사용자별 `Map<string, RelayRateCrossItem>` 이고 키는 `isin + ":" + exchange` 다(같은 종목이 양쪽 거래소에서 돌파하면 원소 둘). **하루 1회 규칙·임계−2%p 이탈 삭제는 relay 가 하지 않는다** — 서버 above 집합을 그대로 보관하고 표시 규칙은 Phase 18 클라 몫이다(D-03).

④ `webapp/src/lib/use-relay-socket.ts` 의 `applyFrame` 에 `case "rate.cross":` 를 더한다. **상태 보관만** 한다 — 새 `rateCrossItems` 배열(또는 Map)에 `isin`+`exchange` 키로 upsert 하고 정렬은 `exchangeTime` 오름차순·동률 시 `isin` 오름차순으로 둔다. 상한(`MAX_RATE_CROSS`)을 `MAX_ORDERS` 와 같은 자리에 상수로 두되 서버 above 집합 크기를 넘어 자를 만큼 작게 잡지 않는다(권장 200). UI 는 만들지 않는다. `RelayData` 타입과 컨텍스트 노출 목록에 새 필드를 더한다.

⑤ 테스트: `relay/tests/helpers/frames.ts` 에 `buildRateCrossAlertFrame(input)` 빌더를 더한다(문자열은 테이블 열기 전에 `createString` — Pitfall 2). `relay/tests/strategy-hub.test.ts`(또는 신규 `relay/tests/rate-cross.test.ts`)에 `<behavior>` 6케이스를 단언한다. 드롭 0 은 **로거 스파이**로 잰다: 76 프레임 처리 중 `unknown-msg-type` 사유로 warn 이 불리지 않음을 단언한다.

⑥ 커밋 메시지 초안: `feat(17-03): RateCrossAlert(76) 중계 — 화이트리스트·파서·세션 캐시·브라우저 프레임`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "MSG.RateCrossAlert" relay/src/hub/subscription-hub.ts</automated>
    <fails_when>카운트가 0 이면 실패 (hub 에 명시 case 가 없어 프레임이 default 로 조용히 떨어진다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm typecheck</automated>
    <fails_when>비영 종료코드, 또는 출력에 `error TS` 가 나오면 실패</fails_when>
  </verify>
  <acceptance_criteria>
    - `relay/src/dma/msg-type.ts` 의 `INBOUND_MSG_TYPES` 원소가 22개다.
    - `relay/src/dma/envelope.ts` 에 `export function parseRateCrossAlert(` 가 1회 있다.
    - `relay/src/hub/subscription-hub.ts` 의 `#onFrame` switch 에 `case MSG.RateCrossAlert:` 가 있다.
    - `webapp/src/lib/use-relay-socket.ts` 의 `applyFrame` 에 `case "rate.cross":` 가 있다.
    - Ready 전 76 이 팬아웃되지 않는 케이스가 테스트에 있다.
    - 76 프레임 처리 중 `unknown-msg-type` warn 0 을 로거 스파이로 단언하는 테스트가 있다.
    - relay 테스트 + 루트 typecheck green.
  </acceptance_criteria>
  <done>KRX 20% 돌파 알림이 relay 를 통과해 브라우저 상태에 쌓인다. 그 경로에서 드롭 경고가 나지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 78 스냅샷 · 77 예약창 상태 — 캐시 교체 규약과 인증 직후 스냅샷</name>
  <files>relay/src/dma/envelope.ts, relay/src/hub/subscription-hub.ts, relay/src/ws/fanout.ts, webapp/src/lib/use-relay-socket.ts, relay/tests/helpers/frames.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/relay/src/ws/fanout.ts` 550-572 (인증 직후 스냅샷 — `lc.snap` 은 **비어도 1프레임**, `vi` 는 `undefined` 면 안 보낸다는 3상태 규율이 여기 있다)
    - `/Users/alex/repos/gh-radar/relay/src/hub/subscription-hub.ts` 428-500 (`requestStrategySnapshot` · getter 3종)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §1-1 77·78행 (전송 클래스 — 78 은 로그인 성공 직후 그 연결에만, 77 은 로그인 직후 1프레임 + 창 전이마다) · §1-2 `QueuedWindowState` 본문행 · `RateCrossSnapshot.items` 행
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-03
    - 17-01 SUMMARY 의 `RelayRateCrossSnapMsg` · `RelayQueuedWindowMsg` 필드 목록
  </read_first>
  <behavior>
    - 원소 3개인 78 프레임 → 세션 캐시 above 집합이 **그 3개로 통째 교체**된다(이전 76 upsert 결과는 남지 않는다)
    - 빈 벡터 78 프레임 → 캐시가 빈 집합이 된다(무시하지 않는다 — 「돌파 없음」의 확정 정보다)
    - 77 프레임 2건 연속 → 캐시에는 **마지막 1건만** 남는다
    - wss 인증을 끝낸 연결은 `{t:"rate.cross.snap", items}` 를 **items 가 비어 있어도** 1프레임 받는다
    - 77 을 한 번도 못 받은 세션의 인증 연결은 `queued.window` 프레임을 **받지 않는다**
    - 78/77 수신에 `unknown-msg-type` warn 이 0건이다
  </behavior>
  <action>
① `relay/src/dma/envelope.ts` 에 `parseRateCrossSnapshot(env): RelayRateCrossItem[] | null` 과 `parseQueuedWindowState(env): RelayQueuedWindowMsg | null` 을 추가한다. 78 은 `env.rateCrossSnapshot()` 의 `items` 벡터를 돌며 각 원소를 Task 1 의 원소 파서와 **같은 함수**로 읽는다(원소 판정이 두 벌이 되지 않게 헬퍼로 뽑는다). 원소 하나가 깨지면 `parseTradeTape` 와 같은 규율로 **프레임 전체를 버린다**(일부만 내보내면 above 집합이 조용히 어긋난다). 77 은 `env.queuedWindowState()` 에서 `open` · `maxPieces` · `preopenOpen` · `g2Open` · `g3Open` · `nxtPreopenOpen` 여섯을 읽는다 — **전부 표시 힌트이고 벽시계로 창을 판정하지 않는다**(fbs 와 features 문서의 시각이 엇갈린다, `open` 플래그만 믿는다).

② `relay/src/hub/subscription-hub.ts` 에 명시 `case MSG.RateCrossSnapshot:` 와 `case MSG.QueuedWindowState:` 를 더한다. 78 은 above 집합 **전량 교체**(`Map` 을 비우고 다시 채운다), 77 은 최신 1건 보관. 둘 다 Ready 세션에만 팬아웃한다. getter 를 더한다: `getRateCrossItems(userId): RelayRateCrossItem[]`(정렬 = `exchangeTime` 오름차순·동률 `isin` 오름차순) 과 `getQueuedWindow(userId): RelayQueuedWindowMsg | undefined`(`undefined` = 아직 못 받음).

③ `relay/src/ws/fanout.ts` 인증 직후 스냅샷 블록에 두 프레임을 더한다. `rate.cross.snap` 은 **비어 있어도 1프레임 보낸다**(빈 배열은 「돌파 없음」의 확정 정보다 — `lc.snap` 과 같은 규율). `queued.window` 는 `getQueuedWindow` 가 `undefined` 면 **보내지 않는다**(`vi` 의 3상태 규율과 같은 이유 — 지어낸 창 상태를 내리면 브라우저가 거짓 라벨을 그린다). 기존 주석 블록에 두 프레임의 규율 차이를 이어서 적는다.

④ `webapp/src/lib/use-relay-socket.ts` 에 `case "rate.cross.snap":`(전량 교체) 와 `case "queued.window":`(최신 1건 보관, 초기값 `undefined`) 를 더한다. 상태 보관만 — UI 없음.

⑤ 테스트: `frames.ts` 에 `buildRateCrossSnapshotFrame(items)` · `buildQueuedWindowStateFrame(input)` 빌더를 더한다(벡터는 원소 N개를 각각 끝낸 **뒤에** `createVector` — Pitfall 2). `<behavior>` 6케이스를 단언한다. 인증 스냅샷 케이스는 `relay/tests/fanout.test.ts` 또는 `strategy-hub.test.ts` 의 기존 인증 스냅샷 단언 옆에 둔다.

⑥ 커밋 메시지 초안: `feat(17-03): RateCrossSnapshot(78)·QueuedWindowState(77) 중계 + 인증 직후 스냅샷 2프레임`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test && pnpm --filter @gh-radar/relay run typecheck:tests</automated>
    <fails_when>비영 종료코드, vitest 요약 줄에 `failed`, 또는 tsc 출력에 `error TS` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "rate.cross.snap" relay/src/ws/fanout.ts</automated>
    <fails_when>카운트가 0 이면 실패 (인증 직후 스냅샷에 돌파 집합이 실리지 않는다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `relay/src/dma/envelope.ts` 에 `export function parseRateCrossSnapshot(` 와 `export function parseQueuedWindowState(` 가 각 1회 있다.
    - `relay/src/hub/subscription-hub.ts` 에 `case MSG.RateCrossSnapshot:` 와 `case MSG.QueuedWindowState:` 가 각 1회 있다.
    - `relay/src/hub/subscription-hub.ts` 에 `getRateCrossItems(` 와 `getQueuedWindow(` 가 export 된 메서드로 있다.
    - 빈 벡터 78 이 캐시를 비우는 케이스와, 77 미수신 세션에 `queued.window` 가 안 나가는 케이스가 각각 테스트에 있다.
    - `webapp/src/lib/use-relay-socket.ts` 에 `case "rate.cross.snap":` 와 `case "queued.window":` 가 있다.
    - relay 테스트 + typecheck:tests green.
  </acceptance_criteria>
  <done>브라우저가 인증만 하면 요청 없이 현재 above 집합과 (알려져 있으면) 예약창 상태를 받는다. 캐시 교체 규약이 테스트로 굳었다.</done>
</task>

<task type="auto">
  <name>Task 3: 드롭 0 게이트 — mock 게이트웨이 76/77/78 발사 + `default:` 무유입 단언</name>
  <files>relay/tests/helpers/fake-gateway.ts, relay/src/hub/subscription-hub.ts, relay/tests/strategy-hub.test.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/relay/tests/helpers/fake-gateway.ts` 전체 (프레임 발사 API · `UNKNOWN_MSG_TYPE` 99 · `sendGarbage`)
    - `/Users/alex/repos/gh-radar/relay/src/hub/subscription-hub.ts` `#onFrame` 의 `default:` 주석 (「조용히 떨어지는 프레임은 0」)
    - `/Users/alex/repos/gh-radar/relay/src/dma/envelope.ts` 433-462 (`drop()` 이 남기는 사유 문자열 — `unknown-msg-type` / `out-of-scope-msg-type`)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §4 Pitfall 1
  </read_first>
  <action>
① `relay/tests/helpers/fake-gateway.ts` 에 76/77/78 프레임을 실제로 내보내는 헬퍼를 더한다(`sendRateCrossAlert` · `sendRateCrossSnapshot` · `sendQueuedWindowState`). Task 1·2 가 만든 `frames.ts` 빌더를 재사용하고 게이트웨이 쪽에서 프레임을 조립하지 않는다.

② hub `#onFrame` 의 `default:` 분기에 **테스트에서 관측 가능한 계수기**를 둔다: `default:` 도달 시 `logger.debug` 대신 전용 카운터(또는 기존 드롭 카운터)를 1 올리고, 그 값을 테스트가 읽을 수 있게 노출한다. 프로덕션 로그 볼륨은 늘리지 않는다(debug 레벨 유지).

③ 회귀 테스트를 한 건 추가한다: fake 게이트웨이가 76·77·78 을 각 1프레임씩 보낸 뒤 ⓐ `default:` 카운터가 0 이고 ⓑ `unknown-msg-type` 사유의 warn 이 0건이며 ⓒ 브라우저 연결이 `rate.cross` · `rate.cross.snap` · `queued.window` 를 각각 받는다. 이어서 fake 게이트웨이가 미등록 번호(`UNKNOWN_MSG_TYPE` 99)를 보내면 **여전히** warn 이 1건 난다는 대조 단언을 같은 테스트에 둔다 — 게이트가 살아 있음의 증거다.

④ 커밋 메시지 초안: `test(17-03): 신규 푸시 3종 드롭 0 게이트 — mock 게이트웨이 왕복 + default 무유입 단언`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
  </verify>
  <acceptance_criteria>
    - `relay/tests/helpers/fake-gateway.ts` 가 76/77/78 발사 헬퍼 3개를 export 한다.
    - hub `#onFrame` 의 `default:` 도달을 세는 계수기가 있고 테스트에서 읽을 수 있다.
    - 76/77/78 각 1프레임 뒤 `default:` 카운터가 0 임을 단언하는 테스트가 있다.
    - 같은 테스트에 미등록 번호 99 가 warn 1건을 남긴다는 대조 단언이 있다.
    - 브라우저 연결이 세 프레임을 각각 수신함을 단언한다.
    - relay 전량 테스트 green.
  </acceptance_criteria>
  <done>「조용히 떨어지는 프레임 0」이 주석이 아니라 실행되는 게이트가 됐다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (17-03)

**함수:** `parseRateCrossAlert()` · `parseRateCrossSnapshot()` · `parseQueuedWindowState()` (`relay/src/dma/envelope.ts`)

**hub API:** `getRateCrossItems(userId)` · `getQueuedWindow(userId)` + `#onFrame` 명시 case 3개 + `default:` 계수기

**브라우저 프레임 `t` 값:** `rate.cross` · `rate.cross.snap` · `queued.window`

**webapp 상태:** `RelayData.rateCrossItems` · `RelayData.queuedWindow` + 상수 `MAX_RATE_CROSS`

**테스트 헬퍼:** `buildRateCrossAlertFrame` · `buildRateCrossSnapshotFrame` · `buildQueuedWindowStateFrame` · `sendRateCrossAlert` · `sendRateCrossSnapshot` · `sendQueuedWindowState`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트웨이 → relay (인증 전 포함) | 76 은 **로그인 전 연결에도** 온다 — 세션 소유자 판정 전의 프레임이다 |
| relay 세션 캐시 → 브라우저 팬아웃 | 캐시가 사용자별로 갈려야 한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-07 | Information Disclosure | Ready 전 76 팬아웃 | high | mitigate | Ready 전 프레임은 캐시만 하고 팬아웃하지 않는다 — 테스트로 단언 |
| T-17-08 | Information Disclosure | 사용자 간 above 집합 유출 | critical | mitigate | 캐시 키를 `userPrefix(userId)` 규율로 사용자별로 가른다(기존 `#quotes`/`#limitChasers` 동형) |
| T-17-09 | Denial of Service | 78 items 벡터 폭주 | medium | mitigate | 기존 `takeCount` 상한 헬퍼를 items 벡터에도 적용한다 |
| T-17-10 | Tampering | 화이트리스트만 넓히고 case 누락 | high | mitigate | `default:` 계수기 0 게이트 (Task 3) |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 plan 은 패키지 설치가 없다 |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/relay test` · `run typecheck` · `run typecheck:tests` green
- `pnpm typecheck` (루트) green — webapp 리듀서와 shared 계약 일치
- mock 게이트웨이 76/77/78 왕복에서 `default:` 계수기 0 · `unknown-msg-type` warn 0
</verification>

<success_criteria>
- 76/77/78 이 파싱돼 세션 캐시와 브라우저 상태에 들어간다.
- 인증 직후 `rate.cross.snap` 1프레임(빈 배열 포함) 이 나가고, 77 미수신이면 `queued.window` 는 나가지 않는다.
- 드롭 경고 0 이 자동 게이트로 굳었다.
</success_criteria>

<output>
Create `.planning/phases/17-gh-trade-led/17-03-SUMMARY.md` when done
</output>