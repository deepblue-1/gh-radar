---
phase: 17-gh-trade-led
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - relay/src/generated/
  - relay/src/dma/msg-type.ts
  - relay/src/dma/envelope.ts
  - packages/shared/src/relay.ts
  - packages/shared/src/strategy-display.ts
  - packages/shared/src/index.ts
  - packages/shared/src/__tests__/strategy-display.test.ts
  - webapp/src/components/layout/__tests__/app-sidebar.test.tsx
  - webapp/src/components/trading/__tests__/
  - webapp/src/lib/__tests__/
  - relay/tests/helpers/frames.ts
autonomous: true
requirements: [TRADE-04, TRADE-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "relay/src/generated 가 gh-trade HEAD 스키마와 같고 sync-relay-schema.sh --check 가 차이 0 으로 끝난다 (D-01)"
    - "게이트웨이 60 에코의 cancel_entry_latched · buy_entry_latched 원값이 브라우저 `lc` 프레임까지 그대로 도달한다 (D-05)"
    - "packages/shared/src/relay.ts 한 파일이 이 phase 의 신규 계약 전부(신규 필드 · 신규 프레임 3종 · lc.arm 인바운드)를 담고 있어 뒤 wave 가 이 파일을 다시 열지 않는다"
  artifacts:
    - relay/src/generated/stock-dma/rate-cross-alert.ts
    - relay/src/generated/stock-dma/rate-cross-snapshot.ts
    - relay/src/generated/stock-dma/queued-window-state.ts
    - packages/shared/src/strategy-display.ts
    - packages/shared/src/__tests__/strategy-display.test.ts
  key_links:
    - "relay/src/dma/msg-type.ts MSG ↔ relay/src/generated/stock-dma/msg-type.ts enum (envelope.test.ts 대조가 알람)"
    - "readLimitChaser() → RelayLimitChaser → webapp use-relay-socket.ts case \"lc\" → limitChasers 상태"
---

<objective>
gh-trade 서버 HEAD(`d7b80618`, 실서버 배포분 `59f7513e` 이상) 스키마로 `relay/src/generated/**` 를 재생성하고, 이 phase 가 쓸 **계약 전체**를 `packages/shared/src/relay.ts` 한 파일에 한 번에 정의한다. 동시에 상따 래치 2필드(`cancel_entry_latched` · `buy_entry_latched`)를 와이어에서 브라우저 리듀서까지 **끝에서 끝까지** 흐르게 해 재동기화가 실제로 통했음을 증명한다.

Purpose: 이 phase 의 모든 뒤 plan 이 같은 타입 정본을 쓰게 만든다. 계약을 여러 wave 가 나눠 정의하면 `packages/shared/src/relay.ts` 가 직렬 축이 되어 병렬성이 사라지고, 픽스처 깨짐이 wave 마다 반복된다.
Output: 재생성된 생성물 42+3 파일, MSG 6종 등록, shared 계약 전체, shared 표시 헬퍼 2종, 갱신된 리터럴 픽스처.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gh-trade-led/17-CONTEXT.md
@.planning/phases/17-gh-trade-led/17-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 스키마 재동기화 + MSG 6종 등록 + 래치 2필드 end-to-end</name>
  <files>relay/src/generated/, relay/src/dma/msg-type.ts, relay/src/dma/envelope.ts, packages/shared/src/relay.ts, relay/src/dma/__tests__/envelope.test.ts, relay/tests/helpers/frames.ts, webapp/src/components/layout/__tests__/app-sidebar.test.tsx</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §1-1 · §1-2 · §3-1 (신규 MsgType 표 · 슬롯 표 · 재생성 startObject 예상치)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-01 · D-02 · D-05
    - `/Users/alex/repos/gh-radar/relay/src/dma/msg-type.ts` (전체 — `MSG` · `INBOUND_MSG_TYPES` · `OUT_OF_SCOPE_INBOUND_MSG_TYPES` 주석 규율)
    - `/Users/alex/repos/gh-radar/relay/src/dma/envelope.ts` 1613-1687 (`readLimitChaser`, `sellEntryLatched` 가 1668 의 본보기)
    - `/Users/alex/repos/gh-radar/packages/shared/src/relay.ts` 129 부근 (`RelayLimitChaser`)
    - `/Users/alex/repos/gh-radar/webapp/src/components/layout/__tests__/app-sidebar.test.tsx` 60-110 (`makeChaser` 팩토리 — 리터럴 픽스처의 본보기)
    - 읽기 전용 정본: `/Users/alex/repos/gh-trade/server/src/protocol/StockDMA.fbs` 50-62 · 526-564, `/Users/alex/repos/gh-trade/server/scripts/expected-vtable.txt`. **gh-trade 저장소는 절대 수정하지 않는다.**
  </read_first>
  <action>
① 재생성: gh-trade 저장소의 `server/` 에서 `RELAY=/Users/alex/repos/gh-radar/relay ./scripts/sync-relay-schema.sh` 를 실행한다. `RELAY=` 를 반드시 명시한다 — 생략하면 스크립트 기본값이 메인 체크아웃을 가리켜 worktree 밖에 쓴다(16-01-SUMMARY:37,91,102 · Pitfall 9). 실행 뒤 같은 디렉터리에서 `RELAY=... ./scripts/sync-relay-schema.sh --check` 로 차이 0 을 확인한다. flatc 는 `/opt/homebrew/bin/flatc` 25.12.19. `relay/src/generated/**` 는 **손편집 금지**(T-16-05) — 스크립트 산출물이다.

② `relay/src/dma/msg-type.ts` 의 `MSG` 에 6종을 추가한다: `ArmSellLatchReq: 36`, `ArmCancelLatchReq: 37`, `ArmBuyLatchReq: 38`, `RateCrossAlert: 76`, `QueuedWindowState: 77`, `RateCrossSnapshot: 78`. 각 항목에 방향·본문 슬롯·전송 클래스를 한 줄 주석으로 적는다(기존 항목과 같은 형식). **`INBOUND_MSG_TYPES` 는 이 plan 에서 건드리지 않는다** — 76/77/78 을 화이트리스트에만 넣고 hub 의 명시 `case` 를 같은 커밋에 두지 않으면 프레임이 `default:` 로 조용히 떨어져 PC-12(「조용히 떨어지는 프레임 0」) 불변식이 깨진다. 그 등록은 17-03 의 몫이다. 이 사실을 `INBOUND_MSG_TYPES` 주석에 한 줄로 남긴다.

③ `relay/src/dma/envelope.ts` 의 `readLimitChaser` 반환 객체에 `cancelEntryLatched: t.cancelEntryLatched()` 와 `buyEntryLatched: t.buyEntryLatched()` 를 추가한다. `sellEntryLatched` 바로 뒤 자리에 둔다. 값은 **무장과 접지 않은 원값**을 그대로 넘긴다(D-05) — `&& enabled` 로 접지 않는다. `buildSetLimitChaserReq` 에는 **추가하지 않는다**(S→C 전용, `sellEntryLatched` 와 같은 규율).

④ `packages/shared/src/relay.ts` 의 `RelayLimitChaser` 에 `cancelEntryLatched: boolean` · `buyEntryLatched: boolean` 을 **필수 필드**로 추가한다(optional 로 두지 않는다 — relay 파서가 항상 채우므로 optional 은 「없을 수 있다」는 거짓 신호다). 각 필드에 S→C 전용임과 `buy_entry_latched` 가 `buyWatchSide === "1"` 갈래 전용(side "0" 은 서버가 켜지 않아 언제나 false, BL-01)임을 주석으로 적는다.

⑤ 픽스처 갱신: `pnpm typecheck` 가 지목하는 모든 `RelayLimitChaser` 리터럴을 두 필드로 채운다. 알려진 지점 — `webapp/src/components/layout/__tests__/app-sidebar.test.tsx`(`makeChaser` 팩토리 한 곳이면 충분), `webapp/src/components/trading/__tests__/` 의 limit-chaser-form · limit-chaser-client · strategy-log · strategy-badge · strategy-status-card, `webapp/src/lib/__tests__/` 의 limit-chaser · relay-provider · isin-labels, `relay/tests/helpers/frames.ts` 의 `FakeLimitChaserInput`(입력이 optional 이면 빌더가 두 필드를 실어 보내도록 `addCancelEntryLatched`/`addBuyEntryLatched` 호출을 더한다), `relay/src/dma/__tests__/envelope.test.ts`, `relay/tests/protocol.test.ts`. 기본값은 `false`.

⑥ `relay/src/dma/__tests__/envelope.test.ts` 의 `MSG` ↔ 생성 enum 대조 단언이 6종 추가로 깨지면 **그것이 의도된 알람**이다(D-02) — 대조 표를 새 6종으로 넓혀 통과시킨다. 그리고 `readLimitChaser` 가 두 래치를 원값으로 실어 오는 케이스를 1건 추가한다(`cancel_entry_latched=true` ∧ `cancelQtyEnabled=false` 조합이 그대로 true 로 나오는 것 — 접지 않음의 증거).

⑦ 커밋 메시지 초안: `feat(17-01): gh-trade HEAD 스키마 재동기화 + MsgType 36/37/38·76/77/78 등록 + 상따 래치 2필드 파서·계약 반영`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-trade/server && RELAY=/Users/alex/repos/gh-radar/relay ./scripts/sync-relay-schema.sh --check</automated>
    <fails_when>비영(非零) 종료코드, 또는 출력에 차이 파일이 한 줄이라도 나열되면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && test -f relay/src/generated/stock-dma/rate-cross-alert.ts && test -f relay/src/generated/stock-dma/rate-cross-snapshot.ts && test -f relay/src/generated/stock-dma/queued-window-state.ts && grep -c "cancelEntryLatched\|buyEntryLatched" relay/src/generated/stock-dma/set-limit-chaser.ts</automated>
    <fails_when>test 가 비영 종료하거나 grep 카운트가 0 이면 실패 (신규 생성물 3파일 부재 또는 래치 접근자 미생성)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm typecheck</automated>
    <fails_when>비영 종료코드, 또는 출력에 `error TS` 가 한 건이라도 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test && pnpm --filter @gh-radar/webapp test</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
  </verify>
  <acceptance_criteria>
    - `relay/src/generated/stock-dma/rate-cross-alert.ts` · `rate-cross-snapshot.ts` · `queued-window-state.ts` 3파일이 존재한다.
    - `git status --short relay/src/generated` 가 비어 있지 않다(재생성이 실제로 파일을 바꿨다).
    - `grep -c "ArmSellLatchReq: 36" relay/src/dma/msg-type.ts` 가 1 이상이고, 같은 파일에서 `ArmCancelLatchReq: 37` · `ArmBuyLatchReq: 38` · `RateCrossAlert: 76` · `QueuedWindowState: 77` · `RateCrossSnapshot: 78` 이 각각 잡힌다.
    - `relay/src/dma/msg-type.ts` 의 `INBOUND_MSG_TYPES` 원소 수는 **19 그대로**다(76/77/78 은 아직 넣지 않았다).
    - `relay/src/dma/envelope.ts` 의 `readLimitChaser` 반환 객체에 `cancelEntryLatched:` 와 `buyEntryLatched:` 가 각 1회 나타난다.
    - `grep -c "cancelEntryLatched" relay/src/dma/envelope.ts` 결과가 `buildSetLimitChaserReq` 본문 밖에서만 잡힌다 — `sed -n '/export function buildSetLimitChaserReq/,/^}/p' relay/src/dma/envelope.ts | grep -c "EntryLatched"` 가 `0` 이다.
    - `packages/shared/src/relay.ts` 의 `RelayLimitChaser` 에 `cancelEntryLatched: boolean` · `buyEntryLatched: boolean` 이 `?` 없이 있다.
    - `pnpm typecheck` · `pnpm --filter @gh-radar/relay test` · `pnpm --filter @gh-radar/webapp test` 가 전부 성공한다.
  </acceptance_criteria>
  <done>gh-trade HEAD 스키마가 relay 에 들어왔고, 60 에코의 취소·매수 래치 원값이 relay 파서 → shared 타입 → 브라우저 `lc` 리듀서까지 한 경로로 흐른다. 전량 스위트가 green.</done>
</task>

<task type="auto">
  <name>Task 2: shared 계약 전체 정의 — 신규 필드 · 신규 프레임 3종 · lc.arm 인바운드</name>
  <files>packages/shared/src/relay.ts, packages/shared/src/index.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/packages/shared/src/relay.ts` 전체 (936줄 — 기존 주석 밀도·명명 규약이 이 파일의 계약이다)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §1-2 (슬롯·기본값 표) · §3-2 (타입별 추가 목록과 줄 번호)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-03 · D-04 · D-06 · D-07 · D-08 · D-09 · D-10 · D-11
    - `/Users/alex/repos/gh-radar/packages/shared/src/index.ts` 1-60 (relay 타입 re-export 목록)
  </read_first>
  <action>
`packages/shared/src/relay.ts` **한 파일에서** 아래를 전부 정의한다. 뒤 wave 는 이 파일을 다시 열지 않는다.

① 기존 타입 필드 추가
  - `RelayQuote` 에 `kc: number` — KRX 정규장 종가. 오늘이 아니면 `0`. **NXT 프레임에도 KRX 값이 실린다**. 벽시계로 판정하지 않으며 `0` 도 권위값이라 거르지 않는다(D-11).
  - `RelayTapeEntry` 에 `bs: "" | "1" | "2"` — 서버 체결구분. `"1"` 매도 · `"2"` 매수 · `""` 미상(D-10).
  - `RelayUnfilled` 에 `orderTime: string`, `queuedStatus: string`, `pendingStatus: string`, `board: string`, `pendingCancelSent: boolean`. `board` 는 `"G2"`/`"G3"`/`""` 만 온다. `queuedStatus`/`pendingStatus` 는 **서버 문구를 표시만** 하고 클라가 재계산하지 않는다. `pendingCancelSent` 가 회색·취소 제외 판정의 **유일한** 근거임을 주석에 못박는다(D-07 · D-14 · gh-trade 교훈 24).
  - `RelayOrderMsg` 에 `bd?: string`(board) · `rk?: string`(request_kind "New"/"Modify"/"Cancel") · `rq?: string`(requester "Manual") 를 추가한다. 빈 값은 생략 가능. **`side`·`isin` 은 계속 싣지 않는다**(Pitfall 8 규율 유지). `msg` 문구는 어디서도 파싱하지 않는다는 주석을 `msg` 필드에 붙인다(D-08 · 804 거부 문구를 서버가 교체한다).
  - `RelayServerMsg.src` 의 어휘 주석에 `"LimitChaser"` · `"VITrigger"` 두 값을 더한다. 판정은 **동등 비교만**이고 자유문 해석은 금지임을 적는다(D-09).
  - `RelayViTrigger` 에 `exchange: RelayExchange`, `RelayViOrderItem` 에 `exchange: RelayExchange` 를 필수로 추가한다. 빈 와이어 값은 relay 가 `"KRX"` 로 정규화한다(D-06).
  - `RelayViTriggerMsg` 을 `{ t: "vi"; x: RelayExchange; cfg: RelayViTrigger | null }` 로 넓힌다 — 거래소별 프레임이다.
  - `RelayViNoticeMsg` 에 `exchange: RelayExchange` 를 추가한다.

② 신규 아웃바운드 프레임 3종(D-03). 필드는 fbs 그대로 옮긴다.
  - `RelayRateCrossItem` = `{ isin: string; exchange: RelayExchange; lastPrice: number; changeRate: number; thresholdPct: number; basePrice: number; exchangeTime: string; serverTime: string }`. `changeRate` 는 double %(내림 아님), `exchangeTime` 은 12자 원문.
  - `RelayRateCrossMsg` = `{ t: "rate.cross"; item: RelayRateCrossItem }` (76 단건 upsert).
  - `RelayRateCrossSnapMsg` = `{ t: "rate.cross.snap"; items: RelayRateCrossItem[] }` (78 전량 교체, `exchangeTime` 오름차순·동률 시 `isin` 오름차순).
  - `RelayQueuedWindowMsg` = `{ t: "queued.window"; open: boolean; maxPieces: number; preopenOpen: boolean; g2Open: boolean; g3Open: boolean; nxtPreopenOpen: boolean }` (77 최신 1건). 전부 **표시 힌트**이며 relay·브라우저는 벽시계로 창을 판정하지 않는다(fbs 와 features 문서의 시각이 엇갈린다 — `open` 플래그만 믿는다).
  - 셋을 `RelayOutbound` 유니온에 더한다.

③ 신규 인바운드(D-04)
  - `RelayLcArmMsg` = `{ t: "lc.arm"; key: string; latch: "sell" | "cancel" | "buy" }`. `key` 는 상따 전략 키 `ISIN:accountNo:exchange`(`GetLimitChaserReq 20` 과 같은 값). 요청은 **토글**이고 별도 ack 프레임이 없으며 결과는 기존 `lc` 에코, 실패는 기존 `msg`(54) 경로로 온다는 것을 주석에 적는다.
  - `RelayViSetMsg` 에 `exchange?: RelayExchange` 를 optional 로 추가한다(미지정 = `"KRX"`, D-06 · D-18).
  - `RelayLcArmMsg` 를 `RelayInbound` 유니온에 더한다.

④ `packages/shared/src/index.ts` 의 relay re-export 목록에 신규 타입 이름을 전부 더한다(`RelayRateCrossItem` · `RelayRateCrossMsg` · `RelayRateCrossSnapMsg` · `RelayQueuedWindowMsg` · `RelayLcArmMsg`).

⑤ 커밋 메시지 초안: `feat(17-01): shared relay 계약 확장 — 신규 프레임 3종·lc.arm 인바운드·기존 타입 13필드`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "rate.cross.snap\|queued.window\|lc.arm" packages/shared/src/relay.ts</automated>
    <fails_when>카운트가 3 미만이면 실패 (신규 프레임/인바운드 판별자 누락)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm typecheck</automated>
    <fails_when>비영 종료코드, 또는 출력에 `error TS` 가 나오면 실패</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/relay.ts` 에 `kc: number` · `bs: "" | "1" | "2"` · `pendingCancelSent: boolean` · `queuedStatus: string` · `pendingStatus: string` · `orderTime: string` 이 각각 존재한다.
    - 같은 파일에 `RelayRateCrossItem` · `RelayRateCrossMsg` · `RelayRateCrossSnapMsg` · `RelayQueuedWindowMsg` · `RelayLcArmMsg` 타입 선언이 각 1회 있다.
    - `RelayViTriggerMsg` 선언에 `x: RelayExchange` 가 있다.
    - `RelayOutbound` 유니온에 `RelayRateCrossMsg` · `RelayRateCrossSnapMsg` · `RelayQueuedWindowMsg` 세 이름이 모두 나타난다.
    - `RelayInbound` 유니온에 `RelayLcArmMsg` 가 나타난다.
    - `RelayOrderMsg` 선언 안에 `isin` 필드가 **없다**: `sed -n '/export type RelayOrderMsg = {/,/^};/p' packages/shared/src/relay.ts | grep -c "isin"` 이 `0` 이다.
    - `packages/shared/src/index.ts` 에서 위 5개 신규 타입 이름이 전부 re-export 된다.
    - `pnpm typecheck` 성공.
  </acceptance_criteria>
  <done>이 phase 가 쓸 브라우저·relay 계약이 한 파일에 전부 있고 타입체크가 통과한다. 뒤 wave 는 `packages/shared/src/relay.ts` 를 열 이유가 없다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: shared 표시 헬퍼 2종 — sideDisplayText · serverMsgBadge</name>
  <files>packages/shared/src/strategy-display.ts, packages/shared/src/index.ts, packages/shared/src/__tests__/strategy-display.test.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-09 · D-13 · D-17
    - `/Users/alex/repos/gh-radar/packages/shared/src/limitUp.ts` 와 `packages/shared/src/limitUp.test.ts` (이 패키지의 순수함수 + 테스트 배치 관례)
    - `/Users/alex/repos/gh-radar/packages/shared/src/index.ts`
    - 읽기 전용 정본: `/Users/alex/repos/gh-trade/client/Services/DMA/NotificationHub.cs` `SideDisplayText`(:172 부근)
  </read_first>
  <behavior>
    sideDisplayText(side, orderNo, pendingStatus, board):
    - side "B" · orderNo "0001234567" · pendingStatus "" · board "" → `매수`
    - side "B" · orderNo `Q` + 9자 → `매수Q`
    - side "S" · pendingStatus 비어 있지 않음 → `매도P`
    - side "B" · board "G2" → `매수/종가` ; board "G3" → `매수/종가`
    - 접미 우선순위는 C# 정본 순서를 따른다(Q-ID → pendingStatus → board). 두 조건이 겹치면 앞 접미 하나만 붙는다.
    - board 가 "G2"/"G3" 가 아닌 값이면 접미 없음.
    serverMsgBadge(src):
    - "LimitChaser" → `[상따]` · "VITrigger" → `[VI]` · 그 밖(빈 문자열·"Account"·"System"·미상) → `[서버]`
    - 판정은 동등 비교만 — 부분일치·정규식·대소문자 접기 금지.
  </behavior>
  <action>
`packages/shared/src/strategy-display.ts` 를 새로 만들고 두 순수함수를 export 한다.

① `sideDisplayText(side: OrderSide, orderNo: string, pendingStatus: string, board: string): string` — gh-trade `NotificationHub.SideDisplayText` 동형. 기본 단어는 `side` 가 매수면 `매수`, 매도면 `매도`. Q-ID 판정은 **주문번호가 10자이고 첫 글자가 영문 `Q`** 인 것으로 한다(서버 형식 `Q`+HHMMSS+3자리). `pendingStatus` 는 **비어 있는지만** 보고 문구를 비교하지 않는다(D-14 · 교훈 24). `board` 는 `"G2"`/`"G3"` 동등 비교만.

② `serverMsgBadge(src: string): string` — 위 `<behavior>` 대로 3분기 동등 비교. 어떤 분기도 자유문을 해석하지 않는다.

③ 두 함수를 `packages/shared/src/index.ts` 에서 re-export 한다.

④ `packages/shared/src/__tests__/strategy-display.test.ts` 를 만들어 `<behavior>` 의 케이스를 전부 단언한다. Q-ID 와 `pendingStatus` 가 동시에 있을 때 접미가 하나만 붙는 케이스, `board` 가 `"G1"` 인 케이스(접미 없음), `src` 가 `"limitchaser"` 소문자일 때 `[서버]` 로 떨어지는 케이스를 포함한다.

⑤ 커밋 메시지 초안: `feat(17-01): shared 표시 헬퍼 sideDisplayText·serverMsgBadge 추가 (C# 정본 동형)`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared test -- --run</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "sideDisplayText\|serverMsgBadge" packages/shared/src/index.ts</automated>
    <fails_when>카운트가 2 미만이면 실패 (두 함수가 index 에서 re-export 되지 않음)</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/strategy-display.ts` 가 존재하고 `export function sideDisplayText(` 와 `export function serverMsgBadge(` 를 각 1회 담는다.
    - `packages/shared/src/__tests__/strategy-display.test.ts` 가 존재하고 `<behavior>` 의 9개 케이스 이상을 단언한다.
    - `serverMsgBadge` 구현에 `includes(` · `startsWith(` · `toLowerCase(` · `RegExp` 가 **쓰이지 않는다**: `sed -n '/export function serverMsgBadge/,/^}/p' packages/shared/src/strategy-display.ts | grep -cE "includes\(|startsWith\(|toLowerCase\(|RegExp"` 가 `0` 이다.
    - `sideDisplayText` 구현이 `pendingStatus` 를 문자열 내용으로 비교하지 않는다: `sed -n '/export function sideDisplayText/,/^}/p' packages/shared/src/strategy-display.ts | grep -cE "pendingStatus\s*===\s*['\"][^'\"]" ` 가 `0` 이다(빈 문자열 비교는 허용되므로 비어 있지 않은 리터럴과의 비교만 잡는다).
    - shared 테스트 스위트 green.
  </acceptance_criteria>
  <done>두 표시 헬퍼가 shared 에 있고 규칙 표 케이스가 테스트로 굳었다. 뒤 wave 의 webapp 표면은 이 함수만 부른다(인라인 접미 금지).</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (17-01)

**생성물(스크립트 산출):** `relay/src/generated/stock-dma/rate-cross-alert.ts` · `rate-cross-snapshot.ts` · `queued-window-state.ts`, 갱신된 `set-limit-chaser.ts` · `unfilled-state.ts` · `order-resp.ts` · `quote-state.ts` · `trade-tape-entry.ts` · `direct-order-req.ts` · `set-vitrigger.ts` · `viorder-notice.ts` · `viorder-item.ts` · `envelope.ts` · `msg-type.ts`

**MsgType 상수:** `MSG.ArmSellLatchReq(36)` · `MSG.ArmCancelLatchReq(37)` · `MSG.ArmBuyLatchReq(38)` · `MSG.RateCrossAlert(76)` · `MSG.QueuedWindowState(77)` · `MSG.RateCrossSnapshot(78)`

**타입:** `RelayRateCrossItem` · `RelayRateCrossMsg`(`t:"rate.cross"`) · `RelayRateCrossSnapMsg`(`t:"rate.cross.snap"`) · `RelayQueuedWindowMsg`(`t:"queued.window"`) · `RelayLcArmMsg`(`t:"lc.arm"`)

**타입 필드:** `RelayLimitChaser.cancelEntryLatched` · `.buyEntryLatched` · `RelayQuote.kc` · `RelayTapeEntry.bs` · `RelayUnfilled.orderTime/.queuedStatus/.pendingStatus/.board/.pendingCancelSent` · `RelayOrderMsg.bd/.rk/.rq` · `RelayViTrigger.exchange` · `RelayViOrderItem.exchange` · `RelayViTriggerMsg.x` · `RelayViNoticeMsg.exchange` · `RelayViSetMsg.exchange?`

**함수:** `sideDisplayText()` · `serverMsgBadge()` (`packages/shared/src/strategy-display.ts`)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gh-trade 게이트웨이 → relay (TCP FlatBuffers) | 신뢰하지 않는 바이트. JS 런타임에 Verifier 가 없어 잘린 버퍼가 예외 없이 깨진 값을 준다 |
| gh-trade 저장소 스크립트 → gh-radar 파일시스템 | `sync-relay-schema.sh` 가 `RELAY=` 경로에 파일을 쓴다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | `sync-relay-schema.sh` 쓰기 경로 | medium | mitigate | `RELAY=` 절대경로 명시 + `--check` 차이 0 확인. worktree 밖 오염 시 즉시 드러난다 |
| T-17-02 | Information Disclosure | `MSG` 화이트리스트 확대 | high | mitigate | `INBOUND_MSG_TYPES` 는 이 plan 에서 **넓히지 않는다** — 명시 `case` 없는 수신 허용을 만들지 않는다(PC-12) |
| T-17-03 | Tampering | `buildSetLimitChaserReq` 에 S→C 전용 필드 유입 | high | mitigate | 조립기 본문에서 `EntryLatched` grep 0 을 acceptance 로 강제 |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 plan 은 패키지 설치가 없다 — 신규 의존성 0건 |
</threat_model>

<verification>
- `sync-relay-schema.sh --check` 차이 0
- `pnpm typecheck` · `pnpm --filter @gh-radar/relay test` · `pnpm --filter @gh-radar/webapp test` · `pnpm --filter @gh-radar/shared test -- --run` 전부 green
- `relay/src/generated` 밖에서 생성물 손편집 0 (`git diff` 로 생성물 diff 가 스크립트 산출 형태인지 육안 확인)
</verification>

<success_criteria>
- gh-trade HEAD 스키마가 relay 에 반영되고 재실행 시 차이가 0 이다.
- 상따 취소·매수 래치 원값이 브라우저 상태까지 도달한다.
- 이 phase 의 계약 전체가 `packages/shared/src/relay.ts` + `strategy-display.ts` 두 파일에 있다.
- 리터럴 픽스처가 전부 갱신돼 전량 스위트가 green 이다.
</success_criteria>

<output>
Create `.planning/phases/17-gh-trade-led/17-01-SUMMARY.md` when done
</output>
