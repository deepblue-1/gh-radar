---
phase: 17-gh-trade-led
plan: 04
type: execute
wave: 4
depends_on: ["17-03"]
files_modified:
  - relay/src/dma/envelope.ts
  - relay/src/ws/protocol.ts
  - relay/src/ws/fanout.ts
  - relay/tests/helpers/fake-gateway.ts
  - relay/tests/protocol.test.ts
  - relay/tests/ws-latch.test.ts
autonomous: true
requirements: [TRADE-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "브라우저가 `{t:\"lc.arm\", key, latch}` 를 올리면 relay 가 `get_strategy_req.key` 를 채운 36/37/38 Envelope 을 그 사용자의 DMA 세션으로 보낸다 (D-04)"
    - "가드는 `lc.set` 과 동형이다 — Ready 세션 · key 형식 · accountNo 가 세션 계좌에 있음. 빈 키는 송신하지 않는다 (D-04)"
    - "응답은 기존 `lc` 에코 경로로, 실패는 기존 `msg`(54) 경로로 온다 — 별도 ack 프레임이 없다 (D-04)"
    - "래치 요청은 pending-key FIFO 를 오염시키지 않는다 (Pitfall 3)"
  artifacts:
    - relay/tests/ws-latch.test.ts
  key_links:
    - "RelayLcArmSchema(zod) → fanout `lc.arm` 분기 → buildArmLatchReq → DMA 세션 send"
    - "60 에코 → parseLimitChaserEcho → `lc` 프레임 (별도 ack 없음)"
---

<objective>
상따 3단계 래치를 **수동으로 켜고 끄는** 요청 경로를 relay 에 만든다. 브라우저의 `lc.arm` 1건이 `ArmSellLatchReq(36)` · `ArmCancelLatchReq(37)` · `ArmBuyLatchReq(38)` 중 하나로 변환돼 게이트웨이로 나가고, 결과는 기존 60 에코가, 실패는 기존 54 문구가 말한다.

Purpose: 17-07/17-11 의 LED 는 이 경로가 없으면 색만 그리는 장식이 된다. 전송 계층을 UI 앞 wave 에서 왕복 테스트까지 끝내 둔다.
Output: `RelayLcArmSchema`, `buildArmLatchReq`, fanout `lc.arm` 분기, fake-gateway 요청 리더, ws 왕복 테스트.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gh-trade-led/17-CONTEXT.md
@.planning/phases/17-gh-trade-led/17-RESEARCH.md
@.planning/phases/17-gh-trade-led/17-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: `lc.arm` 매도 래치 한 경로 — zod → 조립기 → 게이트웨이 → 60 에코</name>
  <files>relay/src/ws/protocol.ts, relay/src/dma/envelope.ts, relay/src/ws/fanout.ts, relay/tests/helpers/fake-gateway.ts, relay/tests/ws-latch.test.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/relay/src/ws/protocol.ts` 104-160 (`RelayLcSetSchema` · `RelayViSetSchema`) · 231-245 (`RelayInboundSchema` 유니온)
    - `/Users/alex/repos/gh-radar/relay/src/ws/fanout.ts` 598-670 (`lc.set` · `vi.set` 분기 — `#strategySession` · `#accountAllowed` · `#buildStrategyPayload` · `#onStrategySendFailed` 4단 형식이 여기 있다)
    - `/Users/alex/repos/gh-radar/relay/src/dma/envelope.ts` 1170-1200 (`buildBareRequest` · `buildGetVITriggerReq`) 와 930-1030 (`buildSetLimitChaserReq` — `startXxx`/`addXxx`/`endXxx` 개별 호출 규율)
    - `/Users/alex/repos/gh-radar/relay/tests/helpers/fake-gateway.ts` 221-300 (`readQuoteRequestKey` · `readViSetRequest` — 요청 리더가 여기 있는 이유의 주석 포함)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §1-1 36/37/38 행 · §4 Pitfall 2 · Pitfall 3
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-04
    - 읽기 전용 정본: `/Users/alex/repos/gh-trade/client/Services/DMA/Client.cs` `SendArmSellLatch`(:785 부근 — 빈 키 송신 취소 · pending FIFO 미투입)
  </read_first>
  <behavior>
    - `{t:"lc.arm", key:"KR7005930003:1234567801:KRX", latch:"sell"}` → 게이트웨이가 msg_type 36 을 받고 `get_strategy_req.key` 가 그 문자열이다
    - 같은 요청의 `latch:"cancel"` → msg_type 37 · `latch:"buy"` → msg_type 38
    - `latch` 가 세 값 밖(`"price"`)이면 zod 가 거부하고 게이트웨이로 아무것도 나가지 않는다
    - `key` 가 빈 문자열이면 zod 가 거부하고 게이트웨이로 아무것도 나가지 않는다
    - 게이트웨이가 60 에코를 돌려주면 브라우저는 기존 `{t:"lc", item}` 프레임을 받는다 — 새 `t` 값이 생기지 않는다
  </behavior>
  <action>
① `relay/src/ws/protocol.ts` 에 `RelayLcArmSchema` 를 더한다: `t: z.literal("lc.arm")`, `key: z.string()`(형식은 아래 ② 에서 판정), `latch: z.enum(["sell","cancel","buy"])`. `key` 는 **빈 문자열을 거부**한다(`min(1)`) — 빈 키는 서버가 「등록된 상따 전략이 없습니다」로 거부하므로 보내는 것 자체가 낭비이고, C# 도 송신을 취소한다. `RelayInboundSchema` 유니온에 더한다.

② `relay/src/dma/envelope.ts` 에 `buildArmLatchReq(msgType: number, key: string): Uint8Array` 를 더한다. **`buildBareRequest` 를 쓰지 않는다** — 36/37/38 은 `get_strategy_req` 슬롯을 실제로 읽으며, 비워 보내면 서버가 「등록된 상따 전략이 없습니다」로 거부한다(Pitfall 2). 조립 순서: `builder.createString(key)` 를 **테이블 빌더를 열기 전에** 만들고 → `GetStrategyReq.startGetStrategyReq` + `addKey` + `end` → `Envelope.startEnvelope` + `addMsgType` + `addGetStrategyReq` + `end` + `finish`(16-RESEARCH Pitfall 1·2 — 위치 인자 생성 함수와 중첩 문자열 금지). `msgType` 은 `MSG.ArmSellLatchReq`/`ArmCancelLatchReq`/`ArmBuyLatchReq` 중 하나만 받도록 타입을 좁힌다.

③ `relay/src/ws/fanout.ts` 의 인바운드 분기에 `if (msg.t === "lc.arm")` 를 더한다. 순서는 `lc.set` 바로 뒤. 본문은 `lc.set` 4단 형식을 그대로 따른다: `#strategySession` → (키 분해 후) `#accountAllowed` → `#buildStrategyPayload(… () => buildArmLatchReq(msgType, msg.key))` → `session.send` 실패 시 `#onStrategySendFailed`. **`lc.set` 의 ②-1 시장 해석과 ②-2 무장 가드는 적용하지 않는다** — 래치 요청에는 `market` 도 게이트 값도 없고, 무장 전제는 서버가 판정해 한글 사유로 거부한다.

④ latch → msgType 매핑은 **상수 객체 하나**로 두고 분기 안에 if 사슬을 만들지 않는다(값이 셋뿐이라 사슬이 늘면 갈린다).

⑤ **pending-key FIFO 에 넣지 않는다.** 응답이 본문에 키가 있는 60 에코라 귀속이 필요 없고, 넣으면 head 를 아무도 꺼내지 않아 다음 빈 응답이 옛 키로 귀속되는 회귀가 생긴다(Pitfall 3).

⑥ `relay/tests/helpers/fake-gateway.ts` 에 `readArmLatchRequest(msgType: number, payload: Buffer): string | null` 을 더한다 — 36/37/38 프레임에서 `get_strategy_req.key` 를 꺼낸다. `readViSetRequest` 와 같은 자리·같은 이유로 여기 둔다.

⑦ `relay/tests/ws-latch.test.ts` 를 새로 만들고 `<behavior>` 5케이스를 단언한다. 왕복은 기존 `relay/tests/ws-order.test.ts` 의 ws 클라이언트 헬퍼 패턴을 따른다.

⑧ 커밋 메시지 초안: `feat(17-04): 상따 래치 수동 점등 경로 — lc.arm 인바운드 → ArmSellLatchReq(36)/ArmCancelLatchReq(37)/ArmBuyLatchReq(38)`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test -- ws-latch</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "buildArmLatchReq" relay/src/dma/envelope.ts relay/src/ws/fanout.ts</automated>
    <fails_when>어느 파일이든 카운트가 0 이면 실패 (조립기 미정의 또는 fanout 미결선)</fails_when>
  </verify>
  <acceptance_criteria>
    - `relay/src/ws/protocol.ts` 에 `RelayLcArmSchema` 가 있고 `RelayInboundSchema` 유니온에 들어 있다.
    - `relay/src/dma/envelope.ts` 에 `export function buildArmLatchReq(` 가 1회 있다.
    - `buildArmLatchReq` 본문이 `buildBareRequest` 를 호출하지 않는다: `sed -n '/export function buildArmLatchReq/,/^}/p' relay/src/dma/envelope.ts | grep -c buildBareRequest` 가 `0` 이다.
    - `buildArmLatchReq` 본문에 `createString` 호출이 `startGetStrategyReq` 호출보다 **앞에** 나온다(줄 번호 비교로 확인).
    - `relay/src/ws/fanout.ts` 에 `msg.t === "lc.arm"` 분기가 있고 `#accountAllowed` 를 호출한다.
    - `relay/tests/helpers/fake-gateway.ts` 가 `readArmLatchRequest` 를 export 한다.
    - `relay/tests/ws-latch.test.ts` 가 36/37/38 각각의 msg_type 과 key 를 단언한다.
    - `lc.arm` 왕복에서 새 아웃바운드 `t` 값이 생기지 않는다 — 테스트가 받은 프레임의 `t` 가 `lc` 임을 단언한다.
  </acceptance_criteria>
  <done>브라우저 래치 클릭 1건이 게이트웨이의 36/37/38 요청으로 도달하고, 60 에코가 기존 경로로 돌아온다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 가드 3종 동형 확인 — Ready 세션 · key 형식 · 세션 계좌</name>
  <files>relay/src/ws/fanout.ts, relay/tests/ws-latch.test.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/relay/src/ws/fanout.ts` 의 `#strategySession` · `#accountAllowed` · `#allowInbound` 구현부 (grep 으로 위치를 잡고 본문을 읽는다)
    - `/Users/alex/repos/gh-radar/relay/src/dma/envelope.ts` 의 `strategyKey()` (키 조립 정본 — `ISIN:accountNo:exchange`)
    - `/Users/alex/repos/gh-radar/relay/tests/ws-order.test.ts` (거부 경로 단언 형식)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-04
  </read_first>
  <behavior>
    - DMA 세션이 Ready 가 아닌 사용자의 `lc.arm` → 게이트웨이로 아무것도 나가지 않고 거부가 기록된다
    - `key` 가 `ISIN:accountNo:exchange` 3토막이 아니면(`"KR7005930003"`) 거부되고 나가지 않는다
    - `key` 의 ISIN 이 12자가 아니면 거부된다
    - `key` 의 `accountNo` 가 세션 허용 계좌에 없으면 거부되고 나가지 않는다
    - `key` 의 exchange 가 `KRX`/`NXT` 밖이면 거부된다
    - 인바운드 상한(`#allowInbound`)이 `lc.arm` 에도 적용된다 — 같은 버킷을 쓴다
  </behavior>
  <action>
① `lc.arm` 의 `key` 를 `ISIN:accountNo:exchange` 로 **분해하는 헬퍼 하나**를 두고(파싱이 두 벌이 되지 않게), 분해 실패·ISIN 길이·거래소 값 3종을 한 자리에서 판정한다. 성공 시 `accountNo` 를 `#accountAllowed` 에 넘긴다 — `lc.set` 이 `cfg.accountNo` 로 하는 것과 **같은 함수**를 부른다(가드가 두 벌이 되면 한쪽만 고쳐진다).

② `#allowInbound` 는 `#onAuthedMessage` 맨 앞에서 이미 `msg.t` 로 불리므로 추가 작업이 없다 — 다만 `lc.arm` 이 별도 버킷을 쓰지 않는지 확인하고 SUMMARY 에 한 줄로 남긴다(경로마다 버킷이 갈리면 합계 상한이 없는 것과 같다, T-16-06).

③ 거부 시 로그에 **계좌번호를 싣지 않는다**(T-16-45) — `latch` 와 사유만 남긴다. `lc.set` 의 거부 로그 형식을 그대로 따른다.

④ `relay/tests/ws-latch.test.ts` 에 `<behavior>` 6케이스를 단언한다. 각 거부 케이스는 「게이트웨이로 나간 프레임 0」을 함께 단언한다(거부 로그만 보면 실제로 안 나갔는지 모른다).

⑤ 커밋 메시지 초안: `feat(17-04): lc.arm 가드 3종을 lc.set 과 동형으로 — Ready 세션·전략 키 형식·세션 계좌`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests</automated>
    <fails_when>비영 종료코드, 또는 tsc 출력에 `error TS` 가 나오면 실패</fails_when>
  </verify>
  <acceptance_criteria>
    - `lc.arm` 분기가 `#accountAllowed` 를 호출한다(`lc.set` 과 같은 메서드).
    - 키 분해 헬퍼가 함수 하나이고 `lc.arm` 분기에서만 호출된다.
    - 6개 거부 케이스 각각에 「게이트웨이 수신 프레임 0」 단언이 붙어 있다.
    - 거부 로그 호출에 계좌번호 변수가 실리지 않는다: `lc.arm` 거부 로그 객체 리터럴에 `accountNo` 키가 없다.
    - relay 테스트 · typecheck · typecheck:tests green.
  </acceptance_criteria>
  <done>래치 요청이 `lc.set` 과 같은 관문을 지난다. 가드가 두 벌이 아니다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 실패 경로 회귀 — 54 한글 사유 그대로 · 별도 ack 없음 · FIFO 무오염</name>
  <files>relay/tests/ws-latch.test.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/relay/src/hub/subscription-hub.ts` `case MSG.ServerMessage:` 분기 (해석 없이 흘린다는 D-36 규율)
    - `/Users/alex/repos/gh-radar/relay/tests/helpers/frames.ts` 187-215 (`buildServerMessageFrame`)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` `<specifics>` 의 **서버 거부 문구 8종 원문** (테스트 픽스처로 그대로 쓴다)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §4 Pitfall 3
  </read_first>
  <behavior>
    - 게이트웨이가 `ServerMessage(54)` WARN 으로 `등록된 상따 전략이 없습니다 — 매도 래치를 켤 수 없습니다` 를 보내면 브라우저는 기존 `{t:"msg"}` 프레임을 그 문구 그대로 받는다 (relay 가 문구를 고치지 않는다)
    - 같은 왕복에서 `lc.arm` 전용 ack 프레임은 나가지 않는다
    - `lc.arm` 을 3회 보낸 뒤 빈 `SetVITriggerResp(61)` 이 와도 옛 키로 귀속되지 않는다 — pending FIFO 가 오염되지 않았다
  </behavior>
  <action>
① `relay/tests/ws-latch.test.ts` 에 거부 문구 왕복 케이스를 더한다. CONTEXT `<specifics>` 의 8종 문구 중 36·37·38 각 1건씩 3건을 픽스처로 쓴다. **relay 가 문구를 해석·치환하지 않음**을 문자열 동등 비교로 단언한다.

② 같은 테스트에서 `lc.arm` 왕복 중 브라우저가 받은 프레임의 `t` 집합에 `lc` 와 `msg` 말고 새 값이 없음을 단언한다(별도 ack 프레임 부재).

③ FIFO 무오염 회귀: `lc.arm` 3연타 뒤 게이트웨이가 **빈 61**(VI 미등록)을 보냈을 때 그 응답이 VI 조회 FIFO 에서 정상 귀속되는지 단언한다. `lc.arm` 이 FIFO 에 무언가를 넣었다면 이 단언이 깨진다(Pitfall 3 의 직접 그물). 17-05 가 21 을 2회 보내도록 바꾸기 **전**의 현행 동작을 기준으로 단언한다.

④ 커밋 메시지 초안: `test(17-04): 래치 거부 문구 왕복·ack 부재·pending FIFO 무오염 회귀`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test -- ws-latch</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
  </verify>
  <acceptance_criteria>
    - `relay/tests/ws-latch.test.ts` 에 gh-trade 서버 거부 문구 3건이 리터럴로 들어 있고 동등 비교로 단언된다.
    - 「브라우저가 받은 `t` 집합에 새 값 없음」 단언이 있다.
    - `lc.arm` 3연타 뒤 빈 61 귀속 회귀 단언이 있다.
    - relay ws-latch 테스트 green.
  </acceptance_criteria>
  <done>래치 실패가 사용자에게 서버 문구 그대로 도달하고, 성공 경로에 잉여 프레임이 없으며, 기존 FIFO 가 오염되지 않았음이 테스트로 굳었다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (17-04)

**zod 스키마:** `RelayLcArmSchema` (`relay/src/ws/protocol.ts`)

**함수:** `buildArmLatchReq(msgType, key)` (`relay/src/dma/envelope.ts`) · `readArmLatchRequest(msgType, payload)` (`relay/tests/helpers/fake-gateway.ts`) · `lc.arm` 키 분해 헬퍼 1종 (`relay/src/ws/fanout.ts`)

**인바운드 `t` 값:** `lc.arm`

**신규 파일:** `relay/tests/ws-latch.test.ts`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → relay wss (`lc.arm`) | 신뢰하지 않는 입력. `key` 안의 계좌번호가 그대로 발주 계좌를 가리킨다 |
| relay → 게이트웨이 (36/37/38) | 래치 ON 은 **발주 판정을 시작**시킨다 — 잘못된 키로 나가면 남의 전략을 무장시킨다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-11 | Elevation of Privilege | `lc.arm` 의 `key` 안 accountNo | critical | mitigate | `#accountAllowed` 를 `lc.set` 과 **같은 메서드**로 호출. 6개 거부 케이스에 「나간 프레임 0」 단언 |
| T-17-12 | Spoofing | Ready 아닌 세션의 요청 | high | mitigate | `#strategySession` 게이트 — `lc.set` 동형 |
| T-17-13 | Denial of Service | `lc.arm` 연타 | medium | mitigate | 기존 `#allowInbound` 버킷 공유(별도 버킷 금지) |
| T-17-14 | Tampering | pending-key FIFO 오염 | high | mitigate | FIFO 미투입 + 빈 61 귀속 회귀 테스트 |
| T-17-15 | Information Disclosure | 거부 로그의 계좌번호 | medium | mitigate | 로그 객체에 accountNo 미포함(T-16-45) |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 plan 은 패키지 설치가 없다 |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/relay test` · `run typecheck` · `run typecheck:tests` green
- 36/37/38 각각의 msg_type·key 왕복 단언 통과
- 거부 6케이스에서 게이트웨이 수신 프레임 0
</verification>

<success_criteria>
- `lc.arm` 이 세 MsgType 으로 정확히 변환돼 나간다.
- 가드가 `lc.set` 과 동형이고 두 벌이 아니다.
- 실패가 서버 한글 문구 그대로 브라우저에 닿고 잉여 ack 프레임이 없다.
</success_criteria>

<output>
Create `.planning/phases/17-gh-trade-led/17-04-SUMMARY.md` when done
</output>