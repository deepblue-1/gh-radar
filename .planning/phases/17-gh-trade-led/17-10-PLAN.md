---
phase: 17-gh-trade-led
plan: 10
type: execute
wave: 7
depends_on: ["17-06", "17-08"]
files_modified:
  - webapp/src/lib/order-notices.ts
  - webapp/src/lib/__tests__/order-notices.test.ts
  - webapp/src/lib/orders-api.ts
  - webapp/src/components/trading/today-orders-card.tsx
  - webapp/src/components/trading/__tests__/today-orders-card.test.tsx
autonomous: true
requirements: [TRADE-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "주문 통보의 행위 단어가 `notice_type` → `request_kind` → side 순으로 정해지고, 어디서도 `message` 문구를 읽지 않는다 (D-08 · D-15)"
    - "`requester === \"Manual\"` 인 통보에 「수동」 메타가 붙고, `board` 가 G2/G3 면 시간외종가 접두가 붙는다 (D-15)"
    - "자동주문 체결과 자동주문 매도 접수가 3초 창으로 묶여 한 줄이 된다 — 매수 접수·거부·취소·정정은 묶이지 않는다 (D-16)"
    - "묶기는 순수함수 하나(`mergeOrderNotices`)가 하고 표면은 그것을 호출만 한다 (D-16)"
  artifacts:
    - webapp/src/lib/order-notices.ts
    - webapp/src/lib/__tests__/order-notices.test.ts
  key_links:
    - "RelayOrderMsg(nt/rk/rq/bd) → orderActionWord() → 오늘 주문 표 행위 단어"
    - "mergeOrderNotices(3초 창) → today-orders-card 행 목록"
---

<objective>
주문 통보가 **무엇을 한 통보인지**(접수·체결·취소·정정·거부), **누가 낸 주문인지**(수동/자동), **어느 장에서**(정규장/시간외종가) 인지를 서버 필드로 말하게 하고, 부분체결 조각 매도로 쏟아지는 통보를 3초 창으로 묶어 읽을 수 있게 만든다.

Purpose: 한방·조각 매도(quick-260916-fq3)에서 통보가 조각 수만큼 쏟아져 표가 한 종목으로 가득 찬다. 동시에 정정·취소 거부(804)는 서버가 `message` 를 교체하므로 문구로 행위를 판정하면 조용히 틀린다.
Output: 행위 단어 순수함수, 3초 창 묶기 순수함수, 오늘 주문 표 적용.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gh-trade-led/17-CONTEXT.md
@.planning/phases/17-gh-trade-led/17-RESEARCH.md
@.planning/phases/17-gh-trade-led/17-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 행위 단어 순수함수 + 오늘 주문 표 적용</name>
  <files>webapp/src/lib/order-notices.ts, webapp/src/lib/__tests__/order-notices.test.ts, webapp/src/components/trading/today-orders-card.tsx</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/today-orders-card.tsx` 95-150 (`stockLabel` · `mergeTodayOrders` 소비 · 복원 로직) 와 행 렌더 JSX(행위 단어가 현재 어디서 오는지 grep 으로 실측)
    - `/Users/alex/repos/gh-radar/webapp/src/lib/orders-api.ts` (`mergeTodayOrders` · `DmaOrderRow` 형태)
    - `/Users/alex/repos/gh-radar/packages/shared/src/relay.ts` (`RelayOrderMsg` — `nt`/`rc`/`bd`/`rk`/`rq`. **`side`·`isin` 이 없다**)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-08 · D-15
    - 읽기 전용 정본: `/Users/alex/repos/gh-trade/client/Services/DMA/NotificationHub.cs` `ActionWord`(:466 부근)
  </read_first>
  <behavior>
    orderActionWord({ noticeType, requestKind, side }):
    - `noticeType === "C"` → `취소` (requestKind 와 무관)
    - `noticeType === "M"` → `정정`
    - `noticeType` 이 그 밖이고 `requestKind === "Cancel"` → `취소`
    - `noticeType` 이 그 밖이고 `requestKind === "Modify"` → `정정`
    - 그 밖 → side 단어(`매수`/`매도`)
    - side 를 모르고 위 분기에도 안 걸리면 빈 문자열(지어내지 않는다)
    방향색:
    - `취소`/`정정` 행은 방향색(빨강/파랑)을 쓰지 않는다
    메타:
    - `requester === "Manual"` → 「수동」 메타가 붙는다. 그 밖(빈 값 포함)에는 안 붙는다
    접두:
    - `board` 가 `"G2"`/`"G3"` 이고 행위가 접수·체결·거부 → `시간외종가 매수` 처럼 side 단어 앞에 붙는다
    - `board` 가 `"G2"`/`"G3"` 이고 행위가 취소·정정 → `시간외종가` 만 붙고 side 단어는 없다
  </behavior>
  <action>
① `webapp/src/lib/order-notices.ts` 를 새로 만들고 `orderActionWord` 와 접두·메타를 조립하는 순수함수를 둔다(이름·시그니처 재량). 위 `<behavior>` 표 그대로. **`message` 를 인자로 받지 않는다** — 받을 수 있게 두면 언젠가 읽는다(D-08 · 804 거부 문구 교체).

② `side` 원천을 실측한다: `RelayOrderMsg` 에는 `side` 가 없고(Pitfall 8 규율), 오늘 주문 표는 복원된 `DmaOrderRow` 와 relay 프레임을 병합한다. **복원 행의 side 를 쓰고 프레임에서 지어내지 않는다.** 병합되지 않은(복원에 없는) 프레임이면 side 를 모르는 것이고, 그때는 빈 문자열이다. 실측 결과를 SUMMARY 에 기록한다.

③ `today-orders-card.tsx` 의 행 렌더가 이 함수를 쓰게 한다. 취소·정정 행에서 방향색 클래스가 적용되지 않게 한다.

④ `<behavior>` 10케이스를 `webapp/src/lib/__tests__/order-notices.test.ts` 에 단언한다. 특히 `noticeType === "C"` 와 `requestKind === "New"` 가 **동시에** 온 경우 `취소` 가 이기는 케이스(우선순위)를 둔다.

⑤ 커밋 메시지 초안: `feat(17-10): 주문통보 행위 단어를 notice_type·request_kind 로 판정 + 수동·시간외종가 표기`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- order-notices</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "orderActionWord" webapp/src/components/trading/today-orders-card.tsx</automated>
    <fails_when>카운트가 0 이면 실패 (행위 단어가 표면에 적용되지 않았다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `webapp/src/lib/order-notices.ts` 가 `orderActionWord` 를 export 한다.
    - `orderActionWord` 의 인자에 메시지 문구가 없다: 함수 시그니처에 `message` 나 `msg` 키가 없다.
    - `today-orders-card.tsx` 가 그 함수를 쓰고, 취소·정정 행에 방향색 클래스가 붙지 않는 단언이 테스트에 있다.
    - 우선순위 케이스(`noticeType "C"` vs `requestKind "New"`)가 테스트에 있다.
    - side 원천 실측 결과가 SUMMARY 에 기록됐다.
    - order-notices 테스트 green.
  </acceptance_criteria>
  <done>통보 한 줄이 무엇을 한 통보인지 서버 필드로 말한다. 문구를 읽는 경로가 없다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `mergeOrderNotices` — 3초 창 묶기 순수함수</name>
  <files>webapp/src/lib/order-notices.ts, webapp/src/lib/__tests__/order-notices.test.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/lib/orders-api.ts` (`mergeTodayOrders` — 기존 병합 순수함수의 형태·반환 구조)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-16 (묶는 것·안 묶는 것의 정확한 집합)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §4 Pitfall 8 (매수 접수를 묶으면 안 되는 이유)
    - 읽기 전용 정본: `/Users/alex/repos/gh-trade/client/Services/DMA/NotificationHub.cs` `MergeKeyOf`(:573 부근)
  </read_first>
  <behavior>
    - 같은 자동주문(`origin` 상따/VI ∧ `requester !== "Manual"`)의 **체결** 통보 3건이 3초 안에 오면 1행으로 묶인다
    - 묶인 행의 시각은 **첫 통보의 시각**이고, 주문번호는 `#첫번호~끝번호` 이며 `(3건)` 이 붙는다
    - 같은 자동주문의 **매도 접수** 2건은 묶인다
    - 같은 자동주문의 **매수 접수** 2건은 묶이지 않는다 (취소 직후 재매수가 합쳐지면 이미 취소된 수량이 더해져 보인다)
    - 거부(`R`) · 취소확인(`C`) · 정정확인(`M`) 은 묶이지 않는다
    - 수동주문(`requester === "Manual"`)과 주체 미상은 **주문번호**가 키라 서로 묶이지 않는다
    - 3초를 넘긴 4번째 체결은 새 행이 된다
    - `origin` 이 같아도 ISIN·거래소·side 중 하나라도 다르면 묶이지 않는다
  </behavior>
  <action>
① `webapp/src/lib/order-notices.ts` 에 `mergeOrderNotices(rows, windowMs = 3000)` 순수함수를 더한다. 입력은 오늘 주문 표가 이미 만든 행 배열(시각 내림차순 또는 오름차순 — 실측해 맞춘다), 출력은 묶인 행 배열이다.

② 묶기 키(C# `MergeKeyOf` 동형):
  - **체결** 이고 자동주문이면 `origin|ISIN|exchange|side`
  - **접수** 이고 자동주문이고 side 가 매도면 `origin|ISIN|exchange|side`
  - 그 밖(매수 접수 · 거부 · 취소확인 · 정정확인 · 수동 · 주체 미상 · 서버 줄)은 **주문번호** — 즉 묶이지 않는다
  키 계산을 분기 안에 흩뿌리지 않고 `mergeKeyOf(row)` 헬퍼 하나로 둔다.

③ 묶인 행의 표기: 시각은 첫 통보, 수량·금액은 합계, 주문번호는 `#` + 첫 번호 + `~` + 끝 번호, 꼬리에 `(N건)`. N 이 1 이면 묶임 표기를 붙이지 않는다(단건이 묶인 것처럼 보이지 않게).

④ **3초 창의 기준은 그 묶음의 첫 통보 시각**이다(슬라이딩 창이 아니다) — 슬라이딩이면 통보가 계속 오는 동안 한 행이 무한히 자란다.

⑤ `<behavior>` 8케이스를 `webapp/src/lib/__tests__/order-notices.test.ts` 에 단언한다. 시각은 고정 타임스탬프 픽스처로 넣고 `Date.now()` 에 의존하지 않는다(순수함수 유지).

⑥ 커밋 메시지 초안: `feat(17-10): 자동주문 통보 3초 창 묶기 순수함수 — 체결 전량·매도 접수만, 매수 접수는 제외`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- order-notices</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "Date.now()" webapp/src/lib/order-notices.ts</automated>
    <fails_when>출력이 `0` 이 아니면 실패 (순수함수가 현재 시각을 읽는다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `mergeOrderNotices` 와 `mergeKeyOf` 가 `webapp/src/lib/order-notices.ts` 에 있다.
    - 매수 접수가 묶이지 않는 케이스가 테스트에 있다(Pitfall 8 직접 그물).
    - 거부·취소확인·정정확인이 묶이지 않는 케이스가 각각 있다.
    - 3초 초과 통보가 새 행이 되는 케이스가 있다.
    - N === 1 일 때 묶임 표기가 붙지 않는 케이스가 있다.
    - `order-notices.ts` 에 `Date.now()` 호출이 0건이다.
    - order-notices 테스트 green.
  </acceptance_criteria>
  <done>조각 매도 통보가 한 줄로 읽히고, 묶으면 안 되는 것은 묶이지 않음이 8케이스로 굳었다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 오늘 주문 표에 묶기 적용 + 표면 회귀</name>
  <files>webapp/src/components/trading/today-orders-card.tsx, webapp/src/components/trading/__tests__/today-orders-card.test.tsx, webapp/src/lib/orders-api.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/today-orders-card.tsx` 130-175 (`mergeTodayOrders` 소비 · `unmatchedOrderNos` 재조회 루프 · `requestedRef`)
    - `/Users/alex/repos/gh-radar/webapp/src/lib/orders-api.ts` (`mergeTodayOrders` 반환 구조)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-16 (「표면이 하나가 아니면 순수함수 하나로 두고 각 표면이 호출」)
  </read_first>
  <behavior>
    - 오늘 주문 표가 묶인 행을 그린다
    - 묶기 **뒤에도** 미매칭 주문번호 재조회 루프가 원래 주문번호 전부를 본다 — 묶였다고 재조회가 빠지지 않는다
    - 복원 행(REST)만 있고 relay 프레임이 없으면 묶기가 아무것도 바꾸지 않는다
    - 묶인 행을 펼치는 UI 는 만들지 않는다(이번 phase 범위 밖)
  </behavior>
  <action>
① `today-orders-card.tsx` 가 `mergeTodayOrders` 결과에 `mergeOrderNotices` 를 이어 적용한다. **순서가 중요하다** — 복원·병합이 끝난 뒤에 묶는다. 묶기를 `mergeTodayOrders` 안으로 넣지 않는다(그 함수는 REST 복원과 프레임을 맞추는 다른 일을 한다).

② `unmatchedOrderNos` 재조회 루프는 **묶기 전 목록**을 본다. 묶인 뒤 목록을 보면 합쳐진 주문번호가 사라져 그 주문의 종목명이 영원히 복원되지 않는다.

③ 다른 표면이 같은 묶기를 필요로 하는지 grep 으로 확인한다(전략 로그·My page 등). 필요하면 **같은 순수함수를 호출**하고 두 번째 구현을 만들지 않는다. 확인 결과를 SUMMARY 에 기록한다.

④ `<behavior>` 4케이스를 `today-orders-card.test.tsx` 에 단언한다.

⑤ 커밋 메시지 초안: `feat(17-10): 오늘 주문 표에 통보 묶기 적용 (재조회 루프는 묶기 전 목록 유지)`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm typecheck</automated>
    <fails_when>비영 종료코드, 또는 출력에 `error TS` 가 나오면 실패</fails_when>
  </verify>
  <acceptance_criteria>
    - `today-orders-card.tsx` 가 `mergeOrderNotices` 를 호출한다.
    - `mergeTodayOrders` 본문에 묶기 로직이 들어가지 않았다: `webapp/src/lib/orders-api.ts` 에 `mergeOrderNotices` 호출이 0건이다.
    - 재조회 루프가 묶기 전 목록을 보는 것이 테스트로 단언된다.
    - 다른 표면 필요성 grep 결과가 SUMMARY 에 기록됐다.
    - webapp 테스트 + 루트 typecheck green.
  </acceptance_criteria>
  <done>오늘 주문 표가 조각 통보에 묻히지 않고, 종목명 복원 경로가 묶기 때문에 끊기지 않는다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (17-10)

**신규 파일:** `webapp/src/lib/order-notices.ts` · `webapp/src/lib/__tests__/order-notices.test.ts`

**함수:** `orderActionWord(...)` · `mergeKeyOf(row)` · `mergeOrderNotices(rows, windowMs = 3000)`

**UI 규칙:** 취소·정정 행 방향색 없음 · 「수동」 메타 · 「시간외종가」 접두 · 묶인 행 표기(`#첫번호~끝번호` · `(N건)` · 첫 시각 유지)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 통보 필드 → 사용자가 읽는 행위 단어 | 「취소」를 「매수」로 읽으면 사용자가 상태를 반대로 판단한다 |
| 묶기 → 수량 합계 표시 | 잘못 묶으면 이미 취소된 수량이 더해져 보인다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-33 | Spoofing | `message` 문구로 행위 판정 | high | mitigate | 함수 시그니처에서 문구 인자 제거 — 받을 수 없으면 읽을 수 없다 |
| T-17-34 | Tampering | 매수 접수 묶기 | high | mitigate | 묶기 키에서 매수 접수 제외 + 전용 회귀 테스트(Pitfall 8) |
| T-17-35 | Repudiation | 묶기로 사라진 주문번호의 종목명 미복원 | medium | mitigate | 재조회 루프가 묶기 전 목록을 본다 — 테스트로 단언 |
| T-17-36 | Tampering | 슬라이딩 창으로 한 행 무한 성장 | medium | mitigate | 창 기준을 묶음 첫 통보 시각으로 고정 + 3초 초과 새 행 테스트 |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 plan 은 패키지 설치가 없다 |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/webapp test` · `pnpm typecheck` green
- `order-notices.ts` 에 `Date.now()` 0건 (순수함수)
- `orders-api.ts` 에 묶기 호출 0건 (책임 분리)
</verification>

<success_criteria>
- 행위 단어가 서버 필드로만 정해진다.
- 조각 통보가 3초 창으로 묶이고 묶으면 안 되는 것은 그대로다.
- 묶기가 순수함수 하나이고 표면은 호출만 한다.
</success_criteria>

<output>
Create `.planning/phases/17-gh-trade-led/17-10-SUMMARY.md` when done
</output>