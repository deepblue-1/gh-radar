---
phase: 17-gh-trade-led
plan: 09
type: execute
wave: 7
depends_on: ["17-06", "17-08"]
files_modified:
  - webapp/src/components/orderbook/account-panel.tsx
  - webapp/src/components/trading/vi-client.tsx
  - webapp/src/components/orderbook/__tests__/account-panel.test.tsx
  - webapp/src/components/trading/__tests__/vi-client.test.tsx
autonomous: true
requirements: [TRADE-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "미체결 행의 매수/매도 표식이 예약(`매수Q`)·접수대기(`매수P`)·시간외종가(`매수/종가`) 접미를 보여 준다 (D-13)"
    - "접미 생성은 `sideDisplayText` 한 함수만 한다 — 두 표면 어디에도 인라인 접미 문자열이 없다 (D-13)"
    - "`pendingCancelSent === true` 행은 회색이고 취소 버튼이 없다. 판정 근거는 그 bool 하나다 (D-14)"
    - "`queuedStatus`/`pendingStatus` 문구는 서버가 준 그대로 표시만 된다 — 클라가 대기/발사중/완료를 다시 계산하지 않는다 (D-07)"
  artifacts:
    - webapp/src/components/orderbook/__tests__/account-panel.test.tsx
  key_links:
    - "RelayUnfilled(5필드) → sideDisplayText() → 미체결 표 표식"
    - "RelayUnfilled.pendingCancelSent → cancellable 판정 → 취소 버튼 가시성 (account-panel · vi-client 두 표면 동일 규칙)"
---

<objective>
미체결 목록이 서버가 아는 네 가지 사실(예약 요약 행 · 증권사 접수대기 · 시간외종가 · 취소 보관)을 그대로 보여 주게 한다. 클라이언트는 이 사실들을 **다시 계산하지 않는다** — 표시하고, 하나의 bool 로 취소를 감춘다.

Purpose: 지금 미체결 표는 예약 요약 행(Q-ID)과 일반 행을 구분하지 못하고, 서버가 이미 취소를 보낸 행에도 취소 버튼을 보여 준다. 그 버튼을 누르면 서버가 `R` 로 답하고 사용자는 첫 취소의 성패를 오해한다.
Output: 두 표면의 표식 헬퍼 적용, 취소보관 회색·버튼 숨김, 상태 문구 표시 경로.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gh-trade-led/17-CONTEXT.md
@.planning/phases/17-gh-trade-led/17-RESEARCH.md
@.planning/phases/17-gh-trade-led/17-01-SUMMARY.md
@.planning/phases/17-gh-trade-led/17-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 미체결 표식 — `sideDisplayText` 한 함수로 두 표면</name>
  <files>webapp/src/components/orderbook/account-panel.tsx, webapp/src/components/trading/vi-client.tsx, webapp/src/components/orderbook/__tests__/account-panel.test.tsx</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/components/orderbook/account-panel.tsx` 228-245 (`unfilled` useMemo · `cancellable` 판정) · 495-525 (미체결 행 렌더 · `SideTag` · `OriginTag`)
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/vi-client.tsx` 130-175 (`filteredUnfilled` · `panelAccount` · `cancellable`)
    - `/Users/alex/repos/gh-radar/packages/shared/src/strategy-display.ts` (`sideDisplayText` — 17-01 산출물, 접미 우선순위가 그 안에 있다)
    - `/Users/alex/repos/gh-radar/packages/shared/src/relay.ts` (`RelayUnfilled` 5필드 주석)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-13
  </read_first>
  <behavior>
    - 주문번호가 10자 Q-ID 인 매수 미체결 → 표식이 `매수Q`
    - `pendingStatus` 가 비어 있지 않은 매도 미체결 → 표식이 `매도P`
    - `board` 가 `"G2"` 인 매수 미체결 → 표식이 `매수/종가`
    - 셋 다 아닌 행 → 표식이 `매수` 또는 `매도` (종전과 같다)
    - 같은 행을 `account-panel` 과 `vi-client` 미체결 표에서 보면 **표식 문자열이 같다**
  </behavior>
  <action>
① `account-panel.tsx` 의 미체결 행에서 side 표시를 `sideDisplayText(row.side, row.orderNo, row.pendingStatus, row.board)` 결과로 그린다. 기존 `SideTag` 컴포넌트를 쓰되 텍스트를 이 함수에서 받게 하거나, `SideTag` 가 접미를 받는 prop 을 갖게 한다 — **어느 쪽이든 접미 문자열을 컴포넌트 안에서 조립하지 않는다**(D-13).

② `OriginTag` 는 그대로 둔다 — 그것은 주문 **주체**(manual/limit_chaser/vi)이고 이 표식은 주문 **형태**다. 두 축을 합치지 않는다.

③ `vi-client.tsx` 의 미체결 표도 같은 함수를 쓴다. `vi-client` 가 `account-panel` 에 행을 넘기는 구조라면(현재 `panelAccount` 로 필터만 걸어 넘긴다) 표식은 `account-panel` 쪽 한 곳에서 나오므로 중복 작업이 없다 — **실측해서 확인하고** 결과를 SUMMARY 에 기록한다. `vi-client` 가 자체 표를 그리는 자리가 따로 있으면 거기에도 같은 함수를 쓴다.

④ `<behavior>` 5케이스를 단언한다. 두 표면 표식 일치는 같은 픽스처 행으로 각각 렌더해 문자열을 비교한다.

⑤ 커밋 메시지 초안: `feat(17-09): 미체결 표식을 sideDisplayText 한 함수로 — 예약·접수대기·시간외종가 접미`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- account-panel</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "sideDisplayText" webapp/src/components/orderbook/account-panel.tsx</automated>
    <fails_when>카운트가 0 이면 실패 (표식 헬퍼가 쓰이지 않았다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `account-panel.tsx` 가 `sideDisplayText` 를 import 해 미체결 행에 쓴다.
    - 두 파일 어디에도 접미 리터럴이 조립되지 않는다: `grep -cE '"Q"|"P"|"/종가"' webapp/src/components/orderbook/account-panel.tsx webapp/src/components/trading/vi-client.tsx` 가 두 파일 모두 `0` 이다.
    - `OriginTag` 렌더가 그대로 남아 있다.
    - 두 표면 표식 일치 단언이 테스트에 있다.
    - `vi-client` 의 미체결 표 구조 실측 결과가 SUMMARY 에 기록됐다.
    - webapp 테스트 green.
  </acceptance_criteria>
  <done>미체결 행이 자기 형태를 말하고, 그 문자열을 만드는 자리가 저장소에 하나뿐이다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 취소보관 행 — 회색 · 취소 버튼 숨김 (bool 하나가 유일 근거)</name>
  <files>webapp/src/components/orderbook/account-panel.tsx, webapp/src/components/trading/vi-client.tsx, webapp/src/components/trading/__tests__/vi-client.test.tsx</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/components/orderbook/account-panel.tsx` 228-245 (`cancellable` 판정) · 262-305 (`handleCancelConfirmed` · `lockedOrderNos` · `catch` 가 없는 이유 주석)
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/vi-client.tsx` 148-175 (`cancellable` 목록 · 전체 취소 경로)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-14 · D-07 (relay 는 거부하지 않는다 — 화면이 감추는 것으로 충분)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §4 Pitfall 6
  </read_first>
  <behavior>
    - `pendingCancelSent === true` 인 행 → 글자가 회색이고 취소 버튼이 렌더되지 않는다
    - `pendingCancelSent === true` 이고 `unfilledQty > 0` 이어도 취소 버튼이 없다
    - `pendingCancelSent === false` 이고 `unfilledQty > 0` → 종전대로 취소 가능
    - `vi-client` 의 「전체 취소」 대상 목록에서 `pendingCancelSent === true` 행이 빠진다
    - `pendingStatus` 문구가 무엇이든 회색·숨김 판정이 바뀌지 않는다 (문구 비교 없음)
  </behavior>
  <action>
① `account-panel.tsx` 의 `cancellable` 판정에 `&& !row.pendingCancelSent` 를 더한다. 행 스타일에 회색 처리를 더한다(`text-[var(--muted-fg)]` — 새 토큰을 만들지 않는다).

② `vi-client.tsx` 의 `cancellable` 목록(전체 취소 대상)에서도 같은 행을 뺀다. **두 곳의 판정이 같은 식이어야 한다** — 한쪽만 고치면 개별 취소는 막히는데 전체 취소는 나가는 상태가 된다.

③ **판정 근거는 `pendingCancelSent` bool 하나다.** `pendingStatus`/`queuedStatus` 문구를 보고 회색이나 숨김을 정하지 않는다(gh-trade 교훈 24 · Pitfall 6 — 문구는 서버가 바꾼다).

④ 회색 행에 **왜 취소할 수 없는지**가 보이게 한다: `pendingStatus` 문구를 그 행의 보조 텍스트나 툴팁으로 노출한다(문구는 그대로 표시만). 버튼이 그냥 사라지면 사용자는 버그로 읽는다.

⑤ `<behavior>` 5케이스를 두 테스트 파일에 나눠 단언한다.

⑥ 커밋 메시지 초안: `feat(17-09): 취소보관(pending_cancel_sent) 행 회색·취소 버튼 숨김 — 개별·전체 취소 두 경로 동일 규칙`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "pendingCancelSent" webapp/src/components/orderbook/account-panel.tsx webapp/src/components/trading/vi-client.tsx</automated>
    <fails_when>어느 파일이든 카운트가 0 이면 실패 (두 취소 경로 중 하나가 규칙을 안 따른다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `account-panel.tsx` 와 `vi-client.tsx` 의 취소 대상 판정에 `pendingCancelSent` 가 각각 들어 있다.
    - 두 파일에서 `pendingStatus` 를 조건식에 쓰지 않는다: `grep -cE 'pendingStatus\s*(===|!==|\.includes)' webapp/src/components/orderbook/account-panel.tsx webapp/src/components/trading/vi-client.tsx` 가 두 파일 모두 `0` 이다.
    - 회색 행이 `--muted-fg` 를 쓰고 이 plan 의 `files_modified` 에 `webapp/src/styles/globals.css` 가 없다(새 CSS 토큰 0건).
    - 회색 행에 `pendingStatus` 문구가 보조 텍스트나 툴팁으로 노출된다.
    - `<behavior>` 5케이스가 테스트에 있다.
    - webapp 테스트 green.
  </acceptance_criteria>
  <done>서버가 이미 취소를 보낸 행에 취소 버튼이 없고, 사용자는 그 이유를 서버 문구로 본다. 개별·전체 두 경로가 같은 규칙을 쓴다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 예약·접수대기 상태 문구 표시 (표시만, 재계산 금지)</name>
  <files>webapp/src/components/orderbook/account-panel.tsx, webapp/src/components/orderbook/__tests__/account-panel.test.tsx</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/components/orderbook/account-panel.tsx` 미체결 표 헤더·셀 구조 (열 수와 폭 배분)
    - `/Users/alex/repos/gh-radar/webapp/src/styles/globals.css` §2.2b 의 「앱 전체(셸·사이드바·미체결/잔고 패널)는 **뷰포트 브레이크포인트**를 그대로 쓴다」 문단
    - `/Users/alex/repos/gh-radar/tasks/lessons.md` 「그리드/플렉스 자식은 `min-w-0` 없으면 조용히 잘린다」 절
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-07 · D-13 · `<deferred>` (「`UnfilledState.order_time` 을 '시간' 열로 노출하는 것은 재량」)
  </read_first>
  <behavior>
    - `queuedStatus` 가 `"발사완료 미발주 3주"` 인 행 → 그 문구가 화면 어딘가에 **그대로** 보인다
    - `pendingStatus` 가 `"증권사 보관 · 09:00 처리"` 인 행 → 그 문구가 그대로 보인다
    - 두 값이 모두 빈 행 → 아무 보조 표시도 추가되지 않는다(표가 두꺼워지지 않는다)
    - 클라이언트가 `"대기"` / `"발사중"` / `"완료"` 를 자체 계산해 넣는 코드가 없다
  </behavior>
  <action>
① `queuedStatus` 와 `pendingStatus` 문구를 미체결 표에 노출한다. 방식은 재량이되 **표 폭을 깨지 않는 것**이 조건이다 — 권장은 행의 종목 셀 아래 보조 줄 또는 표식 옆 툴팁이다. 새 열을 추가한다면 폰 폭에서 표가 잘리지 않는지 확인하고, 잘리면 보조 줄로 낮춘다.

② **문구를 가공하지 않는다.** 자르기·치환·상태 분류 없이 서버 문자열 그대로. 「대기/발사중/완료」류 단어를 클라이언트가 만들어 내지 않는다(D-07).

③ `orderTime` 열 추가는 **하지 않는다** — CONTEXT `<deferred>` 가 재량으로 두었고, 이 표는 이미 7열이라 폰 폭에서 여유가 없다. 파서에는 값이 있으므로 나중에 열을 더할 수 있다는 것만 SUMMARY 에 기록한다.

④ 미체결/잔고 패널은 **뷰포트 브레이크포인트**를 쓴다(§2.2b) — 여기에 컨테이너 쿼리를 새로 도입하지 않는다.

⑤ `<behavior>` 4케이스를 단언한다.

⑥ 커밋 메시지 초안: `feat(17-09): 미체결 예약·접수대기 서버 문구 표시 (가공 없이 그대로)`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- account-panel</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm typecheck</automated>
    <fails_when>비영 종료코드, 또는 출력에 `error TS` 가 나오면 실패</fails_when>
  </verify>
  <acceptance_criteria>
    - `queuedStatus` 와 `pendingStatus` 가 각각 렌더 경로에 나타난다.
    - 두 값이 빈 행에 보조 표시가 붙지 않는 케이스가 테스트에 있다.
    - `account-panel.tsx` 에 `container-type` 또는 `@min-[` 컨테이너 쿼리 유틸이 새로 생기지 않았다.
    - `orderTime` 열이 추가되지 않았고, 미사용 사실이 SUMMARY 에 기록됐다.
    - 문구 가공 코드가 없다 — `queuedStatus`/`pendingStatus` 에 `.slice(`·`.replace(`·`.split(` 호출이 0건이다.
    - webapp 테스트 + 루트 typecheck green.
  </acceptance_criteria>
  <done>서버가 보낸 예약·접수대기 문구가 사용자에게 그대로 닿고, 클라이언트가 상태를 다시 짓지 않는다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (17-09)

**UI 규칙:** 미체결 표식(`매수Q`/`매수P`/`매수/종가`) · 취소보관 행 회색 + 취소 버튼 숨김(개별·전체 두 경로) · 예약/접수대기 서버 문구 표시

**소비:** `sideDisplayText()`(17-01 산출물)의 첫 소비처 2곳
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 미체결 원장 → 취소 버튼 가시성 | 잘못 보이는 취소 버튼은 이미 나간 취소를 두 번 내게 만든다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-30 | Repudiation | 취소보관 행의 중복 취소 | high | mitigate | `pendingCancelSent` 로 버튼 숨김 + 개별·전체 두 경로 동일 판정 게이트 |
| T-17-31 | Tampering | 문구 기반 취소 제외 판정 | high | mitigate | `pendingStatus` 조건식 grep 0 게이트(교훈 24 · Pitfall 6) |
| T-17-32 | Spoofing | 클라가 지어낸 상태 단어 | medium | mitigate | 문구 가공 호출 0 게이트 |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 plan 은 패키지 설치가 없다 |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/webapp test` · `pnpm typecheck` green
- §2.2b 밴드 경계 리터럴(`700px`·`830px`·`992px`) 보존
- 두 취소 경로(개별·전체)에서 취소보관 행 제외 단언 통과
</verification>

<success_criteria>
- 미체결 표식이 한 함수에서만 나온다.
- 취소보관 행이 회색이고 두 취소 경로 모두에서 빠진다.
- 서버 상태 문구가 가공 없이 보인다.
</success_criteria>

<output>
Create `.planning/phases/17-gh-trade-led/17-09-SUMMARY.md` when done
</output>