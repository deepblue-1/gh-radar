---
phase: 17-gh-trade-led
plan: 12
type: execute
wave: 8
depends_on: ["17-09", "17-10", "17-11"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [TRADE-04, TRADE-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "로컬 mock 게이트웨이(서버 HEAD)와 dev 웹앱으로 76/77/78 드롭 0 과 LED 클릭 왕복이 실제로 관측됐다 (D-25)"
    - "전량 게이트(relay test·typecheck·typecheck:tests · webapp test·typecheck · 루트 typecheck)가 green 이다 (D-24)"
    - "TRADE-04 · TRADE-05 가 REQUIREMENTS.md 정의부와 Traceability 양쪽에서 같은 판정으로 기록됐다"
    - "배포는 사용자 확인 뒤, 장 마감(20:00 KST) 이후에만 이뤄진다 (D-26)"
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  key_links:
    - "mock 게이트웨이(gh-trade server/scripts/run-mac.sh) → relay dev → webapp dev(PORT 3100) → 브라우저"
    - "scripts/deploy-relay.sh → VM radar-gw → scripts/smoke-relay.sh"
---

<objective>
Phase 17 의 코드가 **실제 서버 HEAD 와 말이 통하는지** 로컬 mock 게이트웨이로 확인하고, 전량 게이트를 돌리고, 요구사항·로드맵·상태를 갱신한 뒤, 사용자 확인 아래 장 마감 후 배포한다.

Purpose: 이 phase 의 모든 주장(드롭 0 · 래치 왕복 · 새 필드 표시)은 단위 테스트가 아니라 **서버 HEAD 와의 왕복**으로만 증명된다. mock 게이트웨이는 서버 HEAD 이므로 76/77/78·36/37/38 을 실제로 낸다.
Output: 실기 관측 기록, 전량 게이트 결과, 갱신된 3개 문서, (사용자 승인 시) relay·webapp 배포와 smoke 결과.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-gh-trade-led/17-CONTEXT.md
@.planning/phases/17-gh-trade-led/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: mock 게이트웨이 실기 검증 + 전량 게이트</name>
  <files>.planning/phases/17-gh-trade-led/ (관측 기록·스크린샷 — 소스 변경 없음)</files>
  <precondition>gh-trade 저장소(`/Users/alex/repos/gh-trade`)가 HEAD 체크아웃 상태이고 `server/scripts/run-mac.sh` 가 실행 가능하다. 실행 불가면 중단하고 사유를 보고한다.</precondition>
  <read_first>
    - `/Users/alex/repos/gh-radar/relay/README.md` (로컬 mock 기동 절차 · env)
    - `/Users/alex/repos/gh-radar/dev.sh` (**webapp PORT=3100** — 3000 을 가정하지 않는다)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-24 · D-25
    - 17-03-SUMMARY(`default:` 계수기 노출 방식) · 17-04-SUMMARY(`lc.arm` 왕복) · 17-11-SUMMARY(LED 결선)
    - 읽기 전용: `/Users/alex/repos/gh-trade/server/scripts/run-mac.sh`
  </read_first>
  <action>
① 전량 게이트를 먼저 돌린다(아래 `<verify>` 의 명령들). 실패하면 여기서 멈추고 원인을 SUMMARY 에 적는다 — 깨진 상태로 실기 검증에 들어가지 않는다.

② mock 게이트웨이를 띄운다(gh-trade `server/scripts/run-mac.sh`). relay 를 dev 로 띄워 그 게이트웨이에 붙이고, webapp 을 `./dev.sh` 기준으로 띄운다(**PORT 3100**).

③ 관측할 것 5가지를 하나씩 확인하고 각각의 **증거**(로그 줄 · 스크린샷 경로 · 카운터 값)를 SUMMARY 에 적는다:
  1. relay 로그에 `unknown-msg-type` 드롭이 **0건** (76/77/78 이 실제로 오는 동안).
  2. hub `default:` 계수기가 **0**.
  3. 브라우저가 인증 직후 `rate.cross.snap` 을 받는다(빈 배열이어도 1프레임).
  4. 상따 화면에서 LED 를 클릭하면 게이트웨이 로그에 36/37/38 요청이 찍히고, 60 에코가 돌아와 LED 색이 바뀐다. **무장 전제를 어긴 클릭**(예: 매도 무장 OFF 상태)에서는 서버 한글 WARN 이 화면 서버 메시지에 그대로 뜬다.
  5. 미체결·주문통보·체결테이프·호가 종목정보에 새 표기가 실제로 보인다(mock 데이터가 해당 필드를 내는 범위 안에서). 못 내는 필드는 **「mock 에서 관측 불가」로 명시**하고 실서버 관찰 항목으로 넘긴다 — 확인하지 못한 것을 확인한 것처럼 적지 않는다.

④ 스크린샷은 라이트·다크 각 1장씩 상따 상태줄을 담아 `.planning/phases/17-gh-trade-led/` 아래에 남긴다.

⑤ 커밋 메시지 초안: `test(17-12): mock 게이트웨이 실기 검증 — 드롭 0·래치 왕복·새 표기 관측 기록`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm typecheck</automated>
    <fails_when>비영 종료코드, 또는 출력에 `error TS` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests</automated>
    <fails_when>비영 종료코드, vitest 요약 줄에 `failed`, 또는 tsc 출력에 `error TS` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>비영 종료코드, vitest 요약 줄에 `failed`, 또는 tsc 출력에 `error TS` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared test -- --run</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-trade/server && RELAY=/Users/alex/repos/gh-radar/relay ./scripts/sync-relay-schema.sh --check</automated>
    <fails_when>비영 종료코드, 또는 출력에 차이 파일이 나열되면 실패 (생성물이 손편집됐다)</fails_when>
  </verify>
  <acceptance_criteria>
    - 위 5개 자동 게이트가 모두 성공한다.
    - 관측 5가지가 각각 증거와 함께 SUMMARY 에 기록됐다.
    - mock 에서 관측 불가한 항목이 있으면 그 목록이 명시됐다.
    - 상따 상태줄 스크린샷 2장(라이트·다크)이 phase 디렉터리에 있다.
    - 재동기화 `--check` 가 여전히 차이 0 이다.
  </acceptance_criteria>
  <done>서버 HEAD 와의 왕복에서 드롭 0 과 래치 토글이 관측됐고, 관측하지 못한 것은 그렇게 적혔다.</done>
</task>

<task type="auto">
  <name>Task 2: REQUIREMENTS · ROADMAP · STATE 갱신</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/.planning/REQUIREMENTS.md` 97-101 (TRADE-01~05 정의부) · 175-190 (Traceability 표 · Coverage 주석)
    - `/Users/alex/repos/gh-radar/.planning/ROADMAP.md` 34 (상단 Phase 17 항목) · `### Phase 17` 절
    - `/Users/alex/repos/gh-radar/.planning/STATE.md` 프론트매터 + `## Current Position` + `## Accumulated Context`
    - `/Users/alex/.claude/projects/-Users-alex-repos-gh-radar/memory/` 의 `project_gsd_roadmap_append_worktree_restore` 요지 — ROADMAP 은 **상단 목록과 하단 상세 두 곳**을 함께 갱신해야 하고, ROADMAP/STATE 를 고치는 작업은 worktree 로 격리하면 merge 가 main 을 되돌린다
  </read_first>
  <action>
① `REQUIREMENTS.md`: TRADE-04 · TRADE-05 의 **정의부 체크박스**와 **Traceability 표** 두 곳을 같은 판정으로 갱신한다. 판정 근거(어느 plan·어느 SUMMARY·어떤 관측)를 Traceability 셀에 적는다. mock 에서 관측 불가했던 항목이 있으면 **판정을 과장하지 않는다** — 실서버 장중 관찰이 남았으면 그 사실을 셀에 적는다. Coverage 카운트는 총계가 바뀌지 않았으므로 건드리지 않는다(TRADE-04/05 는 2026-09-18 에 이미 45→47 로 반영됐다).

② `ROADMAP.md`: **두 곳**을 고친다 — 상단 목록의 Phase 17 체크박스와 `### Phase 17` 절의 `**Plans:**` 줄·plan 목록. plan 목록은 12개 항목(17-01 ~ 17-12)을 각 한 줄 목적과 함께 채우고 완료 표시한다. **구조 변경은 `gsd roadmap` 서브커맨드로** 하고 직접 편집은 자리표시자 채움에 한정한다.

③ `STATE.md`: 프론트매터(`current_phase` · `status` · `last_activity` · `progress`)와 `## Current Position`, `## Accumulated Context` 를 갱신한다. Accumulated Context 에는 이 phase 가 남긴 **재사용 가능한 사실**만 적는다: 생성물 재동기화 절차(`RELAY=` 필수), `INBOUND_MSG_TYPES` ↔ hub 명시 case 동시 변경 규율, 21 요청 거래소 FIFO, LED 판정 순수함수 위치, 통보 묶기 순수함수 위치.

④ **worktree 격리 금지**: ROADMAP/STATE 를 고치는 이 태스크는 main 체크아웃에서 직접 한다(worktree 에서 하면 merge 가 main 을 옛 내용으로 되돌린 전례가 있다).

⑤ 커밋 메시지 초안: `docs(17-12): Phase 17 완료 반영 — TRADE-04·TRADE-05 판정, ROADMAP plan 12건, STATE 갱신`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "TRADE-04\|TRADE-05" .planning/REQUIREMENTS.md</automated>
    <fails_when>카운트가 4 미만이면 실패 (정의부·Traceability 양쪽에 두 ID 가 다 있어야 한다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "17-12-PLAN.md" .planning/ROADMAP.md</automated>
    <fails_when>카운트가 0 이면 실패 (plan 목록이 12건으로 채워지지 않았다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "Phase 17" .planning/STATE.md</automated>
    <fails_when>카운트가 0 이면 실패 (STATE 에 이 phase 기록이 없다)</fails_when>
  </verify>
  <acceptance_criteria>
    - TRADE-04 · TRADE-05 의 정의부 체크박스와 Traceability 행 판정이 **서로 일치**한다.
    - 판정 근거에 plan/SUMMARY 경로와 관측 내용이 적혀 있다.
    - mock 관측 불가 항목이 있으면 Traceability 셀에 그대로 기록됐다.
    - ROADMAP 상단 목록과 `### Phase 17` 절이 **둘 다** 갱신됐다.
    - ROADMAP plan 목록에 17-01 ~ 17-12 열두 줄이 있다.
    - STATE 프론트매터·Current Position·Accumulated Context 가 갱신됐다.
    - 이 태스크의 커밋이 main 체크아웃에서 이뤄졌다.
  </acceptance_criteria>
  <done>요구사항·로드맵·상태가 실제 결과와 일치하고, 과장 없는 판정이 남았다.</done>
</task>

<task type="auto">
  <name>Task 3: 배포 대상 판정 + 배포 계획 제시 (실행 전)</name>
  <files>.planning/phases/17-gh-trade-led/17-12-SUMMARY.md (판정 기록 — 소스 변경 없음)</files>
  <precondition>Task 1 의 전량 게이트 5종이 전부 green 이고 Task 2 의 문서 갱신이 커밋돼 있다. 아니면 중단한다.</precondition>
  <read_first>
    - `/Users/alex/repos/gh-radar/scripts/deploy-relay.sh` (AR push → IAP SSH → docker run · `DMA_HOST` 3단 우선순위 보존 · `/healthz` 폴링)
    - `/Users/alex/repos/gh-radar/scripts/smoke-relay.sh` (INV 목록 · `SMOKE_AUTH_TOKEN` 의존 프로브)
    - `/Users/alex/.claude/projects/-Users-alex-repos-gh-radar/memory/` 의 `reference_vercel_frontend_deploy` · `reference_gh_radar_deployer_sa` · `reference_deploy_worker_env` · `project_market_hours_0800_2000`
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-26
  </read_first>
  <action>
① **server(Express) 배포 필요 여부를 판정한다.** 이 phase 의 server 패키지 diff 를 낸다(phase 시작 커밋부터 HEAD 까지의 명시 범위로). 변경이 `packages/shared` 타입 전용이고 server 소스에 신규 심볼 소비처가 0건이면 **배포를 생략한다**(16-46 선례). 판정에 쓴 명령과 그 출력을 SUMMARY 에 그대로 적는다. 소비처가 있으면 배포 대상에 넣고, `deploy-server.sh` 에 필요한 env(`GCP_PROJECT_ID` · `SUPABASE_URL` · `CORS_ALLOWED_ORIGINS`)를 라이브 Cloud Run env 에서 추출해 쓴다.

② 현재 KST 시각을 잰다. 장 시간(08:00~20:00) 안이면 배포 계획만 제시하고 **대기**한다.

③ 사용자에게 보여 줄 배포 계획을 만든다: ⓐ **pathspec 없는** `git status --short` 전체 출력, ⓑ 이 phase 의 커밋 목록, ⓒ 배포 대상(relay · webapp · server 여부와 ① 의 근거), ⓓ 현재 KST 시각, ⓔ 배포 순서와 각 단계가 건드리는 것. 이 자료는 다음 태스크의 결정 게이트가 그대로 쓴다.

④ **이 태스크는 아무것도 배포하지 않는다.** 판정과 자료 준비까지다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && git status --short</automated>
    <fails_when>비영 종료코드, 또는 출력에 이 phase 가 만들지 않은 미확인 변경이 섞여 있으면 실패 — 그 경우 사용자에게 보고하고 멈춘다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && TZ=Asia/Seoul date +%H%M</automated>
    <fails_when>출력이 `0800`~`2000` 사이면 장중이므로 다음 태스크로 진행하지 않는다(실패가 아니라 대기 신호 — SUMMARY 에 대기 사유를 적는다)</fails_when>
  </verify>
  <acceptance_criteria>
    - server 배포 생략/수행 판정 근거(명령과 출력)가 SUMMARY 에 있다.
    - pathspec 없는 `git status --short` 전체 출력이 SUMMARY 에 있다.
    - 이 phase 의 커밋 목록이 SUMMARY 에 있다.
    - 현재 KST 시각이 기록됐다.
    - 이 태스크에서 배포 스크립트가 실행되지 않았다.
  </acceptance_criteria>
  <done>무엇을 어디에 배포할지와 그 근거가 문서로 준비됐다. 아직 아무것도 배포하지 않았다.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 4: [BLOCKING · ONE-WAY] 배포 승인</name>
  <reversibility rating="one-way">프로덕션 배포는 실계좌 주문 경로를 바꾼다. 롤백이 가능해도 그 사이에 나간 주문은 되돌릴 수 없다.</reversibility>
  <decision>Phase 17 의 relay · webapp(· 필요 시 server)을 지금 프로덕션에 배포할 것인가</decision>
  <context>
    Task 3 이 준비한 자료를 그대로 보여 준다: pathspec 없는 `git status --short` 전체 · 이 phase 의 커밋 목록 ·
    배포 대상과 그 근거(server 생략 여부 포함) · 현재 KST 시각 · 배포 순서.

    배포가 건드리는 것:
      · `scripts/deploy-relay.sh` — AR 이미지 push → IAP SSH → VM `radar-gw` 에서 docker 재기동.
        **재기동은 살아 있는 DMA 세션을 끊는다** — 그래서 장 시간(08:00~20:00 KST) 밖에만 한다.
        `DMA_HOST` 는 무주입 3단 우선순위를 보존한다(Phase 16 갭 5 실증).
      · Vercel webapp — `ignoreCommand` 가 push 를 skip 한 전례가 있어 push 만으로 배포됐다고 가정하지 않는다.
      · `scripts/smoke-relay.sh` — 배포 후 프로브. `SMOKE_AUTH_TOKEN` 의존 프로브(INV-9)는 토큰 부재 시 **미실행이 정상**이다.

    되돌리기: relay 는 이전 이미지 태그로 재배포하면 돌아오지만, **그 사이에 나간 주문은 되돌릴 수 없다.**
  </context>
  <options>
    <option id="deploy-now">
      <name>지금 배포한다 (relay → webapp → smoke)</name>
      <pros>Phase 17 의 보정이 다음 거래일부터 실서버에 적용된다. 장 마감 후라 세션 단절 영향이 없다</pros>
      <cons>실계좌 주문 경로가 바뀐다. 다음 장에서 사용자가 직접 관찰해야 할 항목이 생긴다</cons>
    </option>
    <option id="defer">
      <name>보류한다</name>
      <pros>코드는 이미 커밋돼 있어 언제든 배포할 수 있다. 관찰 여력이 있는 날로 미룰 수 있다</pros>
      <cons>실서버 검증이 그만큼 늦어지고, 다음 phase 가 미배포 상태 위에 쌓인다</cons>
    </option>
    <option id="relay-only">
      <name>relay 만 배포하고 webapp 은 다음에 한다</name>
      <pros>중계 계층의 드롭 경고 0 을 먼저 실서버에서 확인한다</pros>
      <cons>브라우저가 새 프레임을 아직 모르므로 화면 보정이 드러나지 않는다 — 반쪽 확인이 된다</cons>
    </option>
  </options>
  <resume-signal>`deploy-now` · `defer` · `relay-only` 중 하나를 적어 주세요.</resume-signal>
  <acceptance_criteria>
    - Task 3 의 자료 5종을 모두 보여 준 뒤 물었다.
    - 사용자 선택과 그 시각(KST)이 SUMMARY 에 기록됐다.
  </acceptance_criteria>
  <done>배포 여부가 사용자 결정으로 확정됐다.</done>
</task>

<task type="auto">
  <name>Task 5: 배포 실행 + smoke + 잔여 관찰 인계</name>
  <files>scripts/deploy-relay.sh, scripts/smoke-relay.sh (실행만 — 파일 변경 없음)</files>
  <precondition>Task 4 에서 사용자가 `deploy-now` 또는 `relay-only` 를 골랐고, 현재 KST 시각이 20:00 이후다. `defer` 면 이 태스크를 건너뛴다.</precondition>
  <read_first>
    - `/Users/alex/repos/gh-radar/scripts/deploy-relay.sh`
    - `/Users/alex/repos/gh-radar/scripts/smoke-relay.sh`
    - 17-12-SUMMARY 의 Task 3 배포 대상 판정과 Task 4 사용자 선택
  </read_first>
  <action>
① 사용자가 고른 범위대로 순서대로 실행한다: `scripts/deploy-relay.sh` → (`deploy-now` 면) webapp Vercel 배포 → `scripts/smoke-relay.sh`.

② webapp 배포는 push 만으로 됐다고 가정하지 않는다 — `ignoreCommand` 가 배포를 skip 한 전례가 있다. 실제 배포 산출물을 확인하고, 필요하면 repo root 에서 수동 경로(pull → build → deploy --prebuilt)를 쓴다.

③ 배포 뒤 확인: relay `/healthz` 가 200 이고 `everReadyCount` 가 증가했으며 `stalledCount` 가 0 인지. webapp 은 실제 번들 청크에 이 phase 의 변경(예: `lc.arm` 문자열, LED 토큰명)이 들어갔는지 대조한다.

④ `smoke-relay.sh` 결과를 SUMMARY 에 적는다. `SMOKE_AUTH_TOKEN` 부재로 실행되지 않은 프로브는 **실패가 아니라 미실행**으로 적는다(STATE 승계 항목).

⑤ 실서버 장중 관찰은 사용자 몫이다(D-25). 체크리스트를 SUMMARY 에 남긴다: 76 돌파 알림 수신 · LED 3종 색과 클릭 왕복 · 미체결 표식(Q/P/종가)과 취소보관 회색 · 주문통보 행위 단어와 3초 창 묶기 · 호가 종가 표기 · VI 거래소 열.

⑥ 배포 자체는 커밋을 만들지 않는다. 배포 기록만 SUMMARY 에 남긴다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash scripts/smoke-relay.sh</automated>
    <fails_when>비영 종료코드, 또는 출력에 `SMOKE_AUTH_TOKEN` 부재 사유가 아닌 `FAIL` 이 한 건이라도 나오면 실패</fails_when>
    <automated>curl -fsS https://dma.jx1.io/healthz</automated>
    <fails_when>비영 종료코드(200 아님), 또는 응답 JSON 의 `stalledCount` 가 0 이 아니면 실패</fails_when>
  </verify>
  <acceptance_criteria>
    - 배포 시각이 20:00 KST 이후임이 SUMMARY 에 기록됐다.
    - relay `/healthz` 가 200 이고 `stalledCount` 가 0 이다.
    - (`deploy-now` 면) webapp 번들에 이 phase 변경이 들어갔음을 청크 대조로 확인했다.
    - `smoke-relay.sh` 결과가 SUMMARY 에 있고 미실행 프로브가 실패가 아닌 미실행으로 적혔다.
    - 사용자 장중 관찰 체크리스트 6항목이 SUMMARY 에 있다.
  </acceptance_criteria>
  <done>relay 와 webapp 이 장 마감 후 사용자 승인 아래 배포됐고, smoke 와 `/healthz` 가 정상이며, 남은 실서버 관찰 항목이 문서로 넘겨졌다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (17-12)

**문서:** 갱신된 `.planning/REQUIREMENTS.md`(TRADE-04·TRADE-05 판정) · `.planning/ROADMAP.md`(Phase 17 상단 목록 + 상세 절 + plan 12건) · `.planning/STATE.md`(위치·Accumulated Context)

**기록:** mock 게이트웨이 실기 관측 5항목 + 관측 불가 목록 · 상따 상태줄 스크린샷 2장 · 배포 기록 · 사용자 장중 관찰 체크리스트
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 코드 → 프로덕션 VM(radar-gw) | 배포가 실계좌 주문 경로를 바꾼다 |
| 배포 시각 ↔ 장 시간 | 장중 배포는 살아 있는 전략·미체결을 가진 세션을 끊는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-42 | Denial of Service | 장중 배포로 DMA 세션 단절 | critical | mitigate | 20:00 KST 이후 precondition + 사용자 승인 checkpoint |
| T-17-43 | Tampering | `DMA_HOST` 주입으로 게이트웨이 경로 변경 | critical | mitigate | `deploy-relay.sh` 의 3단 우선순위 무주입 보존(Phase 16 갭 5 실증) |
| T-17-44 | Repudiation | Vercel ignoreCommand 로 배포 skip | high | mitigate | push 로 배포됐다고 가정하지 않고 번들 청크 대조로 확인 |
| T-17-45 | Repudiation | 미실행 프로브를 실패로 기록 | medium | mitigate | `SMOKE_AUTH_TOKEN` 부재는 미실행으로 명시 |
| T-17-46 | Repudiation | 관측 못 한 것을 관측한 것처럼 기록 | high | mitigate | mock 관측 불가 목록 명시 + Traceability 셀에 잔여 관찰 기록 |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 plan 은 패키지 설치가 없다 |
</threat_model>

<verification>
- 전량 게이트 5종 green (루트 typecheck · relay test/typecheck/typecheck:tests · webapp test/typecheck · shared test · 재동기화 --check)
- mock 게이트웨이 관측 5항목 기록
- `smoke-relay.sh` 결과 · `/healthz` 200 · `stalledCount` 0
</verification>

<success_criteria>
- 서버 HEAD 와의 왕복에서 드롭 0 과 래치 토글이 실제로 관측됐다.
- TRADE-04 · TRADE-05 판정이 정의부와 Traceability 에서 일치하고 과장이 없다.
- 배포가 사용자 승인 아래 장 마감 후 이뤄졌고 smoke 가 통과했다.
</success_criteria>

<output>
Create `.planning/phases/17-gh-trade-led/17-12-SUMMARY.md` when done
</output>