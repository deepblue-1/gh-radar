---
phase: 17-gh-trade-led
plan: 05
type: execute
wave: 5
depends_on: ["17-04"]
files_modified:
  - relay/src/dma/envelope.ts
  - relay/src/hub/subscription-hub.ts
  - relay/src/ws/fanout.ts
  - relay/src/ws/protocol.ts
  - relay/tests/strategy-hub.test.ts
  - relay/tests/helpers/frames.ts
  - relay/tests/helpers/fake-gateway.ts
autonomous: true
requirements: [TRADE-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "VI 전략·발동통보·주문목록이 거래소를 싣고, 빈 와이어 값은 relay 가 `KRX` 로 정규화한다 (D-06)"
    - "DMA Ready 프리페치가 `GetVITriggerReq(21)` 을 `KRX`·`NXT` 두 번 보내고, 거래소 정보가 없는 빈 61 응답을 **요청 거래소 FIFO** 로 귀속한다 (D-06 · Pitfall 3)"
    - "relay 세션 캐시와 `{t:\"vi\"}` 프레임이 거래소별이고, 브라우저 인증 스냅샷도 두 거래소 모두 내려간다 (D-06)"
    - "VI 주문 행 상관 키가 ISIN+거래소다 — 같은 종목이 양쪽에서 발동하면 행이 둘이다 (D-06)"
  artifacts:
    - relay/tests/strategy-hub.test.ts
  key_links:
    - "buildGetVITriggerReq(exchange) → 21 요청 FIFO → parseViTrigger(빈 61) 귀속"
    - "#viTriggers(거래소별) → getViTrigger(userId, exchange) → fanout 인증 스냅샷 2프레임"
---

<objective>
VI 전략에 **거래소 축**을 넣는다. 서버는 이미 거래소별 1건으로 VI 전략을 관리하고 `SetVITrigger.exchange` · `VIOrderNotice.exchange` · `VIOrderItem.exchange` 를 보내지만 relay 는 KRX 하나만 조회하고 거래소를 버린다. 이 plan 은 relay 층만 고친다 — 웹 표면은 17-06 이다.

Purpose: NXT 에 등록된 VI 전략이 relay 에 존재하지 않는 것처럼 보이고, 같은 종목이 KRX·NXT 양쪽에서 발동하면 주문 행이 하나로 겹친다. 둘 다 서버 진실과 갈리는 상태다.
Output: 파서 3종 exchange, `buildGetVITriggerReq(exchange)`, 21 2회 + 요청 거래소 FIFO, 거래소별 캐시·프레임·스냅샷, `vi.set` 의 `exchange`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gh-trade-led/17-CONTEXT.md
@.planning/phases/17-gh-trade-led/17-RESEARCH.md
@.planning/phases/17-gh-trade-led/17-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: VI 거래소 한 경로 — 파서 3종 · 조립기 · `{t:"vi", x}` 프레임</name>
  <files>relay/src/dma/envelope.ts, relay/tests/helpers/frames.ts, relay/tests/helpers/fake-gateway.ts, relay/src/dma/__tests__/envelope.test.ts, relay/tests/strategy-hub.test.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/relay/src/dma/envelope.ts` 1757-1793 (`parseViTrigger`, `VI_PRICE_TYPE="U"` 고정) · 1819-1886 (`parseViOrderList`) · 1893-1920 (`parseViOrderNotice`) · `buildSetVITriggerReq` 본문
    - `/Users/alex/repos/gh-radar/relay/src/dma/envelope.ts` 의 `fromWireExchange()` (빈 값 → `"KRX"` 정규화 정본 — 미체결 파서가 이미 쓰고 있다)
    - `/Users/alex/repos/gh-radar/packages/shared/src/relay.ts` (17-01 이 넣은 `RelayViTrigger.exchange` · `RelayViOrderItem.exchange` · `RelayViTriggerMsg.x` · `RelayViNoticeMsg.exchange`)
    - `/Users/alex/repos/gh-radar/relay/tests/helpers/frames.ts` 697-900 (`buildSetVITriggerRespFrame` · `buildViOrderListFrame` · `buildViOrderNoticeFrame`)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §1-2 `SetVITrigger.exchange`(14) · `VIOrderNotice.exchange`(24) · `VIOrderItem.exchange`(34) 행
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-06 · D-18
  </read_first>
  <behavior>
    - `exchange` 가 `"NXT"` 인 61 프레임 → `RelayViTrigger.exchange === "NXT"`
    - `exchange` 가 빈 값인 61 프레임 → `"KRX"` (정규화)
    - `exchange` 가 빈 값인 56 발동통보 → `RelayViNoticeMsg.exchange === "KRX"`
    - `exchange` 가 `"NXT"` 인 72/73 원소 → `RelayViOrderItem.exchange === "NXT"`
    - `buildSetVITriggerReq({... exchange:"NXT"})` → 게이트웨이가 읽은 `SetVITrigger.exchange` 가 `"NXT"`
    - `exchange` 미지정으로 조립하면 게이트웨이가 읽은 값이 `"KRX"`
  </behavior>
  <action>
① `parseViTrigger`(61) · `parseViOrderNotice`(56) · `parseViOrderList`(72/73) 가 각각 `exchange()` 를 읽고 기존 `fromWireExchange()` 로 정규화해 결과에 싣는다. 새 정규화 함수를 만들지 않는다 — 미체결 파서가 쓰는 그 함수를 쓴다(정규화가 두 벌이 되면 갈린다).

② `buildSetVITriggerReq` 가 `exchange` 를 실어 보낸다. 조립기에 **기본값을 두지 않는다** — 호출부가 값을 정한다(`buildSetLimitChaserReq` 의 `market` 이 같은 규율이다: 조립기 기본값 `"K"` 가 코스닥 전략을 코스피로 등록시킨 선례). 호출부(fanout `vi.set` 분기)가 `msg.exchange ?? "KRX"` 로 채운다.

③ `#fanout` 이 내리는 VI 설정 프레임을 `{ t: "vi", x: exchange, cfg }` 로 바꾼다. `cfg === null`(미등록)일 때도 `x` 는 항상 실린다 — 어느 거래소가 미등록인지 말해야 브라우저가 두 칸을 구분한다.

④ `relay/tests/helpers/frames.ts` 의 VI 빌더 3종에 `exchange` optional 입력을 더한다(문자열은 테이블 열기 전에 `createString`). `relay/tests/helpers/fake-gateway.ts` 의 `readViSetRequest` 가 `exchange` 도 돌려주게 넓힌다.

⑤ `<behavior>` 6케이스를 `relay/src/dma/__tests__/envelope.test.ts` 와 `relay/tests/strategy-hub.test.ts` 에 나눠 단언한다.

⑥ 커밋 메시지 초안: `feat(17-05): VI 파서·조립기·프레임에 거래소 축 추가 (빈 값 KRX 정규화)`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm typecheck</automated>
    <fails_when>비영 종료코드, 또는 출력에 `error TS` 가 나오면 실패</fails_when>
  </verify>
  <acceptance_criteria>
    - `parseViTrigger` · `parseViOrderNotice` · `parseViOrderList` 세 함수 본문에서 `fromWireExchange(` 가 각 1회 이상 호출된다.
    - `relay/src/dma/envelope.ts` 에 VI 전용 거래소 정규화 함수가 **새로 생기지 않았다** — `fromWireExchange` 말고 다른 정규화 함수 정의가 0건이다.
    - `sed -n '/export function buildSetVITriggerReq/,/^}/p' relay/src/dma/envelope.ts` 결과에 `"KRX"` 리터럴이 없다(기본값 금지).
    - `{ t: "vi"` 를 만드는 자리마다 `x:` 가 함께 있다.
    - `<behavior>` 6케이스가 테스트에 있다.
    - relay 테스트 + 루트 typecheck green.
  </acceptance_criteria>
  <done>VI 와이어의 거래소가 relay 계약에 실리고, 조립기가 기본값 없이 호출부의 거래소를 보낸다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ready 프리페치 21 을 KRX·NXT 2회 + 요청 거래소 FIFO 귀속</name>
  <files>relay/src/dma/envelope.ts, relay/src/hub/subscription-hub.ts, relay/src/ws/fanout.ts, relay/tests/strategy-hub.test.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/relay/src/hub/subscription-hub.ts` 420-500 (`requestStrategySnapshot` — 24/21/34 각 1회 · getter 3종) · 261 부근 (`#viTriggers` 선언) · `case MSG.SetVITriggerResp:` 분기 · `#onViTrigger`
    - `/Users/alex/repos/gh-radar/relay/src/dma/envelope.ts` 1170-1200 (`buildBareRequest` · 현행 `buildGetVITriggerReq`)
    - `/Users/alex/repos/gh-radar/relay/tests/strategy-hub.test.ts` (Ready 프리페치 24/21/34 **각 1회** 단언이 여기 있다 — 이 단언을 갱신하는 것이 이 태스크의 일부다)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §1-2 `GetStrategyReq.key` 의미 확장 행 · §4 Pitfall 3
    - 읽기 전용 정본: `/Users/alex/repos/gh-trade/server/src/protocol/StockDMA.fbs` 642-648
  </read_first>
  <behavior>
    - DMA Ready 직후 게이트웨이가 받은 21 요청이 **2건**이고 `get_strategy_req.key` 가 각각 `"KRX"` · `"NXT"` 다
    - 게이트웨이가 KRX 요청에 **빈 61**(미등록)을, NXT 요청에 등록된 61 을 순서대로 답하면 캐시는 `KRX → null` · `NXT → cfg` 가 된다
    - 응답 순서가 뒤바뀌어도 FIFO 가 요청 순서로 귀속하므로 같은 결과가 된다 — 빈 61 은 거래소 정보를 담지 않기 때문이다
    - 21 을 한 번도 안 보낸 상태에서 빈 61 이 오면 FIFO 가 비어 있으므로 캐시를 고치지 않고 경고만 남긴다
    - 24(상따 목록)·34(VI 주문목록) 요청 수는 **각 1회 그대로**다
  </behavior>
  <action>
① `buildGetVITriggerReq(exchange: RelayExchange): Uint8Array` 로 시그니처를 바꾼다. 21 은 `get_strategy_req.key` 를 **거래소 문자열**로 읽으므로 `buildBareRequest` 가 아니라 17-04 의 `buildArmLatchReq` 와 같은 조립 형태(문자열 먼저 → `GetStrategyReq` → `Envelope`)를 쓴다. 기존 무인자 호출부를 전부 갱신한다.

② `requestStrategySnapshot` 이 21 을 `"KRX"` · `"NXT"` 두 번 보내고, 보낸 **순서대로** 요청 거래소를 세션별 FIFO 에 push 한다. 자료구조는 재량이되 세션 교체·재로그인 시 **반드시 비운다**(옛 큐가 남으면 다음 빈 응답이 옛 거래소로 귀속된다).

③ `case MSG.SetVITriggerResp:` 처리: 파싱 결과에 거래소가 있으면 **그 값**으로 캐시에 넣는다(60 에코처럼 본문이 정본). 빈 61 이라 거래소가 없으면 FIFO 에서 **shift** 해 귀속한다. FIFO 가 비어 있으면 캐시를 고치지 않고 warn 을 남긴다 — 지어낸 거래소로 귀속하면 사용자가 입력 중인 금액이 엉뚱한 칸에서 지워진다.

④ `#viTriggers` 를 거래소별로 바꾼다: `Map<userIdExchangeKey, RelayViTrigger | null>`. **3상태 규율을 유지한다** — 객체=등록, `null`=조회 결과 미등록, `undefined`(키 부재)=아직 모름. `getViTrigger(userId, exchange)` 로 시그니처를 바꾸고, 기존 호출부를 전부 갱신한다.

⑤ `relay/src/ws/fanout.ts` 의 인증 직후 스냅샷에서 `vi` 를 **두 거래소 모두** 내린다. 각 거래소에 대해 `undefined` 면 보내지 않는 기존 3상태 규율을 그대로 적용한다(거래소마다 독립 판정).

⑥ `relay/tests/strategy-hub.test.ts` 의 기존 「21 1회」 단언을 「21 2회, key 가 KRX·NXT」 로 갱신하고 `<behavior>` 5케이스를 더한다.

⑦ 커밋 메시지 초안: `feat(17-05): Ready 프리페치 21 을 KRX·NXT 2회로 + 요청 거래소 FIFO 귀속 + 거래소별 VI 캐시`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test -- strategy-hub</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test && pnpm --filter @gh-radar/relay run typecheck:tests</automated>
    <fails_when>비영 종료코드, vitest 요약 줄에 `failed`, 또는 tsc 출력에 `error TS` 가 나오면 실패</fails_when>
  </verify>
  <acceptance_criteria>
    - `buildGetVITriggerReq` 가 인자를 받는다: `grep -c "buildGetVITriggerReq(exchange" relay/src/dma/envelope.ts` 가 1 이상이다.
    - `sed -n '/export function buildGetVITriggerReq/,/^}/p' relay/src/dma/envelope.ts | grep -c buildBareRequest` 가 `0` 이다.
    - `requestStrategySnapshot` 본문에 `buildGetVITriggerReq` 호출이 2회 있다.
    - `getViTrigger(` 가 두 인자를 받고, 모든 호출부가 거래소를 넘긴다(typecheck 로 보증).
    - 세션 교체/재로그인 시 FIFO 를 비우는 코드가 있다.
    - FIFO 가 빈 상태에서 빈 61 이 왔을 때 캐시를 고치지 않는 케이스가 테스트에 있다.
    - 24·34 요청 수가 각 1회임을 단언하는 기존 테스트가 여전히 통과한다.
    - relay 테스트 · typecheck:tests green.
  </acceptance_criteria>
  <done>NXT 에 등록된 VI 전략이 relay 캐시에 존재하고, 거래소를 담지 않는 빈 응답이 요청 순서로 정확히 귀속된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `vi.set` 의 exchange 수용 + VI 주문 상관 키에 거래소 포함</name>
  <files>relay/src/ws/protocol.ts, relay/src/ws/fanout.ts, relay/src/hub/subscription-hub.ts, relay/tests/protocol.test.ts, relay/tests/strategy-hub.test.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/relay/src/ws/protocol.ts` 152-175 (`RelayViSetSchema` — `priceType` 을 받지 않는 이유 주석 포함)
    - `/Users/alex/repos/gh-radar/relay/src/ws/fanout.ts` 의 `vi.set` 분기
    - `/Users/alex/repos/gh-radar/relay/src/hub/subscription-hub.ts` 204 부근 (`viOrderKey` — 주문번호 또는 `@isin:acct:trigger` 대체 키) 와 806-826 (`#onViOrderList`)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §1-2 `VIOrderItem.exchange`(34) 행 (「R8 매칭은 ISIN+거래소」)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-06
  </read_first>
  <behavior>
    - `{t:"vi.set", ..., exchange:"NXT"}` → 게이트웨이가 읽은 `SetVITrigger.exchange` 가 `"NXT"`
    - `exchange` 를 생략한 `vi.set` → 게이트웨이가 읽은 값이 `"KRX"`
    - `exchange` 가 `"KOSPI"` 같은 미지 값이면 zod 가 거부하고 아무것도 나가지 않는다
    - 같은 ISIN·같은 계좌의 접수 전(주문번호 빈) VI 주문이 KRX·NXT 양쪽에서 오면 캐시 행이 **2건**이다
    - 주문번호가 있는 행은 종전처럼 주문번호가 키다(거래소를 덧붙여 기존 행을 쪼개지 않는다)
  </behavior>
  <action>
① `RelayViSetSchema` 에 `exchange: ExchangeSchema.optional()` 을 더한다(기존 `ExchangeSchema` 재사용 — 새 enum 을 만들지 않는다). fanout `vi.set` 분기가 `msg.exchange ?? "KRX"` 로 채워 `buildSetVITriggerReq` 에 넘긴다.

② `viOrderKey`(및 접수 전 대체 키) 에 거래소를 포함시킨다. **주문번호가 있는 행의 키는 바꾸지 않는다** — 주문번호는 이미 유일하고, 거래소를 덧붙이면 72 스냅샷과 73 델타의 키가 갈려 기존 행이 중복된다. 바꾸는 것은 **주문번호가 빈 접수 전 행의 대체 키**뿐이다(`@isin:acct:trigger` 에 거래소를 더한다).

③ `#onViOrderList` 의 병합이 새 키로 도는지 확인하고, 거래소별 분리를 단언하는 테스트를 더한다.

④ `<behavior>` 5케이스를 `relay/tests/strategy-hub.test.ts` 와 `relay/tests/protocol.test.ts` 에 나눠 단언한다.

⑤ 커밋 메시지 초안: `feat(17-05): vi.set 거래소 수용 + VI 접수 전 주문 상관 키에 거래소 포함`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests</automated>
    <fails_when>비영 종료코드, vitest 요약 줄에 `failed`, 또는 tsc 출력에 `error TS` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "exchange" relay/src/ws/protocol.ts</automated>
    <fails_when>카운트가 0 이면 실패 (`vi.set` 이 거래소를 받지 않는다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `RelayViSetSchema` 에 `exchange` 키가 optional 로 있고 `ExchangeSchema` 를 재사용한다.
    - fanout `vi.set` 분기가 `"KRX"` 기본값을 **호출부에서** 채운다(조립기 안이 아니다).
    - 주문번호가 있는 VI 주문 행의 키가 바뀌지 않았음을 단언하는 회귀 테스트가 있다.
    - 접수 전 행이 거래소별로 2건이 되는 케이스가 테스트에 있다.
    - relay 테스트 · typecheck · typecheck:tests green.
  </acceptance_criteria>
  <done>브라우저가 거래소를 지정해 VI 전략을 등록할 수 있고, 양쪽 거래소의 접수 전 주문이 한 줄로 겹치지 않는다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (17-05)

**함수 시그니처 변경:** `buildGetVITriggerReq(exchange)` · `getViTrigger(userId, exchange)` · `buildSetVITriggerReq({... exchange})`

**hub 내부:** 거래소별 `#viTriggers` 캐시 · 세션별 21 요청 거래소 FIFO · 거래소를 포함한 VI 접수 전 주문 대체 키

**프레임 형태 변경:** `{t:"vi", x:"KRX"|"NXT", cfg}` (인증 스냅샷은 두 거래소 모두)

**zod:** `RelayViSetSchema.exchange` (optional)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → relay (`vi.set.exchange`) | 거래소가 발주 시장을 가른다 — 미지 값은 거부해야 한다 |
| 게이트웨이 빈 61 → relay 귀속 | 응답에 거래소가 없어 **요청 순서**가 유일한 귀속 근거다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-16 | Tampering | 빈 61 의 거래소 오귀속 | high | mitigate | 요청 거래소 FIFO + 세션 교체 시 비우기 + FIFO 빈 상태에서 캐시 미변경 |
| T-17-17 | Tampering | `buildSetVITriggerReq` 기본 거래소 | high | mitigate | 조립기 기본값 금지 — `"KRX"` 리터럴 grep 0 게이트 |
| T-17-18 | Tampering | VI 주문 키에 거래소 덧붙여 기존 행 중복 | medium | mitigate | 주문번호 있는 행의 키 불변 회귀 테스트 |
| T-17-19 | Spoofing | 미지 거래소 문자열 | medium | mitigate | 기존 `ExchangeSchema` 재사용(새 enum 금지) |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 plan 은 패키지 설치가 없다 |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/relay test` · `run typecheck` · `run typecheck:tests` green
- `pnpm typecheck` (루트) green
- Ready 프리페치에서 24·34 각 1회 · 21 2회(key KRX/NXT) 단언 통과
</verification>

<success_criteria>
- NXT VI 전략이 relay 캐시·브라우저 스냅샷에 존재한다.
- 빈 61 이 요청 순서로 정확히 귀속된다.
- `vi.set` 이 거래소를 받고 조립기에는 기본값이 없다.
</success_criteria>

<output>
Create `.planning/phases/17-gh-trade-led/17-05-SUMMARY.md` when done
</output>