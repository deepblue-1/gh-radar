---
phase: 17-gh-trade-led
plan: 07
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - .planning/phases/17-gh-trade-led/17-latch-led-mockup.html
  - webapp/src/styles/globals.css
  - webapp/src/components/trading/latch-led.tsx
  - webapp/src/components/trading/__tests__/latch-led.test.tsx
autonomous: false
requirements: [TRADE-05]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "상따 래치 LED 3종의 색·클릭 가능 규칙이 C# `LimitChaserForm` 정본과 한 글자도 다르지 않다 (D-19)"
    - "LED 는 색만이 아니라 텍스트 라벨(또는 최소 sr-only)을 함께 싣는다 (D-21 · WCAG 1.4.1)"
    - "사용자가 목업을 보고 답한 뒤에만 구현·커밋이 진행된다 (D-22 · 메모리 `feedback_mockup_review_gate_before_commit`)"
    - "주황·초록 LED 토큰이 라이트/다크 둘 다 정의돼 있다 (D-21)"
  artifacts:
    - .planning/phases/17-gh-trade-led/17-latch-led-mockup.html
    - webapp/src/components/trading/latch-led.tsx
    - webapp/src/components/trading/__tests__/latch-led.test.tsx
  key_links:
    - "globals.css `--led-latent`/`--led-armed` ↔ latch-led.tsx 색 매핑"
    - "RelayLimitChaser(서버 에코 스냅샷) → latchLedStateOf() → LED 색·클릭 가능"
---

<objective>
상따 3단계 래치 LED(매수·매도·취소)를 C# 클라이언트와 **같은 규칙**으로 그리는 표시 컴포넌트를 만든다. 이 plan 은 컴포넌트와 토큰까지다 — 상따 화면 상태줄 결선과 `lc.arm` 클릭 전송은 17-11 이다.

Purpose: LED 는 「지금 이 전략이 발주 판정을 시작했는가」를 말하는 유일한 표면이다. 색 규칙이 정본과 한 글자라도 갈리면 사용자가 무장 상태를 오독하고 실계좌 주문이 나간다.
Output: 사용자가 승인한 목업, LED 색 토큰 라이트/다크, `LatchLed` 컴포넌트 + 순수 판정 함수 + 규칙 표 테스트.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gh-trade-led/17-CONTEXT.md
@.planning/phases/17-gh-trade-led/17-RESEARCH.md
@.planning/phases/17-gh-trade-led/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 상태줄 LED standalone HTML 목업 작성 + 열기</name>
  <files>.planning/phases/17-gh-trade-led/17-latch-led-mockup.html</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/styles/globals.css` 1-215 (`:root`/`.dark` 토큰 전체 + §2.2b 4밴드 정본 주석)
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/limit-chaser-client.tsx` 1010-1060 (`StatusBar` — 현재 「DMA / 매수 / 매도」 칩 3개의 마크업과 `Dot` 톤 사용법)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §2 (C# 색·단계·클릭 규칙 의사코드와 **툴팁 원문 3종**)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-19 · D-21 · D-22
    - `/Users/alex/repos/gh-radar/tasks/lessons.md` 「목업 시각 확인 게이트를 건너뛰고 UI-SPEC 을 확정·커밋하지 말 것」 절
    - 기존 목업 형식의 본보기: `.planning/phases/16-*/16-limit-chaser-mockup-draft.html` (있으면)
  </read_first>
  <action>
`.planning/phases/17-gh-trade-led/17-latch-led-mockup.html` 을 **standalone**(외부 CSS·JS 링크 없음)으로 만든다. `webapp/src/styles/globals.css` 의 관련 토큰 값을 `<style>` 안에 인라인 복사한다(값만 복사하고 globals.css 를 정본으로 남긴다).

목업이 반드시 보여 줘야 하는 것:
  - **라이트·다크 두 벌**을 한 페이지에 나란히.
  - **폰 밴드(본문 ~699px)** 와 **데스크톱 밴드(본문 992px~)** 두 폭. 폰 폭에서 LED 3개 + DMA 칩이 **한 줄에 들어가는지**가 이 목업의 핵심 질문이다.
  - LED 3개를 **매수 · 매도 · 취소 순서**로(D-22), 현재 상태줄의 「DMA 연결」 칩 옆자리에.
  - **3상태 × 3 LED = 9칸 견본**: 회색(무장 아님) · 주황(잠복) · 초록(래치 ON). 여기에 **매수 LED 의 매도잔량 기준 예외**(초록 2단계 유지 · 클릭 불가) 를 10번째 칸으로 따로 보여 준다.
  - **텍스트 라벨안 2종**을 비교로 제시한다: (A) 짧은 라벨 동반(`매수 잠복` / `매수 감시` / `매수 —`), (B) 아이콘+sr-only 만. 사용자가 고를 수 있게 둘 다 그린다.
  - 툴팁 문구는 17-RESEARCH §2 의 **C# 원문 그대로** 보이게 한다(특히 매수 켜는 쪽의 「벽이 이미 감시수량 이상이면 바로 매수 주문이 나갈 수 있다」).
  - 클릭 가능한 칸은 `cursor:pointer` 와 눌린 상태 표시가 보이게, 클릭 불가 칸은 그렇지 않게.

목업을 `open` 한 뒤 **멈춘다.** 사용자에게 물을 것은 정확히 3가지다:
  1. 라벨안 (A) 짧은 라벨 동반 / (B) 아이콘+sr-only — 어느 쪽인가
  2. 폰 밴드에서 LED 3개가 한 줄에 들어가는 배치가 괜찮은가 (안 되면 대안: 상태줄 둘째 줄로 내리기)
  3. 주황·초록 색상 값이 라이트/다크 둘 다 읽히는가

목업 파일을 만든 뒤 `open` 으로 브라우저에 띄운다. **여기서 멈춘다** — 다음 태스크가 사용자 확인 게이트다. 「목업을 열어 두었다」를 완료로 보고하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && test -f .planning/phases/17-gh-trade-led/17-latch-led-mockup.html && grep -cE "link rel=.stylesheet|script src=" .planning/phases/17-gh-trade-led/17-latch-led-mockup.html; test $? -le 1</automated>
    <fails_when>파일이 없어 `test -f` 가 비영 종료하거나, grep 카운트가 `0` 이 아니면 실패 (standalone 이 아니다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "매도잔량 기준" .planning/phases/17-gh-trade-led/17-latch-led-mockup.html</automated>
    <fails_when>카운트가 0 이면 실패 (매수 LED 예외 견본과 C# 툴팁 원문이 목업에 없다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/17-gh-trade-led/17-latch-led-mockup.html` 이 존재하고 외부 링크 태그(스타일시트 link · 외부 script src)가 0개다.
    - 목업에 라이트·다크 두 벌과 폰·데스크톱 두 폭이 모두 있다.
    - 3상태 × 3 LED 견본과 매수 매도잔량 기준 예외 칸이 있다.
    - 라벨안 (A)/(B) 두 변형이 그려져 있다.
    - 17-RESEARCH §2 의 툴팁 원문 3종이 목업 안에 리터럴로 들어 있다.
    - 목업을 `open` 했고, 이 태스크에서 webapp 소스 변경이 0건이다.
  </acceptance_criteria>
  <done>목업이 브라우저에 떠 있고 사용자가 답할 3가지 질문이 준비됐다. 구현은 아직 시작하지 않았다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] 목업 시각 확인 게이트 — 라벨안·배치·색</name>
  <what-built>
    상따 상태줄 래치 LED 목업 `.planning/phases/17-gh-trade-led/17-latch-led-mockup.html` 을 만들어 브라우저에 열어 두었다.
    라이트·다크 두 벌 × 폰(본문 ~699px)·데스크톱(본문 992px~) 두 폭, LED 3종 × 3상태 견본 + 매수 LED 의 매도잔량 기준 예외 칸,
    라벨안 (A) 짧은 라벨 동반 / (B) 아이콘+sr-only 두 변형, C# 원문 툴팁 3종이 들어 있다. webapp 소스는 아직 한 줄도 바뀌지 않았다.
  </what-built>
  <how-to-verify>
    열려 있는 목업에서 세 가지를 보고 답해 주세요.
    1. **라벨안** — (A) LED 옆에 짧은 라벨(`매수 잠복` / `매수 감시` / `매수 —`)을 같이 두는 안과 (B) 아이콘만 두고 화면낭독기용 텍스트만 넣는 안 중 어느 쪽인가요?
    2. **폰 배치** — 폰 폭(본문 ~699px)에서 「DMA 연결」 칩 + LED 3개가 한 줄에 들어간 모습이 괜찮은가요? 안 괜찮으면 LED 를 상태줄 둘째 줄로 내립니다.
    3. **색** — 주황(잠복)·초록(래치 ON) 값이 라이트·다크 둘 다 회색(무장 아님)과 확실히 구분돼 읽히나요? 아니면 원하는 색을 말씀해 주세요.
  </how-to-verify>
  <resume-signal>세 답을 적어 주세요 (예: "A / 폰 한 줄 OK / 색 그대로"). 수정 요청이면 그 내용을 적어 주세요 — 반영 후 다시 엽니다.</resume-signal>
  <acceptance_criteria>
    - 사용자 답 3건이 SUMMARY 에 그대로 기록됐다.
    - 이 게이트 전에 webapp 소스 커밋이 0건이다.
  </acceptance_criteria>
  <done>사용자가 라벨안·배치·색을 확정했고 그 답이 문서로 남았다.</done>
</task>

<task type="auto">
  <name>Task 3: LED 색 토큰 — 라이트/다크 정의</name>
  <files>webapp/src/styles/globals.css</files>
  <precondition>Task 2 게이트의 사용자 답(특히 색 3번 질문)이 SUMMARY 에 기록돼 있다. 미기록이면 중단한다.</precondition>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/styles/globals.css` 18-125 (`:root` 토큰 블록 전체) 와 87-125 (`.dark` 블록)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-21
    - `/Users/alex/.claude/projects/-Users-alex-repos-gh-radar/memory/` 의 `feedback_lightweight_charts_oklch` 요지 — oklch 토큰을 직접 주입하면 안 되는 소비처가 이 저장소에 있다(차트). LED 는 CSS 소비처라 oklch 그대로 써도 되지만, 토큰을 JS 로 읽어 쓰는 경로를 만들지 않는다.
  </read_first>
  <action>
① `webapp/src/styles/globals.css` 의 `:root` 에 `--led-latent`(주황 · 잠복) 와 `--led-armed`(초록 · 래치 ON) 를 더한다. `.dark` 에도 같은 이름으로 다크 값을 더한다. 값은 Task 2 게이트에서 사용자가 승인한 것을 쓴다. 기존 팔레트와 같은 `oklch()` 표기를 따른다.

② **회색은 새 토큰을 만들지 않는다** — 기존 `--muted-fg` 를 쓴다(D-21). `--up`/`--down` 을 재사용하지 않는다: 이 둘은 **가격 방향**을 뜻하는 토큰이고, LED 는 방향이 아니라 무장 단계를 말한다. 두 축을 같은 토큰으로 묶으면 한쪽을 바꿀 때 다른 쪽이 조용히 따라 바뀐다.

③ 토큰 정의 옆에 두 줄 주석을 남긴다: (a) 이 토큰의 의미(잠복/래치 ON), (b) 이 phase 밖에서 재사용하려면 의미가 같은지 먼저 확인하라는 경고.

④ **§2.2b 밴드 경계 주석은 건드리지 않는다.** LED 는 상태줄 안에 들어가고 10칸 구성이나 호가표 폭을 바꾸지 않으므로 경계 재측정이 필요 없다(D-11 · D-22). 상따 본문에 **뷰포트 폭 분기를 새로 만들지 않는다**(§2.2b 마지막 경고).

⑤ 커밋 메시지 초안: `feat(17-07): 상따 래치 LED 색 토큰 --led-latent/--led-armed 라이트·다크 정의`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && grep -c -- "--led-latent" webapp/src/styles/globals.css</automated>
    <fails_when>카운트가 2 미만이면 실패 (라이트·다크 두 곳에 정의되지 않았다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c -- "--led-armed" webapp/src/styles/globals.css</automated>
    <fails_when>카운트가 2 미만이면 실패 (라이트·다크 두 곳에 정의되지 않았다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run build</automated>
    <fails_when>비영 종료코드, 또는 출력에 `Failed to compile` 이 나오면 실패</fails_when>
  </verify>
  <acceptance_criteria>
    - `--led-latent` 와 `--led-armed` 가 각각 `:root` 와 `.dark` 두 블록에 정의돼 있다.
    - 두 토큰 정의 옆에 의미와 재사용 경고 주석이 있다.
    - §2.2b 주석 블록의 밴드 표·경계 숫자가 그대로다: `grep -cE "700px|830px|992px" webapp/src/styles/globals.css` 가 3 이상이고 그 줄들의 내용이 작업 전과 같다.
    - webapp 빌드 성공.
  </acceptance_criteria>
  <done>LED 3색이 라이트·다크 둘 다 토큰으로 있고 밴드 정본은 그대로다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: `LatchLed` 컴포넌트 + 순수 판정 함수 + 규칙 표 테스트</name>
  <files>webapp/src/components/trading/latch-led.tsx, webapp/src/components/trading/__tests__/latch-led.test.tsx</files>
  <precondition>Task 2 게이트의 라벨안 선택(A 또는 B)이 SUMMARY 에 기록돼 있다. 미기록이면 중단한다.</precondition>
  <read_first>
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §2 전체 (C# 의사코드 4블록 + 툴팁 원문)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-19 · D-20 · D-21 · `<specifics>` (매도 래치는 서버 안에서 둘이고 와이어에는 잔량 래치만 온다 — 「잔량 OFF ∧ 가격 ON」은 정상 상태이지 결함이 아니다)
    - `/Users/alex/repos/gh-radar/webapp/src/components/ui/tooltip.tsx` (shadcn Tooltip — Radix Portal 여부를 실측한다)
    - `/Users/alex/repos/gh-radar/webapp/src/styles/globals.css` §2.2b 의 「컨테이너를 걸면 하단 고정 바가 깨진다」 문단 (layout containment ↔ `position:fixed` — 툴팁이 Portal 이 아니면 같은 함정에 걸린다)
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/strategy-badge.tsx` 120-150 (`strategyBadgesOf` — 이 저장소의 「서버 에코 → 표시 상태」 순수함수 본보기. **이 파일은 바꾸지 않는다**, D-23)
    - `/Users/alex/repos/gh-radar/packages/shared/src/relay.ts` (`RelayLimitChaser` 의 래치 3필드와 `buyWatchSide`)
  </read_first>
  <behavior>
    매도 LED — `latchLedStateOf("sell", server)`:
    - `server === null` → 회색 · 클릭 불가 · 툴팁 없음
    - `sellEnabled=false` → 회색 · 클릭 불가
    - `sellEnabled=true, sellEntryLatched=false` → 주황 · 클릭 가능
    - `sellEnabled=true, sellEntryLatched=true` → 초록 · 클릭 가능
    취소 LED — `latchLedStateOf("cancel", server)`:
    - `cancelQtyEnabled=false, cancelTradeEnabled=false` → 회색 · 클릭 불가 (**`cancelQtyTrackEnabled=true` 여도 회색**)
    - `cancelQtyEnabled=true, cancelEntryLatched=false` → 주황 · 클릭 가능
    - `cancelTradeEnabled=true, cancelEntryLatched=true` → 초록 · 클릭 가능
    매수 LED — `latchLedStateOf("buy", server)`:
    - `buyEnabled=false` → 회색 · 클릭 불가
    - `buyEnabled=true, buyWatchSide="0"` → **초록 · 클릭 불가** (매도잔량 기준 2단계 유지, `buyEntryLatched` 값과 무관)
    - `buyEnabled=true, buyWatchSide="1", buyEntryLatched=false` → 주황 · 클릭 가능
    - `buyEnabled=true, buyWatchSide="1", buyEntryLatched=true` → 초록 · 클릭 가능
    접근성:
    - 클릭 가능한 LED 는 `aria-pressed` 를 가진다(초록=true, 주황=false)
    - 클릭 불가 LED 는 `aria-pressed` 를 가지지 않는다
    - 모든 LED 가 색 밖의 텍스트 경로(라벨 또는 sr-only)를 가진다
  </behavior>
  <action>
① `webapp/src/components/trading/latch-led.tsx` 를 새로 만든다. 두 export:
  - `latchLedStateOf(kind: "buy"|"sell"|"cancel", server: RelayLimitChaser | null): { tone: "off"|"latent"|"armed"; clickable: boolean; label: string; tooltip: string | null }` — **순수 함수**. 위 `<behavior>` 표 그대로. 판정 근거는 **마지막 서버 에코 스냅샷 하나**이고 폼 더티값이나 LED 색을 다시 읽지 않는다(D-20).
  - `LatchLed` 컴포넌트 — `latchLedStateOf` 결과를 그린다. `tone` → `off`=`var(--muted-fg)` · `latent`=`var(--led-latent)` · `armed`=`var(--led-armed)`.

② 툴팁 문구는 17-RESEARCH §2 의 **C# 원문을 리터럴로** 옮긴다. 켜는 쪽/끄는 쪽/매도잔량 기준 3종. `server === null` 이면 툴팁은 `null` 이다(없다).

③ 라벨은 Task 2 게이트에서 사용자가 고른 안(A 또는 B)을 따른다. **어느 안이든 색 밖의 텍스트 경로가 반드시 있다**(A 는 보이는 라벨, B 는 `sr-only`).

④ 클릭 가능 표현: `cursor-pointer` + `aria-pressed`. 클릭 불가 LED 는 `button` 이 아니라 비상호작용 요소로 그리거나 `disabled` 를 준다 — 탭 순서에 잡히는 눌리지 않는 버튼을 만들지 않는다.

⑤ 툴팁 구현: `webapp/src/components/ui/tooltip.tsx` 를 실측해 Radix Portal 이면 그대로 쓴다. Portal 이 아니면 상따 본문의 layout containment 안에서 잘리므로 **Portal 경로를 쓰거나 `title` 속성으로 낮춘다** — 잘린 툴팁을 남기지 않는다. 선택 결과를 SUMMARY 에 기록한다.

⑥ **`strategy-badge.tsx` 는 건드리지 않는다**(D-23) — 래치 3종의 표면은 LED 이고 배지에 섞지 않는다.

⑦ `webapp/src/components/trading/__tests__/latch-led.test.tsx` 에 `<behavior>` 의 11 케이스 + 접근성 3케이스를 전부 단언한다. 특히 ⓐ `cancelQtyTrackEnabled=true` 단독이 회색인 케이스(Pitfall 5)와 ⓑ `buyWatchSide="0"` ∧ `buyEntryLatched=true` 가 **초록·클릭 불가**인 케이스(Pitfall 4)를 반드시 둔다.

⑧ 커밋 메시지 초안: `feat(17-07): 상따 래치 LED 컴포넌트·판정 순수함수 + C# 규칙 표 테스트 14케이스`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- latch-led</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm typecheck</automated>
    <fails_when>비영 종료코드, 또는 출력에 `error TS` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -cE "cancelEntryLatched|buyEntryLatched|LatchLed|latchLedStateOf" webapp/src/components/trading/strategy-badge.tsx</automated>
    <fails_when>출력이 `0` 이 아니면 실패 (D-23 을 어기고 래치 심볼을 배지 파일에 들였다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `webapp/src/components/trading/latch-led.tsx` 가 `latchLedStateOf` 와 `LatchLed` 를 export 한다.
    - `latchLedStateOf` 의 취소 분기가 `cancelQtyTrackEnabled` 를 읽지 않는다: `sed -n '/export function latchLedStateOf/,/^}/p' webapp/src/components/trading/latch-led.tsx | grep -c cancelQtyTrackEnabled` 가 `0` 이다.
    - 매수 분기에 `buyWatchSide` 비교가 있고 `"0"` 갈래가 `clickable: false` 를 돌려준다.
    - 17-RESEARCH §2 의 툴팁 원문 3종이 파일 안에 리터럴로 있다.
    - 테스트가 14케이스 이상이고 `cancelQtyTrackEnabled` 단독 회색 케이스와 `buyWatchSide="0"` ∧ `buyEntryLatched=true` 초록·클릭 불가 케이스를 포함한다.
    - `strategy-badge.tsx` 에 `cancelEntryLatched`·`buyEntryLatched`·`LatchLed`·`latchLedStateOf` 가 한 건도 없다(D-23 — 래치의 표면은 LED 다).
    - 툴팁 Portal 실측 결과가 SUMMARY 에 기록됐다.
    - webapp 테스트 + 루트 typecheck green.
  </acceptance_criteria>
  <done>LED 판정이 C# 정본과 같은 순수 함수로 있고, 두 상습 오독(취소 추적 게이트·매도잔량 기준 매수)이 테스트로 막혔다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (17-07)

**CSS 토큰:** `--led-latent`(주황 · 잠복) · `--led-armed`(초록 · 래치 ON) — `:root` 와 `.dark` 양쪽

**함수:** `latchLedStateOf(kind, server)` — 순수 판정 (`webapp/src/components/trading/latch-led.tsx`)

**컴포넌트:** `LatchLed`

**신규 파일:** `.planning/phases/17-gh-trade-led/17-latch-led-mockup.html` · `webapp/src/components/trading/latch-led.tsx` · `webapp/src/components/trading/__tests__/latch-led.test.tsx`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 에코 스냅샷 → LED 색 | LED 가 「발주 판정이 시작됐다」를 말하는 유일한 표면이다 |
| LED 색 → 사용자 판단 → 실계좌 주문 | 오독이 곧 원치 않는 발주다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-23 | Spoofing | 매도잔량 기준(`buyWatchSide="0"`)에 3단계 LED | critical | mitigate | 서버가 그 갈래 래치를 켜지 않아 언제나 false — 2단계 유지·클릭 불가를 테스트로 고정(Pitfall 4) |
| T-17-24 | Spoofing | 취소 무장에 `cancelQtyTrackEnabled` 포함 | high | mitigate | 판정 함수 안에서 그 필드 grep 0 게이트(Pitfall 5) |
| T-17-25 | Information Disclosure | 색만으로 상태 전달 | medium | mitigate | 라벨 또는 sr-only 필수 + 테스트 단언(WCAG 1.4.1) |
| T-17-26 | Denial of Service | 툴팁이 layout containment 에 잘림 | low | accept | Portal 또는 `title` 로 낮춤. 잘린 상태로 남기지 않음이 조건 |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 plan 은 패키지 설치가 없다 — shadcn Tooltip 은 이미 저장소에 있다 |
</threat_model>

<verification>
- 사용자 목업 승인 기록 존재
- `pnpm --filter @gh-radar/webapp test -- latch-led` · `pnpm typecheck` · `pnpm --filter @gh-radar/webapp run build` green
- `strategy-badge.tsx` diff 0
</verification>

<success_criteria>
- 목업이 사용자 확인을 거쳤고 답이 문서로 남았다.
- LED 규칙이 C# 정본과 동형이고 14케이스 이상으로 굳었다.
- 색 토큰이 라이트/다크 둘 다 있고 §2.2b 정본이 손대지지 않았다.
</success_criteria>

<output>
Create `.planning/phases/17-gh-trade-led/17-07-SUMMARY.md` when done
</output>