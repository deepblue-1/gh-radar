---
phase: 17-gh-trade-led
plan: 11
type: execute
wave: 7
depends_on: ["17-04", "17-07"]
files_modified:
  - webapp/src/components/trading/limit-chaser-client.tsx
  - webapp/src/components/trading/strategy-log.tsx
  - webapp/src/components/trading/__tests__/limit-chaser-client.test.tsx
  - webapp/src/components/trading/__tests__/strategy-log.test.tsx
  - webapp/e2e/specs/trading-limit-chaser.spec.ts
autonomous: false
requirements: [TRADE-04, TRADE-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "상따 화면 상태줄에 매수·매도·취소 LED 3개가 그 순서로 있고 §2.2b 4밴드 안에서만 반응한다 (D-22)"
    - "클릭 가능한 LED 를 누르면 확인 다이얼로그 없이 `{t:\"lc.arm\"}` 1건이 나가고, 무반응 조건에서는 아무것도 나가지 않는다 (D-20)"
    - "전략 로그가 취소·매수 래치 전이 4종을 말하고, 래치 필드 변경이 「서버 반영 완료」로 오인되지 않는다 (D-23)"
    - "상따 화면의 서버 메시지에 `[상따]`/`[VI]`/`[서버]` 배지가 붙는다 (D-17)"
    - "`strategyBadgesOf` 는 변경되지 않는다 — 래치 3종의 표면은 LED 다 (D-23)"
  artifacts:
    - webapp/src/components/trading/__tests__/limit-chaser-client.test.tsx
  key_links:
    - "latchLedStateOf(17-07) → StatusBar LED 3개 → 클릭 → send({t:\"lc.arm\"}) → relay(17-04) → 36/37/38"
    - "60 에코 → strategyLogLine 전이 4종 + valuesChanged skip"
---

<objective>
17-07 의 LED 컴포넌트와 17-04 의 `lc.arm` 전송 경로를 상따 화면에서 잇는다. 사용자가 LED 를 클릭하면 래치가 토글되고, 결과는 60 에코가 색으로, 실패는 서버 메시지가 문구로 말한다. 겸해서 이 화면의 전략 로그와 서버 메시지 표기를 보정한다.

Purpose: TRADE-05 가 사용자에게 도달하는 지점이다. 여기까지 와야 「상따 래치 LED 3종 + 수동 점등」이 완성된다.
Output: 상태줄 LED 3개 + 클릭 전송, 전략 로그 전이 4종, 서버 메시지 배지, 상따 헤더 하한 칸 종가 표기.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gh-trade-led/17-CONTEXT.md
@.planning/phases/17-gh-trade-led/17-RESEARCH.md
@.planning/phases/17-gh-trade-led/17-04-SUMMARY.md
@.planning/phases/17-gh-trade-led/17-07-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 상태줄 LED 3개 배치 + 클릭 → `lc.arm` 전송</name>
  <files>webapp/src/components/trading/limit-chaser-client.tsx, webapp/src/components/trading/__tests__/limit-chaser-client.test.tsx, webapp/e2e/specs/trading-limit-chaser.spec.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/limit-chaser-client.tsx` 975-1060 (`statusOf` · `StatusBar` — 현재 DMA/매수/매도 칩 3개와 `Dot` 사용) · 380-420 (전략 로그 푸시 · 서버 메시지 소비) · `send` 를 얻는 자리
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/latch-led.tsx` (17-07 산출물 — `latchLedStateOf` 반환 형태)
    - `/Users/alex/repos/gh-radar/webapp/src/styles/globals.css` §2.2b 전체 (밴드 표 · 「상따 본문 안에 뷰포트 폭 분기를 새로 만들지 마라」 · layout containment ↔ `position:fixed` 문단)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-19 · D-20 · D-21 · D-22
    - 17-CONTEXT D-21 채택안(칩 · 라벨 OFF/대기/감시) 과 17-07-SUMMARY
  </read_first>
  <behavior>
    - 상태줄에 LED 3개가 매수 · 매도 · 취소 순서로 그려진다
    - 서버 전략이 없으면(`server === null`) 세 LED 가 모두 회색이고 클릭해도 아무것도 나가지 않는다
    - 클릭 가능한 매도 LED 클릭 → `{t:"lc.arm", key:<전략키>, latch:"sell"}` 1건이 `send` 로 나간다. 확인 다이얼로그가 뜨지 않는다
    - 취소 LED → `latch:"cancel"` · 매수 LED → `latch:"buy"`
    - 회색 LED 클릭 → `send` 가 불리지 않는다
    - `buyWatchSide === "0"`(매도잔량 기준) 매수 LED 클릭 → `send` 가 불리지 않는다
    - 연타해도 클릭 수만큼 나간다(토글이라 큐잉·중복 억제를 하지 않는다) — 다만 `send` 실패 반환 시 재시도하지 않는다
  </behavior>
  <action>
① `StatusBar` 에 LED 3개를 더한다. 자리는 현재 「DMA 연결」 칩 옆, 순서는 **매수 · 매도 · 취소**(D-22). 기존 매수/매도 텍스트 칩(`s.buyLabel`/`s.sellLabel`)과 **중복 표시가 되지 않게** 정리한다 — 무장 상태를 두 표기가 서로 다르게 말하면 사용자가 어느 쪽을 믿을지 모른다. 어떻게 정리했는지(칩 제거 / LED 가 칩을 대체 / 둘 다 유지하되 역할 분담)를 SUMMARY 에 기록한다.

② 반응형은 **§2.2b 컨테이너 쿼리 안에서만** 한다. 상따 본문에 뷰포트 폭 분기(`sm:`/`md:`/`lg:`)를 새로 만들지 않는다. 폰 밴드에서 한 줄에 들어가게 라벨을 짧게 둔다(17-07 사용자 답 기준). 상태줄은 이미 `flex-wrap` 이므로 넘치면 줄이 바뀐다 — 잘리지는 않는지 확인한다.

③ 클릭 핸들러: `latchLedStateOf(...).clickable === true` 일 때만 `send({ t: "lc.arm", key: item.key, latch })` 를 부른다. **확인 다이얼로그 없다**(D-20). 낙관적 색 변경을 하지 않는다 — 색의 근거는 오직 서버 에코 스냅샷이다. `send` 가 false 를 돌려주면 재전송하지 않는다(사용자가 누르지 않은 두 번째 요청을 만들지 않는다 — 16-RESEARCH Pitfall 8 규율).

④ 전략 키는 `item.key`(= `ISIN:accountNo:exchange`) 를 그대로 쓴다. 화면에서 키를 조립하지 않는다 — relay 파서가 넣어 준 값이 정본이다.

⑤ `<behavior>` 7케이스를 `limit-chaser-client.test.tsx` 에 단언한다. `send` 를 목으로 캡처해 인자를 직접 확인한다.

⑥ e2e `webapp/e2e/specs/trading-limit-chaser.spec.ts` 에 LED 3개가 보이는 케이스를 더한다(클릭 왕복은 17-12 의 mock 게이트웨이 검증에서 확인한다).

⑦ 커밋 메시지 초안: `feat(17-11): 상따 상태줄에 래치 LED 3종 배치 + 클릭 토글로 lc.arm 전송`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- limit-chaser-client</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "lc.arm" webapp/src/components/trading/limit-chaser-client.tsx</automated>
    <fails_when>카운트가 0 이면 실패 (클릭이 전송으로 이어지지 않는다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -cE "700px|830px|992px" webapp/src/styles/globals.css</automated>
    <fails_when>카운트가 3 미만이면 실패 (§2.2b 밴드 경계 리터럴이 사라졌다 — 이 plan 은 CSS 를 건드리지 않는다, 토큰은 17-07 소유다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `StatusBar` 가 `LatchLed` 를 3회 렌더하고 순서가 매수 · 매도 · 취소다.
    - 클릭 핸들러가 `clickable` 을 확인한 뒤에만 `send` 를 부른다.
    - 회색 LED 와 매도잔량 기준 매수 LED 클릭에서 `send` 미호출 단언이 각각 있다.
    - 확인 다이얼로그 컴포넌트가 LED 경로에 붙지 않았다.
    - `limit-chaser-client.tsx` 에 새 뷰포트 브레이크포인트 유틸(`sm:`/`md:`/`lg:`)이 추가되지 않았다 — 추가된 줄에서 해당 접두사 grep 0.
    - 기존 매수/매도 칩과의 중복 정리 방침이 SUMMARY 에 기록됐다.
    - webapp 테스트 green · CSS diff 0.
  </acceptance_criteria>
  <done>사용자가 상따 화면에서 래치를 눈으로 보고 클릭으로 토글할 수 있다. 무반응 조건에서는 아무것도 나가지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 전략 로그 래치 전이 4종 + skip 집합 + 서버 메시지 배지</name>
  <files>webapp/src/components/trading/strategy-log.tsx, webapp/src/components/trading/limit-chaser-client.tsx, webapp/src/components/trading/__tests__/strategy-log.test.tsx</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/strategy-log.tsx` 50-165 (전이 유니온 12종 · `TRANSITION_TEXT` · `TRANSITION_ORDER` · `cancelArmedOf` · `valuesChanged` skip 집합 · `strategyLogLine`)
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/__tests__/strategy-log.test.tsx`
    - `/Users/alex/repos/gh-radar/packages/shared/src/strategy-display.ts` (`serverMsgBadge`)
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/limit-chaser-client.tsx` 380-420 (서버 메시지 → 로그 푸시 자리 — 17-RESEARCH §3-3 이 〔추측〕으로 둔 지점이므로 **grep 으로 실측**한다)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-17 · D-23
  </read_first>
  <behavior>
    - `cancelEntryLatched` false→true → 로그에 취소 진입 래치 ON 문장이 나온다
    - `cancelEntryLatched` true→false → 취소 래치 해제 문장
    - `buyEntryLatched` false→true / true→false → 매수 래치 ON/해제 문장
    - 래치 두 필드**만** 바뀐 에코 → 「서버 반영 완료」가 함께 나오지 않는다
    - 첫 스냅샷(`prev === null`)에서 `cancelEntryLatched`/`buyEntryLatched` 가 true 여도 등록 줄에 래치 문장을 지어 붙이지 않는다(기존 `sellEntryLatched` 처리와 **같은 규율**인지 실측해 맞춘다)
    - `src === "LimitChaser"` 서버 메시지 → 로그 줄 앞에 `[상따]`
    - `src === "VITrigger"` → `[VI]` · 그 밖 → `[서버]`
  </behavior>
  <action>
① `StrategyTransition` 유니온에 `cancelLatched` · `cancelUnlatched` · `buyLatched` · `buyUnlatched` 4종을 더한다. **`TRANSITION_TEXT` 와 `TRANSITION_ORDER` 양쪽을 함께 고친다** — 파일 상단 주석이 「닫힌 집합」이라고 못박은 이유가 그것이다(문구만 있고 아무도 만들지 않는 전이 / 전이는 나는데 문구가 없는 사건 방지).

② 문구는 기존 `sellLatched`/`sellUnlatched` 와 **대구**로 쓴다(예: 취소 진입 래치 ON — 취소 판정 시작 / 취소 진입 래치 해제). 순서는 `TRANSITION_ORDER` 의 매수 → 매도 → 취소 축에 맞춰 각각 제자리에 끼운다.

③ `valuesChanged` 의 `skip` 집합에 `cancelEntryLatched` 와 `buyEntryLatched` 를 더한다. 빠지면 래치만 바뀐 에코가 「서버 반영 완료」로 보고돼 사용자가 자기가 안 누른 수정이 반영된 줄 안다.

④ `strategyLogLine` 의 전이 판정에 4종을 더한다. `prev === null` 갈래의 처리는 기존 `sellEntryLatched` 가 하는 것과 **똑같이** 맞춘다(실측해서 같은 규율을 쓴다 — 한쪽만 다르면 첫 스냅샷에서 매도와 취소가 다르게 보고된다).

⑤ 서버 메시지 배지: `limit-chaser-client.tsx` 의 서버 메시지 표시/로그 푸시 자리를 grep 으로 실측하고 `serverMsgBadge(msg.src)` 를 접두로 붙인다. 컴포넌트 안에서 `src` 를 직접 비교하지 않는다.

⑥ **`strategy-badge.tsx` 는 건드리지 않는다**(D-23).

⑦ `<behavior>` 8케이스를 `strategy-log.test.tsx` 와 `limit-chaser-client.test.tsx` 에 나눠 단언한다.

⑧ 커밋 메시지 초안: `feat(17-11): 전략 로그 취소·매수 래치 전이 4종 + 값 변경 skip 2필드 + 서버 메시지 출처 배지`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- strategy-log</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "cancelLatched\|cancelUnlatched\|buyLatched\|buyUnlatched" webapp/src/components/trading/strategy-log.tsx</automated>
    <fails_when>카운트가 8 미만이면 실패 (유니온·문구 표·순서 표 세 곳에 4종이 다 들어가지 않았다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -cE "cancelEntryLatched|buyEntryLatched|LatchLed|latchLedStateOf" webapp/src/components/trading/strategy-badge.tsx</automated>
    <fails_when>출력이 `0` 이 아니면 실패 (D-23 위반 — 배지 파일에 래치 심볼이 들어갔다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `StrategyTransition` 유니온 원소가 16종이다.
    - `TRANSITION_TEXT` 와 `TRANSITION_ORDER` 가 각각 16개 원소를 가진다.
    - `valuesChanged` 의 `skip` 집합에 `cancelEntryLatched` 와 `buyEntryLatched` 가 있다.
    - 래치 두 필드만 바뀐 에코가 「서버 반영 완료」를 내지 않는 케이스가 테스트에 있다.
    - `limit-chaser-client.tsx` 가 `serverMsgBadge` 를 쓰고 같은 파일의 `src ===` 직접 비교가 0건이다.
    - `strategy-badge.tsx` 에 래치 심볼 4종이 한 건도 없다(D-23).
    - 서버 메시지 표시 자리 실측 경로가 SUMMARY 에 기록됐다.
    - strategy-log 테스트 green.
  </acceptance_criteria>
  <done>래치 전이가 로그에 남고, 래치 변화가 값 수정으로 오인되지 않으며, 서버 메시지가 출처를 말한다.</done>
</task>

<task type="auto">
  <name>Task 3: 상따 헤더 `하한` 칸 — `kc > 0` 이면 `종가` 로 대체 (사용자 결정 2026-09-18)</name>
  <files>webapp/src/components/trading/limit-chaser-client.tsx, webapp/src/components/trading/__tests__/limit-chaser-client.test.tsx</files>
  <precondition>CONTEXT D-11(개정)을 읽었다 — 결정은 계획 단계에서 끝났다: 「헤더 배치 유지하고, 종가가 있을땐 하한가 대신에 종가를 보여줘」.</precondition>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/limit-chaser-client.tsx` 670-770 (`lc-quote-grid` — `QuoteCell` 10개와 `order-[N]` 밴드 배치)
    - `/Users/alex/repos/gh-radar/webapp/src/components/stock/stock-orderbook-section.tsx` (17-08 이 적용한 `kc` 갈래 — 같은 규칙을 쓴다)
    - `/Users/alex/repos/gh-radar/packages/shared/src/relay.ts` (`RelayQuote.kc` 주석 — 벽시계 판정 금지, `0` 도 권위값)
  </read_first>
  <action>
사용자 결정(계획 단계, 2026-09-18): **헤더 10칸 배치 유지, `하한` 칸을 `kc` 로 가른다.**
  - `lc-quote-grid` 의 `하한` `QuoteCell` 하나만 바꾼다: `kc > 0` 이면 라벨 `종가` · 값 `priceText(kc)` · 톤은 `priceTone(kc, basePrice)`(기준가 대비 방향색 — 다른 칸과 같은 규칙), `kc === 0` 이면 종전 그대로(라벨 `하한` · 값 `lowerLimit` · `text-[var(--down)]`).
  - `order-[N]` 배치 번호는 **건드리지 않는다**(§2.2b 실측값). 새 포매터를 만들지 않는다. 벽시계로 판정하지 않는다(`new Date()` · 장 시간 상수 금지). `0` 도 권위값이다.
  - NXT 프레임에도 KRX 종가가 실리므로 라벨은 그대로 `종가`.
  - 테스트: `kc > 0` → 라벨 `종가`·값, `kc === 0` → 라벨 `하한`·값 두 갈래를 각각 단언. `order-[` 카운트 전/후 동일을 SUMMARY 에 기록.

커밋 메시지 초안: `feat(17-11): 상따 헤더 하한 칸을 KRX 정규장 종가가 있으면 종가로 표기`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && grep -cE "700px|830px|992px" webapp/src/styles/globals.css</automated>
    <fails_when>카운트가 3 미만이면 실패 (§2.2b 밴드 경계 리터럴이 사라졌다 — 밴드 정본을 건드렸다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "order-\[" webapp/src/components/trading/limit-chaser-client.tsx</automated>
    <fails_when>카운트가 작업 전 값과 달라지면 실패 — 작업 전 값을 먼저 재서 SUMMARY 에 적는다 (밴드 배치 번호가 바뀌었다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test && pnpm typecheck</automated>
    <fails_when>비영 종료코드, vitest 요약 줄에 `failed`, 또는 출력에 `error TS` 가 나오면 실패</fails_when>
  </verify>
  <acceptance_criteria>
    - `하한` `QuoteCell` 만 `quote?.kc` 로 갈리고(다른 9칸 무변경) `kc > 0`·`kc === 0` 두 갈래가 테스트에 있다.
    - `order-[N]` 배치 번호 개수가 작업 전과 같다(전/후 값 SUMMARY 기록).
    - §2.2b 밴드 경계 리터럴이 그대로다.
    - webapp 테스트 + 루트 typecheck green.
  </acceptance_criteria>
  <done>상따 헤더 하한 칸이 종가가 있을 때 종가를 보여 주고 밴드 배치는 손대지 않았다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (17-11)

**UI:** 상따 상태줄 래치 LED 3개(매수·매도·취소) + 클릭 토글 → `{t:"lc.arm"}` 전송

**전략 로그 전이 값:** `cancelLatched` · `cancelUnlatched` · `buyLatched` · `buyUnlatched` (+ `TRANSITION_TEXT`/`TRANSITION_ORDER` 대응 항목)

**규칙:** `valuesChanged` skip 집합에 `cancelEntryLatched`·`buyEntryLatched` 추가 · 상따 화면 서버 메시지 출처 배지

**결정 기록(계획 단계 확정):** 상따 헤더 10칸 유지 · `하한` 칸을 `kc>0` 이면 `종가` 로 대체
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LED 클릭 → `lc.arm` → 게이트웨이 | 래치 ON 은 발주 판정을 시작시킨다 — 의도치 않은 클릭이 곧 발주 위험이다 |
| 60 에코 → LED 색 | 낙관적 색 변경은 「켜졌다」는 거짓을 만든다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-37 | Elevation of Privilege | 무반응 조건에서의 전송 | high | mitigate | `clickable` 확인 후에만 `send` + 미호출 단언 2건 |
| T-17-38 | Spoofing | 낙관적 LED 색 | high | mitigate | 색 근거를 서버 에코 스냅샷 하나로 고정(D-20) — 낙관적 상태 금지 |
| T-17-39 | Repudiation | 래치 변경이 「서버 반영 완료」로 보고 | medium | mitigate | `valuesChanged` skip 집합 확장 + 전용 테스트 |
| T-17-40 | Denial of Service | `send` 실패 시 자동 재전송 | high | mitigate | 재전송 금지(사용자가 누르지 않은 두 번째 요청을 만들지 않는다) |
| T-17-41 | Tampering | §2.2b 밴드 정본 변경 | medium | mitigate | `globals.css` diff 0 게이트 |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 plan 은 패키지 설치가 없다 |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/webapp test` · `pnpm typecheck` green
- §2.2b 밴드 경계 리터럴 보존 · `strategy-badge.tsx` 에 래치 심볼 0건
- e2e `trading-limit-chaser.spec.ts` 에 LED 가시성 케이스 추가 후 통과
- 하한 칸 종가 대체가 테스트 두 갈래로 단언됨
</verification>

<success_criteria>
- LED 3개가 상태줄에 있고 클릭이 래치를 토글한다.
- 무반응 조건에서 아무것도 나가지 않는다.
- 전략 로그가 래치 전이 4종을 말하고 값 변경과 섞이지 않는다.
- 상따 헤더 하한 칸이 `kc>0` 이면 `종가` 를 보인다.
</success_criteria>

<output>
Create `.planning/phases/17-gh-trade-led/17-11-SUMMARY.md` when done
</output>