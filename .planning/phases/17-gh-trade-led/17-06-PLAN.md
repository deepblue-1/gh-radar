---
phase: 17-gh-trade-led
plan: 06
type: execute
wave: 6
depends_on: ["17-05"]
files_modified:
  - webapp/src/lib/use-relay-socket.ts
  - webapp/src/lib/relay-provider.tsx
  - webapp/src/components/trading/vi-client.tsx
  - webapp/src/components/trading/vi-settings-card.tsx
  - webapp/src/components/trading/vi-order-list.tsx
  - webapp/src/components/trading/me-client.tsx
  - webapp/src/components/trading/strategy-status-card.tsx
  - webapp/src/components/layout/app-sidebar.tsx
  - webapp/src/components/trading/__tests__/vi-order-list.test.tsx
  - webapp/src/components/trading/__tests__/vi-settings-card.test.tsx
  - webapp/src/lib/__tests__/relay-provider.test.tsx
  - webapp/e2e/specs/trading-vi.spec.ts
autonomous: true
requirements: [TRADE-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "브라우저가 KRX·NXT VI 전략을 각각 3상태(미조회/미등록/등록)로 안다 (D-06)"
    - "사이드바·My page·전략 현황의 VI 가동 배지가 KRX·NXT 어느 쪽이든 run 이면 가동이다 (D-18)"
    - "VI 설정 카드 캡션이 사실대로 KRX 편집임을 말하고 `vi.set` 에 `exchange:\"KRX\"` 를 싣는다 (D-18)"
    - "VI 주문 목록에 거래소 열이 있고 행 키가 ISIN+거래소라 양쪽 발동이 두 행으로 보인다 (D-06)"
    - "VI 화면의 서버 메시지에 `[상따]`/`[VI]`/`[서버]` 배지가 붙는다 (D-09 · D-17)"
  artifacts:
    - webapp/src/components/trading/__tests__/vi-order-list.test.tsx
  key_links:
    - "relay `{t:\"vi\", x, cfg}` → applyFrame → RelayData.viTriggers(거래소별) → 사이드바/My page/전략 현황 배지"
    - "vi-settings-card 저장 → `{t:\"vi.set\", exchange:\"KRX\"}` → relay → SetVITrigger.exchange"
---

<objective>
relay 가 거래소별로 내려주기 시작한 VI 전략을 웹이 거래소별로 이해하게 만든다. 가동 배지는 두 거래소의 합집합으로 판정하고, 설정 카드는 지금 무엇을 편집하는지 사실대로 말하며, 주문 목록은 거래소를 보여 준다.

Purpose: Phase 17 범위의 VI 는 **KRX 편집 유지 + NXT 가시화**다. NXT 설정 카드는 Phase 18 이다(D-18) — 캡션이 「KRX 전용」이라고 단정하면 NXT 전략이 존재하는데도 화면이 거짓말을 한다.
Output: 거래소별 리듀서 상태, 소비처 6곳 갱신, 캡션·송신 정정, 거래소 열, 서버 메시지 배지.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gh-trade-led/17-CONTEXT.md
@.planning/phases/17-gh-trade-led/17-RESEARCH.md
@.planning/phases/17-gh-trade-led/17-05-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 리듀서 `viTriggers` 거래소별 + 소비처 6곳 — NXT 전략이 화면에 존재한다</name>
  <files>webapp/src/lib/use-relay-socket.ts, webapp/src/lib/relay-provider.tsx, webapp/src/components/layout/app-sidebar.tsx, webapp/src/components/trading/me-client.tsx, webapp/src/components/trading/strategy-status-card.tsx, webapp/src/components/trading/vi-client.tsx, webapp/src/lib/__tests__/relay-provider.test.tsx</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/lib/use-relay-socket.ts` 190-320 (`RelayData` · 초기값) · 348-430 (`applyFrame`, `case "vi":` 의 3상태 주석) · 920-940 (컨텍스트 노출부)
    - `/Users/alex/repos/gh-radar/webapp/src/lib/relay-provider.tsx` 195-215 (기본값 객체)
    - `/Users/alex/repos/gh-radar/webapp/src/components/layout/app-sidebar.tsx` 265-340 (`viBadgeOf(viTrigger?.run === true)`)
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/me-client.tsx` 150-190 (`viTrigger?.run === true ? "가동" : "중지"`)
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/strategy-status-card.tsx` 330-360 · 465-475 (`viRunning` · `viSummaryText`)
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/vi-client.tsx` 110-240 · 275-290 · 350-365 (`viTrigger` 소비 7곳 — 계좌 축 · 전이 감지 ref · `run` · 카드에 넘기는 `server`)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-06 · D-18
  </read_first>
  <behavior>
    - `{t:"vi", x:"NXT", cfg:{run:true,...}}` 수신 → 사이드바 VI 항목에 「가동」 배지가 뜬다
    - `{t:"vi", x:"KRX", cfg:null}` 만 받은 상태 → 「중지」 (NXT 는 아직 모름)
    - KRX `run:false` + NXT `run:true` → 가동 (합집합 판정)
    - KRX `cfg` 만 받고 NXT 는 미수신 → KRX 카드는 그 값으로 그려지고 NXT 미조회가 KRX 표시를 지우지 않는다
    - `viTriggers` 초기 상태에서 두 거래소 모두 `undefined`(아직 모름)다
  </behavior>
  <action>
① `webapp/src/lib/use-relay-socket.ts` 의 `RelayData.viTrigger` 를 거래소별 구조로 바꾼다: `viTriggers: Partial<Record<RelayExchange, RelayViTrigger | null>>`. **3상태를 거래소마다 독립으로 유지한다** — 키 부재=아직 모름, `null`=조회 결과 미등록, 객체=등록. 한 거래소의 프레임이 다른 거래소 값을 지우지 않는다.

② `applyFrame` 의 `case "vi":` 를 `frame.x` 키로 병합하도록 바꾼다. 기존 「`cfg: null` 은 미등록이지 무응답이 아니다 — undefined 로 되돌리지 않는다」 주석을 거래소별 문장으로 이어 쓴다.

③ 컨텍스트 노출과 `relay-provider.tsx` 기본값을 갱신한다. **하위호환 별칭(`viTrigger`)을 남기지 않는다** — 남기면 어느 거래소의 값인지 모르는 소비처가 조용히 살아남는다. `pnpm typecheck` 가 소비처 전부를 지목하게 둔다.

④ 파생 헬퍼 하나를 `webapp/src/lib` 에 둔다: `viAnyRunning(viTriggers): boolean` — 두 거래소 중 하나라도 `cfg?.run === true` 면 true. 사이드바(`viBadgeOf`)·My page(`me-client`)·전략 현황(`strategy-status-card`)이 **이 함수만** 부른다(판정이 세 벌이 되면 갈린다).

⑤ `vi-client.tsx` 의 `viTrigger` 소비 7곳을 **KRX 값**으로 갱신한다(Phase 17 의 편집 대상은 KRX 다 — D-18). 계좌 축·전이 감지 ref·`run`·카드 `server` prop 전부 KRX 기준. NXT 값은 이 태스크에서 화면에 쓰지 않는다(가동 배지 합집합만).

⑥ `<behavior>` 5케이스를 `webapp/src/lib/__tests__/relay-provider.test.tsx` 와 사이드바 테스트에 나눠 단언한다.

⑦ 커밋 메시지 초안: `feat(17-06): VI 전략 상태를 거래소별로 — 리듀서·가동 배지 합집합·소비처 6곳 갱신`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm typecheck</automated>
    <fails_when>비영 종료코드, 또는 출력에 `error TS` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -rc "viAnyRunning" webapp/src/components/layout/app-sidebar.tsx webapp/src/components/trading/me-client.tsx webapp/src/components/trading/strategy-status-card.tsx</automated>
    <fails_when>세 파일 중 어느 하나라도 카운트가 0 이면 실패 (가동 판정이 한 함수로 모이지 않았다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `webapp/src/lib/use-relay-socket.ts` 에 `viTriggers` 가 있고 `viTrigger` 단수 필드는 남아 있지 않다.
    - `applyFrame` 의 `case "vi":` 가 `frame.x` 를 키로 쓴다.
    - `viAnyRunning` 이 `webapp/src/lib` 에 정의되고 사이드바·me-client·strategy-status-card 세 곳에서 호출된다.
    - 세 소비처에 `?.run === true` 인라인 판정이 남아 있지 않다.
    - `<behavior>` 5케이스가 테스트에 있다.
    - 루트 typecheck + webapp 테스트 green.
  </acceptance_criteria>
  <done>NXT 에 등록된 VI 전략이 사이드바·My page·전략 현황에 「가동」으로 나타나고, 거래소별 3상태가 서로를 지우지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VI 설정 카드 — 캡션 사실 정정 + `exchange:"KRX"` 송신</name>
  <files>webapp/src/components/trading/vi-settings-card.tsx, webapp/src/components/trading/__tests__/vi-settings-card.test.tsx</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/vi-settings-card.tsx` 1-30 (상단 주석 「종목 축이 없다 … KRX 전용」) · 81 (`VI_SETTINGS_CAPTION`) · 425-440 (캡션 렌더) · 820-840 (요약 `주문가` 행) · `vi.set` 전송 지점
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/__tests__/vi-settings-card.test.tsx` (캡션 단언이 있으면 그 자리)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-18
    - `/Users/alex/repos/gh-radar/.planning/phases/16-trading-limit-chaser-vi-my-page/16-CONTEXT.md` D-07 (VI 수정 버튼·시작/중지 확인 다이얼로그 규율 — 바꾸지 않는다)
  </read_first>
  <behavior>
    - 카드 캡션이 지금 편집 대상이 KRX 이고 NXT 는 다음 phase 임을 말한다
    - 저장(수정/시작/중지) 시 relay 로 나가는 `vi.set` 메시지에 `exchange: "KRX"` 가 실린다
    - 16-CONTEXT D-07 의 조작 규율은 그대로다 — 값 변경은 수정 버튼, 시작/중지는 확인 다이얼로그
  </behavior>
  <action>
① `VI_SETTINGS_CAPTION` 문구를 사실대로 바꾼다. 현재 문구는 세션당 1건 · KRX · 주문가 상한가를 말하는데, 서버는 **거래소별 1건**이므로 「세션당 1건」이 이제 틀렸다. 새 캡션은 (a) 거래소별 1건이라는 사실, (b) 이 카드가 편집하는 것은 KRX 라는 사실, (c) NXT 설정은 다음 phase 라는 사실을 담는다. 정확한 문구는 재량이되 세 사실을 모두 담아야 한다.

② 파일 상단 주석의 「KRX 전용」 서술을 같은 사실로 고친다 — 주석이 캡션과 갈리면 다음 사람이 어느 쪽을 믿을지 모른다.

③ `vi.set` 을 만드는 자리에 `exchange: "KRX"` 를 싣는다. **리터럴을 컴포넌트에 흩뿌리지 않는다** — 파일 상단 상수 하나(`VI_EDIT_EXCHANGE`)로 두고 캡션·송신이 그 상수를 쓴다. Phase 18 이 이 상수를 상태로 바꾸면 끝나게 둔다.

④ 요약 행의 `상한가 · KRX` 표기는 거래소 상수를 쓰도록 바꾼다.

⑤ `<behavior>` 3케이스를 `vi-settings-card.test.tsx` 에 단언한다. 송신 케이스는 `send` 목을 캡처해 `exchange` 키를 직접 확인한다.

⑥ 커밋 메시지 초안: `fix(17-06): VI 설정 카드 캡션을 거래소별 1건·KRX 편집 사실로 정정 + vi.set 에 거래소 송신`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- vi-settings-card</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "VI_EDIT_EXCHANGE" webapp/src/components/trading/vi-settings-card.tsx</automated>
    <fails_when>카운트가 2 미만이면 실패 (거래소 상수가 캡션·송신 두 곳에서 쓰이지 않는다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `VI_SETTINGS_CAPTION` 문구에 거래소별 1건 · KRX 편집 · NXT 다음 phase 세 사실이 모두 담겨 있다.
    - 파일 상단 주석의 서술이 새 캡션과 모순되지 않는다.
    - `vi.set` 전송 객체에 `exchange` 키가 있고 값이 `VI_EDIT_EXCHANGE` 상수에서 온다.
    - 컴포넌트 JSX 안에 `"KRX"` 문자열 리터럴이 직접 박혀 있지 않다(상수 정의 줄은 예외).
    - 16-CONTEXT D-07 의 수정 버튼·확인 다이얼로그 동작을 바꾸지 않았다 — 기존 테스트가 그대로 통과한다.
    - vi-settings-card 테스트 green.
  </acceptance_criteria>
  <done>카드가 지금 무엇을 편집하는지 사실대로 말하고, 등록이 거래소를 명시해 나간다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: VI 주문 목록 거래소 열 · 행 키 + 발동통보·서버메시지 표기</name>
  <files>webapp/src/components/trading/vi-order-list.tsx, webapp/src/components/trading/vi-client.tsx, webapp/src/components/trading/__tests__/vi-order-list.test.tsx, webapp/e2e/specs/trading-vi.spec.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/vi-order-list.tsx` 20-60 (`orderNo === ""` 행 키 주석 — 「서로 다른 종목의 접수 전 행이 한 줄로 겹친다」) · 230-300 (낙관적 확인 상태 Map 들) · 345-370 (표 헤더 7열)
    - `/Users/alex/repos/gh-radar/webapp/src/components/trading/vi-client.tsx` 의 `viNotices` 렌더 지점과 `messages` 렌더 지점 (grep 으로 실측한다 — 17-RESEARCH §3-3 은 이 자리를 〔추측〕으로 표기했다)
    - `/Users/alex/repos/gh-radar/packages/shared/src/strategy-display.ts` (`serverMsgBadge` — 17-01 산출물)
    - `/Users/alex/repos/gh-radar/webapp/src/styles/globals.css` §2.2b 상단 주석 (밴드 규율 — VI 화면은 뷰포트 브레이크포인트를 쓴다. 컨테이너 쿼리 대상이 아니다)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-06 · D-17 · D-18
  </read_first>
  <behavior>
    - 같은 ISIN 이 KRX·NXT 양쪽에서 발동한 접수 전 행 2건 → 표에 **2행**이 그려지고 React key 가 충돌하지 않는다
    - 표에 거래소 열이 있고 각 행이 자기 거래소를 보여 준다
    - `vi.notice` 표시에 거래소가 함께 나온다
    - `src === "LimitChaser"` 인 서버 메시지 → 앞에 `[상따]`
    - `src === "VITrigger"` → `[VI]` · `src === "Account"` → `[서버]`
    - 낙관적 확인 체크 상태 Map 의 키가 여전히 주문번호다 — 접수 전 행은 체크가 비활성이므로 영향이 없다
  </behavior>
  <action>
① `vi-order-list.tsx` 에 거래소 열을 더한다. 열 위치는 「종목」 오른쪽. 헤더 `scope="col"`. 폰 폭에서 표가 잘리지 않는지 확인한다 — 잘리면 열 대신 종목 셀 아래 보조 텍스트로 낮춘다(그리드/플렉스 자식의 `min-w-0` 규율은 이 저장소의 상습 함정이다).

② 행 키를 ISIN+거래소 기준으로 바꾼다. **주문번호가 있는 행은 종전대로 주문번호를 키로 쓴다** — 바꾸는 것은 `orderNo === ""` 인 접수 전 행의 대체 키뿐이다(파일 상단 주석이 이미 그 사유를 적어 두었다). 낙관적 확인 Map 들(`optimistic`/`sending`)의 키는 **주문번호 그대로 둔다**(접수 전 행은 확인이 비활성이므로 키를 바꿀 이유가 없고, 바꾸면 두 Map 의 키 축이 갈린다).

③ `vi-client.tsx` 의 `vi.notice` 표시에 거래소를 더한다. 표시 자리는 grep 으로 실측해 찾는다 — 이 위치는 연구 문서에서 확정되지 않았다.

④ `vi-client.tsx` 의 서버 메시지 표시 자리에 `serverMsgBadge(msg.src)` 를 적용한다. 배지는 **텍스트 접두**로 붙이고 색만으로 구분하지 않는다. 판정은 `serverMsgBadge` 한 함수만 쓴다 — 컴포넌트 안에서 `src` 를 직접 비교하지 않는다.

⑤ `<behavior>` 6케이스를 `vi-order-list.test.tsx` 와 vi-client 테스트에 나눠 단언한다. e2e `webapp/e2e/specs/trading-vi.spec.ts` 가 거래소 열 추가로 깨지면 셀렉터를 갱신한다.

⑥ 커밋 메시지 초안: `feat(17-06): VI 주문 목록 거래소 열·행 키 분리 + 발동통보 거래소 표기 + 서버 메시지 배지`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "serverMsgBadge" webapp/src/components/trading/vi-client.tsx</automated>
    <fails_when>카운트가 0 이면 실패 (VI 화면 서버 메시지에 배지가 붙지 않는다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm typecheck</automated>
    <fails_when>비영 종료코드, 또는 출력에 `error TS` 가 나오면 실패</fails_when>
  </verify>
  <acceptance_criteria>
    - `vi-order-list.tsx` 표 헤더에 거래소 열이 있다.
    - 같은 ISIN·다른 거래소의 접수 전 행 2건이 2행으로 그려지는 테스트가 있다.
    - `optimistic`/`sending` Map 의 키가 여전히 주문번호다(회귀 테스트 통과).
    - `vi-client.tsx` 가 `serverMsgBadge` 를 import 해 쓰고, 같은 파일에서 `src ===` 직접 비교가 0건이다.
    - `vi.notice` 표시 자리를 grep 으로 실측했고 그 경로를 SUMMARY 에 기록했다.
    - webapp 테스트 + 루트 typecheck green.
  </acceptance_criteria>
  <done>VI 주문 목록이 거래소를 말하고 양쪽 발동이 겹치지 않으며, VI 화면의 서버 메시지가 출처 배지를 단다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (17-06)

**webapp 상태:** `RelayData.viTriggers`(거래소별 3상태) — 기존 `viTrigger` 단수 필드는 제거

**함수:** `viAnyRunning(viTriggers)` (`webapp/src/lib`)

**상수:** `VI_EDIT_EXCHANGE` (`webapp/src/components/trading/vi-settings-card.tsx`) · 갱신된 `VI_SETTINGS_CAPTION`

**UI:** VI 주문 목록 거래소 열 · 접수 전 행 대체 키(ISIN+거래소) · VI 화면 서버 메시지 `[상따]`/`[VI]`/`[서버]` 배지
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay 프레임 → 브라우저 상태 | 거래소 값이 어느 시장에 발주되는지를 사용자에게 말하는 유일한 근거다 |
| 브라우저 → relay (`vi.set`) | 거래소가 실제 발주 시장을 가른다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-20 | Spoofing | 캡션이 「KRX 전용」이라 단정 | high | mitigate | 캡션을 거래소별 1건 사실로 정정 — NXT 전략 존재를 숨기지 않는다 |
| T-17-21 | Tampering | 가동 판정이 세 소비처에 흩어짐 | medium | mitigate | `viAnyRunning` 단일 함수 + 인라인 판정 0 게이트 |
| T-17-22 | Repudiation | 양쪽 거래소 발동이 한 행으로 겹침 | high | mitigate | 접수 전 행 대체 키에 거래소 포함 + 2행 테스트 |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 plan 은 패키지 설치가 없다 |
</threat_model>

<verification>
- `pnpm typecheck` · `pnpm --filter @gh-radar/webapp test` green
- e2e `trading-vi.spec.ts` 가 거래소 열 추가 뒤에도 통과 (셀렉터 갱신 포함)
</verification>

<success_criteria>
- NXT VI 전략이 화면에 존재하고 가동 배지가 합집합으로 판정된다.
- VI 설정 카드가 사실대로 말하고 거래소를 명시해 등록한다.
- VI 주문 목록이 거래소를 보여 주고 행이 겹치지 않는다.
</success_criteria>

<output>
Create `.planning/phases/17-gh-trade-led/17-06-SUMMARY.md` when done
</output>