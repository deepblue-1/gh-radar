---
phase: 17-gh-trade-led
plan: 08
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - webapp/src/components/orderbook/trade-tape.tsx
  - webapp/src/components/orderbook/relay-status-bar.tsx
  - webapp/src/components/stock/stock-orderbook-section.tsx
  - webapp/src/components/orderbook/__tests__/trade-tape.test.tsx
  - webapp/e2e/specs/orderbook.spec.ts
autonomous: true
requirements: [TRADE-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "체결 테이프의 매수/매도 색이 서버 체결구분(`bs`)을 먼저 쓰고, 값이 없는 원소에서만 추정으로 떨어진다 (D-10)"
    - "하단 고지가 실제로 쓴 근거를 말한다 — 폴백을 한 건도 안 썼으면 「추정」이라고 하지 않는다 (D-10)"
    - "호가 종목정보의 하락VI 자리가 `kc > 0` 이면 종가로 바뀌고, `0` 이면 종전 표기를 유지한다 (D-11)"
    - "호가 상태바의 서버 메시지에 `[상따]`/`[VI]`/`[서버]` 배지가 붙는다 (D-09 · D-17)"
  artifacts:
    - webapp/src/components/orderbook/__tests__/trade-tape.test.tsx
  key_links:
    - "RelayTapeEntry.bs → tapeSidesOf() → 수량 색·sr-only·고지 문구"
    - "RelayQuote.kc → 호가 종목정보 dl 항목"
---

<objective>
서버가 이미 보내던 두 사실을 호가주문 표면에 드러낸다: 체결 한 건 한 건의 **매수/매도 구분**(추정이 아니라 거래소 원문)과 **KRX 정규장 종가**. 겸해서 이 표면의 서버 메시지에 출처 배지를 붙인다.

Purpose: 체결 테이프의 색은 지금 최우선호가·틱 규칙 **추정**이다. 서버가 `bs_code` 를 보내므로 추정을 쓸 이유가 사라졌다. 고지 문구가 계속 「추정」이라고 말하면 그것도 이제 거짓이다.
Output: `bs` 우선 색 판정 + 조건부 고지, 종가 표기, 상태바 배지.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gh-trade-led/17-CONTEXT.md
@.planning/phases/17-gh-trade-led/17-RESEARCH.md
@.planning/phases/17-gh-trade-led/17-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 체결 테이프 — 서버 체결구분 우선 · 값 없는 원소만 추정 폴백</name>
  <files>webapp/src/components/orderbook/trade-tape.tsx, webapp/src/components/orderbook/__tests__/trade-tape.test.tsx</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/components/orderbook/trade-tape.tsx` 120-160 (`deriveTapeSides` — zero-tick 상속 로직 전체) · 385-420 (수량 색 · sr-only · 하단 고지 렌더)
    - `/Users/alex/repos/gh-radar/webapp/src/components/orderbook/__tests__/trade-tape.test.tsx` (기존 `deriveTapeSides` 케이스 형식)
    - `/Users/alex/repos/gh-radar/packages/shared/src/relay.ts` (`RelayTapeEntry.bs` 주석 — `"1"` 매도 · `"2"` 매수)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-10
    - `/Users/alex/repos/gh-radar/tasks/lessons.md` 「손보는 표면 안의 시각 결함은 …그냥 고친다」 절 · 「그리드/플렉스 자식은 `min-w-0`」 절
  </read_first>
  <behavior>
    - 모든 원소의 `bs` 가 `"1"`/`"2"` → 색·sr-only 가 전부 서버값을 따르고 `deriveTapeSides` 는 호출되지 않는다
    - `bs` 가 `"2"` 인 원소 → 매수 색 · sr-only 가 매수
    - `bs` 가 `"1"` 인 원소 → 매도 색 · sr-only 가 매도
    - `bs` 가 `""` 인 원소가 섞여 있으면 **그 원소만** 기존 추정으로 채워지고 나머지는 서버값 그대로다
    - 폴백 원소가 0건이면 하단 고지가 서버 체결구분 기준임을 말한다
    - 폴백 원소가 1건 이상이면 하단 고지가 일부 추정임을 함께 말한다
  </behavior>
  <action>
① `webapp/src/components/orderbook/trade-tape.tsx` 에 `tapeSidesOf(entries, bestAsk, bestBid): { sides: TapeSide[]; usedFallback: boolean }` 를 둔다. 동작: 각 원소의 `bs` 가 `"2"` 면 매수, `"1"` 이면 매도로 확정하고, `""` 인 원소만 기존 `deriveTapeSides` 결과로 채운다. **`deriveTapeSides` 는 지우지 않는다** — 구 서버·낯선 값에서 여전히 필요하고, zero-tick 상속 규칙은 그 함수 안에 있어야 한다.

② 폴백을 쓴 원소가 하나라도 있으면 `usedFallback` 이 true 다. 폴백 판정을 컴포넌트 안에 인라인으로 흩뿌리지 않는다 — 고지 문구가 이 한 값만 본다.

③ 수량 색과 sr-only 텍스트가 `tapeSidesOf` 결과를 쓰게 바꾼다. 색 클래스·sr-only 구조는 그대로 둔다(레이아웃 폭을 차지하지 않는 현재 표현을 지킨다 — 실시간 표에서 폭이 흔들리면 매 틱 좌우로 떨린다는 기존 교훈).

④ 하단 고지 문구를 `usedFallback` 으로 갈라 두 문구를 둔다. 문구 자체는 재량이되 **(a) 폴백 0건일 때는 서버 체결구분이 근거임을, (b) 폴백 1건 이상일 때는 일부가 추정임을** 각각 말해야 한다. 두 문구를 파일 상단 상수로 두고 JSX 안에 리터럴을 박지 않는다.

⑤ `<behavior>` 6케이스를 `trade-tape.test.tsx` 에 단언한다. 「전부 서버값일 때 `deriveTapeSides` 미호출」은 스파이로 확인한다.

⑥ 커밋 메시지 초안: `feat(17-08): 체결 테이프 색을 서버 체결구분 우선으로 + 폴백 사용 시에만 추정 고지`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- trade-tape</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "tapeSidesOf" webapp/src/components/orderbook/trade-tape.tsx</automated>
    <fails_when>카운트가 2 미만이면 실패 (정의 또는 호출이 없다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `trade-tape.tsx` 에 `tapeSidesOf` 가 정의되고 렌더 경로가 그 결과를 쓴다.
    - `deriveTapeSides` 는 여전히 export 돼 있고 `tapeSidesOf` 안에서만 호출된다.
    - 하단 고지 문구 2종이 파일 상단 상수로 정의돼 있고 `usedFallback` 으로 갈린다.
    - 「전부 서버값이면 `deriveTapeSides` 미호출」 스파이 단언이 테스트에 있다.
    - 혼합(`bs` 일부 빈 값) 케이스 단언이 있다.
    - trade-tape 테스트 green.
  </acceptance_criteria>
  <done>체결 색이 거래소 원문을 먼저 쓰고, 화면이 자기 근거를 사실대로 말한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 호가 종목정보 — `kc > 0` 이면 종가 표기</name>
  <files>webapp/src/components/stock/stock-orderbook-section.tsx</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/components/stock/stock-orderbook-section.tsx` 340-375 (종목정보 `dl` — 기준/상한/하한/VI 4항목, VI 항목이 `viu / vid` 를 한 칸에 담는다)
    - `/Users/alex/repos/gh-radar/webapp/src/lib/quote-format.ts` (표기 순수함수 관례)
    - `/Users/alex/repos/gh-radar/packages/shared/src/relay.ts` (`RelayQuote.kc` 주석 — NXT 프레임에도 KRX 값, 벽시계 판정 금지)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-11
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §1-2 `QuoteState.krx_close_price` 행
  </read_first>
  <behavior>
    - `kc = 12625` 인 KRX 프레임 → 종목정보에 라벨 `종가` 와 값 `12,625` 가 보이고, 그 자리에 있던 하락VI 값은 표시되지 않는다
    - `kc = 0` → 종전 표기(VI 상승/하락 두 값) 그대로다
    - `kc = 12625` 인 **NXT** 프레임 → 라벨은 여전히 `종가` 다(KRX 값이 실려 온다 — 라벨을 바꾸지 않는다)
    - 같은 값이 다시 와도 표시가 바뀌지 않는다(no-op)
    - 시각을 읽어 판정하는 코드가 없다
  </behavior>
  <action>
① `stock-orderbook-section.tsx` 의 종목정보 `dl` 에서 **하락VI 값이 놓인 자리**를 `kc` 로 가른다: `kc > 0` 이면 그 항목을 라벨 `종가` · 값 `kc` 로 그리고, `kc === 0` 이면 현행 VI 상승/하락 표기를 그대로 둔다. 상승VI 값은 어느 갈래에서도 사라지지 않는다.

② **벽시계로 판정하지 않는다.** `new Date()` · 장 시간 상수 · `isAfterMarketClose` 류를 이 분기에 쓰지 않는다. `0` 도 권위값이라 「아직 안 왔다」로 해석하지 않는다(D-11).

③ 표기 함수는 기존 `fmt`/`quote-format.ts` 를 쓴다 — 새 포매터를 만들지 않는다.

④ 라벨 폭만 확인한다: `dl` 은 `flex-wrap` 이라 밴드 경계와 무관하지만, 폰 폭에서 `종가` 라벨과 값이 한 항목 안에서 줄바꿈되지 않는지 확인한다. 잘리면 그 항목에 `min-w-0` 규율을 적용한다(이 저장소의 상습 함정).

⑤ **`webapp/src/styles/globals.css` 의 §2.2b 밴드 경계는 재측정하지 않는다** — 이 표면은 컨테이너 쿼리 대상이 아니고 칸 수도 그대로다(D-11).

⑥ `<behavior>` 5케이스를 이 컴포넌트의 기존 테스트 파일(또는 신규)에 단언한다.

⑦ 커밋 메시지 초안: `feat(17-08): 호가 종목정보에 KRX 정규장 종가 표기 (kc>0 일 때 하락VI 자리 대체)`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -cE "new Date\(|isAfterMarketClose|MARKET_CLOSE" webapp/src/components/stock/stock-orderbook-section.tsx</automated>
    <fails_when>17-08 이전 값보다 커지면 실패 — 이 태스크가 시각 기반 판정을 새로 넣었다는 뜻이다. 기준값을 작업 전에 먼저 재서 SUMMARY 에 적는다</fails_when>
  </verify>
  <acceptance_criteria>
    - `stock-orderbook-section.tsx` 의 종목정보 분기가 `quote?.kc` 를 읽는다.
    - `kc > 0` 갈래와 `kc === 0` 갈래가 모두 테스트에 있다.
    - NXT 프레임에서도 라벨이 바뀌지 않는 케이스가 테스트에 있다.
    - 시각 기반 판정 grep 카운트가 작업 전과 같다(SUMMARY 에 전/후 값 기록).
    - 이 plan 의 `files_modified` 에 `webapp/src/styles/globals.css` 가 없고, §2.2b 밴드 경계 리터럴(`700px`·`830px`·`992px`)이 그대로 있다.
    - webapp 테스트 green.
  </acceptance_criteria>
  <done>KRX 정규장 종가가 오늘 것이면 호가 종목정보에 나타나고, 아니면 종전 표기가 그대로다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 호가 상태바 서버 메시지 배지</name>
  <files>webapp/src/components/orderbook/relay-status-bar.tsx, webapp/e2e/specs/orderbook.spec.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/webapp/src/components/orderbook/relay-status-bar.tsx` 145-200 (`messages` prop · `VISIBLE_MESSAGES` · 최근 3건 렌더부) 와 메시지 줄이 실제로 그려지는 JSX 지점(grep 으로 실측)
    - `/Users/alex/repos/gh-radar/packages/shared/src/strategy-display.ts` (`serverMsgBadge` — 17-01 산출물)
    - `/Users/alex/repos/gh-radar/webapp/src/lib/use-relay-socket.ts` 75-80 (`MAX_MESSAGES = 20`) 와 `case "msg":` 의 prepend·slice
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-09 · D-17
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §4 Pitfall 12 (`msg` 볼륨 — 상따 사유 줄이 100ms 드레인으로 늘어난다)
  </read_first>
  <behavior>
    - `src === "LimitChaser"` 인 메시지 줄 앞에 `[상따]` 가 보인다
    - `src === "VITrigger"` → `[VI]`
    - `src === "Account"` · `""` · 미상 → `[서버]`
    - 배지는 텍스트다 — 색만으로 구분되지 않는다
    - 메시지 20건이 쌓여도 상태바는 최근 3건만 그린다(기존 상한 유지)
  </behavior>
  <action>
① `relay-status-bar.tsx` 의 메시지 줄 렌더에 `serverMsgBadge(m.src)` 를 접두로 붙인다. 판정은 그 함수 하나만 쓴다 — 컴포넌트 안에서 `src` 를 직접 비교하지 않는다(D-09: 동등 비교만, 자유문 해석 금지).

② **`msg` 볼륨 확인**(Pitfall 12): 상따 사유 줄이 100ms 드레인으로 훨씬 많이 온다. `use-relay-socket.ts` 의 `case "msg":` 가 `MAX_MESSAGES` 로 자르는 O(1) 경로인지, `relay-status-bar` 가 매 프레임 O(n) 작업을 하지 않는지 읽어 확인하고 **결과를 SUMMARY 에 한 줄로 기록**한다. 문제가 없으면 코드를 바꾸지 않는다 — 없는 병목을 고치지 않는다.

③ `<behavior>` 5케이스를 상태바 테스트(없으면 신규)에 단언한다.

④ e2e `webapp/e2e/specs/orderbook.spec.ts` 가 체결 테이프 고지 문구나 종목정보 셀렉터에 의존해 깨지면 갱신한다.

⑤ 커밋 메시지 초안: `feat(17-08): 호가 상태바 서버 메시지에 출처 배지 적용`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "serverMsgBadge" webapp/src/components/orderbook/relay-status-bar.tsx</automated>
    <fails_when>카운트가 0 이면 실패 (상태바에 배지가 붙지 않았다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm typecheck</automated>
    <fails_when>비영 종료코드, 또는 출력에 `error TS` 가 나오면 실패</fails_when>
  </verify>
  <acceptance_criteria>
    - `relay-status-bar.tsx` 가 `serverMsgBadge` 를 import 해 메시지 줄에 쓴다.
    - 같은 파일에서 `src ===` 직접 비교가 0건이다.
    - 배지가 텍스트 노드로 렌더된다(테스트가 문자열로 찾을 수 있다).
    - `VISIBLE_MESSAGES` 값이 바뀌지 않았다.
    - `msg` 볼륨 확인 결과가 SUMMARY 에 기록됐다.
    - webapp 테스트 + 루트 typecheck green.
  </acceptance_criteria>
  <done>호가 표면의 서버 메시지가 누가 보낸 것인지 말한다. 상따 사유 줄이 늘어도 상한 경로가 그대로임을 확인했다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (17-08)

**함수:** `tapeSidesOf(entries, bestAsk, bestBid)` (`webapp/src/components/orderbook/trade-tape.tsx`)

**상수:** 체결 테이프 하단 고지 문구 2종(서버 기준 / 일부 추정)

**UI:** 호가 종목정보의 `종가` 항목(`kc > 0` 갈래) · 호가 상태바 서버 메시지 배지
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay 프레임 → 호가 화면 | 체결구분·종가가 사용자의 매매 판단 입력이다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-27 | Spoofing | 추정 색을 서버 사실로 표기 | high | mitigate | `usedFallback` 으로 고지 문구를 갈라 근거를 사실대로 말한다 |
| T-17-28 | Spoofing | 벽시계로 종가 유효성 판정 | high | mitigate | 시각 기반 판정 grep 카운트 불변 게이트 — `0` 도 권위값 |
| T-17-29 | Denial of Service | 상따 사유 줄 볼륨 증가 | medium | accept | 기존 `MAX_MESSAGES` 상한이 O(1) 인지 확인만 하고 없는 병목을 고치지 않는다 |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 plan 은 패키지 설치가 없다 |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/webapp test` · `pnpm typecheck` green
- §2.2b 밴드 경계 리터럴(`700px`·`830px`·`992px`) 보존
- e2e `orderbook.spec.ts` 통과(셀렉터 갱신 포함)
</verification>

<success_criteria>
- 체결 색이 서버 원문 우선이고 고지가 근거를 사실대로 말한다.
- KRX 종가가 오늘 것일 때 종목정보에 나타난다.
- 호가 상태바 서버 메시지에 출처 배지가 붙는다.
</success_criteria>

<output>
Create `.planning/phases/17-gh-trade-led/17-08-SUMMARY.md` when done
</output>