---
phase: 17-gh-trade-led
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - relay/src/dma/envelope.ts
  - relay/tests/helpers/frames.ts
  - relay/src/dma/__tests__/envelope.test.ts
  - relay/tests/protocol.test.ts
autonomous: true
requirements: [TRADE-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "호가 프레임이 KRX 종가(`kc`)를 싣고, 체결 테이프 원소가 서버 체결구분(`bs`)을 싣는다 (D-10 · D-11)"
    - "미체결 행이 예약(`queuedStatus`)·접수대기(`pendingStatus`)·시간외종가(`board`)·취소보관(`pendingCancelSent`)·주문시각(`orderTime`)을 싣는다 (D-07)"
    - "주문 통보가 `board`·`request_kind`·`requester` 를 싣고, 어디서도 `message` 문구를 파싱하지 않는다 (D-08)"
    - "Q-ID 주문번호(`Q`+9자, 10자)가 relay 의 주문번호 zod 가드를 통과한다 (D-07)"
    - "`DirectOrderReq` 송신 바이트가 무변경이다 — `piece_count`/`krx_session` 을 보내지 않는다 (D-12)"
  artifacts:
    - relay/tests/helpers/frames.ts
  key_links:
    - "parseQuoteState() → RelayQuote.kc → 브라우저 호가 표면"
    - "parseAccountState() 미체결 루프 → RelayUnfilled 5필드 → account-panel / vi-client"
    - "parseOrderResp() → ParsedOrderResp → RelayOrderMsg.bd/rk/rq → today-orders-card"
---

<objective>
재동기화로 접근자가 생긴 기존 테이블 말미 필드를 relay 파서가 실제로 읽어 브라우저 계약에 채운다. 서버가 이미 보내고 있던 진실(체결구분·KRX 종가·예약/접수대기/시간외종가 미체결·정정취소 요청 종류)이 이 plan 이후로 relay 밖으로 나온다.

Purpose: 웹 표면 plan(wave 6~7)이 손댈 때 데이터가 이미 와 있어야 한다. 파서와 화면을 같은 plan 에 두면 「값이 없는데 화면만 고쳤다」가 조용히 통과한다.
Output: 확장된 파서 4종, 테스트 프레임 빌더, Q-ID 가드 회귀 단언.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gh-trade-led/17-CONTEXT.md
@.planning/phases/17-gh-trade-led/17-RESEARCH.md
@.planning/phases/17-gh-trade-led/17-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 호가 `kc` · 체결테이프 `bs` — 와이어에서 브라우저 프레임까지 한 경로</name>
  <files>relay/src/dma/envelope.ts, relay/tests/helpers/frames.ts, relay/src/dma/__tests__/envelope.test.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/relay/src/dma/envelope.ts` 471-560 (`parseQuoteState` · `parseTradeTape` — `toNum` · `isValidChangeSign` 규율이 여기 있다)
    - `/Users/alex/repos/gh-radar/relay/tests/helpers/frames.ts` 81-186 (`buildQuoteStateFrame` · `buildTradeTapeFrame`)
    - `/Users/alex/repos/gh-radar/packages/shared/src/relay.ts` (17-01 이 넣은 `RelayQuote.kc` · `RelayTapeEntry.bs` 주석)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §1-2 `QuoteState.krx_close_price`(슬롯 60) · `TradeTapeEntry.bs_code`(슬롯 16) 행
    - `/Users/alex/repos/gh-radar/.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md` §Common Pitfalls 3 (bigint → Number, `toNum` 필수)
  </read_first>
  <behavior>
    - `krx_close_price` 가 12,625 인 58 프레임 → `RelayQuote.kc === 12625`
    - `krx_close_price` 가 0 인 59 프레임 → `RelayQuote.kc === 0` (프레임을 버리지 않는다)
    - `bs_code` 가 `"1"` 인 원소 → `RelayTapeEntry.bs === "1"`; `"2"` → `"2"`
    - `bs_code` 가 없거나 낯선 값(`"9"`, `"X"`) → `bs === ""` (**프레임 전체를 버리지 않는다**)
  </behavior>
  <action>
① `parseQuoteState` 의 반환 객체에 `kc: toNum(q.krxClosePrice(), "krx_close_price")` 를 `vid` 다음 자리에 추가한다. `krx_close_price` 는 fbs 에서 **long** 이라 `toNum` 을 반드시 통과시킨다(bigint 가 `encode()` 에 닿으면 팬아웃 루프가 예외로 죽는다 — 16-RESEARCH Pitfall 3). `0` 을 거르지 않는다 — `0` 도 권위값이고 벽시계 판정은 금지다(D-11).

② `parseTradeTape` 의 원소 push 에 `bs` 를 추가한다. 값 정규화는 **허용 집합 동등 비교**로 한다: `"1"` 과 `"2"` 만 그대로 통과시키고 그 밖(빈 값·낯선 코드)은 `""` 로 접는다. `changeSign` 처럼 프레임을 드롭하지 않는다 — 체결구분은 색 힌트일 뿐이고 버리면 누적거래량이 어긋난다.

③ `relay/tests/helpers/frames.ts` 의 `FakeQuoteInput` 에 `krxClosePrice?: number`, `FakeTapeInput` 원소에 `bsCode?: string` 을 더하고 빌더가 `addKrxClosePrice` · `addBsCode` 를 호출하게 한다. 문자열은 **테이블 빌더를 열기 전에** `createString` 으로 만든다(16-RESEARCH Pitfall 2 — `startXxx()` 이후의 `createString` 은 릴리스에서 조용히 깨진 버퍼를 만든다).

④ `relay/src/dma/__tests__/envelope.test.ts` 에 `<behavior>` 의 6케이스를 단언한다.

⑤ 커밋 메시지 초안: `feat(17-02): 호가 KRX 종가·체결테이프 서버 체결구분 파싱 추가`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test -- envelope</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "krxClosePrice" relay/src/dma/envelope.ts</automated>
    <fails_when>카운트가 0 이면 실패 (파서가 종가 접근자를 읽지 않음)</fails_when>
  </verify>
  <acceptance_criteria>
    - `sed -n '/export function parseQuoteState/,/^}/p' relay/src/dma/envelope.ts | grep -c 'toNum(q.krxClosePrice()'` 가 1 이다.
    - `sed -n '/export function parseTradeTape/,/^}/p' relay/src/dma/envelope.ts` 결과에 `bs:` 가 1회 나타난다.
    - 체결구분이 낯선 값일 때 프레임을 드롭하지 않는다 — `parseTradeTape` 본문에서 `dropField` 호출이 17-01 이전과 같은 개수다(`bs` 사유로 새로 추가된 `dropField` 가 0건).
    - `relay/tests/helpers/frames.ts` 에 `krxClosePrice` 와 `bsCode` 가 각각 나타난다.
    - `relay/src/dma/__tests__/envelope.test.ts` 가 `kc === 0` 케이스와 낯선 `bs_code` 케이스를 각각 단언한다.
    - relay envelope 테스트 green.
  </acceptance_criteria>
  <done>58/59 프레임이 KRX 종가를, 69/71 프레임이 서버 체결구분을 실어 브라우저 계약까지 나른다. 0 과 미상 값이 프레임을 죽이지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 미체결 5필드 확장 + Q-ID 주문번호 가드 회귀</name>
  <files>relay/src/dma/envelope.ts, relay/tests/helpers/frames.ts, relay/tests/protocol.test.ts, relay/src/dma/__tests__/envelope.test.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/relay/src/dma/envelope.ts` 1429-1520 (`parseAccountState` · 미체결 루프 1499-1510 · `skipAccountStateItem` 규율)
    - `/Users/alex/repos/gh-radar/relay/src/ws/protocol.ts` 170-230 (`orderNo` `min(1).max(10)` · `orgOrderNo` `min(1)` — **읽기만** 한다. 이 파일은 17-04 소유다)
    - `/Users/alex/repos/gh-radar/relay/tests/protocol.test.ts` (기존 zod 단언 형식)
    - `/Users/alex/repos/gh-radar/relay/tests/helpers/frames.ts` 352-420 (`buildAccountStateFrame`)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §1-2 `UnfilledState.*` 4행
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-07 · D-14
  </read_first>
  <behavior>
    - `queued_status` 가 `"예약대기"` 인 행 → `RelayUnfilled.queuedStatus === "예약대기"` (문구 그대로, 재해석 없음)
    - `pending_status` 가 `"증권사 보관 · 09:00 처리"` 인 행 → `pendingStatus` 가 같은 문구
    - `board` 가 `"G2"` → `board === "G2"`; `board` 가 빈 값 → `""`
    - `pending_cancel_sent` 가 true → `pendingCancelSent === true`; 부재 → `false`
    - `order_time` 이 `"091533"` → `orderTime === "091533"`
    - `RelayOrderCancelSchema` 가 `orgOrderNo: "Q0915331 23"` 형태가 아닌 실제 형식 `"Q091533123"`(10자)을 통과시킨다
  </behavior>
  <action>
① `relay/src/dma/envelope.ts` 의 미체결 push 객체에 5필드를 추가한다: `orderTime: u.orderTime() ?? ""`, `queuedStatus: u.queuedStatus() ?? ""`, `pendingStatus: u.pendingStatus() ?? ""`, `board: u.board() ?? ""`, `pendingCancelSent: u.pendingCancelSent()`. **어떤 필드도 해석하지 않는다** — 문구를 잘라 상태를 만들거나 `board` 로 side 를 바꾸지 않는다. `board` 는 서버가 `"G2"`/`"G3"`/빈 값만 보내므로 별도 정규화 없이 원문을 넘긴다.

② `pendingCancelSent === true` 인 행을 미체결 목록에서 **제외하지 않는다**. 서버가 브로커 앞에서 `R` 로 답하므로 relay 가 이중 판정하면 취소 경로가 두 벌이 된다(D-07). 화면이 취소 버튼을 감추는 것으로 충분하다.

③ `relay/tests/helpers/frames.ts` 의 `buildAccountStateFrame` 미체결 입력에 5필드를 optional 로 더하고 빌더가 채우게 한다. 문자열은 테이블 열기 전에 `createString` (Pitfall 2).

④ Q-ID 회귀: `relay/tests/protocol.test.ts` 에 `RelayOrderCancelSchema` 가 `orgOrderNo` 로 10자 Q-ID 를 통과시키는 단언과, `RelayOrderNewSchema`/취소 경로의 `orderNo` 가드가 같은 값을 거부하지 않는 단언을 더한다. **현재 zod 는 형식 제한 없이 `min(1).max(10)` 이라 통과하는 것이 확인돼 있다**(17-RESEARCH §3-1) — 이 단언은 앞으로 형식 제한이 들어올 때 깨지라고 두는 회귀 그물이다. 통과하지 않는다면 `relay/src/ws/protocol.ts` 를 고치지 말고 **SUMMARY 에 기록하고 17-04 로 넘긴다**(그 파일은 17-04 소유다).

⑤ `relay/src/dma/__tests__/envelope.test.ts` 에 `<behavior>` 5케이스를 단언한다.

⑥ 커밋 메시지 초안: `feat(17-02): 미체결 예약·접수대기·시간외종가·취소보관·주문시각 5필드 파싱 + Q-ID 주문번호 회귀 단언`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test</automated>
    <fails_when>비영 종료코드, 또는 vitest 요약 줄에 `failed` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "pendingCancelSent" relay/src/dma/envelope.ts</automated>
    <fails_when>카운트가 0 이면 실패 (취소보관 플래그를 읽지 않음)</fails_when>
  </verify>
  <acceptance_criteria>
    - `relay/src/dma/envelope.ts` 미체결 push 객체에 `orderTime` · `queuedStatus` · `pendingStatus` · `board` · `pendingCancelSent` 다섯 키가 모두 있다.
    - 미체결 루프에서 `pendingCancelSent` 를 이유로 `continue` 하거나 행을 건너뛰지 않는다: `sed -n '/const unf: RelayUnfilled\[\]/,/^  const rm/p' relay/src/dma/envelope.ts | grep -c "pendingCancelSent()" ` 가 1 이고, 같은 구간에 `pendingCancelSent` 를 조건으로 쓰는 `if` 가 없다.
    - `relay/tests/protocol.test.ts` 에 10자 Q-ID 문자열(`Q` 로 시작하는 10자 리터럴)을 쓰는 단언이 1건 이상 있다.
    - `relay/src/dma/__tests__/envelope.test.ts` 가 `board` 빈 값 케이스와 `pendingCancelSent` 부재 → `false` 케이스를 단언한다.
    - relay 전량 테스트 green.
  </acceptance_criteria>
  <done>미체결 행이 서버가 아는 다섯 사실을 그대로 나르고, Q-ID 취소가 relay 가드를 통과함이 테스트로 굳었다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 주문통보 3필드 + 문구 미파싱·미송신 불변식 확인</name>
  <files>relay/src/dma/envelope.ts, relay/tests/helpers/frames.ts, relay/src/dma/__tests__/envelope.test.ts</files>
  <read_first>
    - `/Users/alex/repos/gh-radar/relay/src/dma/envelope.ts` 1283-1327 (`parseOrderResp` · `ParsedOrderResp`) 와 780-800 (`buildDirectOrderReq`)
    - `/Users/alex/repos/gh-radar/relay/src/order/notice-status.ts` 43-69 (**읽기 전용 확인** — `noticeType`/`resultCode` 만 해석하는지)
    - `/Users/alex/repos/gh-radar/relay/tests/helpers/frames.ts` 282-344 (`buildOrderRespFrame`)
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-RESEARCH.md` §1-2 `OrderResp.*` 3행 · §1-3
    - `/Users/alex/repos/gh-radar/.planning/phases/17-gh-trade-led/17-CONTEXT.md` D-08 · D-12
  </read_first>
  <behavior>
    - `request_kind` 가 `"Cancel"` 인 51 프레임 → `RelayOrderMsg.rk === "Cancel"`
    - `requester` 가 `"Manual"` → `rq === "Manual"`; 빈 값 → 필드 생략 또는 `""`
    - `board` 가 `"G3"` → `bd === "G3"`
    - 세 필드가 전부 빈 구 서버 프레임 → 기존 8필드 통보가 그대로 만들어진다(회귀 없음)
  </behavior>
  <action>
① `ParsedOrderResp` 와 `parseOrderResp` 에 `board` · `requestKind` · `requester` 를 더하고, hub 가 만드는 `RelayOrderMsg` 에 와이어 키 `bd`/`rk`/`rq` 로 싣는다(빈 값은 생략해 프레임을 가볍게 유지한다 — 17-01 계약이 셋을 optional 로 뒀다). `side`·`isin` 은 계속 싣지 않는다(Pitfall 8).

② **`message` 문구를 파싱하지 않는다.** `relay/src/order/notice-status.ts` 를 읽어 `noticeType`/`resultCode` 만 본다는 것을 확인하고, 확인 결과를 SUMMARY 에 한 줄로 남긴다. 문구를 보는 분기가 있으면 `request_kind`/`notice_type` 판정으로 바꾼다(804 정정·취소 거부에서 서버가 문구를 교체하므로 문구 비교는 조용히 틀린다).

③ **`DirectOrderReq` 송신 바이트 무변경 확인**(D-12): `buildDirectOrderReq` 는 `piece_count`·`krx_session` 을 **호출하지 않는다**. 재동기화로 접근자가 생겼어도 이번 phase 에서는 싣지 않는다(Phase 18). 조립기 본문에 두 슬롯 setter 호출이 0건임을 acceptance 로 굳힌다.

④ `relay/tests/helpers/frames.ts` 의 `buildOrderRespFrame` 에 세 필드를 optional 입력으로 더한다.

⑤ `relay/src/dma/__tests__/envelope.test.ts` 에 `<behavior>` 4케이스를 단언한다. 특히 「세 필드 전부 빈 구 서버 프레임」 회귀 케이스를 반드시 둔다.

⑥ 커밋 메시지 초안: `feat(17-02): 주문통보 board·request_kind·requester 파싱 + message 미파싱·DirectOrderReq 미송신 불변식 고정`
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests</automated>
    <fails_when>비영 종료코드, vitest 요약 줄에 `failed`, 또는 tsc 출력에 `error TS` 가 나오면 실패</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && sed -n '/export function buildDirectOrderReq/,/^}/p' relay/src/dma/envelope.ts | grep -cE "addPieceCount|addKrxSession"</automated>
    <fails_when>출력이 `0` 이 아니면 실패 (Phase 18 필드가 이번 phase 에서 송신 경로에 들어갔다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `ParsedOrderResp` 타입에 `board` · `requestKind` · `requester` 세 키가 있다.
    - `RelayOrderMsg` 생성 지점이 `bd`/`rk`/`rq` 를 싣는다: `grep -c "rk:" relay/src/dma/envelope.ts` 또는 hub 생성 지점에서 세 키가 잡힌다.
    - `sed -n '/export function buildDirectOrderReq/,/^}/p' relay/src/dma/envelope.ts | grep -cE "addPieceCount|addKrxSession"` 이 `0` 이다.
    - `relay/src/order/notice-status.ts` 가 `message` 문자열 내용을 비교하지 않는다 — 확인 결과를 SUMMARY 에 기록했다.
    - `relay/src/dma/__tests__/envelope.test.ts` 에 「세 필드 전부 빈 구 서버 프레임」 회귀 케이스가 있다.
    - relay 테스트 · typecheck · typecheck:tests 전부 green.
  </acceptance_criteria>
  <done>주문 통보가 행위 단어·요청 주체·시간외종가 구분을 나르고, 문구 파싱과 Phase 18 필드 송신이 둘 다 0 임이 자동 게이트로 굳었다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (17-02)

**파서 필드:** `parseQuoteState → kc` · `parseTradeTape → bs` · `parseAccountState 미체결 → orderTime/queuedStatus/pendingStatus/board/pendingCancelSent` · `parseOrderResp → board/requestKind/requester`

**와이어 키(브라우저 프레임):** `RelayOrderMsg.bd` · `.rk` · `.rq`

**테스트 빌더 입력:** `FakeQuoteInput.krxClosePrice` · `FakeTapeInput[].bsCode` · `buildAccountStateFrame` 미체결 5필드 · `buildOrderRespFrame` 3필드

**불변식(자동 게이트):** `buildDirectOrderReq` 안에 `addPieceCount`/`addKrxSession` 0건 · Q-ID 10자 취소 zod 통과
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트웨이 → relay 파서 | 문자열 필드(queued_status·pending_status·message)가 신뢰되지 않는 입력이다 |
| relay → 브라우저 프레임 | 파서가 실은 필드가 그대로 화면 판정 근거가 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-04 | Tampering | `OrderResp.message` 문구 기반 판정 | high | mitigate | 문구 파싱 0 을 acceptance 로 강제. 804 거부에서 서버가 문구를 교체하므로 문구 비교는 조용히 틀린다 |
| T-17-05 | Elevation of Privilege | `DirectOrderReq` 송신 바이트 변경 | critical | mitigate | `addPieceCount`/`addKrxSession` grep 0 게이트 — 수동주문 경로 바이트 무변경 |
| T-17-06 | Denial of Service | 낯선 `bs_code`/`board` 값으로 프레임 드롭 | medium | mitigate | 미상 값은 `""` 로 접고 프레임을 버리지 않는다(누적거래량 어긋남 방지) |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 plan 은 패키지 설치가 없다 |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/relay test` · `run typecheck` · `run typecheck:tests` green
- `pnpm typecheck` (루트) green — shared 계약과 파서 반환 타입이 일치
- `buildDirectOrderReq` 송신 바이트 무변경 게이트 통과
</verification>

<success_criteria>
- 서버가 이미 보내던 13개 필드가 relay 계약에 실린다.
- 문구 파싱 0 · Phase 18 필드 송신 0 이 자동 게이트로 굳었다.
- Q-ID 취소 경로가 relay zod 를 통과함이 테스트에 있다.
</success_criteria>

<output>
Create `.planning/phases/17-gh-trade-led/17-02-SUMMARY.md` when done
</output>