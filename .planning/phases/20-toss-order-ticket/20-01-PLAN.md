---
phase: 20-toss-order-ticket
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/src/components/trading/lc/use-lc-field-commit.ts
  - webapp/src/components/trading/lc/inline-value-editor.tsx
  - webapp/src/components/trading/lc/setting-group.tsx
  - webapp/src/components/trading/limit-chaser-form.tsx
  - webapp/src/components/trading/card/card-body.tsx
  - webapp/src/components/trading/lc/__tests__/lc-tracer.test.tsx
  - webapp/src/components/trading/lc/__tests__/use-lc-field-commit.test.tsx
  - webapp/e2e/specs/trading-workbench.spec.ts
autonomous: true
requirements: []

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "트레이서(D-04 · D-14 · D-14a · D-14c · D-20): 마우스 기기에서 「호가변경」 행을 누르면 그 44px 행 안의 값 자리만 입력칸(`#lc-sweep-tick`)으로 바뀌고 값 전체가 선택된 채 포커스된다 — 「Enter 저장 · Esc 취소」 같은 안내 문구와 「저장」 버튼이 없고 행 높이가 44px 그대로다"
    - "Enter 또는 포커스 이탈 = 즉시 반영(D-04): `lc.set` 이 정확히 1회 나가고 cfg 는 서버 값 전체(32필드) + 바꾼 필드 1개다 — 더티 누적도 「수정」 버튼도 거치지 않는다"
    - "행은 전송 뒤에도 서버 값 그대로이고(값 필드 낙관 반영 없음, D-06), 에코의 그 필드 값이 보낸 값과 같을 때만 새 값으로 바뀌며 값 글자가 900ms `--primary` 로 강조된다(D-18)"
    - "답은 왔는데(`serverAnswerSeq` 증가) 에코 값이 보낸 값과 다르면 실패다 — 편집 상태와 입력값이 남고 「반영하지 못했어요 · Enter 로 다시 시도해 주세요」가 보이며 자동 재시도는 없다(D-06). 그 뒤 내 값의 에코가 오면 성공으로 바뀐다(늦은 에코)"
    - "`send` 가 false 면 「연결이 끊겨 보내지 못했어요」가 보이고 행·폼 값은 바뀌지 않는다 · 같은 값 확정은 전송 0 · Esc 는 서버 값으로 돌아가고 전송 0"
    - "미등록 전략(server 없음)에서 값·체크·감시대상 확정은 로컬 반영만 하고 전송하지 않는다 — 등록은 그룹 스위치 4개(`buyEnabled` · `sweepEnabled` · `sellEnabled` · `cancelQtyEnabled`)만 한다 [planner assumption A-P1, RESEARCH Open Q1]"
    - "반영 판정 상태 기계(D-05 · D-06 · D-07 기반): 거부(answerSeq 만 증가) = 실패 · 카드 `unacked`(3초 무응답) = 실패 · 늦은 에코 = 성공 · 동시에 나가 있는 전송은 1건이고 대기 건은 앞 건의 답이 `serverAnswerSeq` 에 반영된 뒤 나간다 · 실패하면 대기 건은 보내지 않고 그 필드들도 실패로 표시한다 · 무장 불가 값은 `armBlocked` 로 막는다(전송 0)"
    - "토글 종류(스위치·체크·감시대상) 확정은 전송 뒤 낙관 표시하고, 실패·대기 폐기 시 확정 전 값으로 되돌린다 · `buyOrderAmount` 에코가 0(서버가 모름)이면 답 도착만으로 성공이다"
    - "CardBody 가 카드의 `unacked` 와 현재가(`quote.p`, 없으면 0)를 LimitChaserForm 에 내려 준다"
    - "편집 중 에코가 와도 입력값은 유지되고 저장하면 내 값이 나간다 · 편집이 끝난 뒤 행 표시값은 에코 값이다 (UI-SPEC UI Considerations E4 partial)"
  artifacts:
    - path: webapp/src/components/trading/lc/use-lc-field-commit.ts
      provides: "필드 1회 확정 = lc.set 1회 상태 기계"
      exports: ["useLcFieldCommit", "LC_FLASH_MS", "LC_COMMIT_TEXT", "LC_GATE_FIELDS"]
    - path: webapp/src/components/trading/lc/inline-value-editor.tsx
      provides: "44px 행 안 인라인 입력(전체 선택 · Enter/blur 저장 · Esc 취소)"
      exports: ["InlineValueEditor"]
    - path: webapp/src/components/trading/lc/setting-group.tsx
      provides: "「라벨 ─ 값 ›」 44px 값 행 + 실패 말풍선"
      exports: ["SettingRow", "FailureBubble"]
    - path: webapp/src/components/trading/lc/__tests__/lc-tracer.test.tsx
      provides: "StrategyCard + CardBody 통합 트레이서 테스트"
    - path: webapp/src/components/trading/lc/__tests__/use-lc-field-commit.test.tsx
      provides: "상태 기계 단위 테스트(renderHook)"
  key_links:
    - from: webapp/src/components/trading/limit-chaser-form.tsx
      to: webapp/src/components/trading/lc/use-lc-field-commit.ts
      via: "행 확정 → commit(field, value, kind) → send({ t: 'lc.set', cfg })"
      pattern: "useLcFieldCommit\\("
    - from: webapp/src/components/trading/card/card-body.tsx
      to: webapp/src/components/trading/limit-chaser-form.tsx
      via: "unacked · currentPrice · serverAnswerSeq prop"
      pattern: "unacked=\\{"
    - from: webapp/src/components/trading/lc/use-lc-field-commit.ts
      to: webapp/src/components/trading/card/strategy-card.tsx
      via: "onSent(cfg) → handleSent 3초 타이머(unacked) · acceptAnswer → answerSeq"
      pattern: "onSent"
  prohibitions:
    - statement: "서버 프로토콜을 바꾸지 않는다 — packages/shared/src/relay.ts · relay/ · supabase/migrations/ 무변경 — lc.set 은 여전히 전략 전체(32필드)를 보낸다"
      status: resolved
      verification: test
    - statement: "사용자가 누르지 않은 lc.set 을 보내지 않는다 — 실패·타임아웃 뒤 자동 재시도·대기 건 자동 전송 금지 (T-16-10)"
      status: resolved
      verification: test
    - statement: "거부·무응답을 성공으로 표시하지 않는다 — 성공은 에코의 그 필드 값 == 보낸 값일 때뿐이다"
      status: resolved
      verification: test
    - statement: "webapp/e2e/specs/zz-theme-gallery.spec.ts(다른 세션의 추적되지 않는 파일)를 수정·스테이징·커밋·삭제하지 않는다"
      status: resolved
      verification: test
---

<objective>
**트레이서(Phase 20 첫 슬라이스).** 상따 설정의 한 행(「호가변경」 = `sweepMinTickCount`)을 토스식 「라벨 ─ 값 ›」 44px 리스트 행으로 바꾸고, 마우스 기기에서 그 행을 인라인 편집 → Enter → `useLcFieldCommit` → `send({ t: 'lc.set' })` → relay → 스텁 게이트웨이 10 수신 → 60 에코 → 행 값 갱신까지 **실제 경로 한 줄**을 끝까지 잇는다. 이어서 Task 2 가 같은 상태 기계를 실패·타임아웃·직렬화·무장 가드·토글까지 완성하고 CardBody 가 `unacked`·현재가를 내려 준다.

Purpose: D-04(한 필드 확정 = 전략 1회 전송)가 기존 「더티 누적 → 수정」 모델을 대체할 수 있음을 가장 이른 커밋에서 증명한다. 이 트레이서가 실패하면 나머지 확장(시트·리스트 전면 전환·수동주문)은 전부 재설계 대상이다.
Output: `lc/` 디렉터리 신설(상태 기계 · 인라인 편집기 · 값 행) + 폼의 「호가변경」 행 1개 전환 + 통합 테스트 + Playwright P20-1.

「호가변경」을 트레이서로 고른 이유: 기존 단위 테스트·e2e 가 이 필드를 한 번도 참조하지 않는다(grep 0건) — 트레이서가 부수 파손 없이 착지한다. 가격 전용 동작(↑↓ 한 호가 · D-15 검증)은 확장 플랜(20-05)이 붙인다.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-toss-order-ticket/20-CONTEXT.md
@.planning/phases/20-toss-order-ticket/20-UI-SPEC.md
@.planning/phases/20-toss-order-ticket/20-RESEARCH.md
@.planning/phases/20-toss-order-ticket/20-PATTERNS.md
@.planning/sketches/002-toss-order-ticket/index.html
@webapp/src/components/trading/limit-chaser-form.tsx
@webapp/src/components/trading/card/card-body.tsx
@webapp/src/lib/limit-chaser.ts

작업 루트는 worktree `/Users/alex/repos/gh-radar/.claude/worktrees/toss-b`(브랜치 `theme/toss-b`)다. 메인 체크아웃(`/Users/alex/repos/gh-radar`)으로 `cd` 하지 않는다. 단일 파일 테스트는 `pnpm --filter @gh-radar/webapp exec vitest --run <경로>` 로 돌린다(`… test -- <이름>` 은 필터가 안 먹고 전체 suite 를 돈다).

<interfaces>
기존 계약(코드에서 추출):

```ts
// webapp/src/lib/limit-chaser.ts
export type LimitChaserFormValues = Omit<RelayLimitChaserInput, 'isin'|'accountNo'|'exchange'|'crud'|'buyOrderQty'|'sweepRecalcEnabled'|'sweepMinCount'|'sweepMinRate'>;
export function formFromServer(server: RelayLimitChaser, prev: LimitChaserFormValues): LimitChaserFormValues; // buyOrderAmount===0 이면 prev 유지
export function isDeleteIntent(gates: LimitChaserGates): boolean;
export function crudOf(gates: LimitChaserGates): RelayLcCrud;

// webapp/src/components/trading/card/strategy-card.tsx
export const ACK_TIMEOUT_MS = 3_000;
export interface StrategyCardState { server: RelayLimitChaser | null; quote: RelayQuote | null; unacked: boolean; answerSeq: number; handleSent: (cfg: RelayLimitChaserInput) => void; /* … */ }

// webapp/src/components/trading/limit-chaser-form.tsx (기존 — 재사용, 복제 금지)
const buildCfg: (values: LimitChaserFormValues) => RelayLimitChaserInput; // 조립 지점 하나
const GATE_KEYS = ['buyEnabled', 'sweepEnabled', 'sellEnabled'] as const;
function armBlockedTextOf(key: GateKey, values: LimitChaserFormValues): string;
const GATE_LABEL: Record<GateKey, string>; const ARM_BLOCKED_SEP = ' · ';
// props: server · serverAnswerSeq · onSent · onServerEcho · disabled · upperLimit …
```

이 플랜이 새로 만드는 계약(후속 플랜 20-03~20-05 가 이 이름으로 소비한다):

```ts
// webapp/src/components/trading/lc/use-lc-field-commit.ts
export type LcFieldKey = keyof LimitChaserFormValues;
export type LcCommitKind = 'value' | 'toggle';
export type LcFailReason = 'rejected' | 'timeout' | 'disconnected' | 'armBlocked';
export interface LcCommitFailure { reason: LcFailReason; text: string; value: unknown }
export type LcCommitOutcome = 'noop' | 'local' | 'sent' | 'queued' | 'blocked' | 'disconnected';
export const LC_FLASH_MS = 900;
export const LC_GATE_FIELDS = ['buyEnabled', 'sweepEnabled', 'sellEnabled', 'cancelQtyEnabled'] as const;
export const LC_COMMIT_TEXT: { failed; inlineFailed; disconnected; busy; retry; otherDevice; armed }; // 문구는 Task 1 action
export interface UseLcFieldCommitOptions {
  server: RelayLimitChaser | null; formRef: { current: LimitChaserFormValues };
  setForm: Dispatch<SetStateAction<LimitChaserFormValues>>;
  buildCfg: (v: LimitChaserFormValues) => RelayLimitChaserInput; send: (msg: RelayLcSetMsg) => boolean;
  onSent?: (cfg: RelayLimitChaserInput) => void; serverAnswerSeq: number; disabled: boolean;
  unacked?: boolean;                                   // Task 2
  armBlockOf?: (v: LimitChaserFormValues) => string | null; // Task 2
}
export function useLcFieldCommit(o: UseLcFieldCommitOptions): {
  commit: <K extends LcFieldKey>(field: K, value: LimitChaserFormValues[K], kind: LcCommitKind) => LcCommitOutcome;
  inflightField: LcFieldKey | null; queuedFields: readonly LcFieldKey[];
  failures: Partial<Record<LcFieldKey, LcCommitFailure>>; flashField: LcFieldKey | null;
  successSeq: number; lastSuccessField: LcFieldKey | null; clearFailure: (f: LcFieldKey) => void;
};
```
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): 「호가변경」 한 행 — 클릭 → 인라인 입력 → Enter → lc.set → 60 에코 → 행 값 (end-to-end)</name>
  <reversibility rating="costly">D-04 즉시 반영 모델의 첫 소비처다 — 되돌리면 더티·「수정」 모델과 그 테스트·e2e 를 다시 세워야 한다(CONTEXT D-04 = costly). 와이어·데이터 이관은 없다.</reversibility>
  <precondition>포트 3100 이 비어 있거나 이 worktree 의 `webapp` dev 서버가 떠 있다(`lsof -iTCP:3100 -sTCP:LISTEN` 로 확인 — 메인 체크아웃 서버면 멈추고 보고한다, RESEARCH Pitfall 8)</precondition>
  <files>webapp/src/components/trading/lc/use-lc-field-commit.ts, webapp/src/components/trading/lc/inline-value-editor.tsx, webapp/src/components/trading/lc/setting-group.tsx, webapp/src/components/trading/limit-chaser-form.tsx, webapp/src/components/trading/lc/__tests__/lc-tracer.test.tsx, webapp/e2e/specs/trading-workbench.spec.ts</files>
  <read_first>
    - webapp/src/components/trading/limit-chaser-form.tsx (전체 — 특히 `buildCfg` :464-481 · `toggleGate` :578-600 · `sweep` 그룹 :827-857 · `NumInput` 전체 선택 :1451-1456 · `parseDigits` :1609)
    - webapp/src/components/trading/card/strategy-card.tsx (:278-454 전송 ↔ 에코 상관 · `acceptAnswer` · `handleSent`)
    - webapp/src/components/trading/__tests__/strategy-card-flow.test.tsx (:1-220 relay 모킹 하네스 `setRelay` · `echo()` · `quote()` · `Card()` 조립)
    - webapp/e2e/specs/trading-workbench.spec.ts (:100-200 조회구 · `waitForSetAtGateway` · `openFocusedCard`, :1225-1250 케이스 12 패턴)
    - webapp/src/components/ui/popover.tsx (`Popover` · `PopoverAnchor` · `PopoverContent` export)
    - .planning/sketches/002-toss-order-ticket/index.html (:124-136 `.lrow`/`.lrow.ied` 수치 · :440-467 인라인 편집 · :513-519 pointerdown)
    - .planning/phases/20-toss-order-ticket/20-UI-SPEC.md (§2 값 행 · §6 인라인 편집 · Copywriting Contract)
    - .planning/phases/20-toss-order-ticket/20-RESEARCH.md (§Q5 즉시 전송 경로 · §Q6 인라인 편집 · Pitfall 1·4)
  </read_first>
  <behavior>
    - 「호가변경」 행은 `<button data-lc-field="lc-sweep-tick">` 이고 접근 가능한 이름이 「호가변경 3건」, 값 슬롯 `[data-slot="lc-row-value"]` 텍스트가 정확히 「3건」, 클래스에 `min-h-[44px]` 가 있다
    - 행 클릭 → `#lc-sweep-tick` 입력이 포커스되고 `selectionStart === 0` · `selectionEnd === value.length`(전체 선택) · 입력을 감싼 행 요소도 `min-h-[44px]` · 문서에 「Enter」「저장」 안내 텍스트 0
    - 입력에 5 → Enter → `send` 가 `{ t: 'lc.set' }` 1회 · cfg.sweepMinTickCount === 5 · 나머지 필드는 서버 에코 값(예: 폼 로컬 값이 달라도 cfg.sellOrderRatio === 서버 100) · 에코 전 행 텍스트는 여전히 「3건」
    - 에코(sweepMinTickCount 5) 도착 → 편집기 닫힘(`#lc-sweep-tick` 없음) · 행 「5건」 · 값 슬롯에 `text-[var(--primary)]` 가 붙고 900ms 뒤 사라진다(가짜 타이머)
    - 에코 없이 답만 증가(카드 `answerSeq` +1, 서버 값 3 그대로) → 편집기 유지 · 입력값 5 유지 · `role="alert"` 「반영하지 못했어요 · Enter 로 다시 시도해 주세요」 · `send` 추가 호출 0 · 이후 에코 5 → 성공(늦은 에코)
    - `send` 가 false → 「연결이 끊겨 보내지 못했어요」 · 행·폼 값 불변
    - 같은 값(3) Enter → `send` 0 · Esc → `send` 0 · 행 「3건」
    - 미등록(limitChasers []) 카드에서 7 Enter → `send` 0 · 행 「7건」(로컬 반영)
  </behavior>
  <action>
    ① `webapp/src/components/trading/lc/use-lc-field-commit.ts` 를 신설한다('use client'). 파일 머리에 한국어 ★ 근거 주석(D-04 · D-06 · Pitfall 1 「거부를 성공으로 읽지 않는다」 · T-16-10 「재전송 금지」 · A-P1 미등록 로컬 반영)을 단다. `<interfaces>` 의 이름으로 export 한다: `useLcFieldCommit`, 타입 `LcFieldKey` · `LcCommitKind` · `LcFailReason` · `LcCommitFailure` · `LcCommitOutcome` · `UseLcFieldCommitOptions`, 상수 `LC_FLASH_MS = 900`, `LC_GATE_FIELDS = ['buyEnabled','sweepEnabled','sellEnabled','cancelQtyEnabled']`, `LC_COMMIT_TEXT = { failed: '반영하지 못했어요', inlineFailed: '반영하지 못했어요 · Enter 로 다시 시도해 주세요', disconnected: '연결이 끊겨 보내지 못했어요', busy: '반영 중…', retry: '다시 시도', otherDevice: '다른 단말에서 바뀌었어요', armed: '감시 중 — 적용하면 바로 반영돼요' }`(UI-SPEC Copywriting Contract 원문 그대로 · D-05 문구는 UI-SPEC 판을 쓴다). 이 태스크의 `commit(field, value, kind)` 규칙(순서대로): (a) `disabled` → `'blocked'`(기록 없음) (b) 같은 필드가 in-flight 면 `'blocked'`(Task 2 가 대기열로 바꾼다 — 이 태스크에서는 행이 in-flight 동안 눌리지 않으므로 드롭 경로가 없다) (c) `server != null && server[field] === value` → `'noop'` (d) `server == null` 이고 `field` 가 `LC_GATE_FIELDS` 밖 → `setForm` 으로 그 필드만 로컬 반영 + 강조 → `'local'`(A-P1) (e) 기준값 = `server != null ? formFromServer(server, formRef.current) : formRef.current` — **폼의 로컬 값이 아니라 서버 동기값**을 기준으로 `buildCfg({ ...기준값, [field]: value })` 를 만든다(T-20-03 오래된 로컬 값 덮어쓰기 차단) (f) `send({ t: 'lc.set', cfg })` 가 false → `failures[field] = { reason: 'disconnected', text: LC_COMMIT_TEXT.disconnected, value }` → `'disconnected'` (g) true → `onSent?.(cfg)` 후 in-flight `{ field, value, answerSeqAtSend: serverAnswerSeq }` → `'sent'`. `kind: 'value'` 는 `setForm` 을 부르지 않는다(값 낙관 반영 금지, D-06). 해소 이펙트(의존성 `server` · `serverAnswerSeq`): `server != null && server[in-flight.field] === in-flight.value` → 성공(in-flight·그 필드 실패 해제 · `flashField = field` · `successSeq + 1` · `lastSuccessField = field` · `LC_FLASH_MS` 뒤 `flashField` 해제 — 타이머는 언마운트 때 정리); 아니고 `serverAnswerSeq !== answerSeqAtSend` → `failures[field] = { reason: 'rejected', text: LC_COMMIT_TEXT.failed, value }`. 늦은 에코: 기록된 실패의 `value` 와 `server[field]` 가 같아지면 그 실패를 성공으로 바꾼다(강조 포함). `clearFailure(field)` 는 그 필드 실패만 지운다. 반환 객체에 `queuedFields: []` 를 둔다(Task 2 가 채운다).
    ② `webapp/src/components/trading/lc/setting-group.tsx` 를 신설한다('use client'). 이 태스크는 `SettingRow` 와 `FailureBubble` 두 개만 만든다(나머지 그룹 조각은 20-04). `SettingRow` props: `id`(옛 입력 id — 예 `lc-sweep-tick`), `label`, `unit: '원' | '주' | '만원' | '%' | '건'`, `value: number`, `disabled?`, `busy?`, `flash?`, `failed?`, `editing: boolean`, `editor?: ReactNode`, `onActivate: (el: HTMLElement) => void`, `hasPopup?: boolean`. 편집 중이 아니면 `<button type="button" data-lc-field={id}>` 하나: 클래스 `flex min-h-[44px] w-full min-w-0 items-center justify-between gap-1.5 rounded-[10px] px-1 text-left pointer-fine:hover:bg-[color-mix(in_srgb,var(--fg)_5%,transparent)] disabled:opacity-50`(목업 `.lrow` — 행 좌우 패딩 4 · 라벨–값 간격 6 · radius 10), 라벨 `<span>` = `whitespace-nowrap text-[14px] leading-[1.5] text-[var(--muted-fg)]`, 값 `<span data-slot="lc-row-value">` = `whitespace-nowrap text-[15px] font-medium leading-[1.5] tabular-nums text-[var(--fg-2)]`(flash 면 `text-[var(--primary)]`, busy 면 `opacity-60`) 이고 텍스트는 `ko-KR` 천 단위 + 단위를 붙여 씀(「3건」「127,400원」), 쉐브런은 값 슬롯 **밖** `<span aria-hidden="true" className="text-[var(--faint)]">` 에 ' ›'. `aria-label` = `${label} ${값}${단위}` · busy 면 `aria-busy="true"` · `hasPopup` 이면 `aria-haspopup="dialog"` · failed 면 `shadow-[inset_0_0_0_1.5px_var(--destructive)]`. 클릭 → `onActivate(event.currentTarget)`. 편집 중이면 같은 `data-lc-field={id}` 에 `data-editing="true"` 를 단 `<div>` 로 그리고 **같은 높이·패딩**을 쓰되 `cursor-text bg-[color-mix(in_srgb,var(--fg)_5%,transparent)] shadow-[inset_0_0_0_1.5px_var(--primary)]`(failed 면 `--destructive`) 를 더한다 — 라벨은 `<label htmlFor={id}>`, 값 자리에는 `editor` 를 둔다(D-14a — 높이 변화 0). `FailureBubble({ open, text, children })` 은 `@/components/ui/popover` 의 `Popover`(open 제어) + `PopoverAnchor asChild`(children) + `PopoverContent`(side bottom · `role="alert"` · `text-[12.5px] text-[var(--destructive)]` · `onOpenAutoFocus={e => e.preventDefault()}` — 입력 포커스를 뺏지 않는다) 로 만든다. 새 파일의 레이아웃 유틸은 컨테이너 쿼리 `@min-[700px]/lc:` 만 쓴다(뷰포트 브레이크포인트 유틸 신설 금지 · §2.2b).
    ③ `webapp/src/components/trading/lc/inline-value-editor.tsx` 를 신설한다('use client'). `InlineValueEditor` props: `id`, `label`, `unit`, `initialValue: number`, `failureText?: string | null`, `busy?: boolean`, `onSave: (value: number) => void`, `onCancel: () => void`. `<input id={id} type="text" inputMode="numeric" autoComplete="off" aria-label={label} data-focus-ring="seamless">` + 단위 `<span>`(15/500 `--fg-2` 왼쪽 2px). 입력 클래스: `w-[9ch] bg-transparent border-0 outline-none text-right text-[15px] font-semibold tabular-nums text-[var(--fg)] selection:bg-[color-mix(in_srgb,var(--primary)_35%,transparent)]`. 버퍼는 숫자만 남기고 `ko-KR` 쉼표로 다시 포맷하며 9자리를 넘는 입력은 무시한다. 마운트 시 `useLayoutEffect` 로 `focus()` + `select()` 후 `window.setTimeout(() => el.select(), 0)`(D-14c · `NumInput` 의 iOS 재선택 차용). Enter → `onSave(숫자 · 빈 값은 0)` · Escape → `onCancel()` · blur → `onSave` (한 번 끝나면 같은 편집에서 두 번 부르지 않도록 ref 로 가드하되, 실패 상태에서 다시 Enter 하면 재시도로 `onSave` 를 다시 부른다). `busy` 면 `readOnly` + `aria-busy="true"`. `failureText` 가 있으면 `FailureBubble` 을 편집기 래퍼에 열어 그 문구를 보인다.
    ④ `webapp/src/components/trading/limit-chaser-form.tsx` 에서 `slot="sweep"` 그룹의 `NumField id="lc-sweep-tick"` **하나만** `SettingRow` 로 바꾼다(다른 필드·그룹·더티 바는 이 태스크에서 건드리지 않는다). `const [editingField, setEditingField] = useState<LcFieldKey | null>(null)` 와 `useLcFieldCommit({ server, formRef, setForm, buildCfg, send, onSent: (cfg) => sentNotifyRef.current?.(cfg), serverAnswerSeq, disabled })` 를 둔다. 행 `onActivate` → `disabled` 가 아니고 그 필드가 in-flight 가 아니면 `setEditingField('sweepMinTickCount')`. 편집기 `initialValue` = `failures.sweepMinTickCount?.value`(숫자면) ?? `form.sweepMinTickCount`. `onSave(v)` → `commit('sweepMinTickCount', v, 'value')` 결과가 `'noop'`·`'local'` 이면 편집 종료, `'sent'` 면 편집기를 `busy` 로 유지(에코 오면 `successSeq` 변화 + `lastSuccessField` 가 그 필드일 때 편집 종료 — 이펙트), `'disconnected'`·실패면 편집기 유지 + `failureText`(거부·타임아웃 = `LC_COMMIT_TEXT.inlineFailed`, 끊김 = `LC_COMMIT_TEXT.disconnected`). blur 로 저장된 경우는 편집을 끝내고 행이 `busy`/`failed` 로 결과를 말한다. `onCancel` → `clearFailure` + 편집 종료. 행 `flash={flashField === 'sweepMinTickCount'}`. 기존 `[server]` 에코 이펙트는 그대로 둔다(편집기는 자기 버퍼를 가져 에코가 입력을 덮지 않는다 — E4 partial).
    ⑤ `webapp/src/components/trading/lc/__tests__/lc-tracer.test.tsx` 를 신설한다 — `strategy-card-flow.test.tsx` 의 relay 모킹 하네스(`vi.mock('@/lib/relay-provider')` + `setRelay` + `echo()` + `quote()`)를 복제해 `StrategyCard` + `CardBody variant="card"` 조립을 렌더하고 `<behavior>` 8개를 각각 `it` 으로 단언한다(가짜 타이머로 900ms 강조 확인). 서버 에코 교체는 `setRelay({ limitChasers: [echo({ sweepMinTickCount: 5 })] })` + `rerender`, 거부는 `messages` 에 `SetLimitChaser` ERROR 통지(`strategy-card-flow.test.tsx` ㉑-c 와 같은 모양)로 `acceptAnswer` 를 일으킨다.
    ⑥ `webapp/e2e/specs/trading-workbench.spec.ts` 메인 describe 안(`test.describe('종목 추가란 글꼴 — 터치 기기 …')` 바로 앞)에 `test('P20-1 인라인 편집 한 행 — 「호가변경」 클릭 → 전체 선택 → 5 → Enter → 게이트웨이 10 수신 → 60 에코 → 행 「5건」 (Phase 20 D-04 · D-14 · D-14a)')` 를 더한다. `relay.seedLimitChasers([{ buyEnabled: true }])` → `page.goto(FOCUS_URL)` → `waitForReady` → 카드 `data-open="true"` → **`openFocusedCard` 를 쓰지 않고** `page.locator('[data-lc-field="lc-sweep-tick"] [data-slot="lc-row-value"]')` 가 「3건」이 될 때까지 기다린다(20-04 가 옛 입력 id 를 걷어도 이 케이스가 살아남게). 행 boundingBox 높이 44(±0.5) 기록 → 클릭 → `#lc-sweep-tick` 포커스 · 행 높이 그대로 44 → `page.keyboard.type('5')` → Enter → `waitForSetAtGateway(relay, before + 1)` → `relay.pushLimitChaserEcho({ buyEnabled: true, sweepMinTickCount: 5 })` → 값 슬롯 「5건」 · `#lc-sweep-tick` 개수 0.
    ⑦ `webapp/e2e/specs/zz-theme-gallery.spec.ts` 는 열지도 스테이징하지도 않는다. 커밋 전 `git status -sb` 로 그 파일이 스테이징에 없음을 확인한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/lc/__tests__/lc-tracer.test.tsx src/components/trading/__tests__/limit-chaser-form.test.tsx src/components/trading/__tests__/strategy-card-flow.test.tsx src/components/trading/__tests__/card-body.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed", or "lc-tracer.test.tsx" is absent from the run output</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/trading-workbench.spec.ts -g "P20-1"</automated>
    <fails_when>non-zero exit, or the output lacks "1 passed", or contains "No tests found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `lc-tracer.test.tsx` 의 `it` 이 8개 이상이고 전부 통과한다(위 첫 명령 요약 줄에 failed 0)
    - `grep -c "useLcFieldCommit(" webapp/src/components/trading/limit-chaser-form.tsx` 가 1 이상
    - `grep -c 'data-lc-field' webapp/src/components/trading/lc/setting-group.tsx` 가 1 이상, `grep -c 'min-h-\[44px\]' webapp/src/components/trading/lc/setting-group.tsx` 가 1 이상
    - `grep -nE "(^|[\"' ])(sm|md|lg|xl|2xl):" webapp/src/components/trading/lc/setting-group.tsx webapp/src/components/trading/lc/inline-value-editor.tsx` 출력 0줄(뷰포트 브레이크포인트 없음)
    - `grep -c "'반영하지 못했어요 · Enter 로 다시 시도해 주세요'" webapp/src/components/trading/lc/use-lc-field-commit.ts` 가 1 이상
    - Playwright `P20-1` 1 passed · 게이트웨이 `SetLimitChaserReq` 수신이 편집 전보다 정확히 1 증가
    - typecheck 종료 코드 0
    - `git status --porcelain -- webapp/e2e/specs/zz-theme-gallery.spec.ts` 결과가 `??`(추적 안 됨) 그대로
  </acceptance_criteria>
  <done>데스크톱에서 「호가변경」 한 행이 토스식 44px 행으로 서고, 인라인 편집 → Enter 한 번으로 서버에 즉시 반영되며 에코로만 값이 바뀐다 — jsdom 통합 테스트와 실브라우저(relay + 스텁 게이트웨이) P20-1 이 둘 다 green 이고 커밋됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 반영 상태 기계 완성 — 타임아웃 · 직렬화 · 무장 가드 · 토글 되돌림 + CardBody 가 unacked · 현재가를 내린다</name>
  <files>webapp/src/components/trading/lc/use-lc-field-commit.ts, webapp/src/components/trading/lc/__tests__/use-lc-field-commit.test.tsx, webapp/src/components/trading/card/card-body.tsx, webapp/src/components/trading/limit-chaser-form.tsx</files>
  <read_first>
    - webapp/src/components/trading/lc/use-lc-field-commit.ts (Task 1 결과)
    - webapp/src/components/trading/card/card-body.tsx (:194-277 — `card` 구조분해 · `LimitChaserForm` props)
    - webapp/src/components/trading/card/strategy-card.tsx (:321-345 `handleSent` · `acceptAnswer` — 같은 커밋의 두 이펙트가 `acceptAnswer` 를 두 번 부를 수 있다 :385-413)
    - webapp/src/components/trading/limit-chaser-form.tsx (:521-576 `canArmBuy/Sell/Sweep` · `gateBlocked` · :602-656 `handleSubmit` 의 철거 면제 + 무장 차단 조립)
    - .planning/phases/20-toss-order-ticket/20-RESEARCH.md (§Q5 상태 기계 · Pitfall 1·2·4 · Anti-Patterns)
  </read_first>
  <behavior>
    - `unacked` 가 in-flight 중 true 가 되면 → `failures[f].reason === 'timeout'` · 텍스트 「반영하지 못했어요」
    - 직렬화: A 전송(in-flight) 중 B 확정 → `'queued'` · `send` 1회뿐 · `queuedFields === ['B필드']` · A 에코(서버 일치) 렌더에서는 B 를 아직 보내지 않고, 그다음 `serverAnswerSeq` 증가 렌더에서 B 를 보낸다(B cfg 는 A 가 반영된 새 서버 값 + B) — 그 뒤 추가 `answerSeq` 증가가 없으면 B 가 거부로 오판되지 않는다
    - 같은 필드를 대기 중에 다시 확정하면 값만 바뀌고 자리는 그대로(대기 건수 불변)
    - A 가 거부되면 B 는 보내지지 않고 `failures.B.reason === 'rejected'` 로 표시된다(조용한 드롭 0 · 자동 전송 0)
    - `armBlockOf` 가 문자열을 돌려주면 → `'blocked'` · `failures[f] = { reason: 'armBlocked', text: 그 문자열 }` · `send` 0 (대기 건을 꺼낼 때도 다시 판정)
    - 토글 종류: `send` true 뒤 `setForm` 으로 낙관 반영, 거부·타임아웃·대기 폐기 시 확정 전 값으로 되돌린다; `send` false 면 `setForm` 호출 0
    - `server == null` 에서 `buyEnabled` 토글 → 전송된다(게이트) · `cancelTradeEnabled` 토글 → `'local'`
    - `field === 'buyOrderAmount'` 이고 에코 `buyOrderAmount === 0` 이면 `serverAnswerSeq` 증가만으로 성공
    - Pitfall 4 순서: `serverAnswerSeq` 가 먼저 오르고(실패 표시) 다음 렌더에 서버 값이 일치하면 성공으로 바뀐다
    - CardBody 가 `card.unacked` 와 `quote.p`(없거나 0 이면 0)를 폼에 넘긴다 — card-body 테스트가 폼 props 로 확인
  </behavior>
  <action>
    ① `use-lc-field-commit.ts` 를 확장한다. 옵션에 `unacked?: boolean`(기본 false) · `armBlockOf?: (values) => string | null` 을 더한다. **대기열**: 필드당 최대 1건인 순서 있는 목록 `{ field, value, kind, prevValue }` — in-flight 가 있을 때의 확정은 여기에 넣고 `'queued'`, 같은 필드면 값만 교체하고 자리는 유지한다. Task 1 의 「같은 필드 in-flight → blocked」 규칙은 걷고 대기로 바꾼다. **꺼내기 규칙(핵심):** in-flight 의 성공이 판정된 뒤, `serverAnswerSeq` 가 그 건의 `answerSeqAtSend` 와 달라진 렌더에서만 다음 대기 건을 보낸다 — 카드는 성공 에코 한 번에 `acceptAnswer` 를 한 커밋 안에서 1~2회 부르고(`strategy-card.tsx` :385-413) 그 증가는 폼 이펙트보다 한 렌더 늦게 보이므로, 같은 렌더에서 곧바로 보내면 뒤따르는 증가가 새 건을 「거부」로 오판한다. 꺼낼 때마다 no-op·무장 판정을 새 서버 값으로 다시 하고, 기준값은 늘 `formFromServer(server, formRef.current)`(서버 없으면 `formRef.current`) + 그 필드다. **실패 판정:** in-flight 중 `unacked` true → `timeout`; `serverAnswerSeq` 변화 + 값 불일치 → `rejected`(`buyOrderAmount` 특례: 에코 `buyOrderAmount === 0` 이면 답 도착만으로 성공 — `lib/limit-chaser.ts` 「서버가 모른다」). 실패하면 대기 건은 **하나도 보내지 않고** 각 필드를 `rejected` 실패로 기록한다(자동 전송 금지 · 조용한 드롭 금지). **무장 가드:** 전송 직전 `armBlockOf({ ...기준값, [field]: value })` 가 문자열이면 `armBlocked` 실패 + `'blocked'`. **토글 종류:** `send` 가 true 일 때만 `setForm(prev => ({ ...prev, [field]: value }))` 낙관 반영하고 `prevValue`(확정 직전 폼 값)를 기억해 실패·폐기 시 되돌린다(대기에 들어간 토글도 같은 규칙으로 즉시 낙관 표시하고 폐기되면 되돌린다). 게이트 4종(`LC_GATE_FIELDS`)은 `server == null` 이어도 전송한다(첫 스위치 = 등록, Phase 16 D-05). 늦은 에코 전이는 대기에서 폐기된 필드에도 적용한다.
    ② `webapp/src/components/trading/lc/__tests__/use-lc-field-commit.test.tsx` 를 신설한다 — `renderHook((p) => useLcFieldCommit(p), { initialProps })` + `rerender` 로 `server` · `serverAnswerSeq` · `unacked` 를 바꿔 가며 `<behavior>` 전부와 Task 1 규칙(no-op · local · disconnected · 성공 강조 900ms · 거부 · 늦은 에코)을 단언한다. `send`·`setForm`·`onSent` 는 `vi.fn()`, `buildCfg` 는 스프레드로 `isin`·`accountNo`·`exchange`·`crud`(`crudOf`)·`buyOrderQty` 를 채우는 테스트 전용 함수, `formRef` 는 `{ current: … }` 객체. 15개 이상의 `it`.
    ③ `webapp/src/components/trading/limit-chaser-form.tsx`: props 에 `unacked?: boolean`(기본 false · JSDoc 「카드 3초 무응답 — 필드 확정 실패 판정 입력, UI-SPEC A10」)과 `currentPrice?: number`(기본 0 · JSDoc 「현재 체결가 — 20-03 시트 칩 『현재가』 원천」, 이 태스크에서는 구조분해하지 않아도 된다)를 더한다. 모듈 수준 순수 함수 `canArmOf(values): Record<GateKey, boolean>` 을 만들어 기존 `canArmBuy`/`canArmSell`/`canArmSweep` 산출식을 **그 함수 하나로 옮기고**(컴포넌트 안 세 값은 `canArmOf(form)` 에서 읽는다 — 산출식 복제 금지), `armBlockOf(values)` = 철거 의도(`isDeleteIntent(values)`)면 null, 아니면 `GATE_KEYS.find(k => values[k] && !canArmOf(values)[k])` 의 `${GATE_LABEL[k]}${ARM_BLOCKED_SEP}${armBlockedTextOf(k, values)}`(기존 `handleSubmit` 조립 규칙 그대로)를 훅에 넘긴다. `unacked` 도 훅에 넘긴다.
    ④ `webapp/src/components/trading/card/card-body.tsx`: `card` 구조분해에 `unacked` 를 더하고 `LimitChaserForm` 에 `unacked={unacked}` 와 `currentPrice={quote !== null && quote.p > 0 ? quote.p : 0}` 를 넘긴다. 그 밖(더티 prop · 그리드 · 사다리)은 이 태스크에서 건드리지 않는다. `card-body.test.tsx` 는 이 태스크 파일 목록 밖이다 — 기존 단언이 깨지면 그 원인을 이 변경에서 찾아 고친다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/lc/__tests__/use-lc-field-commit.test.tsx src/components/trading/lc/__tests__/lc-tracer.test.tsx src/components/trading/__tests__/card-body.test.tsx src/components/trading/__tests__/limit-chaser-form.test.tsx src/components/trading/__tests__/strategy-card-flow.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed", or "use-lc-field-commit.test.tsx" is absent from the output</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp run test</automated>
    <fails_when>non-zero exit, or output contains "error TS", or the "Test Files" summary line contains "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `use-lc-field-commit.test.tsx` 의 `it` 15개 이상 통과 · 직렬화 케이스가 「A 에코 렌더에서 send 1회 → answerSeq 증가 렌더에서 send 2회」를 단언한다
    - `grep -c "function canArmOf" webapp/src/components/trading/limit-chaser-form.tsx` 가 1 이상
    - `grep -c "unacked={unacked}" webapp/src/components/trading/card/card-body.tsx` 가 1 이상
    - `grep -c "currentPrice=" webapp/src/components/trading/card/card-body.tsx` 가 1 이상
    - webapp 전체 suite 통과(기준선 1731 이상 통과 · 실패 0) · typecheck 0
    - 플랜 시작 커밋을 `PLAN_BASE`(실행 시작 때 `git rev-parse HEAD`)로 기록해 두고 `git diff --stat "$PLAN_BASE" -- packages/shared/src/relay.ts relay supabase` 출력 0줄(프로토콜 무변경 — 태스크 커밋 뒤에도 성립)
  </acceptance_criteria>
  <done>필드 확정 상태 기계가 성공·거부·타임아웃·끊김·무장 불가·대기·늦은 에코·토글 되돌림을 모두 다루고, 카드의 3초 무응답 신호와 현재가가 폼까지 내려온다 — 단위 테스트로 잠겼다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 폼 → relay wss (`lc.set`) | 사용자가 확정한 값이 자기 계좌 전략으로 나간다. relay/게이트웨이 스키마가 최종 검증(`UIntSchema` 등) — 클라 판정은 신뢰 경계가 아니다 |
| relay → 브라우저 (60 에코 · 54 통지) | 서버 진실. 에코가 목록 표시를 이긴다(Phase 18 D-27) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering(자기 자산) | 인라인 Enter 즉시 반영(오터치) | high | mitigate | 명시적 Enter/blur 확정만 전송 · Esc 취소 · 같은 값 전송 0 · in-flight 1건 직렬화 · 무장 불가 값은 `armBlockOf` 로 전송 차단(Task 2) · 끄기 방향은 늘 허용(T-16-44) |
| T-20-02 | Tampering | 사용자가 누르지 않은 재전송 | high | mitigate | 실패·타임아웃 뒤 대기 건 폐기 + 실패 표시(Task 2) · 타이머는 표시만(카드 `handleSent`) · 「다시 시도」는 사용자 입력으로만 |
| T-20-03 | Tampering | 오래된 로컬 값으로 다른 필드 덮어쓰기 | high | mitigate | cfg 기준값 = `formFromServer(server, formRef)` + 바꾼 필드 1개(Task 1 ①e) · 값 필드 낙관 반영 없음 |
| T-20-04 | Repudiation | 거부를 성공으로 표시(무로그 fail-safe) | high | mitigate | 성공 = 에코의 그 필드 값 == 보낸 값 · 거부/타임아웃/끊김 각각 문구(Task 1 ① · Task 2 ①) · 카드 `card-server-error` 경로 유지 |
| T-20-08 | Denial of Service | 연타로 `lc.set` 폭주 | medium | mitigate | 동시 in-flight 1건 + 필드당 대기 1건(Task 2 ①) — 전송량은 사용자 확정 수 이하 |
| T-20-SC | Tampering | npm/pip 설치 | low | accept | 이 플랜은 패키지를 설치하지 않는다(RESEARCH Package Legitimacy Audit: 없음) |
</threat_model>

<verification>
- 트레이서: `lc-tracer.test.tsx`(jsdom 통합) + Playwright `P20-1`(실 relay + 스텁 게이트웨이) 둘 다 green.
- 상태 기계: `use-lc-field-commit.test.tsx` 15+ 케이스 green.
- 회귀: webapp 전체 suite · typecheck green. 프로토콜 파일 diff 0.
</verification>

<success_criteria>
- 마우스 사용자가 「호가변경」을 인라인으로 바꾸면 Enter 한 번에 게이트웨이까지 나가고, 에코가 오면 행이 바뀌고 900ms 강조된다.
- 거부·무응답·끊김·무장 불가·대기가 모두 화면 문구 또는 되돌림으로 드러나고, 사용자가 누르지 않은 전송은 0이다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `packages/shared/src/krxTick.ts` — `krxTickSize` · `tickUp` · `tickDown` · `priceInputIssue` · `PriceIssue` | 순수 함수(shared) | 20-02 |
| `packages/shared/src/krxTick.test.ts` | 단위 테스트 | 20-02 |
| `webapp/src/lib/numpad.ts` — `PadUnit` · `PadState` · `padInit` · `padKey` · `PAD_CHIPS` · `PadChip` · `PadChipOp` · `padChipDisabled` · `applyPadChip` · `padValue` · `formatPadDisplay` · `padIssue` · `priceIssueText` · `canConfirmPad` · `stepValue` · `PAD_MAX_DIGITS` | 순수 함수 | 20-02 |
| `webapp/src/lib/__tests__/numpad.test.ts` | 단위 테스트 | 20-02 |
| `webapp/src/lib/use-edit-mode.ts` — `useEditMode` · `EditMode` · `COARSE_POINTER_QUERY` | 훅 | 20-03 |
| `webapp/src/lib/__tests__/use-edit-mode.test.ts` · `webapp/src/lib/__tests__/match-media.ts`(`mockPointer` · `emitPointerChange` · `restoreMatchMedia`) | 테스트 · 헬퍼 | 20-03 |
| `webapp/src/components/trading/lc/use-lc-field-commit.ts` — `useLcFieldCommit` · `LcFieldKey` · `LcCommitKind` · `LcFailReason` · `LcCommitFailure` · `LcCommitOutcome` · `UseLcFieldCommitOptions` · `LC_FLASH_MS` · `LC_COMMIT_TEXT` · `LC_GATE_FIELDS` | 상태 기계 훅 | **20-01** |
| `webapp/src/components/trading/lc/inline-value-editor.tsx` — `InlineValueEditor` | 컴포넌트 | **20-01** (확장 20-05) |
| `webapp/src/components/trading/lc/setting-group.tsx` — `SettingRow` · `FailureBubble` (20-01) · `SettingGroup` · `GroupSwitch` · `CheckValueRow` · `WatchTargetRow` · `DerivedRow` (20-04) | 컴포넌트 | **20-01** · 20-04 |
| `webapp/src/components/trading/lc/number-pad-sheet.tsx` — `NumberPadSheet` · `NumberPadSheetProps` | 컴포넌트(Radix Dialog · body 포털) | 20-03 |
| `webapp/src/components/trading/lc/lc-fields.ts` — `LC_BUY_GROUPS` · `LC_SELL_GROUPS` · `LcGroupSpec` · `LcRowSpec` · `LC_SWITCH_LABEL` · `lcRowById` · `lcNavigableRows` | 필드 스펙(단일 원천) | 20-04 |
| `limit-chaser-form.tsx` 신규 순수 함수 `canArmOf` · `armBlockOf` | 무장 판정 | **20-01** |
| `manual-order-form.tsx` 내부 `TicketBox` · 새 props `currentPrice` · `upperLimit` | 컴포넌트 | 20-06 |
| CSS 토큰 `--group-bg` · `--switch-off` · `--dim`(`:root` · `.dark`) | 토큰 | 20-03 |
| 테스트: `lc/__tests__/lc-tracer.test.tsx`(**20-01**·20-03) · `use-lc-field-commit.test.tsx`(**20-01**) · `number-pad-sheet.test.tsx`(20-03) · `setting-group.test.tsx`(20-04) · `inline-value-editor.test.tsx` · `inline-navigation.test.tsx`(20-05) | 단위/컴포넌트 | 각 플랜 |
| e2e: `P20-1`(**20-01**) · describe 「Phase 20 — 터치 기기 시트」 `P20-2`(20-03) · `P20-3` 최악값 폭 · `P20-4` 매수가격 시트 · `P20-5` 수동주문 시트(20-07) · orderbook 11 대체 · a11y ③⑦ 대체(20-07) | Playwright | 각 플랜 |
| 제거: `LIMIT_CHASER_DIRTY_HINT` · `cardDirtyHint` export(20-04) · `order-panel.tsx` 의 `TICK_TABLE`/`tickFromTable`(20-02) | 제거 | 20-02 · 20-04 |

## Planner assumptions (flagged for user visibility)

- **A-P1 (RESEARCH Open Q1):** 미등록 전략(server 없음)에서 값·체크·감시대상 확정은 로컬 반영만 하고 전송하지 않는다. 등록은 그룹 스위치 4개만 한다(Phase 16 D-05 「첫 스위치 = 등록」 유지). 사용자가 「적용 = 즉시 등록」을 기대했다면 다르게 느껴질 수 있다 — 반대로 보내면 철거 프레임 낭비·의도치 않은 등록(RESEARCH Pitfall 2).

## Spec-less probe note

Spec-less probe fallback: **visible skip** — 이 phase 는 SPEC.md 도 요구사항 ID 도 없어 probe 가 만든 edge 술어·금지 항목이 없다. 대신 UI-SPEC `## UI Considerations` 와 CONTEXT 불변 목록 · RESEARCH Security Domain 에서 `must_haves` 를 직접 썼다.

## Scope exclusion (roadmap goal item deliberately deferred)

- **상단 상태줄 1줄 압축** — ROADMAP Phase 20 goal 에는 들어 있으나 **이 phase 의 7개 플랜 어디에서도 구현하지 않는다**. 근거: UI-SPEC 가정 A7(상태줄에 계좌 선택·거래소 세그먼트·LED 3칩·구간 배지·거부·반영 시각이 있어 폰 폭 1줄 압축은 라벨 잘림 = 오발주 불변식 위반 위험 · 우측 패널 재구성과 독립) + RESEARCH §Q6. 별도 quick 태스크로 분리한다. 플랜 검사(2026-09-25) WARNING 에 따라 여기 명시 — 사용자 확인 필요.

<output>
Create `.planning/phases/20-toss-order-ticket/20-01-SUMMARY.md` when done
</output>
