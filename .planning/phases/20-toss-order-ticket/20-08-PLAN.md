---
phase: 20-toss-order-ticket
plan: 08
type: execute
wave: 6
depends_on: ["20-07"]
files_modified:
  - webapp/src/components/stock/stock-orderbook-section.tsx
  - webapp/src/components/stock/__tests__/stock-orderbook-section.test.tsx
  - webapp/src/components/stock/__tests__/orderbook.test.tsx
  - webapp/e2e/specs/orderbook.spec.ts
  - webapp/e2e/overflow.ts
  - .planning/phases/20-toss-order-ticket/20-UI-SPEC.md
  - .planning/phases/20-toss-order-ticket/20-VALIDATION.md
autonomous: true
gap_closure: true
requirements: []

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-24 · 본문 ≥700: 상태줄 = 「● DMA {상태}」 · 계좌 select(「{번호} · {이름}」) · 거래소 KRX|NXT · 래치 LED 점 3개 · 구간 배지 · (복구 불가면 「다시 연결」) · 반영 시각. 보이는 글자 「계좌」「반영」이 없다 — 계좌는 select 의 aria-label 「계좌」 + title 「{번호} · {이름}」, 반영은 sr-only 「반영 」 + title 「서버 반영 시각」(작업대 상태줄 `stat-applied` 와 같은 규칙)"
    - "D-24 · 서버 거부 문장은 모든 폭에서 상태줄이 아니라 그 아래 고지 줄(`orderbook-notices`)의 첫 줄이다 — `role=alert` · `data-slot=orderbook-server-error` · 출처 배지 `serverMsgBadge` 텍스트 접두 · 상태줄 안 alert 0 · 같은 거부 문장이 화면에 한 번만"
    - "D-24 · 래치 LED 는 모든 폭에서 칩이 아니라 점 3개(`LatchLed variant=\"dot\"` — 접힌 카드 헤더와 같은 문법)다. 각 점은 sr-only 「{매수|매도|취소} 래치 {OFF|대기|감시}」로 상태를 말하고 툴팁이 늘 뜨며, 눌리는 점은 전과 같은 `handleArm` 을 부른다"
    - "D-24 · 본문 <700: 연결 표시 = 점 + 반영 시각(글자 「DMA {상태} · 반영 」은 sr-only · title 「DMA {상태} · 반영 {시각}」) · 계좌는 닫힌 상태에서 이름만(select 는 같은 요소 하나 — 투명 오버레이로 OS 목록을 열고 목록·title·선택값은 「{번호} · {이름}」 전체) · 구간 배지와 연결 이상 문구(「DMA {상태}」 + 복구 불가면 「다시 연결」)는 고지 줄로 내려간다"
    - "D-24 실측 계약(실브라우저 · 최악값 · 목업 안 C 와 같은 내용): 거부·예약구간 시나리오와 미반영·장전 시나리오는 본문 344 · 374 · 700 · 830 · 992 · 뷰포트 1440 에서 상태줄 1줄 · 끊김(다시 연결하지 못했어요 + 다시 연결)·시간외종가 시나리오는 344 · 374 · 830 · 992 · 1440 에서 1줄이고 700 에서 2줄 이하 · 모든 폭·시나리오에서 잘림 0(스크롤 판정 · 잎 좌표 판정 · select 본래 폭 판정 · 고지 줄 스크롤 판정)"
    - "불변식: 폭 분기는 `@min-[700px]/lc:` 컨테이너 쿼리 하나뿐(뷰포트 브레이크포인트 0 · 새 경계 숫자 0) · 상태줄은 `flex-wrap` 을 유지해 폭이 모자라면 잘리지 않고 줄이 늘어난다 · 말줄임/줄 제한 유틸리티 0 · 글자 크기 불변"
    - "동작 불변: 계좌 변경은 같은 `onAccountChange` 한 경로(select 요소 하나) · 거래소 세그먼트는 같은 ToggleGroup · 「다시 연결」은 어느 자리에 있든 같은 `reconnect` · LED 클릭은 같은 `handleArm` · 주문 경로·재연결·계좌 전환·거래소 의미를 바꾸는 코드 줄 0(바뀐 파일이 files_modified 밖으로 나가지 않는다)"
    - "20-UI-SPEC.md 가 계약이다 — 가정 A7 은 D-24 로 대체됐다고 표기되고, 상태줄 해부(§10 · 폭별 항목 · 수치 · 접근성 · 실측 계약)가 추가됐다"
  artifacts:
    - path: webapp/src/components/stock/stock-orderbook-section.tsx
      provides: "D-24 안 C 상태줄(OrderbookStatusBar) + 고지 줄(TabNotices: 거부 · 폰 전용 연결 이상/구간 배지)"
      contains: "orderbook-notices"
    - path: webapp/src/components/stock/__tests__/stock-orderbook-section.test.tsx
      provides: "새 구조 단위 테스트 — 점 LED · sr-only/title · 거부 위치 · 계좌 표기 · 폭별 클래스"
      contains: "orderbook-server-error"
    - path: webapp/src/components/stock/__tests__/orderbook.test.tsx
      provides: "⑬ 이관 — 복구 불가 시 「다시 연결」 두 자리(상태줄 ≥700 · 고지 줄 <700) 모두 같은 reconnect"
      contains: "orderbook-conn-notice"
    - path: webapp/e2e/specs/orderbook.spec.ts
      provides: "P20-6 ①②③ — 최악값 × 본문 폭별 상태줄 줄 수 · 잘림 0 · 접근성"
      contains: "P20-6"
    - path: webapp/e2e/overflow.ts
      provides: "세 번째 잘림 판정 `selectsClipped`(보이는 select 가 자기 본래 폭보다 좁아졌는가)"
      contains: "selectsClipped"
    - path: .planning/phases/20-toss-order-ticket/20-UI-SPEC.md
      provides: "A7 → D-24 대체 표기 + §10 상단 상태줄 계약"
      contains: "D-24"
  key_links:
    - from: webapp/src/components/stock/stock-orderbook-section.tsx
      to: webapp/src/components/trading/latch-led.tsx
      via: "LatchLed variant=\"dot\" × 3 (판정은 latchLedStateOf 한 곳 그대로)"
      pattern: "variant=\"dot\""
    - from: webapp/src/components/stock/stock-orderbook-section.tsx
      to: webapp/src/components/trading/card/strategy-card.tsx
      via: "card.lastError → TabNotices role=alert (CardNotices 와 같은 마크업 · serverMsgBadge)"
      pattern: "orderbook-server-error"
    - from: webapp/e2e/specs/orderbook.spec.ts
      to: webapp/e2e/overflow.ts
      via: "scrollOverflowing · leavesOverflowing · selectsClipped"
      pattern: "selectsClipped"
    - from: webapp/e2e/specs/orderbook.spec.ts
      to: webapp/src/lib/use-relay-socket.ts
      via: "page.routeWebSocket 통과 프록시 + state 프레임 주입(manual_required) — 리듀서가 accounts 를 유지(:633-646)"
      pattern: "routeWebSocket"
  prohibitions:
    - statement: "상태줄·고지 줄 글자를 자르거나 말줄임하지 않는다 — 1줄이 안 되면 목업 안 C 의 기하(간격·패딩)로 맞추고, 그래도 안 되면 줄이 늘어나게 둔다(라벨 잘림 0 = 오발주 불변식 · D-24)"
      status: resolved
      verification: test
    - statement: "뷰포트 브레이크포인트·새 컨테이너 경계를 만들지 않는다 — 폭 분기는 `@min-[700px]/lc:` 만(§2.2b · D-24)"
      status: resolved
      verification: test
    - statement: "작업대 카드(`strategy-card.tsx`)·작업대 상태줄(`workbench-status-bar.tsx`)·`latch-led.tsx` 를 고치지 않는다 — D-24 의 대상은 호가 탭 `OrderbookStatusBar` 하나다(VERIFICATION 갭 문구의 strategy-card 는 D-24 가 대상 확정으로 대체)"
      status: resolved
      verification: test
    - statement: "옛 단언을 지워서 초록을 만들지 않는다 — 보이는 「계좌」 · LED 칩 · 「반영 —」 · 상태줄 「다시 연결」 단언은 새 구조의 등가 단언으로 이관한다(케이스 수 불감소)"
      status: resolved
      verification: test
    - statement: "webapp/e2e/specs/zz-theme-gallery.spec.ts 와 .planning/milestone.lock 을 실행·수정·스테이징·커밋하지 않는다 · git push · relay/webapp 배포를 하지 않는다(theme/toss-b 미병합 실험 브랜치)"
      status: resolved
      verification: judgment
---

<objective>
검증 갭 「상단 상태줄 1줄 압축」(20-VERIFICATION truth 13)을 사용자 결정 **D-24(안 C)** 대로 닫는다. 대상은 종목상세 호가주문 탭의 `OrderbookStatusBar`(`stock-orderbook-section.tsx`) 하나다.

- 본문 ≥700(안 B 와 같음): 거부 문장 → 고지 줄 · LED 칩 → 점 3개(sr-only · 툴팁) · 보이는 「계좌」「반영」 → sr-only/title.
- 본문 <700 추가: 연결 = 점 + 반영 시각 · 계좌는 닫힌 상태에서 이름만 · 구간 배지와 연결 이상 문구 → 고지 줄.
- `flex-wrap` 유지 — 폭이 모자라면 잘리지 않고 2줄.

VERIFICATION 갭은 `strategy-card.tsx` 를 가리켰다. 그러나 D-24 가 대상을 `OrderbookStatusBar` 로 확정했으므로 작업대 카드·작업대 상태줄은 범위 밖이다. `trading-workbench.spec.ts` 케이스 8 도 그대로 둔다.

Purpose: 로드맵 목표의 네 번째 산출물을 표현만 바꿔 채운다. 목표는 폰 344/374 전 시나리오 1줄이고, 유일한 2줄은 본문 700~829 끊김이다. 이것을 실브라우저 최악값으로 증명하고 UI-SPEC 계약을 D-24 로 갱신한다. 주문·재연결·계좌·거래소 동작은 한 줄도 바꾸지 않는다.

Output: 새 상태줄·고지 줄 · 단위 테스트 이관/신설 · e2e P20-6 ①②③ · `overflow.ts` select 폭 판정 · UI-SPEC §10 · VALIDATION 20-08 행.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-toss-order-ticket/20-CONTEXT.md
@.planning/phases/20-toss-order-ticket/20-VERIFICATION.md
@.planning/phases/20-toss-order-ticket/20-UI-SPEC.md
@.planning/phases/20-toss-order-ticket/20-07-SUMMARY.md
@.planning/sketches/002-toss-order-ticket/status-strip-variants.html
@webapp/src/components/stock/stock-orderbook-section.tsx
@webapp/src/components/stock/__tests__/stock-orderbook-section.test.tsx
@webapp/e2e/specs/orderbook.spec.ts
@webapp/e2e/overflow.ts

작업 루트는 worktree `/Users/alex/repos/gh-radar/.claude/worktrees/toss-b`(branch theme/toss-b). 모든 명령은 이 루트에서 실행한다.

Playwright 조건:
- `workers: 1` · `baseURL http://localhost:3100` · `reuseExistingServer: !CI`.
- e2e 전에 `lsof -iTCP:3100 -sTCP:LISTEN` 으로 리스너를 확인한다. 리스너가 없거나 이 worktree 의 `webapp` 서버여야 한다. 메인 체크아웃 서버면 멈추고 보고한다(T-20-16).
- dev 포트는 3100 이다(3000 가정 금지).
- 기본 뷰포트는 Desktop Chrome 1280×720 이다.

**시각 계약 = 목업 `status-strip-variants.html` 의 안 C.** 목업 CSS 가 실제 코드 클래스를 옮겨 적었다(주석 `.strip` · `.acct` · `.xseg` · `.led` · `.wb` 등).

목업에서 새로 생긴 조각과 그 수치:
- 목업 `.conn`: gap 5 · muted-fg · 시각은 `--fg-2`.
- 목업 `.acct-s`: h 24 · 좌 7 · 우 5 · gap 3 · 11/600 · `--muted` 면 · 쉐브런 10px. select 는 `position:absolute; inset:0; opacity:0; font-size:16px`.
- 목업 `.ledds`: gap 2. `.ledd`: 24 히트 · 10 점.
- 목업 `.notices`: flex-col gap 4.
- 목업 `.n-down`: flex-wrap · gap 4/10 · 「DMA <b>{상태}</b>」 + 다시 연결.
- 목업 `.frame.vC` 폰 상태줄 column-gap 8, ≥700 은 12.

**플래너가 이 목업을 실브라우저(Pretendard 로드 확인 `__FONT_OK=true`)로 다시 잰 안 C 수치(1줄 여유 px):**

| 시나리오 | 344 | 374 | 700 | 830 | 992 | 1152 |
|---|---|---|---|---|---|---|
| ① 거부 · 예약구간 | 1줄 +10.5 | 1줄 +40.5 | 1줄 +72.3 | 1줄 +202.3 | 1줄 +364.3 | 1줄 +524.3 |
| ② 미반영 · 장전 | 1줄 +10.5 | 1줄 +40.5 | 1줄 +85.8 | 1줄 +215.8 | 1줄 +377.8 | 1줄 +537.8 |
| ③ 끊김 · 시간외 | 1줄 +10.5 | 1줄 +40.5 | **2줄** | 1줄 +74.9 | 1줄 +236.9 | 1줄 +396.9 |

폰 344 여유가 10.5px 뿐이다. 그래서 폰 기하(간격 8 · 연결 gap 5 · 계좌 칩 7/5/3 · 점 gap 2)는 목업 값에서 벗어나면 안 된다.

<interfaces>
```ts
// stock-orderbook-section.tsx (지금)
function OrderbookStatusBar(p: { status: RelayStatus; statusLabel: string; accounts: RelayAccount[]; accountNo: string;
  onAccountChange(no: string): void; exchange: RelayExchange; exchangeChoices: readonly RelayExchange[];
  onExchangeChange(x: RelayExchange): void; card: StrategyCardState; windowBadge: ReturnType<typeof queuedWindowBadgeOf>;
  onReconnect?: () => void }): JSX.Element;          // data-slot="orderbook-status-bar" data-status={status}
function TabNotices(p: { card: StrategyCardState; exchange: RelayExchange; switching: boolean; nxtEmpty: boolean }): JSX.Element | null;
// 섹션: const isBroken = UNRECOVERABLE_STATES.has(status)  → onReconnect={isBroken ? reconnect : undefined}
// 라벨: statusLabel === '' ? RELAY_STATE_LABELS.connecting : statusLabel   (RELAY_STATE_LABELS.manual_required = '다시 연결하지 못했어요')

// StrategyCardState (card/strategy-card.tsx:195-210)
{ ledServer; handleArm(kind); lastError: { text: string; src: string } | null; appliedAt: string | null; banner: string | null; unacked: boolean }
// CardNotices(strategy-card.tsx:849-890) — 거부 마크업 정본: <p role="alert" data-slot="card-server-error" class="m-0 min-w-0 break-keep text-[var(--destructive)]">
//   <span data-slot="card-server-error-src" class="font-semibold">{serverMsgBadge(src)}</span> {text}</p>

// latch-led.tsx — 고치지 않는다
LatchLed({ kind: 'buy'|'sell'|'cancel', server, onArm, variant?: 'chip'|'dot' })
// dot: data-slot="latch-led" data-variant="dot" · size-6 히트 · 10px 점 · sr-only "{이름} 래치 {라벨}" · 툴팁 늘 뜸("{이름} · {라벨}" + C# 원문)

// workbench-status-bar.tsx:160 — 반영 시각 sr-only 규칙 정본
<span data-testid="stat-applied" title="서버 반영 시각" className="mono whitespace-nowrap"><span className="sr-only">반영 </span>{time}</span>

// queued-window.ts: queuedWindowBadgeOf(w) → { text, tone: 'regular'|'queued'|'offhours' } | null
//   g3/g2 → "시간외종가 G3 창"/"시간외종가 G2 창"(offhours) · open → "예약구간 · 조각 최대 {n}"(queued) · preopen → "장전 · 예약매수/매도"(queued) · "정규"

// e2e — webapp/e2e/overflow.ts (저장소 유일 판정): leavesOverflowing(scope, right) · scrollOverflowing(page, rootSelector)
// relay 픽스처(e2e/fixtures/relay.ts): relay.seedLimitChasers([...]) · relay.pushServerMessage({ level, source, isin, accountNo, message, kind })
//   relay.requestLog() · DMA_MSG.SetLimitChaserReq · E2E_ISIN · E2E_ACCOUNT_NO('1234567801')
//   relay.gateway.respondLoginWithAccounts([{ accountNo, name }]) · relay.gateway.respondLogin()  ← 기본(SAMPLE_ACCOUNTS 1건) 복원. reset() 은 로그인 응답을 되돌리지 않는다
//   const sock = await relay.gateway.waitForConnection(15_000); relay.gateway.sendQueuedWindowState(sock, { open, maxPieces, preopenOpen, g2Open, g3Open, nxtPreopenOpen })
//   (패턴: trading-workbench.spec.ts P20-3 :2235-2262 — 끝나면 닫힌 창으로 되돌린다)
// Playwright 1.59: page.routeWebSocket(url|RegExp, (ws: WebSocketRoute) => { ws.connectToServer(); }) — onMessage 를 달지 않으면 양방향 자동 전달
//   ws.send(JSON.stringify({ t: 'state', s: 'manual_required' }))  → 페이지 쪽으로 프레임 주입. 리듀서는 accounts 생략 시 유지(use-relay-socket.ts:633-646)
```
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (트레이서): 본문 ≥700 한 경로 — 거부 문장 → 고지 줄 · LED 점 3개 · 「계좌」「반영」 sr-only/title → 실브라우저 거부 시나리오 700 · 830 · 992 · 1440 에서 1줄 (D-24)</name>
  <precondition>포트 3100 이 비어 있거나 이 worktree 의 `webapp` dev 서버가 떠 있다(`lsof -iTCP:3100 -sTCP:LISTEN`)</precondition>
  <files>webapp/src/components/stock/stock-orderbook-section.tsx, webapp/src/components/stock/__tests__/stock-orderbook-section.test.tsx, webapp/e2e/specs/orderbook.spec.ts</files>
  <read_first>
    - webapp/src/components/stock/stock-orderbook-section.tsx (전체 — `OrderbookStatusBar` :331-502 · `TabNotices` :504-550 · 파일 머리 주석 ④)
    - webapp/src/components/trading/card/strategy-card.tsx (:840-890 `CardNotices` — 거부 마크업 정본)
    - webapp/src/components/trading/workbench/workbench-status-bar.tsx (:150-170 `stat-applied` — sr-only 「반영 」 규칙)
    - webapp/src/components/trading/latch-led.tsx (`LatchLedDot` — 점 문법 · sr-only · 툴팁)
    - webapp/src/components/stock/__tests__/stock-orderbook-section.test.tsx (전체 — ③ 을 이관한다)
    - webapp/src/components/trading/__tests__/strategy-card-flow.test.tsx (:440-495 — `messages: [msg({ lv:'ERROR', src:'LimitChaser', i: ISIN, m })]` 로 lastError 를 세우는 픽스처)
    - webapp/e2e/specs/orderbook.spec.ts (전체 — 조회구 :85-135 · 케이스 11 describe :423-483 · 9 가 마지막인 이유 :485-512)
    - webapp/e2e/specs/trading-workbench.spec.ts (:250-285 `sizeCardTo` · `expectCardNotClipped` — 폭 맞추기 루프 원형)
    - .planning/sketches/002-toss-order-ticket/status-strip-variants.html (안 B/C 의 ≥700 조각 `acctFull(false)` · `ledDots` · `right(s,false)` · `nErr` · 측정식 `measureFrame`)
  </read_first>
  <behavior>
    - ③(이관 · 삭제 금지) 상태줄에 `[data-slot="latch-led"][data-variant="dot"]` 가 buy · sell · cancel 순서로 3개 있고 variant 가 dot 이 아닌 LED 는 0개다. 첫 점의 textContent 가 /매수\s*래치\s*OFF/ 다(옛 칩 글자 단언의 등가물).
    - ③ 계좌 combobox(이름 「계좌」)는 하나이고 값이 ACCOUNT 다. `title` 은 「{ACCOUNT} · 위탁종합」이다. 계좌 래퍼 `[data-slot="orderbook-account"]` 의 직계 텍스트 노드에 「계좌」가 없다(옛 보이는 「계좌」 단언의 반전 이관).
    - ③ `[data-slot="orderbook-applied-at"]` 는 title 「서버 반영 시각」이고 `.sr-only` 자식 글자가 「반영」이며 전체 textContent 가 「반영 —」다(옛 `getByText('반영 —')` 이관).
    - ③-e(신설) ctx.messages 에 LimitChaser ERROR(i=ISIN, m=「주문 가능 금액이 부족합니다」)가 있으면 `[data-slot="orderbook-notices"]` 안에 `role=alert` · `data-slot=orderbook-server-error` 가 하나 선다. 그 안 `[data-slot="orderbook-server-error-src"]` 는 「[상따]」이고 본문이 들어 있다. 상태줄 안 `role=alert` 는 0개이고 상태줄 textContent 에 그 문장이 없다. 화면 전체에서 그 문장은 한 번만 나온다.
    - ③-e 구간을 모르고(`ctx.queuedWindow: undefined`) 거부·배너·미반영·전환·빈 NXT 도 없으면 `orderbook-notices` 가 렌더되지 않는다. 이 단언은 Task 2 뒤에도 참이다 — 연결 ready · 구간 모름이면 폰 전용 항목도 없다.
  </behavior>
  <action>
    **TDD(RED 먼저)**
    - `<behavior>` 단위 단언을 먼저 쓴다.
    - `pnpm --filter @gh-radar/webapp exec vitest --run src/components/stock/__tests__/stock-orderbook-section.test.tsx` 가 실패하는 것을 확인한다.
    - 그다음 구현한다(per D-24).

    **① `OrderbookStatusBar` — ≥700 모양(목업 안 B = 안 C 의 ≥700)**
    - 연결 필 `<span>`: `data-slot="orderbook-conn"` 을 달고 `whitespace-nowrap` 을 더한다. 글자·점·펄스는 그대로다.
    - 계좌 래퍼: `data-slot="orderbook-account"` 를 단다. 보이는 글자 「계좌」를 지운다.
      - select 의 `aria-label="계좌"` 는 유지한다.
      - select 에 `title` 을 단다. 값은 선택 계좌의 「{accountNo} · {name}」이고, 계좌가 없으면 속성 없음.
      - 옵션 문구 「{번호} · {이름}」은 그대로다.
    - 래치 LED: 세 `LatchLed` 에 `variant="dot"` 을 준다.
      - 래퍼는 `inline-flex items-center gap-0.5`(목업 `.ledds` gap 2)다. 줄바꿈 클래스를 빼 점 3개가 한 덩어리로 움직이게 한다.
      - `onArm={handleArm}` · `server={ledServer}` 는 그대로다.
    - 거부 `<span role="alert" data-slot="orderbook-server-error">` 블록을 상태줄에서 지운다. 아래 ②로 옮긴다.
    - 오른쪽 그룹의 시각: 작업대 `stat-applied` 와 같은 마크업으로 바꾼다.
      - `data-slot="orderbook-applied-at"` · `title="서버 반영 시각"` · `mono whitespace-nowrap`.
      - 안에 sr-only 「반영 」 + `{appliedAt ?? '—'}`.
    - 「다시 연결」 버튼·`ml-auto` 그룹·거래소 세그먼트·구간 배지·`data-status`·`flex-wrap` 은 그대로다.
    - 함수 JSDoc 을 D-24 안 C 설명으로 바꾼다. 「두 줄로 접힌다」는 「≥700 에서 폭이 모자랄 때만 2줄 · 잘리지 않는다」로 고친다.

    **② `TabNotices` — 거부 문장을 받는다**
    - 조건에 `card.lastError !== null` 을 더한다.
    - 컨테이너에 `data-slot="orderbook-notices"` 를 단다.
    - 맨 앞에 `CardNotices` 와 같은 모양의 거부 줄을 둔다.
      - `<p role="alert" data-slot="orderbook-server-error" className="m-0 min-w-0 break-keep text-[var(--destructive)]">`
      - 안에 `<span data-slot="orderbook-server-error-src" className="font-semibold">{serverMsgBadge(src)}</span>` + 공백 + 원문.
      - 문구를 다시 쓰지 않는다 — `msg.m` 원문 그대로다.
    - 나머지 순서(전환 중 → 빈 NXT → 배너 → 미반영)는 그대로다.
    - 파일 머리 주석 ④의 「복구 불가면 그 줄에 「다시 연결」」 문장을 Task 2 결과와 맞게 갱신한다(≥700 상태줄 · <700 고지 줄).

    **③ e2e 트레이서 — `orderbook.spec.ts`**
    바깥 relay describe 안에 새 describe `'Phase 20 — 상단 상태줄 1줄 (D-24 안 C)'` 를 **케이스 9 보다 앞에** 선언한다(9 가 게이트웨이를 내리므로 마지막이어야 한다).

    조회구·헬퍼를 파일 상단 조회구 옆에 더한다:

    - `sectionOf(page)` = `getByTestId('stock-orderbook-section')`
    - `notices(page)` = `[data-slot="orderbook-notices"]`
    - `sizeSectionTo(page, target)`
      - `sizeCardTo` 와 같은 재고-옮기고-다시 재는 루프다. 첫 뷰포트는 target+16, 높이는 1000, 최대 6회다.
      - 섹션 `clientWidth` 가 target 과 같아야 하고, 못 맞추면 `expect` 로 실패한다.
    - `stripLines(page)` — 목업 `measureFrame` 을 옮긴다.
      - 상태줄 안에서 `.sr-only` 밖에 있고 보이는(폭·높이 > 0) 요소 중 select·button 이거나 보이는 자식 요소가 없는 잎만 항목으로 모은다.
      - 세로로 2px 넘게 겹치면 같은 줄로 묶는다.
      - 글자 잎은 `Range` client rect 의 서로 다른 top(4px 이상 차이) 수를 그 항목의 줄 수로 센다. 줄마다 최댓값을 더한 합이 `lines` 다.
      - 함께 돌려줄 값:
        - 안쪽(패딩 안) 좌우 경계를 넘은 최대 px(`over`)
        - 1줄일 때 여유(`slack`). 오른쪽 `ml-auto` 그룹이 보이면 그 왼끝 − 앞 항목 오른끝 최댓값 − column-gap, 아니면 안쪽 오른끝 − 항목 오른끝 최댓값.
        - 상태줄 높이
    - `expectStripOk(page, label, maxLines)`
      - `lines ≤ maxLines`, `over ≤ 0.5`
      - `scrollOverflowing(page, '[data-slot="orderbook-status-bar"]')` → []
      - `leavesOverflowing(상태줄, 상태줄 box 오른끝)` → []
      - 고지 줄이 있으면 `scrollOverflowing(page, '[data-slot="orderbook-notices"]')` → []
      - 실패 메시지에 `label`(폭 · 시나리오)을 싣고, `test.info().annotations` 에 폭별 줄 수·여유를 남긴다(P20-3 관례).

    케이스 **`P20-6 ① 거부 · 예약구간`** — 제목에 「P20-6 ①」·「D-24」를 넣는다.

    준비:
    - `relay.gateway.respondLoginWithAccounts([{ accountNo: E2E_ACCOUNT_NO, name: '개인연금저축' }, { accountNo: '1234567802', name: '위탁종합' }])`. 목업 최악 계좌와 같은 길이이고, 첫 계좌가 자동 선택된다.
    - `relay.seedLimitChasers([{ buyEnabled: true }])` — LED·반영 시각이 서게 한다.
    - `goto(ORDERBOOK_URL)` → ready 까지 기다린다.
    - `sendQueuedWindowState(sock, { open: true, maxPieces: 10 })` → 상태줄 배지 「예약구간 · 조각 최대 10」을 기다린다.
    - `relay.pushServerMessage({ level: 'ERROR', source: 'LimitChaser', isin: E2E_ISIN, accountNo: E2E_ACCOUNT_NO, message: '주문 가능 금액이 부족합니다', kind: '' })` → 고지 줄 alert 에 그 문장이 보일 때까지 기다린다(15s).

    측정 — 이 태스크는 본문 700 · 830 · 992 + 뷰포트 1440×1000 그대로에서 잰다:
    - `expectStripOk(…, 1)`
    - 상태줄 `getByRole('alert')` 0 · 고지 줄 `getByRole('alert')` 1개가 보이고 그 문장을 담는다.
    - 보이는 LED 점 `[data-slot="latch-led"][data-variant="dot"]` 3개.
    - 상태줄 combobox 「계좌」 값 = E2E_ACCOUNT_NO · 선택 옵션 글자 「1234567801 · 개인연금저축」 · `title` 같은 값.

    정리:
    - `finally` 에서 `sendQueuedWindowState(sock, { open: false, maxPieces: 10 })`
    - describe `test.afterEach` 에서 `relay.gateway.respondLogin()` — 뒤 케이스 1 이 계좌 1건을 단언한다.

    Task 2 가 같은 케이스에 344 · 374 를 더하고 ② ③ 을 추가한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec vitest --run src/components/stock/__tests__/stock-orderbook-section.test.tsx</automated>
    <fails_when>non-zero exit, or the summary line contains "failed", or "Tests" count is lower than before this task (케이스 삭제)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS" (tsconfig.e2e.json 이 spec 도 타입체크한다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/orderbook.spec.ts</automated>
    <fails_when>non-zero exit, or the summary reports any "failed" or "did not run", or "P20-6 ①" is absent from the list output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'variant="dot"' webapp/src/components/stock/stock-orderbook-section.tsx` >= 1
    - `grep -c 'data-slot="orderbook-notices"' webapp/src/components/stock/stock-orderbook-section.tsx` == 1
    - `grep -c 'orderbook-server-error-src' webapp/src/components/stock/stock-orderbook-section.tsx` >= 1
    - `grep -c 'title="서버 반영 시각"' webapp/src/components/stock/stock-orderbook-section.tsx` >= 1
    - `grep -c "P20-6 ①" webapp/e2e/specs/orderbook.spec.ts` >= 1 · `grep -c "respondLogin()" webapp/e2e/specs/orderbook.spec.ts` >= 1
    - 단위: stock-orderbook-section 파일의 `it(` 수가 이관 전 이상(③ 삭제 없음 · ③-e 추가)
    - e2e: orderbook.spec 전체 green(1 의 계좌 1건 단언 포함 — 로그인 응답 복원 증명)
  </acceptance_criteria>
  <done>
    본문 ≥700 에서 거부 문장이 상태줄을 떠나 고지 줄 `role=alert` 로 선다. LED 가 점 3개다. 「계좌」「반영」이 sr-only/title 이다.

    실브라우저 최악 거부 시나리오가 700 · 830 · 992 · 1440 에서 1줄 · 잘림 0 이다. 컴포넌트 → 단위 → 실 relay e2e 한 경로가 커밋됐다.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 본문 <700 압축(연결 점+시각 · 계좌 이름 · 구간 배지/연결 이상 → 고지 줄) + 미반영·끊김 시나리오 + 344/374 + select 폭 판정 (D-24)</name>
  <precondition>포트 3100 이 비어 있거나 이 worktree 의 `webapp` dev 서버가 떠 있다(`lsof -iTCP:3100 -sTCP:LISTEN`)</precondition>
  <files>webapp/src/components/stock/stock-orderbook-section.tsx, webapp/src/components/stock/__tests__/stock-orderbook-section.test.tsx, webapp/src/components/stock/__tests__/orderbook.test.tsx, webapp/e2e/specs/orderbook.spec.ts, webapp/e2e/overflow.ts</files>
  <read_first>
    - webapp/src/components/stock/stock-orderbook-section.tsx (Task 1 결과)
    - .planning/sketches/002-toss-order-ticket/status-strip-variants.html (안 C `strip`/`notices` 조립 · `conn` · `acctShort` · `nDown` · `nWin` · `.acct-s` · `@container lc (max-width: 699.98px)` 블록)
    - webapp/src/components/stock/__tests__/orderbook.test.tsx (:260-280 `statusBar()` · :420-465 ⑭ · ⑭-b · ⑬)
    - webapp/e2e/overflow.ts (머리 주석 — 「저장소 유일 정의」 · 두 판정은 대체하지 말고 나란히)
    - webapp/src/lib/use-relay-socket.ts (:629-646 state 프레임 리듀서 · :1335-1350 onmessage)
    - relay/tests/helpers/frames.ts (:1118-1128 `FakeQueuedWindowInput` · :200-210 `FakeServerMessageInput`)
    - tasks/lessons.md 「그리드/플렉스 자식은 `min-w-0` 없으면 조용히 잘린다」 절(`display: contents` 래퍼 함정)
  </read_first>
  <behavior>
    - ③-f 연결 조각이 둘이다.
      - `[data-slot="orderbook-conn"]`: class 에 `hidden` 과 `@min-[700px]/lc:inline-flex`.
      - `[data-slot="orderbook-conn-compact"]`: class 에 `@min-[700px]/lc:hidden`. `title` = 「DMA 실시간 · 반영 —」. `.sr-only` 글자 = 「DMA 실시간 · 반영」. 보이는 시각 글자 = 「—」.
    - ③-g 계좌 select 는 여전히 **하나**다(`getAllByRole('combobox', { name: '계좌' })` 길이 1 · 값 ACCOUNT).
      - `[data-slot="orderbook-account-name"]` 는 `aria-hidden="true"` 이고 글자는 「위탁종합」이다. class 에 `@min-[700px]/lc:hidden`.
      - 계좌 이름이 목록에서 중복이면 이름표가 「{번호} · {이름}」 전체다. 이름이 비면 번호다.
      - 계좌 0개면 「계좌 확인 중…」이다.
    - ③-h 구간 배지 두 자리.
      - 상태줄 `[data-slot="orderbook-window-badge"]`: 「정규」 · class 에 `hidden` · `@min-[700px]/lc:inline-flex`.
      - 고지 줄 `[data-slot="orderbook-window-notice"]`: 같은 배지 「정규」 · class 에 `@min-[700px]/lc:hidden`.
      - 폰 전용 항목만 있을 때 `orderbook-notices` 컨테이너 class 에 `@min-[700px]/lc:hidden` 이 있다. 거부가 함께 있으면 없다.
    - ③-i 상태줄 오른쪽 그룹(`[data-slot="orderbook-strip-right"]` — 다시 연결 + 반영 시각) class 에 `hidden` · `@min-[700px]/lc:inline-flex` 가 있다. 상태줄 class 에 `gap-x-2` · `@min-[700px]/lc:gap-x-3` · `flex-wrap` 이 있다.
    - ⑬(이관 · orderbook.test) status 'failed' · 라벨 「회선 단절」일 때:
      - 상태줄 `orderbook-conn` 에 「회선 단절」이 있고, 상태줄 「다시 연결」 클릭 → reconnect 1회.
      - `[data-slot="orderbook-conn-notice"]`(class `@min-[700px]/lc:hidden`)에 「DMA 회선 단절」이 있고, 그 안 「다시 연결」 클릭 → reconnect 누계 2회.
      - status 'ready' 로 돌리면 `orderbook-conn-notice` 가 없다.
    - ⑭ · ⑭-b(orderbook.test) `within(statusBar()).getByRole('combobox', { name: '계좌' })` 가 그대로 하나를 찾고 계좌 전환이 동작한다(select 하나 유지의 회귀 가드).
  </behavior>
  <action>
    **TDD(RED 먼저)**
    - `<behavior>` 단언을 두 단위 파일에 먼저 쓰고 실패를 확인한다.
    - orderbook.test ⑬ 은 지우지 않고 두 자리로 이관한다.
    - 그다음 구현한다(per D-24).

    **상태줄 — 폰 모양(<700) 을 기본, ≥700 을 `@min-[700px]/lc:` 로**
    `display: contents` 래퍼를 쓰지 않고, 폭별 조각마다 자기 클래스를 직접 단다(lessons).

    (a) 상태줄 간격: `gap-x-3` → `gap-x-2 @min-[700px]/lc:gap-x-3`. 목업 C 폰 8 · ≥700 12.

    (b) 연결:
    - Task 1 의 `orderbook-conn` 에 `hidden @min-[700px]/lc:inline-flex` 를 준다.
    - 그 앞에 `orderbook-conn-compact` 를 새로 둔다.
      - 클래스: `inline-flex @min-[700px]/lc:hidden items-center gap-[5px] whitespace-nowrap`.
      - title 「DMA {라벨} · 반영 {appliedAt ?? '—'}」.
      - 내용: 같은 점(색·펄스 규칙을 한 곳에서 만드는 작은 조각으로 두 자리 재사용) + sr-only 「DMA {라벨} · 반영 」 + `mono text-[var(--fg-2)]` 시각.

    (c) 계좌 — **select 요소는 하나**다. 계좌 전환 한 경로를 지키고, 단위/e2e 의 `getByRole('combobox', { name: '계좌' })` 가 하나를 찾게 한다.
    - 래퍼:
      - 폰: 목업 `.acct-s` 칩이다. `relative inline-flex h-6 shrink-0 items-center gap-[3px] rounded-[var(--r-sm)] bg-[var(--muted)] pl-[7px] pr-[5px] text-[11px] font-semibold text-[var(--fg)] whitespace-nowrap`.
      - ≥700: 칩 모양을 벗는다(면·패딩·높이 해제 · `min-w-0` · 수축 허용).
    - 폰 전용 이름표 `data-slot="orderbook-account-name"`: `aria-hidden="true"` · `@min-[700px]/lc:hidden` + lucide `ChevronDown` 10px(`aria-hidden`).
    - 이름표 글자 규칙:
      - 선택 계좌의 `name`.
      - 이름이 비었거나 목록에 같은 이름이 둘 이상이면 옵션과 같은 「{번호} · {이름}」(또는 번호)을 쓴다. 이름만으로 계좌를 특정할 수 없을 때 번호를 숨기지 않는 오발주 가드다(파일 머리 ⑤ · CR-01 · D-24 「이름만」은 이름이 계좌를 유일하게 가리킬 때의 표기).
      - 계좌 0개면 「계좌 확인 중…」.
    - select:
      - 폰: 래퍼를 덮는 투명 오버레이다. `absolute inset-0` · 전체 크기 · `opacity-0` · `cursor-pointer` · 글자 16px(iOS 확대 방지 · 목업).
      - ≥700: `@min-[700px]/lc:` 로 흐름 안 · 불투명 · 기존 모양(`mono h-6 max-w-full min-w-0 rounded-[var(--r-sm)] bg-[var(--muted)] px-1.5 text-[11px] font-semibold`)으로 돌아온다.
      - `aria-label="계좌"` · `title` · `value` · `onChange` · `disabled` · 옵션은 Task 1 그대로다. `appearance-none` 금지(네이티브 캐럿·OS 목록).

    (d) 거래소 세그먼트·LED 점: 폭 무관 그대로.

    (e) 구간 배지:
    - 배지 마크업을 작은 조각 `WindowBadge`(`data-slot="orderbook-window-badge"` · `data-tone` · 기존 톤 클래스)로 뺀다.
    - 상태줄에서는 `hidden @min-[700px]/lc:inline-flex` 로 쓴다.

    (f) 오른쪽 그룹: `data-slot="orderbook-strip-right"` · `ml-auto hidden @min-[700px]/lc:inline-flex items-center gap-3`. 폰에서는 시각이 연결 조각에 있고 「다시 연결」은 고지 줄에 있다.

    **`TabNotices` — 폰 전용 항목**
    - 새 props: `status` · `label`(연결 문구 — 섹션이 한 번 계산해 상태줄과 함께 쓴다) · `windowBadge` · `onReconnect`.
    - 순서(목업 C: down → err → win → unacked):
      1. 연결 이상 줄 `data-slot="orderbook-conn-notice"` · `role="status"` · `flex @min-[700px]/lc:hidden flex-wrap items-center gap-x-2.5 gap-y-1 text-[var(--muted-fg)]`. `status !== 'ready'` 일 때만 선다. 내용은 「DMA <b className="font-semibold text-[var(--fg)]">{라벨}</b>」 + `onReconnect` 가 있으면 상태줄과 같은 `Button variant="outline" size="sm" className="h-6 px-2"` 「다시 연결」(같은 `onReconnect`).
      2. 거부(Task 1).
      3. 구간 `<p data-slot="orderbook-window-notice" role="status" className="m-0 flex @min-[700px]/lc:hidden">` 안 `WindowBadge` — 모름(null)이면 없음.
      4. 전환 중 → 빈 NXT → 배너 → 미반영(기존).
    - 컨테이너 규칙:
      - ≥700 에서도 보이는 항목(거부 · 전환 중 · 빈 NXT · 배너 · 미반영)이 하나도 없고 폰 전용 항목만 있으면 컨테이너에 `@min-[700px]/lc:hidden` 을 붙인다. 넓은 화면에 빈 고지 줄 간격(섹션 gap 12)이 생기지 않는다.
      - 아무것도 없으면 null.

    섹션이 `TabNotices` 에 새 props 를 넘긴다(`onReconnect={isBroken ? reconnect : undefined}` 같은 값). 주문·계좌·거래소·재연결 핸들러 자체는 건드리지 않는다.

    **`overflow.ts` — 세 번째 판정 `selectsClipped(page, rootSelector)` 를 export 한다**
    - 목업 `naturalSelectWidth` 이식이다.
    - 루트 안 보이는 select(폭 > 0 · 계산 opacity ≠ 0 — 폰의 투명 오버레이는 글자를 그리지 않으므로 제외하고, 보이는 이름표는 기존 두 판정이 본다)를 같은 부모 안 화면 밖 호스트(`position:absolute; left:-9999px; visibility:hidden; width:max-content`)에 `max-width:none; min-width:auto` 로 복제해 잰다.
    - 본래 폭 − 실제 폭 > 0.5 인 것을 `{ text, deficit }` 로 돌려준다.
    - 머리 주석의 「두 판정」 설명을 「세 판정」으로 고치고 이 판정이 보는 실패(select 는 좁아져도 scrollWidth 가 조용하다)를 적는다. 기존 두 함수 본문은 한 글자도 바꾸지 않는다.

    **e2e — `orderbook.spec.ts` P20-6 describe 확장**
    - `expectStripOk` 에 `selectsClipped(page, '[data-slot="orderbook-status-bar"]')` → [] 를 더한다.
    - 공통 폭 목록: [344, 374, 700, 830, 992] + 뷰포트 1440.

    **① 확장**: 344 · 374 를 더한다. 폰 폭에서 확인할 것:
    - `orderbook-conn-compact` 가 보이고 `orderbook-conn` 은 숨는다.
    - 계좌 이름표 「개인연금저축」이 보이고, select 값·title 은 여전히 「1234567801 · 개인연금저축」 계열이다.
    - 상태줄 구간 배지는 숨고 고지 줄 `orderbook-window-notice` 배지가 보인다.
    - 거부 alert 는 고지 줄에 있다.
    - `@axe-core/playwright` `AxeBuilder` 를 `.include('[data-slot="orderbook-status-bar"]')` · `.include('[data-slot="orderbook-notices"]')` · `withTags(['wcag2a','wcag2aa'])` 로 344 · 992 에서 돌린다. critical/serious 0 이어야 한다(a11y.spec 의 blocking 필터와 같은 기준).

    **새 케이스 `P20-6 ② 미반영 · 장전`**
    - 준비: 같은 계좌·시드 → ready → `sendQueuedWindowState(sock, { preopenOpen: true })` → 배지 「장전 · 예약매수/매도」.
    - 미반영 만들기:
      - 기본 뷰포트에서 `getByRole('switch', { name: '매수주문 켜기', exact: true })` 를 누른다. 켜진 전략을 끄는 방향이라 무장 가드와 무관하게 전송된다.
      - `relay.requestLog()` 에 `DMA_MSG.SetLimitChaserReq` 가 1 늘 때까지 폴링한다.
      - 에코를 보내지 않는다. `[data-slot="orderbook-unacked"]` 가 보일 때까지 기다린다(10s).
      - 스위치 전송이 카드 `unacked` 를 세우지 않으면(카드 `handleSent` 를 타지 않는 경로라면) 잔량 행 인라인 편집(행 클릭 → `#lc-buy-watch-qty` → 값 → Enter)으로 보낸다. 어느 경로로 보냈는지 SUMMARY 에 적는다.
    - 측정: 전 폭 `expectStripOk(…, 1)`.
    - 정리: 창을 닫힌 상태로 되돌린다.

    **새 케이스 `P20-6 ③ 끊김 · 시간외종가`**
    - `goto` 전에 `page.routeWebSocket(/:8090\/ws/, ws => { ws.connectToServer(); 보관 })` 로 통과 프록시를 건다.
    - 준비: 같은 계좌·시드 → ready → `sendQueuedWindowState(sock, { g2Open: true })` → 배지 「시간외종가 G2 창」.
    - 보관한 route 로 `ws.send(JSON.stringify({ t: 'state', s: 'manual_required' }))` 를 주입한다. 브라우저 자체 재시도 예산(10회 백오프) 소진을 기다리지 않고 같은 화면 상태를 만드는 유일한 결정적 경로이고, 리듀서가 계좌 목록을 유지한다.
    - 상태줄 `data-status="manual_required"` 를 확인하고, 폭을 바꿀 때마다 측정 직전에 다시 확인한다.
    - relay 가 뒤이어 `state` 프레임(ready)을 다시 보내 상태가 되돌아가면: 보관한 route 에서 서버 쪽 `onMessage` 를 달아 `t:'state'` 프레임만 버리고 나머지는 `ws.send` 로 넘긴다. 페이지 쪽 `onMessage` 도 달아 서버로 넘긴다(한쪽에 핸들러를 달면 그 방향 자동 전달이 꺼진다).
    - 폭별 측정:
      - 344 · 374 · 830 · 992 · 1440 → `expectStripOk(…, 1)`, 700 → `expectStripOk(…, 2)`.
      - 폰 폭: 고지 줄 `orderbook-conn-notice` 에 「다시 연결하지 못했어요」와 보이는 「다시 연결」 버튼이 있고, 상태줄의 「다시 연결」은 숨는다.
      - ≥700: 반대다.
    - 정리: 창을 닫힌 상태로 되돌린다.
    - 이 케이스는 「다시 연결」을 누르지 않는다(재연결 동작은 단위 ⑬ 이 본다).

    **1줄이 안 나올 때**
    - 344 가 2줄이면 폰 기하가 목업 C 값(간격 8 · 연결 gap 5 · 계좌 칩 7/5/3 · 점 gap 2 · 배지/연결 문구가 상태줄에서 빠졌는지)과 같은지 먼저 맞춘다.
    - 글자 크기를 줄이거나 자르는 것으로 맞추지 않는다. 실측 여유는 SUMMARY 에 표로 남긴다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec vitest --run src/components/stock/__tests__/stock-orderbook-section.test.tsx src/components/stock/__tests__/orderbook.test.tsx</automated>
    <fails_when>non-zero exit, or the summary line contains "failed", or "Test Files" does not report 2 passed</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/orderbook.spec.ts</automated>
    <fails_when>non-zero exit, or the summary reports any "failed" or "did not run", or any of "P20-6 ①" / "P20-6 ②" / "P20-6 ③" is absent from the list output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "@min-\[700px\]/lc:hidden" webapp/src/components/stock/stock-orderbook-section.tsx` >= 4 (연결 compact · 계좌 이름표 · 연결 이상 줄 · 구간 고지)
    - 계좌 select 하나: 단위 ③-g 의 `getAllByRole('combobox', { name: '계좌' })` 길이 1 단언이 green(주석에 걸리는 grep 대신 렌더 결과로 잰다)
    - `grep -cE "truncate|text-ellipsis|line-clamp" webapp/src/components/stock/stock-orderbook-section.tsx` == 0
    - `grep -c "flex-wrap" webapp/src/components/stock/stock-orderbook-section.tsx` >= 1
    - `grep -c "export async function selectsClipped" webapp/e2e/overflow.ts` == 1
    - `grep -c "routeWebSocket" webapp/e2e/specs/orderbook.spec.ts` >= 1 · `grep -cE "P20-6 (①|②|③)" webapp/e2e/specs/orderbook.spec.ts` >= 3
    - 단위 ⑬ · ⑭ · ⑭-b · ⑨(뷰포트 브레이크포인트 0) 가 green 이고 두 파일의 `it(` 수가 이관 전 이상
    - `git diff --name-only cb63322..HEAD -- webapp/src webapp/e2e` (cb63322 = 이 플랜을 쓰기 직전 HEAD · D-24 기록 커밋 — 그 사이 플랜 커밋은 .planning 만 바꾼다) 가 files_modified 의 webapp 파일 5개 안에 든다(latch-led · strategy-card · workbench-status-bar · 주문 폼 무변경)
  </acceptance_criteria>
  <done>
    폰 밴드에서 상태줄이 연결 점+시각 · 계좌 이름 · 거래소 · LED 점으로 줄었다. 구간 배지와 연결 이상 문구(+다시 연결)는 고지 줄로 내려갔다.

    세 최악 시나리오가 실브라우저에서 D-24 실측 계약대로다: 폰 전 시나리오 1줄 · 700 끊김만 ≤2줄 · 전 폭 잘림 0. 재연결·계좌 전환은 한 경로 그대로다.
  </done>
</task>

<task type="auto">
  <name>Task 3: UI-SPEC 계약 갱신(A7 → D-24 · §10 상단 상태줄) + VALIDATION 20-08 행 + 게이트 · 시각 확인</name>
  <precondition>포트 3100 이 비어 있거나 이 worktree 의 `webapp` dev 서버가 떠 있다(`lsof -iTCP:3100 -sTCP:LISTEN`)</precondition>
  <files>.planning/phases/20-toss-order-ticket/20-UI-SPEC.md, .planning/phases/20-toss-order-ticket/20-VALIDATION.md</files>
  <read_first>
    - .planning/phases/20-toss-order-ticket/20-UI-SPEC.md (범위 요약 표 :19-30 · 머리 정본 순서 :13 · 컴포넌트 해부 :182-345 · 접근성 계약 :416-431 · 가정 표 :490-503 · 검증 훅 :507-517 · 출처 :530-535)
    - .planning/phases/20-toss-order-ticket/20-VALIDATION.md (Per-Task Verification Map · Manual-Only)
    - webapp/src/components/stock/stock-orderbook-section.tsx (Task 1·2 결과 — 실제 클래스·슬롯 이름을 문서에 옮긴다)
    - Task 2 SUMMARY 초안의 실측 여유 표(없으면 e2e annotation 출력)
  </read_first>
  <action>
    **① `20-UI-SPEC.md`(계약 — UI 게이트가 읽는다)**

    (a) 범위 요약 표
    - 오른쪽 열(Non-goals)에서 상단 상태줄 1줄 압축을 제외하던 A7 행을 지운다.
    - 왼쪽 열(바꾸는 것)에 행을 더한다: 「호가 탭 상단 상태줄(`OrderbookStatusBar`) 1줄 압축 — D-24 안 C(아래 §10). 표현만 · 잘림 0 · 폭 분기 700 하나」.

    (b) 머리 정본 순서
    - 「D-01~D-20」 표기에 D-24 가 포함되게 고친다.
    - 정본 목업에 `status-strip-variants.html`(안 C)을 더한다.

    (c) 컴포넌트 해부 끝(§9 뒤)에 새 절 「### 10. 상단 상태줄 (`OrderbookStatusBar`) + 고지 줄 — D-24 안 C」. 표 3개를 둔다.
    - **폭별 항목 표**:
      - ≥700: 연결 「● DMA {상태}」 · 계좌 select 「{번호} · {이름}」 · 거래소 · LED 점 3 · 구간 배지 · (다시 연결) · 시각.
      - <700: 연결 점+시각 · 계좌 이름 칩 · 거래소 · LED 점 3.
    - **수치 표**(목업 CSS 그대로 · 이미 코드에 있는 값): 상태줄 패딩 10/12 · 간격 x 12(≥700)/8(<700) · y 4 · 연결 점 7 · 연결 gap 6/5 · 계좌 select h24 11/600 · 계좌 칩 h24 좌7 우5 gap3 11/600 쉐브런 10 · 오버레이 select 투명 16px · 거래소 h20 10/700 · LED 히트 24 점 10 gap 2 · 배지 h18 10/700 좌우 7 · 다시 연결 h24 좌우 8 · 고지 줄 세로 gap 4 · 연결 이상 줄 gap 4/10.
    - **고지 줄 순서 표**: 연결 이상(<700 · role=status) → 거부(role=alert) → 구간 배지(<700 · role=status) → 전환 중 → 빈 NXT → 배너 → 미반영.

    같은 절에 둘 문장:
    - 규칙: `flex-wrap` 유지 · 말줄임/줄 제한 금지 · 폭 분기 `@min-[700px]/lc:` 하나 · 글자 크기 불변 · 계좌 이름이 비거나 중복이면 이름표에 번호를 함께 보인다(오발주 가드).
    - 실측 계약: `<context>` 의 안 C 표 + 「실브라우저 P20-6 ①②③ 가 잰다」.

    (d) 접근성 계약 표에 행을 더한다.
    - 「상단 상태줄」: LED 점 sr-only 「{이름} 래치 {상태}」 + 늘 뜨는 툴팁 · 계좌 select 이름 「계좌」 + 값·옵션·title 「{번호} · {이름}」 전체(폰 이름표는 aria-hidden) · sr-only 「반영 」 / 「DMA {상태} · 반영 」 · 거부는 고지 줄 role=alert(한 번만).
    - 「고지 줄」: 연결 이상·구간은 role=status(폰에서만 트리에 있음) · 「다시 연결」은 폭별로 한 자리만 보인다.

    (e) Copywriting Contract 에 sr-only/title 문구를 더한다: 「반영 」 · 「서버 반영 시각」 · 「DMA {상태} · 반영 {시각}」 · 계좌 title 「{번호} · {이름}」. 고지 줄의 기존 문구는 원문 그대로라고 적는다.

    (f) 가정 표 A7 행
    - 판단 칸을 취소선(`~~…~~`)으로 두고 「→ **D-24(2026-09-25, 검증 갭 → 사용자 결정)로 대체**: 이번 phase 에서 안 C 로 구현(§10) · 잘림 위험은 flex-wrap 2줄 안전망 + 실측으로 닫음」을 적는다.
    - 확인 칸은 「확정」.

    (g) 검증 훅에 불릿을 더한다: 「상단 상태줄: 거부·미반영 시나리오 본문 344/374/700/830/992 1줄 · 끊김 700 ≤2줄 · 나머지 1줄 · 잘림 0(스크롤·잎·select 본래 폭) — e2e P20-6」.

    (h) 출처에 `status-strip-variants.html`(A~D 비교 · 안 C 채택)을 더한다.

    frontmatter `status: approved` 와 Checker Sign-Off 는 건드리지 않는다.

    **② `20-VALIDATION.md`**
    - Per-Task Verification Map 에 20-08-T1 · T2 · T3 세 행을 기존 형식으로 더한다.
      - Requirement: D-24 · 불변식.
      - Threat Ref: T-20-18~21.
      - 명령: 이 플랜 `<verify>` 의 명령.
    - 표 머리 설명 문장에 20-08 을 적는다.
    - Status 는 아래 게이트 실행 결과(✅/❌)로 채운다.
    - Manual-Only 표에 「상단 상태줄 목업 안 C 대조(다크/라이트 · 폰 360/390 · 태블릿 768 · 데스크톱 1280)」 행을 더한다.
    - `nyquist_compliant` · `status` · Approval 은 validate-phase 몫이라 건드리지 않는다.

    **③ 게이트** — worktree 루트에서 순서대로 돌리고 수치를 SUMMARY 에 적는다.
    1. config `build_command` 전문
    2. config `test_command` 전문
    3. e2e — orderbook · trading-workbench · a11y · sidebar-tree · stock-detail-tabs 5 spec 을 이름으로 지정한다. `zz-theme-gallery.spec.ts` 는 넣지 않는다.

    실패가 있으면 이 플랜 파일 범위 안에서 고치고 SUMMARY 에 적는다.

    **④ 시각 확인**
    - worktree dev 서버로 `/stocks/005930?tab=orderbook` 를 폰 360 · 390 · 태블릿 768 · 데스크톱 1280 × 다크/라이트로 찍어 목업 안 C 와 직접 대조한다.
    - 스크린샷은 스크래치패드에 두고 커밋하지 않는다. 임시 spec 을 쓰면 `zz-*.tmp.spec.ts` 로 만들고 실행 직후 지운다.
    - 이 표면 안의 줄바꿈·잘림·겹침 결함은 묻지 말고 고친 뒤 한 줄로 보고한다.
    - 끝으로 `git status --porcelain -- webapp/e2e/specs/zz-theme-gallery.spec.ts .planning/milestone.lock` 가 둘 다 `??` 그대로인지 확인한다.
    - push · 배포는 하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/relay run test && pnpm --filter @gh-radar/webapp run test</automated>
    <fails_when>non-zero exit, or any vitest summary line contains "failed", or the webapp "Tests" passed count is below 2075 (20-VERIFICATION 기준선)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/orderbook.spec.ts e2e/specs/trading-workbench.spec.ts e2e/specs/a11y.spec.ts e2e/specs/sidebar-tree.spec.ts e2e/specs/stock-detail-tabs.spec.ts</automated>
    <fails_when>non-zero exit, or the summary reports any "failed" or "did not run", or the passed count is below 71 + 3 (20-07 기준선 + P20-6 ①②③) + stock-detail-tabs 케이스 수</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && grep -c "D-24" .planning/phases/20-toss-order-ticket/20-UI-SPEC.md && grep -c "### 10. 상단 상태줄" .planning/phases/20-toss-order-ticket/20-UI-SPEC.md && grep -c "20-08-T" .planning/phases/20-toss-order-ticket/20-VALIDATION.md</automated>
    <fails_when>non-zero exit (grep 이 0 을 세면 종료 코드 1), or the first count is below 4, or the second count is not 1, or the third count is below 3</fails_when>
    <human-check>
      <test>worktree dev 서버(PORT=3100)에서 `/stocks/005930?tab=orderbook` 을 연다. 목업 `.planning/sketches/002-toss-order-ticket/status-strip-variants.html` 의 안 C 와 나란히 비교한다(다크/라이트 · 폰 360 · 390 · 태블릿 768 · 데스크톱 1280). 거부·미반영·끊김이 생기면 고지 줄 위치도 본다.</test>
      <expected>폰에서 상태줄이 「● 시각 · 계좌 이름 ⌄ · KRX|NXT · 점 3개」 한 줄이다. 구간 배지·연결 이상은 바로 아래 고지 줄에 있다. ≥700 에서는 「● DMA 실시간 · 번호·이름 select · KRX|NXT · 점 3개 · 배지 ··· 시각」 한 줄이다. 어디에도 잘림·말줄임·겹침이 없다.</expected>
      <why_human>톤(색·간격·정렬) 대조는 스크린샷 자동 비교로 판정할 수 없다(20-VALIDATION Manual-Only)</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\| A7 \|.*D-24" .planning/phases/20-toss-order-ticket/20-UI-SPEC.md` == 1
    - `grep -c "상단 상태줄 1줄 압축 — 이번 phase" .planning/phases/20-toss-order-ticket/20-UI-SPEC.md` == 0 (범위 요약의 제외 행 제거)
    - `grep -c "status-strip-variants.html" .planning/phases/20-toss-order-ticket/20-UI-SPEC.md` >= 1
    - `grep -c "20-08-T" .planning/phases/20-toss-order-ticket/20-VALIDATION.md` >= 3
    - 게이트 3종 종료 0 · e2e failed 0 · SUMMARY 에 수치와 실측 여유 표 · 시각 확인 결과가 있다
    - `git status --porcelain -- webapp/e2e/specs/zz-theme-gallery.spec.ts .planning/milestone.lock` 출력이 두 줄 모두 `??`
  </acceptance_criteria>
  <done>
    UI-SPEC 이 D-24 안 C 를 계약으로 담았고(A7 대체 · §10), VALIDATION 에 20-08 행이 실행 결과와 함께 있다.

    build · test · e2e 5 spec 이 green 이다. 목업 안 C 와 톤 대조는 사용자 UAT 로 남았다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 화면 → 사용자 판단(주문 계좌·상태) | 상태줄 표기가 계좌·연결·거부를 잘못 말하면 사용자가 엉뚱한 계좌로 주문하거나 거부를 놓친다(오발주) |
| e2e 러너 → 로컬 dev(3100) · 로컬 relay · 스텁 게이트웨이 | 실서버·실계좌에 붙지 않는다. routeWebSocket 주입은 테스트 페이지 한정 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-18 | Tampering(오발주) | 폰 계좌 이름표(닫힌 표기 = 이름만) | high | mitigate | select 요소 하나 유지(값·옵션·title 전체 번호) · 이름 비거나 중복이면 이름표에 번호 포함 · 단위 ③-g(중복·빈 이름) · e2e ① 폰 폭 select 값/title 단언 |
| T-20-19 | Tampering(오발주) | 라벨·계좌 글자 잘림 | high | mitigate | flex-wrap 유지 · 말줄임 유틸 0(grep) · e2e P20-6 ①②③ × 6폭 스크롤·잎·select 본래 폭·고지 줄 판정 |
| T-20-20 | Repudiation(경보 소실) | 거부 문장 이동 | medium | mitigate | 고지 줄 role=alert 한 번 · 상태줄 alert 0 · 문장 1회(단위 ③-e · e2e ①) |
| T-20-21 | Denial of Service(복구 수단 소실) | 폰 「다시 연결」 위치 이동 | medium | mitigate | 두 자리 모두 같은 reconnect(단위 ⑬ 2회) · e2e ③ 폰 폭 고지 줄 버튼 보임 |
| T-20-16 | Spoofing(검증 대상 착오) | 3100 에 다른 체크아웃 서버 | medium | mitigate | 모든 e2e 태스크 precondition 리스너 확인 |
| T-20-17 | Repudiation(커버리지 소실) | 옛 단언 삭제 | medium | mitigate | ③ · ⑬ 이관(삭제 금지) · `it(` 수 불감소 acceptance |
| T-20-SC | Tampering | 패키지 설치 | low | accept | 설치 없음(`@axe-core/playwright` · Playwright routeWebSocket 모두 기존 의존성) |
</threat_model>

<verification>
- 단위: stock-orderbook-section · orderbook 두 파일 green(③ · ③-e~i · ⑬ · ⑭ · ⑨).
- 실브라우저: orderbook.spec P20-6 ①②③ — D-24 실측 계약(폰 1줄 · 700 끊김만 ≤2줄 · 잘림 0) + axe 0.
- 게이트: build_command · test_command · e2e 5 spec green · 바뀐 webapp 파일 = files_modified 5개.
- 계약: UI-SPEC A7 → D-24 · §10 · VALIDATION 20-08 행.
- 시각 톤 대조는 `<human-check>` 로 end-of-phase UAT.
</verification>

<success_criteria>
- 20-VERIFICATION 갭(truth 13 「상단 상태줄 1줄 압축」)이 D-24 안 C 구현과 실측 증거로 닫혀 재검증에서 13/13 이 될 수 있다.
- 주문·재연결·계좌·거래소 동작 무변경 · 라벨 잘림 0 · 컨테이너 쿼리만 — Phase 20 불변식이 그대로다.
- push · 배포 없음(theme/toss-b 미병합).
</success_criteria>

## Coverage audit (gap closure)

| 출처 | 항목 | 담당 |
|---|---|---|
| GOAL | ROADMAP Phase 20 「상단 상태줄 1줄 압축」 | Task 1 · 2 (구현) · Task 3 (계약) |
| CONTEXT | D-24 ≥700 거부→고지 줄 · LED 점 · 「계좌」「반영」 sr-only/title | Task 1 |
| CONTEXT | D-24 <700 연결 점+시각 · 계좌 이름만 · 구간 배지/연결 이상 → 고지 줄 | Task 2 |
| CONTEXT | D-24 flex-wrap · 잘림 0 · `@min-[700px]/lc:` 만 · 실측(폰 1줄 · 700~829 끊김만 2줄) | Task 2 (e2e) · Task 3 (계약) |
| VERIFICATION | gap truth 13 (strategy-card 지목은 D-24 가 대상 확정으로 대체) | 전 태스크 |
| REQ | 없음(Requirements: TBD) | — |
| RESEARCH | 상태줄 관련 항목 없음(A7 로 제외돼 있었음) | — |

<output>
Create `.planning/phases/20-toss-order-ticket/20-08-SUMMARY.md` when done (gate numbers · P20-6 폭별 줄 수/여유 표 · 시각 확인 결과 · human UAT 요청)
</output>
