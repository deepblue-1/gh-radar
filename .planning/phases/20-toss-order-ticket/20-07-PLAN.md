---
phase: 20-toss-order-ticket
plan: 07
type: execute
wave: 5
depends_on: ["20-04", "20-05", "20-06"]
files_modified:
  - webapp/e2e/specs/trading-workbench.spec.ts
  - webapp/e2e/specs/orderbook.spec.ts
  - webapp/e2e/specs/a11y.spec.ts
  - webapp/e2e/specs/sidebar-tree.spec.ts
  - .planning/phases/20-toss-order-ticket/20-VALIDATION.md
autonomous: true
requirements: []

estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "e2e 가 새 셀렉터로 이관됐다 — 값 단언은 `[data-lc-field=\"{옛 id}\"] [data-slot=\"lc-row-value\"]` 텍스트(「10,000주」「127,400원」) · 편집은 행 클릭 → 옛 id 입력 → Enter · 게이트는 `role=switch` · 더티 바 셀렉터는 부재 단언에만 쓰인다"
    - "옛 더티 바 케이스 7 · 12 · GC2 · 13 · 16 은 지우지 않고 즉시 반영 등가물로 재정의됐다(케이스 수 불감소) — 적용 → 게이트웨이 10 수신 → 60 에코 → 행 값 / 편집 중 다른 단말 에코 → 입력 유지 · 편집 뒤 행 = 에코 값 / 접었다 펴도 반영값 유지 / 이동 확인 0 (D-04 · D-07)"
    - "P20-3: 본문 344 · 700 · 830 · 992 × 최악값(1,274,000원 · 100,000주 · 9,999만원 · 잔량추적 기준선 100,000주)에서 우측 패널 잘림 0 · 모든 리스트 행 높이 정확히 44px · 인라인 편집 중 행도 44px (D-20 · D-14a · UI-SPEC 검증 훅)"
    - "≥700 2열에서 모든 리스트 행이 44px 이라 상태 문구가 한 줄이고 기준선 행이 없을 때 매수주문·매도주문 그룹 높이가 같다 — 두 그룹 `getBoundingClientRect().height` 동일 (UI Considerations 추가 layout 행)"
    - "터치 기기(hasTouch)에서 매수가격 시트는 칩 「현재가」「상한가」「±1호가」가 동작하고, 98,150 이면 「100원 단위로 입력해 주세요 · 가까운 값 98,100 / 98,200」 · 127,450 이면 「상한가 127,400원을 넘을 수 없어요」로 「매수가격 적용」이 잠기며, 매수주문 그룹이 「감시 중」이면 「감시 중 — 적용하면 바로 반영돼요」가 보인다 (D-12 · D-15 · D-17 · D-05)"
    - "터치 기기에서 수동주문 가격·수량 상자를 탭해 시트로 채워도 게이트웨이에 주문(DirectOrderReq)이 나가지 않고, 매수 버튼 → 확인 다이얼로그로만 주문이 시작된다 (D-10)"
    - "호가 탭(종목상세) 시트는 AI FAB 위(z)에 뜨고 확정 버튼 중심점의 최상위 요소가 확정 버튼이다 — 뷰포트 390 폭 370 · 1440 폭 440 가운데 (orderbook 11 재정의 · D-13)"
    - "a11y: 작업대 스위치 4개가 `role=switch` 이름으로 잡히고 · 인라인 편집 중 입력은 이름(`aria-label`)을 갖고 포커스를 유지하며 · 시트 열린 상태는 `role=dialog` · `aria-modal` 이고 닫으면 연 행으로 포커스가 돌아온다 — 각 상태 axe critical/serious 0"
    - "최종 게이트: config `build_command` · `test_command` · shared 전체 테스트 · webapp build · e2e 4 spec(trading-workbench · orderbook · a11y · sidebar-tree) 전부 green · 주문 경로 케이스(orderbook 6 · 7 · GC5 · GC6)는 셀렉터 외 무변경 green · `zz-theme-gallery.spec.ts` 는 추적 안 됨 그대로"
    - statement: "본문 344 · 700 · 830 · 992 × 최악값에서 우측 패널 scrollWidth ≤ clientWidth 이고 라벨·값 잘림이 0 이다 — 넘치면 20-04 가 ① 행 좌우 패딩 4→0 ② 라벨–값 간격 6→4 ③ 폰 밴드 쉐브런 숨김 순서로 적용 · 글자 크기 불변 (UI Considerations E1 overflow · P20-3 + 시각 확인)"
      verification: backstop
  artifacts:
    - path: webapp/e2e/specs/trading-workbench.spec.ts
      provides: "이관된 작업대 e2e + P20-1~P20-5 + 터치 describe"
      contains: "P20-3"
    - path: webapp/e2e/specs/orderbook.spec.ts
      provides: "호가 탭 시트 vs FAB(11 재정의)"
    - path: webapp/e2e/specs/a11y.spec.ts
      provides: "스위치 role · 인라인 편집 · 시트 열린 상태 axe"
    - path: .planning/phases/20-toss-order-ticket/20-VALIDATION.md
      provides: "Per-Task 상태 · wave_0_complete 갱신"
  key_links:
    - from: webapp/e2e/specs/trading-workbench.spec.ts
      to: webapp/src/components/trading/lc/lc-fields.ts
      via: "data-lc-field = 옛 입력 id (lc-buy-watch-qty 등)"
      pattern: "data-lc-field"
    - from: webapp/e2e/specs/orderbook.spec.ts
      to: webapp/src/components/trading/lc/number-pad-sheet.tsx
      via: "hasTouch → [data-slot=numpad-sheet] 기하 · elementFromPoint"
      pattern: "numpad-sheet"
  prohibitions:
    - statement: "옛 더티 바 e2e 케이스를 지워서 초록을 만들지 않는다 — 즉시 반영 등가물로 재정의하고 케이스 수를 줄이지 않는다"
      status: resolved
      verification: test
    - statement: "e2e 를 돌리기 전에 포트 3100 이 이 worktree 의 dev 서버인지 확인한다 — 메인 체크아웃 서버를 테스트해 거짓 초록을 내지 않는다 (RESEARCH Pitfall 8)"
      status: resolved
      verification: judgment
    - statement: "webapp/e2e/specs/zz-theme-gallery.spec.ts 를 실행 대상에 넣거나 수정·스테이징·커밋하지 않는다 — e2e 는 spec 4개를 이름으로 지정해 돌린다"
      status: resolved
      verification: test
    - statement: "git push · relay 배포 · webapp 배포를 하지 않는다 — theme/toss-b 는 미병합 실험 브랜치다"
      status: resolved
      verification: judgment
---

<objective>
e2e 를 새 표현으로 이관하고 터치 경로 · 폭 불변식 · 접근성을 실브라우저로 잠근 뒤, phase 전체 게이트를 돌린다: (1) `trading-workbench.spec.ts` 의 옛 입력/체크박스/더티 바 단언을 새 셀렉터로 옮기고 더티 바 케이스 5개를 즉시 반영 등가물로 재정의 + 최악값 × 4밴드 폭·44px·그룹 높이 P20-3 (2) 터치 describe 에 매수가격 시트(P20-4)·수동주문 시트(P20-5), orderbook 11 · a11y ③⑦ · sidebar-tree 이관 (3) build/test/e2e 전량 게이트 + 시각 확인 + VALIDATION 상태 갱신.

Purpose: 불변식(§2.2b 밴드 · 라벨 잘림 0 · 주문 경로 · 44px 행)과 D-12 · D-13 을 실브라우저로 증명하고 Phase 20 을 검증(`/gsd-verify-work`)에 넘길 수 있게 한다.
Output: e2e 4 spec 이관 · 새 케이스 P20-3~P20-5 · 게이트 결과 · VALIDATION 갱신.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-toss-order-ticket/20-UI-SPEC.md
@.planning/phases/20-toss-order-ticket/20-RESEARCH.md
@.planning/phases/20-toss-order-ticket/20-VALIDATION.md
@.planning/phases/20-toss-order-ticket/20-02-SUMMARY.md
@.planning/phases/20-toss-order-ticket/20-04-SUMMARY.md
@.planning/phases/20-toss-order-ticket/20-06-SUMMARY.md
@webapp/e2e/specs/trading-workbench.spec.ts
@webapp/e2e/overflow.ts

작업 루트는 worktree `/Users/alex/repos/gh-radar/.claude/worktrees/toss-b`. Playwright 는 `workers: 1` · `baseURL http://localhost:3100` · `reuseExistingServer: !CI` — e2e 전에 `lsof -iTCP:3100 -sTCP:LISTEN` 으로 리스너가 없거나 이 worktree 의 `webapp` 서버인지 확인한다(메인 체크아웃 서버면 멈추고 보고). dev 포트는 3100 이다(3000 가정 금지). relay 픽스처는 worktree 의 relay 를 띄운다.

<interfaces>
```ts
// webapp/e2e/specs/trading-workbench.spec.ts (기존 조회구 — 유지)
const cardOf = (page, isin) => …; const toggleOf = (page, isin) => …; const sharedPanels = (page) => …;
const field = (page, id) => page.locator(`#${id}`);      // VI 입력(vi-krx-amount 등)이 계속 쓴다 — 지우지 않는다
async function waitForSetAtGateway(relay, count); async function openFocusedCard(page);
async function sizeCardTo(page, isin, target); async function cardMetrics(page, isin); async function expectCardNotClipped(page, isin, label);
// webapp/e2e/overflow.ts: leavesOverflowing(locator, rightEdge) · scrollOverflowing(page, selector)
// relay 픽스처: relay.seedLimitChasers([...FakeLimitChaserInput]) · relay.pushLimitChaserEcho(cfg) · relay.requestLog() · DMA_MSG.SetLimitChaserReq / DMA_MSG.DirectOrderReq
// FakeLimitChaserInput 은 buyOrderPrice · buyWatchPrice · sweepWatchPrice · sellOrderPrice · sellWatchPrice · buyOrderAmount · buyWatchQty · sellWatchQty · cancelWatchQty · buyMinTradeQty · sellMinTradeQty · sellEntryLatched · sellQtyTrackBaseline 등을 받는다

// 이 플랜이 더하는 조회구
const lcRow = (page, id) => page.locator(`[data-lc-field="${id}"]`);
const lcValue = (page, id) => lcRow(page, id).locator('[data-slot="lc-row-value"]');
async function editLc(page, id, value) { /* 행 클릭 → #id 입력 → fill → Enter */ }
const lcSwitch = (scope, name) => scope.getByRole('switch', { name, exact: true });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `trading-workbench.spec.ts` 이관 — 새 셀렉터 · 더티 바 케이스 5개 즉시 반영 등가물 재정의 · P20-3 최악값 × 4밴드 폭/44px/그룹 높이</name>
  <precondition>포트 3100 이 비어 있거나 이 worktree 의 `webapp` dev 서버가 떠 있다(`lsof -iTCP:3100 -sTCP:LISTEN`)</precondition>
  <files>webapp/e2e/specs/trading-workbench.spec.ts</files>
  <read_first>
    - webapp/e2e/specs/trading-workbench.spec.ts (전체 — 조회구 :100-290 · 케이스 3 · 4 · 5 · 7 · 9 · 10 · 12 · GC2 · 13 · 14 · 16 · 18 · P20-1 · P20-2)
    - webapp/e2e/overflow.ts (`leavesOverflowing` · `scrollOverflowing`)
    - webapp/src/components/trading/lc/lc-fields.ts (옛 id ↔ 필드 · 단위)
    - .planning/phases/20-toss-order-ticket/20-04-SUMMARY.md (20-07 전까지 red 로 남긴 케이스 목록)
    - .planning/phases/20-toss-order-ticket/20-UI-SPEC.md (검증 훅 목록)
  </read_first>
  <action>
    ① 조회구: `dirtyBar` 상수를 지우고 `<interfaces>` 의 `lcRow` · `lcValue` · `editLc`(행 클릭 → `#${id}` 가 보일 때까지 → `fill` → `press('Enter')`) · `lcSwitch` 를 더한다. `field` 는 VI 케이스가 쓰므로 둔다. `openFocusedCard` 의 준비 대기를 `lcValue(page, 'lc-buy-watch-qty')` 가 「10,000주」가 될 때까지로 바꾼다. ② 셀렉터 이관: 케이스 3(스위치 4개 `lcSwitch(card, …)` not checked · 「매수취소 켜기」 포함 · 주문금액 「10만원」 · 잔량 「10,000주」 · `[data-slot="dirty-action-bar"]` 개수 0) · 9(가격 5칸 `lcValue` = `${LIVE_UPPER_LIMIT}원`) · 10(`lcValue('lc-buy-order-price')` · `lcSwitch(card, '매수주문 켜기')` · 미등록 카드에서 `editLc(page, 'lc-buy-order-amount', '50')` 뒤 게이트웨이 `SetLimitChaserReq` 수가 **그대로**(A-P1 로컬 반영)임을 단언한 뒤 스위치 활성 → 클릭 → 10 수신 → 에코 → LED) · 14(스위치 role · 삭제 전 「8,000주」 → 삭제 에코 뒤 「10,000주」) · 18(pane 잎 요소 잘림 판정 유지 · 주석의 「라벨 + 입력」을 「행」으로). ③ **재정의(삭제 금지 · 제목에 「(옛 … 재정의)」 표기)**: **7** → 「폰 밴드 — 인라인 편집 행을 하단 고정 공용 패널 위로 올려 적용 → 10 수신 → 에코 → 행 값 · 패널은 화면 하단 그대로 · 맨 아래까지 스크롤해도 카드 끝이 패널에 묻히지 않는다 (E13 overflow · 옛 E15 더티 바 재정의)」 — 패널 하단 고정 단언과 ③ 맨 아래 단언은 옛 코드 그대로 살리고, 잔량 행 아래 끝이 패널 위 끝보다 위가 되게 스크롤한 뒤 편집·Enter·에코·「8,000주」, 편집 중 행과 패널 boundingBox 세로 겹침 ≤ 1px · **12** → 「값 확정 → 게이트웨이 10 즉시 수신(더티 바·「수정」 없음) → 60 에코 → 행 값 · 반영 시각 (옛 LC 4 · D-04 재정의)」 · **GC2** → 「카드를 접었다 펴도 반영된 값이 그대로다 — 재마운트 없음 · 포커스는 헤더 토글 (WR-02 · D-09 · D-12 · D-04 재정의)」 · **13** → 「인라인 편집 중 다른 단말 변경 — 입력 8,000 유지 · Esc 뒤 행 = 에코 「5,000주」 + 카드 6초 배너(role=status) 「다른 단말에서 변경됐어요 · 서버 값으로 맞췄어요」 + 공용 로그 1줄 (옛 LC 5 · D-07 재정의)」 · **16** → 「상따 설정은 즉시 반영이라 사이드바 이동 때 확인이 뜨지 않는다 — 대조군 + 적용 뒤 「홈」 이동 = 다이얼로그 0 (옛 LC 10 · D-04 재정의)」. ④ 새 케이스 **P20-3**(메인 describe 안): 「P20-3 최악값 × 본문 344 · 700 · 830 · 992 — 우측 패널 잘림 0 · 행 44px(편집 전후) · ≥700 매수주문/매도주문 그룹 높이 동일 (D-20 · UI-SPEC 검증 훅)」 — `relay.seedLimitChasers([{ buyEnabled: true, sellEnabled: true, buyOrderPrice: 1_274_000, buyWatchPrice: 1_274_000, sweepWatchPrice: 1_274_000, sellOrderPrice: 1_274_000, sellWatchPrice: 1_274_000, buyOrderAmount: 9_999, buyWatchQty: 100_000, sellWatchQty: 100_000, cancelWatchQty: 100_000, buyMinTradeQty: 100_000, sellMinTradeQty: 100_000, sellEntryLatched: true, sellQtyTrackBaseline: 100_000 }])` 로 연 카드를 `sizeCardTo` 로 344 · 700 · 830 · 992 에 맞추고 각 폭에서 `expectCardNotClipped` + 보이는 모든 `[data-lc-field]` · `[data-slot="lc-derived"]` · 감시대상 행의 높이가 44(±0.5) · 폰 밴드는 「매수」「매도」 탭을 각각 눌러 두 pane 모두 잰다 · 잔량 행을 인라인 편집으로 연 상태의 높이도 44. 그다음 기준선 없는 시드(`sellEntryLatched: false`)로 다시 열어 700 · 830 · 992 에서 `[data-slot="lc-group-buy"]` 와 `[data-slot="lc-group-sell"]` 높이 차 ≤ 0.5. ⑤ P20-1 · P20-2 는 그대로 둔다. VI 케이스(20~28b)는 건드리지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS" (tsconfig.e2e.json 이 spec 도 타입체크한다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/trading-workbench.spec.ts</automated>
    <fails_when>non-zero exit, or the summary reports any "failed", or "P20-3" is absent from the list output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "const dirtyBar" webapp/e2e/specs/trading-workbench.spec.ts` == 0
    - `grep -c "getByRole('checkbox', { name: '매" webapp/e2e/specs/trading-workbench.spec.ts` == 0
    - `grep -c "lcSwitch(" webapp/e2e/specs/trading-workbench.spec.ts` >= 4
    - `grep -cE "test\\('(7|12|13|16|GC2)\\." webapp/e2e/specs/trading-workbench.spec.ts` >= 5(재정의 케이스 존재 — 삭제 없음)
    - `grep -c "P20-3" webapp/e2e/specs/trading-workbench.spec.ts` >= 1
    - trading-workbench spec 전체 green · 케이스 수가 이관 전(+P20-1 · P20-2) 이상
  </acceptance_criteria>
  <done>작업대 e2e 전체가 새 리스트·즉시 반영 모델에서 green 이고, 최악값 × 4밴드 폭·44px·그룹 높이가 실브라우저로 잠겼다.</done>
</task>

<task type="auto">
  <name>Task 2: 터치 describe 확장(P20-4 매수가격 시트 · P20-5 수동주문 시트) + orderbook 11 · a11y ③⑦ · sidebar-tree 이관 (D-10 · D-12 · D-13 · D-15 · D-17)</name>
  <precondition>포트 3100 이 비어 있거나 이 worktree 의 `webapp` dev 서버가 떠 있다(`lsof -iTCP:3100 -sTCP:LISTEN`)</precondition>
  <files>webapp/e2e/specs/trading-workbench.spec.ts, webapp/e2e/specs/orderbook.spec.ts, webapp/e2e/specs/a11y.spec.ts, webapp/e2e/specs/sidebar-tree.spec.ts</files>
  <read_first>
    - webapp/e2e/specs/trading-workbench.spec.ts (Task 1 결과 · describe 「Phase 20 — 터치 기기 시트」)
    - webapp/e2e/specs/orderbook.spec.ts (:100-160 조회구 · `submitBuyOrder` · :415-450 케이스 11)
    - webapp/e2e/specs/a11y.spec.ts (:240-300 `scanSurface` · `tabbablesIn` · :329-472 작업대 두 케이스)
    - webapp/e2e/specs/sidebar-tree.spec.ts (:200-215 매도주문 켜기 단언)
    - .planning/phases/20-toss-order-ticket/20-UI-SPEC.md (§5 · 접근성 계약 · 검증 훅)
  </read_first>
  <action>
    ① `trading-workbench.spec.ts` 의 `test.describe('Phase 20 — 터치 기기 시트 (D-12 · D-13)')`(`test.use({ hasTouch: true })`)에 두 케이스를 더한다(뷰포트는 테스트 안에서 390×844 로 다시 맞춘다). **P20-4** 「매수가격 시트 — 칩 현재가·상한가·±1호가 · D-15 잠금 · 감시 중 안내 · 적용 → 10 → 에코 → 닫힘」: `seedLimitChasers([{ buyEnabled: true }])`(매도잔량 기준이라 매수주문 상태 = 「감시 중」) → 매수가격 행 탭 → 시트 제목 「매수가격」 · 상태 줄 「감시 중 — 적용하면 바로 반영돼요」 · 「상한가」 칩 → 디스플레이 「127,400」 · 「−1호가」 → 「127,300」 · 「+1호가」 → 「127,400」 · 키로 98150 입력(첫 키가 값을 덮는다) → `role=alert` 「100원 단위로 입력해 주세요 · 가까운 값 98,100 / 98,200」 + 「매수가격 적용」 disabled · ⌫ 로 지우고 127450 → 「상한가 127,400원을 넘을 수 없어요」 · 다시 「상한가」 칩 → 「매수가격 적용」 → 10 수신 → `pushLimitChaserEcho({ buyEnabled: true, buyOrderPrice: 127_400 })` 가 이미 같은 값이면 대신 칩으로 127,300 을 적용하고 그 값의 에코를 보낸다 → 시트 닫힘 · 행 「127,300원」. **P20-5** 「수동주문 시트는 값만 채운다 — 주문은 매수 → 확인 다이얼로그로만」: 폰 밴드 「수동」 탭 → 가격 상자 탭 → 시트 「가격」 · 「현재가」 칩 → 「가격 입력」 → 시트 닫힘 · 상자에 값 · 수량 상자 → 「+1,000」 → 「수량 입력」 → `relay.requestLog()` 에 `DMA_MSG.DirectOrderReq` 0 → 「매수」 → `order-confirm-dialog` 보임 → 다이얼로그 취소 → 여전히 DirectOrderReq 0. ② `orderbook.spec.ts` 케이스 11 을 재정의한다(삭제 금지): 새 `test.describe('… 호가 탭 시트(터치)', () => { test.use({ hasTouch: true }); … })` 안에 「11. 호가 탭 키패드 시트는 AI FAB 위에 뜨고 확정 버튼이 가려지지 않는다 — 390 · 1440 (D-13 · 18-10 인계 재정의)」 — 두 뷰포트에서 `relay.seedLimitChasers([{ buyEnabled: true }])` → `ORDERBOOK_URL` → 잔량 행 「10,000주」 대기 → 탭 → `[data-slot="numpad-sheet"]` 폭 370(390)/440 가운데(1440) · 확정 버튼 중심 좌표의 `document.elementFromPoint` 가 그 버튼(또는 그 자손) · 「닫기」. 이 describe 는 기존 relay beforeAll/afterAll 을 공유하도록 기존 describe 안에 중첩한다. ③ `a11y.spec.ts`: 작업대 데스크톱 케이스 ③ 을 `openCard.getByRole('switch', { name, exact: true })` 4개(「매수취소 켜기」 포함)로 · 모바일 390 케이스의 준비 대기를 `[data-lc-field="lc-buy-watch-qty"] [data-slot="lc-row-value"]` 「10,000주」로 · ⑦ 을 「인라인 편집 중 — 입력이 이름 「잔량」을 갖고 포커스를 유지 · axe 위반 0」으로 재정의(제목의 「더티 바 role=status」도 바꾼다) · 새 hasTouch describe 케이스 「/trading 시트 열린 상태 — role=dialog · aria-modal · axe critical/serious 0 · 닫으면 연 행으로 포커스 복귀」. ④ `sidebar-tree.spec.ts:212` 에서 체크박스 역할로 「매도주문 켜기」를 찾던 단언을 `switch` 역할(같은 이름 · `toBeChecked`)로 바꾼다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/trading-workbench.spec.ts e2e/specs/orderbook.spec.ts e2e/specs/a11y.spec.ts e2e/specs/sidebar-tree.spec.ts</automated>
    <fails_when>non-zero exit, or the summary reports any "failed", or "P20-4" / "P20-5" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "hasTouch: true" webapp/e2e/specs/orderbook.spec.ts` >= 1 · `grep -c "elementFromPoint" webapp/e2e/specs/orderbook.spec.ts` >= 1
    - `grep -c "getByRole('checkbox', { name: '매" webapp/e2e/specs/a11y.spec.ts webapp/e2e/specs/sidebar-tree.spec.ts` 가 파일마다 0
    - `grep -cE "P20-4|P20-5" webapp/e2e/specs/trading-workbench.spec.ts` >= 2
    - 4 spec 전체 green(orderbook 6 · 7 주문 경로 케이스는 `submitBuyOrder` 의 `#mo-*` fill 이 인라인 모드에서 그대로 통과)
  </acceptance_criteria>
  <done>터치 시트(상따 · 수동주문) · 호가 탭 FAB 겹침 · 접근성 · 사이드바가 새 표현에서 실브라우저로 잠겼다.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 20 최종 게이트 — build · test · e2e 4 spec · 시각 확인 · VALIDATION 상태 갱신</name>
  <precondition>포트 3100 이 비어 있거나 이 worktree 의 `webapp` dev 서버가 떠 있다(`lsof -iTCP:3100 -sTCP:LISTEN`)</precondition>
  <files>.planning/phases/20-toss-order-ticket/20-VALIDATION.md</files>
  <read_first>
    - .planning/phases/20-toss-order-ticket/20-VALIDATION.md (Per-Task Verification Map · Wave 0 · Manual-Only)
    - .planning/config.json (`workflow.build_command` · `workflow.test_command`)
    - .planning/sketches/002-toss-order-ticket/index.html (시각 비교 정본)
    - .planning/phases/20-toss-order-ticket/20-01-SUMMARY.md ~ 20-06-SUMMARY.md (게이트 수치 · 남긴 red)
  </read_first>
  <action>
    worktree 루트에서 순서대로 돌리고 수치를 기록한다: (1) config `build_command` 전문 (2) config `test_command` 전문 (3) `pnpm --filter @gh-radar/shared exec vitest --run`(shared 전체) (4) `pnpm --filter @gh-radar/webapp run build` (5) e2e 4 spec(이름 지정 — `zz-theme-gallery.spec.ts` 를 실행 대상에 넣지 않는다). 실패가 있으면 원인 플랜의 파일 범위 안에서 고치고 그 사실을 SUMMARY 에 적는다(조용히 넘기지 않는다). 통과 수는 기준선(webapp 99 파일 · 1731 통과 · 1 skip) 이상이어야 하고 새 파일 수만큼 늘어야 한다. `20-VALIDATION.md` 의 Per-Task Verification Map 각 행 Status 를 실행 결과(✅/❌)로 채우고 frontmatter `wave_0_complete: true` 로 바꾼다(`nyquist_compliant`·`status`·Approval 은 validate-phase 몫이라 건드리지 않는다). 끝으로 `git status --porcelain -- webapp/e2e/specs/zz-theme-gallery.spec.ts` 가 `??` 그대로인지 확인한다. push · 배포는 하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/relay run test && pnpm --filter @gh-radar/webapp run test && pnpm --filter @gh-radar/shared exec vitest --run</automated>
    <fails_when>non-zero exit, or any vitest summary line contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp run build</automated>
    <fails_when>non-zero exit, or output contains "Failed to compile" or "Type error"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/trading-workbench.spec.ts e2e/specs/orderbook.spec.ts e2e/specs/a11y.spec.ts e2e/specs/sidebar-tree.spec.ts</automated>
    <fails_when>non-zero exit, or the summary reports any "failed"</fails_when>
    <human-check>
      <test>worktree dev 서버(PORT=3100)에서 `/trading?focus=…` 카드와 `/stocks/005930?tab=orderbook` 호가 탭을 열고, 목업 `.planning/sketches/002-toss-order-ticket/index.html`(8차)과 나란히 비교한다 — 다크/라이트 · 폰 390(DevTools 터치 에뮬레이션) · 태블릿 768(터치) · 데스크톱 1280(마우스). 행 탭 → 시트, 마우스 클릭 → 인라인, 수동주문 상자·버튼, 매수취소 위치, 감시대상 두 모양을 본다.</test>
      <expected>그룹 둥근 면·44px 행·스위치·원형 체크·감시대상 세그먼트/토글·시트(440 가운데 · 폰 좌우 10 · radius 28 · dim)·키패드·칩·「{필드명} 적용/입력」·수동주문 상자와 48/38 버튼이 목업 톤과 같고, 긴 상태 문구(「발주 완료 · 무장 해제」)는 둘째 줄로 내려가며 말줄임이 없고, 가로 모드 폰에서 시트가 내부 스크롤로 적용 버튼에 닿는다</expected>
      <why_human>색·둥근면·간격·톤 대조와 실기 하이브리드 기기 판정은 grep/jsdom 으로 볼 수 없다(20-VALIDATION Manual-Only)</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - 네 자동 명령 전부 종료 코드 0 · webapp 통과 수 ≥ 1731 + 새 테스트 수 · e2e 4 spec failed 0
    - `grep -c "wave_0_complete: true" .planning/phases/20-toss-order-ticket/20-VALIDATION.md` >= 1
    - Per-Task Map 에 대기 상태 행이 남지 않는다(`grep -c "⬜ pending" .planning/phases/20-toss-order-ticket/20-VALIDATION.md` == 0)
    - `git status --porcelain -- webapp/e2e/specs/zz-theme-gallery.spec.ts` 출력이 `?? webapp/e2e/specs/zz-theme-gallery.spec.ts`
    - SUMMARY 에 게이트 수치(relay · webapp · shared · e2e 통과 수)와 시각 확인 요청이 적혀 있다
  </acceptance_criteria>
  <done>Phase 20 의 자동 게이트가 전량 green 이고 VALIDATION 상태가 실행 결과로 채워졌으며, 시각 확인만 사용자 UAT 로 남았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e 러너 → 로컬 dev 서버(3100) · 로컬 relay · 스텁 게이트웨이 | 실서버·실계좌에 붙지 않는다(D-27 / T-15-28). 잘못된 서버를 재면 거짓 초록 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-16 | Spoofing(검증 대상 착오) | 3100 에 메인 체크아웃 서버 | medium | mitigate | 모든 e2e 태스크 precondition 으로 리스너 확인 · 다르면 멈춤(RESEARCH Pitfall 8) |
| T-20-17 | Repudiation | 옛 케이스 삭제로 커버리지 소실 | medium | mitigate | 더티 바 케이스 5개·orderbook 11·a11y ⑦ 은 재정의(삭제 금지) · 케이스 수 불감소 단언(Task 1 · 2) |
| T-20-09 | Tampering | 수동주문 시트가 주문을 보냄 | high | mitigate | P20-5 가 시트 확정 뒤 DirectOrderReq 0 · 매수 → 확인 다이얼로그 경로만 확인(Task 2) |
| T-20-12 | Tampering | 잘린 라벨(오발주) | high | mitigate | P20-3 최악값 × 4밴드 잘림 0 + 44px(Task 1) |
| T-20-SC | Tampering | 패키지 설치 | low | accept | 설치 없음 |
</threat_model>

<verification>
- e2e 4 spec(trading-workbench · orderbook · a11y · sidebar-tree) green — P20-1~P20-5 포함.
- config build/test · shared 전체 · webapp build green.
- VALIDATION Per-Task Status 채움 · `wave_0_complete: true`.
- 시각 확인은 `<human-check>` 로 end-of-phase UAT 에 넘어간다.
</verification>

<success_criteria>
- 새 표현에서 작업대·호가 탭 e2e 가 전부 green 이고, 폭 불변식(잘림 0 · 44px · 그룹 높이)과 터치 시트 기하·접근성이 실브라우저로 증명됐다.
- Phase 20 이 `/gsd-verify-work` 로 넘어갈 준비가 됐다(배포·push 없음).
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `packages/shared/src/krxTick.ts` — `krxTickSize` · `tickUp` · `tickDown` · `priceInputIssue` · `PriceIssue` | 순수 함수(shared) | 20-02 |
| `packages/shared/src/krxTick.test.ts` | 단위 테스트 | 20-02 |
| `webapp/src/lib/numpad.ts` — `PadUnit` · `PadState` · `padInit` · `padKey` · `PAD_CHIPS` · `PadChip` · `PadChipOp` · `padChipDisabled` · `applyPadChip` · `padValue` · `formatPadDisplay` · `padIssue` · `priceIssueText` · `canConfirmPad` · `stepValue` · `PAD_MAX_DIGITS` | 순수 함수 | 20-02 |
| `webapp/src/lib/__tests__/numpad.test.ts` | 단위 테스트 | 20-02 |
| `webapp/src/lib/use-edit-mode.ts` — `useEditMode` · `EditMode` · `COARSE_POINTER_QUERY` | 훅 | 20-03 |
| `webapp/src/lib/__tests__/use-edit-mode.test.ts` · `webapp/src/lib/__tests__/match-media.ts`(`mockPointer` · `emitPointerChange` · `restoreMatchMedia`) | 테스트 · 헬퍼 | 20-03 |
| `webapp/src/components/trading/lc/use-lc-field-commit.ts` — `useLcFieldCommit` · `LcFieldKey` · `LcCommitKind` · `LcFailReason` · `LcCommitFailure` · `LcCommitOutcome` · `UseLcFieldCommitOptions` · `LC_FLASH_MS` · `LC_COMMIT_TEXT` · `LC_GATE_FIELDS` | 상태 기계 훅 | 20-01 |
| `webapp/src/components/trading/lc/inline-value-editor.tsx` — `InlineValueEditor` | 컴포넌트 | 20-01 (확장 20-05) |
| `webapp/src/components/trading/lc/setting-group.tsx` — `SettingRow` · `FailureBubble` (20-01) · `SettingGroup` · `GroupSwitch` · `CheckValueRow` · `WatchTargetRow` · `DerivedRow` (20-04) | 컴포넌트 | 20-01 · 20-04 |
| `webapp/src/components/trading/lc/number-pad-sheet.tsx` — `NumberPadSheet` · `NumberPadSheetProps` | 컴포넌트(Radix Dialog · body 포털) | 20-03 |
| `webapp/src/components/trading/lc/lc-fields.ts` — `LC_BUY_GROUPS` · `LC_SELL_GROUPS` · `LcGroupSpec` · `LcRowSpec` · `LC_SWITCH_LABEL` · `lcRowById` · `lcNavigableRows` | 필드 스펙(단일 원천) | 20-04 |
| `limit-chaser-form.tsx` 신규 순수 함수 `canArmOf` · `armBlockOf` | 무장 판정 | 20-01 |
| `manual-order-form.tsx` 내부 `TicketBox` · 새 props `currentPrice` · `upperLimit` · 가격 검증 줄 `manual-order-price-issue` | 컴포넌트 | 20-06 |
| CSS 토큰 `--group-bg` · `--switch-off` · `--dim`(`:root` · `.dark`) | 토큰 | 20-03 |
| 테스트: `lc-tracer` · `use-lc-field-commit`(20-01) · `number-pad-sheet`(20-03) · `setting-group`(20-04) · `inline-value-editor` · `inline-navigation`(20-05) · `manual-order-form` 시트 describe(20-06) | 단위/컴포넌트 | 각 플랜 |
| e2e: `P20-1`(20-01) · `P20-2`(20-03) · `P20-3` 최악값 폭 · describe 「Phase 20 — 터치 기기 시트」의 `P20-4` · `P20-5` · 재정의 7/12/GC2/13/16 · orderbook 11 재정의 · a11y ③⑦ 재정의 + 시트 axe(**20-07**) | Playwright | 각 플랜 · **20-07** |
| 제거: `order-panel.tsx` 의 `TICK_TABLE`/`tickFromTable`(20-02) · 더티 바 export/props · 옛 폼 조각(20-04) · 수동주문 `StepButton`/`UnitBox`(20-06) · e2e `dirtyBar` 조회구(**20-07**) | 제거 | 각 플랜 |

## Spec-less probe note

Spec-less probe fallback: **visible skip** — SPEC.md · 요구사항 ID 가 없어 probe 술어를 만들지 않았다. `must_haves` 는 UI-SPEC 검증 훅 · UI Considerations(E1 overflow · 추가 layout 행) · 20-VALIDATION · CONTEXT 불변 목록에서 직접 썼다.

<output>
Create `.planning/phases/20-toss-order-ticket/20-07-SUMMARY.md` when done (gate numbers + the human visual-check request)
</output>
