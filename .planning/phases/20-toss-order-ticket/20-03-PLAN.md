---
phase: 20-toss-order-ticket
plan: 03
type: execute
wave: 2
depends_on: ["20-01", "20-02"]
files_modified:
  - webapp/src/styles/globals.css
  - webapp/src/styles/__tests__/tds-tokens.test.ts
  - webapp/src/lib/use-edit-mode.ts
  - webapp/src/lib/__tests__/use-edit-mode.test.ts
  - webapp/src/lib/__tests__/match-media.ts
  - webapp/src/components/trading/lc/number-pad-sheet.tsx
  - webapp/src/components/trading/lc/__tests__/number-pad-sheet.test.tsx
  - webapp/src/components/trading/lc/setting-group.tsx
  - webapp/src/components/trading/limit-chaser-form.tsx
  - webapp/src/components/trading/lc/__tests__/lc-tracer.test.tsx
  - webapp/e2e/specs/trading-workbench.spec.ts
autonomous: true
requirements: []

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "편집 방식은 입력 장치로 나뉜다 — `matchMedia('(pointer: coarse)')` 가 참이면 바텀시트 + 자체 키패드, 아니면 인라인(폭 무관) · 서버·하이드레이션 첫 렌더는 인라인 · `change` 이벤트를 구독한다 · 하이브리드 기기는 주 입력 장치가 정한다 (D-12)"
    - "새 토큰 셋이 `:root` 와 `.dark` 둘 다에 있고 oklch 가 없다 — `--group-bg` #f9fafb / #2c2c35 · `--switch-off` #d1d6db / #4d4d59 · `--dim` rgba(0, 0, 0, 0.2) / rgba(0, 0, 0, 0.56) (UI-SPEC Color)"
    - "시트는 `document.body` 직속 포털이라 카드 `container-type` 조상 밖에 있고, 폭 `min(440px, 100vw − 20px)` 가운데 · 하단 `max(10px, env(safe-area-inset-bottom))` · radius 28 · 오버레이 `--dim` — 뷰포트 390 에서 폭 370 · 768 에서 440 가운데다 (D-13)"
    - "시트 구성 = 제목(필드명 20/700) · 설명 한 줄 · 값 표시 상자(26/700 숫자 + 단위 18/600 + 「지금 ○○」) · 상태 줄(자리 항상 확보) · 단위별 단축 칩 · 3×4 키패드(1–9 · 00 · 0 · ⌫) · 「닫기 | {필드명} 적용」 56px 두 버튼 (D-16 · D-17 · D-23)"
    - "시트가 열리자마자 현재 값이 선택 강조 상태이고 첫 숫자 키가 값을 통째로 바꾼다 (D-14c)"
    - "가격(원) 시트는 호가 단위 불일치·상한가 초과면 「{필드명} 적용」을 잠그고 이유를 상태 줄(`role=alert`)에 보인다 — 자동 보정 없음 · 무장 불가 값도 같은 자리에 기존 문장으로 잠근다 (D-15)"
    - "값이 비면 「{필드명} 적용」이 비활성이고 상태 줄 문구는 없다 · 명시적 「0」 은 허용한다 (UI Considerations E3 empty)"
    - "그룹이 「감시 중」이어도 추가 확인이 없다 — 상태 줄에 「감시 중 — 적용하면 바로 반영돼요」 한 줄만 · 적용 뒤에는 「반영 중…」 으로 칩·키패드·닫기·Esc·바깥 누름이 잠기고, 에코가 오면 시트가 닫히며 행 값이 900ms 강조된다 — 다음 항목 자동 이동 없음 (D-05 · D-18 · UI Considerations E3 loading)"
    - "실패·거부·3초 무응답이면 시트가 남고 입력값이 보존되며 「반영하지 못했어요」 + 버튼 「다시 시도」 · 연결 끊김이면 「연결이 끊겨 보내지 못했어요」 — 자동 재시도 없음 (D-06 · UI Considerations E3 error)"
    - "시트가 열린 사이 같은 필드 에코가 오면 입력값은 유지되고 「지금 ○○」 가 새 서버 값으로 바뀌며 「다른 단말에서 바뀌었어요」 가 보인다 — 적용하면 내 값이 나가고 목록 행은 에코 값이다 (D-07 · UI Considerations E3 partial)"
    - "시트가 닫히면 연 행으로 포커스가 돌아온다(제어형 Dialog 수동 복원) · `role=dialog` · `aria-modal` · 키패드 `role=group` 「숫자 키패드」 · ⌫ 「한 글자 지우기」 · 「00」 「0 두 번」 · 물리 키 0–9 · Backspace · Enter 를 받는다"
    - "트레이서 확장: 터치 기기에서 「호가변경」 행을 누르면 시트가 열리고 「호가변경 적용」 → 게이트웨이 10 수신 → 60 에코 → 시트 닫힘 → 행 「5건」 이 실브라우저(hasTouch)에서 동작한다"
    - statement: "가로 모드 폰(뷰포트 높이 약 390)에서 시트가 max-height calc(100dvh − 20px) 안에서 내부 스크롤되고 「{필드명} 적용」 버튼에 도달할 수 있다 (UI Considerations 추가 행 · 시트 높이)"
      verification: backstop
  artifacts:
    - path: webapp/src/lib/use-edit-mode.ts
      provides: "시트/인라인 판정 훅(SSR 안전)"
      exports: ["useEditMode", "EditMode", "COARSE_POINTER_QUERY"]
    - path: webapp/src/lib/__tests__/match-media.ts
      provides: "matchMedia 모킹 헬퍼(Pitfall 7 누수 방지)"
      exports: ["mockPointer", "emitPointerChange", "restoreMatchMedia"]
    - path: webapp/src/components/trading/lc/number-pad-sheet.tsx
      provides: "공용 키패드 바텀시트(상따 apply · 수동주문 fill)"
      exports: ["NumberPadSheet", "NumberPadSheetProps"]
    - path: webapp/src/styles/globals.css
      provides: "--group-bg · --switch-off · --dim"
      contains: "--group-bg"
  key_links:
    - from: webapp/src/components/trading/limit-chaser-form.tsx
      to: webapp/src/lib/use-edit-mode.ts
      via: "mode === 'sheet' 이면 행 탭 → NumberPadSheet"
      pattern: "useEditMode\\(\\)"
    - from: webapp/src/components/trading/lc/number-pad-sheet.tsx
      to: webapp/src/lib/numpad.ts
      via: "padKey · applyPadChip · padIssue · canConfirmPad"
      pattern: "padKey|applyPadChip"
    - from: webapp/src/components/trading/limit-chaser-form.tsx
      to: webapp/src/components/trading/lc/use-lc-field-commit.ts
      via: "시트 onConfirm → commit(field, value, 'value') · successSeq 로 닫기"
      pattern: "NumberPadSheet"
  prohibitions:
    - statement: "그룹이 「감시 중」이어도 적용 전에 추가 확인 다이얼로그를 띄우지 않는다 (D-05)"
      status: resolved
      verification: test
    - statement: "시트 안에서 호가 단위·상한가 위반 값을 자동으로 고치지 않는다 — 잠그고 이유만 보인다 (D-15)"
      status: resolved
      verification: test
    - statement: "시트 폭·위치를 뷰포트 브레이크포인트로 분기하지 않는다 — min() 한 식만 쓴다 (§2.2b · UI-SPEC §5)"
      status: resolved
      verification: test
    - statement: "NumberPadSheet 는 스스로 전송하지 않는다 — onConfirm 으로 값만 넘기고 전송·주문은 호출부 몫이다"
      status: resolved
      verification: test
---

<objective>
터치 경로 슬라이스: 편집 방식 판정 훅(`useEditMode`) · 새 토큰 셋 · 공용 키패드 바텀시트(`NumberPadSheet`, Radix Dialog → body 포털)를 만들고, 트레이서 행 「호가변경」에 연결해 **터치 사용자가 시트로 값을 바꾸면 즉시 반영**되는 경로를 실브라우저(hasTouch)까지 증명한다.

Purpose: D-12(입력 장치로 편집 방식 분기) · D-13(440 가운데 시트) · D-16/D-17(자체 키패드·칩) · D-05~D-07(반영 중·실패·다른 단말) · D-23(「{필드명} 적용」)을 한 컴포넌트로 세워 20-04(전체 리스트)와 20-06(수동주문)이 그대로 재사용하게 한다.
Output: `use-edit-mode.ts` · `match-media.ts` 헬퍼 · 토큰 3개 · `number-pad-sheet.tsx`(+테스트) · 트레이서 행 시트 연결 · Playwright P20-2.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-toss-order-ticket/20-CONTEXT.md
@.planning/phases/20-toss-order-ticket/20-UI-SPEC.md
@.planning/phases/20-toss-order-ticket/20-RESEARCH.md
@.planning/phases/20-toss-order-ticket/20-01-SUMMARY.md
@.planning/phases/20-toss-order-ticket/20-02-SUMMARY.md
@.planning/sketches/002-toss-order-ticket/index.html
@webapp/src/styles/globals.css

작업 루트는 worktree `/Users/alex/repos/gh-radar/.claude/worktrees/toss-b`. §2.2b 밴드 표의 정본은 `webapp/src/styles/globals.css` 상단 주석이다 — 표를 다른 곳에 복사하지 않는다.

<interfaces>
```ts
// 20-01 이 만든 계약 — webapp/src/components/trading/lc/use-lc-field-commit.ts
export function useLcFieldCommit(o): { commit(field, value, kind): LcCommitOutcome; inflightField; queuedFields; failures; flashField; successSeq; lastSuccessField; clearFailure };
export const LC_COMMIT_TEXT: { failed; inlineFailed; disconnected; busy; retry; otherDevice; armed };
// 20-01 — webapp/src/components/trading/lc/setting-group.tsx
export function SettingRow(p: { id; label; unit; value; disabled?; busy?; flash?; failed?; editing; editor?; onActivate(el: HTMLElement): void; hasPopup? }): JSX.Element;
// 20-02 — webapp/src/lib/numpad.ts
export function padInit(v: number | null): PadState; export function padKey(s, k): PadState;
export const PAD_CHIPS: Record<PadUnit, readonly PadChip[]>; export function padChipDisabled(c, ctx): boolean;
export function applyPadChip(s, c, ctx): PadState; export function padValue(s): number | null;
export function formatPadDisplay(s): string; export function padIssue(s, unit, ctx): string | null;
export function canConfirmPad(s, unit, ctx): boolean;

// 이 플랜이 만드는 계약
// webapp/src/lib/use-edit-mode.ts
export type EditMode = 'sheet' | 'inline';
export const COARSE_POINTER_QUERY = '(pointer: coarse)';
export function useEditMode(): EditMode;
// webapp/src/components/trading/lc/number-pad-sheet.tsx
export interface NumberPadSheetProps {
  open: boolean; title: string; description: string; unit: PadUnit; purpose: 'apply' | 'fill';
  initialValue: number | null; serverValue?: number | null; ctx: PadCtx;
  status?: 'editing' | 'busy' | 'failed'; failureText?: string | null; armedNotice?: boolean;
  validate?: (value: number) => string | null; returnFocusRef: { current: HTMLElement | null };
  onConfirm: (value: number) => void; onClose: () => void;
}
export function NumberPadSheet(p: NumberPadSheetProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 토큰 3개(`--group-bg` · `--switch-off` · `--dim`) + `useEditMode` + matchMedia 모킹 헬퍼 (D-12)</name>
  <files>webapp/src/styles/globals.css, webapp/src/styles/__tests__/tds-tokens.test.ts, webapp/src/lib/use-edit-mode.ts, webapp/src/lib/__tests__/use-edit-mode.test.ts, webapp/src/lib/__tests__/match-media.ts</files>
  <read_first>
    - webapp/src/styles/globals.css (:1-40 머리 주석 · `:root {` :39 · `.dark {` :149 블록)
    - webapp/src/styles/__tests__/tds-tokens.test.ts (전체 — `parseBlock` · `LIGHT_EXPECTED` · `DARK_EXPECTED` · `THEME_COLOR_TOKENS`)
    - webapp/src/components/trading/workbench/card-grid.tsx (:113-122 `useSyncExternalStore` 선례)
    - webapp/src/components/trading/workbench/alert-toasts.tsx (:46-47 matchMedia 부재 가드)
    - webapp/src/components/trading/__tests__/alert-toasts.test.tsx (:44-66 matchMedia 교체·복원)
    - webapp/tests/setup.ts (:36-47 jsdom matchMedia 폴리필 — `matches: false`)
    - .planning/phases/20-toss-order-ticket/20-UI-SPEC.md (Color 신규 토큰 표 · §7 편집 방식 판정)
  </read_first>
  <behavior>
    - 기본 jsdom → `useEditMode()` === 'inline'
    - `mockPointer(true)` → 'sheet' · `emitPointerChange(false)` → 같은 컴포넌트가 'inline' 으로 재렌더
    - `window.matchMedia` 가 없으면 'inline'(throw 없음)
    - 서버 렌더(`react-dom/server` 의 `renderToString` 으로 훅 결과를 텍스트로 그리는 프로브) → 'inline'
    - `restoreMatchMedia()` 뒤 다음 테스트는 다시 'inline'(누수 없음)
    - 토큰: 라이트 `--group-bg` #f9fafb · `--switch-off` #d1d6db · 다크 #2c2c35 · #4d4d59 · `--dim` 라이트 `rgba(0, 0, 0, 0.2)` · 다크 `rgba(0, 0, 0, 0.56)` · 셋 다 양 블록 정의 · oklch 0
  </behavior>
  <action>
    ① `webapp/src/styles/globals.css` 의 `:root {` 블록에 `--group-bg: #f9fafb;` · `--switch-off: #d1d6db;` · `--dim: rgba(0, 0, 0, 0.2);` 를, `.dark {` 블록에 `--group-bg: #2c2c35;` · `--switch-off: #4d4d59;` · `--dim: rgba(0, 0, 0, 0.56);` 를 기존 토큰 무리 끝(각 블록 안)에 더한다. 바로 위에 한국어 주석 1~3줄: 「Phase 20 호가주문 토스식 — 상따 그룹 면(목업 `.grp`) · 스위치 off · 시트 dim(TDS dimmedBackground). 다크 그룹 면은 부모 `--card` #202027 보다 한 단계 밝은 grey100(UI-SPEC A9)」. §2.2b 밴드 주석·경계·`container-type` 은 건드리지 않는다.
    ② `webapp/src/styles/__tests__/tds-tokens.test.ts`: `LIGHT_EXPECTED` 에 `'group-bg': '#f9fafb'` · `'switch-off': '#d1d6db'`, `DARK_EXPECTED` 에 `'group-bg': '#2c2c35'` · `'switch-off': '#4d4d59'`, `THEME_COLOR_TOKENS` 에 `'group-bg'` · `'switch-off'` · `'dim'` 을 더하고, `--dim` 두 값을 정규식(`/^rgba\(\s*0\s*,\s*0\s*,\s*0\s*,\s*0?\.2\s*\)$/` · `…0?\.56…`)으로 단언하는 `it` 을 하나 더한다.
    ③ `webapp/src/lib/use-edit-mode.ts` 신설('use client'): `COARSE_POINTER_QUERY = '(pointer: coarse)'`, `EditMode = 'sheet' | 'inline'`, `useEditMode()` = `useSyncExternalStore(subscribe, getSnapshot, () => false) ? 'sheet' : 'inline'`. subscribe 는 `window`/`window.matchMedia` 부재 가드 후 `mql.addEventListener('change', …)`, getSnapshot 은 같은 가드 후 `.matches`. 머리 주석에 D-12 · UI-SPEC §7(주 입력 장치 원칙 · `(any-pointer: fine)` 을 쓰지 않는 이유) · RESEARCH Q1 Playwright 실측(hasTouch → coarse) · 「이 훅은 레이아웃을 정하지 않는다 — 폭은 `@container/lc`」.
    ④ `webapp/src/lib/__tests__/match-media.ts` 신설(테스트 전용 헬퍼 · `.test.` 가 아니라 수집되지 않는다): 원본 `window.matchMedia` 를 보관하고 `mockPointer(coarse: boolean)` 가 `matches: coarse && query === '(pointer: coarse)'` 인 객체(addEventListener/removeEventListener 가 리스너를 모아 둠)를 돌려주는 함수로 교체, `emitPointerChange(coarse)` 가 상태를 바꾸고 모인 리스너를 부르며, `restoreMatchMedia()` 가 원본으로 되돌린다. `webapp/tests/setup.ts` 폴리필 모양을 따른다.
    ⑤ `webapp/src/lib/__tests__/use-edit-mode.test.ts` 신설 — `<behavior>` 를 `renderHook`(@testing-library/react) 와 `renderToString`(`react-dom/server`, JSX 없이 `createElement`) 으로 단언하고 `afterEach(restoreMatchMedia)`.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/__tests__/use-edit-mode.test.ts src/styles/__tests__/tds-tokens.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed", or "use-edit-mode.test.ts" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c -- "--group-bg:" webapp/src/styles/globals.css` >= 2
    - `grep -c -- "--switch-off:" webapp/src/styles/globals.css` >= 2
    - `grep -c -- "--dim:" webapp/src/styles/globals.css` >= 2
    - `grep -c "'group-bg'" webapp/src/styles/__tests__/tds-tokens.test.ts` 가 3 이상(LIGHT · DARK · THEME 목록)
    - `grep -c "useSyncExternalStore" webapp/src/lib/use-edit-mode.ts` >= 1
    - `grep -c "matchMedia('(any-pointer" webapp/src/lib/use-edit-mode.ts` == 0(주 입력 장치 원칙 — any-pointer 판정식을 쓰지 않는다)
    - 두 테스트 파일 통과
  </acceptance_criteria>
  <done>시트/인라인 판정이 SSR 안전한 훅 하나로 서고, 새 토큰 셋이 양 테마에 정의돼 팔레트 가드 테스트에 올랐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `NumberPadSheet` — Radix Dialog 직접 조립 · body 포털 · 키패드/칩/상태 줄/반영 중 잠금 · 「{필드명} 적용|입력」 (D-13 · D-16 · D-17 · D-23 · D-05~D-07)</name>
  <files>webapp/src/components/trading/lc/number-pad-sheet.tsx, webapp/src/components/trading/lc/__tests__/number-pad-sheet.test.tsx</files>
  <read_first>
    - webapp/src/components/ui/sheet.tsx (Radix Dialog 조립 · `data-open:`/`data-closed:` tw-animate 유틸 선례 — 이 파일은 고치지 않는다)
    - webapp/src/components/orderbook/order-confirm-dialog.tsx (기존 Dialog 소비 · 포커스 규율)
    - .planning/sketches/002-toss-order-ticket/index.html (:169-192 `.sheet` · `.disp` · `.chips` · `.pad` · `.btns` · :223 `.err` · :388-406 `showSheet`/`paint`)
    - .planning/phases/20-toss-order-ticket/20-UI-SPEC.md (§5 바텀시트 해부 · 전송 상태 기계 표 · 접근성 계약 · 모션)
    - .planning/phases/20-toss-order-ticket/20-RESEARCH.md (§Q3 — `onCloseAutoFocus` 수동 복원 · 초기 포커스 · busy 잠금 · 440 가운데 · `<input>` 없음)
    - webapp/src/lib/numpad.ts (20-02 결과)
  </read_first>
  <behavior>
    - 열리면 `[data-slot="numpad-sheet"]` 가 `document.body` 안에 있고 렌더 컨테이너(카드 자리) 밖이다 · `role="dialog"` · `aria-modal="true"` · 제목 「잔량」 · 설명 문장
    - `purpose='apply'`, title 「잔량」 → 확정 버튼 「잔량 적용」 · `purpose='fill'`, title 「가격」 → 「가격 입력」 · 화면 어디에도 단독 「확인」 버튼 없음
    - initialValue 10000 → 디스플레이 「10,000」 강조(fresh) → '5' 키 → 「5」 · 「00」 첫 키 → 빈 값 → 확정 비활성 · 상태 줄 문구 없음
    - 칩(주) 「+1,000」 on 10000 → 「11,000」 · (원, ctx current 98100 upper 127400) 「현재가」 → 98,100 · 「상한가」 → 127,400 · current 0 → 「현재가」 disabled
    - (원) 98150 → 상태 줄 `role="alert"` 「100원 단위로 입력해 주세요 · 가까운 값 98,100 / 98,200」 + 확정 비활성 · 127500 → 「상한가 127,400원을 넘을 수 없어요」
    - `validate` 가 문장을 돌려주면 같은 자리(alert)에 그 문장 + 확정 비활성
    - `status='busy'` → 확정 글자 「반영 중…」 disabled · 칩·키패드·「닫기」 disabled · Esc 와 오버레이 누름이 `onClose` 를 부르지 않는다
    - `status='failed'` + failureText 「반영하지 못했어요」 → alert 문구 · 확정 글자 「다시 시도」 활성 · 입력값 유지
    - 열린 채 `serverValue` 가 10000 → 12000 으로 바뀌면 입력 버퍼 유지 · 「지금 12,000주」 · `role="status"` 「다른 단말에서 바뀌었어요」 (purpose 'fill' 이면 이 알림 없음)
    - `armedNotice` → `role="status"` 「감시 중 — 적용하면 바로 반영돼요」
    - 물리 키 '7' → Enter → `onConfirm(7)` · Backspace → 한 글자 지움
    - 닫기 → `onClose` · 닫힌 뒤 포커스가 `returnFocusRef.current` 요소로 돌아간다
    - (회, maxPieces 5) 칩 「10」 disabled
    - 키패드 `role="group"` 이름 「숫자 키패드」 · ⌫ 이름 「한 글자 지우기」 · 「00」 이름 「0 두 번」 · 「−1호가」 이름 「1호가 내리기」
  </behavior>
  <action>
    `webapp/src/components/trading/lc/number-pad-sheet.tsx` 신설('use client'). `import { Dialog } from 'radix-ui'` 로 **직접 조립**한다(`ui/sheet.tsx` 는 오버레이 oklch·blur·닫기 X·전폭 bottom 이 강제라 쓰지 않는다 · 그 파일 무수정). 구조: `Dialog.Root`(open 제어 · `onOpenChange(o)` 는 `!o && status !== 'busy'` 일 때만 `onClose`) → `Dialog.Portal`(기본 컨테이너 = `document.body` — `container-type` 조상 밖이라 `position:fixed` 가 뷰포트 기준, D-28 과 같은 이유) → `Dialog.Overlay`(`fixed inset-0 z-50 bg-[var(--dim)]` · 페이드 200ms · blur 없음) + `Dialog.Content`(`data-slot="numpad-sheet"` · `tabIndex={-1}` · 클래스 `fixed inset-x-0 mx-auto bottom-[max(10px,env(safe-area-inset-bottom))] z-50 w-[min(440px,calc(100vw-20px))] max-h-[calc(100dvh-20px)] overflow-y-auto rounded-[28px] bg-[var(--card)] px-5 pt-[22px] pb-4 outline-none` — 가운데 정렬은 `inset-x-0 mx-auto` 로 해 슬라이드 transform 과 다투지 않게(RESEARCH A8) · 등장 250ms translateY(120%)→0, `motion-reduce` 면 opacity 150ms, `data-open:`/`data-closed:` tw-animate 유틸) · `onOpenAutoFocus={e => { e.preventDefault(); contentRef.current?.focus(); }}` · `onCloseAutoFocus={e => { e.preventDefault(); returnFocusRef.current?.focus(); }}`(트리거 없는 제어형이라 Radix 가 복귀를 못 한다 — RESEARCH Pitfall 6) · busy 면 `onEscapeKeyDown`/`onPointerDownOutside` 에서 `e.preventDefault()`. 내용 순서(UI-SPEC §5): `Dialog.Title`(20/700 `--fg` · 아래 4) · `Dialog.Description`(14/400 `--muted-fg` · 아래 14) · `<output aria-live="polite" data-slot="numpad-display">` 상자(`--muted` · radius 16 · 패딩 14 16 · baseline 정렬 · 간격 4): 숫자 26/700 `--fg` min-h 32(fresh 이고 값이 있으면 `bg-[color-mix(in_srgb,var(--primary)_32%,transparent)] rounded-[4px]`) + 캐럿 2×26 `--primary`(1s steps(1) 깜빡임 · reduced-motion 이면 고정) + 단위 18/600 `--fg-2` + 오른쪽 끝 「지금 {serverValue 포맷}{단위}」 12.5 `--muted-fg`(serverValue 가 null/undefined 면 없음) · 상태 줄 `<p data-slot="numpad-status">` 12.5 · `min-h-4` 항상 자리 확보 · 우선순위 ① `padIssue ?? validate(값)` `role="alert"` `--destructive` ② `failureText` `role="alert"` ③ 열 때 기억한 `serverValue` 와 지금 값이 다르고 `purpose === 'apply'` 이며 busy 아님 → 「다른 단말에서 바뀌었어요」 `role="status"` `--muted-fg` ④ `armedNotice` → 6px `--led-armed` 점 + 「감시 중 — 적용하면 바로 반영돼요」 `role="status"` (문구는 `LC_COMMIT_TEXT` 에서 import) · 칩 줄(`PAD_CHIPS[unit]` · 높이 32 · 13/600 `--fg-2` · `--muted` · radius 999 · 좌우 12 · 간격 6 · 줄바꿈 허용 · `touch-manipulation` · `ariaLabel` 있으면 `aria-label` · `padChipDisabled` 또는 busy 면 disabled) · 키패드 `role="group" aria-label="숫자 키패드"` 3×4 격자 간격 2(`1 2 3 / 4 5 6 / 7 8 9 / 00 0 ⌫` · 키 높이 56 · 24/500 `--fg` · 투명 · radius 12 · `active:bg-[var(--muted)]` · `touch-manipulation` · 「00」 `aria-label="0 두 번"` · ⌫ = lucide `Delete` 24px + `aria-label="한 글자 지우기"`) · 버튼 줄 2개 간격 8 · 각 flex:1 높이 56 radius 16 17/600: 「닫기」(`--muted` + `--fg-2`, busy 면 disabled) · 확정(`bg-[var(--primary)] text-[var(--destructive-fg)]` — 채움 면 글자색 LOCKED 규칙 · 비활성 `opacity-40` · 글자 = busy 「반영 중…」 / failed 「다시 시도」 / 그 밖 `${title} 적용`(apply) · `${title} 입력`(fill)). 버퍼는 `useState(padInit(initialValue))` 이고 `open` 이 false→true 로 바뀔 때만 다시 `padInit` 한다(열린 사이 `serverValue`·`initialValue` 변화로 버퍼를 덮지 않는다 — D-07). `Dialog.Content` 의 `onKeyDown`: `0`–`9` → `padKey`, `Backspace` → `'back'`, `Enter` → 확정 가능하면 `onConfirm(padValue)`(Esc 는 Radix). 확정 가능 = `canConfirmPad(state, unit, ctx) && validate?.(값) == null && status !== 'busy'`. 이 컴포넌트는 `send`·`sendOrder` 를 부르지 않는다. 레이아웃 폭 분기는 `min()` 한 식뿐이다(뷰포트 브레이크포인트 유틸 0). 하단 여백의 `env(safe-area-inset-bottom)` 는 현재 `viewport-fit` 이 없어 실효 10px 이다 — 전역 레이아웃(`app/layout.tsx` viewport)은 건드리지 않는다(RESEARCH §Q3). `webapp/src/components/trading/lc/__tests__/number-pad-sheet.test.tsx` 에 `<behavior>` 전부를 단언한다(15개 이상 `it` · `userEvent` · 포커스 복귀는 버튼을 `returnFocusRef` 로 두고 닫은 뒤 `document.activeElement` 비교).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/lc/__tests__/number-pad-sheet.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed", or "number-pad-sheet.test.tsx" is absent from the output</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `number-pad-sheet.test.tsx` 15개 이상 통과
    - `grep -c "w-\[min(440px,calc(100vw-20px))\]" webapp/src/components/trading/lc/number-pad-sheet.tsx` >= 1
    - `grep -c "rounded-\[28px\]" webapp/src/components/trading/lc/number-pad-sheet.tsx` >= 1
    - `grep -c "bg-\[var(--dim)\]" webapp/src/components/trading/lc/number-pad-sheet.tsx` >= 1
    - `grep -c "onCloseAutoFocus" webapp/src/components/trading/lc/number-pad-sheet.tsx` >= 1
    - `grep -c "from '@/components/ui/sheet'" webapp/src/components/trading/lc/number-pad-sheet.tsx` == 0
    - `grep -cE "<input" webapp/src/components/trading/lc/number-pad-sheet.tsx` == 0(시트에 입력칸 없음 — 소프트 키보드 불필요)
    - `grep -nE "(^|[\"' ])(sm|md|lg|xl|2xl):" webapp/src/components/trading/lc/number-pad-sheet.tsx` 출력 0줄
    - typecheck 0
  </acceptance_criteria>
  <done>터치 편집의 모든 상태(편집 · 검증 잠금 · 반영 중 · 실패 · 다른 단말 · 감시 중 안내)를 말하는 시트 하나가 body 포털로 서고, 상따(apply)·수동주문(fill)이 문구만 바꿔 재사용할 수 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 트레이서 행에 시트 연결 — 터치 기기 「호가변경」 탭 → 시트 → 적용 → 에코 → 닫힘 (jsdom + hasTouch e2e)</name>
  <precondition>포트 3100 이 비어 있거나 이 worktree 의 `webapp` dev 서버가 떠 있다(`lsof -iTCP:3100 -sTCP:LISTEN`)</precondition>
  <files>webapp/src/components/trading/limit-chaser-form.tsx, webapp/src/components/trading/lc/setting-group.tsx, webapp/src/components/trading/lc/__tests__/lc-tracer.test.tsx, webapp/e2e/specs/trading-workbench.spec.ts</files>
  <read_first>
    - webapp/src/components/trading/limit-chaser-form.tsx (20-01 의 「호가변경」 행 · `editingField` · `useLcFieldCommit` 배선)
    - webapp/src/components/trading/lc/setting-group.tsx (`SettingRow` `hasPopup` · `onActivate(el)`)
    - webapp/src/components/trading/lc/__tests__/lc-tracer.test.tsx (20-01 하네스)
    - webapp/e2e/specs/trading-workbench.spec.ts (P20-1 · :1960-1981 `hasTouch` describe 선례 — 바깥 beforeEach 가 WIDE_VIEWPORT 로 덮으므로 테스트 안에서 뷰포트를 다시 맞춘다)
    - .planning/phases/20-toss-order-ticket/20-UI-SPEC.md (§5 · 검증 훅 「시트 폭: 뷰포트 390 → 370 · ≥460 → 440 가운데」)
  </read_first>
  <behavior>
    - `mockPointer(true)`: 「호가변경 3건」 행에 `aria-haspopup="dialog"` · 누르면 `#lc-sweep-tick` 입력은 생기지 않고 dialog 「호가변경」 이 열린다 · 확정 버튼 「호가변경 적용」 · 칩 「1」「3」「5」「지우기」
    - 5 → 「호가변경 적용」 → `send` 1회(cfg.sweepMinTickCount 5) · 버튼 「반영 중…」 → 에코 5 → dialog 닫힘 · 행 「5건」 강조 · 포커스가 그 행 버튼
    - 거부(답만 증가) → dialog 유지 · 「반영하지 못했어요」 · 「다시 시도」 · 디스플레이 「5」 유지 · 누르면 `send` 2회째
    - 열린 채 다른 단말 에코(7) → 디스플레이 입력값 유지 · 「지금 7건」 · 「다른 단말에서 바뀌었어요」
    - 「닫기」 → `send` 0 · 실패 기록 지움
    - `mockPointer(false)`(기본) 에서는 20-01 인라인 동작 그대로
  </behavior>
  <action>
    ① `limit-chaser-form.tsx`: `const mode = useEditMode();` 와 `const [sheetField, setSheetField] = useState<LcFieldKey | null>(null)` · `const returnFocusRef = useRef<HTMLElement | null>(null)` 를 둔다. 「호가변경」 행의 `onActivate(el)` 은 `returnFocusRef.current = el` 후 `mode === 'sheet'` 면 `setSheetField('sweepMinTickCount')`, 아니면 20-01 의 인라인 편집 시작. `SettingRow` 에 `hasPopup={mode === 'sheet'}`. 폼 끝(패널 밖)에 `NumberPadSheet` 를 **한 개만** 두고 `open={sheetField !== null}` · `title`=행 라벨(「호가변경」) · `description` = UI-SPEC 카피 표(「호가가 이만큼 바뀌면 한 번에 체결해요」) · `unit='건'` · `purpose='apply'` · `initialValue = failures[field]?.value(숫자면) ?? form[field]` · `serverValue = form[field]`(값 필드는 낙관 반영이 없어 폼 값 = 서버 동기값) · `ctx={{ current: currentPrice, upper: upperLimit ?? 0 }}`(이제 `currentPrice` 를 구조분해한다) · `status` = 그 필드가 `inflightField` 거나 `queuedFields` 에 있으면 'busy', `failures[field]` 가 있으면 'failed', 아니면 'editing' · `failureText = failures[field]?.text` · `armedNotice` = 그 필드가 속한 그룹 상태가 「감시 중」인지(이 태스크의 한방체결 그룹은 늘 「켜짐/꺼짐」이라 false — 20-04 가 필드 스펙으로 일반화) · `validate={(v) => armBlockOf({ ...기준값, [field]: v })}` · `onConfirm(v)` → `commit(field, v, 'value')` 결과 `'noop'`·`'local'` 이면 `setSheetField(null)`, 그 밖은 시트 유지 · 성공 닫기는 이펙트(`successSeq` 증가 && `lastSuccessField === sheetField` → `setSheetField(null)`) · `onClose` → `clearFailure(field)` + `setSheetField(null)` · `returnFocusRef`.
    ② `setting-group.tsx`: `SettingRow` 가 `hasPopup` 이면 `aria-haspopup="dialog"` 를 달게 한다(20-01 에서 prop 만 있었다면 확인만).
    ③ `lc/__tests__/lc-tracer.test.tsx` 에 describe 「터치(시트) 경로」 를 더하고 `beforeEach(() => mockPointer(true))` · `afterEach(restoreMatchMedia)` 로 `<behavior>` 를 단언한다(Pitfall 7 누수 금지).
    ④ `webapp/e2e/specs/trading-workbench.spec.ts` 메인 describe 안에 `test.describe('Phase 20 — 터치 기기 시트 (D-12 · D-13)', () => { test.use({ hasTouch: true }); … })` 를 더하고 `test('P20-2 터치 기기 — 「호가변경」 탭 → 시트(뷰포트 390 폭 370 · 768 폭 440 가운데 · radius 28 · 하단 ≥10 · body 직속) → 5 → 「호가변경 적용」 → 게이트웨이 10 수신 → 60 에코 → 시트 닫힘 · 행 「5건」')` 를 쓴다. 뷰포트를 테스트 안에서 `{ width: 390, height: 844 }` 로 맞추고(바깥 beforeEach 가 WIDE 로 덮는다) `relay.seedLimitChasers([{ buyEnabled: true }])` → `page.goto(FOCUS_URL)` → `waitForReady` → 값 슬롯 「3건」 대기 → 행 탭(`.tap()` 또는 click) → `[data-slot="numpad-sheet"]` 보임 · boundingBox 폭 370(±1) · x 10(±1) · `innerHeight − (y + height)` ≥ 10 · computed `border-radius` 28px · `evaluate(el => el.closest('[data-slot="strategy-card"]') === null)` 참 → 키패드 「5」 → 「호가변경 적용」 → `waitForSetAtGateway(relay, before + 1)` → `relay.pushLimitChaserEcho({ buyEnabled: true, sweepMinTickCount: 5 })` → 시트 사라짐 · 값 슬롯 「5건」. 이어서 뷰포트 768 로 바꿔 다시 열어 폭 440(±1) · x = (768 − 440) / 2(±1) 를 확인하고 「닫기」.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/lc/__tests__/lc-tracer.test.tsx src/components/trading/lc/__tests__/number-pad-sheet.test.tsx src/lib/__tests__/use-edit-mode.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp run test</automated>
    <fails_when>non-zero exit, or output contains "error TS", or the "Test Files" summary line contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/trading-workbench.spec.ts -g "P20-"</automated>
    <fails_when>non-zero exit, or the output lacks "2 passed" (P20-1 and P20-2), or contains "No tests found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "useEditMode()" webapp/src/components/trading/limit-chaser-form.tsx` >= 1
    - `grep -n "<NumberPadSheet" webapp/src/components/trading/limit-chaser-form.tsx` 출력이 정확히 한 줄(시트 인스턴스 하나)
    - lc-tracer 의 터치 describe 케이스 5개 이상 통과 · webapp 전체 suite 통과 · typecheck 0
    - Playwright `P20-1` · `P20-2` 2 passed(폭 370/440 · radius 28 · body 직속 단언 포함)
  </acceptance_criteria>
  <done>터치 사용자가 시트로 「호가변경」을 바꾸면 실브라우저에서 게이트웨이까지 즉시 반영되고, 시트는 에코 뒤 닫히며 포커스는 행으로 돌아온다 — 마우스 경로는 그대로다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 시트 키패드 입력 → `commit` → relay `lc.set` | 사용자가 「{필드명} 적용」을 눌러야만 나간다. 서버 스키마가 최종 |
| body 포털 모달 ↔ 카드 DOM | 모달이 배경을 `aria-hidden`·포인터 차단 — 다른 카드 조작이 시트 뒤로 새지 않는다(Radix `hideOthers`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering(자기 자산) | 시트 오터치 즉시 반영 | high | mitigate | 명시 버튼 「{필드명} 적용」만 전송 · 칩/키는 값만 바꾼다 · busy 동안 모든 입력 잠금(Task 2) · 무장 불가 값은 `validate` 로 확정 잠금 · 끄기는 늘 허용 |
| T-20-04 | Repudiation | 거부를 성공으로 표시 | high | mitigate | 시트는 `successSeq` 이펙트로만 닫힌다(에코 값 일치) · 거부/무응답/끊김 각각 상태 줄 문구(Task 2 · 3) |
| T-20-10 | Tampering | 자동 보정 가격 | high | mitigate | 호가 단위·상한가 위반은 확정 잠금 + 이유만(Task 2) |
| T-20-11 | Spoofing/UI redress | 시트가 다른 표면(FAB · 토스트) 아래 깔려 버튼 오인 | medium | mitigate | z-50 + body 끝 포털로 FAB(z-40) 위 · 오버레이 `--dim` 이 배경 포인터 차단(Task 2) — 20-07 orderbook 11 대체 테스트가 겹침 확인 |
| T-20-SC | Tampering | 패키지 설치 | low | accept | 설치 없음 — `radix-ui` · `lucide-react` 기설치 |
</threat_model>

<verification>
- `use-edit-mode.test.ts` · `tds-tokens.test.ts` · `number-pad-sheet.test.tsx` · `lc-tracer.test.tsx`(터치 describe) green.
- webapp 전체 suite · typecheck green.
- Playwright P20-1 · P20-2 green(시트 기하 실측 포함).
</verification>

<success_criteria>
- 터치 기기에서 행 탭 → 440 가운데(폰은 좌우 10) 시트 → 자체 키패드 → 「{필드명} 적용」 → 즉시 반영 → 닫힘이 실브라우저에서 증명됐다.
- 마우스 기기 경로(20-01 인라인)는 바뀌지 않았다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `packages/shared/src/krxTick.ts` — `krxTickSize` · `tickUp` · `tickDown` · `priceInputIssue` · `PriceIssue` | 순수 함수(shared) | 20-02 |
| `packages/shared/src/krxTick.test.ts` | 단위 테스트 | 20-02 |
| `webapp/src/lib/numpad.ts` — `PadUnit` · `PadState` · `padInit` · `padKey` · `PAD_CHIPS` · `PadChip` · `PadChipOp` · `padChipDisabled` · `applyPadChip` · `padValue` · `formatPadDisplay` · `padIssue` · `priceIssueText` · `canConfirmPad` · `stepValue` · `PAD_MAX_DIGITS` | 순수 함수 | 20-02 |
| `webapp/src/lib/__tests__/numpad.test.ts` | 단위 테스트 | 20-02 |
| `webapp/src/lib/use-edit-mode.ts` — `useEditMode` · `EditMode` · `COARSE_POINTER_QUERY` | 훅 | **20-03** |
| `webapp/src/lib/__tests__/use-edit-mode.test.ts` · `webapp/src/lib/__tests__/match-media.ts`(`mockPointer` · `emitPointerChange` · `restoreMatchMedia`) | 테스트 · 헬퍼 | **20-03** |
| `webapp/src/components/trading/lc/use-lc-field-commit.ts` — `useLcFieldCommit` · `LcFieldKey` · `LcCommitKind` · `LcFailReason` · `LcCommitFailure` · `LcCommitOutcome` · `UseLcFieldCommitOptions` · `LC_FLASH_MS` · `LC_COMMIT_TEXT` · `LC_GATE_FIELDS` | 상태 기계 훅 | 20-01 |
| `webapp/src/components/trading/lc/inline-value-editor.tsx` — `InlineValueEditor` | 컴포넌트 | 20-01 (확장 20-05) |
| `webapp/src/components/trading/lc/setting-group.tsx` — `SettingRow` · `FailureBubble` (20-01) · `SettingGroup` · `GroupSwitch` · `CheckValueRow` · `WatchTargetRow` · `DerivedRow` (20-04) | 컴포넌트 | 20-01 · 20-04 |
| `webapp/src/components/trading/lc/number-pad-sheet.tsx` — `NumberPadSheet` · `NumberPadSheetProps` | 컴포넌트(Radix Dialog · body 포털) | **20-03** |
| `webapp/src/components/trading/lc/lc-fields.ts` — `LC_BUY_GROUPS` · `LC_SELL_GROUPS` · `LcGroupSpec` · `LcRowSpec` · `LC_SWITCH_LABEL` · `lcRowById` · `lcNavigableRows` | 필드 스펙(단일 원천) | 20-04 |
| `limit-chaser-form.tsx` 신규 순수 함수 `canArmOf` · `armBlockOf` | 무장 판정 | 20-01 |
| `manual-order-form.tsx` 내부 `TicketBox` · 새 props `currentPrice` · `upperLimit` | 컴포넌트 | 20-06 |
| CSS 토큰 `--group-bg` · `--switch-off` · `--dim`(`:root` · `.dark`) | 토큰 | **20-03** |
| 테스트: `lc/__tests__/lc-tracer.test.tsx`(20-01 · **20-03**) · `use-lc-field-commit.test.tsx`(20-01) · `number-pad-sheet.test.tsx`(**20-03**) · `setting-group.test.tsx`(20-04) · `inline-value-editor.test.tsx` · `inline-navigation.test.tsx`(20-05) | 단위/컴포넌트 | 각 플랜 |
| e2e: `P20-1`(20-01) · describe 「Phase 20 — 터치 기기 시트」 `P20-2`(**20-03**) · `P20-3`~`P20-5` · orderbook 11 대체 · a11y ③⑦ 대체(20-07) | Playwright | 각 플랜 |
| 제거: `order-panel.tsx` 의 `TICK_TABLE`/`tickFromTable`(20-02) · `LIMIT_CHASER_DIRTY_HINT` · `cardDirtyHint` export(20-04) | 제거 | 20-02 · 20-04 |

## Spec-less probe note

Spec-less probe fallback: **visible skip** — SPEC.md · 요구사항 ID 가 없어 probe 술어를 만들지 않았다. `must_haves` 는 CONTEXT D-05~D-07 · D-12~D-18 · D-23 · UI-SPEC §5 · §7 · UI Considerations E3 에서 직접 썼다.

<output>
Create `.planning/phases/20-toss-order-ticket/20-03-SUMMARY.md` when done
</output>
