---
phase: 20-toss-order-ticket
plan: 06
type: execute
wave: 4
depends_on: ["20-02", "20-03", "20-04"]
files_modified:
  - webapp/src/components/trading/card/manual-order-form.tsx
  - webapp/src/components/trading/__tests__/manual-order-form.test.tsx
  - webapp/src/components/trading/card/card-body.tsx
autonomous: true
requirements: []

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "수동주문 기능은 그대로다(가격 · 수량 · 조건부 조각 수 · 주문금액 · 매수/매도/정정/취소) — ±버튼 · 비율(%) 버튼 · 매수/매도 세그먼트 + 단일 CTA 는 없다 · 가격·수량(·조각 수) 상자는 라벨 위 · 값 17/600 · `--muted` 면 · radius 16 · 패딩 12 12 12 14 (D-08)"
    - "진입은 그대로다 — 본문 <700 「매수 | 매도 | 수동」 3탭(34px · 15/600 · 트랙 `--muted` radius 12 · 중립 선택면), ≥700 옵션 오른쪽 위 「수동주문」 버튼(32px · 13/600)이 옵션을 덮고 머리 줄 「수동주문 · 키 {전략 키}」(12px · 한 줄 말줄임 + title) + ✕(「수동주문 닫기」)로 닫는다 · 덮어도 옵션 값 보존 (D-09)"
    - "터치 기기에서는 가격·수량·조각 수 상자를 누르면 상따 설정과 같은 키패드 시트가 열리고, 확정 버튼 「가격 입력」「수량 입력」「조각 수 입력」은 값만 채우고 곧바로 닫힌다(전송 0 · 확인 다이얼로그 0 · 반영 중 단계 없음) (D-10 · D-23)"
    - "마우스 기기에서는 상자에 직접 타이핑한다 — 포커스 = 값 전체 선택 · ↑/↓ = 가격은 한 호가 · 수량·조각 수는 1 · Enter = blur · 호가 사다리 클릭은 여전히 가격을 채운다 (D-10 · D-14c)"
    - "주문은 오직 매수/매도(정정/취소) 버튼 → 기존 `order-confirm-dialog` 로만 나간다 — `sendOrder` 호출 지점 · 결과 모름 잠금 · 77 힌트 라벨 · 정정/취소 조건 · 종목 전환 리셋은 한 줄도 바뀌지 않았다 (D-10 · TRADE-07 불변식)"
    - "버튼은 매수(`--up`, 왼쪽)·매도(`--down`, 오른쪽) 48px 2열(radius 14 · 600 · 간격 8 · 위 10) + 그 아래 정정·취소 38px 회색 2열(radius 10 · 15/600 · `--muted` 면 · `--muted-fg` 글자) — 정정·취소는 미체결 행 선택 시에만 활성 · 「예약매수/예약매도」 라벨과 시간외종가 규칙 그대로 · 본문 <700 에서는 매수/매도 글자를 키우지 않는다 (D-11)"
    - "수동주문 가격이 호가 단위·상한가 위반이면 시트는 「가격 입력」을 잠그고 이유를 보이며, 인라인은 가격 상자 아래 12.5px `--destructive` 한 줄로 같은 문구를 보이되 주문 버튼은 잠그지 않는다(판정은 서버) · 시간외종가는 검증하지 않는다 (D-15)"
    - "조각 수 시트 칩은 1 · 3 · 5 · 10 이고 `maxPieces` 를 넘는 칩은 비활성 · 0 이면 「1회 이상 입력해 주세요」 · 초과면 「최대 {N}회까지 나눌 수 있어요」로 「조각 수 입력」이 잠긴다 · 가격 시트 칩 「현재가」「상한가」는 CardBody 가 내려 준 같은 카드의 현재가·상한가를 쓰고 0 이면 비활성 (D-17)"
    - "옛 셀렉터가 살아 있다 — 인라인 모드의 `#mo-price-{isin}` · `#mo-qty-{isin}` · `#mo-pieces-{isin}` 입력과 `<label>` 연결, `data-testid` manual-order-form · manual-order-buttons · manual-order-selchip · manual-order-validation · manual-order-locked · manual-order-result · manual-entry-options · manual-entry-form"
    - "값이 없으면 자리표시 「가격 입력」/「수량 입력」이 `--faint` · 주문금액 「0원」 · 정정/취소 비활성 + 각주 「정정·취소는 미체결 행을 선택하면 활성화돼요」 (UI Considerations E5 empty)"
    - "제출은 기존 `order-confirm-dialog` 잠금 흐름 그대로이고, 시세가 없으면 가격 상자는 비어 있고 힌트 「호가를 누르면 채워져요」가 보인다 (UI Considerations E5 loading)"
    - "서버 거부 표시는 기존 경로 그대로이고 가격 검증 위반은 상자 아래 12.5px `--destructive` 한 줄이다(주문 버튼 잠그지 않음) (UI Considerations E5 error)"
    - "조각 수는 예약구간 ∧ KRX 일 때만 · 주문유형(44px 행 · 「지정가 ›」 위에 투명 네이티브 select)은 호가 탭 variant 에만 · 시간외종가 선택 시 가격 상자 잠금 「—」 + 참고 종가 + 기존 안내 줄 · 예약구간 라벨 「예약매수/예약매도」 (UI Considerations E5 partial)"
    - "탭 전환은 로컬 상태 전환뿐이라 로딩이 없고 「수동주문」 버튼으로 폼이 덮여도 옵션 값이 보존된다 (UI Considerations E6 loading)"
    - "「매수」「매도」「수동」 탭은 2글자 고정 · 각 flex:1 · ≥700 에서는 세그먼트 자체가 없다 (UI Considerations E6 overflow)"
    - "탭 라벨은 2글자 고정이고 수동주문 키 문구는 min-width:0 흐름에서 한 줄 말줄임 + title 로 전체 키를 준다(UI-SPEC 레이아웃 계약 「키는 한 줄 말줄임」 채택 — E6 long-text 문장의 「줄바꿈 허용」과 다르게 읽힐 수 있어 기록) (UI Considerations E6 long-text)"
    - statement: "본문 344 의 42% | 1fr 패널에서 「예약매수」「예약매도」 48px 2열 버튼과 「정정」「취소」 38px 버튼 라벨이 잘리지 않는다 (UI Considerations E5 long-text · 20-02 스파이크 실측 + 20-07 Playwright)"
      verification: backstop
  artifacts:
    - path: webapp/src/components/trading/card/manual-order-form.tsx
      provides: "토스 상자(TicketBox) · 48/38 버튼 · 인라인 타이핑/↑↓ · 시트 입력(fill) · 가격 검증 줄 · 진입 탭/버튼 재스타일"
      exports: ["ManualOrderForm", "ManualOrderEntry", "formOrderLockOf", "modifyLockReason", "canModify"]
    - path: webapp/src/components/trading/__tests__/manual-order-form.test.tsx
      provides: "스테퍼 제거 이관 · 상자/버튼 기하 · 시트 입력 = 값만 채움 테스트"
  key_links:
    - from: webapp/src/components/trading/card/manual-order-form.tsx
      to: webapp/src/components/trading/lc/number-pad-sheet.tsx
      via: "상자 탭 → NumberPadSheet purpose='fill' → onConfirm = setPriceText/setQtyText/setPieceText"
      pattern: "purpose=\"fill\"|purpose: 'fill'"
    - from: webapp/src/components/trading/card/card-body.tsx
      to: webapp/src/components/trading/card/manual-order-form.tsx
      via: "currentPrice · upperLimit prop(같은 카드 시세)"
      pattern: "upperLimit=\\{upperLimit\\}"
  prohibitions:
    - statement: "시트의 「{필드명} 입력」 이나 인라인 Enter 가 주문을 보내지 않는다 — 주문은 매수/매도/정정/취소 버튼과 order-confirm-dialog 확정으로만 나간다 (D-10)"
      status: resolved
      verification: test
    - statement: "다른 종목·다른 카드의 가격으로 주문하지 않는다 — 종목 전환 리셋 이펙트와 CardBody 가격 선택 리셋을 바꾸지 않고 현재가·상한가는 같은 카드 시세에서만 온다 (T-15-40 · T-18-48)"
      status: resolved
      verification: test
    - statement: "수동주문에 ±호가 버튼 · 비율(%) 버튼 · 매수/매도 세그먼트 + 단일 CTA 를 만들지 않는다 (CONTEXT Deferred)"
      status: resolved
      verification: test
    - statement: "manual-order-form.tsx 에 shadcn 파랑 강조 토큰(--primary 계열)을 쓰지 않는다 — 매도 파랑과 값이 같다는 기존 LOCKED 규율(파일 머리 ② 5) 유지"
      status: resolved
      verification: test
---

<objective>
수동주문을 토스 스타일로 바꾸고 입력만 키패드 시트로 받는다(기능·진입·전송 경로 불변): 가격·수량·조각 수 상자(TicketBox) · 매수/매도 48px + 정정/취소 38px · 조각 −/+ 스테퍼 제거 · 인라인 타이핑의 전체 선택/↑↓ · 가격 검증 한 줄 · 「매수 | 매도 | 수동」 탭과 「수동주문」 버튼 재스타일, 그리고 터치 기기에서 상자 탭 → 20-03 의 같은 `NumberPadSheet`(purpose fill) → 값만 채움.

Purpose: D-08 · D-09 · D-10 · D-11 · D-15 · D-17(회 칩) · D-23(「{필드명} 입력」)과 UI Considerations E5 · E6 을 닫는다.
Output: `manual-order-form.tsx` 재스타일 + 시트 입력 · CardBody 가 현재가·상한가를 수동주문에 내림 · 테스트 이관.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-toss-order-ticket/20-CONTEXT.md
@.planning/phases/20-toss-order-ticket/20-UI-SPEC.md
@.planning/phases/20-toss-order-ticket/20-RESEARCH.md
@.planning/phases/20-toss-order-ticket/20-02-SUMMARY.md
@.planning/phases/20-toss-order-ticket/20-03-SUMMARY.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/sketches/002-toss-order-ticket/index.html
@webapp/src/components/trading/card/manual-order-form.tsx

작업 루트는 worktree `/Users/alex/repos/gh-radar/.claude/worktrees/toss-b`. 파일 머리 규율 ②(3중 일치 · 확인 다이얼로그 필수 · 제출 후 즉시 재활성 금지 · 결과 셋 · LOCKED 색 규칙)와 ③(catch 없음) · ④(77 은 `affordanceOf` 한 곳) · ⑤(wss 단일 경로)는 한 글자도 완화하지 않는다.

<interfaces>
```ts
// 기존 — webapp/src/components/trading/card/manual-order-form.tsx (유지 · 동작 불변)
export interface ManualOrderFormProps { variant: 'card'|'orderbook'; isin; code; name; accountNo; exchange; queuedWindow; status; selectedPrice?; referenceClose?; selectedUnfilled?; onClearSelection?; onSubmitted?; className? }
export function formOrderLockOf(...); export function modifyLockReason(...); export function canModify(...);
export function ManualOrderEntry({ options, form, keyLabel, onOptionsTab, className });
// 내부: priceText/qtyText/pieceText 상태 · affordanceOf(queuedWindow, exchange, orderType) → { buttonMode, showPieceInput, maxPieces, offHoursSelectable, confirmNote }
//       handleAction('buy'|'sell'|'modify'|'cancel') → setConfirm → OrderConfirmDialog → handleConfirmed → sendOrder

// 20-02 · 20-03 에서 온 것
stepValue(v, unit, dir) · priceIssueText(issue)                  // webapp/src/lib/numpad.ts
priceInputIssue(v, upper)                                        // @gh-radar/shared
useEditMode(): 'sheet' | 'inline'                                // webapp/src/lib/use-edit-mode.ts
NumberPadSheet({ open, title, description, unit, purpose: 'fill', initialValue, ctx: { current, upper, maxPieces }, returnFocusRef, onConfirm, onClose })

// 이 플랜이 더하는 props
interface ManualOrderFormProps { /* … */ currentPrice?: number; upperLimit?: number }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 수동주문 토스 스타일 — TicketBox · 48/38 버튼 · 스테퍼 제거 · 인라인 전체 선택/↑↓ · 가격 검증 줄 · 진입 탭/버튼 재스타일 (D-08 · D-09 · D-11 · D-14c · D-15)</name>
  <files>webapp/src/components/trading/card/manual-order-form.tsx, webapp/src/components/trading/__tests__/manual-order-form.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/card/manual-order-form.tsx (전체 — 머리 규율 · `Row` · `UnitBox` · `StepButton` · `OrderButton` · `SideLabel` · `ManualOrderEntry`)
    - webapp/src/components/trading/__tests__/manual-order-form.test.tsx (:157-195 빈 폼 · :278-320 77/조각 스테퍼 · :391-476 진입 · :477-620 선택/정정/취소)
    - .planning/sketches/002-toss-order-ticket/index.html (:151-167 `.box` · :204-222 `.mbtn` · `.tin` · `.btn2` · `.obtn` · `.fn` · :282-306 `ticket()`)
    - .planning/phases/20-toss-order-ticket/20-UI-SPEC.md (§8 수동주문 폼 표 · 입력 표 · §9 탭/버튼)
    - .planning/phases/20-toss-order-ticket/20-02-SUMMARY.md (`## 폭 스파이크` 의 수동주문 버튼 측정 ⒝ · ⒞)
  </read_first>
  <behavior>
    - 가격·수량 상자: 라벨(「가격」/「수량」)이 값 위 · 가격 머리 줄에 세로 막대 + 힌트 「호가를 누르면 채워져요」 · 상자 클래스 `rounded-[16px]` · `bg-[var(--muted)]` · 값 17/600 · 빈 값 자리표시 「가격 입력」/「수량 입력」
    - 인라인(기본) 모드: `getByLabelText('가격')` = `#mo-price-{isin}` 입력 · 포커스하면 전체 선택 · ↑ on 98,000 → 「98,100」 · ↓ on 2,000 → 「1,999」 · 수량 ↑ on 10 → 「11」 · Enter → blur
    - 조각 수(예약구간 ∧ KRX): 상자 라벨 「조각 수」 · 힌트 「예약구간 · KRX 만」 · 단위 「회」 · 초기 5 · 「조각 늘리기」「조각 줄이기」 버튼 없음 · ↑ 는 `maxPieces` 에서 멈춘다 · 조각 수가 요청에 실리는 규칙(보일 때만)은 그대로
    - (upperLimit 127400) 가격 98150 → `data-testid="manual-order-price-issue"` 「100원 단위로 입력해 주세요 · 가까운 값 98,100 / 98,200」 · 그래도 「매수」 는 활성이고 누르면 확인 다이얼로그가 열린다 · 시간외종가면 이 줄 없음
    - 버튼: `manual-order-buttons` 안에 매수·매도(`h-[48px]` · `rounded-[14px]`) 한 줄 + 정정·취소(`h-[38px]` · `rounded-[10px]`) 한 줄 · 매수가 매도보다 왼쪽 · 선택 없으면 정정·취소 disabled + 각주 「정정·취소는 미체결 행을 선택하면 활성화돼요」 · 선택하면 각주 사라짐
    - 주문금액 행: 「주문금액」 + 「{가격×수량}원」 · 시간외종가면 「종가 확정 후」
    - 호가 탭 variant: 주문유형 44px 행 「주문유형」 + 「지정가 ›」 · `getByLabelText('주문유형')` 이 여전히 `select` · 창 닫힘이면 시간외종가 option disabled + title
    - 진입(ManualOrderEntry): 3탭 버튼 `h-[34px]` · 15/600 · 트랙 `rounded-[12px]` · ≥700 「수동주문」 버튼 `h-8` 13/600 · 덮은 뒤 키 줄 `truncate` + `title={전체 키}` · ✕ 이름 「수동주문 닫기」
    - 기존 동작 테스트(확인 다이얼로그 · 중복 제출 가드 · timeout/rejected · 77 라벨 · 시간외종가 · 정정/취소 · 잠금 원천)는 그대로 통과
  </behavior>
  <action>
    `manual-order-form.tsx` 표현층만 바꾼다(핸들러 · `affordanceOf` · 잠금 · 결과 · 확인 다이얼로그 · 종목 전환 리셋은 무변경). ① `UnitBox`/`Row` 대신 파일 내부 `TicketBox` 컴포넌트를 만든다: props `{ label; hint?; unit; htmlFor?; locked?; children }` → `<div className="rounded-[16px] bg-[var(--muted)] pt-3 pr-3 pb-3 pl-3.5">` 머리 줄 13px(`<label htmlFor>` 라벨 600 `--fg-2` + 힌트가 있으면 세로 막대 `h-2.5 w-px bg-[var(--switch-off)]` + 힌트 12px `--muted-fg`) · 값 줄 `mt-1.5 flex items-center` 에 children(입력 또는 버튼) + 단위 17/600 `--fg` 왼쪽 4. 가격(시간외종가가 아닐 때) · 수량 · 조각 수(`aff.showPieceInput`)를 TicketBox 로 그린다. 입력 요소(`#mo-price-{isin}` 등 id·`inputMode="numeric"`·`autoComplete="off"`·`data-focus-ring="seamless"` 유지)는 17/600 `--fg` · 투명 · 테두리 없음 · 자리표시 `placeholder:text-[var(--faint)] placeholder:font-medium` · 폭은 `[field-sizing:content]` + `min-w-[1ch]`(미지원 브라우저는 입력 폭 폴백). `onFocus` 에서 `select()` + `window.setTimeout(() => el.select(), 0)` · `onClick` 에서 `select()`(D-14c). `onKeyDown`: `ArrowUp`/`ArrowDown` → `preventDefault` 후 가격은 `stepValue(price, '원', ±1)`, 수량은 `stepValue(qty, '주', ±1)`, 조각 수는 ±1 을 `clampPieces(…, aff.maxPieces)` 로 → 포맷해 상태에 넣는다 · `Enter` → `blur()`. `StepButton` 과 「조각 줄이기/늘리기」 버튼은 지운다(조각 입력은 인라인 타이핑·↑↓ / 20-06 T2 의 시트 칩). 시간외종가일 때 가격 상자는 탭·입력이 없는 TicketBox(값 「—」 `--muted-fg` · 힌트 자리에 「참고 종가 {종가}원」)이고 그 아래 12px `--muted-fg` 한 줄로 기존 `OFFHOURS_HINT`. ② 호가 탭 variant 주문유형: 44px 행(`relative flex min-h-[44px] items-center justify-between`) — 왼쪽 `<label htmlFor={`mo-type-${isin}`}>주문유형</label>`(14 `--muted-fg`) · 오른쪽 「지정가 ›」/「시간외종가 ›」(14/500 `--fg-2`) · 기존 `<select id aria-label="주문유형" title …>` 를 `absolute inset-0 opacity-0 cursor-pointer` 로 겹친다(옵션·disabled·title 규칙 그대로). ③ 주문금액 행: 44px · 「주문금액」 14 `--muted-fg` · 값 14/500 `--fg-2`(쉐브런 없음). ④ 가격 검증 줄: 시간외종가가 아니고 가격이 있으면 `priceInputIssue(price, upperLimit ?? 0)` → `priceIssueText` 를 가격 상자 아래 `<p data-testid="manual-order-price-issue" role="status" aria-live="polite" className="m-0 text-[12.5px] text-[var(--destructive)]">` 로 보인다. 주문 버튼은 이 줄 때문에 잠그지 않는다(D-15 · 판정은 서버). 새 prop `upperLimit?: number` · `currentPrice?: number`(T2 가 시트 칩에 쓴다)를 `ManualOrderFormProps` 에 더한다. ⑤ 버튼: `data-testid="manual-order-buttons"` 래퍼 안에 두 줄 — 1줄 `grid grid-cols-2 gap-2 mt-2.5`: `OrderButton` 매수(왼쪽) · 매도 — `h-[48px] rounded-[14px] font-semibold text-[var(--destructive-fg)]` · 매수 `bg-[var(--up)]` · 매도 `bg-[var(--down)]` · 비활성 `opacity-50` · 글자: 본문 <700 은 기존 13px · 줄높이 1.15 · 줄바꿈 허용(「예약」 윗줄, `SideLabel` 유지), ≥700 은 20-02 스파이크 ⒞ 가 한 줄로 들어간다고 기록했으면 `@min-[700px]/lc:text-[17px]`, 아니면 기존 ≥700 크기 유지(D-11 — 좁은 폭에서 키우지 않는다) · 2줄 `grid grid-cols-2 gap-2 mt-2`: 정정 · 취소 — `h-[38px] rounded-[10px] text-[15px] font-semibold bg-[var(--muted)] text-[var(--muted-fg)]` · 기존 disabled 조건·`title`(정정 잠금 사유) 그대로. 선택이 없을 때만 각주 `<p className="mt-2 text-center text-[12px] text-[var(--muted-fg)]">정정·취소는 미체결 행을 선택하면 활성화돼요</p>`. 기존 상태 문구(「주문 전송 중…」 · 잠금 문구 · 검증 · 결과 배너)는 문구·조건 그대로 두고 크기만 12px(오류 톤은 `--destructive`, 그 밖 `--muted-fg`). 원주문 칩: `--muted` · radius 12 · 13px · ✕ 히트 44(`min-h-11 min-w-11`) · 문구·동작 그대로. 폼 최대 폭 400 은 두지 않는다(UI-SPEC A8). 이 파일에 `--primary` 계열 토큰을 쓰지 않는다(머리 ② 5). ⑥ `ManualOrderEntry`: 3탭 트랙 `rounded-[12px] bg-[var(--muted)] p-[3px] gap-0.5` · 탭 버튼 `h-[34px] rounded-[10px] text-[15px] font-semibold`(선택 `--seg-on-*` · 비선택 `--muted-fg`) · `role="tab"`/`aria-selected`/tablist 이름 「주문 진입」 유지 · ≥700 「수동주문」 버튼 `h-8 rounded-[8px] px-3 text-[13px] font-semibold bg-[var(--muted)] text-[var(--fg-2)]` · 덮은 뒤 머리 줄 「수동주문 · 키 {keyLabel}」(12px `--muted-fg` · `truncate` · `title={keyLabel}`) + ✕ 32px(`aria-label="수동주문 닫기"`) · 컨테이너 쿼리 클래스(`@min-[700px]/lc:hidden` 등)와 두 pane 상시 마운트는 그대로. ⑦ `manual-order-form.test.tsx`: 조각 스테퍼 케이스(:278-312)를 「조각 수 상자 · 초기 5 · ↑ 는 maxPieces 에서 멈춤 · 보일 때만 요청에 실림」으로 이관하고, 빈 폼 케이스(:157-172)를 두 줄 버튼 기하로 고치고, `<behavior>` 의 새 단언(상자 · 전체 선택 · ↑↓ · 가격 검증 줄 · 각주 · 주문유형 행 · 진입 재스타일)을 더한다 — 기존 동작 테스트는 그대로 green 이어야 한다(개수 감소 금지).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/__tests__/manual-order-form.test.tsx src/lib/__tests__/queued-window.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed", or "manual-order-form.test.tsx" is absent from the output</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "function StepButton" webapp/src/components/trading/card/manual-order-form.tsx` == 0
    - `grep -c "function TicketBox" webapp/src/components/trading/card/manual-order-form.tsx` >= 1
    - `grep -c "h-\[38px\]" webapp/src/components/trading/card/manual-order-form.tsx` >= 1 · `grep -c "h-\[48px\]" webapp/src/components/trading/card/manual-order-form.tsx` >= 1
    - `grep -c "var(--primary" webapp/src/components/trading/card/manual-order-form.tsx` == 0
    - 플랜 시작 커밋을 `PLAN_BASE`(실행 시작 때 `git rev-parse HEAD`)로 기록해 두고 `git diff "$PLAN_BASE" -- webapp/src/components/trading/card/manual-order-form.tsx` 에서 `sendOrder(` · `setConfirm(` · `formOrderLockOf(` 를 담은 `+`/`-` 줄이 0개(주문 전송·확인·잠금 호출 줄 불변)
    - manual-order-form 테스트 수가 변경 전 이상 · 전부 통과 · typecheck 0
  </acceptance_criteria>
  <done>수동주문이 토스 상자·버튼으로 보이고 마우스 입력은 전체 선택·↑↓·검증 줄을 갖지만, 주문이 나가는 길(확인 다이얼로그 · 잠금 · 77 · 정정/취소 조건)은 그대로다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 수동주문 시트 입력(터치) — 상자 탭 → NumberPadSheet(fill) → 값만 채움 · CardBody 가 현재가·상한가를 내림 (D-10 · D-15 · D-17 · D-23)</name>
  <files>webapp/src/components/trading/card/manual-order-form.tsx, webapp/src/components/trading/card/card-body.tsx, webapp/src/components/trading/__tests__/manual-order-form.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/card/manual-order-form.tsx (Task 1 결과)
    - webapp/src/components/trading/card/card-body.tsx (:247-294 — `upperLimit` 산출 · `ManualOrderForm` props)
    - webapp/src/components/trading/lc/number-pad-sheet.tsx (20-03 — props · purpose fill)
    - webapp/src/lib/__tests__/match-media.ts (`mockPointer` · `restoreMatchMedia`)
    - .planning/phases/20-toss-order-ticket/20-UI-SPEC.md (§5 「수동주문 시트(D-10)」 · §8 입력 표 · Copywriting 시트 제목·설명 표 — 가격 · 수량 · 조각 수)
  </read_first>
  <behavior>
    - `mockPointer(true)`: `#mo-price-{isin}` 입력이 없고 가격 상자가 `button[aria-haspopup="dialog"]`(이름 「가격 98,100원」, 빈 값이면 「가격」)
    - 가격 상자 탭 → dialog 제목 「가격」 · 설명 「호가를 누르면 채워져요」 · 확정 「가격 입력」 · 칩 「−1호가」「+1호가」「현재가」「상한가」 · (currentPrice 98100 · upperLimit 127400) 「현재가」 → 「98,100」 → 「가격 입력」 → dialog 닫힘 · 상자 「98,100」 · `sendOrder` 0 · `order-confirm-dialog` 없음 · 포커스가 가격 상자로 복귀
    - 98150 입력 → 「가격 입력」 disabled + 「100원 단위로 입력해 주세요 · 가까운 값 98,100 / 98,200」
    - currentPrice 0 → 「현재가」 칩 disabled
    - 수량 상자 → 「수량 입력」 · 칩 「+100」「+1,000」「+10,000」「지우기」 · 「+1,000」 → 1,000 입력
    - 조각 수 상자(예약구간 ∧ KRX, maxPieces 5) → 「조각 수 입력」 · 설명 「나눠서 넣는 횟수예요 · 최대 5회」 · 칩 「10」 disabled · 0 → 「1회 이상 입력해 주세요」 잠금
    - 시간외종가면 가격 상자는 버튼이 아니다(탭해도 시트 없음)
    - 시트로 값을 채운 뒤 「매수」 → 기존 확인 다이얼로그 → 확정 시 `sendOrder` 1회(가격·수량 = 시트로 채운 값)
    - 호가 사다리 가격 선택(`selectedPrice`)은 시트 모드에서도 가격 상자를 채운다
    - CardBody: `ManualOrderForm` 이 `currentPrice`(quote.p 또는 0) · `upperLimit`(CardBody 산출값)을 받는다
  </behavior>
  <action>
    ① `manual-order-form.tsx`: `const mode = useEditMode();` · `const [sheetBox, setSheetBox] = useState<'price' | 'qty' | 'pieces' | null>(null)` · `const boxReturnRef = useRef<HTMLElement | null>(null)`. `mode === 'sheet'` 이면 가격(시간외종가 아닐 때)·수량·조각 수 TicketBox 의 값 자리가 `<input>` 대신 상자 전체를 덮는 `<button type="button" aria-haspopup="dialog" aria-label={값 ? `${라벨} ${값}${단위}` : 라벨}>`(값 17/600 · 자리표시 `--faint`)이고, 누르면 `boxReturnRef.current = el` 후 `setSheetBox(…)`. 폼에 `NumberPadSheet` 를 한 개 두고 `open={sheetBox !== null}` · `purpose="fill"` · `title` = 「가격」/「수량」/「조각 수」 · `description` = 「호가를 누르면 채워져요」/「주문 수량이에요」/`나눠서 넣는 횟수예요 · 최대 ${aff.maxPieces}회` · `unit` = '원'/'주'/'회' · `initialValue` = 현재 숫자값(비어 있으면 null) · `serverValue` 없음(「지금 ○○」 · 다른 단말 알림 · 감시 중 안내가 뜨지 않는다) · `ctx={{ current: currentPrice ?? 0, upper: upperLimit ?? 0, maxPieces: aff.maxPieces }}` · `returnFocusRef={boxReturnRef}` · `onConfirm(v)` → 가격이면 `setPriceText(KRW.format(v))`, 수량이면 `setQtyText(KRW.format(v))`, 조각 수면 `setPieceText(String(clampPieces(v, aff.maxPieces)))` 후 `setValidation(null)` + `setSheetBox(null)`(반영 중 단계 없음 · 전송 없음) · `onClose` → `setSheetBox(null)`. 시트 모드에서도 가격 검증 줄(Task 1 ④)은 보인다(사다리로 채운 값 등). 이 태스크는 `sendOrder`·`handleAction`·`handleConfirmed`·잠금 코드를 건드리지 않는다. ② `card-body.tsx`: `ManualOrderForm` 에 `currentPrice={quote !== null && quote.p > 0 ? quote.p : 0}` · `upperLimit={upperLimit}` 를 넘긴다(같은 카드의 단일 `isin`/`exchange` 시세 — T-18-48). 그 밖은 무변경. ③ `manual-order-form.test.tsx` 에 describe 「D-10 시트 입력(터치) — 값만 채운다」 를 더하고 `beforeEach(() => mockPointer(true))` · `afterEach(restoreMatchMedia)` 로 `<behavior>` 전부(10개 이상 `it`)를 단언한다. `sendOrder` 목이 시트 확정만으로는 0회임을 모든 시트 케이스에서 확인한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/__tests__/manual-order-form.test.tsx src/components/trading/__tests__/card-body.test.tsx src/components/trading/lc/__tests__/number-pad-sheet.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp run test</automated>
    <fails_when>non-zero exit, or output contains "error TS", or the "Test Files" summary line contains "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "useEditMode()" webapp/src/components/trading/card/manual-order-form.tsx` >= 1 · `grep -c "NumberPadSheet" webapp/src/components/trading/card/manual-order-form.tsx` >= 1
    - `grep -c "upperLimit={upperLimit}" webapp/src/components/trading/card/card-body.tsx` >= 1
    - 「D-10 시트 입력」 describe 10개 이상 통과 · 모든 시트 케이스에서 시트 확정 뒤 `sendOrder` 호출 0
    - webapp 전체 suite · typecheck green
  </acceptance_criteria>
  <done>터치 기기에서 수동주문 가격·수량·조각 수를 상따 설정과 같은 키패드로 채우고, 주문은 여전히 매수/매도 버튼과 확인 다이얼로그로만 나간다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 수동주문 입력(시트/인라인) → 확인 다이얼로그 → relay `sendOrder` | 입력은 값만 바꾼다. 주문은 확인 다이얼로그 확정 한 곳에서만 만들어진다 |
| CardBody 시세 → 수동주문 칩 | 같은 카드의 단일 `isin`/`exchange` 시세만 쓴다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-05 | Tampering | 다른 종목 가격으로 주문 | high | mitigate | 종목 전환 리셋(`[isin]` 이펙트) · CardBody 가격 선택 리셋 무변경 · 현재가/상한가는 같은 카드 `quote` 에서만(Task 2 ②) |
| T-20-09 | Tampering | 시트 「입력」이 주문 전송으로 오인·동작 | high | mitigate | `purpose="fill"` 은 값만 채우고 닫힘 · 전송 코드 무변경 · 테스트가 시트 확정 뒤 `sendOrder` 0 을 단언(Task 2) |
| T-20-10 | Tampering | 자동 보정 가격으로 주문 | high | mitigate | 시트는 위반 시 「가격 입력」 잠금 · 인라인은 경고 줄만(값 불변) — 판정은 서버(Task 1 ④ · Task 2) |
| T-20-15 | Tampering | 매수/매도 인접 오클릭 | high | mitigate | 3중 일치(색 · 왼쪽/오른쪽 · 글자) 유지 · 확인 다이얼로그(기본 포커스 취소)가 유일한 방어선이라는 기존 규율 불변(Task 1 ⑤) |
| T-20-SC | Tampering | 패키지 설치 | low | accept | 설치 없음 |
</threat_model>

<verification>
- `manual-order-form.test.tsx`(스테퍼 이관 + 새 스타일 + 시트 입력) · `card-body.test.tsx` · `number-pad-sheet.test.tsx` green.
- webapp 전체 suite · typecheck green · `sendOrder(` 호출 줄 diff 0.
</verification>

<success_criteria>
- 수동주문이 토스 상자·48/38 버튼으로 보이고, 터치는 키패드 시트로 · 마우스는 직접 타이핑으로 값을 넣지만 주문 경로는 그대로다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `packages/shared/src/krxTick.ts` — `krxTickSize` · `tickUp` · `tickDown` · `priceInputIssue` · `PriceIssue` | 순수 함수(shared) | 20-02 |
| `packages/shared/src/krxTick.test.ts` | 단위 테스트 | 20-02 |
| `webapp/src/lib/numpad.ts` — `PadUnit` · `PadState` · `padInit` · `padKey` · `PAD_CHIPS` · `PadChip` · `PadChipOp` · `padChipDisabled` · `applyPadChip` · `padValue` · `formatPadDisplay` · `padIssue` · `priceIssueText` · `canConfirmPad` · `stepValue` · `PAD_MAX_DIGITS` | 순수 함수 | 20-02 |
| `webapp/src/lib/__tests__/numpad.test.ts` | 단위 테스트 | 20-02 |
| `webapp/src/lib/use-edit-mode.ts` — `useEditMode` · `EditMode` · `COARSE_POINTER_QUERY` | 훅 | 20-03 |
| `webapp/src/lib/__tests__/use-edit-mode.test.ts` · `webapp/src/lib/__tests__/match-media.ts`(`mockPointer` · `emitPointerChange` · `restoreMatchMedia`) | 테스트 · 헬퍼 | 20-03 |
| `webapp/src/components/trading/lc/use-lc-field-commit.ts` — `useLcFieldCommit` · `LcFieldKey` · `LcCommitKind` · `LcFailReason` · `LcCommitFailure` · `LcCommitOutcome` · `UseLcFieldCommitOptions` · `LC_FLASH_MS` · `LC_COMMIT_TEXT` · `LC_GATE_FIELDS` | 상태 기계 훅 | 20-01 |
| `webapp/src/components/trading/lc/inline-value-editor.tsx` — `InlineValueEditor` | 컴포넌트 | 20-01 (확장 20-05) |
| `webapp/src/components/trading/lc/setting-group.tsx` — `SettingRow` · `FailureBubble` (20-01) · `SettingGroup` · `GroupSwitch` · `CheckValueRow` · `WatchTargetRow` · `DerivedRow` (20-04) | 컴포넌트 | 20-01 · 20-04 |
| `webapp/src/components/trading/lc/number-pad-sheet.tsx` — `NumberPadSheet` · `NumberPadSheetProps` | 컴포넌트(Radix Dialog · body 포털) | 20-03 |
| `webapp/src/components/trading/lc/lc-fields.ts` — `LC_BUY_GROUPS` · `LC_SELL_GROUPS` · `LcGroupSpec` · `LcRowSpec` · `LC_SWITCH_LABEL` · `lcRowById` · `lcNavigableRows` | 필드 스펙(단일 원천) | 20-04 |
| `limit-chaser-form.tsx` 신규 순수 함수 `canArmOf` · `armBlockOf` | 무장 판정 | 20-01 |
| `manual-order-form.tsx` 내부 `TicketBox` · 새 props `currentPrice` · `upperLimit` · 가격 검증 줄 `manual-order-price-issue` | 컴포넌트 | **20-06** |
| CSS 토큰 `--group-bg` · `--switch-off` · `--dim`(`:root` · `.dark`) | 토큰 | 20-03 |
| 테스트: `lc/__tests__/lc-tracer.test.tsx` · `use-lc-field-commit.test.tsx`(20-01) · `number-pad-sheet.test.tsx`(20-03) · `setting-group.test.tsx`(20-04) · `inline-value-editor.test.tsx` · `inline-navigation.test.tsx`(20-05) · `manual-order-form.test.tsx` 「D-10 시트 입력」 describe(**20-06**) | 단위/컴포넌트 | 각 플랜 |
| e2e: `P20-1`(20-01) · `P20-2`(20-03) · `P20-3`~`P20-5` · orderbook 11 대체 · a11y ③⑦ 대체(20-07) | Playwright | 각 플랜 |
| 제거: `order-panel.tsx` 의 `TICK_TABLE`/`tickFromTable`(20-02) · 더티 바 export/props · 옛 폼 조각(20-04) · 수동주문 `StepButton` · `UnitBox`(**20-06**) | 제거 | 20-02 · 20-04 · **20-06** |

## Planner assumptions (flagged for user visibility)

- **A-P5 (UI Considerations E6 error — dismissed):** 탭 전환은 서버 호출이 없어 실패 경로가 없다 — UI-SPEC 가 dismissed 로 판정했고 이 플랜도 실패 표시를 만들지 않는다.
- **A-P6:** ≥700 매수/매도 버튼 글자 17px 은 20-02 스파이크가 한 줄에 든다고 기록한 경우에만 적용하고, 아니면 기존 ≥700 크기를 유지한다(D-11 「좁은 폭에서 키우지 않는다」 + UI-SPEC 17/600 을 함께 만족).
- **A-P7:** 인라인 가격 검증 줄은 입력 중 매 타건 읽히지 않도록 `role="status"`(polite)로 둔다 — 시트 상태 줄의 `role="alert"` 와 다르다.

## Spec-less probe note

Spec-less probe fallback: **visible skip** — SPEC.md · 요구사항 ID 가 없어 probe 술어를 만들지 않았다. `must_haves` 는 CONTEXT D-08~D-11 · D-15 · D-17 · D-23 · Phase 18 D-19~D-23 · UI-SPEC §8 · §9 · UI Considerations E5/E6 에서 직접 썼다.

<output>
Create `.planning/phases/20-toss-order-ticket/20-06-SUMMARY.md` when done
</output>
