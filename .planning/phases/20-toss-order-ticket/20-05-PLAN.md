---
phase: 20-toss-order-ticket
plan: 05
type: execute
wave: 4
depends_on: ["20-04"]
files_modified:
  - webapp/src/components/trading/lc/inline-value-editor.tsx
  - webapp/src/components/trading/lc/__tests__/inline-value-editor.test.tsx
  - webapp/src/components/trading/limit-chaser-form.tsx
  - webapp/src/components/trading/lc/__tests__/inline-navigation.test.tsx
autonomous: true
requirements: []

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "인라인 키보드: Enter · Tab · 포커스 이탈 = 저장(즉시 반영) · Esc = 취소(서버 값 복귀 · 전송 0) · ↑/↓ = 가격(원)은 한 호가(+tick(v) / −tick(v−1)) · 그 밖(주 · 만원 · % · 건)은 1 · 하한 0 · 상한 999,999,999 (D-14)"
    - "Tab = 저장 후 같은 그룹의 다음 값 행을 편집(감시대상·값 없는 체크 행·기준선 행은 건너뛴다 · 그룹 끝이면 저장 후 종료) · Shift+Tab = 저장 후 같은 그룹 이전 값 행 (D-14 · UI-SPEC A5)"
    - "편집 중에도 행 높이는 44px 그대로이고 「Enter 저장 · Esc 취소」 같은 안내 문구·「저장」 버튼이 없다 — 검증 이유·실패 문구는 흐름 밖 말풍선으로만 뜬다 (D-14a)"
    - "편집 중 다른 값 행을 **한 번** 클릭하면 이전 값이 저장되고 그 행이 바로 편집 모드가 된다 — pointerdown(capture)에서 대상을 기록해 blur 저장 뒤 이어받는다 · 앞 행이 반영 중이어도 다음 행 편집은 열리고 확정 전송만 직렬화된다 (D-14b — UI-SPEC E4 loading 의 「다른 행 클릭 무시」는 LOCKED D-14b 로 대체)"
    - "편집기에 포커스가 가면 값 전체가 선택돼 첫 입력이 값을 덮어쓴다 (D-14c)"
    - "가격 행에서 Enter/Tab 을 눌렀는데 호가 단위·상한가 위반이면 저장하지 않고 말풍선(`role=alert`)에 UI-SPEC 문구(「{단위}원 단위로 입력해 주세요 · 가까운 값 {아래} / {위}」 · 「상한가 {상한가}원을 넘을 수 없어요」)를 보인다 · 위반 값을 둔 채 다른 곳을 누르면 Esc 처럼 취소한다(UI-SPEC A6) · 무장 불가 값도 같은 규칙 · 자동 보정 없음 (D-15)"
    - "빈 채로 저장(Enter/Tab/blur)하면 0 으로 반영되고(서버 계약상 빈 값 = 0) Esc 는 원값으로 돌아간다 (UI Considerations E4 empty)"
    - "저장 뒤 에코까지 그 행 입력칸은 `readOnly` · `aria-busy=true` · 값 opacity .6 · 링 유지이고 화면 문구 없이 sr-only live region 에 「반영 중…」 · 에코가 오면 편집 종료 + 900ms 강조 (UI Considerations E4 loading)"
    - "인라인 전송이 실패하면 편집 상태·입력값이 유지되고 링이 `--destructive` 가 되며 행 높이 변화 없이 말풍선 「반영하지 못했어요 · Enter 로 다시 시도해 주세요」(`role=alert`)가 뜬다 — 다음 Enter 가 재시도 (UI Considerations E4 error · D-06)"
    - "입력칸은 9ch · nowrap · 44px 불변이고 9자리를 넘는 입력은 받지 않는다 (UI Considerations E4 long-text)"
    - "다른 행으로 이미 옮긴 뒤 앞 행이 실패하면 앞 행에 `--destructive` 링 + 앵커 말풍선이 뜨되 포커스를 뺏지 않고, 그 행 값 글자는 서버 값 그대로(D-06)이며, 다시 누르면 실패했던 입력값으로 편집이 열린다 — 그 행의 다음 성공 또는 Esc 까지 유지되고 새로 연 행은 영향이 없다 [planner assumption A-P3 · RESEARCH Open Q3]"
  artifacts:
    - path: webapp/src/components/trading/lc/inline-value-editor.tsx
      provides: "↑↓ 스텝 · D-15/무장 검증 말풍선 · 반영 중 readOnly · Tab 내비게이션 콜백"
      exports: ["InlineValueEditor"]
    - path: webapp/src/components/trading/lc/__tests__/inline-value-editor.test.tsx
      provides: "편집기 단위 테스트"
    - path: webapp/src/components/trading/lc/__tests__/inline-navigation.test.tsx
      provides: "폼 수준 Tab · 한 번 클릭 전환 · 직렬화 · 실패 행 테스트"
  key_links:
    - from: webapp/src/components/trading/lc/inline-value-editor.tsx
      to: webapp/src/lib/numpad.ts
      via: "stepValue(v, unit, dir) · padIssue(원 검증)"
      pattern: "stepValue|padIssue"
    - from: webapp/src/components/trading/limit-chaser-form.tsx
      to: webapp/src/components/trading/lc/lc-fields.ts
      via: "onNavigate → lcNavigableRows(slot) 다음/이전"
      pattern: "lcNavigableRows"
  prohibitions:
    - statement: "인라인 편집에 「Enter 저장 · Esc 취소」 안내 문구나 「저장」 버튼을 두지 않는다 (D-14a)"
      status: resolved
      verification: test
    - statement: "호가 단위·상한가 위반 값을 인라인에서 자동 보정하지 않는다 — 저장을 거부하고 이유만 보인다 (D-15)"
      status: resolved
      verification: test
    - statement: "실패한 인라인 값을 자동으로 다시 보내지 않는다 — 재시도는 사용자의 다음 Enter 뿐이다 (T-16-10)"
      status: resolved
      verification: test
---

<objective>
데스크톱(마우스) 인라인 편집을 목업 수준으로 완성한다: 편집기(`InlineValueEditor`)에 ↑↓ 한 호가/1 스텝 · D-15 가격 검증 · 무장 불가 검증 · 반영 중 잠금 · 실패 말풍선을 붙이고, 폼에 Tab/Shift+Tab 같은 그룹 내 이동과 D-14b 「한 번 클릭 전환」, 그리고 다른 행으로 옮긴 뒤 도착한 실패의 표시를 더한다.

Purpose: D-14 · D-14a · D-14b · D-14c · D-15 와 UI Considerations E4(empty · loading · error · long-text)를 닫는다 — 20-04 가 모든 행을 인라인 편집 가능하게 만든 뒤의 키보드·포인터 정확성 슬라이스.
Output: 편집기 확장(+테스트) · 폼 내비게이션/전환/실패 행(+테스트).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-toss-order-ticket/20-CONTEXT.md
@.planning/phases/20-toss-order-ticket/20-UI-SPEC.md
@.planning/phases/20-toss-order-ticket/20-RESEARCH.md
@.planning/phases/20-toss-order-ticket/20-04-SUMMARY.md
@.planning/sketches/002-toss-order-ticket/index.html
@webapp/src/components/trading/lc/inline-value-editor.tsx
@webapp/src/components/trading/limit-chaser-form.tsx

작업 루트는 worktree `/Users/alex/repos/gh-radar/.claude/worktrees/toss-b`.

<interfaces>
```ts
// 20-01 · 20-04 에서 온 것
InlineValueEditor({ id, label, unit, initialValue, failureText?, busy?, onSave(v), onCancel() })  // 이 플랜이 확장
lcNavigableRows(slot): readonly { field; id }[]      // lc-fields.ts — 값 편집 가능한 행만
lcRowById(id): { group; row } | null
useLcFieldCommit(o) → { commit, inflightField, queuedFields, failures, flashField, successSeq, lastSuccessField, clearFailure }
FailureBubble({ open, text, children })               // setting-group.tsx
// 20-02 — webapp/src/lib/numpad.ts
stepValue(v, unit, dir: 1 | -1): number
padIssue({ buf, fresh }, unit, { current, upper, maxPieces? }): string | null

// 이 플랜이 더하는 편집기 props
interface InlineValueEditorProps {
  id: string; label: string; unit: PadUnit; initialValue: number;
  upperLimit?: number;                                  // 원 단위 D-15 검증
  validate?: (value: number) => string | null;          // 무장 불가 등
  failureText?: string | null; busy?: boolean;
  onSave: (value: number) => void; onCancel: () => void;
  onNavigate?: (dir: 'next' | 'prev') => void;          // Tab / Shift+Tab (저장 뒤 호출)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `InlineValueEditor` 확장 — ↑↓ 스텝 · D-15/무장 검증 말풍선 · 위반 blur 취소 · 반영 중 잠금 · 실패 재시도 · Tab 콜백 (D-14 · D-14a · D-14c · D-15)</name>
  <files>webapp/src/components/trading/lc/inline-value-editor.tsx, webapp/src/components/trading/lc/__tests__/inline-value-editor.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/lc/inline-value-editor.tsx (20-01 결과)
    - webapp/src/components/trading/lc/setting-group.tsx (`FailureBubble`)
    - webapp/src/lib/numpad.ts (`stepValue` · `padIssue` · `PAD_MAX_DIGITS`)
    - .planning/sketches/002-toss-order-ticket/index.html (:440-467 인라인 편집 · `step` · Tab)
    - .planning/phases/20-toss-order-ticket/20-UI-SPEC.md (§6 인라인 편집 표 · 가정 A5 · A6)
  </read_first>
  <behavior>
    - 마운트 즉시 입력 포커스 + 전체 선택(`selectionStart === 0` · `selectionEnd === 길이`)
    - (원) 127,400 에서 ↑ → 「127,500」 · 2,000 에서 ↓ → 「1,999」 · (주) 10,000 에서 ↑ → 「10,001」 · 0 에서 ↓ → 「0」 · 두 경우 모두 `preventDefault`(캐럿 이동 없음)
    - (원, upperLimit 127400) 「98150」 Enter → `onSave` 0회 · `role="alert"` 「100원 단위로 입력해 주세요 · 가까운 값 98,100 / 98,200」 · 입력 포커스 유지 · 「127500」 Enter → 「상한가 127,400원을 넘을 수 없어요」
    - `validate` 가 「매수주문 · 시세를 받지 못해 …」를 돌려주면 Enter → `onSave` 0회 + 그 문장
    - 위반 값 상태에서 blur → `onCancel` 1회 · `onSave` 0회(A6)
    - 빈 값 Enter → `onSave(0)`
    - `busy` → `readOnly` · `aria-busy="true"` · 값에 `opacity-60` · sr-only 「반영 중…」 · 타이핑 무시 · blur 해도 `onSave` 재호출 0
    - `failureText` 「반영하지 못했어요 · Enter 로 다시 시도해 주세요」 → alert 말풍선 · Enter → `onSave` 다시 호출(재시도)
    - Tab → `onSave(값)` 뒤 `onNavigate('next')` · Shift+Tab → `onSave` 뒤 `onNavigate('prev')` · 둘 다 `preventDefault` · 위반 값이면 Tab 도 저장·이동 없이 이유 표시
    - 10번째 숫자 입력은 무시(9자리) · Esc → `onCancel`
    - 편집기 DOM 에 「저장」 버튼 · 「Enter」「Esc」 안내 텍스트 0
  </behavior>
  <action>
    `inline-value-editor.tsx` 를 `<interfaces>` 의 props 로 확장한다(20-01 에서 `unit` 이 string 이었다면 `PadUnit` 으로 좁힌다). `onKeyDown`: `ArrowUp`/`ArrowDown` → `preventDefault` 후 `stepValue(현재값, unit, ±1)` 결과를 `ko-KR` 포맷으로 버퍼에 넣는다 · `Enter` → 검증 통과면 `onSave`, 아니면 이유 표시 · `Tab` → `preventDefault` 후 검증 통과면 `onSave` → `onNavigate(e.shiftKey ? 'prev' : 'next')`, 아니면 이유 표시 · `Escape` → `onCancel`. 검증 = `padIssue({ buf, fresh: false }, unit, { current: 0, upper: upperLimit ?? 0 })`(원 단위만 문구를 돌려준다) → 없으면 `validate?.(값)`. 검증 이유와 `failureText` 는 같은 `FailureBubble`(흐름 밖 · `role="alert"` · 12.5px `--destructive` · 입력 포커스 유지)로 보이고, 둘 다 있으면 검증 이유가 먼저다. blur: 이미 저장/취소했거나 `busy` 면 아무것도 하지 않고, 위반 값이면 `onCancel`(A6), 아니면 `onSave`. `busy` 면 `readOnly` · `aria-busy="true"` · 입력에 `opacity-60` · 형제 `<span className="sr-only" aria-live="polite">반영 중…</span>`(`LC_COMMIT_TEXT.busy`). 버퍼 입력은 숫자만 · 9자리(`PAD_MAX_DIGITS`) 초과 무시. 머리 주석에 D-14 · D-14a · D-14c · D-15 · A5 · A6 근거를 적고 「안내 문구·저장 버튼을 두지 않는다」를 명시한다. `lc/__tests__/inline-value-editor.test.tsx` 에 `<behavior>` 전부(16개 이상 `it`) — 편집기를 `SettingRow` 없이 단독 렌더한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/lc/__tests__/inline-value-editor.test.tsx src/components/trading/lc/__tests__/lc-tracer.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed", or "inline-value-editor.test.tsx" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `inline-value-editor.test.tsx` 16개 이상 통과
    - `grep -c "stepValue" webapp/src/components/trading/lc/inline-value-editor.tsx` >= 1 · `grep -c "onNavigate" webapp/src/components/trading/lc/inline-value-editor.tsx` >= 1
    - `grep -c "aria-busy" webapp/src/components/trading/lc/inline-value-editor.tsx` >= 1
    - 편집기 렌더 결과에 `button` 요소 0(테스트가 `container.querySelectorAll('button').length === 0` 단언)
  </acceptance_criteria>
  <done>인라인 편집기가 키보드만으로 목업과 같은 동작(스텝 · 검증 · 반영 중 · 실패 재시도 · Tab)을 하고, 행 높이와 무안내 규율을 지킨다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 폼 내비게이션 — Tab/Shift+Tab 같은 그룹 이동 · D-14b 한 번 클릭 전환 · 옮긴 뒤 도착한 실패 표시 (D-14 · D-14b)</name>
  <files>webapp/src/components/trading/limit-chaser-form.tsx, webapp/src/components/trading/lc/__tests__/inline-navigation.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/limit-chaser-form.tsx (20-04 결과 — 값 행 활성화 함수 · `editingField` · 편집기 배선)
    - webapp/src/components/trading/lc/lc-fields.ts (`lcNavigableRows` · `lcRowById`)
    - webapp/src/components/trading/lc/setting-group.tsx (`SettingRow` `failed` · `FailureBubble`)
    - webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx (하네스 — `echo()` · `props()` · `sentConfigs()`)
    - .planning/sketches/002-toss-order-ticket/index.html (:513-519 pointerdown 기록 · :450-457 finish 이어받기)
    - .planning/phases/20-toss-order-ticket/20-RESEARCH.md (§Q6 한 번 클릭 전환 React 패턴 · LOCKED 결정 간 긴장 해소 · A7)
  </read_first>
  <behavior>
    - 매수주문 그룹: 「비교가격」 편집 → Tab → 「잔량」 편집(감시대상 건너뜀) → Tab → 「체결」 값(buyMinTradeQty) 편집 → Tab → 편집 종료 · 값을 바꾼 행만 전송(바꾸지 않은 행 = 전송 0)
    - Shift+Tab: 「잔량」 → 「비교가격」 · 가격 섹션: 「매수가격」 Tab → 「주문금액」
    - 매수가격 편집 중 150000 입력 → 「주문금액」 행을 `userEvent.click` **한 번** → `lc.set` 1회(buyOrderPrice 150000) · `#lc-buy-order-amount` 포커스 + 전체 선택
    - 직렬화: 매수가격 Enter(in-flight) → 주문금액 편집 · 20 Enter → 전송 여전히 1회 → 매수가격 에코 + 답 증가 → 전송 2회
    - 옮긴 뒤 실패: 매수가격 150000 Tab(전송) → 주문금액 편집 중 거부(답만 증가) → 매수가격 행에 alert 「반영하지 못했어요 · Enter 로 다시 시도해 주세요」 · 행 값 글자는 「130,000원」 그대로 · `document.activeElement` 는 여전히 `#lc-buy-order-amount` · 매수가격 행을 다시 누르면 편집기 값 「150,000」 · Esc 하면 말풍선 사라짐
    - 앞 행 반영 중(busy)에 다른 행 클릭 → 그 행 편집이 열린다(무시하지 않는다) · 앞 행은 `aria-busy` 로 남는다
    - 매수가격 편집 중 ↑ → 「130,100」(130,000 은 100원 구간)
  </behavior>
  <action>
    `limit-chaser-form.tsx`: (1) 편집기 `onNavigate(dir)` → 편집 중 필드의 행 스펙에서 그룹 slot 을 찾고 `lcNavigableRows(slot)` 에서 다음/이전 필드를 골라 `setEditingField(그 필드 | null)` — 저장(commit)은 편집기가 먼저 부른다. (2) D-14b: 폼 루트에 `onPointerDownCapture` 를 달아 `editingField !== null` 이고 대상의 `closest('[data-lc-field]')` 가 값 행/값 버튼이며 그 필드가 편집 중 필드와 다르면 `nextEditRef.current = 그 필드` 로 기록한다(체크 버튼·감시대상·스위치는 기록하지 않는다 — 그 클릭은 제 동작을 한다). 편집기 blur 가 저장/취소를 마친 직후 `setEditingField(nextEditRef.current)` 로 이어받고 ref 를 비운다(기록이 없으면 편집 종료). 행 클릭의 `onActivate` 는 멱등이라 이중 실행이 무해하다. 앞 행이 반영 중이어도 다음 행 편집을 막지 않는다(확정만 상태 기계가 직렬화 — D-14b 가 UI-SPEC E4 loading 문장보다 우선). (3) 편집 중이 아닌 행에 `failures[field]`(값 종류)가 있으면 `SettingRow`/`CheckValueRow` 값 버튼에 `failed` 링을 켜고 `FailureBubble` 로 그 행에 앵커한 문구(거부·타임아웃 = `LC_COMMIT_TEXT.inlineFailed` · 끊김 = `LC_COMMIT_TEXT.disconnected` · 무장 불가 = 그 문장)를 **포커스를 뺏지 않고** 띄운다. 값 글자는 서버 값 그대로(D-06). 그 행을 다시 열면 편집기 `initialValue = failures[field].value`, 편집기 Esc(`onCancel`)는 `clearFailure(field)` 를 부른다. 행의 다음 성공은 상태 기계가 실패를 지운다. (4) 편집기에 `unit`(스펙) · `upperLimit`(원 단위만) · `validate`(= `armBlockOf` 로 그 필드를 바꾼 기준값 판정) · `busy`(그 필드가 in-flight/대기) · `failureText` 를 넘긴다. `lc/__tests__/inline-navigation.test.tsx` 신설 — `limit-chaser-form.test.tsx` 하네스를 복제해 `<behavior>` 전부(12개 이상 `it`, `userEvent` 로 실제 포인터 순서 재현).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/lc/__tests__/inline-navigation.test.tsx src/components/trading/lc/__tests__/inline-value-editor.test.tsx src/components/trading/__tests__/limit-chaser-form.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed", or "inline-navigation.test.tsx" is absent from the output</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp run test</automated>
    <fails_when>non-zero exit, or output contains "error TS", or the "Test Files" summary line contains "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `inline-navigation.test.tsx` 12개 이상 통과 · 「한 번 클릭」 케이스가 `userEvent.click` 1회로 저장과 다음 행 편집을 함께 단언한다
    - `grep -c "onPointerDownCapture" webapp/src/components/trading/limit-chaser-form.tsx` >= 1 · `grep -c "lcNavigableRows" webapp/src/components/trading/limit-chaser-form.tsx` >= 1
    - webapp 전체 suite · typecheck green
  </acceptance_criteria>
  <done>마우스·키보드 사용자가 한 그룹 안을 Tab 으로 훑으며 값을 바꾸고, 다른 행을 한 번 눌러 바로 옮겨 가며, 뒤늦게 온 실패도 제자리에서 보인다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 키보드/포인터 → 인라인 저장 → relay `lc.set` | Enter · Tab · blur 가 전송을 일으킨다 — 저장 의도가 분명한 동작만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering(자기 자산) | blur 저장이 의도치 않은 전송이 됨 | high | mitigate | 위반 값 blur = 취소(A6) · 같은 값 = 전송 0 · Esc = 취소 · busy 중 blur 재저장 0(Task 1) |
| T-20-10 | Tampering | ↑↓ 나 검증이 가격을 몰래 고침 | high | mitigate | ↑↓ 는 사용자 키 입력으로만 · 검증은 저장 거부 + 이유만(Task 1) |
| T-20-02 | Tampering | 실패 뒤 재전송 | high | mitigate | 재시도 = 다음 Enter 뿐 · 대기 폐기는 상태 기계(20-01)(Task 2 가 실패를 표시만) |
| T-20-14 | Repudiation | 옮긴 뒤 실패가 조용히 사라짐 | medium | mitigate | 앞 행 링 + 앵커 말풍선이 다음 성공/Esc 까지 유지(Task 2) |
| T-20-SC | Tampering | 패키지 설치 | low | accept | 설치 없음 |
</threat_model>

<verification>
- `inline-value-editor.test.tsx` 16+ · `inline-navigation.test.tsx` 12+ · `limit-chaser-form.test.tsx` green.
- webapp 전체 suite · typecheck green.
</verification>

<success_criteria>
- 데스크톱에서 Tab/Shift+Tab · ↑↓ · Enter/Esc · 한 번 클릭 전환이 목업대로 동작하고 가격 위반·실패가 행 높이를 바꾸지 않고 드러난다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `packages/shared/src/krxTick.ts` — `krxTickSize` · `tickUp` · `tickDown` · `priceInputIssue` · `PriceIssue` | 순수 함수(shared) | 20-02 |
| `packages/shared/src/krxTick.test.ts` | 단위 테스트 | 20-02 |
| `webapp/src/lib/numpad.ts` — `PadUnit` · `PadState` · `padInit` · `padKey` · `PAD_CHIPS` · `PadChip` · `PadChipOp` · `padChipDisabled` · `applyPadChip` · `padValue` · `formatPadDisplay` · `padIssue` · `priceIssueText` · `canConfirmPad` · `stepValue` · `PAD_MAX_DIGITS` | 순수 함수 | 20-02 |
| `webapp/src/lib/__tests__/numpad.test.ts` | 단위 테스트 | 20-02 |
| `webapp/src/lib/use-edit-mode.ts` — `useEditMode` · `EditMode` · `COARSE_POINTER_QUERY` | 훅 | 20-03 |
| `webapp/src/lib/__tests__/use-edit-mode.test.ts` · `webapp/src/lib/__tests__/match-media.ts`(`mockPointer` · `emitPointerChange` · `restoreMatchMedia`) | 테스트 · 헬퍼 | 20-03 |
| `webapp/src/components/trading/lc/use-lc-field-commit.ts` — `useLcFieldCommit` · `LcFieldKey` · `LcCommitKind` · `LcFailReason` · `LcCommitFailure` · `LcCommitOutcome` · `UseLcFieldCommitOptions` · `LC_FLASH_MS` · `LC_COMMIT_TEXT` · `LC_GATE_FIELDS` | 상태 기계 훅 | 20-01 |
| `webapp/src/components/trading/lc/inline-value-editor.tsx` — `InlineValueEditor` | 컴포넌트 | 20-01 (확장 **20-05**) |
| `webapp/src/components/trading/lc/setting-group.tsx` — `SettingRow` · `FailureBubble` (20-01) · `SettingGroup` · `GroupSwitch` · `CheckValueRow` · `WatchTargetRow` · `DerivedRow` (20-04) | 컴포넌트 | 20-01 · 20-04 |
| `webapp/src/components/trading/lc/number-pad-sheet.tsx` — `NumberPadSheet` · `NumberPadSheetProps` | 컴포넌트(Radix Dialog · body 포털) | 20-03 |
| `webapp/src/components/trading/lc/lc-fields.ts` — `LC_BUY_GROUPS` · `LC_SELL_GROUPS` · `LcGroupSpec` · `LcRowSpec` · `LC_SWITCH_LABEL` · `lcRowById` · `lcNavigableRows` | 필드 스펙(단일 원천) | 20-04 |
| `limit-chaser-form.tsx` 신규 순수 함수 `canArmOf` · `armBlockOf` | 무장 판정 | 20-01 |
| `manual-order-form.tsx` 내부 `TicketBox` · 새 props `currentPrice` · `upperLimit` | 컴포넌트 | 20-06 |
| CSS 토큰 `--group-bg` · `--switch-off` · `--dim`(`:root` · `.dark`) | 토큰 | 20-03 |
| 테스트: `lc/__tests__/lc-tracer.test.tsx` · `use-lc-field-commit.test.tsx`(20-01) · `number-pad-sheet.test.tsx`(20-03) · `setting-group.test.tsx`(20-04) · `inline-value-editor.test.tsx` · `inline-navigation.test.tsx`(**20-05**) | 단위/컴포넌트 | 각 플랜 |
| e2e: `P20-1`(20-01) · `P20-2`(20-03) · `P20-3`~`P20-5` · orderbook 11 대체 · a11y ③⑦ 대체(20-07) | Playwright | 각 플랜 |
| 제거: `order-panel.tsx` 의 `TICK_TABLE`/`tickFromTable`(20-02) · 더티 바 안내·조립 export 와 폼 더티 props · 옛 폼 조각(20-04) | 제거 | 20-02 · 20-04 |

## Planner assumptions (flagged for user visibility)

- **A-P3 (RESEARCH Open Q3):** 다른 행으로 옮긴 뒤 앞 행이 실패하면, 앞 행은 서버 값 글자를 유지(D-06)하면서 `--destructive` 링 + 앵커 말풍선으로 오류 문구를 그 자리에 보이고(포커스 비탈취), 다시 누르면 실패한 입력값으로 편집이 열린다. 다음 성공 또는 Esc 까지 유지.

## Spec-less probe note

Spec-less probe fallback: **visible skip** — SPEC.md · 요구사항 ID 가 없어 probe 술어를 만들지 않았다. `must_haves` 는 CONTEXT D-14/D-14a/D-14b/D-14c/D-15 · UI-SPEC §6 · UI Considerations E4 에서 직접 썼다.

<output>
Create `.planning/phases/20-toss-order-ticket/20-05-SUMMARY.md` when done
</output>
