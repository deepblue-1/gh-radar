---
phase: 20-toss-order-ticket
plan: 04
type: execute
wave: 3
depends_on: ["20-01", "20-02", "20-03"]
files_modified:
  - webapp/src/components/trading/lc/lc-fields.ts
  - webapp/src/components/trading/lc/setting-group.tsx
  - webapp/src/components/trading/lc/__tests__/setting-group.test.tsx
  - webapp/src/components/trading/limit-chaser-form.tsx
  - webapp/src/components/trading/card/card-body.tsx
  - webapp/src/components/trading/__tests__/card-body.test.tsx
  - webapp/src/components/trading/__tests__/strategy-card-flow.test.tsx
  - webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx
autonomous: true
requirements: []

estimate:
  tokens: 190000
  raw_tokens: 190000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "상따 옵션 필드는 전부 「라벨 ─ 값+단위 ›」 44px 리스트 행이고, 그룹은 둥근 면(`--group-bg` · radius 16 · 패딩 10 10 4 · 그룹 사이 10) + 제목줄(제목 15/600 + 상태 12) + 오른쪽 끝 스위치다 · 상태 「감시 중」은 `--led-armed` · 꺼진 매수주문·한방체결·매도주문 그룹의 행은 opacity .45 이지만 여전히 편집할 수 있다 (D-01)"
    - "매수 쪽 그룹 순서 = [매수가격 · 주문금액](제목·스위치 없음 · 이름 「매수 가격 설정」) → 매수주문(비교가격 · 감시대상 · 잔량 · 체결) → 한방체결(호가변경 · 한방가격) · 매도 쪽 = [매도가격 · 매도비율](이름 「매도 가격 설정」) → 매도주문(비교가격 · 호가잔량 · 잔량추적 · 체결 · 조건부 잔량추적 기준선) → 매수취소(취소잔량 · 체결 · 잔량추적) — 매수취소는 매도 쪽 맨 아래다 (D-19)"
    - "매수취소 그룹 스위치는 `cancelQtyEnabled`(접근성 이름 「매수취소 켜기」 · id `lc-cancel-qty`) — 꺼져 있어도 체결·잔량추적 체크를 켤 수 있고 이 그룹 행은 흐리지 않으며 「취소잔량」은 값(주)만 있는 일반 행이다 · 무장 판정은 `lib/limit-chaser.ts` 그대로 (D-21)"
    - "체크 달린 필드는 「○ 라벨 ─ 값 ›」 행 — 왼쪽 20px 원형 체크(`button role=checkbox aria-checked`, 누르면 즉시 반영) + 오른쪽 값 버튼(누르면 시트/인라인) · 매수취소 체결·잔량추적은 체크 + 라벨만 · 각 그룹 끝에 둔다 (D-22)"
    - "감시대상은 시트를 열지 않는 행 안 토글(`role=group` 「감시대상」 + `aria-pressed` 두 버튼 「매도잔량」「매수잔량」 · 누르면 즉시 반영) — 본문 <700 은 라벨 없이 행 전체 폭 세그먼트(버튼 32px), ≥700 은 왼쪽 「감시대상」 라벨 + 오른쪽 토글(버튼 26px) · 선택은 중립 `--seg-on-*` (D-02)"
    - "레이아웃 밴드는 그대로다 — 본문 <700 은 호가 42% | 패널에서 매수/매도 한쪽씩(카드 본문 3탭 · 비활성 pane 은 CSS 숨김이고 DOM 에 남는다), ≥700 은 매수 | 매도 두 열 나란히 + 열 머리 「● 매수」「● 매도」(15/700 · 점 8px `--up`/`--down`) · 새 배치는 `@min-[700px]/lc:` 만 쓴다 (D-03)"
    - "모든 리스트 행 — 값 행 · 체크 행 · 감시대상 행 · 기준선 행 · 인라인 편집 중 행 — 이 44px 이다 (D-20)"
    - "상따 설정에서 더티 누적과 하단 「수정/되돌리기」 액션 바가 사라졌다 — 어떤 편집 뒤에도 문서에 dirty-action-bar 슬롯이 없고 카드 더티 테두리도 없다 · 스위치·체크·감시대상·값 확정이 모두 필드 1개 = 전송 1회다 · 서버 프로토콜은 그대로 전략 전체 전송 (D-04)"
    - "스위치 4개는 확인 없이 즉시 전송된다 · 켜는 방향만 무장 판정으로 막고 끄기는 언제나 허용 · 시각 40×24 · 히트 44×44 · 그룹 제목줄 오른쪽 끝 고정 (Phase 16 D-05 유지)"
    - "모든 값 행은 입력 장치에 따라 시트(터치) 또는 인라인(마우스)으로 편집되고, 시트의 「감시 중 — 적용하면 바로 반영돼요」 안내는 그 필드가 속한 그룹 상태가 「감시 중」일 때만 보인다 (D-05 · D-12)"
    - "스위치·체크·감시대상 전송이 연결 끊김으로 못 나가면 폼 맨 위 `lc-submit-error`(`role=alert`)에 기존 문장 「연결이 끊겨 켜기/끄기를 보내지 못했어요. 연결이 복구된 뒤 다시 눌러 주세요.」 · 거부·무응답이면 그 컨트롤이 서버 값으로 되돌고 그 자리 말풍선 「반영하지 못했어요」(행 높이 불변 · 자동 재시도 없음) (UI Considerations E2 error)"
    - "감시대상 토글을 누르면 에코까지 토글이 비활성(선택 면은 낙관 표시)이고 3초 무응답이면 오류로 간다 (UI Considerations E2 loading)"
    - "감시대상 라벨 「매도잔량」「매수잔량」은 4글자 고정 nowrap 이고 폰에서는 flex:1 이라 넘칠 여지가 없다 (UI Considerations E2 long-text)"
    - "신규 전략(server 없음)은 모든 필드 행을 기본값으로 그리고 가격 5칸은 상한가로 1회 시딩되며 값 0 은 「0원」처럼 그대로 보인다 (UI Considerations E1 empty)"
    - "세션이 ready 가 아니면 모든 행·스위치·체크·토글이 비활성(opacity .5)이고 누를 수 없다 (UI Considerations E1 loading)"
    - "리스트 자체에는 로드 실패 상태가 없다 — 값은 relay 스냅샷/에코에서 오고 연결 단절은 상태줄 DMA 필(기존)이 말하며 전송 실패는 행·시트·말풍선이 말한다 (UI Considerations E1 error)"
    - "정상 상태의 목록은 채택 목업 8차 구성 그대로 매수 3그룹 · 매도 3그룹이다(스키마 고정 — 전형 볼륨이 곧 유일한 볼륨) (UI Considerations E1 populated)"
    - "매도주문 「잔량추적 기준선」은 서버가 매도 진입을 래치했을 때(`sellEntryLatched`)만 44px 읽기 전용 행으로 보이고 · 꺼진 그룹의 행은 opacity .45 이되 편집 가능 (UI Considerations E1 partial)"
    - "그룹 수(3/3)와 행 수는 고정 스키마라 0/1/many 가 없고 유일한 가변은 조건부 기준선 행이다 (UI Considerations E1 zero-one-many)"
    - "20-02 폭 스파이크가 정한 밴드별 백스톱 레벨(① 행 좌우 패딩 4→0 ② 라벨–값 간격 6→4 ③ 폰 밴드 쉐브런 숨김)이 필요한 만큼만 적용됐다 — 글자 크기는 줄이지도 키우지도 않았다 (UI-SPEC overflow backstop)"
    - statement: "「발주 완료 · 무장 해제」 같은 긴 상태 문구가 폰 밴드에서 제목 옆 흐름의 둘째 줄로 내려가고 말줄임되지 않는다 — 헤더만 커지고 행 높이는 불변 (UI Considerations E1 long-text)"
      verification: backstop
  artifacts:
    - path: webapp/src/components/trading/lc/lc-fields.ts
      provides: "그룹·행 순서 · 라벨 · 단위 · 시트 설명 · 옛 id · 게이트 매핑의 단일 원천(D-19 · D-21 · D-22)"
      exports: ["LC_BUY_GROUPS", "LC_SELL_GROUPS", "LcGroupSpec", "LcRowSpec", "LC_SWITCH_LABEL", "lcRowById", "lcNavigableRows"]
    - path: webapp/src/components/trading/lc/setting-group.tsx
      provides: "SettingGroup · GroupSwitch · CheckValueRow · WatchTargetRow · DerivedRow (+ 20-01 SettingRow · FailureBubble)"
      exports: ["SettingGroup", "GroupSwitch", "CheckValueRow", "WatchTargetRow", "DerivedRow", "SettingRow", "FailureBubble"]
    - path: webapp/src/components/trading/lc/__tests__/setting-group.test.tsx
      provides: "그룹·행 조각 컴포넌트 테스트"
    - path: webapp/src/components/trading/limit-chaser-form.tsx
      provides: "필드 스펙으로 조립한 토스식 리스트 · 즉시 반영 · 더티 모델 제거"
  key_links:
    - from: webapp/src/components/trading/limit-chaser-form.tsx
      to: webapp/src/components/trading/lc/lc-fields.ts
      via: "LC_BUY_GROUPS / LC_SELL_GROUPS 로 렌더 · 시트 제목·설명 · 옛 id"
      pattern: "LC_BUY_GROUPS"
    - from: webapp/src/components/trading/limit-chaser-form.tsx
      to: webapp/src/components/trading/lc/setting-group.tsx
      via: "GroupSwitch onCheckedChange → commit(gate, v, 'toggle')"
      pattern: "GroupSwitch"
    - from: webapp/src/components/trading/card/card-body.tsx
      to: webapp/src/components/trading/limit-chaser-form.tsx
      via: "더티 prop 3개 제거 · unacked · currentPrice 유지"
      pattern: "LimitChaserForm"
  prohibitions:
    - statement: "§2.2b 밴드 경계(700 · 830 · 992 · wb 680) · container-type 선언 위치 · 3단 호가표 400px 하한을 바꾸지 않는다 — 새 컨테이너 경계 숫자 0"
      status: resolved
      verification: test
    - statement: "뷰포트 브레이크포인트 유틸(sm/md/lg/xl 접두)이나 뷰포트 미디어 쿼리를 새로 쓰지 않는다 — 감시대상 두 모양도 @min-[700px]/lc: 로만 가른다"
      status: resolved
      verification: test
    - statement: "행 라벨·값에 말줄임(ellipsis/truncate)을 쓰지 않는다 — 잘린 라벨은 오발주다 · 안 맞으면 보이게 넘치고 테스트가 잡는다"
      status: resolved
      verification: test
    - statement: "워크벤치 더티 배관(strategy-card.tsx · trading-workbench.tsx · shared-panels.tsx · dirty-action-bar.tsx)은 고치지 않는다 — 폼이 더티 수를 보내지 않아 0 으로 비활성된다 (RESEARCH Pitfall 10)"
      status: resolved
      verification: test
    - statement: "그룹 스위치를 켜거나 끌 때 확인 다이얼로그를 띄우지 않는다 (Phase 16 D-05 · D-05)"
      status: resolved
      verification: test
---

<objective>
상따 설정 전체를 토스식 리스트로 전환한다: 필드 스펙 한 벌(`lc-fields.ts`)로 D-19 순서의 매수 3그룹 · 매도 3그룹을 그리고, 그룹 스위치 4개(매수취소 = `cancelQtyEnabled`)·체크 값 행·감시대상 행·기준선 행을 새 조각으로 만들고, 모든 확정을 20-01 상태 기계로 즉시 반영한다. 기존 더티 누적 + 하단 「수정/되돌리기」 액션 바는 상따 설정에서 폐기한다(D-04).

Purpose: D-01 · D-02 · D-03 · D-04 · D-19 · D-20 · D-21 · D-22 를 한 번에 착지시킨다 — 트레이서(20-01)와 시트(20-03)가 증명한 경로를 모든 필드로 넓히는 확장 슬라이스.
Output: `lc-fields.ts` · 그룹/행 조각(+테스트) · 폼 재조립 · CardBody 더티 prop 제거 · 폼/카드 테스트 이관.

주의: 이 플랜은 옛 입력 id(`#lc-buy-watch-qty` 등)를 인라인 편집 중에만 존재하게 만들고 게이트를 `role=switch` 로 바꾼다 — `trading-workbench` · `orderbook` · `a11y` · `sidebar-tree` e2e 의 옛 단언이 여기서부터 20-07 전까지 깨진다(계획된 일시 상태 · 20-07 T1/T2 가 이관). P20-1 · P20-2 는 새 셀렉터라 계속 green 이어야 한다.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-toss-order-ticket/20-CONTEXT.md
@.planning/phases/20-toss-order-ticket/20-UI-SPEC.md
@.planning/phases/20-toss-order-ticket/20-RESEARCH.md
@.planning/phases/20-toss-order-ticket/20-PATTERNS.md
@.planning/phases/20-toss-order-ticket/20-01-SUMMARY.md
@.planning/phases/20-toss-order-ticket/20-02-SUMMARY.md
@.planning/phases/20-toss-order-ticket/20-03-SUMMARY.md
@.planning/sketches/002-toss-order-ticket/index.html
@.planning/sketches/002-toss-order-ticket/watch-target-variants.html
@webapp/src/components/trading/limit-chaser-form.tsx
@webapp/src/components/trading/card/card-body.tsx

작업 루트는 worktree `/Users/alex/repos/gh-radar/.claude/worktrees/toss-b`. §2.2b 밴드 표는 `webapp/src/styles/globals.css` 상단 주석이 정본이다 — 표를 복사하지 않는다.

<interfaces>
```ts
// 20-01 · 20-03 이 만든 것 (그대로 소비)
useLcFieldCommit(o) → { commit(field, value, kind): 'noop'|'local'|'sent'|'queued'|'blocked'|'disconnected'; inflightField; queuedFields; failures; flashField; successSeq; lastSuccessField; clearFailure }
LC_COMMIT_TEXT · LC_GATE_FIELDS · LC_FLASH_MS                      // use-lc-field-commit.ts
SettingRow · FailureBubble                                          // setting-group.tsx
InlineValueEditor                                                   // inline-value-editor.tsx
NumberPadSheet({ open, title, description, unit, purpose, initialValue, serverValue, ctx, status, failureText, armedNotice, validate, returnFocusRef, onConfirm, onClose })
useEditMode(): 'sheet' | 'inline'
// limit-chaser-form.tsx (20-01): canArmOf(values) · armBlockOf(values) · buildCfg · gateBlocked · armBlockedGroupsOf · ArmBlockedPanel · GATE_LABEL
// card-body.tsx: cardGroupStatusOf(server, fired) → { buy, sweep, sell, cancel } 상태 문구

// 이 플랜이 만드는 계약 — webapp/src/components/trading/lc/lc-fields.ts
type NumField = 'buyOrderPrice'|'buyOrderAmount'|'buyWatchPrice'|'buyWatchQty'|'buyMinTradeQty'|'sweepMinTickCount'|'sweepWatchPrice'
  |'sellOrderPrice'|'sellOrderRatio'|'sellWatchPrice'|'sellWatchQty'|'sellQtyTrackRatio'|'sellMinTradeQty'|'cancelWatchQty';
type BoolField = 'buyTradeQtyEnabled'|'sellQtyTrackEnabled'|'sellTradeQtyEnabled'|'cancelTradeEnabled'|'cancelQtyTrackEnabled';
export type LcRowSpec =
  | { kind: 'value'; field: NumField; id: string; label: string; unit: PadUnit; desc: string }
  | { kind: 'checkValue'; check: BoolField; checkId: string; field: NumField; id: string; label: string; unit: PadUnit; desc: string }
  | { kind: 'check'; check: BoolField; checkId: string; label: string }
  | { kind: 'watch' }
  | { kind: 'derived'; label: '잔량추적 기준선' };
export interface LcGroupSpec {
  slot: 'buy-price'|'buy'|'sweep'|'sell-price'|'sell'|'cancel';          // data-slot="lc-group-{slot}" 유지
  title?: '매수주문'|'한방체결'|'매도주문'|'매수취소'; ariaLabel?: string; hint?: string;
  gate?: 'buyEnabled'|'sweepEnabled'|'sellEnabled'|'cancelQtyEnabled'; statusKey?: 'buy'|'sweep'|'sell'|'cancel';
  dimWhenOff: boolean; rows: readonly LcRowSpec[];
}
export const LC_BUY_GROUPS: readonly LcGroupSpec[]; export const LC_SELL_GROUPS: readonly LcGroupSpec[];
export const LC_SWITCH_LABEL: Record<NonNullable<LcGroupSpec['gate']>, string>;
export function lcRowById(id: string): { group: LcGroupSpec; row: LcRowSpec } | null;
export function lcNavigableRows(slot: LcGroupSpec['slot']): readonly { field: NumField; id: string }[]; // 값 편집 가능한 행만(감시대상·체크 전용·기준선 제외)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `lc-fields.ts` 필드 스펙(단일 원천) + 그룹/행 조각 — SettingGroup · GroupSwitch · CheckValueRow · WatchTargetRow · DerivedRow (D-01 · D-02 · D-19 · D-20 · D-21 · D-22)</name>
  <files>webapp/src/components/trading/lc/lc-fields.ts, webapp/src/components/trading/lc/setting-group.tsx, webapp/src/components/trading/lc/__tests__/setting-group.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/lc/setting-group.tsx (20-01 의 `SettingRow` · `FailureBubble`)
    - webapp/src/components/trading/limit-chaser-form.tsx (:686-1030 현재 필드·옛 id·라벨 나열 · :1269-1334 `Group` 슬롯 유니온 · :1336-1356 `GateSwitch`)
    - webapp/src/components/ui/switch.tsx (thumb 기하가 하드코딩 — 이 파일은 쓰지 않고 primitive 를 직접 쓴다, RESEARCH Pitfall 9)
    - .planning/sketches/002-toss-order-ticket/index.html (:113-150 `.grp` · `.grp-h` · `.sw` · `.lrow` · `.lrow.watch` · `.tog`)
    - .planning/sketches/002-toss-order-ticket/watch-target-variants.html (폰 B · ≥700 라벨+토글 채택안)
    - .planning/phases/20-toss-order-ticket/20-UI-SPEC.md (§1 그룹 카드 · §2 값 행 · §3 체크 값 행 · §4 감시대상 행 · Copywriting 시트 제목·설명 표 · 접근성 계약)
  </read_first>
  <behavior>
    - `LC_BUY_GROUPS` 슬롯 순서 = ['buy-price', 'buy', 'sweep'] · `LC_SELL_GROUPS` = ['sell-price', 'sell', 'cancel']
    - buy 그룹 행 종류 순서 = value(비교가격) · watch · value(잔량) · checkValue(체결) · sell 그룹 = value · value · checkValue(잔량추적 %) · checkValue(체결) · derived · cancel 그룹 = value(취소잔량) · check(체결) · check(잔량추적)
    - 옛 id 보존: 매수가격 `lc-buy-order-price` · 주문금액 `lc-buy-order-amount` · 비교가격 `lc-buy-watch-price`/`lc-sell-watch-price` · 잔량 `lc-buy-watch-qty` · 체결 값 `lc-buy-min-trade-qty`/`lc-sell-min-trade-qty` (체크 `lc-buy-trade`/`lc-sell-trade`) · 호가변경 `lc-sweep-tick` · 한방가격 `lc-sweep-watch-price` · 매도가격 `lc-sell-order-price` · 매도비율 `lc-sell-order-ratio` · 호가잔량 `lc-sell-watch-qty` · 잔량추적 값 `lc-sell-qty-track-ratio`(체크 `lc-sell-qty-track`) · 취소잔량 `lc-cancel-watch-qty` · 매수취소 체결 `lc-cancel-trade` · 잔량추적 `lc-cancel-qty-track`
    - `LC_SWITCH_LABEL` = 매수주문 켜기 · 한방체결 켜기 · 매도주문 켜기 · 매수취소 켜기 · `dimWhenOff` 는 buy · sweep · sell 만 true
    - `lcNavigableRows('buy')` → [buyWatchPrice, buyWatchQty, buyMinTradeQty] (감시대상 제외) · `lcNavigableRows('cancel')` → [cancelWatchQty]
    - `SettingGroup` 은 `section data-slot="lc-group-{slot}"` · 가격 섹션은 헤더 없음 + `aria-label` 「매수 가격 설정」/「매도 가격 설정」 · 헤더 있을 때 제목·상태·스위치가 한 줄 흐름이고 스위치는 오른쪽 끝
    - `GroupSwitch` 는 `role="switch"` · `aria-checked` · 이름 = 라벨 · 클릭 → `onCheckedChange(!checked)` · disabled 면 호출 0
    - 꺼진(dimWhenOff && !on) 그룹의 행 컨테이너에 `opacity-45` · 매수취소 그룹은 꺼져도 없음
    - 상태 「감시 중」 요소에 `text-[var(--led-armed)]`, 그 밖은 `text-[var(--muted-fg)]`
    - `CheckValueRow`: 체크 `button[role="checkbox"]` `aria-checked` · 이름 「{그룹 제목} {라벨}」(예 「매수주문 체결」) · 값 있으면 별도 값 버튼(이름 「체결 30,000주」) · 값 없으면 값 버튼 없음
    - `WatchTargetRow`: `role="group"` 이름 「감시대상」 · 버튼 둘 `aria-pressed` · 시각 라벨 span 이 `hidden @min-[700px]/lc:inline` · 선택 버튼 `bg-[var(--seg-on-bg)]`
    - `DerivedRow` 는 버튼이 아니고 쉐브런이 없다
    - 모든 행 요소에 `min-h-[44px]` · 렌더된 마크업에 뷰포트 브레이크포인트 클래스 0 · 라벨/값에 `truncate`/`text-ellipsis` 0
  </behavior>
  <action>
    ① `webapp/src/components/trading/lc/lc-fields.ts` 신설(순수 데이터 + 두 조회 함수). `<interfaces>` 이름으로 export. 행 라벨·단위·설명은 UI-SPEC 「시트 제목 · 설명(필드별)」 표 원문 그대로: 매수가격 원 「넣을 매수 주문 가격이에요」 · 주문금액 만원 「한 번에 넣을 금액이에요」 · 비교가격 원 「이 가격의 잔량을 지켜봐요」 · 잔량 주 「잔량이 이 값보다 줄면 매수를 넣어요」 · 체결(매수) 주 「체결이 이 값 이상이면 매수를 넣어요」 · 호가변경 건 「호가가 이만큼 바뀌면 한 번에 체결해요」 · 한방가격 원 「한방 체결 가격이에요」 · 매도가격 원 「넣을 매도 주문 가격이에요」 · 매도비율 % 「보유 수량 중 매도할 비율이에요」 · 호가잔량 주 「잔량이 이 값보다 줄면 매도를 넣어요」 · 잔량추적(매도) % 「처음 잔량 대비 비율로 추적해요」 · 체결(매도) 주 「체결이 이 값 이상이면 매도를 넣어요」 · 취소잔량 주 「잔량이 이 값보다 적으면 취소해요」. 그룹: buy-price(`ariaLabel` 「매수 가격 설정」, dimWhenOff false) · buy(title 매수주문 · gate buyEnabled · statusKey buy · hint 「비교가격의 감시잔량이 위 값 이하로 줄면 매수 발주」 · dimWhenOff true) · sweep(한방체결 · sweepEnabled · sweep · true) · sell-price(「매도 가격 설정」) · sell(매도주문 · sellEnabled · sell · true) · cancel(매수취소 · cancelQtyEnabled · cancel · **false**). 행 구성·옛 id 는 `<behavior>` 목록 그대로. 파일 머리 주석: 「그룹·행 순서 · 라벨 · 단위 · 설명 · 옛 id · 게이트 매핑의 단일 원천 — 렌더 · 시트 문구(D-23) · Tab 순서 · e2e `data-lc-field` 가 모두 여기서 온다. 필드 목록·의미는 기존 폼 그대로(D-01 · D-19) · 매수취소 스위치 = cancelQtyEnabled(D-21) · 체크 행(D-22)」.
    ② `webapp/src/components/trading/lc/setting-group.tsx` 에 조각을 더한다(20-01 의 `SettingRow` · `FailureBubble` 유지). **`SettingGroup`** props `{ spec: LcGroupSpec; statusText?: string; on?: boolean; switchNode?: ReactNode; children }` → `<section data-slot={`lc-group-${slot}`} title={hint} aria-label={ariaLabel} className="min-w-0 rounded-[16px] bg-[var(--group-bg)] px-2.5 pt-2.5 pb-1">` (그룹 사이 10 은 부모 `flex flex-col gap-2.5` 가 준다). 헤더(title 있을 때만): `flex min-h-6 min-w-0 items-center gap-2 pb-0.5` 안에 `<span className="min-w-0 flex-1">` 한 흐름(제목 15/600 `--fg` + 상태 12/400 · 「감시 중」이면 `--led-armed` 아니면 `--muted-fg` · 말줄임 금지 — 길면 둘째 줄로 내려간다) + 오른쪽 `switchNode`. 행 컨테이너: `spec.dimWhenOff && on === false` 면 `opacity-45`(편집은 막지 않는다). **`GroupSwitch`** props `{ id?; label; checked; disabled?; onCheckedChange }`: `import { Switch as SwitchPrimitive } from 'radix-ui'` 의 `Root`/`Thumb` 직접 사용(`ui/switch.tsx` 무수정 · 무사용 — thumb 기하 하드코딩, Pitfall 9) — Root `relative inline-flex h-6 w-10 flex-none items-center rounded-full bg-[var(--switch-off)] transition-colors duration-150 motion-reduce:transition-none data-[state=checked]:bg-[var(--primary)] disabled:opacity-50 after:absolute after:-inset-x-[2px] after:-inset-y-[10px] after:content-['']`(시각 40×24 · 히트 44×44), Thumb `size-5 rounded-full bg-white translate-x-[2px] transition-transform duration-150 motion-reduce:transition-none data-[state=checked]:translate-x-[18px]`, `aria-label={label}`. **`CheckValueRow`** props `{ checkId; groupTitle; label; checked; onToggle; value?; unit?; valueId?; disabled?; busy?; flash?; failed?; editing?; editor?; onActivateValue?(el); hasPopup? }`: 44px 행 `flex min-h-[44px] items-center justify-between gap-1.5 px-1` — 왼쪽 `<button type="button" role="checkbox" aria-checked={checked} id={checkId} aria-label={`${groupTitle} ${label}`}>` 안에 20px 원(테두리 1.5px `--switch-off` · 켜짐 = `--primary` 채움 + 흰 체크 표시 lucide `Check` 14px) + 간격 8 + 라벨 14/400 `--muted-fg` nowrap(히트 높이 44) · 오른쪽은 `value` 가 있을 때만 `SettingRow` 와 같은 값 버튼(`data-lc-field={valueId}` · 값 슬롯 `data-slot="lc-row-value"` · 쉐브런 · 이름 「{라벨} {값}{단위}」) 또는 편집 중이면 `editor`. 값 버튼은 체크가 꺼져 있어도 누를 수 있다(quick-260912-u58 ④). **`WatchTargetRow`** props `{ value: '0' | '1'; onSelect; disabled?; busy?; flash? }`: 44px 행, `role="group" aria-label="감시대상"`, 시각 라벨 `<span className="hidden whitespace-nowrap text-[14px] text-[var(--muted-fg)] @min-[700px]/lc:inline">감시대상</span>`, 트랙 `flex w-full rounded-[8px] bg-[var(--raised-2)] p-0.5 @min-[700px]/lc:inline-flex @min-[700px]/lc:w-auto`, 버튼 2개 「매도잔량」('0') 「매수잔량」('1') `aria-pressed` · `h-8 flex-1 whitespace-nowrap rounded-[6px] px-2 text-[14px] font-semibold @min-[700px]/lc:h-[26px] @min-[700px]/lc:flex-none @min-[700px]/lc:px-2.5 @min-[700px]/lc:text-[13px]` · 선택 `bg-[var(--seg-on-bg)] text-[var(--seg-on-fg)] shadow-[var(--seg-on-shadow)]` / 비선택 `text-[var(--muted-fg)]` · busy 나 disabled 면 두 버튼 disabled · hover 면 없음 · 폰은 행 전체 폭, ≥700 은 `justify-between`(라벨 왼쪽 · 토글 오른쪽). **`DerivedRow`** `{ label; value; unit }` → `<div data-slot="lc-derived">` 44px · 라벨 14 `--muted-fg` · 값 15/500 `--fg-2` · 쉐브런 없음. 모든 조각: 라벨·값 `whitespace-nowrap` 이고 말줄임 유틸 금지, 폭 분기는 `@min-[700px]/lc:` 만.
    ③ `webapp/src/components/trading/lc/__tests__/setting-group.test.tsx` 신설 — `<behavior>` 전부(18개 이상 `it`). 마크업 전체에서 `/(^|\s)(sm|md|lg|xl|2xl):/` 와 `truncate` 가 없음을 정규식으로 단언한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/lc/__tests__/setting-group.test.tsx src/components/trading/lc/__tests__/lc-tracer.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed", or "setting-group.test.tsx" is absent from the output</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `setting-group.test.tsx` 18개 이상 통과
    - `grep -c "cancelQtyEnabled" webapp/src/components/trading/lc/lc-fields.ts` >= 1 · `grep -c "'매수취소 켜기'" webapp/src/components/trading/lc/lc-fields.ts` >= 1
    - `grep -c "from 'radix-ui'" webapp/src/components/trading/lc/setting-group.tsx` >= 1
    - `grep -c "from '@/components/ui/switch'" webapp/src/components/trading/lc/setting-group.tsx` == 0
    - `grep -nE "(^|[\"' ])(sm|md|lg|xl|2xl):" webapp/src/components/trading/lc/setting-group.tsx webapp/src/components/trading/lc/lc-fields.ts` 출력 0줄
    - `grep -cE "truncate|text-ellipsis" webapp/src/components/trading/lc/setting-group.tsx` == 0
  </acceptance_criteria>
  <done>그룹·행 조각과 필드 스펙 한 벌이 생겨 폼은 데이터로 목록을 그릴 수 있고, 각 조각의 접근성·44px·컨테이너 쿼리 규율이 테스트로 잠겼다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 폼 재조립 — 스펙 기반 리스트 · 스위치 4 · 체크/감시대상 즉시 반영 · 모든 값 행 시트/인라인 · 더티 모델 폐기 + CardBody 정리 + 카드 테스트 이관 (D-01 · D-02 · D-03 · D-04 · D-19 · D-20 · D-21 · D-22)</name>
  <reversibility rating="costly">D-01 · D-04 — 폼 트리·더티 액션 바·관련 단위 테스트·e2e 셀렉터를 전면 교체한다(CONTEXT 가 costly 로 기록). 서버 계약·데이터는 그대로라 one-way 는 아니다.</reversibility>
  <precondition>`.planning/phases/20-toss-order-ticket/20-02-SUMMARY.md` 에 `## 폭 스파이크` 절이 있고 밴드별 채택 레벨이 적혀 있으며 「L3 로도 넘침 ⚠」 표시가 없다(있으면 멈추고 사용자 결정을 받는다 — 글자 크기 불변 규칙과 충돌)</precondition>
  <files>webapp/src/components/trading/limit-chaser-form.tsx, webapp/src/components/trading/card/card-body.tsx, webapp/src/components/trading/__tests__/card-body.test.tsx, webapp/src/components/trading/__tests__/strategy-card-flow.test.tsx, webapp/src/components/trading/lc/setting-group.tsx (20-02 스파이크가 백스톱을 요구할 때만)</files>
  <read_first>
    - webapp/src/components/trading/limit-chaser-form.tsx (전체 — 20-01/20-03 배선 포함)
    - webapp/src/components/trading/card/card-body.tsx (전체)
    - webapp/src/components/trading/card/strategy-card.tsx (:700-840 — `dirtyCount` 가 0 이면 핀·호스트·테두리가 비활성이라는 사실 확인만, 수정 금지)
    - webapp/src/components/trading/__tests__/card-body.test.tsx (전체 — ⑥ 더티 바 describe · `Harness`)
    - webapp/src/components/trading/__tests__/strategy-card-flow.test.tsx (:150-376 · :551-684)
    - webapp/src/components/trading/lc/lc-fields.ts · webapp/src/components/trading/lc/setting-group.tsx (Task 1)
    - .planning/phases/20-toss-order-ticket/20-02-SUMMARY.md (`## 폭 스파이크` — 백스톱 레벨)
    - .planning/phases/20-toss-order-ticket/20-UI-SPEC.md (§레이아웃 계약 · §1~§4 · Copywriting Error state)
    - .planning/phases/20-toss-order-ticket/20-RESEARCH.md (§Q4 DirtyActionBar 제거 범위 표 · Pitfall 3 · 10)
  </read_first>
  <behavior>
    - CardBody(작업대 카드) 에서 「잔량 10,000주」 행 인라인 편집 → 8000 Enter → `lc.set` 1회 · 문서에 `[data-slot="dirty-action-bar"]` 0 · 카드 클래스에 더티 테두리(`var(--primary)_55%`) 없음
    - 호가 탭 variant 도 같은 편집 뒤 더티 바 0
    - 스위치 4개: `getByRole('switch', { name })` 로 「매수주문 켜기」「한방체결 켜기」「매도주문 켜기」「매수취소 켜기」 · 매수취소 스위치 → cfg.cancelQtyEnabled 토글 1회 전송
    - 카드 `unacked` 가 전송 뒤 true 가 되면 편집 중이던 행에 「반영하지 못했어요 · Enter 로 다시 시도해 주세요」(alert)
    - 터치(mockPointer(true)) 에서 매수가격 행 탭 → 시트 칩 「현재가」 는 `quote.p > 0` 이면 활성 · quote 없으면 비활성
    - strategy-card-flow 이관: 다른 단말 에코 배너(⑦b 재정의) · 3초 무응답 「미반영」(⑧, switch) · 삭제 에코 후 잔량 행 「10,000주」(⑪) · 카드 안 더티 호스트 비어 있음(⑭b 재정의) · 미등록 카드에서 주문금액 인라인 100 → 전송 0 → 스위치 → cfg.buyOrderAmount 100(㉑ armThenDisarm) · 거부 답이 인라인 반영 중 잠금을 풀고 입력값 8,000 보존(㉑-e 재정의)
  </behavior>
  <action>
    ① **limit-chaser-form.tsx 재조립.** 렌더를 `LC_BUY_GROUPS` / `LC_SELL_GROUPS` 로 돌린다: 루트 `data-slot="limit-chaser-form"` 유지 · 맨 위 `lc-submit-error`(`role="alert"`, 이제 스위치·체크·감시대상 전송 실패 전용) · `hideTabs` 가 아니면 기존 2탭 · 두 pane 그리드(`grid grid-cols-1 gap-2 @min-[700px]/lc:grid-cols-2 @min-[992px]/lc:gap-4 [&>*]:min-w-0` — 간격 8/16 은 기존 값 유지) · `data-pane="buy"`/`"sell"` 와 숨김 클래스(`hidden @min-[700px]/lc:block`)는 **그대로**(조건부 렌더 금지 — 탭 전환 때 in-flight 가 사라진다). 각 pane 안: 열 머리(`hidden @min-[700px]/lc:flex items-center gap-1.5 mt-2.5 text-[15px] font-bold text-[var(--fg)]` + 8px 점 `--up`(매수)/`--down`(매도) + 「매수」/「매도」) → `flex flex-col gap-2.5` 안에 그룹들 → 열 맨 아래 `ArmBlockedPanel`(매수 열 = buyEnabled·sweepEnabled 사유 · 매도 열 = sellEnabled 사유 — 기존 규율). 행 렌더(스펙 kind 별): **value** → `SettingRow`(값 = `form[field]` · 인라인 편집 중이면 `InlineValueEditor`) · **checkValue** → `CheckValueRow`(체크 `onToggle` = `commit(check, !form[check], 'toggle')`, 값 버튼 = value 행과 같은 편집 경로) · **check** → `CheckValueRow`(값 없음) · **watch** → `WatchTargetRow`(`onSelect(side)` = `commit('buyWatchSide', side, 'toggle')`, 그 필드가 in-flight/대기면 busy) · **derived** → `server?.sellEntryLatched` 일 때만 `DerivedRow`(「잔량추적 기준선」 · `server.sellQtyTrackBaseline` 주). 그룹 헤더 스위치 = `GroupSwitch`(`checked={form[gate]}` · `onCheckedChange={v => commit(gate, v, 'toggle')}` · `disabled` = gate 가 buy/sweep/sell 이면 기존 `gateBlocked(gate, !form[gate])`, cancelQtyEnabled 면 `disabled` 만 · cancel 에 `id="lc-cancel-qty"` · 이름은 `LC_SWITCH_LABEL`) · `SettingGroup` 의 `statusText` = 기존 `buyStatusText`·`sweepStatusText`·`sellStatusText`·`cancelStatusText` prop(statusKey 로 고른다) · `on={form[gate]}`. 값 행 활성화는 하나의 함수로 일반화한다: `disabled` 면 무시 → `returnFocusRef.current = el` → `mode === 'sheet'` 면 `setSheetField(field)` 아니면 `setEditingField(field)`. 시트 인스턴스는 폼에 하나(20-03)이고 제목·설명·단위는 `lcRowById`/스펙에서, `armedNotice` = 그 필드 그룹의 상태 문구가 「감시 중」인지, `ctx={{ current: currentPrice, upper: upperLimit ?? 0 }}`, `validate` = `armBlockOf`. 토글 결과가 `'disconnected'` 면 `setSubmitError(SEND_FAILED_TEXT.gate)`(기존 문장), 토글 실패(`failures[field].reason` 이 rejected/timeout)는 그 컨트롤을 `FailureBubble` 로 감싸 「반영하지 못했어요」(상태 기계가 값을 되돌린다) — 스위치는 그룹 헤더에 앵커. `flashField` → 해당 행/토글 900ms 강조. 전역 `disabled`(세션 미준비)는 모든 행·스위치·체크·토글에 `disabled` → opacity .5. **제거**(D-04 · RESEARCH §Q4 표): 더티 액션 바 모듈 import 와 포털 블록, `createPortal` · `useContext` 의 바 호스트 조회, `mounted`/`submitting`/`dirty`/`dirtySet`/150ms 에코 `flash` 상태, `handleSubmit`/`handleRevert`, 보조문 export 상수(액션 바 안내 문장), props `dirtyHint` · `dirtyBarClassName` · `onDirtyCountChange`, `SEND_FAILED_TEXT.submit`, 옛 조각 `Card` · `Row` · `NumInput` · `NumField` · `CheckRow` · `GateSwitch` · `Derived` · 그리고 20-01/20-03 이 「호가변경」에만 걸었던 개별 배선(일반화된 경로로 흡수). **유지**: `buildCfg` · `canArmOf` · `armBlockOf` · `armBlockedTextOf` · `armBlockedGroupsOf` · `ArmBlockedPanel` · `gateBlocked` · `GATE_LABEL` · 에코 이펙트(`formFromServer` 로 폼 덮기 + `onServerEcho({ changed, overwrittenDirty: 0 })`) · `[server, serverAnswerSeq]` 에서 `submitError` 접기 · `tab`/`hideTabs` · 신규 폼 상한가 1회 시딩. `dirtyFieldsOf` 는 더 이상 부르지 않는다(`lib/limit-chaser.ts` 와 그 테스트는 무수정). 파일 머리 주석 ①~⑧ 을 새 모델로 고쳐 쓴다: ② 오조작 방지(스위치 = 확인 없이 즉시 · 시각 40×24 + 히트 44×44 + 그룹 제목줄 오른쪽 끝 고정 · 켜기만 무장 판정 · 끄기 늘 허용 · 매수/매도는 열 머리 점 색 + 글자 + 위치 3중), ③ 「값은 확정 1회 = 즉시 반영(D-04) · 더티·「수정」 없음 · 성공 판정은 에코 값 일치」, ④ 에코가 목록을 이긴다 · 편집 중 버퍼는 보존(E4), ⑤ 32필드 규율 그대로, ⑧ 스위치를 Radix primitive 로 직접 그리는 이유. **폭 백스톱**: 20-02-SUMMARY 가 정한 밴드별 레벨만 적용한다 — 폰 밴드 전용이면 `px-0 @min-[700px]/lc:px-1` · `gap-1 @min-[700px]/lc:gap-1.5` · 쉐브런 `hidden @min-[700px]/lc:inline` 식의 컨테이너 쿼리 쌍으로 `SettingRow`/`CheckValueRow` 에 반영하고(이 경우 `setting-group.tsx` 도 이 태스크에서 고친다), 레벨 L0 이면 아무것도 바꾸지 않는다. 글자 크기는 건드리지 않는다.
    ② **card-body.tsx 정리**: 액션 바 보조문 상수 import · 호가 탭 더티 바 보조문을 조립하던 export 함수(종목명 16자 말줄임 + 「N개 미반영」) · `ORDERBOOK_DIRTY_BAR_CLASS` · `CHAT_FAB_CLEARANCE_CLASS` import · `DIRTY_NAME_MAX` · 폼에 넘기던 `dirtyHint`/`dirtyBarClassName`/`onDirtyCountChange` · `card` 구조분해의 `dirtyCount`/`setDirtyCount` 를 지운다. 그리드 · 사다리 · `unacked`/`currentPrice` 전달 · `cardGroupStatusOf` 는 그대로. 머리 주석에서 더티 바 문단을 「D-04 로 상따 더티 바 폐기 — VI 설정 줄의 「수정」은 별개」로 바꾼다. `strategy-card.tsx` · `trading-workbench.tsx` · `shared-panels.tsx` · `dirty-action-bar.tsx` 는 한 줄도 고치지 않는다(카드 `dirtyCount` 가 늘 0 → 핀·호스트·테두리·이탈 경고가 스스로 비활성).
    ③ **card-body.test.tsx**: 그 보조문 조립 함수 import 와 ⑥ 더티 바 describe(4개)를 지우고 「⑥ 즉시 반영 — 더티 바 없음 (D-04)」 describe 로 대체한다(`<behavior>` 첫 네 항목 · 4개 이상 `it`, 개수가 줄지 않게). ① 의 스위치 단언을 `getByRole('switch', …)` 4개로 바꾼다. 더 이상 쓰지 않는 `Harness` 는 지운다.
    ④ **strategy-card-flow.test.tsx**: `watchQtyInput()` 헬퍼를 `rowValue(id)`(= `[data-lc-field="{id}"] [data-slot="lc-row-value"]` 텍스트) · `editInline(id, value)`(행 버튼 클릭 → `#{id}` 입력 → change → Enter) 로 바꾸고 `<behavior>` 의 이관 6건을 적용한다: ⑦b 「인라인 편집 중 다른 단말 에코 → 입력 8,000 유지 · Esc 뒤 행 = 에코 5,000주 · 배너 「다른 단말에서 변경됐어요 · 서버 값으로 맞췄어요」」 · ⑧ 스위치 role · ⑪ 행 텍스트 · ⑭b 「값 확정·에코 뒤 카드 안 `card-dirty-host` 가 비어 있고 테두리가 파래지지 않는다」 · ㉑ `armThenDisarm` 은 미등록 카드에서 `editInline('lc-buy-order-amount', '100')` → 전송 0 → 스위치 · ㉑-e 「거부 답 → 인라인 편집기 busy 해제 · 입력값 8,000 보존 · 「반영하지 못했어요 · Enter 로 다시 시도해 주세요」」. 테스트 개수는 줄이지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/__tests__/card-body.test.tsx src/components/trading/__tests__/strategy-card-flow.test.tsx src/components/trading/lc/__tests__/lc-tracer.test.tsx src/components/trading/lc/__tests__/setting-group.test.tsx src/components/trading/__tests__/strategy-card.test.tsx src/components/stock/__tests__/stock-orderbook-section.test.tsx src/lib/__tests__/limit-chaser.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed", or "card-body.test.tsx" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "from '@/components/trading/dirty-action-bar'" webapp/src/components/trading/limit-chaser-form.tsx` == 0
    - `grep -c "cardDirtyHint" webapp/src/components/trading/card/card-body.tsx` == 0
    - `grep -c "LC_BUY_GROUPS" webapp/src/components/trading/limit-chaser-form.tsx` >= 1 · `grep -c "LC_SELL_GROUPS" webapp/src/components/trading/limit-chaser-form.tsx` >= 1
    - 플랜 시작 커밋을 `PLAN_BASE`(실행 시작 때 `git rev-parse HEAD`)로 기록해 두고 `git diff --stat "$PLAN_BASE" -- webapp/src/components/trading/card/strategy-card.tsx webapp/src/components/trading/workbench webapp/src/components/trading/dirty-action-bar.tsx webapp/src/lib/limit-chaser.ts` 출력 0줄(워크벤치 배관·lib 무수정 — 태스크 커밋 뒤에도 성립)
    - `grep -nE "(^|[\"' ])(sm|md|lg|xl|2xl):" webapp/src/components/trading/limit-chaser-form.tsx` 출력 0줄
    - card-body 의 「⑥ 즉시 반영」 describe 4개 이상 · strategy-card-flow 테스트 수가 이관 전 이상 · 위 명령 전부 통과
  </acceptance_criteria>
  <done>상따 설정 전체가 스펙 기반 토스식 리스트로 그려지고 모든 확정이 즉시 반영되며 더티 바가 사라졌다 — 카드 수준 통합 테스트가 새 모델로 green 이다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `limit-chaser-form.test.tsx` 재작성 — 즉시 반영 · 리스트 구성 · 스위치/체크/감시대상 · 무장 가드 · 미등록 · 에코 (D-01 · D-02 · D-04 · D-19 · D-20 · D-21 · D-22)</name>
  <files>webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx (전체 — 하네스 :35-145 · 살릴 규율 ①⑥⑦⑨⑩⑬⑮⑯)
    - webapp/src/components/trading/limit-chaser-form.tsx (Task 2 결과)
    - webapp/src/components/trading/lc/lc-fields.ts
    - webapp/src/lib/__tests__/match-media.ts (시트 모드 케이스용)
    - .planning/phases/20-toss-order-ticket/20-RESEARCH.md (Validation Architecture 표 · §Q4 테스트 영향 표)
  </read_first>
  <behavior>
    - ① 스위치 4개 즉시 전송(다이얼로그 없음 · `lc.set` 1회 · buyEnabled/sweepEnabled/sellEnabled/cancelQtyEnabled 각각 · 매수취소 = cancelQtyEnabled, D-21) · `send` false 면 스위치 낙관 반영 없음 + `lc-submit-error` 기존 문장
    - ② 값 확정 = 1회 전송(D-04): 매수가격 인라인 150000 Enter → cfg.buyOrderPrice 150000 · 다른 필드는 서버 값 · 더티 바 0 · 행은 에코 전 「130,000원」
    - ③ 반영 판정: 에코 일치 → 행 「150,000원」 + 강조 · 답만 증가 → 실패 문구 · 늦은 에코 → 성공
    - ④ 직렬화: 매수가격 in-flight 중 주문금액 확정 → 전송 1회, 에코 + 답 증가 뒤 2회
    - ⑤ 삭제 판정(옛 ⑥⑦): 매수·매도·취소 게이트가 전부 꺼지는 마지막 OFF = crud "D" · 취소잔량 스위치 켜짐이면 "C"
    - ⑥ S→C 전용 필드 미송신 + cfg 키 정확히 32개(스위치 경로 · 값 경로 둘 다) · 클라 고정 3(true/0/0)(옛 ⑨)
    - ⑦ 리스트 구성(D-19 · D-22 · D-21 · D-20): 매수 pane 그룹 slot 순서 buy-price → buy → sweep · 매도 pane sell-price → sell → cancel · 가격 섹션 헤더 없음 · 체크 행이 그룹 끝 · 매수취소 그룹 꺼져도 흐림 없음 · 모든 행 `min-h-[44px]` · 꺼진 매수주문 그룹 행 opacity .45 인데 행 클릭하면 편집 열림 · 파생값 행(옛 ⑩) 없음 · 기준선 행은 `sellEntryLatched` 일 때만
    - ⑧ 감시대상(D-02): `role=group` 「감시대상」 · 「매수잔량」 클릭 → cfg.buyWatchSide '1' 1회 · 시트/인라인 안 열림 · 전송 중 두 버튼 disabled · 시각 라벨 클래스 쌍 `hidden @min-[700px]/lc:inline`
    - ⑨ 체크 행(D-22): 「매수주문 체결」 체크 클릭 → cfg.buyTradeQtyEnabled true 1회 · 체크가 꺼져 있어도 값 버튼 편집 가능 · 매수취소 체결·잔량추적은 값 버튼 없음
    - ⑩ 무장 불가(옛 ⑬⑮⑯): 가격 0 이면 매수 스위치 disabled + 매수 열 맨 아래 사유 · 켜진 게이트의 매수가격을 0 으로 확정 → 전송 0 + 「매수주문 · 시세를 받지 못해 …」 · 철거 의도(게이트 전부 꺼짐)는 막지 않음 · 이미 켜진 게이트는 값이 0 이어도 끌 수 있다
    - ⑪ 미등록(A-P1): server 없음 → 값·체크·감시대상 확정 전송 0 · 로컬 행 값 반영 · 그 뒤 스위치 ON → cfg 에 로컬 값 실림
    - ⑫ pane·탭(옛 ⑫): 두 pane 이 늘 DOM · 클래스 쌍 · 제어형 `tab="sell"` · `hideTabs`
    - ⑬ 에코(옛 ⑧⑪): 새 에코가 행을 덮는다 · `buyOrderAmount: 0` 에코는 주문금액 행을 덮지 않는다 · 인라인 편집 중 에코는 입력 버퍼를 덮지 않는다(E4 partial)
    - ⑭ 시트 모드(mockPointer(true)): 매수가격 행 탭 → 시트 「매수가격 적용」 · 매수주문이 「감시 중」이면 「감시 중 — 적용하면 바로 반영돼요」 · 한방체결(「켜짐/꺼짐」) 필드에는 없음
  </behavior>
  <action>
    `webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx` 를 새 모델로 재작성한다. 기존 하네스(`vi.mock('@/lib/relay-provider')` · `sendMock` · `sentConfigs` · `lastConfig` · `echo()` · `props()`)는 그대로 쓰고, 헬퍼를 더한다: `row(id)` · `rowText(id)`(값 슬롯 텍스트) · `editInline(id, value)`(행 버튼 클릭 → `#id` → `fireEvent.change` → Enter) · `sw(name)` = `screen.getByRole('switch', { name })` · `chk(name)` = `getByRole('checkbox', { name })`. 더티·「수정」·「되돌리기」·46px 입력·`--lw`·17px 체크박스·`● ` 접두·액션 바 포털·`dirtyHint` 를 단언하던 describe(옛 ②③④⑤⑧ 의 더티 부분 · ⑫ 의 체크박스 라벨 · ⑰ · quick-260912-ok2 · 18-10)는 **삭제가 아니라** `<behavior>` ①~⑭ 로 의미를 옮겨 다시 쓴다(각 describe 이름 앞에 옛 번호를 괄호로 남겨 추적성을 유지 — 예 「⑤ 삭제 판정(옛 ⑥⑦)」). 서버 응답은 `rerender(<LimitChaserForm {...props({ server: echo({...}), serverAnswerSeq: n })} />)` 로 흉내 낸다. 시트 케이스는 `mockPointer(true)` + `afterEach(restoreMatchMedia)`. 목표: `it` 60개 이상 · 위 14개 describe 각 2개 이상. 끝으로 webapp 전체 suite 와 typecheck, 그리고 트레이서 e2e(P20-1 · P20-2)가 이 플랜 뒤에도 green 인지 돌린다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/__tests__/limit-chaser-form.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary line contains "failed", or fewer than 60 tests are reported for this file</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp run test</automated>
    <fails_when>non-zero exit, or output contains "error TS", or the "Test Files" summary line contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/trading-workbench.spec.ts -g "P20-"</automated>
    <fails_when>non-zero exit, or the output lacks "2 passed", or contains "No tests found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `limit-chaser-form.test.tsx` 60개 이상 통과 · describe 14개(①~⑭) 존재
    - `grep -c "getByRole('switch'" webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx` >= 4
    - `grep -c "dirty-action-bar" webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx` 의 모든 줄이 「부재」 단언(`toBeNull`/`toHaveLength(0)`)이다(`grep -n` 으로 확인)
    - webapp 전체 suite · typecheck green · Playwright P20-1 · P20-2 green
  </acceptance_criteria>
  <done>폼의 새 계약(즉시 반영 · 리스트 구성 · 스위치 4 · 체크/감시대상 · 무장 가드 · 미등록 · 에코)이 60개 이상의 단위 테스트로 잠겼고, 옛 더티 규율은 의미를 옮겨 재정의됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 리스트 스위치·체크·토글·값 → relay `lc.set` | 사용자 한 번의 확정이 자기 계좌 전략 1회 전송이 된다. 서버가 최종 |
| 에코 → 목록 | 서버 진실이 목록 표시를 이긴다(D-27) — 편집 중 버퍼만 예외 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering(자기 자산) | 스위치·체크·감시대상 오터치 즉시 반영 | high | mitigate | 스위치 기하(40×24 · 히트 44 · 오른쪽 끝 고정) · 켜는 방향 무장 판정(`gateBlocked`) · 감시대상 `aria-pressed` 버튼(방향키로 바뀌지 않음) · 전송 중 토글 비활성 · 실패 시 서버 값으로 되돌림(Task 1 · 2) |
| T-20-03 | Tampering | 오래된 로컬 값 덮어쓰기 | high | mitigate | 모든 확정이 상태 기계의 서버 동기 기준값 + 필드 1개(20-01) — 폼이 더 이상 더티 값을 들고 있지 않다(Task 2 더티 모델 제거) |
| T-20-04 | Repudiation | 거부를 성공으로 표시 | high | mitigate | 토글 실패는 서버 값 복귀 + 말풍선 · 끊김은 `lc-submit-error` · 값 실패는 행/시트 문구(Task 2) |
| T-20-12 | Tampering | 잘린 라벨로 다른 필드를 편집(오발주) | high | mitigate | 라벨·값 nowrap + 말줄임 금지 + 20-02 스파이크 기반 백스톱(Task 2) · 20-07 P20-3 이 4밴드 × 최악값 잘림 0 을 실브라우저로 단언 |
| T-20-13 | Elevation of privilege | 미등록 전략이 사용자가 켜지 않았는데 등록됨 | medium | mitigate | 미등록에서 값·체크·감시대상은 로컬만(A-P1) · 등록은 스위치 4개만(Task 2 · 3 ⑪) |
| T-20-SC | Tampering | 패키지 설치 | low | accept | 설치 없음 |
</threat_model>

<verification>
- 조각: `setting-group.test.tsx` 18+ green.
- 카드 통합: `card-body.test.tsx`(⑥ 즉시 반영) · `strategy-card-flow.test.tsx`(이관 6건) · `strategy-card.test.tsx`(컨테이너·뷰포트 BP 0) · `stock-orderbook-section.test.tsx`(`lc-group-*` 슬롯) green.
- 폼: `limit-chaser-form.test.tsx` 60+ green.
- webapp 전체 · typecheck · P20-1/P20-2 green. 워크벤치 배관·lib diff 0.
- 이 시점 옛 e2e(trading-workbench 옛 케이스 · orderbook 11 · a11y ③⑦ · sidebar-tree)는 계획대로 20-07 이관 전까지 red 일 수 있다 — SUMMARY 에 목록으로 남긴다.
</verification>

<success_criteria>
- 마우스·터치 어느 쪽에서든 상따 설정의 모든 필드를 토스식 리스트에서 바꾸면 그 한 필드가 즉시 반영된다.
- 매수취소는 매도 쪽 맨 아래에 스위치 = 취소잔량 켜기로 서고, 체크 필드는 「○ 라벨 ─ 값 ›」 행이며, 감시대상은 폭에 따라 두 모양이다.
- 상따 설정에서 더티 바와 「수정」 버튼이 사라졌고 워크벤치 코드는 한 줄도 바뀌지 않았다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `packages/shared/src/krxTick.ts` — `krxTickSize` · `tickUp` · `tickDown` · `priceInputIssue` · `PriceIssue` | 순수 함수(shared) | 20-02 |
| `packages/shared/src/krxTick.test.ts` | 단위 테스트 | 20-02 |
| `webapp/src/lib/numpad.ts` — `PadUnit` · `PadState` · `padInit` · `padKey` · `PAD_CHIPS` · `PadChip` · `PadChipOp` · `padChipDisabled` · `applyPadChip` · `padValue` · `formatPadDisplay` · `padIssue` · `priceIssueText` · `canConfirmPad` · `stepValue` · `PAD_MAX_DIGITS` | 순수 함수 | 20-02 |
| `webapp/src/lib/__tests__/numpad.test.ts` | 단위 테스트 | 20-02 |
| `webapp/src/lib/use-edit-mode.ts` — `useEditMode` · `EditMode` · `COARSE_POINTER_QUERY` | 훅 | 20-03 |
| `webapp/src/lib/__tests__/use-edit-mode.test.ts` · `webapp/src/lib/__tests__/match-media.ts`(`mockPointer` · `emitPointerChange` · `restoreMatchMedia`) | 테스트 · 헬퍼 | 20-03 |
| `webapp/src/components/trading/lc/use-lc-field-commit.ts` — `useLcFieldCommit` · `LcFieldKey` · `LcCommitKind` · `LcFailReason` · `LcCommitFailure` · `LcCommitOutcome` · `UseLcFieldCommitOptions` · `LC_FLASH_MS` · `LC_COMMIT_TEXT` · `LC_GATE_FIELDS` | 상태 기계 훅 | 20-01 |
| `webapp/src/components/trading/lc/inline-value-editor.tsx` — `InlineValueEditor` | 컴포넌트 | 20-01 (확장 20-05) |
| `webapp/src/components/trading/lc/setting-group.tsx` — `SettingRow` · `FailureBubble` (20-01) · `SettingGroup` · `GroupSwitch` · `CheckValueRow` · `WatchTargetRow` · `DerivedRow` (**20-04**) | 컴포넌트 | 20-01 · **20-04** |
| `webapp/src/components/trading/lc/number-pad-sheet.tsx` — `NumberPadSheet` · `NumberPadSheetProps` | 컴포넌트(Radix Dialog · body 포털) | 20-03 |
| `webapp/src/components/trading/lc/lc-fields.ts` — `LC_BUY_GROUPS` · `LC_SELL_GROUPS` · `LcGroupSpec` · `LcRowSpec` · `LC_SWITCH_LABEL` · `lcRowById` · `lcNavigableRows` | 필드 스펙(단일 원천) | **20-04** |
| `limit-chaser-form.tsx` 신규 순수 함수 `canArmOf` · `armBlockOf` | 무장 판정 | 20-01 |
| `manual-order-form.tsx` 내부 `TicketBox` · 새 props `currentPrice` · `upperLimit` | 컴포넌트 | 20-06 |
| CSS 토큰 `--group-bg` · `--switch-off` · `--dim`(`:root` · `.dark`) | 토큰 | 20-03 |
| 테스트: `lc/__tests__/lc-tracer.test.tsx` · `use-lc-field-commit.test.tsx`(20-01) · `number-pad-sheet.test.tsx`(20-03) · `setting-group.test.tsx`(**20-04**) · `inline-value-editor.test.tsx` · `inline-navigation.test.tsx`(20-05) | 단위/컴포넌트 | 각 플랜 |
| e2e: `P20-1`(20-01) · `P20-2`(20-03) · `P20-3`~`P20-5` · orderbook 11 대체 · a11y ③⑦ 대체(20-07) | Playwright | 각 플랜 |
| 제거: `order-panel.tsx` 의 `TICK_TABLE`/`tickFromTable`(20-02) · 액션 바 안내 상수 · `cardDirtyHint` export · 폼 더티 props 3개 · 옛 폼 조각(`Card`/`Row`/`NumInput`/`NumField`/`CheckRow`/`GateSwitch`/`Derived`)(**20-04**) | 제거 | 20-02 · **20-04** |

## Planner assumptions (flagged for user visibility)

- **A-P1:** 미등록 전략의 값·체크·감시대상 확정은 로컬 반영만(20-01 과 같음).
- **A-P2 (RESEARCH Open Q2):** 매수취소 스위치 접근성 이름 = 「매수취소 켜기」(기존 GateSwitch 이름 패턴).
- **A-P8 (RESEARCH Pitfall 3 — 허용):** 폼 remount 키(`resetSeq` · `liveSeed`)가 바뀌는 순간(미등록 카드의 첫 실시간 상한가 시딩 · 삭제 에코) 열려 있던 시트·인라인 편집은 사라질 수 있다. 첫 수백 ms 한정이고 삭제는 스위치 조작의 결과라 그때 시트가 열려 있지 않으므로 RESEARCH 판단대로 허용한다 — 문제가 되면 시트 열림 상태를 CardBody 로 끌어올리는 별도 quick.
- **A-P4:** 스위치·체크·감시대상의 거부/무응답 표시는 그 컨트롤에 앵커한 말풍선 「반영하지 못했어요」 + 서버 값 복귀, 연결 끊김만 폼 맨 위 `lc-submit-error` — UI-SPEC 의 E2 error(말풍선)와 레이아웃 계약(맨 위 줄 = 토글 전송 실패 전용)을 둘 다 만족시키는 배분.

## Spec-less probe note

Spec-less probe fallback: **visible skip** — SPEC.md · 요구사항 ID 가 없어 probe 술어를 만들지 않았다. `must_haves` 는 CONTEXT D-01~D-04 · D-19~D-22 · UI-SPEC §1~§4 · UI Considerations E1/E2 · CONTEXT 불변 목록에서 직접 썼다.

<output>
Create `.planning/phases/20-toss-order-ticket/20-04-SUMMARY.md` when done (list the e2e cases that are intentionally red until 20-07)
</output>
