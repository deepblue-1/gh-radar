---
phase: 15-dma-relay-kb-gh-trade-server-10-wss
plan: 08
type: execute
wave: 3
depends_on: ["15-07", "15-05"]
files_modified:
  - scripts/deploy-relay.sh
  - scripts/smoke-relay.sh
  - ops/alert-relay-down.yaml
  - infra/relay/README.md
autonomous: false
requirements: [RELAY-03]
must_haves:
  truths:
    - "SC-8: `relay/Dockerfile` + `scripts/deploy-relay.sh` + `scripts/smoke-relay.sh` + Cloud Monitoring 알림 정책이 존재하고, 배포가 Artifact Registry push → IAP SSH `docker pull` + `docker run --restart=always` 로 이뤄진다"
    - "SC-8: `smoke-relay.sh` 의 INV-1~8 이 전부 통과한다 — VM RUNNING / 방화벽 3규칙 / 고정 IP 결선 / VPN 터널·split-tunnel / `https://dma.jx1.io/healthz` 200+유효 TLS / wss 인증 왕복(잘못된 토큰 4401) / **내부 포트가 공인망에서 닫힘** / 알림 정책 결선"
    - "SC-8 / D-05: 브라우저는 Caddy 443 만 보고, 9100·8091 은 공인망에 노출되지 않는다 (INV-7 이 능동 검증)"
    - "SC-8 / D-27: 배포된 relay 의 `DMA_HOST` 는 사용자 지시 전까지 실서버(10.41.1.120)를 향하지 않는다"
  artifacts:
    - path: "scripts/deploy-relay.sh"
      provides: "amd64 빌드 → AR push → IAP SSH docker run --restart=always → 알림 정책 update-or-create"
      contains: "--platform=linux/amd64"
    - path: "scripts/smoke-relay.sh"
      provides: "INV-1~8 + --check-isin 서브커맨드 골격"
      contains: "INV-7"
    - path: "ops/alert-relay-down.yaml"
      provides: "uptime check 기반 relay 다운 알림 정책"
      contains: "uptime_check"
  key_links:
    - from: "scripts/deploy-relay.sh"
      to: "relay/Dockerfile"
      via: "docker build -f"
      pattern: "-f relay/Dockerfile"
    - from: "scripts/deploy-relay.sh"
      to: "ops/alert-relay-down.yaml"
      via: "NOTIFICATION_CHANNEL_ID sed 치환 후 update-or-create"
      pattern: "NOTIFICATION_CHANNEL_ID"
    - from: "smoke-relay.sh INV-7"
      to: "VM 공인 IP:8091"
      via: "nc 실패해야 PASS"
      pattern: "! nc -z"
---

<objective>
relay 를 VM 에 배포하고, 인프라 불변식 8개(INV-1~8)를 자동 검증하는 스모크 스크립트와 Cloud Monitoring 알림 정책을 갖춘다.

Purpose: SC-8 의 나머지 절반. 기존 워커 3종 세트(`setup-*-iam.sh` / `deploy-*.sh` / `smoke-*.sh` + `ops/alert-*.yaml`) 규약을 relay 에 그대로 적용하되, 배포 대상이 Cloud Run Job 이 아니라 **VM 위 Docker 컨테이너**이고 알림 근거가 실행 실패 메트릭이 아니라 **uptime check** 라는 두 가지가 다르다. INV-7 은 "실패해야 PASS" 인 능동 노출 검증이다.
Output: 배포 스크립트 1 + 스모크 1 + 알림 정책 1 + 실제 배포 실행.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-07-SUMMARY.md

<interfaces>
<!-- 배포 스크립트 필수 env (자동 메모리 reference_deploy_worker_env) -->
GCP_PROJECT_ID  (필수 — gcloud guard)
SUPABASE_URL    (필수 — 컨테이너 env)
NOTIFICATION_CHANNEL_ID (알림 정책 단계에 필수. 없으면 그 단계만 실패하고 배포는 완료)
알림 채널 실측값: projects/gh-radar/notificationChannels/14409521670382124894

<!-- Artifact Registry 태깅 규약 (deploy-intraday-sync.sh) -->
REGISTRY="asia-northeast3-docker.pkg.dev/gh-radar/gh-radar"
IMAGE="${REGISTRY}/relay:${SHA}"  + :latest

<!-- smoke 러너 골격 (smoke-intraday-sync.sh) -->
set -uo pipefail  (-e 끄고 개별 invariant 추적)
check() { name; shift; if "$@" >/dev/null 2>&1; then PASS++; else FAIL++; FAILED_INVS+=(name); fi }
종료 요약: "PASS: $PASS  FAIL: $FAIL" + exit 1 on FAIL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/deploy-relay.sh — AR push + IAP SSH docker run</name>
  <files>scripts/deploy-relay.sh</files>
  <read_first>
    - scripts/deploy-intraday-sync.sh (analog: 6구간 구조 — gcloud guard / 변수 / amd64 빌드 / push / 배포 / 알림 정책 update-or-create + 채널 ID 정규화)
    - relay/Dockerfile (빌드 대상 — 포트 8090/8091, dist/index.js)
    - relay/src/config.ts (컨테이너에 주입할 env 키 목록)
    - infra/relay/README.md (VM 접근 절차 — IAP SSH)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md §G-2
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md §Pitfall 11 (컨테이너 메모리 유계)
  </read_first>
  <action>
`scripts/deploy-relay.sh` 를 작성한다. 6구간(`deploy-intraday-sync.sh` mirror):

1. **gcloud guard** — `GCP_PROJECT_ID` 필수 + active configuration `gh-radar` 확인.
2. **변수** — `VM=radar-gw`, `ZONE=asia-northeast3-a`, `REGION=asia-northeast3`, `REPO=gh-radar`, `SHA=$(git rev-parse --short HEAD)`, `REGISTRY="${REGION}-docker.pkg.dev/${GCP_PROJECT_ID}/${REPO}"`, `IMAGE="${REGISTRY}/relay:${SHA}"`, `IMAGE_LATEST="${REGISTRY}/relay:latest"`. `: "${SUPABASE_URL:?SUPABASE_URL must be set}"`.
3. **선행 검증** — VM 이 RUNNING 인지, Secret 3종이 존재하는지, 방화벽 3규칙이 있는지 확인 후 진행(없으면 `setup-relay-iam.sh` 를 먼저 돌리라는 힌트).
4. **amd64 빌드 + push** — `docker build --platform=linux/amd64 --build-arg GIT_SHA=${SHA} -f relay/Dockerfile -t "$IMAGE" -t "$IMAGE_LATEST" .` → `docker push` 2개.
5. **VM 배포(IAP SSH)** — `gcloud compute ssh radar-gw --tunnel-through-iap --zone=$ZONE --command='...'` 로 다음을 수행:
   - `docker pull "$IMAGE"`
   - `docker rm -f gh-radar-relay || true`
   - `docker run -d --name gh-radar-relay --restart=always --network=host --memory=384m --memory-swap=768m --log-driver=json-file --log-opt max-size=10m --log-opt max-file=3 -e ... "$IMAGE"`
     - `--network=host` 를 쓰는 이유(VPN tun0 로 나가는 아웃바운드 + Caddy 가 127.0.0.1:8090 으로 프록시)를 주석에 남긴다.
     - `--memory=384m` 유계 이유(e2-micro 1GB — OOM 이 호스트 전체가 아니라 컨테이너에서 나게, Pitfall 11)를 주석에 남긴다.
   - env 주입: `NODE_ENV=production`, `LOG_LEVEL=info`, `APP_VERSION=${SHA}`, `SUPABASE_URL`, `WS_PORT=8090`, `ORDER_API_PORT=8091`, `DMA_HOST`, `DMA_PORT=9100`, `DMA_BROKER=KB`. **비밀 3종(`SUPABASE_SERVICE_ROLE_KEY`, `DMA_CRED_KEY`, `RELAY_ORDER_SECRET`)은 스크립트에 값을 넣지 않고**, VM 에서 메타데이터 토큰 → Secret Manager REST 로 읽어 `--env-file`(tmpfs, 0600, 실행 직후 삭제) 또는 `-e` 로 주입한다. **명령줄에 비밀 값이 그대로 들어가면 `ps`·`journalctl` 에 노출되므로 env-file 경로를 1순위로 한다.**
   - `DMA_HOST` 기본값: **`DMA_HOST` env 가 명시되지 않으면 실서버가 아니라 `127.0.0.1`(로컬 mock)** 로 둔다. 실서버 주소는 배포자가 명시적으로 `DMA_HOST=10.41.1.120` 을 넘길 때만 들어간다(D-27 오접속 방지). 스크립트가 실서버 주소를 감지하면 `⚠ 실서버 접속 모드 — 사용자 지시가 있었는지 확인하세요` 경고를 출력한다.
   - 배포 후 `docker ps --filter name=gh-radar-relay --format '{{.Status}}'` 로 Up 확인 + `curl -sf http://127.0.0.1:8091/healthz`.
6. **알림 정책 update-or-create** — `ops/alert-relay-down.yaml` 이 있으면 `NOTIFICATION_CHANNEL_ID` 를 full resource name 으로 정규화(`projects/*` 접두 없으면 붙임) 후 `sed` 치환 → `gcloud alpha monitoring policies list --filter="displayName=gh-radar-relay-down"` 로 존재 확인 → update 또는 create. 이 단계에 uptime check(`https://dma.jx1.io/healthz`) 생성/갱신도 포함한다(`gcloud monitoring uptime create/update` 또는 describe→create 멱등 패턴).

`--rollback` 서브커맨드로 직전 태그 이미지로 되돌릴 수 있게 한다(`docker run` 대상 태그만 바꿈).
  </action>
  <acceptance_criteria>
    - `bash -n scripts/deploy-relay.sh` exit 0
    - `grep -c -- '--platform=linux/amd64' scripts/deploy-relay.sh` == 1
    - `grep -c -- '-f relay/Dockerfile' scripts/deploy-relay.sh` == 1
    - `grep -c -- '--restart=always' scripts/deploy-relay.sh` == 1 이고 `grep -c -- '--memory=384m' scripts/deploy-relay.sh` == 1
    - `grep -c 'tunnel-through-iap' scripts/deploy-relay.sh` >= 1
    - `grep -c 'NOTIFICATION_CHANNEL_ID' scripts/deploy-relay.sh` >= 2 (필수 검사 + 정규화)
    - `grep -c '10.41.1.120' scripts/deploy-relay.sh` <= 1 이고 그 1건이 실서버 경고 분기 안에 있음
    - `grep -cE '(SERVICE_ROLE_KEY|CRED_KEY|ORDER_SECRET)=[A-Za-z0-9+/]{8}' scripts/deploy-relay.sh` == 0 (하드코딩 비밀 0건)
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/deploy-relay.sh && grep -c -- '--platform=linux/amd64' scripts/deploy-relay.sh</automated>
  </verify>
  <done>배포 스크립트가 문법 검사를 통과하고 amd64 빌드·AR push·IAP SSH docker run·알림 정책 6구간을 포함한다.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/smoke-relay.sh INV-1~8 + ops/alert-relay-down.yaml</name>
  <files>scripts/smoke-relay.sh, ops/alert-relay-down.yaml</files>
  <read_first>
    - scripts/smoke-intraday-sync.sh (analog: `check()` 러너 골격 + 서브커맨드 분기 + 종료 요약)
    - ops/alert-intraday-sync-failure.yaml (analog: documentation.content 원인 추정 목록 + `${NOTIFICATION_CHANNEL_ID}` 플레이스홀더 + autoClose)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md §Validation Architecture (INV-1~8 정의), §Assumptions A7
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md §G-3, §G-4
  </read_first>
  <action>
`scripts/smoke-relay.sh` — `set -uo pipefail`(개별 invariant 추적을 위해 `-e` 끔) + `check()` 러너. INV 8종:

- **INV-1** VM RUNNING — `gcloud compute instances describe radar-gw --zone=asia-northeast3-a --format='value(status)'` == RUNNING
- **INV-2** 방화벽 3규칙 — `gcloud compute firewall-rules list --filter='network=gh-radar-vpc'` 가 정확히 3행이고 이름이 `relay-allow-https`/`relay-allow-iap-ssh`/`relay-allow-internal-order`
- **INV-3** 고정 IP 결선 — `gcloud compute addresses describe gh-radar-relay-ip` 값 == VM 의 `networkInterfaces[0].accessConfigs[0].natIP`
- **INV-4** VPN 터널 + **split-tunnel 유지** — IAP SSH 원격 명령으로 `ip -br addr show tun0` 에 주소가 있고 **`ip route show default` 의 dev 가 `ens4`** 인지 확인. VPN 이 비활성 상태면 이 INV 는 `SKIP` 으로 표기하고 FAIL 로 세지 않는다(장중 외 시간 대응) — SKIP 카운터를 별도로 둔다.
- **INV-5** 공개 헬스 + TLS — `curl -sf --max-time 10 https://dma.jx1.io/healthz` 200 이고 `openssl s_client` 인증서 issuer 가 Let's Encrypt, `notAfter` 가 미래
- **INV-6** wss 인증 왕복 — node 원라이너로 `wss://dma.jx1.io/ws` 에 접속해 ① 잘못된 토큰 전송 시 close code **4401** ② 5초 무전송 시 close **4401** 을 단언. `ws` 는 relay 워크스페이스 의존성이므로 `pnpm --filter @gh-radar/relay exec node -e '...'` 로 실행한다.
- **INV-7** **내부 포트가 공인망에서 닫힘 (실패해야 PASS)** — `check "INV-7 ..." bash -c '! nc -z -w3 <VM 공인IP> 8091'`. 같은 방식으로 9100 도 검사한다. `nc` 가 성공하면 FAIL. **부정을 `bash -c '! ...'` 로 명시**한다.
- **INV-8** 알림 정책 + 채널 결선 — `gcloud alpha monitoring policies list --filter="displayName=gh-radar-relay-down"` 가 1행이고 `notificationChannels` 가 비어 있지 않으며, uptime check `gh-radar-relay-healthz` 가 존재

서브커맨드 분기(`smoke-intraday-sync.sh` 형식): `--check-isin`(15-10 이 채운다 — 지금은 "미구현" 안내 후 exit 0), `--check-tls`, `--check-exposure`(INV-7 만).

종료 요약: `PASS / FAIL / SKIP` 카운트 + 실패 목록 + `exit 1 on FAIL`.

`ops/alert-relay-down.yaml` — `alert-intraday-sync-failure.yaml` 형식:
- `displayName: gh-radar-relay-down`
- `documentation.content` 에 **원인 추정 목록**을 반드시 적는다(호출받은 사람이 즉시 분기): VPN 터널 단절 / KB 게이트웨이 다운 / 컨테이너 OOM(e2-micro 1GB) / Caddy 인증서 만료 / VM 재부팅 / DMA 로그인 거부(`/healthz` degraded).
- `conditions[].conditionThreshold.filter` 는 uptime check 기반 — `metric.type = "monitoring.googleapis.com/uptime_check/check_passed"` + `resource.type = "uptime_url"` + host `dma.jx1.io`. `comparison: COMPARISON_LT`, `thresholdValue: 1`, `duration: 300s`, `aggregations`(`alignmentPeriod: 300s`, `perSeriesAligner: ALIGN_FRACTION_TRUE`).
- `notificationChannels: - "${NOTIFICATION_CHANNEL_ID}"` **플레이스홀더**(리터럴 금지 — 배포 스크립트가 sed 치환).
- `alertStrategy.autoClose: 1800s`, `combiner: OR`, `enabled: true`.
- `/healthz` 가 degraded 를 **503 으로 내려야** uptime check 가 컨테이너 내부 장애를 잡는다는 점을 `documentation.content` 에 명시한다(RESEARCH A7).
  </action>
  <acceptance_criteria>
    - `bash -n scripts/smoke-relay.sh` exit 0
    - `grep -c 'INV-1' scripts/smoke-relay.sh` >= 1 … `grep -c 'INV-8' scripts/smoke-relay.sh` >= 1 (8종 전부 존재)
    - `grep -c "'! nc -z" scripts/smoke-relay.sh` >= 1 (INV-7 부정 명시)
    - `grep -c '8091' scripts/smoke-relay.sh` >= 1 이고 `grep -c '9100' scripts/smoke-relay.sh` >= 1
    - `grep -c 'ens4' scripts/smoke-relay.sh` >= 1 (split-tunnel 검증)
    - `grep -c '4401' scripts/smoke-relay.sh` >= 1
    - `grep -c 'check-isin' scripts/smoke-relay.sh` >= 1
    - `grep -c 'uptime_check' ops/alert-relay-down.yaml` >= 1 이고 `grep -c '${NOTIFICATION_CHANNEL_ID}' ops/alert-relay-down.yaml` == 1
    - `grep -c '14409521670382124894' ops/alert-relay-down.yaml` == 0 (채널 ID 리터럴 금지)
    - `python3 -c "import yaml,sys; yaml.safe_load(open('ops/alert-relay-down.yaml'))"` exit 0
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/smoke-relay.sh && python3 -c "import yaml; yaml.safe_load(open('ops/alert-relay-down.yaml'))" && grep -c "'! nc -z" scripts/smoke-relay.sh</automated>
  </verify>
  <done>INV-1~8 스모크 스크립트와 uptime 기반 알림 정책이 문법 검사를 통과하고 INV-7 부정 검증을 포함한다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] relay 배포 실행 + smoke INV 8/8</name>
  <files>infra/relay/README.md</files>
  <read_first>
    - scripts/deploy-relay.sh, scripts/smoke-relay.sh (Task 1·2 산출)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-07-SUMMARY.md (VPN 선검증 결과 — 실패였다면 INV-4 는 SKIP)
    - infra/relay/README.md (수정 대상 — 배포 결과 기록)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md D-27
  </read_first>
  <action>
`deploy-relay.sh` 로 amd64 이미지를 빌드·push 하고 IAP SSH 로 VM 에 `docker run --restart=always` 배포한 뒤 `smoke-relay.sh` 로 INV-1~8 을 검증한다. **`DMA_HOST` 를 실서버(10.41.1.120)로 지정하지 않는다**(D-27) — 이번 배포는 로컬/스텁 대상이다. INV-7(내부 포트 공인 차단)은 `nc` 로 육안 재확인하고, `free -m` / `docker stats` 로 e2-micro 메모리를 실측해 700MB 초과 시 e2-small 전환을 검토한다. 결과를 `infra/relay/README.md` §현재 배포 상태에 기록하고, 익일 `smoke-relay.sh --check-tls` 재확인을 SUMMARY 에 예정으로 남긴다.
  </action>
  <what-built>
`deploy-relay.sh` 가 amd64 이미지를 빌드해 Artifact Registry 에 올리고 IAP SSH 로 VM 에 `docker run --restart=always` 한다. `smoke-relay.sh` 가 INV-1~8 을 검증하고 `ops/alert-relay-down.yaml` 이 uptime check 알림을 건다.
  </what-built>
  <how-to-verify>
1. Secret 3종에 값이 들어 있는지 확인한다(`gcloud secrets versions list`). 비어 있으면 채운다 — 값은 대화·커밋에 남기지 않는다.
2. **`DMA_HOST` 를 실서버로 지정하지 않는다.** 이번 배포는 `DMA_HOST=127.0.0.1`(로컬 mock 없음 = 재접속 백오프 상태) 또는 VM 안에 띄운 스텁으로 진행한다. 실서버(10.41.1.120) 접속은 **사용자 지시가 있을 때만**이며 그 검증은 15-20 소관이다(D-27).
3. `GCP_PROJECT_ID=gh-radar SUPABASE_URL=... NOTIFICATION_CHANNEL_ID=14409521670382124894 bash scripts/deploy-relay.sh` 실행.
4. `bash scripts/smoke-relay.sh` 실행 → **INV 8/8 PASS**(VPN 미기동이면 INV-4 는 SKIP 허용). 실패 항목이 있으면 원인을 기록하고 수정 후 재실행한다.
5. **INV-7 을 눈으로도 확인**한다 — `nc -z -w3 <VM 공인IP> 8091` 과 `nc -z -w3 <VM 공인IP> 9100` 이 **둘 다 실패**해야 한다. 성공하면 방화벽 구성 오류이므로 즉시 중단하고 규칙을 고친다.
6. 메모리 실측: IAP SSH 로 `free -m` 과 `docker stats --no-stream gh-radar-relay` 를 기록한다. 합계가 700MB 를 넘으면 e2-small 전환을 검토한다(D-07 허용).
7. **다음 날 1회 재실행** — `smoke-relay.sh --check-tls` 로 인증서 갱신·VPN 지속성을 확인한다(VALIDATION §Sampling Rate). 이 항목은 SUMMARY 에 "익일 재확인 예정"으로 남기고 결과를 추후 STATE 에 기록한다.
8. `infra/relay/README.md` §현재 배포 상태에 이미지 태그(SHA), 컨테이너 상태, INV 결과, 메모리 실측을 기록한다.
  </how-to-verify>
  <acceptance_criteria>
    - `bash scripts/smoke-relay.sh` 종료 요약이 `FAIL: 0`
    - `nc -z -w3 <VM 공인IP> 8091` exit non-zero (닫힘) 이고 `nc -z -w3 <VM 공인IP> 9100` exit non-zero
    - `curl -sf https://dma.jx1.io/healthz` 가 200 이고 본문 JSON 에 `accountNo`/`userId` 키 없음
    - IAP SSH `docker ps --filter name=gh-radar-relay --format '{{.Status}}'` 출력이 `Up` 으로 시작
    - `gcloud alpha monitoring policies list --filter="displayName=gh-radar-relay-down" --format='value(name)' | wc -l` == 1
    - VM 컨테이너 env 에 `DMA_HOST=10.41.1.120` 이 **없음** (D-27) — `docker inspect gh-radar-relay --format '{{json .Config.Env}}'` 에 미포함
    - `grep -c '현재 배포 상태' infra/relay/README.md` >= 1 이고 이미지 SHA 기록 존재
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/smoke-relay.sh</automated>
  </verify>
  <done>smoke `FAIL: 0`, 컨테이너 Up, `/healthz` 200, 8091·9100 공인 차단, 알림 정책 1건, 컨테이너 env 에 실서버 주소 없음.</done>
  <resume-signal>smoke INV 결과(8/8 또는 실패 항목)와 메모리 실측을 확인한 뒤 "배포 확인" 입력. 문제가 있으면 항목을 기술</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 공인 인터넷 → VM | 443(Caddy)만 열려야 한다. 8091·9100 노출은 즉각적 침해 경로 |
| Artifact Registry → VM | 컨테이너 이미지 공급망 |
| Secret Manager → 컨테이너 env | 비밀 3종이 프로세스 환경으로 들어간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | Information Disclosure | 내부 포트 공인 노출 | mitigate | **INV-7 이 능동 검증** — `bash -c '! nc -z -w3 <공인IP> 8091'` 및 9100. 성공하면 FAIL 로 배포가 실패한다. Task 3 ⑤ 가 육안 확인도 요구 |
| T-15-06 | Spoofing | 내부 주문 포트 | mitigate | 방화벽 source-ranges(15-06/07) + `X-Relay-Secret` 상수시간 비교(15-05). 이중 방어의 양쪽이 모두 배포됨 |
| T-15-29 | Information Disclosure | 배포 명령줄 비밀 노출 | mitigate | 비밀 3종을 `docker run -e VALUE` 로 직접 넘기지 않고 VM 안에서 Secret Manager → tmpfs env-file 로 주입. `ps`·`journalctl`·shell history 에 값이 남지 않는다 |
| T-15-08 | DoS (메모리) | 컨테이너 | mitigate | `--memory=384m --memory-swap=768m` 유계 + json-file 로그 로테이션(10m×3). OOM 이 호스트 전체를 죽이지 않는다. Task 3 ⑥ 이 실측 기록 |
| T-15-30 | Tampering (오접속) | `DMA_HOST` | mitigate | 기본값이 실서버가 아니며, 실서버 지정 시 경고 출력. acceptance 가 컨테이너 env 에 `10.41.1.120` 부재를 검사(D-27) |
| T-15-13 | DoS | 인증서 만료 | mitigate | uptime check + `gh-radar-relay-down` 알림 정책 + `smoke-relay.sh --check-tls` 익일 재확인 |
| T-15-22 | Information Disclosure | 공개 `/healthz` | mitigate | acceptance 가 응답 JSON 에 `accountNo`/`userId` 부재를 검사 |
</threat_model>

<verification>
- `bash scripts/smoke-relay.sh` → `FAIL: 0` (INV-1~8, VPN 미기동 시 INV-4 SKIP)
- `bash -n scripts/deploy-relay.sh && bash -n scripts/smoke-relay.sh` exit 0
- `python3 -c "import yaml; yaml.safe_load(open('ops/alert-relay-down.yaml'))"` exit 0
- `curl -sf https://dma.jx1.io/healthz` 200
- `nc -z -w3 <공인IP> 8091` / `9100` 둘 다 실패
</verification>

<success_criteria>
- SC-8 충족: Dockerfile + setup/deploy/smoke 3종 + 알림 정책 + VM 프로비저닝 문서가 존재하고 실제로 동작
- relay 컨테이너가 VM 에서 `--restart=always` 로 돌고 공개 `/healthz` 가 200
- 내부 포트·게이트웨이 포트가 공인망에서 닫혀 있음이 능동 검증됨
- 실서버 접속은 일어나지 않았음
</success_criteria>

<output>
After completion, create `.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-08-SUMMARY.md`
</output>
