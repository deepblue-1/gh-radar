---
phase: 15-dma-relay-kb-gh-trade-server-10-wss
plan: 13
type: execute
wave: 4
depends_on: ["15-12", "15-10"]
files_modified:
  - webapp/src/components/orderbook/orderbook-ladder.tsx
  - webapp/src/components/orderbook/trade-tape.tsx
  - webapp/src/components/stock/stock-orderbook-section.tsx
  - webapp/src/components/stock/stock-detail-client.tsx
autonomous: true
requirements: [RELAY-01]
must_haves:
  truths:
    - "SC-7 / D-04: 호가 10단(중앙 가격 1열 · 좌 매도잔량 / 우 매수잔량)과 체결 테이프가 KRX/NXT 토글과 함께 표시된다"
    - "SC-7 / D-02a: 호가주문 탭이 ≥900px 에서 L1=B 2축 그리드(사다리 380 / 체결 220 / 우측 1fr)이고 <900px 에서 M1 세로 순서(연결상태 → 호가 5단 → 주문 → 체결 → 잔고 → 미체결)를 따른다"
    - "SC-7 / D-12: `dma_credentials` 매핑이 없는 사용자는 섹션이 숨겨지지 않고 `권한 없음` 게이트 카드가 본문을 대체한다 (행동 버튼 없음)"
    - "SC-7: 호가 가격 클릭이 주문 가격 입력으로 반영되고 매수/매도 탭은 절대 자동 전환되지 않는다"
    - "SC-7: 재접속 중에도 사다리·테이프가 비워지지 않고 마지막 값 + `opacity:.55` 로 유지된다"
  artifacts:
    - path: "webapp/src/components/orderbook/orderbook-ladder.tsx"
      provides: "호가 10단 사다리 — 중앙 가격 1열, 단계 최대 정규화 잔량 바, roving tabindex"
      contains: "roving"
    - path: "webapp/src/components/orderbook/trade-tape.tsx"
      provides: "체결 테이프 — 200ms 배치 prepend, 스크롤 핀, 링버퍼 200"
      contains: "새 체결"
    - path: "webapp/src/components/stock/stock-orderbook-section.tsx"
      provides: "섹션 셸 — 헤더/상태 바/그리드/게이트 분기"
      contains: "실시간 호가·주문 권한이 없어요"
  key_links:
    - from: "webapp/src/components/stock/stock-orderbook-section.tsx"
      to: "webapp/src/lib/use-relay-socket.ts"
      via: "훅 상태 소비"
      pattern: "useRelaySocket"
    - from: "webapp/src/components/stock/stock-detail-client.tsx"
      to: "webapp/src/components/stock/stock-orderbook-section.tsx"
      via: "orderbook 탭 패널 교체 (placeholder 제거)"
      pattern: "StockOrderbookSection"
---

<objective>
호가주문 탭의 **시장 관찰 축**을 구현한다. 호가 10단 사다리, 체결 테이프, 그리고 이 둘과 상태 바를 담는 섹션 셸(권한 없음 게이트 포함)을 만들고 15-11 이 남긴 placeholder 를 교체한다.

Purpose: SC-7 의 핵심 표면. UI-SPEC 은 L2=A(중앙 가격 1열)·L3=A(단계 최대 정규화 잔량 바)·L1=B(2축)로 확정돼 있고 목업 검토도 끝났으므로 **목업을 다시 만들지 않고 구현만** 한다. 주문 패널·계좌 패널(내 계좌 축)은 D-25 게이트 뒤(15-18)이므로 이 plan 은 우측 컬럼에 자리만 잡는다.
Output: 사다리 1 + 테이프 1 + 섹션 셸 1 + 탭 마운트 교체.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-UI-SPEC.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-12-SUMMARY.md

<interfaces>
<!-- 훅 계약 (15-12 산출) -->
```ts
useRelaySocket({ isin, exchange, enabled }): {
  status: RelaySessionState | "idle"; statusLabel: string; statusMessage: string;
  attempt: number; accounts: RelayAccount[];
  quote: RelayQuote | null; tape: RelayTapeEntry[]; account: RelayAccountState | null;
  messages: RelayServerMessage[]; isStale: boolean;
  send(msg: RelayInbound): void; reconnect(): void;
}
```

<!-- UI-SPEC 확정값 (변경 금지) -->
L1 = B · 2축      좌: 호가+체결 / 우: 주문(위)·잔고·미체결(아래)
L2 = A            중앙 가격 1열 (좌 매도잔량 / 중앙 가격 / 우 매수잔량)
L3 = A            잔량 바 = **단계 최대값 정규화**, 숫자 뒤 16% 틴트 배경 레이어
M1                <900px 세로 순서: 연결상태 → 호가 5단(+10단 전체 보기) → 주문 → 체결 → 잔고 → 미체결
≥900px 그리드     grid-template-columns: 380px 220px minmax(360px,1fr); gap: 1px
밀도              사다리·테이프 `data-density="compact"`(32px), 잔고·미체결 `default`(36px)

<!-- 색 규칙 (UI-SPEC §Color 열거표 — 이 표에 없으면 쓰지 않는다) -->
사다리 가격 열 텍스트 = 기준가(전일종가) 대비 `--up`/`--down`/`--flat`
매도호가 행 배경 `--down-bg` 65% 틴트 · 매수호가 행 배경 `--up-bg` 65% 틴트
잔량 바 = 매도 `--down` 16% / 매수 `--up` 16%, **숫자는 `--fg` 유지**
체결 테이프 매수/매도 = `▲ 매수` / `▼ 매도` **부호+라벨 병기 필수**
금지: 상태 배지·잔량 숫자·로딩/빈 상태에 `--up`/`--down`
토큰 충돌: `--destructive` == `--up`, `--primary` == `--down` → 사다리 내부에 `--primary` 금지

<!-- Copywriting (verbatim) -->
호가 빈 상태  `호가 정보가 없어요` / `장 시작(09:00) 이후 실시간 호가가 표시돼요.`
체결 빈 상태  `아직 체결이 없어요` / `장 시작(09:00) 이후 체결이 발생하면 위에서부터 쌓여요.`
권한 없음     제목 `실시간 호가·주문 권한이 없어요`
              본문 `실시간 호가와 주문은 증권사 계정이 연결된 사용자만 이용할 수 있어요. 연결이 필요하면 관리자에게 문의해 주세요.`
              보조 `이 종목의 차트·뉴스·종목토론방은 그대로 이용할 수 있어요.` (버튼 없음)
섹션 로드 실패 `호가를 불러오지 못했어요` / `연결에 실패했어요. 페이지를 새로고침해 주세요.` (버튼 `다시 연결`)
NXT 빈 상태   `이 종목은 NXT 호가가 없어요`
테이프 스크롤 핀 `새 체결 N건 · 맨 위로`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 호가 10단 사다리 (orderbook-ladder.tsx)</name>
  <files>webapp/src/components/orderbook/orderbook-ladder.tsx</files>
  <read_first>
    - webapp/src/components/stock/stock-comovement-section.tsx (analog: 잔량 바 마크업 L93-103 + 방향색 헬퍼 L47-51 + 파일 상단 docblock 규약)
    - webapp/src/components/ui/number.tsx (모든 방향색 숫자의 진입점 — `withColor` 사용 규칙)
    - webapp/src/styles/globals.css (`.mono`/`.num`/`[data-density]`/`--row-h`/`--up`/`--down`/`--up-bg`/`--down-bg`/`--flat`)
    - packages/shared/src/relay.ts (`RelayQuote` — ap/aq/bp/bq 10 배열, cs, et)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-UI-SPEC.md §Component Inventory C4·C5, §Color 열거표+금지 목록+토큰 충돌, §Interaction Contract "호가 → 주문 연결"·"실시간 갱신 시각 피드백"·"키보드 접근성"·"색 비의존", §확정 결정 L2·L3
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-orderbook-mockup.html (채택 목업 — 사다리 상세)
  </read_first>
  <action>
`webapp/src/components/orderbook/orderbook-ladder.tsx` — `"use client"`. 전용 `<table>` 마크업(`ui/table.tsx` 로는 중앙 가격 1열 + 좌우 잔량 + 바 레이어 + roving tabindex 를 표현할 수 없다).

props: `{ quote: RelayQuote | null; depth: 5 | 10; isStale: boolean; onPriceClick(price: number): void; basePrice: number }`.

구조(L2=A):
- `<thead>` 열 헤더 3열: `매도잔량` / `가격` / `매수잔량`.
- 행 20개(매도 10단 역순 → 스프레드 구분선 → 매수 10단). `depth === 5` 면 매도5~매수5 만 렌더하고 하단에 `10단 전체 보기` 버튼.
- 가격 셀은 `<th scope="row">`, **중앙 정렬**(좌우 잔량 열과 축을 맞추기 위한 유일한 예외 — 다른 숫자는 `.num` 우측 정렬).
- 각 가격 셀 안에 `sr-only` 로 `매도 3호가` / `매수 1호가` 단계 라벨(색 비의존, WCAG 1.4.1).
- 현재가 행: 좌측 3px `--fg` 마커 + `체결` 칩 + 스프레드 구분선(1차 시각 앵커).
- 총잔량 행(`<tfoot>`) — 마이크로 라벨 11px.

색(UI-SPEC 열거표 그대로):
- 가격 열 텍스트 = **기준가(`basePrice`, 전일종가) 대비** `--up`/`--down`/`--flat`. 급등 종목에서 20단 전부 `--up` 이 되는 것은 정상이며, **그래서 매도/매수 구분을 색에 의존하면 안 된다**는 주석을 남긴다.
- 매도 행 배경 `--down-bg` 65% 틴트 / 매수 행 배경 `--up-bg` 65% 틴트.
- 잔량 바 = 매도 `--down` 16% / 매수 `--up` 16%, **단계 최대값 정규화**(L3=A — 누적 아님). 숫자 색은 `--fg` 유지.
- **`--primary` 를 사다리 내부에 절대 쓰지 않는다**(`--primary` == `--down` 이라 매도로 오독).

인터랙션:
- 가격 클릭 → `onPriceClick(price)`. **매수/매도 탭을 자동 전환하지 않는다**(오조작의 직접 원인). 사다리에는 잔상을 남기지 않는다(피드백은 주문 입력칸 테두리 플래시 — 15-18 소관).
- **roving tabindex**: 사다리 전체가 tab stop 1개(`tabIndex=0`), `↑`/`↓` 로 행 이동, `Enter`/`Space` 로 그 행 가격 반영. 20개 버튼이 tab 순서를 잡아먹지 않게 한다.
- `aria-label="호가 10단 (매도 10단계 · 매수 10단계)"`.

갱신 피드백:
- 변경된 셀에 **배경 플래시 1회 ≤150ms**(`background-color` 만 — `transform`/`width` 애니메이션 금지, 레이아웃 shift 0).
- **플래시를 큐잉하지 않는다** — 갱신이 겹치면 최신만 남기고 이전 플래시 즉시 종료.
- 잔량 바 폭 변화는 **transition 없이 즉시 반영**(100ms 주기에서 애니메이션은 항상 뒤처진다).
- `prefers-reduced-motion: reduce` → 플래시 비활성, 값만 교체.

상태:
- `quote === null` → 빈 상태 `호가 정보가 없어요` / `장 시작(09:00) 이후 실시간 호가가 표시돼요.`
- `isStale` → 마지막 값 유지 + `opacity:.55`(**비우지 않는다**).
- 밀도: 루트에 `data-density="compact"` 명시(모바일에서도 32px 유지 — globals.css 의 모바일 comfortable 규칙이 `:not([data-density="compact"])` 이므로 명시가 곧 방어).
  </action>
  <acceptance_criteria>
    - `grep -c 'data-density="compact"' webapp/src/components/orderbook/orderbook-ladder.tsx` >= 1
    - `grep -cE '\-\-primary' webapp/src/components/orderbook/orderbook-ladder.tsx` == 0 (토큰 충돌 회피)
    - `grep -c 'sr-only' webapp/src/components/orderbook/orderbook-ladder.tsx` >= 1
    - `grep -c 'scope="row"' webapp/src/components/orderbook/orderbook-ladder.tsx` >= 1
    - `grep -c 'tabIndex' webapp/src/components/orderbook/orderbook-ladder.tsx` >= 1 이고 `grep -c 'ArrowDown\|ArrowUp' ...` >= 2 (roving)
    - `grep -c '호가 정보가 없어요' webapp/src/components/orderbook/orderbook-ladder.tsx` == 1
    - `grep -cE 'transition[^;]*width' webapp/src/components/orderbook/orderbook-ladder.tsx` == 0 (바 폭 transition 금지)
    - `grep -c 'prefers-reduced-motion\|motion-reduce' webapp/src/components/orderbook/orderbook-ladder.tsx` >= 1
    - `pnpm --filter webapp run typecheck` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter webapp run typecheck</automated>
  </verify>
  <done>사다리가 L2=A/L3=A 확정값, 색 열거표, roving tabindex, 플래시 규율을 갖추고 typecheck 통과.</done>
</task>

<task type="auto">
  <name>Task 2: 체결 테이프 (trade-tape.tsx)</name>
  <files>webapp/src/components/orderbook/trade-tape.tsx</files>
  <read_first>
    - webapp/src/components/stock/stock-comovement-section.tsx (analog: 행 렌더 + 칩)
    - webapp/src/components/ui/button.tsx (스크롤 핀 — `size="sm"`)
    - webapp/src/components/ui/number.tsx (방향색 숫자 진입점)
    - packages/shared/src/relay.ts (`RelayTapeEntry` — t/p/cs/c/q/cv)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-UI-SPEC.md §Component Inventory C6·C7, §Interaction Contract "체결 테이프 스크롤"·"실시간 갱신 시각 피드백", §Color(체결 테이프 행)
  </read_first>
  <action>
`webapp/src/components/orderbook/trade-tape.tsx` — `"use client"`. 전용 `<table>` + 스크롤 컨테이너.

props: `{ entries: RelayTapeEntry[]; isStale: boolean; basePrice: number }`.

- 열: `시각` · `체결가` · `수량` · `구분`. **최신 상단 prepend.**
- 체결가 색 = 기준가 대비 `--up`/`--down`/`--flat`.
- 구분은 **`▲ 매수` / `▼ 매도` 부호+라벨 병기 필수**(색만으로 구분 금지 — WCAG 1.4.1).
- 모든 숫자 `.mono` + `.num` 우측 정렬, 시각은 `HH:MM:SS`(마이크로초 절삭).
- 밀도 `data-density="compact"`(32px) — 사다리와 baseline 정렬.

스크롤 규율:
- `scrollTop <= 4px` 일 때만 자동으로 맨 위 유지. 사용자가 스크롤을 내리면 **자동 스크롤 정지**.
- 정지 상태에서 신규 체결이 쌓이면 상단에 핀 버튼 `새 체결 N건 · 맨 위로`(`ui/button.tsx` `size="sm"`). 클릭 시 맨 위 + 카운터 리셋. `scrollTop<=4` 이면 자동 숨김.
- 링버퍼 상한 **200건**(relay 캐시와 동일). 초과분은 하단부터 제거.

갱신 피드백:
- 200ms 배치 단위로 **1회 플래시**. **행마다 개별 애니메이션 금지.**
- `prefers-reduced-motion: reduce` → 플래시 비활성.

상태:
- `entries.length === 0` → `아직 체결이 없어요` / `장 시작(09:00) 이후 체결이 발생하면 위에서부터 쌓여요.`
- `isStale` → 마지막 값 유지 + `opacity:.55`.
  </action>
  <acceptance_criteria>
    - `grep -c '새 체결' webapp/src/components/orderbook/trade-tape.tsx` >= 1
    - `grep -c '▲ 매수' webapp/src/components/orderbook/trade-tape.tsx` >= 1 이고 `grep -c '▼ 매도' ...` >= 1
    - `grep -c '아직 체결이 없어요' webapp/src/components/orderbook/trade-tape.tsx` == 1
    - `grep -c '200' webapp/src/components/orderbook/trade-tape.tsx` >= 1 (링버퍼 상한)
    - `grep -c 'scrollTop' webapp/src/components/orderbook/trade-tape.tsx` >= 2
    - `grep -c 'data-density="compact"' webapp/src/components/orderbook/trade-tape.tsx` >= 1
    - `pnpm --filter webapp run typecheck` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter webapp run typecheck</automated>
  </verify>
  <done>테이프가 prepend·스크롤 핀·링버퍼 200·부호 병기·배치 플래시를 갖추고 typecheck 통과.</done>
</task>

<task type="auto">
  <name>Task 3: 섹션 셸 + 권한 없음 게이트 + 탭 마운트 교체</name>
  <files>webapp/src/components/stock/stock-orderbook-section.tsx, webapp/src/components/stock/stock-detail-client.tsx</files>
  <read_first>
    - webapp/src/components/stock/stock-comovement-section.tsx (analog: 섹션 파일 docblock 규약 + 종목 전환 state sticky 방어 L280-305 + 빈 상태 박스 L334-345. **단, 에러 시 `return null` 은 따르지 않는다** — UI-SPEC C1 이 섹션 숨김을 금지)
    - webapp/src/components/stock/stock-detail-client.tsx (수정 대상 — placeholder 를 교체할 지점)
    - webapp/src/components/orderbook/relay-status-bar.tsx, orderbook-skeleton.tsx (15-12 산출)
    - webapp/src/components/orderbook/orderbook-ladder.tsx, trade-tape.tsx (Task 1·2 산출)
    - webapp/src/lib/use-relay-socket.ts (15-12 산출 — 훅 계약)
    - webapp/src/components/ui/toggle-group.tsx, ui/card.tsx
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-UI-SPEC.md §Component Inventory C1·C5·C13·C14, §확정 결정 L1·L5·L6·M1·M2·M3·D1·D3, §반응형 표
  </read_first>
  <action>
`webapp/src/components/stock/stock-orderbook-section.tsx` — `"use client"`. 파일 상단 docblock 4블록(무엇 / 어디에 마운트 / ★LOCKED 색 규칙 / 빈·에러·게이트 상태).

props: `{ code: string; isin: string | null; basePrice: number }`.

동작:
- `useRelaySocket({ isin, exchange, enabled: Boolean(isin) })`.
- **거래소 상태**를 로컬로 소유(`KRX` 기본) — `ui/toggle-group.tsx` 로 KRX/NXT 단일 선택, `aria-label="거래소 선택"`. 세션 미준비 시 비활성. 전환 중 사다리 상단에 `{거래소} 호가 불러오는 중` 인라인(스켈레톤으로 되돌리지 않는다). NXT 호가가 비면 `이 종목은 NXT 호가가 없어요` 빈 상태(D3 — 토글 비활성화는 deferred).
- **헤더**: 종목명·코드 + **실시간 체결가 20px**(`--t-h3`, `.mono`) + `실시간(DMA) · 체결 HH:MM:SS` 출처 라벨 상시 노출 + 기준/상한/하한/VI. 히어로(30px 스냅샷)와 값이 다를 수 있음을 크기 위계와 출처 라벨로 설명한다(D1). **히어로는 수정하지 않는다.**
- **상태 바**: `relay-status-bar.tsx` 를 섹션 상단 전폭에 배치(L6).
- **본문 분기**:
  - `status === "unauthorized"` 또는 `isin === null` → **C13 게이트 카드**로 본문 교체. `ui/card.tsx` + 자물쇠 아이콘(lucide) + 제목/본문/보조 문구 verbatim. **행동 버튼 없음**(v1 은 관리자 수기 등록이라 셀프서비스 경로가 없다). **섹션 자체를 숨기지 않는다.**
  - 초기 로딩 → `orderbook-skeleton.tsx`.
  - 연결 실패(`failed` 이후 복구 불가) → `호가를 불러오지 못했어요` / `연결에 실패했어요. 페이지를 새로고침해 주세요.` + `다시 연결` 버튼(`reconnect()`).
  - 정상 → 그리드.
- **그리드**:
  - `≥900px`: `grid-template-columns: 380px 220px minmax(360px,1fr); gap: 1px` — 좌 사다리(10단) / 체결 테이프 / **우측 컬럼**. 우측 컬럼에는 이 plan 에서 `data-testid="orderbook-account-column"` 자리만 잡고 `주문 패널은 준비 중이에요` 안내를 넣는다(15-18 이 교체).
  - `<900px`: **M1 세로 순서** — ① 연결상태(+ServerMessage) ② 호가 **5단** + `10단 전체 보기` 버튼 ③ 주문 패널 자리 ④ 체결 테이프 ⑤ 잔고 자리 ⑥ 미체결 자리. 주문 패널 sticky 없음(M2). 잔고·미체결은 탭이 아니라 **독립 2섹션 세로 나열**(M3).
  - 브레이크포인트는 CSS(`@media`/Tailwind `lg:`)로 처리하고 JS 뷰포트 측정을 쓰지 않는다.
- **종목 전환 state sticky 방어**: `code`/`isin` 변경 시 거래소 토글·스크롤·에러 플래그를 리셋한다. `stock-comovement-section.tsx` L280-305 의 주석 형식을 그대로 옮겨 **"리셋 누락 = 다른 종목 호가로 주문하는 사고"** 이유를 박제한다.
- 가격 클릭 핸들러는 이 plan 에서 상태만 보관(`selectedPrice`)하고 15-18 의 주문 패널에 전달한다.

`stock-detail-client.tsx`: `orderbook` prop 의 placeholder 를 `<StockOrderbookSection code={stock.code} isin={stock.isin} basePrice={...} />` 로 교체하고 `stock-orderbook-placeholder` testid 를 제거한다. `isin` 은 15-10 이 `/api/stocks/:code` 에 추가한 필드에서 온다.
  </action>
  <acceptance_criteria>
    - `grep -c '실시간 호가·주문 권한이 없어요' webapp/src/components/stock/stock-orderbook-section.tsx` == 1
    - `grep -c '이 종목의 차트·뉴스·종목토론방은 그대로 이용할 수 있어요' webapp/src/components/stock/stock-orderbook-section.tsx` == 1
    - `grep -c 'return null' webapp/src/components/stock/stock-orderbook-section.tsx` == 0 (섹션 숨김 금지 — UI-SPEC C1)
    - `grep -c '380px 220px' webapp/src/components/stock/stock-orderbook-section.tsx` >= 1
    - `grep -c '실시간(DMA)' webapp/src/components/stock/stock-orderbook-section.tsx` >= 1 (출처 라벨 — D1)
    - `grep -c '이 종목은 NXT 호가가 없어요' webapp/src/components/stock/stock-orderbook-section.tsx` == 1
    - `grep -c 'orderbook-account-column' webapp/src/components/stock/stock-orderbook-section.tsx` >= 1
    - `grep -c 'stock-orderbook-placeholder' webapp/src/components/stock/stock-detail-client.tsx` == 0 (placeholder 제거됨)
    - `git diff --stat webapp/src/components/stock/stock-hero.tsx` 결과 없음 (히어로 무변경 — D1)
    - `pnpm --filter webapp run typecheck && pnpm --filter webapp run build` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter webapp run typecheck && pnpm --filter webapp test</automated>
  </verify>
  <done>섹션 셸이 게이트·빈·에러·정상 4분기와 L1=B 그리드/M1 모바일 순서를 갖추고 탭에 마운트됨. 히어로 무변경.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay wss 데이터 → 사다리/테이프 렌더 | 서버 값이 사용자가 클릭할 주문 가격이 된다 |
| allowlist 판정 결과 → UI 게이트 | 권한 없는 사용자에게 무엇을 보여줄지 결정 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14 | (안전성) 오주문 | 가격 클릭 → 주문 입력 | mitigate | **매수/매도 탭 자동 전환 금지**(매도호가를 클릭해도 매수 탭 유지). 색·위치·문구 3중 일치는 15-18 이 완성. 클릭은 가격만 채우고 제출하지 않는다 |
| T-15-40 | Tampering (오주문 유발) | 종목 전환 stale 표시 | mitigate | `code`/`isin` 변경 시 거래소·에러·선택 가격 리셋 + 훅이 구독 해제(15-12). 사유를 코드 주석에 박제 |
| T-15-21 | Elevation of Privilege | 권한 없음 게이트 | mitigate | `status === "unauthorized"` 또는 `isin === null` 이면 본문을 게이트 카드로 교체하고 구독·주문 진입점을 렌더하지 않는다. 서버측 판정(15-04/15-17)이 정본이며 UI 는 표시만 |
| T-15-44 | (접근성) 색 비의존 | 매수/매도 구분 | mitigate | `▲ 매수`/`▼ 매도` 부호+라벨 병기, 사다리는 열 위치 + `sr-only` 단계 라벨 + 시맨틱 `<table>`. 급등 종목에서 20단 전부 `--up` 이 되어도 구분 가능 |
| T-15-45 | DoS (렌더 성능) | 100ms 갱신 + 플래시 | mitigate | 플래시 큐잉 금지(최신만), 바 폭 transition 금지, 테이프는 200ms 배치 단위 1회 플래시. 링버퍼 200 상한. `prefers-reduced-motion` 전면 비활성 |
| T-15-43 | (안전성) 가격 오독 | 이중 가격(히어로 vs 호가창) | mitigate | 크기 위계(30px 스냅샷 / 20px 실시간) + `실시간(DMA) · 체결 HH:MM:SS` 출처 라벨 상시 노출(D1). 히어로는 수정하지 않는다 |
</threat_model>

<verification>
- `pnpm --filter webapp run typecheck && pnpm --filter webapp run build` exit 0
- `pnpm --filter webapp test` exit 0 (기존 테스트 회귀 없음)
- `grep -cE '\-\-primary' webapp/src/components/orderbook/orderbook-ladder.tsx` == 0
- `grep -c 'return null' webapp/src/components/stock/stock-orderbook-section.tsx` == 0
- `git diff --stat webapp/src/components/stock/stock-hero.tsx` 결과 없음
</verification>

<success_criteria>
- 호가 10단(중앙 가격 1열) + 체결 테이프가 UI-SPEC 확정값대로 렌더된다
- 권한 없는 사용자에게 게이트 카드가 뜨고 섹션이 사라지지 않는다
- 재접속 중 데이터가 유지되고(`opacity:.55`) 빈 화면으로 되돌아가지 않는다
- ≥900px 2축 / <900px M1 세로 순서가 CSS 로 처리된다
- 호가 클릭이 가격을 전달하되 매매 구분을 자동 전환하지 않는다
</success_criteria>

<output>
After completion, create `.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-13-SUMMARY.md`
</output>
