---
phase: 15-dma-relay-kb-gh-trade-server-10-wss
plan: 20
type: execute
wave: 6
depends_on: ["15-19", "15-14"]
files_modified:
  - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-LIVE-VERIFICATION.md
  - .planning/STATE.md
  - infra/relay/README.md
autonomous: false
requirements: [RELAY-01, RELAY-02, RELAY-03]
must_haves:
  truths:
    - "SC-8 / D-27: 실서버(10.41.1.120)·실계좌 접속은 **사용자의 명시 지시가 있을 때만** 수행되며, 지시가 없으면 이 plan 은 mock 상태로 종료하고 phase 를 완료 처리한다"
    - "SC-8 / D-03: VPN 선검증 결과(터널 IP·동시 세션·출발지 IP 제한·split-tunnel 라우팅)가 STATE.md 와 SUMMARY 에 기록돼 있다"
    - "SC-8 / D-17: 실서버 검증 시 gh-radar 용 DMA `user_id` 가 WinForms 값과 다른 값으로 users.toml 에 등록돼 있음이 확인된다 (값은 저장소에 미기록)"
    - "SC-1~SC-8: 8개 성공 기준의 충족 여부가 증거와 함께 한 문서에 집계된다"
  artifacts:
    - path: ".planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-LIVE-VERIFICATION.md"
      provides: "실서버 검증 기록 또는 '미수행(사용자 지시 없음)' 명시 + SC-1~8 집계"
      contains: "SC-8"
  key_links:
    - from: "15-VPN-PREFLIGHT.md"
      to: "15-LIVE-VERIFICATION.md"
      via: "선검증 결과 인용"
      pattern: "선검증"
    - from: "phase 결과"
      to: ".planning/STATE.md"
      via: "Accumulated Context 기록"
      pattern: "Phase 15"
---

<objective>
Phase 15 를 닫는다. **사용자의 명시 지시가 있을 때만** 실서버(10.41.1.120)·실계좌 접속을 1회 수행해 기록하고, 지시가 없으면 mock 검증 상태 그대로 SC-1~SC-8 을 집계해 완료 처리한다.

Purpose: SC-8 의 마지막 조항이자 이 phase 의 안전 계약이다. D-27 은 "실서버·실계좌 접속은 사용자 지시 전엔 절대 하지 않는다"이므로, 이 plan 의 기본 동작은 **접속하지 않는 것**이다. 지시가 있을 때만 게이트가 열린다.
Output: 검증 기록 문서 1 + STATE 갱신 + README 최종 상태.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-VALIDATION.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-19-SUMMARY.md

<interfaces>
<!-- ROADMAP Phase 15 성공 기준 8개 (집계 대상) -->
SC-1 relay 워크스페이스 등록 + 생성물 커밋 + `sync-relay-schema.sh --check` 무변경
SC-2 VM/고정 IP/방화벽 3규칙 + kbs124 VPN 선검증 기록(수동 ≤3회)
SC-3 프레이밍 코덱·LivePing·백오프 재접속 + vitest 가짜 서버 소켓 증명
SC-4 wss 첫 메시지 인증 + allowlist + 구독/스냅샷/체결 200ms 배치 + 5분 유예
SC-5 계좌 전부 선언 + 계좌 상태 스냅샷/델타 팬아웃 (gh-trade 17 재동기화 후)
SC-6 `POST /api/orders` 왕복 + 체결·취소확인 wss 푸시 + `dma_orders` + 409 + mock 가격 0 거부
SC-7 4탭 재구성 + 호가주문 탭 + Playwright wss 왕복 + Vercel env
SC-8 Dockerfile + 3스크립트 + 알림 정책 + VM 문서(비밀 미기록) + **실서버는 사용자 지시 전 금지**

<!-- 선행 기록 문서 -->
15-VPN-PREFLIGHT.md      (15-07) — D-03 선검증 7항목
15-MOCK-ORDER-EVIDENCE.md (15-19) — mock 주문 4케이스
infra/relay/README.md     — 배포 상태·이미지 SHA·INV 결과
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: [BLOCKING] 실서버·실계좌 검증 수행 여부 결정 (D-27)</name>
  <files>.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-LIVE-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md D-03, D-17, D-20, D-27 (실서버 금지 원칙과 선행 조건)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-VPN-PREFLIGHT.md (선행 조건 1 — 출발지 IP 제한·동시 세션·split-tunnel 결과)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-MOCK-ORDER-EVIDENCE.md (mock 으로 이미 검증된 범위 — 무엇이 남았는지 판단 근거)
    - infra/relay/README.md (현재 배포 상태 — `DMA_HOST` 값·이미지 SHA)
  </read_first>
  <action>
D-27 에 따라 실서버·실계좌 접속 여부를 **사용자에게 결정받는다**. Claude 는 이 선택을 스스로 하지 않으며 기본 경로는 미수행(A안)이다. 선행 조건 4가지(VPN 선검증 통과 · gh-trade 17 배포 · users.toml 에 WinForms 와 다른 gh-radar 전용 DMA user_id 등록 · `dma_credentials` 매핑)를 사용자에게 제시하고, 선택 결과를 `15-LIVE-VERIFICATION.md` 상단에 기록한다.
  </action>
  <decision>실서버(10.41.1.120) DMA 게이트웨이와 실계좌에 접속해 라이브 검증을 수행할 것인가</decision>
  <context>
Phase 15 의 모든 자동 검증은 **mock 게이트웨이**로 완료됐다(15-19). D-27 은 "실서버·실계좌 접속은 사용자 지시가 있을 때만"으로 못박혀 있으므로 Claude 는 이 결정을 스스로 내리지 않는다.

**실서버 검증의 선행 조건 4가지** — 아래가 모두 충족돼야 옵션 A 가 가능하다:
1. D-03 VPN 선검증 통과 (`15-VPN-PREFLIGHT.md` — 출발지 IP 제한 없음 + 동시 세션 가능 + split-tunnel 유지)
2. gh-trade Phase 17 배포 완료 (10.41.1.120 에 users.toml 배치)
3. users.toml 에 **gh-radar 전용 DMA `user_id`** 등록 (WinForms 값과 달라야 한다 — D-17. 같으면 게이트웨이가 세션을 합류시켜 전략 상태·계좌 범위가 섞인다)
4. `dma_credentials` 에 그 `user_id`/비밀번호 매핑 등록 (`scripts/dma-credentials.ts`)

실서버 검증은 **실계좌 주문 위험**을 동반한다. 서버측 금액·수량 한도가 없으므로(D-20) 오주문이 곧 실손실이다.
  </context>
  <options>
    <option id="skip-live">
      <name>A안: 실서버 검증 미수행 — mock 상태로 phase 완료</name>
      <pros>실계좌 위험 0. 선행 조건(gh-trade 17 배포·users.toml)이 아직 준비되지 않아도 phase 를 닫을 수 있다. 시세·주문 코드 경로는 이미 mock 으로 전부 검증됐다</pros>
      <cons>실서버 프로토콜 차이(mock 과 실브로커의 응답 코드·타이밍)가 미검증으로 남는다. 후속 quick task 로 분리해야 한다</cons>
    </option>
    <option id="live-quote-only">
      <name>B안: 실서버 **시세만** 검증 (주문 없음)</name>
      <pros>실브로커 호가·체결 데이터의 형식·타이밍을 실측할 수 있고 주문 위험은 0. 로그인·구독까지만 수행</pros>
      <cons>주문 경로(OrderResp 실응답 코드·체결 통보)는 여전히 미검증. VPN·계정 준비는 필요</cons>
    </option>
    <option id="live-full">
      <name>C안: 실서버 + 실계좌 최소 주문 1건 검증</name>
      <pros>전 경로 실증. 접수→체결/취소 통보→`dma_orders` 기록까지 실제로 확인</pros>
      <cons>**실손실 위험.** 최소 수량·시장 최저가 지정가로 제한하고 즉시 취소해야 한다. 장중에만 가능. 4가지 선행 조건이 모두 충족돼야 한다</cons>
    </option>
  </options>
  <acceptance_criteria>
    - 사용자가 A/B/C 중 하나를 명시적으로 선택했다
    - 선택 결과가 `15-LIVE-VERIFICATION.md` 상단에 기록됐다
    - A안 선택 시 실서버 접속이 **일어나지 않았음**이 확인된다 (`docker inspect gh-radar-relay` env 에 `10.41.1.120` 부재)
  </acceptance_criteria>
  <verify>
    <manual>사용자 결정 사항이다. Claude 가 스스로 실서버 접속을 선택할 수 없다(D-27).</manual>
    <automated>test -f .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-LIVE-VERIFICATION.md</automated>
  </verify>
  <done>사용자가 A/B/C 중 하나를 명시 선택했고 결과가 문서 상단에 기록됨. A안이면 실서버 접속 흔적 0.</done>
  <resume-signal>선택: skip-live, live-quote-only, 또는 live-full</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [조건부] 실서버 검증 실행 (Task 1 이 B 또는 C 를 선택한 경우에만)</name>
  <files>.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-LIVE-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-VPN-PREFLIGHT.md (선행 조건 1 — 선검증 결과)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-MOCK-ORDER-EVIDENCE.md (mock 기준선 — 실서버 응답과 대조)
    - infra/relay/README.md (VM 운영 절차 — VPN 기동·로그 확인)
    - scripts/deploy-relay.sh (실서버 접속은 `DMA_HOST=10.41.1.120` 명시 지정 시에만)
    - scripts/dma-credentials.ts (자격증명 등록 — D-17 user_id 규칙)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md D-17, D-20, D-27
  </read_first>
  <action>
Task 1 이 B/C 안을 선택한 경우에만 실행한다. 선행 조건 4가지를 먼저 확인하고 하나라도 미충족이면 중단한다. VPN 기동 후 split-tunnel(기본 경로 `ens4`) 유지를 확인하고 `DMA_HOST=10.41.1.120` 로 relay 를 배포해 로그인·계좌 목록·호가·체결·잔고를 실측한다. C안이면 **체결되지 않을 지정가 1주**로 신규 주문 → 접수 확인 → **즉시 취소** → 취소확인까지만 수행하고 체결시키지 않는다. 검증 후 `DMA_HOST` 되돌림 여부를 사용자에게 확인하고 결과를 `15-LIVE-VERIFICATION.md` 에 기록한다. 계좌번호는 뒤 4자리만, DMA user_id·비밀번호는 기록하지 않는다.
  </action>
  <what-built>
relay 가 VM 에서 돌고 있고 mock 으로 전 경로가 검증됐다. `DMA_HOST` 를 실서버로 바꾸고 VPN 을 올리면 실브로커에 붙는다.
  </what-built>
  <how-to-verify>
**Task 1 이 `skip-live` 를 선택했다면 이 태스크를 건너뛴다.**

**선행 조건 4가지를 먼저 확인한다.** 하나라도 미충족이면 중단하고 사용자에게 보고한다.
1. `15-VPN-PREFLIGHT.md` 의 ③ 출발지 IP 제한 없음 + ④ 동시 세션 가능 + ⑤ split-tunnel 유지가 전부 PASS.
2. gh-trade Phase 17 이 10.41.1.120 에 배포됨(사용자 확인).
3. users.toml 에 gh-radar 전용 `user_id` 등록 — **WinForms 값과 다른 값**(D-17). 값은 사용자에게만 확인하고 문서·로그·커밋에 적지 않는다.
4. `dma_credentials` 에 매핑 등록(`scripts/dma-credentials.ts`).

**실행 순서:**
1. VM 에서 `sudo systemctl start openconnect@kb` → `ip -br addr show tun0` + `ip route show default` 가 `ens4` 인지 확인(split-tunnel 유지).
2. `nc -zv 10.41.1.120 9100` 도달성 확인.
3. `DMA_HOST=10.41.1.120 bash scripts/deploy-relay.sh` — 배포 스크립트가 출력하는 **실서버 접속 모드 경고**를 확인한다.
4. 브라우저에서 `/stocks/005930?tab=orderbook` → 상태 배지가 `실시간 · 계좌 N개` 가 되는지, 계좌 목록이 실제 값인지 확인.
5. **호가·체결 실측** — 10단 호가·체결 테이프가 실데이터로 흐르는지. mock 과 달라진 필드(예: `change_sign`, `exchange_time` 형식, 벡터 길이)를 기록한다.
6. **잔고·미체결 실측** — `GetAccountStateReq` 응답의 실제 필드 값(계좌번호·보유 종목)을 확인한다. **문서에는 계좌번호를 뒤 4자리만 기록한다.**
7. **C안일 때만 — 최소 주문 1건:**
   - 최소 수량(1주) + **체결되지 않을 가격**(매수라면 시장 최저 호가보다 훨씬 낮은 지정가)으로 신규 주문.
   - 접수(`A`) 확인 → 미체결 목록에 등장 → **즉시 취소** → 취소확인(`C`) 통보 확인.
   - `dma_orders` 행의 status 전이(`requested` → `accepted` → `cancelled`) 확인.
   - **체결시키지 않는다.** 실손실 방지가 이 절차의 목적이다.
   - 주문 실패·이상 시 즉시 중단하고 사용자에게 보고한다.
8. 검증 후 `DMA_HOST` 를 다시 mock/로컬로 되돌릴지 실서버로 유지할지 **사용자에게 확인**한다.
9. `15-LIVE-VERIFICATION.md` 에 기록 — 접속 시각, 터널 IP, 계좌 수, 호가/체결 실측 차이, (C안) 주문 왕복 결과, `dma_orders` 전이, 되돌림 여부. **비밀번호·DMA user_id·전체 계좌번호는 기록하지 않는다.**
  </how-to-verify>
  <acceptance_criteria>
    - Task 1 이 `skip-live` 면 이 태스크가 `건너뜀`으로 기록되고 실서버 접속 흔적이 없다
    - B/C 안이면 `15-LIVE-VERIFICATION.md` 에 접속 시각·계좌 수·호가 실측 결과가 기록돼 있다
    - C 안이면 주문 status 전이(`requested` → `accepted` → `cancelled`)가 기록되고 **체결 0건**
    - 문서에 전체 계좌번호·DMA user_id·비밀번호 문자열이 0건 (`grep -riE 'kbs124|passwd|password:' | wc -l` == 0)
    - VPN 연결 중 `ip route show default` 가 `ens4` 를 유지했음이 기록됨
    - 검증 후 `docker inspect gh-radar-relay` 의 `DMA_HOST` 값이 사용자가 확인한 상태와 일치
  </acceptance_criteria>
  <verify>
    <manual>실서버·실계좌 접속은 사용자 지시가 있을 때만 수행되는 대화형 절차다(D-27). 자동 실행 경로를 만들지 않는다.</manual>
    <automated>test "$(grep -riE 'kbs124|passwd=|password:' .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-LIVE-VERIFICATION.md | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>A안이면 건너뜀 기록 + 접속 흔적 0. B/C안이면 실측 결과 기록, C안은 status 전이 기록 + 체결 0건, 비밀 값 0건.</done>
  <resume-signal>실서버 검증 결과 기록 후 "라이브 검증 완료" 또는 "건너뜀(A안)" 입력</resume-signal>
</task>

<task type="auto">
  <name>Task 3: SC-1~SC-8 집계 + STATE/README 최종 갱신</name>
  <files>.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-LIVE-VERIFICATION.md, .planning/STATE.md, infra/relay/README.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 15 절 — 성공 기준 8개 원문)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-VALIDATION.md (Per-Task Verification Map — 검증 상태 집계 원천)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-VPN-PREFLIGHT.md, 15-MOCK-ORDER-EVIDENCE.md (증거 문서)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-01-SUMMARY.md ~ 15-19-SUMMARY.md (각 plan 결과 — 큰 파일은 결론부만)
    - .planning/STATE.md (수정 대상 — Accumulated Context / Current Position / Performance Metrics)
    - infra/relay/README.md (수정 대상 — 최종 운영 상태)
  </read_first>
  <action>
`15-LIVE-VERIFICATION.md` 하단에 **SC-1~SC-8 집계표**를 만든다. 각 행: 기준 번호 · 요약 · 상태(✅/⚠/❌) · **증거**(명령 출력·파일 경로·plan 번호). 증거 없이 ✅ 를 쓰지 않는다.

기준별 증거 원천:
- SC-1 → `sync-relay-schema.sh --check` 무변경 출력 + `relay/src/generated` 파일 수 (15-01/15-15)
- SC-2 → `gcloud compute instances describe` + 방화벽 3행 + `15-VPN-PREFLIGHT.md` (15-06/15-07)
- SC-3 → `pnpm --filter @gh-radar/relay test` 통과 수 (codec/envelope/dma-client/session) (15-02/15-03)
- SC-4 → fanout/hub/credentials 테스트 + `smoke-relay.sh` INV-6 (15-04/15-08)
- SC-5 → `account-declare`/`account-state` 테스트 + 재동기화 diff (15-15/15-16)
- SC-6 → `server test orders` + `15-MOCK-ORDER-EVIDENCE.md` + `/api/orders` 409 실측 (15-17/15-19)
- SC-7 → `playwright test orderbook stock-detail-tabs` + Vercel env 검증 (15-11~15-14)
- SC-8 → `smoke-relay.sh` INV 전체 + 알림 정책 + 비밀 미기록 grep 결과 + **실서버 접속 여부**(Task 1/2)

**⚠ 로 남는 항목**(예: gh-trade 17 미완으로 SC-5/6 보류, VPN 선검증 실패로 SC-2 부분 충족, 실서버 미검증)은 **무엇이 남았고 어떤 조건에서 해소되는지**를 명시한다. 억지로 ✅ 로 만들지 않는다.

`.planning/STATE.md`:
- Current Position 을 Phase 15 완료 상태로 갱신.
- Accumulated Context §Roadmap Evolution 에 Phase 15 결과 한 문단 추가 — VM/고정 IP/방화벽 구성, VPN 선검증 결과 요약(터널 IP·동시 세션·출발지 IP 제한), relay 이미지 SHA, mock 검증 범위, 실서버 검증 여부, 남은 ⚠ 항목.
- Performance Metrics 표에 Phase 15 각 plan 행 추가.
- **비밀 값·전체 계좌번호는 기록하지 않는다.**

`infra/relay/README.md`: 최종 운영 상태(이미지 SHA, VPN 기동 정책, 알림 정책 이름, 익일 `--check-tls` 재확인 결과, `DMA_HOST` 현재 값)를 정리한다.

`15-VALIDATION.md` 의 Per-Task Verification Map Status 열을 실제 실행 결과(✅/❌/⚠️)로 갱신하고 `wave_0_complete: true` 로 바꾼다(Wave 0 산출물이 전부 생성됐으므로).
  </action>
  <acceptance_criteria>
    - `15-LIVE-VERIFICATION.md` 에 SC-1~SC-8 8행 집계표가 있고 각 행에 증거 열이 채워져 있다
    - ✅ 로 표기된 행마다 재현 가능한 명령 또는 파일 경로가 증거로 적혀 있다
    - `grep -c 'Phase 15' .planning/STATE.md` >= 2 (Roadmap Evolution + Current Position)
    - `grep -riE 'kbs124|passwd=|password:' .planning/STATE.md .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-LIVE-VERIFICATION.md infra/relay/README.md | wc -l` == 0
    - `15-VALIDATION.md` frontmatter 의 `wave_0_complete` == true
    - `.planning/STATE.md` Performance Metrics 표에 Phase 15 행이 존재
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -riE 'kbs124|passwd=|password:' .planning/STATE.md infra/relay/README.md .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-LIVE-VERIFICATION.md | wc -l | tr -d ' ')" = "0" && grep -c 'SC-8' .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-LIVE-VERIFICATION.md</automated>
  </verify>
  <done>SC-1~8 집계표 완성 + STATE/README/VALIDATION 갱신 + 비밀 값 0건.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude 의 판단 → 실서버 접속 | D-27 이 금지한 경로. 사용자 결정만이 게이트를 연다 |
| 실계좌 → 주문 | 서버 한도가 없으므로 오주문이 곧 실손실 |
| 검증 기록 문서 → 저장소 | 계좌번호·자격증명이 커밋될 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-28 | (안전성) 무단 실서버 접속 | Task 1 결정 게이트 | mitigate | `checkpoint:decision` 으로 사용자 선택을 강제. 기본 경로는 `skip-live`. Task 2 는 A안일 때 건너뛰며 acceptance 가 접속 흔적 부재를 검사(D-27) |
| T-15-14 | (안전성) 실계좌 오주문 | C안 최소 주문 | mitigate | 1주 + **체결되지 않을 지정가** + 즉시 취소 + 체결 0건 acceptance. 이상 시 즉시 중단. 서버 한도 없음(D-20)이므로 절차로만 방어 |
| T-15-47 | Spoofing (세션 합류) | DMA `user_id` | mitigate | 선행 조건 3 — WinForms 와 다른 값 확인(D-17). 값은 사용자에게만 확인하고 문서·커밋에 미기록 |
| T-15-11 | DoS (관리 경로 상실) | 실서버 VPN 기동 | mitigate | 실행 순서 ① 이 `ip route show default` 가 `ens4` 인지 확인. split-tunnel 이 깨지면 즉시 중단 |
| T-15-15 | Information Disclosure | 검증 기록 | mitigate | 계좌번호는 뒤 4자리만, DMA user_id·비밀번호는 미기록. acceptance 가 `grep` 0건을 검사(3개 문서 전부) |
| T-15-56 | Repudiation | 성공 기준 집계 | mitigate | ✅ 마다 재현 가능한 명령/파일 증거를 요구. 증거 없는 ✅ 금지, 미충족은 ⚠ 로 남기고 해소 조건을 명시 |
</threat_model>

<verification>
- `15-LIVE-VERIFICATION.md` SC-1~8 집계표 8행 + 증거 열
- `grep -riE 'kbs124|passwd=|password:'` 가 STATE/README/검증문서에서 0건
- Task 1 결정이 문서 상단에 기록됨
- A안 선택 시 `docker inspect gh-radar-relay` env 에 `10.41.1.120` 부재
- `15-VALIDATION.md` `wave_0_complete: true`
</verification>

<success_criteria>
- 실서버 접속 여부가 사용자 결정으로만 정해졌고 그 결정이 기록됐다
- (B/C안) 실서버 검증 결과가 mock 기준선과 대조돼 기록됐다
- SC-1~SC-8 이 증거와 함께 집계되고 미충족 항목의 해소 조건이 명시됐다
- STATE.md 가 Phase 15 결과를 반영하고 비밀 값이 어디에도 없다
</success_criteria>

<output>
After completion, create `.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-20-SUMMARY.md`
</output>
