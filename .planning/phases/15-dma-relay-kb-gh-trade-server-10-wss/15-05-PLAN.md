---
phase: 15-dma-relay-kb-gh-trade-server-10-wss
plan: 05
type: execute
wave: 2
depends_on: ["15-04"]
files_modified:
  - relay/src/index.ts
  - relay/src/order/order-api.ts
  - relay/tests/order-api.test.ts
  - relay/Dockerfile
  - relay/.dockerignore
  - scripts/dma-credentials.ts
  - dev.sh
  - relay/README.md
autonomous: true
requirements: [RELAY-01, RELAY-03]
must_haves:
  truths:
    - "SC-8 / D-05: relay 가 평문 ws(:8090) 와 내부 HTTP(:8091) 두 포트만 열고 TLS 는 Caddy 가 종단한다 — relay 프로세스는 인증서를 다루지 않는다"
    - "SC-8 / D-07: relay 는 Docker 컨테이너로 돌고 openconnect 는 host systemd 소관이라 컨테이너 재시작이 VPN 터널을 흔들지 않는다"
    - "SC-8 / D-19: `X-Relay-Secret` 헤더가 상수시간 비교로 검증되고 불일치 시 401 + 본문 미노출이며, `/healthz` 만 비밀 없이 통과한다"
    - "SC-8: 종료 시 모든 wss 를 close(1001) 하고 DMA 세션을 정리해 KB 게이트웨이에 고아 세션을 남기지 않는다"
    - "D-18 / D-17: 관리자 수기 등록 스크립트가 DMA 자격증명을 AES-GCM 으로 암호화해 `dma_credentials` 에 upsert 하며, 비밀번호는 stdin 으로만 받고 shell history·stdout 에 남지 않는다"
    - "D-40 / D-41: VPN 없이 로컬 mock(gh-trade `run-mac.sh` 또는 순수 net 스텁)으로 relay 를 띄울 수 있고 `dev.sh` 가 webapp:3100 · server:8080 · relay:8090/8091 을 함께 기동한다"
  artifacts:
    - path: "relay/src/index.ts"
      provides: "부팅 결선(config → supabase → SessionManager → Hub → WsFanout → OrderApi) + graceful shutdown"
      contains: "SIGTERM"
    - path: "relay/src/order/order-api.ts"
      provides: "내부 HTTP(express) — /healthz + X-Relay-Secret 미들웨어 (주문 라우트는 15-16)"
      contains: "timingSafeEqual"
    - path: "relay/Dockerfile"
      provides: "멀티스테이지 pnpm deploy + non-root + GIT_SHA"
      contains: "USER app"
    - path: "scripts/dma-credentials.ts"
      provides: "관리자 수기 자격증명 등록(AES-GCM upsert, stdin 비밀번호)"
      contains: "gh-radar-dma-cred-key"
  key_links:
    - from: "relay/src/index.ts"
      to: "relay/src/ws/fanout.ts"
      via: "http server + handleUpgrade"
      pattern: "handleUpgrade"
    - from: "relay/src/order/order-api.ts"
      to: "relay/src/config.ts"
      via: "RELAY_ORDER_SECRET"
      pattern: "relayOrderSecret"
    - from: "relay/Dockerfile"
      to: "packages/shared"
      via: "멀티스테이지 shared 빌드"
      pattern: "@gh-radar/shared"
---

<objective>
relay 를 **실행 가능한 프로세스**로 완성한다. 부팅 결선과 graceful shutdown, 내부 HTTP(`/healthz` + 공유 비밀 미들웨어), 컨테이너 이미지, 관리자 자격증명 등록 스크립트, 로컬 개발 기동을 갖춘다.

Purpose: 여기까지가 "인프라 없이 로컬에서 완전 검증 가능한" 마지막 지점이다(D-40). 이후 wave 는 GCP/VPN/DNS 라는 외부 좌표에 묶인다. 주문 라우트 본체는 D-25 게이트 뒤(15-16)이므로 이 plan 은 **`/healthz` 와 비밀 헤더 미들웨어까지만** 만든다.
Output: 엔트리포인트 1 + 내부 HTTP 1 + Dockerfile 1 + 관리자 스크립트 1 + dev.sh 수정 + README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-04-SUMMARY.md

<interfaces>
<!-- 15-03/15-04 산출 — index.ts 가 결선할 대상 -->
```ts
export class SessionManager { acquire(...); release(...); get(...); closeAll(): Promise<void>; stats(): {sessionCount:number; readyCount:number}; }
export class SubscriptionHub { subscribe(...); unsubscribe(...); resubscribeAll(...); on("fanout", cb); }
export class WsFanout { handleUpgrade(req, socket, head): void; closeAll(code: number): void; }
```

<!-- server/Dockerfile 복사 시 바꿀 곳 4군데: server/→relay/, @gh-radar/server→@gh-radar/relay, EXPOSE 8080→8090 8091, dist/server.js→dist/index.js -->
From server/Dockerfile:
```dockerfile
FROM node:22-alpine AS builder
RUN corepack enable && corepack prepare pnpm@10 --activate
...
RUN pnpm --filter=@gh-radar/server --prod --legacy deploy /out
FROM node:22-alpine
RUN addgroup -S app && adduser -S app -G app
ARG GIT_SHA=dev
ENV APP_VERSION=${GIT_SHA}
USER app
```

<!-- dev.sh 현행: 3100/8080 kill 후 기동 -->
From dev.sh:
```bash
lsof -ti :3100 -ti :8080 2>/dev/null | xargs kill -9 2>/dev/null || true
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: relay/src/index.ts 부팅 결선 + graceful shutdown</name>
  <files>relay/src/index.ts</files>
  <read_first>
    - server/src/server.ts (analog: 부팅 + graceful degradation + listen + SIGTERM 루프)
    - relay/src/config.ts, relay/src/logger.ts, relay/src/store/supabase.ts
    - relay/src/dma/session-manager.ts, relay/src/hub/subscription-hub.ts, relay/src/ws/fanout.ts
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md §A-3
  </read_first>
  <action>
`relay/src/index.ts` 를 만든다. 순서: `loadConfig()` → `logger` → `supabase`(서비스롤) → `SessionManager` → `SubscriptionHub`(SessionManager 주입) → `WsFanout`(verifyToken·credentials·SessionManager·Hub 주입) → `node:http` 서버 생성 후 `server.on("upgrade", (req, socket, head) => fanout.handleUpgrade(req, socket, head))` → `WS_PORT`(8090) listen → `createOrderApi()` 를 `ORDER_API_PORT`(8091) 로 listen.

부팅 로그 1건에 `{wsPort, orderApiPort, dmaHost, dmaPort, env, version}` 을 남긴다(비밀 없음).

**graceful shutdown** — `SIGTERM`/`SIGINT` 에서 `process.exit(0)` 직전에 반드시:
1. HTTP 서버 2개 `close()`
2. `fanout.closeAll(1001)` — 모든 wss 를 정상 close
3. `await sessionManager.closeAll()` — 구독 해제 + DMA 소켓 종료
4. 5초 안에 끝나지 않으면 강제 exit(타이머)

Docker `--restart=always` 로 재시작될 때마다 KB 게이트웨이에 고아 세션을 남기지 않는 것이 목적임을 주석으로 남긴다.

`unhandledRejection` / `uncaughtException` 핸들러를 두어 `logger.fatal` 후 exit(1) — 조용한 죽음 금지.
  </action>
  <acceptance_criteria>
    - `grep -c 'handleUpgrade' relay/src/index.ts` == 1
    - `grep -c 'SIGTERM' relay/src/index.ts` >= 1 이고 `grep -c 'closeAll' relay/src/index.ts` >= 2 (fanout + sessionManager)
    - `grep -c '1001' relay/src/index.ts` >= 1
    - `grep -c 'unhandledRejection' relay/src/index.ts` >= 1
    - `pnpm --filter @gh-radar/relay run build` exit 0 이고 `test -f relay/dist/index.js` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @gh-radar/relay run build && node -e "require('fs').accessSync('relay/dist/index.js')"</automated>
  </verify>
  <done>부팅 결선 + upgrade 라우팅 + graceful shutdown 4단계 구현, 빌드 산출물 존재.</done>
</task>

<task type="auto">
  <name>Task 2: 내부 HTTP — /healthz + X-Relay-Secret 상수시간 검증</name>
  <files>relay/src/order/order-api.ts, relay/tests/order-api.test.ts</files>
  <read_first>
    - server/src/app.ts (analog: 미들웨어 결선 순서 + app.locals DI + errorHandler 마지막)
    - server/src/middleware/error-handler.ts, server/src/errors.ts (analog: `{error:{code,message}}` envelope — relay 도 동일 형식이어야 server 가 재매핑하기 쉽다)
    - relay/src/config.ts (`relayOrderSecret`, `orderApiPort`)
    - relay/src/dma/session-manager.ts (`stats()` — /healthz 페이로드)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md §A-13, §S-1
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md §Open Questions 4 (/healthz 노출 범위)
  </read_first>
  <action>
`relay/src/order/order-api.ts` 에 `createOrderApi(deps): Express` 를 만든다. **CORS·rate-limit 은 두지 않는다**(내부 전용, Cloud Run 만 호출). 미들웨어 순서: `express.json({limit:"16kb"})` → **공유 비밀 검사 미들웨어** → 라우터 → 404 → errorHandler.

공유 비밀 미들웨어:
- 헤더 `X-Relay-Secret` 을 `config.relayOrderSecret` 과 **`crypto.timingSafeEqual`** 로 비교한다(길이가 다르면 먼저 false — `timingSafeEqual` 은 길이 불일치 시 throw 하므로 길이 검사를 선행). 문자열 `===` 비교 금지.
- 불일치/부재 → `401 {error:{code:"UNAUTHORIZED_RELAY", message:"..."}}`. **요청 본문·기대값을 응답이나 로그에 싣지 않는다.**
- **`/healthz` 는 비밀 없이 통과**시킨다(`req.path === "/healthz"` skip) — Caddy 를 통해 uptime check 대상이 되기 때문.

라우트:
- `GET /healthz` → `{status: "ok"|"degraded", vpn: boolean, dma: boolean, version, sessionCount}` 만 반환한다. **계좌번호·userId·DMA user_id 를 절대 포함하지 않는다.** `degraded` 조건: DMA 세션이 하나도 Ready 가 아니거나(활성 세션이 있을 때) 게이트웨이 도달 불가. `vpn` 은 게이트웨이 TCP 도달성 최근 결과로 근사한다. 컨테이너 내부 에러가 uptime check 로 잡히려면 `/healthz` 가 degraded 를 **HTTP 503** 으로 내려야 한다는 점을 주석에 남긴다(RESEARCH Assumption A7).
- `POST /internal/orders` — **이 plan 에서는 만들지 않는다.** `// TODO(D-25): 주문 라우트는 15-16 에서 추가` 마커만 남긴다.

에러 envelope 는 server 와 동일한 `{error:{code,message}}` 형식(S-1). 프로덕션에서 `err.message` 를 노출하지 않는다.

`relay/tests/order-api.test.ts` (supertest): ① `/healthz` 는 비밀 헤더 없이 200 ② `/healthz` 응답에 `accountNo`/`userId`/`dmaUserId` 키가 없음 ③ 임의 내부 경로에 비밀 헤더 없음 → 401 + 본문에 기대값 문자열 없음 ④ 잘못된 비밀 → 401 ⑤ 올바른 비밀 → 미들웨어 통과(404 라우터 도달) ⑥ 비밀 길이가 다른 값 → throw 없이 401 ⑦ 세션 0개일 때 `/healthz` 가 `status:"ok"` (활성 세션이 없으면 degraded 가 아니다).
  </action>
  <acceptance_criteria>
    - `grep -c 'timingSafeEqual' relay/src/order/order-api.ts` >= 1
    - `grep -c 'healthz' relay/src/order/order-api.ts` >= 2
    - `grep -c 'TODO(D-25)' relay/src/order/order-api.ts` >= 1
    - `grep -c 'cors\|rateLimit' relay/src/order/order-api.ts` == 0 (내부 전용)
    - `grep -c 'accountNo\|account_no' relay/src/order/order-api.ts` == 0 (healthz 페이로드에 계좌 정보 없음)
    - `pnpm --filter @gh-radar/relay test order-api` exit 0, 테스트 7건 이상 통과
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test order-api</automated>
  </verify>
  <done>/healthz 200 + 공유 비밀 상수시간 검증 401 경로가 7+ 테스트로 증명됨.</done>
</task>

<task type="auto">
  <name>Task 3: Dockerfile + 관리자 자격증명 등록 스크립트 + dev.sh + README</name>
  <files>relay/Dockerfile, relay/.dockerignore, scripts/dma-credentials.ts, dev.sh, relay/README.md</files>
  <read_first>
    - server/Dockerfile (복사 원본 — 멀티스테이지 pnpm deploy --legacy, non-root, GIT_SHA)
    - webapp/scripts/seed-test-user.ts (analog: tsx shebang + 필수 env·동작·exit code 3블록 docblock + service_role 클라이언트)
    - dev.sh (수정 대상 — 포트 kill 목록 + 프로세스 기동 블록)
    - relay/src/store/credentials.ts (15-04 산출 — `encryptDmaPassword` 재사용)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md §A-2, §G-5
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md D-17, D-40, D-41
  </read_first>
  <action>
**`relay/Dockerfile`**: `server/Dockerfile` 을 복사하고 4군데만 바꾼다 — ① `COPY server/package.json ./server/` → `relay/`, ② `pnpm -F @gh-radar/server build` → `@gh-radar/relay`, `pnpm --filter=@gh-radar/server --prod --legacy deploy /out` → `@gh-radar/relay`, ③ `EXPOSE 8080` → `EXPOSE 8090 8091`, ④ `CMD ["node","dist/server.js"]` → `["node","dist/index.js"]`. **`corepack prepare pnpm@10` 은 그대로 유지**(로컬 pnpm 11 과 의도적으로 다름). `USER app` non-root 유지. `relay/.dockerignore` 에 `node_modules`, `dist`, `.env` 추가.

**`scripts/dma-credentials.ts`**: `#!/usr/bin/env tsx` shebang + docblock 3블록(실행 명령 / 필수 env / 동작·exit code).
- 사용법: `pnpm exec tsx scripts/dma-credentials.ts --email <gh-radar 계정 이메일> --dma-user <DMA user_id>` — 비밀번호는 **인자·env 로 받지 않고 stdin prompt**(`readline` + echo off)로 받는다. shell history 에 남기지 않는 것이 목적.
- AES 키는 `DMA_CRED_KEY` env 가 있으면 그것을, 없으면 `gcloud secrets versions access latest --secret=gh-radar-dma-cred-key` 로 읽는다. 실패 시 "`setup-relay-iam.sh` 로 secret 을 만들고 실행 주체에 secretAccessor 를 부여하세요" 안내를 출력한다(RESEARCH Open Question 3).
- 이메일 → `auth.users` id 조회(admin API) → `encryptDmaPassword(plain, userId, key)` → `dma_credentials` upsert(`onConflict: user_id`). 멱등.
- **평문 비밀번호·AES 키를 stdout/stderr 에 출력하지 않는다.** 성공 시 `등록 완료: user_id=<uuid> dma_user_id=<value>` 만.
- docblock 에 D-17 을 명시한다 — **gh-radar 용 DMA `user_id` 는 WinForms 클라이언트가 쓰는 값과 달라야 한다**(게이트웨이가 user_id+broker 로 세션을 합류시키므로 같은 값이면 전략 상태·계좌 범위가 섞인다). 실제 값은 이 저장소에 적지 않는다.
- 부수적으로 `--list` 서브커맨드로 등록된 매핑 목록(user_id, dma_user_id, updated_at — **비밀번호 제외**)을 출력한다.

**`dev.sh`**: kill 대상 포트에 `:8090`, `:8091` 추가. relay 기동 블록을 추가하되 **기본은 비활성(opt-in)** 으로 둔다 — `--with-relay` 플래그가 있을 때만 `pnpm --filter @gh-radar/relay dev` 를 띄운다(로컬에서 mock 게이트웨이 없이 relay 를 띄우면 재접속 로그만 쌓인다). 헤더 주석에 로컬 포트 규약(`webapp:3100 · server:8080 · relay ws:8090 · relay internal:8091`)과 `NEXT_PUBLIC_RELAY_WS_URL=ws://localhost:8090/ws` 를 명시한다(D-41).

**`relay/README.md`**: ① 로컬 실행 순서 — gh-trade mock 기동(`cd ../gh-trade/server && ./scripts/build.sh && ./scripts/run-mac.sh`, 포트 9100, `[broker] name="mock"`) 또는 vitest 스텁 사용, `server/scripts/uat/inject_b6.py --send` 로 가짜 호가 주입 ② 필수 env 표 ③ **9100 포트 충돌 시 UAT 사본 toml 로 포트 이동** ④ **D-27 경고: 실서버(10.41.1.120)·실계좌 접속은 사용자 지시가 있을 때만** — 기본 `DMA_HOST` 는 로컬 mock(`127.0.0.1`)으로 두고 실서버 주소는 배포 env 로만 주입한다. 비밀번호·계정 값은 문서에 적지 않는다.
  </action>
  <acceptance_criteria>
    - `grep -c '@gh-radar/relay' relay/Dockerfile` >= 2 이고 `grep -c '@gh-radar/server' relay/Dockerfile` == 0
    - `grep -c 'EXPOSE 8090 8091' relay/Dockerfile` == 1 이고 `grep -c 'USER app' relay/Dockerfile` == 1
    - `grep -c 'corepack prepare pnpm@10' relay/Dockerfile` == 1
    - `docker build --platform=linux/amd64 -f relay/Dockerfile -t gh-radar-relay:planbuild .` exit 0
    - `grep -c 'gh-radar-dma-cred-key' scripts/dma-credentials.ts` >= 1 이고 `grep -c 'process.argv' scripts/dma-credentials.ts` >= 1
    - `grep -cE 'console\.log\(.*(plain|password|key)' scripts/dma-credentials.ts` == 0
    - `grep -c '8090' dev.sh` >= 1 이고 `grep -c '3100' dev.sh` >= 1
    - `grep -v '^#' relay/README.md | grep -ci 'kbs124\|비밀번호 값' == 0` — 비밀번호/계정 값 문자열 0건
    - `grep -c '10.41.1.120' relay/README.md` >= 1 이고 같은 문단에 `D-27` 경고 존재
  </acceptance_criteria>
  <verify>
    <automated>bash -n dev.sh && pnpm exec tsx --version >/dev/null && docker build --platform=linux/amd64 -f relay/Dockerfile -t gh-radar-relay:planbuild . >/dev/null</automated>
  </verify>
  <done>relay 이미지가 amd64 로 빌드되고, 관리자 등록 스크립트·dev.sh·README 가 존재하며 비밀 값이 기록돼 있지 않다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud Run(Direct VPC Egress) → relay 내부 HTTP :8091 | 서브넷 출발지 + 공유 비밀 헤더 이중 방어의 애플리케이션 쪽 절반 |
| 공인망 → Caddy → relay ws :8090 | TLS 종단은 Caddy, relay 는 평문 ws 만 listen |
| 관리자 로컬 → Secret Manager → Supabase | 자격증명 등록 경로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Spoofing / Elevation | `order-api.ts` 공유 비밀 검사 | mitigate | `crypto.timingSafeEqual` 상수시간 비교(길이 선검사) + 불일치 401 + 기대값·본문 미노출. Task 2 ③④⑥ 테스트가 단언. 네트워크 쪽 절반(방화벽 source-ranges)은 15-06/15-07 |
| T-15-22 | Information Disclosure | 공개 `/healthz` | mitigate | 페이로드를 `{status, vpn, dma, version, sessionCount}` 로 제한 — 계좌번호·userId·DMA user_id 미포함(grep acceptance) |
| T-15-05 | Information Disclosure | `scripts/dma-credentials.ts` | mitigate | 비밀번호는 stdin prompt 전용(인자·env 금지 → shell history 미기록), 평문·AES 키 stdout 미출력, AES 키는 Secret Manager 에서만 |
| T-15-23 | Elevation of Privilege | 컨테이너 런타임 | mitigate | Dockerfile `USER app` non-root 유지 + `pnpm deploy --prod` 로 dev 의존성 미포함 |
| T-15-24 | DoS (고아 세션) | 컨테이너 재시작 | mitigate | graceful shutdown 이 wss close(1001) + `sessionManager.closeAll()` 을 수행. `--restart=always` 재시작마다 KB 게이트웨이에 세션이 쌓이지 않는다 |
| T-15-25 | Tampering (오접속) | 기본 `DMA_HOST` | mitigate | README·config 기본값을 로컬 mock 으로 두고 실서버 주소는 배포 env 로만 주입(D-27). 로컬 실행이 실수로 10.41.1.120 에 붙지 않는다 |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/relay test` exit 0
- `pnpm --filter @gh-radar/relay run build` exit 0
- `docker build --platform=linux/amd64 -f relay/Dockerfile -t gh-radar-relay:planbuild .` exit 0
- `bash -n dev.sh` exit 0
- `grep -c 'timingSafeEqual' relay/src/order/order-api.ts` >= 1
</verification>

<success_criteria>
- relay 가 로컬에서 ws:8090 + 내부:8091 로 기동되고 `/healthz` 가 200
- 공유 비밀 헤더가 상수시간 비교로 검증되고 불일치는 401
- amd64 컨테이너 이미지가 빌드되고 non-root 로 실행
- 관리자가 `scripts/dma-credentials.ts` 로 매핑을 등록할 수 있고 비밀 값이 어디에도 남지 않는다
- 종료 시 wss·DMA 세션이 정리된다
</success_criteria>

<output>
After completion, create `.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-05-SUMMARY.md`
</output>
