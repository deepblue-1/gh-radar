---
phase: 15-dma-relay-kb-gh-trade-server-10-wss
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - relay/package.json
  - relay/tsconfig.json
  - relay/vitest.config.ts
  - relay/.gitignore
  - relay/src/config.ts
  - relay/src/logger.ts
  - relay/tests/setup.ts
  - relay/src/generated/
  - packages/shared/src/relay.ts
  - packages/shared/src/index.ts
autonomous: true
requirements: [RELAY-01]
must_haves:
  truths:
    - "SC-1 / D-26: `relay/` 가 pnpm 워크스페이스 멤버이고 gh-trade `sync-relay-schema.sh` 산출물이 커밋돼 있으며 `--check` 가 무변경으로 통과한다 (생성물은 손으로 만들거나 고치지 않는다)"
    - "SC-1 / D-01: relay 가 루트 `pnpm typecheck` / `pnpm -r run test` 에 자동으로 포함된다 (typecheck·build·test 스크립트 3종 존재)"
    - "D-25: 현행 스키마(`LoginResp{success,message}`, `accounts` 필드 없음) 기준으로 초반 wave 가 컴파일된다 — 계좌 접근자를 참조하는 코드가 이 시점에 존재하지 않는다"
    - "SC-4: server·webapp·relay 3자가 공유하는 wss 메시지·주문 DTO 계약이 `@gh-radar/shared` 단일 진실 소스에 있다"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "relay 워크스페이스 등록"
      contains: "- relay"
    - path: "relay/package.json"
      provides: "@gh-radar/relay 패키지 정의 (type module, engines node>=22, typecheck/build/test)"
      contains: "@gh-radar/relay"
    - path: "relay/tsconfig.json"
      provides: "ESM + NodeNext (생성 코드 .js import 대응)"
      contains: "NodeNext"
    - path: "relay/src/generated/StockDMA.fbs"
      provides: "gh-trade 정본 스키마 사본 + SYNC MARKER"
      contains: "SYNC MARKER"
    - path: "packages/shared/src/relay.ts"
      provides: "wss 인바운드/아웃바운드 메시지 + 주문 DTO + 상태 라벨 계약"
      contains: "RELAY_STATE_LABELS"
  key_links:
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/relay.ts"
      via: "barrel export"
      pattern: "from \"./relay\""
    - from: "relay/package.json"
      to: "@gh-radar/shared"
      via: "workspace dependency"
      pattern: "\"@gh-radar/shared\""
---

<objective>
`relay/` 워크스페이스의 뼈대를 세우고, gh-trade 의 `sync-relay-schema.sh` 로 FlatBuffers 생성물을 받아 커밋하며, server·webapp·relay 3자가 공유할 wss/주문 계약 타입을 `packages/shared/src/relay.ts` 에 정의한다.

Purpose: SC-1 의 전부. 생성물 동기화는 `relay/` 디렉터리 + `pnpm-workspace.yaml` 등록 + `pnpm install` 이 선행 조건(스크립트 가드 0)이므로 이 순서를 한 plan 안에서 강제한다. 계약 타입을 먼저 못박아 이후 wave 들이 서로를 탐색하지 않고 병렬 구현할 수 있게 한다(interface-first).
Output: relay 워크스페이스 config 5종 + `relay/src/generated/**` 40개 + shared 계약 1종.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md

<interfaces>
<!-- 워크스페이스 등록 현황 (실측). `- relay` 를 추가하지 않으면 sync-relay-schema.sh 가드 0 도, 루트 typecheck 도 relay 를 보지 못한다. -->
From pnpm-workspace.yaml:
```yaml
packages:
  - webapp
  - server
  - workers/*
  - packages/*
onlyBuiltDependencies:
  - esbuild
  - msw
  - protobufjs
  - sharp
  - unrs-resolver
```

<!-- 루트 스크립트: relay 에 typecheck/build/test 가 없으면 조용히 빠진다. -->
From package.json:
```json
"scripts": {
  "typecheck": "pnpm --filter @gh-radar/shared build && pnpm -r run typecheck",
  "build": "pnpm --filter @gh-radar/shared build && pnpm -r run build",
  "lint": "pnpm -r run lint"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: relay 워크스페이스 스캐폴드 + pnpm 등록</name>
  <files>pnpm-workspace.yaml, relay/package.json, relay/tsconfig.json, relay/vitest.config.ts, relay/.gitignore, relay/src/config.ts, relay/src/logger.ts, relay/tests/setup.ts</files>
  <read_first>
    - pnpm-workspace.yaml (수정 대상 — 현재 상태 확인)
    - server/package.json (analog: type/engines/scripts 형태)
    - server/tsconfig.json (analog: ESM + NodeNext, exclude 규약)
    - server/vitest.config.ts (analog: include 2경로 + setupFiles)
    - server/src/config.ts (analog: get()/optional() env 로더 + optional env 주석 규약)
    - server/src/logger.ts (analog: pino + createGcpLoggingPinoConfig + redact paths)
    - server/tests/setup.ts (analog: 테스트용 env 주입)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md §A-1, §A-4, §A-5
  </read_first>
  <action>
`pnpm-workspace.yaml` 의 `packages:` 목록에 `- relay` 를 추가한다(`- server` 다음 줄).

`relay/package.json`: name `@gh-radar/relay`, `private: true`, `type: "module"`, `engines.node >= 22`, scripts `dev`(tsx watch -r dotenv/config src/index.ts) · `start`(node dist/index.js) · `build`(tsc) · `typecheck`(tsc --noEmit) · `test`(vitest run) 5종. dependencies: `ws` ^8.21.3, `flatbuffers` ^25.9.23, `@supabase/supabase-js`(server 와 동일 버전), `express` ^5.2.1, `zod` ^4, `pino` ^10.3.1, `pino-http` ^11, `@google-cloud/pino-logging-gcp-config` ^1.3.3, `@gh-radar/shared`(workspace:*). devDependencies: `@types/ws` ^8.18.1, `@types/express`, `@types/node`, `typescript`, `vitest` ^4.1.4, `tsx` ^4.21.0, `dotenv` ^16.4. **주의: `test` 는 watch 모드가 아니라 `vitest run` 이어야 한다**(CI/plan 검증이 멈추지 않게).

`relay/tsconfig.json`: `extends: "../tsconfig.base.json"`, `outDir: "./dist"`, `rootDir: "./src"`, `module: "NodeNext"`, `moduleResolution: "NodeNext"`, `types: ["node"]`, `include: ["src/**/*"]`, `exclude: ["node_modules","dist","tests","src/**/*.test.ts"]`. **워커의 commonjs 패턴을 따르지 않는다** — 생성 코드가 `.js` 확장자로 import 하므로 server 와 동일한 ESM/NodeNext 여야 한다(D-26).

`relay/vitest.config.ts`: `globals: false`, `environment: "node"`, `include: ["tests/**/*.test.ts","src/**/*.test.ts"]`, `setupFiles: ["tests/setup.ts"]`.

`relay/tests/setup.ts`: `loadConfig()` 가 테스트에서 throw 하지 않도록 필수 env(`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `DMA_CRED_KEY`, `RELAY_ORDER_SECRET`, `DMA_HOST`, `DMA_PORT`)에 더미 값을 주입한다(server/tests/setup.ts 선례).

`relay/src/config.ts`: server 선례의 `get()`(필수, 미설정 시 throw) / `optional()` 로더. 키 목록 — 필수: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `DMA_CRED_KEY`(base64 32B AES 키), `RELAY_ORDER_SECRET`. 선택(기본값 명시): `WS_PORT`(8090), `ORDER_API_PORT`(8091), `DMA_HOST`(10.41.1.120), `DMA_PORT`(9100), `DMA_BROKER`("KB"), `SESSION_GRACE_MS`(300000), `LOG_LEVEL`(info), `APP_VERSION`(dev), `NODE_ENV`. 각 optional 키에 "미설정 시 어떤 동작이 되는지" 주석을 단다(server/src/config.ts L18-25 규약).

`relay/src/logger.ts`: server/src/logger.ts 를 복제하되 `serviceContext.service` 를 `"gh-radar-relay"` 로 바꾸고 redact paths 에 다음을 **추가**한다 — `*.password`, `*.dma_password`, `*.dmaPassword`, `*.dma_password_enc`, `*.DMA_CRED_KEY`, `*.RELAY_ORDER_SECRET`, `*.token`, `req.headers['x-relay-secret']`.

`relay/.gitignore`: `dist/`, `node_modules/`, `.env` (생성물 `src/generated/` 는 **커밋 대상이므로 절대 무시하지 않는다**).

마지막에 저장소 루트에서 `pnpm install` 을 실행해 lockfile 에 relay 를 반영한다.

파일 상단 docblock 4블록 규약(PATTERNS S-6): ① `Phase 15 Plan 01 — RELAY-01` ② 무엇을 하는지 ③ D-번호 역참조 ④ 하지 않는 것/함정.
  </action>
  <acceptance_criteria>
    - `grep -c '^  - relay$' pnpm-workspace.yaml` == 1
    - `node -e "const p=require('./relay/package.json'); if(p.name!=='@gh-radar/relay')process.exit(1); if(p.type!=='module')process.exit(1); for(const s of ['typecheck','build','test'])if(!p.scripts[s])process.exit(1)"` exit 0
    - `grep -c '"test": "vitest run"' relay/package.json` == 1 (watch 모드 금지)
    - `grep -c 'NodeNext' relay/tsconfig.json` >= 2
    - `grep -v '^#' relay/src/logger.ts | grep -c 'RELAY_ORDER_SECRET'` >= 1 이고 `grep -v '^#' relay/src/logger.ts | grep -c 'gh-radar-relay'` >= 1
    - `pnpm --filter @gh-radar/relay exec node -e "1"` exit 0 (워크스페이스가 relay 를 인식)
    - `grep 'generated' relay/.gitignore` 결과 없음
  </acceptance_criteria>
  <verify>
    <automated>pnpm install --frozen-lockfile=false >/dev/null && pnpm --filter @gh-radar/relay run typecheck</automated>
  </verify>
  <done>relay 워크스페이스가 pnpm 에 등록되고 typecheck 가 exit 0. config/logger/vitest 설정 존재.</done>
</task>

<task type="auto">
  <name>Task 2: sync-relay-schema.sh 실행 + 생성물 커밋 (SC-1)</name>
  <files>relay/src/generated/</files>
  <read_first>
    - /Users/alex/repos/gh-trade/server/scripts/sync-relay-schema.sh (실행할 스크립트 — 가드 3종·`--check` 동작 확인)
    - /Users/alex/repos/gh-trade/server/src/protocol/StockDMA.fbs (스키마 정본 — 현행에 `accounts`/`AccountEntry` 가 없음을 직접 확인)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md §Pitfall 1, §Code Examples 6 (MsgType 실측표)
  </read_first>
  <action>
`/Users/alex/repos/gh-trade/server/scripts/sync-relay-schema.sh` 를 **인자 없이 1회 실행**해 `relay/src/generated/` 를 생성한다. 생성물은 `StockDMA.ts`, `stock-dma.ts`, `stock-dma/*.ts` 38개, SYNC MARKER 가 붙은 `StockDMA.fbs` 사본이다(총 40 파일 전후 — 스크립트가 보고하는 개수를 SUMMARY 에 기록).

**생성물을 손으로 만들거나 고치지 않는다(D-26).** 스크립트가 flatc 25.12.19 버전 가드를 갖고 있으므로 버전 불일치 시 중단하고 사용자에게 보고한다.

실행 후 `sync-relay-schema.sh --check` 를 돌려 **무변경**을 확인한다(멱등성 = acceptance).

생성물이 relay 의 `typecheck` 를 깨지 않는지 확인한다. 만약 생성 코드가 `relay/tsconfig.json` 의 strict 옵션과 충돌하면 **생성 코드를 고치지 말고** `relay/tsconfig.json` 의 `include`/`exclude` 또는 컴파일러 옵션 쪽에서 해결한다(예: `src/generated/**` 를 `skipLibCheck`/개별 옵션으로 완화). 어떤 완화를 했는지 tsconfig 주석에 남긴다.

생성물의 실측 사실 2건을 SUMMARY 에 기록한다: ① `LoginResp` 에 `accounts()` 접근자가 **없다**(D-25 게이트의 근거) ② `Envelope` 에 `createEnvelope()` 정적 함수가 **없다**(start/add/end 조립 강제).
  </action>
  <acceptance_criteria>
    - `test -f relay/src/generated/StockDMA.fbs` exit 0 이고 `grep -c 'SYNC MARKER' relay/src/generated/StockDMA.fbs` >= 1
    - `ls relay/src/generated/stock-dma/*.ts | wc -l` >= 30
    - `grep -c 'accounts' relay/src/generated/stock-dma/login-resp.ts` == 0 (현행 스키마 실측 — D-25 게이트 근거)
    - `grep -c 'static createEnvelope' relay/src/generated/stock-dma/envelope.ts` == 0 (start/add/end 조립 강제)
    - `bash /Users/alex/repos/gh-trade/server/scripts/sync-relay-schema.sh --check` exit 0 (무변경)
    - `git status --porcelain relay/src/generated | wc -l` == 0 (커밋 완료 후)
  </acceptance_criteria>
  <verify>
    <automated>bash /Users/alex/repos/gh-trade/server/scripts/sync-relay-schema.sh --check && pnpm --filter @gh-radar/relay run typecheck</automated>
  </verify>
  <done>`relay/src/generated/` 40개 전후 커밋됨. `--check` 무변경 통과. relay typecheck exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: packages/shared/src/relay.ts 계약 타입 (3자 공유)</name>
  <files>packages/shared/src/relay.ts, packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/chat.ts (analog: 계약 파일 docblock 규약 + UI 라벨 상수를 계약에 두는 선례 `SPECIALIST_LABELS`)
    - packages/shared/src/index.ts (수정 대상 — barrel export 형식)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md §Pattern 6 (wss 메시지 스키마 제안표)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-UI-SPEC.md §Copywriting Contract "연결 상태 배지 6종 + 게이트 2종"
  </read_first>
  <action>
`packages/shared/src/relay.ts` 를 신설한다. 파일 상단 docblock 에 "server·webapp·relay **3자**가 공유하는 단일 진실 소스"임을 명시하고 D-11/D-33/D-36/D-22 를 역참조한다.

정의할 것:

1. **인바운드(브라우저 → relay)** discriminated union `RelayInbound` — `t` 필드로 분기.
   - `{t:"auth", token: string}` (첫 메시지 필수, D-11)
   - `{t:"sub", isin: string, ex: RelayExchange}`
   - `{t:"unsub", isin: string, ex: RelayExchange}`
2. **`RelayExchange = "KRX" | "NXT"`** (D-04).
3. **세션 상태** `RelaySessionState = "connecting" | "logging_in" | "declaring" | "ready" | "reconnecting" | "manual_required" | "failed" | "session_rejected" | "unauthorized"`.
4. **`RELAY_STATE_LABELS: Record<RelaySessionState, string>`** — UI-SPEC §Copywriting 의 배지 라벨 verbatim(`시세 서버 연결 중…` / `DMA 로그인 중…` / `계좌 확인 중…` / `실시간` / `재접속 중` / `다시 연결하지 못했어요` / `회선 단절` / `로그인 거부` / `권한 없음`). 브라우저가 상태 switch 를 중복 구현하지 않게 하는 것이 목적(D-36 사상, `SPECIALIST_LABELS` 선례).
5. **아웃바운드(relay → 브라우저)** union `RelayOutbound` — `t` 값 `state` / `q` / `tape` / `acct` / `order` / `msg`. 필드는 RESEARCH §Pattern 6 표의 축약 키를 그대로 쓰되, **가격·수량은 전부 `number`**(bigint 금지 — D-34). `state` 는 `{t:"state", s: RelaySessionState, msg?: string, attempt?: number, accounts?: RelayAccount[]}`.
6. **`RelayAccount = {accountNo: string; name: string}`** — D-25 게이트 전에는 항상 빈 배열이 온다는 주석을 단다.
7. **호가 스냅샷** `RelayQuote` — `ap`/`aq`/`bp`/`bq` 는 각각 길이 10 배열, `cs`(change_sign 원문 1자), `et`(exchange_time "HHMMSSuuuuuu"), `snap: boolean`.
8. **체결 테이프** `RelayTape` — `e: RelayTapeEntry[]` 200ms 배치(D-35).
9. **계좌 상태** `RelayAccountState` — `hold`(잔고) / `unf`(미체결, `unfilledQty` 포함) / `rm`(removed_order_nos) / `snap`.
10. **주문 DTO** — `OrderSide = "B" | "S"`, `OrderType = "N" | "C"`, `OrderMarket = "K" | "Q"`, `ORDER_CONDITION_NORMAL = "0"`(보통 고정, D-21). `CreateOrderRequest = {code, accountNo, exchange, side, orderType, orgOrderNo?, qty, price}`, `CreateOrderResponse = {orderNo, resultCode, message, status}`, `DmaOrderStatus = "requested"|"accepted"|"rejected"|"filled"|"partially_filled"|"cancelled"|"timeout"`.
11. **close 코드 상수** `RELAY_WS_CLOSE = {AUTH_TIMEOUT: 4401, BAD_MESSAGE: 4400}` (D-11).
12. **`RELAY_MAX_FRAME_SIZE = 1_048_576`** 은 여기 두지 않는다 — relay 내부 상수다(코덱 모듈이 단일 정본).

DB snake_case ↔ 타입 camelCase 변환 책임이 **server/relay 순수함수**에 있음을 주석으로 고정한다(chat.ts 규약).

`packages/shared/src/index.ts` 에 barrel 을 추가한다: `export type { ... } from "./relay";` + `export { RELAY_STATE_LABELS, RELAY_WS_CLOSE, ORDER_CONDITION_NORMAL } from "./relay";`
  </action>
  <acceptance_criteria>
    - `grep -c 'RELAY_STATE_LABELS' packages/shared/src/relay.ts` >= 1
    - `grep -c 'session_rejected' packages/shared/src/relay.ts` >= 1 이고 `grep -c 'unauthorized' packages/shared/src/relay.ts` >= 1
    - `grep -c '4401' packages/shared/src/relay.ts` >= 1
    - `grep -c 'bigint' packages/shared/src/relay.ts` == 0 (와이어 계약은 전부 number — D-34)
    - `grep -c 'from "./relay"' packages/shared/src/index.ts` >= 1
    - `node -e "const m=require('./packages/shared/dist/index.js'); const k=Object.keys(m.RELAY_STATE_LABELS); if(k.length!==9)process.exit(1)"` exit 0 (shared build 후 상태 9종)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @gh-radar/shared build && pnpm typecheck</automated>
  </verify>
  <done>shared 계약 파일 존재 + barrel 등록 + 루트 typecheck exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gh-trade 저장소 → gh-radar `relay/src/generated/**` | 외부 저장소 스크립트가 이 저장소에 코드를 생성한다 |
| npm registry → relay 의존성 | 신규 패키지 3종(`ws`, `flatbuffers`, `@types/ws`) 도입 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-SC | Tampering | npm install (`ws`, `flatbuffers`, `@types/ws`) | mitigate | 15-RESEARCH §Package Legitimacy Audit 에서 slopcheck `[OK]` + postinstall 훅 부재 확인 완료. 3종 모두 **Approved** — `[ASSUMED]`/`[SUS]` 없음이므로 추가 체크포인트 불요. `[SLOP]` 0건 |
| T-15-16 | Tampering | `relay/src/generated/**` 수기 편집 | mitigate | Task 2 acceptance 가 `sync-relay-schema.sh --check` 무변경을 요구. 이후 모든 plan 의 verification 에도 동일 게이트 유지(D-26) |
| T-15-04 | Information Disclosure | relay pino 로그 | mitigate | Task 1 이 redact paths 에 `*.password`·`*.DMA_CRED_KEY`·`*.RELAY_ORDER_SECRET`·`*.token`·`req.headers['x-relay-secret']` 추가 (ASVS V7) |
</threat_model>

<verification>
- `bash /Users/alex/repos/gh-trade/server/scripts/sync-relay-schema.sh --check` 무변경 (SC-1)
- `pnpm typecheck` exit 0 (relay 포함)
- `pnpm --filter @gh-radar/relay test` exit 0 (테스트 0건이어도 config 유효)
- `grep -c '^  - relay$' pnpm-workspace.yaml` == 1
</verification>

<success_criteria>
- SC-1 충족: relay 워크스페이스 등록 + 생성물 커밋 + `--check` 무변경
- 루트 `pnpm typecheck` 가 relay 를 포함하고 exit 0
- `@gh-radar/shared` 에 wss/주문 계약 존재, webapp·server·relay 3자가 import 가능
</success_criteria>

<output>
After completion, create `.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-01-SUMMARY.md`
</output>
