---
phase: 15-dma-relay-kb-gh-trade-server-10-wss
plan: 15
type: execute
wave: 5
depends_on: ["15-05"]
files_modified:
  - relay/src/generated/
  - relay/src/dma/envelope.ts
  - relay/src/dma/session.ts
  - relay/tests/session.test.ts
  - relay/tests/account-declare.test.ts
  - packages/shared/src/relay.ts
autonomous: false
requirements: [RELAY-01]
must_haves:
  truths:
    - "SC-5 / D-25: gh-trade Phase 17 완료가 확인되고 `sync-relay-schema.sh` 재실행 산출물이 재커밋됐으며 `--check` 가 무변경으로 통과한다 — 생성 코드에 `LoginResp.accounts` 접근자가 생겼다"
    - "SC-5 / D-14: 세션 부팅이 `LoginResp.accounts` 의 **모든 계좌**를 `UpdateAccountNoReq(3)` mode \"1\" 로 연속 선언하고 `UpdateAccountNoResp(55)` 목록과 대조한 뒤에만 Ready 가 된다 (mode \"2\" 조회 왕복 없음)"
    - "SC-5 / D-14: 계좌 0건이면 세션이 실패하고 `서버에 등록된 계좌가 없습니다` 상태가 브라우저에 내려간다"
    - "SC-5 / D-17: gh-radar 용 DMA `user_id` 가 WinForms 클라이언트 값과 다른 값으로 운영되며(값은 저장소에 미기록) 세션 합류 사고가 발생하지 않는다"
    - "SC-5: 계좌 목록이 상태 프레임 `accounts` 로 브라우저에 내려가 주문 패널 계좌 선택의 원천이 된다"
  artifacts:
    - path: "relay/src/dma/session.ts"
      provides: "계좌 선언 루프 + UpdateAccountNoResp 대조 + 5초 타임아웃 + 계좌 0건 실패"
      contains: "UpdateAccountNo"
    - path: "relay/tests/account-declare.test.ts"
      provides: "계좌 선언 시퀀스·대조 실패·0건 실패 테스트"
      contains: "계좌"
  key_links:
    - from: "relay/src/dma/session.ts"
      to: "relay/src/generated/stock-dma/login-resp.js"
      via: "accounts() 접근자 (재동기화 후 생김)"
      pattern: "accounts"
    - from: "세션 accounts"
      to: "wss 상태 프레임"
      via: "{t:\"state\", accounts}"
      pattern: "accounts"
---

<objective>
gh-trade Phase 17 의존 게이트를 통과시키고, 생성 코드를 재동기화한 뒤, 15-03 이 축약 경로로 남겨 둔 `DeclaringAccounts` 단계를 실제 계좌 선언 루프로 채운다.

Purpose: SC-5 의 전반부이자 **이 phase 의 두 번째 [BLOCKING] 게이트**다. 리서치 실측 결과 gh-trade 정본 스키마에 `AccountEntry` 테이블도 `LoginResp.accounts` 필드도 **아직 없다** — 재동기화 전에는 계좌 코드를 작성하는 것 자체가 불가능하다(생성 코드에 접근자가 없어 타입 에러). 따라서 게이트가 가정이 아니라 확인된 사실이다.
Output: 재동기화된 생성물 + 계좌 선언 루프 + 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-03-SUMMARY.md

<interfaces>
<!-- 15-03 이 남긴 삽입 지점 -->
relay/src/dma/session.ts 의 `declaring` 단계에 `// TODO(D-25)` 마커. 현재는 "선언할 계좌 0건 → 즉시 Ready" 축약 경로.
`accounts: RelayAccount[]` 필드는 처음부터 존재하며 현재는 항상 `[]`.

<!-- gh-trade Phase 17 계약 (17-CONTEXT.md) -->
D-19  LoginResp 에 `accounts: [AccountEntry]` append (`AccountEntry{account_no, name}`)
D-11  mode "2" 조회 왕복 제거 — 선언은 mode "1" 만
D-12  계좌 0건이면 세션 기동 거부
D-13  허용 밖 계좌는 warn
D-16/D-17  재접속 시 재로그인, 서버 거부 시 루프 중단

<!-- C# 기준 구현 (Session.cs L690-812) -->
② 활성 계좌 일괄 선언 (A2 — 건별 대기 없이 연속 송신, 응답은 매번 현재 목록 전체)
   for (i in targets) SendUpdateAccountNo("1", targets[i]);
   accountsMatched = _accountsMatchedEvent.WaitOne(ACCOUNT_RESP_TIMEOUT_MS);  // 5000
   // 선언한 계좌 중 하나라도 서버 목록에 없으면 실패 (D-08, T-02-13)
③ Ready. Client.ResetReconnectAttempts(); _hasBeenReady = true;
MAX_ACCOUNT_NO_LEN = 12 (서버 계좌번호 가드)
MAX_ACCOUNT_LIST_COUNT = 256 (소비측 상한)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] gh-trade Phase 17 완료 확인 + sync-relay-schema.sh 재실행·재커밋</name>
  <files>relay/src/generated/</files>
  <read_first>
    - /Users/alex/repos/gh-trade/server/src/protocol/StockDMA.fbs (정본 스키마 — `AccountEntry` / `LoginResp.accounts` 존재 여부를 직접 확인)
    - /Users/alex/repos/gh-trade/server/scripts/sync-relay-schema.sh (재실행할 스크립트)
    - /Users/alex/repos/gh-trade/.planning/phases/17-dma-login-auth-user-accounts/17-CONTEXT.md (계약 D-19/D-11/D-12)
    - relay/src/generated/stock-dma/login-resp.ts (재동기화 전 상태 — accounts 접근자 부재 확인)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md §Pitfall 1, §Open Questions 1
  </read_first>
  <action>
`/Users/alex/repos/gh-trade` 의 `StockDMA.fbs` 에 `table AccountEntry` 와 `LoginResp.accounts` 가 **둘 다** 존재하는지 확인해 D-25 게이트를 판정한다. 없으면 이 plan 과 15-16~15-20 을 보류하고 사용자에게 보고한다(시세 팬아웃은 이미 완료돼 사용 가능). 있으면 `sync-relay-schema.sh` 를 재실행하고 `git diff --stat relay/src/generated` 로 변경 규모를 검토한다 — append-only 규약 위반으로 보이는 대규모 diff 는 중단 사유다. `--check` 무변경과 기존 relay 테스트 green 을 확인한 뒤 생성물을 커밋한다(**손으로 고치지 않는다**, D-26).
  </action>
  <what-built>
15-01 이 현행 스키마(`LoginResp{success, message}`)로 생성물을 커밋했고, 15-03 의 세션은 계좌 0건 축약 경로로만 Ready 에 간다. gh-trade Phase 17 이 `AccountEntry` 테이블과 `LoginResp.accounts` 를 append 하면 이 게이트가 열린다.
  </what-built>
  <how-to-verify>
1. **gh-trade Phase 17 완료 확인** — `/Users/alex/repos/gh-trade` 에서 `git log --oneline -10` 과 `grep -n "accounts\|AccountEntry" server/src/protocol/StockDMA.fbs` 를 실행한다. 스키마에 `table AccountEntry` 와 `LoginResp.accounts` 가 **둘 다** 있어야 게이트가 열린다.
   - **아직 없으면 이 plan 을 중단한다.** 사용자에게 보고하고 15-16~15-20 을 보류한다. 15-01~15-14 는 이미 완료돼 있으므로 **시세 팬아웃(RELAY-01 의 UI 부분까지)은 그대로 사용 가능**하다.
2. `flatc --version` 이 `25.12.19` 인지 확인(스크립트 가드).
3. `bash /Users/alex/repos/gh-trade/server/scripts/sync-relay-schema.sh` 재실행.
4. `git diff --stat relay/src/generated` 로 변경 규모를 확인한다. 기대: `login-resp.ts` 수정 + `account-entry.ts` 신규 + `StockDMA.fbs` 사본 갱신 (파일 수 39→40 전후). 예상 밖의 대규모 diff(예: 기존 필드 시그니처 변경)가 보이면 **중단하고 사용자에게 보고**한다 — append-only 규약 위반 가능성.
5. `bash /Users/alex/repos/gh-trade/server/scripts/sync-relay-schema.sh --check` 무변경 확인.
6. `pnpm --filter @gh-radar/relay run typecheck` 과 `pnpm --filter @gh-radar/relay test` 를 돌려 **기존 테스트가 전부 green** 인지 확인한다(스키마 변경이 기존 파싱을 깨지 않았는지).
7. 생성물을 커밋한다(**손으로 고치지 않는다** — D-26).
8. SUMMARY 에 diff 요약(신규/수정 파일 목록, `LoginResp.accounts` 접근자 시그니처)을 기록한다.
  </how-to-verify>
  <acceptance_criteria>
    - `grep -c 'table AccountEntry' /Users/alex/repos/gh-trade/server/src/protocol/StockDMA.fbs` >= 1
    - `grep -c 'accounts' relay/src/generated/stock-dma/login-resp.ts` >= 1 (접근자 생김)
    - `test -f relay/src/generated/stock-dma/account-entry.ts` exit 0
    - `bash /Users/alex/repos/gh-trade/server/scripts/sync-relay-schema.sh --check` exit 0 (무변경)
    - `git status --porcelain relay/src/generated | wc -l` == 0 (커밋 완료)
    - `pnpm --filter @gh-radar/relay test` exit 0 (기존 테스트 회귀 0)
    - `pnpm --filter @gh-radar/relay run typecheck` exit 0
  </acceptance_criteria>
  <verify>
    <automated>bash /Users/alex/repos/gh-trade/server/scripts/sync-relay-schema.sh --check && pnpm --filter @gh-radar/relay test && pnpm --filter @gh-radar/relay run typecheck</automated>
  </verify>
  <done>`AccountEntry` 존재, `login-resp.ts` 에 accounts 접근자, `--check` 무변경, 기존 테스트 회귀 0, 생성물 커밋 완료.</done>
  <resume-signal>재동기화 완료 + `--check` 무변경 + 기존 테스트 green 확인 후 "재동기화 완료" 입력. Phase 17 미완이면 "17 미완 — 보류" 입력</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 계좌 조립·파싱 확장 (envelope.ts) + shared 계약 갱신</name>
  <files>relay/src/dma/envelope.ts, packages/shared/src/relay.ts</files>
  <read_first>
    - relay/src/generated/stock-dma/login-resp.ts, account-entry.ts, update-account-no-req.ts, update-account-no-resp.ts (재동기화 후 접근자 시그니처 확인)
    - relay/src/dma/envelope.ts (수정 대상 — 기존 build*/parse* 규약, `takeCount`, 형식 가드)
    - relay/src/dma/msg-type.ts (`UpdateAccountNoReq=3`, `UpdateAccountNoResp=55` 존재 확인)
    - packages/shared/src/relay.ts (수정 대상 — `RelayAccount` 타입)
    - /Users/alex/repos/gh-trade/client/Services/DMA/Client.cs (계좌 목록 소비 + `MAX_ACCOUNT_LIST_COUNT = 256`)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md §A-7
  </read_first>
  <action>
`relay/src/dma/envelope.ts` 에 계좌 관련 빌더·파서를 추가한다(기존 규약 유지 — start/add/end 조립, total 파서, 필드 가드).

- `buildUpdateAccountNoReq(mode: "1", accountNo: string): Buffer` — `MsgType.UpdateAccountNoReq(3)`. **`mode` 는 `"1"` 만 허용**하는 타입으로 좁힌다(D-11 — mode "2" 조회 왕복은 하지 않는다). 단일 문자 필드 변환은 이미 있는 변환 함수 한 곳을 통과시킨다(D-21 사상).
- `parseLoginRespAccounts(env): RelayAccount[]` — `LoginResp.accounts` 벡터를 `takeCount(n, MAX_ACCOUNT_LIST_COUNT=256, "계좌 목록")` 으로 클램프하고, 각 `AccountEntry` 의 `account_no` 는 **길이 1~12 문자열**(`MAX_ACCOUNT_NO_LEN = 12`)이어야 하며 위반 시 그 항목만 건너뛰고 warn 로그를 남긴다(항목 스킵도 카운터 — S-5).
- `parseUpdateAccountNoResp(env): string[]` — 서버가 돌려주는 **현재 계좌 목록 전체**. 같은 상한·형식 가드 적용.
- 기존 `parseLoginResp` 는 시그니처를 유지하되 `accounts` 를 함께 반환하도록 확장한다(호출부 1곳만 바뀌게).

`packages/shared/src/relay.ts`: `RelayAccount` 에 필요한 필드가 더 있으면(예: `name`) 재동기화된 `AccountEntry` 에 맞춰 갱신한다. "D-25 게이트 전에는 항상 빈 배열" 주석을 "게이트 통과 — 실제 계좌 목록이 온다"로 갱신한다.

기존 테스트가 깨지지 않아야 한다(`parseLoginResp` 반환 형태 변경 시 15-03 테스트 갱신 포함).
  </action>
  <acceptance_criteria>
    - `grep -c 'buildUpdateAccountNoReq' relay/src/dma/envelope.ts` >= 1
    - `grep -c 'MAX_ACCOUNT_LIST_COUNT' relay/src/dma/envelope.ts` >= 1 이고 값이 256
    - `grep -c 'MAX_ACCOUNT_NO_LEN' relay/src/dma/envelope.ts` >= 1 이고 값이 12
    - `grep -c '"2"' relay/src/dma/envelope.ts` == 0 (mode "2" 미사용 — D-11)
    - `pnpm --filter @gh-radar/relay test envelope` exit 0 (기존 + 신규 케이스)
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test envelope && pnpm typecheck</automated>
  </verify>
  <done>계좌 조립/파싱이 상한·형식 가드와 함께 추가되고 기존 테스트 회귀 0.</done>
</task>

<task type="auto">
  <name>Task 3: Session 계좌 선언 루프 + 대조 + 0건 실패</name>
  <files>relay/src/dma/session.ts, relay/tests/session.test.ts, relay/tests/account-declare.test.ts</files>
  <read_first>
    - relay/src/dma/session.ts (수정 대상 — `// TODO(D-25)` 마커 지점, 상태 전이, generation 규율)
    - relay/src/dma/envelope.ts (Task 2 산출)
    - relay/tests/session.test.ts (기존 테스트 — 축약 경로 케이스를 갱신해야 한다)
    - relay/tests/helpers/fake-gateway.ts (계좌 응답을 흉내낼 수 있게 확장 필요)
    - /Users/alex/repos/gh-trade/client/Services/DMA/Session.cs (기준 구현 — 부트 시퀀스 ②③, `ACCOUNT_RESP_TIMEOUT_MS`, 대조 실패 처리)
    - /Users/alex/repos/gh-trade/.planning/phases/17-dma-login-auth-user-accounts/17-CONTEXT.md (D-12 계좌 0건, D-13 허용 밖 계좌 warn)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md §A-9
  </read_first>
  <action>
`relay/src/dma/session.ts` 의 `declaring` 단계를 실제 루프로 교체하고 `// TODO(D-25)` 마커를 제거한다.

시퀀스(C# `Session.cs` L690-812 이식):
1. `LoginResp` 의 `accounts` 를 세션 필드에 저장한다.
2. **계좌 0건이면 즉시 실패** — `failNoRetry("서버에 등록된 계좌가 없습니다")` 로 확정하고 그 문구를 상태 프레임 `msg` 로 브라우저에 내린다(D-14/17 D-12). 재접속 루프를 돌리지 않는다(재시도해도 결과가 같다).
3. `declaring` 상태 전이 후 **모든 계좌**에 대해 `buildUpdateAccountNoReq("1", accountNo)` 를 **건별 대기 없이 연속 송신**한다(C# A2 — 응답은 매번 현재 목록 전체를 준다).
4. `UpdateAccountNoResp(55)` 를 **5초**(`ACCOUNT_RESP_TIMEOUT_MS = 5000`) 대기하며 수신할 때마다 `parseUpdateAccountNoResp` 결과가 선언 목록을 **모두 포함하는지** 대조한다. 전부 포함되면 성공, 5초 내 미충족이면 `fail("계좌 선언 미확인 (5초): {누락 목록}")`.
5. 서버 목록에 있으나 우리가 선언하지 않은 계좌는 **warn 로그만** 남기고 진행한다(17 D-13).
6. 성공 시 `ready` 전이 + `resetReconnectAttempts()`. 상태 프레임에 `accounts` 를 실어 내린다 — 이것이 주문 패널 계좌 선택의 원천이다(D-14).
7. **재접속 시에도 같은 경로**를 탄다 — 재로그인 → 계좌 재선언 → 재구독. 경로를 두 벌 만들지 않는다(Pitfall 4).
8. generation 검사는 각 await 재개부에 유지한다.
9. 계좌번호는 **로그에서만 뒤 4자리 마스킹**하고 상태 프레임(화면)에는 전체를 내린다(UI-SPEC D2).

`relay/tests/helpers/fake-gateway.ts` 에 `respondLoginWithAccounts(accounts)` 와 `respondUpdateAccountNo(list)` 를 추가한다.

`relay/tests/account-declare.test.ts` 케이스 8건: ① 계좌 2건 → `UpdateAccountNoReq` 2건이 **연속 송신**되고 그 사이에 응답 대기가 없음 ② 응답 목록이 선언 목록을 포함 → `ready` ③ 응답에 1건 누락 → 5초 후 `failed` + 누락 계좌번호가 메시지에 포함 ④ 계좌 0건 → `failed` + `서버에 등록된 계좌가 없습니다` + **재접속 시도 0** ⑤ 서버 목록에 여분 계좌 → warn 만, `ready` 진행 ⑥ 재접속 후 계좌 재선언이 다시 일어남 ⑦ `ready` 상태 프레임에 `accounts` 배열이 실림 ⑧ 로그에 계좌번호 전체가 아니라 마스킹 값이 남음.

`relay/tests/session.test.ts` 의 기존 "accounts 항상 빈 배열" 케이스를 갱신한다(축약 경로는 이제 계좌 0건 실패 경로다).
  </action>
  <acceptance_criteria>
    - `grep -c 'TODO(D-25)' relay/src/dma/session.ts` == 0 (마커 제거됨)
    - `grep -c 'ACCOUNT_RESP_TIMEOUT_MS' relay/src/dma/session.ts` >= 1 이고 값이 5000
    - `grep -c '서버에 등록된 계좌가 없습니다' relay/src/dma/session.ts` == 1
    - `grep -c 'buildUpdateAccountNoReq' relay/src/dma/session.ts` >= 1
    - `pnpm --filter @gh-radar/relay test account-declare` exit 0, 8건 통과
    - `pnpm --filter @gh-radar/relay test` exit 0 (기존 전체 회귀 0)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test</automated>
  </verify>
  <done>계좌 선언 루프·대조·0건 실패·재접속 재선언이 8+ 테스트로 증명되고 기존 relay 테스트 전부 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gh-trade 저장소 스키마 변경 → gh-radar 생성물 | 외부 저장소가 이 저장소의 코드를 바꾼다 |
| 게이트웨이 계좌 목록 → 주문 대상 계좌 집합 | 이 목록이 곧 주문 허용 계좌 화이트리스트가 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-16 | Tampering | 재동기화 diff | mitigate | Task 1 이 diff 규모를 검토하고 예상 밖 변경(기존 필드 시그니처 변경 = append-only 위반) 시 중단·보고. `--check` 무변경 + 기존 테스트 green 이 acceptance |
| T-15-01 | Elevation of Privilege (IDOR 기반) | 계좌 목록 확정 | mitigate | 선언한 계좌가 **서버 목록에 전부 포함될 때만** Ready. 이 검증된 목록이 15-17 의 `account_no ∈ 세션 계좌 목록` 대조 원천이 된다 |
| T-15-47 | Spoofing (세션 합류) | DMA `user_id` | mitigate | D-17 — gh-radar 용 `user_id` 를 WinForms 값과 다르게 운영(게이트웨이는 user_id+broker 로 세션을 합류시킨다). 값은 저장소에 미기록, gh-trade users.toml 운영 절차 |
| T-15-10 | DoS (자기유발) | 계좌 0건 / 대조 실패 | mitigate | 계좌 0건은 `failNoRetry` — 재접속 루프를 돌리지 않는다(Task 3 ④ 가 재시도 0 을 단언). 대조 실패는 5초 타임아웃 후 확정 |
| T-15-07 | Tampering / DoS | 계좌 벡터 | mitigate | `takeCount(n, 256, "계좌 목록")` + `account_no` 길이 1~12 형식 가드. 위반 항목은 스킵 + 카운터 |
| T-15-15 | Information Disclosure | 로그의 계좌번호 | mitigate | 로그는 뒤 4자리 마스킹, 화면(상태 프레임)은 전체 표시(UI-SPEC D2). Task 3 ⑧ 이 단언 |
</threat_model>

<verification>
- `bash /Users/alex/repos/gh-trade/server/scripts/sync-relay-schema.sh --check` 무변경
- `pnpm --filter @gh-radar/relay test` exit 0 (기존 + account-declare 8건)
- `pnpm typecheck` exit 0
- `grep -c 'TODO(D-25)' relay/src/dma/session.ts` == 0
</verification>

<success_criteria>
- gh-trade 17 게이트가 열리고 생성물이 재동기화·재커밋됨
- 계좌 전부 선언 → 목록 대조 → Ready 시퀀스가 동작
- 계좌 0건이 재시도 없이 명확한 문구로 실패함
- 계좌 목록이 상태 프레임으로 브라우저에 전달됨
</success_criteria>

<output>
After completion, create `.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-15-SUMMARY.md`
</output>
