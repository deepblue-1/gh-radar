---
phase: 15-dma-relay-kb-gh-trade-server-10-wss
plan: 06
type: execute
wave: 3
depends_on: ["15-01"]
files_modified:
  - scripts/setup-relay-iam.sh
  - infra/relay/startup.sh
  - infra/relay/Caddyfile
  - infra/relay/openconnect@.service
  - infra/relay/kbvpn-vpnc-wrapper.sh
  - infra/relay/kbvpn-fetch-secret.sh
  - infra/relay/kbvpn-connect.sh
  - infra/relay/README.md
autonomous: true
requirements: [RELAY-03]
must_haves:
  truths:
    - "SC-2 / D-07: `radar-gw`(e2-micro, Debian 12, asia-northeast3, 기존 `gh-radar-vpc`/`gh-radar-subnet-an3`)를 신규 외부 고정 IP 로 프로비저닝하는 멱등 스크립트가 존재하며, Cloud NAT 용 `gh-radar-static-ip` 를 재사용하지 않는다"
    - "SC-2 / D-09: 방화벽 3규칙(443 공개 / 22 IAP 35.235.240.0/20 / 내부포트 10.10.0.0/26)이 스크립트로 정의되고 `gh-radar-vpc` 최초 규칙으로 생성된다"
    - "SC-8 / D-08: 내부 주문 포트 규칙이 네트워크 태그가 아니라 `--source-ranges` 로 좁혀진다 (Cloud Run 워크로드에는 태그를 붙일 수 없다)"
    - "SC-8 / D-05: Caddy 가 `dma.jx1.io` 443 에서 TLS 를 종단하고 `127.0.0.1:8090` 으로 리버스 프록시하며 WebSocket 설정은 별도로 필요 없다"
    - "SC-2: openconnect systemd 유닛이 재시도 상한(`StartLimitBurst=5/1h`)을 갖고 split-tunnel(`CISCO_SPLIT_INC` 10.41.0.0/16)을 강제해 기본 경로를 VPN 에 빼앗기지 않는다"
    - "SC-8 / D-06: KB VPN 비밀번호는 Secret Manager 에서 tmpfs(`/run`, 0600)로만 흐르고 유닛 파일·문서·로그·커밋 어디에도 값이 없다"
  artifacts:
    - path: "scripts/setup-relay-iam.sh"
      provides: "SA + Secret 3 + 외부/내부 고정 IP + 방화벽 3규칙 + VM 생성 (멱등)"
      contains: "35.235.240.0/20"
    - path: "infra/relay/startup.sh"
      provides: "멱등 startup-script (swap 1GB, docker, openconnect, Caddy, 로그 로테이션, 유닛 배치)"
      contains: "fallocate"
    - path: "infra/relay/Caddyfile"
      provides: "dma.jx1.io TLS + reverse_proxy 127.0.0.1:8090"
      contains: "reverse_proxy"
    - path: "infra/relay/openconnect@.service"
      provides: "재시도 상한 있는 VPN 유닛 (StartLimitBurst=5)"
      contains: "StartLimitBurst"
  key_links:
    - from: "infra/relay/openconnect@.service"
      to: "infra/relay/kbvpn-vpnc-wrapper.sh"
      via: "--script 인자"
      pattern: "CISCO_SPLIT_INC"
    - from: "infra/relay/kbvpn-fetch-secret.sh"
      to: "Secret Manager gh-radar-kb-vpn-password"
      via: "메타데이터 토큰 + REST"
      pattern: "metadata.google.internal"
    - from: "scripts/setup-relay-iam.sh"
      to: "infra/relay/startup.sh"
      via: "--metadata-from-file=startup-script"
      pattern: "startup-script"
---

<objective>
GCP 인프라를 **파일로만** 정의한다. IAM/네트워크/VM 프로비저닝 스크립트와 VM 안에서 돌 startup-script·Caddyfile·openconnect systemd 유닛·VPN 래퍼 3종을 작성한다. **이 plan 은 어떤 GCP 리소스도 만들지 않는다** — 실행은 15-07 의 체크포인트 소관이다.

Purpose: SC-2·SC-8 의 정의부. 이 phase 최대 미검증 리스크는 openconnect 가 기본 경로를 가져가는 것(Pitfall 10)이며, 그 대비를 코드로 먼저 못박아야 15-07 의 1회성 시도가 안전해진다. `gh-radar-vpc` 에는 방화벽 규칙이 0개라 3규칙 없이는 IAP SSH 조차 안 된다.
Output: gcloud 스크립트 1 + VM 자산 6 + 인프라 README 1. GCP 상태 변경 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md

<interfaces>
<!-- GCP 실측 전제 (2026-09-05, 읽기 전용 조회). 스크립트는 이 상태를 가정한다. -->
VM: 0대 · `gh-radar-vpc` 방화벽 규칙: 0개 · 서브넷 `gh-radar-subnet-an3` / asia-northeast3 / 10.10.0.0/26
외부 IP `gh-radar-static-ip` 34.64.195.151 = Cloud NAT 용(IN_USE, 재사용 금지)
내부 예약 `serverless-ipv4-…` 10.10.0.16/28 RESERVED (Cloud Run Direct VPC Egress)
Artifact Registry `gh-radar` (asia-northeast3, DOCKER)
알림 채널 `projects/gh-radar/notificationChannels/14409521670382124894`
Secret `gh-radar-dma-cred-key` / `gh-radar-relay-order-secret` / `gh-radar-kb-vpn-password` — 전부 없음(신설 필요)
e2-micro = 2 vCPU(공유) / 1024MB · 이미지 패밀리 `debian-12`

<!-- Mac 참조 인자 (`/usr/local/sbin/kbvpn-connect` 실측, 값 미기록) -->
--protocol=anyconnect · --user=<stdin 1행> · --authgroup=KBSEC_DMA · --servercert pin-sha256:<핀>
--passwd-on-stdin · 서버 https://211.47.35.211:65535 · 비밀번호는 stdin 2행

<!-- 기존 인프라 스크립트 골격 (setup-intraday-sync-iam.sh) -->
gcloud guard: GCP_PROJECT_ID 필수 + active configuration == "gh-radar" 확인 후 진행
멱등 패턴: describe → 없으면 create → "✓ ... ready" 출력
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/setup-relay-iam.sh — SA·Secret·고정 IP·방화벽 3규칙·VM (작성만)</name>
  <files>scripts/setup-relay-iam.sh</files>
  <read_first>
    - scripts/setup-intraday-sync-iam.sh (analog: gcloud guard 머리 + API enable 멱등 + describe→create 패턴 + VPC/서브넷/고정 IP 블록)
    - scripts/deploy-server.sh (Direct VPC Egress 설정 — 서브넷/VPC 이름 확인)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md §Pattern 7 (gcloud 초안 전문), §Pitfall 12
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md §G-1
  </read_first>
  <action>
`scripts/setup-relay-iam.sh` 를 작성한다. **스크립트를 실행하지 않는다** — 문법 검사만 한다(실행은 15-07).

구조(6구간, `setup-intraday-sync-iam.sh` mirror):

1. **gcloud guard** — `GCP_PROJECT_ID` 필수, active configuration 이 `gh-radar` 인지 확인 후 진행. 아니면 힌트 출력 후 exit 1.
2. **API enable(멱등)** — `compute`, `secretmanager`, `artifactregistry`, `iap`, `monitoring`.
3. **서비스 계정** `gh-radar-relay-sa` 생성(멱등). 역할은 **프로젝트 레벨 `secretmanager.secretAccessor` 를 주지 않고** Secret 단위 바인딩으로 좁힌다. 프로젝트 레벨로 부여하는 것은 `roles/artifactregistry.reader`, `roles/logging.logWriter`, `roles/monitoring.metricWriter` 3개만.
4. **Secret 3종 생성(멱등, 값은 비움)** — `gh-radar-dma-cred-key`, `gh-radar-relay-order-secret`, `gh-radar-kb-vpn-password`. **스크립트가 값을 생성·출력하지 않는다.** 값 주입은 별도 안내문으로만 출력한다(`gcloud secrets versions add <name> --data-file=-` 를 사람이 실행). 각 secret 에 `gh-radar-relay-sa` 의 `secretAccessor` 를 바인딩하고, 관리자 등록 스크립트 실행 주체를 위해 `gh-radar-dma-cred-key` 에는 `DEPLOYER_SA_EMAIL` env 가 주어지면 그 SA 에도 accessor 를 바인딩한다(RESEARCH Open Question 3).
5. **네트워크**
   - 기존 `gh-radar-vpc` / `gh-radar-subnet-an3` 은 **존재만 확인**하고 만들지 않는다(이미 있음).
   - 외부 고정 IP `gh-radar-relay-ip` 를 `--region=asia-northeast3` 로 예약(멱등). **`gh-radar-static-ip` 는 Cloud NAT 용이므로 절대 재사용하지 않는다** 는 주석 필수. 스크립트 말미에 예약된 IP 값을 출력하고 "이 값을 `jx1.io` 의 `dma` A 레코드로 등록해야 합니다(사용자 작업)" 안내를 출력한다.
   - 내부 고정 IP `gh-radar-relay-internal` = `10.10.0.5`, `--purpose=GCE_ENDPOINT`, `--subnet=gh-radar-subnet-an3`. serverless 예약 대역 `10.10.0.16/28` 을 피한 값임을 주석에 남긴다.
   - **방화벽 3규칙**(전부 `--target-tags=radar-gw`, `--network=gh-radar-vpc`):
     `relay-allow-https` tcp:443 `--source-ranges=0.0.0.0/0`
     `relay-allow-iap-ssh` tcp:22 `--source-ranges=35.235.240.0/20`
     `relay-allow-internal-order` tcp:8091 `--source-ranges=10.10.0.0/26`
     내부 포트 규칙에 "**Cloud Run 워크로드에는 네트워크 태그를 붙일 수 없어 source-ranges 로만 좁힌다. 공유 비밀 헤더가 두 번째 방어선**" 주석을 단다.
     **80 은 열지 않는다** — Caddy 는 TLS-ALPN-01(443)로 발급 가능. 발급 실패 시에만 임시 개방하라는 안내를 주석으로 남긴다.
6. **VM `radar-gw`** — `--zone=asia-northeast3-a`, `--machine-type=e2-micro`, `--image-family=debian-12 --image-project=debian-cloud`, `--boot-disk-size=20GB --boot-disk-type=pd-balanced`, `--subnet=gh-radar-subnet-an3`, `--private-network-ip=10.10.0.5`, `--address=<gh-radar-relay-ip>`, `--tags=radar-gw`, `--service-account=gh-radar-relay-sa@…`, `--scopes=cloud-platform`, `--metadata-from-file=startup-script=infra/relay/startup.sh`, `--shielded-secure-boot --shielded-vtpm --shielded-integrity-monitoring`. **`--can-ip-forward` 는 붙이지 않는다**(최소권한 — tun 라우팅에는 불필요. VPN 라우팅 문제가 실측되면 그때 추가하라는 주석).
   - VM 생성 **전에** 방화벽 3규칙이 만들어졌는지 확인하는 가드를 둔다(규칙 0개 상태에서는 IAP SSH 조차 안 된다 — Pitfall 12).
   - `--dry-run` 플래그를 지원해 실제 create 없이 실행 계획만 출력하게 한다(15-07 이 먼저 dry-run 으로 검토).

머리에 `set -euo pipefail`, 각 구간마다 `✓` 확인 출력. 스크립트 어디에도 비밀 값이 없어야 한다.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/setup-relay-iam.sh` exit 0
    - `grep -c '35.235.240.0/20' scripts/setup-relay-iam.sh` == 1
    - `grep -c '10.10.0.0/26' scripts/setup-relay-iam.sh` == 1
    - `grep -c 'radar-gw' scripts/setup-relay-iam.sh` >= 4 (태그 3규칙 + VM)
    - `grep -c 'gh-radar-static-ip' scripts/setup-relay-iam.sh` <= 1 이고 그 1건이 "재사용 금지" 주석 안에 있음
    - `grep -c 'can-ip-forward' scripts/setup-relay-iam.sh` == 0
    - `grep -c 'dry-run' scripts/setup-relay-iam.sh` >= 1
    - `grep -cE '(password|secret)\s*=\s*["'\'']\S' scripts/setup-relay-iam.sh` == 0 (하드코딩 비밀 0건)
    - `grep -c 'gh-radar-relay-ip' scripts/setup-relay-iam.sh` >= 2 (예약 + 값 출력)
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/setup-relay-iam.sh && grep -c '35.235.240.0/20' scripts/setup-relay-iam.sh</automated>
  </verify>
  <done>멱등 프로비저닝 스크립트가 문법 검사를 통과하고 방화벽 3규칙·고정 IP 2종·Secret 3종·VM 정의를 포함한다. GCP 리소스 변경 0.</done>
</task>

<task type="auto">
  <name>Task 2: VPN 자산 — openconnect 유닛 + split-tunnel 래퍼 + Secret 페치 (작성만)</name>
  <files>infra/relay/openconnect@.service, infra/relay/kbvpn-vpnc-wrapper.sh, infra/relay/kbvpn-fetch-secret.sh, infra/relay/kbvpn-connect.sh</files>
  <read_first>
    - /usr/local/sbin/kbvpn-connect (Mac 참조 — openconnect 실인자만 확인. **비밀번호 값은 읽지도 기록하지도 않는다**)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md §Pattern 8 (split-tunnel 3안 + systemd 유닛 규율), §Pitfall 10, §Pattern 7 (메타데이터 토큰 → Secret Manager REST 무의존 경로)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md D-03, D-16, Claude's Discretion(openconnect 유닛 옵션)
  </read_first>
  <action>
`infra/relay/kbvpn-fetch-secret.sh`: 메타데이터 서버 토큰 → Secret Manager REST 로 `gh-radar-kb-vpn-password` 를 읽어 `/run/kbvpn.cred`(tmpfs) 에 **0600** 으로 쓴다. `gcloud` 존재를 가정하지 않는다(Debian 공개 이미지에 있는지 불확실 — RESEARCH A4). `curl` + `python3`(Debian 12 기본 포함) 조합만 사용하고 `jq` 를 요구하지 않는다. 파일 내용은 stdout 에 절대 출력하지 않는다. 형식은 **1행=user, 2행=password**(Mac 스크립트와 동형).

`infra/relay/kbvpn-connect.sh`: `/run/kbvpn.cred` 를 stdin 으로 openconnect 를 **foreground**(`--background` 없이 — `Type=simple` 과 정합) 실행한다. 인자: `--protocol=anyconnect`, `--authgroup=KBSEC_DMA`, `--servercert pin-sha256:${KBVPN_SERVERCERT}`, `--user`(cred 1행), `--passwd-on-stdin`, `--script=/usr/local/sbin/kbvpn-vpnc-wrapper`, 서버 `${KBVPN_SERVER}`. **서버 주소·인증서 핀·계정은 스크립트에 하드코딩하지 않고 `/etc/kbvpn.env`(0600, startup-script 가 Secret/메타데이터에서 배치) 에서 읽는다.** cred 내용을 `echo` 하지 않고, openconnect stdout 만 저널로 넘긴다.

`infra/relay/kbvpn-vpnc-wrapper.sh`: **split-tunnel 1순위 대안 — 추가 패키지 0.** stock `vpnc-script` 호출 전에 `CISCO_SPLIT_INC=1`, `CISCO_SPLIT_INC_0_ADDR=10.41.0.0`, `CISCO_SPLIT_INC_0_MASK=255.255.0.0`, `CISCO_SPLIT_INC_0_MASKLEN=16` 을 export 하고 `CISCO_BANNER` 를 unset 한 뒤 `exec /usr/share/vpnc-scripts/vpnc-script "$@"`. 이 파일 주석에 **왜 필요한지**(서버가 default route 를 푸시하면 Secret Manager·Artifact Registry·ACME·IAP 가 동시에 죽는다 — Pitfall 10)와 **2순위 대안**(`vpn-slice`, Debian 저장소 부재 → pipx 설치 필요, 도입 시 별도 승인 필요)을 남긴다. `vpn-slice` 를 자동 설치하는 코드는 **넣지 않는다**.

`infra/relay/openconnect@.service`: `[Unit]` `After/Wants=network-online.target`, `StartLimitIntervalSec=3600`, **`StartLimitBurst=5`**(1시간 5회 초과 재시작 시 유닛 정지 → KB 계정 잠금 방지). `[Service]` `Type=simple`, `ExecStartPre=/usr/local/sbin/kbvpn-fetch-secret`, `ExecStart=/usr/local/sbin/kbvpn-connect`, `ExecStopPost=/bin/rm -f /run/kbvpn.cred`, `Restart=on-failure`, `RestartSec=30`. **`ExecStart=` 라인에 비밀번호를 넣지 않는다**(`systemctl show`·`ps` 노출). `[Install] WantedBy=multi-user.target` 은 두되 startup-script 가 `enable` 하지 않고 **수동 start 만** 하도록 주석으로 명시한다 — 첫 연결은 D-03 선검증 체크포인트에서 사람이 관찰하며 시작해야 한다.

4개 파일 어디에도 비밀번호·계정 값 문자열이 없어야 한다.
  </action>
  <acceptance_criteria>
    - `grep -c 'StartLimitBurst=5' infra/relay/openconnect@.service` == 1
    - `grep -c 'StartLimitIntervalSec=3600' infra/relay/openconnect@.service` == 1
    - `grep -c 'ExecStopPost=/bin/rm -f /run/kbvpn.cred' infra/relay/openconnect@.service` == 1
    - `grep -c 'passwd\|password' infra/relay/openconnect@.service` == 0
    - `grep -c 'CISCO_SPLIT_INC_0_ADDR=10.41.0.0' infra/relay/kbvpn-vpnc-wrapper.sh` == 1
    - `grep -c 'metadata.google.internal' infra/relay/kbvpn-fetch-secret.sh` >= 1 이고 `grep -c 'jq' infra/relay/kbvpn-fetch-secret.sh` == 0
    - `grep -c 'background' infra/relay/kbvpn-connect.sh` == 0 (foreground 실행)
    - `bash -n infra/relay/kbvpn-fetch-secret.sh && bash -n infra/relay/kbvpn-connect.sh && bash -n infra/relay/kbvpn-vpnc-wrapper.sh` exit 0
    - `grep -rniE 'kbs124|[0-9]{4,}!|pw=|passwd=' infra/relay/ | wc -l` == 0 (비밀 값 0건)
  </acceptance_criteria>
  <verify>
    <automated>bash -n infra/relay/kbvpn-fetch-secret.sh && bash -n infra/relay/kbvpn-connect.sh && bash -n infra/relay/kbvpn-vpnc-wrapper.sh && test "$(grep -rniE 'kbs124|pw=|passwd=' infra/relay/ | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>VPN 유닛 + 래퍼 3종이 문법 검사를 통과하고 재시도 상한·split-tunnel·tmpfs cred 규율을 포함하며 비밀 값이 0건이다.</done>
</task>

<task type="auto">
  <name>Task 3: startup.sh + Caddyfile + infra README (작성만)</name>
  <files>infra/relay/startup.sh, infra/relay/Caddyfile, infra/relay/README.md</files>
  <read_first>
    - infra/relay/openconnect@.service, infra/relay/kbvpn-*.sh (Task 2 산출 — startup.sh 가 배치할 대상)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md §Pattern 7 (startup-script 항목표), §Pattern 9 (Caddy), §Pitfall 11 (e2-micro 메모리 예산)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md §No Analog Found (gh-radar 최초 IaaS — 설계 정본 지정)
  </read_first>
  <action>
`infra/relay/startup.sh` — GCE `startup-script` 로 매 부팅 실행되므로 **전 단계가 멱등**이어야 한다:
1. swap 1GB — `fallocate -l 1G /swapfile` + `chmod 600` + `mkswap` + `swapon` + `/etc/fstab` 등록(이미 있으면 skip).
2. `apt-get update && apt-get install -y docker.io openconnect vpnc-scripts ca-certificates curl python3 unattended-upgrades`.
3. `/etc/docker/daemon.json` 로그 로테이션 — `{"log-driver":"json-file","log-opts":{"max-size":"10m","max-file":"3"}}` (20GB 디스크에서 로그 폭주 = 디스크 풀 = VM 정지).
4. Caddy 설치 — 공식 apt 저장소(Cloudsmith) 등록 후 `apt-get install -y caddy`. `infra/relay/Caddyfile` 을 `/etc/caddy/Caddyfile` 로 배치하되 **DNS A 레코드가 확인되기 전에는 caddy 를 start 하지 않는다**(Let's Encrypt rate limit — `systemctl disable --now caddy` 로 두고 15-07 체크포인트가 start).
5. VPN 자산 배치 — `kbvpn-fetch-secret.sh`/`kbvpn-connect.sh`/`kbvpn-vpnc-wrapper.sh` → `/usr/local/sbin/`(0700), `openconnect@.service` → `/etc/systemd/system/`(0644), `systemctl daemon-reload`. **`enable`/`start` 하지 않는다**(D-03 선검증에서 사람이 시작).
6. Artifact Registry 인증 — `gcloud` 존재 여부에 의존하지 않도록 `docker-credential-gcr` 설치 또는 메타데이터 토큰으로 `docker login -u oauth2accesstoken` 하는 경로 중 하나를 구현하고, 선택 이유를 주석에 남긴다.
7. **3분 자동 안전장치** — startup-script 안에 "VPN 유닛이 활성 상태로 부팅됐고 3분 뒤에도 기본 경로가 `tun0` 이면 `systemctl stop openconnect@` 을 실행한다"는 oneshot 타이머를 배치한다(Pitfall 10 — 라우팅 탈취로 IAP SSH 까지 막히는 상황의 자가 복구). 최초 검증은 직렬 콘솔로도 접근 가능함을 README 에 남긴다.

`infra/relay/Caddyfile`: 전역 블록에 `email {$CADDY_EMAIL}` 만 두고, 사이트 블록은 `dma.jx1.io` 하나다. 지시어 4개 — `encode zstd gzip`, `reverse_proxy 127.0.0.1:8090`(기본 라우트 = relay wss), `handle /healthz { reverse_proxy 127.0.0.1:8091 }`(내부 포트의 헬스만 공개 노출), `log { output file /var/log/caddy/dma.log · format json }`.

주석으로 남길 것: ① **WebSocket 설정 불필요** — Caddy v2 는 Upgrade 헤더를 자동 통과 ② 80 을 열지 않아도 TLS-ALPN-01(443)로 발급된다 ③ **Caddyfile 리로드 시 WebSocket 이 강제 종료**되므로 장중 변경 금지(필요 시 `stream_close_delay`) ④ access log 는 URI 만 남기고 `Authorization` 헤더를 로그하지 않는다(토큰은 wss 첫 메시지 본문 전용이라 URL 에 없음).

`infra/relay/README.md` — 운영 문서:
- VM 접근: **IAP 터널 SSH** (`gcloud compute ssh radar-gw --tunnel-through-iap --zone=asia-northeast3-a`), 실행 주체에 `roles/iap.tunnelResourceAccessor` 필요.
- Secret 3종 값 주입 절차(값은 미기록).
- VPN 수동 시작/중지/상태 확인 명령(`systemctl start openconnect@kb`, `journalctl -u openconnect@kb -n 50`, `ip -br addr show tun0`, `ip route`).
- **D-03 선검증 체크리스트 7항목**을 그대로 표로 옮긴다(연결 성립 / 터널 IP / 출발지 IP 제한 / Mac 동시 세션 / 라우팅 영향 / 게이트웨이 도달성 `nc -zv 10.41.1.120 9100` **연결만** / 실패 시 3회 초과 금지).
- **D-27 경고**: 게이트웨이 로그인·주문은 사용자 지시 전 금지. `nc` 도달성 확인까지만.
- 메모리 예산표(합계 400~690MB / 1024MB)와 압박 시 e2-small 변경 절차.
- 비밀번호·계정 값은 이 문서에 절대 적지 않는다.
  </action>
  <acceptance_criteria>
    - `bash -n infra/relay/startup.sh` exit 0
    - `grep -c 'fallocate' infra/relay/startup.sh` >= 1 이고 `grep -c 'max-size' infra/relay/startup.sh` >= 1
    - `grep -c 'systemctl enable' infra/relay/startup.sh` == 0 (VPN·Caddy 자동 기동 금지)
    - `grep -c 'reverse_proxy 127.0.0.1:8090' infra/relay/Caddyfile` == 1
    - `grep -c 'dma.jx1.io' infra/relay/Caddyfile` == 1
    - `grep -c '35.235.240.0/20\|tunnel-through-iap' infra/relay/README.md` >= 1
    - `grep -c 'nc -zv 10.41.1.120 9100' infra/relay/README.md` >= 1 이고 같은 표에 "연결만 확인, 로그인·주문 금지" 문구 존재
    - `grep -c 'D-27' infra/relay/README.md` >= 1
    - `grep -rniE 'kbs124|pw=|passwd=|password:' infra/relay/README.md | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>bash -n infra/relay/startup.sh && grep -c 'reverse_proxy 127.0.0.1:8090' infra/relay/Caddyfile && test "$(grep -c 'systemctl enable' infra/relay/startup.sh)" = "0"</automated>
  </verify>
  <done>startup-script·Caddyfile·운영 README 가 존재하고 자동 기동 금지·비밀 미기록·D-03 체크리스트를 포함한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 공인 인터넷 → VM 443 | Caddy TLS 종단. 유일한 공개 인바운드 |
| Cloud Run 서브넷(10.10.0.0/26) → VM 8091 | 내부 주문 경로. 태그 불가 → source-ranges 로만 좁힌다 |
| KB VPN(tun0) → VM 라우팅 테이블 | VPN 이 기본 경로를 푸시하면 관리 경로 전체가 죽는다 |
| Secret Manager → /run/kbvpn.cred(tmpfs) | VPN 비밀번호가 평문으로 존재하는 유일한 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | Information Disclosure | 내부 포트 8091 · 게이트웨이 9100 | mitigate | Task 1: `relay-allow-internal-order` 가 `--source-ranges=10.10.0.0/26` 로만 허용, 공인 개방 규칙 없음. 능동 검증은 15-08 의 INV-7 |
| T-15-11 | DoS (관리 경로 상실) | openconnect 라우팅 | mitigate | Task 2: `CISCO_SPLIT_INC` 래퍼로 10.41.0.0/16 만 터널링. Task 3: 3분 자동 `systemctl stop` 안전장치 + 직렬 콘솔 복구 경로 문서화. 검증은 15-07 체크포인트 |
| T-15-10 | DoS (KB 계정 잠금) | systemd 재시작 정책 | mitigate | `StartLimitIntervalSec=3600` + `StartLimitBurst=5` + `Restart=on-failure`/`RestartSec=30`. startup-script 가 `enable` 하지 않아 부팅 루프도 없다 |
| T-15-13 | DoS (인증서 발급 실패) | Caddy ACME | mitigate | startup-script 가 caddy 를 `disable --now` 로 두고, DNS A 레코드 `dig` 확인 후 15-07 이 수동 start. 80 미개방(TLS-ALPN-01 사용) |
| T-15-26 | Information Disclosure | VPN 비밀번호 | mitigate | Secret Manager → `/run`(tmpfs) 0600 → `ExecStopPost` 삭제. `ExecStart=` 라인·`journalctl`·문서·커밋에 값 없음(grep acceptance 3건) |
| T-15-27 | Elevation of Privilege | VM 서비스 계정 | mitigate | `secretmanager.secretAccessor` 를 프로젝트 레벨이 아니라 **Secret 단위**로 바인딩. `--can-ip-forward` 미부여. Shielded VM 3옵션 활성 |
| T-15-SC | Tampering | `vpn-slice`(PyPI) | accept | 1순위 무의존 `CISCO_SPLIT_INC` 래퍼를 채택했고 자동 설치 코드를 넣지 않는다. 2순위로 전환이 필요하면 별도 `checkpoint:human-verify` 로 승인받는다(15-RESEARCH 가 Flagged 로 표기) |
</threat_model>

<verification>
- `bash -n` 이 4개 셸 스크립트 전부 통과
- `grep -rniE 'kbs124|pw=|passwd=' infra/relay/ scripts/setup-relay-iam.sh | wc -l` == 0
- 방화벽 3규칙·고정 IP 2종·Secret 3종·VM 정의가 `scripts/setup-relay-iam.sh` 에 존재
- **GCP 리소스 변경 0건** — `gcloud compute instances list` 결과가 이 plan 전후로 동일
</verification>

<success_criteria>
- 인프라 전체가 파일로 정의되고 15-07 이 그대로 실행할 수 있다
- split-tunnel 강제·재시도 상한·자동 중지 안전장치가 코드에 존재한다
- 비밀번호·계정 값이 저장소 어디에도 없다
- 이 plan 실행 후에도 GCP 상태는 변하지 않는다
</success_criteria>

<output>
After completion, create `.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-06-SUMMARY.md`
</output>
