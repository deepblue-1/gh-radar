---
phase: 15-dma-relay-kb-gh-trade-server-10-wss
plan: 18
type: execute
wave: 5
depends_on: ["15-17", "15-13"]
files_modified:
  - webapp/src/lib/orders-api.ts
  - webapp/src/components/orderbook/order-panel.tsx
  - webapp/src/components/orderbook/order-confirm-dialog.tsx
  - webapp/src/components/orderbook/account-panel.tsx
  - webapp/src/components/stock/stock-orderbook-section.tsx
  - webapp/src/components/orderbook/__tests__/order-panel.test.tsx
  - webapp/src/components/orderbook/__tests__/account-panel.test.tsx
autonomous: true
requirements: [RELAY-02]
must_haves:
  truths:
    - "SC-7 / D-02a: 호가주문 탭 우측(L1=B 내 계좌 축)에 주문 패널(위)과 잔고·미체결(아래)이 배치되고, 모바일에서는 M1 순서·M3 독립 2섹션으로 나열된다"
    - "SC-6 / D-21: 주문 패널이 신규 매수/매도와 취소만 제공하고 `지정가 · 보통` 고정 칩을 노출하며 정정 경로가 없다"
    - "SC-6 / D-20: 확인 다이얼로그가 신규·취소 모두에 강제되고 `서버에는 금액·수량 한도가 없어요` 를 고지하며 기본 포커스가 취소 버튼이다"
    - "SC-6 / D-23: 미체결 행에서 취소가 가능하고 취소 수량은 미체결 잔량 전부이며 잔량 0 행에는 취소 버튼이 렌더되지 않는다"
    - "SC-6: 5초 초과 응답이 `접수 응답이 늦어지고 있어요` 로 표시되고 **실패로 단정되지 않으며** 재주문 버튼이 비활성된다"
    - "SC-7: 매수/매도가 색·위치·문구 3중으로 일치하고 제출 버튼이 하나뿐이며 취소 버튼은 채움이 아니라 테두리다"
  artifacts:
    - path: "webapp/src/components/orderbook/order-panel.tsx"
      provides: "매수/매도 탭 · 계좌 셀렉터 · 가격 스텝퍼 · 수량(매도 비율) · 주문금액 · 단일 제출 · 결과 배너"
      contains: "지정가 · 보통"
    - path: "webapp/src/components/orderbook/order-confirm-dialog.tsx"
      provides: "주문/취소 확인 다이얼로그 (기본 포커스 취소)"
      contains: "되돌릴 수 없어요"
    - path: "webapp/src/components/orderbook/account-panel.tsx"
      provides: "계좌 셀렉터 + 미체결(취소)/잔고 탭"
      contains: "미체결 주문이 없어요"
    - path: "webapp/src/lib/orders-api.ts"
      provides: "authFetch 기반 주문 REST 클라이언트 + 에러 코드 분기"
      contains: "SESSION_NOT_READY"
  key_links:
    - from: "webapp/src/components/orderbook/order-panel.tsx"
      to: "webapp/src/lib/orders-api.ts"
      via: "createOrder POST /api/orders"
      pattern: "createOrder"
    - from: "미체결 행 order_no"
      to: "취소 요청 orgOrderNo"
      via: "account-panel → order-panel 취소 경로"
      pattern: "orgOrderNo"
    - from: "사다리 가격 클릭"
      to: "주문 가격 입력"
      via: "selectedPrice prop + 테두리 플래시"
      pattern: "selectedPrice"
---

<objective>
호가주문 탭의 **내 계좌 축**을 완성한다. 주문 패널(매수/매도 탭·계좌·가격·수량·제출·결과 배너), 확인 다이얼로그, 계좌 패널(미체결 취소 / 잔고)을 만들고 섹션 셸의 우측 컬럼 placeholder 를 교체한다.

Purpose: SC-6 의 UI 절반 + SC-7 의 나머지. 이 표면은 **되돌릴 수 없는 액션**을 다루므로 UI-SPEC 의 오조작 방지 5규율(색·위치·문구 3중 일치 / 단일 제출 버튼 / 확인 다이얼로그 필수 / 제출 후 재활성 금지 / 취소 버튼 채움 금지)이 전부 acceptance 로 강제된다. `--destructive == --up`, `--primary == --down` 토큰 충돌도 여기서 문제가 된다.
Output: API 클라이언트 1 + 주문 패널 1 + 다이얼로그 1 + 계좌 패널 1 + 섹션 결선 + 테스트 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-UI-SPEC.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-17-SUMMARY.md

<interfaces>
<!-- server 계약 (15-17) -->
POST /api/orders  { code, accountNo, exchange, side, orderType, orgOrderNo?, qty, price }
  200 { orderNo, resultCode, message, status }
  401 UNAUTHENTICATED · 403 DMA_NOT_ALLOWED / ACCOUNT_NOT_ALLOWED · 409 SESSION_NOT_READY
  422 ISIN_UNAVAILABLE · 502 ORDER_TIMEOUT / RELAY_UNAVAILABLE · 503 RELAY_UNAVAILABLE
GET /api/orders?date=YYYY-MM-DD → bare array

<!-- 훅 상태 (15-12) -->
useRelaySocket(...).accounts / .account(잔고·미체결) / .orders / .status

<!-- UI-SPEC 오조작 방지 5규율 (전부 acceptance 대상) -->
1 색·위치·문구 3중 일치 — 매수 = 빨강 `--up` · 왼쪽 탭 · `매수` / 매도 = 파랑 `--down` · 오른쪽 탭 · `매도`
2 단일 제출 버튼 — 매수/매도 버튼을 나란히 두지 않는다
3 확인 다이얼로그 필수(신규·취소 모두), **기본 포커스 = 취소/닫기**
4 제출 후 즉시 재활성 금지 — 응답 또는 5초 전까지 비활성 + `주문 전송 중…`
5 취소 버튼 채움 금지 — `--destructive == --up` 이므로 **테두리 + `--destructive` 텍스트 + `✕`**

<!-- 토큰 충돌 (실측) -->
--destructive == --up  (light/dark 둘 다 동일 oklch)
--primary == --down    → 사다리·주문 버튼에 `--primary` 절대 금지

<!-- Copywriting (verbatim) -->
CTA `매수 주문` / `매도 주문` · 탭 `매수` / `매도` · 고정 칩 `지정가 · 보통`
가격 힌트 `호가 단위 {tick}원 · 사다리의 가격을 클릭하면 여기에 채워져요`
스냅 힌트 `호가 단위(100원)에 맞춰 {값}원으로 맞췄어요`
패널 하단 `신규 매수/매도와 취소만 지원해요 · 정정은 취소 후 다시 주문해 주세요`
접수 `주문이 접수됐어요 · 주문번호 {order_no}` / `체결되면 미체결·잔고가 자동으로 갱신돼요.`
거부 `주문이 거부됐어요 · {message}` / `{해결 안내}. (코드 {result_code})`
5초 초과 `접수 응답이 늦어지고 있어요` / `주문이 이미 나갔을 수 있어요. 미체결 목록에서 접수 여부를 확인한 뒤 다시 주문해 주세요.`
세션 없음 `호가창을 먼저 열어 주세요` / `주문은 호가창이 연결된 상태에서만 보낼 수 있어요. 페이지를 새로고침하면 다시 연결돼요.`
주문 확인 `{매수/매도} 주문을 넣을까요?` / `접수되면 취소하기 전까지 되돌릴 수 없어요.`
확인 경고 `가격·수량을 다시 확인해 주세요. 서버에는 금액·수량 한도가 없어요.`
취소 확인 `미체결 주문을 취소할까요?` / `취소 수량은 미체결 잔량 전부예요.` / 버튼 `✕ 주문 취소` · `닫기`
미체결 빈 `미체결 주문이 없어요` / 잔고 빈 `보유 종목이 없어요`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: orders-api.ts + 주문 패널 + 확인 다이얼로그</name>
  <files>webapp/src/lib/orders-api.ts, webapp/src/components/orderbook/order-panel.tsx, webapp/src/components/orderbook/order-confirm-dialog.tsx</files>
  <read_first>
    - webapp/src/lib/chat-api.ts (analog: `authFetch` — `getSession()` + Authorization + `ApiClientError` — **그대로 복제**)
    - webapp/src/lib/api.ts (에러 envelope → `ApiClientError.code` 변환 + `timeoutMs` 옵션)
    - webapp/src/components/chat/delete-conversation-dialog.tsx (analog: 되돌릴 수 없는 액션 다이얼로그 — 중복 제출 가드 / catch 인라인 피드백 / 닫힘 시 에러 리셋)
    - webapp/src/components/ui/dialog.tsx, ui/button.tsx, ui/toggle-group.tsx, ui/number.tsx
    - webapp/src/styles/globals.css (`--up`/`--down`/`--up-bg`/`--down-bg`/`--destructive`/`--ring` 실측값 — 충돌 확인)
    - packages/shared/src/relay.ts (`CreateOrderRequest`/`CreateOrderResponse`)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-UI-SPEC.md §Component Inventory C8·C9·C12, §Interaction Contract "호가 → 주문 연결"·"오조작 방지", §Color(주문 패널/버튼) + 토큰 충돌 경보, §확정 결정 L4
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-orderbook-mockup.html (채택 목업 — 주문 패널 상세)
  </read_first>
  <action>
**`webapp/src/lib/orders-api.ts`**: `chat-api.ts` 의 `authFetch` 를 복제해 `createOrder(req): Promise<CreateOrderResponse>` 와 `listOrders(date?): Promise<DmaOrderRow[]>` 를 만든다. **`timeoutMs` 를 명시**한다 — 서버가 relay 응답을 최대 5초 기다리므로 클라이언트 타임아웃은 그보다 길게(예: 9000ms) 두고, 그 경계를 코드 주석에 남긴다(`apiFetch` 기본 8초와의 관계 명시). 에러는 `ApiClientError.code` 로 던져 UI 가 `SESSION_NOT_READY`/`DMA_NOT_ALLOWED`/`ACCOUNT_NOT_ALLOWED`/`ORDER_TIMEOUT`/`RELAY_UNAVAILABLE`/`ISIN_UNAVAILABLE` 을 분기하게 한다.

**`webapp/src/components/orderbook/order-panel.tsx`** (`"use client"`, L4 = 매수/매도 탭 전환 · 단일 폼 · 단일 제출 버튼):
- props: `{ code, accounts, exchange, selectedPrice, tick, sellableQty, status, onSubmitted }`.
- **매매 구분 탭**: `ui/toggle-group.tsx`, `role="tablist"` + `aria-label="매매 구분"`. 매수 = **왼쪽** 탭, `--up-bg` 배경 + `--up` 텍스트/테두리. 매도 = **오른쪽** 탭, `--down-bg` + `--down`. **자동 전환 절대 금지** — 매도호가를 클릭해도 매수 탭 그대로.
- **계좌 셀렉터**: 네이티브 `<select>` + `<label>` 명시 연결(shadcn `select` 미설치 — 계좌 2~3개에 과잉). 토큰(`--input`/`--bg`/`--fg`/`--r`)으로 스타일링. 계좌번호는 **화면에 전체 표시**(`12345678-01 · 위탁종합`, D2).
- **주문 유형**: `지정가 · 보통` **고정 칩**(변경 불가). 시장가·정정 없음(D-21).
- **가격**: 숫자 입력 + `−`/`＋` 스텝퍼(**호가 단위 `tick`** 단위 증감, `aria-label="호가 한 단계 올리기"`/`내리기`). 직접 입력 후 blur 시 가장 가까운 tick 으로 스냅하고, 스냅이 일어나면 힌트를 `호가 단위({tick})원에 맞춰 {값}원으로 맞췄어요` 로 1회 교체. `selectedPrice` prop 변경 시 입력값 반영 + **입력칸 테두리 1회 플래시(260ms, `--ring`)**.
- **수량**: `inputmode="numeric"`. **매도 탭에서만** 비율 버튼 `10% 25% 50% 100%`(기준 = 잔고 `sellableQty`). **매수 비율 버튼은 만들지 않는다** — `AccountState` 에 주문가능금액이 없어 근거 데이터가 없다.
- **주문금액**: `가격 × 수량` 을 `ui/number.tsx` 로 표시(방향색 없음).
- **제출 버튼 1개**: 활성 탭 색을 이어받아 매수 `--up` 채움 / 매도 `--down` 채움, 텍스트 `--primary-fg`. `size="lg"`(40px). 문구 `매수 주문` / `매도 주문`. **`--primary` 를 쓰지 않는다**(=`--down` 이라 매수 버튼이 파랗게 된다).
- **상태별 비활성**: UI-SPEC 표대로 — `connecting`/`logging_in`/`declaring` → `연결 중…`, `reconnecting` → `재접속 중…`, `failed` → `연결 실패`, `session_rejected` → `로그인 거부됨`, `unauthorized` → 패널 **미표시**.
- **제출 흐름**: 클릭 → **확인 다이얼로그** → 확인 시 `createOrder` 호출 + 버튼 비활성 + `주문 전송 중…`. 응답(또는 타임아웃) 전까지 재활성 금지. `if (submitting) return` 중복 제출 가드.
- **결과 배너(C9)**: 접수 = `role="status"` 중립 톤 `주문이 접수됐어요 · 주문번호 {order_no}` / 거부 = `role="alert"` `--destructive` 테두리 `주문이 거부됐어요 · {message}` / **`ORDER_TIMEOUT` = 중립 톤** `접수 응답이 늦어지고 있어요` + 재주문 버튼 비활성. **"실패했어요"라고 쓰지 않는다.** `SESSION_NOT_READY`(409) → `호가창을 먼저 열어 주세요` 안내.
- 패널 하단 상시 안내: `신규 매수/매도와 취소만 지원해요 · 정정은 취소 후 다시 주문해 주세요`.
- **토스트를 쓰지 않는다**(저장소에 토스트 라이브러리 없음 — 인라인 배너 + 상태 바 누적으로 통일).

**`webapp/src/components/orderbook/order-confirm-dialog.tsx`**: `ui/dialog.tsx`(Radix 포커스 트랩·ESC 상속). 두 모드(신규 / 취소).
- 신규: 제목 `{매수/매도} 주문을 넣을까요?` / 부제 `접수되면 취소하기 전까지 되돌릴 수 없어요.` / 요약 7항목(종목(코드)·계좌·거래소·주문유형·가격·수량·**주문금액**) / 경고 박스 `가격·수량을 다시 확인해 주세요. 서버에는 금액·수량 한도가 없어요.`(D-20 사실 고지) / 버튼 `{매수/매도} 주문`(채움 `--up`/`--down`) · `취소`(outline, **기본 포커스**).
- 취소: 제목 `미체결 주문을 취소할까요?` / 부제 `취소 수량은 미체결 잔량 전부예요.` / 요약 4항목(주문번호·구분·종목·주문가·미체결 수량) / 버튼 `✕ 주문 취소`(**테두리** `--destructive`, `variant="destructive"` 채움 **금지**) · `닫기`(outline, **기본 포커스**).
- `delete-conversation-dialog.tsx` 에서 가져올 것: 중복 제출 가드, `catch` 인라인 피드백(unhandled rejection 방지), 닫힘 시 에러 리셋. **다른 점: 기본 포커스가 삭제가 아니라 취소/닫기 버튼**이다.
  </action>
  <acceptance_criteria>
    - `grep -c '지정가 · 보통' webapp/src/components/orderbook/order-panel.tsx` == 1
    - `grep -c 'variant="destructive"' webapp/src/components/orderbook/order-confirm-dialog.tsx` == 0 (취소 채움 금지 — 토큰 충돌)
    - `grep -cE '\-\-primary\b' webapp/src/components/orderbook/order-panel.tsx` == 0
    - `grep -c '서버에는 금액·수량 한도가 없어요' webapp/src/components/orderbook/order-confirm-dialog.tsx` == 1
    - `grep -c '접수 응답이 늦어지고 있어요' webapp/src/components/orderbook/order-panel.tsx` == 1 이고 `grep -c '주문에 실패' webapp/src/components/orderbook/order-panel.tsx` == 0
    - `grep -c '신규 매수/매도와 취소만 지원해요' webapp/src/components/orderbook/order-panel.tsx` == 1
    - `grep -c 'submitting' webapp/src/components/orderbook/order-panel.tsx` >= 2 (중복 제출 가드 + 비활성)
    - `grep -c 'autoFocus' webapp/src/components/orderbook/order-confirm-dialog.tsx` >= 1 (취소/닫기 버튼에)
    - `grep -c 'SESSION_NOT_READY' webapp/src/lib/orders-api.ts` >= 1
    - `pnpm --filter webapp run typecheck` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter webapp run typecheck</automated>
  </verify>
  <done>주문 패널·다이얼로그가 오조작 방지 5규율과 Copywriting verbatim 을 만족하고 typecheck 통과.</done>
</task>

<task type="auto">
  <name>Task 2: 계좌 패널(미체결 취소 / 잔고) + 섹션 결선</name>
  <files>webapp/src/components/orderbook/account-panel.tsx, webapp/src/components/stock/stock-orderbook-section.tsx</files>
  <read_first>
    - webapp/src/components/ui/table.tsx + webapp/src/styles/globals.css `.tbl-wrap`(`webapp/src/app/design/_sections/numbers.tsx` L98·L135 사용례)
    - webapp/src/components/orderbook/order-panel.tsx, order-confirm-dialog.tsx (Task 1 산출)
    - webapp/src/components/stock/stock-orderbook-section.tsx (수정 대상 — 15-13 의 `orderbook-account-column` placeholder)
    - webapp/src/lib/use-relay-socket.ts (`account` 상태 — holdings/unfilled/removed)
    - webapp/src/lib/orders-api.ts (Task 1 산출 — 취소도 `createOrder({orderType:"C"})`)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-UI-SPEC.md §Component Inventory C10·C11, §확정 결정 L1·L5·M1·M3·D2, §Spacing "행 높이 밀도 계약"·"미체결 취소 버튼 26px 예외", §반응형 표
  </read_first>
  <action>
**`webapp/src/components/orderbook/account-panel.tsx`** (`"use client"`):
- props: `{ accounts, selectedAccountNo, onAccountChange, account, code, onCancelSubmitted }`.
- ≥900px: **탭 전환**(L5) — `ui/toggle-group.tsx` 로 `미체결` / `잔고`, **기본 탭 `미체결`**, 라벨에 건수 표기(`미체결 (3)`).
- <900px: **탭이 아니라 독립 2섹션 세로 나열**(M3), 표는 가로 스크롤 허용.
- 표는 `ui/table.tsx` + `.tbl-wrap`. 밀도 `data-density="default"`(36px). 좁은 폭(<900px)에서는 행 44px + 취소 버튼 `size="sm"`(32px).
- **미체결 표**: 주문번호 · 구분(`▲ 매수`/`▼ 매도` 부호 병기) · 종목 · 주문가 · 주문수량 · **미체결 잔량** · 취소 버튼. 취소 버튼(C11)은 `✕ 취소`, **`--destructive` 테두리**(채움 금지), 26px(행 36px 안), `aria-label="주문번호 {order_no} 취소"`. **미체결 잔량이 0 이면 취소 버튼을 렌더하지 않는다**(D-21 — 취소수량 0 은 즉시 거부).
- 취소 클릭 → **취소 확인 다이얼로그** → 확인 시 `createOrder({ code, accountNo, exchange, side, orderType: "C", orgOrderNo: order_no, qty: unfilledQty, price })`. 취소 수량은 **미체결 잔량 전부**.
- **잔고 표**: 종목 · 보유수량 · 매도가능수량 · 평균단가 · 평가금액 · 평가손익 · 수익률. 평가손익·수익률은 `ui/number.tsx` `withColor`(방향색 허용 대상). 나머지 숫자는 `withColor` 없이.
- 빈 상태: `미체결 주문이 없어요` / `주문을 넣으면 여기에 표시되고, 여기서 바로 취소할 수 있어요.` · `보유 종목이 없어요` / `체결된 주문이 있으면 잔고에 반영돼요.`
- 세션 미준비 시 표 흐림 + 취소 버튼 비활성.
- 계좌 셀렉터는 이 패널이 소유하고 선택값을 주문 패널과 **공유**한다(섹션 셸이 상태를 들고 양쪽에 내려준다).

**`webapp/src/components/stock/stock-orderbook-section.tsx`** 결선:
- `orderbook-account-column` placeholder 를 `<OrderPanel .../>` + `<AccountPanel .../>` 로 교체하고 `주문 패널은 준비 중이에요` 문구를 제거한다.
- 계좌 선택 상태(`selectedAccountNo`)를 섹션이 소유하고 `accounts[0]` 을 기본값으로 둔다. `accounts` 가 바뀌면(재접속·계좌 재선언) 선택값이 목록에 없을 때만 초기화한다.
- 사다리의 `selectedPrice` 를 주문 패널에 전달한다(15-13 이 보관하던 상태).
- `sellableQty` 는 `account` 의 해당 종목 잔고에서 뽑아 주문 패널에 전달한다.
- 모바일 세로 순서 M1 을 완성한다: 연결상태 → 호가 5단 → **주문 패널** → 체결 → **잔고** → **미체결**. 주문 패널 sticky 없음(M2).
  </action>
  <acceptance_criteria>
    - `grep -c '미체결 주문이 없어요' webapp/src/components/orderbook/account-panel.tsx` == 1
    - `grep -c '보유 종목이 없어요' webapp/src/components/orderbook/account-panel.tsx` == 1
    - `grep -c 'variant="destructive"' webapp/src/components/orderbook/account-panel.tsx` == 0 (취소 채움 금지)
    - `grep -c 'aria-label={`주문번호' webapp/src/components/orderbook/account-panel.tsx` >= 1 (또는 동등한 aria-label 구성)
    - `grep -c 'data-density="default"' webapp/src/components/orderbook/account-panel.tsx` >= 1
    - `grep -c '주문 패널은 준비 중이에요' webapp/src/components/stock/stock-orderbook-section.tsx` == 0 (placeholder 제거)
    - `grep -c 'OrderPanel' webapp/src/components/stock/stock-orderbook-section.tsx` >= 1 이고 `grep -c 'AccountPanel' ...` >= 1
    - `pnpm --filter webapp run typecheck && pnpm --filter webapp run build` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter webapp run typecheck && pnpm --filter webapp test</automated>
  </verify>
  <done>계좌 패널이 미체결 취소·잔고를 렌더하고 섹션 우측 컬럼이 완성됨. 빌드 통과.</done>
</task>

<task type="auto">
  <name>Task 3: 주문·계좌 패널 컴포넌트 테스트</name>
  <files>webapp/src/components/orderbook/__tests__/order-panel.test.tsx, webapp/src/components/orderbook/__tests__/account-panel.test.tsx</files>
  <read_first>
    - webapp/src/components/stock/__tests__/stock-comovement-section.test.tsx (analog: 모듈 mock + render + userEvent)
    - webapp/src/components/orderbook/order-panel.tsx, order-confirm-dialog.tsx, account-panel.tsx (Task 1·2 산출)
    - webapp/src/lib/orders-api.ts (mock 대상)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-UI-SPEC.md §Interaction Contract "오조작 방지", §Copywriting "주문 CTA · 결과"
  </read_first>
  <action>
`vi.mock('@/lib/orders-api')` 로 REST 를 스텁하고 `@testing-library/react` + `userEvent` 로 검증한다. **경로는 `src/**/__tests__/` co-located.**

`order-panel.test.tsx` 케이스 14건:
1. 기본 탭이 `매수` 이고 제출 버튼 문구가 `매수 주문`.
2. `매도` 탭 선택 → 제출 버튼 `매도 주문` 으로 바뀌고 **버튼이 1개뿐**(문서에 `매수 주문`/`매도 주문` 동시 존재 금지).
3. `selectedPrice` prop 변경 → 가격 입력값이 반영되고 **매매 구분 탭은 그대로**(자동 전환 금지).
4. 가격 스텝퍼 `＋` → tick 만큼 증가.
5. 임의 가격 입력 후 blur → 가장 가까운 tick 으로 스냅 + 스냅 힌트 문구 표시.
6. 매도 탭에서만 비율 버튼 4개가 보이고 **매수 탭에는 없음**.
7. `50%` 클릭 → 수량이 `sellableQty/2`(내림).
8. 제출 클릭 → **확인 다이얼로그가 열리고** `createOrder` 는 아직 호출되지 않음.
9. 다이얼로그 기본 포커스가 **취소 버튼**.
10. 다이얼로그에 `서버에는 금액·수량 한도가 없어요` 경고 존재.
11. 확인 클릭 → `createOrder` 1회 호출 + 버튼 비활성 + `주문 전송 중…`. 연타해도 **호출 1회**(중복 제출 가드).
12. 성공 응답 → `주문이 접수됐어요 · 주문번호 …` 배너(`role="status"`).
13. 거부 응답 → `주문이 거부됐어요 · …` 배너(`role="alert"`).
14. `ORDER_TIMEOUT` 에러 → `접수 응답이 늦어지고 있어요` + **"실패" 문자열 없음** + 재주문 버튼 비활성.
15. `SESSION_NOT_READY`(409) → `호가창을 먼저 열어 주세요`.
16. `status: "unauthorized"` → 패널이 렌더되지 않음.

`account-panel.test.tsx` 케이스 9건:
1. 미체결 3건 → 3행 + 탭 라벨 `미체결 (3)`.
2. 기본 탭이 `미체결`.
3. 미체결 잔량 0 행 → **취소 버튼 없음**.
4. 취소 클릭 → **취소 확인 다이얼로그** 열림, 기본 포커스 `닫기`.
5. 확인 → `createOrder` 가 `orderType:"C"`, `orgOrderNo`=그 주문번호, `qty`=미체결 잔량으로 호출.
6. 취소 버튼에 채움 스타일(`variant="destructive"`)이 **아님**을 DOM 클래스로 확인.
7. 잔고 탭 → 평가손익·수익률이 방향색으로 렌더.
8. 미체결 0건 → `미체결 주문이 없어요` / 잔고 0건 → `보유 종목이 없어요`.
9. 계좌번호가 화면에 **전체 표시**됨(마스킹 없음 — D2).
  </action>
  <acceptance_criteria>
    - `pnpm --filter webapp test order-panel` exit 0, 16건 이상 통과
    - `pnpm --filter webapp test account-panel` exit 0, 9건 이상 통과
    - `grep -c '접수 응답이 늦어지고 있어요' webapp/src/components/orderbook/__tests__/order-panel.test.tsx` >= 1
    - `grep -c 'orderType' webapp/src/components/orderbook/__tests__/account-panel.test.tsx` >= 1
    - `pnpm --filter webapp test` exit 0 (기존 전체 회귀 0)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter webapp test</automated>
  </verify>
  <done>주문·계좌 패널 25+ 테스트 green (오조작 방지 5규율 + 결과 4분기 + 취소 경로).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 입력 → 주문 요청 | 되돌릴 수 없는 금융 액션. UI 가 마지막 방어선 |
| 서버 에러 코드 → 사용자 문구 | 잘못 매핑하면 중복 주문·오판을 유발 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14 | (안전성) 오주문 | 주문 패널 · 확인 다이얼로그 | mitigate | 오조작 방지 5규율 전부 acceptance 로 강제 — ① 색·위치·문구 3중 일치 ② 단일 제출 버튼(테스트 ② 가 동시 존재 금지 단언) ③ 확인 다이얼로그 필수 + **기본 포커스 취소** ④ 제출 후 재활성 금지(중복 제출 가드, 테스트 ⑪) ⑤ 취소 버튼 채움 금지 |
| T-15-52 | (안전성) 방향 오조작 | 호가 클릭 → 매매 구분 자동 전환 | mitigate | **매수/매도 탭 자동 전환 금지**(테스트 ③). 매도호가를 클릭해도 매수 탭 유지 |
| T-15-53 | (안전성) 시각 혼동 | `--destructive == --up`, `--primary == --down` | mitigate | 취소 버튼은 테두리 + `✕`(채움 금지, grep acceptance). 주문 패널에 `--primary` 사용 0건(grep acceptance) |
| T-15-48 | (안전성) 중복 주문 | 5초 타임아웃 문구 | mitigate | `접수 응답이 늦어지고 있어요` + `미체결 목록에서 접수 여부를 확인한 뒤` + 재주문 버튼 비활성. **"실패" 문자열 금지**(grep acceptance + 테스트 ⑭) |
| T-15-54 | (안전성) 취소 오류 | 미체결 잔량 0 | mitigate | 잔량 0 행에 취소 버튼 미렌더(테스트 ③). 취소 수량은 항상 미체결 잔량 전부 |
| T-15-15 | Information Disclosure | 계좌번호 표시 | accept | **화면은 전체 표시**가 사용자 결정(D2) — 본인 계좌이고 화면은 본인만 본다. 마스킹은 로그에서만(15-16) |
| T-15-14b | (안전성) 한도 없음 고지 | 확인 다이얼로그 | mitigate | `서버에는 금액·수량 한도가 없어요` 경고 박스로 D-20 을 사용자에게 사실 고지(grep acceptance) |
</threat_model>

<verification>
- `pnpm --filter webapp test` exit 0 (order-panel 16+, account-panel 9+, 기존 전체)
- `pnpm --filter webapp run typecheck && run build` exit 0
- `grep -c 'variant="destructive"' webapp/src/components/orderbook/order-confirm-dialog.tsx webapp/src/components/orderbook/account-panel.tsx` == 0
- `grep -cE '\-\-primary\b' webapp/src/components/orderbook/order-panel.tsx` == 0
</verification>

<success_criteria>
- 주문 패널이 신규 매수/매도 + 미체결 취소를 제공하고 정정 경로가 없다
- 확인 다이얼로그가 신규·취소 모두에 강제되고 기본 포커스가 취소다
- 5초 초과 응답이 실패로 단정되지 않고 재주문이 차단된다
- 잔고·미체결이 실시간으로 표시되고 미체결에서 바로 취소할 수 있다
- 색·위치·문구 3중 일치와 취소 버튼 테두리 규율이 코드로 강제된다
</success_criteria>

<output>
After completion, create `.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-18-SUMMARY.md`
</output>
