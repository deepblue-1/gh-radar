---
phase: 15-dma-relay-kb-gh-trade-server-10-wss
plan: 07
type: execute
wave: 3
depends_on: ["15-06"]
files_modified:
  - infra/relay/README.md
  - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-VPN-PREFLIGHT.md
autonomous: false
requirements: [RELAY-03]
must_haves:
  truths:
    - "SC-2 / D-03: kbs124 계정으로 VM 에서 openconnect 연결이 성립하는지, 출발지 공인 IP 제한이 있는지, Mac 세션과 동시 접속이 되는지가 **수동 ≤3회**로 검증·기록됐다 (실패 시 자동 재시도 없이 중단)"
    - "SC-2 / D-09: VM `radar-gw` 가 RUNNING 이고 방화벽 3규칙이 `gh-radar-vpc` 에 존재하며 IAP 터널 SSH 로 접속된다"
    - "SC-2: VPN 연결 후에도 기본 경로가 `ens4` 로 유지되고(`curl ifconfig.me` = VM 공인 IP) Secret Manager·Artifact Registry 도달성이 살아 있다 (split-tunnel 성립)"
    - "SC-8 / D-27: 게이트웨이는 `nc -zv 10.41.1.120 9100` 도달성만 확인하고 로그인·주문은 하지 않는다"
    - "SC-8 / D-06: 사용자가 `dma.jx1.io` A 레코드를 예약 고정 IP 로 등록했고 `dig` 로 확인된 **뒤에** Caddy 가 기동돼 인증서가 발급됐다"
  artifacts:
    - path: ".planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-VPN-PREFLIGHT.md"
      provides: "D-03 선검증 7항목 결과 기록 (터널 IP·동시 세션·출발지 IP 제한·라우팅 diff·도달성)"
      contains: "동시 세션"
  key_links:
    - from: "예약 고정 IP gh-radar-relay-ip"
      to: "dma.jx1.io A 레코드"
      via: "사용자 DNS 등록 → dig 확인"
      pattern: "dig \\+short dma.jx1.io"
    - from: "Caddy 기동"
      to: "Let's Encrypt 발급"
      via: "DNS 확인 후에만 start"
      pattern: "systemctl start caddy"
---

<objective>
15-06 이 파일로 정의한 인프라를 **실제로 실행**한다. GCP 리소스 생성 → IAP SSH 확인 → **[BLOCKING] kbs124 VPN 선검증(D-03)** → **[BLOCKING] DNS A 레코드(D-06)** → Caddy 기동·인증서 발급 순서로 진행하고, 선검증 결과를 문서로 남긴다.

Purpose: SC-2 의 전부. 이 phase 의 두 외부 좌표(KB 계정, 사용자 도메인)를 여기서 해소한다. VPN 선검증은 **KB 계정 잠금 위험** 때문에 자동 재시도가 금지된 수동 ≤3회 시도이고, DNS 는 Let's Encrypt rate limit 때문에 확인 후에만 Caddy 를 켠다. 두 게이트 모두 사용자 참여가 필수라 이 plan 은 `autonomous: false` 다.
Output: 실행된 GCP 인프라 + 선검증 기록 문서 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-06-SUMMARY.md

<interfaces>
<!-- 실행할 자산 (15-06 산출) -->
scripts/setup-relay-iam.sh           # --dry-run 지원. SA·Secret 3·고정 IP 2·방화벽 3·VM
infra/relay/startup.sh               # VM startup-script (멱등). VPN·Caddy 는 enable 하지 않음
infra/relay/openconnect@.service     # StartLimitBurst=5/1h, ExecStartPre=fetch-secret
infra/relay/kbvpn-vpnc-wrapper.sh    # CISCO_SPLIT_INC 10.41.0.0/16 강제
infra/relay/README.md                # 운영 절차 + D-03 체크리스트 7항목

<!-- GCP 인증 (프로젝트 메모리 reference_gh_radar_deployer_sa) -->
Key `~/.config/gcloud/gh-radar-deployer.json`
env `GOOGLE_APPLICATION_CREDENTIALS` + `CLOUDSDK_CORE_PROJECT=gh-radar`
→ 사용자에게 credential 을 재요청하지 않는다. 먼저 이 경로를 확인할 것.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GCP 인프라 생성 + IAP SSH 도달 확인</name>
  <files>infra/relay/README.md</files>
  <read_first>
    - scripts/setup-relay-iam.sh (실행 대상 — dry-run 출력을 먼저 검토)
    - infra/relay/startup.sh (VM 메타데이터로 들어갈 내용 확인)
    - infra/relay/README.md (수정 대상 — 실측 값을 기록)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md §Pitfall 12 (방화벽 0개 → IAP SSH 불가)
  </read_first>
  <action>
GCP 인증을 먼저 확인한다 — `GOOGLE_APPLICATION_CREDENTIALS=~/.config/gcloud/gh-radar-deployer.json`, `CLOUDSDK_CORE_PROJECT=gh-radar`, active configuration `gh-radar`. **사용자에게 credential 을 다시 묻지 않는다**(프로젝트 메모리 규칙).

1. `GCP_PROJECT_ID=gh-radar bash scripts/setup-relay-iam.sh --dry-run` 을 먼저 돌려 생성 계획을 확인한다. 계획에 `gh-radar-static-ip` 재사용이나 `--can-ip-forward` 가 들어가 있으면 **중단하고 15-06 을 고친다**.
2. `GCP_PROJECT_ID=gh-radar bash scripts/setup-relay-iam.sh` 실행. 생성 결과 확인:
   - SA `gh-radar-relay-sa@gh-radar.iam.gserviceaccount.com`
   - Secret 3종(값은 아직 비어 있을 수 있음)
   - 외부 고정 IP `gh-radar-relay-ip`(값을 기록), 내부 고정 IP `10.10.0.5`
   - 방화벽 3규칙 — `gcloud compute firewall-rules list --filter='network=gh-radar-vpc'` 가 **정확히 3행**
   - VM `radar-gw` STATUS=RUNNING
3. Secret 3종에 값을 주입한다. `gh-radar-relay-order-secret` 과 `gh-radar-dma-cred-key` 는 **로컬에서 생성**해 주입한다(`openssl rand -base64 32`). 생성 값을 대화 로그·커밋·문서에 출력하지 않는다. `gh-radar-kb-vpn-password` 는 **사용자에게 값을 요청하지 말고**, 값이 이미 있는지 먼저 확인한 뒤 없으면 Task 2 체크포인트에서 사용자가 직접 `gcloud secrets versions add` 로 넣도록 안내한다.
4. IAP SSH 도달 확인: `gcloud compute ssh radar-gw --tunnel-through-iap --zone=asia-northeast3-a --command='echo ok && free -m && systemctl is-enabled caddy openconnect@kb || true'`. 기대: `ok` + 메모리 표 출력 + caddy/openconnect 가 **enabled 가 아님**(15-06 규율).
   - 접속이 안 되면 실행 주체에 `roles/iap.tunnelResourceAccessor` 가 있는지 확인한다.
5. startup-script 완료 확인: `sudo journalctl -u google-startup-scripts -n 50` 에 오류가 없고, `swapon --show` 가 1G, `docker --version` / `openconnect --version` / `caddy version` 이 전부 응답.
6. 실측 값을 `infra/relay/README.md` 의 "현재 배포 상태" 절에 기록한다 — 외부 고정 IP, 내부 IP, 존, 이미지, 부팅 디스크, `free -m` 결과. **비밀 값은 기록하지 않는다.**
  </action>
  <acceptance_criteria>
    - `gcloud compute instances describe radar-gw --zone=asia-northeast3-a --format='value(status)'` 출력 == `RUNNING`
    - `gcloud compute firewall-rules list --filter='network=gh-radar-vpc' --format='value(name)' | wc -l` == 3
    - `gcloud compute addresses describe gh-radar-relay-ip --region=asia-northeast3 --format='value(status)'` == `IN_USE`
    - `gcloud compute instances describe radar-gw --zone=asia-northeast3-a --format='value(canIpForward)'` == `False`
    - `gcloud compute ssh radar-gw --tunnel-through-iap --zone=asia-northeast3-a --command='echo ok'` 출력에 `ok` 포함
    - VM 에서 `systemctl is-enabled caddy` 결과가 `enabled` 가 **아님** (DNS 확인 전 기동 금지)
    - `grep -c '현재 배포 상태' infra/relay/README.md` >= 1 이고 그 절에 외부 고정 IP 값 존재
    - `gcloud secrets list --filter='name~gh-radar-(dma-cred-key|relay-order-secret|kb-vpn-password)' --format='value(name)' | wc -l` == 3
  </acceptance_criteria>
  <verify>
    <automated>gcloud compute instances describe radar-gw --zone=asia-northeast3-a --format='value(status)' | grep -q RUNNING && test "$(gcloud compute firewall-rules list --filter='network=gh-radar-vpc' --format='value(name)' | wc -l | tr -d ' ')" = "3"</automated>
  </verify>
  <done>VM RUNNING + 방화벽 3규칙 + 고정 IP IN_USE + IAP SSH 도달 + Secret 3종 존재. README 에 실측 기록.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] kbs124 VPN 선검증 (D-03 · 수동 ≤3회 · 실패 시 자동 재시도 금지)</name>
  <files>.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-VPN-PREFLIGHT.md</files>
  <read_first>
    - infra/relay/README.md §D-03 선검증 체크리스트 (7항목 표)
    - infra/relay/openconnect@.service, infra/relay/kbvpn-connect.sh, infra/relay/kbvpn-vpnc-wrapper.sh (실행될 자산)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md §Pattern 8 (선검증 체크리스트), §Pitfall 10 (라우팅 탈취)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md D-03, D-27
  </read_first>
  <action>
VM `radar-gw` 에서 openconnect 로 KB VPN 에 **최초 1회** 연결을 시도하고 D-03 선검증 체크리스트 7항목(연결 성립 · 터널 IP · 출발지 공인 IP 제한 · Mac 세션 동시 접속 · 라우팅 영향 · 게이트웨이 도달성 · 실패 처리)을 사용자와 함께 관찰·기록한다. 시도는 **최대 3회**이며 실패해도 자동 재시도하지 않는다(KB 계정 잠금 방지). 게이트웨이는 `nc -zv 10.41.1.120 9100` 도달성만 확인하고 **로그인·주문은 하지 않는다**(D-27). 결과를 `15-VPN-PREFLIGHT.md` 에 표로 남기고 STATE.md 에 한 줄 요약을 추가한다. 비밀번호·계정 값은 어떤 산출물에도 기록하지 않는다.
  </action>
  <what-built>
VM `radar-gw` 가 RUNNING 이고 openconnect·split-tunnel 래퍼·systemd 유닛(재시도 상한 5회/1h)·Secret 페치 스크립트가 배치돼 있다. 아직 **한 번도 VPN 을 연결하지 않았다.** 이 체크포인트가 최초 1회 연결을 사용자와 함께 관찰한다.
  </what-built>
  <how-to-verify>
**전제:** `gh-radar-kb-vpn-password` Secret 에 값이 들어 있어야 한다. 값이 비어 있으면 사용자가 먼저 `gcloud secrets versions add gh-radar-kb-vpn-password --data-file=-` 로 주입한다(값은 대화·문서·커밋에 남기지 않는다). 또 `/etc/kbvpn.env` 에 서버 주소·authgroup·servercert 핀·DMA 계정 ID 가 배치돼 있어야 한다.

**규율(D-03):** 시도는 **최대 3회**. 실패해도 **자동 재시도하지 않는다.** 3회 초과는 KB 계정 잠금 위험이므로 즉시 중단하고 KB 문의로 전환한다.

1. IAP SSH 로 접속하고, 라우팅 탈취 대비로 **별도 터미널에 직렬 콘솔**(`gcloud compute connect-to-serial-port radar-gw --zone=asia-northeast3-a`)을 열어 둔다.
2. 연결 전 상태를 기록: `ip route`, `curl -s ifconfig.me`, `curl -sI https://secretmanager.googleapis.com | head -1`.
3. `sudo systemctl start openconnect@kb` (1회). `journalctl -u openconnect@kb -f` 로 관찰.
4. 체크리스트 7항목을 기록한다:
   - ① 연결 성립 — `ip -br addr show tun0` 에 주소가 있는가
   - ② 터널 IP 값 (Mac 실측은 3회 모두 10.41.1.124 — 계정 고정 추정)
   - ③ **출발지 공인 IP 제한 여부** — GCE 외부 IP 에서 연결 성공하면 제한 없음. 실패하면 KB 문의로 전환
   - ④ **Mac 세션과 동시 접속** — Mac 터널을 유지한 채 VM 연결 시도. 둘 다 유지되는가, 한쪽이 끊기는가
   - ⑤ **라우팅 영향** — 연결 전/후 `ip route` diff 에서 기본 경로가 `ens4` 로 유지되는가. `curl -s ifconfig.me` 가 여전히 VM 공인 IP 인가. `curl -sI https://secretmanager.googleapis.com` 이 200/4xx 로 응답하는가(무응답이면 라우팅 탈취 — 즉시 `sudo systemctl stop openconnect@kb`)
   - ⑥ **게이트웨이 도달성** — `nc -zv 10.41.1.120 9100`. **연결만 확인한다. 로그인·주문 금지(D-27).**
   - ⑦ 실패 시 — 자동 재시도 없이 중단, 시도 횟수 기록, 사용자에게 즉시 보고
5. 검증 후 `sudo systemctl stop openconnect@kb` 로 내린다(상시 기동은 15-08 배포 시점에 결정).
6. 결과를 `.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-VPN-PREFLIGHT.md` 에 표로 기록한다 — 7항목 결과, 시도 횟수, 관측된 라우팅 diff, `tun0` IP, 동시 세션 결과, 다음 조치. **비밀번호·계정 값은 기록하지 않는다.**
7. STATE.md 의 Accumulated Context 에 한 줄 요약을 추가한다.

**실패 시 진로:** ③ 또는 ④ 가 불가로 판명되면 phase 재설계가 필요하다(전용 계정 발급 또는 Mac 상시 릴레이). 즉시 사용자에게 보고하고 KB 문의로 전환한다. **코드 wave(15-09~15-14)는 mock 으로 계속 진행 가능하다** — 이 실패가 전체를 막지 않는다.
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-VPN-PREFLIGHT.md` 가 존재하고 7항목 결과 행이 전부 채워져 있다
    - 문서에 `시도 횟수` 필드가 있고 값이 3 이하
    - 문서에 `기본 경로` 항목이 있고 연결 후 값이 기록돼 있다 (`ens4` 유지 여부)
    - `grep -c '10.41.1.120' 15-VPN-PREFLIGHT.md` >= 1 이고 그 행에 "도달성만 확인 · 로그인/주문 없음" 명시
    - `grep -rniE 'kbs124|pw=|passwd=|password:' .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-VPN-PREFLIGHT.md | wc -l` == 0
    - VM 에서 `systemctl is-active openconnect@kb` 가 검증 종료 후 `inactive`
  </acceptance_criteria>
  <verify>
    <manual>KB 계정 잠금 위험 때문에 자동 재시도가 금지된 수동 검증이다(D-03). 자동화 명령으로 반복 실행할 수 없다. 사후 산출물 검증만 자동화한다.</manual>
    <automated>test -f .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-VPN-PREFLIGHT.md && test "$(grep -rniE 'kbs124|pw=|passwd=|password:' .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-VPN-PREFLIGHT.md | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>선검증 7항목이 문서화되고 시도 횟수 ≤3, 검증 후 `openconnect@kb` 가 inactive, 비밀 값 기록 0건.</done>
  <resume-signal>선검증 7항목 기록 완료 후 "선검증 통과" 또는 "선검증 실패: {항목}" 입력. 실패 시 KB 문의로 전환하고 코드 wave 를 계속 진행할지 지시</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] dma.jx1.io A 레코드 등록 + Caddy 기동·인증서 발급 (D-06)</name>
  <files>infra/relay/README.md</files>
  <read_first>
    - infra/relay/Caddyfile (배치될 설정 — dma.jx1.io + reverse_proxy 8090)
    - infra/relay/README.md (현재 배포 상태 절 — 고정 IP 값)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md §Pattern 9 (Caddy 자동 HTTPS, 80 불필요)
  </read_first>
  <action>
Task 1 이 예약한 외부 고정 IP 값을 사용자에게 전달해 `jx1.io` 에 `dma` A 레코드를 등록하게 하고, `dig +short dma.jx1.io` 가 그 IP 를 반환하는 것을 확인한 **뒤에만** VM 에서 `systemctl enable --now caddy` 로 Caddy 를 기동해 Let's Encrypt 인증서를 발급받는다. DNS 확인 전 기동은 rate limit 소진 위험이 있으므로 금지한다. 발급 실패 시 반복 시도하지 않고 원인(전파·443 방화벽·TLS-ALPN)을 확인하며, 필요하면 staging CA 로 먼저 검증한다. 발급 결과(issuer·notAfter)를 `infra/relay/README.md` 에 기록한다.
  </action>
  <what-built>
외부 고정 IP `gh-radar-relay-ip` 가 예약돼 VM 에 결선됐고, Caddy 가 설치됐지만 **기동돼 있지 않다**(DNS 미확인 상태에서 켜면 Let's Encrypt rate limit 을 소진한다).
  </what-built>
  <how-to-verify>
1. Task 1 이 기록한 외부 고정 IP 값을 사용자에게 전달한다(`infra/relay/README.md` §현재 배포 상태).
2. **사용자 작업:** `jx1.io` DNS 에 `dma` A 레코드를 그 IP 로 추가한다. (Claude 는 사용자의 도메인 등록기관에 접근할 수 없다.)
3. 전파 확인: `dig +short dma.jx1.io` 가 예약 IP 를 반환할 때까지 기다린다. **A 레코드가 확인되기 전에는 Caddy 를 절대 켜지 않는다.**
4. 확인되면 VM 에서 `sudo systemctl enable --now caddy` 후 `journalctl -u caddy -n 50` 으로 인증서 발급 성공을 확인한다.
5. 외부에서 `curl -sI https://dma.jx1.io/healthz` — relay 컨테이너가 아직 없으므로 **502/503 이 정상**이다. 중요한 것은 **TLS 핸드셰이크 성공과 인증서 유효성**이다: `echo | openssl s_client -connect dma.jx1.io:443 -servername dma.jx1.io 2>/dev/null | openssl x509 -noout -issuer -dates`.
6. 발급이 실패하면 **재시도를 반복하지 않는다**(rate limit). 원인을 확인하고(A 레코드 전파 / 443 방화벽 / TLS-ALPN) 필요하면 Let's Encrypt **staging CA** 로 먼저 검증한 뒤 프로덕션으로 전환한다. 최초 발급만 80 을 임시 개방하는 것도 선택지다(발급 후 규칙 삭제).
7. `infra/relay/README.md` 에 발급 결과(issuer, notAfter)를 기록한다.
  </how-to-verify>
  <acceptance_criteria>
    - `dig +short dma.jx1.io` 출력이 `gcloud compute addresses describe gh-radar-relay-ip --region=asia-northeast3 --format='value(address)'` 와 일치
    - `echo | openssl s_client -connect dma.jx1.io:443 -servername dma.jx1.io 2>/dev/null | openssl x509 -noout -issuer` 출력에 `Let's Encrypt` 포함
    - 인증서 `notAfter` 가 현재보다 미래
    - VM 에서 `systemctl is-active caddy` == `active`
    - `gcloud compute firewall-rules list --filter='network=gh-radar-vpc AND allowed.ports=80' --format='value(name)' | wc -l` == 0 (임시 개방했다면 삭제 완료)
    - `grep -c 'notAfter\|인증서 발급' infra/relay/README.md` >= 1
  </acceptance_criteria>
  <verify>
    <manual>사용자 소유 도메인의 DNS 레코드 등록은 Claude 가 수행할 수 없다(외부 좌표, D-06). 등록 후 검증은 자동화한다.</manual>
    <automated>test "$(dig +short dma.jx1.io | tail -1)" = "$(gcloud compute addresses describe gh-radar-relay-ip --region=asia-northeast3 --format='value(address)')" && echo | openssl s_client -connect dma.jx1.io:443 -servername dma.jx1.io 2>/dev/null | openssl x509 -noout -issuer | grep -qi "let's encrypt"</automated>
  </verify>
  <done>`dig` 결과가 예약 IP 와 일치하고 `openssl s_client` 인증서 issuer 가 Let's Encrypt, caddy 가 active, 임시 80 규칙 잔존 0건.</done>
  <resume-signal>A 레코드 등록 완료 후 "DNS 등록 완료" 입력. `dig` 확인 + Caddy 기동 + 인증서 발급까지 마치면 "TLS 발급 완료"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KB VPN 서버 → VM 라우팅 | 서버가 푸시하는 라우트가 VM 의 기본 경로를 가져갈 수 있다 |
| 사용자 DNS(jx1.io) → Let's Encrypt | 외부 좌표. 잘못된 상태에서 반복 시도하면 rate limit 소진 |
| KB 인증 서버 → kbs124 계정 | 반복 실패가 계정 잠금으로 이어진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | DoS (KB 계정 잠금) | VPN 선검증 | mitigate | Task 2: 수동 **≤3회**, 자동 재시도 금지, systemd `StartLimitBurst=5/1h`. acceptance 가 시도 횟수 ≤3 을 문서로 요구 |
| T-15-11 | DoS (관리 경로 상실) | openconnect 라우팅 | mitigate | Task 2 ⑤: 연결 전/후 `ip route` diff + `curl ifconfig.me` + Secret Manager 도달성 확인. 무응답 시 즉시 `systemctl stop`. 직렬 콘솔을 별도 터미널에 미리 열어 둔다 |
| T-15-13 | DoS (ACME rate limit) | Caddy 기동 시점 | mitigate | Task 3: `dig` 로 A 레코드 확인 **후에만** `systemctl enable --now caddy`. 실패 시 반복 금지 + staging CA 우회 경로 명시 |
| T-15-26 | Information Disclosure | VPN 비밀번호 | mitigate | Secret Manager → tmpfs 0600. 선검증 문서·대화·커밋에 값 미기록(grep acceptance) |
| T-15-28 | Elevation of Privilege / 오조작 | 실서버 게이트웨이 | mitigate | Task 2 ⑥: `nc -zv` 도달성만. 로그인·주문 금지(D-27). acceptance 가 문서에 해당 문구를 요구 |
| T-15-12 | Information Disclosure | 임시 80 개방 | mitigate | Task 3 acceptance 가 80 규칙 잔존 0건을 검사 |
</threat_model>

<verification>
- `gcloud compute instances describe radar-gw --zone=asia-northeast3-a --format='value(status)'` == RUNNING
- `gcloud compute firewall-rules list --filter='network=gh-radar-vpc' | wc -l` 이 헤더 제외 3행
- `dig +short dma.jx1.io` == 예약 고정 IP
- `openssl s_client` 인증서 issuer 가 Let's Encrypt
- `15-VPN-PREFLIGHT.md` 7항목 기록 완료 + 비밀 값 0건
</verification>

<success_criteria>
- SC-2 충족: VM·고정 IP·방화벽 3규칙 프로비저닝 + VPN 선검증 결과 기록
- split-tunnel 이 성립해 VPN 연결 중에도 GCP 관리 경로가 살아 있다
- `https://dma.jx1.io` 가 유효한 Let's Encrypt 인증서로 응답한다
- 실서버 게이트웨이는 도달성만 확인되고 로그인·주문은 일어나지 않았다
</success_criteria>

<output>
After completion, create `.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-07-SUMMARY.md`
</output>
