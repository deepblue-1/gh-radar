---
phase: 15-dma-relay-kb-gh-trade-server-10-wss
plan: 11
type: execute
wave: 4
depends_on: []
files_modified:
  - webapp/src/components/ui/tabs.tsx
  - webapp/src/components/stock/stock-detail-tabs.tsx
  - webapp/src/components/stock/stock-detail-client.tsx
  - webapp/src/app/stocks/[code]/page.tsx
  - webapp/e2e/specs/stock-detail-tabs.spec.ts
autonomous: true
requirements: [RELAY-01]
must_haves:
  truths:
    - "SC-7 / D-02a: `/stocks/[code]` 가 히어로(공통) + 상단 4탭 `차트 · 호가주문 · 종목정보 · 뉴스토론` 으로 재구성되고 탭 바가 sticky 다"
    - "SC-7 / D-02a: 탭 전환이 `?tab=chart|orderbook|info|news` 로 URL 에 반영되고(router.push — 뒤로가기 지원), 딥링크 진입이 해당 탭을 활성화하며 알 수 없는 값은 기본 `chart` 로 떨어진다"
    - "SC-7 / D-02: 기존 섹션(Phase 6/7/9/11/12 표면)이 **내용 무변경**으로 탭 안에 재배치되기만 하고, Playwright 회귀 E2E 가 각 탭에서 기존 섹션 렌더를 증명한다"
    - "SC-7 / D-02a: `호가주문` 탭 패널만 넓은 컨테이너(좌우 여백 24px)를 쓰고 나머지 3탭은 현행 `max-w-4xl` 을 유지한다"
  artifacts:
    - path: "webapp/src/components/ui/tabs.tsx"
      provides: "shadcn 공식 tabs (Radix) — ARIA tablist/tabpanel"
      contains: "TabsContent"
    - path: "webapp/src/components/stock/stock-detail-tabs.tsx"
      provides: "4탭 셸 + ?tab= URL 동기화 + sticky 탭 바 + 패널별 컨테이너 폭"
      contains: "orderbook"
    - path: "webapp/e2e/specs/stock-detail-tabs.spec.ts"
      provides: "4탭 진입·딥링크·기존 섹션 회귀 E2E"
      contains: "aria-selected"
  key_links:
    - from: "webapp/src/components/stock/stock-detail-client.tsx"
      to: "webapp/src/components/stock/stock-detail-tabs.tsx"
      via: "기존 섹션을 탭 패널로 재배치"
      pattern: "StockDetailTabs"
    - from: "탭 상태"
      to: "URL ?tab="
      via: "useSearchParams + router.push"
      pattern: "\\?tab="
---

<objective>
종목상세 페이지를 상단 4탭 구조로 재구성한다. shadcn 공식 `tabs` 를 설치하고, `?tab=` 딥링크·sticky 탭 바·패널별 컨테이너 폭을 구현하며, **기존 섹션은 내용을 전혀 바꾸지 않고 탭 안으로 옮기기만** 한 뒤 회귀 E2E 로 증명한다.

Purpose: SC-7 의 페이지 구조 절반. 호가창은 별도 섹션이 아니라 `호가주문` 탭 패널 전체이므로(D-02a), 탭 셸이 먼저 서야 15-13 이 패널을 채울 수 있다. 재배치 과정에서 기존 5개 phase 의 표면을 깨뜨리면 회귀 사고이므로 E2E 가 필수다.
Output: shadcn tabs 1 + 탭 셸 1 + 클라이언트 재배치 1 + 페이지 폭 1 + 회귀 E2E 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-UI-SPEC.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md

<interfaces>
<!-- UI-SPEC §확정 결정 T1~T7 (변경 금지) -->
T1 히어로 + 상단 4탭. 히어로와 갱신시각·새로고침 행은 탭 밖 공통 영역
T2 탭 구성/순서/라벨 고정: `차트` `호가주문` `종목정보` `뉴스토론` (띄어쓰기 없음)
T3 `?tab=` 값 `chart|orderbook|info|news`, 기본 `chart`, **router.push** (뒤로가기 지원)
T4 탭 바 sticky. **탭 라벨에 연결 상태 표식 없음**
T5 shadcn 공식 `tabs`(Radix) 신규 설치
T6 `호가주문` 만 넓은 컨테이너(좌우 여백 24px), 나머지 3탭은 현행 `max-w-4xl`
T7 기존 섹션 **내용 무변경**, 탭 안 재배치만

<!-- 재배치 맵 (UI-SPEC P3) -->
차트     = StockDailyChartSection
호가주문 = (15-13 이 채운다 — 이 plan 은 placeholder)
종목정보 = StockStatsGrid · StockThemeChips · StockLimitUpSection · StockComovementSection
뉴스토론 = StockNewsSection · StockDiscussionSection

<!-- 현행 배치 (stock-detail-client.tsx L126-163) — 이 순서를 탭으로 옮긴다 -->
```tsx
<div className="space-y-8">
  <StockHero stock={stock} />
  <div className="flex items-center justify-between gap-3">{updatedAtLabel}{새로고침 Button}</div>
  <StockDailyChartSection code={stock.code} refreshSignal={isRefreshing} />
  <StockStatsGrid stock={stock} />
  <StockThemeChips stockCode={stock.code} />
  <StockLimitUpSection stockCode={stock.code} />
  <StockComovementSection stockCode={stock.code} />
  <div className="space-y-6"><StockNewsSection .../><StockDiscussionSection .../></div>
</div>
```

<!-- 페이지 컨테이너 (webapp/src/app/stocks/[code]/page.tsx L19-25) -->
```tsx
<AppShell sidebar={<AppSidebar />}>
  <div className="mx-auto w-full max-w-4xl"><StockDetailClient code={code} /></div>
</AppShell>
```

<!-- 보존해야 할 기존 규율 (재배치 중 잃기 쉬움) -->
L49-52  notFound() 를 useEffect 안에서 throw 하지 않고 state 플래그로 승격
L86-93  setStockContext 발행/언마운트 해제 (챗 FAB 라벨 연동)
L108,124 초기 로딩 스켈레톤 2경로
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn tabs 설치 + StockDetailTabs 셸 (URL 동기화·sticky·패널 폭)</name>
  <files>webapp/src/components/ui/tabs.tsx, webapp/src/components/stock/stock-detail-tabs.tsx</files>
  <read_first>
    - webapp/src/components/ui/toggle-group.tsx (analog: radix 배럴 import + `cn` + "use client" 규약 — 설치된 tabs.tsx 가 이 형태여야 한다)
    - webapp/components.json (registries 는 `{}` — 3rd-party 없음, style radix-nova, iconLibrary lucide)
    - webapp/src/styles/globals.css (토큰 — `--fg`/`--muted-fg`/`--s-*`/`--t-*`)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-UI-SPEC.md §Component Inventory P1·P2, §Interaction Contract "탭 전환", §Registry Safety
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-tabs-mockup.html (채택 목업 — 탭 바 시각·모바일 390px 한 줄 배치)
  </read_first>
  <action>
`npx shadcn@latest add tabs` 로 **shadcn 공식 registry** 의 `tabs` 를 설치한다(1st-party 이므로 3rd-party 벳팅 게이트 대상이 아니다 — UI-SPEC §Registry Safety). 설치 후 `webapp/src/components/ui/tabs.tsx` 가 `"use client"` + `radix-ui` 배럴 import + `cn` 형태인지 확인하고, 아니면 `toggle-group.tsx` 규약에 맞춘다. **신규 npm 의존성은 없어야 한다**(이미 설치된 `radix-ui` ^1.4.3 의 하위 export 사용).

`webapp/src/components/stock/stock-detail-tabs.tsx` 신설 — `"use client"`.
- props: `{ code: string; chart: ReactNode; orderbook: ReactNode; info: ReactNode; news: ReactNode }`.
- 탭 값 상수: `const TABS = [{v:"chart", label:"차트"}, {v:"orderbook", label:"호가주문"}, {v:"info", label:"종목정보"}, {v:"news", label:"뉴스토론"}] as const;` — **라벨은 띄어쓰기 없이**(모바일 390px 한 줄).
- URL 동기화: `useSearchParams()` 로 `tab` 을 읽어 초기값 결정, 알 수 없는 값이면 `chart`. 전환 시 `router.push(\`?tab=\${v}\`, { scroll: false })` — **`replace` 가 아니라 `push`**(뒤로가기가 이전 탭으로 돌아가야 한다, T3).
- 탭 바: `sticky top-0 z-*` + 배경(`--bg`) — 스크롤해도 상단 고정. 활성 탭 = `--fg` 텍스트 + 하단 2px `--fg` 인디케이터, 비활성 = `--muted-fg`. **탭 라벨에 상태 점·배지를 붙이지 않는다**(T4 — 연결 상태는 호가주문 탭 안 상태 바가 단독 소유).
- 넘칠 때 가로 스크롤(`overflow-x-auto` + 스크롤바 숨김).
- 패널 폭: `chart`/`info`/`news` 패널은 `mx-auto w-full max-w-4xl`, **`orderbook` 패널만** `w-full px-[var(--s-5)]`(좌우 24px, max-w 해제) — T6.
- 탭 전환 시 **탭 바 바로 아래로 스크롤**(히어로는 지나간 상태). 탭별 스크롤 위치는 보존하지 않는다.
- 키보드는 Radix Tabs 기본 동작(←/→, Home/End) 상속 — override 하지 않는다.
- `useSearchParams` 를 쓰므로 Next 15 에서 **`<Suspense>` 경계**가 필요할 수 있다. 필요하면 상위(`stock-detail-client.tsx`)에서 감싸고 그 이유를 주석으로 남긴다.
- 각 탭 패널에 `data-testid="stock-tab-panel-{v}"` 를 부여해 E2E 가 잡을 수 있게 한다.

파일 상단 docblock 4블록: Phase 15 Plan 11 — RELAY-01 / 무엇 / D-02a·UI-SPEC T1~T7 역참조 / 하지 않는 것(탭 라벨에 상태 표식 금지, 기존 섹션 내용 수정 금지).
  </action>
  <acceptance_criteria>
    - `test -f webapp/src/components/ui/tabs.tsx` exit 0 이고 `grep -c '"use client"' webapp/src/components/ui/tabs.tsx` == 1
    - `git diff --name-only webapp/package.json` 결과 없음 (신규 npm 의존성 0)
    - `grep -c '호가주문' webapp/src/components/stock/stock-detail-tabs.tsx` >= 1 이고 `grep -c '호가 주문' ...` == 0 (띄어쓰기 금지)
    - `grep -c 'router.push' webapp/src/components/stock/stock-detail-tabs.tsx` >= 1 이고 `grep -c 'router.replace' ...` == 0
    - `grep -c 'sticky' webapp/src/components/stock/stock-detail-tabs.tsx` >= 1
    - `grep -c 'max-w-4xl' webapp/src/components/stock/stock-detail-tabs.tsx` >= 1 (3탭 전용)
    - `grep -c 'data-testid="stock-tab-panel-' webapp/src/components/stock/stock-detail-tabs.tsx` >= 4
    - `pnpm --filter webapp run typecheck` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter webapp run typecheck && test "$(git diff --name-only webapp/package.json | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>shadcn tabs 설치(신규 의존성 0) + 4탭 셸이 URL 동기화·sticky·패널 폭 분기를 갖춤.</done>
</task>

<task type="auto">
  <name>Task 2: stock-detail-client 재배치 + page.tsx 컨테이너 폭</name>
  <files>webapp/src/components/stock/stock-detail-client.tsx, webapp/src/app/stocks/[code]/page.tsx</files>
  <read_first>
    - webapp/src/components/stock/stock-detail-client.tsx (수정 대상 전문 — 특히 L49-52 notFound 승격 / L86-93 setStockContext / L108·L124 스켈레톤 2경로 / L126-163 현행 배치)
    - webapp/src/app/stocks/[code]/page.tsx (수정 대상 — `'use client'` + `use(params)` 형태 유지)
    - webapp/src/components/stock/stock-detail-tabs.tsx (Task 1 산출 — props 계약)
    - webapp/src/components/stock/stock-detail-skeleton.tsx (초기 로딩 경로)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-UI-SPEC.md §Component Inventory P3, §확정 결정 T1/T6/T7
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md §F-1
  </read_first>
  <action>
`stock-detail-client.tsx` 를 다음 구조로 바꾼다. **기존 섹션 컴포넌트의 props·내부 구현은 전혀 건드리지 않는다**(T7).

최상위는 `space-y-6` 컨테이너이고 자식은 3개다 — ① `StockHero` ② 갱신시각 라벨 + 새로고침 버튼 행 ③ `StockDetailTabs`. `StockDetailTabs` 에 넘길 4개 패널 prop 은 다음과 같이 매핑한다(UI-SPEC P3):
- `chart` = `StockDailyChartSection`(기존 `code`·`refreshSignal` prop 그대로)
- `orderbook` = 이 plan 의 placeholder(15-13 이 `StockOrderbookSection` 으로 교체)
- `info` = `StockStatsGrid` → `StockThemeChips` → `StockLimitUpSection` → `StockComovementSection` (현행 세로 순서 유지)
- `news` = `StockNewsSection` → `StockDiscussionSection`

- `StockHero` 와 갱신시각·새로고침 행은 **탭 밖 공통 영역**(T1). 히어로는 수정하지 않는다(D1 — 이중 가격 정책상 히어로는 스냅샷 30px 그대로).
- `orderbook` 자리에는 이 plan 에서 **placeholder 컴포넌트**를 넣는다 — `data-testid="stock-orderbook-placeholder"` 를 가진 안내 박스(`실시간 호가는 준비 중이에요`). 15-13 이 이 자리를 `StockOrderbookSection` 으로 교체한다. placeholder 를 **섹션 숨김으로 처리하지 않는다**(UI-SPEC C1 — 호가주문 탭은 항상 렌더된다).
- `info` 탭 내부는 현행 세로 순서(`StockStatsGrid` → `StockThemeChips` → `StockLimitUpSection` → `StockComovementSection`)를 유지하고 간격은 기존 `space-y-8` 상당을 쓴다.
- **보존 필수 3규율**: ① `notFound()` 를 `useEffect` 안에서 throw 하지 않고 state 플래그로 승격하는 처리 ② `setStockContext` 발행/언마운트 해제(챗 FAB 라벨 연동) ③ 초기 로딩 스켈레톤 2경로. 재배치로 이 셋 중 하나라도 사라지면 회귀다 — 각 지점에 "재배치 중 보존" 주석을 남긴다.
- `useSearchParams` 사용으로 Suspense 가 필요하면 `StockDetailTabs` 를 `<Suspense fallback={<StockDetailSkeleton/>}>` 로 감싼다.

`webapp/src/app/stocks/[code]/page.tsx`: 컨테이너에서 `max-w-4xl` 을 **제거**하고 `<div className="mx-auto w-full">` 로 바꾼다. 폭 제한은 이제 탭 패널별로 걸린다(T6). `'use client'` + `React.use(params)` 구조는 그대로 유지한다.
  </action>
  <acceptance_criteria>
    - `grep -c 'StockDetailTabs' webapp/src/components/stock/stock-detail-client.tsx` >= 1
    - `grep -c 'setStockContext' webapp/src/components/stock/stock-detail-client.tsx` >= 2 (발행 + 언마운트 해제 보존)
    - `grep -c 'notFound' webapp/src/components/stock/stock-detail-client.tsx` >= 1
    - `grep -c 'StockDailyChartSection\|StockStatsGrid\|StockThemeChips\|StockLimitUpSection\|StockComovementSection\|StockNewsSection\|StockDiscussionSection' webapp/src/components/stock/stock-detail-client.tsx` >= 7 (기존 7섹션 전부 잔존)
    - `grep -c 'max-w-4xl' webapp/src/app/stocks/\[code\]/page.tsx` == 0
    - `grep -c 'stock-orderbook-placeholder' webapp/src/components/stock/stock-detail-client.tsx` >= 1
    - `git diff --stat webapp/src/components/stock/stock-daily-chart-section.tsx webapp/src/components/stock/stock-comovement-section.tsx webapp/src/components/stock/stock-news-section.tsx` 결과 없음 (기존 섹션 파일 무변경 — T7)
    - `pnpm --filter webapp run typecheck && pnpm --filter webapp run build` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter webapp run typecheck && pnpm --filter webapp test</automated>
  </verify>
  <done>7개 기존 섹션이 탭 안으로 재배치되고 기존 섹션 파일은 무변경. 보존 3규율 유지. 빌드·타입 통과.</done>
</task>

<task type="auto">
  <name>Task 3: 탭 회귀 E2E (stock-detail-tabs.spec.ts)</name>
  <files>webapp/e2e/specs/stock-detail-tabs.spec.ts</files>
  <read_first>
    - webapp/e2e/specs/stock-detail-chart.spec.ts (analog: 섹션 마운트 검증형 — `getByRole('tab')` + `aria-selected` + mock 픽스처 조합)
    - webapp/e2e/fixtures/mock-api.ts, webapp/e2e/fixtures/news.ts, webapp/e2e/fixtures/stocks.ts (라우트 mock 규약 — 먼저 읽고 계약 확인)
    - webapp/e2e/specs/discussions.spec.ts (토론 섹션 렌더 단언 형태)
    - webapp/src/components/stock/stock-detail-tabs.tsx (Task 1 산출 — testid 계약)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-UI-SPEC.md §확정 결정 T2·T3, §Interaction Contract "탭 전환"
  </read_first>
  <action>
`webapp/e2e/specs/stock-detail-tabs.spec.ts` 를 신설한다. baseURL 은 `http://localhost:3100`(dev.sh 규약). 기존 mock 픽스처를 재사용해 API 를 스텁한다.

케이스 8건:
1. `/stocks/005930` 진입 → 히어로가 보이고 **탭 4개**(`차트`/`호가주문`/`종목정보`/`뉴스토론`)가 `role="tab"` 으로 존재.
2. 기본 활성 탭이 `차트`(`aria-selected="true"`)이고 `StockDailyChartSection`(`data-testid="stock-daily-chart-section"`)이 보인다.
3. `종목정보` 탭 클릭 → URL 이 `?tab=info` 로 바뀌고, 통계·테마·상한가·동조 4섹션이 렌더된다(각 섹션의 기존 testid/heading 으로 단언 — **기존 섹션이 살아 있음을 증명하는 회귀 테스트**).
4. `뉴스토론` 탭 → `?tab=news` + 뉴스·토론 섹션 렌더.
5. `호가주문` 탭 → `?tab=orderbook` + 호가주문 패널이 존재(현재는 placeholder testid).
6. **딥링크**: `page.goto('/stocks/005930?tab=info')` → 종목정보 탭이 활성 상태로 진입.
7. **알 수 없는 값**: `?tab=zzz` → 기본 `chart` 활성.
8. **뒤로가기**: 차트 → 종목정보 → `page.goBack()` → 차트 탭으로 복귀(router.push 사용 증명).

추가로 히어로와 새로고침 버튼이 **탭 밖**에 있어 어느 탭에서도 보이는지 1건 단언한다(T1).

기존 `stock-detail-chart.spec.ts` 가 4탭 재구성으로 깨지면 **그 spec 도 함께 갱신**한다(차트 섹션이 이제 탭 안에 있으므로 진입 전 탭 활성화가 필요할 수 있다). 기존 spec 의 검증 의도는 바꾸지 않는다.
  </action>
  <acceptance_criteria>
    - `grep -c "getByRole('tab'" webapp/e2e/specs/stock-detail-tabs.spec.ts` >= 4
    - `grep -c "aria-selected" webapp/e2e/specs/stock-detail-tabs.spec.ts` >= 3
    - `grep -c "goBack" webapp/e2e/specs/stock-detail-tabs.spec.ts` >= 1
    - `grep -c "tab=info\|tab=news\|tab=orderbook" webapp/e2e/specs/stock-detail-tabs.spec.ts` >= 3
    - `pnpm --filter webapp exec playwright test stock-detail-tabs` exit 0, 8 케이스 통과
    - `pnpm --filter webapp exec playwright test stock-detail-chart discussions` exit 0 (기존 spec 회귀 없음)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter webapp exec playwright test stock-detail-tabs stock-detail-chart</automated>
  </verify>
  <done>탭 4종·딥링크·뒤로가기·기존 섹션 회귀가 E2E 8건으로 증명되고 기존 spec 도 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL 쿼리스트링 `?tab=` → 클라이언트 상태 | 사용자 제어 입력이 UI 상태를 결정한다 |
| shadcn registry → 저장소 코드 | 컴포넌트 코드가 외부에서 들어온다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-37 | Tampering (상태 오염) | `?tab=` 파싱 | mitigate | 4개 화이트리스트 밖 값은 전부 기본 `chart` 로 폴백. Task 3 ⑦ 테스트가 단언. 임의 문자열이 렌더 경로에 들어가지 않는다 |
| T-15-SC | Tampering | shadcn `tabs` 설치 | mitigate | **shadcn 공식(1st-party) registry** 만 사용하고 `components.json.registries` 는 `{}` 유지. 신규 npm 의존성 0(acceptance 가 `webapp/package.json` 무변경을 검사) — 공급망 표면 증가 없음 |
| T-15-38 | (안전성) 회귀 | 기존 5개 phase 표면 | mitigate | 기존 섹션 파일 `git diff` 무변경 acceptance + 탭별 렌더 E2E 8건 + 기존 spec 재실행. 재배치가 기능 손실을 만들지 않음을 증명 |
| T-15-39 | (안전성) 컨텍스트 손실 | `setStockContext` / `notFound` 승격 | mitigate | 보존 3규율을 acceptance grep 으로 강제. 챗 FAB 라벨 연동과 404 처리가 재배치로 사라지지 않는다 |
</threat_model>

<verification>
- `pnpm --filter webapp run typecheck && pnpm --filter webapp run build` exit 0
- `pnpm --filter webapp test` exit 0
- `pnpm --filter webapp exec playwright test stock-detail-tabs stock-detail-chart discussions` exit 0
- `git diff --stat` 에 기존 섹션 컴포넌트 파일 0건
- `webapp/package.json` 무변경
</verification>

<success_criteria>
- 4탭이 UI-SPEC T1~T7 확정값 그대로 구현됨
- `?tab=` 딥링크·뒤로가기가 동작
- 기존 7개 섹션이 내용 변경 없이 탭 안에서 렌더됨(E2E 증명)
- `호가주문` 탭이 넓은 컨테이너로 준비되어 15-13 이 채울 수 있음
</success_criteria>

<output>
After completion, create `.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-11-SUMMARY.md`
</output>
