---
phase: 15-dma-relay-kb-gh-trade-server-10-wss
plan: 19
type: execute
wave: 5
depends_on: ["15-18", "15-08"]
files_modified:
  - scripts/deploy-server.sh
  - scripts/smoke-relay.sh
  - scripts/smoke-server.sh
  - infra/relay/README.md
  - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-MOCK-ORDER-EVIDENCE.md
autonomous: false
requirements: [RELAY-02, RELAY-03]
must_haves:
  truths:
    - "SC-6 / D-08: `deploy-server.sh` 가 `RELAY_INTERNAL_URL` env 와 `RELAY_ORDER_SECRET` secret 을 주입하고 Cloud Run 이 Direct VPC Egress 로 VM 내부 포트에 도달한다"
    - "SC-6 / D-40: mock 브로커(로컬 gh-trade `run-mac.sh` 또는 VM 내 스텁)에서 **가격 0 주문이 거부(`R`)되는 경로**가 실측되고 증거가 문서로 남는다"
    - "SC-6 / D-24: 주문 왕복 후 `dma_orders` 에 요청·결과 행이 남고 `GET /api/orders` 로 오늘 주문 목록이 복원된다"
    - "SC-8: relay·server 재배포 후 `smoke-relay.sh` INV 전부와 `smoke-server.sh` 가 통과하고, 내부 포트는 여전히 공인망에서 닫혀 있다"
    - "SC-8 / D-27: 이 검증 전 과정에서 실서버(10.41.1.120)·실계좌에 접속하지 않는다"
  artifacts:
    - path: "scripts/deploy-server.sh"
      provides: "RELAY_INTERNAL_URL env + RELAY_ORDER_SECRET secret 주입"
      contains: "RELAY_INTERNAL_URL"
    - path: ".planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-MOCK-ORDER-EVIDENCE.md"
      provides: "mock 브로커 주문 왕복·가격 0 거부 실측 증거"
      contains: "가격 0"
  key_links:
    - from: "Cloud Run gh-radar-server"
      to: "http://10.10.0.5:8091/internal/orders"
      via: "Direct VPC Egress + X-Relay-Secret"
      pattern: "RELAY_INTERNAL_URL"
    - from: "smoke-relay.sh INV-9"
      to: "POST /api/orders 왕복"
      via: "requireAuth 토큰 + mock 세션"
      pattern: "INV-9"
---

<objective>
주문 경로 전체를 실제로 결선하고 mock 브로커로 왕복을 실측한다. `deploy-server.sh` 에 relay env 를 추가해 재배포하고, relay 를 최신 이미지로 재배포하며, **가격 0 거부 경로**를 증거로 남긴다.

Purpose: SC-6 의 마지막 조각. 코드가 전부 있어도 Cloud Run 이 relay 내부 포트에 도달하지 못하면 주문은 동작하지 않는다 — `--set-env-vars` 는 **전량 치환**이라 한 항목만 빠져도 재배포마다 초기화되는 함정이 있다. mock 브로커의 가격 0 거부는 "거부 경로가 실제로 UI 까지 온다"를 증명하는 유일한 실측이다.
Output: 배포 스크립트 수정 + 재배포 2건 + INV 확장 + mock 주문 증거 문서.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-18-SUMMARY.md

<interfaces>
<!-- deploy-server.sh 수정 지점 (L143-144). 델리미터 `^@^`. --set-env-vars 는 전량 치환 -->
--set-env-vars="^@^NODE_ENV=production@LOG_LEVEL=info@SUPABASE_URL=${SUPABASE_URL}@...@APP_VERSION=${SHA}@DISCUSSION_CLASSIFY_ENABLED=${DISCUSSION_CLASSIFY_ENABLED:-false}"
--update-secrets="SUPABASE_SERVICE_ROLE_KEY=gh-radar-supabase-service-role:latest,...,ANTHROPIC_API_KEY=gh-radar-anthropic-api-key:latest"

<!-- 추가할 값 -->
env    @RELAY_INTERNAL_URL=http://10.10.0.5:8091   (+ ORDER_TIMEOUT_MS=5000 선택)
secret RELAY_ORDER_SECRET=gh-radar-relay-order-secret:latest

<!-- 로컬 mock 브로커 (D-40) -->
cd ../gh-trade/server && ./scripts/build.sh && ./scripts/run-mac.sh   # 포트 9100, [broker] name="mock"
server/scripts/uat/inject_b6.py --send                                 # 가짜 호가 주입 (기본 dry-run)
mock 은 **가격 0 주문에 거부를 돌려준다** → 거부 경로 검증용
9100 을 다른 프로세스가 잡고 있으면 UAT 사본 toml 로 포트 이동
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: deploy-server.sh env 추가 + smoke INV 확장</name>
  <files>scripts/deploy-server.sh, scripts/smoke-relay.sh, scripts/smoke-server.sh</files>
  <read_first>
    - scripts/deploy-server.sh (수정 대상 — L143-144 `--set-env-vars` 전량 치환 함정 + `--update-secrets` append 지점 + VPC 블록 L120-145)
    - scripts/smoke-server.sh (수정 대상 — 기존 INV 목록·러너)
    - scripts/smoke-relay.sh (수정 대상 — 15-08/15-10 이 만든 INV-1~8 + `--check-isin`)
    - server/src/config.ts (주입할 env 키 이름 확인 — `relayInternalUrl`/`relayOrderSecret`/`orderTimeoutMs`)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-PATTERNS.md §G-2
  </read_first>
  <action>
**`scripts/deploy-server.sh`**:
- `--set-env-vars` 문자열에 `@RELAY_INTERNAL_URL=${RELAY_INTERNAL_URL:-http://10.10.0.5:8091}` 와 `@ORDER_TIMEOUT_MS=${ORDER_TIMEOUT_MS:-5000}` 를 append 한다.
- `--update-secrets` 에 `RELAY_ORDER_SECRET=gh-radar-relay-order-secret:latest` 를 append 한다.
- **`--set-env-vars` 는 env 를 전량 치환**하므로 기존 항목이 하나라도 빠지면 재배포마다 초기화된다 — 이 함정을 경고 주석으로 명시하고(`deploy-intraday-sync.sh` L91 동형), 배포 후 `gcloud run services describe` 로 env 개수를 대조하는 확인 단계를 추가한다.
- 배포 후 확인 출력에 `RELAY_INTERNAL_URL` 값과 `RELAY_ORDER_SECRET` 바인딩 여부를 포함한다(**값은 출력하지 않는다**).

**`scripts/smoke-server.sh`**: INV 를 2건 추가한다.
- `POST /api/orders` 가 **미인증에서 401** 을 반환(라우트 결선 확인 — 200 이 아니라 401 이 정상).
- `GET /api/orders` 도 미인증 401.
- Cloud Run 서비스의 env 에 `RELAY_INTERNAL_URL` 이 존재하고 secret `RELAY_ORDER_SECRET` 이 바인딩돼 있는지(`gcloud run services describe` 파싱).

**`scripts/smoke-relay.sh`**: INV 를 2건 추가한다.
- **INV-9** — Cloud Run 에서 VM 내부 포트 도달성. 직접 확인이 어려우므로 **`/api/orders` 를 인증 토큰과 함께 호출했을 때 `503 RELAY_UNAVAILABLE` 이 아닌 응답**(409/403/200 중 하나)이 오는지로 판정한다. 503 이면 env 미주입 또는 방화벽 문제다. 테스트 토큰이 없으면 `SKIP`.
- **INV-10** — `dma_orders` 테이블 접근성(service_role 로 `count` 조회 성공, anon key 로는 실패). anon 접근이 성공하면 **FAIL**(RLS 회귀 감지).
- 기존 **INV-7**(내부 포트 공인 차단)은 그대로 유지한다 — 이번 배포로 열리지 않았는지 재확인이 목적이다.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/deploy-server.sh && bash -n scripts/smoke-server.sh && bash -n scripts/smoke-relay.sh` exit 0
    - `grep -c 'RELAY_INTERNAL_URL' scripts/deploy-server.sh` >= 2 (env 주입 + 배포 후 확인)
    - `grep -c 'gh-radar-relay-order-secret:latest' scripts/deploy-server.sh` == 1
    - `grep -cE 'RELAY_ORDER_SECRET=[A-Za-z0-9+/]{8}' scripts/deploy-server.sh` == 0 (값 하드코딩 0건)
    - `grep -c 'INV-9' scripts/smoke-relay.sh` >= 1 이고 `grep -c 'INV-10' scripts/smoke-relay.sh` >= 1
    - `grep -c 'INV-7' scripts/smoke-relay.sh` >= 1 (기존 유지)
    - `grep -c '/api/orders' scripts/smoke-server.sh` >= 2
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/deploy-server.sh && bash -n scripts/smoke-server.sh && bash -n scripts/smoke-relay.sh && grep -c 'RELAY_INTERNAL_URL' scripts/deploy-server.sh</automated>
  </verify>
  <done>배포 스크립트에 relay env 2종이 추가되고 smoke 에 INV 4건이 확장됨. 비밀 값 하드코딩 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: relay·server 재배포 + 주문 경로 결선 검증</name>
  <files>infra/relay/README.md</files>
  <read_first>
    - scripts/deploy-relay.sh, scripts/deploy-server.sh, scripts/smoke-relay.sh, scripts/smoke-server.sh (Task 1 산출 포함)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-08-SUMMARY.md (현재 배포 상태·이미지 태그)
    - infra/relay/README.md (수정 대상 — 배포 상태 절)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md D-27
  </read_first>
  <action>
relay 와 Cloud Run server 를 최신 코드로 재배포하고 주문 경로 결선을 실측한다. relay 는 **`DMA_HOST` 를 실서버로 지정하지 않는다**(D-27). server 재배포 후 `gcloud run services describe` 로 기존 env(`SUPABASE_URL`·`ANTHROPIC_API_KEY`·`DISCUSSION_CLASSIFY_ENABLED`)가 `--set-env-vars` 전량 치환으로 소실되지 않았는지 대조한다. 인증 토큰으로 `POST /api/orders` 를 호출해 **409 `SESSION_NOT_READY`** 가 오는지 확인한다 — 503 이면 env 미주입 또는 방화벽 문제다. INV-7 을 재확인해 이번 배포로 내부 포트가 열리지 않았는지 검증하고 결과를 `infra/relay/README.md` 에 기록한다.
  </action>
  <what-built>
relay 에 계좌 팬아웃·주문 API 가 추가됐고(15-16), server 에 `/api/orders` 가 생겼으며(15-17), 웹앱에 주문 UI 가 붙었다(15-18). VM 에는 아직 15-08 시점 이미지가 돌고 있고 Cloud Run 에는 relay env 가 없다.
  </what-built>
  <how-to-verify>
1. **relay 재배포** — `GCP_PROJECT_ID=gh-radar SUPABASE_URL=... NOTIFICATION_CHANNEL_ID=... bash scripts/deploy-relay.sh`. **`DMA_HOST` 는 실서버로 지정하지 않는다**(D-27) — VM 안에 mock 스텁을 띄우거나 `127.0.0.1` 로 둔다.
2. **server 재배포** — `bash scripts/deploy-server.sh`. 배포 후 `gcloud run services describe gh-radar-server --region=asia-northeast3 --format=yaml` 로 **기존 env 가 전부 남아 있는지** 확인한다(전량 치환 함정). 특히 `SUPABASE_URL`, `ANTHROPIC_API_KEY` 바인딩, `DISCUSSION_CLASSIFY_ENABLED` 가 사라지지 않았는지 대조한다.
3. `bash scripts/smoke-server.sh` — 기존 INV 전부 + 신규 2건 PASS.
4. `bash scripts/smoke-relay.sh` — INV-1~10 PASS(VPN 미기동 시 INV-4 SKIP, 테스트 토큰 없으면 INV-9 SKIP).
5. **INV-7 재확인** — `nc -z -w3 <VM 공인IP> 8091` 이 여전히 실패해야 한다. 이번 배포로 열렸다면 즉시 방화벽을 고친다.
6. **주문 경로 결선 확인(실주문 아님)** — 로그인 토큰으로 `POST /api/orders` 를 호출한다. 세션(호가창)을 열지 않은 상태이므로 기대 응답은 **409 `SESSION_NOT_READY`** 다. `503 RELAY_UNAVAILABLE` 이 오면 env 미주입 또는 방화벽 문제이므로 원인을 찾아 고친다. 이 단계가 **Cloud Run → Direct VPC Egress → VM 8091 도달성의 증거**다.
7. `infra/relay/README.md` 배포 상태 절에 두 이미지 태그(SHA), 확인한 응답 코드, INV 결과를 기록한다.
  </how-to-verify>
  <acceptance_criteria>
    - `bash scripts/smoke-server.sh` 종료 요약 `FAIL: 0`
    - `bash scripts/smoke-relay.sh` 종료 요약 `FAIL: 0`
    - 인증 토큰 포함 `POST /api/orders` 응답 코드가 **409**(`SESSION_NOT_READY`) — 503 이 아님
    - `nc -z -w3 <VM 공인IP> 8091` exit non-zero (여전히 닫힘)
    - `gcloud run services describe gh-radar-server ... ` 의 env 목록에 `RELAY_INTERNAL_URL` 존재 + 기존 env 항목 수가 배포 전과 동일하거나 증가(감소 0)
    - Cloud Run 리비전이 새 SHA 로 서빙 중
    - `grep -c '현재 배포 상태' infra/relay/README.md` >= 1 이고 두 이미지 SHA 기록 존재
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/smoke-server.sh && bash scripts/smoke-relay.sh</automated>
  </verify>
  <done>smoke-server/smoke-relay 모두 `FAIL: 0`, `/api/orders` 409, 8091 공인 차단 유지, 기존 env 소실 0, 새 SHA 서빙.</done>
  <resume-signal>두 스모크 결과와 `/api/orders` 409 확인 후 "결선 확인" 입력. 503 이면 원인을 기술</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: mock 브로커 주문 왕복 + 가격 0 거부 경로 실측</name>
  <files>.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-MOCK-ORDER-EVIDENCE.md</files>
  <read_first>
    - relay/README.md (15-05 산출 — 로컬 mock 실행 절차)
    - /Users/alex/repos/gh-trade/server/scripts/run-mac.sh, /Users/alex/repos/gh-trade/server/config/server.toml (mock 브로커 설정 — 포트 9100, `[broker] name="mock"`)
    - /Users/alex/repos/gh-trade/server/scripts/uat/inject_b6.py (가짜 호가 주입 — 기본 dry-run, `--send` 필요)
    - dev.sh (로컬 포트 — webapp 3100 / server 8080 / relay 8090·8091)
    - webapp/src/components/orderbook/order-panel.tsx (결과 배너 문구 — 화면 증거 대조용)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md D-27, D-40
  </read_first>
  <action>
로컬에서 gh-trade mock 서버(`run-mac.sh`, `[broker] name="mock"`)와 `inject_b6.py --send` 가짜 호가로 주문 왕복 4케이스를 실측한다 — ① 정상 접수 ② **가격 0 거부**(어느 층에서 거부되는지: server zod / relay 조립 / mock 브로커) ③ 미체결 취소 왕복 ④ 새로고침 후 `GET /api/orders` 목록 복원. 전 과정을 로컬에서만 수행하고 실서버(10.41.1.120)에 접속하지 않는다(D-27/D-40). 결과와 `dma_orders` status 전이, 화면 배너 문구, relay 로그의 비밀 미노출을 `15-MOCK-ORDER-EVIDENCE.md` 에 기록한다. 비밀 값·실계좌번호는 기록하지 않는다.
  </action>
  <what-built>
로컬에서 gh-trade mock 서버(`[broker] name="mock"`)를 띄우면 실계좌 없이 주문 왕복을 검증할 수 있다. **mock 은 가격 0 주문에 거부를 돌려준다** — 이것이 거부 경로가 relay → server → UI 까지 온전히 전달되는지 증명하는 유일한 실측이다.
  </what-built>
  <how-to-verify>
**전 과정을 로컬에서만 수행한다. 실서버(10.41.1.120)·실계좌에 접속하지 않는다(D-27).**

1. mock 서버 기동 — `cd ../gh-trade/server && ./scripts/build.sh && ./scripts/run-mac.sh`(포트 9100). 다른 프로세스가 9100 을 잡고 있으면 UAT 사본 toml 로 포트를 옮긴다.
2. 가짜 호가 주입 — `python3 server/scripts/uat/inject_b6.py --send`(기본이 dry-run 이므로 `--send` 필수).
3. 로컬 relay 기동 — `DMA_HOST=127.0.0.1 DMA_PORT=9100 pnpm --filter @gh-radar/relay dev`. 로컬 `dma_credentials` 에 테스트 계정 매핑을 `scripts/dma-credentials.ts` 로 등록한다(mock 은 무인증이므로 임의 값 가능).
4. `./dev.sh` 로 webapp(3100) + server(8080) 기동, `NEXT_PUBLIC_RELAY_WS_URL=ws://localhost:8090/ws`.
5. 브라우저에서 `/stocks/005930?tab=orderbook` 진입 → 상태 배지 `실시간` + 호가 렌더 확인.
6. **정상 주문 1건** — 유효 가격·수량으로 매수 주문 → 확인 다이얼로그 → 접수. 기대: `주문이 접수됐어요 · 주문번호 …` 배너 + 미체결 목록에 행 추가 + `dma_orders` 에 `status='accepted'` 행.
7. **가격 0 거부 1건(핵심)** — API 를 직접 호출한다(UI 는 가격>0 을 강제하므로): `curl -X POST http://localhost:8080/api/orders -H "Authorization: Bearer <token>" -d '{"code":"005930","accountNo":"...","exchange":"KRX","side":"B","orderType":"N","qty":1,"price":0}'`. 기대: **server zod 가 400 으로 거부**(price>0 형식 검사). 그 다음 **relay 내부 API 를 직접 호출**해 mock 브로커까지 보낸다: `curl -X POST http://localhost:8091/internal/orders -H "X-Relay-Secret: <값>" -d '{... "price":0 ...}'` → relay 조립 단계에서 거부되거나 mock 이 `OrderResp("R")` 을 돌려주는지 확인한다. **어느 층에서 거부됐는지**를 기록하는 것이 핵심이다(다층 방어의 실측).
8. **취소 1건** — 미체결 행에서 취소 → 취소 확인 다이얼로그 → mock 의 취소확인 통보가 wss 로 와서 미체결 목록에서 사라지는지 확인.
9. **주문 목록 복원** — 페이지를 새로고침하고 `GET /api/orders` 결과가 오늘 주문을 담고 있는지 확인(D-24).
10. `.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-MOCK-ORDER-EVIDENCE.md` 에 기록한다 — ① 정상 접수 요청/응답(계좌번호 마스킹) ② **가격 0 거부: 거부된 층(server zod / relay 조립 / mock 브로커)과 응답 본문** ③ 취소 왕복 ④ `dma_orders` 행 상태 전이 ⑤ 화면 배너 문구 ⑥ relay 로그에 비밀번호·토큰이 없음(`grep` 결과 0건). **비밀 값·실계좌번호는 기록하지 않는다.**
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-MOCK-ORDER-EVIDENCE.md` 존재
    - 문서에 `가격 0` 케이스의 **거부 층**과 응답 본문이 기록돼 있음
    - 문서에 정상 접수 → `dma_orders.status` 전이(`requested` → `accepted`)가 기록돼 있음
    - 문서에 취소 왕복(`orderType:"C"` → 취소확인 통보 → 미체결 목록 제거)이 기록돼 있음
    - 문서에 `10.41.1.120` 문자열 0건 (로컬 mock 만 사용 — D-27)
    - `grep -riE 'kbs124|passwd=|password:' .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-MOCK-ORDER-EVIDENCE.md | wc -l` == 0
    - relay 로컬 로그에서 `grep -ci 'password'` 결과가 redact 표기(`[REDACTED]`)만 (평문 0건)
  </acceptance_criteria>
  <verify>
    <manual>로컬 mock 브로커 기동과 브라우저 주문 왕복은 대화형 절차다(D-40). 사후 산출물 검증만 자동화한다.</manual>
    <automated>test -f .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-MOCK-ORDER-EVIDENCE.md && test "$(grep -c '10.41.1.120' .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-MOCK-ORDER-EVIDENCE.md)" = "0" && grep -q '가격 0' .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-MOCK-ORDER-EVIDENCE.md</automated>
  </verify>
  <done>증거 문서에 4케이스 기록, 가격 0 거부 층 명시, 실서버 문자열 0건, 로그 평문 비밀 0건.</done>
  <resume-signal>mock 왕복 4케이스(접수·가격 0 거부·취소·목록 복원) 기록 후 "mock 검증 완료" 입력</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud Run → VM 8091 | 배포 설정이 잘못되면 주문이 아예 안 나가거나(503) 방화벽이 열린다 |
| 배포 스크립트 → Cloud Run env | `--set-env-vars` 전량 치환이 기존 설정을 날릴 수 있다 |
| mock 브로커 vs 실서버 | 검증 대상을 혼동하면 실계좌 사고 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | Information Disclosure | 내부 포트 | mitigate | Task 2 ⑤ 가 배포 후 INV-7 을 **재확인** — 이번 배포로 8091 이 공인망에 열리지 않았는지 능동 검증 |
| T-15-55 | DoS (설정 소실) | `--set-env-vars` 전량 치환 | mitigate | Task 1 이 경고 주석 + 배포 후 env 개수 대조 단계 추가. Task 2 ② 가 기존 env(SUPABASE_URL·ANTHROPIC·DISCUSSION_CLASSIFY_ENABLED) 잔존을 확인 |
| T-15-06 | Spoofing | 공유 비밀 배포 | mitigate | 비밀은 `--update-secrets` 로만 주입(값 하드코딩 0건 grep). Cloud Run 이 Secret Manager 에서 읽는다 |
| T-15-28 | (안전성) 실서버 오접속 | mock 검증 | mitigate | Task 3 acceptance 가 증거 문서에 `10.41.1.120` 0건을 요구. relay 배포 시 `DMA_HOST` 실서버 미지정(Task 2 ①) |
| T-15-14 | (안전성) 거부 경로 미검증 | 가격 0 | mitigate | Task 3 ⑦ 이 **어느 층에서 거부됐는지**를 기록 — 다층 방어(server zod → relay 조립 → mock 브로커) 중 실제로 동작한 층을 실측 |
| T-15-01 | Elevation of Privilege | `dma_orders` RLS | mitigate | INV-10 이 anon key 접근 실패를 능동 검증(RLS 회귀 감지) |
| T-15-04 | Information Disclosure | relay 로그 | mitigate | Task 3 ⑥ 이 로그의 평문 비밀번호 0건을 확인(redact 동작 실증) |
</threat_model>

<verification>
- `bash scripts/smoke-server.sh` → `FAIL: 0`
- `bash scripts/smoke-relay.sh` → `FAIL: 0` (INV-1~10)
- 인증 `POST /api/orders` → 409 `SESSION_NOT_READY` (503 아님)
- `nc -z -w3 <공인IP> 8091` 실패
- `15-MOCK-ORDER-EVIDENCE.md` 에 4케이스 기록 + 실서버 문자열 0건
</verification>

<success_criteria>
- Cloud Run → Direct VPC Egress → VM 내부 포트 경로가 실측으로 확인됨
- 기존 server env 가 재배포로 소실되지 않음
- mock 브로커에서 접수·거부·취소·목록 복원 4케이스가 왕복으로 확인됨
- 가격 0 거부가 어느 층에서 일어나는지 문서화됨
- 내부 포트가 여전히 공인망에서 닫혀 있음
</success_criteria>

<output>
After completion, create `.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-19-SUMMARY.md`
</output>
