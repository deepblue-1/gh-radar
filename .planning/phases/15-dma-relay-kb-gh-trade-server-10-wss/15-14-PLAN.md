---
phase: 15-dma-relay-kb-gh-trade-server-10-wss
plan: 14
type: execute
wave: 4
depends_on: ["15-13", "15-05"]
files_modified:
  - webapp/src/components/stock/__tests__/orderbook.test.tsx
  - webapp/e2e/fixtures/relay.ts
  - webapp/e2e/specs/orderbook.spec.ts
  - webapp/playwright.config.ts
  - webapp/README.md
autonomous: false
requirements: [RELAY-01]
must_haves:
  truths:
    - "SC-7: 호가창 컴포넌트 테스트가 10단 렌더·거래소 토글·상태 배지·권한 없음 게이트·빈 상태를 증명한다"
    - "SC-7 / D-40: Playwright E2E 가 **로컬 relay + 스텁 게이트웨이** 위에서 브라우저 wss 왕복(인증 → 구독 → 호가 렌더)을 증명한다 — 실서버에 붙지 않는다"
    - "SC-7 / D-41: Vercel 프로덕션에 `NEXT_PUBLIC_RELAY_WS_URL=wss://dma.jx1.io/ws` 가 설정되고 trailing newline 이 없음이 검증된다"
    - "SC-7: 비로그인·권한 없음 사용자가 호가주문 탭에서 게이트 카드를 보고 기존 탭은 그대로 쓸 수 있다"
  artifacts:
    - path: "webapp/e2e/fixtures/relay.ts"
      provides: "E2E 픽스처 — 로컬 relay + 스텁 게이트웨이 기동/종료, wss URL 주입"
      exports: ["withLocalRelay"]
    - path: "webapp/e2e/specs/orderbook.spec.ts"
      provides: "wss 왕복 + 게이트 분기 E2E"
      contains: "orderbook"
    - path: "webapp/src/components/stock/__tests__/orderbook.test.tsx"
      provides: "호가창 컴포넌트 단위 테스트"
      contains: "권한 없어요"
  key_links:
    - from: "webapp/e2e/fixtures/relay.ts"
      to: "relay/tests/helpers/fake-gateway.ts"
      via: "스텁 게이트웨이 재사용"
      pattern: "fake-gateway"
    - from: "Vercel env NEXT_PUBLIC_RELAY_WS_URL"
      to: "webapp/src/lib/relay-url.ts"
      via: "빌드 타임 주입 + .trim()"
      pattern: "NEXT_PUBLIC_RELAY_WS_URL"
---

<objective>
호가창 표면을 자동 검증으로 잠근다. 컴포넌트 단위 테스트와, **로컬 relay + 스텁 게이트웨이**를 띄워 실제 브라우저에서 wss 왕복을 증명하는 Playwright E2E 를 만들고, Vercel 프로덕션 env 를 설정한다.

Purpose: SC-7 의 검증 절반. RESEARCH §Validation Architecture 가 지정한 `webapp test orderbook`(component)과 `playwright test orderbook.spec`(e2e) 두 항목이 여기서 채워진다. E2E 는 **실서버에 붙지 않는다**(D-27/D-40) — 픽스처가 로컬 스텁을 띄운다.
Output: component test 1 + E2E 픽스처 1 + E2E spec 1 + Vercel env 설정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-CONTEXT.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-UI-SPEC.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-VALIDATION.md
@.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-13-SUMMARY.md

<interfaces>
<!-- 테스트 경로 규칙 (플랜이 자주 틀리는 곳) -->
webapp 단위/컴포넌트 테스트 = `src/**/__tests__/` **co-located**. `webapp/tests/` 는 vitest include 에 안 걸려 조용히 실행되지 않는다.
E2E baseURL = `http://localhost:3100` (dev.sh 규약)

<!-- relay 로컬 포트 (D-41) -->
ws :8090 · 내부 HTTP :8091 · `NEXT_PUBLIC_RELAY_WS_URL` 로컬값 `ws://localhost:8090/ws`

<!-- 재사용할 스텁 (15-02 산출) -->
relay/tests/helpers/fake-gateway.ts — startFakeGateway / respondLogin / pushQuote / pushTape / sendGarbage / hardClose

<!-- Vercel 배포 규약 (자동 메모리) -->
repo root 에서 `vercel pull → vercel build → vercel deploy --prebuilt`
`vercel env pull` 후 `tail -c1 | xxd -p` 가 `0a` 인지 확인 — paste 끝 enter 가 client bundle 을 깨뜨린 전례
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 호가창 컴포넌트 테스트</name>
  <files>webapp/src/components/stock/__tests__/orderbook.test.tsx</files>
  <read_first>
    - webapp/src/components/stock/__tests__/stock-comovement-section.test.tsx (analog: 모듈 mock + render + co-located 경로 규약)
    - webapp/src/components/stock/stock-orderbook-section.tsx, webapp/src/components/orderbook/orderbook-ladder.tsx, trade-tape.tsx (15-13 산출)
    - webapp/src/lib/use-relay-socket.ts (mock 대상 — 훅 반환 형태)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-UI-SPEC.md §Component Inventory C1·C4·C5·C6·C13, §Copywriting
  </read_first>
  <action>
`webapp/src/components/stock/__tests__/orderbook.test.tsx` — **co-located 경로 필수**. `vi.mock('@/lib/use-relay-socket')` 로 훅을 스텁해 상태를 직접 주입한다(네트워크 없음).

케이스 12건:
1. `ready` + 10단 호가 → 매도 10행 + 매수 10행 = 20행 렌더, 가격이 `.mono` 로 표시.
2. 잔량 바 폭이 **단계 최대값 정규화**(L3=A) — 최대 잔량 행이 100%, 절반 잔량 행이 ~50%.
3. 가격 셀 클릭 → `onPriceClick` 이 그 가격으로 호출되고 **매매 구분은 변하지 않음**.
4. roving tabindex — 사다리 루트가 tab stop 1개이고 `↓` 키로 행 포커스가 이동.
5. 거래소 토글 KRX→NXT → 훅에 새 `exchange` 가 전달됨.
6. NXT 호가 없음 → `이 종목은 NXT 호가가 없어요`.
7. `status: "unauthorized"` → **게이트 카드**(`실시간 호가·주문 권한이 없어요`)가 보이고 사다리·주문 진입점이 렌더되지 않으며 **섹션은 사라지지 않는다**.
8. `isin === null` → 동일 게이트.
9. `quote === null` → `호가 정보가 없어요`.
10. `tape` 빈 배열 → `아직 체결이 없어요`.
11. `isStale: true` → 사다리·테이프가 마지막 값을 유지(행 수 > 0)하고 감쇠 스타일이 적용됨.
12. 체결 행에 `▲ 매수` / `▼ 매도` 라벨이 텍스트로 존재(색 비의존).

추가로 상태 배지 4종(`connecting`/`ready`/`reconnecting`/`failed`)의 문구가 UI-SPEC verbatim 인지 단언한다.
  </action>
  <acceptance_criteria>
    - 파일 경로가 `webapp/src/components/stock/__tests__/orderbook.test.tsx`
    - `pnpm --filter webapp test orderbook` exit 0, 테스트 12건 이상 통과
    - `grep -c '실시간 호가·주문 권한이 없어요' webapp/src/components/stock/__tests__/orderbook.test.tsx` >= 1
    - `grep -c '▲ 매수' webapp/src/components/stock/__tests__/orderbook.test.tsx` >= 1
    - `grep -c 'isStale' webapp/src/components/stock/__tests__/orderbook.test.tsx` >= 1
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter webapp test orderbook</automated>
  </verify>
  <done>호가창 12+ 컴포넌트 테스트 green (10단·정규화 바·클릭·게이트·빈 상태·stale·색 비의존).</done>
</task>

<task type="auto">
  <name>Task 2: E2E 픽스처(로컬 relay + 스텁 게이트웨이) + orderbook.spec.ts</name>
  <files>webapp/e2e/fixtures/relay.ts, webapp/e2e/specs/orderbook.spec.ts, webapp/playwright.config.ts</files>
  <read_first>
    - webapp/e2e/fixtures/mock-api.ts, webapp/e2e/fixtures/chat.ts, webapp/e2e/fixtures/auth.ts (route-mock 규약 — **먼저 읽고 계약 확인**. PATTERNS 가 chat.ts 미열람을 명시)
    - webapp/e2e/specs/chat.spec.ts (analog: 로그인/비로그인 게이트 분기형 — `test.use({ storageState: ... })` + `context.clearCookies()`)
    - webapp/e2e/specs/stock-detail-tabs.spec.ts (15-11 산출 — 탭 진입 방법)
    - webapp/playwright.config.ts (webServer 설정 — relay 기동을 여기에 추가할지 픽스처에서 spawn 할지 결정)
    - relay/tests/helpers/fake-gateway.ts (재사용할 스텁 게이트웨이)
    - relay/src/index.ts, relay/src/config.ts (로컬 기동 env)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-VALIDATION.md §Wave 0 Requirements
  </read_first>
  <action>
`webapp/e2e/fixtures/relay.ts` — E2E 가 실제 wss 왕복을 하려면 relay 프로세스와 게이트웨이가 필요하다. **실서버에 붙지 않는다**(D-27).

제공할 것:
- `withLocalRelay(): Promise<{ wsUrl: string; gateway: FakeGatewayHandle; stop(): Promise<void> }>` — ① `relay/tests/helpers/fake-gateway.ts` 의 스텁 게이트웨이를 임의 포트로 띄우고 ② `tsx relay/src/index.ts` 를 `DMA_HOST=127.0.0.1 DMA_PORT=<스텁포트> WS_PORT=<임의> ORDER_API_PORT=<임의>` 로 spawn 한다. Supabase 는 실제 dev 프로젝트를 쓰거나(E2E 계정 존재 시) `SUPABASE_URL` 을 로컬 mock 으로 돌린다 — 저장소의 기존 E2E 인증 방식(`webapp/e2e/auth.setup.ts` + `seed-test-user.ts`)을 먼저 확인하고 그것에 맞춘다.
- `seedDmaCredential(userId)` — E2E 테스트 계정에 대해 `dma_credentials` 행을 넣고(AES 암호화는 `relay/src/store/credentials.ts` 의 `encryptDmaPassword` 재사용) 테스트 종료 시 제거한다. **allowlist 있음/없음 두 경로를 모두 재현하기 위함**이다.
- `pushQuoteFixture(gateway, { isin, exchange })` — 10단 호가 + 체결 3건 주입 헬퍼.
- `stop()` 이 프로세스·소켓을 확실히 정리해 Playwright 가 hang 하지 않게 한다.

`webapp/playwright.config.ts` 는 기존 `webServer` 를 유지하고, relay 기동은 **spec 단위 픽스처**로 처리한다(전역 webServer 에 넣으면 다른 spec 이 불필요하게 relay 를 기다린다). 어떤 방식을 택했는지 주석으로 남긴다. 웹앱 프로세스에는 `NEXT_PUBLIC_RELAY_WS_URL` 을 픽스처가 정한 포트로 주입해야 하므로, 필요하면 이 spec 전용으로 `webServer.env` 를 지정하거나 spec 내에서 `page.addInitScript` 로 오버라이드 훅을 제공한다(구현 선택을 주석에 남긴다).

`webapp/e2e/specs/orderbook.spec.ts` 케이스 6건:
1. **wss 왕복(핵심)** — 로그인 + `dma_credentials` 시드 상태로 `/stocks/005930?tab=orderbook` 진입 → 상태 배지가 `실시간` 이 되고 → 스텁이 푸시한 호가 10단이 렌더된다(특정 가격/잔량 문자열 단언).
2. **체결 테이프** — 스텁이 체결 3건 푸시 → 테이프 상단에 3행이 최신순으로 쌓인다.
3. **거래소 토글** — NXT 로 전환 → `unsub`/`sub` 왕복 후 NXT 호가가 렌더되거나 `이 종목은 NXT 호가가 없어요` 표시.
4. **권한 없음 게이트** — `dma_credentials` 행이 **없는** 계정으로 진입 → `실시간 호가·주문 권한이 없어요` 카드가 보이고 사다리는 없다. 다른 탭(`?tab=chart`)은 정상 동작.
5. **비로그인** — `test.use({ storageState: { cookies: [], origins: [] } })` + `context.clearCookies()` → 호가주문 탭이 로그인 안내를 보이고 wss 연결을 시도하지 않는다.
6. **재접속 stale** — 스텁이 `hardClose` → 상태 배지가 `재접속 중` 으로 바뀌고 **사다리가 비워지지 않는다**(행 수 유지).

각 케이스는 `withLocalRelay()` 를 `beforeAll`/`afterAll` 로 감싸 프로세스를 공유하고, 테스트 간 상태 오염을 막기 위해 매 테스트에서 게이트웨이 상태를 리셋한다.
  </action>
  <acceptance_criteria>
    - `grep -c 'export async function withLocalRelay' webapp/e2e/fixtures/relay.ts` == 1
    - `grep -c 'seedDmaCredential' webapp/e2e/fixtures/relay.ts` >= 1
    - `grep -c '10.41.1.120' webapp/e2e/fixtures/relay.ts webapp/e2e/specs/orderbook.spec.ts` == 0 (실서버 미접속 — D-27)
    - `grep -c '실시간 호가·주문 권한이 없어요' webapp/e2e/specs/orderbook.spec.ts` >= 1
    - `grep -c 'storageState' webapp/e2e/specs/orderbook.spec.ts` >= 1 (비로그인 케이스)
    - `pnpm --filter webapp exec playwright test orderbook` exit 0, 6 케이스 통과
    - `pnpm --filter webapp exec playwright test` 전체가 hang 없이 종료(픽스처 정리 확인)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter webapp exec playwright test orderbook</automated>
  </verify>
  <done>로컬 relay + 스텁 게이트웨이 위에서 브라우저 wss 왕복 6케이스 green. 실서버 미접속.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Vercel `NEXT_PUBLIC_RELAY_WS_URL` 설정 + 프로덕션 확인</name>
  <files>webapp/README.md</files>
  <read_first>
    - webapp/src/lib/relay-url.ts (15-12 산출 — `.trim()` + 폴백)
    - scripts/vercel-ignore-build.sh (Vercel 배포 skip 함정 — 자동 메모리 `reference_vercel_frontend_deploy`)
    - webapp/README.md (수정 대상 — env 표)
    - .planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-RESEARCH.md §Pitfall 14
  </read_first>
  <action>
Vercel 프로젝트의 Production/Preview env 에 `NEXT_PUBLIC_RELAY_WS_URL=wss://dma.jx1.io/ws` 를 추가하고 `vercel env pull` 로 **trailing newline 이 없는지** 마지막 바이트를 검증한다(paste 끝 enter 가 클라이언트 번들을 깨뜨린 전례). repo root 에서 `vercel pull → vercel build → vercel deploy --prebuilt` 로 수동 배포한다(`scripts/vercel-ignore-build.sh` 가 자동 배포를 skip 할 수 있다). 프로덕션에서 wss 요청 URL 이 `wss://dma.jx1.io/ws` 이고 쿼리스트링에 토큰이 없는지 확인하고, `webapp/README.md` env 표에 항목을 추가한다.
  </action>
  <what-built>
`resolveRelayWsUrl()` 이 `NEXT_PUBLIC_RELAY_WS_URL` 을 `.trim()` 해서 읽고 미설정 시 로컬로 폴백한다. Vercel 프로덕션에는 아직 이 값이 없다.
  </what-built>
  <how-to-verify>
1. Vercel 프로젝트 env 에 `NEXT_PUBLIC_RELAY_WS_URL=wss://dma.jx1.io/ws` 를 Production/Preview 양쪽에 추가한다. **paste 끝의 enter 로 trailing newline 이 들어가면 클라이언트 번들이 깨진다**(자동 메모리 사고 선례).
2. **검증:** `vercel env pull .env.vercel.check` 후 해당 줄의 마지막 바이트를 확인한다 — `grep '^NEXT_PUBLIC_RELAY_WS_URL' .env.vercel.check | tr -d '\n' | tail -c1 | xxd -p` 가 `0a` 가 **아니어야** 한다. 값 길이와 마지막 문자(`s`)도 대조한다. 확인 후 `.env.vercel.check` 삭제.
3. repo root 에서 `vercel pull → vercel build → vercel deploy --prebuilt` 로 프론트를 배포한다. **`scripts/vercel-ignore-build.sh` 가 docs-only push 를 skip 하므로 자동 배포를 기다리지 말고 수동 배포한다**(13일 stuck 전례).
4. 프로덕션에서 `/stocks/005930?tab=orderbook` 진입 → 상태 배지가 나타나는지 확인한다. relay 가 mock 을 향하고 있으므로(15-08) `재접속 중`/`회선 단절` 이 정상이며, **번들이 깨지지 않고 게이트/상태 바가 렌더되는 것**이 이 단계의 확인 대상이다.
5. 브라우저 devtools Network 탭에서 wss 요청 URL 이 `wss://dma.jx1.io/ws` 이고 **쿼리스트링에 토큰이 없는지** 확인한다.
6. `webapp/README.md` env 표에 `NEXT_PUBLIC_RELAY_WS_URL` 행을 추가한다(로컬값 `ws://localhost:8090/ws`, 프로덕션 `wss://dma.jx1.io/ws`, trailing newline 주의 문구 포함).
  </how-to-verify>
  <acceptance_criteria>
    - `vercel env pull` 결과에서 `NEXT_PUBLIC_RELAY_WS_URL` 값의 마지막 문자가 `s` 이고 trailing newline 없음
    - 프로덕션 페이지의 wss 요청 URL 이 `wss://dma.jx1.io/ws` 이고 쿼리스트링에 `token` 파라미터 없음
    - 프로덕션 `/stocks/005930?tab=orderbook` 에서 상태 바가 렌더되고 JS 콘솔 에러 0건
    - `grep -c 'NEXT_PUBLIC_RELAY_WS_URL' webapp/README.md` >= 1
    - 배포 후 기존 페이지(`/`, `/scanner`, `/stocks/005930?tab=chart`) 200 (회귀 없음)
  </acceptance_criteria>
  <verify>
    <manual>Vercel 대시보드 env 등록과 프로덕션 브라우저 확인은 외부 좌표다. 값 무결성 검증은 `vercel env pull` 로 자동화한다.</manual>
    <automated>pnpm --filter webapp exec playwright test orderbook stock-detail-tabs</automated>
  </verify>
  <done>env 값에 trailing newline 없음, 프로덕션 wss URL 정확, 콘솔 에러 0, 기존 페이지 회귀 없음, README 기록.</done>
  <resume-signal>Vercel env 등록 + newline 검증 + 프로덕션 확인 후 "env 설정 완료" 입력</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E 픽스처 → DMA 게이트웨이 | 테스트가 실서버에 붙으면 실계좌 사고로 이어질 수 있다 |
| Vercel 빌드 env → 클라이언트 번들 | 값이 잘못되면 전 사용자 번들이 깨진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-28 | (안전성) 실서버 오접속 | E2E 픽스처 | mitigate | 픽스처가 `DMA_HOST=127.0.0.1` + 스텁 게이트웨이만 사용. acceptance 가 spec/픽스처에 `10.41.1.120` 문자열 0건을 검사(D-27/D-40) |
| T-15-42 | (안전성) 오설정 | Vercel env | mitigate | `vercel env pull` 후 마지막 바이트 검증(자동 메모리 사고 선례) + 코드의 `.trim()` 이중 방어 |
| T-15-04 | Information Disclosure | wss URL | mitigate | 프로덕션 Network 탭에서 쿼리스트링에 토큰이 없음을 확인. 토큰은 첫 메시지 본문 전용 |
| T-15-21 | Elevation of Privilege | allowlist 게이트 | mitigate | E2E 4·5 가 **매핑 없음**과 **비로그인** 두 경로를 실제 브라우저에서 검증. UI 게이트가 서버 판정과 어긋나지 않음을 증명 |
| T-15-46 | (안전성) 테스트 자원 누수 | 로컬 relay 프로세스 | mitigate | `stop()` 이 프로세스·소켓 정리. acceptance 가 전체 Playwright 실행이 hang 없이 종료함을 요구 |
</threat_model>

<verification>
- `pnpm --filter webapp test orderbook` exit 0 (12+)
- `pnpm --filter webapp exec playwright test orderbook stock-detail-tabs` exit 0
- `pnpm --filter webapp exec playwright test` 전체 hang 없이 종료
- `grep -c '10.41.1.120' webapp/e2e/` == 0
- Vercel env 값에 trailing newline 없음
</verification>

<success_criteria>
- 호가창이 컴포넌트·E2E 두 층에서 자동 검증된다
- 브라우저 wss 왕복(인증 → 구독 → 호가 렌더)이 로컬 스텁 위에서 증명된다
- 권한 없음·비로그인 게이트가 실제 브라우저에서 확인된다
- 프로덕션 웹앱이 `wss://dma.jx1.io/ws` 를 바라보고 번들이 깨지지 않는다
</success_criteria>

<output>
After completion, create `.planning/phases/15-dma-relay-kb-gh-trade-server-10-wss/15-14-SUMMARY.md`
</output>
