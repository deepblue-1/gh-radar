---
phase: 16-trading-limit-chaser-vi-my-page
plan: 12
type: execute
wave: 11
depends_on: ["16-11"]
files_modified:
  - webapp/src/lib/limit-chaser.ts
  - webapp/src/lib/__tests__/limit-chaser.test.ts
  - webapp/src/components/trading/limit-chaser-form.tsx
  - webapp/src/components/trading/dirty-action-bar.tsx
  - webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx
autonomous: true
requirements: [TRADE-01]

must_haves:
  truths:
    - "매수·매도·한방체결 스위치는 확인 다이얼로그 없이 즉시 전송된다 (D-05)"
    - "값 변경은 즉시 전송하지 않고 더티 표시 + 하단 「수정」 액션 바로만 반영된다 (D-06)"
    - "스위치 전송에는 그 시점의 폼 값(더티 포함)이 함께 나가고 액션 바가 그 사실을 상시 고지한다 (D-06)"
    - "매수·매도·취소 게이트가 전부 OFF 가 되면 삭제(crud \"D\")이며 별도 삭제 버튼이 없다 (D-08 / Pitfall 7)"
    - "서버 에코가 도착하면 더티 필드도 서버값으로 덮어쓰고 액션 바가 사라지며 배너가 뜬다 (D-11)"
    - "전송하는 필드는 클라 입력 30 + 고정 3 이며 S→C 전용 4필드는 보내지 않는다"
  artifacts:
    - path: "webapp/src/lib/limit-chaser.ts"
      provides: "수량 산출·전략 키·더티 판정·기본값 시딩 순수 함수"
      contains: "buyOrderQtyFromAmount"
    - path: "webapp/src/components/trading/limit-chaser-form.tsx"
      provides: "매수/매도 폼 카드 + 그룹 6개 + 스위치 즉시 전송"
      contains: "data-slot=\"limit-chaser-form\""
    - path: "webapp/src/components/trading/dirty-action-bar.tsx"
      provides: "「수정」/「되돌리기」 하단 액션 바 (상따·VI 공용)"
      contains: "수정"
  key_links:
    - from: "webapp/src/components/trading/limit-chaser-form.tsx"
      to: "webapp/src/lib/relay-provider.tsx"
      via: "send({t:\"lc.set\", cfg})"
      pattern: "lc\\.set"
---

<objective>
상따 전략 폼의 **조작 규율**을 구현한다 — 이 phase 에서 사용자 사고와 가장 가까운 표면이다.

- `lib/limit-chaser.ts`: 매수수량 산출 `floor(금액(만원) × 10000 / 매수가격)`(**역산 금지**), 전략 키 조립, 더티 판정, 신규 폼 기본값·상한가 시딩, 예상 매도수량.
- `limit-chaser-form.tsx`: UI-SPEC A4~A9 의 **카드 2개 · 그룹 6개**(매수주문/매수가격/한방체결 · 매도주문/매도가격/매수 미체결 자동취소). 스위치 즉시 전송(D-05), 값 변경은 더티(D-06), 에코 도착 시 서버값 우선(D-11).
- `dirty-action-bar.tsx`: 더티가 있을 때만 나타나는 하단 고정 바(상따·VI 공용, analog 없음 — UI-SPEC A10/B4 가 정본).

Purpose: 「스위치는 즉시, 값은 수정 버튼」이 이 phase 의 핵심 규율이다. 여기서 자동 반영이 되살아나면 D-06 위반이다.
Output: 순수 함수 모듈 · 폼 컴포넌트 · 액션 바 · 단위/RTL 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/limit-chaser.ts 순수 함수 + 단위 테스트</name>
  <files>webapp/src/lib/limit-chaser.ts, webapp/src/lib/__tests__/limit-chaser.test.ts</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md` §Code Examples 매수수량 산출·전략 키(1002~1020행) · §Protocol A WinForms 기본값 표(357~370행) · Pitfall 11(909~912행)
    - `/Users/alex/repos/gh-trade/client/Forms/Trading/LimitChaserForm.cs` — `BuyOrderQtyFromAmount`(2809~2817), `SeedFromUpperLimitOnce`(711), 기본값 상수(129~178), 예상 매도수량(2394~2400)
    - `packages/shared/src/relay.ts` — `RelayLimitChaser` / `RelayLimitChaserInput`(16-03)
    - `webapp/src/lib/limit-up-format.ts` — 순수 유틸 모듈 형태(파일 헤더·export 규약)
  </read_first>
  <behavior>
    - `buyOrderQtyFromAmount(10, 30000)` === 3 (10만원 ÷ 30,000원, 내림)
    - `buyOrderQtyFromAmount(10, 0)` === 0 (가격 0 이면 0)
    - `buyOrderQtyFromAmount(1_000_000, 1)` 이 uint 래핑 없이 큰 정수를 반환(long 계산 후 좁힘)
    - `strategyKey("KR7005930003", "1234567890", "KRX")` === "KR7005930003:1234567890:KRX"
    - `strategyKey` 는 isin·accountNo 를 12자로 절단한다(서버 strncpy 동형)
    - `dirtyFieldsOf(server, form)` 이 값이 다른 필드 이름 배열을 돌려주고, **S→C 전용 4필드는 비교 대상에서 제외**한다
    - `buyOrderAmount === 0` 인 서버값은 더티 비교에서 제외된다(「서버가 모른다」 — 사용자 입력을 덮지 않는다)
    - `isDeleteIntent({buyEnabled:false, sellEnabled:false, cancelQtyEnabled:false, cancelTradeEnabled:false})` === true
    - `isDeleteIntent({buyEnabled:false, sellEnabled:false, cancelQtyEnabled:true, ...})` === false (취소 게이트가 살아 있으면 전략이 남는다)
    - `seedFromUpperLimit(upperLimit)` 가 매수감시가·매수가격·매도감시가·매도가격·한방가격 5칸을 상한가로 채운다
    - `defaultLimitChaserForm()` 이 WinForms 기본값(감시잔량 10000 · 주문금액 10 만원 · 최소체결 30000 · 매도호가잔량 10 · 잔량추적 50 · 매도비율 100 · 호가변경 3 · 취소잔량 10 · 게이트 전부 false · buyWatchSide "0")을 돌려준다
    - `estimatedSellQty(sellableQty, ratio)` === `Math.floor(sellableQty * ratio / 100)`
  </behavior>
  <action>
위 behavior 를 만족하는 순수 함수 모듈을 만든다. 파일 상단에 「결정 근거 / 함정 / 하지 않는 것」 3블록 주석(S-1):
- 결정 근거: D-06(수정 버튼) 더티 판정의 유일 지점 · D-08 삭제 판정 · 수량 산출식은 gh-trade 정본과 동형
- 함정: **역산 금지**(수량×가격÷10000 은 나머지 손실로 왕복이 깨진다 — 무장 수량 2주가 598주가 된 실측 사고), `buyOrderAmount === 0` 은 「서버가 모른다」이므로 금액 칸을 덮지 않는다, 삭제 판정에 **취소 게이트를 포함**해야 한다(Pitfall 7)
- 하지 않는 것: 서버로 보낼 페이로드를 여기서 조립하지 않는다(전송은 폼이 컨텍스트 `send` 로), S→C 전용 4필드를 입력 타입에 넣지 않는다

`crud` 결정도 여기서 한다: `crudOf(form)` = `isDeleteIntent(form) ? "D" : "C"`. **UI 의 「삭제됨」 표시는 서버 에코의 `crud` 를 봐야 한다**는 주석을 남긴다(클라 판정은 전송용 힌트일 뿐).
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp test -- limit-chaser</automated>
  </verify>
  <acceptance_criteria>
    - 위 behavior 11개가 각각 테스트 케이스로 존재하고 green
    - `grep -c "export function \(buyOrderQtyFromAmount\|strategyKey\|dirtyFieldsOf\|isDeleteIntent\|seedFromUpperLimit\|defaultLimitChaserForm\|estimatedSellQty\|crudOf\)" webapp/src/lib/limit-chaser.ts` == 8
    - `dirtyFieldsOf` 비교 대상에 `sellOrderQty`·`sellQtyTrackBaseline`·`sellEntryLatched`·`cancelQtyTrackBaseline` 이 포함되지 않음을 단언하는 케이스 존재
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>순수 함수 8종 + 11 케이스 green.</done>
</task>

<task type="auto">
  <name>Task 2: dirty-action-bar + limit-chaser-form 구현</name>
  <files>webapp/src/components/trading/dirty-action-bar.tsx, webapp/src/components/trading/limit-chaser-form.tsx</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-limit-chaser-mockup.html` — **정본**(시나리오 5종: 신규 빈 폼 / 매수 ON·매도 대기 / 값 변경→수정 버튼 / 발주됨·매도 감시 / DMA 매핑 없음). 마크업·클래스·간격을 여기서 읽는다
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md` §A4~A10(267~340행) · §상따 라벨 표(458~492행) · §CTA·더티 액션 바(508~521행) · §조작 규율(585~597행) · §R7 모바일 2열(748~762행)
    - `webapp/src/components/orderbook/order-panel.tsx` — 파일 상단 규율 헤더(:3~40), `FormRow` + 네이티브 `<select>`(:451~473), 세그먼트 탭 마크업(:426~449, Radix ToggleGroup 미사용 근거 주석 :422), 제출 가드(:326~406)
    - `webapp/src/components/ui/{switch,checkbox,card,button,toggle-group,tabs}.tsx`
    - `webapp/src/lib/limit-chaser.ts`(Task 1)
    - `webapp/src/lib/relay-provider.tsx`(16-09) — `send` · `limitChasers`
  </read_first>
  <action>
① `dirty-action-bar.tsx`(신규, analog 없음 — UI-SPEC A10/B4 가 정본): props `{ dirtyCount, submitting, onSubmit, onRevert, hint }`. `dirtyCount === 0` 이면 **렌더 자체를 하지 않는다**. 제목 `변경한 값 {N}개가 아직 서버에 반영되지 않았어요`, 보조문은 prop(상따 = `「수정」을 눌러야 반영돼요 · 스위치를 켜면 변경한 값까지 함께 반영돼요`), 버튼 `수정`(`--primary` 채움, 글자색은 `--destructive-fg` — S-6 토큰 충돌) + `되돌리기`(보조). 전송 중 `반영 중…` + 비활성. 모바일은 `position: fixed` 하단 + 본문 하단 96px 여백, 등장은 **fade 120ms**(slide-up 금지). `role="status"` + `aria-live="polite"`, **포커스를 빼앗지 않는다**. `data-slot="dirty-action-bar"`.

② `limit-chaser-form.tsx`(신규): 매수 카드 1개(그룹 3: 매수주문·매수가격·한방체결) + 매도 카드 1개(그룹 3: 매도주문·매도가격·매수 미체결 자동취소). **카드를 6개로 쪼개지 않는다.**
   - 필드 라벨은 UI-SPEC 표 **그대로**: 매수주문 `비교가격`(원)·`감시 대상`(매도잔량/매수잔량 세그먼트)·`잔량`(주)·☑`체결`(주) / 매수가격 `매수가격`(원)·`주문금액`(만원) + 읽기전용 `산출 주문수량`·`실제 주문금액` / 한방체결 `호가변경`(건)·`한방가격`(원) / 매도주문 `비교가격`·`호가잔량`·☑`잔량추적`(%)·☑`체결`(주) + (래치 후)`잔량추적 기준선` / 매도가격 `매도가격`·`매도비율`(%) + `예상 매도수량 (매도가능 {N}주 × {R}%)` / 자동취소 ☑`취소잔량`(주)·☑`매도 「체결」 값 재사용`·☑`매도 「비율」 값 재사용`.
   - 그룹 힌트 문장은 **화면에 렌더하지 않고 `title` 툴팁**으로만(고밀도 폼에서 Radix 툴팁 6개는 마우스마다 팝오버가 튄다). 한방체결·자동취소 힌트는 데스크톱만 표시.
   - **스위치 3개**(매수주문·매도주문·한방체결): 44×26, 그룹 헤더 우측 끝 `margin-left:auto`, 최소 8px 간격. `onCheckedChange` → **확인 없이 즉시** `send({t:"lc.set", cfg})` — cfg 는 **그 시점 폼 전체 값 + 뒤집힌 스위치**, `crud` 는 `crudOf(next)`. `aria-label` 은 `매수주문 켜기`/`매도주문 켜기`/`한방체결 켜기`.
   - **값 변경**: 더티 표시(라벨 `--primary` + `● ` 접두, 입력 테두리 `--primary` + 2px 링) + `DirtyActionBar` 등장. 「수정」 클릭 시에만 `lc.set` 전송(**crud "C" · 전체 33필드**).
   - **에코 수신**(D-11): 상위에서 받은 `RelayLimitChaser` 가 바뀌면 **폼 상태를 서버값으로 덮어쓴다**(더티 필드 포함) → 액션 바 소멸 + 배너(상위가 렌더) + 변경 필드 배경 플래시 1회 ≤150ms. 편집 중 보호·보류 없음. `buyOrderAmount === 0` 인 에코는 금액 칸을 덮지 않는다.
   - 자동취소 그룹은 **중립 색**(좌측 3px `--border`) + 헤더 캡션 `가드`. **`--warn` 을 쓰지 않는다**(UI-SPEC C1/FLAG-1 — 저장소에 토큰이 없다). 취소잔량 미체크면 잔량추적 비활성(`opacity:.45`).
   - 모바일: 오른쪽 58% 컬럼 안의 **「매수」/「매도」 세그먼트 탭**. `order-panel.tsx:426~449` 마크업 그대로(`role="tablist"`/`role="tab"`/`aria-selected`), Radix ToggleGroup 을 쓰지 않는 근거 주석도 승계. **비활성 pane 은 `hidden` 속성 + `[hidden]{display:none!important}`**(작성자 `display:grid` 가 UA 규칙을 이기는 함정 — Pitfall 13). 라벨 컬럼 `--lw` 60px, 입력 12px/패딩 8px.
   - **그리드/플렉스 자식 전부 `min-w-0`**(lessons.md).
   - 파일 상단 규율 헤더를 `order-panel.tsx` 형식으로 쓰되 **규율 3(확인 다이얼로그)은 D-05 로 뒤집혔음**을 명시하고, 대신 「44×26 크기 + 8px 간격 + 헤더 우측 고정 위치」가 오터치 방어라고 적는다. 「토스트를 쓰지 않는다」(UI-SPEC D3)는 유지.
   - `data-slot="limit-chaser-form"` · 그룹마다 `data-slot="lc-group-{buy|buy-price|sweep|sell|sell-price|cancel}"`(E2E 앵커, S-9).
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp test -- limit-chaser-form</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "confirm\|Dialog" webapp/src/components/trading/limit-chaser-form.tsx` 에서 **스위치 경로에 확인 다이얼로그가 없다**(스위치 핸들러가 곧바로 send 를 호출)
    - `grep -c "setTimeout(.*300\|debounce" webapp/src/components/trading/limit-chaser-form.tsx` == 0 (0.3초 자동 반영 폐기 — D-06)
    - `grep -c "\-\-warn" webapp/src/components/trading/limit-chaser-form.tsx` == 0
    - `grep -c "min-w-0" webapp/src/components/trading/limit-chaser-form.tsx` >= 3
    - `hidden` 속성으로 비활성 pane 을 감추는 마크업이 존재하고 전역 CSS 에 `[hidden]{display:none!important}` 가 있다
    - `dirty-action-bar.tsx` 가 `dirtyCount === 0` 일 때 `null` 을 반환한다
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>폼·액션 바가 UI-SPEC/목업대로 구현되고 자동 반영 경로가 0건이다.</done>
</task>

<task type="auto">
  <name>Task 3: limit-chaser-form RTL 테스트</name>
  <files>webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx</files>
  <read_first>
    - `webapp/src/components/orderbook/__tests__/account-panel.test.tsx` — 「잠그는 규칙」 헤더 · 팩토리 + `Partial<T>` 픽스처 · `userEvent` 사용법
    - `webapp/src/lib/relay-provider.tsx`(16-09/16-10) — 테스트용 Provider 주입 방식
  </read_first>
  <action>
파일 상단에 **「잠그는 규칙」 목록**을 적는다(D-05 스위치 즉시 / D-06 값은 수정 버튼 / D-08 전부 OFF = crud "D" / D-11 에코 우선 / S→C 4필드 미송신 / 토스트 없음).

케이스:
1. **스위치 즉시 전송**: 매수주문 스위치 ON → `send` 가 `{t:"lc.set"}` 로 1회, `cfg.buyEnabled === true`, `cfg.crud === "C"`. **확인 다이얼로그가 뜨지 않는다.**
2. **값 변경은 전송 없음**: 매수가격 입력 변경 → `send` 호출 0회, 액션 바가 나타나고 제목에 「1개」가 들어간다.
3. **수정 버튼 전송**: 「수정」 클릭 → `lc.set` 1회, cfg 에 변경값이 실린다.
4. **되돌리기**: 「되돌리기」 → 폼이 서버값으로 복귀, 액션 바 사라짐, `send` 호출 0회.
5. **더티 + 스위치**: 더티가 있는 상태에서 스위치를 켜면 전송 cfg 에 **더티 값도 함께** 실린다.
6. **전부 OFF = D**: 매수·매도·취소 게이트가 모두 꺼지는 마지막 스위치 OFF 시 `cfg.crud === "D"`.
7. **취소 게이트 생존**: 취소잔량 체크가 켜져 있으면 매수·매도를 둘 다 꺼도 `crud === "C"`.
8. **에코 덮어쓰기**: 더티 2개인 상태에서 새 `RelayLimitChaser` prop 이 들어오면 폼이 서버값으로 바뀌고 액션 바가 사라진다.
9. **S→C 4필드 미송신**: 어떤 전송에서도 cfg 에 `sellOrderQty`·`sellQtyTrackBaseline`·`sellEntryLatched`·`cancelQtyTrackBaseline` 키가 없다.
10. **파생값**: 주문금액 10(만원) + 매수가격 30,000 → `산출 주문수량` 3, `실제 주문금액` 90,000.
11. **금액 0 에코**: `buyOrderAmount: 0` 인 에코는 금액 입력값을 덮지 않는다.
12. **접근성**: 스위치 3개에 `aria-label`, 모바일 탭에 `role="tablist"`/`aria-selected`, 비활성 pane 이 `hidden`.
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp test -- limit-chaser-form</automated>
  </verify>
  <acceptance_criteria>
    - 12 케이스 전부 green
    - 케이스 1 이 다이얼로그 부재를 명시적으로 단언한다(`queryByRole('dialog')` null)
    - 케이스 9 가 전송 인자 객체의 키 집합을 검사한다
    - `pnpm --filter gh-radar-webapp test` 전량 exit 0
  </acceptance_criteria>
  <done>조작 규율 6종이 RTL 로 고정된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 손가락 → 스위치 | 확인 없이 실계좌 전략이 등록·삭제된다 |
| 폼 상태 → 전송 페이로드 | 잘못된 필드가 실리면 무장 수량이 수백 배로 튄다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | 금전 손실(오조작) | 스위치 즉시 전송 | accept | D-05 가 확인 다이얼로그를 명시적으로 배제(상한가 직전 조작 속도 우선). 대신 44×26 크기 + 8px 간격 + 고정 위치로 오터치를 낮추고, 액션 바가 「스위치를 켜면 변경한 값까지 함께 반영돼요」를 상시 고지 |
| T-16-10 | 금전 손실(수량 오산) | `buyOrderQtyFromAmount` | mitigate | 역산 금지 + long 계산 후 좁힘 + 단위 테스트 3케이스. 실측 사고(2주→598주) 이력을 주석에 남긴다 |
| T-16-01 | Elevation of Privilege | `cfg.accountNo` | transfer | 폼은 계좌를 고르기만 한다. 소유권 대조는 relay(`session.allowedAccounts`, 16-07) — 폼에서 통과했다고 권한이 아니다 |
| T-16-05 | Tampering | S→C 전용 4필드 | mitigate | 전송 페이로드에서 제외(케이스 9). 보내면 「값이 왕복한다」는 착각으로 에코 비교가 오염된다 |
</threat_model>

<verification>
- `pnpm typecheck` exit 0
- `pnpm --filter gh-radar-webapp test` exit 0
</verification>

<success_criteria>
- 스위치 즉시 / 값은 수정 버튼 / 전부 OFF = D / 에코 우선 4규율이 테스트로 고정
- 파생값·기본값·시딩이 gh-trade 정본과 동형
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-12-SUMMARY.md` when done
</output>
