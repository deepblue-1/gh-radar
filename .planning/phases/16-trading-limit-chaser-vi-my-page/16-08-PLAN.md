---
phase: 16-trading-limit-chaser-vi-my-page
plan: 08
type: execute
wave: 7
depends_on: ["16-07"]
files_modified:
  - relay/src/store/symbols.ts
  - relay/src/store/orders.ts
  - relay/src/ws/order-handler.ts
  - relay/src/ws/fanout.ts
  - relay/src/index.ts
  - relay/tests/ws-order.test.ts
  - relay/tests/order-store.test.ts
  - relay/tests/symbols.test.ts
autonomous: true
requirements: [TRADE-03]

must_haves:
  truths:
    - "브라우저가 wss 로 보낸 주문이 relay 에서 5초 상관 후 order.result 로 돌아온다 (D-02)"
    - "5초를 넘긴 주문은 「실패」가 아니라 「결과 모름」으로 처리되고 제출 버튼이 다시 열리지 않는다 (Pitfall 9 / S-8)"
    - "dma_orders 행의 insert 와 update 를 relay 가 전담한다 (D-03)"
    - "상따·VI 자동주문 통보는 기존 행을 못 찾으면 새 행으로 insert 되어 감사 기록이 비지 않는다 (D-03 / Pitfall 18)"
    - "주문의 account_no 는 session.allowedAccounts 와 대조된 뒤에만 게이트웨이로 나간다 (T-16-01)"
  artifacts:
    - path: "relay/src/ws/order-handler.ts"
      provides: "wss 주문 요청/응답 상관(5초) + 5단계 처리 순서"
      contains: "ORDER_RESP_TIMEOUT_MS"
    - path: "relay/src/store/orders.ts"
      provides: "insertRequest(동기) + update 큐 + origin 매핑"
      contains: "insertRequest"
    - path: "relay/src/store/symbols.ts"
      provides: "ISIN → 단축코드·시장 조회"
      contains: "market"
  key_links:
    - from: "relay/src/ws/order-handler.ts"
      to: "relay/src/store/symbols.ts"
      via: "lookup(isin) → { code, market }"
      pattern: "lookup\\("
    - from: "relay/src/ws/order-handler.ts"
      to: "relay/src/store/orders.ts"
      via: "insertRequest 로 얻은 id 가 상관 1순위 키"
      pattern: "insertRequest"
---

<objective>
주문 경로를 **REST → wss 로 이관**한다(D-02). 이것은 재작성이 아니라 **이식**이다 — `order-api.ts:340~565` 의 5단계 순서 · `statusOf` · `filledQtyOf` · 5초 타임아웃 문구를 그대로 옮기고 HTTP 껍데기만 벗긴다(PATTERNS 핵심 3줄 ②).

바뀌는 것 4가지:
1. `userId` 는 요청 바디가 아니라 **연결**(`conn.userId`)에서, `orderRowId` 는 **relay 자신이 insert 한 행 id** 에서 온다.
2. 브라우저는 **ISIN 을 보낸다**(6자 단축코드가 아니다). relay 가 `SymbolMap.lookup(isin)` 으로 `code`(dma_orders.stock_code)와 `market`("K"/"Q")을 채운다 — `SymbolMap` 은 이미 ISIN 키 맵이므로 역방향 인덱스가 필요 없다. 다만 **`market` 컬럼을 select 에 추가**해야 한다.
3. 타임아웃 응답은 HTTP 202 가 아니라 `{t:"order.result", rid, status:"timeout"}` 프레임 1건.
4. `dma_orders` insert 를 relay 가 한다(**insert 는 큐잉하지 않고 await** — 반환 id 가 상관 1순위 키). 자동주문(상따·VI) 통보는 대기 큐에 없으므로 **새 행 insert** 로 분기한다.

**REST 경로는 이 plan 에서 지우지 않는다** — 16-16 에서 webapp 이 wss 로 넘어간 뒤 같은 흐름으로 제거한다.
Purpose: 세션을 쥔 프로세스가 상관도 쥔다. 두 경로가 같은 `dma_orders` 행을 다투는 기간을 만들지 않는다.
Output: `order-handler.ts` · `OrderStore.insertRequest` · `SymbolMap.market` · fanout 결선 · 테스트 3파일.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SymbolMap 에 market 추가 + OrderStore insert 경로</name>
  <files>relay/src/store/symbols.ts, relay/src/store/orders.ts, relay/tests/symbols.test.ts, relay/tests/order-store.test.ts</files>
  <read_first>
    - `relay/src/store/symbols.ts` — `SymbolInfo`(:49~54), `SymbolLookup`(:57~59), `StockRow`(:61~66), `refresh()`(:114~) 의 `.select("code, name, isin, is_delisted")`
    - `relay/src/store/orders.ts` 전문(288줄) — 파일 상단 「하지 않는 것: insert 를 하지 않는다. 행을 만드는 것은 server 다」 주석(:26 부근), `supabaseOrderSink`(:111~120), `OrderUpdate`/`OrderSelector`/`rowPatchOf`(:275), `enqueueUpdate`, `ORDER_FLUSH_INTERVAL_MS`
    - `server/src/services/dma-orders.ts` — `insertOrderRequest`(이식 원본) · `resolveIsinAndMarket`(:118~137) · `toOrderMarket`(:143~147, KOSPI→"K"/KOSDAQ→"Q", **모르는 값은 null — 기본값 금지**)
    - `relay/tests/order-store.test.ts` · `relay/tests/symbols.test.ts`
  </read_first>
  <action>
① `SymbolInfo` 에 `market: OrderMarket | null` 을 추가하고 `refresh()` 의 select 를 `"code, name, isin, market, is_delisted"` 로 넓힌다. `stocks.market` 은 `"KOSPI"`/`"KOSDAQ"` 문자열이므로 `toOrderMarket` 을 **server 에서 relay 로 이식**해 `symbols.ts` 안 한 곳에 둔다(D-21 「변환은 한 곳」). **모르는 값은 `null`** 이며 기본값 `"K"` 로 메우지 않는다 — 코스닥 주문이 코스피로 나가는 사고를 막는다. `market` 이 null 인 종목은 주문 조립 단계에서 명시 거부한다(Task 2).

② `OrderStore` 에 `insertRequest(row): Promise<string>` 을 추가한다. `server/src/services/dma-orders.ts` 의 `insertOrderRequest` 를 그대로 이식하되 컬럼에 **`origin`** 을 더한다(`'manual' | 'limit_chaser' | 'vi'`). **`await` 로 즉시 insert 하고 id 를 반환**한다 — 큐잉하지 않는 이유(반환 id 가 상관 1순위 키)를 주석에 남긴다. 실패는 throw 하고 호출자가 사용자에게 알린다(조용한 실패 금지).

③ `rowPatchOf`(:275, camelCase↔snake_case 경계의 유일 지점)에 `origin` 매핑을 추가한다. `OrderUpdate` 타입에도 `origin?` 을 더한다.

④ 파일 상단 주석의 「하지 않는 것: insert 를 하지 않는다」를 **D-03 근거로 교체**한다: 「insert·update 를 전부 relay 가 한다(D-03). insert 는 `await`(id 가 상관 키), update 만 큐잉한다(D-32 — 수신 콜백에서 Supabase 를 await 하면 게이트웨이 송신 큐가 찬다).」

⑤ 테스트: `symbols.test.ts` 에 「`market` 이 KOSPI→"K"/KOSDAQ→"Q"/그 외→null 로 매핑된다」 + 「lookup 결과에 market 이 실린다」. `order-store.test.ts` 에 「insertRequest 가 id 를 반환하고 origin 이 저장된다」 + 「insert 실패는 throw 되고 큐에 남지 않는다」 + 「update 는 여전히 큐잉된다」.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test -- symbols &amp;&amp; pnpm --filter @gh-radar/relay test -- order-store</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "code, name, isin, market, is_delisted" relay/src/store/symbols.ts` == 1
    - `grep -c "toOrderMarket" relay/src/store/symbols.ts` >= 2 이고 함수 본문에 `return null` 이 존재(기본값 금지)
    - `grep -c "insertRequest" relay/src/store/orders.ts` >= 2
    - `grep -c "행을 만드는 것은 server 다" relay/src/store/orders.ts` == 0
    - `grep -c "origin" relay/src/store/orders.ts` >= 2
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>ISIN→(code, market) 조회와 dma_orders insert 경로가 relay 안에 있다.</done>
</task>

<task type="auto">
  <name>Task 2: wss 주문 핸들러 — 5단계 이식 + rid 상관</name>
  <files>relay/src/ws/order-handler.ts, relay/src/ws/fanout.ts, relay/src/index.ts</files>
  <read_first>
    - `relay/src/order/order-api.ts` 전문 — `ORDER_RESP_TIMEOUT_MS`(5000), `PendingOrder`(:277~283), 통보 소비(:352~382), 5단계 처리(:400~560, ①~⑤ 주석), 타임아웃 블록(:461~473), `statusOf`(:246), `filledQtyOf`(:272), `maskAccountNo` 사용(:393~399)
    - `relay/src/ws/fanout.ts` — 16-07 이 만든 전략 분기 자리, `#send`/`#deliver`, `FanoutSessions.get`
    - `relay/src/index.ts` — `createOrderApi({ orders: hub, orderStore })` 결선(:117~), `new WsFanout({...})`(:98~)
    - `relay/src/hub/subscription-hub.ts` — `emit("order", { userId, notice })` (`HubOrderEvent`)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md` §wss 주문 핸들러(367~428행)
  </read_first>
  <action>
`relay/src/ws/order-handler.ts` 를 신규로 만든다. 파일 상단에 「결정 근거 / 함정 / 하지 않는 것」 3블록 주석(S-1)을 쓰고, `order-api.ts` 에서 **그대로 옮기는 것**과 **바뀌는 것**을 명시한다.

**구조**
- `createOrderHandler(deps: { sessions, hub, orderStore, symbols, supabase })` → `{ handle(conn, userId, msg): Promise<void>, close(): void }`.
- 상관 자료구조: `Map<Conn, PendingOrder[]>` — **연결 스코프**다(T-16-03). `PendingOrder = { rid, orderRowId, isin, qty, timer, settle }`. 사용자 전 연결이 아니라 **요청한 연결에만** `order.result` 를 돌려준다(게이트웨이의 「요청 연결에만 vs 전 연결」 규약과 동형; 51 푸시 `{t:"order"}` 는 기존대로 사용자 전 연결).
- `hub.on("order", ({userId, notice}))` 소비: 그 userId 의 **모든 연결**의 대기열을 훑어 `p.isin === notice.isin` 인 첫 항목과 매칭한다. 못 찾으면 → **자동주문 경로**(아래 Task 3).

**5단계 처리 순서(번호 주석 ①~⑤ 를 그대로 옮긴다)**
① 활성 Ready 세션 확인(`sessions.get(userId)`, 여기서 대신 로그인하지 않는다).
② **계좌 화이트리스트 대조** — `session.allowedAccounts` 만 근거(T-16-01). 실패 시 `logger.error` + `{t:"order.result", rid, status:"rejected", message}`.
③ 취소는 `orgOrderNo` 필수(zod 에서 이미 강제하지만 방어적으로 재확인).
③-1 **ISIN 해석**: `symbols.lookup(isin)` → `{code, market}`. 없거나 `market === null` 이면 거부 프레임 + error 로그(지어내지 않는다).
③-2 **`dma_orders` insert**: `orderStore.insertRequest({userId, accountNo, isin, code, exchange, market, side, orderType, orgOrderNo, qty, price, origin:'manual'})` → `orderRowId`.
④ **대기 등록을 송신보다 먼저** — 통보가 동기적으로 돌아오는 저지연/테스트 환경에서 순서가 뒤바뀌면 응답을 영영 놓친다. `timer.unref?.()` 유지.
⑤ 송신: `buildDirectOrderReq(...)` → `session.send(payload)`. `false` 면 대기를 즉시 걷어내고 `clearTimeout`.

**결과 프레임 3분류**
- 통보 도착: `statusOf(notice, requestedQty)` / `filledQtyOf(notice)` 를 **그대로 import 해 재사용**(재작성 금지) → `orderStore.enqueueUpdate({orderRowId, orderNo, status, resultCode, noticeType, message, filledQty})` + `{t:"order.result", rid, orderNo, resultCode, message, status}`.
- 5초 초과: `enqueueUpdate({orderRowId, status:"timeout"})` + `logger.error(... "첫 주문 통보 미수신 — 결과 확인 필요")` + `{t:"order.result", rid, status:"timeout", message:"주문 결과를 확인하지 못했습니다. 미체결 목록을 확인해 주세요."}`. **「실패」라는 단어를 쓰지 않는다**(S-8).
- 중복 방지: 같은 `rid` 가 이미 대기 중이거나, 같은 `(accountNo, isin, side, price, qty)` 가 대기 중이면 거부하고 사유를 돌려준다(T-16-10).

**결선**
- `fanout.ts` 의 `#onAuthedMessage` 전략 분기 옆에 `order.new`/`order.cancel` 분기를 추가해 핸들러로 위임한다(`keyOf` 는 이미 sub/unsub 안으로 옮겨져 있다). 연결 종료 시 그 연결의 대기열을 정리한다(타이머 누수 0).
- `index.ts` 에서 `WsFanout` 에 `orderStore`·`symbols` 를 주입한다. `createOrderApi(...)` 결선은 **아직 그대로 둔다**(16-16 에서 제거).
- 계좌번호는 로그에서 `maskAccountNo`, 화면·프레임에는 전체(S-5).
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test -- ws-order</automated>
  </verify>
  <acceptance_criteria>
    - `relay/src/ws/order-handler.ts` 가 `statusOf`/`filledQtyOf` 를 `order-api.ts`(또는 공용 모듈)에서 **import** 한다 — 같은 이름의 재구현 0건
    - `grep -c "ORDER_RESP_TIMEOUT_MS" relay/src/ws/order-handler.ts` >= 1 이고 값 5000 을 재선언하지 않고 재사용
    - 대기 맵의 키가 연결(`Conn`)이며 전역 `rid` 맵이 아니다 (`grep -c "Map<string, PendingOrder" relay/src/ws/order-handler.ts` == 0)
    - `grep -c "실패" relay/src/ws/order-handler.ts` 의 타임아웃 블록 내 사용 0건 (「결과 확인 필요」 문구 사용)
    - `grep -c "allowedAccounts" relay/src/ws/order-handler.ts` >= 1
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>wss 주문 요청/응답이 5초 상관으로 동작하고 5단계 순서·타임아웃 규율이 보존된다.</done>
</task>

<task type="auto">
  <name>Task 3: 자동주문 통보 insert 분기 + ws-order 테스트 재작성</name>
  <files>relay/src/ws/order-handler.ts, relay/tests/ws-order.test.ts</files>
  <read_first>
    - `relay/src/order/order-api.ts:352~382` — 「접수 이후의 통보다. `order_no` 로 행을 좁혀 갱신한다」 분기
    - `relay/tests/order-api.test.ts` 전문 — 재작성 원본(HTTP → wss)
    - `relay/src/store/orders.ts`(Task 1 결과) — `insertRequest` · `selectorOf`
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md` Pitfall 18(936~939행) · §Supabase D-03(817~836행)
  </read_first>
  <action>
① **자동주문 insert 분기**: 통보가 대기열과 매칭되지 않을 때 현재 코드는 `enqueueUpdate({orderNo, ...})` 로 끝난다. PostgREST 의 update 는 **0행이어도 에러가 아니므로** 상따·VI 자동주문 통보는 조용히 사라진다. 다음으로 바꾼다:
   - `notice.origin` 을 `toOrderOrigin` 으로 매핑(16-05). `manual` 이 아니고(= 자동주문) **`order_no` 로 조회했을 때 행이 없으면** `orderStore.insertRequest({ ..., origin, status 는 통보에서 파생 })` 로 **새 행을 만든다**. `qty`/`price` 는 통보 값, `stock_code`/`market` 은 `symbols.lookup(notice.isin)`, `accountNo` 는 통보 값(없으면 세션의 계좌 중 매칭 불가 시 로그 남기고 `null` 대신 거부하지 말고 기록은 남긴다 — 감사 우선).
   - 기존 행이 있으면 지금처럼 update 큐.
   - 어느 쪽이든 `logger.info` 로 origin 과 orderNo 를 남긴다(마스킹 규칙 유지).
   - **`status='requested'` 로 시작하지 않아도 CHECK 를 통과**한다(accepted/filled/…). 이 사실을 주석으로 남긴다.

② `relay/tests/order-api.test.ts` 를 **`relay/tests/ws-order.test.ts` 로 재작성**한다(HTTP 하네스 → wss 하네스). 케이스:
   1. 정상 신규: `order.new` → 게이트웨이가 msg_type 2 수신 → 51(A) 주입 → `{t:"order.result", status:"accepted", orderNo}` 가 **요청한 연결로** 온다.
   2. 결과는 요청 연결에만: 같은 사용자의 두 번째 연결에는 `order.result` 가 가지 않는다. 단 `{t:"order"}`(51 푸시)는 두 연결 모두 받는다.
   3. 계좌 거부: 허용되지 않은 `accountNo` → 게이트웨이로 0바이트, `status:"rejected"`, error 로그.
   4. 5초 타임아웃: 통보를 주지 않으면 `status:"timeout"` + `dma_orders` update 에 `status:"timeout"` 이 큐잉되고 메시지에 「실패」 문구가 없다.
   5. 송신 실패: `session.send` 가 false 를 반환하면 대기열이 즉시 비고 5초를 기다리지 않는다.
   6. 취소: `order.cancel` 은 `orgOrderNo` 없으면 거부, 있으면 msg_type 2 + orderType "C" 로 나간다.
   7. ISIN 미해석: `symbols.lookup` 이 undefined 이거나 `market === null` 이면 거부되고 게이트웨이로 나가지 않는다.
   8. 중복 rid: 같은 rid 재전송은 거부된다.
   9. **자동주문 insert**: 대기열에 없는 51 통보(origin "LimitChaser")가 오면 `insertRequest` 가 호출되고 `origin:'limit_chaser'` 로 기록된다.
   10. 기존 행 update: 대기열에 없지만 `order_no` 로 행이 있으면 insert 가 아니라 update 큐잉.
   11. 연결 종료 시 대기열·타이머 정리(누수 0).
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test -- ws-order</automated>
  </verify>
  <acceptance_criteria>
    - `relay/tests/ws-order.test.ts` 에 11개 이상 케이스가 있고 전부 green
    - `relay/tests/order-api.test.ts` 는 삭제되었거나 **HTTP 주문 라우트가 살아 있는 동안의 회귀 테스트로만** 남는다(둘 중 무엇을 택했는지 SUMMARY 에 명시)
    - 케이스 9 가 `insertRequest` 호출 인자에 `origin: 'limit_chaser'` 를 단언한다
    - 케이스 2 가 두 소켓을 열어 프레임 도달 범위를 구분해 단언한다
    - `pnpm --filter @gh-radar/relay test` 전량 exit 0
  </acceptance_criteria>
  <done>자동주문 감사 기록이 남고, wss 주문 경로가 테스트로 고정된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → relay wss(주문) | 금전이 걸린 경계. 형식 통과 ≠ 권한 |
| relay → Supabase(service_role) | `dma_orders` 는 서비스롤만 쓴다 |
| 게이트웨이 통보 → 상관 매칭 | 매칭이 틀리면 남의 주문 결과를 받는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Elevation of Privilege | `order.new` / `order.cancel` 의 `accountNo` | mitigate | ② 단계 `session.allowedAccounts` 대조 — 요청 바디·상태 프레임 사본 금지. 케이스 3 이 「거부 시 0바이트」 증명 |
| T-16-03 | Spoofing | `rid` 상관 | mitigate | 대기 맵을 **연결 스코프**(`Map<Conn, …>`)로 두고 전역 `rid` 맵을 만들지 않는다. `order.result` 는 요청 연결에만, 51 푸시는 사용자 전 연결(기존 규약) — 케이스 2 로 고정 |
| T-16-10 | 금전 손실(중복 주문) | 재전송·더블클릭 | mitigate | 같은 `rid` 또는 같은 (accountNo,isin,side,price,qty) 대기 중이면 거부. 타임아웃을 실패로 렌더하지 않는다(S-8) |
| T-16-07 | Repudiation | 자동주문 통보 | mitigate | 0행 update 로 조용히 사라지는 경로를 insert 분기로 교체(Pitfall 18). origin 을 기록해 상따/VI/수동을 구분 |
| T-16-05 | Tampering | `symbols.lookup` 결과 | mitigate | `market === null` 이면 **거부**한다. 기본값 "K" 로 메우면 코스닥 주문이 코스피로 나간다 |
| T-16-09 | Information Disclosure | 주문 로그 | mitigate | `maskAccountNo` 로 계좌 마스킹, 인바운드 원문 미기록 |
</threat_model>

<verification>
- `pnpm typecheck` exit 0
- `pnpm --filter @gh-radar/relay test` exit 0
- `pnpm --filter @gh-radar/relay build` exit 0
</verification>

<success_criteria>
- wss 주문 왕복(접수/거부/타임아웃/취소)이 테스트로 green
- `dma_orders` insert·update 를 relay 가 전담하고 자동주문 행이 남는다
- 상관은 연결 스코프, 계좌 대조는 allowedAccounts 단독 근거
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-08-SUMMARY.md` when done
</output>
