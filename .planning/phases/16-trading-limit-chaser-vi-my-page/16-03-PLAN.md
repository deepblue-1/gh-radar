---
phase: 16-trading-limit-chaser-vi-my-page
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - packages/shared/src/relay.ts
  - relay/src/ws/protocol.ts
  - relay/tests/protocol.test.ts
autonomous: true
requirements: [TRADE-03]

must_haves:
  truths:
    - "브라우저와 relay 가 전략·주문 메시지를 하나의 타입 계약으로 주고받는다 (D-01 전략 wss 직접 · D-02 주문 wss 통일)"
    - "스키마를 위반한 인바운드는 조용히 무시되지 않고 거부된다 (PC-7 무로그 fail-safe 금지)"
    - "인바운드 주문 메시지 판별자가 기존 아웃바운드 `{t:\"order\"}` 와 충돌하지 않는다 (RESEARCH Pitfall 15)"
  artifacts:
    - path: "packages/shared/src/relay.ts"
      provides: "전략·주문 wss 메시지 계약"
      contains: "RelayLimitChaser"
    - path: "relay/src/ws/protocol.ts"
      provides: "인바운드 zod 스키마 확장"
      contains: "RelayLcSetSchema"
    - path: "relay/tests/protocol.test.ts"
      provides: "스키마 경계 테스트"
  key_links:
    - from: "relay/src/ws/protocol.ts"
      to: "packages/shared/src/relay.ts"
      via: "RelayInbound 판별 유니온"
      pattern: "z\\.discriminatedUnion\\(\"t\""
---

<objective>
전략·주문 wss 메시지의 **계약(타입 + zod)** 을 먼저 못박는다. 이후 relay 3층(codec/hub/fanout)과 webapp 이 전부 이 계약을 참조하므로 인터페이스가 먼저다.

- `packages/shared/src/relay.ts` 에 인바운드 6종(`lc.set` · `vi.set` · `vi.confirm` · `strategies.disable` · `order.new` · `order.cancel`)과 아웃바운드 7종(`lc` · `lc.snap` · `vi` · `vi.list` · `vi.notice` · `strategies.disabled` · `order.result`) 타입을 append 한다.
- `relay/src/ws/protocol.ts` 의 `RelayInboundSchema` 에 같은 6종 zod 스키마를 append 하고, 파일 상단의 「주문 메시지를 받지 않는다(D-08)」 주석을 **D-02 로 뒤집는다**.

Purpose: 와이어 계약이 흔들리면 relay·webapp 두 벌이 갈린다. 값 검증(계좌 화이트리스트)은 여기서 하지 않는다 — 형식 검증만이 이 층의 책임이고, 소유권 검증은 세션을 쥔 핸들러(16-07/16-08)가 한다.
Output: shared 타입 계약 · relay zod 스키마 · 스키마 경계 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: shared 계약에 전략·주문 메시지 타입 추가</name>
  <files>packages/shared/src/relay.ts</files>
  <read_first>
    - `packages/shared/src/relay.ts` 전문 — `RelayInbound`(:104), `RelayAccountState`(:266~286, `snap` 규약), `RelayOrderMsg`(:293~310, `t:"order"`), `RelayUnfilled`(:243~264), `CreateOrderRequest`(:365~377), `RelayOutbound`(:330~337)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md` §Protocol A~E(269~447행) · §Shared 계약(640~681행)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md` §packages/shared/src/relay.ts(461~492행)
  </read_first>
  <action>
같은 파일의 「타입 + 필드별 JSDoc + 유니온 append」 형식으로 아래를 추가한다. **와이어는 전부 `number`** — bigint 를 계약에 넣지 않는다(D-34, `encode()` 가 런타임 throw).

**상태 타입 3종**
- `RelayLimitChaser` — `SetLimitChaser` 활성 37필드의 JSON 표현. 필드명은 생성 코드 접근자와 같은 camelCase(`isin`·`accountNo`·`market`·`crud`·`buyOrderPrice`·`buyOrderQty`·`buyWatchPrice`·`buyWatchQty`·`buyMinTradeQty`·`buyWatchSide`·`buyTradeQtyEnabled`·`buyEnabled`·`sellOrderPrice`·`sellOrderQty`·`sellWatchPrice`·`sellWatchQty`·`sellMinTradeQty`·`sellEnabled`·`sellTradeQtyEnabled`·`sweepWatchPrice`·`sweepEnabled`·`sweepMinTickCount`·`sweepRecalcEnabled`·`sweepMinCount`·`sweepMinRate`·`exchange`·`sellOrderRatio`·`sellQtyTrackEnabled`·`sellQtyTrackRatio`·`sellQtyTrackBaseline`·`buyOrderAmount`·`sellEntryLatched`·`cancelQtyEnabled`·`cancelWatchQty`·`cancelTradeEnabled`·`cancelQtyTrackEnabled`·`cancelQtyTrackBaseline`) + 파생 `key: string`(= `` `${isin}:${accountNo}:${exchange}` ``). JSDoc 에 **S→C 전용 4필드**(`sellOrderQty`·`sellQtyTrackBaseline`·`sellEntryLatched`·`cancelQtyTrackBaseline`)와 **에코의 `buyEnabled`/`sellEnabled` 는 설정값이 아니라 무장 상태**(`cfg.buyEnabled && buyArmed`)라는 것, `buyOrderAmount === 0` 은 「서버가 모른다」라는 것을 반드시 적는다(Pitfall 6/10/11).
- `RelayViTrigger` — `accountNo` · `orderAmountKrw`(number, **원 단위**) · `checkRate`(정수 %) · `priceType`("U" 고정) · `run`.
- `RelayViOrderItem` — `isin`·`market`·`accountNo`·`orderNo`(`""` = 접수 전)·`orderQty`·`orderPrice`·`triggerPrice`·`basePrice`·`viEndTime`(`"HHMMSSuuu"`)·`deadline110Ms`(number)·`deadline119Ms`(number)·`confirmed`·`confirmLocked`(**서버 계산 — 클라 재계산 금지**)·`state`(6종 유니온)·`filledQty`. JSDoc 에 「부분체결은 별 상태가 아니라 `Accepted ∧ filledQty>0` 파생」을 적는다. 종목명(`name?`)은 relay 가 SymbolMap 으로 채운다.

**인바운드 6종** (`RelayInbound` 유니온에 append)
`{t:"lc.set", cfg}`(cfg = `RelayLimitChaser` 중 **클라 입력 30 + 고정 3** 만, S→C 전용 4 제외 — 별도 타입 `RelayLimitChaserInput` 으로 `Omit` 한다) · `{t:"vi.set", accountNo, orderAmountKrw, checkRate, run}` · `{t:"vi.confirm", orderNo, confirmed}` · `{t:"strategies.disable", key?}` · `{t:"order.new", rid, isin, exchange, side, qty, price, accountNo}` · `{t:"order.cancel", rid, isin, exchange, orgOrderNo, qty, price, accountNo}`.
**`market` 은 브라우저가 보내지 않는다** — relay 가 `SymbolMap` 으로 ISIN→단축코드·시장을 채운다(16-08). JSDoc 으로 근거를 남긴다.

**아웃바운드 7종** (`RelayOutbound` 유니온에 append)
`{t:"lc", item: RelayLimitChaser}`(60 에코 1건, `crud:"D"` 포함) · `{t:"lc.snap", items: RelayLimitChaser[]}`(64 전량) · `{t:"vi", cfg: RelayViTrigger | null}`(61, null = 미등록) · `{t:"vi.list", snap: boolean, items: RelayViOrderItem[]}`(72/73 — `snap` 규약은 `RelayAccountState.snap` 과 동형) · `{t:"vi.notice", ...}`(56) · `{t:"strategies.disabled", count, viDisabled}`(65, **완료 신호로만 쓰고 상태는 에코로 갱신**) · `{t:"order.result", rid, orderNo, resultCode, message, status: DmaOrderStatus | "timeout"}`.

**이름 충돌 방지:** 기존 아웃바운드 `RelayOrderMsg.t === "order"` 를 그대로 두고 인바운드는 `order.new`/`order.cancel`, 응답은 `order.result` 로 둔다. `RelayOrderMsg` 와 `CreateOrderRequest`/`CreateOrderResponse` 의 JSDoc 중 「`POST /api/orders`」·「D-08/D-22」 문구를 **D-02(wss 단일 경로)** 로 갱신한다. `CreateOrderRequest`/`CreateOrderResponse` 타입 자체는 16-16 에서 제거·정리하므로 여기서 지우지 않는다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/shared build &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RelayLimitChaser\b" packages/shared/src/relay.ts` >= 3
    - `grep -c "\"lc.set\"\|\"vi.set\"\|\"vi.confirm\"\|\"strategies.disable\"\|\"order.new\"\|\"order.cancel\"" packages/shared/src/relay.ts` >= 6
    - `grep -c "\"lc\"\|\"lc.snap\"\|\"vi\"\|\"vi.list\"\|\"vi.notice\"\|\"strategies.disabled\"\|\"order.result\"" packages/shared/src/relay.ts` >= 7
    - `grep -c "bigint" packages/shared/src/relay.ts` == 0
    - `grep -c "cancelQtyTrackBaseline" packages/shared/src/relay.ts` >= 1
    - `pnpm --filter @gh-radar/shared build` exit 0, `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>shared 에 상태 3종 + 인바운드 6종 + 아웃바운드 7종 타입이 존재하고 빌드가 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: relay protocol.ts 인바운드 zod 확장 + 경계 테스트</name>
  <files>relay/src/ws/protocol.ts, relay/tests/protocol.test.ts</files>
  <read_first>
    - `relay/src/ws/protocol.ts` 전문(109줄) — 파일 상단 주석 :18 「주문 메시지를 받지 않는다 … (D-08)」, `IsinSchema`/`ExchangeSchema`(:30~), `RelaySubSchema`(:47), `RelayInboundSchema`(:60), `parseInbound`(:73~92), `encode`(:102)
    - `packages/shared/src/relay.ts` (Task 1 결과)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md` §relay/src/ws/protocol.ts(206~240행)
  </read_first>
  <action>
`IsinSchema`/`ExchangeSchema` 를 **재사용**해(새로 정의 금지) 아래 스키마를 만들고 `RelayInboundSchema` 판별 유니온에 append 한다. 값 범위는 게이트웨이 검증(RESEARCH §Protocol A 「서버측 게이트」 표)과 **같은 경계**로 잡아 조용한 거부를 미리 막는다:

- `RelayLcSetSchema` — `t:"lc.set"` + `cfg` 객체. `isin: IsinSchema`, `accountNo: z.string().min(1).max(12)`, `exchange: ExchangeSchema`, `market: z.enum(["K","Q"])`, `crud: z.enum(["C","D"])`, `buyWatchSide: z.enum(["0","1"])`, 가격·수량 계열은 `z.number().int().min(0)`, `sellOrderRatio: z.number().int().min(1).max(100)`, `sellQtyTrackRatio: z.number().int().min(1).max(90)`(**100 금지 — 서버 검증**), `sweepMinTickCount`/`sweepMinCount` 는 `z.number().int().min(0).max(255)`(ubyte), bool 게이트는 `z.boolean()`. **S→C 전용 4필드는 스키마에 두지 않는다**(Pitfall 6 — 보내면 왕복 착각이 생긴다).
- `RelayViSetSchema` — `accountNo`(1..12), `orderAmountKrw: z.number().int().min(0)`(원), `checkRate: z.number().int()`, `run: z.boolean()`. `priceType` 은 받지 않는다(서버 규약 "U" 고정, relay 가 채운다).
- `RelayViConfirmSchema` — `orderNo: z.string().min(1).max(10)`(**빈 문자열은 서버가 드롭하므로 애초에 거부**), `confirmed: z.boolean()`.
- `RelayStrategiesDisableSchema` — `key: z.string().max(64).optional()`(서버 WR-09 상한과 동형; 생략·`""` = 전체).
- `RelayOrderNewSchema` / `RelayOrderCancelSchema` — `rid: z.string().min(1).max(64)`, `isin: IsinSchema`, `exchange: ExchangeSchema`, `side: z.enum(["B","S"])`(new 만), `orgOrderNo: z.string().min(1)`(cancel 만 — 취소는 원주문번호 필수), `qty`/`price`: `z.number().int().positive()`, `accountNo`: 1..12.

`parseInbound` 의 total-parser 형태(throw 안 함, `null` 반환, 첫 issue 의 `path`/`code` 만 로그)는 **그대로 유지**한다 — 로그에 원문을 싣지 않는 규율(T-15-04)을 전략 스키마에도 적용한다(`lc.set` 바디에 계좌번호가 있다).

파일 상단 주석 :18 을 D-02 근거로 교체한다: 「주문·전략 메시지는 **이 소켓으로 받는다**(D-01/D-02). `POST /api/orders` 는 16-16 에서 제거된다. 계좌 소유권 검증은 이 층이 아니라 세션을 쥔 핸들러가 `session.allowedAccounts` 로 한다(T-16-01)」.

`relay/tests/protocol.test.ts` 를 새로 만든다(기존 파일이 없으면 신규): ① 6종이 각각 파싱된다 ② `sellQtyTrackRatio: 100` 은 `null` ③ `vi.confirm` 의 `orderNo: ""` 는 `null` ④ `strategies.disable` 의 65자 key 는 `null` ⑤ `order.new` 의 `qty: 0` 은 `null` ⑥ **로그에 인바운드 원문·계좌번호가 실리지 않는다**(logger 스파이로 인자 검사) ⑦ 알 수 없는 `t` 는 `null`.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test -- protocol</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RelayLcSetSchema\|RelayViSetSchema\|RelayViConfirmSchema\|RelayStrategiesDisableSchema\|RelayOrderNewSchema\|RelayOrderCancelSchema" relay/src/ws/protocol.ts` >= 12
    - `grep -c "POST /api/orders 전용" relay/src/ws/protocol.ts` == 0
    - `relay/tests/protocol.test.ts` 에 7개 이상 `it(`/`test(` 케이스가 있고 `pnpm --filter @gh-radar/relay test -- protocol` exit 0
    - `grep -c "z.string().length(12)" relay/src/ws/protocol.ts` == 1 (IsinSchema 재정의 없음 — 재사용 확인)
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>인바운드 6종이 zod 로 검증되고, 경계값 위반이 `null`(→ 상위에서 거부)로 떨어지며, 원문 로그가 없다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → relay wss | 인증된 연결이어도 **바디는 신뢰하지 않는다** — 형식·범위 검증이 여기서 끝난다 |
| relay 로그 → 운영자 | 인바운드 원문에는 계좌번호(그리고 첫 메시지에는 토큰)가 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Tampering | `RelayInboundSchema` | mitigate | zod discriminated union + 범위 경계를 게이트웨이 검증과 동형으로(ratio 1..100, trackRatio 1..90, qty/price>0). 위반은 `parseInbound → null` → 상위가 거부. **관대한 무시 금지** |
| T-16-06 | Denial of Service | `strategies.disable.key` | mitigate | `z.string().max(64)` — 서버 WR-09 상한과 같은 값으로 relay 에서 먼저 자른다. 로그 출력도 절단 |
| T-16-09 | Information Disclosure | `parseInbound` 로그 | mitigate | 실패 로그에 `path`/`code` 만 남기고 원문·바디를 싣지 않는다 — 테스트 케이스 ⑥ 으로 고정 |
| T-16-01 | Elevation of Privilege | `accountNo` 필드 | transfer | 이 층은 **형식만** 본다. 소유권 대조(`session.allowedAccounts`)는 16-07/16-08 핸들러의 책임 — 여기서 통과했다고 권한이 있는 것이 아님을 주석으로 명시 |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/shared build` exit 0
- `pnpm typecheck` exit 0
- `pnpm --filter @gh-radar/relay test` exit 0
</verification>

<success_criteria>
- shared 에 전략·주문 메시지 계약이 존재하고 bigint 가 0건
- relay 가 인바운드 6종을 zod 로 파싱하고 경계 위반을 거부
- 상단 주석의 D-08 서술이 D-02 로 교체됨
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-03-SUMMARY.md` when done
</output>
