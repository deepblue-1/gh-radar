---
phase: 16-trading-limit-chaser-vi-my-page
plan: 17
type: execute
wave: 14
depends_on: ["16-16"]
files_modified:
  - webapp/e2e/specs/a11y.spec.ts
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md
autonomous: false
requirements: [TRADE-01, TRADE-02, TRADE-03, NAV-01, MYPAGE-01]

must_haves:
  truths:
    - "신규 3표면(상따·VI·My page)이 axe 위반 0 으로 통과한다"
    - "전체 테스트(typecheck · 전 워크스페이스 단위 · E2E 전량)가 green 이다"
    - "relay·server·webapp 이 재배포되어 프로덕션에서 전략·주문 wss 경로가 살아 있다"
    - "server 리비전에서 RELAY_INTERNAL_URL·ORDER_TIMEOUT_MS·RELAY_ORDER_SECRET 바인딩이 사라졌다"
    - "실서버·실계좌 검증은 Manual-Only 로 남고 이 phase 는 mock 검증으로 종결한다 (Phase 15 D-27 · 15-20 A안 승계)"
  artifacts:
    - path: "webapp/e2e/specs/a11y.spec.ts"
      provides: "신규 3표면 a11y 검사"
      contains: "/trading/limit-chaser"
    - path: ".planning/STATE.md"
      provides: "Phase 16 완료 상태 기록"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 16 체크 + 진행표 갱신"
  key_links:
    - from: "scripts/deploy-relay.sh"
      to: "GCE VM radar-gw"
      via: "컨테이너 재배포 후 smoke"
      pattern: "deploy-relay"
---

<objective>
phase 를 닫는다: a11y 확장 → 전체 테스트 → **배포 3종** → smoke → 문서 갱신.

배포 순서(의존 방향대로): **relay → server → webapp(Vercel prebuilt)**. relay 가 먼저여야 브라우저가 새 프레임을 받을 상대가 있고, server 는 REST 제거분을 리비전에서 실제로 걷어내야 하며, webapp 은 마지막이다.

⚠ Vercel 은 `ignoreCommand` 때문에 push 가 skip 될 수 있다(자동 메모리 `reference_vercel_frontend_deploy`) — **저장소 루트에서 `vercel pull` → `vercel build` → `vercel deploy --prebuilt`** 로 수동 배포한다.
⚠ 실서버(10.41.1.120)·실계좌 접속은 **사용자 명시 지시가 있을 때만**(Phase 15 D-27). 기본 경로는 mock 검증이며, 실계좌 E2E 는 `16-VALIDATION.md` §Manual-Only 로 남긴다.

Purpose: 코드가 아니라 **운영 상태**를 phase 의 산출물로 만든다.
Output: a11y 확장 · 전체 green · 배포 3종 · smoke · STATE/ROADMAP/VALIDATION 갱신.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: a11y 확장 + 전체 테스트 green</name>
  <files>webapp/e2e/specs/a11y.spec.ts</files>
  <read_first>
    - `webapp/e2e/specs/a11y.spec.ts` — 기존 표면 목록과 `@axe-core/playwright` 사용 형태, 위반 필터링 규칙
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md` §접근성 라벨(561~582행) · §키보드 접근성(629~637행) · §색 비의존(664~675행)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md` §Per-Task Verification Map
  </read_first>
  <action>
`a11y.spec.ts` 에 신규 3표면을 추가한다: `/trading/limit-chaser/new` · `/trading/vi` · `/me`(로그인 + DMA 매핑 시드 상태). relay 픽스처가 필요하므로 **같은 serial 규약**(`mode:'serial'` + `withLocalRelay` 1회)을 지킨다.

axe 위반 0 외에 아래를 명시 단언한다(자동 검사로 안 잡히는 계약):
- 사이드바 `<nav aria-label="주 메뉴">` + 활성 항목 `aria-current="page"`
- 3단 전략 항목 원 아이콘 묶음이 `role="img"` + `aria-label` 1개(개별 원에 라벨 0)
- 스위치 3종 `aria-label`, 모바일 탭 `role="tablist"`/`aria-selected`, 비활성 pane `hidden`
- 데드라인 `role="progressbar"` + `aria-valuenow/min/max`
- 상태줄 `aria-live="polite"`, 더티 액션 바 `role="status"`, 반영 실패 `role="alert"`
- 호가 사다리가 **포커스 대상이 아님**(탭 순서에 호가 셀 0)
- `confirm_locked` 체크가 tab 순서에서 제외됨

그다음 전체 스위트를 돌린다: `pnpm typecheck && pnpm -r test && pnpm --filter gh-radar-webapp test:e2e`. E2E 는 단일 워커(16-02)라 순차 실행되며 예상 5~8분이다. 실패가 있으면 **고치고 다시 돌린다** — 배포 전에 red 를 남기지 않는다.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm -r test &amp;&amp; pnpm --filter gh-radar-webapp test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter gh-radar-webapp test:e2e` 전량 exit 0 (기존 18 spec + 신규 4 spec)
    - a11y spec 이 3표면을 포함하고 axe 위반 0
    - 위 7개 명시 단언이 각각 케이스로 존재
    - `pnpm -r test` exit 0
  </acceptance_criteria>
  <done>전체 테스트 green, a11y 3표면 통과.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 배포 3종 실행 승인 + smoke</name>
  <what-built>
    Phase 16 전 코드가 로컬 테스트(typecheck · 전 워크스페이스 단위 · E2E 전량)에서 green 이다.
    다음 배포가 필요하다:
    1. **relay** — `scripts/deploy-relay.sh` (GCE VM `radar-gw` 컨테이너 재배포). 전략 중계 + wss 주문 핸들러가 여기 있다.
    2. **server** — `scripts/deploy-server.sh` + Cloud Run 리비전에서 `--remove-env-vars=RELAY_INTERNAL_URL,ORDER_TIMEOUT_MS --remove-secrets=RELAY_ORDER_SECRET`. 스크립트 수정만으로는 기존 리비전 env 가 사라지지 않는다.
    3. **webapp** — 저장소 루트에서 `vercel pull` → `vercel build` → `vercel deploy --prebuilt`. (Vercel `ignoreCommand` 가 push 를 skip 한 전례가 있어 수동 배포한다.)
    인증: `GOOGLE_APPLICATION_CREDENTIALS=~/.config/gcloud/gh-radar-deployer.json`, `CLOUDSDK_CORE_PROJECT=gh-radar`.
    Secret Manager 의 `gh-radar-relay-order-secret` 은 relay 가 계속 쓰므로 **삭제하지 않는다**.
  </what-built>
  <how-to-verify>
    1. 배포를 **Claude 가 실행**해도 되는지 확인해 주세요(승인/보류/직접 실행).
    2. 승인 시 순서: relay → server → webapp.
    3. 배포 후 smoke:
       - `bash scripts/smoke-relay.sh` (또는 relay healthz) 통과
       - `bash scripts/smoke-server.sh` 전량 통과 + `GET /api/orders?date=오늘` 200
       - 프로덕션 웹앱에서 로그인 → 사이드바에 트레이딩/My page 표시 여부(DMA 매핑 유무에 따라 다름) → `/me` · `/trading/vi` · `/trading/limit-chaser/new` 200
       - Cloud Run 리비전 env 에 `RELAY_INTERNAL_URL`/`ORDER_TIMEOUT_MS`/`RELAY_ORDER_SECRET` 이 **없음** 확인
    4. **실서버(KB 게이트웨이)·실계좌 검증은 하지 않습니다** — `dma_credentials` 0행이고 Phase 15 D-27 상 사용자 명시 지시가 필요합니다. 필요하면 지금 알려 주세요.
  </how-to-verify>
  <resume-signal>「배포 승인」(Claude 가 실행) / 「직접 실행」(사용자가 실행 후 결과 공유) / 「보류」 중 하나를 알려주세요. 실계좌 검증을 원하시면 함께 적어 주세요.</resume-signal>
  <files>scripts/deploy-relay.sh, scripts/deploy-server.sh, scripts/smoke-relay.sh, scripts/smoke-server.sh</files>
  <read_first>
    - `scripts/deploy-relay.sh` — GCE VM `radar-gw` 컨테이너 재배포 절차·필요 env
    - `scripts/deploy-server.sh`(16-16 정리본) — Cloud Run 배포 인자
    - `scripts/smoke-relay.sh` · `scripts/smoke-server.sh` — 검증 항목 목록
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md` §Runtime State Inventory — 스크립트 수정과 리비전 상태가 별개라는 근거
  </read_first>
  <action>
사용자 승인을 받은 뒤에만 실행한다. 순서는 **relay → server → webapp** 이며 각 단계 실패 시 다음으로 넘어가지 않는다.
① `GOOGLE_APPLICATION_CREDENTIALS=~/.config/gcloud/gh-radar-deployer.json CLOUDSDK_CORE_PROJECT=gh-radar bash scripts/deploy-relay.sh` → `bash scripts/smoke-relay.sh`
② `bash scripts/deploy-server.sh` 실행 후, 같은 리비전에 `gcloud run services update gh-radar-server --remove-env-vars=RELAY_INTERNAL_URL,ORDER_TIMEOUT_MS --remove-secrets=RELAY_ORDER_SECRET` 를 적용한다(스크립트 수정만으로는 기존 리비전 env 가 사라지지 않는다) → `bash scripts/smoke-server.sh` + `GET /api/orders?date=오늘` 200
③ 저장소 루트에서 `vercel pull` → `vercel build` → `vercel deploy --prebuilt`(`ignoreCommand` 로 push 배포가 skip 된 전례 — 자동 메모리)
④ 프로덕션에서 `/me` · `/trading/vi` · `/trading/limit-chaser/new` 200 과 사이드바 트리 노출을 확인한다.
**Secret Manager 의 `gh-radar-relay-order-secret` 은 삭제하지 않는다**(relay 가 사용 중).
사용자가 「보류」를 택하면 배포를 건너뛰고 그 사실을 SUMMARY 와 STATE 에 남긴다.
  </action>
  <verify>
    <automated>bash scripts/smoke-server.sh</automated>
  </verify>
  <acceptance_criteria>
    - 사용자의 승인/보류 응답이 SUMMARY 에 인용된다
    - 승인 시: `smoke-relay.sh` · `smoke-server.sh` 전량 통과 출력이 SUMMARY 에 인용된다
    - 승인 시: `gcloud run services describe gh-radar-server` 출력에 `RELAY_INTERNAL_URL`/`ORDER_TIMEOUT_MS`/`RELAY_ORDER_SECRET` 이 0건
    - 승인 시: 프로덕션 3라우트가 200
    - 보류 시: 배포 미실행 사실이 STATE.md 에 기록된다
  </acceptance_criteria>
  <done>배포 3종 완료 + smoke green, 또는 사용자 「보류」 결정이 문서에 기록됨.</done>
</task>

<task type="auto">
  <name>Task 3: smoke 결과 반영 + STATE/ROADMAP/VALIDATION 갱신</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md</files>
  <read_first>
    - `.planning/STATE.md` — frontmatter(`progress`·`last_updated`·`stopped_at`) · `## Current Position` · `### Roadmap Evolution` · `## Performance Metrics` 표 형식
    - `.planning/ROADMAP.md` — Phase 16 항목(33행 요약 체크박스 · 597~605행 상세) · `## Progress` 표
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md` — Task ID 열 · Status 열 · `nyquist_compliant` frontmatter · Manual-Only 표
  </read_first>
  <action>
① `16-VALIDATION.md`: Per-Task Verification Map 의 `Task ID`/`Plan` 열을 실제 plan·task 로 채우고 Status 를 ✅/❌ 로 갱신한다. `wave_0_complete: true`, `nyquist_compliant: true`(모든 태스크가 `<automated>` 를 갖거나 Wave 0 로 커버됨) 로 frontmatter 를 바꾼다. §Manual-Only 표는 **그대로 유지**하고 「실서버·실계좌 검증 미실시(D-27, dma_credentials 0행)」를 한 줄 명시한다.

② `ROADMAP.md`: 33행 Phase 16 요약 항목을 `- [x]` 로 바꾸고 상태를 `(complete)` 로. 597~605행 상세의 `**Requirements**: TBD` 를 `TRADE-01, TRADE-02, TRADE-03, NAV-01, MYPAGE-01` 로, `**Plans:** 0 plans` 를 `**Plans:** 17 plans` 로 바꾸고 Plans 목록을 17줄(`- [x] 16-NN-PLAN.md — 한 줄 요약`)로 교체한다. `## Progress` 표에 `| 16. 트레이딩 메뉴(상따·VI) | 17/17 | Complete | 2026-09-08 |` 행을 추가한다. ⚠ **상단 요약 목록과 하단 상세·진행표 세 곳을 모두** 고친다(자동 메모리 `project_gsd_roadmap_append_worktree_restore` — 상단 목록 미갱신이 반복 함정).

③ `STATE.md`: frontmatter `progress.completed_phases`/`completed_plans`/`percent` 와 `last_updated`·`stopped_at`·`last_activity` 갱신. `## Current Position` 을 Phase 16 완료로 바꾸고, `### Roadmap Evolution` 에 Phase 16 완료 한 줄(핵심 사실: 전략 wss 중계 + 주문 REST→wss 이관 + 사이드바 트리 + My page, mock 검증 종결·실계좌 미검증)을 추가한다. `## Performance Metrics` 표에 Phase 16 plan 별 행을 추가한다.
   배포 결과(리비전 ID·이미지 태그·Vercel URL·smoke 결과)를 `### Phase 16 Production State` 섹션으로 남긴다 — Phase 10/13 선례 형식.

**커밋하지 않는다.** 커밋 메시지는 사용자에게 먼저 보여주고 확인받는다(프로젝트 규칙 PC-1).
  </action>
  <verify>
    <automated>grep -c "16-01-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -c "nyquist_compliant: true" .planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TRADE-01, TRADE-02, TRADE-03, NAV-01, MYPAGE-01" .planning/ROADMAP.md` >= 1
    - ROADMAP 의 Phase 16 요약 체크박스가 `- [x]` 이고 `## Progress` 표에 Phase 16 행이 있다
    - `grep -c "16-1[0-7]-PLAN.md" .planning/ROADMAP.md` >= 8 (Plans 목록 17줄)
    - `16-VALIDATION.md` 의 Task ID 열에 `TBD` 가 0건
    - STATE.md 에 `Phase 16 Production State` 섹션과 배포 리비전·smoke 결과가 있다
    - `git status` 에 커밋되지 않은 변경으로 남아 있다(자동 커밋 0)
  </acceptance_criteria>
  <done>문서 3종이 실제 결과로 갱신되고 커밋은 사용자 확인 대기 상태다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → GCP/Vercel 프로덕션 | 배포가 실제 사용자 트래픽 경로를 바꾼다 |
| Cloud Run 리비전 env | 스크립트 수정과 리비전 상태는 별개다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Information Disclosure | server 리비전의 `RELAY_ORDER_SECRET` | mitigate | 재배포 시 `--remove-secrets=RELAY_ORDER_SECRET` 로 바인딩 제거. Secret 자체는 relay 가 쓰므로 삭제 금지 — 지우면 relay 부팅 실패 |
| T-16-12 | Denial of Service | 배포 순서 | mitigate | relay → server → webapp 순. webapp 을 먼저 올리면 새 프레임을 받을 상대가 없어 사용자가 빈 화면을 본다 |
| T-16-13 | 운영 사고 | 실서버·실계좌 접속 | accept | 이 phase 는 mock 검증으로 종결한다(D-27 · 15-20 A안). 실계좌 E2E 는 사용자 명시 지시가 있을 때만 — VALIDATION §Manual-Only 에 기록 |
| T-16-09 | Information Disclosure | 배포 로그 | mitigate | 스크립트가 secret 값을 출력하지 않는지 확인(기존 규약 유지). 계좌번호·토큰이 smoke 출력에 남지 않게 한다 |
</threat_model>

<verification>
- `pnpm typecheck && pnpm -r test && pnpm --filter gh-radar-webapp test:e2e` 전량 green
- relay/server smoke 통과, 프로덕션 3라우트 200
- Cloud Run 리비전 env 에 제거 대상 3종 없음
</verification>

<success_criteria>
- 신규 3표면 a11y 위반 0
- 배포 3종 완료 + smoke green (또는 사용자 「보류」 결정 기록)
- STATE/ROADMAP/VALIDATION 갱신, 커밋은 사용자 확인 대기
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-17-SUMMARY.md` when done
</output>
