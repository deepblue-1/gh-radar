---
phase: 16-trading-limit-chaser-vi-my-page
plan: 45
type: execute
wave: 26
depends_on: []
gap_closure: true
files_modified:
  - scripts/deploy-relay.sh
  - scripts/smoke-relay.sh
autonomous: true
requirements: [TRADE-03]

must_haves:
  truths:
    - "`DMA_HOST` 주입 없이 relay 를 배포해도 **실행 중인 컨테이너의 값이 보존된다** — 프로덕션이 실 게이트웨이에서 로컬 mock 으로 조용히 강등되지 않는다 (갭 5)"
    - "명시 주입(`DMA_HOST=...`)이 있으면 **그것이 이긴다** — 보존은 기본값을 대체할 뿐 주입을 덮지 않는다"
    - "값이 바뀌는 배포는 **변경 전/후를 나란히** 출력한다 — 강등이든 승격이든 배포자가 그 자리에서 본다"
    - "저장소에 실서버 주소 리터럴을 **새로 추가하지 않는다** — D-27 의 취지(「실주소를 저장소에 박제하지 않는다」)는 그대로다"
    - "smoke `INV-9` 프로브의 판정 문자열이 파이프에서 잘리지 않는다 — FAIL 이 SKIP 으로 강등되는 경로가 사라진다 (R2-IN-05)"
  artifacts:
    - path: "scripts/deploy-relay.sh"
      provides: "배포 전 컨테이너 DMA_HOST 조회 → 기본값으로 사용 · 출처 표시 · 변경 전/후 출력"
      contains: "CURRENT_DMA_HOST"
    - path: "scripts/smoke-relay.sh"
      provides: "프로브가 stdout 쓰기 완료 후 종료"
      contains: "process.stdout.write"
  key_links:
    - from: "scripts/deploy-relay.sh 배포 전 조회"
      to: "docker inspect DMA_HOST"
      via: "배포 후 요약(:415-419)과 같은 명령을 배포 전에도 쓴다"
      pattern: "docker inspect"
    - from: "scripts/deploy-relay.sh DMA_HOST 결정"
      to: "REMOTE_HEAD 로 컨테이너에 전달되는 값"
      via: "명시 주입 > 현재 컨테이너 > 로컬 mock 순 우선"
      pattern: "DMA_HOST"
---

<objective>
**갭 5(프로덕션 회귀의 근본 원인)** 와 **R2-IN-05** 를 닫는다.

- **갭 5.** `scripts/deploy-relay.sh:95` 가 `DMA_HOST="${DMA_HOST:-127.0.0.1}"` 라서 **현재 컨테이너 값을 보존하지 않는다.** 주입 없이 배포하면 실 게이트웨이에 붙어 있던 프로덕션이 로컬 mock 으로 내려가고, 그 mock 은 VM 에 떠 있지도 않아 `connect ECONNREFUSED 127.0.0.1:9100` 이 된다. **이번 phase 에서 실제로 두 번 일어났다** — 16-26(`2cb5620`)·16-35(`c8aa7ae`). 두 executor 다 D-27(「실서버 주소는 명시 주입 전용」)을 **「주입하지 말라」**로 읽고 그 강등을 의도된 상태로 SUMMARY 에 기록했으며, 검증도 그 기록을 승계했다. 한 원인이 세 증상(`/healthz` 503 · `gh-radar-relay-down` 알림 · 웹앱 사이드바 「트레이딩」 그룹 미표시)으로 보였다.
  **D-27 의 취지는 「실주소를 저장소에 박제하지 않는다」이지 「배포마다 mock 으로 되돌린다」가 아니다.** 고치지 않으면 다음 배포에서 **또** 떨어진다 — 그리고 지금은 실계좌가 붙어 있다.
- **R2-IN-05.** `scripts/smoke-relay.sh` 의 INV-9 프로브 `finish()` 가 `console.log(verdict); process.exit(0);` 다(`:400-401`). Node 의 `process.stdout` 은 POSIX 에서 **파이프일 때 비동기**이고, 이 프로브의 stdout 은 `verdict="$(... node ...)"` 로 **항상 파이프**다. `process.exit()` 가 대기 중인 쓰기를 버릴 수 있다는 것이 Node 문서의 명시 경고다. 잘리면 `verdict=""` → 호출부 `case` 의 `*` 갈래 → **FAIL 이 SKIP 으로 강등**되는데, 그것이 정확히 T-16-57 이 막겠다고 선언한 결과다(rc 는 0 이라 `inconclusive` 덮어쓰기도 안 걸린다).

Purpose: 배포가 프로덕션 상태를 조용히 되돌리지 않게 하고, 판정이 조용히 사라지지 않게 한다.
Output: 보존형 `DMA_HOST` 해석 + 변경 전/후 출력 · 프로브 stdout 안전 종료.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION-R2.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW-R2.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md
</context>

<interfaces>
`scripts/deploy-relay.sh` — 이미 있는 것들(재발명하지 말 것):
- `VM=radar-gw` · `ZONE=asia-northeast3-a` · `CONTAINER=gh-radar-relay` (:~68-75) — **`DMA_HOST` 결정부(:95)보다 앞에 정의돼 있다.** 조회에 필요한 것이 전부 손에 있다
- `:95` `DMA_HOST="${DMA_HOST:-127.0.0.1}"` — 고칠 자리
- `:96-99` — `10.41.1.120` 일 때의 D-27 경고 2줄. **지우지 않는다**(안전장치이며, 리터럴 grep 0건은 만족 불가능한 승인 기준이다)
- `:113` `echo "✓ variables: mode=$MODE SHA=$SHA TARGET=$TARGET_IMAGE DMA_HOST=$DMA_HOST"`
- `:204-206` `REMOTE_HEAD=$(printf ... 'DMA_HOST=%q\n' ... "$DMA_HOST" ...)` — 실제 전달 경로
- `:415-419` **배포 후** 실컨테이너 값 되읽기:
  `gcloud compute ssh "$VM" --zone="$ZONE" --tunnel-through-iap --command "sudo docker inspect $CONTAINER --format '{{range .Config.Env}}{{println .}}{{end}}' | sed -n 's/^DMA_HOST=//p'" 2>/dev/null | tr -d '\r' | tail -1`
  → **배포 전 조회는 이 명령을 그대로 쓴다.** 두 벌로 적지 말고 함수로 뽑는다
- `:419-424` — mock 이면 경고 + 「실서버로 붙이려면 …」 안내를 출력하는 기존 분기
- `--rollback` 모드 (`MODE=rollback`, :~100-115) — 이 경로도 같은 해석을 지나야 한다

`scripts/smoke-relay.sh`:
- `ws_order_probe()` (:335~) — `token="${SMOKE_AUTH_TOKEN:-}"`, 임시 JS 파일 작성, `verdict="$(SMOKE_TOKEN="$token" node "$js" ...)"` (:469)
- 프로브 JS 의 `finish(verdict, why)` (:389-401) — `console.error` 로 사유, `console.log(verdict)` + `process.exit(0)`
- 호출부 (:657-670) — `SMOKE_AUTH_TOKEN` 없으면 `skip`(프로브 본체는 한 줄도 실행되지 않는다), 있으면 `case` 로 판정
- ⚠️ **이 plan 은 smoke 를 실행하지 않는다.** 실행에는 GCP 환경·로그인 토큰이 필요하고, 그것은 종결 plan(16-46)의 몫이다
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 배포가 현재 DMA_HOST 를 보존한다 (갭 5)</name>
  <files>scripts/deploy-relay.sh</files>
  <read_first>
    - `scripts/deploy-relay.sh` **전문**(약 430줄). 특히 헤더 주석 §선택 env(:23), Section 2 변수부, `DMA_HOST` 결정부와 D-27 경고, `REMOTE_HEAD` 조립부, 배포 후 요약의 `LIVE_DMA_HOST` 되읽기, `--rollback` 분기. **한 번에 읽는다**
    - `.planning/STATE.md` §「DMA_HOST 배포 회귀 — 발견·수정」 — 무엇이 어떻게 일어났고 무엇이 오진이었는지
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION-R2.md` §부록 A·B — 전제 정정과 결선 실측
    - `relay/src/config.ts` — `DMA_HOST` 기본값과 그 근거 주석(스크립트와 같은 논리인지 확인)
  </read_first>
  <action>
① **조회를 함수로 뽑는다.** 배포 후 요약(:415-419)이 쓰는 `docker inspect` 명령을 `read_live_dma_host()` 같은 함수로 만들고, 배포 **전**과 **후**가 같은 함수를 쓴다. 두 벌로 적으면 언젠가 한쪽만 고쳐진다.
   - 실패(VM 접근 불가·컨테이너 부재·최초 배포)는 **정상 경로**다. `2>/dev/null` + 빈 문자열 반환으로 처리하고 **배포를 중단시키지 않는다**(`set -e` 아래이므로 `|| true` 가 필요하다). 실패 시 출력에 「현재 값 확인 실패 — 기본값을 쓴다」를 남긴다

② `DMA_HOST` 해석을 **3단 우선순위**로 바꾼다: **명시 주입 > 실행 중인 컨테이너 값 > 로컬 mock(`127.0.0.1`)**. 주입 여부를 판정하려면 `${DMA_HOST:-}` 를 **덮어쓰기 전에** 기억해 두어야 한다(`DMA_HOST_INJECTED="${DMA_HOST:-}"` 같은 형태).

③ **출처를 출력한다.** 최종 값과 함께 어디서 왔는지(`명시 주입` / `실행 중 컨테이너 보존` / `기본값(로컬 mock)`)를 `:113` 의 variables 줄 근처에 찍는다.

④ **변경 전/후를 나란히 출력한다.** 현재 컨테이너 값과 이번에 들어갈 값이 **다르면** 눈에 띄는 경고(`⚠`)와 함께 `이전 → 이후` 를 한 줄로 찍는다. 강등(실주소 → `127.0.0.1`)이든 승격이든 배포자가 그 자리에서 본다. 같으면 조용히 한 줄만 남긴다.

⑤ **D-27 안전장치는 그대로 둔다.** `:96-99` 의 경고 2줄을 지우지 않는다. 헤더 주석 §선택 env(:23)의 `DMA_HOST` 설명을 **새 동작에 맞게** 고친다 — 「미설정 시 127.0.0.1」은 이제 거짓이다. 새 문장에 D-27 의 취지(「실주소를 저장소에 박제하지 않는다」)와 이번 변경의 취지(「배포가 현재 상태를 되돌리지 않는다」)가 **다른 것**임을 적는다. 이 오독이 두 번의 강등을 만들었다.

⑥ **저장소에 실서버 주소 리터럴을 새로 추가하지 않는다.** 보존은 런타임 조회로만 이루어진다. 기존 리터럴(경고 조건 `:96`, 안내 문구 `:421`)은 유지한다.

⑦ `--rollback` 경로도 같은 해석을 지나는지 확인한다. `MODE` 분기가 `DMA_HOST` 결정 뒤에 오는지 앞에 오는지 코드로 확인하고, 필요하면 순서를 조정한다.

⑧ **논리를 격리해 검증할 수 있게 둔다.** 우선순위 결정을 한 줄(또는 작은 함수)로 모아 두어, 아래 승인 기준의 스니펫 검증이 스크립트 원문과 **문자 그대로 대조**될 수 있게 한다.
  </action>
  <verify>
    <automated>bash -n scripts/deploy-relay.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/deploy-relay.sh` exit 0
    - `grep -c '\${DMA_HOST:-127\.0\.0\.1}' scripts/deploy-relay.sh` == 0 (옛 무조건 기본값이 사라졌다)
    - `grep -c 'CURRENT_DMA_HOST\|read_live_dma_host' scripts/deploy-relay.sh` >= 3
    - `docker inspect` 조회가 **1곳에 정의**되고 배포 전/후 **2곳에서 호출**된다 (함수명과 호출 지점 SUMMARY 인용)
    - **우선순위 3케이스를 실제로 실행해 증명한다.** 스크립트의 해석 줄과 문자 그대로 같은 스니펫으로:
      `주입="1.2.3.4" 현재="9.9.9.9" → 1.2.3.4` / `주입="" 현재="9.9.9.9" → 9.9.9.9` / `주입="" 현재="" → 127.0.0.1`
      세 결과를 SUMMARY 에 출력 그대로 인용하고, 스니펫이 원문과 동일함을 인용으로 보인다
    - ⚠️ **`grep "10.41.1.120"` 0건을 승인 기준으로 쓰지 않는다.** 이 조건은 만족 불가능하며 2라운드 plan 6건이 6회 연속 같은 불일치를 관측했다. 대신 **개수가 늘지 않았음**을 쓴다: `grep -c '10\.41\.1\.120' scripts/deploy-relay.sh` 의 전/후 값이 같다 (전 값을 먼저 측정해 SUMMARY 에 인용)
    - 헤더 주석 §선택 env 의 `DMA_HOST` 설명이 새 동작을 반영한다 (소스 인용)
    - `:96-99` D-27 경고 2줄 diff 0줄
    - 이 plan 은 **실제 배포를 하지 않는다** — `gcloud` 를 부르는 실행이 없었음을 SUMMARY 에 명시
  </acceptance_criteria>
  <done>주입 없이 배포해도 실 게이트웨이가 유지되고, 값이 바뀌면 배포자가 그 자리에서 본다.</done>
</task>

<task type="auto">
  <name>Task 2: 프로브 판정이 파이프에서 잘리지 않는다 (R2-IN-05)</name>
  <files>scripts/smoke-relay.sh</files>
  <read_first>
    - `scripts/smoke-relay.sh` — 헤더 주석 §INV-9 설명(:20-24), `ws_order_probe()` 전문(:335-475, 임시 JS 작성 + 호출 + `case` 판정), 특히 `finish()`(:389-401) 와 호출부(:657-670). **한 번에 읽는다**
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md` §16-35 「INV-9 재실행 명령」 — 이 프로브가 **16-21 재작성 이후 한 번도 실행된 적이 없다**는 사실(「돌렸는데 SKIP」이 아니다)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-30-SUMMARY.md` — GC-WR-11 이 이 프로브를 어떻게 고쳤는지(argv → env, 판정 이어붙임 제거)
  </read_first>
  <action>
① `finish()` 의 종료를 안전하게 바꾼다. `process.stdout.write(verdict + "\n", () => process.exit(0));` 또는 `process.exitCode = 0` 만 설정하고 자연 종료에 맡긴다(타이머는 이미 전부 `clearTimeout` 된다 — **확인하고** 자연 종료를 택했다면 그 근거를 주석에 적는다).

② **왜 이 한 줄이 필요한가**를 프로브 주석에 남긴다: 이 프로브의 stdout 은 명령 치환(`verdict="$(...)"`)이라 **항상 파이프**이고, POSIX 파이프에서 `process.stdout` 은 비동기다. `process.exit()` 는 대기 중인 쓰기를 버릴 수 있다(Node 문서 명시 경고). 잘리면 `verdict=""` → 호출부 `case` 의 `*` 갈래 → **FAIL 이 SKIP 으로 강등**된다 — T-16-57 이 막겠다고 선언한 바로 그 결과이고, rc 가 0 이라 `inconclusive` 덮어쓰기도 걸리지 않는다.

③ 호출부의 `case` 에 **빈 verdict 방어**가 있는지 확인한다. 없으면 「빈 판정은 SKIP 이 아니라 FAIL(또는 inconclusive)」로 명시 처리한다 — 잘림을 근본에서 막더라도, 판정이 비었을 때 조용히 초록이 되는 경로는 남기지 않는다(이중 방어). 기존 갈래를 바꾼다면 무엇을 왜 바꿨는지 SUMMARY 에 적는다.

④ `console.error` 로 사유를 찍는 부분은 그대로 둔다(stderr 는 명령 치환에 잡히지 않는다).

⑤ **이 plan 에서 smoke 를 실행하지 않는다.** GCP 환경과 `SMOKE_AUTH_TOKEN`(브라우저 로그인 access_token, 약 1시간 만료)이 필요하고, 저장소 어디에도 값이 없는 것이 정상이다(T-16-74). 실행은 종결 plan(16-46)의 몫이다.

⑥ 대신 **프로브 JS 를 문법 검사한다.** 임시 파일로 추출하는 경로가 스크립트 안에 있으므로, 같은 방식으로 JS 본문만 뽑아 `node --check` 를 돌린다. 추출 방법과 결과를 SUMMARY 에 적는다.
  </action>
  <verify>
    <automated>bash -n scripts/smoke-relay.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/smoke-relay.sh` exit 0
    - 프로브 JS 를 추출해 `node --check` 가 exit 0 (추출 명령과 결과 SUMMARY 인용)
    - `grep -c 'console\.log(verdict)' scripts/smoke-relay.sh` == 0 (옛 형태가 사라졌다)
    - `grep -c 'process\.stdout\.write' scripts/smoke-relay.sh` >= 1 **또는** `process.exitCode` 방식 채택 근거가 주석·SUMMARY 에 있다
    - 호출부 `case` 의 빈 verdict 처리가 SKIP 이 아님을 소스로 보인다 (인용)
    - smoke 를 **실행하지 않았음**을 SUMMARY 에 명시 (「돌렸는데 SKIP」과 「못 돌렸다」를 섞지 않는다)
  </acceptance_criteria>
  <done>판정 문자열이 잘려도 조용히 초록이 되지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 배포 스크립트 ↔ 프로덕션 런타임 상태 | 스크립트의 기본값이 실행 중 상태를 덮으면 배포가 **상태를 되돌리는 행위**가 된다. 배포자는 그것을 의도하지 않았다 |
| D-27(저장소에 실주소 미박제) ↔ 배포 시 실주소 사용 | 두 문장은 다르다. 이 구분이 흐려져 두 번의 강등이 일어났다 |
| 프로브 판정 문자열 ↔ 스모크 결과 | 판정이 유실되면 실패가 SKIP 으로 강등된다. 검증 체계 자체의 신뢰가 걸린다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-94 | Denial of Service (조용한 강등) | `deploy-relay.sh` `DMA_HOST` 기본값 | mitigate | 실행 중 값을 기본값으로 삼고, 변경 시 전/후를 나란히 출력 |
| T-16-95 | Tampering (의도치 않은 구성 변경) | 배포 파라미터 우선순위 | mitigate | 명시 주입 > 보존 > mock 3단을 스니펫 3케이스로 실증 |
| T-15-30 / D-27 | Information Disclosure | 실서버 주소 저장 | accept | 런타임 조회만 쓴다. 리터럴 개수가 늘지 않음을 승인 기준으로 |
| T-16-57 | Repudiation (판정 유실) | `smoke-relay.sh` 프로브 종료 | mitigate | 쓰기 완료 후 종료 + 빈 verdict 를 SKIP 으로 두지 않는 이중 방어 |
| T-16-74 | Information Disclosure | `SMOKE_AUTH_TOKEN` | accept | 저장소에 기록하지 않는다. 이 plan 은 smoke 를 실행하지 않는다 |
| T-16-13 | 운영 사고 | 실제 배포 | accept | 이 plan 은 배포하지 않는다 — 종결 plan(16-46) 소관 |
</threat_model>

<verification>
- `bash -n scripts/deploy-relay.sh` · `bash -n scripts/smoke-relay.sh` exit 0
- 프로브 JS `node --check` exit 0
- 우선순위 3케이스 스니펫 실행 결과가 기대와 일치
- 실제 배포·smoke 실행은 **하지 않는다** (종결 plan 소관)
- `grep "10.41.1.120"` **0건**은 승인 기준으로 쓰지 않는다 — 정본 계약은 리터럴 0건이 아니라 「접속 경로 0건」이다
</verification>

<success_criteria>
- 주입 없는 배포가 실 게이트웨이를 보존한다
- 명시 주입이 보존을 이긴다
- 값이 바뀌면 전/후가 함께 출력된다
- 저장소에 실주소 리터럴이 늘지 않았다
- 프로브 판정이 잘려도 초록이 되지 않는다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-45-SUMMARY.md` when done
</output>
