---
phase: 16-trading-limit-chaser-vi-my-page
plan: 39
type: execute
wave: 24
depends_on: ["16-38"]
gap_closure: true
files_modified:
  - relay/src/store/orders.ts
  - relay/tests/order-store.test.ts
autonomous: true
requirements: [TRADE-03]

must_haves:
  truths:
    - "`order_no` 갱신이 `23505` 로 거부돼도 **같은 갱신에 실린 나머지 필드**(`status`·`filled_qty`·`result_code` 등)는 행에 반영된다 — 수동 주문 행이 `requested`·`filled_qty:0` 인 채 영구히 남지 않는다 (R2-WR-01)"
    - "`flushed` 카운터가 실제로 반영된 갱신만 센다 — 「반영된 누적 건수」가 반영되지 않은 갱신을 세지 않는다 (S-5)"
    - "포기하는 것은 **`order_no` 컬럼 하나**이고, 그 포기가 로그에 사유와 함께 남는다 (16-28 이 감사 기록 분기라고 인정한 대가는 그대로)"
    - "재시도도 실패하면 그것은 「이유를 모르는 실패」이므로 throw 되어 큐 재시도 규율을 탄다"
  artifacts:
    - path: "relay/src/store/orders.ts"
      provides: "23505 시 order_no 만 제외하고 1회 재시도하는 update sink"
      contains: "23505"
    - path: "relay/tests/order-store.test.ts"
      provides: "행 상태가 실제로 갱신됐음 + flushed 정합을 단언하는 케이스 ⓽"
      contains: "flushed"
  key_links:
    - from: "relay/src/store/orders.ts supabaseOrderSink 23505 분기"
      to: "같은 셀렉터의 재시도 update"
      via: "order_no 만 뺀 patch"
      pattern: "order_no"
    - from: "relay/tests/order-store.test.ts fakeDmaOrders"
      to: "rows"
      via: "성공한 update 가 실제로 행에 반영된다(가짜 테이블이 patch 를 적용한다)"
      pattern: "patch"
---

<objective>
**R2-WR-01 + R2-IN-04** 를 닫는다. `23505` 갱신 예외 처리가 **패치 전체를 버린다** — 충돌한 컬럼(`order_no`)만이 아니라 수명주기 필드 전부가 사라지고, 그 실패가 `flushed` 로 집계된다.

지금 상태:
- `finish`(`relay/src/ws/order-handler.ts:869-878`)가 내는 갱신은 `{orderRowId, orderNo, status, resultCode, noticeType, message, filledQty, origin}` **한 덩어리**다.
- 그 갱신이 `23505` 로 거부되면 `supabaseOrderSink`(`relay/src/store/orders.ts:284-291`)는 사유를 로그하고 **`return`** 한다. 결과: 수동 주문의 행이 `status:'requested'` · `filled_qty:0` 인 채 **영구히** 남는다. 16-28 의 주석이 인정한 대가는 「주문번호를 못 채운다」였지 「자기 행의 상태가 갱신되지 않는다」가 아니다.
- 게다가 `#drain`(`:616-618`)은 sink 가 throw 하지 않았으므로 `#flushed += 1` 을 올린다 — 「반영된 누적 건수」가 **반영되지 않은 갱신**을 센다.
- 기존 테스트 ⓽ 는 `dropped`/`retried`/`queued`/`queries.length` 만 단언하고 `flushed` 와 「행 상태가 실제로 갱신됐는가」를 보지 않아 **이 손실을 「진실」로 잠근 상태**다(R2-IN-04).

**16-28 이 얻은 것을 잃지 않는다.** ① `23505` 는 재시도해도 영원히 같은 결과이므로 큐 재시도를 태우지 않는다 ② `dropped` 를 올려 「이유를 아는 실패」와 「이유를 모르는 실패」를 섞지 않는다 ③ 조건을 셀렉터가 아니라 **patch** 에 건다. 셋 다 유지된다 — 바뀌는 것은 「포기의 단위」가 **패치 전체에서 컬럼 하나로** 줄어드는 것뿐이다.

Purpose: 감사 기록의 결손을 컬럼 하나로 좁히고, 카운터가 참말을 하게 한다.
Output: `order_no` 만 뺀 1회 재시도 · `flushed` 정합 · 「행이 실제로 갱신됐다」를 보는 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW-R2.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-28-SUMMARY.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-38-SUMMARY.md
</context>

<interfaces>
<!-- 16-38 이 같은 파일을 먼저 손댔다. 그 SUMMARY 를 읽고 줄번호를 다시 잡는다. -->

`relay/src/store/orders.ts`:
- `export type OrderSelector = { column: "id"; value: string } | { column: "order_no"; value: string; userId: string }` — 판별 유니온. `order_no` 셀렉터는 **`user_id` + 당일 범위 3축**으로 좁힌다(gap 1 / T-16-14)
- `export type OrderRowPatch = Record<string, unknown>`
- `supabaseOrderSink(supabase): OrderUpdateSink` — 셀렉터에 따라 두 갈래로 `table.update(patch)` 를 만든다. **재시도는 그 갈래를 다시 조립해야 한다** — 손으로 두 번 적으면 한쪽만 3축이 되어 전역 쓰기가 열린다
- `kstDayRangeUtc()` — 당일 범위
- `enqueueUpdate` (:512~) — `patch` 는 언제나 `updated_at` + 실제 필드 ≥ 1 이다(`Object.keys(patch).length === 1` 이면 큐에 넣지 않는다). 즉 **`order_no` 를 빼고 남는 것이 `updated_at` 뿐인 경우**만 「보낼 것이 없다」다
- `OrderStore.#drain` (:~605-645) — 성공 시 `#flushed += 1`, 실패 시 재큐잉 1회 → `#dropped`
- `safePgError` (16-38 이 도입) — 재시도 실패 로그에 쓴다

`relay/tests/order-store.test.ts`:
- `fakeDmaOrders(rows, opts)` (:455~) — `update` 는 `dupToday()` 로 23505 를 **실제로 계산**한다. 다만 성공한 update 가 `rows` 를 **갱신하지 않는다**(`{ data: null, error: null }` 만 돌려준다) — Task 2 에서 이것을 고쳐야 「행이 실제로 갱신됐다」를 단언할 수 있다
- ⓽ (:720-742) — 고칠 케이스
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 포기의 단위를 패치 전체에서 컬럼 하나로 줄인다 (R2-WR-01)</name>
  <files>relay/src/store/orders.ts</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-38-SUMMARY.md` — **먼저 읽는다.** 같은 파일의 최신 상태(`safePgError` 교체 지점)
    - `relay/src/store/orders.ts` `supabaseOrderSink` 전문 — 두 셀렉터 갈래의 조립 방식과 23505 분기 주석 전문
    - `relay/src/ws/order-handler.ts` `finish` (:~860-885) — 이 갱신에 실제로 어떤 필드가 함께 실리는지. 「무엇을 잃고 있었는지」의 근거
    - `relay/src/store/orders.ts` `enqueueUpdate` — `updated_at` 이 항상 실린다는 사실과 「갱신할 필드가 없는 요청」 판정
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW-R2.md` §R2-WR-01 — Fix 스니펫(방향이지 정답이 아니다. 코드에서 검증할 것)
  </read_first>
  <action>
① **update 실행부를 함수로 뽑는다.** 지금은 셀렉터 갈래(`id` 한 축 / `order_no` 3축)가 인라인 삼항식 안에 있어 재시도가 그것을 복제하게 된다. `(sel, patch) => Promise<{error}>` 형태의 지역 헬퍼로 **한 번만** 조립하고 최초 시도와 재시도가 **같은 함수**를 쓰게 한다. 복제하면 언젠가 한쪽이 3축을 잃고, 그것이 곧 전역 쓰기다(T-16-14).

② `23505` 분기를 다음으로 바꾼다:
   - `patch` 에서 `order_no` 를 뺀 `rest` 를 만든다
   - `Object.keys(rest).length <= 1`(= `updated_at` 뿐)이면 보낼 것이 없다 → 지금처럼 `return`. 이 경우가 무엇인지(주문번호만 채우는 갱신) 주석에 적는다
   - 그렇지 않으면 **같은 셀렉터로 `rest` 를 한 번 더 보낸다**
   - 재시도가 성공하면 `return` — 큐 재시도를 태우지 않는다는 16-28 의 규율은 그대로다
   - 재시도가 실패하면 `logger.error({ error: safePgError(retryError), column: sel.column }, ...)` 후 **throw** 한다. 이것은 「이유를 아는 실패」가 아니므로 큐의 재시도·드롭 규율을 타야 한다

③ 로그를 두 갈래로 가른다. 지금 한 줄이 「주문번호를 못 채웠다」와 「갱신 전체를 버렸다」를 함께 뜻했다. 이제:
   - 재시도 성공: 「`order_no` 만 포기하고 나머지는 반영했다」 — `logger.error` 로 남긴다(감사 결손이 실재하므로 warn 으로 낮추지 않는다). 페이로드는 `{ column, code: "23505" }` 수준 — **주문번호 원문을 싣지 않는다**(T-16-45, 16-28 이 이 분기에서 명시적으로 뺀 이유)
   - 「보낼 것이 없다」: 별도 메시지
④ 주석을 갱신한다. 16-28 의 근거 3줄(재시도 무의미 / `dropped` 오염 방지 / 조건을 patch 에 건다)은 **인용하며 유지**하고, 그 아래에 「그러나 포기의 단위는 컬럼 하나여야 한다 — 수명주기 필드까지 버리면 행이 `requested` 로 영구히 남는다 (R2-WR-01)」를 잇는다.

⑤ `#drain`·카운터 코드는 **건드리지 않는다.** 재시도가 성공하면 sink 가 정상 반환하므로 `flushed += 1` 은 이제 참말이다 — 카운터를 고치는 것이 아니라 **카운터가 참이 되게 동작을 고치는** 것이 이 수정의 형태다. 그 논리를 SUMMARY 에 적는다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 셀렉터 조립이 **한 곳**에만 있다: `grep -c 'eq("order_no"' relay/src/store/orders.ts` 값이 변경 전과 같거나 줄었다 (전/후 값 SUMMARY 인용). 늘었다면 복제한 것이다
    - 23505 분기에 재시도 호출이 있고, 그 인자가 `order_no` 를 제외한 patch 다 (소스 인용)
    - 재시도 실패 경로가 `throw` 한다 (소스 인용)
    - 23505 관련 로그 페이로드에 `order_no` 값·주문번호 원문이 실리지 않는다 (소스 인용)
    - `#drain` 의 `#flushed`/`#retried`/`#dropped` 대입문 diff 0줄 (`git diff` 인용)
    - `pnpm --filter @gh-radar/relay run typecheck` exit 0 · `run typecheck:tests` exit 0
  </acceptance_criteria>
  <done>포기하는 것은 컬럼 하나다. 나머지는 반영되고, 재시도 실패는 정상적으로 큐 규율을 탄다.</done>
</task>

<task type="auto">
  <name>Task 2: 가짜 테이블이 갱신을 실제로 반영하게 하고, ⓽ 가 그것을 본다 (R2-IN-04)</name>
  <files>relay/tests/order-store.test.ts</files>
  <read_first>
    - `relay/tests/order-store.test.ts` — `FakeRow`·`FakeQuery` 타입, `fakeDmaOrders` 전문(:455-545, 특히 `self.then` 안의 update 처리), ⓻·⓼·⓽(:686-742). **한 번에 읽는다**
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-38-SUMMARY.md` — 16-38 이 이 테스트 파일에 무엇을 추가했는지(주입 훅 확장 포함)
  </read_first>
  <action>
① **가짜 테이블을 진짜에 가깝게 만든다.** 지금 `self.then` 의 update 갈래는 `{ data: null, error: null }` 만 돌려주고 `rows` 를 건드리지 않는다. 성공한 update 가 `query.matched` 행들에 `patch` 를 **실제로 병합**하도록 고친다. 이 변화가 기존 케이스를 깨뜨리는지 먼저 전량 실행해 확인하고, 깨진다면 **그 단언이 옛 스텁 동작을 베낀 것인지** 판정한 뒤 처리한다(이 phase 가 3+1건 겪은 함정이다 — 깨졌다고 곧 회귀가 아니다). 판정 결과를 SUMMARY 에 적는다.

② ⓽ 를 다시 쓴다. 제목을 「`order_no` 갱신의 23505 는 **주문번호만 포기하고 나머지는 반영한다**」로 바꾸고 다음을 단언한다:
   - `rows` 에서 `row-manual` 을 찾아 `status === "accepted"` 가 **실제로 반영**됐다
   - `order_no` 는 여전히 비어 있다 (포기한 컬럼)
   - `store.stats().flushed === 1` — 이제 참말이다
   - `dropped === 0` · `retried === 0` · `queued === 0` (기존 규율 유지)
   - `queries` 는 update **2건**이다(최초 + 재시도). 기존의 `toHaveLength(1)` 단언은 **거짓이 됐으므로** 고친다 — 무엇이 왜 바뀌었는지 케이스 주석에 남긴다

③ 새 케이스 「`order_no` 만 담긴 갱신의 23505 는 재시도하지 않는다」: patch 에 `orderNo` 외 실필드가 없는 경우(=`updated_at` 뿐)를 만들고 `queries` 가 1건임을 단언한다. `enqueueUpdate` 가 그런 요청을 큐에 넣지 않을 수 있으므로, 그렇다면 sink 를 직접 호출해 단언한다(그 선택과 이유를 주석에 적는다).

④ 새 케이스 「재시도도 실패하면 throw 되어 큐 재시도 규율을 탄다」: 재시도 update 가 다른 코드(예: `23514`)로 실패하도록 주입하고, `retried`(또는 `dropped`)가 올라가는 것을 단언한다. `safePgError` 덕에 로그에 `details` 가 없음도 함께 단언하면 16-38 의 회귀 게이트가 된다.

⑤ **회귀 잠금을 실증한다**: Task 1 의 재시도를 임시로 제거(= 옛 `return`)한 뒤 ⓽ 가 실제로 실패하는지 확인하고 복원한다. 실패 건수를 숫자로 SUMMARY 에 적는다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay exec vitest run tests/order-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f relay/tests/order-store.test.ts` 가 참임을 먼저 확인한 뒤 위 명령을 돌린다
    - `fakeDmaOrders` 의 update 갈래가 `rows` 에 patch 를 병합한다 (소스 인용)
    - ⓽ 가 `expect(store.stats().flushed)` 와 「`row-manual` 의 `status` 가 `accepted` 다」를 **둘 다** 단언한다 (소스 인용)
    - ⓽ 가 update 2건을 단언한다 (최초 + 재시도)
    - 「보낼 것이 없다」 케이스와 「재시도 실패 → throw」 케이스가 존재한다
    - 회귀 잠금 실증: 재시도 제거 시 실패 ≥ 1 건 관측 후 복원 (숫자 인용)
    - `pnpm --filter @gh-radar/relay test` exit 0 · `run typecheck:tests` exit 0
    - relay 테스트 총 건수 전/후 인용
  </acceptance_criteria>
  <done>테스트가 「행이 실제로 갱신됐는가」를 본다. 옛 손실을 진실로 잠그던 단언이 사라졌다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay → `dma_orders` (감사 기록) | 이 테이블은 「주문이 실제로 어떻게 됐는가」의 유일한 사후 근거다. 결손은 조용히 생긴다 |
| 카운터 → 운영자 판단 | `flushed` 가 거짓이면 결손이 있는데 없다고 보고하는 계기판이 된다 (S-5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-79 | Repudiation (감사 결손) | `supabaseOrderSink` 23505 분기 | mitigate | `order_no` 만 빼고 1회 재시도. ⓽ 가 「status 가 실제로 반영됐다」로 잠근다 |
| T-16-80 | Repudiation (계기판 거짓) | `#flushed` 집계 | mitigate | 동작을 고쳐 카운터가 참이 되게 한다. `flushed === 1` 단언 추가 |
| T-16-14 | Tampering (전역 쓰기) | 재시도의 셀렉터 조립 | mitigate | 조립을 지역 헬퍼 한 곳으로 모으고, `eq("order_no"` 등장 횟수를 승인 기준으로 둔다 |
| T-16-45 | Information Disclosure | 23505 로그 페이로드 | mitigate | 주문번호 원문을 싣지 않는다. 재시도 실패 로그는 `safePgError`(16-38) |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/relay test` exit 0
- `pnpm --filter @gh-radar/relay run typecheck` · `run typecheck:tests` exit 0
- 포매터를 돌리지 않는다
</verification>

<success_criteria>
- 23505 시 수명주기 필드가 행에 반영된다
- `flushed` 가 참말을 한다
- 재시도 실패는 throw 되어 큐 규율을 탄다
- 재시도를 제거하면 테스트가 빨개진다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-39-SUMMARY.md` when done
</output>
