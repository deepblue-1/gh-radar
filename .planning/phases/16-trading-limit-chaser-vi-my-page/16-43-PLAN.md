---
phase: 16-trading-limit-chaser-vi-my-page
plan: 43
type: execute
wave: 26
depends_on: ["16-38", "16-40"]
gap_closure: true
files_modified:
  - relay/src/ws/order-handler.ts
  - relay/tests/ws-order.test.ts
autonomous: true
requirements: [TRADE-03]

must_haves:
  truths:
    - "브로커 주문번호의 **표기 차이**(선행 0·공백)가 정산을 막지 않는다 — 실제로 접수·확인된 취소가 감사 기록에 `timeout` 으로 남지 않는다 (R2-WR-03①)"
    - "강한 축이 후보를 **전부** 지운 상황은 일반 미매칭과 구분되는 로그로 남는다 — 어느 축이 원인인지 알 수 있다 (R2-WR-03②)"
    - "통보 종류 축은 **아는 값만** 쓴다 — 장래에 추가될 통보 종류가 자동으로 「신규」로 단정되지 않는다 (R2-WR-03③ / R2-IN-03)"
    - "감사 행의 `side` 가 모르는 매매구분을 「매수」로 지어내지 않는다 — 같은 파일이 매칭 축에서 쓰는 `fromWireSide` 규율과 일치한다 (R2-WR-06)"
  artifacts:
    - path: "relay/src/ws/order-handler.ts"
      provides: "주문번호 비교 정규화 + 강한 축 전멸 로그 + noticeType 화이트리스트 + sideOf 의 fromWireSide 통일"
      contains: "fromWireSide"
    - path: "relay/tests/ws-order.test.ts"
      provides: "표기 차이 정산 · 축 전멸 로그 · 미지 noticeType · 빈 side 기록 케이스"
      contains: "orgOrderNo"
  key_links:
    - from: "relay/src/ws/order-handler.ts narrowPending"
      to: "요청측 orgOrderNo / 통보측 orgOrderNo"
      via: "같은 정규화 함수를 양쪽에 건다"
      pattern: "orgOrderNo"
    - from: "relay/src/ws/order-handler.ts sideOf"
      to: "relay/src/dma/envelope.ts fromWireSide"
      via: "모르는 값을 지어내지 않는 규율의 단일 정본"
      pattern: "fromWireSide"
---

<objective>
`order-handler.ts` 의 **매칭·기록 축 3건**을 닫는다. 셋 다 「relay 가 이미 아는 것을 잘못 단정하거나, 표기 차이 하나로 진실을 잃는」 자리다.

- **R2-WR-03.** GC-CR-01(16-27)이 ①·② 축을 `refine`(0건이면 축을 건너뜀)에서 **하드 필터**(0건이면 `null`)로 승격했다. 의도(취소확인이 살아 있는 신규 주문을 정산하지 않게)는 옳다. 그러나 부수효과가 생겼다 — **게이트웨이가 실어 보낸 `orgOrderNo` 가 요청에 담은 값과 한 글자라도 다르면 그 취소는 영영 정산되지 않는다.** 양쪽 어디에도 정규화가 없다(요청 `msg.orgOrderNo` 원문, 통보 `r.orgOrderNo() ?? ""` 원문). 브로커 주문번호는 자릿수 표기(선행 0·좌측 패딩·공백)가 단말마다 갈리는 값이다. 이전 구현은 ① 축이 0건이면 **건너뛰고** ③④(수량·가격)로 정산할 수 있었으나 지금은 그 경로가 없다. 실패 결과가 나쁜 쪽이다 — 5초 뒤 `finish(null)` → `status:"timeout"` → **실제로 접수·확인된 취소가 감사 기록에 `timeout` 으로 남고** 화면에 「결과를 확인하지 못했습니다」가 뜬다.
- **R2-IN-03 (= R2-WR-03③).** ② 축이 `n.noticeType !== "" && n.noticeType !== "R"` 이면 `refine((p) => p.isCancel === isCancelNotice)` 인데 `isCancelNotice` 는 `"C"|"M"` 만 true 다. 장래에 추가될 통보 종류(부분취소·예약 확인 등)는 전부 `false` 로 떨어져 **신규 대기 쪽으로** 좁혀진다. 화이트리스트가 아니라 **블랙리스트**라 확장에 취약하다.
- **R2-WR-06.** GC-WR-03(16-34)은 매칭 축에 `fromWireSide` 를 도입하며 「모르는 값은 매수로 지어내지 않는다」고 적었다. 그런데 **감사 행의 `side` 를 정하는 `sideOf`(`:664-667`)는 고쳐지지 않았다** — `notice.side.startsWith("S") ? "S" : "B"` 라 빈 값·`"X"`·소문자 `"b"` 가 전부 `"B"` 가 된다. `sideTrusted` 는 `noticeType !== "C" && !== "M"` 일 뿐이므로 **구 게이트웨이의 빈 `side`("")도 `sideTrusted === true`** 다. 매도 자동주문이 감사 기록에 매수로 남는다. 두 함수가 같은 필드를 서로 다르게 읽는다.

**⚠️ REVIEW 의 Fix 스니펫을 그대로 믿지 않는다.** 16-34 가 정확히 그 함정을 만났다 — 리뷰가 제안한 `sideTrusted && side !== ""` 가 취소거부(`"R"`)에서 살아 있는 신규 매수를 오정산시켰고 `noticeType ∈ {A,E}` 가드를 더 걸어야 했다. 아래 지시도 **코드에서 검증한 뒤** 적용한다.

Purpose: 표기 차이가 진실을 지우지 않게 하고, 모르는 값을 단정하지 않게 한다.
Output: 주문번호 정규화 · 축 전멸 로그 · 화이트리스트 축 · `sideOf` 통일 · 각각의 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW-R2.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-27-SUMMARY.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-34-SUMMARY.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-38-SUMMARY.md
</context>

<interfaces>
`relay/src/ws/order-handler.ts`:
- `export function narrowPending(candidates: PendingOrder[], n: ParsedOrderResp): PendingOrder | null` (:~1010-1090) — 하드 필터(`orgOrderNo` 완전일치 + 취소성 `noticeType`) → `refine` ①②②-1③④
- `const isCancelNotice = n.noticeType === "C" || n.noticeType === "M"`
- ② 축 (:1043-1046) — `if (n.noticeType !== "" && n.noticeType !== "R") refine((p) => p.isCancel === isCancelNotice)`
- ②-1 매매구분 축 (16-34) — `sideTrusted` ∧ `noticeType ∈ {A,E}` ∧ `!p.isCancel` 세 겹. **이 축의 근거 주석이 `sideOf` 수정의 참고 자료다**
- `function sideOf(notice: ParsedOrderResp): OrderSide` (:664-667) — 고칠 자리
- 취소 요청 송신부 (:~754) — `msg.orgOrderNo` 원문이 `PendingOrder.orgOrderNo` 로 들어간다
- 미매칭 warn (:~390) — 지금은 원인 축을 구분하지 못하는 한 줄
- `logCtx` — 계좌번호 마스킹 규율이 이미 적용된 로그 컨텍스트

`relay/src/dma/envelope.ts`:
- `ParsedOrderResp` (:~1150-1195) — `side: string` · `sideTrusted: boolean`(`noticeType !== "C" && !== "M"`) · `noticeType` · `orgOrderNo`(`:1277` `r.orgOrderNo() ?? ""`)
- `fromWireSide(v: string): OrderSide | null` (:~1322-1333) — 첫 글자로 판정하고 아니면 `null`. **지어내지 않는 규율의 정본**

`packages/shared/src/relay.ts` — `OrderSide` 는 `"B" | "S"` 둘뿐이고 `dma_orders.side` CHECK 도 둘뿐이다(행을 남기려면 하나를 골라야 한다)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 주문번호 비교를 정규화하고, 축이 후보를 전부 지운 상황을 드러낸다 (R2-WR-03①②)</name>
  <files>relay/src/ws/order-handler.ts</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-27-SUMMARY.md` · `16-34-SUMMARY.md` · `16-38-SUMMARY.md` — **먼저 읽는다.** 같은 파일의 최신 상태와 줄번호 이동
    - `relay/src/ws/order-handler.ts` — `narrowPending` 전문(하드 필터 + ①~④ 축과 각 축의 근거 주석), `PendingOrder` 타입, 취소 요청에서 `orgOrderNo` 가 `PendingOrder` 로 들어가는 지점, `finish(null)` 이 `status:"timeout"` 을 큐에 넣는 지점, 미매칭 warn 한 줄. **한 번에 읽는다**
    - `relay/src/dma/envelope.ts` — `orgOrderNo` 를 뽑는 지점과 그 원문 처리, `ParsedOrderResp` 전문
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW-R2.md` §R2-WR-03 — Fix 3항목(방향이지 정답이 아니다)
  </read_first>
  <action>
① **정규화 함수를 하나 만든다.** 브로커 주문번호 비교용: 공백 제거 + 선행 0 제거 수준(정확한 규칙은 재량이되 **양쪽에 같은 함수**를 건다). 조립 지점을 하나로 두는 것이 요점이다 — 손으로 두 번 적으면 언젠가 한쪽만 바뀐다.
   - ⚠️ **부작용을 주석에 명시한다**: 선행 0 을 지우면 `"0123"` 과 `"123"` 이 같아진다. 이 축은 항상 `p.isCancel` 과 함께 걸리고 취소 대기가 동시에 여럿일 확률과 그 표기가 갈릴 확률을 저울질한 결과임을 적는다. 저울질 결과가 반대라고 판단하면(코드·게이트웨이 실측 근거가 있으면) 공백 제거만 하고 선행 0 은 건드리지 않아도 된다 — **어느 쪽을 골랐고 왜인지** SUMMARY 에 적는다.

② 하드 필터의 `p.orgOrderNo === n.orgOrderNo` 와 ① 축의 같은 비교를 정규화 버전으로 바꾼다. `n.orgOrderNo === ""` 로 축을 건너뛰는 조건은 **정규화 전 원문 기준으로 유지**할지 정규화 후 기준으로 할지 판단한다(공백만 담긴 값이 오면 결과가 갈린다).

③ **강한 축이 후보를 전부 지운 상황**을 별도 로그로 남긴다: `candidates.length > 0 && hard.length === 0` 일 때, 어느 축이 원인인지(원주문번호 / 취소성 통보) 알 수 있는 필드와 함께 `logger.warn`(또는 error). 지금은 `narrowPending` 밖의 한 줄로 뭉뚱그려져 원인을 알 수 없다.
   - 로그에 **주문번호 원문을 싣지 않는다**(T-16-45). 길이·정규화 후 일치 여부 같은 비식별 신호로 충분하다. 어떤 필드를 골랐는지 SUMMARY 에 적는다.

④ ② 축을 **화이트리스트**로 바꾼다 (R2-IN-03 / R2-WR-03③). 아는 값만 축으로 쓴다 — 신규 통보로 확정할 수 있는 종류(`"A"`·`"E"` 등)일 때만 `refine((p) => !p.isCancel)` 를 건다. 취소성(`"C"|"M"`)은 하드 필터가 이미 걸렀다.
   - ⚠️ **`"A"` 를 신규로 단정하는 근거를 먼저 확인한다.** `/Users/alex/repos/gh-trade` 의 `StockDMA.fbs` 와 게이트웨이 구현에서 「취소 접수 통보의 `noticeType`·`orgOrderNo` 실제 값」을 찾아본다. 근거를 찾으면 주석에 출처(파일·줄)와 함께 박고, **못 찾으면 `"A"` 를 축에서 제외**한다(축을 하나 덜 쓰는 것이 오귀속보다 안전하다 — 이 파일의 `refine` 규율과 같은 방향). 어느 쪽인지 SUMMARY 에 반드시 적는다
   - 16-34 가 넣은 ②-1 매매구분 축의 `noticeType ∈ {A,E}` 조건과 **일관되게** 둔다. 두 축이 다른 집합을 쓰면 언젠가 한쪽만 갱신된다
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 정규화 함수가 **하나**이고 하드 필터·① 축 **양쪽**에서 쓰인다 (소스 인용, 함수명과 호출 지점 2곳)
    - 정규화의 부작용(선행 0 충돌)이 주석에 적혀 있다
    - `candidates.length > 0 && hard.length === 0` 분기의 로그가 존재하고 주문번호 원문을 싣지 않는다 (소스 인용)
    - ② 축이 화이트리스트다: `noticeType !== "R"` 형태의 **부정 조건이 사라졌다** (소스 전/후 인용)
    - `"A"` 채택 여부와 그 근거(gh-trade 파일·줄 또는 「근거 없음 → 제외」)가 SUMMARY 에 있다
    - ②-1 축과 ② 축이 같은 `noticeType` 집합을 쓴다 (소스 인용)
    - `pnpm --filter @gh-radar/relay run typecheck` exit 0 · `run typecheck:tests` exit 0
  </acceptance_criteria>
  <done>표기 차이가 정산을 막지 않고, 축이 후보를 지웠을 때 그 사실이 로그에 남는다.</done>
</task>

<task type="auto">
  <name>Task 2: 감사 행의 매매구분을 지어내지 않는다 (R2-WR-06)</name>
  <files>relay/src/ws/order-handler.ts</files>
  <read_first>
    - `relay/src/ws/order-handler.ts` `sideOf` 전문과 그 호출부 전부(`grep -n "sideOf(" relay/src`), 그리고 자동주문 insert 분기에서 `side` 가 `dma_orders` 로 들어가는 경로
    - `relay/src/ws/order-handler.ts` ②-1 매매구분 축(16-34)의 주석 — **`sideTrusted` 가 「표시해도 되는가」의 축이지 「좁혀도 되는가」의 축이 아니다**라는 판정이 그대로 적혀 있다. `sideOf` 는 전자다
    - `relay/src/dma/envelope.ts` `fromWireSide` 전문과 `sideTrusted` 정의
    - `packages/shared/src/relay.ts` `OrderSide` 정의 · `dma_orders.side` CHECK 를 다루는 주석(행을 남기려면 둘 중 하나를 골라야 한다는 제약)
  </read_first>
  <action>
① `sideOf` 를 `fromWireSide` 로 통일한다. `sideTrusted === false`(취소·정정 표기)는 지금처럼 `"S"` 를 돌려준다 — 그것이 **표기이지 방향의 정본이 아니라는** 기존 규율(16-34 가 `handle` 의 `const side = isCancel ? "S"` 에 적어 둔 것)을 그대로 잇는다.

② `sideTrusted === true` 인데 `fromWireSide` 가 `null` 을 돌려주는 경우(빈 값·미지 표기): **지어내지 않는다.** `dma_orders.side` CHECK 가 `B`/`S` 둘뿐이라 행을 남기려면 하나를 골라야 하므로, **취소 행과 같은 규율**로 `"S"` 를 적고 **사유를 `logger.warn` 으로 남긴다**(S-5, 무로그 fail-safe 금지). 로그 페이로드는 `{ orderNo, noticeType }` 수준 — 계좌번호를 싣지 않는다(T-16-45).
   - ⚠️ **`"B"` 로 바꾸지 않는다.** 지금 동작이 `"B"` 이므로 `"S"` 로 바꾸는 것은 감사 기록의 기본값을 뒤집는 변경이다. 그 방향이 옳은 이유(취소 표기와 같은 규율 = 「방향의 정본이 아님」을 뜻하는 값으로 통일)를 주석에 적고, 반대 판단을 할 근거가 코드에 있으면 그것을 우선한다 — **판단 결과와 근거를 SUMMARY 에 반드시 적는다**

③ `sideOf` 의 docstring 에 「매칭 축(②-1)과 이 함수는 **같은 필드를 다르게 쓴다**」를 명시한다: 축은 「좁혀도 되는가」, 여기는 「표시해도 되는가」. 그래서 조건이 다른 것이 정상이고, 다만 **모르는 값을 지어내지 않는 규율은 공통**이라 둘 다 `fromWireSide` 를 지난다.

④ 호출부는 바꾸지 않는다 — 반환 타입 `OrderSide` 그대로다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `sideOf` 본문에 `startsWith("S")` 가 **없다** (소스 전/후 인용)
    - `sideOf` 가 `fromWireSide` 를 쓴다 (소스 인용)
    - 미해석 시 `logger.warn` 이 있고 계좌번호를 싣지 않는다 (소스 인용)
    - 미해석 기본값 선택(`"S"` vs `"B"`)의 근거가 주석과 SUMMARY 양쪽에 있다
    - `sideOf` 호출부 diff 0줄
    - `pnpm --filter @gh-radar/relay run typecheck` exit 0
  </acceptance_criteria>
  <done>같은 파일의 두 함수가 같은 규율로 같은 필드를 읽는다.</done>
</task>

<task type="auto">
  <name>Task 3: 네 가지를 각각 잠근다</name>
  <files>relay/tests/ws-order.test.ts</files>
  <read_first>
    - `relay/tests/ws-order.test.ts` — 취소 정산 케이스 전부(16-27·16-34 가 넣은 것 포함), `narrowPending` 단위 케이스가 있다면 그 형태, 통보를 주입하는 하네스(`ParsedOrderResp` 를 어떻게 만드는지), `logger` spy 관례. **한 번에 읽는다**
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-34-SUMMARY.md` — 「㉑ 은 좁혀지지 않는 예를 바꿔야 했다」 사례. 기존 케이스를 고칠 때 같은 판단이 필요할 수 있다
  </read_first>
  <action>
① 「표기 차이가 있어도 취소가 정산된다」: 요청 `orgOrderNo = "0000123"`, 통보 `orgOrderNo = "123"`(또는 그 반대) → 그 취소 대기가 정산된다. **정규화가 없으면 `status:"timeout"` 이 된다**는 것이 이 케이스의 의미다.

② 「강한 축이 후보를 전부 지우면 그 사실이 로그에 남는다」: 정규화 후에도 일치하지 않는 `orgOrderNo` 로 통보를 보내고, (a) 아무것도 정산되지 않고 (b) 축 전멸 로그가 남고 (c) **주문번호 원문이 로그에 없다**를 단언한다.

③ 「미지의 `noticeType` 은 축으로 쓰이지 않는다」: `"C"|"M"` 도 화이트리스트 값도 아닌 통보 종류로, 취소 대기가 **신규 쪽으로 잘못 좁혀지지 않음**을 단언한다. 이것이 R2-IN-03 의 잠금이다.

④ 「구 게이트웨이의 빈 `side` 가 감사 행에 매수로 기록되지 않는다」: `sideTrusted === true` ∧ `side === ""` 인 통보로 자동주문 행이 만들어질 때 `dma_orders.side` 가 `"B"` 가 **아니고**, 사유 로그가 남았음을 단언한다.

⑤ 16-34 가 넣은 케이스(매수/매도 동시 대기 정산 · 같은 방향 2건은 좁혀지지 않음 · 연속 취소 허용 · 신규 중복 차단)가 **전부 통과**해야 한다. 깨지면 **그 단언이 옛 구현을 베낀 것인지 먼저 판정**하고, 판정 결과를 SUMMARY 에 적는다.

⑥ **회귀 잠금을 실증한다**: 정규화를 제거 / ② 축을 블랙리스트로 되돌림 / `sideOf` 를 `startsWith` 로 되돌림 — 셋을 각각 무력화했을 때 대응하는 새 케이스가 **실제로 실패**하는지 확인하고 전부 복원한다. 실패 건수를 항목별 숫자로 SUMMARY 에 적고, 복원 후 임시 마커가 남지 않았음을 grep 으로 증명한다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay exec vitest run tests/ws-order.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f relay/tests/ws-order.test.ts` 가 참임을 확인한 뒤 위 명령을 돌린다
    - 새 케이스 4개가 존재하고 통과 (제목 SUMMARY 인용)
    - ② 케이스가 「주문번호 원문이 로그에 없다」를 단언한다 (소스 인용)
    - 16-34 의 4케이스가 통과 (테스트 출력 인용)
    - 회귀 잠금 실증 3건이 각각 실패를 관측하고 복원됐다 (항목별 숫자 인용)
    - `pnpm --filter @gh-radar/relay test` exit 0 · `run typecheck:tests` exit 0
    - relay 테스트 총 건수 전/후 인용
  </acceptance_criteria>
  <done>표기 차이·미지 통보·빈 매매구분이 각각 잠겼다. 되돌리면 빨개진다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트웨이 통보 문자열 ↔ relay 의 요청 문자열 | 두 단말이 만든 표기가 같다는 보장이 없다. 완전일치를 전제로 삼으면 진실을 잃는다 |
| 알려진 통보 종류 ↔ 장래 확장 | 블랙리스트는 확장될 때마다 조용히 오분류한다 |
| 통보의 `side` ↔ 감사 기록의 `side` | 「표시해도 되는가」와 「좁혀도 되는가」는 다른 질문이다 (16-34) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-87 | Repudiation (허위 timeout) | `narrowPending` 하드 필터의 `orgOrderNo` 완전일치 | mitigate | 양쪽 같은 정규화. 표기 차이 정산 케이스로 잠금 |
| T-16-88 | Repudiation (원인 불명) | 축 전멸 시 로그 부재 | mitigate | `candidates>0 && hard===0` 전용 로그. 주문번호 원문 미포함 |
| T-16-89 | Tampering (오귀속) | ② 축 블랙리스트 | mitigate | 화이트리스트로 전환. 근거 없는 `"A"` 는 제외 |
| T-16-90 | Repudiation (감사 왜곡) | `sideOf` 의 `"B"` 지어내기 | mitigate | `fromWireSide` 통일 + 미해석 사유 로그 |
| T-16-45 | Information Disclosure | 새 로그 페이로드 | mitigate | 주문번호·계좌번호 원문 미포함. 테스트로 단언 |
| T-16-13 | 운영 사고 | 실서버·실계좌 | accept | FakeGateway 만 쓴다 (D-27) |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/relay test` exit 0
- `pnpm --filter @gh-radar/relay run typecheck` · `run typecheck:tests` exit 0
- 포매터를 돌리지 않는다
</verification>

<success_criteria>
- 표기가 다른 주문번호도 정산된다
- 축이 후보를 지웠을 때 원인이 로그에 남는다
- 미지의 통보 종류가 신규로 단정되지 않는다
- 빈 매매구분이 매수로 기록되지 않는다
- 16-34 가 잠근 것이 그대로다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-43-SUMMARY.md` when done
</output>
