---
phase: 16-trading-limit-chaser-vi-my-page
plan: 09
type: execute
wave: 8
depends_on: ["16-03", "16-08"]
files_modified:
  - webapp/src/lib/relay-provider.tsx
  - webapp/src/lib/use-relay-socket.ts
  - webapp/src/app/layout.tsx
  - webapp/src/components/stock/stock-orderbook-section.tsx
  - webapp/src/lib/__tests__/relay-socket.test.ts
  - webapp/src/lib/__tests__/relay-provider.test.tsx
autonomous: true
requirements: [TRADE-03, NAV-01]

must_haves:
  truths:
    - "로그인 상태면 앱 전역에서 relay wss 1연결이 열려 유지된다 (D-22)"
    - "비로그인이면 연결하지 않고, 로그아웃 시 즉시 close 된다 (D-23)"
    - "여러 소비자가 같은 종목을 구독해도 sub/unsub 는 참조계수 0→1 · 1→0 에서만 나간다"
    - "호가주문 탭이 전역 연결 위에서 회귀 없이 동작한다 (orderbook.spec.ts green)"
    - "전략 프레임(lc · lc.snap · vi · vi.list · vi.notice · strategies.disabled · order.result)이 전역 상태에 반영된다 (D-12)"
  artifacts:
    - path: "webapp/src/lib/relay-provider.tsx"
      provides: "전역 wss 컨텍스트 + 구독 ref-count + Provider 밖 안전 폴백"
      contains: "useRelaySubscription"
    - path: "webapp/src/app/layout.tsx"
      provides: "AuthProvider 안쪽 RelayProvider 중첩"
      contains: "RelayProvider"
  key_links:
    - from: "webapp/src/components/stock/stock-orderbook-section.tsx"
      to: "webapp/src/lib/relay-provider.tsx"
      via: "useRelaySubscription({isin, exchange, enabled})"
      pattern: "useRelaySubscription"
---

<objective>
`useRelaySocket`(631줄, 현재 호가주문 탭 1곳 소비)을 **전역 컨텍스트로 승격**한다(D-22). 사이드바 전략 목록·My page 가 종목 구독 없이도 상태를 봐야 하기 때문이다.

승격 방식은 **최소 변경**이다(RESEARCH 권고 · PATTERNS 형태 analog = `use-watchlist-set.tsx`):
1. `useRelaySocket` 의 연결·인증·리듀서는 **그대로** 두고 `isin`/`exchange` 옵션만 떼어낸 `useRelayConnection()` 으로 분리한다.
2. `RelayProvider` 가 그 연결을 소유하고 컨텍스트로 `RelaySocketState` + `subscribe/unsubscribe` 를 노출한다.
3. 구독 **ref-count**(`Map<key, count>`, 0→1 에서만 `sub`, 1→0 에서만 `unsub`)를 프로바이더가 관리한다.
4. `useRelaySubscription({isin, exchange, enabled})` 이 **기존 `useRelaySocket` 과 같은 반환 형태**를 유지해 `stock-orderbook-section.tsx` 는 **훅 호출 한 줄만** 바뀐다(나머지 499줄 무변경).
5. `applyFrame` 에 전략 프레임 case 7종을 추가한다. 알 수 없는 `t` 를 무시하는 `default:` 는 유지한다(구버전 브라우저 안전 — T-15-41).

Purpose: 여기서 회귀가 나면 뒤의 UI 3표면이 전부 흔들린다. **`orderbook.spec.ts` green 이 이 plan 의 게이트다.**
Output: relay-provider.tsx · use-relay-socket 분리 · layout 중첩 · 소비자 1줄 교체 · 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: useRelayConnection 분리 + RelayProvider + 구독 ref-count</name>
  <files>webapp/src/lib/use-relay-socket.ts, webapp/src/lib/relay-provider.tsx</files>
  <read_first>
    - `webapp/src/lib/use-relay-socket.ts` 전문(631줄) — `UseRelaySocketOptions`(:~120), `RelaySocketState`(:128~158), 리듀서(:203~236), `applyFrame`(:237~290, `default:` 주석 T-15-41), 연결 effect(`authAckedRef`/`setAuthEpoch`), 구독 effect, `quoteCacheRef`, `wantedKeyRef` 지연 프레임 필터, `mergeAccount`, stale 타이머(:555~), `RELAY_MAX_RECONNECT_ATTEMPTS`(10)/`relayBackoffDelayMs`
    - `webapp/src/hooks/use-watchlist-set.tsx` — Provider + context + hook + **Provider 밖 안전 폴백**(`useContext(...) ?? EMPTY_VALUE`) 형태(:36~64, :105~126)
    - `webapp/src/lib/relay-url.ts` — `resolveRelayWsUrl()`
    - `packages/shared/src/relay.ts` — 16-03 의 아웃바운드 7종
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md` §relay-provider(493~545행)
  </read_first>
  <action>
① `use-relay-socket.ts` 에서 **연결·인증·리듀서·재접속·stale 타이머**를 `useRelayConnection(): RelayConnectionState & { send, reconnect, subscribe, unsubscribe }` 로 분리한다. `isin`/`exchange` 의존을 제거한다 — 연결 effect 는 원래도 종목에 의존하지 않았고, 종목 전환은 재구독이었다. `quote`/`tape` 는 **키별 맵**(`Map<"isin|ex", RelayQuote>` / tape)으로 보관한다. 기존 `wantedKeyRef` 지연 프레임 필터는 훅 안이 아니라 **소비자 경계**(Task 2 의 `useRelaySubscription`)로 옮긴다 — 전역 상태에는 여러 종목이 섞이므로 「내 키에 해당하는 것만 고르기」가 소비자 책임이 된다.
   `mergeAccount` 는 **한 글자도 바꾸지 않는다**(relay `#mergeAccountState` 와 쌍이라는 파일 주석 유지 — Pitfall 16).
   `applyFrame` 에 전략 프레임 case 를 추가한다: `lc`(단건 upsert, `crud === "D"` 면 목록에서 제거) · `lc.snap`(전량 교체) · `vi`(설정, `null` = 미등록) · `vi.list`(`snap ? 전량교체 : 항목 upsert`) · `vi.notice`(누적) · `strategies.disabled`(완료 신호 — 상태를 고치지 않고 로그/배너 용도로만 노출) · `order.result`(rid 별 1회성 결과 — 상태에 쌓지 않고 대기 중인 Promise 로 흘린다, Task 2). **`default:` 는 그대로 둔다.**

② `webapp/src/lib/relay-provider.tsx` 를 신규로 만든다(`'use client'`). `use-watchlist-set.tsx` 골격 그대로:
   - `RelayProvider({children})` 가 `useAuth()` 로 세션 유무를 보고, **로그인 상태에서만** `useRelayConnection()` 을 활성화한다(D-23: 비로그인 미연결, 로그아웃 시 close).
   - 컨텍스트 값: 연결 상태 전부 + `subscribe(isin, ex)` / `unsubscribe(isin, ex)` / `getQuote(isin, ex)` / `getTape(isin, ex)` + 전략 상태(`limitChasers`, `viTrigger`, `viOrders`, `strategyEvents`) + `sendOrder`(16-10 에서 채운다 — 여기서는 자리만).
   - **구독 ref-count**: `Map<`${isin}|${ex}`, number>` 를 ref 로 두고 0→1 에서만 `{t:"sub"}`, 1→0 에서만 `{t:"unsub"}`. 재접속(auth epoch 변경) 시 **참조계수 ≥1 인 키 전부 재구독**.
   - **Provider 밖 안전 폴백**: `useRelayContext()` 가 `useContext(...) ?? EMPTY_RELAY_VALUE` 를 돌려준다. Provider 없이 렌더되는 기존 RTL 테스트(`account-panel.test.tsx` 등)가 throw 하면 회귀다 — 이 규율을 주석으로 명시한다.
   - 상태 프레임의 `unauthorized` / 연결 여부를 사이드바가 읽을 수 있도록 `status`/`isMapped` 를 노출한다(D-19 판정 원천).
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp test -- relay-provider</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/src/lib/relay-provider.tsx` 가 `useContext(RelayContext) ?? EMPTY_RELAY_VALUE` 형태의 폴백을 갖는다 (throw 0건)
    - `grep -c "case \"lc\"\|case \"lc.snap\"\|case \"vi\"\|case \"vi.list\"\|case \"vi.notice\"\|case \"strategies.disabled\"" webapp/src/lib/use-relay-socket.ts` >= 6
    - `applyFrame` 의 `default:` 무시 분기가 그대로 존재
    - ref-count 로직에서 `sub` 송신이 count 0→1 에서만, `unsub` 이 1→0 에서만 일어남을 단위 테스트가 단언
    - `mergeAccount` 함수 diff 0 (`git diff webapp/src/lib/use-relay-socket.ts | grep -c "^[-+].*mergeAccount"` 가 이동 외 변경 없음)
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>전역 연결 + ref-count 구독 + 전략 프레임 반영 + 안전 폴백이 존재한다.</done>
</task>

<task type="auto">
  <name>Task 2: layout 결선 + useRelaySubscription 으로 소비자 교체</name>
  <files>webapp/src/app/layout.tsx, webapp/src/lib/relay-provider.tsx, webapp/src/components/stock/stock-orderbook-section.tsx</files>
  <read_first>
    - `webapp/src/app/layout.tsx`(53줄) — 중첩 `ThemeProvider > AuthProvider > ChatProvider > WatchlistSetProvider`(:39~49) + 각 Provider 배치 근거 주석
    - `webapp/src/components/stock/stock-orderbook-section.tsx` — `useRelaySocket({isin, exchange, enabled})` 호출부(:155 부근)와 반환값 사용처 전부
    - `webapp/src/lib/use-relay-socket.ts` — `RelaySocketState` 반환 계약(:128~158)
  </read_first>
  <action>
① `layout.tsx` 중첩을 `AuthProvider > RelayProvider > ChatProvider > WatchlistSetProvider` 로 바꾼다. **배치 근거 주석까지 복사**한다: RelayProvider 는 Supabase 세션 토큰이 필요하므로 AuthProvider 안쪽(D-22/D-23).

② `relay-provider.tsx` 에 `useRelaySubscription({ isin, exchange, enabled })` 를 추가한다. **반환 형태를 기존 `RelaySocketState` 와 동일하게 유지**한다(`status · statusLabel · statusMessage · attempt · accounts · quote · tape · account · orders · messages · isStale · send · reconnect`). 내부에서:
   - `enabled !== false` 일 때 마운트 시 `subscribe(isin, ex)`, 언마운트/키 변경 시 `unsubscribe(이전 키)`.
   - `quote`/`tape` 는 **자기 키에 해당하는 것만** 골라 반환한다(전역 맵에서 조회). 지연 프레임 필터(구 `wantedKeyRef`)가 여기로 옮겨진다 — 종목 A 의 호가가 종목 B 화면에 뜨지 않아야 한다(RESEARCH A2 위험).
   - `account`/`accounts`/`messages`/`orders` 는 전역 값을 그대로 통과시킨다(종목 축이 없다).

③ `stock-orderbook-section.tsx` 의 훅 호출을 `useRelaySubscription(...)` **한 줄로 교체**한다. 반환 형태가 같으므로 나머지는 손대지 않는다. 만약 손대야 하는 곳이 생기면 그것은 반환 계약이 어긋난 신호이므로 계약을 맞추는 쪽으로 고친다.

④ 기존 `useRelaySocket` export 는 **남기지 않는다**(두 벌 연결이 생기는 사고 방지). 사용처가 0인지 grep 으로 확인하고 제거하거나, 남긴다면 내부에서 `useRelaySubscription` 을 그대로 부르는 얇은 별칭으로 둔다.
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp test &amp;&amp; pnpm --filter gh-radar-webapp test:e2e -- orderbook</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RelayProvider" webapp/src/app/layout.tsx` >= 2 (import + 중첩) 이고 `AuthProvider` 안쪽에 위치
    - `grep -rn "useRelaySocket(" webapp/src --include=*.tsx --include=*.ts` 결과가 0건이거나 별칭 정의 1건뿐
    - `grep -c "useRelaySubscription" webapp/src/components/stock/stock-orderbook-section.tsx` == 1
    - `pnpm --filter gh-radar-webapp test:e2e -- orderbook` exit 0 (**회귀 게이트**)
    - `pnpm --filter gh-radar-webapp test:e2e -- stock-detail-tabs` exit 0
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>전역 Provider 결선 완료, 호가주문 탭 E2E 회귀 green.</done>
</task>

<task type="auto">
  <name>Task 3: 전역 승격 회귀·격리 테스트</name>
  <files>webapp/src/lib/__tests__/relay-socket.test.ts, webapp/src/lib/__tests__/relay-provider.test.tsx</files>
  <read_first>
    - `webapp/src/lib/__tests__/relay-socket.test.ts` — 기존 재접속·백오프·상태 전이 케이스와 WS 목 하네스
    - `webapp/src/hooks/__tests__/` 하위 Provider 테스트가 있으면 그 형태
    - `webapp/src/components/orderbook/__tests__/account-panel.test.tsx` — Provider 없이 렌더되는 기존 테스트(폴백 회귀 확인 대상)
  </read_first>
  <action>
① `relay-socket.test.ts` 를 새 구조(`useRelayConnection`)에 맞게 갱신하되 **기존 단언(재접속 10회 상한 · 지수 백오프 · unauthorized 무백오프 · stale 전이)은 그대로 유지**한다. 케이스가 사라지면 회귀 감지가 사라진다.

② `relay-provider.test.tsx` 신규:
   1. **비로그인 미연결**: 세션 없음 → WebSocket 생성 0회, `status === "idle"`(D-23).
   2. **로그아웃 close**: 로그인 상태에서 세션 제거 → 소켓 close 1회.
   3. **ref-count**: 소비자 2개가 같은 (isin, ex) 를 구독 → `sub` 1회. 하나 언마운트 → `unsub` 0회. 둘 다 언마운트 → `unsub` 1회.
   4. **재접속 재구독**: 참조계수 2인 상태에서 재접속(auth epoch 변경) → 그 키에 대해 `sub` 이 **1회만** 다시 나간다.
   5. **키 격리**: 종목 A 의 `q` 프레임이 종목 B 를 구독한 소비자의 `quote` 로 새지 않는다.
   6. **전략 프레임 반영**: `lc.snap`(2건) → `limitChasers.length === 2`. 이어 `lc`(같은 key, `crud:"D"`) → 1건.
   7. **미지 프레임 무해**: `{t:"zzz"}` 수신해도 상태가 그대로이고 예외가 없다.
   8. **Provider 밖 폴백**: Provider 없이 `useRelayContext()` 를 쓰는 컴포넌트가 throw 하지 않고 빈 값을 받는다.
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp test -- relay</automated>
  </verify>
  <acceptance_criteria>
    - `relay-provider.test.tsx` 에 8개 이상 케이스가 있고 전부 green
    - 케이스 3·4 가 실제 송신된 프레임 배열을 검사해 횟수를 단언한다
    - `pnpm --filter gh-radar-webapp test` 전량 exit 0 (기존 orderbook RTL 테스트 포함)
  </acceptance_criteria>
  <done>전역 승격의 회귀면(재접속·격리·ref-count·폴백)이 테스트로 고정된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 세션 → wss 인증 | 로그아웃한 사용자의 소켓이 남으면 세션이 계속 살아 있다 |
| 전역 상태 → 화면 | 여러 종목·계좌가 한 상태에 섞인다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Information Disclosure | 전역 quote/tape 맵 | mitigate | 소비자 경계에서 **자기 키만** 반환(구 `wantedKeyRef` 이동). 테스트 케이스 5 가 교차 노출 0을 증명 |
| T-16-04 | Elevation of Privilege | 비로그인 연결 | mitigate | 세션이 없으면 소켓을 열지 않는다(D-23). 로그아웃 시 즉시 close — 케이스 1·2 |
| T-16-03 | Session Management | 전역 1연결 + 탭 다중 | accept | `SessionManager` 는 이미 참조계수 기반이라 `SESSION_GRACE_MS` 5분 유예가 그대로 성립(RESEARCH A9). 탭 여러 개는 같은 userId 이므로 세션이 하나로 공유된다 |
| T-16-05 | Tampering | 미지 프레임 | mitigate | `applyFrame` 의 `default:` 무시 유지 — 서버가 앞서 나가도 브라우저가 터지지 않는다(T-15-41). 케이스 7 |
</threat_model>

<verification>
- `pnpm typecheck` exit 0
- `pnpm --filter gh-radar-webapp test` exit 0
- `pnpm --filter gh-radar-webapp test:e2e -- orderbook` exit 0 (필수 회귀)
- `pnpm --filter gh-radar-webapp test:e2e -- stock-detail-tabs` exit 0
</verification>

<success_criteria>
- 앱 전역 wss 1연결, 로그인 조건부
- 소비자 훅 반환 계약 동일 → 호가주문 탭 무변경 통과
- 전략 프레임이 전역 상태로 들어온다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-09-SUMMARY.md` when done
</output>
