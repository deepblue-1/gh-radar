---
phase: 16-trading-limit-chaser-vi-my-page
plan: 15
type: execute
wave: 12
depends_on: ["16-10", "16-11"]
files_modified:
  - webapp/src/components/trading/strategy-status-card.tsx
  - webapp/src/components/trading/me-client.tsx
  - webapp/src/app/me/page.tsx
  - webapp/src/components/trading/__tests__/strategy-status-card.test.tsx
  - webapp/e2e/specs/me.spec.ts
autonomous: true
requirements: [MYPAGE-01]

must_haves:
  truths:
    - "/me 가 전략 현황 → 계좌별 미체결 → 잔고 순서로 세로 배치된다 (D-20)"
    - "계좌가 2개 이상이면 계좌 선택 UI 없이 계좌별 섹션이 세로로 반복되고 계좌번호는 전체 표시된다 (D-21)"
    - "전략 현황 행이 종목·코드·거래소 태그·계좌번호·상태 배지를 모두 보여주고 클릭하면 편집 페이지로 간다 (D-14/D-20 · UI-SPEC C2)"
    - "전체 비활성화 버튼이 My page 에만 있고 확인 다이얼로그를 거친다 (D-09 · UI-SPEC D5)"
    - "전체 비활성화 후 상태는 65 가 아니라 60/61 에코로 갱신된다"
  artifacts:
    - path: "webapp/src/components/trading/strategy-status-card.tsx"
      provides: "상따 전략 행 + VI 행 + 전체 비활성화"
      contains: "data-slot=\"strategy-status-card\""
    - path: "webapp/src/components/trading/me-client.tsx"
      provides: "My page 조립 · 계좌별 반복"
      contains: "data-slot=\"me-page\""
  key_links:
    - from: "webapp/src/components/trading/strategy-status-card.tsx"
      to: "webapp/src/lib/relay-provider.tsx"
      via: "send({t:\"strategies.disable\"}) — key 생략 = 전체"
      pattern: "strategies\\.disable"
---

<objective>
My page(`/me`)를 만든다. 사이드바가 3단에서 축약한 전략 상태의 **정본 화면**이며(UI-SPEC N3a), 계좌별 미체결·잔고를 세로로 반복한다.

- `strategy-status-card.tsx`: 상따 전략 행(`.srow`, min-height 44px) + VI 행 + **전체 비활성화**(D-09, 확인 다이얼로그).
- `me-client.tsx`: 상태줄 → 전략 현황 → 계좌 A(미체결 → 잔고) → 계좌 B(…) 세로 반복. 계좌 카드 내부만 ≥1280 에서 2열(477/477).
- 오늘 주문 이력 표는 **v1 미포함**(D-20 deferred) — 만들지 않는다.

Purpose: 전략이 지금 어떤 상태인지, 어느 계좌에 무엇이 걸려 있는지 한 화면에서 본다. 전체 비활성화는 이 화면에만 두어 편집 중 오클릭 경로를 만들지 않는다(UI-SPEC §오조작 방지 4).
Output: 전략 현황 카드 · My page 클라이언트 · RTL/E2E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: strategy-status-card — 전략 행 · VI 행 · 전체 비활성화</name>
  <files>webapp/src/components/trading/strategy-status-card.tsx, webapp/src/components/trading/__tests__/strategy-status-card.test.tsx</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-mypage-sidebar-mockup.html` — **정본**(시나리오 5종: 전략 3·계좌 2 / 전략 0 / 전체 비활성화 확인 / 매핑 없음 / 비로그인)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md` §C1~C4(375~390행) · §확인 다이얼로그 3(550~560행) · §빈 상태(531~549행) · §접근성 라벨 My page 전략 행
    - `webapp/src/components/trading/strategy-badge.tsx`(16-11) — `strategyBadgesOf`
    - `webapp/src/components/orderbook/order-confirm-dialog.tsx` — 확인 다이얼로그 패턴(기본 포커스 = 닫기)
    - `webapp/src/lib/limit-chaser.ts`(16-12) — `strategyKey`
  </read_first>
  <action>
① **상따 전략 행**(`.srow`, min-height 44px): `종목명(14/600)` · `종목코드(11 mono)` · `거래소 태그` · `계좌번호 전체(11 mono)` · `상태 배지 1~2개` · `›`. **사이드바와 달리 여기서는 거래소 태그·상태 배지를 전부 표시한다**(폭 여유 + 이 화면이 상태 요약의 정본). 클릭 → `/trading/limit-chaser/{encodeURIComponent(key)}`. `aria-label="{종목명} {거래소} 전략 — {상태 배지 텍스트}"`.
   모바일에서는 행이 wrap 되고 **계좌번호가 둘째 줄 전폭**(`order:9`)으로 내려간다. 종목명만 `min-w-0` + ellipsis.
   빈 상태: `등록된 상따 전략이 없어요` / `트레이딩 › 상따에서 종목을 고르면 여기에 표시돼요.`

② **VI 행**: 「VI 자동매수」 + 가동/중지 배지 + 요약 `{금액}만원 · {상승률}% 이상` → 클릭 시 `/trading/vi`. 중지면 요약 `—`.

③ **전체 비활성화**(C4): 카드 하단(`--border` 상단선 + 12px 여백) 우측, `--destructive` **테두리** 버튼(36px) + 좌측 설명문(11px `--muted-fg`). **전략 0 + VI 중지면 `disabled`**(`opacity:.45`).
   확인 다이얼로그: 제목 `전략을 전부 비활성화할까요?` / 부제 `상따 {N}건과 VI 자동매수가 한 번에 꺼져요.` / 요약 = 상따 N건 → 전부 OFF · VI 가동 → 중지 · 계좌 목록 / 경고 `이미 접수된 주문은 취소되지 않아요. 미체결은 아래 표에서 개별 취소해 주세요.` / 버튼 `전체 비활성화`(테두리) · `닫기`(**기본 포커스**).
   확정 → `send({t:"strategies.disable"})` (**key 생략 = 전체**). **65 를 완료 신호로만 쓰고 상태는 60/61 에코로 갱신한다** — 65 수신 시 버튼을 다시 열되 목록 상태는 건드리지 않는다(RESEARCH §D). 단건 비활성화 UI 는 만들지 않는다(D-09 — 스위치 OFF 로 갈음).

RTL: ① 전략 3건 렌더 + 배지·거래소·계좌번호 전체 표시 ② 행 클릭 시 인코딩된 key 링크 ③ 전략 0 + VI 중지 → 버튼 `disabled` ④ 확인 다이얼로그 기본 포커스 = 닫기 ⑤ 확정 시 `{t:"strategies.disable"}` 1회, `key` 프로퍼티 없음 ⑥ 65 수신이 목록을 바꾸지 않는다 ⑦ 빈 상태 문구.
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp test -- strategy-status-card</automated>
  </verify>
  <acceptance_criteria>
    - 7 케이스 green
    - `grep -c "strategies.disable" webapp/src/components/trading/strategy-status-card.tsx` >= 1 이고 전송 객체에 `key` 가 없다
    - 계좌번호가 마스킹 없이 렌더됨을 단언하는 케이스 존재(S-5/D2)
    - 「전체 비활성화」 버튼이 채움이 아니라 테두리(`--destructive` 텍스트/보더)임을 클래스로 확인
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>전략 현황 카드와 전체 비활성화가 계약대로 동작한다.</done>
</task>

<task type="auto">
  <name>Task 2: me-client 조립 — 계좌별 세로 반복</name>
  <files>webapp/src/components/trading/me-client.tsx, webapp/src/app/me/page.tsx</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md` §C5/C6/C7 · §R4(계좌 카드 내부 477/477, `.acct-grid > * { min-width:0 }` 필수) · 모바일 세로 순서
    - `webapp/src/components/orderbook/account-panel.tsx`(16-10) — **계좌 전용 모드** props · `.rlist` · `originTag`
    - `webapp/src/components/trading/{dma-gate,strategy-status-card}.tsx`
    - `webapp/src/lib/relay-provider.tsx` — `accounts` · 계좌별 `RelayAccountState`
    - `webapp/src/app/watchlist/page.tsx` — 라우트 셸 템플릿
  </read_first>
  <action>
`me-client.tsx`:
- **상태줄(C1)**: `DMA Ready` · 상따 N건 · VI 가동/중지 · 계좌 N개 · 반영 시각. `aria-live="polite"`.
- **전략 현황 카드**(Task 1).
- **계좌 섹션 반복(C5)**: `accounts` 를 순회해 계좌마다 카드 1개. 헤더 = `계좌 {전체번호}`(mono) + 우측 `{상품명}`. 내부 `.acct-grid` 안에 **소제목(12px/600 `--muted-fg`) + 미체결** / **소제목 + 잔고**. `AccountPanel` 을 **계좌 전용 모드**(종목 축 없음)로 계좌마다 1개씩 렌더한다. **계좌 선택 UI 없음**(D-21).
  - ≥1280: 계좌 카드 내부만 2열(477/477, gap 12). **`.acct-grid > * { min-width: 0 }` 필수** — 없으면 미체결 439px·잔고 444px 콘텐츠 최소폭 때문에 `overflow-x:hidden` 아래에서 **스크롤이 아니라 조용한 잘림**이 된다(lessons.md 등재 함정, 목업 R2① 회귀 원인). <1280 은 1열.
  - 모바일: 미체결·잔고 모두 `.rlist` 2줄 카드 행(16-10 구현 재사용 — 여기서 다시 만들지 않는다).
- **게이트(C6)**: 비로그인 → `<DmaGate reason="unauthenticated">`(버튼 `로그인`), 매핑 없음 → `<DmaGate reason="unmapped" surface="전략·잔고·미체결">`. 사이드바에서는 숨겨져 있어도 **직접 URL 진입은 이 화면**이다(D-19).
- **오늘 주문 이력 표를 만들지 않는다**(D-20 deferred). `GET /api/orders` 를 호출하지 않는다.
- 세로 순서 고정: 상태줄 → 전략 현황 → 계좌 A(미체결, 잔고) → 계좌 B(…).
- `data-slot="me-page"` · 계좌 카드 `data-slot="me-account-card"`.
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp build &amp;&amp; pnpm --filter gh-radar-webapp test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "listOrders\|/api/orders" webapp/src/components/trading/me-client.tsx` == 0 (주문 이력 표 미포함)
    - `.acct-grid` 자식에 `min-w-0`(또는 `min-width:0`)이 적용되어 있다
    - 계좌 `<select>` 가 My page 에 렌더되지 않는다(계좌 선택 UI 없음)
    - `/me` 가 빌드되고 `data-slot="me-page"` 를 렌더한다
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>My page 가 계좌별 세로 반복으로 조립되고 잘림 방지 규칙이 적용된다.</done>
</task>

<task type="auto">
  <name>Task 3: me E2E</name>
  <files>webapp/e2e/specs/me.spec.ts</files>
  <read_first>
    - `webapp/e2e/specs/orderbook.spec.ts` — spec 골격(serial · withLocalRelay)
    - `webapp/e2e/fixtures/relay.ts`(16-02) — `seedLimitChasers` · `seedViTrigger` · 계좌 2개 시드 방법(`respondLoginWithAccounts`)
  </read_first>
  <action>
`mode:'serial'` + `withLocalRelay()` 1회. 케이스:

1. **전략 3 · 계좌 2**: `/me` → 전략 현황에 3행(배지·거래소·계좌번호 전체) + 계좌 카드 **2개**가 세로로 반복되고 계좌 선택 UI 가 없다.
2. **행 클릭 이동**: 전략 행 클릭 → `/trading/limit-chaser/` + 인코딩된 key 로 이동하고 그 전략이 폼에 채워진다.
3. **전략 0**: 시드 없이 진입 → 빈 문구 + 「전체 비활성화」 `disabled`.
4. **전체 비활성화**: 확인 다이얼로그 → 기본 포커스 닫기 → 확정 → relay 가 msg_type **14** 수신 → 60/61 에코 푸시 후 배지가 꺼지고 사이드바 3단 원 아이콘이 속빔으로 바뀐다.
5. **매핑 없음 게이트**: `clearDmaCredentials()` → `data-slot="dma-gate"` + 제목 「DMA 계정이 연결되지 않았어요」.
6. **비로그인**: 로그아웃 상태 진입 → 「로그인이 필요해요」 + 「로그인」 버튼.
7. **데스크톱 1280 2열**: 계좌 카드 내부 미체결/잔고가 2열이고 **요소 실측 폭이 컨테이너를 넘지 않는다**(`boundingBox()` 비교, 잘림 0).
8. **모바일 390**: `.rlist` 2줄 카드 행으로 리플로우되고 표가 렌더되지 않는다.
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp test:e2e -- me</automated>
  </verify>
  <acceptance_criteria>
    - 8 케이스 green
    - 케이스 4 가 `requestLog()` 로 msg_type 14 수신을 단언한다
    - 케이스 7 이 `boundingBox().width` 비교로 잘림 0 을 단언한다
    - spec 상단 `test.describe.configure({ mode: 'serial' })`
  </acceptance_criteria>
  <done>My page 의 구조·전체 비활성화·반응형이 E2E 로 고정된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 「전체 비활성화」 클릭 | 상따 전부 + VI 가 한 번에 꺼진다(되돌리려면 각각 다시 켜야 한다) |
| 계좌 정보 표시 | 여러 계좌의 잔고·미체결이 한 화면에 모인다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | 금전 손실(오조작) | 전체 비활성화 | mitigate | 확인 다이얼로그 + 기본 포커스 닫기 + 테두리형 버튼(채움 빨강은 매수 전용, S-6) + 전략 0·VI 중지 시 `disabled`. **상따/VI 페이지에는 두지 않는다**(편집 중 오클릭 경로 제거) |
| T-16-07 | Repudiation | 65 완료 신호 | accept | 상태는 60/61 에코로만 갱신한다. 65 유실 시 버튼 재활성이 늦어질 뿐 상태는 정확 |
| T-16-02 | Information Disclosure | 계좌 섹션 | mitigate | 표시 원천은 전역 wss 계좌 스냅샷(자기 userId 한정)뿐. `GET /api/orders` 호출 0(주문 이력 표 미포함) |
| T-16-09 | Information Disclosure | 계좌번호 전체 표시 | accept | UI-SPEC D2 계약(화면 전체 · 로그 마스킹). 로그로 나가는 경로가 없다 |
</threat_model>

<verification>
- `pnpm typecheck` exit 0
- `pnpm --filter gh-radar-webapp test` exit 0
- `pnpm --filter gh-radar-webapp test:e2e -- me` exit 0
</verification>

<success_criteria>
- `/me` 가 전략 현황 → 계좌별 미체결·잔고 순서로 동작
- 전체 비활성화가 확인 후 14 를 보내고 에코로 상태가 갱신
- 1280 2열 / 390 `.rlist` 잘림 0
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-15-SUMMARY.md` when done
</output>
