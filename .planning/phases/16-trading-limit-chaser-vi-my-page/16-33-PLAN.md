---
phase: 16-trading-limit-chaser-vi-my-page
plan: 33
type: execute
wave: 20
depends_on: ["16-27"]
gap_closure: true
files_modified:
  - relay/src/ws/order-handler.ts
  - relay/src/hub/subscription-hub.ts
  - relay/tests/ws-order.test.ts
autonomous: true
requirements: [TRADE-03]

must_haves:
  truths:
    - "좁히기에 실패했거나 연결 종료 후 도착한 **수동** 통보가 0행 갱신으로 조용히 사라지지 않는다 — `findIdByOrderNo(userId, orderNo)` 로 좁혀 `orderRowId` 로 갱신하고, 행이 없으면 통보 원문을 `logger.error` 로 남긴다 (16-VERIFICATION 갭 2 ① / GC-CR-02 / S-5)"
    - "모든 통보에 **두 번째 감사 사본(stdout)** 이 있다 — `dma_orders` 에 붙지 못한 통보라도 로그로 대조할 수 있다 (D-24)"
    - "통보 기록 경로의 예외가 relay 프로세스를 내리지 않는다 — `void recordUnmatched(...)` 에 `.catch` 가 있고 `autoInsertRow` 가 try 안에 있다 (GC-WR-01)"
    - "빈 주문번호(`orderNo === \"\"`) 통보들이 하나의 in-flight 키로 병합되지 않는다 — 서로 다른 자동주문 거부가 한 행에 겹쳐 쓰이지 않는다 (GC-WR-02)"
  artifacts:
    - path: "relay/src/ws/order-handler.ts"
      provides: "manual 분기 조회 경유 + .catch 방어 + 빈 주문번호 in-flight 제외"
      contains: "findIdByOrderNo"
    - path: "relay/src/hub/subscription-hub.ts"
      provides: "통보 stdout 감사 사본"
    - path: "relay/tests/ws-order.test.ts"
      provides: "0행 수동 통보 로그 · recordUnmatched 예외 격리 · 빈 주문번호 분리 케이스"
  key_links:
    - from: "relay/src/ws/order-handler.ts (recordUnmatched manual 분기)"
      to: "relay/src/store/orders.ts (findIdByOrderNo)"
      via: "userId + orderNo 3축 조회 후 orderRowId 셀렉터로 갱신"
      pattern: "findIdByOrderNo"
    - from: "relay/src/ws/order-handler.ts (통보 소비 루프)"
      to: "relay/src/index.ts (unhandledRejection)"
      via: ".catch 로 프로세스 종료 경로를 끊는다"
      pattern: "\\.catch\\("
---

<objective>
**16-VERIFICATION 갭 2 의 ①(GC-CR-02)** 과 그 인접 결함 **GC-WR-01 · GC-WR-02** 를 닫는다. 셋 다 `recordUnmatched`/`ensureRow` 한 경로에 있고, 셋 다 「통보 1건의 사고가 기록 소실 또는 전면 장애로 번진다」는 모양이다.

지금 상태:
- **GC-CR-02.** 수동 주문의 insert 는 `order_no` 를 **싣지 않는다**(`order-handler.ts:661-681` — 접수 전이라 주문번호를 모른다). `order_no` 는 `finish` 가 정산할 때 채운다(:749-758). 그런데 `recordUnmatched` 의 `manual` 분기(:395-403)는 `{...patch, userId}` 를 큐에 넣고 `selectorOf` 가 이를 `order_no` 셀렉터로 만든다(`store/orders.ts:597-609`). **그 시점에 그 사용자의 오늘 행 중 `order_no = notice.orderNo` 인 행은 없다.** PostgREST 의 update 는 0행이어도 에러가 아니므로 이 갱신은 아무것도 하지 않고 끝나며 **로그도 없다** — 이 모듈이 머리말에서 「Pitfall 18」이라고 부르며 없애겠다고 선언한 바로 그 침묵이다. 발생 경로는 둘 다 실사용에서 재현된다: ① 좁히기 실패(테스트 ㉑ 이 고정한 상태), ② `closeConn` 이후 도착(후보 0건이라 **경고 로그조차 없다**). 결과는 실제로 접수·체결된 주문이 `status:'requested'` → 5초 뒤 `'timeout'`, `order_no: null` 로 남고 브로커 주문번호와 대조할 수단이 사라지는 것이다.
- **GC-WR-01.** `void recordUnmatched(userId, notice)`(:367)에 `.catch` 가 없다. `ensureRow` 안에서 `autoInsertRow(userId, notice)`(:454)와 그 안의 `deps.symbols.lookup` · `deps.hub.getLimitChasers` · `deps.hub.getViTrigger` 호출은 **try 밖**이다. 여기서 예외가 나면 `recordUnmatched` 가 reject 되고 아무도 잡지 않으며, `index.ts:220` 의 핸들러가 `unhandledRejection` 을 `logger.fatal` + **프로세스 종료**로 다룬다 — 통보 1건의 파손이 그 순간 접속한 **모든 사용자의 DMA 세션**을 끊는다.
- **GC-WR-02.** `ensureRow` 의 in-flight 키가 `${userId}|${notice.orderNo}` 다(:435). 접수 전 거부("R")는 `orderNo === ""` 로 오므로 그 사용자의 **모든** 빈 주문번호 통보가 같은 키 `"user|"` 를 공유한다. 동시에 도착한 서로 다른 자동주문 거부 2건이 첫 번째의 Promise 를 재사용해 **같은 `row.id`** 를 받고, 두 `patch` 가 그 한 행에 차례로 덮어써진다 — 한 건의 거부 기록이 사라진다. 게다가 `OrderStore.findIdByOrderNo` 는 `orderNo === ""` 에서 항상 `null` 이므로(`orders.ts:428`) 이 키에는 dedup 의 의미가 애초에 없다.

Purpose: TRADE-03 의 「`dma_orders` insert/update 를 relay 가 전담」이 **전담이 정확할 때만 성립한다**는 사실을 코드로 지킨다.
Output: manual 분기 조회 경유 + 0행 error 로그 · stdout 감사 사본 · `.catch` 방어 · 빈 주문번호 in-flight 제외 · 회귀 테스트 4케이스.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-CONTEXT.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-18-SUMMARY.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-27-SUMMARY.md
</context>

<interfaces>
<!-- 16-27 이 같은 파일을 먼저 손댄다. 그 SUMMARY 를 읽고 최신 줄번호를 다시 잡을 것. -->

`relay/src/ws/order-handler.ts`:
- `type EnsureResult = { kind:"row"; id } | { kind:"lookup-failed" } | { kind:"unavailable" }` — 「행을 못 만들었다」와 「조회 자체가 실패했다」를 갈라 두는 이유가 docstring 에 있다. `lookup-failed` 는 **`order_no` 셀렉터로 열화 갱신**(S-5), `unavailable` 은 드롭
- `async function recordUnmatched(userId, notice): Promise<void>` — `manual` 분기(조회 없이 `{...patch, userId}`) / 그 외(`ensureRow`)
- `async function ensureRow(userId, notice): Promise<EnsureResult>` — `inflight` Map(`${userId}|${orderNo}`), 내부 `task` 안에서 `findIdByOrderNo` → `autoInsertRow` → `insertRequest`
- `autoInsertRow(userId, notice)` — `deps.symbols.lookup` · `deps.hub.getLimitChasers` · `deps.hub.getViTrigger` 를 부른다. **현재 try 밖**
- 통보 소비 루프 끝: `void recordUnmatched(userId, notice);`
- `patchOf(notice): OrderUpdate` — insert 든 update 든 같은 값

`relay/src/store/orders.ts`:
- `findIdByOrderNo(userId, orderNo): Promise<string | null>` — `orderNo === ""` 또는 `userId === ""` 면 `null`, sink 미결선이면 throw
- `selectorOf` — `orderRowId` 있으면 `id` 셀렉터, 없고 `userId`+`orderNo` 있으면 `order_no` 셀렉터(3축), 둘 다 아니면 `null`(드롭 로그)

`relay/src/hub/subscription-hub.ts:689` `#onOrderNotice(userId, notice)` — `RelayOrderMsg` 를 만들어 `#fanout` 하고 `this.emit("order", ...)`. **로그가 없다**

`relay/src/index.ts:220-223` — `unhandledRejection` → `logger.fatal` + 프로세스 종료
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 수동 통보도 조회를 거치고, 붙을 행이 없으면 통보 원문을 error 로 남긴다 (GC-CR-02 + D-24 stdout 사본)</name>
  <files>relay/src/ws/order-handler.ts, relay/src/hub/subscription-hub.ts, relay/tests/ws-order.test.ts</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-27-SUMMARY.md` — **먼저 읽는다.** 같은 파일을 방금 고쳤다(하드 필터 · await 가드). 줄번호가 옮겨졌을 수 있다
    - `relay/src/ws/order-handler.ts` 파일 머리말(Pitfall 18 선언 문단), `recordUnmatched` 전문과 그 docstring(「수동 주문은 이 분기를 타지 않는다」는 **현재 주석의 전제가 틀렸다** — 좁히기 실패·연결 종료 경로가 실제로 여기로 온다), `ensureRow` 전문, `patchOf`
    - `relay/src/store/orders.ts` L400~435 (`findIdByOrderNo` 의 빈 값 방어), L590~615 (`selectorOf` — `order_no` 셀렉터가 만들어지는 조건), L440~460 (셀렉터 `null` 드롭 로그)
    - `relay/src/hub/subscription-hub.ts` L675~705 (`#onOrderNotice` 전문 + 그 위 docstring)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §GC-CR-02 — Issue 2경로 + Fix 스니펫(조회 → `orderRowId` / 없으면 `logger.error` / 조회 실패 시 열화 갱신)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md` `gaps[1]` reason ① 과 `missing[0]`
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-CONTEXT.md` — D-24(두 번째 감사 사본) · S-5(무로그 fail-safe 금지) 원문
  </read_first>
  <action>
① `recordUnmatched` 의 `manual` 분기를 자동 분기와 **같은 조회**를 거치게 바꾼다. 16-REVIEW §GC-CR-02 의 Fix 스니펫을 참조하되 이 파일의 `EnsureResult` 규율과 어긋나지 않게 한다. 세 갈래를 명확히 가른다:
  - 조회 성공 + 행 있음 → `enqueueUpdate({ ...patch, orderRowId: id })`. **`order_no` 셀렉터는 「행이 있음이 확인된」 경우에만 쓴다**는 규율을 주석으로 못박는다.
  - 조회 성공 + 행 없음 → **`logger.error` 로 통보 원문을 남긴다.** 필드: `orderNo`·`noticeType`·`resultCode`·`isin`·`quantity`·`price`(계좌번호는 없다 — 통보에 실려 오지 않는다). 문구는 「대기·행 어디에도 붙지 않는 수동 통보 — stdout 이 유일한 기록이다」. 그리고 **갱신을 큐에 넣지 않는다**(0행 update 를 보내지 않는다).
  - 조회 실패(throw) → 기존 `lookup-failed` 규율과 동형으로 `enqueueUpdate({ ...patch, userId })` 열화 갱신을 시도하고, 실패 사실을 `logger.error` 로 남긴다 — 「조회가 죽었다고 통보를 버리지 않는다」(S-5).

② `recordUnmatched` 의 docstring 을 사실과 맞춘다. 현재의 「수동 주문은 이 분기를 타지 않는다」 전제를 **좁히기 실패·연결 종료 후 도착 두 경로가 실제로 여기로 온다**로 정정하고, 그래서 조회가 필요하다는 근거를 잇는다.

③ **D-24 의 두 번째 감사 사본**을 `subscription-hub.ts` `#onOrderNotice` 에 넣는다. `#fanout` 직전 또는 직후에 `logger.info` 한 줄 — 필드는 `userId`·`orderNo`·`noticeType`·`resultCode`·`origin`(있다면) 수준으로 하고, **계좌번호·비밀번호·DMA user_id 는 싣지 않는다**(D-19 승계). 문구에 「통보 수신(감사 사본)」이 드러나게 한다. 이 로그가 있으면 `dma_orders` 에 붙지 못한 통보라도 대조할 수 있다.

④ 테스트 2케이스를 `ws-order.test.ts` 에 추가한다.
  - 「좁히기에 실패한 수동 통보는 `order_no` 셀렉터 갱신을 큐에 넣지 않고 error 로그를 남긴다」 — `enqueueUpdate` 스텁 호출 0건(또는 `orderRowId` 없는 갱신 0건)과 `findIdByOrderNo` 호출 1회를 단언한다. logger 는 이 테스트 파일의 기존 방식(스파이 또는 주입)을 따른다.
  - 「연결 종료 후 도착한 수동 통보(후보 0건)도 같은 경로로 가고 사라지지 않는다」 — `closeConn` 후 통보를 주입해 같은 단언.
  기존 테스트 ㉑ (좁히기 실패)이 이 변경으로 깨지면, **그 케이스가 지금까지 관찰하지 않던 뒷부분**을 새로 단언하도록 확장한다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay exec vitest run tests/ws-order.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `recordUnmatched` 의 manual 분기에 `findIdByOrderNo` 호출이 있다 (줄번호를 SUMMARY 에 인용)
    - `grep -c "대기·행 어디에도 붙지 않는 수동 통보" relay/src/ws/order-handler.ts` >= 1
    - manual 분기에 `enqueueUpdate({ ...patch, userId })` 형태의 **무조건** 갱신이 남아 있지 않다 — 조회 실패 갈래에서만 나온다 (소스 확인 후 SUMMARY 에 인용)
    - `grep -c "감사 사본" relay/src/hub/subscription-hub.ts` >= 1
    - `grep -c "logger.info\|logger.error" relay/src/hub/subscription-hub.ts` 값이 변경 전보다 증가 (전후 값 SUMMARY 인용)
    - `pnpm --filter @gh-radar/relay exec vitest run tests/ws-order.test.ts` exit 0 — 신규 2케이스 포함
    - 신규 케이스 이름에 「0행」 또는 「붙지 않는」 이 포함 (`grep -c "붙지 않는\|0행" relay/tests/ws-order.test.ts` >= 1)
  </acceptance_criteria>
  <done>수동 통보가 침묵으로 사라지는 경로가 없고, 붙지 못한 통보는 stdout 에 원문이 남는다.</done>
</task>

<task type="auto">
  <name>Task 2: 통보 기록 경로의 예외를 격리하고, 빈 주문번호를 in-flight 에서 뺀다 (GC-WR-01 + GC-WR-02)</name>
  <files>relay/src/ws/order-handler.ts, relay/tests/ws-order.test.ts</files>
  <read_first>
    - `relay/src/ws/order-handler.ts` 통보 소비 루프 끝의 `void recordUnmatched(userId, notice);`, `ensureRow` 전문(`inflight` 키 · `task` 안의 try 범위 · `finally` 의 키 해제), `autoInsertRow` 전문(`deps.symbols.lookup` · `deps.hub.getLimitChasers` · `deps.hub.getViTrigger` 호출부와 `null` 반환 조건)
    - `relay/src/index.ts` L215~230 — `unhandledRejection` 핸들러(`logger.fatal` + 프로세스 종료). **이 plan 이 무엇으로부터 무엇을 지키는지의 근거**
    - `relay/src/store/orders.ts` L420~435 — `findIdByOrderNo` 가 `orderNo === ""` 에서 항상 `null` 인 사실(빈 키에 dedup 의미가 없다는 증거)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §GC-WR-01 · §GC-WR-02 — Fix 스니펫 2종
    - Task 1 이 바꾼 `recordUnmatched` 최신 형태
  </read_first>
  <action>
① **GC-WR-01 — 두 겹으로 막는다.**
  - 호출부: `void recordUnmatched(userId, notice).catch((err) => logger.error({ err, orderNo: notice.orderNo }, "[WS-order] 통보 기록 경로 예외"));` — 주석에 「`index.ts` 의 `unhandledRejection` 은 **프로세스 종료**다. 통보 1건의 파손이 전 사용자 세션을 끊게 두지 않는다 (GC-WR-01)」를 적는다.
  - 원인 쪽: `autoInsertRow(userId, notice)` 호출을 `ensureRow` 의 `try` **안으로** 옮긴다. 그 안에서 예외가 나면 기존 insert 실패 경로와 같은 등급의 `logger.error`(「감사 기록 결손 (stdout 이 두 번째 사본이다)」 계열)를 남기고 `{ kind: "unavailable" }` 을 돌려준다. 두 겹 중 하나만 하면 원인은 남고 증상만 가린다 — 둘 다 한다.

② **GC-WR-02 — 빈 주문번호는 in-flight 를 태우지 않는다.** `ensureRow` 진입부에서 `notice.orderNo === ""` 이면 키를 만들지 않고 **바로 insert 경로**로 간다(조회는 어차피 `null` 이다). 16-REVIEW §GC-WR-02 의 Fix 스니펫을 참조하라. 별도 헬퍼(`insertOnly` 같은 이름)로 뽑아도 좋고 인라인이어도 좋되, **`inflight.set` 을 지나지 않는다**는 것이 결과다. 주석: 「빈 주문번호는 상관 키가 아니다 — 합치면 서로 다른 거부가 한 행에 겹친다. `findIdByOrderNo` 도 이 값에서 항상 `null` 이라 dedup 의 의미가 애초에 없다 (GC-WR-02)」.

③ `inflight` 맵의 `finally` 키 해제 규율과 「`closeConn` 은 `inflight` 을 건드리지 않는다」(WR-01 주석)는 그대로 유지한다.

④ 테스트 2케이스를 추가한다.
  - 「`autoInsertRow` 안에서 예외가 나도 프로세스가 죽지 않고 error 로그로 끝난다」 — `deps.symbols.lookup` 또는 `deps.hub.getLimitChasers` 스텁이 throw 하게 만들고, `recordUnmatched` 를 태우는 통보를 주입한 뒤 **unhandled rejection 이 발생하지 않음**을 단언한다(테스트 러너가 잡아 주는 형태 또는 명시 스파이).
  - 「빈 주문번호 거부 통보 2건이 서로 다른 행으로 기록된다」 — `insertRequest` 스텁 호출 2회 + 서로 다른 id 반환을 단언한다. 빈 주문번호가 아닌 통보의 in-flight 병합(16-18 이 만든 것)은 그대로 통과해야 한다 — 그 회귀 케이스가 이미 있으면 재사용한다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "recordUnmatched(userId, notice).catch" relay/src/ws/order-handler.ts` >= 1
    - `grep -c "통보 기록 경로 예외" relay/src/ws/order-handler.ts` >= 1
    - `autoInsertRow` 호출이 `ensureRow` 의 `try` 블록 안에 있다 (try 시작 줄과 호출 줄 번호를 SUMMARY 에 인용)
    - `ensureRow` 진입부에 `notice.orderNo === ""` 분기가 있고 그 갈래가 `inflight.set` 을 지나지 않는다 (줄번호 인용)
    - 신규 2케이스가 테스트 파일에 존재 (케이스 이름 SUMMARY 인용)
    - `pnpm --filter @gh-radar/relay test` exit 0
    - `pnpm --filter @gh-radar/relay run typecheck` · `typecheck:tests` exit 0
  </acceptance_criteria>
  <done>통보 1건의 파손이 프로세스를 내리지 않고, 빈 주문번호 거부들이 서로를 덮어쓰지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트웨이 통보 → `dma_orders` | 통보가 붙을 행이 있는지는 relay 만 안다. 확인 없이 갱신하면 PostgREST 는 0행을 성공으로 답한다 |
| 통보 1건의 예외 → relay 프로세스 | `unhandledRejection` 이 프로세스 종료이므로, 잡지 않은 reject 하나가 전 사용자 세션 절단이다 |
| 빈 주문번호(`""`) → in-flight 키 공간 | 「없는 값」이 키가 되면 서로 다른 사건이 하나로 합쳐진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-66 | Repudiation (무로그 기록 소실) | `recordUnmatched` manual 분기 | mitigate | 조회를 거쳐 `orderRowId` 로만 갱신. 행이 없으면 통보 원문을 `logger.error` 로 남기고 **갱신을 보내지 않는다** (Pitfall 18 / S-5). 0행·연결 종료 두 경로를 테스트로 잠금 |
| T-16-67 | Repudiation (감사 사본 부재) | `#onOrderNotice` | mitigate | 통보 수신 시 stdout 감사 사본 1줄 (D-24). 계좌번호·자격증명 미포함 (D-19 승계) |
| T-16-68 | Denial of Service (전 사용자 세션 절단) | `void recordUnmatched(...)` | mitigate | 호출부 `.catch` + `autoInsertRow` 를 try 안으로. 두 겹으로 막는다 — 증상만 가리지 않는다 |
| T-16-69 | Tampering (기록 덮어쓰기) | `ensureRow` 의 빈 주문번호 in-flight 키 | mitigate | `orderNo === ""` 는 in-flight 을 태우지 않고 각자 insert. 「서로 다른 행 2건」으로 잠금 |
| T-16-45 | Information Disclosure | 새 error/info 로그 | mitigate | 통보 원문 필드만(계좌번호는 통보에 없다). 자격증명·DMA user_id 미포함 |
| T-16-13 | 운영 사고 | 실서버·실계좌 접속 | accept | FakeGateway·스텁만 쓴다 (D-27 승계) |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/relay test` exit 0
- `pnpm --filter @gh-radar/relay run typecheck` · `typecheck:tests` exit 0
- `grep -rn "10\.41\.1\.120" relay/` 0건 (D-27 승계)
</verification>

<success_criteria>
- 수동 통보가 0행 갱신으로 사라지지 않는다
- 모든 통보에 stdout 감사 사본이 있다
- 통보 기록 경로의 예외가 프로세스를 내리지 않는다
- 빈 주문번호 통보들이 각자의 행을 갖는다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-33-SUMMARY.md` when done
</output>
