---
phase: 16-trading-limit-chaser-vi-my-page
plan: 30
type: execute
wave: 19
depends_on: ["16-21"]
gap_closure: true
files_modified:
  - relay/src/dma/session-manager.ts
  - relay/src/order/order-api.ts
  - relay/tests/session-manager.test.ts
  - relay/tests/order-api.test.ts
  - scripts/smoke-relay.sh
autonomous: true
requirements: [TRADE-03]

must_haves:
  truths:
    - "게이트웨이 장애 중에 relay 가 재시작해도 `/healthz` 가 영원히 초록으로 남지 않는다 — 「생성 후 N분이 지나도록 한 번도 Ready 가 아닌 세션」이 degraded 로 잡힌다 (GC-WR-07)"
    - "한 번도 Ready 인 적 없는 환경의 **부팅 직후 유예 구간**은 여전히 200 이다 — 16-21 이 gap 4 로 얻은 것을 잃지 않는다"
    - "smoke INV-9 프로브가 Supabase 액세스 토큰을 **argv 로 넘기지 않는다** — `ps` 로 다른 프로세스가 읽을 수 없다 (GC-WR-11)"
    - "프로브가 판정을 찍은 뒤 비정상 종료해도 판정 문자열이 **이어 붙지 않는다** — FAIL 이 SKIP 으로 강등되지 않는다 (GC-WR-11)"
  artifacts:
    - path: "relay/src/dma/session-manager.ts"
      provides: "생성 시각 기반 stalledCount"
      contains: "stalled"
    - path: "relay/src/order/order-api.ts"
      provides: "stalledCount 를 포함한 degraded 판정"
      contains: "stalled"
    - path: "scripts/smoke-relay.sh"
      provides: "env 토큰 전달 + verdict 덮어쓰기"
      contains: "SMOKE_TOKEN"
  key_links:
    - from: "relay/src/order/order-api.ts (/healthz)"
      to: "relay/src/dma/session-manager.ts (stats)"
      via: "stats().stalledCount 를 sessionsOk 판정에 포함"
      pattern: "stalledCount"
    - from: "scripts/smoke-relay.sh (ws_order_probe)"
      to: "프로브 node 프로세스"
      via: "SMOKE_TOKEN 환경변수 (argv 아님)"
      pattern: "SMOKE_TOKEN="
---

<objective>
**GC-WR-07 + GC-WR-11** 을 닫는다. 둘 다 「우리가 장애를 **관측하는 수단**」이 스스로를 무력화하는 문제다 — 하나는 uptime check, 하나는 smoke 판정이다.

지금 상태:
- **GC-WR-07.** 16-21 이 gap 4 를 닫으며 `sessionsOk = everReadyCount === 0 || readyCount > 0` 으로 바꿨다(`order-api.ts:223`). `hasBeenReady` 는 `DmaSession` 인스턴스의 **인메모리 래치**이므로 relay 컨테이너가 재시작하면 0 으로 초기화된다. 게이트웨이가 죽어 있는 동안 relay 가 재배포·OOM·크래시로 **한 번만** 재시작하면, 그 뒤로는 사용자가 아무리 붙어도 `everReadyCount === 0` 이라 uptime check 가 **영원히 초록**이다. 예전 규칙(`sessionCount > 0 && readyCount === 0` → degraded)이 잡던 사례가 통째로 빠졌다. `vpn` 신호는 인터페이스 존재만 보므로(터널은 살아 있고 게이트웨이 프로세스만 죽은 경우) 이를 대체하지 못한다.
- **GC-WR-11.** ① `node "$js" "wss://…" "$ws_module" "$token"` — Supabase 액세스 토큰이 **프로세스 목록(`ps`)에 노출**된다(`smoke-relay.sh:465`). ② 프로브가 판정을 stdout 에 찍은 **뒤** 비정상 종료하면 `rc != 0` 분기가 `inconclusive` 를 **덧붙인다**(`:468`). 결과 문자열이 `reachableinconclusive` 가 되어 `case` 의 어느 갈래에도 매치하지 않고 `*` → **SKIP** 이 된다. 실패 판정이 조용히 「확인 안 함」으로 바뀐다 — 이 검사가 3갈래인 이유를 스스로 무너뜨린다.

**이 plan 은 16-21 을 되돌리지 않는다.** `everReadyCount === 0` 유예는 유지하되, 그 유예에 **시간 상한**을 붙인다.

Purpose: 장애 탐지 수단이 장애를 놓치지 않게 한다. TRADE-03 실서버 결선(인간 검증 #1·#2) 전에 반드시 닫혀야 하는 자리다.
Output: `stalledCount` 판정 · env 토큰 전달 · verdict 덮어쓰기 · 회귀 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-21-SUMMARY.md
</context>

<interfaces>
<!-- 손대는 자리의 현재 계약. -->

`relay/src/dma/session-manager.ts`:
- `export type SessionStats = { sessionCount: number; readyCount: number; everReadyCount: number }` — 「optional 이 아니다. 필수 필드여야 소비자·테스트가 컴파일 단계에서 갱신을 강제받는다」(:57-62). **새 필드도 같은 규율로 넣는다**
- `type Entry = { session: DmaSession; refCount: number; graceTimer: NodeJS.Timeout | null }` — 생성 시각이 없다
- `stats()` — `#sessions` 를 훑어 `isReady` / `hasBeenReady` 를 센다
- `#create(userId, creds)` — `this.#sessions.set(userId, { session, refCount: 1, graceTimer: null })`
- `export const SESSION_GRACE_MS = 300_000` — 상수 선언 관례(값 + 근거 주석)

`relay/src/order/order-api.ts`:
- `export type HealthPayload = {...}`(:82) — `/healthz` 응답 본문 타입
- `/healthz` 핸들러(:215-235): `const stats = deps.sessions.stats()` → `linkUp` → `sessionsOk` → `healthy` → `res.status(healthy ? 200 : 503).json(payload)`
- `:177-210` ★ 2026-09-09 문단 — 판정 변경의 근거가 문서화된 자리. **이번 변경도 같은 자리에 이어 적는다**

`scripts/smoke-relay.sh`:
- `ws_order_probe()`(:335-470) — `token="${SMOKE_AUTH_TOKEN:-}"`, 프로브 JS 를 `mktemp -d` 에 쓰고 `node "$js" url ws_module token` 실행. 프로브 안에서 `const token = process.argv[4];`(:372)
- 프로브는 stdout 에 판정 한 단어만 찍는다(`console.log(verdict)`), 사유는 stderr
- 호출부(:646-660): `ORDER_PATH_VERDICT="$(ws_order_probe)"` → `case` 로 `reachable`/`unreachable`/그 외
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 「생성 후 N분 미Ready」 세션을 세어 degraded 판정에 넣는다 (GC-WR-07)</name>
  <files>relay/src/dma/session-manager.ts, relay/src/order/order-api.ts, relay/tests/session-manager.test.ts, relay/tests/order-api.test.ts</files>
  <read_first>
    - `relay/src/dma/session-manager.ts` L25~70 (`SESSION_GRACE_MS` 선언 관례 + `SessionStats` docstring), L190~239 (`stats()` · `#create()`)
    - `relay/src/order/order-api.ts` L170~240 (`/healthz` docstring 전문 — 15-05 원문과 ★ 2026-09-09 문단, 그리고 판정·페이로드 코드). **「Ready 였다가 죽은 세션은 여전히 degraded 다」와 「`vpn` 은 세션과 독립이다」 두 문장을 깨지 않는 것이 이 task 의 제약이다**
    - `relay/tests/order-api.test.ts` L220~290 — 기존 4케이스(never-Ready 200 · Ready 였다가 죽음 503 · vpn false 503 · sessionCount 페이로드). 새 케이스는 이 형식을 따른다
    - `relay/tests/session-manager.test.ts` — 가짜 시계·타이머를 쓰는 기존 방식(`vi.useFakeTimers` 여부)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §GC-WR-07 — Issue + Fix 스니펫
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-21-SUMMARY.md` — 판정 완화가 무엇을 의도했는지 (되돌리지 않는다)
  </read_first>
  <action>
① `session-manager.ts` 에 상한 상수를 선언한다. 이름은 `STALE_SESSION_MS`, 값은 **부팅·로그인 왕복의 정상 구간을 넉넉히 덮는** 값으로 정하고(예: 5분 — `SESSION_GRACE_MS` 와 같은 눈금이면 그 사실을 근거로 적는다), docstring 에 「이 유예 안에서는 never-Ready 가 정상이다. 넘으면 게이트웨이가 응답하지 않는다는 뜻이다」를 적는다. env 로 덮을 수 있게 할지는 실행자 판단이되, 덮는다면 `config` 관례를 따른다.

② `Entry` 에 `createdAt: number` 를 추가하고 `#create` 에서 `Date.now()` 로 채운다. **세션 인스턴스가 아니라 매니저 엔트리에 둔다** — `session.ts` 를 건드리지 않는다(래치 `hasBeenReady` 의 의미를 흐리지 않기 위해서다. 그 이유를 주석에 적는다).

③ `SessionStats` 에 `stalledCount: number` 를 **필수 필드로** 추가한다(기존 규율 그대로 — optional 로 두면 소비자가 갱신을 강제받지 않는다). `stats()` 에서 `now - entry.createdAt > STALE_SESSION_MS && !entry.session.hasBeenReady` 인 세션을 센다. 시각 소스는 테스트가 제어할 수 있어야 하므로 주입 가능한 형태(생성자 옵션 `now?: () => number` 등)를 고려하되, 기존 테스트를 깨지 않는 기본값을 둔다.

④ `order-api.ts` 의 판정을 바꾼다: `sessionsOk = (stats.everReadyCount === 0 && stats.stalledCount === 0) || stats.readyCount > 0`. `HealthPayload` 에 `stalledCount` 를 추가해 **진단 가능**하게 한다(`sessionCount` 와 같은 취급 — 페이로드에는 있고 식별자는 없다).

⑤ `/healthz` docstring 의 ★ 2026-09-09 문단 **뒤에** 새 문단을 잇는다: 「★ 2026-09-09 보강 (16-30 / GC-WR-07) — `hasBeenReady` 는 **프로세스 메모리 래치**다. 게이트웨이 장애 중에 relay 가 한 번 재시작하면 `everReadyCount` 가 0 으로 초기화되어 진짜 장애가 영원히 `ok` 가 된다. 그래서 「생성 후 `STALE_SESSION_MS` 가 지나도록 한 번도 Ready 가 아닌 세션」을 함께 센다. 부팅 직후의 정상 구간은 그 유예가 흡수하고, 그 시간을 넘긴 미Ready 는 예전 규칙(`sessionCount > 0 && readyCount === 0`)이 잡던 사례다.」

⑥ 테스트를 추가한다.
  - `session-manager.test.ts`: 「생성 직후에는 `stalledCount` 가 0 이고, `STALE_SESSION_MS` 를 넘기면 1 이 된다」 · 「Ready 를 한 번이라도 본 세션은 시간이 지나도 `stalled` 로 세지 않는다」.
  - `order-api.test.ts`: 「`everReadyCount:0` + `stalledCount:1` → **degraded 503**」 · 「`everReadyCount:0` + `stalledCount:0` → **ok 200**(16-21 유예 유지 회귀 방지)」. 기존 4케이스는 그대로 통과해야 하며, `SessionStats` 필드 추가로 스텁이 컴파일 에러를 내면 그것이 이 설계의 의도다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay exec vitest run tests/order-api.test.ts tests/session-manager.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "stalledCount" relay/src/dma/session-manager.ts` >= 2 (타입 + 계산)
    - `grep -c "stalledCount" relay/src/order/order-api.ts` >= 2 (판정 + 페이로드)
    - `grep -c "STALE_SESSION_MS" relay/src/dma/session-manager.ts` >= 2 (선언 + 사용)
    - `grep -c "createdAt" relay/src/dma/session-manager.ts` >= 2
    - `grep -c "stalledCount" relay/tests/order-api.test.ts` >= 2 — degraded·ok 두 케이스
    - `git diff --stat relay/src/dma/session.ts` 변경 0줄 — 래치의 의미를 건드리지 않았다
    - `pnpm --filter @gh-radar/relay test` exit 0 (기존 order-api 4케이스 포함)
  </acceptance_criteria>
  <done>게이트웨이 장애 + relay 재시작 조합이 더 이상 초록으로 보고되지 않고, 부팅 직후 유예는 그대로다.</done>
</task>

<task type="auto">
  <name>Task 2: smoke INV-9 의 토큰 노출과 판정 강등을 고친다 (GC-WR-11)</name>
  <files>scripts/smoke-relay.sh</files>
  <read_first>
    - `scripts/smoke-relay.sh` L317~340 (INV-9 판정표 주석 — 「확인하지 않았다」를 PASS 로 세지 않는다는 이 검사의 존재 이유), L360~400 (프로브 JS 머리말 + `finish()` — stdout 은 판정 한 단어, stderr 은 사유), L365~375 (`process.argv` 로 url·ws 모듈·token 을 읽는 자리), L460~470 (`node "$js" …` 실행 + `rc != 0` 분기), L640~665 (호출부 `ORDER_PATH_VERDICT` 와 `case`)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §GC-WR-11 — 두 결함 + Fix 스니펫
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md` `human_verification[1]` — 이 프로브가 **아직 한 번도 실행된 적이 없다**는 사실. 이 task 는 그 첫 실행을 안전하게 만드는 준비다
    - `scripts/smoke-relay.sh` 의 다른 프로브(`ws_auth_probe`)가 비밀을 어떻게 넘기는지 — 같은 관례를 따를 수 있으면 따른다
  </read_first>
  <action>
① **토큰을 argv 에서 env 로 옮긴다.** 실행을 `SMOKE_TOKEN="$token" node "$js" "wss://${HOST}/ws" "$ws_module"` 로 바꾸고, 프로브 JS 안의 `const token = process.argv[4];` 를 `const token = process.env.SMOKE_TOKEN;` 으로 바꾼다. 프로브 머리말 주석에 「토큰은 **env 로만** 넘긴다 — argv 는 `ps` 로 같은 호스트의 다른 프로세스가 읽는다 (GC-WR-11 / T-16-56)」를 적는다. 토큰이 비어 있으면 프로브가 `inconclusive` 로 즉시 끝나는 기존 방어는 유지한다.

② **판정 문자열을 덧붙이지 않고 덮어쓴다.** 현재의 `if [[ "$rc" -ne 0 ]]; then printf 'inconclusive'; fi` 를, 프로브 stdout 을 변수에 받아 두고 `rc != 0` 이면 그 변수를 `inconclusive` 로 **대입**한 뒤 마지막에 한 번만 `printf '%s' "$verdict"` 하는 형태로 바꾼다. 16-REVIEW §GC-WR-11 의 Fix 스니펫을 참조하라. `rm -rf "$dir"` 정리는 어느 경로에서도 반드시 실행되게 유지한다.

③ 호출부(`case "$ORDER_PATH_VERDICT"`)는 그대로 둔다 — 이 task 는 그 `case` 가 **의도한 세 값만** 받게 만드는 것이 목적이다. 다만 `*` 갈래(예상 밖 문자열)가 SKIP 으로 조용히 넘어가지 않도록, 그 갈래에 관측한 문자열 원문을 남기는 메시지가 있는지 확인하고 없으면 추가한다 — **토큰은 절대 로그에 싣지 않는다**.

④ 스크립트 문법과 동작을 확인한다. 토큰 없이 실행하면 INV-9 는 여전히 SKIP 이고 나머지 검사는 이전과 같은 결과여야 한다. `bash -n scripts/smoke-relay.sh` 로 문법을, 실제 실행으로 회귀를 확인한다(프로덕션 엔드포인트를 치므로 결과 요약을 SUMMARY 에 인용한다).
  </action>
  <verify>
    <automated>bash -n scripts/smoke-relay.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SMOKE_TOKEN=' scripts/smoke-relay.sh` >= 1 — env 전달이 실재
    - `grep -c 'process.env.SMOKE_TOKEN' scripts/smoke-relay.sh` >= 1
    - `grep -c 'process.argv\[4\]' scripts/smoke-relay.sh` == 0 — argv 토큰이 사라졌다
    - `grep -vc '^\s*#' /dev/null; grep -n 'node "\$js"' scripts/smoke-relay.sh` 의 해당 줄에 `"$token"` 인자가 없다 (줄 원문을 SUMMARY 에 인용)
    - `printf 'inconclusive'` 가 **판정 출력 경로에서 덧붙이는 형태로 남아 있지 않다** — `rc != 0` 분기가 대입이고 출력은 한 곳뿐임을 줄번호로 SUMMARY 에 인용
    - `bash -n scripts/smoke-relay.sh` exit 0
    - `bash scripts/smoke-relay.sh` 실행 결과(PASS/FAIL/SKIP 수)가 이 plan 전후로 동일 — SUMMARY 에 인용
  </acceptance_criteria>
  <done>토큰이 `ps` 에 노출되지 않고, 프로브의 FAIL 이 SKIP 으로 강등되지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay 프로세스 메모리 ↔ 운영 판정 | 인메모리 래치는 재시작으로 사라진다 — 그것을 장애 판정의 유일 근거로 삼으면 재시작이 곧 관측 실명이다 |
| smoke 실행 호스트의 프로세스 목록 | 같은 호스트의 다른 사용자·프로세스가 `ps` 로 argv 를 읽는다 |
| smoke 판정 문자열 → CI/사람의 결론 | 문자열 한 개가 「실패」와 「확인 안 함」을 가른다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-56 | Information Disclosure | `ws_order_probe` 의 argv 토큰 | mitigate | `SMOKE_TOKEN` env 로만 전달. 프로브는 `process.env` 로 읽는다. 토큰을 로그·에러 메시지에 싣지 않는다 |
| T-16-57 | Repudiation (판정 위조) | `rc != 0` 분기의 문자열 덧붙임 | mitigate | 판정을 변수에 받고 실패 시 **덮어쓴다**. 출력은 한 곳. `case` 의 `*` 갈래가 관측 문자열을 남긴다 |
| T-16-58 | Denial of Service (탐지 실명) | `/healthz` 의 `everReadyCount` 단독 판정 | mitigate | `stalledCount` 를 판정에 넣어 「재시작 후 영원히 초록」을 막는다. 부팅 유예는 `STALE_SESSION_MS` 가 흡수 |
| T-16-26 | 탐지 회귀 | `hasBeenReady` 래치 | mitigate | **지우지 않는다.** 「Ready 였다가 죽은 세션은 여전히 degraded」 케이스를 회귀로 유지 |
| T-16-28 | 운영 사고 (실주문) | smoke INV-9 프로브 | accept | 화이트리스트 밖 계좌번호 + 미해석 ISIN — 게이트웨이 송신·DB insert 이전에 끝난다 (16-21 승계, 이 plan 이 바꾸지 않는다) |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/relay test` exit 0
- `pnpm --filter @gh-radar/relay run typecheck` · `typecheck:tests` exit 0
- `bash -n scripts/smoke-relay.sh` exit 0
- `bash scripts/smoke-relay.sh` 결과가 변경 전과 동일 (토큰 미설정 시 INV-9 SKIP)
</verification>

<success_criteria>
- 재시작 후에도 게이트웨이 장애가 degraded 로 보고된다
- 부팅 직후 never-Ready 유예(16-21)는 유지된다
- 토큰이 argv 에 없다
- 판정 문자열이 이어 붙지 않는다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-30-SUMMARY.md` when done
</output>
