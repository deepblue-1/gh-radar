---
phase: 16-trading-limit-chaser-vi-my-page
plan: 26
type: execute
wave: 18
depends_on: ["16-18", "16-19", "16-20", "16-21", "16-22", "16-23", "16-24", "16-25"]
gap_closure: true
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md
  - .planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md
autonomous: false
requirements: [TRADE-01, TRADE-02, TRADE-03, NAV-01, MYPAGE-01]

must_haves:
  truths:
    - "전체 스위트(typecheck 13 워크스페이스 · 전 워크스페이스 단위 · Playwright 전량)가 갭 수정 후에도 green 이다 — 9개 plan 의 변경이 서로를 깨지 않았다"
    - "relay·server·webapp 3종이 갭 수정본으로 재배포됐다 (또는 사용자가 보류를 택했고 그 사실이 STATE 에 남았다)"
    - "**로그인 사용자가 붙어 never-Ready 세션이 생긴 상태에서도** `https://dma.jx1.io/healthz` 가 200 이다 — gap 4 해법 ③ 이 프로덕션에서 실제로 동작한다 (배포 직후 세션 0 인 200 은 증거가 아니다)"
    - "`scripts/smoke-relay.sh` 가 새 INV-9(wss 주문 왕복)로 통과하거나, SKIP 사유가 `SMOKE_AUTH_TOKEN` 미설정임이 기록된다"
    - "닫힌 14건과 남은 항목이 VALIDATION·STATE·ROADMAP·REQUIREMENTS 에 정직하게 반영된다 — 과장(「게이트웨이가 살아났다」) 없이"
  artifacts:
    - path: ".planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md"
      provides: "갭 클로징 결과 + 배포 실측 기록"
      contains: "16-18"
    - path: ".planning/STATE.md"
      provides: "Phase 16 갭 클로징 완료 상태"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TRADE-03 · MYPAGE-01 재판정"
  key_links:
    - from: "scripts/deploy-relay.sh"
      to: "GCE VM radar-gw"
      via: "컨테이너 재배포 후 /healthz 확인"
      pattern: "deploy-relay"
---

<objective>
갭 클로징 9개 plan(16-18 ~ 16-25)의 결과를 **프로덕션까지 밀어 넣고 실측**한 뒤, 문서를 정직하게 갱신한다.

배포가 필요한 이유:
- **relay** — 16-18(사용자·당일 경계) · 16-21(`/healthz` 판정) · 16-22(통보 상관 축·중복 가드) · 16-24(VI 상한·flushNow) · 16-25(lc.set 시장 소유권)가 전부 relay 코드다. 그중 16-21 은 **배포해야 의미가 생긴다**(현재 프로덕션 `/healthz` 는 503 이다).
- **server** — 16-20 이 `ORDER_COLS`·`DmaOrderRow`·`errors.ts` 를 바꿨다.
- **webapp** — 16-19(킬 스위치) · 16-20(죽은 모듈 삭제) · 16-23(계좌축) · 16-24(VI 상한 UI) · 16-25(시장 추정 제거)가 사용자 표면이다.
- **DB** — 16-18 이 `supabase db push` 로 이미 적용했다(이 plan 에서 다시 하지 않는다).

**gap 4 의 진짜 검증 조건.** 배포 직후에는 세션이 0이라 `/healthz` 가 무조건 200 이다 — 그것은 ③ 의 증거가 아니다. **로그인 사용자가 붙어 never-Ready 세션이 생긴 상태에서 200 인지**를 봐야 한다. 그래서 배포 후 확인 단계에 사람이 필요하다.

Purpose: 코드에서 닫은 것을 운영에서도 닫는다.
Output: 전체 green · 배포 3종 · `/healthz` 세션 있는 상태 200 실측 · smoke · 문서 4종 갱신.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 스위트 green 확인 (배포 전 게이트)</name>
  <files>(코드 변경 없음 — 실패 시에만 원인 파일을 고친다)</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-18-SUMMARY.md` ~ `16-25-SUMMARY.md` 8건 — 각 plan 이 무엇을 바꿨고 어떤 테스트를 추가했는지. 실패가 나면 **어느 plan 의 경계인지** 여기서 판별한다
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-17-PLAN.md` Task 1 — 직전 전체 스위트 실행 절차(E2E 단일 워커·예상 소요)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md` — 선행 실패로 알려진 항목이 남아 있는지(16-17 이 3건을 해소했다)
  </read_first>
  <action>
순서대로 돌린다. 앞 단계가 실패하면 뒤를 돌리지 않는다.
1. `pnpm typecheck` — 13 워크스페이스. 16-25 가 shared 계약을 바꿨으므로 `@gh-radar/shared` 빌드가 선행돼야 한다(루트 스크립트가 이미 그렇게 한다).
2. `pnpm -r test` — shared / relay / server / webapp 전부. 기준선은 검증 시점 1,293 통과였고, 9개 plan 이 케이스를 늘렸으므로 **그보다 커야 한다**. 실제 수를 SUMMARY 에 인용한다.
3. `pnpm --filter @gh-radar/relay run typecheck:tests` — relay `tests/` 는 루트 typecheck 밖이다(deferred-items 기록).
4. `pnpm --filter gh-radar-webapp test:e2e` — Playwright 전량. 단일 워커라 순차 실행이며 5~8분이다. 기준선은 126 통과 · 0 실패 · 9 skip.

실패가 나면 **고치고 다시 돌린다.** 배포 전에 red 를 남기지 않는다. 다만 고칠 때 **어느 plan 의 경계인지 판별**해 그 파일만 손댄다 — 진단이 흐려지면 다음 검증이 무의미해진다. 이 phase 의 변경과 무관한 선행 실패라고 판단되면 고치지 말고 `deferred-items.md` 에 근거(재현 방법 + 무관함의 증거)와 함께 기록한다.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm -r test &amp;&amp; pnpm --filter @gh-radar/relay run typecheck:tests</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exit 0 (13 워크스페이스)
    - `pnpm -r test` exit 0, 총 통과 수가 1,293 **초과** (실제 수를 SUMMARY 에 인용)
    - `pnpm --filter @gh-radar/relay run typecheck:tests` exit 0
    - `pnpm --filter gh-radar-webapp test:e2e` exit 0, 실패 0 (통과/skip 수를 SUMMARY 에 인용)
    - `grep -rn "10\.41\.1\.120" relay/ webapp/src webapp/e2e scripts/` 0건 (D-27 승계 게이트)
  </acceptance_criteria>
  <done>배포해도 되는 상태임이 네 개 명령의 exit 0 으로 증명됐다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 배포 3종 승인·실행 + /healthz 실측 (세션이 있는 상태에서)</name>
  <files>(배포 실행 — 저장소 파일 변경 없음)</files>
  <read_first>
    - `scripts/deploy-relay.sh` — GCE VM `radar-gw` 컨테이너 재배포 절차, `HEALTH_HOST=dma.jx1.io`, 배포 후 로컬 `/healthz` 대기(L290~315), uptime check `gh-radar-relay-healthz` 재생성 경로
    - `scripts/deploy-server.sh` — Cloud Run 배포 인자와 필수 env
    - `scripts/smoke-relay.sh` — 16-21 이 바꾼 INV-9 를 포함한 INV-1~10
    - `scripts/smoke-server.sh` — server 계약 불변식
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-17-SUMMARY.md` — 직전 배포 3종의 실제 명령·결과(리비전 이름·버전 해시 형식)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-21-SUMMARY.md` — `/healthz` 판정 변경의 정확한 조건(무엇이 200 이고 무엇이 503 인지)
  </read_first>
  <what-built>
갭 클로징 9개 plan 의 코드가 전부 병합됐고 전체 스위트가 green 이다. 이제 프로덕션 3종 재배포와 `/healthz` 실측이 남았다.

배포 대상과 이유:
1. **relay** (GCE VM `radar-gw`) — 16-18·21·22·24·25. **16-21 은 배포해야 효과가 생긴다** (현재 프로덕션 `/healthz` 503).
2. **server** (Cloud Run `gh-radar-server`) — 16-20 의 `origin` 읽기 계약.
3. **webapp** (Vercel) — 16-19·20·23·24·25 의 사용자 표면.

DB 마이그레이션은 16-18 에서 이미 적용했다 — 여기서 다시 하지 않는다.
  </what-built>
  <how-to-verify>
1. **배포를 Claude 가 실행해도 되는지** 알려 주세요 (승인 / 직접 실행 / 보류).

2. 승인 시 Claude 가 실행할 명령 (순서 고정: relay → server → webapp):
   - relay: `GOOGLE_APPLICATION_CREDENTIALS=~/.config/gcloud/gh-radar-deployer.json CLOUDSDK_CORE_PROJECT=gh-radar bash scripts/deploy-relay.sh`
   - server: `bash scripts/deploy-server.sh` (필수 env `GCP_PROJECT_ID`·`SUPABASE_URL` 포함)
   - webapp: 저장소 **루트**에서 `vercel pull` → `vercel build` → `vercel deploy --prebuilt`
     (`ignoreCommand` 때문에 git push 배포가 skip 된 전례가 있어 수동 배포한다)

3. **배포 후 `/healthz` 확인 — 두 단계입니다. 두 번째가 진짜 검증입니다.**
   - ① 배포 직후: `curl -s https://dma.jx1.io/healthz` → 200 (세션 0 이므로 당연히 200 — **증거가 아닙니다**)
   - ② **로그인한 브라우저로 앱에 접속**해 relay 세션이 하나 생긴 뒤 다시:
     `curl -s https://dma.jx1.io/healthz` → **200 이고 본문이 `{"status":"ok", …, "sessionCount":1, "everReadyCount":0}`** 이어야 합니다.
     여기서 503 이 나오면 16-21 의 수정이 프로덕션에서 동작하지 않는 것입니다 — 본문 전체를 알려 주세요.

4. smoke: `bash scripts/smoke-relay.sh` · `bash scripts/smoke-server.sh`
   (`SMOKE_AUTH_TOKEN` 이 없으면 INV-9 는 SKIP 으로 떨어집니다 — FAIL 이 아닙니다. 토큰을 주시면 wss 주문 왕복까지 확인합니다. 그 프로브는 화이트리스트 밖 계좌번호를 쓰므로 **실계좌에 주문이 나가지 않습니다**.)

5. Cloud Monitoring 에서 `gh-radar-relay-down` 알림이 해소됐는지 확인해 주세요.

**바뀌지 않는 것(과장 방지):** 이 배포로 DMA 게이트웨이가 살아나지는 않습니다. 로그인 사용자는 여전히 트레이딩 3표면에서 DMA 게이트를 봅니다. 바뀌는 것은 **그 상태가 더 이상 relay 장애로 보고되지 않는다**는 것뿐입니다.
  </how-to-verify>
  <resume-signal>「배포 승인」(Claude 실행) / 「직접 실행」(사용자 실행 후 결과 공유) / 「보류」 중 하나. 3-② 의 `/healthz` 본문을 함께 알려 주세요.</resume-signal>
  <action>
사용자가 「배포 승인」이면 위 3종을 순서대로 실행하고, 각 단계마다 결과를 확인한 뒤 다음으로 넘어간다(앞 단계 실패 시 멈춘다).
- relay 배포 후 `scripts/deploy-relay.sh` 자체가 로컬 `/healthz` 200 을 기다린다 — 그 출력의 `version` 해시가 이번 커밋과 일치하는지 확인한다.
- server 배포 후 리비전 이름과 `gcloud run revisions describe` 의 env 이름 목록을 SUMMARY 에 인용한다. **`RELAY_*`·`ORDER_TIMEOUT_MS` 0건**(16-16 승계)을 함께 확인한다.
- webapp 배포 후 프로덕션 HTML 에서 Phase 16 사이드바 문자열(「상승률 상위」)이 여전히 검출되는지 확인한다(회귀 없음).
- 그다음 `/healthz` 를 **①·② 두 번** 재고, ② 의 본문 전체를 SUMMARY 에 인용한다. ② 를 위해서는 로그인 세션이 필요하므로 사용자에게 접속을 요청한다.
- smoke 2종을 돌리고 PASS/SKIP/FAIL 요약을 인용한다.

「직접 실행」이면 사용자가 공유한 출력을 그대로 SUMMARY 에 인용한다. 「보류」면 배포를 건너뛰고 **그 사실과 사유**를 SUMMARY·STATE 에 남긴다 — 미배포를 배포된 것처럼 적지 않는다.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}' https://dma.jx1.io/healthz</automated>
  </verify>
  <acceptance_criteria>
    - 배포 승인 시: `curl -s -o /dev/null -w '%{http_code}' https://dma.jx1.io/healthz` 가 `200`
    - **세션이 있는 상태**(로그인 접속 후)의 `/healthz` 본문이 `"status":"ok"` 이고 `"everReadyCount":0` — 본문 전체를 SUMMARY 에 인용
    - relay `/healthz` 의 `version` 이 이번 갭 클로징 커밋 해시와 일치
    - server 리비전 env 목록에 `RELAY_INTERNAL_URL`·`RELAY_ORDER_SECRET`·`ORDER_TIMEOUT_MS` 0건
    - webapp 프로덕션 HTML 에 「상승률 상위」 검출 (Phase 16 표면 회귀 없음)
    - `bash scripts/smoke-relay.sh` · `bash scripts/smoke-server.sh` 결과 요약이 SUMMARY 에 인용됨 (INV-9 는 PASS 또는 「토큰 미설정 SKIP」)
    - 보류 시: 미배포 사실과 사유가 STATE.md 에 기록됨
  </acceptance_criteria>
  <done>프로덕션이 갭 수정본으로 돌고 있고, `/healthz` 가 **세션이 있는 상태에서** 200 임이 실측으로 확인됐다.</done>
</task>

<task type="auto">
  <name>Task 3: 문서 정직 갱신 — VALIDATION · STATE · ROADMAP · REQUIREMENTS · deferred-items</name>
  <files>.planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md` — gaps 4건 + Gaps Summary + Requirements Coverage 표(특히 TRADE-03 `BLOCKED` · MYPAGE-01 `PARTIAL` 판정 근거)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` — CR-01·WR-01~09 (닫은 10건과 **범위 밖으로 둔 Info 7건**)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md` — frontmatter 형식 · Per-Task Verification Map · §Manual-Only 표 · §Deployment Verification 열린 항목
    - `.planning/REQUIREMENTS.md` L170~186 — Traceability 표 5행(전부 `Pending`)과 `*Last updated:*` 줄 형식
    - `.planning/ROADMAP.md` L596~ Phase 16 절 — `**Plans:** 17/17 plans complete` 와 Wave 목록 형식
    - `.planning/STATE.md` — 현재 Phase 16 상태 기록 형식
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md` — 마지막 절 형식
    - 16-18 ~ 16-25 의 SUMMARY 8건 — 실제로 무엇이 닫혔는지의 정본
  </read_first>
  <action>
① `16-VALIDATION.md` 에 새 절 `## Gap Closure (16-18 ~ 16-26)` 를 추가한다. 표 하나로 **14건 전부**의 처리 결과를 적는다: 항목 ID(G1~G4 · CR-01 · WR-01~09) / 담당 plan / 무엇을 바꿨는가 / 회귀를 잠근 테스트 명령. §Deployment Verification 의 열린 항목 1(relay `/healthz` 503)을 **해소로 갱신**하되 근거를 세션 있는 상태의 실측 본문으로 적는다. §Manual-Only 표는 그대로 유지한다(실서버·실계좌는 여전히 D-27 상 미실시).

② `.planning/ROADMAP.md` Phase 16 절을 갱신한다. `**Plans:** 17/17` → `26/26`. Wave 15~18 항목을 기존 형식대로 추가하고 완료 표시한다. Goal 문장은 바꾸지 않는다.

③ `.planning/REQUIREMENTS.md` Traceability 표를 재판정한다. 근거는 이번 SUMMARY 들이다.
  - **TRADE-01 · TRADE-02 · NAV-01** — 검증에서 이미 `SATISFIED` 였다. `Complete` 로 올린다.
  - **TRADE-03** — gap 1·2 가 닫혔다면 `Complete`. 다만 「같은 DMA 세션으로 WinForms 와 즉시 공유」의 **실서버 실측은 여전히 미실시**(D-27)이므로, 그 사실을 Coverage 이력 줄에 한 문장으로 남긴다. 판단이 애매하면 `Complete` 로 올리지 말고 사유를 적는다 — **올리는 쪽으로 반올림하지 않는다.**
  - **MYPAGE-01** — gap 3 이 닫혔다면 `Complete`.
  `*Last updated:*` 줄에 이번 갱신 사유를 기존 형식대로 덧붙인다.

④ `.planning/STATE.md` 에 Phase 16 갭 클로징 완료를 기록한다. 배포 보류를 택했다면 **미배포 사실과 사유**를 함께 남긴다.

⑤ `deferred-items.md` 에 새 절을 추가한다. **여전히 남는 것**을 정직하게 적는다:
  - Info 7건(IN-01~07) 중 **IN-01 은 16-22 가 해소**했다. 나머지 6건은 범위 밖으로 남는다 — 각 항목의 한 줄 요약과 「사용자가 범위를 14건으로 명시 확정했다」는 근거.
  - `surface-placeholder.tsx` 죽은 코드(사용처 0건) — 이번에도 지우지 않았다면 그 사유(계획 밖 파일).
  - `use-relay-socket.ts` `clockStamp()` ko-KR locale 버그 — 16-19·16-23 이 같은 파일을 만졌으므로 **함께 고쳤는지 여부를 확인해** 상태를 갱신한다. 안 고쳤으면 「같은 파일을 만졌으나 스코프 밖이라 손대지 않았다」를 근거와 함께 남긴다.
  - 실서버·실계좌 검증(D-27) — 여전히 Manual-Only.

**과장 금지 게이트:** 어느 문서에도 「게이트웨이가 살아났다」·「WinForms 와의 세션 공유가 검증됐다」를 쓰지 않는다. 이번에 확인된 것은 ① 코드 수정 ② mock 검증 ③ 배포 ④ `/healthz` 판정 정상화 뿐이다.
  </action>
  <verify>
    <automated>grep -c "16-18\|16-25" .planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `16-VALIDATION.md` 의 `## Gap Closure` 표에 **14행**(G1~G4 · CR-01 · WR-01~09)이 모두 있다
    - `grep -c "26/26" .planning/ROADMAP.md` >= 1
    - `.planning/REQUIREMENTS.md` Traceability 5행의 상태가 근거와 함께 갱신됨 (`grep -c "Pending" .planning/REQUIREMENTS.md` 값을 갱신 전후로 SUMMARY 에 인용)
    - `grep -c "게이트웨이가 살아" .planning .planning/phases/16-trading-limit-chaser-vi-my-page -r` == 0 — 과장 문구 없음
    - `deferred-items.md` 새 절에 IN-02~07 6건이 각각 한 줄로 남아 있다
    - `grep -c "Manual-Only" .planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md` >= 1 — 실서버 검증 미실시 사실 유지
  </acceptance_criteria>
  <done>닫힌 것과 남은 것이 문서 4종에 사실대로 적혀 있고, 어디에도 과장이 없다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 개발 머신 → GCE VM `radar-gw` / Cloud Run / Vercel | 배포 자격증명이 프로덕션을 바꾼다 |
| 문서 기록 ↔ 실제 운영 상태 | 과장된 기록은 다음 검증의 기준선을 오염시킨다 |
| smoke 프로브 → 프로덕션 relay wss | 실계좌 주문이 나갈 수 있는 유일한 경로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-46 | Elevation of Privilege | 배포 실행 | mitigate | `checkpoint:human-verify` blocking — 사용자 승인 없이 배포하지 않는다. GCP 자격증명은 전용 배포 SA(`gh-radar-deployer.json`)만 쓴다 |
| T-16-47 | Repudiation | 문서 갱신 | mitigate | 「게이트웨이가 살아났다」 류 과장 문구 0건 grep 게이트. 배포 보류 시 미배포 사실을 STATE 에 남긴다 |
| T-16-28 | 운영 사고 (실주문) | `smoke-relay.sh` INV-9 | mitigate | 16-21 이 만든 프로브는 화이트리스트 밖 계좌번호 + 미해석 ISIN 을 쓴다 — 어느 세션 상태에서도 게이트웨이 송신·DB insert 이전에 끝난다 |
| T-16-13 | 운영 사고 | 실서버·실계좌 검증 | accept | 이번에도 하지 않는다(D-27). VALIDATION §Manual-Only 유지 |
| T-16-48 | Denial of Service | 배포 순서 | mitigate | relay → server → webapp 고정. 앞 단계 실패 시 멈춘다 — 반쪽 배포로 계약이 갈리는 구간을 만들지 않는다 |
</threat_model>

<verification>
- `pnpm typecheck` · `pnpm -r test` · `pnpm --filter @gh-radar/relay run typecheck:tests` · `pnpm --filter gh-radar-webapp test:e2e` 전부 exit 0
- `curl -s -o /dev/null -w '%{http_code}' https://dma.jx1.io/healthz` == 200 (**세션이 있는 상태에서**)
- `bash scripts/smoke-relay.sh` · `bash scripts/smoke-server.sh` 결과 인용
- `grep -rn "10\.41\.1\.120" relay/ webapp/ scripts/` 0건
</verification>

<success_criteria>
- 전체 스위트 green (기준선 초과)
- 배포 3종 완료 또는 보류 사실 기록
- 세션이 있는 상태의 `/healthz` 200 실측
- 14건 처리 결과가 VALIDATION 표에 전부 있음
- 문서에 과장 문구 0건
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-26-SUMMARY.md` when done
</output>
</content>
