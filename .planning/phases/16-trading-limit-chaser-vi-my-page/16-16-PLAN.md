---
phase: 16-trading-limit-chaser-vi-my-page
plan: 16
type: execute
wave: 13
depends_on: ["16-13", "16-14", "16-15"]
files_modified:
  - server/src/routes/orders.ts
  - server/src/app.ts
  - server/src/server.ts
  - server/src/config.ts
  - server/src/services/relay-client.ts
  - server/src/services/dma-orders.ts
  - webapp/src/lib/orders-api.ts
  - relay/src/order/order-api.ts
  - relay/src/index.ts
  - scripts/deploy-server.sh
autonomous: true
requirements: [TRADE-03]

must_haves:
  truths:
    - "주문 경로가 wss 하나뿐이다 — POST /api/orders 와 relay 내부 HTTP 주문 라우트가 사라졌다 (D-02)"
    - "GET /api/orders?date= 조회 라우트는 그대로 동작한다 (D-03 오늘 주문 목록 복원용)"
    - "server 에 relay 결선(RelayClient · RELAY_INTERNAL_URL · RELAY_ORDER_SECRET · ORDER_TIMEOUT_MS)이 남지 않는다"
    - "relay 내부 HTTP 는 /healthz 만 남고 relaySecretGuard 는 유지된다"
  artifacts:
    - path: "server/src/routes/orders.ts"
      provides: "GET 전용 주문 조회 라우트"
      contains: "ordersRouter.get"
    - path: "webapp/src/lib/orders-api.ts"
      provides: "listOrders + 오류 판정 유틸(createOrder 제거)"
      contains: "UNKNOWN_OUTCOME_CODES"
  key_links:
    - from: "server/src/app.ts"
      to: "server/src/routes/orders.ts"
      via: "app.use(\"/api/orders\", ordersRouter) — GET 만 남는다"
      pattern: "/api/orders"
---

<objective>
REST 주문 경로를 **제거**한다. 두 경로가 같은 `dma_orders` 행을 다투는 기간을 만들지 않기 위해, wss 경로가 E2E 로 green 이 된 직후(16-13~16-15 완료) 같은 흐름에서 지운다.

제거 대상(RESEARCH §Server 정리 대상 표):
- `server`: `ordersRouter.post("/")` 블록 + `recordFailure` · `RelayClient` import/`AppDeps.relayClient`/`app.locals.relayClient` · `server.ts` 의 `createRelayClient` 결선 · `services/relay-client.ts` **파일 삭제** · `services/dma-orders.ts` 의 `insertOrderRequest`/`updateOrderResult`/`patchFromRelayResult`(relay 로 이미 이식됨) · `config.ts` 의 `relayInternalUrl`/`relayOrderSecret`/`orderTimeoutMs` · `deploy-server.sh` 의 env/secret 주입 3곳.
- `webapp`: `orders-api.ts` 의 `createOrder` + `ORDER_REQUEST_TIMEOUT_MS`(wss 로 이관됨). `listOrders`·`ORDER_ERROR_CODES`·`UNKNOWN_OUTCOME_CODES`·`isUnknownOutcome`·`orderErrorCode`·`orderErrorMessage` 는 **유지**.
- `relay`: `order-api.ts` 의 `ORDERS_PATH` 라우트 블록(`/healthz` 유지) + `index.ts` 의 `createOrderApi({orders, orderStore})` 인자.

**남기는 것:** `ordersRateLimit` 는 POST 전용이었으므로 함께 제거(GET 에는 걸려 있지 않다 — 실측 확인). `RELAY_ORDER_SECRET`/`relaySecretGuard`/방화벽 8091 규칙은 `/healthz` 가 남는 한 유지(CONTEXT deferred). Secret Manager 의 `gh-radar-relay-order-secret` 은 relay 가 계속 쓰므로 **삭제하지 않는다**.
Purpose: 경로를 하나로 만들고, 사라진 env 가 리비전에 남지 않게 배포 스크립트까지 함께 정리한다.
Output: server/webapp/relay 정리 + 배포 스크립트 정리 + 회귀 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: server POST 라우트 · relay-client · config 제거</name>
  <files>server/src/routes/orders.ts, server/src/app.ts, server/src/server.ts, server/src/config.ts, server/src/services/relay-client.ts, server/src/services/dma-orders.ts</files>
  <read_first>
    - `server/src/routes/orders.ts` 전문(186줄) — `ordersRateLimit`(:53~68, **POST 에만 적용**), `ordersRouter.post("/")`(:72~132), `recordFailure`(~163), `ordersRouter.get("/")`(:166~186)
    - `server/src/app.ts` — `RelayClient` import(:19), `AppDeps.relayClient`(:43), `app.locals.relayClient`(:59), `app.use("/api/orders", ordersRouter)`(:87)
    - `server/src/server.ts:7,67~91` — `createRelayClient` 결선
    - `server/src/config.ts:48~49,102~106` — `relayInternalUrl`/`relayOrderSecret`/`orderTimeoutMs`
    - `server/src/services/dma-orders.ts` — `listTodayOrders`(유지) 와 나머지(제거)
    - `server/tests/**` 중 주문 라우트 테스트
  </read_first>
  <action>
① `orders.ts`: `ordersRouter.post("/")` 블록과 `recordFailure` 를 삭제한다. `ordersRateLimit`(:53~68)도 함께 삭제한다 — **GET 에는 걸려 있지 않음이 실측 확인됐다**(`ordersRouter.get("/")` 는 `requireAuth()` 만 사용). `ordersRouter.get("/")` 는 그대로 둔다. 파일 상단 주석에서 주문 접수 관련 서술을 **D-02(주문은 relay wss 전용, 이 라우트는 조회만)** 로 교체한다.

② `app.ts`: `RelayClient` import · `AppDeps.relayClient` · `app.locals.relayClient` 삭제. `app.use("/api/orders", ordersRouter)`(:87)는 **유지**(GET 이 남는다).

③ `server.ts`: `createRelayClient` import 와 결선(:7, :67~91) 삭제.

④ `services/relay-client.ts`: **파일 삭제**.

⑤ `services/dma-orders.ts`: `insertOrderRequest` · `updateOrderResult` · `patchFromRelayResult` · `resolveIsinAndMarket` 를 삭제하고 `listTodayOrders` 만 남긴다(전자들은 16-08 에서 relay 로 이식 완료). 삭제 전에 **relay 쪽에 대응 구현이 있는지 grep 으로 확인**하고, 없으면 삭제하지 말고 보고한다.

⑥ `config.ts`: `relayInternalUrl`·`relayOrderSecret`·`orderTimeoutMs` 제거. 이들이 required 였다면 제거로 부팅 검증이 느슨해지지 않는지 확인한다.

⑦ server 테스트에서 POST 주문 케이스를 삭제하고, **GET 조회 케이스는 유지·보강**한다(날짜 필터·인증 필요·bare array 응답).
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/server test &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "ordersRouter.post" server/src/` 결과 0건
    - `grep -rn "relayClient\|RelayClient\|RELAY_INTERNAL_URL\|ORDER_TIMEOUT_MS" server/src/` 결과 0건
    - `test -f server/src/services/relay-client.ts` 실패(파일 없음)
    - `grep -c "ordersRouter.get" server/src/routes/orders.ts` == 1
    - `grep -c "listTodayOrders" server/src/services/dma-orders.ts` >= 1
    - `pnpm --filter @gh-radar/server test` exit 0
  </acceptance_criteria>
  <done>server 에 주문 접수 경로와 relay 결선이 남지 않고 조회 라우트는 동작한다.</done>
</task>

<task type="auto">
  <name>Task 2: webapp createOrder 제거 + relay 내부 HTTP 주문 라우트 제거</name>
  <files>webapp/src/lib/orders-api.ts, relay/src/order/order-api.ts, relay/src/index.ts</files>
  <read_first>
    - `webapp/src/lib/orders-api.ts`(151줄) — `ORDER_REQUEST_TIMEOUT_MS`·`ORDER_ERROR_CODES`·`UNKNOWN_OUTCOME_CODES`·`createOrder`·`listOrders`·판정 3함수
    - `relay/src/order/order-api.ts` — `ORDERS_PATH`(`/internal/orders`) 라우트 블록, `HEALTH_PATH`(`/healthz`, 유지), `relaySecretGuard`, `createOrderApi(deps)` 시그니처(:344)
    - `relay/src/index.ts:117~` — `createOrderApi({ relayOrderSecret, dmaHost, ..., orders: hub, orderStore })`
    - `relay/src/config.ts:34` — `relayOrderSecret` required
  </read_first>
  <action>
① `webapp/src/lib/orders-api.ts`: `createOrder` 와 `ORDER_REQUEST_TIMEOUT_MS`(16-10 에서 provider 로 옮겼다면 그쪽 상수로 일원화) 를 제거한다. **`listOrders`·`ORDER_ERROR_CODES`·`OrderErrorCode`·`UNKNOWN_OUTCOME_CODES`·`isUnknownOutcome`·`orderErrorCode`·`orderErrorMessage` 는 유지**한다(wss 결과 판정에 계속 쓰인다). 파일 상단 주석의 「주문 접수는 POST /api/orders」 서술을 D-02 로 교체한다. 사용처 grep 으로 0건을 확인하고 제거한다.

② `relay/src/order/order-api.ts`: `ORDERS_PATH` 라우트 처리 블록과 그에 딸린 대기 큐·타이머·요청 스키마를 제거한다(모두 `ws/order-handler.ts` 로 이식 완료). **`/healthz` 와 `relaySecretGuard` 는 유지**한다. `statusOf`/`filledQtyOf` 가 `order-handler.ts` 에서 import 되고 있으므로 **그 두 순수 함수는 남기거나 공용 모듈로 옮긴다**(옮긴다면 import 경로를 함께 갱신). `createOrderApi` 시그니처에서 `orders`/`orderStore` 인자를 제거한다.

③ `relay/src/index.ts`: `createOrderApi` 호출에서 `orders`/`orderStore` 인자를 빼고, `orderStore` 는 `WsFanout`/`order-handler` 쪽 주입만 남긴다. `relayOrderSecret` 은 `/healthz` 가드가 쓰므로 **required 그대로 둔다**(deferred — CONTEXT).

④ relay 테스트: `order-api.test.ts` 중 HTTP 주문 케이스를 제거하고 `/healthz` 가드 케이스는 유지한다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test &amp;&amp; pnpm --filter gh-radar-webapp test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "createOrder\b" webapp/src/` 결과 0건
    - `grep -c "isUnknownOutcome\|UNKNOWN_OUTCOME_CODES" webapp/src/lib/orders-api.ts` >= 2 (유지 확인)
    - `grep -rn "internal/orders" relay/src/` 결과 0건
    - `grep -c "HEALTH_PATH\|/healthz" relay/src/order/order-api.ts` >= 1 (유지)
    - `grep -c "relaySecretGuard" relay/src/order/order-api.ts` >= 1
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>주문 HTTP 경로가 webapp·relay 양쪽에서 사라지고 healthz 는 남는다.</done>
</task>

<task type="auto">
  <name>Task 3: deploy-server.sh env 정리 + 전 워크스페이스 회귀</name>
  <files>scripts/deploy-server.sh</files>
  <read_first>
    - `scripts/deploy-server.sh:188,189,205,208,230~233` — `RELAY_INTERNAL_URL`·`ORDER_TIMEOUT_MS` env 주입 + `RELAY_ORDER_SECRET` secret 바인딩 + 검증 출력 3줄
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md` §Runtime State Inventory(837~853행) — **git 의 스크립트를 고쳐도 기존 Cloud Run 리비전은 그대로다**
  </read_first>
  <action>
`deploy-server.sh` 에서 `RELAY_INTERNAL_URL`·`ORDER_TIMEOUT_MS` env 주입과 `RELAY_ORDER_SECRET` **secret 바인딩**(server 쪽만), 그리고 배포 후 검증 출력 3줄을 제거한다. 제거 지점에 「Phase 16 D-02: 주문은 relay wss 전용이므로 server 는 relay 를 호출하지 않는다」 주석을 남긴다.

**중요 — 리비전 정리**: 스크립트 수정만으로는 기존 리비전의 env·secret 바인딩이 사라지지 않는다. 실제 제거는 16-17 재배포에서 `--remove-env-vars=RELAY_INTERNAL_URL,ORDER_TIMEOUT_MS --remove-secrets=RELAY_ORDER_SECRET` 를 함께 넘겨야 한다는 사실을 스크립트 주석과 SUMMARY 양쪽에 남긴다(Phase 09.1 KIS 정리 선례와 동형). **Secret Manager 의 `gh-radar-relay-order-secret` 자체는 relay 가 쓰므로 삭제하지 않는다.**

정리 후 전 워크스페이스 회귀를 돌린다: `pnpm typecheck && pnpm -r test`. E2E 는 16-17 에서 전량 실행하되, 여기서는 주문 경로 회귀만 확인한다(`orderbook`).
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm -r test &amp;&amp; pnpm --filter gh-radar-webapp test:e2e -- orderbook</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RELAY_INTERNAL_URL\|ORDER_TIMEOUT_MS" scripts/deploy-server.sh` == 0
    - `grep -c "RELAY_ORDER_SECRET" scripts/deploy-server.sh` == 0 (server 배포에서 바인딩 제거)
    - 스크립트에 `--remove-env-vars` / `--remove-secrets` 후속 조치 안내 주석이 존재
    - `pnpm -r test` exit 0
    - `pnpm --filter gh-radar-webapp test:e2e -- orderbook` exit 0
  </acceptance_criteria>
  <done>배포 스크립트가 정리되고 전 워크스페이스 단위 테스트가 green 이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud Run server → relay 내부 HTTP | 이 경계가 **사라진다**(주문 한정) |
| 배포 스크립트 → 실행 리비전 | 스크립트 수정만으로는 기존 리비전이 바뀌지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-11 | Elevation of Privilege | 남아 있는 REST 주문 라우트 | mitigate | 제거로 **공격면 자체를 없앤다**. grep 게이트로 잔재 0 확인. 두 경로가 공존하면 같은 `dma_orders` 행을 다투어 상태가 갈린다 |
| T-16-08 | Information Disclosure | `RELAY_ORDER_SECRET` 바인딩 | mitigate | server 리비전에서 바인딩 제거(16-17 `--remove-secrets`). Secret 자체는 relay 가 쓰므로 **삭제하지 않는다** — 지우면 relay 부팅이 깨진다 |
| T-16-06 | Denial of Service | `ordersRateLimit` 제거 | accept | POST 전용이었고 함께 사라진다. 새 경로(wss)의 상한은 16-07 의 연결당 토큰 버킷이 담당 |
| T-16-04 | Access Control | `GET /api/orders` | mitigate | `requireAuth()` + 서비스 계층의 `WHERE user_id` 유지(T-15-01). 이 라우트는 건드리지 않는다 |
</threat_model>

<verification>
- `pnpm typecheck` exit 0
- `pnpm -r test` exit 0
- `pnpm --filter gh-radar-webapp test:e2e -- orderbook` exit 0
</verification>

<success_criteria>
- 주문 경로가 wss 하나
- GET 조회 라우트 유지
- server 코드·config·배포 스크립트에 relay 주문 결선 잔재 0
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-16-SUMMARY.md` when done
</output>
