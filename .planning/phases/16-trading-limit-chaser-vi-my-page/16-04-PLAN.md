---
phase: 16-trading-limit-chaser-vi-my-page
plan: 04
type: execute
wave: 3
depends_on: ["16-01", "16-03"]
files_modified:
  - relay/src/dma/msg-type.ts
  - relay/src/dma/envelope.ts
  - relay/src/dma/__tests__/codec.test.ts
autonomous: true
requirements: [TRADE-03]

must_haves:
  truths:
    - "relay 가 전략 요청 7종(10/11/14/21/24/33/34)을 FlatBuffers 로 조립해 DMA 세션으로 보낼 수 있다 (D-01)"
    - "화이트리스트 확장으로 새로 통과하는 수신 msg_type 7종(56/60/61/64/65/72/73)이 전부 열거되어 있다 (PC-12 — 필터 완화 시 유입 집합을 먼저 센다)"
    - "deprecated 슬롯과 위치 인자 생성 함수를 쓰지 않는다 (Pitfall 1)"
  artifacts:
    - path: "relay/src/dma/msg-type.ts"
      provides: "전략 MsgType 상수 + 인바운드 화이트리스트"
      contains: "SetLimitChaserResp"
    - path: "relay/src/dma/envelope.ts"
      provides: "전략 요청 빌더 7종"
      contains: "buildSetLimitChaserReq"
  key_links:
    - from: "relay/src/dma/envelope.ts"
      to: "relay/src/generated/stock-dma/set-limit-chaser.ts"
      via: "startSetLimitChaser + addXxx 개별 호출"
      pattern: "startSetLimitChaser"
---

<objective>
relay codec 층의 **송신 절반**을 만든다: MsgType 화이트리스트 확장 + 전략 요청 빌더 7종.

- `msg-type.ts` 의 `MSG` 에 요청 7종(10·11·14·21·24·33·34)과 응답 7종(56·60·61·64·65·72·73)을 추가하고, `INBOUND_MSG_TYPES` 에 응답 7종을 넣는다. 파일 상단 주석의 「상따/VI 계열은 제외한다」를 D-01 근거로 뒤집는다.
- `envelope.ts` 에 요청 빌더 7종을 추가한다. `SetLimitChaser` 는 **클라 입력 30 + 고정 3 = 33 필드**만 채우고 S→C 전용 4필드는 채우지 않는다.

Purpose: 화이트리스트를 넓히면 지금까지 `default:` 로 조용히 떨어지던 프레임이 새로 들어온다 — 무엇이 들어오는지 먼저 세고(PC-12), 하류(16-05 파서 · 16-06 Hub)가 전부 명시 case 로 받게 한다.
Output: MSG 14종 추가 · INBOUND 7종 추가 · 요청 빌더 7종 · codec 테스트 갱신.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: msg-type.ts 화이트리스트 확장 + 유입 집합 열거</name>
  <files>relay/src/dma/msg-type.ts, relay/src/dma/__tests__/codec.test.ts</files>
  <read_first>
    - `relay/src/dma/msg-type.ts` 전문 — 파일 상단 주석 :19 「상따/VI/종목마스터 계열(10·11·27·33·57·72·73 등)도 같은 이유로 제외한다」, `MSG` 상수 블록(:27~96), `INBOUND_MSG_TYPES`
    - `relay/src/dma/__tests__/codec.test.ts` — 생성 enum(`MsgType`) 과 `MSG` 대조 케이스
    - `relay/src/generated/stock-dma/msg-type.ts`(생성 enum) — 값 대조 원천
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md` §0 MsgType 표(236~268행) — 전송 클래스 Notice/Broadcast 정정 포함
  </read_first>
  <action>
`MSG` 에 아래 14개를 **생성 enum 값과 대조해** 추가한다(요청 7 + 응답 7). 각 항목에 한 줄 JSDoc 을 붙이되, 응답에는 **전송 클래스**를 적는다(RESEARCH 정정 사실):
- 요청: `SetLimitChaserReq: 10` · `SetVITriggerReq: 11` · `DisableStrategiesReq: 14` · `GetVITriggerReq: 21`(요청 본문 없음) · `GetLimitChaserListReq: 24`(**요청 테이블 없음 — 빈 Envelope**) · `ConfirmVIOrderReq: 33` · `GetVIOrderListReq: 34`(빈 Envelope)
- 응답: `VIOrderNotice: 56`(Notice) · `SetLimitChaserResp: 60`(**Set 에코 = Notice**, 300ms 런타임 푸시만 Broadcast) · `SetVITriggerResp: 61`(Set 에코 = Notice, 15:40 자동중지만 Broadcast) · `GetLimitChaserListResp: 64`(요청 연결에만) · `DisableStrategiesResp: 65`(요청 연결에만, 에코 60/61 이 먼저 나간 뒤) · `GetVIOrderListResp: 72`(요청 연결에만) · `VIOrderListPush: 73`(Notice, 세션 전 연결)

`INBOUND_MSG_TYPES` 에 응답 7종만 추가한다. 파일 상단 주석 :19 를 교체해 **PC-12 근거로 유입 집합을 명시**한다: 「화이트리스트에 56/60/61/64/65/72/73 7종을 새로 넣는다. 이 7종은 전부 `SubscriptionHub.#onFrame` 에 명시 `case` 가 있고(16-06), 파싱 실패는 `envelope.ts` 의 `drop`/`dropField` 가 사유·카운터를 남긴다. `default:` 로 조용히 떨어지는 프레임은 여전히 0이다.」 그리고 여전히 제외하는 것(27 종목마스터 계열 등)을 남겨 둔다.

`codec.test.ts` 를 갱신한다: ① `MSG` 의 모든 값이 생성 `MsgType` enum 값과 일치 ② `INBOUND_MSG_TYPES` 가 정확히 기존 12종 + 신규 7종 = 19종을 담는다(개수 단언) ③ 화이트리스트 밖 값(예: 10 — 요청이므로 수신 대역이 아니다)은 `INBOUND_MSG_TYPES.has()` 가 false.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test -- codec</automated>
  </verify>
  <acceptance_criteria>
    - `relay/src/dma/msg-type.ts` 의 `MSG` 에 `SetLimitChaserReq: 10`·`SetVITriggerReq: 11`·`DisableStrategiesReq: 14`·`GetVITriggerReq: 21`·`GetLimitChaserListReq: 24`·`ConfirmVIOrderReq: 33`·`GetVIOrderListReq: 34`·`VIOrderNotice: 56`·`SetLimitChaserResp: 60`·`SetVITriggerResp: 61`·`GetLimitChaserListResp: 64`·`DisableStrategiesResp: 65`·`GetVIOrderListResp: 72`·`VIOrderListPush: 73` 가 모두 존재
    - `grep -c "상따/VI/종목마스터 계열(10·11·27·33·57·72·73 등)도 같은 이유로 제외" relay/src/dma/msg-type.ts` == 0
    - `codec.test.ts` 에 `INBOUND_MSG_TYPES.size` 를 19 로 단언하는 케이스가 있다
    - `pnpm --filter @gh-radar/relay test -- codec` exit 0
  </acceptance_criteria>
  <done>MSG 14종 추가, INBOUND 19종, 상단 주석이 새 유입 집합을 열거한다.</done>
</task>

<task type="auto">
  <name>Task 2: envelope.ts 전략 요청 빌더 7종</name>
  <files>relay/src/dma/envelope.ts</files>
  <read_first>
    - `relay/src/dma/envelope.ts` — `buildGetQuoteReq`(:332, 빌더 골격), `buildDirectOrderReq`(:722), `toWireSide`/`toWireMarket`/`toWireOrderType`/`ORDER_CONDITION`(:662~692, 단일문자 변환의 유일 지점), `takeCount`(:180), `drop`/`dropField`(:154~172), `OrderBuildError`
    - `relay/src/generated/stock-dma/{set-limit-chaser,set-vi-trigger,confirm-vi-order-req,disable-strategies-req,envelope}.ts` (16-01 재동기화본)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md` §Protocol A 필드 표(269~370행) · §Code Examples 빈 Envelope 빌더(954~965행)
    - `/Users/alex/repos/gh-trade/client/Forms/Trading/LimitChaserForm.cs` `Send()`(2690~2755) — 33 인자 송신 목록과 고정 3값의 원본
  </read_first>
  <action>
아래 7개를 추가한다. 모든 테이블 조립은 **`startXxx` + `addXxx` + `endXxx` 개별 호출**이며 문자열은 테이블 열기 **전에** `createString` 한다.

- `buildGetLimitChaserListReq(): Uint8Array` — msg_type 24, 본문 없음. `buildGetQuoteReq` 구조를 축약해 `Envelope.startEnvelope` → `addMsgType` → `endEnvelope` → `Finish`.
- `buildGetVITriggerReq(): Uint8Array` — msg_type 21, 본문 없음(서버가 `get_strategy_req` 를 파싱하되 무시).
- `buildGetVIOrderListReq(): Uint8Array` — msg_type 34, 본문 없음.
- `buildSetLimitChaserReq(cfg: RelayLimitChaserInput): Uint8Array` — msg_type 10, 슬롯 `set_limit_chaser`. **33 필드**를 채운다: 클라 입력 30 + 고정 3(`sweepRecalcEnabled = true`, `sweepMinCount = 0`, `sweepMinRate = 0`). **S→C 전용 4필드(`sellOrderQty`·`sellQtyTrackBaseline`·`sellEntryLatched`·`cancelQtyTrackBaseline`)와 deprecated 8슬롯은 건드리지 않는다.** `isin`·`accountNo` 는 **12자로 절단**해 넣는다(서버 `strncpy(…,12)` 와 키가 일치해야 한다). `market`·`crud`·`buyWatchSide` 는 단일 문자 문자열이며 변환은 이 파일의 기존 단일문자 헬퍼 옆에 `toWireMarketCode`/`toWireCrud`/`toWireWatchSide` 로 **한 곳에만** 둔다. `exchange` 는 `"KRX"`/`"NXT"` 문자열 그대로.
- `buildSetVITriggerReq(cfg): Uint8Array` — msg_type 11. `orderAmountKrw` 는 **`BigInt(...)` 로 승격해 `addOrderAmountKrw`**(ulong). `priceType` 은 `"U"` 를 relay 가 채운다(브라우저는 보내지 않는다). `checkRate` 는 정수 %(BasisPoints 아님 — `sweepMinRate` 와 단위가 다르다는 주석을 붙인다).
- `buildConfirmVIOrderReq({orderNo, confirmed}): Uint8Array` — msg_type 33. `orderNo` 가 빈 문자열이면 **조립하지 않고 `OrderBuildError` 를 던진다**(서버가 조용히 드롭하므로 최후 방어선).
- `buildDisableStrategiesReq(key = ""): Uint8Array` — msg_type 14. `key` 는 64바이트 초과 시 `OrderBuildError`.

변환 실패·상한 초과는 전부 `drop`/`dropField`/`OrderBuildError` 로 **사유와 함께** 남긴다 — 조용한 드롭 금지(PC-7/S-2).
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test -- envelope</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createSetLimitChaser(\|createSetVITrigger(\|createConfirmVIOrderReq(\|createDisableStrategiesReq(" relay/src/dma/envelope.ts` == 0
    - `grep -c "startSetLimitChaser" relay/src/dma/envelope.ts` == 1
    - `grep -c "addSellOrderQty\|addSellQtyTrackBaseline\|addSellEntryLatched\|addCancelQtyTrackBaseline" relay/src/dma/envelope.ts` == 0 (S→C 전용 4필드 미송신)
    - `grep -c "export function build\(GetLimitChaserListReq\|GetVITriggerReq\|GetVIOrderListReq\|SetLimitChaserReq\|SetVITriggerReq\|ConfirmVIOrderReq\|DisableStrategiesReq\)" relay/src/dma/envelope.ts` == 7
    - `buildSetLimitChaserReq` 가 항상 `sweepRecalcEnabled=true`·`sweepMinCount=0`·`sweepMinRate=0` 을 넣는다 (테스트로 왕복 검증 — 16-05 Task 2 에서 단언)
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>요청 빌더 7종이 존재하고, 위치 인자 생성 함수·S→C 전용 필드 송신이 0건이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay → gh-trade 게이트웨이(TCP) | 조립한 FlatBuffer 가 **실계좌 발주**로 이어진다 |
| 게이트웨이 → relay | 화이트리스트가 넓어져 새 프레임 7종이 파서에 도달한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Tampering | `buildSetLimitChaserReq` | mitigate | 위치 인자 생성 함수 금지(grep 0건 게이트) — deprecated 8슬롯 때문에 인자가 한 칸 밀려도 컴파일된다. bool 자리에 uint 가 들어가면 게이트가 뒤바뀐 채 실계좌 발주가 나간다 |
| T-16-05 | Tampering | `INBOUND_MSG_TYPES` 확장 | mitigate | 새로 통과하는 7종을 상단 주석에 **전부 열거**하고 16-06 에서 명시 case 를 만든다(PC-12). JS 런타임에 FlatBuffers Verifier 가 없으므로 화이트리스트 + 파서 null 가드가 실질 방어선 |
| T-16-06 | Denial of Service | `buildDisableStrategiesReq(key)` | mitigate | 64바이트 상한을 relay 에서 먼저 던진다(`OrderBuildError`) |
| T-16-07 | Repudiation | 조립 실패 경로 | mitigate | `drop`/`dropField`/`OrderBuildError` 로 사유+카운터를 남긴다. 무로그 fail-safe 금지(PC-7) |
</threat_model>

<verification>
- `pnpm typecheck` exit 0
- `pnpm --filter @gh-radar/relay test` exit 0
</verification>

<success_criteria>
- MSG/INBOUND 확장 + 주석의 유입 집합 열거
- 전략 요청 빌더 7종, 위치 인자·S→C 필드 송신 0건
- codec 테스트가 생성 enum 과 대조해 green
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-04-SUMMARY.md` when done
</output>
