---
phase: 16-trading-limit-chaser-vi-my-page
plan: 11
type: execute
wave: 10
depends_on: ["16-09"]
files_modified:
  - webapp/src/components/layout/app-sidebar.tsx
  - webapp/src/components/trading/strategy-badge.tsx
  - webapp/src/components/trading/dma-gate.tsx
  - webapp/src/app/trading/limit-chaser/new/page.tsx
  - webapp/src/app/trading/limit-chaser/[key]/page.tsx
  - webapp/src/app/trading/limit-chaser/page.tsx
  - webapp/src/app/trading/vi/page.tsx
  - webapp/src/app/me/page.tsx
  - webapp/src/components/layout/__tests__/app-sidebar.test.tsx
  - webapp/e2e/specs/sidebar-tree.spec.ts
  - webapp/e2e/specs/auth-guards.spec.ts
autonomous: true
requirements: [NAV-01, TRADE-01, TRADE-02, MYPAGE-01]

must_haves:
  truths:
    - "사이드 메뉴가 홈 / 종목검색(상승률 상위·테마·관심종목) / 트레이딩(상따 + 등록 전략 목록 · VI) / My page / AI 애널리스트 2단 트리다 (D-15/NAV-01)"
    - "그룹 소제목은 링크가 아니고 항상 펼쳐져 있으며 접기 상태를 저장하지 않는다 (D-16)"
    - "사이드바 3단에 등록 전략이 종목명 + 매수/매도 원 아이콘 2개로 나열되고 클릭하면 편집 페이지로 간다 (D-17 · UI-SPEC N3/N3a)"
    - "비로그인 · unauthorized · 미연결이면 트레이딩 그룹과 My page 가 렌더되지 않는다 (D-19)"
    - "모바일 drawer 가 같은 트리를 쓰고 링크 클릭 시 자동으로 닫힌다 (D-18)"
    - "`/scanner` 라벨이 「상승률 상위」이고 URL 은 그대로다 (D-15)"
    - "사이드바·게이트의 시각 계약은 사용자 승인 목업 `16-mypage-sidebar-mockup.html` 을 정본으로 따른다 (D-24 채택안 — 문서와 어긋나면 목업이 이긴다)"
    - "직접 URL 진입 시 비로그인은 로그인 유도, 매핑 없음은 「DMA 계정이 연결되지 않았어요」 게이트를 본다 (D-19)"
  artifacts:
    - path: "webapp/src/components/layout/app-sidebar.tsx"
      provides: "2단 그룹 트리 + 3단 전략 목록 + 조건부 숨김"
      contains: "상승률 상위"
    - path: "webapp/src/components/trading/strategy-badge.tsx"
      provides: "전략 상태 배지 6종 ↔ 에코 필드 매핑 단일 지점"
      contains: "매도감시"
    - path: "webapp/src/components/trading/dma-gate.tsx"
      provides: "매핑 없음 / 비로그인 게이트"
      contains: "DMA 계정이 연결되지 않았어요"
    - path: "webapp/src/app/me/page.tsx"
      provides: "/me 라우트 셸"
  key_links:
    - from: "webapp/src/components/layout/app-sidebar.tsx"
      to: "webapp/src/lib/relay-provider.tsx"
      via: "전역 전략 스냅샷 + 상태(status/isMapped)"
      pattern: "useRelayContext|useRelayStrategies"
---

<objective>
사이드 메뉴를 트리로 재편하고(NAV-01), 신규 라우트 4개의 **빈 셸**과 3표면 공용 컴포넌트 2개(`strategy-badge` · `dma-gate`)를 세운다. 페이지 내용은 16-12~16-15 가 채운다.

라우트 확정(UI-SPEC P1 — CONTEXT D-15 와 어긋나는 부분은 **UI-SPEC 이 정본**):
- `/trading/limit-chaser/new` = 새 전략 빈 폼 (사이드바 「상따」 항목이 여기를 가리킨다)
- `/trading/limit-chaser/[key]` = 편집 (`{ISIN}:{accountNo}:{exchange}` 를 `encodeURIComponent` 로 인코딩)
- `/trading/limit-chaser` = `/trading/limit-chaser/new` 로 **redirect**
- `/trading/vi`, `/me`

Purpose: 트리·게이트·배지는 세 표면이 전부 참조하므로 먼저 한 번만 만든다. 여기서 `data-nav-item`/`aria-current` 계약이 깨지면 drawer 자동 닫힘이 회귀한다.
Output: 사이드바 트리 · strategy-badge · dma-gate · 라우트 셸 5개 · RTL/E2E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 사이드바 2단 그룹 트리 + 3단 전략 목록 + 조건부 숨김</name>
  <files>webapp/src/components/layout/app-sidebar.tsx, webapp/src/components/layout/__tests__/app-sidebar.test.tsx</files>
  <read_first>
    - `webapp/src/components/layout/app-sidebar.tsx` 전문(69줄) — `NAV` 배열(:28~36), `pathname === href` 정확 일치 판정, `data-nav-item`/`aria-current` 부여 지점, 클래스 토큰
    - `webapp/src/components/layout/app-shell.tsx` — drawer 자동 닫힘 조상 순회(:68~84, `A` 또는 `BUTTON[data-nav-item]`), `aside w-60`, Sheet `w-[min(280px,85vw)]`
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md` §N1~N7(252~266행) · §내비게이션 라벨 표(403~416행) · §접근성 라벨(561~582행)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-mypage-sidebar-mockup.html` — 사이드바 상태 3종(매핑 있음/없음/비로그인) **정본**
    - `webapp/src/lib/relay-provider.tsx`(16-09) — `status` · `isMapped` · `limitChasers`
  </read_first>
  <action>
`NAV` 상수를 **트리 구조**로 바꾼다(1단 항목 / 그룹 = 소제목 + 2단 항목들). 라벨은 UI-SPEC 표 그대로: `홈`(`/`) · 소제목 `종목검색` · `상승률 상위`(`/scanner`) · `테마`(`/themes`) · `관심종목`(`/watchlist`) · 소제목 `트레이딩` · `상따`(`/trading/limit-chaser/new`) · `VI`(`/trading/vi`) · `My page`(`/me`) · `AI 애널리스트`(`/chat`).

- **그룹 소제목**은 `<li>` + 시각 스타일만(11px/600/`--muted-fg`). **`<button>`/`<a>` 로 만들지 않는다** — drawer 자동 닫힘 순회가 `BUTTON[data-nav-item]`/`A` 를 닫힘 트리거로 보기 때문(D-16 클릭 불가).
- **활성 판정**: 현재는 `pathname === href` 정확 일치다. `/trading/limit-chaser/[key]` 에서 2단 「상따」가 활성으로 보이면 안 되고(**`new` 일 때만** `aria-current`), 3단 항목은 자기 key 일 때 활성이다. `/scanner` 등 기존 항목의 정확 일치 동작은 유지한다.
- **3단 전략 목록**(UI-SPEC N3): 「상따」 아래 `ul`. 항목 = **종목명 + 우측 원 아이콘 2개**뿐. 종목명 14px/600 `--fg`(활성 `--accent-fg`), `flex:1` + ellipsis. 원 아이콘 10px·gap 6px, **좌 = 매수(`--up`) / 우 = 매도(`--down`)**, ON = 채움 / OFF = 1.5px 속빔 `--muted-fg`. 묶음에 `role="img"` + `aria-label="매수 켜짐 · 매도 꺼짐"` + 동일 `title`(개별 원에는 라벨을 달지 않는다 — 중복 낭독 방지). **거래소 태그·상태 배지는 3단에 두지 않는다**(N3a — 240px 에서 종목명이 잘린다). 링크는 `/trading/limit-chaser/{encodeURIComponent(key)}`.
- ON/OFF 판정 원천은 전역 전략 스냅샷의 `buyEnabled`/`sellEnabled`(에코값 = 무장 상태). 전략 0건이면 `등록된 전략 없음`(12px, 링크 아님).
- **VI 항목 배지**(N7): 가동 = 「가동」(`--up-bg`/`--up`, 채운 도트) / 중지 = 「중지」(`--muted`, 속 빈 도트).
- **조건부 숨김**(N4/D-19): 비로그인 · `status === "unauthorized"` · 미연결이면 **「트레이딩」 그룹 전체와 「My page」를 렌더하지 않는다**. 판정 전(연결 중)에는 **숨긴 상태로 시작**해 나타났다 사라지는 깜빡임을 만들지 않는다(UI-SPEC §사이드바·라우팅).
- 모든 링크에 기존 `data-nav-item` + `aria-current` 를 유지한다(drawer 자동 닫힘 계약).
- 홈 등 다른 표면의 「스캐너」 문구도 「상승률 상위」로 통일한다(`grep -rn "스캐너" webapp/src` 로 훑어 일괄 교체 — URL 은 건드리지 않는다).

`app-sidebar.test.tsx`(신규): ① 트리 구조(소제목 2개가 링크가 아님) ② 로그인+매핑 시 트레이딩·My page 렌더 ③ 비로그인/unauthorized 시 미렌더 ④ 전략 2건이 3단에 종목명+원아이콘으로 렌더되고 `aria-label` 이 「매수 켜짐 · 매도 꺼짐」 형태 ⑤ `/trading/limit-chaser/new` 에서만 「상따」에 `aria-current="page"` ⑥ 모든 링크에 `data-nav-item` ⑦ 전략 0건 빈 문구.
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp test -- app-sidebar</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "상승률 상위" webapp/src/components/layout/app-sidebar.tsx` >= 1 이고 `grep -rn "스캐너" webapp/src` 결과 0건
    - `grep -c "data-nav-item" webapp/src/components/layout/app-sidebar.tsx` >= 1 이고 소제목 요소에는 `data-nav-item` 이 없다
    - 소제목이 `<li>` 로 렌더되고 `button`/`a` 태그가 아님을 단언하는 RTL 케이스 존재
    - `role="img"` + `aria-label` 이 원 아이콘 묶음에 정확히 1개씩(개별 원 라벨 0)
    - `pnpm --filter gh-radar-webapp test -- app-sidebar` exit 0 (7 케이스)
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>트리·전략 목록·조건부 숨김·라벨 통일이 RTL 로 고정된다.</done>
</task>

<task type="auto">
  <name>Task 2: strategy-badge · dma-gate 공용 컴포넌트 + 라우트 셸 5개</name>
  <files>webapp/src/components/trading/strategy-badge.tsx, webapp/src/components/trading/dma-gate.tsx, webapp/src/app/trading/limit-chaser/new/page.tsx, webapp/src/app/trading/limit-chaser/[key]/page.tsx, webapp/src/app/trading/limit-chaser/page.tsx, webapp/src/app/trading/vi/page.tsx, webapp/src/app/me/page.tsx</files>
  <read_first>
    - `webapp/src/app/watchlist/page.tsx`(22줄 전체) — 라우트 셸 템플릿(`AppShell` + `AppSidebar` + `*-Client`)
    - `webapp/src/app/stocks/[code]/page.tsx` — 동적 세그먼트 + `React.use(params)` (Next 15 Promise params)
    - `webapp/src/components/orderbook/relay-status-bar.tsx` — `data-slot` 계약, 배지 블록(:180~), `RELAY_STATE_LABELS` 사용
    - `webapp/src/components/orderbook/order-panel.tsx:409` — `status === 'unauthorized'` 게이트(상따/VI/My page 는 `null` 대신 `<DmaGate>`)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md` §전략 상태 배지 6종(431~446행) · §빈 상태·게이트(531~549행) · §페이지 제목(418~429행)
  </read_first>
  <action>
① `strategy-badge.tsx` — 배지 6종을 **한 곳의 상수 맵**으로 정의하고(`RELAY_STATE_LABELS` 가 상태 라벨을 한 곳에 두는 규약과 동형), 에코 필드 → 배지 매핑 순수 함수 `strategyBadgesOf(item: RelayLimitChaser & { hadOrder?: boolean }): Badge[]` 를 export 한다:
   - `매수ON` = `buyEnabled(=buyArmed)` true → `--up-bg`/`--up`, 채운 도트
   - `발주됨` = 직전에 매수 발주 이력이 있고 `buyEnabled` 가 false → 중립 `--muted`, 채운 도트
   - `매도대기` = `sellEnabled` true ∧ `sellEntryLatched` false → 중립, **속 빈 도트**
   - `매도감시` = `sellEntryLatched` true → `--down-bg`/`--down`, 채운 도트
   - `가동`/`중지`(VI) = `run`
   - 거래소 태그 `KRX`/`NXT` = 중립(KRX 테두리형 / NXT 채운 `--muted`), 도트 없음
   **색·형태·텍스트 3중**(WCAG 1.4.1). 「비활성」 배지는 만들지 않는다(두 스위치 OFF = 삭제라 항목 자체가 사라진다). `data-slot="strategy-badge"`.
   ⚠ 「에코의 `enabled` 는 설정값이 아니라 무장 상태」를 파일 상단 주석에 명시한다(Pitfall 10) — 「내가 켰는데 서버가 껐다」가 아니라 「발주가 나갔다」는 뜻이다.

② `dma-gate.tsx` — 3표면 공용(A14/B9/C6). props: `reason: "unauthenticated" | "unmapped"`, `surface: "상따 전략" | "VI 자동매수" | "전략·잔고·미체결"`. 문구는 UI-SPEC §빈 상태·게이트 **그대로**:
   - 매핑 없음: 제목 `DMA 계정이 연결되지 않았어요` / 본문 `{surface}은 증권사 계정이 연결된 사용자만 이용할 수 있어요. 연결이 필요하면 관리자에게 문의해 주세요.` / 보조 `차트·뉴스·종목토론방은 그대로 이용할 수 있어요.` — **버튼 없음**
   - 비로그인: 제목 `로그인이 필요해요` / 본문 `My page 는 로그인한 뒤 이용할 수 있어요.`(표면별 문구) / 보조 + 버튼 `로그인`
   자물쇠 아이콘(lucide) + `data-slot="dma-gate"`.

③ 라우트 셸 5개 — `watchlist/page.tsx` 22줄 템플릿 그대로. 각 페이지는 `AppShell sidebar={<AppSidebar/>}` 로 감싸고 데이터·wss 소비는 전부 `*-client.tsx` 로 내린다(16-12~16-15 가 클라이언트를 채운다. 이 plan 에서는 **자리표시 클라이언트**(제목/부제 + `<DmaGate>` 분기만)를 두고 다음 plan 이 교체한다).
   - `/trading/limit-chaser/page.tsx` 는 화면을 그리지 않고 `redirect('/trading/limit-chaser/new')`(Next `redirect`) 를 호출한다.
   - `[key]/page.tsx` 는 Next 15 Promise params 를 `React.use(params)` 로 풀고 `decodeURIComponent(key)` 한다.
   - 제목·부제는 UI-SPEC §페이지 제목 표 그대로(상따 빈 폼 = 「상따」 + `종목을 고르면 아래 값이 상한가 기준으로 채워져요`).
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp test -- strategy-badge &amp;&amp; pnpm --filter gh-radar-webapp build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "비활성" webapp/src/components/trading/strategy-badge.tsx` == 0
    - `strategyBadgesOf` 가 순수 함수로 export 되고 단위 테스트가 6종 조합을 단언
    - `webapp/src/app/trading/limit-chaser/page.tsx` 가 `redirect(` 를 호출한다
    - `grep -c "DMA 계정이 연결되지 않았어요" webapp/src/components/trading/dma-gate.tsx` == 1
    - `pnpm --filter gh-radar-webapp build` exit 0 (5개 라우트가 빌드된다)
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>배지·게이트 공용 컴포넌트와 라우트 5개가 존재하고 빌드된다.</done>
</task>

<task type="auto">
  <name>Task 3: sidebar-tree E2E 신규 + auth-guards 확장</name>
  <files>webapp/e2e/specs/sidebar-tree.spec.ts, webapp/e2e/specs/auth-guards.spec.ts</files>
  <read_first>
    - `webapp/e2e/specs/auth-guards.spec.ts` — 비로그인 리다이렉트 패턴, storageState 전환 방식
    - `webapp/e2e/specs/orderbook.spec.ts` — `mode:'serial'` + `withLocalRelay` + `beforeAll/afterAll/beforeEach` 규약
    - `webapp/e2e/fixtures/relay.ts`(16-02) — `seedDmaCredential`/`clearDmaCredentials`/`seedLimitChasers`
  </read_first>
  <action>
① `sidebar-tree.spec.ts` 신규(`mode:'serial'` + `withLocalRelay` 1회):
   1. 매핑 있음 + 전략 3건 시드 → 사이드바에 트리 8항목 + 3단 전략 3개가 보이고, 3단 항목 클릭 시 URL 이 `/trading/limit-chaser/` + 인코딩된 key 로 바뀐다.
   2. 매핑 없음(`clearDmaCredentials`) → 트레이딩 그룹·My page 가 **DOM 에 없다**(숨김이 아니라 미렌더).
   3. 비로그인 → 동일 미렌더 + 홈·상승률 상위·테마·AI 애널리스트는 보인다.
   4. 모바일 390 → ☰ 로 drawer 를 열면 **같은 트리**가 보이고, 3단 링크 클릭 시 drawer 가 닫힌다. **그룹 소제목 클릭으로는 닫히지 않는다.**
   5. `/scanner` 링크 텍스트가 「상승률 상위」이고 이동 후 URL 이 `/scanner` 다.

② `auth-guards.spec.ts` 확장: `/trading/limit-chaser/new` · `/trading/vi` · `/me` 직접 진입 시 ① 비로그인 → 로그인 유도(기존 규약과 동일한 방식) ② 로그인+매핑 없음 → `data-slot="dma-gate"` 가 보이고 제목이 「DMA 계정이 연결되지 않았어요」.
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp test:e2e -- sidebar-tree &amp;&amp; pnpm --filter gh-radar-webapp test:e2e -- auth-guards</automated>
  </verify>
  <acceptance_criteria>
    - 두 spec 모두 exit 0
    - 케이스 2·3 이 `toHaveCount(0)` 로 **미렌더**를 단언한다(`toBeHidden` 아님)
    - 케이스 4 가 390px 뷰포트에서 drawer 개폐를 검증한다
    - `sidebar-tree.spec.ts` 상단에 `test.describe.configure({ mode: 'serial' })` 존재
  </acceptance_criteria>
  <done>트리·게이트·drawer 동작이 E2E 로 고정된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 UI 노출 → 실제 권한 | 사이드바 숨김은 **편의**이지 권한이 아니다 |
| 직접 URL 진입 | 숨긴 메뉴도 URL 로 접근 가능하다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Elevation of Privilege | 사이드바 조건부 숨김 + 라우트 게이트 | mitigate | UI 숨김은 오진입 방지용이며 **실제 차단은 relay 의 `conn.unauthorized`**(16-07)다. 직접 URL 진입은 `<DmaGate>` 로 명시 차단하고 E2E 로 고정 |
| T-16-02 | Information Disclosure | 3단 전략 목록 | mitigate | 목록의 원천은 전역 wss 전략 스냅샷(자기 userId 한정, 16-07)뿐. 별도 API·조회 경로를 만들지 않는다 |
| T-16-09 | Information Disclosure | 전략 항목 표시 | accept | 3단에는 종목명만 표시(계좌번호·거래소 없음). 상세는 My page 에서 전체 표시가 계약(D2) |
</threat_model>

<verification>
- `pnpm typecheck` exit 0
- `pnpm --filter gh-radar-webapp test` exit 0
- `pnpm --filter gh-radar-webapp test:e2e -- sidebar-tree` / `-- auth-guards` / `-- smoke` exit 0
</verification>

<success_criteria>
- 사이드바 트리 + 3단 전략 목록 + 조건부 숨김
- 라우트 5개(리다이렉트 포함) 빌드·진입 가능
- strategy-badge / dma-gate 공용 컴포넌트 존재
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-11-SUMMARY.md` when done
</output>
