---
phase: 16-trading-limit-chaser-vi-my-page
plan: 25
type: execute
wave: 17
depends_on: ["16-19", "16-20", "16-23", "16-24"]
gap_closure: true
files_modified:
  - packages/shared/src/relay.ts
  - relay/src/ws/protocol.ts
  - relay/src/ws/fanout.ts
  - webapp/src/components/trading/limit-chaser-form.tsx
  - webapp/src/components/trading/limit-chaser-client.tsx
  - relay/tests/fanout.test.ts
  - relay/tests/protocol.test.ts
  - webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx
  - webapp/src/components/trading/__tests__/limit-chaser-client.test.tsx
autonomous: true
requirements: [TRADE-01, TRADE-03]

must_haves:
  truths:
    - "상따 전략의 시장구분을 **relay 가 ISIN 으로 푼다** — 브라우저는 `market` 을 싣지 않는다 (D-28. `order.new` 와 같은 규율이 `lc.set` 에도 적용된다)"
    - "브라우저의 `row.market === 'KOSDAQ' ? 'Q' : 'K'` 추정이 사라진다 — KONEX·`null`·미확인 sentinel 이 조용히 KOSPI 가 되지 않는다 (WR-03 / Pitfall 7 「엉뚱한 시장으로 주문이 나간다」)"
    - "relay 가 ISIN 을 못 풀면 전략을 **거부하고 사유를 프레임으로 돌려준다** — 형식만 보고 통과시키지 않는다"
    - "발주가 0 이거나 산출 수량이 0 이면 매수 게이트를 켤 수 없다 — 「매수 켜짐」 배지가 뜬 채 영원히 발주하지 않는 전략이 생기지 않는다 (WR-06)"
    - "relay 도 `buyEnabled && (buyOrderPrice === 0 || buyOrderQty === 0)` 를 거부한다 — UI 를 우회한 경로가 있어도 무장 상태가 만들어지지 않는다"
    - "검색 결과에서 시장구분을 알 수 없는 종목은 `isin === null` 과 같은 취급으로 선택 자체가 막힌다"
  artifacts:
    - path: "packages/shared/src/relay.ts"
      provides: "market 을 뺀 RelayLimitChaserInput"
      contains: "RelayLimitChaserInput"
    - path: "relay/src/ws/fanout.ts"
      provides: "lc.set 의 ISIN→market 해석 + 무장 조건 거부"
      contains: "symbols"
    - path: "webapp/src/components/trading/limit-chaser-form.tsx"
      provides: "발주가·수량 0 일 때 게이트 비활성 + 사유"
      contains: "data-slot"
  key_links:
    - from: "relay/src/ws/fanout.ts"
      to: "relay/src/store/symbols.ts"
      via: "symbols.lookup(cfg.isin) → market"
      pattern: "symbols.*lookup\\("
    - from: "webapp/src/components/trading/limit-chaser-form.tsx"
      to: "webapp/src/lib/limit-chaser.ts"
      via: "buyOrderQtyFromAmount 로 산출한 수량이 게이트 활성 조건"
      pattern: "buyOrderQtyFromAmount"
---

<objective>
**WR-03 + WR-06** 을 닫는다. 둘 다 「브라우저가 지어낸 값이 실계좌 자동발주 설정이 된다」는 한 뿌리다.

- **WR-03 (D-28 위반):** `limit-chaser-client.tsx:834` 가 `market: row.market === 'KOSDAQ' ? 'Q' : 'K'` 로 시장을 **추정**한다. KOSDAQ 이 아닌 **모든** 값(KONEX, `null`, master-sync 의 미확인 sentinel 등)이 조용히 KOSPI 가 된다. 이 값은 `buildCfg` 를 거쳐 `lc.set.cfg.market` 으로 나가고, relay 는 `z.enum(["K","Q"])` **형식만** 볼 뿐 `SymbolMap` 과 대조하지 않는다. 즉 `order.new` 에서는 relay 가 시장을 소유하는데(D-28) 전략 등록에서는 브라우저의 추측이 그대로 자동발주 설정이 된다 — `envelope.ts` 가 Pitfall 7 로 적어 둔 「엉뚱한 시장으로 주문이 나간다」의 정확한 조건이다.
- **WR-06:** `mergeMasterAndQuote` 는 `stock_quotes` 행이 없으면 `upperLimit: 0`·`price: 0` 을 돌려준다. 그런 종목을 고르면 `seedFromUpperLimit(0)` 이 모든 가격 칸을 0 으로 채우고, 폼의 `disabled` 는 `isin`·`accountNo`·`status` 만 보므로 **매수 스위치를 켤 수 있다.** `toggleGate` 가 확인 없이 `{buyEnabled:true, buyOrderPrice:0, buyOrderQty:0}` 을 보내고(`UIntSchema` 는 0 을 통과시킨다), 화면은 「매수 켜짐」 배지를 그린다. 사용자는 무장했다고 믿지만 그 전략은 영원히 발주하지 않는다 — 조용한 실패다.

**해결 방향은 D-28 그대로다.** 브라우저가 보낸 `market` 을 *검증*하는 것이 아니라 **애초에 받지 않고 relay 가 ISIN 으로 푼다.** `order.new` 가 이미 그렇게 한다(`order-handler.ts` 게이트 ③-1: 「모르는 종목·모르는 시장은 지어내지 않고 거부한다 — 기본값 "K" 로 메우면 코스닥 주문이 코스피로 나간다」). `lc.set` 만 예외였다.

Purpose: 시장구분의 소유권을 relay 하나로 통일하고, 「켜졌는데 아무 일도 안 하는」 전략을 만들 수 없게 한다.
Output: `RelayLimitChaserInput` 에서 `market` 제거 · relay 가 ISIN 으로 해석 · 무장 조건 거부 · UI 게이트 가드 · 검색 결과 선택 차단.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-23-SUMMARY.md
</context>

<interfaces>
<!-- 바꿔야 하는 현재 계약. -->

`packages/shared/src/relay.ts:229` — `RelayLimitChaserInput = Omit<RelayLimitChaser, "sellOrderQty"|"sellQtyTrackBaseline"|"sellEntryLatched"|"cancelQtyTrackBaseline"|"key">`. 주석이 「클라 입력 30 + 클라 고정 3 = 33필드」라고 적는다 — `market` 을 빼면 **32필드**가 되고 그 숫자도 함께 고쳐야 한다.

`relay/src/ws/protocol.ts:100` — `RelayLcSetSchema.cfg.market: z.enum(["K","Q"])` + 주석 「서버는 첫 글자만 읽는다 — 빈 값이 오면 KOSPI 로 오인되므로 열거로 못박는다」. **`z.object` 가 미지 키를 떨어뜨리므로 필드를 지우면 브라우저가 실어 보내도 통과하지 못한다**(같은 파일이 S→C 4필드에 이미 쓴 논리다).

`relay/src/ws/fanout.ts`:
- `WsFanoutDeps.symbols?: SymbolLookup` (L203, **optional**) — 「`orderStore` 와 **둘 다** 있어야 주문 분기가 열린다」(L198)
- `lc.set` 분기 (L559~570): `#strategySession` → `#accountAllowed` → `#buildStrategyPayload(… () => buildSetLimitChaserReq(cfg))` → `session.send`
- `#buildStrategyPayload` 가 조립 실패를 거부 프레임 + 로그로 바꾼다 — 새 거부도 이 경로를 쓴다
- `order.new` 경로는 `order-handler.ts` 에 위임하며 그쪽이 `symbols.lookup` 을 이미 쓴다

`relay/src/dma/envelope.ts` `buildSetLimitChaserReq(cfg)` — `cfg.market` 을 그대로 읽는다. 시그니처는 바꾸지 않고 **호출부가 채워 넣는다**.

`webapp/src/components/trading/limit-chaser-form.tsx`:
- `buildCfg` (L272~288) — `market` 을 `...values` 위에 얹어 넣는다. 의존성 배열 `[isin, accountNo, market, exchange]`
- `toggleGate` (L305~315) — `if (disabled) return;` 하나가 유일한 가드
- 매수 스위치 렌더 (L349~356) `checked: form.buyEnabled, onChange: (v) => toggleGate('buyEnabled', v)`
- `const buyQty = buyOrderQtyFromAmount(form.buyOrderAmount, form.buyOrderPrice);` (L337) — **이미 계산돼 있다**
- `const sellQty = estimatedSellQty(sellableQty, form.sellOrderRatio);` (L338)

`webapp/src/lib/limit-chaser.ts:89` `buyOrderQtyFromAmount(amountManwon, price)` — 가격이 0 이하면 **0** 을 돌려준다
`webapp/src/components/trading/limit-chaser-client.tsx`:
- L376 `const market: OrderMarket = picked?.market ?? server?.market ?? 'K';`
- L820~845 검색 결과 `<li>` — `disabled={row.isin === null}` + `onPick({... market: row.market === 'KOSDAQ' ? 'Q' : 'K' ...})`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: lc.set 의 시장구분 소유권을 relay 로 옮긴다 (WR-03 / D-28)</name>
  <files>packages/shared/src/relay.ts, relay/src/ws/protocol.ts, relay/src/ws/fanout.ts, relay/tests/fanout.test.ts, relay/tests/protocol.test.ts</files>
  <read_first>
    - `packages/shared/src/relay.ts` L150~240 — `RelayLimitChaser` 전문(특히 `market` 필드 주석)과 `RelayLimitChaserInput` 의 Omit 목록·「33필드」 문장
    - `relay/src/ws/protocol.ts` L85~135 `RelayLcSetSchema` 전문 + 그 위 「⚠️ **S→C 전용 4필드를 두지 않는다** … `z.object` 가 미지 키를 떨어뜨리므로 실려 와도 통과하지 못한다」 주석 — **이 논리를 `market` 에 그대로 적용한다**
    - `relay/src/ws/fanout.ts` L165~215(`WsFanoutDeps` — `symbols` optional 과 그 근거 주석), L285~325(생성자에서 `orderStore`+`symbols` 조합 판정), L520~615(전략 4분기 전문), `#buildStrategyPayload`·`#strategySession`·`#accountAllowed`·`#onStrategySendFailed` 정의
    - `relay/src/ws/order-handler.ts` 게이트 ③-1 (`deps.symbols.lookup(msg.isin)` → `info === undefined || info.market === null` 이면 거부, 문구 「이 종목은 지금 주문할 수 없습니다」) — **동형으로 만들 본보기**
    - `relay/src/store/symbols.ts` `SymbolLookup` 반환 형태(`{ code, market }`, `market` 은 `null` 가능)
    - `relay/tests/fanout.test.ts` — `WsFanout` 을 만드는 하네스가 `symbols` 를 어떻게(또는 안) 넘기는지. **lc.set 케이스가 있다면 그 하네스에 `symbols` 를 채워야 한다**
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §WR-03
  </read_first>
  <action>
① `packages/shared/src/relay.ts`: `RelayLimitChaserInput` 의 Omit 목록에 `"market"` 을 더한다. 주석의 「클라 입력 30 + 클라 고정 3 = 33필드」를 실제 개수로 고치고, 다음 한 줄을 더한다: 「**`market` 은 브라우저가 싣지 않는다** — relay 가 `SymbolMap` 으로 ISIN 을 풀어 채운다(D-28). `order.new` 와 같은 규율이다: 브라우저의 추측이 실계좌 발주 설정이 되지 않게 한다(WR-03).」

② `relay/src/ws/protocol.ts`: `RelayLcSetSchema.cfg` 에서 `market` 필드를 **삭제**한다. 삭제 자리에 S→C 4필드와 같은 형식의 ⚠️ 주석을 남긴다(왜 받지 않는지 + `z.object` 가 미지 키를 떨어뜨린다는 사실).

③ `relay/src/ws/fanout.ts` `lc.set` 분기를 고친다. `#accountAllowed` 통과 뒤, `#buildStrategyPayload` **앞에** ISIN 해석 단계를 넣는다:
  - `this.#symbols`(생성자에서 `deps.symbols` 를 인스턴스에 보관 — 지금은 주문 분기 조합 판정에만 쓰인다) 가 `undefined` 면 `lc.set` 을 **거부**한다. 사유 로그 + 거부 프레임. 「symbols 없이 전략을 조립하면 시장을 지어내게 된다」를 주석으로 남긴다. 프로덕션 부팅은 항상 `symbols` 를 결선하므로 이 분기는 테스트 하네스 보호용이다.
  - `const info = this.#symbols.lookup(cfg.isin)` → `info === undefined || info.market === null` 이면 거부한다. 로그는 `logger.error`, 프레임 문구는 `order-handler.ts` 게이트 ③-1 과 **같은 어조**로 짧게(「이 종목은 지금 전략을 등록할 수 없습니다.」). 계좌번호를 로그에 싣지 않는다(`maskAccountNo` 규율 유지).
  - 조립 호출을 `buildSetLimitChaserReq({ ...cfg, market: info.market })` 로 바꾼다.
  - 분기 위 주석(「① Ready 세션 확인 → ② 계좌 화이트리스트 대조 → ③ 조립·송신」)에 `lc.set` 만 **②-1 ISIN→시장 해석** 단계가 있다는 사실을 더한다.

④ 거부 프레임 형식은 **기존 전략 거부 경로와 동일**해야 한다 — `#buildStrategyPayload` 가 조립 실패에 쓰는 프레임 형태를 그대로 재사용하거나, 같은 헬퍼를 호출한다. 새 프레임 종류를 만들지 않는다(브라우저 리듀서에 새 case 가 필요해진다).

⑤ 테스트:
  - `relay/tests/protocol.test.ts`: 「`lc.set.cfg` 에 `market` 을 실어 보내도 파싱 결과에 그 키가 없다」(미지 키 탈락) + 「`market` 없이 보낸 정상 `lc.set` 은 통과한다」.
  - `relay/tests/fanout.test.ts`: 「`SymbolMap` 이 아는 KOSDAQ ISIN 으로 `lc.set` 을 보내면 게이트웨이로 나간 `SetLimitChaserReq` 의 market 이 `"Q"` 다」 — 브라우저가 아무것도 안 실었는데 relay 가 채웠음을 증명한다. FakeGateway 로 나간 프레임을 파싱해 확인한다.
  - `relay/tests/fanout.test.ts`: 「`SymbolMap` 이 모르는 ISIN 은 거부 프레임이 나가고 게이트웨이로 **0바이트**다」.
  - 기존 `lc.set` 케이스 중 `market` 을 싣던 것은 전부 빼고, 하네스에 `symbols` 를 결선한다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/shared build &amp;&amp; pnpm --filter @gh-radar/relay exec vitest run tests/protocol.test.ts tests/fanout.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 위 명령 exit 0
    - `grep -c "market" relay/src/ws/protocol.ts` 에서 `RelayLcSetSchema.cfg` 안의 `market:` 필드 0건 (`grep -n "market:" relay/src/ws/protocol.ts` 출력에 `lc.set` 스키마 줄 없음 — SUMMARY 에 인용)
    - `grep -c '"market"' packages/shared/src/relay.ts` >= 1 (Omit 목록)
    - `grep -c "lookup(cfg.isin)" relay/src/ws/fanout.ts` >= 1
    - `grep -c "buildSetLimitChaserReq({ ...cfg" relay/src/ws/fanout.ts` >= 1 — 조립 시점에 relay 가 채운다
    - `pnpm typecheck` exit 0 — webapp 이 `market` 을 여전히 싣고 있으면 **여기서 깨진다**(Task 2 가 그것을 고친다. 두 태스크를 같은 커밋 경계로 묶어도 된다)
  </acceptance_criteria>
  <done>브라우저가 시장을 보낼 수 없고, relay 가 못 풀면 전략이 등록되지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: 브라우저에서 시장 추정 제거 + 시장 미상 종목 선택 차단</name>
  <files>webapp/src/components/trading/limit-chaser-form.tsx, webapp/src/components/trading/limit-chaser-client.tsx, webapp/src/components/trading/__tests__/limit-chaser-client.test.tsx</files>
  <read_first>
    - `webapp/src/components/trading/limit-chaser-form.tsx` L265~290 `buildCfg` 와 그 의존성 배열, `market` prop 선언부
    - `webapp/src/components/trading/limit-chaser-client.tsx` L370~380 (`market` 파생), L805~850 (검색 결과 `<li>` 와 `onPick` 페이로드, `disabled={row.isin === null}` 형식과 그 옆 클래스 `disabled:opacity-45`)
    - `webapp/src/components/trading/limit-chaser-client.tsx` 의 `picked` 타입 선언 — `market` 필드를 지울지 표시용으로 남길지 판단 근거
    - `webapp/src/lib/stock-api.ts` 검색 결과 타입의 `market` 필드 값 종류(`'KOSPI' | 'KOSDAQ' | …`)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md` — 검색 결과 행의 비활성 표기(「주문 불가」 류) 규약. **새 배지 문구는 UI-SPEC 어조를 따른다**
    - Task 1 이 바꾼 `RelayLimitChaserInput`
  </read_first>
  <action>
① `limit-chaser-form.tsx` `buildCfg` 에서 `market` 을 뺀다. 의존성 배열에서도 뺀다. `market` prop 자체는 **표시용으로 남길 수 있다**(상태줄·배지가 쓰면) — 쓰지 않으면 prop 도 지운다. 어느 쪽이든 「이 값은 **와이어로 나가지 않는다**(D-28: relay 가 ISIN 으로 푼다)」를 주석으로 못박는다.

② `limit-chaser-client.tsx`:
  - L376 의 `?? 'K'` **폴백을 없앤다.** 표시용으로 남긴다면 `picked?.market ?? server?.market ?? null` 로 두고, `null` 이면 화면에 시장 배지를 그리지 않는다(모르면 모른다고 둔다). 지어낸 기본값을 표시에 쓰지 않는다.
  - 검색 결과 `onPick` 페이로드에서 `market: row.market === 'KOSDAQ' ? 'Q' : 'K'` 를 **삭제**한다. `picked` 타입에서 `market` 이 필요 없어지면 함께 지운다.
  - 검색 결과 행의 `disabled` 조건을 `row.isin === null || (row.market !== 'KOSPI' && row.market !== 'KOSDAQ')` 으로 넓힌다. 비활성 행에는 기존 `disabled:opacity-45` 와 함께 사유 배지 한 개(문구는 UI-SPEC 어조, `data-slot="lc-search-unorderable"`)를 붙인다. 이유: relay 가 못 푸는 종목을 고르게 두면 사용자가 폼을 다 채운 뒤에야 거부 프레임을 본다.

③ `limit-chaser-client.test.tsx` 케이스 2건:
  - 「`market` 이 `KOSPI`·`KOSDAQ` 가 아닌 검색 결과는 선택할 수 없고 사유 배지가 붙는다」(KONEX·`null` 두 변형).
  - 「선택 후 전송되는 `lc.set.cfg` 에 `market` 키가 **없다**」 — send 스텁이 받은 객체에 `'market' in cfg === false` 를 단언한다. 이것이 D-28 회귀 잠금이다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp exec vitest run src/components/trading/__tests__/limit-chaser-client.test.tsx src/components/trading/__tests__/limit-chaser-form.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 위 명령 exit 0
    - `grep -c "'KOSDAQ' ? 'Q' : 'K'" webapp/src/components/trading/limit-chaser-client.tsx` == 0
    - `grep -c "?? 'K'" webapp/src/components/trading/limit-chaser-client.tsx` == 0
    - `grep -n "market" webapp/src/components/trading/limit-chaser-form.tsx` 출력에 `buildCfg` 본문 줄이 없다 (SUMMARY 에 인용)
    - `grep -c "lc-search-unorderable" webapp/src/components/trading/limit-chaser-client.tsx` >= 1
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>브라우저 어디에도 시장 추정이 없고, 고를 수 없는 종목은 목록에서 그렇게 보인다.</done>
</task>

<task type="auto">
  <name>Task 3: 발주가·수량 0 인 게이트 무장을 UI 와 relay 양쪽에서 막는다 (WR-06)</name>
  <files>webapp/src/components/trading/limit-chaser-form.tsx, relay/src/ws/fanout.ts, webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx, relay/tests/fanout.test.ts</files>
  <read_first>
    - `webapp/src/components/trading/limit-chaser-form.tsx` L295~345 (`toggleGate`·`handleSubmit`·`buyQty`/`sellQty` 계산), L340~420 (매수 카드 렌더 — `Group slot="buy"` 와 스위치 props 형식), 매도·스윕 카드의 같은 자리
    - `webapp/src/lib/limit-chaser.ts` L80~95 `buyOrderQtyFromAmount`(가격 0 이하면 0), `estimatedSellQty`, `crudOf(gates)`
    - `server/src/mappers/stock.ts` L110~125 `mergeMasterAndQuote` — `stock_quotes` 행이 없을 때 `upperLimit: 0`·`price: 0` 을 돌려주는 자리(WR-06 의 발생 조건)
    - `relay/src/ws/fanout.ts` `lc.set` 분기 (Task 1 이 바꾼 뒤 상태) 와 `#buildStrategyPayload` 의 거부 프레임 형식
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §WR-06
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md` 상따 폼 매수/매도/한방 박스 절 — 비활성 사유 문구의 형태·위치 규약
  </read_first>
  <action>
① `limit-chaser-form.tsx` 에 게이트별 무장 가능 판정을 만든다. 이미 있는 파생값(`buyQty`, `sellQty`)을 재사용한다 — **산출식을 복제하지 않는다**(`lib/limit-chaser.ts` 가 유일 지점이다).
  - 매수: `form.buyOrderPrice > 0 && buyQty > 0`
  - 매도: `form.sellOrderPrice > 0 && sellQty > 0`
  - 한방(스윕): `form.sweepWatchPrice > 0` (스윕은 매수 발주를 재계산하므로 매수 조건도 함께 본다 — 어느 쪽을 요구할지는 `crudOf`/서버 검증과 대조해 정하고 그 근거를 주석에 적는다)
  각 스위치의 `disabled` 에 `|| !canArmX` 를 더하고, **켜져 있는 상태를 끄는 것은 언제나 허용**한다(무장 해제를 막으면 그게 더 위험하다 — `checked === true` 면 `disabled` 를 걸지 않는다). 이 예외를 주석으로 못박는다.

② 비활성 사유를 한 줄로 말한다. 각 카드 안에 `data-slot="lc-arm-blocked"` 한 줄(문구는 UI-SPEC 어조, 예: 「시세를 받지 못해 발주가·수량이 0 이에요. 가격을 입력하면 켤 수 있어요.」). 배지만 회색으로 두고 이유를 말하지 않으면 사용자는 왜 안 켜지는지 모른다.

③ `toggleGate` 에도 같은 판정을 건다 — `disabled` 는 렌더 시점 방어이고 `toggleGate` 는 전송 직전 방어다. 두 곳이 **같은 파생값**을 읽게 해서 갈리지 않게 한다(`vi-order-list.tsx` 의 `isConfirmable` 이 「`disabled` 와 전송 가드가 같은 함수를 쓴다」로 이미 쓰는 규율이다 — 같은 형태로 만든다).

④ `relay/src/ws/fanout.ts` `lc.set` 분기에 무장 조건 검사를 더한다(Task 1 의 ISIN 해석 다음, 조립 전). `cfg.buyEnabled === true && (cfg.buyOrderPrice === 0 || cfg.buyOrderQty === 0)` 이면 거부한다 — 매도(`sellEnabled` + `sellOrderPrice === 0`)도 동형으로 본다. 사유 로그 + 기존 형식의 거부 프레임. 주석: 「UI 가 막지만 **조립 단계가 모든 호출 경로의 마지막 관문**이어야 한다. `UIntSchema` 는 0 을 통과시키므로 스키마는 이것을 잡지 못한다.」

⑤ 테스트:
  - `limit-chaser-form.test.tsx`: 「`upperLimit 0` 종목(가격 칸 전부 0)에서 매수 스위치가 비활성이고 `lc-arm-blocked` 문구가 뜬다」 · 「가격을 입력하면 켤 수 있게 된다」 · 「이미 켜진 게이트는 값이 0 이 돼도 **끌 수 있다**」.
  - `relay/tests/fanout.test.ts`: 「`buyEnabled:true, buyOrderPrice:0` 인 `lc.set` 은 거부 프레임이 나가고 게이트웨이로 0바이트다」.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp exec vitest run src/components/trading/__tests__/limit-chaser-form.test.tsx &amp;&amp; pnpm --filter @gh-radar/relay exec vitest run tests/fanout.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 위 명령 exit 0
    - `grep -c "lc-arm-blocked" webapp/src/components/trading/limit-chaser-form.tsx` >= 1
    - `grep -c "buyOrderQtyFromAmount" webapp/src/components/trading/limit-chaser-form.tsx` >= 1 — 산출식을 복제하지 않고 기존 파생값을 쓴다
    - `grep -c "buyEnabled" relay/src/ws/fanout.ts` >= 1 — relay 쪽 거부가 실재
    - 「이미 켜진 게이트는 끌 수 있다」 케이스가 테스트 파일에 존재 (`grep -c "끌 수 있" webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx` >= 1)
    - `pnpm --filter @gh-radar/webapp test` · `pnpm --filter @gh-radar/relay test` 전체 exit 0
  </acceptance_criteria>
  <done>발주할 수 없는 전략은 무장되지 않고, 이미 무장된 것은 언제나 해제할 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 `lc.set.cfg` → 게이트웨이 자동발주 설정 | 이 값이 그대로 **실계좌에서 반복 발주**하는 설정이 된다 — 한 번의 주문보다 위험 표면이 크다 |
| `SymbolMap`(relay) ↔ 검색 API `market`(server) | 시장구분의 정본이 둘일 수 없다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-42 | Tampering (엉뚱한 시장) | `lc.set.cfg.market` | mitigate | 스키마에서 필드를 **제거**하고 relay 가 `symbols.lookup(isin)` 으로 채운다(D-28). 못 풀면 거부. `order.new` 게이트 ③-1 과 동형. 「`cfg` 에 `market` 키가 없다」를 webapp 테스트가 잠근다 |
| T-16-43 | Spoofing (무장 위장) | 발주가·수량 0 인 게이트 | mitigate | UI 스위치 `disabled` + `toggleGate` 전송 가드가 **같은 파생값**을 읽는다. relay 도 `buyEnabled && price/qty 0` 을 거부 — UI 우회 경로 봉쇄 |
| T-16-44 | Denial of Service (사용자 자산) | 무장 해제 차단 | mitigate | `checked === true` 면 `disabled` 를 걸지 않는다. 끄는 길을 막으면 그게 더 위험하다 — 전용 테스트로 잠금 |
| T-16-45 | Information Disclosure | 새 거부 로그 | mitigate | `isin`·사유만 로그. 계좌번호는 `maskAccountNo` 규율 유지 |
| T-16-13 | 운영 사고 | 실서버·실계좌 접속 | accept | FakeGateway·RTL 만 쓴다(D-27 승계) |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/relay test` · `pnpm --filter @gh-radar/webapp test` · `pnpm --filter @gh-radar/shared test` exit 0
- `pnpm typecheck` (13 워크스페이스) exit 0
- `pnpm --filter @gh-radar/webapp exec playwright test webapp/e2e/specs/trading-limit-chaser.spec.ts` exit 0 — 상따 표면 회귀 확인
- `grep -rn "10\.41\.1\.120" relay/ webapp/src` 0건 (D-27 승계)
</verification>

<success_criteria>
- `lc.set` 에 `market` 이 없고 relay 가 ISIN 으로 채운다
- relay 가 못 푸는 ISIN 은 거부되고 게이트웨이로 0바이트다
- 시장 미상 종목은 검색 결과에서 선택할 수 없다
- 발주가·수량 0 이면 게이트를 켤 수 없고 이유가 화면에 있다
- 이미 켜진 게이트는 언제나 끌 수 있다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-25-SUMMARY.md` when done
</output>
</content>
