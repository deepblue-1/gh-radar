---
phase: 16-trading-limit-chaser-vi-my-page
plan: 18
type: execute
wave: 15
depends_on: ["16-17"]
gap_closure: true
files_modified:
  - relay/src/store/orders.ts
  - relay/src/ws/order-handler.ts
  - supabase/migrations/20260909120000_dma_orders_user_order_no_unique.sql
  - relay/tests/order-store.test.ts
  - relay/tests/ws-order.test.ts
autonomous: false
requirements: [TRADE-03]

must_haves:
  truths:
    - "같은 `order_no` 가 다른 사용자·다른 날짜에 존재해도 오늘 내 자동주문 통보는 **내 오늘 행만** 찾는다 — 못 찾으면 새 행으로 insert 되어 감사 기록이 비지 않는다 (gap 1 / 16-08 must-have / Pitfall 18)"
    - "`order_no` 셀렉터 update 는 `user_id` 와 KST 당일 범위로 좁혀져 **다른 사용자의 행을 덮을 수 없다** (gap 1 테넌트 경계 / T-16-14)"
    - "`userId` 없는 `order_no` 갱신은 큐에 실리지 않고 error 로그 + 드롭 카운터로 드러난다 (S-5 · 전역 update 구조적 차단)"
    - "같은 자동주문의 접수(A)·체결(E) 통보가 수십 ms 간격으로 겹쳐도 `dma_orders` 행은 **1건만** 만들어진다 (WR-01)"
    - "DB 가 `(user_id, order_no, KST일)` 중복을 부분 UNIQUE 인덱스로 구조적으로 막고, 그 인덱스가 프로덕션에 적용돼 있다"
    - "경계 테스트가 sink 를 스텁으로 바꾸지 않고 **Supabase 쿼리 자체**(적용된 필터 목록)를 검사한다 — gap 1 이 통과했던 사각지대가 닫힌다"
  artifacts:
    - path: "relay/src/store/orders.ts"
      provides: "사용자·당일 경계가 붙은 조회/갱신 sink + KST 일 범위 헬퍼"
      contains: "kstDayRangeUtc"
    - path: "relay/src/ws/order-handler.ts"
      provides: "userId 를 실은 조회 + orderNo 단위 in-flight 가드"
      contains: "inflight"
    - path: "supabase/migrations/20260909120000_dma_orders_user_order_no_unique.sql"
      provides: "(user_id, order_no, KST일) 부분 UNIQUE 인덱스 + 선행 중복 점검"
      contains: "CREATE UNIQUE INDEX"
    - path: "relay/tests/order-store.test.ts"
      provides: "가짜 SupabaseClient 로 적용 필터를 검사하는 경계 테스트"
      contains: "supabaseOrderLookupSink"
  key_links:
    - from: "relay/src/ws/order-handler.ts"
      to: "relay/src/store/orders.ts"
      via: "findIdByOrderNo(userId, orderNo)"
      pattern: "findIdByOrderNo\\(\\s*userId"
    - from: "relay/src/store/orders.ts"
      to: "Supabase dma_orders"
      via: "user_id 필터가 붙은 select/update"
      pattern: "eq\\(\"user_id\""
---

<objective>
**gap 1 (BLOCKER) + WR-01** 을 닫는다. 자동주문 통보의 행 조회·갱신에 **사용자 경계와 당일 경계**를 넣고, 조회↔insert 사이의 경주를 막고, DB 에 구조적 방어선(부분 UNIQUE 인덱스)을 세운다.

지금 상태: `supabaseOrderLookupSink` 가 `.eq("order_no", orderNo).maybeSingle()` 만 한다. 브로커 주문번호는 **일별 재사용 시퀀스**이고 `dma_orders.order_no` 에는 UNIQUE 가 없다(`20260905120200_dma_orders.sql:76` 은 일반 인덱스). 그래서 운영 2일차부터 **어제 행이 매치되어 오늘 자동주문의 insert 가 일어나지 않고**(감사 기록 결손 — 16-08 이 막겠다고 선언한 바로 그 실패), 다중 매치에서는 `maybeSingle()` 이 던져 catch 가 셀렉터 없는 `enqueueUpdate` 로 열화해 **전 사용자·전 날짜 행**을 덮는다(테넌트 간 쓰기).

Purpose: 요구사항 TRADE-03 의 「`dma_orders` insert/update 를 relay 가 전담」은 그 전담이 **정확할 때만** 성립한다. 지금은 구현돼 있지만 틀렸다.
Output: 사용자·당일 경계가 붙은 sink 3종 · in-flight 가드 · 부분 UNIQUE 인덱스(프로덕션 적용) · sink 를 스텁으로 바꾸지 않는 경계 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-CONTEXT.md
</context>

<interfaces>
<!-- 실행자가 그대로 따라야 하는 현재 계약. 자세한 구현은 read_first 로 직접 본다. -->

`relay/src/store/orders.ts` (현재):
- `export type OrderLookupSink = (orderNo: string) => Promise<string | null>;`
- `export type OrderSelector = { column: "id" | "order_no"; value: string };`
- `export type OrderUpdate = { orderRowId?; orderNo?; status?; resultCode?; noticeType?; message?; filledQty?; origin? }`
- `export function selectorOf(update: OrderUpdate): OrderSelector | null` — `id` 우선, 없으면 `order_no`, 둘 다 없으면 `null`
- `supabaseOrderSink(supabase)` 는 `.update(patch).eq(sel.column, sel.value)` 하나뿐이다

`server/src/services/dma-orders.ts:110` 의 KST 규칙(그대로 복제할 정본):
`new Date(\`${day}T00:00:00+09:00\`)` 를 `from`, `from + 24*3600_000` 을 `to` 로 하는 **반열린 구간** `[from, to)`.

`relay/src/ws/order-handler.ts` 의 소비 계약:
- `interface OrderRecorder { insertRequest(row): Promise<string>; enqueueUpdate(update): void; findIdByOrderNo(orderNo): Promise<string | null>; }`
- `recordUnmatched(userId, notice)` 안에서 `userId` 는 이미 스코프에 있다.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: OrderLookupSink·OrderSelector 에 사용자·당일 경계 도입 + 가짜 SupabaseClient 경계 테스트</name>
  <files>relay/src/store/orders.ts, relay/tests/order-store.test.ts</files>
  <read_first>
    - `relay/src/store/orders.ts` 전문 — 특히 파일 상단 결정근거 주석(A10 / S-5 / D-03), `OrderUpdate`(L82~100), `OrderSelector`(L102), `supabaseOrderSink`(L181~190), `supabaseOrderLookupSink`(L232~252), `selectorOf`(L452~457), `enqueueUpdate` 의 셀렉터 null 드롭 경로(L358~370)
    - `relay/tests/order-store.test.ts` 전문 — 특히 파일 상단 「Supabase 를 흉내 내는 대신 쓰기 sink 를 주입한다」 주석과 `recordingSinks()` 구조. **이 테스트가 gap 1 을 놓친 이유가 그 문장이다** — 이 태스크는 그 규율에 예외 한 벌을 추가하는 것이지 기존 케이스를 지우는 것이 아니다
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md` gaps[0] (truth / reason / artifacts / missing 4항목)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §CR-02 (경로 A / 경로 B 서술과 Fix 3단계)
    - `server/src/services/dma-orders.ts` L105~121 `kstDayRangeUtc` — 복제할 KST 규칙의 정본
  </read_first>
  <action>
① `relay/src/store/orders.ts` 에 KST 일 범위 헬퍼를 추가한다. 이름은 `kstDayRangeUtc`, 시그니처는 `(now: Date = new Date()) => { from: string; to: string }`. 구현 규칙은 server 정본과 **같은 반열린 구간**이다: 현재 시각에 `9 * 3600_000` ms 를 더한 뒤 `.toISOString().slice(0, 10)` 으로 KST 날짜(`YYYY-MM-DD`)를 얻고, `new Date(\`${day}T00:00:00+09:00\`)` 를 `from`, `from.getTime() + 24 * 3600_000` 을 `to` 로 한다. export 한다(테스트가 직접 부른다). server 를 import 하지 않는다 — relay 는 server 를 의존하지 않는다.

② `OrderLookupSink` 시그니처를 `(userId: string, orderNo: string) => Promise<string | null>` 로 좁힌다. `supabaseOrderLookupSink` 의 쿼리를 `.select("id").eq("user_id", userId).eq("order_no", orderNo).gte("created_at", from).lt("created_at", to).order("created_at", { ascending: false }).limit(1)` 로 바꾼다. `maybeSingle()` 을 쓰지 않는다 — 같은 사용자·같은 날 중복은 이제 DB 인덱스가 막고, 그럼에도 2행이 있으면 던져서 catch 열화를 부르는 것보다 **가장 최근 1행**을 고르는 편이 안전하다. `error` 는 기존대로 `logger.error` + `throw`.

③ `OrderSelector` 를 판별 유니온으로 바꾼다: `{ column: "id"; value: string }` 또는 `{ column: "order_no"; value: string; userId: string }`. `OrderUpdate` 에 `userId?: string` 필드를 더하고 JSDoc 에 「`order_no` 셀렉터에서만 쓰인다. 없으면 그 갱신은 **드롭**이다 — `order_no` 단독 update 는 전역 쓰기다」를 적는다.

④ `selectorOf` 를 고친다. `orderRowId` 가 있으면 그대로 `id` 셀렉터. 없고 `orderNo` 가 있는데 **`userId` 가 없으면 `null` 을 돌려준다**(= 드롭). `userId` 가 있을 때만 `order_no` 셀렉터를 만든다. `enqueueUpdate` 의 셀렉터 null 드롭 로그 문구에 「userId 없는 order_no 갱신 포함」을 덧붙여 사유가 로그에서 갈라지게 한다.

⑤ `supabaseOrderSink` 를 고친다. `sel.column === "id"` 면 지금 그대로. `sel.column === "order_no"` 면 `.update(patch).eq("order_no", sel.value).eq("user_id", sel.userId).gte("created_at", from).lt("created_at", to)` 로 **세 축을 전부** 건다. `from`/`to` 는 ①의 헬퍼로 얻는다. 파일 상단 주석의 「`WHERE` 를 셀렉터 한 개로 반드시 좁힌다」 문장을 「`order_no` 셀렉터는 사용자·당일까지 세 축으로 좁힌다 — 한 축만으로는 전역 쓰기다」로 정정한다.

⑥ `relay/tests/order-store.test.ts` 에 새 describe 블록 「Supabase 쿼리 경계 (gap 1)」를 추가한다. **여기서는 sink 를 스텁으로 바꾸지 않는다** — `supabaseOrderLookupSink`/`supabaseOrderSink` 에 가짜 `SupabaseClient` 를 주입한다. 가짜는 `from(table)` → `{ select, update }` → 체이닝 메서드(`eq`/`gte`/`lt`/`order`/`limit`)가 각각 `{ column, op, value }` 를 배열에 기록하고 자기 자신을 돌려주는 형태이며, 마지막에 `then`(또는 `await` 대상)이 필터를 실제로 적용한 행 배열을 돌려준다. 픽스처 행 3건을 둔다: (a) userB · 같은 `order_no` · 오늘, (b) userA · 같은 `order_no` · **어제**, (c) userA · 같은 `order_no` · 오늘. 케이스:
  - `supabaseOrderLookupSink(fake)("userA", "0000012345")` 가 **(c) 의 id 만** 돌려준다.
  - 적용된 필터 목록에 `{op:"eq", column:"user_id"}` 와 `created_at` 의 `gte`·`lt` 가 **모두** 있다.
  - `supabaseOrderSink(fake)({ column:"order_no", value:"0000012345", userId:"userA" }, patch)` 의 update 필터에 `user_id` 와 `created_at` 범위가 붙는다 → 가짜가 계산한 「영향 받은 행」에 (a)·(b) 가 **없다**.
  - `selectorOf({ orderNo:"0000012345", status:"filled" })`(userId 없음)가 `null` 이다.
기존 케이스 중 `OrderLookupSink` 시그니처 변경으로 깨지는 것은 `(userId, orderNo)` 로 맞춰 고친다 — 케이스를 지우지 않는다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay exec vitest run tests/order-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @gh-radar/relay exec vitest run tests/order-store.test.ts` exit 0
    - `grep -c 'eq("user_id"' relay/src/store/orders.ts` >= 2 (lookup 1 + update 1)
    - `grep -c "kstDayRangeUtc" relay/src/store/orders.ts` >= 3 (정의 1 + 사용 2)
    - `grep -c "maybeSingle" relay/src/store/orders.ts` == 0
    - `grep -c "supabaseOrderLookupSink\|supabaseOrderSink" relay/tests/order-store.test.ts` >= 2 — 새 경계 테스트가 실제 sink 팩토리를 부른다
    - `pnpm --filter @gh-radar/relay run typecheck` exit 0 · `pnpm --filter @gh-radar/relay run typecheck:tests` exit 0
  </acceptance_criteria>
  <done>조회·갱신 두 경로 모두 `user_id` + KST 당일로 좁혀졌고, 가짜 SupabaseClient 가 그 필터의 실재를 단언한다.</done>
</task>

<task type="auto">
  <name>Task 2: recordUnmatched 에 userId 전달 + orderNo 단위 in-flight 가드 (WR-01)</name>
  <files>relay/src/ws/order-handler.ts, relay/tests/ws-order.test.ts</files>
  <read_first>
    - `relay/src/ws/order-handler.ts` L100~115 `OrderRecorder` 인터페이스, L232~245 통보 소비 루프, L246~260 `patchOf`, L262~330 `recordUnmatched` 와 그 주석(「여기가 Pitfall 18 의 자리다」), `autoInsertRow`
    - `relay/tests/ws-order.test.ts` 전문 — 특히 sink 를 스텁으로 대체하는 부분과 케이스 ⑬(「행이 이미 있는」 경우)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §WR-01 (`ensureRow` 스케치 포함)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md` gaps[0].artifacts 의 `order-handler.ts` 항목 (「catch 가 셀렉터 없는 enqueueUpdate 로 열화한다」)
    - Task 1 이 바꾼 `relay/src/store/orders.ts` 의 새 시그니처
  </read_first>
  <action>
① `OrderRecorder.findIdByOrderNo` 를 `(userId: string, orderNo: string) => Promise<string | null>` 로 맞춘다.

② `recordUnmatched` 를 고친다.
  - `originKind === "manual"` 분기의 `enqueueUpdate(patch)` 를 `enqueueUpdate({ ...patch, userId })` 로 바꾼다. 이게 없으면 Task 1 의 `selectorOf` 가 `null` 을 돌려 그 갱신이 드롭된다 — **접수 이후 수동 통보가 통째로 사라진다.**
  - 조회 실패 catch 의 열화 경로도 `enqueueUpdate({ ...patch, userId })` 로 바꾸고, 주석의 전제 문장(「없으면 0행이라 무해하고」)을 「셀렉터가 사용자·당일로 좁혀져 있으므로 없으면 0행이고 있으면 **내 행**이다」로 정정한다.
  - `findIdByOrderNo(notice.orderNo)` 호출을 `findIdByOrderNo(userId, notice.orderNo)` 로 바꾼다.

③ **in-flight 가드**(WR-01). 팩토리 스코프(`createOrderHandler` 안, `conns`/`byUser` 와 같은 레벨)에 `const inflight = new Map<string, Promise<string | null>>()` 을 두고, 「조회 → 없으면 insert」 구간 전체를 `ensureRow(userId, notice): Promise<string | null>` 로 감싼다. 키는 `` `${userId}|${notice.orderNo}` `` 다. 이미 진행 중인 Promise 가 있으면 **그것을 재사용**하고, 없으면 새로 만들어 `inflight.set` → `finally` 에서 `inflight.delete`. `ensureRow` 는 기존 행 id 또는 새로 만든 행 id 를 돌려주고, 행을 만들 수 없으면(`autoInsertRow` 가 `null`, 또는 insert 실패) `null` 을 돌려준다 — 사유 로그는 지금 자리 그대로 남긴다(S-5). `recordUnmatched` 는 `ensureRow` 결과가 `null` 이 아니면 `enqueueUpdate({ ...patch, orderRowId })` 를 부른다.
  - `closeConn`/`close` 는 `inflight` 를 건드리지 않는다 — 진행 중인 Supabase 왕복을 끊으면 그게 곧 기록 결손이다. 이 사실을 한 줄 주석으로 남긴다.

④ `relay/tests/ws-order.test.ts` 에 케이스 2건을 추가한다.
  - 「같은 자동주문의 접수·체결 통보가 insert 왕복 중에 겹쳐도 insert 는 1회다」: 스텁 `insertRequest` 를 **의도적으로 지연**시키고(예: 20ms) 같은 `order_no` 통보 2건을 연속 주입한 뒤 `inserts.length === 1` 을 단언한다.
  - 「자동주문 조회는 `(userId, orderNo)` 두 인자로 나간다」: 스텁 `findIdByOrderNo` 가 받은 첫 인자가 그 연결의 userId 임을 단언한다.
  기존 시그니처 변경으로 깨지는 스텁은 두 인자 형태로 고친다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay exec vitest run tests/ws-order.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @gh-radar/relay exec vitest run tests/ws-order.test.ts` exit 0
    - `grep -c "findIdByOrderNo(userId" relay/src/ws/order-handler.ts` >= 1
    - `grep -c "inflight" relay/src/ws/order-handler.ts` >= 3 (선언 + get/set + delete)
    - `grep -vc '^\s*\*' relay/src/ws/order-handler.ts` 로 주석을 걸러낸 뒤 `enqueueUpdate(patch)`(userId 없는 형태) 잔존 0건 — `grep -n "enqueueUpdate(patch)" relay/src/ws/order-handler.ts` 가 0줄
    - `pnpm --filter @gh-radar/relay test` 전체 exit 0 (315 + 신규 케이스)
  </acceptance_criteria>
  <done>자동주문 통보 경로가 항상 `userId` 를 싣고, 같은 주문번호의 동시 통보가 행을 2개 만들지 않는다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] (user_id, order_no, KST일) 부분 UNIQUE 인덱스 마이그레이션 작성 + supabase db push</name>
  <files>supabase/migrations/20260909120000_dma_orders_user_order_no_unique.sql</files>
  <read_first>
    - `supabase/migrations/20260905120200_dma_orders.sql` 전문 — 특히 L72~76 인덱스 2종과 L78~85 RLS/GRANT 블록(**이 블록을 다시 실행하지 않는다**)
    - `supabase/migrations/20260908120000_dma_orders_origin.sql` 전문 — 이 저장소의 마이그레이션 주석 형식·「하지 않는 것」 절의 본보기
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-01-PLAN.md` Task 3 — 직전 `supabase db push` 실행 절차의 선례(`SUPABASE_ACCESS_TOKEN`, applied 목록 확인)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md` gaps[0].missing 3번째 항목
  </read_first>
  <action>
`supabase/migrations/20260909120000_dma_orders_user_order_no_unique.sql` 을 만든다. 첫 줄은 `-- Phase 16 Plan 18 — TRADE-03 gap 1: dma_orders (user_id, order_no, KST일) 부분 UNIQUE 인덱스` 로 시작하고, 주석에 다음 근거를 적는다: 브로커 주문번호는 일별 재사용 시퀀스라 전역 UNIQUE 는 걸 수 없다 · relay 조회가 사용자·당일로 좁혀졌으므로 DB 도 같은 세 축으로 유일성을 보장해야 2차 방어선이 된다 · `timezone(text, timestamptz)` 와 `timestamp::date` 는 IMMUTABLE 이라 인덱스 표현식으로 허용된다.

본문 구성:
1. `BEGIN;`
2. **선행 중복 점검** `DO $$ … $$;` 블록. `public.dma_orders` 에서 `order_no IS NOT NULL` 인 행을 `user_id, order_no, (created_at AT TIME ZONE 'Asia/Seoul')::date` 로 그룹핑해 `HAVING count(*) > 1` 인 조합 수를 센다. 0 이 아니면 `RAISE EXCEPTION '선행 중복 %건 — 정리 후 재실행 (조회: 아래 SELECT)', dup_count;` 로 **인덱스를 만들지 않고 멈춘다.** 조용히 지나가면 push 가 실패한 이유를 다음 사람이 알 수 없다.
3. `CREATE UNIQUE INDEX idx_dma_orders_user_order_no_kst_day ON public.dma_orders (user_id, order_no, ((created_at AT TIME ZONE 'Asia/Seoul')::date)) WHERE order_no IS NOT NULL;`
4. `COMMENT ON INDEX public.idx_dma_orders_user_order_no_kst_day IS '한 사용자의 하루 안에서 order_no 는 유일하다. 브로커 주문번호가 일별 재사용 시퀀스라 전역 UNIQUE 는 불가능하다.';`
5. `COMMIT;`

**하지 않는 것:** `CREATE POLICY` 를 추가하지 않는다(정책 0개 default-deny 가 의도다). `GRANT`/`REVOKE` 를 다시 실행하지 않는다. 기존 `idx_dma_orders_order_no` 를 **지우지 않는다** — 인덱스를 지우는 것은 이 갭의 요구가 아니고, 남겨 두어도 조회 계획에 해가 없다.

그다음 **[BLOCKING] 게이트**: `SUPABASE_ACCESS_TOKEN` 을 환경변수로 둔 상태에서 `supabase db push` 를 실행한다(16-01 Task 3 선례). 대화형 프롬프트를 억제할 수 없으면 **멈추고 사용자에게 실행 방법을 물은 뒤 진행한다.** 적용 없이 다음 plan 으로 넘어가지 않는다 — 타입은 config 에서 오지 라이브 DB 에서 오지 않으므로, push 없이도 build·typecheck 는 통과하고 그건 **위양성 검증**이다.

push 가 실패하면 사유를 갈라 보고한다:
  - `RAISE EXCEPTION '선행 중복 …'` → 프로덕션에 이미 중복 조합이 있다. 어떤 `(user_id, order_no, KST일)` 조합이 몇 건인지 Supabase SQL 에디터로 조회한 결과를 SUMMARY 에 인용하고, **정리 방법을 사용자에게 물은 뒤** 진행한다(임의 삭제 금지 — 감사 기록이다).
  - `functions in index expression must be marked IMMUTABLE` → 표현식 인덱스 가정이 깨진 것이다. 임의 우회하지 말고 오류 원문을 그대로 보고하고 멈춘다.
  </action>
  <what-built>
`(user_id, order_no, KST일)` 부분 UNIQUE 인덱스 마이그레이션 파일 + `supabase db push` 실행 결과.
  </what-built>
  <how-to-verify>
1. `supabase db push` 출력에 `20260909120000_dma_orders_user_order_no_unique` 가 applied 로 나타나는지 확인한다.
2. Supabase SQL 에디터에서 `SELECT indexname FROM pg_indexes WHERE tablename = 'dma_orders';` 를 실행해 `idx_dma_orders_user_order_no_kst_day` 가 목록에 있는지 확인한다.
3. 실패했다면 오류 원문(선행 중복 건수 또는 IMMUTABLE 오류)을 그대로 붙여 알려 준다.
  </how-to-verify>
  <resume-signal>"approved" 또는 관측된 오류 원문</resume-signal>
  <verify>
    <automated>grep -c "CREATE UNIQUE INDEX idx_dma_orders_user_order_no_kst_day" supabase/migrations/20260909120000_dma_orders_user_order_no_unique.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE POLICY" supabase/migrations/20260909120000_dma_orders_user_order_no_unique.sql` == 0
    - `grep -c "GRANT\|REVOKE" supabase/migrations/20260909120000_dma_orders_user_order_no_unique.sql` == 0
    - `grep -c "RAISE EXCEPTION" supabase/migrations/20260909120000_dma_orders_user_order_no_unique.sql` >= 1 — 선행 중복 점검이 실재
    - `grep -c "DROP INDEX" supabase/migrations/20260909120000_dma_orders_user_order_no_unique.sql` == 0
    - `supabase db push` 출력에 `20260909120000_dma_orders_user_order_no_unique` 가 applied 로 나타난다 (실행 로그를 SUMMARY 에 인용)
    - `pg_indexes` 조회 결과에 `idx_dma_orders_user_order_no_kst_day` 존재 (조회 출력을 SUMMARY 에 인용)
  </acceptance_criteria>
  <done>인덱스가 프로덕션 DB 에 실재한다. push 출력과 `pg_indexes` 조회 결과 두 벌이 SUMMARY 에 인용돼 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 A ↔ 사용자 B 의 `dma_orders` 행 | relay 는 서비스롤이라 RLS 가 없다 — 경계는 **쿼리 필터가 전부**다 |
| DMA 게이트웨이 → relay 통보 | `order_no` 는 게이트웨이가 준 값이고 **일별 재사용**된다 (신뢰 불가한 유일성) |
| 로컬 개발 머신 → Supabase 프로덕션 | `supabase db push` 가 스키마를 바꾼다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-14 | Elevation of Privilege | `relay/src/store/orders.ts` `supabaseOrderSink` / `supabaseOrderLookupSink` | mitigate | `order_no` 축 조회·갱신에 `.eq("user_id", …)` + KST 당일 `created_at` 범위를 **필수**로 만든다. `userId` 가 없는 `order_no` 갱신은 `selectorOf` 가 `null` 을 돌려 드롭 + error 로그. 가짜 SupabaseClient 테스트가 「다른 사용자 행에 쓰기 불가」를 단언 |
| T-16-15 | Repudiation | `relay/src/ws/order-handler.ts` `recordUnmatched` | mitigate | 어제 행 오매치로 오늘 자동주문 행이 만들어지지 않는 경로를 당일 범위 필터로 제거. 조회 실패 catch 도 `userId` 를 실어 열화 경로가 전역 쓰기가 되지 않게 한다 |
| T-16-16 | Tampering | `dma_orders` 중복 행 (WR-01) | mitigate | `orderNo` 단위 in-flight Promise 재사용 + DB 부분 UNIQUE 인덱스 2차 방어선 |
| T-16-17 | Denial of Service | `supabase/migrations/20260909120000_*.sql` | mitigate | 선행 중복 점검 `DO` 블록이 인덱스 생성 전에 `RAISE EXCEPTION` — 절반만 적용된 마이그레이션을 만들지 않는다. RLS·GRANT 블록 미변경(acceptance grep 0건) |
| T-16-13 | 운영 사고 | 실서버·실계좌 접속 | accept | 이 plan 은 mock·가짜 SupabaseClient 로만 검증한다(D-27 승계). 테스트에 실서버 IP·실계좌 리터럴 0건을 유지 |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/relay test` exit 0
- `pnpm --filter @gh-radar/relay run typecheck` · `typecheck:tests` exit 0
- `pnpm typecheck` (13 워크스페이스) exit 0
- `grep -rn "10\.41\.1\.120" relay/tests/ relay/src/ supabase/migrations/` 0건 (D-27 승계)
- `supabase db push` applied 확인 + `pg_indexes` 조회로 인덱스 실재 확인
</verification>

<success_criteria>
- `order_no` 조회·갱신 두 경로가 `user_id` + KST 당일로 좁혀졌다
- `userId` 없는 `order_no` 갱신은 드롭 + error 로그다
- 같은 주문번호의 동시 통보가 행을 1건만 만든다
- 부분 UNIQUE 인덱스가 프로덕션 DB 에 적용됐다
- 경계 테스트가 sink 를 스텁으로 바꾸지 않고 적용 필터를 직접 단언한다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-18-SUMMARY.md` when done
</output>
</content>
</invoke>
