---
phase: 16-trading-limit-chaser-vi-my-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - relay/src/generated/StockDMA.fbs
  - relay/src/generated/stock-dma/set-limit-chaser.ts
  - relay/src/generated/stock-dma/unfilled-state.ts
  - webapp/src/components/ui/checkbox.tsx
  - supabase/migrations/20260908120000_dma_orders_origin.sql
autonomous: false
requirements: [TRADE-01, TRADE-02, TRADE-03, NAV-01, MYPAGE-01]

must_haves:
  truths:
    - "REQUIREMENTS.md 에 TRADE-01/TRADE-02/TRADE-03/NAV-01/MYPAGE-01 5건이 등록되고 Traceability·Coverage 가 40→45 로 정합한다 (CONTEXT 「Requirements: TBD — plan 단계에서 신규 등록」)"
    - "relay 생성 코드가 gh-trade 정본 .fbs 와 동기화되어 `SetLimitChaser.cancelQtyTrackBaseline()` 접근자가 존재한다 (D-06 「전체 필드를 현재 표시값으로 전송」의 S→C 표시값 4번째 필드)"
    - "webapp 에 shadcn checkbox 컴포넌트가 존재한다 (UI-SPEC A4a/A7a/A9 체크박스 6개 · B5 VI 확인 체크)"
    - "dma_orders 에 origin 컬럼이 있고 프로덕션 DB 에 적용되어 있다 (D-03 「자동주문 행의 출처 구분 컬럼은 재량」)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 16 요구사항 5건 + Traceability 5행 + Coverage 45"
      contains: "TRADE-01"
    - path: "relay/src/generated/stock-dma/set-limit-chaser.ts"
      provides: "SetLimitChaser 45 슬롯 빌더·접근자"
      contains: "cancelQtyTrackBaseline"
    - path: "webapp/src/components/ui/checkbox.tsx"
      provides: "Radix Checkbox 래핑"
    - path: "supabase/migrations/20260908120000_dma_orders_origin.sql"
      provides: "dma_orders.origin 컬럼"
      contains: "limit_chaser"
  key_links:
    - from: "relay/src/generated/stock-dma/set-limit-chaser.ts"
      to: "/Users/alex/repos/gh-trade/server/src/protocol/StockDMA.fbs"
      via: "sync-relay-schema.sh (flatc 25.12.19)"
      pattern: "startObject\\(45\\)"
---

<objective>
Phase 16 실행의 **선행 조건 4건**을 한 번에 세운다.

1. `REQUIREMENTS.md` 에 **TRADE-01 · TRADE-02 · TRADE-03 · NAV-01 · MYPAGE-01** 5건을 Phase 11~15 선례 그대로 등록한다(이후 모든 plan 의 `requirements:` 가 이 ID 를 참조한다).
2. relay 생성 코드를 gh-trade 정본 `.fbs` 와 **재동기화**한다 — 현재 `set-limit-chaser.ts` 에 `cancelQtyTrackBaseline()`(field 44), `unfilled-state.ts` 에 `orderTime()`(field 9)이 **없다**(RESEARCH §Relay 통합 지점 실측).
3. shadcn `checkbox` 를 설치한다(소스 파일 1개, npm 의존성 0건 — UI-SPEC Registry Safety).
4. `dma_orders.origin` 마이그레이션을 작성하고 **[BLOCKING] `supabase db push`** 로 적용한다(D-03).

Purpose: 재동기화 없이는 상따 취소 잔량추적 기준선을 읽을 수 없고, 요구사항 등록 없이는 이후 plan 의 추적성이 끊긴다.
Output: REQUIREMENTS.md 갱신 · 생성 코드 2파일 + .fbs 사본 갱신 · `webapp/src/components/ui/checkbox.tsx` · origin 마이그레이션(적용 완료).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-CONTEXT.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS.md 에 Phase 16 요구사항 5건 등록</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` 전문 — 특히 `### DMA Relay` 섹션(91~93행 RELAY-01/02/03 불릿 형식), `## Traceability` 표(162~164행), `## Coverage` 괄호 이력(167행), 파일 말미 `*Last updated: …*`(173행)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md` §Phase Requirements (78~107행) — 5건 초안 문구 + 「선례 — Phase 11~15 의 요구사항 등록 방식」 4단계
  </read_first>
  <action>
`## v1 Requirements` 아래 `### DMA Relay` 섹션 **다음에** 새 소제목 `### Trading` 을 추가하고, RELAY-01 불릿과 **같은 형식**(`- [ ] **ID**: 설명 — Phase 16`)으로 5건을 적는다. 설명 문구는 RESEARCH §Phase Requirements 표의 초안을 기반으로 하되 아래 확정 사실을 반영한다:

- **TRADE-01**: 상따 전략 페이지 `/trading/limit-chaser/new`(빈 폼) · `/trading/limit-chaser/{ISIN}:{accountNo}:{exchange}`(편집). `SetLimitChaser` **활성 37필드**(클라 입력 30 + 클라 고정 3 `sweep_recalc_enabled=true`/`sweep_min_count=0`/`sweep_min_rate=0` + S→C 표시 전용 4 `sell_order_qty`/`sell_qty_track_baseline`/`sell_entry_latched`/`cancel_qty_track_baseline`). 매수·매도·한방 스위치 즉시 전송(crud "C") · 세 게이트 전부 OFF = 삭제(crud "D") · 값 변경은 「수정」 버튼 · 호가 10단 + 데스크톱 최근 체결 10건 · 미체결/잔고 · 전략 로그.
- **TRADE-02**: VI 종합주문 페이지 `/trading/vi` — 세션당 1건(`SetVITrigger`: account_no · order_amount_krw(원, UI 만원×10,000) · check_rate(정수 %) · price_type "U" 고정 · run), 시작/중지 확인 다이얼로그, VI 주문내역(`VIOrderList` 72/73, state 6종 + `Accepted ∧ filledQty>0` 파생 부분체결, `confirm_locked`, deadline110/119) + `ConfirmVIOrderReq(33)` 확인 체크.
- **TRADE-03**: relay 전략 중계 + 주문 wss 이관 — 인바운드 `lc.set`/`vi.set`/`vi.confirm`/`strategies.disable`/`order.new`/`order.cancel`, DMA Ready 시 24/21/34 프리페치 + 세션 전략 캐시, auth 직후 전략 스냅샷 팬아웃, 56/60/61/64/65/72/73 파싱·팬아웃, `DirectOrderReq(2)` 5초 상관 이관 + `dma_orders` insert/update 를 relay 가 전담(origin manual/limit_chaser/vi), server `POST /api/orders` 제거(GET 유지).
- **NAV-01**: 사이드 메뉴 2단 그룹 트리 — 홈 / 종목검색(상승률 상위 `/scanner` · 테마 · 관심종목) / 트레이딩(상따 + 3단 등록 전략 목록 · VI) / My page / AI 애널리스트. 기존 URL 유지, 비로그인·`unauthorized`·미연결 시 트레이딩·My page 숨김, 모바일 Sheet drawer 동일 트리.
- **MYPAGE-01**: My page `/me` — 전략 현황(상따 목록 + VI 상태 + 전체 비활성화 `DisableStrategiesReq(14)` key="") → 계좌별 미체결·잔고 세로 반복(`account-panel` 재사용, 모바일 2줄 카드 행).

이어서 세 곳을 같은 편집에서 갱신한다:
① `## Traceability` 표 말미에 `| TRADE-01 | Phase 16 | Pending |` 5행(TRADE-01/02/03 · NAV-01 · MYPAGE-01) 추가.
② `## Coverage` 의 `v1 requirements: 40 total (…)` 를 `45 total (…)` 로 바꾸고 괄호 이력 끝에 `TRADE-01·TRADE-02·TRADE-03·NAV-01·MYPAGE-01 added 2026-09-08 with Phase 16(트레이딩 메뉴 — 상따·VI·My page) → 40→45` 를 append. `Mapped to phases: 45`, `Unmapped: 0 ✓` 로 갱신.
③ 파일 말미 `*Last updated: …*` 줄을 `*Last updated: 2026-09-08 — Phase 16 (trading-limit-chaser-vi-my-page) plan-phase: TRADE-01/02/03·NAV-01·MYPAGE-01 v1 정의 + Traceability(Pending) 5행 + Coverage 40→45.*` 로 교체.
④ `## Out of Scope` 표의 「주문/매매 기능(공개 사용자 대상)」 행 주석에 한 문장을 덧붙인다: Phase 16 의 전략 자동매매(상따·VI)는 **사용자 본인이 값을 정하고 본인이 스위치를 켜는** 같은 allowlist 범위이며, 범위 밖인 「AI 자동매매 추천」과 다르다.
  </action>
  <verify>
    <automated>grep -c "TRADE-01\|TRADE-02\|TRADE-03\|NAV-01\|MYPAGE-01" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[ \] \*\*\(TRADE-0[123]\|NAV-01\|MYPAGE-01\)\*\*" .planning/REQUIREMENTS.md` == 5
    - `grep -c "| Phase 16 | Pending |" .planning/REQUIREMENTS.md` == 5
    - `grep -c "v1 requirements: 45 total" .planning/REQUIREMENTS.md` == 1
    - `grep -c "Mapped to phases: 45" .planning/REQUIREMENTS.md` == 1
    - `grep -c "40→45" .planning/REQUIREMENTS.md` == 1
    - `.planning/REQUIREMENTS.md` 에 `### Trading` 소제목이 존재하고 `### DMA Relay` 뒤에 온다
  </acceptance_criteria>
  <done>요구사항 5건 등록 + Traceability 5행 + Coverage 45 + Last updated 교체 완료.</done>
</task>

<task type="auto">
  <name>Task 2: relay 생성 코드 재동기화 + shadcn checkbox 설치</name>
  <files>relay/src/generated/StockDMA.fbs, relay/src/generated/stock-dma/set-limit-chaser.ts, relay/src/generated/stock-dma/unfilled-state.ts, webapp/src/components/ui/checkbox.tsx</files>
  <read_first>
    - `/Users/alex/repos/gh-trade/server/scripts/sync-relay-schema.sh` — `FLATC_REQUIRED` 버전 고정·산출 경로·`--check` 모드
    - `relay/src/generated/stock-dma/set-limit-chaser.ts` — 현재 `startObject(44)` 이고 `cancelQtyTrackBaseline` 접근자가 없음을 눈으로 확인
    - `relay/src/generated/stock-dma/unfilled-state.ts` — 현재 `startObject(9)` 이고 `orderTime` 접근자가 없음
    - `webapp/components.json` — `registries: {}` (3rd-party 없음), `iconLibrary: lucide`
    - `webapp/src/components/ui/switch.tsx` — 같은 Radix 래핑 규약(참고용, 수정 대상 아님)
  </read_first>
  <action>
① `cd /Users/alex/repos/gh-trade/server && ./scripts/sync-relay-schema.sh` 를 **`--check` 없이** 실행한다. 스크립트가 gh-radar 의 `relay/src/generated/**` 와 `relay/src/generated/StockDMA.fbs` 사본을 덮어쓴다. 실행 후 `./scripts/sync-relay-schema.sh --check` 를 다시 돌려 「신규/변경 예정: 없음」인지 확인한다. **gh-trade 저장소는 무변경**(정본 .fbs 는 이미 최신)이므로 gh-trade 쪽에 커밋하지 않는다. flatc 버전이 25.12.19 가 아니면 스크립트가 중단되므로 그 경우 중단 사유를 보고하고 태스크를 실패시킨다(대체 경로를 지어내지 않는다).
② 재동기화 후 relay 를 다시 빌드해 생성 코드가 컴파일되는지 확인한다: `pnpm --filter @gh-radar/shared build && pnpm typecheck`.
③ `cd webapp && npx shadcn@latest add checkbox` 를 실행해 `webapp/src/components/ui/checkbox.tsx` 1개를 만든다. **`webapp/package.json` 이 변경되면 안 된다**(이미 설치된 `radix-ui@^1.4.3` 하위 export 를 쓴다). 만약 CLI 가 `@radix-ui/react-checkbox` 를 새 의존성으로 추가하려 하면 설치를 되돌리고, 저장소의 `switch.tsx` 가 쓰는 것과 **같은 import 경로**(`radix-ui` 배럴)로 소스를 맞춘 뒤 `package.json` diff 를 0 으로 되돌린다.
  </action>
  <verify>
    <automated>grep -c "cancelQtyTrackBaseline" relay/src/generated/stock-dma/set-limit-chaser.ts && test -f webapp/src/components/ui/checkbox.tsx && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "startObject(45)" relay/src/generated/stock-dma/set-limit-chaser.ts` == 1
    - `grep -c "cancelQtyTrackBaseline" relay/src/generated/stock-dma/set-limit-chaser.ts` >= 2 (접근자 + addCancelQtyTrackBaseline)
    - `grep -c "orderTime" relay/src/generated/stock-dma/unfilled-state.ts` >= 2
    - `/Users/alex/repos/gh-trade/server/scripts/sync-relay-schema.sh --check` 출력에 `신규/변경 예정  : 0 개` 또는 `없음`
    - `test -f webapp/src/components/ui/checkbox.tsx` 성공
    - `git diff --stat webapp/package.json` 이 빈 출력 (신규 npm 의존성 0건)
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>생성 코드 2파일 + .fbs 사본 갱신, checkbox.tsx 존재, typecheck green, package.json 무변경.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] dma_orders.origin 마이그레이션 작성 + supabase db push</name>
  <files>supabase/migrations/20260908120000_dma_orders_origin.sql</files>
  <read_first>
    - `supabase/migrations/20260905120200_dma_orders.sql` 전문 — 머리말 주석 4블록 형식(`Phase NN Plan NN — REQ-ID: 무엇 (D-xx)` / 결정 근거 / 함정 / 하지 않는 것), `BEGIN;`/`COMMIT;`, 말미 RLS·REVOKE·GRANT 3줄
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md` §Supabase (800~836행) — 권고 마이그레이션 + 「정책을 추가하지 말 것」
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md` §supabase/migrations (773~793행)
  </read_first>
  <action>
`supabase/migrations/20260908120000_dma_orders_origin.sql` 을 새로 만든다. 내용은 **정확히 다음 요소만** 갖는다:

- 머리말 주석 4블록: 제목 `Phase 16 Plan 01 — TRADE-03: dma_orders 주문 출처 컬럼 (D-03)` / **결정 근거**(D-03 relay 가 insert·update 전담 · 상따·VI 자동주문은 relay 가 요청을 만들지 않으므로 51 통보 수신 시 새 행을 insert 한다 · 출처 원천은 `OrderResp.origin` 의 `"Manual"`/`"LimitChaser"`/`"VITrigger"`) / **함정**(구 서버는 origin 을 빈 값으로 보낼 수 있으므로 기본값 `'manual'` · 51 통보가 `order_no` 셀렉터로 0행 update 를 해도 PostgREST 는 에러가 아니다 → 조용한 기록 결손) / **하지 않는 것**(RLS 정책을 추가하지 않는다 — 이 테이블은 정책 0개 default-deny 가 의도다 · REVOKE/GRANT 를 다시 실행하지 않는다 — 테이블 단위 권한은 20260905120200 에서 이미 확정 · 데이터 마이그레이션을 하지 않는다 — DEFAULT 로 기존 행이 채워진다)
- `BEGIN;`
- `ALTER TABLE public.dma_orders ADD COLUMN origin text NOT NULL DEFAULT 'manual' CHECK (origin IN ('manual','limit_chaser','vi'));`
- `COMMENT ON COLUMN public.dma_orders.origin IS ...` 한 줄(값 3종의 의미)
- `COMMIT;`

그다음 **[BLOCKING] 게이트**: `SUPABASE_ACCESS_TOKEN` 을 환경변수로 둔 상태에서 `supabase db push` 를 실행해 프로덕션에 적용한다(Phase 15 15-09 선례). 비대화식으로 진행되지 않으면 진행을 멈추고 사용자에게 실행 방법을 물어본 뒤 진행한다 — **적용 없이 다음 plan 으로 넘어가지 않는다.** 적용 후 컬럼 존재를 실제 DB 에서 확인한다(`supabase db push` 출력의 applied 목록 또는 Supabase SQL 에디터 1회 조회).
  </action>
  <verify>
    <automated>grep -c "origin IN ('manual','limit_chaser','vi')" supabase/migrations/20260908120000_dma_orders_origin.sql</automated>
  </verify>
  <acceptance_criteria>
    - 파일에 `ADD COLUMN origin text NOT NULL DEFAULT 'manual'` 문자열이 존재
    - `grep -c "CREATE POLICY" supabase/migrations/20260908120000_dma_orders_origin.sql` == 0
    - `grep -c "REVOKE\|GRANT" supabase/migrations/20260908120000_dma_orders_origin.sql` == 0
    - `supabase db push` 출력에 `20260908120000_dma_orders_origin` 이 applied 로 나타난다 (실행 로그를 SUMMARY 에 인용)
    - 마이그레이션 파일 첫 줄이 `-- Phase 16 Plan 01 — TRADE-03` 으로 시작
  </acceptance_criteria>
  <done>마이그레이션 파일 존재 + 프로덕션 DB 적용 확인.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → Supabase (anon/authenticated) | `dma_orders` 는 이 경계에서 **읽히지 않아야** 한다(default-deny) |
| 로컬 개발 머신 → Supabase 프로덕션 | `supabase db push` 가 스키마를 바꾼다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Information Disclosure | `supabase/migrations/20260908120000_dma_orders_origin.sql` | mitigate | 컬럼만 추가하고 **RLS 정책·GRANT·REVOKE 를 건드리지 않는다**. 기존 default-deny(정책 0개) + service_role 단독 권한을 유지 — acceptance 에 `CREATE POLICY`/`GRANT` 0건 grep 게이트 |
| T-16-05 | Tampering | `relay/src/generated/stock-dma/**` | mitigate | 생성 코드는 **손으로 편집하지 않는다**. gh-trade 정본 `.fbs` + flatc 25.12.19 산출물만 커밋하고 `--check` 로 동기화 0 차이를 증명 |
| T-16-SC | Tampering | `npx shadcn add checkbox` | mitigate | shadcn 공식(1st-party) registry, `components.json.registries = {}` 실측. 산출물은 소스 파일 1개이며 **`package.json` diff 0** 을 acceptance 로 강제 — 신규 npm 설치가 0건이므로 slopcheck 대상 없음 |
</threat_model>

<verification>
- `pnpm typecheck` exit 0
- `/Users/alex/repos/gh-trade/server/scripts/sync-relay-schema.sh --check` 차이 0
- `git status` 에 gh-trade 저장소 변경 없음(다른 저장소이므로 별도 확인)
</verification>

<success_criteria>
- REQUIREMENTS.md 에 5개 ID 등록 + Coverage 45 정합
- `SetLimitChaser.cancelQtyTrackBaseline` 접근자 존재, `startObject(45)`
- `webapp/src/components/ui/checkbox.tsx` 존재, `package.json` 무변경
- `dma_orders.origin` 컬럼이 프로덕션에 적용됨
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-01-SUMMARY.md` when done
</output>
