---
phase: 16-trading-limit-chaser-vi-my-page
plan: 46
type: execute
wave: 28
depends_on: ["16-36", "16-37", "16-38", "16-39", "16-40", "16-41", "16-42", "16-43", "16-44", "16-45"]
gap_closure: true
files_modified:
  - .planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md
  - .planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION-R2.md
  - .planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [TRADE-01, TRADE-02, TRADE-03, NAV-01, MYPAGE-01]

must_haves:
  truths:
    - "3라운드가 닫겠다고 선언한 17건(Critical 3 · Warning 7 · Info 5 · 갭 4 · 갭 5)의 처리 결과가 **한 표**에 정리돼 있고, 각 행이 코드·명령 근거를 가리킨다"
    - "전 워크스페이스 게이트가 green 이다 — `pnpm -r typecheck` · **`@gh-radar/relay` `typecheck:tests`** · `pnpm -r test` · `pnpm build` · Playwright"
    - "`DMA_HOST` 를 **주입하지 않은** relay 배포가 실 게이트웨이를 보존한다는 것이 프로덕션에서 실측됐다 — 갭 5 수정의 유일한 진짜 증명이다"
    - "smoke `INV-9` 의 상태가 정직하게 기록된다 — 「돌렸는데 SKIP」과 「토큰이 없어 못 돌렸다」를 섞지 않는다"
    - "TRADE-03 재판정의 근거가 **과장 없이** 정리되고, 판단은 사용자가 내린다"
  artifacts:
    - path: ".planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md"
      provides: "§Gap Closure 3라운드 표 (17행) + §Deployment Verification (16-46)"
      contains: "3라운드"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TRADE-03 재판정 결과와 그 근거"
      contains: "TRADE-03"
    - path: ".planning/STATE.md"
      provides: "3라운드 종결 상태 · 다음 행동"
      contains: "3라운드"
  key_links:
    - from: "scripts/deploy-relay.sh (16-45 수정본)"
      to: "프로덕션 컨테이너 DMA_HOST"
      via: "주입 없는 배포가 실 게이트웨이를 보존한다"
      pattern: "DMA_HOST"
    - from: "16-VALIDATION.md §Gap Closure 3라운드"
      to: "16-36 ~ 16-45 SUMMARY"
      via: "각 행이 담당 plan 과 근거 명령을 가리킨다"
      pattern: "16-4"
---

<objective>
3라운드 갭 클로징을 **종결**한다. 코드는 16-36~16-45 가 닫았고, 이 plan 은 ① 전량 게이트 ② 배포와 실측 ③ 문서 정합 ④ **TRADE-03 재판정** 을 맡는다.

**이 라운드가 특별한 이유 — 결선이 일어났다.** 2026-09-09 프로덕션 relay 가 실 게이트웨이(`10.41.1.120:9100`)에 붙었고 `/healthz` 가 `everReadyCount: 1` 을 실측했다. **프로덕션에서 DMA 세션이 Ready 에 도달한 첫 기록**이다. TRADE-03 이 Pending 이던 근거(「Ready 에 도달한 세션이 한 건도 없다」)는 해소됐다 — 그러나 **자동 승격은 하지 않는다.** WinForms ↔ 웹 세션 공유 실측(human-only)이 여전히 미실행이고, 이 라운드의 Critical 3건이 있었다.

**이 plan 이 반복하지 말아야 할 것 (앞선 2라운드가 실제로 겪은 것):**
- `grep "10.41.1.120"` **0건**을 승인 기준으로 쓰지 않는다. 실측 33건이고 **6회 연속** 같은 불일치를 관측했다. 정본 계약은 리터럴 0건이 아니라 **「접속 경로 0건」**이다(산문·주석·경고문은 D-27 안전장치라 지우면 안 된다).
- `pnpm --filter gh-radar-webapp` 은 **없는 필터**이고 `No projects matched the filters` + **exit 0** 이다. 정본은 **`@gh-radar/webapp`**. 2라운드에서 정정했음에도 두 plan 이 또 만났다.
- relay `tests/` 는 루트 typecheck **밖**이다. `pnpm --filter @gh-radar/relay run typecheck:tests` 를 **따로** 돌린다(2라운드에서 형 경계 오류 14건을 `pnpm -r test` 가 통째로 놓쳤다 — vitest 는 형을 안 본다).
- 이 저장소에 **prettier 설정이 없다.** `npx prettier --write` 를 돌리면 무관한 줄을 통째로 재배열한다(16-30 사고, 459 insertions, 되돌림). **돌리지 않는다.**

Purpose: 닫았다는 주장을 실측으로 바꾸고, 남은 것을 정직하게 남긴다.
Output: 전량 게이트 실측 · 배포 3종 판정 · 문서 6종 · TRADE-03 재판정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION-R2.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW-R2.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md
</context>

<interfaces>
**기준선 (3라운드 시작 시점, 오케스트레이터 실측):**
- `pnpm -r typecheck` exit 0 · `pnpm --filter @gh-radar/relay run typecheck:tests` exit 0
- `pnpm -r test` exit 0 — **2,012 passed · 1 skipped · 6 todo** (relay 373 · server 252 · webapp 672 · shared 99 · workers 전량)
- Playwright **126 passed / 9 skipped / 0 failed**
- 프로덕션 relay `relay:59465e1` @ VM `radar-gw` · `DMA_HOST=10.41.1.120` · 게이트웨이 TCP `REACHABLE`
- 프로덕션 `/healthz` **200** `{"vpn":true,"dma":true,"version":"59465e1","sessionCount":1,"everReadyCount":1,"stalledCount":0}`
- `smoke-relay.sh` PASS 12 · FAIL 0 · SKIP 1(INV-9) / `smoke-server.sh` PASS 15 · FAIL 0 · SKIP 0
- webapp: Vercel **git 통합 자동 배포**(`ignoreCommand` 문제는 `b691b15` 로 해결됨)
- server 리비전 `gh-radar-server-00043-s4f`

**3라운드 범위 17건:**
- Critical 3 — R2-CR-01(16-36) · R2-CR-02(16-37) · R2-CR-03(16-38)
- Warning 7 — R2-WR-01(16-39) · WR-02(16-42) · WR-03(16-43) · WR-04(16-40) · WR-05(16-44) · WR-06(16-43) · WR-07(16-40)
- Info 5 — R2-IN-01(16-42) · IN-02(16-41) · IN-03(16-43) · IN-04(16-39) · IN-05(16-45)
- 새 갭 2 — 갭 4 사이드바 ISIN 표시(16-41 relay + 16-42 webapp) · 갭 5 `deploy-relay.sh` DMA_HOST 보존(16-45)

**배포 스크립트 (순서 고정: relay → server → webapp):**
- `GCP_PROJECT_ID=gh-radar SUPABASE_URL=https://ivdbzxgaapbmrxreyuht.supabase.co bash scripts/deploy-relay.sh`
- `bash scripts/deploy-server.sh` — **`git diff --stat <이전배포SHA>..HEAD -- server/ packages/shared/` 가 비어 있으면 건너뛴다**(16-35 선례)
- webapp — Vercel git 통합 자동 배포. 수동이 필요하면 `reference_vercel_frontend_deploy` 메모리 참조

**GCP 인증:** `~/.config/gcloud/gh-radar-deployer.json` + `GOOGLE_APPLICATION_CREDENTIALS` · `CLOUDSDK_CORE_PROJECT=gh-radar` (기존 credentials 를 사용자에게 다시 묻지 않는다)

**smoke INV-9 재실행 명령** (`deferred-items.md` §16-35):
`GCP_PROJECT_ID=gh-radar SUPABASE_URL=... SMOKE_AUTH_TOKEN='<access_token>' bash scripts/smoke-relay.sh`
토큰은 로그인 브라우저 DevTools localStorage `sb-ivdbzxgaapbmrxreyuht-auth-token` → `access_token`(약 1시간 만료). **저장소 어디에도 값이 없는 것이 정상이다**(T-16-74).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 전량 게이트 + 회귀 잠금 감사</name>
  <files>(소스 변경 없음 — 측정만)</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-36-SUMMARY.md` ~ `16-45-SUMMARY.md` **10건 전부**. 각 plan 이 무엇을 닫았고, **회귀 잠금 실증**(무력화 시 실패 관측)을 실제로 했는지, 못 잠근 것을 정직하게 적었는지 확인한다
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW-R2.md` — 17건 원문. SUMMARY 주장과 대조할 기준
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md` §16-35 「이번 라운드가 드러낸 규율」
  </read_first>
  <action>
① 전량 게이트를 **순서대로** 돌리고 각 결과를 숫자로 기록한다:
   - `pnpm -r typecheck`
   - `pnpm --filter @gh-radar/relay run typecheck:tests` ← **루트 typecheck 밖이다. 반드시 따로**
   - `pnpm -r test` — 총 pass/skip/todo 와 워크스페이스별 건수. 기준선(2,012 pass · 190 파일)과 **증감**을 적는다
   - `pnpm build`
   - Playwright 전량 (`pnpm --filter @gh-radar/webapp test:e2e`) — pass/skip/fail
   실패가 나오면 **거기서 멈추고** 원인을 밝힌다. 「대부분 통과」로 넘어가지 않는다.

② **회귀 잠금 감사.** 10개 SUMMARY 에서 「무력화 시 실패를 관측하고 복원했다」가 기록된 항목을 표로 모은다. 기록이 없는 plan 이 있으면 그 사실을 표에 적는다 — 사후에 대신 실증하지 말고(범위 밖) **누락을 드러낸다.**

③ **임시 마커 잔존 확인.** 회귀 실증 과정에서 남았을 수 있는 흔적을 훑는다: `grep -rn "MUTATION\|TEMP_DISABLE\|XXX_REVERT" relay/src webapp/src packages/shared/src scripts` → 0건.

④ **부채 마커 확인.** `grep -rnE "\b(TODO|FIXME|XXX|HACK|TBD)\b" relay/src webapp/src packages/shared/src scripts` 결과를 세고, 기준선(0건)과 비교한다.

⑤ **SUMMARY 주장 vs 코드 대조 (표본).** 17건 중 **Critical 3 + 갭 2** 5건에 대해 SUMMARY 를 믿지 않고 코드를 직접 열어 확인한다: `#isTeardown` 반환식 · `stats()` 의 `NO_RETRY_STATES` 참조 · `orders.ts` 의 `safePgError` 사용 지점 수 · `#enrichLimitChaser` 호출 위치 · `deploy-relay.sh` 의 `CURRENT_DMA_HOST` 3단 우선순위. 각 확인의 인용을 SUMMARY 에 남긴다.
  </action>
  <verify>
    <automated>pnpm -r test</automated>
  </verify>
  <acceptance_criteria>
    - 5개 게이트 결과가 전부 숫자로 기록됐다 (typecheck · **typecheck:tests** · test · build · e2e)
    - `pnpm -r test` 총 건수가 기준선 2,012 대비 **증가**했다 (전/후 숫자 인용)
    - Playwright 0 failed
    - 회귀 잠금 감사 표에 10개 plan 이 전부 등장한다 (실증함 / 기록 없음)
    - 임시 마커 grep 0건 · 부채 마커 개수 인용
    - Critical 3 + 갭 2 의 코드 직접 확인 인용 5건
    - ⚠️ 필터는 `@gh-radar/webapp` · `@gh-radar/relay` 다 (`gh-radar-webapp` 은 없는 필터이고 exit 0 이다)
  </acceptance_criteria>
  <done>초록이 무엇을 뜻하는지 숫자로 말할 수 있다.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: 배포 승인 + DMA_HOST 처리 방식 선택</name>
  <decision>3종 배포를 지금 진행할지, 그리고 relay 배포에 `DMA_HOST` 를 주입할지 말지</decision>
  <context>
Task 1 의 전량 게이트 결과를 **그대로 보여준 뒤** 묻는다. 함께 알려야 할 사실:

- 프로덕션은 현재 실 게이트웨이(`10.41.1.120:9100`)에 **결선돼 있다**. `/healthz` 200 · `everReadyCount: 1`.
- 16-45 가 `deploy-relay.sh` 를 고쳤다 — **주입 없이 배포해도 실행 중인 컨테이너의 `DMA_HOST` 를 보존**하도록.
- 그래서 **주입하지 않고 배포하는 것이 갭 5 수정의 유일한 진짜 증명**이다. 동시에, 보존 로직이 틀렸다면 프로덕션이 다시 mock 으로 떨어진다.
- 안전망: 배포 **전에** 현재 값을 기록해 두고, 배포 후 실컨테이너 값이 다르면 **즉시** 그 값으로 재배포한다. 이 복구 절차는 Task 3 에 이미 들어 있다.
- 배포 순서는 relay → server → webapp 로 고정한다. server 는 `git diff` 가 비면 건너뛴다(16-35 선례).
  </context>
  <options>
    <option id="deploy-preserve">
      <name>배포 진행 — `DMA_HOST` 를 주입하지 않는다 (권장)</name>
      <pros>갭 5 수정을 프로덕션에서 실제로 증명한다. 다음 배포부터 같은 회귀가 재발하지 않음을 확인할 수 있다</pros>
      <cons>보존 로직에 결함이 있으면 프로덕션이 일시적으로 mock 으로 떨어진다 (복구 절차가 준비돼 있고, `/healthz` 로 즉시 감지된다)</cons>
    </option>
    <option id="deploy-inject">
      <name>배포 진행 — `DMA_HOST=10.41.1.120` 을 명시 주입한다</name>
      <pros>강등 위험이 0 이다</pros>
      <cons>갭 5 수정이 **검증되지 않은 채** 남는다. 주입이 보존을 이기는지만 확인되고, 정작 회귀를 만든 「주입 없는 배포」 경로는 미검증이다</cons>
    </option>
    <option id="no-deploy">
      <name>배포하지 않는다 — 코드와 문서만 종결</name>
      <pros>프로덕션을 건드리지 않는다</pros>
      <cons>Critical 3건 수정이 **실계좌가 붙은 프로덕션에 반영되지 않은 채** 남는다. 갭 5 도 미검증이다</cons>
    </option>
  </options>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-45-SUMMARY.md` — 보존 로직의 실제 구현과 스니펫 3케이스 검증 결과
    - `.planning/STATE.md` §「DMA_HOST 배포 회귀」 — 두 번의 강등이 어떻게 일어났는지
  </read_first>
  <acceptance_criteria>
    - Task 1 의 게이트 숫자를 **다이얼로그 본문에** 담아 사용자가 그것만 보고 판단할 수 있다 (VS Code 에서는 도구 사이 텍스트가 보이지 않는다)
    - 사용자의 선택과 그 시각을 SUMMARY 에 기록한다
    - 승인 없이 배포하지 않는다
  </acceptance_criteria>
  <files>(결정만 — 저장소 파일 변경 없음)</files>
  <action>Task 1 의 게이트 숫자(typecheck · typecheck:tests · test 총 건수 · build · Playwright)와 배포 전 프로덕션 `/healthz` 전문·현재 `DMA_HOST` 를 **다이얼로그 본문에 직접 담아** 위 3안을 제시한다. VS Code 사용자는 도구 호출 사이의 텍스트를 보지 못하므로 설명을 질문 안에 넣는다. 「제가/내가 실행」 같은 표현을 쓰지 않고 「Claude가 실행」 / 「직접 실행」으로 라벨을 적는다. 사용자의 답을 받기 전에는 어떤 배포 명령도 실행하지 않는다.</action>
  <verify>
    <automated>echo "checkpoint — 사용자 응답 대기"</automated>
  </verify>
  <done>사용자가 3안 중 하나를 골랐고, 그 선택과 시각이 기록됐다.</done>
  <resume-signal>Select: deploy-preserve, deploy-inject, or no-deploy</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 배포 3종 + 프로덕션 실측 + smoke</name>
  <files>(소스 변경 없음 — 배포·측정만)</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-45-SUMMARY.md` — 새 `deploy-relay.sh` 의 출력 형태(출처 표시·변경 전/후)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-35-SUMMARY.md` — 16-35 의 배포 절차와 「server 는 diff 가 비어 건너뜀」 판정 방식, webapp Vercel 정황 증명 방식
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md` §16-35 — INV-9 재실행 명령과 「돌렸는데 SKIP 이 아니다」 구분
    - `infra/relay/README.md` §실서버 라이브 상태 — 실계좌 경고
  </read_first>
  <action>
① **배포 전 상태를 기록한다.** 프로덕션 `/healthz` 전문 · 실컨테이너 `DMA_HOST` · 현재 이미지 태그. 이 세 값이 이후 모든 비교의 기준이다.

② **relay 배포.** Task 2 의 선택에 따른다. `deploy-relay.sh` 의 출력에서 **`DMA_HOST` 출처와 변경 전/후 줄을 그대로** SUMMARY 에 인용한다 — 그것이 갭 5 의 증거다.

③ **배포 후 즉시 검증.** 실컨테이너 `DMA_HOST` 를 되읽고 `/healthz` 를 찍는다.
   - **강등이 감지되면**(값이 `127.0.0.1` 로 바뀌었거나 `dma:false`) **즉시** ① 에서 기록한 값으로 재배포하고, **실패 사실과 원인을 SUMMARY 에 그대로 적는다.** 「보존이 동작했다」로 덮지 않는다 — 이 항목의 가치는 정직함에 있다
   - `/healthz` 는 `status`·`vpn`·`dma`·`version`·`sessionCount`·`everReadyCount`·`stalledCount` **전 필드**를 인용한다

④ **server.** `git diff --stat <이전 배포 SHA>..HEAD -- server/ packages/shared/` 를 먼저 본다. **`packages/shared/src/relay.ts` 가 16-41 에서 바뀌었으므로 이번에는 비어 있지 않을 가능성이 높다** — 비어 있지 않으면 배포하고 리비전을 인용한다. 비어 있으면 건너뛰고 그 판정 근거(diff 출력)를 인용한다.

⑤ **webapp.** Vercel git 통합 자동 배포 결과를 확인한다. 트레이딩 표면은 인증 게이트 뒤라 청크 대조가 불가능하다 — 16-35 가 쓴 **정황 증명 4종**(build 시간 · alias 결선 · 공개 HTML 마커 · 공개 청크 해시)을 그대로 적용하고, **무엇을 증명했고 무엇을 못 했는지** 구분해 적는다.

⑥ **smoke 2종.** `smoke-relay.sh` · `smoke-server.sh` 를 돌리고 PASS/FAIL/SKIP 을 인용한다.
   - **INV-9 를 정직하게 다룬다.** `SMOKE_AUTH_TOKEN` 이 있으면 실행하고 결과(`reachable` 기대)를 적는다. 없으면 **「토큰이 없어 프로브 본체가 한 줄도 실행되지 않았다」**로 적는다 — 「돌렸는데 SKIP」과 섞지 않는다. 16-21 재작성 이후 **한 번도 실행된 적이 없다**는 사실이 이 항목의 핵심이다
   - 토큰을 사용자에게 요청할지는 재량이되, **값을 저장소 어디에도 기록하지 않는다**(T-16-74)
   - `INV-5a` 판정이 시각 의존이 됐다는 기록(deferred §16-35)을 확인하고, 이번 실행에서 어떻게 나왔는지 적는다

⑦ **알림 상태 확인.** `gh-radar-relay-down` 이 발화 중인지 확인하고, `/healthz` 200 이면 정상 복귀했는지 본다. 알림 정책 변경은 **사용자 결정 사항**이므로 실행자가 단독으로 고르지 않는다(deferred §16-35 의 후보 4개 참조).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://dma.jx1.io/healthz</automated>
  </verify>
  <acceptance_criteria>
    - 배포 전/후 `DMA_HOST` 값과 `/healthz` 전문이 **각각** 인용됐다
    - `deploy-relay.sh` 의 출처·변경 전/후 출력이 그대로 인용됐다
    - 강등이 있었으면 그 사실과 복구가 **숨김 없이** 기록됐다
    - server 배포 여부 판정 근거(`git diff --stat` 출력)가 인용됐다
    - webapp 증명 4종 중 **무엇이 증명됐고 무엇이 안 됐는지** 구분돼 있다
    - smoke 2종 PASS/FAIL/SKIP 인용 + INV-9 의 상태가 「실행함/못 함」으로 명확히 구분돼 있다
    - `SMOKE_AUTH_TOKEN` 값이 SUMMARY·커밋 어디에도 없다
    - ⚠️ 실주문을 내는 검증을 **하지 않았다** (D-27 — 사용자 명시 지시 없이는 하지 않는다). 그 사실을 명시
  </acceptance_criteria>
  <done>프로덕션 상태가 숫자로 기록됐고, 갭 5 의 증명 여부가 정직하게 남았다.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: TRADE-03 재판정</name>
  <decision>TRADE-03 을 Complete 로 승격할지, Pending 을 유지할지</decision>
  <context>
**Pending 이던 근거 하나가 해소됐다.** 프로덕션 `/healthz` 가 `everReadyCount: 1` 을 보였다 — 「Ready 에 도달한 DMA 세션이 한 건도 없다」는 문장이 더 이상 참이 아니다. 이것이 이전 두 라운드의 재판정과 **다른 점**이다.

다이얼로그 본문에 **아래를 전부 담아** 사용자가 그것만 보고 판단할 수 있게 한다(VS Code 에서는 도구 사이 텍스트가 보이지 않는다):

**해소된 것**
- `everReadyCount ≥ 1` 실측 (Task 3 의 실제 값을 적는다)
- 3라운드 Critical 3건(R2-CR-01 무장 가드 우회 · R2-CR-02 영구 503 · R2-CR-03 계좌번호 로그 유출) 종결 — 「실서버 결선 전에 반드시 닫아야 한다」던 조건이 충족됐다
- 1·2라운드 갭 전량 + 이번 Warning 7 · Info 5 · 새 갭 2

**남은 것 (정직하게)**
- **WinForms ↔ 웹 「한 세션」 동기화 실측이 여전히 미실행이다.** 이것은 **phase goal 의 핵심 문장**이며 human-only 다(`16-VERIFICATION-R2.md` §human_verification #1). D-27 상 사용자 명시 지시가 있어야 한다
- smoke `INV-9` 의 이번 실행 결과 (Task 3 의 사실을 그대로)
- RELAY-02 도 같은 기준(취소 왕복·주문 목록 복원 미관측)으로 Pending 유지 중이다 — 두 요구사항의 판정 기준을 갈라 놓으면 다음 사람이 어느 쪽을 믿을지 모른다
- Task 3 에서 실주문 검증을 하지 않았다

**과장 금지.** `everReadyCount: 1` 은 「세션이 Ready 에 도달했다」는 뜻이지 「전략·체결·미체결이 WinForms 와 공유됐다」는 뜻이 아니다. 두 문장의 거리를 다이얼로그에 명시한다.
  </context>
  <options>
    <option id="complete">
      <name>Complete 로 승격한다</name>
      <pros>코드 층위와 프로덕션 Ready 도달이 모두 확인됐다. 세션 공유는 relay 구조상 같은 DMA 세션을 쓰므로 파생 결과라고 볼 수 있다</pros>
      <cons>phase goal 의 핵심 문장(WinForms 와 즉시 공유)이 **한 번도 관측되지 않았다.** RELAY-02 와 판정 기준이 갈린다</cons>
    </option>
    <option id="pending">
      <name>Pending 유지 — 잔여를 「WinForms 세션 공유 실측」 하나로 좁힌다</name>
      <pros>RELAY-02 와 같은 기준을 유지한다. 잔여가 1건으로 좁혀졌다는 진전이 문서에 남는다</pros>
      <cons>phase 가 계속 미완결로 남는다</cons>
    </option>
    <option id="verify-now">
      <name>지금 WinForms ↔ 웹 세션 공유를 실측한다 (사용자가 직접 수행)</name>
      <pros>판정 근거를 지금 만든다. 성공하면 그 자리에서 Complete 다</pros>
      <cons>실계좌·실서버 조작이 필요하다(D-27). 사용자의 시간과 명시 지시가 필요하다</cons>
    </option>
  </options>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION-R2.md` §human_verification · §Requirements Coverage
    - `.planning/REQUIREMENTS.md` :170-186 — TRADE-03 · RELAY-02 의 Traceability 문장과 판정 기준
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md` §16-35 「인간 검증 6건」 표
  </read_first>
  <acceptance_criteria>
    - 해소된 것 / 남은 것이 **다이얼로그 본문에** 전부 들어 있다
    - `everReadyCount` 의 **실제 측정값**이 인용돼 있다 (추정치 금지)
    - 「Ready 도달」과 「WinForms 공유」의 거리가 명시돼 있다
    - 사용자의 선택과 근거를 SUMMARY 에 기록한다
  </acceptance_criteria>
  <files>(결정만 — 문서 반영은 Task 5)</files>
  <action>Task 3 의 실측값(`everReadyCount` · `/healthz` 전문 · smoke INV-9 의 실행 여부)과 3라운드 종결 항목 수를 **다이얼로그 본문에 직접 담아** 위 3안을 제시한다. 「해소된 것」과 「남은 것」을 나란히 적고, 「Ready 도달」과 「WinForms 와 전략·체결·미체결이 즉시 공유된다」 사이의 거리를 명시한다. 근거 없이 승격을 권하지 않으며, 추정치를 실측처럼 적지 않는다. 사용자의 답을 받기 전에 REQUIREMENTS.md 를 고치지 않는다.</action>
  <verify>
    <automated>echo "checkpoint — 사용자 응답 대기"</automated>
  </verify>
  <done>사용자가 판정을 내렸고, 그 선택과 근거가 기록됐다.</done>
  <resume-signal>Select: complete, pending, or verify-now</resume-signal>
</task>

<task type="auto">
  <name>Task 5: 문서 6종 정합</name>
  <files>.planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md, .planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION-R2.md, .planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md` §Gap Closure · §Gap Closure 2라운드 · §Deployment Verification — **표의 형식과 열 구성을 그대로 본떠** 3라운드 절을 잇는다
    - `.planning/REQUIREMENTS.md` :90-105(체크박스) · :170-186(Traceability) — 두 곳이 **일치해야 한다**(2차 검증의 갭 3 이 정확히 이 불일치였다)
    - `.planning/ROADMAP.md` :33(상단 목록 한 줄) · :596-705(Phase 16 상세와 Wave 목록 끝) — **상단 목록과 상세 양쪽**을 고친다(상단 미갱신이 이 저장소의 반복 함정이다)
    - `.planning/STATE.md` :1-30(frontmatter·Current Position) · §Phase 16 절 · 파일 끝 「Last session / Stopped at / Next」
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md` §16-35 — 이어 쓸 형식
  </read_first>
  <action>
① **`16-VALIDATION.md`** — §Gap Closure **3라운드** (16-36 ~ 16-46) 절을 잇는다. **17행 표**: ID(R2-CR-01…IN-05 · 갭4 · 갭5) / 담당 plan / 처리 결과 / 근거(파일·줄 또는 명령). 그 아래 §Deployment Verification (16-46) 에 Task 3 의 실측을 전부 옮긴다. **이 파일이 처리 결과의 정본이다.**

② **`16-VERIFICATION-R2.md`** — 파일을 다시 쓰지 않는다. 끝에 **「## 부록 2 — 3라운드 갭 클로징 결과 (16-46)」** 를 잇고, 본문의 갭 1·2·3(= R2-CR-01·02·03)이 어떻게 닫혔는지와 부록의 갭 4·5 결과를 적는다. 기존 부록(orchestrator 작성)은 **보존**한다. frontmatter 의 `gaps`/`status` 는 **건드리지 않는다** — 그것은 검증자의 기록이지 실행자의 것이 아니다. 대신 부록에서 「어느 갭이 어느 plan 으로 닫혔는지」를 가리킨다.

③ **`.planning/REQUIREMENTS.md`** — Task 4 의 결정을 반영한다. **체크박스와 Traceability 표를 함께** 고친다(둘 중 하나만 고치면 2차 검증의 갭 3 이 재발한다). Traceability 문장에는 근거를 적는다 — `everReadyCount` 실측값 · 닫힌 항목 수 · **남은 것**. 파일 끝 `*Last updated:*` 줄에 3라운드 한 줄을 잇는다.

④ **`.planning/ROADMAP.md`** — 상단 목록 :33 한 줄과 Phase 16 상세 :596-601 의 「Plans:」 줄을 **plan 46개 · wave 28개**로 갱신하고, Wave 목록 끝에 **Wave 23~28** 을 잇는다(16-36 ~ 16-46 각 한 줄, 형식은 기존 Wave 항목과 동일). 상단과 상세가 어긋나지 않게 **둘 다** 확인한다.

⑤ **`.planning/STATE.md`** — frontmatter(`stopped_at`·`last_activity`·`progress`)와 Current Position 을 갱신하고, §「Phase 16 Gap Closure 3라운드」 절을 새로 쓴다. 담을 것: 17건 처리 · 전량 게이트 숫자 · 배포 실측 · **갭 5 증명 여부** · INV-9 상태 · TRADE-03 판정. 파일 끝 「Last session / Stopped at / Next」 를 다시 쓴다.
   - §「DMA_HOST 배포 회귀」 절에 **후속 한 줄**을 잇는다 — 스크립트가 고쳐졌고 이번 배포에서 보존이 확인됐는지(또는 안 됐는지).

⑥ **`deferred-items.md`** — §16-46 절을 잇는다. 담을 것:
   - **이 라운드는 17건을 전부 범위에 넣었다** — 3라운드에서 새로 미룬 항목이 있으면 그것, 없으면 「없음」을 명시(빈 칸을 남기지 않는다)
   - 승계되는 기존 항목: 1라운드 Info `IN-02`~`IN-07` 6건 · `surface-placeholder.tsx` 죽은 코드 · `use-relay-socket.ts` `clockStamp()` ko-KR 버그 · 미체결 표 6열 전환 · `/healthz` 알림 정책 사용자 결정 · `INV-5a` 시각 의존 · 실서버·실계좌 검증(D-27)
   - 인간 검증 6건 표를 이번 결과로 갱신(#1·#2 는 Task 3·4 의 사실로)
   - **이번 라운드가 드러낸 규율**을 잇는다 — 다음 라운드가 반복하지 말 것

⑦ **커밋.** `commit_docs` 가 켜져 있다. 커밋 메시지는 **한글**로, 사용자 확인 후 진행하고 push 까지 한다. `Co-Authored-By` 를 넣지 않는다.
  </action>
  <verify>
    <automated>grep -c "3라운드" .planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `16-VALIDATION.md` §Gap Closure 3라운드 표가 **17행**이다 (행 수 인용)
    - `16-VERIFICATION-R2.md` 의 기존 본문·부록이 보존됐다 (`git diff --stat` 이 추가만 보인다)
    - `.planning/REQUIREMENTS.md` 의 **체크박스와 Traceability 가 5개 ID 전부에서 일치**한다 (두 위치를 나란히 인용해 대조)
    - `.planning/ROADMAP.md` 의 **상단 목록(:33)과 상세(:596~) 양쪽**이 갱신됐다 (두 줄 다 인용)
    - ROADMAP 에 Wave 23~28 항목이 있고 16-36~16-46 이 전부 등장한다
    - `.planning/STATE.md` §DMA_HOST 배포 회귀 절에 후속 한 줄이 이어졌다
    - `deferred-items.md` §16-46 에 「새로 미룬 것」이 **명시**돼 있다(「없음」이라도 적는다)
    - 커밋 메시지가 한글이고 `Co-Authored-By` 가 없다
    - `pnpm -r test` 가 여전히 exit 0 (문서 변경이 코드를 건드리지 않았다는 확인)
  </acceptance_criteria>
  <done>문서 6종이 서로 모순되지 않고, 남은 것이 빈 칸 없이 적혀 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SUMMARY 주장 ↔ 실제 코드·프로덕션 상태 | 주장을 근거로 삼으면 세 번 연속 같은 오진이 승계된다(이번 라운드가 실제로 겪은 일이다) |
| 배포 스크립트 ↔ 실계좌가 붙은 프로덕션 | 배포가 구성을 되돌릴 수 있다. 배포 전 상태 기록이 유일한 복구 근거다 |
| 「실행했는데 SKIP」 ↔ 「못 실행했다」 | 두 문장을 섞으면 검증되지 않은 것이 검증된 것으로 승계된다 (INV-9) |
| 실계좌 주문 경로 | D-27 — 사용자 명시 지시 없이는 건드리지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-96 | Repudiation (오진 승계) | SUMMARY → 문서 → 다음 라운드 | mitigate | Critical 3 + 갭 2 를 코드에서 직접 확인하고 인용한다 (Task 1 ⑤) |
| T-16-94 | Denial of Service (배포 강등) | `deploy-relay.sh` 실행 | mitigate | 배포 전 값 기록 + 배포 후 즉시 검증 + 강등 시 즉시 복구·정직 기록 |
| T-16-74 | Information Disclosure | `SMOKE_AUTH_TOKEN` | mitigate | 값을 SUMMARY·커밋 어디에도 남기지 않는다 |
| T-16-13 | 운영 사고 (실주문) | smoke·수동 검증 | accept | 실주문을 내지 않는다. INV-9 프로브는 화이트리스트 밖 계좌 + 미해석 ISIN 이라 송신 전에 끝난다 (T-16-28) |
| T-16-97 | Repudiation (과장 판정) | TRADE-03 재판정 | mitigate | 「Ready 도달」과 「WinForms 공유」의 거리를 다이얼로그에 명시하고, 판단을 사용자에게 넘긴다 |
</threat_model>

<verification>
- `pnpm -r typecheck` · `pnpm --filter @gh-radar/relay run typecheck:tests` · `pnpm -r test` · `pnpm build` · `pnpm --filter @gh-radar/webapp test:e2e` 전부 exit 0
- 프로덕션 `/healthz` 전 필드 인용
- smoke 2종 결과 인용 (INV-9 는 「실행함/못 함」 구분)
- ⚠️ **`grep "10.41.1.120"` 0건을 승인 기준으로 쓰지 않는다** — 만족 불가능하며 6회 연속 같은 불일치를 낳았다. 정본 계약은 「접속 경로 0건」(`webapp/src`·`webapp/e2e` 에 접속 경로 없음)이다
- ⚠️ 필터 정본은 `@gh-radar/webapp` · `@gh-radar/relay` 다
- 포매터를 돌리지 않는다 (prettier 설정이 없다)
</verification>

<success_criteria>
- 17건 처리 결과가 한 표에 있고 각 행이 근거를 가리킨다
- 전량 게이트가 green 이고 숫자로 기록됐다
- 갭 5 의 증명 여부가 프로덕션 실측으로 남았다 (성공이든 실패든)
- INV-9 의 상태가 정직하다
- TRADE-03 판정과 그 근거가 문서 3곳(REQUIREMENTS·STATE·VALIDATION)에서 일치한다
- 남은 것이 빈 칸 없이 `deferred-items.md` 에 있다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-46-SUMMARY.md` when done
</output>
