---
phase: 16-trading-limit-chaser-vi-my-page
plan: 05
type: execute
wave: 4
depends_on: ["16-04"]
files_modified:
  - relay/src/dma/envelope.ts
  - relay/src/dma/__tests__/envelope.test.ts
autonomous: true
requirements: [TRADE-03]

must_haves:
  truths:
    - "relay 가 60/61/64/65/72/73/56 을 JSON 계약으로 파싱한다 — 브라우저에 FlatBuffers 를 얹지 않는다 (D-34 승계)"
    - "bigint 필드(orderAmountKrw · deadline110/119Ms)가 Number 로 변환되어 팬아웃 루프가 예외로 죽지 않는다 (Pitfall 3)"
    - "60 에코의 crud 'D' 가 삭제 신호로 보존된다 — 두 스위치만 보고 삭제를 판정하지 않는다 (Pitfall 7 / D-08)"
  artifacts:
    - path: "relay/src/dma/envelope.ts"
      provides: "전략 응답 파서 7종"
      contains: "parseLimitChaserEcho"
    - path: "relay/src/dma/__tests__/envelope.test.ts"
      provides: "37필드 왕복 + 경계 테스트"
  key_links:
    - from: "relay/src/dma/envelope.ts"
      to: "packages/shared/src/relay.ts"
      via: "RelayLimitChaser / RelayViTrigger / RelayViOrderItem 반환"
      pattern: "RelayLimitChaser"
---

<objective>
relay codec 층의 **수신 절반**을 만든다: 16-04 에서 화이트리스트를 통과시킨 7종을 전부 JSON 계약으로 파싱한다.

- `parseLimitChaserEcho`(60) · `parseLimitChaserList`(64) · `parseViTrigger`(61) · `parseViOrderList`(72/73) · `parseViOrderNotice`(56) · `parseDisableStrategiesResp`(65).
- `SetLimitChaser` **37 활성 필드 전부**를 읽는다(S→C 전용 4 포함 — 이것들은 **표시**를 위해 읽는다).
- bigint → Number 변환은 `toNum` 한 곳을 통과시킨다.

Purpose: 파서가 없으면 Hub(16-06)가 case 만 있고 내용이 없다. 그리고 파싱 실패를 조용히 삼키면 「서버가 거부했는데 화면은 반영됨」이 된다.
Output: 파서 6함수 + 왕복/경계 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전략 응답 파서 6함수</name>
  <files>relay/src/dma/envelope.ts</files>
  <read_first>
    - `relay/src/dma/envelope.ts` — `parseAccountState`(:930 부근, 벡터·null 가드·이름 채움의 본보기), `parseOrderResp`(:822), `toNum`/`drop`/`dropField`/`takeCount`(:154~200), `tryParseEnvelope`(수신 화이트리스트)
    - `relay/src/generated/stock-dma/{set-limit-chaser,set-vi-trigger,limit-chaser-list,vi-order-item,vi-order-list,vi-order-notice,disable-strategies-resp}.ts`
    - `packages/shared/src/relay.ts` — `RelayLimitChaser`/`RelayViTrigger`/`RelayViOrderItem` (16-03 산출물)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md` §Protocol A~D(269~434행) · Pitfalls 3·4·6·7·10·11(867~898행) · §Code Examples 60 에코 파싱(966~980행)
  </read_first>
  <action>
아래 6함수를 추가한다. 전부 `(env: Envelope) => T | null` 형태이며 **실패 시 `drop`/`dropField` 로 사유·카운터를 남기고 null** 을 돌려준다(상위는 재로그하지 않는다 — S-2).

- `parseLimitChaserEcho(env): RelayLimitChaser | null` — 슬롯 `set_limit_chaser` 를 읽어 **37 활성 필드 전부**를 채운다(S→C 전용 4 포함). `key` 는 `` `${isin}:${accountNo}:${exchange}` `` 로 조립하되 **조립 지점은 이 파일 한 곳뿐**이어야 한다(`strategyKey(isin, accountNo, exchange)` 헬퍼를 export 해 webapp 이 아니라 relay 안에서만 쓰고, 브라우저는 계약의 `key` 필드를 그대로 신뢰한다). `crud` 는 첫 글자만 취해 `"D"`/`"C"` 로 정규화한다 — **`crud === "D"` 는 삭제 신호이며 반드시 계약에 실어 보낸다**(Pitfall 7). `exchange` 가 비어 있으면 `"KRX"` 로 정규화한다.
- `parseLimitChaserList(env): RelayLimitChaser[] | null` — 슬롯 `limit_chaser_list`. 0건도 **빈 배열**(null 아님)을 돌려준다. 항목 수는 `takeCount(n, 200, "limitChaserList")` 로 상한을 둔다(파손 프레임 방어).
- `parseViTrigger(env): RelayViTrigger | null | "absent"` — 슬롯 `set_vi_trigger` 가 **없으면 미등록**이다. 「파싱 실패(null)」와 「미등록」을 반드시 구분한다 — 반환 타입으로 구분하거나 `{ ok: true, cfg: RelayViTrigger | null }` 형태로 만든다(재량, 단 두 경우가 같은 값으로 뭉개지면 안 된다). `orderAmountKrw` 는 `toNum(bigint, "orderAmountKrw")`.
- `parseViOrderList(env, isSnapshot: boolean): { snap: boolean; items: RelayViOrderItem[] } | null` — 72/73 공용(msg_type 이 유일한 구분자). `deadline110Ms`/`deadline119Ms` 는 `toNum`. `state` 는 6종 화이트리스트 밖이면 그 항목만 드롭하고 사유를 남긴다. `orderNo` 는 `""` 를 그대로 보존한다(접수 전 = 확인 체크 불가의 원천).
- `parseViOrderNotice(env): RelayViNotice | null` — 슬롯 `vi_order_notice`.
- `parseDisableStrategiesResp(env): { count: number; viDisabled: boolean } | null` — 슬롯 `disable_strategies_resp`.

**단일 문자 → 의미 변환은 이 파일의 기존 헬퍼 옆에 모은다**(Phase 15 D-21 승계): `fromWireMarket`("Q"→KOSDAQ 그 외 KOSPI) · `fromWireCrud` · `fromWireWatchSide`. 웹앱에 `"K"`/`"Q"` 리터럴을 흩뿌리지 않는다.

또한 `parseOrderResp` 가 `OrderResp.origin` 을 읽는지 확인하고, 읽지 않는다면 **읽도록 확장**한다(RESEARCH Assumption A4). 값은 `"Manual"`/`"LimitChaser"`/`"VITrigger"`/빈 문자열이며, `dma_orders.origin` 의 `manual`/`limit_chaser`/`vi` 로 매핑하는 순수 함수 `toOrderOrigin(raw): "manual" | "limit_chaser" | "vi"` 를 같은 파일에 둔다(**빈 값·미지의 값은 `manual`**, 그 사실을 주석에 남긴다).
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test -- envelope</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function parse\(LimitChaserEcho\|LimitChaserList\|ViTrigger\|ViOrderList\|ViOrderNotice\|DisableStrategiesResp\)" relay/src/dma/envelope.ts` == 6
    - `grep -c "toNum(" relay/src/dma/envelope.ts` >= 3 (orderAmountKrw · deadline110Ms · deadline119Ms)
    - `grep -c "toOrderOrigin" relay/src/dma/envelope.ts` >= 2
    - `parseLimitChaserEcho` 반환 객체에 `cancelQtyTrackBaseline` 키가 포함된다
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>응답 파서 6종 + origin 매핑이 존재하고 bigint 가 계약 밖으로 새지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: envelope 왕복·경계 테스트</name>
  <files>relay/src/dma/__tests__/envelope.test.ts</files>
  <read_first>
    - `relay/src/dma/__tests__/envelope.test.ts` — 기존 왕복 테스트 형태(빌드 → 파스 → 필드 단언)
    - `relay/tests/helpers/frames.ts` (16-02 산출물) — `buildSetLimitChaserRespFrame` 등
    - `relay/src/ws/protocol.ts` `encode` — bigint 를 만나면 TypeError 를 던지는 지점
  </read_first>
  <action>
아래 케이스를 추가한다. 각 케이스는 **한 문장으로 무엇을 잠그는지** 를 `it()` 제목에 적는다.

1. **37필드 왕복**: `buildSetLimitChaserReq(입력 33필드)` → `Envelope` 파스 → `parseLimitChaserEcho` 결과가 입력 33필드와 일치하고, S→C 전용 4필드는 **0/false**(서버가 안 채운 상태)로 나온다.
2. **고정 3값**: 어떤 입력을 주어도 조립된 버퍼의 `sweepRecalcEnabled === true`, `sweepMinCount === 0`, `sweepMinRate === 0`.
3. **12자 절단**: 13자 `accountNo` 를 넣으면 조립·파스 왕복 후 12자다.
4. **crud "D" 보존**: `crud:"D"` 로 조립한 60 에코가 파서에서 `crud === "D"` 로 나온다.
5. **취소 게이트가 켜져 있으면 삭제가 아니다**(Pitfall 7 문서화 테스트): `buyEnabled=false, sellEnabled=false, cancelQtyEnabled=true` 인 에코는 `crud === "C"` 로 들어오며, 파서가 이를 삭제로 바꾸지 않는다.
6. **빈 61 = 미등록**: `buildSetVITriggerRespFrame(null)` 을 `parseViTrigger` 에 넣으면 「미등록」이며 「파싱 실패」와 다른 값이다.
7. **bigint 미유출**: `parseViTrigger`/`parseViOrderList` 결과를 `encode()` 에 넣어도 TypeError 가 나지 않는다(계약에 bigint 가 없음을 런타임으로 증명).
8. **72/73 구분**: 같은 `vi_order_list` 슬롯이라도 `snap` 값이 msg_type 에 따라 true/false 로 나온다. `items` 0건도 정상.
9. **알 수 없는 state 드롭**: `state:"Zzz"` 항목은 결과 배열에서 빠지고 나머지 항목은 살아남는다.
10. **deprecated 슬롯 미사용 정적 단언**: `envelope.ts` 소스를 읽어 `createSetLimitChaser(`/`createSetVITrigger(` 가 0건임을 단언(정규식 소스 검사 — Pitfall 1 회귀 방지).
11. **origin 매핑**: `toOrderOrigin("LimitChaser") === "limit_chaser"`, `toOrderOrigin("") === "manual"`, `toOrderOrigin("Zzz") === "manual"`.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test -- envelope</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @gh-radar/relay test -- envelope` exit 0 이고 신규 케이스 11개가 실행된다
    - 케이스 10 이 `envelope.ts` 소스를 읽어 `create(SetLimitChaser|SetVITrigger)\(` 0건을 단언한다
    - 케이스 7 이 실제로 `encode()` 를 호출한다 (타입 단언이 아니라 런타임 호출)
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>왕복·경계 11 케이스 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트웨이 → relay 파서 | JS 런타임에 FlatBuffers Verifier 가 없다 — 잘린 버퍼가 잘못된 필드로 읽힐 수 있다 |
| relay 파서 → 브라우저 계약 | 여기서 bigint 가 새면 팬아웃 루프가 예외로 죽는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Tampering | `parse*` 6함수 | mitigate | 모든 접근자에 null 가드 + `takeCount(n, 200)` 벡터 상한. 실패는 `drop`/`dropField` 로 사유·카운터를 남기고 null 반환 — 상위가 조용히 통과시키지 못한다 |
| T-16-07 | Repudiation | 파싱 실패 로그 | mitigate | 한 곳에서만 로그(S-2). 카운터가 없으면 「가끔 화면이 안 바뀐다」의 원인을 영영 못 찾는다 |
| T-16-05 | Tampering | `toOrderOrigin` | mitigate | 미지의 값을 지어내지 않고 `manual` 로 고정 + 주석 명시. 자동주문을 수동으로 오분류해도 **주문이 잘못 나가지는 않는다**(표시·감사 영역) |
| T-16-06 | Denial of Service | `parseViOrderList` 벡터 | mitigate | 항목 상한 + 항목 단위 드롭(전체 프레임을 버리지 않는다) |
</threat_model>

<verification>
- `pnpm typecheck` exit 0
- `pnpm --filter @gh-radar/relay test` exit 0
</verification>

<success_criteria>
- 응답 파서 6종이 37필드·bigint·crud "D"·빈 61 을 정확히 다룬다
- envelope 테스트 11 케이스 green
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-05-SUMMARY.md` when done
</output>
