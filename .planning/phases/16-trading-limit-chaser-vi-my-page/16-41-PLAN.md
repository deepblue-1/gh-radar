---
phase: 16-trading-limit-chaser-vi-my-page
plan: 41
type: execute
wave: 26
depends_on: []
gap_closure: true
files_modified:
  - packages/shared/src/relay.ts
  - relay/src/hub/subscription-hub.ts
  - relay/tests/strategy-hub.test.ts
autonomous: true
requirements: [TRADE-01, TRADE-03, NAV-01]

must_haves:
  truths:
    - "상따 전략 에코(60)와 전량 스냅샷(64)이 브라우저로 나갈 때 **종목명·단축코드가 붙어 있다** — 보유도 미체결도 없는 종목의 전략이 사이드바에 `KR7005930003` 원문으로 뜨지 않는다 (갭 4)"
    - "이름의 원천은 여전히 **relay wss 하나**다 — 웹앱에 새 조회 경로(REST·Supabase 직접)가 생기지 않는다 (T-16-02)"
    - "`SymbolMap` 이 못 푸는 ISIN 은 **필드를 비워 둔다** — 이름 자리에 ISIN 을 넣지 않는다. 「모르면 ISIN 을 그대로」 폴백은 소비자(UI)의 몫으로 남는다"
    - "브라우저는 `name`·`code` 를 **보낼 수 없다** — `RelayLimitChaserInput` 이 두 필드를 뺀다 (`market` 과 같은 규율, D-28/WR-03)"
  artifacts:
    - path: "packages/shared/src/relay.ts"
      provides: "RelayLimitChaser 의 선택 필드 name·code + Input 에서의 제외"
      contains: "RelayLimitChaserInput"
    - path: "relay/src/hub/subscription-hub.ts"
      provides: "60/64 수신 시 SymbolMap 으로 이름을 채우는 보강기"
      contains: "#symbols"
    - path: "relay/tests/strategy-hub.test.ts"
      provides: "에코·스냅샷 이름 보강 + 미해석 ISIN 은 필드 부재 케이스"
      contains: "FakeSymbols"
  key_links:
    - from: "relay/src/hub/subscription-hub.ts #onLimitChaserEcho / #onLimitChaserList"
      to: "relay/src/store/symbols.ts SymbolMap.lookup"
      via: "잔고·미체결·VI 와 같은 보강 경로"
      pattern: "lookup"
    - from: "relay/src/hub/subscription-hub.ts getLimitChasers"
      to: "relay/src/ws/fanout.ts lc.snap"
      via: "캐시에 보강된 값이 들어가므로 스냅샷도 이름을 갖는다"
      pattern: "getLimitChasers"
---

<objective>
**갭 4(사용자 직접 보고)** 의 절반을 닫는다 — 사이드바의 상따 전략이 종목명 대신 ISIN 으로 표시된다.

지금 상태:
- `webapp/src/lib/isin-labels.ts` 는 `accountStates.hold`·`unf`·`viOrders` **에서만** 이름을 모은다. 보유도 미체결도 없는 종목에 전략을 걸면 이름을 알 길이 없어 `KR7005930003` 원문이 사이드바(`app-sidebar.tsx:310` `labels.get(item.isin)?.name ?? null`)에 그대로 뜬다.
- 그 파일의 주석은 이를 「모르면 ISIN 을 그대로 보여준다가 정답」이라 규정하고 근거로 **T-16-02(목록의 원천을 둘로 만들지 않는다)** 를 든다. 그 근거 자체는 옳다.

**그러나 원천을 늘리지 않고 고칠 수 있다.** relay 는 이미 `SymbolMap`(`relay/src/store/symbols.ts` — Supabase `stocks` 기반, 부팅 1회 + 매일 08:30 KST 재적재)을 메모리에 들고 있고 `lookup(isin)` 이 `{code, name, market}` 을 돌려준다. **잔고·미체결·VI 주문의 이름도 Hub 가 같은 맵으로 붙인다**(`#enrichNames`·`#enrichViOrder`·`#enrichViNotice`, `envelope.ts` 가 「`name` 은 여기서 채우지 않는다 — Hub 가 SymbolMap 으로 붙인다」고 명시). **상따 에코(60)·스냅샷(64)에도 같은 방식으로 붙이면** 웹앱에 새 조회 경로가 생기지 않고 원천도 relay wss 하나로 유지된다 — T-16-02 를 지키면서 증상이 사라진다.

이 plan 은 **계약 + relay** 까지다. 웹앱 소비는 16-42 가 받는다.

함께 처리: **R2-IN-02** — `SubscriptionHub.detach()`·`releaseAll()` 은 `relay/src` · `relay/tests` 어디에서도 호출되지 않는다. 둘 다 세션 상태를 파괴하는 공개 메서드라 남겨 두면 「호출하면 된다」는 오해를 만들고, 특히 `detach` 는 `session.on("frame"/"ready")` 리스너를 떼지 않아 호출되는 순간 리스너 누적을 만든다.

Purpose: 이름을 아는 유일한 프로세스(relay)가 이름을 붙이게 한다. 화면이 두 진실을 갖지 않도록.
Output: `RelayLimitChaser.name`·`code` 선택 필드 · Hub 보강기 · 테스트 · 죽은 공개 메서드 정리.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION-R2.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW-R2.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-CONTEXT.md
</context>

<interfaces>
`packages/shared/src/relay.ts`:
- `export type RelayLimitChaser = { isin; accountNo; market; crud; ...29필드...; key }` (:129-221). `key` 는 「와이어에 실려 오는 필드가 아니라 **relay 가 파싱하며 채우는 파생값**」 — **새 선택 필드도 같은 부류다**
- `export type RelayLimitChaserInput = Omit<RelayLimitChaser, "sellOrderQty"|"sellQtyTrackBaseline"|"sellEntryLatched"|"cancelQtyTrackBaseline"|"key"|"market">` (:235-243). `market` 을 뺀 이유(D-28/WR-03 — 「브라우저의 추측이 실계좌 발주 설정이 되지 않게 한다」)가 그 위 주석에 있다
- 참고 선례: `RelayViOrderItem.name`, `RelayAccountState.hold[].name`/`.code` 가 이미 같은 형태다 — **그 선언을 그대로 본뜬다**

`relay/src/store/symbols.ts`:
- `export type SymbolInfo = { code: string; name: string; market: OrderMarket | null }`
- `export interface SymbolLookup { lookup(isin: string): SymbolInfo | undefined }` — 「모르면 `undefined`. 호출부는 ISIN 원문으로 폴백한다」

`relay/src/hub/subscription-hub.ts`:
- `readonly #symbols: SymbolLookup | undefined` (:273), `constructor(opts?: { symbols?: SymbolLookup })` (:275)
- `#enrichViOrder(item)` (:~835-840) · `#enrichViNotice(notice)` (:~842-846) · `#enrichNames(state)` (:~858-880) — **본뜰 선례 3개.** 규율: 「맵에 없으면 **필드를 비워 둔다**. ISIN 을 이름 자리에 넣으면 UI 가 "이름이 없다"와 "이름이 ISIN 이다"를 구분하지 못한다」
- `#onLimitChaserEcho(userId, item)` (:759-768) — `crud === "D"` 면 캐시 삭제, 아니면 `set`. 그 뒤 `#fanout(userId, { t: "lc", item })`
- `#onLimitChaserList(userId, items)` (:777-786) — 사용자 엔트리 전량 교체 후 `#fanout(userId, { t: "lc.snap", items })`
- `getLimitChasers(userId)` (:491-500) — 캐시 복사본. `fanout.ts:551` 의 `lc.snap` 과 `fanout.ts:822` 의 `#teardownMarket` 이 읽는다
- `detach()` (:316) · `releaseAll()` (:383) — R2-IN-02 대상

`relay/tests/strategy-hub.test.ts`:
- `class FakeSymbols implements SymbolLookup` (:97) — 기성 하네스
- ③(60 에코 upsert) · ④(crud "D" 삭제) · ⑤(64 전량 교체) · ⑫(56/72/73 이름 보강, :354 — `symbols: new FakeSymbols({...})` 를 주입하는 **정확한 선례**)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 계약에 선택 필드를 더한다 — 브라우저는 보낼 수 없게</name>
  <files>packages/shared/src/relay.ts</files>
  <read_first>
    - `packages/shared/src/relay.ts` L120-260 — `RelayLimitChaser` 전문(특히 `key` 의 「파생값」 주석), `RelayLimitChaserInput` 의 Omit 목록과 그 위의 `market` 제외 근거 주석 전문
    - `packages/shared/src/relay.ts` 의 `RelayViOrderItem` · `RelayAccountState` 안의 `name`/`code` 선언 — **그대로 본뜰 형태**(선택인지 필수인지, 주석 표현)
    - `webapp/src/lib/limit-chaser.ts` L40-70 — `LimitChaserFormValues = Omit<RelayLimitChaserInput, ...>`. Input 에서 두 필드를 빼면 여기는 **변경 0줄**이어야 한다. 그 사실을 확인한다
    - `relay/src/ws/protocol.ts` L100-150 — `RelayLcSetSchema`. `z.object` 가 미지 키를 떨어뜨리므로 브라우저가 실어 보내도 통과하지 못한다는 기존 근거
  </read_first>
  <action>
① `RelayLimitChaser` 에 선택 필드 두 개(`name`·`code`)를 더한다. 형태는 `RelayViOrderItem`/`RelayAccountState` 의 기존 선언을 본뜬다 — 프로젝트 안에서 같은 개념이 두 가지 모양을 갖지 않게 한다.

② 각 필드 주석에 반드시 담을 것:
   - **게이트웨이가 주는 값이 아니다.** relay 가 `SymbolMap` 으로 붙이는 파생값이다 — `key` 와 같은 부류
   - **모를 수 있다.** `SymbolMap` 이 못 푸는 ISIN(신규 상장 직후·마스터 미로딩)은 필드가 **없다**. 소비자는 ISIN 원문으로 폴백한다
   - 원천이 하나임을 명시: 잔고·미체결·VI 주문의 이름과 **같은 맵**이다 (T-16-02)

③ `RelayLimitChaserInput` 의 `Omit` 목록에 두 필드를 **추가한다.** 그 이유를 `market` 제외 근거 바로 아래에 잇는다 — 「이름의 소유자도 relay 다. 브라우저가 실어 보내면 화면이 자기가 만든 이름을 자기가 믿는 순환이 생긴다」. 이것을 빼먹으면 브라우저가 임의의 종목명을 서버 캐시에 밀어 넣을 수 있는 표면이 열린다.

④ `packages/shared` 를 소비하는 곳의 형 영향이 있는지 확인한다: `pnpm --filter @gh-radar/shared run typecheck` → `pnpm -r typecheck`. 선택 필드이므로 기존 리터럴은 깨지지 않아야 한다. 깨지는 곳이 있으면 그것이 무엇을 뜻하는지 판정하고 SUMMARY 에 적는다.
  </action>
  <verify>
    <automated>pnpm -r typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `RelayLimitChaser` 에 `name`·`code` 가 선택 필드로 있다 (소스 인용)
    - `RelayLimitChaserInput` 의 `Omit` 목록에 두 필드가 **둘 다** 있다 (소스 인용) — 하나만 빼면 반쪽이다
    - `git diff -- webapp/src/lib/limit-chaser.ts` 가 비어 있다 (`LimitChaserFormValues` 무영향 확인)
    - `pnpm -r typecheck` exit 0
    - `pnpm --filter @gh-radar/relay run typecheck:tests` exit 0
  </acceptance_criteria>
  <done>계약이 「relay 가 붙이고 브라우저는 못 보내는 값」을 표현한다.</done>
</task>

<task type="auto">
  <name>Task 2: Hub 가 상따 에코·스냅샷에 이름을 붙인다 (갭 4) + 죽은 공개 메서드 정리 (R2-IN-02)</name>
  <files>relay/src/hub/subscription-hub.ts</files>
  <read_first>
    - `relay/src/hub/subscription-hub.ts` — `#symbols` 선언·생성자, `#enrichViOrder`·`#enrichViNotice`·`#enrichNames` 전문(**규율 주석 포함**), `#onLimitChaserEcho`·`#onLimitChaserList`·`getLimitChasers`·`lcKey`·`userPrefix`, `detach()`·`releaseAll()` 전문. **한 번에 읽는다**
    - `relay/src/store/symbols.ts` — `SymbolInfo`·`SymbolLookup`·`lookup` 의 「모르면 `undefined`」 계약
    - `relay/src/ws/fanout.ts` :540-560(`lc.snap` 송신부) 과 :800-830(`#teardownMarket` 이 `getLimitChasers` 를 읽는 부분) — 캐시에 보강값을 넣어도 **`market` 판정이 영향받지 않음**을 확인
    - `grep -rn "detach(\|releaseAll(" relay/src relay/tests` — 호출자 0건 재확인(리뷰 주장을 그대로 믿지 않는다)
  </read_first>
  <action>
① `#enrichLimitChaser(item: RelayLimitChaser): RelayLimitChaser` 를 만든다. `#enrichViOrder` 와 **같은 모양**이다: `this.#symbols?.lookup(item.isin)` 이 `undefined` 면 `item` 을 그대로 돌려주고, 있으면 `{ ...item, name: info.name, code: info.code }`.
   - ★ **못 풀면 필드를 비워 둔다.** ISIN 을 이름 자리에 넣지 않는다 — 그 규율은 `#enrichNames` 주석에 이미 적혀 있으니 인용한다.

② `#onLimitChaserEcho` 와 `#onLimitChaserList` 가 **캐시에 넣기 전에** 보강한다. 캐시에 보강된 값이 들어가야 `getLimitChasers` → `lc.snap`(재접속 복원 경로)도 이름을 갖는다. 팬아웃 프레임도 같은 보강값이어야 「지금 화면」과 「새 탭의 화면」이 갈리지 않는다.
   - `crud === "D"` 로 캐시에서 지우는 갈래도 **프레임은 그대로 내린다**(기존 규율). 그 프레임에도 이름을 붙일지 결정하고 근거를 주석에 적는다 — 삭제 통지에도 이름이 있으면 「무엇이 사라졌는지」를 말할 수 있다.

③ `#onLimitChaserList` 의 전량 교체 규율(사용자 엔트리 전부 삭제 후 재삽입)은 **바꾸지 않는다.**

④ 미해석 건수 로그를 남길지 판단한다. `#enrichNames` 는 「전량 미스는 맵이 비었다는 뜻」이라 건수를 남긴다(ISIN 은 식별자가 아니므로 건수만). 상따는 건수가 적으므로 과할 수 있다 — 어느 쪽이든 **결정과 근거**를 주석·SUMMARY 에 남긴다.

⑤ **R2-IN-02.** `detach()`·`releaseAll()` 을 처리한다. 기본 방침은 **삭제**다(호출자 0건, 리스너를 떼지 않아 호출되는 순간 누수를 만든다). 삭제가 어떤 테스트·타입을 깨는지 먼저 확인하고, 남기기로 한다면 (a) `@internal` 명시 (b) `session.on("frame"/"ready")` 리스너 정리를 함께 넣기 **둘 다** 해야 한다. 어느 쪽을 골랐고 왜인지 SUMMARY 에 적는다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `#enrichLimitChaser`(또는 동등 이름)가 존재하고 `#onLimitChaserEcho`·`#onLimitChaserList` **양쪽**에서 캐시 삽입 **이전에** 호출된다 (소스 인용)
    - 보강기가 `undefined` 일 때 원본을 그대로 돌려준다 — ISIN 을 name 에 넣지 않는다 (소스 인용)
    - `grep -c "detach(" relay/src/hub/subscription-hub.ts` 와 `releaseAll` 의 전/후 값을 SUMMARY 에 인용하고, 선택한 처리(삭제 / 유지+정리)의 근거를 적는다
    - `pnpm --filter @gh-radar/relay run typecheck` exit 0 · `run typecheck:tests` exit 0
  </acceptance_criteria>
  <done>relay 가 이름을 아는 유일한 곳에서 이름을 붙인다. 죽은 파괴적 API 가 남아 오해를 만들지 않는다.</done>
</task>

<task type="auto">
  <name>Task 3: 보강과 폴백을 각각 잠근다</name>
  <files>relay/tests/strategy-hub.test.ts</files>
  <read_first>
    - `relay/tests/strategy-hub.test.ts` — `FakeSymbols`(:97) 전문, ③(:168)·④(:182)·⑤(:195) 상따 케이스, ⑫(:354) 이름 보강 케이스의 **주입 방식과 단언 형태**. **한 번에 읽는다**
    - `relay/tests/hub.test.ts` — 같은 Hub 를 다루는 다른 스위트가 상따 프레임을 단언하고 있는지 확인(있다면 그쪽도 통과해야 한다)
  </read_first>
  <action>
① 새 케이스 「60 에코와 64 스냅샷에 종목명·단축코드가 붙는다 (갭 4)」:
   - `symbols: new FakeSymbols({ [SAMPLE_ISIN]: { code: "005930", name: "삼성전자", market: "K" } })` 로 Hub 를 세운다(⑫ 와 같은 주입)
   - 60 에코 1건 → 팬아웃된 `{t:"lc", item}` 의 `item.name === "삼성전자"` · `item.code === "005930"`
   - 64 스냅샷 → `{t:"lc.snap", items}` 의 각 항목에 이름이 붙었다
   - `hub.getLimitChasers(userId)` 가 돌려주는 **캐시 복사본에도** 이름이 있다 — 재접속 복원(`lc.snap`) 경로의 잠금이다

② 새 케이스 「`SymbolMap` 이 모르는 ISIN 은 필드를 비워 둔다 — ISIN 을 이름 자리에 넣지 않는다」:
   - `FakeSymbols` 에 없는 ISIN 으로 60 에코를 보내고, `item.name` 이 `undefined` 임(그리고 **ISIN 문자열이 아님**)을 단언한다
   - 이것이 「모르면 ISIN 을 그대로」 폴백의 **서버측 계약**이다. 폴백 자체는 UI 의 몫이다

③ 새 케이스 「`symbols` 를 주입하지 않은 Hub 도 무해하다」: `#symbols === undefined` 인 기본 구성에서 60/64 가 예외 없이 흐르고 이름이 없을 뿐임을 단언한다. 기존 ③·④·⑤ 가 이 구성이라면 그들이 이 역할을 하고 있으니 통과 확인만 한다.

④ 기존 ③·④·⑤(에코 upsert · crud "D" 삭제 · 64 전량 교체)가 **전부 통과**해야 한다. 깨지면 보강이 캐시 규율을 건드린 것이다.

⑤ Task 2 에서 `detach`/`releaseAll` 을 삭제했다면 그것을 참조하던 테스트가 없음을 확인한다(있었다면 그것이 「호출자 0건」이 거짓이었다는 뜻이므로 판정을 다시 한다).
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay exec vitest run tests/strategy-hub.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f relay/tests/strategy-hub.test.ts` 가 참임을 먼저 확인한 뒤 위 명령을 돌린다
    - 「60/64 이름 보강」 케이스가 `getLimitChasers` 반환값에 대해서도 단언한다 (소스 인용)
    - 「미해석 ISIN 은 `undefined`」 케이스가 **ISIN 문자열이 아님**을 명시 단언한다 (`expect(item.name).toBeUndefined()` 형태 + ISIN 비교)
    - 기존 ③·④·⑤ 가 통과 (테스트 출력 인용)
    - `pnpm --filter @gh-radar/relay test` exit 0 · `run typecheck:tests` exit 0
    - relay 테스트 총 건수 전/후 인용
  </acceptance_criteria>
  <done>이름이 붙는 것과 안 붙는 것이 각각 잠겼다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → relay `lc.set` | 표시용 문자열(`name`)을 클라이언트가 실어 보낼 수 있으면 화면이 자기가 만든 값을 서버 진실로 믿는 순환이 생긴다 |
| Supabase `stocks` → relay `SymbolMap` → wss | 이름의 원천은 하나여야 한다. 웹앱이 별도 조회를 열면 두 진실이 갈린다 (T-16-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-84 | Spoofing (표시 위조) | `RelayLimitChaserInput` | mitigate | `name`·`code` 를 Omit 목록에 넣는다. `z.object` 가 미지 키를 떨어뜨리는 기존 방어와 이중 |
| T-16-02 | 진실의 분기 | 이름 조회 경로 | mitigate | 새 경로를 만들지 않는다. relay wss 한 경로에 필드를 얹는다 |
| T-16-05 | 지어내기 | 미해석 ISIN | mitigate | 필드를 비워 둔다. ISIN 을 이름 자리에 넣지 않는다 — 테스트로 명시 단언 |
| T-16-85 | 오해 유발 API | `detach`/`releaseAll` | mitigate | 삭제하거나 `@internal` + 리스너 정리를 함께 넣는다 |
| T-16-13 | 운영 사고 | 실서버·실계좌 | accept | FakeGateway·FakeSymbols 만 쓴다 (D-27) |
</threat_model>

<verification>
- `pnpm -r typecheck` exit 0
- `pnpm --filter @gh-radar/relay run typecheck:tests` exit 0
- `pnpm --filter @gh-radar/relay test` exit 0
- 포매터를 돌리지 않는다
</verification>

<success_criteria>
- 60/64 프레임과 캐시 복사본에 이름·단축코드가 붙는다
- 못 푸는 ISIN 은 필드가 없다 (ISIN 을 이름으로 지어내지 않는다)
- 브라우저는 두 필드를 보낼 수 없다
- 상따 캐시 규율(upsert·삭제·전량 교체)이 그대로다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-41-SUMMARY.md` when done
</output>
