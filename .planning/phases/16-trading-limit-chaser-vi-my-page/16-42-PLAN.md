---
phase: 16-trading-limit-chaser-vi-my-page
plan: 42
type: execute
wave: 27
depends_on: ["16-41"]
gap_closure: true
files_modified:
  - webapp/src/lib/isin-labels.ts
  - webapp/src/lib/__tests__/isin-labels.test.tsx
  - webapp/src/components/trading/limit-chaser-form.tsx
  - webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx
  - webapp/src/components/trading/vi-order-list.tsx
  - webapp/src/components/trading/__tests__/vi-order-list.test.tsx
autonomous: true
requirements: [TRADE-01, TRADE-02, NAV-01, MYPAGE-01]

must_haves:
  truths:
    - "보유도 미체결도 없는 종목에 건 상따 전략이 사이드바·전략 현황·상따 페이지에서 **종목명으로** 표시된다 — `KR7005930003` 원문이 뜨지 않는다 (갭 4)"
    - "relay 가 이름을 못 푼 종목은 여전히 ISIN 을 그대로 보여준다 — 지어내지 않는다"
    - "전략을 **내리는** 「수정」은 무장 조건에 막히지 않는다 — relay(`#strategyArmable` 의 철거 면제)보다 UI 가 더 엄격하지 않다 (R2-WR-02 / T-16-44)"
    - "안전 문구(전송 실패·무장 차단)는 원인이 사라지면 접힌다 — 상시 표시되어 다음번에 읽히지 않는 상태가 되지 않는다 (R2-IN-01)"
  artifacts:
    - path: "webapp/src/lib/isin-labels.ts"
      provides: "limitChasers 를 이름 원천에 포함 (relay wss 한 경로 유지)"
      contains: "limitChasers"
    - path: "webapp/src/components/trading/limit-chaser-form.tsx"
      provides: "철거 의도의 「수정」 무장 가드 면제 + 값 변경 시 문구 해제"
      contains: "isDeleteIntent"
    - path: "webapp/src/lib/__tests__/isin-labels.test.tsx"
      provides: "전략만 있는 종목의 이름 해석 + 미해석 시 부재 케이스"
      contains: "limitChasers"
  key_links:
    - from: "webapp/src/lib/isin-labels.ts"
      to: "useRelayContext().limitChasers"
      via: "이미 받고 있는 전역 wss 상태를 추가 원천으로 쓴다 (새 조회 경로 없음)"
      pattern: "limitChasers"
    - from: "webapp/src/components/trading/limit-chaser-form.tsx handleSubmit"
      to: "webapp/src/lib/limit-chaser.ts isDeleteIntent"
      via: "relay #isTeardown 과 같은 네 항으로 면제"
      pattern: "isDeleteIntent"
---

<objective>
웹앱 쪽 3건을 닫는다. 셋 다 **화면이 사용자에게 거짓말을 하거나 정당한 조작을 막는** 자리다.

- **갭 4 (사용자 직접 보고).** `webapp/src/lib/isin-labels.ts` 가 `accountStates.hold`·`unf`·`viOrders` 에서만 이름을 모은다. 보유도 미체결도 없는 종목에 전략을 걸면 사이드바에 `KR7005930003` 원문이 뜬다. **16-41 이 relay 쪽에서 `RelayLimitChaser` 에 `name`·`code` 를 붙였으므로**, 이 파일은 이미 받고 있는 `limitChasers` 를 원천 목록에 더하기만 하면 된다 — 새 조회 경로가 생기지 않아 T-16-02 를 지킨다.
- **R2-WR-02.** 「수정」의 무장 가드(`limit-chaser-form.tsx:495-500`)에 **철거 면제가 없다.** `GATE_KEYS = ['buyEnabled','sweepEnabled','sellEnabled']` 인데 `sweepEnabled` 는 삭제 판정 4종(`isDeleteIntent`: buy/sell/cancelQty/cancelTrade)에 **들어가지 않는다.** 그래서 게이트 4종이 전부 꺼진(= 삭제 의도) 상태인데 `sweepEnabled:true` 이고 시세가 끊겨 `buyOrderPrice === 0` 이면 「수정」이 차단된다. **relay 는 이 요청을 받아 준다**(`fanout.ts` `#strategyArmable` 첫 줄이 `#isTeardown` 으로 면제 — 16-36 이 그 줄을 남겨 둔 이유가 정확히 이것이다). 마지막 관문이 첫 관문보다 관대한 상태이고, 이 파일이 스스로 적어 둔 규율(「끄는 방향은 여기서도 막지 않는다 — T-16-44」)이 이 경우에 성립하지 않는다.
- **R2-IN-01.** `submitError`/`sendError` 는 (a) 다음 성공 전송 (b) `[server]` 이펙트에서만 지워진다. 사용자가 「주문금액이 매수가격보다 작아 주문수량이 0 주예요」를 보고 **금액을 올려도** `role="alert"` 문구가 그대로 남는다. 안전 문구가 상시 표시되면 다음번엔 읽히지 않는다.

Purpose: 화면이 아는 것만 말하고, 막지 말아야 할 것을 막지 않고, 해결된 경고를 치운다.
Output: 이름 원천 확장 · 철거 면제 · 문구 자동 해제 · 각각의 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION-R2.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW-R2.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-41-SUMMARY.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-36-SUMMARY.md
</context>

<interfaces>
`webapp/src/lib/isin-labels.ts`:
- `export interface IsinLabel { name?: string; code?: string }`
- `export function useIsinLabels(): ReadonlyMap<string, IsinLabel>` — `const { accountStates, viOrders } = useRelayContext();` + `useMemo`
- 내부 `put(isin, next)` — **나중 프레임이 빈 값으로 이전 값을 지우지 않는다**(깜빡임 방지). 새 원천도 이 규칙을 통과해야 한다
- 파일 머리 주석 (a) 「`RelayLimitChaser` 에는 종목명도 단축코드도 **없다**」 — **16-41 이후 거짓이 됐다. 반드시 고친다**
- 소비자 3곳: `app-sidebar.tsx:310` · `strategy-status-card.tsx:418` · `limit-chaser-client.tsx:166`

`webapp/src/lib/relay-provider.tsx`:
- 컨텍스트 값에 `limitChasers: RelayLimitChaser[]` 가 **이미 있다**(:200, `EMPTY_RELAY_VALUE`). `use-relay-socket.ts:408/413` 이 `lc`/`lc.snap` 프레임으로 채운다 — **새 배선이 필요 없다**

`webapp/src/components/trading/limit-chaser-form.tsx`:
- `const GATE_KEYS = ['buyEnabled', 'sweepEnabled', 'sellEnabled'] as const` (:113)
- `gateBlocked(key, next)` (:449) · `armBlockedTextOf(key, values)` (:147)
- `toggleGate` 전송 직전 가드 (:462) · `handleSubmit` 무장 가드 (:495-500) — **후자만** 이 plan 의 대상
- `setSubmitError('')` 는 `useEffect(..., [server])`(:527~)와 성공 경로에서만 일어난다
- import 에 `crudOf` 가 이미 있다(:94) — `isDeleteIntent` 를 같은 모듈에서 추가로 가져온다

`webapp/src/lib/limit-chaser.ts`:
- `isDeleteIntent(gates)` (:167-174) = `!buyEnabled && !sellEnabled && !cancelQtyEnabled && !cancelTradeEnabled`
- `LimitChaserFormValues` 는 네 게이트를 **전부 포함**한다(Omit 목록에 없다) — 그대로 넘길 수 있다

`webapp/src/components/trading/vi-order-list.tsx`:
- `const [sendError, setSendError] = useState('')` (:213), 설정 (:254), 해제 (:257), 표시 (:298-305). props `items` (:189)

테스트 하네스 선례 — `webapp/src/components/trading/__tests__/strategy-status-card.test.tsx:29-42`:
`type RelayShape = ReturnType<typeof import("@/lib/relay-provider").useRelayContext>` + `vi.mock("@/lib/relay-provider", async (importOriginal) => ({ ...actual, useRelayContext: () => mockRelay }))` + `EMPTY_RELAY_VALUE` 기반 픽스처. **그대로 본뜬다.**
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 이름의 원천에 상따 전략을 더한다 (갭 4)</name>
  <files>webapp/src/lib/isin-labels.ts, webapp/src/lib/__tests__/isin-labels.test.tsx</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-41-SUMMARY.md` — **먼저 읽는다.** relay 가 어떤 필드를 어떤 조건에서 붙이는지(못 풀면 필드 부재)
    - `webapp/src/lib/isin-labels.ts` 전문 — 특히 머리 주석 (a)(b)(c) 세 규율과 `put` 의 병합 규칙
    - `webapp/src/lib/relay-provider.tsx` — 컨텍스트 값의 형태와 `EMPTY_RELAY_VALUE`
    - `webapp/src/components/layout/app-sidebar.tsx` :300-320 — `labels.get(item.isin)?.name ?? null` 소비 형태. **여기는 바뀌지 않아야 한다**
    - `webapp/src/components/trading/__tests__/strategy-status-card.test.tsx` :20-60 — `useRelayContext` 모킹 하네스
  </read_first>
  <action>
① `useIsinLabels` 가 `useRelayContext()` 에서 `limitChasers` 를 함께 꺼내고, `accountStates`·`viOrders` 순회 **뒤에** `for (const item of limitChasers) put(item.isin, { name: item.name, code: item.code })` 를 넣는다. `useMemo` 의존성 배열에 `limitChasers` 를 **반드시** 더한다(빠뜨리면 전략이 늘어도 라벨이 갱신되지 않는다 — 조용한 실패다).

② 머리 주석 (a) 를 고친다. 지금 문장(「`RelayLimitChaser` 에는 종목명도 단축코드도 **없다**」)은 16-41 이후 거짓이다. 새 문장에 담을 것:
   - relay 가 잔고·미체결·VI 와 **같은 `SymbolMap`** 으로 상따 에코에도 이름을 붙인다(16-41). 그래서 원천은 **여전히 relay wss 하나**다 — T-16-02 를 지킨다
   - **그래도 「모르면 ISIN 을 그대로」는 남는다.** `SymbolMap` 이 못 푸는 종목(신규 상장 직후·마스터 미로딩)이 있고, relay 는 그때 **필드를 비워 보낸다**. 지어내지 않는 규율의 양 끝이다
   - (b)(c) 규율은 그대로 살린다

③ 새 테스트 파일 `webapp/src/lib/__tests__/isin-labels.test.tsx` 를 만든다(`renderHook` 이 필요하므로 `.tsx`). `strategy-status-card.test.tsx` 의 모킹 하네스를 본뜬다. 케이스 3개:
   - 「보유도 미체결도 없는 종목의 전략이 이름을 갖는다」 — `accountStates` 는 비우고 `limitChasers` 에만 `{isin, name, code}` 를 넣어 `labels.get(isin)?.name` 을 단언
   - 「relay 가 이름을 못 준 전략은 라벨이 비어 있다」 — `name`/`code` 없는 항목 → `labels.get(isin)?.name` 이 `undefined`. **ISIN 문자열이 아님**을 함께 단언한다
   - 「잔고에 이름이 있고 전략에 없으면 잔고 이름이 유지된다」 — `put` 의 「빈 값이 이전 값을 지우지 않는다」 규칙 회귀

④ `app-sidebar.tsx`·`strategy-status-card.tsx`·`limit-chaser-client.tsx` 는 **건드리지 않는다.** 소비 형태가 그대로여야 이 수정이 한 곳에서 끝난다는 증거가 된다. `git diff` 로 확인해 SUMMARY 에 인용한다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp exec vitest run src/lib/__tests__/isin-labels.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f webapp/src/lib/__tests__/isin-labels.test.tsx` 가 참임을 확인한 뒤 위 명령을 돌린다 (⚠️ `pnpm --filter @gh-radar/webapp test` 는 `--passWithNoTests` 라 파일이 없어도 통과한다 — 존재 확인이 먼저다)
    - ⚠️ 필터 이름은 **`@gh-radar/webapp`** 이다. `gh-radar-webapp` 은 존재하지 않는 필터이고 `No projects matched the filters` + **exit 0** 으로 끝난다(절대 실패할 수 없는 검증)
    - `useIsinLabels` 의 `useMemo` 의존성 배열에 `limitChasers` 가 있다 (소스 인용)
    - 머리 주석 (a) 가 갱신됐고 「모르면 ISIN 을 그대로」 규율이 **여전히 명시**돼 있다 (소스 인용)
    - 케이스 3개가 전부 존재하고 통과 (제목 SUMMARY 인용)
    - `git diff -- webapp/src/components/layout/app-sidebar.tsx webapp/src/components/trading/strategy-status-card.tsx webapp/src/components/trading/limit-chaser-client.tsx` 가 비어 있다
    - `pnpm --filter @gh-radar/webapp run typecheck` exit 0
  </acceptance_criteria>
  <done>전략만 걸린 종목도 이름으로 보이고, 모르는 종목은 여전히 ISIN 이다.</done>
</task>

<task type="auto">
  <name>Task 2: 「수정」의 무장 가드에 철거 면제를 붙인다 (R2-WR-02) + 문구 자동 해제 (R2-IN-01)</name>
  <files>webapp/src/components/trading/limit-chaser-form.tsx, webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-36-SUMMARY.md` — **먼저 읽는다.** relay `#isTeardown` 이 이제 게이트 4종으로 판정하고, `#strategyArmable` 의 철거 면제가 **왜 남았는지**(sweep). 이 task 는 그 대칭이다
    - `webapp/src/components/trading/limit-chaser-form.tsx` — `GATE_KEYS`·`armBlockedTextOf`·`gateBlocked`·`toggleGate`·`handleSubmit`·`handleRevert`·`[server]` 이펙트·`setField` 계열. **한 번에 읽는다**
    - `webapp/src/lib/limit-chaser.ts` — `isDeleteIntent`·`crudOf`·`LimitChaserFormValues` 의 필드 목록(네 게이트가 들어 있는지 직접 확인)
    - `webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx` — 기존 무장 가드 케이스(GC-WR-09 가 넣은 것)와 문구 단언 방식
  </read_first>
  <action>
① `handleSubmit` 의 무장 가드에 **철거 면제**를 붙인다. `isDeleteIntent(values)` 가 참이면 `GATE_KEYS` 검사를 건너뛴다. `isDeleteIntent` 를 `@/lib/limit-chaser` 에서 import 한다(`crudOf` 와 같은 줄).

② 그 줄 옆 주석에 근거를 적는다:
   - relay `#strategyArmable` 첫 줄이 `#isTeardown(cfg)` 로 면제한다 — **여기가 relay 보다 엄격하면 사용자는 전략을 내리려는데 화면이 막는다**
   - `sweepEnabled` 는 삭제 판정 4종에 **없다.** 그래서 「게이트 4종 OFF + sweep ON + 시세 끊김」이 정확히 이 함정이다
   - 이 파일이 이미 적어 둔 T-16-44(「끄는 방향은 여기서도 막지 않는다」)의 **누락된 나머지 절반**이다

③ `toggleGate` 의 전송 직전 가드(:462)는 **건드리지 않는다.** 그쪽은 「지금 켜려는 스위치」 하나를 보는 다른 판정이다. 왜 다른지 SUMMARY 에 한 줄로 적는다.

④ **R2-IN-01.** 값이 바뀌면 `submitError` 를 접는다. 구현 위치는 재량이되(폼 값 변경 이펙트 / `setField` 안 / 둘 다), 다음을 만족해야 한다:
   - 사용자가 원인 필드를 고치면 `role="alert"` 문구가 **사라진다**
   - 「수정」 직후의 실패 문구가 그 즉시 지워지지는 않는다(자기 자신에 의해 사라지면 사용자가 읽을 시간이 없다) — 렌더 순서를 확인하고, 문제가 있으면 값 변경만을 트리거로 삼는다
   - `[server]` 이펙트의 기존 해제는 그대로 둔다

⑤ 테스트를 추가한다:
   - 「게이트 4종 OFF + `sweepEnabled:true` + `buyOrderPrice:0` 상태에서 「수정」을 누르면 **전송된다**」 — `send` 가 `{t:'lc.set'}` 로 호출됐음을 단언. 이것이 R2-WR-02 의 회귀 잠금이다
   - 「게이트가 하나라도 켜진 상태의 무장 미달은 **여전히 막힌다**」 — GC-WR-09 가 얻은 것을 잃지 않았다는 증거. 기존 케이스가 이것이면 통과 확인만 한다
   - 「값을 고치면 무장 차단 문구가 사라진다」 — R2-IN-01
   - ⚠️ 기존 테스트가 깨지면 **그 단언이 옛 구현을 베낀 것인지 먼저 판정한다.** 「사용자가 전략을 못 내린다」를 진실로 잠근 단언이라면 그 단언을 고치는 것이 맞다. 판정 근거를 SUMMARY 에 적는다
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp exec vitest run src/components/trading/__tests__/limit-chaser-form.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx` 가 참임을 확인한 뒤 위 명령을 돌린다
    - `handleSubmit` 의 가드에 `isDeleteIntent` 가 쓰인다 (소스 인용)
    - `toggleGate` 가드 diff 가 0줄이다 (`git diff` 인용)
    - 「철거 의도 + sweep 무장 미달 → 전송된다」 케이스가 존재하고 통과
    - 「게이트 ON + 무장 미달 → 차단된다」 케이스가 여전히 통과
    - 「값 변경 시 문구 해제」 케이스가 존재하고 통과
    - `pnpm --filter @gh-radar/webapp run typecheck` exit 0
  </acceptance_criteria>
  <done>UI 가 relay 보다 엄격하지 않다. 해결된 경고는 접힌다.</done>
</task>

<task type="auto">
  <name>Task 3: VI 주문 목록의 전송 실패 문구도 접힌다 (R2-IN-01)</name>
  <files>webapp/src/components/trading/vi-order-list.tsx, webapp/src/components/trading/__tests__/vi-order-list.test.tsx</files>
  <read_first>
    - `webapp/src/components/trading/vi-order-list.tsx` — `sendError` 상태·설정·해제·표시 지점 전부와 `items` prop 이 갱신되는 경로, `optimistic`/`sending` 상태와의 상호작용
    - `webapp/src/components/trading/__tests__/vi-order-list.test.tsx` — 16-32 가 넣은 `send()` 반환값 케이스의 형태
  </read_first>
  <action>
① `items` 가 바뀌면 `sendError` 를 접는다. 근거: `items` 갱신은 서버(73 푸시)가 무언가를 말했다는 뜻이므로 이전 전송 실패 사건은 이미 지나간 것이다 — `limit-chaser-form` 의 `[server]` 이펙트와 **같은 논리**다. 그 대응 관계를 주석에 적는다.

② 낙관적 상태(`optimistic`)·`sending` 잠금과 충돌하지 않는지 확인한다. `items` 변경이 잠금을 푸는 신호로 이미 쓰이고 있다면 그 이펙트에 한 줄을 잇는다 — 이펙트를 새로 만들어 순서 의존을 늘리지 않는다.

③ 테스트: 「전송 실패 문구가 뜬 뒤 `items` 가 갱신되면 문구가 사라진다」. 16-32 가 넣은 `send()` 실패 케이스를 이어 쓴다.

④ `vi-settings-card.tsx` 는 이 plan 의 범위가 **아니다.** 같은 성질의 문구가 그쪽에도 있으면 SUMMARY 에 기록만 하고 손대지 않는다(범위를 조용히 넓히지 않는다).
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp exec vitest run src/components/trading/__tests__/vi-order-list.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f webapp/src/components/trading/__tests__/vi-order-list.test.tsx` 가 참임을 확인한 뒤 위 명령을 돌린다
    - `items` 를 트리거로 `sendError` 를 접는 코드가 있다 (소스 인용)
    - 새 케이스가 존재하고 통과 (제목 인용)
    - `git diff -- webapp/src/components/trading/vi-settings-card.tsx` 가 비어 있다
    - `pnpm --filter @gh-radar/webapp test` exit 0 · `run typecheck` exit 0
  </acceptance_criteria>
  <done>서버가 말을 걸면 지난 실패 문구가 물러난다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay wss → 웹앱 표시 | 이름은 relay 가 준 것만 쓴다. 화면이 별도 조회로 두 번째 진실을 만들지 않는다 (T-16-02) |
| UI 가드 ↔ relay 가드 | 첫 관문이 마지막 관문보다 엄격하면 정당한 조작이 막힌다. 자산을 인질로 잡는 방향이다 (T-16-44) |
| 안전 문구 ↔ 사용자 주의력 | 상시 표시되는 경고는 읽히지 않는다. 그 순간 진짜 경고도 함께 죽는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | 진실의 분기 | `isin-labels.ts` 원천 목록 | mitigate | 이미 받고 있는 `limitChasers` 를 쓴다. REST·Supabase 직접 조회를 만들지 않는다 |
| T-16-05 | 지어내기 | 미해석 ISIN 표시 | mitigate | relay 가 필드를 비우고 UI 가 ISIN 폴백. 「name 이 ISIN 문자열이 아니다」를 테스트로 단언 |
| T-16-44 | Denial of Service (자산 인질) | `handleSubmit` 무장 가드 | mitigate | `isDeleteIntent` 면제. 「철거 + sweep 무장 미달 → 전송」 케이스로 잠금 |
| T-16-60 | Elevation (무장 미달 전송) | 같은 가드 | mitigate | 게이트가 켜진 경우의 차단은 **그대로**. 기존 케이스를 회귀 게이트로 둔다 |
| T-16-86 | 경고 피로 | `submitError`/`sendError` 수명 | mitigate | 원인 변경·서버 응답을 해제 트리거로 삼는다 |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/webapp test` exit 0 (⚠️ 이 스크립트는 `--passWithNoTests` 다 — **대상 파일 존재를 먼저 단언**한 뒤에만 근거로 쓴다)
- `pnpm --filter @gh-radar/webapp run typecheck` exit 0 (`tsc --noEmit` + e2e tsconfig 를 함께 돈다)
- 필터 정본은 `@gh-radar/webapp` 이다 — `gh-radar-webapp` 은 없는 필터이고 exit 0 으로 끝난다
- Playwright 전량은 종결 plan(16-46)이 돌린다. 이 plan 에서 E2E 문구 단언이 깨질 만한 변경을 했다면 SUMMARY 에 후보를 적어 넘긴다
- 포매터를 돌리지 않는다 (이 저장소에 prettier 설정이 없다)
</verification>

<success_criteria>
- 전략만 걸린 종목이 이름으로 보인다
- 모르는 종목은 여전히 ISIN 이다
- 철거 의도의 「수정」이 막히지 않는다
- 게이트가 켜진 무장 미달은 여전히 막힌다
- 원인이 사라지면 문구가 접힌다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-42-SUMMARY.md` when done
</output>
