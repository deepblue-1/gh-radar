---
phase: 16-trading-limit-chaser-vi-my-page
plan: 27
type: execute
wave: 19
depends_on: ["16-22"]
gap_closure: true
files_modified:
  - relay/src/ws/order-handler.ts
  - relay/tests/ws-order.test.ts
autonomous: true
requirements: [TRADE-03]

must_haves:
  truths:
    - "통보로 대기 항목을 하나로 좁히지 못하면 「가장 오래된 것」을 고르지 않고 매칭 실패로 두어 `recordUnmatched` 로 보낸다 — **후보가 1건일 때도 마찬가지다** (16-VERIFICATION 갭 1 / GC-CR-01)"
    - "통보가 **실어 온** 강한 축(비어 있지 않은 `orgOrderNo` · 취소성 `noticeType` C/M)은 후보 수와 무관하게 하드 필터로 적용되고, **비어 있는 축은 건너뛴다** — 구 게이트웨이 호환은 그대로 유지된다 (GC-CR-01)"
    - "`await insertRequest` 중에 연결이 닫히면 주문이 게이트웨이로 **나가지 않는다** — 조립·대기 등록·타이머 이전에 중단하고 `status:\"rejected\"` 로 기록한다 (16-VERIFICATION 갭 2 ② / GC-CR-03)"
    - "고아 `ConnState` 에 등록된 대기가 5초 뒤 `status:\"timeout\"` 으로 확정되는 경로가 존재하지 않는다 — 실제로 접수·체결된 주문이 `timeout` 으로 감사 기록에 남지 않는다 (GC-CR-03)"
  artifacts:
    - path: "relay/src/ws/order-handler.ts"
      provides: "후보 수와 무관한 하드 필터 + await 후 연결 생존 재확인"
      contains: "narrowPending"
    - path: "relay/tests/ws-order.test.ts"
      provides: "「유일 후보라도 취소확인은 신규 대기를 정산하지 않는다」 · 「await 중 연결 종료면 송신하지 않는다」"
  key_links:
    - from: "relay/src/ws/order-handler.ts"
      to: "relay/src/dma/envelope.ts"
      via: "ParsedOrderResp 의 orgOrderNo·noticeType 을 **후보 수와 무관한** 하드 필터로 소비"
      pattern: "isCancelNotice|orgOrderNo"
    - from: "relay/src/ws/order-handler.ts (handle)"
      to: "relay/src/ws/order-handler.ts (conns Map)"
      via: "await insertRequest 직후 conns.get(conn) !== state 재확인"
      pattern: "conns\\.get\\(conn\\) !== state"
---

<objective>
**16-VERIFICATION 갭 1(GC-CR-01)** 과 **갭 2 의 ②(GC-CR-03)** 를 닫는다. 둘 다 「돈이 나가는 자리에서 relay 가 사실이 아닌 것을 사실로 확정한다」는 한 뿌리다.

지금 상태:
- **GC-CR-01.** `narrowPending` 의 첫 실행문이 `if (candidates.length <= 1) return candidates[0] ?? null;`(order-handler.ts:864-865) 이고, 후보 수집 필터는 `entry.isin === notice.isin` 하나뿐이다(:340). 대기가 1건이면 통보가 무엇을 실어 왔든 그 대기가 정산된다 — `orgOrderNo` 가 채워진 취소확인이든, 세션 합류로 들어온 남의 통보든(`notice-status.ts:51` 이 그 존재를 명시한다) 상관없다. 결과는 이 파일이 스스로 「최악의 결과」라고 적어 둔 상황이다: **살아 있는 매수 주문이 화면에 「취소됨」으로 뜨고**(`statusOf` 의 `"C"→cancelled`), 사용자가 그 표시를 믿고 재주문하면 중복 체결이다. 동시에 `dma_orders` 행에 남의 `order_no` 가 박힌다(`finish` 의 `enqueueUpdate`, :749-758). 단위 테스트 `ws-order.test.ts:991-998` 이 이 동작을 **의도로 못박아** 두었으므로 회귀가 아니라 설계 결함이다.
- **GC-CR-03.** `handle` 이 `deps.orderStore.insertRequest(...)` 를 `await` 하는 동안(Supabase 왕복 수십~수백 ms) 사용자가 탭을 닫으면 `closeConn` 이 먼저 돌아 `state.pending` 을 비우고 `conns`·`byUser` 에서 그 연결을 지운다(:816-835). `await` 재개 후 코드는 **연결 생존을 다시 확인하지 않고** 타이머를 걸고(:770) 고아 `ConnState` 에 대기를 등록하고(:786) **주문을 실제로 게이트웨이에 보낸다**(:789). 통보 상관은 `byUser`→`conns` 를 훑으므로 이 대기는 영원히 후보가 되지 않고, 5초 뒤 고아 타이머가 `enqueueUpdate({orderRowId, status:"timeout"})` 을 확정한다 — **실제로 접수·체결된 주문이 감사 기록에 `timeout` 으로 남고 정정 경로가 없다.**

「후보가 1개면 그것」이라는 근거로 든 「구 서버가 축을 비워 보낸다」는 **비어 있는 축을 건너뛸** 이유이지 **통보가 실제로 실어 온 축을 무시할** 이유가 아니다. 그 구분이 이 plan Task 1 의 전부다.

Purpose: TRADE-03 의 「`DirectOrderReq(2)` 5초 상관」이 **옳은 주문에만** 상관되고, **보내기 전에** 연결 생존을 확인하게 한다.
Output: 후보 수와 무관한 하드 필터 · `await` 후 TOCTOU 가드 · 뒤집힌 회귀 테스트 2케이스.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-CONTEXT.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-22-SUMMARY.md
</context>

<interfaces>
<!-- 손대는 자리의 현재 계약. 16-22 가 세운 것 위에 올린다 — 뒤집는 것이 아니다. -->

`relay/src/ws/order-handler.ts`:
- `export type PendingOrder = { rid; orderRowId; isin; qty; price; isCancel; orgOrderNo; timer; settle }` — 매칭 축 4종이 이미 실려 있다(16-22)
- `export function narrowPending(candidates: PendingOrder[], n: ParsedOrderResp): PendingOrder | null` — 파일 하단 순수 함수 구역. 현재 첫 실행문이 `if (candidates.length <= 1) return candidates[0] ?? null;`
- 내부 `refine(keep)` — 「남는 후보가 0이 되면 그 축은 없던 것으로 한다」는 **단계적 좁히기** 헬퍼. ①~④ 축이 이것을 쓴다
- 팩토리 스코프: `const conns = new Map<C, ConnState>()`, `const byUser = new Map<string, Set<C>>()`
- `handle` 의 흐름: 키 잡기 → 게이트 ①(세션) → ②(계좌 화이트리스트) → ③(취소 원주문번호) → ③-1(ISIN 해석) → **③-2 `await insertRequest`** → 조립(`buildDirectOrderReq`) → ④ 타이머·`entry`·`state.pending.push` → ⑤ `session.send(payload)`
- `release(state, keys)` · `reject(conn, rid, message)` — 중단 경로가 반드시 지나야 하는 두 함수
- `closeConn(conn)` — `state.pending` 비우기 · `state.claims.clear()` · `dropUserDupKeys` · `conns.delete(conn)` · `byUser` 정리

`relay/src/dma/envelope.ts` `ParsedOrderResp` 의 매칭 축:
- `noticeType: string` — "A"=접수 "E"=체결 "C"=취소확인 "M"=정정확인 "R"=거부. **구 서버는 빈 값**
- `orgOrderNo: string` — 원주문번호. **신규 통보는 ""**
- `quantity`·`price` — 체결 통보에서는 체결값(부분체결이면 주문값과 다르다)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 통보가 실어 온 강한 축을 후보 수와 무관한 하드 필터로 만든다 (GC-CR-01)</name>
  <files>relay/src/ws/order-handler.ts, relay/tests/ws-order.test.ts</files>
  <read_first>
    - `relay/src/ws/order-handler.ts` L840~892 (`narrowPending` 전문 + 그 위 docstring — 「하드 필터가 아니라 단계적 좁히기」의 근거가 적혀 있다. **그 근거의 유효 범위가 이번 변경의 핵심**이다), L326~372 (통보 소비 루프 — 후보 수집 필터가 ISIN 하나뿐임을 확인)
    - `relay/src/order/notice-status.ts` L40~60 — `statusOf` 의 `"C"→cancelled` 매핑과 L51 의 「세션 합류로 남의 통보가 올 수 있다」 주석
    - `relay/tests/ws-order.test.ts` L985~1000 — `describe("narrowPending …")` 첫 케이스 「후보가 없으면 null, 하나면 축이 어긋나도 그것이다 (정상 경로 회귀 방지)」. **이 케이스를 뒤집는다**
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §GC-CR-01 — Issue 3경로 + Fix 스니펫(`isCancelNotice`·`hard` 필터)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md` `gaps[0]` — truth / reason / missing 3항목
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-22-SUMMARY.md` — 이 함수를 만든 plan 이 무엇을 의도했는지
  </read_first>
  <action>
① `narrowPending` 의 첫 실행문 `if (candidates.length <= 1) return candidates[0] ?? null;` 을 **제거**한다. 대신 `if (candidates.length === 0) return null;` 만 남긴다.

② 그 자리에 **하드 필터** 단계를 새로 넣는다. `refine`(단계적 좁히기)과 **다른 규율**임을 주석으로 못박는다 — 하드 필터는 결과가 0건이면 `null` 을 돌려주고, `refine` 은 0건이면 그 축을 건너뛴다. 두 규율이 갈리는 지점이 「통보가 그 축을 **실어 왔는가**」다.
  - `isCancelNotice = n.noticeType === "C" || n.noticeType === "M"` 를 계산한다.
  - 하드 필터 조건 2개(16-REVIEW §GC-CR-01 의 Fix 스니펫을 그대로 참조하라):
    - `n.orgOrderNo === ""` 이면 이 축을 적용하지 않는다. 비어 있지 않으면 `p.isCancel && p.orgOrderNo === n.orgOrderNo` 인 후보만 남긴다.
    - `!isCancelNotice` 이면 이 축을 적용하지 않는다. 취소성 통보면 `p.isCancel` 인 후보만 남긴다.
  - 필터 결과가 0건이면 **`null` 을 돌려준다**. 주석: 「아무것도 정산하지 않는다 — 5초 타임아웃이 이 상황의 진실이다」.
  - 결과가 1건이면 그것을 돌려준다.
  - 2건 이상이면 그 결과(`hard`)를 `pool` 의 시작값으로 삼아 기존 ①~④ 단계적 좁히기를 **그대로** 이어 간다. ① 취소 축은 하드 필터와 중복되지만 무해하므로 지우지 않아도 된다 — 지운다면 그 근거를 주석에 남길 것.

③ 함수 docstring 을 갱신한다. 기존 「축을 하드 필터가 아니라 단계적 좁히기로 쓰는 이유」 문단은 **삭제하지 말고 범위를 좁힌다**: 「구 게이트웨이가 **비워 보내는** 축과 체결 통보의 값 변동 때문에 ①~④ 는 단계적 좁히기다. 그러나 통보가 **실제로 실어 온** 강한 축은 후보 수와 무관한 하드 필터다 — 비어 있는 축만 건너뛰는 것이 원래 의미였다 (GC-CR-01)」.

④ 테스트를 뒤집는다. `ws-order.test.ts` 의 「후보가 없으면 null, 하나면 축이 어긋나도 그것이다 (정상 경로 회귀 방지)」 케이스를 **「유일 후보라도 취소확인은 신규 대기를 정산하지 않는다」** 로 바꾼다. 새 케이스가 잠그는 것:
  - 후보 0건 → `null` (그대로 유지)
  - 신규 대기 1건 + `noticeType:"C"`, `orgOrderNo:"0000012345"` 통보 → **`null`**
  - 신규 대기 1건 + `noticeType:"A"`(접수), `orgOrderNo:""` 통보 → **그 대기** (구 서버 호환·정상 경로 회귀 방지. `quantity`/`price` 가 어긋나도 여전히 그것이다 — ③④ 는 단계적 좁히기이므로)
  - 취소 대기 1건 + `orgOrderNo` 가 **다른** 취소확인 통보 → **`null`**

⑤ 통보 소비 루프의 `logger.warn` 조건을 점검한다. 지금은 `candidates.length > 1` 일 때만 남기는데, 이제 후보 1건도 미정산으로 끝날 수 있다. **후보가 1건 이상인데 좁히지 못한 모든 경우**로 조건을 넓힌다(`candidates.length > 0 && picked === null`). 로그 필드는 지금 그대로 `isin`·`noticeType`·후보 수만 싣는다 — 계좌번호·주문번호 원문 금지(T-16-32).
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay exec vitest run tests/ws-order.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "candidates.length <= 1" relay/src/ws/order-handler.ts` == 0 — 후보 1건 지름길이 사라졌다
    - `grep -c "isCancelNotice" relay/src/ws/order-handler.ts` >= 1 — 취소성 통보 판정이 하드 필터 자리에 있다
    - `grep -c "유일 후보라도 취소확인은 신규 대기를 정산하지 않는다" relay/tests/ws-order.test.ts` == 1 — 뒤집힌 케이스가 실재
    - `grep -c "유일 후보는 축을 보지 않는다" relay/tests/ws-order.test.ts` == 0 — 옛 의도 고정 주석이 남아 있지 않다
    - `pnpm --filter @gh-radar/relay exec vitest run tests/ws-order.test.ts` exit 0
    - 통보 소비 루프의 warn 조건이 `candidates.length > 1` 이 아니다 (소스 확인 후 SUMMARY 에 줄번호 인용) — 후보 1건 미정산도 로그를 남긴다 (무로그 fail-safe 금지 / S-5)
  </acceptance_criteria>
  <done>통보가 실어 온 축과 어긋나는 대기는 후보가 1건이어도 정산되지 않고, 그 사실이 로그에 남는다.</done>
</task>

<task type="auto">
  <name>Task 2: `await insertRequest` 직후 연결 생존을 재확인하고, 죽었으면 보내기 전에 중단한다 (GC-CR-03)</name>
  <files>relay/src/ws/order-handler.ts, relay/tests/ws-order.test.ts</files>
  <read_first>
    - `relay/src/ws/order-handler.ts` L655~700 (게이트 ③-1 → ③-2 `await deps.orderStore.insertRequest(...)` → catch 분기), L700~800 (조립 → `finish` 정의 → `setTimeout` → `entry` → `state.pending.push` → ⑤ `session.send`), L810~845 (`closeConn` — 무엇을 비우고 무엇을 지우는지)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §GC-CR-03 — 결과 3항목(영원히 후보가 되지 않음 / 고아 타이머의 `timeout` 확정 / `release` 조기 반환이 존재를 감춤) + Fix 스니펫
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md` `gaps[1]` reason 의 ② 문단
    - `relay/tests/ws-order.test.ts` — `insertRequest` 를 스텁하는 방식(해소 시점을 붙잡을 수 있는지)과 `closeConn` 을 부르는 헬퍼. `relay/tests/order-store.test.ts` 의 `insertGate` 패턴(경주를 결정론적으로 재현하는 기존 관례)도 함께 본다
  </read_first>
  <action>
① `await deps.orderStore.insertRequest(...)` 의 `try/catch` **직후**, 조립(`buildDirectOrderReq`)보다 **먼저** 연결 생존 가드를 넣는다. 판정은 `conns.get(conn) !== state` 하나다 — `closeConn` 이 `conns.delete(conn)` 을 하므로 이 비교가 「그 사이에 닫혔다」의 유일한 정본이다. 주석에 「**보내기 전에 확인한다**가 유일한 안전한 순서다. 대기 등록·타이머 생성 이전이라 회수할 것이 없다 (GC-CR-03)」를 적는다.

② 가드에 걸리면 순서대로:
  - `logger.warn({ ...logCtx, orderRowId }, "[WS-order] 요청 처리 중 연결 종료 — 주문을 보내지 않는다")` — **조용히 반환하지 않는다**(무로그 fail-safe 금지 / Pitfall 18 / S-5).
  - `deps.orderStore.enqueueUpdate({ orderRowId, status: "rejected", message: "요청 처리 중 연결이 끊겨 주문을 보내지 않았습니다." })` — 행이 `requested` 로 영원히 남지 않게 한다.
  - `release(state, keys)` 를 부른다. `closeConn` 이 이미 `claims`·dup 키를 회수했다면 `release` 는 무해하게 지나간다(기존 조기 반환 규율) — 그 사실을 주석 한 줄로 남긴다.
  - `reject(conn, msg.rid, ...)` 는 **부르지 않는다**(받을 소켓이 없다). 부르지 않는 이유를 주석에 적는다.
  - `return`.

③ ⑤ 송신 실패 분기(`if (!session.send(payload))`) 는 **그대로 둔다** — 이 가드는 그 앞자리에 새로 생기는 것이지 대체가 아니다.

④ 테스트 1케이스를 `ws-order.test.ts` 에 추가한다: **「`await insertRequest` 중 `closeConn` 이 돌면 게이트웨이로 송신되지 않는다」**. 재현 방법:
  - `insertRequest` 스텁을 수동으로 해소할 수 있는 Promise 로 둔다(`order-store.test.ts` 의 `insertGate` 와 같은 패턴).
  - `handle(conn, msg)` 를 await 하지 않고 시작 → `closeConn(conn)` 호출 → insert Promise 해소 → `handle` 완료를 기다린다.
  - 단언: FakeGateway 로 나간 `DirectOrderReq` 0건 · `state.pending` 에 남은 항목 0건 · `enqueueUpdate` 에 `status:"rejected"` 1건 · 5초를 진행시켜도(`vi.advanceTimersByTime`) `status:"timeout"` 갱신이 **추가로 들어오지 않는다**.
  - 케이스 이름에 「await 중 연결 종료」를 포함한다.

⑤ 기존 케이스 회귀 확인: 연결이 살아 있는 정상 경로는 그대로 송신돼야 한다. 기존 케이스가 이미 그것을 잠그고 있으므로 새로 만들지 않는다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "conns.get(conn) !== state" relay/src/ws/order-handler.ts` >= 1
    - 그 가드가 `buildDirectOrderReq` 호출보다 **위**에 있다 (두 줄번호를 SUMMARY 에 인용)
    - `grep -c "요청 처리 중 연결 종료" relay/src/ws/order-handler.ts` >= 1 — 무로그 반환이 아니다
    - `grep -c "요청 처리 중 연결이 끊겨" relay/src/ws/order-handler.ts` >= 1 — `status:"rejected"` 기록 메시지가 실재
    - `grep -c "await 중 연결 종료" relay/tests/ws-order.test.ts` >= 1
    - `pnpm --filter @gh-radar/relay test` exit 0
    - `pnpm --filter @gh-radar/relay run typecheck` · `pnpm --filter @gh-radar/relay run typecheck:tests` exit 0
  </acceptance_criteria>
  <done>연결이 끊긴 요청은 게이트웨이로 나가지 않고, 그 사실이 로그와 `dma_orders.status` 양쪽에 남는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트웨이 통보 → relay 대기열 | 통보는 **어느 주문의 것인지 스스로 말하지 않는다**(계좌번호도 없다). 상관은 relay 의 추론이며, 추론이 틀리면 사용자가 잘못된 화면을 믿고 재주문한다 |
| 같은 DMA 세션의 다른 단말(WinForms) → relay | 세션 합류로 **남의 통보**가 이 relay 의 대기열에 도달한다 (`notice-status.ts:51`) |
| 브라우저 연결 수명 ↔ 게이트웨이 송신 | `await` 하나가 두 시점을 갈라 놓는다 — 그 사이의 상태 변화를 확인하지 않으면 고아 주문이 생긴다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-49 | Spoofing (주문 오귀속) | `narrowPending` 후보 1건 경로 | mitigate | 통보가 실어 온 `orgOrderNo`·취소성 `noticeType` 을 **후보 수와 무관한 하드 필터**로 적용. 0건이면 `null`. 「유일 후보라도 취소확인은 신규 대기를 정산하지 않는다」로 회귀 잠금 |
| T-16-50 | Tampering (감사 기록 오확정) | `handle` 의 `await insertRequest` TOCTOU | mitigate | `conns.get(conn) !== state` 재확인을 조립·송신 **이전**에 둔다. 중단 시 `status:"rejected"` 기록 + `logger.warn`. 「await 중 연결 종료면 송신 0건」으로 회귀 잠금 |
| T-16-51 | Repudiation (무로그 미정산) | 통보 소비 루프 `logger.warn` 조건 | mitigate | 후보 1건 미정산도 warn 을 남기도록 조건 확대. 필드는 `isin`·`noticeType`·후보 수만 — 계좌번호·주문번호 원문 미포함 (T-16-32 승계) |
| T-16-32 | Information Disclosure | 좁히기 실패 로그 | mitigate | 기존 규율 유지. 새 warn 도 같은 필드 집합만 싣는다 |
| T-16-13 | 운영 사고 | 실서버·실계좌 접속 | accept | FakeGateway 만 쓴다 (D-27 승계). 실서버 IP·실계좌 리터럴 0건 유지 |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/relay test` exit 0
- `pnpm --filter @gh-radar/relay run typecheck` · `typecheck:tests` exit 0
- `grep -rn "10\.41\.1\.120" relay/` 0건 (D-27 승계)
</verification>

<success_criteria>
- 후보가 1건이어도 통보가 실어 온 축과 어긋나면 아무것도 정산되지 않는다
- 구 게이트웨이의 **빈 축** 통보는 여전히 정상 정산된다 (호환 유지)
- `await` 중 연결이 닫히면 주문이 게이트웨이로 나가지 않는다
- 두 경로 모두 침묵하지 않는다 — warn/error 와 `dma_orders` 상태가 함께 남는다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-27-SUMMARY.md` when done
</output>
