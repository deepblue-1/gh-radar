---
phase: 16-trading-limit-chaser-vi-my-page
plan: 20
type: execute
wave: 15
depends_on: ["16-17"]
gap_closure: true
files_modified:
  - webapp/src/lib/orders-api.ts
  - server/src/errors.ts
  - server/src/services/dma-orders.ts
  - packages/shared/src/relay.ts
  - server/tests/routes/orders.test.ts
autonomous: true
requirements: [TRADE-03]

must_haves:
  truths:
    - "webapp 에 임포터 0건인 죽은 주문 모듈이 남아 있지 않다 — 다음 사람이 `orders-api.ts` 를 살아 있는 계약으로 오해하지 않는다 (WR-04)"
    - "`server/src/errors.ts` 의 「웹앱의 `lib/orders-api.ts` 는 `ORDER_ERROR_CODES` 를 그대로 유지한다」 문장이 사실과 맞게 정정됐다 (WR-04 — 지금은 거짓이다)"
    - "`GET /api/orders` 응답에 `origin` 이 실린다 — `dma_orders.origin` 마이그레이션이 선언한 「내가 낸 주문 vs 전략이 낸 주문」 구분이 읽기 계약에 도달한다 (WR-05)"
    - "`DmaOrderRow.origin` 이 `\"manual\" | \"limit_chaser\" | \"vi\"` 로 shared 계약에 실린다 — DB CHECK 3종과 같은 열거다"
    - "주문 이력 **표(UI)** 는 이 plan 의 범위가 아니다(D-20 deferred)라는 사실이 코드 주석과 SUMMARY 에 근거로 남는다"
  artifacts:
    - path: "packages/shared/src/relay.ts"
      provides: "DmaOrderRow.origin 필드 + DmaOrderOrigin 열거"
      contains: "limit_chaser"
    - path: "server/src/services/dma-orders.ts"
      provides: "origin 을 포함한 ORDER_COLS + mapOrder"
      contains: "origin"
    - path: "server/tests/routes/orders.test.ts"
      provides: "origin 왕복 계약 케이스"
  key_links:
    - from: "server/src/services/dma-orders.ts"
      to: "packages/shared/src/relay.ts"
      via: "mapOrder → DmaOrderRow.origin"
      pattern: "origin:\\s*r\\.origin"
---

<objective>
**WR-04 + WR-05** 를 닫는다. 이 phase 가 만든 두 계약이 「쓰기만 하고 읽히지 않는」 / 「있는데 아무도 안 쓰는」 상태로 남아 있다.

- **WR-04:** `webapp/src/lib/orders-api.ts` 는 임포터가 **0건**이다(`listOrders`·`isUnknownOutcome`·`orderErrorCode`·`orderErrorMessage`·`ORDER_ERROR_CODES` 전부 사용처 0 — 실측 확인). 그런데 `server/src/errors.ts:62-63` 은 「웹앱의 `lib/orders-api.ts` 는 `ORDER_ERROR_CODES` 를 그대로 유지한다 — wss 결과 판정에 계속 쓰이는 브라우저 측 상수다」라고 적어 두었다. **사실이 아니다** — 실제 판정은 `order-panel.tsx:370`·`account-panel.tsx:265` 가 `res.status === 'timeout'` 으로 인라인 수행한다.
- **WR-05:** `dma_orders.origin` 은 relay 가 insert·update 양쪽에 채우는데 `ORDER_COLS` 에도 `DmaOrderRow` 에도 없다. 마이그레이션이 명시한 목적(「My page 전략 현황과 주문내역이 「내가 낸 주문」과 「전략이 낸 주문」을 갈라 보여줘야 한다」)이 읽기 계약에 도달하지 못한다.

**확정 판단(리뷰가 요구한 「하나로 확정」):**
1. **모듈은 지운다.** WR-04 의 다른 선택지(「`listOrders` 를 실제로 결선」)는 곧 **주문 이력 표를 만드는 것**인데, 그 표는 D-20 이 「오늘 주문 이력 표는 미포함(deferred)」으로 이 phase 밖에 두었다. 새로고침 후 미체결·잔고 복원은 이미 wss 계좌 상태(66/67 스냅샷)가 한다 — `GET /api/orders` 가 메우는 구멍은 「이력 표」뿐이다. 없는 소비자를 위해 죽은 모듈을 남기면 다음 사람이 살아 있는 계약으로 읽는다.
2. **라우트는 남기고 읽기 계약은 완성한다.** `GET /api/orders?date=` 는 D-03 이 「오늘 주문 목록 복원용」으로 유지하기로 한 사용자 결정이라 지우지 않는다. 대신 `origin` 을 응답 계약에 실어 **이력 표가 만들어질 때 곧바로 쓸 수 있는 상태**로 둔다. 마이그레이션의 목적 문장을 재정의하지 않는다 — 목적은 유효하고, 미달성 구간은 「표가 아직 없다」뿐이다.

Purpose: 「있는데 안 쓰는 상태」를 없애고, 자동주문 감사가 API 층까지 도달하게 한다.
Output: `orders-api.ts` 삭제 · `errors.ts` 주석 정정 · `ORDER_COLS`/`DmaOrderRow` 에 `origin` · 계약 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-CONTEXT.md
</context>

<interfaces>
<!-- 바꿔야 하는 현재 계약. -->

`packages/shared/src/relay.ts:850` `DmaOrderRow` — 19필드. `origin` 이 없다.
`packages/shared/src/index.ts:45` 가 `DmaOrderRow` 를 재export 한다.
`server/src/services/dma-orders.ts:53-54` `ORDER_COLS` — 19컬럼 문자열. `origin` 이 없다.
`server/src/services/dma-orders.ts:56` `mapOrder(r: DmaOrderDbRow): DmaOrderRow` — snake→camel 매핑의 유일 지점.
`relay/src/dma/envelope.ts:1192` `export type OrderOriginKind = "manual" | "limit_chaser" | "vi";` — relay 쪽 동형 열거. **relay 를 import 하지 않는다**(server 는 relay 를 의존하지 않는다). shared 에 같은 값의 열거를 새로 둔다.
DB CHECK: `origin text NOT NULL DEFAULT 'manual' CHECK (origin IN ('manual','limit_chaser','vi'))`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DmaOrderRow·ORDER_COLS 에 origin 을 싣고 계약 테스트를 붙인다 (WR-05)</name>
  <files>packages/shared/src/relay.ts, server/src/services/dma-orders.ts, server/tests/routes/orders.test.ts</files>
  <read_first>
    - `packages/shared/src/relay.ts` L838~880 `DmaOrderRow` 전문(주석 포함) 과 그 위 `OrderMarket`/`OrderSide`/`DmaOrderStatus` 열거들의 선언 형식
    - `packages/shared/src/index.ts` L40~50 — 재export 목록의 형식
    - `server/src/services/dma-orders.ts` 전문 — 특히 L20~30(「★ **쓰기 함수가 없다**」 주석), `DmaOrderDbRow`(L30~50), `ORDER_COLS`(L52~54), `mapOrder`(L56~78), `listTodayOrders`(L85~100)
    - `server/tests/routes/orders.test.ts` — 케이스 ⑯-b(「bare array + camelCase 매핑」)가 Supabase 를 어떻게 스텁하고 무엇을 단언하는지. 새 단언이 그 케이스에 붙는다
    - `supabase/migrations/20260908120000_dma_orders_origin.sql` — CHECK 3종과 목적 문장(주석 근거로 인용한다)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §WR-05
  </read_first>
  <action>
① `packages/shared/src/relay.ts` 에 `export type DmaOrderOrigin = "manual" | "limit_chaser" | "vi";` 를 `DmaOrderRow` **바로 위**에 선언한다. JSDoc 에 「`dma_orders.origin` CHECK 3종과 같은 값이다. relay 의 `OrderOriginKind` 와 동형이지만 server 는 relay 를 의존하지 않으므로 사본을 둔다 — 값이 갈리면 CHECK 위반으로 드러난다」를 적는다.

② `DmaOrderRow` 에 `origin: DmaOrderOrigin;` 필드를 추가한다(`filledQty` 다음, `createdAt` 앞). JSDoc: 「발주 주체. `manual`=사용자가 직접 낸 주문 / `limit_chaser`=상따 자동주문 / `vi`=VI 자동주문. 구 게이트웨이가 빈 값을 보내면 relay 가 `manual` 로 좁힌다 — **「수동」과 「출처 불명」이 같은 값**이라는 사실을 표시에서 잊지 말 것.」

③ `packages/shared/src/index.ts` 의 타입 재export 목록에 `DmaOrderOrigin` 을 더한다.

④ `server/src/services/dma-orders.ts`:
  - `DmaOrderDbRow` 에 `origin: DmaOrderOrigin;` 추가(DB 는 NOT NULL 이므로 optional 이 아니다).
  - `ORDER_COLS` 문자열에 `,origin` 을 더한다. **`user_id` 는 계속 넣지 않는다** — 그 주석(「`user_id` 는 응답에 싣지 않는다」)을 유지한다.
  - `mapOrder` 에 `origin: r.origin,` 추가.
  - 파일 상단 「쓰기 함수가 없다」 주석 아래에 한 줄: 「`origin` 은 relay 만 쓴다(D-03). server 는 **읽기만** 한다 — 여기에 기본값 보정을 넣지 않는다. DB DEFAULT 가 이미 `'manual'` 이다.」

⑤ `server/tests/routes/orders.test.ts` 케이스 ⑯-b 를 확장한다. Supabase 스텁 픽스처 행에 `origin: "limit_chaser"` 를 넣고, 응답 원소에 `origin: "limit_chaser"` 가 실려 나오는지 단언한다. 추가 케이스 1건: 「조회 컬럼 목록에 `origin` 이 포함된다」 — 스텁이 받은 `select(...)` 인자 문자열에 `origin` 이 있음을 단언한다(`user_id` 가 **없음**도 함께 단언해 기존 규율을 회귀 잠금한다).
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/server exec vitest run tests/routes/orders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @gh-radar/server exec vitest run tests/routes/orders.test.ts` exit 0
    - `grep -c "origin" server/src/services/dma-orders.ts` >= 4 (타입 + ORDER_COLS + mapOrder + 주석)
    - `grep -c "user_id" server/src/services/dma-orders.ts` 에서 `ORDER_COLS` 문자열 안 0건 — `grep -n "ORDER_COLS" -A 2 server/src/services/dma-orders.ts` 출력에 `user_id` 없음
    - `grep -c "DmaOrderOrigin" packages/shared/src/relay.ts packages/shared/src/index.ts` 가 각 파일 1건 이상
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>`GET /api/orders` 응답에 `origin` 이 실리고 테스트가 그 왕복을 잠근다.</done>
</task>

<task type="auto">
  <name>Task 2: 죽은 주문 모듈 삭제 + server 주석의 거짓 문장 정정 (WR-04)</name>
  <files>webapp/src/lib/orders-api.ts, server/src/errors.ts</files>
  <read_first>
    - `webapp/src/lib/orders-api.ts` 전문(127줄) — 특히 L96 의 「새로고침 후 미체결·체결 목록을 복원하는 경로다」 약속과 `UNKNOWN_OUTCOME_CODES`·`isUnknownOutcome` 의 근거 주석. **지우기 전에 그 규율이 다른 곳에 살아 있는지 확인**한다
    - `webapp/src/components/orderbook/order-panel.tsx` L350~380 — `res.status === 'timeout'` 인라인 판정과 「catch 가 없는 것이 의도」 주석. WR-04 가 「실제 판정은 여기」라고 가리키는 자리다
    - `webapp/src/components/orderbook/account-panel.tsx` L255~270 — 같은 판정의 두 번째 사본
    - `server/src/errors.ts` L54~66 — Phase 16 Plan 16 주석 블록 전문
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-CONTEXT.md` D-03(「`GET /api/orders?date=` 조회 라우트는 유지」) · D-20(「오늘 주문 이력 표는 미포함(deferred)」)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §WR-04
  </read_first>
  <action>
① 삭제 전 **임포터 0건을 다시 확인**한다: `grep -rn "orders-api\|listOrders\|ORDER_ERROR_CODES\|isUnknownOutcome\|orderErrorCode\|orderErrorMessage" webapp/src webapp/e2e` 가 `webapp/src/lib/orders-api.ts` 자기 자신 외에 0건이어야 한다. 1건이라도 나오면 **삭제하지 말고 멈춘 뒤** SUMMARY 에 그 사용처를 적는다(전제가 깨진 것이다).

② `webapp/src/lib/orders-api.ts` 를 삭제한다(`git rm`). 이 모듈이 재export 하던 `DmaOrderRow` 는 shared 에서 직접 import 할 수 있으므로 별도 조치가 필요 없다.

③ `server/src/errors.ts` 의 Phase 16 Plan 16 주석 블록에서 마지막 문단(「웹앱의 `lib/orders-api.ts` 는 `ORDER_ERROR_CODES` 를 **그대로 유지**한다 …」)을 사실로 바꾼다. 새 문장이 담아야 할 것:
  - wss 결과 판정(「결과 모름 vs 거부」, Pitfall 9)은 브라우저 상수 테이블이 아니라 **`RelayOrderResultMsg.status`** 로 한다 — `order-panel.tsx` 와 `account-panel.tsx` 가 `status === 'timeout'` 을 인라인으로 본다(파일명 2개를 명시).
  - `webapp/src/lib/orders-api.ts` 는 16-20 에서 **삭제**했다(임포터 0건). 여기에 그 모듈을 가리키는 문장을 다시 만들지 말 것.
  - `GET /api/orders` 는 살아 있고(D-03), 브라우저 호출자는 **주문 이력 표가 생길 때** 만든다(D-20 deferred).

④ `webapp` 빌드가 삭제로 깨지지 않는지 확인한다: `pnpm --filter @gh-radar/webapp run typecheck` 와 `pnpm --filter @gh-radar/webapp test`.
  </action>
  <verify>
    <automated>test ! -f webapp/src/lib/orders-api.ts && pnpm --filter @gh-radar/webapp run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f webapp/src/lib/orders-api.ts` 참
    - `grep -rn "orders-api" webapp/src webapp/e2e` 0건
    - `grep -c "그대로 유지" server/src/errors.ts` == 0
    - `grep -c "order-panel\|account-panel" server/src/errors.ts` >= 1 — 새 주석이 실제 판정 자리를 가리킨다
    - `pnpm --filter @gh-radar/webapp test` exit 0 · `pnpm --filter @gh-radar/webapp run typecheck` exit 0
    - `pnpm --filter @gh-radar/server test` exit 0
  </acceptance_criteria>
  <done>죽은 모듈이 사라졌고, 그 모듈을 가리키던 거짓 주석이 실제 판정 자리를 가리킨다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 ↔ `GET /api/orders` | 본인 행만 나가야 한다 — `user_id` 는 응답에 싣지 않고 `WHERE` 로만 쓴다 |
| 계약 문서(주석) ↔ 실제 동작 | 거짓 주석은 다음 사람이 지우면 안 되는 것을 지우게 만든다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-22 | Information Disclosure | `server/src/services/dma-orders.ts` `ORDER_COLS` | mitigate | `origin` 만 추가하고 `user_id` 는 계속 제외. 테스트가 `select(...)` 인자에 `user_id` 가 **없음**을 단언 |
| T-16-23 | Repudiation | `dma_orders.origin` 읽기 경로 | mitigate | 응답 계약에 `origin` 을 실어 자동주문/수동주문 구분이 API 층까지 도달하게 한다. 표시 층(이력 표)은 D-20 deferred 임을 주석과 SUMMARY 에 명시 |
| T-16-24 | Tampering (오독) | `server/src/errors.ts` 주석 | mitigate | 거짓 문장을 삭제하고 실제 판정 자리(`order-panel.tsx`·`account-panel.tsx`)를 명시. acceptance 가 두 파일명 존재를 grep 으로 강제 |
| T-16-25 | Denial of Service (자기 발등) | `orders-api.ts` 삭제 | mitigate | 삭제 전 임포터 0건 재확인 게이트. 1건이라도 나오면 멈춘다 |
</threat_model>

<verification>
- `pnpm typecheck` (13 워크스페이스) exit 0
- `pnpm --filter @gh-radar/server test` · `pnpm --filter @gh-radar/webapp test` · `pnpm --filter @gh-radar/shared test` exit 0
- `grep -rn "orders-api" webapp/` 0건
</verification>

<success_criteria>
- `DmaOrderRow.origin` + `ORDER_COLS` 에 `origin` 이 있고 테스트가 왕복을 잠근다
- `webapp/src/lib/orders-api.ts` 가 없다
- `server/src/errors.ts` 주석이 사실과 맞는다
- 「이력 표는 D-20 deferred」가 근거로 기록됐다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-20-SUMMARY.md` when done
</output>
</content>
