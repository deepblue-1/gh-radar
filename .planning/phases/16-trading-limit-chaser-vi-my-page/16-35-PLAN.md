---
phase: 16-trading-limit-chaser-vi-my-page
plan: 35
type: execute
wave: 22
depends_on: ["16-27", "16-28", "16-29", "16-30", "16-31", "16-32", "16-33", "16-34"]
gap_closure: true
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md
  - .planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md
  - .planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md
autonomous: false
requirements: [TRADE-01, TRADE-02, TRADE-03, NAV-01, MYPAGE-01]

must_haves:
  truths:
    - "전체 스위트(typecheck 13 워크스페이스 · `typecheck:tests` · `pnpm -r test` · `pnpm build` · Playwright 전량)가 2라운드 갭 수정 후에도 green 이다 — 8개 plan 의 변경이 서로를 깨지 않았다"
    - "relay·server·webapp 3종이 2라운드 수정본으로 재배포됐다 (또는 사용자가 보류를 택했고 그 사실과 사유가 STATE 에 남았다)"
    - "프로덕션 `https://dma.jx1.io/healthz` 를 **직접 쳐서** 상태코드와 본문을 실측했고, `version` 이 이번 커밋 해시와 일치한다"
    - "`scripts/smoke-relay.sh` 를 실행했고 INV-9 의 결과(PASS / 토큰 미설정 SKIP)가 기록된다 — GC-WR-11 수정 후에도 판정이 강등되지 않음을 확인한다"
    - "닫힌 19건(GC-CR-01~03 · GC-WR-01~12 · GC-IN-01~04)과 **여전히 남은 항목**이 VALIDATION·STATE·ROADMAP·REQUIREMENTS·deferred-items·16-REVIEW 에 정직하게 반영된다 — TRADE-03 은 D-27 상 실서버 결선 전까지 **Pending 유지**이며 어디에도 과장이 없다"
  artifacts:
    - path: ".planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md"
      provides: "2라운드 갭 클로징 19건 처리 표 + 배포 실측"
      contains: "GC-CR-01"
    - path: ".planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md"
      provides: "GC- 항목별 종결 표시"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TRADE-03 재판정(Pending 유지 근거 갱신)"
  key_links:
    - from: "scripts/deploy-relay.sh"
      to: "GCE VM radar-gw"
      via: "재배포 후 /healthz version 대조"
      pattern: "deploy-relay"
---

<objective>
2라운드 갭 클로징 8개 plan(16-27 ~ 16-34)의 결과를 **프로덕션까지 밀어 넣고 실측**한 뒤, 문서를 정직하게 갱신한다.

배포가 필요한 이유:
- **relay** — 16-27(하드 필터·await 가드) · 16-28(`23505` 수렴) · 16-29(`crud` 인지 시장 해석·무장 가드) · 16-30(`stalledCount` 판정) · 16-33(수동 통보 조회 경유·`.catch`·빈 주문번호) · 16-34(매매구분 축·취소 dup 키)가 전부 relay 코드다. 그중 **16-30 은 배포해야 의미가 생긴다**(현재 프로덕션 판정에는 `stalledCount` 가 없다).
- **webapp** — 16-31(상따 폼 가드·문구) · 16-32(VI/My page send 분기·시각 비교)가 사용자 표면이다.
- **server** — 이번 라운드는 server 코드를 바꾸지 않았다. 그래도 배포 순서 규율상 **바뀐 것이 없음을 확인**하고 넘어간다(불필요한 재배포는 하지 않되, 건너뛴 사실을 기록한다).
- **DB** — 이번 라운드는 마이그레이션이 없다. `supabase db push` 를 부르지 않는다.

**과장 금지의 정본.** 이번 라운드로 바뀌는 것은 ① 코드 결함 19건 ② mock·단위 검증 ③ 배포 ④ `/healthz` 판정 강화다. **바뀌지 않는 것:** DMA 게이트웨이는 여전히 뜨지 않았고(D-27), WinForms ↔ 웹 세션 공유는 여전히 한 번도 실행된 적이 없으며, 그래서 **TRADE-03 은 Pending 을 유지한다.**

Purpose: 코드에서 닫은 것을 운영에서도 닫고, 문서가 다음 검증의 기준선을 오염시키지 않게 한다.
Output: 전체 green · 배포 2~3종 · `/healthz` 실측 · smoke · 문서 6종 갱신.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 스위트 green 확인 (배포 전 게이트)</name>
  <files>(코드 변경 없음 — 실패 시에만 원인 파일을 고친다)</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-27-SUMMARY.md` ~ `16-34-SUMMARY.md` 8건 — 각 plan 이 무엇을 바꿨고 어떤 테스트를 추가했는지. 실패가 나면 **어느 plan 의 경계인지** 여기서 판별한다
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-26-PLAN.md` Task 1 + `16-26-SUMMARY.md` — 직전 라운드의 전체 스위트 실행 절차와 **실측 기준선**(typecheck 13 워크스페이스 exit 0 · `pnpm -r test` 1,970 pass / 1 skip / 6 todo · 189 파일 · `pnpm build` exit 0 · Playwright 단일 워커 5~8분)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md` — 선행 실패로 알려진 항목이 남아 있는지
  </read_first>
  <action>
순서대로 돌린다. 앞 단계가 실패하면 뒤를 돌리지 않는다.
1. `pnpm typecheck` — 13 워크스페이스. 16-30 이 `SessionStats` 를 바꿨으므로 relay 소비자·테스트 스텁이 컴파일 단계에서 걸릴 수 있다(그것이 그 설계의 의도다).
2. `pnpm --filter @gh-radar/relay run typecheck:tests` — relay `tests/` 는 루트 typecheck 밖이다(deferred-items 기록).
3. `pnpm -r test` — shared / relay / server / webapp 전부. 기준선은 **1,970 pass**였고 8개 plan 이 케이스를 늘렸으므로 **그보다 커야 한다**. 실제 수(pass/skip/todo/파일 수)를 SUMMARY 에 인용한다.
4. `pnpm build` — exit 0.
5. `pnpm --filter gh-radar-webapp test:e2e` — Playwright 전량, 단일 워커. 기준선은 126 통과 · 0 실패 · 9 skip. 16-31 이 문구를 바꿨으므로 `trading-limit-chaser.spec.ts` 의 문구 단언이 걸릴 수 있다 — 그 경우 **e2e 를 새 문구로 갱신**한다(문구가 옳아진 것이지 회귀가 아니다. 그 판단 근거를 SUMMARY 에 적는다).

실패가 나면 **고치고 다시 돌린다.** 배포 전에 red 를 남기지 않는다. 고칠 때 **어느 plan 의 경계인지 판별**해 그 파일만 손댄다. 이 phase 의 변경과 무관한 선행 실패라고 판단되면 고치지 말고 `deferred-items.md` 에 근거(재현 방법 + 무관함의 증거)와 함께 기록한다.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm --filter @gh-radar/relay run typecheck:tests &amp;&amp; pnpm -r test &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exit 0 (13 워크스페이스)
    - `pnpm --filter @gh-radar/relay run typecheck:tests` exit 0
    - `pnpm -r test` exit 0, 총 통과 수가 **1,970 초과** (실제 수를 SUMMARY 에 인용)
    - `pnpm build` exit 0
    - `pnpm --filter gh-radar-webapp test:e2e` exit 0, 실패 0 (통과/skip 수를 SUMMARY 에 인용)
    - `grep -rn "10\.41\.1\.120" relay/ webapp/src webapp/e2e scripts/` 0건 (D-27 승계 게이트)
  </acceptance_criteria>
  <done>배포해도 되는 상태임이 다섯 개 명령의 exit 0 으로 증명됐다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 배포 승인·실행 + `/healthz` 실측 + smoke</name>
  <files>(배포 실행 — 저장소 파일 변경 없음)</files>
  <read_first>
    - `scripts/deploy-relay.sh` — GCE VM `radar-gw` 컨테이너 재배포 절차, `HEALTH_HOST=dma.jx1.io`, 배포 후 `/healthz` 대기 구간, uptime check 재생성 경로
    - `scripts/deploy-server.sh` — Cloud Run 배포 인자와 필수 env(`GCP_PROJECT_ID`·`SUPABASE_URL`)
    - `scripts/smoke-relay.sh` — **16-30 이 방금 고친 INV-9**(env 토큰 · verdict 덮어쓰기)
    - `scripts/smoke-server.sh` — server 계약 불변식
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-26-SUMMARY.md` — 직전 배포 3종의 실제 명령·결과(리비전 이름·버전 해시 형식·Vercel 수동 배포 절차)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-30-SUMMARY.md` — `stalledCount` 판정의 정확한 조건(무엇이 200 이고 무엇이 503 인지). **배포 후 실측을 해석하는 기준**이다
  </read_first>
  <what-built>
2라운드 갭 클로징 8개 plan 의 코드가 전부 병합됐고 전체 스위트가 green 이다. 닫은 것: GC-CR-01·02·03(주문 오귀속 · 수동 통보 0행 소실 · await TOCTOU) · GC-WR-01~12 · GC-IN-01~04 — **19건 전부**.

배포 대상과 이유:
1. **relay** (GCE VM `radar-gw`) — 16-27·28·29·30·33·34. **16-30 은 배포해야 효과가 생긴다**(`stalledCount` 가 판정에 들어간다).
2. **webapp** (Vercel) — 16-31·32 의 사용자 표면(상따 「수정」 가드·안내 문구·VI 확인 잠금·My page 시각).
3. **server** (Cloud Run) — 이번 라운드 변경 **없음**. `git diff` 로 `server/` 무변경을 확인하고 재배포를 건너뛴다(건너뛴 사실을 기록한다).

DB 마이그레이션은 이번 라운드에 없다.
  </what-built>
  <how-to-verify>
1. **배포를 Claude 가 실행해도 되는지** 알려 주세요 (승인 / 직접 실행 / 보류).

2. 승인 시 Claude 가 실행할 명령 (순서 고정: relay → webapp):
   - relay: `GOOGLE_APPLICATION_CREDENTIALS=~/.config/gcloud/gh-radar-deployer.json CLOUDSDK_CORE_PROJECT=gh-radar bash scripts/deploy-relay.sh`
   - webapp: 저장소 **루트**에서 `vercel pull` → `vercel build` → `vercel deploy --prebuilt`
     (`ignoreCommand` 때문에 git push 배포가 skip 된 전례가 있어 수동 배포합니다)
   - server 는 변경이 없어 건너뜁니다. 그래도 배포하길 원하시면 알려 주세요.

3. **배포 후 `/healthz` 확인 — 두 단계입니다.**
   - ① 배포 직후: `curl -s https://dma.jx1.io/healthz` → 200. 본문의 `version` 이 이번 커밋 해시여야 합니다.
   - ② **로그인한 브라우저로 앱에 접속**해 relay 세션이 생긴 뒤 다시 칩니다.
     16-30 의 새 판정 때문에 **여기서 갈립니다**: 세션이 생긴 직후(`STALE_SESSION_MS` 유예 안)는 `200 {"status":"ok", …, "stalledCount":0}` 이고, **그 유예를 넘겨도 Ready 가 안 되면 503 degraded** 로 바뀝니다. 이것이 GC-WR-07 의 의도된 동작입니다 — 게이트웨이가 없는 상태를 영원히 초록으로 보고하지 않겠다는 것입니다.
     ⚠️ 그래서 **Cloud Monitoring 알림이 다시 울릴 수 있습니다.** 그 알림이 「게이트웨이가 뜨지 않았다」는 사실을 정확히 말하는 것이므로 정상입니다. 이 동작을 원치 않으시면 알려 주세요 — 유예 시간을 늘리거나 판정을 되돌리는 선택지를 제시하겠습니다.
   - 두 번 다 본문 전체를 알려 주세요.

4. smoke: `bash scripts/smoke-relay.sh` · `bash scripts/smoke-server.sh`
   - `SMOKE_AUTH_TOKEN` 이 없으면 INV-9 는 SKIP 입니다(FAIL 아님).
   - **토큰을 주시면 16-21 이 재작성하고 16-30 이 고친 프로브의 첫 실행이 됩니다.** 브라우저 localStorage 의 `access_token` 이고 약 1시간 만료입니다. 프로브는 화이트리스트 밖 계좌번호 + 미해석 ISIN 을 쓰므로 **실계좌에 주문이 나가지 않습니다**.

**바뀌지 않는 것(과장 방지):** 이 배포로 DMA 게이트웨이가 살아나지 않습니다. WinForms ↔ 웹 세션 공유는 여전히 미검증이고 TRADE-03 은 Pending 을 유지합니다.
  </how-to-verify>
  <resume-signal>「배포 승인」(Claude 실행) / 「직접 실행」(사용자 실행 후 결과 공유) / 「보류」 중 하나. 3-①②의 `/healthz` 본문과, 토큰을 주실지 여부를 함께 알려 주세요.</resume-signal>
  <action>
사용자가 「배포 승인」이면 relay → webapp 순서로 실행하고, 각 단계 결과를 확인한 뒤 다음으로 넘어간다(앞 단계 실패 시 멈춘다).
- `git diff --stat <직전 배포 커밋>..HEAD -- server/` 로 server 무변경을 **확인하고 그 출력을 SUMMARY 에 인용**한다. 변경이 있으면 server 도 배포한다.
- relay 배포 후 `deploy-relay.sh` 출력의 `version` 해시가 이번 커밋과 일치하는지 확인한다.
- webapp 배포 후 프로덕션 HTML 에서 Phase 16 사이드바 문자열(「상승률 상위」)이 검출되는지 확인한다(회귀 없음).
- `/healthz` 를 **①·② 두 번** 재고 본문 전체를 SUMMARY 에 인용한다. ② 가 503 이면 **본문의 `stalledCount`·`everReadyCount`·`readyCount` 로 그것이 16-30 의 의도된 판정인지 아닌지를 가려 적는다** — 「배포가 실패했다」와 「판정이 옳게 울렸다」를 뭉개지 않는다.
- smoke 2종을 돌리고 PASS/SKIP/FAIL 요약을 인용한다. 토큰을 받았다면 INV-9 의 판정 문자열 원문을 인용한다(**토큰 자체는 어디에도 기록하지 않는다**).
- Cloud Monitoring `gh-radar-relay-down` 알림 상태를 확인해 기록한다.

「직접 실행」이면 사용자가 공유한 출력을 그대로 인용한다. 「보류」면 배포를 건너뛰고 **그 사실과 사유**를 SUMMARY·STATE 에 남긴다 — 미배포를 배포된 것처럼 적지 않는다.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}' https://dma.jx1.io/healthz</automated>
  </verify>
  <acceptance_criteria>
    - 배포 승인 시: relay `/healthz` 본문의 `version` 이 이번 갭 클로징 커밋 해시와 일치 (본문 전체 SUMMARY 인용)
    - 세션이 있는 상태의 `/healthz` 본문에 `stalledCount` 필드가 실재 — 16-30 이 프로덕션에 반영됐다는 1차 증거
    - ② 의 상태코드가 200 이든 503 이든, **그 값이 16-30 판정의 어느 갈래인지**가 본문 필드로 설명됨 (SUMMARY 에 판정 근거 문장)
    - webapp 프로덕션 HTML 에 「상승률 상위」 검출
    - `git diff --stat` 로 server 무변경 확인 출력이 SUMMARY 에 인용됨 (또는 배포 사실 기록)
    - `bash scripts/smoke-relay.sh` · `bash scripts/smoke-server.sh` 결과 요약이 SUMMARY 에 인용됨. INV-9 판정 문자열이 `reachable`/`unreachable`/`inconclusive` **셋 중 하나의 정확한 값** (이어 붙은 문자열이 아님 = GC-WR-11 실증)
    - SUMMARY·문서 어디에도 `SMOKE_AUTH_TOKEN` 값이 기록되지 않음
    - 보류 시: 미배포 사실과 사유가 STATE.md 에 기록됨
  </acceptance_criteria>
  <done>프로덕션이 2라운드 수정본으로 돌고 있고, `/healthz` 판정과 smoke 판정이 실측으로 확인됐다.</done>
</task>

<task type="auto">
  <name>Task 3: 문서 정직 갱신 — VALIDATION · REVIEW · STATE · ROADMAP · REQUIREMENTS · deferred-items</name>
  <files>.planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md, .planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md` — `gaps` 3건(갭 3 은 커밋 `1ba7400` 에서 이미 닫혔다) · §Gaps Summary · §Requirements Coverage · §Human Verification Required 6건
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §「갭 클로징 재리뷰 (16-18 ~ 16-26)」 전문 — GC-CR-01~03 · GC-WR-01~12 · GC-IN-01~04 의 제목과 File 줄. **항목별로 종결 표시를 다는 대상**이다
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md` — frontmatter 형식 · §Gap Closure (16-18~16-26) 표 형식 · §Manual-Only · §Deployment Verification
    - `.planning/REQUIREMENTS.md` L95~102(요구사항 목록 체크박스 — TRADE-03 만 `- [ ]`) · L170~180(Traceability 표 + `*Last updated:*` 형식)
    - `.planning/ROADMAP.md` L596~ Phase 16 절 — `**Plans:** 26/26 plans complete` 와 Wave 목록 형식. **상단 phase 목록도 함께 갱신 대상인지 확인한다**(append 만 하고 상단을 놓치는 함정이 이 저장소에 있다)
    - `.planning/STATE.md` — frontmatter `progress`·`stopped_at`·`last_activity` 형식과 Current Position 절
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md` — 마지막 절 형식과 기존 항목(IN-02~07 · UI 후속 · 실서버 검증)
    - 16-27 ~ 16-34 의 SUMMARY 8건 — **무엇이 실제로 닫혔는지의 정본.** SUMMARY 주장과 코드가 어긋나면 코드를 따른다
  </read_first>
  <action>
① `16-VALIDATION.md` 에 새 절 `## Gap Closure 2라운드 (16-27 ~ 16-35)` 를 추가한다. 표 하나로 **19건 전부**를 적는다: 항목 ID(GC-CR-01·02·03 / GC-WR-01~12 / GC-IN-01~04) / 담당 plan / 무엇을 바꿨는가 / 회귀를 잠근 테스트 명령. §Deployment Verification 을 이번 배포 실측으로 갱신하고, §Manual-Only 표는 유지한다(실서버·실계좌는 여전히 D-27 상 미실시).

② `16-REVIEW.md` 의 §갭 클로징 재리뷰 안에서 **항목별로 종결 표시**를 단다. 각 `#### GC-xx-nn:` 제목 아래 또는 절 끝에 「**종결:** 16-NN (요약 한 줄) — <테스트/실측 근거>」 형식으로 남긴다. 원문 Issue·Fix 는 **지우지 않는다**(다음 사람이 근거를 되짚을 수 있어야 한다). 절 머리말의 상태 줄(`issues_found — Critical 3 · Warning 12 · Info 4`)에 「2라운드 갭 클로징(16-27~16-35)에서 19건 전부 종결 — 상세는 항목별 표시 참조」를 잇는다.

③ `.planning/ROADMAP.md` Phase 16 절을 갱신한다. `**Plans:** 26/26` → `35/35 plans complete (17 실행 + 갭 클로징 1라운드 9 + 2라운드 9)`. Wave 19~22 항목을 기존 형식대로 추가하고 완료 표시한다. Goal 문장은 바꾸지 않는다. **상단 phase 목록에 plan 수가 적혀 있으면 그것도 함께 고친다.**

④ `.planning/REQUIREMENTS.md` 를 갱신한다.
  - **TRADE-01·02·NAV-01·MYPAGE-01** — 이미 `- [x]` + Complete 다. 체크박스와 Traceability 가 계속 일치하는지 **확인만** 하고 불필요하게 건드리지 않는다.
  - **TRADE-03** — **Pending 을 유지한다.** Traceability 행의 근거 문장을 이번 라운드로 갱신한다: 코드 층위에서 GC-CR-01·02·03 을 포함한 19건이 닫혔다는 사실 + **여전히 프로덕션에서 이 경로가 한 프레임도 나른 적이 없다**는 사실(이번 `/healthz` 실측값을 인용) + D-27 상 실서버 결선은 사용자 명시 지시 사항이라는 사실. **올리는 쪽으로 반올림하지 않는다** — RELAY-02 와 같은 기준이다.
  - `*Last updated:*` 줄에 이번 갱신 사유를 기존 형식대로 덧붙인다.

⑤ `.planning/STATE.md` 를 갱신한다. `stopped_at`·`last_activity`·`progress`(총 plan 수 165 → 이번 8개 plan 만큼 증가, 완료 수도 함께)를 실제 값으로 맞춘다. 배포를 보류했다면 **미배포 사실과 사유**를 함께 남긴다.

⑥ `deferred-items.md` 에 새 절을 추가한다. **여전히 남는 것**을 정직하게 적는다:
  - 1라운드가 범위 밖으로 둔 Info 6건(IN-02~07)의 현재 상태 — 이번 라운드에서 GC-IN-01~04 는 닫혔으므로 **혼동하지 않게 네임스페이스를 구분해 적는다**.
  - 인간 검증 6건(16-VERIFICATION §Human Verification Required) — 특히 #1(WinForms 세션 공유)·#2(INV-9 첫 실행)의 현재 상태를 이번 Task 2 결과로 갱신한다. INV-9 를 실제로 돌렸다면 「돌렸고 결과가 X」로, 못 돌렸다면 「여전히 못 돌렸다(토큰 부재)」로 — **「돌렸는데 SKIP」과 「못 돌렸다」를 섞지 않는다.**
  - 이번 라운드가 **새로 미루기로 한 것이 있으면** 여기에 추가한다(예: 16-28 Task 2 에서 상수 식을 바꾸지 않기로 했다면 그 근거, 16-29 의 삭제 폴백 선택에서 남은 잔여 위험 등). 없으면 「이번 라운드가 새로 미룬 항목: 없음」이라고 명시한다 — 빈 칸을 남기지 않는다.

**과장 금지 게이트:** 어느 문서에도 「게이트웨이가 살아났다」·「WinForms 와의 세션 공유가 검증됐다」·「TRADE-03 완료」를 쓰지 않는다. 이번에 확인된 것은 ① 코드 수정 19건 ② mock·단위 검증 ③ 배포 ④ `/healthz`·smoke 실측뿐이다.
  </action>
  <verify>
    <automated>grep -c "GC-CR-01" .planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `16-VALIDATION.md` 의 2라운드 표에 **19행**(GC-CR-01·02·03 · GC-WR-01~12 · GC-IN-01~04)이 모두 있다 — 각 ID 를 `grep -o "GC-[A-Z]*-[0-9]*" | sort -u | wc -l` 로 세어 19 임을 SUMMARY 에 인용
    - `grep -c "종결:" .planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` >= 19
    - `grep -c "35/35" .planning/ROADMAP.md` >= 1
    - `.planning/REQUIREMENTS.md` 의 TRADE-03 이 **여전히 Pending** 이고(`grep -c "TRADE-03 | Phase 16 | Pending" .planning/REQUIREMENTS.md` 또는 동등 확인) 근거 문장에 이번 `/healthz` 실측값이 인용돼 있다
    - `.planning/REQUIREMENTS.md` L95~102 체크박스와 Traceability 표가 5개 ID 전부에서 서로 일치 (전후 대조 결과를 SUMMARY 에 인용)
    - `grep -rc "게이트웨이가 살아" .planning/` 합계 0 — 과장 문구 없음
    - `deferred-items.md` 새 절에 인간 검증 #1·#2 의 갱신된 상태와 「이번 라운드가 새로 미룬 항목」이 명시돼 있다
    - `.planning/STATE.md` 의 `progress` 총 plan 수·완료 수가 실제 파일 수와 일치 (`ls .planning/phases/*/*-PLAN.md | wc -l` 결과를 SUMMARY 에 인용)
  </acceptance_criteria>
  <done>닫힌 19건과 남은 것이 문서 6종에 사실대로 적혀 있고, 어디에도 과장이 없다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 개발 머신 → GCE VM `radar-gw` / Vercel | 배포 자격증명이 프로덕션을 바꾼다 |
| 문서 기록 ↔ 실제 운영 상태 | 과장된 기록은 다음 검증의 기준선을 오염시킨다 |
| smoke 프로브 → 프로덕션 relay wss | 실계좌 주문이 나갈 수 있는 유일한 경로 |
| `SMOKE_AUTH_TOKEN` → 로그·SUMMARY | 사용자 액세스 토큰이 기록에 남으면 그 자체가 유출이다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-72 | Elevation of Privilege | 배포 실행 | mitigate | `checkpoint:human-verify` blocking — 사용자 승인 없이 배포하지 않는다. GCP 자격증명은 전용 배포 SA(`gh-radar-deployer.json`)만 |
| T-16-73 | Repudiation | 문서 갱신 | mitigate | 과장 문구 grep 게이트 0건. TRADE-03 Pending 유지. 배포 보류 시 미배포 사실을 STATE 에 남긴다 |
| T-16-74 | Information Disclosure | `SMOKE_AUTH_TOKEN` | mitigate | 토큰 값을 SUMMARY·로그·문서 어디에도 기록하지 않는다. 판정 문자열만 인용 |
| T-16-75 | 오탐/과탐 해석 | 배포 후 `/healthz` 503 | mitigate | 503 이 나오면 본문 필드(`stalledCount`·`everReadyCount`·`readyCount`)로 **의도된 판정인지 장애인지**를 가려 적는다. 뭉개지 않는다 |
| T-16-28 | 운영 사고 (실주문) | smoke INV-9 | mitigate | 화이트리스트 밖 계좌번호 + 미해석 ISIN — 게이트웨이 송신·DB insert 이전에 끝난다 (16-21 승계) |
| T-16-13 | 운영 사고 | 실서버·실계좌 결선 | accept | 이번에도 하지 않는다 (D-27). VALIDATION §Manual-Only 유지 |
</threat_model>

<verification>
- `pnpm typecheck` · `pnpm --filter @gh-radar/relay run typecheck:tests` · `pnpm -r test` · `pnpm build` · `pnpm --filter gh-radar-webapp test:e2e` 전부 exit 0
- `curl -s -w '%{http_code}' https://dma.jx1.io/healthz` 본문·상태코드 실측 인용
- `bash scripts/smoke-relay.sh` · `bash scripts/smoke-server.sh` 결과 인용
- `grep -rn "10\.41\.1\.120" relay/ webapp/ scripts/` 0건
</verification>

<success_criteria>
- 전체 스위트 green (기준선 초과)
- 배포 완료 또는 보류 사실 기록
- `/healthz`·smoke 실측이 기록되고 판정 근거가 설명됨
- 19건 처리 결과가 VALIDATION 표와 REVIEW 항목별 표시에 전부 있음
- TRADE-03 이 Pending 을 유지하고 근거가 갱신됨
- 문서에 과장 문구 0건
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-35-SUMMARY.md` when done
</output>
