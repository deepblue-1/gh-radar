---
phase: 16-trading-limit-chaser-vi-my-page
plan: 34
type: execute
wave: 21
depends_on: ["16-33"]
gap_closure: true
files_modified:
  - relay/src/ws/order-handler.ts
  - relay/tests/ws-order.test.ts
autonomous: true
requirements: [TRADE-03]

must_haves:
  truths:
    - "같은 종목·같은 수량·같은 가격의 **매수와 매도가 동시에 대기**해도 `sideTrusted` 인 통보는 옳은 쪽을 정산한다 — 두 주문 모두 「결과를 확인하지 못했습니다」로 끝나지 않는다 (GC-WR-03)"
    - "취소·정정 통보에는 매매구분 축을 적용하지 않는다 — `sideTrusted === false` 인 통보가 축을 잘못 걸어 정상 매칭을 깨뜨리지 않는다 (Pitfall 8 유지)"
    - "같은 종목·같은 가격·같은 잔량의 미체결 2건을 **연달아 취소할 수 있다** — 취소 중복 키가 원주문번호로 갈린다 (GC-WR-10). 급락 국면의 일괄 취소가 막히지 않는다"
    - "신규 주문의 중복 판정은 지금과 동일하다 — 두 탭 동시 발주 차단(16-22 truth 25)이 그대로다"
  artifacts:
    - path: "relay/src/ws/order-handler.ts"
      provides: "PendingOrder.side 매칭 축 + 취소 전용 dup 키"
      contains: "sideTrusted"
    - path: "relay/tests/ws-order.test.ts"
      provides: "매수/매도 동시 대기 정산 · 연속 취소 허용 · 신규 중복 차단 유지 케이스"
  key_links:
    - from: "relay/src/ws/order-handler.ts (narrowPending)"
      to: "relay/src/dma/envelope.ts (ParsedOrderResp.sideTrusted)"
      via: "믿을 수 있을 때만 매매구분 축을 적용"
      pattern: "sideTrusted"
    - from: "relay/src/ws/order-handler.ts (dupKey)"
      to: "취소 요청의 orgOrderNo"
      via: "취소 키의 정체성을 원주문번호로 바꾼다"
      pattern: "orgOrderNo"
---

<objective>
**GC-WR-03 + GC-WR-10** 을 닫는다. 둘 다 「relay 가 이미 가지고 있는 식별 정보를 쓰지 않아서 정상 조작이 막히거나 결과를 잃는다」는 모양이다.

지금 상태:
- **GC-WR-03.** `ParsedOrderResp` 는 `side` 와 「믿어도 되는가」를 말하는 `sideTrusted` 를 **둘 다** 싣는다(`envelope.ts:1183-1192`). 그런데 `narrowPending` 은 이 축을 전혀 쓰지 않는다. 테스트 ㉑ 이 고정한 「매수 10@70000 + 매도 10@70000」은 `sideTrusted === true` 인 접수 통보 하나로 **완전히 가를 수 있는데도** 좁히기에 실패해 두 주문 모두 `timeout` 으로 끝난다 — 사용자는 실제로 접수된 두 주문에 대해 「결과를 확인하지 못했습니다」를 본다.
- **GC-WR-10.** `dupKey`(`:215-218`)는 `dup:${accountNo}|${isin}|${side}|${price}|${qty}` 이고 취소는 `side` 자리에 `"C"` 를 넣는다. **취소 요청의 정체성은 `orgOrderNo`** 인데 키에 없다. 같은 종목·같은 가격·같은 잔량의 미체결이 2건 있으면(다른 단말·전일 잔여·자동주문으로 흔히 생긴다) 두 번째 취소가 「같은 주문이 이미 처리 중입니다」로 **최대 5초 거부**된다. 급락 국면에서 미체결 일괄 취소가 막히는 것은 자산 위험이다.

**16-22 가 세운 두 규율을 깨지 않는다:** ① `refine` 은 남는 후보가 0이면 그 축을 건너뛴다, ② dup 키는 사용자 스코프이고 `rid` 키는 연결 스코프다.

Purpose: TRADE-03 의 5초 상관이 **가를 수 있는 것을 가르고**, 중복 가드가 **막지 말아야 할 것을 막지 않게** 한다.
Output: `PendingOrder.side` + `sideTrusted` 축 · 취소 전용 dup 키 · 회귀 테스트 3케이스.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-22-SUMMARY.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-33-SUMMARY.md
</context>

<interfaces>
<!-- 16-27·16-33 이 같은 파일을 먼저 손댔다. 두 SUMMARY 를 읽고 최신 줄번호를 다시 잡을 것. -->

`relay/src/ws/order-handler.ts`:
- `export type PendingOrder = { rid; orderRowId; isin; qty; price; isCancel; orgOrderNo; timer; settle }` — **`side` 가 없다**. 타입 주석이 「이 값들은 통보 매칭 축이다. 저장만 하고 읽지 않으면 gap 2 가 재발한다」라고 적어 둔 자리
- `handle` 안: `const side = isCancel ? "S" : msg.side;` — 취소 행의 `side` 는 CHECK 를 만족시키기 위한 표기이지 **방향의 정본이 아니다**(그 이유가 insert 호출부 주석에 있다)
- `function dupKey(msg): string` — 사용자 스코프 키(`userDupKeys`). docstring 이 「중복 체결이 이 파일 최악의 결과이므로 이 판정만 사용자 축으로 올린다」
- `function ridKey(msg): string` · `claimKeys(msg): ClaimKeys` — 두 키를 한 쌍으로 다루는 관례
- `narrowPending` — 16-27 이 하드 필터를 앞에 붙였고, 그 뒤 ①~④ 단계적 좁히기(`refine`)가 이어진다

`relay/src/dma/envelope.ts` `ParsedOrderResp`:
- `side: string` · `sideTrusted: boolean` — 취소·정정 통보의 매매구분은 믿을 수 없다(Pitfall 8). `side` 는 `"B"`/`"S"` 계열 원문이 올 수 있어 `startsWith` 판정이 필요하다

`packages/shared/src/relay.ts` — `RelayOrderCancelMsg.orgOrderNo`(필수) · `RelayOrderNewMsg.side`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 매매구분을 매칭 축으로 쓴다 — 믿을 수 있을 때만 (GC-WR-03)</name>
  <files>relay/src/ws/order-handler.ts, relay/tests/ws-order.test.ts</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-33-SUMMARY.md` · `16-27-SUMMARY.md` — **먼저 읽는다.** 같은 파일의 최신 상태(하드 필터 · await 가드 · manual 분기 조회)
    - `relay/src/ws/order-handler.ts` `PendingOrder` 선언과 매칭 축 주석, `handle` 의 `const side` 계산부와 insert 호출부의 「취소 행의 side 를 표시에 쓰지 말 것」 주석, `entry` 생성부, `narrowPending` 의 ①~④ 축과 `refine` 규율
    - `relay/src/dma/envelope.ts` L1150~1195 — `ParsedOrderResp` 전문. 특히 `side`·`sideTrusted` 의 「언제 믿을 수 있는가」 주석
    - `relay/tests/ws-order.test.ts` 의 테스트 ㉑ (「같은 ISIN·같은 수량·같은 가격의 신규 2건은 좁혀지지 않는다」) — **이 케이스가 매수/매도 조합이면 이제 좁혀져야 한다.** 어떤 조합인지 확인하고, 좁혀지지 않아야 하는 케이스(같은 방향 2건)를 따로 남긴다
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §GC-WR-03 — Fix 스니펫(②-1 축)
  </read_first>
  <action>
① `PendingOrder` 에 `side` 를 추가한다. 값은 **취소가 아닌 요청의 원래 방향**이다 — `handle` 의 `const side = isCancel ? "S" : msg.side` 는 DB CHECK 용 표기이므로 그 값을 그대로 쓰면 취소 대기가 매도로 보인다. 취소 대기의 `side` 를 어떻게 둘지(예: 빈 문자열 또는 `msg.side` 부재를 드러내는 값) 정하고, **타입 주석에 「취소 대기의 `side` 는 방향의 정본이 아니다 — 그래서 ②-1 축은 `!p.isCancel` 인 후보에만 적용한다」**를 적는다.

② `narrowPending` 의 ② 통보 종류 축 **뒤에** ②-1 매매구분 축을 넣는다. 16-REVIEW §GC-WR-03 의 Fix 스니펫을 참조하라:
  - `n.sideTrusted && n.side !== ""` 일 때만 적용한다. `sideTrusted === false`(취소·정정 통보)면 **건너뛴다** — Pitfall 8.
  - 통보의 `side` 를 `"B"`/`"S"` 로 정규화한다(원문이 `"S"` 계열 접두일 수 있으므로 `startsWith` 판정. `envelope.ts` 의 실제 값 집합을 확인하고 그 근거를 주석에 인용한다).
  - `refine((p) => !p.isCancel && p.side === noticeSide)` — **`refine` 이므로 남는 후보가 0이면 축을 건너뛴다.** 하드 필터가 아니다(구 게이트웨이가 `sideTrusted` 를 못 주는 경우를 이미 앞의 조건이 거른다).

③ 축의 순서를 근거와 함께 적는다: 원주문번호(하드) → 통보 종류 → **매매구분** → 수량 → 가격. 매매구분이 수량·가격보다 강한 이유(체결 통보에서 수량·가격은 변하지만 방향은 변하지 않는다)를 한 줄로 남긴다.

④ 테스트를 추가·조정한다.
  - 「매수 10@70000 과 매도 10@70000 이 동시 대기 중일 때, `sideTrusted` 인 접수 통보는 그 방향의 대기만 정산한다」 — 반대쪽 대기가 `pending` 에 남아 있음과 `order.result` 가 한 건만 나갔음을 단언한다.
  - 「`sideTrusted === false` 인 취소확인 통보는 매매구분 축을 쓰지 않는다」 — 축이 잘못 걸려 정상 매칭이 깨지지 않음을 잠근다.
  - 기존 테스트 ㉑ 이 매수/매도 조합이면, **좁혀지지 않아야 하는 케이스**를 같은 방향 2건(매수 10@70000 두 건)으로 바꿔 「좁히지 못하면 아무것도 정산하지 않는다」 규율을 계속 잠근다. 케이스 이름도 그에 맞게 고친다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay exec vitest run tests/ws-order.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sideTrusted" relay/src/ws/order-handler.ts` >= 1
    - `grep -c "p.side" relay/src/ws/order-handler.ts` >= 1 — 대기의 매매구분이 실제로 읽힌다
    - `PendingOrder` 타입에 `side` 필드가 있다 (선언 줄번호를 SUMMARY 에 인용)
    - 매매구분 축이 `refine` 을 쓴다 (하드 필터가 아님을 소스로 확인해 SUMMARY 에 인용)
    - 「매수/매도 동시 대기」 케이스와 「같은 방향 2건은 좁혀지지 않는다」 케이스가 **둘 다** 존재
    - `pnpm --filter @gh-radar/relay exec vitest run tests/ws-order.test.ts` exit 0
  </acceptance_criteria>
  <done>가를 수 있는 통보는 갈라 정산하고, 못 가르는 것은 여전히 정산하지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: 취소 중복 키를 원주문번호로 가른다 (GC-WR-10)</name>
  <files>relay/src/ws/order-handler.ts, relay/tests/ws-order.test.ts</files>
  <read_first>
    - `relay/src/ws/order-handler.ts` `ridKey`·`dupKey`·`claimKeys` 세 함수와 각각의 docstring(**어느 자료구조가 무엇을 막는지**의 근거), 게이트 ⓪ 중복 검사부, `release`·`dropUserDupKeys`·`closeConn` 의 키 회수 경로
    - `packages/shared/src/relay.ts` — `RelayOrderCancelMsg` 의 필드(특히 `orgOrderNo` 가 필수인지, `price`·`qty` 가 취소에 실리는지)
    - `relay/tests/ws-order.test.ts` 의 중복 관련 케이스(16-22 가 넣은 「두 연결 중복 거부」·「closeConn 후 재주문 가능」 포함) — **이 케이스들이 그대로 통과해야 한다**
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §GC-WR-10 — Fix 스니펫
    - `webapp/src/components/trading/account-panel.tsx` 의 `✕ 취소` 호출부 — 사용자가 미체결을 연달아 취소하는 실제 동선(자산 위험의 근거)
  </read_first>
  <action>
① `dupKey` 를 요청 종류로 가른다. 16-REVIEW §GC-WR-10 의 Fix 스니펫을 참조하라:
  - 취소(`msg.t === "order.cancel"`): `dup:${accountNo}|${isin}|C|${orgOrderNo}` — **취소의 정체성은 원주문번호다.** 가격·수량은 취소 요청에서 식별자가 아니다.
  - 신규(`order.new`): 지금과 **완전히 동일**하다 — `dup:${accountNo}|${isin}|${side}|${price}|${qty}`. 두 탭 동시 발주 차단(16-22 truth 25)을 그대로 유지한다.

② `dupKey` 의 docstring 에 이 분기의 근거를 잇는다: 「같은 종목·같은 가격·같은 잔량의 미체결이 2건 있는 상황은 흔하다(다른 단말·전일 잔여·자동주문). 가격·수량 키로 묶으면 두 번째 취소가 최대 5초 거부되고, **급락 국면의 일괄 취소가 막히는 것은 자산 위험이다** (GC-WR-10)」.

③ 같은 취소 요청의 **연타**는 여전히 막혀야 한다 — 같은 `orgOrderNo` 로 두 번 누르면 두 번째는 거부다. 그 사실을 확인하고 주석에 남긴다(이 변경이 중복 가드를 없애는 것이 아님을 분명히 한다).

④ 키 회수 경로(`release` · `closeConn` · `dropUserDupKeys`)가 새 키 형태에서도 정확히 도는지 확인한다 — `claimKeys` 가 한 쌍으로 만들고 한 쌍으로 놓는 관례를 유지하면 추가 변경은 없어야 한다. 없음을 확인했다면 그 사실을 SUMMARY 에 적는다.

⑤ 테스트를 추가한다.
  - 「같은 종목·같은 가격·같은 잔량의 미체결 2건을 서로 다른 `orgOrderNo` 로 연달아 취소하면 **둘 다 게이트웨이로 나간다**」 — `DirectOrderReq` 2건을 단언한다.
  - 「같은 `orgOrderNo` 취소를 연타하면 두 번째는 거부된다」 — 기존 중복 규율 유지.
  기존 신규 주문 중복 케이스 2종은 그대로 통과해야 한다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test</automated>
  </verify>
  <acceptance_criteria>
    - `dupKey` 본문에 `msg.t === "order.cancel"` 분기가 있다 (줄번호 SUMMARY 인용)
    - `grep -c "orgOrderNo" relay/src/ws/order-handler.ts` 값이 변경 전보다 증가 (전후 값 인용)
    - 신규 주문 키 문자열 형태가 변하지 않았다 (`dup:${...}|${side}|${price}|${qty}` 유지 — 소스 인용)
    - 「서로 다른 원주문번호 연속 취소 2건 통과」·「같은 원주문번호 연타 거부」 두 케이스가 존재
    - 16-22 의 「두 연결 중복 거부」·「closeConn 후 재주문 가능」 케이스가 여전히 통과
    - `pnpm --filter @gh-radar/relay test` exit 0
    - `pnpm --filter @gh-radar/relay run typecheck` · `typecheck:tests` exit 0
  </acceptance_criteria>
  <done>서로 다른 미체결의 연속 취소가 막히지 않고, 같은 취소의 연타는 여전히 막힌다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 통보의 `side` ↔ 신뢰 가능성 | 취소·정정 통보의 매매구분은 믿을 수 없다(Pitfall 8) — `sideTrusted` 가 그 경계의 유일한 신호다 |
| 중복 가드 ↔ 정상 조작 | 가드가 너무 넓으면 급락 국면의 일괄 취소를 막는다. 가드의 오작동은 사고 방지가 아니라 사고 유발이다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-70 | Repudiation (결과 미상) | `narrowPending` 매매구분 축 부재 | mitigate | `sideTrusted && side !== ""` 일 때만 `refine` 으로 방향 축 적용. 취소·정정 통보에는 적용하지 않는다(Pitfall 8) |
| T-16-71 | Denial of Service (자산 위험) | 취소 dup 키의 원주문번호 부재 | mitigate | 취소 키를 `orgOrderNo` 로 가른다. 「서로 다른 원주문번호 연속 취소 2건 통과」로 잠금 |
| T-16-31 | Tampering (중복 발주) | 신규 dup 키 | mitigate | **변경하지 않는다.** 두 탭 동시 발주 차단 회귀 케이스 유지 |
| T-16-29 | Tampering (기록 교차) | 좁히기 규율 | mitigate | 새 축도 `refine`(0이면 건너뜀)이다. 「같은 방향 2건은 좁혀지지 않는다」로 폴백 부재를 계속 잠금 |
| T-16-13 | 운영 사고 | 실서버·실계좌 접속 | accept | FakeGateway 만 쓴다 (D-27 승계) |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/relay test` exit 0
- `pnpm --filter @gh-radar/relay run typecheck` · `typecheck:tests` exit 0
- `grep -rn "10\.41\.1\.120" relay/` 0건 (D-27 승계)
</verification>

<success_criteria>
- 매수/매도 동시 대기가 각자의 통보로 정산된다
- 취소·정정 통보에는 매매구분 축이 적용되지 않는다
- 서로 다른 미체결의 연속 취소가 통과한다
- 신규 주문 중복 차단은 그대로다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-34-SUMMARY.md` when done
</output>
