---
phase: 16-trading-limit-chaser-vi-my-page
plan: 19
type: execute
wave: 15
depends_on: ["16-17"]
gap_closure: true
files_modified:
  - webapp/src/lib/use-relay-socket.ts
  - webapp/src/lib/relay-provider.tsx
  - webapp/src/components/trading/strategy-status-card.tsx
  - webapp/src/components/trading/limit-chaser-form.tsx
  - webapp/src/components/trading/vi-settings-card.tsx
  - webapp/src/components/trading/vi-order-list.tsx
  - webapp/src/lib/__tests__/relay-socket.test.ts
  - webapp/src/components/trading/__tests__/strategy-status-card.test.tsx
autonomous: true
requirements: [MYPAGE-01, TRADE-03]

must_haves:
  truths:
    - "소켓이 열려 있지 않을 때 `send()` 가 **로그를 남기고 false 를 돌려준다** — 전략 4종의 유일한 출구에 무로그 드롭이 없다 (gap 3 / PC-7 · S-5 「무로그 fail-safe 금지」)"
    - "「전체 비활성화」 버튼은 `status !== 'ready'` 면 비활성이다 — 단절 중에는 눌리지 않는다 (gap 3)"
    - "전송에 실패하면 `awaitingAck` 가 서지 않고 화면에 「연결이 끊겨 …」가 뜬다 — 사용자가 껐다고 오인하지 않는다"
    - "8초 ack 타임아웃이 조용히 지나가지 않고 「반영을 확인하지 못했어요」를 남긴다"
    - "`send` 를 부르는 나머지 4개 호출부가 전부 세션 준비 상태로 가려져 있음이 소스 단언으로 확인된다 (킬 스위치만 예외였던 사실이 회귀하지 않는다)"
  artifacts:
    - path: "webapp/src/lib/use-relay-socket.ts"
      provides: "boolean 을 돌려주고 드롭을 로그로 남기는 send"
      contains: "console.error"
    - path: "webapp/src/components/trading/strategy-status-card.tsx"
      provides: "세션 가드 + 전송 실패·ack 타임아웃 표시"
      contains: "status !== 'ready'"
    - path: "webapp/src/components/trading/__tests__/strategy-status-card.test.tsx"
      provides: "단절 상태 킬 스위치 회귀 케이스"
  key_links:
    - from: "webapp/src/components/trading/strategy-status-card.tsx"
      to: "webapp/src/lib/relay-provider.tsx"
      via: "send 반환값 분기"
      pattern: "if\\s*\\(\\s*!send\\("
    - from: "webapp/src/lib/relay-provider.tsx"
      to: "webapp/src/lib/use-relay-socket.ts"
      via: "RelayInbound → boolean 계약"
      pattern: "=> boolean"
---

<objective>
**gap 3** 을 닫는다. 「전체 비활성화」(킬 스위치)가 소켓 단절 상태에서 **0바이트를 내보내고 아무 신호도 남기지 않는** 자리를 없앤다.

지금 상태: `use-relay-socket.ts` L850~854 의 `send()` 는 `readyState !== OPEN` 이면 **로그도 반환값도 없이** 반환한다. 이것은 전략 4종(`lc.set`/`vi.set`/`vi.confirm`/`strategies.disable`)의 유일한 출구다. `lc.set`/`vi.set`/`vi.confirm` 호출부는 전부 `status !== 'ready'` 로 가려져 있어 이 구멍에 닿지 않는데, **킬 스위치만 그 가드가 없다**(`strategy-status-card.tsx:456` `disabled={nothingToDisable || awaitingAck}`). 리듀서는 단절 시 `isStale` 만 세우고 `limitChasers` 를 유지하므로 재접속 중에도 버튼이 활성이고, 누르면 8초 뒤 `awaitingAck` 만 조용히 내려간다. 사용자는 껐다고 믿고 자리를 뜨고 자동매매는 계속 돈다.

프로젝트 규율 **PC-7 / S-5 「무로그 fail-safe 금지」를 정면 위반**하는 자리이고, 하필 그 대상이 가장 안전 임계적인 컨트롤이다.

Purpose: 요구사항 MYPAGE-01 의 킬 스위치 절이 「연결돼 있을 때만 참」인 상태를 끝낸다.
Output: `send(msg): boolean` + 드롭 로그 · 킬 스위치 세션 가드 · 전송 실패/ack 타임아웃 문구 · 나머지 호출부 감사.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md
</context>

<interfaces>
<!-- 바꿔야 하는 현재 계약. 구현은 read_first 로 직접 본다. -->

`webapp/src/lib/use-relay-socket.ts`:
- `RelayConnectionState.send: (msg: RelayInbound) => void` (L207 부근 JSDoc: 「소켓이 열려 있지 않으면 조용히 무시한다」 ← 이 문장이 결함의 선언이다)
- `RelaySocketState.send: (msg: RelayInbound) => void` (L242 부근)
- `const send = useCallback((msg: RelayInbound) => { … if (!ws || ws.readyState !== WS_READY_OPEN) return; … }, [])` (L850)
- `sendOrder` 는 **이미** 실패를 `RelayOrderResultMsg{status:"rejected"}` 로 표면화한다 — 이 태스크의 본보기다

`webapp/src/lib/relay-provider.tsx`:
- `EMPTY_RELAY_VALUE.send: NOOP` (Provider 밖 폴백 — 같은 파일의 `sendOrder` 폴백 주석이 「보내지 **않았음**이 확실하다. 조용히 성공으로 위장하지 않는다(PC-7)」로 이미 규율을 적어 두었다)
- `useRelaySubscription` 이 `send: relay.send` 를 그대로 통과시킨다

`webapp/src/components/trading/strategy-status-card.tsx`:
- `const { status, limitChasers, viTrigger, accounts, strategiesDisabled, send } = useRelayContext();` (L331)
- `DISABLE_ACK_TIMEOUT_MS = 8_000` (L74)
- `handleConfirm` (L360~368) — `ackBaseline.current` 설정 → `setAwaitingAck(true)` → `setDialogOpen(false)` → `send(...)`
- ack 백스톱 `useEffect` (L354~358) — `setTimeout(() => setAwaitingAck(false), DISABLE_ACK_TIMEOUT_MS)`
- 버튼 `disabled={nothingToDisable || awaitingAck}` (L456)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: send 를 boolean 계약으로 바꾸고 드롭을 로그로 남긴다</name>
  <files>webapp/src/lib/use-relay-socket.ts, webapp/src/lib/relay-provider.tsx, webapp/src/lib/__tests__/relay-socket.test.ts</files>
  <read_first>
    - `webapp/src/lib/use-relay-socket.ts` L145~250 (`RelayConnectionState` · `RelaySocketState` 계약과 `send` JSDoc), L820~900 (`unsubscribe` · `send` · `sendOrder` 구현 — `sendOrder` 의 실패 표면화 방식을 본보기로 삼는다)
    - `webapp/src/lib/relay-provider.tsx` L185~215 `EMPTY_RELAY_VALUE`(특히 `sendOrder` 폴백의 PC-7 주석), L280~332 `useRelaySubscription` 반환 객체
    - `webapp/src/lib/__tests__/relay-socket.test.ts` — 특히 케이스 ⑰-a(「소켓이 없으면 rejected 로 …」)의 FakeWebSocket 구성. 새 케이스가 그 하네스를 재사용한다
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md` gaps[2] (truth / reason / missing 4항목)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §CR-03
  </read_first>
  <action>
① `send` 의 반환 타입을 `boolean` 으로 바꾼다. 소켓이 없거나 `readyState !== WS_READY_OPEN` 이면 `console.error` 로 한 줄 남기고 `false` 를 돌려준다. 로그 문구에 **메시지 종류만** 싣는다: `` `[relay] 소켓 미연결 — 전송하지 않음 (t=${msg.t})` ``. `msg` 본문 전체를 로그에 넣지 않는다 — `lc.set.cfg` 와 `vi.set.accountNo` 에 계좌번호가 들어 있고 이 로그는 브라우저 콘솔이다(T-16-18). 정상 경로는 `ws.send(...)` 후 `true`.

② 두 계약의 타입을 함께 고친다: `RelayConnectionState.send: (msg: RelayInbound) => boolean`, `RelaySocketState.send: (msg: RelayInbound) => boolean`. `RelayConnectionState.send` 의 JSDoc 에서 「소켓이 열려 있지 않으면 조용히 무시한다」 문장을 **삭제**하고 「소켓이 열려 있지 않으면 `console.error` 를 남기고 `false` 를 돌려준다. 호출부는 반환값으로 「보내지 않았음」을 알 수 있다 — 조용한 드롭은 PC-7 위반이다」로 바꾼다. 이 문장이 남아 있으면 다음 사람이 같은 구멍을 다시 만든다.

③ `relay-provider.tsx` 의 `EMPTY_RELAY_VALUE.send` 를 `NOOP` 에서 `() => false` 로 바꾸고, 바로 위 `sendOrder` 폴백 주석과 같은 규율(「보내지 **않았음**이 확실하다」)을 한 줄 붙인다. `NOOP` 이 다른 필드(`reconnect`/`subscribe`/`unsubscribe`)에도 쓰이므로 `NOOP` 자체는 지우지 않는다.

④ `webapp/src/lib/__tests__/relay-socket.test.ts` 에 케이스 2건을 추가한다.
  - 「소켓이 열리기 전 `send` 는 `false` 를 돌려주고 `console.error` 를 한 번 남긴다」 — `vi.spyOn(console, 'error')` 로 호출 1회와 인자에 `t=` 가 포함됨을 단언하고, **인자 문자열에 계좌번호가 없음**도 함께 단언한다.
  - 「연결 후 `send` 는 `true` 를 돌려주고 소켓에 정확히 1프레임이 나간다」.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp exec vitest run src/lib/__tests__/relay-socket.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @gh-radar/webapp exec vitest run src/lib/__tests__/relay-socket.test.ts` exit 0
    - `grep -c "=> boolean" webapp/src/lib/use-relay-socket.ts` >= 2 (두 계약)
    - `grep -c "조용히 무시" webapp/src/lib/use-relay-socket.ts` == 0
    - `grep -n "console.error" webapp/src/lib/use-relay-socket.ts` 가 `send` 안에서 1건 이상
    - `pnpm --filter @gh-radar/webapp run typecheck` exit 0
  </acceptance_criteria>
  <done>`send` 가 실패를 반환값과 로그 양쪽으로 드러낸다. 계약 JSDoc 이 그 사실을 말한다.</done>
</task>

<task type="auto">
  <name>Task 2: 킬 스위치에 세션 가드 + 전송 실패·ack 타임아웃 표시</name>
  <files>webapp/src/components/trading/strategy-status-card.tsx, webapp/src/components/trading/__tests__/strategy-status-card.test.tsx</files>
  <read_first>
    - `webapp/src/components/trading/strategy-status-card.tsx` 전문 — 특히 파일 상단 ④(「65 는 완료 신호로만」)·⑤(단건 비활성화 UI 없음) 주석, L330~370(`useRelayContext` 구조분해 · `awaitingAck` · `ackBaseline` · 두 `useEffect` · `handleConfirm`), L444~470(버튼과 `DisableAllDialog`)
    - `webapp/src/components/trading/__tests__/strategy-status-card.test.tsx` — 기존 케이스가 `useRelayContext` 를 어떻게 스텁하는지(새 케이스가 그 하네스를 재사용한다)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md` — 전략 현황 카드(C4) 절의 문구 규약. **새 문구는 UI-SPEC 의 어조와 같은 존댓말 한 줄**이어야 한다
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md` gaps[2].missing 2~4번째 항목
    - Task 1 이 바꾼 `send` 반환 계약
  </read_first>
  <action>
① 버튼 `disabled` 를 `nothingToDisable || awaitingAck || status !== 'ready'` 로 바꾼다. `status` 는 이미 구조분해돼 있다.

② `handleConfirm` 의 순서를 뒤집는다. 먼저 `setDialogOpen(false)` → `if (!send({ t: "strategies.disable" })) { setSendError('연결이 끊겨 비활성화 요청을 보내지 못했어요. 연결이 복구된 뒤 다시 눌러 주세요.'); return; }` → 성공했을 때만 `ackBaseline.current = strategiesDisabled; setAwaitingAck(true); setSendError('')`. **실패 시 `awaitingAck` 를 세우지 않는다** — 세우면 8초 동안 버튼이 잠긴 채 아무 일도 일어나지 않는다.

③ 상태 하나를 추가한다: `const [sendError, setSendError] = useState('')`. 값이 비어 있지 않으면 「전체 비활성화」 버튼 아래(같은 `data-slot="strategy-disable-all"` 블록 안)에 `role="alert"` 를 가진 한 줄을 그린다. 색은 `--destructive` 를 쓰되 그 블록의 기존 토큰 사용 방식과 같은 형태를 따른다. 새 `data-slot` 은 `strategy-disable-error` 로 둔다(E2E·RTL 이 잡을 수 있어야 한다).

④ ack 타임아웃 백스톱 `useEffect` 를 고친다. 8초가 지나면 `setAwaitingAck(false)` 만 하는 대신 `setSendError('전체 비활성화 요청의 반영을 확인하지 못했어요. 전략 목록을 확인해 주세요.')` 를 함께 세운다. **「실패했다」고 말하지 않는다** — 65 가 유실됐을 뿐 전략은 실제로 꺼졌을 수 있다(Pitfall 9 동형: 결과를 모르는 것과 실패를 뭉개지 않는다). 65 가 도착해 `awaitingAck` 가 정상적으로 내려가는 경로에서는 `setSendError('')` 로 지운다.

⑤ 새 케이스 3건을 `strategy-status-card.test.tsx` 에 추가한다.
  - 「`status` 가 `reconnecting` 이고 상따 목록이 남아 있어도 전체 비활성화 버튼이 비활성이다」 — 이것이 gap 3 의 회귀 잠금이다.
  - 「`send` 가 `false` 를 돌려주면 `awaitingAck` 가 서지 않고 `strategy-disable-error` 가 뜬다」 — `send` 스텁이 `false` 를 돌려주게 하고, 다이얼로그 확인 후 버튼이 다시 눌릴 수 있는 상태(잠기지 않음)임을 함께 단언한다.
  - 「8초가 지나도 65 가 오지 않으면 `strategy-disable-error` 가 뜬다」 — `vi.useFakeTimers()` 로 `DISABLE_ACK_TIMEOUT_MS` 를 넘긴다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp exec vitest run src/components/trading/__tests__/strategy-status-card.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @gh-radar/webapp exec vitest run src/components/trading/__tests__/strategy-status-card.test.tsx` exit 0
    - `grep -c "status !== 'ready'" webapp/src/components/trading/strategy-status-card.tsx` >= 1
    - `grep -c "strategy-disable-error" webapp/src/components/trading/strategy-status-card.tsx` >= 1
    - `grep -c "!send(" webapp/src/components/trading/strategy-status-card.tsx` >= 1 — 반환값을 실제로 분기한다
    - `grep -n "setAwaitingAck(true)" webapp/src/components/trading/strategy-status-card.tsx` 가 `!send(...)` 의 early return **뒤**에 온다 (소스 확인 후 SUMMARY 에 줄번호 인용)
    - `pnpm --filter @gh-radar/webapp run typecheck` exit 0
  </acceptance_criteria>
  <done>단절 중에는 버튼이 눌리지 않고, 눌렸는데 못 나갔으면 화면이 그 사실을 말한다.</done>
</task>

<task type="auto">
  <name>Task 3: 나머지 send 호출부 4곳 감사 — 세션 가드 실재 확인, 없으면 보강</name>
  <files>webapp/src/components/trading/limit-chaser-form.tsx, webapp/src/components/trading/vi-settings-card.tsx, webapp/src/components/trading/vi-order-list.tsx</files>
  <read_first>
    - `webapp/src/components/trading/limit-chaser-form.tsx` L295~325 (`toggleGate` · `handleSubmit`) 와 `disabled` prop 의 출처
    - `webapp/src/components/trading/limit-chaser-client.tsx` L570~580 — 폼에 넘기는 `disabled={isin === '' || accountNo === '' || status !== 'ready'}`
    - `webapp/src/components/trading/vi-settings-card.tsx` L245~275 (`submit`) 와 L313 `const locked = disabled || server === undefined`
    - `webapp/src/components/trading/vi-client.tsx` L280~430 — `sessionReady` 파생과 자식들에게 넘기는 `disabled`
    - `webapp/src/components/trading/vi-order-list.tsx` L130~145 `isConfirmable` 과 L227 `send({t:'vi.confirm'})`
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §「검토했으나 결함을 찾지 못한 영역」 마지막 항목 — 「`lc.set`/`vi.set`/`vi.confirm` 호출부는 전부 `status !== 'ready'` 로 가려져 있다(CR-03 의 킬 스위치만 예외)」. **이 주장을 이 태스크가 직접 확인한다**
  </read_first>
  <action>
`send` 를 부르는 호출부는 정확히 5곳이다: `limit-chaser-form.tsx:311`(`toggleGate`) · `:322`(`handleSubmit`) · `vi-settings-card.tsx:260`(`submit`) · `vi-order-list.tsx:227`(`vi.confirm`) · `strategy-status-card.tsx:368`(Task 2 에서 처리). 앞의 4곳을 **하나씩 추적**해 「세션이 ready 가 아니면 이 호출에 도달할 수 없다」가 참인지 확인한다. 추적 경로는 각 호출부의 가드 → 그 가드가 읽는 prop → 그 prop 을 넘기는 부모 → 부모가 `status` 를 읽는 자리까지다.

- 참이면 **코드를 바꾸지 않는다.** 대신 각 호출부 바로 위에 한 줄 주석으로 근거를 못박는다: 어느 부모가 `status !== 'ready'` 를 어느 prop 으로 내려보내는지(파일명 + 심볼). 지금은 그 사실이 파일 3개에 흩어져 있어 다음 사람이 킬 스위치와 같은 예외를 다시 만든다.
- 거짓인 곳이 있으면 그 호출부의 `disabled` 조건에 세션 상태를 더해 가드를 세우고, 그 파일의 기존 테스트에 회귀 케이스 1건을 추가한다.

**하지 않는 것:** 이 4곳에서 `send` 반환값을 분기하는 UI 를 새로 만들지 않는다. 가드가 실재하면 드롭 자체가 일어나지 않고, 그럼에도 드롭되면 Task 1 의 `console.error` 가 남는다 — PC-7 은 그것으로 충족된다. 문구·상태를 4곳에 복제하면 유지 지점이 늘 뿐이다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @gh-radar/webapp test` exit 0 (기존 628 + 신규)
    - 4개 호출부 각각의 가드 경로가 SUMMARY 에 **파일:줄번호 → prop 이름 → 부모 파일:줄번호** 형태로 인용돼 있다
    - `grep -c "status !== 'ready'\|sessionReady" webapp/src/components/trading/limit-chaser-client.tsx webapp/src/components/trading/vi-client.tsx` 가 각 파일에서 1건 이상
    - 가드가 없는 호출부를 발견했다면 그 파일의 diff 와 신규 테스트 케이스가 존재한다. 발견하지 못했다면 SUMMARY 가 「4곳 전부 가드 실재」를 근거와 함께 명시한다
  </acceptance_criteria>
  <done>킬 스위치가 유일한 예외였다는 사실이 소스 주석과 SUMMARY 근거로 고정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 의도 ↔ 게이트웨이 실제 상태 | 「껐다」는 화면 표시와 「실제로 꺼졌다」 사이에 wss 한 홉이 있다 |
| 브라우저 콘솔 로그 ↔ 계좌 식별자 | 새 `console.error` 가 무엇을 싣는가 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-19 | Repudiation | `webapp/src/lib/use-relay-socket.ts` `send` | mitigate | 드롭을 `console.error` + `false` 반환으로 표면화. 계약 JSDoc 의 「조용히 무시한다」 문장을 삭제해 규율이 코드 옆에 남게 한다 (PC-7 / S-5) |
| T-16-20 | Denial of Service (사용자 자산) | `strategy-status-card.tsx` 킬 스위치 | mitigate | `status !== 'ready'` 가드 + 전송 실패 시 `awaitingAck` 미설정 + `role="alert"` 문구. 단절 중 「껐다고 믿는」 상태를 만들지 않는다 |
| T-16-18 | Information Disclosure | 새 `console.error` 로그 | mitigate | 로그에 `msg.t` 만 싣는다. `lc.set.cfg`·`vi.set.accountNo` 본문을 넣지 않는다 — 테스트가 로그 인자에 계좌번호가 없음을 단언 |
| T-16-21 | Tampering (오독) | ack 타임아웃 문구 | accept | 8초 미수신은 「실패」가 아니라 「반영을 확인하지 못함」이다. 실패로 단정하면 사용자가 다시 눌러 65 를 두 번 만든다 — 재시도 자체는 무해하지만 문구가 사실과 달라진다 |
| T-16-13 | 운영 사고 | 실서버·실계좌 접속 | accept | 이 plan 은 RTL·가짜 소켓만 쓴다(D-27 승계) |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/webapp test` exit 0
- `pnpm --filter @gh-radar/webapp run typecheck` exit 0 (`tsc --noEmit` + `tsconfig.e2e.json`)
- `pnpm typecheck` (13 워크스페이스) exit 0
- `grep -rn "10\.41\.1\.120" webapp/src/` 0건 (D-27 승계)
</verification>

<success_criteria>
- `send` 가 `boolean` 을 돌려주고 드롭을 로그로 남긴다
- 킬 스위치가 `status !== 'ready'` 에서 비활성이다
- 전송 실패·ack 미수신이 각각 다른 문구로 화면에 드러난다
- 나머지 4개 호출부의 가드 실재가 근거와 함께 기록됐다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-19-SUMMARY.md` when done
</output>
</content>
