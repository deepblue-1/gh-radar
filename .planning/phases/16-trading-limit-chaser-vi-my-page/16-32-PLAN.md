---
phase: 16-trading-limit-chaser-vi-my-page
plan: 32
type: execute
wave: 19
depends_on: ["16-19", "16-23"]
gap_closure: true
files_modified:
  - webapp/src/components/trading/vi-order-list.tsx
  - webapp/src/components/trading/vi-settings-card.tsx
  - webapp/src/components/trading/me-client.tsx
  - webapp/src/components/trading/__tests__/vi-order-list.test.tsx
  - webapp/src/components/trading/__tests__/vi-settings-card.test.tsx
  - webapp/src/components/trading/__tests__/me-client.test.tsx
autonomous: true
requirements: [TRADE-02, MYPAGE-01]

must_haves:
  truths:
    - "`vi.confirm` 이 나가지 못하면 체크박스 낙관 반영도 행 잠금도 걸리지 않는다 — 서버 73 델타가 올 리 없는 요청에 사용자가 「확인했다」고 믿는 회색 행이 남지 않는다 (GC-WR-06 / 16-19 boolean 계약)"
    - "`vi.set` 도 전송 실패를 화면에 드러낸다 — 보내지 못한 설정 변경이 성공처럼 보이지 않는다 (GC-WR-06)"
    - "상태줄의 「가장 최근 반영 시각」이 `HH:MM:SS` 로 자른 뒤가 아니라 **정규화 전 값**(또는 epoch)으로 비교된다 — 자정 경계·혼합 포맷에서 최댓값이 뒤집히지 않는다 (GC-IN-03)"
  artifacts:
    - path: "webapp/src/components/trading/vi-order-list.tsx"
      provides: "send 반환값 분기 — 실패 시 낙관 반영·잠금 금지"
      contains: "if (!send("
    - path: "webapp/src/components/trading/me-client.tsx"
      provides: "정규화 전 값 기준 최신 계좌 시각 선택"
    - path: "webapp/src/components/trading/__tests__/me-client.test.tsx"
      provides: "latestAccountTime 혼합 포맷·자정 경계 케이스"
  key_links:
    - from: "vi-order-list.tsx (toggle)"
      to: "webapp/src/lib/use-relay-socket.ts"
      via: "send boolean 반환값 분기 (16-19 계약)"
      pattern: "if \\(!send\\("
---

<objective>
VI 표면과 My page 의 **GC-WR-06(VI 2곳) · GC-IN-03** 을 닫는다.

지금 상태:
- **GC-WR-06(vi-order-list).** `toggle`(`:230-233`)이 `send({ t:'vi.confirm', … })` 의 반환값을 버리고 곧바로 `setOptimistic(...)` 과 `setSending(...)` 을 건다. 전송이 실패해도 체크박스는 「확인됨」으로 바뀌고 행은 `sending` 으로 잠긴다. **잠금을 푸는 신호는 서버의 73 델타인데 요청이 나가지 않았으므로 그 델타는 오지 않는다** — 사용자는 확인했다고 믿고 행은 영구히 회색으로 남는다. `isConfirmable(item, disabled)` 가드가 대부분을 막지만, `ready` 표시와 소켓 `readyState` 가 어긋나는 창이 정확히 이 경로다.
- **GC-WR-06(vi-settings-card).** `send(msg)`(`:327`) 도 반환값을 읽지 않는다. 16-19 가 이 파일의 `locked` 가드를 세웠지만(감사 중 발견한 두 번째 구멍), **보냈다고 믿는 경로**는 그대로 남아 있다.
- **GC-IN-03.** `latestAccountTime`(`me-client.tsx:88-97`)은 `formatServerTime(state.st)` 로 **날짜를 버리고 시각만 남긴 뒤** 문자열 비교한다. 게이트웨이가 `YYYYMMDDHHMMSS` 를 주고 스텁이 `HH:MM:SS` 를 주는 혼합 상황이나 자정을 넘긴 프레임에서 「가장 최근」이 뒤집힌다. 표시 전용이라 영향은 작지만, 비교는 **정규화 전** 값으로 하는 것이 맞다.

Purpose: TRADE-02·MYPAGE-01 표면이 「보냈다」와 「보냈다고 믿는다」를 구분하게 한다.
Output: `send` 반환값 분기 2곳 · 정규화 전 비교 · 회귀 테스트 3종.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-19-SUMMARY.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-23-SUMMARY.md
</context>

<interfaces>
<!-- 손대는 자리의 현재 계약. -->

`webapp/src/components/trading/vi-order-list.tsx`:
- `toggle = useCallback((item: RelayViOrderItem, next: boolean) => { if (!isConfirmable(item, disabled)) return; if (sending.has(item.orderNo)) return; send({ t:'vi.confirm', orderNo: item.orderNo, confirmed: next }); setOptimistic(...); setSending(...); }, [disabled, send, sending])`
- `optimistic: Map<orderNo, boolean>` · `sending: Set<orderNo>` — 서버 73 델타가 오면 정리하는 `useEffect` 가 위에 있다(`:210-225`): `item.confirmed === wanted || item.confirmLocked` 이면 두 상태에서 지운다
- `isConfirmable(item, disabled)` — 렌더의 `locked` 판정과 **같은 함수**(「비활성인데 눌리면 나가는」 것을 막는 규율)

`webapp/src/components/trading/vi-settings-card.tsx`:
- `:306` `if (locked) return;` (16-19 가 세운 세션 가드) → `:327` `send(msg)`

`webapp/src/components/trading/me-client.tsx`:
- `export function latestAccountTime(states: ReadonlyMap<string, RelayAccountState>): string | null` — `formatServerTime(state.st)` 결과끼리 `>` 비교. 반환값은 **표시 문자열**이다
- `formatServerTime` — `YYYYMMDDHHMMSS` 든 `HH:MM:SS` 든 `HH:MM:SS` 로 정규화(날짜를 버린다)

`webapp/src/lib/use-relay-socket.ts:863-876` — `send: (msg: RelayInbound) => boolean`. 미연결이면 `console.error(...)` 후 `false`, 본문 미기재
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: VI 두 호출부가 `send()` 반환값을 읽는다 (GC-WR-06 — VI 2곳)</name>
  <files>webapp/src/components/trading/vi-order-list.tsx, webapp/src/components/trading/vi-settings-card.tsx, webapp/src/components/trading/__tests__/vi-order-list.test.tsx, webapp/src/components/trading/__tests__/vi-settings-card.test.tsx</files>
  <read_first>
    - `webapp/src/components/trading/vi-order-list.tsx` L200~250 (73 델타로 `optimistic`/`sending` 을 정리하는 `useEffect` → `toggle`) 와 렌더에서 `locked`·`checked` 를 계산하는 자리(:240~270). **잠금을 푸는 유일한 신호가 서버 델타라는 사실**이 이 task 의 전제다
    - `webapp/src/components/trading/vi-settings-card.tsx` L295~340 (`locked` 가드 → 메시지 조립 → `send(msg)`)
    - `webapp/src/components/trading/strategy-status-card.tsx` L330~370 — **이미 반환값을 읽는 유일한 호출부.** 실패 문구·`awaitingAck` 미설정·8초 ack 타임아웃 문구 관례를 그대로 따른다
    - `webapp/src/lib/use-relay-socket.ts` L855~880 — boolean 계약과 `console.error` 문구
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §GC-WR-06 — Issue + Fix 스니펫(「낙관 반영·잠금 어느 것도 걸지 않는다」)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-19-SUMMARY.md` — boolean 계약을 만든 plan 이 무엇을 의도했는지, 그리고 `vi.set` 의 두 번째 구멍을 어떻게 찾았는지
  </read_first>
  <action>
① `vi-order-list.tsx` 의 `toggle` 을 반환값 분기로 바꾼다. 16-REVIEW §GC-WR-06 의 Fix 스니펫을 참조하라.
  - `if (!send({ t:'vi.confirm', orderNo: item.orderNo, confirmed: next })) { …실패 표시…; return; }`
  - **실패 경로에서 `setOptimistic`·`setSending` 어느 것도 부르지 않는다.** 이것이 이 task 의 핵심이다 — 잠금을 푸는 신호가 서버 델타뿐이므로 잘못 건 잠금은 영구적이다.
  - 실패 표시 채널: 이 컴포넌트에 에러 표시 자리가 없으면 최소한의 상태 하나(`error` 문자열)를 만들어 목록 상단 또는 그 행 근처에 띄운다. 문구는 「연결이 끊겨 확인을 보내지 못했어요. 다시 시도해 주세요.」 계열로 `strategy-status-card` 어조에 맞춘다. **새 컴포넌트를 만들지 않는다** — 이 파일 안에서 끝낸다.
  - `disabled`/`isConfirmable` 가드는 그대로 둔다(이 분기는 그 뒤의 얇은 창을 메우는 것이다).

② `vi-settings-card.tsx` 의 `send(msg)` 도 같은 규율으로 바꾼다. 이 카드는 확인 다이얼로그를 거치는 경로이므로, 실패 시 **다이얼로그를 성공처럼 닫지 않고** 사유를 남긴다. 성공 경로의 낙관 반영·잠금이 있으면 실패 시 걸지 않는다.

③ 두 파일의 관련 주석을 갱신한다 — 「16-19 가 `send` 를 boolean 으로 바꾼 이유가 이 분기다」를 한 줄로 남겨, 다음 사람이 반환값을 다시 버리지 않게 한다.

④ 테스트를 추가한다.
  - `vi-order-list.test.tsx`: 「`send` 가 `false` 를 돌려주면 체크박스가 바뀌지 않고 행이 잠기지 않으며 문구가 뜬다」 · 「`send` 가 `true` 면 기존 낙관 반영·잠금이 그대로 걸린다」(회귀 방지).
  - `vi-settings-card.test.tsx`: 「`send` 가 `false` 면 실패 문구가 뜨고 성공 상태로 전환되지 않는다」.
  기존 케이스는 그대로 통과해야 한다 — `send` 스텁이 `undefined` 를 돌려주고 있다면 `true` 로 고쳐야 할 수 있다(그 사실을 SUMMARY 에 적는다).
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp exec vitest run src/components/trading/__tests__/vi-order-list.test.tsx src/components/trading/__tests__/vi-settings-card.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "if (!send(" webapp/src/components/trading/vi-order-list.tsx` >= 1
    - `grep -c "if (!send(" webapp/src/components/trading/vi-settings-card.tsx` >= 1
    - `vi-order-list.tsx` 의 실패 분기가 `setOptimistic`·`setSending` **이전에** `return` 한다 (줄번호를 SUMMARY 에 인용)
    - `grep -c "보내지 못했" webapp/src/components/trading/vi-order-list.tsx` >= 1 — 실패가 화면에 드러난다
    - 신규 3케이스가 테스트 파일에 존재 (케이스 이름을 SUMMARY 에 인용)
    - `pnpm --filter gh-radar-webapp exec vitest run src/components/trading/__tests__/vi-order-list.test.tsx src/components/trading/__tests__/vi-settings-card.test.tsx` exit 0
  </acceptance_criteria>
  <done>보내지 못한 `vi.confirm`·`vi.set` 에 낙관 반영도 잠금도 걸리지 않고, 사용자가 그 사실을 안다.</done>
</task>

<task type="auto">
  <name>Task 2: 「가장 최근 반영 시각」을 정규화 전 값으로 비교한다 (GC-IN-03)</name>
  <files>webapp/src/components/trading/me-client.tsx, webapp/src/components/trading/__tests__/me-client.test.tsx</files>
  <read_first>
    - `webapp/src/components/trading/me-client.tsx` L70~110 (`latestAccountTime` 전문 + 그 위 문단 — 16-23 이 계좌 전체를 훑도록 바꾼 근거와 **★ 「비교는 정규화한 뒤에 한다」는 현재의 (틀린) 근거 문단**. 그 문단을 정정하는 것이 이 task 다) 와 소비부(상태줄 C1)
    - `formatServerTime` 의 정의 위치와 입력 포맷 2종(`YYYYMMDDHHMMSS` · `HH:MM:SS`) 처리
    - `packages/shared/src/relay.ts` `RelayAccountState.st` 의 계약 주석 — 어떤 포맷이 오는지
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §GC-IN-03
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-23-SUMMARY.md` — 계좌 축 정리(CR-01)가 이 함수를 만든 맥락
  </read_first>
  <action>
① 비교 축을 **정규화 전 값**으로 옮긴다. 구현 선택지 두 가지 중 하나를 고르고 근거를 주석에 남긴다:
  - (a) `state.st` 원문에서 비교 가능한 키(epoch ms 또는 `YYYYMMDDHHMMSS` 로 정규화한 14자리)를 만들어 최댓값을 고르고, **표시 문자열은 그 승자에게서** `formatServerTime` 으로 뽑는다.
  - (b) 포맷을 판별해 `HH:MM:SS` 만 오는 경우와 날짜가 있는 경우를 분리 처리한다.
  어느 쪽이든 **혼합 포맷에서 날짜 있는 값이 날짜 없는 값에 지지 않아야** 하고, 모든 값이 `HH:MM:SS` 뿐인 기존 동작은 바뀌지 않아야 한다(계좌가 하나면 결과가 이전과 완전히 같다는 기존 보장도 유지).

② 함수 위 ★ 문단을 정정한다 — 「정규화한 뒤 비교한다」는 근거를 「**비교는 정규화 전** 값으로 한다. 날짜를 버린 뒤 비교하면 자정 경계와 혼합 포맷에서 최댓값이 뒤집힌다 (GC-IN-03)」로 바꾼다.

③ 값이 하나도 없을 때 `null` 을 돌려주는 기존 계약은 유지한다. 소비부(상태줄)의 표시 형식도 바꾸지 않는다 — 이 task 는 **어느 값을 고르는가**만 바꾼다.

④ 새 테스트 파일 `webapp/src/components/trading/__tests__/me-client.test.tsx` 를 만들어 `latestAccountTime` 을 직접 친다(이 함수는 export 되어 있고 지금 테스트가 0건이다).
  - 「모두 `HH:MM:SS` 면 가장 늦은 시각을 고른다」(기존 동작 회귀)
  - 「`YYYYMMDDHHMMSS`(오늘 09:00)와 `HH:MM:SS`(23:59, 어제 프레임) 가 섞이면 날짜가 이기고 09:00 이 선택된다」 — 혼합 포맷 뒤집힘 방지
  - 「자정을 넘긴 두 프레임(전일 23:59 · 당일 00:01)에서 00:01 이 선택된다」
  - 「빈 맵이면 `null`」
  케이스의 픽스처는 실제 `RelayAccountState` 모양을 따른다 — 최소 필드만 채우는 헬퍼를 파일 안에 둔다.
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp exec vitest run src/components/trading/__tests__/me-client.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/src/components/trading/__tests__/me-client.test.tsx` 가 존재하고 `latestAccountTime` 케이스 4개 이상
    - `grep -c "자정" webapp/src/components/trading/me-client.tsx` >= 1 — 정정된 근거 문단이 실재
    - `grep -c "정규화한 뒤에 한다" webapp/src/components/trading/me-client.tsx` == 0 — 틀린 근거가 남아 있지 않다
    - `pnpm --filter gh-radar-webapp exec vitest run src/components/trading/__tests__/me-client.test.tsx` exit 0
    - `pnpm --filter gh-radar-webapp run typecheck` exit 0
  </acceptance_criteria>
  <done>혼합 포맷·자정 경계에서도 「가장 최근」이 실제로 가장 최근이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 UI ↔ 소켓 실제 상태 | `ready` 표시와 `readyState` 가 어긋나는 창에서 요청이 조용히 사라진다 |
| 서버 73 델타 ↔ UI 잠금 해제 | 잠금을 푸는 신호가 서버뿐이므로, 나가지 않은 요청에 건 잠금은 **영구**다 |
| 게이트웨이 시각 포맷 ↔ 표시 로직 | 두 포맷이 섞여 들어온다 — 정규화 순서가 곧 정확성이다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-62 | Repudiation (조용한 미전송) | `vi-order-list.toggle` | mitigate | `send` boolean 분기. 실패 시 `setOptimistic`·`setSending` 미호출 + 문구. 「false 면 잠기지 않는다」 케이스로 회귀 잠금 |
| T-16-63 | Denial of Service (영구 잠금) | `sending` Set 해제 신호 | mitigate | 잘못 건 잠금을 애초에 만들지 않는다. 성공 경로의 낙관 반영·잠금은 그대로 유지(회귀 케이스) |
| T-16-64 | Repudiation | `vi-settings-card` 의 `send(msg)` | mitigate | 실패 시 성공 상태 전환 금지 + 사유 표시 |
| T-16-65 | 사용자 오도 | `latestAccountTime` 비교 축 | mitigate | 정규화 전 값(또는 epoch)으로 비교. 혼합 포맷·자정 경계 케이스로 잠금 |
| T-16-13 | 운영 사고 | 실계좌 조작 | accept | 단위 테스트·스텁만 쓴다 (D-27 승계) |
</threat_model>

<verification>
- `pnpm --filter gh-radar-webapp test` exit 0
- `pnpm --filter gh-radar-webapp run typecheck` exit 0
- `pnpm --filter gh-radar-webapp exec eslint src/components/trading/vi-order-list.tsx src/components/trading/vi-settings-card.tsx src/components/trading/me-client.tsx` exit 0
</verification>

<success_criteria>
- 보내지 못한 `vi.confirm` 에 잠금·낙관 반영이 걸리지 않는다
- `vi.set` 전송 실패가 화면에 드러난다
- 「가장 최근 반영 시각」이 혼합 포맷에서도 옳다
- `latestAccountTime` 에 테스트가 생겼다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-32-SUMMARY.md` when done
</output>
