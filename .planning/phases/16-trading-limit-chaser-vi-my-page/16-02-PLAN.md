---
phase: 16-trading-limit-chaser-vi-my-page
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - relay/tests/helpers/frames.ts
  - relay/tests/helpers/fake-gateway.ts
  - relay/tests/fake-gateway.test.ts
  - webapp/e2e/fixtures/relay.ts
  - webapp/playwright.config.ts
autonomous: true
requirements: [TRADE-01, TRADE-02, TRADE-03]

must_haves:
  truths:
    - "테스트가 60/61/64/72/73/56/51 프레임을 FakeGateway 로 주입할 수 있다 — 전략 왕복(D-01/D-12)을 실서버 없이 검증하는 유일한 경로"
    - "E2E 가 relay 를 쓰는 spec 을 여러 파일로 나눠도 8090 포트 충돌(EADDRINUSE)이 나지 않는다"
    - "실서버 IP·실계좌 리터럴이 테스트 코드에 0건이다 (Phase 15 D-27 승계)"
  artifacts:
    - path: "relay/tests/helpers/frames.ts"
      provides: "전략 응답 FlatBuffers 프레임 빌더"
      contains: "buildSetLimitChaserRespFrame"
    - path: "relay/tests/helpers/fake-gateway.ts"
      provides: "전략 응답 주입 API 5종"
      contains: "respondLimitChaserList"
    - path: "webapp/e2e/fixtures/relay.ts"
      provides: "LocalRelay 전략 주입 표면"
      contains: "pushLimitChaserEcho"
    - path: "webapp/playwright.config.ts"
      provides: "relay spec 직렬 실행"
      contains: "workers: 1"
  key_links:
    - from: "webapp/e2e/fixtures/relay.ts"
      to: "relay/tests/helpers/fake-gateway.ts"
      via: "LocalRelay.gateway 위임"
      pattern: "gateway\\.(pushLimitChaserEcho|respondLimitChaserList)"
---

<objective>
전략 메시지를 **실서버 없이** 검증할 수 있는 테스트 하네스를 세운다. RESEARCH §Wave 0 Gaps 의 3·4번 항목이다.

1. `relay/tests/helpers/frames.ts` 에 전략 응답 프레임 빌더를 추가한다(60/61/64/65/72/73/56).
2. `FakeGateway` 에 주입 API 5종(`respondLimitChaserList` · `pushLimitChaserEcho` · `respondViTrigger` · `pushViOrderList` · `pushOrderResp`)을 붙인다.
3. `webapp/e2e/fixtures/relay.ts` 의 `LocalRelay` 에 같은 표면을 노출하고, **relay 를 쓰는 spec 들의 8090 포트 충돌을 구조적으로 막는다**(RESEARCH Pitfall 20 / Open Question A6).

Purpose: 이후 모든 relay·webapp plan 의 `<verify>` 가 이 하네스를 전제로 한다. 여기가 비면 뒤의 모든 테스트가 「MISSING」이 된다.
Output: 전략 프레임 빌더 · FakeGateway 5 API · LocalRelay 노출 · Playwright 직렬화.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전략 응답 FlatBuffers 프레임 빌더 추가</name>
  <files>relay/tests/helpers/frames.ts</files>
  <read_first>
    - `relay/tests/helpers/frames.ts` 전문 — 특히 `buildQuoteStateFrame`(:73), `buildAccountStateFrame`(:342, 벡터·중첩 문자열 순서의 본보기), `buildOrderRespFrame`(:274), `buildBareEnvelope`(:33)
    - `relay/src/generated/stock-dma/set-limit-chaser.ts` (16-01 재동기화본) — `startSetLimitChaser`/`addXxx`/`endSetLimitChaser` 접근자 이름
    - `relay/src/generated/stock-dma/{set-vi-trigger,limit-chaser-list,vi-order-item,vi-order-list,vi-order-notice,disable-strategies-resp}.ts`
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md` §Protocol A~D (269~434행) — 필드 표·타입·단위
  </read_first>
  <action>
`frames.ts` 에 아래 빌더와 입력 타입을 추가한다. **전부 `startXxx` + `addXxx` + `endXxx` 개별 호출**로 만든다 — `createSetLimitChaser(...)` 같은 위치 인자 생성 함수는 deprecated 슬롯 때문에 인자가 밀려도 컴파일되므로 **쓰지 않는다**(RESEARCH Pitfall 1). 문자열·벡터는 **테이블 빌더를 열기 전에** 전부 만든다(Pitfall 2).

- `FakeLimitChaserInput` + `buildSetLimitChaserRespFrame(input)` → msg_type 60, 슬롯 `set_limit_chaser`. 기본값은 `SAMPLE_ISIN` · `SAMPLE_ACCOUNT_NO` · `market:"K"` · `crud:"C"` · `exchange:"KRX"` · 매수/매도/한방/취소 게이트 전부 false. S→C 전용 4필드(`sellOrderQty` · `sellQtyTrackBaseline` · `sellEntryLatched` · `cancelQtyTrackBaseline`)도 **주입 가능**해야 한다(에코 표시 검증용).
- `buildLimitChaserListRespFrame(items: FakeLimitChaserInput[])` → msg_type 64, 슬롯 `limit_chaser_list`. 0건도 **길이 0 벡터로** 정상 응답한다(「무응답 금지」 규약).
- `FakeViTriggerInput` + `buildSetVITriggerRespFrame(input | null)` → msg_type 61. `input === null` 이면 **테이블 없는 빈 Envelope**(미등록 표현, RESEARCH §Protocol B). `orderAmountKrw` 는 **bigint(원 단위)**.
- `FakeViOrderItemInput` + `buildViOrderListFrame(items, isSnapshot)` → msg_type 72(`isSnapshot=true`) / 73(`false`), 슬롯 `vi_order_list`. `deadline110Ms`/`deadline119Ms` 는 bigint, `state` 는 `Pending|Accepted|Cancelling|Cancelled|Filled|Rejected` 중 하나, `orderNo` 기본 `""`(접수 전).
- `buildViOrderNoticeFrame(input)` → msg_type 56.
- `buildDisableStrategiesRespFrame({ disabledCount, viDisabled })` → msg_type 65.

기존 파일의 규율을 그대로 따른다: 각 빌더는 `Uint8Array`(길이 헤더 없는 Envelope 페이로드)를 반환하고, 입력 타입은 전 필드 optional + 기본값 상수.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test -- fake-gateway</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createSetLimitChaser(\|createSetVITrigger(\|createVIOrderItem(\|createVIOrderList(" relay/tests/helpers/frames.ts` == 0
    - `grep -c "export function build\(SetLimitChaserResp\|LimitChaserListResp\|SetVITriggerResp\|ViOrderList\|ViOrderNotice\|DisableStrategiesResp\)Frame" relay/tests/helpers/frames.ts` == 6
    - `buildSetVITriggerRespFrame(null)` 이 반환한 페이로드를 `Envelope.getRootAsEnvelope` 로 읽으면 `msgType() === 61` 이고 `setVITrigger() === null`
    - `buildViOrderListFrame([], true)` 가 `isSnapshot() === true` + `itemsLength() === 0` 인 Envelope 를 만든다
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>전략 응답 프레임 빌더 6종 + 입력 타입이 존재하고 relay 테스트가 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: FakeGateway 전략 응답 주입 API 5종</name>
  <files>relay/tests/helpers/fake-gateway.ts, relay/tests/fake-gateway.test.ts</files>
  <read_first>
    - `relay/tests/helpers/fake-gateway.ts` 전문 — `FakeGateway` 타입(:68~110), `pushQuote`/`pushTape`/`respondLogin` 구현부, `onFrame`/`readMsgType` 요청 관찰 경로
    - `relay/tests/fake-gateway.test.ts` — 기존 검증 형태
    - `relay/src/dma/msg-type.ts` — `MSG` 상수(16-04 에서 확장되지만 여기서는 **숫자 리터럴을 직접 쓰지 않고** 필요한 값만 지역 상수로 둔다)
  </read_first>
  <action>
`FakeGateway` 타입과 팩토리에 아래 5개를 추가한다(기존 `pushQuote`(:94) 형태 그대로 — 소켓 인자를 받고 `sendFrame` 으로 내보낸다):

- `respondLimitChaserList(items: FakeLimitChaserInput[])` — 이후 수신하는 `GetLimitChaserListReq(24)` 에 대해 **자동으로** 64 응답. 목록은 마지막 지정값을 유지한다(`respondLogin` 의 자동응답 패턴 동형).
- `respondViTrigger(cfg: FakeViTriggerInput | null)` — `GetVITriggerReq(21)` 수신 시 61 자동 응답. `null` 이면 빈 테이블 61.
- `respondViOrderList(items: FakeViOrderItemInput[])` — `GetVIOrderListReq(34)` 수신 시 72(is_snapshot=true) 자동 응답.
- `pushLimitChaserEcho(sock, cfg: FakeLimitChaserInput)` — 60 즉시 주입(에코).
- `pushViOrderList(sock, items, snap: boolean)` — 72/73 주입.
- `pushOrderResp(sock, input: FakeOrderRespInput)` — 기존 `buildOrderRespFrame` 을 쓰는 51 주입(주문 wss 상관 테스트용).

요청 관찰(`declaredAccounts` 형태)도 넓힌다: `strategyRequests(): { msgType: number; payload: Buffer }[]` 를 추가해 10/11/14/33 수신 여부와 페이로드를 테스트가 직접 파싱할 수 있게 한다. **게이트웨이 업무 로직은 흉내 내지 않는다**(파일 상단 규율) — 자동 응답은 24/21/34 3종의 「무응답 금지」 규약을 재현하기 위한 것뿐이고, 10/11/14/33 은 **받아 기록만** 한다.

`relay/tests/fake-gateway.test.ts` 에 케이스를 추가한다: ① 24 요청 → 64 응답 프레임이 소켓으로 나간다 ② 21 요청 → `respondViTrigger(null)` 이면 빈 61 이 나간다 ③ 34 요청 → 72(snapshot) 가 나간다 ④ 10 송신 시 `strategyRequests()` 에 msgType 10 이 기록된다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test -- fake-gateway</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "respondLimitChaserList\|respondViTrigger\|respondViOrderList\|pushLimitChaserEcho\|pushViOrderList\|pushOrderResp\|strategyRequests" relay/tests/helpers/fake-gateway.ts` >= 14 (타입 선언 + 구현)
    - `pnpm --filter @gh-radar/relay test -- fake-gateway` exit 0 이고 신규 4 케이스가 실행된다
    - `grep -rn "10\.41\.1\.120" relay/tests/` 결과 0건
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>FakeGateway 가 전략 요청 3종에 자동 응답하고 60/72/73/51 을 주입할 수 있으며 10/11/14/33 수신을 기록한다.</done>
</task>

<task type="auto">
  <name>Task 3: LocalRelay 표면 확장 + E2E 8090 포트 직렬화</name>
  <files>webapp/e2e/fixtures/relay.ts, webapp/playwright.config.ts</files>
  <read_first>
    - `webapp/e2e/fixtures/relay.ts` — `LocalRelay` 인터페이스(:88~109), `pushQuoteFixture`(:269), relay 프로세스 기동부
    - `webapp/playwright.config.ts` — `fullyParallel: true` / `workers: process.env.CI ? 1 : undefined` / projects(setup·chromium) / webServer env `NEXT_PUBLIC_RELAY_WS_URL`
    - `webapp/e2e/specs/orderbook.spec.ts` 상단 주석 ③ 「왜 serial 인가」 — 8090 고정 포트 근거
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md` Pitfall 20 (945~950행)
  </read_first>
  <action>
① `LocalRelay` 인터페이스에 Task 2 의 6개 주입 API 를 **위임 형태로** 노출한다(`relay.gateway.pushLimitChaserEcho(...)` 를 매번 쓰지 않아도 되게, 그리고 소켓 인자를 픽스처가 알아서 채우도록): `seedLimitChasers(items)` · `seedViTrigger(cfg|null)` · `seedViOrders(items)` · `pushLimitChaserEcho(cfg)` · `pushViOrderList(items, snap)` · `pushOrderResp(input)`. 현재 연결된 게이트웨이 소켓은 픽스처가 이미 알고 있으므로(기존 호가 주입 경로와 동일) spec 이 소켓을 다루지 않게 한다. `reset()` 에 전략 시드 3종 초기화를 추가한다 — 시드가 spec 사이에 새면 다음 spec 이 남의 전략을 본다.

② **포트 충돌 해결 = Playwright 전역 `workers: 1`.** `NEXT_PUBLIC_RELAY_WS_URL` 이 빌드 시점 인라인이라 spec 이 런타임에 임의 포트를 쓸 수 없고, `test.describe.configure({mode:'serial'})` 은 **파일 내부만** 직렬화한다. 따라서 `webapp/playwright.config.ts` 의 `workers` 를 `1` 로 고정하고(`process.env.CI ? 1 : undefined` → `1`), 바로 위에 근거 주석을 남긴다: relay 를 쓰는 spec 이 `orderbook`·`trading-limit-chaser`·`trading-vi`·`me`·`sidebar-tree` 5개로 늘어 파일 간 병렬 시 8090 EADDRINUSE 가 확정적으로 발생한다는 것, `fullyParallel` 은 파일 간 병렬을 막지 못한다는 것. `fullyParallel: true` 자체는 유지해도 무해하나 오해를 부르므로 `false` 로 내리고 그 이유도 같은 주석에 적는다.

③ 신규 relay spec 4종이 지켜야 할 규약을 `fixtures/relay.ts` 파일 상단 주석에 **한 곳으로** 못박는다: 파일마다 `test.describe.configure({ mode: 'serial' })` + `withLocalRelay()` 는 `beforeAll` 1회 + `afterAll` 에서 `stop()` + `beforeEach` 에서 `reset()`. 실서버 IP·실계좌 리터럴 금지(D-27).
  </action>
  <verify>
    <automated>grep -n "workers: 1" webapp/playwright.config.ts &amp;&amp; pnpm --filter gh-radar-webapp test:e2e -- orderbook</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "workers: 1" webapp/playwright.config.ts` == 1 이고 `grep -c "process.env.CI ? 1 : undefined" webapp/playwright.config.ts` == 0
    - `grep -c "seedLimitChasers\|seedViTrigger\|seedViOrders\|pushLimitChaserEcho\|pushViOrderList\|pushOrderResp" webapp/e2e/fixtures/relay.ts` >= 12
    - `pnpm --filter gh-radar-webapp test:e2e -- orderbook` exit 0 (기존 회귀 green, EADDRINUSE 0건)
    - `grep -rn "10\.41\.1\.120" webapp/e2e/` 결과 0건
  </acceptance_criteria>
  <done>LocalRelay 가 전략 프레임을 주입할 수 있고, E2E 가 단일 워커로 직렬 실행되어 8090 충돌이 구조적으로 불가능하다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 테스트 하네스 → relay 프로세스 | 스텁 게이트웨이가 신뢰되지 않은 바이트를 relay 파서에 밀어 넣는다 |
| 테스트 코드 → 실서버(KB 게이트웨이) | **넘지 않는다** — 리터럴조차 남기지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Tampering | `relay/tests/helpers/frames.ts` | mitigate | 위치 인자 생성 함수 금지(`create*` grep 0건) + 문자열/벡터를 테이블 빌더 열기 전에 생성 — 잘못 조립된 버퍼로 파서를 검증하면 테스트가 거짓 green 이 된다 |
| T-16-09 | Information Disclosure | E2E 픽스처 | mitigate | 실서버 IP·실계좌 리터럴 0건 grep 게이트(D-27). `seedDmaCredential` 은 기존 테스트 전용 암호화 경로만 사용 |
| T-16-06 | Denial of Service | Playwright 8090 고정 포트 | accept | 워커 1개 고정으로 충돌을 없애는 대신 E2E 전량 소요가 늘어난다(~5–8분, VALIDATION 예상치 내). 대안(런타임 포트 주입)은 `NEXT_PUBLIC_*` 빌드 인라인 제약으로 불가 |
</threat_model>

<verification>
- `pnpm typecheck` exit 0
- `pnpm --filter @gh-radar/relay test` exit 0
- `pnpm --filter gh-radar-webapp test:e2e -- orderbook` exit 0
</verification>

<success_criteria>
- 전략 프레임 6종을 테스트에서 만들 수 있다
- FakeGateway 가 24/21/34 에 자동 응답하고 60/72/73/51 을 주입한다
- LocalRelay 가 같은 표면을 spec 에 노출한다
- Playwright 가 단일 워커로 돌아 relay spec 파일 간 포트 충돌이 없다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-02-SUMMARY.md` when done
</output>
