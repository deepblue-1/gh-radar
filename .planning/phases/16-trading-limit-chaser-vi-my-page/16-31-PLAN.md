---
phase: 16-trading-limit-chaser-vi-my-page
plan: 31
type: execute
wave: 19
depends_on: ["16-19", "16-25"]
gap_closure: true
files_modified:
  - webapp/src/components/trading/limit-chaser-form.tsx
  - webapp/src/components/trading/limit-chaser-client.tsx
  - webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx
  - webapp/src/components/trading/__tests__/limit-chaser-client.test.tsx
autonomous: true
requirements: [TRADE-01]

must_haves:
  truths:
    - "「수정」 버튼도 무장 판정을 지난다 — 게이트가 켜져 있는데 발주가·수량이 0 이면 relay 에 통째로 거부되기 전에 화면이 사유를 말한다 (GC-WR-09). 파일 머리말의 「전송 직전 가드」 주장이 사실이 된다"
    - "상따 폼의 `send()` 호출 2곳이 반환값을 읽는다 — 보내지 못한 요청에 낙관 반영·상태 전환을 걸지 않는다 (GC-WR-06 상따 폼 2곳 / 16-19 의 boolean 계약)"
    - "무장 불가 안내가 원인을 값으로 가른다 — 「시세 미수신(가격 0)」과 「주문금액 부족(수량 0)」이 서로 다른 문구다 (GC-WR-12)"
    - "`gateBlocked` 의 `useCallback` 이 실제로 메모된다 — 매 렌더 새 객체가 의존성에 들어가지 않는다 (GC-IN-01)"
    - "`isPickable` 이 타입 서술자라 `row.isin as string` 단언이 필요 없다 (GC-IN-02)"
  artifacts:
    - path: "webapp/src/components/trading/limit-chaser-form.tsx"
      provides: "handleSubmit 무장 가드 + send 반환값 분기 + 원인별 안내 문구 + 메모된 gateBlocked"
    - path: "webapp/src/components/trading/limit-chaser-client.tsx"
      provides: "타입 서술자 isPickable"
      contains: "row is StockDetailResponse"
  key_links:
    - from: "limit-chaser-form.tsx (handleSubmit)"
      to: "limit-chaser-form.tsx (gateBlocked)"
      via: "전송 직전 같은 판정 함수 호출"
      pattern: "gateBlocked"
    - from: "limit-chaser-form.tsx (send 호출부)"
      to: "webapp/src/lib/use-relay-socket.ts"
      via: "boolean 반환값 분기 (16-19 계약)"
      pattern: "if \\(!send\\("
---

<objective>
상따 폼·클라의 **GC-WR-09 · GC-WR-12 · GC-IN-01 · GC-IN-02 · GC-WR-06(상따 폼 2곳)** 을 닫는다. 공통점은 「이 화면이 **자기 주석이 말하는 대로 동작하지 않는다**」이다.

지금 상태:
- **GC-WR-09.** 파일 머리말(`:26`)은 「판정은 `gateBlocked()` 하나이고 렌더의 `disabled` 와 **전송 직전 가드**가 그것을 함께 읽는다」고 적었지만, 실제로 읽는 것은 `toggleGate`(`:394`)뿐이다. 「수정」 경로(`handleSubmit`, `:405-415`)는 `submitting || disabled` 만 본다. 서버 에코로 `buyEnabled: true` 를 받은 상태에서 시세가 끊겨 가격 칸이 0 이 되면 「수정」이 relay 의 `#strategyArmable` 에 **통째로 거부**되고 — 사용자는 **아무 값도 저장하지 못한 채** 일반 거부 프레임 한 줄만 본다.
- **GC-WR-06(상따 폼).** 16-19 가 `send` 를 `void → boolean` 으로 바꾼 목적은 「호출부가 반환값으로 **보내지 않았음**을 알 수 있어야 한다」였다. 실제로 그것을 읽는 곳은 `strategy-status-card.tsx:354` 하나뿐이고, 이 파일의 두 호출부(`:398` `toggleGate`, `:412` `handleSubmit`)는 반환값을 버린다. `disabled` 가드가 대부분을 막지만 `ready` 표시와 소켓 `readyState` 가 어긋나는 창(재접속 직전 · `onclose` 직전)이 정확히 이 경로다.
- **GC-WR-12.** `ARM_BLOCKED_TEXT.buy` 는 「**시세를 받지 못해** 발주가·수량이 0 이에요」라고 단정한다. 그런데 같은 phase 의 e2e(`trading-limit-chaser.spec.ts:181-192`)가 고정한 실제 재현 조건은 「기본 주문금액 10만원으로 127,400원 종목을 사면 `floor(10만/12.74만) = 0주`」다 — **시세는 정상이고 금액이 부족한 것**이다. 안전 게이트의 안내가 원인을 틀리게 말하면 사용자는 엉뚱한 곳(재접속·새로고침)을 만진다.
- **GC-IN-01 / GC-IN-02.** `canArm` 은 렌더마다 새로 만드는 객체 리터럴이라 `useCallback([disabled, canArm])` 이 아무것도 메모하지 않는다. `isPickable(row)` 이 `row.isin !== null` 을 확인하는데도 TS 가 좁히지 못해 `row.isin as string` 단언으로 메웠다.

Purpose: TRADE-01 의 안전 게이트가 **말한 대로 동작하고, 옳은 원인을 말하게** 한다.
Output: `handleSubmit` 무장 가드 · send 반환값 분기 2곳 · 원인별 문구 · 메모된 `gateBlocked` · 타입 서술자.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-19-SUMMARY.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-25-SUMMARY.md
</context>

<interfaces>
<!-- 손대는 자리의 현재 계약. -->

`webapp/src/components/trading/limit-chaser-form.tsx`:
- `const ARM_BLOCKED_TEXT = { buy, sell, sweep } as const`(:113-118) — 무장 불가 안내 3종
- 파생값: `buyQty = buyOrderQtyFromAmount(form.buyOrderAmount, form.buyOrderPrice)` · `sellQty = estimatedSellQty(...)`(표시 전용)
- `canArmBuy = form.buyOrderPrice > 0 && buyQty > 0` · `canArmSell = form.sellOrderPrice > 0 && form.sellWatchQty > 0` · `canArmSweep = form.sweepWatchPrice > 0 && canArmBuy`
- `const canArm: Record<'buyEnabled'|'sellEnabled'|'sweepEnabled', boolean> = {...}` — **매 렌더 새 객체**
- `gateBlocked = useCallback((key, next) => { if (disabled) return true; if (!next) return false; return !canArm[key]; }, [disabled, canArm])` — ★ 「끄는 것은 언제나 허용한다」(T-16-44)
- `toggleGate = useCallback((key, next) => { if (gateBlocked(key, next)) return; …; send({ t:'lc.set', cfg }); sentNotifyRef.current?.(cfg); }, [...])`
- `handleSubmit = useCallback(() => { if (submitting || disabled) return; setSubmitting(true); const cfg = buildCfg(formRef.current); send({ t:'lc.set', cfg }); sentNotifyRef.current?.(cfg); }, [...])`
- `send: (msg: RelayInbound) => boolean` (16-19 계약. 미연결이면 `console.error` 후 `false`)

`webapp/src/components/trading/limit-chaser-client.tsx`:
- `const ORDERABLE_MARKETS: readonly string[] = ['KOSPI','KOSDAQ']`
- `function isPickable(row: StockDetailResponse): boolean { return row.isin !== null && ORDERABLE_MARKETS.includes(row.market); }`
- 소비부 `:847-872` — `isin: row.isin as string`(:851)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 「수정」도 무장 판정을 지나고, 두 `send` 호출부가 반환값을 읽는다 (GC-WR-09 + GC-WR-06 상따 폼 2곳)</name>
  <files>webapp/src/components/trading/limit-chaser-form.tsx, webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx</files>
  <read_first>
    - `webapp/src/components/trading/limit-chaser-form.tsx` L1~40 (파일 머리말 — 특히 `:26` 「전송 직전 가드가 그것을 함께 읽는다」. **이 문장을 사실로 만드는 것이 이 task 다**), L335~420 (`canArm*` 3식 · `gateBlocked` · `toggleGate` · `handleSubmit` · `handleRevert`), 그리고 `submitting` 이 언제 풀리는지(에코 수신? 타임아웃?)를 확인한다 — 조기 반환에서 `setSubmitting(true)` 를 남기면 버튼이 영구히 잠긴다
    - `webapp/src/lib/use-relay-socket.ts` L855~880 — `send()` 의 boolean 계약과 `console.error` 문구(16-19)
    - `webapp/src/components/trading/strategy-status-card.tsx` L350~370 — **이미 반환값을 읽는 유일한 호출부**. 실패 문구·`awaitingAck` 미설정 관례를 그대로 따른다
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §GC-WR-09 · §GC-WR-06 — Issue + Fix 스니펫
    - `webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx` — 23케이스의 `send` 스텁 방식(반환값을 무엇으로 두는지)과 문구 단언 방식
  </read_first>
  <action>
① `handleSubmit` 에 무장 판정을 넣는다. `submitting || disabled` 조기 반환 **뒤**, `setSubmitting(true)` **앞**에 둔다(잠금 전에 막아야 버튼이 살아 있다). 16-REVIEW §GC-WR-09 의 Fix 스니펫을 참조하라: 세 게이트 키 중 **현재 폼에서 켜져 있으면서** `gateBlocked(key, true)` 인 첫 키를 찾아, 있으면 그 키의 안내 문구를 표시하고 반환한다. 표시 채널은 이 파일이 이미 쓰는 것(예: `submitError` 상태와 그 렌더 자리)을 그대로 쓴다 — 새 UI 를 만들지 않는다.

② `send` 호출부 2곳을 반환값 분기로 바꾼다.
  - `toggleGate`: `if (!send({ t:'lc.set', cfg })) { …실패 표시…; return; }` — **`setForm(values)` 낙관 반영과 `sentNotifyRef.current?.(cfg)` 를 걸지 않는다.** 스위치가 켜진 것처럼 보이는데 아무것도 안 나간 상태가 이 화면 최악의 결과다. 이미 `setForm` 이 앞서 실행되는 구조라면 **순서를 바꾸어** 전송 성공 후에만 반영되게 한다(그 변경의 근거를 주석에 적는다).
  - `handleSubmit`: 같은 규율. 실패면 `setSubmitting(false)` 로 되돌리고 실패 문구를 남긴다.
  - 실패 문구는 `strategy-status-card.tsx` 의 「연결이 끊겨…」 계열과 **같은 어조**로 맞춘다.

③ 머리말 주석(`:26`)을 사실과 맞춘다 — 「렌더의 `disabled` 와 **전송 직전 가드 2곳**(`toggleGate`·`handleSubmit`)이 그것을 함께 읽는다」로 갱신하고, 뒤에 「(GC-WR-09 이전에는 `toggleGate` 만 읽었다)」를 남긴다.

④ 테스트를 추가한다(`limit-chaser-form.test.tsx`).
  - 「게이트가 켜진 채 발주가 0 인 상태에서 「수정」을 누르면 `send` 가 호출되지 않고 안내 문구가 뜬다」
  - 「`send` 가 `false` 를 돌려주면 스위치 낙관 반영이 걸리지 않고 실패 문구가 뜬다」
  - 「`send` 가 `false` 를 돌려주면 「수정」 버튼이 다시 눌릴 수 있다(`submitting` 이 풀린다)」
  기존 23케이스는 그대로 통과해야 한다.
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp exec vitest run src/components/trading/__tests__/limit-chaser-form.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "if (!send(" webapp/src/components/trading/limit-chaser-form.tsx` >= 2 — 두 호출부가 반환값을 읽는다
    - `handleSubmit` 본문에 `gateBlocked` 호출이 있다 (줄번호를 SUMMARY 에 인용)
    - `grep -c "gateBlocked" webapp/src/components/trading/limit-chaser-form.tsx` >= 4 (정의 + toggleGate + handleSubmit + 렌더 disabled)
    - `pnpm --filter gh-radar-webapp exec vitest run src/components/trading/__tests__/limit-chaser-form.test.tsx` exit 0 — 신규 3케이스 포함
    - `pnpm --filter gh-radar-webapp run typecheck` exit 0
  </acceptance_criteria>
  <done>「수정」이 무장 판정을 지나고, 보내지 못한 요청에 어떤 낙관 반영도 걸리지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: 무장 불가 안내가 원인을 값으로 가르고, `gateBlocked` 가 실제로 메모된다 (GC-WR-12 + GC-IN-01)</name>
  <files>webapp/src/components/trading/limit-chaser-form.tsx, webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx</files>
  <read_first>
    - `webapp/src/components/trading/limit-chaser-form.tsx` L105~125 (`ARM_BLOCKED_TEXT` 선언 + 그 위 「발생 조건은 시세를 못 받은 종목이다」 문단 — **이 전제가 틀렸다는 것이 GC-WR-12 다**), L335~395 (`buyQty` 산출 · `canArm*` · `canArm` 객체 리터럴 · `gateBlocked` 의존성 배열)
    - `webapp/e2e/trading-limit-chaser.spec.ts` L175~200 — 실제 재현 조건(기본 주문금액 10만원 · 127,400원 종목 → 0주). **이 케이스의 문구 단언이 있으면 함께 갱신해야 한다**
    - `webapp/src/lib/limit-chaser.ts` — `buyOrderQtyFromAmount` 의 산출식(내림)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §GC-WR-12 · §GC-IN-01 — Fix 스니펫
  </read_first>
  <action>
① 매수 갈래 안내를 **값으로 가른다**. 16-REVIEW §GC-WR-12 의 Fix 를 참조하라: `form.buyOrderPrice === 0` 이면 「시세를 받지 못해 매수가격이 0 이에요…」, 아니면(가격은 있는데 `buyQty === 0`) 「주문금액이 매수가격보다 작아 주문수량이 0 주예요. 금액을 올리면 켤 수 있어요.」. 상수 객체를 유지할지 함수로 바꿀지는 실행자 판단이되, Task 1 의 `handleSubmit` 도 **같은 함수/값을 읽어야** 한다(문구가 두 곳에 갈리면 그것이 다음 결함이다).

② `ARM_BLOCKED_TEXT` 위 문단의 잘못된 전제(「발생 조건은 시세를 못 받은 종목이다」)를 정정한다 — 두 원인이 있고 e2e 가 고정한 흔한 쪽이 금액 부족임을 적는다.

③ 매도·한방 갈래도 같은 기준으로 점검한다. `canArmSell` 은 `sellOrderPrice`·`sellWatchQty` 두 값이므로 문구가 그 둘을 가리키는지 확인하고, 어긋나면 맞춘다. 한방은 `sweepWatchPrice` 와 매수 조건 둘 다이므로 「어느 쪽이 0 인가」를 문구가 오도하지 않게 한다.

④ **GC-IN-01** — `canArm` 을 `useMemo` 로 감싸(의존성은 세 파생 boolean) `gateBlocked` 의 `useCallback` 이 실제로 메모되게 한다. 또는 세 boolean 을 개별 의존성으로 넘긴다. 어느 쪽이든 「의존성 배열에 매 렌더 새 객체가 들어가지 않는다」가 결과다. `useMemo` 의존성은 `canArmBuy`/`canArmSell`/`canArmSweep` 세 값이면 충분하다는 근거를 주석에 한 줄 남긴다.

⑤ 테스트를 추가한다.
  - 「매수가격 0 이면 시세 문구가, 매수가격은 있고 주문금액이 부족하면 금액 문구가 뜬다」 — 두 상태를 각각 렌더해 문구를 단언한다.
  - e2e 에 문구 단언이 있으면 새 문구로 갱신한다(단언이 없으면 만들지 않는다 — 이 plan 의 범위는 단위 테스트다).
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp exec vitest run src/components/trading/__tests__/limit-chaser-form.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "주문금액이 매수가격보다 작아" webapp/src/components/trading/limit-chaser-form.tsx` >= 1
    - `grep -c "시세를 받지 못해 발주가·수량이 0 이에요" webapp/src/components/trading/limit-chaser-form.tsx` == 0 — 원인을 뭉뚱그린 옛 문구가 사라졌다
    - `grep -c "useMemo" webapp/src/components/trading/limit-chaser-form.tsx` >= 1 이거나 `gateBlocked` 의존성 배열에 `canArm` 객체가 없다 (둘 중 무엇을 골랐는지 SUMMARY 에 명시)
    - 두 문구 케이스가 테스트 파일에 존재 (케이스 이름을 SUMMARY 에 인용)
    - `pnpm --filter gh-radar-webapp exec vitest run src/components/trading/__tests__/limit-chaser-form.test.tsx` exit 0
    - `pnpm --filter gh-radar-webapp exec eslint src/components/trading/limit-chaser-form.tsx` exit 0 — hooks 의존성 규칙 경고 0건
  </acceptance_criteria>
  <done>안내 문구가 사용자가 실제로 만져야 할 곳을 가리키고, 메모가 실제로 작동한다.</done>
</task>

<task type="auto">
  <name>Task 3: `isPickable` 을 타입 서술자로 만들어 `as string` 단언을 없앤다 (GC-IN-02)</name>
  <files>webapp/src/components/trading/limit-chaser-client.tsx, webapp/src/components/trading/__tests__/limit-chaser-client.test.tsx</files>
  <read_first>
    - `webapp/src/components/trading/limit-chaser-client.tsx` L770~800 (`ORDERABLE_MARKETS` + `isPickable` 선언과 그 위 ★ 「타입은 KOSPI|KOSDAQ 이라고 말하지만 런타임은 그렇지 않다」 주석 — **이 주석은 유지한다**), L840~880 (소비부 — `:851` `row.isin as string` 과 `:847·849·872` 의 다른 사용처)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §GC-IN-02 — 제안 시그니처
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-25-SUMMARY.md` — `isPickable` 을 만든 plan 의 의도(시장 미상 종목 선택 차단, truth 44)
  </read_first>
  <action>
① `isPickable` 시그니처를 타입 서술자로 바꾼다: `function isPickable(row: StockDetailResponse): row is StockDetailResponse & { isin: string }`. 본문은 그대로다.

② 소비부에서 `row.isin as string` 단언을 제거한다. 서술자가 좁히지 못하는 자리가 남으면(예: `.filter(isPickable)` 를 거치지 않고 개별 호출로 좁히는 흐름) 흐름을 서술자가 좁힐 수 있는 형태로 바꾼다 — **단언을 다른 곳으로 옮기지 않는다.**

③ 16-25 truth 44 의 규율(「시장 미상 종목은 `isin === null` 과 같은 취급으로 선택 차단」)이 그대로인지 확인한다. `ORDERABLE_MARKETS` 판정은 손대지 않는다.

④ 기존 `limit-chaser-client.test.tsx` 가 선택 차단을 잠그고 있다면 그대로 통과해야 한다. 잠그지 않고 있다면 「시장 미상 행은 선택되지 않는다」 케이스 1개를 추가한다.
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp exec vitest run src/components/trading/__tests__/limit-chaser-client.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "row is StockDetailResponse" webapp/src/components/trading/limit-chaser-client.tsx` >= 1
    - `grep -c "row.isin as string" webapp/src/components/trading/limit-chaser-client.tsx` == 0
    - `grep -c "as string" webapp/src/components/trading/limit-chaser-client.tsx` 값이 변경 전보다 줄었다 (전후 값을 SUMMARY 에 인용) — 단언을 다른 곳으로 옮기지 않았다
    - `pnpm --filter gh-radar-webapp run typecheck` exit 0
    - `pnpm --filter gh-radar-webapp exec vitest run src/components/trading/__tests__/limit-chaser-client.test.tsx` exit 0
  </acceptance_criteria>
  <done>타입이 런타임 검사와 같은 것을 말하고, 단언이 사라졌다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 UI ↔ 소켓 실제 상태 | `status === 'ready'` 표시와 소켓 `readyState` 가 어긋나는 창이 존재한다 (재접속 직전 · `onclose` 직전) |
| 안내 문구 → 사용자의 다음 행동 | 안전 게이트가 원인을 틀리게 말하면 사용자가 엉뚱한 곳을 만지고, 그 사이 시장은 움직인다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-59 | Repudiation (조용한 미전송) | `toggleGate`·`handleSubmit` 의 `send` 반환값 | mitigate | 두 호출부가 boolean 을 읽고, 실패 시 낙관 반영·`submitting` 잠금 어느 것도 걸지 않으며 문구를 남긴다 (16-19 계약 이행) |
| T-16-60 | Denial of Service (저장 불가) | `handleSubmit` 의 무장 판정 부재 | mitigate | 전송 전에 `gateBlocked` 를 지나 relay 통째 거부를 막는다. 게이트를 **끄는** 방향은 여전히 막지 않는다 (T-16-44 승계) |
| T-16-61 | 사용자 오도 | `ARM_BLOCKED_TEXT.buy` | mitigate | 원인을 값(`buyOrderPrice === 0` vs `buyQty === 0`)으로 갈라 서로 다른 문구를 낸다. e2e 가 고정한 재현 조건과 일치시킨다 |
| T-16-44 | 자산 인질 | 무장 해제 경로 | mitigate | `gateBlocked` 의 「끄는 것은 언제나 허용」 규율을 **바꾸지 않는다.** Task 1 의 새 가드도 켜는 방향에만 적용 |
| T-16-13 | 운영 사고 | 실계좌 조작 | accept | 단위 테스트·스텁만 쓴다 (D-27 승계) |
</threat_model>

<verification>
- `pnpm --filter gh-radar-webapp test` exit 0
- `pnpm --filter gh-radar-webapp run typecheck` exit 0
- `pnpm --filter gh-radar-webapp exec eslint src/components/trading/limit-chaser-form.tsx src/components/trading/limit-chaser-client.tsx` exit 0
</verification>

<success_criteria>
- 「수정」이 무장 판정을 지난다
- 상따 폼의 `send` 2곳이 반환값을 읽는다
- 안내 문구가 원인을 값으로 가른다
- `gateBlocked` 메모가 실제로 작동한다
- `as string` 단언이 사라졌다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-31-SUMMARY.md` when done
</output>
