---
phase: 16-trading-limit-chaser-vi-my-page
plan: 13
type: execute
wave: 12
depends_on: ["16-10", "16-12"]
files_modified:
  - webapp/src/components/orderbook/orderbook-ladder.tsx
  - webapp/src/components/trading/strategy-log.tsx
  - webapp/src/components/trading/limit-chaser-client.tsx
  - webapp/src/app/trading/limit-chaser/new/page.tsx
  - webapp/src/app/trading/limit-chaser/[key]/page.tsx
  - webapp/e2e/specs/trading-limit-chaser.spec.ts
autonomous: true
requirements: [TRADE-01]

must_haves:
  truths:
    - "상따 페이지가 종목·거래소·계좌 · 상태줄 · 호가 10단 · 매수/매도 폼 · 미체결/잔고 · 전략 로그로 구성된다 (UI-SPEC A1~A13)"
    - "데스크톱 ≥1280 은 3열(호가 460 | 매수 250 | 매도 250), 모바일 390 은 2열(호가 42% | 매수·매도 탭 58%)이다 (UI-SPEC R2/R7)"
    - "종목을 고르면 가격 5칸이 상한가로 1회 시딩되고, 이후에는 서버 에코가 이긴다 (D-11)"
    - "서버 거부(ServerMessage ERROR)가 상태줄과 전략 로그 양쪽에 남는다 (PC-7 · Pitfall 8)"
    - "전략 편집 페이지가 전략 키로 진입하고 두 스위치 OFF 후 빈 폼으로 돌아간다 (D-08)"
    - "더티 상태로 페이지를 벗어나려 하면 경고한다 (UI-SPEC §조작 규율 7)"
    - "레이아웃·색·간격은 사용자 승인 목업 `16-limit-chaser-mockup.html` 을 정본으로 따른다 (D-24 채택안, D-24(b) 는 UI-SPEC C1/FLAG-1 로 중립 확정)"
  artifacts:
    - path: "webapp/src/components/trading/limit-chaser-client.tsx"
      provides: "상따 페이지 조립 · 전역 전략 스냅샷 소비 · 종목 선택"
      contains: "data-slot=\"limit-chaser-page\""
    - path: "webapp/src/components/trading/strategy-log.tsx"
      provides: "에코·ServerMessage 누적 로그(브라우저 메모리)"
      contains: "data-slot=\"strategy-log\""
    - path: "webapp/src/components/orderbook/orderbook-ladder.tsx"
      provides: "호가 10단 + 데스크톱 최근 체결 10건 변형"
      contains: "recentTrades"
  key_links:
    - from: "webapp/src/components/trading/limit-chaser-client.tsx"
      to: "webapp/src/lib/relay-provider.tsx"
      via: "useRelaySubscription(선택 종목) + limitChasers 스냅샷"
      pattern: "useRelaySubscription"
---

<objective>
상따 페이지를 조립한다 — 16-12 의 폼을 실제 화면에 앉히고 보조 패널(호가 10단 + 최근 체결 · 미체결/잔고 · 전략 로그)을 붙인다.

- `orderbook-ladder.tsx` 에 **데스크톱 최근 체결 10건 변형**을 추가한다(매수호가 왼쪽 열, 매수 10단 행과 1:1 정렬, 「체결」 헤더 행 20px). 모바일에는 렌더하지 않는다.
- `strategy-log.tsx`: 에코 상태 전이 + `ServerMessage(54)` 를 문장으로 누적(브라우저 메모리, 새로고침 시 소실 고지).
- `limit-chaser-client.tsx`: 종목 선택(검색) · 거래소 세그먼트 · 계좌 `<select>` · 상태줄 · 에코 배너(6초) · 폼 · 계좌 패널 · 로그를 배치하고 라우트 2개(`new` / `[key]`)에 연결.

Purpose: 이 페이지가 phase 의 가장 큰 단일 표면이다. 목업(`16-limit-chaser-mockup.html`)이 정본이며 문서와 어긋나면 목업을 따른다.
Output: 호가 변형 · 전략 로그 · 클라이언트 셸 · E2E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 호가 10단 + 최근 체결 변형 · 전략 로그</name>
  <files>webapp/src/components/orderbook/orderbook-ladder.tsx, webapp/src/components/trading/strategy-log.tsx</files>
  <read_first>
    - `webapp/src/components/orderbook/orderbook-ladder.tsx` 전문 — 현재 열 구성·`data-slot`(:251,:365)·행 높이·현재가 표시 방식(레이아웃 폭을 차지하지 않는 방식이어야 함 — lessons.md 「틱마다 좌우로 흔들린다」)
    - `webapp/src/components/orderbook/trade-tape.tsx` — 체결 데이터 형태(시각·가격·수량·방향)
    - `webapp/src/components/orderbook/relay-status-bar.tsx` — `data-slot="relay-alerts"` 누적 리스트(:218~), props 계약
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md` §A11/A11a/A13(호가·최근체결·로그) · §C8 호가 색 · §T3 10px 예외 2곳 · §호가 범례·로그 캡션 문구(458~492행)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-limit-chaser-mockup.html` — 호가 카드 마크업 **정본**(434px 표 · 148/139/148 열 · 24px 행 · 마커 슬롯 16px)
  </read_first>
  <action>
① `orderbook-ladder.tsx` 확장(기존 사용처 회귀 금지 — 새 prop 은 전부 optional):
   - `recentTrades?: RelayTapeEntry[]` 를 받으면 **데스크톱에서만** 매수호가 왼쪽 열(= 매도잔량과 같은 열)에 최근 체결 10건을 그린다: `시각`(10px `--muted-fg`) · `체결가`(`--fg` 600) · `체결량`(매수 `--up` / 매도 `--down`, 600). **매수 10단 행과 1:1 정렬**, 첫 매수행 위에 「체결」 헤더 행(20px, `--border` 상단선), 셀 패딩 4px. 체결이 없으면 셀을 비운다.
   - **모든 행에 고정폭 16px 마커 슬롯**(상한가 = 「상」 아이콘 `--up`, 최근 체결가 = 6px 도트, 그 외 빈 슬롯) — 마커가 붙고 떨어져도 가격이 좌우로 밀리지 않아야 한다(lessons.md 부수 교훈).
   - 가격 오른쪽에 **등락률**(기준가 대비, 소수 1자리, `%` 없음, 양수 부호 없음, 10px `--muted-fg`, min-width 40px 우측정렬). 10px 은 UI-SPEC T3 이 허용한 **정확히 2곳** 중 하나다.
   - 현재가 행 `1px solid --fg` outline. **가격은 클릭 대상이 아니다**(비교가격 자동 채움 없음, roving tabindex 없음) — 기존에 클릭 핸들러가 있으면 상따 변형에서는 비활성화한다.
   - 하단 범례 1줄: `● 최근 체결가 · 상 상한가 · 체결 수량 매수/매도`(모바일은 마지막 항목 제외).
   - 접근성: 데스크톱 `<table aria-label="호가 10단 (매도 10단계 · 매수 10단계) 및 최근 체결 10건">`, 모바일 `aria-label="호가 10단 (매도 10단계 · 매수 10단계)"`.
   - 모바일 변형(UI-SPEC R7): 행 32px, 가격(12px/600 방향색) + 등락률(10px) **2줄**, 우측 잔량 막대(`::before`) + 잔량 숫자(11px), 400px 독립 스크롤 + **현재가 행 중앙 초기 스크롤(최초 마운트 1회만)**.

② `strategy-log.tsx`(신규, analog = `relay-status-bar.tsx` 의 `relay-alerts` 누적 리스트): props `{ entries: StrategyLogEntry[] }`. 시각 + 문장, 최신 상단, 최대 높이 160px 스크롤, 빈 상태 `아직 반영된 이벤트가 없어요` / `스위치를 켜거나 값을 수정하면 서버 응답이 여기에 쌓여요.`, 캡션 `서버 에코 기준 · 새로고침 시 지워져요`. `data-slot="strategy-log"`.
   문장 생성은 **순수 함수**로 분리한다(`strategyLogLine(prev, next)` — 전이 종류를 6~8개로 고정: 무장/해제/발주/래치/취소무장/삭제/거부/서버통지). `ServerMessage(54)` 는 level·source 를 그대로 실어 `level==="ERROR"` 면 `--destructive` 텍스트. **15:40 자동 비활성화**는 로그 1줄 `15:40:00 서버가 모든 전략을 자동 비활성화했어요 (장 마감 규칙)`.
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp test -- orderbook-ladder &amp;&amp; pnpm --filter gh-radar-webapp test -- strategy-log</automated>
  </verify>
  <acceptance_criteria>
    - `recentTrades` prop 이 optional 이고 미전달 시 기존 렌더가 그대로(기존 orderbook RTL/E2E green)
    - `grep -c "16px\|w-4" webapp/src/components/orderbook/orderbook-ladder.tsx` 로 마커 슬롯 고정폭이 확인되고, 마커 유무에 따라 가격 셀 폭이 변하지 않음을 RTL 로 단언
    - 10px 사용처가 **최근 체결 시각 · 호가 등락률 2곳뿐**임을 grep 으로 확인(다른 표면 0건)
    - `strategyLogLine` 이 순수 함수로 export 되고 전이 6종 이상 단위 테스트 존재
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>호가 변형과 전략 로그가 존재하고 기존 호가 사용처가 회귀 없이 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: limit-chaser-client 조립 + 라우트 연결</name>
  <files>webapp/src/components/trading/limit-chaser-client.tsx, webapp/src/app/trading/limit-chaser/new/page.tsx, webapp/src/app/trading/limit-chaser/[key]/page.tsx</files>
  <read_first>
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-limit-chaser-mockup.html` — 전체 레이아웃 **정본**(데스크톱 3열 `.lc3` 460/250/250 gap16 · 모바일 2열 `.lc2` 42%/1fr gap8 · 전폭 하단)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-UI-SPEC.md` §A1~A14 · §동기화·알림 문구(522~530행) · §R1/R2/R7/R8
    - `webapp/src/components/stock/stock-orderbook-section.tsx` — 클라이언트 셸 형태(전역 구독 소비 · 상태 분기 · 스켈레톤)
    - `webapp/src/lib/stock-api.ts` — `searchStocks`(:21, 응답에 `isin` 포함) · `fetchStockDetail`(:31, `isin`/`upperLimit`/`basePrice`)
    - `webapp/src/components/search/global-search.tsx` — 인자를 받지 않으므로 **그대로 쓸 수 없다**(인라인 검색을 만든다)
    - `webapp/src/components/orderbook/{account-panel,relay-status-bar,order-panel}.tsx` — 재사용 대상 + `deriveTickSize`(order-panel:121 export)
    - `webapp/src/lib/limit-chaser.ts`(16-12) · `webapp/src/components/trading/{limit-chaser-form,dirty-action-bar,dma-gate,strategy-badge,strategy-log}.tsx`
  </read_first>
  <action>
`limit-chaser-client.tsx`(신규) 를 만들고 두 라우트에서 소비한다. `new` 는 `key` 없이, `[key]` 는 `decodeURIComponent(key)` 로 넘긴다.

**구성(위→아래)**
1. **종목·거래소·계좌 카드(A1, 전폭)**: 1행 = 종목(미선택 시 인라인 검색 — `searchStocks` 직접 호출, 선택 시 종목명·코드·현재가·등락률) + KRX/NXT 세그먼트(40px). 2행 = 계좌 `<select>`(계좌번호 **전체** + 상품명, `order-panel.tsx:451~473` 형태). 3행 = 가격 칩(상한가 `--up` / 기준가 / 하한가 `--down` / 호가단위 — `deriveTickSize` 재사용). **계좌 비밀번호 입력 없음.**
   - 종목 선택 시 `seedFromUpperLimit(upperLimit)` 로 가격 5칸 **1회** 시딩(이미 서버 전략이 있으면 시딩하지 않는다).
   - `[key]` 진입 시 key 를 분해해 종목·계좌·거래소를 채우고 전역 스냅샷에서 해당 전략을 찾아 폼을 초기화한다. 스냅샷 도착 전에는 스켈레톤 + `aria-busy="true"` + `전략 정보를 불러오는 중이에요…`.
2. **상태줄(A2)**: `RelayStatusBar` 재사용 + `DMA Ready` · 매수 상태 · 매도 상태 · (발주 후)잔량추적 기준선 · `반영 HH:MM:SS`. `aria-live="polite"`. 배지 문구는 `strategyBadgesOf`(16-11).
3. **에코 배너(A3)**: 다른 단말 변경 감지 시 상태줄 아래 **6초** 배너(`role="status"`) — `다른 단말에서 변경됐어요 · 서버 값으로 맞췄어요`, 더티를 덮었으면 `다른 단말에서 변경돼 수정하던 값 {N}개가 서버 값으로 바뀌었어요`. **토스트 라이브러리를 쓰지 않는다.** 동시에 전략 로그에 1줄 영구 기록.
4. **본문 그리드**: `.lc3`(≥1280) = 호가 460 | 매수 폼 250 | 매도 폼 250, gap 16. `.lc2`(<1024, 390 기준) = 42% | 1fr, gap 8. 1024~1279 는 **1열**. **모든 그리드 자식 `min-width:0`**.
5. **미체결/잔고(A12, 전폭)**: `AccountPanel` 재사용, 미체결 행에 출처 태그 `상따`(16-10 의 `originTag`). 모바일은 `.rlist`.
6. **전략 로그(A13, 전폭)**.
7. **게이트(A14)**: 비로그인/`unauthorized` 면 본문 전체를 `<DmaGate>` 로 교체.

**전송·수신 결선**
- 폼의 스위치/「수정」 → `send({t:"lc.set", cfg})`(16-09 컨텍스트). 응답은 기다리지 않는다 — 60 에코가 오면 폼이 서버값으로 갱신된다(D-11).
- `ServerMessage(level==="ERROR")` 판정: `source==="SetLimitChaser"` 또는 (`source==="Account"` ∧ `isin` **비어 있지 않음**)이 상따 몫이다. `isin` 이 빈 것은 VI 몫이므로 **여기서 표시하지 않는다**(Pitfall 9). 이 판정 함수는 **한 곳에만** 둔다(`lib/limit-chaser.ts` 또는 공용 유틸).
- **3초 무응답**: 전송 후 3초 내 에코가 없으면 상태줄에 「미반영」 표시(WinForms `RespTimeoutMs=3000` 동형). **자동 재전송 금지** — 재전송은 사용자가 누르지 않은 두 번째 등록이다.
- **이탈 경고**: 더티가 있으면 라우트 이동·새로고침 시 확인(`beforeunload` + 라우터 가드). 문구 `수정하지 않은 값이 있어요. 이 페이지를 벗어나면 사라져요.`
- **삭제 반영**: 에코 `crud === "D"` 면 폼을 빈 상태로 되돌리고(신규 폼 기본값) 사이드바 목록에서 사라진다(전역 스냅샷이 처리).
- `prefers-reduced-motion: reduce` 에서 플래시·점멸·fade 비활성.
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp build &amp;&amp; pnpm --filter gh-radar-webapp test</automated>
  </verify>
  <acceptance_criteria>
    - `/trading/limit-chaser/new` · `/trading/limit-chaser/{key}` 가 빌드되고 `data-slot="limit-chaser-page"` 를 렌더한다
    - `grep -c "sonner\|toast(" webapp/src/components/trading/limit-chaser-client.tsx` == 0
    - 배너 자동 소멸 6초 · 무응답 3초 상수가 코드에 명시되어 있다
    - `beforeunload` 등록이 더티일 때만 걸린다(테스트 또는 조건 분기 확인)
    - ServerMessage 상따/VI 판정 함수가 **한 곳에만** 정의된다(grep 1건)
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>상따 페이지가 두 라우트에서 동작하고 서버값 우선·거부 표시·이탈 경고가 결선된다.</done>
</task>

<task type="auto">
  <name>Task 3: trading-limit-chaser E2E</name>
  <files>webapp/e2e/specs/trading-limit-chaser.spec.ts</files>
  <read_first>
    - `webapp/e2e/specs/orderbook.spec.ts` — spec 골격(serial · withLocalRelay · data-slot 앵커 · `waitForReady`)
    - `webapp/e2e/fixtures/relay.ts`(16-02) — `seedLimitChasers` · `pushLimitChaserEcho` · `seedDmaCredential`
  </read_first>
  <action>
`mode:'serial'` + `withLocalRelay()` `beforeAll` 1회 규약으로 작성한다. 케이스:

1. **빈 폼 진입**: `/trading/limit-chaser/new` → 제목 「상따」 + 부제, 폼 기본값(주문금액 10 · 감시잔량 10,000 · 게이트 전부 OFF), 액션 바 **미렌더**.
2. **종목 선택 시딩**: 검색으로 종목 선택 → 가격 5칸이 상한가로 채워지고 호가 카드가 뜬다.
3. **스위치 즉시 전송 + 에코**: 매수주문 ON → relay 가 msg_type 10 수신 → `pushLimitChaserEcho({buyEnabled:true})` → 상태줄 배지가 「매수ON」, 로그 1줄, 사이드바 3단에 항목 등장(원 아이콘 좌측 채움).
4. **값 변경 → 수정**: 매수가격 변경 → 액션 바 등장(「1개」) → 「수정」 클릭 → 10 재수신 → 에코 후 액션 바 소멸 + 상태줄 `반영` 시각 갱신.
5. **다른 단말 변경**: 폼을 더티로 둔 채 서버가 다른 값의 60 에코를 푸시 → 폼이 서버값으로 바뀌고 배너 `다른 단말에서 변경됐어요` 가 6초간 보인다.
6. **둘 다 OFF = 삭제**: 매수·매도 OFF → 전송 cfg 의 crud 가 D 이고, `crud:"D"` 에코 수신 후 사이드바에서 항목이 사라지고 폼이 빈 상태로 돌아간다.
7. **서버 거부 표시**: `ServerMessage(level:"ERROR", source:"SetLimitChaser")` 푸시 → 상태줄·로그 양쪽에 문구가 보인다(조용한 무시 0).
8. **매핑 없음 게이트**: `clearDmaCredentials()` 후 진입 → `data-slot="dma-gate"`.
9. **모바일 390 2열**: 뷰포트 390 → 호가 컬럼과 설정 컬럼이 나란히 있고, 「매수」/「매도」 탭 전환 시 **한 쪽 pane 만** 보인다(비활성 pane 이 `hidden`). 요소 실측 폭이 컨테이너를 넘지 않는다(잘림 0).
10. **이탈 경고**: 더티 상태에서 사이드바 다른 항목 클릭 → 확인 UI 가 뜬다.
  </action>
  <verify>
    <automated>pnpm --filter gh-radar-webapp test:e2e -- trading-limit-chaser</automated>
  </verify>
  <acceptance_criteria>
    - 10 케이스 전부 green
    - 케이스 9 가 `boundingBox()` 비교로 잘림 0 을 단언하고 비활성 pane 의 `hidden` 속성을 확인한다
    - 케이스 3·6 이 relay 수신 msg_type 을 `requestLog()` 로 확인한다
    - spec 상단에 `test.describe.configure({ mode: 'serial' })`
    - 실서버 IP·실계좌 리터럴 0건
  </acceptance_criteria>
  <done>상따 표면의 조작 규율·동기화·반응형이 E2E 로 고정된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트웨이 에코 → 화면 상태 | 에코가 안 오는 것이 「거부」의 실제 신호다 |
| 브라우저 메모리 로그 → 사용자 판단 | 로그가 비면 조용한 거부를 사용자가 모른다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Repudiation | 서버 거부 표시 | mitigate | `ServerMessage(ERROR)` 를 상태줄 + 전략 로그 **양쪽**에 남긴다(PC-7). 3초 무응답 시 「미반영」 표시하되 **자동 재전송 금지** — E2E 케이스 7 |
| T-16-10 | 금전 손실 | 3초 무응답 후 사용자 재시도 | mitigate | 자동 재전송 경로를 만들지 않는다. 재전송은 사용자가 누르지 않은 두 번째 등록이 된다(WinForms 동일 규율) |
| T-16-02 | Information Disclosure | 전략 로그 내용 | accept | 로그는 브라우저 메모리 전용(새로고침 시 소실, 서버 저장 없음). 계좌번호는 화면 전체 표시가 계약(D2) |
| T-16-05 | Tampering | 호가 마커/등락률 렌더 | mitigate | 마커 슬롯 고정폭으로 값 변동 시 레이아웃 shift 0 — 틱마다 가격이 밀리면 사용자가 잘못된 가격을 클릭·판독한다(lessons.md) |
</threat_model>

<verification>
- `pnpm typecheck` exit 0
- `pnpm --filter gh-radar-webapp test` exit 0
- `pnpm --filter gh-radar-webapp test:e2e -- trading-limit-chaser` exit 0
- `pnpm --filter gh-radar-webapp test:e2e -- orderbook` exit 0 (호가 변형 회귀)
</verification>

<success_criteria>
- 상따 페이지 2 라우트 동작 · 3열/2열 반응형 · 잘림 0
- 서버값 우선 동기화와 거부 표시가 E2E 로 증명
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-13-SUMMARY.md` when done
</output>
