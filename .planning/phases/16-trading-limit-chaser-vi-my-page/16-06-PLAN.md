---
phase: 16-trading-limit-chaser-vi-my-page
plan: 06
type: execute
wave: 5
depends_on: ["16-02", "16-05"]
files_modified:
  - relay/src/hub/subscription-hub.ts
  - relay/tests/strategy-hub.test.ts
autonomous: true
requirements: [TRADE-03]

must_haves:
  truths:
    - "relay 가 세션 단위 전략 캐시(상따 전수 · VI 설정 · VI 주문 추적)를 갖는다 (D-12)"
    - "DMA 세션 Ready 직후 24/21/34 를 한 번 호출해 캐시를 채우고, 재조회는 재접속 시에만 일어난다 (D-12/D-13)"
    - "60 에코의 crud 'D' 는 캐시에서 삭제로 반영된다 (D-08)"
    - "세션이 교체되면 전략 캐시가 폐기되어 옛 전략이 화면에 남지 않는다"
    - "전략 팬아웃은 언제나 userId 한 명 대상이다 (T-16-02)"
  artifacts:
    - path: "relay/src/hub/subscription-hub.ts"
      provides: "전략 3맵 캐시 + Ready 프리페치 + 60/61/64/65/72/73/56 case"
      contains: "requestStrategySnapshot"
    - path: "relay/tests/strategy-hub.test.ts"
      provides: "캐시 upsert/delete · 프리페치 · 세션 교체 폐기 테스트"
  key_links:
    - from: "relay/src/hub/subscription-hub.ts"
      to: "relay/src/dma/envelope.ts"
      via: "parseLimitChaserEcho / parseViTrigger / parseViOrderList"
      pattern: "parseLimitChaserEcho"
    - from: "relay/src/hub/subscription-hub.ts"
      to: "relay/src/ws/fanout.ts"
      via: "emit(\"fanout\", {userId, msg})"
      pattern: "#fanout\\(userId"
---

<objective>
`SubscriptionHub` 에 **전략 캐시**를 얹는다. 계좌 상태 캐시(`#accountStates`)의 **4점 세트**(캐시 맵 · Ready 프리페치 · `#onFrame` case · `#clearCaches`)를 그대로 복제하는 것이 이 plan 의 전부다 — 새 아키텍처를 만들지 않는다(PATTERNS 핵심 3줄 ①).

- `#limitChasers: Map<`${userId}|${strategyKey}`, RelayLimitChaser>` · `#viTriggers: Map<userId, RelayViTrigger|null>` · `#viOrders: Map<`${userId}|${orderNo}`, RelayViOrderItem>`
- `#onReady` 에 `requestStrategySnapshot(userId)` 한 줄 — 24/21/34 3연발(D-12). **주기 재조회·수동 새로고침 없음**(D-13).
- `#onFrame` 에 60/61/64/65/72/73/56 **명시 case** 7개(PC-12).
- `getLimitChasers` / `getViTrigger` / `getViOrders` 스냅샷 getter(16-07 의 auth 직후 팬아웃이 소비).

Purpose: 전략의 정본은 게이트웨이이고 relay 는 세션당 1개인 **뷰의 소유자**다. 캐시가 여기 있어야 새 탭이 붙자마자 종목 구독 없이 전략을 본다(D-12).
Output: Hub 전략 캐시 + 프리페치 + 파싱 분기 + 신규 테스트 파일.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전략 캐시 3맵 + Ready 프리페치 + 스냅샷 getter</name>
  <files>relay/src/hub/subscription-hub.ts</files>
  <read_first>
    - `relay/src/hub/subscription-hub.ts` 전문 — `#accountStates`(:178~185 주석 포함), `#refs`(:168), `requestAccountState`(:344~356), `getAccountStates`(:367~375), `stats`(:392), `closeAll`(:406), `#onFrame`(:418~464), `#onAccountState`(:513~531), `#onReady`(:699~705), `#fanout`(:708~711), `#clearCaches`(:712~723), `attach`(멱등·세션 교체 시 캐시 폐기)
    - `relay/src/dma/envelope.ts` — 16-04/16-05 의 빌더·파서 시그니처
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-PATTERNS.md` §relay 전략 캐시(241~326행) — ①~⑥ 복제 지점
  </read_first>
  <action>
① **캐시 맵 3개 선언** — `#accountStates`(:178) 바로 아래. 각 맵에 「무엇이 정본이고 왜 여기 두는가」 JSDoc 을 단다: 상따는 `` `${userId}|${key}` `` (key = ISIN:accountNo:exchange), VI 설정은 userId 당 1건(미등록은 `null` 을 명시 저장 — 「조회한 적 없음」과 구분), VI 주문은 `` `${userId}|${orderNo}` ``. `orderNo === ""`(접수 전) 항목은 **키가 없으므로** 별도 배열로 두거나 `isin+triggerPrice` 복합키를 쓴다(재량, 단 73 upsert 시 같은 항목이 중복 누적되지 않아야 한다 — 테스트로 고정).

② **`requestStrategySnapshot(userId): void`** — `requestAccountState`(:344) 와 **같은 형태**로 만든다: 세션 없거나 `!isReady` 면 `logger.warn` 후 return, 아니면 `session.send(buildGetLimitChaserListReq())` · `buildGetVITriggerReq()` · `buildGetVIOrderListReq()` 3연발 + `logger.info({userId}, "[HUB] Ready — 전략 스냅샷 3종 요청")`.

③ **`#onReady`(:699) 에 한 줄 추가** — `resubscribeAll` / `requestAccountState` 옆. 「재구독·계좌 재요청·전략 재요청은 같은 트리거 한 자리」 규율을 주석으로 유지한다(Pitfall 4). **D-13: 주기 타이머·수동 재조회 진입점을 만들지 않는다**는 문장을 같은 자리에 남긴다.

④ **스냅샷 getter 3개** — `getAccountStates`(:367) 형태 그대로 `getLimitChasers(userId): RelayLimitChaser[]` · `getViTrigger(userId): RelayViTrigger | null | undefined`(undefined = 아직 모름 → 프레임을 내리지 않는다) · `getViOrders(userId): RelayViOrderItem[]`.

⑤ **`#clearCaches`(:712) 에 3맵 삭제 추가** + `stats()`(:392)·`closeAll()`(:406) 에도 3맵 카운트/정리를 더한다. 누락하면 재로그인 후 옛 전략이 남는다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test -- hub</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "#limitChasers\|#viTriggers\|#viOrders" relay/src/hub/subscription-hub.ts` >= 12 (선언 + onFrame + getter + clearCaches + stats + closeAll)
    - `grep -c "requestStrategySnapshot" relay/src/hub/subscription-hub.ts` == 2 (정의 + `#onReady` 호출)
    - `#clearCaches` 함수 본문에 `#limitChasers`·`#viTriggers`·`#viOrders` 삭제가 모두 포함된다
    - `grep -c "setInterval\|setTimeout" relay/src/hub/subscription-hub.ts` 가 이 plan 이전과 동일 (주기 재조회 타이머 추가 0건 — D-13)
    - `pnpm typecheck` exit 0, 기존 `pnpm --filter @gh-radar/relay test -- hub` exit 0
  </acceptance_criteria>
  <done>전략 3맵 · 프리페치 · getter · 폐기 경로가 존재하고 기존 hub 테스트가 회귀 없이 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: #onFrame 전략 프레임 case 7종 + 캐시 반영·팬아웃</name>
  <files>relay/src/hub/subscription-hub.ts</files>
  <read_first>
    - `relay/src/hub/subscription-hub.ts` `#onFrame`(:418~464) — 세대 대조 후 `switch (e.msgType)` 구조, `parseAccountState` null 가드 주석
    - `relay/src/dma/envelope.ts` 파서 6종 (16-05 산출물)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-RESEARCH.md` Pitfall 7·8·9·10·19
  </read_first>
  <action>
`#onFrame` 의 `switch` 에 **명시 case 7개**를 추가한다(`default:` 로 조용히 떨어지는 프레임 0 유지):

- `MSG.SetLimitChaserResp`(60) → `parseLimitChaserEcho`. null 이면 파서가 이미 로그했으므로 return. `crud === "D"` 면 `#limitChasers.delete(key)`, 아니면 upsert. 두 경우 모두 `#fanout(userId, { t:"lc", item })` — **삭제도 프레임으로 내려야** 브라우저가 목록에서 지운다.
- `MSG.GetLimitChaserListResp`(64) → `parseLimitChaserList`. 그 userId 의 상따 엔트리를 **전량 교체**(prefix 순회 삭제 후 재삽입)하고 `#fanout(userId, { t:"lc.snap", items })` 1프레임.
- `MSG.SetVITriggerResp`(61) → `parseViTrigger`. 「미등록」이면 `#viTriggers.set(userId, null)` + `{ t:"vi", cfg:null }`, 아니면 upsert + `{ t:"vi", cfg }`.
- `MSG.GetVIOrderListResp`(72) → `parseViOrderList(env, true)` → 그 userId 의 VI 주문 **전량 교체** + `{ t:"vi.list", snap:true, items }`.
- `MSG.VIOrderListPush`(73) → `parseViOrderList(env, false)` → **항목별 upsert** + `{ t:"vi.list", snap:false, items }`. `confirm_locked` 되돌림(단건 푸시)도 이 경로다.
- `MSG.DisableStrategiesResp`(65) → `parseDisableStrategiesResp` → `{ t:"strategies.disabled", count, viDisabled }` 를 **그대로 팬아웃**한다. **캐시를 여기서 고치지 않는다** — 서버가 60/61 에코를 먼저 보냈으므로 상태는 이미 갱신돼 있다(RESEARCH §D). 65 는 완료 신호일 뿐이라는 주석을 남긴다.
- `MSG.VIOrderNotice`(56) → `parseViOrderNotice` → `{ t:"vi.notice", ... }` 팬아웃(캐시 없음).

이미 존재하는 `MSG.ServerMessage`(54) 경로는 그대로 두되, **상따/VI 거부 판정에 필요한 필드(level·source·isin)가 브라우저 계약에 실려 나가는지** 확인하고 빠졌으면 채운다 — `source==="Account" ∧ isin===""` 만 VI 몫이라는 판정(Pitfall 9)은 **브라우저 한 함수**에서 하므로 relay 는 필드를 온전히 전달하기만 하면 된다.

모든 팬아웃은 기존 `#fanout(userId, msg)` 한 경로를 쓴다 — 새 전송 경로를 만들지 않는다(S-3 / T-16-02).
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test -- strategy-hub</automated>
  </verify>
  <acceptance_criteria>
    - `#onFrame` 의 switch 에 `MSG.SetLimitChaserResp`·`MSG.GetLimitChaserListResp`·`MSG.SetVITriggerResp`·`MSG.GetVIOrderListResp`·`MSG.VIOrderListPush`·`MSG.DisableStrategiesResp`·`MSG.VIOrderNotice` case 7개가 모두 존재
    - `grep -c "this.emit(\"fanout\"" relay/src/hub/subscription-hub.ts` == 1 (팬아웃 경로가 여전히 `#fanout` 하나뿐)
    - 65 case 본문에 `#limitChasers`/`#viTriggers` 쓰기가 없다 (완료 신호만)
    - `pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>전략 프레임 7종이 캐시에 반영되고 단일 팬아웃 경로로 내려간다.</done>
</task>

<task type="auto">
  <name>Task 3: strategy-hub 테스트 신규</name>
  <files>relay/tests/strategy-hub.test.ts</files>
  <read_first>
    - `relay/tests/hub.test.ts` — `decodeReq` 규율(게이트웨이가 받은 요청 프레임을 디코드해 단언), HubSession 스텁 형태, `attach`/`detach` 시나리오
    - `relay/tests/helpers/fake-gateway.ts` + `frames.ts` (16-02 산출물)
  </read_first>
  <action>
`relay/tests/hub.test.ts` 의 구조를 그대로 복제해 새 파일을 만든다. 케이스:

1. **Ready 프리페치**: 세션 ready 이벤트 후 게이트웨이가 받은 요청 msg_type 에 **24·21·34 가 각각 1회** 있다(순서 무관, 중복 0).
2. **재접속 시에만 재조회**: 같은 세션이 계속 ready 상태로 있는 동안(예: 가짜 타이머 10분 진행) 추가 24/21/34 요청이 **0건**이다. 세션 교체(재접속) 후 `ready` 가 다시 오면 다시 3건.
3. **60 upsert → 팬아웃**: 60 에코 1건 주입 시 `fanout` 이벤트가 `{t:"lc"}` 로 1회, `getLimitChasers(userId)` 길이 1.
4. **60 crud "D" → delete**: 같은 key 로 `crud:"D"` 를 주입하면 `getLimitChasers` 길이 0 이고 `{t:"lc"}` 프레임은 여전히 나간다.
5. **64 전량 교체**: 캐시에 2건이 있는 상태에서 64(1건)를 받으면 최종 1건이며 `{t:"lc.snap"}` 1프레임.
6. **61 미등록**: 빈 61 → `getViTrigger(userId) === null` + `{t:"vi", cfg:null}`.
7. **73 항목 upsert**: 72(3건) 후 73(1건, 같은 orderNo, state 변경) → 총 3건이고 그 항목의 state 가 갱신된다(중복 누적 0).
8. **65 는 캐시를 고치지 않는다**: 60 에코로 buyEnabled=false 가 온 뒤 65 를 받아도 캐시 내용이 바뀌지 않고 `{t:"strategies.disabled"}` 만 나간다.
9. **세션 교체 시 캐시 폐기**: `attach` 로 세션을 갈아끼우면 `getLimitChasers`/`getViOrders` 가 비고 `getViTrigger` 가 `undefined`(모름)로 돌아간다.
10. **팬아웃 대상 격리**: 두 userId 를 붙이고 A 의 60 에코가 B 의 fanout 이벤트로 나가지 않는다(T-16-02).
11. **파싱 실패 무해**: 깨진 60 프레임(잘린 버퍼)을 주입해도 예외가 나지 않고 캐시가 그대로다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test -- strategy-hub</automated>
  </verify>
  <acceptance_criteria>
    - `relay/tests/strategy-hub.test.ts` 에 11개 이상 케이스가 있고 전부 green
    - 케이스 1 이 게이트웨이 수신 요청을 **디코드해** msg_type 24/21/34 를 단언한다(문자열 매칭 아님)
    - 케이스 10 이 두 userId 로 실행되고 상호 누출 0을 단언한다
    - `pnpm --filter @gh-radar/relay test` 전량 exit 0
  </acceptance_criteria>
  <done>전략 캐시·프리페치·격리·폐기가 테스트로 고정된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DMA 세션(사용자 1명) → Hub 캐시 | 캐시 키에 userId 가 없으면 다른 사용자의 전략이 섞인다 |
| Hub → fanout | 팬아웃 대상 선택이 틀리면 타인의 전략·계좌번호가 샌다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Information Disclosure | `#fanout` / 전략 캐시 키 | mitigate | 캐시 키 접두사가 항상 `${userId}|`, 팬아웃은 `emit("fanout",{userId,msg})` **단일 경로**(grep 1건 게이트). 테스트 케이스 10 이 두 사용자 격리를 증명 |
| T-16-05 | Tampering | `#onFrame` 신규 case 7종 | mitigate | 파서 null 가드 후에만 캐시 반영. `default:` 조용한 드롭 0 유지 |
| T-16-07 | Repudiation | 65 처리 | accept | 65 는 완료 신호로만 쓰고 캐시를 고치지 않는다 — 60/61 에코가 이미 상태를 옮겼기 때문. 65 유실 시 「완료 표시」만 지연되며 상태는 정확하다 |
| T-16-06 | Denial of Service | 전략 스냅샷 프레임 크기 | accept | 현실적 전략 수 10건 미만 + 파서 항목 상한 200. 백프레셔 1MB 한계 대비 여유 — 분할하지 않는다(RESEARCH Open Question 2) |
</threat_model>

<verification>
- `pnpm typecheck` exit 0
- `pnpm --filter @gh-radar/relay test` exit 0 (기존 hub/fanout/account-state 회귀 포함)
</verification>

<success_criteria>
- Ready 시 24/21/34 각 1회, 이후 주기 재조회 0
- 60/61/64/65/72/73/56 이 캐시·팬아웃으로 이어진다
- 세션 교체 시 3맵 폐기
- 두 사용자 간 전략 누출 0
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-06-SUMMARY.md` when done
</output>
