---
phase: 16-trading-limit-chaser-vi-my-page
plan: 29
type: execute
wave: 19
depends_on: ["16-25"]
gap_closure: true
files_modified:
  - relay/src/ws/fanout.ts
  - relay/tests/fanout.test.ts
autonomous: true
requirements: [TRADE-01, TRADE-03]

must_haves:
  truths:
    - "상장폐지·마스터 미로딩으로 ISIN 을 못 푸는 종목이어도 **전략을 내릴 수 있다** — `crud:\"D\"`(삭제)와 전 게이트 OFF 요청은 시장 해석 실패로 거부되지 않는다 (GC-WR-04 / T-16-44 와 같은 규율의 서버측)"
    - "등록·수정(`crud:\"C\"`)은 여전히 시장 해석 실패로 거부된다 — 기본값 \"K\" 로 메워 코스닥 전략을 코스피로 등록하는 경로가 생기지 않는다 (16-25 truth 41 회귀 방지)"
    - "relay 의 무장 가드가 UI 의 `canArmBuy`·`canArmSell`·`canArmSweep` 세 식과 **동일하게** 판정한다 — `sellWatchQty === 0` 과 한방 게이트가 서버에서 통과하지 않는다 (GC-WR-05 / T-16-43)"
  artifacts:
    - path: "relay/src/ws/fanout.ts"
      provides: "crud 별 시장 해석 분기 + UI 와 동형인 무장 가드"
      contains: "crud"
    - path: "relay/tests/fanout.test.ts"
      provides: "삭제는 통과 · 등록은 거부 · sellWatchQty 0 거부 · sweep 게이트 거부 케이스"
  key_links:
    - from: "relay/src/ws/fanout.ts (#strategyArmable)"
      to: "webapp/src/components/trading/limit-chaser-form.tsx (canArm*)"
      via: "같은 세 식을 서버측 최후 관문으로 이식"
      pattern: "sellWatchQty|sweepEnabled"
    - from: "relay/src/ws/fanout.ts (lc.set crud D)"
      to: "SubscriptionHub 전략 캐시 (getLimitChasers)"
      via: "에코된 전략의 market 을 삭제 요청의 시장 원천으로 사용"
      pattern: "getLimitChasers"
---

<objective>
**GC-WR-04 + GC-WR-05** 를 닫는다. 둘 다 `lc.set` 의 관문이 **잘못된 방향으로 엄격하거나 느슨한** 문제다.

지금 상태:
- **GC-WR-04.** `#strategyMarket` 은 `crud` 를 보지 않는다(`fanout.ts:579-581`, `:761-790`). `SymbolMap` 이 그 ISIN 을 못 풀면(상장폐지로 `stocks` 에서 빠졌거나, relay 부팅 직후 `symbols.start()` 가 아직 안 끝났거나) **등록·수정·삭제가 전부** 「이 종목은 지금 전략을 등록할 수 없습니다」로 막힌다. 즉 사용자가 **자기 전략을 내릴 수 없는** 상태가 만들어진다 — `limit-chaser-form.tsx` 가 T-16-44 로 「무장 해제를 막으면 그게 더 위험하다」고 적어 둔 것과 정확히 같은 종류의 위험이 서버 쪽에 남아 있다.
- **GC-WR-05.** UI 는 `canArmBuy = buyOrderPrice > 0 && buyQty > 0`, `canArmSell = sellOrderPrice > 0 && sellWatchQty > 0`, `canArmSweep = sweepWatchPrice > 0 && canArmBuy` 로 판정한다(`limit-chaser-form.tsx:343-365`). relay 의 `#strategyArmable`(`fanout.ts:796-812`)은 `sellEnabled && sellOrderPrice === 0` 하나만 보고 **`sellWatchQty` 도 `sweepEnabled` 도 보지 않는다.** 그런데 이 검사의 존재 이유가 「UI 를 우회한 경로(직접 wss, 옛 탭)가 있어도 무장 상태가 만들어지면 안 된다」(T-16-43)이므로, 마지막 관문이 첫 관문보다 느슨하면 그 문장이 성립하지 않는다. 특히 `sellWatchQty === 0` 은 계약이 「0 이면 서버가 매도 활성화를 **거부**(눕힘)한다」고 못박은 값(`packages/shared/src/relay.ts:167`)이라 조용한 부분 거부가 그대로 재현된다.

Purpose: 서버측 관문이 「자산을 인질로 잡지 않으면서」 UI 와 같은 강도를 갖게 한다.
Output: `crud` 인지 시장 해석 · UI 3식과 동형인 `#strategyArmable` · fanout 회귀 테스트 4케이스.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-25-SUMMARY.md
</context>

<interfaces>
<!-- 손대는 자리의 현재 계약. -->

`relay/src/ws/fanout.ts`:
- `lc.set` 처리 흐름(:574-590): `#strategySession` → `#accountAllowed` → `#strategyMarket(conn, userId, msg.t, cfg.isin)` → `#strategyArmable(conn, userId, msg.t, cfg)` → `#buildStrategyPayload(… buildSetLimitChaserReq({ ...cfg, market }))` → `session.send`
- `#strategyMarket(conn, userId, t, isin): OrderMarket | null` — `#symbols === null` 이거나 `lookup` 실패면 `rejectFrame` 후 `null`
- `#strategyArmable(conn, userId, t, cfg: RelayLimitChaserInput): boolean` — 현재 `reason` 은 `"buy"` 또는 `"sell"` 두 갈래
- `this.#hub.getLimitChasers(userId)` — **서버가 에코한 전략 목록**(`RelayLimitChaser[]`, `market: OrderMarket` 포함). `auth` 직후 `lc.snap` 으로 그대로 내보내는 그 캐시다(:536)
- `rejectFrame(message, "", isin)` — 사유 프레임 헬퍼

`packages/shared/src/relay.ts`:
- `RelayLimitChaserInput.crud: RelayLcCrud` — `"C"`(등록·수정) / `"D"`(삭제). 「UI 의 '삭제됨' 판정은 스위치가 아니라 **에코의 `crud`** 를 봐야 한다」(:139)
- `sellWatchQty` — 「0 이면 서버가 매도 활성화를 거부한다」(:167)
- `RelayLimitChaser.market: OrderMarket`(:135) — 에코에 시장이 실려 있다

`webapp/src/components/trading/limit-chaser-form.tsx:343-365` — 이식 대상 3식(`canArmBuy`/`canArmSell`/`canArmSweep`)과 각각의 근거 주석
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 삭제(`crud:"D"`)를 시장 해석 실패로 막지 않는다 (GC-WR-04)</name>
  <files>relay/src/ws/fanout.ts, relay/tests/fanout.test.ts</files>
  <read_first>
    - `relay/src/ws/fanout.ts` L560~600 (`lc.set` 분기 전문 + 그 위 ★ 주석 — 16-25 가 `market` 소유권을 relay 로 옮긴 근거), L745~790 (`#strategyMarket` 전문), L530~545 (`lc.snap` — `getLimitChasers` 사용례)
    - `packages/shared/src/relay.ts` L125~175 (`RelayLimitChaser` / `RelayLimitChaserInput` — `crud`·`market`·`sellWatchQty` 의 계약 주석)
    - `webapp/src/components/trading/limit-chaser-form.tsx` L370~400 (`gateBlocked` 의 ★ 「끄는 것은 언제나 허용한다」 — T-16-44. **이 plan 의 서버측 대응이 바로 그 규율이다**)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §GC-WR-04 — Issue + Fix 스니펫(캐시된 에코의 market 우선)
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-25-SUMMARY.md` — `market` 소유권 이전이 무엇을 의도했는지
    - `relay/tests/fanout.test.ts` — `lc.set` 을 주입하고 `rejectFrame`·게이트웨이 송신을 세는 기존 헬퍼
  </read_first>
  <action>
① `lc.set` 분기에서 시장 해석을 `crud` 로 가른다. 우선순위를 **명시적 폴백 사슬**로 만들고 각 단계에 근거 주석을 붙인다:
  - `cfg.crud !== "D"` (등록·수정): 지금과 **완전히 동일**하다. `#strategyMarket` 이 `null` 이면 거부. 기본값 `"K"` 로 메우지 않는다는 규율(Pitfall 7 / T-16-42)은 그대로다.
  - `cfg.crud === "D"` (삭제): ① `this.#hub.getLimitChasers(userId)` 에서 `(isin, accountNo, exchange)` 가 일치하는 에코를 찾아 그 `market` 을 쓴다 — **서버가 그 값으로 저장했다는 1차 증거**다. ② 없으면 `#symbols` 로 풀어 본다. ③ 둘 다 실패하면 **거부하지 않는다.** 마지막 폴백을 무엇으로 할지(고정값 사용 vs 그래도 통과)는 실행자가 정하되, **`logger.error` 로 「시장 미해석 상태의 전략 삭제 — 폴백 X 로 송신」을 반드시 남기고**(무로그 fail-safe 금지 / S-5), 그 선택 근거를 코드 주석과 SUMMARY 에 함께 적는다. 사용자가 자기 전략을 못 내리는 상태를 만드는 것이 이 셋 중 무엇보다 나쁘다는 것이 판단 기준이다.

② `#strategyMarket` 자체는 등록·수정 전용으로 남긴다. docstring 에 「`crud:"D"` 는 이 함수를 지나지 않는다 — 삭제를 해석 실패로 막으면 사용자의 자산을 인질로 잡는다 (GC-WR-04 / T-16-44 의 서버측)」를 추가한다. 삭제용 해석을 별도 private 메서드로 뽑아도 좋다(이름에 `Delete` 또는 `Teardown` 이 드러나게).

③ 「전 게이트 OFF = 삭제」 규율 확인. `crudOf` 계산이 브라우저에 있고 relay 는 `cfg.crud` 를 그대로 받으므로, 전 게이트 OFF 요청은 `crud:"D"` 로 도착한다. 그 사실을 코드 주석으로 확인해 적는다 — 만약 아니라면(게이트 4종이 전부 false 인데 `crud:"C"` 로 오는 경우가 있다면) 그 경우도 ① 의 삭제 경로로 보낸다.

④ 테스트 2케이스를 `fanout.test.ts` 에 추가한다.
  - 「`#symbols` 가 못 푸는 ISIN 이어도 `crud:"D"` 는 게이트웨이로 나간다」 — 거부 프레임 0건 + 송신 1건을 단언한다.
  - 「같은 ISIN 의 `crud:"C"` 는 여전히 거부된다」 — 거부 프레임 1건 + 송신 0건. **16-25 truth 41 의 회귀 방지**다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay exec vitest run tests/fanout.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'crud === "D"' relay/src/ws/fanout.ts` >= 1 — 삭제 분기가 실재
    - `grep -c "getLimitChasers" relay/src/ws/fanout.ts` >= 2 — `lc.snap` 외에 삭제 시장 해석에서도 쓴다
    - `pnpm --filter @gh-radar/relay exec vitest run tests/fanout.test.ts` exit 0
    - 「삭제는 통과」·「등록은 거부」 두 케이스가 테스트 파일에 존재 (케이스 이름을 SUMMARY 에 인용)
    - 폴백 최종 단계에 `logger.error` 가 있다 (줄번호를 SUMMARY 에 인용) — 조용한 폴백이 아니다
  </acceptance_criteria>
  <done>시장을 못 푸는 종목의 전략도 내릴 수 있고, 등록·수정의 엄격함은 그대로다.</done>
</task>

<task type="auto">
  <name>Task 2: relay 무장 가드를 UI 의 `canArm*` 3식과 동형으로 만든다 (GC-WR-05)</name>
  <files>relay/src/ws/fanout.ts, relay/tests/fanout.test.ts</files>
  <read_first>
    - `relay/src/ws/fanout.ts` L783~825 (`#strategyArmable` 전문 + docstring — 「조립 단계가 모든 호출 경로의 마지막 관문」 T-16-43, 「매도 수량(`sellOrderQty`)은 S→C 전용이라 요청에 없다」)
    - `webapp/src/components/trading/limit-chaser-form.tsx` L335~370 — `buyQty`(`buyOrderQtyFromAmount`) · `canArmBuy` · `canArmSell`(★ `sellQty` 를 조건으로 쓰지 않는 근거 문단 전문) · `canArmSweep`(★ 한방이 매수 무장 조건을 함께 요구하는 근거)
    - `packages/shared/src/relay.ts` L160~175 — `sellWatchQty` 「0 이면 서버가 매도 활성화를 거부한다」
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW.md` §GC-WR-05 — Fix 스니펫(3갈래 `reason`)
    - `relay/tests/fanout.test.ts` — 기존 무장 거부 케이스(16-25 가 넣은 것)의 단언 방식
  </read_first>
  <action>
① `#strategyArmable` 의 `reason` 계산을 **3갈래**로 넓힌다. 16-REVIEW §GC-WR-05 의 Fix 스니펫을 참조하되, 각 갈래가 UI 의 어느 식과 대응하는지 주석으로 짝지어 적는다:
  - `"buy"` — `cfg.buyEnabled && (cfg.buyOrderPrice === 0 || cfg.buyOrderQty === 0)` (현행 유지, UI `canArmBuy`)
  - `"sell"` — `cfg.sellEnabled && (cfg.sellOrderPrice === 0 || cfg.sellWatchQty === 0)` (UI `canArmSell`)
  - `"sweep"` — `cfg.sweepEnabled && (cfg.sweepWatchPrice === 0 || cfg.buyOrderPrice === 0 || cfg.buyOrderQty === 0)` (UI `canArmSweep` = `sweepWatchPrice > 0 && canArmBuy`)
  UI 는 `buyQty` 를 `buyOrderQtyFromAmount(금액, 가격)` 로 산출하지만 relay 가 받는 것은 이미 산출된 `buyOrderQty` 다 — 그래서 relay 는 `buyOrderQty === 0` 을 본다. 이 대응 관계를 주석에 한 줄로 남긴다(다음 사람이 「식이 다르다」고 오해하지 않게).

② **삭제 요청에는 이 가드를 적용하지 않는다.** `crud:"D"` 는 무장이 아니라 해제이므로, Task 1 의 규율(자산을 인질로 잡지 않는다)과 같은 이유다. 현재 코드가 이미 그런지 확인하고, 아니면 `cfg.crud !== "D"` 조건을 앞에 둔다.

③ 거부 로그·거부 프레임은 지금 형태를 유지한다 — `logger.error({ userId, t, isin, gate: reason }, …)` 에 계좌번호를 싣지 않는 규율(T-16-45) 그대로. 사용자 대면 문구는 갈래별로 갈라도 좋으나, 갈랐다면 세 문구가 UI 의 `ARM_BLOCKED_TEXT` 와 **모순되지 않아야** 한다.

④ 테스트 2케이스를 `fanout.test.ts` 에 추가한다.
  - 「`sellEnabled: true` + `sellWatchQty: 0` 은 거부된다」 — 거부 프레임 1건 + 게이트웨이 송신 0건.
  - 「`sweepEnabled: true` + `sweepWatchPrice: 0`(또는 `buyOrderQty: 0`) 은 거부된다」 — 같은 단언.
  기존 `"buy"` 갈래 케이스는 그대로 통과해야 한다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sellWatchQty" relay/src/ws/fanout.ts` >= 1
    - `grep -c "sweepEnabled" relay/src/ws/fanout.ts` >= 1
    - `grep -c '"sweep"' relay/src/ws/fanout.ts` >= 1 — 세 번째 갈래가 실재
    - `grep -c "sellWatchQty\|sweepWatchPrice" relay/tests/fanout.test.ts` >= 2
    - `pnpm --filter @gh-radar/relay test` exit 0
    - `pnpm --filter @gh-radar/relay run typecheck` · `typecheck:tests` exit 0
  </acceptance_criteria>
  <done>UI 가 막는 무장 조합은 relay 도 전부 막고, 해제는 어느 쪽도 막지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 `lc.set` → relay 조립 | UI 를 우회한 경로(직접 wss·옛 탭)가 존재한다 — 마지막 관문은 relay 다 (T-16-43) |
| `SymbolMap` 로딩 상태 → 전략 관문 | 마스터 미로딩·상장폐지가 **사용자의 조작 권한**을 좌우해서는 안 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-55 | Denial of Service (자산 인질) | `#strategyMarket` 의 `crud` 무시 | mitigate | 삭제·전 게이트 OFF 는 시장 해석 실패로 막지 않는다. 에코 캐시 → `SymbolMap` → 명시 폴백 순서, 최종 폴백은 `logger.error` 동반 |
| T-16-42 | Tampering (잘못된 시장 등록) | 등록·수정의 시장 해석 | mitigate | **완화하지 않는다.** `crud:"C"` 는 여전히 거부. 「등록은 거부」 회귀 케이스로 잠금 |
| T-16-43 | Tampering (무장 우회) | `#strategyArmable` | mitigate | UI 3식을 서버로 이식해 마지막 관문이 첫 관문보다 느슨하지 않게 한다. `sellWatchQty === 0`·sweep 게이트 케이스로 잠금 |
| T-16-45 | Information Disclosure | 전략 거부 로그 | mitigate | 계좌번호 미포함 유지 — `userId`·`t`·`isin`·`gate` 만 |
| T-16-13 | 운영 사고 | 실서버·실계좌 접속 | accept | FakeGateway 만 쓴다 (D-27 승계) |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/relay test` exit 0
- `pnpm --filter @gh-radar/relay run typecheck` · `typecheck:tests` exit 0
- `grep -rn "10\.41\.1\.120" relay/` 0건 (D-27 승계)
</verification>

<success_criteria>
- 시장 미해석 종목의 전략을 내릴 수 있다
- 등록·수정의 시장 엄격함은 16-25 그대로다
- relay 무장 가드가 UI 3식과 동형이다
- 삭제 경로에는 무장 가드가 적용되지 않는다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-29-SUMMARY.md` when done
</output>
