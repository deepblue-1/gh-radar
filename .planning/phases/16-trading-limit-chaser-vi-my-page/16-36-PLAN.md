---
phase: 16-trading-limit-chaser-vi-my-page
plan: 36
type: execute
wave: 23
depends_on: []
gap_closure: true
files_modified:
  - relay/src/ws/fanout.ts
  - relay/tests/fanout.test.ts
autonomous: true
requirements: [TRADE-03]

must_haves:
  truths:
    - "무장(발주가·수량이 유효한) 상따 전략 설정은 시장 해석 엄격성(T-16-42)과 무장 가드(T-16-43)를 반드시 거친 뒤에만 게이트웨이로 나간다 — 판정 근거는 클라이언트가 주장하는 `crud` 가 아니라 **게이트 4종의 실제 상태**다 (R2-CR-01)"
    - "게이트 4종이 전부 꺼진 요청은 시장 해석에 실패해도 거부되지 않는다 — 사용자가 상장폐지·마스터 미로딩 종목의 전략을 영원히 못 지우는 상태를 만들지 않는다 (GC-WR-04 / T-16-55 유지)"
    - "게이트가 전부 꺼진 요청은 `crud` 가 `\"C\"` 로 와도 철거로 본다 — 게이트웨이가 그렇게 정규화하기 때문이다 (기존 ② 갈래 유지)"
    - "`crud:\"D\"` 인데 게이트가 켜져 있는 프레임은 조용히 통과하지 않는다 — 운영 신호가 로그로 남고 계좌번호는 실리지 않는다 (S-5 / T-16-45)"
  artifacts:
    - path: "relay/src/ws/fanout.ts"
      provides: "게이트 4종으로 판정하는 #isTeardown + crud 불일치 로그"
      contains: "cancelTradeEnabled"
    - path: "relay/tests/fanout.test.ts"
      provides: "crud:\"D\" 스푸핑 거부 2케이스 + 진짜 철거 통과 케이스 + crud:\"C\" 전 게이트 OFF 철거 케이스"
      contains: "crud"
  key_links:
    - from: "relay/src/ws/fanout.ts #isTeardown"
      to: "webapp/src/lib/limit-chaser.ts isDeleteIntent"
      via: "게이트 4종(buy/sell/cancelQty/cancelTrade) 동일 판정"
      pattern: "cancelQtyEnabled"
    - from: "relay/src/ws/fanout.ts #strategyArmable"
      to: "#isTeardown"
      via: "철거에만 무장 가드를 면제한다 — 면제 조건이 게이트 상태가 됐다"
      pattern: "#isTeardown"
---

<objective>
**R2-CR-01** 을 닫는다. `#isTeardown` 이 클라이언트가 보낸 `crud:"D"` 를 게이트 상태 확인 없이 **단독 근거**로 믿는 바람에, 한 프레임이 시장 해석 엄격성(T-16-42)과 무장 가드(T-16-43)를 **동시에** 우회한다.

지금 상태 (`relay/src/ws/fanout.ts:793-798`):
- `#isTeardown` 첫 줄이 `if (cfg.crud === "D") return true;` 다. `crud` 는 **인바운드 필드**이고(`relay/src/ws/protocol.ts:109` `z.enum(["C","D"])`, `RelayLimitChaserInput` 이 `crud` 를 Omit 하지 않는다), 브라우저 또는 임의의 wss 클라이언트가 값을 정한다.
- 계약 원문(`packages/shared/src/relay.ts:136-141`)은 정반대를 못박고 있다 — 「매수·매도·취소 게이트가 **전부** 꺼지면 **서버가** `"D"` 로 정규화한다」. 삭제의 정본은 게이트다.
- 그래서 `{crud:"D", buyEnabled:true, buyOrderPrice:0, buyOrderQty:0, isin:<마스터에 없는 ISIN>}` 한 프레임이 `#teardownMarket`(못 풀면 폴백 `"K"`, `:824`)과 `#strategyArmable` 첫 줄의 조기 반환(`:903`)을 함께 지나 **시장이 틀리고 무장까지 걸린 반복 발주 설정**으로 게이트웨이에 나간다.

**⚠️ 이 결함은 이제 실계좌 위에서 돈다.** 2026-09-09 프로덕션이 실 게이트웨이(`10.41.1.120:9100`)에 결선됐고 `/healthz` 가 `everReadyCount: 1` 을 실측했다(`.planning/STATE.md` §DMA_HOST 배포 회귀). 라운드 3 최우선이다.

**동시에 지켜야 하는 것 — 16-29 가 GC-WR-04 로 얻은 것을 잃지 않는다.** 「전략을 내리는 요청은 시장 해석 실패로 막지 않는다」는 여전히 참이어야 한다. 두 요구는 충돌하지 않는다: **철거의 정의를 「클라이언트가 D 라고 말했다」에서 「게이트가 실제로 다 꺼졌다」로 바꾸면** 둘 다 성립한다.

Purpose: 마지막 관문이 클라이언트의 자칭을 근거로 자기 자신을 면제하지 않게 한다.
Output: 게이트 기반 `#isTeardown` · 불일치 로그 · 회귀 잠금 테스트 3케이스 · 전제가 틀렸던 기존 테스트 2건 정정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-VERIFICATION-R2.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-REVIEW-R2.md
@.planning/phases/16-trading-limit-chaser-vi-my-page/16-29-SUMMARY.md
</context>

<interfaces>
<!-- 실행자가 코드 탐색 없이 바로 쓸 수 있는 계약. 줄번호는 16-29 이후 기준이며 이동했을 수 있다. -->

`relay/src/ws/fanout.ts`:
- `#isTeardown(cfg: RelayLimitChaserInput): boolean` (:793-798) — 고칠 자리
- `#teardownMarket(userId, t, cfg): OrderMarket` (:800~) — 폴백 사슬 ① 에코 캐시 ② SymbolMap ③ `TEARDOWN_MARKET_FALLBACK`(+`logger.error`). **정책 변경 없음**
- `#strategyMarket(conn, userId, t, isin): OrderMarket | null` — 못 풀면 `null`(= 거부). **정책 변경 없음**
- `#strategyArmable(conn, userId, t, cfg): boolean` (:895~) — 첫 줄 `if (this.#isTeardown(cfg)) return true;` **유지한다**(아래 ★ 참조)
- `lc.set` 분기 (:590-620) — `const teardown = this.#isTeardown(cfg)` → market 갈림 → `#strategyArmable`

`packages/shared/src/relay.ts`:
- `RelayLimitChaser.crud` 계약 주석 (:136-141) — 「전부 꺼지면 **서버가** `"D"` 로 정규화한다」
- `RelayLimitChaserInput = Omit<RelayLimitChaser, "sellOrderQty"|"sellQtyTrackBaseline"|"sellEntryLatched"|"cancelQtyTrackBaseline"|"key"|"market">` — `crud` 는 **남아 있다**(인바운드)

`webapp/src/lib/limit-chaser.ts`:
- `isDeleteIntent(gates)` (:167-174) = `!buyEnabled && !sellEnabled && !cancelQtyEnabled && !cancelTradeEnabled` — **원본**. `sweepEnabled` 는 들어 있지 않다
- `crudOf(gates)` (:182-184) = `isDeleteIntent(gates) ? 'D' : 'C'` — 브라우저의 `crud` 는 이 파생값일 뿐이다

`relay/tests/fanout.test.ts`:
- `lcInput(overrides)` (:124~) — **기본값에 `buyEnabled: true`** 가 있다. 이것이 ⑰-e/⑰-e2 의 전제를 오염시킨 원인이다
- ⑰-e (:729) · ⑰-e2 (:759) · ⑰-f (:790) — 아래 Task 2 에서 다룬다
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 철거 판정의 근거를 게이트 4종으로 옮긴다 (R2-CR-01)</name>
  <files>relay/src/ws/fanout.ts</files>
  <read_first>
    - `relay/src/ws/fanout.ts` — `#isTeardown` 전문(docstring 포함), `#teardownMarket` 전문, `#strategyMarket`, `#strategyArmable` 전문, `lc.set` 분기(②-1·②-2 주석 포함). **한 번에 읽고 필요한 것을 전부 뽑아낸다**
    - `packages/shared/src/relay.ts` L120-245 — `RelayLimitChaser.crud` 계약 주석과 `RelayLimitChaserInput` 의 Omit 목록
    - `webapp/src/lib/limit-chaser.ts` L160-190 — `isDeleteIntent`/`crudOf` 원본 정의와 그 docstring(「`crud` 는 전송용 힌트일 뿐이다」)
    - `relay/src/ws/protocol.ts` L100-115 — `crud: z.enum(["C","D"])` 가 인바운드임을 직접 확인
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/16-29-SUMMARY.md` — GC-WR-04 가 **무엇을 얻으려 했는지**. 그 목적은 유지하고 수단만 바꾼다
  </read_first>
  <action>
① `#isTeardown(cfg)` 를 **게이트 4종 단독 판정**으로 바꾼다. 반환값은 `!cfg.buyEnabled && !cfg.sellEnabled && !cfg.cancelQtyEnabled && !cfg.cancelTradeEnabled` 하나다. `cfg.crud === "D"` 를 조기 반환 근거로 쓰지 않는다.

② `crud` 를 **불일치 신호**로만 남긴다: `cfg.crud === "D"` 인데 게이트가 하나라도 켜져 있으면 `logger.error` 를 남긴다. 로그 필드는 `{ userId, t, isin, crud: cfg.crud }` 수준으로 하고 **계좌번호는 싣지 않는다**(T-16-45 — 이 파일이 도처에서 지키는 규율, `#strategyArmable` 의 로그가 선례다). 메시지는 「`crud:"D"` 인데 게이트가 켜져 있다 — 철거로 보지 않고 등록 경로 가드를 적용한다」 취지로 한글로 적는다. `#isTeardown` 이 `userId`/`t` 를 인자로 받지 않는다면 시그니처를 넓히고 호출부 2곳(`lc.set` 분기 · `#strategyArmable`)을 함께 고친다.

③ docstring 을 **판정의 정본이 게이트임**을 말하는 문장으로 다시 쓴다. 반드시 담을 것:
   - 계약 원문(`packages/shared/src/relay.ts:136-141`)의 「게이트가 전부 꺼지면 **서버가** `"D"` 로 정규화한다」 인용과 그 파일 경로
   - `crud` 는 **인바운드 필드**라 브라우저·임의 wss 클라이언트가 정한다는 사실
   - `webapp/src/lib/limit-chaser.ts` `isDeleteIntent()` 와 **같은 네 항**이며 `sweepEnabled` 는 들어가지 않는다는 기존 ★ 주석은 그대로 살린다
   - 기존 ② 갈래(「`crud` 가 `"C"` 로 와도 게이트가 다 꺼졌으면 사실상 삭제」)는 **여전히 참이며 이제 유일한 갈래**라는 점

④ ★ `#strategyArmable` 첫 줄 `if (this.#isTeardown(cfg)) return true;` **를 지우지 않는다.** 이 줄은 무용지물이 아니다 — 게이트 4종이 전부 꺼져 있어도 `sweepEnabled: true` ∧ `sweepWatchPrice === 0`(또는 `buyOrderPrice === 0`)이면 `reason === "sweep"` 이 되어 **철거가 거부된다**. `sweepEnabled` 는 삭제 판정 4종에 없으므로 이 면제가 없으면 「한방만 켜 둔 전략을 못 지우는」 상태가 만들어진다. 그 이유를 그 줄 옆 주석에 남긴다(뒤에 오는 16-42 의 UI 측 대칭 수정 R2-WR-02 가 이 문장을 근거로 삼는다).

⑤ `lc.set` 분기(②-1/②-2 주석)의 서술을 새 판정에 맞춘다. 「시장 해석은 `crud` 로 갈린다」는 이제 거짓이다 — 「**게이트 상태**로 갈린다」로 고치고, 등록·수정(게이트가 하나라도 켜짐)은 `#strategyMarket`(못 풀면 거부) 그대로임을 다시 못박는다.

⑥ `#teardownMarket` 과 `#strategyMarket` 의 **정책은 건드리지 않는다.** 이번 변경은 「어느 쪽으로 갈지 정하는 판정」만 바꾼다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `#isTeardown` 본문(주석 제외)에 `cfg.crud === "D"` 를 **반환 근거로 쓰는 조기 반환이 없다**: `sed -n '<함수 시작>,<함수 끝>p' relay/src/ws/fanout.ts | grep -vE '^\s*(\*|//|/\*)' | grep -c 'return true'` 결과와 실제 반환문을 SUMMARY 에 인용
    - `#isTeardown` 반환식에 `cancelTradeEnabled` 가 포함된다 (소스 인용)
    - `crud` 불일치 `logger.error` 가 존재하고, 그 로그 객체에 `accountNo` 가 없다 (소스 인용)
    - `#strategyArmable` 의 `if (this.#isTeardown(cfg)) return true;` 가 **남아 있고** 그 이유(sweep 면제)를 설명하는 주석이 붙어 있다
    - `pnpm --filter @gh-radar/relay run typecheck` exit 0
    - `pnpm --filter @gh-radar/relay run typecheck:tests` exit 0
  </acceptance_criteria>
  <done>철거 판정이 게이트만 본다. `crud:"D"` 는 게이트와 일치할 때만 의미를 갖고, 어긋나면 로그가 남는다.</done>
</task>

<task type="auto">
  <name>Task 2: 회귀를 잠근다 — 기존 테스트 2건의 전제가 틀렸다</name>
  <files>relay/tests/fanout.test.ts</files>
  <read_first>
    - `relay/tests/fanout.test.ts` — `lcInput()` 헬퍼 전문(:124~), ⑰-c/⑰-d(무장 가드 케이스, :680-728), ⑰-e(:729-758), ⑰-e2(:759-789), ⑰-f(:790-830), 그리고 `authed()`·`framesOf()`·`gateway.strategyRequests()`·`rootEnvelope()` 헬퍼 시그니처. **한 번에 읽는다**
    - `.planning/phases/16-trading-limit-chaser-vi-my-page/deferred-items.md` §16-35 「이번 라운드가 드러낸 규율」 — 「기존 테스트가 결함을 진실로 잠그고 있을 수 있다」
  </read_first>
  <action>
⚠️ **깨지는 기존 테스트가 곧 회귀 신호는 아니다.** ⑰-e·⑰-e2 는 `lcInput({isin: UNKNOWN_ISIN, crud: "D"})` 를 보내는데 `lcInput()` **기본값이 `buyEnabled: true`** 다. 즉 두 테스트는 「진짜 철거」가 아니라 **정확히 이 갭이 지목한 스푸핑 조합**을 태우면서, 그 위험(무장 상태로 나갔다)을 단언하지 않고 초록이었다. 전제가 거짓이었던 것이지 수정이 틀린 것이 아니다. **수정을 약화시켜 테스트를 통과시키지 않는다.**

① ⑰-e 를 **진짜 철거**로 고친다: `lcInput({ isin: UNKNOWN_ISIN, crud: "D", buyEnabled: false, sellEnabled: false, cancelQtyEnabled: false, cancelTradeEnabled: false })`. 게이트 4종을 **명시적으로 전부** 적는다 — 헬퍼 기본값에 의존하면 같은 함정이 재발한다. 단언(거부 0건 · `market:"K"` 폴백 · 「시장 미해석 상태의 전략 삭제」 로그 · 계좌번호 미포함)은 그대로 유지한다. 케이스 제목에 「게이트 4종이 전부 꺼진」을 넣어 전제를 제목에 박는다.

② ⑰-e2 도 같은 방식으로 게이트 4종을 명시적으로 끈다. 「삭제의 시장은 에코 캐시가 1순위」 단언은 그대로다.

③ 새 케이스 **⑰-e3**: `crud:"D"` + `buyEnabled:true` + `UNKNOWN_ISIN` → **거부**. 단언 3개 — (a) `SetLimitChaserReq` 가 게이트웨이로 **나가지 않았다**(`gateway.strategyRequests()` 에 그 msgType 이 없다), (b) 거부 프레임 1건이 브라우저로 갔다, (c) 「`crud:"D"` 인데 게이트가 켜져 있다」 로그가 남았다. 이것이 시장 해석 엄격성(T-16-42) 복원의 잠금이다.

④ 새 케이스 **⑰-e4**: `crud:"D"` + `buyEnabled:true` + `buyOrderQty:0` + **알려진 ISIN**(SymbolMap 이 푸는 값) → **거부**. 시장은 풀리는데 무장 가드(T-16-43)가 잡는다는 것을 가른다 — ⑰-e3 과 실패 원인이 다른 케이스여야 두 가드가 각각 살아 있음을 증명한다. `#strategyArmable` 의 「발주가·수량 0 인 게이트 무장」 로그를 단언한다.

⑤ 새 케이스 **⑰-e5**: `crud:"C"` + 게이트 4종 전부 OFF + `UNKNOWN_ISIN` → **통과**(거부 0건, `SetLimitChaserReq` 송신). 게이트가 정본이라는 **대칭**을 잠근다 — 옛 탭·`crudOf` 를 안 태운 경로가 여기로 온다는 기존 ② 갈래의 회귀 잠금이다.

⑥ ⑰-f(`crud:"C"` + `buyEnabled:true` + UNKNOWN_ISIN → 거부)는 손대지 않는다. 통과하는지 확인만 한다.

⑦ **회귀 잠금을 실증한다** (이 phase 의 규율): Task 1 의 수정을 임시로 무력화(`#isTeardown` 첫 줄에 `if (cfg.crud === "D") return true;` 를 되돌림)한 뒤 ⑰-e3·⑰-e4 가 **실제로 실패하는지** 확인하고 복원한다. 몇 건이 실패했는지 SUMMARY 에 숫자로 적는다. 복원 확인은 소스에 임시 마커를 남기지 않았음을 grep 으로 증명한다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay exec vitest run tests/fanout.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f relay/tests/fanout.test.ts` 가 참임을 먼저 확인한 뒤 위 명령을 돌린다 (대상 부재 시 조용히 통과하는 검증을 만들지 않는다)
    - ⑰-e·⑰-e2 의 cfg 에 `buyEnabled: false`·`sellEnabled: false`·`cancelQtyEnabled: false`·`cancelTradeEnabled: false` 가 **명시**돼 있다 (소스 인용)
    - ⑰-e3·⑰-e4·⑰-e5 세 케이스가 존재하고 통과한다 (케이스 제목 SUMMARY 인용)
    - ⑰-e3 은 `SetLimitChaserReq` **미송신**을, ⑰-e5 는 **송신**을 단언한다 (서로 반대 방향)
    - 회귀 잠금 실증: 무력화 시 실패 건수 ≥ 2 를 관측하고 복원했다 (숫자 SUMMARY 인용, 복원 후 `pnpm --filter @gh-radar/relay test` exit 0)
    - `pnpm --filter @gh-radar/relay test` exit 0 · `pnpm --filter @gh-radar/relay run typecheck:tests` exit 0
    - relay 테스트 총 건수가 변경 전(373) 대비 증가했다 (전/후 숫자 SUMMARY 인용)
  </acceptance_criteria>
  <done>스푸핑 조합이 두 가드에 각각 걸리고, 진짜 철거는 여전히 막히지 않는다. 무력화하면 새 테스트가 실제로 빨개진다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저/임의 wss 클라이언트 → relay `lc.set` | `crud` 는 인바운드 필드다. 클라이언트가 정하는 값을 서버 가드의 면제 조건으로 쓰면 가드가 아니다 |
| relay → DMA 게이트웨이 (실계좌) | 2026-09-09 결선됨. 여기를 지나는 전략 설정은 실제 반복 발주가 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-42 | Tampering (시장 오기재) | `fanout.ts` `#teardownMarket` 우회 | mitigate | 철거 판정을 게이트 4종으로 좁혀 등록·수정이 `#strategyMarket`(못 풀면 거부)로만 가게 한다. ⑰-e3 이 잠근다 |
| T-16-43 | Elevation (무장 가드 우회) | `fanout.ts` `#strategyArmable` 조기 반환 | mitigate | 면제 조건이 게이트 상태가 된다. ⑰-e4 가 잠근다 |
| T-16-55 | Denial of Service (자산 인질) | 철거가 시장 해석 실패로 막힘 | mitigate | 게이트 4종 OFF 는 여전히 폴백 시장으로 통과. ⑰-e·⑰-e2·⑰-e5 가 잠근다 |
| T-16-45 | Information Disclosure | 새 `logger.error` 의 필드 | mitigate | 계좌번호를 싣지 않는다. `isin`·`crud`·`t` 만 |
| T-16-13 | 운영 사고 | 실서버·실계좌 접속 | accept | FakeGateway 만 쓴다. 실주문 검증 없음 (D-27) |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/relay test` exit 0
- `pnpm --filter @gh-radar/relay run typecheck` exit 0
- `pnpm --filter @gh-radar/relay run typecheck:tests` exit 0 (relay `tests/` 는 루트 typecheck 밖이다 — 반드시 따로 돌린다)
- 이 저장소에는 prettier 설정이 없다. **포매터를 돌리지 않는다** (16-30 사고)
</verification>

<success_criteria>
- 철거 판정의 정본이 게이트 4종이다
- `crud:"D"` + 게이트 ON 조합이 시장 해석·무장 가드 **양쪽**에 걸린다
- 게이트 4종 OFF 는 시장을 못 풀어도 통과한다 (GC-WR-04 유지)
- 새 테스트가 수정 무력화 시 실제로 실패한다
</success_criteria>

<output>
Create `.planning/phases/16-trading-limit-chaser-vi-my-page/16-36-SUMMARY.md` when done
</output>
