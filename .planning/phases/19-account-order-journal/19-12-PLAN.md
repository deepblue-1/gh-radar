---
phase: 19-account-order-journal
plan: 12
type: execute
wave: 8
depends_on: ["19-11"]
files_modified:
  - .planning/STATE.md
autonomous: false
requirements: [D-01, D-03, D-05, D-06, D-11, D-13, D-14]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-14 · D-13: 같은 배포 창에서 relay 를 새 이미지로 배포하면 부팅 즉시 관찰자가 게이트웨이에 로그인해 `/healthz` journal.state 가 live 가 되고, `dma_journal_cursor` 행과 `dma_account_access` 매핑 행(users.toml 계좌 수)이 생긴다 · DMA_HOST 는 실행 중 값이 보존된다"
    - "D-01 · D-05: 새 relay 는 `dma_orders` 에 쓰지 않는다 — 배포 뒤 `dma_orders` 의 최신 `updated_at` 이 배포 시각 이후로 늘지 않는다(동결 시작)"
    - "D-11: 게이트웨이 업그레이드 이후 저널 레코드가 매핑 여부와 무관하게 전부 `dma_journal_events` 에 적재되고 `apply_error` 가 0 이다"
    - "D-14 · Pitfall 12: server(GET /api/orders RPC 전환)는 webapp push **직전**에 배포되고, 사용자의 go 결정(체크포인트) 뒤에만 push 한다 · push 뒤 Vercel 이 실제로 그 커밋을 빌드·승격했는지 확인한다(Pitfall 13)"
    - "D-06 · D-03: 같은 DMA 계정을 공유하는 두 gh-radar 사용자에 대해 `dma_journal_orders_for_user` 결과의 id 집합이 같다(당일 행이 없으면 둘 다 빈 집합 — 실측은 19-13)"
  artifacts: []
  key_links:
    - from: "deploy-relay.sh"
      to: "https://dma.jx1.io/healthz journal"
      via: "관찰자 부팅 결선(19-10)"
      pattern: "journal"
    - from: "git push origin master"
      to: "Vercel 프로덕션"
      via: "vercel-ignore-build.sh 가 webapp 변경을 감지해 빌드"
      pattern: "vercel"
  prohibitions:
    - "MUST NOT relay 검증 전에 server 배포·push 한다 (메모리 「배포는 relay 먼저·push 나중」)"
    - "MUST NOT DMA_HOST 를 주입한다 — 실행 중 값 보존(주입 생략)"
    - "MUST NOT main 체크아웃 작업 트리 그대로 relay 이미지를 빌드한다 — 배포 커밋의 detached worktree 에서(메모리 relay 빌드 원천 함정)"
    - "MUST NOT 비밀 값을 출력한다 — 서비스롤 키는 셸 변수로만"
---

<objective>
**전환 배포 창 ②** — D-14 순서의 나머지. relay 배포 → 관찰자 경로 프로덕션 검증 → 사용자 go 결정 → server 배포 → webapp push → 배포 확인 · 공유 계정 동일 표시 확인. 19-11 과 **같은 창**(20:00 이후)에서 잇는다.

Purpose: push 는 곧 webapp 프로덕션 배포이고 webapp 은 새 server 응답 모양·새 wss 프레임에 결합돼 있다 — relay(+server)가 먼저 서고 검증된 뒤에만 push 한다(Phase 17 반쪽 배포 교훈). 이 plan 은 배포·검증 plan 이라 구현 tracer 가 없다(의도 — 관찰자 경로의 종단 증명은 19-10 부팅 테스트와 이 plan 의 프로덕션 healthz 가 맡는다).
Output: SUMMARY 에 배포 해시 · healthz/행 수/동결 확인 원문 · 사용자 결정 · Vercel 배포 확인 · 공유 계정 대조.

커밋 규율: 저장소 파일 변경 없음(SUMMARY 만) · push 는 Task 3 에서 **사용자 go 뒤 한 번** · 직전 `git status -sb`(동시 세션 경합 — 남의 로컬 커밋이 섞이면 멈추고 보고).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-account-order-journal/19-11-SUMMARY.md

근거(메모리 · 비밀 아님): relay 배포 = `GCP_PROJECT_ID=gh-radar SUPABASE_URL=https://ivdbzxgaapbmrxreyuht.supabase.co NOTIFICATION_CHANNEL_ID=14409521670382124894 bash scripts/deploy-relay.sh`(DMA_HOST 미주입 → 실행 중 값 보존 · 약 3~4분) — **배포할 커밋의 detached worktree**(`git worktree add --detach <scratchpad>/relay-deploy <sha>`)에서 실행하고 끝나면 `git worktree remove`. 라이브 env 되읽기: `gcloud compute ssh radar-gw --zone=asia-northeast3-a --tunnel-through-iap --command "sudo docker inspect gh-radar-relay --format '{{range .Config.Env}}{{println .}}{{end}}'"`(값 중 비밀 줄은 출력하지 않도록 `grep -E '^(DMA_HOST|APP_VERSION|SUPABASE_URL)='` 로 거른다). server 배포 = `scripts/deploy-server.sh` · `SUPABASE_URL` + `CORS_ALLOWED_ORIGINS` 는 `gcloud run services describe gh-radar-server --region=asia-northeast3 --format=json` 의 containers[0].env 에서 추출(비밀 아님). Vercel: `scripts/vercel-ignore-build.sh` 가 직전 배포 이후 webapp/·packages/shared/·lockfile 변경을 보면 빌드 — 확인 `vercel inspect https://gh-radar-webapp.vercel.app`, 건너뛰었으면 repo root 에서 `vercel pull --yes --environment=production && vercel build --prod && vercel deploy --prebuilt --prod`. 서비스롤 키는 `gcloud secrets versions access latest --secret=gh-radar-supabase-service-role --project=gh-radar` 를 셸 변수에만 담는다(출력 금지).
</context>

<tasks>

<task type="auto">
  <name>Task 1: relay 배포(detached worktree) → 관찰자 프로덕션 검증 · 매핑·커서·이벤트 · dma_orders 동결 확인</name>
  <precondition>19-11 SUMMARY 에 G2 완료(epoch)와 원격 DB 반영이 기록돼 있고 현재 KST 가 20:00 이후다</precondition>
  <files>(배포 — 저장소 파일 없음)</files>
  <read_first>
    - .planning/phases/19-account-order-journal/19-11-SUMMARY.md (롤백 태그 · epoch · 동반 배포)
    - scripts/deploy-relay.sh (1-60 사용법 · 182-205 비밀 4종 검사)
    - scripts/smoke-relay.sh (사용법 · health_probe)
    - docs/relay-operations.md (관찰자 기록 연결 항목 · 롤백)
  </read_first>
  <action>
① `git status -sb` → 배포 커밋 SHA 확정(19-10 까지의 phase 커밋이 모두 들어간 HEAD, 다른 세션 커밋이 섞였으면 멈추고 보고) → `git worktree add --detach <scratchpad>/relay-deploy <sha>` → 그 안에서 메모리의 relay 배포 명령(DMA_HOST 미주입). 비밀 4종 사전 검사 실패면 멈추고 보고.
② 검증(원문을 SUMMARY 에): `curl -s https://dma.jx1.io/healthz` → `version` = 배포 SHA · `journal.state` = "live"(replaying 이면 1분 간격 3회까지 재확인) · `vpn` true · 식별자 키 없음. 컨테이너 env 되읽기로 `DMA_HOST` 가 배포 전과 같음(값은 사내 주소라 SUMMARY 에는 「보존됨」 만). 서비스롤 키를 셸 변수에 담아 PostgREST 로(값 출력 금지): `dma_journal_cursor?gateway=eq.KB` 1행(epoch = 19-11 의 게이트웨이 epoch) · `dma_account_access?gateway=eq.KB` 행 수(사용자에게 users.toml 계좌 수와 대조 요청) · `dma_journal_events` 행 수와 `apply_error=not.is.null` 행 수(0) · `dma_orders?select=updated_at&order=updated_at.desc&limit=1` 의 최신 시각이 배포 시각 이전(동결 — 이후 19-13 에서 다시 확인).
③ `bash scripts/smoke-relay.sh` PASS 요약. relay 로그에서 `[JOURNAL]` 줄(로그인 성공 · 매핑 N행 · resync 여부)을 `gcloud compute ssh ... sudo docker logs --since 15m gh-radar-relay | grep -E "\[JOURNAL\]|\[journal\]"` 로 확인해 요약(계좌 원문 없음).
④ `git worktree remove <scratchpad>/relay-deploy`.
검증 중 하나라도 어긋나면 server·push 로 가지 않는다 — Task 2 체크포인트에서 롤백(`deploy-relay.sh --rollback <19-11 기록 태그>`)을 제안한다.
  </action>
  <verify>
    <automated>curl -sf https://dma.jx1.io/healthz | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const j=JSON.parse(s);console.log("journal.state="+(j.journal&&j.journal.state));process.exit(j.journal&&j.journal.state==="live"?0:1)})'</automated>
    <fails_when>non-zero exit, 또는 출력이 "journal.state=live" 가 아님</fails_when>
    <automated>curl -sf https://dma.jx1.io/healthz | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const bad=/"(accountNo|userId|account_no|user_id)"/.test(s);console.log(bad?"identifier-key-present":"no-identifier-keys");process.exit(bad?1:0)})'</automated>
    <fails_when>non-zero exit, 또는 출력에 "identifier-key-present"</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 배포 SHA · healthz 원문(journal 필드) · 커서 1행 · 매핑 행 수 · 이벤트 수 · apply_error 0 · dma_orders 최신 updated_at · smoke 요약
    - `git worktree list` 에 relay-deploy 가 남아 있지 않다
  </acceptance_criteria>
  <done>프로덕션 relay 가 관찰자로 게이트웨이 저널을 받아 새 테이블에 기록하고 있고, 옛 테이블 쓰기는 멈췄다.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: server 배포 + webapp push 진행 여부 (push = webapp 프로덕션 배포)</name>
  <decision>relay 검증 결과를 보고 server 배포와 webapp push 를 진행한다</decision>
  <context>Task 1 의 SUMMARY 원문(healthz journal live · 커서 · 매핑 행 수 · apply_error 0 · dma_orders 동결 · smoke)을 표로 보여 준다. push 하면 webapp 이 즉시 프로덕션에 나가고 「오늘 주문」 카드가 새 원천(REST RPC + journal.rows)과 B′ 로 바뀐다 — server 가 먼저 새 응답 모양으로 서야 한다(Pitfall 12). 매핑 행 수가 users.toml 계좌 수와 다르면 여기서 멈추는 것이 맞다(가시성이 틀린다).</context>
  <options>
    <option id="proceed">
      <name>server 배포 후 push</name>
      <pros>전환 완료 — 다음 거래일부터 카드가 새 테이블만 읽는다(D-14)</pros>
      <cons>이후 문제는 webapp 이전 배포 승격 + server 이전 revision + relay 롤백이 모두 필요</cons>
    </option>
    <option id="rollback">
      <name>relay 롤백(19-11 기록 태그)</name>
      <pros>옛 기록 경로로 즉시 복귀 — 원격 DB 는 추가 전용이라 그대로 둬도 무해</pros>
      <cons>전환 재시도에 다시 창이 필요 · gh-trade 는 새 바이너리로 남는다(관찰자 없음 → 저널은 디스크에 쌓인다)</cons>
    </option>
    <option id="hold">
      <name>relay 는 두고 push 보류</name>
      <pros>관찰자 기록은 계속 쌓인다 — 다음 창에서 server·push 만 하면 된다</pros>
      <cons>그 사이 카드는 동결된 dma_orders 를 읽어 오늘 주문이 비어 보인다(구 server + 구 webapp)</cons>
    </option>
  </options>
  <resume-signal>「proceed」 · 「rollback」 · 「hold」</resume-signal>
</task>

<task type="auto">
  <name>Task 3: server 배포 → webapp push → Vercel 배포 확인 → 공유 계정 동일 표시 확인</name>
  <precondition>Task 2 resume-signal 이 proceed 다</precondition>
  <files>.planning/STATE.md</files>
  <read_first>
    - scripts/deploy-server.sh (1-40 사용법·필수 env)
    - scripts/vercel-ignore-build.sh (빌드 판정)
    - server/src/services/dma-orders.ts (RPC 이름·파라미터)
  </read_first>
  <action>
① server: 라이브 Cloud Run env 에서 `SUPABASE_URL` · `CORS_ALLOWED_ORIGINS` 를 추출해(비밀 아님) `GCP_PROJECT_ID=gh-radar ... bash scripts/deploy-server.sh` → 새 revision · `curl -s -o /dev/null -w '%{http_code}' https://<server>/api/orders` = 401(라우트 생존 · 인증 필요) · 스모크(`scripts/smoke-server.sh` 가 있으면 PASS 요약).
② push: `git status -sb`(ahead 커밋이 이 phase 의 것뿐인지 · 다른 세션 커밋 섞이면 멈추고 보고) → 커밋 메시지는 이미 되어 있으므로 `git push origin master` 한 번. 전역 규칙상 push 전 사용자 확인은 Task 2 의 proceed 가 그것이다.
③ Vercel: `vercel inspect https://gh-radar-webapp.vercel.app` 로 방금 커밋의 배포가 prod alias 인지(age · git sha) 확인 — 건너뛰었으면 repo root 에서 prebuilt 수동 배포(context 의 3줄) 후 다시 확인.
④ 공유 계정 대조(D-06): 서비스롤로 `dma_credentials?select=user_id,dma_user_id` 를 읽어 같은 `dma_user_id` 를 가진 사용자 묶음을 찾고(사용자 id 는 SUMMARY 에 앞 4자만), 각 사용자에 대해 `rpc/dma_journal_orders_for_user`(오늘 KST 날짜)를 불러 id 집합이 묶음 안에서 같음을 확인한다. 20:00 이후라 행이 0 일 수 있다 — 그 사실을 적고 실측은 19-13 으로 넘긴다.
⑤ STATE.md 에 「Phase 19 전환 완료 · relay {SHA} · server {revision} · webapp {배포} · 첫 거래일 대조 19-13 대기」 를 기록한다(ROADMAP·STATE 수정은 main 트리에서 — worktree 격리 금지, 메모리).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && git fetch origin master --quiet && test "$(git rev-parse HEAD)" = "$(git rev-parse origin/master)" && echo pushed</automated>
    <fails_when>non-zero exit, 또는 출력에 "pushed" 가 없음(로컬 HEAD 와 origin/master 불일치)</fails_when>
    <automated>vercel inspect https://gh-radar-webapp.vercel.app 2>&1 | grep -ciE "ready|READY"</automated>
    <fails_when>non-zero exit, 또는 출력 0(프로덕션 배포가 Ready 가 아님)</fails_when>
    <human-check>
      <test>프로덕션 https://gh-radar-webapp.vercel.app/me 를 폰과 데스크톱에서 연다</test>
      <expected>「오늘 주문」 카드가 계좌별 묶음(B′)으로 뜨고(당일 행이 없으면 빈 상태 문구) 오류 문구가 없다 · 기록 지연 배지는 relay journal live 라 보이지 않는다</expected>
      <why_human>프로덕션 로그인 세션과 실제 계좌 매핑으로만 확인된다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 server revision · push 해시 · Vercel 배포 확인 원문 · 공유 계정 대조 결과(묶음 수 · 사용자별 행 수 · id 집합 동일)
    - STATE.md 에 전환 기록 1줄
  </acceptance_criteria>
  <done>전환이 끝났다 — relay 는 관찰자로 기록하고, server 는 새 테이블을 읽고, webapp 은 B′ 카드로 보여 준다. 같은 계정을 쓰는 사용자는 같은 행을 본다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 운영자 → 프로덕션(relay VM · Cloud Run · Vercel) | 배포 순서가 곧 가용성 |
| 서비스롤 키(검증용) | 셸 변수로만, 출력 금지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-18 | Denial of Service | 반쪽 배포(webapp 먼저) | medium | mitigate | relay 검증 → 사용자 go → server → push 순서 · 체크포인트 |
| T-19-41 | Tampering | 다른 세션 커밋이 이미지·push 에 섞임 | medium | mitigate | detached worktree 빌드 · `git status -sb` 재확인 · 섞이면 정지 |
| T-19-42 | Denial of Service | DMA_HOST 조용한 강등(mock) | high | mitigate | 주입 생략 · 배포 후 컨테이너 env 되읽기 대조 |
| T-19-03 | Information Disclosure | 서비스롤 키·관찰자 비밀 | high | mitigate | 셸 변수만 · env 되읽기 필터(비밀 줄 제외) · SUMMARY 에 값 없음 |
| T-19-07 | Information Disclosure | 프로덕션 healthz 식별자 | medium | mitigate | healthz 식별자 키 grep(verify 두 번째 명령) · smoke health_probe |
</threat_model>

<verification>
- 프로덕션 healthz journal live · 식별자 없음 · push = origin/master · Vercel Ready · 공유 계정 대조.
- spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</verification>

<success_criteria>
- D-14 순서(DB → gh-trade → relay → 검증 → server → webapp push)가 한 창에서 완료되고 각 단계 증거가 SUMMARY 에 있다.
</success_criteria>

## Artifacts this phase produces (이 plan 분)

| 종류 | 이름 |
|------|------|
| 프로덕션 | relay 이미지(배포 SHA) · server revision · Vercel 프로덕션 배포 |
| 기록 | STATE.md 전환 기록 · 19-12-SUMMARY(검증 원문) |

<output>
Create `.planning/phases/19-account-order-journal/19-12-SUMMARY.md` when done
</output>
