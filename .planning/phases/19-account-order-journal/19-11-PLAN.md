---
phase: 19-account-order-journal
plan: 11
type: execute
wave: 7
depends_on: ["19-03", "19-08", "19-10"]
files_modified: []
autonomous: false
requirements: [D-05, D-09, D-10, D-14]
user_setup:
  - service: gh-trade 게이트웨이(KB 120)
    why: "관찰자 계약을 구현한 게이트웨이 바이너리 배포·재시작과 관찰자 비밀 파일 배치는 VPN/WG 너머 120 에서만 가능하다(Claude 접근 경로 없음)"
    dashboard_config:
      - task: "gh-trade Phase 23 산출 바이너리 배포 + 20:00 이후 stock-dma 재시작 + config/observer.toml(600) 배치"
        location: "gh-trade 저장소 scripts/deploy.sh kb prod-120 · 120 SSH"
  - service: GCP Secret Manager
    why: "관찰자 비밀 값은 사람만 생성·주입한다(스크립트는 빈 껍데기와 접근권만 만든다)"
    env_vars:
      - name: gh-radar-dma-observer-secret
        source: "한 번 생성한 값을 Secret Manager 새 버전과 120 config/observer.toml 에 같은 값으로 — 터미널 비출력 파이프(infra/relay/README.md 관찰자 비밀 절)"

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-14: 전환은 장 마감(20:00 KST) 뒤 한 배포 창에서 사용자 승인으로 시작하고, 순서는 DB → gh-trade 게이트웨이 → (19-12) relay → 검증 → server → webapp push 다 · 그림자 병행 없음"
    - "D-05: 원격 DB 에 새 계좌 기준 테이블·RPC 마이그레이션 2개만 적용된다(dry-run 으로 대상 확인) — 추가 전용이라 라이브 relay 무영향 · `dma_orders` 는 그대로(동결은 19-12 relay 배포로 쓰기 주체가 사라지는 시점부터)"
    - "T-19-01 · T-19-11: 원격 반영 뒤 덤프에서 네 테이블 RLS 활성 · 정책 0 · 여섯 함수의 권한이 service_role 뿐임을 확인한다"
    - "D-10: 관찰자 비밀 Secret Manager 껍데기와 relay SA 접근권이 준비되고, 사람이 같은 값을 Secret Manager 와 120 `config/observer.toml`(600) 에 넣는다 — 값은 대화·로그·저장소에 남지 않는다"
    - "D-09 · G2: gh-trade 게이트웨이가 관찰자 연결의 주문·전략·시세·계좌 요청을 서버측에서 거부하는 빌드(cloud-verify 통과)이고, 동반 배포 목록(120 미배포 quick 들)을 사용자가 확인했다"
  artifacts: []
  key_links:
    - from: "supabase db push"
      to: "원격 public 스키마"
      via: "migration list --linked 에 20260924200000 · 20260924200100 Remote 적용"
      pattern: "20260924200100"
  prohibitions:
    - "MUST NOT checkpoint 승인 전에 `supabase db push`(dry-run 제외)를 실행한다"
    - "MUST NOT 20:00 KST 이전에 게이트웨이 재시작을 요청·수행한다 (D-14 · 전략 무인 복원)"
    - "MUST NOT 비밀 값을 생성·출력·저장한다(Claude) — 사람 몫"
    - "MUST NOT relay · server 배포나 push 를 이 plan 에서 한다 — 19-12"
---

<objective>
**전환 배포 창 ①** — D-14 순서의 앞 두 단계. 사용자의 창 진입 승인(G2 확인 포함) → [BLOCKING] 원격 DB push(새 테이블·RPC) + 권한 확인 + 관찰자 비밀 껍데기·IAM → 사람이 비밀 값을 두 곳에 넣고 gh-trade 게이트웨이를 배포·재시작한다. relay·server·webapp 은 19-12 가 같은 창에서 잇는다.

Purpose: D-05 는 일방향 문이다(새 테이블 전환 뒤 `dma_orders` 로 돌아가면 전환 이후 데이터가 없다 — discuss-phase 에서 사용자가 이미 승인). 이 plan 은 그 결정을 다시 묻지 않고, **프로덕션 DB 반영과 실서버 재시작의 운영 승인**을 받는다. 구현 tracer 는 19-01·19-05·19-09·19-10 이 이미 증명했다 — 이 plan 은 배포·검증 plan 이라 tracer task 가 없다(의도).
Output: SUMMARY 에 승인 기록 · db push/덤프 확인 원문 · 사람이 한 단계의 확인 결과.

커밋 규율: 저장소 파일 변경 없음(SUMMARY 만) · `git status -sb` 재확인 · push 금지.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-account-order-journal/19-CONTEXT.md
@.planning/phases/19-account-order-journal/19-GH-TRADE-HANDOFF.md

근거: RESEARCH §배포 순서 (D-14)(669-690) · Pitfall 12·13·14 · 18-29 가 실제로 통과한 원격 반영 순서(`supabase migration list --linked` → `supabase db push --dry-run` → `supabase db push --yes` → `migration list` → `supabase db dump --linked -s public` 전후 diff, 자격은 `supabase/.temp/project-ref` + CLI 저장 로그인). `scripts/setup-relay-iam.sh` 는 `GCP_PROJECT_ID=gh-radar` 필수 · `--dry-run` 지원 · Secret 껍데기만 만든다. 자격: deployer SA(`GOOGLE_APPLICATION_CREDENTIALS`) — 사용자에게 다시 묻지 않는다(메모리).
휴장: 2026-09-24(목)·25(금) 추석 연휴 · 첫 거래일은 2026-09-28(월) — 전환이 이번 주면 첫 거래일 대조는 19-13 에서 09-28 장 마감 뒤.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: 전환 배포 창 진입 승인 (20:00 이후 · G2 · 동반 배포 목록 · D-05 일방향 문 재확인)</name>
  <decision>지금 전환 배포 창(DB → gh-trade → relay → 검증 → server → webapp push)을 연다</decision>
  <context>
    진입 조건 — Claude 가 이 체크포인트를 띄우기 전에 확인해 목록으로 보여 준다:
    ① 현재 KST 가 20:00 이후(`TZ=Asia/Seoul date`) — 게이트웨이 재시작은 전략 무인 복원을 수반한다(D-14).
    ② gh-radar HEAD 전량 게이트 green: `pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/relay run test && pnpm --filter @gh-radar/webapp run test && pnpm --filter @gh-radar/server exec vitest run` + pgTAP 2파일 + `sync-relay-schema.sh --check` 차이 0 — 결과 요약을 보여 준다.
    ③ G2: gh-trade Phase 23 빌드 게이트(cloud-verify) 통과 · 관찰자 서버측 요청 거부(D-09) 구현 확인 · observer.toml 배치 절차 합의 · **동반 배포 목록**(gh-trade STATE 의 120 미배포 quick 들 — Pitfall 14)을 사용자가 봤다.
    ④ 라이브 relay 버전(`curl -s https://dma.jx1.io/healthz` 의 version)을 롤백 태그로 기록.
    D-05 는 일방향 문이다 — discuss-phase 에서 이미 승인됐고 여기서는 운영 승인만 받는다. 롤백 경로: relay 이전 태그(옛 기록 경로 부활 = 수용 가능한 롤백 상태) · server 이전 revision · webapp 이전 배포 승격 · gh-trade 백업 바이너리 · DB 는 추가 전용이라 롤백 불요.
  </context>
  <options>
    <option id="proceed">
      <name>지금 창을 연다</name>
      <pros>D-14 한 번 전환 — 다음 거래일(최초 2026-09-28)부터 부재 중 자동주문이 계좌 기준으로 빠짐없이 기록된다</pros>
      <cons>문제 시 롤백에 relay·server·webapp·gh-trade 재배포가 모두 필요(D-14 costly) · 그림자 병행 없음 — 19-13 첫 거래일 대조가 유일한 실장 검증</cons>
    </option>
    <option id="postpone">
      <name>미룬다</name>
      <pros>G2 미충족·진입 조건 미달 시 안전</pros>
      <cons>그 사이 사용자 세션 기록(dma_orders) 누락이 계속된다(09-23 43 vs 2 재현 가능)</cons>
    </option>
  </options>
  <resume-signal>「proceed」 또는 「postpone {사유}」</resume-signal>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] 원격 DB push — 새 테이블·RPC 반영 · 덤프로 권한 확인 · 관찰자 비밀 껍데기·IAM</name>
  <reversibility rating="one-way">D-05 — 원격에 새 계좌 기준 테이블이 생기고 곧 유일한 기록처가 된다. 전환 뒤 dma_orders 로 돌아가면 전환 이후 데이터가 없다(discuss-phase 사용자 승인 · Task 1 운영 승인).</reversibility>
  <precondition>Task 1 resume-signal 이 proceed 이고 현재 KST 가 20:00 이후다</precondition>
  <files>(원격 DB · GCP 리소스 — 저장소 파일 없음)</files>
  <read_first>
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-29-SUMMARY.md (원격 반영 실제 순서 · 출력 형태)
    - supabase/migrations/20260924200000_dma_journal_tables.sql · supabase/migrations/20260924200100_dma_journal_rpcs.sql (함수 시그니처 원문)
    - scripts/setup-relay-iam.sh (1-40 사용법 · 170-215 Secret 루프)
  </read_first>
  <action>
① 읽기 전용 사전: `supabase migration list --linked` 로 Local/Remote 가 어긋난 행이 `20260924200000` · `20260924200100` 두 개뿐인지 본다 → `supabase db dump --linked -s public > <scratchpad>/19-before.sql`(저장소 밖) → `supabase db push --dry-run` 이 그 두 파일만 적용 대상으로 말하는지 본다. 다른 미적용 마이그레이션이 섞이면 멈추고 보고한다.
② `supabase db push --yes` — 출력에 두 파일의 「Applying migration …」 과 「Finished supabase db push.」 가 있어야 한다. 실패하면 원문을 적고 멈춘다(손으로 고치지 않는다).
③ `supabase migration list --linked` 로 두 버전의 Remote 열이 채워졌는지 확인 → `supabase db dump --linked -s public > <scratchpad>/19-after.sql` → 전후 diff 가 새 테이블 4 · 인덱스 · 함수 6 · 권한 문장뿐이고 `dma_orders` 관련 줄 변화 0 · 새 테이블 대상 정책 생성 0 · 네 테이블 `ENABLE ROW LEVEL SECURITY` · 여섯 함수에 대한 권한 문장이 `service_role` 뿐(anon · authenticated 대상 GRANT 0)임을 확인해 SUMMARY 에 해당 줄 원문(값 없음)과 함께 적는다.
④ `GCP_PROJECT_ID=gh-radar bash scripts/setup-relay-iam.sh --dry-run` 으로 계획을 본 뒤 실제 실행 — `gh-radar-dma-observer-secret` 빈 껍데기 생성(이미 있으면 유지) + relay SA accessor 바인딩. 값은 만들지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && supabase migration list --linked 2>&1 | grep -cE "20260924200100 +\| +20260924200100"</automated>
    <fails_when>non-zero exit, 또는 출력이 0(원격 미적용)</fails_when>
    <automated>gcloud secrets describe gh-radar-dma-observer-secret --project=gh-radar --format='value(name)'</automated>
    <fails_when>non-zero exit, 또는 출력에 "NOT_FOUND"</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 dry-run · push · migration list 원문과 전후 덤프 diff 요약(테이블 4 · 함수 6 · 권한 service_role 뿐 · dma_orders 변화 0 · 정책 0)
    - `supabase migration list --linked` 에서 `20260924200000` 과 `20260924200100` 둘 다 Local = Remote
    - Secret 껍데기 존재 + relay SA accessor 바인딩(스크립트 출력 「✓ secretAccessor bound: gh-radar-dma-observer-secret」)
  </acceptance_criteria>
  <done>원격 DB 에 새 기록 테이블이 브라우저 역할에게 닫힌 채 생겼고, 관찰자 비밀을 받을 자리가 준비됐다.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: [BLOCKING G2] 관찰자 비밀 주입(두 곳 같은 값) · gh-trade 게이트웨이 배포·재시작</name>
  <action>관찰자 비밀 값을 만들어 Secret Manager 와 120 에 넣고, gh-trade 게이트웨이를 배포·재시작한다 — Claude 는 120(VPN/WG 너머)에 닿을 수 없고 비밀 값을 다루지 않는다.</action>
  <instructions>
    Claude 가 이미 한 것: 원격 DB 반영(Task 2) · Secret 껍데기 `gh-radar-dma-observer-secret` + relay SA 접근권 · relay 배포 스크립트의 4번째 비밀 검사·주입(19-10) · 절차 문서 `infra/relay/README.md` 「관찰자 비밀 주입」 절.
    사람이 할 것:
    1) 값 한 번 생성 → Secret Manager `gh-radar-dma-observer-secret` 새 버전 + 120 `config/observer.toml`(`[observer] secret`, chmod 600, git 미추적)에 **같은 값** — 터미널에 찍지 않는 파이프로(README 절차).
    2) gh-trade 저장소에서 `./scripts/deploy.sh kb prod-120`(바이너리 + server.toml, 자동 재시작 없음) → **20:00 이후** `systemctl restart stock-dma`(전략 무인 복원 동반).
    3) 게이트웨이 기동 로그에서 저널 epoch · seq 복원 · observer 설정 로드(비밀 값 없이 「loaded」 류)를 확인하고, 동반 배포된 변경 목록을 한 줄로 알려 준다.
  </instructions>
  <verification>Claude 가 확인한다: `gcloud secrets versions list gh-radar-dma-observer-secret --filter='state=ENABLED' --format='value(name)' --project=gh-radar` 가 1줄 이상(값은 읽지 않는다) · 사용자가 알려 준 게이트웨이 기동 로그 요약에 epoch 와 observer 설정이 있다.</verification>
  <resume-signal>「G2 완료 — epoch {값} · 동반 배포 {목록}」 또는 문제 설명</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 운영자 → 프로덕션 DB | 원격 스키마 변경은 사람 승인 뒤에만 |
| 운영자 → Secret Manager · 120 파일 | 관찰자 비밀의 생성·주입 지점 |
| gh-trade 게이트웨이(실서버) | 관찰자 요청 거부(D-09)의 서버측 강제 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Information Disclosure (IDOR) | 원격 조회 RPC 권한 | high | mitigate | 반영 뒤 덤프로 service_role 전용 확인(Task 2 ③) |
| T-19-11 | Information Disclosure | 원격 새 테이블 | high | mitigate | 덤프로 RLS 활성 · 정책 0 · anon/authenticated GRANT 0 확인 |
| T-19-03 | Information Disclosure | 관찰자 비밀 생성·주입 | high | mitigate | 사람만 · 비출력 파이프 · 600 파일 · Claude 는 버전 존재만 확인 |
| T-19-04 | Elevation of Privilege | 관찰자 연결로 주문·전략 | high | transfer | gh-trade Phase 23 서버측 강제 — G2 확인 항목(Task 1 ③) |
| T-19-39 | Tampering | 미확인 마이그레이션 동반 반영 | medium | mitigate | dry-run 으로 대상 2파일만 확인 · 섞이면 정지 |
| T-19-40 | Denial of Service | 장중 게이트웨이 재시작 | high | mitigate | 20:00 이후만(precondition · Task 3 지시) |
</threat_model>

<verification>
- 원격 migration list 두 버전 Remote 적용 · 덤프 권한 확인 · Secret ENABLED 버전 존재 · G2 사용자 확인.
- spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</verification>

<success_criteria>
- 사용자 승인 아래 DB → gh-trade 두 단계가 끝나고 19-12 가 relay 부터 이을 수 있다.
</success_criteria>

## Artifacts this phase produces (이 plan 분)

| 종류 | 이름 |
|------|------|
| 원격 DB | `20260924200000_dma_journal_tables` · `20260924200100_dma_journal_rpcs` 적용(테이블 4 · 함수 6) |
| GCP | Secret `gh-radar-dma-observer-secret`(ENABLED 버전 1) + relay SA accessor |
| 게이트웨이 | gh-trade Phase 23 바이너리 · 120 `config/observer.toml`(600) |

<output>
Create `.planning/phases/19-account-order-journal/19-11-SUMMARY.md` when done
</output>
