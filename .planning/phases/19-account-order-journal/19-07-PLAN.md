---
phase: 19-account-order-journal
plan: 07
type: execute
wave: 4
depends_on: ["19-05"]
files_modified:
  - relay/src/journal/observer.ts
  - relay/src/journal/status.ts
  - relay/src/journal/trading-window.ts
  - relay/src/ws/fanout.ts
  - relay/src/order/order-api.ts
  - relay/src/config.ts
  - relay/src/logger.ts
  - relay/tests/journal-observer.test.ts
  - relay/tests/journal-status.test.ts
  - relay/tests/order-api.test.ts
autonomous: true
requirements: [D-04, D-09, D-10, D-12, D-13]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-13: 관찰자는 비밀이 있으면 기동 즉시 커서를 읽고 붙는다 — 끊기면 `DmaClient` 의 상한 있는 무한 백오프(1→30초)로 계속 재접속하고, 장 시간 판정은 연결 유지에 쓰지 않는다"
    - "D-13 · D-09: 게이트웨이가 관찰자 로그인을 거부하면 `stopReconnect` 로 루프를 멈추고 state `rejected` 로 확정한다 — 그 뒤 다시 로그인을 보내지 않는다(재시작 전 복구 없음)"
    - "D-09(relay 몫): 관찰자 연결은 로그인 요청 외에 아무 요청도 보내지 않는다(LivePing 은 DmaClient 가 소유) — 테스트가 송신 페이로드 전부가 로그인이었음을 단언한다"
    - "D-12: 재접속 로그인의 since_seq 는 기록기의 마지막 수신 seq, epoch 는 기록기의 epoch 다 · 로그인 응답의 resync 는 기록기 epoch 교체로 이어진다 · 기록기가 gap/overflow 를 돌리면 관찰자가 연결을 끊는다"
    - "D-04 (b): `/healthz` 가 `journal{state, lastSeq, headSeq, lagSeq, disconnectedSec, lastAppliedAgeSec}` 를 싣고, 장중(평일 · KRX 휴장일 아님 · 08:00~20:00 KST) 에 state 가 rejected 이거나 live 아님이 180초 이상이면 503 degraded — 장 밖에서는 본문에만 드러내고 200"
    - "D-04 (a) relay 몫: live 를 벗어난 지 10초가 지나면 인증된 모든 연결에 `{t:\"journal.state\", s:\"delayed\", since}` · 복구되면 `s:\"live\"` · 인증 직후 스냅샷 1프레임(모르면 안 보냄)"
    - "D-10 · T-19-03: 관찰자 비밀은 env `DMA_OBSERVER_SECRET` 으로만 들어오고 production 에서 없으면 기동이 실패한다 · 어떤 로그에도 평문이 없다(pino redact + 로그 인자 금지)"
  artifacts:
    - path: relay/src/journal/observer.ts
      provides: "JournalObserver 상태기계(DmaClient · JournalCodec 주입)"
      exports: ["JournalObserver", "OBSERVER_CLIENT_NAME"]
    - path: relay/src/journal/status.ts
      provides: "JournalStatus(journal.state 프레임 · healthz 스냅샷)"
      exports: ["JournalStatus", "JOURNAL_DELAYED_AFTER_MS", "JOURNAL_ALERT_AFTER_MS"]
    - path: relay/src/journal/trading-window.ts
      provides: "inTradingWindow(now) — 알림 판정 전용"
    - path: relay/src/order/order-api.ts
      provides: "HealthPayload.journal · 장중 503 규칙"
      contains: "journal"
  key_links:
    - from: "JournalObserver 'state'"
      to: "JournalStatus → WsFanout.deliverJournalState"
      via: "10초 디바운스 뒤 delayed · live 복귀 즉시"
      pattern: "deliverJournalState"
    - from: "createOrderApi /healthz"
      to: "JournalStatus.health(now) + inTradingWindow(now)"
      via: "journalAlerting → healthy AND"
      pattern: "inTradingWindow"
  prohibitions:
    - "MUST NOT `isKoreanMarketOpen`(08:00~15:30)을 쓴다 — 창은 08:00~20:00 (RESEARCH Pattern G)"
    - "MUST NOT 장 시간 판정으로 연결을 끊거나 막는다 (D-13)"
    - "MUST NOT healthz 필드 이름·값에 account·user 식별자를 싣는다 (T-19-07 · smoke health_probe)"
    - "MUST NOT 관찰자 로그인 거부 사유를 추측해 재시도한다 (D-13 · T-15-10 동형)"
    - "MUST NOT index.ts 결선을 이 plan 에서 한다 — 실 코덱이 G1 뒤(19-09)에 생기므로 결선은 19-10"
---

<objective>
relay **관찰자 상태기계(JournalObserver) · 기록 상태 요약(JournalStatus) · `/healthz` 기록 필드와 장중 알림 규칙 · `journal.state` 브라우저 표식 · 비밀 설정**을 만든다. 와이어 코덱은 `JournalCodec` 주입 지점 뒤에 있어 G1 전에는 가짜 코덱으로 전 경로를 시험한다(19-09 에서 실 코덱으로 교체 — 이 plan 의 코드는 무수정). tracer 는 「기동 → 커서 → 연결 → 로그인 → 로그인 성공 → 배치 1건 → 기록기 push → live → journal.state live 프레임 + healthz journal.state live」 한 경로다.

Purpose: D-13(24시간 상시 · 거부 시 정지) · D-04(끊김을 카드와 healthz 두 곳에) · D-09 의 relay 몫(관찰자는 로그인 외 아무것도 보내지 않음) · D-10(비밀 하나 · Secret Manager). relay 로그는 Cloud Logging 에 없으므로 운영 알림은 기존 uptime check 가 보는 healthz 503 에 편승한다(RESEARCH Pattern G).
Output: journal 모듈 3파일 · fanout 상태 프레임 · order-api 확장 · config/logger · 테스트 3파일.

커밋 규율: 한글 메시지 · Co-Authored-By 없음 · 파일 지정 add · `git status -sb` · **push 금지** · relay 배포 금지(19-12).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-account-order-journal/19-CONTEXT.md
@.planning/phases/19-account-order-journal/19-05-SUMMARY.md

<interfaces>
19-05 도메인 타입(`relay/src/journal/types.ts`): `JournalCodec { buildLoginReq({secret, sinceSeq, epoch, client}); decode(e: TransportFrameEvent): ObserverFrame }` · `ObserverFrame` = login{result} | batch{batch} | ignore{msgType} | unexpected{msgType} | malformed{msgType} · `ObserverLoginResult { success, message, broker, epoch, headSeq, oldestSeq, resync, accounts }` · `JournalBatchFrame { records, headSeq, caughtUp }` · `ObserverTransport`(DmaClient 부분집합) · `JournalObserverState = "disabled"|"connecting"|"logging_in"|"replaying"|"live"|"rejected"` · `JournalWriterHealth { queueDepth, consecutiveFailures, dbError, lastAppliedSeq, lastAppliedAtMs }` · `JournalPushResult`.
19-05 `JournalWriter`: `readCursor(): Promise<JournalCursor>` · `beginEpoch(epoch, {resync})` · `push(records): JournalPushResult` · 게터 `epoch` · `lastReceivedSeq` · `health()` · 이벤트 `health`. `JournalAccess.replace(rows)`. `WsFanout.deliverJournalRows`.
relay 기존: `relay/src/dma/session.ts:60` `LOGIN_RESP_TIMEOUT_MS = 5000` · 346-372 로그인 응답 처리와 `#failNoRetry` → `stopReconnect`(527-533) · `relay/src/dma/dma-client.ts` `DmaClient`(generation 규율 · stopReconnect · dropTransport · resetReconnectAttempts) · `relay/src/order/order-api.ts` 60-110 `OrderApiDeps`/`HealthPayload` · 253-279 핸들러 · 머리 주석의 날짜 붙은 ★ 보강 문단 규율 · `relay/tests/order-api.test.ts` 30-85 하네스 · `relay/src/config.ts` 75-86 `get`/`optional` · `relay/src/logger.ts` redact 목록 · `packages/shared/src/krxCalendar.ts` `isKrxHoliday(dateIso)` · `kstDateIso(now)`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] 기동 → 로그인 → 배치 1건 → 기록기 → live → journal.state 프레임 · healthz journal 필드</name>
  <files>relay/src/journal/observer.ts, relay/src/journal/status.ts, relay/src/ws/fanout.ts, relay/src/order/order-api.ts, relay/tests/journal-observer.test.ts</files>
  <read_first>
    - relay/src/journal/types.ts · relay/src/journal/writer.ts · relay/src/journal/access.ts (19-05 결과)
    - relay/src/dma/session.ts (280-380 로그인 송신·타이머·응답 처리 · 520-540 #failNoRetry)
    - relay/src/dma/dma-client.ts (100-260)
    - relay/src/ws/fanout.ts (237-280 deps · 555-700 #onFirstMessage · 19-05 의 deliverJournalRows)
    - relay/src/order/order-api.ts (60-110 · 195-285)
    - .planning/phases/19-account-order-journal/19-RESEARCH.md (330-345 Pattern C · 572-619 관찰자 뼈대 예시 · 416-422 Pattern G)
  </read_first>
  <behavior>
    - observer.start() → writer.readCursor 1회 → transport.connect 1회 → "up"(gen 1) → codec.buildLoginReq({secret, sinceSeq: 0, epoch: "", client: "gh-radar-relay"}) 결과가 transport.send 로 1회 → state logging_in
    - 로그인 응답 {success, epoch "ep-1", headSeq 1, resync true, accounts 2행} → access.replace(2행) · writer.beginEpoch("ep-1",{resync:true}) · transport.resetReconnectAttempts · state replaying
    - batch {records [seq 1], headSeq 1, caughtUp true} → writer.push([seq 1]) · state live
    - JournalStatus: live 전이 → "frame" 이벤트 {t:"journal.state", s:"live"} · fanout.deliverJournalState 가 인증 사용자 전원에게 1프레임 · 새로 인증한 연결은 스냅샷으로 live 1프레임
    - /healthz 본문에 journal.state "live" · journal.lastSeq(기록기 lastAppliedSeq) · 식별자 키 없음 · 200
  </behavior>
  <action>
**관찰자** `relay/src/journal/observer.ts` — `JournalObserver extends EventEmitter`. deps `{ secret: string | undefined; gateway: string; codec: JournalCodec; writer; access; transport?: ObserverTransport; host: string; port: number; client?: string(OBSERVER_CLIENT_NAME = "gh-radar-relay"); loginTimeoutMs?: number(session.ts 의 LOGIN_RESP_TIMEOUT_MS import — 값 복제 금지) }`. `transport` 가 없으면 `new DmaClient({ host, port })` 를 **하나** 만든다(관찰자 전용 소켓 · 사용자 세션과 독립 — D-13). 이 task 의 경로: `start()` → secret 이 있으면 `writer.readCursor()` 를 기다린 뒤(실패 처리는 Task 2) `transport.connect()`, 상태 connecting → `"up"`(세대 캡처) → `codec.buildLoginReq({ secret, sinceSeq: writer.lastReceivedSeq ?? cursor.lastSeq, epoch: writer.epoch, client })` 를 `transport.send` · 상태 logging_in · 로그인 타이머 → `"frame"` 은 **동기**로 `e.generation === transport.generation` 대조 후 `codec.decode(e)` 분기: login 성공 → 타이머 해제 · `access.replace(result.accounts)` · `writer.beginEpoch(result.epoch, { resync: result.resync })` · `transport.resetReconnectAttempts()` · `headSeq = result.headSeq` · 상태 = headSeq 가 기록기 lastReceivedSeq(없으면 0)보다 크면 replaying 아니면 live; batch → `writer.push(batch.records)` 가 "ok" 면 `headSeq = batch.headSeq`, `caughtUp` 이면 live. 게터 `state` · `headSeq` · 상태 바뀔 때마다 `emit("state", state)`. 수신 콜백에서 await 금지(D-32 — 머리 주석). 로그 접두 `[JOURNAL]`, 로그 인자에 secret·계좌 원문 금지.
**상태 요약** `relay/src/journal/status.ts` — `JournalStatus extends EventEmitter`. deps `{ observer: { state; headSeq; on("state") }; writer: { health(); on("health") }; delayedAfterMs?: number(JOURNAL_DELAYED_AFTER_MS = 10_000); now?: () => number }`. 파생 상태 = `writer.health().dbError ? "db_error" : observer.state`. 이 task 에서는 live 전이에 `frame = { t: "journal.state", s: "live" }` 를 만들어 `emit("frame", frame)`, `frame(): RelayJournalStateMsg | null`(관찰자 disabled 면 null · 아직 판정 전이면 null) · `health(nowMs): JournalHealth` = `{ state: 파생 상태; lastSeq: writer.health().lastAppliedSeq; headSeq: observer.headSeq; lagSeq: 둘 다 있으면 max(headSeq − lastSeq, 0) 아니면 null; disconnectedSec: live 면 null 아니면 live 이탈 시각부터 초; lastAppliedAgeSec }` 을 완성한다(디바운스 delayed 와 알림 규칙은 Task 3). `JournalHealth`·`JournalDerivedState` 타입을 types.ts 에 더한다. `close()` 는 타이머 정리.
**fanout**: `WsFanoutDeps.journalState?: { frame(): RelayJournalStateMsg | null }` · public `deliverJournalState(frame)` — `#users` 의 모든 연결에 보낸다(사용자 데이터가 아니라 relay 운영 상태 — `#broadcastNxtSnap` 선례와 같은 근거를 주석으로). `#onFirstMessage` 끝(nxt.snap 뒤)에 `journalState?.frame()` 이 null 이 아니면 그 연결에 1프레임(모르면 보내지 않음 — 브라우저는 부재를 「표식 없음」 으로 읽는다).
**healthz**: `OrderApiDeps.journal?: { health(nowMs: number): JournalHealth }` · `now?: () => Date`(테스트 주입) · `HealthPayload.journal?: JournalHealth` — 소스가 있으면 항상 싣는다. 이 task 에서는 판정식을 바꾸지 않는다(503 규칙은 Task 3). 키 이름에 account/user 가 없음을 주석으로 못박는다(T-15-22 · smoke `health_probe` grep).
**테스트** `relay/tests/journal-observer.test.ts` — 가짜 transport(EventEmitter + 호출 기록 · `generation` 게터), 가짜 codec(buildLoginReq 가 입력을 기록하고 표식 바이트를 돌려줌 · decode 가 테스트가 넣은 프레임 객체를 돌려줌), 가짜 writer/access(호출 기록 · 기록기 `health()`·`lastReceivedSeq` 스텁), 실제 `JournalStatus`, 실제 `WsFanout` 대신 `deliverJournalState` 스파이로 `<behavior>` 1~4 를, `relay/tests/order-api.test.ts` 하네스에 `journal` 스텁을 넣어 5 를 단언한다(order-api 케이스는 Task 3 과 같은 파일이라 이 task 에서는 journal-observer.test.ts 안에서 `createOrderApi` 를 직접 띄워도 된다).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay exec vitest run tests/journal-observer.test.ts tests/fanout.test.ts tests/order-api.test.ts</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 "Test Files" 요약줄에 failed</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "LOGIN_RESP_TIMEOUT_MS" relay/src/journal/observer.ts` >= 1 (정본 import)
    - `grep -n "deliverJournalState" relay/src/ws/fanout.ts` >= 1 · `grep -n "journalState" relay/src/ws/fanout.ts` 가 `#onFirstMessage` 안의 줄을 보여 준다
    - `awk '/on\("frame"/,/\}\);/' relay/src/journal/observer.ts | grep -c "await"` 출력 0(== 0) — 수신 콜백 동기
    - `grep -nE '"(accountNo|userId|account_no|user_id)"' relay/src/order/order-api.ts` 출력 0줄(== 0)
  </acceptance_criteria>
  <done>관찰자가 게이트웨이에 로그인해 받은 저널 레코드를 기록기로 넘기고, 그 상태가 브라우저 표식과 healthz 에 같은 원천으로 드러난다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 관찰자 실패 경로 — 로그인 거부 정지 · 응답 타임아웃 · 갭/상한 끊기 · resync · 76 무시 · 커서 재시도 · 비밀 설정</name>
  <files>relay/src/journal/observer.ts, relay/src/config.ts, relay/src/logger.ts, relay/tests/journal-observer.test.ts</files>
  <read_first>
    - relay/src/journal/observer.ts (Task 1 결과)
    - relay/src/config.ts (전문 86줄)
    - relay/src/logger.ts (전문 57줄)
    - relay/src/dma/session.ts (340-380 · 520-540)
    - .planning/phases/19-account-order-journal/19-RESEARCH.md (544-546 Pitfall 8 · 518-524 Pitfall 3)
  </read_first>
  <behavior>
    - 로그인 응답 success false · message "거부" → transport.stopReconnect 1회(사유 문구에 비밀 없음) · state rejected · error 로그 1줄 · 이후 "down" 에도 rejected 유지 · 이후 "up" 이 와도 로그인 송신 0
    - "up" 뒤 5초 무응답(가짜 타이머) → transport.dropTransport("관찰자 로그인 응답 타임아웃") · state connecting · 다음 "up"(gen 2) 에서 로그인 재송신
    - 옛 세대(gen 1) 프레임이 gen 2 뒤 도착 → 무시
    - writer.push → "gap" → dropTransport("저널 seq 갭") · "overflow" → dropTransport("저널 큐 상한") · 재접속 로그인 sinceSeq = writer.lastReceivedSeq · epoch = writer.epoch
    - decode → ignore(76) → 로그·상태 변화 0 · unexpected(msgType 51) → warn 1회(같은 번호 두 번째는 로그 0) · malformed → error + dropTransport
    - readCursor 가 throw → error 로그 · connect 0 · 백오프 뒤 재시도 성공 → connect 1
    - secret undefined → state disabled · connect 0 · warn 1 · status.frame() null
    - 전 시나리오에서 transport.send 로 나간 페이로드는 전부 codec.buildLoginReq 결과였다(D-09 relay 몫)
    - logger 로 나간 전 레코드를 직렬화해도 비밀 문자열이 없다
    - loadConfig: NODE_ENV production + DMA_OBSERVER_SECRET 없음 → throw · test/development + 없음 → dmaObserverSecret undefined
  </behavior>
  <action>
**관찰자 실패 경로**(`observer.ts`): 로그인 거부 → `transport.stopReconnect("관찰자 로그인 거부")` + 상태 rejected + `logger.error({ gatewayMessage: result.message }, "[JOURNAL] 관찰자 로그인 거부 — 재접속 중단 (D-13)")`. 거부 문구는 게이트웨이의 단일 문구 그대로(D-09 — 사유 추측 금지). rejected 는 `stop()`·재시작 전까지 바뀌지 않는다(down/up 무시). 로그인 타이머 만료(세대 대조) → `transport.dropTransport("관찰자 로그인 응답 타임아웃")`. 기록기 `push` 결과 gap/overflow → `dropTransport("저널 seq 갭")`/`dropTransport("저널 큐 상한")`(재접속 로그인은 Task 1 의 sinceSeq·epoch 식이 그대로 이어받는다). decode 결과 ignore → 아무것도 하지 않음(76 브로드캐스트 — Pitfall 8), unexpected → 번호별 warn 1회(Set), malformed → `logger.error({ msgType })` + `dropTransport("관찰자 프레임 파손")`. 커서 읽기 실패 → `logger.error({ pgError: safePgError(err) })` 후 `backoffDelayMs` 로 재시도(unref 타이머) — 성공 전에는 connect 하지 않는다(since 를 모르고 붙으면 전체 재생). secret 미설정 → 상태 disabled + warn 1회 · 아무것도 열지 않는다. `stop()` → 타이머 해제 · 리스너 해제 · `transport.destroy()`. 머리 주석에 D-13(24h 상시 · 장 시간 판정 미사용) · D-09(로그인 외 송신 없음) · 거부 = 정지(15 D-16 · T-15-10 동형)를 적는다.
**비밀 설정**(D-10 · T-19-03): `relay/src/config.ts` 의 `RelayConfig` 에 `dmaObserverSecret: string | undefined` 를 `optional("DMA_OBSERVER_SECRET")` 로 추가하고, `nodeEnv === "production"` 인데 비어 있으면 `throw new Error("DMA_OBSERVER_SECRET must be set in production (Phase 19 D-13)")` — 컨테이너가 재시작 루프로 원인을 로그에 남기고 healthz 가 사라져 uptime 알림이 울린다. 개발·테스트에서는 없으면 관찰자 disabled(e2e 로컬 relay 가 그대로 뜬다). 주석은 기존 「미설정 시 어떤 동작」 규약대로. `relay/src/logger.ts` redact 경로에 `"*.secret"` · `"*.dmaObserverSecret"` · `"*.DMA_OBSERVER_SECRET"` 을 더한다(주석: 실수 방어일 뿐 — 로그 인자로 넘기지 않는 것이 설계).
**테스트**: `journal-observer.test.ts` 에 `<behavior>` 전 항목(config 2항목은 env 를 바꿔 `loadConfig` 를 부르고 원복). 로그 비밀 부재는 logger 메서드 스파이의 인자를 `JSON.stringify` 해 비밀 문자열 포함 여부로 단언한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay exec vitest run tests/journal-observer.test.ts</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 "Test Files" 요약줄에 failed</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "stopReconnect" relay/src/journal/observer.ts` >= 1 · `grep -n "dropTransport" relay/src/journal/observer.ts` >= 3
    - `grep -n "DMA_OBSERVER_SECRET" relay/src/config.ts` >= 1 · `grep -n "dmaObserverSecret" relay/src/logger.ts` >= 1
    - `grep -n "secret" relay/src/journal/observer.ts | grep -c "logger\."` 출력 0(== 0) — 로그 인자에 비밀 없음
  </acceptance_criteria>
  <done>관찰자는 거부되면 멈춰서 드러나고, 끊기거나 밀리면 알아서 이어받고, 무관한 방송·예상 밖 프레임에 흔들리지 않으며, 비밀은 어디에도 찍히지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 장중 창 · 기록 지연 디바운스 · healthz 503 규칙</name>
  <files>relay/src/journal/trading-window.ts, relay/src/journal/status.ts, relay/src/order/order-api.ts, relay/tests/journal-status.test.ts, relay/tests/order-api.test.ts</files>
  <read_first>
    - relay/src/journal/status.ts (Task 1 결과)
    - relay/src/order/order-api.ts (Task 1 결과 · 195-285 판정 문단 형식)
    - relay/tests/order-api.test.ts (전문 — 하네스·가짜 타이머 미사용 규율)
    - packages/shared/src/krxCalendar.ts (전문)
    - packages/shared/src/marketHours.ts (isKoreanMarketOpen 이 08:00~15:30 인 근거 — 쓰지 않는다)
    - ops/alert-relay-down.yaml (1-40 — 5분 창·0.9 임계)
  </read_first>
  <behavior>
    - inTradingWindow: 2026-09-28(월) 07:59 KST false · 08:00 true · 19:59 true · 20:00 false · 2026-09-26(토) 12:00 false · 2026-09-25(금, 추석) 12:00 false · 2026-10-05(휴장) 10:00 false
    - JournalStatus: 기동 후 10초 안 live → live 프레임 1 · 10초 동안 live 못 됨 → delayed(since = 기동 시각 ISO) · live → connecting 3초 → live 복귀 → delayed 프레임 0(디바운스) · live → connecting 12초 → delayed 1 → live 1 · writer dbError → 파생 db_error → 10초 뒤 delayed · disabled → frame null · 프레임 없음
    - healthz(장중): state live → 200 · connecting 179초 → 200 · connecting 180초 → 503 degraded · rejected 0초 → 503 · db_error 181초 → 503 · disabled → 200(관찰자 비활성은 알림 대상 아님)
    - healthz(장 밖 21:00): connecting 1시간 → 200 · 본문 journal.state connecting · disconnectedSec 3600
    - 503 이어도 본문의 vpn · dma 필드 의미는 그대로(기존 케이스 무수정 green)
  </behavior>
  <action>
`relay/src/journal/trading-window.ts` — `inTradingWindow(now: Date): boolean` = KST 요일(월~금) ∧ `!isKrxHoliday(kstDateIso(now))` ∧ 08:00 ≤ KST 시각 < 20:00. KST 날짜는 shared `kstDateIso` 로만 얻는다(새 KST 함수 금지 — PATTERNS 「날짜/휴장일」). 머리 주석: 알림 판정 전용이며 연결 유지에 쓰지 않는다(D-13) · 08:00~20:00 은 NXT 포함 실사용 창(메모리 「장 시간 08:00~20:00」) · `isKoreanMarketOpen` 을 쓰지 않는 이유.
`relay/src/journal/status.ts` 완성 — 파생 상태가 live 가 아니게 된 순간 `notLiveSinceMs` 를 잡고 `delayedAfterMs` 타이머(unref)를 걸어, 만료 때 여전히 live 가 아니면 `{ t: "journal.state", s: "delayed", since: new Date(notLiveSinceMs).toISOString() }` 를 emit, live 로 돌아오면 타이머 해제 후 직전 프레임이 delayed(또는 null)였을 때만 live 를 emit. 기동 시각을 첫 `notLiveSinceMs` 로 둔다. `JOURNAL_ALERT_AFTER_MS = 180_000` 과 순수 함수 `journalAlerting(health: JournalHealth, now: Date): boolean` = `inTradingWindow(now) && (health.state === "rejected" || (health.state !== "live" && health.state !== "disabled" && (health.disconnectedSec ?? 0) * 1000 >= JOURNAL_ALERT_AFTER_MS))` 를 export 한다.
`relay/src/order/order-api.ts` — 핸들러에서 `const now = deps.now?.() ?? new Date()` · `journal = deps.journal?.health(now.getTime())` · `journalOk = journal === undefined || !journalAlerting(journal, now)` · `healthy = linkUp && sessionsOk && journalOk`. 머리 주석 판정 문단 끝에 「★ 2026-09-24 보강 (Phase 19 D-04 (b)) — 기록 연결 판정을 AND 로 더한다. 기존 vpn·세션 판정을 되돌리지 않는다. 장 밖에서는 본문에만 드러낸다(D-13 — 운영 알림은 장중 끊김에만). 알림 경로는 기존 uptime `gh-radar-relay-healthz` + 정책 `gh-radar-relay-down`(끊김 후 알림까지 ≈ 3분 + 5분 창)」 문단을 더한다.
테스트: `relay/tests/journal-status.test.ts` 에 창·디바운스 항목(가짜 타이머 · `now` 주입), `relay/tests/order-api.test.ts` 에 describe「journal 판정 (Phase 19 D-04)」 — 하네스 `start()` 에 `journal`·`now` 옵션을 더해 healthz 항목(이 파일의 「가짜 타이머 금지」 규율은 유지 — 시각은 `now` 주입으로만).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay exec vitest run tests/journal-status.test.ts tests/order-api.test.ts tests/journal-observer.test.ts</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 "Test Files" 요약줄에 failed</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/relay run test</automated>
    <fails_when>non-zero exit, 또는 "error TS", 또는 vitest 요약줄에 failed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "isKrxHoliday" relay/src/journal/trading-window.ts` >= 1 · `grep -rn "isKoreanMarketOpen" relay/src/journal` 출력 0줄(== 0)
    - `grep -n "JOURNAL_ALERT_AFTER_MS = 180_000" relay/src/journal/status.ts` >= 1 · `grep -n "JOURNAL_DELAYED_AFTER_MS = 10_000" relay/src/journal/status.ts` >= 1
    - `grep -n "2026-09-24 보강" relay/src/order/order-api.ts` >= 1
    - relay 전체 테스트 green(기존 order-api 케이스 무수정)
  </acceptance_criteria>
  <done>장중에 기록 연결이 3분 넘게 끊기거나 비밀이 거부되면 healthz 가 503 이 되어 기존 알림이 울리고, 브라우저에는 10초 넘은 끊김만 「기록 지연」 으로 간다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay → 게이트웨이 9100(VPN/WG) | 관찰자 비밀이 지나는 유일한 경로 |
| 인터넷 → `/healthz`(Caddy) | 공개 경로 — 식별자 금지 |
| relay → 브라우저 wss | `journal.state` 는 운영 상태(사용자 데이터 아님) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-03 | Information Disclosure | 관찰자 비밀(env · 로그) | high | mitigate | env 한 곳 · production 필수 · redact 3경로 · 로그 인자 금지 grep · 로그 직렬화 부재 단언 |
| T-19-04 | Elevation of Privilege | 관찰자 연결의 추가 요청 | high | mitigate | 송신은 로그인뿐(테스트 단언) — 서버측 강제는 gh-trade(transfer, 19-01 인계서 · 19-11 G2) |
| T-19-28 | Denial of Service | 거부된 비밀로 재시도 폭주(게이트웨이 부하) | medium | mitigate | 거부 → `stopReconnect` · rejected 고정 |
| T-19-07 | Information Disclosure | healthz journal 필드 | medium | mitigate | 키에 account/user 없음 grep · seq 는 계수라 수용(거래량 신호 수준 — D-04 가 명시한 필드) |
| T-19-29 | Denial of Service | 알림 공백(끊김이 안 보임) | medium | mitigate | 장중 180초 → 503 · rejected 즉시 503 · 장 밖 본문 노출 |
| T-19-12 | Spoofing | 가짜 게이트웨이 | low | accept | 관찰자는 사용자 세션과 같은 VPN/WG 경계 · DMA_HOST 로컬 기본값(D-27) 그대로 |
</threat_model>

<verification>
- relay 전체 테스트 · typecheck 2종 green.
- spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</verification>

<success_criteria>
- 관찰자 상태기계가 가짜 코덱으로 성공·거부·타임아웃·갭·상한·resync·방송 무시를 통과.
- healthz 장중 503 규칙과 journal.state 디바운스가 테스트로 잠김.
</success_criteria>

## Artifacts this phase produces (이 plan 분)

| 종류 | 이름 |
|------|------|
| 모듈 | `relay/src/journal/observer.ts` · `relay/src/journal/status.ts` · `relay/src/journal/trading-window.ts` |
| 클래스·함수 | `JournalObserver`(start · stop · state · headSeq · 이벤트 state) · `JournalStatus`(frame · health · close · 이벤트 frame) · `journalAlerting(health, now)` · `inTradingWindow(now)` · `WsFanout.deliverJournalState` |
| 상수 | `OBSERVER_CLIENT_NAME = "gh-radar-relay"` · `JOURNAL_DELAYED_AFTER_MS = 10_000` · `JOURNAL_ALERT_AFTER_MS = 180_000` |
| 타입 | `JournalHealth` · `JournalDerivedState` · `HealthPayload.journal` · `OrderApiDeps.journal` · `OrderApiDeps.now` · `WsFanoutDeps.journalState` |
| 설정 | env `DMA_OBSERVER_SECRET` → `RelayConfig.dmaObserverSecret`(production 필수) · pino redact `*.secret` · `*.dmaObserverSecret` · `*.DMA_OBSERVER_SECRET` |
| 테스트 | `relay/tests/journal-observer.test.ts` · `relay/tests/journal-status.test.ts` · order-api journal describe |

<output>
Create `.planning/phases/19-account-order-journal/19-07-SUMMARY.md` when done
</output>
