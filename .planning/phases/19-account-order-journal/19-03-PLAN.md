---
phase: 19-account-order-journal
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - supabase/migrations/20260924200100_dma_journal_rpcs.sql
  - supabase/tests/dma_journal_apply.test.sql
  - supabase/tests/dma_journal_schema.test.sql
autonomous: true
requirements: [D-02, D-08, D-12]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-12: 체결 E 는 `filled_qty` 를 체결 수량만큼 누적하고, qty 를 알면 `filled_qty + modified_qty >= qty` 에서 filled · 아니면 partially_filled 로 격자 단조 전이한다. E 가 A 보다 먼저 오면 qty NULL 행을 만들고 뒤의 A 가 qty 를 채워 재판정한다. 같은 E 를 재생해도 체결 수량은 두 배가 되지 않는다"
    - "D-12: 상태는 뒤로 가지 않는다 — 종결(filled·cancelled·rejected·modified) 행은 늦게 온 A·E·M 으로 바뀌지 않고, 체결 수량 누적은 상태와 무관하게 사실로 남는다"
    - "취소확인 C 는 자기 주문번호 행을 cancelled 로, 원주문 행(원주문번호 · 비면 자기 번호 = 거래소 자동취소)을 격자 단조로 cancelled 로 옮긴다 — filled 원주문은 덮지 않는다"
    - "정정확인 M 은 이동 수량 = LEAST(M 수량, 원주문 잔량)으로 정정 행을 만들고 원주문 `modified_qty` 에 더하며, 합이 qty 이상이면 원주문을 modified 로 닫는다 — 원주문을 모르면 M 수량 그대로"
    - "정정·취소 거부 R(주문번호 있음)은 자기 행만 rejected 로 두고 원주문을 건드리지 않는다"
    - "D-02: 로컬 거부(`local_reject` 참) 또는 주문번호 없는 R 은 `reject_seq = seq` 새 행 · status rejected · 서로 다른 로컬 거부가 한 행에 겹치지 않는다"
    - "D-08: C/M/R 행의 방향은 원주문 행의 side 로 채우고(원주문을 모르면 `side_trusted` 일 때만 레코드 값), origin 원문이 비거나 미지면 NULL 이다 — 「수동」 으로 지어내지 않는다"
    - "예약 Q-ID(`^Q[0-9]{9}$`) 접수 A·취소 C 는 일반 주문번호와 같은 규칙으로 행이 된다(오케스트레이터 Q3)"
    - "D-12: journal_epoch 가 바뀌면 커서가 새 epoch 의 seq 로 교체되고, 새 epoch 의 seq 1 은 옛 epoch seq 1 과 다른 이벤트로 적재된다 · `created_at` 은 가장 이른 게이트웨이 시각이라 저녁 재생이 오전 주문 시각을 바꾸지 않는다"
  artifacts:
    - path: supabase/migrations/20260924200100_dma_journal_rpcs.sql
      provides: "dma_journal_project 전 통보 종류 투영"
      contains: "reject_seq"
    - path: supabase/tests/dma_journal_apply.test.sql
      provides: "투영 규칙 표 전 행 pgTAP"
  key_links:
    - from: "dma_journal_project(C)"
      to: "원주문 행"
      via: "COALESCE(NULLIF(org_order_no,''), order_no) 로 같은 (gateway, trade_date, account_no) 행을 찾아 dma_journal_next_status"
      pattern: "dma_journal_next_status"
    - from: "dma_journal_project(M)"
      to: "원주문 modified_qty"
      via: "LEAST(order_qty, qty - filled_qty - modified_qty)"
      pattern: "modified_qty"
  prohibitions:
    - "MUST NOT `message` 문구를 읽어 분기한다 — notice_type · request_kind 동등 비교뿐 (17 D-08 · T-17-33)"
    - "MUST NOT relay TS 에서 투영을 계산한다 — 멱등 근거가 트랜잭션 밖으로 나간다 (RESEARCH Anti-Pattern)"
    - "MUST NOT 원격 DB 에 push 한다 — 19-11"
---

<objective>
19-01 이 A 갈래로 증명한 `dma_journal_project` 에 나머지 통보 종류(E · C · M · R · 로컬 거부)를 채워, 게이트웨이 저널 한 흐름이 주문 1건 = 1행으로 단조 투영되게 한다. tracer 는 「체결 E 가 A 행의 체결 수량·상태를 올리는 한 경로」 이고, 이어서 취소·정정·거부·로컬 거부, 끝으로 epoch·시각·순서·미지 통보의 견고성을 잠근다.

Purpose: relay 가 1,000여 줄로 하던 정정·취소 상관(quick-260923-m23 · e1m)을 DB 투영 한 곳으로 옮긴다(RESEARCH 「투영 규칙」 표 · Don't Hand-Roll 「상태 단조 = 같은 격자 1:1 이식」). 재생 안전성은 이벤트 PK 게이트가 쥐고, 이 함수는 「이 이벤트가 처음 적용될 때 무엇이 바뀌는가」 만 정의한다.
Output: 완성된 투영 함수 · 투영 규칙 표 전 행을 덮는 pgTAP.

커밋 규율: 한글 메시지 · Co-Authored-By 없음 · 파일 지정 add · `git status -sb` · **push 금지**.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-account-order-journal/19-CONTEXT.md
@.planning/phases/19-account-order-journal/19-01-SUMMARY.md

근거 인용:
- RESEARCH §Pattern D 「투영 규칙」 표(385-396) — 이 plan 의 규칙 정본. 그 표의 근거 원문: `relay/src/order/notice-status.ts` 83-95 격자 · `relay/src/ws/order-handler.ts` 851-860(거부 = 원주문 살아 있음 · org 비면 자기 번호 = 거래소 자동취소 예외) · 912-917(빈 주문번호 upsert 가 거부를 겹친다 — GC-WR-02).
- 19-01 산출: `dma_journal_project(p_gateway, p_epoch, p_ev)` 의 A 갈래 · `dma_journal_next_status` · `dma_journal_status_rank` · 포이즌 격리 · 추적 pgTAP. 이벤트 JSON 키 23종.
- RESEARCH Assumption A4 — 원주문이 저널에 없으면(전일·예약) 정정 이동은 M 수량 폴백.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] 체결 E 한 경로 — A 행 체결 누적 · 부분→전량 · E-먼저-A · 재생 무증가</name>
  <files>supabase/migrations/20260924200100_dma_journal_rpcs.sql, supabase/tests/dma_journal_apply.test.sql</files>
  <read_first>
    - supabase/migrations/20260924200100_dma_journal_rpcs.sql (19-01 결과 전문)
    - supabase/tests/dma_journal_apply.test.sql (19-01 결과 전문)
    - .planning/phases/19-account-order-journal/19-RESEARCH.md (378-398 적용 RPC · 투영 규칙 표)
    - relay/src/order/notice-status.ts (전문 — `filledQtyOf` 가 E 를 어떻게 읽는지)
  </read_first>
  <behavior>
    - A(seq 10 · qty 10) → E(seq 11 · exec_qty 4) → 행 filled_qty 4 · partially_filled → E(seq 12 · exec_qty 6) → filled_qty 10 · filled · last_seq 12 · notice_type E
    - 같은 [seq 11, 12] 배치 재호출 → filled_qty 10 그대로 · applied 0
    - E(seq 20 · exec_qty 3) 가 A 보다 먼저 → qty NULL · filled_qty 3 · partially_filled → A(seq 21 · qty 3) → qty 3 · filled(재판정) · created_at = 더 이른 E 의 gw_time
    - filled 행에 늦은 A(seq 13) → status filled 유지 · last_seq 13
  </behavior>
  <action>
`dma_journal_project` 에 **E 갈래**를 채운다(19-01 의 「E 는 예외로 떨어진다」 동작을 대체 — 그 동작을 단언하던 추적 pgTAP 항목이 있으면 이 task 의 E 단언으로 교체한다). 키 `(gateway, trade_date, account_no, order_no)` 로 행이 없으면 INSERT(qty NULL · price NULL · `filled_qty` = exec_qty · order_type 은 A 와 같은 request_kind 사상 · side/origin/requester/board/dma_user_id/exchange 는 A 와 같은 규칙 · status = qty 를 모르므로 partially_filled · first/last_seq · 시각). 있으면 `filled_qty = filled_qty + exec_qty`(상태와 무관한 사실 누적) → `status = dma_journal_next_status(status, <qty 가 NULL 이 아니고 filled_qty + modified_qty >= qty 면 'filled' 아니면 'partially_filled'>)` → 공통 갱신(last_seq GREATEST · notice_type/result_code/message 최신 · updated_at GREATEST · created_at LEAST · 빈 칸 COALESCE). 19-01 A 갈래의 「filled_qty > 0 인 행에 qty 가 채워지면 재판정」 이 E-먼저-A 를 닫는지 이 task 의 behavior 로 확인한다. 체결 가격(`exec_price`)은 행 `price` 를 바꾸지 않는다(주문가가 정본 — 51 의 useExecuted 혼용을 저널이 끊었다). pgTAP 에 `<behavior>` 4항목을 (seq, 기대 filled_qty, 기대 status) 튜플 설명으로 더한다 — 수정 전 함수로 먼저 돌려 E 단언이 실패(apply_error)하는 RED 를 SUMMARY 에 적고 GREEN 으로 바꾼다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash scripts/verify-dma-orders-price-check.sh --test supabase/tests/dma_journal_apply.test.sql</automated>
    <fails_when>non-zero exit, 또는 출력에 "not ok" 줄, 또는 "# Looks like you planned" 줄</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -v '^ *--' supabase/migrations/20260924200100_dma_journal_rpcs.sql | grep -c "filled_qty + "` >= 1 (누적식)
    - SUMMARY 에 E 단언의 RED(수정 전) → GREEN 원문
    - pgTAP `not ok` 0
  </acceptance_criteria>
  <done>체결 통보가 올 때마다 그 주문 행의 체결 수량과 상태가 한 방향으로만 나아가고, 재생은 수량을 부풀리지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 취소 C · 정정 M · 거부 R · 로컬 거부 · Q-ID · 방향/출처 채움</name>
  <files>supabase/migrations/20260924200100_dma_journal_rpcs.sql, supabase/tests/dma_journal_apply.test.sql, supabase/tests/dma_journal_schema.test.sql</files>
  <read_first>
    - supabase/migrations/20260924200100_dma_journal_rpcs.sql (Task 1 결과)
    - .planning/phases/19-account-order-journal/19-RESEARCH.md (385-396 투영 규칙 표 · 535-538 Pitfall 6 · 705-716 Assumption A4)
    - relay/src/ws/order-handler.ts (840-900 원주문 정산 주석 — 거부 = 원주문 살아 있음 · org 비면 자기 번호)
  </read_first>
  <behavior>
    - 원주문 A(0000200001 · 매도 S · qty 10 · origin LimitChaser) → C(order_no 0000200002 · org 0000200001 · request_kind Cancel · side '' · side_trusted false) → 취소 행 order_type C · status cancelled · side S(원주문에서) · origin limit_chaser · 원주문 status cancelled
    - 원주문 filled 뒤 늦은 C → 원주문 filled 유지 · 취소 행은 cancelled
    - C(order_no 0000200009 · org '') + 같은 번호 행 존재 → 그 행 자체가 cancelled(자동취소 예외) · order_type 은 원래 값 유지
    - 원주문 A(qty 10) · E 3 → M(order_no 0000200011 · org 원주문 · order_qty 10) → 정정 행 order_type M · qty 7 · accepted · 원주문 modified_qty 7 · 3+7 >= 10 → 원주문 modified
    - 원주문 모름 → M 정정 행 qty = order_qty(폴백) · 원주문 갱신 없음
    - 정정 거부 R(order_no 0000200021 · request_kind Modify · org 원주문) → 자기 행 order_type M · rejected · 원주문 status/modified_qty 불변
    - 로컬 거부 2건(local_reject true · order_no '' · seq 30, 31 · 같은 계좌·종목) → reject_seq 30 · 31 두 행 · status rejected · order_no NULL · journal_epoch = 배치 epoch · qty = order_qty(0 이면 NULL)
    - 주문번호 없는 브로커 R(local_reject false · order_no '') → reject_seq 행
    - Q-ID A(Q000000001) → accepted 행 · Q-ID C(order_no Q000000001 · org '') → 그 행 cancelled
    - origin '' · 'Unknown' → origin NULL · origin VITrigger → vi
  </behavior>
  <action>
`dma_journal_project` 에 갈래를 채운다. 판정 순서가 곧 우선순위다 — ① `local_reject` 참 또는 (`notice_type` 'R' 이고 order_no 빈 값) → **거부 행**: INSERT `reject_seq = seq` · `journal_epoch = p_epoch` · order_no NULL · status 'rejected' · order_type = request_kind 사상(빈 값 N) · org_order_no NULLIF · qty NULLIF(order_qty, 0) · price = order_price · side(신규 거부는 side_trusted 면 레코드 값, C/M 거부는 원주문 행 side → 없으면 NULL) · origin/requester/board/dma_user_id/exchange 공통 규칙 · 시각 · `ON CONFLICT (gateway, journal_epoch, reject_seq) WHERE reject_seq IS NOT NULL DO NOTHING`(Pitfall 6 — 서로 다른 거부가 한 행에 겹치지 않는다). ② 'C' → 원주문 번호 = `COALESCE(NULLIF(org_order_no, ''), order_no)`. 원주문 번호가 자기 번호와 같으면 그 행 하나만(없으면 INSERT, 있으면 order_type 유지) `dma_journal_next_status(status, 'cancelled')`. 다르면 자기 행(없으면 INSERT order_type C · qty NULLIF(order_qty,0) · org_order_no)을 cancelled 로, 원주문 행이 있으면 `dma_journal_next_status(status, 'cancelled')`(filled 는 종결이라 그대로) + last_seq/updated_at 갱신. ③ 'M' → 원주문 행을 찾아 잔량 = GREATEST(qty − filled_qty − modified_qty, 0)(qty NULL 이면 원주문 모름과 같게 취급), 이동 수량 moved = 원주문 있고 qty 알면 LEAST(order_qty, 잔량) 아니면 order_qty. 자기 행(없으면 INSERT order_type M · qty NULLIF(moved,0) · price order_price · org_order_no) status accepted(격자). 원주문 행이 있으면 `modified_qty += moved` → qty 알고 filled_qty + modified_qty >= qty 면 `dma_journal_next_status(status, 'modified')`. ④ 'R'(주문번호 있음) → 자기 행(없으면 INSERT, order_type = request_kind 사상) `dma_journal_next_status(status, 'rejected')` — request_kind 가 Cancel/Modify 여도 원주문은 건드리지 않는다(거부 = 원주문 살아 있음). ⑤ 'A' · 'E' 는 19-01/Task 1 그대로. ⑥ 그 밖의 notice_type → `RAISE EXCEPTION 'dma_journal_project: 알 수 없는 notice_type %'`(포이즌 격리가 apply_error 에 남긴다 — 조용히 무시하지 않는다).
공통: C/M/R 자기 행의 side 가 NULL 이고 원주문 행이 있으면 원주문 side 로 채운다(D-08 — 방향색은 신규 행에만 쓰이지만 기록은 정확해야 한다). origin 사상은 19-01 규칙 하나(`Manual`/`LimitChaser`/`VITrigger` → 3종, 그 밖·빈 값 → NULL — D-08 보충) — 사상 코드를 갈래마다 복제하지 말고 헬퍼 `public.dma_journal_origin(text) RETURNS text` IMMUTABLE 로 뽑아 권한 3줄(PUBLIC · anon, authenticated REVOKE · service_role GRANT)을 붙인다. Q-ID 는 특별 취급하지 않는다(형식 CHECK 없음 — 같은 규칙으로 성립하는지만 단언). `message` 는 저장만 하고 분기에 쓰지 않는다. pgTAP 에 `<behavior>` 10항목을 튜플 설명으로 더하고, `supabase/tests/dma_journal_schema.test.sql` 의 `has_function_privilege` 목록에 새 헬퍼 `public.dma_journal_origin(text)` 의 anon · authenticated false · service_role true 3줄을 더한다(권한 회귀가 새 함수에서 새지 않게 — T-19-01).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash scripts/verify-dma-orders-price-check.sh --test supabase/tests/dma_journal_apply.test.sql && bash scripts/verify-dma-orders-price-check.sh --test supabase/tests/dma_journal_schema.test.sql</automated>
    <fails_when>non-zero exit, 또는 출력에 "not ok" 줄, 또는 "# Looks like you planned" 줄</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -v '^ *--' supabase/migrations/20260924200100_dma_journal_rpcs.sql | grep -c "reject_seq"` >= 1 · 같은 필터로 `grep -c "modified_qty"` >= 1 · `grep -c "dma_journal_origin"` >= 2
    - `grep -v '^ *--' supabase/migrations/20260924200100_dma_journal_rpcs.sql | grep -ci "message *="` 이 분기 조건에 쓰이지 않는다 — `grep -n "message" supabase/migrations/20260924200100_dma_journal_rpcs.sql` 줄을 읽어 IF/CASE 조건 안에 없음을 SUMMARY 에 적는다
    - 두 pgTAP 파일 `not ok` 0
  </acceptance_criteria>
  <done>취소·정정·거부·로컬 거부·예약 주문이 주문 1건 = 1행 규칙으로 기록되고, 원주문은 거부에 흔들리지 않으며 정정으로 옮겨간 잔량만큼 닫힌다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 견고성 — epoch 교체 · 시각 정본 · 배치 순서 · 미지 통보 · 포이즌 중간 배치</name>
  <files>supabase/tests/dma_journal_apply.test.sql, supabase/migrations/20260924200100_dma_journal_rpcs.sql</files>
  <read_first>
    - supabase/migrations/20260924200100_dma_journal_rpcs.sql (Task 2 결과 — 커서 upsert · 순회 정렬)
    - supabase/tests/dma_journal_apply.test.sql (Task 2 결과)
    - .planning/phases/19-account-order-journal/19-RESEARCH.md (522-533 Pitfall 3·5)
  </read_first>
  <behavior>
    - apply('KB','ep-1', seq 1..3) → 커서 ('ep-1', 3) → apply('KB','ep-2', seq 1) → 커서 ('ep-2', 1) · events 에 (ep-1,1) 과 (ep-2,1) 두 행
    - 같은 epoch 에서 더 작은 seq 배치(재생) → 커서 last_seq 줄지 않음
    - 배열 순서 [seq 5, seq 4] 로 넣어도 seq 4 가 먼저 투영된다(A seq 4 → E seq 5 결과가 filled_qty 누적으로 성립)
    - 오전 gw_time 이벤트를 저녁 트랜잭션에서 적용 → created_at = 오전 gw_time · applied_at 은 now()
    - notice_type 'X' → apply_error 에 사유 · errors 에 seq · 같은 배치 뒤 이벤트 투영됨
    - 빈 배열 → 커서 변화 없음 · rows []
  </behavior>
  <action>
이 task 는 테스트로 잠그는 것이 목적이다 — `<behavior>` 6항목을 pgTAP 에 더하고 실행한다. 실패하는 항목이 있으면 그 원인이 되는 `dma_journal_apply`/`dma_journal_project` 부분을 고치고(예: 순회가 seq 오름차순이 아니면 `ORDER BY (value->>'seq')::bigint`, 커서가 epoch 교체 때 GREATEST 를 쓰면 RESEARCH 골격의 CASE 로), 고친 내용을 SUMMARY 에 적는다(고칠 것이 없으면 마이그레이션은 무수정). 시각 단언은 `to_timestamp` 값을 직접 비교하고 `now()` 에 기대지 않는다(Pitfall 5 — `applied_at` 만 now). 파일 끝에 투영 규칙 표(RESEARCH 385-396) 각 행 → 단언 번호 대응 주석을 둬, 규칙 하나를 바꾸면 어느 단언이 깨지는지 읽히게 한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash scripts/verify-dma-orders-price-check.sh --test supabase/tests/dma_journal_apply.test.sql</automated>
    <fails_when>non-zero exit, 또는 출력에 "not ok" 줄, 또는 "# Looks like you planned" 줄</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "ep-2" supabase/tests/dma_journal_apply.test.sql` >= 1 · `grep -c "apply_error" supabase/tests/dma_journal_apply.test.sql` >= 2
    - 파일 끝 대응 주석에 A · E · C · M · R(번호 있음) · 로컬 거부 · 공통 규칙 7개 행이 있다
    - pgTAP `not ok` 0
  </acceptance_criteria>
  <done>저널 저장소가 초기화돼도(epoch) 기록이 이어지고, 늦은 재생·뒤섞인 배치·모르는 통보가 기록을 조용히 망가뜨리지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트웨이 레코드 → 투영 함수 | 레코드 값이 주문 상태를 결정 — 문구·순서·중복에 흔들리지 않아야 한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-06 | Tampering | 체결 수량 이중 누적 | high | mitigate | 누적은 투영 안에서만, 투영은 이벤트 PK 게이트 뒤에서만 — Task 1 재생 단언 |
| T-19-22 | Tampering | 늦은 통보가 종결 상태를 되돌림 | medium | mitigate | `dma_journal_next_status` 격자 — Task 1·2 종결 유지 단언 |
| T-19-13 | Tampering | 문구 변경으로 분기 오동작 | low | mitigate | `message` 는 저장만 · 분기는 notice_type/request_kind 동등 비교(Task 2 수용 기준) |
| T-19-15 | Denial of Service | 미지 통보·CHECK 위반이 배치를 막음 | medium | mitigate | RAISE → 포이즌 격리 · Task 3 단언 |
| T-19-23 | Repudiation | epoch 초기화 뒤 조용한 누락 | medium | mitigate | 커서 epoch 교체 규칙 · Task 3 단언(relay 쪽 resync 로그는 19-07) |
</threat_model>

<verification>
- `dma_journal_apply.test.sql` · `dma_journal_schema.test.sql` 모두 `not ok` 0.
- spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</verification>

<success_criteria>
- RESEARCH 투영 규칙 표의 모든 행이 단언으로 잠겼다(대응 주석).
- 재생·역순·epoch 교체·미지 통보에서 상태와 수량이 결정적이다.
</success_criteria>

## Artifacts this phase produces (이 plan 분)

| 종류 | 이름 |
|------|------|
| 함수 | `dma_journal_project` 전 갈래(A · E · C · M · R · 로컬 거부) · `dma_journal_origin(text)` |
| 규칙 | 이동 수량 = LEAST(M 수량, 원주문 잔량) · 원주문 번호 = COALESCE(org, 자기 번호) · 거부 행 키 `reject_seq` |

<output>
Create `.planning/phases/19-account-order-journal/19-03-SUMMARY.md` when done
</output>
