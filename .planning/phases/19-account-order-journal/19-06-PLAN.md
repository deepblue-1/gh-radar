---
phase: 19-account-order-journal
plan: 06
type: execute
wave: 3
depends_on: ["19-04"]
files_modified:
  - webapp/src/lib/use-relay-socket.ts
  - webapp/src/lib/relay-provider.tsx
  - webapp/src/lib/orders-api.ts
  - webapp/src/lib/order-notices.ts
  - webapp/src/components/trading/today-orders-card.tsx
  - webapp/src/components/trading/__tests__/today-orders-card.test.tsx
  - webapp/src/lib/__tests__/orders-api.test.ts
  - webapp/src/lib/__tests__/order-notices.test.ts
  - webapp/src/lib/__tests__/relay-socket.test.ts
  - packages/shared/src/relay.ts
  - packages/shared/src/index.ts
  - webapp/e2e/specs/me.spec.ts
autonomous: true
requirements: [D-03, D-04, D-05, D-08]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-03: 「오늘 주문」 카드의 원천은 두 가지뿐이다 — REST `GET /api/orders` 복원과 wss `journal.rows` 푸시. 같은 `id` 면 `lastSeq` 가 큰 쪽이 이기고, 푸시 행은 복원에 없던 행도 **만든다**"
    - "D-03: 세션 51 기반 `{t:\"order\"}` 는 카드 병합에 쓰이지 않는다 — 토스트·전략 로그(trading-alerts) 표면은 그대로 받는다"
    - "D-03: 라이브↔복원 join(진행 단계 격자 · unmatched 재조회 루프 · requestedRef)이 사라지고 표시 상태는 DB 투영의 `status` 하나에서 나온다"
    - "D-04 (a) 데이터: 리듀서가 `journal.state` 최신 프레임을 보관하고, 상태가 delayed → live 로 바뀌면 카드가 한 번 다시 조회한다(빠진 줄 채움) · relay ready 재진입 재조회는 유지"
    - "D-05: webapp 은 옛 `DmaOrderRow` 계약을 참조하지 않는다 — shared 에서 `DmaOrderRow`·`DmaOrderOrigin` 이 삭제됐다"
    - "T-19-16: 로그아웃(reset) 때 `journalRows`·`journalState` 가 비워져 다음 사용자가 이전 사용자의 주문 행을 보지 않는다"
  artifacts:
    - path: webapp/src/lib/use-relay-socket.ts
      provides: "journalRows(id 기준 upsert · lastSeq 큰 쪽) · journalState 보관"
      contains: "journal.rows"
    - path: webapp/src/lib/orders-api.ts
      provides: "fetchTodayOrders(JournalOrderRow[]) · mergeJournalRows · orderDisplayStatus"
      exports: ["fetchTodayOrders", "mergeJournalRows", "orderDisplayStatus"]
    - path: webapp/src/components/trading/today-orders-card.tsx
      provides: "REST + journal.rows 병합 카드"
  key_links:
    - from: "use-relay-socket applyFrame"
      to: "RelayConnectionState.journalRows"
      via: "case \"journal.rows\" upsert"
      pattern: "journal\\.rows"
    - from: "TodayOrdersCard"
      to: "mergeJournalRows(restored, relay.journalRows, today)"
      via: "useMemo 병합"
      pattern: "mergeJournalRows"
  prohibitions:
    - "MUST NOT 브라우저에서 KST 날짜를 새로 계산하는 함수를 만든다 — shared `kstDateIso` 하나"
    - "MUST NOT `{t:\"order\"}` 수신·토스트 경로(trading-alerts)를 바꾼다"
    - "MUST NOT push 한다 — 이 변경은 server 응답 모양에 결합돼 19-12 에서 server 배포 직후에만 나간다"
---

<objective>
webapp 의 「오늘 주문」 데이터 원천을 새 저널로 바꾼다(D-03). tracer 는 「`journal.rows` 프레임 1개 → 리듀서 → RelayContext → 카드 병합 → 화면에 행」 이고, 이어서 옛 라이브 join·재조회 루프를 걷어내고 `journal.state` 전이 재조회를 더하며, 끝으로 shared 의 옛 계약을 지우고 e2e 픽스처 모양을 맞춘다. 계좌별 묶음·칩·표식(B′ 표시)은 19-08 이다.

Purpose: 카드가 「REST + 이 푸시」 한 원천으로 수렴해야 부재 중 자동주문(D-12 이어받기로 채워진 행)과 같은 계좌 다른 단말 주문(D-06)이 빠짐없이 보인다. 푸시 행은 완전한 행이라 옛 규율(「라이브는 행을 못 만든다」)과 그 보완책(unmatched 재조회 루프)이 필요 없어진다.
Output: 리듀서·컨텍스트·병합 함수·카드 데이터 경로·테스트 4파일·shared 정리·e2e 픽스처.

커밋 규율: 한글 메시지 · Co-Authored-By 없음 · 파일 지정 add · `git status -sb` · **push 금지**(server 응답 모양 결합 — Pitfall 12).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-account-order-journal/19-CONTEXT.md
@.planning/phases/19-account-order-journal/19-04-SUMMARY.md

<interfaces>
19-04 shared: `JournalOrderRow`(id · tradeDate · accountNo · isin · stockCode · exchange · board · side · orderType · orgOrderNo · qty · price · orderNo · filledQty · modifiedQty · status(6종) · resultCode · noticeType · message · origin(3종|null) · requester · requestKind · lastSeq · createdAt · updatedAt) · `RelayJournalRowsMsg { t:"journal.rows"; rows }` · `RelayJournalStateMsg { t:"journal.state"; s:"live"|"delayed"; since? }` · `kstDateIso(now?)`.

webapp 현재: `use-relay-socket.ts` `RelayConnectionState`(287-345 · `orders: RelayOrderMsg[]` 등) · `RelayData`/`INITIAL_DATA`(516-560) · `applyFrame` switch(628-) · `case "order"` 664 · `reset` → `INITIAL_DATA` 609 · 반환 useMemo(1650-1680). `relay-provider.tsx` `EMPTY_RELAY_VALUE`(323-) · `useRelayContext`(459). `orders-api.ts` 전문(167줄 — `TodayOrderRow` · `mergeTodayOrders` · `NOTICE_LABELS` · `STATUS_LABELS` · `STATUS_RANK` · `orderDisplayStatus`). `order-notices.ts` 23 `import type { TodayOrderRow }` · 142-254 `MergedOrderNotice` · `mergeKeyOf`(`row.live` 의존) · `mergeOrderNotices`. `today-orders-card.tsx` 헤더 ①~⑦ · 132-247 상태·재조회·병합. 테스트 `today-orders-card.test.tsx` 22-35 `useRelayContext`/`fetchTodayOrders` 목 방식. `trading-alerts.ts` 는 `orderNoticeLabel` 만 import 한다(카드 병합과 무관).
</interfaces>
</context>

<!-- planner-discipline-allow: TodayOrderRow -->
<!-- planner-discipline-allow: mergeTodayOrders -->
<!-- planner-discipline-allow: unmatchedOrderNos -->
<!-- planner-discipline-allow: requestedRef -->
<!-- planner-discipline-allow: row.live -->
<!-- planner-discipline-allow: DmaOrderRow -->
<!-- planner-discipline-allow: DmaOrderOrigin -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] journal.rows 프레임 1개 → 리듀서 → 컨텍스트 → 카드 병합 → 행 표시</name>
  <files>webapp/src/lib/use-relay-socket.ts, webapp/src/lib/relay-provider.tsx, webapp/src/lib/orders-api.ts, webapp/src/lib/order-notices.ts, webapp/src/components/trading/today-orders-card.tsx, webapp/src/components/trading/__tests__/today-orders-card.test.tsx</files>
  <read_first>
    - webapp/src/lib/use-relay-socket.ts (280-345 · 480-560 · 600-700 · 1640-1680)
    - webapp/src/lib/relay-provider.tsx (300-345 · 455-465)
    - webapp/src/lib/orders-api.ts (전문)
    - webapp/src/lib/order-notices.ts (140-254)
    - webapp/src/components/trading/today-orders-card.tsx (전문)
    - webapp/src/components/trading/__tests__/today-orders-card.test.tsx (1-200)
    - packages/shared/src/journal.ts (19-04 결과)
  </read_first>
  <behavior>
    - REST [행 X(id x · lastSeq 3 · accepted)] + 컨텍스트 journalRows [행 X(lastSeq 5 · filled)] → 카드 1줄 · 상태 「체결」
    - REST [] + journalRows [행 Y(오늘 tradeDate)] → 카드 1줄(푸시가 행을 만든다)
    - journalRows 의 어제 tradeDate 행 → 카드에 없음
    - 컨텍스트 `orders`(`{t:"order"}`)에만 있는 주문번호 → 카드 줄 수 변화 없음 · 재조회 없음
  </behavior>
  <action>
(파일 6개는 한 컴파일 단위다 — 컨텍스트 타입에 필드를 더하면 리듀서·Provider 폴백·병합 함수·묶기 입력 타입·카드가 함께 바뀌어야 컴파일되고, 나누면 중간 커밋이 빌드되지 않는다.) **리듀서**(`use-relay-socket.ts`): `RelayConnectionState`·`RelayData` 에 `journalRows: JournalOrderRow[]` 와 `journalState: RelayJournalStateMsg | null` 를 더한다(`INITIAL_DATA` 는 `[]`·`null` — reset 이 비운다 · T-19-16). `applyFrame` 에 `case "journal.rows"`: 들어온 각 행을 id 로 upsert — 기존 행이 있고 기존 `lastSeq >= 새 lastSeq` 면 기존 유지, 아니면 교체, 없으면 추가; 결과를 `createdAt` 내림차순으로 두고 상한 `MAX_JOURNAL_ROWS = 2000` 을 넘으면 오래된 쪽을 자른다(주석: 주문 1건 = 1행이라 하루 수백 행 · 상한은 메모리 방어). 변화가 없으면 같은 state 참조를 돌린다(소비자 memo 보호). `case "journal.state"`: `journalState = frame`. 반환 useMemo 에 두 필드를 싣는다. `{t:"order"}` 처리(`orders`)는 그대로 둔다(D-03 — 토스트·전략 로그 전용).
**컨텍스트**(`relay-provider.tsx`): `EMPTY_RELAY_VALUE` 에 `journalRows: EMPTY_JOURNAL_ROWS`(고정 빈 배열 참조) · `journalState: null` 을 더한다.
**병합**(`orders-api.ts`): `fetchTodayOrders(): Promise<JournalOrderRow[]>`(쿼리 없음 규율 ② 유지). `mergeJournalRows(restored, pushed, today: string): JournalOrderRow[]` — `Map<id, row>` 에 복원 행을 넣고, 푸시 행은 `tradeDate === today` 인 것만 같은 규칙(lastSeq 큰 쪽)으로 덮거나 추가, `createdAt` 내림차순(동률은 lastSeq 내림차순). `orderDisplayStatus(row: JournalOrderRow)` 는 `STATUS_LABELS[row.status]` 조회(6종: accepted 접수 · partially_filled 부분체결 · filled 체결 · cancelled 취소(muted) · rejected 거부(danger) · modified 정정(muted)) + 모르는 값은 muted 원문. 이 task 에서는 `mergeTodayOrders`·`TodayOrderRow` 를 새 함수로 **대체**하고 카드가 새 함수만 쓰게 한다(옛 함수 삭제와 테스트 정리는 Task 2).
**묶기 입력 타입**(`order-notices.ts`): `TodayOrderRow` import 를 shared `JournalOrderRow` 로 바꾸고 `MergedOrderNotice.head` 타입을 맞춘다 — 이 task 에서 `mergeKeyOf` 는 `row.live` 대신 `row.noticeType`·`row.requester` 를 읽게 바꿔 컴파일을 유지하고, 자동주문 판정 규칙(origin null 처리 포함)의 완성은 같은 plan 의 Task 2 가 한다.
**카드**(`today-orders-card.tsx`): `useRelayContext()` 에서 `journalRows` 를 받아 `useMemo(() => mergeJournalRows(restored ?? [], journalRows, kstDateIso()), …)` 로 행을 만든다(`today` 는 shared `kstDateIso` — 브라우저에 두 번째 KST 함수를 만들지 않는다). `noticeFactsOf(row)` 를 `{ noticeType: row.noticeType ?? "", requestKind: row.requestKind ?? (orderType C → "Cancel" · M → "Modify" · 그 밖 ""), side: row.side, requester: row.requester ?? "", board: row.board ?? "" }` 로 바꾼다. `stockLabel` 은 `row.stockCode` 를 그대로 쓴다. 헤더 주석 ③(「라이브 프레임으로 행을 만들지 않는다」)을 「D-03 — 원천은 REST 복원 + `journal.rows` 푸시 둘뿐, 푸시 행은 완전한 행이라 행을 만든다」 로 바꾼다.
**테스트**: `today-orders-card.test.tsx` 의 행 픽스처를 `JournalOrderRow` 로 바꾸고 목 컨텍스트에 `journalRows` 를 더해 `<behavior>` 4항목을 추가한다(기존 케이스 중 라이브 join·unmatched 재조회에 의존하는 것은 Task 2 에서 정리 — 이 task 에서는 컴파일되게 픽스처 타입만 바꾸고, 깨지는 케이스는 skip 하지 말고 Task 2 에서 삭제·대체한다는 표를 SUMMARY 에 둔다).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/webapp exec vitest run src/components/trading/__tests__/today-orders-card.test.tsx -t "journal"</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 "Tests" 요약줄에 passed 가 없거나 0</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'case "journal.rows"' webapp/src/lib/use-relay-socket.ts` >= 1 · `grep -n 'case "journal.state"' webapp/src/lib/use-relay-socket.ts` >= 1
    - `grep -n "mergeJournalRows" webapp/src/components/trading/today-orders-card.tsx` >= 1
    - `grep -n "kstDateIso" webapp/src/components/trading/today-orders-card.tsx` >= 1
    - 새 테스트 이름에 「journal」 이 들어가 `-t "journal"` 필터가 4케이스 이상을 실행한다
  </acceptance_criteria>
  <done>relay 가 민 저널 행이 카드에 곧바로 나타나고, 같은 주문의 더 최신 행이 오면 그 줄의 상태가 바뀐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 옛 라이브 join · unmatched 재조회 제거 · journal.state 전이 재조회 · 묶기 키를 행 사실로</name>
  <files>webapp/src/components/trading/today-orders-card.tsx, webapp/src/lib/orders-api.ts, webapp/src/lib/order-notices.ts, webapp/src/lib/__tests__/orders-api.test.ts, webapp/src/lib/__tests__/order-notices.test.ts, webapp/src/lib/__tests__/relay-socket.test.ts, webapp/src/components/trading/__tests__/today-orders-card.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/today-orders-card.tsx (Task 1 결과)
    - webapp/src/lib/orders-api.ts · webapp/src/lib/order-notices.ts (Task 1 결과)
    - webapp/src/lib/__tests__/orders-api.test.ts · webapp/src/lib/__tests__/order-notices.test.ts (전문)
    - webapp/src/lib/__tests__/relay-socket.test.ts (1-120 하네스 · 1819-1878 nxt.snap describe 형식)
    - webapp/src/components/trading/__tests__/today-orders-card.test.tsx (136-460 기존 describe 3종)
  </read_first>
  <behavior>
    - mergeJournalRows: 같은 id lastSeq 3 vs 5 → 5 · 5 vs 3(푸시가 더 옛것) → 복원 유지 · 어제 푸시 제외 · 정렬 createdAt 내림차순
    - orderDisplayStatus: 6종 라벨·톤 표 그대로 · 모르는 값 muted 원문
    - 리듀서: journal.rows 2프레임(같은 id lastSeq 7 → 6) → 7 유지 · 새 id 추가 · 변화 없는 프레임은 같은 참조 · journal.state 보관 · reset 뒤 둘 다 비움
    - 카드: journal.state delayed → live 전이 1회 → 재조회 1회 · live → live 재조회 0 · 마운트 직후 첫 live 재조회 0 · ready 재진입 재조회 유지(⑧-1~3)
    - 묶기: origin limit_chaser · requester '' · noticeType E · 3초 창 조각 매도 체결 3행 → 1줄(qty 합 · 가격 범위 · #번호~번호) · origin manual 또는 requester Manual → 묶지 않음 · origin null → 묶지 않음(자동주문 증거 없음) · qty null 행은 합계에 0 · 단건 qty null → 표시 「—」
  </behavior>
  <action>
**카드**: `requestedRef` · `unmatchedOrderNos` 재조회 효과를 지운다. relay ready 재진입 재조회(⑦)는 유지한다. `journalState` 를 받아 직전 값을 ref 로 들고 `delayed → live` 전이에서만 `load()` 를 1회 부른다(마운트 뒤 첫 live 는 마운트 조회와 같은 시점이라 건너뛴다 — ⑦ 과 같은 판정 방식). 헤더 주석 ③·⑦ 을 새 규칙으로 고치고 ⑧ 「journal.state 복구 재조회(D-04) — 끊긴 사이 이어받기로 채워진 행을 가져온다」 를 더한다.
**orders-api**: 옛 `TodayOrderRow` · `MergeTodayOrdersResult` · `mergeTodayOrders` · `NOTICE_LABELS` · `NOTICE_RANK` · `STATUS_RANK` 를 지운다(표시 상태는 DB 투영이 이미 단조 — D-03). 지운 이름은 주석에도 남기지 않는다(아래 grep 게이트 — 경위는 「Phase 19 D-03 로 라이브 join 제거」 로만 적는다). 머리 주석 ①~④ 를 저널 원천 기준으로 고친다(② 쿼리 없음 · ④ bare array 유지).
**order-notices**: `mergeKeyOf(row: JournalOrderRow)` — 자동주문 판정 = `row.origin` 이 `limit_chaser` 또는 `vi` 이고 `row.requester !== "Manual"`(origin null 은 증거 없음 → 묶지 않음 · D-08 보충과 같은 원칙). 축 = `${origin}|${isin}|${exchange}|${side}`. `noticeType === "E"` → `FG|축`, `noticeType === "A"` 이고 side S → `AG|축`, 그 밖 자기 키(`NO|orderNo ?? id`). 머리 주석의 「라이브 통보가 없는 복원 행은 묶기 대상이 아니다」 를 「저널 행은 모두 통보 사실(noticeType)을 가진다 — 복원·푸시가 같은 규칙으로 묶인다」 로 바꾼다. `MergedOrderNotice.qty` 를 `number | null`(구성원 전부 null 이면 null), `priceMin`/`priceMax` 를 `number | null`(null·0 은 범위에서 제외, 전부 없으면 null)로 바꾸고 카드의 수량·가격 표시가 null 이면 「—」 를 쓴다(D-08 로컬 거부 행). `orderNoticeLabel`·`orderActionWord`·`orderActionSide` 는 바꾸지 않는다(trading-alerts 가 쓴다).
**테스트**: `orders-api.test.ts` 를 `mergeJournalRows`/`orderDisplayStatus` 기준으로 재작성 · `order-notices.test.ts` 픽스처를 `JournalOrderRow` 로 바꾸고 `<behavior>` 묶기 항목 반영 · `relay-socket.test.ts` 에 describe「journal.rows / journal.state (Phase 19 D-03·D-04)」 추가 · `today-orders-card.test.tsx` 에서 라이브 join·unmatched 재조회 케이스(「같은 orderNo 의 라이브 프레임」 · 「라이브에만 있는 주문번호」 · ⑦-2)를 삭제하고 사유(D-03)를 SUMMARY 표에 적은 뒤 journal.state 전이 케이스를 더한다. 옛 케이스 ⑥-2 의 「수동」 메타 표시 단언은 19-08(D-08 꼬리 제거)이 바꾸므로 이 task 에서는 유지한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest run src/lib/__tests__/orders-api.test.ts src/lib/__tests__/order-notices.test.ts src/lib/__tests__/relay-socket.test.ts src/lib/__tests__/trading-alerts.test.ts src/components/trading/__tests__/today-orders-card.test.tsx</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 "Test Files" 요약줄에 failed</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "mergeTodayOrders|unmatchedOrderNos|requestedRef|TodayOrderRow" webapp/src` 출력 0줄(== 0)
    - `grep -n "row.live" webapp/src/lib/order-notices.ts` 출력 0줄(== 0)
    - trading-alerts 테스트 무수정 green(`git diff --stat -- webapp/src/lib/__tests__/trading-alerts.test.ts` 출력 없음)
  </acceptance_criteria>
  <done>카드는 저널 원천 하나로 상태를 그리고, 기록 연결이 복구되면 스스로 한 번 다시 불러 빠진 줄을 채운다.</done>
</task>

<task type="auto">
  <name>Task 3: shared 옛 계약(DmaOrderRow) 삭제 · e2e 픽스처를 저널 행 모양으로 · webapp 전량 회귀</name>
  <files>packages/shared/src/relay.ts, packages/shared/src/index.ts, webapp/e2e/specs/me.spec.ts</files>
  <read_first>
    - packages/shared/src/relay.ts (1224-1276 DmaOrderOrigin · DmaOrderRow)
    - packages/shared/src/index.ts (전문)
    - webapp/e2e/specs/me.spec.ts (1-265 픽스처 · TODAY_ORDERS · 600-700 테스트 8)
  </read_first>
  <action>
`grep -rn "DmaOrderRow\|DmaOrderOrigin" relay/src server/src webapp/src webapp/e2e packages/shared/src` 로 참조가 shared 정의·export 뿐인지 확인한 뒤 `packages/shared/src/relay.ts` 의 두 타입과 주석 블록, `index.ts` export 를 지운다(D-05 — 옛 계약을 남기면 다음 소비자가 동결 테이블 모양을 다시 쓴다). 참조가 남아 있으면 그 파일을 먼저 `JournalOrderRow` 로 바꾸고 SUMMARY 에 적는다. `DmaOrderStatus` 는 relay `order.result` 가 쓰므로 유지한다.
`webapp/e2e/specs/me.spec.ts` 의 `TODAY_ORDERS` 픽스처를 `JournalOrderRow[]` 로 바꾼다(type import · 필드 `tradeDate`(오늘 = 스펙 실행 시각의 `kstDateIso()`) · `lastSeq` · `modifiedQty` · `origin`(한 행 null 포함) · `requester` · `requestKind` · `board` 추가, `market` 제거). 테스트 8 의 긴 종목명 행 `0000900002` 단언이 그대로 성립하게 `stockCode` 를 둔다. 새 단언은 더하지 않는다(B′ 단언은 19-08).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/server run typecheck && pnpm --filter @gh-radar/webapp run test</automated>
    <fails_when>non-zero exit, 또는 "error TS", 또는 vitest 요약줄에 failed</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/me.spec.ts</automated>
    <fails_when>non-zero exit, 또는 요약줄에 "failed", 또는 "0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "DmaOrderRow|DmaOrderOrigin" packages/shared/src relay/src server/src webapp/src webapp/e2e` 출력 0줄(== 0)
    - `grep -n "JournalOrderRow" webapp/e2e/specs/me.spec.ts` >= 1
  </acceptance_criteria>
  <done>저장소 어디에도 옛 주문 행 계약이 없고, My page e2e 가 새 행 모양으로 green 이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay wss → 브라우저 리듀서 | 사용자별로 걸러진 행만 온다(19-05) — 브라우저는 받은 것만 보관 |
| 로그아웃 경계 | 같은 브라우저에서 다음 사용자 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-16 | Information Disclosure | 리듀서 `journalRows` 잔존 | medium | mitigate | `reset` → `INITIAL_DATA`(빈 배열·null), 리듀서 테스트 |
| T-19-26 | Denial of Service | 무한 누적 메모리 | low | mitigate | `MAX_JOURNAL_ROWS = 2000` 상한 |
| T-19-27 | Tampering | 옛 행이 새 행을 덮음(표시 역전) | low | mitigate | id 기준 lastSeq 큰 쪽 규칙 — orders-api·리듀서 테스트 |
</threat_model>

<verification>
- webapp 전체 vitest · typecheck(e2e tsconfig 포함) green · Playwright me.spec green.
- spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</verification>

<success_criteria>
- 카드 원천 = REST + journal.rows, 옛 join·루프 0, 옛 계약 0.
</success_criteria>

## Artifacts this phase produces (이 plan 분)

| 종류 | 이름 |
|------|------|
| 상태 필드 | `RelayConnectionState.journalRows` · `RelayConnectionState.journalState` · `MAX_JOURNAL_ROWS = 2000` · `EMPTY_JOURNAL_ROWS` |
| 함수 | `mergeJournalRows(restored, pushed, today)` · `orderDisplayStatus(JournalOrderRow)` · `mergeKeyOf(JournalOrderRow)`(규칙 변경) |
| 삭제 | `TodayOrderRow` · `mergeTodayOrders` · `MergeTodayOrdersResult` · shared `DmaOrderRow` · `DmaOrderOrigin` |

<output>
Create `.planning/phases/19-account-order-journal/19-06-SUMMARY.md` when done
</output>
