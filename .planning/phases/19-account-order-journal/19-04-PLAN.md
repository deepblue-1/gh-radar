---
phase: 19-account-order-journal
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - packages/shared/src/journal.ts
  - packages/shared/src/index.ts
  - packages/shared/src/relay.ts
  - packages/shared/src/__tests__/journal.test.ts
  - server/src/services/dma-orders.ts
  - server/src/routes/orders.ts
  - server/tests/routes/orders.test.ts
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-08]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-05: `GET /api/orders` 는 새 테이블만 읽는다 — 서비스 함수가 `dma_journal_orders_for_user` RPC 를 **왕복 1회**로 부르고 `dma_orders` 를 조회하지 않는다(Cloud Run 왕복 비용 메모리)"
    - "D-06 · T-19-17: RPC 의 `p_user_id` 는 `requireAuth` 가 확정한 `req.userId` 하나뿐이다 — 쿼리·바디에서 사용자 id 를 받지 않는다"
    - "D-08 · T-19-08: 응답 행에 `dma_user_id` 가 없다 — 공유 매퍼 `toJournalOrderRow` 가 공개 컬럼만 camelCase 로 옮기고, `origin` 이 NULL 이면 null 그대로(「수동」 으로 채우지 않는다)"
    - "`date` 쿼리 형식·실재 검증(형식은 zod · `2026-13-45` 같은 가짜 날짜는 400)과 bare array 응답 규약은 그대로다"
    - "D-03 · D-04: 브라우저로 가는 새 wss 프레임 계약 `journal.rows`(완전한 행 배열)와 `journal.state`(live · delayed + since)가 shared 에 정의돼 `RelayOutbound` 유니온에 들어 있다"
  artifacts:
    - path: packages/shared/src/journal.ts
      provides: "JournalOrderRow · JournalOrderDbRow · toJournalOrderRow · JOURNAL_ORDER_PUBLIC_COLUMNS"
      exports: ["JournalOrderRow", "JournalOrderDbRow", "JournalOrderStatus", "JournalOrderOrigin", "toJournalOrderRow", "JOURNAL_ORDER_PUBLIC_COLUMNS"]
    - path: packages/shared/src/relay.ts
      provides: "RelayJournalRowsMsg · RelayJournalStateMsg · RelayJournalState"
      contains: "journal.rows"
    - path: server/src/services/dma-orders.ts
      provides: "listTodayOrders → RPC 1회"
      contains: "dma_journal_orders_for_user"
  key_links:
    - from: "server/src/routes/orders.ts"
      to: "listTodayOrders(supabase, req.userId!, date)"
      via: "requireAuth 뒤 서비스 호출"
      pattern: "req.userId"
    - from: "server/src/services/dma-orders.ts"
      to: "public.dma_journal_orders_for_user(uuid, date)"
      via: "supabase.rpc 파라미터 p_user_id · p_trade_date"
      pattern: "p_trade_date"
  prohibitions:
    - "MUST NOT 새 경로를 만든다 — `GET /api/orders` 같은 경로 유지(오케스트레이터 Q2)"
    - "MUST NOT server 를 배포한다 — 19-12 에서 webapp push 직전"
    - "MUST NOT `DmaOrderRow` 를 이 plan 에서 지운다 — webapp 이 19-06 까지 참조한다"
---

<objective>
공유 저널 행 계약(`JournalOrderRow` + 매퍼)을 만들고 server `GET /api/orders` 를 새 테이블 RPC 1회로 바꾼다. tracer 는 「RPC 행 1건 → 공유 매퍼 → 라우트 bare array → 브라우저가 받는 camelCase 행」 이고, 이어서 브라우저 wss 프레임 계약(`journal.rows` · `journal.state`)을 shared 에 더한다.

Purpose: D-05 로 카드는 새 테이블만 읽는다. 가시성 필터(D-06)는 RPC 안의 조인이 정본이라 server 는 인증된 사용자 id 만 넘긴다. 매퍼를 shared 에 두는 이유는 relay 기록기(19-05)가 적용 RPC 반환 행을 **같은 매퍼**로 푸시하기 때문이다 — 두 벌이면 REST 와 푸시 행의 모양이 갈라져 카드 병합(id · lastSeq)이 틀어진다.
Output: `packages/shared/src/journal.ts` · 프레임 타입 · server 서비스/라우트/테스트 갱신.

커밋 규율: 한글 메시지 · Co-Authored-By 없음 · 파일 지정 add · `git status -sb` · **push 금지**. server 는 배포하지 않는다(응답 모양이 바뀌므로 webapp push 직전에만 — Pitfall 12).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-account-order-journal/19-CONTEXT.md
@.planning/phases/19-account-order-journal/19-01-SUMMARY.md

<interfaces>
19-01 이 만든 RPC(시그니처 원문): `public.dma_journal_orders_for_user(p_user_id uuid, p_trade_date date) RETURNS TABLE(id uuid, trade_date date, account_no text, isin text, stock_code text, exchange text, board text, side text, order_type text, org_order_no text, qty integer, price integer, order_no text, filled_qty integer, modified_qty integer, status text, result_code integer, notice_type text, message text, origin text, requester text, request_kind text, last_seq bigint, created_at timestamptz, updated_at timestamptz)` — 공개 컬럼 25종, `dma_user_id` 없음. 적용 RPC 의 반환 `rows` 도 같은 25종 jsonb.

현 server (`server/src/services/dma-orders.ts`): `listTodayOrders(supabase, userId, date?)` 가 `kstDayRangeUtc(date)` 로 UTC 구간을 만들고 `dma_orders` 를 `.eq("user_id", userId)` 로 조회 · `DbError = (msg) => new ApiError(500, "DB_ERROR", msg)` · `kstDayRangeUtc` 는 가짜 날짜를 400 으로 막는다. 라우트(`server/src/routes/orders.ts`)는 `OrderListQuery.safeParse(req.query)` → `listTodayOrders(supabase, req.userId!, parsed.data.date)` → `res.json(data)`.

shared(`packages/shared/src/relay.ts`): `OrderSide = "B"|"S"` · `OrderType = "N"|"M"|"C"` · `RelayExchange = "KRX"|"NXT"` · `RelayOutbound` 유니온(1119-1136) · `DmaOrderRow`/`DmaOrderOrigin`(1240-1276, 이 plan 에서는 유지). `index.ts` 는 relay.ts 타입을 `export type {...} from "./relay"` 목록으로 내보낸다(75-84). `kstDateIso(now?)` 는 `krxCalendar.ts:53`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] RPC 행 1건 → 공유 매퍼 → GET /api/orders bare array (서비스 RPC 전환 · 라우트 테스트 재작성)</name>
  <files>packages/shared/src/journal.ts, packages/shared/src/index.ts, server/src/services/dma-orders.ts, server/src/routes/orders.ts, server/tests/routes/orders.test.ts</files>
  <read_first>
    - server/src/services/dma-orders.ts (전문 128줄)
    - server/src/routes/orders.ts (전문 58줄)
    - server/tests/routes/orders.test.ts (전문 — supabase mock · ⓪ POST 404 · ⑯ GET 케이스들)
    - server/tests/fixtures/supabase-mock.ts (200-230 `rpc` 목 규약)
    - packages/shared/src/relay.ts (1140-1276 주문 DTO · DmaOrderRow 주석 형식)
    - packages/shared/src/index.ts (전문)
    - packages/shared/src/krxCalendar.ts (전문)
  </read_first>
  <behavior>
    - GET /api/orders (인증) → mock `rpc("dma_journal_orders_for_user")` 1회 · 파라미터 `{p_user_id: 인증 사용자 id, p_trade_date: kstDateIso()}` · 200 bare array · 행이 camelCase(`accountNo` · `tradeDate` · `lastSeq` number · `origin` null 보존) · 응답 JSON 문자열에 `dma_user_id`·`dmaUserId` 없음
    - `?date=2026-09-28` → p_trade_date '2026-09-28' · `?date=2026-13-45` → 400 VALIDATION_FAILED · `?date=abc` → 400
    - 쿼리에 `user_id=남의id` 를 붙여도 p_user_id 는 인증 사용자 id
    - rpc error → 500 DB_ERROR(원문 비노출) · `from("dma_orders")` 호출 0 · 쓰기 0
    - 미인증 401 · POST /api/orders 404 (기존 ⓪ 유지)
  </behavior>
  <action>
**shared** `packages/shared/src/journal.ts` 새 파일(머리 주석에 Phase 19 D-05 · D-06 · D-08 근거와 「REST(server)와 wss 푸시(relay)가 같은 매퍼를 쓴다 — 두 벌 금지」):
- `JournalOrderStatus = "accepted" | "partially_filled" | "filled" | "cancelled" | "rejected" | "modified"` (requested·timeout 은 relay 즉시응답 전용 — 저널 행에 없다는 주석).
- `JournalOrderOrigin = "manual" | "limit_chaser" | "vi"`.
- `JournalOrderDbRow` — RPC 컬럼 25종 snake_case 그대로(`last_seq: number | string` — PostgREST bigint 직렬화 방어, `side: "B"|"S"|null`, `origin: JournalOrderOrigin | null` 등 nullable 은 RPC 정의대로).
- `JournalOrderRow` — camelCase: `id` · `tradeDate` · `accountNo` · `isin` · `stockCode: string|null` · `exchange: RelayExchange` · `board: string|null` · `side: OrderSide|null` · `orderType: OrderType` · `orgOrderNo: string|null` · `qty: number|null` · `price: number|null` · `orderNo: string|null` · `filledQty` · `modifiedQty` · `status: JournalOrderStatus` · `resultCode: number|null` · `noticeType: string|null` · `message: string|null` · `origin: JournalOrderOrigin|null`(null = 출처 미상 → 칩 생략, D-08 보충) · `requester: string|null` · `requestKind: string|null` · `lastSeq: number` · `createdAt` · `updatedAt`. `dmaUserId` 필드는 **없다**(주석으로 이유 — D-08 주문자 표시 안 함 · T-19-08).
- `JOURNAL_ORDER_PUBLIC_COLUMNS` — 25종 snake 컬럼명 readonly 배열(테스트가 `dma_user_id` 부재를 이 목록으로 단언).
- `toJournalOrderRow(r: JournalOrderDbRow): JournalOrderRow` — 순수 매핑, `lastSeq = Number(r.last_seq)`, 값 보정·기본값 채움 없음(`origin` null → null).
`packages/shared/src/index.ts` 에 위 이름들을 `./journal` 에서 export 한다.

**server** `server/src/services/dma-orders.ts`: `listTodayOrders(supabase, userId, date?)` 를 `supabase.rpc("dma_journal_orders_for_user", { p_user_id: userId, p_trade_date: tradeDate })` 1회 + `toJournalOrderRow` 매핑으로 바꾸고 반환 타입을 `JournalOrderRow[]` 로. 날짜는 `kstDayRangeUtc` 의 검증 로직을 살린 `resolveTradeDate(date?: string): string` 으로 바꾼다(기본 `kstDateIso()`, 형식은 맞지만 실재하지 않는 날짜는 `ApiError(400, "VALIDATION_FAILED", "date: 올바른 날짜가 아닙니다.")` — 판정은 입력 날짜에 KST 자정(+09:00)을 붙여 만든 Date 가 NaN 이거나, 그 Date 를 다시 `kstDateIso` 로 내린 값이 입력과 다를 때). `kstDayRangeUtc` 는 다른 import 가 없으면 지운다. rpc 오류는 `DbError("주문 목록 조회에 실패했습니다.")`. 옛 `DmaOrderDbRow`/`ORDER_COLS`/`mapOrder` 를 지운다. 머리 주석을 「Phase 19 D-05 — 새 계좌 기준 테이블만 읽는다 · 가시성 필터는 RPC 조인이 정본(D-06) · 이 함수는 인증된 userId 만 넘긴다(T-19-17)」 로 갱신하고 T-15-01 문단(`WHERE user_id` 필터가 방어선)을 새 사실로 고친다.
`server/src/routes/orders.ts`: 코드 흐름은 유지하고 타입을 `JournalOrderRow[]` 로, 머리 주석의 방어선 목록(T-15-01)을 「조회 RPC EXECUTE 는 service_role 전용(T-19-01) · p_user_id = req.userId 하나(T-19-17)」 로 고친다.
`server/tests/routes/orders.test.ts`: supabase mock 에 `rpc(fn, params)` 를 추가해 호출 이름·파라미터를 기록하고 픽스처 25종 snake 행을 돌려준다(`from` 호출은 기록만 — `dma_orders` 조회 0 단언용). ⑯ GET 케이스들을 `<behavior>` 대로 재작성하고 ⓪·⓪-b(POST 404)와 401 케이스는 유지한다. 픽스처 행의 `origin` 은 한 행을 null, 한 행을 limit_chaser 로 둬 매핑 왕복을 잠근다(WR-05 규율).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/server exec vitest run tests/routes/orders.test.ts</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 "Test Files" 요약줄에 failed</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/server run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "dma_journal_orders_for_user" server/src/services/dma-orders.ts` >= 1 · 서비스에 옛 테이블 조회가 없다 — `grep -n 'from("dma_orders")' server/src/services/dma-orders.ts` 출력 0줄(== 0)
    - `grep -n "dmaUserId\|dma_user_id" packages/shared/src/journal.ts` 가 주석 줄만 보여 준다(타입 필드 0)
    - `grep -n "p_user_id: userId" server/src/services/dma-orders.ts` >= 1 · 라우트가 `req.userId` 를 넘긴다 — `grep -n "req.userId" server/src/routes/orders.ts` >= 1
  </acceptance_criteria>
  <done>로그인 사용자가 `GET /api/orders` 로 자기 계좌 권한의 새 테이블 행만 받고, 응답에는 주문자 식별자가 없다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: wss 프레임 계약 `journal.rows` · `journal.state` + 매퍼 단위 테스트</name>
  <files>packages/shared/src/relay.ts, packages/shared/src/index.ts, packages/shared/src/__tests__/journal.test.ts</files>
  <read_first>
    - packages/shared/src/relay.ts (395-420 인바운드 규약 · 1040-1136 아웃바운드 타입 형식 · `RelayNxtSnapMsg` 주석 규율)
    - packages/shared/src/journal.ts (Task 1 결과)
    - packages/shared/src/__tests__/strategy-display.test.ts (shared 테스트 형식)
  </read_first>
  <behavior>
    - `toJournalOrderRow` 가 25 컬럼을 모두 옮기고 `last_seq` "42"(문자열) → 42 · origin null → null · side null → null · qty null → null
    - 결과 객체 키 집합 = 기대 camelCase 25종 · `dmaUserId` 없음
    - `JOURNAL_ORDER_PUBLIC_COLUMNS` 길이 25 · `dma_user_id` 미포함
  </behavior>
  <action>
`packages/shared/src/relay.ts` 아웃바운드 절에 두 프레임을 더한다(주석 규율은 `RelayNxtSnapMsg` 와 같게 — 무엇이 원천이고 언제 오는지):
- `RelayJournalRowsMsg = { t: "journal.rows"; rows: JournalOrderRow[] }` — 관찰자 기록기가 적용 RPC 반환 행을 **그 계좌에 접근할 수 있는 사용자에게만** 푸시한다(D-03 · T-15-02 사용자 스코프). 행은 완전한 행이라 카드가 행을 **만들 수 있다**(구 「라이브는 행을 못 만든다」 규율 폐기)는 주석.
- `RelayJournalState = "live" | "delayed"` · `RelayJournalStateMsg = { t: "journal.state"; s: RelayJournalState; since?: string }` — `since` 는 기록 연결이 live 를 벗어난 시각(ISO). 인증 직후 스냅샷 1프레임 + 전이 때. relay 가 모르면(관찰자 비활성) 보내지 않는다 — 브라우저는 프레임 부재를 「표식 없음」 으로 읽는다(D-04 (a)).
두 타입을 `RelayOutbound` 유니온에 추가하고 `index.ts` 에서 export 한다. `JournalOrderRow` 는 `./journal` 에서 import 한다(순환 import 가 생기면 journal.ts 가 relay.ts 의 `OrderSide`·`OrderType`·`RelayExchange` 만 type import 하는 방향으로 고정).
`packages/shared/src/__tests__/journal.test.ts` 를 `<behavior>` 대로 만든다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/shared exec vitest run src/__tests__/journal.test.ts</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 "Test Files" 요약줄에 failed</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/server run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS" (유니온 확장이 소비처의 exhaustive switch 를 깨면 여기서 드러난다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n '"journal.rows"' packages/shared/src/relay.ts` >= 1 · `grep -n '"journal.state"' packages/shared/src/relay.ts` >= 1
    - `grep -n "RelayJournalRowsMsg\|RelayJournalStateMsg" packages/shared/src/index.ts` >= 1
    - relay·webapp·server typecheck 0 error (소비처 switch 가 새 t 를 모르면 컴파일 오류 대신 기존 default 로 흘러야 한다 — 오류가 나면 이 task 에서 그 switch 에 무시 case 를 더하고 SUMMARY 에 적는다)
  </acceptance_criteria>
  <done>relay 와 webapp 이 같은 두 프레임 타입과 같은 행 타입을 import 할 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → server `GET /api/orders` | 인증 토큰으로 사용자 확정, 쿼리는 날짜만 |
| server(service_role) → Supabase RPC | 가시성 필터의 정본(조인)이 여기서 실행된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-17 | Elevation of Privilege | `p_user_id` 출처 | high | mitigate | `req.userId`(requireAuth) 하나만 전달, 쿼리 `user_id` 무시 단언 |
| T-19-08 | Information Disclosure | 응답의 `dma_user_id` | medium | mitigate | 공유 매퍼·공개 컬럼 목록, 응답 문자열 부재 단언 |
| T-19-24 | Information Disclosure | DB 오류 원문 노출 | low | mitigate | `DbError` 고정 문구 · 기존 errorHandler |
| T-19-25 | Tampering | 가짜 날짜로 500 유발 | low | mitigate | `resolveTradeDate` 400 |
</threat_model>

<verification>
- shared build + journal 테스트 · server orders 테스트 · relay/webapp/server typecheck green.
- spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</verification>

<success_criteria>
- `GET /api/orders` 가 새 테이블 RPC 1회로 응답하고 주문자 식별자가 없다.
- `journal.rows`/`journal.state` 계약이 shared 에 있다.
</success_criteria>

## Artifacts this phase produces (이 plan 분)

| 종류 | 이름 |
|------|------|
| 타입 | `JournalOrderRow` · `JournalOrderDbRow` · `JournalOrderStatus` · `JournalOrderOrigin` · `RelayJournalRowsMsg` · `RelayJournalStateMsg` · `RelayJournalState` |
| 함수·상수 | `toJournalOrderRow` · `JOURNAL_ORDER_PUBLIC_COLUMNS` · server `resolveTradeDate` |
| 프레임 | `{t:"journal.rows", rows}` · `{t:"journal.state", s, since?}` |
| 파일 | `packages/shared/src/journal.ts` · `packages/shared/src/__tests__/journal.test.ts` |

<output>
Create `.planning/phases/19-account-order-journal/19-04-SUMMARY.md` when done
</output>
