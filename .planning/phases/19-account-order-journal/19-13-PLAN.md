---
phase: 19-account-order-journal
plan: 13
type: execute
wave: 9
depends_on: ["19-12"]
files_modified:
  - .planning/phases/19-account-order-journal/19-RECONCILIATION.md
autonomous: false
requirements: [D-01, D-02, D-06, D-11, D-12, D-14]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-14: 전환 뒤 첫 거래일(이번 주 전환이면 2026-09-28 월) 장 마감 뒤, 계좌별로 브로커 체결내역 · 게이트웨이 저널/주문 기록 · `dma_account_orders` 의 건수와 상태를 대조해 불일치 0 이면 종결한다(그림자 병행이 없으므로 이 대조가 유일한 실장 검증)"
    - "D-12 · D-11: 그날 relay 부재·끊김 구간이 있었다면 그 구간 주문도 새 테이블에 있다(커서 이어받기) · 매핑 없는 계좌의 주문도 이벤트로 적재돼 있다 · `apply_error` 0"
    - "D-06: 같은 DMA 계정을 공유하는 두 사용자(95e9…·b1e2…)의 그날 `dma_journal_orders_for_user` id 집합이 같고 행 수가 0 이 아니다 — 09-23 의 43 vs 2 가 재현되지 않는다"
    - "D-01: 그날 `dma_orders` 에 새 쓰기가 0 이다(동결 유지)"
    - "D-02: 그날 로컬 거부(게이트웨이 사전 거부)가 있었다면 `reject_seq` 행으로 있다"
  artifacts:
    - path: .planning/phases/19-account-order-journal/19-RECONCILIATION.md
      provides: "첫 거래일 계좌별 대조표(마스킹) · 판정"
  key_links:
    - from: "dma_journal_events(trade_date)"
      to: "dma_account_orders(trade_date)"
      via: "계좌별 이벤트 수·주문 행 수·상태 분포"
      pattern: "trade_date"
  prohibitions:
    - "MUST NOT 계좌번호·사용자 id 원문을 저장소 문서에 적는다 — 마스킹(앞 4자 + ****)"
    - "MUST NOT 불일치를 이 plan 에서 코드로 고친다 — 갭 클로징(/gsd-plan-phase 19 --gaps)으로 넘긴다"
---

<objective>
**첫 거래일 실장 대조(D-14 체크포인트)**. 전환 뒤 첫 거래일 장 마감(20:00 KST) 이후, DB 쪽 사실을 자동으로 모아 대조표를 만들고, 사람이 브로커 체결내역·게이트웨이 기록과 맞춰 본 뒤 종결 또는 갭 클로징을 결정한다.

Purpose: D-14 는 그림자 병행을 기각했다 — 대신 이 대조가 「브라우저 접속과 무관하게 모든 계좌의 주문이 빠짐없이 기록된다」 는 phase 목표의 실장 증거다. 브로커 체결내역은 사용자만 조회할 수 있어 사람 단계가 필요하다. 배포·검증 plan 이라 구현 tracer 가 없다(의도).
Output: `19-RECONCILIATION.md`(계좌별 대조표 · 판정) · 사용자 판정.

커밋 규율: 한글 메시지 · Co-Authored-By 없음 · 파일 지정 add · `git status -sb`. 이 문서 커밋은 docs 전용이라 push 해도 Vercel 이 빌드를 건너뛴다(정상) — push 는 사용자 요청 시에만.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-account-order-journal/19-CONTEXT.md
@.planning/phases/19-account-order-journal/19-12-SUMMARY.md

근거: 09-23 실측(같은 DMA 계정 두 사용자 자동주문 기록 43 vs 2 — debug mobile-bg-resume-gaps) · 휴장 2026-09-24·25(추석) · 10-05 · 10-09. 서비스롤 키는 Secret Manager `gh-radar-supabase-service-role` 을 셸 변수로만(출력 금지) · `SUPABASE_URL` = `https://ivdbzxgaapbmrxreyuht.supabase.co`(비밀 아님) · healthz `https://dma.jx1.io/healthz`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB 쪽 사실 수집 → 계좌별 대조표 초안(19-RECONCILIATION.md)</name>
  <precondition>19-12 전환 완료 뒤 첫 거래일(KRX 영업일)의 20:00 KST 가 지났다 — `TZ=Asia/Seoul date` 와 shared `isKrxHoliday` 로 그날이 영업일인지 확인</precondition>
  <files>.planning/phases/19-account-order-journal/19-RECONCILIATION.md</files>
  <read_first>
    - .planning/phases/19-account-order-journal/19-12-SUMMARY.md (전환 시각 · 배포 SHA · 공유 계정 묶음)
    - supabase/migrations/20260924200000_dma_journal_tables.sql (컬럼 이름)
  </read_first>
  <action>
대상 거래일 D 를 정하고(전환 뒤 첫 영업일) 서비스롤 PostgREST 로 읽기 전용 조회만 한다(값 출력 금지 · 결과는 집계로만):
① `dma_journal_events?trade_date=eq.D` — 계좌별(마스킹) notice_type 분포 · local_reject 수 · `apply_error` 있는 행 수.
② `dma_account_orders?trade_date=eq.D` — 계좌별 행 수 · status 분포 · origin 분포(null 포함) · reject_seq 행 수 · 체결 수량 합.
③ `dma_journal_cursor?gateway=eq.KB` — epoch · last_seq · updated_at, 그리고 healthz `journal` 필드(lagSeq 0 · state live).
④ `dma_orders` 최신 `updated_at` 이 전환 시각 이전인지(동결 유지 · D-01).
⑤ 공유 계정 묶음(19-12 에서 찾은 사용자들)에 대해 `rpc/dma_journal_orders_for_user(p_user_id, D)` 를 불러 사용자별 행 수와 id 집합 동일 여부(D-06 — 43 vs 2 재현 여부).
⑥ relay 로그에서 그날 관찰자 끊김·resync·gap 흔적(`gcloud compute ssh radar-gw ... sudo docker logs --since 24h gh-radar-relay | grep -E "\[JOURNAL\]|\[journal\]"` 요약 — 계좌 원문 없음)과 그 구간의 주문이 새 테이블에 있는지(created_at 분포).
`19-RECONCILIATION.md` 를 쓴다: 대상 거래일 · 전환 SHA · 계좌별 표(계좌 마스킹 | 이벤트 수 | 주문 행 수 | 상태 분포 | 로컬 거부 | apply_error | 브로커 체결내역(사람 입력 칸) | 게이트웨이 기록(사람 입력 칸) | 판정 칸) · 공유 계정 대조 결과 · 끊김 구간 · 동결 확인 · 자동 판정 요약(DB 쪽 이상 유무). 사람 입력 칸은 비워 둔다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && f=.planning/phases/19-account-order-journal/19-RECONCILIATION.md && test -s "$f" && grep -cE "apply_error|공유 계정|동결|브로커 체결내역" "$f"</automated>
    <fails_when>non-zero exit(파일 없음·빈 파일), 또는 출력 숫자가 4 미만</fails_when>
  </verify>
  <acceptance_criteria>
    - 문서에 계좌번호 원문 없음 — `grep -nE "[0-9]{8,}" .planning/phases/19-account-order-journal/19-RECONCILIATION.md` 가 날짜·seq·주문번호 외 계좌 형태 숫자를 보여 주지 않는다(SUMMARY 에 확인 결과)
    - 공유 계정 묶음의 사용자별 행 수와 id 집합 동일 여부가 표에 있다
    - apply_error 수 · dma_orders 동결 확인 결과가 있다
  </acceptance_criteria>
  <done>그날 DB 가 무엇을 기록했는지 계좌별로 한 장에 모였고, 사람이 브로커 기록과 나란히 볼 수 있다.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: 브로커 체결내역 · 게이트웨이 기록 대조 — 종결 또는 갭 클로징</name>
  <action>계좌별 브로커 체결내역(사용자만 조회 가능)과 게이트웨이 주문 기록(gh-trade `MMDD_order.bin`·서버 로그)을 19-RECONCILIATION.md 의 DB 집계와 맞춰 본다.</action>
  <instructions>
    Claude 가 이미 한 것: DB 이벤트·주문 행·커서·공유 계정 대조·동결 확인·끊김 구간을 계좌별 표로 모았다(Task 1).
    사람이 할 것: 표의 「브로커 체결내역」 · 「게이트웨이 기록」 칸을 계좌별로 채우거나 Claude 에게 숫자를 알려 준다(주문 건수 · 체결 수량 · 취소 건수 · 거부 건수). 특히 같은 DMA 계정을 쓰는 두 사용자가 My page 「오늘 주문」 에서 같은 줄을 보는지, 브라우저를 닫아 둔 사이의 자동주문이 보이는지 눈으로 확인한다.
    불일치가 있으면 계좌·시각·종류를 알려 준다 — Claude 는 코드를 고치지 않고 `/gsd-plan-phase 19 --gaps` 로 넘길 갭 목록을 문서에 적는다.
  </instructions>
  <verification>Claude 가 확인한다: 문서의 판정 칸이 모두 채워졌고, 불일치 0 이면 「종결」, 아니면 갭 목록이 있다. STATE.md 에 결과 1줄(종결/갭 수)을 기록한다.</verification>
  <resume-signal>「일치 — 종결」 또는 「불일치: {계좌 말미 · 시각 · 종류} …」</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 프로덕션 DB(서비스롤 읽기) → 저장소 문서 | 계좌·사용자 식별자가 문서로 새지 않아야 한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-43 | Information Disclosure | 대조 문서의 계좌·사용자 id | medium | mitigate | 마스킹(앞 4자 + ****) · 수용 기준 grep |
| T-19-03 | Information Disclosure | 서비스롤 키 | high | mitigate | 셸 변수만 · 출력 금지 · 읽기 전용 조회 |
| T-19-44 | Repudiation | 실장 누락의 무증거 종결 | medium | mitigate | 사람 대조 칸이 채워져야 종결 · 불일치는 갭 목록으로 |
</threat_model>

<verification>
- `19-RECONCILIATION.md` 판정 칸 전부 채움 · 사용자 판정 기록.
- spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</verification>

<success_criteria>
- 첫 거래일 대조 불일치 0 → Phase 19 종결, 아니면 갭 목록이 `/gsd-plan-phase 19 --gaps` 입력으로 준비됨.
</success_criteria>

## Artifacts this phase produces (이 plan 분)

| 종류 | 이름 |
|------|------|
| 문서 | `.planning/phases/19-account-order-journal/19-RECONCILIATION.md` |
| 기록 | STATE.md 첫 거래일 대조 결과 |

<output>
Create `.planning/phases/19-account-order-journal/19-13-SUMMARY.md` when done
</output>
