---
phase: 19-account-order-journal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - relay/src/ws/order-handler.ts
  - relay/src/ws/fanout.ts
  - relay/src/index.ts
  - relay/tests/ws-order.test.ts
  - relay/src/store/orders.ts
  - relay/tests/order-store.test.ts
  - relay/tests/helpers/fake-dma-orders.ts
  - relay/src/order/notice-status.ts
  - relay/src/hub/subscription-hub.ts
  - webapp/e2e/specs/orderbook.spec.ts
  - webapp/e2e/specs/trading-workbench.spec.ts
files_deleted:
  - relay/src/store/orders.ts
  - relay/tests/order-store.test.ts
  - relay/tests/helpers/fake-dma-orders.ts
autonomous: true
requirements: [D-01, D-05]

estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01: relay 의 사용자 세션 경로는 DB 에 한 줄도 쓰지 않는다 — 요청 시점 requested 행 · 정산 갱신 · 미부착 통보 기록 · 자동주문 행 생성 · 원주문 정산 · 정정 이동 기록 · 전략 캐시로 계좌를 추정하는 로직이 relay/src 에서 사라졌다"
    - "D-01: 수동 주문의 즉시 응답은 그대로다 — `order.new`/`order.modify`/`order.cancel` → DirectOrderReq(2) → 첫 51 → `order.result` 가 rid 로 **요청한 연결에만** 가고, 중복 가드(rid · 사용자 튜플 · 취소 원주문번호) · 5초 타임아웃(「결과를 확인하지 못했습니다」 문구) · `narrowPending` 상관은 메모리에서만 한다"
    - "D-01: 주문 분기는 종목맵(`symbols`) 하나로 열린다 — 기록 창구 미주입으로 주문 인바운드가 막히는 갈래가 없다"
    - "D-03(토스트 경로 유지): hub 의 51 → `{t:\"order\"}` 사용자 전 연결 팬아웃과 감사 로그 사본은 바뀌지 않는다"
    - "D-05: relay 는 동결 테이블 `dma_orders` 에 쓸 수 있는 코드를 갖지 않는다 — 기록 모듈·테스트·가짜 PostgREST 가 삭제됐고, e2e 두 spec 이 「감사 기록 0건」 을 단언한다"
  artifacts:
    - path: relay/src/ws/order-handler.ts
      provides: "rid 즉시응답 상관만 남은 wss 주문 핸들러"
    - path: relay/tests/ws-order.test.ts
      provides: "DB 단언을 걷어낸 주문 상관 회귀(유지 케이스 + narrowPending)"
  key_links:
    - from: "relay/src/ws/fanout.ts"
      to: "createOrderHandler"
      via: "symbols 만 있으면 주문 분기가 열린다"
      pattern: "deps.symbols !== undefined"
    - from: "hub.on(\"order\")"
      to: "narrowPending → settle → order.result"
      via: "대기 주문 좁히기 뒤 요청 연결에만 결과 프레임"
      pattern: "narrowPending"
  prohibitions:
    - "MUST NOT `order.result` 프레임의 모양·문구·대상(요청 연결 한정)을 바꾼다"
    - "MUST NOT hub 의 `{t:\"order\"}` 팬아웃과 `[HUB] 주문 통보 수신(감사 사본)` 로그를 지운다 (Open Q7 — 유지)"
    - "MUST NOT 새 기록 경로(관찰자 기록기)를 이 plan 에서 만든다 — 19-05 소관"
    - "MUST NOT relay 를 배포한다 · push 한다 — 전환은 19-11/19-12"
---

<objective>
D-01 의 relay 쪽 몫: 사용자 세션 경로에서 `dma_orders` 기록부를 **전부** 걷어내고, 수동 주문의 rid 즉시응답 상관만 남긴다. tracer 는 「수동 신규 주문 한 경로(order.new → 2 → 51 A → order.result)가 DB 기록 없이 끝까지 돈다」 이고, 이어서 죽은 모듈·테스트를 지우고 e2e 단언을 D-01 로 뒤집는다.

Purpose: 관찰자 연결이 유일한 기록자가 되려면(D-01) 이중 기록자 상관 규칙(requested 행 · 원주문 정산 · 정정 이동 · 전략 캐시 계좌 추정 — order-handler 1,000여 줄)이 사라져야 한다. 남기면 동결 테이블(D-05)에 쓰는 코드가 살아 있는 것 자체가 위험이다. 관찰자 기록기(19-05)와 코드 의존이 없어 wave 1 에서 먼저 한다 — 배포는 19-12 전환 창에서 한 번에(D-14).
Output: 기록부가 빠진 `order-handler.ts` · 결선 정리(`fanout.ts`·`index.ts`) · 삭제 3파일 · 정리된 `ws-order.test.ts` · e2e 2 spec 단언 반전.

커밋 규율: 한글 메시지 · Co-Authored-By 없음 · 파일 지정 add · 커밋 직전 `git status -sb` · **push 금지**(19-12 에서만).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-account-order-journal/19-CONTEXT.md

근거 인용(HEAD d756385 · RESEARCH 라인은 46e8bea 기준이며 그 사이 relay 코드 diff 0):
- RESEARCH §「D-01 제거 인벤토리」(434-477) — 걷어낼 것(라인 표) · 남길 것 · 결선 변화 · 테스트 영향(삭제 / DB 단언만 걷어냄 / 유지 목록). **이 표가 이 plan 의 작업 목록 정본이다.**
- `relay/src/ws/order-handler.ts` 1-160 계약(`OrderNoticeSource` 112-129 · 기록 창구 인터페이스 131-144 · `OrderHandlerDeps` 146-157) · 473-550 `hub.on("order")` · 1239-1299 ③-2/③-3 · 1333 · 1355-1404 · 1439-1471.
- `relay/src/ws/fanout.ts` 237-277 `WsFanoutDeps`(기록 창구 옵션 263-267) · 400-420 주문 핸들러 결선(`deps.symbols` 와 기록 창구 둘 다 요구) · 머리 주석 50-60 「주문 상관을 여기서 하지 않는다 … 기록은 그쪽 몫」.
- `relay/src/index.ts` 1-45 머리 주석(종료 절차 5단계 기록 flush) · 111-131 기록 모듈 생성·주입 · 205-217 종료 시 flush/close.
- `relay/tests/ws-order.test.ts` 60-160 하네스(가짜 기록 창구 · 가짜 PostgREST import) · 263 describe「wss 주문 경로 (D-02)」 · 1817「quick-260923-m23 — 정정확인 M 수량」 · 2103「quick-260923-e1m — 통보 기록 경로」 · 2441「isCancelLikeNotice」 · 2465「narrowPending」 · 2830「modifyMovedQty」.
- `relay/src/order/notice-status.ts` 83-117 상태 순위 표 · 교체 가능 상태 함수(기록 모듈만 import).
- `webapp/e2e/specs/orderbook.spec.ts` 296-309 · `webapp/e2e/specs/trading-workbench.spec.ts` 1016-1019 · 1117-1119 — 로컬 relay 의 감사 기록 insert 1건을 단언.
</context>

<!-- planner-discipline-allow: OrderStore -->
<!-- planner-discipline-allow: orderStore -->
<!-- planner-discipline-allow: insertRequest -->
<!-- planner-discipline-allow: enqueueUpdate -->
<!-- planner-discipline-allow: findIdByOrderNo -->
<!-- planner-discipline-allow: supabaseOrderSinks -->
<!-- planner-discipline-allow: recordUnmatched -->
<!-- planner-discipline-allow: autoInsertRow -->
<!-- planner-discipline-allow: settleOriginal -->

<tasks>

<task type="tracer">
  <name>Task 1: [tracer] 수동 신규 주문 한 경로가 DB 없이 끝까지 — order-handler 기록부 제거 · 결선 정리 · ws-order 하네스</name>
  <reversibility rating="costly">D-01 — relay 기록 경로와 테스트 다수가 걸리고, 되돌리면 이중 기록자 상관 규칙을 다시 설계해야 한다(CONTEXT 원문 rating). 사용자 결정이 이미 있어 체크포인트는 두지 않는다.</reversibility>
  <files>relay/src/ws/order-handler.ts, relay/src/ws/fanout.ts, relay/src/index.ts, relay/tests/ws-order.test.ts</files>
  <read_first>
    - .planning/phases/19-account-order-journal/19-RESEARCH.md (434-477 D-01 제거 인벤토리 전문)
    - relay/src/ws/order-handler.ts (전문 1,886줄 — 한 번 읽고 제거 목록을 만든다)
    - relay/src/ws/fanout.ts (1-110 머리 주석 · import / 237-277 `WsFanoutDeps` / 395-425 생성자 주문 결선)
    - relay/src/index.ts (전문 242줄)
    - relay/tests/ws-order.test.ts (1-330 하네스 · 이후 describe 경계 263 · 1817 · 2103 · 2441 · 2465 · 2830)
    - relay/src/order/notice-status.ts (전문 — `statusOf`/`filledQtyOf` 는 남는다)
  </read_first>
  <behavior>
    - ① 수동 `order.new` → 게이트웨이가 DirectOrderReq(2) 1건 수신 → 51 A → 요청 연결에만 `order.result` accepted · orderNo · 다른 탭엔 결과 프레임 없음 (DB 창구 주입 없이)
    - 같은 rid 재전송·같은 (계좌, ISIN, 방향, 가격, 수량) 대기 중 재요청 → 거부 프레임 · 게이트웨이 송신 0
    - 5초 무응답 → `order.result` status timeout · 「결과를 확인하지 못했습니다」 문구 · 「실패」 단어 없음
    - 대기와 매칭 안 되는 51(상따·VI 자동주문 통보) → `order.result` 없음 · 예외 없음 · hub 의 `{t:"order"}` 는 그대로 사용자 전 연결로 감
    - `WsFanout` 을 `symbols` 만 주고 만들면 주문 인바운드가 열린다(「미주입 — 주문 인바운드를 받지 않는다」 warn 은 `symbols` 부재에만)
    - RESEARCH 「유지」 케이스(②⑦⑧⑨⑩⑪⑮⑰⑳㉑㉒㉓㉔㉙㉚㉛㉜㉝㉟㊱㊲㊳㊵㊶㊷㊸ · describe narrowPending 전체)가 green
  </behavior>
  <action>
RESEARCH 「D-01 제거 인벤토리」 표를 작업 목록으로 삼아 `relay/src/ws/order-handler.ts` 에서 기록부를 전부 걷어낸다(D-01):
- 계약: `OrderNoticeSource` 를 `on("order", listener)` 하나로 좁힌다(전략·계좌 상태 조회 3종 제거 — 자동주문 계좌 추정이 사라지므로). 기록 창구 인터페이스(요청 insert · 갱신 큐 · 주문번호 조회) 전체와 `OrderHandlerDeps` 의 기록 창구 필드를 지운다. `OrderHandlerDeps` 에는 `sessions` · `hub` · `symbols` · `send` · `timeoutMs` 만 남는다. store/orders 에서 오던 import(`kstDayRangeUtc` · insert/갱신 행 타입)와 `safePgError` · `stocksCodeOf` import 를 지운다(`symbols.lookup` 은 시장 구분 때문에 남는다).
- 제거 대상(인벤토리 라인 그대로): `EnsureResult` · 진행 중 맵과 relay trail 계열(미부착 통보 ERROR 판정 전용) · `hub.on("order")` 안의 정정 이동 기록 호출과 미부착 통보 기록 호출(후보 수집 · `narrowPending` · `settle` · 좁히기 실패 warn 은 **남긴다** — `settle` 인자에서 이동 수량을 뺀다) · `modifyMoveOf` · 원주문 조회·정정 이동 기록 · `patchOf` · 미부착 통보 기록 함수 · 원주문 정산 함수 · 행 보장/insert 전용 함수 · 자동주문 행 생성과 그 보조(`sideOf` · `strategyOf` · 전략 캐시 단일 계좌 추정) · `handle` 의 ③-2 요청 행 insert await(「기록 실패 → 주문 안 보냄」 게이트)와 ③-3 연결 생존 재확인(await 가 사라져 동기 경로가 된다 — 그 사이 경쟁도 사라진다는 주석) · 조립 거부·timeout·settle·송신 실패 경로의 갱신 큐 호출과 trail 표식(각 경로의 **프레임 송신은 유지**) · `closeConn` 의 trail 표식(타이머 정리는 유지) · `isCancelLikeNotice` · `orderNoLookupKeys` · `ModifyMove` · `modifyMovedQty`. `PendingOrder` 에서 행 id 필드를 지운다.
- **남긴다**: `InboundOrderMsg` · `OrderHandlerSession(s)` · `PendingOrder` · `ConnState` · `ridKey`/`dupKey`/`claimKeys` · `RELAY_RESULT_CODE` · `stateOf`/`release`/`dropUserDupKeys`/`reject` · `handle`(게이트 ⓪①②③③-1 · 조립 · ④ 대기 · ⑤ 송신) · `closeConn`/`close` · `normalizeOrderNo` · `NEW_ORDER_NOTICE_TYPES` · `REQUEST_KIND_BY_*` · `ANSWERABLE_KINDS_BY_NOTICE_TYPE` · `narrowPending` · `statusOf`/`filledQtyOf` 사용(order.result status 계산).
- 머리 주석(1-45)을 고친다: D-03(「insert·update 를 전부 relay 가 한다」)·T-16-07(자동주문 새 행 insert) 문단을 「Phase 19 D-01 — 기록은 관찰자 기록기 단독, 이 모듈은 rid 즉시응답 상관만」 으로 바꾼다. 과거 함수 이름은 주석에도 남기지 않는다(아래 grep 게이트) — 경위는 「Phase 19 D-01 로 기록 경로 제거」 로만 적는다.

`relay/src/ws/fanout.ts`: `WsFanoutDeps` 의 기록 창구 옵션과 import 를 지우고, 생성자 결선을 `deps.symbols !== undefined` 하나로 연다. 미주입 warn 문구를 「종목맵 미주입 — 주문 인바운드를 받지 않는다」 로 바꾼다. 머리 주석의 「5초 상관·`dma_orders` 기록은 그쪽 몫」 문장을 「5초 상관은 그쪽 몫 — 기록은 관찰자 기록기(Phase 19 D-01)」 로 고친다.

`relay/src/index.ts`: 기록 모듈 import · 생성 · `start()` · `WsFanout` 주입 · 종료 절차 5단계(flush/close)를 지우고 머리 주석의 종료 절차 목록과 D-02 문단(「기록 창구는 WsFanout 에만 주입」)을 그에 맞춰 고친다(관찰자 결선은 19-10 이 같은 파일에 더한다).

`relay/tests/ws-order.test.ts`: 하네스에서 가짜 기록 창구 생성·주입과 가짜 PostgREST import 를 지운다. RESEARCH 「테스트 영향」 대로 **삭제**(⑫⑬⑭⑯⑱⑲㉕㉖㉗㉘㉞㊹㊺ · describe「quick-260923-m23 — 정정확인 M 수량」 · describe「quick-260923-e1m — 통보 기록 경로」 · describe「isCancelLikeNotice」 · describe「modifyMovedQty」), **DB 단언만 걷어냄**(① ①-b ①-c ③ ④ ⑤ ⑥ ㊴ — 와이어 조각·세션·프레임 단언은 유지), **유지**(나머지 + describe「narrowPending」). 삭제·수정마다 SUMMARY 표에 케이스 번호와 사유(D-01)를 적는다. `<behavior>` 의 자동주문 통보 케이스가 기존에 없으면 유지 describe 안에 하나 더한다(`order.result` 없음 · hub 팬아웃 그대로).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay exec vitest run tests/ws-order.test.ts tests/fanout.test.ts</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 "Test Files" 요약줄에 failed</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rnwE "insertRequest|enqueueUpdate|findIdByOrderNo|recordUnmatched|autoInsertRow|settleOriginal|orderStore" relay/src/ws relay/src/index.ts` 출력 0줄(== 0)
    - `grep -n "deps.symbols !== undefined" relay/src/ws/fanout.ts` 가 주문 핸들러 결선 줄을 보여 준다(>= 1)
    - `grep -n "narrowPending" relay/src/ws/order-handler.ts` >= 1 · `grep -n "결과를 확인하지 못했습니다" relay/src/ws/order-handler.ts` >= 1 (유지 경로)
    - SUMMARY 에 삭제·수정·유지 케이스 표(번호 · 사유)가 있다
  </acceptance_criteria>
  <done>수동 주문은 여전히 요청한 탭에 즉시 결과를 받고, relay 는 그 과정에서 DB 에 아무것도 쓰지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: 죽은 기록 모듈 삭제 · 순위 표 정리 · hub 주석 (relay 전량 회귀)</name>
  <files>relay/src/store/orders.ts, relay/tests/order-store.test.ts, relay/tests/helpers/fake-dma-orders.ts, relay/src/order/notice-status.ts, relay/src/hub/subscription-hub.ts</files>
  <read_first>
    - relay/src/order/notice-status.ts (전문 117줄)
    - relay/src/hub/subscription-hub.ts (160-210 `HubOrderEvent` 주석 · 1190-1240 `#onOrderNotice` · 감사 로그)
  </read_first>
  <action>
① 삭제: `relay/src/store/orders.ts`(1,318줄) · `relay/tests/order-store.test.ts` · `relay/tests/helpers/fake-dma-orders.ts` 를 `git rm` 한다. 삭제 전에 `grep -rln` 로 relay/src·relay/tests·webapp/e2e 에 남은 import 가 0 인지 확인하고, 남아 있으면 그 import 를 먼저 지운다(Task 1 이 order-handler·index·ws-order 를 이미 정리했다). 동결 테이블에 쓸 수 있는 코드가 남는 것 자체가 위험이라는 근거(D-05)를 SUMMARY 에 적는다.
② `relay/src/order/notice-status.ts`: 기록 모듈만 쓰던 교체 가능 상태 함수와 그 순위 표·종결 집합을 지운다(import 0 확인 후). `statusOf`/`filledQtyOf`/`ORDER_RESP_TIMEOUT_MS` 는 남긴다. 머리 주석에 「상태 단조 격자의 정본은 이제 DB 투영(`dma_journal_next_status` — 19-01)이다」 한 줄을 더한다.
③ `relay/src/hub/subscription-hub.ts`: 코드는 바꾸지 않는다. `HubOrderEvent` 와 `#onOrderNotice` 주석에서 「주문 라우트가 기록에 원문이 필요」 류 문장을 「order-handler 의 rid 즉시응답 상관과 브라우저 `{t:"order"}` 토스트·전략 로그 표면 전용(Phase 19 D-01·D-03)」 으로 고친다. 감사 로그 사본(`[HUB] 주문 통보 수신(감사 사본)`)은 유지한다(Open Q7).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/relay run test</automated>
    <fails_when>non-zero exit, 또는 "error TS", 또는 vitest 요약줄에 failed</fails_when>
  </verify>
  <acceptance_criteria>
    - `test ! -e relay/src/store/orders.ts && test ! -e relay/tests/order-store.test.ts && test ! -e relay/tests/helpers/fake-dma-orders.ts` 성공
    - `grep -rnwE "OrderStore|supabaseOrderSinks|insertRequest|enqueueUpdate|findIdByOrderNo" relay/src relay/tests` 출력 0줄(== 0)
    - relay/src 에 동결 테이블 쓰기 호출이 없다 — `grep -rn "dma_orders\")" relay/src` 출력 0줄(== 0)
    - `grep -n "감사 사본" relay/src/hub/subscription-hub.ts` >= 1 (감사 로그 유지)
  </acceptance_criteria>
  <done>relay 에는 동결 테이블에 쓸 수 있는 코드가 없고 relay 전체 회귀가 green 이다.</done>
</task>

<task type="auto">
  <name>Task 3: e2e 감사 기록 단언을 0건으로 반전 (orderbook · trading-workbench)</name>
  <files>webapp/e2e/specs/orderbook.spec.ts, webapp/e2e/specs/trading-workbench.spec.ts</files>
  <read_first>
    - webapp/e2e/specs/orderbook.spec.ts (280-310)
    - webapp/e2e/specs/trading-workbench.spec.ts (1000-1025 · 1105-1125)
    - webapp/e2e/fixtures/relay.ts (236-250 `orderInserts` 계약 · 320-345 스텁 주석)
  </read_first>
  <action>
e2e 반전(D-01): `orderbook.spec.ts` 의 「감사 기록이 게이트웨이 송신 전에 남는다」 단언 블록을 `expect(relay.orderInserts()).toHaveLength(0)` 한 줄로 바꾸고 주석을 「D-01 — relay 사용자 세션 경로는 DB 에 쓰지 않는다. 기록은 관찰자 기록기 단독」 으로 고친다(REST 0건 단언은 유지). `trading-workbench.spec.ts` 두 곳의 「감사 기록 1건」 단언도 `toHaveLength(0)` 으로 바꾸고 주석의 「감사 기록 1건」 을 「DB 기록 0건(D-01)」 으로 고친다(게이트웨이 주문 1건 단언은 유지). e2e 픽스처(`webapp/e2e/fixtures/relay.ts`)의 스텁 경로는 건드리지 않는다 — 0건 단언의 관측 수단이다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/orderbook.spec.ts e2e/specs/trading-workbench.spec.ts</automated>
    <fails_when>non-zero exit, 또는 출력에 "failed" 가 있는 요약줄, 또는 "0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "orderInserts()).toHaveLength(0)" webapp/e2e/specs/orderbook.spec.ts` >= 1 · 같은 패턴 `webapp/e2e/specs/trading-workbench.spec.ts` >= 2
    - `git diff --stat -- webapp/e2e/fixtures/relay.ts` 출력 없음(픽스처 무수정)
  </acceptance_criteria>
  <done>두 e2e spec 이 「주문은 나가되 DB 기록은 0건」 을 실제 relay 프로세스로 증명한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 wss → relay 주문 핸들러 | 인바운드 주문이 실계좌 주문이 되는 관문 — 제거 작업이 게이트를 건드리면 안 된다 |
| relay → 게이트웨이 사용자 세션 | 51 통보가 hub 를 거쳐 order.result/토스트로 갈린다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-19 | Information Disclosure | `order.result` 대상 선택 | high | mitigate | 연결 스코프 대기 맵 · `send` 주입 경로 유지, ws-order 유지 케이스(요청 연결에만)로 회귀 |
| T-19-20 | Elevation of Privilege | 주문 게이트(⓪①②③ · 계좌 = `session.allowedAccounts`) | high | mitigate | 게이트 코드는 제거 대상이 아니다 — 유지 케이스 ② 등으로 회귀, ③-2/③-3 만 제거 |
| T-19-21 | Tampering | 중복 체결(더블클릭·두 탭) | high | mitigate | `dupKey`/`claimKeys` 유지, 중복 거부 유지 케이스 |
| T-19-10 | Repudiation | 수동 주문 감사 흔적 | low | accept | DB 사본은 관찰자 기록기(19-05) 몫, relay stdout 감사 로그는 유지 — 전환 전까지 배포하지 않으므로 공백 없음(D-14 한 번에 전환) |
</threat_model>

<verification>
- relay 전체 `pnpm --filter @gh-radar/relay run test` green · typecheck 2종 0 error.
- e2e orderbook · trading-workbench green(DB 기록 0건 단언).
- spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</verification>

<success_criteria>
- relay/src 에 기록 경로 식별자 0 · 동결 테이블 쓰기 0.
- 수동 주문 즉시응답 경로(rid · 중복 가드 · 5초 타임아웃 · narrowPending)는 회귀 green.
</success_criteria>

## Artifacts this phase produces (이 plan 분)

| 종류 | 이름 |
|------|------|
| 변경 계약 | `OrderNoticeSource`(on("order") 만) · `OrderHandlerDeps`(sessions · hub · symbols · send · timeoutMs) · `WsFanoutDeps`(기록 창구 옵션 제거) |
| 삭제 | `relay/src/store/orders.ts` · `relay/tests/order-store.test.ts` · `relay/tests/helpers/fake-dma-orders.ts` · notice-status 의 교체 가능 상태 함수 |
| e2e 단언 | orderbook · trading-workbench 의 DB 기록 0건 |

<output>
Create `.planning/phases/19-account-order-journal/19-02-SUMMARY.md` when done
</output>
