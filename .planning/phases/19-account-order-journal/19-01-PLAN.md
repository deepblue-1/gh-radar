---
phase: 19-account-order-journal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260924200000_dma_journal_tables.sql
  - supabase/migrations/20260924200100_dma_journal_rpcs.sql
  - supabase/tests/dma_journal_apply.test.sql
  - supabase/tests/dma_journal_schema.test.sql
  - .planning/phases/19-account-order-journal/19-GH-TRADE-HANDOFF.md
autonomous: true
requirements: [D-02, D-05, D-06, D-09, D-11, D-12]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-05: 새 계좌 기준 테이블 4종(`dma_journal_events` · `dma_account_orders` · `dma_account_access` · `dma_journal_cursor`)이 `user_id` 컬럼 없이 계좌·주문번호·seq 를 키로 생기고, 이 plan 의 diff 에 `dma_orders` 를 건드리는 줄이 0 이다(동결 · 이관 없음)"
    - "D-12: 같은 배치를 `dma_journal_apply` 에 두 번 넣으면 두 번째 호출은 applied 0 · skipped N 이고 투영 행의 `last_seq`·`filled_qty`·커서가 그대로다 — 재생 멱등의 근거는 이벤트 PK `(gateway, journal_epoch, seq)` 삽입 성공 하나뿐이다"
    - "D-12: 이벤트 적재 · 투영 · 커서 전진이 한 트랜잭션(한 RPC 호출)이다 — 투영이 예외를 던진 이벤트는 `apply_error` 에 사유가 남고 같은 배치의 다음 이벤트와 커서는 계속 진행한다(포이즌 격리)"
    - "D-06: 같은 `dma_user_id` 를 공유하는 두 gh-radar 사용자는 `dma_journal_orders_for_user` 로 같은 id 집합을 본다 · 매핑이 없는 사용자는 0행이다 · 행 가시성은 `user_id → dma_user_id → 계좌` 조인 하나로만 정해진다"
    - "D-11: 매핑되지 않은 DMA 사용자·계좌의 통보도 `dma_account_orders` 행으로 기록되고, 뒤에 `dma_journal_sync_access` 로 매핑이 생기면 그날 앞선 행이 바로 조회된다"
    - "D-02: 주문번호 없는 행을 위한 `reject_seq` 키와 `(order_no IS NULL) <> (reject_seq IS NULL)` CHECK 가 스키마에 있다(로컬 거부 투영 규칙은 19-03)"
    - "T-19-01 · T-19-11: 네 테이블은 RLS 활성 · 정책 0 · anon/authenticated 테이블 권한 0 이고, 이 plan 의 함수 전부의 EXECUTE 는 service_role 만 가진다 — pgTAP 가 `has_function_privilege`/`has_table_privilege` 로 잠근다"
    - "D-09 · D-12 인계: gh-trade 세션이 읽을 `19-GH-TRADE-HANDOFF.md` 에 와이어 계약(요청 5 · 응답 79/80 제안) · seq 조밀성(필터 → seq 부여) · 관찰자 서버측 요청 거부 · 펌프 스로틀 · 디스크 보관 · epoch/resync 규칙이 적혀 있다"
  artifacts:
    - path: supabase/migrations/20260924200000_dma_journal_tables.sql
      provides: "저널 이벤트 · 계좌 주문 투영 · 매핑 · 커서 테이블 + RLS/REVOKE"
      contains: "dma_account_orders"
    - path: supabase/migrations/20260924200100_dma_journal_rpcs.sql
      provides: "dma_journal_apply · dma_journal_project · dma_journal_sync_access · dma_journal_orders_for_user + 상태 격자 헬퍼"
      contains: "pg_advisory_xact_lock"
    - path: supabase/tests/dma_journal_apply.test.sql
      provides: "추적 경로 pgTAP (A 투영 · 멱등 · 가시성 · 전 계좌 기록 · 포이즌 격리)"
    - path: supabase/tests/dma_journal_schema.test.sql
      provides: "제약 · RLS · 권한 pgTAP"
    - path: .planning/phases/19-account-order-journal/19-GH-TRADE-HANDOFF.md
      provides: "gh-trade Phase 23 가 읽을 와이어 계약 · 게이트웨이 요구 인계서"
  key_links:
    - from: "dma_journal_apply"
      to: "dma_journal_events PK"
      via: "INSERT ... ON CONFLICT DO NOTHING 뒤 NOT FOUND 면 투영을 건너뛴다"
      pattern: "ON CONFLICT DO NOTHING"
    - from: "dma_journal_orders_for_user"
      to: "dma_credentials.dma_user_id"
      via: "dma_account_orders JOIN dma_account_access JOIN dma_credentials WHERE c.user_id = p_user_id"
      pattern: "c.user_id = p_user_id"
  prohibitions:
    - "MUST NOT `dma_orders` 와 그 마이그레이션 · 테스트를 수정한다 (D-05 동결)"
    - "MUST NOT 새 테이블에 접근 정책을 만든다 — 서비스롤 전용 (T-19-11)"
    - "MUST NOT `created_at`/`updated_at` 에 DEFAULT now() 를 둔다 — 게이트웨이 시각이 정본 (Pitfall 5)"
    - "MUST NOT 계좌번호를 DB 에서 재정규화한다 (Pitfall 7)"
    - "MUST NOT 원격 DB 에 push 한다 — 원격 반영은 19-11 [BLOCKING] 에서 사람 승인 뒤"
    - "MUST NOT gh-trade 저장소 파일을 수정한다 — 인계서는 gh-radar phase 디렉터리에만 쓴다"
---

<objective>
관찰자 기록 경로의 **DB 쪽 뼈대를 한 경로로 먼저 증명**한다(tracer): 게이트웨이 저널 레코드 1건(접수 A) → `dma_journal_apply` → 원문 이벤트 적재 → 계좌 기준 주문 행 투영 → 커서 전진 → 매핑된 gh-radar 사용자가 `dma_journal_orders_for_user` 로 그 행을 본다. 같은 plan 에서 제약·권한을 pgTAP 로 잠그고, gh-trade 세션이 소비할 와이어 계약 인계서를 쓴다.

Purpose: D-05(새 계좌 기준 테이블 · `dma_orders` 동결)·D-06(계좌 매핑 가시성)·D-11(전 계좌 기록)·D-12(since_seq 재생이 안전하려면 적용이 멱등)의 정본이 DB 다. 체결 수량 누적처럼 멱등이 아닌 연산이 있으므로 「이벤트 삽입 성공 = 1회 적용」 이 한 트랜잭션 안에서 성립해야 한다(RESEARCH Pitfall 2). 나머지 통보 종류(E·C·M·R·로컬 거부)의 투영 규칙은 19-03 이 같은 함수에 채운다.
Output: 마이그레이션 2개(원격 미반영) · pgTAP 2파일 · `19-GH-TRADE-HANDOFF.md`.

커밋 규율(프로젝트 메모리): 커밋 메시지 한글 · Co-Authored-By 없음 · `git add` 는 파일 지정(`-A` 금지) · 커밋 직전 `git status -sb` 재확인(동시 세션 경합) · **push 금지**(이 저장소에서 push = webapp 프로덕션 배포 — 19-12 에서만).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-account-order-journal/19-CONTEXT.md

근거 인용(계획 시점 재확인 · HEAD d756385 — 46e8bea 이후 코드 diff 0):
- `supabase/migrations/20260905120200_dma_orders.sql` 1-45 머리 주석 3단(결정 근거 · 함정 · 하지 않는 것) · 47-72 `BEGIN; CREATE TABLE ... ENABLE ROW LEVEL SECURITY` 골격.
- `supabase/migrations/20260905120100_dma_credentials.sql` 50-54 — `ENABLE ROW LEVEL SECURITY` · `REVOKE ALL ... FROM PUBLIC` · `REVOKE ALL ... FROM anon, authenticated` · `GRANT SELECT, INSERT, UPDATE, DELETE ... TO service_role`. `user_id uuid PRIMARY KEY` → 사용자당 `dma_user_id` 1개, `dma_user_id` 는 UNIQUE 가 아니다(여러 사용자 공유).
- `supabase/migrations/20260914090000_stock_comovement_inputs_rpc.sql` 21-30 · 87-92 — 함수 권한 3줄 형식(시그니처 정확히).
- `supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql` 85-99 `stocks(code PK, name NOT NULL, market CHECK KOSPI/KOSDAQ, ...)` · `20260905120000_stocks_isin.sql` 부분 유니크 `idx_stocks_isin (isin) WHERE isin IS NOT NULL`.
- `supabase/tests/dma_orders_modified.test.sql` 1-40 — pgTAP 형식(`BEGIN; CREATE EXTENSION IF NOT EXISTS pgtap WITH SCHEMA extensions; SET LOCAL search_path = public, extensions; INSERT INTO auth.users ...; SELECT plan(N); ... ROLLBACK`).
- `scripts/verify-dma-orders-price-check.sh` — `--test <file>` 로 일회용 로컬 Postgres 17 컨테이너에 전 마이그레이션을 재생한 뒤 TAP 실행, `not ok` 1줄이면 non-zero. 원격 접촉 0.
- RESEARCH §Pattern D(347-398) · §Code Examples 「적용 RPC 골격」(621-654) · §Pattern A 제안 계약(233-317) · §Pattern B(319-328) · Pitfall 1·2·3·4·5·6·7·10·11·14(510-570) · Open Q1~Q8(718-734).
- 오케스트레이터 해소 사항: Q1 관찰자 비밀은 gh-trade 쪽 미추적 `config/observer.toml`(600, users.toml 처럼 수동 배치) · Q3 예약 Q-ID(`^Q[0-9]{9}$`) 접수·취소 회신을 저널에 포함 · Q4 예열 주문은 seq 부여 전 제외 · Q6 origin 미상 행은 출처 칩 생략(D-08 보충).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: [tracer] 접수 A 1건 → 이벤트 적재 → 계좌 행 투영 → 커서 → 매핑 사용자 조회 (테이블 4 · RPC · 추적 pgTAP)</name>
  <reversibility rating="costly">로컬 마이그레이션 파일 단계라 원격 반영 전까지는 커밋 되돌리기로 끝난다. D-05 의 일방향 문(새 테이블 전환 뒤 `dma_orders` 로 돌아가면 전환 이후 데이터가 없다 — discuss-phase 사용자 승인)은 19-11 의 원격 push task 가 checkpoint:decision 뒤에서 통과한다.</reversibility>
  <files>supabase/migrations/20260924200000_dma_journal_tables.sql, supabase/migrations/20260924200100_dma_journal_rpcs.sql, supabase/tests/dma_journal_apply.test.sql</files>
  <read_first>
    - supabase/migrations/20260905120200_dma_orders.sql (1-80 전문 — 머리 주석 형식과 RLS 블록)
    - supabase/migrations/20260905120100_dma_credentials.sql (1-60 전문)
    - supabase/migrations/20260914090000_stock_comovement_inputs_rpc.sql (전문 — 함수 권한 3줄)
    - supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql (85-99 stocks 정의)
    - supabase/tests/dma_orders_modified.test.sql (전문 — pgTAP 형식 · finish/ROLLBACK)
    - scripts/verify-dma-orders-price-check.sh (1-30 사용법)
    - .planning/phases/19-account-order-journal/19-RESEARCH.md (347-398 Pattern D · 621-654 적용 RPC 골격 · 510-540 Pitfall 1·2·5·6)
    - .planning/phases/19-account-order-journal/19-CONTEXT.md (D-05 · D-06 · D-08 보충 · D-11 · D-12 · Claude's Discretion 「행 모델」)
  </read_first>
  <behavior>
    - sync_access('KB', [dma-shared → 1234567801 · 위탁종합 · priority 1]) 반환 1
    - apply('KB','ep-1',[A seq 1 · 계좌 1234567801 · order_no 0000100001 · order_qty 10 · order_price 1000 · origin Manual · gw_time_ms 09:00:01 KST]) → applied 1 · skipped 0 · errors []
    - U1(dma-shared) 조회 1행 · status accepted · qty 10 · price 1000 · origin manual · created_at = gw_time · stock_code 가 stocks 조인으로 채워짐
    - U2(같은 dma-shared) 조회 id 집합 = U1 의 id 집합 (D-06) · U3(dma-solo, 매핑 없음) 조회 0행
    - 같은 배치 재호출 → applied 0 · skipped 1 · 행 last_seq 1 · events 1행 · 커서 ('ep-1', 1) 불변 (D-12)
    - 매핑 안 된 계좌 9876543201 의 A(seq 2) → dma_account_orders 에 그 행이 있음 · U1·U3 에게 안 보임 → sync_access 로 dma-solo → 9876543201 을 더한 뒤 U3 에게 1행 (D-11)
    - 한 배치 [seq 3 isin 5자(CHECK 위반) · seq 4 정상 A] → seq 3 이벤트 apply_error 채워짐 · errors 에 seq 3 · seq 4 투영됨 · 커서 last_seq 4 (포이즌 격리)
    - apply 반환 rows 원소 키에 dma_user_id 가 없다
  </behavior>
  <action>
**테이블 마이그레이션** `supabase/migrations/20260924200000_dma_journal_tables.sql` 을 `20260905120200_dma_orders.sql` 의 머리 주석 3단(결정 근거 · 함정 · 하지 않는 것) 형식으로 만든다. 결정 근거에 D-05(새 계좌 기준 테이블 · `dma_orders` 는 전환 시점부터 쓰기 중단 · 이관·복사 없음) · D-06(매핑 원천은 관찰자 로그인 응답) · D-11(전 계좌 기록) · D-12(정본은 Supabase, 게이트웨이 디스크는 relay 부재를 넘기는 버퍼)를 적는다. 함정에 Pitfall 5(재생 시각이 주문 시각이 된다 → 시각 컬럼 DEFAULT 금지) · Pitfall 6(빈 주문번호 upsert 가 서로 다른 거부를 한 행에 겹친다 → `reject_seq`) · Pitfall 7(계좌번호는 게이트웨이 정규화값 그대로, 재정규화 금지)을 적는다. BEGIN/COMMIT 한 트랜잭션에 네 테이블:

① `public.dma_journal_events` — 저널 레코드 원문 1건 = 1행. 컬럼 `gateway text NOT NULL` · `journal_epoch text NOT NULL` · `seq bigint NOT NULL CHECK (seq > 0)` · `trade_date date NOT NULL` · `gw_time timestamptz NOT NULL` · `dma_user_id text NOT NULL DEFAULT ''` · `account_no text NOT NULL` · `isin text NOT NULL` · `side text NOT NULL DEFAULT ''` · `side_trusted boolean NOT NULL DEFAULT false` · `order_no text NOT NULL DEFAULT ''` · `org_order_no text NOT NULL DEFAULT ''` · `notice_type text NOT NULL DEFAULT ''` · `request_kind text NOT NULL DEFAULT ''` · `requester text NOT NULL DEFAULT ''` · `origin text NOT NULL DEFAULT ''` · `exchange text NOT NULL DEFAULT ''` · `board text NOT NULL DEFAULT ''` · `order_price integer NOT NULL DEFAULT 0` · `order_qty integer NOT NULL DEFAULT 0` · `exec_price integer NOT NULL DEFAULT 0` · `exec_qty integer NOT NULL DEFAULT 0` · `result_code integer NOT NULL DEFAULT 0` · `message text NOT NULL DEFAULT ''` · `local_reject boolean NOT NULL DEFAULT false` · `applied_at timestamptz NOT NULL DEFAULT now()` · `apply_error text` · `PRIMARY KEY (gateway, journal_epoch, seq)`. 원문 보존이라 값 CHECK 를 걸지 않는다(여기서 거부하면 멱등 게이트 자체가 사라진다는 주석). 인덱스 `idx_dma_journal_events_account_day (gateway, account_no, trade_date)`.

② `public.dma_account_orders` — 주문 1건 = 1행 투영. `id uuid PRIMARY KEY DEFAULT gen_random_uuid()` · `gateway text NOT NULL` · `trade_date date NOT NULL` · `account_no text NOT NULL` · `order_no text` · `journal_epoch text` · `reject_seq bigint` · `isin text NOT NULL CHECK (length(isin) = 12)` · `exchange text NOT NULL CHECK (exchange IN ('KRX','NXT'))` · `board text` · `side text CHECK (side IN ('B','S'))` · `order_type text NOT NULL CHECK (order_type IN ('N','M','C'))` · `org_order_no text` · `qty integer CHECK (qty > 0)` · `price integer CHECK (price >= 0)` · `filled_qty integer NOT NULL DEFAULT 0 CHECK (filled_qty >= 0)` · `modified_qty integer NOT NULL DEFAULT 0 CHECK (modified_qty >= 0)` · `status text NOT NULL CHECK (status IN ('accepted','partially_filled','filled','cancelled','rejected','modified'))` · `result_code integer` · `notice_type text` · `message text` · `origin text CHECK (origin IN ('manual','limit_chaser','vi'))` · `requester text` · `request_kind text` · `dma_user_id text` · `first_seq bigint NOT NULL` · `last_seq bigint NOT NULL` · `created_at timestamptz NOT NULL` · `updated_at timestamptz NOT NULL` · `CONSTRAINT dma_account_orders_key_xor CHECK ((order_no IS NULL) <> (reject_seq IS NULL))` · `CONSTRAINT dma_account_orders_reject_epoch CHECK (reject_seq IS NULL OR journal_epoch IS NOT NULL)`. `order_no` 에 형식 CHECK 를 걸지 않는다(예약 Q-ID `^Q[0-9]{9}$` 도 같은 칸 — 오케스트레이터 Q3). `dma_user_id` 는 감사용이며 조회·푸시 컬럼에 싣지 않는다(D-08 — 주문자 표시 안 함)는 주석. 부분 유니크 `uq_dma_account_orders_order_no (gateway, trade_date, account_no, order_no) WHERE order_no IS NOT NULL` · `uq_dma_account_orders_reject (gateway, journal_epoch, reject_seq) WHERE reject_seq IS NOT NULL` · 조회 인덱스 `idx_dma_account_orders_account_day (gateway, account_no, trade_date, created_at DESC)`.

③ `public.dma_account_access` — `gateway text` · `dma_user_id text` · `account_no text` · `account_name text NOT NULL DEFAULT ''` · `priority integer NOT NULL DEFAULT 0` · `synced_at timestamptz NOT NULL DEFAULT now()` · `PRIMARY KEY (gateway, dma_user_id, account_no)` · 인덱스 `idx_dma_account_access_account (gateway, account_no)`. 비밀번호 칸을 두지 않는다.

④ `public.dma_journal_cursor` — `gateway text PRIMARY KEY` · `journal_epoch text NOT NULL` · `last_seq bigint NOT NULL CHECK (last_seq >= 0)` · `updated_at timestamptz NOT NULL DEFAULT now()`.

네 테이블 모두 `dma_credentials` 50-54 와 같은 4줄(RLS 활성 · PUBLIC REVOKE · anon/authenticated 명시 REVOKE · service_role GRANT)을 붙이고 정책은 만들지 않는다(메모리 「RPC 는 REVOKE anon/authenticated 명시」).

**RPC 마이그레이션** `supabase/migrations/20260924200100_dma_journal_rpcs.sql` — 전부 `SECURITY INVOKER` · `SET search_path = public, pg_temp`:
⑤ 상태 격자 헬퍼 `public.dma_journal_status_rank(p_status text) RETURNS integer` IMMUTABLE(accepted 1 · partially_filled 2 · filled/cancelled/rejected/modified 3 · 그 밖 0)와 `public.dma_journal_next_status(p_cur text, p_next text) RETURNS text` IMMUTABLE — `p_cur` 가 NULL 이면 `p_next`, `p_cur` 가 종결(filled·cancelled·rejected·modified)이면 `p_cur`, rank(cur) 가 rank(next) 이하면 `p_next`, 아니면 `p_cur`. relay `order/notice-status.ts` 의 단조 격자와 같은 축이라는 주석(상태는 뒤로 가지 않는다 — CONTEXT 「행 모델」).
⑥ `public.dma_journal_project(p_gateway text, p_epoch text, p_ev jsonb) RETURNS uuid[]` — 이벤트 1건을 투영하고 건드린 행 id 배열을 돌려준다. 이 task 는 **`notice_type` 'A' · 주문번호 있음** 갈래를 완성한다: 키 `(gateway, trade_date, account_no, order_no)` 로 행이 없으면 INSERT — `order_type` = request_kind 사상(New→N · Modify→M · Cancel→C · 빈 값→N) · `qty` = NULLIF(order_qty, 0) · `price` = order_price · `org_order_no` = NULLIF(org_order_no, '') · `side` = side_trusted 이고 값이 B/S 면 그 값 아니면 NULL · `status` 'accepted' · `origin` 사상(Manual→manual · LimitChaser→limit_chaser · VITrigger→vi · 그 밖·빈 값→NULL — D-08 보충, 「수동」 과 「미상」 을 섞지 않는다) · `requester`/`request_kind`/`board`/`dma_user_id` = NULLIF(…, '') · `exchange` = 'NXT' 면 NXT 아니면 KRX · `first_seq = last_seq = seq` · `created_at = updated_at = to_timestamp(gw_time_ms / 1000.0)`. 행이 있으면 `status = dma_journal_next_status(status, 'accepted')` · `last_seq = GREATEST(last_seq, seq)` · `notice_type`/`result_code`/`message` 최신화 · `updated_at = GREATEST(updated_at, gw_time)` · `created_at = LEAST(created_at, gw_time)` · 비어 있는 `qty`/`price`/`org_order_no`/`origin`/`side`/`dma_user_id` 만 COALESCE 로 채운다. 체결이 먼저 와 `filled_qty > 0` 인 행에 A 가 qty 를 채우면 `filled_qty + modified_qty >= qty` 로 filled/partially_filled 를 격자로 재판정한다. E·C·M·R·로컬 거부 갈래는 19-03 이 같은 함수에 채운다 — 이 task 에서 그 통보들은 `RAISE EXCEPTION` 으로 떨어져 적용 RPC 의 포이즌 격리가 `apply_error` 에 사유를 남긴다(조용히 삼키지 않는다).
⑦ `public.dma_journal_apply(p_gateway text, p_epoch text, p_events jsonb) RETURNS jsonb` — RESEARCH 「적용 RPC 골격」 순서 그대로: `PERFORM pg_advisory_xact_lock(hashtext('dma_journal:' || p_gateway))` → `jsonb_array_elements` 를 seq 오름차순으로 순회 → `dma_journal_events` INSERT(키 snake_case: seq · trade_date · gw_time_ms · dma_user_id · account_no · isin · side · side_trusted · order_no · org_order_no · notice_type · request_kind · requester · origin · exchange · board · order_price · order_qty · exec_price · exec_qty · result_code · message · local_reject — 이 키 목록이 relay 기록기와의 계약이라는 주석) `ON CONFLICT DO NOTHING` → `IF NOT FOUND` 면 skipped+1 후 다음(재생 멱등의 유일한 근거) → 서브블록 `BEGIN ... dma_journal_project ... EXCEPTION WHEN OTHERS THEN UPDATE dma_journal_events SET apply_error = SQLERRM WHERE (gateway, journal_epoch, seq) = ...; errors 에 {seq, error} 추가 END` → 커서 `INSERT ... ON CONFLICT (gateway) DO UPDATE` (같은 epoch 면 `GREATEST(last_seq)`, 다르면 교체 — RESEARCH 골격 그대로) 를 **같은 트랜잭션**에서 → 반환 `jsonb_build_object('applied', …, 'skipped', …, 'errors', …, 'last_seq', …, 'rows', …)`. `rows` 는 건드린 id 들을 중복 없이 모아 ⑨ 와 **같은 공개 컬럼 목록**(`dma_user_id` 제외 · `stocks` LEFT JOIN 으로 `stock_code`)으로 `jsonb_agg`. 빈 배열 입력은 커서를 건드리지 않는다.
⑧ `public.dma_journal_sync_access(p_gateway text, p_rows jsonb) RETURNS integer` — 같은 advisory lock → `DELETE FROM dma_account_access WHERE gateway = p_gateway` → `p_rows`(`[{dma_user_id, account_no, name, priority}]`) INSERT `ON CONFLICT DO NOTHING`(users.toml 중복 흡수) → 넣은 행 수. 한 호출 = 원자 교체(D-06 — users.toml 은 핫리로드가 없어 관찰자 로그인 시점 스냅샷이 정본).
⑨ `public.dma_journal_orders_for_user(p_user_id uuid, p_trade_date date) RETURNS TABLE(id uuid, trade_date date, account_no text, isin text, stock_code text, exchange text, board text, side text, order_type text, org_order_no text, qty integer, price integer, order_no text, filled_qty integer, modified_qty integer, status text, result_code integer, notice_type text, message text, origin text, requester text, request_kind text, last_seq bigint, created_at timestamptz, updated_at timestamptz)` · `LANGUAGE sql STABLE` — `dma_account_orders o JOIN dma_account_access a ON (a.gateway, a.account_no) = (o.gateway, o.account_no) JOIN dma_credentials c ON c.dma_user_id = a.dma_user_id LEFT JOIN stocks s ON s.isin = o.isin WHERE c.user_id = p_user_id AND o.trade_date = p_trade_date ORDER BY o.created_at DESC, o.last_seq DESC`. `dma_user_id` 를 반환하지 않는다. `dma_credentials.user_id` 가 PK 이고 access PK 가 `(gateway, dma_user_id, account_no)` 라 행 중복이 생기지 않는다는 주석.
함수 전부(⑤ 헬퍼 2 · ⑥ · ⑦ · ⑧ · ⑨)에 `REVOKE EXECUTE ON FUNCTION <시그니처> FROM PUBLIC;` · `REVOKE EXECUTE ... FROM anon, authenticated;` · `GRANT EXECUTE ... TO service_role;` 3줄을 **시그니처 정확히** 반복한다(Pitfall 10 — authenticated 에 열리면 PostgREST 로 남의 `p_user_id` 를 넣어 조회한다).

**추적 pgTAP** `supabase/tests/dma_journal_apply.test.sql` — `dma_orders_modified.test.sql` 형식(한 트랜잭션 · 끝에서 `finish()` 후 ROLLBACK). 픽스처: `auth.users` 3명(U1·U2·U3 고정 uuid) · `dma_credentials` U1·U2 → `dma_user_id` 'dma-shared', U3 → 'dma-solo'(`dma_password_enc` 는 임의 문자열) · `stocks` 1행(code 005930 · name 삼성전자 · market KOSPI · isin KR7005930003). 단언은 `<behavior>` 8항목을 전부 담고, 설명 문자열에 (seq, 계좌 말미, 기대값) 튜플을 적어 `not ok` 줄만 보고 어느 경우인지 알 수 있게 한다. 파일 머리 주석에 실행 명령과 「19-03 이 E·C·M·R·로컬 거부 단언을 이 파일에 더한다」 를 적는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash scripts/verify-dma-orders-price-check.sh --test supabase/tests/dma_journal_apply.test.sql</automated>
    <fails_when>non-zero exit, 또는 출력에 "not ok" 줄, 또는 "# Looks like you planned" 줄(계획 수 불일치), 또는 "ERROR: 마이그레이션 재생 실패"</fails_when>
  </verify>
  <acceptance_criteria>
    - 주석을 뺀 테이블 마이그레이션에 `dma_` 접두 없는 `user_id` 컬럼이 없다 — `grep -v '^ *--' supabase/migrations/20260924200000_dma_journal_tables.sql | grep -E "(^|[^_a-z])user_id" | grep -v dma_user_id` 출력이 빈 줄 0개(== 0)
    - `grep -v '^ *--' supabase/migrations/20260924200000_dma_journal_tables.sql | grep -c "ENABLE ROW LEVEL SECURITY"` >= 4 · 같은 필터로 `grep -c "FROM anon, authenticated"` >= 4 (테이블 4종 각각 — `grep -n` 으로 네 테이블 이름 옆 줄인지 읽는다)
    - `grep -v '^ *--' supabase/migrations/20260924200100_dma_journal_rpcs.sql | grep -c "FROM anon, authenticated"` >= 6 · 같은 필터로 `grep -c "TO service_role"` >= 6 (함수 6종 각각 — `grep -n` 으로 시그니처별 3줄을 읽는다)
    - `grep -v '^ *--' supabase/migrations/20260924200100_dma_journal_rpcs.sql | grep -c "pg_advisory_xact_lock"` ≥ 2 · 같은 필터로 `grep -c "ON CONFLICT DO NOTHING"` ≥ 1
    - 동결 파일 무수정 — `git status --porcelain -- 'supabase/migrations/*dma_orders*' 'supabase/tests/dma_orders*'` 출력 없음
    - 위 pgTAP 실행 출력에 `ok` 줄이 `<behavior>` 8항목을 모두 덮는 수만큼 있고 `not ok` 0
  </acceptance_criteria>
  <done>접수 통보 1건이 적재 · 투영 · 커서까지 한 호출로 끝나고, 같은 DMA 계정을 공유하는 두 사용자가 같은 행을, 매핑 없는 사용자는 0행을 본다. 재생은 아무것도 바꾸지 않고 포이즌 이벤트는 배치를 막지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 스키마 · 권한 pgTAP — 제약 · RLS · 정책 0 · 테이블/함수 권한 잠금</name>
  <files>supabase/tests/dma_journal_schema.test.sql</files>
  <read_first>
    - supabase/migrations/20260924200000_dma_journal_tables.sql (Task 1 결과)
    - supabase/migrations/20260924200100_dma_journal_rpcs.sql (Task 1 결과 — 함수 시그니처 원문)
    - supabase/tests/dma_orders_price_check.test.sql (throws_ok · CHECK 위반 단언 형식)
    - .planning/phases/19-account-order-journal/19-RESEARCH.md (548-554 Pitfall 10 · 801-824 Security Domain)
  </read_first>
  <behavior>
    - has_table 4종 · 네 테이블 `pg_class.relrowsecurity` = true · `pg_policies` 에 네 테이블 행 0
    - anon · authenticated 각각 네 테이블에 SELECT/INSERT/UPDATE/DELETE 권한 false · service_role 은 SELECT/INSERT/UPDATE/DELETE true
    - 함수 6종 각각 anon · authenticated EXECUTE false · service_role EXECUTE true (시그니처 문자열로 `has_function_privilege`)
    - `dma_account_orders`: order_no · reject_seq 둘 다 NULL → throws(키 xor) · 둘 다 채움 → throws · status 'requested' · 'timeout' → throws · exchange 'KOSPI' → throws · side 'X' → throws · origin 'unknown' → throws · isin 5자 → throws · qty 0 → throws · 같은 (gateway, trade_date, account_no, order_no) 두 번 → throws(유니크) · 같은 (gateway, journal_epoch, reject_seq) 두 번 → throws · reject_seq 있고 journal_epoch NULL → throws
    - 정상 행(order_no 행 1 · reject_seq 행 1)은 lives_ok
    - `dma_journal_cursor.last_seq` 음수 → throws
  </behavior>
  <action>
`supabase/tests/dma_journal_schema.test.sql` 를 `dma_orders_price_check.test.sql` 의 throws_ok 형식으로 만든다(한 트랜잭션 · ROLLBACK). 권한 단언은 `SELECT is(has_table_privilege('anon', 'public.dma_account_orders', 'SELECT'), false, '...')` 형태로 role × 테이블 × 권한을 전부 적고, 함수 단언은 Task 1 마이그레이션의 **시그니처 원문**을 그대로 옮긴 문자열로 `has_function_privilege('authenticated', 'public.dma_journal_orders_for_user(uuid, date)', 'EXECUTE')` 처럼 적는다(T-19-01 · Pitfall 10 — 조회 RPC 가 authenticated 에 열리면 IDOR). RLS 는 `pg_class` 의 `relrowsecurity`, 정책 0 은 `pg_policies` 에서 `tablename IN (...)` 개수 0 으로 단언한다. 제약 단언 설명에는 위반 튜플을 그대로 적는다. 머리 주석에 이 파일이 T-19-01 · T-19-11 의 회귀 정본이며 실행 명령이 `bash scripts/verify-dma-orders-price-check.sh --test supabase/tests/dma_journal_schema.test.sql` 이라고 적는다. 테스트가 실패하면 테스트가 아니라 Task 1 마이그레이션을 고친다(권한이 새면 원격 반영 전에 막아야 한다).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash scripts/verify-dma-orders-price-check.sh --test supabase/tests/dma_journal_schema.test.sql</automated>
    <fails_when>non-zero exit, 또는 출력에 "not ok" 줄, 또는 "# Looks like you planned" 줄</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "has_function_privilege" supabase/tests/dma_journal_schema.test.sql` ≥ 18 (함수 6 × role 3)
    - `grep -c "has_table_privilege" supabase/tests/dma_journal_schema.test.sql` ≥ 32 (테이블 4 × anon/authenticated 2 × 권한 4)
    - `grep -c "pg_policies" supabase/tests/dma_journal_schema.test.sql` ≥ 1 · `grep -c "relrowsecurity" supabase/tests/dma_journal_schema.test.sql` ≥ 1
    - 위 pgTAP 실행 `not ok` 0
  </acceptance_criteria>
  <done>새 테이블·함수가 브라우저 역할(anon/authenticated)에게 닫혀 있고, 행 모델의 제약(키 xor · 상태 6종 · 거래소 · 방향 · 출처 · ISIN · 유니크)이 회귀 테스트로 잠겼다.</done>
</task>

<task type="auto">
  <name>Task 3: gh-trade 인계서 — 관찰자 와이어 계약 · 게이트웨이 요구 · G1/G2 신호 (19-GH-TRADE-HANDOFF.md)</name>
  <files>.planning/phases/19-account-order-journal/19-GH-TRADE-HANDOFF.md</files>
  <read_first>
    - .planning/phases/19-account-order-journal/19-RESEARCH.md (233-345 Pattern A·B·C · 510-570 Pitfall 1·3·4·8·9·11·14 · 669-690 배포 순서 · 718-734 Open Questions)
    - .planning/phases/19-account-order-journal/19-CONTEXT.md (D-02 · D-06 · D-09 · D-10 · D-12 · D-13 · Claude's Discretion 「기록 통보 메시지」 「seq」)
    - supabase/migrations/20260924200100_dma_journal_rpcs.sql (Task 1 결과 — 이벤트 키 목록 · 투영이 기대하는 값 원문)
    - docs/relay-operations.md (1-20 스키마 재동기화 규칙)
  </read_first>
  <action>
gh-trade 세션(gh-trade 저장소 자체 GSD · 신규 Phase 23 권장)이 discuss-phase 입력으로 그대로 읽을 인계서를 `.planning/phases/19-account-order-journal/19-GH-TRADE-HANDOFF.md` 에 쓴다. gh-trade 파일은 수정하지 않는다. 절 구성:
(1) **목적과 경계** — 배경(debug mobile-bg-resume-gaps 1번 · 연결 0 세션 51 폐기) · gh-radar 19 가 이미 결정한 것(D-01~D-14 요약 중 게이트웨이 몫: D-02 · D-06 · D-09 · D-10 · D-12 · D-13) · gh-radar 가 하지 않는 것(gh-trade 코드 수정).
(2) **와이어 계약 제안** — RESEARCH Pattern A 를 옮기되 확정 권한은 gh-trade 에 있음을 명시: `ObserverLoginReq = 5`(요청 5~9 비어 있음) · `ObserverLoginResp = 79` · `JournalBatch = 80`(응답 79 이상 비어 있음) · 재사용 금지 번호(12·13·22·23·62·63·30·31·70) · Envelope 끝 append 규칙 · 테이블 필드 전체(`ObserverLoginReq{secret, since_seq, journal_epoch, client}` · `ObserverAccount{dma_user_id, account_no, name, priority}` · `ObserverLoginResp{success, message, broker, journal_epoch, head_seq, oldest_seq, resync, accounts}` · `JournalRecord` 23필드 · `JournalBatch{records, head_seq, caught_up}`) · `ulong` 이 relay 생성 코드에서 bigint 라는 점. `JournalRecord` 필드마다 **Task 1 의 이벤트 JSON 키**와의 대응표를 둔다(relay 기록기가 그대로 옮긴다).
(3) **의미 규칙(relay/DB 가 기대하는 것)** — seq 는 게이트웨이 전역 단조·조밀(모든 레코드가 seq 를 소비, 필터링은 seq 부여 **전** — Pitfall 11) · 재시작·자정을 넘어 이어짐 · `journal_epoch` 는 저널 저장소 신원(생성 시 1회) · `resync=true` 조건(epoch 불일치 · since_seq 가 oldest−1 보다 작음 · since_seq 가 head 보다 큼)이면 oldest 부터 · since_seq **초과**부터 전송 · 예열 주문은 저널 제외(Q4) · 예약 Q-ID 접수(A)·취소(C) 회신은 저널 포함(Q3 — order_no = Q-ID) · 로컬 거부(D-02) 발행 지점 4곳(WireExecutionCallback 의 NotifyLocalReject · ProcessDirectOrderReq 즉시 실패 · 예약 회신 · 전략 사전 거부)과 `local_reject=true` · 빈 order_no · C/M 의 side 는 원주문 메타로 채울 수 있으면 채우고 `side_trusted` 로 알림 · origin 원문 값(`Manual`/`LimitChaser`/`VITrigger`/빈 값 — 재시작 뒤 termId=0 이면 빈 값이 정상이며 gh-radar 는 칩을 생략) · `order_price`/`order_qty` 와 `exec_price`/`exec_qty` 를 섞지 않음(51 의 useExecuted 와 다름) · `trade_date` 는 KST 레코드 발생일 · `gw_time_ms` 는 게이트웨이 벽시계 · `message` 는 51 과 같은 조립(파싱 금지 전제) · 계좌번호는 `NormalizeAccountNo` 결과 그대로(Pitfall 7).
(4) **관찰자 연결 요구(D-09 서버측 강제)** — `[observer]` 비밀은 users.toml 과 분리된 미추적 `config/observer.toml`(chmod 600 · users.toml 처럼 수동 배치 — Q1) · 상수시간 비교 · 거부는 단일 문구(사유는 게이트웨이 로그만 — 17 D-04 동형) · 관찰자 연결은 어떤 Session 에도 붙지 않음 · `ProcessPacket` 단일 관문에서 5/4 외 요청 전부 거부(주문·전략·시세·계좌) · 76 등 브로드캐스트 대상에서 제외(Pitfall 8) · 유휴 스윕 대상 포함(relay 는 30초 LivePing) · 복수 관찰자 허용(relay 재시작 직후 옛 연결 공존, 상한 2~4) · 계좌 0건 기동 거부 규칙과 무관.
(5) **펌프·보관(D-12)** — 로그인 시 일괄 enqueue 금지(송신 큐 1,024프레임 초과 시 연결 절단 — Pitfall 1) · 연결별 nextSeq 펌프 · 큐 깊이 스로틀 · 배치 ≤100건/≈32KB · 따라잡으면 append 마다 1건 · 디스크 저장(OrderRecorder 선례 · 날짜별 파일 · seq.state · epoch 파일 · 보관 일수는 gh-trade 재량) · 핫패스(로컬 거부 = MarketFeed 스레드)에 락·파일 I/O 금지(Pitfall 4 — MPSC push 만).
(6) **relay 쪽 수신 계약(참고)** — relay 는 로그인 거부 시 재접속을 멈춘다(D-13) · seq 갭이면 연결을 끊고 since_seq = 마지막 수신 seq 로 다시 붙는다 · 큐 5,000건 초과 시 스스로 끊는다 · 매핑은 로그인 응답 스냅샷으로 원자 교체한다.
(7) **동기화·배포 게이트** — G1 계약 고정 = gh-trade `.fbs` 커밋 해시 + `cd /Users/alex/repos/gh-trade/server && RELAY=/Users/alex/repos/gh-radar/relay ./scripts/sync-relay-schema.sh` 가 gh-radar 생성물을 만든다(생성물 커밋은 gh-radar 19-09) · C# `sync-client-schema.sh` 와 `expected-vtable.txt --update-expected` 는 gh-trade 몫 · G2 배포 준비 = 빌드 게이트(cloud-verify) 통과 + observer.toml 배치 절차 합의 + **동반 배포 목록 확인**(Pitfall 14 — gh-trade STATE 의 120 미배포 quick 들) · 게이트웨이 재시작은 20:00 이후만(전략 무인 복원 동반 — D-14).
(8) **gh-radar 쪽 대응 plan 표** — 19-01/19-03(DB) · 19-05(기록기) · 19-07(관찰자) · 19-09(G1 코덱) · 19-11/19-12(전환 창) · 19-13(첫 거래일 대조).
문서 머리에 작성일(2026-09-24)과 「제안 — 확정은 gh-trade Phase 23 discuss」 표지를 둔다. 비밀 값·실계좌 번호·게이트웨이 IP 는 적지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && f=.planning/phases/19-account-order-journal/19-GH-TRADE-HANDOFF.md && test -s "$f" && grep -c -E "ObserverLoginReq|ObserverLoginResp|JournalBatch|since_seq|journal_epoch|local_reject|observer.toml|sync-relay-schema.sh" "$f"</automated>
    <fails_when>non-zero exit(파일 없음·빈 파일), 또는 출력 숫자가 8 미만</fails_when>
  </verify>
  <acceptance_criteria>
    - 문서에 절 (1)~(8) 제목이 모두 있다 — `grep -cE "^## " .planning/phases/19-account-order-journal/19-GH-TRADE-HANDOFF.md` ≥ 8
    - `JournalRecord` 필드 ↔ 이벤트 JSON 키 대응표에 Task 1 의 23개 키(seq · trade_date · gw_time_ms · dma_user_id · account_no · isin · side · side_trusted · order_no · org_order_no · notice_type · request_kind · requester · origin · exchange · board · order_price · order_qty · exec_price · exec_qty · result_code · message · local_reject)가 전부 나온다
    - `grep -cE "10\.41\.|secret *= *\"" .planning/phases/19-account-order-journal/19-GH-TRADE-HANDOFF.md` = 0 (IP·비밀 값 없음)
    - gh-trade 저장소 `git -C /Users/alex/repos/gh-trade status --porcelain` 출력이 이 task 시작 전과 같다(작업 전 출력을 SUMMARY 에 적고 끝에서 대조)
  </acceptance_criteria>
  <done>gh-trade 세션이 이 문서 하나로 관찰자 로그인 · 저널 레코드 · 펌프 · 보관 · 서버측 거부 요구를 읽고 Phase 23 을 시작할 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저(anon/authenticated) → PostgREST | 로그인 사용자가 Supabase 에 직접 닿는 표면. 새 테이블·함수가 여기서 열리면 IDOR |
| relay(service_role) → Supabase RPC | 저널 레코드가 DB 로 들어오는 유일한 쓰기 경로 |
| gh-trade 게이트웨이 → relay → DB | 레코드 필드는 게이트웨이가 만든 값 — 형식 이상이 투영을 막지 않아야 한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Information Disclosure (IDOR) | `dma_journal_orders_for_user` EXECUTE | high | mitigate | PUBLIC · anon · authenticated 명시 REVOKE + service_role GRANT, Task 2 `has_function_privilege` 단언 |
| T-19-11 | Information Disclosure | 새 테이블 4종 | high | mitigate | RLS 활성 · 정책 0 · 명시 REVOKE, Task 2 `has_table_privilege`·`pg_policies` 단언 |
| T-19-06 | Tampering | 재생 이중 적용(`filled_qty` 두 배) | high | mitigate | 이벤트 PK INSERT 성공 시에만 투영 · 커서 같은 트랜잭션, Task 1 재호출 단언 |
| T-19-05 | Tampering | 두 writer 동시 적용 | medium | mitigate | `pg_advisory_xact_lock(hashtext('dma_journal:'||gateway))` + PK 게이트 |
| T-19-15 | Denial of Service | 포이즌 이벤트가 커서를 영구 정지 | medium | mitigate | 이벤트별 EXCEPTION 서브블록 → `apply_error`, Task 1 포이즌 단언 |
| T-19-08 | Information Disclosure | `dma_user_id` 가 조회 결과로 나감 | medium | mitigate | 공개 컬럼 목록에서 제외, Task 1 rows 키 단언 |
| T-19-10 | Repudiation | 통보 감사 기록 | low | mitigate | 원문 이벤트 행 보존(`apply_error` 포함), `dma_orders` 동결(삭제 없음) |
| T-19-13 | Tampering | 형식 이상 레코드(ISIN·계좌) | low | mitigate | 투영 CHECK + 포이즌 격리 — 원문은 이벤트 행에 남는다 |
| T-19-04 | Elevation of Privilege | 관찰자 연결로 주문·전략 주입 | high | transfer | 게이트웨이 서버측 강제(D-09)를 인계서 절 (4)에 요구로 명시 — gh-trade Phase 23 소관, 19-11 G2 에서 확인 |
</threat_model>

<verification>
- `bash scripts/verify-dma-orders-price-check.sh --test supabase/tests/dma_journal_apply.test.sql` · `--test supabase/tests/dma_journal_schema.test.sql` 둘 다 `not ok` 0 · non-zero exit 없음.
- `git diff --name-only HEAD -- supabase/` 에 `dma_orders` 파일 없음.
- spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip) — SPEC.md 없음, CONTEXT D-01~D-14 가 요구사항 집합.
</verification>

<success_criteria>
- 저널 테이블 4종 · 함수 6종이 로컬 컨테이너에서 전 마이그레이션 재생과 함께 적용되고 pgTAP 2파일이 green.
- 멱등 · 가시성(공유 계정 동일 · 미매핑 0행) · 전 계좌 기록 · 포이즌 격리가 단언으로 잠겼다.
- `19-GH-TRADE-HANDOFF.md` 가 gh-trade 세션의 입력으로 완결.
</success_criteria>

## Artifacts this phase produces (이 plan 분)

| 종류 | 이름 |
|------|------|
| 테이블 | `public.dma_journal_events` · `public.dma_account_orders` · `public.dma_account_access` · `public.dma_journal_cursor` |
| 제약·인덱스 | `dma_account_orders_key_xor` · `dma_account_orders_reject_epoch` · `uq_dma_account_orders_order_no` · `uq_dma_account_orders_reject` · `idx_dma_account_orders_account_day` · `idx_dma_journal_events_account_day` · `idx_dma_account_access_account` |
| 함수(RPC) | `dma_journal_status_rank(text)` · `dma_journal_next_status(text, text)` · `dma_journal_project(text, text, jsonb)` · `dma_journal_apply(text, text, jsonb)` · `dma_journal_sync_access(text, jsonb)` · `dma_journal_orders_for_user(uuid, date)` |
| 계약 | 적용 이벤트 JSON 키 23종(seq … local_reject) · 공개 행 컬럼 25종(id … updated_at, `dma_user_id` 제외) |
| 파일 | `supabase/migrations/20260924200000_dma_journal_tables.sql` · `supabase/migrations/20260924200100_dma_journal_rpcs.sql` · `supabase/tests/dma_journal_apply.test.sql` · `supabase/tests/dma_journal_schema.test.sql` · `.planning/phases/19-account-order-journal/19-GH-TRADE-HANDOFF.md` |

<output>
Create `.planning/phases/19-account-order-journal/19-01-SUMMARY.md` when done
</output>
