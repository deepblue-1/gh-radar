---
phase: 19-account-order-journal
plan: 08
type: execute
wave: 4
depends_on: ["19-06"]
files_modified:
  - webapp/src/components/trading/origin-tag.tsx
  - webapp/src/components/orderbook/account-panel.tsx
  - webapp/src/lib/orders-api.ts
  - webapp/src/components/trading/today-orders-card.tsx
  - webapp/src/components/trading/__tests__/today-orders-card.test.tsx
  - webapp/e2e/specs/me.spec.ts
autonomous: true
requirements: [D-04, D-07, D-08]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-07: 「오늘 주문」 카드는 B′ 계좌별 묶음이다 — 계좌마다 소제목(계좌 · 번호 전체 · 상품명 · N건) 아래 목록을 반복하고, 순서는 relay 계좌 목록 순 · 목록에 없는 계좌는 번호만으로 뒤에 붙는다 · 행 없는 계좌는 묶음을 그리지 않는다"
    - "D-07: 좁은 폭 카드 행 ↔ 넓은 폭(≥1280) 표 전환 규칙은 현행 뷰포트 브레이크포인트 그대로이고(상따 컨테이너 쿼리 규칙 비적용), 390px 에서 ②줄이 넘치면 주문번호가 다음 줄로 내려가 잘리지 않는다"
    - "D-08: 출처를 아는 모든 행에 출처 칩(상따 · VI · 수동)이 붙고, origin 미상(null) 행은 칩이 없다(D-08 보충) · 칩은 account-panel 출처 태그와 같은 클래스(새 색 없음)"
    - "D-08: NXT 행에만 거래소 태그(ExchangeTag 채움형)가 종목 코드 옆에 붙고 KRX 행에는 없다 · 로컬 거부 행의 주문번호 자리는 「—」 · 구분 칸의 「· 수동」 꼬리는 없다 · 주문자(DMA 사용자) 표시는 없다"
    - "D-04 (a): `journal.state` 가 delayed 면 카드 제목 옆 「기록 지연」 배지(role=status · 진행 점 · reduced-motion 이면 정지)와 한 줄 안내 「기록 지연 — 복구되면 채워집니다. HH:MM:SS 이후 주문이 아직 안 보일 수 있어요.」 가 뜨고, 이미 기록된 행은 흐리게 하지 않는다"
    - "헤더 「N건」 은 묶기 전 행 수다(목업 주석 · 현행 규칙)"
  artifacts:
    - path: webapp/src/components/trading/origin-tag.tsx
      provides: "OriginTag(상따·VI·수동) · originTagOf(origin)"
      exports: ["OriginTag", "originTagOf", "OriginTagLabel"]
    - path: webapp/src/components/trading/today-orders-card.tsx
      provides: "B′ 계좌별 묶음 · 출처 칩 · NXT 태그 · 기록 지연 표식"
      contains: "today-orders-delayed"
    - path: webapp/src/lib/orders-api.ts
      provides: "groupJournalRowsByAccount(rows, accounts)"
  key_links:
    - from: "TodayOrdersCard"
      to: "groupJournalRowsByAccount(merged, relay.accounts)"
      via: "계좌 순서 = RelayAccount 목록"
      pattern: "groupJournalRowsByAccount"
    - from: "TodayOrdersCard"
      to: "relay.journalState"
      via: "s === \"delayed\" → 배지 + 안내"
      pattern: "journalState"
    - from: "account-panel.tsx"
      to: "components/trading/origin-tag.tsx"
      via: "import { OriginTag } — 비공개 함수 추출"
      pattern: "origin-tag"
  prohibitions:
    - "MUST NOT 새 색·새 토큰을 만든다 — 칩·배지는 기존 토큰(muted · muted-fg · accent · border)만"
    - "MUST NOT 목업 A · C 요소(줄마다 계좌 라벨 · 다른 단말 칩)를 넣는다 (D-07 미채택 · Deferred)"
    - "MUST NOT 컨테이너 쿼리로 바꾼다 — My page 카드는 뷰포트 max-[1279px] 규칙 유지"
    - "MUST NOT push 한다 — 19-12"
---

<objective>
「오늘 주문」 카드를 채택 목업 **B′**(`19-today-orders-mockup.html` 최상단 「B′ — 채택안」)대로 그린다. tracer 는 「계좌별 묶음 + 출처 칩 + NXT 태그」 를 모바일 카드 행과 데스크톱 표 양쪽에 한 경로로 세우는 것이고, 이어서 기록 지연 표식 · 390px 줄바꿈 수정 · 「· 수동」 꼬리 제거를, 끝으로 e2e 와 시각 확인을 붙인다. 목업이 UI 계약이다(UI-SPEC 없음 — 사용자 결정 --skip-ui).

Purpose: 기준이 「로그인한 사람」 에서 「계좌」 로 바뀌면서(D-06) 같은 계좌의 WinForms·다른 DMA 사용자 주문이 섞인다 — 계좌별로 묶고 출처를 칩으로 말해야 트레이더가 누가 무엇을 냈는지 읽는다(D-07·D-08). 기록 연결이 끊기면 빈 목록을 「주문 없음」 으로 오해하지 않게 표식을 띄운다(D-04 (a)).
Output: `origin-tag.tsx`(추출) · 카드 B′ · 묶기 함수 · 단위 테스트 · e2e.

커밋 규율: 한글 메시지 · Co-Authored-By 없음 · 파일 지정 add · `git status -sb` · **push 금지**(19-12). 손보는 표면 안의 줄바꿈·잘림·겹침 결함은 묻지 말고 고친 뒤 SUMMARY 에 한 줄 보고(메모리 규칙).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-account-order-journal/19-CONTEXT.md
@.planning/phases/19-account-order-journal/19-06-SUMMARY.md

UI 계약: `.planning/phases/19-account-order-journal/19-today-orders-mockup.html` — 345-365 B′ 절(설명 문단 2개 · 평상시/기록 지연 프레임) · 276-301 CSS(`.grp` · `.ahead` · `.stale` · `.stale-note` · `.org` · `.xt`) · 470-548 조각 함수(`sideTag(r, noMeta)` · `orgChip` · `nxtTag` · `mRow` · `dTable` · `header(rows, stale)` · `card`). 목업 `.fix .l2, .fix .l3 { flex-wrap: wrap; row-gap: 2px }` 가 390px 수정의 정본.

<interfaces>
19-06 결과: 카드가 `mergeJournalRows(restored, journalRows, kstDateIso())` → `mergeOrderNotices(rows)` 로 행을 만들고 `MergedOrderNotice { head: JournalOrderRow; count; qty: number|null; priceMin: number|null; priceMax: number|null; at; orderNoText: string|null }` 를 그린다. `useRelayContext()` 에 `accounts: RelayAccount[]`(accountNo · name) · `journalState: RelayJournalStateMsg | null`.
`webapp/src/components/orderbook/account-panel.tsx` 178 `AccountOriginTag = '상따' | 'VI'` · 213 `originTag?` prop · 735 사용처 · 1406-1420 비공개 `OriginTag`(클래스 `whitespace-nowrap rounded-[var(--r-sm)] bg-[var(--muted)] px-1.5 py-0.5 text-[11px] font-semibold text-[var(--muted-fg)]`, `data-slot="account-origin-tag"`, 주석 「수동 주문은 태그가 없다」). `webapp/src/components/trading/exchange-tag.tsx` `ExchangeTag({ exchange, size })`(NXT = 채움 accent). 카드 현 구조: 머리(제목 · N건) · 데스크톱 `max-[1279px]:hidden` 표 · 모바일 `min-[1280px]:hidden` 2줄 행(①줄 종목명 flex-1 truncate · 코드 · 구분 · ml-auto 상태 / ②줄 시각 · 수량 · 가격 · ml-auto 주문) · `SideTag`(label.meta 꼬리) · `StatusTag`.
</interfaces>
</context>

<!-- planner-discipline-allow: label.meta -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] B′ 한 경로 — 계좌별 묶음 · 출처 칩 · NXT 태그 (모바일 행 + 데스크톱 표) · OriginTag 추출</name>
  <files>webapp/src/components/trading/origin-tag.tsx, webapp/src/components/orderbook/account-panel.tsx, webapp/src/lib/orders-api.ts, webapp/src/components/trading/today-orders-card.tsx, webapp/src/components/trading/__tests__/today-orders-card.test.tsx</files>
  <read_first>
    - .planning/phases/19-account-order-journal/19-today-orders-mockup.html (270-310 CSS · 345-365 B′ 절 · 443-548 스크립트 조각)
    - webapp/src/components/trading/today-orders-card.tsx (19-06 결과 전문)
    - webapp/src/components/orderbook/account-panel.tsx (170-220 · 725-745 · 1400-1425)
    - webapp/src/components/trading/exchange-tag.tsx (전문)
    - webapp/src/lib/orders-api.ts (19-06 결과)
    - webapp/src/components/trading/__tests__/today-orders-card.test.tsx (19-06 결과)
    - webapp/src/components/orderbook/__tests__/account-panel.test.tsx (origin-tag 셀렉터 사용처 grep)
  </read_first>
  <behavior>
    - relay accounts [A(위탁종합), B(위탁CMA)] · 행 [B 1건, A 2건, X(목록 밖) 1건] → 묶음 순서 A · B · X · A 소제목 「계좌 A번호 위탁종합 2건」 · X 소제목은 번호만
    - 행 없는 계좌(목록에 있지만 0행) → 묶음 없음
    - origin limit_chaser → 칩 「상따」 · vi → 「VI」 · manual → 「수동」 · null → 칩 없음(data-slot today-order-origin 0개)
    - exchange NXT 행 → ExchangeTag 1개(data-exchange NXT) · KRX 행 → 0개
    - 데스크톱 표 머리: 시각 · 종목 · 구분 · 출처 · 수량 · 가격 · 상태 · 주문번호 (출처가 구분 바로 뒤)
    - account-panel 의 출처 태그는 그대로 `data-slot="account-origin-tag"` 로 렌더된다(기존 테스트 무수정 green)
  </behavior>
  <action>
**추출**: `webapp/src/components/trading/origin-tag.tsx` 새 파일 — account-panel 의 비공개 `OriginTag` 를 클래스 그대로 옮겨 export 한다. `OriginTagLabel = '상따' | 'VI' | '수동'` · `OriginTag({ tag, slot = 'account-origin-tag' }: { tag?: OriginTagLabel; slot?: string })`(tag 없으면 null) · `originTagOf(origin: JournalOrderOrigin | null): OriginTagLabel | undefined`(manual → 수동 · limit_chaser → 상따 · vi → VI · null → undefined — D-08 보충). 순수 표시 원자로 두고(`'use client'`·relay 훅 없음 — exchange-tag.tsx 규율) 머리 주석에 D-08(카드에서는 「수동」 도 표시 · account-panel 은 종전대로 상따/VI 만 넘긴다)을 적는다. `account-panel.tsx` 는 비공개 함수를 지우고 이 파일을 import 한다(`AccountOriginTag` 는 `Exclude<OriginTagLabel, '수동'>` 로 두어 기존 prop 계약 유지).
**묶기 함수**(`orders-api.ts`): `groupJournalRowsByAccount(rows: readonly JournalOrderRow[], accounts: readonly RelayAccount[]): { accountNo: string; name: string; rows: JournalOrderRow[] }[]` — accounts 순서대로 행이 있는 계좌만, 그다음 목록에 없는 계좌를 rows 첫 등장 순으로(name ""), 각 묶음의 rows 는 입력 순서 유지(createdAt 내림차순).
**카드**(`today-orders-card.tsx`): 병합된 행을 `groupJournalRowsByAccount(rows, accounts)` 로 나눈 뒤 **묶음마다** `mergeOrderNotices(group.rows)` 를 부른다(묶기는 계좌 경계를 넘지 않는다). 묶음 머리는 목업 `.ahead` 문법(「계좌」 라벨 11px muted · 번호 mono 전체 표시 · 상품명 · 오른쪽 「N건」 = 그 계좌의 묶기 전 행 수)을 기존 토큰 클래스로, `data-slot="today-orders-group"` · `data-account` 속성. 모바일 ①줄 순서 = 종목명(유일한 신축 항목) · 코드 · NXT 태그(`exchange === 'NXT'` 일 때만 `ExchangeTag size="sm"`) · 구분 · 출처 칩(`OriginTag tag={originTagOf(row.origin)} slot="today-order-origin"`) · (ml-auto) 상태 — 새 조각은 전부 `flex-none`(헤더 ⑤ 규율). 데스크톱은 묶음마다 표 하나(목업 dTable), 「출처」 열을 구분 뒤에, NXT 태그는 종목 셀의 코드 뒤에. 헤더 주석에 B′ 규칙(⑨ 계좌별 묶음 · ⑩ 칩·태그)을 더하고 ② 「조회는 페이지당 1회 · 계좌 카드 안에 넣지 않는다」 는 유지한다.
**테스트**: `today-orders-card.test.tsx` 목 컨텍스트에 `accounts` 를 넣고 `<behavior>` 1~5 를 추가. account-panel 테스트는 수정하지 않고 green 확인.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest run src/components/trading/__tests__/today-orders-card.test.tsx src/components/orderbook/__tests__/account-panel.test.tsx</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 "Test Files" 요약줄에 failed</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "function OriginTag" webapp/src/components/orderbook/account-panel.tsx` 출력 0줄(== 0) · `grep -n "origin-tag" webapp/src/components/orderbook/account-panel.tsx` >= 1
    - `grep -n "groupJournalRowsByAccount" webapp/src/components/trading/today-orders-card.tsx` >= 1
    - `grep -n "today-order-origin" webapp/src/components/trading/today-orders-card.tsx` >= 1 · `grep -n "ExchangeTag" webapp/src/components/trading/today-orders-card.tsx` >= 1
    - `git diff --stat -- webapp/src/components/orderbook/__tests__/account-panel.test.tsx` 출력 없음(기존 테스트 무수정)
  </acceptance_criteria>
  <done>카드가 계좌별로 묶여 그려지고, 줄마다 누가 낸 주문인지(상따·VI·수동)와 NXT 여부가 보인다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 기록 지연 배지·안내 · 390px ②줄 줄바꿈 · 「· 수동」 꼬리 제거 · 로컬 거부 「—」</name>
  <files>webapp/src/components/trading/today-orders-card.tsx, webapp/src/components/trading/__tests__/today-orders-card.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/today-orders-card.tsx (Task 1 결과)
    - .planning/phases/19-account-order-journal/19-today-orders-mockup.html (285-292 `.stale`·`.stale-note` · 525-531 header(rows, stale))
    - webapp/src/lib/order-notices.ts (80-120 orderNoticeLabel · meta — trading-alerts 가 쓰므로 함수는 유지)
  </read_first>
  <behavior>
    - journalState {s:"delayed", since: 13:52:10 KST 의 ISO} → `[data-testid="today-orders-delayed"]` role=status 텍스트 「기록 지연」 · 안내 `[data-testid="today-orders-delayed-note"]` 에 「기록 지연 — 복구되면 채워집니다.」 와 「13:52:10 이후 주문이 아직 안 보일 수 있어요.」 · 행들에 opacity 클래스 없음
    - since 없음 → 안내에 시각 문장 없음 · journalState null 또는 live → 배지·안내 0
    - 로딩·빈 상태·오류 상태에서도 delayed 면 배지가 제목 옆에 있다
    - requester Manual 행의 구분 칸에 「· 수동」 없음(출처 칩이 대신 말한다)
    - 로컬 거부 행(orderNo null · qty null · price null) → 주문 칸 「—」 · 수량 「—」 · 가격 「—」
    - ②줄 컨테이너 className 에 `flex-wrap` 과 `gap-y-0.5` 가 있다
  </behavior>
  <action>
**기록 지연(D-04 (a))**: 카드 머리(제목 · N건) 뒤에 `journalState?.s === "delayed"` 일 때만 배지 — 목업 `.stale` 를 기존 토큰 클래스로: `inline-flex flex-none items-center gap-1.5 h-5 px-2 rounded-full border border-[var(--border)] bg-[var(--muted)] text-[11px] font-semibold text-[var(--muted-fg)] whitespace-nowrap`, `role="status"`, `data-testid="today-orders-delayed"`, 안에 `aria-hidden` 진행 점(7px 원 · `bg-current` · `animate-pulse` · `motion-reduce:animate-none`) + 「기록 지연」. 머리 아래 한 줄 안내 `data-testid="today-orders-delayed-note"`(목업 `.stale-note` — muted 배경 · border · r-md · caption 크기 · 「기록 지연」 만 fg 굵게) 「기록 지연 — 복구되면 채워집니다.」 + since 가 있으면 「{KST HH:MM:SS} 이후 주문이 아직 안 보일 수 있어요.」(시각은 카드의 기존 `KST_TIME` 포맷터 — 새 KST 함수 금지). 로딩·빈·오류 본문과 무관하게 머리에 붙는다. 이미 그린 행을 흐리게 하지 않는다(목업 lead — 기록된 줄은 맞는 값).
**390px 수정(D-07)**: 모바일 ②줄 div 에 `flex-wrap gap-y-0.5` 를 더한다(목업 `.fix .l2 { flex-wrap: wrap; row-gap: 2px }`). ①줄 규율(종목명만 신축)은 그대로.
**꼬리 제거(D-08)**: 카드의 `SideTag` 에서 수동 메타 꼬리 렌더 분기를 지운다 — `orderNoticeLabel` 의 `meta` 필드와 함수는 trading-alerts 가 쓰므로 그대로 둔다. `SideTag` 주석에 「D-08 — 출처 칩이 수동을 말한다」.
**로컬 거부(D-08)**: 수량·가격이 null 이면 「—」(19-06 이 이미 했으면 단언만), 주문번호 null → 「—」(기존 `orderNoText ?? "—"`).
테스트에 `<behavior>` 6항목. 옛 ⑥-2(「수동」 메타 표시) 케이스는 D-08 로 바뀐 규칙에 맞춰 「꼬리 없음 + 칩 수동」 으로 고치고 SUMMARY 에 사유를 적는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest run src/components/trading/__tests__/today-orders-card.test.tsx src/lib/__tests__/trading-alerts.test.ts</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 "Test Files" 요약줄에 failed</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 카드에 수동 메타 꼬리 렌더가 없다 — `grep -c "label.meta" webapp/src/components/trading/today-orders-card.tsx` 출력 0(== 0)
    - `grep -n "today-orders-delayed" webapp/src/components/trading/today-orders-card.tsx` >= 2 · `grep -n "motion-reduce:animate-none" webapp/src/components/trading/today-orders-card.tsx` >= 1
    - `grep -n "flex-wrap gap-y-0.5" webapp/src/components/trading/today-orders-card.tsx` >= 1
  </acceptance_criteria>
  <done>기록 연결이 끊기면 카드가 그 사실과 시각을 말하고, 좁은 폭에서도 주문번호가 잘리지 않으며, 출처는 칩 하나로만 말한다.</done>
</task>

<task type="auto">
  <name>Task 3: e2e B′ — 계좌 순 묶음 · 칩 · NXT · 390px 주문번호 비잘림 + 목업 대조 시각 확인</name>
  <files>webapp/e2e/specs/me.spec.ts</files>
  <read_first>
    - webapp/e2e/specs/me.spec.ts (1-265 픽스처 · 600-700 테스트 8 의 잎 넘침 측정)
    - webapp/e2e/overflow.ts (넘침 측정 헬퍼가 있으면 재사용)
    - webapp/src/components/trading/today-orders-card.tsx (Task 2 결과 — data-slot · data-testid)
  </read_first>
  <action>
`TODAY_ORDERS` 픽스처를 B′ 검증에 맞게 넓힌다: ACCOUNTS 두 계좌에 걸쳐 5행 이상 — 계좌 A: 수동 KRX 매수 · 상따 NXT 매도(긴 주문번호) · 로컬 거부(orderNo null · qty·price null · origin manual) / 계좌 B: VI KRX 매수 · origin null 행 1(칩 없음 확인). 기존 긴 종목명 행 `0000900002` 는 유지. 테스트 두 개를 더한다: 「9. B′ — 데스크톱 1280: 계좌 순 묶음 · 출처 열 · NXT 태그」(`today-orders-group` 2개가 ACCOUNTS 순 · 각 머리 번호 · 「출처」 열 머리 · NXT 태그 1개 · origin null 행에 칩 0 · 「· 수동」 텍스트 0) · 「10. B′ — 폰 390: 묶인 행 ②줄 줄바꿈 · 주문번호 비잘림」(MOBILE_VIEWPORT · 긴 주문번호 행의 주문번호 잎 `getBoundingClientRect().right` ≤ 목록 right + 1 · 그 행 ②줄 높이가 한 줄보다 크면 주문번호 잎의 y 가 시각 잎 y 보다 크다(내려갔다) · 테스트 8 과 같은 잎 넘침 측정이 모든 행에서 빈 배열). 기록 지연 배지는 로컬 relay 관찰자가 비활성이라(e2e 는 비밀 미주입) e2e 대상이 아니다 — 단위 테스트(Task 2)가 정본이라는 주석.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/me.spec.ts</automated>
    <fails_when>non-zero exit, 또는 요약줄에 "failed", 또는 "0 passed"</fails_when>
    <human-check>
      <test>`pnpm --filter @gh-radar/webapp dev`(PORT 3100 — dev.sh 기준) 로 띄운 `/me` 를 라이트·다크 × 폰 390 · 데스크톱 1280 으로 열고, 옆에 `open .planning/phases/19-account-order-journal/19-today-orders-mockup.html` 의 「B′ — 채택안」 절을 띄워 대조한다. 기록 지연은 단위 테스트 렌더(스크린샷 첨부) 또는 개발자 도구로 `journal.state` 프레임을 주입해 본다</test>
      <expected>계좌 소제목 문법 · 칩(상따·VI·수동) 모양·색 · NXT 태그 위치(코드 옆) · 390 에서 주문번호 줄바꿈 · 「· 수동」 꼬리 없음 · 기록 지연 배지+안내가 목업과 같다. 새 색 없음</expected>
      <why_human>시각 대조(목업 채택안과 같은 문법인지)는 grep·단위 테스트로 판정할 수 없다</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "today-orders-group" webapp/e2e/specs/me.spec.ts` >= 1 · `grep -n "B′" webapp/e2e/specs/me.spec.ts` >= 2
    - Playwright me.spec 전 케이스 통과(기존 1~8 포함)
  </acceptance_criteria>
  <done>실제 브라우저에서 B′ 묶음·칩·태그가 계좌 순으로 서고 390px 에서 어떤 잎도 목록 밖으로 밀려나지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버·relay 가 준 행 → 브라우저 렌더 | 이미 계좌 권한으로 걸러진 행만 그린다 — 표시층은 권한을 판단하지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-08 | Information Disclosure | 주문자 표시 | low | mitigate | 카드에 DMA 사용자 필드가 없다(계약에 없음 — 19-04) · 목업 C 칩 미채택 |
| T-19-30 | Spoofing(오인) | 출처 미상 행을 「수동」 으로 표시 | low | mitigate | origin null → 칩 생략(D-08 보충) 단위·e2e 단언 |
| T-19-31 | Repudiation(오인) | 끊김 중 빈 목록을 「주문 없음」 으로 오해 | low | mitigate | 기록 지연 배지+안내(D-04 (a)) |
</threat_model>

<verification>
- webapp 카드·account-panel·trading-alerts 단위 테스트 · typecheck · Playwright me.spec green.
- human-check: 목업 B′ 대조(end-of-phase UAT 로 수집).
- spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</verification>

<success_criteria>
- B′ 계좌별 묶음 · 출처 칩 · NXT 태그 · 기록 지연 표식 · 390 줄바꿈 · 꼬리 제거가 단위·e2e 로 잠김.
</success_criteria>

## Artifacts this phase produces (이 plan 분)

| 종류 | 이름 |
|------|------|
| 컴포넌트 | `webapp/src/components/trading/origin-tag.tsx` — `OriginTag({ tag, slot })` · `originTagOf(origin)` · `OriginTagLabel` |
| 함수 | `groupJournalRowsByAccount(rows, accounts)` |
| DOM 계약 | `data-slot="today-orders-group"`(+`data-account`) · `data-slot="today-order-origin"` · `data-testid="today-orders-delayed"` · `data-testid="today-orders-delayed-note"` |
| e2e | me.spec 테스트 9 · 10 |

<output>
Create `.planning/phases/19-account-order-journal/19-08-SUMMARY.md` when done
</output>
