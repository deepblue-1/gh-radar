---
phase: 19-account-order-journal
plan: 09
type: execute
wave: 5
depends_on: ["19-07"]
files_modified:
  - relay/src/generated/
  - relay/src/dma/msg-type.ts
  - relay/src/hub/subscription-hub.ts
  - relay/src/dma/envelope.ts
  - relay/src/journal/codec.ts
  - relay/tests/helpers/frames.ts
  - relay/tests/journal-codec.test.ts
  - relay/tests/helpers/fake-gateway.ts
  - relay/tests/journal-gateway.test.ts
  - relay/tests/hub.test.ts
autonomous: false
requirements: [D-02, D-09, D-12, D-13]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G1: relay 생성물은 gh-trade 가 커밋한 관찰자 계약 .fbs 에서 `sync-relay-schema.sh`(RELAY= 지정 · flatc 25.12.19)로만 만들어지고 `--check` 차이 0 이다 · 생성물은 손편집하지 않는다"
    - "D-09 · Pitfall 9: 관찰자 응답 번호(제안 79 · 80 — G1 확정값)는 `INBOUND_MSG_TYPES` 와 사용자 세션 hub 의 명시 case(warn 후 무시)에 **같은 커밋**으로 들어가고, 사용자 세션에 우연히 와도 `unhandledFrameCount` 가 오르지 않는다"
    - "D-12: 실 코덱이 로그인 요청(비밀 · since_seq · epoch · client)을 만들고, 로그인 응답(epoch · head/oldest · resync · 매핑)과 저널 배치(레코드 · head · caught_up)를 19-05 도메인 타입으로 풀어 관찰자가 무수정으로 동작한다 — 레코드는 형식 이상이어도 버리지 않는다(버리면 seq 갭 루프)"
    - "D-02: 로컬 거부 레코드(`local_reject` 참 · 빈 주문번호)가 코덱을 지나 기록기로 그대로 간다"
    - "D-13: 실 TCP(가짜 게이트웨이)에서 관찰자가 끊김 뒤 since_seq = 마지막 수신 seq 로 다시 로그인하고, 거부되면 두 번째 로그인을 보내지 않으며, 76 브로드캐스트를 조용히 무시한다 · 게이트웨이가 관찰자 연결에서 받은 요청 종류는 로그인과 LivePing 뿐이다"
  artifacts:
    - path: relay/src/journal/codec.ts
      provides: "createJournalCodec — JournalCodec 실구현"
      exports: ["createJournalCodec"]
    - path: relay/src/dma/envelope.ts
      provides: "buildObserverLoginReq · parseObserverLoginResp · parseJournalBatch · MAX_JOURNAL_BATCH_RECORDS · MAX_OBSERVER_ACCOUNT_COUNT"
      contains: "parseJournalBatch"
    - path: relay/tests/journal-gateway.test.ts
      provides: "실 DmaClient + 가짜 게이트웨이 + 실 코덱 + 관찰자 통합"
  key_links:
    - from: "DmaClient tryParseEnvelope"
      to: "INBOUND_MSG_TYPES(79·80)"
      via: "화이트리스트 통과 → frame 이벤트 → createJournalCodec().decode"
      pattern: "INBOUND_MSG_TYPES"
    - from: "SubscriptionHub.#onFrame"
      to: "case MSG.ObserverLoginResp / MSG.JournalBatch"
      via: "사용자 세션 도착 시 warn 후 무시(default 로 떨어지지 않음)"
      pattern: "MSG.JournalBatch"
  prohibitions:
    - "MUST NOT `relay/src/generated/**` 를 손편집한다"
    - "MUST NOT gh-trade 저장소 파일을 수정·커밋한다 — sync 스크립트 실행만(생성물은 gh-radar 쪽)"
    - "MUST NOT 번호를 INBOUND_MSG_TYPES 에만 넣고 hub case 를 다음 커밋으로 미룬다"
    - "MUST NOT 파싱 이상 레코드를 조용히 건너뛴다 — seq 갭 재접속 루프가 된다"
---

<objective>
**[BLOCKING G1]** gh-trade Phase 23 이 관찰자 와이어 계약(.fbs)을 커밋한 뒤에만 진행한다. 계약을 gh-radar relay 생성물로 동기화하고, 19-05 가 정의한 `JournalCodec` 주입 지점에 **실 코덱**을 꽂는다. tracer 는 「게이트웨이가 보낸 JournalBatch 프레임 바이트 → 화이트리스트 → 실 코덱 → 도메인 레코드」 왕복이고, 이어서 실 TCP 가짜 게이트웨이 위에서 관찰자 전 경로(로그인 · 배치 · 끊김 이어받기 · 거부 정지 · 76 무시)를 통합 검증한다.

Purpose: 코덱 스왑이 이 phase 의 교차 저장소 결합점이다 — 관찰자·기록기·DB·webapp 은 G1 이전에 끝나 있고, 여기서는 번호와 필드 매핑만 확정한다(RESEARCH Assumption A1 을 G1 이 흡수). 화이트리스트와 hub case 를 같은 커밋에 넣는 규율(`docs/relay-operations.md`)이 「조용히 사라지는 프레임 0」 불변식을 지킨다.
Output: 재생성된 `relay/src/generated/**` · MSG 3종 · hub case · envelope 코덱 함수 · `relay/src/journal/codec.ts` · 테스트 헬퍼 확장 · 통합 테스트.

커밋 규율: 생성물은 **별도 커밋**(메시지에 gh-trade 커밋 해시) · 그 뒤 MSG + INBOUND + hub case + 코덱을 한 커밋 · 한글 메시지 · Co-Authored-By 없음 · 파일 지정 add · `git status -sb` · **push 금지**.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-account-order-journal/19-CONTEXT.md
@.planning/phases/19-account-order-journal/19-GH-TRADE-HANDOFF.md
@.planning/phases/19-account-order-journal/19-07-SUMMARY.md

<interfaces>
19-05/19-07 relay: `relay/src/journal/types.ts` — `JournalCodec { buildLoginReq({secret, sinceSeq, epoch, client}): Uint8Array; decode(e: TransportFrameEvent): ObserverFrame }` · `ObserverFrame`(login · batch · ignore · unexpected · malformed) · `ObserverLoginResult` · `ObserverAccountRow` · `JournalBatchFrame` · `JournalRecord`(23필드 camelCase). `JournalObserver` 는 codec 을 주입받는다(무수정 대상).
envelope 규약: `relay/src/dma/envelope.ts` 96-122 상한 상수 · 170 `skipAccount` · 193 `drop` · 211 `dropField` · 230 `takeCount` · 249 `toNum(v: bigint, label)` · 283 `isValidAccountNo` · 293 `maskAccountNo` · 322-335 `buildLoginReq` 빌더 패턴 · 733-811 `readAccountEntries`/`parseLoginResp` 파서 패턴(항목만 건너뛰고 프레임은 살린다) · 1623 `parseOrderResp`(형식 이상은 경고, 프레임은 살린다).
msg-type 규약: `relay/src/dma/msg-type.ts` 20-60 「유입 집합」 주석 · 65-170 `MSG` · 186-210 `INBOUND_MSG_TYPES`. hub: `relay/src/hub/subscription-hub.ts` 950-1100 `#onFrame` switch(명시 case · `LoginResp`/`UpdateAccountNoResp` 무시 case 1079-1084 선례 · default 계수).
테스트 헬퍼: `relay/tests/helpers/frames.ts` 257-280 `buildLoginRespFrame`(문자열을 테이블 조립 전에 만든다) · `relay/tests/helpers/fake-gateway.ts` 83-196 API · 240-356 `read*Request` · 363- `startFakeGateway`.
동기화: `cd /Users/alex/repos/gh-trade/server && RELAY=/Users/alex/repos/gh-radar/relay ./scripts/sync-relay-schema.sh [--check]` (gh-trade 소유 · flatc 25.12.19 · 생성물 SYNC MARKER `server-repo-commit` 갱신 — 현재 6adcb62f).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: [BLOCKING G1] gh-trade Phase 23 관찰자 와이어 계약(.fbs) 커밋 확인</name>
  <action>gh-trade 저장소에서 관찰자 계약(.fbs)을 확정·커밋한다 — gh-radar 는 gh-trade 파일을 수정할 권한이 없다(교차 저장소 경계).</action>
  <instructions>
    Claude 가 이미 한 것: 19-01 에서 `.planning/phases/19-account-order-journal/19-GH-TRADE-HANDOFF.md`(와이어 계약 제안 · 의미 규칙 · 관찰자 요구 · 펌프·보관 · G1/G2 신호)를 썼고, relay 쪽 관찰자·기록기·DB·webapp 은 가짜 코덱으로 끝나 있다(19-01~19-08).
    사람이 할 것(gh-trade 세션): gh-trade 저장소에서 새 phase(권장 Phase 23)를 열어 인계서를 입력으로 discuss → `server/src/protocol/StockDMA.fbs` 에 관찰자 로그인 요청·응답 · 저널 배치 테이블과 MsgType 을 추가해 **커밋**한다(게이트웨이 구현·배포는 G2 까지 이어가도 된다 — 이 게이트는 계약 커밋만 본다). C# `sync-client-schema.sh` · `expected-vtable.txt --update-expected` 는 gh-trade 몫이다.
    계약이 인계서와 **의미**가 다르면(epoch/resync 없음 · 펌프 대신 요청-응답 페이징 · seq 가 조밀하지 않음 · local_reject 표식 없음 등) 커밋 해시와 함께 차이를 적어 준다 — Claude 는 도메인 타입을 바꾸지 않고 코덱 매핑으로 흡수할 수 있는지 먼저 판정하고, 불가하면 이 plan 을 멈추고 재계획을 요청한다.
  </instructions>
  <verification>Claude 가 확인한다: `git -C /Users/alex/repos/gh-trade log -1 --format=%h -- server/src/protocol/StockDMA.fbs` 가 알려 준 해시와 같고, `grep -nE "ObserverLoginReq|ObserverLoginResp|JournalBatch" /Users/alex/repos/gh-trade/server/src/protocol/StockDMA.fbs` 가 MsgType 과 table 정의를 보여 준다(이름이 다르면 사람이 알려 준 이름으로).</verification>
  <resume-signal>「G1 {gh-trade 커밋 해시}」 (계약이 인계서와 다르면 차이 목록을 함께)</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: [tracer] 스키마 동기화 → MSG·화이트리스트·hub case(한 커밋) → 실 코덱 → 배치 프레임 왕복</name>
  <precondition>gh-trade `server/src/protocol/StockDMA.fbs` 의 최신 커밋에 관찰자 MsgType 과 테이블이 있다(Task 1 resume-signal 의 해시)</precondition>
  <files>relay/src/generated/, relay/src/dma/msg-type.ts, relay/src/hub/subscription-hub.ts, relay/src/dma/envelope.ts, relay/src/journal/codec.ts, relay/tests/helpers/frames.ts, relay/tests/journal-codec.test.ts</files>
  <read_first>
    - docs/relay-operations.md (1-20 재동기화 규칙)
    - /Users/alex/repos/gh-trade/server/src/protocol/StockDMA.fbs (관찰자 계약 부분 — 읽기 전용)
    - relay/src/dma/msg-type.ts (전문)
    - relay/src/hub/subscription-hub.ts (940-1100 #onFrame)
    - relay/src/dma/envelope.ts (90-300 상수·가드 · 315-340 빌더 · 720-815 로그인 파서 · 1580-1672 origin·OrderResp 파서)
    - relay/src/journal/types.ts (19-05/19-07 결과)
    - relay/tests/helpers/frames.ts (240-300 buildLoginRespFrame)
  </read_first>
  <behavior>
    - frames.buildObserverLoginRespFrame({success, epoch "ep-1", headSeq 12, oldestSeq 1, resync true, accounts 2}) → tryParseEnvelope → codec.decode → {k:"login", result} 필드 일치 · headSeq number 12
    - frames.buildJournalBatchFrame({records [A seq 11 · 로컬 거부 seq 12(orderNo "" · localReject true)], headSeq 12, caughtUp true}) → {k:"batch"} · 레코드 23필드가 입력과 같다 · 로컬 거부 레코드가 빠지지 않는다
    - 계좌번호 형식 이상 레코드(13자) → 버리지 않고 warn 1회 · 레코드 그대로
    - RateCrossAlert(76) 프레임 → {k:"ignore"} · OrderResp(51) 프레임 → {k:"unexpected"}
    - accounts 중 형식 이상 1건 → 그 항목만 건너뛰고 나머지 유지(skipAccount 계수 +1)
    - codec.buildLoginReq({secret "s", sinceSeq 7, epoch "ep-1", client "gh-radar-relay"}) → 생성 코드 reader 로 읽으면 네 값이 같다 · sinceSeq 가 음수·비정수·2^53 이상이면 throw
  </behavior>
  <action>
(files 의 `relay/src/generated/` 는 동기화 스크립트 산출물 한 묶음이다 — 손으로 여는 파일은 나머지 6개이고, 번호·화이트리스트·hub case 를 한 커밋에 묶어야 해서(Pitfall 9) 이 task 를 더 쪼개지 않는다.) ① **동기화(생성물 별도 커밋)**: `cd /Users/alex/repos/gh-trade/server && RELAY=/Users/alex/repos/gh-radar/relay ./scripts/sync-relay-schema.sh` 를 실행하고 `--check` 로 차이 0 을 확인한다. `relay/src/generated/StockDMA.fbs` 의 SYNC MARKER `server-repo-commit` 이 Task 1 해시로 바뀌었는지 본다. 생성물만 먼저 커밋한다(메시지에 gh-trade 해시 · 「생성물 손편집 없음」). flatc 버전이 25.12.19 가 아니면 멈추고 보고한다.
② **번호·화이트리스트·hub case(한 커밋 — Pitfall 9)**: `relay/src/dma/msg-type.ts` 의 `MSG` 에 G1 확정 이름·번호로 관찰자 로그인 요청(요청 대역 · 제안 5), 로그인 응답(응답 대역 · 제안 79), 저널 배치(응답 대역 · 제안 80)를 JSDoc 1줄씩(「관찰자 연결 전용 · gh-radar 19 D-09」) 더하고 `INBOUND_MSG_TYPES` 에 응답 2종을 넣는다(요청 번호는 넣지 않는다 — C→S). 머리 「유입 집합」 주석에 19-09 문단(두 번호의 하류 책임 = 관찰자 코덱, 사용자 세션에 오면 hub 명시 case 가 warn 후 무시)을 더하고 총 종 수를 갱신한다. `relay/src/hub/subscription-hub.ts` `#onFrame` 에 두 번호의 명시 case — `logger.warn({ userId, msgType: e.msgType }, "[HUB] 사용자 세션에 관찰자 전용 프레임 — 무시")` 후 break(default 로 떨어지지 않아 `unhandledFrameCount` 가 오르지 않는다).
③ **envelope 코덱 함수**(`relay/src/dma/envelope.ts`, 기존 패턴 그대로): 상한 상수 `MAX_JOURNAL_BATCH_RECORDS = 500` · `MAX_OBSERVER_ACCOUNT_COUNT = 1024` 를 다른 MAX_* 옆에. `buildObserverLoginReq({ secret, sinceSeq, epoch, client })` — `buildLoginReq` 형식(문자열 먼저 → 테이블 → Envelope · msgType), since_seq 가 ulong 이면 `BigInt(sinceSeq)`(음수·비정수·`Number.MAX_SAFE_INTEGER` 초과는 throw — 호출자 버그), 비밀은 어디에도 로그하지 않는다. `parseObserverLoginResp(env): ObserverLoginResult | null` — 슬롯 null 이면 `dropField`, 계좌 벡터는 `takeCount(n, MAX_OBSERVER_ACCOUNT_COUNT, …)` + 항목별 `isValidAccountNo` 실패는 `skipAccount` 로 그 항목만 건너뛴다(재정규화 금지 — Pitfall 7), ulong 은 `toNum`. `parseJournalBatch(env): JournalBatchFrame | null` — 슬롯 null 이면 `dropField` · 레코드 수 `takeCount(n, MAX_JOURNAL_BATCH_RECORDS, …)` · 레코드는 **하나도 버리지 않는다**(형식 이상 ISIN·계좌는 `maskAccountNo`·길이만 warn — `parseOrderResp` 의 「프레임은 살린다」 규율, 버리면 seq 갭 → 재접속 루프) · 빈 문자열 기본값 · 숫자 필드는 `toNum`/정수. G1 필드 이름이 인계서와 다르면 매핑만 여기서 흡수하고 `JournalRecord` 는 바꾸지 않는다.
④ **코덱 어댑터** `relay/src/journal/codec.ts` — `createJournalCodec(): JournalCodec`: `buildLoginReq` → `buildObserverLoginReq` · `decode(e)` → 로그인 응답 번호면 파싱 결과로 login/malformed, 저널 배치 번호면 batch/malformed, `MSG.RateCrossAlert` 면 ignore(Pitfall 8), 그 밖 unexpected. 머리 주석: G1 해시 · 「스왑 지점은 이 파일 하나 — 관찰자·기록기는 무수정」.
⑤ **프레임 빌더**(`relay/tests/helpers/frames.ts`): `buildObserverLoginRespFrame(input)` · `buildJournalBatchFrame(input)` · `FakeJournalRecordInput`(기본값 채움 — 계좌는 기존 `SAMPLE_ACCOUNT_NO`, 실계좌·실서버 IP 리터럴 금지 D-27) — `buildLoginRespFrame` 과 같은 「문자열 먼저」 규율.
⑥ **테스트** `relay/tests/journal-codec.test.ts` — `<behavior>` 6항목(프레임 바이트 → `tryParseEnvelope` → `createJournalCodec().decode({ ...parsed, generation: 1 })`).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-trade/server && RELAY=/Users/alex/repos/gh-radar/relay ./scripts/sync-relay-schema.sh --check</automated>
    <fails_when>non-zero exit, 또는 출력에 차이(diff) 줄 · "MISMATCH" · "differs" 가 있음</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay exec vitest run tests/journal-codec.test.ts tests/protocol.test.ts</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 "Test Files" 요약줄에 failed</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format=%s -- relay/src/generated` 제목에 Task 1 의 gh-trade 해시가 있다 · 그 커밋 SHA 를 GEN_SHA 로 SUMMARY 에 적고 `git show --stat --format= "$GEN_SHA"` 가 `relay/src/generated/` 아래 파일만 보여 준다
    - `grep -n "JournalBatch" relay/src/dma/msg-type.ts` >= 2(MSG · INBOUND) · `grep -n "MSG.JournalBatch" relay/src/hub/subscription-hub.ts` >= 1 · 두 파일 변경이 같은 커밋 — `git log -1 --format=%H -- relay/src/dma/msg-type.ts` 와 `git log -1 --format=%H -- relay/src/hub/subscription-hub.ts` 가 같다
    - `grep -n "export function parseJournalBatch" relay/src/dma/envelope.ts` >= 1 · `grep -n "export function createJournalCodec" relay/src/journal/codec.ts` >= 1
    - `git -C /Users/alex/repos/gh-trade status --porcelain` 출력이 Task 2 전과 같다(gh-trade 무수정)
  </acceptance_criteria>
  <done>gh-trade 가 보낸 저널 프레임이 relay 화이트리스트를 지나 도메인 레코드로 풀리고, 관찰자 코드는 한 줄도 바뀌지 않았다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 실 TCP 통합 — 가짜 게이트웨이 관찰자 모드 · 이어받기 · 거부 정지 · 76 무시 · 요청 종류 · hub 불변식</name>
  <files>relay/tests/helpers/fake-gateway.ts, relay/tests/journal-gateway.test.ts, relay/tests/hub.test.ts</files>
  <read_first>
    - relay/tests/helpers/fake-gateway.ts (전문)
    - relay/tests/fake-gateway.test.ts (헬퍼 자체 테스트 형식)
    - relay/tests/session.test.ts (실 DmaClient + 가짜 게이트웨이 대기 헬퍼 형식)
    - relay/tests/hub.test.ts (unhandledFrameCount 단언 위치 · 없으면 name-refresh.test.ts / fanout.test.ts 의 사용례)
    - relay/src/journal/observer.ts · relay/src/journal/codec.ts (Task 2 결과)
  </read_first>
  <behavior>
    - 로그인 성공 → pushJournalBatch([1,2], caughtUp) → 가짜 writer.push 가 레코드 2건(필드 일치) · observer.state live
    - hardClose → DmaClient 재접속 → 두 번째 로그인 요청의 since_seq 2 · epoch "ep-1"
    - 로그인 거부 응답 → observer rejected · 이후 3초 동안 게이트웨이가 받은 관찰자 로그인 요청 1건뿐
    - 관찰자 소켓에 76 전송 → state 불변 · warn/error 로그 0
    - 전 시나리오에서 게이트웨이가 관찰자 연결에서 받은 msg_type 집합 ⊆ {관찰자 로그인 요청, LivePing}
    - 사용자 세션(hub)에 관찰자 응답 번호 프레임 도착 → `unhandledFrameCount()` 0 유지 · warn 1
  </behavior>
  <action>
`relay/tests/helpers/fake-gateway.ts` 에 관찰자 모드를 더한다: `readObserverLoginRequest(msgType, payload): { secret; sinceSeq; epoch; client } | null`(기존 `read*Request` 형식) · 옵션/메서드 `respondObserverLogin(resp)`(관찰자 로그인 요청이 오면 그 연결에 로그인 응답을 자동 송신 — 일반 `LoginReq` 자동 응답과 분리) · `pushJournalBatch(sock, input)` · `observerLoginRequests()`(누적) · `waitForObserverConnection(timeoutMs)`. 기존 API·기본 동작은 바꾸지 않는다(`fake-gateway.test.ts` 무수정 green).
`relay/tests/journal-gateway.test.ts` — 실 `DmaClient`(127.0.0.1 · 가짜 게이트웨이 포트) + `createJournalCodec()` + `JournalObserver` + 가짜 writer(`readCursor` 는 {epoch:"",lastSeq:0} · `push` 기록 · `lastReceivedSeq`/`epoch` 는 push·beginEpoch 로 갱신하는 작은 구현) + 가짜 access 로 `<behavior>` 1~5. 대기에는 기존 테스트의 `waitFor`/`flushIo` 형식을 쓰고, 거부 뒤 「재시도 0」 은 백오프 첫 지연(1초)보다 긴 실시간 대기(3초)로 본다.
`relay/tests/hub.test.ts` 에 `<behavior>` 6 케이스 — 사용자 세션 프레임 경로로 관찰자 응답 번호 프레임(frames.ts 빌더)을 흘려 `unhandledFrameCount()` 0 과 warn 을 단언한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay exec vitest run tests/journal-gateway.test.ts tests/hub.test.ts tests/fake-gateway.test.ts tests/journal-observer.test.ts</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 "Test Files" 요약줄에 failed</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/relay run test</automated>
    <fails_when>non-zero exit, 또는 "error TS", 또는 vitest 요약줄에 failed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "respondObserverLogin\|pushJournalBatch" relay/tests/helpers/fake-gateway.ts` >= 2
    - plan 시작 시 `git rev-parse HEAD` 를 PLAN_BASE 로 SUMMARY 에 적고, `git diff --stat "$PLAN_BASE"..HEAD -- relay/src/journal/observer.ts relay/src/journal/writer.ts` 출력 없음 — 이 plan 이 관찰자·기록기를 수정하지 않았다(코덱 스왑이 깨끗했다)
    - relay 전체 테스트 green
  </acceptance_criteria>
  <done>실제 TCP 위에서 관찰자가 로그인 · 배치 수신 · 끊김 이어받기 · 거부 정지를 해내고, 게이트웨이에는 로그인과 핑 외에 아무것도 보내지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gh-trade 저장소 → gh-radar 생성물 | 계약 파일은 gh-trade 가 소유 — gh-radar 는 스크립트 결과만 커밋 |
| 게이트웨이 바이트 → relay 파서 | JS 런타임에 FlatBuffers Verifier 없음 — 화이트리스트·상한이 구조 방어선 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-32 | Tampering | 생성물 손편집·버전 불일치 | medium | mitigate | sync 스크립트만 · `--check` 차이 0 · flatc 25.12.19 확인 · 별도 커밋 |
| T-19-33 | Denial of Service | 거대 벡터(레코드·계좌) | medium | mitigate | `takeCount` 상한 500 · 1024 |
| T-19-34 | Denial of Service | 레코드 드롭 → seq 갭 재접속 루프 | medium | mitigate | 레코드는 버리지 않고 warn(파서 규율) — 투영 CHECK·포이즌 격리가 DB 에서 받는다 |
| T-19-35 | Integrity | 사용자 세션에 관찰자 프레임 → 불변식 파손 | low | mitigate | 화이트리스트와 hub 명시 case 한 커밋 · hub 테스트 |
| T-19-04 | Elevation of Privilege | 관찰자 연결의 추가 요청 | high | mitigate | 게이트웨이가 받은 요청 종류 ⊆ {로그인, LivePing} 통합 단언(relay 몫) · 서버측 강제는 gh-trade(19-11 G2 확인) |
| T-19-03 | Information Disclosure | 로그인 요청 비밀 | high | mitigate | 빌더가 비밀을 로그하지 않음 · 테스트 픽스처 비밀은 가짜 값 |
</threat_model>

<verification>
- `sync-relay-schema.sh --check` 차이 0 · relay 전체 테스트 · typecheck 2종 green.
- spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</verification>

<success_criteria>
- G1 계약이 생성물·코덱에 반영되고 관찰자·기록기는 무수정으로 실 TCP 통합을 통과.
</success_criteria>

## Artifacts this phase produces (이 plan 분)

| 종류 | 이름 |
|------|------|
| 상수·번호 | `MSG.ObserverLoginReq`(제안 5) · `MSG.ObserverLoginResp`(제안 79) · `MSG.JournalBatch`(제안 80) — G1 확정 이름·값 · `INBOUND_MSG_TYPES` +2 · `MAX_JOURNAL_BATCH_RECORDS = 500` · `MAX_OBSERVER_ACCOUNT_COUNT = 1024` |
| 함수 | `buildObserverLoginReq` · `parseObserverLoginResp` · `parseJournalBatch` · `createJournalCodec` |
| 파일 | `relay/src/journal/codec.ts` · `relay/tests/journal-codec.test.ts` · `relay/tests/journal-gateway.test.ts` · 재생성 `relay/src/generated/**`(SYNC MARKER = G1 해시) |
| 테스트 헬퍼 | `buildObserverLoginRespFrame` · `buildJournalBatchFrame` · `FakeJournalRecordInput` · `readObserverLoginRequest` · `respondObserverLogin` · `pushJournalBatch` · `observerLoginRequests` · `waitForObserverConnection` |
| hub | 관찰자 응답 번호 명시 무시 case |

<output>
Create `.planning/phases/19-account-order-journal/19-09-SUMMARY.md` when done
</output>
