---
phase: 19-account-order-journal
plan: 10
type: execute
wave: 6
depends_on: ["19-09"]
files_modified:
  - relay/src/index.ts
  - relay/tests/journal-boot.test.ts
  - relay/tests/helpers/supabase-stub.ts
  - scripts/deploy-relay.sh
  - scripts/setup-relay-iam.sh
  - ops/alert-relay-down.yaml
  - infra/relay/README.md
  - docs/relay-operations.md
autonomous: true
requirements: [D-04, D-10, D-13, D-14]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-13: 실제 relay 프로세스가 부팅하면 사용자 접속 없이도 관찰자 연결 1개를 연다(커서 읽기 → 로그인 → 매핑 동기화) — 사용자 DMA 세션은 여전히 wss 인증에서만 생긴다"
    - "D-13 · 종료: SIGTERM 은 1) HTTP 2개 close 2) fanout.closeAll 3) sessionManager.closeAll 4) 관찰자 stop 5) 기록기 drain(상한 2초) · close 6) hub·symbols 순으로 5초 안에 끝나고, drain 을 못 끝내도 커서가 RPC 안에서만 전진하므로 유실 0 이다"
    - "D-03 결선: 기록기 applied → `fanout.deliverJournalRows` · JournalStatus frame → `fanout.deliverJournalState` · `/healthz` journal = JournalStatus — 한 원천"
    - "D-10 · T-19-03: 배포 스크립트가 Secret Manager `gh-radar-dma-observer-secret` 의 존재·ENABLED 버전·relay SA 접근권을 사전 검사하고, 값을 tmpfs env-file(0600)에만 `DMA_OBSERVER_SECRET` 으로 싣는다 · 로그·명령줄·저장소에 평문 0"
    - "D-04 (b) 운영 문서: 알림 정책 문서 표에 `503 {journal.state: rejected|connecting|db_error}` → 관찰자 기록 연결 행과 대응 절차가 있다(정책 조건 불변 — 기존 uptime 에 편승)"
    - "production 에서 비밀이 빠지면 relay 가 기동을 거부한다(부팅 테스트가 프로세스 종료 코드와 문구로 확인)"
  artifacts:
    - path: relay/src/index.ts
      provides: "관찰자·기록기·매핑·상태 결선 + 종료 순서"
      contains: "journalObserver.start()"
    - path: relay/tests/journal-boot.test.ts
      provides: "실 프로세스 부팅 종단 테스트(가짜 게이트웨이 + Supabase 스텁)"
    - path: scripts/deploy-relay.sh
      provides: "관찰자 비밀 4번째 주입"
      contains: "gh-radar-dma-observer-secret"
  key_links:
    - from: "relay/src/index.ts"
      to: "WsFanout.deliverJournalRows / deliverJournalState"
      via: "journalWriter.on('applied') · journalStatus.on('frame')"
      pattern: "deliverJournalRows"
    - from: "scripts/deploy-relay.sh"
      to: "DMA_OBSERVER_SECRET"
      via: "fetch_secret → env-file"
      pattern: "DMA_OBSERVER_SECRET"
  prohibitions:
    - "MUST NOT relay 를 배포하거나 비밀 값을 만든다 — 19-11/19-12 에서 사람 승인 뒤"
    - "MUST NOT 비밀 값을 echo · 로그 · 명령줄 인자로 다룬다"
    - "MUST NOT 알림 정책 조건(5분 창 · 0.9 임계 · 싱가포르 AND)을 바꾼다 — 문서 행만"
---

<objective>
관찰자 경로를 relay **부팅 결선**에 꽂고, 배포·IAM 스크립트와 운영 문서를 새 비밀·새 알림 신호에 맞춘다. tracer 는 「실제 relay 프로세스(`tsx src/index.ts`) 부팅 → 가짜 게이트웨이에 관찰자 로그인 → 저널 배치 1건 → Supabase 스텁이 적용 RPC 수신 → `/healthz` journal.state live → SIGTERM 정상 종료」 한 경로다.

Purpose: D-13(부팅 즉시 상시 연결) · D-10(비밀 공급 경로) · D-04 (b)(운영 알림 문서) · D-14 준비(전환 창에서 스크립트가 그대로 돌아야 한다). 부팅 결선은 단위 테스트가 못 잡는 곳(순서·누락 결선·종료 순서)이라 실제 프로세스로 증명한다.
Output: `index.ts` 결선 · 부팅 테스트 + Supabase 스텁 헬퍼 · 배포/IAM 스크립트 · 운영 문서 3종.

커밋 규율: 한글 메시지 · Co-Authored-By 없음 · 파일 지정 add · `git status -sb` · **push 금지** · 배포 금지.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-account-order-journal/19-CONTEXT.md
@.planning/phases/19-account-order-journal/19-09-SUMMARY.md

<interfaces>
relay journal 모듈(19-05·19-07·19-09): `new JournalWriter({ supabase, gateway })`(readCursor · push · drain(ms) · close · 이벤트 applied/health) · `new JournalAccess({ supabase, gateway })`(replace · accountsOf · close) · `new JournalObserver({ secret, gateway, host, port, codec, writer, access })`(start · stop · state · headSeq) · `new JournalStatus({ observer, writer })`(frame · health · close · 이벤트 frame) · `createJournalCodec()`. `WsFanoutDeps.journalAccess` · `WsFanoutDeps.journalState` · `WsFanout.deliverJournalRows` · `WsFanout.deliverJournalState`. `OrderApiDeps.journal`. `RelayConfig.dmaObserverSecret`(production 필수) · `RelayConfig.dmaBroker`(gateway 키, 기본 "KB").
`relay/src/index.ts` — 1-45 머리 주석(종료 절차 목록 · 「부팅 시 DMA 게이트웨이에 미리 접속하지 않는다」) · 결선 순서 config → supabase → sessionManager → symbols → hub → fanout → orderApi → listen · shutdown 5초 데드맨.
테스트 선례: `webapp/e2e/fixtures/relay.ts` 528-620 `withLocalRelay`(tsx 로 실 relay spawn · env · `waitForRelay` healthz 200 대기) · 320-460 `startSupabaseStub`(auth.getUser · dma_credentials · stocks · dma_orders 경로). `relay/tests/helpers/fake-gateway.ts` 관찰자 모드(19-09: `respondObserverLogin` · `pushJournalBatch` · `observerLoginRequests` · `waitForObserverConnection`).
배포: `scripts/deploy-relay.sh` 182-202 비밀 사전 검사 루프 · 287-319 `fetch_secret` · 빈 값 검사 · env-file 생성 · 411-470 uptime/정책 멱등 적용 · `scripts/setup-relay-iam.sh` 175-190 Secret 껍데기 생성 + accessor 루프 · `ops/alert-relay-down.yaml` documentation 표 · `infra/relay/README.md` 188 「Secret 3종 상태」 · 1008 「Secret 3종 값 주입」 · `docs/relay-operations.md` 운영 지식 목록.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] 실 relay 프로세스 부팅 → 관찰자 로그인 → 배치 → 적용 RPC → healthz live → SIGTERM (index.ts 결선)</name>
  <files>relay/src/index.ts, relay/tests/journal-boot.test.ts, relay/tests/helpers/supabase-stub.ts</files>
  <read_first>
    - relay/src/index.ts (전문)
    - relay/src/journal/observer.ts · status.ts · writer.ts · access.ts · codec.ts (결과물 — 생성자 deps)
    - relay/src/ws/fanout.ts (WsFanoutDeps · deliverJournalRows · deliverJournalState)
    - relay/src/order/order-api.ts (OrderApiDeps)
    - webapp/e2e/fixtures/relay.ts (296-460 waitForRelay · Supabase 스텁 · 528-620 spawn)
    - relay/tests/helpers/fake-gateway.ts (관찰자 모드)
  </read_first>
  <behavior>
    - env(NODE_ENV test · DMA_HOST 127.0.0.1 · DMA_PORT 가짜 게이트웨이 · DMA_OBSERVER_SECRET "boot-test-secret" · SUPABASE_URL 스텁 · 빈 포트 2개)로 spawn → healthz 200
    - 스텁이 `dma_journal_cursor`(gateway=eq.KB) 조회 1회 수신 → 게이트웨이가 관찰자 로그인 수신(secret 일치 · since 0 · epoch "")
    - 로그인 응답(accounts 2) → 스텁이 `rpc/dma_journal_sync_access`(p_gateway KB · p_rows 2) 수신
    - pushJournalBatch([seq 1], caughtUp) → 스텁이 `rpc/dma_journal_apply`(p_events[0].seq 1) 수신 → healthz journal.state "live" · journal.lastSeq 1
    - SIGTERM → 5초 안에 종료 코드 0 · 수집한 stdout/stderr 에 "boot-test-secret" 문자열 없음
    - NODE_ENV production + DMA_OBSERVER_SECRET 없음(나머지 필수 env 있음) → 5초 안에 비정상 종료 · 출력에 "DMA_OBSERVER_SECRET must be set"
    - DMA_OBSERVER_SECRET 없음 + NODE_ENV test → healthz 200 · journal.state "disabled" · 게이트웨이 관찰자 로그인 0
  </behavior>
  <action>
**결선**(`relay/src/index.ts`): supabase 생성 뒤 `journalWriter = new JournalWriter({ supabase, gateway: config.dmaBroker })` · `journalAccess = new JournalAccess({ supabase, gateway: config.dmaBroker })` · `journalObserver = new JournalObserver({ secret: config.dmaObserverSecret, gateway: config.dmaBroker, host: config.dmaHost, port: config.dmaPort, codec: createJournalCodec(), writer: journalWriter, access: journalAccess })` · `journalStatus = new JournalStatus({ observer: journalObserver, writer: journalWriter })`. `WsFanout` deps 에 `journalAccess` · `journalState: journalStatus` 를 넣고 `journalWriter.on("applied", (rows) => fanout.deliverJournalRows(rows))` · `journalStatus.on("frame", (frame) => fanout.deliverJournalState(frame))`. `createOrderApi` 에 `journal: journalStatus`. 결선 직후 `journalObserver.start()`(D-13 — 부팅 즉시, 장 시간과 무관). 부팅 로그에 `journalObserver: enabled|disabled` 만 싣는다(비밀·계좌 없음). 종료 절차를 `must_haves` 의 6단계로 바꾸고(`JOURNAL_DRAIN_TIMEOUT_MS = 2_000` · 5초 데드맨 유지) 머리 주석의 종료 목록과 「하지 않는 것 — 부팅 시 DMA 게이트웨이에 미리 접속하지 않는다」 를 「사용자 세션은 그렇다 · 관찰자 연결 1개는 예외(Phase 19 D-13)」 로 고친다.
**Supabase 스텁**(`relay/tests/helpers/supabase-stub.ts`): webapp e2e 픽스처의 스텁 형식(node:http · 임의 포트)을 relay 테스트 헬퍼로 — `GET /rest/v1/stocks`(빈 배열) · `GET /rest/v1/dma_journal_cursor`(시드 가능 · 기본 빈 배열) · `POST /rest/v1/rpc/dma_journal_sync_access`(행 수 반환) · `POST /rest/v1/rpc/dma_journal_apply`(입력 seq 로 `{applied, skipped:0, errors:[], last_seq, rows:[]}` 반환) · 모든 요청을 (method, path, query, body) 로 기록 · `close()`. relay 부팅이 부르는 다른 경로가 있으면 빈 응답으로 받고 기록한다(누락 경로를 테스트 출력에서 보이게).
**부팅 테스트**(`relay/tests/journal-boot.test.ts`): `relay/node_modules/.bin/tsx src/index.ts` 를 `child_process.spawn`(cwd relay) — 포트는 빈 포트, `DMA_HOST=127.0.0.1` 명시(실서버 주소 리터럴 금지 D-27). `<behavior>` 7항목을 describe 하나에(각 케이스 뒤 프로세스·게이트웨이·스텁 정리 — 남으면 vitest 가 매달린다). 테스트 타임아웃 30초. 실패 시 수집 로그를 메시지에 붙인다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay exec vitest run tests/journal-boot.test.ts</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 "Test Files" 요약줄에 failed, 또는 "Hanging process"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/relay run test</automated>
    <fails_when>non-zero exit, 또는 "error TS", 또는 vitest 요약줄에 failed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "journalObserver.start()" relay/src/index.ts` >= 1 · `grep -n "deliverJournalRows\|deliverJournalState" relay/src/index.ts` >= 2
    - 종료 절차에서 `sessionManager.closeAll` 뒤에 관찰자 stop, 그 뒤 기록기 drain 이 온다 — `grep -n "sessionManager.closeAll\|journalObserver.stop\|journalWriter.drain" relay/src/index.ts` 의 줄 번호가 이 순서로 증가
    - 부팅 테스트가 비밀 문자열 부재를 단언한다
  </acceptance_criteria>
  <done>relay 를 켜기만 하면 관찰자가 붙어 기록을 시작하고, 끄면 순서대로 정리되며 비밀은 어디에도 찍히지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: 배포·IAM 스크립트 관찰자 비밀 · 알림 정책 문서 행 · 운영 문서(비밀 주입·전환·롤백)</name>
  <files>scripts/deploy-relay.sh, scripts/setup-relay-iam.sh, ops/alert-relay-down.yaml, infra/relay/README.md, docs/relay-operations.md</files>
  <read_first>
    - scripts/deploy-relay.sh (1-60 · 180-205 · 285-340)
    - scripts/setup-relay-iam.sh (170-215)
    - ops/alert-relay-down.yaml (전문)
    - infra/relay/README.md (180-200 · 1000-1060)
    - docs/relay-operations.md (전문)
    - .planning/phases/19-account-order-journal/19-RESEARCH.md (496-508 Runtime State Inventory · 669-690 배포 순서 · 롤백)
  </read_first>
  <action>
`scripts/deploy-relay.sh`: 사전 검사 루프에 `gh-radar-dma-observer-secret` 추가(존재 · ENABLED 버전 · relay SA 접근권 — 기존 3종과 같은 판정), `OBS_SECRET="$(fetch_secret gh-radar-dma-observer-secret)"` · 빈 값 검사 `[ -n "$OBS_SECRET" ] || { echo "빈 비밀: dma observer secret" >&2; exit 1; }` · env-file 에 `DMA_OBSERVER_SECRET` 한 줄. 「비밀 3종」 표현을 「비밀 4종」 으로 바꾸고(주석·로그·README 참조), 머리 주석의 비밀 목록에 관찰자 비밀과 근거(Phase 19 D-10 · gh-trade `config/observer.toml` 과 같은 값)를 더한다. 값은 어디에도 출력하지 않는다.
`scripts/setup-relay-iam.sh`: Secret 껍데기 생성 + accessor 루프에 `gh-radar-dma-observer-secret` 추가(값은 만들지 않는다 — 기존 규율).
`ops/alert-relay-down.yaml` documentation: 「먼저 한 줄로 범위를 좁힌다」 표에 `503 {"journal":{"state":"rejected"}}` → 관찰자 비밀 불일치(Secret Manager 값 vs 120 `config/observer.toml`) · `503 {"journal":{"state":"connecting"|"logging_in"|"replaying"}}` 180초 이상 → 게이트웨이 관찰자 미지원·재시작 중·VPN · `503 {"journal":{"state":"db_error"}}` → Supabase 적용 RPC 실패 행을 더하고, 원인 절에 대응 절차(relay 로그 `[JOURNAL]`·`[journal]` · `dma_journal_cursor` · `dma_journal_events.apply_error` 조회 · 장 밖에서는 503 이 아니라 본문에만 드러난다는 점 · 알림까지 ≈ 3분 + 5분 창)를 적는다. 정책 조건·임계는 바꾸지 않는다.
`infra/relay/README.md`: 「Secret 3종」 절들을 4종으로 갱신하고 관찰자 비밀 주입 절차를 더한다 — 값은 한 번 생성해 **같은 값**을 Secret Manager 새 버전과 gh-trade 120 `config/observer.toml`(chmod 600 · git 미추적)에 넣되 터미널에 찍지 않는 파이프로만 다룬다(생성 → 두 목적지로 동시 전달하는 형태를 문장으로 설명 · 실제 값·실서버 주소 없음). 순환(로테이션) 때 두 곳을 같은 창(20:00 이후)에서 바꾸고 relay 재배포 → 게이트웨이 재시작 순서라는 점.
`docs/relay-operations.md`: 「관찰자 기록 연결(Phase 19)」 항목 — 커서(`dma_journal_cursor`)와 epoch 의미 · resync 로그 · 유실 0 근거(커서는 RPC 안에서만 전진) · 로그인 거부 = 재시작 전 복구 없음 · 롤백(relay 이전 태그 = 옛 기록 경로 부활, 동결 해제가 수용 가능한 롤백 상태 · webapp/server 는 이전 배포 · DB 는 추가 전용이라 롤백 불요) · 전환 순서 D-14(DB → gh-trade → relay → 검증 → server → webapp push).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash -n scripts/deploy-relay.sh && bash -n scripts/setup-relay-iam.sh && grep -c "gh-radar-dma-observer-secret" scripts/deploy-relay.sh scripts/setup-relay-iam.sh</automated>
    <fails_when>non-zero exit(문법 오류), 또는 두 파일 중 하나의 카운트가 0</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ruby -ryaml -e 'YAML.load_file("ops/alert-relay-down.yaml"); puts "yaml ok"'</automated>
    <fails_when>non-zero exit, 또는 출력에 "yaml ok" 가 없음</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "DMA_OBSERVER_SECRET" scripts/deploy-relay.sh` >= 1 · `grep -n "빈 비밀: dma observer secret" scripts/deploy-relay.sh` >= 1
    - `grep -n "journal" ops/alert-relay-down.yaml` >= 3 · 정책 조건 무변경 — plan 시작 SHA(PLAN_BASE)와 비교해 `git diff "$PLAN_BASE"..HEAD -- ops/alert-relay-down.yaml` 의 변경 줄이 `documentation` 블록 안에만 있다(SUMMARY 에 diff 요약)
    - `grep -n "observer.toml" infra/relay/README.md` >= 1 · `grep -n "관찰자 기록 연결" docs/relay-operations.md` >= 1
    - 추가 문서·스크립트에 실서버 IP·비밀 값 리터럴 없음 — `grep -nE "10\.41\.[0-9]+\.[0-9]+" docs/relay-operations.md` 의 새 줄 0
  </acceptance_criteria>
  <done>전환 창에서 스크립트 한 번으로 관찰자 비밀이 안전하게 주입되고, 알림이 울렸을 때 무엇을 볼지 문서가 말한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Secret Manager → VM tmpfs env-file → 컨테이너 env | 관찰자 비밀의 유일한 공급 경로 |
| relay 프로세스 부팅 | 결선 누락이 조용한 기능 부재가 되는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-03 | Information Disclosure | 관찰자 비밀(배포·로그·문서) | high | mitigate | tmpfs 0600 env-file · 빈 값 검사는 키 이름만 · 부팅 테스트 로그 부재 단언 · 문서에 값 없음 |
| T-19-36 | Denial of Service | 비밀 누락 배포(조용한 비활성) | medium | mitigate | production config fail-fast + 배포 사전 검사 4종 |
| T-19-37 | Denial of Service | 종료 순서 오류로 유실·매달림 | medium | mitigate | 6단계 순서 · drain 상한 2초 · 5초 데드맨 · 커서 RPC 내 전진 |
| T-19-38 | Repudiation | 알림 후 대응 공백 | low | mitigate | 정책 문서 행 · 운영 문서 절차 |
</threat_model>

<verification>
- 부팅 테스트 포함 relay 전체 green · 스크립트 문법 · yaml 파싱.
- spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</verification>

<success_criteria>
- 실제 relay 프로세스가 관찰자 경로를 끝까지 돌리고 정상 종료.
- 배포·IAM·문서가 관찰자 비밀과 알림 신호를 포함.
</success_criteria>

## Artifacts this phase produces (이 plan 분)

| 종류 | 이름 |
|------|------|
| 결선 | `index.ts` — `journalWriter` · `journalAccess` · `journalObserver` · `journalStatus` · 종료 6단계 · `JOURNAL_DRAIN_TIMEOUT_MS = 2_000` |
| 테스트·헬퍼 | `relay/tests/journal-boot.test.ts` · `relay/tests/helpers/supabase-stub.ts` |
| 비밀·env | Secret Manager `gh-radar-dma-observer-secret` (스크립트 참조 — 값은 19-11) · env `DMA_OBSERVER_SECRET` |
| 문서 | 알림 정책 journal 행 · README 관찰자 비밀 주입 절 · relay-operations 관찰자 기록 연결 항목 |

<output>
Create `.planning/phases/19-account-order-journal/19-10-SUMMARY.md` when done
</output>
