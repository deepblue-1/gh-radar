---
phase: 06.2
plan: 04
type: execute
wave: 2
depends_on: [06.2-01]
files_modified:
  - webapp/src/components/ui/popover.tsx
  - webapp/src/components/layout/app-sidebar.tsx
  - webapp/src/components/layout/user-section.tsx
  - webapp/src/app/scanner/page.tsx
  - webapp/src/app/stocks/[code]/page.tsx
  - webapp/src/components/layout/app-shell.tsx
autonomous: true
requirements: [AUTH-02, PERS-01]
requirements_addressed:
  - id: AUTH-02
    status: partial
    notes: "UserSection 팝오버로 로그아웃 UX 완성 (Google 로그인 동선 완결)."
  - id: PERS-01
    status: in_progress
    notes: "관심종목 사이드바 nav 링크 추가 (페이지 자체는 Plan 06)."

must_haves:
  truths:
    - "/scanner, /stocks/[code] 페이지 진입 시 좌측 사이드바(w-60)에 '스캐너' + '관심종목' 2개 nav 링크가 표시된다"
    - "사이드바 하단 UserSection 트리거(아바타+이름)를 클릭하면 팝오버가 열리며 이메일 + 로그아웃 버튼이 노출된다"
    - "팝오버 로그아웃 클릭 시 supabase.auth.signOut() 실행 후 /login 으로 리다이렉트된다"
    - "현재 경로와 일치하는 nav 항목은 active 스타일 (bg-accent text-accent-fg font-semibold, aria-current=page) 로 강조된다"
    - "/design 카탈로그 페이지는 hideSidebar 유지되어 사이드바 비노출 (D-17)"
  artifacts:
    - path: "webapp/src/components/layout/app-sidebar.tsx"
      provides: "AppSidebar — 2개 nav 링크 + 하단 UserSection slot"
      exports: ["AppSidebar"]
    - path: "webapp/src/components/layout/user-section.tsx"
      provides: "UserSection — Popover 트리거 (아바타+이름) + 콘텐츠 (이메일 + 로그아웃)"
      exports: ["UserSection"]
  key_links:
    - from: "webapp/src/app/scanner/page.tsx"
      to: "AppSidebar"
      via: "AppShell sidebar prop"
      pattern: "sidebar=\\{<AppSidebar"
    - from: "webapp/src/components/layout/user-section.tsx"
      to: "useAuth()"
      via: "user + signOut from context"
      pattern: "useAuth\\("
---

<objective>
AppShell 의 사이드바 레이아웃을 활성화하여 Phase 06.2 전역 UI shell 을 구축한다. 신규 `AppSidebar` 컴포넌트가 '스캐너' + '관심종목' 2개 nav 링크 + 하단 `UserSection` (Popover 트리거 + 팝오버 콘텐츠) 을 렌더한다. 기존 `/scanner`, `/stocks/[code]` 페이지의 `<AppShell hideSidebar>` prop 을 `<AppShell sidebar={<AppSidebar />}>` 로 교체하며, `/design` 만 `hideSidebar` 를 보존한다 (D-17).

Purpose: D-16 (UserSection 팝오버) + D-17 (/design 보존) + UI-SPEC §2 §3 §3.3 정확 구현.

Output: 6개 파일 (신규 3 + 수정 3). 이 plan 완료 후 로그인 상태로 /scanner 접근 시 사이드바 + UserSection 이 노출되며, UserSection 을 클릭하면 팝오버가 열린다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-auth-watchlist/06.2-CONTEXT.md
@.planning/phases/06.2-auth-watchlist/06.2-UI-SPEC.md
@.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md
@.planning/phases/06.2-auth-watchlist/06.2-01-SUMMARY.md
@webapp/src/components/layout/app-shell.tsx
@webapp/src/components/ui/popover.tsx

<interfaces>
<!-- Plan 01 산출물 -->

From webapp/src/lib/auth-context.tsx (Plan 01 Task 3):
```typescript
interface AuthContextValue {
  user: User | null;
  displayName: string | null;  // user_metadata.full_name ?? email.split('@')[0] ?? null
  isLoading: boolean;
  signOut: () => Promise<void>;  // supabase.auth.signOut() + hard redirect /login
}
export function useAuth(): AuthContextValue;
```

From webapp/src/components/ui/popover.tsx (shadcn — 이미 설치됨):
- `Popover`, `PopoverTrigger`, `PopoverContent` exports
- `asChild` prop on Trigger for button wrapping
- `side="top" align="start"` prop on Content for 사이드바 하단 방향

From webapp/src/components/layout/app-shell.tsx (기존, 변경 최소화):
- `sidebar?: ReactNode` prop
- `hideSidebar?: boolean` prop (기본 false)
- Desktop: `<aside className="hidden w-60 shrink-0 border-r border-[var(--border)] bg-[var(--muted)] p-3 lg:block">{sidebar}</aside>`
- Mobile: Sheet side="left" drawer (hamburger 트리거)

From UI-SPEC §2, §3 (사이드바 + UserSection 레이아웃):
- 사이드바 내부: `flex h-full flex-col justify-between` → top=nav, bottom=UserSection
- Nav 항목: Link + `flex items-center gap-2 rounded-[var(--r)] px-3 py-2 text-[length:var(--t-sm)]`
- Active: `bg-[var(--accent)] text-[var(--accent-fg)] font-semibold`
- Inactive: `text-[var(--muted-fg)] hover:bg-[color-mix(in_oklch,var(--muted)_60%,transparent)] hover:text-[var(--fg)]`
- 아이콘: `Activity` (스캐너), `Star` (관심종목), `LogOut` (로그아웃)

From UI-SPEC §3.2 (아바타 규칙):
- 1순위: `user.user_metadata.avatar_url` (img 32×32 rounded-full)
- 2순위: 이메일 로컬파트 첫 글자 대문자 이니셜 (`flex size-8 items-center justify-center rounded-full bg-[var(--muted-fg)]/15 text-sm font-semibold text-[var(--fg)] uppercase`)

From UI-SPEC §3.3 (팝오버 콘텐츠):
- `w-56` + `p-2` + `bg-[var(--card)]` + `border-[var(--border)]` + `shadow-sm`
- 상단 헤더: 아바타 40×40 + 이름 (t-sm semibold) + 이메일 (t-caption muted-fg truncate)
- `<Separator />`
- 로그아웃 Button `variant="ghost" size="sm" className="w-full justify-start gap-2"` + `<LogOut />` + "로그아웃"
- Hover 시 `text-[var(--destructive)]`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: UserSection 컴포넌트 — Popover 트리거 + 콘텐츠</name>

  <read_first>
    - /Users/alex/repos/gh-radar/webapp/src/components/ui/popover.tsx (기존 shadcn — 인터페이스 확인)
    - /Users/alex/repos/gh-radar/webapp/src/components/ui/separator.tsx (기존 shadcn)
    - /Users/alex/repos/gh-radar/webapp/src/components/ui/button.tsx (기존 shadcn)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-UI-SPEC.md §"3. UserSection 팝오버 규칙"
    - /Users/alex/repos/gh-radar/webapp/src/lib/auth-context.tsx (Plan 01 산출물 — useAuth API)
  </read_first>

  <files>
    webapp/src/components/layout/user-section.tsx
  </files>

  <action>
    `webapp/src/components/layout/user-section.tsx` 신규 작성. 구조:

    ```typescript
    "use client";
    import { LogOut, User as UserIcon } from "lucide-react";
    import { useState } from "react";
    import { useAuth } from "@/lib/auth-context";
    import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover";
    import { Separator } from "@/components/ui/separator";
    import { Button } from "@/components/ui/button";
    import { cn } from "@/lib/utils";

    export function UserSection() {
      const { user, displayName, signOut } = useAuth();
      const [imgError, setImgError] = useState(false);
      if (!user) return null;  // middleware 가드가 벽이지만 방어

      const email = user.email ?? "";
      const avatarUrl = user.user_metadata?.avatar_url as string | undefined;
      const initial = (email[0] ?? "?").toUpperCase();
      const name = displayName ?? "사용자";

      // 트리거 + 팝오버 JSX (아래 지침)
    }
    ```

    **트리거 UI (버튼 wrapping `PopoverTrigger asChild`):**
    - `<button className="flex w-full items-center gap-2 rounded-md p-2 hover:bg-[var(--bg)] text-left" aria-haspopup="menu">`
    - 내부: 아바타 (32×32) + 이름 (`<span className="truncate max-w-[140px] text-[length:var(--t-sm)] font-semibold text-[var(--fg)]">{name}</span>`)
    - 아바타 렌더:
      - `avatarUrl && !imgError` 이면 `<img src={avatarUrl} alt="" onError={() => setImgError(true)} className="size-8 rounded-full" />`
      - 아니면 이니셜 fallback: `<span className="flex size-8 items-center justify-center rounded-full bg-[var(--muted-fg)]/15 text-[length:var(--t-sm)] font-semibold text-[var(--fg)] uppercase">{initial}</span>`

    **팝오버 콘텐츠 (`PopoverContent side="top" align="start"`):**
    - className: `"w-56 p-2 bg-[var(--card)] border-[var(--border)] shadow-sm"`
    - 상단 헤더 div: `<div className="flex items-center gap-2 px-2 py-2">`
      - 아바타 40×40 (size-10) — 트리거와 동일 렌더 규칙 (avatarUrl → img, else 이니셜)
      - 이름/이메일 column:
        - `<span className="truncate text-[length:var(--t-sm)] font-semibold text-[var(--fg)]">{name}</span>`
        - `<span className="truncate text-[length:var(--t-caption)] text-[var(--muted-fg)]">{email}</span>`
    - `<Separator className="my-1" />`
    - 로그아웃 Button:
      ```tsx
      <Button
        variant="ghost"
        size="sm"
        className="w-full justify-start gap-2 text-[var(--muted-fg)] hover:text-[var(--destructive)]"
        onClick={() => signOut()}
        aria-label="로그아웃"
      >
        <LogOut className="size-4" aria-hidden="true" />
        로그아웃
      </Button>
      ```

    **접근성:**
    - `aria-haspopup="menu"` on trigger
    - `aria-label="로그아웃"` on button
    - Popover `onOpenChange` Radix 자동 (ESC + 바깥클릭 닫힘)
  </action>

  <acceptance_criteria>
    - `/Users/alex/repos/gh-radar/webapp/src/components/layout/user-section.tsx` exists and exports `UserSection`.
    - File imports `useAuth` from `@/lib/auth-context`.
    - File imports `Popover`, `PopoverTrigger`, `PopoverContent` from `@/components/ui/popover`.
    - File contains `side="top"` and `align="start"` (Popover positioning).
    - File contains `signOut()` call (로그아웃 핸들러).
    - File contains `user_metadata?.avatar_url` access + `onError` fallback to initial.
    - File contains `aria-haspopup="menu"` and `aria-label="로그아웃"`.
    - File contains `"w-56"` and `bg-[var(--card)]` (팝오버 스타일).
    - File contains `hover:text-[var(--destructive)]` on 로그아웃 button.
    - `pnpm --filter @gh-radar/webapp exec tsc --noEmit` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && pnpm exec tsc --noEmit && grep -q 'useAuth' src/components/layout/user-section.tsx && grep -q 'side="top"' src/components/layout/user-section.tsx && grep -q 'signOut()' src/components/layout/user-section.tsx && grep -q 'aria-haspopup="menu"' src/components/layout/user-section.tsx</automated>
  </verify>

  <done>
    UserSection 컴포넌트 생성, Popover 팝오버 트리거+콘텐츠 구현, avatar fallback 체인, signOut 배선.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AppSidebar — 2개 nav 링크 + 하단 UserSection</name>

  <read_first>
    - /Users/alex/repos/gh-radar/webapp/src/components/layout/user-section.tsx (Task 1 산출물)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-UI-SPEC.md §"2. AppShell Sidebar + UserSection"
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"예시 3: AppSidebar + UserSection"
  </read_first>

  <files>
    webapp/src/components/layout/app-sidebar.tsx
  </files>

  <action>
    `webapp/src/components/layout/app-sidebar.tsx` 신규 작성. 구조 (RESEARCH §예시 3 기반):

    ```typescript
    "use client";
    import Link from "next/link";
    import { usePathname } from "next/navigation";
    import { Activity, Star } from "lucide-react";
    import { UserSection } from "./user-section";
    import { cn } from "@/lib/utils";

    const NAV = [
      { href: "/scanner", label: "스캐너", icon: Activity },
      { href: "/watchlist", label: "관심종목", icon: Star },
    ] as const;

    export function AppSidebar() {
      const pathname = usePathname();
      return (
        <nav aria-label="주 메뉴" className="flex h-full flex-col justify-between">
          <ul className="flex flex-col gap-1 list-none p-0 m-0">
            {NAV.map(({ href, label, icon: Icon }) => {
              const active = pathname === href;
              return (
                <li key={href}>
                  <Link
                    href={href}
                    aria-current={active ? "page" : undefined}
                    data-nav-item
                    className={cn(
                      "flex items-center gap-2 rounded-[var(--r)] px-3 py-2 text-[length:var(--t-sm)]",
                      active
                        ? "bg-[var(--accent)] text-[var(--accent-fg)] font-semibold"
                        : "text-[var(--muted-fg)] hover:bg-[color-mix(in_oklch,var(--muted)_60%,transparent)] hover:text-[var(--fg)]"
                    )}
                  >
                    <Icon className="size-4" aria-hidden="true" />
                    {label}
                  </Link>
                </li>
              );
            })}
          </ul>
          <UserSection />
        </nav>
      );
    }
    ```

    **중요:**
    - `<nav aria-label="주 메뉴">` 한글 라벨 (UI-SPEC §Accessibility Contract)
    - `data-nav-item` 속성 포함 — AppShell 의 모바일 Sheet drawer 가 이 속성을 감지해 클릭 시 자동 닫음 (app-shell.tsx 기존 로직 참조)
    - `aria-current="page"` active 링크에만 (UI-SPEC §Accessibility)
    - `ul` 태그에 `list-none p-0 m-0` — Tailwind reset 대응
  </action>

  <acceptance_criteria>
    - `/Users/alex/repos/gh-radar/webapp/src/components/layout/app-sidebar.tsx` exists and exports `AppSidebar`.
    - File contains `aria-label="주 메뉴"` (한글 nav label).
    - File contains `href: "/scanner"` and `href: "/watchlist"` (2 nav items).
    - File contains `label: "스캐너"` and `label: "관심종목"`.
    - File contains `aria-current={active ? "page" : undefined}`.
    - File contains `data-nav-item` (Sheet drawer close 연동).
    - File imports `UserSection` from `./user-section`.
    - File uses `usePathname` for active detection.
    - `pnpm --filter @gh-radar/webapp exec tsc --noEmit` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && pnpm exec tsc --noEmit && grep -q 'aria-label="주 메뉴"' src/components/layout/app-sidebar.tsx && grep -q '"/scanner"' src/components/layout/app-sidebar.tsx && grep -q '"/watchlist"' src/components/layout/app-sidebar.tsx && grep -q 'aria-current' src/components/layout/app-sidebar.tsx && grep -q 'data-nav-item' src/components/layout/app-sidebar.tsx</automated>
  </verify>

  <done>
    AppSidebar 컴포넌트 생성, 2개 nav 링크 + UserSection 배치, a11y 속성 포함.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 기존 page 에 AppSidebar 배선 (+ /design 보존)</name>

  <read_first>
    - /Users/alex/repos/gh-radar/webapp/src/app/scanner/page.tsx (현재 AppShell hideSidebar 사용)
    - /Users/alex/repos/gh-radar/webapp/src/app/stocks/[code]/page.tsx (현재 AppShell hideSidebar 사용)
    - /Users/alex/repos/gh-radar/webapp/src/app/design/page.tsx (D-17 보존 대상)
    - /Users/alex/repos/gh-radar/webapp/src/components/layout/app-shell.tsx (기존 sidebar prop 인터페이스)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-CONTEXT.md D-17 (/design 보존)
    - /Users/alex/repos/gh-radar/webapp/src/components/layout/app-sidebar.tsx (Task 2 산출물)
  </read_first>

  <files>
    webapp/src/app/scanner/page.tsx,
    webapp/src/app/stocks/[code]/page.tsx,
    webapp/src/components/layout/app-shell.tsx
  </files>

  <action>
    1. **`webapp/src/app/scanner/page.tsx`:** 기존 `<AppShell hideSidebar>` 를 `<AppShell sidebar={<AppSidebar />}>` 로 교체. `hideSidebar` prop 삭제. `import { AppSidebar } from "@/components/layout/app-sidebar"` 추가. 다른 props(nav, 기타) 및 children 은 보존.

    2. **`webapp/src/app/stocks/[code]/page.tsx`:** 위와 동일하게 `hideSidebar` 제거 + `sidebar={<AppSidebar />}` 삽입. `React.use(params)` 로직 및 StockDetailClient 호출 보존.

    3. **`webapp/src/app/design/page.tsx`:** **변경 금지** — `<AppShell hideSidebar>` 유지 (D-17). 코드 그대로 둠.

    4. **`webapp/src/components/layout/app-shell.tsx` 변경 최소화:** 현재 `hideSidebar = false` 기본값 유지 (이미 false). 내부 주석 `Phase 4 v1 은 개인화(관심종목 등)가 없으므로 기본 레이아웃에서 hideSidebar 사용` 은 Phase 06.2 기준 재작성:
       ```
       // Phase 06.2: 일반 페이지는 sidebar 를 지정하여 사용 (Auth 도입). /design 은 여전히 hideSidebar 로 스캐폴드 회귀 방지.
       ```
       다른 로직 변경 금지.

    5. Watchlist 페이지 (`webapp/src/app/watchlist/page.tsx`) 는 Plan 06 에서 생성. 본 plan 은 /scanner + /stocks/[code] 만 전환.
  </action>

  <acceptance_criteria>
    - `/Users/alex/repos/gh-radar/webapp/src/app/scanner/page.tsx` contains `sidebar={<AppSidebar` and does NOT contain `hideSidebar`.
    - `/Users/alex/repos/gh-radar/webapp/src/app/scanner/page.tsx` contains `import { AppSidebar }` from `@/components/layout/app-sidebar`.
    - `/Users/alex/repos/gh-radar/webapp/src/app/stocks/[code]/page.tsx` contains `sidebar={<AppSidebar` and does NOT contain `hideSidebar`.
    - `/Users/alex/repos/gh-radar/webapp/src/app/design/page.tsx` STILL contains `hideSidebar` (D-17 보존 검증).
    - `/Users/alex/repos/gh-radar/webapp/src/components/layout/app-shell.tsx` contains updated Phase 06.2 주석 또는 기존 주석 유지 (로직 미변경).
    - `pnpm --filter @gh-radar/webapp exec tsc --noEmit` exits 0.
    - `pnpm --filter @gh-radar/webapp build` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && pnpm exec tsc --noEmit && grep -q 'sidebar={<AppSidebar' src/app/scanner/page.tsx && ! grep -q 'hideSidebar' src/app/scanner/page.tsx && grep -q 'sidebar={<AppSidebar' src/app/stocks/\[code\]/page.tsx && ! grep -q 'hideSidebar' src/app/stocks/\[code\]/page.tsx && grep -q 'hideSidebar' src/app/design/page.tsx && pnpm build 2>&1 | tail -5</automated>
  </verify>

  <done>
    /scanner + /stocks/[code] 에 AppSidebar 배선, /design 보존, build 통과.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ AuthProvider client context | `useAuth()` 가 반환하는 user/displayName 이 UI 렌더에 직접 사용 |
| user.user_metadata.avatar_url | Google 프로필 이미지 URL — 외부 리소스 |
| UserSection signOut | 클라이언트 action → Supabase auth.signOut → hard redirect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-16 | Tampering | XSS via user_metadata (displayName, email) | mitigate | React 자동 escape. `dangerouslySetInnerHTML` 금지. `truncate` 는 CSS only — innerHTML 조작 없음. ASVS V5.3.3. Verified by: Task 1 grep (`dangerouslySetInnerHTML` 없음 확인). |
| T-06.2-17 | Tampering | user_metadata.avatar_url 이 `javascript:` 또는 `data:` URL 을 반환 | mitigate | `<img src>` 브라우저가 `javascript:` URL 무시 (스크립트 컨텍스트 아님). `data:` URL 은 이미지 렌더 가능하나 실행 컨텍스트 없음 — 이미지 크기 제약으로 저위험. 추가 방어: CSP `img-src https:` 고려 (Phase 06.2 범위 외, accept). ASVS V5.3.5. |
| T-06.2-18 | Information Disclosure | UserSection 팝오버에 이메일 평문 노출 | accept | UX 필수 요구사항 (D-16). 팝오버는 사용자 자신만 조작. 물리적 화면 노출은 사용자 책임. ASVS V8.1. |
| T-06.2-19 | Denial of Service | Broken avatar URL 로 반복 img 오류 이벤트 발생 | mitigate | `onError={() => setImgError(true)}` 로 상태 잠금 후 이니셜 fallback 영구. 반복 호출 없음. Verified by: Task 1 acceptance (onError + imgError state). |
| T-06.2-20 | Spoofing | UserSection signOut 실패 후 stale 세션 유지 | mitigate | `signOut()` 은 Plan 01 AuthProvider 에서 `window.location.href = '/login'` hard redirect — 실패 여부와 무관하게 새 로딩 시 middleware 가 재검증. Fallback `/login` 에서 다시 로그인 유도. ASVS V3.5.3. |

</threat_model>

<verification>
- **Per-task:** tsc + grep.
- **Wave-end (Plan 03 + 04):** 로컬 `pnpm dev` → 로그인 브라우저 → `/scanner` → 사이드바 노출 + 두 nav 링크 + UserSection 트리거 → 팝오버 열기 → 이메일 + 로그아웃 → 클릭 → `/login` 복귀. 이 수동 smoke 는 Plan 08 에서 E2E 로 자동화.
- **Visual:** Playwright 이전 단계 — 수동 스크린샷 1장 `sidebar-userpopover.png` 권고 (선택).
- **Regression:** `/design` 진입 시 기존대로 사이드바 없음 — Plan 08 E2E `smoke.spec.ts` 가드 추가.
</verification>

<success_criteria>
- AppSidebar + UserSection 2개 컴포넌트 생성
- /scanner + /stocks/[code] 가 sidebar={<AppSidebar />} 사용, hideSidebar 제거
- /design 은 hideSidebar 유지 (D-17 회귀 방지)
- build 통과
- Popover a11y 속성 (side, align, aria-haspopup, aria-label) 포함
</success_criteria>

<output>
`.planning/phases/06.2-auth-watchlist/06.2-04-SUMMARY.md`:
- 3개 신규 + 3개 수정 파일 목록
- 트리거 + 팝오버 JSX 구조 요약
- avatar fallback 체인 (Google img → onError → 이니셜)
- /design 보존 재확인
- Plan 06 (Watchlist 페이지) 가 사용할 인터페이스 — `<AppSidebar />` 를 AppShell 의 sidebar prop 에 넘김
</output>
