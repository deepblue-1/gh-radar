---
phase: 06.2
plan: 10
type: execute
wave: 5
depends_on: [06.2-08]
files_modified:
  - webapp/src/app/mockup
  - webapp/src/lib/supabase/middleware.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [AUTH-01, AUTH-02, PERS-01]
requirements_addressed:
  - id: AUTH-01
    status: deferred
    notes: "Google OAuth 로 부분 충족, 이메일/비밀번호 UI 는 별도 phase 로 정식 이관."
  - id: AUTH-02
    status: partial
    notes: "Google 완료, Kakao 별도 phase. REQUIREMENTS.md status 갱신."
  - id: PERS-01
    status: complete
    notes: "Watchlist CRUD 완료. REQUIREMENTS.md status 갱신."

must_haves:
  truths:
    - "/mockup/06-2 디렉터리가 webapp 트리에서 제거되었다 (디자인 결정 종료, 회귀 가드는 E2E 가 담당)"
    - "middleware matcher 의 mockup 제외 규칙이 정리된다 (전체 mockup 디렉터리 삭제 시 자연스럽게 무효 — 또는 명시적 제거)"
    - ".planning/REQUIREMENTS.md 의 AUTH-01/AUTH-02/PERS-01 status 가 갱신되어 traceability 표가 정확하다"
    - "git diff 가 깨끗 (build/test 회귀 0)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "갱신된 traceability 표"
      contains: "AUTH-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 06.2 완료 표시"
      via: "Status 컬럼"
      pattern: "AUTH-02.*Partial|PERS-01.*Complete"
---

<objective>
Phase 06.2 마무리 작업 — 더 이상 필요 없는 디자인 mockup 디렉터리 (`webapp/src/app/mockup/06-2`) 정리, middleware matcher 의 `mockup` 제외 규칙 검토, REQUIREMENTS.md 의 AUTH-01/AUTH-02/PERS-01 상태 갱신.

Purpose: Phase 회귀 가드 책임을 mockup → E2E 로 이관 (UI 결정이 본진 코드에 통합되었으므로 더 이상 mockup 비교 불필요). REQUIREMENTS traceability 정확도 유지.

Output: 3개 변경 (디렉터리 삭제 + middleware 라인 + REQUIREMENTS 표). build + E2E 회귀 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/06.2-auth-watchlist/06.2-CONTEXT.md
@.planning/phases/06.2-auth-watchlist/06.2-08-SUMMARY.md
@webapp/src/lib/supabase/middleware.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: /mockup/06-2 디렉터리 삭제 + middleware matcher 정리</name>

  <read_first>
    - /Users/alex/repos/gh-radar/webapp/src/app/mockup/ (전체 트리 ls 로 확인)
    - /Users/alex/repos/gh-radar/webapp/src/lib/supabase/middleware.ts (matcher 의 mockup 제외 라인)
    - /Users/alex/repos/gh-radar/webapp/middleware.ts (config.matcher)
  </read_first>

  <files>
    webapp/src/app/mockup,
    webapp/middleware.ts
  </files>

  <action>
    1. **`webapp/src/app/mockup/06-2` 디렉터리 삭제** (또는 mockup 전체 디렉터리 — 다른 phase 의 mockup 잔재가 없을 때):
       - `ls webapp/src/app/mockup/` 로 06-2 외 다른 디렉터리 존재 확인:
         - 없으면 → `rm -rf webapp/src/app/mockup` (전체)
         - 있으면 → `rm -rf webapp/src/app/mockup/06-2` (해당 디렉터리만)
       - 변경 후 `pnpm --filter @gh-radar/webapp build` 통과 확인 (불필요 import 없음).

    2. **middleware matcher 검토:**
       - `webapp/middleware.ts` 의 `config.matcher` 에서 `mockup` 제외 패턴이 남아있는지 확인.
       - **mockup 전체 삭제했으면 matcher 의 `mockup` 토큰 제거** (불필요해짐):
         ```typescript
         // 변경 전
         "/((?!_next/static|_next/image|favicon\\.ico|mockup|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)",
         // 변경 후
         "/((?!_next/static|_next/image|favicon\\.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)",
         ```
       - mockup 일부만 삭제했으면 → matcher 그대로 유지.

    3. 변경 후:
       - `pnpm --filter @gh-radar/webapp build` exit 0
       - `pnpm --filter @gh-radar/webapp test` exit 0
  </action>

  <acceptance_criteria>
    - `/Users/alex/repos/gh-radar/webapp/src/app/mockup/06-2` does NOT exist (디렉터리 삭제 확인).
    - 만약 mockup 전체 삭제: `/Users/alex/repos/gh-radar/webapp/src/app/mockup` 부재 + `webapp/middleware.ts` 의 `mockup` 토큰 제거.
    - 만약 mockup 일부만 삭제: middleware matcher 변경 없음.
    - `pnpm --filter @gh-radar/webapp build` exits 0.
    - `pnpm --filter @gh-radar/webapp test` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && ! test -d src/app/mockup/06-2 && pnpm build 2>&1 | tail -5 && pnpm test 2>&1 | tail -5</automated>
  </verify>

  <done>
    /mockup/06-2 삭제, build/test 회귀 없음, middleware matcher 정합 확인.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: REQUIREMENTS.md AUTH-01/AUTH-02/PERS-01 status 갱신</name>

  <read_first>
    - /Users/alex/repos/gh-radar/.planning/REQUIREMENTS.md (현재 traceability 표)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-CONTEXT.md (D-08, D-09 — Kakao/Email 제외)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"Phase Requirements" (status 권고)
  </read_first>

  <files>
    .planning/REQUIREMENTS.md
  </files>

  <action>
    `.planning/REQUIREMENTS.md` 수정 — 다음 부분만 수정 (다른 라인 보존):

    1. **§Authentication 섹션:**
       - `- [ ] **AUTH-01**: 이메일/비밀번호 로그인 (Supabase Auth 기반)`
         → `- [ ] **AUTH-01**: 이메일/비밀번호 로그인 (Supabase Auth 기반) — Phase 06.2 Deferred (Google OAuth 우선, 이메일/비밀번호는 별도 phase)`
       - `- [ ] **AUTH-02**: 소셜 로그인 (Google, Kakao)`
         → `- [x] **AUTH-02**: 소셜 로그인 (Google 완료, Kakao 별도 phase) — Phase 06.2`

    2. **§Personalization 섹션:**
       - `- [ ] **PERS-01**: 관심종목 저장 및 관리 (로그인 계정별 watchlist CRUD)`
         → `- [x] **PERS-01**: 관심종목 저장 및 관리 (로그인 계정별 watchlist CRUD) — Phase 06.2`

    3. **§Traceability 표:**
       - `| AUTH-01 | Phase 6.2 | Pending |` → `| AUTH-01 | Phase 6.2 | Deferred |`
       - `| AUTH-02 | Phase 6.2 | Pending |` → `| AUTH-02 | Phase 6.2 | Partial (Google 완료, Kakao 별도) |`
       - `| PERS-01 | Phase 6.2 | Pending |` → `| PERS-01 | Phase 6.2 | Complete |`

    4. **하단 갱신 일자:**
       - `*Last updated: 2026-04-16 — AUTH-01/02, PERS-01 v2→v1 승격, Phase 6.2 매핑 추가*`
         → `*Last updated: <오늘 날짜 YYYY-MM-DD> — Phase 06.2 완료, AUTH-02 Partial, PERS-01 Complete*`

    다른 라인 (다른 requirement, Out of Scope 표 등) 일체 변경 금지.
  </action>

  <acceptance_criteria>
    - `/Users/alex/repos/gh-radar/.planning/REQUIREMENTS.md` AUTH-01 라인에 `Deferred` 또는 `별도 phase` 텍스트 포함.
    - File contains `AUTH-02 | Phase 6.2 | Partial` (또는 동등 표현으로 Google 완료 명시).
    - File contains `PERS-01 | Phase 6.2 | Complete`.
    - File `Last updated` 날짜가 갱신됨 (이전 날짜 2026-04-16 보다 최신, 또는 동일 날짜라면 메시지 변경).
    - File 의 다른 requirement 항목 (SCAN-*, SRCH-*, NEWS-*, DISC-*, INFR-*) 변경 없음.
  </acceptance_criteria>

  <verify>
    <automated>grep -q 'Deferred' /Users/alex/repos/gh-radar/.planning/REQUIREMENTS.md && grep -q 'AUTH-02 | Phase 6.2 | Partial' /Users/alex/repos/gh-radar/.planning/REQUIREMENTS.md && grep -q 'PERS-01 | Phase 6.2 | Complete' /Users/alex/repos/gh-radar/.planning/REQUIREMENTS.md</automated>
  </verify>

  <done>
    REQUIREMENTS.md 의 3 requirement status 갱신, 다른 라인 보존, Last updated 갱신.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mockup 디렉터리 → 미인증 접근 허용 | 삭제 후 자연스럽게 소멸 |
| REQUIREMENTS.md ↔ 외부 traceability 도구 | 표 형식 정확도 유지 필요 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-40 | Information Disclosure | mockup 삭제 후 잔재 import 가 build 깨뜨림 | mitigate | Task 1 의 build/test 회귀 검증 (acceptance_criteria 4번). |
| T-06.2-41 | Tampering | REQUIREMENTS.md 수정 시 다른 항목 의도치 않은 변경 | mitigate | Task 2 의 acceptance_criteria 5번 — 다른 라인 변경 금지. git diff 로 line-by-line 검증. |

(이 plan 은 정리 plan — 신규 보안 위협 없음. 위 2 항목이 운영 무결성 관련 전부.)

</threat_model>

<verification>
- **Per-task:** Task 1 build/test + 디렉터리 부재 확인. Task 2 grep + git diff line count 확인.
- **Phase-end (Plan 10 완료 후 Phase 06.2 전체 종료):**
  - `pnpm --filter @gh-radar/webapp build` exit 0
  - `pnpm --filter @gh-radar/webapp test` exit 0
  - `pnpm --filter @gh-radar/webapp test:e2e` exit 0
  - `git status` 깨끗
  - `.planning/REQUIREMENTS.md` 정확
- **Manual check (Phase 종료 시점):** `/gsd-validate-phase 06.2` 실행 권고 — VALIDATION.md 의 nyquist_compliant 갱신.
</verification>

<success_criteria>
- /mockup/06-2 (또는 mockup 전체) 삭제
- middleware matcher 정합 (mockup 토큰 제거 또는 유지 결정)
- REQUIREMENTS.md AUTH-01 Deferred / AUTH-02 Partial / PERS-01 Complete
- 모든 build/test/E2E 회귀 0
</success_criteria>

<output>
`.planning/phases/06.2-auth-watchlist/06.2-10-SUMMARY.md`:
- 삭제된 mockup 디렉터리 경로
- middleware matcher 변경 전/후 (있다면)
- REQUIREMENTS.md 변경 diff 핵심 3 라인
- Phase 06.2 전체 완료 선언 + 다음 phase (Phase 7 News Ingestion) 시작 준비 완료 메시지
</output>
