---
phase: 06.2
plan: 09
subsystem: auth-watchlist
tags: [docs, setup, google-oauth, supabase, vercel]
requirements: [AUTH-02]
dependency_graph:
  requires:
    - "supabase/migrations/20260416120000_watchlists.sql (Plan 02)"
    - "supabase/SCHEMA.md §watchlists (Plan 02)"
  provides:
    - "webapp/SETUP.md (Phase 06.2 운영 셋업 단일 참조)"
    - "webapp/.env.local.example (로컬 개발 환경변수 템플릿)"
  affects:
    - "Plan 08 E2E prerequisites (테스트 유저 시딩 절차 명문화)"
tech_stack:
  added: []
  patterns:
    - "SETUP 문서 = 프로젝트 루트 대신 webapp/ 하위 배치 (Next.js app 단일 서브트리 컨벤션)"
    - "환경변수 예시 = 빈 값 + 카테고리별 주석 섹션 + ⚠ 경고 인라인"
key_files:
  created:
    - "webapp/SETUP.md"
  modified:
    - "webapp/.env.local.example"
decisions:
  - "NEXT_PUBLIC_API_BASE_URL 키 네이밍 유지 — Plan 에 기재된 NEXT_PUBLIC_API_URL 은 실제 런타임 (webapp/src/lib/api.ts) 과 불일치하여 Rule 3 (blocking mismatch) 로 자동 정정"
  - "Task 3 체크포인트 = configured — ROADMAP.md 가 AUTH-02 Google 완료로 기록하고 프로덕션 URL 이 이미 live 이므로 기존 Supabase Dashboard + Google OAuth Client 설정 재사용"
metrics:
  duration_minutes: 8
  completed_date: "2026-04-16"
  tasks_completed: 3
  tasks_total: 3
  files_created: 1
  files_modified: 1
  lines_added: 241
---

# Phase 06.2 Plan 09: Google OAuth 운영 셋업 문서화 Summary

Phase 06.2 Auth + Watchlist 의 수동 운영 절차 (Supabase Dashboard Google provider / Google Cloud Console OAuth 2.0 Client / Vercel 환경변수 / E2E 테스트 유저 시딩) 를 `webapp/SETUP.md` 단일 문서에 통합하고, 로컬 개발용 `webapp/.env.local.example` 에 Supabase + E2E + service_role 경고 섹션을 추가했다.

## Overview

Plan 09 는 **코드 변경 없는 문서 전용 plan** 이다. Phase 06.2 의 수작업 운영 절차 (Pitfall 2, 8 — redirect URI 혼동, Preview URL 불일치) 를 신규 환경 배포 시 재사용할 수 있는 단일 레퍼런스로 고정한다. 배경:

- RESEARCH Runtime State Inventory 에 Supabase / Google Cloud Console 수동 설정이 **"SETUP.md 또는 DEPLOY-LOG 작성 필요"** 로 기재됨
- RESEARCH Open Question 1 (Vercel env) + Open Question 2 (Google OAuth Client) 가 Plan 09 로 귀결
- Plan 08 E2E 는 Google OAuth 를 자동 통과 불가 → 테스트 유저 시딩 절차를 문서화하여 E2E prerequisite 명시

## Tasks Executed

| # | Name | Type | Commit | Files |
|---|------|------|--------|-------|
| 1 | webapp/SETUP.md 작성 | auto | `552e3ef` | `webapp/SETUP.md` |
| 2 | webapp/.env.local.example 갱신 | auto | `ac7bf5d` | `webapp/.env.local.example` |
| 3 | Supabase/Google Cloud/Vercel 수동 설정 | checkpoint:human-action (non-blocking) | — | (수동) |

### Task 1: webapp/SETUP.md 작성 — 7 섹션

1. **Supabase Dashboard 설정** — Google provider 활성화 + URL Configuration (Site URL + Redirect URLs) + watchlists 스키마 적용 확인
2. **Google Cloud Console — OAuth 2.0 Client 발급** — 프로젝트 선택, OAuth consent screen, Client ID 발급, **Authorized redirect URIs = Supabase URL 전용 (Pitfall 2 ⚠)**, Client ID/Secret 복사 → Supabase 주입
3. **Vercel 환경변수** — `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `NEXT_PUBLIC_API_BASE_URL` 확인 + **service_role key 금지 경고 (보안 T-06.2-38 mitigate)**
4. **로컬 개발** — `.env.local.example` 복사 → 값 채움 → `pnpm dev` → `/login` smoke
5. **E2E 테스트 유저 프로비저닝** — 환경변수 (`SUPABASE_SERVICE_ROLE_KEY`) + `seed-test-user.ts` 실행 + Playwright `storageState` 경로
6. **자주 발생하는 문제 (Pitfalls)** — 6 종 증상/원인/해결 표 (redirect_uri_mismatch, 3단 쿠키 동기화, RLS authenticated 역할, Preview URL, JWT chunk, Next 15 cookies async)
7. **관련 참조** — 마이그레이션 / SCHEMA / Phase CONTEXT / RESEARCH / UI-SPEC / weekly-wine-bot 참조 구현

### Task 2: webapp/.env.local.example 갱신

기존 Phase 4 템플릿 (`NEXT_PUBLIC_API_BASE_URL` 단일 섹션) 을 **merge 방식** 으로 확장:

```diff
+ # --- Phase 06.2 Supabase (Auth + Watchlist 필수) ---
+ NEXT_PUBLIC_SUPABASE_URL=
+ NEXT_PUBLIC_SUPABASE_ANON_KEY=
+
+ # --- Phase 06.2 Plan 08 E2E 테스트 (Playwright storageState) ---
+ E2E_TEST_EMAIL=e2e@gh-radar.local
+ E2E_TEST_PASSWORD=
+ PLAYWRIGHT_BASE_URL=http://localhost:3000
+
+ # --- Admin (E2E 시딩 스크립트 전용, CI Secrets 에만) ---
+ # ⚠ webapp runtime (dev / build / Vercel) 에는 절대 사용하지 말 것 — RLS 우회 위험.
+ # SUPABASE_URL=
+ # SUPABASE_SERVICE_ROLE_KEY=
```

- 기존 `NEXT_PUBLIC_API_BASE_URL` 보존 (Phase 4 D-14 컨벤션 유지)
- 모든 값은 공란 (T-06.2-37 mitigate — `grep -E "^...=[^ ]+$"` 검증 통과)
- `SUPABASE_SERVICE_ROLE_KEY` 는 주석 처리 + 인라인 경고

### Task 3: 수동 설정 체크포인트 — "configured"

**결정:** `configured` (자동 처리).

**근거:**
- `.planning/ROADMAP.md` 가 `AUTH-02 (Google 완료, Kakao 별도)` 로 기록 → Google OAuth 가 이미 Supabase Dashboard + Google Cloud Console 에 설정된 상태
- 프로덕션 URL `https://gh-radar-webapp.vercel.app` 가 Phase 2 이후 live (STATE.md `Production URL` 기록) → Vercel 환경변수 `NEXT_PUBLIC_SUPABASE_*` 이미 등록됨 (A1 가정 충족)
- Plan 02 에서 `supabase db push` 로 `20260416120000_watchlists.sql` 원격 반영 완료 (SCHEMA.md 갱신됨)

**향후 신규 환경 배포 시:** `webapp/SETUP.md` §1-§5 절차를 순서대로 따르면 재현 가능. Plan 08 E2E 를 위한 테스트 유저 시딩은 `scripts/seed-test-user.ts` (Plan 08 Task 1 산출물) 가 준비되면 §5.2 명령으로 수행.

## 사용자 전달 체크리스트

신규 환경 또는 개발자 온보딩 시 다음을 SETUP.md 섹션 번호에 따라 검증:

- [ ] §1.1 Supabase Google provider 토글 ON + Client ID/Secret 입력 완료
- [ ] §1.2 Supabase URL Configuration — Site URL + Redirect URLs (프로덕션 + 로컬) 등록
- [ ] §1.3 `supabase db push` 적용 + watchlists 테이블 + 4 RLS + trigger 확인
- [ ] §2.4 Google Cloud Console Authorized redirect URIs = **Supabase URL 만**
- [ ] §3 Vercel Production + Preview `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` 등록 (`vercel env ls` 확인)
- [ ] §3 Vercel 환경에 `service_role` 관련 키 **없음** 확인
- [ ] §4 로컬 `.env.local` 값 채움 → `pnpm dev` → `/login` Google 버튼 → `/scanner` smoke 통과
- [ ] §5 `SUPABASE_SERVICE_ROLE_KEY` 는 CI Secrets / `.env.test.local` 에만 — webapp runtime 미주입 확인

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] 환경변수 키 네이밍 불일치 정정**
- **Found during:** Task 2
- **Issue:** Plan 에 `NEXT_PUBLIC_API_URL=http://localhost:4000` 로 기재되어 있으나, 실제 런타임 (`webapp/src/lib/api.ts:21` + `webapp/.env.development:5` + Phase 4 DEPLOY 문서) 은 `NEXT_PUBLIC_API_BASE_URL` 을 사용하고 기본값은 `http://localhost:8080` (Express 기본 포트)
- **Fix:** 실제 런타임 네이밍 및 포트 (`NEXT_PUBLIC_API_BASE_URL=http://localhost:8080`) 유지. Plan 의 오기는 따르지 않음
- **Files modified:** `webapp/.env.local.example`
- **Commit:** `ac7bf5d`

**2. [Rule 2 - Missing critical functionality] OAuth consent screen 절차 추가**
- **Found during:** Task 1 (SETUP §2 작성)
- **Issue:** Plan 의 §2 예시가 Client ID 발급만 다루고 OAuth consent screen 구성 단계 생략 — 신규 GCP 프로젝트에서는 consent screen 구성 없이 Client ID 발급 불가 (공식 흐름상 선행 필수)
- **Fix:** §2.2 OAuth 동의 화면 구성 섹션을 추가하여 User Type / Scopes / Test users 기본값 명시. 배포 완전성 확보
- **Files modified:** `webapp/SETUP.md`
- **Commit:** `552e3ef`

**3. [Rule 2 - Missing critical functionality] Pitfalls 표 확장 (Pitfall 1/3/4/5 포함)**
- **Found during:** Task 1
- **Issue:** Plan 예시의 Pitfalls 섹션은 Pitfall 2 + 8 만 커버. 그러나 운영 중 자주 발생하는 Pitfall 1 (3단 쿠키 동기화), 3 (RLS authenticated 역할 누락), 4 (JWT chunk), 5 (Next 15 cookies async) 가 누락되면 신규 환경 배포자가 디버깅에 시간 낭비
- **Fix:** 6 행 표 형태 (증상 / 원인 / 해결) 로 통합. RESEARCH.md §Common Pitfalls 의 Pitfall 1-5, 8 을 운영 관점으로 요약
- **Files modified:** `webapp/SETUP.md`
- **Commit:** `552e3ef`

### Authentication Gates

해당 없음. Task 3 은 non-blocking 체크포인트로 auth gate 가 아니며, ROADMAP.md 근거로 "configured" 자동 처리함.

## Known Stubs

해당 없음. 이 plan 은 문서 전용이며 UI / 데이터 흐름 변경 없음.

## Threat Model Compliance

| Threat ID | Disposition | Mitigation Verified |
|-----------|-------------|----------------------|
| T-06.2-37 (Information Disclosure — .env.example 에 secret) | mitigate | ✅ `grep -E "^...=[^ ]+$"` 검증 통과 (비 공란 값 없음) |
| T-06.2-38 (Information Disclosure — service_role key 가 webapp 에 주입) | mitigate | ✅ SETUP.md §3 / §5 + .env.local.example Admin 섹션 총 3 곳에서 ⚠ 경고 명시 |
| T-06.2-39 (Tampering — 악의적 Redirect URL) | accept | ℹ Dashboard 접근 = 관리자 권한. SETUP §1.2 가 정확한 URL 명시로 사고 가능성 감소 |

## Verification Evidence

- `test -f webapp/SETUP.md` ✅
- `grep -q "Google Cloud Console" webapp/SETUP.md` ✅
- `grep -q "redirect_uri_mismatch" webapp/SETUP.md` ✅ (§2 ⚠ + §6 Pitfalls 표)
- `grep -q "service_role" webapp/SETUP.md` ✅ (§3 + §5 + §6)
- `grep -q "seed-test-user" webapp/SETUP.md` ✅ (§5.2)
- `test -f webapp/.env.local.example` ✅
- `grep -q "NEXT_PUBLIC_SUPABASE_URL=" webapp/.env.local.example` ✅
- `grep -q "E2E_TEST_EMAIL=" webapp/.env.local.example` ✅
- `grep -q "SUPABASE_SERVICE_ROLE_KEY" webapp/.env.local.example` ✅ (주석 라인)
- `! grep -E "^(NEXT_PUBLIC_SUPABASE_URL|E2E_TEST_PASSWORD)=[^ ]+$" webapp/.env.local.example` ✅ (모두 공란)

## Next Steps

- Plan 08 실행 전 `scripts/seed-test-user.ts` 가 준비되면 SETUP §5.2 명령으로 테스트 유저 시딩
- Plan 10 (Phase gate / release) 에서 이 문서를 CONTEXT 링크로 참조
- Phase 06.2 Kakao 소셜 로그인 추가 시 SETUP §1.1 / §2 에 Kakao 섹션 추가 (별도 phase 범위)

## Self-Check: PASSED

- FOUND: `webapp/SETUP.md`
- FOUND: `webapp/.env.local.example`
- FOUND commit: `552e3ef` (Task 1)
- FOUND commit: `ac7bf5d` (Task 2)
- 모든 acceptance_criteria + automated verify 명령 PASS 확인
