---
phase: 06.2
plan: 08
type: execute
wave: 5
depends_on: [06.2-03, 06.2-04, 06.2-07]
files_modified:
  - webapp/playwright.config.ts
  - webapp/e2e/auth.setup.ts
  - webapp/e2e/fixtures/auth.ts
  - webapp/e2e/fixtures/supabase-mock.ts
  - webapp/e2e/fixtures/watchlist-seed.ts
  - webapp/e2e/specs/auth.spec.ts
  - webapp/e2e/specs/watchlist.spec.ts
  - webapp/e2e/specs/search.spec.ts
  - webapp/e2e/specs/stock-detail.spec.ts
  - webapp/e2e/specs/smoke.spec.ts
  - webapp/e2e/specs/a11y.spec.ts
  - webapp/scripts/seed-test-user.ts
  - webapp/.gitignore
autonomous: false
requirements: [AUTH-02, PERS-01]
requirements_addressed:
  - id: AUTH-02
    status: partial
    notes: "E2E 가드 확인 (미인증 302, 로그인 → /scanner)."
  - id: PERS-01
    status: complete
    notes: "E2E watchlist CRUD (add, 50 limit, responsive breakpoint, rollback) 검증."

must_haves:
  truths:
    - "playwright setup project 가 Supabase REST 로 테스트 유저 세션을 생성하고 storageState 를 저장한다"
    - "기존 4 spec (search/stock-detail/smoke/a11y) 이 storageState 의존성을 통해 로그인 상태로 실행된다"
    - "신규 auth.spec.ts 가 D1 (login 리다이렉트), D2 (middleware 가드), open-redirect 가드 3종 테스트를 커버한다"
    - "신규 watchlist.spec.ts 가 D5 (50 limit), D7 (responsive breakpoint), toggle-roundtrip 3종을 커버한다"
    - "e2e full suite 전체 green (exit 0)"
  artifacts:
    - path: "webapp/e2e/auth.setup.ts"
      provides: "Playwright setup project — Supabase REST auth token + storageState 저장"
      contains: "storageState"
    - path: "webapp/e2e/fixtures/auth.ts"
      provides: "test.extend 로 authedPage fixture 제공"
      exports: ["test"]
    - path: "webapp/e2e/specs/auth.spec.ts"
      provides: "D1 + D2 + open-redirect 가드 3종 테스트"
      contains: "middleware-guard"
    - path: "webapp/e2e/specs/watchlist.spec.ts"
      provides: "D5 + D7 + toggle-roundtrip 테스트"
      contains: "responsive-breakpoint"
  key_links:
    - from: "playwright.config.ts"
      to: "auth.setup.ts"
      via: "setup project dependency"
      pattern: "dependencies: \\['setup'\\]"
    - from: "기존 4 spec"
      to: "storageState"
      via: "project config storageState path"
      pattern: "storageState:"
---

<objective>
Phase 06.2 의 로그인 벽을 E2E 로 검증하고 기존 4개 spec (search/stock-detail/smoke/a11y) 을 storageState fixture 로 전환한다. Phase 06.2 전환 이후에는 모든 기능 경로가 로그인 벽 뒤이므로, 기존 spec 은 storageState 없이 실패. 신규 auth.spec.ts + watchlist.spec.ts 로 VALIDATION.md 의 D1, D2, D5, D7 커버.

Purpose: VALIDATION.md Wave 0 Gaps 전량 해소 + D8 (기존 4 spec 전환). RESEARCH §Pattern 11 의 storageState + REST seeding 패턴 차용.

Output: 2개 신규 spec + 4개 spec 수정 + 3개 fixture/setup 신규 + playwright.config.ts 수정 + 테스트 유저 시딩 스크립트. autonomous=false — 테스트 유저 프로비저닝 (Supabase Dashboard 또는 Admin API) 은 수동 단계 1회 필요.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-auth-watchlist/06.2-CONTEXT.md
@.planning/phases/06.2-auth-watchlist/06.2-VALIDATION.md
@.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md
@.planning/phases/06.2-auth-watchlist/06.2-01-SUMMARY.md
@.planning/phases/06.2-auth-watchlist/06.2-02-SUMMARY.md
@webapp/playwright.config.ts
@webapp/e2e/specs/smoke.spec.ts
@webapp/e2e/specs/search.spec.ts

<interfaces>
<!-- 기존 Playwright 설정 + Phase 06 smoke/search/stock-detail/a11y 경로 -->

From webapp/playwright.config.ts (현재):
- projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }]
- testDir: './e2e/specs'
- Phase 06.2 전환: setup project 추가 + storageState 경로

From RESEARCH §Pattern 11:
- Supabase REST POST `/auth/v1/token?grant_type=password` 로 세션 획득
- 쿠키 이름 `sb-<project-ref>-auth-token` (청크 분할 가능 — 0.10.x 규칙)
- storageState 저장: `await page.context().storageState({ path: AUTH_FILE })`

From Plan 02 Schema:
- `watchlists` 테이블 + RLS + trigger — integration test 에서 pre-seed 필요
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Playwright setup project + fixture + 테스트 유저 시딩 스크립트</name>

  <read_first>
    - /Users/alex/repos/gh-radar/webapp/playwright.config.ts (현재 설정 전체)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"Pattern 11"
    - /Users/alex/repos/gh-radar/webapp/e2e/specs/smoke.spec.ts (현재 spec 스타일 참조)
  </read_first>

  <files>
    webapp/playwright.config.ts,
    webapp/e2e/auth.setup.ts,
    webapp/e2e/fixtures/auth.ts,
    webapp/scripts/seed-test-user.ts,
    webapp/.gitignore
  </files>

  <action>
    **1.1 — `webapp/scripts/seed-test-user.ts` (신규):**
    Supabase Admin API 를 사용해 테스트 유저 1명 생성 + `user_metadata.full_name = "E2E Tester"` + 알려진 비밀번호. 환경변수 필수: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `E2E_TEST_EMAIL`, `E2E_TEST_PASSWORD`. 스크립트는 멱등 (이미 존재 시 skip).
    ```typescript
    #!/usr/bin/env tsx
    import { createClient } from "@supabase/supabase-js";

    const url = process.env.SUPABASE_URL!;
    const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY!;
    const email = process.env.E2E_TEST_EMAIL ?? "e2e@gh-radar.local";
    const password = process.env.E2E_TEST_PASSWORD!;
    if (!url || !serviceKey || !password) {
      console.error("Missing SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY / E2E_TEST_PASSWORD");
      process.exit(1);
    }

    const admin = createClient(url, serviceKey, { auth: { autoRefreshToken: false, persistSession: false } });
    const { data: existing } = await admin.auth.admin.listUsers();
    const found = existing?.users?.find((u) => u.email === email);
    if (found) { console.log(`User exists: ${email}`); process.exit(0); }
    const { data, error } = await admin.auth.admin.createUser({
      email, password, email_confirm: true,
      user_metadata: { full_name: "E2E Tester" },
    });
    if (error) { console.error(error); process.exit(1); }
    console.log(`Created: ${data.user?.id} <${email}>`);
    ```
    **수동 실행 가이드 (SETUP.md 에서도 안내 — Plan 09):** `pnpm --filter @gh-radar/webapp exec tsx scripts/seed-test-user.ts` 1회 실행.

    **1.2 — `webapp/e2e/auth.setup.ts` (신규):**
    ```typescript
    import { test as setup, expect } from "@playwright/test";
    import fs from "node:fs";
    import path from "node:path";

    const AUTH_FILE = path.resolve(__dirname, "../.playwright/auth.json");

    setup("authenticate E2E user", async ({ page, request }) => {
      const url = process.env.NEXT_PUBLIC_SUPABASE_URL;
      const anonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;
      const email = process.env.E2E_TEST_EMAIL ?? "e2e@gh-radar.local";
      const password = process.env.E2E_TEST_PASSWORD;
      if (!url || !anonKey || !password) throw new Error("E2E auth env vars missing");

      // Supabase REST grant_type=password 로 세션 획득
      const res = await request.post(`${url}/auth/v1/token?grant_type=password`, {
        headers: { apikey: anonKey, "Content-Type": "application/json" },
        data: { email, password },
      });
      expect(res.ok()).toBeTruthy();
      const session = await res.json();

      // Supabase 쿠키 이름 추출 — URL 의 project ref 부분
      const ref = new URL(url).hostname.split(".")[0];
      const cookieName = `sb-${ref}-auth-token`;
      const cookieValue = encodeURIComponent(JSON.stringify(session));
      // localhost + production 두 domain 모두 저장 (baseURL 에 따라 사용)
      const domain = new URL(process.env.PLAYWRIGHT_BASE_URL ?? "http://localhost:3000").hostname;
      await page.context().addCookies([{
        name: cookieName, value: cookieValue, domain, path: "/", sameSite: "Lax", httpOnly: false,
      }]);

      fs.mkdirSync(path.dirname(AUTH_FILE), { recursive: true });
      await page.context().storageState({ path: AUTH_FILE });
    });
    ```

    **1.3 — `webapp/e2e/fixtures/auth.ts` (신규):**
    ```typescript
    import { test as base } from "@playwright/test";
    export const test = base;  // storageState 는 config.projects 에서 자동 적용
    export { expect } from "@playwright/test";
    ```
    (단순 re-export — 프로젝트 단위 storageState 가 이미 context 에 주입됨)

    **1.4 — `webapp/playwright.config.ts` 수정:**
    `projects` 배열 재구성:
    ```typescript
    import { defineConfig, devices } from "@playwright/test";

    const AUTH_FILE = "./.playwright/auth.json";

    export default defineConfig({
      testDir: "./e2e/specs",
      // ...기존 옵션 보존 (retries, reporter, webServer 등)
      projects: [
        { name: "setup", testMatch: /auth\.setup\.ts$/, teardown: undefined },
        {
          name: "chromium",
          use: { ...devices["Desktop Chrome"], storageState: AUTH_FILE },
          dependencies: ["setup"],
          testIgnore: /auth\.setup\.ts$/,
        },
      ],
    });
    ```
    webServer 옵션이 있으면 그대로 유지. baseURL 도 유지.

    **1.5 — `webapp/.gitignore` 수정:** `.playwright/` 디렉터리 추가 (또는 `.playwright/auth.json` 만) — storageState 파일이 커밋되지 않도록.
  </action>

  <acceptance_criteria>
    - `/Users/alex/repos/gh-radar/webapp/scripts/seed-test-user.ts` exists and contains `admin.auth.admin.createUser` and `email_confirm: true`.
    - `/Users/alex/repos/gh-radar/webapp/e2e/auth.setup.ts` exists and contains `storageState({ path: AUTH_FILE })` and `grant_type=password`.
    - `/Users/alex/repos/gh-radar/webapp/e2e/fixtures/auth.ts` exists (even if minimal re-export).
    - `/Users/alex/repos/gh-radar/webapp/playwright.config.ts` contains `name: "setup"` and `dependencies: ["setup"]` and `storageState:`.
    - `/Users/alex/repos/gh-radar/webapp/.gitignore` contains `.playwright` entry.
    - `pnpm --filter @gh-radar/webapp exec tsc --noEmit` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && pnpm exec tsc --noEmit && grep -q 'storageState' playwright.config.ts && grep -q 'dependencies: \["setup"\]' playwright.config.ts && grep -q 'grant_type=password' e2e/auth.setup.ts && grep -q '.playwright' .gitignore</automated>
  </verify>

  <done>
    Setup project + fixture + 시딩 스크립트 + config 수정 + gitignore 완료, tsc 통과.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [MANUAL] 테스트 유저 프로비저닝 + .env.test 환경변수 설정</name>

  <read_first>
    - /Users/alex/repos/gh-radar/webapp/scripts/seed-test-user.ts (Task 1 산출물)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"Environment Availability"
  </read_first>

  <files>
    (수동 단계 — 사용자가 Supabase Dashboard 에서 테스트 유저 1명 생성 + 환경변수 설정. webapp/.env.test.local 은 gitignore 대상이므로 커밋 안 함.)
  </files>

  <action>
    실행자는 사용자에게 아래 §how-to-verify 의 Option A/B 안내 후 결과 확인.
    `SUPABASE_SERVICE_ROLE_KEY` 가 이미 환경에 배치되어 있다면 (Phase 1/05.1 잔재 가능) 실행자가 직접 `pnpm exec tsx scripts/seed-test-user.ts` 실행 시도.
  </action>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && test -f .playwright/auth.json || echo "MANUAL — setup project 미실행 또는 auth.json 미생성"</automated>
  </verify>

  <done>
    테스트 유저 존재 + env 설정 완료 + `pnpm exec playwright test --project=setup` exit 0 + `webapp/.playwright/auth.json` 생성.
  </done>

  <what-built>
    E2E 테스트가 실제 Supabase dev 프로젝트에서 로그인 동작하려면:
    1. 테스트 유저 1명 (`e2e@gh-radar.local` 또는 사용자 지정) 이 `auth.users` 에 존재
    2. 해당 유저의 비밀번호를 알고 있음
    3. `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` + `E2E_TEST_EMAIL` + `E2E_TEST_PASSWORD` 를 playwright 실행 환경에 노출

    **autonomous=false 사유:** Supabase Admin API 호출은 `SUPABASE_SERVICE_ROLE_KEY` 필요 — 사용자 수동 준비. 만약 이미 Phase 1 에 service_role key 가 배치되어 있으면 자동 실행 가능.
  </what-built>

  <how-to-verify>
    **Option A — 자동 스크립트 (권장, 이미 service_role key 보유 시):**
    1. `.env.test` 또는 쉘 환경에 설정:
       ```bash
       export SUPABASE_URL="<project URL>"
       export SUPABASE_SERVICE_ROLE_KEY="<service_role key>"
       export E2E_TEST_EMAIL="e2e@gh-radar.local"
       export E2E_TEST_PASSWORD="<32자 이상 랜덤>"
       ```
    2. `cd /Users/alex/repos/gh-radar/webapp && pnpm exec tsx scripts/seed-test-user.ts`
    3. 출력 `Created: <uuid> <e2e@gh-radar.local>` 또는 `User exists` 확인.

    **Option B — Dashboard 수동 (service_role key 없을 때):**
    1. Supabase Dashboard → Authentication → Users → Add User.
    2. Email `e2e@gh-radar.local`, Password `<생성>`, `Auto Confirm User` 체크.
    3. 저장 후 `user.id` 메모.

    **공통 — Playwright 실행 env 설정:**
    - 로컬: `webapp/.env.test.local` 파일 생성 (gitignore 대상 확인):
      ```
      NEXT_PUBLIC_SUPABASE_URL=...
      NEXT_PUBLIC_SUPABASE_ANON_KEY=...
      E2E_TEST_EMAIL=e2e@gh-radar.local
      E2E_TEST_PASSWORD=...
      PLAYWRIGHT_BASE_URL=http://localhost:3000
      ```
    - CI: GitHub Secrets 또는 CI vault 에 동일 키 등록.

    **검증 스모크:**
    1. `cd /Users/alex/repos/gh-radar/webapp`
    2. `pnpm dev &` (또는 build+start) 대기 후
    3. `pnpm exec playwright test --project=setup` — exit 0 + `webapp/.playwright/auth.json` 생성 확인
    4. `cat webapp/.playwright/auth.json | jq '.cookies[0].name'` → `sb-<ref>-auth-token` 포맷 확인
  </how-to-verify>

  <resume-signal>
    "seeded" — 테스트 유저 존재 + env 설정 완료 + setup project exit 0.
    "failed: {이유}" — 실패 시 원인 보고, 필요 시 Task 1 의 setup.ts 를 조정.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 신규 auth.spec.ts + watchlist.spec.ts + 기존 4 spec storageState 적합화</name>

  <read_first>
    - /Users/alex/repos/gh-radar/webapp/e2e/specs/smoke.spec.ts (현재 로그인 없이 /scanner 진입 전제)
    - /Users/alex/repos/gh-radar/webapp/e2e/specs/search.spec.ts
    - /Users/alex/repos/gh-radar/webapp/e2e/specs/stock-detail.spec.ts
    - /Users/alex/repos/gh-radar/webapp/e2e/specs/a11y.spec.ts
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-VALIDATION.md (D1~D8 매핑)
  </read_first>

  <files>
    webapp/e2e/specs/auth.spec.ts,
    webapp/e2e/specs/watchlist.spec.ts,
    webapp/e2e/specs/search.spec.ts,
    webapp/e2e/specs/stock-detail.spec.ts,
    webapp/e2e/specs/smoke.spec.ts,
    webapp/e2e/specs/a11y.spec.ts,
    webapp/e2e/fixtures/supabase-mock.ts,
    webapp/e2e/fixtures/watchlist-seed.ts
  </files>

  <action>
    **3.1 — `webapp/e2e/specs/auth.spec.ts` (신규):**
    ```typescript
    import { test, expect } from "@playwright/test";

    test.describe("auth — 로그인 벽 + 리다이렉트", () => {
      // 이 스펙은 storageState 없이 실행해야 middleware 가드를 테스트 가능
      test.use({ storageState: { cookies: [], origins: [] } });

      test("middleware-guard: 미인증 /scanner → /login?next=/scanner", async ({ page }) => {
        await page.goto("/scanner");
        await expect(page).toHaveURL(/\/login\?next=%2Fscanner/);
      });

      test("middleware-guard: 미인증 /watchlist → /login?next=/watchlist", async ({ page }) => {
        await page.goto("/watchlist");
        await expect(page).toHaveURL(/\/login\?next=%2Fwatchlist/);
      });

      test("public whitelist: / 루트는 미인증도 접근 가능", async ({ page }) => {
        await page.goto("/");
        await expect(page).not.toHaveURL(/login/);
      });

      test("login page: ?error=auth_failed 한글 메시지 표시", async ({ page }) => {
        await page.goto("/login?error=auth_failed");
        await expect(page.getByRole("alert")).toContainText("로그인 처리에 실패");
      });

      test("login page: ?error=oauth_denied 한글 메시지", async ({ page }) => {
        await page.goto("/login?error=oauth_denied");
        await expect(page.getByRole("alert")).toContainText("Google 로그인을 취소");
      });

      test("login page: Google 버튼 노출", async ({ page }) => {
        await page.goto("/login");
        await expect(page.getByRole("button", { name: /Google로 로그인/ })).toBeVisible();
      });

      test("open-redirect-guard — protocol-relative URL 거부 + /scanner fallback", async ({ page }) => {
        // /auth/callback 이 open-redirect 를 먼저 차단해야 한다. code=fake 는 어차피 실패 →
        // auth_failed 처리되어도 OK. 핵심은 최종 URL 이 attacker 도메인 이 아닐 것.
        await page.goto("/auth/callback?code=fake-code&next=//attacker.com/steal");
        await expect(page).not.toHaveURL(/attacker/);
        await expect(page).not.toHaveURL(/^https?:\/\/(?!localhost)[^\/]*\/\/attacker/);
        // fallback 은 login(auth_failed) 또는 /scanner — 둘 다 안전.
        await expect(page).toHaveURL(/\/(login|scanner)/);
      });

      test("open-redirect-guard — /login?next=//attacker.com 은 safeNext 로 치환", async ({ page }) => {
        await page.goto("/login?next=//attacker.com");
        // login 페이지 자체는 정상 로드 (Google 버튼 노출) — safeNext 치환은 클라이언트 state 에서
        await expect(page.getByRole("button", { name: /Google로 로그인/ })).toBeVisible();
        // URL 이 attacker 로 자동 이동되지 않음을 확인
        await expect(page).not.toHaveURL(/attacker/);
      });
    });

    test.describe("auth — 로그인된 사용자", () => {
      // 이 블록은 기본 storageState (authed) 사용
      test("로그인 상태로 /login 접근 → /scanner 리다이렉트 (D-12)", async ({ page }) => {
        await page.goto("/login");
        await expect(page).toHaveURL(/\/scanner/);
      });

      test("AppSidebar nav 2개 노출", async ({ page }) => {
        await page.goto("/scanner");
        await expect(page.getByRole("link", { name: /스캐너/ })).toBeVisible();
        await expect(page.getByRole("link", { name: /관심종목/ })).toBeVisible();
      });

      test("UserSection 팝오버 + 로그아웃 버튼 노출", async ({ page }) => {
        await page.goto("/scanner");
        await page.getByRole("button", { name: /사용자|E2E/ }).first().click();
        await expect(page.getByRole("button", { name: /로그아웃/ })).toBeVisible();
      });
    });
    ```

    **3.2a — `webapp/e2e/fixtures/watchlist-seed.ts` (신규 — seed/cleanup helper):**
    ```typescript
    import { createClient, type SupabaseClient } from "@supabase/supabase-js";

    /** service_role 로 만든 admin 클라이언트 — E2E 전용. webapp runtime 절대 사용 금지. */
    export function createServiceClient(): SupabaseClient {
      const url = process.env.SUPABASE_URL ?? process.env.NEXT_PUBLIC_SUPABASE_URL;
      const key = process.env.SUPABASE_SERVICE_ROLE_KEY;
      if (!url || !key) {
        throw new Error("SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY required for seed helper");
      }
      return createClient(url, key, { auth: { autoRefreshToken: false, persistSession: false } });
    }

    /** 테스트 유저 id 조회 (email 기반). */
    export async function getTestUserId(admin: SupabaseClient, email: string): Promise<string> {
      const { data, error } = await admin.auth.admin.listUsers();
      if (error) throw error;
      const found = data.users.find((u) => u.email === email);
      if (!found) throw new Error(`Test user not found: ${email}`);
      return found.id;
    }

    /** watchlists 50 row 일괄 시드. 기존 row 는 cleanup 선행 권장. */
    export async function seed50Watchlists(userId: string, admin: SupabaseClient): Promise<void> {
      const rows = Array.from({ length: 50 }, (_, i) => ({
        user_id: userId,
        stock_code: String(5930 + i).padStart(6, "0"),
        position: i,
      }));
      const { error } = await admin.from("watchlists").insert(rows);
      if (error) throw error;
    }

    /** 테스트 유저의 모든 watchlists row 삭제. */
    export async function cleanupWatchlists(userId: string, admin: SupabaseClient): Promise<void> {
      const { error } = await admin.from("watchlists").delete().eq("user_id", userId);
      if (error) throw error;
    }
    ```

    **3.2b — `webapp/e2e/specs/watchlist.spec.ts` (신규):**
    ```typescript
    import { test, expect } from "@playwright/test";
    import { createServiceClient, getTestUserId, seed50Watchlists, cleanupWatchlists } from "../fixtures/watchlist-seed";
    import { mockWatchlistInsertFail } from "../fixtures/supabase-mock";

    const E2E_EMAIL = process.env.E2E_TEST_EMAIL ?? "e2e@gh-radar.local";
    const HAS_SERVICE_KEY = !!process.env.SUPABASE_SERVICE_ROLE_KEY;

    test.describe("watchlist — CRUD + 반응형", () => {
      test.describe.configure({ mode: "serial" });  // 시드/정리 순서 보존

      test("empty state — 관심종목 없을 때 한글 카피 + CTA 노출", async ({ page }) => {
        test.skip(!HAS_SERVICE_KEY, "SUPABASE_SERVICE_ROLE_KEY 없음 — seed/cleanup 불가");
        const admin = createServiceClient();
        const userId = await getTestUserId(admin, E2E_EMAIL);
        await cleanupWatchlists(userId, admin);

        await page.goto("/watchlist");
        await expect(page.getByRole("heading", { name: "아직 관심종목이 없습니다" })).toBeVisible();
        const cta = page.getByRole("link", { name: /스캐너로 가기/ });
        await expect(cta).toBeVisible();
        await expect(cta).toHaveAttribute("href", "/scanner");
      });

      test("toggle-roundtrip — /scanner 에서 ⭐ 추가 → /watchlist 에 반영", async ({ page }) => {
        test.skip(!HAS_SERVICE_KEY, "SUPABASE_SERVICE_ROLE_KEY 없음");
        const admin = createServiceClient();
        const userId = await getTestUserId(admin, E2E_EMAIL);
        await cleanupWatchlists(userId, admin);

        await page.goto("/scanner");
        const firstToggle = page.locator('[aria-label*="관심종목 추가"]').first();
        await firstToggle.click();
        // optimistic 반영 대기
        await expect(firstToggle).toHaveAttribute("aria-pressed", "true", { timeout: 3000 });

        await page.goto("/watchlist");
        await expect(page.locator('[role="table"]').or(page.locator('[role="row"]')).first()).toBeVisible();
      });

      test("responsive-breakpoint — lg+ Table / <lg InfoStockCard (D7)", async ({ page }) => {
        test.skip(!HAS_SERVICE_KEY, "SUPABASE_SERVICE_ROLE_KEY 없음");
        const admin = createServiceClient();
        const userId = await getTestUserId(admin, E2E_EMAIL);
        await cleanupWatchlists(userId, admin);
        // 최소 1 row 시드 (표/카드 렌더 보장)
        await admin.from("watchlists").insert({ user_id: userId, stock_code: "005930", position: 0 });

        // Desktop 1280 → Table 노출
        await page.setViewportSize({ width: 1280, height: 800 });
        await page.goto("/watchlist");
        await expect(page.locator('[role="table"]')).toBeVisible();

        // Mobile 375 → Table 숨김
        await page.setViewportSize({ width: 375, height: 667 });
        await page.reload();
        await expect(page.locator('[role="table"]')).toBeHidden();
      });

      test("50-limit — 51번째 ⭐ 클릭 시 disabled + title 툴팁 + alert 메시지 (D5)", async ({ page }) => {
        test.skip(!HAS_SERVICE_KEY, "SUPABASE_SERVICE_ROLE_KEY 없음 — 50-row seed 불가");
        const admin = createServiceClient();
        const userId = await getTestUserId(admin, E2E_EMAIL);
        await cleanupWatchlists(userId, admin);
        await seed50Watchlists(userId, admin);

        await page.goto("/watchlist");
        // Table 에 50 row 렌더 확인
        await expect(page.locator('[role="row"]')).toHaveCount(51, { timeout: 5000 });  // header + 50

        // Scanner 로 이동 — 시드된 50 코드 이외의 종목에서 ⭐ 가 disabled 상태인지 확인
        await page.goto("/scanner");
        const unsetToggle = page.locator('[aria-label*="관심종목 추가"]').first();
        await expect(unsetToggle).toBeVisible();
        await expect(unsetToggle).toBeDisabled();
        await expect(unsetToggle).toHaveAttribute("title", "관심종목은 최대 50개까지 저장할 수 있습니다.");

        // (선택) disabled 우회 강제 클릭 시 — disabled 면 클릭 무반응. 이 경로는 UI 만 검증.
      });

      test("rollback-on-error — 네트워크 실패 시 optimistic 롤백 + 2초 내 inline 에러", async ({ page }) => {
        test.skip(!HAS_SERVICE_KEY, "SUPABASE_SERVICE_ROLE_KEY 없음");
        const admin = createServiceClient();
        const userId = await getTestUserId(admin, E2E_EMAIL);
        await cleanupWatchlists(userId, admin);

        await mockWatchlistInsertFail(page);
        await page.goto("/scanner");
        const firstToggle = page.locator('[aria-label*="관심종목 추가"]').first();
        await firstToggle.click();
        await expect(page.getByRole("alert")).toContainText("관심종목 변경에 실패했습니다", { timeout: 3000 });
        // rollback: aria-pressed 가 다시 false
        await expect(firstToggle).toHaveAttribute("aria-pressed", "false", { timeout: 3000 });
      });
    });
    ```

    **3.3 — 기존 4 spec (`search.spec.ts`, `stock-detail.spec.ts`, `smoke.spec.ts`, `a11y.spec.ts`) 수정:**
    playwright.config.ts 의 `storageState` 가 프로젝트 레벨로 주입되므로 **대부분 변경 불필요**. 단 아래 사항 확인:
    1. 각 spec 의 첫 `await page.goto("/scanner")` 가 로그인 벽 없이 동작하는지.
    2. 만약 기존 spec 이 명시적으로 `storageState: { cookies: [] }` 를 설정했다면 제거.
    3. Login 벽 테스트가 아닌 이상 `storageState` override 없음.
    4. a11y.spec.ts 에는 **UserSection 팝오버 a11y 체크 추가** (D6 VALIDATION):
       ```typescript
       test("UserSection popover a11y", async ({ page }) => {
         await page.goto("/scanner");
         await page.getByRole("button", { name: /사용자|E2E/ }).first().click();
         // axe 검사
         const results = await new AxeBuilder({ page }).include('[role="dialog"]').analyze();
         expect(results.violations).toEqual([]);
         // ESC 로 닫힘
         await page.keyboard.press("Escape");
         await expect(page.getByRole("dialog")).toBeHidden();
       });
       ```

    **3.4 — `webapp/e2e/fixtures/supabase-mock.ts` (신규 — 선택적 스텁):**
    스텁 route 인터셉터 helper 를 모듈화. 간단 export 만 제공 — 실행자가 각 spec 에서 호출.
    ```typescript
    import type { Page } from "@playwright/test";

    /** Supabase watchlists POST 를 500 으로 실패시킴. */
    export async function mockWatchlistInsertFail(page: Page) {
      await page.route("**/rest/v1/watchlists*", (route) => {
        if (route.request().method() === "POST") {
          return route.fulfill({ status: 500, body: JSON.stringify({ message: "internal" }) });
        }
        return route.continue();
      });
    }
    ```
  </action>

  <acceptance_criteria>
    - `/Users/alex/repos/gh-radar/webapp/e2e/specs/auth.spec.ts` exists with ≥7 test cases (middleware guard, error params, public whitelist, authed redirect).
    - `/Users/alex/repos/gh-radar/webapp/e2e/specs/watchlist.spec.ts` exists with ≥5 test cases (empty-state, toggle-roundtrip, responsive-breakpoint, 50-limit, rollback-on-error) — no `test.skip(true, ...)` 플레이스홀더. 조건부 skip 은 `test.skip(!HAS_SERVICE_KEY, ...)` 형태만 허용.
    - `/Users/alex/repos/gh-radar/webapp/e2e/fixtures/supabase-mock.ts` exists with helper export.
    - `/Users/alex/repos/gh-radar/webapp/e2e/fixtures/watchlist-seed.ts` exists and exports `seed50Watchlists`, `cleanupWatchlists`, `getTestUserId`, `createServiceClient`. Grep check: `grep -q 'seed50Watchlists' webapp/e2e/fixtures/watchlist-seed.ts` exits 0.
    - `/Users/alex/repos/gh-radar/webapp/e2e/specs/a11y.spec.ts` contains `UserSection` test (new).
    - 기존 4 spec (search/stock-detail/smoke/a11y) 이 `storageState: { cookies: [] }` override 를 갖지 않음 (로그인 기본 사용).
    - `pnpm --filter @gh-radar/webapp exec tsc --noEmit` exits 0.
    - `pnpm --filter @gh-radar/webapp test:e2e` exits 0 (Task 2 가 선행되어 테스트 유저 + env 세팅 완료 후).
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && pnpm exec tsc --noEmit && grep -q 'middleware-guard' e2e/specs/auth.spec.ts && grep -q 'responsive-breakpoint' e2e/specs/watchlist.spec.ts && grep -q 'UserSection' e2e/specs/a11y.spec.ts && grep -q 'seed50Watchlists' e2e/fixtures/watchlist-seed.ts && grep -q 'cleanupWatchlists' e2e/fixtures/watchlist-seed.ts && ! grep -q 'test\.skip(true,' e2e/specs/watchlist.spec.ts && pnpm test:e2e 2>&1 | tail -20</automated>
  </verify>

  <done>
    auth.spec.ts + watchlist.spec.ts 신규, 기존 4 spec 조정, a11y UserSection 추가, supabase-mock helper, E2E suite 전체 통과.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E 테스트 계정 credentials | CI/로컬 환경변수에 평문 저장 — 노출 시 dev 프로젝트 침투 가능 |
| storageState 파일 | 세션 쿠키 평문 — gitignore 필수 |
| Playwright route interception | mockWatchlistInsertFail 은 테스트 내부 — 프로덕션 영향 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-33 | Information Disclosure | E2E 테스트 유저 비밀번호 유출 | mitigate | `.env.test.local` 은 gitignore + CI Secrets 에만 저장. dev Supabase 프로젝트 전용 계정 (prod 영향 없음). 로테이션은 `scripts/seed-test-user.ts` 재실행으로 처리. ASVS V8.2.3. |
| T-06.2-34 | Information Disclosure | storageState JSON 이 repo 에 커밋됨 | mitigate | `webapp/.gitignore` 에 `.playwright` 추가 (Task 1). ASVS V8.2. Verified by: Task 1 grep. |
| T-06.2-35 | Tampering | 테스트 유저가 50-limit seed 후 방치 | accept | E2E 유저 전용 계정이라 50 row 잔존 영향 없음. 필요 시 `beforeEach` cleanup helper 가 DELETE. 실행자 구현 재량. |
| T-06.2-36 | Spoofing | E2E 에서 service_role key 노출 | mitigate | `seed-test-user.ts` 는 로컬/CI 일회성 실행 — webapp client 와 분리. service_role key 는 CI Secrets 에만. webapp runtime 에는 anon key 만. ASVS V8.2.3. |

</threat_model>

<verification>
- **Per-task:** Task 1 tsc + grep. Task 2 수동 검증 (setup project run). Task 3 tsc + grep + 전체 E2E 실행.
- **Wave-end:** `pnpm --filter @gh-radar/webapp test:e2e` exit 0.
- **Full integration:** Plan 08 은 Phase 06.2 의 최종 가드 — 완료 시 VALIDATION.md Wave 0 Gaps 전량 해소 + nyquist_compliant 로 전환 후보.
</verification>

<success_criteria>
- Playwright setup project + storageState fixture 작동
- 테스트 유저 + env 세팅 완료 (Task 2)
- auth.spec.ts (≥7 tests) + watchlist.spec.ts (≥4 tests) + a11y.spec.ts UserSection 추가
- 기존 4 spec 이 storageState 로 동작
- `pnpm test:e2e` 전체 통과
</success_criteria>

<output>
`.planning/phases/06.2-auth-watchlist/06.2-08-SUMMARY.md`:
- Playwright 설정 변경 전/후
- setup project + auth.setup.ts 동작 원리 요약
- 신규/수정 spec 파일 목록 + 커버하는 VALIDATION Dim (D1-D8)
- E2E suite 실행 결과 (pass count / duration)
- 테스트 유저 email (평문 ok) + 비밀번호 위치 (env only)
</output>
