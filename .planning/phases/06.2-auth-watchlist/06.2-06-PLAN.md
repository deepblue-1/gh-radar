---
phase: 06.2
plan: 06
type: execute
wave: 3
depends_on: [06.2-02, 06.2-04]
files_modified:
  - webapp/src/lib/watchlist-api.ts
  - webapp/src/hooks/use-watchlist-query.ts
  - webapp/src/hooks/__tests__/use-watchlist-query.test.ts
  - webapp/src/components/watchlist/watchlist-client.tsx
  - webapp/src/components/watchlist/watchlist-table.tsx
  - webapp/src/components/watchlist/watchlist-empty.tsx
  - webapp/src/components/watchlist/watchlist-skeleton.tsx
  - webapp/src/app/watchlist/page.tsx
  - webapp/src/lib/__tests__/watchlist-api.test.ts
autonomous: true
requirements: [PERS-01]
requirements_addressed:
  - id: PERS-01
    status: in_progress
    notes: "/watchlist 페이지 CRUD 읽기 경로 완성 — 작성/해제 경로는 Plan 07 ⭐ 토글."

must_haves:
  truths:
    - "/watchlist 페이지는 로그인 사용자의 관심종목을 added_at DESC 로 정렬해서 표시한다"
    - "lg+ 에서 WatchlistTable (7컬럼: 종목명/코드/마켓/현재가/등락률/거래대금/⭐해제), <lg 에서 InfoStockCard 리스트가 렌더된다"
    - "관심종목이 0개면 WatchlistEmpty (Star 아이콘 + 한글 메시지 + 스캐너로 가기 CTA) 가 표시된다"
    - "페이지 헤더에 최근 갱신 시각 (MAX(quote.updated_at)) 이 Scanner 와 동일 스타일로 표시된다"
    - "1분 간격 폴링이 Scanner 와 동일 패턴으로 작동한다 (useWatchlistQuery)"
    - "RLS 가 타인 row 차단: integration test 에서 2명 유저 간 row 격리 검증"
  artifacts:
    - path: "webapp/src/lib/watchlist-api.ts"
      provides: "fetchWatchlist + addWatchlistItem + removeWatchlistItem (Supabase 직접 CRUD)"
      exports: ["fetchWatchlist", "addWatchlistItem", "removeWatchlistItem", "WatchlistRow"]
    - path: "webapp/src/hooks/use-watchlist-query.ts"
      provides: "1분 폴링 훅 + lastUpdatedAt 계산 (useScannerQuery 동형 패턴)"
      exports: ["useWatchlistQuery"]
    - path: "webapp/src/components/watchlist/watchlist-client.tsx"
      provides: "Client wrapper — 폴링 + lg+/<lg 분기 + empty 처리"
      exports: ["WatchlistClient"]
    - path: "webapp/src/app/watchlist/page.tsx"
      provides: "/watchlist 라우트 진입점 — AppShell + AppSidebar + WatchlistClient"
      contains: "WatchlistClient"
  key_links:
    - from: "useWatchlistQuery"
      to: "watchlist-api.fetchWatchlist"
      via: "1분 setInterval"
      pattern: "fetchWatchlist\\("
    - from: "WatchlistClient"
      to: "InfoStockCard / WatchlistTable"
      via: "lg breakpoint 분기"
      pattern: "lg:hidden|hidden lg:block"
---

<objective>
`/watchlist` 페이지 진입점 + 데이터 fetch 레이어 + lg+ Table/ <lg InfoStockCard 분기 + 1분 폴링 + empty state 를 구축한다. Plan 07 ⭐ 토글 통합 전 단계 — 이 plan 완료 후에는 Plan 07 이 작성하는 ⭐ 토글이 정보 표시와 해제 동작을 모두 수행할 수 있도록 모든 렌더 자리가 준비된다.

Purpose: D-23 + D-24 + D-25 + D-27 구현. Phase 05 Scanner 의 useScannerQuery 패턴을 그대로 복제 (1분 폴링, lastUpdatedAt MAX 계산은 클라이언트 계산으로 변경 — Pattern 10).

Output: 8개 파일 (신규 7 + 수정 0). 이 plan 완료 후 로그인 상태로 `/watchlist` 접근 시 빈 리스트 / Empty state / 폴링 동작이 작동. 쓰기는 Plan 07 에서.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-auth-watchlist/06.2-CONTEXT.md
@.planning/phases/06.2-auth-watchlist/06.2-UI-SPEC.md
@.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md
@.planning/phases/06.2-auth-watchlist/06.2-02-SUMMARY.md
@.planning/phases/06.2-auth-watchlist/06.2-04-SUMMARY.md
@webapp/src/components/scanner/scanner-client.tsx
@webapp/src/components/scanner/scanner-table.tsx
@webapp/src/hooks/use-polling.ts
@webapp/src/components/stock/info-stock-card.tsx

<interfaces>
<!-- Plan 02 산출물 — watchlists 스키마 -->

Supabase schema (from Plan 02):
- `watchlists(user_id uuid, stock_code text, added_at timestamptz, position int)`
- RLS: auth.uid() = user_id 전량
- JOIN 대상: `stocks (code, name, market, kosdaq_segment)`, `stock_quotes (code, price, change_amount, change_rate, trade_amount, updated_at)`

<!-- Plan 01 산출물 — Supabase client -->
From webapp/src/lib/supabase/client.ts:
```typescript
export function createClient(): SupabaseClient;
```

<!-- Plan 01 산출물 — AuthProvider -->
From webapp/src/lib/auth-context.tsx:
```typescript
export function useAuth(): { user: User | null, displayName: string | null, isLoading: boolean, signOut: () => Promise<void> };
```

<!-- 기존 Scanner 동형 패턴 -->
From webapp/src/hooks/use-polling.ts (Phase 05 Scanner 폴링 훅 공용):
- 1분 interval + visibility API + abort 지원 패턴

From webapp/src/components/scanner/scanner-client.tsx (폴링 클라이언트 동형):
- lastUpdatedAt 은 `X-Last-Updated-At` 헤더 (Scanner) — Watchlist 는 클라이언트 MAX 계산 (RESEARCH Pattern 10)

From RESEARCH §Pattern 9 — watchlist JOIN 쿼리:
```typescript
supabase.from('watchlists').select(`
  stock_code, added_at, position,
  stock:stocks!inner (code, name, market, kosdaq_segment),
  quote:stock_quotes (price, change_amount, change_rate, trade_amount, updated_at)
`).order('added_at', { ascending: false });
```

From Plan 05 — InfoStockCard:
```typescript
interface InfoStockCardProps {
  stock: StockWithProximity;
  showWatchlistToggle?: boolean;
  watchlistToggleSlot?: React.ReactNode;  // Plan 07 에서 WatchlistToggle 주입
}
```

From UI-SPEC §4.2 — Watchlist Table 컬럼:
- `종목명 / 코드 / 마켓 / 현재가 / 등락률 / 거래대금 / [⭐해제]`
- Grid: `grid-cols-[1fr_100px_80px_120px_100px_140px_44px]`

From UI-SPEC §4.5 — Empty state:
- 240px min-h Card + `<Star size-10 text-[var(--muted-fg)]>` + heading "아직 관심종목이 없습니다" + body "스캐너에서 ⭐ 를 눌러 관심종목을 추가해보세요." + CTA "스캐너로 가기" primary Button href=/scanner
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: watchlist-api.ts + integration test (RLS 격리)</name>

  <read_first>
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"Pattern 9" (JOIN 쿼리)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-VALIDATION.md D3 (RLS 격리 integration test)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-02-SUMMARY.md (watchlists 스키마 확인)
    - /Users/alex/repos/gh-radar/webapp/src/lib/scanner-api.ts (타입 참조 — StockWithProximity 구조)
  </read_first>

  <behavior>
    - Test 1: `fetchWatchlist(supabase)` 는 `{ data, error }` 반환. data 는 WatchlistRow[] 형식.
    - Test 2: `addWatchlistItem(supabase, userId, stockCode)` 는 단일 row insert 후 `{ error }` 반환.
    - Test 3: `removeWatchlistItem(supabase, userId, stockCode)` 는 eq 매칭 delete.
    - Test 4 (integration, optional — `SUPABASE_E2E_URL` env 존재 시만 실행): 유저 A 가 추가한 row 를 유저 B 의 세션으로 fetchWatchlist → 0 rows.
  </behavior>

  <files>
    webapp/src/lib/watchlist-api.ts,
    webapp/src/lib/__tests__/watchlist-api.test.ts
  </files>

  <action>
    1. 먼저 테스트 파일 작성 — unit 에서는 `createClient` mock (from 의 chain builder) + 통합 부분은 `describe.skipIf(!process.env.SUPABASE_E2E_URL)` 로 conditional.

    2. `webapp/src/lib/watchlist-api.ts` 작성:
       ```typescript
       import type { SupabaseClient } from "@supabase/supabase-js";

       export interface WatchlistRow {
         stockCode: string;
         addedAt: string;
         position: number | null;
         stock: {
           code: string;
           name: string;
           market: "KOSPI" | "KOSDAQ";
           kosdaqSegment: string | null;
         };
         quote: {
           price: number;
           changeAmount: number;
           changeRate: number;
           tradeAmount: number;
           updatedAt: string;
         } | null;
       }

       export async function fetchWatchlist(supabase: SupabaseClient): Promise<{ data: WatchlistRow[] | null; error: Error | null }> {
         const { data, error } = await supabase
           .from("watchlists")
           .select(`
             stock_code,
             added_at,
             position,
             stock:stocks!inner (
               code, name, market, kosdaq_segment
             ),
             quote:stock_quotes (
               price, change_amount, change_rate, trade_amount, updated_at
             )
           `)
           .order("added_at", { ascending: false });
         if (error || !data) return { data: null, error };
         // snake_case → camelCase 매핑
         const mapped: WatchlistRow[] = data.map((row: any) => ({
           stockCode: row.stock_code,
           addedAt: row.added_at,
           position: row.position,
           stock: {
             code: row.stock.code,
             name: row.stock.name,
             market: row.stock.market,
             kosdaqSegment: row.stock.kosdaq_segment,
           },
           quote: row.quote ? {
             price: Number(row.quote.price),
             changeAmount: Number(row.quote.change_amount),
             changeRate: Number(row.quote.change_rate),
             tradeAmount: Number(row.quote.trade_amount),
             updatedAt: row.quote.updated_at,
           } : null,
         }));
         return { data: mapped, error: null };
       }

       export async function addWatchlistItem(
         supabase: SupabaseClient,
         userId: string,
         stockCode: string
       ) {
         return supabase.from("watchlists").insert({ user_id: userId, stock_code: stockCode });
       }

       export async function removeWatchlistItem(
         supabase: SupabaseClient,
         userId: string,
         stockCode: string
       ) {
         return supabase
           .from("watchlists")
           .delete()
           .eq("user_id", userId)
           .eq("stock_code", stockCode);
       }
       ```

    3. 테스트 실행 + GREEN 확인. Integration 블록은 env 없으면 skip.
  </action>

  <acceptance_criteria>
    - `/Users/alex/repos/gh-radar/webapp/src/lib/watchlist-api.ts` exists.
    - File exports `fetchWatchlist`, `addWatchlistItem`, `removeWatchlistItem`, `WatchlistRow`.
    - File contains `stock:stocks!inner` and `quote:stock_quotes` (embedded resource 문법).
    - File contains `.order("added_at", { ascending: false })`.
    - File contains snake_case → camelCase 매핑 (e.g., `row.stock_code` → `stockCode`).
    - `/Users/alex/repos/gh-radar/webapp/src/lib/__tests__/watchlist-api.test.ts` exists with at least 3 unit tests.
    - `pnpm --filter @gh-radar/webapp test -- watchlist-api` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && pnpm test -- watchlist-api 2>&1 | tail -20</automated>
  </verify>

  <done>
    watchlist-api.ts + 테스트 파일 생성, 3 exports, JOIN 쿼리 + camelCase 매핑, 테스트 통과.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useWatchlistQuery 훅 (1분 폴링 + lastUpdatedAt MAX 계산) + unit test (D4)</name>

  <read_first>
    - /Users/alex/repos/gh-radar/webapp/src/hooks/use-polling.ts (기존 Scanner 폴링 훅 공용)
    - /Users/alex/repos/gh-radar/webapp/src/components/scanner/scanner-client.tsx (Scanner 클라이언트 동형 패턴)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"Pattern 10" (클라이언트 MAX 계산)
    - /Users/alex/repos/gh-radar/webapp/src/lib/watchlist-api.ts (Task 1 산출물)
    - /Users/alex/repos/gh-radar/webapp/src/lib/auth-context.tsx (Plan 01 산출물 — `useAuth()` 가 반환하는 `{ user, displayName, isLoading, signOut }` 사용)
  </read_first>

  <behavior>
    - Test 1 (fetches on mount): userId 주어지면 mount 직후 fetchWatchlist 1회 호출 + data 상태 반영.
    - Test 2 (polls every 60s): `vi.advanceTimersByTime(60_000)` → 2번째 fetch 호출 발생. `setInterval` interval 이 60_000ms 임을 call-count 로 검증.
    - Test 3 (stops polling when user becomes null): user=null 로 전환 시 setInterval clear + 이후 timers advance 해도 추가 fetch 호출 없음.
    - Test 4 (re-fetches on visibilitychange → visible): `document.dispatchEvent(new Event("visibilitychange"))` + `document.visibilityState === "visible"` 에서 즉시 refetch.
    - Test 5 (lastUpdatedAt = MAX(stock_quotes.updated_at)): 3 row 를 각기 다른 updated_at 으로 주입 → lastUpdatedAt 이 가장 큰 epoch ms 와 동일.
    - Test 6 (handles fetch errors gracefully): fetchWatchlist 가 error 반환 시 state.error 세팅 + 이전 data 유지 (rollback 금지).
  </behavior>

  <files>
    webapp/src/hooks/use-watchlist-query.ts,
    webapp/src/hooks/__tests__/use-watchlist-query.test.ts
  </files>

  <action>
    **RED — 테스트 파일 먼저 작성** (`webapp/src/hooks/__tests__/use-watchlist-query.test.ts`):
    ```typescript
    import { renderHook, act, waitFor } from "@testing-library/react";
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";

    // fetchWatchlist mock — 각 테스트마다 주입
    const fetchMock = vi.fn();
    vi.mock("@/lib/watchlist-api", () => ({
      fetchWatchlist: (...args: unknown[]) => fetchMock(...args),
    }));
    vi.mock("@/lib/supabase/client", () => ({ createClient: () => ({}) }));

    import { useWatchlistQuery } from "../use-watchlist-query";

    describe("useWatchlistQuery", () => {
      beforeEach(() => {
        vi.useFakeTimers();
        fetchMock.mockReset();
        fetchMock.mockResolvedValue({ data: [], error: null });
      });
      afterEach(() => { vi.useRealTimers(); vi.restoreAllMocks(); });

      it("fetches on mount", async () => {
        renderHook(() => useWatchlistQuery());
        await waitFor(() => expect(fetchMock).toHaveBeenCalledTimes(1));
      });

      it("polls every 60s (second fetch after 60_000ms)", async () => {
        renderHook(() => useWatchlistQuery());
        await waitFor(() => expect(fetchMock).toHaveBeenCalledTimes(1));
        await act(async () => { vi.advanceTimersByTime(60_000); });
        await waitFor(() => expect(fetchMock).toHaveBeenCalledTimes(2));
      });

      it("re-fetches on visibilitychange → visible", async () => {
        renderHook(() => useWatchlistQuery());
        await waitFor(() => expect(fetchMock).toHaveBeenCalledTimes(1));
        Object.defineProperty(document, "visibilityState", { value: "visible", configurable: true });
        await act(async () => { document.dispatchEvent(new Event("visibilitychange")); });
        await waitFor(() => expect(fetchMock).toHaveBeenCalledTimes(2));
      });

      it("computes lastUpdatedAt as MAX(quote.updatedAt)", async () => {
        const t1 = new Date("2026-04-16T06:00:00Z").toISOString();
        const t2 = new Date("2026-04-16T06:02:00Z").toISOString();  // MAX
        const t3 = new Date("2026-04-16T06:01:00Z").toISOString();
        fetchMock.mockResolvedValue({
          data: [
            { stockCode: "A", addedAt: t1, position: 0, stock: {}, quote: { updatedAt: t1, price: 0, changeAmount: 0, changeRate: 0, tradeAmount: 0 } },
            { stockCode: "B", addedAt: t2, position: 1, stock: {}, quote: { updatedAt: t2, price: 0, changeAmount: 0, changeRate: 0, tradeAmount: 0 } },
            { stockCode: "C", addedAt: t3, position: 2, stock: {}, quote: { updatedAt: t3, price: 0, changeAmount: 0, changeRate: 0, tradeAmount: 0 } },
          ],
          error: null,
        });
        const { result } = renderHook(() => useWatchlistQuery());
        await waitFor(() => expect(result.current.lastUpdatedAt).toBe(new Date(t2).getTime()));
      });

      it("handles fetch errors gracefully (sets error, preserves previous data)", async () => {
        fetchMock.mockResolvedValueOnce({ data: [{ stockCode: "Z", addedAt: "x", position: 0, stock: {}, quote: null }], error: null });
        const { result } = renderHook(() => useWatchlistQuery());
        await waitFor(() => expect(result.current.data).toHaveLength(1));
        fetchMock.mockResolvedValueOnce({ data: null, error: new Error("boom") });
        await act(async () => { await result.current.refresh(); });
        expect(result.current.error).toBeTruthy();
        expect(result.current.data).toHaveLength(1);  // 이전 data 유지
      });

      it("stops polling when unmounted", async () => {
        const { unmount } = renderHook(() => useWatchlistQuery());
        await waitFor(() => expect(fetchMock).toHaveBeenCalledTimes(1));
        unmount();
        await act(async () => { vi.advanceTimersByTime(120_000); });
        expect(fetchMock).toHaveBeenCalledTimes(1);
      });
    });
    ```
    `pnpm --filter @gh-radar/webapp test -- use-watchlist-query` → RED 확인 (파일 없음).

    **GREEN — 훅 구현:** `webapp/src/hooks/use-watchlist-query.ts` 작성. Scanner `use-polling.ts` + `scanner-client.tsx` 의 패턴 계승:

    ```typescript
    "use client";
    import { useCallback, useEffect, useRef, useState } from "react";
    import { createClient } from "@/lib/supabase/client";
    import { fetchWatchlist, type WatchlistRow } from "@/lib/watchlist-api";

    const POLL_INTERVAL_MS = 60_000;  // 1분 (Scanner 동일)

    export interface UseWatchlistQueryResult {
      data: WatchlistRow[];
      isLoading: boolean;
      isRefreshing: boolean;
      error: Error | null;
      lastUpdatedAt: number | null;  // epoch ms — MAX(quote.updated_at)
      refresh: () => Promise<void>;
    }

    export function useWatchlistQuery(): UseWatchlistQueryResult {
      const [data, setData] = useState<WatchlistRow[]>([]);
      const [isLoading, setIsLoading] = useState(true);
      const [isRefreshing, setIsRefreshing] = useState(false);
      const [error, setError] = useState<Error | null>(null);
      const [lastUpdatedAt, setLastUpdatedAt] = useState<number | null>(null);
      const mountedRef = useRef(true);

      const load = useCallback(async (initial: boolean) => {
        if (initial) setIsLoading(true);
        else setIsRefreshing(true);
        const supabase = createClient();
        const { data: rows, error: err } = await fetchWatchlist(supabase);
        if (!mountedRef.current) return;
        if (err) {
          setError(err);
        } else if (rows) {
          setData(rows);
          setError(null);
          // MAX(quote.updated_at) 계산 (Pattern 10)
          const maxMs = rows.reduce((max, row) => {
            const t = row.quote?.updatedAt ? new Date(row.quote.updatedAt).getTime() : 0;
            return t > max ? t : max;
          }, 0);
          setLastUpdatedAt(maxMs > 0 ? maxMs : null);
        }
        if (initial) setIsLoading(false);
        else setIsRefreshing(false);
      }, []);

      useEffect(() => {
        mountedRef.current = true;
        load(true);
        const id = setInterval(() => {
          if (document.visibilityState === "visible") load(false);
        }, POLL_INTERVAL_MS);
        const onVis = () => {
          if (document.visibilityState === "visible") load(false);
        };
        document.addEventListener("visibilitychange", onVis);
        return () => {
          mountedRef.current = false;
          clearInterval(id);
          document.removeEventListener("visibilitychange", onVis);
        };
      }, [load]);

      return { data, isLoading, isRefreshing, error, lastUpdatedAt, refresh: () => load(false) };
    }
    ```

    **중요:**
    - 60_000ms interval (Scanner 동일)
    - visibilityState 체크로 백그라운드 탭 폴링 방지
    - lastUpdatedAt 은 epoch ms — UI 에서 `new Date(lastUpdatedAt).toLocaleTimeString("ko-KR", { hour12: false })` 로 포맷 (Scanner 동일 스타일)
    - mountedRef 로 unmount 후 setState 경고 방지
  </action>

  <acceptance_criteria>
    - `/Users/alex/repos/gh-radar/webapp/src/hooks/use-watchlist-query.ts` exists and exports `useWatchlistQuery`, `UseWatchlistQueryResult`.
    - File contains `POLL_INTERVAL_MS = 60_000` (or equivalent 60000).
    - File contains `fetchWatchlist(` import from watchlist-api.
    - File contains MAX 계산 reduce (`> max ? t : max`).
    - File contains visibilitychange listener.
    - `/Users/alex/repos/gh-radar/webapp/src/hooks/__tests__/use-watchlist-query.test.ts` exists with ≥6 test cases covering: fetches on mount, polls every 60s, re-fetches on visibilitychange visible, lastUpdatedAt MAX computation, error handling with previous-data preservation, unmount stops polling.
    - `pnpm --filter @gh-radar/webapp exec tsc --noEmit` exits 0.
    - `pnpm --filter @gh-radar/webapp test -- use-watchlist-query` exits 0 with all ≥6 tests passing.
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && pnpm exec tsc --noEmit && grep -q 'POLL_INTERVAL_MS = 60' src/hooks/use-watchlist-query.ts && grep -q 'fetchWatchlist' src/hooks/use-watchlist-query.ts && grep -q 'visibilitychange' src/hooks/use-watchlist-query.ts && test -f src/hooks/__tests__/use-watchlist-query.test.ts && pnpm test -- use-watchlist-query 2>&1 | tail -15</automated>
  </verify>

  <done>
    useWatchlistQuery 훅 구현 + unit test 6종 통과. 1분 폴링 + visibility API + lastUpdatedAt MAX 계산 + 에러 핸들링 검증됨.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Watchlist UI — Table + Empty + Skeleton + Client + Page 진입점</name>

  <read_first>
    - /Users/alex/repos/gh-radar/webapp/src/components/scanner/scanner-client.tsx (동형 패턴 — 헤더 + 분기 + 폴링)
    - /Users/alex/repos/gh-radar/webapp/src/components/scanner/scanner-table.tsx (Table grid 패턴 — 7컬럼으로 확장)
    - /Users/alex/repos/gh-radar/webapp/src/components/scanner/scanner-skeleton.tsx (skeleton 패턴)
    - /Users/alex/repos/gh-radar/webapp/src/components/stock/info-stock-card.tsx (Plan 05 산출물)
    - /Users/alex/repos/gh-radar/webapp/src/components/layout/app-sidebar.tsx (Plan 04 산출물)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-UI-SPEC.md §"4.2", §"4.3", §"4.4", §"4.5"
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"Pattern 10"
  </read_first>

  <files>
    webapp/src/components/watchlist/watchlist-empty.tsx,
    webapp/src/components/watchlist/watchlist-table.tsx,
    webapp/src/components/watchlist/watchlist-skeleton.tsx,
    webapp/src/components/watchlist/watchlist-client.tsx,
    webapp/src/app/watchlist/page.tsx
  </files>

  <action>
    **주의 — 5 파일 순차 생성 순서** (의존성 역방향: empty → skeleton → table → client → page): 3.1 empty → 3.2 skeleton → 3.3 table → 3.4 client (table/empty/skeleton 합성) → 3.5 page (client 주입). 실제 분할 아님 — 한 task 안 순차 작성.

    **3.1 — `webapp/src/components/watchlist/watchlist-empty.tsx`** (UI-SPEC §4.5):
    ```tsx
    import Link from "next/link";
    import { Star } from "lucide-react";
    import { Button } from "@/components/ui/button";

    export function WatchlistEmpty() {
      return (
        <div
          role="status"
          className="flex min-h-[240px] flex-col items-center justify-center gap-3 rounded-lg border border-[var(--border)] bg-[var(--card)] p-6 text-center"
        >
          <Star className="size-10 text-[var(--muted-fg)]" aria-hidden="true" />
          <h2 className="text-[length:var(--t-base)] font-semibold text-[var(--fg)]">
            아직 관심종목이 없습니다
          </h2>
          <p className="text-[length:var(--t-sm)] text-[var(--muted-fg)]">
            스캐너에서 ⭐ 를 눌러 관심종목을 추가해보세요.
          </p>
          <Button asChild>
            <Link href="/scanner">스캐너로 가기</Link>
          </Button>
        </div>
      );
    }
    ```

    **3.2 — `webapp/src/components/watchlist/watchlist-table.tsx`** (UI-SPEC §4.2, 7컬럼):
    ```tsx
    import Link from "next/link";
    import { memo } from "react";
    import { Badge } from "@/components/ui/badge";
    import { Number as NumberDisplay } from "@/components/ui/number";
    import { cn } from "@/lib/utils";
    import type { WatchlistRow } from "@/lib/watchlist-api";

    export interface WatchlistTableProps {
      rows: WatchlistRow[];
      isRefreshing?: boolean;
      /** Plan 07 에서 toggle slot 주입 */
      renderToggle?: (row: WatchlistRow) => React.ReactNode;
    }

    const GRID_COLS = "grid grid-cols-[1fr_100px_80px_120px_100px_140px_44px] items-center gap-3 px-3";

    function WatchlistTableBase({ rows, isRefreshing, renderToggle }: WatchlistTableProps) {
      return (
        <div
          role="table"
          className={cn(
            "hidden lg:block overflow-hidden rounded-[var(--r)] border border-[var(--border)]",
            isRefreshing && "opacity-90 transition-opacity"
          )}
        >
          <div
            role="row"
            className={cn(GRID_COLS, "bg-[var(--muted)] py-2 text-[length:var(--t-caption)] font-semibold text-[var(--muted-fg)] uppercase tracking-wide")}
          >
            <span>종목명</span>
            <span>코드</span>
            <span>마켓</span>
            <span className="text-right">현재가</span>
            <span className="text-right">등락률</span>
            <span className="text-right">거래대금</span>
            <span className="text-right" aria-label="관심종목 해제">⭐</span>
          </div>
          {rows.map((row) => {
            const quote = row.quote;
            const changeRate = quote?.changeRate ?? 0;
            const sign = changeRate > 0 ? "+" : "";
            const variant = changeRate > 0 ? "up" : changeRate < 0 ? "down" : "flat";
            return (
              <div key={row.stockCode} role="row" className={cn(GRID_COLS, "border-t border-[var(--border)] py-2 text-[length:var(--t-sm)] hover:bg-[var(--muted)]")}>
                <Link href={`/stocks/${row.stockCode}`} className="truncate font-medium text-[var(--fg)]">
                  {row.stock.name}
                </Link>
                <span className="mono text-[var(--muted-fg)]">{row.stockCode}</span>
                <Badge variant={row.stock.market === "KOSPI" ? "secondary" : "outline"}>
                  {row.stock.market}
                </Badge>
                <span className="mono text-right text-[var(--fg)]">
                  {quote ? <NumberDisplay value={quote.price} format="price" /> : <span className="text-[var(--muted-fg)]">—</span>}
                </span>
                <span className={cn("mono text-right", variant === "up" && "text-[var(--up)]", variant === "down" && "text-[var(--down)]", variant === "flat" && "text-[var(--flat)]")}>
                  {quote ? `${sign}${changeRate.toFixed(2)}%` : "—"}
                </span>
                <span className="mono text-right text-[var(--muted-fg)]">
                  {quote ? <NumberDisplay value={quote.tradeAmount} format="trade-amount" /> : "—"}
                </span>
                <span className="text-right">
                  {renderToggle ? renderToggle(row) : <div className="size-9" aria-hidden="true" />}
                </span>
              </div>
            );
          })}
        </div>
      );
    }

    export const WatchlistTable = memo(WatchlistTableBase);
    ```

    **3.3 — `webapp/src/components/watchlist/watchlist-skeleton.tsx`:** scanner-skeleton 복제 후 `lg:block`/`lg:hidden` + 7컬럼 grid.
    ```tsx
    import { Skeleton } from "@/components/ui/skeleton";

    export function WatchlistSkeleton() {
      return (
        <>
          {/* Table skeleton (lg+) */}
          <div className="hidden lg:block space-y-2">
            {Array.from({ length: 8 }).map((_, i) => (
              <Skeleton key={i} className="h-10 w-full" />
            ))}
          </div>
          {/* Card skeleton (<lg) */}
          <div className="lg:hidden flex flex-col gap-2">
            {Array.from({ length: 6 }).map((_, i) => (
              <Skeleton key={i} className="h-16 w-full rounded-lg" />
            ))}
          </div>
        </>
      );
    }
    ```

    **3.4 — `webapp/src/components/watchlist/watchlist-client.tsx`:**
    ```tsx
    "use client";
    import { useWatchlistQuery } from "@/hooks/use-watchlist-query";
    import { InfoStockCard } from "@/components/stock/info-stock-card";
    import { WatchlistTable } from "./watchlist-table";
    import { WatchlistEmpty } from "./watchlist-empty";
    import { WatchlistSkeleton } from "./watchlist-skeleton";
    import type { WatchlistRow } from "@/lib/watchlist-api";

    function rowToStock(row: WatchlistRow) {
      // InfoStockCard 는 StockWithProximity 타입을 받음 — 필요한 필드만 매핑
      const quote = row.quote;
      return {
        code: row.stockCode,
        name: row.stock.name,
        market: row.stock.market,
        price: quote?.price ?? 0,
        changeRate: quote?.changeRate ?? 0,
        changeAmount: quote?.changeAmount ?? 0,
        tradeAmount: quote?.tradeAmount ?? 0,
        volume: 0,  // Watchlist 는 volume 미표시 — InfoStockCard 는 안 씀
        // 기타 StockWithProximity 필드는 optional 처리 또는 타입 만족용 0/empty
      } as any;  // 실행자 재량: 공통 StockCardView 타입 분리 or cast
    }

    export function WatchlistClient() {
      const { data, isLoading, isRefreshing, error, lastUpdatedAt } = useWatchlistQuery();
      const fmtTime = lastUpdatedAt ? new Date(lastUpdatedAt).toLocaleTimeString("ko-KR", { hour12: false }) : "—";

      return (
        <section aria-label="관심종목" className="space-y-4">
          <header className="flex items-center justify-between">
            <div>
              <h1 className="text-[length:var(--t-h3)] font-semibold text-[var(--fg)]">관심종목</h1>
              <p className="text-[length:var(--t-sm)] text-[var(--muted-fg)]">저장한 종목의 시세를 1분마다 갱신합니다</p>
            </div>
            <div className="flex items-center gap-2 text-[length:var(--t-sm)] text-[var(--muted-fg)]">
              <span className={`size-2 rounded-full ${isRefreshing ? "bg-[var(--up)] animate-ping" : "bg-[var(--flat)]"}`} aria-hidden="true" />
              <span className="mono tabular-nums">최근 갱신 {fmtTime} KST</span>
            </div>
          </header>

          {isLoading ? (
            <WatchlistSkeleton />
          ) : error ? (
            <div role="alert" className="rounded-md border border-[var(--destructive)]/40 bg-[var(--destructive)]/10 p-4 text-[var(--destructive)]">
              관심종목을 불러오지 못했습니다. 새로고침해주세요.
            </div>
          ) : data.length === 0 ? (
            <WatchlistEmpty />
          ) : (
            <>
              {/* lg+ Table */}
              <WatchlistTable rows={data} isRefreshing={isRefreshing} />
              {/* <lg InfoStockCard */}
              <ul className="lg:hidden flex flex-col gap-2 list-none p-0 m-0">
                {data.map((row) => (
                  <li key={row.stockCode}>
                    <InfoStockCard stock={rowToStock(row)} />
                  </li>
                ))}
              </ul>
            </>
          )}
        </section>
      );
    }
    ```

    **3.5 — `webapp/src/app/watchlist/page.tsx`:**
    ```tsx
    "use client";
    import { AppShell } from "@/components/layout/app-shell";
    import { AppSidebar } from "@/components/layout/app-sidebar";
    import { WatchlistClient } from "@/components/watchlist/watchlist-client";

    export default function WatchlistPage() {
      return (
        <AppShell sidebar={<AppSidebar />}>
          <WatchlistClient />
        </AppShell>
      );
    }
    ```

    **참고 (실행자 재량):** `rowToStock` cast 는 타입 안전성 개선 위해 별도 `StockCardView` interface 분리 가능. 그 경우 InfoStockCard 시그니처도 함께 수정 — 하지만 Plan 05 의 Scanner 경로 회귀 없도록 주의. 이 plan 은 최소 수정으로 진행.
  </action>

  <acceptance_criteria>
    - All 5 files exist: watchlist-empty.tsx, watchlist-table.tsx, watchlist-skeleton.tsx, watchlist-client.tsx, `app/watchlist/page.tsx`.
    - `watchlist-empty.tsx` contains `"아직 관심종목이 없습니다"` and `href="/scanner"`.
    - `watchlist-table.tsx` contains `grid-cols-[1fr_100px_80px_120px_100px_140px_44px]` (7-col grid) and `hidden lg:block` and `renderToggle` prop.
    - `watchlist-skeleton.tsx` contains both `hidden lg:block` and `lg:hidden` variants.
    - `watchlist-client.tsx` contains `useWatchlistQuery()` usage and `WatchlistEmpty` + `WatchlistTable` + `InfoStockCard` branches.
    - `watchlist-client.tsx` contains `toLocaleTimeString("ko-KR"` for 갱신시각.
    - `app/watchlist/page.tsx` contains `<AppShell sidebar={<AppSidebar`.
    - `pnpm --filter @gh-radar/webapp exec tsc --noEmit` exits 0.
    - `pnpm --filter @gh-radar/webapp build` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && pnpm exec tsc --noEmit && grep -q 'grid-cols-\[1fr_100px_80px_120px_100px_140px_44px\]' src/components/watchlist/watchlist-table.tsx && grep -q '아직 관심종목이 없습니다' src/components/watchlist/watchlist-empty.tsx && grep -q 'useWatchlistQuery' src/components/watchlist/watchlist-client.tsx && grep -q 'AppSidebar' src/app/watchlist/page.tsx && pnpm build 2>&1 | tail -5</automated>
  </verify>

  <done>
    5 Watchlist UI 파일 생성, lg+ Table / <lg InfoStockCard 분기, empty/error/loading 상태 완비, build 통과.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user ↔ Supabase PostgREST (watchlists) | RLS 가 본인 row 만 반환 |
| Client Supabase query → UI | JOIN 결과의 `stock.name`, `stock.code`, `quote.price` 등 렌더 |
| 탭 가시성 → 폴링 | visibilitychange 로 백그라운드 탭 차단 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-24 | Elevation of Privilege | 타인 watchlist row 읽기 시도 | mitigate | Plan 02 의 RLS `auth_select_own_watchlists` (`auth.uid() = user_id`) 가 DB 레벨 차단. 클라이언트 `fetchWatchlist()` 가 `user_id` 필터 없이도 안전 (RLS 가 자동 필터). ASVS V4.1.1. Verified by: Task 1 integration test `watchlist-api.test.ts::rls-isolation` (env 있을 때) + Plan 08 `playwright watchlist.spec.ts`. |
| T-06.2-25 | Tampering | JOIN 쿼리의 `!inner` 힌트 오남용 (stock 마스터 없는 row 노출) | accept | Plan 02 의 FK CASCADE 로 stocks 삭제 시 watchlists 도 자동 삭제 — 실질 불일치 없음. 만약 race 로 불일치가 생기면 `!inner` 가 row 제외 → 안전. ASVS V5.3.10. |
| T-06.2-26 | Denial of Service | 1분 폴링 + 모바일 bfcache 복귀 시 burst 요청 | mitigate | `visibilityState === "visible"` 체크로 백그라운드 탭 폴링 금지. `mountedRef` 로 unmount 후 setState 금지. 50개 row 상한. ASVS V11.1. Verified by: Task 2 acceptance. |
| T-06.2-27 | Information Disclosure | 에러 메시지가 DB 내부 구조 노출 | mitigate | UI 에 표시되는 에러는 한글 고정 문구 `"관심종목을 불러오지 못했습니다. 새로고침해주세요."` — Supabase 원본 에러는 console 만. ASVS V7.4.1. Verified by: Task 3 acceptance (`alert` div 고정 문구). |

</threat_model>

<verification>
- **Per-task:** Task 1 TDD + tsc + grep.
- **Wave-end:** `pnpm --filter @gh-radar/webapp build` + 수동 `pnpm dev` → 로그인 후 `/watchlist` → 빈 리스트 → Empty state → `/scanner` 이동 등 smoke.
- **Integration (D3):** Task 1 RLS 격리 integration test — SUPABASE_E2E_URL env 있을 때 실행.
- **Plan 08 E2E:** `e2e/specs/watchlist.spec.ts` 가 `::responsive-breakpoint` (Table lg+ / Card <lg) + `::empty-state` 커버.
</verification>

<success_criteria>
- 8 Watchlist 파일 생성 (api + hook + 5 components + page)
- lg+ Table 7컬럼 / <lg InfoStockCard 분기 작동
- 1분 폴링 + visibility API
- Empty state + Error state + Loading skeleton 커버
- watchlist-api 단위 테스트 통과
- build 통과
</success_criteria>

<output>
`.planning/phases/06.2-auth-watchlist/06.2-06-SUMMARY.md`:
- 8 파일 경로 + 책임 요약
- `WatchlistRow` / `useWatchlistQuery` / `WatchlistTable.renderToggle` 인터페이스 (Plan 07 에서 소비)
- 폴링 주기 + visibility API 동작 확인
- Empty state 한글 copy 인용
- 다음 plan (07 ⭐ 토글) 이 wire up 해야 할 slot: `WatchlistTable.renderToggle`, `InfoStockCard.watchlistToggleSlot`
</output>
