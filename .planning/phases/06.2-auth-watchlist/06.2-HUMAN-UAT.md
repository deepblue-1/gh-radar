---
status: resolved
phase: 06.2-auth-watchlist
source: [06.2-VERIFICATION.md]
started: 2026-04-16T13:00:00Z
updated: 2026-04-16T13:05:00Z
---

## Current Test

[all approved]

## Tests

### 1. Google OAuth 전체 플로우 (프로덕션/로컬 실기)
expected: /login → '구글로 로그인' 클릭 → Google 동의 → /auth/callback → /scanner 자동 도달. 로그아웃 후 재진입 시 prompt=select_account 로 계정 선택창 재노출
result: passed — 사용자가 https://gh-radar-webapp.vercel.app/login 에서 Google 로그인 왕복 성공 확인 (2026-04-16)

### 2. Vercel production 재배포 후 /login → Google 로그인 종단 확인
expected: NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY 가 프로덕션 빌드에 실제로 주입되어 /login → Google → /scanner 가 작동
result: passed — dpl_mf2776cl1 (2026-04-16 21:56 KST) 배포 후 middleware 307 리다이렉트 정상 + 사용자 실기 로그인 성공

### 3. 50 limit 메시지 UX 톤
expected: 50개 도달 사용자가 51번째 ⭐ 클릭 시 '관심종목은 최대 50개까지 저장할 수 있습니다.' 노출 + disabled 시각 피드백이 단호하지도 무례하지도 않은 톤으로 전달
result: approved — 문구/로직은 E2E 자동 검증 통과 (watchlist.spec.ts:50-limit). UX 톤 주관 평가는 사용자 승인.

### 4. iPad / 태블릿 실기 viewport (≈1024px) 에서 Scanner / Watchlist 분기
expected: iPad 가로에서 /scanner, /watchlist 접근 시 lg 브레이크포인트가 의도대로 Table / InfoStockCard 중 어느 쪽으로 떨어지는지 시각적으로 확인
result: approved — lg 브레이크포인트 분기 로직 E2E 검증 통과 (watchlist.spec.ts:responsive-breakpoint). 실기 뷰포트 검증은 사용자 승인.

### 5. UserSection 팝오버 포커스 트랩 + 스크린리더 라벨링
expected: 트리거 클릭 → 팝오버 open → Tab 순환 → ESC 닫힘 → 포커스 트리거로 복귀. 스크린리더(VoiceOver) 가 이메일 + '로그아웃' 버튼을 순차 읽음
result: approved — a11y axe spec 정적 검증 통과. 실기 키보드/VoiceOver 검증은 사용자 승인.

## Summary

total: 5
passed: 2
approved: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
