# Phase 06.2: Auth + Watchlist - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-16
**Phase:** 06.2-auth-watchlist
**Areas discussed:** Auth 아키텍처 경로, 소셜 로그인 (Kakao 중심), 로그인 UI + 라우트 보호, Watchlist 스키마 & UI

---

## Gray Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Auth 아키텍처 경로 | webapp @supabase/ssr 직접 vs Express server 경유 | ✓ |
| 소셜 로그인 (Kakao 중심) | Google은 Supabase built-in / Kakao 통합 방식 결정 | ✓ |
| 로그인 UI + 라우트 보호 | 모달 vs /login, middleware 가드, ?next 정책 | ✓ |
| Watchlist 스키마 & UI | 테이블 구조, 정렬, 표현, ⭐ 토글 배치 | ✓ |

---

## Auth 아키텍처 경로

### Q1: 인증 세션 주관자 경로는 어디?

| Option | Description | Selected |
|--------|-------------|----------|
| webapp 직접 (@supabase/ssr) | Next.js 미들웨어가 httpOnly 쿠키로 세션 관리. Supabase 표준 패턴 | |
| Express server 경유 | 기존 API 경로 일관성. Express가 세션/service_role 대리 | |
| 하이브리드 | 세션 webapp, Watchlist CRUD Express 경유 | |

**User's choice:** "weekly-wine-bot 어드민의 인증 방식을 참고해줄래? supabase 기반으로 구글로그인을 지원하고 있는데, 이대로 하는게 어떨까?"
**Notes:** Claude가 weekly-wine-bot/admin 코드베이스(~/repos/weekly-wine-bot)를 읽고 패턴 확인 — @supabase/ssr, 3-파일 클라이언트, proxy.ts(루트 middleware), /auth/callback/route.ts, AuthProvider context. gh-radar는 일반 사용자 대상이라 user_roles 화이트리스트 제거 외 그대로 차용.

### Q2: Watchlist CRUD API 위치

| Option | Description | Selected |
|--------|-------------|----------|
| webapp에서 Supabase 직접 (RLS) | createBrowserClient로 watchlists 테이블 직접 CRUD, RLS 보장 | ✓ |
| Express /api/watchlist 신규 라우트 | JWT 검증 + service_role 대리 | |
| Next API routes (/app/api/watchlist) | Next 내부 API Route에서 처리 | |

**User's choice:** webapp에서 Supabase 직접 (RLS)
**Notes:** 추가 계층 없이 RLS가 auth.uid() = user_id 보장. weekly-wine-bot의 user_roles 직접 조회 패턴과 동일.

---

## 소셜 로그인 (Kakao 중심)

### Q1: Kakao 로그인 통합 방식

| Option | Description | Selected |
|--------|-------------|----------|
| Supabase 커스텀 OIDC 등록 | Supabase Dashboard OIDC로 Kakao 등록, signInWithOAuth 동일 API | |
| Kakao 자체 OAuth + signInWithIdToken | 클라이언트가 Kakao OAuth 직접 수행 + Supabase 매핑 | |
| Phase 06.2 범위에서 Kakao 제외 (Google만) | v1 Google only, Kakao는 후속 | ✓ |

**User's choice:** "구글로그인만 지원하자."
**Notes:** 초기 명시적 선택.

### Q2: 이메일/비밀번호 로그인(AUTH-01)도 포함?

| Option | Description | Selected |
|--------|-------------|----------|
| Google만 — 이메일도 뒤로 | 소셜 루트만 | ✓ |
| Google + 이메일/비번 모두 | 완전 AUTH-01/02 | |

**User's choice:** "카카오/이메일 등을 후속 phase로 미루는게 아니고, 그냥 오직 구글로그인만 지원할거야"
**Notes:** 단순한 범위 축소가 아닌 명시적 "Google ONLY" 의도. REQUIREMENTS.md의 AUTH-01은 v1 Scope에서 제거 또는 축소 필요 (verification 단계에서 반영).

---

## 로그인 UI + 라우트 보호

### Q1: 라우트 가드 기본 정책

| Option | Description | Selected |
|--------|-------------|----------|
| 기본 공개 + /watchlist만 보호 | Scanner/Search/Detail 공개 유지, /watchlist만 로그인 벽 | |
| 기본 차단 + 공개 경로 whitelist | 모든 경로 인증 필수, /login·/auth만 예외 | ✓ |

**User's choice:** 기본 차단 + 공개 경로 화이트리스트
**Notes:** Claude가 공개 기능 중단 위험 지적 후 확인 질의 (Q2).

### Q2: 기본 차단 선택 — whitelist 범위

| Option | Description | Selected |
|--------|-------------|----------|
| /, /scanner, /stocks/*, /login, /auth/* 공개 | watchlist만 보호, 현 공개 기능 보존 | |
| /, /login, /auth/* 만 공개 | 모든 기능 로그인 필요, 제품 성격 전환 | ✓ |

**User's choice:** 모든 기능 로그인 필요 — /, /login, /auth/* 만 공개
**Notes:** 제품 positioning 전환 — "공개 스캐너"에서 "로그인 사용자 전용 툴"로. Scanner 회귀 E2E(Phase 6의 9종) 로그인 fixture 재구성 필요. PROJECT.md Core Value 재진술은 verification 단계에서.

### Q3: 로그인 후 리다이렉트 기본값

| Option | Description | Selected |
|--------|-------------|----------|
| ?next=... 있으면 따르고 없으면 /scanner | deep link 보존 + 핵심 경로 유도 | ✓ |
| 항상 /watchlist로 | 로그인의 주목적이 관심종목 | |
| 항상 /scanner로 | ?next 무시 | |

**User's choice:** ?next=... 있으면 따르고 없으면 /scanner
**Notes:** weekly-wine-bot도 동일 ?next 패턴.

### Q4: 헤더 유저 메뉴(로그인 후)

| Option | Description | Selected |
|--------|-------------|----------|
| 아바타 클릭 → shadcn popover/dropdown | 헤더 우측 아바타 popover에 유저 정보/메뉴 | |
| 텍스트 로그아웃 버튼만 | 간단하게 [이메일] [로그아웃] | |
| 사이드바에 사용자 섹션 | AppShell 사이드바 하단에 배치 | ✓ |

**User's choice:** 사이드바에 사용자 섹션
**Notes:** AppShell hideSidebar=true 기본값 변경 수반. /design 카탈로그는 기존 hideSidebar 유지 (회귀 방지).

---

## Watchlist 스키마 & UI

### Q1: Watchlist 테이블 구조

| Option | Description | Selected |
|--------|-------------|----------|
| watchlists(user_id, stock_code, added_at, position) | PK=(user_id, stock_code), position으로 드래그 리오더 가능 | ✓ |
| 단순 (position 없이) | added_at DESC 고정 | |
| 폴더/에이리어스 다권 구조 | watchlists(id, name) + items | |

**User's choice:** watchlists(user_id, stock_code, added_at, position)
**Notes:** v1 UI는 added_at DESC 정렬, position 컬럼은 후속 드래그 리오더용.

### Q2: Watchlist UI 표현

| Option | Description | Selected |
|--------|-------------|----------|
| Scanner와 동일한 테이블 리스트 | Table+Card 듀얼, useScannerQuery 재사용 | ✓ |
| 심플 카드 그리드 | 2-3열 그리드, 큰 가격 표시 | |
| 컴팩트 리스트 | 1행당 종목명/가격/등락률만 | |

**User's choice:** Scanner와 동일한 테이블 리스트
**Notes:** Scanner 컴포넌트 재사용 — 개발 부담 감소 + UX 일관성.

### Q3: ⭐ (관심종목 추가/해제) 토글 배치

| Option | Description | Selected |
|--------|-------------|----------|
| 종목 상세 페이지만 | /stocks/[code] StockHero에 ⭐ 하나 | |
| 종목 상세 + Scanner 행 | 양쪽 모두 | ✓ |
| 종목 상세 + Watchlist 페이지(해제용) | 추가 상세에서만, 해제 watchlist에서 | |

**User's choice:** 종목 상세 + Scanner 행
**Notes:** Scanner에서 한번에 여러 종목 빠르게 추가 가능. UI 밀도 확인 필요 (planner 재량).

### Q4: 중복 저장 방지 · watchlist 크기 제한

| Option | Description | Selected |
|--------|-------------|----------|
| PK(user_id, stock_code), 개수 무제한 | DB 중복 방지, 개수 제한 없음 | |
| 중복 방지 + 사용자당 50개 제한 | RLS CHECK/trigger로 50개 제한 | ✓ |

**User's choice:** 중복 방지 + 사용자당 50개 제한
**Notes:** UI 스크롤 단절 방지 + DB 비용 관리.

---

## 결론 확인

**Q:** 다음 단계
**Options:** CONTEXT.md 작성 / 추가 gray area / 영역 재방문
**User's choice:** CONTEXT.md 작성

---

## Claude's Discretion

- 사이드바 유저 섹션 구체 디자인 (아바타 이니셜 / 구글 프로필 이미지)
- 50개 제한 초과 UI 메시지
- `?error=` 파라미터 전체 케이스
- AuthProvider 타임아웃/재검증 상수 값
- Scanner E2E 로그인 fixture 전환 방식
- Supabase Dashboard 설정 자동화 범위
- ⭐ 토글 구체 shadcn 컴포넌트 선택

## Deferred Ideas

- Kakao 소셜 로그인 (별도 phase)
- 이메일/비밀번호 로그인 (별도 phase)
- 드래그 리오더 UI (position 컬럼 확보, UI는 후속)
- 복수 watchlists / 폴더 구조 (v2)
- PERS-02 관심종목 기반 뉴스 피드 (Phase 7 또는 후속)
- NOTF-02 관심종목 급등 알림 (v2)
