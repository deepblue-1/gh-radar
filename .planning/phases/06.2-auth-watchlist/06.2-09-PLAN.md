---
phase: 06.2
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/SETUP.md
  - webapp/.env.local.example
autonomous: false
requirements: [AUTH-02]
requirements_addressed:
  - id: AUTH-02
    status: partial
    notes: "Google OAuth 운영 셋업 문서화 (Supabase Dashboard + Google Cloud Console + Vercel env). 수동 절차 안내."

must_haves:
  truths:
    - "webapp/SETUP.md 문서가 Google Cloud Console OAuth 2.0 클라이언트 발급 + Supabase Dashboard Google provider 활성화 + Site URL / Redirect URLs + Vercel env 확인 절차를 단계별로 안내한다"
    - "webapp/.env.local.example 이 NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, E2E_TEST_* 키를 주석과 함께 나열한다"
    - "Supabase 와 Google 의 redirect URI 혼동 (Pitfall 2) 을 방지하는 명시적 경고가 포함된다"
  artifacts:
    - path: "webapp/SETUP.md"
      provides: "Phase 06.2 Auth + Watchlist 운영 셋업 가이드"
      contains: "Google Cloud Console"
    - path: "webapp/.env.local.example"
      provides: "로컬 개발 환경변수 템플릿"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
  key_links:
    - from: "SETUP.md"
      to: "supabase/SCHEMA.md"
      via: "Watchlists 스키마 참조"
      pattern: "watchlists"
---

<objective>
Phase 06.2 의 Google OAuth 운영 셋업 (Supabase Dashboard + Google Cloud Console + Vercel env) 절차를 `webapp/SETUP.md` 에 문서화하고, 로컬 개발용 `webapp/.env.local.example` 템플릿을 갱신한다. 이 문서들은 실행자가 Phase 06.2 를 새 환경에 재배포하거나 E2E 테스트 유저를 새로 세팅할 때 참조하는 유일한 레퍼런스가 된다.

Purpose: RESEARCH Pitfall 2 (redirect URI 혼동) + Open Question 1 (Vercel env) + Open Question 2 (Google OAuth Client) 해결. 문서는 1회 수작업 기반이지만 재배포/roll-forward 시 재사용.

Output: 2개 파일. `autonomous: true` — 문서 작성은 완전 자동화되며, Task 3 체크포인트는 `non-blocking` 이라 실행자가 사용자 안내 후 `deferred` 로도 진행 가능 (실제 Dashboard 설정은 배포 시점에 수행).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-auth-watchlist/06.2-CONTEXT.md
@.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"Runtime State Inventory", §"Pitfall 2", §"Environment Availability"
@.planning/phases/06.2-auth-watchlist/06.2-UI-SPEC.md §"Copywriting Contract"
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: webapp/SETUP.md 작성 — Google OAuth + Supabase + Vercel 절차</name>

  <read_first>
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"Runtime State Inventory"
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"Pitfall 2: Google OAuth Redirect URI 혼동"
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"Pitfall 8: Vercel Preview Deploy URL"
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-02-SUMMARY.md (watchlists 스키마 push 결과)
    - /Users/alex/repos/gh-radar/webapp/scripts/seed-test-user.ts (Plan 08 Task 1 — 존재 여부 확인 후 링크)
  </read_first>

  <files>
    webapp/SETUP.md
  </files>

  <action>
    `webapp/SETUP.md` 신규 작성 (한글). 섹션:

    ```markdown
    # webapp Setup — Phase 06.2 Auth + Watchlist

    이 문서는 Phase 06.2 이후 Google OAuth 로그인 + 관심종목 기능을 운영하기 위한
    수동 설정 절차를 단계별로 기록한다. 신규 환경(프로덕션, staging, 개발자 로컬)에
    코드를 배포할 때 참조한다.

    ## 1. Supabase Dashboard 설정

    ### 1.1 Google OAuth Provider 활성화
    1. Supabase Dashboard → Project `gh-radar` → Authentication → Providers
    2. Google 활성화 (토글 ON)
    3. 아래 Google Cloud Console 단계 (§2) 에서 얻은 **Client ID + Client Secret** 입력

    ### 1.2 URL Configuration
    - Authentication → URL Configuration
    - **Site URL:** `https://gh-radar-webapp.vercel.app`
    - **Redirect URLs (모두 등록):**
      - `https://gh-radar-webapp.vercel.app/auth/callback`
      - `http://localhost:3000/auth/callback`
      - 필요 시 Vercel preview URL 개별 추가 (Pitfall 8: wildcard 미지원)

    ### 1.3 watchlists 스키마 적용 (Plan 02 에서 완료됨)
    - `supabase db push` 로 `20260416120000_watchlists.sql` 반영
    - Table Editor → watchlists 테이블 + 4 RLS 정책 + trigger `trg_enforce_watchlist_limit` 확인
    - stocks / stock_quotes 정책이 `read_*` (TO anon, authenticated) 로 rename 되어 있는지 확인

    ## 2. Google Cloud Console — OAuth 2.0 Client 발급

    ⚠ **Pitfall 2 — redirect URI 혼동 주의:**
    Google Cloud Console 에는 **Supabase URL 만** 등록한다. gh-radar 앱 URL
    (`/auth/callback`) 을 등록하면 `redirect_uri_mismatch` 오류 발생.

    1. https://console.cloud.google.com/ → 프로젝트 선택 (없으면 신규 생성)
    2. APIs & Services → Credentials → Create Credentials → OAuth 2.0 Client ID
    3. Application type: **Web application**
    4. **Authorized redirect URIs (이것만):**
       ```
       https://<supabase-project-ref>.supabase.co/auth/v1/callback
       ```
       (`<supabase-project-ref>` 는 Supabase 프로젝트 URL 의 subdomain 에서 확인)
    5. Client ID + Client Secret 복사 → Supabase Dashboard §1.1 에 입력

    ## 3. Vercel 환경변수

    아래 변수들이 Vercel Production + Preview 환경에 설정되어 있는지 확인:

    | Key | 값 출처 |
    |-----|---------|
    | `NEXT_PUBLIC_SUPABASE_URL` | Supabase Dashboard → Settings → API → Project URL |
    | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase Dashboard → Settings → API → anon public key |

    ⚠ **service_role key 는 webapp 에 절대 설정하지 말 것** — RLS 를 우회할 수 있어 보안 위험.

    확인 명령 (Vercel CLI 설치 전제):
    ```bash
    vercel env ls
    ```

    ## 4. 로컬 개발

    1. `cp webapp/.env.local.example webapp/.env.local`
    2. `.env.local` 의 Supabase 키 값을 채움 (Vercel env 값과 동일)
    3. `pnpm --filter @gh-radar/webapp dev`
    4. `http://localhost:3000/login` → "Google로 로그인" 클릭 → Google OAuth → `/scanner` 도달 확인

    ## 5. E2E 테스트 유저 프로비저닝 (Plan 08 전제)

    1. 환경변수 설정 (`.env.test.local` 또는 쉘):
       ```bash
       export SUPABASE_URL="<Supabase URL>"
       export SUPABASE_SERVICE_ROLE_KEY="<service_role key — 절대 커밋 금지>"
       export E2E_TEST_EMAIL="e2e@gh-radar.local"
       export E2E_TEST_PASSWORD="<32자 이상 랜덤>"
       ```
    2. 스크립트 실행:
       ```bash
       pnpm --filter @gh-radar/webapp exec tsx scripts/seed-test-user.ts
       ```
    3. Playwright setup project 가 이 계정을 사용 — `pnpm --filter @gh-radar/webapp test:e2e` 전 자동 로그인.

    ## 6. 자주 발생하는 문제 (Pitfalls)

    - **"redirect_uri_mismatch"**: Google Cloud Console §2 redirect URI 를 앱 URL 로 잘못 등록. Supabase URL 만 등록.
    - **로그인 직후 /login 으로 튕김**: middleware 3단 쿠키 동기화 깨짐. `supabase/middleware.ts` 의 `supabaseResponse = NextResponse.next({ request })` 재생성 위치 확인.
    - **watchlist 페이지에서 종목명 비어있음**: stocks / stock_quotes RLS 가 authenticated 역할을 포함하지 않음. Plan 02 마이그레이션의 `TO anon, authenticated` 확장 재확인.
    - **Vercel Preview URL 에서만 로그인 실패**: Preview URL 이 Supabase Redirect URLs 에 없음. 개별 등록 필요 (wildcard 미지원).

    ## 7. 관련 참조

    - 마이그레이션: `supabase/migrations/20260416120000_watchlists.sql`
    - RLS 정책 설명: `supabase/SCHEMA.md` §watchlists
    - Phase 06.2 CONTEXT: `.planning/phases/06.2-auth-watchlist/06.2-CONTEXT.md`
    - weekly-wine-bot admin 참조 구현 (Supabase 3-파일 클라이언트 원본): `/Users/alex/repos/weekly-wine-bot/admin/`
    ```
  </action>

  <acceptance_criteria>
    - `/Users/alex/repos/gh-radar/webapp/SETUP.md` exists.
    - File contains `Google Cloud Console`, `Supabase Dashboard`, `Vercel`.
    - File contains `redirect_uri_mismatch` warning (Pitfall 2).
    - File contains reference to `seed-test-user.ts`.
    - File contains `.env.local.example` usage instructions.
    - File contains `watchlists` table reference + `read_*` policy rename note.
    - File contains warning against `service_role key` in webapp env.
  </acceptance_criteria>

  <verify>
    <automated>test -f /Users/alex/repos/gh-radar/webapp/SETUP.md && grep -q "Google Cloud Console" /Users/alex/repos/gh-radar/webapp/SETUP.md && grep -q "redirect_uri_mismatch" /Users/alex/repos/gh-radar/webapp/SETUP.md && grep -q "service_role" /Users/alex/repos/gh-radar/webapp/SETUP.md && grep -q "seed-test-user" /Users/alex/repos/gh-radar/webapp/SETUP.md</automated>
  </verify>

  <done>
    SETUP.md 작성 완료, Pitfall 2 경고 + 전체 절차 커버.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: webapp/.env.local.example 작성 또는 갱신</name>

  <read_first>
    - /Users/alex/repos/gh-radar/webapp/.env.local.example (존재 시 현재 내용 — 없으면 신규 생성)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"Environment Availability"
  </read_first>

  <files>
    webapp/.env.local.example
  </files>

  <action>
    `webapp/.env.local.example` 작성 (또는 덮어쓰기):

    ```env
    # gh-radar webapp 로컬 개발 환경변수
    # 사용법: cp .env.local.example .env.local 후 값 채우기
    # .env.local 은 .gitignore 대상 — 절대 커밋 금지.

    # --- Supabase (Phase 06.2 Auth + Watchlist 필수) ---
    # Dashboard > Settings > API 에서 확인
    NEXT_PUBLIC_SUPABASE_URL=
    NEXT_PUBLIC_SUPABASE_ANON_KEY=

    # --- Express API (Phase 2 — Cloud Run) ---
    # 프로덕션 Cloud Run URL 또는 로컬 dev 서버
    NEXT_PUBLIC_API_URL=http://localhost:4000

    # --- E2E 테스트 (Plan 08 — Playwright storageState) ---
    # seed-test-user.ts 실행 후 사용
    E2E_TEST_EMAIL=e2e@gh-radar.local
    E2E_TEST_PASSWORD=
    PLAYWRIGHT_BASE_URL=http://localhost:3000

    # --- Admin (E2E 시딩 스크립트 전용, CI Secrets 에만) ---
    # webapp runtime 에는 절대 사용하지 말 것 — RLS 우회 위험
    # SUPABASE_URL=
    # SUPABASE_SERVICE_ROLE_KEY=
    ```

    기존 파일이 있으면 섹션을 merge — 기존 변수는 보존.
  </action>

  <acceptance_criteria>
    - `/Users/alex/repos/gh-radar/webapp/.env.local.example` exists.
    - File contains `NEXT_PUBLIC_SUPABASE_URL=` and `NEXT_PUBLIC_SUPABASE_ANON_KEY=`.
    - File contains `E2E_TEST_EMAIL=` and `E2E_TEST_PASSWORD=`.
    - File contains commented-out `SUPABASE_SERVICE_ROLE_KEY` with warning.
    - File does NOT contain any real secret values (empty after `=`).
  </acceptance_criteria>

  <verify>
    <automated>test -f /Users/alex/repos/gh-radar/webapp/.env.local.example && grep -q "NEXT_PUBLIC_SUPABASE_URL=" /Users/alex/repos/gh-radar/webapp/.env.local.example && grep -q "E2E_TEST_EMAIL=" /Users/alex/repos/gh-radar/webapp/.env.local.example && grep -q "SUPABASE_SERVICE_ROLE_KEY" /Users/alex/repos/gh-radar/webapp/.env.local.example && ! grep -E "^(NEXT_PUBLIC_SUPABASE_URL|E2E_TEST_PASSWORD)=[^ ]+$" /Users/alex/repos/gh-radar/webapp/.env.local.example</automated>
  </verify>

  <done>
    .env.local.example 작성 완료, 값 비어있음 확인, service_role 경고 주석 포함.
  </done>
</task>

<task type="checkpoint:human-action" gate="non-blocking">
  <name>Task 3: [MANUAL] 사용자가 SETUP.md 절차를 Supabase/Google Cloud 에 적용</name>

  <files>
    (수동 단계 — Supabase Dashboard, Google Cloud Console, Vercel 환경 설정. 코드/파일 변경 없음.)
  </files>

  <action>
    실행자는 SETUP.md 의 §1 §2 §3 §5 절차를 사용자에게 안내하고 사용자가 각 단계를 완료했는지 확인 (configured/deferred 결정).
    이 단계는 Plan 08 E2E 가 통과하기 위한 prerequisite 이지만 Plan 09 자체는 문서 작성 만으로 완료 가능 — 실제 설정은 Phase 종료 전 배포 시점에 사용자가 수행 가능.
  </action>

  <verify>
    <automated>test -f /Users/alex/repos/gh-radar/webapp/SETUP.md && echo "SETUP.md exists — 실제 Dashboard 설정은 사용자 수동 검증"</automated>
  </verify>

  <done>
    사용자가 "configured" (모든 §1-§5 완료) 또는 "deferred" (배포 시점에 적용) 응답.
  </done>

  <what-built>
    Task 1 의 SETUP.md 문서에 따라 사용자가 실제로:
    - Supabase Dashboard Google Provider 활성화
    - Google Cloud Console OAuth 2.0 Client 발급 + Supabase URL 등록
    - Vercel env 확인
    - 테스트 유저 시딩 (Plan 08 prerequisites)

    이 단계는 Plan 08 E2E 실행 전에 완료되어야 하지만, Plan 09 자체의 blocking 은 아님 (문서 작성 vs 실제 설정 분리).
  </what-built>

  <how-to-verify>
    1. Supabase Dashboard → Authentication → Providers → Google 이 활성화됨 (녹색 토글).
    2. Site URL + Redirect URLs 에 프로덕션 + 로컬 두 URL 모두 등록.
    3. Google Cloud Console → Credentials → OAuth 2.0 Client IDs 에 신규 클라이언트 노출 + redirect URI 가 Supabase URL (`<ref>.supabase.co/auth/v1/callback`) 만 등록.
    4. `vercel env ls` 출력에 `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` 존재.
    5. (선택) 로컬에서 `pnpm --filter @gh-radar/webapp dev` → `/login` → "Google로 로그인" 클릭 → Google consent → `/scanner` 도달 (1회 수동 smoke).
  </how-to-verify>

  <resume-signal>
    "configured" — 위 5 체크 통과 후.
    "deferred" — 현재는 문서만 작성, 실제 설정은 Phase 종료 전 배포 시점에 수행하는 경우. Plan 08 E2E 스킵 가능성 명시.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SETUP.md 문서 ↔ 사용자 수동 행동 | 문서가 service_role key 취급 원칙을 명확히 해야 사용자가 실수 없음 |
| .env.local.example ↔ .env.local | 예제에 실제 값이 있으면 유출 위험 — 빈 값 엄수 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-37 | Information Disclosure | .env.local.example 에 실제 secret 이 commit 됨 | mitigate | Task 2 acceptance_criteria 가 빈 값만 허용하는 grep 검증. 추가: pre-commit hook 또는 gitleaks 권고 (Phase 외 범위). ASVS V8.2. |
| T-06.2-38 | Information Disclosure | SETUP.md 에 잘못된 안내로 service_role key 가 webapp env 에 입력됨 | mitigate | Task 1 의 ⚠ 경고 문구 ("webapp 에 절대 설정하지 말 것") 2곳 명시 + §3 + §5 주석. ASVS V8.2.3. |
| T-06.2-39 | Tampering | 악의적 Redirect URL 이 Supabase Dashboard 에 등록됨 | accept | Supabase Dashboard 접근 자체가 관리자 권한 — 내부 위협. 문서가 정확한 URL 명시로 사고 가능성 감소. ASVS V4.1. |

</threat_model>

<verification>
- **Per-task:** Task 1 + Task 2 grep 검증. Task 3 수동 확인.
- **Wave-end:** 문서 기반이므로 build/test 영향 없음. Plan 08 E2E 가 실제 설정 동작을 간접 검증 (Google OAuth 자동 테스트는 불가하나 setup project 가 Supabase REST 로그인으로 검증).
</verification>

<success_criteria>
- webapp/SETUP.md 작성 완료 (7 섹션)
- webapp/.env.local.example 갱신 완료 (값 비어있음)
- Pitfall 2 경고 명시
- service_role key 취급 원칙 2곳 명시
- Task 3 (수동 설정) 은 deferred 가능 — Phase 종료 전 배포 시점에 수행
</success_criteria>

<output>
`.planning/phases/06.2-auth-watchlist/06.2-09-SUMMARY.md`:
- SETUP.md 섹션 목차
- .env.local.example 변경 diff (있으면)
- Task 3 수동 단계 진행 여부 (configured / deferred)
- 사용자에게 전달할 체크리스트 (Dashboard + Cloud Console + Vercel)
</output>
