---
phase: 06.2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260416120000_watchlists.sql
  - supabase/SCHEMA.md
autonomous: false
requirements: [PERS-01]
requirements_addressed:
  - id: PERS-01
    status: in_progress
    notes: "스키마 + RLS + trigger 배치 (라이브 DB 에 push 까지). UI/훅은 Plan 06/07."

must_haves:
  truths:
    - "public.watchlists 테이블이 라이브 Supabase 프로젝트에 존재한다 (PK user_id+stock_code, FK CASCADE 2건)"
    - "authenticated role 사용자는 본인 row 만 SELECT/INSERT/UPDATE/DELETE 할 수 있다 (RLS 4정책)"
    - "동일 사용자가 51번째 row 를 INSERT 하면 SQLSTATE P0001 'watchlist_limit_exceeded' 가 발생한다"
    - "stocks / stock_quotes 테이블의 기존 anon-only RLS 가 anon+authenticated 로 확장되어 로그인 사용자도 JOIN 으로 조회 가능하다"
    - "supabase db push 가 실행되어 위 스키마가 원격 프로젝트에 반영되어 있다"
  artifacts:
    - path: "supabase/migrations/20260416120000_watchlists.sql"
      provides: "watchlists 테이블 + FK + RLS 4정책 + BEFORE INSERT trigger + stocks/stock_quotes 정책 확장"
      contains: "CREATE TABLE watchlists, CREATE TRIGGER trg_enforce_watchlist_limit, DROP POLICY anon_read_stocks_master"
    - path: "supabase/SCHEMA.md"
      provides: "watchlists 스키마 문서화 (테이블/정책/trigger)"
      contains: "watchlists"
  key_links:
    - from: "watchlists.user_id"
      to: "auth.users(id)"
      via: "FK ON DELETE CASCADE"
      pattern: "REFERENCES auth\\.users\\(id\\) ON DELETE CASCADE"
    - from: "watchlists.stock_code"
      to: "stocks(code)"
      via: "FK ON DELETE CASCADE"
      pattern: "REFERENCES stocks\\(code\\) ON DELETE CASCADE"
    - from: "enforce_watchlist_limit()"
      to: "watchlists"
      via: "BEFORE INSERT trigger"
      pattern: "BEFORE INSERT ON watchlists"
---

<objective>
Watchlist 백엔드 기반 — `public.watchlists` 테이블 + FK + RLS 4정책 + 50-limit BEFORE INSERT trigger + 기존 `stocks`/`stock_quotes` 의 anon RLS 를 authenticated 로 확장하는 마이그레이션 작성 및 **[BLOCKING] supabase db push** 로 라이브 원격 DB 에 반영한다. Plan 06 Watchlist 페이지와 Plan 07 ⭐ Toggle 이 이 스키마를 직접 질의 (D-06: webapp → Supabase 직접 CRUD).

Purpose: D-18~D-22 전량 + RESEARCH Pattern 3/Pattern 9/Pitfall 3 해결. 50-limit 은 RLS subquery 대신 trigger 방식 채택 — 에러 코드 `P0001` 로 "limit 초과" 와 "타인 row 조작(42501)" 구분 가능.

Output: 1개 신규 마이그레이션 파일 + SCHEMA.md 갱신 + 원격 DB 반영. 이 plan 완료 이후 모든 watchlist 쿼리가 라이브로 작동.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06.2-auth-watchlist/06.2-CONTEXT.md
@.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md
@supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql
@supabase/SCHEMA.md

<interfaces>
<!-- 기존 RLS 패턴 — Phase 06.1 Step 8 에서 선언된 anon-only 정책들. -->
<!-- 이번 plan 에서 TO anon → TO anon, authenticated 로 확장. -->

From supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql Step 8:
```sql
CREATE POLICY "anon_read_stocks_master"
  ON stocks FOR SELECT TO anon USING (true);
CREATE POLICY "anon_read_stock_quotes"
  ON stock_quotes FOR SELECT TO anon USING (true);
```
→ Plan 02 에서 `DROP POLICY` 후 `TO anon, authenticated` 로 재생성. Pitfall 3 방지.

From auth.users (Supabase 내장):
- `id uuid` — JWT `sub` claim 과 매칭
- `email text` — Google OAuth 수신 시 user_metadata 에도 복제됨
- ON DELETE 는 Supabase 가 관리 — watchlists FK 는 CASCADE 로 본인 데이터 정리
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 20260416120000_watchlists.sql 마이그레이션 파일 작성</name>

  <read_first>
    - /Users/alex/repos/gh-radar/supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql (절대경로, Step 8 RLS 패턴 계승)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"Pattern 3" (RLS+trigger 조합)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"Pattern 9" (JOIN 시 authenticated 정책 필요)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-CONTEXT.md (D-18~D-22)
  </read_first>

  <files>
    supabase/migrations/20260416120000_watchlists.sql
  </files>

  <action>
    파일 시작은 단일 `BEGIN;` + 끝은 `COMMIT;` 으로 감싸고, 다음 섹션을 순차 작성.

    **Step 1 — watchlists 테이블 (D-18/D-19/D-21):**
    ```sql
    CREATE TABLE watchlists (
      user_id    uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
      stock_code text NOT NULL REFERENCES stocks(code)   ON DELETE CASCADE,
      added_at   timestamptz NOT NULL DEFAULT now(),
      position   int,
      PRIMARY KEY (user_id, stock_code)
    );
    CREATE INDEX idx_watchlists_user_added_at
      ON watchlists (user_id, added_at DESC);
    ```
    컬럼 4개 정확 일치 (추가 컬럼 금지). PK 복합키 명시.

    **Step 2 — RLS 활성화 + 4정책 (D-22):**
    ```sql
    ALTER TABLE watchlists ENABLE ROW LEVEL SECURITY;

    CREATE POLICY "auth_select_own_watchlists"
      ON watchlists FOR SELECT TO authenticated
      USING (auth.uid() = user_id);

    CREATE POLICY "auth_insert_own_watchlists"
      ON watchlists FOR INSERT TO authenticated
      WITH CHECK (auth.uid() = user_id);

    CREATE POLICY "auth_update_own_watchlists"
      ON watchlists FOR UPDATE TO authenticated
      USING (auth.uid() = user_id)
      WITH CHECK (auth.uid() = user_id);

    CREATE POLICY "auth_delete_own_watchlists"
      ON watchlists FOR DELETE TO authenticated
      USING (auth.uid() = user_id);
    ```
    정책 이름/액션/조건 정확 일치. 50-limit 은 trigger 담당이므로 `auth_insert_own_watchlists` 의 WITH CHECK 에 subquery 금지.

    **Step 3 — BEFORE INSERT trigger (D-20, Pattern 3):**
    ```sql
    CREATE OR REPLACE FUNCTION enforce_watchlist_limit()
    RETURNS TRIGGER AS $$
    DECLARE
      cnt int;
    BEGIN
      SELECT count(*) INTO cnt FROM watchlists WHERE user_id = NEW.user_id;
      IF cnt >= 50 THEN
        RAISE EXCEPTION 'watchlist_limit_exceeded'
          USING HINT = '관심종목은 최대 50개까지 저장할 수 있습니다.',
                ERRCODE = 'P0001';
      END IF;
      RETURN NEW;
    END;
    $$ LANGUAGE plpgsql;

    CREATE TRIGGER trg_enforce_watchlist_limit
      BEFORE INSERT ON watchlists
      FOR EACH ROW
      EXECUTE FUNCTION enforce_watchlist_limit();
    ```
    함수 이름 `enforce_watchlist_limit`, trigger 이름 `trg_enforce_watchlist_limit`, 메시지 `'watchlist_limit_exceeded'` 문자열 정확 일치 (Plan 07 에서 `error.code === 'P0001'` + `error.message === 'watchlist_limit_exceeded'` 매칭).

    **Step 4 — stocks / stock_quotes 기존 anon 정책을 authenticated 로 확장 (Pattern 9, Pitfall 3):**
    ```sql
    DROP POLICY "anon_read_stocks_master" ON stocks;
    CREATE POLICY "read_stocks_master"
      ON stocks FOR SELECT TO anon, authenticated USING (true);

    DROP POLICY "anon_read_stock_quotes" ON stock_quotes;
    CREATE POLICY "read_stock_quotes"
      ON stock_quotes FOR SELECT TO anon, authenticated USING (true);
    ```
    기존 정책 drop 후 이름 변경 (`anon_` prefix 제거) + role 확장. **주의**: `top_movers` 테이블의 `anon_read_top_movers` 는 이번 phase 에서 건드리지 않음 (Scanner 는 `/api/*` Express 경유라서 anon 만으로 충분).

    **Step 5 — 문서 주석:** 각 Step 위에 `-- Phase 06.2 Plan 02: 관심종목 스키마 + RLS + trigger (D-18~D-22, RESEARCH Pattern 3/9)` 형식 헤더. 50-limit 을 trigger 로 구현한 근거 2-3 줄 주석 (42501 vs P0001 구분 + infinite-recursion 회피).
  </action>

  <acceptance_criteria>
    - `/Users/alex/repos/gh-radar/supabase/migrations/20260416120000_watchlists.sql` exists.
    - File contains `CREATE TABLE watchlists` and `PRIMARY KEY (user_id, stock_code)`.
    - File contains `REFERENCES auth.users(id) ON DELETE CASCADE` and `REFERENCES stocks(code) ON DELETE CASCADE`.
    - File contains all 4 policies: `auth_select_own_watchlists`, `auth_insert_own_watchlists`, `auth_update_own_watchlists`, `auth_delete_own_watchlists`.
    - File contains `CREATE TRIGGER trg_enforce_watchlist_limit` and `BEFORE INSERT ON watchlists`.
    - File contains `RAISE EXCEPTION 'watchlist_limit_exceeded'` and `ERRCODE = 'P0001'`.
    - File contains `DROP POLICY "anon_read_stocks_master"` and `TO anon, authenticated`.
    - File starts with `BEGIN;` and ends with `COMMIT;`.
    - `psql --variable ON_ERROR_STOP=1 -f supabase/migrations/20260416120000_watchlists.sql --dry-run` **not available**; substitute: SQL parse check via `supabase db lint supabase/migrations/20260416120000_watchlists.sql` (if CLI supports) OR visual review.
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar && grep -q "CREATE TRIGGER trg_enforce_watchlist_limit" supabase/migrations/20260416120000_watchlists.sql && grep -q "ERRCODE = 'P0001'" supabase/migrations/20260416120000_watchlists.sql && grep -q "TO anon, authenticated" supabase/migrations/20260416120000_watchlists.sql && grep -c "CREATE POLICY" supabase/migrations/20260416120000_watchlists.sql | grep -E "^[6-9]|[0-9][0-9]+$"</automated>
  </verify>

  <done>
    마이그레이션 파일 1개 생성, RLS 4정책 + trigger + stocks/stock_quotes 정책 확장 포함, BEGIN/COMMIT 감싸짐, grep 검증 통과.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] supabase db push — 원격 Supabase 프로젝트에 마이그레이션 반영</name>

  <read_first>
    - /Users/alex/repos/gh-radar/supabase/migrations/20260416120000_watchlists.sql (Task 1 산출물)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"Runtime State Inventory"
    - /Users/alex/repos/gh-radar/.planning/phases/01-data-foundation/ (절대경로, 기존 db push 운영 기록)
  </read_first>

  <what-built>
    Task 1 에서 작성된 `20260416120000_watchlists.sql` 를 원격 Supabase 프로젝트 (`gh-radar`) 에 적용한다. 이 단계 없이는 RLS / trigger 가 라이브 DB 에 반영되지 않아 Plan 06/07 의 모든 Watchlist 동작이 "build 통과 → 런타임 401/RLS 거부" 의 false-positive 검증을 만든다.

    **autonomous=false 사유:** Supabase CLI 는 인터랙티브 prompt 로 project ref 확인 + DB 비밀번호 입력을 요구할 수 있음. Non-TTY 환경에서는 `SUPABASE_ACCESS_TOKEN` env 또는 Supabase Dashboard SQL Editor 수동 실행이 필요.
  </what-built>

  <files>
    (수동 단계 — supabase 원격 DB 에 마이그레이션 반영. 파일 변경 없음.)
  </files>

  <action>
    실행자는 사용자에게 아래 절차를 안내하고 사용자가 수행한 결과(applied/failed)를 확인할 때까지 대기.
    Task 1 의 마이그레이션 파일을 원격 Supabase 프로젝트에 적용하는 단계 (다음 §how-to-verify 참조).
    자동화 가능하면 (`SUPABASE_ACCESS_TOKEN` env 보유) 실행자가 직접 `supabase db push` 시도.
  </action>

  <verify>
    <automated>supabase db push --dry-run 2>&1 | tail -5 || echo "MANUAL — Dashboard 또는 supabase login 필요"</automated>
  </verify>

  <done>
    원격 Supabase 프로젝트에 watchlists 테이블 + 4 정책 + trigger + stocks/stock_quotes 정책 rename 반영 확인 (Dashboard 5체크).
  </done>

  <how-to-verify>
    **방법 A — CLI 경로 (권장, 자동화 가능):**
    1. `cd /Users/alex/repos/gh-radar`
    2. Supabase CLI 가 이미 project 에 link 되어 있는지 확인: `supabase projects list` + `supabase link --project-ref <ref>` (이미 link 되어 있으면 skip).
    3. 실행: `supabase db push`
       - 프롬프트가 뜨면 **"Apply this migration?"** 에 `y` 입력.
       - Non-TTY 환경이면 먼저 `export SUPABASE_ACCESS_TOKEN=<token>` 설정 후 재실행.
    4. 성공 메시지 `Applied migration: 20260416120000_watchlists.sql` 확인.

    **방법 B — Dashboard SQL Editor (CLI 실패 시 fallback):**
    1. Supabase Dashboard → Project `gh-radar` → SQL Editor.
    2. `20260416120000_watchlists.sql` 전체 내용을 복사 → 붙여넣기 → Run.
    3. 성공 응답 확인 (COMMIT).

    **검증 스모크 (방법 무관 공통):**
    ```bash
    # psql 직접 쿼리 (Supabase 연결 문자열 필요) OR supabase CLI 쿼리:
    supabase db dump --data-only -f /dev/null 2>&1 | grep -q "watchlists" && echo "SCHEMA PRESENT" || echo "MISSING"

    # 대안: Dashboard Table Editor 에서 `watchlists` 테이블 존재 확인
    # 대안: Dashboard Database → Policies → watchlists 에 4정책 노출 확인
    # 대안: Dashboard Database → Functions → enforce_watchlist_limit 함수 노출 확인
    ```

    **수동 스모크 (Dashboard):**
    - Table Editor → `watchlists` 테이블 보임 (rows 0)
    - Authentication → Policies → `auth_select_own_watchlists` / `auth_insert_own_watchlists` / `auth_update_own_watchlists` / `auth_delete_own_watchlists` 4개 노출
    - Database → Functions → `enforce_watchlist_limit` 노출
    - Table Editor → `stocks` 테이블 Policies → `read_stocks_master` (TO anon, authenticated) 노출 + `anon_read_stocks_master` 없음
    - Table Editor → `stock_quotes` 테이블 Policies → `read_stock_quotes` (TO anon, authenticated) 노출 + `anon_read_stock_quotes` 없음

    **추가 E2E 스모크 (Task 3 이전 권고, 선택):** psql 로 `SET ROLE authenticated; SELECT * FROM watchlists;` 실행 시 `permission denied` 또는 빈 row 반환 (정상 — `auth.uid()` 없이 authenticated role 만으로는 조건 미충족).
  </how-to-verify>

  <resume-signal>
    "applied" — 원격 DB 에 마이그레이션 반영 + 위 5개 Dashboard 검증 통과 후.
    "failed: {이유}" — 실패 시 원인 보고 후 Task 1 수정 → 재진행.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: supabase/SCHEMA.md 문서화 갱신</name>

  <read_first>
    - /Users/alex/repos/gh-radar/supabase/SCHEMA.md (현재 내용 전체)
    - /Users/alex/repos/gh-radar/supabase/migrations/20260416120000_watchlists.sql (Task 1 산출물)
  </read_first>

  <files>
    supabase/SCHEMA.md
  </files>

  <action>
    SCHEMA.md 에 다음 섹션 append (기존 내용 삭제/수정 금지):

    ```markdown
    ## watchlists (Phase 06.2)

    로그인 사용자의 관심종목 — 사용자당 최대 50개, Supabase Auth `auth.users` FK + `stocks` FK, RLS 로 본인 row 만 접근.

    | Column | Type | Description |
    |--------|------|-------------|
    | user_id | uuid NOT NULL | FK → auth.users(id) ON DELETE CASCADE |
    | stock_code | text NOT NULL | FK → stocks(code) ON DELETE CASCADE |
    | added_at | timestamptz NOT NULL DEFAULT now() | 추가 시각 (기본 정렬 키) |
    | position | int | v1 미사용 — 드래그 리오더 deferred |

    **PK:** (user_id, stock_code) — 중복 방지
    **Index:** idx_watchlists_user_added_at (user_id, added_at DESC)

    **RLS (authenticated role):**
    - SELECT/INSERT/UPDATE/DELETE 4정책 모두 `auth.uid() = user_id`
    - 50-limit 은 BEFORE INSERT trigger `trg_enforce_watchlist_limit` 로 강제 (P0001 SQLSTATE)

    **관련 변경 (Phase 06.2):** stocks / stock_quotes 테이블의 기존 `anon_read_*` 정책을 `read_*` 로 rename + `TO anon, authenticated` 로 확장 (로그인 사용자 JOIN 허용).
    ```

    이미 watchlists 섹션이 존재하면 이 task 는 skip.
  </action>

  <acceptance_criteria>
    - `/Users/alex/repos/gh-radar/supabase/SCHEMA.md` contains `## watchlists` heading.
    - File contains `trg_enforce_watchlist_limit` and `P0001`.
    - File contains `read_stocks_master` (rename 기록).
    - git diff 으로 SCHEMA.md 변경이 append-only 임을 확인 (기존 라인 삭제 0 건).
  </acceptance_criteria>

  <verify>
    <automated>grep -q "## watchlists" /Users/alex/repos/gh-radar/supabase/SCHEMA.md && grep -q "trg_enforce_watchlist_limit" /Users/alex/repos/gh-radar/supabase/SCHEMA.md</automated>
  </verify>

  <done>
    SCHEMA.md 에 watchlists 섹션 + stocks/stock_quotes 정책 rename 이 문서화됨.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webapp (anon JWT) ↔ Supabase PostgREST | RLS 정책이 본인 row 외 접근 차단 |
| anonymous (비로그인) ↔ Supabase PostgREST | watchlists 테이블 전체 차단 (authenticated role 전용) |
| Concurrent INSERT (동일 user) | 51번째 row 동시 INSERT 의 race window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-06 | Elevation of Privilege | 타인 watchlist 읽기/수정 (Horizontal privilege escalation) | mitigate | RLS 4정책 (SELECT/INSERT/UPDATE/DELETE) 모두 `auth.uid() = user_id` 조건. PostgreSQL 레벨 강제. authenticated role 만 적용 — anon 은 SELECT 도 불가. ASVS V4.1.1, V4.2.1. Verified by: Plan 08 `vitest run src/lib/__tests__/watchlist-api.test.ts::rls-isolation` (2명 테스트 유저). |
| T-06.2-07 | Tampering | 50-limit bypass via concurrent insert | mitigate | BEFORE INSERT trigger 가 row-level 원자성 보장 — 동일 트랜잭션 내 SELECT count(*) + RAISE EXCEPTION 조합으로 race window 없음. PostgreSQL MVCC 상 BEFORE INSERT 는 tuple lock 이후 실행. ASVS V4.2.2. Verified by: Plan 08 `playwright test ...50-limit` (storageState 로 50개 사전 주입 후 51번째). |
| T-06.2-08 | Tampering | Authenticated user 가 anon 권한으로 다른 row 조작 (horizontal with role confusion) | mitigate | 정책 확장 시 `TO anon, authenticated` 는 SELECT 만 해당 (watchlists 는 authenticated 전용). stocks/stock_quotes 는 공개 읽기로 분류 — 쓰기는 여전히 service_role 경유. ASVS V4.1.3. |
| T-06.2-09 | Information Disclosure | watchlists 테이블 존재 자체 노출 | accept | Schema 노출은 PostgREST 기본 동작 — RLS 로 row-level 차단되므로 저위험. 공격자가 테이블 존재를 알아도 row 를 얻을 수 없음. ASVS V8.1.6. |
| T-06.2-10 | Denial of Service | watchlists 테이블에 대규모 삽입 공격 | accept | 50-limit + authenticated role 필요 + Supabase 기본 rate limit. 1인 50 row × N user 은 무료 tier 충분. 추가 방어 미필요. |

</threat_model>

<verification>
- **Sampling rate:** Task 1 은 grep 검증 즉시. Task 2 는 Dashboard 수동 확인. Task 3 은 grep 즉시.
- **Wave-end suite:** Plan 02 는 독립적으로 완료 후 Plan 06/07 의 E2E 테스트에서 RLS/trigger 동작 검증.
- **Post-push smoke:** `supabase db remote diff` 또는 `supabase db dump --schema public` 로 원격 스키마에 `watchlists` + trigger 존재 재확인.
- **Manual check:** Supabase Dashboard 에서 5개 항목 (Table / 4 Policies / 1 Function) 시각 확인 — Task 2 how-to-verify 참조.
</verification>

<success_criteria>
- `supabase/migrations/20260416120000_watchlists.sql` 존재, grep 체크 5건 통과
- 원격 Supabase `gh-radar` 프로젝트에 `watchlists` 테이블 + 4 정책 + `enforce_watchlist_limit` 함수 + `trg_enforce_watchlist_limit` trigger 반영됨 (Dashboard 확인)
- `stocks`, `stock_quotes` 기존 `anon_read_*` 정책이 `read_*`(TO anon, authenticated) 로 교체됨
- `supabase/SCHEMA.md` 에 watchlists 섹션 append 됨
</success_criteria>

<output>
Plan 완료 후 `.planning/phases/06.2-auth-watchlist/06.2-02-SUMMARY.md` 작성:
- 마이그레이션 파일 경로 + timestamp
- supabase db push 실행 결과 (로그 or Dashboard 스크린샷 패스 기준)
- Plan 06/07 이 소비할 계약 — 테이블명 `watchlists`, 정책명 4종, trigger 이름, 에러 코드 `P0001` + 메시지 `watchlist_limit_exceeded`
- stocks/stock_quotes 정책 rename 전/후 이름 대조
</output>
