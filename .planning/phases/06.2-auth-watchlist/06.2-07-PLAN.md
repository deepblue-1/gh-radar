---
phase: 06.2
plan: 07
type: execute
wave: 4
depends_on: [06.2-01, 06.2-02, 06.2-05, 06.2-06]
files_modified:
  - webapp/src/app/layout.tsx
  - webapp/src/components/watchlist/watchlist-toggle.tsx
  - webapp/src/components/watchlist/__tests__/watchlist-toggle.test.tsx
  - webapp/src/hooks/use-watchlist-set.ts
  - webapp/src/components/stock/stock-hero.tsx
  - webapp/src/components/stock/info-stock-card.tsx
  - webapp/src/components/scanner/scanner-table.tsx
  - webapp/src/components/scanner/scanner-card-list.tsx
  - webapp/src/components/watchlist/watchlist-client.tsx
autonomous: true
requirements: [PERS-01]
requirements_addressed:
  - id: PERS-01
    status: complete
    notes: "⭐ 토글 통합 완료 — StockHero + Scanner (Table + Card) + Watchlist (Table + Card) 전부 동작. 50 limit P0001 처리, optimistic update + rollback."

must_haves:
  truths:
    - "WatchlistToggle 컴포넌트가 Ghost variant 로 pressed 시 배경 없이 색만 변화 (fill-[var(--primary)] + text-[var(--primary)])"
    - "/stocks/[code] StockHero 제목행에 ⭐ Toggle 이 노출되어 로그인 사용자가 토글 가능"
    - "Scanner Table 행의 맨 끝 셀에 ⭐ Toggle 노출 (Link 를 셀 단위로 축소하여 중첩 방지)"
    - "Scanner InfoStockCard 의 watchlistToggleSlot 에 ⭐ Toggle 주입 — stopPropagation 으로 행 클릭과 격리"
    - "Watchlist Table renderToggle + InfoStockCard watchlistToggleSlot 에 ⭐ Toggle 주입 — 기본 set 상태"
    - "50개 도달한 사용자가 51번째 unset 종목 ⭐ 클릭 시 disabled + title 툴팁 (UI-SPEC §5)"
    - "51번째 insert 시 P0001 SQLSTATE + 'watchlist_limit_exceeded' 메시지 감지 → 한글 에러 노출 + 롤백"
    - "네트워크 실패 시 optimistic 상태 롤백 + 2초 자동 소거 inline 에러"
  artifacts:
    - path: "webapp/src/hooks/use-watchlist-set.ts"
      provides: "전역 watchlist 멤버십 Set 조회 훅 — 어느 stock_code 가 관심종목에 있는지 O(1) 확인"
      exports: ["useWatchlistSet", "WatchlistSetContext", "WatchlistSetProvider"]
    - path: "webapp/src/components/watchlist/watchlist-toggle.tsx"
      provides: "⭐ Toggle (Ghost variant) — optimistic + P0001 매칭 + 50 limit disabled + inline 에러"
      exports: ["WatchlistToggle"]
  key_links:
    - from: "StockHero"
      to: "WatchlistToggle"
      via: "제목행 Badge 다음"
      pattern: "<WatchlistToggle"
    - from: "Scanner Table 행"
      to: "WatchlistToggle (마지막 셀)"
      via: "Link 셀 단위 축소 후 맨 끝"
      pattern: "<WatchlistToggle"
    - from: "WatchlistToggle"
      to: "supabase.from('watchlists').insert/delete"
      via: "optimistic update + error.code === 'P0001' 분기"
      pattern: "error\\.code === ['\"]P0001['\"]"
---

<objective>
⭐ 관심종목 Toggle 을 Ghost variant 로 구현하고 StockHero, Scanner Table, Scanner InfoStockCard, Watchlist Table, Watchlist InfoStockCard 5개 위치에 통합한다. 50 limit 도달 시 disabled, 실패 시 optimistic rollback + inline 에러, P0001 에러 코드 매칭으로 50 limit 초과 메시지 분기.

Purpose: D-26 완전 충족 + UI-SPEC §5 정확 구현. Phase 06.2 PERS-01 requirement 의 최종 완성 (Plan 06 읽기 + 본 Plan 쓰기/토글).

Output: 8개 파일 (신규 3 + 수정 5). Scanner Table 의 `<Link>` 구조를 셀 단위로 축소 (Pattern 8) 하여 중첩 클릭 방지.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.2-auth-watchlist/06.2-CONTEXT.md
@.planning/phases/06.2-auth-watchlist/06.2-UI-SPEC.md
@.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md
@.planning/phases/06.2-auth-watchlist/06.2-05-SUMMARY.md
@.planning/phases/06.2-auth-watchlist/06.2-06-SUMMARY.md
@webapp/src/components/ui/toggle.tsx
@webapp/src/components/stock/stock-hero.tsx
@webapp/src/components/scanner/scanner-table.tsx
@webapp/src/components/scanner/scanner-card-list.tsx

<interfaces>
<!-- 필요한 계약: Plan 01 (useAuth) + Plan 02 (P0001 에러 코드) + Plan 05 (InfoStockCard slot) + Plan 06 (WatchlistTable renderToggle, WatchlistRow, watchlist-api) -->

From webapp/src/lib/watchlist-api.ts (Plan 06):
```typescript
export async function addWatchlistItem(supabase, userId, stockCode): Promise<{ error: PostgrestError | null }>;
export async function removeWatchlistItem(supabase, userId, stockCode): Promise<{ error: PostgrestError | null }>;
```

From webapp/src/lib/auth-context.tsx (Plan 01):
```typescript
export function useAuth(): { user: User | null, ... };
```

From webapp/src/components/ui/toggle.tsx (shadcn 기존):
- `<Toggle pressed={boolean} onPressedChange={(p) => void} disabled aria-label aria-busy>` 인터페이스
- Radix Toggle 이 `aria-pressed` 자동 관리

From Plan 02 에러 코드:
- SQLSTATE `P0001` + message `'watchlist_limit_exceeded'` → 50 limit 초과
- SQLSTATE `23505` → unique_violation (이미 추가됨 — set 상태로 간주, 에러 아님)
- 기타 → "관심종목 변경에 실패했습니다." 롤백 + 2초 자동 소거

From UI-SPEC §5 (Ghost variant):
- size="default" 36×36 + data-[state=on]:bg-transparent + hover:bg-transparent
- Unset: `<Star className="size-4" strokeWidth={1.5}/>` + 부모 `text-[var(--muted-fg)]/70 hover:text-[var(--primary)]`
- Set: `<Star className="size-4 fill-[var(--primary)]" />` + 부모 `text-[var(--primary)]`
- Loading (optimistic 중): `aria-busy="true"` + `animate-pulse`
- Disabled (50 limit 도달 + unset): `disabled` + `title="관심종목은 최대 50개까지 저장할 수 있습니다"` + `opacity-40`
- aria-label: set 시 `{종목명} 관심종목 해제`, unset 시 `{종목명} 관심종목 추가`

From RESEARCH §예시 2 (Optimistic pattern):
- `useOptimistic` (React 19) 또는 useState + setter 어느 쪽이든 가능 — **실행자 재량**
- 50 limit 사전 감지 (상위에서 count 전달) → disabled + tooltip
- 51번째 insert 시도 시 server rollback + inline 에러
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: useWatchlistSet — 전역 멤버십 Set 조회 훅 + Provider</name>

  <read_first>
    - /Users/alex/repos/gh-radar/webapp/src/lib/watchlist-api.ts (Plan 06 Task 1 — fetchWatchlist 재사용)
    - /Users/alex/repos/gh-radar/webapp/src/lib/auth-context.tsx (Plan 01 Task 3 — useAuth 참조)
    - /Users/alex/repos/gh-radar/webapp/src/app/layout.tsx (AuthProvider 배선 위치)
  </read_first>

  <files>
    webapp/src/hooks/use-watchlist-set.ts,
    webapp/src/app/layout.tsx
  </files>

  <action>
    **목적:** StockHero / Scanner Table / Scanner Card / Watchlist Table 에서 각 stock 이 관심종목에 들어있는지 O(1) 조회. Scanner 행마다 개별 쿼리는 낭비 — 전역 Provider 가 한 번 fetch 후 Set 공유.

    `webapp/src/hooks/use-watchlist-set.ts` 작성:
    ```typescript
    "use client";
    import { createContext, useCallback, useContext, useEffect, useState, type ReactNode } from "react";
    import { createClient } from "@/lib/supabase/client";
    import { useAuth } from "@/lib/auth-context";

    interface WatchlistSetValue {
      set: Set<string>;
      count: number;
      isAtLimit: boolean;
      // optimistic 업데이트 API
      optimisticAdd: (code: string) => void;
      optimisticRemove: (code: string) => void;
      // 서버 재동기화 (성공 후)
      refresh: () => Promise<void>;
    }

    const WatchlistSetContext = createContext<WatchlistSetValue | null>(null);

    const WATCHLIST_LIMIT = 50;

    export function WatchlistSetProvider({ children }: { children: ReactNode }) {
      const { user } = useAuth();
      const [set, setSet] = useState<Set<string>>(new Set());

      const refresh = useCallback(async () => {
        if (!user) { setSet(new Set()); return; }
        const supabase = createClient();
        const { data } = await supabase.from("watchlists").select("stock_code").eq("user_id", user.id);
        if (data) setSet(new Set(data.map((r: { stock_code: string }) => r.stock_code)));
      }, [user]);

      useEffect(() => { refresh(); }, [refresh]);

      const optimisticAdd = useCallback((code: string) => {
        setSet((prev) => { const next = new Set(prev); next.add(code); return next; });
      }, []);
      const optimisticRemove = useCallback((code: string) => {
        setSet((prev) => { const next = new Set(prev); next.delete(code); return next; });
      }, []);

      const value: WatchlistSetValue = {
        set,
        count: set.size,
        isAtLimit: set.size >= WATCHLIST_LIMIT,
        optimisticAdd,
        optimisticRemove,
        refresh,
      };
      return <WatchlistSetContext.Provider value={value}>{children}</WatchlistSetContext.Provider>;
    }

    export function useWatchlistSet(): WatchlistSetValue {
      const ctx = useContext(WatchlistSetContext);
      if (!ctx) throw new Error("useWatchlistSet must be used within WatchlistSetProvider");
      return ctx;
    }

    export { WatchlistSetContext };
    ```

    **`webapp/src/app/layout.tsx` 수정:** `<AuthProvider>` 내부에 `<WatchlistSetProvider>` 래핑 추가:
    ```tsx
    <AuthProvider>
      <WatchlistSetProvider>
        {children}
      </WatchlistSetProvider>
    </AuthProvider>
    ```
    (layout.tsx 수정은 이 Plan frontmatter `files_modified` 에 명시되어 있음. ThemeProvider > AuthProvider > WatchlistSetProvider 중첩 순서 반드시 유지 — 역순 배선은 `useWatchlistSet` 이 `useAuth` 를 요구하므로 런타임 에러.)
  </action>

  <acceptance_criteria>
    - `/Users/alex/repos/gh-radar/webapp/src/hooks/use-watchlist-set.ts` exists.
    - File exports `useWatchlistSet`, `WatchlistSetProvider`, `WatchlistSetContext`.
    - File contains `WATCHLIST_LIMIT = 50`.
    - File contains `isAtLimit` derivation and `optimisticAdd` + `optimisticRemove`.
    - File uses `useAuth()` for user.id filter.
    - `/Users/alex/repos/gh-radar/webapp/src/app/layout.tsx` contains `<WatchlistSetProvider>` (wrapping children inside AuthProvider).
    - Provider 중첩 순서 검증: `grep -A3 '<AuthProvider>' webapp/src/app/layout.tsx | grep -q 'WatchlistSetProvider'` exits 0 (ThemeProvider > AuthProvider > WatchlistSetProvider 순 확인).
    - `pnpm --filter @gh-radar/webapp exec tsc --noEmit` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && pnpm exec tsc --noEmit && grep -q 'WatchlistSetProvider' src/hooks/use-watchlist-set.ts && grep -q 'WATCHLIST_LIMIT = 50' src/hooks/use-watchlist-set.ts && grep -q 'WatchlistSetProvider' src/app/layout.tsx && grep -A3 '<AuthProvider>' src/app/layout.tsx | grep -q 'WatchlistSetProvider'</automated>
  </verify>

  <done>
    useWatchlistSet 훅 + Provider 생성, layout.tsx 에 배선, optimistic API 노출.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WatchlistToggle (Ghost variant) — optimistic + P0001 + 50 limit + unit test</name>

  <read_first>
    - /Users/alex/repos/gh-radar/webapp/src/components/ui/toggle.tsx (shadcn 기존 — Toggle Primitive)
    - /Users/alex/repos/gh-radar/webapp/src/lib/watchlist-api.ts (Plan 06)
    - /Users/alex/repos/gh-radar/webapp/src/hooks/use-watchlist-set.ts (Task 1 산출물)
    - /Users/alex/repos/gh-radar/webapp/src/lib/auth-context.tsx (Plan 01 산출물 — `useAuth()` 반환: `{ user, displayName, isLoading, signOut }`. 본 Task 는 `user.id` 만 사용.)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-UI-SPEC.md §"5. ⭐ Toggle 시각 규칙"
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"예시 2" (optimistic pattern)
  </read_first>

  <behavior>
    - Test 1: initial pressed=false + 클릭 → pressed=true (optimistic). insert 호출.
    - Test 2: insert 실패 (P0001) → pressed=false (rollback) + inline 에러 "관심종목은 최대 50개까지 저장할 수 있습니다."
    - Test 3: insert 실패 (기타 에러) → rollback + "관심종목 변경에 실패했습니다."
    - Test 4: insert 23505 (unique_violation) → pressed=true 유지 (silent — 이미 있음).
    - Test 5: isAtLimit=true + pressed=false → disabled + title 툴팁 포함.
    - Test 6: pressed=true → aria-label "{name} 관심종목 해제"; pressed=false → "{name} 관심종목 추가".
    - Test 7: Ghost variant — className 에 `data-[state=on]:bg-transparent` 포함.
  </behavior>

  <files>
    webapp/src/components/watchlist/watchlist-toggle.tsx,
    webapp/src/components/watchlist/__tests__/watchlist-toggle.test.tsx
  </files>

  <action>
    1. 테스트 파일 먼저 작성 (RED). `vi.mock("@/lib/supabase/client")` 로 insert/delete 반환값 제어. 필요한 mock:
       ```typescript
       vi.mock("@/lib/supabase/client", () => ({
         createClient: () => ({
           from: () => ({
             insert: vi.fn().mockResolvedValue({ error: { code: "P0001", message: "watchlist_limit_exceeded" } }),
             delete: () => ({ eq: () => ({ eq: () => vi.fn().mockResolvedValue({ error: null }) }) }),
           }),
         }),
       }));
       ```
       useAuth mock: `vi.mock("@/lib/auth-context", () => ({ useAuth: () => ({ user: { id: "u1" } }) }))`.
       useWatchlistSet mock: `vi.mock("@/hooks/use-watchlist-set", () => ({ useWatchlistSet: () => ({ isAtLimit: false, optimisticAdd: vi.fn(), optimisticRemove: vi.fn(), refresh: vi.fn() }) }))`.
       각 테스트마다 mock 재구성.

    2. `webapp/src/components/watchlist/watchlist-toggle.tsx` 구현:
       ```typescript
       "use client";
       import { Star } from "lucide-react";
       import { useState, useTransition, useEffect } from "react";
       import { Toggle } from "@/components/ui/toggle";
       import { createClient } from "@/lib/supabase/client";
       import { useAuth } from "@/lib/auth-context";
       import { useWatchlistSet } from "@/hooks/use-watchlist-set";
       import { cn } from "@/lib/utils";

       export interface WatchlistToggleProps {
         stockCode: string;
         stockName: string;
         initialPressed?: boolean;
         size?: "default" | "sm";
       }

       const LIMIT_MESSAGE = "관심종목은 최대 50개까지 저장할 수 있습니다.";
       const FAIL_MESSAGE = "관심종목 변경에 실패했습니다.";

       export function WatchlistToggle({ stockCode, stockName, initialPressed, size = "default" }: WatchlistToggleProps) {
         const { user } = useAuth();
         const ws = useWatchlistSet();
         const [localPressed, setLocalPressed] = useState<boolean | null>(null);  // null = Set 기반
         const [isPending, startTransition] = useTransition();
         const [error, setError] = useState<string | null>(null);

         const pressedFromSet = ws.set.has(stockCode);
         const pressed = localPressed ?? (initialPressed ?? pressedFromSet);

         // 2초 후 에러 자동 소거
         useEffect(() => {
           if (!error) return;
           const t = setTimeout(() => setError(null), 2000);
           return () => clearTimeout(t);
         }, [error]);

         if (!user) return null;

         const isLimitDisabled = !pressed && ws.isAtLimit;

         const handleToggle = (next: boolean) => {
           setLocalPressed(next);
           setError(null);
           startTransition(async () => {
             const supabase = createClient();
             if (next) {
               ws.optimisticAdd(stockCode);
               const { error: err } = await supabase.from("watchlists").insert({ user_id: user.id, stock_code: stockCode });
               if (err) {
                 if (err.code === "23505") {
                   // 이미 있음 — set 상태 유지, 에러 없음
                 } else if (err.code === "P0001" || (err.message && err.message.includes("watchlist_limit_exceeded"))) {
                   ws.optimisticRemove(stockCode);
                   setLocalPressed(false);
                   setError(LIMIT_MESSAGE);
                 } else {
                   ws.optimisticRemove(stockCode);
                   setLocalPressed(false);
                   setError(FAIL_MESSAGE);
                 }
               } else {
                 ws.refresh();
               }
             } else {
               ws.optimisticRemove(stockCode);
               const { error: err } = await supabase.from("watchlists").delete().eq("user_id", user.id).eq("stock_code", stockCode);
               if (err) {
                 ws.optimisticAdd(stockCode);
                 setLocalPressed(true);
                 setError(FAIL_MESSAGE);
               } else {
                 ws.refresh();
               }
             }
           });
         };

         return (
           <span className="inline-flex flex-col items-end gap-0.5" onClick={(e) => e.stopPropagation()}>
             <Toggle
               pressed={pressed}
               onPressedChange={handleToggle}
               disabled={isPending || isLimitDisabled}
               aria-label={pressed ? `${stockName} 관심종목 해제` : `${stockName} 관심종목 추가`}
               aria-busy={isPending || undefined}
               size={size}
               className={cn(
                 // Ghost variant: pressed 시 배경 없음
                 "data-[state=on]:bg-transparent hover:bg-transparent",
                 size === "default" && "size-9",
                 pressed
                   ? "text-[var(--primary)]"
                   : "text-[var(--muted-fg)]/70 hover:text-[var(--primary)]",
                 isPending && "animate-pulse",
                 isLimitDisabled && "opacity-40"
               )}
               title={isLimitDisabled ? LIMIT_MESSAGE : undefined}
             >
               <Star
                 className={cn("size-4", pressed && "fill-[var(--primary)]")}
                 strokeWidth={1.5}
                 aria-hidden="true"
               />
             </Toggle>
             {error && (
               <span role="alert" className="text-[11px] text-[var(--destructive)]">{error}</span>
             )}
           </span>
         );
       }
       ```

    3. 테스트 GREEN 확인.
  </action>

  <acceptance_criteria>
    - `/Users/alex/repos/gh-radar/webapp/src/components/watchlist/watchlist-toggle.tsx` exists and exports `WatchlistToggle`, `WatchlistToggleProps`.
    - File contains `error.code === "P0001"` OR `err.code === "P0001"` (exact match).
    - File contains `"23505"` (unique_violation 경로).
    - File contains `"관심종목은 최대 50개까지 저장할 수 있습니다."` (한글 고정 문구).
    - File contains `"관심종목 변경에 실패했습니다."`.
    - File contains `data-[state=on]:bg-transparent` (Ghost variant 마커).
    - File contains `fill-[var(--primary)]` (pressed fill).
    - File contains `aria-label={pressed ? ` and ` 관심종목 해제` and ` 관심종목 추가`.
    - File contains `stopPropagation()` (행 클릭 격리).
    - `/Users/alex/repos/gh-radar/webapp/src/components/watchlist/__tests__/watchlist-toggle.test.tsx` exists with ≥7 test cases.
    - `pnpm --filter @gh-radar/webapp test -- watchlist-toggle` exits 0 with all passing.
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && pnpm test -- watchlist-toggle 2>&1 | tail -30</automated>
  </verify>

  <done>
    WatchlistToggle Ghost variant 구현, P0001/23505/기타 에러 분기, optimistic rollback, 50 limit disabled, 7 TDD 테스트 통과.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ⭐ 토글 5개 위치 통합 — StockHero + Scanner Table/Card + Watchlist Table/Card</name>

  <read_first>
    - /Users/alex/repos/gh-radar/webapp/src/components/stock/stock-hero.tsx (⭐ 삽입 지점)
    - /Users/alex/repos/gh-radar/webapp/src/components/scanner/scanner-table.tsx (Link 축소 + ⭐ 컬럼 추가)
    - /Users/alex/repos/gh-radar/webapp/src/components/scanner/scanner-card-list.tsx (showWatchlistToggle + slot 주입)
    - /Users/alex/repos/gh-radar/webapp/src/components/stock/info-stock-card.tsx (watchlistToggleSlot 확장)
    - /Users/alex/repos/gh-radar/webapp/src/components/watchlist/watchlist-client.tsx (Plan 06)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-RESEARCH.md §"Pattern 8" (Link 셀 단위 축소)
    - /Users/alex/repos/gh-radar/.planning/phases/06.2-auth-watchlist/06.2-UI-SPEC.md §"5. 배치" (D-26)
    - /Users/alex/repos/gh-radar/webapp/src/components/watchlist/watchlist-toggle.tsx (Task 2 산출물)
  </read_first>

  <files>
    webapp/src/components/stock/stock-hero.tsx,
    webapp/src/components/scanner/scanner-table.tsx,
    webapp/src/components/scanner/scanner-card-list.tsx,
    webapp/src/components/stock/info-stock-card.tsx,
    webapp/src/components/watchlist/watchlist-client.tsx
  </files>

  <action>
    **3.1 — StockHero (`webapp/src/components/stock/stock-hero.tsx`):**
    제목행의 `<Badge variant="outline">{stock.market}</Badge>` 다음 줄에 추가:
    ```tsx
    <WatchlistToggle stockCode={stock.code} stockName={stock.name} />
    ```
    `import { WatchlistToggle } from "@/components/watchlist/watchlist-toggle";` 추가. 기존 레이아웃/데이터 변경 금지.

    **3.2 — Scanner Table (`webapp/src/components/scanner/scanner-table.tsx`):**
    Pattern 8 (Link 셀 단위 축소) 적용:
    1. 현재 `<Link role="row">` 전체 wrap 을 제거.
    2. 외부 `<div role="row">` 로 대체 (기존 grid 유지).
    3. 종목명/코드/마켓/현재가/등락률/거래대금 6개 셀만 개별 `<Link href=/stocks/[code]>` 로 감싸기 (또는 각 셀 내부 값만 Link — tighter). 아래 구조 권고:
       ```tsx
       <div role="row" className={cn(GRID_COLS_7, "...")}>
         <Link href={`/stocks/${stock.code}`} className="truncate ...">{stock.name}</Link>
         <Link href={`/stocks/${stock.code}`} className="mono ...">{stock.code}</Link>
         <Link href={`/stocks/${stock.code}`}><Badge>{stock.market}</Badge></Link>
         <Link href={`/stocks/${stock.code}`} className="mono text-right">...</Link>
         ...
         <WatchlistToggle stockCode={stock.code} stockName={stock.name} />
       </div>
       ```
       또는 cell 단위 `<Link>` 대신 **전체 행을 radix-div 로 하고 마지막 셀 제외 `<Link>` 1개** — 실행자 재량. 핵심은 ⭐ Toggle 이 `<a>` 내부에 중첩되지 않아야 함.
    4. `GRID_COLS` 상수를 7컬럼으로 업데이트: `"grid grid-cols-[1fr_100px_80px_120px_100px_140px_44px] items-center gap-3 px-3"`.
    5. 헤더 row 에도 `⭐` 컬럼 추가 (`<span className="text-right" aria-label="관심종목">⭐</span>`).

    **3.3 — Scanner Card (`webapp/src/components/scanner/scanner-card-list.tsx`):**
    InfoStockCard 호출에 `showWatchlistToggle` + `watchlistToggleSlot` 주입:
    ```tsx
    <InfoStockCard
      stock={stock}
      showWatchlistToggle
      watchlistToggleSlot={<WatchlistToggle stockCode={stock.code} stockName={stock.name} />}
    />
    ```
    `import { WatchlistToggle } from "@/components/watchlist/watchlist-toggle";` 추가.

    **3.4 — InfoStockCard (`webapp/src/components/stock/info-stock-card.tsx`):**
    Plan 05 에서 이미 `watchlistToggleSlot` prop 구조가 구현되어 있음. Task 는 검증만: `{watchlistToggleSlot ?? <div className="size-9" aria-hidden="true" />}` 로직 확인. 변경 없으면 skip (단, Plan 05 가 onClick stopPropagation 을 외부 Link 에 배치했는지 확인 — 만약 Link 가 ⭐ 를 감싸고 있다면 WatchlistToggle 내부 stopPropagation (Task 2) 으로 격리됨).

    **3.5 — WatchlistClient (`webapp/src/components/watchlist/watchlist-client.tsx`):**
    Plan 06 에서 렌더된 Table + InfoStockCard 에 Toggle 주입:
    1. `<WatchlistTable rows={data} isRefreshing={isRefreshing} renderToggle={(row) => <WatchlistToggle stockCode={row.stockCode} stockName={row.stock.name} />} />`
    2. `<InfoStockCard stock={rowToStock(row)} showWatchlistToggle watchlistToggleSlot={<WatchlistToggle stockCode={row.stockCode} stockName={row.stock.name} />} />`
    `import { WatchlistToggle } from "./watchlist-toggle";` 추가.
  </action>

  <acceptance_criteria>
    - `/Users/alex/repos/gh-radar/webapp/src/components/stock/stock-hero.tsx` contains `<WatchlistToggle`.
    - `/Users/alex/repos/gh-radar/webapp/src/components/scanner/scanner-table.tsx` contains `<WatchlistToggle` and `grid-cols-[1fr_100px_80px_120px_100px_140px_44px]` (7 columns).
    - `/Users/alex/repos/gh-radar/webapp/src/components/scanner/scanner-table.tsx` does NOT contain single `<Link role="row">` wrapping entire row (Pattern 8 applied). Grep check: `<Link` occurrences ≥ 2 (cell-level 분할).
    - `/Users/alex/repos/gh-radar/webapp/src/components/scanner/scanner-card-list.tsx` contains `showWatchlistToggle` and `watchlistToggleSlot={<WatchlistToggle`.
    - `/Users/alex/repos/gh-radar/webapp/src/components/watchlist/watchlist-client.tsx` contains `renderToggle={` and `watchlistToggleSlot={<WatchlistToggle`.
    - `pnpm --filter @gh-radar/webapp exec tsc --noEmit` exits 0.
    - `pnpm --filter @gh-radar/webapp test` exits 0 (기존 unit test 회귀 없음).
    - `pnpm --filter @gh-radar/webapp build` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && pnpm exec tsc --noEmit && grep -q '<WatchlistToggle' src/components/stock/stock-hero.tsx && grep -q 'grid-cols-\[1fr_100px_80px_120px_100px_140px_44px\]' src/components/scanner/scanner-table.tsx && grep -q 'watchlistToggleSlot={<WatchlistToggle' src/components/scanner/scanner-card-list.tsx && grep -q 'renderToggle={' src/components/watchlist/watchlist-client.tsx && pnpm test 2>&1 | tail -10 && pnpm build 2>&1 | tail -5</automated>
  </verify>

  <done>
    ⭐ Toggle 이 StockHero / Scanner Table / Scanner Card / Watchlist Table / Watchlist Card 5개 위치 모두 작동, Scanner Table Link 축소 적용, build + test 통과.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI Toggle click → Supabase insert/delete | RLS + trigger 가 DB 레벨 최종 방어 |
| error.code 매칭 로직 | 클라이언트 JS 에서 에러 분기 — DB 에러 코드가 실제로 `P0001` 인지 의존 |
| optimistic set update | 실패 시 rollback 해야 UI-state ≠ DB-state 불일치 방지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-28 | Tampering | 50 limit bypass via concurrent UI clicks | mitigate | Plan 02 `BEFORE INSERT trigger` 가 DB 레벨 원자성 — UI 에서 동시 클릭해도 51번째는 P0001 반환. 클라이언트 `useTransition` + optimistic rollback 으로 UI 일관성 복구. ASVS V4.2.2. Verified by: Plan 08 `playwright watchlist.spec.ts::50-limit`. |
| T-06.2-29 | Elevation of Privilege | 타인 user_id 를 insert payload 에 지정 | mitigate | Plan 02 RLS `WITH CHECK (auth.uid() = user_id)` — 위조 user_id 는 DB 에서 42501 반환. 클라이언트는 `user.id` (세션 JWT sub) 만 사용. ASVS V4.1.1. |
| T-06.2-30 | Tampering | XSS via stockName in aria-label 템플릿 문자열 | mitigate | React 자동 escape — 템플릿 리터럴도 문자열로 취급 + DOM attribute 로 설정 시 browser 가 escape. ASVS V5.3.3. Verified by: Task 2 grep + unit test. |
| T-06.2-31 | Denial of Service | 연속 토글 클릭으로 Supabase 요청 폭주 | mitigate | `useTransition` 으로 동시 요청 1개. `disabled={isPending}` 으로 UI 블로킹. Supabase 기본 rate limit 보조. ASVS V11.1. |
| T-06.2-32 | Information Disclosure | error.message 그대로 UI 노출 시 DB 내부 구조 유출 | mitigate | UI 표시 에러는 한글 고정 문구 2종 (LIMIT_MESSAGE, FAIL_MESSAGE) — 원본 error.message 노출 없음. ASVS V7.4.1. Verified by: Task 2 acceptance + unit test. |

</threat_model>

<verification>
- **Per-task:** Task 2 TDD (≥7 tests) + tsc + grep.
- **Wave-end:** `pnpm --filter @gh-radar/webapp test` + `build` 통과. Scanner/Watchlist breakpoint 회귀 없음.
- **Manual smoke (Plan 08 E2E 이전 권고):** `pnpm dev` → 로그인 → /scanner → ⭐ 클릭 → /watchlist 이동 → 종목 표시 확인 → ⭐ 해제 → 사라짐.
- **E2E (Plan 08):** `e2e/specs/watchlist.spec.ts::toggle-roundtrip`, `::50-limit`, `::rollback-on-error`.
</verification>

<success_criteria>
- WatchlistToggle Ghost variant 컴포넌트 + 7 unit test 통과
- useWatchlistSet 전역 멤버십 훅 + Provider
- ⭐ 5개 위치 통합 (StockHero, Scanner Table/Card, Watchlist Table/Card)
- Scanner Table Link 축소 (Pattern 8 적용)
- 50 limit disabled + tooltip + P0001 매칭
- optimistic add/remove + rollback + 2초 inline 에러 자동 소거
- build + test 전체 통과
</success_criteria>

<output>
`.planning/phases/06.2-auth-watchlist/06.2-07-SUMMARY.md`:
- 8 파일 경로 요약
- ⭐ 토글 상태 머신 도식 (unset → optimistic set → DB insert → P0001/23505/success)
- Scanner Table Link 축소 전/후 코드 발췌
- 5개 삽입 지점 시각 목록
- PERS-01 requirement 완전 충족 증거
</output>
