# Phase 06.1 Plan 02 -- Migration Verification

**Applied:** 2026-04-16 11:00 KST
**Migration:** `supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql`
**Method:** `supabase db push` exit code 0

```
Applying migration 20260415120000_split_stocks_master_quotes_movers.sql...
NOTICE (00000): index "idx_top_movers_change_rate_desc" does not exist, skipping
Finished supabase db push.
```

Note: `DROP INDEX IF EXISTS idx_top_movers_change_rate_desc` NOTICE -- 해당 인덱스는 Step 3 에서 시세 컬럼 제거 시 이미 무효화되어 존재하지 않았음. `IF EXISTS` 가드로 안전 처리됨.

## Schema Verification

### stocks (마스터)

| Column | Type | Nullable | Default |
|--------|------|----------|---------|
| code | text | NO | - |
| name | text | NO | - |
| market | text | NO | - |
| sector | text | YES | - |
| kosdaq_segment | text | YES | - |
| security_type | text | NO | '보통주'::text |
| security_group | text | NO | '주권'::text |
| english_name | text | YES | - |
| listing_date | date | YES | - |
| par_value | bigint | YES | - |
| listing_shares | bigint | YES | - |
| is_delisted | boolean | NO | false |
| updated_at | timestamptz | NO | now() |

- price, change_amount, change_rate, volume 등 시세 컬럼 **없음** (마스터 메타 전용)
- CHECK constraint: `market IN ('KOSPI', 'KOSDAQ')`
- PK: `stocks_pkey1` on code (btree)

### stock_quotes

| Column | Type | Nullable | Default |
|--------|------|----------|---------|
| code | text | NO | - |
| price | numeric | NO | - |
| change_amount | numeric | NO | - |
| change_rate | numeric | NO | - |
| volume | bigint | NO | 0 |
| trade_amount | bigint | NO | 0 |
| open | numeric | YES | - |
| high | numeric | YES | - |
| low | numeric | YES | - |
| market_cap | bigint | YES | - |
| upper_limit | numeric | NO | - |
| lower_limit | numeric | NO | - |
| updated_at | timestamptz | NO | now() |

- FK: `stock_quotes_code_fkey` -- FOREIGN KEY (code) REFERENCES stocks(code) ON DELETE CASCADE
- PK: `stock_quotes_pkey` on code (btree)

### top_movers

| Column | Type | Nullable | Default |
|--------|------|----------|---------|
| code | text | NO | - |
| name | text | NO | - |
| market | text | NO | - |
| updated_at | timestamptz | NO | now() |
| rank | integer | YES | - |
| ranked_at | timestamptz | NO | now() |
| scan_id | uuid | YES | - |

- 시세 컬럼(price, change_amount, volume 등) **없음** -- 랭킹 전용
- PK: `stocks_pkey` on code (btree) -- rename 시 PostgreSQL이 PK index 이름을 자동 변경하지 않음 (기능 정상)

## FK Re-point (Pitfall 1 검증)

### news_articles_stock_code_fkey

```
FOREIGN KEY (stock_code) REFERENCES stocks(code) ON DELETE CASCADE
```

top_movers가 아닌 **새 stocks 테이블**을 가리킴 -- Pitfall 1 회피 확인.

### discussions_stock_code_fkey

```
FOREIGN KEY (stock_code) REFERENCES stocks(code) ON DELETE CASCADE
```

동일하게 새 stocks 를 가리킴 -- 정상.

### stock_quotes_code_fkey

```
FOREIGN KEY (code) REFERENCES stocks(code) ON DELETE CASCADE
```

stock_quotes -> stocks FK CASCADE 정상.

## RLS Policies

| Table | Policy | Roles | Command | Qual |
|-------|--------|-------|---------|------|
| stocks | anon_read_stocks_master | {anon} | SELECT | true |
| stock_quotes | anon_read_stock_quotes | {anon} | SELECT | true |
| top_movers | anon_read_top_movers | {anon} | SELECT | true |

3개 테이블 모두 anon SELECT 허용 정책 활성 -- Phase 1 D-17 패턴 승계 완료.

## Indexes

### stocks

| Index | Definition |
|-------|-----------|
| stocks_pkey1 | UNIQUE btree (code) |
| idx_stocks_market | btree (market) |
| idx_stocks_updated_at | btree (updated_at DESC) |
| idx_stocks_name_trgm | **gin (name gin_trgm_ops)** |

pg_trgm GIN 인덱스 `idx_stocks_name_trgm` 존재 확인.

### stock_quotes

| Index | Definition |
|-------|-----------|
| stock_quotes_pkey | UNIQUE btree (code) |
| idx_stock_quotes_change_rate_desc | btree (change_rate DESC NULLS LAST) |
| idx_stock_quotes_updated_at | btree (updated_at DESC) |

### top_movers

| Index | Definition |
|-------|-----------|
| stocks_pkey | UNIQUE btree (code) -- rename 잔류 이름, 기능 정상 |
| idx_top_movers_market | btree (market) |
| idx_top_movers_rank | btree (rank) |
| idx_top_movers_ranked_at | btree (ranked_at DESC) |

## Row Counts (부트스트랩 후)

| Table | Count |
|-------|-------|
| stocks | 55 |
| stock_quotes | 55 |
| top_movers | 55 |

stocks와 top_movers row count 동일 (rename 보존 + INSERT ... ON CONFLICT DO NOTHING 결과). stock_quotes도 55로 동일 (Step 2 INSERT 복제).

## Cloud Scheduler

```
gcloud scheduler jobs describe gh-radar-ingestion-scheduler --location=asia-northeast3 --format='value(state)'
PAUSED
```

마이그레이션 중 ingestion cycle 방지를 위해 일시 중단됨. Plan 06 Task 4 Step 3.5 에서 ENABLED 로 resume 예정.

## Sign-off

- [x] BEGIN/COMMIT 트랜잭션으로 push 성공 (exit code 0)
- [x] news_articles / discussions FK 가 새 stocks 를 가리킴 (Pitfall 1 회피)
- [x] stock_quotes FK CASCADE 동작
- [x] anon_read_stocks_master / anon_read_stock_quotes / anon_read_top_movers 정책 활성
- [x] idx_stocks_name_trgm (pg_trgm GIN) 인덱스 존재
- [x] 부트스트랩: 3개 테이블 모두 55 row (기존 데이터 보존)
- [x] Cloud Scheduler PAUSED (BLOCKER #5)
