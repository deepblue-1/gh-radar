---
phase: 06.1
slug: stock-master-universe
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-15
---

# Phase 06.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Populated from `06.1-RESEARCH.md` §"Validation Architecture". Planner은 각 PLAN.md 태스크의 `<automated>` 항목을 아래 명령과 정합하게 작성해야 함.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (server, webapp, workers) + playwright (webapp E2E) |
| **Config file** | `server/vitest.config.ts`, `webapp/vitest.config.ts`, `webapp/playwright.config.ts` (기존) · `workers/master-sync/vitest.config.ts` (Wave 0 신설) |
| **Quick run command** | `pnpm --filter server test -- --run && pnpm --filter webapp test -- --run` |
| **Full suite command** | `pnpm --filter server test -- --run && pnpm --filter webapp test -- --run && pnpm --filter master-sync test -- --run && pnpm --filter webapp exec playwright test` |
| **Estimated runtime** | ~120 seconds (full) / ~30 seconds (quick) |

---

## Sampling Rate

- **After every task commit:** Run quick command (해당 워크스페이스만 필터)
- **After every plan wave:** Run full suite command
- **Before `/gsd-verify-work`:** Full suite must be green + `bash scripts/smoke-master-sync.sh` PASS + 삼성전자 검색 E2E PASS
- **Max feedback latency:** 30 seconds (quick)

---

## Per-Task Verification Map

| Req ID | Behavior | Test Type | Automated Command | File Exists | Status |
|--------|----------|-----------|-------------------|-------------|--------|
| SRCH-01 | "삼성전자"(005930) 검색 시 결과 포함 (회귀) | e2e | `pnpm --filter webapp exec playwright test search.spec.ts -g "삼성전자"` | ❌ W3 | ⬜ pending |
| SRCH-01 | `/api/stocks/search?q=005930` 마스터 universe 에서 반환 | integration | `pnpm --filter server test search` | ✅ (수정) | ⬜ pending |
| SRCH-01 | 마스터에 없는 종목 검색 시 빈 배열 | unit | `pnpm --filter server test search` | ✅ | ⬜ pending |
| SRCH-02 | `searchStocks(q)` → stocks + stock_quotes LEFT JOIN 병합 | unit | `pnpm --filter webapp test stock-api` | ✅ | ⬜ pending |
| SRCH-03 | `/api/stocks/:code` 마스터 존재 + on-demand inquirePrice 병합 | integration | `pnpm --filter server test stock-detail` | ✅ (수정) | ⬜ pending |
| SRCH-03 | inquirePrice 실패 시 cached stock_quotes 폴백 | integration | `pnpm --filter server test stock-detail` | ❌ W0 | ⬜ pending |
| SRCH-03 | 마스터 부재 → 404 STOCK_NOT_FOUND (inquirePrice 미호출) | integration | `pnpm --filter server test stock-detail` | ✅ | ⬜ pending |
| SCAN-01~07 (회귀) | `/api/scanner` 3-테이블 JOIN 응답 스키마 유지 | integration | `pnpm --filter server test scanner` | ✅ (수정) | ⬜ pending |
| SCAN-08 (회귀) | `X-Last-Updated-At` 헤더가 `stock_quotes.updated_at MAX` 기준 | integration | `pnpm --filter server test scanner` | ✅ (수정) | ⬜ pending |
| INFR-02 | 마스터 동기화 Job idempotent (같은 basDd 2회 실행 동일 upsert) | e2e smoke | `bash scripts/smoke-master-sync.sh` | ❌ W3 | ⬜ pending |
| INFR-02 | 마이그레이션이 `news_articles.stock_code` FK 를 새 stocks 로 re-point | migration verify | `psql -c "\d news_articles"` 검증 + SUMMARY 기록 | ❌ W1 | ⬜ pending |
| A11y (회귀) | axe 접근성 — 상세/Dialog/404 green 유지 | e2e | `pnpm --filter webapp exec playwright test a11y.spec.ts` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave별 산출물 요구사항 (BLOCKER #3 재분류)

### Wave 0 (Plan 01 — scaffold/stub only)

- [ ] `workers/master-sync/` 워크스페이스 신설 (package.json, tsconfig, vitest.config.ts, Dockerfile, .gitignore, src/* stubs)
- [ ] `workers/master-sync/tests/krx-client.test.ts` — axios mock + AUTH_KEY header 검증 (RED 허용, Plan 03 GREEN)
- [ ] `workers/master-sync/tests/map.test.ts` — krxToMasterRow 매핑 단위 테스트 (RED 허용, Plan 03 GREEN)
- [ ] `server/src/kis/` 디렉토리 복제 + 단위 테스트 (inquirePrice/rateLimiter/client/tokenStore/readOnlyGuard)
- [ ] `server/tests/routes/stock-detail.test.ts` — 마스터 universe + on-demand inquirePrice 폴백 RED 시나리오 4종 (Plan 04 가 GREEN 으로 전환)
- [ ] `server/tests/fixtures/supabase-mock.ts` — multi-table 분기 본격 구현 (BLOCKER #1 수정 결과 — Plan 04 가 재사용)
- [ ] `packages/shared/src/stock.ts` — `StockMaster`, `StockQuote`, `StockWithQuote`, `SecurityType` 타입 추가

### Wave 1 (Plan 02 — 스키마 마이그레이션)

- [ ] `supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql` — 3-테이블 분리 + FK re-point + RLS + pg_trgm 인덱스 (단일 BEGIN/COMMIT)
- [ ] `.planning/phases/06.1-stock-master-universe/06.1-02-MIGRATION-VERIFY.md` — production push 결과 + psql 검증 paste
- [ ] **BLOCKER #5:** 마이그레이션 직전 `gh-radar-ingestion-scheduler` 를 PAUSED 로 전환 (Plan 06 Step 3.5 에서 ENABLED 로 resume)

### Wave 2 (Plan 03 + Plan 04 + Plan 05 — 구현)

- [ ] `server/tests/routes/search.test.ts` — 마스터 universe 기반 검색 케이스 재작성 + WARN #7 2자 쿼리 회귀 (Plan 04)
- [ ] `workers/master-sync/src/*` 실제 로직 (Plan 03 — fetchMasterFromKrx / krxToMasterRow / upsertMasters / retry / index 파이프라인)
- [ ] `server/src/routes/stocks.ts` 마스터 universe + on-demand inquirePrice + 폴백 (Plan 04)
- [ ] `server/src/routes/scanner.ts` + `workers/ingestion` 3-테이블 전환 (Plan 05)

### Wave 3 (Plan 06 — 배포/백필/E2E)

- [ ] `scripts/setup-master-sync-iam.sh` / `scripts/deploy-master-sync.sh` / `scripts/smoke-master-sync.sh` — Cloud Run Job + Scheduler + KRX Secret
- [ ] `scripts/smoke-master-sync.sh` INV-4: supabase stocks count >= 2500 검증
- [ ] `webapp/e2e/specs/search.spec.ts` — "삼성전자 검색 PASS" 회귀 시나리오 (mock-api fixture 확장)
- [ ] `.planning/phases/06.1-stock-master-universe/06.1-06-DEPLOY-LOG.md` — 배포/백필/E2E 전 결과 paste

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| KRX AUTH_KEY 발급 | INFR-02 선행 | 외부 서비스 가입/승인 1일 소요 | 사용자가 https://openapi.krx.co.kr 가입 + 서비스 승인 → `gh-radar-krx-auth-key` Secret 등록 |
| Cloud Scheduler 생성 + 실행 권한 | INFR-02 | gcloud CLI + 프로젝트 IAM 필요 | `scripts/deploy-master-sync.sh` 실행 후 `gcloud scheduler jobs list` 확인 |
| 마이그레이션 프로덕션 적용 | 모든 요구사항 | 데이터 보존 트랜잭션 rename 수동 검증 | `supabase db push` 후 psql 로 기존 FK/row count 검증 |

---

## Validation Sign-Off

- [ ] 모든 task에 `<automated>` verify 또는 Wave 0 의존성 존재
- [ ] Sampling continuity: 연속 3개 task 이상 automated verify 누락 없음
- [ ] Wave 0이 모든 MISSING 참조를 커버
- [ ] watch-mode 플래그 금지 — 모든 test는 `--run` 또는 단발 실행
- [ ] Feedback latency < 30s (quick)
- [ ] 최종 plan-checker 통과 시 `nyquist_compliant: true` 로 전환

**Approval:** pending
