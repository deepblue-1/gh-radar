---
phase: "06.1"
fixed_at: "2026-04-16T03:23:48Z"
review_path: ".planning/phases/06.1-stock-master-universe/06.1-REVIEW.md"
iteration: 1
findings_in_scope: 6
fixed: 6
skipped: 0
status: all_fixed
---

# Phase 06.1: Code Review Fix Report

**Fixed at:** 2026-04-16T03:23:48Z
**Source review:** `.planning/phases/06.1-stock-master-universe/06.1-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope: 6 (CRITICAL 2 + HIGH 4)
- Fixed: 6
- Skipped: 0
- Out of scope (not attempted): MED-1 (sanitize 싱글쿼트 — fix_scope=critical_warning)

## Fixed Issues

### CRIT-1: ingestion `index.ts` — `fetchAllRanking` 이중 호출

**Files modified:** `workers/ingestion/src/index.ts`, `workers/ingestion/src/pipeline/run.ts`
**Commit:** `b4fb44d`
**Applied fix:** `fetchAllRanking` 을 `main()` 에서 `withRetry` 로 한 번만 호출한 뒤, `rankings` 를 `runPipeline(client, supabase, rankings)` 의 세 번째 파라미터로 전달. `runPipeline` 내부의 중복 호출 제거. 타입은 `{market, rows}[]` 로 명시. KIS API 호출량 2회로 축소, holiday 판정/파이프라인이 동일 스냅샷을 사용해 일관성 복구.

### CRIT-2: `GET /api/scanner` — 상장폐지 종목 노출

**Files modified:** `server/src/routes/scanner.ts`
**Commit:** `f0e8e76`
**Applied fix:** stocks 마스터 조회에 `.eq("is_delisted", false)` 필터 추가. 병합 단계에서 master 에 없는 top_movers row(폐지/미동기화) 를 `filter((mv) => masterByCode.has(mv.code))` 로 제외. 기존 13개 scanner 라우트 테스트 전부 green.

### HIGH-1: `KisRankingRow` 타입 — `stck_shrn_iscd` · `acml_hgpr_date` 미선언

**Files modified:** `packages/shared/src/kis.ts`
**Commit:** `d9e7f92`
**Applied fix:** 실제 사용되는 `stck_shrn_iscd` (pipeline primary key), `acml_hgpr_date` (holidayGuard 거래일 판정) 를 타입에 명시. 주석으로 각 필드 용도 문서화. 스키마 변화에 대비해 index signature 는 유지. ingestion/server 양쪽 `tsc --noEmit` 모두 통과.

### HIGH-2: `map.ts` — open 필드에 고가(stck_hgpr) 오기입

**Files modified:** `workers/ingestion/src/pipeline/map.ts`, `workers/ingestion/tests/map.test.ts`
**Commit:** `a4a9f85`
**Applied fix:** inquirePrice fallback 경로에서 `Number(row.stck_hgpr)` → `Number(row.stck_oprc) || 0` 로 정정. 기존 테스트(`fallback → stock.open === 1157`) 는 새 동작에 맞춰 `0` 기대값으로 수정. ranking 응답에 stck_oprc 가 실제 존재할 때의 동작을 검증하는 신규 테스트(`stck_oprc: "950"` → `open === 950`) 추가.

### HIGH-3: delist-sweep 조회가 PostgREST 기본 limit 1000에 걸림

**Files modified:** `workers/master-sync/src/index.ts`, `workers/master-sync/tests/index.test.ts`
**Commit:** `0f185ba`
**Applied fix:** `.limit(10000)` 명시. 테스트 mock supabase chain(`.eq` → `.limit` → 결과) 을 해당 체이닝 지원하도록 업데이트. 5개 master-sync index 테스트 모두 green (delist-sweep 2건 케이스 포함).

### HIGH-4: master-sync Job 이 ingestion-sa 사용 (최소권한 위반)

**Files modified:** `scripts/deploy-master-sync.sh`, `scripts/setup-master-sync-iam.sh`
**Commit:** `926d625`
**Applied fix:**
- `setup-master-sync-iam.sh`: 전용 SA `gh-radar-master-sync-sa` idempotent 생성, KRX/Supabase 시크릿에만 `secretAccessor` 바인딩 (KIS 시크릿은 의도적 미바인딩).
- `deploy-master-sync.sh`: Cloud Run Job deploy 시 `--service-account` 를 `gh-radar-master-sync-sa` 로 변경. 선행 SA 검증 루프에서도 `ingestion-sa` 대신 `master-sync-sa` 확인.
- `bash -n` syntax check 통과.

---

_Fixed: 2026-04-16T03:23:48Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
