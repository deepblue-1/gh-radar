# Phase 06.1 — API Verification

**Verified:** 2026-04-15
**Purpose:** Planner의 LOW confidence 가정(A1: KRX 응답 필드명) 및 server-side KIS 호출 경로의 응답 스키마를 실제 호출로 확정한다. 본 문서의 VERIFIED 항목은 Plan 03(KRX mapper), Plan 04(KIS on-demand) 구현의 **잠긴 계약**으로 사용된다.

---

## 1. KIS `inquirePrice` — **VERIFIED**

**Endpoint:** `GET /uapi/domestic-stock/v1/quotations/inquire-price`
**Auth:** `Authorization: Bearer <token>` + `appkey` + `appsecret` (현 `workers/ingestion/.env`)
**tr_id:** `FHKST01010100`
**Params:** `fid_cond_mrkt_div_code=J`, `fid_input_iscd=<6자리 종목코드>`
**Base URL:** `https://openapi.koreainvestment.com:9443` (prod)

### Test Run

```bash
cd workers/ingestion
npx tsx scripts/verify-inquire-price.ts 005930
```

**Call result:** `rt_cd=0` (success), `output` = KOSPI 200 삼성전자 실시간 시세 dump. 토큰은 Supabase `kis_tokens.current` 에서 재사용(25시간 TTL 중 ~24시간 남아 있음).

### Response `output` — 확정 필드 (104개 키, 핵심만 발췌)

| 필드 | 타입(문자열) | 예시값(005930) | 용도 |
|------|---------|--------|------|
| `stck_prpr` | 현재가 | `"211000"` | Plan 04 `stock_quotes.price` |
| `prdy_vrss` | 전일대비 | `"4500"` | `stock_quotes.change` |
| `prdy_vrss_sign` | 부호("2"=상승,"5"=하락,"3"=보합) | `"2"` | `stock_quotes.change_sign` |
| `prdy_ctrt` | 전일대비율(%) | `"2.18"` | `stock_quotes.change_pct` |
| `stck_mxpr` | 상한가 | `"268000"` | SCAN scanner 상한가 근접 산출 |
| `stck_llam` | 하한가 | `"145000"` | (선택) |
| `stck_oprc` | 시가 | `"215000"` | `stock_quotes.open` (기존 유지) |
| `stck_hgpr` | 고가 | `"215500"` | `stock_quotes.high` |
| `stck_lwpr` | 저가 | `"210000"` | `stock_quotes.low` |
| `acml_vol` | 누적거래량 | `"23966133"` | `stock_quotes.volume` |
| `acml_tr_pbmn` | 누적거래대금(원) | `"5118011886500"` | `stock_quotes.trade_amount` (Phase 1-hotfix 필드) |
| `hts_avls` | 시가총액(억) | `"12335648"` | 상세 페이지 표시 |
| `per` / `pbr` / `eps` / `bps` | 재무 지표 | `"32.15"`/`"3.30"`/`"6564.00"`/`"63997.00"` | 상세 페이지 |
| `stck_shrn_iscd` | 단축종목코드 | `"005930"` | 역참조 검증 |
| `rprs_mrkt_kor_name` | 대표시장명 | `"KOSPI200"` | (참고) — 본 phase 에서는 마스터의 `market` 사용 |
| `bstp_kor_isnm` | 업종명 | `"전기·전자"` | (참고) — KRX `SECT_TP_NM` 우선 |
| `temp_stop_yn` | 거래정지 | `"N"` | 상태 표시 |
| `vi_cls_code` | VI 발동 | `"N"` | 상태 표시 |

**모든 값은 문자열로 반환 — Plan 04 `inquirePriceToQuoteRow` mapper 에서 `Number()` 캐스팅 필요.**

### Error Envelope

정상: `{ rt_cd: "0", msg_cd: "...", msg1: "...", output: {...} }`
실패: `rt_cd !== "0"` → `workers/ingestion/src/kis/inquirePrice.ts:30` 이 `throw new Error("KIS inquire-price error for <code>: <msg_cd> <msg1>")`. Plan 04 폴백 경로는 이 throw 를 catch 하여 `stock_quotes` cached 조회로 전환.

### Confidence

- **Endpoint, tr_id, params, auth header set:** VERIFIED (live prod response)
- **Response 필드명 104개:** VERIFIED (snapshot in Appendix A)
- **Rate limit 5 req/sec:** VERIFIED via `workers/ingestion/src/kis/rateLimiter.ts` 상수
- **Token TTL 24h:** VERIFIED via `tokenStore.ts` + `kis_tokens.expires_at`

### Plan 04 영향

- `server/src/kis/` 복제 시 **위 스펙 그대로 이식** — 수정 없음.
- `inquirePriceToQuoteRow(code, output)` 매핑은 위 표의 6개 핵심 필드만 `Number()` 캐스팅 후 upsert.
- 폴백: `inquirePrice` 가 throw 하면 `stock_quotes` select — 이미 Plan 04 Task 2 Step B 설계에 반영됨.

---

## 2. KRX OpenAPI `stk_isu_base_info` / `ksq_isu_base_info` — **VERIFIED (2026-04-16)**

**Endpoint:** `GET https://data-dbg.krx.co.kr/svc/apis/sto/{stk|ksq}_isu_base_info?basDd=YYYYMMDD`
**Auth:** HTTP 헤더 `AUTH_KEY: <발급키>` — 공백/개행 시 401, `.trim()` 필수
**HTTP 200 + 정상 응답 확인** — 사용자 발급 키로 2026-04-15 기준일 호출 성공.

### Test Run

```bash
KRX_AUTH_KEY="<발급키>"
curl -s -H "AUTH_KEY: $KRX_AUTH_KEY" \
  "https://data-dbg.krx.co.kr/svc/apis/sto/stk_isu_base_info?basDd=20260415"
curl -s -H "AUTH_KEY: $KRX_AUTH_KEY" \
  "https://data-dbg.krx.co.kr/svc/apis/sto/ksq_isu_base_info?basDd=20260415"
```

### 실측 결과

| 항목 | KOSPI (`stk_*`) | KOSDAQ (`ksq_*`) |
|------|----------|----------|
| 응답 사이즈 | 281 KB | 562 KB |
| 종목 수 | **950** | **1,821** |
| **합계** | **2,771** (RESEARCH 추정 ~2,800 일치) | |
| 삼성전자(005930) 포함 | ✅ 확인 | — |

### 응답 스키마 — 12개 필드 (KOSPI/KOSDAQ 동일)

| 필드 | 예시값(005930) | Plan 03 mapper 매핑 |
|------|----------------|---------------------|
| `ISU_CD` | `KR7005930003` | (참고용, 12자 표준코드) |
| `ISU_SRT_CD` | `005930` | → `stocks.code` |
| `ISU_NM` | `삼성전자보통주` | (풀네임, fallback) |
| `ISU_ABBRV` | `삼성전자` | → `stocks.name` (검색 친화) ✅ |
| `ISU_ENG_NM` | `SamsungElectronics` | (선택, 영문명) |
| `LIST_DD` | `19750611` | → `stocks.listing_date` (YYYYMMDD → YYYY-MM-DD 변환) |
| `MKT_TP_NM` | `KOSPI` | → `stocks.market` (KOSPI/KOSDAQ) |
| `SECUGRP_NM` | `주권` | **증권그룹** (주권/부동산투자회사/투자회사/외국주권/주식예탁증권/사회간접자본투융자회사) |
| `SECT_TP_NM` | `""` (KOSPI는 전부 빈 문자열) | **소속부**(KOSDAQ 전용) — RESEARCH의 "sector" 추정 **틀림**, 업종 아님 |
| `KIND_STKCERT_TP_NM` | `보통주` | → `stocks.security_type` ✅ (보통주/구형우선주/신형우선주/종류주권) |
| `PARVAL` | `100` | (선택, 액면가) |
| `LIST_SHRS` | `5846278608` | (선택, 상장주식수) |

### KRX 응답 분포 (KOSPI 950종목 기준)

- `SECUGRP_NM`: 주권 919 / 부동산투자회사(REIT) 25 / 투자회사 2 / 사회간접자본투융자회사 2 / 외국주권 1 / 주식예탁증권 1
- `KIND_STKCERT_TP_NM`: 보통주 840 / 구형우선주 78 / 신형우선주 21 / 종류주권 11
- `SECT_TP_NM`: 전부 빈 문자열

### KOSDAQ `SECT_TP_NM` 분포 (소속부)

중견기업부 532 / 우량기업부 461 / 벤처기업부 352 / 기술성장기업부 259 / 관리종목 78 / SPAC 72 / 투자주의환기종목 50 / 외국기업 17

### 🚨 RESEARCH/Plan 03 정정 사항 (검증으로 발견)

| # | RESEARCH/Plan 03 가정 | 실측 결과 | Plan 03 mapper 영향 |
|---|----------------------|-----------|---------------------|
| **C1** | `SECT_TP_NM` = sector(업종, 예: "전기·전자") | **소속부 카테고리** (KOSDAQ만), KOSPI는 빈 문자열. 진짜 업종 정보는 KRX 응답에 **없음** | mapper의 `sector` 매핑 의미 변경: "section" 또는 "market_segment"로 rename 권장. 진짜 업종 채우려면 KIS `inquirePrice.bstp_kor_isnm` (`전기·전자`)에 의존해야 함 |
| **C2** | `securityType: r.SECUGRP_NM ?? "보통주"` (Plan 03:365) | `SECUGRP_NM`은 증권그룹(주권/REIT/투자회사 등). 종목 종류(보통주/우선주)는 `KIND_STKCERT_TP_NM` | mapper 변경: `securityType: r.KIND_STKCERT_TP_NM ?? "보통주"` |
| **C3** | "상장폐지 여부" 컬럼 (CONTEXT D6) | KRX 응답에 없음. 본 응답에 포함된 종목은 모두 활성 | 동기화 시 마스터에서 사라진 코드를 `is_delisted=true`로 마킹하는 로직 필요 (Plan 03 upsert 단계 보강) |
| **C4** | KOSPI 회사구분 SECUGRP_NM에 ETF/ETN 포함 | 본 응답엔 ETF/ETN 카테고리 **없음**. 별도 endpoint(`etf_isu_base_info`/`etn_isu_base_info`) 필요. 단 SCAN 사용 케이스는 일반 주식 위주라 본 phase는 보류 가능 | scope 결정 필요 — "보통주만"이면 추가 endpoint 불필요 |

**Plan 03 revision 필요 여부:** **YES** — C1, C2, C3 mapper 보정 필수. ETF/ETN(C4)은 본 phase deferred 권장.

### 요금 정책

- 호출 자체는 발급 키만으로 200 OK — **결제 정보 없이 동작 → 무료 가능성 높음**
- 단, **응답에 요금 정보가 포함되지 않으며**, KRX 사이트 로그인 후 마이페이지에서만 정확한 요금 정책 확인 가능
- 공식 답변이 필요한 경우: krxdata@krx.co.kr / 051-662-2000

### 갱신 시점 (RESEARCH 일치)

- 매일 08:00 KST에 전일 데이터 갱신
- 본 phase의 cron(`30 7 * * 1-5` Asia/Seoul)은 갱신 전 시각 — **08:30 KST로 늦춰야 함** (Plan 06 deploy script 보정 필요)

### Secret 등록 (배포 시)

```bash
echo -n "0705AC6833454749AB05792A68AC2D47D56E1E6D" | gcloud secrets create gh-radar-krx-auth-key \
  --project=<PROJECT_ID> \
  --data-file=-

gcloud secrets add-iam-policy-binding gh-radar-krx-auth-key \
  --project=<PROJECT_ID> \
  --member=serviceAccount:gh-radar-ingestion-sa@<PROJECT_ID>.iam.gserviceaccount.com \
  --role=roles/secretmanager.secretAccessor
```

---

## 3. Decision Impact Summary

| Decision (CONTEXT.md) | 검증 상태 | 결과 |
|-----|---------|------|
| D4 — 상세 진입 시 KIS `inquirePrice` on-demand fetch | ✅ VERIFIED | stck_prpr/acml_tr_pbmn 등 핵심 필드 확정, mapper 설계 잠금 |
| D6 — 마스터 메타 풍부 (sector/listing_date/security_type 등) | ⚠️ PARTIALLY VERIFIED | KRX 응답에 **업종(sector) 정보 없음** — `bstp_kor_isnm`(KIS)에서만 가져올 수 있음. 종목구분은 `KIND_STKCERT_TP_NM` 사용. 상장폐지 컬럼도 없음 |
| D7 — KRX OpenAPI 우선, KIS 종목기본조회 폴백 | ✅ VERIFIED | KRX 호출 성공, 2,771 종목(KOSPI 950 + KOSDAQ 1,821) 확보 가능 확인 |
| D9 — on-demand 무조건 1회 호출 | ✅ VERIFIED | 단일 호출 응답 크기 ~2KB, 5 req/sec 안에서 충분한 여유 |

---

## 4. Next Steps

**즉시 가능 (배포 차단 없음):**
- Wave 0~3 전 plan 실행 가능 — KRX 키 + 응답 스키마 모두 확정.
- 배포 직전 `gh-radar-krx-auth-key` Secret 등록 1회로 충분.

**Plan 03 revision 필요 (§2 정정 사항 C1~C4 반영):**
- mapper의 `securityType` 소스를 `SECUGRP_NM` → `KIND_STKCERT_TP_NM`으로 변경 (C2)
- `sector` 필드 의미 재정의 또는 NULL 처리 (C1) — KOSPI는 빈 값, KOSDAQ은 소속부
- `is_delisted` 동기화 로직 추가 (C3) — 응답에서 사라진 코드 마킹
- mapper 테스트 케이스 fixture를 실측 응답으로 교체 (`SECT_TP_NM:"전기·전자"` → `""` 또는 `"중견기업부"`)

**Plan 06 cron 시각 보정:**
- 현재: `30 7 * * 1-5` Asia/Seoul (07:30) — KRX 갱신(08:00) **이전** 시각, 전일 데이터 미갱신 가능
- 권장: `30 8 * * 1-5` Asia/Seoul (08:30)으로 변경

**KRX 요금 정책 확인 (선택):**
- 응답에는 요금 정보 없음. 사이트 마이페이지 또는 krxdata@krx.co.kr 문의로 최종 확인 권장
- 현 호출 패턴(일 1회 KOSPI+KOSDAQ 2 calls)은 어떤 요금 체계에서도 비용 부담 거의 없음

---

## Appendix A — KIS `inquirePrice` 전체 응답 키 (005930 실측)

acml_tr_pbmn, acml_vol, aspr_unit, bps, bstp_kor_isnm, clpr_rang_cont_yn, cpfn, cpfn_cnnm, crdt_able_yn, d250_hgpr, d250_hgpr_date, d250_hgpr_vrss_prpr_rate, d250_lwpr, d250_lwpr_date, d250_lwpr_vrss_prpr_rate, dmrs_val, dmsp_val, dryy_hgpr_date, dryy_hgpr_vrss_prpr_rate, dryy_lwpr_date, dryy_lwpr_vrss_prpr_rate, elw_pblc_yn, eps, fcam_cnnm, frgn_hldn_qty, frgn_ntby_qty, grmn_rate_cls_code, hts_avls, hts_deal_qty_unit_val, hts_frgn_ehrt, invt_caful_yn, iscd_stat_cls_code, last_ssts_cntg_qty, lstn_stcn, mang_issu_cls_code, marg_rate, mrkt_warn_cls_code, oprc_rang_cont_yn, ovtm_vi_cls_code, pbr, per, pgtr_ntby_qty, prdy_ctrt, prdy_vrss, prdy_vrss_sign, prdy_vrss_vol_rate, pvt_frst_dmrs_prc, pvt_frst_dmsp_prc, pvt_pont_val, pvt_scnd_dmrs_prc, pvt_scnd_dmsp_prc, rprs_mrkt_kor_name, rstc_wdth_prc, short_over_yn, sltr_yn, ssts_yn, stac_month, stck_dryy_hgpr, stck_dryy_lwpr, stck_fcam, stck_hgpr, stck_llam, stck_lwpr, stck_mxpr, stck_oprc, stck_prpr, stck_sdpr, stck_shrn_iscd, stck_sspr, temp_stop_yn, vi_cls_code, vol_tnrt, w52_hgpr, w52_hgpr_date, w52_hgpr_vrss_prpr_ctrt, w52_lwpr, w52_lwpr_date, w52_lwpr_vrss_prpr_ctrt, wghn_avrg_stck_prc, whol_loan_rmnd_rate

## Appendix B — KRX `stk_isu_base_info` 실측 응답 키 (basDd=20260415)

**12개 필드 (KOSPI/KOSDAQ 동일 스키마):** ISU_ABBRV, ISU_CD, ISU_ENG_NM, ISU_NM, ISU_SRT_CD, KIND_STKCERT_TP_NM, LIST_DD, LIST_SHRS, MKT_TP_NM, PARVAL, SECT_TP_NM, SECUGRP_NM

**삼성전자(005930) 실측 row:**
```json
{
  "ISU_CD": "KR7005930003",
  "ISU_SRT_CD": "005930",
  "ISU_NM": "삼성전자보통주",
  "ISU_ABBRV": "삼성전자",
  "ISU_ENG_NM": "SamsungElectronics",
  "LIST_DD": "19750611",
  "MKT_TP_NM": "KOSPI",
  "SECUGRP_NM": "주권",
  "SECT_TP_NM": "",
  "KIND_STKCERT_TP_NM": "보통주",
  "PARVAL": "100",
  "LIST_SHRS": "5846278608"
}
```

**KOSDAQ row 예시 (`SECT_TP_NM` 값 있는 경우):**
```json
{
  "ISU_CD": "KR7098120009",
  "ISU_SRT_CD": "098120",
  "ISU_NM": "(주)마이크로컨텍솔루션",
  "ISU_ABBRV": "마이크로컨텍솔",
  "MKT_TP_NM": "KOSDAQ",
  "SECUGRP_NM": "주권",
  "SECT_TP_NM": "중견기업부",
  "KIND_STKCERT_TP_NM": "보통주",
  "LIST_DD": "20080923",
  "PARVAL": "500",
  "LIST_SHRS": "8312766"
}
```

---

## Appendix — Verification Script

`workers/ingestion/scripts/verify-inquire-price.ts` (본 검증을 위해 작성, diagnostic 용도로 보관).

```
cd workers/ingestion
npx tsx scripts/verify-inquire-price.ts [종목코드]
# 기본값: 005930 (삼성전자)
```
