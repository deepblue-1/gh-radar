---
phase: 06.1-stock-master-universe
plan: 06
type: execute
wave: 3
depends_on:
  - 06.1-02
  - 06.1-03
  - 06.1-04
  - 06.1-05
files_modified:
  - scripts/setup-master-sync-iam.sh
  - scripts/deploy-master-sync.sh
  - scripts/smoke-master-sync.sh
  - scripts/deploy-server.sh
  - webapp/e2e/specs/search.spec.ts
  - webapp/e2e/fixtures/mock-api.ts
  - webapp/e2e/fixtures/stocks.ts
  - .planning/phases/06.1-stock-master-universe/06.1-06-DEPLOY-LOG.md
autonomous: false
requirements:
  - INFR-02
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SCAN-01
  - SCAN-02
  - SCAN-03
  - SCAN-04
  - SCAN-05
  - SCAN-06
  - SCAN-07
  - SCAN-08

user_setup:
  - service: KRX OpenAPI Secret 등록
    why: "master-sync Cloud Run Job 이 KRX_AUTH_KEY 를 Secret Manager 에서 주입받음"
    env_vars:
      - name: KRX_AUTH_KEY
        source: "사용자 .env.deploy 또는 1Password — Plan 02 user_setup 에서 발급 완료된 값"
    dashboard_config:
      - task: "Secret Manager 에 gh-radar-krx-auth-key 등록 (setup-master-sync-iam.sh 실행 시 자동)"
        location: "Google Cloud Secret Manager"

must_haves:
  truths:
    - "scripts/setup-master-sync-iam.sh 가 KRX Secret + secretAccessor 바인딩을 idempotent 추가한다"
    - "scripts/deploy-master-sync.sh 가 ingestion 패턴 미러로 이미지 빌드 + Cloud Run Job + Scheduler 생성"
    - "Cloud Run Job gh-radar-master-sync 가 평일 08:10 KST 1회 실행되도록 Scheduler 등록"
    - "초기 마스터 백필을 위해 Job 1회 수동 execute 후 Supabase stocks count >= 2500"
    - "기존 ingestion Cloud Run Job 이 Plan 05 코드(stock_quotes + top_movers)로 재배포되어 EGW00201 없이 cycle 완료"
    - "기존 server Cloud Run 이 Plan 04 코드로 재배포되어 KIS_APP_KEY/SECRET 새 env 가 주입됨"
    - "webapp E2E 9종 + 신규 '삼성전자 검색 PASS' 회귀 시나리오 GREEN"
    - "DEPLOY-LOG.md 에 모든 배포 결과·smoke 출력·E2E 결과 기록"
  artifacts:
    - path: "scripts/deploy-master-sync.sh"
      provides: "master-sync Cloud Run Job + Scheduler 배포 (ingestion 미러)"
      contains: "gh-radar-master-sync"
    - path: "scripts/smoke-master-sync.sh"
      provides: "master-sync 배포 검증 invariants"
      contains: "stocks count"
    - path: ".planning/phases/06.1-stock-master-universe/06.1-06-DEPLOY-LOG.md"
      provides: "전체 배포 + 백필 + E2E 결과 기록"
      contains: "INV-"
  key_links:
    - from: "Cloud Scheduler gh-radar-master-sync-scheduler"
      to: "Cloud Run Job gh-radar-master-sync"
      via: "OAuth (--oauth-service-account-email)"
      pattern: "oauth-service-account-email"
    - from: "Cloud Run Job gh-radar-master-sync"
      to: "Supabase stocks 테이블"
      via: "service_role key + REST upsert"
      pattern: "stocks"
---

<objective>
Phase 06.1 production 활성화 — master-sync Cloud Run Job 신규 배포 + ingestion/server 코드 재배포 + 초기 마스터 백필 (~2,800 종목) + webapp E2E "삼성전자 검색 PASS" 회귀 검증.

Purpose: D8/D10/INFR-02. CONTEXT 의 "삼성전자 검색 불가" (STATE.md:97) 발견 사유 실제 해결.

Output: 새 Job/Scheduler 가동, 마스터 백필 완료, ingestion/server 새 코드 재배포, E2E 회귀 GREEN, DEPLOY-LOG 보존.

`autonomous: false` — gcloud 자격 + KRX_AUTH_KEY + .env.deploy 필요. 마지막에 사용자 검증 checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.1-stock-master-universe/06.1-CONTEXT.md
@.planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md
@.planning/phases/06.1-stock-master-universe/06.1-02-MIGRATION-VERIFY.md

# 미러 대상 (Phase 05.1)
@scripts/setup-ingestion-iam.sh
@scripts/deploy-ingestion.sh
@scripts/smoke-ingestion.sh
@scripts/deploy-server.sh

# 산출물 의존
@workers/master-sync/Dockerfile
@workers/ingestion/Dockerfile

# 회귀 기준 (변경 최소화)
@webapp/e2e/specs/search.spec.ts
@webapp/e2e/specs/stock-detail.spec.ts
@webapp/e2e/specs/a11y.spec.ts
@webapp/e2e/fixtures/mock-api.ts
@webapp/e2e/fixtures/stocks.ts

<interfaces>
신규 GCP 리소스:
- Secret: gh-radar-krx-auth-key
- Cloud Run Job: gh-radar-master-sync (region asia-northeast3)
- Scheduler: gh-radar-master-sync-scheduler (cron "10 8 * * 1-5" tz Asia/Seoul)
- SA 재사용:
  - runtime: gh-radar-ingestion-sa (Supabase + KRX secret accessor)
  - invoker: gh-radar-scheduler-sa (Job 리소스에 roles/run.invoker 바인딩)

server 재배포 변경: KIS_APP_KEY/SECRET 주입 신규 (Plan 04). deploy-server.sh 에 secret 추가.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: master-sync 배포 스크립트 3종 작성 (Phase 05.1 미러 + KRX Secret)</name>
  <files>
    scripts/setup-master-sync-iam.sh,
    scripts/deploy-master-sync.sh,
    scripts/smoke-master-sync.sh
  </files>

  <read_first>
    - scripts/setup-ingestion-iam.sh (정확 미러 대상)
    - scripts/deploy-ingestion.sh (정확 미러 대상)
    - scripts/smoke-ingestion.sh (정확 미러 대상)
    - .planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md §"Pattern 3" §"Pitfall 2" §"Don't Hand-Roll" Scheduler OAuth
  </read_first>

  <action>
세 스크립트 모두 ingestion 카운터파트를 그대로 복사 + 다음 차이만 적용.

### scripts/setup-master-sync-iam.sh
- Section 2 필수 env: `KRX_AUTH_KEY` 만 신규 추가 (KIS/SUPABASE 는 기존 setup 에서 등록됨; 본 스크립트는 idempotent 가드만).
- Section 6: `create_or_update_secret gh-radar-krx-auth-key "$KRX_AUTH_KEY"` 만 추가. 다른 4개 secret 분기는 "이미 존재" 분기로 통과.
- Section 7: `gcloud secrets add-iam-policy-binding gh-radar-krx-auth-key --member="serviceAccount:${INGESTION_SA}" --role=roles/secretmanager.secretAccessor` 만 신규.
- 마지막: `Next: bash scripts/deploy-master-sync.sh`.

### scripts/deploy-master-sync.sh
ingestion 미러, 다음만 변경:
- 변수: `JOB=gh-radar-master-sync`, IMAGE 경로 `master-sync:${SHA}`, Dockerfile 경로 `workers/master-sync/Dockerfile`.
- `gcloud run jobs deploy` env/secrets:
  ```
  --task-timeout=300s \
  --max-retries=1 \
  --set-env-vars="^@^SUPABASE_URL=${SUPABASE_URL}@KRX_BASE_URL=https://data-dbg.krx.co.kr/svc/apis@LOG_LEVEL=info@APP_VERSION=${SHA}" \
  --set-secrets="KRX_AUTH_KEY=gh-radar-krx-auth-key:latest,SUPABASE_SERVICE_ROLE_KEY=gh-radar-supabase-service-role:latest"
  ```
- Section 5.5 invoker 바인딩: JOB 변수만 master-sync 로 교체 (invoker SA 는 동일).
- Section 6 Scheduler:
  - 이름: `gh-radar-master-sync-scheduler`
  - schedule: `"10 8 * * 1-5"` (KRX 데이터 갱신 08:00 + 안전 10분)
  - tz: `Asia/Seoul`
  - 인증: `--oauth-service-account-email="$SCHED_SA"` (OIDC 금지)
- Section 7: `bash "$(dirname "$0")/smoke-master-sync.sh" "$JOB" "$REGION"`.

### scripts/smoke-master-sync.sh
smoke-ingestion 미러, invariant 변경:

INV-1: `gcloud run jobs execute "$JOB" --region="$REGION" --wait` exit 0 (백필 겸 첫 실행).

INV-2: 최근 5분 로그에 `master-sync cycle complete` 1건 이상.

INV-3: 최근 5분 로그에 `master-sync failed` OR `401` 0건.

INV-4: Supabase stocks count >= 2500 — `curl -I` Content-Range 헤더 파싱 (`Prefer: count=exact`, `Range: 0-0`).

INV-5: Scheduler ENABLED + schedule == `"10 8 * * 1-5"`.

INV-6: 005930 종목이 stocks 마스터에 존재 + name 에 "삼성전자" 포함.

마지막에 PASS/FAIL 합산 + exit code.

세 스크립트 모두 `chmod +x` + `bash -n` syntax check 통과.
  </action>

  <verify>
    <automated>bash -n scripts/setup-master-sync-iam.sh && bash -n scripts/deploy-master-sync.sh && bash -n scripts/smoke-master-sync.sh</automated>
  </verify>

  <acceptance_criteria>
    - 세 파일 모두 존재 + 실행 권한 (`test -x scripts/deploy-master-sync.sh`)
    - `bash -n` syntax check 3개 모두 exit 0
    - `grep "gh-radar-master-sync" scripts/deploy-master-sync.sh` 매치 (JOB 이름)
    - `grep "workers/master-sync/Dockerfile" scripts/deploy-master-sync.sh` 매치
    - `grep "gh-radar-krx-auth-key" scripts/setup-master-sync-iam.sh` 매치
    - `grep "10 8 \\* \\* 1-5" scripts/deploy-master-sync.sh` 매치 (schedule)
    - `grep "oauth-service-account-email" scripts/deploy-master-sync.sh` 매치 (OIDC 금지)
    - `grep "stocks count\\|count=exact" scripts/smoke-master-sync.sh` 매치 (INV-4)
    - `grep "005930" scripts/smoke-master-sync.sh` 매치 (INV-6)
  </acceptance_criteria>

  <done>3개 배포 스크립트 작성 완료, syntax OK.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/deploy-server.sh 에 KIS Secret 주입 추가 (Plan 04 server 재배포 준비)</name>
  <files>scripts/deploy-server.sh</files>

  <read_first>
    - scripts/deploy-server.sh (현재 — Phase 2 산출, KIS secret 미주입)
    - scripts/deploy-ingestion.sh (참조 — KIS secret 주입 패턴)
    - server/src/config.ts (Plan 04 — KIS_APP_KEY/SECRET 필수 env 로 추가됨)
  </read_first>

  <action>
1. `scripts/deploy-server.sh` 의 `gcloud run deploy` 또는 `gcloud run services update` 호출에서 `--set-secrets=` 부분을 찾아 다음 값 추가:
```
--set-secrets="...,KIS_APP_KEY=gh-radar-kis-app-key:latest,KIS_APP_SECRET=gh-radar-kis-app-secret:latest"
```
기존 secret 들 (SUPABASE_SERVICE_ROLE_KEY 등) 보존 — append.

2. `--set-env-vars` 에 KIS_BASE_URL 추가 (default 와 동일하지만 명시):
```
KIS_BASE_URL=https://openapi.koreainvestment.com:9443
```

3. server 의 Cloud Run runtime SA 가 KIS secret accessor 권한이 있는지 확인:
```bash
# gh-radar-server 의 SA (현재 deploy-server.sh 에 정의된 이름 확인) 에
# gh-radar-kis-app-key, gh-radar-kis-app-secret 두 secret 의 secretAccessor 바인딩 추가.
# 만약 server 가 ingestion 과 다른 SA 라면 setup 에 추가.
```
deploy-server.sh 의 SA 변수를 grep 으로 찾고, 만약 별도 SA 면 다음 명령 1회 수동 실행 (또는 setup-ingestion-iam.sh 패턴으로 별도 setup-server-iam.sh 추가):
```bash
SERVER_SA="<server runtime SA>"
for SECRET in gh-radar-kis-app-key gh-radar-kis-app-secret; do
  gcloud secrets add-iam-policy-binding "$SECRET" \
    --member="serviceAccount:${SERVER_SA}" \
    --role=roles/secretmanager.secretAccessor
done
```
만약 기존 deploy-server.sh 가 default Compute SA 를 사용한다면 한 번에 바인딩 (가장 단순 경로).

4. `bash -n scripts/deploy-server.sh` syntax 통과.

(주: 본 task 는 스크립트 수정만. 실제 server 재배포 실행은 Task 4.)
  </action>

  <verify>
    <automated>bash -n scripts/deploy-server.sh && grep "KIS_APP_KEY=gh-radar-kis-app-key" scripts/deploy-server.sh && grep "KIS_APP_SECRET=gh-radar-kis-app-secret" scripts/deploy-server.sh</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n scripts/deploy-server.sh` exit 0
    - `grep "KIS_APP_KEY=gh-radar-kis-app-key:latest" scripts/deploy-server.sh` 매치
    - `grep "KIS_APP_SECRET=gh-radar-kis-app-secret:latest" scripts/deploy-server.sh` 매치
    - `grep "KIS_BASE_URL" scripts/deploy-server.sh` 매치
  </acceptance_criteria>

  <done>deploy-server.sh 가 KIS secret 주입 포함하도록 수정. 실제 배포는 Task 4.</done>
</task>

<task type="auto">
  <name>Task 3: webapp E2E 회귀 시나리오 추가 (삼성전자 검색 PASS) + 기존 9종 호환 유지</name>
  <files>
    webapp/e2e/specs/search.spec.ts,
    webapp/e2e/fixtures/mock-api.ts,
    webapp/e2e/fixtures/stocks.ts
  </files>

  <read_first>
    - webapp/e2e/specs/search.spec.ts (현재 3개 시나리오)
    - webapp/e2e/fixtures/mock-api.ts (현재 — searchResults 옵션 단일)
    - webapp/e2e/fixtures/stocks.ts (현재 FIXTURE_SAMSUNG)
    - .planning/phases/06.1-stock-master-universe/06.1-CONTEXT.md §"Specifics" (삼성전자 회귀 시나리오)
  </read_first>

  <action>
1. **webapp/e2e/fixtures/stocks.ts** — 다중 종목 픽스처 추가 (기존 FIXTURE_SAMSUNG 보존):
```typescript
export const FIXTURE_SK_HYNIX = {
  ...FIXTURE_SAMSUNG,
  code: '000660', name: 'SK하이닉스',
  price: 195000, changeAmount: 2500, changeRate: 1.30,
};
export const FIXTURE_KAKAO = {
  ...FIXTURE_SAMSUNG,
  code: '035720', name: '카카오',
  price: 55000, changeAmount: 500, changeRate: 0.91,
};
export const FIXTURE_MASTER_UNIVERSE = [FIXTURE_SAMSUNG, FIXTURE_SK_HYNIX, FIXTURE_KAKAO];
```

2. **webapp/e2e/fixtures/mock-api.ts** — 변경 없음 (기존 시그니처가 이미 다종목 배열 수용 — `searchResults: Stock[]`).

3. **webapp/e2e/specs/search.spec.ts** — 신규 시나리오 추가 (기존 3개 보존):
```typescript
import { FIXTURE_MASTER_UNIVERSE } from '../fixtures/stocks';

test.describe('Phase 06.1 — 마스터 universe 회귀 (SRCH-01)', () => {
  test.beforeEach(async ({ page }) => {
    // 마스터 universe 모킹 — 서버가 q 별 필터링한 결과를 반환한다고 가정 (mock-api 가 q 무관 전체 반환)
    await mockStockApi(page, { searchResults: FIXTURE_MASTER_UNIVERSE });
  });

  test('"삼성전자" 입력 → 자동완성에 005930 노출 (회귀: STATE.md:97 사유)', async ({ page }) => {
    await page.goto('/scanner');
    await page.getByLabel('종목 검색 열기').first().click();
    await expect(page.getByRole('dialog')).toBeVisible();
    const input = page.getByRole('dialog').getByPlaceholder('종목명 또는 종목코드를 입력하세요');
    await input.fill('삼성전자');
    const option = page.getByRole('option', { name: /삼성전자/ });
    await expect(option).toBeVisible({ timeout: 3000 });
  });

  test('"005930" 코드 직접 입력 → 005930 매치', async ({ page }) => {
    await page.goto('/scanner');
    await page.getByLabel('종목 검색 열기').first().click();
    const input = page.getByRole('dialog').getByPlaceholder('종목명 또는 종목코드를 입력하세요');
    await input.fill('005930');
    const option = page.getByRole('option', { name: /005930|삼성전자/ });
    await expect(option).toBeVisible({ timeout: 3000 });
  });
});
```

4. webapp 로컬 빌드 + (옵션) playwright 시나리오 발견:
```bash
pnpm -F webapp typecheck
pnpm -F webapp exec playwright test --list 2>&1 | grep "삼성전자"
```
  </action>

  <verify>
    <automated>pnpm -F webapp typecheck && pnpm -F webapp exec playwright test --list 2>&1 | grep -E "삼성전자|005930" | head -5</automated>
  </verify>

  <acceptance_criteria>
    - `grep "FIXTURE_SK_HYNIX\\|FIXTURE_KAKAO\\|FIXTURE_MASTER_UNIVERSE" webapp/e2e/fixtures/stocks.ts | wc -l` ≥ 3
    - `grep "삼성전자" webapp/e2e/specs/search.spec.ts | wc -l` ≥ 2 (기존 1개 + 신규 1개+)
    - `grep "STATE.md:97\\|마스터 universe" webapp/e2e/specs/search.spec.ts` 매치 (회귀 사유 명시)
    - `pnpm -F webapp typecheck` exit 0
    - playwright `--list` 출력에 신규 "삼성전자" 시나리오 발견
    - 기존 9종 시나리오 (smoke/search 3 + stock-detail 3 + a11y 3) 변경 없음 (`git diff --stat webapp/e2e/specs/{smoke,stock-detail,a11y}.spec.ts` 빈 결과)
  </acceptance_criteria>

  <done>마스터 universe 회귀 E2E 시나리오 2개 추가. 기존 9종 무회귀.</done>
</task>

<task type="auto">
  <name>Task 4: production 배포 — IAM/master-sync/ingestion/server 순차 적용 + smoke + 백필 검증</name>
  <files>.planning/phases/06.1-stock-master-universe/06.1-06-DEPLOY-LOG.md</files>

  <read_first>
    - scripts/setup-master-sync-iam.sh (Task 1)
    - scripts/deploy-master-sync.sh (Task 1)
    - scripts/smoke-master-sync.sh (Task 1)
    - scripts/deploy-server.sh (Task 2 수정)
    - scripts/deploy-ingestion.sh (기존 — 코드 변경분 자동 빌드)
    - .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/ DEPLOY-LOG 패턴
    - .planning/phases/06.1-stock-master-universe/06.1-02-MIGRATION-VERIFY.md (Plan 02 완료 확인)
  </read_first>

  <action>
**[BLOCKING] 본 task 는 production 배포. 실패 시 STOP + 사용자 보고.**

순서:

### Step 1: KRX Secret + IAM
```bash
source .env.deploy  # GCP_PROJECT_ID, KRX_AUTH_KEY, SUPABASE_SERVICE_ROLE_KEY 등
bash scripts/setup-master-sync-iam.sh
```
출력 캡처.

### Step 2: master-sync 배포 + smoke (포함된 INV-1 이 백필 1회 실행)
```bash
bash scripts/deploy-master-sync.sh 2>&1 | tee /tmp/master-sync-deploy.log
```
deploy-master-sync 가 끝에 smoke 자동 호출. INV-1~6 모두 PASS 확인. INV-4 의 stocks count >= 2500 가 마스터 백필 검증.

만약 INV 가 FAIL 하면 STOP.

### Step 3: ingestion 재배포 (Plan 05 코드)
```bash
bash scripts/deploy-ingestion.sh 2>&1 | tee /tmp/ingestion-redeploy.log
```
기존 smoke (INV-1~5) 통과 확인. ingestion 새 코드가 stock_quotes + top_movers 양쪽에 쓰는지 INV-2 의 "cycle complete" 와 stocks 갱신 시각 (Phase 05.2 INV-4) 으로 확인. 단 본 phase 의 INV-4 는 stocks (마스터) 가 아닌 stock_quotes 갱신을 봐야 정확 — 기존 smoke 는 stocks 를 봄. **이 mismatch 를 DEPLOY-LOG 에 명시**하고, 정확 검증을 위해 추가 수동 query:
```bash
curl -fsS "${SUPABASE_URL}/rest/v1/stock_quotes?select=updated_at&order=updated_at.desc&limit=1" \
  -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY"
```
가 최근 60s 이내 (장 시간) 또는 직전 영업일 종료 시각인지 확인.

### Step 4: server 재배포 (Plan 04 코드)
```bash
bash scripts/deploy-server.sh 2>&1 | tee /tmp/server-redeploy.log
```
KIS_APP_KEY/SECRET 주입 확인. 배포 후:
```bash
SERVER_URL=$(gcloud run services describe gh-radar-server --region=asia-northeast3 --format='value(status.url)')
curl -fsS "$SERVER_URL/api/health" | jq .
curl -fsS "$SERVER_URL/api/stocks/search?q=$(echo -n 삼성전자 | jq -sRr @uri)" | jq '. | length, .[0]'
curl -fsS "$SERVER_URL/api/stocks/005930" | jq .
curl -fsSI "$SERVER_URL/api/scanner" | grep -i "x-last-updated-at"
```
4개 모두 정상 응답:
- /api/health: 200
- /api/stocks/search?q=삼성전자: 1개 이상 결과 (code=005930 포함)
- /api/stocks/005930: 200 + 시세 (on-demand inquirePrice 성공) 또는 cached
- /api/scanner: X-Last-Updated-At 헤더 존재

### Step 5: webapp E2E 회귀 (로컬 — production server URL 대상)
```bash
NEXT_PUBLIC_API_BASE_URL="$SERVER_URL" pnpm -F webapp exec playwright test
```
9 (기존) + 2 (Task 3 신규) = 11 시나리오 모두 GREEN.

(주: webapp Vercel 재배포는 코드 변경 없으므로 불필요 — 단 server URL 갱신이 필요 없으면 skip. CONTEXT 의 mock-api 는 e2e 시 항상 fulfill 하므로 production server 호출 없이도 PASS.)

### Step 6: DEPLOY-LOG 작성

`.planning/phases/06.1-stock-master-universe/06.1-06-DEPLOY-LOG.md`:

```markdown
# Phase 06.1 Plan 06 — Deploy Log

**Date:** YYYY-MM-DD HH:MM KST
**Operator:** [git user]
**Migration prereq:** Plan 02 완료 (06.1-02-MIGRATION-VERIFY.md)

## Step 1 — IAM/Secret
- setup-master-sync-iam.sh exit 0
- New secret: gh-radar-krx-auth-key (version 1)
- Bound: gh-radar-ingestion-sa → secretAccessor

## Step 2 — master-sync 배포 + 백필
- Image: asia-northeast3-docker.pkg.dev/<project>/gh-radar/master-sync:<sha>
- Job: gh-radar-master-sync (region asia-northeast3)
- Scheduler: gh-radar-master-sync-scheduler (cron "10 8 * * 1-5" Asia/Seoul) — ENABLED
- Smoke: INV-1~6 [PASS/FAIL 결과]
- Stocks count after backfill: <number>
- Sample query: 005930 = 삼성전자 ✓

## Step 3 — ingestion 재배포
- Image: <new sha>
- Smoke: INV-1~5 [결과]
- stock_quotes latest updated_at: <timestamp>

## Step 4 — server 재배포
- Image: <new sha>
- New env: KIS_APP_KEY/SECRET injected
- /api/health: 200
- /api/stocks/search?q=삼성전자: <result count, first item summary>
- /api/stocks/005930: <on-demand 결과 or cached fallback>
- /api/scanner X-Last-Updated-At: <timestamp>

## Step 5 — webapp E2E
- Total: 11 (기존 9 + 신규 2)
- Pass: <N> / Fail: <N>
- Failed (if any): <시나리오 + 로그>

## Sign-off
- [x] master-sync 백필 PASS (>=2500 종목)
- [x] ingestion EGW00201 0건
- [x] server on-demand inquirePrice 동작
- [x] webapp E2E 11/11 GREEN
- [x] STATE.md 의 "삼성전자 검색 불가" 사유 해결
```
  </action>

  <verify>
    <automated>test -f .planning/phases/06.1-stock-master-universe/06.1-06-DEPLOY-LOG.md && grep -E "Stocks count after backfill: [0-9]{4,}" .planning/phases/06.1-stock-master-universe/06.1-06-DEPLOY-LOG.md && grep "005930.*삼성전자" .planning/phases/06.1-stock-master-universe/06.1-06-DEPLOY-LOG.md</automated>
  </verify>

  <acceptance_criteria>
    - `gcloud run jobs describe gh-radar-master-sync --region=asia-northeast3 --format='value(name)'` exit 0 (Job 존재)
    - `gcloud scheduler jobs describe gh-radar-master-sync-scheduler --location=asia-northeast3 --format='value(state)'` = `ENABLED`
    - `gcloud scheduler jobs describe gh-radar-master-sync-scheduler --location=asia-northeast3 --format='value(schedule)'` = `10 8 * * 1-5`
    - Supabase stocks count >= 2500 (curl Content-Range)
    - 005930 종목이 stocks 마스터에 존재 (curl name 필드에 "삼성전자" 포함)
    - `curl -fsS "$SERVER_URL/api/stocks/search?q=$(echo -n 삼성전자 | jq -sRr @uri)" | jq 'length'` >= 1
    - `curl -fsS "$SERVER_URL/api/stocks/005930" | jq -r '.code'` = `"005930"`
    - playwright 결과: 11/11 PASS (또는 명시적 한국어 사유로 11/11 GREEN, FAIL 시 fix 필요)
    - DEPLOY-LOG.md 에 모든 step 결과 + sign-off 체크 기록
  </acceptance_criteria>

  <done>마스터 백필 + ingestion + server 모두 production 적용. webapp E2E 회귀 GREEN. STATE.md 의 "삼성전자 검색 불가" 사유 해결.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: 사용자 production 검증 — production URL 에서 "삼성전자" 검색 직접 확인</name>
  <files>(checkpoint — 사용자 수동 검증, 파일 수정 없음)</files>
  <action>사용자에게 production webapp URL 의 "삼성전자" 검색 회귀 검증을 요청한다. how-to-verify 단계를 그대로 안내하고, resume-signal 입력을 대기한다. 이슈 발견 시 사용자 보고를 근거로 fix-plan 을 새로 작성한다.</action>
  <verify><automated>echo "checkpoint: 사용자 수동 검증 대기 — 자동 검증 없음"</automated></verify>
  <done>사용자가 "approved" 입력 시 phase 완료 처리.</done>
  <what-built>
    Phase 06.1 production 배포 완료:
    - master-sync Cloud Run Job + 일 1회 Scheduler
    - 마스터 백필 (~2,800 종목)
    - ingestion 재배포 (stock_quotes + top_movers 분리 쓰기)
    - server 재배포 (마스터 universe + on-demand inquirePrice)
    - webapp E2E 11/11 GREEN
  </what-built>

  <how-to-verify>
1. https://gh-radar-webapp.vercel.app 접속
2. ⌘K 또는 헤더 "검색" 버튼 클릭 → 검색 Dialog 오픈
3. **"삼성전자"** 입력 → 자동완성에 005930 삼성전자 노출 ✓ (CONTEXT 의 회귀 사유 해결)
4. 다른 종목 시도 — 예: "SK하이닉스", "카카오", "셀트리온" → 모두 자동완성 노출
5. 005930 클릭 → /stocks/005930 진입 → 시세 (현재가/등락률/거래대금/시총) 표시
   - 시세가 0/em-dash 면 inquirePrice 실패 가능 → DEPLOY-LOG 의 server 로그 확인 후 사용자 보고
6. 임의 코드 진입 — 예: /stocks/999999 (마스터에 없음) → 404 페이지 (Phase 6 not-found.tsx)
7. /scanner 페이지 → 등락률 종목 표시 + 갱신시각 표시 (장 시간이면 최근 1분 이내)
  </how-to-verify>

  <resume-signal>
    "approved" 입력 시 phase 완료 처리 + STATE.md/ROADMAP.md 갱신.
    이슈 발견 시 자세히 보고 — 어떤 종목 검색이 안 되는지, 어떤 페이지에서 에러 나는지, 콘솔/네트워크 탭 메시지.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 .env.deploy → setup/deploy 스크립트 | KRX_AUTH_KEY 등 시크릿 환경변수 노출 위험 |
| Cloud Scheduler → Cloud Run Job (OAuth) | 외부 트리거 — 비인가 호출 차단 필요 |
| 새 Job runtime SA → Supabase 마스터 쓰기 | service_role bypass RLS — 잘못된 코드면 마스터 손상 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-06-01 | INFORMATION DISCLOSURE | KRX_AUTH_KEY env | mitigate | setup 스크립트가 `printf '%s' "$value" \| gcloud secrets versions add --data-file=-` 로 stdin 주입 — 프로세스 리스트 노출 차단 (setup-ingestion-iam 패턴 그대로). DEPLOY-LOG 에 KRX_AUTH_KEY 값 paste 절대 금지 — 시크릿 이름만 기록. |
| T-06.1-06-02 | SPOOFING | Scheduler → Job 호출 | mitigate | Phase 05.1 Pitfall 2 패턴 — `--oauth-service-account-email` 전용 (OIDC 금지). invoker SA 가 `roles/run.invoker` 를 Job 리소스에만 바인딩 (프로젝트 단위 금지). |
| T-06.1-06-03 | TAMPERING (마스터 손상) | master-sync runtime SA | mitigate | runtime SA 가 service_role 키로 쓰지만 Plan 03 의 upsertMasters 가 정해진 컬럼만 사용. dedup by code. RLS 측면에서 anon 은 SELECT 만 — 비인가 쓰기는 불가. |
| T-06.1-06-04 | INFORMATION DISCLOSURE | DEPLOY-LOG.md commit | mitigate | DEPLOY-LOG 에는 종목 코드/이름 + 카운트 + URL 만 기록. 시크릿 토큰/세션 쿠키 paste 금지. PR 리뷰 시 grep 로 검증. |
| T-06.1-06-05 | DENIAL OF SERVICE | E2E 회귀 시 production server 부하 | accept | mock-api fixture 가 모든 API 호출을 page.route 로 fulfill 하므로 production server 호출 거의 없음 (NEXT_PUBLIC_API_BASE_URL 만 설정). 본 phase E2E 11종은 production 부하 무시 가능. |
| T-06.1-06-06 | TAMPERING (배포 순서) | server 재배포 vs ingestion 재배포 | accept | server 재배포 (Plan 04) 는 새 스키마 코드 — 기존 stocks 단일 테이블로 실행 시 search 가 깨짐. 따라서 마이그레이션 (Plan 02) → master-sync 백필 → ingestion 재배포 → server 재배포 순서 엄수. Task 4 의 Step 1~4 가 이 순서를 강제. |

</threat_model>

<verification>
- `gcloud run jobs list --region=asia-northeast3` 에 `gh-radar-master-sync` + `gh-radar-ingestion` 둘 다 존재
- `gcloud scheduler jobs list --location=asia-northeast3` 에 두 scheduler 모두 ENABLED
- Supabase 에 stocks (~2,800), stock_quotes (~수십, ingestion 1 cycle 후), top_movers (~수십) 모두 존재
- production webapp 에서 "삼성전자" 검색 PASS (사용자 검증 task 5)
- DEPLOY-LOG.md 가 git 에 commit 됨
</verification>

<success_criteria>
- 신규 master-sync Job + Scheduler 가동
- 마스터 백필 완료 (>= 2500 종목)
- ingestion + server 새 코드로 재배포
- webapp E2E 11/11 GREEN
- 사용자 production "삼성전자" 검색 회귀 PASS
- DEPLOY-LOG.md 에 모든 결과 보존
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-stock-master-universe/06.1-06-SUMMARY.md`
</output>
