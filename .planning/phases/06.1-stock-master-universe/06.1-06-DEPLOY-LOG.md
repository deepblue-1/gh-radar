# Phase 06.1 Plan 06 -- Deploy Log

**Date:** 2026-04-16 11:45~12:00 KST
**Operator:** deepblue-1
**Migration prereq:** Plan 02 완료 (06.1-02-MIGRATION-VERIFY.md, supabase db push 성공)

## Step 1 -- IAM/Secret

- `setup-master-sync-iam.sh` exit 0
- New secret: `gh-radar-krx-auth-key` (version 1)
- Bound: `gh-radar-ingestion-sa` -> `secretAccessor` on `gh-radar-krx-auth-key`
- 기존 4개 secret (kis-app-key/secret, kis-account-number, supabase-service-role) 재사용 확인

## Step 2 -- master-sync 배포 + 백필

- Image: `asia-northeast3-docker.pkg.dev/gh-radar/gh-radar/master-sync:22f783f-fix1`
- Job: `gh-radar-master-sync` (region `asia-northeast3`)
- Scheduler: `gh-radar-master-sync-scheduler` (cron `10 8 * * 1-5` Asia/Seoul) -- ENABLED
- **Dockerfile 수정 (deviation):** `.gitignore`에 `dist/` 포함으로 `pnpm deploy`가 dist 미포함 -> `RUN cp -r ... /out/dist` 추가
- **parseBigint 수정 (deviation):** KRX PARVAL에 소수값("0.25") 포함 -> `Math.trunc()` 적용하여 bigint 호환
- **BAS_DD 보정:** 오늘(20260416) KRX 데이터 미갱신 -> BAS_DD=20260415 로 초기 백필 실행 -> 성공 후 BAS_DD 환경변수 제거
- Smoke INV-1~6:
  - INV-1 jobs execute --wait exit 0: **PASS**
  - INV-2 logs "master-sync cycle complete": **PASS** (count=2771, delistedCount=0)
  - INV-3 no failed/401: **PASS**
  - INV-4 Supabase stocks count >= 2500: **PASS** (2771)
  - INV-5 scheduler ENABLED + schedule "10 8 * * 1-5": **PASS**
  - INV-6 005930 삼성전자 exists: **PASS**
- **Stocks count after backfill: 2771**
- Sample query: 005930 = 삼성전자 (market=KOSPI, security_type=보통주)

## Step 3 -- ingestion 재배포

- Image: `asia-northeast3-docker.pkg.dev/gh-radar/gh-radar/ingestion:22f783f`
- Smoke INV-1~5:
  - INV-1 jobs execute --wait exit 0: **PASS**
  - INV-2 logs "cycle complete": **PASS** (quotesCount=57, moversCount=57)
  - INV-3 no cycle failed / EGW00201: **PASS**
  - INV-4 Supabase stocks freshness: **PASS**
  - INV-5 scheduler ENABLED: initially FAIL (PAUSED) -> Step 3.5에서 resume
- **Note:** INV-2 초기 FAIL은 로그 freshness 타이밍 이슈 -- 실제 "cycle complete" 정상 확인

## Step 3.5 -- ingestion Scheduler resume

- `gcloud scheduler jobs resume gh-radar-ingestion-scheduler --location=asia-northeast3`
- State: **ENABLED** (Plan 02 Task 2 Step 0 이후 최초 resume)

## Step 4 -- server 재배포

- Image: `asia-northeast3-docker.pkg.dev/gh-radar/gh-radar/server:22f783f`
- New env: KIS_APP_KEY/KIS_APP_SECRET injected via Secret Manager
- KIS secret accessor: default compute SA -> gh-radar-kis-app-key, gh-radar-kis-app-secret
- Server smoke: 8/9 PASS (INV-6 CORS preflight 1건 FAIL -- 기존 이슈)
- API 검증 (rate limit 해제 후):
  - `/api/health`: 200 `{"status":"ok","version":"22f783f"}`
  - `/api/stocks/search?q=삼성전자`: 2건 (code=005930 삼성전자 포함)
  - `/api/stocks/005930`: price=217000, changeRate=2.84 (on-demand inquirePrice 성공)
  - `/api/scanner`: `X-Last-Updated-At: 2026-04-16T02:58:32.646Z`

## Step 5 -- webapp E2E (로컬 mock-based)

- Total: **12** (기존 10 + 신규 2)
- Pass: **12** / Fail: **0**
- 모든 시나리오 GREEN

**WARN #8 명시:** E2E는 mock-api fixture 기반. production server 실제 검증은 Step 4의 curl 4건이 담당.

## Sign-off

- [x] master-sync 백필 PASS (2771 종목 >= 2500)
- [x] ingestion EGW00201 0건
- [x] server on-demand inquirePrice 동작 (005930 price=217000)
- [x] webapp E2E 12/12 GREEN
- [x] Cloud Scheduler 모두 ENABLED (ingestion + master-sync)
- [x] STATE.md의 "삼성전자 검색 불가" 사유 해결
