---
phase: 06.1-stock-master-universe
plan: 05
type: execute
wave: 2
depends_on:
  - 06.1-01
  - 06.1-02
files_modified:
  - server/src/routes/scanner.ts
  - server/src/mappers/scanner.ts
  - server/tests/routes/scanner.test.ts
  - server/tests/mappers/scanner.test.ts
  - workers/ingestion/src/pipeline/upsert.ts
  - workers/ingestion/src/pipeline/run.ts
  - workers/ingestion/src/pipeline/map.ts
  - workers/ingestion/tests/pipeline/upsert.test.ts
autonomous: true
requirements:
  - SCAN-01
  - SCAN-02
  - SCAN-03
  - SCAN-04
  - SCAN-05
  - SCAN-06
  - SCAN-07
  - SCAN-08

must_haves:
  truths:
    - "/api/scanner 가 top_movers JOIN stocks JOIN stock_quotes 로 응답한다 (또는 동등한 다단계 쿼리)"
    - "응답 스키마는 기존 Stock + upperLimitProximity 호환 유지 — webapp scanner UI 회귀 없음"
    - "X-Last-Updated-At 헤더가 stock_quotes.updated_at MAX 기준으로 설정된다 (SCAN-08 회귀)"
    - "Cache-Control: no-store 헤더 유지"
    - "ingestion 파이프라인이 stock_quotes 와 top_movers 두 테이블에만 쓰기 (stocks 마스터는 절대 안 건드림 — Anti-Patterns)"
    - "ingestion 의 stale cleanup 은 top_movers 에만 적용 (stocks/stock_quotes 절대 건드리지 않음)"
    - "기존 server scanner 테스트 10종 + ingestion 테스트 GREEN"
  artifacts:
    - path: "server/src/routes/scanner.ts"
      provides: "스캐너 라우트 — 3-테이블 JOIN"
      contains: "from(\"top_movers\")"
    - path: "server/src/mappers/scanner.ts"
      provides: "scannerRowToStock — top_movers + stocks + stock_quotes 평탄화"
      contains: "export function scannerRowToStock"
    - path: "workers/ingestion/src/pipeline/upsert.ts"
      provides: "stock_quotes upsert + top_movers upsert (트랜잭션 분리)"
      contains: "stock_quotes"
  key_links:
    - from: "server/src/routes/scanner.ts"
      to: "top_movers, stocks, stock_quotes (3-table)"
      via: "PostgREST nested select 또는 다단계 in() 쿼리"
      pattern: "top_movers"
    - from: "workers/ingestion/src/pipeline/upsert.ts"
      to: "stock_quotes + top_movers"
      via: "supabase.from('stock_quotes').upsert + supabase.from('top_movers').upsert"
      pattern: "from\\(\"stock_quotes\"\\)\\.upsert"
---

<objective>
스캐너 데이터 경로 (server `/api/scanner` + workers/ingestion 파이프라인) 를 3-테이블 분리 구조로 전환. 응답 스키마와 헤더는 SCAN-01~08 회귀 회피.

Purpose: D5 (ingestion 분해 — stock_quotes + top_movers 양쪽 쓰기, stocks 절대 안 건드림), Anti-Patterns (마스터·ingestion 경쟁 upsert 방지), SCAN-08 (X-Last-Updated-At 서버 기준).

Output: scanner 라우트 + ingestion 파이프라인 양쪽 전환 + 회귀 테스트 GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.1-stock-master-universe/06.1-CONTEXT.md
@.planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md

# 수정 대상
@server/src/routes/scanner.ts
@server/src/mappers/stock.ts (read-only — Plan 04 가 업데이트, 본 plan 은 신규 mapper 분리)
@server/tests/routes/scanner.test.ts
@workers/ingestion/src/pipeline/upsert.ts
@workers/ingestion/src/pipeline/run.ts
@workers/ingestion/src/pipeline/map.ts

# 패턴 참조
@server/src/routes/stocks.ts (Plan 04 산출 — 다단계 쿼리 패턴)
@workers/ingestion/src/index.ts
@workers/ingestion/tests/

<interfaces>
<!-- Plan 02 산출 스키마 -->
-- top_movers: code PK, name, market, rank int, ranked_at timestamptz, scan_id uuid, updated_at
-- stocks (마스터): code PK, name, market, sector, security_type, listing_date, is_delisted
-- stock_quotes: code PK FK→stocks, price/change_amount/change_rate/volume/trade_amount/
--               open/high/low/market_cap/upper_limit/lower_limit/updated_at

<!-- 응답 스키마 (회귀 회피) -->
type ScannerResponse = (Stock & { upperLimitProximity: number })[];
// X-Last-Updated-At: ISO 8601 from MAX(stock_quotes.updated_at)
// Cache-Control: no-store

<!-- ingestion 의 새 책임 분리 (D5) -->
// 매 cycle:
//   1. KIS 등락률 순위 → ranking rows
//   2. 각 row 별 inquirePrice
//   3. stock_quotes upsert (시세 컬럼 11개)
//   4. top_movers upsert (rank, ranked_at, scan_id, name, market — 표시 캐시용)
//   5. stale cleanup: top_movers 의 ranked_at < cycleStart 만 DELETE
//   6. stocks (마스터) 는 절대 건드리지 않음 — master-sync Job 만 쓰기
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: server scanner 라우트 + scanner mapper 분리 + 테스트 회귀 GREEN</name>
  <files>
    server/src/routes/scanner.ts,
    server/src/mappers/scanner.ts,
    server/tests/routes/scanner.test.ts,
    server/tests/mappers/scanner.test.ts
  </files>

  <read_first>
    - server/src/routes/scanner.ts (현재 — stocks 단일 테이블 가정, COLS export)
    - server/src/routes/stocks.ts (Plan 04 산출 — 다단계 쿼리 패턴)
    - server/src/mappers/stock.ts (Plan 04 — rowToStock 보존 + mergeMasterAndQuote 추가)
    - server/tests/routes/scanner.test.ts (현재 10개 테스트 — 회귀 기준)
    - server/tests/fixtures/supabase-mock.ts (Plan 04 강화본 — masters/quotes/topMovers 분기)
    - server/tests/fixtures/stocks.ts (Plan 01 + 04 — masters/quotes 픽스처)
    - .planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md §"Code Examples" Scanner JOIN
  </read_first>

  <behavior>
    - Test (회귀): 200 + 전체 종목 반환, upperLimitProximity 포함 (3개)
    - Test (회귀): market=KOSPI → KOSPI 만
    - Test (회귀): minRate=1.0 → changeRate>=1.0 만
    - Test (회귀): sort=volume_desc → volume 내림차순
    - Test (회귀): limit=1
    - Test (회귀): Cache-Control: no-store
    - Test (회귀): invalid market → 400
    - Test (회귀): invalid limit → 400
    - Test (회귀): limit > 10000 → 400
    - Test (회귀): market+minRate AND
    - Test (SCAN-08 회귀): X-Last-Updated-At = MAX(stock_quotes.updated_at)
    - Test (신규): top_movers 비어있음 → 200 + 빈 배열 + 헤더 생략
    - Test (신규): top_movers 에 있지만 stock_quotes 없는 종목 → price=0 (em-dash)
  </behavior>

  <action>

### Step A: server/src/mappers/scanner.ts 신규 (stocks.ts mapper 와 분리)

```typescript
import type { Stock, Market } from "@gh-radar/shared";
import type { StockMasterRow, StockQuoteRow } from "./stock.js";

export type TopMoverRow = {
  code: string;
  name: string;
  market: string;
  rank: number | null;
  ranked_at: string;
  scan_id: string | null;
  updated_at: string;
};

export type ScannerScreenResponse = Stock & { upperLimitProximity: number };

// top_movers (rank 정보) + master (보조) + quote (시세) 평탄화
export function scannerRowToStock(
  mover: TopMoverRow,
  master: StockMasterRow | null,
  quote: StockQuoteRow | null,
): ScannerScreenResponse {
  const price = quote ? Number(quote.price) : 0;
  const upper = quote ? Number(quote.upper_limit) : 0;
  return {
    code: mover.code,
    name: master?.name ?? mover.name,
    market: (master?.market ?? mover.market) as Market,
    price,
    changeAmount: quote ? Number(quote.change_amount) : 0,
    changeRate: quote ? Number(quote.change_rate) : 0,
    volume: quote ? quote.volume : 0,
    tradeAmount: quote ? quote.trade_amount : 0,
    open: quote ? Number(quote.open ?? 0) : 0,
    high: quote ? Number(quote.high ?? 0) : 0,
    low: quote ? Number(quote.low ?? 0) : 0,
    marketCap: quote ? Number(quote.market_cap ?? 0) : 0,
    upperLimit: upper,
    lowerLimit: quote ? Number(quote.lower_limit) : 0,
    updatedAt: quote ? quote.updated_at : mover.updated_at,
    upperLimitProximity: upper > 0 ? price / upper : 0,
  };
}
```

### Step B: server/src/routes/scanner.ts 재작성 (3-테이블)

```typescript
import { Router, type Router as RouterT } from "express";
import type { SupabaseClient } from "@supabase/supabase-js";
import { ScannerQuery } from "../schemas/scanner.js";
import {
  scannerRowToStock,
  type TopMoverRow,
} from "../mappers/scanner.js";
import type { StockMasterRow, StockQuoteRow } from "../mappers/stock.js";
import { ApiError } from "../errors.js";

// 시세 컬럼 export — 다른 곳에서 import 하지 않으므로 unexport (Plan 04 가 stocks.ts 에서 dependency 끊음)
const QUOTE_COLS = "code,price,change_amount,change_rate,volume,trade_amount,open,high,low,market_cap,upper_limit,lower_limit,updated_at";
const MASTER_COLS = "code,name,market";  // scanner 는 name/market 만 필요
const MOVER_COLS = "code,name,market,rank,ranked_at,scan_id,updated_at";

export const scannerRouter: RouterT = Router();

scannerRouter.get("/", async (req, res, next) => {
  try {
    const parsed = ScannerQuery.safeParse(req.query);
    if (!parsed.success) {
      const issue = parsed.error.issues[0];
      throw new ApiError(400, "INVALID_QUERY_PARAM", `${issue.path.join(".")}: ${issue.message}`);
    }
    const { market, minRate, sort, limit } = parsed.data;
    const supabase = req.app.locals.supabase as SupabaseClient;

    // 1. top_movers 후보 fetch (랭킹 + 표시 캐시)
    let moverQ = supabase.from("top_movers").select(MOVER_COLS);
    if (market !== "ALL") moverQ = moverQ.eq("market", market);
    const { data: movers, error: moErr } = await moverQ;
    if (moErr) throw moErr;
    const moverRows = (movers ?? []) as unknown as TopMoverRow[];

    if (moverRows.length === 0) {
      res.setHeader("Cache-Control", "no-store");
      res.json([]);
      return;
    }

    // 2. stock_quotes IN (codes) — 시세
    const codes = moverRows.map((m) => m.code);
    const { data: quotes, error: qErr } = await supabase
      .from("stock_quotes")
      .select(QUOTE_COLS)
      .in("code", codes);
    if (qErr) throw qErr;
    const quoteByCode = new Map<string, StockQuoteRow>();
    for (const q of (quotes ?? []) as unknown as StockQuoteRow[]) {
      quoteByCode.set(q.code, q);
    }

    // 3. stocks (마스터) IN (codes) — name/market 캐노니컬 (top_movers 의 캐시보다 우선)
    const { data: masters, error: mErr } = await supabase
      .from("stocks")
      .select(MASTER_COLS)
      .in("code", codes);
    if (mErr) throw mErr;
    const masterByCode = new Map<string, StockMasterRow>();
    for (const m of (masters ?? []) as unknown as StockMasterRow[]) {
      masterByCode.set(m.code, m);
    }

    // 4. 평탄화 + 필터
    let merged = moverRows.map((mv) =>
      scannerRowToStock(mv, masterByCode.get(mv.code) ?? null, quoteByCode.get(mv.code) ?? null),
    );

    if (typeof minRate === "number") {
      merged = merged.filter((s) => s.changeRate >= minRate);
    }

    // 5. 정렬
    const sortMap = {
      rate_desc: (a: any, b: any) => b.changeRate - a.changeRate,
      rate_asc: (a: any, b: any) => a.changeRate - b.changeRate,
      volume_desc: (a: any, b: any) => b.volume - a.volume,
    } as const;
    merged.sort(sortMap[sort]);

    // 6. limit
    if (typeof limit === "number") merged = merged.slice(0, limit);

    // 7. X-Last-Updated-At — MAX(stock_quotes.updated_at) (SCAN-08 회귀)
    if (merged.length > 0) {
      const maxMs = Math.max(
        ...merged.map((s) => new Date(s.updatedAt).getTime()).filter(Number.isFinite),
      );
      if (Number.isFinite(maxMs) && maxMs > 0) {
        res.setHeader("X-Last-Updated-At", new Date(maxMs).toISOString());
      }
    }
    res.setHeader("Cache-Control", "no-store");
    res.json(merged);
  } catch (e) {
    next(e);
  }
});
```

(주: 기존 `export const COLS = "..."` 이 stocks.ts 에서 import 됐었지만 Plan 04 가 dependency 끊었음. 본 plan 의 scanner.ts 는 더 이상 COLS 를 export 하지 않음.)

### Step C: server/tests/routes/scanner.test.ts 회귀 + 신규 시나리오

```typescript
import { describe, it, expect } from "vitest";
import request from "supertest";
import { createApp } from "../../src/app";
import { mockSupabase } from "../fixtures/supabase-mock";
import { samsungMaster, masterOnly, samsungQuote } from "../fixtures/stocks";

// 회귀 기준선 — 기존 3 종목과 동일 의미
const samsungMover = {
  code: "005930", name: "삼성전자", market: "KOSPI",
  rank: 1, ranked_at: "2026-04-15T05:00:00Z", scan_id: null,
  updated_at: "2026-04-13T10:00:00Z",
};
const kakaoMover = {
  code: "035720", name: "카카오", market: "KOSPI",
  rank: 2, ranked_at: "2026-04-15T05:00:00Z", scan_id: null,
  updated_at: "2026-04-13T10:00:00Z",
};
const kosdaqMover = {
  code: "091990", name: "셀트리온헬스케어", market: "KOSDAQ",
  rank: 3, ranked_at: "2026-04-15T05:00:00Z", scan_id: null,
  updated_at: "2026-04-13T10:00:00Z",
};

const kakaoMaster = { ...samsungMaster, code: "035720", name: "카카오" };
const kosdaqMaster = { ...samsungMaster, code: "091990", name: "셀트리온헬스케어", market: "KOSDAQ" };

const kakaoQuote = {
  ...samsungQuote, code: "035720",
  price: "55000.00", change_amount: "500.00", change_rate: "0.9100",
  volume: 2345678, upper_limit: "71500.00", lower_limit: "38500.00",
  market_cap: 24000000000000,
};
const kosdaqQuote = {
  ...samsungQuote, code: "091990",
  price: "80000.00", change_amount: "-1000.00", change_rate: "-1.2300",
  volume: 987654, upper_limit: "104000.00", lower_limit: "56000.00",
  market_cap: 13000000000000,
  updated_at: "2026-04-13T10:05:00Z",  // 가장 최신
};

const fullState = () => ({
  topMovers: [samsungMover, kakaoMover, kosdaqMover],
  masters: [samsungMaster, kakaoMaster, kosdaqMaster],
  quotes: [samsungQuote, kakaoQuote, kosdaqQuote],
});

const app = (state = fullState()) => createApp({ supabase: mockSupabase(state) });

describe("/api/scanner (3-테이블 JOIN)", () => {
  it("200 + 전체 종목 + upperLimitProximity", async () => {
    const r = await request(app()).get("/api/scanner");
    expect(r.status).toBe(200);
    expect(r.body.length).toBe(3);
    const s = r.body.find((x: any) => x.code === "005930");
    expect(s.upperLimitProximity).toBeCloseTo(70000 / 91000);
  });

  it("market=KOSPI → KOSPI만", async () => {
    const r = await request(app()).get("/api/scanner?market=KOSPI");
    expect(r.body.every((s: any) => s.market === "KOSPI")).toBe(true);
    expect(r.body.length).toBe(2);
  });

  it("minRate=1.0 → changeRate>=1.0 만", async () => {
    const r = await request(app()).get("/api/scanner?minRate=1.0");
    expect(r.body.every((s: any) => s.changeRate >= 1.0)).toBe(true);
  });

  it("sort=volume_desc → volume 내림차순", async () => {
    const r = await request(app()).get("/api/scanner?sort=volume_desc");
    const vols = r.body.map((s: any) => s.volume);
    expect(vols).toEqual([...vols].sort((a, b) => b - a));
  });

  it("limit=1 → 1개", async () => {
    const r = await request(app()).get("/api/scanner?limit=1");
    expect(r.body.length).toBe(1);
  });

  it("Cache-Control: no-store", async () => {
    const r = await request(app()).get("/api/scanner");
    expect(r.headers["cache-control"]).toBe("no-store");
  });

  it("invalid market → 400", async () => {
    const r = await request(app()).get("/api/scanner?market=INVALID");
    expect(r.status).toBe(400);
  });

  it("invalid limit → 400", async () => {
    const r = await request(app()).get("/api/scanner?limit=abc");
    expect(r.status).toBe(400);
  });

  it("limit > 10000 → 400", async () => {
    const r = await request(app()).get("/api/scanner?limit=99999");
    expect(r.status).toBe(400);
  });

  it("market+minRate AND", async () => {
    const r = await request(app()).get("/api/scanner?market=KOSPI&minRate=1.0");
    expect(r.body.every((s: any) => s.market === "KOSPI" && s.changeRate >= 1.0)).toBe(true);
    expect(r.body.some((s: any) => s.code === "091990")).toBe(false);
  });

  it("X-Last-Updated-At = MAX(stock_quotes.updated_at) (SCAN-08)", async () => {
    const r = await request(app()).get("/api/scanner");
    expect(r.headers["x-last-updated-at"]).toBe("2026-04-13T10:05:00.000Z");
  });

  it("top_movers 비어있음 → 200 + 빈 배열, X-Last-Updated-At 헤더 생략", async () => {
    const r = await request(app({ topMovers: [], masters: [], quotes: [] })).get("/api/scanner");
    expect(r.status).toBe(200);
    expect(r.body).toEqual([]);
    expect(r.headers["x-last-updated-at"]).toBeUndefined();
  });

  it("stock_quotes 없는 종목 → price=0, upperLimitProximity=0", async () => {
    const noQuoteState = {
      topMovers: [samsungMover],
      masters: [samsungMaster],
      quotes: [],
    };
    const r = await request(app(noQuoteState)).get("/api/scanner");
    expect(r.body[0].price).toBe(0);
    expect(r.body[0].upperLimitProximity).toBe(0);
  });
});
```

### Step D: server/tests/mappers/scanner.test.ts 신규 단위 테스트

```typescript
import { describe, it, expect } from "vitest";
import { scannerRowToStock } from "../../src/mappers/scanner";

const mover = { code: "005930", name: "Mover Cache", market: "KOSPI",
  rank: 1, ranked_at: "2026-04-15T05:00:00Z", scan_id: null,
  updated_at: "2026-04-15T05:00:00Z" };
const master = { code: "005930", name: "삼성전자", market: "KOSPI",
  sector: null, security_type: "보통주", listing_date: null, is_delisted: false,
  updated_at: "2026-04-15T00:00:00Z" };
const quote = { code: "005930", price: "70000", change_amount: "1000", change_rate: "1.45",
  volume: 1234, trade_amount: 5678,
  open: "69000", high: "70500", low: "68500", market_cap: 999,
  upper_limit: "91000", lower_limit: "49000",
  updated_at: "2026-04-15T05:00:00Z" };

describe("scannerRowToStock", () => {
  it("master 우선 — name/market 은 stocks 마스터 캐노니컬 사용", () => {
    const r = scannerRowToStock(mover, master, quote);
    expect(r.name).toBe("삼성전자");  // master 우선
    expect(r.market).toBe("KOSPI");
  });

  it("master 누락 시 mover 의 name/market fallback", () => {
    const r = scannerRowToStock(mover, null, quote);
    expect(r.name).toBe("Mover Cache");
  });

  it("quote 누락 시 시세 0 + upperLimitProximity=0", () => {
    const r = scannerRowToStock(mover, master, null);
    expect(r.price).toBe(0);
    expect(r.upperLimitProximity).toBe(0);
    expect(r.updatedAt).toBe(mover.updated_at);
  });

  it("quote 존재 시 upperLimitProximity = price/upper_limit", () => {
    const r = scannerRowToStock(mover, master, quote);
    expect(r.upperLimitProximity).toBeCloseTo(70000 / 91000);
  });
});
```

### Step E: 실행
```bash
pnpm -F server typecheck
pnpm -F server test -- --run scanner
pnpm -F server test -- --run  # 전체 회귀 (search/stock-detail 도 PASS 유지)
```
  </action>

  <verify>
    <automated>pnpm -F server typecheck && pnpm -F server test -- --run --reporter=verbose 2>&1 | tail -50</automated>
  </verify>

  <acceptance_criteria>
    - `grep "from(\"top_movers\")" server/src/routes/scanner.ts` 매치
    - `grep "from(\"stock_quotes\")" server/src/routes/scanner.ts` 매치
    - `grep "from(\"stocks\")" server/src/routes/scanner.ts` 매치
    - `grep "X-Last-Updated-At" server/src/routes/scanner.ts` 매치
    - `grep "Cache-Control.*no-store" server/src/routes/scanner.ts` 매치
    - `grep "scannerRowToStock" server/src/routes/scanner.ts` 매치
    - `grep "scannerRowToStock\\|TopMoverRow" server/src/mappers/scanner.ts | wc -l` ≥ 2 (export)
    - `grep -E "^export const COLS" server/src/routes/scanner.ts` 결과 0건 (더 이상 export 하지 않음)
    - `pnpm -F server typecheck` exit 0
    - `pnpm -F server test -- --run scanner` exit 0 (12+ 테스트 GREEN)
    - `pnpm -F server test -- --run` 전체 exit 0 (search/stock-detail/scanner/health/mappers 회귀 없음)
  </acceptance_criteria>

  <done>scanner 라우트 3-테이블 JOIN 전환 + scanner mapper 분리. 회귀 + 신규 시나리오 모두 GREEN. SCAN-08 X-Last-Updated-At 보존.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ingestion 파이프라인 분해 (stock_quotes + top_movers 양쪽 쓰기, stocks 절대 안 건드림)</name>
  <files>
    workers/ingestion/src/pipeline/upsert.ts,
    workers/ingestion/src/pipeline/run.ts,
    workers/ingestion/src/pipeline/map.ts,
    workers/ingestion/tests/pipeline/upsert.test.ts
  </files>

  <read_first>
    - workers/ingestion/src/pipeline/upsert.ts (현재 — stocks 단일 테이블 upsert)
    - workers/ingestion/src/pipeline/run.ts (현재 — stale cleanup `from('stocks').delete().lt(...)`)
    - workers/ingestion/src/pipeline/map.ts (현재 — toStock)
    - workers/ingestion/tests/ (기존 테스트 패턴 확인)
    - .planning/phases/06.1-stock-master-universe/06.1-CONTEXT.md §"D5"
    - .planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md §"Anti-Patterns" (마스터·ingestion 경쟁 + stale cleanup top_movers 만)
    - .planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md §"State of the Art" Deprecated 항목
  </read_first>

  <behavior>
    - Test: upsertStockQuotes 가 stock_quotes 테이블에 시세 컬럼 11개만 upsert
    - Test: upsertTopMovers 가 top_movers 테이블에 (code, name, market, rank, ranked_at, scan_id, updated_at) 만 upsert
    - Test: ingestion 이 stocks 테이블에 절대 쓰지 않음 (supabase.from 호출이 'stocks' 인 것 0건)
    - Test: stale cleanup 이 top_movers 만 대상 (`from('top_movers').delete().lt('ranked_at', cycleStart)`)
    - Test: 빈 입력 → no-op
  </behavior>

  <action>

### Step A: workers/ingestion/src/pipeline/upsert.ts 재작성

```typescript
import type { SupabaseClient } from "@supabase/supabase-js";
import type { Stock } from "@gh-radar/shared";
import { logger } from "../logger";

// 시세 upsert (stock_quotes)
export async function upsertStockQuotes(
  supabase: SupabaseClient,
  stocks: Stock[],
): Promise<{ count: number }> {
  if (stocks.length === 0) return { count: 0 };

  const deduped = new Map<string, Stock>();
  for (const s of stocks) deduped.set(s.code, s);

  const rows = [...deduped.values()].map((s) => ({
    code: s.code,
    price: s.price,
    change_amount: s.changeAmount,
    change_rate: s.changeRate,
    volume: s.volume,
    trade_amount: s.tradeAmount,
    open: s.open,
    high: s.high,
    low: s.low,
    market_cap: s.marketCap,
    upper_limit: s.upperLimit,
    lower_limit: s.lowerLimit,
    updated_at: s.updatedAt,
  }));

  const { error } = await supabase
    .from("stock_quotes")
    .upsert(rows, { onConflict: "code" });

  if (error) {
    logger.error({ error }, "upsertStockQuotes failed");
    throw error;
  }
  return { count: rows.length };
}

// 랭킹 캐시 upsert (top_movers) — 시세 컬럼 없음
export async function upsertTopMovers(
  supabase: SupabaseClient,
  stocks: Stock[],
  scanId: string,
  rankedAt: string,
): Promise<{ count: number }> {
  if (stocks.length === 0) return { count: 0 };

  const deduped = new Map<string, Stock>();
  for (const s of stocks) deduped.set(s.code, s);

  // rank 는 changeRate 내림차순 (등락률 순위 = KIS API 가 이미 정렬해 보냄)
  const sorted = [...deduped.values()].sort((a, b) => b.changeRate - a.changeRate);

  const rows = sorted.map((s, i) => ({
    code: s.code,
    name: s.name,
    market: s.market,
    rank: i + 1,
    ranked_at: rankedAt,
    scan_id: scanId,
    updated_at: rankedAt,
  }));

  const { error } = await supabase
    .from("top_movers")
    .upsert(rows, { onConflict: "code" });

  if (error) {
    logger.error({ error }, "upsertTopMovers failed");
    throw error;
  }
  return { count: rows.length };
}

// 레거시 시그니처 — 호출자(run.ts) 가 본 plan 에서 분리 호출로 전환하지만,
// 외부에서 import 시 컴파일 실패 방지를 위해 deprecated alias 유지 X.
// 기존 export upsertStocks 는 삭제. (호출처는 run.ts 만 — 본 plan 에서 동시 수정)
```

### Step B: workers/ingestion/src/pipeline/run.ts 재작성

```typescript
import type { AxiosInstance } from "axios";
import type { SupabaseClient } from "@supabase/supabase-js";
import type { Stock } from "@gh-radar/shared";
import { fetchAllRanking } from "../kis/ranking";
import { fetchInquirePrice } from "../kis/inquirePrice";
import { toStock } from "./map";
import { upsertStockQuotes, upsertTopMovers } from "./upsert";
import { withRetry } from "../retry";
import { logger } from "../logger";
import { randomUUID } from "node:crypto";

export async function runPipeline(
  client: AxiosInstance,
  supabase: SupabaseClient,
): Promise<{ quotesCount: number; moversCount: number }> {
  const cycleStart = new Date().toISOString();
  const scanId = randomUUID();

  const rankings = await withRetry(() => fetchAllRanking(client), "fetchAllRanking");

  const allStocks: Stock[] = [];
  let priceFailCount = 0;

  for (const { market, rows } of rankings) {
    for (const row of rows) {
      let priceData;
      try {
        priceData = await withRetry(
          () => fetchInquirePrice(client, row.stck_shrn_iscd),
          `inquirePrice:${row.stck_shrn_iscd}`,
        );
      } catch (err) {
        priceFailCount += 1;
        logger.warn(
          { code: row.stck_shrn_iscd, error: (err as Error).message },
          "inquirePrice failed — tradeAmount/marketCap/upperLimit/lowerLimit 0으로 저장",
        );
      }
      allStocks.push(toStock(row, market, priceData));
    }
  }

  logger.info({ totalStocks: allStocks.length, priceFailCount, scanId }, "pipeline mapped");

  // D5 — 두 테이블에 분리 쓰기. stocks (마스터) 는 절대 안 건드림.
  const { count: quotesCount } = await upsertStockQuotes(supabase, allStocks);
  const { count: moversCount } = await upsertTopMovers(supabase, allStocks, scanId, cycleStart);

  // Stale cleanup — top_movers 만 대상.
  // 이번 cycle 에 upsert 되지 않은 (= 등락률 순위에서 밀려난) 종목 제거.
  // ⚠ 절대 stocks (마스터) / stock_quotes (영구 캐시) 는 건드리지 말 것.
  const { error: delErr, count: delCount } = await supabase
    .from("top_movers")
    .delete({ count: "exact" })
    .lt("ranked_at", cycleStart);

  if (delErr) {
    logger.warn({ err: delErr }, "top_movers stale cleanup 실패 — 다음 cycle 에서 재시도");
  } else {
    logger.info({ deleted: delCount ?? 0 }, "top_movers stale cleaned");
  }

  return { quotesCount, moversCount };
}
```

### Step C: workers/ingestion/src/pipeline/map.ts — 변경 없음 (toStock 그대로)

기존 `toStock` 시그니처 유지. Stock 타입은 그대로 사용 (시세 통합형 — upsert 시점에 stock_quotes/top_movers 양쪽으로 쪼개짐).

### Step D: workers/ingestion/tests/pipeline/upsert.test.ts 신규

```typescript
import { describe, it, expect, vi } from "vitest";
import { upsertStockQuotes, upsertTopMovers } from "../../src/pipeline/upsert";
import type { Stock } from "@gh-radar/shared";

const mk = (code: string, over: Partial<Stock> = {}): Stock => ({
  code, name: `name-${code}`, market: "KOSPI",
  price: 70000, changeAmount: 1000, changeRate: 1.5,
  volume: 1000, tradeAmount: 70_000_000,
  open: 69000, high: 70500, low: 68500,
  marketCap: 1_000_000, upperLimit: 91000, lowerLimit: 49000,
  updatedAt: "2026-04-15T05:00:00Z",
  ...over,
});

const mkSupa = () => {
  const upsertFn = vi.fn().mockResolvedValue({ error: null });
  const fromFn = vi.fn().mockReturnValue({ upsert: upsertFn });
  return { fromFn, upsertFn, supa: { from: fromFn } as any };
};

describe("upsertStockQuotes", () => {
  it("빈 배열 → no-op", async () => {
    const { fromFn, supa } = mkSupa();
    const r = await upsertStockQuotes(supa, []);
    expect(r.count).toBe(0);
    expect(fromFn).not.toHaveBeenCalled();
  });

  it("from('stock_quotes').upsert(rows, {onConflict:'code'})", async () => {
    const { fromFn, upsertFn, supa } = mkSupa();
    await upsertStockQuotes(supa, [mk("005930"), mk("000660")]);
    expect(fromFn).toHaveBeenCalledWith("stock_quotes");
    const passed = upsertFn.mock.calls[0][0];
    expect(passed).toHaveLength(2);
    // 시세 컬럼만 (마스터 컬럼 sector/security_type/listing_date 없음)
    expect(passed[0]).toHaveProperty("price");
    expect(passed[0]).not.toHaveProperty("sector");
    expect(passed[0]).not.toHaveProperty("security_type");
    expect(passed[0]).not.toHaveProperty("name");  // 시세에는 name 없음
  });

  it("dedup by code", async () => {
    const { upsertFn, supa } = mkSupa();
    await upsertStockQuotes(supa, [mk("005930", { price: 1 }), mk("005930", { price: 2 })]);
    expect(upsertFn.mock.calls[0][0]).toHaveLength(1);
    expect(upsertFn.mock.calls[0][0][0].price).toBe(2);
  });
});

describe("upsertTopMovers", () => {
  it("from('top_movers').upsert — rank 부여 + scan_id + ranked_at", async () => {
    const { fromFn, upsertFn, supa } = mkSupa();
    await upsertTopMovers(supa, [mk("A", { changeRate: 1 }), mk("B", { changeRate: 5 }), mk("C", { changeRate: 3 })],
      "scan-123", "2026-04-15T05:00:00Z");
    expect(fromFn).toHaveBeenCalledWith("top_movers");
    const rows = upsertFn.mock.calls[0][0];
    // rank: changeRate 내림차순 = B(5)→C(3)→A(1)
    expect(rows.find((r: any) => r.code === "B").rank).toBe(1);
    expect(rows.find((r: any) => r.code === "C").rank).toBe(2);
    expect(rows.find((r: any) => r.code === "A").rank).toBe(3);
    expect(rows[0].scan_id).toBe("scan-123");
    expect(rows[0].ranked_at).toBe("2026-04-15T05:00:00Z");
    // 시세 컬럼 없음
    expect(rows[0]).not.toHaveProperty("price");
  });

  it("ingestion 은 stocks 테이블 절대 안 건드림 (회귀 가드)", async () => {
    const { fromFn, supa } = mkSupa();
    await upsertStockQuotes(supa, [mk("005930")]);
    await upsertTopMovers(supa, [mk("005930")], "s", "2026-04-15T05:00:00Z");
    const tableCalls = fromFn.mock.calls.map((c: any[]) => c[0]);
    expect(tableCalls).not.toContain("stocks");
  });
});
```

### Step E: 실행
```bash
pnpm -F @gh-radar/ingestion typecheck
pnpm -F @gh-radar/ingestion test -- --run
pnpm -F @gh-radar/ingestion build
```
기존 ingestion 테스트가 있으면 회귀 확인. `upsertStocks` 를 import 하던 기존 테스트가 있으면 본 plan 에서 신규 함수로 전환.
  </action>

  <verify>
    <automated>pnpm -F @gh-radar/ingestion typecheck && pnpm -F @gh-radar/ingestion test -- --run --reporter=verbose 2>&1 | tail -30 && pnpm -F @gh-radar/ingestion build</automated>
  </verify>

  <acceptance_criteria>
    - `grep "from(\"stock_quotes\").upsert\\|from(\"stock_quotes\")\\s*\\.upsert" workers/ingestion/src/pipeline/upsert.ts` 매치
    - `grep "from(\"top_movers\").upsert\\|from(\"top_movers\")\\s*\\.upsert" workers/ingestion/src/pipeline/upsert.ts` 매치
    - `grep "from(\"stocks\")" workers/ingestion/src/pipeline/upsert.ts workers/ingestion/src/pipeline/run.ts` 결과 0건 (마스터 절대 안 건드림)
    - `grep "from(\"top_movers\").delete()\\|from(\"top_movers\")\\s*\\.delete" workers/ingestion/src/pipeline/run.ts` 매치 (stale cleanup top_movers 만)
    - `grep "from(\"stocks\").delete\\|from(\"stock_quotes\").delete" workers/ingestion/src/pipeline/run.ts` 결과 0건 (마스터/quote DELETE 금지)
    - `grep "upsertStockQuotes\\|upsertTopMovers" workers/ingestion/src/pipeline/run.ts | wc -l` ≥ 2
    - `grep "scan_id" workers/ingestion/src/pipeline/upsert.ts` 매치
    - `grep "rank" workers/ingestion/src/pipeline/upsert.ts` 매치
    - `pnpm -F @gh-radar/ingestion typecheck` exit 0
    - `pnpm -F @gh-radar/ingestion test -- --run` exit 0
    - `pnpm -F @gh-radar/ingestion build` 후 `workers/ingestion/dist/pipeline/upsert.js` 존재
    - 신규 upsert 테스트 (Step D) 4개 모두 GREEN — 특히 "stocks 테이블 절대 안 건드림" 회귀 가드
  </acceptance_criteria>

  <done>ingestion 파이프라인이 stock_quotes + top_movers 양쪽 쓰기로 전환. stocks 마스터는 절대 건드리지 않음 (Anti-Patterns 준수). stale cleanup 은 top_movers 만. 회귀 가드 테스트 GREEN.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KIS 응답 → ingestion upsert (stock_quotes + top_movers) | 외부 응답 신뢰 후 두 테이블 동시 쓰기 — 트랜잭션 분리로 부분 실패 가능 |
| ingestion → stocks (마스터) | 절대 쓰지 않아야 함 (master-sync 와 경쟁 시 마스터 메타 손상) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-05-01 | TAMPERING (마스터·ingestion 경쟁) | workers/ingestion/src/pipeline/upsert.ts | mitigate | Anti-Patterns 명시 — ingestion 코드에서 `from("stocks")` 호출 0건. acceptance_criteria 의 grep 가드로 회귀 방지. RLS 측면에서도 추가 보호 가능하나 service_role 는 RLS 우회라 코드 가드가 1차. |
| T-06.1-05-02 | TAMPERING (stale cleanup 오작동) | workers/ingestion/src/pipeline/run.ts | mitigate | DELETE 대상이 `top_movers` 만으로 제한. stocks/stock_quotes 는 영구 데이터(stock_quotes 는 watchlist-ready) 이므로 DELETE 금지. acceptance_criteria grep 가드. |
| T-06.1-05-03 | TAMPERING (트랜잭션 분리) | upsertStockQuotes + upsertTopMovers | accept | 두 upsert 가 별개 호출 — stock_quotes 성공 + top_movers 실패 시 다음 cycle 의 stale cleanup 이 자동 회복. 본 phase 에서는 트랜잭션 통합 deferred (RPC 도입 필요). |
| T-06.1-05-04 | INJECTION (PostgREST) | scanner.ts query params | mitigate | Zod ScannerQuery 기존 검증 유지. enum (KOSPI/KOSDAQ/ALL) + numeric 검증. |
| T-06.1-05-05 | INFORMATION DISCLOSURE | X-Last-Updated-At 헤더 | accept | 노출되는 정보 = stock_quotes 의 가장 최신 갱신 시각 (DB 메타). 보안 민감 정보 아님. CORS exposedHeaders 에 이미 포함 (Phase 05.2). |
| T-06.1-05-06 | DENIAL OF SERVICE | scanner 다단계 쿼리 (3 round-trip) | accept | top_movers + stocks + stock_quotes 3회 round-trip — top_movers 가 ~수십 종목 (랭킹 캐시) 라 IN list 작음. p95 < 500ms 예상. PostgREST nested select 로 1회 round-trip 최적화는 Plan 06 후 측정 후 결정 (deferred). |

</threat_model>

<verification>
- `pnpm -F server typecheck && pnpm -F server test -- --run` 전체 GREEN
- `pnpm -F @gh-radar/ingestion typecheck && pnpm -F @gh-radar/ingestion test -- --run && pnpm -F @gh-radar/ingestion build` 모두 exit 0
- ingestion 코드에서 `from("stocks")` 호출 0건 (회귀 가드)
- scanner 응답 스키마 + X-Last-Updated-At 헤더 SCAN-08 회귀 보존
</verification>

<success_criteria>
- scanner 라우트 + scanner mapper + scanner 테스트 12개+ GREEN
- ingestion 파이프라인 분해 + 신규 upsert 테스트 4개 GREEN
- "stocks 테이블 절대 안 건드림" 회귀 가드 통과
- ingestion build 산출물 dist/ 생성 → Plan 06 가 새 ingestion 이미지 재배포 가능
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-stock-master-universe/06.1-05-SUMMARY.md`
</output>
