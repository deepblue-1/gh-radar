---
phase: 06.1-stock-master-universe
plan: 02
type: execute
wave: 1
depends_on:
  - 06.1-01
files_modified:
  - supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql
  - .planning/phases/06.1-stock-master-universe/06.1-02-MIGRATION-VERIFY.md
autonomous: false
requirements:
  - INFR-02
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SCAN-01
  - SCAN-02
  - SCAN-03
  - SCAN-04
  - SCAN-05
  - SCAN-06
  - SCAN-07
  - SCAN-08

user_setup:
  - service: KRX OpenAPI (https://openapi.krx.co.kr)
    why: "마스터 동기화 Job 의 인증키. Plan 03 에서 첫 호출 시 필요. Plan 06 배포 시 Secret Manager 등록."
    env_vars:
      - name: KRX_AUTH_KEY
        source: "openapi.krx.co.kr 회원가입 → 마이페이지 > AUTH_KEY → '유가증권 종목기본정보' + '코스닥 종목기본정보' 서비스 사용 신청 → 승인 후 .env.deploy 에 export"
    dashboard_config:
      - task: "AUTH_KEY 발급 + stk_isu_base_info / ksq_isu_base_info 2개 서비스 별도 사용 신청"
        location: "https://openapi.krx.co.kr → 로그인 → API 신청"
        why: "RESEARCH Pitfall 2 — 키 발급과 서비스 사용 신청이 별개. 승인 최대 1일 소요."

must_haves:
  truths:
    - "supabase 마이그레이션 파일이 단일 트랜잭션(BEGIN/COMMIT)으로 rename + split + 부트스트랩을 수행한다"
    - "마이그레이션 적용 후 stocks 테이블이 마스터 메타 컬럼만 갖는다 (price/change_amount/change_rate/volume/trade_amount/open/high/low/market_cap/upper_limit/lower_limit 없음)"
    - "stock_quotes 테이블이 시세 컬럼 + code FK → stocks(code) 로 존재한다"
    - "top_movers 테이블이 시세 컬럼 없이 (code, rank, ranked_at, scan_id) 랭킹 전용 컬럼만 갖는다"
    - "news_articles.stock_code FK 가 명시적 drop+re-create 로 새 stocks 를 참조한다 (Pitfall 1)"
    - "discussions.stock_code FK 도 동일하게 새 stocks 를 참조한다"
    - "anon SELECT 정책이 stocks/stock_quotes/top_movers 3개 모두에 적용된다"
    - "pg_trgm GIN 인덱스가 stocks.name 에 생성된다"
    - "supabase db push 가 production Supabase 에 성공 적용된다 (BLOCKING)"
  artifacts:
    - path: "supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql"
      provides: "전체 스키마 분리 마이그레이션"
      contains: "BEGIN;.*ALTER TABLE stocks RENAME TO top_movers.*COMMIT;"
    - path: ".planning/phases/06.1-stock-master-universe/06.1-02-MIGRATION-VERIFY.md"
      provides: "supabase db push 결과 + psql 로 FK/index/RLS 검증 결과 기록"
      contains: "stock_quotes_code_fkey"
  key_links:
    - from: "stock_quotes.code"
      to: "stocks(code)"
      via: "FOREIGN KEY ... ON DELETE CASCADE"
      pattern: "REFERENCES stocks\\(code\\)"
    - from: "news_articles.stock_code"
      to: "stocks(code) (새 마스터)"
      via: "DROP CONSTRAINT + ADD CONSTRAINT (Pitfall 1 명시 re-point)"
      pattern: "ALTER TABLE news_articles DROP CONSTRAINT.*ADD CONSTRAINT"
---

<objective>
Phase 06.1 의 핵심 스키마 변경 — 단일 트랜잭션 마이그레이션으로 `stocks` (마스터+시세+랭킹 혼재) 를 3-테이블(`stocks` 마스터 / `stock_quotes` 시세 / `top_movers` 랭킹) 로 분리하고 production Supabase 에 push.

Purpose: D1, D2, D5, D6, INFR-02 — Plan 03~06 가 의존하는 DB 형태를 만든다. RESEARCH Pitfall 1 (news_articles FK OID 추적) 을 명시적 drop+re-create 로 회피하고, RLS 정책을 3테이블에 승계한다.

Output: 단일 마이그레이션 SQL 1개 + production push 완료 + psql 검증 결과 기록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.1-stock-master-universe/06.1-CONTEXT.md
@.planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md

# 마이그레이션 컨벤션·기존 스키마
@supabase/migrations/20260413120000_init_tables.sql
@supabase/migrations/20260413120100_rls_policies.sql
@supabase/migrations/20260414110000_add_trade_amount.sql

<interfaces>
<!-- 마이그레이션 후 최종 스키마 (Plan 03~05 가 이걸 가정) -->
```sql
-- 새 stocks (마스터 메타 전용, ~2,800 종목)
CREATE TABLE stocks (
  code          text PRIMARY KEY,
  name          text NOT NULL,
  market        text NOT NULL CHECK (market IN ('KOSPI', 'KOSDAQ')),
  sector        text,                -- nullable: ETF/일부 종목 누락 가능
  security_type text NOT NULL,       -- '보통주'/'우선주'/'ETF'/'ETN'/'REIT'
  listing_date  date,                -- nullable
  is_delisted   boolean NOT NULL DEFAULT false,
  updated_at    timestamptz NOT NULL DEFAULT now()
);

-- stock_quotes (시세, code FK → stocks)
CREATE TABLE stock_quotes (
  code          text PRIMARY KEY REFERENCES stocks(code) ON DELETE CASCADE,
  price         numeric(20,2) NOT NULL,
  change_amount numeric(20,2) NOT NULL,
  change_rate   numeric(8,4)  NOT NULL,
  volume        bigint NOT NULL DEFAULT 0,
  trade_amount  bigint NOT NULL DEFAULT 0,
  open          numeric(20,2),
  high          numeric(20,2),
  low           numeric(20,2),
  market_cap    bigint,
  upper_limit   numeric(20,2) NOT NULL,
  lower_limit   numeric(20,2) NOT NULL,
  updated_at    timestamptz NOT NULL DEFAULT now()
);

-- top_movers (랭킹 스냅샷, 시세 컬럼 없음 — 매 cycle 교체 모델 옵션 A)
CREATE TABLE top_movers (
  code          text PRIMARY KEY,
  name          text NOT NULL,           -- rename 보존 (Plan 05 가 stocks JOIN 로 대체 가능)
  market        text NOT NULL,
  rank          int,
  ranked_at     timestamptz NOT NULL DEFAULT now(),
  scan_id       uuid,
  updated_at    timestamptz NOT NULL DEFAULT now()
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 마이그레이션 SQL 작성 (단일 트랜잭션 split + FK re-point + RLS 승계)</name>
  <files>supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql</files>

  <read_first>
    - supabase/migrations/20260413120000_init_tables.sql (기존 stocks/news_articles/discussions 컬럼·FK 정의)
    - supabase/migrations/20260413120100_rls_policies.sql (anon_read_stocks 등 RLS 정책 — 3테이블 승계 대상)
    - supabase/migrations/20260414110000_add_trade_amount.sql (trade_amount 컬럼 추가 — 본 마이그레이션이 이걸 stock_quotes 로 옮김)
    - .planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md §"Pattern 1" (Step 1~6 SQL 예시), §"Pitfall 1" (FK re-point), §"Open Q2" (top_movers 옵션 A), §"Anti-Patterns" (마스터 ingestion 경쟁)
    - .planning/phases/06.1-stock-master-universe/06.1-CONTEXT.md §"D1, D2, D5, D6"
  </read_first>

  <action>
파일 `supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql` 에 다음 내용을 한 트랜잭션으로 작성:

```sql
-- ============================================================
-- Phase 06.1: stocks 테이블 3-분리 (D1, D2, D5, D6)
--   기존 stocks (마스터+시세+랭킹) → stocks (마스터) + stock_quotes (시세) + top_movers (랭킹)
--
-- 결정 근거:
--   D1: 역할 분리 (stocks=무엇이 존재 / stock_quotes=지금 얼마 / top_movers=이번 스캔에 뽑힘)
--   D2: 마이그레이션 경로 — 기존 stocks → top_movers rename → 시세 분리 → 새 stocks 신설
--   D5: ingestion 은 stock_quotes + top_movers 에만 쓰기, stocks 는 master-sync Job 만 쓰기
--   D6: 마스터 메타 = code/name/market/sector/security_type/listing_date/is_delisted
--   Pitfall 1: news_articles/discussions FK 명시적 drop+re-create (rename 후 OID 가 top_movers 가리킴)
--   Open Q2: top_movers 는 매 cycle 교체 모델 (옵션 A) — scan_id 컬럼은 추가하되 단일 row/code
-- ============================================================

BEGIN;

-- ─────────────────────────────────────────────────────────
-- Step 1. 기존 stocks → top_movers 로 rename + 시세 컬럼 보존
--   인덱스/시퀀스 자동 승계되지만 인덱스 이름은 명시 rename 필요
-- ─────────────────────────────────────────────────────────
ALTER TABLE stocks RENAME TO top_movers;
ALTER INDEX idx_stocks_change_rate_desc RENAME TO idx_top_movers_change_rate_desc;
ALTER INDEX idx_stocks_market           RENAME TO idx_top_movers_market;

-- Pitfall 1 — news_articles/discussions FK 가 rename 후 top_movers 를 가리킴.
-- Step 4 에서 새 stocks 생성 후 명시적으로 re-point.

-- ─────────────────────────────────────────────────────────
-- Step 2. stock_quotes 분리 생성 — 시세 컬럼 이전
--   FK 는 Step 6 (새 stocks 생성 + 부트스트랩 후) 에서 추가
-- ─────────────────────────────────────────────────────────
CREATE TABLE stock_quotes (
  code          text PRIMARY KEY,
  price         numeric(20,2) NOT NULL,
  change_amount numeric(20,2) NOT NULL,
  change_rate   numeric(8,4)  NOT NULL,
  volume        bigint NOT NULL DEFAULT 0,
  trade_amount  bigint NOT NULL DEFAULT 0,
  open          numeric(20,2),
  high          numeric(20,2),
  low           numeric(20,2),
  market_cap    bigint,
  upper_limit   numeric(20,2) NOT NULL,
  lower_limit   numeric(20,2) NOT NULL,
  updated_at    timestamptz NOT NULL DEFAULT now()
);

INSERT INTO stock_quotes (
  code, price, change_amount, change_rate, volume, trade_amount,
  open, high, low, market_cap, upper_limit, lower_limit, updated_at
)
SELECT code, price, change_amount, change_rate, volume, trade_amount,
       open, high, low, market_cap, upper_limit, lower_limit, updated_at
FROM top_movers;

-- ─────────────────────────────────────────────────────────
-- Step 3. top_movers 에서 시세 컬럼 제거 + 랭킹 컬럼 추가
-- ─────────────────────────────────────────────────────────
ALTER TABLE top_movers
  DROP COLUMN price,
  DROP COLUMN change_amount,
  DROP COLUMN change_rate,
  DROP COLUMN volume,
  DROP COLUMN trade_amount,
  DROP COLUMN open,
  DROP COLUMN high,
  DROP COLUMN low,
  DROP COLUMN market_cap,
  DROP COLUMN upper_limit,
  DROP COLUMN lower_limit;

-- 시세 컬럼이 빠진 후 idx_top_movers_change_rate_desc 는 무효 → drop
DROP INDEX IF EXISTS idx_top_movers_change_rate_desc;

ALTER TABLE top_movers
  ADD COLUMN rank      int,
  ADD COLUMN ranked_at timestamptz NOT NULL DEFAULT now(),
  ADD COLUMN scan_id   uuid;

CREATE INDEX idx_top_movers_rank ON top_movers (rank ASC NULLS LAST);
CREATE INDEX idx_top_movers_ranked_at ON top_movers (ranked_at DESC);

-- ─────────────────────────────────────────────────────────
-- Step 4. 새 stocks 마스터 테이블 생성
-- ─────────────────────────────────────────────────────────
CREATE TABLE stocks (
  code          text PRIMARY KEY,
  name          text NOT NULL,
  market        text NOT NULL CHECK (market IN ('KOSPI', 'KOSDAQ')),
  sector        text,
  security_type text NOT NULL DEFAULT '보통주',
  listing_date  date,
  is_delisted   boolean NOT NULL DEFAULT false,
  updated_at    timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX idx_stocks_market     ON stocks (market);
CREATE INDEX idx_stocks_updated_at ON stocks (updated_at DESC);

-- 한국어 부분일치 검색 인덱스 (RESEARCH Standard Stack)
CREATE EXTENSION IF NOT EXISTS pg_trgm;
CREATE INDEX idx_stocks_name_trgm ON stocks USING GIN (name gin_trgm_ops);

-- ─────────────────────────────────────────────────────────
-- Step 5. 새 stocks 부트스트랩 — 기존 top_movers 의 종목을 마스터에 시드
--   Plan 03 master-sync 가 KRX 전종목으로 덮어쓰기 전 임시 universe.
--   ingestion(stock_quotes upsert) FK 제약을 막기 위함.
-- ─────────────────────────────────────────────────────────
INSERT INTO stocks (code, name, market, security_type, updated_at)
SELECT code, name, market, '보통주', now()
FROM top_movers
ON CONFLICT (code) DO NOTHING;

-- ─────────────────────────────────────────────────────────
-- Step 6. FK 정책 — stock_quotes.code → stocks(code) ON DELETE CASCADE
-- ─────────────────────────────────────────────────────────
ALTER TABLE stock_quotes
  ADD CONSTRAINT stock_quotes_code_fkey
  FOREIGN KEY (code) REFERENCES stocks(code) ON DELETE CASCADE;

CREATE INDEX idx_stock_quotes_change_rate_desc
  ON stock_quotes (change_rate DESC NULLS LAST);
CREATE INDEX idx_stock_quotes_updated_at
  ON stock_quotes (updated_at DESC);

-- ─────────────────────────────────────────────────────────
-- Step 7. news_articles / discussions FK 명시적 re-point (Pitfall 1)
--   rename 후 OID 가 top_movers 를 가리키므로 drop + add 필수.
--   기존 제약 이름: news_articles_stock_code_fkey, discussions_stock_code_fkey
-- ─────────────────────────────────────────────────────────
ALTER TABLE news_articles
  DROP CONSTRAINT IF EXISTS news_articles_stock_code_fkey;
ALTER TABLE news_articles
  ADD CONSTRAINT news_articles_stock_code_fkey
  FOREIGN KEY (stock_code) REFERENCES stocks(code) ON DELETE CASCADE;

ALTER TABLE discussions
  DROP CONSTRAINT IF EXISTS discussions_stock_code_fkey;
ALTER TABLE discussions
  ADD CONSTRAINT discussions_stock_code_fkey
  FOREIGN KEY (stock_code) REFERENCES stocks(code) ON DELETE CASCADE;

-- ─────────────────────────────────────────────────────────
-- Step 8. RLS 승계 (Phase 1 D-17 정책 — anon SELECT 허용, 쓰기는 service_role)
--   기존 stocks RLS 는 rename 후 top_movers 에 자동 승계됨 (정책명만 rename 필요).
--   정책 이름: anon_read_stocks → anon_read_top_movers
-- ─────────────────────────────────────────────────────────
ALTER POLICY "anon_read_stocks" ON top_movers RENAME TO "anon_read_top_movers";

-- 새 stocks (마스터) + stock_quotes 에 anon SELECT 정책 신설
ALTER TABLE stocks       ENABLE ROW LEVEL SECURITY;
ALTER TABLE stock_quotes ENABLE ROW LEVEL SECURITY;

CREATE POLICY "anon_read_stocks_master"
  ON stocks FOR SELECT TO anon USING (true);
CREATE POLICY "anon_read_stock_quotes"
  ON stock_quotes FOR SELECT TO anon USING (true);

COMMIT;
```

추가로 파일 헤더 주석에 RESEARCH 인용 + Plan 02 식별자 표기.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const sql=fs.readFileSync('supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql','utf8');const r=(p,n)=>{if(!p.test(sql))throw new Error('miss '+n)};r(/^BEGIN;/m,'BEGIN');r(/COMMIT;\s*$/,'COMMIT');r(/ALTER TABLE stocks RENAME TO top_movers/,'rename');r(/CREATE TABLE stock_quotes/,'create stock_quotes');r(/CREATE TABLE stocks/,'create new stocks');r(/REFERENCES stocks\(code\) ON DELETE CASCADE/,'FK');r(/CREATE EXTENSION IF NOT EXISTS pg_trgm/,'pg_trgm');r(/USING GIN \(name gin_trgm_ops\)/,'trgm index');r(/news_articles_stock_code_fkey/,'news FK re-point');r(/discussions_stock_code_fkey/,'disc FK re-point');r(/anon_read_stocks_master/,'RLS master');r(/anon_read_stock_quotes/,'RLS quotes');r(/anon_read_top_movers/,'RLS top_movers rename');console.log('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - 파일 `supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql` 가 1개 `BEGIN;` + 1개 `COMMIT;` 로 감싸짐
    - `grep -c "ALTER TABLE stocks RENAME TO top_movers" <file>` = 1
    - `grep -c "CREATE TABLE stock_quotes" <file>` = 1
    - `grep -c "CREATE TABLE stocks" <file>` = 1 (새 마스터)
    - `grep "REFERENCES stocks(code) ON DELETE CASCADE" <file>` 가 최소 3회 매치 (stock_quotes + news_articles + discussions)
    - `grep "DROP CONSTRAINT.*news_articles_stock_code_fkey" <file>` 매치
    - `grep "DROP CONSTRAINT.*discussions_stock_code_fkey" <file>` 매치
    - `grep "CREATE EXTENSION IF NOT EXISTS pg_trgm" <file>` 매치
    - `grep "USING GIN (name gin_trgm_ops)" <file>` 매치
    - `grep "anon_read_stocks_master" <file>` 매치
    - `grep "anon_read_stock_quotes" <file>` 매치
    - `grep "ALTER POLICY \"anon_read_stocks\" ON top_movers RENAME TO" <file>` 매치
    - 새 stocks 테이블 정의에 `price` / `change_amount` / `volume` 컬럼이 **없음** (`grep -E "^\s+(price|change_amount|volume)\s+" <file>` 결과는 stock_quotes 블록 안에서만 매치)
  </acceptance_criteria>

  <done>마이그레이션 SQL 이 작성되고 모든 정적 검증 통과. 아직 production 적용 전.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: [BLOCKING] supabase db push + psql 검증 + MIGRATION-VERIFY.md 기록</name>
  <files>
    .planning/phases/06.1-stock-master-universe/06.1-02-MIGRATION-VERIFY.md
  </files>

  <read_first>
    - supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql (Task 1 산출물)
    - .planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md §"Validation Architecture" Manual-Only Verifications
    - .planning/phases/06.1-stock-master-universe/06.1-VALIDATION.md (Sampling/Phase gate)
    - 환경: `SUPABASE_ACCESS_TOKEN`, `SUPABASE_DB_URL` (또는 `supabase link` 완료 상태) 필요
  </read_first>

  <action>
**[BLOCKING] 본 task 는 production Supabase 에 마이그레이션을 적용하므로 verification 이전에 반드시 완료해야 한다.**

1. 사전 가드:
```bash
test -n "$SUPABASE_ACCESS_TOKEN" || { echo "SUPABASE_ACCESS_TOKEN 필요"; exit 1; }
ls supabase/migrations/20260415120000_*.sql || { echo "Task 1 마이그레이션 없음"; exit 1; }
```

2. 마이그레이션 push (non-TTY 환경):
```bash
supabase db push
```
대화형 confirmation 이 떠서 non-TTY 진행 불가하면:
```bash
supabase db push --include-all  # 또는 --linked
```
실패하면 `--debug` 로 재실행, 출력을 그대로 SUMMARY 에 첨부.

3. 적용 결과 psql 검증 — `SUPABASE_DB_URL` 사용:
```bash
psql "$SUPABASE_DB_URL" <<'SQL'
\echo === stocks (마스터) ===
\d stocks
\echo === stock_quotes ===
\d stock_quotes
\echo === top_movers ===
\d top_movers
\echo === news_articles FK ===
SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint
  WHERE conrelid = 'news_articles'::regclass AND contype = 'f';
\echo === discussions FK ===
SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint
  WHERE conrelid = 'discussions'::regclass AND contype = 'f';
\echo === RLS 정책 ===
SELECT tablename, policyname, roles, cmd, qual FROM pg_policies
  WHERE tablename IN ('stocks','stock_quotes','top_movers') ORDER BY tablename, policyname;
\echo === pg_trgm 인덱스 ===
SELECT indexname, indexdef FROM pg_indexes WHERE tablename='stocks';
\echo === 부트스트랩 row count ===
SELECT 'stocks' AS t, count(*) FROM stocks
UNION ALL SELECT 'stock_quotes', count(*) FROM stock_quotes
UNION ALL SELECT 'top_movers', count(*) FROM top_movers;
SQL
```

4. 결과를 `.planning/phases/06.1-stock-master-universe/06.1-02-MIGRATION-VERIFY.md` 에 기록:

```markdown
# Phase 06.1 Plan 02 — Migration Verification

**Applied:** YYYY-MM-DD HH:MM KST
**Migration:** `supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql`
**Method:** `supabase db push` exit code 0

## Schema Verification

### stocks (마스터)
[psql \d stocks 결과 paste]

### stock_quotes
[psql \d stock_quotes 결과 paste — code FK → stocks(code) ON DELETE CASCADE 확인]

### top_movers
[psql \d top_movers 결과 paste — 시세 컬럼 없음 + rank/ranked_at/scan_id 확인]

## FK Re-point (Pitfall 1 검증)

### news_articles_stock_code_fkey
[FOREIGN KEY (stock_code) REFERENCES stocks(code) ON DELETE CASCADE — top_movers 가 아닌 새 stocks 가리킴 확인]

### discussions_stock_code_fkey
[동일 검증]

## RLS Policies
[3개 테이블 모두 anon SELECT 허용 확인]

## pg_trgm 인덱스
[idx_stocks_name_trgm 존재 확인]

## Row Counts (부트스트랩 후)
| Table | Count |
|-------|-------|
| stocks | (Step 5 부트스트랩 — 기존 top_movers 와 동일 수) |
| stock_quotes | (Step 2 INSERT — 기존 top_movers 와 동일 수) |
| top_movers | (rename 보존, ~58) |

## Sign-off
- [x] BEGIN/COMMIT 트랜잭션으로 push 성공
- [x] news_articles / discussions FK 가 새 stocks 를 가리킴
- [x] stock_quotes FK CASCADE 동작
- [x] anon_read_stocks_master / anon_read_stock_quotes / anon_read_top_movers 정책 활성
```

5. 만약 push 가 실패하면 STOP — 사용자에게 보고 후 재계획 (마이그레이션 SQL 수정).
  </action>

  <verify>
    <automated>test -f .planning/phases/06.1-stock-master-universe/06.1-02-MIGRATION-VERIFY.md && grep -E "Method:.*supabase db push exit code 0" .planning/phases/06.1-stock-master-universe/06.1-02-MIGRATION-VERIFY.md && grep "FOREIGN KEY (stock_code) REFERENCES stocks(code)" .planning/phases/06.1-stock-master-universe/06.1-02-MIGRATION-VERIFY.md</automated>
  </verify>

  <acceptance_criteria>
    - `supabase db push` exit code 0 (출력에 "Applying migration 20260415120000_split_stocks_master_quotes_movers.sql" 또는 동등 메시지 포함)
    - `psql "$SUPABASE_DB_URL" -c "\d stocks"` 결과에 `price` / `change_rate` / `volume` 컬럼 **없음** (마스터만)
    - `psql "$SUPABASE_DB_URL" -c "\d stock_quotes"` 결과에 `price`, `change_rate`, `volume`, `trade_amount`, `upper_limit` 모두 존재
    - `psql "$SUPABASE_DB_URL" -c "\d top_movers"` 결과에 `rank`, `ranked_at`, `scan_id` 존재 + `price` 없음
    - psql 출력에서 news_articles FK 가 `REFERENCES stocks(code)` (top_movers 가 아님)
    - psql 출력에서 discussions FK 가 `REFERENCES stocks(code)`
    - psql 출력에서 `anon_read_stocks_master`, `anon_read_stock_quotes`, `anon_read_top_movers` 정책 모두 존재
    - psql 출력에서 `idx_stocks_name_trgm` 인덱스 존재 (`USING gin (name gin_trgm_ops)`)
    - `.planning/phases/06.1-stock-master-universe/06.1-02-MIGRATION-VERIFY.md` 파일 존재 + 위 결과 paste
    - 부트스트랩: stocks 와 top_movers row count 가 동일 (rename 보존 + INSERT ... ON CONFLICT 결과)
  </acceptance_criteria>

  <done>Production Supabase 에 3-테이블 분리 적용 완료. psql 검증 결과 + push 출력이 MIGRATION-VERIFY.md 에 보존됨. Plan 03~06 가 안전하게 새 스키마 가정 가능.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → production DB | 트랜잭션 실패 시 row 손실 위험 |
| anon role → 3개 신규/rename 테이블 | RLS 미승계 시 비인가 쓰기 노출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-02-01 | TAMPERING (data loss) | rename + drop + create 시퀀스 | mitigate | 단일 BEGIN/COMMIT 트랜잭션 — 어느 단계에서든 실패 시 자동 ROLLBACK. Step 2 INSERT 가 시세 row 손실 방지. |
| T-06.1-02-02 | INFORMATION DISCLOSURE | 새 stocks/stock_quotes 테이블 anon 접근 | mitigate | Step 8 에서 ENABLE ROW LEVEL SECURITY + anon_read_stocks_master/anon_read_stock_quotes 정책 명시 — Phase 1 D-17 패턴 그대로 승계. anon 은 SELECT 만, 쓰기는 service_role bypass. |
| T-06.1-02-03 | TAMPERING (FK 깨짐) | news_articles / discussions FK | mitigate | Pitfall 1 — Step 7 에서 명시적 DROP + ADD CONSTRAINT 로 새 stocks 를 가리키도록 re-point. psql `\d` 로 검증. |
| T-06.1-02-04 | DENIAL OF SERVICE | pg_trgm GIN 인덱스 빌드 | accept | ~58 row 부트스트랩 단계라 인덱스 빌드 즉시 완료. Plan 03 마스터 백필(~2,800 row) 후에도 GIN 빌드 비용 미미. |
| T-06.1-02-05 | RLS BYPASS | top_movers RLS 정책 rename | mitigate | Step 8 의 `ALTER POLICY "anon_read_stocks" ON top_movers RENAME TO "anon_read_top_movers"` — 정책 자체는 rename 시 자동 승계되지만 이름 일관성 위해 명시 rename. |
| T-06.1-02-06 | INFORMATION DISCLOSURE | SUPABASE_ACCESS_TOKEN / SUPABASE_DB_URL | mitigate | Task 2 는 사용자 로컬 환경의 env 만 사용. SUMMARY 에 토큰 문자열을 절대 paste 금지 (psql 출력만 paste). |

</threat_model>

<verification>
- supabase/migrations/ 에 새 파일 1개 추가됨
- `supabase db push` 성공 후 psql 검증 결과가 MIGRATION-VERIFY.md 에 기록됨
- Plan 03 가 stocks 테이블에 마스터 컬럼만 있음을 가정 가능
- Plan 04/05 가 stock_quotes / top_movers 분리 구조를 가정 가능
- 기존 ingestion(workers/ingestion) 은 본 plan 범위 외 — 다음 cycle 에서 stocks 에 쓰면 FK 정상 (부트스트랩 완료) 이지만 컬럼 mismatch 로 실패 가능. Plan 05 에서 ingestion 코드 수정 완료 전까지 ingestion Cloud Run Job 일시 중단 권장 (Plan 06 가 재개).
</verification>

<success_criteria>
- 마이그레이션 SQL 작성 + production push 적용
- 3개 테이블 스키마 + FK + RLS + pg_trgm 인덱스 모두 production 에서 검증
- 부트스트랩 row count 가 기존 stocks 와 일치 (~58)
- MIGRATION-VERIFY.md 에 모든 검증 출력 보존
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-stock-master-universe/06.1-02-SUMMARY.md`
</output>
