---
phase: 06.1-stock-master-universe
plan: 04
type: execute
wave: 2
depends_on:
  - 06.1-01
  - 06.1-02
files_modified:
  - server/src/routes/stocks.ts
  - server/src/mappers/stock.ts
  - server/src/services/kis-runtime.ts
  - server/src/app.ts
  - server/src/config.ts
  - server/src/server.ts
  - server/tests/fixtures/supabase-mock.ts
  - server/tests/routes/search.test.ts
  - server/tests/routes/stock-detail.test.ts
  - webapp/src/lib/__tests__/stock-api.test.ts
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03

must_haves:
  truths:
    - "/api/stocks/search 가 새 stocks (마스터) 테이블을 대상으로 ilike 검색하고 stock_quotes LEFT JOIN 으로 시세 병합한다"
    - "/api/stocks/:code 가 (1) 마스터 존재 확인 → (2) 없으면 404 inquirePrice 미호출 → (3) 있으면 inquirePrice on-demand 호출 → (4) 성공 시 stock_quotes upsert + 응답 병합 → (5) 실패 시 cached stock_quotes 폴백 → (6) cached 도 없으면 시세 0 (em-dash) 응답"
    - "응답 스키마는 기존 Stock + upperLimitProximity 호환 — webapp/E2E 9종 회귀 없음 (시세 부재 시 0 반환, webapp formatter 가 em-dash 처리)"
    - "server/tests/routes/search.test.ts 가 마스터 universe 케이스 (마스터 존재 + 시세 부재 종목 포함) GREEN"
    - "server/tests/routes/stock-detail.test.ts 의 Wave 0 RED 4종이 모두 GREEN"
    - "webapp stock-api.test.ts 회귀 GREEN"
  artifacts:
    - path: "server/src/routes/stocks.ts"
      provides: "search + detail 라우트 (마스터 universe + on-demand inquirePrice)"
      contains: "fetchInquirePrice"
    - path: "server/src/mappers/stock.ts"
      provides: "rowToStockMaster + mergeMasterAndQuote (rowToStock 보존)"
      contains: "export function mergeMasterAndQuote"
    - path: "server/src/services/kis-runtime.ts"
      provides: "server 부팅 시 KIS 토큰 발급 + axios 클라이언트 생성 + app.locals.kisClient 주입"
      contains: "createKisClient"
  key_links:
    - from: "server/src/routes/stocks.ts (GET /:code)"
      to: "server/src/kis/inquirePrice.ts"
      via: "fetchInquirePrice(req.app.locals.kisClient, code)"
      pattern: "fetchInquirePrice\\("
    - from: "server/src/routes/stocks.ts (GET /:code)"
      to: "stock_quotes 테이블"
      via: "supabase.from('stock_quotes').upsert(...)"
      pattern: "from\\(\"stock_quotes\"\\)\\.upsert"
    - from: "server/src/routes/stocks.ts (GET /search)"
      to: "stocks + stock_quotes (LEFT JOIN)"
      via: "PostgREST nested select 또는 2단계 쿼리"
      pattern: "from\\(\"stocks\"\\)"
---

<objective>
서버 `/api/stocks/search` 와 `/api/stocks/:code` 를 새 3-테이블 구조로 완전 전환. 검색은 마스터 universe (~2,800 종목) + LEFT JOIN 시세, 상세는 마스터 존재 확인 + on-demand KIS inquirePrice + stock_quotes upsert + cached 폴백.

Purpose: D3 (search 마스터 완전 전환), D4 (detail 진입 시 항상 inquirePrice), D9 (캐시 stale 판정 없이 무조건 1회), Pitfall 4 (마스터 부재 시 inquirePrice 미호출), SRCH-01/02/03. Wave 0 RED 4종 GREEN 전환.

Output: stocks 라우트 전환 + KIS 런타임 부팅 배선 + 9개 E2E 호환 응답 스키마 유지 + server/webapp 단위 테스트 회귀 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.1-stock-master-universe/06.1-CONTEXT.md
@.planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md
@.planning/phases/06.1-stock-master-universe/06.1-02-MIGRATION-VERIFY.md

# 수정 대상
@server/src/routes/stocks.ts
@server/src/mappers/stock.ts
@server/src/app.ts
@server/src/config.ts
@server/src/server.ts
@server/src/errors.ts
@server/tests/fixtures/supabase-mock.ts
@server/tests/routes/search.test.ts
@server/tests/routes/stock-detail.test.ts
@webapp/src/lib/__tests__/stock-api.test.ts

# 본 plan 이 사용하는 자산 (Plan 01 산출)
@server/src/kis/client.ts
@server/src/kis/inquirePrice.ts
@server/src/kis/rateLimiter.ts
@server/src/kis/tokenStore.ts

# 회귀 회피 — 변경 금지 자산
@server/src/routes/scanner.ts (Plan 05 가 처리, 본 plan 은 import 만)
@webapp/src/lib/stock-api.ts (응답 스키마 호환 유지)
@webapp/e2e/specs/search.spec.ts
@webapp/e2e/specs/stock-detail.spec.ts

<interfaces>
<!-- Plan 02 산출 — 새 스키마 -->
-- stocks (마스터): code, name, market, sector, security_type, listing_date, is_delisted, updated_at
-- stock_quotes (시세): code FK, price, change_amount, change_rate, volume, trade_amount,
--                      open, high, low, market_cap, upper_limit, lower_limit, updated_at

<!-- 응답 스키마 (호환 유지 — webapp E2E 9종 회귀 회피) -->
```typescript
type StockResponse = {
  code: string; name: string; market: "KOSPI" | "KOSDAQ";
  price: number;       // 시세 부재 시 0 → webapp em-dash
  changeAmount: number; changeRate: number;
  volume: number; tradeAmount: number;
  open: number; high: number; low: number;
  marketCap: number; upperLimit: number; lowerLimit: number;
  updatedAt: string;   // stock_quotes.updated_at, 시세 부재 시 stocks.updated_at fallback
  upperLimitProximity: number;  // upper > 0 ? price/upper : 0
};
```

<!-- KIS inquirePrice 응답 (Plan 01 복제) -->
```typescript
type KisInquirePriceRow = {
  stck_prpr: string;     // 현재가
  prdy_vrss: string;     // 전일대비
  prdy_ctrt: string;     // 등락률
  acml_vol: string;      // 누적거래량
  acml_tr_pbmn: string;  // 누적거래대금 (정확값)
  stck_oprc: string;     // 시가
  stck_hgpr: string;     // 고가
  stck_lwpr: string;     // 저가
  stck_avls: string;     // 시가총액
  stck_mxpr: string;     // 상한가
  stck_llam: string;     // 하한가
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KIS 런타임 부팅 배선 (kis-runtime + app.locals.kisClient + config)</name>
  <files>
    server/src/services/kis-runtime.ts,
    server/src/app.ts,
    server/src/config.ts,
    server/src/server.ts
  </files>

  <read_first>
    - server/src/config.ts (Plan 01 확장 후 — KIS 필드 존재 확인)
    - server/src/app.ts (createApp + AppDeps 시그니처)
    - server/src/server.ts (server 부팅 — supabase 생성 위치)
    - server/src/kis/client.ts (Plan 01 — createKisClient(config, token))
    - server/src/kis/tokenStore.ts (Plan 01 — getKisToken)
    - workers/ingestion/src/index.ts (참조 — 부팅 시 token + client 생성 패턴)
  </read_first>

  <behavior>
    - createApp 의 AppDeps 가 `kisClient?: AxiosInstance` 옵션 추가
    - 테스트 시: AppDeps 에 kisClient 미주입 가능 (옵션) → 라우트가 없으면 graceful fallback
    - 프로덕션: server.ts 가 부팅 시 getKisToken → createKisClient → app.locals.kisClient 주입
    - 토큰 만료 처리: 본 plan 에서는 단순화 — 부팅 시 1회 발급, 만료 시 401 응답에서 logger.warn + 다음 요청에서 lazy refresh (Pitfall 3 — server 는 빈도 낮음)
  </behavior>

  <action>
1. **server/src/services/kis-runtime.ts** 신규:
```typescript
import type { AxiosInstance } from "axios";
import type { SupabaseClient } from "@supabase/supabase-js";
import type { AppConfig } from "../config.js";
import { createKisClient } from "../kis/client.js";
import { getKisToken } from "../kis/tokenStore.js";
import { logger } from "../logger.js";

export async function createKisRuntime(
  config: AppConfig,
  supabase: SupabaseClient,
): Promise<AxiosInstance> {
  const token = await getKisToken(supabase, config);
  logger.info({ tokenLen: token.length }, "KIS runtime ready");
  return createKisClient(config, token);
}
```

2. **server/src/app.ts** 수정 — AppDeps 확장:
```typescript
// 변경 전:
// export type AppDeps = { supabase: SupabaseClient };

// 변경 후:
import type { AxiosInstance } from "axios";
export type AppDeps = {
  supabase: SupabaseClient;
  kisClient?: AxiosInstance;  // 옵션 — 테스트 시 미주입 가능
};

// createApp 본문에 추가 (deps 주입 직후):
app.locals.kisClient = deps.kisClient;
```
나머지 미들웨어/라우터 결선은 그대로.

3. **server/src/config.ts** — Plan 01 Task 3 에서 추가했으면 OK. 누락 시 추가:
```typescript
// AppConfig 에 (없으면 추가):
kisBaseUrl: string;
kisAppKey: string;
kisAppSecret: string;

// loadConfig:
kisBaseUrl: process.env.KIS_BASE_URL ?? "https://openapi.koreainvestment.com:9443",
kisAppKey: get("KIS_APP_KEY"),
kisAppSecret: get("KIS_APP_SECRET"),
```

4. **server/src/server.ts** 수정 — 부팅 시 kis-runtime 초기화:
```typescript
// 기존 supabase 생성 직후:
import { createKisRuntime } from "./services/kis-runtime.js";

const supabase = createSupabaseClient(/* 기존 */);

let kisClient = undefined;
try {
  kisClient = await createKisRuntime(config, supabase);
} catch (err) {
  logger.error({ err }, "KIS runtime init failed — /api/stocks/:code 폴백 모드 (cached only)");
}

const app = createApp({ supabase, kisClient });
```
(server.ts 가 ESM top-level await 사용 가능 — Node 22 + "type":"module" 확인)

5. axios 의존성 확인 — 이미 server/package.json 에 없으면 추가:
```bash
pnpm -F server add axios
```

6. typecheck:
```bash
pnpm -F server typecheck
```
  </action>

  <verify>
    <automated>pnpm -F server typecheck && grep -E "kisClient" server/src/app.ts && grep "createKisRuntime" server/src/server.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep "kisClient" server/src/app.ts` 매치 (AppDeps + locals 주입)
    - `grep "createKisRuntime" server/src/server.ts` 매치
    - `grep "createKisRuntime" server/src/services/kis-runtime.ts` 매치 (export)
    - `grep "kisAppKey\|kisAppSecret" server/src/config.ts` 2건 매치
    - `cat server/package.json | grep "axios"` 매치 (deps)
    - `pnpm -F server typecheck` exit 0
    - 기존 server 테스트(scanner/search/health) 회귀 없음 (테스트가 kisClient 주입 안 해도 통과 — Task 2 가 graceful fallback 구현)
  </acceptance_criteria>

  <done>KIS 런타임 부팅 배선 완료. createApp 이 kisClient optional 수용, server.ts 가 prod 환경에서 자동 발급.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stocks 라우트 전환 (search 마스터 universe + detail on-demand) + mappers + supabase-mock 확장</name>
  <files>
    server/src/routes/stocks.ts,
    server/src/mappers/stock.ts,
    server/tests/fixtures/supabase-mock.ts,
    server/tests/routes/search.test.ts,
    server/tests/routes/stock-detail.test.ts,
    webapp/src/lib/__tests__/stock-api.test.ts
  </files>

  <read_first>
    - server/src/routes/stocks.ts (현재 — search/detail 둘 다 stocks 단일 테이블 가정)
    - server/src/mappers/stock.ts (현재 rowToStock — 보존)
    - server/src/routes/scanner.ts (현재 — COLS 상수 export, 본 plan 에서 dependency 끊기)
    - server/tests/fixtures/supabase-mock.ts (Plan 01 확장본 — masters/quotes/inquirePriceImpl 지원)
    - server/tests/routes/search.test.ts (현재 + Plan 01 확장 시나리오)
    - server/tests/routes/stock-detail.test.ts (Plan 01 RED 4종)
    - server/src/kis/inquirePrice.ts (Plan 01 — fetchInquirePrice 시그니처)
    - .planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md §"Pattern 2" on-demand fetch + §"Anti-Patterns" 검색 응답 스키마 부주의
  </read_first>

  <behavior>
    - Test (search): q=삼성전자 → 마스터 universe 에서 매치, 시세 부재 종목도 결과 포함 (price=0 응답)
    - Test (search): q=005930 → code 매치
    - Test (search): q 누락 → 400 INVALID_QUERY_PARAM
    - Test (search): 마스터에 없는 q → 빈 배열
    - Test (detail): 마스터 존재 + inquirePrice 성공 → 200 + on-demand 값 우선 반영 + stock_quotes upsert 호출
    - Test (detail): 마스터 존재 + inquirePrice 실패 → 200 + cached stock_quotes 값 반환
    - Test (detail): 마스터 존재 + inquirePrice 실패 + cached 도 없음 → 200 + price=0 (em-dash 폴백)
    - Test (detail): 마스터 부재 → 404 STOCK_NOT_FOUND, inquirePrice 미호출
    - Test (detail): kisClient 미주입 → cached stock_quotes 만으로 응답 (graceful)
    - 응답 스키마: 기존 Stock + upperLimitProximity (webapp 회귀 회피)
  </behavior>

  <action>

### Step A: server/src/mappers/stock.ts 확장 (rowToStock 보존 + 신규 헬퍼)

기존 `rowToStock(r: StockRow)` 함수와 `StockRow` 타입은 **그대로 유지** (Plan 05 scanner 가 자기 mapper 로 분리 전까지 호환). 다음 추가:

```typescript
import type { Stock, Market } from "@gh-radar/shared";

// 기존 StockRow / rowToStock 보존 — 절대 삭제 금지

// 새로 추가:

export type StockMasterRow = {
  code: string;
  name: string;
  market: string;
  sector: string | null;
  security_type: string;
  listing_date: string | null;
  is_delisted: boolean;
  updated_at: string;
};

export type StockQuoteRow = {
  code: string;
  price: string;
  change_amount: string;
  change_rate: string;
  volume: number;
  trade_amount: number;
  open: string | null;
  high: string | null;
  low: string | null;
  market_cap: number | null;
  upper_limit: string;
  lower_limit: string;
  updated_at: string;
};

export type StockWithProximityResponse = Stock & { upperLimitProximity: number };

export function mergeMasterAndQuote(
  master: StockMasterRow,
  quote: StockQuoteRow | null,
): StockWithProximityResponse {
  const price = quote ? Number(quote.price) : 0;
  const upper = quote ? Number(quote.upper_limit) : 0;
  return {
    code: master.code,
    name: master.name,
    market: master.market as Market,
    price,
    changeAmount: quote ? Number(quote.change_amount) : 0,
    changeRate: quote ? Number(quote.change_rate) : 0,
    volume: quote ? quote.volume : 0,
    tradeAmount: quote ? quote.trade_amount : 0,
    open: quote ? Number(quote.open ?? 0) : 0,
    high: quote ? Number(quote.high ?? 0) : 0,
    low: quote ? Number(quote.low ?? 0) : 0,
    marketCap: quote ? Number(quote.market_cap ?? 0) : 0,
    upperLimit: upper,
    lowerLimit: quote ? Number(quote.lower_limit) : 0,
    updatedAt: quote ? quote.updated_at : master.updated_at,
    upperLimitProximity: upper > 0 ? price / upper : 0,
  };
}

// KIS inquirePrice 응답 → stock_quotes upsert row
export function inquirePriceToQuoteRow(
  code: string,
  price: { stck_prpr: string; prdy_vrss: string; prdy_ctrt: string;
           acml_vol: string; acml_tr_pbmn: string;
           stck_oprc: string; stck_hgpr: string; stck_lwpr: string;
           stck_avls: string; stck_mxpr: string; stck_llam: string },
): StockQuoteRow {
  return {
    code,
    price: price.stck_prpr,
    change_amount: price.prdy_vrss,
    change_rate: price.prdy_ctrt,
    volume: Number(price.acml_vol),
    trade_amount: Number(price.acml_tr_pbmn),
    open: price.stck_oprc,
    high: price.stck_hgpr,
    low: price.stck_lwpr,
    market_cap: Number(price.stck_avls),
    upper_limit: price.stck_mxpr,
    lower_limit: price.stck_llam,
    updated_at: new Date().toISOString(),
  };
}
```

### Step B: server/src/routes/stocks.ts 전체 재작성

```typescript
import { Router, type Router as RouterT } from "express";
import type { SupabaseClient } from "@supabase/supabase-js";
import type { AxiosInstance } from "axios";
import { SearchQuery, sanitizeSearchTerm } from "../schemas/search.js";
import {
  mergeMasterAndQuote,
  inquirePriceToQuoteRow,
  type StockMasterRow,
  type StockQuoteRow,
} from "../mappers/stock.js";
import { fetchInquirePrice } from "../kis/inquirePrice.js";
import { ApiError, StockNotFound } from "../errors.js";
import { logger } from "../logger.js";

export const stocksRouter: RouterT = Router();

const MASTER_COLS = "code,name,market,sector,security_type,listing_date,is_delisted,updated_at";
const QUOTE_COLS = "code,price,change_amount,change_rate,volume,trade_amount,open,high,low,market_cap,upper_limit,lower_limit,updated_at";

// 순서 중요: /search 를 /:code 보다 먼저 등록
stocksRouter.get("/search", async (req, res, next) => {
  try {
    const parsed = SearchQuery.safeParse(req.query);
    if (!parsed.success) {
      const issue = parsed.error.issues[0];
      throw new ApiError(400, "INVALID_QUERY_PARAM", `${issue.path.join(".")}: ${issue.message}`);
    }
    const q = sanitizeSearchTerm(parsed.data.q);
    if (q.length === 0) {
      throw new ApiError(400, "INVALID_QUERY_PARAM", "q: empty after sanitization");
    }
    const supabase = req.app.locals.supabase as SupabaseClient;

    // 1. 마스터 universe 에서 매치 (name ilike + code ilike)
    const { data: masters, error: mErr } = await supabase
      .from("stocks")
      .select(MASTER_COLS)
      .or(`name.ilike.%${q}%,code.ilike.%${q}%`)
      .order("name", { ascending: true })
      .limit(20);
    if (mErr) throw mErr;
    const masterRows = (masters ?? []) as unknown as StockMasterRow[];
    if (masterRows.length === 0) {
      res.json([]);
      return;
    }

    // 2. 매치된 code 들의 시세 LEFT JOIN (별도 쿼리, 코드 IN list)
    const codes = masterRows.map((m) => m.code);
    const { data: quotes, error: qErr } = await supabase
      .from("stock_quotes")
      .select(QUOTE_COLS)
      .in("code", codes);
    if (qErr) throw qErr;
    const quoteByCode = new Map<string, StockQuoteRow>();
    for (const qr of (quotes ?? []) as unknown as StockQuoteRow[]) {
      quoteByCode.set(qr.code, qr);
    }

    // 3. 병합 + 응답
    res.json(masterRows.map((m) => mergeMasterAndQuote(m, quoteByCode.get(m.code) ?? null)));
  } catch (e) {
    next(e);
  }
});

stocksRouter.get("/:code", async (req, res, next) => {
  try {
    const code = req.params.code;
    if (!/^[A-Za-z0-9]{1,10}$/.test(code)) {
      throw new ApiError(400, "INVALID_QUERY_PARAM", "code: invalid format");
    }
    const supabase = req.app.locals.supabase as SupabaseClient;
    const kisClient = req.app.locals.kisClient as AxiosInstance | undefined;

    // 순서 불변식 (Pitfall 4): 마스터 존재 확인 먼저
    const { data: master, error: mErr } = await supabase
      .from("stocks")
      .select(MASTER_COLS)
      .eq("code", code)
      .maybeSingle();
    if (mErr) throw mErr;
    if (!master) throw StockNotFound(code);
    const masterRow = master as unknown as StockMasterRow;

    // On-demand inquirePrice (D9 — 무조건 1회). kisClient 미주입(테스트/폴백) 시 skip.
    let freshQuote: StockQuoteRow | null = null;
    if (kisClient) {
      try {
        const price = await fetchInquirePrice(kisClient, code);
        freshQuote = inquirePriceToQuoteRow(code, price);
        // upsert (실패해도 응답 우선 — try/catch 분리)
        const { error: upErr } = await supabase
          .from("stock_quotes")
          .upsert(freshQuote, { onConflict: "code" });
        if (upErr) {
          logger.warn({ code, err: upErr }, "stock_quotes upsert failed (continuing with fresh quote)");
        }
      } catch (err) {
        logger.warn({ code, err: (err as Error).message }, "inquirePrice failed — fall back to cached quote");
      }
    }

    // 폴백: cached stock_quotes 조회 (fresh 가 있어도 한 번 더 — 단순화: fresh 우선)
    let quote: StockQuoteRow | null = freshQuote;
    if (!quote) {
      const { data: cached, error: cErr } = await supabase
        .from("stock_quotes")
        .select(QUOTE_COLS)
        .eq("code", code)
        .maybeSingle();
      if (cErr) throw cErr;
      quote = (cached as unknown as StockQuoteRow | null) ?? null;
    }

    res.json(mergeMasterAndQuote(masterRow, quote));
  } catch (e) {
    next(e);
  }
});
```

### Step C: server/tests/fixtures/supabase-mock.ts 강화 (Plan 01 에서 stub 만 추가했으면 본격 구현)

기존 시그니처 `mockSupabase({stocks: rows})` 는 그대로 동작 유지. 추가 분기:

```typescript
import { vi } from "vitest";
import type { SupabaseClient } from "@supabase/supabase-js";
import type { StockRow, StockMasterRow, StockQuoteRow } from "../../src/mappers/stock";

type State = {
  stocks?: StockRow[];                      // 레거시 (scanner 기존 테스트)
  masters?: StockMasterRow[];               // 새 stocks 마스터
  quotes?: StockQuoteRow[];                 // stock_quotes
  topMovers?: any[];                        // Plan 05 가 사용
  upserts?: { table: string; rows: any; }[]; // 호출 추적
};

export function mockSupabase(state: State): SupabaseClient {
  const upsertCalls: { table: string; rows: any }[] = [];
  state.upserts = upsertCalls;

  const makeBuilder = (table: string) => {
    const datasetFor = (t: string): any[] => {
      if (t === "stocks") {
        // 기존 호환: state.stocks 가 있으면 그것 사용, 아니면 masters
        return state.stocks ?? state.masters ?? [];
      }
      if (t === "stock_quotes") return state.quotes ?? [];
      if (t === "top_movers") return state.topMovers ?? [];
      return [];
    };

    let filtered: any[] = [...datasetFor(table)];
    let orderCol: string | null = null;
    let orderAsc = true;
    let limitN: number | null = null;

    const exec = () => {
      let rows = [...filtered];
      if (orderCol) {
        rows.sort((a, b) => {
          const av = (a as any)[orderCol!]; const bv = (b as any)[orderCol!];
          if (typeof av === "string" && typeof bv === "string")
            return orderAsc ? av.localeCompare(bv) : bv.localeCompare(av);
          const an = Number(av) || 0; const bn = Number(bv) || 0;
          return orderAsc ? an - bn : bn - an;
        });
      }
      if (limitN != null) rows = rows.slice(0, limitN);
      return rows;
    };

    const builder: any = {
      select: vi.fn().mockReturnThis(),
      eq: vi.fn().mockImplementation((col: string, val: unknown) => {
        filtered = filtered.filter((r) => (r as any)[col] === val);
        return builder;
      }),
      gte: vi.fn().mockImplementation((col: string, val: number) => {
        filtered = filtered.filter((r) => Number((r as any)[col]) >= Number(val));
        return builder;
      }),
      in: vi.fn().mockImplementation((col: string, vals: any[]) => {
        const set = new Set(vals);
        filtered = filtered.filter((r) => set.has((r as any)[col]));
        return builder;
      }),
      or: vi.fn().mockImplementation((expr: string) => {
        const parts = expr.split(",");
        const patterns = parts.map((p) => {
          const m = p.match(/^(\w+)\.ilike\.%(.*)%$/);
          return m ? { col: m[1], q: m[2].toLowerCase() } : null;
        }).filter(Boolean) as { col: string; q: string }[];
        filtered = filtered.filter((r) =>
          patterns.some((p) => String((r as any)[p.col]).toLowerCase().includes(p.q)),
        );
        return builder;
      }),
      order: vi.fn().mockImplementation((col: string, opts?: { ascending?: boolean }) => {
        orderCol = col; orderAsc = opts?.ascending ?? true; return builder;
      }),
      limit: vi.fn().mockImplementation((n: number) => { limitN = n; return builder; }),
      maybeSingle: vi.fn().mockImplementation(async () => {
        const rows = exec(); return { data: rows[0] ?? null, error: null };
      }),
      upsert: vi.fn().mockImplementation((rows: any) => {
        upsertCalls.push({ table, rows: Array.isArray(rows) ? rows : [rows] });
        // 가짜 upsert: state 에 반영
        const arr = Array.isArray(rows) ? rows : [rows];
        if (table === "stock_quotes") {
          const existing = state.quotes ?? (state.quotes = []);
          for (const r of arr) {
            const i = existing.findIndex((x) => x.code === r.code);
            if (i >= 0) existing[i] = r as StockQuoteRow;
            else existing.push(r as StockQuoteRow);
          }
        }
        return Promise.resolve({ error: null, data: arr });
      }),
      then: (resolve: any) => resolve({ data: exec(), error: null }),
    };
    return builder;
  };

  return {
    from: vi.fn().mockImplementation((table: string) => makeBuilder(table)),
  } as unknown as SupabaseClient;
}

// inquirePrice 모킹은 axios mock 으로 — kisClient 자체를 주입해서 처리
export function mockKisClient(impl: (code: string) => Promise<any>): any {
  return {
    get: vi.fn().mockImplementation(async (_path: string, opts: any) => {
      const code = opts?.params?.fid_input_iscd;
      try {
        const output = await impl(code);
        return { data: { rt_cd: "0", msg_cd: "MCA00000", msg1: "OK", output } };
      } catch (err) {
        throw err;
      }
    }),
  };
}
```

### Step D: server/tests/routes/search.test.ts 재작성 (마스터 universe 시나리오)

```typescript
import { describe, it, expect } from "vitest";
import request from "supertest";
import { createApp } from "../../src/app";
import { mockSupabase } from "../fixtures/supabase-mock";
import { allMasters, samsungQuote, samsungMaster, masterOnly } from "../fixtures/stocks";
import { sanitizeSearchTerm } from "../../src/schemas/search";

const app = (state: any = { masters: allMasters, quotes: [samsungQuote] }) =>
  createApp({ supabase: mockSupabase(state) });

describe("/api/stocks/search (마스터 universe + LEFT JOIN stock_quotes)", () => {
  it("q=삼성전자 → 마스터 매치 + 시세 병합", async () => {
    const r = await request(app()).get("/api/stocks/search?q=" + encodeURIComponent("삼성전자"));
    expect(r.status).toBe(200);
    const samsung = r.body.find((s: any) => s.code === "005930");
    expect(samsung).toBeDefined();
    expect(samsung.name).toBe("삼성전자");
    expect(samsung.price).toBe(70000);  // stock_quotes 에서 LEFT JOIN
  });

  it("q=005930 → code 매치", async () => {
    const r = await request(app()).get("/api/stocks/search?q=005930");
    expect(r.body.some((s: any) => s.code === "005930")).toBe(true);
  });

  it("q=신규상장 → 마스터 매치 + 시세 부재 → price=0 (em-dash)", async () => {
    const r = await request(app()).get("/api/stocks/search?q=" + encodeURIComponent("신규상장"));
    const m = r.body.find((s: any) => s.code === "999999");
    expect(m).toBeDefined();
    expect(m.price).toBe(0);
    expect(m.upperLimitProximity).toBe(0);
  });

  it("q=존재하지않는키워드xyz → 빈 배열", async () => {
    const r = await request(app()).get("/api/stocks/search?q=xyz");
    expect(r.status).toBe(200);
    expect(r.body).toEqual([]);
  });

  it("q 누락 → 400", async () => {
    const r = await request(app()).get("/api/stocks/search");
    expect(r.status).toBe(400);
    expect(r.body.error.code).toBe("INVALID_QUERY_PARAM");
  });

  it("q 빈 문자열 → 400", async () => {
    const r = await request(app()).get("/api/stocks/search?q=");
    expect(r.status).toBe(400);
  });
});

describe("sanitizeSearchTerm (회귀)", () => {
  it("removes ,()%", () => {
    expect(sanitizeSearchTerm("삼성,(주)%")).toBe("삼성주");
  });
  it("preserves Korean/English/digits", () => {
    expect(sanitizeSearchTerm("Samsung 005930 삼성")).toBe("Samsung 005930 삼성");
  });
});
```

### Step E: server/tests/routes/stock-detail.test.ts — Plan 01 RED 4종 GREEN 전환

```typescript
import { describe, it, expect, vi } from "vitest";
import request from "supertest";
import { createApp } from "../../src/app";
import { mockSupabase, mockKisClient } from "../fixtures/supabase-mock";
import { allMasters, samsungQuote } from "../fixtures/stocks";

const okPrice = {
  stck_prpr: "70500", prdy_vrss: "500", prdy_ctrt: "0.71",
  acml_vol: "1234567", acml_tr_pbmn: "900000000000",
  stck_oprc: "70000", stck_hgpr: "71000", stck_lwpr: "69500",
  stck_avls: "418000000000000", stck_mxpr: "91000", stck_llam: "49000",
};

describe("/api/stocks/:code (마스터 universe + on-demand inquirePrice)", () => {
  it("마스터 존재 + inquirePrice 성공 → 200 + on-demand 값 우선 + stock_quotes upsert", async () => {
    const state: any = { masters: allMasters, quotes: [samsungQuote] };
    const supa = mockSupabase(state);
    const kis = mockKisClient(async () => okPrice);
    const r = await request(createApp({ supabase: supa, kisClient: kis }))
      .get("/api/stocks/005930");
    expect(r.status).toBe(200);
    expect(r.body.code).toBe("005930");
    expect(r.body.price).toBe(70500);  // on-demand 값
    expect(state.upserts.some((u: any) => u.table === "stock_quotes")).toBe(true);
  });

  it("마스터 존재 + inquirePrice 실패 → cached stock_quotes 폴백", async () => {
    const state: any = { masters: allMasters, quotes: [samsungQuote] };
    const supa = mockSupabase(state);
    const kis = mockKisClient(async () => { throw new Error("EGW00201"); });
    const r = await request(createApp({ supabase: supa, kisClient: kis }))
      .get("/api/stocks/005930");
    expect(r.status).toBe(200);
    expect(r.body.price).toBe(70000);  // cached 값
  });

  it("마스터 부재 → 404 STOCK_NOT_FOUND, inquirePrice 미호출", async () => {
    const state: any = { masters: allMasters, quotes: [] };
    const supa = mockSupabase(state);
    const kisGet = vi.fn();
    const kis = { get: kisGet } as any;
    const r = await request(createApp({ supabase: supa, kisClient: kis }))
      .get("/api/stocks/000000");
    expect(r.status).toBe(404);
    expect(r.body.error.code).toBe("STOCK_NOT_FOUND");
    expect(kisGet).not.toHaveBeenCalled();
  });

  it("마스터 존재 + inquirePrice 실패 + cached 없음 → 200 + price=0", async () => {
    const state: any = { masters: allMasters, quotes: [] };
    const supa = mockSupabase(state);
    const kis = mockKisClient(async () => { throw new Error("network"); });
    const r = await request(createApp({ supabase: supa, kisClient: kis }))
      .get("/api/stocks/999999");
    expect(r.status).toBe(200);
    expect(r.body.code).toBe("999999");
    expect(r.body.price).toBe(0);
    expect(r.body.upperLimitProximity).toBe(0);
  });

  it("kisClient 미주입 (테스트/폴백) → cached 만으로 응답", async () => {
    const state: any = { masters: allMasters, quotes: [samsungQuote] };
    const r = await request(createApp({ supabase: mockSupabase(state) }))
      .get("/api/stocks/005930");
    expect(r.status).toBe(200);
    expect(r.body.price).toBe(70000);
  });

  it("잘못된 형식 → 400", async () => {
    const r = await request(createApp({ supabase: mockSupabase({ masters: allMasters }) }))
      .get("/api/stocks/!!@@");
    expect(r.status).toBe(400);
  });
});
```

### Step F: webapp/src/lib/__tests__/stock-api.test.ts 회귀 확인 (변경 없이 PASS 확인)

webapp searchStocks/fetchStockDetail 시그니처 변경 없음 → 기존 4개 테스트 GREEN 유지. 만약 회귀 발생 시 mock 응답 형식 점검.

### Step G: 실행
```bash
pnpm -F server typecheck
pnpm -F server test -- --run
pnpm -F webapp test -- --run stock-api
```
모든 server 테스트 GREEN, webapp 회귀 없음.
  </action>

  <verify>
    <automated>pnpm -F server typecheck && pnpm -F server test -- --run --reporter=verbose 2>&1 | tail -40 && pnpm -F webapp test -- --run stock-api 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep "fetchInquirePrice" server/src/routes/stocks.ts` 매치
    - `grep "stock_quotes" server/src/routes/stocks.ts` 매치 최소 3회 (search LEFT JOIN + detail upsert + detail cached)
    - `grep "from(\"stocks\")" server/src/routes/stocks.ts` 매치 (마스터 조회)
    - `grep "from(\"stock_quotes\").*upsert\\|stock_quotes.*upsert\\|upsert.*stock_quotes" server/src/routes/stocks.ts | head -1` 매치 (on-demand upsert)
    - `grep "StockNotFound(code)" server/src/routes/stocks.ts` 매치
    - `grep "kisClient" server/src/routes/stocks.ts` 매치 (graceful fallback)
    - `grep "mergeMasterAndQuote\\|inquirePriceToQuoteRow" server/src/mappers/stock.ts | wc -l` ≥ 2 (export)
    - `grep "rowToStock" server/src/mappers/stock.ts` 매치 (기존 보존)
    - `grep "upsert\\|in:" server/tests/fixtures/supabase-mock.ts | head -3` — `upsert`, `in` 메서드 모두 mock 구현
    - `pnpm -F server typecheck` exit 0
    - `pnpm -F server test -- --run` exit 0 — 모든 server 테스트 GREEN (search 6 + stock-detail 6 + scanner 10 + health 등)
    - `pnpm -F webapp test -- --run stock-api` exit 0 (회귀 없음)
    - `grep "마스터 부재 → 404 STOCK_NOT_FOUND, inquirePrice 미호출" server/tests/routes/stock-detail.test.ts` 매치 (Wave 0 RED → GREEN)
  </acceptance_criteria>

  <done>stocks 라우트가 3-테이블 구조로 완전 전환. 마스터 universe 검색 + on-demand inquirePrice + cached 폴백 + em-dash 폴백 모두 동작. 모든 server 테스트 GREEN, webapp stock-api 회귀 없음.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 (anon) → /api/stocks/search | q 파라미터로 임의 문자열 입력 가능 |
| 사용자 (anon) → /api/stocks/:code | 임의 code URL 진입 시 KIS 호출 트리거 |
| KIS API 응답 → stock_quotes upsert | 외부 응답 신뢰 후 DB 쓰기 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-04-01 | INJECTION (PostgREST) | /api/stocks/search q | mitigate | Zod SearchQuery 검증(min 1, max 64) + sanitizeSearchTerm 가 `,()%` 제거 (기존 로직 유지). PostgREST or-expr 파서 안전. |
| T-06.1-04-02 | DENIAL OF SERVICE (rate limit evasion) | /api/stocks/:code on-demand inquirePrice | mitigate | (a) regex `^[A-Za-z0-9]{1,10}$` 로 invalid code 즉시 400. (b) Pitfall 4 — 마스터 부재 시 inquirePrice 미호출 (마스터 universe = ~2,800 만 호출 가능). (c) server/src/kis/rateLimiter.ts 5 req/sec 토큰 버킷이 자체 보호. (d) Express 글로벌 rate limit (apiRateLimiter) 가 1차 방어. 추가 per-IP token bucket 은 deferred (Pitfall 3). |
| T-06.1-04-03 | TAMPERING | stock_quotes upsert payload | mitigate | inquirePriceToQuoteRow 가 KIS 응답을 정해진 컬럼명으로만 매핑. Number() 변환으로 NaN 이 들어가도 supabase numeric 컬럼이 reject. RLS — service_role 키만 쓰기 가능. |
| T-06.1-04-04 | INFORMATION DISCLOSURE | inquirePrice 실패 로그 | mitigate | logger.warn 에 code + err.message 만 포함. KIS app key/secret 절대 출력 금지 — kis-runtime 가 token 만 응답에 사용. |
| T-06.1-04-05 | SPOOFING | kisClient 부팅 실패 시 graceful fallback | accept | kis-runtime 초기화 실패 시 server 가 cached-only 모드로 부팅 (logger.error 후). 사용자에게는 stale 시세가 보일 수 있으나 502 보다 낫다. 모니터링은 Plan 06 의 Cloud Run 알람으로 처리. |
| T-06.1-04-06 | TAMPERING (mapper 회귀) | mergeMasterAndQuote | mitigate | Wave 0 RED 4종이 응답 스키마(price=0 em-dash, upperLimitProximity=0) 를 검증. 기존 webapp E2E 9종 회귀 회피 — 응답 키/타입 변경 없음. |

</threat_model>

<verification>
- `pnpm -F server typecheck && pnpm -F server test -- --run` exit 0 — server 전 테스트 GREEN
- `pnpm -F webapp test -- --run` exit 0 — webapp 단위 테스트 회귀 없음
- /api/stocks/:code 동작 시퀀스: 마스터 확인 → 404 또는 inquirePrice → upsert → cached 폴백 → 응답
- /api/stocks/search 가 마스터 universe (Plan 03 백필 후 ~2,800) 에서 검색
- Plan 06 의 회귀 E2E (삼성전자 검색) 가 의존할 응답 스키마 보존
</verification>

<success_criteria>
- stocks 라우트 마스터 universe + on-demand 시세 전환 완료
- Wave 0 RED 4종 GREEN
- 응답 스키마 webapp 호환 유지 (E2E 9종 회귀 회피 — Plan 06 가 검증)
- KIS 런타임 부팅 + graceful fallback 동작
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-stock-master-universe/06.1-04-SUMMARY.md`
</output>
