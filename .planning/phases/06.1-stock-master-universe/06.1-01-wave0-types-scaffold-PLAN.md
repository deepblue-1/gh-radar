---
phase: 06.1-stock-master-universe
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/shared/src/stock.ts
  - packages/shared/src/index.ts
  - workers/master-sync/package.json
  - workers/master-sync/tsconfig.json
  - workers/master-sync/vitest.config.ts
  - workers/master-sync/Dockerfile
  - workers/master-sync/.gitignore
  - workers/master-sync/src/index.ts
  - workers/master-sync/src/config.ts
  - workers/master-sync/src/logger.ts
  - workers/master-sync/src/krx/client.ts
  - workers/master-sync/src/krx/fetchBaseInfo.ts
  - workers/master-sync/src/pipeline/map.ts
  - workers/master-sync/src/pipeline/upsert.ts
  - workers/master-sync/src/services/supabase.ts
  - workers/master-sync/tests/krx-client.test.ts
  - workers/master-sync/tests/map.test.ts
  - server/src/kis/client.ts
  - server/src/kis/inquirePrice.ts
  - server/src/kis/rateLimiter.ts
  - server/src/kis/tokenStore.ts
  - server/src/kis/readOnlyGuard.ts
  - server/tests/kis/inquirePrice.test.ts
  - server/tests/kis/rateLimiter.test.ts
  - server/tests/routes/stock-detail.test.ts
  - server/tests/fixtures/stocks.ts
  - server/tests/fixtures/supabase-mock.ts
autonomous: true
requirements: []  # Wave 0 scaffold-only — 실제 요구사항 충족은 Plan 02~06에서 분배

must_haves:
  truths:
    - "@gh-radar/shared 가 StockMaster, StockQuote, StockWithQuote 타입을 export 한다"
    - "workers/master-sync 워크스페이스가 pnpm install 후 typecheck/test 통과한다 (구현 stub 만 있더라도)"
    - "server/src/kis/ 디렉터리가 ingestion 과 동등 시그니처(fetchInquirePrice, waitForSlot, getKisToken, createKisClient)를 export 한다"
    - "server unit 테스트(inquirePrice/rateLimiter/stock-detail 폴백 axios mock) 가 vitest 에서 발견되고 RED 또는 GREEN 으로 실행된다"
    - "server tests/fixtures/supabase-mock 이 마스터 universe 시나리오(stocks + stock_quotes 분리 row)를 표현 가능하다"
  artifacts:
    - path: "packages/shared/src/stock.ts"
      provides: "StockMaster, StockQuote, StockWithQuote 타입 + 기존 Stock 타입 유지"
      contains: "export type StockMaster"
    - path: "workers/master-sync/package.json"
      provides: "@gh-radar/master-sync 워크스페이스 정의"
      contains: "@gh-radar/master-sync"
    - path: "workers/master-sync/Dockerfile"
      provides: "ingestion 패턴 미러 (node:22-alpine + pnpm deploy)"
      contains: "FROM node:22-alpine"
    - path: "server/src/kis/inquirePrice.ts"
      provides: "fetchInquirePrice 함수 — workers/ingestion 과 동일 시그니처"
      contains: "export async function fetchInquirePrice"
    - path: "server/tests/kis/inquirePrice.test.ts"
      provides: "axios mock 으로 inquirePrice 단위 테스트"
      contains: "fetchInquirePrice"
    - path: "server/tests/routes/stock-detail.test.ts"
      provides: "inquirePrice 폴백 시나리오 RED 테스트 (Plan 04 가 GREEN 으로 전환)"
      contains: "inquirePrice 실패"
  key_links:
    - from: "workers/master-sync/src/services/supabase.ts"
      to: "@supabase/supabase-js"
      via: "createClient(supabaseUrl, serviceRoleKey)"
      pattern: "createClient"
    - from: "server/src/kis/inquirePrice.ts"
      to: "server/src/kis/rateLimiter.ts"
      via: "waitForSlot import"
      pattern: "import.*rateLimiter"
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/stock.ts"
      via: "re-export StockMaster, StockQuote, StockWithQuote"
      pattern: "export.*from.*stock"
---

<objective>
Wave 0 — Phase 06.1 의 모든 후속 plan 이 의존할 타입·워크스페이스·테스트 스캐폴드를 한 번에 셋업한다.

Purpose: Plan 02~06 의 executor 가 코드를 작성하기 전에 (1) 공유 타입 계약, (2) `workers/master-sync` 워크스페이스, (3) `server/src/kis/` 복제본, (4) Wave 0 RED 테스트(stock-detail 폴백/inquirePrice/rateLimiter)가 모두 존재하도록 한다. CONTEXT D1, D2, D6, D7, D8, RESEARCH §"Architecture Patterns"·§"Wave 0 Gaps" 정합.

Output: `@gh-radar/master-sync` 워크스페이스 + `@gh-radar/shared` 타입 확장 + `server/src/kis/` 5개 파일 + 신규 vitest RED 테스트 5개. 구현 로직은 stub(throw 또는 return 빈 값) 으로 두고, 실제 구현은 후속 plan 에서.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-stock-master-universe/06.1-CONTEXT.md
@.planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md
@.planning/phases/06.1-stock-master-universe/06.1-VALIDATION.md

# 복제 원본 (read-only)
@workers/ingestion/src/kis/client.ts
@workers/ingestion/src/kis/inquirePrice.ts
@workers/ingestion/src/kis/rateLimiter.ts
@workers/ingestion/src/kis/tokenStore.ts
@workers/ingestion/src/kis/readOnlyGuard.ts
@workers/ingestion/Dockerfile
@workers/ingestion/package.json
@workers/ingestion/vitest.config.ts
@workers/ingestion/tsconfig.json

# 수정 대상 (read-modify)
@packages/shared/src/stock.ts
@packages/shared/src/index.ts
@server/tests/fixtures/stocks.ts
@server/tests/fixtures/supabase-mock.ts
@server/tests/routes/stock-detail.test.ts

<interfaces>
<!-- packages/shared/src/stock.ts 현재 (변경 전) -->
```typescript
export type Market = "KOSPI" | "KOSDAQ";
export type Stock = {
  code: string; name: string; market: Market;
  price: number; changeAmount: number; changeRate: number;
  volume: number; tradeAmount: number;
  open: number; high: number; low: number;
  marketCap: number; upperLimit: number; lowerLimit: number;
  updatedAt: string;
};
```

<!-- 추가할 타입 (Plan 02 마이그레이션 컬럼 정의와 1:1) -->
```typescript
export type SecurityType = "보통주" | "우선주" | "ETF" | "ETN" | "REIT" | string;

export type StockMaster = {
  code: string;
  name: string;
  market: Market;
  sector: string | null;
  securityType: SecurityType;
  listingDate: string | null; // ISO date YYYY-MM-DD
  isDelisted: boolean;
  updatedAt: string;
};

export type StockQuote = {
  code: string;
  price: number;
  changeAmount: number;
  changeRate: number;
  volume: number;
  tradeAmount: number;
  open: number | null;
  high: number | null;
  low: number | null;
  marketCap: number | null;
  upperLimit: number;
  lowerLimit: number;
  updatedAt: string;
};

// 검색·상세 응답에서 마스터 + 시세 병합 결과 (시세 부재 시 0/em-dash 폴백 — RESEARCH Open Q4 옵션 A)
export type StockWithQuote = StockMaster & {
  price: number;
  changeAmount: number;
  changeRate: number;
  volume: number;
  tradeAmount: number;
  open: number;
  high: number;
  low: number;
  marketCap: number;
  upperLimit: number;
  lowerLimit: number;
  quoteUpdatedAt: string | null; // null 이면 시세 없음 → webapp em-dash
};
```

<!-- workers/ingestion/Dockerfile 미러 — 단지 ingestion → master-sync 치환 -->
<!-- workers/ingestion/package.json 미러 — name: @gh-radar/master-sync -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 공유 타입 확장 (StockMaster/StockQuote/StockWithQuote)</name>
  <files>packages/shared/src/stock.ts, packages/shared/src/index.ts</files>

  <read_first>
    - packages/shared/src/stock.ts (현재 Stock 타입 — 보존)
    - packages/shared/src/index.ts (re-export 패턴 확인)
    - .planning/phases/06.1-stock-master-universe/06.1-CONTEXT.md §"D6" (마스터 메타 범위)
    - .planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md §"Pattern 1" (마이그레이션 컬럼명 — snake_case 와의 매핑)
  </read_first>

  <behavior>
    - StockMaster, StockQuote, StockWithQuote, SecurityType 타입을 export 한다
    - 기존 Stock 타입은 변경 없이 유지 (Plan 04/05 에서 점진 전환)
    - 모든 신규 타입에 `updatedAt: string` 필드 포함 (ISO 8601)
    - StockMaster.sector / listingDate 는 nullable, isDelisted 는 boolean (NOT NULL DEFAULT false)
    - StockQuote.open/high/low/marketCap 은 nullable, 나머지는 non-null
    - StockWithQuote 는 시세 없음 표현을 위해 quoteUpdatedAt: string | null 필드 추가
  </behavior>

  <action>
1. `packages/shared/src/stock.ts` 에 위 `<interfaces>` 블록의 타입 4개(SecurityType, StockMaster, StockQuote, StockWithQuote)를 정확히 그대로 추가한다. 기존 `export type Market` 와 `export type Stock` 은 한 줄도 수정하지 말 것.

2. `packages/shared/src/index.ts` 가 `export * from "./stock"` 또는 명시적 named export 패턴인지 확인 후, StockMaster/StockQuote/StockWithQuote/SecurityType 가 외부 import 가능하도록 보장한다 (이미 `export *` 면 자동 — 작업 없음).

3. 워크스페이스 build:
```bash
pnpm -F @gh-radar/shared build
```
  </action>

  <verify>
    <automated>pnpm -F @gh-radar/shared build && node -e "const m=require('./packages/shared/dist/index.js'); console.log(Object.keys(m))"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -E "^export type StockMaster" packages/shared/src/stock.ts` 가 1줄 매치
    - `grep -E "^export type StockQuote" packages/shared/src/stock.ts` 가 1줄 매치
    - `grep -E "^export type StockWithQuote" packages/shared/src/stock.ts` 가 1줄 매치
    - `grep -E "^export type Stock " packages/shared/src/stock.ts` (기존) 여전히 존재
    - `pnpm -F @gh-radar/shared build` exit 0
    - `packages/shared/dist/stock.d.ts` 에 `StockMaster`, `StockQuote`, `StockWithQuote` 모두 포함
  </acceptance_criteria>

  <done>공유 타입 4개가 export 되고 dist 빌드 통과. 기존 Stock 타입 무손상.</done>
</task>

<task type="auto">
  <name>Task 2: workers/master-sync 워크스페이스 스캐폴드 (Dockerfile/tsconfig/vitest/package.json)</name>
  <files>
    workers/master-sync/package.json,
    workers/master-sync/tsconfig.json,
    workers/master-sync/vitest.config.ts,
    workers/master-sync/Dockerfile,
    workers/master-sync/.gitignore,
    workers/master-sync/src/index.ts,
    workers/master-sync/src/config.ts,
    workers/master-sync/src/logger.ts,
    workers/master-sync/src/krx/client.ts,
    workers/master-sync/src/krx/fetchBaseInfo.ts,
    workers/master-sync/src/pipeline/map.ts,
    workers/master-sync/src/pipeline/upsert.ts,
    workers/master-sync/src/services/supabase.ts,
    workers/master-sync/tests/krx-client.test.ts,
    workers/master-sync/tests/map.test.ts
  </files>

  <read_first>
    - workers/ingestion/package.json (정확 미러 대상 — name 만 변경)
    - workers/ingestion/tsconfig.json (그대로 복사)
    - workers/ingestion/vitest.config.ts (그대로 복사)
    - workers/ingestion/Dockerfile (정확 미러 — ingestion → master-sync 5곳 치환)
    - workers/ingestion/src/config.ts (env 변수 패턴 — 신규 KRX_AUTH_KEY 추가 패턴 학습)
    - workers/ingestion/src/logger.ts (그대로 복사)
    - workers/ingestion/src/services/supabase.ts (그대로 복사)
    - workers/ingestion/src/index.ts (main 진입점 패턴 학습 — non-trading day guard 등)
    - pnpm-workspace.yaml (`workers/*` glob 이미 매칭 — 추가 등록 불필요)
    - .planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md §"Code Examples" KRX 부분
  </read_first>

  <action>
1. **package.json** — `workers/ingestion/package.json` 을 그대로 복사 후 다음만 변경:
```json
{
  "name": "@gh-radar/master-sync",
  ... (나머지 동일)
}
```
의존성: `@gh-radar/shared workspace:*`, `@supabase/supabase-js ^2.49.0`, `axios ^1.7.0`, `dotenv ^16.4.0`, `pino ^9.0.0`. devDeps: `tsx ^4`, `typescript ^5`, `vitest ^3`, `@types/node ^22`. scripts: ingestion 과 동일.

2. **tsconfig.json** — `workers/ingestion/tsconfig.json` 바이트 단위 복사.

3. **vitest.config.ts** — `workers/ingestion/vitest.config.ts` 바이트 단위 복사 (`globals: true`).

4. **Dockerfile** — `workers/ingestion/Dockerfile` 복사 후 `ingestion` 5개를 `master-sync` 로 치환:
   - `COPY workers/ingestion/package.json` → `COPY workers/master-sync/package.json`
   - `COPY workers/ingestion/` → `COPY workers/master-sync/`
   - `pnpm -F @gh-radar/ingestion build` → `pnpm -F @gh-radar/master-sync build`
   - `pnpm --filter=@gh-radar/ingestion --prod --legacy deploy /out` → `pnpm --filter=@gh-radar/master-sync --prod --legacy deploy /out`
   - 그 외 (`FROM node:22-alpine`, COPY shared, USER app, CMD) 그대로.

5. **.gitignore** — 한 줄: `dist/` `node_modules/`

6. **src/config.ts** — ingestion config 패턴 미러 + 신규 필드 `krxAuthKey`. 시그니처:
```typescript
export type Config = {
  supabaseUrl: string;
  supabaseServiceRoleKey: string;
  krxAuthKey: string;
  krxBaseUrl: string;       // default "https://data-dbg.krx.co.kr/svc/apis"
  logLevel: string;
  appVersion: string;
  basDd?: string;           // optional — 미설정 시 today (KST 영업일)
};
export function loadConfig(): Config { ... process.env.KRX_AUTH_KEY!.trim() ... }
```
필수 env: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `KRX_AUTH_KEY`. 누락 시 throw `${k} must be set`.

7. **src/logger.ts** — `workers/ingestion/src/logger.ts` 바이트 단위 복사.

8. **src/services/supabase.ts** — `workers/ingestion/src/services/supabase.ts` 바이트 단위 복사 (createClient 래퍼).

9. **src/krx/client.ts** — RESEARCH §"Code Examples" KRX axios 인스턴스 패턴 그대로:
```typescript
import axios, { type AxiosInstance } from "axios";
import type { Config } from "../config";

export function createKrxClient(config: Config): AxiosInstance {
  return axios.create({
    baseURL: config.krxBaseUrl,
    headers: {
      AUTH_KEY: config.krxAuthKey,  // ⚠ config.ts 에서 .trim() 완료 (Pitfall 6)
      "Content-Type": "application/json",
      Accept: "application/json",
    },
    timeout: 30_000,
  });
}
```

10. **src/krx/fetchBaseInfo.ts** — stub:
```typescript
import type { AxiosInstance } from "axios";
export type KrxBaseInfoRow = {
  ISU_SRT_CD?: string; ISU_NM?: string; ISU_ABBRV?: string;
  MKT_TP_NM?: string; SECUGRP_NM?: string; SECT_TP_NM?: string;
  LIST_DD?: string; market: "KOSPI" | "KOSDAQ";
};
export async function fetchMasterFromKrx(
  client: AxiosInstance, basDd: string,
): Promise<KrxBaseInfoRow[]> {
  throw new Error("NOT_IMPLEMENTED — Plan 03");
}
```

11. **src/pipeline/map.ts** — stub:
```typescript
import type { StockMaster } from "@gh-radar/shared";
import type { KrxBaseInfoRow } from "../krx/fetchBaseInfo";
export function krxToMasterRow(r: KrxBaseInfoRow): StockMaster {
  throw new Error("NOT_IMPLEMENTED — Plan 03");
}
```

12. **src/pipeline/upsert.ts** — stub:
```typescript
import type { SupabaseClient } from "@supabase/supabase-js";
import type { StockMaster } from "@gh-radar/shared";
export async function upsertMasters(
  supabase: SupabaseClient, rows: StockMaster[],
): Promise<{ count: number }> {
  throw new Error("NOT_IMPLEMENTED — Plan 03");
}
```

13. **src/index.ts** — main 엔트리 stub:
```typescript
import "dotenv/config";
import { loadConfig } from "./config";
import { logger } from "./logger";
import { createSupabaseClient } from "./services/supabase";
import { createKrxClient } from "./krx/client";
import { fetchMasterFromKrx } from "./krx/fetchBaseInfo";
import { krxToMasterRow } from "./pipeline/map";
import { upsertMasters } from "./pipeline/upsert";

async function main(): Promise<void> {
  const config = loadConfig();
  const log = logger.child({ app: "master-sync", version: config.appVersion });
  log.info("master-sync starting (NOT_IMPLEMENTED — Plan 03)");
  // Plan 03 가 fetch → map → upsert → log 로직 채움
  process.exit(0);
}

main().catch((err) => {
  logger.error({ err }, "master-sync failed");
  process.exit(1);
});
```

14. **tests/krx-client.test.ts** — RED stub (Plan 03 가 GREEN 으로):
```typescript
import { describe, it, expect } from "vitest";
import { createKrxClient } from "../src/krx/client";

describe("createKrxClient", () => {
  it("AUTH_KEY 헤더가 config 값으로 설정", () => {
    const c = createKrxClient({
      krxAuthKey: "test-key", krxBaseUrl: "https://example.com",
      supabaseUrl: "x", supabaseServiceRoleKey: "x",
      logLevel: "info", appVersion: "test",
    } as any);
    expect(c.defaults.headers["AUTH_KEY"]).toBe("test-key");
    expect(c.defaults.baseURL).toBe("https://example.com");
  });
});
```

15. **tests/map.test.ts** — RED stub (Plan 03 가 GREEN 으로):
```typescript
import { describe, it, expect } from "vitest";
import { krxToMasterRow } from "../src/pipeline/map";

describe("krxToMasterRow (Plan 03 가 구현)", () => {
  it("ISU_SRT_CD → code, ISU_ABBRV → name, MKT_TP_NM → market", () => {
    const result = krxToMasterRow({
      ISU_SRT_CD: "005930", ISU_ABBRV: "삼성전자", ISU_NM: "삼성전자보통주",
      MKT_TP_NM: "KOSPI", SECUGRP_NM: "보통주",
      LIST_DD: "19750611", SECT_TP_NM: "전기·전자",
      market: "KOSPI",
    });
    expect(result.code).toBe("005930");
    expect(result.name).toBe("삼성전자");
    expect(result.market).toBe("KOSPI");
    expect(result.securityType).toBe("보통주");
    expect(result.listingDate).toBe("1975-06-11");
    expect(result.sector).toBe("전기·전자");
    expect(result.isDelisted).toBe(false);
  });
});
```

16. workspace 등록 + 설치:
```bash
pnpm install
pnpm -F @gh-radar/master-sync typecheck
```
(typecheck 는 통과 — stub throw 는 타입 에러 없음)

테스트는 RED 가 정상 (Plan 03 가 GREEN). 그러나 `--reporter=verbose` 로 테스트가 발견되었음을 확인.
  </action>

  <verify>
    <automated>pnpm install && pnpm -F @gh-radar/master-sync typecheck && pnpm -F @gh-radar/master-sync test -- --run --reporter=verbose 2>&1 | grep -E "(krx-client|krxToMasterRow)"</automated>
  </verify>

  <acceptance_criteria>
    - `cat workers/master-sync/package.json | grep '"name": "@gh-radar/master-sync"'` 매치
    - `grep -c "FROM node:22-alpine" workers/master-sync/Dockerfile` ≥ 2 (2-stage)
    - `grep "master-sync" workers/master-sync/Dockerfile | wc -l` ≥ 5 (ingestion → master-sync 5곳 치환 완료)
    - `grep -c "ingestion" workers/master-sync/Dockerfile` = 0 (잔존 ingestion 없음)
    - `grep "AUTH_KEY:" workers/master-sync/src/krx/client.ts` 매치
    - `grep "KRX_AUTH_KEY" workers/master-sync/src/config.ts` 매치
    - `grep "\.trim()" workers/master-sync/src/config.ts` 매치 (Pitfall 6 — 공백 제거)
    - `pnpm -F @gh-radar/master-sync typecheck` exit 0
    - `pnpm -F @gh-radar/master-sync test -- --run` 가 2개 테스트 발견 (RED 또는 GREEN; 본 task 에서는 RED 허용)
    - `ls workers/master-sync/src/{index,config,logger}.ts workers/master-sync/src/krx/{client,fetchBaseInfo}.ts workers/master-sync/src/pipeline/{map,upsert}.ts workers/master-sync/src/services/supabase.ts` 모두 존재
  </acceptance_criteria>

  <done>master-sync 워크스페이스가 pnpm install + typecheck 통과, 2개 RED 테스트가 vitest 에 발견됨. Dockerfile 은 ingestion 패턴 정확 미러.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: server/src/kis/ 복제 + RED 테스트 5종 (inquirePrice/rateLimiter/stock-detail 폴백)</name>
  <files>
    server/src/kis/client.ts,
    server/src/kis/inquirePrice.ts,
    server/src/kis/rateLimiter.ts,
    server/src/kis/tokenStore.ts,
    server/src/kis/readOnlyGuard.ts,
    server/tests/kis/inquirePrice.test.ts,
    server/tests/kis/rateLimiter.test.ts,
    server/tests/routes/stock-detail.test.ts,
    server/tests/fixtures/stocks.ts,
    server/tests/fixtures/supabase-mock.ts
  </files>

  <read_first>
    - workers/ingestion/src/kis/client.ts (복제 원본 — config 의존성 주의)
    - workers/ingestion/src/kis/inquirePrice.ts (복제 원본 — 본문 그대로)
    - workers/ingestion/src/kis/rateLimiter.ts (복제 원본 — MAX_REQUESTS_PER_SEC=5)
    - workers/ingestion/src/kis/tokenStore.ts (복제 원본 — supabase.kis_tokens 공유)
    - workers/ingestion/src/kis/readOnlyGuard.ts (복제 원본)
    - server/src/config.ts (server AppConfig — KIS_APP_KEY/SECRET 추가 필요)
    - server/tests/routes/stock-detail.test.ts (현재 — 시나리오 확장 대상)
    - server/tests/fixtures/supabase-mock.ts (현재 mockSupabase — stocks 단일 테이블 가정)
    - server/tests/fixtures/stocks.ts (samsungRow 등 — Plan 04 전환 시 분리)
    - .planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md §"Pitfall 3" (rate limiter 복제 정책) §"Pitfall 6" (별도 버킷 의도적)
  </read_first>

  <action>
1. **server/src/kis/rateLimiter.ts** — `workers/ingestion/src/kis/rateLimiter.ts` 바이트 단위 복사. `MAX_REQUESTS_PER_SEC = 5` 상수 유지 (Pitfall 3 — 별도 버킷 의도적).

2. **server/src/kis/readOnlyGuard.ts** — `workers/ingestion/src/kis/readOnlyGuard.ts` 바이트 단위 복사.

3. **server/src/kis/inquirePrice.ts** — `workers/ingestion/src/kis/inquirePrice.ts` 바이트 단위 복사. `import { waitForSlot } from "./rateLimiter"` 경로 그대로 동작.

4. **server/src/kis/client.ts** — `workers/ingestion/src/kis/client.ts` 복사 후 import 경로만 server 환경에 맞춰 수정:
   - `import type { Config } from "../config"` → `import type { AppConfig } from "../config.js"`
   - 시그니처 변경: `createKisClient(config: AppConfig, token: string)` — 단, AppConfig 에 `kisBaseUrl`, `kisAppKey`, `kisAppSecret` 가 없으면 추가 필요 (아래 5번).

5. **server/src/config.ts** 수정 — AppConfig 에 KIS 필드 추가:
```typescript
export type AppConfig = {
  // 기존 필드 ...
  kisBaseUrl: string;        // default "https://openapi.koreainvestment.com:9443"
  kisAppKey: string;         // required
  kisAppSecret: string;      // required
};
```
loadConfig 에서 `kisBaseUrl: process.env.KIS_BASE_URL ?? "https://openapi.koreainvestment.com:9443"`, `kisAppKey: get("KIS_APP_KEY")`, `kisAppSecret: get("KIS_APP_SECRET")` 추가.

6. **server/src/kis/tokenStore.ts** — `workers/ingestion/src/kis/tokenStore.ts` 복사 + import 수정:
   - `import type { Config } from "../config"` → `import type { AppConfig } from "../config.js"`
   - `import { logger } from "../logger"` → `import { logger } from "../logger.js"` (server 의 logger)
   - 시그니처: `getKisToken(supabase: SupabaseClient, config: AppConfig)`

7. **server/tests/kis/rateLimiter.test.ts** — 새로 작성:
```typescript
import { describe, it, expect, beforeEach } from "vitest";
import { waitForSlot, resetRateLimiter } from "../../src/kis/rateLimiter";

describe("server rate limiter (5 req/sec, 별도 버킷)", () => {
  beforeEach(() => resetRateLimiter());
  it("5번 호출은 1초 미만 (버킷 가득)", async () => {
    const start = Date.now();
    for (let i = 0; i < 5; i++) await waitForSlot();
    expect(Date.now() - start).toBeLessThan(500);
  });
  it("6번째 호출은 ~200ms 대기 (1/5 sec)", async () => {
    for (let i = 0; i < 5; i++) await waitForSlot();
    const start = Date.now();
    await waitForSlot();
    expect(Date.now() - start).toBeGreaterThan(150);
  });
});
```

8. **server/tests/kis/inquirePrice.test.ts** — 새로 작성 (axios mock):
```typescript
import { describe, it, expect, vi } from "vitest";
import { fetchInquirePrice } from "../../src/kis/inquirePrice";

describe("fetchInquirePrice (server 측 복제본)", () => {
  it("rt_cd=0 응답 시 output 반환", async () => {
    const client = {
      get: vi.fn().mockResolvedValue({
        data: {
          rt_cd: "0", msg_cd: "MCA00000", msg1: "OK",
          output: { stck_mxpr: "91000", stck_llam: "49000", stck_oprc: "70000",
                    stck_avls: "418000000", acml_tr_pbmn: "900000000000",
                    stck_prpr: "70500", prdy_vrss: "500", prdy_ctrt: "0.71",
                    acml_vol: "1234567", stck_hgpr: "71000", stck_lwpr: "69500" },
        },
      }),
    } as any;
    const result = await fetchInquirePrice(client, "005930");
    expect(result.stck_mxpr).toBe("91000");
    expect(client.get).toHaveBeenCalledWith(
      "/uapi/domestic-stock/v1/quotations/inquire-price",
      expect.objectContaining({
        headers: { tr_id: "FHKST01010100" },
        params: { fid_cond_mrkt_div_code: "J", fid_input_iscd: "005930" },
      }),
    );
  });
  it("rt_cd != 0 응답 시 throw", async () => {
    const client = {
      get: vi.fn().mockResolvedValue({
        data: { rt_cd: "1", msg_cd: "EGW00201", msg1: "rate limited" },
      }),
    } as any;
    await expect(fetchInquirePrice(client, "005930")).rejects.toThrow(/EGW00201/);
  });
});
```

9. **server/tests/fixtures/stocks.ts** 수정 — 마스터/시세 분리 row 추가 (기존 row 보존):
```typescript
// 기존 samsungRow/kakaoRow/kosdaqRow 보존
// 추가: 마스터 universe 테스트용 row (Plan 04 가 사용)
import type { StockMaster, StockQuote } from "@gh-radar/shared";

export const samsungMaster: StockMaster = {
  code: "005930", name: "삼성전자", market: "KOSPI",
  sector: "전기·전자", securityType: "보통주",
  listingDate: "1975-06-11", isDelisted: false,
  updatedAt: "2026-04-15T00:00:00Z",
};
export const samsungQuote: StockQuote = {
  code: "005930", price: 70000, changeAmount: 1000, changeRate: 1.45,
  volume: 12345678, tradeAmount: 900000000000,
  open: 69500, high: 70500, low: 69000, marketCap: 418000000000000,
  upperLimit: 91000, lowerLimit: 49000,
  updatedAt: "2026-04-13T10:00:00Z",
};
// 마스터에는 있지만 시세 없는 종목 (em-dash 폴백 시나리오)
export const masterOnly: StockMaster = {
  code: "999999", name: "신규상장종목", market: "KOSDAQ",
  sector: null, securityType: "보통주",
  listingDate: null, isDelisted: false,
  updatedAt: "2026-04-15T00:00:00Z",
};
export const allMasters: StockMaster[] = [samsungMaster, masterOnly];
export const allQuotes: StockQuote[] = [samsungQuote];
```

10. **server/tests/fixtures/supabase-mock.ts** 수정 — multi-table 분기 본격 구현 (Plan 04 Step C와 동일 깊이). 기존 `mockSupabase({stocks: rows})` 시그니처는 그대로 지원 + 새 시그니처 `mockSupabase({masters, quotes, topMovers, inquirePriceImpl})` 추가. `from(name)` 에서 name 별 데이터 소스 분기 + `maybeSingle()/in()/or()/upsert()` 빌더를 Plan 04 라우트(stocks.ts)가 호출하는 그대로 구현.

```typescript
import { vi } from "vitest";
import type { SupabaseClient } from "@supabase/supabase-js";
import type { StockMaster, StockQuote } from "@gh-radar/shared";
// StockRow 는 기존 scanner 테스트 호환용 (서버측 타입은 server/src/mappers/stock 에 정의)
import type { StockRow } from "../../src/mappers/stock";
// StockMasterRow / StockQuoteRow 는 Plan 04 가 추가 — 본 Wave 0 에선 shared 의 StockMaster/StockQuote 로 대체해도 무방.

type TopMoverRow = { code: string; rank: number; ranked_at: string; scan_id?: string };

type State = {
  stocks?: StockRow[];                      // 레거시 (기존 scanner.test.ts)
  masters?: StockMaster[];                  // 새 'stocks' 마스터 테이블
  quotes?: StockQuote[];                    // 'stock_quotes' 테이블
  topMovers?: TopMoverRow[];                // 'top_movers' 테이블
  inquirePriceImpl?: (code: string) => Promise<any> | any;  // kisClient 미주입 시에만 사용
  upserts?: { table: string; rows: any[] }[]; // 호출 추적 (Plan 04 테스트가 검증)
};

export function mockSupabase(state: State): SupabaseClient {
  const upsertCalls: { table: string; rows: any[] }[] = [];
  state.upserts = upsertCalls;

  const datasetFor = (t: string): any[] => {
    if (t === "stocks") return state.stocks ?? state.masters ?? [];
    if (t === "stock_quotes") return state.quotes ?? [];
    if (t === "top_movers") return state.topMovers ?? [];
    return [];
  };

  const makeBuilder = (table: string) => {
    let filtered: any[] = [...datasetFor(table)];
    let orderCol: string | null = null;
    let orderAsc = true;
    let limitN: number | null = null;

    const exec = () => {
      let rows = [...filtered];
      if (orderCol) {
        rows.sort((a, b) => {
          const av = (a as any)[orderCol!]; const bv = (b as any)[orderCol!];
          if (typeof av === "string" && typeof bv === "string")
            return orderAsc ? av.localeCompare(bv) : bv.localeCompare(av);
          const an = Number(av) || 0; const bn = Number(bv) || 0;
          return orderAsc ? an - bn : bn - an;
        });
      }
      if (limitN != null) rows = rows.slice(0, limitN);
      return rows;
    };

    const builder: any = {
      select: vi.fn().mockReturnThis(),
      eq: vi.fn().mockImplementation((col: string, val: unknown) => {
        filtered = filtered.filter((r) => (r as any)[col] === val);
        return builder;
      }),
      gte: vi.fn().mockImplementation((col: string, val: number) => {
        filtered = filtered.filter((r) => Number((r as any)[col]) >= Number(val));
        return builder;
      }),
      in: vi.fn().mockImplementation((col: string, vals: any[]) => {
        const set = new Set(vals);
        filtered = filtered.filter((r) => set.has((r as any)[col]));
        return builder;
      }),
      or: vi.fn().mockImplementation((expr: string) => {
        const parts = expr.split(",");
        const patterns = parts.map((p) => {
          const m = p.match(/^(\w+)\.ilike\.%(.*)%$/);
          return m ? { col: m[1], q: m[2].toLowerCase() } : null;
        }).filter(Boolean) as { col: string; q: string }[];
        filtered = filtered.filter((r) =>
          patterns.some((p) => String((r as any)[p.col]).toLowerCase().includes(p.q)),
        );
        return builder;
      }),
      order: vi.fn().mockImplementation((col: string, opts?: { ascending?: boolean }) => {
        orderCol = col; orderAsc = opts?.ascending ?? true; return builder;
      }),
      limit: vi.fn().mockImplementation((n: number) => { limitN = n; return builder; }),
      maybeSingle: vi.fn().mockImplementation(async () => {
        const rows = exec(); return { data: rows[0] ?? null, error: null };
      }),
      upsert: vi.fn().mockImplementation((rows: any) => {
        const arr = Array.isArray(rows) ? rows : [rows];
        upsertCalls.push({ table, rows: arr });
        if (table === "stock_quotes") {
          const existing = state.quotes ?? (state.quotes = []);
          for (const r of arr) {
            const i = existing.findIndex((x) => x.code === r.code);
            if (i >= 0) existing[i] = r as StockQuote;
            else existing.push(r as StockQuote);
          }
        }
        return Promise.resolve({ error: null, data: arr });
      }),
      then: (resolve: any) => resolve({ data: exec(), error: null }),
    };
    return builder;
  };

  return {
    from: vi.fn().mockImplementation((table: string) => makeBuilder(table)),
  } as unknown as SupabaseClient;
}

// kisClient mock — inquirePrice 흐름 테스트용. `impl` 가 throw 하면 axios 에러로 흘러감.
export function mockKisClient(impl: (code: string) => Promise<any>): any {
  return {
    get: vi.fn().mockImplementation(async (_path: string, opts: any) => {
      const code = opts?.params?.fid_input_iscd;
      const output = await impl(code);
      return { data: { rt_cd: "0", msg_cd: "MCA00000", msg1: "OK", output } };
    }),
  };
}
```

구체 규칙:
- 기존 `mockSupabase({stocks: rows})` 호출은 `datasetFor("stocks")` 가 `state.stocks` 우선 반환 → scanner 레거시 테스트 무회귀.
- 새 테스트 (Plan 04 stock-detail/search) 는 `mockSupabase({masters, quotes})` 형태로 호출 → `from("stocks")` 가 `state.masters`, `from("stock_quotes")` 가 `state.quotes` 반환.
- `.upsert({table: "stock_quotes"})` 는 `state.upserts` 에 기록 + `state.quotes` 인-플레이스 반영 → Plan 04 테스트의 `expect(state.upserts.some(...))` 검증 가능.
- **본 Wave 0 에서 완성 구현.** Plan 04 Step C 는 "변경 없음 (Plan 01 구현 재사용)" 으로 축소됨.

11. **server/tests/routes/stock-detail.test.ts** 수정 — RED 시나리오 추가 (Plan 04 가 GREEN):

> **BLOCKER #4 RED runtime 메모:** 아래 신규 케이스는 Plan 01 에서 `it(...)` 로 작성하지만 구현은 Plan 04 가 채운다. 테스트가 **자연스럽게 RED** 가 되는 메커니즘:
> - 엔드포인트 import 는 실제 `../../../src/routes/stocks.ts` 에서 가져온다 (stub import 아님).
> - 현재 `/api/stocks/:code` 핸들러는 여전히 기존 **단일 stocks 테이블 조회** 구현 상태 (Phase 6 산출) 이므로 `masters`/`quotes` 분리 mock 을 주입해도 응답이 새 기대값(예: `price=70500` on-demand, `upserts` 에 `stock_quotes` 기록)과 맞지 않음.
> - 따라서 Plan 04 구현이 들어오기 전 Wave 0 에서 `pnpm -F server test -- --run` 을 돌리면 해당 케이스들은 `expect(response.body.price).toBe(70500)` 나 `expect(state.upserts.some(u => u.table === "stock_quotes")).toBe(true)` 같은 **검증 실패** 로 RED 가 떨어진다 (throw 아닌 assertion-fail).
> - Plan 04 Task 2 (Step B 라우트 재작성 + Step C 는 Plan 01 mock 재사용) 가 완료되면 동일 테스트가 GREEN 으로 전환된다.

```typescript
// 기존 3개 테스트 보존
// 추가:
import { allMasters, samsungQuote } from "../fixtures/stocks";

describe("/api/stocks/:code 마스터 universe + on-demand inquirePrice (Plan 04 구현)", () => {
  it("마스터 존재 + inquirePrice 성공 → 200 + 시세 병합", async () => {
    const inquirePriceImpl = vi.fn().mockResolvedValue({
      stck_mxpr: "91000", stck_llam: "49000", stck_oprc: "70000",
      stck_avls: "418000000000000", acml_tr_pbmn: "900000000000",
      stck_prpr: "70500", prdy_vrss: "500", prdy_ctrt: "0.71",
      acml_vol: "1234567", stck_hgpr: "71000", stck_lwpr: "69500",
    });
    const supa = mockSupabase({ masters: allMasters, quotes: [samsungQuote], inquirePriceImpl });
    const r = await request(createApp({ supabase: supa })).get("/api/stocks/005930");
    expect(r.status).toBe(200);
    expect(r.body.code).toBe("005930");
    expect(r.body.price).toBe(70500);  // on-demand 호출 결과가 cached 보다 우선
    expect(inquirePriceImpl).toHaveBeenCalledWith("005930");
  });

  it("마스터 존재 + inquirePrice 실패 → 200 + cached stock_quotes 폴백", async () => {
    const inquirePriceImpl = vi.fn().mockRejectedValue(new Error("EGW00201"));
    const supa = mockSupabase({ masters: allMasters, quotes: [samsungQuote], inquirePriceImpl });
    const r = await request(createApp({ supabase: supa })).get("/api/stocks/005930");
    expect(r.status).toBe(200);
    expect(r.body.price).toBe(70000);  // cached 값
  });

  it("마스터 부재 → 404 (inquirePrice 미호출)", async () => {
    const inquirePriceImpl = vi.fn();
    const supa = mockSupabase({ masters: allMasters, quotes: [], inquirePriceImpl });
    const r = await request(createApp({ supabase: supa })).get("/api/stocks/000000");
    expect(r.status).toBe(404);
    expect(inquirePriceImpl).not.toHaveBeenCalled();
  });

  it("마스터 존재 + 시세도 cached 도 없음 → 200 + price=0 (em-dash 폴백)", async () => {
    const inquirePriceImpl = vi.fn().mockRejectedValue(new Error("network"));
    const supa = mockSupabase({ masters: allMasters, quotes: [], inquirePriceImpl });
    const r = await request(createApp({ supabase: supa })).get("/api/stocks/999999");
    expect(r.status).toBe(200);
    expect(r.body.price).toBe(0);
  });
});
```

12. 실행 — RED 가 정상 (Plan 04 가 GREEN):
```bash
pnpm -F server test -- --run
```

기존 3개 detail 테스트 + scanner/search/health 는 GREEN 유지. 신규 4개 detail + 2개 rateLimiter + 2개 inquirePrice 테스트는 GREEN 또는 RED. (rateLimiter/inquirePrice 는 본 task 에서 GREEN 가능 — 단순 복제본). detail 4개는 Plan 04 까지 RED.
  </action>

  <verify>
    <automated>pnpm -F server typecheck && pnpm -F server test -- --run --reporter=verbose 2>&1 | tail -50</automated>
  </verify>

  <acceptance_criteria>
    - `ls server/src/kis/{client,inquirePrice,rateLimiter,tokenStore,readOnlyGuard}.ts` 모두 존재
    - `diff <(grep -v "^import" workers/ingestion/src/kis/inquirePrice.ts) <(grep -v "^import" server/src/kis/inquirePrice.ts)` 차이 없음 (본문 동일)
    - `grep "MAX_REQUESTS_PER_SEC = 5" server/src/kis/rateLimiter.ts` 매치
    - `grep "AppConfig" server/src/config.ts | grep -E "kisBaseUrl|kisAppKey|kisAppSecret"` 3개 모두 매치
    - `grep "tr_id.*FHKST01010100" server/src/kis/inquirePrice.ts` 매치
    - `pnpm -F server typecheck` exit 0
    - `pnpm -F server test -- --run --reporter=verbose 2>&1 | grep -c "rateLimiter"` ≥ 2 (테스트 발견)
    - `pnpm -F server test -- --run --reporter=verbose 2>&1 | grep "inquirePrice"` 매치 (테스트 발견)
    - `pnpm -F server test -- --run --reporter=verbose 2>&1 | grep "마스터 부재 → 404"` 매치 (RED 또는 GREEN)
    - **BLOCKER #4 RED runtime:** `pnpm -F server test -- --run stock-detail 2>&1` 출력에 "expected" 또는 "FAIL" 포함된 assertion-fail (throw/import-error 아님) — 즉 테스트가 발견은 되지만 새 마스터 universe 기대값과 기존 핸들러 응답이 달라 RED. Plan 04 완료 후 동일 명령이 GREEN 으로 전환되어야 함.
    - 기존 health/scanner/search 테스트는 모두 GREEN 유지 (회귀 없음)
  </acceptance_criteria>

  <done>server/src/kis/ 5개 파일 복제 완료 + AppConfig 확장 + Wave 0 RED 테스트 4개(stock-detail) 발견. inquirePrice/rateLimiter 단위 테스트는 GREEN 가능.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 외부 KIS API → server kis client | KIS 응답을 server 가 신뢰 후 Supabase 에 upsert |
| Stub 코드 → production import | NOT_IMPLEMENTED stub 가 실수로 prod build 에 포함될 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-01-01 | INFORMATION DISCLOSURE | server/src/kis/tokenStore.ts | mitigate | KIS access_token 을 supabase.kis_tokens 에 RLS 보호된 상태로 저장 (Phase 1 D-17 정책 자동 승계). server 프로세스만 service_role 키로 접근. |
| T-06.1-01-02 | TAMPERING | server/src/kis/readOnlyGuard.ts | mitigate | axios request interceptor 가 매도/매수 endpoint(`/order-cash`, `/order-credit` 등) 호출 시 throw — Phase 1 D-13 패턴 그대로 복제. server 측에서도 동일 가드 필요. |
| T-06.1-01-03 | DENIAL OF SERVICE (rate limit evasion) | server/src/kis/rateLimiter.ts | accept | Pitfall 3 명시 — server 와 ingestion 이 동일 KIS 앱키 공유, 별도 토큰 버킷. on-demand 빈도가 낮아 본 phase 스코프에서 허용. EGW00201 모니터링 필요 시 Upstash 공유 버킷 도입(deferred). |
| T-06.1-01-04 | TAMPERING | workers/master-sync/src/index.ts | mitigate | NOT_IMPLEMENTED stub 는 `process.exit(0)` 로 종료 — 실수로 production Cloud Run Job 으로 배포되어도 데이터 변경 없음. Plan 03 가 실제 로직 구현 후 Plan 06 에서 배포. |
| T-06.1-01-05 | INFORMATION DISCLOSURE | workers/master-sync/src/config.ts | mitigate | KRX_AUTH_KEY 를 process.env 에서만 읽고 .trim() 적용 (Pitfall 6). 절대 logger.info 로 출력 금지 — Plan 03 코드 리뷰 시 확인. |
| T-06.1-01-06 | INJECTION | server/src/kis/inquirePrice.ts | mitigate | stockCode 파라미터를 쿼리 스트링으로 전달하기 전 호출자(stocks.ts route) 가 `^[A-Za-z0-9]{1,10}$` regex 검증 (기존 server/src/routes/stocks.ts:50 패턴 보존). |

</threat_model>

<verification>
- `pnpm install` 후 `@gh-radar/master-sync` workspace 가 등록됨 (`pnpm list -r --depth -1 | grep master-sync`)
- `pnpm -F @gh-radar/shared build && pnpm -F @gh-radar/master-sync typecheck && pnpm -F server typecheck` 모두 exit 0
- `pnpm -F server test -- --run` 실행 시 새 inquirePrice/rateLimiter 테스트 GREEN, stock-detail 신규 4개는 RED 허용 (Plan 04 책임)
- `pnpm -F @gh-radar/master-sync test -- --run` 실행 시 2개 RED 테스트 발견 (Plan 03 책임)
- 기존 server 테스트(scanner/search/health/3개 detail) 회귀 없음
</verification>

<success_criteria>
- 공유 타입 4종 export · master-sync 워크스페이스 스캐폴드 · server kis 5종 복제 · Wave 0 RED 테스트 8개(server 6 + master-sync 2) 발견
- 기존 워크스페이스 회귀 없음
- 어느 production 코드도 변경되지 않음 (Plan 02~06 가 점진 전환)
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-stock-master-universe/06.1-01-SUMMARY.md`
</output>
