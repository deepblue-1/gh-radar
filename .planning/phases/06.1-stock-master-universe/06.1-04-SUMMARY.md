---
phase: 06.1-stock-master-universe
plan: 04
subsystem: server
tags: [route, stocks, search, detail, inquirePrice, 3-table, mapper]
dependency_graph:
  requires: [06.1-01, 06.1-02]
  provides: [server-stocks-route-3table]
  affects: [webapp-search, webapp-detail, scanner]
tech_stack:
  added: []
  patterns: [on-demand-fetch, cached-fallback, kis-client-di]
key_files:
  created:
    - server/src/services/kis-runtime.ts
  modified:
    - server/src/routes/stocks.ts
    - server/src/mappers/stock.ts
    - server/src/app.ts
    - server/src/server.ts
    - server/tests/fixtures/stocks.ts
    - server/tests/fixtures/supabase-mock.ts
    - server/tests/routes/search.test.ts
    - server/tests/routes/stock-detail.test.ts
decisions:
  - KIS 클라이언트는 AppDeps optional DI로 주입 (테스트 시 미주입 가능)
  - StockMasterRow/StockQuoteRow DB row 타입을 mapper에 정의 (shared 타입과 분리)
  - inquirePriceToQuoteRow가 KisInquirePriceRow 타입을 직접 import하여 타입 안전성 확보
metrics:
  duration: 6m
  completed: 2026-04-16T02:21:00Z
  tasks: 2
  files: 9
---

# Phase 06.1 Plan 04: Server Stocks Route 전환 Summary

서버 `/api/stocks/search` + `/api/stocks/:code` 를 새 3-테이블 구조(stocks 마스터 + stock_quotes)로 완전 전환. 검색은 마스터 universe LEFT JOIN 시세, 상세는 마스터 확인 후 on-demand KIS inquirePrice + stock_quotes upsert + cached 폴백.

## Task Completion

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | KIS 런타임 부팅 배선 | bf378ba | kis-runtime.ts, app.ts, server.ts |
| 2 | stocks 라우트 전환 + mappers + 테스트 | 7b65c0b | stocks.ts, stock.ts, fixtures, tests (6 files) |

## Implementation Details

### Task 1: KIS 런타임 부팅 배선
- `server/src/services/kis-runtime.ts`: `createKisRuntime(config, supabase)` — getKisToken + createKisClient 래핑
- `server/src/app.ts`: `AppDeps`에 `kisClient?: AxiosInstance` optional 필드 추가, `app.locals.kisClient` 주입
- `server/src/server.ts`: 부팅 시 try/catch로 KIS 런타임 초기화. 실패해도 cached-only 모드로 서버 기동

### Task 2: stocks 라우트 3-테이블 전환
- **search**: 마스터 universe에서 ilike 검색 → 매치된 code로 stock_quotes LEFT JOIN → `mergeMasterAndQuote` 병합 응답
- **detail**: 마스터 존재 확인(Pitfall 4) → kisClient 있으면 inquirePrice on-demand → stock_quotes upsert → 실패 시 cached 폴백 → cached도 없으면 price=0 (em-dash)
- **mapper**: `mergeMasterAndQuote` + `inquirePriceToQuoteRow` 추가 (기존 `rowToStock` 보존)
- **fixtures**: `StockMasterRow` + `StockQuoteRow` DB row 형식으로 전환 (레거시 StockRow 보존)
- **mock**: `supabase-mock.ts` import를 shared 타입에서 mapper 타입으로 전환

## Test Results

- server 전체: 12 files, 62 tests GREEN
  - search.test.ts: 9 tests (마스터 universe 7 + sanitize 2)
  - stock-detail.test.ts: 6 tests (Wave 0 RED 4종 → GREEN + kisClient 미주입 + 400)
  - scanner, health 등 기존 회귀 없음
- webapp stock-api: 4 tests GREEN (회귀 없음)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] KisInquirePriceRow 타입 호환**
- **Found during:** Task 2 typecheck
- **Issue:** `inquirePriceToQuoteRow`의 인라인 파라미터 타입과 `KisInquirePriceRow`(index signature 포함)이 호환 불가
- **Fix:** `KisInquirePriceRow`를 `../kis/inquirePrice.js`에서 직접 import하여 사용
- **Files modified:** server/src/mappers/stock.ts
- **Commit:** 7b65c0b

**2. [Rule 2 - Missing] supabase-mock 타입 import 변경**
- **Found during:** Task 2 fixtures 변경
- **Issue:** mock이 `@gh-radar/shared`의 camelCase 타입을 사용했으나 라우트는 DB row (snake_case) 기대
- **Fix:** `StockMasterRow` / `StockQuoteRow`를 mapper에서 import, fixtures도 snake_case DB row 형식으로 전환
- **Files modified:** server/tests/fixtures/supabase-mock.ts, server/tests/fixtures/stocks.ts
- **Commit:** 7b65c0b

## Decisions Made

1. **KIS 클라이언트 DI 방식**: `createApp({ kisClient })` optional 주입. RED 테스트가 사용하던 `inquirePriceImpl` mock state 방식을 `mockKisClient` DI로 전환.
2. **DB row 타입 분리**: `StockMasterRow` / `StockQuoteRow`를 mapper 모듈에 정의. shared의 `StockMaster` / `StockQuote`와 별도로 관리 (snake_case DB 스키마 vs camelCase API 응답).
3. **inquirePriceToQuoteRow 타입**: 인라인 대신 `KisInquirePriceRow` import 사용으로 KIS 응답 스키마 변경 시 단일 수정점 보장.

## Self-Check: PASSED

- All 9 created/modified files verified on disk
- Both commits (bf378ba, 7b65c0b) verified in git log
- server typecheck: PASS
- server tests: 12 files, 62 tests GREEN
- webapp stock-api: 4 tests GREEN
