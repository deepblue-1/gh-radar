---
phase: 06.1-stock-master-universe
verified: 2026-04-16T03:13:41Z
status: human_needed
score: 11/11 must-haves verified (automated) + 1 human checkpoint required
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: null
  gaps_closed: []
  gaps_remaining: []
  regressions: []
requirements_verified:
  - SRCH-01  # 마스터 universe 전환 + 삼성전자 검색 PASS
  - SRCH-02  # 검색 자동완성 (회귀)
  - SRCH-03  # 종목 상세 (on-demand inquirePrice)
  - SCAN-01  # 스캐너 리스트 표시 (회귀, top_movers 3-table JOIN)
  - SCAN-02  # 상한가 근접 (회귀, upperLimitProximity)
  - SCAN-03  # 임계값 슬라이더 (UI, 회귀)
  - SCAN-04  # 현재가+등락률+거래대금 (회귀)
  - SCAN-05  # 마켓 뱃지 (회귀)
  - SCAN-06  # 데이터 갱신 시각 (회귀)
  - SCAN-07  # 1분 간격 자동 갱신 (회귀)
  - SCAN-08  # X-Last-Updated-At 서버 DB 기준 (회귀, stock_quotes MAX)
  - INFR-02  # Supabase 스키마 구축 (3-테이블 split + master-sync Cloud Run Job)
human_verification:
  - test: "production webapp 에서 ‘삼성전자’ 검색 자동완성 회귀 (STATE.md:97 사유 해결)"
    expected: "자동완성 목록에 ‘삼성전자 (005930)’ 노출 → 클릭 시 /stocks/005930 상세 페이지 진입 + 시세 표시"
    why_human: "Plan 06 Task 5 checkpoint. production webapp (Vercel) 의 실사용 UX 는 playwright mock 으로 커버 안 됨. DEPLOY-LOG 에 server curl 4건은 PASS 보고됐으나 최종 브라우저 UX 확인은 사람 필요"
---

# Phase 06.1: stock-master-universe Verification Report

**Phase Goal:** 전 종목(KRX 상장 ~2,800종목) 마스터 테이블을 확보해 검색 universe 를 완전 종목으로 확장하고, 기존 `stocks` 의 마스터/시세/랭킹 역할을 3-테이블(`stocks` 마스터 / `stock_quotes` 시세 / `top_movers` 랭킹) 로 분리한다. 검색 API 는 마스터 universe 로 전환하고, 상세 페이지는 진입 시 on-demand KIS `inquirePrice` fetch 로 전환한다. STATE.md 의 '삼성전자 검색 불가' 사유 해결.

**Verified:** 2026-04-16T03:13:41Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                          | Status     | Evidence |
| --- | ---------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | 새 `stocks` 마스터 테이블이 KRX 전 종목 ~2,800종을 보유                                         | VERIFIED   | DEPLOY-LOG Step 2 INV-4 PASS: "Stocks count after backfill: 2771" (KOSPI 950 + KOSDAQ 1821, RESEARCH 추정 ~2,800 일치). 06.1-06-DEPLOY-LOG.md:29 |
| 2   | 기존 `stocks` 가 3-테이블(마스터/시세/랭킹)로 분리됨                                           | VERIFIED   | supabase/migrations/20260415120000 (163 lines, single BEGIN/COMMIT). MIGRATION-VERIFY.md 에 psql \\d 결과 paste: stocks(13컬럼 마스터 메타) / stock_quotes(13컬럼 시세+FK CASCADE) / top_movers(7컬럼 랭킹) 각각 확인 |
| 3   | `/api/stocks/search` 가 마스터 universe 대상 + 삼성전자 검색 PASS                               | VERIFIED   | server/src/routes/stocks.ts:46 `from("stocks")` + stock_quotes LEFT JOIN via IN. DEPLOY-LOG Step 4: `curl /api/stocks/search?q=삼성전자` 2건 응답 (005930 포함). 06.1-06 webapp E2E 신규 시나리오 "삼성전자" 통과 (12/12 GREEN) |
| 4   | `/api/stocks/:code` 가 on-demand KIS inquirePrice 호출 + graceful fallback                      | VERIFIED   | server/src/routes/stocks.ts:102-114 (kisClient 조건 호출 → try/catch → stock_quotes upsert). DEPLOY-LOG: `/api/stocks/005930` 응답에 `price=217000, changeRate=2.84` 확인 (on-demand 성공). server test stock-detail.test.ts 6 tests GREEN (Wave 0 RED 4종 포함) |
| 5   | `/api/scanner` 가 3-테이블 JOIN 으로 동작 + SCAN-08 `X-Last-Updated-At` 헤더 보존               | VERIFIED   | server/src/routes/scanner.ts: top_movers(34) → stock_quotes IN(49) → stocks IN(60) → scannerRowToStock 평탄화. X-Last-Updated-At 헤더(:102) = MAX(stock_quotes.updated_at). DEPLOY-LOG curl 확인: `X-Last-Updated-At: 2026-04-16T02:58:32.646Z`. scanner.test.ts 13 tests GREEN (SCAN-01..08 회귀 포함) |
| 6   | ingestion 파이프라인이 stock_quotes + top_movers 만 쓰기 (stocks 마스터 절대 안 건드림)         | VERIFIED   | `grep from("stocks") workers/ingestion/src/` = 0 matches. workers/ingestion/src/pipeline/upsert.ts: upsertStockQuotes(from("stock_quotes")) + upsertTopMovers(from("top_movers")). run.ts stale cleanup 은 top_movers.delete().lt(ranked_at) 만. workers/ingestion/tests/pipeline/upsert.test.ts 의 "stocks 테이블 절대 안 건드림 (회귀 가드)" 테스트 GREEN |
| 7   | master-sync Cloud Run Job + Scheduler production 가동                                         | VERIFIED   | scripts/deploy-master-sync.sh:43 JOB=gh-radar-master-sync + :117 schedule="10 8 * * 1-5" Asia/Seoul + oauth-service-account-email (OIDC 금지). DEPLOY-LOG Step 2 INV-5 PASS: Scheduler ENABLED + schedule "10 8 * * 1-5" (R1 보정). workers/master-sync/dist/index.js 존재 (Plan 06 Task 4 배포 완료) |
| 8   | webapp E2E 회귀 GREEN (기존 9 + 신규 2 = 11/12)                                                | VERIFIED   | DEPLOY-LOG Step 5: "Total: 12 / Pass: 12 / Fail: 0". search.spec.ts:79 "Phase 06.1 — 마스터 universe 회귀 (SRCH-01)" describe block 추가 + "STATE.md:97 사유 해결" 문자열 명시. FIXTURE_MASTER_UNIVERSE 3종 추가 |
| 9   | master-sync KRX mapper 가 실측 응답 기반 (C1/C2/C3 정정 반영)                                   | VERIFIED   | workers/master-sync/src/pipeline/map.ts:28-41: sector=null(C1) + kosdaqSegment=SECT_TP_NM + securityType=KIND_STKCERT_TP_NM(C2) + securityGroup=SECUGRP_NM. index.ts:54 MIN_EXPECTED_MASTERS=1000 MASS_DELIST guard + :66-93 delist-sweep(C3). master-sync 4 test files, 23 tests GREEN |
| 10  | Plan 02 마이그레이션이 production Supabase 에 적용됨 + FK re-point + RLS 승계                  | VERIFIED   | MIGRATION-VERIFY.md: "supabase db push exit code 0". news_articles/discussions FK 가 새 stocks 가리킴 (Pitfall 1 회피). RLS 3테이블 (anon_read_stocks_master/anon_read_stock_quotes/anon_read_top_movers) 확인. pg_trgm GIN idx_stocks_name_trgm 존재. 부트스트랩 55 row 보존 |
| 11  | 기존 ingestion Cloud Scheduler resume + 새 ingestion/server 재배포 완료                          | VERIFIED   | DEPLOY-LOG Step 3 ingestion redeploy (quotesCount=57, moversCount=57, EGW00201 0건). Step 3.5 ingestion Scheduler ENABLED resume. Step 4 server 재배포 (KIS Secret 주입 + on-demand inquirePrice 동작) |

**Score:** 11/11 truths verified (automated)

Note: 1개 human verification 항목 (아래 "Human Verification Required" 참조) 은 production webapp 의 최종 UX 확인 필요.

### Required Artifacts

| Artifact                                                                 | Expected                                          | Status     | Details |
| ------------------------------------------------------------------------ | ------------------------------------------------- | ---------- | ------- |
| `packages/shared/src/stock.ts`                                           | StockMaster/StockQuote/StockWithQuote 타입 export | VERIFIED   | 69 lines, 3개 타입 export line 23/39/56, 기존 Stock 보존 |
| `workers/master-sync/package.json`                                       | `@gh-radar/master-sync` 워크스페이스 정의         | VERIFIED   | 존재 + pnpm install 통과 |
| `workers/master-sync/Dockerfile`                                         | node:22-alpine 2-stage build (ingestion 미러)     | VERIFIED   | Plan 06 Task 4 에서 `RUN cp -r dist /out/dist` 수정 (pnpm deploy .gitignore 우회). 배포 성공 |
| `workers/master-sync/src/krx/fetchBaseInfo.ts`                           | KOSPI + KOSDAQ 병렬 호출 + 401 early throw        | VERIFIED   | fetchMasterFromKrx line 23, stk_isu_base_info line 30, 401 handling line 37 |
| `workers/master-sync/src/pipeline/map.ts`                                | krxToMasterRow 실측 기반 매핑                     | VERIFIED   | ISU_SRT_CD 필수 체크 line 18, sector=null(C1), kosdaqSegment, securityType from KIND_STKCERT_TP_NM (C2), securityGroup from SECUGRP_NM. parseBigint + Math.trunc(KRX PARVAL 소수값 대응) |
| `workers/master-sync/src/pipeline/upsert.ts`                             | stocks 테이블 upsert (9개 메타 컬럼)             | VERIFIED   | from("stocks").upsert line 32, dedup by code |
| `workers/master-sync/src/index.ts`                                       | MASS_DELIST guard + delist-sweep 전체 파이프라인  | VERIFIED   | MIN_EXPECTED_MASTERS=1000 line 24, delist-sweep line 66-93, log {count, delistedCount} |
| `server/src/kis/inquirePrice.ts`                                         | fetchInquirePrice — ingestion 동일 시그니처     | VERIFIED   | 바이트 복제 from workers/ingestion, server/tests/kis/inquirePrice.test.ts 2 tests GREEN |
| `server/src/kis/rateLimiter.ts`                                          | MAX_REQUESTS_PER_SEC=5 별도 버킷                  | VERIFIED   | 바이트 복제, 2 tests GREEN |
| `server/src/services/kis-runtime.ts`                                     | createKisRuntime 부팅 배선                        | VERIFIED   | getKisToken + createKisClient 래핑. server.ts 에서 try/catch 로 graceful fallback |
| `server/src/routes/stocks.ts`                                            | 마스터 universe search + on-demand detail         | VERIFIED   | from("stocks") + from("stock_quotes") IN + mergeMasterAndQuote + fetchInquirePrice(kisClient) + StockNotFound |
| `server/src/mappers/stock.ts`                                            | mergeMasterAndQuote + inquirePriceToQuoteRow      | VERIFIED   | 기존 rowToStock 보존 + 신규 export |
| `server/src/routes/scanner.ts`                                           | 3-테이블 JOIN + X-Last-Updated-At                 | VERIFIED   | top_movers + stock_quotes + stocks 3단계 쿼리. Cache-Control: no-store. SCAN-08 헤더 |
| `server/src/mappers/scanner.ts`                                          | scannerRowToStock 평탄화                          | VERIFIED   | 신규 mapper 분리 (stock.ts 에서 분리). master 우선, quote 없으면 price=0 |
| `workers/ingestion/src/pipeline/upsert.ts`                               | upsertStockQuotes + upsertTopMovers 분리 쓰기    | VERIFIED   | from("stock_quotes") + from("top_movers") 양쪽 쓰기. from("stocks") 0건 (anti-pattern guard) |
| `workers/ingestion/src/pipeline/run.ts`                                  | stale cleanup top_movers 만                       | VERIFIED   | top_movers.delete().lt(ranked_at, cycleStart). stocks/stock_quotes DELETE 0건 |
| `supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql` | 3-테이블 분리 마이그레이션                        | VERIFIED   | 163 lines, BEGIN/COMMIT 단일 트랜잭션, FK re-point, RLS 승계, pg_trgm |
| `scripts/setup-master-sync-iam.sh`                                       | KRX Secret + SA binding                           | VERIFIED   | bash -n OK, gh-radar-krx-auth-key 등록 |
| `scripts/deploy-master-sync.sh`                                          | Job + Scheduler 배포                              | VERIFIED   | JOB=gh-radar-master-sync, schedule="10 8 * * 1-5", oauth-service-account-email |
| `scripts/smoke-master-sync.sh`                                           | INV-1~6 검증                                      | VERIFIED   | count=exact INV-4 (>=2500) + INV-6 005930 삼성전자 |
| `.planning/phases/06.1-stock-master-universe/06.1-02-MIGRATION-VERIFY.md` | supabase db push 결과 + psql 검증                 | VERIFIED   | 168 lines, schema/FK/RLS/index/row-count 모두 기록 |
| `.planning/phases/06.1-stock-master-universe/06.1-06-DEPLOY-LOG.md`       | 전체 배포 결과                                    | VERIFIED   | Step 1~5 + Sign-off 모든 항목 기록, stocks count 2771 |

### Key Link Verification

| From | To  | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `server/src/routes/stocks.ts (GET /:code)` | `server/src/kis/inquirePrice.ts` | `fetchInquirePrice(kisClient, code)` | WIRED | stocks.ts:104 호출 + try/catch + stock_quotes upsert 후속 처리 |
| `server/src/routes/stocks.ts (GET /:code)` | `stock_quotes` 테이블 | `from("stock_quotes").upsert` | WIRED | stocks.ts:108 upsert + onConflict:"code" |
| `server/src/routes/stocks.ts (GET /search)` | `stocks + stock_quotes` | 2단계 IN 쿼리 | WIRED | stocks.ts:46 from("stocks") or() + :61 from("stock_quotes") in(codes) + mergeMasterAndQuote |
| `server/src/routes/scanner.ts` | `top_movers + stocks + stock_quotes` | 3단계 IN 쿼리 | WIRED | scanner.ts:34 + :49 + :60 각 테이블, scannerRowToStock 평탄화 |
| `workers/ingestion/src/pipeline/upsert.ts` | `stock_quotes + top_movers` | from().upsert() 분리 | WIRED | upsertStockQuotes(stock_quotes) + upsertTopMovers(top_movers). from("stocks") 호출 0건 |
| `workers/master-sync/src/pipeline/upsert.ts` | `stocks` 테이블 | `from("stocks").upsert` | WIRED | upsertMasters line 32 + onConflict:"code" |
| `workers/master-sync/src/index.ts` | `fetchMasterFromKrx + krxToMasterRow + upsertMasters` | import + withRetry 파이프라인 | WIRED | runMasterSync line 37-96: fetch → map → upsert → delist-sweep |
| `Cloud Scheduler gh-radar-master-sync-scheduler` | `Cloud Run Job gh-radar-master-sync` | `--oauth-service-account-email` | WIRED | deploy-master-sync.sh:121/130, DEPLOY-LOG Step 2 INV-5 ENABLED |
| `packages/shared/src/index.ts` | `packages/shared/src/stock.ts` | re-export StockMaster/StockQuote/StockWithQuote | WIRED | Plan 01 Task 1 index.ts 확장 완료, Plan 03~05 의 import 경로 동작 확인 |
| `server/src/server.ts` | `server/src/services/kis-runtime.ts` | `createKisRuntime(config, supabase)` 부팅 호출 | WIRED | try/catch 로 graceful fallback — 실패 시 cached-only mode |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `/api/stocks/search` 응답 | `masterRows + quotes` | Supabase `stocks` + `stock_quotes` (production 2771 row) | Yes | FLOWING (DEPLOY-LOG curl 2건 응답 확인) |
| `/api/stocks/:code` 응답 | `freshQuote` (on-demand) / cached `quote` | KIS inquirePrice (production 호출) / Supabase `stock_quotes` | Yes | FLOWING (DEPLOY-LOG: price=217000 on-demand) |
| `/api/scanner` 응답 | `moverRows + masterByCode + quoteByCode` | Supabase `top_movers` + `stocks` + `stock_quotes` (ingestion cycle ~57 row) | Yes | FLOWING (DEPLOY-LOG: X-Last-Updated-At 실제값) |
| `master-sync Cloud Run Job` upsert | `masters` StockMaster[] | KRX OpenAPI 실측 2771 row | Yes | FLOWING (DEPLOY-LOG: count=2771, delistedCount=0) |
| `ingestion Cloud Run Job` upsert | `allStocks` with priceData | KIS 등락률 순위 + inquirePrice per code | Yes | FLOWING (DEPLOY-LOG Step 3: quotesCount=57, moversCount=57) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| master-sync vitest 23 tests GREEN | `pnpm -F @gh-radar/master-sync test -- --run` | "Test Files 4 passed (4), Tests 23 passed (23)" | PASS |
| server vitest 69 tests GREEN | `pnpm -F server test -- --run` | "Test Files 13 passed (13), Tests 69 passed (69)" | PASS |
| ingestion vitest 26 tests GREEN | `pnpm -F @gh-radar/ingestion test -- --run` | "Test Files 6 passed (6), Tests 26 passed (26)" | PASS |
| webapp 83 tests GREEN | `pnpm -F webapp test -- --run` | "Test Files 14 passed (14), Tests 83 passed (83)" | PASS |
| 3개 배포 스크립트 bash syntax OK | `bash -n scripts/{setup,deploy,smoke}-master-sync.sh` | 3 scripts OK | PASS |
| ingestion anti-pattern guard — `from("stocks")` 0건 | `grep from("stocks") workers/ingestion/src/` | No matches found | PASS |
| master-sync dist 빌드 산출물 존재 | `ls workers/master-sync/dist/` | (Plan 06 에서 build + deploy 완료 확인) | PASS |
| production webapp E2E 회귀 (12종) | 06.1-06-DEPLOY-LOG.md Step 5 | 12/12 GREEN | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| SRCH-01 | 06.1-04, 06.1-06 | 종목명 또는 종목코드로 검색 — 마스터 universe 전환 | SATISFIED | stocks.ts search handler + ilike or() + production curl 결과 2건 + E2E "삼성전자" PASS |
| SRCH-02 | 06.1-04 (간접) | 검색 자동완성 드롭다운 | SATISFIED | Phase 6 의 webapp 자동완성 UI 를 Phase 06.1 이 마스터 universe 로 backend 전환하여 회귀 없이 유지. search.test.ts 9 tests GREEN |
| SRCH-03 | 06.1-04 | 종목 상세 페이지 (on-demand inquirePrice) | SATISFIED | stocks.ts :code handler + fetchInquirePrice + cached fallback + em-dash 폴백. stock-detail.test.ts 6 tests GREEN. production curl: price=217000 |
| SCAN-01 | 06.1-05 | 코스피/코스닥 전 종목 실시간 등락률 리스트 | SATISFIED | scanner.ts 3-테이블 JOIN. scanner.test.ts :79 "200 + 전체 종목 + upperLimitProximity" |
| SCAN-02 | 06.1-05 | 상한가 근접 필터링 | SATISFIED | upperLimitProximity = price/upper_limit in scannerRowToStock. scanner.test.ts 회귀 PASS |
| SCAN-03 | 06.1-05 (UI 변경 없음) | 임계값 슬라이더 (10~29% 범위) | SATISFIED | webapp UI 변경 없음 (backend 호환 유지 — response schema 보존) |
| SCAN-04 | 06.1-05 | 종목별 현재가 + 등락률 + 거래대금 | SATISFIED | response schema Stock + upperLimitProximity 보존. quote 없으면 price=0 (em-dash) |
| SCAN-05 | 06.1-05 | 마켓 뱃지 | SATISFIED | market 필드 scannerRowToStock 에서 master 우선, fallback to mover |
| SCAN-06 | 06.1-05 | 데이터 갱신 시각 | SATISFIED | updatedAt 필드 quote.updated_at 또는 mover.updated_at |
| SCAN-07 | 06.1-05 (ingestion 재배포) | 1분 간격 자동 갱신 (장 시간) | SATISFIED | Cloud Scheduler gh-radar-ingestion-scheduler ENABLED (Step 3.5). DEPLOY-LOG cycle complete 확인 |
| SCAN-08 | 06.1-05 | 갱신시각 서버 DB 기준 표시 | SATISFIED | X-Last-Updated-At 헤더 = MAX(stock_quotes.updated_at). scanner.test.ts :141 "X-Last-Updated-At = MAX(stock_quotes.updated_at) (SCAN-08)" PASS |
| INFR-02 | 06.1-02, 06.1-03, 06.1-06 | Supabase 스키마 구축 (3-테이블 split + master-sync Job) | SATISFIED | Plan 02 migration production 적용. Plan 03 master-sync 파이프라인. Plan 06 Cloud Run Job + Scheduler ENABLED + 2771 백필 |

**Note:** REQUIREMENTS.md traceability table 은 아직 Phase 06.1 를 반영하지 않음 (SRCH-01/02/03 가 "Phase 6 / In Progress" 로 남아 있음). 이는 REQUIREMENTS.md 갱신 누락이며 phase 산출물의 gap 은 아님 — 별도 milestone audit 에서 처리.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| workers/master-sync/src/** | - | `TODO/FIXME/NOT_IMPLEMENTED` | none | grep 0 matches — stub 모두 실제 구현으로 교체됨 |
| server/src/routes/stocks.ts | - | `TODO/FIXME` | none | 0 matches |
| server/src/routes/scanner.ts | - | `TODO/FIXME` | none | 0 matches |
| workers/ingestion/src/pipeline/** | - | `from("stocks")` (마스터 경쟁 쓰기) | none | 0 matches — anti-pattern guard PASSES |

### Human Verification Required

#### 1. production webapp 에서 "삼성전자" 검색 자동완성 회귀 (Phase 06.1 핵심 해결 사유)

**Test:**
1. https://gh-radar-webapp.vercel.app 접속
2. ⌘K 또는 헤더 검색 버튼 → 검색 Dialog 오픈
3. "삼성전자" 입력 → 자동완성 목록 확인
4. 005930 클릭 → /stocks/005930 상세 페이지 진입
5. 시세 (현재가/등락률/거래대금/시총) 표시 확인
6. 다른 종목 spot-check — "SK하이닉스", "카카오", "셀트리온" 모두 자동완성 노출
7. /scanner 페이지 → 등락률 종목 표시 + 갱신시각 (장 시간이면 최근 1분 이내)

**Expected:**
- STATE.md:97 의 "삼성전자 검색 불가" 사유가 완전히 해소되어 자동완성에 005930 삼성전자 노출
- 상세 페이지에서 실시간 시세 (on-demand inquirePrice 호출 결과) 표시 — DEPLOY-LOG 의 price=217000 같은 값
- 마스터 universe 가 작동하여 스캐너 랭킹에 없는 종목도 검색 가능

**Why human:**
- Plan 06 Task 5 checkpoint 가 명시적으로 사용자 수동 검증 요청
- production webapp (Vercel) 의 실사용 UX 는 playwright mock fixture 기반 E2E 로 커버 안 됨 (WARN #8 명시)
- DEPLOY-LOG Step 4 의 curl 4건은 server API 레벨 검증이며, 브라우저 렌더링·네트워크·SSR 경로는 사람 확인 필요
- SUMMARY 와 DEPLOY-LOG 는 "auto-approved (checkpoint)" 로 기록되었으나, 본 verifier 는 자동화된 증거로는 검증 불가능 — 사용자 공식 approval 필요

### Gaps Summary

자동화 가능한 모든 must-have 는 VERIFIED. 구현/배포/테스트 측면의 gap 은 발견되지 않음.

**주요 deviations (auto-fixed, 영향 없음):**
- Dockerfile dist 복사 누락 → `RUN cp -r /app/workers/master-sync/dist /out/dist` 추가 (Plan 06)
- KRX PARVAL 소수값("0.25") bigint 불호환 → `Math.trunc()` 적용 (Plan 06)
- 초기 백필 BAS_DD=20260415 환경변수로 보정 후 제거 (Plan 06)
- Plan 01: server/package.json axios 의존성 누락 + server/tests/setup.ts KIS 환경 변수 누락 → Rule 3 blocking auto-fix
- Plan 02: psql 직접 연결 불가 → Supabase Management API 로 검증 방식 교체 (결과 동일)
- Plan 03: Plan 01/02 prereq 미실행 상황에서 scaffolding 직접 생성 (이후 Plan 01/02 가 완성된 worktree 와 merge 성공)
- Plan 04: KisInquirePriceRow 타입 호환 + supabase-mock 타입 import 경로 → 내부 refactor
- Plan 05: run.ts 반환 타입 `{count}` → `{quotesCount, moversCount}` 변경에 따른 index.ts destructuring 수정

모든 deviation 은 SUMMARY 에 기록되고 커밋에 반영됨. 최종 산출물 정합성에 영향 없음.

**Production metrics (DEPLOY-LOG 에서 확인):**
- stocks (마스터): 2771 rows (KOSPI 950 + KOSDAQ 1821)
- stock_quotes: 57 rows (ingestion 1 cycle 후, 랭킹 대상 종목만 시세 보유)
- top_movers: 57 rows (ingestion cycle 스냅샷)
- master-sync Scheduler: ENABLED, "10 8 * * 1-5" Asia/Seoul
- ingestion Scheduler: ENABLED (resume 완료)
- server `/api/stocks/005930`: price=217000, changeRate=2.84 (on-demand inquirePrice 성공)

---

*Verified: 2026-04-16T03:13:41Z*
*Verifier: Claude (gsd-verifier)*
