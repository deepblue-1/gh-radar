---
phase: 06.1-stock-master-universe
plan: 02
subsystem: database
tags: [postgresql, supabase, migration, pg_trgm, rls, schema-split]

requires:
  - phase: 01-data-foundation
    provides: 초기 stocks/news_articles/discussions 테이블 + RLS 정책
provides:
  - 3-테이블 분리 (stocks 마스터 / stock_quotes 시세 / top_movers 랭킹) production 적용
  - stocks.name pg_trgm GIN 인덱스 (한국어 부분일치 검색)
  - stock_quotes FK -> stocks(code) ON DELETE CASCADE
  - news_articles/discussions FK re-point -> 새 stocks
  - anon SELECT RLS 3테이블 승계
affects: [06.1-03-master-sync, 06.1-04-server-stocks-route, 06.1-05-scanner-and-ingestion, 06.1-06-deploy-backfill]

tech-stack:
  added: [pg_trgm]
  patterns: [3-table-split, single-transaction-migration, fk-re-point-after-rename]

key-files:
  created:
    - supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql
    - .planning/phases/06.1-stock-master-universe/06.1-02-MIGRATION-VERIFY.md
  modified: []

key-decisions:
  - "단일 BEGIN/COMMIT 트랜잭션으로 rename+split+bootstrap 원자적 수행"
  - "Pitfall 1 회피: news_articles/discussions FK 명시적 DROP+ADD로 새 stocks re-point"
  - "부트스트랩: 기존 top_movers(구 stocks) 55 row를 새 stocks에 시드 -- Plan 03 KRX 전종목 백필 전 임시 universe"
  - "Cloud Scheduler PAUSED -- ingestion 컬럼 mismatch 방지, Plan 06에서 resume"

patterns-established:
  - "Supabase migration: single transaction wrap (BEGIN/COMMIT) for multi-step schema changes"
  - "FK re-point after table rename: explicit DROP CONSTRAINT + ADD CONSTRAINT"
  - "RLS policy rename: ALTER POLICY ... RENAME TO after table rename"

requirements-completed: [INFR-02]

duration: 4min
completed: 2026-04-16
---

# Phase 06.1 Plan 02: Migration Split Summary

**stocks 3-테이블 분리 (마스터/시세/랭킹) production Supabase 적용 + pg_trgm GIN + FK re-point + RLS 3테이블 승계**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-16T02:17:21Z
- **Completed:** 2026-04-16T02:21:01Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- 단일 트랜잭션 마이그레이션으로 기존 stocks(마스터+시세+랭킹 혼재)를 stocks(마스터 13컬럼) + stock_quotes(시세 13컬럼) + top_movers(랭킹 7컬럼)로 분리
- production Supabase에 `supabase db push` 성공 적용 (exit code 0)
- Pitfall 1 회피: news_articles/discussions FK를 새 stocks로 명시적 re-point
- pg_trgm GIN 인덱스(idx_stocks_name_trgm)로 한국어 종목명 부분일치 검색 기반 마련
- 부트스트랩으로 55 row 데이터 보존 (stocks=55, stock_quotes=55, top_movers=55)

## Task Commits

Each task was committed atomically:

1. **Task 1: 마이그레이션 SQL 작성** - `fbff1dd` (feat) -- cherry-picked as `2fd97f9`
2. **Task 2: supabase db push + psql 검증 + MIGRATION-VERIFY.md** - `b6d74fa` (docs)

## Files Created/Modified

- `supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql` - 8-step 단일 트랜잭션 마이그레이션 (rename -> split -> bootstrap -> FK -> RLS)
- `.planning/phases/06.1-stock-master-universe/06.1-02-MIGRATION-VERIFY.md` - supabase db push 결과 + 스키마/FK/RLS/인덱스/row count psql 검증 기록

## Decisions Made

- **단일 트랜잭션**: BEGIN/COMMIT으로 8단계를 원자적 수행 -- 어느 단계든 실패 시 자동 ROLLBACK으로 데이터 손실 방지
- **FK re-point**: rename 후 OID가 top_movers를 가리키는 문제를 DROP+ADD CONSTRAINT로 해결
- **Cloud Scheduler PAUSED**: 마이그레이션 중 ingestion이 구 컬럼으로 실행되면 실패 가능 -- Plan 05에서 ingestion 코드 수정 완료 후 Plan 06에서 resume
- **부트스트랩 55 row**: Plan 03 KRX 전종목 백필 전까지 기존 데이터로 FK 제약 충족

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] supabase link 재실행**
- **Found during:** Task 2 (supabase db push)
- **Issue:** worktree에 supabase link가 없어서 `Cannot find project ref` 에러
- **Fix:** `supabase link --project-ref ivdbzxgaapbmrxreyuht` 실행
- **Files modified:** supabase/.temp/ (CLI 내부 상태)
- **Verification:** 이후 `supabase db push` 성공
- **Committed in:** N/A (CLI 상태 파일, git tracked 아님)

**2. [Rule 3 - Blocking] psql 대신 Supabase Management API 사용**
- **Found during:** Task 2 (psql 검증)
- **Issue:** `SUPABASE_DB_URL` 환경변수 미설정, psql 직접 연결 불가
- **Fix:** `https://api.supabase.com/v1/projects/{ref}/database/query` REST API로 모든 검증 쿼리 실행
- **Files modified:** 없음 (검증만)
- **Verification:** 동일한 SQL 쿼리로 동일한 결과 확인

---

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** 검증 방법만 변경(psql -> API). 결과 동일. 스코프 변경 없음.

## Issues Encountered

- `DROP INDEX IF EXISTS idx_top_movers_change_rate_desc` 실행 시 NOTICE 발생 -- 해당 인덱스가 Step 3 컬럼 제거로 이미 무효화되어 존재하지 않았음. `IF EXISTS` 가드로 안전 처리됨.
- top_movers PK 인덱스 이름이 `stocks_pkey`로 잔류 -- PostgreSQL이 rename 시 PK index 이름을 자동 변경하지 않음. 기능에 영향 없음.

## User Setup Required

None - 이 plan은 기존 Supabase 인스턴스에 마이그레이션만 적용.

## Next Phase Readiness

- Plan 03 (master-sync): stocks 마스터 테이블이 준비됨. KRX 전종목 2,771건 INSERT/UPDATE 가능.
- Plan 04 (server-stocks-route): stocks + stock_quotes JOIN 쿼리로 종목 상세 API 제공 가능.
- Plan 05 (scanner-and-ingestion): stock_quotes/top_movers 분리 구조에 맞춰 ingestion 코드 수정 필요.
- **주의:** Cloud Scheduler가 PAUSED 상태 -- Plan 05 ingestion 코드 수정 전까지 재개 금지.

---
*Phase: 06.1-stock-master-universe*
*Completed: 2026-04-16*
