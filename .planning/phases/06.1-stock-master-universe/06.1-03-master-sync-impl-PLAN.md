---
phase: 06.1-stock-master-universe
plan: 03
type: execute
wave: 2
depends_on:
  - 06.1-01
  - 06.1-02
files_modified:
  - workers/master-sync/src/index.ts
  - workers/master-sync/src/krx/fetchBaseInfo.ts
  - workers/master-sync/src/pipeline/map.ts
  - workers/master-sync/src/pipeline/upsert.ts
  - workers/master-sync/src/retry.ts
  - workers/master-sync/tests/krx-client.test.ts
  - workers/master-sync/tests/map.test.ts
  - workers/master-sync/tests/upsert.test.ts
  - workers/master-sync/tests/index.test.ts
autonomous: true
requirements:
  - INFR-02  # master-sync 파이프라인 구현. SRCH-01 webapp 회귀 검증은 Plan 06.

must_haves:
  truths:
    - "fetchMasterFromKrx 가 KOSPI + KOSDAQ 두 엔드포인트를 Promise.all 로 호출하고 합친 KrxBaseInfoRow 배열을 반환한다"
    - "krxToMasterRow 가 KRX 실측 응답(2026-04-16 검증)을 StockMaster 로 변환한다 — C1/C2 정정 반영 (sector 는 항상 NULL, kosdaqSegment 는 SECT_TP_NM, securityType 은 KIND_STKCERT_TP_NM, securityGroup 은 SECUGRP_NM 보존)"
    - "upsertMasters 가 supabase.from('stocks').upsert(rows, {onConflict:'code'}) 를 호출한다 — 신규 9개 메타 컬럼(sector/kosdaq_segment/security_type/security_group/english_name/listing_date/par_value/listing_shares/is_delisted) 모두 매핑"
    - "runMasterSync 가 MASS_DELIST_RISK 가드(응답 row<1000 시 throw) + is_delisted 상향 동기화 분기(응답에 사라진 활성 종목을 is_delisted=true 로 마킹) 를 수행한다"
    - "main() 이 config 로드 → KRX fetch → map → upsert → delist-sweep → log {count,delisted} → exit 0 흐름을 갖는다"
    - "vitest 로 krx-client/map/upsert/main 4종 테스트가 GREEN (실측 응답 기반 fixture, delist-sweep 포함)"
    - "Plan 02 의 새 stocks 마스터 스키마와 컬럼명·타입 정합 (sector/kosdaq_segment/security_type/security_group/english_name/listing_date/par_value/listing_shares/is_delisted)"
    - "본 plan scope 는 KOSPI/KOSDAQ 일반 주식 (stk_isu_base_info + ksq_isu_base_info) 만 동기화. ETF/ETN(etf_isu_base_info/etn_isu_base_info)은 deferred — CONTEXT deferred 와 정합."
  artifacts:
    - path: "workers/master-sync/src/krx/fetchBaseInfo.ts"
      provides: "fetchMasterFromKrx — KRX REST 호출 + market 태깅"
      contains: "stk_isu_base_info"
    - path: "workers/master-sync/src/pipeline/map.ts"
      provides: "krxToMasterRow — KRX row → StockMaster"
      contains: "ISU_SRT_CD"
    - path: "workers/master-sync/src/pipeline/upsert.ts"
      provides: "upsertMasters — Supabase upsert"
      contains: "from(\"stocks\").upsert"
    - path: "workers/master-sync/src/retry.ts"
      provides: "withRetry — exponential backoff (ingestion 패턴 재사용)"
      contains: "withRetry"
    - path: "workers/master-sync/src/index.ts"
      provides: "main 엔트리 — 전체 파이프라인 실행"
      contains: "fetchMasterFromKrx"
  key_links:
    - from: "workers/master-sync/src/index.ts"
      to: "workers/master-sync/src/krx/fetchBaseInfo.ts"
      via: "fetchMasterFromKrx import + 호출"
      pattern: "fetchMasterFromKrx"
    - from: "workers/master-sync/src/pipeline/upsert.ts"
      to: "stocks 테이블 (Plan 02)"
      via: "supabase.from('stocks').upsert(...)"
      pattern: "from\\(\"stocks\"\\)\\.upsert"
---

<objective>
master-sync Cloud Run Job 의 핵심 비즈니스 로직 — KRX OpenAPI 로 KOSPI+KOSDAQ 일반 주식 ~2,771건(실측 2026-04-15 기준: KOSPI 950 + KOSDAQ 1,821)을 가져와 새 stocks 마스터 테이블에 idempotent upsert + 응답에서 사라진 종목을 is_delisted=true 로 마킹한다.

Purpose: D7 (KRX OpenAPI 1차 소스), D8 (Cloud Run Job 일 1회), INFR-02 (마스터 동기화 파이프라인). Plan 06 의 배포 + 백필이 본 plan 의 산출물을 실행한다.

**Scope (C4):** 본 plan 은 KOSPI/KOSDAQ 일반 주식(stk_isu_base_info + ksq_isu_base_info) 만 동기화. ETF/ETN 은 별도 endpoint(etf_isu_base_info/etn_isu_base_info) 이며 CONTEXT deferred 에 정합하여 **본 phase 제외**.

**실측 기반 정정 (06.1-API-VERIFICATION.md §2 + Appendix B, 2026-04-16 검증):**
- **C1:** KRX 응답에 업종(sector) 정보 없음 → `stocks.sector = NULL` 로 유지(옵션 A), `kosdaq_segment` 신규 컬럼에 SECT_TP_NM(KOSDAQ 소속부) 보존. 업종 보강은 후속 phase (KIS `bstp_kor_isnm`).
- **C2:** `securityType` 소스를 `SECUGRP_NM` → `KIND_STKCERT_TP_NM` 로 정정. `SECUGRP_NM` 은 `security_group` 으로 별도 보존 (주권/REIT/투자회사 필터용).
- **C3:** 응답에서 사라진 활성 종목을 `is_delisted=true` 로 업데이트. MASS_DELIST_RISK 가드(응답 row<1000 시 throw).
- **R3:** KrxBaseInfoRow 타입이 실측 12개 필드 전부 커버 (ISU_CD/ISU_SRT_CD/ISU_NM/ISU_ABBRV/ISU_ENG_NM/LIST_DD/MKT_TP_NM/SECUGRP_NM/SECT_TP_NM/KIND_STKCERT_TP_NM/PARVAL/LIST_SHRS).

Output: master-sync 의 4개 src 파일이 stub → 실제 로직, 4종 vitest 테스트 GREEN. KRX `AUTH_KEY` 가 있으면 로컬에서 `pnpm -F @gh-radar/master-sync dev` 로 1회 dry-run 가능 (실제 upsert + delist-sweep 은 Plan 06 백필).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.1-stock-master-universe/06.1-CONTEXT.md
@.planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md
@.planning/phases/06.1-stock-master-universe/06.1-API-VERIFICATION.md
@.planning/phases/06.1-stock-master-universe/06.1-02-MIGRATION-VERIFY.md

# 본 plan 이 채우는 stub
@workers/master-sync/src/index.ts
@workers/master-sync/src/krx/client.ts
@workers/master-sync/src/krx/fetchBaseInfo.ts
@workers/master-sync/src/pipeline/map.ts
@workers/master-sync/src/pipeline/upsert.ts
@workers/master-sync/src/config.ts
@workers/master-sync/src/logger.ts
@workers/master-sync/src/services/supabase.ts
@workers/master-sync/tests/krx-client.test.ts
@workers/master-sync/tests/map.test.ts

# 패턴 미러링 원본
@workers/ingestion/src/pipeline/run.ts
@workers/ingestion/src/pipeline/upsert.ts

<interfaces>
<!-- KRX 응답 필드 (2026-04-16 실측 — 06.1-API-VERIFICATION.md §2 + Appendix B 로 잠김) -->
```typescript
type KrxBaseInfoRow = {
  ISU_CD?: string;              // 표준코드 KR로 시작 12자 (ex: "KR7005930003")
  ISU_SRT_CD?: string;          // 단축코드 6자 — code 필수 (ex: "005930")
  ISU_NM?: string;              // 풀네임 (ex: "삼성전자보통주")
  ISU_ABBRV?: string;           // 약칭 — name 우선 (ex: "삼성전자")
  ISU_ENG_NM?: string;          // 영문명 (선택)
  MKT_TP_NM?: string;           // "KOSPI"/"KOSDAQ" (응답에 있을 수도, market 필드가 권위)
  SECUGRP_NM?: string;          // 증권그룹 — 주권/부동산투자회사(REIT)/투자회사/외국주권/주식예탁증권/사회간접자본투융자회사
  SECT_TP_NM?: string;          // KOSDAQ 소속부 — 중견기업부/우량기업부/벤처기업부/기술성장기업부/SPAC/관리종목 등. KOSPI 는 빈 문자열. ⚠ 업종(sector) 아님.
  KIND_STKCERT_TP_NM?: string;  // 종목구분 — 보통주/구형우선주/신형우선주/종류주권 (C2 정정 — 이게 securityType)
  LIST_DD?: string;             // 상장일 YYYYMMDD
  PARVAL?: string;              // 액면가 (문자열)
  LIST_SHRS?: string;           // 상장주식수 (문자열)
  market: "KOSPI" | "KOSDAQ";   // 호출 엔드포인트로 결정
};
```

<!-- StockMaster (Plan 01 에서 정의 — C1 옵션 A 반영) -->
```typescript
type StockMaster = {
  code: string; name: string; market: Market;
  sector: string | null;           // KRX 응답에 업종 없음 — 본 phase 는 항상 NULL (후속 KIS bstp_kor_isnm 보강)
  kosdaqSegment: string | null;    // KRX SECT_TP_NM (KOSDAQ 소속부). KOSPI NULL.
  securityType: string;            // KRX KIND_STKCERT_TP_NM
  securityGroup: string;           // KRX SECUGRP_NM
  englishName: string | null;      // KRX ISU_ENG_NM
  listingDate: string | null;      // ISO YYYY-MM-DD
  parValue: number | null;         // KRX PARVAL
  listingShares: number | null;    // KRX LIST_SHRS
  isDelisted: boolean; updatedAt: string;
};
```

<!-- Plan 02 의 새 stocks 컬럼 (DB 컬럼명 — upsert row 작성 시 정확히 매칭) -->
-- code text, name text, market text, sector text, kosdaq_segment text,
-- security_type text, security_group text, english_name text,
-- listing_date date, par_value bigint, listing_shares bigint,
-- is_delisted boolean, updated_at timestamptz
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KRX 클라이언트 + fetchMasterFromKrx 구현 + 단위 테스트 GREEN</name>
  <files>
    workers/master-sync/src/krx/fetchBaseInfo.ts,
    workers/master-sync/tests/krx-client.test.ts
  </files>

  <read_first>
    - workers/master-sync/src/krx/client.ts (Plan 01 산출 — AUTH_KEY 헤더 axios 인스턴스)
    - workers/master-sync/src/krx/fetchBaseInfo.ts (Plan 01 stub)
    - workers/master-sync/tests/krx-client.test.ts (Plan 01 RED 테스트)
    - .planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md §"Code Examples" KRX 호출 예시
    - .planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md §"Pitfall 6" (rate limiter 별도 버킷)
  </read_first>

  <behavior>
    - Test: KOSPI + KOSDAQ 두 엔드포인트를 axios.get 으로 호출 (Promise.all)
    - Test: 각 응답의 OutBlock_1 배열을 합쳐 반환, market 필드 태깅
    - Test: 응답이 빈 배열이면 빈 배열 반환 (throw 없음)
    - Test: HTTP 에러 시 axios error propagate (호출자가 retry)
  </behavior>

  <action>
1. **workers/master-sync/src/krx/fetchBaseInfo.ts** — stub 을 실제 구현으로 교체:
```typescript
import type { AxiosInstance } from "axios";

export type KrxBaseInfoRow = {
  ISU_CD?: string;              // 표준코드 KR로 시작 12자
  ISU_SRT_CD?: string;          // 단축코드 6자 — code 필수
  ISU_NM?: string;              // 풀네임
  ISU_ABBRV?: string;           // 약칭 (검색 친화) — name 우선
  ISU_ENG_NM?: string;          // 영문명
  MKT_TP_NM?: string;           // KOSPI/KOSDAQ (응답에 있을 수도)
  SECUGRP_NM?: string;          // 증권그룹 (주권/REIT/투자회사 등) — securityGroup
  SECT_TP_NM?: string;          // KOSDAQ 소속부 (KOSPI 는 빈 문자열) — kosdaqSegment. ⚠ 업종 아님.
  KIND_STKCERT_TP_NM?: string;  // 종목구분 (보통주/우선주 등) — securityType (C2 정정)
  LIST_DD?: string;             // 상장일 YYYYMMDD
  PARVAL?: string;              // 액면가 (문자열)
  LIST_SHRS?: string;           // 상장주식수 (문자열)
  market: "KOSPI" | "KOSDAQ";
};

type KrxResponse = {
  OutBlock_1?: Array<Omit<KrxBaseInfoRow, "market">>;
};

export async function fetchMasterFromKrx(
  client: AxiosInstance,
  basDd: string,
): Promise<KrxBaseInfoRow[]> {
  let kospiRes, kosdaqRes;
  try {
    [kospiRes, kosdaqRes] = await Promise.all([
      client.get<KrxResponse>("/sto/stk_isu_base_info", { params: { basDd } }),
      client.get<KrxResponse>("/sto/ksq_isu_base_info", { params: { basDd } }),
    ]);
  } catch (err: any) {
    // WARN #9 방어: 401 은 AUTH_KEY 미승인 / 잘못된 값 → retry 없이 즉시 명확한 에러 throw
    if (err?.response?.status === 401) {
      throw new Error(
        `KRX 401 — AUTH_KEY 미승인 또는 잘못된 값. openapi.krx.co.kr 에서 stk_isu_base_info + ksq_isu_base_info 서비스 신청 상태 확인 필요.`,
      );
    }
    throw err;
  }
  const kospi = (kospiRes.data.OutBlock_1 ?? []).map((r) => ({
    ...r,
    market: "KOSPI" as const,
  }));
  const kosdaq = (kosdaqRes.data.OutBlock_1 ?? []).map((r) => ({
    ...r,
    market: "KOSDAQ" as const,
  }));
  return [...kospi, ...kosdaq];
}
```

2. **workers/master-sync/tests/krx-client.test.ts** — 기존 1개 RED 테스트 + fetchMasterFromKrx 신규 테스트 추가 (vi.mock axios):
```typescript
import { describe, it, expect, vi } from "vitest";
import { createKrxClient } from "../src/krx/client";
import { fetchMasterFromKrx } from "../src/krx/fetchBaseInfo";

const mkConfig = (over: any = {}) => ({
  krxAuthKey: "test-key",
  krxBaseUrl: "https://example.com",
  supabaseUrl: "x", supabaseServiceRoleKey: "x",
  logLevel: "info", appVersion: "test",
  ...over,
} as any);

describe("createKrxClient", () => {
  it("AUTH_KEY 헤더 + baseURL 설정", () => {
    const c = createKrxClient(mkConfig());
    expect(c.defaults.headers["AUTH_KEY"]).toBe("test-key");
    expect(c.defaults.baseURL).toBe("https://example.com");
  });
});

describe("fetchMasterFromKrx", () => {
  it("KOSPI + KOSDAQ 합쳐서 반환 + market 태깅", async () => {
    const client = {
      get: vi.fn()
        .mockResolvedValueOnce({ data: { OutBlock_1: [
          { ISU_CD: "KR7005930003", ISU_SRT_CD: "005930", ISU_NM: "삼성전자보통주", ISU_ABBRV: "삼성전자",
            ISU_ENG_NM: "SamsungElectronics", MKT_TP_NM: "KOSPI",
            SECUGRP_NM: "주권", SECT_TP_NM: "", KIND_STKCERT_TP_NM: "보통주",
            LIST_DD: "19750611", PARVAL: "100", LIST_SHRS: "5846278608" },
          { ISU_CD: "KR7000660001", ISU_SRT_CD: "000660", ISU_NM: "SK하이닉스보통주", ISU_ABBRV: "SK하이닉스",
            MKT_TP_NM: "KOSPI", SECUGRP_NM: "주권", SECT_TP_NM: "", KIND_STKCERT_TP_NM: "보통주",
            LIST_DD: "19960712" },
        ]}})
        .mockResolvedValueOnce({ data: { OutBlock_1: [
          { ISU_CD: "KR7098120009", ISU_SRT_CD: "098120", ISU_NM: "(주)마이크로컨텍솔루션", ISU_ABBRV: "마이크로컨텍솔",
            MKT_TP_NM: "KOSDAQ", SECUGRP_NM: "주권", SECT_TP_NM: "중견기업부", KIND_STKCERT_TP_NM: "보통주",
            LIST_DD: "20080923", PARVAL: "500", LIST_SHRS: "8312766" },
        ]}}),
    } as any;
    const rows = await fetchMasterFromKrx(client, "20260415");
    expect(rows).toHaveLength(3);
    expect(rows[0].market).toBe("KOSPI");
    expect(rows[2].market).toBe("KOSDAQ");
    expect(client.get).toHaveBeenCalledWith("/sto/stk_isu_base_info", { params: { basDd: "20260415" } });
    expect(client.get).toHaveBeenCalledWith("/sto/ksq_isu_base_info", { params: { basDd: "20260415" } });
  });

  it("OutBlock_1 누락 시 빈 배열 반환", async () => {
    const client = {
      get: vi.fn()
        .mockResolvedValueOnce({ data: {} })
        .mockResolvedValueOnce({ data: {} }),
    } as any;
    const rows = await fetchMasterFromKrx(client, "20260415");
    expect(rows).toEqual([]);
  });

  it("HTTP 에러 propagate", async () => {
    const client = {
      get: vi.fn().mockRejectedValue(new Error("HTTP 401")),
    } as any;
    await expect(fetchMasterFromKrx(client, "20260415")).rejects.toThrow(/HTTP 401/);
  });
});
```

3. 실행:
```bash
pnpm -F @gh-radar/master-sync test -- --run krx-client
```
4개 테스트 모두 GREEN 확인.
  </action>

  <verify>
    <automated>pnpm -F @gh-radar/master-sync test -- --run --reporter=verbose 2>&1 | grep -E "krx-client|fetchMasterFromKrx" | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep "stk_isu_base_info" workers/master-sync/src/krx/fetchBaseInfo.ts` 매치
    - `grep "ksq_isu_base_info" workers/master-sync/src/krx/fetchBaseInfo.ts` 매치
    - `grep "Promise.all" workers/master-sync/src/krx/fetchBaseInfo.ts` 매치
    - `grep "OutBlock_1" workers/master-sync/src/krx/fetchBaseInfo.ts` 매치
    - `grep "NOT_IMPLEMENTED" workers/master-sync/src/krx/fetchBaseInfo.ts` 결과 0건 (stub 제거됨)
    - `pnpm -F @gh-radar/master-sync test -- --run krx-client` exit 0
    - 출력에 "✓" 또는 GREEN 표시가 4개 (createKrxClient 1 + fetchMasterFromKrx 3)
    - **WARN #9:** `grep "status === 401" workers/master-sync/src/krx/fetchBaseInfo.ts` 매치 — 401 응답 시 retry 없이 즉시 명확한 에러 throw (withRetry 가 401 을 3회 반복 호출하지 않도록 호출자가 early throw 로 처리; 본 plan 은 fetchMasterFromKrx 내부에서 throw 하므로 withRetry 는 3회 재시도 후 실패하지만 매 시도마다 401 로 빠르게 실패 — 이는 accept. 필요 시 Plan 후속에서 `isNonRetryable(err)` 로 분리)
  </acceptance_criteria>

  <done>fetchMasterFromKrx 구현 완료, 4개 단위 테스트 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: krxToMasterRow 매핑 + upsertMasters + retry 유틸 + 테스트 GREEN</name>
  <files>
    workers/master-sync/src/pipeline/map.ts,
    workers/master-sync/src/pipeline/upsert.ts,
    workers/master-sync/src/retry.ts,
    workers/master-sync/tests/map.test.ts,
    workers/master-sync/tests/upsert.test.ts
  </files>

  <read_first>
    - workers/master-sync/src/pipeline/map.ts (Plan 01 stub)
    - workers/master-sync/src/pipeline/upsert.ts (Plan 01 stub)
    - workers/master-sync/tests/map.test.ts (Plan 01 RED 테스트)
    - workers/ingestion/src/pipeline/upsert.ts (패턴 미러 — supabase upsert + onConflict)
    - .planning/phases/06.1-stock-master-universe/06.1-CONTEXT.md §"D6" (마스터 메타 범위)
    - .planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md §"Code Examples" KRX → master row 매핑 + §"Anti-Patterns" (마스터 ingestion 경쟁)
    - supabase/migrations/20260415120000_split_stocks_master_quotes_movers.sql (Plan 02 산출 — stocks 컬럼 정확명)
  </read_first>

  <behavior>
    - Test: ISU_SRT_CD → code, ISU_ABBRV (없으면 ISU_NM) → name
    - Test: LIST_DD "19750611" → "1975-06-11" (ISO date)
    - Test: SECUGRP_NM 누락 시 securityType="보통주" 기본
    - Test: SECT_TP_NM 빈 문자열/누락 시 sector=null
    - Test: ISU_SRT_CD 누락 시 throw (필수 필드)
    - Test: upsertMasters 가 빈 배열 입력 시 {count:0} 반환 (호출 안 함)
    - Test: upsertMasters 가 dedup 후 supabase.from('stocks').upsert 호출
    - Test: dedup 동작 — 같은 code 가 2번 들어오면 마지막 row 만 upsert
    - Test: withRetry 3회 시도 + exponential backoff
  </behavior>

  <action>
1. **workers/master-sync/src/retry.ts** — ingestion 패턴 미러 (간소):
```typescript
import { logger } from "./logger";

export async function withRetry<T>(
  fn: () => Promise<T>,
  label: string,
  attempts = 3,
): Promise<T> {
  let lastErr: unknown;
  for (let i = 1; i <= attempts; i++) {
    try {
      return await fn();
    } catch (err) {
      lastErr = err;
      if (i === attempts) break;
      const waitMs = 200 * Math.pow(2, i - 1);
      logger.warn({ label, attempt: i, waitMs, err: (err as Error).message }, "retry");
      await new Promise((r) => setTimeout(r, waitMs));
    }
  }
  throw lastErr;
}
```

2. **workers/master-sync/src/pipeline/map.ts** — stub 교체:
```typescript
import type { Market, StockMaster } from "@gh-radar/shared";
import type { KrxBaseInfoRow } from "../krx/fetchBaseInfo";

function parseListingDate(yyyymmdd: string | undefined): string | null {
  if (!yyyymmdd || yyyymmdd.length !== 8) return null;
  return `${yyyymmdd.slice(0, 4)}-${yyyymmdd.slice(4, 6)}-${yyyymmdd.slice(6, 8)}`;
}

function parseBigint(s: string | undefined): number | null {
  if (!s) return null;
  const n = Number(s);
  return Number.isFinite(n) ? n : null;
}

export function krxToMasterRow(r: KrxBaseInfoRow): StockMaster {
  if (!r.ISU_SRT_CD) {
    throw new Error(`KRX row missing ISU_SRT_CD: ${JSON.stringify(r)}`);
  }
  // C1 옵션 A: KRX 응답에 업종(sector) 정보 없음 — 항상 NULL.
  //           후속 phase 에서 KIS inquirePrice.bstp_kor_isnm 으로 보강.
  //           KOSDAQ 소속부(SECT_TP_NM) 는 kosdaqSegment 로 분리 보존.
  // C2: 종목구분은 KIND_STKCERT_TP_NM (보통주/우선주), SECUGRP_NM 은 증권그룹.
  const kosdaqSegment =
    r.SECT_TP_NM && r.SECT_TP_NM.trim().length > 0 ? r.SECT_TP_NM.trim() : null;
  return {
    code: r.ISU_SRT_CD,
    name: r.ISU_ABBRV ?? r.ISU_NM ?? r.ISU_SRT_CD,
    market: r.market as Market,
    sector: null,                                  // KRX 응답에 업종 정보 없음 (C1)
    kosdaqSegment,                                 // KOSPI 는 null, KOSDAQ 소속부
    securityType: r.KIND_STKCERT_TP_NM ?? "보통주", // C2: KIND_STKCERT_TP_NM (SECUGRP_NM 아님)
    securityGroup: r.SECUGRP_NM ?? "주권",         // C2 보존: 증권그룹
    englishName: r.ISU_ENG_NM ?? null,
    listingDate: parseListingDate(r.LIST_DD),
    parValue: parseBigint(r.PARVAL),
    listingShares: parseBigint(r.LIST_SHRS),
    isDelisted: false,
    updatedAt: new Date().toISOString(),
  };
}
```

3. **workers/master-sync/src/pipeline/upsert.ts** — stub 교체 (ingestion upsertStocks 미러):
```typescript
import type { SupabaseClient } from "@supabase/supabase-js";
import type { StockMaster } from "@gh-radar/shared";
import { logger } from "../logger";

export async function upsertMasters(
  supabase: SupabaseClient,
  rows: StockMaster[],
): Promise<{ count: number }> {
  if (rows.length === 0) return { count: 0 };

  // dedup by code (마지막 값 우선)
  const deduped = new Map<string, StockMaster>();
  for (const r of rows) deduped.set(r.code, r);

  const dbRows = [...deduped.values()].map((m) => ({
    code: m.code,
    name: m.name,
    market: m.market,
    sector: m.sector,
    kosdaq_segment: m.kosdaqSegment,
    security_type: m.securityType,
    security_group: m.securityGroup,
    english_name: m.englishName,
    listing_date: m.listingDate,
    par_value: m.parValue,
    listing_shares: m.listingShares,
    is_delisted: m.isDelisted,
    updated_at: m.updatedAt,
  }));

  const { error } = await supabase
    .from("stocks")
    .upsert(dbRows, { onConflict: "code" });

  if (error) {
    logger.error({ error }, "upsertMasters failed");
    throw error;
  }

  return { count: dbRows.length };
}
```

4. **workers/master-sync/tests/map.test.ts** — 기존 1개 RED 테스트가 GREEN, 추가:
```typescript
import { describe, it, expect } from "vitest";
import { krxToMasterRow } from "../src/pipeline/map";

describe("krxToMasterRow (2026-04-16 실측 응답 기반)", () => {
  it("KOSPI 삼성전자 실측 row — code/name/market/securityType/securityGroup/listingDate 매핑", () => {
    const r = krxToMasterRow({
      ISU_CD: "KR7005930003", ISU_SRT_CD: "005930",
      ISU_NM: "삼성전자보통주", ISU_ABBRV: "삼성전자",
      ISU_ENG_NM: "SamsungElectronics",
      MKT_TP_NM: "KOSPI", SECUGRP_NM: "주권", SECT_TP_NM: "",
      KIND_STKCERT_TP_NM: "보통주",
      LIST_DD: "19750611", PARVAL: "100", LIST_SHRS: "5846278608",
      market: "KOSPI",
    });
    expect(r.code).toBe("005930");
    expect(r.name).toBe("삼성전자");             // ISU_ABBRV 우선
    expect(r.market).toBe("KOSPI");
    expect(r.securityType).toBe("보통주");       // KIND_STKCERT_TP_NM (C2)
    expect(r.securityGroup).toBe("주권");        // SECUGRP_NM 보존 (C2)
    expect(r.sector).toBeNull();                 // C1: KRX 응답에 업종 없음 — 항상 NULL
    expect(r.kosdaqSegment).toBeNull();          // KOSPI 는 SECT_TP_NM 빈 문자열
    expect(r.englishName).toBe("SamsungElectronics");
    expect(r.listingDate).toBe("1975-06-11");
    expect(r.parValue).toBe(100);
    expect(r.listingShares).toBe(5846278608);
    expect(r.isDelisted).toBe(false);
  });

  it("KOSDAQ row — SECT_TP_NM='중견기업부' → kosdaqSegment 채움, sector 는 여전히 NULL", () => {
    const r = krxToMasterRow({
      ISU_CD: "KR7098120009", ISU_SRT_CD: "098120",
      ISU_NM: "(주)마이크로컨텍솔루션", ISU_ABBRV: "마이크로컨텍솔",
      MKT_TP_NM: "KOSDAQ", SECUGRP_NM: "주권", SECT_TP_NM: "중견기업부",
      KIND_STKCERT_TP_NM: "보통주",
      LIST_DD: "20080923", PARVAL: "500", LIST_SHRS: "8312766",
      market: "KOSDAQ",
    });
    expect(r.market).toBe("KOSDAQ");
    expect(r.kosdaqSegment).toBe("중견기업부");
    expect(r.sector).toBeNull();                 // C1: 여전히 NULL
    expect(r.securityGroup).toBe("주권");
    expect(r.securityType).toBe("보통주");
  });

  it("SECUGRP_NM='부동산투자회사' (REIT) 보존", () => {
    const r = krxToMasterRow({
      ISU_SRT_CD: "330590", ISU_ABBRV: "맥쿼리인프라",
      MKT_TP_NM: "KOSPI", SECUGRP_NM: "부동산투자회사", SECT_TP_NM: "",
      KIND_STKCERT_TP_NM: "보통주", market: "KOSPI",
    });
    expect(r.securityGroup).toBe("부동산투자회사");
    expect(r.securityType).toBe("보통주");
  });

  it("KIND_STKCERT_TP_NM='구형우선주' → securityType='구형우선주'", () => {
    const r = krxToMasterRow({
      ISU_SRT_CD: "005935", ISU_ABBRV: "삼성전자우",
      MKT_TP_NM: "KOSPI", SECUGRP_NM: "주권", SECT_TP_NM: "",
      KIND_STKCERT_TP_NM: "구형우선주", market: "KOSPI",
    });
    expect(r.securityType).toBe("구형우선주");
  });

  it("ISU_ABBRV 누락 시 ISU_NM fallback", () => {
    const r = krxToMasterRow({
      ISU_SRT_CD: "999999", ISU_NM: "풀네임만",
      market: "KOSDAQ",
    });
    expect(r.name).toBe("풀네임만");
  });

  it("KIND_STKCERT_TP_NM 누락 시 보통주 기본, SECUGRP_NM 누락 시 '주권' 기본", () => {
    const r = krxToMasterRow({
      ISU_SRT_CD: "111111", ISU_ABBRV: "X", market: "KOSPI",
    });
    expect(r.securityType).toBe("보통주");
    expect(r.securityGroup).toBe("주권");
  });

  it("SECT_TP_NM 빈 문자열 → kosdaqSegment null, sector 는 항상 null", () => {
    const r = krxToMasterRow({
      ISU_SRT_CD: "111111", ISU_ABBRV: "X", SECT_TP_NM: "  ", market: "KOSPI",
    });
    expect(r.kosdaqSegment).toBeNull();
    expect(r.sector).toBeNull();
  });

  it("LIST_DD 누락 시 listingDate null, PARVAL/LIST_SHRS 누락 시 nullable 정수", () => {
    const r = krxToMasterRow({
      ISU_SRT_CD: "111111", ISU_ABBRV: "X", market: "KOSPI",
    });
    expect(r.listingDate).toBeNull();
    expect(r.parValue).toBeNull();
    expect(r.listingShares).toBeNull();
    expect(r.englishName).toBeNull();
  });

  it("ISU_SRT_CD 누락 시 throw", () => {
    expect(() => krxToMasterRow({ market: "KOSPI" } as any)).toThrow(/ISU_SRT_CD/);
  });
});
```

5. **workers/master-sync/tests/upsert.test.ts** — 새로 작성:
```typescript
import { describe, it, expect, vi } from "vitest";
import { upsertMasters } from "../src/pipeline/upsert";
import type { StockMaster } from "@gh-radar/shared";

const mk = (code: string, over: Partial<StockMaster> = {}): StockMaster => ({
  code, name: `name-${code}`, market: "KOSPI",
  sector: null,
  kosdaqSegment: null,
  securityType: "보통주",
  securityGroup: "주권",
  englishName: null,
  listingDate: null,
  parValue: null,
  listingShares: null,
  isDelisted: false,
  updatedAt: "2026-04-15T00:00:00Z",
  ...over,
});

describe("upsertMasters", () => {
  it("빈 배열 → 호출 안 함, count=0", async () => {
    const supa = { from: vi.fn() } as any;
    const res = await upsertMasters(supa, []);
    expect(res.count).toBe(0);
    expect(supa.from).not.toHaveBeenCalled();
  });

  it("정상 row → from('stocks').upsert(rows, {onConflict:'code'})", async () => {
    const upsert = vi.fn().mockResolvedValue({ error: null });
    const supa = { from: vi.fn().mockReturnValue({ upsert }) } as any;
    const res = await upsertMasters(supa, [mk("005930"), mk("000660")]);
    expect(supa.from).toHaveBeenCalledWith("stocks");
    expect(upsert).toHaveBeenCalledWith(
      expect.arrayContaining([
        expect.objectContaining({ code: "005930", name: "name-005930", security_type: "보통주", security_group: "주권", kosdaq_segment: null }),
      ]),
      { onConflict: "code" },
    );
    expect(res.count).toBe(2);
  });

  it("dedup — 같은 code 두 번 → 마지막만 upsert", async () => {
    const upsert = vi.fn().mockResolvedValue({ error: null });
    const supa = { from: vi.fn().mockReturnValue({ upsert }) } as any;
    await upsertMasters(supa, [
      mk("005930", { name: "old" }),
      mk("005930", { name: "new" }),
    ]);
    const passed = upsert.mock.calls[0][0];
    expect(passed).toHaveLength(1);
    expect(passed[0].name).toBe("new");
  });

  it("supabase error → throw", async () => {
    const upsert = vi.fn().mockResolvedValue({ error: new Error("RLS denied") });
    const supa = { from: vi.fn().mockReturnValue({ upsert }) } as any;
    await expect(upsertMasters(supa, [mk("005930")])).rejects.toThrow(/RLS denied/);
  });
});
```

6. 실행:
```bash
pnpm -F @gh-radar/master-sync test -- --run
```
4 + 9 + 4 = 17 테스트 GREEN (map.test 는 실측 기반 9종: 삼성전자/KOSDAQ/REIT/구형우선주/fallback/defaults/empty-segment/null-numerics/throw).
  </action>

  <verify>
    <automated>pnpm -F @gh-radar/master-sync test -- --run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `grep "ISU_SRT_CD" workers/master-sync/src/pipeline/map.ts` 매치
    - `grep "KIND_STKCERT_TP_NM" workers/master-sync/src/pipeline/map.ts` 매치 (C2 — securityType 소스)
    - `grep "SECUGRP_NM" workers/master-sync/src/pipeline/map.ts` 매치 (C2 — securityGroup 보존)
    - `grep "kosdaqSegment" workers/master-sync/src/pipeline/map.ts` 매치 (C1 옵션 A — SECT_TP_NM 의 새 의미)
    - `grep "sector: null" workers/master-sync/src/pipeline/map.ts` 매치 (C1 옵션 A — 항상 NULL)
    - `grep "보통주" workers/master-sync/src/pipeline/map.ts` 매치 (default securityType)
    - `grep "from(\"stocks\").upsert" workers/master-sync/src/pipeline/upsert.ts` 매치
    - `grep "onConflict.*code" workers/master-sync/src/pipeline/upsert.ts` 매치
    - `grep "security_group" workers/master-sync/src/pipeline/upsert.ts` 매치 (신규 DB 컬럼)
    - `grep "kosdaq_segment" workers/master-sync/src/pipeline/upsert.ts` 매치 (신규 DB 컬럼)
    - `grep "NOT_IMPLEMENTED" workers/master-sync/src/pipeline/` 결과 0건
    - `grep "withRetry" workers/master-sync/src/retry.ts` 매치
    - `pnpm -F @gh-radar/master-sync test -- --run` exit 0
    - 출력 마지막에 "Test Files  3 passed (3)" 또는 동등 + 모든 테스트 GREEN (map 9 + upsert 4 + krx-client 4 = 17)
  </acceptance_criteria>

  <done>map/upsert/retry 구현 완료 (실측 기반 + C1 옵션 A + C2 + 9개 신규 DB 컬럼 매핑), 단위 테스트 17개 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: main 엔트리 파이프라인 + integration test</name>
  <files>
    workers/master-sync/src/index.ts,
    workers/master-sync/tests/index.test.ts
  </files>

  <read_first>
    - workers/master-sync/src/index.ts (Plan 01 stub)
    - workers/ingestion/src/index.ts (참조 — non-trading day guard / SIGTERM 핸들러)
    - workers/master-sync/src/{config,logger,services/supabase,krx/client,krx/fetchBaseInfo,pipeline/map,pipeline/upsert,retry}.ts (모두 import)
    - .planning/phases/06.1-stock-master-universe/06.1-RESEARCH.md §"Pattern 3" Cloud Run Job 구조
  </read_first>

  <behavior>
    - Test: main 이 config 로드 → KRX fetch → map → upsert → log {count} → exit 0 흐름
    - Test: KRX fetch 실패 시 withRetry 3회 후 exit 1
    - Test: empty rows → upsert 0 → exit 0 (KRX 가 빈 응답)
    - basDd 는 config.basDd 또는 today (KST) 기본
  </behavior>

  <action>
1. **workers/master-sync/src/index.ts** — 실제 파이프라인:
```typescript
import "dotenv/config";
import { loadConfig } from "./config";
import { logger } from "./logger";
import { createSupabaseClient } from "./services/supabase";
import { createKrxClient } from "./krx/client";
import { fetchMasterFromKrx } from "./krx/fetchBaseInfo";
import { krxToMasterRow } from "./pipeline/map";
import { upsertMasters } from "./pipeline/upsert";
import { withRetry } from "./retry";

function todayBasDdKst(): string {
  // KST = UTC+9. KRX 는 영업일 기준 — 본 worker 는 매일 새벽 호출이라 today 사용 가능.
  // 단, KRX 데이터는 전일 기준 익영업일 갱신이라 어차피 보수적. Plan 06 Scheduler 가 08:10 KST 호출 (KRX 08:00 갱신 이후 10분 마진 — R1 보정).
  const now = new Date();
  const kst = new Date(now.getTime() + 9 * 60 * 60 * 1000);
  const y = kst.getUTCFullYear();
  const m = String(kst.getUTCMonth() + 1).padStart(2, "0");
  const d = String(kst.getUTCDate()).padStart(2, "0");
  return `${y}${m}${d}`;
}

// C3: MASS_DELIST_RISK guard threshold — KRX 정상 응답은 KOSPI+KOSDAQ 합 ~2,771 (실측 2026-04-15).
// 부분 응답/장애로 1000 미만이면 대규모 실수 상장폐지 마킹 위험 → throw.
const MIN_EXPECTED_MASTERS = 1000;

export async function runMasterSync(deps?: {
  config?: ReturnType<typeof loadConfig>;
}): Promise<{ count: number; delistedCount: number }> {
  const config = deps?.config ?? loadConfig();
  const log = logger.child({ app: "master-sync", version: config.appVersion });
  const basDd = config.basDd ?? todayBasDdKst();

  log.info({ basDd }, "master-sync cycle start");

  const supabase = createSupabaseClient(config);
  const krx = createKrxClient(config);

  const krxRows = await withRetry(
    () => fetchMasterFromKrx(krx, basDd),
    "fetchMasterFromKrx",
  );
  log.info({ krxRows: krxRows.length, basDd }, "KRX fetched");

  // WARN: 0 row 는 에러로 처리하되 "서비스 승인 미완료" 가능성을 명시 (신규 배포 직후)
  if (krxRows.length === 0) {
    log.warn(
      { basDd },
      "KRX returned 0 rows — 서비스 승인 미완료 또는 기준일 데이터 없음 (stocks 마스터 미변경)",
    );
    return { count: 0, delistedCount: 0 };
  }

  // C3: MASS_DELIST_RISK 가드 — 응답이 비정상적으로 적으면 상장폐지 마킹 건너뛰고 throw
  if (krxRows.length < MIN_EXPECTED_MASTERS) {
    throw new Error(
      `KRX returned ${krxRows.length} rows (< ${MIN_EXPECTED_MASTERS}) — partial response suspected, aborting to avoid mass-delist. basDd=${basDd}`,
    );
  }

  const masters = krxRows.map(krxToMasterRow);
  const { count } = await withRetry(
    () => upsertMasters(supabase, masters),
    "upsertMasters",
  );

  // C3: delist-sweep — KRX 응답에 없는 활성 종목을 is_delisted=true 로 마킹
  const activeCodes = new Set(masters.map((m) => m.code));
  const { data: existing, error: selErr } = await supabase
    .from("stocks")
    .select("code")
    .eq("is_delisted", false);
  if (selErr) {
    log.error({ err: selErr }, "delist-sweep: select existing active failed");
    throw selErr;
  }
  const toDelist = (existing ?? [])
    .map((r: { code: string }) => r.code)
    .filter((code) => !activeCodes.has(code));

  let delistedCount = 0;
  if (toDelist.length > 0) {
    const nowIso = new Date().toISOString();
    const { error: updErr } = await supabase
      .from("stocks")
      .update({ is_delisted: true, updated_at: nowIso })
      .in("code", toDelist);
    if (updErr) {
      log.error({ err: updErr, delistCount: toDelist.length }, "delist-sweep update failed");
      throw updErr;
    }
    delistedCount = toDelist.length;
    log.info({ delistedCount, sample: toDelist.slice(0, 10) }, "delist-sweep applied");
  }

  log.info({ count, delistedCount, basDd }, "master-sync cycle complete");
  return { count, delistedCount };
}

async function main(): Promise<void> {
  try {
    await runMasterSync();
    process.exit(0);
  } catch (err) {
    logger.error({ err }, "master-sync failed");
    process.exit(1);
  }
}

// CLI 진입점 (vitest import 시에는 실행 안 함)
if (process.argv[1] && process.argv[1].endsWith("index.js")) {
  main();
}
```

(주: top-level guard — vitest 에서 import 시 main() 실행 금지. ingestion 패턴 참조.)

2. **workers/master-sync/tests/index.test.ts** — integration mock:
```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";

vi.mock("../src/krx/fetchBaseInfo", () => ({
  fetchMasterFromKrx: vi.fn(),
}));
vi.mock("../src/pipeline/upsert", () => ({
  upsertMasters: vi.fn(),
}));
vi.mock("../src/services/supabase", () => ({
  createSupabaseClient: vi.fn().mockReturnValue({}),
}));
vi.mock("../src/krx/client", () => ({
  createKrxClient: vi.fn().mockReturnValue({}),
}));

import { runMasterSync } from "../src/index";
import { fetchMasterFromKrx } from "../src/krx/fetchBaseInfo";
import { upsertMasters } from "../src/pipeline/upsert";

const mockFetch = fetchMasterFromKrx as any;
const mockUpsert = upsertMasters as any;

const baseConfig = {
  krxAuthKey: "k", krxBaseUrl: "https://x",
  supabaseUrl: "u", supabaseServiceRoleKey: "s",
  logLevel: "silent", appVersion: "test",
  basDd: "20260415",
} as any;

// Helper — delist-sweep 용 supabase chain mock
function mkSupabaseMock(existingActive: string[] = []) {
  const updateIn = vi.fn().mockResolvedValue({ error: null });
  const update = vi.fn().mockReturnValue({ in: updateIn });
  const eq = vi.fn().mockResolvedValue({ data: existingActive.map((code) => ({ code })), error: null });
  const select = vi.fn().mockReturnValue({ eq });
  const from = vi.fn().mockReturnValue({ select, update });
  return { client: { from } as any, update, updateIn, select };
}

// KRX 실측 fixture row builder (MIN_EXPECTED_MASTERS=1000 가드 통과용 대량 생성)
function mkKrxRows(total: number) {
  return Array.from({ length: total }, (_, i) => ({
    ISU_SRT_CD: String(100000 + i).padStart(6, "0"),
    ISU_ABBRV: `종목${i}`,
    ISU_NM: `종목${i}`,
    MKT_TP_NM: i % 2 === 0 ? "KOSPI" : "KOSDAQ",
    SECUGRP_NM: "주권",
    SECT_TP_NM: i % 2 === 0 ? "" : "중견기업부",
    KIND_STKCERT_TP_NM: "보통주",
    LIST_DD: "20000101",
    market: (i % 2 === 0 ? "KOSPI" : "KOSDAQ") as "KOSPI" | "KOSDAQ",
  }));
}

describe("runMasterSync", () => {
  beforeEach(() => {
    mockFetch.mockReset();
    mockUpsert.mockReset();
  });

  it("정상 흐름 — KRX rows → map → upsert → delist-sweep(0건) → return {count, delistedCount}", async () => {
    const rows = mkKrxRows(1200); // MIN_EXPECTED_MASTERS 통과
    mockFetch.mockResolvedValue(rows);
    mockUpsert.mockResolvedValue({ count: rows.length });
    const supaMock = mkSupabaseMock(rows.map((r) => r.ISU_SRT_CD!)); // 모두 active 로 존재
    const { createSupabaseClient } = await import("../src/services/supabase");
    (createSupabaseClient as any).mockReturnValue(supaMock.client);

    const res = await runMasterSync({ config: baseConfig });
    expect(res.count).toBe(1200);
    expect(res.delistedCount).toBe(0);
    expect(mockFetch).toHaveBeenCalledWith(expect.anything(), "20260415");
    expect(supaMock.updateIn).not.toHaveBeenCalled();
  });

  it("delist-sweep — 응답에 없는 활성 종목을 is_delisted=true 마킹", async () => {
    const rows = mkKrxRows(1200);
    mockFetch.mockResolvedValue(rows);
    mockUpsert.mockResolvedValue({ count: rows.length });
    // 기존 활성: 응답의 1200 + 추가 2개 (→ 2개 delist 대상)
    const existing = [...rows.map((r) => r.ISU_SRT_CD!), "999998", "999999"];
    const supaMock = mkSupabaseMock(existing);
    const { createSupabaseClient } = await import("../src/services/supabase");
    (createSupabaseClient as any).mockReturnValue(supaMock.client);

    const res = await runMasterSync({ config: baseConfig });
    expect(res.delistedCount).toBe(2);
    expect(supaMock.update).toHaveBeenCalledWith(
      expect.objectContaining({ is_delisted: true }),
    );
    expect(supaMock.updateIn).toHaveBeenCalledWith("code", ["999998", "999999"]);
  });

  it("MASS_DELIST_RISK 가드 — KRX 응답 row < 1000 시 throw (delist-sweep 실행 안 됨)", async () => {
    const rows = mkKrxRows(500); // 가드 임계 미만
    mockFetch.mockResolvedValue(rows);
    const supaMock = mkSupabaseMock([]);
    const { createSupabaseClient } = await import("../src/services/supabase");
    (createSupabaseClient as any).mockReturnValue(supaMock.client);

    await expect(runMasterSync({ config: baseConfig })).rejects.toThrow(/partial response|mass-delist/i);
    expect(mockUpsert).not.toHaveBeenCalled();
    expect(supaMock.updateIn).not.toHaveBeenCalled();
  });

  it("KRX 빈 응답 → count=0, delistedCount=0 (sweep 스킵, exit 0)", async () => {
    mockFetch.mockResolvedValue([]);
    const supaMock = mkSupabaseMock([]);
    const { createSupabaseClient } = await import("../src/services/supabase");
    (createSupabaseClient as any).mockReturnValue(supaMock.client);

    const res = await runMasterSync({ config: baseConfig });
    expect(res.count).toBe(0);
    expect(res.delistedCount).toBe(0);
    expect(mockUpsert).not.toHaveBeenCalled();
  });

  it("KRX fetch 실패 (3회 retry 모두 실패) → throw", async () => {
    mockFetch.mockRejectedValue(new Error("HTTP 500"));
    await expect(runMasterSync({ config: baseConfig })).rejects.toThrow(/HTTP 500/);
    expect(mockFetch).toHaveBeenCalledTimes(3);
  });
});
```

3. 실행:
```bash
pnpm -F @gh-radar/master-sync test -- --run
pnpm -F @gh-radar/master-sync typecheck
pnpm -F @gh-radar/master-sync build
```
22개 테스트 GREEN (Task 1: 4 + Task 2: map 9 + upsert 4 + Task 3: index 5).
  </action>

  <verify>
    <automated>pnpm -F @gh-radar/master-sync test -- --run --reporter=verbose 2>&1 | tail -50 && pnpm -F @gh-radar/master-sync build</automated>
  </verify>

  <acceptance_criteria>
    - `grep "fetchMasterFromKrx" workers/master-sync/src/index.ts` 매치
    - `grep "krxToMasterRow" workers/master-sync/src/index.ts` 매치
    - `grep "upsertMasters" workers/master-sync/src/index.ts` 매치
    - `grep "withRetry" workers/master-sync/src/index.ts` 매치 (최소 1회)
    - `grep "basDd" workers/master-sync/src/index.ts` 매치
    - `grep "process.exit" workers/master-sync/src/index.ts` 매치 (성공 0 / 실패 1)
    - `grep "NOT_IMPLEMENTED" workers/master-sync/src/index.ts` 결과 0건
    - **C3:** `grep "MIN_EXPECTED_MASTERS" workers/master-sync/src/index.ts` 매치 (MASS_DELIST_RISK 가드)
    - **C3:** `grep "is_delisted" workers/master-sync/src/index.ts` 매치 (delist-sweep 분기)
    - **C3:** `grep "delistedCount" workers/master-sync/src/index.ts` 매치 (반환값)
    - `pnpm -F @gh-radar/master-sync typecheck` exit 0
    - `pnpm -F @gh-radar/master-sync test -- --run` exit 0 + 22개 테스트 GREEN (index.test.ts 의 MASS_DELIST + delist-sweep + 정상 흐름 3종 포함)
    - `pnpm -F @gh-radar/master-sync build` exit 0 (`workers/master-sync/dist/index.js` 생성)
    - **WARN #9:** `grep "KRX returned 0 rows" workers/master-sync/src/index.ts` 매치 — empty 응답 시 log.warn + count=0 정상 처리 (process.exit 0, throw 아님)
  </acceptance_criteria>

  <done>master-sync 전체 파이프라인 완성 (실측 기반 mapper + MASS_DELIST_RISK 가드 + delist-sweep). 22개 단위/integration 테스트 GREEN. 빌드 산출물 dist/ 생성 → Plan 06 Docker 빌드 준비 완료.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KRX OpenAPI 응답 → upsert payload | KRX row 가 신뢰할 수 없는 데이터를 포함할 수 있음 (예: 빈 ISU_SRT_CD) |
| service_role key → stocks 마스터 쓰기 | RLS 우회 — 잘못된 데이터 upsert 시 전 사용자 영향 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-03-01 | TAMPERING | krxToMasterRow | mitigate | ISU_SRT_CD 누락 시 명시적 throw — bad row 가 stocks 에 들어가는 것을 막음. dedup by code 로 중복 처리. |
| T-06.1-03-02 | INFORMATION DISCLOSURE | logger.info | mitigate | KRX_AUTH_KEY 또는 SUPABASE_SERVICE_ROLE_KEY 를 절대 logger 로 출력 금지 — index.ts 의 log.info 는 basDd/count/krxRows 만 포함. |
| T-06.1-03-03 | DENIAL OF SERVICE | KRX 호출 retry | accept | withRetry 3회 + exponential backoff 200/400/800ms — KRX 일시 장애 흡수. 3회 모두 실패 시 Job exit 1 → Cloud Scheduler 가 다음날 재시도 (일 1회 배치라 무리 없음). |
| T-06.1-03-04 | TAMPERING (스키마 mismatch) | upsertMasters | mitigate | dbRows 의 컬럼명이 Plan 02 마이그레이션과 1:1 매칭 (security_type/listing_date/is_delisted). 잘못된 컬럼명이면 supabase error → throw → Job exit 1. CI 에서 vitest 가 매핑 검증. |
| T-06.1-03-05 | INJECTION | basDd 파라미터 | accept | basDd 는 자체 생성 (todayBasDdKst) 또는 config.basDd (사용자 통제). KRX REST 파라미터로만 사용, SQL 주입 경로 없음. |
| T-06.1-03-06 | TAMPERING (대규모 상장폐지 오탐) | runMasterSync delist-sweep | mitigate | **MASS_DELIST_RISK** — KRX 응답이 일시적으로 비거나 부분 응답(network partial/server 장애)일 때 다량의 활성 종목을 실수로 is_delisted=true 로 마킹. 완화: `if (krxRows.length < MIN_EXPECTED_MASTERS=1000) throw` 가드 — 정상 응답은 ~2,771 이므로 1000 미만은 partial 로 간주하고 Job 실패 처리 → Cloud Scheduler 가 다음날 재시도. index.test.ts 의 MASS_DELIST 테스트가 가드 동작 검증. |

</threat_model>

<verification>
- `pnpm -F @gh-radar/master-sync test -- --run` 22개 테스트 GREEN (krx-client 4 + map 9 + upsert 4 + index 5)
- `pnpm -F @gh-radar/master-sync typecheck` exit 0
- `pnpm -F @gh-radar/master-sync build` 후 `workers/master-sync/dist/index.js` 존재
- KRX_AUTH_KEY 가 로컬에 있다면 (선택) `pnpm -F @gh-radar/master-sync dev` 로 dry-run — Plan 06 가 prod 실행
</verification>

<success_criteria>
- master-sync 4개 src 파일 (fetchBaseInfo/map/upsert/index) + retry.ts 구현 완료 (실측 기반 mapper + MASS_DELIST_RISK 가드 + delist-sweep)
- 22개 vitest GREEN
- Plan 06 가 build → docker → cloud run 배포 가능 상태
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-stock-master-universe/06.1-03-SUMMARY.md`
</output>
