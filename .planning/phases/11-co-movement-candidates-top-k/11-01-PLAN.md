---
phase: 11-co-movement-candidates-top-k
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - supabase/migrations/20260611120000_comovement_tables.sql
  - packages/shared/src/comovement.ts
  - packages/shared/src/index.ts
  - server/src/lib/computeComovement.test.ts
  - server/tests/routes/co-movement.test.ts
  - server/tests/fixtures/supabase-mock.ts
  - workers/co-movement-sync/package.json
  - workers/co-movement-sync/tsconfig.json
  - workers/co-movement-sync/vitest.config.ts
  - workers/co-movement-sync/Dockerfile
  - workers/co-movement-sync/src/config.ts
  - workers/co-movement-sync/src/logger.ts
  - workers/co-movement-sync/src/services/supabase.ts
  - workers/co-movement-sync/tests/config.test.ts
autonomous: true
requirements: [COMV-01]

must_haves:
  truths:
    - "COMV-01 이 REQUIREMENTS.md v1 목록 + Traceability + 커버리지(34) 에 등록됨"
    - "comovement_tables.sql 에 theme_comovement + cosurge_edges 두 테이블 + 부분인덱스 + rebuild_comovement() RPC 가 정의됨"
    - "마이그레이션이 RPC REVOKE 3줄 + 신규 테이블 RLS(TO anon, authenticated) + SET search_path + CHECK(code_a < code_b) 를 포함"
    - "packages/shared 에 CoMovementCandidate / CoMovementResponse 공유 타입이 export 됨 (객체 반환 계약)"
    - "computeComovement.test.ts + co-movement.test.ts 가 fixture 3쌍 + 빈 상태 + db-max-rows mock 을 박제하고 RED(소스 미존재) 상태로 실패"
    - "workers/co-movement-sync 스캐폴드가 candle-sync 1:1 복제(KRX 의존성 제거)로 빌드·테스트 가능"
  artifacts:
    - path: "supabase/migrations/20260611120000_comovement_tables.sql"
      provides: "2 테이블 + idx_ohlcv_surge_bar 부분인덱스 + rebuild_comovement() plpgsql RPC + REVOKE/RLS"
      contains: "CREATE TABLE theme_comovement"
    - path: "packages/shared/src/comovement.ts"
      provides: "CoMovementCandidate / CoMovementEvidence / CoMovementResponse 타입"
      exports: ["CoMovementCandidate", "CoMovementResponse"]
    - path: "server/src/lib/computeComovement.test.ts"
      provides: "결합점수/타이트니스/dedup/후행 fixture 테스트 (RED)"
      min_lines: 60
    - path: "workers/co-movement-sync/src/config.ts"
      provides: "SUPABASE_URL + SERVICE_ROLE_KEY + LOOKBACK_MONTHS config (KRX 제거)"
      contains: "LOOKBACK_MONTHS"
  key_links:
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/comovement"
      via: "type re-export (확장자 없이 — Turbopack lesson)"
      pattern: "from \"./comovement\""
    - from: "supabase/migrations/20260611120000_comovement_tables.sql"
      to: "rebuild_comovement() RPC"
      via: "REVOKE EXECUTE 3줄"
      pattern: "REVOKE EXECUTE ON FUNCTION public.rebuild_comovement"
---

<objective>
Phase 11 (Co-movement Candidates) 의 Wave 0 — 계약·스키마·테스트 스캐폴드를 한 번에 박제한다. 이 플랜은 **인터페이스 우선**: 다운스트림 플랜(02 push, 03 읽기경로, 04 워커, 05 UI)이 소비할 마이그레이션 SQL·공유 타입·RED 테스트·워커 스캐폴드를 먼저 생성해 executor 가 코드베이스를 탐험하지 않고 계약을 받게 한다.

Purpose: COMV-01 정식 등록 + 사전계산 스키마/RPC 확정 + fixture 3쌍 기반 RED 테스트 + co-movement-sync 워크스페이스 스캐폴드(candle-sync 1:1 복제, KRX 의존성 제거).
Output: REQUIREMENTS.md 갱신 · `20260611120000_comovement_tables.sql` · `packages/shared/src/comovement.ts` · server 단위 테스트 2종(RED) · `workers/co-movement-sync/` 스캐폴드.

이 플랜은 SQL 을 production 에 적용하지 않는다 (Plan 02 의 [BLOCKING] db push 가 담당). 여기서는 파일만 작성하고 정적 검증(typecheck/build/RED 테스트)만 수행.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-co-movement-candidates-top-k/11-CONTEXT.md
@.planning/phases/11-co-movement-candidates-top-k/11-RESEARCH.md
@.planning/phases/11-co-movement-candidates-top-k/11-VALIDATION.md

<interfaces>
<!-- executor 가 직접 복제할 선례. 코드베이스 탐험 불필요 — 아래 계약을 그대로 사용. -->

이벤트 소스 `stock_daily_ohlcv` (supabase/migrations/20260512120000 + 20260512123000):
- PK (code, date). `change_rate numeric(10,4)` nullable (사전계산). 이벤트 = `WHERE change_rate` 범위.
- 거래일만 행 존재 (휴장일 row 없음 → conf_d1 은 LEAD(date) 거래일 시퀀스 필수).

테마 `theme_stocks` (supabase/migrations/20260609120000 + 20260610130000):
```
theme_stocks(theme_id uuid, stock_code text, source text, effective_to timestamptz, manual_override text)
  effective_to IS NULL = active 멤버
  manual_override IN ('included','excluded') NULL=auto  (admin override; 'excluded' 는 사전계산에서 제외)
themes(id uuid, name text, is_system boolean, hidden boolean, ...)  hidden=true = tombstone(제외)
```

RPC 권한 패턴 (supabase/migrations/20260514120200_intraday_upsert_close_rpc.sql — verbatim 복제):
```sql
CREATE OR REPLACE FUNCTION public.fn(...) RETURNS ... LANGUAGE plpgsql SECURITY DEFINER AS $$ ... $$;
REVOKE EXECUTE ON FUNCTION public.fn(...) FROM PUBLIC;
REVOKE EXECUTE ON FUNCTION public.fn(...) FROM anon, authenticated;
GRANT  EXECUTE ON FUNCTION public.fn(...) TO service_role;
```

테이블 RLS 패턴 (supabase/migrations/20260609120000_theme_tables.sql — TO anon, authenticated 둘 다):
```sql
ALTER TABLE t ENABLE ROW LEVEL SECURITY;
CREATE POLICY "read_t" ON t FOR SELECT TO anon, authenticated USING (true);
```

SET search_path 규약 (SECURITY DEFINER 함수 — supabase/migrations/20260610130000:50):
```sql
SET search_path = public, pg_temp
```

공유 타입 계약 (packages/shared/src/theme.ts 동형 — Market import from "./stock.js"):
```typescript
import type { Market } from "./stock.js";
export interface CoMovementCandidate {
  code: string; name: string; market: Market;
  liveChangeRate: number | null;    // stock_quotes 실시간 (없으면 null)
  confD0: number;                   // 동반율 (표시 메트릭, 0~1)
  strength: number;                 // 결합 점수 (강도바 width)
  isTrailing: boolean;              // 후행형 배지
  sharedThemes: { id: string; name: string }[];  // 공유 테마 칩
  coSurgeCount: number | null;      // "직접동반 N회" (없으면 null)
  sampleConfidence: "high" | "low"; // 표본수 배지 (ignite_days >= 8 → high)
}
export interface CoMovementResponse { candidates: CoMovementCandidate[]; }
```

워커 스캐폴드 선례 (workers/candle-sync — 1:1 복제):
- package.json: name `@gh-radar/candle-sync` → `@gh-radar/co-movement-sync`, deps {@gh-radar/shared, @supabase/supabase-js, dotenv, pino} (axios 제거 — 외부 HTTP 없음), devDeps {tsx, typescript, vitest, @types/node}.
- tsconfig.json: extends ../../tsconfig.base.json, module commonjs, moduleResolution node, outDir ./dist, rootDir ./src.
- vitest.config.ts: `{ test: { globals: true, passWithNoTests: true } }`.
- Dockerfile: candle-sync Dockerfile verbatim, `candle-sync` → `co-movement-sync` 치환.
- src/logger.ts: pino + redact paths `["*.supabaseServiceRoleKey"]` (krxAuthKey 제거).
- src/services/supabase.ts: `createClient(config.supabaseUrl, config.supabaseServiceRoleKey)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: COMV-01 REQUIREMENTS 등록 + 사전계산 마이그레이션 SQL 작성</name>
  <read_first>
    - .planning/REQUIREMENTS.md (등록 대상 — v1 목록 + Traceability + 커버리지 카운트)
    - .planning/phases/11-co-movement-candidates-top-k/11-RESEARCH.md §스키마 / §SQL 사전계산 (DDL·CTE·HAVING verbatim)
    - supabase/migrations/20260514120200_intraday_upsert_close_rpc.sql (plpgsql + REVOKE 3줄 + SECURITY DEFINER 선례)
    - supabase/migrations/20260609120000_theme_tables.sql (테이블 + 부분인덱스 + RLS TO anon,authenticated 톤)
    - supabase/migrations/20260512120000_create_stock_daily_ohlcv.sql (이벤트 소스 PK)
    - supabase/migrations/20260610130000_theme_admin_overrides.sql (SET search_path + manual_override/hidden 필터)
  </read_first>
  <action>
**1a. REQUIREMENTS.md 에 COMV-01 등록.**
- `## v1 Requirements` 에 새 섹션 추가 (THEME 섹션 다음):
  ```markdown
  ### Co-movement

  - **COMV-01**: 종목 X 급등 시 일봉 통계적 동조(테마-풀링 + co-surge 두 경로 사전계산·병합)로 "따라 오를 후보 Y" 를 점수화해 종목 상세 페이지에 TOP-K 로 표시 — `theme_comovement`/`cosurge_edges` 사전계산 테이블 + `rebuild_comovement()` plpgsql RPC + 야간 1회 `co-movement-sync` 워커 + server `/api/stocks/:code/co-movement` + 종목상세 "동조 후보" 섹션 (Phase 11)
  ```
- `## Traceability` 표에 행 추가: `| COMV-01 | Phase 11 | Pending |`
- `**Coverage:**` 블록: "v1 requirements: 33 total ..." → 34 로 갱신하고 끝에 `; COMV-01 added 2026-06-11 with Phase 11(Co-movement Candidates) → 33→34` 추가. "Mapped to phases: 33" → 34, "Unmapped: 0 ✓" 유지.
- acceptance = ROADMAP Phase 11 의 5개 성공기준.

**1b. `supabase/migrations/20260611120000_comovement_tables.sql` 작성** (RESEARCH §스키마 + §SQL 사전계산 DDL·CTE verbatim). 파일 구조 = theme_tables.sql 톤 (BEGIN/COMMIT, 주석 결정근거):

```sql
BEGIN;

-- 1) theme_comovement (테마-풀링 경로) — RESEARCH §스키마
CREATE TABLE theme_comovement (
  theme_id      uuid NOT NULL REFERENCES themes(id) ON DELETE CASCADE,
  stock_code    text NOT NULL REFERENCES stocks(code) ON DELETE CASCADE,
  ignite_days   int  NOT NULL,                 -- 멤버별 LOO 발화일 수 (R4 — 해당 멤버 제외 ≥2 발화한 날; 게이팅·표본수 배지 분모)
  member_count  int  NOT NULL,                 -- 현재 active 멤버 수 (타이트니스 분모, D-11)
  conf_d0       numeric(5,4) NOT NULL,         -- 동반율 (주 점수, D-12) 0~1
  conf_d1       numeric(5,4) NOT NULL,         -- 익일 후행율 (후행형 판정, D-07)
  lift          numeric(8,4),                  -- conf_d0 / base_rate (디노이즈, nullable)
  avg_ret       numeric(8,4),                  -- 발화일 평균 수익률 (강도)
  computed_at   timestamptz NOT NULL DEFAULT now(),
  PRIMARY KEY (theme_id, stock_code)
);
CREATE INDEX idx_theme_comovement_code  ON theme_comovement (stock_code);
CREATE INDEX idx_theme_comovement_theme ON theme_comovement (theme_id);

-- 2) cosurge_edges (글로벌 co-surge 경로, 무향 code_a<code_b)
CREATE TABLE cosurge_edges (
  code_a       text NOT NULL REFERENCES stocks(code) ON DELETE CASCADE,
  code_b       text NOT NULL REFERENCES stocks(code) ON DELETE CASCADE,
  co_count     int  NOT NULL,                  -- 동반급등 횟수 ("직접동반 N회")
  lift         numeric(8,4),                   -- 독립 대비 초과 동반 (디노이즈)
  avg_pair_ret numeric(8,4),                   -- 페어 평균 수익률 (강도)
  computed_at  timestamptz NOT NULL DEFAULT now(),
  PRIMARY KEY (code_a, code_b),
  CHECK (code_a < code_b)                      -- 무향 정규화 강제
);
CREATE INDEX idx_cosurge_a ON cosurge_edges (code_a);
CREATE INDEX idx_cosurge_b ON cosurge_edges (code_b);

-- 3) 부분 인덱스 — co-surge self-join 커버 (date 선두, change_rate>=10 부분조건)
CREATE INDEX idx_ohlcv_surge_bar
  ON stock_daily_ohlcv (date, code) WHERE change_rate >= 10;

-- 4) RLS — 공개 read (TO anon, authenticated 둘 다 — default-deny 함정 회피)
ALTER TABLE theme_comovement ENABLE ROW LEVEL SECURITY;
ALTER TABLE cosurge_edges    ENABLE ROW LEVEL SECURITY;
CREATE POLICY "read_theme_comovement" ON theme_comovement FOR SELECT TO anon, authenticated USING (true);
CREATE POLICY "read_cosurge_edges"    ON cosurge_edges    FOR SELECT TO anon, authenticated USING (true);

-- 5) rebuild_comovement() — full-rebuild plpgsql (RESEARCH §SQL 사전계산 a/b/c/d)
CREATE OR REPLACE FUNCTION public.rebuild_comovement(
  p_lookback_months int DEFAULT 24
)
RETURNS jsonb
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, pg_temp
AS $$
DECLARE
  v_since date := (current_date - (p_lookback_months || ' months')::interval)::date;
  v_theme_rows int;
  v_edge_rows  int;
BEGIN
  -- ── theme_comovement: 발화일 → 멤버 통계 ──
  TRUNCATE theme_comovement;
  INSERT INTO theme_comovement (theme_id, stock_code, ignite_days, member_count, conf_d0, conf_d1, lift, avg_ret)
  WITH
  -- 종목별 다음 거래일 (휴장일 안전 — 캘린더 +1 금지, Pitfall 1)
  trading_next AS (
    SELECT code, date, LEAD(date) OVER (PARTITION BY code ORDER BY date) AS next_date
    FROM stock_daily_ohlcv WHERE date >= v_since
  ),
  -- 시장 광역일 (>100 종목 ≥10% — D-13). co-surge 와 동일하게 발화일에서도 제외 (R2):
  -- 광역일엔 거의 모든 테마가 베타로 "발화"해 conf_d0 가 일괄 부풀려짐 (RESEARCH §(d) 권고).
  broad_days AS (
    SELECT date FROM stock_daily_ohlcv
    WHERE date >= v_since AND change_rate >= 10
    GROUP BY date HAVING count(*) > 100
  ),
  -- 종목별 기저 급등률 (lift 디노이즈)
  base_rate AS (
    SELECT code,
           count(*) FILTER (WHERE change_rate >= 10)::numeric / NULLIF(count(*), 0) AS p_surge
    FROM stock_daily_ohlcv WHERE date >= v_since GROUP BY code
  ),
  -- active 멤버 (admin override 'excluded' 제외, hidden 테마 제외)
  active_members AS (
    SELECT ts.theme_id, ts.stock_code
    FROM theme_stocks ts
    JOIN themes th ON th.id = ts.theme_id AND th.hidden = false
    WHERE ts.effective_to IS NULL
      AND ts.manual_override IS DISTINCT FROM 'excluded'
  ),
  theme_member_count AS (
    SELECT theme_id, count(*) AS member_count FROM active_members GROUP BY theme_id
  ),
  -- 발화일 = 멤버 >=2 종목이 같은 날 15~31% 급등 (D-13 아티팩트 제외)
  ignite AS (
    SELECT am.theme_id, o.date AS ignite_date,
           count(*) AS ignite_member_cnt              -- 그날 발화 멤버 수 (R4 LOO 판정용)
    FROM active_members am
    JOIN stock_daily_ohlcv o
      ON o.code = am.stock_code AND o.date >= v_since
     AND o.change_rate >= 15 AND o.change_rate <= 31
    WHERE o.date NOT IN (SELECT date FROM broad_days)   -- 광역일 제외 (R2 — D-13 일관 적용)
    GROUP BY am.theme_id, o.date
    HAVING count(*) >= 2
  ),
  -- 멤버×발화일 단위 행 + LOO 판정 플래그 (R4)
  member_day AS (
    SELECT am.theme_id, am.stock_code, i.ignite_date,
           -- LOO: 해당 멤버 자신의 발화 기여(15~31%)를 빼고도 ≥2 종목이 발화한 날만 그 멤버의 표본.
           -- o0 가 NULL(그날 미거래)이면 CASE=0 → 발화 멤버 ≥2 그대로 → 유효 표본 (Y 미동반으로 집계)
           (i.ignite_member_cnt
              - CASE WHEN o0.change_rate >= 15 AND o0.change_rate <= 31 THEN 1 ELSE 0 END) >= 2 AS loo_day,
           o0.change_rate AS d0_rate,
           o1.change_rate AS d1_rate
    FROM active_members am
    JOIN ignite i ON i.theme_id = am.theme_id
    LEFT JOIN stock_daily_ohlcv o0 ON o0.code = am.stock_code AND o0.date = i.ignite_date
    LEFT JOIN trading_next tn      ON tn.code = am.stock_code AND tn.date = i.ignite_date
    LEFT JOIN stock_daily_ohlcv o1 ON o1.code = am.stock_code AND o1.date = tn.next_date
  ),
  -- 멤버별 D0 동반 + D+1 후행 + avg_ret — 전부 LOO 발화일 기준 (R4):
  -- 자기발화 포함 시 변동성 큰 테마 리더의 동반율이 구조적으로 부풀어 TOP-K 왜곡.
  -- "Y 없이도 테마가 발화했을 때 Y 가 동반한 비율"이 '따라갈 종목'의 정확한 정의.
  member_stats AS (
    SELECT
      theme_id, stock_code,
      count(*) FILTER (WHERE loo_day)                        AS ignite_days,  -- 멤버별 LOO 분모
      count(*) FILTER (WHERE loo_day AND d0_rate >= 10)      AS d0_co,
      count(*) FILTER (WHERE loo_day AND d1_rate >= 10)      AS d1_co,
      avg(d0_rate) FILTER (WHERE loo_day AND d0_rate >= 10)  AS avg_ret
    FROM member_day
    GROUP BY theme_id, stock_code
  )
  SELECT
    ms.theme_id, ms.stock_code, ms.ignite_days, tmc.member_count,
    (ms.d0_co::numeric / NULLIF(ms.ignite_days, 0))::numeric(5,4)            AS conf_d0,
    (ms.d1_co::numeric / NULLIF(ms.ignite_days, 0))::numeric(5,4)            AS conf_d1,
    ((ms.d0_co::numeric / NULLIF(ms.ignite_days, 0))
       / NULLIF(br.p_surge, 0))::numeric(8,4)                               AS lift,
    ms.avg_ret::numeric(8,4)                                                AS avg_ret
  FROM member_stats ms
  JOIN theme_member_count tmc ON tmc.theme_id = ms.theme_id
  LEFT JOIN base_rate br ON br.code = ms.stock_code
  WHERE ms.ignite_days >= 5;        -- 적재 컷 ≥5 — 멤버별 LOO 발화일 기준 (노출 최저, D-04). ≥8 은 읽기 시 배지 (§6). 분모 0 행 자동 배제
  GET DIAGNOSTICS v_theme_rows = ROW_COUNT;

  -- ── cosurge_edges: 페어 동반급등 (광역일 제외) ──
  TRUNCATE cosurge_edges;
  INSERT INTO cosurge_edges (code_a, code_b, co_count, lift, avg_pair_ret)
  WITH
  broad_days AS (   -- 시장 광역일 (>100 종목 동반) 제외 (D-13, Pitfall 2)
    SELECT date FROM stock_daily_ohlcv
    WHERE date >= v_since AND change_rate >= 10
    GROUP BY date HAVING count(*) > 100
  ),
  daily_bars AS (
    SELECT o.date, o.code, o.change_rate
    FROM stock_daily_ohlcv o
    -- 적격성 JOIN (R1): FK 고아 가드(ohlcv→stocks FK NOT VALID) 겸
    -- 상폐·스팩·ETP 제외 — isThemeEligible(89ca729, upsertThemes.ts) 기준과 정확히 일치.
    -- 미필터 시 스팩 동반 상한가 클러스터·상폐 티커가 동조 후보로 노출됨
    -- (테마 경로는 theme_stocks 가 이미 필터됨 — co-surge 만 raw ohlcv 라 필수).
    -- 주의: kosdaq_segment 는 KOSPI 에서 NULL → NOT LIKE 단독 사용 금지(IS NULL OR 필수).
    JOIN stocks s ON s.code = o.code
      AND s.is_delisted = false
      AND s.security_group NOT IN ('ETF', 'ETN', 'ELW')
      AND (s.kosdaq_segment IS NULL OR s.kosdaq_segment NOT LIKE 'SPAC%')
      AND s.name NOT LIKE '%스팩%'
    WHERE o.date >= v_since AND o.change_rate >= 10
      AND o.date NOT IN (SELECT date FROM broad_days)
  ),
  total_days AS (SELECT count(DISTINCT date) AS n FROM daily_bars),
  surge_count AS (SELECT code, count(*) AS n FROM daily_bars GROUP BY code)
  SELECT
    a.code AS code_a, b.code AS code_b,
    count(*) AS co_count,
    -- lift = 관측 동반 / 독립 가정 기대 동반 (sc_a*sc_b/total_days)
    (count(*)::numeric
      / NULLIF((sa.n::numeric * sb.n::numeric) / NULLIF(td.n, 0), 0))::numeric(8,4) AS lift,
    avg((a.change_rate + b.change_rate) / 2)::numeric(8,4)                           AS avg_pair_ret
  FROM daily_bars a
  JOIN daily_bars b ON a.date = b.date AND a.code < b.code
  JOIN surge_count sa ON sa.code = a.code
  JOIN surge_count sb ON sb.code = b.code
  CROSS JOIN total_days td
  GROUP BY a.code, b.code, sa.n, sb.n, td.n
  HAVING count(*) >= 3;             -- 적재 컷 ≥3 (노출 최저, D-04). ≥5 강함은 읽기 시 (§6)
  GET DIAGNOSTICS v_edge_rows = ROW_COUNT;

  RETURN jsonb_build_object(
    'lookback_since', v_since,
    'theme_comovement_rows', v_theme_rows,
    'cosurge_edge_rows', v_edge_rows,
    'rebuilt_at', now()
  );
END;
$$;

REVOKE EXECUTE ON FUNCTION public.rebuild_comovement(int) FROM PUBLIC;
REVOKE EXECUTE ON FUNCTION public.rebuild_comovement(int) FROM anon, authenticated;
GRANT  EXECUTE ON FUNCTION public.rebuild_comovement(int) TO service_role;

COMMIT;
```

주의: 위 lift 식(co-surge)·계수는 RESEARCH §Open Q 2 의 "정확식 plan 확정" 대상 — Plan 02 의 [BLOCKING] fixture sanity 가 검증·조정한다. 본 task 는 RESEARCH 권고식 그대로 작성. 이 task 는 **파일 작성만** — push 는 Plan 02 [BLOCKING].
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE theme_comovement\|CREATE TABLE cosurge_edges\|CREATE INDEX idx_ohlcv_surge_bar\|REVOKE EXECUTE ON FUNCTION public.rebuild_comovement\|SET search_path = public, pg_temp\|CHECK (code_a < code_b)\|TO anon, authenticated" supabase/migrations/20260611120000_comovement_tables.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep "COMV-01" .planning/REQUIREMENTS.md` 가 v1 Co-movement 섹션 + Traceability 행 둘 다 매치 (≥2 라인).
    - `.planning/REQUIREMENTS.md` 커버리지가 "34 total" + "Mapped to phases: 34" + "Unmapped: 0" 포함.
    - `supabase/migrations/20260611120000_comovement_tables.sql` 에 `CREATE TABLE theme_comovement` 와 `CREATE TABLE cosurge_edges` 둘 다 존재.
    - 동일 파일에 `REVOKE EXECUTE ON FUNCTION public.rebuild_comovement` 가 정확히 2회(PUBLIC + anon,authenticated) + `GRANT  EXECUTE ON FUNCTION public.rebuild_comovement(int) TO service_role` 1회 존재.
    - 동일 파일에 `CREATE POLICY "read_theme_comovement" ... TO anon, authenticated` 와 `CREATE POLICY "read_cosurge_edges" ... TO anon, authenticated` 존재 (RLS 두 테이블).
    - 동일 파일에 `CREATE INDEX idx_ohlcv_surge_bar ON stock_daily_ohlcv (date, code) WHERE change_rate >= 10` 존재.
    - 동일 파일에 `SET search_path = public, pg_temp` + `CHECK (code_a < code_b)` + `o.change_rate >= 15 AND o.change_rate <= 31` + `HAVING count(*) > 100` (광역일) 존재.
    - 동일 파일에 `manual_override IS DISTINCT FROM 'excluded'` + `th.hidden = false` (admin override 제외) 존재.
    - 동일 파일에 `ms.ignite_days >= 5` (멤버별 LOO 적재 컷) + `HAVING count(*) >= 3` (co-surge 적재 컷) 존재.
    - 동일 파일에 `loo_day` + `ignite_member_cnt` (R4 leave-one-out — 자기발화 제외 분모) 존재, `ignite_count AS` CTE 미존재 (member_day/member_stats 로 대체).
    - 동일 파일에 cosurge daily_bars 적격성 JOIN 존재 — `is_delisted = false` + `NOT IN ('ETF', 'ETN', 'ELW')` + `NOT LIKE 'SPAC%'` + `NOT LIKE '%스팩%'` + `kosdaq_segment IS NULL OR` (KOSPI NULL 처리) 전부 매치 (R1 — FK 고아 가드 겸 상폐·스팩·ETP 제외).
    - 동일 파일에 `NOT IN (SELECT date FROM broad_days)` 가 **2회** 존재 (발화일 ignite CTE + cosurge daily_bars — R2 광역일 일관 제외), `HAVING count(*) > 100` (broad_days 정의) 도 2회 (각 INSERT 의 WITH).
  </acceptance_criteria>
  <done>
    COMV-01 이 REQUIREMENTS.md 에 등록되고 커버리지 34. 마이그레이션 SQL 이 2 테이블 + 부분인덱스 + RPC + REVOKE 3줄 + RLS(anon,authenticated) + SET search_path + 이벤트/광역일/적재컷 로직을 모두 포함. push 미실행.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 공유 타입(comovement.ts) + RED 단위 테스트 2종 (computeComovement / 읽기 라우트)</name>
  <read_first>
    - packages/shared/src/theme.ts (CoMovementCandidate 동형 — Market import from "./stock.js", 객체 반환 계약)
    - packages/shared/src/index.ts (re-export — 확장자 없이, Turbopack lesson)
    - server/src/lib/computeTop3.ts (순수함수 패턴 — NaN 가드 + 상위N)
    - server/tests/routes/themes.test.ts (라우트 테스트 구조 — request(createApp({supabase})) + mockSupabase)
    - server/tests/fixtures/supabase-mock.ts (db-max-rows 1000 시뮬 + .range()/.in()/.eq()/.is() 지원)
    - .planning/phases/11-co-movement-candidates-top-k/11-RESEARCH.md §검증 fixture (3쌍 ground truth) / §읽기 경로 / §두 경로 결합 / §후행형 판정
    - .planning/phases/11-co-movement-candidates-top-k/11-CONTEXT.md (D-03 evidence 분리, D-08 후행, D-10 결합점수)
  </read_first>
  <behavior>
**computeComovement.test.ts** — 순수 함수 `computeComovement(themeRows, cosurgeRows, anchorThemes, quoteByCode, k)` (Plan 03 이 구현, 여기서는 RED). fixture 3쌍 박제:
  - Test A — 한국석유(004090) 앵커: 흥구석유(024060) 가 테마 경로(conf_d0 높음) + co-surge 9회 → 둘 다 evidence (공유 테마 칩 + coSurgeCount=9), 결합 점수 상위, candidates[0].code === '024060'.
  - Test B — 타이트니스: 작은 테마(member_count 3) 멤버가 큰 테마(member_count 70) 멤버보다 conf_d0 동일 시 strength 더 큼 (`1/sqrt(member_count)` 가중).
  - Test C — 휴림 계열(078590↔090710): co-surge 경로만(테마 없음) → coSurgeCount 채워지고 sharedThemes=[] (D-03 분리).
  - Test D — 후행형: conf_d1 > conf_d0 AND conf_d1 >= 0.3 인 후보만 isTrailing=true.
  - Test E — dedup: 한 종목이 양쪽 경로에 나오면 candidates 에 1번만 (code key), evidence 합집합.
  - Test F — sampleConfidence: ignite_days >= 8 → 'high', 5~7 → 'low'.
  - Test G — k 클램프: k=8 이면 최대 8개, 결합점수 desc 정렬.
  - Test H — 앵커 참여도 가중 (R3): 같은 conf_d0·member_count 인 두 후보가 각각 다른 테마 소속이고, 앵커의 conf_d0 가 테마1 에서 높고 테마2 에서 0 근처면 → 테마1 후보의 strength 가 더 큼 (anchor_rel = sqrt(ANCHOR_REL_FLOOR + (1-ANCHOR_REL_FLOOR)·anchor_conf_d0)). themeRows 에 앵커 자신의 행을 포함시켜 anchor_conf_d0 추출 검증.

**co-movement.test.ts** — 읽기 라우트 `GET /api/stocks/:code/co-movement` (Plan 03 이 구현, 여기서는 RED). mockSupabase 사용:
  - Test 1: 응답이 **객체** `{ candidates: [...] }` (배열 아님 — 계약 드리프트 lesson). `Array.isArray(res.body)` === false, `Array.isArray(res.body.candidates)` === true.
  - Test 2: theme_comovement + cosurge_edges + stock_quotes 조인 후 candidates 에 liveChangeRate 포함.
  - Test 3: 후보 0 (앵커 테마 없음 + co-surge 이웃 없음) → `{ candidates: [] }`.
  - Test 4: k 쿼리 클램프 — `?k=999` → 최대 50 (Math.min(k,50)).
  - Test 5: db-max-rows mock — theme_comovement 멤버 1000 초과 시 .range() 페이지네이션으로 전수 수집 (절단 회귀 가드).
  </behavior>
  <action>
**2a. `packages/shared/src/comovement.ts` 작성** (interfaces 블록 verbatim):
```typescript
import type { Market } from "./stock.js";

/** 동조 후보 1개 — server·webapp 공유 (apiFetch<CoMovementResponse> 계약). */
export interface CoMovementCandidate {
  code: string;
  name: string;
  market: Market;
  liveChangeRate: number | null;
  confD0: number;
  strength: number;
  isTrailing: boolean;
  sharedThemes: { id: string; name: string }[];
  coSurgeCount: number | null;
  sampleConfidence: "high" | "low";
}

/** GET /api/stocks/:code/co-movement 응답 — **객체**(배열 아님, 계약 드리프트 회피). */
export interface CoMovementResponse {
  candidates: CoMovementCandidate[];
}
```

**2b. `packages/shared/src/index.ts` 에 re-export 추가** (확장자 없이 — Turbopack `.js`→`.ts` resolve lesson, type-only 라 런타임 값 아님이지만 통일):
```typescript
export type { CoMovementCandidate, CoMovementResponse } from "./comovement";
```

**2c. `server/src/lib/computeComovement.test.ts` 작성** (RED — `computeComovement` 미존재). behavior 의 Test A~G 를 vitest 로 구현. fixture 상수:
```typescript
// RESEARCH §검증 fixture — ground truth (probe 2026-06-11)
// 한국석유 004090 ↔ 흥구석유 024060: D0 동반 7, ≥10% 바 동반 9
// 광전자 017900 ↔ 이노 215790: 12
// 휴림에이텍 078590 ↔ 휴림로봇 090710: 9 (테마 미태깅 가능 → co-surge only)
```
import 는 `import { computeComovement } from "./computeComovement";` (구현 파일 없어 RED). 입력 타입은 Plan 03 이 확정할 row shape 를 테스트가 먼저 정의 — `ThemeComovementRow`/`CosurgeEdgeRow` 형태(snake_case: theme_id, stock_code, conf_d0, conf_d1, lift, avg_ret, ignite_days, member_count / code_a, code_b, co_count, lift, avg_pair_ret)를 테스트 내 헬퍼로 생성. 반환은 `CoMovementCandidate[]`.

**2d. `server/tests/routes/co-movement.test.ts` 작성** (RED — 라우트 미존재). themes.test.ts 구조 복제: `request(createApp({ supabase: mockSupabase({...}) })).get("/api/stocks/004090/co-movement?k=8")`. mockSupabase state 에 `themeComovement` / `cosurgeEdges` 데이터셋을 추가해야 하므로 — supabase-mock 의 `State`/`datasetFor` 에 `themeComovement?` + `cosurgeEdges?` 키 + `theme_comovement`/`cosurge_edges` 테이블 매핑을 추가(최소 확장, themes 패턴 동형). behavior Test 1~5 구현.

주의: RED 가 정상 — 구현(computeComovement.ts, 라우트, app 등록)은 Plan 03. 이 task 는 테스트가 **소스 미존재로 실패**(import 에러 또는 404)함을 확인하면 done. `pnpm -F @gh-radar/shared build` 는 통과해야 함(타입만 추가).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm -F @gh-radar/shared build && (pnpm -F @gh-radar/server test computeComovement 2>&1 | grep -qiE "fail|cannot find|no test found|error" && echo "RED-OK") && grep -q "candidates" server/tests/routes/co-movement.test.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "CoMovementCandidate|CoMovementResponse" packages/shared/src/comovement.ts` 가 두 interface export 매치.
    - `grep "from \"./comovement\"" packages/shared/src/index.ts` 매치 (확장자 없이 — Turbopack lesson 준수).
    - `pnpm -F @gh-radar/shared build` exit 0.
    - `server/src/lib/computeComovement.test.ts` 에 `024060` + `017900` + `078590` (fixture 3쌍 code) + `isTrailing` + `sampleConfidence` + `sharedThemes` + `ANCHOR_REL_FLOOR` 또는 앵커 참여도 테스트(Test H) 문자열 존재.
    - `server/tests/routes/co-movement.test.ts` 에 `candidates` + `Array.isArray` + `co-movement` (라우트 경로) 존재.
    - `server/tests/fixtures/supabase-mock.ts` 의 `datasetFor` 에 `theme_comovement` + `cosurge_edges` 케이스 추가됨 (grep `theme_comovement` matches).
    - `pnpm -F @gh-radar/server test computeComovement` 가 RED (구현 미존재로 fail/no-test/import-error) — green 이면 안 됨.
  </acceptance_criteria>
  <done>
    공유 타입 2종 export + shared build green. computeComovement.test.ts 가 fixture 3쌍 + 후행 + dedup + 타이트니스 + 표본배지 박제(RED). co-movement.test.ts 가 객체계약 + 빈상태 + k클램프 + db-max-rows 박제(RED). supabase-mock 이 신규 2 테이블 데이터셋 지원.
  </done>
</task>

<task type="auto">
  <name>Task 3: workers/co-movement-sync 스캐폴드 (candle-sync 1:1 복제, KRX 의존성 제거)</name>
  <read_first>
    - workers/candle-sync/package.json (deps — axios 제거 대상)
    - workers/candle-sync/tsconfig.json
    - workers/candle-sync/vitest.config.ts (passWithNoTests:true)
    - workers/candle-sync/Dockerfile (멀티스테이지 — candle-sync → co-movement-sync 치환)
    - workers/candle-sync/src/config.ts (config shape — KRX 제거, LOOKBACK_MONTHS 추가)
    - workers/candle-sync/src/logger.ts (redact paths — krxAuthKey 제거)
    - workers/candle-sync/src/services/supabase.ts
    - pnpm-workspace.yaml (workers/* glob — 자동 인식)
    - .planning/phases/11-co-movement-candidates-top-k/11-RESEARCH.md §워커/배포 (변경점 표 — 단일 cycle, RPC 1줄, supabase-service-role 1개)
  </read_first>
  <action>
candle-sync 워크스페이스를 `workers/co-movement-sync/` 로 1:1 복제하되 KRX 의존성을 전면 제거하고 SQL RPC 호출 워커로 만든다 (얇음 — fetch/map/dedup 불요).

**3a. `workers/co-movement-sync/package.json`** — candle-sync verbatim 복제 후:
- `"name": "@gh-radar/co-movement-sync"`
- dependencies 에서 `"axios"` 제거 (외부 HTTP 없음). 나머지 {@gh-radar/shared, @supabase/supabase-js, dotenv, pino} 유지.
- scripts/devDeps 동일.

**3b. `workers/co-movement-sync/tsconfig.json`** — candle-sync verbatim (extends ../../tsconfig.base.json, module commonjs, moduleResolution node, outDir ./dist, rootDir ./src, include ["src"]).

**3c. `workers/co-movement-sync/vitest.config.ts`** — candle-sync verbatim (`{ test: { globals: true, passWithNoTests: true } }`). 주석만 "Plan 04 가 rebuild.ts 테스트 추가" 로 갱신.

**3d. `workers/co-movement-sync/Dockerfile`** — candle-sync Dockerfile verbatim, 모든 `candle-sync` → `co-movement-sync` 치환 (COPY 경로 4곳 + pnpm -F + pnpm deploy + cp). GIT_SHA ARG 유지.

**3e. `workers/co-movement-sync/src/logger.ts`** — candle-sync logger verbatim, redact paths 에서 `"*.krxAuthKey"` 제거 → `paths: ["*.supabaseServiceRoleKey"]` 만.

**3f. `workers/co-movement-sync/src/services/supabase.ts`** — candle-sync verbatim (`createClient(config.supabaseUrl, config.supabaseServiceRoleKey)`).

**3g. `workers/co-movement-sync/src/config.ts`** — KRX 제거 + LOOKBACK_MONTHS 추가:
```typescript
export type Config = {
  supabaseUrl: string;
  supabaseServiceRoleKey: string;
  logLevel: string;
  appVersion: string;
  lookbackMonths: number;   // LOOKBACK_MONTHS env, default 24 (D-12)
};

export function loadConfig(): Config {
  const supabaseUrl = process.env.SUPABASE_URL;
  const supabaseServiceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY;
  if (!supabaseUrl || !supabaseServiceRoleKey) {
    throw new Error("SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY must be set");
  }
  const lm = Number(process.env.LOOKBACK_MONTHS ?? "24");
  if (!Number.isFinite(lm)) throw new Error(`Invalid LOOKBACK_MONTHS: ${process.env.LOOKBACK_MONTHS}`);
  return {
    supabaseUrl,
    supabaseServiceRoleKey,
    logLevel: process.env.LOG_LEVEL ?? "info",
    appVersion: process.env.APP_VERSION ?? "0.0.0",
    lookbackMonths: lm,
  };
}
```
KRX_AUTH_KEY/KRX_BASE_URL/MODE/backfill/recover 등 candle-sync 전용 필드 전부 제거. `index.ts`/`rebuild.ts` 는 Plan 04 가 작성 (이 task 는 config/logger/supabase 토대만).

**3h. `workers/co-movement-sync/tests/config.test.ts`** — 최소 단위 테스트(passWithNoTests 보완): loadConfig 가 SUPABASE_URL 없으면 throw, 있으면 lookbackMonths=24 기본. (candle-sync 에 config 테스트가 있으면 그 패턴 복제, 없으면 간단히.)

**3i. pnpm install** — `pnpm install` 실행해 워크스페이스가 신규 패키지를 인식하게 함 (pnpm-workspace.yaml workers/* 자동).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm install --silent && pnpm -F @gh-radar/co-movement-sync build && pnpm -F @gh-radar/co-movement-sync test && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `workers/co-movement-sync/package.json` 의 name === `@gh-radar/co-movement-sync` 이고 `grep -c axios workers/co-movement-sync/package.json` === 0 (axios 제거).
    - `workers/co-movement-sync/src/config.ts` 에 `lookbackMonths` + `LOOKBACK_MONTHS` 존재, `KRX_AUTH_KEY` 미존재 (`grep -c KRX workers/co-movement-sync/src/config.ts` === 0).
    - `workers/co-movement-sync/src/logger.ts` 에 `supabaseServiceRoleKey` 존재, `krxAuthKey` 미존재.
    - `workers/co-movement-sync/Dockerfile` 에 `co-movement-sync` 존재, `candle-sync` 미존재 (`grep -c candle-sync workers/co-movement-sync/Dockerfile` === 0).
    - `pnpm -F @gh-radar/co-movement-sync build` exit 0.
    - `pnpm -F @gh-radar/co-movement-sync test` exit 0 (config.test.ts green 또는 passWithNoTests).
  </acceptance_criteria>
  <done>
    workers/co-movement-sync 스캐폴드가 candle-sync 1:1 복제(KRX 제거 + LOOKBACK_MONTHS)로 빌드·테스트 green. index.ts/rebuild.ts 는 Plan 04 대기. pnpm 워크스페이스가 신규 패키지 인식.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 워커(service_role) → Postgres | service_role 키로 rebuild_comovement() 호출 — RPC 가 service_role only |
| anon/authenticated → 신규 테이블 read | 공개 시세 집계 read (로그인 불요, scanner/themes 동일) |
| plpgsql SECURITY DEFINER 함수 | 정의자 권한 실행 — search_path 하이재킹 방지 필요 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Elevation | `rebuild_comovement()` RPC anon auto-grant | mitigate | `REVOKE EXECUTE ... FROM PUBLIC` + `REVOKE ... FROM anon, authenticated` + `GRANT ... TO service_role` 3줄 (Task 1, feedback_supabase_rpc_revoke). grep 검증: REVOKE 2회 + GRANT service_role 1회 |
| T-11-02 | DoS(자기) | 신규 테이블 `theme_comovement`/`cosurge_edges` default-deny | mitigate | RLS `TO anon, authenticated USING(true)` 둘 다 명시 (Task 1, feedback_supabase_rls_authenticated). anon-only 시 로그인 사용자 빈응답 회귀 회피 |
| T-11-03 | Elevation | plpgsql SECURITY DEFINER search_path 하이재킹 | mitigate | `SET search_path = public, pg_temp` (Task 1, triggers_followup WR-D-03 선례). grep 검증 |
| T-11-04 | Information Disclosure | 워커 로그에 service_role 키 노출 | mitigate | logger redact `["*.supabaseServiceRoleKey"]` (Task 3). krxAuthKey redact 는 불요(제거) |
| T-11-05 | Tampering | manual_override='excluded' / hidden 테마가 사전계산에 포함 | mitigate | rebuild_comovement() 의 active_members CTE 에 `manual_override IS DISTINCT FROM 'excluded'` + `th.hidden = false` (Task 1, Pitfall 5). 운영자 제외 종목이 동조 후보에 등장 차단 |

본 phase 의 모든 통제는 기존 production 선례(intraday RPC REVOKE / theme_tables RLS / triggers_followup search_path)라 신규 고위험 위협 없음. HIGH severity 위협 0 → 차단 없음.
</threat_model>

<verification>
- `pnpm -F @gh-radar/shared build` exit 0 (공유 타입).
- `pnpm -F @gh-radar/co-movement-sync build && pnpm -F @gh-radar/co-movement-sync test` exit 0 (워커 스캐폴드).
- `pnpm -F @gh-radar/server test computeComovement` RED (구현 미존재 — Plan 03 이 GREEN 으로 전환).
- 마이그레이션 SQL grep 검증 (Task 1 acceptance — REVOKE/RLS/search_path/인덱스/적재컷).
- REQUIREMENTS.md COMV-01 등록 + 커버리지 34.
- push 미실행 (Plan 02 [BLOCKING] 책임) — 로컬 정적 검증만.
</verification>

<success_criteria>
- COMV-01 이 REQUIREMENTS.md 에 등록되고 모든 다운스트림 plan 의 requirements 필드에 출현 가능.
- `comovement_tables.sql` 이 ROADMAP 성공기준 1·2 의 스키마/RPC/인덱스/이벤트컷을 코드로 표현 (push 는 Plan 02).
- `CoMovementCandidate`/`CoMovementResponse` 공유 타입이 server·webapp 계약 단일 소스로 export.
- fixture 3쌍 + 후행 + dedup + 타이트니스 + db-max-rows RED 테스트가 Plan 03 구현의 검증 게이트로 박제.
- co-movement-sync 워크스페이스가 빌드·테스트 가능 (Plan 04 가 rebuild.ts/index.ts 추가).
</success_criteria>

<output>
After completion, create `.planning/phases/11-co-movement-candidates-top-k/11-01-SUMMARY.md`.

커밋(GSD 경유, CLAUDE.md 규칙 — 한글 메시지, Co-Authored-By 금지, 커밋 전 사용자 확인): 예) `docs(11): COMV-01 등록 + 사전계산 스키마/타입/RED 테스트 스캐폴드`. 커밋 전 `git status --short`(pathspec 없이) 전체 트리 확인 후 명시 경로만 add (lessons 2026-06-10 scoped-staging).
</output>
