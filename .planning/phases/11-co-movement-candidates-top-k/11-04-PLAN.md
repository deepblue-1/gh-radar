---
phase: 11-co-movement-candidates-top-k
plan: 04
type: execute
wave: 2
depends_on: [11-02]
files_modified:
  - workers/co-movement-sync/src/rebuild.ts
  - workers/co-movement-sync/src/index.ts
  - workers/co-movement-sync/tests/rebuild.test.ts
  - scripts/setup-comovement-sync-iam.sh
  - scripts/deploy-comovement-sync.sh
  - scripts/smoke-comovement-sync.sh
  - .planning/phases/11-co-movement-candidates-top-k/11-DEPLOY-LOG.md
autonomous: false
requirements: [COMV-01]

must_haves:
  truths:
    - "co-movement-sync 워커가 supabase.rpc('rebuild_comovement', {p_lookback_months}) 한 줄 호출 + 결과 로깅(단일 cycle)"
    - "setup-comovement-sync-iam.sh 가 candle-sync 복제하되 KRX secret 제거(supabase-service-role 1개만) + 신규 SA"
    - "deploy-comovement-sync.sh 가 Job 1개 + Scheduler 1개(EOD 이후 야간) OAuth invoker(OIDC 금지) + task-timeout=Plan02 측정값"
    - "Cloud Run Job 실행 → Cloud Logging 에 {theme_comovement_rows, cosurge_edge_rows} > 0 로깅"
    - "Scheduler 가 ENABLED + cron 이 candle-sync EOD 이후 시각"
  artifacts:
    - path: "workers/co-movement-sync/src/rebuild.ts"
      provides: "rebuild_comovement RPC 호출 + jsonb 결과 로깅"
      contains: "rebuild_comovement"
    - path: "scripts/deploy-comovement-sync.sh"
      provides: "Job 1개 + Scheduler 1개 OAuth (KRX 제거)"
      contains: "oauth-service-account-email"
    - path: "scripts/smoke-comovement-sync.sh"
      provides: "Job execute + 로깅 행수 + Scheduler cron 검증"
  key_links:
    - from: "workers/co-movement-sync/src/index.ts"
      to: "rebuild.ts"
      via: "단일 cycle dispatch (MODE 없음 — intraday-sync 선례)"
      pattern: "runRebuild|rebuild"
    - from: "scripts/deploy-comovement-sync.sh"
      to: "gh-radar-scheduler-sa"
      via: "--oauth-service-account-email (OIDC 금지)"
      pattern: "oauth-service-account-email"
---

<objective>
얇은 `co-movement-sync` 워커를 완성하고 Cloud Run Job + Scheduler 로 배포한다 — candle-sync 1:1 복제지만 fetch/map/dedup 없이 `rebuild_comovement()` RPC 1줄만 호출하는 단일 cycle 워커(intraday-sync 선례). EOD candle-sync 이후 야간 1회 Scheduler 로 트리거해 사전계산 테이블을 멱등 full-rebuild 한다. 외부 API 키 불요(자체 DB 집계) → supabase-service-role 1개만.

Purpose: COMV-01 성공기준 3(얇은 워커 + Job + Scheduler EOD 이후 야간 1회) 구현 + smoke 검증.
Output: `rebuild.ts` + `index.ts`(Plan 01 스캐폴드 위에) · IAM/deploy/smoke 스크립트 3종 · Cloud Run Job + Scheduler 배포 · DEPLOY-LOG.

이 플랜은 autonomous=false (GCP 배포 [BLOCKING] — IAM + deploy + smoke). 워커 코드(Task 1)는 autonomous, 배포(Task 2~3)가 [BLOCKING]. Plan 02 가 확정한 task-timeout 값이 deploy 전제.

**병렬 주의:** Plan 03 과 같은 Wave 2 이나 파일 겹침 0(`workers/co-movement-sync/` + `scripts/` vs `server/`). 병렬 실행 시 git add race 방지를 위해 worktree 분리 또는 순차 실행(MEMORY feedback_parallel_wave_worktree). 11-DEPLOY-LOG.md 는 양 플랜이 append — 충돌 시 섹션 분리.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-co-movement-candidates-top-k/11-RESEARCH.md
@.planning/phases/11-co-movement-candidates-top-k/11-CALIBRATION.md
@workers/candle-sync/src/index.ts
@workers/candle-sync/src/config.ts
@scripts/deploy-candle-sync.sh
@scripts/setup-candle-sync-iam.sh
@scripts/smoke-candle-sync.sh

<interfaces>
<!-- candle-sync 선례 — 1:1 복제 + 변경점. Plan 01 이 config/logger/supabase 스캐폴드 작성 완료. -->

워커 단일 cycle (intraday-sync 선례 — MODE dispatch 제거, RESEARCH §워커/배포 변경점 표):
```typescript
// workers/co-movement-sync/src/rebuild.ts
import type { Logger } from "pino";
import type { SupabaseClient } from "@supabase/supabase-js";

export async function runRebuild(deps: {
  supabase: SupabaseClient; log: Logger; lookbackMonths: number;
}): Promise<Record<string, unknown>> {
  const { supabase, log, lookbackMonths } = deps;
  log.info({ lookbackMonths }, "rebuild_comovement start");
  const { data, error } = await supabase.rpc("rebuild_comovement", {
    p_lookback_months: lookbackMonths,
  });
  if (error) throw new Error(`rebuild_comovement failed: ${error.message}`);
  log.info({ result: data }, "rebuild_comovement complete");  // {theme_comovement_rows, cosurge_edge_rows, ...}
  return (data ?? {}) as Record<string, unknown>;
}
```

index.ts entry (candle-sync index.ts 패턴 — dispatch + CLI 가드, MODE 없음):
```typescript
// workers/co-movement-sync/src/index.ts
import "dotenv/config";
import { loadConfig } from "./config";
import { logger } from "./logger";
import { createSupabaseClient } from "./services/supabase";
import { runRebuild } from "./rebuild";

export async function dispatch(): Promise<Record<string, unknown>> {
  const config = loadConfig();
  const log = logger.child({ app: "co-movement-sync", version: config.appVersion });
  const supabase = createSupabaseClient(config);
  return runRebuild({ supabase, log, lookbackMonths: config.lookbackMonths });
}

async function main(): Promise<void> {
  try {
    const out = await dispatch();
    logger.info({ result: out }, "co-movement-sync complete");
    process.exit(0);
  } catch (err) {
    logger.error({ err }, "co-movement-sync failed");
    process.exit(1);
  }
}
if (process.argv[1] && process.argv[1].endsWith("index.js")) { main(); }
```

IAM 스크립트 변경점 (setup-candle-sync-iam.sh 복제 — RESEARCH §워커/배포):
- 신규 SA `gh-radar-comovement-sync-sa` (candle-sync-sa → comovement-sync-sa 치환).
- 선행 SA `gh-radar-scheduler-sa` 존재 확인 (재사용).
- **KRX secret 바인딩 제거** — `gh-radar-krx-auth-key` accessor 바인딩 삭제. `gh-radar-supabase-service-role` accessor 1개만.

deploy 스크립트 변경점 (deploy-candle-sync.sh 복제):
- **Job 1개** (`gh-radar-comovement-sync`, 3개 아님).
- COMMON_ENV 에서 KRX_BASE_URL 제거, `LOOKBACK_MONTHS=24` 추가. MODE 없음.
- COMMON_SECRETS 에서 `KRX_AUTH_KEY=...` 제거 → `SUPABASE_SERVICE_ROLE_KEY=gh-radar-supabase-service-role:latest` 1개만.
- **task-timeout = Plan 02 측정값** (11-CALIBRATION.md 의 확정 값; 초기 권고 600s, 측정 후 조정). memory 512Mi.
- Scheduler 1개 — cron `0 2 * * 2-6` Asia/Seoul (EOD candle-sync 17:30 이후 다음날 새벽, 전 영업일 데이터 확정 — RESEARCH §Open Q 3 권고. 또는 `0 18 * * 1-5` EOD+30m). `--oauth-service-account-email`(OIDC 금지).
- Scheduler → Job invoker 바인딩(Job 생성 후, 리소스 단위).

smoke 스크립트 (smoke-candle-sync.sh 복제):
- INV-1: Job execute --wait exit 0.
- INV-2: Cloud Logging `jsonPayload.msg="rebuild_comovement complete"` 또는 `co-movement-sync complete` 1건 + result.cosurge_edge_rows > 0 (jsonPayload.msg 로 쿼리 — Job 측, STATE.md Phase 10 lesson).
- INV-3: 로그에 "co-movement-sync failed" 0건.
- INV-4: theme_comovement + cosurge_edges 행수 > 0 (service_role REST count).
- INV-5: Scheduler ENABLED + cron 확인.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: rebuild.ts + index.ts 워커 본문 + 단위 테스트</name>
  <read_first>
    - workers/co-movement-sync/src/config.ts (Plan 01 — lookbackMonths)
    - workers/co-movement-sync/src/logger.ts (Plan 01 — redact supabaseServiceRoleKey)
    - workers/co-movement-sync/src/services/supabase.ts (Plan 01 — createSupabaseClient)
    - workers/candle-sync/src/index.ts (dispatch + CLI 가드 패턴 — MODE 제거)
    - .planning/phases/11-co-movement-candidates-top-k/11-RESEARCH.md §워커/배포 (단일 cycle, RPC 1줄)
  </read_first>
  <action>
**1a. `workers/co-movement-sync/src/rebuild.ts`** — interfaces 블록 verbatim. `supabase.rpc("rebuild_comovement", { p_lookback_months })` 호출, error throw, jsonb 결과 로깅. (raw SQL 아님 — supabase-js RPC.)

**1b. `workers/co-movement-sync/src/index.ts`** — interfaces 블록 verbatim. candle-sync index.ts 의 dispatch + main + CLI 가드(`process.argv[1].endsWith("index.js")`) 패턴, MODE dispatch 제거(단일 cycle). vitest import 시 main 미실행.

**1c. `workers/co-movement-sync/tests/rebuild.test.ts`** — runRebuild 단위 테스트(mock supabase.rpc):
- rpc 가 `{theme_comovement_rows: 7500, cosurge_edge_rows: 12000}` 반환 → runRebuild 가 그 객체 반환 + log.info "rebuild_comovement complete" 호출.
- rpc error 반환 → runRebuild throw (`rebuild_comovement failed`).
mock 패턴: `{ rpc: vi.fn().mockResolvedValue({ data: {...}, error: null }) }` (supabase-js RPC shape).

**1d.** `pnpm -F @gh-radar/co-movement-sync build && test` green. Plan 01 의 vitest.config(passWithNoTests) 는 이제 rebuild.test.ts 로 실제 테스트 보유.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm -F @gh-radar/co-movement-sync build && pnpm -F @gh-radar/co-movement-sync test && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `workers/co-movement-sync/src/rebuild.ts` 에 `supabase.rpc("rebuild_comovement"` + `p_lookback_months` + `error` throw 존재.
    - `workers/co-movement-sync/src/index.ts` 에 `dispatch` + `runRebuild` + `process.argv[1]` + `endsWith("index.js")` (CLI 가드) 존재, `MODE` 미존재 (`grep -c MODE workers/co-movement-sync/src/index.ts` === 0).
    - `workers/co-movement-sync/tests/rebuild.test.ts` 에 `rebuild_comovement` + `cosurge_edge_rows` + error 케이스 존재.
    - `pnpm -F @gh-radar/co-movement-sync build` exit 0.
    - `pnpm -F @gh-radar/co-movement-sync test` exit 0 (rebuild.test.ts green).
  </acceptance_criteria>
  <done>
    워커가 단일 cycle 로 rebuild_comovement RPC 1줄 호출 + 결과 로깅(theme_comovement_rows/cosurge_edge_rows). MODE dispatch 없음(intraday-sync 선례). rebuild.test.ts 가 성공/에러 케이스 green.
  </done>
</task>

<task type="auto">
  <name>Task 2: IAM + deploy + smoke 스크립트 3종 (candle-sync 복제, KRX 제거, Job/Scheduler 1개)</name>
  <read_first>
    - scripts/setup-candle-sync-iam.sh (복제 — SA 치환 + KRX accessor 제거)
    - scripts/deploy-candle-sync.sh (복제 — Job 1개 + Scheduler 1개 + KRX secret 제거 + task-timeout)
    - scripts/smoke-candle-sync.sh (복제 — INV 재정의)
    - .planning/phases/11-co-movement-candidates-top-k/11-CALIBRATION.md (Plan 02 확정 task-timeout 값)
    - .planning/STATE.md (Phase 09.1/10 — OAuth invoker OIDC 금지, jsonPayload.msg Job 측 로깅)
    - .planning/phases/11-co-movement-candidates-top-k/11-RESEARCH.md §워커/배포 (변경점 표 + Scheduler cron 권고)
  </read_first>
  <action>
candle-sync 스크립트 3종을 복제하되 KRX 의존성 제거 + Job/Scheduler 1개 + task-timeout=측정값.

**2a. `scripts/setup-comovement-sync-iam.sh`** — setup-candle-sync-iam.sh 복제 후:
- 모든 `candle-sync` → `comovement-sync` 치환 (SA 이름 `gh-radar-comovement-sync-sa`, 주석).
- Section 5/6 의 KRX 부분 제거: `gh-radar-krx-auth-key` 존재 확인 + accessor 바인딩 **삭제**. `gh-radar-supabase-service-role` 1개만 존재 확인 + accessor 바인딩.
- 선행 `gh-radar-scheduler-sa` 존재 확인 유지(재사용).
- SA display-name = "gh-radar co-movement-sync (rebuild_comovement RPC, Phase 11 COMV-01)".

**2b. `scripts/deploy-comovement-sync.sh`** — deploy-candle-sync.sh 복제 후:
- 이미지 `co-movement-sync:${SHA}` (Dockerfile `-f workers/co-movement-sync/Dockerfile`).
- 선행 SA 확인: `gh-radar-scheduler-sa` + `gh-radar-comovement-sync-sa`.
- COMMON_ENV: `^@^SUPABASE_URL=${SUPABASE_URL}@LOG_LEVEL=info@APP_VERSION=${SHA}@LOOKBACK_MONTHS=24` (KRX_BASE_URL 제거, MODE 없음).
- COMMON_SECRETS: `SUPABASE_SERVICE_ROLE_KEY=gh-radar-supabase-service-role:latest` (KRX_AUTH_KEY 제거).
- **Job 1개** `gh-radar-comovement-sync` (deploy_job 헬퍼 1회 호출). task-timeout = **Plan 02 측정값**(11-CALIBRATION.md; 미측정 시 600s fallback), memory 512Mi, max-retries=0.
- Scheduler **1개** `gh-radar-comovement-sync-nightly` → Job, cron `0 2 * * 2-6` (Asia/Seoul, 다음날 새벽 — 전 영업일 확정. RESEARCH §Open Q 3 권고. EOD 완료 신뢰 시 `0 18 * * 1-5` 대안), `--oauth-service-account-email="$SCHED_SA"` (OIDC 금지).
- Scheduler → Job invoker 바인딩 (Job 생성 후, `gcloud run jobs add-iam-policy-binding ... roles/run.invoker`).

**2c. `scripts/smoke-comovement-sync.sh`** — smoke-candle-sync.sh 복제 후 INV 재정의 (interfaces INV-1~5):
- INV-1: `gcloud run jobs execute gh-radar-comovement-sync --region=... --wait` exit 0.
- INV-2: Cloud Logging `jsonPayload.msg="co-movement-sync complete"` (또는 "rebuild_comovement complete") 최근 5분 1건 — `--freshness=5m`. (STATE.md: Job 측은 jsonPayload.msg.)
- INV-3: "co-movement-sync failed" 0건.
- INV-4: theme_comovement + cosurge_edges 행수 > 0 (service_role REST `Prefer: count=exact` + `Range: 0-0` content-range — smoke-candle-sync INV-4 패턴).
- INV-5: `gcloud scheduler jobs describe gh-radar-comovement-sync-nightly` → state ENABLED + cron `0 2 * * 2-6` (또는 배포한 cron).

스크립트 3종 모두 `chmod +x` 또는 `bash scripts/...` 실행 전제. gcloud 가드(EXPECTED_PROJECT/CONFIG) 헤더 유지. 이 task 는 **스크립트 작성만** — 실행은 Task 3 [BLOCKING].
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash -n scripts/setup-comovement-sync-iam.sh && bash -n scripts/deploy-comovement-sync.sh && bash -n scripts/smoke-comovement-sync.sh && grep -q "oauth-service-account-email" scripts/deploy-comovement-sync.sh && test "$(grep -c 'KRX' scripts/deploy-comovement-sync.sh)" -eq 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` (syntax check) 3 스크립트 모두 exit 0.
    - `scripts/setup-comovement-sync-iam.sh` 에 `gh-radar-comovement-sync-sa` + `gh-radar-supabase-service-role` 존재, `grep -c 'krx-auth-key' scripts/setup-comovement-sync-iam.sh` === 0 (KRX accessor 제거).
    - `scripts/deploy-comovement-sync.sh` 에 `gh-radar-comovement-sync` (Job) + `LOOKBACK_MONTHS=24` + `oauth-service-account-email` 존재, `grep -c KRX scripts/deploy-comovement-sync.sh` === 0.
    - `scripts/deploy-comovement-sync.sh` 의 deploy_job 호출이 1회 (Job 1개 — `grep -c 'deploy_job ' scripts/deploy-comovement-sync.sh` 가 정의 제외 1).
    - `scripts/deploy-comovement-sync.sh` 에 Scheduler cron (`0 2 * * 2-6` 또는 `0 18 * * 1-5`) + `gh-radar-comovement-sync-nightly` 존재.
    - `scripts/smoke-comovement-sync.sh` 에 `co-movement-sync complete` + `cosurge_edges` + `theme_comovement` + Scheduler describe 존재.
  </acceptance_criteria>
  <done>
    IAM/deploy/smoke 3종이 candle-sync 복제로 작성 — KRX 의존성 전면 제거(secret/env), Job 1개 + Scheduler 1개 OAuth(OIDC 금지), task-timeout=Plan02 측정값. syntax green. 실행은 Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] IAM + 배포 + Job 실행 smoke + DEPLOY-LOG</name>
  <read_first>
    - scripts/setup-comovement-sync-iam.sh / deploy-comovement-sync.sh / smoke-comovement-sync.sh (Task 2 작성)
    - .planning/STATE.md (Phase 10 10-08 — Cloud Run Job/Scheduler 배포 + smoke INV 패턴, jsonPayload.msg)
    - .planning/phases/11-co-movement-candidates-top-k/11-CALIBRATION.md (task-timeout)
    - MEMORY reference_gh_radar_deployer_sa (GCP 인증)
  </read_first>
  <action>
GCP 인증 후 IAM → deploy → Job 1회 실행 → smoke → DEPLOY-LOG.

**3a. GCP 인증** (Deployer SA — MEMORY: `GOOGLE_APPLICATION_CREDENTIALS=~/.config/gcloud/gh-radar-deployer.json`, `CLOUDSDK_CORE_PROJECT=gh-radar`, `GCP_PROJECT_ID=gh-radar`). gcloud config 가 gh-radar active 확인.

**3b. IAM:** `bash scripts/setup-comovement-sync-iam.sh` — 신규 SA + supabase-service-role accessor 바인딩.

**3c. Deploy:** `bash scripts/deploy-comovement-sync.sh` — 이미지 빌드/push + Job 1개 + Scheduler 1개. (SUPABASE_URL 등 env 는 .env.deploy 또는 기존 서비스에서.) Job + Scheduler 생성 확인.

**3d. Job 1회 실행 + smoke:** `bash scripts/smoke-comovement-sync.sh` — INV-1~5. 특히 INV-2(로깅 result.cosurge_edge_rows > 0) + INV-4(테이블 행수 > 0) + INV-5(Scheduler ENABLED + cron). Job 실행은 Plan 02 가 이미 1회 rebuild 했어도 멱등 full-rebuild(TRUNCATE+INSERT)라 안전.

**3e. `11-DEPLOY-LOG.md` 갱신** (Plan 03 섹션과 분리 또는 append) — co-movement-sync image SHA + Job 이름 + Scheduler cron + smoke INV 결과 + Job 실행 result(theme_comovement_rows/cosurge_edge_rows) 기록.

주의: Scheduler 첫 자동 실행은 다음 새벽(cron). smoke 는 수동 execute 로 즉시 검증.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && gcloud run jobs describe gh-radar-comovement-sync --region=asia-northeast3 --format='value(metadata.name)' 2>/dev/null | grep -q comovement-sync && gcloud scheduler jobs describe gh-radar-comovement-sync-nightly --location=asia-northeast3 --format='value(state)' 2>/dev/null | grep -q ENABLED && test -f .planning/phases/11-co-movement-candidates-top-k/11-DEPLOY-LOG.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `gcloud run jobs describe gh-radar-comovement-sync` → 존재 (metadata.name 매치).
    - `gcloud run jobs execute gh-radar-comovement-sync --wait` exit 0 (INV-1).
    - Cloud Logging 최근 5분 `jsonPayload.msg="co-movement-sync complete"` 1건 이상 (INV-2).
    - Cloud Logging 최근 5분 "co-movement-sync failed" 0건 (INV-3).
    - service_role REST count: theme_comovement 행수 > 0 AND cosurge_edges 행수 > 0 (INV-4).
    - `gcloud scheduler jobs describe gh-radar-comovement-sync-nightly` → state ENABLED + cron (배포값) (INV-5).
    - SA `gh-radar-comovement-sync-sa` 에 `gh-radar-supabase-service-role` accessor 바인딩 존재, `gh-radar-krx-auth-key` 바인딩 **없음** (KRX 미사용).
    - `11-DEPLOY-LOG.md` 에 co-movement-sync image SHA + Scheduler cron + Job 실행 result 기록.
  </acceptance_criteria>
  <done>
    co-movement-sync Job + Scheduler(야간 1회 OAuth)가 GCP 에 배포·ENABLED. Job 1회 실행 smoke INV-1~5 PASS (로깅 행수>0, 테이블 행수>0, Scheduler cron). SA 가 service-role 만 바인딩(KRX 없음). DEPLOY-LOG 기록.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scheduler → Cloud Run Job | OAuth invoker 로 Job 트리거 (OIDC 금지) |
| Job(comovement-sync-sa) → Supabase | service_role 키로 rebuild_comovement RPC |
| Secret Manager → Job | supabase-service-role secret 바인딩 (최소권한) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-14 | Elevation | Scheduler→Job 인증 OIDC 오용 | mitigate | `--oauth-service-account-email`(OIDC 금지) (Task 2, deploy-candle-sync 선례 + Phase 05.1 D-07 lesson). 리소스 단위 invoker 바인딩 |
| T-11-15 | Information Disclosure | 워커 로그에 service_role 키 노출 | mitigate | logger redact `["*.supabaseServiceRoleKey"]` (Plan 01 logger). result 객체만 로깅(키 없음) |
| T-11-16 | Elevation | comovement-sync SA 과대권한(불요 secret 바인딩) | mitigate | SA 최소권한 — supabase-service-role 1개만 accessor(KRX/Anthropic/Kiwoom 미바인딩) (Task 2). acceptance: krx-auth-key 바인딩 없음 |
| T-11-17 | Tampering | 외부 입력 경로 없음(자체 DB 집계) | accept | co-movement-sync 은 외부 HTTP 0(KRX/Naver/Anthropic 미사용) → 외부 입력 위협 표면 0. Naver 5원칙 무관(명시) |

OAuth/redact/최소권한 모두 candle-sync/intraday-sync 검증 선례. HIGH severity 위협 0.
</threat_model>

<verification>
- `pnpm -F @gh-radar/co-movement-sync build && test` green (rebuild.test.ts).
- `bash -n` 3 스크립트 syntax green + KRX 제거 grep.
- Job + Scheduler GCP 배포 + ENABLED.
- smoke INV-1~5 PASS (Job execute, 로깅 행수>0, 테이블 행수>0, Scheduler cron).
- DEPLOY-LOG 기록.
</verification>

<success_criteria>
- ROADMAP 성공기준 3 충족: 얇은 co-movement-sync 워커 + Cloud Run Job + Scheduler 가 EOD candle-sync 이후 야간 1회 실행되어 theme_comovement/cosurge_edges 갱신.
- 워커가 RPC 1줄(rebuild_comovement) 단일 cycle — fetch/map/dedup 없음(intraday-sync 선례).
- 외부 API 키 불요(service-role 1개) → SA 최소권한 + Naver 5원칙 무관.
- Scheduler 자동화로 매일 사전계산 신선도 유지 (Plan 03 읽기경로가 최신 데이터 read).
</success_criteria>

<output>
After completion, create `.planning/phases/11-co-movement-candidates-top-k/11-04-SUMMARY.md` (Job/Scheduler 이름 + cron + image SHA + smoke 결과 명시).

커밋(GSD 경유, CLAUDE.md 규칙 — 한글, Co-Authored-By 금지, 커밋 전 사용자 확인 + `git status --short` 전체 트리): 예) `feat(11): co-movement-sync 워커 + Job/Scheduler 배포`.
</output>
