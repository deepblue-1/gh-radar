---
phase: 11-co-movement-candidates-top-k
plan: 05
type: execute
wave: 3
depends_on: [11-03]
files_modified:
  - webapp/src/lib/comovement-api.ts
  - webapp/src/components/stock/stock-comovement-section.tsx
  - webapp/src/components/stock/stock-detail-client.tsx
  - webapp/tests/components/stock-comovement-section.test.tsx
  - .planning/phases/11-co-movement-candidates-top-k/11-DEPLOY-LOG.md
autonomous: false
requirements: [COMV-01]

must_haves:
  truths:
    - "StockComovementSection 이 apiFetch<CoMovementResponse> 로 동조 후보를 fetch 하고 종목상세 ThemeChips 다음에 마운트됨"
    - "후보 행이 theme-rank-row 강도바(.underbar 3px) + theme-chips 근거칩 + 동반율(중립색, co-surge 전용은 \"—\") + 실시간 등락률(방향색) + 후행형 배지 렌더"
    - "초기 3행 + 더보기(useState expanded), 후보 ≤3 이면 더보기 없음, >3 이면 '동조 후보 N개 더 보기'"
    - "후보 0(테마없음 AND co-surge 없음) → '동조 데이터 부족' 빈 상태, 에러 → 섹션 조용히 숨김"
    - "Vercel 배포 후 종목상세에서 동조 후보 섹션 시각 렌더(후보 有) + 빈 상태(무테마 종목) 확인"
  artifacts:
    - path: "webapp/src/components/stock/stock-comovement-section.tsx"
      provides: "동조 후보 섹션 (UI-SPEC 변형C + 기본3/더보기 + 빈상태 + quiet fallback)"
      min_lines: 120
    - path: "webapp/src/lib/comovement-api.ts"
      provides: "fetchStockComovement (apiFetch<CoMovementResponse>)"
  key_links:
    - from: "webapp/src/components/stock/stock-detail-client.tsx"
      to: "StockComovementSection"
      via: "StockThemeChips 다음 마운트 (space-y-8)"
      pattern: "StockComovementSection"
    - from: "webapp/src/components/stock/stock-comovement-section.tsx"
      to: "/api/stocks/:code/co-movement"
      via: "apiFetch<CoMovementResponse>"
      pattern: "co-movement"
---

<objective>
종목 상세 페이지에 "동조 후보" 섹션을 렌더한다 — UI-SPEC 채택안(변형 C 지표 듀얼 + 사용자 수정 기본 3행/더보기)을 theme-rank-row(강도바)·theme-chips(근거칩) 재사용으로 구현하고, StockThemeChips 바로 다음에 마운트한다. apiFetch<CoMovementResponse> 객체 계약으로 Plan 03 라우트를 소비하고, 후보별 동반율(중립색)·실시간 등락률(방향색)·근거 칩(공유 테마/직접동반)·후행형 배지·강도바를 표시. 빈 상태("동조 데이터 부족")와 quiet fallback(에러 시 섹션 숨김)을 포함하고 Vercel 배포 + 시각 검증한다.

Purpose: COMV-01 성공기준 5(종목상세 ThemeChips 다음 동조 후보 섹션 + 빈 상태) 구현 + production 시각 검증.
Output: `comovement-api.ts` · `stock-comovement-section.tsx` · stock-detail-client.tsx 마운트 · 컴포넌트 테스트 · Vercel 배포 · DEPLOY-LOG.

이 플랜은 autonomous=false (Vercel 배포 + 시각 checkpoint). 컴포넌트 구현·테스트(Task 1~2)는 autonomous, 배포·시각 검증(Task 3)이 checkpoint. Plan 03(라우트 live) 완료가 prod 렌더 전제. UI-SPEC 이 시각 SOURCE OF TRUTH (6/6 PASS approved).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-co-movement-candidates-top-k/11-UI-SPEC.md
@.planning/phases/11-co-movement-candidates-top-k/mockups/co-movement-adopted.html
@.planning/phases/11-co-movement-candidates-top-k/11-RESEARCH.md
@webapp/src/components/theme/theme-rank-row.tsx
@webapp/src/components/theme/theme-chips.tsx
@webapp/src/components/stock/stock-detail-client.tsx
@webapp/src/components/stock/stock-daily-chart-section.tsx
@packages/shared/src/comovement.ts

<interfaces>
<!-- UI-SPEC = 시각 SOURCE OF TRUTH (approved 6/6). mockups/co-movement-adopted.html = 픽셀 참조. -->

apiFetch 패턴 (webapp/src/lib — stock-api.ts 동형, ApiClientError):
```typescript
// webapp/src/lib/comovement-api.ts
import { apiFetch } from "./api";
import type { CoMovementResponse } from "@gh-radar/shared";
export function fetchStockComovement(code: string, k = 8, signal?: AbortSignal): Promise<CoMovementResponse> {
  return apiFetch<CoMovementResponse>(`/api/stocks/${code}/co-movement?k=${k}`, { signal });
}
```

CoMovementCandidate 계약 (packages/shared/src/comovement.ts — Plan 01):
```typescript
{ code, name, market, liveChangeRate: number|null, confD0, strength, isTrailing,
  sharedThemes: {id,name}[], coSurgeCount: number|null, sampleConfidence: "high"|"low" }
```

마운트 위치 (stock-detail-client.tsx:143 — StockThemeChips 다음, 뉴스/토론 div 앞):
```tsx
<StockThemeChips stockCode={stock.code} />
<StockComovementSection stockCode={stock.code} />   {/* ← 추가 (space-y-8 스택) */}
<div className="space-y-6">
  <StockNewsSection ... />
```

UI-SPEC 핵심 계약 (11-UI-SPEC.md §Component Structure — 그대로 구현):
- 루트 `<section aria-label="동조 후보">`. 'use client'. mount fetch + AbortController(theme-chips/daily-chart 선례).
- 헤더 `<h2>` (StockThemeChips h2 캡션 패턴): lucide `Waypoints` size-3.5 + "동조 후보" + 우측 힌트(선택) "급등 시 동반 ↑ · 동반율 순".
- 행 카드: `relative overflow-hidden border bg-[var(--card)] rounded-[var(--r)] py-[var(--s-3)] px-[var(--s-4)]`, 전체 `/stocks/[code]` Link(aria-label "{name} 종목 상세 보기"), hover border `color-mix(in oklch,var(--primary) 30%,var(--border))`, grid `1fr auto` items-center gap-[var(--s-3)].
  - 좌측: 1줄 종목명 `<b>`(700, --t-base) + 코드(.mono, --t-caption, --muted-fg). 2줄 근거칩 + 후행형 배지.
  - 우측 metric (`flex items-end gap-[var(--s-3)]`): 동반율 (.mono 800 --t-h3, **중립 --fg**, 미니라벨 "동반율" 10px) + 실시간 (.mono 700 --t-sm, **방향색** --up/--down/--flat, min-width 62px, 미니라벨 "실시간").
  - **co-surge 전용 후보(`sharedThemes.length === 0`)의 동반율 = "—"** (.mono, --muted-fg, 미니라벨 유지) — Plan 03 이 co-surge 전용에 confD0=0 을 주므로 "0%" 그대로 표시하면 테마무관 동조(co-surge 경로의 존재 이유, 실측 41%)가 가장 약해 보이는 오해 유발 (UI-SPEC §우측 메트릭 규칙).
  - 강도바 `.underbar`: `absolute left-0 bottom-0 h-[3px] rounded-[0_999px_999px_0] bg-[var(--up)]`, width = `Math.max(4, Math.min(100, strength*100))` (theme-rank-row barPct 동형), aria-hidden.
- 근거 칩 (D-03, 테마 먼저 → 직접동반):
  - 공유 테마 칩: outline border + flat 도트(size-[5px]) + 테마명 (theme-chips 톤, rounded-full px-[9px] py-px --t-caption).
  - 직접동반 칩: **accent fill** `bg-[var(--accent)] text-[var(--accent-fg)]` + lucide `ArrowUpRight`(~11px) + "직접동반 {coSurgeCount}회" (coSurgeCount != null 일 때).
- 후행형 배지 (isTrailing): `border color-mix(in oklch,var(--down) 35%,var(--border)) text-[var(--down)] bg-[var(--down-bg)] rounded-full px-2` 11px 700 + lucide `History`(~10px) + "후행형".
- 표시 동작 (★ 사용자 수정 D-10): 초기 최대 3행, useState expanded. 후보 ≤3 → 더보기 없음. >3 → full-width 버튼 `border bg-[var(--card)] text-[var(--muted-fg)] rounded-[var(--r)] py-[var(--s-2)] px-[var(--s-3)] --t-sm 600 mt-[var(--s-2)]`, 라벨 접힘="동조 후보 {N-3}개 더 보기" / 펼침="접기", hover text-[var(--fg)].
- 빈 상태 (D-06, 후보 0): `border border-dashed rounded-[var(--r-md)] px-[var(--s-4)] py-[var(--s-5)] text-center` + lucide `CircleOff`(~22px --muted-fg mb-8px) + "동조 데이터 부족"(--t-base 700) + "아직 이 종목과 함께 움직인 패턴을 찾지 못했습니다. 테마·동반 급등 이력이 쌓이면 표시됩니다."(--t-sm --muted-fg).
- 로딩: 3행 스켈레톤(.skeleton-list) 또는 loaded 전 null(레이아웃 점프 방지, theme-chips 선례).
- 에러: **섹션 조용히 숨김(null)** — error.message 미노출 (theme-chips/daily-chart quiet fallback 선례, T-11-12).

색 규칙 (UI-SPEC ★ LOCKED): 동반율 = 중립 --fg (확률, 빨강/파랑 금지). 실시간 등락률만 방향색. 강도 라인 = --up. 후행형 배지 = --down 톤. 공유 테마 칩 도트 = --flat. 모든 색 globals.css oklch 토큰 (차트 아님 → 직접 사용, lightweight-charts hex 변환 무관). 신규 토큰/하드코딩 금지.

Vercel 배포 (lessons + MEMORY reference_vercel_frontend_deploy):
- ignoreCommand 가 일부 push skip → 수동 배포는 repo root 에서 `vercel pull` → `vercel build` → `vercel deploy --prebuilt`.
- prod URL: https://gh-radar-webapp.vercel.app.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: comovement-api + StockComovementSection 컴포넌트 (UI-SPEC 구현)</name>
  <read_first>
    - .planning/phases/11-co-movement-candidates-top-k/11-UI-SPEC.md (§Component Structure — 실행 계약, executor 가 이것만으로 구현)
    - .planning/phases/11-co-movement-candidates-top-k/mockups/co-movement-adopted.html (픽셀 참조 — 행 카드/칩/배지/강도바/더보기/빈상태 markup + barPct/evidence/trailBadge 함수)
    - webapp/src/components/theme/theme-rank-row.tsx (강도바 barPct + Link + 색 함수)
    - webapp/src/components/theme/theme-chips.tsx (헤더 h2 캡션 + 칩 + quiet fallback + loaded null)
    - webapp/src/components/stock/stock-daily-chart-section.tsx (mount fetch + AbortController + error 숨김 선례)
    - webapp/src/lib/stock-api.ts (apiFetch 패턴 + ApiClientError)
    - packages/shared/src/comovement.ts (CoMovementCandidate/CoMovementResponse 계약)
  </read_first>
  <action>
**1a. `webapp/src/lib/comovement-api.ts`** — interfaces 블록 verbatim. `fetchStockComovement(code, k=8, signal)` → `apiFetch<CoMovementResponse>("/api/stocks/${code}/co-movement?k=${k}", { signal })`.

**1b. `webapp/src/components/stock/stock-comovement-section.tsx`** — 'use client'. UI-SPEC §Component Structure 를 그대로 구현 (mockups markup + theme-rank-row/theme-chips 재사용):
- props `{ stockCode: string }`. state: `candidates: CoMovementCandidate[]`, `loaded: boolean`, `expanded: boolean`.
- useEffect mount fetch (AbortController, daily-chart 선례): `fetchStockComovement(stockCode, 8, signal)` → setCandidates(res.candidates). 에러 → setLoaded(true) 만 (candidates=[] 유지, 섹션 숨김 분기로).
- 렌더 분기:
  - `!loaded` → null (레이아웃 점프 방지).
  - 에러 발생(fetch throw) → null (quiet fallback — error state 로 추적하되 섹션 미렌더). **error.message 절대 미노출**.
  - `candidates.length === 0` (정상 응답이 빈 배열) → 빈 상태 박스 (CircleOff + "동조 데이터 부족" + 본문).
  - 후보 有 → 헤더 + 리스트(초기 3 / expanded 전체) + 더보기 버튼(>3).
- 행 카드 = mockups `.row-card` 구조 (Link 전체 + grid 1fr auto + 좌측 종목명/코드/근거칩/후행배지 + 우측 동반율/실시간 + .underbar). `barPct = Math.max(4, Math.min(100, c.strength*100))`.
- 동반율 표시 = `${Math.round(c.confD0 * 100)}%` (confD0 는 0~1 → % 변환, 중립 --fg). **단 co-surge 전용 후보(`c.sharedThemes.length === 0`)는 값 대신 "—"** (.mono, --muted-fg, 미니라벨 "동반율" 유지) — confD0=0 의 "0%" 오표시 방지 (UI-SPEC 규칙). 실시간 = `fmtLive(c.liveChangeRate)` (null → "—", 방향색 liveCls).
- evidence: c.sharedThemes 각각 공유 테마 칩(flat 도트 + name), c.coSurgeCount != null 이면 직접동반 칩(accent fill + ArrowUpRight + "직접동반 {N}회"). 테마 먼저 → 직접동반.
- 후행형 배지: c.isTrailing 이면 History + "후행형" (--down 톤).
- lucide import: `Waypoints, ArrowUpRight, History, CircleOff`.
- 모든 색 globals.css 토큰 (UI-SPEC LOCKED 규칙 — 동반율 중립, 실시간만 방향, 강도 --up, 후행 --down, 칩 도트 --flat). 신규 토큰/하드코딩 0.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm -F @gh-radar/webapp typecheck && grep -q "apiFetch<CoMovementResponse>" webapp/src/components/stock/stock-comovement-section.tsx webapp/src/lib/comovement-api.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/src/lib/comovement-api.ts` 에 `apiFetch<CoMovementResponse>` + `/api/stocks/${code}/co-movement` 존재.
    - `webapp/src/components/stock/stock-comovement-section.tsx` 에 `'use client'` + `aria-label="동조 후보"` + `Waypoints` + `CircleOff` 존재.
    - 동일 파일에 빈 상태 문자열 "동조 데이터 부족" 존재.
    - 동일 파일에 강도바 식 `Math.max(4, Math.min(100,` (barPct) + `.underbar` 또는 `bottom-0` `h-[3px]` 존재.
    - 동일 파일에 "직접동반" (co-surge 칩) + "후행형" (배지) + `var(--accent)` (직접동반 fill) + `var(--down)` (후행 톤) 존재.
    - 동일 파일에 동반율 색이 `var(--fg)` (중립 — 동반율 영역), 실시간만 `var(--up)`/`var(--down)`/`var(--flat)` (방향) — 동반율에 --up/--down 미적용.
    - 동일 파일에 더보기 라벨 "더 보기" + "접기" + `expanded` (useState) 존재.
    - 동일 파일에 co-surge 전용 동반율 분기 — `sharedThemes.length === 0` 존재 (동반율 "—" 처리, "0%" 미표시).
    - 하드코딩 hex 색 없음 (`grep -iE '#[0-9a-f]{3,6}' webapp/src/components/stock/stock-comovement-section.tsx` 빈 결과 — oklch 토큰만).
    - `pnpm -F @gh-radar/webapp typecheck` exit 0.
  </acceptance_criteria>
  <done>
    StockComovementSection 이 UI-SPEC §Component Structure 를 구현 — apiFetch<CoMovementResponse> mount fetch, 행 카드(강도바+근거칩+동반율 중립색+실시간 방향색+후행 배지), 초기3/더보기, 빈상태, quiet fallback. 색은 globals.css 토큰만(LOCKED 규칙). typecheck green.
  </done>
</task>

<task type="auto">
  <name>Task 2: stock-detail-client 마운트 + 컴포넌트 단위 테스트</name>
  <read_first>
    - webapp/src/components/stock/stock-detail-client.tsx (마운트 위치 — StockThemeChips:143 다음, 뉴스/토론 div 앞)
    - webapp/src/components/stock/stock-comovement-section.tsx (Task 1 — 마운트 대상)
    - webapp 기존 컴포넌트 테스트 (theme-chips 또는 daily-chart-section.test — fetch mock + 렌더 패턴)
    - .planning/phases/11-co-movement-candidates-top-k/11-UI-SPEC.md (표시 동작 — 3행/더보기, 빈상태 조건)
  </read_first>
  <action>
**2a. `webapp/src/components/stock/stock-detail-client.tsx` 에 마운트** (interfaces 블록):
```tsx
import { StockComovementSection } from './stock-comovement-section';
// ... (StockThemeChips 다음, <div className="space-y-6"> 앞)
<StockThemeChips stockCode={stock.code} />
<StockComovementSection stockCode={stock.code} />
<div className="space-y-6">
```

**2b. `webapp/tests/components/stock-comovement-section.test.tsx`** — fetchStockComovement mock + 렌더 (기존 webapp 컴포넌트 테스트 패턴 복제):
- Test 1: 후보 5개 응답 → 초기 3행만 렌더 + "동조 후보 2개 더 보기" 버튼. 버튼 클릭 → 5행 전체 + "접기".
- Test 2: 후보 ≤3 → 더보기 버튼 없음.
- Test 3: 빈 응답 `{candidates: []}` → "동조 데이터 부족" 빈 상태.
- Test 4: fetch reject → 섹션 미렌더 (null, error.message 노출 0).
- Test 5: 후보에 coSurgeCount 있으면 "직접동반 N회" 칩, isTrailing 이면 "후행형" 배지, sharedThemes 있으면 테마명 칩.
- Test 6: 동반율 = Math.round(confD0*100)% 표시, liveChangeRate null → "—".
- Test 7: co-surge 전용 후보(sharedThemes=[], coSurgeCount=9, confD0=0) → 동반율 영역 "—" 렌더, "0%" 미표시.
mock: `vi.mock('@/lib/comovement-api', () => ({ fetchStockComovement: vi.fn() }))` + 각 테스트 mockResolvedValue/mockRejectedValue.

**2c.** `pnpm -F @gh-radar/webapp test stock-comovement` green + `pnpm -F @gh-radar/webapp build` exit 0.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm -F @gh-radar/webapp test stock-comovement && pnpm -F @gh-radar/webapp build && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/src/components/stock/stock-detail-client.tsx` 에 `import { StockComovementSection }` + `<StockComovementSection stockCode={stock.code} />` 존재.
    - `<StockComovementSection` 가 `<StockThemeChips` 다음 + `<div className="space-y-6">` 앞 라인 (grep 라인 번호: ThemeChips < Comovement < space-y-6).
    - `webapp/tests/components/stock-comovement-section.test.tsx` 에 "동조 데이터 부족" + "더 보기" + "직접동반" + "후행형" 문자열 + co-surge 전용 동반율 "—" 테스트(Test 7) 존재.
    - `pnpm -F @gh-radar/webapp test stock-comovement` exit 0 (Test 1~7 green — 초기3/더보기, 빈상태, quiet fallback, 칩/배지, co-surge 전용 "—").
    - `pnpm -F @gh-radar/webapp build` exit 0.
    - `pnpm -F @gh-radar/webapp dev` 로 `/stocks/004090` 1회 열어 Turbopack `.js`→`.ts` resolve 회귀 없음 확인 (shared 신규 타입 re-export — lessons 2026-06-09. type-only 라 안전하나 dev 스모크 권장).
  </acceptance_criteria>
  <done>
    StockComovementSection 이 stock-detail-client 의 ThemeChips 다음에 마운트됨. 컴포넌트 테스트가 초기3/더보기·빈상태·quiet fallback·근거칩·후행배지·동반율 표시 green. build + dev 스모크 green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Vercel 배포 + 종목상세 동조 후보 섹션 시각 검증</name>
  <what-built>
    StockComovementSection 이 종목상세(/stocks/[code]) ThemeChips 다음에 마운트되고, Plan 03 의 server 라우트(/api/stocks/:code/co-movement)를 apiFetch<CoMovementResponse> 로 소비. UI-SPEC 채택안(변형 C + 기본3/더보기 + 빈상태 + quiet fallback) 구현. Vercel 에 배포.
  </what-built>
  <action>
**3a. Vercel 배포** (MEMORY reference_vercel_frontend_deploy — ignoreCommand 가 일부 push skip 발견). repo root 에서 수동 배포:
```bash
vercel pull --yes --environment=production
vercel build --prod
vercel deploy --prebuilt --prod
```
배포 URL(https://gh-radar-webapp.vercel.app) production 활성 확인. (또는 git push 후 Vercel 자동 배포가 동작하면 그대로 — 단 ignoreCommand skip 여부 확인.)
  </action>
  <how-to-verify>
1. https://gh-radar-webapp.vercel.app/stocks/004090 (한국석유 — 후보 有) 방문. 로그인 필요 시 로그인.
2. 일봉차트 → 통계 → "이 종목의 테마" 칩 **다음**에 "동조 후보" 섹션이 보이는지 확인 (Waypoints 아이콘 + "동조 후보" 캡션).
3. 후보 행 확인: 종목명/코드 + 동반율(중립 검정/흰색, 빨강/파랑 아님) + 실시간 등락률(상승=빨강/하락=파랑) + 근거 칩(공유 테마 또는 "직접동반 N회" accent) + 강도바(하단 빨강 라인). 흥구석유가 상위에 보이는지. co-surge 전용 후보("직접동반 N회" 칩만, 공유 테마 칩 없음)는 동반율이 "0%" 가 아니라 "—" 인지.
4. 후보 >3 이면 "동조 후보 N개 더 보기" 버튼 → 클릭 → 전체 펼침 → "접기".
5. 후행형 후보 있으면 "후행형" 배지(파랑 톤).
6. 다크/라이트 토글 → 색 자동 전환 (oklch 토큰).
7. 무테마 종목(theme_comovement 미존재 + co-surge 없음, Plan 02/03 에서 확인한 code) 방문 → "동조 데이터 부족" 빈 상태 박스(CircleOff 아이콘).
8. (선택) 네트워크 끊고 새로고침 또는 잘못된 종목 → 섹션이 조용히 사라지는지(에러 카피 노출 0) 확인.

UI-SPEC(11-UI-SPEC.md) + mockups/co-movement-adopted.html 와 시각 대조 — 채택안 6/6 PASS 계약 준수.
  </how-to-verify>
  <resume-signal>승인하려면 "approved", 문제가 있으면 구체적으로 기술 (예: "동반율이 빨강으로 나옴", "섹션이 테마 칩 위에 뜸", "빈 상태 카피 다름").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webapp(브라우저) → server 라우트 | apiFetch 로 동조 후보 fetch (Plan 03 라우트가 입력 검증) |
| server 응답 → webapp 렌더 | 응답 shape(객체) 불일치 시 렌더 throw 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-18 | Information Disclosure | error.message(PostgREST/내부) UI 노출 | mitigate | 에러 시 섹션 조용히 숨김(null) — error.message 미렌더 (Task 1, theme-chips/daily-chart quiet fallback + Phase 09.2 선례). 사용자 노출 카피 0 |
| T-11-19 | Tampering | 응답 shape(배열↔객체) 드리프트로 렌더 throw | mitigate | apiFetch<CoMovementResponse> ↔ server res.json<CoMovementResponse> shared 타입 일치 (Plan 01/03). 객체 계약 (lessons 2026-06-10). 컴포넌트 테스트가 {candidates} 가정 |
| T-11-20 | DoS(자기) | Vercel env trailing newline → client bundle 깨짐 | accept | 본 plan 은 신규 env 추가 0(기존 NEXT_PUBLIC_* 재사용). env 변경 없으면 newline 위협 무관 (lessons DI-04). 변경 시 tail -c1 검증 |

UI 보안은 quiet fallback + 타입 계약 일치 선례. HIGH severity 위협 0.
</threat_model>

<verification>
- `pnpm -F @gh-radar/webapp typecheck && build` exit 0.
- `pnpm -F @gh-radar/webapp test stock-comovement` green (초기3/더보기, 빈상태, quiet fallback, 칩/배지).
- `pnpm -F @gh-radar/webapp dev` `/stocks/004090` Turbopack resolve 회귀 0.
- Vercel 배포 + 종목상세 동조 후보 섹션 시각 렌더(후보 有 + 빈 상태) checkpoint 승인.
</verification>

<success_criteria>
- ROADMAP 성공기준 5 충족: 종목상세(/stocks/[code]) ThemeChips 다음에 "동조 후보" 섹션 — 후보별 동반율·표본수·후행 배지·공유 테마 칩·강도바·실시간 등락률. 테마없음/표본부족 시 "동조 데이터 부족" 빈 상태.
- UI-SPEC 채택안(변형 C + 기본3/더보기) 구현 — 색 LOCKED 규칙(동반율 중립, 실시간 방향), quiet fallback.
- apiFetch<CoMovementResponse> 객체 계약 일치 (계약 드리프트 회피).
- Vercel production 활성 + 시각 검증 — 풀스택 라이브 (Supabase→worker→server→webapp).
</success_criteria>

<output>
After completion, create `.planning/phases/11-co-movement-candidates-top-k/11-05-SUMMARY.md` (Vercel 배포 URL + 시각 검증 결과 + 채택안 준수 명시). 이 plan 완료 시 Phase 11 5개 성공기준 전부 충족 — `/gsd-verify-work` 준비 완료 명시.

커밋(GSD 경유, CLAUDE.md 규칙 — 한글, Co-Authored-By 금지, 커밋 전 사용자 확인 + `git status --short` 전체 트리): 예) `feat(11): 동조 후보 종목상세 섹션 + Vercel 배포`.
</output>
