---
phase: 11-co-movement-candidates-top-k
plan: 02
type: execute
wave: 1
depends_on: [11-01]
files_modified:
  - supabase/migrations/20260611120000_comovement_tables.sql
  - .planning/phases/11-co-movement-candidates-top-k/11-CALIBRATION.md
autonomous: false
requirements: [COMV-01]

must_haves:
  truths:
    - "supabase db push --yes 가 comovement_tables.sql 을 production 에 적용(exit 0)하고 재실행 시 'up to date'"
    - "rebuild_comovement(24) 가 **REST RPC 경로**(PostgREST)로 statement timeout 없이 jsonb {theme_comovement_rows, cosurge_edge_rows} > 0 을 반환 — Plan 04 워커 production 경로와 동일 경로"
    - "cosurge_edges 의 fixture 페어 co_count 가 실측 ground truth(흥구석유 9 / 광전자-이노 12 / 휴림 9)에 근접(±2)"
    - "EXPLAIN ANALYZE 로 co-surge self-join 의 wall-clock + 부분인덱스 사용 + 발화일 경로 plan 이 측정됨"
    - "측정된 실행시간으로 task-timeout 값이 확정되고 11-CALIBRATION.md 에 기록됨"
  artifacts:
    - path: ".planning/phases/11-co-movement-candidates-top-k/11-CALIBRATION.md"
      provides: "EXPLAIN 실측 + fixture co_count 대조 + task-timeout 확정 + lift 식 sanity"
      min_lines: 30
  key_links:
    - from: "supabase db push"
      to: "theme_comovement + cosurge_edges + idx_ohlcv_surge_bar + rebuild_comovement()"
      via: "production DDL apply"
      pattern: "Remote database is up to date"
    - from: "rebuild_comovement(24)"
      to: "cosurge_edges fixture 페어"
      via: "co_count fixture 대조"
      pattern: "co_count"
---

<objective>
[BLOCKING] 사전계산 마이그레이션을 production 에 적용하고, SQL 정확성의 1차 게이트를 실측으로 통과시킨다. 노드 mock 이 못 잡는 self-join 로직(광역일 제외·페어 카운트)을 **실제 DB 의 fixture co_count 대조**로 검증하고, RESEARCH §Open Questions 의 미확정 항목(실행시간→task-timeout, co-surge lift 정확식, 부분인덱스 사용, 발화일 경로 인덱스)을 EXPLAIN ANALYZE 로 확정한다.

Purpose: 마이그레이션 push + rebuild 실행 + fixture 대조 + 계수/timeout 확정. 이것이 통과해야 Plan 03(읽기경로 prod curl)·Plan 04(워커 task-timeout)가 동작 가능.
Output: production 에 2 테이블 + 부분인덱스 + RPC 적재 · `11-CALIBRATION.md`(EXPLAIN 실측 + fixture 대조 + timeout 확정).

이 플랜은 autonomous=false ([BLOCKING]) — Supabase 인증/CLI 가 필요하고 production DB 를 변경하므로 오케스트레이터가 사용자 승인 후 실행한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-co-movement-candidates-top-k/11-RESEARCH.md
@.planning/phases/11-co-movement-candidates-top-k/11-VALIDATION.md
@supabase/migrations/20260611120000_comovement_tables.sql

<interfaces>
<!-- push/검증 선례 — Phase 10 10-02 가 검증한 패턴. -->

db push (Phase 10 10-02 선례 — STATE.md):
- `supabase db push --yes` → migration 적용 (exit 0).
- 재실행 → "Remote database is up to date" (idempotent 확인).
- service_role REST GET 으로 테이블 존재 확인.

read-only probe / RPC 환경 (MEMORY feedback_check + lessons 2026-06-10):
- `workers/master-sync/.env` 에 SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY + KRX_AUTH_KEY (이미 존재 — 재요청 금지, feedback_dont_ask_existing_creds).
- 직접 DB connection string(패스워드)은 .env 에 없음 — service_role_key 는 REST 전용.
- EXPLAIN ANALYZE / 임의 SQL 은 **Supabase SQL Editor**(대시보드) 경유. RPC 호출은 service_role REST POST `/rest/v1/rpc/{fn}`.
- 로드: `set -a; source workers/master-sync/.env; set +a`.

fixture ground truth (RESEARCH §검증 fixture, probe 2026-06-11):
| 페어 | code | ≥10% 바 동반 co_count | 검증 |
| 한국석유↔흥구석유 | 004090↔024060 | 9 | cosurge_edges co_count ≈ 9 |
| 광전자↔이노인스트루먼트 | 017900↔215790 | 12 | co_count ≈ 12, "강함"(≥5) |
| 휴림에이텍↔휴림로봇 | 078590↔090710 | 9 | ≥3 노출 통과 |

RESEARCH §Open Questions (이 plan 이 확정):
1. rebuild_comovement() 실행 시간 + EXPLAIN → task-timeout (초기 600s, 측정 후 조정).
2. co-surge lift 정확식 — fixture 페어 lift 가 무관 페어보다 유의하게 큰지 sanity.
3. Scheduler 시각 — Plan 04 가 확정(0 2 * * 2-6 권고).
4. 발화일 경로 추가 인덱스 — EXPLAIN 이 seq scan 타면 `(code,date) WHERE change_rate>=15` 검토.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] supabase db push + 테이블/RPC 존재 검증</name>
  <read_first>
    - supabase/migrations/20260611120000_comovement_tables.sql (push 대상)
    - .planning/STATE.md (Phase 10 10-02 push 선례 — "supabase db push --yes 가 theme_tables.sql 적용 exit 0")
    - .planning/phases/11-co-movement-candidates-top-k/11-RESEARCH.md §Environment Availability (Supabase managed, service_role REST 전용)
  </read_first>
  <action>
**1a. 마이그레이션 push.** 프로젝트 검증 방법:
```bash
supabase db push --yes
```
exit 0 확인.

**1b. idempotent 재확인.** `supabase db push --yes` 재실행 → 출력에 "Remote database is up to date" 포함 확인 (새 마이그레이션 미적용).

**1c. service_role REST 로 테이블 존재 검증** (Phase 10 선례 — connection string 없이 REST). master-sync/.env 의 SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY 사용:
```bash
set -a; source workers/master-sync/.env; set +a
# theme_comovement 존재 (빈 배열이라도 200 = 테이블 존재, PGRST205 아님)
curl -fsS "${SUPABASE_URL}/rest/v1/theme_comovement?select=theme_id&limit=1" \
  -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" -w "\n%{http_code}\n"
# cosurge_edges 존재
curl -fsS "${SUPABASE_URL}/rest/v1/cosurge_edges?select=code_a&limit=1" \
  -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" -w "\n%{http_code}\n"
```
두 호출 모두 HTTP 200 (PGRST205 "table not found" 아님). 시드 부재로 빈 배열 정상.

주의: SUPABASE_URL/SERVICE_ROLE_KEY 는 master-sync/.env 또는 GCP Secret 에 이미 존재 — 사용자에게 재요청 금지(MEMORY feedback_dont_ask_existing_creds).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && set -a && source workers/master-sync/.env && set +a && curl -fsS "${SUPABASE_URL}/rest/v1/theme_comovement?select=theme_id&limit=1" -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" -o /dev/null -w "%{http_code}" | grep -q 200 && curl -fsS "${SUPABASE_URL}/rest/v1/cosurge_edges?select=code_a&limit=1" -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" -o /dev/null -w "%{http_code}" | grep -q 200 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push --yes` 첫 실행 exit 0.
    - `supabase db push --yes` 재실행 출력에 "Remote database is up to date" 포함.
    - service_role REST GET `/rest/v1/theme_comovement` → HTTP 200 (PGRST205 아님).
    - service_role REST GET `/rest/v1/cosurge_edges` → HTTP 200.
    - anon key REST GET `/rest/v1/theme_comovement?select=theme_id&limit=1` → HTTP 200 (RLS read_theme_comovement 활성 — default-deny 아님; anon key 는 master-sync/.env 의 SUPABASE_ANON_KEY 또는 Supabase 대시보드).
  </acceptance_criteria>
  <done>
    comovement_tables.sql 이 production 에 적용(2 테이블 + 부분인덱스 + RPC + RLS). 재push idempotent. service_role + anon REST 로 두 테이블 존재·RLS 활성 확인.
  </done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] rebuild_comovement 실행 + fixture co_count 대조 + EXPLAIN → task-timeout 확정</name>
  <read_first>
    - .planning/phases/11-co-movement-candidates-top-k/11-RESEARCH.md §검증 fixture (3쌍 ground truth) / §Open Questions (실행시간/lift식/인덱스) / §SQL 사전계산 (실측 행수)
    - supabase/migrations/20260611120000_comovement_tables.sql (rebuild_comovement 본문 — fixture 불일치 시 디버깅 대상)
    - .planning/STATE.md (Phase 9 — EXPLAIN/SQL 은 Supabase SQL Editor 경유)
  </read_first>
  <action>
push 직후 사전계산을 실행하고 fixture 로 SQL 정확성을 검증, EXPLAIN 으로 미확정 계수를 확정한다. **Supabase SQL Editor**(대시보드 https://supabase.com/dashboard/project/{ref}/sql) 또는 service_role RPC POST 로 실행 (connection string 부재).

**2a. rebuild 실행** — service_role REST 로 RPC 호출 (또는 SQL Editor `SELECT rebuild_comovement(24);`):
```bash
curl -fsS -X POST "${SUPABASE_URL}/rest/v1/rpc/rebuild_comovement" \
  -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" \
  -H "Content-Type: application/json" -d '{"p_lookback_months":24}'
```
반환 jsonb `{theme_comovement_rows, cosurge_edge_rows, lookback_since, rebuilt_at}` 캡처. 두 행수 > 0 확인.

**주의 — REST 경로 완주가 Plan 04 의 전제:** Plan 04 워커의 야간 production 경로가 바로 이 REST RPC 다. statement timeout 으로 실패하면 SQL Editor 우회(`SELECT rebuild_comovement(24);`)는 **캘리브레이션 한정**이며 워커는 매일 밤 실패한다. 이 경우 `ALTER ROLE service_role SET statement_timeout = '600s';` 를 후속 마이그레이션으로 추가해 해결하고 CALIBRATION 에 기록 (함수 정의 내 `SET statement_timeout` 은 이미 시작된 outer statement 의 타이머에 무효 — role 레벨이 정답).

**2b. fixture co_count 대조** (SQL 정확성 1차 게이트 — 노드 mock 이 못 잡는 self-join):
```bash
for pair in "004090:024060" "017900:215790" "078590:090710"; do
  A=${pair%%:*}; B=${pair##*:}
  curl -fsS "${SUPABASE_URL}/rest/v1/cosurge_edges?code_a=eq.${A}&code_b=eq.${B}&select=co_count,lift,avg_pair_ret" \
    -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY"
done
```
기대 (RESEARCH ground truth): 흥구석유(004090,024060) co_count ≈ 9, 광전자(017900,215790) ≈ 12, 휴림(078590,090710) ≈ 9. **허용 오차 ±2** (이벤트 정의/광역일 제외로 ±1~2 변동 가능). 크게 벗어나면(0 또는 50) self-join/광역일/이벤트컷 버그 → SQL 디버깅(broad_days·daily_bars·HAVING 점검) 후 마이그레이션 수정 + 재push + 재실행. rebuild 자체가 FK 23503(stocks 고아 code)으로 실패하면 daily_bars 의 `EXISTS (SELECT 1 FROM stocks ...)` 가드(Plan 01 SQL 포함 — ohlcv→stocks FK 가 NOT VALID 라 backfill 고아 가능) 적용 여부 확인.

**2c. theme_comovement sanity** — conf_d0 범위 + 흥구석유 앵커 검증:
```bash
# conf_d0 > 1 행이 없어야 함 (numeric(5,4) 제약 + 식 정확성) — 빈 배열 기대
curl -fsS "${SUPABASE_URL}/rest/v1/theme_comovement?select=conf_d0&conf_d0=gt.1&limit=1" -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY"
# 흥구석유(024060) 가 정유/석유 테마로 theme_comovement 에 존재
curl -fsS "${SUPABASE_URL}/rest/v1/theme_comovement?stock_code=eq.024060&select=theme_id,conf_d0,ignite_days" -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY"
```

**2d. 실행시간 + 인덱스 사용 → task-timeout 확정** (RESEARCH §Open Q 1/4). Supabase SQL Editor 에서 **두 단계로** — plpgsql 함수 호출의 EXPLAIN 은 내부 INSERT 플랜을 보여주지 않는다(Result 노드 1개 + 총 시간만):

(1) **총 실행시간:** `EXPLAIN (ANALYZE, BUFFERS) SELECT rebuild_comovement(24);` → Total Execution Time(ms) 캡처. task-timeout 산정은 이 값 기준.
(2) **인덱스 사용 — 함수 본문 SELECT 를 standalone 으로 복사해 별도 EXPLAIN:**
   - co-surge 경로: broad_days + daily_bars CTE 를 포함한 self-join SELECT 에 `EXPLAIN (ANALYZE, BUFFERS)` → daily_bars 접근이 `idx_ohlcv_surge_bar` Index Scan 인지 확인 (Seq Scan 이면 인덱스 정의/ANALYZE 통계 점검).
   - 발화일 경로: ignite CTE SELECT 에 EXPLAIN → PK(code,date) 사용 확인 — seq scan 이면 RESEARCH §Open Q 4 의 `(code,date) WHERE change_rate>=15` 추가 인덱스 검토(이번엔 측정만 기록, 필요 시 후속 마이그레이션).
- **task-timeout 결정 규칙:** Execution Time ×4 + 마진 (측정 30s→120s, 90s→360s). 초기 권고 600s 이내면 그대로, 초과면 실측 ×3. Plan 04 deploy 스크립트가 이 값 사용.

**2e. lift 식 sanity** (RESEARCH §Open Q 2): fixture 페어(2b 의 lift)가 1보다 충분히 큰지(독립 대비 초과 동반) + 무관 페어보다 큰지. lift 비정상(NaN/음수/전부 1)이면 식(`co_count / (sa.n*sb.n/total_days)`) 점검·조정 + 재push.

**2f. `11-CALIBRATION.md` 작성** — 위 실측 전부 기록: rebuild 반환 jsonb, fixture 3쌍 co_count(기대 vs 실측), conf_d0 sanity, EXPLAIN Execution Time, 부분인덱스 사용 여부, 발화일 경로 plan, **확정 task-timeout 값**, lift sanity. Plan 04 가 task-timeout 을, 후속이 인덱스 결정을 참조.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && set -a && source workers/master-sync/.env && set +a && OUT=$(curl -fsS -X POST "${SUPABASE_URL}/rest/v1/rpc/rebuild_comovement" -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" -H "Content-Type: application/json" -d '{"p_lookback_months":24}') && echo "rebuild: $OUT" && echo "$OUT" | grep -q "cosurge_edge_rows" && CNT=$(curl -fsS "${SUPABASE_URL}/rest/v1/cosurge_edges?code_a=eq.004090&code_b=eq.024060&select=co_count" -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY") && echo "흥구석유 co_count: $CNT" && test -f .planning/phases/11-co-movement-candidates-top-k/11-CALIBRATION.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - rebuild RPC 반환 jsonb 에 `theme_comovement_rows` + `cosurge_edge_rows` 둘 다 존재하고 값 > 0.
    - rebuild 가 **REST RPC 경로**(PostgREST)로 statement timeout 없이 완주 (Plan 04 워커 production 경로 전제). timeout 발생 시 `ALTER ROLE service_role SET statement_timeout` 해법 적용 + 11-CALIBRATION.md 에 기록 후 REST 재검증.
    - `cosurge_edges` 흥구석유(004090,024060) co_count ∈ [7,11] (실측 9 ±2).
    - `cosurge_edges` 광전자(017900,215790) co_count ∈ [10,14] (실측 12 ±2).
    - `cosurge_edges` 휴림(078590,090710) co_count ∈ [7,11] (실측 9 ±2).
    - `theme_comovement?conf_d0=gt.1` → 빈 배열 (conf_d0 ∈ [0,1]).
    - `theme_comovement?stock_code=eq.024060` → 최소 1행 (흥구석유가 석유 테마로 적재).
    - `11-CALIBRATION.md` 에 "Execution Time" + "task-timeout" + "idx_ohlcv_surge_bar" + fixture 3쌍 co_count 기록 존재.
  </acceptance_criteria>
  <done>
    rebuild_comovement(24) 가 두 테이블 적재(행수>0). fixture 3쌍 co_count 가 ground truth ±2 (self-join/광역일/이벤트컷 정확). conf_d0 ∈ [0,1]. EXPLAIN 으로 부분인덱스 사용·발화일 plan·Execution Time 측정 → task-timeout 확정. lift sanity. 11-CALIBRATION.md 기록.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 운영자/CLI → production Postgres | supabase db push 가 DDL 적용 (실수 시 production 영향) |
| service_role REST → rebuild_comovement() | service_role 키로 RPC 호출 (TRUNCATE + INSERT 권한) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering | db push 가 잘못된 DDL 을 production 적용 | mitigate | 마이그레이션은 Plan 01 에서 grep 검증 완료. push 후 idempotent 재push("up to date") + REST GET 으로 테이블/RLS 검증 (Task 1) |
| T-11-07 | DoS | rebuild self-join 이 statement timeout/장시간 점유 | accept→measure | EXPLAIN ANALYZE 로 실 wall-clock 측정 → task-timeout 확정 (Task 2). 광역일 제외로 self-join 60% 절감(RESEARCH). 야간 1회라 사용자 영향 0 |
| T-11-08 | Information Disclosure | RPC/probe 가 service_role 키를 로그/출력 노출 | mitigate | verify 명령이 키를 echo 하지 않음 (curl -o /dev/null 또는 데이터만 출력). master-sync/.env 는 git-ignored (재요청 금지) |

T-11-01~05(RPC REVOKE / RLS / search_path / redact / override 제외)는 Plan 01 의 마이그레이션 SQL 에서 이미 통제 — 본 plan 은 그 적용·검증. HIGH severity 위협 0.
</threat_model>

<verification>
- `supabase db push --yes` exit 0 + 재실행 "up to date".
- service_role + anon REST GET 으로 2 테이블 존재 + RLS 활성.
- rebuild_comovement(24) 반환 jsonb 행수 > 0.
- fixture 3쌍 co_count ground truth ±2 (SQL 정확성 1차 게이트).
- EXPLAIN ANALYZE 로 부분인덱스 사용 + Execution Time 측정 → task-timeout 확정.
- 11-CALIBRATION.md 에 실측 전부 기록.
</verification>

<success_criteria>
- ROADMAP 성공기준 1(테이블+부분인덱스 production 존재, >31 제외) + 2(SQL 함수 발화일/conf 계산 적재) 가 실측으로 충족.
- fixture co_count 대조로 self-join/광역일/이벤트컷 로직이 ground truth 와 일치 — Plan 03 읽기경로가 신뢰할 데이터 확보.
- task-timeout 확정으로 Plan 04 워커 deploy 가능.
- production 테이블에 데이터 존재 → Plan 03 prod curl + Plan 05 UI 렌더 가능.
</success_criteria>

<output>
After completion, create `.planning/phases/11-co-movement-candidates-top-k/11-02-SUMMARY.md` (확정 task-timeout 값 + fixture 실측 co_count + 인덱스 결정 명시 — 다운스트림 참조).

커밋(GSD 경유, CLAUDE.md 규칙 — 한글, Co-Authored-By 금지, 커밋 전 사용자 확인 + `git status --short` 전체 트리 확인): 예) `docs(11): 사전계산 마이그레이션 push + fixture 대조 + task-timeout 확정`. 마이그레이션 SQL 수정이 있었으면 함께 커밋.
</output>
