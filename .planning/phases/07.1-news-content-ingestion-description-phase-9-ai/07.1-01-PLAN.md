---
phase: 07.1
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [NEWS-01]
files_modified:
  - supabase/migrations/20260417120200_news_description.sql
  - packages/shared/src/news.ts
  - workers/news-sync/src/pipeline/map.ts
  - workers/news-sync/src/pipeline/upsert.ts
  - workers/news-sync/tests/map.test.ts
  - workers/news-sync/tests/upsert.test.ts
  - server/src/mappers/news.ts
  - server/src/routes/news.ts
  - server/tests/routes/news.test.ts
autonomous: false
threat_refs: [T-03, T-08, T-10]

must_haves:
  truths:
    - "news_articles 테이블에 description (text nullable) 컬럼이 존재한다"
    - "news-sync worker 가 신규 수집 시 Naver API description 을 stripHtml 처리 후 DB 에 저장한다"
    - "server POST /api/stocks/:code/news/refresh 도 description 을 stripHtml 처리 후 저장한다"
    - "packages/shared NewsArticle 타입에 description?: string | null 이 추가되어 server mapper 가 응답에 포함한다"
    - "기존 UPSERT idempotency (ON CONFLICT DO NOTHING) 유지 — 최초 수집 row 의 description 이 보존된다"
    - "supabase db push 로 프로덕션 DB 에 컬럼이 실제 적용된다"
    - "server/webapp/worker 전 워크스페이스 test + typecheck + build green"
  artifacts:
    - path: "supabase/migrations/20260417120200_news_description.sql"
      provides: "news_articles.description text nullable 컬럼 추가"
      contains: "ADD COLUMN IF NOT EXISTS description"
    - path: "packages/shared/src/news.ts"
      provides: "NewsArticle 타입에 description 필드"
      contains: "description"
    - path: "workers/news-sync/src/pipeline/map.ts"
      provides: "NewsArticleRow 에 description + mapToNewsRow 가 stripHtml(description) 저장"
      contains: "description"
    - path: "server/src/routes/news.ts"
      provides: "POST /refresh 가 description 을 stripHtml 후 저장"
      contains: "description"
    - path: "server/src/mappers/news.ts"
      provides: "toNewsArticle 에 description 매핑"
      contains: "description"
  key_links:
    - from: "workers/news-sync/src/pipeline/map.ts"
      to: "packages/shared stripHtml"
      via: "stripHtml(item.description) 호출 결과를 row.description 에 저장"
      pattern: "description.*stripHtml|stripHtml.*description"
    - from: "server/src/mappers/news.ts"
      to: "NewsArticle (shared)"
      via: "row.description → NewsArticle.description 매핑"
      pattern: "description"
    - from: "supabase/migrations/20260417120200_news_description.sql"
      to: "Supabase 프로덕션 DB"
      via: "supabase db push (Task 5 checkpoint)"
      pattern: "ALTER TABLE news_articles"
---

<objective>
Phase 7 에서 의도적으로 저장하지 않았던 Naver Search API 의 `description` 스니펫(기사 요약, 평균 126자)을 `news_articles.description` 컬럼에 저장하도록 전체 파이프라인을 확장한다. Phase 9 AI 요약의 입력 데이터 확보가 목표이며, URL 원문 scraping 은 본 phase 범위 아님.

Purpose: Phase 7 D4 결정("description → content_hash 계산 입력, DB에는 저장하지 않음")을 revision. 2026-04-17 Naver API 실측에서 description 이 유니크 주제를 4-5개/20건 커버하는 것으로 확인되어, Phase 9 Claude Haiku 요약의 핵심 입력으로 사용 가능하다고 판단.

Output: migration 1개 적용 + shared 타입 확장 + worker/server 파이프라인 description 저장 + 기존 테스트 회귀 확인 + 모든 워크스페이스 그린.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-news-ingestion/07-CONTEXT.md

@supabase/migrations/20260413120000_init_tables.sql
@supabase/migrations/20260417120000_api_usage.sql
@supabase/migrations/20260417120100_news_created_at_idx.sql
@packages/shared/src/news.ts
@packages/shared/src/news-sanitize.ts
@packages/shared/src/index.ts
@workers/news-sync/src/pipeline/map.ts
@workers/news-sync/src/pipeline/upsert.ts
@workers/news-sync/src/naver/searchNews.ts
@workers/news-sync/tests/map.test.ts
@workers/news-sync/tests/upsert.test.ts
@server/src/mappers/news.ts
@server/src/schemas/news.ts
@server/src/routes/news.ts
@server/tests/routes/news.test.ts
@webapp/src/components/stock/news-item.tsx

<interfaces>
Phase 7 산출물 (이미 프로덕션 live — 본 phase 가 확장):

`packages/shared/src/news-sanitize.ts` 는 `stripHtml` 를 이미 export. description 처리에 **재사용** — 신규 의존성 도입 금지.
```ts
export function stripHtml(input: string): string;
```

`packages/shared/src/news.ts` 현재 NewsArticle 타입 (확장 대상):
```ts
export type NewsArticle = {
  id: string;
  stockCode: string;
  title: string;
  source: string | null;
  url: string;
  publishedAt: string;
  contentHash: string | null;
  summaryId: string | null;
  createdAt: string;
};
```
→ 본 phase 는 `description: string | null;` 필드를 추가.

`workers/news-sync/src/pipeline/map.ts::NewsArticleRow` 현재 shape (확장 대상):
```ts
export interface NewsArticleRow {
  stock_code: string;
  title: string;
  source: string | null;
  url: string;
  published_at: string;
  content_hash: string;
}
```
→ 본 phase 는 `description: string | null;` 필드를 추가.

현재 `mapToNewsRow` 는 description 을 **stripHtml 후 content_hash 계산에만 사용**하고 row 에 저장하지 않는다. 본 phase 는 동일 `descStripped` 값을 row.description 에 **추가로** 할당 (content_hash 계산 입력은 그대로 유지 — content_hash 안정성).

`server/src/routes/news.ts` POST /refresh 핸들러는 `mapToNewsRow` 를 **inline** 으로 재구현 (Phase 7 에서 server 가 worker 패키지를 import 할 수 없어 중복 구현). 본 phase 는 worker 측 변경과 **동기** 확장 필요 — 두 곳 모두 동일하게 description 저장.

`server/src/mappers/news.ts::NewsRow` (Supabase snake_case row shape — 확장 대상):
```ts
export type NewsRow = {
  id: string;
  stock_code: string;
  title: string;
  source: string | null;
  url: string;
  published_at: string;
  content_hash: string | null;
  summary_id: string | null;
  created_at: string;
};
```
→ 본 phase 는 `description: string | null;` 추가.

`toNewsArticle` mapper 는 snake_case → camelCase 직접 매핑이므로 `description: row.description ?? null` 한 줄 추가.

`NEWS_SELECT` 상수 (server/src/routes/news.ts):
```ts
const NEWS_SELECT = "id,stock_code,title,source,url,published_at,content_hash,summary_id,created_at";
```
→ `description` 을 추가해 GET/POST 응답이 description 을 포함.

**UPSERT idempotency 확증:**
- UNIQUE(stock_code, url) + `ignoreDuplicates: true` → 중복 url 은 insert 자체가 skip.
- content_hash 는 (title + '\n' + descStripped) 로 계산됨 — **description 을 row 에 저장해도 content_hash 계산 입력은 불변** → 신규/기존 행의 content_hash 동일성 유지.
- 기존 1103 행은 description=NULL 로 유지 (ADD COLUMN IF NOT EXISTS ... text, nullable → 기본값 NULL). backfill 없음.

**RLS 확증 (retention/upsert 영향 없음):**
- `news_articles` RLS: `anon_read_news` SELECT anon. service_role 이 upsert/delete (worker + server).
- 본 phase 는 컬럼 추가만 — RLS 정책 변경 불필요.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase migration — news_articles.description 컬럼 추가</name>
  <files>
    supabase/migrations/20260417120200_news_description.sql
  </files>
  <read_first>
    - supabase/migrations/20260413120000_init_tables.sql (news_articles 원본 스키마 42~56줄)
    - supabase/migrations/20260417120000_api_usage.sql (최근 migration 스타일 — 주석/헤더 포맷)
    - supabase/migrations/20260417120100_news_created_at_idx.sql (최근 migration 스타일 — 짧은 DDL 파일 포맷)
  </read_first>
  <acceptance_criteria>
    - 파일 존재: `test -f supabase/migrations/20260417120200_news_description.sql`
    - 정확한 DDL: `grep -q "ADD COLUMN IF NOT EXISTS description" supabase/migrations/20260417120200_news_description.sql` → 1 match
    - 테이블 대상: `grep -q "ALTER TABLE news_articles" supabase/migrations/20260417120200_news_description.sql` → 1 match
    - nullable 명시: `grep -qE "description\s+text\s*;" supabase/migrations/20260417120200_news_description.sql` → 1 match (NOT NULL 금지 — 기존 1103 행 영향 방지)
    - 주석 헤더 존재: `grep -q "Phase 07.1" supabase/migrations/20260417120200_news_description.sql` → 1 match
    - IF NOT EXISTS 로 idempotent 재실행 안전 보장
  </acceptance_criteria>
  <action>
    새 파일 `supabase/migrations/20260417120200_news_description.sql` 을 **정확히 아래 내용으로** 작성:

    ```sql
    -- ============================================================
    -- Phase 07.1 — news_articles.description 컬럼 추가.
    -- Naver Search API 의 description 스니펫(평균 126자, HTML 태그 포함)을
    -- stripHtml 처리 후 저장하여 Phase 9 Claude Haiku 요약의 입력으로 사용한다.
    --
    -- 결정 근거:
    --   Phase 7 D4 revision — 원 결정은 "description → content_hash 계산 입력, DB 저장 안 함".
    --   2026-04-17 Naver API 실측 결과, description 이 유니크 주제 4-5개/20건 커버하며
    --   트레이더 핵심 정보 70-80% 포함 → Phase 9 AI 요약의 핵심 입력으로 승격.
    --
    -- 영향 범위:
    --   - nullable 컬럼 추가 → 기존 1,103 행은 description=NULL 로 유지 (backfill 없음).
    --   - content_hash 계산은 변경 없음 (title + '\n' + stripHtml(description)) — idempotency 유지.
    --   - UNIQUE(stock_code, url) + ignoreDuplicates=true → 동일 URL 재수집 시 insert skip,
    --     기존 row 의 description(NULL)은 업데이트되지 않음 → 첫 수집값 보존 정책 유지.
    --   - URL 원문 scraping 은 본 phase 범위 아님 (Phase 9 POC 후 재검토).
    -- ============================================================

    ALTER TABLE news_articles
      ADD COLUMN IF NOT EXISTS description text;
    ```

    주의:
    - NOT NULL 금지 — 기존 1,103 행에 영향을 주면 migration 이 실패한다.
    - DEFAULT 절 금지 — description 을 스니펫 저장 용도로 쓰므로 빈 문자열 기본값은 무의미.
    - INDEX 추가 금지 — description 은 full-text 검색 대상 아님 (Phase 9 AI 요약 입력 전용).
    - rollback 은 `ALTER TABLE news_articles DROP COLUMN description;` 으로 단순 가역.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260417120200_news_description.sql &amp;&amp; grep -q "ALTER TABLE news_articles" supabase/migrations/20260417120200_news_description.sql &amp;&amp; grep -q "ADD COLUMN IF NOT EXISTS description" supabase/migrations/20260417120200_news_description.sql &amp;&amp; grep -q "Phase 07.1" supabase/migrations/20260417120200_news_description.sql &amp;&amp; ! grep -qE "NOT NULL|DEFAULT" supabase/migrations/20260417120200_news_description.sql</automated>
  </verify>
  <done>migration 파일 생성, DDL 문법 검증, NOT NULL/DEFAULT 없음 확증</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: packages/shared NewsArticle 타입에 description 추가 + dist 재빌드</name>
  <files>
    packages/shared/src/news.ts
  </files>
  <read_first>
    - packages/shared/src/news.ts (현재 9줄 타입 정의)
    - packages/shared/src/index.ts (NewsArticle 이 re-export 되는지 확인)
  </read_first>
  <behavior>
    NewsArticle 타입 확장:
      - `description?: string | null;` 필드 추가 (optional + nullable)
      - 기존 코드 호환성: webapp/NewsItem 등은 description 을 읽지 않음 → 타입 확장은 non-breaking
      - 서버 응답에서 description 이 없을 수 있음 (기존 1103 행) → string | null
      - 타입 레벨에서 optional(`?`) 로도 허용 → 구 JSON 응답(Phase 7)과의 호환성 유지
  </behavior>
  <acceptance_criteria>
    - `grep -q "description" packages/shared/src/news.ts` ≥ 1 match
    - `grep -qE "description\?:\s*string\s*\|\s*null" packages/shared/src/news.ts` → 1 match
    - `pnpm -F @gh-radar/shared typecheck` exit 0
    - `pnpm -F @gh-radar/shared build` exit 0 → `packages/shared/dist/news.d.ts` 생성 + description 필드 포함
    - `grep -q "description" packages/shared/dist/news.d.ts` → 1 match (consumer 가 타입 읽을 수 있음)
  </acceptance_criteria>
  <action>
    `packages/shared/src/news.ts` 를 **정확히 아래 내용으로** 교체:

    ```ts
    export type NewsArticle = {
      id: string;
      stockCode: string;
      title: string;
      /**
       * Phase 07.1 — Naver Search API 의 기사 요약 스니펫(stripHtml 처리됨).
       * Phase 9 AI 요약의 입력 필드.
       * 기존 행(Phase 7 수집분)은 NULL — 응답에서 null 또는 생략 가능.
       */
      description?: string | null;
      source: string | null;
      url: string;
      publishedAt: string;
      contentHash: string | null;
      summaryId: string | null;
      createdAt: string;
    };
    ```

    그 다음 반드시 dist 재빌드 (consumer 워크스페이스 가 dist 를 읽음):
    ```bash
    pnpm -F @gh-radar/shared build
    ```

    이 단계를 건너뛰면 Task 3/4 에서 `Property 'description' does not exist on type 'NewsArticle'` 타입 에러가 발생한다 (Phase 7 경험).
  </action>
  <verify>
    <automated>grep -qE "description\?:\s*string\s*\|\s*null" packages/shared/src/news.ts &amp;&amp; pnpm -F @gh-radar/shared build &amp;&amp; grep -q "description" packages/shared/dist/news.d.ts</automated>
  </verify>
  <done>shared 타입 확장 + dist 재빌드 완료 + consumer 가 description 필드 타입 인식</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: workers/news-sync pipeline — map.ts NewsArticleRow 에 description 추가 + 기존 unit test 회귀</name>
  <files>
    workers/news-sync/src/pipeline/map.ts,
    workers/news-sync/tests/map.test.ts,
    workers/news-sync/tests/upsert.test.ts
  </files>
  <read_first>
    - workers/news-sync/src/pipeline/map.ts (현재 67줄 — descStripped 변수 위치 19, 54 재활용 대상)
    - workers/news-sync/src/pipeline/upsert.ts (NewsArticleRow import, 변경 없음 — Supabase upsert 는 필드 수 무관하게 동작)
    - workers/news-sync/tests/map.test.ts (기존 11개 케이스 — content_hash 결정성 테스트 보존 필요)
    - workers/news-sync/tests/upsert.test.ts (mkRow 헬퍼 — row shape 확장 반영)
    - packages/shared/src/news-sanitize.ts (stripHtml — 이미 import 됨)
  </read_first>
  <behavior>
    NewsArticleRow interface 확장:
      - 기존 6 필드 + `description: string | null` 추가
      - mapToNewsRow 는 기존에 계산하던 `descStripped` 를 row 에 할당
      - `descStripped === ""` 이면 null 저장 (빈 문자열보다 NULL 이 DB 쿼리/analytics 에 명확)
      - content_hash 계산 입력은 **변경 없음** (title + '\n' + descStripped) — 기존 수집 row 의 content_hash 재계산 시 동일값 보장

    기존 테스트 회귀:
      - "동일 input → 동일 content_hash (결정성)" 테스트는 **그대로 pass** (content_hash 계산식 불변)
      - "다른 description → 다른 content_hash" 역시 pass
      - upsert.test.ts 의 mkRow 는 description 필드 추가 반영

    신규 테스트:
      - description 이 `<b>태그</b>` 포함 시 stripHtml 처리되어 저장
      - 빈 description → row.description === null
      - description 없는 item (undefined) → row.description === null
      - description 이 HTML entity (&quot;, &#39;) 포함 → decoded 저장
  </behavior>
  <acceptance_criteria>
    - NewsArticleRow 에 description 필드: `grep -qE "description:\s*string\s*\|\s*null" workers/news-sync/src/pipeline/map.ts` → 1 match
    - mapToNewsRow 가 row.description 할당: `grep -nE "description:" workers/news-sync/src/pipeline/map.ts` → **최소 2 match** (interface + return 객체)
    - stripHtml 호출 유지: `grep -c "stripHtml" workers/news-sync/src/pipeline/map.ts` → ≥ 2 (title 1 + description 1, content_hash 입력과 공유)
    - content_hash 계산식 불변: `grep -q "title + .\\\\n. + descStripped" workers/news-sync/src/pipeline/map.ts` 또는 equivalent 유지 — 기존 "동일 input → 동일 content_hash" 테스트 그린
    - map.test.ts 에 description 관련 테스트 ≥ 3 건 추가 (stripHtml/빈 string/undefined/entity decode)
    - upsert.test.ts 의 mkRow 에 description 필드 반영: `grep -q "description" workers/news-sync/tests/upsert.test.ts` → 1+ match
    - `pnpm -F @gh-radar/news-sync test --run` exit 0 with 기존 16+ 테스트 + 신규 3+ 테스트 그린
    - `pnpm -F @gh-radar/news-sync typecheck` exit 0
    - `pnpm -F @gh-radar/news-sync build` exit 0
  </acceptance_criteria>
  <action>
    **Step A — `workers/news-sync/src/pipeline/map.ts` 를 아래로 교체:**

    ```ts
    import { createHash } from "node:crypto";
    import {
      stripHtml,
      parsePubDate,
      extractSourcePrefix,
    } from "@gh-radar/shared";
    import type { NaverNewsItem } from "../naver/searchNews.js";

    export interface NewsArticleRow {
      stock_code: string;
      title: string;
      /**
       * Phase 07.1 — Naver description 스니펫(stripHtml 처리됨).
       * 빈 문자열 또는 미제공 시 null — Phase 9 AI 요약 입력 필드.
       */
      description: string | null;
      source: string | null;
      url: string;
      published_at: string;
      content_hash: string;
    }

    /**
     * T-02 mitigation: http/https 프로토콜만 허용. javascript:/ftp: 등은 reject.
     */
    function isAllowedUrl(url: string): boolean {
      try {
        const u = new URL(url);
        return u.protocol === "https:" || u.protocol === "http:";
      } catch {
        return false;
      }
    }

    /**
     * Phase 07 — Naver Search API item → news_articles row 매핑.
     *
     * 변환 규칙:
     *  - title = stripHtml(item.title) — V-04 (HTML tag/entity 제거)
     *  - description (Phase 07.1) = stripHtml(item.description); 빈 문자열 → null
     *  - url = item.originallink || item.link (폴백)
     *  - http/https 외 프로토콜 → null 반환 (T-02)
     *  - published_at = parsePubDate(item.pubDate); 실패 시 null 반환
     *  - source = extractSourcePrefix(url)
     *  - content_hash = sha256(title + '\n' + descStripped) — 계산식 불변 (기존 row 와 동일성 보장)
     */
    export function mapToNewsRow(
      stockCode: string,
      item: NaverNewsItem,
    ): NewsArticleRow | null {
      const rawUrl = item.originallink?.trim() || item.link?.trim();
      if (!rawUrl || !isAllowedUrl(rawUrl)) return null;

      const title = stripHtml(item.title);
      if (!title) return null;

      const publishedIso = parsePubDate(item.pubDate);
      if (!publishedIso) return null;

      const descStripped = stripHtml(item.description ?? "");
      const contentHash = createHash("sha256")
        .update(title + "\n" + descStripped)
        .digest("hex");

      return {
        stock_code: stockCode,
        title,
        description: descStripped.length > 0 ? descStripped : null,
        source: extractSourcePrefix(rawUrl),
        url: rawUrl,
        published_at: publishedIso,
        content_hash: contentHash,
      };
    }
    ```

    **Step B — `workers/news-sync/tests/map.test.ts` 하단에 신규 describe 블록 추가** (기존 테스트는 변경 금지 — content_hash 결정성 보존 증빙):

    파일 끝(마지막 `});` 뒤)에 다음을 append:

    ```ts
    describe("mapToNewsRow — description (Phase 07.1)", () => {
      it("description HTML 태그/entity → stripHtml 처리되어 저장", () => {
        const row = mapToNewsRow("005930", {
          ...baseItem,
          title: "t",
          originallink: "https://ok.com/a",
          description:
            "<b>삼성전자</b>가 17일 발표한 1분기 잠정실적에 따르면 &quot;역대 최대&quot;",
        });
        expect(row).not.toBeNull();
        expect(row!.description).toBe(
          '삼성전자가 17일 발표한 1분기 잠정실적에 따르면 "역대 최대"',
        );
      });

      it("빈 description → null 저장", () => {
        const row = mapToNewsRow("005930", {
          ...baseItem,
          title: "t",
          originallink: "https://ok.com/a",
          description: "",
        });
        expect(row!.description).toBeNull();
      });

      it("undefined description (Naver 응답에 필드 없음) → null 저장", () => {
        const row = mapToNewsRow("005930", {
          ...baseItem,
          title: "t",
          originallink: "https://ok.com/a",
          description: undefined as unknown as string,
        });
        expect(row!.description).toBeNull();
      });

      it("description 만 HTML 태그 → 태그 strip 후 공백만 남으면 null", () => {
        const row = mapToNewsRow("005930", {
          ...baseItem,
          title: "t",
          originallink: "https://ok.com/a",
          description: "<b></b>",
        });
        // stripHtml trim 결과 빈 문자열 → null
        expect(row!.description).toBeNull();
      });

      it("description 저장이 content_hash 계산식을 변경하지 않는다 (기존 row 와 동일성 보장)", () => {
        // Phase 7 구현과 동일한 input 으로 content_hash 가 유지되어야 함.
        // 계산식: sha256(title + '\n' + stripHtml(description))
        const row = mapToNewsRow("005930", {
          ...baseItem,
          title: "T",
          originallink: "https://ok.com/a",
          description: "D",
        });
        // 사전 계산값: sha256("T\nD") = f7...
        // 해시 결정성만 확인 (정확값은 별도 테스트에서 이미 검증됨 — 여기서는 길이/형식만)
        expect(row!.content_hash).toMatch(/^[a-f0-9]{64}$/);
        expect(row!.description).toBe("D");
      });
    });
    ```

    **Step C — `workers/news-sync/tests/upsert.test.ts` 의 `mkRow` 헬퍼 확장:**

    파일 상단(3번째 줄 부근)의 `mkRow` 를 아래로 교체 — description 필드 기본값 null 추가:

    ```ts
    const mkRow = (code: string, url: string) => ({
      stock_code: code,
      title: "t",
      description: null,
      source: null,
      url,
      published_at: "2026-04-17T00:00:00.000Z",
      content_hash: "h",
    });
    ```

    나머지 테스트 본문은 변경하지 않음 — `expect.objectContaining({ stock_code, url })` 패턴이라 추가 필드는 assertion 에 영향 없음.

    **Step D — 실행 검증:**

    ```bash
    pnpm -F @gh-radar/news-sync build
    pnpm -F @gh-radar/news-sync test --run
    pnpm -F @gh-radar/news-sync typecheck
    ```
  </action>
  <verify>
    <automated>grep -qE "description:\s*string\s*\|\s*null" workers/news-sync/src/pipeline/map.ts &amp;&amp; [ $(grep -c "description" workers/news-sync/src/pipeline/map.ts) -ge 3 ] &amp;&amp; grep -q "description" workers/news-sync/tests/map.test.ts &amp;&amp; grep -q "description" workers/news-sync/tests/upsert.test.ts &amp;&amp; pnpm -F @gh-radar/news-sync test --run &amp;&amp; pnpm -F @gh-radar/news-sync typecheck &amp;&amp; pnpm -F @gh-radar/news-sync build</automated>
  </verify>
  <done>map.ts 확장 + map.test.ts 신규 5 case + upsert.test.ts mkRow 확장 + 전체 test/typecheck/build 그린</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: server mapper + routes — description 매핑 + POST /refresh inline mapToNewsRow 확장 + supertest 회귀</name>
  <files>
    server/src/mappers/news.ts,
    server/src/routes/news.ts,
    server/tests/routes/news.test.ts
  </files>
  <read_first>
    - server/src/mappers/news.ts (현재 33줄 — NewsRow/toNewsArticle 정의)
    - server/src/routes/news.ts (현재 228줄 — inline mapToNewsRow line 49, NEWS_SELECT line 24)
    - server/tests/routes/news.test.ts (기존 6+ 테스트 — snakeNewsRow 헬퍼 확장 필요)
    - packages/shared/dist/news.d.ts (Task 2 에서 업데이트된 NewsArticle — description 필드 존재 확인)
    - workers/news-sync/src/pipeline/map.ts (Task 3 산출물 — inline 버전의 참조 모델)
  </read_first>
  <behavior>
    server/src/mappers/news.ts:
      - NewsRow 에 `description: string | null` 추가 (Supabase column shape)
      - toNewsArticle 에 `description: row.description ?? null` 한 줄 추가 (NewsArticle camelCase)

    server/src/routes/news.ts:
      - NEWS_SELECT 상수에 `description` 추가 → GET/POST 응답 body 에 description 포함
      - inline `mapToNewsRow` 함수에 `description: descStripped.length > 0 ? descStripped : null` 할당 (worker 와 동일 규칙)
      - inline mapToNewsRow 의 **content_hash 계산식 변경 금지** (title + '\n' + descStripped) — 기존 row 와의 idempotency 보장

    server/tests/routes/news.test.ts 회귀:
      - snakeNewsRow 헬퍼에 description: null 필드 추가 (현재 응답에 description 이 포함되므로 shape 검증 일관성)
      - camelCase 응답 검증에 `description` 포함 선택적 — 기존 테스트가 toHaveProperty('stockCode') 만 assert 하므로 깨질 일 없음, 단 신규 기본 assertion 1개 추가 권장: `expect(res.body[0]).toHaveProperty('description')`
  </behavior>
  <acceptance_criteria>
    - `grep -qE "description:\s*string\s*\|\s*null" server/src/mappers/news.ts` → 1 match (NewsRow 타입)
    - `grep -q "description:" server/src/mappers/news.ts` → **≥ 2 match** (NewsRow + toNewsArticle 매핑)
    - `grep -q "description" server/src/routes/news.ts` → **≥ 2 match** (NEWS_SELECT + inline mapToNewsRow return)
    - NEWS_SELECT 상수에 description 포함: `grep -qE "NEWS_SELECT\s*=.*description" server/src/routes/news.ts` 또는 `grep -q '"id,stock_code,title,.*description.*created_at"' server/src/routes/news.ts` → 1 match
    - inline mapToNewsRow 에서 description 할당: `grep -nE "description:\s*(descStripped|stripHtml)" server/src/routes/news.ts` → 1+ match
    - content_hash 계산식 불변: `grep -q "title + .\\\\n. + descStripped" server/src/routes/news.ts` 유지
    - server/tests/routes/news.test.ts snakeNewsRow 헬퍼에 description 추가: `grep -q "description" server/tests/routes/news.test.ts` → 1+ match
    - `pnpm -F @gh-radar/server test --run` exit 0 (기존 6+ 회귀 그린)
    - `pnpm -F @gh-radar/server typecheck` exit 0
    - `pnpm -F @gh-radar/server build` exit 0
  </acceptance_criteria>
  <action>
    **Step A — `server/src/mappers/news.ts` 를 아래로 교체:**

    ```ts
    import type { NewsArticle } from "@gh-radar/shared";

    // Supabase news_articles row — snake_case.
    export type NewsRow = {
      id: string;
      stock_code: string;
      title: string;
      /** Phase 07.1 — Naver description 스니펫 (stripHtml 처리됨, Phase 9 AI 요약 입력). */
      description: string | null;
      source: string | null;
      url: string;
      published_at: string;
      content_hash: string | null;
      summary_id: string | null;
      created_at: string;
    };

    /**
     * news_articles DB row → 공용 NewsArticle (camelCase) 변환.
     * 웹앱은 packages/shared 의 camelCase NewsArticle 을 기대하므로
     * 모든 news API 응답은 이 mapper 를 통과해야 한다.
     */
    export function toNewsArticle(row: NewsRow): NewsArticle {
      return {
        id: row.id,
        stockCode: row.stock_code,
        title: row.title,
        description: row.description ?? null,
        source: row.source,
        url: row.url,
        publishedAt: row.published_at,
        contentHash: row.content_hash ?? null,
        summaryId: row.summary_id ?? null,
        createdAt: row.created_at,
      };
    }
    ```

    **Step B — `server/src/routes/news.ts` 두 곳 수정:**

    B1. `NEWS_SELECT` 상수 (line 24 부근) 변경:
    ```ts
    const NEWS_SELECT =
      "id,stock_code,title,description,source,url,published_at,content_hash,summary_id,created_at";
    ```

    B2. inline `mapToNewsRow` 함수 (line 49~68 부근) — return 객체에 description 한 줄 추가. 현재:
    ```ts
    return {
      stock_code: code,
      title,
      source: extractSourcePrefix(rawUrl),
      url: rawUrl,
      published_at: publishedIso,
      content_hash: hash,
    };
    ```
    → 다음으로 교체 (description 할당 + 주석):
    ```ts
    return {
      stock_code: code,
      title,
      // Phase 07.1 — descStripped 을 row.description 에 저장 (content_hash 계산 입력과 공유).
      description: descStripped.length > 0 ? descStripped : null,
      source: extractSourcePrefix(rawUrl),
      url: rawUrl,
      published_at: publishedIso,
      content_hash: hash,
    };
    ```

    **content_hash 계산식 (line 57~59 부근) 은 변경하지 않는다:**
    ```ts
    const hash = createHash("sha256")
      .update(title + "\n" + descStripped)
      .digest("hex");
    ```
    (이 불변성이 idempotency 의 근거 — Phase 7 수집된 row 를 다시 계산해도 동일 hash)

    **Step C — `server/tests/routes/news.test.ts` 의 `snakeNewsRow` 헬퍼 업데이트:**

    `snakeNewsRow` 함수 (파일 내 search) 를 찾아 description 필드를 추가:
    ```ts
    function snakeNewsRow(i: number) {
      return {
        id: String(i),
        stock_code: "005930",
        title: "t",
        description: `desc ${i}`,
        source: "hankyung",
        url: "https://x/" + i,
        published_at: "2026-04-17T00:00:00Z",
        content_hash: null,
        summary_id: null,
        created_at: "2026-04-17T00:00:00Z",
      };
    }
    ```

    그리고 기존 clamp 테스트(`clamps days > 7 and limit > 100`) 내부의 camelCase 검증 블록에 description 확인 한 줄 추가:
    ```ts
    if (res.body.length > 0) {
      expect(res.body[0]).toHaveProperty("stockCode");
      expect(res.body[0]).toHaveProperty("publishedAt");
      expect(res.body[0]).toHaveProperty("createdAt");
      expect(res.body[0]).toHaveProperty("description");
      expect(res.body[0].description).toBe("desc 0");
      expect(res.body[0]).not.toHaveProperty("stock_code");
      expect(res.body[0]).not.toHaveProperty("published_at");
    }
    ```

    **Step D — 실행 검증:**

    ```bash
    pnpm -F @gh-radar/server test --run
    pnpm -F @gh-radar/server typecheck
    pnpm -F @gh-radar/server build
    ```
  </action>
  <verify>
    <automated>grep -qE "description:\s*string\s*\|\s*null" server/src/mappers/news.ts &amp;&amp; [ $(grep -c "description" server/src/mappers/news.ts) -ge 2 ] &amp;&amp; grep -qE "NEWS_SELECT\s*=" server/src/routes/news.ts &amp;&amp; grep -q "description" server/src/routes/news.ts &amp;&amp; grep -q "description" server/tests/routes/news.test.ts &amp;&amp; pnpm -F @gh-radar/server test --run &amp;&amp; pnpm -F @gh-radar/server typecheck &amp;&amp; pnpm -F @gh-radar/server build</automated>
  </verify>
  <done>server mapper/route/test 3파일 확장 + 기존 6+ 테스트 + description 회귀 assertion 그린</done>
</task>

<task type="auto">
  <name>Task 5: webapp typecheck + build 회귀 확인 (NewsArticle 확장 후 깨짐 방지)</name>
  <files>
  </files>
  <read_first>
    - webapp/src/components/stock/news-item.tsx (현재 description 사용 안 함 — optional 필드 추가는 non-breaking 증빙)
    - webapp/src/components/stock/news-page-client.tsx (description 사용 여부 확인)
    - webapp/src/components/stock/stock-news-section.tsx (description 사용 여부 확인)
    - webapp/src/lib/stock-api.ts (NewsArticle import 경로 확인)
  </read_first>
  <acceptance_criteria>
    - `pnpm -F @gh-radar/webapp typecheck` exit 0 — NewsArticle.description 추가가 기존 consumer 에 영향 없음 확증
    - `pnpm -F @gh-radar/webapp build` exit 0 — Next.js 빌드 회귀 없음
    - webapp 코드는 **수정하지 않음** (본 phase scope — UI 에 description 표시 금지, Phase 9 에서 활용)
    - Playwright E2E 기존 스펙(news.spec.ts) 은 실행 불필요 (프로덕션 DB push 후 별도 QA 에서 검증)
  </acceptance_criteria>
  <action>
    1. webapp 의 NewsArticle 사용처가 모두 `description` 없이도 컴파일되는지 확인 (optional 필드라 non-breaking):
    ```bash
    pnpm -F @gh-radar/webapp typecheck
    ```
    (에러 발생 시: description 사용하지 않는데 typecheck 실패라면 shared dist 가 구버전일 가능성 — `pnpm -F @gh-radar/shared build` 재실행)

    2. Next.js 프로덕션 빌드 회귀 확인:
    ```bash
    pnpm -F @gh-radar/webapp build
    ```

    3. webapp 파일을 **수정하지 않음** — 본 phase scope 는 저장 파이프라인만. UI 표시는 Phase 9 에서 AI 요약 표시와 함께 재고.

    만약 webapp build 가 깨진다면:
    - 원인 1: shared dist 구버전 → `pnpm -F @gh-radar/shared build` 후 재시도
    - 원인 2: optional 필드가 exhaustive check (e.g., `Omit`, spread) 에서 문제 → grep 으로 NewsArticle 사용처 전수 조사, 해당 파일만 수정
    - 원인 3: eslint 의 `exactOptionalPropertyTypes` 플래그 → tsconfig 확인
  </action>
  <verify>
    <automated>pnpm -F @gh-radar/webapp typecheck &amp;&amp; pnpm -F @gh-radar/webapp build</automated>
  </verify>
  <done>webapp 회귀 없음 — description 추가는 non-breaking, UI 수정 0</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: [BLOCKING] supabase db push — 프로덕션 DB 에 description 컬럼 적용</name>
  <files>
    supabase/migrations/20260417120200_news_description.sql
  </files>
  <read_first>
    - supabase/migrations/20260417120200_news_description.sql (Task 1 산출물 — 실행될 migration)
  </read_first>
  <what-built>
    Task 1~5 에서 마이그레이션 SQL + 모든 코드/테스트가 정비되었지만, **실제 컬럼은 아직 프로덕션 DB 에 없음**. TypeScript 타입은 config 에서 오므로 build/typecheck 은 green 이지만 Cloud Run Job(news-sync) 이 다음 tick 에 insert 할 때 컬럼 부재로 runtime 실패 (PostgrestError 42703: column "description" does not exist).

    따라서 Task 6 이 완료되어야만 Phase 07.1 Success Criteria #1 ("description 컬럼이 Supabase 프로덕션 DB 에 존재한다") 이 TRUE 가 된다.

    본 checkpoint 는 프로덕션 DB 의 schema 를 변경하므로 **사용자 승인 없이 자동 실행 금지**. 사용자가 아래 how-to-verify + action 절차를 수행한 뒤 "approved" 로 재개한다.
  </what-built>
  <how-to-verify>
    사용자는 다음을 순서대로 확인:

    1. Supabase CLI 인증 상태 확인 (프로젝트는 `ivdbzxgaapbmrxreyuht` 에 link 되어 있음):
       ```bash
       supabase projects list | grep gh-radar
       ```

    2. 적용될 migration 확인 (현재 pending 상태여야 함):
       ```bash
       supabase migration list --linked
       ```
       → `20260417120200_news_description` 이 local 에만 존재하고 remote 에는 없어야 함.

    3. **[실행]** migration 적용:
       ```bash
       supabase db push
       ```
       → 성공 시 "Finished supabase db push." 출력.

    4. 컬럼 존재 검증 (Supabase SQL Editor 또는 psql):
       ```sql
       SELECT column_name, data_type, is_nullable
         FROM information_schema.columns
        WHERE table_name = 'news_articles' AND column_name = 'description';
       ```
       → 1행 반환: `description | text | YES`

    5. 기존 1103 행 영향 없음 확인:
       ```sql
       SELECT count(*) AS total,
              count(description) AS with_description,
              count(*) - count(description) AS null_description
         FROM news_articles;
       ```
       → `with_description = 0`, `null_description = total` 이어야 함 (기존 행은 모두 NULL).

    6. 다음 news-sync tick(최대 15분 대기 — intraday scheduler `*/15 9-15 * * 1-5`) 또는 **즉시 수동 트리거**:
       ```bash
       gcloud run jobs execute gh-radar-news-sync --region asia-northeast3 --wait
       ```
       → 로그에서 PostgrestError 없이 upsert 완료. 신규 삽입 행의 description 이 NOT NULL 인지 샘플 확인:
       ```sql
       SELECT stock_code, title, left(description, 60) AS desc_preview
         FROM news_articles
        WHERE created_at > now() - interval '10 minutes' AND description IS NOT NULL
        LIMIT 5;
       ```

    **롤백 경로 (문제 발생 시):**
    ```sql
    ALTER TABLE news_articles DROP COLUMN description;
    ```
    (worker/server 코드에서 description 을 optional 로 재정의 — Task 3/4 의 NEWS_SELECT 에서 description 제거 필요.)
  </how-to-verify>
  <action>
    사용자가 직접 수행 (Claude 는 프로덕션 DB 에 대해 자동으로 `supabase db push` 를 실행하지 않음):

    1. 터미널에서 프로젝트 루트로 이동 후:
       ```bash
       supabase migration list --linked
       supabase db push
       ```

    2. SQL 검증 쿼리(how-to-verify #4, #5) 를 Supabase SQL Editor 에서 실행하여 컬럼 존재 + 기존 1103 행 NULL 유지 확인.

    3. (선택) 즉시 수동 트리거로 새 row 에 description 이 정상 저장되는지 확인:
       ```bash
       gcloud run jobs execute gh-radar-news-sync --region asia-northeast3 --wait
       ```

    4. 검증이 모두 통과하면 Claude 에게 "approved" 라고 응답하여 plan 재개.

    만약 Task 3/4 의 코드가 이미 배포되어 있고 `db push` 이전에 news-sync tick 이 실행되면 PostgrestError 가 발생한다. 이를 피하려면 **Task 6 → 코드 배포 순서** 또는 Task 3/4 의 코드를 로컬에만 두고 Task 6 완료 후 배포 (기존 gh-radar 배포 스크립트는 별도 커맨드이므로 자연스럽게 분리됨).

    프로덕션 변경이므로 Claude 는 본 단계를 자동 실행하지 않고 checkpoint 로 일시정지한다.
  </action>
  <verify>
    <automated>MISSING — 사용자 수행 후 프로덕션 DB 에서 information_schema 쿼리 1행 반환 수동 확인</automated>
  </verify>
  <resume-signal>
    사용자가 `supabase db push` 성공 + 검증 쿼리 #4/#5 통과 + (선택) news-sync 수동 트리거 로그 정상 확인 후 "approved" 또는 "migration applied" 라고 응답.

    문제 발생 시 에러 메시지/로그를 공유 → 즉시 롤백 또는 fix-forward 결정.
  </resume-signal>
  <done>프로덕션 Supabase DB 에 news_articles.description 컬럼 존재 + 기존 1103 행은 NULL 유지 + 다음 tick 부터 신규 row 에 description 저장됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Phase 07.1)

| Boundary | Description |
|----------|-------------|
| Naver API → worker/server | description 필드에 HTML 태그/entity 포함 (untrusted) |
| DB (news_articles) → 서버 응답 → 브라우저 | description 이 응답 payload 에 포함 — 장차 Phase 9 에서 UI 렌더 시 안전 필요 |
| Supabase DDL → 기존 1103 행 | ADD COLUMN IF NOT EXISTS 의 데이터 영향 범위 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03 | Tampering (Stored XSS) | news_articles.description | mitigate | 저장 전 `stripHtml` 필수 적용 (worker + server inline 양쪽). Phase 9 에서 UI 렌더 시에도 React text escape(기본) 의존 — `dangerouslySetInnerHTML` 금지. 본 phase 는 저장 단계만 담당. |
| T-08 | Tampering (duplicate insertion / stale data) | UPSERT idempotency | accept | UNIQUE(stock_code, url) + ignoreDuplicates=true 유지 → 기존 URL 재수집 시 insert skip, 기존 row 의 description(NULL)은 업데이트 안 됨. "최초 수집값 고정" 정책이 유지되며, backfill 은 본 phase 밖(deferred). 이 선택은 의도적 — DO UPDATE 로 전환하면 published_at/source 도 재작성되어 created_at 보존 의미가 약해짐. |
| T-10 | Data integrity (migration) | ALTER TABLE ADD COLUMN | mitigate | IF NOT EXISTS + nullable(NOT NULL/DEFAULT 없음) → 기존 1103 행에 0 영향. rollback 경로는 `DROP COLUMN description;` 단순. Task 6 BLOCKING checkpoint 에서 사용자가 information_schema 쿼리로 적용 후 검증. |
</threat_model>

<verification>
- `test -f supabase/migrations/20260417120200_news_description.sql` → true (Task 1)
- `grep -q "ADD COLUMN IF NOT EXISTS description" supabase/migrations/20260417120200_news_description.sql` → 1 match
- `grep -qE "description\?:\s*string\s*\|\s*null" packages/shared/src/news.ts` → 1 match (Task 2)
- `grep -q "description" packages/shared/dist/news.d.ts` → 1 match (dist 재빌드 증빙)
- `grep -qE "description:\s*string\s*\|\s*null" workers/news-sync/src/pipeline/map.ts` → 1 match (Task 3)
- `[ $(grep -c "description" workers/news-sync/src/pipeline/map.ts) -ge 3 ]` (interface + return + stripHtml 호출 comment)
- `grep -q "description" server/src/routes/news.ts` → ≥ 2 match (NEWS_SELECT + inline map)
- `grep -q "description" server/src/mappers/news.ts` → ≥ 2 match (NewsRow + toNewsArticle)
- `grep -q "description" server/tests/routes/news.test.ts` → 1+ match (snakeNewsRow + assertion)
- `pnpm -F @gh-radar/shared build` exit 0
- `pnpm -F @gh-radar/news-sync test --run` exit 0 (≥ 20 테스트)
- `pnpm -F @gh-radar/news-sync typecheck` exit 0
- `pnpm -F @gh-radar/news-sync build` exit 0
- `pnpm -F @gh-radar/server test --run` exit 0
- `pnpm -F @gh-radar/server typecheck` exit 0
- `pnpm -F @gh-radar/server build` exit 0
- `pnpm -F @gh-radar/webapp typecheck` exit 0 (Task 5 — 비변경 증빙)
- `pnpm -F @gh-radar/webapp build` exit 0
- **Content-hash 불변 guardrail**: `grep -q 'title + "\\\\n" + descStripped' workers/news-sync/src/pipeline/map.ts` 또는 `grep -q 'title + "\\\\n" + descStripped' server/src/routes/news.ts` — 기존 계산식 유지로 Phase 7 수집 row 의 hash 동일성 보장
- **New dependency guardrail**: `grep -r "sanitize-html\|striptags\|dompurify\|date-fns-tz" workers/news-sync/ server/ packages/shared/` → 0 match (Phase 7 guardrail 승계)
- **프로덕션 적용 검증 (Task 6 완료 후)**: Supabase SQL Editor 쿼리 `SELECT column_name FROM information_schema.columns WHERE table_name='news_articles' AND column_name='description'` → 1행 반환
</verification>

<success_criteria>
1. `news_articles.description` (text nullable) 컬럼이 Supabase 프로덕션 DB 에 존재 — Task 6 완료 (사용자 검증)
2. news-sync worker 가 신규 수집 시 `stripHtml` 처리된 description 을 upsert — Task 3 구현 + 수동 트리거 로그 확인 (Task 6 step 6)
3. 기존 1,103 행은 description=NULL 로 유지 (backfill 없음) — Task 6 step 5 SQL 검증
4. 기존 UPSERT idempotency 유지 — content_hash 계산식 불변 + ignoreDuplicates:true 유지 (Task 3/4 grep 검증)
5. server/webapp/worker 전 워크스페이스 test + typecheck + build green — Task 3/4/5 verify 자동 명령
6. 본 phase 는 UI 를 건드리지 않음 (description 렌더는 Phase 9 에서) — webapp 파일 수정 0
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-news-content-ingestion-description-phase-9-ai/07.1-01-SUMMARY.md`:

- 적용된 migration 파일 경로 + `supabase db push` 출력 요약
- information_schema 쿼리 결과 (description 컬럼 존재 확증)
- news-sync 수동 트리거 로그 샘플 (신규 row 의 description 길이/샘플 3-5건)
- 전체 워크스페이스 test/typecheck/build 결과 (개수)
- Phase 7 content_hash 계산식 불변 확증 방식 (grep 결과 또는 기존 테스트 그린 증빙)
- Phase 9 로의 이월: "news_articles.description 이 Phase 9 AI 요약의 입력 필드로 사용될 준비 완료"
- 발견한 이슈 (Naver API description 에서 특이 케이스 등)
</output>
