---
phase: 18-gh-trade-ui-nxt-vi
plan: 25
type: execute
wave: 14
depends_on: ["18-23"]
gap_closure: true
files_modified:
  - relay/src/ws/order-handler.ts
  - relay/tests/ws-order.test.ts
  - relay/src/dma/envelope.ts
  - relay/src/order/notice-status.ts
autonomous: true
requirements: [TRADE-07]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GC-WR-01 · D-21 · D-27: 통보가 화이트리스트 `requestKind`(New/Modify/Cancel)를 실어 오면 그 값이 요청 종류의 **정본**이다 — 정정 대기가 있을 때 원주문번호 X · `noticeType:\"E\"` · `requestKind:\"Modify\"` 통보가 첫 통보로 오면 그 정정 대기가 정산된다(`order.result` 가 5초 timeout 이 아니다 · 정정 감사 행이 `timeout` 으로 남지 않는다)"
    - "통보 종류 → 요청 종류 휴리스틱(E→신규)은 wire `requestKind` 가 **비어 있을 때**(구 서버)만 쓴다 — 18-19 의 (d) 「원주문의 체결(E)은 정정 대기를 정산하지 않는다(requestKind 빈 값)」 는 그대로 green 이다"
    - "모순된 통보로는 정산하지 않는다 — 취소확인 C + Modify · 정정확인 M + Cancel · 체결 E + Cancel(체결은 취소 요청의 답이 될 수 없다)은 후보 0건이고 정산 0, 축 전멸 warn 로그가 남는다(주문번호 원문 없음 · T-16-45)"
    - "18-19 의 WR-03 교차 정산 방지(㊷ · narrowPending (a)~(e))는 한 글자도 약해지지 않는다"
    - "GC-IN-01(relay 몫): `PendingOrder` 의 원주문 참조 플래그 이름이 `refersOrg` 다 — 취소·정정을 함께 뜻하는 값에 「취소」 이름이 붙어 있지 않다. `envelope.ts` 머리 주석 · `notice-status.ts` 「M」 주석 · `order-handler.ts` 플래그 주석이 Phase 18 D-21(정정 개방) 이후의 사실을 말한다"
  artifacts:
    - relay/src/ws/order-handler.ts
    - relay/tests/ws-order.test.ts
  key_links:
    - "게이트웨이 `OrderResp.request_kind`(fbs 282-284, 브로커 ExecutionReport 의 정정취소구분) → `ParsedOrderResp.requestKind` → `narrowPending` 요청 종류 축 → 정정 대기 정산 → `order.result` + `dma_orders` 정정 행 갱신"
  prohibitions:
    - "MUST NOT 원주문번호 · 취소성 통보 하드 필터를 `refine`(0건이면 건너뜀)으로 약화한다 — 모순된 통보는 여전히 0건(GC-CR-01 규율)"
    - "MUST NOT warn 로그 필드 이름(`cancelNoticeApplied` · `noticeTypeKind` · `requestKind` 등)을 바꾼다 — 운영 로그 판독 규칙이 그 이름을 쓴다. 새 필드를 더하는 것은 된다"
    - "MUST NOT 로그에 주문번호·계좌 원문을 싣는다(T-16-45)"
    - "MUST NOT relay 를 배포한다 — 배포 순서는 18-32 가 기록하고 사람이 실행한다"
---

<objective>
relay 통보 정산의 요청 종류 축을 「wire 값이 있으면 wire 가 정본」 으로 바로잡고(GC-WR-01), 같은 파일군의 R1 IN-01·IN-02 잔재(옛 주석 · 취소 전용 시절의 플래그 이름)를 정리한다(GC-IN-01 relay 몫).

Purpose: 18-19 는 통보 종류 매핑(E→신규)과 wire `requestKind` 를 AND 로 걸었다. 그래서 정정된 주문의 체결 통보가 원주문번호와 `requestKind:"Modify"` 를 **둘 다** 명시해도(가장 모호하지 않은 통보) 약한 휴리스틱이 그것을 버린다. 정정확인(M)이 유실·지연돼 체결(E)이 첫 통보가 되면 정정 감사 행이 영구 `timeout` 이 되고, 폼은 「결과 모름」 으로 잠긴다(18-REVIEW-R2 GC-WR-01).
Output: 요청 종류 축 규칙 교체(RED 재현 → GREEN) · `refersOrg` 개명 · 주석 3곳 정정.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-REVIEW-R2.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-19-SUMMARY.md

근거 인용(재확인 완료):
- `relay/src/ws/order-handler.ts:1212-1235` — `REQUEST_KIND_BY_NOTICE_TYPE`(C→C · M→M · E→N) · `REQUEST_KIND_BY_WIRE`(New→N · Modify→M · Cancel→C), `:1274-1282` — 두 축을 AND 하드 필터로 건다.
- `relay/tests/ws-order.test.ts:1851-1918` — describe 「WR-03 — 취소·정정 대기의 요청 종류」 (a)~(e) · `mkPending`/`mkNotice` 픽스처(1583-1628). `mkNotice` 기본은 구 서버(`requestKind:""`).
- `relay/tests/helpers/frames.ts:288-336` — `FakeOrderRespInput.requestKind` 를 이미 지원한다(`gateway.pushOrderResp`).
- `relay/src/order/notice-status.ts:51` — 「정정은 v1 이 만들지 않지만(D-21)」 · `relay/src/dma/envelope.ts:33-34` — 「정정(`order_type` "M")… 만들지 않는다 (D-21)」 · `relay/src/ws/order-handler.ts:190-201` — 플래그 이름이 취소 전용 시절 것이라는 자기 고백 주석.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] 정정 대기 + 체결 E(Modify) 첫 통보 → 정정 정산 (RED 재현 → wire 가 정본)</name>
  <files>relay/src/ws/order-handler.ts, relay/tests/ws-order.test.ts</files>
  <read_first>
    - relay/src/ws/order-handler.ts (165-215 `PendingOrder` · 1150-1371 `normalizeOrderNo`~`narrowPending` 전문 — 두 맵 · 하드 필터 · 축 전멸 warn · refine ①~④)
    - relay/src/order/notice-status.ts (`statusOf` — E 는 요청 수량을 알 때 filled/partially_filled)
    - relay/tests/ws-order.test.ts (232-420 하네스 `authed`·`orderReqsOf`·`orders.inserts/updates` · 1373-1403 ㉟ 정정 왕복 · 1542-1629 ㊷ · 1851-1918 WR-03 describe)
    - relay/tests/helpers/frames.ts (288-340 `FakeOrderRespInput` — `requestKind`·`noticeType`·`orgOrderNo`)
  </read_first>
  <behavior>
    - ws ㊸: 정정 요청(원주문 X, 10주) 송신 → 게이트웨이가 `noticeType:"E"` · `orgOrderNo:X` · `requestKind:"Modify"` · 새 주문번호 Y · 체결 4주를 첫 통보로 → `order.result` 가 그 rid 로 **timeout 이 아닌** 상태(`partially_filled`)로 온다 · 정정 행 갱신이 `status:"partially_filled"`·`orderNo:Y` (수정 전: 5초 뒤 timeout = RED)
    - narrowPending (f): 정정 대기 1건 + E(org X, Modify) → 그 정정 대기
    - (g): 취소 대기 + 정정 대기(같은 X) + C(org X, Modify) → null (모순)
    - (h): 취소 대기 1건 + E(org X, Cancel) → null (체결은 취소의 답이 아니다)
    - (i): 신규 대기 + 정정 대기 + A(org "", New) → 신규 대기 (wire 가 정본 · 접수 통보)
    - 기존 (a)~(e) · ㊷ · (d)(E + 빈 requestKind → null) 무수정 green
  </behavior>
  <action>
**RED 먼저.** `ws-order.test.ts` 에 ws 케이스 「㊸ 정정 대기 + 체결 E(org X, requestKind Modify) 가 첫 통보여도 정정이 정산된다 — timeout 이 아니다 (GC-WR-01)」 를 ㊷ 뒤에 붙이고, WR-03 describe 안에 `<behavior>` (f)~(i) 를 더한다. **수정 전 코드로** 돌려 SUMMARY 표에 적는다 — ㊸ · (f) 는 RED 여야 한다(수정 전 AND 규칙이 E→신규와 Modify 를 동시에 요구해 0건). (g)·(h)·(i) 는 수정 전에도 같은 결과일 수 있는 회귀 가드다 — 결과를 그대로 적는다. ㊸ 은 ㉟ 의 정정 송신 형태와 ㊷ 의 `gateway.pushOrderResp` 형태를 그대로 쓴다.

**규칙 교체 (GC-WR-01 · D-21 · D-27).** 요청 종류 축을 「정본 한 값 + 허용 집합」 으로 다시 세운다:
① wire `requestKind` 가 화이트리스트 값이면 그 종류가 축이다. 비어 있거나 모르는 값이면(구 서버) 지금의 통보 종류 매핑(C→C · M→M · E→N)이 축이고, 둘 다 없으면 축을 적용하지 않는다.
② 통보 종류마다 **답일 수 있는 요청 종류 집합**을 화이트리스트 맵 하나로 둔다 — C→{C} · M→{M} · E→{N, M}(정정된 주문의 체결은 정정 요청의 결과로 온다) · 그 밖(A · R · 빈 값 · 모르는 값)은 제한 없음. wire 값이 그 집합 밖이면 **모순**이고 후보 0건이다(C + Modify · M + Cancel · E + Cancel).
③ 하드 필터는 「축이 있으면 `p.kind === 축`」 하나다. 0건이면 지금처럼 `null` + 축 전멸 warn — 원인 판독용으로 `kindAxis`(최종 축 기호)와 `kindConflict`(모순 여부) 두 필드를 **더한다**(기존 필드 이름은 그대로).
두 맵의 머리 주석은 이 파일의 화이트리스트 규율(부정 조건 금지 · 모르는 값은 축을 건너뜀)과 근거(fbs `request_kind` 원천 · 18-19 가 「알고 받아들인 경계」 로 적은 E→N 은 wire 가 비었을 때만의 휴리스틱)를 적는다. `narrowPending` 머리 주석의 WR-03 문단도 새 규칙으로 고친다. refine ②(신규 통보 → 신규 대기)는 wire 가 Modify 인 E 통보에서 후보를 0으로 만들면 건너뛰므로 그대로 둔다 — 그 동작을 ㊸ 이 함께 증명한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay test -- ws-order</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄 passed 수가 0 — ㊸ 이 timeout 으로 끝나면 실패한다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 수정 전 코드로 돌린 ㊸ · (f)~(i) 의 RED/GREEN 표가 있고 ㊸ · (f) 가 RED 였다
    - ㊸ · (f)~(i) green, 기존 WR-03 (a)~(e) · ㊷ · ㉟ 무수정 green — `git diff "$PLAN_HEAD_BEFORE"..HEAD -- relay/tests/ws-order.test.ts` 에서 기존 `it(` 블록 본문의 삭제 줄이 0
    - `narrowPending` 에 통보 종류별 허용 집합 맵이 하나 있고, 모순 판정이 그 맵만 읽는다 — `grep -n "Map<string, ReadonlySet" relay/src/ws/order-handler.ts` 가 1줄 이상
    - 축 전멸 warn 에 `kindAxis` · `kindConflict` 가 더해졌고 기존 필드는 그대로다
  </acceptance_criteria>
  <done>정정된 주문의 체결이 정정확인보다 먼저 와도 정정 감사 행과 폼이 「결과 모름」 에 갇히지 않는다 — 모순된 통보는 여전히 아무것도 정산하지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: GC-IN-01 relay 몫 — 원주문 참조 플래그 `refersOrg` 개명 · 옛 주석 3곳 정정</name>
  <files>relay/src/ws/order-handler.ts, relay/tests/ws-order.test.ts, relay/src/dma/envelope.ts, relay/src/order/notice-status.ts</files>
  <read_first>
    - relay/src/ws/order-handler.ts (Task 1 결과 — 플래그 선언 165-215 · `handle` 1040-1050 대기 등록 · `narrowPending` 전 구간의 참조 · 700-720 `sideOf` 주석)
    - relay/tests/ws-order.test.ts (`mkPending` 기본값 1583-1600 · 플래그를 넘기는 모든 케이스)
    - relay/src/dma/envelope.ts (1-60 머리 주석 — 33-34 「정정… 만들지 않는다 (D-21)」)
    - relay/src/order/notice-status.ts (전문 70줄 — 51 「정정은 v1 이 만들지 않지만」)
  </read_first>
  <action>
<!-- planner-discipline-allow: isCancel -->
**개명 (R1 IN-02 · GC-IN-01).** `PendingOrder` 의 원주문 참조 플래그 `isCancel` 을 `refersOrg` 로 바꾼다 — 선언 · 대기 등록(`handle` 의 `needsOrg` 대입) · `narrowPending` 의 모든 필터·refine · 테스트 픽스처와 케이스의 인자까지 전부. 같은 이유로 `narrowPending` 의 지역 변수 `isCancelNotice`(C·M 통보를 뜻한다)는 `refersOrgNotice` 로 바꾼다. **로그 필드 이름은 바꾸지 않는다**(prohibitions). 플래그 JSDoc 은 「이 대기가 원주문을 참조하는 주문(취소·정정)인가 — 원주문번호 축으로 매칭된다. 요청 종류의 정본은 `kind` 다」 로 짧게 다시 쓰고, 「이름은 Phase 16 의 것을 유지한다」 문단은 지운다(사실이 아니게 됐다). `sideOf` 주석 등 본문 주석 안의 옛 이름 인용도 새 이름으로 고친다. 동작 변경은 0 이다.

**주석 정정 (R1 IN-01 · GC-IN-01).**
- `relay/src/dma/envelope.ts` 머리 주석 「정정(`order_type` "M")·시장가·IOC/FOK 를 만들지 않는다 (D-21)」 → 정정은 Phase 18 D-21 로 열렸다(`order.modify` → `order_type "M"` + 원주문번호)는 사실과, 시장가·IOC/FOK 는 여전히 만들지 않는다는 사실을 가른다.
- `relay/src/order/notice-status.ts` `statusOf` 의 `"M"` 주석 → 「정정확인은 자기 정정(Phase 18 D-21) 또는 세션에 합류한 다른 단말 정정의 결과다 — 접수로 읽는다」.
- `relay/src/ws/order-handler.ts` 플래그 주석(위 개명 문단)으로 201 행의 자기 고백 주석이 사라진다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run test</automated>
    <fails_when>non-zero exit, 또는 relay 통과 수가 516 미만(18-23 기준선) — 개명이 동작을 바꿨다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -rn "isCancel\b" relay/src relay/tests | grep -v "isCancelNotice" | wc -l</automated>
    <fails_when>출력이 0 이 아니다 — 옛 플래그 이름이 코드나 테스트에 남았다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn "refersOrg" relay/src/ws/order-handler.ts` 가 선언·등록·필터 줄을 보여 준다(1줄 이상)
    - 옛 플래그 이름이 relay/src · relay/tests 에서 0 (위 verify)
    - `grep -n "v1 이 만들지 않지만" relay/src/order/notice-status.ts` 출력 없음 · `grep -n "D-21" relay/src/dma/envelope.ts` 로 읽은 머리 주석 줄이 정정 개방을 말한다
    - relay 전량 green · 통과 수 ≥ 516 + Task 1 신규 케이스 수
  </acceptance_criteria>
  <done>relay 주문 매칭 코드의 이름과 주석이 Phase 18 이후의 사실(정정 개방 · 원주문 참조 = 취소+정정)과 일치한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트웨이 → relay (OrderResp 51) | 통보가 실어 온 원주문번호·통보 종류·`requestKind` 로 어느 대기(어느 사용자의 어느 주문)를 정산할지 정한다 |
| relay → 브라우저 `order.result` | 잘못 정산된 결과는 사용자의 재주문(중복 체결) 판단으로 이어진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-106 | Repudiation | 정정 감사 행 영구 timeout | medium | mitigate | wire `requestKind` 정본(Task 1 ①) · ㊸ 이 E(Modify) 첫 통보 정산을 고정 |
| T-18-107 | Tampering | 모순 통보로 남의 대기 정산 | high | mitigate | 통보 종류별 허용 집합 밖이면 0건(Task 1 ②) · (g)(h) 가 C+Modify · E+Cancel 을 고정 · 원주문번호/취소성 하드 필터 무변경 |
| T-18-108 | Information Disclosure | 축 전멸 warn 로그 | low | mitigate | 새 필드는 종류 기호·불리언뿐 — 주문번호·계좌 원문 없음(T-16-45 · ㉝ 회귀 유지) |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 |
</threat_model>

## Artifacts this phase produces

- **relay:** `narrowPending` 요청 종류 축(wire 정본 + 통보 종류별 허용 집합 맵) · 축 전멸 warn 필드 `kindAxis`·`kindConflict` · `PendingOrder.refersOrg`(개명)
- **주석:** `envelope.ts` 머리 주석 · `notice-status.ts` `"M"` 주석 · `order-handler.ts` 플래그 JSDoc
- **테스트:** `ws-order.test.ts` ㊸ · WR-03 describe (f)(g)(h)(i)

<verification>
- ㊸ RED(수정 전) → GREEN · (a)~(e)·㊷·㉟ 무수정 green
- 옛 플래그 이름 0 · relay 전량 ≥ 516 green · typecheck 클린
</verification>

<success_criteria>
- 정정된 주문의 체결 통보가 정정 대기를 정산한다 — 모순된 통보는 여전히 정산하지 않는다
- 이름·주석이 코드의 현재 의미를 말한다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-25-SUMMARY.md` when done
</output>
