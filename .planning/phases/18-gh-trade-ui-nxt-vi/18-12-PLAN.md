---
phase: 18-gh-trade-ui-nxt-vi
plan: 12
type: execute
wave: 6
depends_on: ["18-11"]
files_modified:
  - webapp/src/app/trading/limit-chaser/page.tsx
  - webapp/src/app/trading/limit-chaser/new/page.tsx
  - webapp/src/app/trading/limit-chaser/[key]/page.tsx
  - webapp/src/app/trading/vi/page.tsx
  - webapp/src/components/layout/app-sidebar.tsx
  - webapp/src/components/trading/strategy-status-card.tsx
  - webapp/src/components/chat/chat-fab.tsx
  - webapp/src/components/layout/__tests__/app-sidebar.test.tsx
autonomous: true
requirements: [TRADE-09]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-02: `/trading/limit-chaser` · `/trading/limit-chaser/new` · `/trading/vi` 는 `/trading` 으로, `/trading/limit-chaser/{key}` 는 `/trading?focus={key}` 로 **서버 컴포넌트 `redirect()`** 로 보낸다 — 클라이언트에서 밀면 빈 화면이 한 프레임 그려진 뒤 이동한다 (TRADE-09)"
    - "D-02: `[key]` 는 현행 `decodeURIComponent` 를 거친 뒤 `encodeURIComponent` 로 다시 싸서 보낸다. 전략 키가 `:` 를 품으므로 `next.config` 의 정규식 리다이렉트로 옮기지 않는다 (TRADE-09)"
    - "D-02: My page 의 전략 현황 카드·계좌 패널은 구조를 바꾸지 않고 **링크 대상만** `/trading(?focus=)` 로 바뀐다 (TRADE-09)"
    - "D-03: 사이드바 「트레이딩」 그룹 제목 자체가 `/trading` 링크이고 활성 표시를 받는다. 3단 목록은 「KRX VI(가동중 배지)」 · 「NXT VI」 · 등록된 상따 전략(종목명 + LED 3점 요약, 카드와 동기)이며 개별 「상따」「VI」 메뉴는 사라진다 (TRADE-09)"
    - "D-03: 그룹 항상 펼침과 트레이딩 가시성 게이트는 Phase 16 D-16/D-19 그대로다 — 가시성 판정을 다시 쓰지 않는다 (TRADE-09)"
    - "D-03: `limitChaserHref` 는 **이름을 유지하고 본문만** 바뀐다 — 호출부 2곳(사이드바 전략 항목 · My page 전략 현황 카드)이 무수정으로 산다 (TRADE-09)"
    - "D-03: 「어느 하나라도 가동중」 판정이 필요한 곳에서만 기존 `viAnyRunning` 을 쓰고, 줄마다는 `viTriggers.KRX`/`.NXT` 를 직접 본다 — 판정을 다시 쓰지 않는다 (TRADE-09)"
    - "E16 empty: 등록 전략 0 → 3단에 「KRX VI」「NXT VI」 2항목만 남고 빈 문구가 없다 (TRADE-09)"
    - "E16 loading: 64 스냅샷 전에는 2항목만(빈 상태와 동일) · 스피너 없음 (TRADE-09)"
    - "E16 populated: 「KRX VI(가동중 배지)」 · 「NXT VI」 · 등록 전략 N개(종목명 + LED 3점 7px)가 카드 집합과 동기이고 그룹은 항상 펼침이다 (TRADE-09)"
    - "E16 partial: `RelayLimitChaser.name` 이 없는 전략은 코드를 쓰고, 코드도 없으면 ISIN 을 표시한다 (TRADE-09)"
    - "E16 overflow: 전략 수 상한이 없으므로 사이드바가 세로로 자연 확장하고 앱 셸이 스크롤한다. 앱 셸·사이드바는 **뷰포트 브레이크포인트 그대로**다 (TRADE-09)"
    - "E16 zero-one-many: 0 은 2항목 · 1+ 는 동일 항목 문법이며 단/복수 어휘가 없다 (TRADE-09)"
    - "E16 long-text: 종목명은 1줄 ellipsis 이고 전체는 `title` 에 담긴다 — 240px 폭 예산 안에서 LED 3점 요약과 공존한다 (TRADE-09)"
  artifacts:
    - webapp/src/app/trading/limit-chaser/[key]/page.tsx
    - webapp/src/components/layout/app-sidebar.tsx
  key_links:
    - "`limitChaserHref` 본문 ↔ 호출부 2곳 — 헬퍼 이름을 유지하는 것이 호출부 무수정의 조건이다"
    - "사이드바 3단 전략 목록 ↔ `/trading` 카드 집합 — 같은 `limitChasers` 를 보므로 자동으로 동기된다(별도 동기화 로직 없음)"
    - "`[key]` 리다이렉트 ↔ `/trading?focus=` 소비(18-11) — 인코딩 왕복이 양쪽에서 대칭이어야 카드가 열린다"
  prohibitions:
    - "MUST NOT `next.config` 정규식 리다이렉트로 옮긴다 — 전략 키가 `:` 를 품고 e2e 에서 dev 서버 설정 변경을 요구해 회귀를 잡기 어려워진다"
    - "MUST NOT 트레이딩 가시성 게이트·VI 가동 판정·전략 키 헬퍼를 사이드바에서 다시 구현한다 — 전부 단일 정의가 이미 있다"
    - "MUST NOT 사이드바에 컨테이너 쿼리를 도입한다 — 앱 셸·사이드바는 뷰포트 브레이크포인트가 정본이다"
---

<objective>
옛 화면 셋을 `/trading` 으로 보내고 사이드바를 새 구조로 바꾼다 — 리다이렉트 4개(그중 하나는 동적 세그먼트를 쿼리로 옮기는 변환), 「트레이딩」 그룹 제목의 링크 승격, 3단 목록 재구성(KRX VI · NXT VI · 등록 전략), 그리고 흩어진 링크 지점 정리.

Purpose: 18-11 이 `/trading` 을 세웠으므로 이제 **들어오는 길**을 하나로 모은다. 헬퍼 이름을 유지하는 방식으로 호출부 수정을 최소화하는 것이 이 플랜의 설계 선택이다.
Output: 리다이렉트 라우트 4개 · 사이드바 재구성 · 링크 대상 갱신.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 리다이렉트 라우트 4개 — `/trading` 과 `?focus=`</name>
  <reversibility rating="costly">D-01/D-02 — 옛 화면 3개의 진입로가 여기서 사라진다. 되돌리려면 이 태스크의 커밋을 `git revert` 해 네 라우트 파일의 이전 내용(상따 `new`·`[key]` 폼 진입, VI 페이지 셸)과 Task 2 가 걷어낸 사이드바 「상따」·「VI」 항목을 함께 복원해야 한다 — 라우트만 되돌리면 18-13 이 지운 클라이언트 컴포넌트가 없어 빌드가 깨진다.</reversibility>
  <files>webapp/src/app/trading/limit-chaser/page.tsx, webapp/src/app/trading/limit-chaser/new/page.tsx, webapp/src/app/trading/limit-chaser/[key]/page.tsx, webapp/src/app/trading/vi/page.tsx</files>
  <read_first>
    - webapp/src/app/trading/limit-chaser/page.tsx (파일 전문 — 서버 컴포넌트 `redirect()` 선례와 「네트워크 왕복 한 번에 끝나게 한다」 docstring)
    - webapp/src/app/trading/limit-chaser/[key]/page.tsx (현행 `decodeURIComponent` 지점과 Next 15 `params` 가 Promise 인 형태)
    - webapp/src/app/trading/vi/page.tsx (18-11 이 조립 패턴을 가져간 뒤 남은 내용)
    - webapp/src/components/layout/app-sidebar.tsx (`limitChaserHref` 의 인코딩 규율)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md §Pattern 6
  </read_first>
  <action>
네 라우트를 전부 **서버 컴포넌트 `redirect()`** 로 바꾼다.

- `/trading/limit-chaser/page.tsx` · `/trading/limit-chaser/new/page.tsx` · `/trading/vi/page.tsx` → `redirect('/trading')`.
- `/trading/limit-chaser/[key]/page.tsx` → `params` 를 await 한 뒤(Next 15 는 `params` 가 Promise) 현행 `decodeURIComponent` 를 거쳐 원문 키를 얻고, `encodeURIComponent` 로 다시 싸서 `redirect('/trading?focus=' + …)`. 클라이언트 컴포넌트였다면 서버 컴포넌트로 되돌린다.

각 파일의 docstring 은 기존 선례의 논거를 잇는다 — 「서버 컴포넌트로 두어 리다이렉트가 네트워크 왕복 한 번에 끝나게 한다. 클라이언트에서 밀면 빈 화면이 한 프레임 그려진 뒤 이동한다.」 그리고 `[key]` 파일에는 「`next.config` 로 옮기지 않는다 — 키가 `:` 를 품고, 디코드→재인코드를 정규식으로 하는 것보다 여기서 하는 편이 기존 규율과 같다」를 적는다.

`vi/page.tsx` 가 갖고 있던 AppShell 조립 패턴은 18-11 이 `app/trading/page.tsx` 로 옮겼으므로, 여기서는 리다이렉트만 남긴다. `vi-client.tsx`·`limit-chaser-client.tsx` 등 더 이상 쓰이지 않는 파일의 **삭제는 18-13 이 한다**(e2e 정리와 함께).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "redirect(" webapp/src/app/trading/limit-chaser/page.tsx webapp/src/app/trading/limit-chaser/new/page.tsx webapp/src/app/trading/vi/page.tsx "webapp/src/app/trading/limit-chaser/[key]/page.tsx"</automated>
    <fails_when>어느 파일이든 카운트가 0, 또는 grep 이 non-zero</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "focus=" "webapp/src/app/trading/limit-chaser/[key]/page.tsx"</automated>
    <fails_when>카운트가 0 — 동적 세그먼트가 쿼리로 옮겨지지 않았다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -q "redirects" webapp/next.config.* 2>/dev/null || grep -c "limit-chaser" webapp/next.config.*</automated>
    <fails_when>`next.config` 에 상따 경로 리다이렉트가 들어갔다(카운트 1 이상) — 키가 `:` 를 품어 정규식 왕복이 깨진다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 네 라우트 파일이 전부 `redirect(` 를 호출하고 클라이언트 컴포넌트 지시자가 없다
    - `[key]` 가 디코드 → 재인코드 왕복을 거쳐 `?focus=` 로 보낸다
    - `next.config` 에 상따/VI 경로 리다이렉트가 없다
    - 각 파일 docstring 에 서버 컴포넌트를 쓰는 이유가 남아 있다
  </acceptance_criteria>
  <done>옛 북마크·링크가 전부 `/trading` 으로 모이고, 전략 키 북마크는 그 카드를 펼친 채 연다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 사이드바 「트레이딩」 그룹 재구성 — 제목 링크 + 3단 3종</name>
  <files>webapp/src/components/layout/app-sidebar.tsx, webapp/src/components/layout/__tests__/app-sidebar.test.tsx</files>
  <read_first>
    - webapp/src/components/layout/app-sidebar.tsx (전문 — `NavLeaf` 인터페이스 · `GroupHeading`(비링크) · `StrategyItem`(`<Link href aria-current` · `LINK_ACTIVE`/`LINK_IDLE` · 「240px 폭에서 종목명이 3~4글자로 잘렸다」 근거 주석) · `useTradingVisible` · `limitChaserHref` · `samePath` · `viBadgeOf`/`viAnyRunning` import)
    - webapp/src/components/trading/latch-led.tsx (LED 3점 요약에 쓸 판정 — 다시 쓰지 않는다)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §레이아웃 계약의 사이드바 문단 · §UI Considerations E16 행 전부
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md §사이드바 헬퍼 표(재구현 금지 목록)
  </read_first>
  <behavior>
    - 「트레이딩」 그룹 제목이 `/trading` 링크이고, `/trading` 에 있을 때 활성 표시를 받는다.
    - `/trading?focus=…` 에서도 그룹 제목이 활성이다(쿼리는 활성 판정에 영향을 주지 않는다).
    - 3단에 「KRX VI」·「NXT VI」 항목이 항상 있다.
    - `viTriggers.KRX.run` 이 참이면 KRX VI 항목에 「가동중」 배지가 붙고, NXT 는 독립적으로 판정된다.
    - 등록 전략 0개 → 3단에 VI 2항목만.
    - 등록 전략 N개 → 종목명 + LED 3점 요약이 붙은 항목 N개가 추가된다.
    - `name` 이 없는 전략은 코드, 코드도 없으면 ISIN 을 표시한다.
    - 개별 「상따」·「VI」 메뉴 항목이 없다.
    - 긴 종목명이 1줄 ellipsis + `title`.
    - 트레이딩 가시성 게이트가 꺼져 있으면 그룹 전체가 렌더되지 않는다.
  </behavior>
  <action>
`GroupHeading` 을 **링크로 승격**한다 — 현재는 `label`+`icon` 만 받는 비링크다. `StrategyItem` 의 `<Link href aria-current={active ? "page" : undefined} data-nav-item …>` + `LINK_ACTIVE`/`LINK_IDLE` 조합을 그대로 쓰고, 활성 판정은 기존 `samePath` 를 재사용한다(쿼리는 무시한다 — 경로만 비교).

3단 목록을 재구성한다 — 「KRX VI」(가동중이면 배지) · 「NXT VI」 · 등록된 상따 전략 N개(종목명 + LED 3점 7px 요약). 개별 「상따」·「VI」 메뉴 상수를 제거한다. VI 가동 배지는 줄마다 `viTriggers.KRX`/`.NXT` 를 **직접** 보고, 「어느 하나라도 가동중」이 필요한 자리에서만 기존 `viAnyRunning` 을 쓴다 — 판정을 다시 쓰지 않는다.

LED 3점 요약은 `latchLedStateOf` 판정을 그대로 부르고 7px 점 3개로 압축한다. `StrategyItem` 주석의 「240px 폭에서 종목명이 3~4글자로 잘렸다」 근거를 유지하고, LED 3점이 그 폭 예산 안에 들어가야 한다는 사실을 한 줄 덧붙인다.

`limitChaserHref` 는 **이름을 유지하고 본문만** `/trading?focus=${encodeURIComponent(key)}` 로 바꾼다 — 호출부 2곳이 무수정으로 산다. `useTradingVisible` 과 그룹 항상 펼침은 Phase 16 D-16/D-19 그대로다.

사이드바는 **뷰포트 브레이크포인트가 정본**이다 — 컨테이너 쿼리를 도입하지 않는다.

테스트 `__tests__/app-sidebar.test.tsx` 는 `<behavior>` 10케이스를 RTL 로 덮는다(파일이 없으면 신설한다).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- app-sidebar</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "limitChaserHref" webapp/src/components/layout/app-sidebar.tsx webapp/src/components/trading/strategy-status-card.tsx</automated>
    <fails_when>어느 파일이든 카운트가 0 — 헬퍼 이름이 바뀌어 호출부가 깨졌다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "/trading?focus=" webapp/src/components/layout/app-sidebar.tsx</automated>
    <fails_when>카운트가 0 — 헬퍼 본문이 새 경로로 바뀌지 않았다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "@container" webapp/src/components/layout/app-sidebar.tsx</automated>
    <fails_when>non-zero exit — 사이드바에 컨테이너 쿼리가 들어갔다(앱 셸은 뷰포트 브레이크포인트가 정본)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 그룹 제목이 `/trading` 링크이고 `?focus=` 가 붙어도 활성임이 단언된다
    - 3단에 VI 2항목이 항상 있고 거래소별 「가동중」 배지가 독립 판정됨이 단언된다
    - 등록 전략 N개가 종목명 + LED 3점으로 표시됨이 단언된다
    - `name`/`code` 부재 폴백 순서가 단언된다
    - 개별 「상따」·「VI」 메뉴가 없음이 단언된다
    - `limitChaserHref` 이름이 유지되고 호출부 2곳이 무수정이다
    - 사이드바에 컨테이너 쿼리가 없다
  </acceptance_criteria>
  <done>사이드바에서 트레이딩 그룹 제목을 눌러 작업대로 가고, 3단에서 VI 두 거래소와 등록 종목을 한눈에 본다.</done>
</task>

<task type="auto">
  <name>Task 3: 흩어진 링크·주석 지점 정리</name>
  <files>webapp/src/components/trading/strategy-status-card.tsx, webapp/src/components/chat/chat-fab.tsx</files>
  <read_first>
    - webapp/src/components/trading/strategy-status-card.tsx (`limitChaserHref` 호출부와 VI 화면 직접 링크)
    - webapp/src/components/chat/chat-fab.tsx (파일 머리 주석의 경로 예시 · `STOCK_DETAIL_PATH` — FAB 은 종목상세에서만 뜬다는 동작)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md §「함께 바뀌는 링크 지점(전수)」 표
  </read_first>
  <action>
RESEARCH §「함께 바뀌는 링크 지점(전수)」 표를 기준으로 남은 지점을 정리한다.

- `strategy-status-card.tsx`: `limitChaserHref` 호출부는 헬퍼가 바뀌었으므로 **무수정**이다. 다만 VI 화면을 직접 가리키는 링크는 `/trading` 으로 바꾼다. My page 의 전략 현황 카드·계좌 패널 **구조는 바꾸지 않는다**(D-02 — 이번엔 링크 대상만).
- `chat-fab.tsx`: 파일 머리 주석의 경로 예시를 갱신한다. **동작은 바꾸지 않는다** — FAB 은 `STOCK_DETAIL_PATH` 기준으로 종목상세에서만 뜨고, 이 phase 가 그 조건을 건드리지 않는다.

`limit-chaser-client.tsx` 의 a11y 주석에 남아 있는 옛 경로 문구가 있으면 함께 갱신한다(파일 자체의 삭제는 18-13 몫).

`grep -rn "/trading/limit-chaser\|/trading/vi" webapp/src` 로 남은 지점을 **전수 확인**하고, 리다이렉트 라우트 파일 자신을 제외한 모든 참조가 새 경로이거나 주석임을 SUMMARY 에 목록으로 남긴다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && grep -rn "/trading/vi\|/trading/limit-chaser" webapp/src --include=*.tsx --include=*.ts | grep -v "^webapp/src/app/trading/" | grep -vE ":[0-9]+: *(//|\*|/\*)" | wc -l</automated>
    <fails_when>출력 숫자가 0 이 아니다 — 리다이렉트 라우트 파일과 주석을 뺀 실제 코드에 옛 경로 참조가 남아 있다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- strategy-status-card</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test</automated>
    <fails_when>non-zero exit, 또는 통과 수가 Phase 17 기준선 1008 보다 작다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build &amp;&amp; pnpm --filter @gh-radar/relay run typecheck &amp;&amp; pnpm --filter @gh-radar/relay run typecheck:tests &amp;&amp; pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 리다이렉트 라우트와 주석을 제외한 코드에 옛 경로 참조가 0건이다
    - `strategy-status-card.tsx` 의 `limitChaserHref` 호출부가 무수정이다
    - My page 전략 현황 카드·계좌 패널의 **구조**가 바뀌지 않았다(링크 대상만 변경)
    - `chat-fab.tsx` 의 동작(종목상세에서만 표시)이 바뀌지 않았다
    - 전수 확인 목록이 SUMMARY 에 있다
    - config `build_command` 전문이 exit 0
  </acceptance_criteria>
  <done>옛 경로를 가리키는 코드가 저장소에 남아 있지 않다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 외부 URL(북마크·링크) → 라우트 | `[key]` 동적 세그먼트가 쿼리로 변환되는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-58 | Input Validation | `[key]` 디코드→재인코드 | medium | mitigate | 서버 컴포넌트에서 `decodeURIComponent` → `encodeURIComponent` 왕복. `/trading` 쪽은 `parseStrategyKey` 로만 해석하고 어긋나면 무시한다 |
| T-18-59 | Spoofing | 그룹 제목 활성 오표시 | low | mitigate | 기존 `samePath` 재사용. 활성 판정을 새로 쓰지 않는다 |
| T-18-60 | Elevation of Privilege | 가시성 게이트 우회 | high | mitigate | `useTradingVisible` 단일 정의를 그대로 쓴다. 사이드바에서 재구현 금지 |
| T-18-61 | Spoofing | VI 가동 배지 오표시 | medium | mitigate | 줄마다 `viTriggers.{거래소}` 를 직접 보고, 합산 판정이 필요한 곳에서만 기존 `viAnyRunning` 을 쓴다 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 phase 는 새 패키지를 설치하지 않는다(RESEARCH §Package Legitimacy Audit 표 비어 있음) |
</threat_model>

## Artifacts this phase produces

새로 만드는 심볼·파일 (phase 전체 — 드리프트 검증에서 제외되어야 한다):

- **마이그레이션:** `supabase/migrations/20260921120000_dma_orders_modify_offhours.sql` · 컬럼 `dma_orders.piece_count` · `dma_orders.krx_session`
- **shared:** `RelayOrderModifyMsg` · `RelayKrxSession` · `OrderType` 의 `"M"` 멤버 · `RelayOrderNewMsg.pieceCount?` · `RelayOrderNewMsg.krxSession?` · `RelayRateCrossItem.name?` · `RelayRateCrossItem.code?`
- **relay:** `RelayOrderModifySchema` · `OrderInsertRow.pieceCount?` · `OrderInsertRow.krxSession?` · 돌파 항목 보강 private 메서드 · `relay/src/ws/__tests__/protocol.test.ts`
- **webapp lib:** `RelayOrderRequest.kind` 의 `"modify"` · `queued-window.ts`(`OrderButtonMode`·`ManualOrderAffordance`·`affordanceOf`) · `breakout-list.ts`(`kstDateKey`·`shouldRemoveBreakout`·`breakoutRowsFrom`·`BreakoutRow`·키 상수 4종·`REMOVE_MARGIN_PCT`·`ARM_GRACE_MS`·`HIGHLIGHT_MS`) · `use-breakout-quotes.ts`(`useBreakoutQuotes`·`MAX_BREAKOUT_SUBS`) · `alert-tone.ts`(`playBreakoutTone`·`isTonePlaybackBlocked`·`resumeToneContext`) · `limit-chaser.ts` 로 이동한 `parseStrategyKey` · 작업대로 옮긴 `useLeaveWarning`
- **webapp 컴포넌트:** `components/trading/workbench/` 의 `TradingWorkbench`·`WorkbenchStatusBar`·`ViSettingsRows`·`ViTriggerStrip`·`BreakoutStrip`·`StockAddBar`·`CardGrid`·`SharedPanels` · `components/trading/card/` 의 `StrategyCard`·`CardHeader`·`QuoteGrid10`·`CardBody`·`ManualOrderForm`(+`canModify`)·`StockInfoModal` · `LimitChaserForm` 의 `dirtyHint?` prop · `AccountPanel` 의 `onSelectUnfilled?`/`selectedOrderNo?` prop · 링크로 승격된 `GroupHeading`
- **라우트:** `webapp/src/app/trading/page.tsx` · 리다이렉트로 바뀐 기존 라우트 4개
- **CSS 토큰:** `--new-bg` · `--new-bd` (globals.css §9 라이트/다크 양쪽) · 컨테이너 이름 `wb`
- **e2e:** `webapp/e2e/specs/trading-workbench.spec.ts`

<verification>
- config `build_command` 전문이 exit 0
- `pnpm --filter @gh-radar/webapp test` 전량 exit 0 (통과 수 1008 이상)
- 옛 경로 코드 참조 0건(리다이렉트 라우트·주석 제외)
</verification>

<success_criteria>
- 옛 화면 셋이 전부 `/trading` 으로 리다이렉트된다
- 전략 키 북마크가 그 카드를 펼친 채 연다
- 사이드바 그룹 제목이 링크이고 3단이 VI 2종 + 등록 전략이다
- 재구현 금지 헬퍼(가시성·VI 가동·키 헬퍼·활성 판정)가 전부 단일 정의로 남아 있다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-12-SUMMARY.md` when done
</output>