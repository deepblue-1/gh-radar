---
phase: 18-gh-trade-ui-nxt-vi
plan: 09
type: execute
wave: 3
depends_on: ["18-06"]
files_modified:
  - webapp/src/components/trading/workbench/shared-panels.tsx
  - webapp/src/components/trading/card/stock-info-modal.tsx
  - webapp/src/components/orderbook/account-panel.tsx
  - webapp/src/components/trading/__tests__/shared-panels.test.tsx
  - webapp/src/components/trading/__tests__/stock-info-modal.test.tsx
autonomous: true
requirements: [TRADE-09]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-13: 하단 공용 패널은 「미체결 (N) / 잔고 (N) / 전략 로그」 3탭이고 전 종목 합산(종목 열 포함)이다. 컨테이너 `wb` 700 이상은 격자 아래 섹션, 폰 밴드는 하단 sticky 접이식 바다 (TRADE-09)"
    - "D-21: 미체결 행에 주문No 열과 취소 버튼이 있고, 행 클릭이 수동주문 폼의 정정/취소 대상 선택을 일으킨다. 행 `title` 은 「행을 누르면 수동주문 폼에서 정정·취소할 수 있어요」 (TRADE-09)"
    - "D-13: 공용 패널의 계좌 축은 **상태줄에서 고른 단일 계좌**다 — `AccountPanel` 의 계좌 전용 모드를 쓰고 다계좌 합산을 새로 만들지 않는다(그것은 `/me` 담당, MYPAGE-01 과 표면 중복 방지) (TRADE-09)"
    - "D-25: 카드 헤더 ⓘ 가 여는 모달은 탭 「차트 | 종목정보 | 뉴스·토론」 3개이고 내용은 기존 종목상세 4탭에서 호가주문을 뺀 것을 **그대로 재사용**한다 — 새 데이터 경로가 없다. 닫기는 ✕ · 배경 클릭 · ESC 셋 다 (TRADE-09)"
    - "D-28: 모달은 `container-type` containment 를 벗어나야 하므로 `document.body` 포털이다(Radix Dialog 기본 Portal 로 충족되는지 확인하고, 아니면 명시 포털로 감싼다). 폰은 전체화면 시트 · 700 이상은 최대 960px 중앙 (TRADE-09)"
    - "D-30: `code` 가 없는 카드(돌파 유래 · relay lookup 실패)에서는 ⓘ 가 비활성이라 팝업이 열리지 않는다 — 팝업 내용이 단축코드 키로 조회되기 때문이다 (TRADE-09)"
    - "D-25: 팝업을 닫으면 언마운트한다 — 모달 N개가 쌓이지 않게 한다(기존 종목상세의 `forceMount` 규율은 호가주문 탭에만 예외였다) (TRADE-09)"
    - "E13 empty: 탭별 빈 문구 — 「미체결 주문이 없어요」 · 「보유 종목이 없어요」 · 「아직 기록이 없어요」. 탭 카운트는 「미체결 (0)」 (TRADE-09)"
    - "E13 loading: 미체결·잔고는 인증 직후 relay 스냅샷으로 채워진다. 스냅샷 전은 빈 문구이고 스피너가 없다 (TRADE-09)"
    - "E13 error: 행 취소 버튼 실패는 그 행 인라인 `role=\"status\"` 3분기 문구로 고지한다. 목록 자체의 로드 실패 경로는 없다(push) (TRADE-09)"
    - "E13 populated: 미체결 열은 종목 · 거래소 · 구분 · 주문가 · 주문/미체결 · 주문No · 취소, 잔고 열은 종목 · 수량 · 매도가능 · 평단 · 현재가 · 평가손익 · 손익률, 로그는 시각 + 메시지다. 출처 배지는 「상따」/「수동」 (TRADE-09)"
    - "E13 partial: 주문No 없는 미체결 행(접수 전)은 취소 버튼·행 선택이 disabled + `title` 사유(기존 Q-ID 가드). 잔고에 현재가 미수신 종목은 평가손익 「—」 (TRADE-09)"
    - "E13 zero-one-many: 탭 라벨 「(N)」 은 숫자형 · 0 은 빈 문구 · 1+ 는 동일 행 문법으로 단/복수 어휘 분기가 없다 (TRADE-09)"
    - "E13 long-text: 전략 로그 메시지는 clamp 없이 줄바꿈 허용(`min-width:0` + `word-break:keep-all`)이고 종목 열은 1줄 ellipsis 다 (TRADE-09)"
    - "E14 empty: 뉴스/토론 0건은 기존 종목상세 탭의 빈 문구 그대로(신규 문구 없음) (TRADE-09)"
    - "E14 loading: 기존 종목상세 탭의 로딩 처리 그대로 — 준비 중이면 준비 중이라고 쓴다(`surface-placeholder` 규율) (TRADE-09)"
    - "E14 error: 기존 종목상세 탭의 오류 처리 그대로. 새 데이터 경로가 없다 (TRADE-09)"
    - "E14 populated: 폰 전체화면 시트 / ≥700 최대 960px 중앙 · 탭 3개 · 기존 4탭 내용에서 호가주문 제외 · 닫기 ✕·배경·ESC (TRADE-09)"
    - "E14 partial: 탭별 독립 로드 — 한 탭이 실패해도 다른 탭은 정상(기존 종목상세 규율) (TRADE-09)"
    - "E14 overflow: 모달 본문 세로 스크롤(Radix Dialog) · 차트는 컨테이너 폭에 맞춘다(lightweight-charts resize, hex/rgb 색 주입 — oklch 직접 주입 금지) (TRADE-09)"
    - "E14 long-text: 제목 종목명은 wrap 허용, 본문 텍스트는 기존 컴포넌트 규율 그대로 (TRADE-09)"
    - statement: "컨테이너 `wb` 700 이상은 표 열 접기 + 세로 자연 확장이고, 폰 sticky 패널 펼침 시 격자·더티 바와 겹침 0 · 잘림 0 이다 (E13 overflow)"
      verification: backstop
  artifacts:
    - webapp/src/components/trading/workbench/shared-panels.tsx
    - webapp/src/components/trading/card/stock-info-modal.tsx
  key_links:
    - "미체결 행 선택 ↔ 수동주문 폼의 `selectedUnfilled` prop(18-07) — 이 연결이 정정/취소의 유일한 진입이다"
    - "`stock-info-modal.tsx` ↔ `stock-detail-tabs.tsx` 의 탭 본문 — 재사용이지 재구현이 아니다(새 데이터 경로 없음)"
    - "카드 `code` 부재 ↔ ⓘ 비활성 ↔ 팝업 미개방 — 세 지점이 같은 사실을 말한다"
  prohibitions:
    - "MUST NOT z-index 로 더티 바가 공용 패널·채팅 FAB 을 덮게 한다 — 덮으면 진입점이 조용히 사라진다. 여백이나 접힘으로 푼다"
    - "MUST NOT 팝업 안에 호가주문 탭을 넣는다 — 보이지 않는 탭에서 실시간 호가 구독을 유지하지 않는다"
    - "MUST NOT 차트에 oklch 색 토큰을 직접 주입한다 — lightweight-charts 가 거부한다. hex/rgb 로 변환해 주입한다"
    - "MUST NOT 공용 패널에 다계좌 합산을 새로 만든다 — `/me` 와 표면이 중복된다"
---

<objective>
작업대 아래쪽 두 표면을 만든다 — 전 종목 합산 **공용 패널**(미체결·잔고·전략 로그 3탭, 미체결 행 클릭이 정정/취소 대상 선택)과 카드 헤더 ⓘ 가 여는 **종목정보 팝업**(차트·종목정보·뉴스·토론 3탭).

Purpose: 공용 패널의 미체결 행 선택이 D-21 정정/취소의 **유일한 진입**이고, 팝업은 「여러 메뉴 왔다갔다 안 하고 그 페이지에서 해결」이라는 이 phase 의 원 컨셉을 완성한다. 둘 다 기존 컴포넌트 재사용이 본체라 새 데이터 경로가 없다.
Output: `workbench/shared-panels.tsx` · `card/stock-info-modal.tsx` 신설 + `account-panel.tsx` 에 행 선택 prop 추가.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-PATTERNS.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `account-panel.tsx` 에 미체결 행 선택 prop 추가</name>
  <files>webapp/src/components/orderbook/account-panel.tsx, webapp/src/components/orderbook/__tests__/account-panel.test.tsx</files>
  <read_first>
    - webapp/src/components/orderbook/account-panel.tsx (전문 — 계좌 전용 모드 분기, 미체결/잔고 표 열 구성, 기존 Q-ID 가드와 `pendingCancelSent` 회색·취소 숨김, 인라인 `role="status"` 선례)
    - packages/shared/src/relay.ts (`RelayUnfilled` 전 필드)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §공용 패널 문구표 · §접근성 계약의 「미체결 행 선택」 행(시맨틱·키 규칙)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-orderbook-tab-mockup.html (선택된 행 강조 · `.selchip`)
  </read_first>
  <behavior>
    - `onSelectUnfilled` 를 주지 않으면 기존 동작과 픽셀·DOM 이 같다(기존 호출부 무영향).
    - 행을 클릭하면 `onSelectUnfilled(row)` 가 1회 불린다.
    - 선택된 행이 accent 강조를 받고 `aria-pressed="true"` 를 갖는다.
    - `orderNo` 가 빈 행은 선택·취소 모두 disabled 이고 `title` 이 사유를 말한다.
    - `pendingCancelSent` 행은 회색이고 취소 버튼이 없으며 선택도 disabled.
    - Enter/Space 로 선택 토글이 된다.
    - 행 `title` 이 「행을 누르면 수동주문 폼에서 정정·취소할 수 있어요」.
  </behavior>
  <action>
`account-panel.tsx` 에 optional prop 을 더한다 — `onSelectUnfilled?(row: RelayUnfilled): void` 와 `selectedOrderNo?: string | null`. **prop 이 없으면 현행과 동일하게 동작해야 한다**(기존 호출부가 무수정으로 산다).

행 시맨틱은 UI-SPEC §접근성 계약을 따른다 — `<tr>` 에 `aria-selected` 를 쓰지 않고, 행에 `tabindex=0` + `role="button"` + `aria-pressed` 를 주거나 첫 셀에 버튼을 둔다. Enter/Space 로 선택 토글. 선택 해제 ✕ 는 폼 쪽 칩이 담당한다(여기서는 재클릭 토글까지).

기존 가드는 한 글자도 완화하지 않는다 — `orderNo` 가 빈 행(접수 전)은 선택·취소 불가 + `title` 사유, `pendingCancelSent` 행은 회색 + 취소 숨김 + 선택 불가. 이 판정은 `pendingStatus` 문구 비교가 아니라 **불리언 필드**로만 한다.

선택 강조 색은 UI-SPEC §Color 의 Accent 허용 목록 6번(「미체결 표에서 선택된 행」)에 이미 있다 — 새 색을 만들지 않는다.

테스트에 `<behavior>` 7케이스를 더한다. 특히 **prop 없는 렌더가 기존과 동일**함을 단언한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- account-panel</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "pendingStatus *[=!]==? *['\"]" webapp/src/components/orderbook/account-panel.tsx</automated>
    <fails_when>non-zero exit — 문구 비교로 취소 가능 여부를 판정하고 있다(근거는 불리언 필드다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `onSelectUnfilled` 없이 렌더한 결과가 기존과 동일함이 단언된다
    - 선택 행 `aria-pressed="true"` 와 Enter/Space 토글이 단언된다
    - `orderNo` 빈 행 · `pendingCancelSent` 행의 선택 불가가 단언된다
    - 행 `title` 원문이 단언된다
    - 문구 비교 기반 판정이 파일에 없다
  </acceptance_criteria>
  <done>미체결 표의 한 행이 정정·취소의 대상이 될 수 있고, 될 수 없는 행은 그 이유를 말한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `shared-panels.tsx` — 미체결·잔고·전략 로그 3탭 (폰 sticky 접이식)</name>
  <files>webapp/src/components/trading/workbench/shared-panels.tsx, webapp/src/components/trading/__tests__/shared-panels.test.tsx</files>
  <read_first>
    - webapp/src/components/orderbook/account-panel.tsx (Task 1 결과 — 계좌 전용 모드 · 새 선택 prop)
    - webapp/src/components/trading/strategy-log.tsx (로그 탭 원천 · 메시지 렌더 규율)
    - webapp/src/components/ui/tabs.tsx (탭 프리미티브)
    - webapp/src/components/trading/dirty-action-bar.tsx (`fixed inset-x-0 bottom-0 z-40` · 「z-index 로 FAB 을 덮는 것은 해법이 아니다」 주석)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §공용 패널 문구표 전문 · §접근성 Pitfall(더티 바 z-40 vs 폰 공용 패널 z-20)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html (공용 패널 `:754` · 렌더 `:1104-1131` · 폰 sticky CSS `:562-568`)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md §Pitfall 5
  </read_first>
  <behavior>
    - 3탭이 있고 라벨이 「미체결 (N)」·「잔고 (N)」·「전략 로그」.
    - 각 탭 0건 → 해당 빈 문구가 뜨고 스피너가 없다.
    - 미체결 행 클릭 → 상위 `onSelectUnfilled` 가 1회 불린다.
    - 행 취소 실패 → 그 행에 인라인 `role="status"` 문구가 뜬다.
    - 잔고에 현재가 미수신 종목 → 평가손익 「—」.
    - 로그 메시지가 길면 clamp 없이 줄바꿈된다.
    - 종목 열의 긴 이름은 1줄 ellipsis.
    - 폰 밴드에서 「접기 ▾」/「펼치기 ▴」 토글이 있고 sticky 바로 뜬다.
    - 더티 바가 떠 있을 때 폰 sticky 패널이 더티 바 높이만큼 하단 여백을 갖는다(겹침 회피를 z-index 로 하지 않는다).
  </behavior>
  <action>
`webapp/src/components/trading/workbench/shared-panels.tsx` 를 신설한다. 3탭은 `Tabs` 프리미티브로 만들고, 미체결·잔고는 **`AccountPanel` 의 계좌 전용 모드**를 그대로 쓰며(다계좌 합산을 새로 만들지 않는다 — `/me` 담당), 로그는 `StrategyLog` 를 쓴다. 계좌 축은 상태줄에서 내려받은 **단일 계좌**다.

열 구성·문구는 UI-SPEC §공용 패널 표 원문 그대로다 — 미체결 열 7개, 잔고 열 7개, 로그는 시각 + 메시지, 출처 배지 「상따」/「수동」, 빈 문구 3종, 접기 문구 2종. 전략 로그 메시지는 **clamp 없이 줄바꿈 허용**(`min-width:0` + `word-break:keep-all`) — 로그는 읽히는 것이 목적이다.

반응형은 컨테이너 `wb` 기준이다 — 700 이상은 격자 아래 일반 섹션, 폰 밴드는 하단 `sticky bottom:0; z-index:20` 접이식 바. **뷰포트 브레이크포인트를 쓰지 않는다.**

⚠️ **레이어 예산을 명시한다.** 더티 바는 `document.body` 포털의 `fixed … z-40` 이라 페이지 스태킹 컨텍스트 밖이고, 폰 sticky 패널은 `z-20` 이다. 카드가 N개이므로 이 충돌은 폰에서 **상시** 일어난다. 해법은 더티 바 높이만큼 패널에 하단 여백을 주거나 패널을 접힘으로 밀어내리는 것이다 — **z-index 로 덮지 않는다**(선례: 「z-index 로 FAB 을 덮는 것은 해법이 아니다 — 덮으면 채팅 진입점이 조용히 사라진다」). 더티 바 표시 여부를 prop 으로 받아 여백을 계산한다.

테스트 `__tests__/shared-panels.test.tsx` 는 `<behavior>` 9케이스를 RTL 로 덮는다. 겹침 0 의 **실측**은 18-13 의 Playwright `boundingBox()` 가 맡고, 여기서는 여백 클래스/스타일 적용까지만 단언한다는 사실을 SUMMARY 에 적는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- shared-panels</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "z-\[?(4[1-9]|[5-9][0-9])" webapp/src/components/trading/workbench/shared-panels.tsx</automated>
    <fails_when>non-zero exit — 공용 패널이 더티 바(z-40)보다 높은 레이어로 올라가 덮고 있다(금지된 해법)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "\b(sm|md|lg|xl):" webapp/src/components/trading/workbench/shared-panels.tsx</automated>
    <fails_when>non-zero exit — 뷰포트 브레이크포인트가 섞였다(D-28 위반 · 컨테이너 `wb` 기준이어야 한다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 3탭 라벨·빈 문구 3종·접기 문구 2종이 원문 그대로 단언된다
    - 미체결 행 클릭이 상위 콜백으로 전달됨이 단언된다
    - 로그 메시지 줄바꿈 허용(clamp 없음)이 단언된다
    - 더티 바 표시 시 하단 여백이 적용됨이 단언된다
    - 파일에 z-40 이상 레이어와 뷰포트 브레이크포인트가 없다
  </acceptance_criteria>
  <done>전 종목의 미체결·잔고·로그를 한 자리에서 보고, 미체결 한 행이 수동주문 폼으로 이어진다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `stock-info-modal.tsx` — 차트 | 종목정보 | 뉴스·토론 3탭 팝업</name>
  <files>webapp/src/components/trading/card/stock-info-modal.tsx, webapp/src/components/trading/__tests__/stock-info-modal.test.tsx</files>
  <read_first>
    - webapp/src/components/stock/stock-detail-tabs.tsx (탭 정의 4종과 `forceMount` 규율 주석 — 호가주문만 떠나면 언마운트하는 이유)
    - webapp/src/app/stocks/[code]/page.tsx (탭 본문 조립 — 재사용할 내용)
    - webapp/src/components/orderbook/order-confirm-dialog.tsx (`Dialog` 골격 · `DialogTitle`+`DialogDescription` 필수 a11y)
    - webapp/src/lib/chart-colors.ts (lightweight-charts 색 주입 — oklch 직접 주입 금지, hex/rgb 변환)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §종목정보 팝업 문구 · §UI Considerations E14 행 전부
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md §종목상세·팝업 재사용(D-25) · §Assumptions A5(Radix Portal)
  </read_first>
  <behavior>
    - `code` 가 없으면 모달이 열리지 않는다(트리거가 비활성).
    - 열리면 제목이 종목명 + 코드(mono)이고 `DialogTitle`·`DialogDescription` 이 존재한다.
    - 탭 3개가 「차트」「종목정보」「뉴스·토론」이고 호가주문 탭이 없다.
    - ✕ · 배경 클릭 · ESC 셋 다로 닫힌다.
    - 닫으면 내용이 언마운트된다(다시 열면 새로 마운트).
    - 닫은 뒤 포커스가 트리거(ⓘ)로 돌아온다.
    - 모달 DOM 이 카드 `<article>` 의 자손이 아니다(포털).
    - 차트에 주입되는 색 값이 oklch 문자열이 아니다.
  </behavior>
  <action>
`webapp/src/components/trading/card/stock-info-modal.tsx` 를 신설한다. `order-confirm-dialog.tsx` 의 `Dialog` 골격을 그대로 따르고 `DialogTitle`+`DialogDescription` 을 반드시 둔다(a11y). 제목은 종목명 + 코드(mono), 닫기 `aria-label` 「닫기」.

탭 3개의 **내용은 기존 종목상세 탭 본문을 그대로 재사용**한다 — 차트(기간 버튼 「1개월」「3개월」「6개월」「1년」), 종목정보(섹션 「상한가 다음날 이력」·「동반상승」), 뉴스·토론(「뉴스」·「종목토론 요약」 + 「AI 요약」 배지 + 출처 「네이버 종목토론방 (24h 캐시)」). **새 데이터 경로를 만들지 않는다**(D-25). 호가주문 탭은 넣지 않는다 — 보이지 않는 탭에서 실시간 호가 구독을 유지하지 않는다는 기존 규율과 같은 이유다.

**포털:** `container-type: inline-size` 가 layout containment 를 걸어 `position:fixed` 자손의 컨테이닝 블록이 되므로, 모달은 반드시 `document.body` 로 포털돼야 한다. Radix Dialog 가 기본 `Portal` 을 쓰는지 **실제로 확인**하고(이번 리서치에서 코드로 확인하지 않은 가정이다), 기본 포털이 아니거나 확실하지 않으면 명시적으로 `DialogPortal` 로 감싼다. 테스트가 「모달 DOM 이 카드 `<article>` 의 자손이 아니다」를 단언해 이 가정을 기계적으로 닫는다.

**크기:** 폰은 전체화면 시트, 700 이상은 최대 960px 중앙. 모달은 컨테이너 밖이라 기존 `sm:` 유틸 관례를 따른다(D-28 의 「뷰포트 분기 신설 금지」는 컨테이너 안쪽 규율이다 — 목업이 정본). 본문은 세로 스크롤.

**닫기 시 언마운트:** 모달 N개가 쌓이지 않게 닫으면 내용을 버린다. 닫은 뒤 포커스는 트리거(카드 헤더 ⓘ)로 복귀한다(Radix 기본).

**차트 색:** `chart-colors.ts` 를 통해 hex/rgb 로 변환해 주입한다 — lightweight-charts 는 oklch 를 거부한다.

테스트 `__tests__/stock-info-modal.test.tsx` 는 `<behavior>` 8케이스를 RTL 로 덮는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- stock-info-modal</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "oklch\(" webapp/src/components/trading/card/stock-info-modal.tsx</automated>
    <fails_when>non-zero exit — 차트에 주입될 수 있는 oklch 리터럴이 모달 파일에 있다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "DialogDescription" webapp/src/components/trading/card/stock-info-modal.tsx</automated>
    <fails_when>카운트가 0 — a11y 필수 요소가 없다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test</automated>
    <fails_when>non-zero exit, 또는 통과 수가 Phase 17 기준선 1008 보다 작다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 모달 DOM 이 카드 `<article>` 의 자손이 아님이 단언된다(포털 가정을 기계적으로 닫는다)
    - 탭 3개이고 호가주문 탭이 없음이 단언된다
    - ✕·배경·ESC 세 경로 닫힘이 단언된다
    - 닫으면 내용이 언마운트됨이 단언된다
    - `code` 부재 시 열리지 않음이 단언된다
    - 모달 파일에 oklch 리터럴이 없다
    - webapp 테스트 전량 통과 수가 1008 이상
  </acceptance_criteria>
  <done>카드에서 나가지 않고 차트·종목정보·뉴스·토론을 볼 수 있고, 팝업이 카드 컨테이너에 갇히지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay wss → 브라우저 (미체결·잔고 스냅샷) | 주문 상태가 여기서 들어온다 |
| 브라우저 → 종목상세 API (차트·뉴스·토론) | 팝업 내용. 기존 경로를 그대로 쓴다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-42 | Tampering | 취소 불가 행의 취소 시도 | high | mitigate | `orderNo` 부재·`pendingCancelSent` 불리언 필드로만 판정. 문구 비교 금지(verify 가 grep 으로 단언) |
| T-18-43 | Tampering | 잘못된 행이 정정 대상으로 선택됨 | high | mitigate | 선택 페이로드는 `RelayUnfilled` 행 전체이고 폼이 `orgOrderNo`·`side` 를 그 행에서만 읽는다 |
| T-18-44 | Denial of Service | 폰에서 더티 바가 공용 패널을 덮음 | medium | mitigate | 여백/접힘으로 회피. z-index 로 덮는 해법 금지(verify 가 레이어 상한을 grep) |
| T-18-45 | Information Disclosure | 팝업이 새 데이터 경로를 만듦 | low | mitigate | 기존 종목상세 탭 본문 재사용. 새 API 호출 0건 |
| T-18-46 | Denial of Service | 모달 누적 | low | mitigate | 닫으면 언마운트. 호가주문 탭을 넣지 않아 숨은 구독도 없다 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 phase 는 새 패키지를 설치하지 않는다(RESEARCH §Package Legitimacy Audit 표 비어 있음) |
</threat_model>

## Artifacts this phase produces

새로 만드는 심볼·파일 (phase 전체 — 드리프트 검증에서 제외되어야 한다):

- **마이그레이션:** `supabase/migrations/20260921120000_dma_orders_modify_offhours.sql` · 컬럼 `dma_orders.piece_count` · `dma_orders.krx_session`
- **shared:** `RelayOrderModifyMsg` · `RelayKrxSession` · `OrderType` 의 `"M"` 멤버 · `RelayOrderNewMsg.pieceCount?` · `RelayOrderNewMsg.krxSession?` · `RelayRateCrossItem.name?` · `RelayRateCrossItem.code?`
- **relay:** `RelayOrderModifySchema` · `OrderInsertRow.pieceCount?` · `OrderInsertRow.krxSession?` · 돌파 항목 보강 private 메서드 · `relay/src/ws/__tests__/protocol.test.ts`
- **webapp lib:** `RelayOrderRequest.kind` 의 `"modify"` · `queued-window.ts`(`OrderButtonMode`·`ManualOrderAffordance`·`affordanceOf`) · `breakout-list.ts`(`kstDateKey`·`shouldRemoveBreakout`·`breakoutRowsFrom`·`BreakoutRow`·키 상수 4종·`REMOVE_MARGIN_PCT`·`ARM_GRACE_MS`·`HIGHLIGHT_MS`) · `use-breakout-quotes.ts`(`useBreakoutQuotes`·`MAX_BREAKOUT_SUBS`) · `alert-tone.ts`(`playBreakoutTone`·`isTonePlaybackBlocked`·`resumeToneContext`) · `limit-chaser.ts` 로 이동한 `parseStrategyKey`
- **webapp 컴포넌트:** `components/trading/workbench/` 의 `TradingWorkbench`·`WorkbenchStatusBar`·`ViSettingsRows`·`ViTriggerStrip`·`BreakoutStrip`·`StockAddBar`·`CardGrid`·`SharedPanels` · `components/trading/card/` 의 `StrategyCard`·`CardHeader`·`QuoteGrid10`·`CardBody`·`ManualOrderForm`(+`canModify`)·`StockInfoModal` · `AccountPanel` 의 `onSelectUnfilled?`/`selectedOrderNo?` prop
- **라우트:** `webapp/src/app/trading/page.tsx`
- **CSS 토큰:** `--new-bg` · `--new-bd` (globals.css §9 라이트/다크 양쪽)
- **e2e:** `webapp/e2e/specs/trading-workbench.spec.ts`

<verification>
- `pnpm --filter @gh-radar/webapp test` 전량 exit 0 (통과 수 1008 이상)
- `pnpm --filter @gh-radar/webapp run typecheck` exit 0
- 공용 패널 파일에 z-40 이상 레이어·뷰포트 브레이크포인트 0건
</verification>

<success_criteria>
- 공용 패널 3탭이 전 종목 합산으로 그려지고 미체결 행이 정정/취소 대상으로 이어진다
- 종목정보 팝업이 새 데이터 경로 없이 3탭을 보여주고 카드 컨테이너에 갇히지 않는다
- 폰에서 더티 바와 공용 패널의 겹침을 z-index 가 아닌 레이아웃으로 푼다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-09-SUMMARY.md` when done
</output>