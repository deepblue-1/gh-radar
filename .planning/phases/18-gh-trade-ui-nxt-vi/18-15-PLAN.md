---
phase: 18-gh-trade-ui-nxt-vi
plan: 15
type: execute
wave: 8
depends_on: ["18-13"]
gap_closure: true
files_modified:
  - webapp/src/components/trading/workbench/vi-settings-rows.tsx
  - webapp/src/components/trading/__tests__/vi-settings-rows.test.tsx
autonomous: true
requirements: [TRADE-08]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "VI 설정 「수정」·스위치가 등록된 전략의 계좌를 존중해, 상태줄에서 고른 다른 계좌로 가동 중인 VI 자동매수를 조용히 옮기지 않는다 (CR-02)"
    - "등록된 전략이 있으면(`server` 가 객체 ∧ `server.accountNo` 비어 있지 않음) 그 줄이 보내는 **모든** `vi.set`(「수정」 · 시작 · 중지)의 `accountNo` 는 `server.accountNo` 다. 미등록(`null`)·계좌 공란일 때만 상태줄 계좌를 쓴다 — 판정은 `viRowAccountOf` 한 함수이고, 송신 · 잠금 · 확인 요약 · 고지가 같은 값을 읽는다"
    - "UI-SPEC Q-3 의 VI 판: 등록된 전략은 자기 계좌를 유지하고 상태줄을 바꿔도 움직이지 않는다 — 그 사실을 UI 가 말한다. 상태줄 계좌와 다르면 줄 아래 `role=\"status\"` 로 「계좌 {번호}{ · 이름} 에 등록된 VI 예요 — 수정·시작·중지는 이 계좌로 나가요」가 선다. 같으면 아무것도 서지 않는다(D-05 「계좌는 줄에 표시하지 않는다」 유지)"
    - "시작·중지 확인 다이얼로그 요약의 「계좌」 가 실제로 나갈 계좌(정본 계좌)다 — 중지 다이얼로그에도 「계좌」 줄이 선다(Phase 16 D-07 요약 규율)"
    - "D-05 · D-27 기존 계약 무변경 — 줄의 거래소를 명시 송신, `run` 은 「수정」 때 현재값 그대로, 에코가 이기고 더티 중 남의 에코는 「다른 단말에서 변경됨」, 만원↔원 변환은 `manwonToKrw`/`krwToManwon` 뿐"
  artifacts:
    - webapp/src/components/trading/workbench/vi-settings-rows.tsx
  key_links:
    - "`viTriggers[exchange].accountNo`(서버 61 에코) → `viRowAccountOf` → `vi.set.accountNo` — 18-VERIFICATION key link 「`server.accountNo` → `vi.set` 페이로드 `accountNo`」 NOT_WIRED 를 WIRED 로"
    - "`useRelayContext().accounts` → 정본 계좌의 이름 — 확인 요약과 고지가 같은 이름을 쓴다(상태줄 계좌 이름 prop 을 다른 계좌에 붙이지 않는다)"
  prohibitions:
    - "MUST NOT 등록된 전략이 있는데 상태줄 계좌로 `vi.set` 을 보낸다 — 가동 중인 무인 매수가 다른 계좌로 옮겨 간다(실돈 오계좌 주문)"
    - "MUST NOT 계좌가 다르다고 중지 스위치를 잠근다 — 무인 매수를 끄는 조작은 어떤 상태에서도 막지 않는다. 중지는 정본 계좌로 나가므로 아무것도 옮기지 않는다"
    - "MUST NOT 줄에 계좌 셀렉터를 되살린다 — 계좌는 상태줄이 고른다(D-05). 이 플랜은 불일치 고지 한 줄만 더한다"
---

<objective>
CR-02 를 닫는다. `ViSettingsRow` 가 `server.accountNo` 를 전혀 읽지 않고 상태줄 계좌로 `vi.set` 을 보내, 2계좌 사용자가 계좌 B 로 가동 중인 VI 를 상태줄 계좌 A 에서 「수정」 한 번으로 조용히 A 로 옮기던 경로를 없앤다.

설계 선택(Claude 재량 — 새 UX 결정이 아니다): **등록된 전략의 계좌가 정본**이다. 이미 채택된 UI-SPEC Q-3(「이미 등록된 카드는 자기 키의 계좌를 유지하고, 상태줄을 바꿔도 움직이지 않는다 — 그 사실을 UI 가 말해야 한다」)을 VI 줄에 그대로 적용한다. 리뷰가 제안한 「줄 잠금」 은 택하지 않는다 — 잠그면 계좌가 다를 때 **중지**도 막혀 무인 매수를 끄려면 상태줄부터 바꿔야 한다. 정본 계좌로 보내면 옮겨지는 것이 원천적으로 없고(잠금·확인 다이얼로그가 막으려던 결과 자체가 사라진다), 고지 한 줄이 「어느 계좌의 VI 인지」를 말한다.

Purpose: TRADE-08 의 신뢰 가능한 VI 설정 화면 — 18-VERIFICATION truth #10 FAILED.
Output: `viRowAccountOf` 순수 판정 · 정본 계좌 송신 · 불일치 고지 · 확인 요약의 정본 계좌 · 회귀 테스트.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-VERIFICATION.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-REVIEW.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-05-SUMMARY.md

근거 인용(재확인 완료): `vi-settings-rows.tsx` 130-135 `formFromServer` 는 `checkRate`·`orderAmountKrw` 만 읽는다 · 358 `locked` 에 계좌 판정 없음 · 386-394 `msg.accountNo = accountNo`(상태줄 prop) · 479-489 「수정」 은 다이얼로그 없이 `submit(run)` · 724-728 중지 요약에 계좌 줄 없음. `RelayViTrigger.accountNo` 는 `packages/shared/src/relay.ts:309-311` 에 있다.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] 정본 계좌 한 경로 — `viRowAccountOf` → `vi.set.accountNo` · 불일치 고지</name>
  <files>webapp/src/components/trading/workbench/vi-settings-rows.tsx, webapp/src/components/trading/__tests__/vi-settings-rows.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/workbench/vi-settings-rows.tsx (전문 786줄 — 머리 주석 ①~⑧, `ViSettingsRows` 189-245, `ViSettingsRow` 262-542, `ViConfirmDialog` 651-776)
    - webapp/src/components/trading/__tests__/vi-settings-rows.test.tsx (전문 — 렌더 하네스·`send` 모킹·describe 「줄 구성 (D-05)」 「① 줄의 거래소를 실어 보낸다」 단언 형태)
    - packages/shared/src/relay.ts (309-328 `RelayViTrigger`)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md (§레이아웃 계약 3 · Q-3 행 624 · §Copywriting 의 VI 줄 문구)
  </read_first>
  <behavior>
    - `viRowAccountOf(undefined, "A")` · `viRowAccountOf(null, "A")` → `{ accountNo: "A", differs: false }`
    - `viRowAccountOf({ accountNo: "B", … }, "A")` → `{ accountNo: "B", differs: true }` / `{ accountNo: "A" }` 와 "A" → `differs: false`
    - `viRowAccountOf({ accountNo: "", … }, "A")` → `{ accountNo: "A", differs: false }` (공란은 등록 계좌가 아니다)
    - 서버 KRX 전략 계좌 B · 상태줄 A · 상승률 수정 → 「수정」 → `send` 1회, 페이로드 `accountNo:"B"` · `exchange:"KRX"` · `run` 은 현재값
    - 위 상태에서 KRX 줄 아래 `[data-slot="vi-row-account"]` 가 `role="status"` 이고 「B」를 포함한다. NXT 줄(미등록)에는 없다
    - 서버 계좌 = 상태줄 계좌면 고지가 DOM 에 없다(기존 「계좌·마감알림은 줄에 없다」 케이스 green 유지)
    - 서버 `null`(미등록) · 상태줄 A → 「수정」 페이로드 `accountNo:"A"`
  </behavior>
  <action>
**판정의 유일 지점을 먼저 세운다 (D-05 · D-27 · UI-SPEC Q-3).** `vi-settings-rows.tsx` 에 순수 함수 `viRowAccountOf(server: RelayViTrigger | null | undefined, statusAccountNo: string): { accountNo: string; differs: boolean }` 를 export 한다 — 등록 전략(객체 ∧ `accountNo` 공란 아님)이면 그 계좌, 아니면 상태줄 계좌. `differs` 는 등록 계좌가 상태줄 계좌와 다를 때만 참.

`ViSettingsRow` 에서 이 값을 한 번 계산해 **세 곳이 같은 값을 읽게** 한다: ① `locked`(계좌 공란 판정이 정본 계좌 기준) ② `submit` 이 만드는 `RelayViSetMsg.accountNo` ③ 불일치 고지. `submit` 의 `useCallback` 의존성도 정본 계좌로 바꾼다. 상태줄 `accountNo` prop 은 「미등록 줄의 기본 계좌」로만 남긴다 — prop 주석을 그렇게 고친다.

**불일치 고지:** `differs` 이면 줄 아래(기존 `vi-row-error`·`vi-row-echo` 와 같은 자리·같은 11px 문법)에 `<p role="status" data-slot="vi-row-account">` 로 export 상수 `viRegisteredAccountText(accountNo, name?)` 의 문구 「계좌 {번호}{ · 이름} 에 등록된 VI 예요 — 수정·시작·중지는 이 계좌로 나가요」를 그린다. 이름은 `useRelayContext().accounts` 에서 정본 계좌로 찾는다(없으면 번호만). 같으면 아무것도 그리지 않는다 — D-05 「계좌는 줄에 표시하지 않는다」 는 평상시 그대로다.

**잠그지 않는다** — 불일치여도 입력·「수정」·스위치는 정본 계좌 기준으로 동작한다(중지를 막지 않는다, 위 prohibitions). 파일 머리 주석에 ⑨ 로 「계좌 정본(CR-02 · UI-SPEC Q-3 VI 판)」 문단을 더해 왜 잠금이 아니라 정본인지 한 문단으로 남긴다.

테스트: 새 describe 「CR-02 — 등록 전략의 계좌가 정본」에 `<behavior>` 7케이스. 순수 함수 4케이스는 렌더 없이 호출로 본다. 기존 describe 는 수정하지 않는다(회귀 확인용).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- vi-settings-rows</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "viRowAccountOf" webapp/src/components/trading/workbench/vi-settings-rows.tsx</automated>
    <fails_when>카운트가 2 미만 — 판정 함수가 정의만 있고 송신·잠금이 그것을 부르지 않는다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 「서버 계좌 B · 상태줄 A → 「수정」 → 페이로드 `accountNo:"B"`」 케이스가 green 이고, 이 케이스는 수정 전 코드에서 `accountNo:"A"` 로 실패한다(RED 확인을 SUMMARY 에 한 줄)
    - `[data-slot="vi-row-account"]` 가 `differs` 일 때만 DOM 에 있다(같을 때 `queryBy…` null 단언)
    - 기존 describe 「줄 구성 (D-05)」 「① 줄의 거래소를 실어 보낸다」 「E2 loading · error」 「D-27」 전부 무수정으로 green
    - `vi-settings-rows.tsx` 에 만원 곱셈 인라인이 없다 — `grep -nE "\* *10000|\* *10_000" webapp/src/components/trading/workbench/vi-settings-rows.tsx` 가 0줄
  </acceptance_criteria>
  <done>등록된 VI 전략의 「수정」 은 언제나 그 전략의 계좌로 나가고, 계좌가 상태줄과 다르면 줄이 그 사실을 말한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 시작·중지 확인 요약이 정본 계좌를 말한다 + VI e2e 회귀</name>
  <files>webapp/src/components/trading/workbench/vi-settings-rows.tsx, webapp/src/components/trading/__tests__/vi-settings-rows.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/workbench/vi-settings-rows.tsx (Task 1 결과 — `ViConfirmDialog` 651-776 · 호출부 513-539)
    - webapp/src/components/trading/__tests__/vi-settings-rows.test.tsx (describe 「시작/중지 확인 다이얼로그 (Phase 16 D-07 승계)」)
    - webapp/e2e/specs/trading-workbench.spec.ts (케이스 20~23 — VI 줄 시작/중지/수정 단언. 이 태스크는 이 파일을 **수정하지 않고** 회귀로만 돌린다)
  </read_first>
  <behavior>
    - 서버 계좌 B(중지 상태) · 상태줄 A → 스위치 → 시작 다이얼로그 요약 「계좌」 가 B(+ B 의 이름) · 확정 → 페이로드 `accountNo:"B"`, `run:true`
    - 서버 계좌 B(가동 중) · 상태줄 A → 스위치 → 중지 다이얼로그 요약 첫 줄이 「계좌」 B · 확정 → 페이로드 `accountNo:"B"`, `run:false` (중지가 잠기지 않는다)
    - 계좌가 같을 때 두 다이얼로그 요약의 「계좌」 는 상태줄 계좌 + 상태줄 이름 그대로(기존 동작)
  </behavior>
  <action>
`ViConfirmDialog` 호출부가 넘기는 `accountNo`/`accountName` 을 **정본 계좌와 그 이름**으로 바꾼다(Task 1 의 한 값). 중지 요약(724-728)의 맨 앞에 「계좌」 줄을 더한다 — 시작 요약과 같은 `SummaryRow` 형식이다. 이름은 Task 1 과 같은 조회(`useRelayContext().accounts`)를 쓴다 — 상태줄 계좌 이름(prop)을 다른 계좌 번호 옆에 붙이지 않는다.

다이얼로그의 기존 규율(기본 포커스 = 취소/닫기 · `showCloseButton={false}` · 「보내지 못했으면 닫지 않는다」)은 한 글자도 바꾸지 않는다.

테스트: 「CR-02 — 등록 전략의 계좌가 정본」 describe 에 `<behavior>` 3케이스를 더한다. 끝으로 e2e VI 케이스(20~23)를 **수정 없이** 돌려 기존 왕복이 그대로임을 확인한다 — e2e 픽스처는 계좌가 1개라 불일치 경로는 단위 테스트가 정본이다(2계좌 실기 재현은 18-VERIFICATION human_verification #7 로 UAT 에 남는다).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- vi-settings-rows</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test:e2e -- trading-workbench -g "VI"</automated>
    <fails_when>non-zero exit, 또는 출력에 "No tests found", 또는 "failed" 가 1 이상</fails_when>
  </verify>
  <acceptance_criteria>
    - 중지 다이얼로그 요약에 「계좌」 줄이 있고 값이 정본 계좌임이 단언된다
    - 계좌 불일치 상태에서 중지 스위치가 `disabled` 가 아님이 단언된다
    - e2e `trading-workbench -g "VI"` 가 수정 없이 green 이다(케이스 20~28 중 VI 제목 전부)
  </acceptance_criteria>
  <done>사용자가 확인하는 계좌와 실제로 나가는 계좌가 언제나 같다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → relay `vi.set` | 계좌번호가 실린 무인 매수 설정. relay 는 세션 계좌 화이트리스트만 보고 「어느 허용 계좌인가」는 판정하지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-70 | Tampering | `vi.set.accountNo` | critical | mitigate | 등록 전략이 있으면 `server.accountNo` 만 싣는다(`viRowAccountOf` 한 곳) — 상태줄 변경으로 가동 전략이 옮겨지는 경로 제거 |
| T-18-71 | Repudiation | 확인 다이얼로그 요약 | high | mitigate | 요약 「계좌」 = 실제 송신 계좌(같은 값). 중지 요약에도 계좌 줄 추가 |
| T-18-72 | Denial of Service | 중지 조작 | high | mitigate | 불일치여도 중지를 잠그지 않는다 — 무인 매수 차단이 상태줄 조작에 묶이지 않는다 |
| T-18-73 | Information Disclosure | 불일치 고지의 계좌번호 | low | accept | 자기 세션의 허용 계좌이고, 상태줄·확인 다이얼로그가 이미 전체 번호를 보인다(UI-SPEC D2) |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 |
</threat_model>

## Artifacts this phase produces

- **webapp 컴포넌트:** `viRowAccountOf`(export 순수 함수) · `viRegisteredAccountText`(export 문구 함수) · `[data-slot="vi-row-account"]` 고지 줄 · 중지 확인 요약 「계좌」 줄
- **테스트:** `vi-settings-rows.test.tsx` describe 「CR-02 — 등록 전략의 계좌가 정본」(10케이스)

<verification>
- 계좌 B 로 등록된 VI 를 상태줄 A 에서 「수정」·시작·중지해도 페이로드 계좌는 B 다
- 불일치 고지는 불일치일 때만, 확인 요약은 정본 계좌를 말한다
- 기존 VI 단위 describe 와 e2e VI 케이스가 무수정 green
</verification>

<success_criteria>
- 18-VERIFICATION truth #10 이 VERIFIED 로 돌아설 근거(단위 테스트 + key link WIRED)가 있다
- 줄 레이아웃(D-05)은 평상시 한 글자도 바뀌지 않았다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-15-SUMMARY.md` when done
</output>
