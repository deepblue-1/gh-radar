---
phase: 18-gh-trade-ui-nxt-vi
plan: 24
type: execute
wave: 14
depends_on: ["18-23"]
gap_closure: true
files_modified:
  - supabase/tests/dma_orders_price_check.test.sql
  - scripts/verify-dma-orders-price-check.sh
  - supabase/migrations/20260922180000_dma_orders_price_check_null_safe.sql
  - relay/src/store/orders.ts
autonomous: true
requirements: [TRADE-07]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GC-CR-01 · D-21 · D-23: `dma_orders_price_check` 가 3값 논리 때문에 통과시키던 행 — `(0,'N',NULL)` · `(0,'M',NULL)` · `(0,'M','G2')` — 을 새 마이그레이션 적용 뒤 DB 가 **거부**한다(check_violation 23514). 수정 전 스키마에서는 같은 세 행이 insert 된다는 것을 같은 회귀로 먼저 재현(RED)한다"
    - "D-21 · D-23: 허용해야 할 가격 0 은 두 갈래뿐이고 새 CHECK 에서도 통과한다 — 취소 `(0,'C',NULL)` 과 시간외종가 신규 `(0,'N','G2')` · `(0,'N','G3')`. 양수 신규·정정 `(70000,'N',NULL)` · `(70000,'M',NULL)` 도 통과한다. 음수는 종류·세션과 무관하게 거부된다"
    - "새 CHECK 식은 NULL 을 FALSE 로 접는다 — `krx_session` 이 NULL 이어도 식 전체가 NULL 로 평가되는 갈래가 없다(식 안의 모든 비교가 NOT NULL 컬럼이거나 COALESCE/IS NOT NULL 로 감싸여 있다). G2/G3 가격 0 은 `order_type = 'N'` 에만 열린다"
    - "마이그레이션은 한 트랜잭션이고, 적용 시점에 새 CHECK 를 어기는 기존 행이 1건이라도 있으면 **개수를 말하며 트랜잭션째 실패**한다(DB 가 아무것도 바꾸지 않는다) — 사전 확인이 적용과 같은 트랜잭션 안에 있어 확인과 적용 사이에 끼어드는 insert 가 없다"
    - "회귀는 공유·원격 DB 에 한 번도 닿지 않는다 — 일회용 로컬 컨테이너에 저장소 마이그레이션을 파일명 순으로 재생한 뒤에만 돈다"
    - "18-18 SUMMARY 의 T-18-82 문장(「세션 없는 가격 0 신규·정정도 계속 거부된다」)이 사실이 아니었다는 정정이 이 플랜의 SUMMARY(R3 기록)에 원인·증거와 함께 남는다 — 18-18 SUMMARY 원본은 한 글자도 바뀌지 않는다"
  artifacts:
    - supabase/tests/dma_orders_price_check.test.sql
    - scripts/verify-dma-orders-price-check.sh
    - supabase/migrations/20260922180000_dma_orders_price_check_null_safe.sql
  key_links:
    - "pgTAP 회귀 ↔ 러너 스크립트 ↔ 저장소 마이그레이션 이력 — 러너가 `--until` 컷오프까지 재생한 스키마에 회귀를 돌린다. 컷오프 20260922120000 = 수정 전(RED), 컷오프 없음 = 수정 후(GREEN)"
    - "새 마이그레이션 파일 ↔ 18-29 원격 반영 — 이 플랜은 파일과 로컬 증거만 만든다. 원격 적용은 18-29 의 사람 확인 게이트 뒤다"
    - "relay 코드 층(zod · 조립기)이 세션 없는 가격 0 신규·정정을 이미 막고 있다(`relay/src/ws/protocol.ts` · `relay/src/dma/envelope.ts` `priceFloor`) — DB CHECK 는 그 규칙의 최후 방어선이고, 이 플랜은 그 방어선이 선언대로 서게 한다"
  prohibitions:
    - "MUST NOT 이 플랜에서 원격(linked) DB 에 연결하거나 마이그레이션을 적용한다 — `supabase db push` · `--linked` 쓰기 명령 금지. 원격 반영은 18-29"
    - "MUST NOT 이미 원격에 적용된 마이그레이션 파일(20260921120000 · 20260922120000)을 고친다 — 틀린 머리 주석의 정정은 새 파일의 머리 주석과 SUMMARY 에 쓴다"
    - "MUST NOT 18-18-SUMMARY.md · 18-REVIEW-R2.md · 18-VERIFICATION-R2.md 를 덮어쓴다(갭 클로징 기록은 새 파일·새 절에만 쓴다)"
    - "MUST NOT CHECK 를 `NOT VALID` 로 걸어 기존 행 검증을 건너뛴다 — 위반 행이 있으면 실패하는 것이 의도다"
---

<objective>
GC-CR-01 을 닫는 **수직 슬라이스(tracer)**: 「DB CHECK 가 NULL 을 통과시킨다」 를 로컬 Postgres 회귀로 먼저 재현하고(RED), NULL 을 FALSE 로 접는 후속 마이그레이션으로 같은 회귀를 GREEN 으로 만든다.

Purpose: 실돈 주문 감사 테이블 `public.dma_orders` 의 최후 방어선이 선언과 다르게 동작했다 — `price = 0 AND (order_type = 'C' OR krx_session IN ('G2','G3'))` 는 `krx_session IS NULL` 에서 NULL 로 평가되고, Postgres 는 CHECK 의 NULL 을 통과로 본다. 그래서 세션 없는 가격 0 신규·정정이 DB 에서 거부된 적이 없고, 18-18 의 T-18-82 검증 기록도 사실이 아니다. 지금은 relay zod · 조립기가 막고 있을 뿐이라, 코드 층이 회귀하는 날 DB 가 받쳐 주지 못한다(18-REVIEW-R2 GC-CR-01).
Output: pgTAP 회귀 · 일회용 컨테이너 러너 · 후속 마이그레이션 파일(미적용) · relay `orders.ts` 주석 정정 · SUMMARY 의 T-18-82 정정 기록.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-REVIEW-R2.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-18-SUMMARY.md

근거 인용(재확인 완료):
- `supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql:47-48` — 현행 CHECK `price > 0 OR (price = 0 AND (order_type = 'C' OR krx_session IN ('G2','G3')))`. 머리 주석 「정정('M') 가격 0 은 여전히 거부된다」 는 사실이 아니다.
- `supabase/migrations/20260921120000_dma_orders_modify_offhours.sql:66-67` — `krx_session text CHECK (krx_session IS NULL OR krx_session IN ('G2','G3'))` · 「NULL = 서버 자동 판정」. `order_type` 은 `N/M/C` 만(같은 파일 57-59).
- `supabase/migrations/20260905120200_dma_orders.sql:48-69` — `price integer NOT NULL` · `order_type text NOT NULL` · `user_id uuid NOT NULL REFERENCES auth.users(id)` · `market`·`exchange`·`side` NOT NULL CHECK. `20260908120000` 이 `origin text NOT NULL DEFAULT 'manual'` 을 더했다.
- relay 행 생성 경로는 가격 0 을 두 갈래로만 쓴다: 수동 취소(원주문 가격 사본) · 시간외종가 신규(`krxSession` G2/G3). 자동주문 통보 행은 가격 0 이하면 만들지 않는다(`relay/src/ws/order-handler.ts` `autoInsertRow` 661-664). 그래서 새 CHECK 가 막는 행을 코드가 만드는 경로는 없다.
- 로컬 재생 선례: `.planning/quick/260908-qnf-phase-15-rls-auto-enable-e2e-11-dockerig/260908-qnf-SUMMARY.md` 「Task 1-A」 — 이미지 `public.ecr.aws/supabase/postgres:17.6.1.104`(로컬에 이미 있음) · `auth.jwt()` 스텁 1개 사전 생성 → 파일명 순 전량 적용 → 오류 0. 이미지 안에 `psql`·`pg_isready`·pgTAP 1.3.x 확장이 있다(2026-09-22 planner 실측).
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] 일회용 컨테이너 pgTAP 회귀 — 수정 전 스키마에서 NULL 통과를 재현한다 (RED)</name>
  <files>supabase/tests/dma_orders_price_check.test.sql, scripts/verify-dma-orders-price-check.sh</files>
  <read_first>
    - supabase/migrations/20260905120200_dma_orders.sql (48-85 — 테이블 정의 · NOT NULL · FK · 권한)
    - supabase/migrations/20260908120000_dma_orders_origin.sql (origin 컬럼 기본값)
    - supabase/migrations/20260909120000_dma_orders_user_order_no_unique.sql (order_no 유일 인덱스 — 회귀 행은 order_no 를 비워 둔다)
    - supabase/migrations/20260921120000_dma_orders_modify_offhours.sql (전문 — order_type · krx_session · 옛 price CHECK)
    - supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql (전문 — 현행 CHECK와 틀린 머리 주석)
    - .planning/quick/260908-qnf-phase-15-rls-auto-enable-e2e-11-dockerig/260908-qnf-SUMMARY.md (Task 1-A 재생 절차 — 컨테이너 이름·포트·`auth.jwt()` 스텁·정리)
    - scripts/smoke-relay.sh (저장소 셸 스크립트 머리 주석·`set -euo pipefail`·출력 형식 관례)
  </read_first>
  <behavior>
    - 컷오프 `--until 20260922120000`(수정 전 스키마): 회귀 출력에 `not ok` 가 3줄 — `(0,'N',NULL)` · `(0,'M',NULL)` · `(0,'M','G2')` 거부 기대가 실패한다(= GC-CR-01 재현). 스크립트 exit 는 non-zero
    - 같은 컷오프에서 취소 `(0,'C',NULL)` · 시간외종가 신규 `(0,'N','G2')`·`(0,'N','G3')` 통과 · 음수 3종 거부 · 양수 신규·정정 통과는 `ok`
    - 컷오프 `--until 20260921120000`(18-14 이전): 취소 `(0,'C',NULL)` 도 이미 `ok`(통과) — R1 CR-01 의 「DB 층이 가격 0 취소를 막는다」 전제가 처음부터 틀렸다는 증거
    - 러너는 어떤 경우에도 컨테이너를 남기지 않는다(성공·실패·중단 모두 `trap` 정리)
  </behavior>
  <action>
**회귀 파일 `supabase/tests/dma_orders_price_check.test.sql` (pgTAP, `supabase test db` 관례 경로).** `BEGIN;` → pgTAP 확장 준비 → `plan(12)` → 12개 단언 → `finish(true)` → `ROLLBACK;` 형태의 한 트랜잭션이다(D-21 · D-23 · GC-CR-01). FK 는 트랜잭션 안에서 `auth.users` 에 고정 uuid 1행을 넣어 충족한다(이미지의 `auth.users` 필수 컬럼에 맞춘다 — 넣을 수 없으면 superuser 세션에서 `session_replication_role = replica` 로 FK 트리거만 끈다. CHECK 는 트리거가 아니므로 그대로 강제된다 — 어느 방법을 썼는지 SUMMARY 에 적는다). 회귀 행은 `account_no`·12자 `isin`·`exchange 'KRX'`·`market 'K'`·`side 'B'`·`qty 1` 을 공통으로 쓰고, 취소·정정 행만 `org_order_no` 를 싣고, `order_no` 는 비운다(유일 인덱스와 무관하게).
- 거부 6건(`throws_ok(..., '23514')`): `(0,'N',NULL)` · `(0,'M',NULL)` · `(0,'M','G2')` · `(-1,'N',NULL)` · `(-1,'C',NULL)` · `(-1,'N','G2')`
- 통과 5건(`lives_ok`): `(0,'C',NULL)` · `(0,'N','G2')` · `(0,'N','G3')` · `(70000,'N',NULL)` · `(70000,'M',NULL)`
- 구조 1건(`is`): `public.dma_orders` 의 CHECK 중 정의에 `price` 단어가 들어간 것이 정확히 1개(두 CHECK 가 겹쳐 걸린 상태를 잡는다 — `pg_constraint` · `pg_get_constraintdef` · 단어 경계 정규식은 20260921120000 의 조회식과 같은 형태)
단언 설명 문자열에 `(price, order_type, krx_session)` 튜플을 그대로 적어 `not ok` 줄만 보고도 어느 행인지 알게 한다.

**러너 `scripts/verify-dma-orders-price-check.sh`.** 저장소 셸 스크립트 관례(머리 주석에 목적·사용법·「공유/원격 DB 접촉 0」 명시 · `set -euo pipefail`)를 따른다. 동작: ① 일회용 컨테이너(`public.ecr.aws/supabase/postgres:17.6.1.104`, 이름 `dma-price-check-<PID>` 로 충돌 방지, 포트 공개 없이 `docker exec` 로만 접속)를 띄우고 `pg_isready` 로 기다린다 ② qnf 선례대로 `auth.jwt()` 스텁을 만든다 ③ `supabase/migrations/*.sql` 을 파일명 순으로 적용하되 `--until <버전>` 이 주어지면 그 버전 **이하**만 적용한다 ④ 회귀 파일을 `psql -v ON_ERROR_STOP=1` 로 돌려 TAP 출력을 그대로 내보낸다 ⑤ 출력에 `not ok` 가 있거나 psql 이 실패하면 non-zero 로 끝난다 ⑥ `trap` 으로 컨테이너를 항상 지운다. 재생이 이 phase 와 무관한 마이그레이션에서 실패하면(qnf 이후 추가된 파일 등) qnf 처럼 **러너 안의 사전 스텁**을 더하는 것까지만 허용한다 — 마이그레이션 파일은 고치지 않는다. 그래도 막히면 멈추고 실패 파일·오류 원문을 보고한다. 이미지가 로컬에 없으면 받지 않고 멈춘다(설명 문구와 함께) — 네트워크에서 새 이미지를 끌어오지 않는다. 원격 링크 플래그·원격 URL·Supabase 환경변수를 읽는 코드가 이 스크립트에 없다 — 머리 주석에도 그 플래그·변수 이름 문자열을 적지 않는다(acceptance grep 이 주석까지 센다. 「원격 DB 접촉 0」 은 말로 적는다).

**RED 기록.** `--until 20260922120000` 와 `--until 20260921120000` 두 컷오프로 돌려 `not ok` 줄 원문을 SUMMARY 에 붙인다(아직 새 마이그레이션이 없으므로 컷오프 없이 돌려도 같은 RED 다). 이 태스크에서는 마이그레이션을 만들지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash scripts/verify-dma-orders-price-check.sh --until 20260922120000 2>&1 | grep -c "^not ok"</automated>
    <fails_when>출력 카운트가 0 — 회귀가 GC-CR-01 을 재현하지 못한다(판별력 없음) 또는 러너가 컨테이너를 띄우지 못했다. 카운트가 3 을 넘으면 통과해야 할 행까지 막히는 등 회귀 식이 틀렸다(acceptance 가 줄 원문으로 세 튜플을 확인한다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && docker ps -a --format '{{.Names}}' | grep -c "dma-price-check" || true</automated>
    <fails_when>출력이 0 이 아니다 — 러너가 일회용 컨테이너를 남겼다(trap 정리 실패)</fails_when>
  </verify>
  <acceptance_criteria>
    - `supabase/tests/dma_orders_price_check.test.sql` 에 거부 6 · 통과 5 · 구조 1 단언이 있다 — `grep -n "throws_ok\|lives_ok" supabase/tests/dma_orders_price_check.test.sql` 로 읽은 줄의 튜플이 `<action>` 의 11개 목록과 하나씩 대응하고, 러너 GREEN 출력의 `ok` 줄이 12개다
    - `--until 20260922120000` 실행의 `not ok` 줄이 정확히 `(0,'N',NULL)` · `(0,'M',NULL)` · `(0,'M','G2')` 세 건이고, 그 원문이 SUMMARY 에 있다
    - `--until 20260921120000` 실행에서 `(0,'C',NULL)` 단언이 `ok` 라는 원문이 SUMMARY 에 있다(R1 CR-01 DB 층 전제 오류의 증거)
    - 러너에 `--linked` · `db push` · `SUPABASE_` 문자열이 없다 — `grep -cE "\-\-linked|db push|SUPABASE_" scripts/verify-dma-orders-price-check.sh` = 0
    - 실행 뒤 컨테이너 잔존 0
  </acceptance_criteria>
  <done>수정 전 스키마가 세션 없는 가격 0 신규·정정을 받는다는 사실이 공유 DB 접촉 없이 자동 회귀로 재현된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NULL 을 FALSE 로 접는 후속 마이그레이션 → 같은 회귀 GREEN · relay 주석 정정 · T-18-82 정정 기록</name>
  <reversibility rating="reversible">마이그레이션 **파일**만 만든다 — 원격 적용(18-29)은 사람 확인 뒤다. 로컬 회귀는 일회용 컨테이너라 되돌릴 상태가 없다.</reversibility>
  <files>supabase/migrations/20260922180000_dma_orders_price_check_null_safe.sql, relay/src/store/orders.ts</files>
  <read_first>
    - supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql (머리 주석 형식 — 결정 근거 · 기각한 대안 · 함정 · 하지 않는 것 · 트랜잭션)
    - supabase/tests/dma_orders_price_check.test.sql · scripts/verify-dma-orders-price-check.sh (Task 1 결과)
    - relay/src/store/orders.ts (210-240 — `OrderInsertRow.krxSession` 주석 「DB CHECK 가 `price = 0` 을 이 값이 G2/G3 일 때만 받는다」)
    - relay/src/dma/envelope.ts (960-975 — `priceFloor` 와 거부 문구 세 갈래: 코드 층이 DB 와 같은 규칙인지 대조만 한다, 고치지 않는다)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-18-SUMMARY.md (Task 3 ① 의 T-18-82 문장 원문 — SUMMARY 정정 절에 인용한다)
  </read_first>
  <behavior>
    - 컷오프 없이(새 파일 포함) 러너 실행: `not ok` 0줄 · `ok` 12줄 · exit 0
    - 새 파일만 빼고 재생하면 여전히 RED(Task 1 과 같은 3줄) — 새 파일이 유일한 원인이다
    - 새 CHECK 를 어기는 행이 있는 상태에서 새 파일을 적용하면 트랜잭션이 개수를 말하며 실패하고 옛 CHECK 가 그대로 남는다
  </behavior>
  <action>
**`supabase/migrations/20260922180000_dma_orders_price_check_null_safe.sql` 신설 (GC-CR-01 · D-21 · D-23).** 파일명은 20260922120000 뒤에 정렬된다. 한 트랜잭션(`BEGIN;`/`COMMIT;`) 안에서 순서대로:
① **사전 확인(같은 트랜잭션)** — `DO` 블록이 새 CHECK 를 어기는 기존 행 수를 센다. 셈의 조건식 자체가 NULL 로 빠지지 않게 새 CHECK 와 **같은 null-safe 식의 부정**으로 쓴다(부정한 식 안에 NULL 이 남으면 WHERE 가 그 행을 빼 버려 위반 행을 0 으로 잘못 센다 — 이 파일이 고치는 결함과 같은 함정이다). 1건 이상이면 개수를 담은 `RAISE EXCEPTION` 으로 트랜잭션째 실패한다.
② 명시 이름 `dma_orders_price_check` 를 떨어뜨린다(`IF EXISTS` 없이 — 20260922120000 과 같은 이유).
③ 같은 이름으로 다시 건다: 「`price > 0` 또는 (`price = 0` 이고 (`order_type = 'C'` 또는 (`order_type = 'N'` 이고 `krx_session` 이 G2/G3 — NULL 은 FALSE 로 접는다)))」. NULL 접기는 `COALESCE(krx_session IN ('G2','G3'), false)` 또는 `krx_session IS NOT NULL AND krx_session IN ('G2','G3')` 중 하나로 쓴다(둘 다 NULL 을 남기지 않는다). `order_type = 'N'` 을 명시해 정정 + G2/G3 + 0 을 막는다(현행 식은 통과시킨다). 기존 행 검증을 건너뛰는 제약 옵션을 붙이지 않는다 — ADD CONSTRAINT 의 기존 행 검증이 ① 과 함께 이중 안전장치다(머리 주석에서도 그 옵션 이름을 적지 않는다 — acceptance grep 이 주석 밖 SQL 만 보지만 오해를 만들지 않는다).
머리 주석은 20260922120000 형식을 따른다: 결정 근거(GC-CR-01 — 3값 논리 평가 순서 4단계를 그대로 적는다 · D-21 · D-23), **정정 기록**(20260922120000 머리 주석의 「정정('M') 가격 0 은 여전히 거부된다」 와 18-18 T-18-82 는 사실이 아니었다 · 옛 CHECK 도 NULL 세션의 가격 0 취소를 원래부터 통과시켰다 · 근거는 이 파일과 짝인 pgTAP 회귀), 함정(NULL 은 CHECK 에서 통과 · 부정식의 NULL), 하지 않는 것(접근 정책·권한 구문을 다시 쓰지 않는다 · 기존 행을 고치지 않는다 · 적용은 18-29 의 사람 확인 뒤).

**`relay/src/store/orders.ts` 주석 정정 (GC-IN-01 중 DB 규칙 몫).** `OrderInsertRow.krxSession` 주석을 현재 규칙으로 고친다 — 「DB CHECK(20260922180000)는 가격 0 을 취소 행 또는 G2/G3 세션이 실린 **신규** 행에만 받는다. 세션 없는 가격 0 신규·가격 0 정정은 DB 가 거부한다 — 그래서 신규의 가격 0 과 이 값은 같은 행에 함께 실려야 한다」. 코드는 바꾸지 않는다.

**GREEN 확인과 대조.** 러너를 컷오프 없이 돌려 12/12 `ok` 를 확인한다. 새 파일 직전 버전(`--until 20260922120000`)을 다시 돌려 RED 가 그대로임도 확인한다(원인 격리). 위반 행이 있을 때의 실패 동작은 회귀 파일 밖에서 한 번만 수동 확인한다 — 컨테이너에 20260922120000 까지 재생 → `(0,'N',NULL)` 1행 insert → 새 파일 적용 → `RAISE` 메시지와 옛 CHECK 잔존을 원문으로 SUMMARY 에 남긴다(러너에 영구 모드로 넣지 않는다).

**SUMMARY 의 R3 정정 절.** 「## T-18-82 정정 (R3 · GC-CR-01)」 절에 18-18 SUMMARY Task 3 ① 의 문장을 인용하고, 무엇이 틀렸는지(NULL 3값 평가) · 증거(Task 1 두 컷오프 RED 원문 · Task 2 GREEN 원문) · 원격 반영은 18-29 몫임을 적는다. 18-18 SUMMARY 파일은 열어서 읽기만 한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash scripts/verify-dma-orders-price-check.sh</automated>
    <fails_when>non-zero exit, 또는 출력에 "not ok" 가 1줄 이상, 또는 "ok" 로 시작하는 줄이 12 미만 — 새 CHECK 가 NULL 을 여전히 통과시키거나 허용 갈래를 막았다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -v '^ *--' supabase/migrations/20260922180000_dma_orders_price_check_null_safe.sql | grep -ciE 'create policy|grant |revoke |not valid'</automated>
    <fails_when>출력이 0 이 아니다 — 접근 정책·권한 구문 또는 기존 행 검증 생략이 섞였다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS" — 주석만 고쳤는데 타입이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 새 마이그레이션 파일명이 `20260922180000_` 로 시작하고 `ls supabase/migrations | tail -1` 이 그 파일이다
    - 파일에 `BEGIN;` · `COMMIT;` · `RAISE EXCEPTION` · `order_type = 'N'` 이 있다 — 각각 `grep -c` ≥ 1
    - 컷오프 없는 러너 실행 12/12 ok 원문과 `--until 20260922120000` 재실행 RED 원문이 SUMMARY 에 있다
    - 위반 행이 있을 때 트랜잭션이 실패하고 옛 CHECK 가 남는다는 수동 확인 원문이 SUMMARY 에 있다
    - `relay/src/store/orders.ts` 의 `krxSession` 주석에 `20260922180000` 과 「신규」 가 있다
    - SUMMARY 에 「T-18-82 정정 (R3 · GC-CR-01)」 절이 있고, `git diff --stat "$PLAN_HEAD_BEFORE"..HEAD -- .planning/phases/18-gh-trade-ui-nxt-vi/18-18-SUMMARY.md` 출력이 비어 있다(`PLAN_HEAD_BEFORE` = 이 플랜 시작 시 기록한 HEAD sha — SUMMARY frontmatter `plan_head_before` 와 같은 값)
    - 20260921120000 · 20260922120000 파일이 바뀌지 않았다 — `git diff --stat "$PLAN_HEAD_BEFORE"..HEAD -- supabase/migrations/20260921120000_dma_orders_modify_offhours.sql supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql` 출력이 비어 있다
  </acceptance_criteria>
  <done>DB CHECK 가 3값 논리의 구멍 없이 「가격 0 = 취소 또는 G2/G3 신규」 를 강제하는 마이그레이션이 준비됐고, 틀렸던 검증 기록의 정정이 새 기록으로 남았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay → Supabase `dma_orders` | 실돈 주문 감사 행이 DB 로 들어간다 — CHECK 가 코드 층 규칙의 마지막 방어선이다 |
| 개발자 워크스테이션 → Docker | 일회용 컨테이너만 쓴다 — 공유·원격 DB 로 새는 경로가 없어야 한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-102 | Tampering | `dma_orders_price_check` NULL 통과 | high | mitigate | NULL 을 FALSE 로 접는 CHECK(Task 2 ③) · pgTAP 12단언이 경계 양쪽을 고정(Task 1) — 코드 층이 느슨해져도 DB 가 세션 없는 가격 0 신규·정정을 거부한다 |
| T-18-103 | Repudiation | 틀린 검증 기록(T-18-82) | medium | mitigate | 원본을 덮지 않고 R3 정정 절(SUMMARY) + 새 마이그레이션 머리 주석에 원인·증거를 남긴다 |
| T-18-104 | Tampering | 회귀가 공유 DB 에 쓰기 | high | mitigate | 러너는 `docker exec` 로 일회용 컨테이너에만 접속 · `--linked`/`SUPABASE_` 0 을 grep 게이트로 강제 · trap 정리 |
| T-18-105 | Denial of Service | 기존 위반 행 때문에 적용 실패 | low | accept | 트랜잭션째 실패하고 아무것도 바뀌지 않는다(fail-closed). 원격 사전 확인은 18-29 가 한다 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 · Docker 이미지도 로컬에 있는 것만 쓰고 새로 받지 않는다 |
</threat_model>

## Artifacts this phase produces

- **DB 회귀:** `supabase/tests/dma_orders_price_check.test.sql` — pgTAP 12단언(거부 6 · 통과 5 · CHECK 단일성 1)
- **스크립트:** `scripts/verify-dma-orders-price-check.sh` — 일회용 `supabase/postgres` 컨테이너 재생 러너(`--until` 컷오프 · trap 정리 · 원격 접촉 0)
- **마이그레이션 파일(미적용):** `supabase/migrations/20260922180000_dma_orders_price_check_null_safe.sql` — 사전 확인 `DO` 블록 + null-safe `dma_orders_price_check`
- **relay 주석:** `relay/src/store/orders.ts` `krxSession` 주석(새 DB 규칙)
- **기록:** SUMMARY 「T-18-82 정정 (R3 · GC-CR-01)」 절 — RED 두 컷오프 · GREEN · 위반 행 실패 원문

<verification>
- `--until 20260922120000` RED 3줄 · 컷오프 없음 GREEN 12/12
- 새 파일에 접근 정책·권한·NOT VALID 0
- 원격 DB 접촉 0 · 적용된 옛 마이그레이션 파일 무변경 · 18-18 SUMMARY 무변경
</verification>

<success_criteria>
- GC-CR-01 이 로컬 증거로 재현되고 같은 증거로 닫혔다(원격 반영만 18-29 에 남는다)
- 허용해야 할 가격 0(취소 · G2/G3 신규)은 한 건도 막히지 않았다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-24-SUMMARY.md` when done
</output>
