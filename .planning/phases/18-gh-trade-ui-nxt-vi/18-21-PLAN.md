---
phase: 18-gh-trade-ui-nxt-vi
plan: 21
type: execute
wave: 11
depends_on: ["18-20"]
gap_closure: true
files_modified:
  - webapp/src/components/trading/__tests__/trading-workbench.test.tsx
  - webapp/src/components/trading/workbench/card-grid.tsx
  - webapp/src/components/trading/__tests__/card-grid.test.tsx
  - webapp/src/components/trading/card/strategy-card.tsx
  - webapp/src/components/trading/__tests__/strategy-card.test.tsx
  - webapp/src/components/trading/card/card-header.tsx
  - webapp/src/components/trading/workbench/trading-workbench.tsx
  - webapp/e2e/specs/trading-workbench.spec.ts
autonomous: true
requirements: [TRADE-09]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WR-05 확인 우선: 수정 전 코드로 「같은 종목(ISIN)의 두 번째 등록 전략이 작업대에서 사라진다」를 재현 테스트로 먼저 판정하고 SUMMARY 에 남긴다. 재현되지 않으면 코드를 바꾸지 않는다"
    - "D-03 「등록된 상따 전략 — 카드와 동기」: 등록 전략 하나에 카드 하나다 — 같은 ISIN 에 전략이 둘(KRX·NXT, 또는 계좌 A·B)이면 카드도 둘이고, 각 카드의 스위치가 **자기 전략 키**만 켜고 끈다. 가동 중인 어느 전략도 작업대에서 끌 수단을 잃지 않는다"
    - "카드 정체성은 ISIN 이 아니라 **카드 id**(작업대가 만든 단조 증가 식별자)다. 격자 key · DOM id · 콜백 인자 · 더티 합산 · 로그 합산이 전부 카드 id 로 간다. 전략 키(`ISIN:계좌:거래소`)는 카드의 **현재 값**이고 등록 전 카드는 거래소 토글로 바뀔 수 있으므로 정체성으로 쓰지 않는다"
    - "사이드바 포커스·`?focus=` 는 **요청한 전략 키의 카드**를 펼친다 — 같은 ISIN 의 다른 키 카드를 펼치지 않는다. 그 키의 카드가 없으면 그 전략의 계좌·거래소로 카드를 붙인다"
    - "D-07 · D-08 종목 단위 규칙 유지: 돌파 칩·종목 추가는 그 ISIN 의 카드가 이미 있으면 **첫 카드**를 펼치고 스크롤하며 새 카드를 만들지 않는다. 「거래중」 표식은 ISIN 단위 그대로"
    - "전략 키 충돌 금지: 두 카드가 같은 전략 키를 가질 수 없다 — 등록 전 카드의 거래소 토글이 다른 카드의 키와 같아지면 토글하지 않고 **그 카드를 펼쳐 스크롤**한다. 등록 전략 유입도 현재 키가 같은 카드가 있으면 새 카드를 만들지 않는다"
    - "UI-SPEC Q-3: 카드 헤더 종목명 `title` 이 「{종목명} · 계좌 {계좌} · {거래소}」 를 말한다 — 같은 종목 카드가 둘일 때 어느 전략인지 구분된다(보이는 레이아웃은 불변)"
    - "[Phase 18 사용자 결정 2026-09-22] 등록 전략이 있는 카드의 ✕ 는 카드만 닫는다 — 서버 상따 전략은 유지. 닫힌 전략은 사이드바/`?focus=` 로 그 키의 카드로 다시 열린다"
  artifacts:
    - webapp/src/components/trading/workbench/trading-workbench.tsx
    - webapp/src/components/trading/workbench/card-grid.tsx
    - webapp/src/components/trading/card/strategy-card.tsx
  key_links:
    - "`limitChasers`(64/60) 의 각 키 → 작업대 카드 1장(현재 키 대조) → `StrategyCard cardId` → 자기 키로만 에코 필터(D-27 · T-18-25 그대로)"
    - "사이드바 `requestTradingFocus(key)` / `?focus=` → 현재 키가 같은 카드 → 펼침·스크롤"
  prohibitions:
    - "MUST NOT 카드 정체성으로 전략 키를 쓴다 — 등록 전 카드가 거래소를 바꾸면 정체성이 바뀌어 다시 마운트된다(WR-02 가 막은 사고의 재발)"
    - "MUST NOT 돌파 칩·종목 추가가 같은 ISIN 의 두 번째 카드를 만든다 — 사용자 트리거 추가는 종목 단위다(D-07). 추가 카드는 서버 전략 유입과 미체결 선택(18-22)만 만든다"
    - "MUST NOT 카드 헤더에 새 보이는 요소(계좌 배지 등)를 더한다 — 구분은 이미 보이는 거래소 세그먼트와 UI-SPEC Q-3 이 허용한 `title` 로 한다"
---

<objective>
18-REVIEW WR-05 — 「ISIN 당 카드 1장 규칙 때문에 같은 종목의 두 번째 등록 전략이 작업대에서 보이지 않는다(`trading-workbench.tsx:247-248` 의 `continue`), `withFocusedCard` 는 같은 ISIN 의 **다른 키** 카드를 펼친다(143-157)」 — 를 먼저 재현으로 확인하고, 카드 정체성을 ISIN 에서 **카드 id** 로 옮겨 닫는다.

설계 근거(새 UX 결정이 아니다): D-03 이 이미 사이드바의 등록 전략 목록을 「카드와 동기」로 정했다 — 사이드바에는 같은 종목 전략이 두 줄로 보이는데 작업대 카드는 하나뿐인 지금이 D-03 위반이다. 보이는 레이아웃(D-09 격자·스택, D-10 헤더)은 그대로고, 같은 종목 카드 둘은 이미 보이는 거래소 세그먼트와 UI-SPEC Q-3 이 허용한 헤더 `title` 로 구분된다.

Purpose: TRADE-09 작업대에서 가동 중인 실전략을 끌 수단이 사라지는 경로를 없앤다.
Output: 재현 판정 · 카드 id 계약(격자·카드·헤더 title) · 작업대 키 규칙(유입·포커스·충돌) · e2e GC3.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-REVIEW.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-11-SUMMARY.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-12-SUMMARY.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-20-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer · 확인 우선] WR-05 재현 — 같은 ISIN 두 전략 · 두 번째 키 포커스</name>
  <files>webapp/src/components/trading/__tests__/trading-workbench.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/workbench/trading-workbench.tsx (126-157 `WorkbenchCard`·`withFocusedCard` · 223-298 `?focus=`·등록 전략 유입·사이드바 요청·`focusCard`·`addCard`)
    - webapp/src/components/trading/__tests__/trading-workbench.test.tsx (40-110 `StubCard`·`cardProps` · 160-197 `lc`/`relay` 헬퍼 · 339-428 「등록된 전략과 ?focus=」「사이드바 포커스 요청」 describe · `byKey`/`cardsInDom` 헬퍼)
  </read_first>
  <behavior>
    - 등록 전략 `X:A:KRX` · `X:A:NXT` 두 건 → 카드가 2장이고 `data-key` 가 두 키 각각이다
    - 등록 전략 `X:A:KRX` · `X:B:KRX` 두 건(계좌 A·B) → 카드 2장
    - 위 첫 상황에서 사이드바 요청 `X:A:NXT` → `data-key="X:A:NXT"` 카드가 펼쳐지고 `X:A:KRX` 카드는 접힌 그대로
    - `?focus=X:A:NXT` 로 마운트 → 그 키의 카드만 펼쳐진다
  </behavior>
  <action>
**코드를 고치기 전에** 새 describe 「WR-05 — 같은 종목의 두 번째 전략」 에 `<behavior>` 4케이스를 쓴다. 단언은 DOM 의 `[data-slot="strategy-card"]` 와 `data-key`·`data-open` 으로만 한다 — 지금 `StubCard` 의 `cardProps` Map 은 ISIN 키라 두 카드를 구분하지 못하므로 이 describe 는 그 Map 을 쓰지 않는다.

수정 전 코드로 돌려 케이스별 RED/GREEN 을 SUMMARY 표에 적는다. RED 가 하나라도 있으면 `test(18-21): WR-05 재현 테스트` 로 커밋(TDD RED 커밋)하고 Task 2 로 간다. 전부 GREEN 이면 WR-05 는 「재현되지 않음」으로 기록하고 Task 2·3 을 건너뛴다(테스트는 회귀 가드로 남긴다) — 그 경우 18-22 의 미체결 선택 규칙은 18-22 PLAN 의 대체 갈래(선택 거부)를 쓴다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- trading-workbench 2>&1 | tail -40</automated>
    <fails_when>출력에 "No test files found" 또는 「WR-05」 describe 가 실행 목록에 없다 — 케이스가 돌지 않았다(RED 자체는 이 태스크의 정상 결과일 수 있다 · 판정은 SUMMARY 표)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "WR-05" webapp/src/components/trading/__tests__/trading-workbench.test.tsx` 가 1 이상, 4케이스 존재
    - SUMMARY 에 수정 전 코드로 실제 실행한 케이스별 RED/GREEN 표가 있다
  </acceptance_criteria>
  <done>WR-05 의 실재 여부가 수정 전 코드로 판정됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: (RED 가 있을 때만) 카드 id 계약 — 격자 key · 카드 콜백/DOM id · 헤더 title</name>
  <reversibility rating="costly">카드 식별 축을 ISIN 에서 카드 id 로 바꾼다 — 격자·카드·작업대·테스트 스텁·e2e 선택자가 함께 바뀐다. 저장 형식·와이어는 무관하다.</reversibility>
  <files>webapp/src/components/trading/workbench/card-grid.tsx, webapp/src/components/trading/__tests__/card-grid.test.tsx, webapp/src/components/trading/card/strategy-card.tsx, webapp/src/components/trading/__tests__/strategy-card.test.tsx, webapp/src/components/trading/card/card-header.tsx</files>
  <read_first>
    - webapp/src/components/trading/workbench/card-grid.tsx (18-20 결과 — 호스트 노드 Map 키 · `toggleIdOf` · 자리표 key)
    - webapp/src/components/trading/card/strategy-card.tsx (566-640 `StrategyCardProps` · 콜백 `useCallback` 들 · `idBase`)
    - webapp/src/components/trading/card/card-header.tsx (160-175 종목명 `title={name}` · props 인터페이스)
    - webapp/src/components/trading/__tests__/card-grid.test.tsx · webapp/src/components/trading/__tests__/strategy-card.test.tsx (스텁의 `strategy-card-${isin}-toggle` id 규약 · 콜백 인자 단언)
  </read_first>
  <behavior>
    - `CardGrid` 가 `id` 로 key·호스트 노드·토글 id(`strategy-card-{id}-toggle`)를 만든다 — 같은 `isin` 두 장이 id 가 다르면 둘 다 그려지고 서로의 호스트를 공유하지 않는다
    - `StrategyCard` 는 `cardId` 로 콜백을 부른다(`onToggle(cardId)` 등) · DOM id 접두가 `cardId` 에서 나온다 · 구독·에코 필터는 여전히 `isin`/전략 키
    - 헤더 종목명 요소의 `title` 이 「{종목명} · 계좌 {계좌} · {거래소}」
  </behavior>
  <action>
**Task 1 이 전부 GREEN 이면 실행하지 않는다.** 이 태스크는 계약(인터페이스)을 먼저 바꾸는 단계라, 끝난 시점에는 작업대(`trading-workbench.tsx`)가 아직 옛 prop 을 넘겨 webapp typecheck 가 작업대 한 곳에서 실패한다 — Task 3 가 소비처를 맞춘다(인터페이스 우선 순서). 이 태스크의 게이트는 두 단위 테스트다.

- `CardGridItem` 에 `id: string` 을 더하고 key·호스트 Map·자리표·`toggleIdOf`·✕ 뒤 포커스의 순서 배열을 전부 `id` 로 바꾼다. `isin` 은 `CardGridItem` 에서 빼지 않는다(작업대가 여전히 넘긴다).
- `StrategyCardProps` 에 `cardId: string` 을 더하고 `onToggle`·`onClose`·`onExchangeChange`·`onInfo`·`onDirtyCountChange`·`onLogChange` 의 첫 인자를 `cardId` 로 바꾼다(인자 이름·JSDoc 도). `idBase` 는 `cardId` 에서 만든다. 부모가 카드마다 클로저를 만들지 않는 규율(④ · `memo`)은 그대로다.
- `CardHeader` 에 선택 prop `nameTitle?: string` 을 더해 종목명 요소 `title` 에 쓰고(미지정이면 지금처럼 `name`), `StrategyCard` 가 「{표시명} · 계좌 {계좌} · {거래소}」 를 넘긴다(UI-SPEC Q-3 — 보이는 요소 추가 없음).
- 테스트 스텁·단언을 새 계약에 맞춘다: `card-grid.test.tsx` 스텁 id 규약을 `strategy-card-${c.id}-toggle` 로, 18-20 의 마운트 유지 케이스에 「같은 isin 두 장(id 다름)이 둘 다 그려진다」 1케이스 추가. `strategy-card.test.tsx` 의 콜백 인자 단언을 `cardId` 로, `title` 단언 1케이스 추가.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- card-grid strategy-card card-header</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "c\.isin" webapp/src/components/trading/workbench/card-grid.tsx` 가 0 이다 — key·호스트 Map·자리표·토글 id·✕ 뒤 포커스 순서가 전부 `id` 를 쓴다
    - 「같은 isin 두 장이 둘 다 그려진다」 케이스 green
    - `StrategyCard` 콜백이 `cardId` 로 불린다는 단언 green · 18-20 의 마운트 유지 케이스 green
  </acceptance_criteria>
  <done>격자와 카드가 「같은 종목 카드 둘」을 서로 섞지 않고 그릴 수 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: (RED 가 있을 때만) 작업대 — 카드 id 부여 · 전략 키 기준 유입/포커스 · 키 충돌 방지 · e2e GC3</name>
  <files>webapp/src/components/trading/workbench/trading-workbench.tsx, webapp/src/components/trading/__tests__/trading-workbench.test.tsx, webapp/e2e/specs/trading-workbench.spec.ts</files>
  <read_first>
    - webapp/src/components/trading/workbench/trading-workbench.tsx (전문 726줄 — 126-157 · 207-360 카드 집합 연산 · 390-420 로그 합치기 · 533-561 `renderCard`)
    - webapp/src/components/trading/__tests__/trading-workbench.test.tsx (Task 1 결과 · `StubCard`)
    - webapp/e2e/specs/trading-workbench.spec.ts (489 `toggle` 헬퍼 `#strategy-card-${isin}-toggle` · 672-710 케이스 10 사이드바 · 조회구 `cardOf`·`strategyItems`)
    - webapp/src/components/layout/app-sidebar.tsx (277-321 `StrategyItem` — `data-strategy-key` · 클릭 시 `requestTradingFocus`)
  </read_first>
  <behavior>
    - Task 1 의 4케이스 green
    - 돌파 칩/종목 추가로 이미 카드가 둘인 ISIN 을 누르면 카드가 늘지 않고 첫 카드가 펼쳐진다
    - 등록 전 카드(KRX)의 거래소를 NXT 로 바꾸려는데 `X:A:NXT` 카드가 이미 있으면 → 등록 전 카드는 KRX 그대로 · `X:A:NXT` 카드가 펼쳐진다
    - 같은 키의 60 에코가 다시 와도 카드가 늘지 않는다(현재 키 대조)
    - 카드 ✕ 와 이탈 경고 더티 합산·로그 합산이 카드 id 로 동작한다(기존 케이스 green)
  </behavior>
  <action>
- `WorkbenchCard` 에 `id: string` 을 더하고, 카드를 만드는 모든 자리(`addCard` · 등록 전략 유입 · `withFocusedCard`)가 작업대 ref 카운터로 `wb-card-{n}` 을 부여한다(재사용·충돌 없음).
- **전략 키 기준:** 등록 전략 유입은 「현재 키(`strategyKey(isin, accountNo, exchange)`)가 같은 카드가 있으면 건너뜀」(ISIN 비교 삭제 — 247-248 의 결함 지점). `withFocusedCard` 는 「현재 키가 요청 키인 카드를 펼침, 없으면 그 전략의 계좌·거래소로 펼친 카드 추가」.
- **종목 단위 유지(D-07 · D-08):** `addCard(isin…)`·`focusCard(isin)` 는 그 ISIN 의 첫 카드를 펼친다(없을 때만 추가). `cardIsins` 집합(스트립 「거래중」)은 그대로 ISIN.
- **키 충돌:** `changeExchange(id, ex)` 는 바꾼 뒤의 키를 다른 카드가 이미 쓰면 바꾸지 않고 그 카드를 펼쳐 스크롤한다 — 주석으로 「두 카드가 같은 키 = 같은 서버 전략을 두 훅이 소유」 위험을 적는다.
- 카드별 연산(`toggleCard`·`closeCard`·`removeCard`·`openInfo`·`reportDirty`·`reportLog`·`closeAsk`)을 카드 id 로 바꾼다. `renderCard` 가 `cardId={c.id}` 를 넘긴다. 미체결 선택 전달 조건(544-551)은 이 태스크에서 바꾸지 않는다(18-22 몫).
- 407-411 의 「카드는 펼침/접힘으로 … 다시 마운트되고」 주석은 18-20 이후 사실이 아니다 — 「재접속 스냅샷이 같은 문장을 다시 쓰는 경우」 방어로 근거를 고친다(합치기 규칙 자체는 유지).
- 테스트: `StubCard` 가 `props.cardId` 로 콜백을 부르고 `cardProps` 를 `cardId` 키로 기록하게 바꾼다. `<behavior>` 2~4 케이스 추가.
- e2e: 489 `toggle` 헬퍼를 id 대신 `cardOf(page, isin)` 안의 헤더 토글(`aria-expanded` 를 가진 버튼)로 찾게 바꾼다. 새 케이스 「GC3 같은 종목의 KRX·NXT 두 전략 = 카드 두 장, 사이드바 NXT 항목은 NXT 카드를 펼친다 (WR-05 · D-03)」: `pushLimitChaserEcho` 로 같은 ISIN·같은 계좌·거래소 KRX/NXT 두 건 → 카드 2장(`data-key` 두 개) · 사이드바 전략 항목 2개 → NXT 항목 클릭 → NXT 카드 `data-open="true"` · KRX 카드는 접힘.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- trading-workbench card-grid strategy-card</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS" — Task 2 의 계약 변경이 소비처에서 아직 안 맞는다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test:e2e -- trading-workbench sidebar-tree</automated>
    <fails_when>non-zero exit, 또는 출력에 "No tests found", 또는 "failed" 가 1 이상</fails_when>
  </verify>
  <acceptance_criteria>
    - Task 1 의 WR-05 4케이스가 green 이다
    - `trading-workbench.tsx` 의 등록 전략 유입 루프에 `x.isin === c.isin` 비교가 남아 있지 않다(키 대조로 교체)
    - 「키 충돌 시 토글 대신 기존 카드 펼침」 단위 케이스 green
    - e2e GC3 green · 기존 케이스 6(스택) · 10(사이드바) · 16(이탈 경고)과 `sidebar-tree.spec.ts` 전량 green
  </acceptance_criteria>
  <done>같은 종목의 전략이 둘이면 카드가 둘이고, 사이드바에서 누른 전략이 정확히 그 카드로 열린다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 전략 집합(64/60) → 작업대 카드 집합 | 등록 전략이 화면에서 사라지면 가동 중인 무인 주문을 끌 수단이 사라진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-92 | Denial of Service | 두 번째 전략 은닉 | high | mitigate | 등록 전략 유입을 전략 키 대조로 — 전략마다 카드(D-03 동기) |
| T-18-93 | Spoofing | 포커스가 다른 전략 카드를 펼침 | medium | mitigate | 포커스는 요청 키의 카드만 · 없으면 그 키로 추가 |
| T-18-94 | Tampering | 두 카드가 같은 전략 키(한 서버 전략을 두 훅이 소유) | high | mitigate | 거래소 토글·유입 모두 현재 키 중복을 거부 — 토글은 기존 카드로 이동 |
| T-18-95 | Repudiation | 같은 종목 카드 구분 | low | mitigate | 헤더 `title` 에 계좌·거래소(UI-SPEC Q-3), 거래소 세그먼트는 이미 보인다 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 |
</threat_model>

## Artifacts this phase produces

- **webapp 계약:** `WorkbenchCard.id` · `CardGridItem.id` · `StrategyCardProps.cardId`(콜백 첫 인자) · `CardHeaderProps.nameTitle`
- **webapp 동작:** 전략 키 기준 유입·포커스 · 거래소 토글 키 충돌 시 기존 카드 이동 · 카드 DOM id `strategy-card-{cardId}-…`
- **테스트:** `trading-workbench.test.tsx` describe 「WR-05 — 같은 종목의 두 번째 전략」 + 키 규칙 케이스 · `card-grid.test.tsx`·`strategy-card.test.tsx` 계약 케이스 · e2e `GC3` · e2e `toggle` 헬퍼 선택자

<verification>
- 수정 전 재현 판정이 SUMMARY 에 있다
- (재현 시) 같은 ISIN 두 전략 = 카드 둘 · 포커스 정확 · 키 충돌 없음 · 기존 작업대/사이드바 e2e green
</verification>

<success_criteria>
- 사이드바 전략 목록과 작업대 카드가 1:1 로 동기한다(D-03)
- 보이는 레이아웃은 바뀌지 않았다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-21-SUMMARY.md` when done
</output>
