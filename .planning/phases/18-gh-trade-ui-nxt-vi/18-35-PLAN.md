---
phase: 18-gh-trade-ui-nxt-vi
plan: 35
type: execute
wave: 19
depends_on: ["18-34"]
gap_closure: true
files_modified:
  - webapp/src/components/trading/workbench/trading-workbench.tsx
  - webapp/src/components/trading/card/card-body.tsx
  - webapp/src/components/trading/card/manual-order-form.tsx
  - webapp/src/components/trading/__tests__/trading-workbench.test.tsx
  - webapp/src/components/trading/__tests__/manual-order-form.test.tsx
  - webapp/src/components/orderbook/order-confirm-dialog.tsx
  - .planning/phases/18-gh-trade-ui-nxt-vi/deferred-items.md
autonomous: true
requirements: [TRADE-07, TRADE-09]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "R3-WR-02 마무리 · 사용자 결정 1: 주문 잠금의 원천이 하나다 — 작업대 · 카드 본문 · 수동주문 폼 어디에도 18-30 의 페이지 단위 잠금 집합과 prop 경로가 남지 않는다. 폼은 컨텍스트 `orderLocks` 만, 작업대 ✕ 는 컨텍스트 `orderLocks` 만 읽는다"
    - "R3-IN-02 작업대 회귀: 진행 중 키의 카드 ✕ → 결과 모름 다이얼로그 → 「카드 닫기」 → 돌파 칩 · 종목 추가 · 미체결 선택 어느 경로로 다시 열어도 새 카드 본문에는 잠금 prop 이 없고(폼이 컨텍스트로 잠긴다) 이 과정의 `send`(lc.set) · `sendOrder` 호출은 0 이다. 결과 모름 키도 같다"
    - "UI-SPEC E7 error: ✕ 확인 다이얼로그 갈래(`unknown` · `registered` · 둘 다면 등록 전략 문장 추가)와 문구는 18-34 그대로다. 잠기지 않은 키의 미등록 카드는 즉시 사라진다"
    - "R3-IN-03: 카드 더티 수 · 직전 로그 문장의 정리는 커밋된 `cards` 에서 파생한다 — 치운 카드 id 를 따로 계산하지 않는다. 계좌 채움 효과는 `setCards` 업데이터 한 번만 부르고 두 번째 계산(`cardsRef` 기준)이 없다. 더티 합산 · 더티 바 수 · 이탈 경고 · 합친 로그 귀속은 지금과 같다"
    - "R3-IN-04: `isOffhoursOrder` 근거 주석이 「가격 0 ⇒ 시간외종가」 전제를 relay 발 주문으로 한정하고, 같은 계좌의 다른 단말(세션 합류 · WinForms) 시장가 미체결도 가격 0 으로 올 수 있으며 그때 칩 · 요약 표기와 정정 잠금 사유가 틀릴 수 있다(잠그는 방향이라 보수적)는 사실을 적는다. 중립 표기는 서버 주문유형 필드가 필요해 이번 라운드에서 하지 않고 deferred-items 에 기록한다 — 동작 변경 0"
  artifacts:
    - webapp/src/components/trading/workbench/trading-workbench.tsx
    - webapp/src/components/trading/card/card-body.tsx
    - webapp/src/components/trading/card/manual-order-form.tsx
    - webapp/src/components/orderbook/order-confirm-dialog.tsx
    - .planning/phases/18-gh-trade-ui-nxt-vi/deferred-items.md
  key_links:
    - "`RelayProvider.orderLocks` → `ManualOrderForm`(작업대 카드 · 호가 탭) — 유일한 잠금 입력"
    - "`RelayProvider.orderLocks` → `TradingWorkbench.closeCard` → `workbench-close-confirm[data-reason]`"
    - "커밋된 `cards` → 정리 효과 → `cardDirty` · `lastLogText` 에서 사라진 id 제거"
  prohibitions:
    - "MUST NOT 잠금을 작업대 · 카드 본문에 다시 상태로 두거나 prop 으로 내린다 — 원천은 `RelayProvider` 하나다(사용자 결정 1)"
    - "MUST NOT 카드에 전략 배열 · 라벨 Map · 잠금 Map 을 prop 으로 내린다(③ · T-18-52)"
    - "MUST NOT 더티 합산 · 더티 바 수 · 이탈 경고의 계산식을 바꾼다 — R3-IN-03 은 정리 경로만 바꾼다"
    - "MUST NOT `isOffhoursOrder` 판정식 · 표기 문구 · 정정 잠금 사유를 바꾼다 — R3-IN-04 는 주석과 기록뿐이다(중립 표기에는 서버 필드가 필요하다)"
    - "MUST NOT 18-34 가 바꾼 `CLOSE_UNKNOWN_BODY` · `RESULT_UNKNOWN_LOCKED_TEXT` 원문을 바꾼다"
---

<objective>
18-34 가 세운 앱 수명 잠금을 **유일한 원천**으로 만들고(18-30 페이지 배선 제거 · R3-WR-02 마무리 · R3-IN-02 작업대 회귀), 같은 파일의 계좌 채움 정리 이중 계산(R3-IN-03)과 `isOffhoursOrder` 근거 주석(R3-IN-04)을 정리한다.

Purpose: 18-34 는 태스크 크기 때문에 18-30 의 페이지 집합 · prop 경로를 남겨 두었다 — 두 경로가 같은 규칙으로 잠그기만 하지만, 잠금의 원천이 둘이면 다음 변경에서 한쪽만 고쳐진다. 계좌 채움 효과는 치운 카드를 `cardsRef` 로, 남길 카드를 업데이터 `prev` 로 따로 계산해 같은 배치의 다른 갱신과 갈릴 수 있다(18-REVIEW-R3 R3-IN-03 — 기능 영향 없음 · 정합성). `isOffhoursOrder` 근거 2는 relay 발 주문에만 성립한다(R3-IN-04).
Output: 단일 원천 잠금 · 작업대 잠금 회귀 describe 재작성 · `cards` 파생 정리 · 주석 정정 · deferred 기록.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-REVIEW-R3.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-34-SUMMARY.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-31-SUMMARY.md

근거 인용(계획 시점 재확인 — 18-34 실행 뒤 줄 번호는 달라질 수 있으니 심볼로 찾는다):
- `webapp/src/components/trading/workbench/trading-workbench.tsx` — `TradingWorkbench` 의 `resultUnknownKeys` · `markResultUnknown`(게이트 분기 위) · `WorkbenchSurfaceProps` · `resultUnknownRef` · `closeCard` · `WorkbenchCardItem` 의 본문 렌더 함수가 `CardBody` 에 잠금 prop 을 넘기는 줄 · 머리 주석 ⑧ ⑨.
- 같은 파일 `forgetCardState`(더티 키 · `lastLogText` 정리) · `removeCard` · 계좌 채움 효과(`forgetCardState(fillAccountCards(cardsRef.current, accountNo).dropped)` 다음 `setCards((prev) => fillAccountCards(prev, accountNo).next)`) · `cardDirtySum` · `dirtyCardCount` 는 `cards` 를 순회한다(사라진 id 의 더티 키는 합산에 들어가지 않는다) · 카드 id 는 재사용하지 않는다(`nextCardId`).
- `webapp/src/components/trading/card/card-body.tsx` `CardBodyProps` 의 두 잠금 prop 과 폼 전달 줄 · `webapp/src/components/trading/card/manual-order-form.tsx` 의 두 잠금 prop · `ResultUnknownKey` export · 18-34 가 남긴 prop 판정.
- `webapp/src/components/trading/__tests__/trading-workbench.test.tsx:1021-1275` describe 「GC-WR-03 — 결과 모름 잠금은 작업대 키 상태다」(본문 prop 을 읽는 `bodyOfKey` · `timeoutOn` 하네스) · `:1277-1366` `fillAccountCards` · GC-IN-05 describe · `:1367-1380` GC-IN-01 더티 바 수.
- `webapp/src/components/trading/__tests__/manual-order-form.test.tsx:834-980` describe 「제어형 결과 모름 잠금 (GC-WR-03)」(prop 기반).
- `webapp/src/components/orderbook/order-confirm-dialog.tsx:74-89` `isOffhoursOrder` 머리 주석 근거 1 · 2.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: [tracer] 잠금 단일 원천 — 18-30 페이지 집합 · prop 경로 제거, 작업대 ✕ · 카드 폼이 컨텍스트 하나만 읽는다 (+ R3-IN-02 작업대 회귀)</name>
  <files>webapp/src/components/trading/workbench/trading-workbench.tsx, webapp/src/components/trading/card/card-body.tsx, webapp/src/components/trading/card/manual-order-form.tsx, webapp/src/components/trading/__tests__/trading-workbench.test.tsx, webapp/src/components/trading/__tests__/manual-order-form.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/workbench/trading-workbench.tsx (전문 — 머리 주석 ⑧ ⑨ · `TradingWorkbench` · `WorkbenchSurface` 인자 · `closeAsk` · `closeCard` · `WorkbenchCardItem` 본문 렌더 함수 · 다이얼로그 렌더)
    - webapp/src/components/trading/card/card-body.tsx (160-230 `CardBodyProps` · 인자 · 285-305 폼 조립)
    - webapp/src/components/trading/card/manual-order-form.tsx (1-60 규율 ② · 100-135 상수 · `ResultUnknownKey` · 225-345 props · 인자 · 400-410 잠금 판정 · 550-610 `handleConfirmed` · 830-845 잠금 문구)
    - webapp/src/components/trading/__tests__/trading-workbench.test.tsx (1-230 모킹 · `relay()` 헬퍼 · `StubCard` · 332-372 카드 제거 describe · 739-782 에코 비분배 · 806-946 미체결 선택 · 1021-1275 GC-WR-03 describe 전문)
    - webapp/src/components/trading/__tests__/manual-order-form.test.tsx (18-34 결과 — 모킹 · 「잠금 원천 = RelayProvider」 describe · 834-980 prop describe)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-34-SUMMARY.md (Provider API 이름 · 바뀐 케이스 목록)
  </read_first>
  <action>
<!-- planner-discipline-allow: resultUnknownLocked -->
<!-- planner-discipline-allow: onResultUnknown -->
<!-- planner-discipline-allow: markResultUnknown -->
<!-- planner-discipline-allow: resultUnknownKeys -->
<!-- planner-discipline-allow: ResultUnknownKey -->
<!-- planner-discipline-allow: orderLocks -->
**제거 (R3-WR-02 · 사용자 결정 1 — 원천은 `RelayProvider` 하나).**
- `trading-workbench.tsx`: `TradingWorkbench` 의 `resultUnknownKeys` 상태 · `markResultUnknown` 콜백 · `WorkbenchSurfaceProps` 의 두 필드 · `resultUnknownRef` 를 지우고, `TradingWorkbench` 는 게이트 분기와 `<WorkbenchSurface />` 만 남긴다. `closeCard` 의 `unknown` 판정은 컨텍스트 `orderLocks`(ref) 하나다. `WorkbenchCardItem` 본문 렌더 함수는 `CardBody` 에 잠금 prop 을 넘기지 않는다. `ResultUnknownKey` import 를 지운다. 머리 주석 ⑧ ⑨ 에서 18-30 배선 설명을 지우고 ⑨ 를 「잠금은 작업대가 들지 않는다 — `RelayProvider.orderLocks`(앱 수명)를 ✕ 판정에만 읽는다 · 카드 폼은 같은 컨텍스트를 스스로 읽는다」 로 쓴다.
- `card-body.tsx`: `CardBodyProps` 의 `resultUnknownLocked` · `onResultUnknown` 과 폼 전달 줄 · `ResultUnknownKey` import 를 지운다(호가 탭 · 작업대 모두 이 두 prop 을 넘기지 않게 된다).
- `manual-order-form.tsx`: `ManualOrderFormProps` 의 두 prop 과 인자 기본값 · `ResultUnknownKey` export · timeout 분기의 콜백 호출을 지운다. 잠금 판정은 `lock !== undefined`, 잠금 문구 조건은 `lock === "result-unknown"` 이고 결과 배너가 없을 때 — 컨텍스트 하나다. 규율 ②-4 에서 prop 경로 문장을 지운다.
- 지운 이름들은 주석 · 테스트 이름 · 테스트 주석에서도 인용하지 않는다(아래 grep 게이트가 `webapp/src` 전체에서 0 을 요구한다). 18-30 을 가리켜야 하면 「18-30 페이지 잠금 배선」 처럼 플랜 번호로 쓴다.

**작업대 회귀 재작성 (R3-IN-02 포함).** `trading-workbench.test.tsx` 의 describe 「GC-WR-03 — 결과 모름 잠금은 작업대 키 상태다」 를 「R3-WR-02 — 잠금은 RelayProvider 가 들고 작업대는 ✕ 에만 읽는다」 로 다시 쓴다. 하네스는 `mockRelay.orderLocks` 를 바꿔 다시 렌더하는 방식이다(본문 prop 에서 잠금을 읽지 않는다). 케이스: ① 결과 모름 키 카드 ✕ → `data-reason="unknown"` · 제목 원문 · 18-34 본문 원문 → 「취소」 면 카드 유지 · 「카드 닫기」 면 제거 ② **진행 중** 키 카드 ✕ → 같은 `unknown` 다이얼로그(R3-IN-02) ③ ①·② 뒤 돌파 칩 · 종목 추가 · 미체결 선택 세 경로로 재추가 → 새 카드가 선다 · 본문 요소 props 에 잠금 키가 없다 · `orderLocks` 는 작업대가 바꾸지 않는다(같은 참조) ④ 잠기지 않은 미등록 카드 ✕ → 다이얼로그 없이 즉시 제거 ⑤ 잠김 + 등록 전략 카드 ✕ → `unknown` 다이얼로그에 등록 전략 문장 추가 ⑥ 잠기지 않은 등록 전략 카드 ✕ → 기존 `registered` 다이얼로그 그대로 ⑦ 다른 계좌 · 다른 거래소 키 잠금은 이 카드 ✕ 에 영향 없음 ⑧ 이 전 과정에서 `send` · `sendOrder` 호출 0. 기존 describe 의 케이스 수보다 줄지 않게 한다 — 옮기지 못한 케이스(prop 전용 단언)는 이름과 대체 케이스를 SUMMARY 에 적는다. `manual-order-form.test.tsx` 의 describe 「제어형 결과 모름 잠금 (GC-WR-03)」 은 prop 이 사라졌으므로 지우고, 그 describe 가 지키던 성질(4버튼 · 문구 · 전송 0 · 요청 키 · 배너와 문구 비중복 · 종목 전환)이 18-34 의 「잠금 원천 = RelayProvider」 describe 에 모두 있는지 대조해 빠진 것만 그 describe 에 더한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS" — 지운 prop 을 아직 넘기거나 읽는 곳이 남았다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/webapp && npx vitest run trading-workbench manual-order-form card-body stock-orderbook-section strategy-card-flow</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄 passed 수가 0</fails_when>
    <automated>cd /Users/alex/repos/gh-radar/webapp && pnpm exec playwright test trading-workbench -g "GC5|GC6"</automated>
    <fails_when>non-zero exit, 또는 "No tests found", 또는 "failed" 가 1 이상, 또는 passed 가 2 미만 — 단일 원천 이관 뒤 재추가 카드 · 호가 탭 잠금이 풀렸다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -rnE "resultUnknownLocked|onResultUnknown|markResultUnknown|resultUnknownKeys|ResultUnknownKey" webapp/src | wc -l</automated>
    <fails_when>출력이 0 이 아니다 — 18-30 배선이 코드 · 테스트에 남았다</fails_when>
  </verify>
  <acceptance_criteria>
    - 위 grep 이 0 이고, `grep -n "orderLocks" webapp/src/components/trading/workbench/trading-workbench.tsx webapp/src/components/trading/card/manual-order-form.tsx` 가 두 파일 모두 1줄 이상 · `grep -c "orderLocks" webapp/src/components/trading/card/card-body.tsx` = 0 (본문은 잠금을 모른다)
    - 작업대 새 describe 의 케이스 수 ≥ 옛 GC-WR-03 describe 의 케이스 수 — SUMMARY 에 옛 → 새 대응표가 있다
    - e2e GC5 · GC6 green · webapp 대상 단위 green
  </acceptance_criteria>
  <done>주문 잠금을 드는 곳이 앱에 하나뿐이다 — 작업대는 닫을 때 그것을 읽기만 하고, 두 표면의 폼은 같은 값을 스스로 읽는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: R3-IN-03 — 카드 정리를 커밋된 `cards` 에서 파생 (계좌 채움 이중 계산 제거)</name>
  <files>webapp/src/components/trading/workbench/trading-workbench.tsx, webapp/src/components/trading/__tests__/trading-workbench.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/workbench/trading-workbench.tsx (Task 1 결과 — `fillAccountCards` · `cardDirty` · `reportDirty` · `cardDirtySum` · `dirtyCardCount` · `lastLogText` · `forgetCardState` · `removeCard` · 계좌 채움 효과 · `reportLog`)
    - webapp/src/components/trading/__tests__/trading-workbench.test.tsx (1277-1380 `fillAccountCards` 순수 함수 · GC-IN-05 · GC-IN-01 더티 바 수 describe · 715-738 이탈 경고)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-31-SUMMARY.md (GC-IN-05 · 「업데이터는 순수하게 두고 치운 id 는 효과 본문에서 같은 함수로 다시 읽는다」 결정)
  </read_first>
  <behavior>
    - 계좌 도착과 같은 키 등록 전략 유입이 **같은 렌더**에 들어와도(한 `act` 안에서 `accounts` 와 `limitChasers` 를 함께 바꿈) 카드 집합 · 펼침 승계 · 더티 바 수 · 이탈 경고가 18-31 규칙 그대로다
    - 더티 2 인 사용자 카드가 계좌 채움으로 치워진 뒤 → 공용 패널 더티 바 수 0 · 이탈 경고 해제 · 같은 키로 다시 만든 카드의 더티 0(새 id)
    - ✕ 로 치운 카드의 직전 로그 문장이 남아 새 카드(다른 id)의 같은 문장을 막지 않는다 · 합친 로그 귀속은 그대로
    - 기존 `fillAccountCards` 순수 함수 · GC-IN-05 · GC-IN-01 · 카드 제거 describe 무수정 green
  </behavior>
  <action>
**RED 는 기대하지 않는다.** R3-IN-03 은 18-REVIEW-R3 가 「합산은 `cards` 기준이라 기능 영향은 없다」 고 판정한 정합성 결함이다 — 사용자가 관찰할 수 있는 결과가 없으므로 수정 전 코드에서 실패하는 테스트를 만들 수 없다. `<behavior>` 는 불변식 회귀 고정이며, 수정 전 · 후 모두 green 인 것이 정상이다. 이 사실과 수정 전 실행 결과를 SUMMARY 에 적는다.

**정리를 `cards` 에서 파생한다 (R3-IN-03 · 리뷰 권고 두 번째 안).** 치운 카드 id 를 따로 계산하는 경로를 없앤다 — 커밋된 `cards` 가 바뀐 뒤 도는 효과 하나가 `cardDirty` 의 키와 `lastLogText` 의 항목 중 **현재 `cards` 에 없는 id** 를 지운다(바뀐 것이 없으면 상태를 그대로 돌려 재렌더를 만들지 않는다). `removeCard` 는 `setCards` 만 부르고, 계좌 채움 효과는 `setCards((prev) => fillAccountCards(prev, accountNo).next)` 한 줄만 부른다 — `cardsRef` 로 한 두 번째 계산이 사라지므로 같은 배치의 다른 효과가 넣은 카드와 계산이 갈라질 자리가 없다. `forgetCardState` 는 이 효과 본문으로 흡수한다. `fillAccountCards` 의 반환 모양은 순수 함수 테스트가 `dropped` 를 단언하는 동안 유지한다(쓰는 곳이 테스트뿐이면 그 사실을 함수 주석에 적는다). 계좌 채움 효과 위 주석과 `forgetCardState` 자리의 주석을 「정리는 `cards` 커밋에서 파생한다 — 치운 id 를 계산하지 않는다 · 카드 id 는 재사용되지 않고 합산은 `cards` 를 순회한다」 로 다시 쓴다. 더티 합산 · 더티 바 수 · 이탈 경고 · `reportLog` 의 계산식은 바꾸지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && npx vitest run trading-workbench shared-panels</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄 passed 수가 0</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "fillAccountCards(cardsRef.current" webapp/src/components/trading/workbench/trading-workbench.tsx</automated>
    <fails_when>출력이 0 이 아니다 — 계좌 채움 효과에 `cardsRef` 기준 두 번째 계산이 남았다</fails_when>
  </verify>
  <acceptance_criteria>
    - `<behavior>` 4항목이 green 이고, SUMMARY 에 「RED 없음 — 관찰 결과가 없는 정합성 결함」 과 수정 전 실행 결과가 있다
    - 위 grep 0 · `removeCard` 본문과 계좌 채움 효과 본문에 정리(더티 키 · 로그 문장 삭제) 호출이 없고, 정리 코드는 `cards` 의존 효과 한 곳에만 있다 — SUMMARY 에 두 자리의 diff 발췌가 있다
    - `git diff "$PLAN_HEAD_BEFORE"..HEAD -- webapp/src/components/trading/workbench/trading-workbench.tsx` 에서 `cardDirtySum` · `dirtyCardCount` 줄이 바뀌지 않았다
  </acceptance_criteria>
  <done>카드를 치우는 경로가 몇 개든 정리는 한 번 · 한 기준(커밋된 카드 집합)으로만 일어난다.</done>
</task>

<task type="auto">
  <name>Task 3: R3-IN-04 — `isOffhoursOrder` 근거 주석 범위 한정 · 중립 표기 deferred 기록</name>
  <files>webapp/src/components/orderbook/order-confirm-dialog.tsx, .planning/phases/18-gh-trade-ui-nxt-vi/deferred-items.md</files>
  <read_first>
    - webapp/src/components/orderbook/order-confirm-dialog.tsx (60-95 `OFFHOURS_PRICE_LABEL` · `isOffhoursOrder` 머리 주석)
    - webapp/src/components/trading/card/manual-order-form.tsx (`modifyLockReason` 주석 — `isOffhoursOrder` 호출 자리)
    - .planning/phases/18-gh-trade-ui-nxt-vi/deferred-items.md (전문 — 항목 형식)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-REVIEW-R3.md (R3-IN-04 원문)
  </read_first>
  <action>
**주석 (R3-IN-04 · 동작 변경 0).** `isOffhoursOrder` 머리 주석의 근거 2를 좁힌다: 가격 0 이 G2/G3 원주문에서만 나온다는 것은 **relay 가 만든 주문**(DB CHECK · zod 불변식)에만 성립한다. 미체결 목록(`RelayAccountState.unf`)은 게이트웨이 계좌 상태라 같은 계좌의 **다른 단말**(세션 합류 · WinForms) 주문도 싣고, 그 단말의 시장가 미체결(단일가 · VI 구간에서 잔존 가능)도 가격 0 으로 올 수 있다 — 그때 칩 · 확인 요약은 「시간외종가」 로, 정정 잠금 사유는 시간외종가 문구로 잘못 말한다. 잠그는 방향이라 보수적이어서 판정식은 그대로 둔다는 것, 중립 표기(「가격 없음」)에는 서버 주문유형 필드가 필요해 deferred 로 넘겼다는 것을 한 문단으로 적는다. 판정식 · `OFFHOURS_PRICE_LABEL` · 정정 잠금 문구는 바꾸지 않는다.

**deferred-items.md.** 새 절 「## 18-35 발견 (R4 · 범위 밖 · 수정 안 함)」 을 파일 끝에 더하고 항목 하나를 기존 형식(어디 · 무엇 · 영향 · 근본 수정 제안)으로 적는다: 「가격 0 · `board` 빈 미체결 행의 중립 표기」 — 어디: `order-confirm-dialog.tsx` `isOffhoursOrder` · 무엇: 다른 단말 시장가 미체결이 「시간외종가」 로 표기되고 정정 잠금 사유가 틀린다 · 영향: 표기 오류(잠금은 보수적 방향이라 주문 위험 없음) · 근본 수정 제안: gh-trade 미체결 스키마에 주문유형 필드를 요청(서버 변경 — Phase 18 범위 밖, CONTEXT 「서버 변경 범위 밖」) → relay · shared 경유 → `board` 빈 가격 0 행을 「가격 없음」 으로. 기존 문단은 고치지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && npx vitest run order-confirm-dialog manual-order-form account-panel</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄 passed 수가 0 — 주석 변경이 판정 동작을 바꿨다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "다른 단말" webapp/src/components/orderbook/order-confirm-dialog.tsx && grep -c "18-35 발견" .planning/phases/18-gh-trade-ui-nxt-vi/deferred-items.md</automated>
    <fails_when>둘 중 하나라도 0 — 주석 범위 한정 또는 deferred 기록이 없다</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff "$PLAN_HEAD_BEFORE"..HEAD -- webapp/src/components/orderbook/order-confirm-dialog.tsx` 의 바뀐 줄이 전부 주석이다(`export function isOffhoursOrder` 줄 · 반환식 · 상수 줄 불변)
    - deferred-items.md 는 추가 줄만 있다(삭제 0)
  </acceptance_criteria>
  <done>「가격 0 = 시간외종가」 가 언제 참이고 언제 아닌지를 코드가 스스로 말하고, 고칠 길은 추적되는 이연 항목으로 남았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 조작(✕ · 재추가) → 실주문 송신 | 잠금 원천이 둘로 갈리면 한쪽만 풀리는 경로가 생긴다 |
| 게이트웨이 미체결(다른 단말 주문 포함) → 화면 표기 | 표기 규칙의 전제가 relay 발 주문에만 성립한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-144 | Tampering | 페이지 집합 · prop 잔재로 잠금 원천이 둘 → 한쪽만 고쳐져 재주문 경로 발생 | medium | mitigate | 18-30 배선 제거 grep 0 게이트 · 작업대 ✕ 와 폼이 같은 컨텍스트 하나를 읽음 · e2e GC5 · GC6 재실행 |
| T-18-145 | Tampering | 정리 파생 변경이 더티 합산 · 이탈 경고를 망가뜨려 미반영 변경 유실 | low | mitigate | 계산식 불변(diff 게이트) · 같은 렌더 유입 + 채움 · 이탈 경고 회귀 |
| T-18-146 | Repudiation | 다른 단말 시장가 미체결을 「시간외종가」 로 오표기 | low | accept | 잠그는 방향이라 주문 위험 없음 — 주석으로 전제 범위 명시 · 근본 수정(서버 주문유형 필드)은 deferred-items 로 추적 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 |
</threat_model>

## Artifacts this phase produces

- **webapp:** 18-30 페이지 잠금 배선 제거(`TradingWorkbench` · `WorkbenchSurface` · `CardBody` · `ManualOrderForm` prop) · 작업대 ✕ 컨텍스트 판정 단일화 · `cards` 파생 정리 효과(`forgetCardState` 흡수) · `isOffhoursOrder` 근거 주석
- **테스트:** `trading-workbench.test.tsx` describe 「R3-WR-02 — 잠금은 RelayProvider 가 들고 작업대는 ✕ 에만 읽는다」(옛 GC-WR-03 대체 · R3-IN-02 포함) · R3-IN-03 불변식 회귀 · `manual-order-form.test.tsx` prop describe 제거(성질은 18-34 describe 로 이관)
- **기록:** `deferred-items.md` 「18-35 발견」 — 가격 0 · `board` 빈 행 중립 표기

<verification>
- 18-30 배선 grep 0 · webapp typecheck 클린 · e2e GC5 · GC6 green
- 작업대 · 폼 · 카드 본문 · 호가 탭 섹션 · 공용 패널 단위 green
- `isOffhoursOrder` 동작 불변 · deferred 기록
</verification>

<success_criteria>
- 주문 잠금의 원천이 앱에 하나뿐이다
- 카드 정리가 한 기준으로만 일어나고, 가격 0 표기의 전제 범위가 코드와 이연 기록에 남았다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-35-SUMMARY.md` when done
</output>
