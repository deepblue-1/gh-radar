---
phase: 18-gh-trade-ui-nxt-vi
plan: 27
type: execute
wave: 14
depends_on: ["18-23"]
gap_closure: true
files_modified:
  - webapp/src/components/trading/card/manual-order-form.tsx
  - webapp/src/components/trading/__tests__/manual-order-form.test.tsx
  - webapp/src/components/orderbook/account-panel.tsx
  - webapp/src/components/orderbook/__tests__/account-panel.test.tsx
  - webapp/src/components/orderbook/order-confirm-dialog.tsx
  - webapp/src/components/orderbook/__tests__/order-confirm-dialog.test.tsx
autonomous: true
requirements: [TRADE-07]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GC-WR-02 · D-21: 취소 확인 다이얼로그가 열린 사이 부분체결로 잔량이 줄면(10 → 4), 확정 때 나가는 취소 수량은 **현재 잔량(4)** 이다 — 막지 않고 내린다(의미가 「잔량 전부」 라서 사용자가 확인한 뜻과 같다). 잔량이 늘거나 같으면 확인한 수량 그대로다. 수동주문 폼과 공용/계좌 패널의 옛 취소 경로가 **같은 순수 함수 하나**로 판정한다"
    - "취소는 여전히 확정 직전에 막히지 않는다 — 원주문이 미체결에서 사라졌어도 확인한 요청을 보낸다(급락 국면 취소 지연 금지 · 서버가 최종 판정). 수량만 내려간다"
    - "GC-IN-06 · D-21: 정정 확정 직전 재대조가 두 경우를 다른 문구로 말한다 — 선택이 사라짐(`selected === null`) → 「원주문이 더 이상 미체결이 아니에요」, 다른 행이 선택됨(주문번호가 다름) → 「선택한 원주문이 바뀌었어요 — 다시 확인해 주세요」. 두 경우 모두 보내지 않는다(막는 동작은 그대로)"
    - "GC-IN-03 · D-21 · D-23: 「시간외종가 원주문」 판정은 `isOffhoursOrder(row)` 하나다 — `board` 가 G2/G3 **또는** `price === 0`. 선택 칩 표기 · 확인 다이얼로그 원주문 줄/주문가 · 정정 잠금(`modifyLockReason`)이 이 함수를 함께 부른다. 그래서 `board === ''` 인데 가격 0 인 행은 칩이 「시간외종가」 라 말하고 정정 버튼도 잠긴다"
  artifacts:
    - webapp/src/components/trading/card/manual-order-form.tsx
    - webapp/src/components/orderbook/order-confirm-dialog.tsx
    - webapp/src/components/orderbook/account-panel.tsx
  key_links:
    - "공용 패널/계좌 패널 미체결 행 → (선택) 수동주문 폼 `selected` · (행 취소 버튼) `account-panel` `cancelTarget` → 확정 시 **지금 렌더의 같은 주문번호 행**으로 수량을 다시 읽는다 → `sendOrder({kind:'cancel', qty})`"
    - "`isOffhoursOrder` → 칩 · `OrderConfirmDialog` 원주문 줄/옛 취소 요약 · `modifyLockReason` — 판정의 유일 지점"
  prohibitions:
    - "MUST NOT 취소를 확정 직전에 막는다(원주문 소멸·잔량 변화 어느 쪽이든) — 수량을 내리는 것만 한다"
    - "MUST NOT 취소 수량을 올린다 — 확인한 수량보다 큰 값은 절대 나가지 않는다"
    - "MUST NOT 정정 재대조를 「고쳐서 보내기」 로 바꾼다 — 정정은 확인한 값과 다르면 보내지 않는다(WR-06 규율 유지)"
    - "MUST NOT `isOffhoursOrder` 를 `manual-order-form.tsx` 에 둔다 — `order-confirm-dialog.tsx` 가 그것을 import 하면 순환 의존(폼 → 다이얼로그 → 폼)이 생긴다. `OFFHOURS_PRICE_LABEL` 옆(다이얼로그 파일)에 둔다"
---

<objective>
수동주문의 「확인한 것 = 나가는 것」 을 취소 수량과 재대조 문구까지 넓히고(GC-WR-02 · GC-IN-06), 가격 0 원주문의 표시·잠금 규칙을 한 판정 함수로 합친다(GC-IN-03).

Purpose: 18-16 은 정정만 확정 직전에 재대조했다. 취소는 「지연은 자산 위험」 이라 재대조를 뺐는데, 뺀 것은 차단만이 아니라 **갱신**이기도 했다 — 다이얼로그가 열린 사이 부분체결로 잔량이 줄면 잔량보다 큰 옛 수량으로 취소가 나가고, 브로커가 「취소가능수량 초과」 로 거부하면 급락 국면에 사용자가 다시 선택·확인해야 한다(GC-WR-02). 재대조 문구는 선택 변경을 「미체결 아님」 으로 말해 사용자가 원주문이 끝났다고 믿을 수 있다(GC-IN-06). 가격 0 = 시간외종가 표시 규칙과 정정 잠금 규칙이 두 벌이라 구 서버의 `board ''` · 가격 0 행에서 칩과 버튼이 서로 다른 말을 한다(GC-IN-03).
Output: `cancelQtyAtConfirm` 순수 함수(폼 · 계좌 패널 공용) · 재대조 문구 둘 · `isOffhoursOrder` 판정 함수와 세 호출부 · 회귀.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-REVIEW-R2.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-16-SUMMARY.md

근거 인용(재확인 완료):
- `webapp/src/components/trading/card/manual-order-form.tsx:359-390` — 취소 스냅샷 `qty: selected.unfilledQty` · `:486-508` `handleConfirmed` 는 `req.kind === 'modify'` 만 재대조, `:497-499` 선택 변경도 `MODIFY_TARGET_GONE_TEXT` · `:139-147` `modifyLockReason` 은 `board` 만 · `:562-565` 칩은 `board` 또는 `price === 0`.
- `webapp/src/components/orderbook/account-panel.tsx:360-399` `handleCancelConfirmed` — `cancelTarget.row`(다이얼로그 열 때의 사본) 수량으로 보낸다. `account` prop 이 지금 렌더의 병합 상태다. `:141` 이미 `unfilledSelectBlockReason` 을 폼 파일에서 import 한다(순환 없음).
- `webapp/src/components/orderbook/order-confirm-dialog.tsx:72` `OFFHOURS_PRICE_LABEL` · `:156-167` `orgLine` 은 `price === 0` 만 · `:432` 옛 취소 요약 주문가도 `price === 0` 만. 확인 상세 타입(취소·정정)에 `board` 가 없다.
- `RelayUnfilled.board: string`(`packages/shared/src/relay.ts:767`) — 구 서버는 빈 값.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] 취소 확정 = 현재 잔량으로 내림 (폼 · 계좌 패널 공용) + 정정 재대조 문구 둘</name>
  <files>webapp/src/components/trading/card/manual-order-form.tsx, webapp/src/components/trading/__tests__/manual-order-form.test.tsx, webapp/src/components/orderbook/account-panel.tsx, webapp/src/components/orderbook/__tests__/account-panel.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/card/manual-order-form.tsx (96-108 재대조 문구 상수 · 351-390 취소 스냅샷 · 486-525 `handleConfirmed`)
    - webapp/src/components/trading/__tests__/manual-order-form.test.tsx (525-573 취소 확정 케이스 · 653-722 WR-06 describe 의 `rerender` 로 잔량을 바꾸는 패턴 · `sendOrder` 모킹)
    - webapp/src/components/orderbook/account-panel.tsx (296-415 상태·`handleCancelConfirmed`·`openCancel`)
    - webapp/src/components/orderbook/__tests__/account-panel.test.tsx (267-345 ④⑤⑦ 취소 다이얼로그·확정·결과 모름 케이스)
  </read_first>
  <behavior>
    - 폼: 잔량 10 행 선택 → 「취소」 다이얼로그 → 같은 주문번호 잔량 4 로 rerender → 확정 → `sendOrder` 1회 · `qty 4` (수정 전: qty 10 = RED)
    - 폼: 잔량 10 → 다이얼로그 → 잔량 12 로 rerender(정정 등으로 늘어남) → 확정 → `qty 10`(올리지 않는다)
    - 폼: 잔량 10 → 다이얼로그 → 선택 해제(null) → 확정 → `qty 10` 으로 **보낸다**(막지 않는다)
    - 계좌 패널: 행 취소 다이얼로그 → `account` 의 같은 주문번호 행 잔량 4 로 rerender → 확정 → `qty 4` (수정 전: 10 = RED)
    - `cancelQtyAtConfirm` 단위: (확인 10, 현재 행 4) → 4 · (10, 12) → 10 · (10, null) → 10 · (10, 주문번호 다른 행) → 10 · (10, 잔량 0 행) → 10
    - 정정 재대조: 다이얼로그 뒤 선택 null → 「원주문이 더 이상 미체결이 아니에요」 · 다른 주문번호 행 선택 → 「선택한 원주문이 바뀌었어요 — 다시 확인해 주세요」 · 두 경우 전송 0 (수정 전: 둘째가 첫째 문구 = RED)
  </behavior>
  <action>
**RED 먼저.** `<behavior>` 의 폼 · 계좌 패널 · 재대조 케이스를 먼저 쓰고 **수정 전 코드로** 돌려 RED 줄을 SUMMARY 에 적는다.

**판정 함수 (GC-WR-02 · D-21).** `manual-order-form.tsx` 에 `cancelQtyAtConfirm(confirmedQty: number, orgOrderNo: string, live: RelayUnfilled | null): number` 를 export 한다 — `live` 가 있고 `live.orderNo === orgOrderNo` 이고 `0 < live.unfilledQty < confirmedQty` 일 때만 `live.unfilledQty`, 그 밖에는 `confirmedQty`. JSDoc 에 규칙(내리기만 · 막지 않음 · 근거: 취소 = 「잔량 전부」 이고 브로커는 잔량 초과 취소를 거부한다 · relay `dupKey` 가 취소에서 수량을 보지 않아 거부 뒤 재시도가 앞선 대기 정산까지 막힌다 · 18-REVIEW-R2 GC-WR-02)을 적는다.
- 폼 `handleConfirmed`: 취소 요청이면 지금 렌더의 `selected`(선택 차단 행을 거른 값)를 `live` 로 넘겨 수량을 다시 정하고 그 요청을 보낸다. 18-16 의 「취소는 재대조하지 않는다」 주석은 「취소는 **막지 않고** 수량만 현재 잔량으로 내린다」 로 고친다.
- 계좌 패널 `handleCancelConfirmed`: `account?.unf` 에서 `cancelTarget.row.orderNo` 와 같은 행을 찾아 `live` 로 넘긴다(콜백 의존성에 `account` 추가). 다이얼로그에 보였던 수량과 나간 수량이 다를 수 있다는 사실은 결과 배너가 아니라 이 함수 JSDoc 이 말한다(새 화면 문구를 만들지 않는다).

**재대조 문구 (GC-IN-06).** `MODIFY_TARGET_CHANGED_TEXT = '선택한 원주문이 바뀌었어요 — 다시 확인해 주세요'` 를 기존 재대조 상수 옆에 export 하고, 정정 재대조에서 `selected === null` → 기존 `MODIFY_TARGET_GONE_TEXT`, 주문번호 다름 → 새 문구, 잔량 감소 → 기존 `MODIFY_REMAINING_CHANGED_TEXT` 로 가른다. 보내지 않는 동작은 그대로다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- manual-order-form account-panel</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 수정 전 코드의 RED 줄(폼 잔량 4 · 계좌 패널 잔량 4 · 선택 변경 문구)이 있다
    - `grep -n "cancelQtyAtConfirm" webapp/src/components/trading/card/manual-order-form.tsx webapp/src/components/orderbook/account-panel.tsx` 가 두 파일 모두 1줄 이상(판정 한 곳 · 호출 두 곳)
    - `grep -n "MODIFY_TARGET_CHANGED_TEXT" webapp/src/components/trading/card/manual-order-form.tsx` 가 선언·사용 2줄 이상
    - 기존 WR-06 describe · 취소 확정 · 계좌 패널 ⑤⑦ 케이스가 무수정 green
  </acceptance_criteria>
  <done>확인한 취소는 언제나 나가고, 나가는 수량은 그 순간의 잔량을 넘지 않는다 — 정정 재대조는 무엇이 바뀌었는지 정확히 말한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `isOffhoursOrder` 한 판정 — 칩 · 확인 다이얼로그 · 정정 잠금</name>
  <files>webapp/src/components/orderbook/order-confirm-dialog.tsx, webapp/src/components/orderbook/__tests__/order-confirm-dialog.test.tsx, webapp/src/components/trading/card/manual-order-form.tsx, webapp/src/components/trading/__tests__/manual-order-form.test.tsx, webapp/src/components/orderbook/account-panel.tsx</files>
  <read_first>
    - webapp/src/components/orderbook/order-confirm-dialog.tsx (60-120 상세 타입 3종 · 150-170 `orgLine` · 400-440 옛 취소 요약)
    - webapp/src/components/orderbook/__tests__/order-confirm-dialog.test.tsx (234-270 describe 「가격 0 원주문 표기 (CR-01 표시 정합)」)
    - webapp/src/components/trading/card/manual-order-form.tsx (Task 1 결과 — 115-167 잠금 사유·`modifyLockReason`·`canModify` · 376-430 취소·정정 확인 상세 · 550-580 칩)
    - webapp/src/components/trading/__tests__/manual-order-form.test.tsx (491-524 잠금 케이스 · 723-760 칩 표기 describe)
    - webapp/src/components/orderbook/account-panel.tsx (400-415 `openCancel` 확인 상세)
  </read_first>
  <behavior>
    - `isOffhoursOrder`: `{board:'G2', price:5000}` → true · `{board:'G3', price:0}` → true · `{board:'', price:0}` → true · `{board:'', price:70000}` → false
    - `board ''` · 가격 0 행 선택 → 칩 「시간외종가」 · 정정 버튼 disabled + `title` 「시간외종가 주문은 정정할 수 없어요 · 취소 후 재등록」 · 취소 버튼 활성 (수정 전: 정정 활성 = RED)
    - `board 'G2'` · 가격 > 0 행(서버가 가격을 실어 온 경우) → 확인 다이얼로그 원주문 줄이 「시간외종가」 (수정 전: 숫자 = RED)
    - 계좌 패널 옛 취소 경로의 `board 'G3'` · 가격 > 0 행 → 요약 주문가 「시간외종가」
    - 기존 「board G3 · price 0 칩」 · 「가격 > 0 행 숫자 표기」 · CR-01 표시 정합 describe 무수정 green
  </behavior>
  <action>
**판정 함수 (GC-IN-03 · D-21 · D-23).** `order-confirm-dialog.tsx` 에 `OFFHOURS_PRICE_LABEL` 과 나란히 `isOffhoursOrder(row: { board?: string; price: number }): boolean` 을 export 한다 — `board` 가 `'G2'`/`'G3'` 이거나 `price === 0`. JSDoc 에 두 근거(서버 `board` 가 정본 · 가격 0 은 DB·zod 불변식상 G2/G3 원주문에서만 나온다 → 구 서버의 빈 `board` 를 보완)와 「판정의 유일 지점 — 칩 · 다이얼로그 · 정정 잠금」 을 적는다(다이얼로그 파일의 주석에는 폼 파일 이름을 적지 않는다 — 역방향 import 검사가 파일 이름 문자열로 센다. 「수동주문 폼」 으로 부른다). `orgLine` 주석의 「보드 인자 없이도 판정이 닫힌다」 는 이제 사실이 아니므로 지운다.
- 다이얼로그: 취소·정정 확인 상세 타입에 선택 필드 `board?: string`(정정은 원주문의 보드)을 더하고, `orgLine` 과 옛 취소 요약의 주문가 표기가 `isOffhoursOrder({ board, price })` 를 부른다. 부재면 가격 규칙만 남는다(기존 호출부 무수정 컴파일).
- 폼: 칩 표기 조건 · `modifyLockReason` 이 같은 함수를 부른다 — 잠금은 `isOffhoursOrder(row)` 면 `MODIFY_LOCK_OFFHOURS_ORDER`. `modifyLockReason`/`canModify` 의 `Pick` 에 `price` 를 더한다. 취소·정정 확인 상세에 `board: selected.board` 를 싣는다.
- 계좌 패널: `openCancel` 확인 상세에 `board: row.board` 를 싣는다.
테스트는 `order-confirm-dialog.test.tsx` 에 판정 4케이스 + `board G2 · 가격 > 0` 원주문 줄 1케이스, `manual-order-form.test.tsx` 에 `board '' · 가격 0` 잠금·칩 1케이스를 더한다(**수정 전 RED** 를 SUMMARY 에).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- order-confirm-dialog manual-order-form account-panel</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp run test</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS", 또는 webapp 통과 수가 1383 미만(18-23 기준선)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "isOffhoursOrder" webapp/src/components/orderbook/order-confirm-dialog.tsx webapp/src/components/trading/card/manual-order-form.tsx` 가 선언 1(다이얼로그) + 호출(다이얼로그 2 · 폼 2 이상)을 보여 준다
    - 폼 칩 표기 줄과 `modifyLockReason` 에서 보드 리터럴 비교가 사라졌다 — `grep -n "=== 'G2'" webapp/src/components/trading/card/manual-order-form.tsx` 출력 없음
    - `manual-order-form.tsx` 가 `order-confirm-dialog.tsx` 를 import 하고 그 반대 import 는 없다 — `grep -n "manual-order-form" webapp/src/components/orderbook/order-confirm-dialog.tsx` 출력 없음
    - SUMMARY 에 수정 전 RED 줄(빈 board · 가격 0 정정 활성 · G2 가격 > 0 숫자 표기)이 있다 · webapp 전량 ≥ 1383 green
  </acceptance_criteria>
  <done>「이 원주문은 시간외종가다」 를 화면 세 곳이 같은 함수로 말하고, 말한 대로 잠근다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 확인 → 실주문 송신 | 다이얼로그가 보여 준 요청과 확정 시 나가는 요청의 차이가 실돈 결과를 바꾼다 |
| relay 미체결 스냅샷/델타 → 폼·패널 | 부분체결로 잔량이 확인 도중에 바뀐다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-113 | Denial of Service | 잔량 초과 취소 거부 → 급락 국면 취소 지연 | high | mitigate | `cancelQtyAtConfirm` 가 현재 잔량으로 내림(막지 않음) · 폼·계좌 패널 공용 · 잔량 4 RED 회귀 두 경로 |
| T-18-114 | Tampering | 확인한 수량보다 큰 취소 | medium | mitigate | 판정 함수는 내리기만 한다(`<` 비교) · (10, 12) → 10 단위 회귀 |
| T-18-115 | Tampering | 시간외종가 원주문 정정 시도(표시·잠금 불일치) | medium | mitigate | `isOffhoursOrder` 단일 판정 · 빈 board · 가격 0 잠금 회귀 · 서버가 최종 거부(2차 방어) |
| T-18-116 | Repudiation | 재대조 문구가 원주문 상태를 오도 | low | mitigate | 선택 소멸/선택 변경 문구 분리 · 두 경우 전송 0 유지 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 |
</threat_model>

## Artifacts this phase produces

- **webapp 폼:** `cancelQtyAtConfirm`(export 순수 함수) · `MODIFY_TARGET_CHANGED_TEXT` · 취소 확정 수량 재판정 · 정정 재대조 3갈래 문구
- **webapp 계좌 패널:** 취소 확정 시 현재 행 잔량으로 재판정 · 확인 상세 `board`
- **webapp 다이얼로그:** `isOffhoursOrder`(export 판정 함수) · 상세 타입 선택 필드 `board`
- **테스트:** `manual-order-form.test.tsx` 취소 수량 3 · 재대조 2 · 빈 board 잠금 1 / `account-panel.test.tsx` 잔량 4 1 / `order-confirm-dialog.test.tsx` 판정 4 · G2 가격 > 0 1

<verification>
- 취소 확정 수량 = min(확인 수량, 현재 잔량>0) — 폼·계좌 패널 동일
- 정정 재대조 문구 3갈래 · 전송 0 유지
- `isOffhoursOrder` 한 곳 판정 · 세 호출부 · 순환 import 0 · webapp 전량 ≥ 1383
</verification>

<success_criteria>
- 사용자가 확인한 「잔량 전부 취소」 가 잔량 변화에도 그대로 이뤄진다
- 가격 0 원주문의 표시와 잠금이 한 규칙이다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-27-SUMMARY.md` when done
</output>
