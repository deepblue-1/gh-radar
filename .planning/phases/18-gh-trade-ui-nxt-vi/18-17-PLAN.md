---
phase: 18-gh-trade-ui-nxt-vi
plan: 17
type: execute
wave: 9
depends_on: ["18-14"]
gap_closure: true
files_modified:
  - webapp/src/lib/use-relay-socket.ts
  - relay/src/hub/subscription-hub.ts
  - relay/tests/rate-cross.test.ts
  - webapp/src/lib/__tests__/relay-socket.test.ts
  - packages/shared/src/relay.ts
  - webapp/e2e/specs/trading-workbench.spec.ts
autonomous: true
requirements: [TRADE-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "[Phase 18 사용자 결정 2026-09-22] 돌파 목록은 **가장 최신 돌파가 맨 위**다 — 정렬 축은 `exchangeTime` **내림차순** · 동률이면 `isin` 오름차순. 화면의 「최신 위」(UI-SPEC E4 · 표 헤더 문구)가 사실이 된다 (TRADE-06)"
    - "D-14: 순서는 relay 와 웹 리듀서가 **같은 축 한 벌**로 정한다 — relay 인증 직후 스냅샷(`getRateCrossItems`) · relay 78 팬아웃 · 웹 76 upsert · 웹 78 전량 교체가 전부 같은 내림차순이다. 돌파 스트립·표 컴포넌트는 재정렬하지 않는다(서버 집합 가공 금지 유지)"
    - "D-14 「재돌파 자리 유지」 : `exchangeTime` 은 above 구간을 **연** A3 시각이라(shared `RelayRateCrossItem` 주석) 같은 구간 안의 76 갱신은 자리를 지키고, 이탈 후 재돌파(새 구간)만 맨 위로 오른다"
    - "D-14 기존 계약 무변경 — 78 은 전량 교체(빈 배열도 권위값), 76 은 `isin`+`exchange` upsert, 상한 200 은 정렬 **뒤** 자르기"
  artifacts:
    - webapp/src/lib/use-relay-socket.ts
    - relay/src/hub/subscription-hub.ts
  key_links:
    - "relay `sortRateCrossNewestFirst`(getter · 78 팬아웃) ↔ webapp `sortRateCross`(76 · 78 리듀서) — 두 파일의 축 주석이 서로를 가리킨다"
    - "`rateCrossItems` 순서 → `breakout-strip.tsx` `breakoutRowsFrom`(순서 그대로) → 칩 줄·표 행 순서"
  prohibitions:
    - "MUST NOT 돌파 스트립/표 컴포넌트에서 `.sort()`·`.reverse()` 로 뒤집는다 — 표시 계층 재정렬은 D-14(서버 집합 가공 금지) 예외를 새로 만드는 것이다. 순서는 리듀서/relay 한 벌에서만 정한다"
    - "MUST NOT 76 upsert 를 자리 보존(인덱스 교체)으로 바꾼다 — 78 전량 교체 경로와 순서가 갈린다"
---

<objective>
사용자 결정(2026-09-22, STATE.md)대로 돌파 목록을 **최신 돌파가 맨 위**로 바꾼다. 지금은 relay getter 와 웹 리듀서가 `exchangeTime` 오름차순(오래된 것이 위)이라 UI-SPEC 의 「최신 위」 문구와 실제 순서가 반대였다(18-08 SUMMARY 미해결 판단 → 사용자 결정으로 확정).

Purpose: TRADE-06 돌파감지 목록이 「새 돌파 상단」(REQUIREMENTS 원문)을 말 그대로 보여 준다.
Output: relay 정렬 헬퍼 1개(getter·78 팬아웃 공용) · 웹 리듀서 정렬 축 반전 · 계약 주석 정정 · relay/webapp/e2e 회귀.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-08-SUMMARY.md

근거 인용(재확인 완료): `webapp/src/lib/use-relay-socket.ts:590-605` `upsertRateCross`/`sortRateCross` 오름차순 · `:279-288` 상태 주석 「정렬은 exchangeTime 오름차순」 · `relay/src/hub/subscription-hub.ts:614-627` `getRateCrossItems` 오름차순 · `:872-888` 78 팬아웃은 서버 원순서 그대로(정렬 없음) · `packages/shared/src/relay.ts` `RelayRateCrossSnapMsg` 주석 「원소 순서는 exchangeTime 오름차순」 · `relay/tests/rate-cross.test.ts:267-272` 오름차순 단언.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] 최신순 한 축 — relay 헬퍼(getter·78 팬아웃) + 웹 리듀서</name>
  <reversibility rating="reversible">정렬 비교자 방향 하나 — 저장 형식·와이어 필드는 그대로다.</reversibility>
  <files>relay/src/hub/subscription-hub.ts, relay/tests/rate-cross.test.ts, webapp/src/lib/use-relay-socket.ts, webapp/src/lib/__tests__/relay-socket.test.ts</files>
  <read_first>
    - relay/src/hub/subscription-hub.ts (600-630 `getRateCrossItems` · 840-900 `#onRateCrossAlert`/`#onRateCrossSnapshot`/`#enrichRateCross`)
    - relay/tests/rate-cross.test.ts (전문 — ⑤-2 오름차순 단언 · 보강 describe ③ 78 팬아웃 단언)
    - webapp/src/lib/use-relay-socket.ts (270-300 상태 주석 · 540-610 `rate.cross`/`rate.cross.snap` 리듀서 · `upsertRateCross`/`sortRateCross`)
    - webapp/src/lib/__tests__/relay-socket.test.ts (훅 하네스 — 프레임 주입·상태 읽기 방식)
    - packages/shared/src/relay.ts (957-1012 `RelayRateCrossItem` · `RelayRateCrossSnapMsg` 주석 — `exchangeTime` 은 above 구간을 연 시각)
  </read_first>
  <behavior>
    - relay: 78 `[t=0901, 0902, 0903]` 수신 → `getRateCrossItems` 순서 `[0903, 0902, 0901]` · 같은 프레임의 `rate.cross.snap` 팬아웃 `items` 순서도 `[0903, 0902, 0901]`
    - relay: `exchangeTime` 동률 두 원소는 `isin` 오름차순
    - webapp 리듀서: 78 `[0901, 0903]` 적용 → `rateCrossItems` `[0903, 0901]` · 이어 76 새 종목(0905) → 맨 위 `[0905, 0903, 0901]`
    - webapp 리듀서: 76 같은 `isin`+`exchange` · 같은 `exchangeTime`(구간 안 갱신) → 자리 그대로 · 값만 교체
    - webapp 리듀서: 76 같은 종목 · 더 늦은 `exchangeTime`(재돌파) → 맨 위로 이동
  </behavior>
  <action>
**relay (D-14 · 사용자 결정 2026-09-22):** `subscription-hub.ts` 모듈 스코프에 순수 비교 헬퍼 `sortRateCrossNewestFirst(items)`(사본 정렬 — `exchangeTime` 내림차순 · 동률 `isin` 오름차순)를 두고, `getRateCrossItems` 의 인라인 정렬과 `#onRateCrossSnapshot` 의 팬아웃 `items` 가 **둘 다** 이 헬퍼를 거치게 한다. 캐시(`#rateCrossItems`)에 넣는 원본과 76 단건 팬아웃은 그대로다(단건은 순서가 없다). 두 자리의 주석(「오름차순 … 78 스냅샷이 서버에서 오는 순서와 같게」)을 「사용자 결정 2026-09-22 — 최신 돌파가 맨 위. 서버 78 원순서(오름차순)와 무관하게 relay 가 내리는 순서는 이 헬퍼 한 곳이 정한다」로 고친다.

**webapp:** `use-relay-socket.ts` `sortRateCross` 비교자를 같은 축(내림차순 · 동률 `isin` 오름차순)으로 바꾸고, `upsertRateCross`·상태 필드 `rateCrossItems` 주석을 「relay `sortRateCrossNewestFirst` 와 같은 축」으로 고친다. `slice(0, MAX_RATE_CROSS)` 는 여전히 정렬 **뒤**다 — 상한을 넘으면 **가장 오래된** 돌파가 잘려 나간다(최신 유지)는 사실을 주석 한 줄로 남긴다.

**테스트:** `rate-cross.test.ts` ⑤-2 의 오름차순 단언을 내림차순으로 고치고(주석 「정렬 축은 exchangeTime ↓ · 동률이면 isin ↑」), 78 팬아웃 순서 · 동률 케이스를 더한다. `relay-socket.test.ts` 에 새 describe 「rate.cross 순서 — 최신 돌파가 맨 위 (사용자 결정 2026-09-22)」로 `<behavior>` 웹 3케이스.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test -- rate-cross</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 passed 수가 0</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- relay-socket breakout-strip</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "\.sort\(|\.reverse\(\)" webapp/src/components/trading/workbench/breakout-strip.tsx</automated>
    <fails_when>non-zero exit — 스트립 컴포넌트가 순서를 스스로 뒤집고 있다(D-14 · 순서는 리듀서 한 벌)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "sortRateCrossNewestFirst" relay/src/hub/subscription-hub.ts` 가 3 이상이다(정의 1 + getter 1 + 78 팬아웃 1)
    - relay 78 팬아웃의 `items` 순서가 내림차순임이 단언된다(캐시 getter 와 별도 단언)
    - webapp 리듀서 3케이스(스냅샷 내림차순 · 새 76 맨 위 · 구간 안 76 자리 유지)가 green
    - `breakout-strip.test.tsx` 가 무수정 green 이다(컴포넌트는 순서를 만들지 않는다)
  </acceptance_criteria>
  <done>relay 가 내리는 어떤 경로의 돌파 목록도, 웹이 76/78 을 적용한 뒤의 목록도 최신 돌파가 맨 위다.</done>
</task>

<task type="auto">
  <name>Task 2: 계약 주석 정정 + e2e 「최신 위」 실브라우저 단언</name>
  <files>packages/shared/src/relay.ts, webapp/e2e/specs/trading-workbench.spec.ts</files>
  <read_first>
    - packages/shared/src/relay.ts (1000-1012 `RelayRateCrossSnapMsg` 주석)
    - webapp/e2e/specs/trading-workbench.spec.ts (1-140 머리 주석·조회구 · 250-265 `pushBreakout` · 347-396 케이스 3 · 1335-1345 돌파 표 열기 패턴)
    - relay/tests/helpers/frames.ts (1088-1102 `buildRateCrossSnapshotFrame` — `FakeRateCrossInput.exchangeTime` 지원)
  </read_first>
  <action>
`packages/shared/src/relay.ts` `RelayRateCrossSnapMsg` 주석의 「원소 순서는 `exchangeTime` 오름차순 · 동률이면 `isin` 오름차순이다」를 「게이트웨이 78 원순서는 오름차순이지만 **relay 가 내리는 순서는 `exchangeTime` 내림차순(최신 돌파가 맨 위) · 동률이면 `isin` 오름차순**이다 — 사용자 결정 2026-09-22. 브라우저 리듀서도 같은 축으로 다시 정렬한다」로 바꾼다. 타입 모양은 무변경.

e2e: 케이스 번호 체계와 겹치지 않게 제목 접두 `GC1` 로 새 케이스 「GC1 돌파 목록은 가장 최신 돌파가 맨 위 — 칩 줄 첫 칩과 표 첫 행이 가장 늦은 돌파 (사용자 결정 2026-09-22 · TRADE-06)」를 더한다. 케이스 3 처럼 시세 자동 응답을 끄고(`relay.setRespondingExchanges([])` — 이탈 삭제 방지), `sendRateCrossSnapshot` 으로 `exchangeTime` 이 다른 두 종목(`E2E_ISIN` 0901 · `E2E_LONG_NAME_ISIN` 0903)을 넣은 뒤 `[data-slot="breakout-chip"]` 첫 요소가 늦은 종목이고, 「더보기」 표의 첫 데이터 행도 같은 종목임을 단언한다. 이어 76(`sendRateCrossAlert`)으로 0905 의 세 번째 종목을 넣으면 그 종목이 첫 칩이 됨을 단언한다. `pushBreakout` 헬퍼는 기존 케이스가 쓰므로 모양을 바꾸지 않는다 — 여러 원소가 필요하면 이 케이스 안에서 게이트웨이 소켓을 직접 기다려 보낸다(헬퍼와 같은 `waitForConnection` 규율).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"(webapp typecheck 는 `tsconfig.e2e.json` 까지 본다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test:e2e -- trading-workbench -g "GC1|돌파"</automated>
    <fails_when>non-zero exit, 또는 출력에 "No tests found", 또는 "failed" 가 1 이상</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "내림차순" packages/shared/src/relay.ts` 가 1 이상이고, 그 줄이 `RelayRateCrossSnapMsg` 주석 블록 안에 있다(「relay 가 내리는 순서는 … 내림차순」)
    - e2e GC1 이 첫 칩 · 표 첫 행 · 76 추가 후 첫 칩 세 지점을 단언하고 green 이다
    - 기존 케이스 3(칩 클릭 → 카드)과 28(폰 밴드 스트립 잘림 0)이 무수정 green
  </acceptance_criteria>
  <done>실브라우저에서 돌파 칩 줄과 표가 가장 최신 돌파부터 보인다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트웨이 76/78 → relay → 브라우저 | 서버 집합을 그대로 나르는 경로 — 순서만 바뀐다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-78 | Tampering | 돌파 집합 가공 | low | mitigate | 정렬만 바꾸고 원소 추가·삭제·병합 규칙(76 upsert · 78 전량 교체)은 무변경. 캐시에는 서버 원본을 둔다 |
| T-18-79 | Denial of Service | 상한 200 절단 | low | accept | 정렬 뒤 절단이라 넘치면 가장 오래된 돌파가 빠진다 — 사용자 결정(최신 우선)과 같은 방향 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 |
</threat_model>

## Artifacts this phase produces

- **relay:** `sortRateCrossNewestFirst`(subscription-hub.ts 모듈 스코프 헬퍼)
- **webapp lib:** `sortRateCross` 비교 축 반전(이름 유지)
- **shared:** `RelayRateCrossSnapMsg` 순서 주석(타입 무변경)
- **테스트:** `rate-cross.test.ts` 내림차순·팬아웃·동률 · `relay-socket.test.ts` describe 「rate.cross 순서 — 최신 돌파가 맨 위」 · e2e `GC1`

<verification>
- relay getter · 78 팬아웃 · 웹 76/78 리듀서가 같은 내림차순
- 스트립·표 컴포넌트는 재정렬하지 않는다(grep 게이트)
- e2e GC1 green
</verification>

<success_criteria>
- UI-SPEC 「최신 위」 문구와 실제 순서가 일치한다
- D-14 의 upsert/전량 교체/상한 규칙은 그대로다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-17-SUMMARY.md` when done
</output>
