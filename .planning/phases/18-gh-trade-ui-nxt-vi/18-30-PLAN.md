---
phase: 18-gh-trade-ui-nxt-vi
plan: 30
type: execute
wave: 15
depends_on: ["18-26", "18-27"]
gap_closure: true
files_modified:
  - webapp/src/components/trading/card/manual-order-form.tsx
  - webapp/src/components/trading/card/card-body.tsx
  - webapp/src/components/trading/workbench/trading-workbench.tsx
  - webapp/e2e/specs/trading-workbench.spec.ts
  - webapp/src/components/trading/__tests__/trading-workbench.test.tsx
  - webapp/src/components/trading/__tests__/manual-order-form.test.tsx
autonomous: true
requirements: [TRADE-07, TRADE-09]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GC-WR-03 · D-20 · D-27: 「결과 모름」(timeout) 잠금이 카드가 아니라 작업대의 `계좌|ISIN|거래소` 키(전략 키 형식) 상태다 — 잠긴 카드를 ✕ 로 닫고 같은 종목을 종목 추가 · 돌파 칩 · 미체결 선택(`cardForUnfilled`) 어느 경로로 다시 열어도 새 카드의 수동주문 4버튼이 잠긴 채이고 「결과를 모르는 주문이 있어…」 문구가 보인다 — 같은 주문을 다시 낼 수 없다"
    - "잠금이 풀리는 때는 옛 카드 로컬 규칙을 페이지 단위로 옮긴 것 하나다 — `/trading` 페이지(`TradingWorkbench`)를 떠날 때(언마운트)만 풀린다. ✕ · 접기/펴기 · 재추가 · 상태줄 계좌 전환 · DMA 게이트 전환으로는 풀리지 않는다. 다른 키(다른 계좌·다른 거래소·다른 종목)는 잠기지 않는다"
    - "UI-SPEC E7 확장: 「결과 모름」 잠금이 걸린 카드의 ✕ 는 확인 다이얼로그를 거친다 — 제목 「결과를 모르는 주문이 있어요」 · 본문 「미체결 목록에서 접수 여부를 확인하세요. 카드를 닫았다 다시 열어도 이 종목의 주문 버튼은 잠긴 채로 남아요.」 · 「취소」 면 카드가 남고 「카드 닫기」 면 사라진다. 등록 전략도 있는 카드는 같은 다이얼로그에 등록 전략 문장이 한 줄 더 붙는다"
    - "종목상세 호가 탭(작업대 밖)의 수동주문 폼은 지금 규칙 그대로다 — 잠금은 그 폼의 로컬 상태이고 종목 전환·언마운트에 풀린다(이 플랜 범위 밖 표면을 바꾸지 않는다)"
    - "서버 송신 0: 잠금·✕ 다이얼로그·재추가는 화면 상태다 — `lc.set`·주문 프레임이 추가로 나가지 않는다"
  artifacts:
    - webapp/src/components/trading/workbench/trading-workbench.tsx
    - webapp/src/components/trading/card/manual-order-form.tsx
    - webapp/src/components/trading/card/card-body.tsx
  key_links:
    - "`sendOrder` → `status:'timeout'` → 폼 `onResultUnknown({accountNo, isin, exchange})` → `TradingWorkbench` 잠금 집합 → `WorkbenchCardItem` `resultUnknownLocked` → `CardBody` → `ManualOrderForm` 버튼 잠금 + 문구"
    - "카드 ✕ → `closeCard` → 잠금 키면 결과 모름 다이얼로그(등록 전략이면 문장 추가) → 「카드 닫기」 → `removeCard`"
  prohibitions:
    - "MUST NOT 잠금 해제 버튼·타이머·에코 기반 자동 해제를 만든다 — 결과를 모르는 주문에 재주문 경로를 주면 그 자리에서 중복 체결이 난다(D-27 · Pitfall 9)"
    - "MUST NOT 잠금 상태를 relay 컨텍스트로 올려 호가 탭까지 바꾼다 — 이 결함의 표면은 작업대다"
    - "MUST NOT 결과 모름 문구에 「실패」 를 쓴다"
    - "MUST NOT 카드에 전략 배열·라벨 Map 을 prop 으로 내린다(③ · T-18-52) — 잠금은 카드별 불리언 + 안정 콜백으로만 내린다"
---

<objective>
GC-WR-03 을 닫는 **수직 슬라이스(tracer)**: 「결과 모름」 잠금을 카드 로컬에서 작업대의 키 상태로 끌어올리고, 잠긴 카드의 ✕ 를 확인 다이얼로그로 막아, ✕ → 재추가로 같은 주문을 다시 낼 수 있는 경로를 없앤다.

Purpose: 18-20 은 접기·펴기의 재마운트만 없앴다. 라운드 1 WR-02 의 근본(중복 체결 방지 잠금이 컴포넌트 수명에 묶임)은 그대로라 — 미등록 카드는 ✕ 한 번에 확인 없이 사라지고(`closeCard` → `removeCard`), 같은 종목을 종목 추가·돌파 칩·미체결 선택으로 다시 열면 잠기지 않은 새 폼이 생긴다. timeout 직후 「결과 모름」 을 본 사용자가 카드를 닫고 다시 여는 것은 자연스러운 행동이다(18-REVIEW-R2 GC-WR-03).
Output: 작업대 잠금 집합(페이지 수명) · 폼·카드 본문의 제어형 잠금 prop · 결과 모름 ✕ 다이얼로그 · e2e GC5 · 단위 회귀.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-REVIEW-R2.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-20-SUMMARY.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md

근거 인용(재확인 완료):
- `webapp/src/components/trading/card/manual-order-form.tsx:14-24` 규율 ②-4(「잠금 해제는 종목 전환(언마운트 포함)뿐」) · `:247` `blocked` 로컬 상태 · `:271-279` 종목 전환 리셋 · `:339` `busy` · `:486-525` timeout → `setBlocked(true)` · `:944-971` `ResultBanner`(결과 모름 원문). 18-27 이 `handleConfirmed` 에 취소 수량 재판정을 더했다 — 그 위에 얹는다.
- `webapp/src/components/trading/card/card-body.tsx:166-208` `CardBodyProps`(선택 행 전달 형식) · `:278-293` 폼 조립.
- `webapp/src/components/trading/workbench/trading-workbench.tsx:246-253` `TradingWorkbench`(게이트 분기 — `WorkbenchSurface` 는 게이트가 서면 언마운트된다) · `:491-521` `removeCard`·`closeCard` · `:766-802` 등록 전략 ✕ 다이얼로그(`workbench-close-confirm`) · `:836-910` `WorkbenchCardItem`(memo · 본문 렌더 함수). 18-26 이 `knowsRegistered` 를 바꿨다.
- e2e: 스텁 게이트웨이는 주문(DirectOrderReq)에 자동 응답하지 않는다(`relay/tests/helpers/fake-gateway.ts` — `pushOrderResp` 만 있음) → 카드 수동주문은 relay 5초 뒤 `timeout` 이 된다. `webapp/e2e/fixtures/relay.ts` `requestLog()` · `orderInserts()` 로 송신 수를 센다.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: [tracer] 작업대 잠금 집합 → 카드 본문 → 폼 · 결과 모름 ✕ 다이얼로그 · e2e GC5(timeout → ✕ → 재추가 = 잠긴 채)</name>
  <files>webapp/src/components/trading/card/manual-order-form.tsx, webapp/src/components/trading/card/card-body.tsx, webapp/src/components/trading/workbench/trading-workbench.tsx, webapp/e2e/specs/trading-workbench.spec.ts</files>
  <read_first>
    - webapp/src/components/trading/card/manual-order-form.tsx (1-60 규율 · 172-203 props · 238-340 상태·리셋·busy · 486-560 확정·버튼 비활성 · 723-770 인라인 문구 · 944-971 `ResultBanner`)
    - webapp/src/components/trading/card/card-body.tsx (160-300 props · 폼 조립)
    - webapp/src/components/trading/workbench/trading-workbench.tsx (전문 — 특히 60-70 ⑧ 머리 주석 · 246-255 · 483-521 · 718-742 `renderCard` · 766-802 다이얼로그 · 836-910 `WorkbenchCardItem`)
    - webapp/e2e/specs/trading-workbench.spec.ts (1-200 헬퍼 `waitForReady`·`cards`·`cardOf`·`field`·종목 추가 조작 · 801-895 GC4 의 수동주문 열기·`manual-order-selchip` 조회 형식)
    - webapp/e2e/fixtures/relay.ts (190-260 `requestLog`·`orderInserts`·`reset`)
  </read_first>
  <action>
**폼 (GC-WR-03 · D-20 · D-27).** `ManualOrderFormProps` 에 선택 prop 두 개를 더한다 — `resultUnknownLocked?: boolean`(상위가 든 잠금) · `onResultUnknown?: (key: { accountNo: string; isin: string; exchange: RelayExchange }) => void`. 버튼 잠금·중복 제출 가드는 「로컬 `blocked` 또는 `resultUnknownLocked`」 를 본다. timeout 이면 지금처럼 로컬 잠금을 걸고 **보낸 요청의** 계좌·ISIN·거래소로 `onResultUnknown` 을 부른다. `resultUnknownLocked` 인데 이 폼 인스턴스에 결과 배너가 없으면(재추가된 카드) 인라인 `role="status"` 로 새 문구 `RESULT_UNKNOWN_LOCKED_TEXT = '결과를 모르는 주문이 있어 주문 버튼을 잠갔어요 — 미체결 목록에서 접수 여부를 확인하세요'` 를 보인다(export · 「실패」 없음). 종목 전환 리셋 효과는 로컬 잠금만 푼다(상위 잠금은 상위 소유). 규율 ②-4 를 「작업대 카드는 작업대가 키별로 잠금을 들고 페이지를 떠날 때만 푼다 · 호가 탭은 이 폼의 로컬 잠금(종목 전환·언마운트)」 으로 고친다.

**카드 본문.** `CardBodyProps` 에 같은 두 prop 을 선택 필드로 더하고 폼에 그대로 넘긴다(선택 행 전달과 같은 형식). 호가 탭(`stock-orderbook-section.tsx`)은 넘기지 않는다 — 무수정.

**작업대.** 잠금 집합(`ReadonlySet<string>`, 키 = 요청의 `strategyKey(isin, accountNo, exchange)`)을 **`TradingWorkbench` 에서** 들고(게이트 분기보다 위의 훅 — 게이트가 서서 `WorkbenchSurface` 가 언마운트돼도 잠금이 산다) `WorkbenchSurface` 에 집합과 안정 콜백(`markResultUnknown`)으로 내린다. 선언 자리 주석에 해제 규칙(페이지 언마운트뿐 — ✕·접기·재추가·계좌 전환·게이트 전환으로 풀리지 않는다 · 옛 카드 로컬 규칙 「언마운트」 를 페이지 단위로 옮김)을 적는다. `WorkbenchCardItem` 에 `resultUnknownLocked={집합.has(keyOf(c))}` 와 안정 콜백을 내리고 본문 렌더 함수가 `CardBody` 로 넘긴다(③ — 불리언 + 안정 콜백만).
✕ (UI-SPEC E7 확장): `closeCard` 가 카드 키가 잠금 집합에 있으면 결과 모름 이유로 다이얼로그를 연다 — 기존 `closeAsk` 를 `{ id, reason: 'unknown' | 'registered' }` 로 넓히고, 다이얼로그는 `data-testid="workbench-close-confirm"` 에 `data-reason` 을 달아 제목·본문을 이유로 가른다. 결과 모름: 제목 「결과를 모르는 주문이 있어요」 · 본문 「미체결 목록에서 접수 여부를 확인하세요. 카드를 닫았다 다시 열어도 이 종목의 주문 버튼은 잠긴 채로 남아요.」, 등록 전략도 있으면 기존 등록 전략 본문 문장을 한 줄 더 붙인다. 버튼은 기존과 같은 「취소」·「카드 닫기」. 머리 주석 ⑧ 을 갱신한다.

**e2e 새 케이스 「GC5 결과 모름 잠금은 카드를 닫았다 다시 열어도 풀리지 않는다 — ✕ 는 확인을 거친다 (GC-WR-03 · D-20)」:** 카드 0장 → 종목 추가로 `E2E_ISIN` 카드 → 수동주문 열기 → 가격·수량 → 「매수」 → 확인 → `manual-order-result[data-kind="unknown"]`(relay 5초 timeout, 대기 상한 15초) → 요청 로그의 주문 요청 1건 → 카드 ✕ → `workbench-close-confirm[data-reason="unknown"]` 에 제목 원문 → 「카드 닫기」 → 카드 0 → 같은 종목을 다시 추가 → 새 카드 수동주문의 4버튼 전부 `disabled` · 잠금 문구 보임 → 주문 요청 수 여전히 1 · `orderInserts()` 1.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test:e2e -- trading-workbench -g "GC5"</automated>
    <fails_when>non-zero exit, 또는 출력에 "No tests found", 또는 "failed" 가 1 이상 — 재추가한 카드의 버튼이 열리거나 ✕ 가 확인 없이 닫히면 실패한다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- trading-workbench manual-order-form card-body</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed" — 기존 카드 제거·등록 전략 ✕·결과 모름 케이스가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - e2e GC5 green · 수정 전 코드(이 태스크 이전 커밋)에서 GC5 가 「재추가 카드 버튼 활성」 으로 실패한다는 한 줄이 SUMMARY 에 있다
    - 잠금 집합 선언이 `TradingWorkbench` 함수 안(게이트 분기 위)에 있다 — `grep -n "markResultUnknown" webapp/src/components/trading/workbench/trading-workbench.tsx` 로 읽은 선언 줄이 `function TradingWorkbench` 와 `if (gateReason` 사이
    - `grep -n "resultUnknownLocked" webapp/src/components/trading/card/card-body.tsx webapp/src/components/trading/card/manual-order-form.tsx` 가 두 파일 모두 1줄 이상 · `grep -n "resultUnknownLocked" webapp/src/components/stock/stock-orderbook-section.tsx` 출력 없음(호가 탭 무수정)
    - 기존 「등록된 전략이 있는 카드는 확인을 거친다」 케이스가 무수정 green(다이얼로그 testid 유지)
  </acceptance_criteria>
  <done>결과를 모르는 주문이 나간 종목은 이 페이지에 있는 동안 카드를 어떻게 닫고 다시 열어도 다시 주문할 수 없다 — 닫는 순간에도 그 사실을 말한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 잠금 경로 회귀 — 재추가 3경로 · 키 범위 · 다이얼로그 갈래 · 호가 탭 불변</name>
  <files>webapp/src/components/trading/__tests__/trading-workbench.test.tsx, webapp/src/components/trading/__tests__/manual-order-form.test.tsx, webapp/src/components/trading/workbench/trading-workbench.tsx</files>
  <read_first>
    - webapp/src/components/trading/__tests__/trading-workbench.test.tsx (1-210 모킹 relay·`StubCard`/본문 스텁 하네스 · 311-352 카드 제거 describe · 742-882 미체결 선택 · 676-718 prop 규율 케이스)
    - webapp/src/components/trading/__tests__/manual-order-form.test.tsx (165-244 확인·timeout 케이스 · `sendOrder` 모킹 형식)
    - webapp/src/components/trading/workbench/trading-workbench.tsx (Task 1 결과)
  </read_first>
  <behavior>
    - 폼(제어형): `resultUnknownLocked` true → 4버튼 disabled · `RESULT_UNKNOWN_LOCKED_TEXT` 보임 · 「매수」 클릭해도 다이얼로그 없음 · `sendOrder` 0 / timeout → `onResultUnknown` 이 보낸 요청의 `{accountNo, isin, exchange}` 로 1회 · 정정·취소 timeout 도 원주문 행의 키로 1회 / prop 없음(호가 탭) → 기존 로컬 잠금 · 종목 전환 시 해제 그대로
    - 작업대: 잠긴 카드 ✕ → `data-reason="unknown"` 다이얼로그 → 「취소」 면 카드 유지 · 「카드 닫기」 면 제거 → **돌파 칩**으로 재추가 → 새 카드 `resultUnknownLocked` true
    - 같은 흐름에서 **미체결 행 선택**(`cardForUnfilled` 새 카드)으로 재추가 → 잠긴 채
    - 같은 ISIN 다른 거래소(NXT) 카드 · 다른 계좌 키는 잠기지 않는다
    - 잠긴 카드 접기/펴기 · 상태줄 계좌 A→B→A 전환 뒤에도 A 키 잠금 유지
    - 잠김 + 등록 전략 카드 ✕ → 결과 모름 다이얼로그에 등록 전략 문장이 추가로 보인다
    - 잠금·✕·재추가 동안 `send`(lc.set) 0 · `sendOrder` 추가 0
  </behavior>
  <action>
`<behavior>` 를 `trading-workbench.test.tsx`(새 describe 「GC-WR-03 — 결과 모름 잠금은 작업대 키 상태다」)와 `manual-order-form.test.tsx`(새 describe 「제어형 결과 모름 잠금 (GC-WR-03)」)에 쓴다. 작업대 테스트는 기존 하네스가 카드 본문을 스텁으로 바꾸고 있으면 그 스텁이 `resultUnknownLocked` · `onResultUnknown` 을 관찰·호출할 수 있게 넓힌다(스텁에서 timeout 을 흉내 내 콜백을 부른다). prop 규율 케이스(676-718)가 카드 prop 목록을 열거한다면 새 불리언·콜백을 허용 목록에 더한다 — 배열·Map 금지 규칙은 그대로다.
테스트가 Task 1 의 키 범위·해제 규칙과 어긋나는 동작을 드러내면 `trading-workbench.tsx` 를 그 규칙에 맞게 고치고 그 사실을 SUMMARY 에 적는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- trading-workbench manual-order-form</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test:e2e -- trading-workbench orderbook</automated>
    <fails_when>non-zero exit, 또는 출력에 "No tests found", 또는 "failed" 가 1 이상 — 작업대 GC1~GC5 · 호가 탭 수동주문(로컬 잠금 규칙) 중 하나가 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 두 새 describe 가 `<behavior>` 전 항목을 덮고 green 이다
    - 호가 탭 로컬 잠금 기존 케이스(「응답 timeout → … 버튼이 잠긴다」 · 종목 전환 리셋)가 무수정 green
    - e2e `trading-workbench` 전량 · `orderbook` 전량 green
  </acceptance_criteria>
  <done>잠금이 키 하나로만 판정되고, 재추가 경로가 셋이어도 같은 규칙이 선다는 것이 단위로 고정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 조작(✕·재추가) → 실주문 송신 | 결과를 모르는 주문 뒤의 재주문은 중복 체결이 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-125 | Tampering | ✕ → 재추가로 잠금 해제 → 중복 주문 | high | mitigate | 잠금을 `TradingWorkbench` 키 집합으로(카드·게이트 수명과 분리) · 재추가 3경로 회귀 · e2e GC5 가 주문 요청 1건 유지를 단언 |
| T-18-126 | Repudiation | 잠긴 이유를 모르는 재추가 카드 | low | mitigate | `RESULT_UNKNOWN_LOCKED_TEXT` 인라인 status · ✕ 다이얼로그 본문이 잠금 지속을 말한다 |
| T-18-127 | Denial of Service | 잠금 범위 과대(다른 계좌·거래소까지 막힘) | low | mitigate | 키 = 요청의 계좌·ISIN·거래소 · 다른 키 비잠금 회귀 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 |
</threat_model>

## Artifacts this phase produces

- **webapp 작업대:** `TradingWorkbench` 결과 모름 잠금 집합(페이지 수명) · `markResultUnknown` 안정 콜백 · `closeAsk` 이유 갈래(`data-reason="unknown" | "registered"`) · 결과 모름 ✕ 다이얼로그 문구
- **webapp 카드 본문 · 폼:** 선택 prop `resultUnknownLocked` · `onResultUnknown` · `RESULT_UNKNOWN_LOCKED_TEXT`(export)
- **테스트:** e2e `GC5` · `trading-workbench.test.tsx` describe 「GC-WR-03 — 결과 모름 잠금은 작업대 키 상태다」 · `manual-order-form.test.tsx` describe 「제어형 결과 모름 잠금 (GC-WR-03)」

<verification>
- GC5: timeout → ✕(확인) → 재추가 → 4버튼 잠김 · 주문 요청 1건 유지
- 재추가 3경로 · 키 범위 · 다이얼로그 갈래 · 호가 탭 불변 회귀 green
- e2e trading-workbench · orderbook 전량 green
</verification>

<success_criteria>
- 결과를 모르는 주문 뒤 같은 키로 다시 주문할 화면 경로가 작업대에 없다
- 잠금이 풀리는 때가 한 문장으로 정의돼 있고 그 밖의 조작으로는 풀리지 않는다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-30-SUMMARY.md` when done
</output>
