---
phase: 18-gh-trade-ui-nxt-vi
plan: 23
type: execute
wave: 13
depends_on: ["18-14", "18-15", "18-16", "18-17", "18-18", "18-19", "18-20", "18-21", "18-22"]
gap_closure: true
files_modified:
  - .planning/phases/18-gh-trade-ui-nxt-vi/18-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TRADE-06, TRADE-07, TRADE-08, TRADE-09]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "갭 클로징 10건(CR-01 · CR-02 · 돌파 최신순 · WR-01~07)이 전부 「닫힘」 또는 「재현되지 않음(회귀 가드만)」 또는 「사람이 보류(defer)」 중 하나로 **근거와 함께** 18-VALIDATION 에 기록된다 — 빈칸·TBD 없음"
    - "자동 게이트 전량 green — config `build_command` 전문 · `test_command` 전문 · Playwright 전량. 통과 수가 18-13 기준선(relay 500 · webapp 1323 · e2e 137) 이상이다"
    - "REQUIREMENTS 추적성은 조기 Complete 없이 정확하다 — TRADE-06~09 는 정의부 체크박스 `[ ]` 그대로, Traceability 는 `Gaps Found` 에서 `Pending`(갭 클로징 완료 · 재검증 대기)으로만 바뀐다. Complete 판정은 재검증(gsd-verifier) 몫이다"
    - "UAT 인계가 늘어난 만큼 정확하다 — 18-VERIFICATION human_verification #6(`close_price_mode=\"zero\"` 실계좌 G2/G3 취소) · #7(2계좌 VI 계좌 불변) 이 Manual-Only 표에 들어가고, 배포 순서(DB → relay → webapp push)와 relay 미배포 사실이 적힌다"
  artifacts:
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-VALIDATION.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "18-14~18-22 SUMMARY 의 재현 판정·게이트 결과 → 18-VALIDATION §Gap Closure 표"
    - "18-VALIDATION 결과 → REQUIREMENTS Traceability(Pending) — 재검증 전 Complete 금지"
  prohibitions:
    - "MUST NOT REQUIREMENTS 에서 TRADE-06~09 를 Complete 로 표시하거나 체크박스를 `[x]` 로 바꾼다"
    - "MUST NOT git push · relay 배포 · webapp 배포를 한다 — push 가 곧 webapp 프로덕션 배포이고, relay(18-14 의 zod 완화) 배포 전 webapp 이 나가면 안 된다(배포는 relay 먼저·push 나중)"
    - "MUST NOT 실패한 게이트를 이 플랜에서 조용히 넘긴다 — 실패는 원인 플랜의 파일 범위 안에서 고치고 그 사실을 SUMMARY 에 적는다"
---

<objective>
Phase 18 갭 클로징 라운드(18-14~18-22)를 **전량 게이트로 확인**하고, 결과를 18-VALIDATION 과 REQUIREMENTS 추적성에 조기 판정 없이 정확히 남긴다.

Purpose: 18-VERIFICATION 이 `gaps_found`(13/15) 로 되돌린 TRADE-06~09 를 재검증에 넘길 수 있는 상태로 만든다 — 판정(Complete)은 재검증이 한다.
Output: 18-VALIDATION §Gap Closure(2026-09-22) · REQUIREMENTS Traceability `Pending` 4행 · UAT 인계 2행 추가.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-VERIFICATION.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전량 게이트 — build_command · test_command · Playwright 전량</name>
  <files>.planning/phases/18-gh-trade-ui-nxt-vi/18-VALIDATION.md</files>
  <read_first>
    - .planning/config.json (`workflow.build_command` · `workflow.test_command` 원문 — 잘라 쓰지 않는다)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-14-SUMMARY.md ~ 18-22-SUMMARY.md (각 플랜의 재현 판정 · 건너뛴 태스크 · 게이트 수치)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-VALIDATION.md (현행 표 형식 · Sign-Off)
  </read_first>
  <action>
config `build_command` 전문(`pnpm --filter @gh-radar/shared build` 로 시작 — 낡은 dist 함정 회피)과 `test_command` 전문, 그리고 `pnpm --filter @gh-radar/webapp run test:e2e` 전량을 실행하고 통과/실패/건너뜀 수를 기록한다. 실패가 있으면 원인 플랜(18-14~18-22)을 찾아 **그 플랜의 파일 범위 안에서** 고치고 다시 돌린다 — 고친 내용과 이유를 SUMMARY 에 적는다. 서비스키 부재로 선재하던 skip(18-13 기준 9건)은 그 사실만 적는다.

이 태스크는 수치를 18-VALIDATION 에 쓰지 않는다(Task 2 가 한 번에 쓴다) — 파일 목록에 둔 것은 Task 2 와 같은 커밋 범위를 명시하기 위해서다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run test && pnpm --filter @gh-radar/webapp run test</automated>
    <fails_when>non-zero exit, 또는 relay 통과 수가 500 미만, 또는 webapp 통과 수가 1323 미만</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test:e2e</automated>
    <fails_when>non-zero exit, 또는 요약의 "failed" 가 1 이상, 또는 통과 수가 137 미만</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 세 명령의 통과/실패/건너뜀 수가 원문 요약줄과 함께 있다
    - 실패를 고쳤다면 원인 플랜 번호·파일·이유가 SUMMARY 에 있다
    - e2e 새 케이스 GC1~GC4 가 통과 목록에 있다(재현되지 않아 건너뛴 플랜의 케이스는 그 사실과 함께)
  </acceptance_criteria>
  <done>갭 클로징 전체가 자동 게이트 전량에서 green 이다.</done>
</task>

<task type="auto">
  <name>Task 2: 18-VALIDATION §Gap Closure 기록 + REQUIREMENTS 추적성(Pending) + UAT 인계</name>
  <files>.planning/phases/18-gh-trade-ui-nxt-vi/18-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-VALIDATION.md (전문 — Per-Task Map · Manual-Only · Sign-Off 형식)
    - .planning/REQUIREMENTS.md (100-106 TRADE-06~09 정의부 체크박스 · 186-189 Traceability 4행 · 199 footer 의 날짜별 기록 형식 — Phase 16 갭 클로징 라운드 문장 형식을 따른다)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-VERIFICATION.md (human_verification 7항목 · gaps 2건 · advisory)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-18-SUMMARY.md (원격 DB 반영 여부 — `apply`/`defer`)
  </read_first>
  <action>
**18-VALIDATION.md:** 맨 아래에 「## Gap Closure (2026-09-22 · 18-14~18-23)」 절을 더한다. 표 한 줄 = 갭 항목 하나(CR-01 코드 3겹 · CR-01 원격 DB · CR-02 · 돌파 최신순 · WR-01 · WR-02 · WR-03 · WR-04 · WR-05 · WR-06 · WR-07 — 11행) × 열(플랜-태스크 · 요구사항 · 판정[닫힘 / 재현 안 됨(회귀 가드) / 보류(defer)] · 자동 명령 · 상태). 판정은 각 SUMMARY 원문을 따른다 — 추정으로 채우지 않는다. Manual-Only 표에 두 행을 더한다: `close_price_mode="zero"` 실계좌 G2/G3 원주문 취소 왕복(TRADE-07 · 18-VERIFICATION #6) · 2계좌 환경 VI 「수정」 계좌 불변(TRADE-08 · #7). Sign-Off 에 「갭 클로징 재판정」 줄과 배포 순서 문장(DB 18-18 → relay 배포 → webapp push, **relay 는 아직 미배포** — 18-14 의 zod 완화가 relay 에 없으면 브라우저의 가격 0 취소는 여전히 close(4400))을 남긴다. `nyquist_compliant` 는 모든 행에 자동 명령이 있고 green 일 때만 true 를 유지한다.

**REQUIREMENTS.md:** Traceability 의 TRADE-06·07·08·09 4행 상태를 `Gaps Found` → `Pending` 으로 바꾼다. 정의부 체크박스는 네 개 모두 `[ ]` 그대로 둔다. 18-18 이 `defer` 였다면 TRADE-07 행 상태 옆이 아니라 footer 문장에 「CR-01 원격 CHECK 미반영」을 적는다(표 칸 형식은 유지). footer `*Last updated:*` 줄 끝에 이 저장소의 기존 형식대로 「/ 2026-09-22 — Phase 18 갭 클로징(18-14~18-23, 10건: CR-01·CR-02·돌파 최신순·WR-01~07): TRADE-06~09 를 Gaps Found → **Pending**(재검증 대기)으로 되돌렸다. Complete 판정은 재검증 몫이다. 정의부 체크박스와 Traceability 대칭 확인. 근거 정본: `18-VALIDATION.md` §Gap Closure.」 한 문단을 붙인다. 요구사항 총계·Coverage 는 바꾸지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && grep -cE "^\| TRADE-0[6-9] \| Phase 18 \| Complete" .planning/REQUIREMENTS.md</automated>
    <fails_when>출력이 1 이상 — 재검증 전에 Complete 로 표시했다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -cE "^\| TRADE-0[6-9] \| Phase 18 \| Pending" .planning/REQUIREMENTS.md</automated>
    <fails_when>출력이 4 가 아니다 — Traceability 4행이 Pending 으로 정렬되지 않았다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -cE "^- \[x\] \*\*TRADE-0[6-9]" .planning/REQUIREMENTS.md</automated>
    <fails_when>출력이 1 이상 — 정의부 체크박스를 조기에 체크했다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "## Gap Closure" .planning/phases/18-gh-trade-ui-nxt-vi/18-VALIDATION.md && ! grep -nE "TBD|미기입" .planning/phases/18-gh-trade-ui-nxt-vi/18-VALIDATION.md</automated>
    <fails_when>첫 grep 출력 0(절이 없다), 또는 둘째 명령 non-zero(빈칸 표식이 남았다)</fails_when>
  </verify>
  <acceptance_criteria>
    - 18-VALIDATION §Gap Closure 표가 11행이고 모든 행의 판정 칸이 세 값 중 하나다
    - Manual-Only 표에 #6·#7 두 행이 추가됐다
    - REQUIREMENTS Traceability 4행이 `Pending`, 체크박스 4개 `[ ]`, Complete 0건 — 위 grep 세 개가 그것을 증명한다
    - footer 에 2026-09-22 갭 클로징 문단이 붙었고 총계·Coverage 숫자는 그대로다
  </acceptance_criteria>
  <done>갭 클로징 결과가 판정 앞서가기 없이 기록돼 재검증이 바로 시작할 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 계획 문서 → 사람의 배포 판단 | 문서가 「완료」를 앞서 말하면 relay 배포 전 webapp push 같은 순서 사고가 난다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-100 | Repudiation | 조기 Complete 표기 | medium | mitigate | grep 게이트 3종으로 Complete·`[x]` 0 을 강제 · Pending 4행 |
| T-18-101 | Tampering | 배포 순서 | high | mitigate | push·배포 금지(prohibitions) · VALIDATION Sign-Off 에 순서와 relay 미배포 사실 명시 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 |
</threat_model>

## Artifacts this phase produces

- **문서:** `18-VALIDATION.md` §Gap Closure(2026-09-22) 표 11행 · Manual-Only 2행 · Sign-Off 재판정 줄 / `REQUIREMENTS.md` Traceability TRADE-06~09 `Pending` · footer 2026-09-22 문단

<verification>
- build_command · test_command · e2e 전량 green(기준선 이상)
- 18-VALIDATION §Gap Closure 11행 판정 완료
- REQUIREMENTS: Complete 0 · `[x]` 0 · Pending 4
</verification>

<success_criteria>
- 재검증(gsd-verifier)이 18-VERIFICATION 의 failed truth #8·#10 과 advisory 7건을 바로 대조할 수 있다
- 배포는 일어나지 않았고, 그 순서가 문서에 있다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-23-SUMMARY.md` when done
</output>
