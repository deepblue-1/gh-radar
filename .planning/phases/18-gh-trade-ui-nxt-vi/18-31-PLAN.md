---
phase: 18-gh-trade-ui-nxt-vi
plan: 31
type: execute
wave: 16
depends_on: ["18-30"]
gap_closure: true
files_modified:
  - webapp/src/components/trading/workbench/trading-workbench.tsx
  - webapp/src/components/trading/__tests__/trading-workbench.test.tsx
  - webapp/src/components/trading/workbench/shared-panels.tsx
  - webapp/src/components/trading/__tests__/shared-panels.test.tsx
  - webapp/src/lib/limit-chaser.ts
  - webapp/src/lib/__tests__/limit-chaser.test.ts
  - webapp/src/components/layout/app-sidebar.tsx
  - webapp/src/components/layout/__tests__/app-sidebar.test.tsx
  - webapp/src/components/trading/card/strategy-card.tsx
  - webapp/e2e/specs/sidebar-tree.spec.ts
  - webapp/e2e/specs/trading-workbench.spec.ts
autonomous: true
requirements: [TRADE-09]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "GC-IN-05 · D-07: 계좌 도착 전에 만든 카드(`X::KRX`, 사용자가 연 카드)와 같은 키의 등록 전략 카드가 함께 있을 때 계좌가 정해지면, 사용자 카드는 치우되 **등록 카드가 사용자 카드의 펼침 상태를 이어받는다**(펼쳐져 있었으면 `open:true`). 치운 카드의 더티 수·직전 로그 문장은 `removeCard` 와 **같은 정리 경로**로 지워진다"
    - "GC-IN-01(R1 IN-03): 공용 패널의 더티 바 비킴 높이는 떠 있는 더티 바 **수가 바뀔 때마다** 다시 잰다 — 이미 바 하나가 떠 있는 동안 더 높은 두 번째 카드의 바가 나타나도 공용 패널이 그 바에 가리지 않는다"
    - "GC-IN-04: 잔고 평가 가격은 카드 순서와 무관하다 — 잔고 행에는 거래소가 없으므로(게이트웨이 `HoldingState` 에 거래소 필드 없음) KRX 시세를 먼저, 없을 때만 NXT 시세를 쓴다. 같은 종목 KRX·NXT 카드가 어떤 순서로 있어도 같은 평가 가격이다"
    - "GC-IN-04 · D-03: 합친 전략 로그의 `who` 와 사이드바 전략 항목 이름이 NXT 전략에 「{종목명} · NXT」 를 붙인다 — 같은 종목 KRX·NXT 두 카드의 줄이 구분된다. KRX(기본 거래소)는 지금처럼 이름만이다"
    - "GC-IN-01: `strategy-card.tsx` 머리 주석의 붙어 버린 두 줄이 다시 두 문장으로 나뉜다(동작 변경 0)"
  artifacts:
    - webapp/src/components/trading/workbench/trading-workbench.tsx
    - webapp/src/components/trading/workbench/shared-panels.tsx
    - webapp/src/lib/limit-chaser.ts
    - webapp/src/components/layout/app-sidebar.tsx
  key_links:
    - "상태줄 계좌 도착 → `fillAccountCards`(순수) → 카드 집합 + 치운 id → `forgetCardState`(= `removeCard` 의 정리 몫)"
    - "카드 `reportDirty` → 더티 카드 수 → `SharedPanels dirtyBarCount` → `useDirtyBarReserve(count)` 재측정"
    - "`quotes` → `holdingQuotePrice(quotes, isin)` → 공용 패널 `priceOf` → 잔고 평가 / `exchangeLabeledName(name, exchange)` → 합친 로그 `who` · 사이드바 전략 이름"
  prohibitions:
    - "MUST NOT 잔고 평가 가격을 카드 집합에서 고른다(카드 순서 의존의 원인이다)"
    - "MUST NOT KRX 전략에 「· KRX」 를 붙여 기존 표면(로그·사이드바)의 KRX 문구를 바꾼다"
    - "MUST NOT 사이드바 항목의 `data-strategy-key`·링크(`/trading?focus=`)를 바꾼다 — 사이드바 → 카드 포커스(18-12 · GC3)가 그 속성을 쓴다"
---

<objective>
작업대의 남은 표시·상태 잔재를 닫는다 — 계좌 채움 효과가 사용자 카드를 말없이 치우는 것(GC-IN-05), 더티 바 관찰이 효과 시점의 바만 보는 것(GC-IN-01 · R1 IN-03), 같은 종목 카드 둘(WR-05) 이후에도 종목 단위로 남은 잔고 평가 가격과 로그/사이드바 이름(GC-IN-04), 붙어 버린 주석(GC-IN-01).

Purpose: 18-21 이 카드 정체성을 카드 id 로 옮긴 뒤에도 몇 곳이 여전히 「ISIN 의 첫 카드」 를 기준으로 판단한다. 발생 창은 좁지만(18-REVIEW-R2 Info) 실돈 화면의 평가 가격이 카드 순서로 바뀌고, 같은 종목 두 전략의 로그 줄이 구분되지 않으며, 두 번째 더티 바가 공용 패널을 가릴 수 있다.
Output: `fillAccountCards` · `forgetCardState` · `dirtyBarCount` · `holdingQuotePrice` · `exchangeLabeledName` · 회귀.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-REVIEW-R2.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-21-SUMMARY.md

근거 인용(재확인 완료):
- `webapp/src/components/trading/workbench/trading-workbench.tsx:302-325` 계좌 채움 효과(키 충돌 카드를 `continue` 로 버리고 정리 없음) · `:483-503` `cardDirty`·`removeCard`(더티 키·`lastLogText` 삭제) · `:558-565` `priceOf`(그 ISIN 의 **첫 카드** 거래소) · `:571-600` `reportLog` `who` = 종목명 · `:752` `dirtyBarVisible={cardDirtySum > 0}`. 18-30 이 잠금 집합·`closeAsk` 갈래를 더했다 — 그 위에 얹는다.
- `webapp/src/components/trading/workbench/shared-panels.tsx:100-127` `useDirtyBarReserve(visible)` — 효과 의존성이 `[visible]` 뿐이라 바가 늘어도 다시 재지 않는다. 테스트 `shared-panels.test.tsx:291-360` ⑨·⑩-a·⑨-a(`offsetHeight` 정의 패턴).
- 잔고 행에 거래소가 없다: `relay/src/generated/StockDMA.fbs:666-671` `HoldingState`(isin · 수량 · 매도가능 · 평단) · `packages/shared/src/relay.ts:718-733` `RelayHolding`. 돌파 가격도 KRX 한 축이다(`webapp/src/lib/use-breakout-quotes.ts:52-58` `breakoutQuotePrice`).
- 사이드바: `webapp/src/components/layout/app-sidebar.tsx:131` `strategyDisplayName` · `:393-397` 전략 항목(`data-strategy-key` 로 GC3 가 찾는다 — `webapp/e2e/specs/trading-workbench.spec.ts:769-799`). 카드 헤더 이름 `title` 은 이미 「{종목명} · 계좌 {계좌} · {거래소}」(18-21 · UI-SPEC Q-3).
- `webapp/src/components/trading/card/strategy-card.tsx:41` — 「… 건넨다. *   접힌 카드는」 로 두 주석 줄이 붙었다.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] 계좌 채움이 사용자 카드의 펼침을 잇는다 · 같은 정리 경로 (IN-05) + 더티 바 수가 바뀔 때마다 재측정 (IN-01 / R1 IN-03)</name>
  <files>webapp/src/components/trading/workbench/trading-workbench.tsx, webapp/src/components/trading/__tests__/trading-workbench.test.tsx, webapp/src/components/trading/workbench/shared-panels.tsx, webapp/src/components/trading/__tests__/shared-panels.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/workbench/trading-workbench.tsx (281-326 계좌·채움 효과 · 482-521 더티 합산·`removeCard`·`closeCard` · 744-756 `SharedPanels` 호출)
    - webapp/src/components/trading/__tests__/trading-workbench.test.tsx (1-210 하네스 — 계좌·`limitChasers` 를 바꾸는 rerender 방식 · 353-400 등록 전략 유입 describe)
    - webapp/src/components/trading/workbench/shared-panels.tsx (65-130 `DIRTY_BAR_FALLBACK_PX`·props·`useDirtyBarReserve` · 140-160 호출)
    - webapp/src/components/trading/__tests__/shared-panels.test.tsx (80-100 기본 props · 270-360 더티 바 describe)
  </read_first>
  <behavior>
    - `fillAccountCards`: [사용자 `X::KRX` open, 등록 `X:A:KRX` closed] + 계좌 A → next = [등록 카드 open:true] · dropped = [사용자 카드 id] / 사용자 카드가 접혀 있었으면 등록 카드는 접힌 채 / 충돌 없는 빈 계좌 카드는 A 로 채워 남는다(dropped 없음) / 빈 계좌 카드가 없으면 같은 배열 참조를 돌려준다
    - 작업대 RTL: 계좌 없음 → 종목 추가로 X 카드(펼침) → 등록 전략 `X:A:KRX` 유입(접힌 카드 추가) → 계좌 A 도착 → 카드 1장 · `data-key="X:A:KRX"` · 펼침 (수정 전: 접힘 = RED)
    - 치운 카드가 더티를 보고했었다면(스텁 카드가 `onDirtyCountChange` 로 보고) 치운 뒤 이탈 경고 합산에 남지 않는다
    - 공용 패널: 바 1개(높이 96)·`dirtyBarCount` 1 → 96px 비킴 → 더 높은 두 번째 바(140)를 DOM 에 붙이고 `dirtyBarCount` 2 로 rerender → 140px (수정 전: 96 그대로 = RED) · `dirtyBarCount` 0 → 비킴 없음
    - 기존 ⑨·⑩-a·⑨-a 는 새 prop 이름으로만 바뀌고 기대값은 그대로 green
  </behavior>
  <action>
**계좌 채움 (GC-IN-05 · D-07).** 채움 규칙을 export 순수 함수 `fillAccountCards(cards, accountNo): { next: WorkbenchCard[]; dropped: string[] }` 로 뺀다 — 빈 계좌 카드를 채우고, 채운 키가 이미 다른 카드의 키면 그 빈 계좌 카드를 치우며(`dropped`), 치운 카드가 펼쳐져 있었으면 그 키의 카드를 `open: true` 로 만든다(접혀 있었으면 건드리지 않는다). 빈 계좌 카드가 없으면 입력 배열을 그대로 돌려준다(효과 무한 루프 방지). `removeCard` 에서 **정리 몫**(카드 더티 키 · `lastLogText` 삭제)을 `forgetCardState(ids)` 로 떼어 두 경로가 함께 부른다 — `removeCard` 는 카드 제거 + `forgetCardState([id])`, 채움 효과는 `setCards(prev => fillAccountCards(prev, accountNo).next)`(업데이터는 순수) + 효과 본문에서 `fillAccountCards(cardsRef.current, accountNo).dropped` 로 `forgetCardState`. 효과 주석에 「사용자가 연 카드의 맥락(펼침)을 등록 카드가 잇는다 · 정리는 `removeCard` 와 같은 경로」 를 적는다.

**더티 바 재측정 (GC-IN-01 · R1 IN-03).** `SharedPanelsProps` 의 옛 불리언 prop(「더티 바가 떠 있는가」)을 `dirtyBarCount: number`(더티가 있는 카드 수)로 **대체**하고(옛 이름은 코드·주석 어디에도 남기지 않는다 — acceptance grep 이 주석까지 센다), `useDirtyBarReserve(count)` 의 레이아웃 효과 의존성을 `count` 로 한다 — 수가 바뀔 때마다 바 목록을 다시 모아 재고 `ResizeObserver` 를 다시 건다. 보인다 = `count > 0`. 작업대는 `cards` 중 `cardDirty[id] > 0` 인 카드 수를 넘긴다. 공용 패널 JSDoc(⑤-b 계열)과 훅 JSDoc 에 「바 수가 바뀌면 다시 잰다 — effect 시점의 바만 관찰하면 새로 뜬 더 높은 바에 가린다」 를 적는다. 기존 테스트의 prop 이름을 바꾸고 `<behavior>` 새 케이스를 더한다.

**RED 기록.** 작업대 RTL 채움 케이스 · 공용 패널 두 번째 바 케이스를 **수정 전 코드로** 돌려 SUMMARY 에 적는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- trading-workbench shared-panels</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS" — `dirtyBarVisible` 을 쓰던 호출부가 남았다</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 수정 전 RED 두 줄(채움 뒤 접힘 · 두 번째 바 96 고정)이 있다
    - `grep -n "fillAccountCards\|forgetCardState" webapp/src/components/trading/workbench/trading-workbench.tsx` 가 선언·호출을 보여 주고 `forgetCardState` 호출이 `removeCard` 와 채움 효과 두 곳이다
    - `grep -rn "dirtyBarVisible" webapp/src` 출력 없음
    - 기존 등록 전략 유입 · 카드 제거 · 이탈 경고 describe 무수정 green
  </acceptance_criteria>
  <done>계좌가 늦게 와도 사용자가 연 카드의 맥락이 이어지고, 떠 있는 더티 바가 몇 개든 공용 패널이 그 가장 높은 바 위에 선다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 잔고 평가 가격 = KRX 우선 · NXT 폴백(카드 순서 무관) · 합친 로그 `who` 거래소 꼬리 (IN-04)</name>
  <files>webapp/src/components/trading/workbench/trading-workbench.tsx, webapp/src/components/trading/__tests__/trading-workbench.test.tsx, webapp/src/lib/limit-chaser.ts, webapp/src/lib/__tests__/limit-chaser.test.ts</files>
  <read_first>
    - webapp/src/components/trading/workbench/trading-workbench.tsx (Task 1 결과 — 558-600 `priceOf`·`reportLog`)
    - webapp/src/lib/limit-chaser.ts (95-127 `strategyKey`·`parseStrategyKey` — 표기 헬퍼가 사는 곳)
    - webapp/src/lib/__tests__/limit-chaser.test.ts (describe 형식)
    - webapp/src/lib/use-breakout-quotes.ts (52-58 `breakoutQuotePrice` — 시세 유효성 판정 형식: 유한·양수)
    - webapp/src/components/trading/__tests__/trading-workbench.test.tsx (공용 패널 `priceOf` · 로그 합치기를 관찰하는 기존 케이스 위치)
  </read_first>
  <behavior>
    - `holdingQuotePrice(quotes, isin)`: KRX 100 · NXT 110 → 100 · NXT 만 110 → 110 · KRX 0/NaN · NXT 110 → 110 · 둘 다 없음 → undefined
    - 작업대: 같은 ISIN 카드 순서 [NXT, KRX] 와 [KRX, NXT] 에서 공용 패널이 받는 `priceOf(isin)` 가 같은 값(KRX) (수정 전: 첫 카드 NXT 면 110 = RED)
    - `exchangeLabeledName('삼성전자', 'NXT')` → 「삼성전자 · NXT」 · `('삼성전자', 'KRX')` → 「삼성전자」
    - 작업대: NXT 카드의 로그 줄 `who` = 「삼성전자 · NXT」 · KRX 카드 = 「삼성전자」 (수정 전: 둘 다 「삼성전자」 = RED)
  </behavior>
  <action>
**평가 가격 (GC-IN-04).** `trading-workbench.tsx` 에 export 순수 함수 `holdingQuotePrice(quotes, isin): number | undefined` 를 두고(KRX 시세가 유한·양수면 그 값, 아니면 NXT 같은 조건, 아니면 `undefined`) `priceOf` 가 그것만 부른다 — 카드 집합(`cardsRef`)을 읽지 않는다. JSDoc 에 근거(잔고 행에 거래소 축이 없다 — `HoldingState` · `RelayHolding` · KRX 가 기본 거래소이자 돌파 가격 축과 같다 · NXT 폴백은 NXT 카드만 있는 종목의 평가를 비우지 않기 위해서다 · 카드 순서 의존 제거 = 18-REVIEW-R2 GC-IN-04)를 적는다.

**거래소 꼬리 (GC-IN-04 · D-03).** `webapp/src/lib/limit-chaser.ts` 에 `exchangeLabeledName(name: string, exchange: RelayExchange): string` 을 export 한다 — NXT 면 「{name} · NXT」, KRX 면 `name` 그대로. JSDoc: 「KRX 는 기본 거래소라 꼬리를 붙이지 않는다 — 같은 종목 KRX·NXT 두 전략을 가르는 최소 표기. 카드 헤더 `title` 의 「· {거래소}」 표기와 같은 구분자」. `reportLog` 의 `who` 가 카드의 이름을 이 함수로 감싼다(카드 id 가 없을 때의 폴백 `id` 는 그대로).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- trading-workbench limit-chaser</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 수정 전 RED 두 줄(카드 순서 [NXT, KRX] 의 평가 가격 · NXT 로그 `who`)이 있다
    - `priceOf` 본문에 `cardsRef` 가 없다 — `grep -n "holdingQuotePrice" webapp/src/components/trading/workbench/trading-workbench.tsx` 로 읽은 `priceOf` 정의 줄 앞뒤 5줄에 카드 참조가 없다
    - `grep -n "export function exchangeLabeledName" webapp/src/lib/limit-chaser.ts` 가 1줄
  </acceptance_criteria>
  <done>잔고 평가 가격이 카드 배치와 무관하고, 합친 로그에서 같은 종목의 NXT 전략 줄이 구분된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 사이드바 NXT 전략 이름 꼬리 (IN-04) · `strategy-card.tsx` 붙은 주석 분리 (IN-01) · 표면 e2e</name>
  <files>webapp/src/components/layout/app-sidebar.tsx, webapp/src/components/layout/__tests__/app-sidebar.test.tsx, webapp/src/components/trading/card/strategy-card.tsx, webapp/e2e/specs/sidebar-tree.spec.ts, webapp/e2e/specs/trading-workbench.spec.ts</files>
  <read_first>
    - webapp/src/components/layout/app-sidebar.tsx (118-135 `strategyLedLabel`·`strategyDisplayName` · 380-410 전략 항목 렌더 — 이름·`title`·`aria-label` 이 어디서 오는지)
    - webapp/src/components/layout/__tests__/app-sidebar.test.tsx (438-520 전략 항목 이름·폴백 케이스)
    - webapp/src/components/trading/card/strategy-card.tsx (30-50 머리 주석 ④·⑤)
    - webapp/src/lib/limit-chaser.ts (Task 2 결과 — `exchangeLabeledName`)
  </read_first>
  <behavior>
    - 사이드바: NXT 전략 항목 이름 「삼성전자 · NXT」 · KRX 전략 「삼성전자」 · 이름 폴백 체인(전략 `name` → 계좌 역매핑 → `code` → ISIN) 뒤에 꼬리가 붙는다(ISIN 폴백이어도 NXT 면 꼬리) (수정 전: NXT 도 이름만 = RED)
    - 항목의 `data-strategy-key` · 링크 `/trading?focus=` · LED 텍스트 대체는 그대로
  </behavior>
  <action>
**사이드바 (GC-IN-04 · D-03 · 「사이드바 쪽도 싸면」 — 한 줄 변경이라 싼 편에 든다).** `strategyDisplayName` 의 결과를 `exchangeLabeledName(…, item.exchange)` 로 감싼다. 항목이 이름을 `title`/`aria-label` 에도 쓰면 같은 값이 흐른다(따로 바꾸지 않는다). `data-strategy-key`·링크·활성 규칙은 건드리지 않는다. `app-sidebar.test.tsx` 에 NXT/KRX 두 케이스와 ISIN 폴백 + NXT 케이스를 더한다(**수정 전 RED** 한 줄을 SUMMARY 에).

**주석 (GC-IN-01).** `strategy-card.tsx` 머리 주석 ⑤ 의 41행은 두 주석 줄이 한 줄로 붙어 있다 — 「본문에 건넨다.」 문장 끝에 다음 줄의 JSDoc 머리(별표·들여쓰기)와 「접힌 카드는」 이 이어 붙었다. 그 문장 끝에서 줄을 끊어, 「접힌 카드는 …」 이 JSDoc 머리를 가진 새 줄로 시작하게 한다. 코드 변경 0.

**표면 e2e.** 사이드바 이름 변화가 e2e 기대(이름 텍스트 조회)와 부딪치는지 `sidebar-tree` · `trading-workbench` 를 돌려 확인한다 — GC3 는 `data-strategy-key` 로 찾으므로 통과해야 한다. 이름 텍스트로 찾는 단언이 NXT 항목에 있을 때만 그 단언을 새 표기로 고치고 SUMMARY 에 적는다(두 spec 은 그 경우에만 바뀐다 — 없으면 무수정).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- app-sidebar strategy-card</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test:e2e -- sidebar-tree trading-workbench</automated>
    <fails_when>non-zero exit, 또는 출력에 "No tests found", 또는 "failed" 가 1 이상 — 사이드바 → 카드 포커스(GC3) 또는 작업대 케이스가 깨졌다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test</automated>
    <fails_when>non-zero exit, 또는 webapp 통과 수가 1383 미만(18-23 기준선)</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 사이드바 NXT 이름 RED 한 줄이 있다
    - `grep -n "exchangeLabeledName" webapp/src/components/layout/app-sidebar.tsx` 가 1줄 이상
    - `grep -n "건넨다. \*" webapp/src/components/trading/card/strategy-card.tsx` 출력 없음
    - e2e `sidebar-tree` · `trading-workbench` green · webapp 전량 ≥ 1383
  </acceptance_criteria>
  <done>사이드바와 합친 로그가 같은 종목의 NXT 전략을 같은 표기로 가른다 — 주석은 읽히는 모양으로 돌아왔다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay 시세·계좌 상태 → 작업대 표시 | 평가 가격·로그 귀속이 사용자의 매매 판단 근거다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-128 | Tampering | 카드 순서에 따른 잔고 평가 가격 흔들림 | low | mitigate | `holdingQuotePrice` KRX 우선·NXT 폴백 고정 축 · 카드 순서 두 가지 회귀 |
| T-18-129 | Repudiation | 같은 종목 두 전략 로그 줄 구분 불가 | low | mitigate | `exchangeLabeledName` 로그·사이드바 공용 · NXT 꼬리 회귀 |
| T-18-130 | Denial of Service | 두 번째 더티 바가 공용 패널(미체결 취소 버튼)을 가림 | medium | mitigate | `dirtyBarCount` 의존 재측정 · 두 번째 바 140px 회귀 |
| T-18-131 | Tampering | 사용자가 연 카드가 말없이 사라짐 | low | mitigate | `fillAccountCards` 펼침 승계 · `forgetCardState` 단일 정리 경로 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 |
</threat_model>

## Artifacts this phase produces

- **webapp 작업대:** `fillAccountCards`(export 순수 함수) · `forgetCardState` · `holdingQuotePrice`(export 순수 함수) · `SharedPanels` 에 `dirtyBarCount` 전달 · 로그 `who` 거래소 꼬리
- **webapp 공용 패널:** `SharedPanelsProps.dirtyBarCount`(옛 불리언 prop 대체) · `useDirtyBarReserve(count)`
- **webapp lib:** `exchangeLabeledName`(export)
- **webapp 사이드바:** NXT 전략 이름 꼬리
- **주석:** `strategy-card.tsx` ⑤ 줄 분리
- **테스트:** `trading-workbench.test.tsx` 채움·평가 가격·로그 `who` · `shared-panels.test.tsx` 두 번째 바 · `limit-chaser.test.ts` 표기 · `app-sidebar.test.tsx` NXT 이름

<verification>
- 채움 뒤 등록 카드가 펼침 승계 · 정리 경로 단일
- 더티 바 수 변화마다 재측정 · 가장 높은 바 기준
- 평가 가격 카드 순서 무관 · NXT 꼬리(로그·사이드바)
- e2e sidebar-tree · trading-workbench green · webapp 전량 ≥ 1383
</verification>

<success_criteria>
- 18-21 이후 종목 단위로 남아 있던 판단이 작업대에서 사라졌다
- 표시 변화는 NXT 꼬리 하나이고 기존 KRX 표면은 그대로다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-31-SUMMARY.md` when done
</output>
