---
phase: 18-gh-trade-ui-nxt-vi
plan: 19
type: execute
wave: 9
depends_on: ["18-14"]
gap_closure: true
files_modified:
  - relay/tests/ws-order.test.ts
  - relay/src/ws/order-handler.ts
autonomous: true
requirements: [TRADE-07]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WR-03 확인 우선: 수정 전 코드에 대해 재현 테스트를 먼저 돌려 결함이 실재하는지 판정하고, 판정(재현됨/재현 안 됨 · 케이스별)을 SUMMARY 에 원문으로 남긴다. 재현되지 않은 케이스는 코드를 바꾸지 않고 회귀 가드로만 남긴다"
    - "D-21 · D-27: 같은 원주문번호의 취소 대기와 정정 대기가 동시에 있을 때 취소확인(`noticeType` \"C\")은 **취소 대기만**, 정정확인(\"M\")은 **정정 대기만** 정산한다 — 수량·가격 축이 우연히 같거나 교차해도 서로의 통보로 정산되지 않는다"
    - "통보가 실어 온 요청 종류(`requestKind` \"New\"/\"Modify\"/\"Cancel\", 비어 있지 않을 때)는 하드 필터다 — 그 종류의 대기만 후보로 남는다. 비어 있으면(구 서버) 이 축은 적용하지 않는다(GC-CR-01 「실어 온 강한 축만 하드 필터」 규율 그대로)"
    - "체결(`noticeType` \"E\") 통보는 정정·취소 대기를 정산하지 않는다 — 체결은 원주문의 사건이지 정정·취소 요청의 답이 아니다. 후보가 1건이어도 마찬가지다"
    - "좁히지 못하면 아무것도 정산하지 않는다(가장 오래된 것 폴백 없음) · 축이 후보를 전부 지우면 전용 warn 로그 · 로그에 주문번호/계좌 원문 없음 — 기존 규율 무변경"
  artifacts:
    - relay/src/ws/order-handler.ts
  key_links:
    - "`handle` 의 `kind`(N/M/C) → `PendingOrder.kind` → `narrowPending` 하드 필터 — 요청 종류가 대기에 실려야 통보 종류와 맞출 수 있다"
  prohibitions:
    - "MUST NOT 재현되지 않은 케이스를 위해 매칭 규칙을 바꾼다 — 실계좌 정산 경로를 근거 없이 건드리지 않는다"
    - "MUST NOT 하드 필터를 `refine`(0건이면 건너뜀) 규율로 쓴다 — 통보가 실어 온 강한 축과 어긋나면 정산하지 않는 것이 GC-CR-01 이다"
    - "MUST NOT `isCancel` 필드를 이 플랜에서 개명한다 — 원주문번호 축 판정과 테스트 전반이 이 이름을 쓴다. 요청 종류는 새 `kind` 필드로 더한다"
---

<objective>
18-REVIEW WR-03 — 「정정 대기가 `isCancel:true` 로 합류해 `narrowPending` 이 취소와 정정 대기를 한 묶음으로 보므로, 같은 원주문의 취소와 정정이 서로의 통보로 정산될 수 있다」 — 를 **먼저 재현 테스트로 확인**하고, 실재하는 케이스만 요청 종류 축(`PendingOrder.kind`)으로 닫는다. 검증자는 이 건을 독립 재현하지 않았다(18-VERIFICATION advisory 「리뷰 인용만」).

Purpose: TRADE-07 정정/취소의 정산 정확성 — 오정산은 「살아 있는 주문이 취소됨으로 뜸 → 재주문 → 중복 체결」, 정산 실패는 「결과 모름」 잠금으로 이어진다.
Output: 재현 판정 기록 · (실재 시) `PendingOrder.kind` + 하드 필터 3종 · 회귀 테스트.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-REVIEW.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-01-SUMMARY.md

플래너가 코드로 대조한 사실(재현 테스트가 판정한다 — 여기 적힌 추정을 결론으로 쓰지 말 것):
- `order-handler.ts:1022-1040` 정정 대기는 `isCancel: needsOrg`(true) 로 등록된다 · 대기에 요청 종류(N/M/C) 필드가 없다(`PendingOrder` 170-201).
- `narrowPending` 1207-1312: 「C」/「M」 통보를 한 묶음(`isCancelNotice`)으로 보고 `isCancel` 대기 전체를 남긴 뒤 수량·가격 `refine` 으로만 가른다 → 취소·정정의 수량·가격이 같으면 좁히지 못해 **둘 다 timeout**, 통보 값이 상대 대기와 우연히 맞으면 **교차 오정산**.
- 같은 함수에서 후보가 1건이면 `refine` 전에 바로 반환한다(1239행 부근) → 정정 대기 1건만 있을 때 **원주문의 체결(E) 통보**(원주문번호 빈 값)가 그 정정 대기를 정산할 수 있다 — 리뷰가 적지 않은 같은 계열의 변형.
- `ParsedOrderResp.requestKind`(envelope.ts:1519-1525, 「New」/「Modify」/「Cancel」, 구 서버 "")가 이미 파싱돼 있지만 매칭에 쓰이지 않는다.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer · 확인 우선] WR-03 재현 테스트 — 수정 전 코드로 RED/GREEN 판정 기록</name>
  <files>relay/tests/ws-order.test.ts</files>
  <read_first>
    - relay/src/ws/order-handler.ts (170-201 `PendingOrder` · 772-800 `handle` 의 `kind`/`needsOrg` · 1010-1045 대기 등록 · 1180-1312 `narrowPending`)
    - relay/tests/ws-order.test.ts (1480-1699 `mkPending`/`mkNotice` 헬퍼와 `narrowPending` describe · 1373-1440 정정 ws 케이스 ㉟㊱ · 836-880 ⑳ 신규·취소 겹침 ws 케이스)
    - relay/src/dma/envelope.ts (1500-1530 `ParsedOrderResp` 필드 — `noticeType`·`orgOrderNo`·`requestKind`·`sideTrusted`)
  </read_first>
  <behavior>
    - (a) 같은 원주문 X 에 취소 대기(수량 10 · 가격 70,000)와 정정 대기(수량 10 · 가격 70,000) · 통보 C(원주문 X · 10 · 70,000) → 기대: 취소 대기
    - (b) (a) 와 같은 두 대기 · 통보 M(원주문 X · 10 · 70,000) → 기대: 정정 대기
    - (c) 취소 대기(10 · 70,000) + 정정 대기(10 · 71,000) · 통보 C(원주문 X · 10 · 71,000) → 기대: 취소 대기 (값이 교차해도 종류가 이긴다)
    - (d) 정정 대기 1건만(원주문 X) · 통보 E(주문번호 X · 원주문번호 "" · `requestKind` "") → 기대: `null`(정산 안 함)
    - (e) 취소·정정 대기 · 통보 A(원주문 X · `requestKind` "Cancel") → 기대: 취소 대기 / `requestKind` "Modify" → 정정 대기
    - ws 수준(f): 같은 원주문에 `order.cancel` 과 값이 다른 `order.modify` 를 연달아 보내고 C 통보 1건 → `order.result` 는 취소 rid 에만 `cancelled`, 정정 rid 는 정산되지 않는다
  </behavior>
  <action>
**코드를 고치기 전에** 재현 테스트부터 쓴다(오케스트레이터 지시 · 검증자 미재현). `ws-order.test.ts` 의 `narrowPending` describe 에 새 하위 describe 「WR-03 — 취소·정정 대기의 요청 종류」로 `<behavior>` (a)~(e) 를, ws describe 에 (f) 를 더한다. 대기 픽스처는 **처음부터** 요청 종류를 싣는다 — `mkPending({ …, isCancel: true, side: "", kind: "C" })`(취소) · `mkPending({ …, isCancel: true, side: "B", kind: "M" })`(정정). 수정 전 `PendingOrder` 에는 `kind` 가 없으므로 vitest(esbuild 변환 — 타입 검사 없음)에서는 그 값이 무시된 채 돌고, 기대값은 「올바른 동작」으로 쓴다. 이 상태에서는 `typecheck:tests` 가 실패하는 것이 정상이다(Task 2 가 필드를 더하면 풀린다).

그다음 **수정 전 `order-handler.ts` 그대로** relay 테스트를 돌려 각 케이스의 결과(RED=결함 재현 · GREEN=재현 안 됨)를 표로 SUMMARY 에 적는다. 판정 규칙:
- RED 인 케이스가 하나라도 있으면 → RED 출력을 SUMMARY 에 남기고 `test(18-19): WR-03 재현 테스트` 로 커밋한다(TDD RED 커밋 — 실패하는 채로 커밋하는 것이 이 단계의 정상이다). 바로 Task 2 가 GREEN 을 만든다.
- 전부 GREEN 이면 → WR-03 은 「재현되지 않음」으로 기록하고 **Task 2 를 건너뛴다**. 픽스처의 `kind` 인자를 지워 `typecheck:tests` 를 통과시킨 뒤 테스트를 회귀 가드로 커밋한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test -- ws-order 2>&1 | tail -40</automated>
    <fails_when>출력에 "No test files found" — 필터가 파일을 못 찾았다. (이 태스크에서는 RED 가 **정상 결과일 수 있다** — 판정은 SUMMARY 표로 하고, 새 케이스 이름 「WR-03」 이 출력에 한 번도 없으면 케이스가 실행되지 않은 것이다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "WR-03" relay/tests/ws-order.test.ts` 가 1 이상이고 (a)~(f) 6케이스가 있다
    - SUMMARY 에 케이스별 「수정 전 결과(RED/GREEN)」 표가 있다 — 수정 전 코드로 실제 실행한 출력에 근거한다
    - 전부 GREEN 이면 SUMMARY 에 「WR-03 재현되지 않음 — 코드 무변경, 회귀 가드만 추가」가 있고 `git diff <이 플랜 시작 전 HEAD>..HEAD -- relay/src/ws/order-handler.ts` 가 0줄이다
  </acceptance_criteria>
  <done>WR-03 이 실재하는지 수정 전 코드로 판정됐고, 그 판정이 테스트와 SUMMARY 에 남았다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: (Task 1 에서 RED 가 있을 때만) `PendingOrder.kind` + 하드 필터 3종</name>
  <reversibility rating="costly">실계좌 통보 매칭 규칙 — 되돌리면 교차 오정산·양쪽 timeout 이 돌아오고, 규칙이 바뀐 사이의 감사 행 정산 결과는 되돌릴 수 없다. 체크포인트는 두지 않되(되돌리기 자체는 코드 한 곳) 무게를 표시한다.</reversibility>
  <files>relay/src/ws/order-handler.ts, relay/tests/ws-order.test.ts</files>
  <read_first>
    - relay/src/ws/order-handler.ts (Task 1 의 read_first 와 같은 구간 — 특히 `narrowPending` 머리 주석의 GC-CR-01 「실어 온 축만 하드 필터」 문단)
    - relay/tests/ws-order.test.ts (Task 1 결과 · `mkPending` 헬퍼)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-19-SUMMARY.md (Task 1 의 RED 케이스 목록 — 이 태스크의 범위)
  </read_first>
  <action>
**Task 1 이 전부 GREEN 이었으면 이 태스크를 실행하지 않는다.** RED 인 케이스만 목표로 한다.

① `PendingOrder` 에 `kind: OrderType`("N"·"M"·"C") 필드를 더하고, 대기 등록(1022-1040)에서 `handle` 의 `kind` 를 그대로 싣는다. `isCancel` 은 이름과 의미(「원주문번호 축으로 매칭되는 대기」)를 그대로 둔다 — 필드 주석에 「요청 종류는 `kind` 가 정본, `isCancel` 은 원주문 참조 여부」를 적는다.

② `narrowPending` 의 하드 필터 단계에 세 축을 더한다 — 셋 다 **통보가 실제로 실어 온 값**에만 적용하는 하드 필터다(GC-CR-01):
- `noticeType` "C" → `kind === "C"` 대기만, "M" → `kind === "M"` 대기만(기존 `isCancelNotice` 묶음을 종류별로 가른다).
- `requestKind` 가 비어 있지 않으면 "New"→N · "Modify"→M · "Cancel"→C 로 매핑한 종류의 대기만. 모르는 값은 축을 적용하지 않는다(화이트리스트 — `NEW_ORDER_NOTICE_TYPES` 와 같은 규율, 부정 조건 금지).
- `noticeType` "E" → `kind === "N"` 대기만(체결은 정정·취소 요청의 답이 아니다).
세 축이 후보를 전부 지우면 기존 전용 warn 로그 경로로 간다 — 로그 필드에 어느 축이 적용됐는지(`kindAxisApplied` 같은 불리언)를 더하고 원문 주문번호·계좌는 싣지 않는다.

③ `mkPending` 기본값에 `kind: "N"` 을 더하고, 기존 취소 대기 픽스처(`isCancel:true`)를 쓰는 케이스에는 `kind:"C"`, 정정 케이스에는 `kind:"M"` 을 명시한다 — 기존 케이스의 기대값은 바꾸지 않는다(바꿔야 green 이 되는 기존 케이스가 있으면 멈추고 SUMMARY 에 그 사실과 이유를 적는다 — 그건 새 축이 정상 정산을 막는다는 신호다).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test -- ws-order</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 passed 수가 0 — Task 1 의 RED 케이스가 아직 실패한다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/relay run test</automated>
    <fails_when>non-zero exit, 또는 "error TS", 또는 relay 통과 수가 18-14 완료 시점보다 적다</fails_when>
  </verify>
  <acceptance_criteria>
    - Task 1 에서 RED 였던 케이스가 전부 green 이다
    - 기존 `narrowPending` describe 케이스(GC-CR-01 · ①~④ · R2-WR-03① · R2-IN-03 등)가 **기대값 무수정**으로 green 이다
    - `grep -c "kind" relay/src/ws/order-handler.ts` 증가분에 `PendingOrder` 필드 정의와 대기 등록 대입이 포함된다(두 줄 이상)
    - warn 로그 필드에 주문번호·계좌 원문이 없다(기존 ㉝ 케이스 green)
  </acceptance_criteria>
  <done>같은 원주문의 취소·정정이 서로의 통보로 정산되지 않고, 원주문의 체결이 정정·취소 요청을 정산하지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트웨이 51 통보 → relay 대기 정산 | 통보가 어느 요청의 답인지의 판정 — 틀리면 브라우저에 거짓 결과가 간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-85 | Spoofing | 취소↔정정 교차 정산 | high | mitigate | 통보 종류 C/M → 같은 `kind` 대기만(하드 필터) |
| T-18-86 | Spoofing | 원주문 체결이 정정·취소 대기를 정산 | high | mitigate | E 통보 → `kind` N 대기만 |
| T-18-87 | Denial of Service | 과도한 하드 필터로 정상 정산 실패(timeout) | medium | mitigate | 통보가 실어 온 값에만 적용 · `requestKind` 는 화이트리스트 · 기존 매칭 케이스 기대값 무수정 green 을 acceptance 로 고정 |
| T-18-88 | Information Disclosure | 축 전멸 warn 로그 | low | mitigate | 원문 주문번호·계좌 없이 축 적용 여부·후보 수만(㉝ 유지) |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 |
</threat_model>

## Artifacts this phase produces

- **relay:** `PendingOrder.kind`(재현 시) · `narrowPending` 요청 종류 하드 필터(C/M 통보 · `requestKind` · E 통보) · warn 로그 축 필드
- **테스트:** `ws-order.test.ts` describe 「WR-03 — 취소·정정 대기의 요청 종류」 (a)~(e) + ws 케이스 (f)
- **기록:** SUMMARY 의 수정 전 재현 판정 표

<verification>
- 수정 전 재현 판정이 SUMMARY 에 있다
- (재현 시) WR-03 케이스 전부 green · 기존 매칭 케이스 기대값 무수정 green · relay 전량 green
</verification>

<success_criteria>
- WR-03 이 실재하면 닫혔고, 실재하지 않으면 그 사실과 회귀 가드가 남았다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-19-SUMMARY.md` when done
</output>
