---
phase: 18-gh-trade-ui-nxt-vi
plan: 22
type: execute
wave: 12
depends_on: ["18-21", "18-17", "18-14"]
gap_closure: true
files_modified:
  - webapp/src/components/trading/workbench/trading-workbench.tsx
  - webapp/src/components/trading/__tests__/trading-workbench.test.tsx
  - webapp/e2e/specs/trading-workbench.spec.ts
  - webapp/src/lib/use-relay-socket.ts
  - webapp/src/lib/relay-provider.tsx
  - webapp/src/lib/__tests__/relay-socket.test.ts
autonomous: true
requirements: [TRADE-09]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WR-04 · D-13 · D-21: 공용 패널 미체결 행을 선택하면 그 행을 받을 카드(같은 ISIN ∧ 행의 거래소 ∧ 상태줄 계좌)가 **언제나** 격자에 있고 펼쳐져 화면에 들어온다 — 그런 카드가 없으면 행의 ISIN·거래소와 상태줄 계좌로 카드를 새로 붙인다(서버 송신 0). 「선택됨」 으로 강조됐는데 정정·취소 폼이 어디에도 없는 상태가 없다"
    - "다른 계좌·다른 거래소 카드에는 선택이 여전히 전달되지 않는다(다른 계좌 원주문번호로 정정·취소가 나가는 경로 0 — 18-11 결정 유지)"
    - "WR-07 확인 우선: 수정 전 코드로 「스냅샷 이후 찾지 못한 포커스 요청이 보류된 채 남아, 나중에 같은 키가 등록되면 사용자 조작 없이 카드가 펼쳐지고 스크롤된다」를 재현 테스트로 먼저 판정하고 SUMMARY 에 남긴다. 재현되지 않으면 코드를 바꾸지 않는다"
    - "WR-07 · D-02: 포커스 요청(사이드바 · `?focus=`)의 보류는 **첫 64 스냅샷 전에만** 허용한다. 스냅샷 이후의 미스는 버린다 — 보류 중이던 요청도 스냅샷이 그 키 없이 도착하면 버린다. 스냅샷 전 요청이 스냅샷에서 찾아지면 지금처럼 펼친다"
    - "「64 스냅샷을 받았는가」 는 relay 상태 `limitChaserSnapSeq`(lc.snap 적용 횟수 · 연결 전/리셋 0) 하나로 판정한다 — 상태(`status`)나 배열 비어 있음으로 추론하지 않는다(인증 ACK 가 lc.snap 보다 먼저 온다 · 빈 배열은 「전략 없음」도 된다)"
  artifacts:
    - webapp/src/components/trading/workbench/trading-workbench.tsx
    - webapp/src/lib/use-relay-socket.ts
  key_links:
    - "`SharedPanels onSelectUnfilled` → 작업대 `selectUnfilled` → (정확 일치 카드 | 새 카드) → `renderCard` 의 `selectedUnfilled` 전달 조건(544-551)"
    - "relay `lc.snap` 프레임 → 리듀서 `limitChaserSnapSeq` → 작업대 보류 판정"
  prohibitions:
    - "MUST NOT 선택 전달 조건(같은 ISIN ∧ 거래소 ∧ 카드 계좌 = 상태줄 계좌)을 느슨하게 한다 — 카드를 붙이는 쪽으로 해결한다"
    - "MUST NOT 미체결 선택으로 카드를 붙이면서 서버에 무언가를 보낸다(lc.set 등) — 카드 추가는 화면 상태다(D-07 과 같은 규율)"
    - "MUST NOT `?focus=` 를 마운트 1회 소비 규율(D-02 · T-18-53)에서 풀어 URL 변화마다 다시 읽는다"
---

<objective>
작업대의 선택·포커스 두 결함을 닫는다.

- WR-04(검증자 코드 대조로 확인됨): 미체결 행 선택은 `isin ∧ exchange ∧ accountNo` 가 모두 맞는 카드에만 폼을 연결한다(`trading-workbench.tsx:544-551`). 카드가 없는 종목, NXT 미체결인데 카드는 KRX, 카드 계좌가 상태줄과 다른 경우 — 행은 「선택됨」 인데 정정·취소 폼이 어디에도 없다. 급락 국면의 취소가 지연되는 UX 결함이다.
- WR-07(검증자 미재현 → 확인 우선): 사이드바 포커스 요청이 키를 못 찾으면 `pendingFocus.current = f` 로 영원히 보류한다(270-283). 삭제된 전략·오래된 링크의 키가 남았다가 나중에 같은 키가 등록되면 사용자 조작 없이 카드가 튀어나와 펼쳐지고 스크롤된다.

18-21 이 카드 정체성을 카드 id 로 옮겨 같은 ISIN 카드 둘이 가능해졌으므로, WR-04 는 「받을 카드가 없으면 붙인다」 한 규칙으로 닫힌다. **18-21 SUMMARY 가 WR-05 를 「재현되지 않음」으로 기록했다면**(정체성이 ISIN 그대로) — 같은 ISIN 카드가 이미 있는데 정확 일치가 아닐 때는 카드를 붙이지 말고 선택을 풀고(`setSelected(null)`) 공용 패널 바로 위 `role="status"` 한 줄로 「이 미체결은 {거래소} · 계좌 {계좌} 주문이에요 — 같은 종목 카드가 달라 여기서 정정할 수 없어요. 행의 「취소」 는 쓸 수 있어요」를 띄운다.

Purpose: TRADE-09 공용 패널 → 수동주문 정정/취소 동선이 끊기지 않는다.
Output: 선택 → 카드 보장 · 스냅샷 기준 포커스 보류 · 회귀(단위 + e2e GC4).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-REVIEW.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-11-SUMMARY.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-12-SUMMARY.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-21-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] WR-04 — 미체결 선택은 받을 카드를 보장한다 (없으면 붙인다)</name>
  <files>webapp/src/components/trading/workbench/trading-workbench.tsx, webapp/src/components/trading/__tests__/trading-workbench.test.tsx, webapp/e2e/specs/trading-workbench.spec.ts</files>
  <read_first>
    - webapp/src/components/trading/workbench/trading-workbench.tsx (18-21 결과 — `WorkbenchCard.id` · `addCard`/`focusCard` · 361-379 `selected`/`liveSelected`/`selectUnfilled` · 533-561 `renderCard` 의 선택 전달 조건)
    - webapp/src/components/trading/__tests__/trading-workbench.test.tsx (520-572 「미체결 행 선택 (D-21)」 describe · 18-21 이 바꾼 `StubCard`)
    - webapp/src/components/trading/workbench/shared-panels.tsx (140-165 행 선택 콜백 — 같은 행 재클릭은 해제)
    - webapp/e2e/specs/trading-workbench.spec.ts (938-1018 케이스 18 의 `pushAccountState` 미체결 주입 형식 · `sharedPanels` 조회구)
  </read_first>
  <behavior>
    - 카드 없는 ISIN Y 의 KRX 미체결 선택 → 카드가 1장 늘고 `data-key="Y:{상태줄계좌}:KRX"` · 펼침 · 그 카드에만 `selectedUnfilled` 가 내려간다 · `send` 0회
    - X 의 KRX 카드가 있는데 X 의 NXT 미체결 선택 → `X:{계좌}:NXT` 카드가 새로 붙고 선택은 그 카드에만 · KRX 카드의 `selectedUnfilled` 는 null
    - 정확 일치 카드가 있으면 카드가 늘지 않고 그 카드가 펼쳐진다(기존 케이스 그대로)
    - 같은 행 재클릭(해제) → 카드는 남고 선택만 풀린다
  </behavior>
  <action>
`selectUnfilled(row)` 를 「받을 카드 보장」 으로 바꾼다 (D-13 · D-21 · WR-04). `null` 이면 선택만 푼다. 행이면: 정확 일치 카드(같은 ISIN ∧ `c.exchange === row.exchange` ∧ `c.accountNo === 상태줄 계좌`)를 찾아 펼치고 스크롤한다. 없으면 18-21 의 카드 부여 규칙(`wb-card-{n}`)으로 **행의 ISIN · 행의 거래소 · 상태줄 계좌 · 펼침 · 행의 `name`/`code`** 카드를 붙이고 스크롤한다 — 서버에는 아무것도 보내지 않는다. 판정은 순수 함수 하나(`cardForUnfilled(cards, row, accountNo)` — 「정확 일치 카드 id | 새로 붙일 카드 모양」)로 두고 그 함수만 테스트한다(판정의 유일 지점 규율).

`renderCard` 의 선택 전달 조건(544-551)은 **바꾸지 않는다** — 이제 그 조건을 만족하는 카드가 언제나 있다는 것이 이 태스크의 결과다. 붙인 카드는 등록 전 카드이므로 거래소 토글·✕ 규칙이 그대로 적용되고, 18-21 의 키 충돌 규칙과 부딪치지 않는다(정확 일치가 없을 때만 붙이므로 그 키를 쓰는 카드가 없다).

18-21 이 WR-05 를 재현하지 못해 정체성이 ISIN 그대로라면 objective 의 대체 갈래(선택 거부 + 인라인 문구)로 구현하고 그 사실을 SUMMARY 에 적는다.

e2e 새 케이스 「GC4 카드 없는 종목의 미체결을 누르면 그 종목 카드가 붙어 펼쳐지고 수동주문에 원주문 칩이 선다 (WR-04 · D-21)」: 카드 0장 → `pushAccountState` 로 `E2E_LONG_NAME_ISIN` KRX 미체결 1건 → 공용 패널 「미체결」 탭에서 그 행 클릭 → 그 ISIN 카드 1장 `data-open="true"` → 카드의 「수동주문」 을 열어 `manual-order-selchip` 이 그 주문번호를 말한다 → 게이트웨이 요청 로그에 `SetLimitChaserReq` 증가 0.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- trading-workbench</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test:e2e -- trading-workbench -g "GC4"</automated>
    <fails_when>non-zero exit, 또는 출력에 "No tests found", 또는 "failed" 가 1 이상</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 「카드 없는 종목 선택 → 카드 추가 · 선택 전달 · 송신 0」 케이스가 green 이고 수정 전 코드에서 실패한다(RED 한 줄)
    - `cardForUnfilled` 가 export 되고 정확 일치 · 새 카드 두 갈래를 단위로 단언한다
    - 기존 「행을 누르면 같은 종목·계좌·거래소 카드의 본문에만 내려가고 …」 케이스가 무수정 green
    - e2e GC4 green
  </acceptance_criteria>
  <done>미체결 행을 누르면 그 주문을 정정·취소할 카드가 언제나 화면에 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-07 확인 → (재현 시) 포커스 보류는 첫 64 스냅샷 전에만</name>
  <files>webapp/src/components/trading/__tests__/trading-workbench.test.tsx, webapp/src/lib/use-relay-socket.ts, webapp/src/lib/relay-provider.tsx, webapp/src/lib/__tests__/relay-socket.test.ts, webapp/src/components/trading/workbench/trading-workbench.tsx</files>
  <read_first>
    - webapp/src/components/trading/workbench/trading-workbench.tsx (223-283 `pendingFocus`·등록 전략 유입 효과·`useTradingFocusRequest` 처리)
    - webapp/src/components/trading/__tests__/trading-workbench.test.tsx (387-428 「사이드바 포커스 요청」 describe — 특히 「스냅샷 전에 온 요청은 등록 전략이 보이는 순간 펼친다」)
    - webapp/src/lib/use-relay-socket.ts (236-300 상태 타입 주석 · 380-440 `RelayData`/`INITIAL_DATA`/`reset` · 505-523 `lc`/`lc.snap` 리듀서 · 1100-1120 상태 매핑)
    - webapp/src/lib/relay-provider.tsx (242-278 `EMPTY_RELAY_VALUE`)
    - webapp/src/lib/__tests__/relay-socket.test.ts (프레임 주입·상태 읽기 하네스)
    - relay/src/ws/fanout.ts (578-600 — 인증 ACK(state) 가 lc.snap 보다 먼저, lc.snap 은 빈 배열도 1프레임)
  </read_first>
  <behavior>
    - (확인) 스냅샷 이후(키 K 없음) 사이드바 요청 K → 카드 없음 → 나중에 K 가 60 으로 등록 → **카드는 접힌 채 들어오고 펼쳐지지 않는다**(수정 전: 펼쳐짐 = RED)
    - (확인) 스냅샷 이후 마운트 · `?focus=K`(K 없음) → 나중에 K 등록 → 접힌 채(수정 전: 펼쳐짐 = RED)
    - 스냅샷 전 요청 K → 스냅샷에 K 있음 → 펼침(기존 케이스 그대로)
    - 스냅샷 전 요청 K → 스냅샷에 K 없음 → 보류 폐기 → 나중에 K 등록 → 접힌 채
    - 리듀서: `lc.snap` 적용마다 `limitChaserSnapSeq` +1 · `lc`(60) 는 불변 · 리셋 후 0
  </behavior>
  <action>
**확인 먼저 (WR-07 · 검증자 미재현).** `trading-workbench.test.tsx` 에 describe 「WR-07 — 스냅샷 이후의 포커스 미스는 보류하지 않는다」 를 쓰고 `<behavior>` (확인) 2케이스를 **수정 전 코드로** 돌려 RED/GREEN 을 SUMMARY 표에 적는다. 이 두 케이스는 스냅샷 여부를 모킹 relay 의 새 필드 `limitChaserSnapSeq` 로 표현하되(수정 전 코드는 이 필드를 읽지 않으므로 판정에 영향 없음), 기대값은 「올바른 동작」으로 쓴다. 전부 GREEN 이면 WR-07 은 「재현되지 않음」 으로 기록하고 아래 구현을 건너뛴다(필드 모킹 인자를 지우고 테스트를 회귀 가드로 남긴다).

**재현 시 구현:**
- `use-relay-socket.ts`: `RelayData`·`RelayConnectionState` 에 `limitChaserSnapSeq: number` 를 더한다 — JSDoc 은 `rateCrossSnapSeq` 와 같은 형식으로 「64 스냅샷(lc.snap)을 적용한 횟수 · 0 = 이 연결에서 아직 못 받음 · 값이 아니라 바뀌었는가/0 인가만 읽는다 · 인증 ACK 가 lc.snap 보다 먼저 오므로 `status` 로 추론하지 않는다」. `INITIAL_DATA` 0, `lc.snap` 리듀서에서 +1, 상태 매핑에 통과. `relay-provider.tsx` `EMPTY_RELAY_VALUE` 에 0(「Provider 밖 = 받은 적 없다」 주석).
- `trading-workbench.tsx`: 등록 전략 유입 효과와 사이드바 요청 처리에서 키를 못 찾았을 때 `limitChaserSnapSeq > 0` 이면 보류하지 않고 버린다. 유입 효과가 스냅샷 이후 돌 때 보류 중인 키가 목록에 없으면 그 보류도 버린다(효과 의존성에 `limitChaserSnapSeq` 추가). `?focus=` 의 마운트 1회 소비(D-02 · T-18-53)는 그대로다.
- `relay-socket.test.ts` 에 리듀서 3케이스, `trading-workbench.test.tsx` 에 `<behavior>` 나머지 2케이스.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- trading-workbench relay-socket relay-provider</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test:e2e -- sidebar-tree trading-workbench</automated>
    <fails_when>non-zero exit, 또는 출력에 "No tests found", 또는 "failed" 가 1 이상 — 사이드바 → 작업대 포커스 왕복(18-12)이 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 수정 전 코드로 실행한 (확인) 2케이스의 RED/GREEN 표가 있다
    - (재현 시) `grep -c "limitChaserSnapSeq" webapp/src/lib/use-relay-socket.ts webapp/src/lib/relay-provider.tsx webapp/src/components/trading/workbench/trading-workbench.tsx` 가 세 파일 모두 1 이상
    - 기존 「스냅샷 전에 온 요청은 등록 전략이 보이는 순간 펼친다」 · 「?focus= … 뒤로가기 다시 덮지 않는다」 케이스가 무수정 green
    - `sidebar-tree.spec.ts` 전량 · `trading-workbench.spec.ts` 전량 green
  </acceptance_criteria>
  <done>오래된 포커스 요청이 나중에 카드를 저절로 펼치는 일이 없다 — 스냅샷 전 요청은 지금처럼 산다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 공용 패널 선택 → 수동주문 정정/취소 | 선택된 원주문번호가 어느 카드의 폼으로 가는가 — 계좌·거래소가 어긋나면 남의 주문을 건드린다 |
| URL `?focus=` / 사이드바 이벤트 → 카드 펼침 | 신뢰할 수 없는 문자열(키 원문) — `parseStrategyKey` 로만 해석 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-96 | Elevation of Privilege | 다른 계좌 카드로 선택 전달 | high | mitigate | 전달 조건(ISIN ∧ 거래소 ∧ 상태줄 계좌) 무변경 · 부족하면 카드를 **상태줄 계좌로** 붙인다 |
| T-18-97 | Denial of Service | 선택됐는데 폼이 없음(취소 지연) | medium | mitigate | `cardForUnfilled` 로 받을 카드를 보장 · 펼치고 스크롤 |
| T-18-98 | Tampering | 오래된 포커스 키의 지연 발동 | low | mitigate | 보류는 첫 64 스냅샷 전에만 · `limitChaserSnapSeq` 한 신호 |
| T-18-99 | Tampering | 미체결 선택이 서버 전략 생성으로 이어짐 | medium | mitigate | 카드 추가는 화면 상태 — 송신 0 을 단위·e2e 로 단언 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 |
</threat_model>

## Artifacts this phase produces

- **webapp 컴포넌트:** `cardForUnfilled`(trading-workbench export 순수 함수) · 선택 → 카드 보장 동작
- **webapp lib:** `RelayConnectionState.limitChaserSnapSeq`(재현 시) · `EMPTY_RELAY_VALUE.limitChaserSnapSeq`
- **테스트:** `trading-workbench.test.tsx` WR-04 4 · describe 「WR-07 — 스냅샷 이후의 포커스 미스는 보류하지 않는다」 · `relay-socket.test.ts` 리듀서 3 · e2e `GC4`

<verification>
- 미체결 선택 = 받을 카드 보장(단위 + GC4)
- WR-07 재현 판정 기록 · (재현 시) 스냅샷 이후 미스 폐기
- 사이드바·작업대 e2e 전량 green
</verification>

<success_criteria>
- 「선택됨 + 폼 없음」 상태가 사라졌다
- 오래된 포커스 키가 사용자 조작 없이 카드를 펼치지 않는다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-22-SUMMARY.md` when done
</output>
