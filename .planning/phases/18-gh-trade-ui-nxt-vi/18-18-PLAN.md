---
phase: 18-gh-trade-ui-nxt-vi
plan: 18
type: execute
wave: 9
depends_on: ["18-14"]
gap_closure: true
files_modified:
  - supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql
autonomous: false
requirements: [TRADE-07]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "시간외종가(G2/G3) 원주문의 취소가 웹의 4겹(webapp 번역기 · relay zod · 조립기 · DB CHECK) 을 전부 통과한다 — D-21 설계가 정정을 잠그고 유일한 조치로 「취소 후 재등록」을 안내하기 때문에, 이 취소 경로는 TRADE-07 의 필수 기능이다 (CR-01 · 이 플랜은 네 번째 겹 — 실 원격 DB)"
    - "D-21 · D-23: `[BLOCKING]` 원격 `dma_orders_price_check` 정의가 `price > 0 OR (price = 0 AND (order_type = 'C' OR krx_session IN ('G2','G3')))` 형태다 — 가격 0 취소 행 insert 가 원격에서 통과하고, 세션 없는 가격 0 신규·음수 가격은 여전히 거부된다"
    - "원격 public 스키마 전후 덤프 diff 가 `dma_orders_price_check` 한 줄 교체 외에 0줄이다 — 정책 0개(default-deny)·`service_role` GRANT·다른 CHECK·컬럼이 푸시 전후 같다"
    - "한쪽 문(one-way) 확인: 사람이 체크포인트에서 적용을 승인한 뒤에만 푸시한다 — 가격 0 취소 행이 한 번이라도 기록되면 CHECK 를 되조이는 데 감사 행 재작성이 필요하다"
  artifacts:
    - supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql
  key_links:
    - "원격 CHECK ↔ relay `order-handler` 의 insert 선행 규율 — 원격에 없으면 가격 0 취소는 게이트웨이 송신 **전에** 「주문 기록에 실패했습니다」로 끝난다(relay 단위 테스트는 DB 를 모킹하므로 이 결함을 잡지 못한다 · Pitfall 1)"
    - "배포 순서 DB → relay → webapp push — 이 플랜은 DB 단계만 한다"
  prohibitions:
    - "MUST NOT 체크포인트 승인 전에 `supabase db push` 를 실행한다"
    - "MUST NOT 이 phase 밖의 미적용 마이그레이션이 `--dry-run` 목록에 섞였는데 푸시한다 — 멈추고 보고한다"
    - "MUST NOT 기존 Supabase 자격증명을 사용자에게 다시 묻는다 — 18-03 과 같이 저장소 링크(`supabase/.temp/project-ref`)와 CLI 로그인 세션을 먼저 쓴다"
    - "MUST NOT 예상과 다른 원격 상태를 손으로 고쳐 밀어 넣는다 — 다음 마이그레이션 파일로 바로잡는 것이 이 저장소의 방식이다"
---

<objective>
`[BLOCKING]` 18-14 가 만든 `20260922120000_dma_orders_cancel_price_zero.sql` 을 **한쪽 문 확인 뒤** 실 원격 DB 에 반영하고, 18-03 과 같은 방법(원격 public 스키마 덤프 전후 diff)으로 반영 결과를 증명한다.

Purpose: CR-01 의 네 번째 겹. 코드 3층이 가격 0 취소를 받아도 원격 CHECK 가 그대로면 relay 는 insert 실패로 보내지 않는다 — 단위 테스트는 DB 를 모킹하므로 이 결함은 실계좌 첫 취소에서만 드러난다.
Output: 원격 `dma_orders_price_check` 재정의 + 반영 증거(migration list · 덤프 diff 원문).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-03-PLAN.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-03-SUMMARY.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-14-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: [한쪽 문] 원격 `dma_orders_price_check` 를 취소 가격 0 허용으로 바꿀지 확인</name>
  <read_first>
    - supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql (18-14 산출 — 사람에게 보여 줄 SQL 원문)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-03-SUMMARY.md (현행 원격 CHECK 정의 원문)
  </read_first>
  <decision>원격 Supabase(`ivdbzxgaapbmrxreyuht`) `dma_orders_price_check` 를 「취소 행은 가격 0 허용」으로 넓히는 마이그레이션을 지금 적용할 것인가</decision>
  <context>
CR-01: 시간외종가(G2/G3) 원주문은 `close_price_mode="zero"` 에서 가격 0 으로 접수되고, D-21 은 그 원주문의 정정을 잠그고 「취소 후 재등록」만 안내한다. 18-14 가 webapp 번역기 · relay zod · 조립기를 열었지만, 원격 CHECK(`price > 0 OR (price = 0 AND krx_session IN ('G2','G3'))`)는 취소 행(`krx_session` NULL)의 가격 0 을 거부한다.

적용될 변경은 한 줄이다 — `price > 0 OR (price = 0 AND (order_type = 'C' OR krx_session IN ('G2','G3')))`. 음수는 계속 거부, 세션 없는 가격 0 신규·정정도 계속 거부. 정책·권한·컬럼은 건드리지 않는다.

**되돌리기 비용(한쪽 문인 이유):** 적용 후 가격 0 취소 행이 한 번이라도 기록되면, CHECK 를 옛 정의로 되조이는 마이그레이션은 그 감사 행들을 고치지 않고는 실패한다. 감사 테이블 행 재작성은 이 저장소가 피하는 일이다.
  </context>
  <options>
    <option id="apply">
      <name>지금 적용 (권장)</name>
      <pros>가격 0 시간외종가 원주문의 취소가 실계좌에서 동작한다(4겹 전부 열림). 서버 설정을 `zero` 로 바꿔도 사용자가 살아 있는 주문을 웹에서 지울 수 있다</pros>
      <cons>한쪽 문 — 되조이려면 감사 행 재작성이 필요하다. 다만 취소의 가격은 게이트웨이가 판정에 쓰지 않는 원주문 사본이라 여는 것의 안전상 손실은 없다</cons>
    </option>
    <option id="defer">
      <name>보류</name>
      <pros>원격 스키마 무변경</pros>
      <cons>가격 0 취소는 relay 에서 「주문 기록에 실패했습니다」로 끝난다(18-14 이후 소켓은 끊기지 않지만 취소는 여전히 불가). 18-VERIFICATION truth #8 은 PARTIAL 로 남는다</cons>
    </option>
  </options>
  <acceptance_criteria>
    - 사람이 `apply` 또는 `defer` 중 하나를 명시적으로 골랐고, 그 선택이 SUMMARY 에 기록됐다
    - `defer` 면 Task 2·3 을 실행하지 않고 SUMMARY 에 「truth #8 PARTIAL — 원격 CHECK 미반영」을 남긴다
  </acceptance_criteria>
  <resume-signal>`apply` 또는 `defer` 를 입력</resume-signal>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] 전 덤프 → `--dry-run` → `supabase db push --yes`</name>
  <reversibility rating="one-way">원격 감사 테이블의 CHECK 완화 — 가격 0 취소 행이 기록되는 순간 되조이기에 감사 행 재작성이 필요하다(Task 1 이 사람 확인을 받았다).</reversibility>
  <precondition>Task 1 선택이 `apply` 이고, 저장소가 원격에 링크돼 있다(`supabase/.temp/project-ref` 존재) — 없으면 18-03 과 같은 순서(저장소 env · `gcloud secrets` · 이전 phase 기록)로 자격을 찾고, 그래도 없을 때만 인증 게이트로 멈춘다.</precondition>
  <files>supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql</files>
  <read_first>
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-03-SUMMARY.md (실제로 통한 명령 순서와 출력 형태 — `migration list --linked` · `db push --dry-run` · `db push --yes` · `db dump --linked -s public`)
    - supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql
  </read_first>
  <action>
18-03 이 실제로 통과한 순서를 그대로 따른다(D-21 · D-23). ① `supabase db dump --linked -s public` 으로 **푸시 전** 스냅숏을 저장소 밖 임시 경로에 저장한다(저장소에 커밋하지 않는다). ② `supabase migration list --linked` 로 Local/Remote 가 어긋난 행이 `20260922120000` 하나뿐인지 본다. ③ `supabase db push --dry-run` 출력이 이 파일 하나만 적용 대상으로 말하는지 확인한다 — 다른 미적용 마이그레이션이 섞이면 **멈추고 보고한다**. ④ `supabase db push --yes`. ⑤ 다시 `supabase migration list --linked` 로 Remote 열이 채워졌는지 본다.

CLI 가 억제할 수 없는 프롬프트나 인증 오류로 막히면 그 자리에서 멈추고 동적 인증 게이트로 올린다(시도한 명령과 응답 원문 포함, 토큰·비밀번호 값은 마스킹). 재시도로 두드리지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && supabase migration list --linked 2>&1 | grep -cE "20260922120000 +\| +20260922120000"</automated>
    <fails_when>카운트가 0 — Remote 열이 비어 있다(푸시가 일어나지 않았다), 또는 명령이 non-zero(인증/링크 실패 — 인증 게이트로 올린다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `--dry-run` 출력 원문(적용 대상 1건)이 SUMMARY 에 있다
    - `db push --yes` 출력 원문(「Applying migration 20260922120000_dma_orders_cancel_price_zero.sql…」·「Finished supabase db push.」)이 SUMMARY 에 있다
    - 자격은 기존 원천에서 해결했고 사용자에게 재요청한 기록이 없다(원천 이름만 기록, 값 없음)
  </acceptance_criteria>
  <done>원격 `dma_orders` 가 가격 0 취소 행을 받아들인다.</done>
</task>

<task type="auto">
  <name>Task 3: 반영 결과 조회 검증 — 전후 덤프 diff 가 CHECK 한 줄뿐</name>
  <files>supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql</files>
  <read_first>
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-03-SUMMARY.md (Task 2 검증 형식 — 제약 원문 · 정책 0개 · GRANT 불변 · 전체 diff 요약)
  </read_first>
  <action>
**푸시 후** `supabase db dump --linked -s public` 스냅숏을 떠 Task 2 의 전 스냅숏과 diff 한다(읽기 전용). 확인 항목: ① `dma_orders_price_check` 정의 원문에 `order_type` 과 `'C'` 가 있고 `"price" > 0` 갈래와 `krx_session` G2/G3 갈래가 그대로다 ② `dma_orders` 대상 `CREATE POLICY` 전 0 / 후 0 ③ `ENABLE ROW LEVEL SECURITY`·`service_role` GRANT 전후 동일 ④ 전체 diff 가 그 CHECK 한 줄 교체(+ 이 파일이 COMMENT 를 달았다면 그 1건) 뿐이다. 원문(CHECK 정의 1줄 · diff 요약)을 SUMMARY 에 붙인다. 하나라도 어긋나면 멈추고 보고한다 — 손으로 고치지 않는다.

`18-VALIDATION.md` 갱신은 이 플랜이 하지 않는다(18-23 이 갭 클로징 전체를 한 번에 기록한다).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && supabase db dump --linked -s public 2>/dev/null | grep "dma_orders_price_check" | grep -c "order_type"</automated>
    <fails_when>카운트가 0 — 원격 CHECK 에 취소 갈래가 없다(반영 안 됨), 또는 dump 가 아무것도 출력하지 않아 0</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 원격 `dma_orders_price_check` 정의 원문 1줄이 있다
    - SUMMARY 에 「정책 전 0 / 후 0 · GRANT 전후 동일 · 전체 diff = CHECK 1줄(+COMMENT)」 이 원문 근거와 함께 있다
    - 예상과 다른 항목이 있었다면 손으로 고치지 않고 보고했다
  </acceptance_criteria>
  <done>원격 DB 가 계약대로 바뀌었고, 바뀌지 않아야 할 접근 규칙은 그대로임이 원문으로 증명됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 개발자 워크스테이션 → Supabase 프로덕션 DB | 실 감사 테이블 제약을 바꾼다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-80 | Tampering | 의도치 않은 마이그레이션 동반 적용 | high | mitigate | `--dry-run` 으로 적용 목록 1건 확인, 섞이면 멈춤(Task 2 ③) |
| T-18-81 | Elevation of Privilege | RLS/GRANT 의도치 않은 변경 | high | mitigate | 전후 덤프 diff 로 정책 0 · GRANT 불변 확인(Task 3). 마이그레이션은 정책·권한 구문이 없다(18-14 acceptance) |
| T-18-82 | Tampering | CHECK 과완화 | medium | mitigate | 새 정의가 음수·세션 없는 가격 0 신규/정정을 여전히 거부함을 정의 원문으로 확인 |
| T-18-83 | Information Disclosure | 액세스 토큰·DB 비밀번호 | high | mitigate | 로그·SUMMARY 에 값 없음(원천 이름만). 새 비밀 생성 없음 |
| T-18-84 | Repudiation | 한쪽 문 | medium | mitigate | 사람 확인 체크포인트(Task 1) 뒤에만 푸시 · 선택을 SUMMARY 에 기록 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 |
</threat_model>

## Artifacts this phase produces

- **원격 DB 상태:** `public.dma_orders` 의 `dma_orders_price_check` 재정의(취소 가격 0 허용)
- **증거:** SUMMARY 의 `--dry-run`·`db push`·`migration list --linked` 출력 원문, 전후 덤프 diff 요약(덤프 파일 자체는 저장소 밖 임시 경로 — 커밋 안 함)

<verification>
- `supabase migration list --linked` 가 `20260922120000` 을 Remote 적용으로 보인다
- 원격 CHECK 정의 원문에 취소 갈래가 있다
- 정책·GRANT 불변
</verification>

<success_criteria>
- CR-01 네 겹 중 마지막(DB) 이 원격에서 열렸다 — 또는 사람이 `defer` 를 골라 그 사실이 기록됐다
- 이 phase 밖 스키마는 한 줄도 바뀌지 않았다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-18-SUMMARY.md` when done
</output>
