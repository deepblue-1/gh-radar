---
phase: 18-gh-trade-ui-nxt-vi
plan: 13
type: execute
wave: 7
depends_on: ["18-03", "18-12"]
files_modified:
  - webapp/e2e/specs/trading-workbench.spec.ts
  - webapp/e2e/specs/sidebar-tree.spec.ts
  - webapp/e2e/specs/a11y.spec.ts
  - webapp/e2e/specs/me.spec.ts
  - webapp/e2e/specs/orderbook.spec.ts
  - webapp/e2e/specs/trading-limit-chaser.spec.ts
  - webapp/e2e/specs/trading-vi.spec.ts
  - webapp/src/components/trading/vi-client.tsx
  - webapp/src/components/trading/limit-chaser-client.tsx
  - webapp/src/components/trading/vi-settings-card.tsx
  - .planning/phases/18-gh-trade-ui-nxt-vi/18-VALIDATION.md
autonomous: true
requirements: [TRADE-06, TRADE-07, TRADE-08, TRADE-09]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-29: 자동 게이트는 config `build_command`/`test_command` 전문 + Playwright 다. **카드 폭 4밴드 × 격자 1/2/3단 × 폰/와이드에서 잘림 0** 을 실브라우저로 단언한다 (TRADE-09)"
    - "D-29: 잘림 판정은 기존 `trading-limit-chaser.spec.ts` 의 `overflowX` 검사 헬퍼를 **재사용**한다 — 새 판정식을 쓰지 않는다 (TRADE-09)"
    - "D-02: `/trading/limit-chaser` · `/new` · `/trading/vi` 가 `/trading` 으로, `/trading/limit-chaser/{key}` 가 `/trading?focus={key}` 로 가는 것을 e2e 가 실제 네비게이션으로 단언한다 (TRADE-09)"
    - "D-07: 돌파 칩 클릭 → 카드 추가(KRX · 스위치 OFF) → 그 행 「거래중」 전환을 e2e 가 단언한다 (TRADE-06)"
    - "D-03: 사이드바 「트레이딩」 제목 링크 + 3단(KRX VI · NXT VI · 상따 종목)을 `sidebar-tree.spec.ts` 가 단언한다 (TRADE-09)"
    - "D-01: `/trading` 의 접근성 위반이 0 이다(`a11y.spec.ts` 의 기존 상따 케이스를 이 경로로 교체) (TRADE-09)"
    - "D-01: 더 이상 쓰이지 않는 `vi-client.tsx` · `limit-chaser-client.tsx` · `vi-settings-card.tsx` 와 대응 테스트가 제거되고, 그 파일들이 덮던 단언 중 새 컴포넌트 테스트로 옮겨지지 않은 것이 **없다** (TRADE-09)"
    - "D-05/D-08: `VI_EDIT_EXCHANGE` 와 개별 「상따」/「VI」 메뉴 상수가 저장소 전체에서 0건이다 (TRADE-08, TRADE-09)"
    - "D-28: 폰 밴드에서 더티 바(z-40)와 sticky 공용 패널(z-20)의 `boundingBox()` 가 겹치지 않음을 Playwright 가 단언한다 — z-index 로 덮는 해법 금지 (TRADE-09)"
    - "D-21: 실 게이트웨이 정정 왕복과 예약·시간외종가 실발주는 자동 게이트로 닫히지 않는 **실기 관측 항목**이다 — UAT 로 명시 인계하고 「테스트가 통과했으니 됐다」로 종결하지 않는다 (TRADE-07)"
    - "D-26: 18-VALIDATION.md 의 Per-Task Verification Map 의 Task ID·Status 열이 실제 플랜·태스크로 채워지고 Wave 0 항목이 전부 닫힌다 (TRADE-06, TRADE-07, TRADE-08, TRADE-09)"
  artifacts:
    - webapp/e2e/specs/trading-workbench.spec.ts
  key_links:
    - "e2e `overflowX` 헬퍼 ↔ 카드 4밴드 × 격자 3단 조합 — 이 조합이 §2.2b 이관이 실제로 작동하는지 증명하는 유일한 자리다"
    - "`18-VALIDATION.md` Per-Task Map ↔ 각 플랜의 자동 검증 명령 — 둘이 어긋나면 샘플링 연속성이 깨진다"
    - "삭제되는 옛 컴포넌트의 단언 ↔ 새 컴포넌트 테스트 — 옮겨지지 않은 단언이 곧 조용한 커버리지 손실이다"
  prohibitions:
    - "MUST NOT 잘림 판정식을 새로 쓴다 — 기존 `overflowX` 헬퍼를 재사용한다. 판정이 둘로 갈리면 한쪽만 고쳐진다"
    - "MUST NOT 실 게이트웨이 왕복이 필요한 항목을 mock 통과로 종결한다 — UAT 로 인계한다"
    - "MUST NOT 옛 컴포넌트를 지우면서 그것이 덮던 안전 단언을 함께 버린다"
    - "MUST NOT Playwright 포트를 가정한다 — webServer 와 relay 픽스처 포트는 `playwright.config.ts` 가 정본이다"
---

<objective>
Phase 18 을 **자동 게이트로 닫는다** — `/trading` 작업대 e2e 신설(리다이렉트 · 격자 단 수 · 카드 4밴드 잘림 0 · 칩 클릭 카드 추가 · 더티 바 겹침 0), 기존 e2e 재작성·교체, 더 이상 쓰이지 않는 옛 컴포넌트 제거, 그리고 전량 게이트 실행과 검증 계약 갱신.

Purpose: 이 phase 의 가장 큰 구조적 주장은 「§2.2b 4밴드의 측정 대상을 본문에서 카드로 옮겼다」이고, 그것은 **실브라우저에서 실제 폭으로만** 증명된다. 동시에 자동 게이트로 닫히지 않는 것(실 게이트웨이 정정 왕복 · 예약/시간외종가 실발주 · 자동재생 차단 표시)을 UAT 로 정직하게 인계한다.
Output: `trading-workbench.spec.ts` 신설 · 기존 spec 4종 갱신 · 옛 컴포넌트 3종 제거 · `18-VALIDATION.md` 갱신.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-VALIDATION.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `trading-workbench.spec.ts` 신설 — 리다이렉트 · 단 수 · 4밴드 잘림 0 · 칩 클릭</name>
  <files>webapp/e2e/specs/trading-workbench.spec.ts</files>
  <read_first>
    - webapp/e2e/specs/trading-limit-chaser.spec.ts (전문 — 특히 잘림 판정 `overflowX` 헬퍼, 케이스 9(390px 2열/탭)·11(본문 700 경계)·12(뷰포트 360·390·768·1023 램프)·13(컨테이너 832·880·960), relay 픽스처 기동 방식, 테스트 제목 관례 `'번호. 한국어 행동 서술 (근거 ID)'`)
    - webapp/playwright.config.ts (testDir · webServer 포트 · relay 픽스처 포트 — **포트를 가정하지 말 것**)
    - webapp/e2e/specs/trading-vi.spec.ts (VI 화면 케이스 — workbench 로 흡수할 것 선별)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md D-29
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §반응형 계약 · §UI Considerations 의 backstop 5행(E1 overflow · E6 overflow · E6 zero-one-many · E13 overflow · E15 overflow)
  </read_first>
  <behavior>
    - `/trading/limit-chaser` 로 이동하면 최종 URL 이 `/trading`.
    - `/trading/limit-chaser/new` → `/trading`.
    - `/trading/vi` → `/trading`.
    - `/trading/limit-chaser/{인코딩된 키}` → `/trading?focus={키}` 이고 해당 카드가 펼친 상태.
    - 단 수 세그먼트 1/2/3 을 누르면 격자 열 수가 바뀌고, 새로고침 후에도 유지된다.
    - 폰 뷰포트에서는 단 수 세그먼트가 접근성 트리에 없고 격자가 1단이다.
    - 카드 컨테이너 폭 699/700 · 829/830 · 991/992 와 한 스텝 양옆에서 §2.2b 밴드 전환이 일어나고, **각 폭에서 잘림이 0**.
    - 격자 1/2/3단 × 폰/와이드 조합에서 잘림이 0.
    - 펼침 0/1/2+ × 접힘 0/1/N 조합에서 스택이 한 칸을 차지하고 펼친 카드가 먼저 온다.
    - 돌파 칩을 클릭하면 카드가 1장 생기고 거래소가 KRX · 스위치가 전부 OFF · 그 행이 「거래중」으로 바뀐다.
    - 폰 밴드에서 더티 상태를 만들면 더티 바와 sticky 공용 패널의 `boundingBox()` 가 겹치지 않는다.
    - 상태줄 필이 폰 밴드에서 2줄로 wrap 하며 어느 필도 잘리지 않는다.
  </behavior>
  <action>
`webapp/e2e/specs/trading-workbench.spec.ts` 를 신설한다. `trading-limit-chaser.spec.ts` 를 본뜨되 **잘림 판정 헬퍼를 재사용한다** — 새 판정식을 쓰지 않는다(판정이 둘로 갈리면 한쪽만 고쳐진다). 헬퍼가 그 파일 안에 비공개로 있으면 공용 위치(`webapp/e2e/` 아래 헬퍼 모듈)로 승격하고 두 spec 이 같은 것을 쓰게 한다.

이식할 케이스는 기존 9·11·12·13 이고, 측정 대상만 「본문 폭」에서 「카드 컨테이너 폭」으로 바뀐다. 카드 폭을 직접 제어하려면 격자 단 수와 뷰포트 폭을 조합한다 — 3단 격자의 카드가 폰 밴드로 떨어지는 것이 D-12 가 말한 의도된 결과이므로, **그 조합이 정확히 어느 밴드로 가는지**를 단언한다.

추가 케이스 넷을 새로 쓴다 — ① 리다이렉트 4경로 ② 단 수 세그먼트 왕복 + 새로고침 유지 + 폰 밴드 DOM 부재 ③ 돌파 칩 클릭 → 카드 추가 → 「거래중」 ④ 폰 더티 바 vs 공용 패널 `boundingBox()` 겹침 0.

UI-SPEC 의 **backstop 5행이 이 spec 의 책임**이다 — E1 overflow(상태줄 wrap·잘림 0) · E6 overflow(카드 4밴드 × 단 수 잘림 0) · E6 zero-one-many(펼침/접힘 조합 스택) · E13 overflow(폰 패널 겹침 0) · E15 overflow(더티 바 겹침 0). 각 케이스 제목에 어느 backstop 을 닫는지 근거 ID 를 적는다.

테스트 제목 관례는 `'번호. 한국어 행동 서술 (근거 ID)'` 다. relay 픽스처와 webServer 포트는 `playwright.config.ts` 를 읽어서 쓰고 **가정하지 않는다**.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test:e2e -- trading-workbench</automated>
    <fails_when>non-zero exit, 또는 출력에 "0 passed", 또는 "No tests found"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "overflowX" webapp/e2e/specs/trading-workbench.spec.ts webapp/e2e/*.ts 2>/dev/null | grep -v ":0$" | wc -l</automated>
    <fails_when>출력이 0 — 잘림 판정 헬퍼를 쓰지 않고 새 판정식을 썼거나 판정 자체가 없다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -cE "boundingBox\(" webapp/e2e/specs/trading-workbench.spec.ts</automated>
    <fails_when>카운트가 2 미만 — 더티 바와 공용 패널 두 요소의 상자를 재 겹침을 단언하지 않았다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"(webapp typecheck 는 `tsconfig.e2e.json` 까지 본다)</fails_when>
  </verify>
  <acceptance_criteria>
    - 리다이렉트 4경로가 실제 네비게이션으로 단언된다
    - 카드 폭 경계 6지점(699/700 · 829/830 · 991/992)에서 밴드 전환 + 잘림 0 이 단언된다
    - 격자 1/2/3단 × 폰/와이드 조합 잘림 0 이 단언된다
    - 펼침/접힘 조합 스택 구조가 단언된다
    - 돌파 칩 클릭 → 카드 추가 → 「거래중」 이 단언된다
    - 더티 바 vs 공용 패널 겹침 0 이 두 `boundingBox()` 로 단언된다
    - 잘림 판정 헬퍼가 기존 것과 **같은 하나**다
    - 각 케이스 제목에 근거 ID 가 있다
  </acceptance_criteria>
  <done>§2.2b 이관과 격자 정렬이 실브라우저에서 증명됐다.</done>
</task>

<task type="auto">
  <name>Task 2: 기존 e2e 갱신 · 옛 컴포넌트 제거 (커버리지 손실 0)</name>
  <reversibility rating="costly">D-01 — 옛 화면의 마지막 잔재를 지우는 지점이다. 되돌리려면 이 태스크의 커밋을 `git revert` 해 `vi-client.tsx`·`limit-chaser-client.tsx`·`vi-settings-card.tsx` 와 `trading-limit-chaser.spec.ts`·`trading-vi.spec.ts` 를 복원한 뒤 18-12 의 리다이렉트 커밋까지 이어서 되감아야 옛 화면이 다시 뜬다 — 삭제 커밋만 되돌리면 진입로가 없다.</reversibility>
  <files>webapp/e2e/specs/sidebar-tree.spec.ts, webapp/e2e/specs/a11y.spec.ts, webapp/e2e/specs/me.spec.ts, webapp/e2e/specs/orderbook.spec.ts, webapp/e2e/specs/trading-limit-chaser.spec.ts, webapp/e2e/specs/trading-vi.spec.ts, webapp/src/components/trading/vi-client.tsx, webapp/src/components/trading/limit-chaser-client.tsx, webapp/src/components/trading/vi-settings-card.tsx</files>
  <read_first>
    - webapp/e2e/specs/sidebar-tree.spec.ts (현행 2단 「상따」·「VI」 항목 단언)
    - webapp/e2e/specs/a11y.spec.ts (상따 경로 위반 0 케이스가 있는 지점)
    - webapp/e2e/specs/me.spec.ts (전략 현황 카드 링크 대상 단언)
    - webapp/e2e/specs/orderbook.spec.ts (18-10 이 SUMMARY 에 남긴 「바뀐 셀렉터 목록」과 대조)
    - webapp/e2e/specs/trading-limit-chaser.spec.ts · trading-vi.spec.ts (삭제/흡수 판단 대상 — 어느 단언이 workbench 로 옮겨졌고 어느 것이 남았는지)
    - webapp/src/components/trading/__tests__/vi-client.test.tsx · limit-chaser-client.test.tsx · vi-settings-card.test.tsx (삭제 전에 덮던 단언 목록)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-05-SUMMARY.md · 18-06-SUMMARY.md · 18-08-SUMMARY.md · 18-10-SUMMARY.md (앞선 플랜들이 남긴 인계 목록)
  </read_first>
  <action>
**먼저 커버리지 대조표를 만든다.** 삭제 후보 3파일(`vi-client.tsx` · `limit-chaser-client.tsx` · `vi-settings-card.tsx`)과 그 테스트, 그리고 e2e 2파일(`trading-limit-chaser.spec.ts` · `trading-vi.spec.ts`)이 **현재 덮고 있는 단언**을 열거하고, 각각을 새 컴포넌트 테스트 또는 `trading-workbench.spec.ts` 의 어느 케이스가 이어받는지 매핑한다. **이어받는 곳이 없는 단언이 하나라도 있으면 지우지 말고 먼저 옮긴다.** 이 대조표를 SUMMARY 에 싣는다.

그다음 갱신한다:
- `sidebar-tree.spec.ts`: 2단 「상따」·「VI」 단언을 「트레이딩」 제목 링크 + 3단(KRX VI · NXT VI · 등록 전략)으로 재작성한다.
- `a11y.spec.ts`: 상따 경로 위반 0 케이스를 `/trading` 위반 0 으로 교체한다.
- `me.spec.ts`: 전략 현황 카드 링크 대상 단언을 `/trading(?focus=)` 로 갱신한다(카드 구조 단언은 그대로 — 구조는 안 바뀐다).
- `orderbook.spec.ts`: 18-10 이 남긴 「바뀐 셀렉터 목록」에 따라 갱신한다. 목록에 없는 셀렉터가 깨지면 **그것은 의도치 않은 변경**이므로 보고한다.
- `trading-limit-chaser.spec.ts` · `trading-vi.spec.ts`: 대조표에서 전부 이어받았음이 확인되면 삭제한다. 남은 단언이 있으면 그 케이스만 `trading-workbench.spec.ts` 로 옮긴 뒤 삭제한다.

마지막으로 옛 컴포넌트 3파일과 그 테스트를 삭제한다. 삭제 후 `grep -rn` 으로 남은 import 가 0건임을 확인한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -rq "vi-client\|vi-settings-card\|limit-chaser-client" webapp/src webapp/e2e</automated>
    <fails_when>non-zero exit — 삭제한 모듈을 아직 import 하는 곳이 있다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -rq "VI_EDIT_EXCHANGE" webapp/src</automated>
    <fails_when>non-zero exit — 고정 거래소 상수가 아직 남아 있다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test:e2e -- sidebar-tree</automated>
    <fails_when>non-zero exit, 또는 출력에 "0 passed" 또는 "No tests found"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test:e2e -- a11y</automated>
    <fails_when>non-zero exit, 또는 출력에 "0 passed" 또는 "No tests found"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 커버리지 대조표가 SUMMARY 에 있고, 이어받는 곳이 없는 단언 항목이 0 이다
    - 삭제한 모듈을 import 하는 코드가 0건이다
    - 고정 거래소 상수가 저장소에 0건이다
    - `sidebar-tree` · `a11y` e2e 가 새 구조로 통과한다
    - `orderbook.spec.ts` 가 18-10 목록 밖의 셀렉터 변경 없이 통과한다(있으면 보고)
    - webapp 단위 테스트가 전량 통과한다
  </acceptance_criteria>
  <done>옛 화면의 잔재가 저장소에서 사라졌고, 그것이 지키던 단언은 하나도 잃지 않았다.</done>
</task>

<task type="auto">
  <name>Task 3: 전량 게이트 실행 · `18-VALIDATION.md` 갱신 · UAT 인계</name>
  <files>.planning/phases/18-gh-trade-ui-nxt-vi/18-VALIDATION.md</files>
  <read_first>
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-VALIDATION.md (Per-Task Verification Map 의 TBD 열 · Wave 0 Requirements 체크리스트 · Manual-Only Verifications 표 · Validation Sign-Off 체크리스트)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-01-SUMMARY.md ~ 18-12-SUMMARY.md (각 플랜의 실제 태스크 ID 와 실행된 자동 검증 명령)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md §Environment Availability(실 게이트웨이 부재와 그 폴백)
  </read_first>
  <action>
**전량 게이트를 실행한다** — config `build_command` 전문, config `test_command` 전문, 그리고 Playwright 전량. 결과(각 워크스페이스 통과 수, Playwright pass/fail 수)를 SUMMARY 에 숫자로 남긴다. Phase 17 기준선은 relay 467 · webapp 1008 · shared 108 · Playwright 135 pass / 0 fail 이고, 이 phase 는 그보다 **늘어야** 한다(새 테스트가 추가됐으므로).

**`18-VALIDATION.md` 를 갱신한다** — Per-Task Verification Map 의 `Task ID`·`Plan`·`Wave`·`Status` 열을 실제 플랜·태스크로 채우고, `File Exists` 열의 ❌ W0 표기를 실제 상태로 바꾼다. Wave 0 Requirements 체크리스트 7항목을 전부 닫는다(`trading-limit-chaser.spec.ts`·`trading-vi.spec.ts` 의 「삭제 또는 흡수」는 Task 2 의 결정으로 확정된다). frontmatter 의 `status` 를 `validated` 로, `wave_0_complete` 를 `true` 로, `nyquist_compliant` 를 실제 충족 여부에 따라 설정한다 — **충족하지 않으면 `false` 로 두고 그 이유를 적는다.** 통과한 척하지 않는다.

**UAT 로 인계한다.** Manual-Only Verifications 표의 3항목이 이 phase 에서 자동 게이트로 닫히지 않는다는 사실을 SUMMARY 에 명시 기록한다 — ① 실 게이트웨이 정정(`order_type "M"`) 왕복과 통보 `notice_type "M"` 표시(장 시간 08:00~20:00 KST · VPN·터널 필요, mock 은 프레임 형태만 단언) ② 예약구간 조각 발주와 시간외종가 G2/G3 실발주(서버가 창을 판정하므로 mock 으로는 라벨·프레임만 검증) ③ 알림음 자동재생 차단 상태 표시(브라우저 autoplay 정책은 실브라우저 사용자 제스처 의존). Phase 17 D-25 와 같은 성격의 실기 관측 이연 항목이며, 「테스트가 통과했으니 됐다」로 종결하지 않는다.

UI-SPEC 의 **backstop 6행**(E3 error · E1 overflow · E6 overflow · E6 zero-one-many · E13 overflow · E15 overflow) 중 Task 1 이 닫은 것과 남은 것(E3 error — 확인 체크 전송 거부/타임아웃 왕복)을 구분해 SUMMARY 에 적는다. 남은 것은 UAT 항목이다.

**배포는 이 플랜에서 하지 않는다.** 이 저장소에서 `git push` 는 곧 webapp 프로덕션 배포이고, 배포 순서는 relay 먼저 → 검증 → push 다. relay 계약이 바뀌었으므로(18-01) webapp 을 먼저 올리면 zod 가 새 필드를 조용히 버린다. 배포 계획과 그 순서를 SUMMARY 에 명시하고 사용자 승인 대상으로 남긴다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build &amp;&amp; pnpm --filter @gh-radar/relay run typecheck &amp;&amp; pnpm --filter @gh-radar/relay run typecheck:tests &amp;&amp; pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run test &amp;&amp; pnpm --filter @gh-radar/webapp run test</automated>
    <fails_when>non-zero exit, 또는 relay 통과 수가 467 미만, 또는 webapp 통과 수가 1008 미만</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test:e2e</automated>
    <fails_when>non-zero exit, 또는 요약에 "failed" 가 0 이 아니거나 통과 수가 135 미만</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -q "TBD" .planning/phases/18-gh-trade-ui-nxt-vi/18-VALIDATION.md</automated>
    <fails_when>non-zero exit — Per-Task Map 에 미기입 칸이 남아 있다</fails_when>
    <human-check>`18-VALIDATION.md` 의 `nyquist_compliant` 값이 실제 충족 상태와 일치하는지, 그리고 Manual-Only 3항목이 UAT 로 인계됐는지 확인한다. 자동 게이트가 green 이라는 사실이 실 게이트웨이 정정 왕복을 대신하지 않는다.</human-check>
  </verify>
  <acceptance_criteria>
    - config `build_command`·`test_command` 전문과 Playwright 전량이 exit 0 이고 통과 수가 SUMMARY 에 숫자로 기록된다
    - `18-VALIDATION.md` 에 미기입 칸이 0 이고 Wave 0 체크리스트 7항목이 닫혔다
    - frontmatter `status`·`wave_0_complete`·`nyquist_compliant` 가 실제 상태를 말한다(통과한 척 없음)
    - Manual-Only 3항목과 남은 backstop 1항목이 UAT 인계 목록으로 SUMMARY 에 있다
    - 배포 순서(relay 먼저 → 검증 → push)가 SUMMARY 에 있고 이 플랜에서 배포하지 않았다
  </acceptance_criteria>
  <done>Phase 18 의 자동 게이트가 전부 green 이고, 자동으로 닫히지 않는 것이 무엇인지 문서에 정직하게 남았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e 실브라우저 → dev 서버 · relay 픽스처 | 테스트 환경 경계. 실 게이트웨이는 여기 없다 |
| 저장소 → 프로덕션 | `git push` 가 곧 webapp 배포다(이 플랜은 배포하지 않는다) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-62 | Repudiation | 커버리지 손실 은폐 | high | mitigate | 삭제 전 커버리지 대조표를 만들고 이어받는 곳이 없는 단언 0 을 acceptance 로 건다 |
| T-18-63 | Spoofing | mock 통과를 실기 검증으로 오인 | critical | mitigate | Manual-Only 3항목을 UAT 로 명시 인계. `nyquist_compliant` 를 실제 상태로 기록 |
| T-18-64 | Tampering | 배포 순서 역전(webapp 먼저) | critical | mitigate | 이 플랜에서 배포하지 않는다. zod 가 미지의 키를 조용히 버리므로 relay 먼저가 계약이다 |
| T-18-65 | Tampering | 잘림 판정식 분기 | medium | mitigate | 기존 `overflowX` 헬퍼 재사용을 verify 가 grep 으로 확인 |
| T-18-66 | Denial of Service | e2e 포트 가정 | low | mitigate | `playwright.config.ts` 를 읽어서 쓴다. 포트를 상수로 박지 않는다 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 phase 는 새 패키지를 설치하지 않는다(RESEARCH §Package Legitimacy Audit 표 비어 있음). 설치 태스크 없음 → legitimacy 체크포인트 없음 |
</threat_model>

## Artifacts this phase produces

새로 만드는 심볼·파일 (phase 전체 — 드리프트 검증에서 제외되어야 한다):

- **마이그레이션:** `supabase/migrations/20260921120000_dma_orders_modify_offhours.sql` · 컬럼 `dma_orders.piece_count` · `dma_orders.krx_session`
- **shared:** `RelayOrderModifyMsg` · `RelayKrxSession` · `OrderType` 의 `"M"` 멤버 · `RelayOrderNewMsg.pieceCount?` · `RelayOrderNewMsg.krxSession?` · `RelayRateCrossItem.name?` · `RelayRateCrossItem.code?`
- **relay:** `RelayOrderModifySchema` · `OrderInsertRow.pieceCount?` · `OrderInsertRow.krxSession?` · 돌파 항목 보강 private 메서드 · `relay/src/ws/__tests__/protocol.test.ts`
- **webapp lib:** `RelayOrderRequest.kind` 의 `"modify"` · `queued-window.ts`(`OrderButtonMode`·`ManualOrderAffordance`·`affordanceOf`) · `breakout-list.ts`(`kstDateKey`·`shouldRemoveBreakout`·`breakoutRowsFrom`·`BreakoutRow`·키 상수 4종·`REMOVE_MARGIN_PCT`·`ARM_GRACE_MS`·`HIGHLIGHT_MS`) · `use-breakout-quotes.ts`(`useBreakoutQuotes`·`MAX_BREAKOUT_SUBS`) · `alert-tone.ts`(`playBreakoutTone`·`isTonePlaybackBlocked`·`resumeToneContext`) · `limit-chaser.ts` 로 이동한 `parseStrategyKey` · 작업대로 옮긴 `useLeaveWarning`
- **webapp 컴포넌트:** `components/trading/workbench/` 의 `TradingWorkbench`·`WorkbenchStatusBar`·`ViSettingsRows`·`ViTriggerStrip`·`BreakoutStrip`·`StockAddBar`·`CardGrid`·`SharedPanels` · `components/trading/card/` 의 `StrategyCard`·`CardHeader`·`QuoteGrid10`·`CardBody`·`ManualOrderForm`(+`canModify`)·`StockInfoModal` · `LimitChaserForm` 의 `dirtyHint?` prop · `AccountPanel` 의 `onSelectUnfilled?`/`selectedOrderNo?` prop · 링크로 승격된 `GroupHeading`
- **라우트:** `webapp/src/app/trading/page.tsx` · 리다이렉트로 바뀐 기존 라우트 4개
- **CSS 토큰:** `--new-bg` · `--new-bd` (globals.css §9 라이트/다크 양쪽) · 컨테이너 이름 `wb`
- **e2e:** `webapp/e2e/specs/trading-workbench.spec.ts` · 승격된 잘림 판정 헬퍼

<verification>
- config `build_command` 전문 exit 0
- config `test_command` 전문 exit 0 (relay ≥467 · webapp ≥1008)
- `pnpm --filter @gh-radar/webapp run test:e2e` 전량 pass · fail 0 (≥135)
- `18-VALIDATION.md` 미기입 칸 0
</verification>

<success_criteria>
- `/trading` 이 실브라우저에서 4밴드 × 3단 × 폰/와이드 조합 전부에서 잘림 0
- 옛 화면·옛 spec 이 제거되고 커버리지 손실이 0
- 자동으로 닫히지 않는 항목(실 게이트웨이 정정·예약/시간외종가 실발주·자동재생 차단 표시·VI 확인 거부 왕복)이 UAT 로 인계됐다
- 배포는 하지 않았고 순서(relay 먼저)가 기록됐다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-13-SUMMARY.md` when done
</output>
