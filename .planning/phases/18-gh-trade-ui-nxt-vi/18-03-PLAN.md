---
phase: 18-gh-trade-ui-nxt-vi
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - supabase/migrations/20260921120000_dma_orders_modify_offhours.sql
autonomous: true
requirements: [TRADE-07]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-21: `[BLOCKING]` — `supabase db push` 로 `20260921120000_dma_orders_modify_offhours.sql` 가 실 데이터베이스에 반영되어, `dma_orders.order_type` 이 `'M'` 을 받아들인다. 이 푸시 없이는 relay 가 게이트웨이 송신 **전에** insert 실패로 정정을 자체 거부한다 (TRADE-07)"
    - "D-23: 푸시 후 `dma_orders.price` CHECK 가 `price > 0 OR krx_session IN ('G2','G3')` 형태이고, 감사 컬럼 `piece_count`·`krx_session` 이 실제로 존재한다 (TRADE-07)"
    - "D-27: 마이그레이션이 기존 RLS 정책·REVOKE 문을 바꾸지 않았고, `dma_orders` 의 접근 규칙은 푸시 전후가 같다 (TRADE-07)"
  artifacts:
    - supabase/migrations/20260921120000_dma_orders_modify_offhours.sql
  key_links:
    - "마이그레이션 적용 ↔ relay `order-handler` 의 insert 선행 규율 — 제약이 실 DB 에 없으면 정정·시간외종가가 「주문 기록에 실패했습니다」로만 끝난다(단위 테스트는 DB 를 모킹하므로 이 결함을 잡지 못한다)"
  prohibitions:
    - "MUST NOT 마이그레이션 없이 정정(`order_type 'M'`)이나 시간외종가(`price 0`)를 실 경로로 내보낸다 — 감사 기록이 없는 주문이 되거나, 사용자에게 원인 불명 실패로 보인다"
    - "MUST NOT 기존 Supabase 자격증명을 사용자에게 다시 묻는다 — 저장소 env 파일 · GCP Secret Manager · 이전 phase 기록을 먼저 확인하고, 그래도 없을 때만 인증 게이트를 올린다"
---

<objective>
`[BLOCKING]` 18-01 이 만든 `dma_orders` 마이그레이션을 **실 데이터베이스에 반영**하고 반영 결과를 조회로 확인한다.

Purpose: relay 는 게이트웨이에 보내기 **전에** `dma_orders` 에 insert 하고, 실패하면 보내지 않는다(감사 기록 없는 실주문을 만들지 않는 규율). 따라서 제약이 실 DB 에 없으면 정정과 시간외종가는 코드가 전부 옳아도 「주문 기록에 실패했습니다」로만 끝난다. relay 단위 테스트는 DB 를 모킹하므로 이 결함을 **잡지 못한다** — 스테이징/프로덕션 첫 정정에서만 드러난다.
Output: 실 DB 에 반영된 제약 3건(정정 허용 · 감사 컬럼 2개 · 조건부 가격) + 반영 증거.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] `supabase db push` — 마이그레이션 실 반영</name>
  <precondition>Supabase 접근 자격이 사용자에게 다시 묻지 않고 해결된다 — 저장소 `.env` 계열 파일, GCP Secret Manager(`gcloud secrets`), 이전 phase 기록 순으로 `SUPABASE_ACCESS_TOKEN`(및 링크된 프로젝트/DB 비밀번호)을 찾는다. 어느 원천에도 없으면 동적 인증 게이트 체크포인트로 올리고 이 태스크를 멈춘다.</precondition>
  <files>supabase/migrations/20260921120000_dma_orders_modify_offhours.sql</files>
  <read_first>
    - supabase/migrations/20260921120000_dma_orders_modify_offhours.sql (18-01 이 만든 파일 — 무엇이 적용되는지 확인)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-01-SUMMARY.md (제약 이름을 조회로 찾았는지 · 예상 반영 내용)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md §Pitfall 1 · §표 C-2
  </read_first>
  <action>
먼저 **자격증명을 재요청하지 않고** 해결한다 — 저장소의 env 파일(`grep -rl "SUPABASE" --include=".env*" .`), `gcloud secrets list --project gh-radar`, 이전 phase 기록 순으로 찾는다. 찾은 값은 로그·요약에 그대로 쓰지 않는다(마스킹).

`supabase db push` 를 **비대화형으로** 실행한다. `SUPABASE_ACCESS_TOKEN` 을 환경변수로 넘기고, 링크가 안 돼 있으면 `supabase link` 를 먼저 같은 방식으로 실행한다. 실행 전에 `supabase db push --dry-run` 으로 **적용될 마이그레이션 목록**을 먼저 찍어 `20260921120000_dma_orders_modify_offhours.sql` 만 남아 있는지 확인한다 — 다른 미적용 마이그레이션이 함께 딸려 나오면 멈추고 그 사실을 보고한다(이 phase 밖의 스키마를 말없이 바꾸지 않는다).

CLI 가 억제할 수 없는 확인 프롬프트나 인증 오류로 막히면 그 자리에서 실행을 멈추고 **동적 인증 게이트 체크포인트**로 사용자에게 올린다(무엇을 시도했고 어떤 응답을 받았는지 원문 포함). 재시도로 두드리지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && supabase migration list --linked 2>&amp;1 | grep -c "20260921120000"</automated>
    <fails_when>카운트가 0(목록에 이 마이그레이션이 아예 없다), 또는 명령이 non-zero 로 끝난다(인증/링크 실패 — 인증 게이트로 올린다)</fails_when>
    <human-check>`supabase migration list --linked` 출력에서 `20260921120000_dma_orders_modify_offhours` 행의 Remote 열이 채워져 있는지(= 원격에 적용됨) 확인한다. Local 만 채워져 있으면 푸시가 실제로는 일어나지 않은 것이다.</human-check>
  </verify>
  <acceptance_criteria>
    - `supabase db push --dry-run` 출력에 `20260921120000_dma_orders_modify_offhours` 가 있고, 이 phase 와 무관한 다른 미적용 마이그레이션이 함께 있으면 실행을 멈추고 보고했다
    - `supabase migration list --linked` 가 이 마이그레이션을 원격 적용 상태로 보여준다
    - 자격증명은 기존 원천(env / Secret Manager / 이전 phase 기록)에서 해결했고, 사용자에게 재요청한 기록이 없다
    - SUMMARY 에 마스킹된 형태로 어떤 원천에서 자격을 얻었는지 한 줄 기록했다
  </acceptance_criteria>
  <done>실 `dma_orders` 테이블이 `order_type 'M'` 과 조건부 `price 0` 을 받아들인다.</done>
</task>

<task type="auto">
  <name>Task 2: 반영 결과 조회 검증 — 제약 3건과 컬럼 2개</name>
  <files>supabase/migrations/20260921120000_dma_orders_modify_offhours.sql</files>
  <read_first>
    - supabase/migrations/20260905120200_dma_orders.sql (원래 제약·정책 문단 — 무엇이 바뀌지 **않아야** 하는지)
    - supabase/migrations/20260921120000_dma_orders_modify_offhours.sql
  </read_first>
  <action>
읽기 전용 조회로 실 DB 상태를 확인한다. `pg_constraint` 를 `conrelid = 'public.dma_orders'::regclass` 로 조회해 `pg_get_constraintdef(oid)` 를 뽑고, ① `order_type` CHECK 가 `'M'` 을 포함하는지 ② `price` CHECK 가 `krx_session` 을 포함하는지 확인한다. `information_schema.columns` 로 `piece_count`·`krx_session` 두 컬럼의 존재와 자료형을 확인한다. `pg_policies` 로 `dma_orders` 의 정책 목록을 뽑아 18-01 이전과 **개수·이름이 같은지** 확인한다(컬럼 추가가 접근 규칙을 건드리지 않았다는 증거).

조회 수단은 Supabase CLI 또는 프로젝트가 이미 쓰는 service role 경로 중 **이미 있는 것**을 쓴다. 새 스크립트·새 비밀을 만들지 않는다. 조회 결과 원문(제약 정의 문자열 2줄 · 컬럼 2행 · 정책 목록)을 SUMMARY 에 붙여 증거로 남긴다.

어느 하나라도 예상과 다르면 **멈추고 보고한다** — 마이그레이션을 손으로 고쳐 밀어넣지 않는다(다음 마이그레이션 파일로 바로잡는 것이 이 저장소의 방식이다).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "krx_session" supabase/migrations/20260921120000_dma_orders_modify_offhours.sql</automated>
    <fails_when>카운트가 3 미만 — 감사 컬럼 추가 · 컬럼 CHECK · price CHECK 세 자리 중 하나가 빠졌다</fails_when>
    <human-check>조회 결과에서 ① `order_type` CHECK 정의 문자열에 `'M'` 이 있고 ② `price` CHECK 정의 문자열에 `krx_session` 이 있고 ③ `piece_count`·`krx_session` 컬럼이 존재하고 ④ `dma_orders` 의 RLS 정책 개수·이름이 18-01 이전과 같은지 — 네 가지를 SUMMARY 에 붙은 원문으로 확인한다.</human-check>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 `pg_constraint` 조회 결과 원문(제약 정의 2줄)이 붙어 있다
    - SUMMARY 에 `piece_count`·`krx_session` 컬럼 존재 증거가 붙어 있다
    - SUMMARY 에 `dma_orders` 정책 목록이 붙어 있고 18-01 이전과 같음을 명시한다
    - 예상과 다른 항목이 있었다면 손으로 고치지 않고 보고했다
  </acceptance_criteria>
  <done>실 DB 가 계약대로 바뀌었고, 바뀌지 않아야 할 접근 규칙은 그대로임이 조회 원문으로 증명됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 개발자 워크스테이션 → Supabase 프로덕션 DB | 마이그레이션이 실 감사 테이블의 제약을 바꾼다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-10 | Tampering | 의도치 않은 마이그레이션 동반 적용 | high | mitigate | `--dry-run` 으로 적용 목록을 먼저 확인하고, 이 phase 밖 마이그레이션이 섞이면 멈추고 보고 |
| T-18-11 | Elevation of Privilege | RLS/REVOKE 의도치 않은 변경 | high | mitigate | Task 2 가 `pg_policies` 로 정책 개수·이름 불변을 조회 증거로 확인. 마이그레이션은 정책 문단을 건드리지 않는다 |
| T-18-12 | Information Disclosure | `SUPABASE_ACCESS_TOKEN`·DB 비밀번호 | high | mitigate | 로그·SUMMARY 에 값을 쓰지 않는다(원천 이름만 기록). 새 비밀을 만들지 않고 기존 원천에서 해결 |
| T-18-13 | Denial of Service | 푸시 실패 반복 재시도 | low | mitigate | 인증/프롬프트 차단은 1회에 멈추고 인증 게이트로 올린다. 재시도로 두드리지 않는다 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 phase 는 새 패키지를 설치하지 않는다(RESEARCH §Package Legitimacy Audit 표 비어 있음). 설치 태스크 없음 → legitimacy 체크포인트 없음 |
</threat_model>

## Artifacts this phase produces

새로 만드는 심볼·파일 (phase 전체 — 드리프트 검증에서 제외되어야 한다):

- **마이그레이션:** `supabase/migrations/20260921120000_dma_orders_modify_offhours.sql` · 컬럼 `dma_orders.piece_count` · `dma_orders.krx_session`
- **shared:** `RelayOrderModifyMsg` · `RelayKrxSession` · `OrderType` 의 `"M"` 멤버 · `RelayOrderNewMsg.pieceCount?` · `RelayOrderNewMsg.krxSession?` · `RelayRateCrossItem.name?` · `RelayRateCrossItem.code?`
- **relay:** `RelayOrderModifySchema` · `OrderInsertRow.pieceCount?` · `OrderInsertRow.krxSession?` · 돌파 항목 보강 private 메서드 · `relay/src/ws/__tests__/protocol.test.ts`
- **webapp lib:** `RelayOrderRequest.kind` 의 `"modify"` · `queued-window.ts`(`ManualOrderAffordance`·`OrderButtonMode`·`affordanceOf`) · `breakout-list.ts`(`kstDateKey`·`shouldRemoveBreakout`·`breakoutRowsFrom`·`BREAKOUT_SOUNDED_KEY`·`BREAKOUT_DISMISSED_KEY`·`BREAKOUT_TONE_KEY`·`TRADING_COLS_KEY`·`REMOVE_MARGIN_PCT`·`ARM_GRACE_MS`·`HIGHLIGHT_MS`) · `use-breakout-quotes.ts`(`useBreakoutQuotes`·`MAX_BREAKOUT_SUBS`) · `alert-tone.ts`(`playBreakoutTone`·`isTonePlaybackBlocked`·`resumeToneContext`) · `limit-chaser.ts` 로 이동한 `parseStrategyKey`
- **webapp 컴포넌트:** `components/trading/workbench/` 의 `TradingWorkbench`·`WorkbenchStatusBar`·`ViSettingsRows`·`ViTriggerStrip`·`BreakoutStrip`·`StockAddBar`·`CardGrid`·`SharedPanels` · `components/trading/card/` 의 `StrategyCard`·`CardHeader`·`QuoteGrid10`·`CardBody`·`ManualOrderForm`·`StockInfoModal`
- **라우트:** `webapp/src/app/trading/page.tsx`
- **CSS 토큰:** `--new-bg` · `--new-bd` (globals.css §9 라이트/다크 양쪽)
- **e2e:** `webapp/e2e/specs/trading-workbench.spec.ts`

<verification>
- `supabase migration list --linked` 가 `20260921120000` 을 원격 적용 상태로 보여준다
- 실 DB 제약 정의 조회 결과가 `'M'` 과 `krx_session` 을 포함한다
- `dma_orders` RLS 정책 개수·이름이 18-01 이전과 같다
</verification>

<success_criteria>
- 정정과 시간외종가가 실 경로에서 DB CHECK 에 막히지 않는다
- 이 phase 밖의 스키마는 한 줄도 바뀌지 않았다
- 자격증명 재요청이 없었다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-03-SUMMARY.md` when done
</output>
