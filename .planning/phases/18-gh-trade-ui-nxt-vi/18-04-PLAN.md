---
phase: 18-gh-trade-ui-nxt-vi
plan: 04
type: tdd
wave: 2
depends_on: ["18-01"]
files_modified:
  - webapp/src/lib/queued-window.ts
  - webapp/src/lib/__tests__/queued-window.test.ts
  - webapp/src/lib/breakout-list.ts
  - webapp/src/lib/__tests__/breakout-list.test.ts
  - webapp/src/lib/use-breakout-quotes.ts
  - webapp/src/lib/__tests__/use-breakout-quotes.test.tsx
  - webapp/src/lib/alert-tone.ts
  - webapp/src/lib/__tests__/alert-tone.test.ts
autonomous: true
requirements: [TRADE-06, TRADE-07]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-22: `affordanceOf(window, exchange, orderType)` 가 77 → 라벨·조각입력·확인문구 매핑의 **유일 지점**이고, `queuedWindow === undefined`(모름)는 전부 false 로 접힌다. 이 파일은 현재 시각을 읽지 않는다 — 벽시계 판정 금지 (TRADE-07)"
    - "D-22: 매핑 5경우가 정본 그대로다 — `offhours` → 매수/매도·조각 숨김 / `open ∧ KRX` → 예약 라벨·조각 보임(기본 5·상한 `maxPieces`) / `preopenOpen ∧ KRX` → 예약 라벨·조각 숨김·「예약: 증권사 보관 후 09:00 처리」 / `nxtPreopenOpen ∧ NXT` → 예약 라벨·조각 숨김·08:00 문구 / 그 외·모름 → 매수/매도·조각 숨김 (TRADE-07)"
    - "D-22: 77 값으로 주문을 **막지 않는다** — `affordanceOf` 의 반환에 「제출 금지」 성격의 필드가 없다 (TRADE-07)"
    - "D-16: 돌파 행 삭제 판정은 `등락률 < (행의 임계 − 2.0%p)` **∧ (무장 ∨ 추가 후 3초 유예 경과)** 일 때만 참이다. CONTEXT D-16 이 생략한 무장/유예 조건이 정본(gh-trade `rate-cross-alert.md` ③)에 있으므로 반드시 포함한다 (TRADE-06)"
    - "D-16: 현재가를 모르는 행(구독 실패·상한 초과)은 **이탈 판정을 하지 않는다** — 76 의 마지막 가격을 그대로 보이고 지우지 않는다. 모르는 값으로 지우는 것이 오래된 값을 보이는 것보다 나쁘다 (TRADE-06)"
    - "D-15: 「오늘 울린 종목」·「지운 종목」 집합은 `{d, ids}` 형태로 KST `yyyyMMdd` 날짜 키와 함께 localStorage 에 저장되고, 날짜가 다르면 읽을 때 **빈 집합**이다(별도 리셋 타이머 없음). 값은 단축코드가 아니라 **ISIN** 이다 (TRADE-06)"
    - "D-15/D-17: 읽기·쓰기 전부 `typeof window` 가드 + try/catch 로 감싸 안전한 기본값으로 수렴하고 throw 하지 않는다(Safari 프라이빗 모드) (TRADE-06)"
    - "D-17: 알림음은 **집합에 먼저 쓰고 그 다음 재생**한다 — 순서가 계약이다. 반대로 하면 재생 실패 시 하루 종일 중복 알림이 난다 (TRADE-06)"
    - "D-17: 알림음 기본값은 꺼짐이고, `AudioContext.state === 'suspended'` 면 차단으로 판정한다. `resume()` 은 사용자 제스처 안에서만 호출한다 (TRADE-06)"
    - "D-18: 신규 강조는 깜박임 없이 30초 `--new-bg` + 「신규」 배지이며, 타이머는 **목록 전체에 1개**(1초 tick, 강조 행이 있을 때만 가동)다 — 행마다 `setTimeout` 을 걸지 않는다(최대 200행) (TRADE-06)"
    - "D-14: 78 스냅샷으로 들어온 행은 **무음·무강조**이면서 「오늘 울린 종목」 집합에는 기록된다 (TRADE-06)"
    - "D-16: 돌파 구독은 `useRelayContext().subscribe/unsubscribe` 참조계수 API 만 쓰고 `{t:\"sub\"}` 프레임을 직접 만들지 않는다 — 재구독 경로가 둘이 되면 재접속 후 돌파 행 가격만 멎는다 (TRADE-06)"
    - "D-16: 돌파 구독은 거래소 `\"KRX\"` 고정이고 자율 상한 `MAX_BREAKOUT_SUBS = 40` 을 넘지 않으며, 우선순위는 카드 있는 종목 > 최근 돌파 > 오래된 돌파다 (TRADE-06)"
  artifacts:
    - webapp/src/lib/queued-window.ts
    - webapp/src/lib/breakout-list.ts
    - webapp/src/lib/use-breakout-quotes.ts
    - webapp/src/lib/alert-tone.ts
  key_links:
    - "`affordanceOf` ↔ 카드 수동주문 폼 · 호가 탭 수동주문 폼 — 두 표면이 **같은 함수**를 부른다. 조건식을 두 번 적으면 한쪽만 고쳐진다"
    - "`breakout-list.ts` 의 삭제 판정 ↔ 돌파 스트립/표 렌더 — 표시와 삭제 가드가 같은 함수를 쓴다"
    - "`useBreakoutQuotes` ↔ `useRelayContext().subscribe/unsubscribe` 참조계수 — 재구독 트리거는 세션 `ready` 하나뿐이다"
  prohibitions:
    - "MUST NOT 구독하지 못해 현재가를 모르는 돌파 행을 이탈로 판정해 지운다 — 모르는 값으로 지우는 것은 사용자에게 없는 사실을 말하는 것이다"
    - "MUST NOT localStorage 에 계좌번호·주문번호·주문 금액을 저장한다 — 돌파 집합에는 ISIN 만 담는다"
    - "MUST NOT 알림 재생을 집합 기록보다 먼저 한다 — 재생 실패 시 하루 종일 중복 알림이 난다"
    - "MUST NOT 돌파 목록을 클라가 재정렬·재해석하거나 카드를 자동으로 열어 사용자가 요청하지 않은 거래 표면을 만든다"
---

<objective>
UI 가 부를 **판정 함수 네 벌**을 테스트가 먼저 있는 상태로 만든다 — 77 창 힌트 → 라벨·입력 매핑(`queued-window.ts`), 돌파 목록의 클라 몫 규칙(`breakout-list.ts`), 돌파 종목 다중 시세 구독(`use-breakout-quotes.ts`), 알림음 합성·차단 감지(`alert-tone.ts`).

Purpose: 이 phase 에서 화면을 한 페이지로 합치면 「이 카드에서만 다르게」가 생기기 쉽고, 갈린 판정은 전부 오발주·오삭제 경로다. 판정을 순수 함수로 먼저 뽑아 두면 표시와 가드가 같은 함수를 부르게 강제된다.
Output: `webapp/src/lib/` 신규 4파일 + 대응 테스트 4파일.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `queued-window.ts` — 77 → 라벨·조각입력·확인문구 매핑 (판정의 유일 지점)</name>
  <files>webapp/src/lib/queued-window.ts, webapp/src/lib/__tests__/queued-window.test.ts</files>
  <read_first>
    - packages/shared/src/relay.ts (`RelayQueuedWindowMsg` 6필드 — `open`·`maxPieces`·`preopenOpen`·`g2Open`·`g3Open`·`nxtPreopenOpen`)
    - webapp/src/lib/relay-provider.tsx (`queuedWindow` 컨텍스트 값이 `undefined` 를 「모름」으로 두는 주석)
    - webapp/src/components/trading/vi-order-list.tsx (`isConfirmable` — 「판정의 유일 지점」 docstring 문법과 `Pick<>` 최소 입력 관례)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md §Pattern 5 (규칙 표 정본 인용)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §「77 → 라벨·입력 매핑」 · §시간외종가
  </read_first>
  <behavior>
    - `affordanceOf(undefined, "KRX", "limit")` → `buttonMode "normal"` · `showPieceInput false` · `confirmNote null` · `offHoursSelectable false`.
    - `affordanceOf({open:true, maxPieces:10, …}, "KRX", "limit")` → `buttonMode "queued"` · `showPieceInput true` · `maxPieces 10` · `confirmNote null`.
    - `affordanceOf({open:true, …}, "NXT", "limit")` → `buttonMode "normal"` · `showPieceInput false` (예약구간은 KRX 만).
    - `affordanceOf({preopenOpen:true, …}, "KRX", "limit")` → `buttonMode "queued"` · `showPieceInput false` · `confirmNote` 가 09:00 문구.
    - `affordanceOf({nxtPreopenOpen:true, …}, "NXT", "limit")` → `buttonMode "queued"` · `showPieceInput false` · `confirmNote` 가 08:00 문구.
    - `affordanceOf({open:true, g2Open:true, …}, "KRX", "offhours")` → `buttonMode "normal"` · `showPieceInput false` · `confirmNote null` (시간외종가는 창과 무관하게 매수/매도·조각 1).
    - `offHoursSelectable` 는 `KRX ∧ (g2Open ∨ g3Open)` 일 때만 true 이고 NXT 에서는 항상 false.
    - 반환 객체에 「주문 제출 금지」 성격의 필드가 없다.
  </behavior>
  <action>
`webapp/src/lib/queued-window.ts` 를 신설한다. export 는 셋이다 — 타입 `OrderButtonMode = 'normal' | 'queued'`, 인터페이스 `ManualOrderAffordance { buttonMode; showPieceInput; maxPieces; confirmNote; offHoursSelectable }`, 함수 `affordanceOf(w: RelayQueuedWindowMsg | undefined, exchange: RelayExchange, orderType: 'limit' | 'offhours'): ManualOrderAffordance`.

파일 머리 docstring 은 `vi-order-list.tsx` 의 `isConfirmable` 문법을 따른다 — 「**판정의 유일 지점**이다: 라벨·조각 입력 표시·확인 문구가 전부 이 함수를 부른다. 두 곳에 따로 적으면 한쪽만 고쳐지고, 그때 나가는 것이 「화면에 없는 조각 수」다」. 그리고 D-22 규율을 ★ 주석으로 못 박는다 — 「`undefined` 는 **모름**이고 전부 false 로 읽는다. 창이 닫혔다(`open:false`)로 위장하지 않는다」, 「**벽시계를 읽지 않는다** — 이 파일에 현재 시각 상수·`Date` 를 들이지 말 것. 창 판정은 서버 몫이다」, 「이 값으로 주문을 막지 않는다」.

매핑은 RESEARCH §Pattern 5 표를 그대로 옮긴다. 우선순위는 `orderType === 'offhours'` 가 최상위이고(창과 무관하게 매수/매도·조각 1), 그 다음 `open ∧ KRX`, `preopenOpen ∧ KRX`, `nxtPreopenOpen ∧ NXT`, 그 외. `maxPieces` 는 서버 값 그대로 쓰고 클라 기본값을 만들지 않는다(기본 조각 수 5 는 스테퍼의 초기값이지 상한이 아니다 — 그 5 는 폼이 갖는다). 확인 문구 두 개는 UI-SPEC §Copywriting 원문(「예약: 증권사 보관 후 09:00 처리」 / NXT 는 08:00)을 상수로 export 한다.

테스트 `webapp/src/lib/__tests__/queued-window.test.ts` 는 위 `<behavior>` 8케이스를 전부 덮고, 추가로 「`Date` 를 모킹해 시각을 바꿔도 결과가 변하지 않는다」를 한 케이스로 단언한다(벽시계 미사용의 기계적 증거).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- queued-window</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다 — 이 문구가 유일한 경보다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "new Date|Date\.now" webapp/src/lib/queued-window.ts</automated>
    <fails_when>non-zero exit — 매핑 파일이 벽시계를 읽고 있다(D-22 위반)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `webapp/src/lib/queued-window.ts` 에 `export function affordanceOf(` 가 있다
    - 매핑 5경우 + 시간외종가 우선순위 + NXT 예약구간 제외가 테스트로 단언된다
    - 파일 안에 시각을 읽는 호출이 없다
    - `pnpm --filter @gh-radar/webapp test -- queued-window` exit 0
  </acceptance_criteria>
  <done>카드와 호가 탭이 같은 한 함수로 예약·장전·시간외종가 라벨을 결정한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `breakout-list.ts` — 이탈 판정 · KST 날짜 키 집합 · 30초 강조</name>
  <files>webapp/src/lib/breakout-list.ts, webapp/src/lib/__tests__/breakout-list.test.ts</files>
  <read_first>
    - webapp/src/lib/vi-alert.ts (SSR 가드 + try/catch + 안전 기본값 수렴 패턴 · 「(이 기기만)」 문구 선례 · 키 상수 관례)
    - packages/shared/src/relay.ts (`RelayRateCrossItem` — 18-01 이 더한 `name?`/`code?` 포함)
    - webapp/src/lib/relay-provider.tsx (`rateCrossItems` 리듀서가 이미 76 upsert · 78 전량 교체 · 정렬 · 상한 200 을 한다는 사실)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md §돌파감지 목록 클라 규칙 정본 대조 표 · §Pattern 1 이탈 판정 공식 · §예 4 · §Pitfall 6
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §localStorage 키 표 · §상호작용 「이탈 삭제」/「78 스냅샷 행은 무음·무강조」
  </read_first>
  <behavior>
    - `kstDateKey(new Date("2026-09-21T14:59:59Z"))` 와 `kstDateKey(new Date("2026-09-21T15:00:01Z"))` 가 **다른** 날짜 키를 준다(KST 자정 경계).
    - `readDatedSet` 이 저장된 날짜와 오늘이 다르면 빈 집합을 준다.
    - `localStorage` 가 throw 하도록 모킹해도 읽기·쓰기가 throw 하지 않고 안전 기본값으로 수렴한다.
    - 삭제 판정: 임계 20 · 등락률 17.9 · 무장됨 → true. 임계 20 · 등락률 17.9 · 미무장 · 추가 후 1초 → false. 같은 조건 · 추가 후 4초 → true. 임계 20 · 등락률 18.1 · 무장됨 → false(경계 −2.0%p 정확히).
    - 현재가를 모르는 행(가격 입력이 `undefined`) → 조건과 무관하게 항상 false.
    - 78 스냅샷 유래 행은 강조 대상이 아니고(`highlightUntil` 없음) 「울린 종목」 집합에는 기록된다.
    - 행이 추가된 지 29.9초 → 강조 참, 30.1초 → 거짓.
  </behavior>
  <action>
`webapp/src/lib/breakout-list.ts` 를 신설한다. 파일 머리에 「이 모듈은 서버 집합을 **재해석하지 않는다** — 76 upsert·78 전량 교체·정렬·상한 200 은 relay 리듀서가 이미 했다. 여기 있는 것은 gh-trade 정본이 **클라 몫**으로 규정한 세 가지뿐이다: 하루 1회 알림 · 임계−2%p 이탈 삭제 · 사용자가 지운 종목」을 ★ 주석으로 적는다.

**상수:** `BREAKOUT_SOUNDED_KEY = 'gh-radar:breakout-sounded'` · `BREAKOUT_DISMISSED_KEY = 'gh-radar:breakout-dismissed'` · `BREAKOUT_TONE_KEY = 'gh-radar:breakout-tone'` · `TRADING_COLS_KEY = 'gh-radar:trading-cols'` · `REMOVE_MARGIN_PCT = 2.0` · `ARM_GRACE_MS = 3_000` · `HIGHLIGHT_MS = 30_000`.

**날짜 키:** `kstDateKey(now: Date = new Date()): string` — KST(UTC+9) 기준 `yyyyMMdd`. 서버의 06:00 거래일 리셋과 다르다는 사실을 주석에 적는다(클라는 날짜 키가 정본).

**집합 I/O:** `{ d: string; ids: string[] }` 형태로 저장하는 `readDatedSet(key, today)` / `writeDatedSet(key, today, ids)` 를 만들고 `vi-alert.ts:137-158` 의 가드 패턴(`typeof window === 'undefined'` 조기 반환 → try/catch → 안전 기본값)을 그대로 따른다. 날짜가 다르면 읽을 때 빈 집합으로 간주하고 쓸 때 새 날짜로 덮는다 — **별도 리셋 타이머를 만들지 않는다.** 값에는 **ISIN 만** 담는다(단축코드가 아니라 — 돌파 항목의 유일한 확실한 키). 이 위에 `readSoundedSet`/`addSounded`/`readDismissedSet`/`addDismissed`/`readTonePref`/`writeTonePref`(기본 `off`)/`readColsPref`/`writeColsPref`(기본 `1`)를 얇게 얹는다.

**행 상태와 삭제 판정:** 행마다 `armed: boolean`(추가 후 임계 이상을 한 번이라도 관측) 과 `addedAt: number` 를 들고 있는 뷰 모델 타입 `BreakoutRow` 를 export 한다. `shouldRemoveBreakout(row, currentPrice | undefined): boolean` 이 **삭제 판정의 유일 지점**이다 — ① `currentPrice` 가 `undefined` 면 **무조건 false**(모르는 값으로 지우지 않는다) ② 등락률은 `(현재가 − 행의 기준가) × 100 ÷ 행의 기준가` 로 계산하고 기준가가 없는 행만 76 이 준 등락률로 폴백한다 ③ `등락률 < row.thresholdPct − REMOVE_MARGIN_PCT` 이고 `(row.armed || now - row.addedAt >= ARM_GRACE_MS)` 일 때만 true. 무장/유예 조건이 CONTEXT D-16 에는 없고 gh-trade 정본에는 있다는 사실과, 빼면 낡은 체결 한 건이 목록을 즉시 지운다는 근거를 docstring 에 남긴다.

**집합 조립:** `breakoutRowsFrom(items, { dismissed, cards, now })` 가 relay 의 `rateCrossItems` 배열을 화면 행으로 접는다 — 서버 순서를 **그대로** 유지하고(정렬·필터 재구현 금지), 지운 종목을 빼고, 카드가 있는 종목에 「거래중」 표식을 붙이고, `highlightUntil = addedAt + HIGHLIGHT_MS` 를 계산한다. 78 스냅샷 유래 행은 `silent: true` 로 표시해 무음·무강조이면서 「울린 종목」 집합 기록 대상임을 나타낸다. 강조 만료는 **호출자가 1초 tick 타이머 1개**로 다시 부르는 구조를 전제로 하고(행마다 `setTimeout` 금지 — 최대 200행), 그 사실을 docstring 에 적는다.

테스트 `webapp/src/lib/__tests__/breakout-list.test.ts` 는 `<behavior>` 전부와, 저장 값에 ISIN 외의 것(계좌번호·주문번호·금액)이 들어가지 않는다는 단언을 포함한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- breakout-list</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "export function shouldRemoveBreakout" webapp/src/lib/breakout-list.ts</automated>
    <fails_when>카운트가 1 이 아니다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "setTimeout|setInterval" webapp/src/lib/breakout-list.ts</automated>
    <fails_when>non-zero exit — 순수 판정 모듈이 타이머를 소유하고 있다(타이머는 호출자가 목록 전체에 1개만 돌린다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `shouldRemoveBreakout` 이 `currentPrice === undefined` 에서 항상 false 를 준다(테스트로 단언)
    - 경계 테스트가 임계−2.0%p 정확값 양옆(17.9 / 18.1)을 덮는다
    - 무장·3초 유예 조합 4경우가 전부 단언된다
    - KST 자정 경계 두 케이스가 서로 다른 날짜 키를 준다
    - localStorage throw 모킹에서 읽기·쓰기가 throw 하지 않는다
    - 이 파일에 타이머 호출이 없다
  </acceptance_criteria>
  <done>돌파 목록의 클라 몫 규칙 전부가 한 모듈에 있고, 표시와 삭제 가드가 같은 함수를 부른다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `use-breakout-quotes.ts` 다중 구독 훅 + `alert-tone.ts` 알림음</name>
  <files>webapp/src/lib/use-breakout-quotes.ts, webapp/src/lib/__tests__/use-breakout-quotes.test.tsx, webapp/src/lib/alert-tone.ts, webapp/src/lib/__tests__/alert-tone.test.ts</files>
  <read_first>
    - webapp/src/lib/relay-provider.tsx (`useRelaySubscription` 단일 키 훅 — 키가 바뀌면 이전 키를 해제하는 클로저 규율 · `useRelayContext()` 의 `subscribe`/`unsubscribe`/`quotes` · `relayQuoteKey`)
    - webapp/src/lib/use-relay-socket.ts (브라우저 쪽 참조계수와 `flushSubscriptions` — 재구독 트리거가 하나뿐이라는 사실)
    - webapp/src/lib/vi-alert.ts (SSR 가드 · 기본 꺼짐 규율 · 「본문에는 종목명만 싣는다」 알림 규율)
    - webapp/src/lib/breakout-list.ts (Task 2 의 `BREAKOUT_TONE_KEY`·`readTonePref`)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md §Pattern 1(코드 포함) · §Pitfall 7 · §Standard Stack 알림음 파라미터
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §알림음 확정 파라미터
  </read_first>
  <behavior>
    - `useBreakoutQuotes([])` → `subscribe` 가 한 번도 불리지 않는다.
    - 키 집합 `[A, B]` 로 마운트 → `subscribe(A,"KRX")`·`subscribe(B,"KRX")` 각 1회.
    - 같은 집합으로 재렌더(새 배열 인스턴스) → 추가 `subscribe` 호출 0회(안정 시그니처).
    - `[A, B]` → `[B, C]` → `unsubscribe(A)` 1회 · `subscribe(C)` 1회 (B 는 그대로).
    - 언마운트 → 남은 키 전부 `unsubscribe`.
    - 키가 상한을 넘으면 우선순위(카드 있는 종목 > 최근 돌파 > 오래된 돌파) 상위 `MAX_BREAKOUT_SUBS` 개만 구독된다.
    - 훅이 `ws.send` 나 `{t:"sub"}` 프레임을 직접 만들지 않는다.
    - `playBreakoutTone()` 은 토글이 꺼져 있으면 아무 노드도 만들지 않는다.
    - `AudioContext.state === 'suspended'` 이면 `isTonePlaybackBlocked()` 가 true.
  </behavior>
  <action>
`webapp/src/lib/use-breakout-quotes.ts` 를 신설한다. `useRelaySubscription` 과 **같은 계약**을 갖되 키가 집합이라는 점만 다르다 — `useRelayContext()` 에서 `subscribe`/`unsubscribe`/`quotes` 를 꺼내 쓰고 `sub` 프레임을 직접 만들지 않는다(재구독 경로를 두 벌 만들면 「재접속 후 새로고침해야 시세가 나온다」가 된다). effect 재실행은 키 집합을 정렬·결합한 **안정 문자열 시그니처**로만 유발한다. cleanup 은 클로저가 붙잡은 **그 키들**을 해제한다. 훅은 `quotes` 맵을 그대로 돌려주고 소비자가 `relayQuoteKey(isin, exchange)` 로 골라 쓴다.

구독 거래소는 `"KRX"` 고정이다 — 돌파 감지는 서버 정본상 KRX A3 에서만 발화하고, 그것이 「칩에 거래소를 표시하지 않는다」(D-07)와도 일관된다. 그 근거를 주석에 적는다.

자율 상한 `MAX_BREAKOUT_SUBS = 40` 을 export 한다. 주석에 **이 값이 실측이 아니라 보수적 선택**임을 명시한다 — relay `SubscriptionHub` 에는 구독 상한이 없음이 실측됐고, 게이트웨이 세션당 상한은 **확인되지 않았다**(「상한이 없다」고 읽지 말 것). 훅은 카드가 소유한 구독을 상한 예산에서 제외하도록 `excludeIsins` 를 받고, 남은 예산을 최근 돌파 순으로 채운다. 상한을 넘겨 구독하지 못한 키는 호출자가 알 수 있게 반환값에 포함한다 — 그 행은 `shouldRemoveBreakout` 이 `currentPrice === undefined` 로 읽어 삭제되지 않는다.

`webapp/src/lib/alert-tone.ts` 를 신설한다. 코드베이스 최초의 `AudioContext` 사용처이므로 구조·가드는 `vi-alert.ts` 를 따른다. export 는 셋 — `playBreakoutTone()` · `isTonePlaybackBlocked()` · `resumeToneContext()`. 합성 파라미터는 UI-SPEC 확정값: sine 880Hz · 총 160ms · gain 0 → 0.18(ramp 10ms) → 0(exponential ramp). 짧은 attack/release 로 클릭 노이즈를 피한다. `AudioContext` 는 **지연 생성**하고 모듈 로드 시점에 만들지 않는다(SSR·자동재생 정책). 차단 판정은 `AudioContext.state === 'suspended'` 이고, `resume()` 은 **사용자 제스처 안에서만** 호출한다(호출자가 제스처 핸들러에서 부른다는 것을 docstring 에 적는다). 파일 자산을 추가하지 않는다.

⚠️ **순서가 계약이다** — 「소리를 내기 전에 집합에 저장한다」. 이 모듈은 저장을 하지 않으므로(그것은 `breakout-list.ts` 몫), `playBreakoutTone` 의 docstring 에 「호출자는 `addSounded` 를 **먼저** 부른 뒤 이 함수를 부른다. 반대로 하면 재생 실패 시 하루 종일 중복 알림이 난다」를 남긴다. 알림 본문·로그에 계좌번호·금액을 싣지 않는다.

테스트: `use-breakout-quotes.test.tsx` 는 `useRelayContext` 를 모킹해 `<behavior>` 의 구독 diff 6케이스와 상한·우선순위를 단언하고, 훅 소스에 `ws.send` 호출이 없음을 단언한다. `alert-tone.test.ts` 는 `AudioContext` 를 모킹해 토글 꺼짐 시 무동작, `suspended` 차단 판정, 파라미터(880/160ms/0.18)를 단언한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- use-breakout-quotes</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- alert-tone</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "\.send\(" webapp/src/lib/use-breakout-quotes.ts</automated>
    <fails_when>non-zero exit — 훅이 구독 프레임을 직접 보내고 있다(재구독 경로가 둘이 된다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "MAX_BREAKOUT_SUBS" webapp/src/lib/use-breakout-quotes.ts</automated>
    <fails_when>카운트가 0 — 자율 상한 상수가 없다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `webapp/src/lib/use-breakout-quotes.ts` 에 `export function useBreakoutQuotes(` 와 `MAX_BREAKOUT_SUBS` 가 있다
    - 구독 diff 6케이스(빈 집합 · 초기 구독 · 재렌더 무호출 · 교체 · 언마운트 · 상한 초과)가 테스트로 단언된다
    - 훅 소스에 소켓 전송 호출이 없다
    - `webapp/src/lib/alert-tone.ts` 에 `playBreakoutTone`·`isTonePlaybackBlocked`·`resumeToneContext` 세 export 가 있고, `AudioContext` 를 모듈 최상위에서 생성하지 않는다
    - `alert-tone` 테스트가 880Hz · 160ms · gain 0.18 파라미터와 토글 꺼짐 무동작, `suspended` 차단 판정을 단언한다
    - `webapp/` 아래에 새 오디오 파일 자산이 추가되지 않았다
  </acceptance_criteria>
  <done>돌파 행이 카드 없이도 현재가를 받고, 알림음이 기기별 토글·하루 1회 규율 아래에서 울린다.</done>
</task>

</tasks>

## Flagged Assumptions (probe unresolved — 조용히 버리지 않는다)

| 출처 | 항목 | 상태 | 플래너 판단 |
|------|------|------|------------|
| edge-probe fallback | **TRADE-06 / `unclassified`** — 「돌파감지 목록」 요구사항이 8종 형태 분류(boundary·precision·adjacency·empty·ordering 등) 어디에도 떨어지지 않아 프로브가 분류하지 못했다 | `unresolved` — 자동 해소하지 않는다 | 이 요구사항의 실제 위험은 **형태**가 아니라 **권한 경계**다(어느 집합을 누가 소유하는가: 서버 76/78 · relay 캐시 · 브라우저 localStorage). 그 축은 taxonomy 밖이라 프로브가 비어 있는 것이 정상이다. 대신 D-14/D-15/D-16/D-18 truths 와 `must_haves.prohibitions` 4건이 같은 위험을 명시적으로 덮는다. **backstop 으로 자동 승격하지 않고 unresolved 로 남겨 검증자에게 그대로 올린다.** |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay wss → 브라우저 | 76/78 돌파 집합과 77 창 힌트가 여기서 들어온다. 클라는 재해석하지 않는다 |
| 브라우저 → localStorage | 기기별 「울린/지운 종목」 집합. 서버 왕복 없음 |
| 브라우저 → Web Audio | 사용자 제스처 정책이 걸린 브라우저 API |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-14 | Information Disclosure | localStorage 돌파 집합 | medium | mitigate | 값에 **ISIN 만** 담는다. 계좌번호·주문번호·금액 금지(`vi-alert.ts` 선례). 테스트가 저장 페이로드를 단언 |
| T-18-15 | Tampering | 이탈 삭제 오판 | high | mitigate | `currentPrice === undefined` 면 무조건 false + 무장/3초 유예 조건. 모르는 값으로 목록을 지우지 않는다 |
| T-18-16 | Denial of Service | 구독 상한 초과 | medium | mitigate | 자율 상한 `MAX_BREAKOUT_SUBS = 40` + 카드 구독 예산 제외. 게이트웨이 상한 미확인 사실을 주석에 명시 |
| T-18-17 | Denial of Service | 행마다 타이머 | low | mitigate | 순수 모듈에 타이머 금지(verify 가 grep 으로 단언). 강조는 목록 전체 1초 tick 1개 |
| T-18-18 | Information Disclosure | 알림 경로 | low | mitigate | 알림 본문·로그에 종목명만. 계좌번호·금액 금지 |
| T-18-19 | Input Validation | 깨진 localStorage JSON | low | mitigate | try/catch → 「기억 없음」으로 수렴. throw 없음 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 phase 는 새 패키지를 설치하지 않는다(RESEARCH §Package Legitimacy Audit 표 비어 있음). Web Audio 는 브라우저 네이티브 |
</threat_model>

## Artifacts this phase produces

새로 만드는 심볼·파일 (phase 전체 — 드리프트 검증에서 제외되어야 한다):

- **마이그레이션:** `supabase/migrations/20260921120000_dma_orders_modify_offhours.sql` · 컬럼 `dma_orders.piece_count` · `dma_orders.krx_session`
- **shared:** `RelayOrderModifyMsg` · `RelayKrxSession` · `OrderType` 의 `"M"` 멤버 · `RelayOrderNewMsg.pieceCount?` · `RelayOrderNewMsg.krxSession?` · `RelayRateCrossItem.name?` · `RelayRateCrossItem.code?`
- **relay:** `RelayOrderModifySchema` · `OrderInsertRow.pieceCount?` · `OrderInsertRow.krxSession?` · 돌파 항목 보강 private 메서드 · `relay/src/ws/__tests__/protocol.test.ts`
- **webapp lib:** `RelayOrderRequest.kind` 의 `"modify"` · `queued-window.ts`(`OrderButtonMode`·`ManualOrderAffordance`·`affordanceOf`·예약 안내 문구 상수 2개) · `breakout-list.ts`(`kstDateKey`·`readDatedSet`·`writeDatedSet`·`readSoundedSet`·`addSounded`·`readDismissedSet`·`addDismissed`·`readTonePref`·`writeTonePref`·`readColsPref`·`writeColsPref`·`BreakoutRow`·`shouldRemoveBreakout`·`breakoutRowsFrom`·`BREAKOUT_SOUNDED_KEY`·`BREAKOUT_DISMISSED_KEY`·`BREAKOUT_TONE_KEY`·`TRADING_COLS_KEY`·`REMOVE_MARGIN_PCT`·`ARM_GRACE_MS`·`HIGHLIGHT_MS`) · `use-breakout-quotes.ts`(`useBreakoutQuotes`·`MAX_BREAKOUT_SUBS`) · `alert-tone.ts`(`playBreakoutTone`·`isTonePlaybackBlocked`·`resumeToneContext`) · `limit-chaser.ts` 로 이동한 `parseStrategyKey`
- **webapp 컴포넌트:** `components/trading/workbench/` 의 `TradingWorkbench`·`WorkbenchStatusBar`·`ViSettingsRows`·`ViTriggerStrip`·`BreakoutStrip`·`StockAddBar`·`CardGrid`·`SharedPanels` · `components/trading/card/` 의 `StrategyCard`·`CardHeader`·`QuoteGrid10`·`CardBody`·`ManualOrderForm`·`StockInfoModal`
- **라우트:** `webapp/src/app/trading/page.tsx`
- **CSS 토큰:** `--new-bg` · `--new-bd` (globals.css §9 라이트/다크 양쪽)
- **e2e:** `webapp/e2e/specs/trading-workbench.spec.ts`

<verification>
- `pnpm --filter @gh-radar/webapp test` 전량 exit 0 (Phase 17 기준선 1008 이상)
- `pnpm --filter @gh-radar/webapp run typecheck` exit 0
- 네 신규 모듈 모두 대응 테스트 파일이 존재하고 실제로 실행된다(`No test files found` 미출현)
</verification>

<success_criteria>
- 77 매핑·이탈 판정·구독 diff·알림음이 전부 순수 함수/훅으로 분리돼 테스트가 있다
- 어느 모듈도 벽시계로 창을 판정하거나 서버 집합을 재정렬하지 않는다
- 모르는 값으로 행을 지우는 경로가 없다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-04-SUMMARY.md` when done
</output>