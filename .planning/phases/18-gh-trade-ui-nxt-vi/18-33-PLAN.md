---
phase: 18-gh-trade-ui-nxt-vi
plan: 33
type: execute
wave: 18
depends_on: ["18-32"]
gap_closure: true
files_modified:
  - relay/src/order/notice-status.ts
  - relay/src/store/orders.ts
  - relay/tests/helpers/fake-dma-orders.ts
  - relay/tests/ws-order.test.ts
  - relay/tests/order-store.test.ts
  - relay/src/ws/order-handler.ts
autonomous: true
requirements: [TRADE-07]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "R3-WR-01 · D-27: `dma_orders` 상태 갱신은 단조다 — 같은 행을 더 낮은 순위로 되돌리는 갱신은 반영되지 않는다. 순위는 requested·timeout 0 < accepted 1 < partially_filled 2 < filled·cancelled·rejected 3 이고, 종결 상태(filled·cancelled·rejected)는 같은 값으로만 갱신된다. 판정은 Postgres 가 UPDATE 한 문장 안에서 한다(조건부 `status` 필터) — 큐에 먼저 들어간 갱신과 경합하지 않는다"
    - "ws ㊹: 정정 대기를 전량 체결 E(org X · `requestKind:\"Modify\"` · 새 주문번호 Y · 10주)가 정산한 뒤 늦게 도착한 정정확인 M(org X · 주문번호 Y)은 정정 행을 `filled` 에서 `accepted` 로 되돌리지 않는다 — `status` · `notice_type`(E) · `filled_qty`(10) 가 그대로다"
    - "ws ㊺: 같은 순서에서 늦은 M 의 주문번호가 원주문 X 이면 원주문 행(`partially_filled` · `notice_type` E · `filled_qty` 6)을 덮지 않는다 — 정정 행도 `filled` 그대로다"
    - "가드에 막힌 갱신은 조용히 사라지지 않는다 — warn 로그 1줄(셀렉터 컬럼 · 목표 상태 · 통보 종류만. 주문번호 · 계좌 원문 없음 · T-16-45)과 `stats().flushedNoop` 가산. 드롭 · 재시도 카운터는 오르지 않는다(이유를 아는 무동작 · S-5)"
    - "정상 전이는 하나도 막히지 않는다 — requested → timeout · rejected · accepted · partially_filled · filled · cancelled / timeout → accepted(늦은 접수가 결과 모름을 푼다) / accepted → partially_filled → filled / accepted · partially_filled → cancelled / 같은 상태 재기록(접수 A 뒤 정정확인 M 의 `notice_type` 갱신). `status` 가 없는 갱신(체결수량만 · 주문번호만)은 쿼리 모양이 이 플랜 이전과 같다"
    - "18-25 의 정산 규칙(ws ㊸ · `narrowPending` WR-03 (a)~(i))과 테넌트 경계(`order_no` 셀렉터 3축 · ⓷)는 무수정 green 이다"
  artifacts:
    - relay/src/order/notice-status.ts
    - relay/src/store/orders.ts
    - relay/tests/helpers/fake-dma-orders.ts
    - relay/tests/ws-order.test.ts
    - relay/tests/order-store.test.ts
  key_links:
    - "게이트웨이 51 늦은 M → hub `order` → `narrowPending` null(대기 없음) → `recordUnmatched` 수동 분기 → `findIdByOrderNo` → `enqueueUpdate` → `OrderStore#drain` → `supabaseOrderSink` 의 `runUpdate` 조건부 `status` 필터 → 0행 → `{applied:false}` → `flushedNoop`"
    - "`replaceableStatusesOf(next)`(notice-status.ts · 판정의 유일 지점) → `runUpdate`(셀렉터 조립의 유일 지점)가 최초 시도와 `23505` 재시도 모두에 같은 필터를 건다"
  prohibitions:
    - "MUST NOT 새 Supabase 마이그레이션 · 트리거 · RPC 를 만든다 — 조건부 UPDATE 는 relay 쿼리 필터로 충분하고, 스키마 변경은 원격 반영(사람 게이트 · 18-29 형식)을 부른다"
    - "MUST NOT 가드를 `recordUnmatched` 에서 기존 행 상태를 따로 읽어 판정하는 방식으로 만든다 — 읽기와 쓰기 사이에 큐(200ms)에 먼저 들어간 갱신이 아직 반영되지 않은 행을 읽어, 부분체결 E 직후의 M 이 여전히 되돌린다"
    - "MUST NOT 셀렉터 · 필터 조립 지점을 둘로 만든다 — `runUpdate` 한 곳(gap 1 / T-16-14)"
    - "MUST NOT 로그에 주문번호 · 계좌 원문을 싣는다(T-16-45)"
    - "MUST NOT `filled_qty` 단조성 등 이 발견 밖의 규칙을 더한다 — 범위는 상태 순위다"
    - "MUST NOT relay 를 배포한다 — 배포 순서는 18-36 이 기록하고 사람이 실행한다"
---

<objective>
R3-WR-01 을 닫는 **수직 슬라이스(tracer)**: 게이트웨이 통보 → hub → 주문 핸들러 기록 경로 → 갱신 큐 → PostgREST UPDATE 까지 한 경로에서, 늦게 도착한 통보가 이미 진행된 감사 행의 상태를 되돌리지 못하게 한다.

Purpose: 18-25(GC-WR-01)가 「체결 E(Modify)가 정정 대기를 먼저 정산」 하는 경로를 열었다. 그 뒤 **지연된** 정정확인 M 은 대기가 없어 `recordUnmatched` 수동 분기로 가고, `findIdByOrderNo(Y)` 가 방금 체결로 채운 정정 행을 찾아 `statusOf(M) = "accepted"` 로 덮는다 — 전량 체결된 정정이 감사 기록에 영구히 「접수」 로 남는다. M 의 주문번호가 원주문 X 면 원주문 행이 덮인다. `rowPatchOf` 에도 DB 에도 단조성 가드가 없다(18-REVIEW-R3 R3-WR-01).
Output: 상태 순위 판정 함수 · UPDATE 조건부 필터 · 필터를 실제로 적용하는 공용 가짜 PostgREST · ws ㊹ ㊺(RED → GREEN) · 전이표 회귀.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-REVIEW-R3.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-25-SUMMARY.md

근거 인용(계획 시점 재확인):
- `relay/src/ws/order-handler.ts:450-460` `patchOf`(`status: statusOf(notice, null)`) · `:486-560` `recordUnmatched` 수동 분기(`findIdByOrderNo` → `enqueueUpdate({ ...patch, orderRowId: existingId })`) · `:977-1025` `finish`(정산 = 정정 행에 `orderNo` · `status` · `filledQty` 기록).
- `relay/src/order/notice-status.ts:39-62` `statusOf` — `"M"` → `"accepted"`, `"E"` 는 주문수량을 알 때만 `filled`/`partially_filled`.
- `relay/src/store/orders.ts:328-429` `supabaseOrderSink` — `runUpdate` 가 셀렉터 조립의 유일 지점(주석 「셀렉터 조립은 이 함수 한 곳뿐이다」), `23505` 재시도도 여기를 지난다. `:887-902` `rowPatchOf` 는 `status` 를 그대로 싣는다. `:806-848` `#drain` 은 `{applied:false}` 를 `flushedNoop` 으로 센다(16-40).
- `supabase/migrations/20260905120200_dma_orders.sql:62` — `status text NOT NULL DEFAULT 'requested' CHECK (7종)`.
- `relay/tests/order-store.test.ts:520-700` `fakeDmaOrders` — 필터를 **실제로 적용**하고 성공한 update 를 행에 병합하는 가짜 PostgREST(지금은 `eq`·`gte`·`lt`·`order`·`limit` 만).
- `relay/tests/ws-order.test.ts:241-262` `startHarness(overrides)` 가 `orderStore` 를 덮어쓸 수 있다 · `:758-764` `restartHarness` · `:1577-1612` ㊸(정정 → E(org X, Modify, Y, 4주) 정산).
- `relay/tests/helpers/frames.ts:275-300` `FakeOrderRespInput` — `origin` 기본 `"Manual"`, `requestKind` 기본 빈 값.

층 결정(R3-WR-01 「Decide the layer」): **relay `orders.ts` update 경로의 조건부 UPDATE** 로 한다. `.in("status", …)` 필터는 relay 코드이지만 판정은 Postgres 가 UPDATE 한 문장 안에서 원자적으로 한다 — 마이그레이션이 필요 없고(사람 게이트 없음), `recordUnmatched` 에서 기존 행을 읽어 판정하는 안(읽기-쓰기 경합)보다 정확하다.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] 늦은 정정확인 M → 기록 경로 → 조건부 UPDATE — ws ㊹ ㊺ RED 재현 → 상태 단조성 GREEN</name>
  <files>relay/src/order/notice-status.ts, relay/src/store/orders.ts, relay/tests/helpers/fake-dma-orders.ts, relay/tests/ws-order.test.ts, relay/tests/order-store.test.ts</files>
  <read_first>
    - relay/src/order/notice-status.ts (전문 70줄 — `statusOf` · `filledQtyOf`)
    - relay/src/store/orders.ts (120-200 `OrderUpdate` · `OrderUpdateResult` / 300-430 `supabaseOrderSink` 전문 — `runUpdate` · `23505` 재시도 / 585-760 `OrderStore` · `flushNow` / 800-850 `#drain` / 860-902 `selectorOf` · `rowPatchOf`)
    - relay/src/ws/order-handler.ts (384-560 hub `order` 소비 → `recordUnmatched` / 960-1030 `finish`)
    - relay/tests/order-store.test.ts (500-720 `fakeDmaOrders` 전문 · `fixtureRows` · 731-800 「Supabase 쿼리 경계」 ⓵~⓺ 가 필터 배열을 단언하는 형식)
    - relay/tests/ws-order.test.ts (90-145 `mkOrderStore` · 196-230 `orderModify` · 232-300 `startHarness` · `authed` · `gatewaySocket` · 758-764 `restartHarness` · 1577-1612 ㊸)
    - relay/tests/helpers/frames.ts (275-340 `FakeOrderRespInput` · `buildOrderRespFrame`)
    - supabase/migrations/20260905120200_dma_orders.sql (55-70 `status` 컬럼 정의)
  </read_first>
  <behavior>
    - ws ㊹: 실제 `OrderStore(supabaseOrderSinks(가짜 PostgREST))` 로 하네스를 다시 띄운다 → 정정(org X = "0000012345", 10주) → E(org X · `requestKind:"Modify"` · 주문번호 Y = "0000012400" · 10주) → `order.result` `filled` → `flushNow()` → 정정 행 `status:"filled"` · `order_no:Y` · `notice_type:"E"` · `filled_qty:10` → 늦은 M(org X · 주문번호 Y · `requestKind:"Modify"`) → 조회 완료 대기 → `flushNow()` → 정정 행 여전히 `filled` · `notice_type:"E"` · `filled_qty:10` · `stats().flushedNoop` ≥ 1 (수정 전: `status:"accepted"` · `notice_type:"M"` = RED)
    - ws ㊺: 가짜 테이블에 원주문 행 X(사용자 A · 오늘 · `status:"partially_filled"` · `notice_type:"E"` · `filled_qty:6`)를 심고 ㊹ 와 같은 순서 → 늦은 M 의 주문번호가 X → `flushNow()` → 원주문 행 불변(`partially_filled` · E · 6) · 정정 행 `filled` (수정 전: 원주문 행 `accepted` · `M` = RED)
    - 기존 ㊸ · WR-03 (a)~(i) · order-store ⓵~⓺ ⓻ ⓽ 는 가짜 교체 뒤에도 무수정 green
  </behavior>
  <action>
**공용 가짜 PostgREST 로 옮긴다(RED 준비).** `relay/tests/order-store.test.ts` 의 `fakeDmaOrders` 와 그 타입(`FakeRow` · `FakeQuery` · `FakeFilter` · `FakePgError`)을 새 파일 `relay/tests/helpers/fake-dma-orders.ts` 로 **그대로** 옮기고 export 한 뒤, `order-store.test.ts` 는 그 파일에서 import 한다(정의는 한 곳 — 두 벌 금지). 옮기면서 세 가지만 더한다: ① `in` 필터 연산 — 행의 그 컬럼 값이 목록에 있는 행만 남긴다. `status` 가 없는 행은 `'requested'` 로 읽는다(DB `NOT NULL DEFAULT 'requested'` 의 거울 — 실제 DB 에는 `status` 없는 행이 존재할 수 없다는 주석) ② update 빌더의 `select(cols)` — 호출되면 결과 `data` 로 필터를 적용해 **실제로 병합된** 행들의 `{ id }` 배열을 돌려준다(호출되지 않으면 지금처럼 `data: null`) ③ insert 가 만드는 행에 `status: values.status ?? 'requested'` 를 싣는다. 기존 동작(필터 실적용 · `23505` 실계산 · 오류 주입 · 성공 update 병합)은 한 줄도 바꾸지 않는다.

**RED 먼저.** `ws-order.test.ts` 에 `<behavior>` ㊹ · ㊺ 를 ㊸ 뒤에 붙인다. 하네스는 `restartHarness` 와 같은 순서로 닫은 뒤 `startHarness({ orderStore: new OrderStore(supabaseOrderSinks(fake.supabase)) })` 로 다시 띄운다(`start()` 는 부르지 않는다 — 가짜 타이머 환경이라 `flushNow()` 를 명시 호출한다). 통보는 ㊸ 의 `gateway.pushOrderResp` 형태를 그대로 쓴다. 늦은 M 뒤에는 가짜의 `queries` 에서 조회(select) 쿼리가 끝난 것을 `waitFor` 로 확인한 다음 `flushNow()` 한다. **수정 전 코드로** 돌려 두 케이스가 RED 인 것(행 상태 `accepted` · `notice_type` `M` 이 단언 실패 메시지에 보이는 것)을 SUMMARY 표에 원문으로 적는다. 가짜 이관만으로 기존 케이스가 깨지지 않았음도 같은 실행으로 적는다.

**판정 함수 (R3-WR-01 · 판정의 유일 지점).** `relay/src/order/notice-status.ts` 에 상태 순위 표(`requested` 0 · `timeout` 0 · `accepted` 1 · `partially_filled` 2 · `filled` 3 · `cancelled` 3 · `rejected` 3)와 종결 집합(`filled` · `cancelled` · `rejected`)을 두고 `export function replaceableStatusesOf(next: DmaOrderStatus): readonly DmaOrderStatus[]` 를 만든다 — 「이 상태로 갱신해도 되는 **기존** 상태 집합」 = `next` 자신 ∪ (종결이 아니고 순위가 `next` 이하인 상태). 표는 7종 전부를 화이트리스트로 적는다(모르는 값을 지어내지 않는 이 모듈의 규율). 머리 주석에 근거(R3-WR-01 · 18-25 가 연 「E 가 먼저 정산 → 늦은 M」 경로 · timeout 은 「결과 모름」 이라 늦은 접수가 풀 수 있다 · 종결 상태 사이 이동 금지)를 적는다.

**조건부 UPDATE (R3-WR-01).** `supabaseOrderSink` 의 `runUpdate` 에서, 보낼 값에 `status` 문자열이 있으면 필터 체인 끝에 `.in("status", [...replaceableStatusesOf(status)])` 와 `.select("id")` 를 붙인다. 셀렉터 3축(`id` 또는 `order_no`+`user_id`+당일)은 그대로이고 조립 지점도 `runUpdate` 하나다 — 최초 시도와 `23505` 재시도가 같은 필터를 받는다. `status` 가 없는 값은 쿼리 모양을 바꾸지 않는다. 가드가 걸린 쿼리가 오류 없이 0행을 돌려주면(`data` 가 빈 배열) warn 로그 1줄 — 메시지 「[orders] 상태 단조성 — 더 진행된 행을 되돌리는 갱신이라 반영하지 않았다(또는 대상 행 없음)」, 필드는 `column`(셀렉터) · `status`(목표) · `noticeType`(값의 `notice_type`, 없으면 null)뿐 — 를 남기고 `{ applied: false }` 를 돌려준다. `23505` 재시도가 0행이면 같은 규칙을 따르고, 「order_no 만 포기하고 나머지는 반영했다」 로그는 실제로 반영했을 때만 남긴다. `supabaseOrderSink` 머리 주석에 ★ 「상태 단조성 (R3-WR-01)」 문단을 더한다 — 왜 relay 필터인가(원자적 · 마이그레이션 불요 · 읽기-쓰기 경합 없음)와 막힌 갱신이 통째로 반영되지 않는 이유(늦은 확인이 `notice_type` · `message` 도 덮지 않게).
  </action>
  <reversibility rating="reversible">relay 쿼리 필터 한 곳과 순수 함수 하나다. 스키마 · 저장 데이터를 바꾸지 않으므로 커밋을 되돌리면 이전 동작으로 돌아간다.</reversibility>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && cd relay && npx vitest run ws-order order-store</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄 passed 수가 0 — ㊹ ㊺ 에서 행 상태가 accepted 로 바뀌거나 기존 ㊸ · ⓵~⓽ 가 깨지면 실패한다 (`pnpm --filter … test -- <필터>` 는 필터를 무시하므로 쓰지 않는다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 수정 전 코드로 돌린 ㊹ · ㊺ 의 RED 원문(행 `status` `accepted` · `notice_type` `M`)과 수정 후 GREEN 이 표로 있다
    - ㊸ 과 `narrowPending` WR-03 describe 가 무수정 green — `git diff "$PLAN_HEAD_BEFORE"..HEAD -- relay/tests/ws-order.test.ts` 에서 기존 `it(` 블록 본문의 삭제 줄이 0
    - `grep -n "export function replaceableStatusesOf" relay/src/order/notice-status.ts` 1줄 · `grep -n "replaceableStatusesOf" relay/src/store/orders.ts` 가 `runUpdate` 안의 줄을 보여 준다
    - `grep -c "function fakeDmaOrders" relay/tests/helpers/fake-dma-orders.ts` = 1 · `grep -c "function fakeDmaOrders" relay/tests/order-store.test.ts` = 0 (가짜 정의가 한 곳)
    - `git diff --name-only "$PLAN_HEAD_BEFORE"..HEAD -- supabase/` 출력 없음 (마이그레이션 0)
  </acceptance_criteria>
  <done>체결로 끝난 정정 행과 부분체결된 원주문 행이, 뒤늦게 도착한 정정확인 한 통으로 「접수」 가 되지 않는다 — 막힌 사실은 로그와 카운터에 남는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 상태 단조성 전이표 · 로그 · 카운터 · 재시도 경로 회귀 + 기록 경로 주석</name>
  <files>relay/tests/order-store.test.ts, relay/src/ws/order-handler.ts</files>
  <read_first>
    - relay/tests/order-store.test.ts (Task 1 결과 — 106-235 `OrderStore` 큐 케이스 형식 · 819-1056 「23505 수렴」 ⓻~⓽-c · 1057-1150 로그 유출 케이스의 `logger` 스파이 형식)
    - relay/tests/helpers/fake-dma-orders.ts (Task 1 결과)
    - relay/src/order/notice-status.ts · relay/src/store/orders.ts (Task 1 결과)
    - relay/src/ws/order-handler.ts (430-560 `recordUnmatched` 머리 주석 · 수동 분기)
  </read_first>
  <behavior>
    - 전이표 전수(7 × 7 = 49칸): 기존 행 상태 × 갱신 상태마다 가짜 행 하나에 `supabaseOrderSink` 를 부른다 — 허용 칸은 행 `status` 가 갱신 값이 되고, 금지 칸은 행이 그대로이고 반환이 `{ applied: false }`. 기대값은 **손으로 쓴 49칸 표**다(구현 함수로 기대값을 만들지 않는다 — 순환 금지). 금지 칸의 예: filled → accepted · partially_filled → accepted · cancelled → filled · filled → partially_filled · rejected → accepted. 허용 칸의 예: timeout → accepted · accepted → accepted · requested → timeout
    - 막힌 갱신 1건 → warn 1회, 로그 객체 키가 `column` · `status` · `noticeType` 뿐이고 직렬화한 로그에 주문번호 원문 · 계좌번호가 없다
    - `OrderStore.flushNow()` 로 막힌 갱신 1건 → `stats().flushedNoop` +1 · `dropped` 0 · `retried` 0
    - `status` 없는 갱신(`filled_qty` 만) → 쿼리 필터에 `status` `in` 이 없고 `select` 도 부르지 않으며 행에 반영된다
    - `23505` 재시도(주문번호 충돌 · 나머지 필드 반영) → 재시도 쿼리에도 같은 `status` `in` 필터가 있다
    - 접수 A 로 `accepted` 인 행에 정정확인 M(`accepted`) → 반영되어 `notice_type` 이 `M` 이 된다(과차단 대조군)
  </behavior>
  <action>
`<behavior>` 를 `order-store.test.ts` 새 describe 「상태 단조성 (R3-WR-01)」 로 쓴다. 가짜는 Task 1 의 공용 파일을 쓴다. 로그 스파이는 파일의 기존 유출 케이스(⓼-b ~ ⓼-e)와 같은 형식이다. 49칸 표는 테스트 파일 안의 상수로 두고, 각 칸의 기대를 순위 규칙(순위가 내려가지 않는다 · 종결 상태는 같은 값으로만)에서 손으로 채운다.

`relay/src/ws/order-handler.ts` 의 `recordUnmatched` 머리 주석에 한 문단을 더한다 — 이 분기로 오는 **늦은 확인 통보**(예: E 가 먼저 정정 대기를 정산한 뒤 도착한 M)는 이미 진행된 행을 찾을 수 있으며, 그 행을 되돌리지 않는 규칙은 여기가 아니라 갱신 sink 의 상태 단조성(`replaceableStatusesOf` · R3-WR-01)이 UPDATE 안에서 원자적으로 지킨다는 것, 그래서 이 함수는 기존 행 상태를 읽지 않는다는 것. 동작 변경은 0 이다.

테스트가 Task 1 규칙과 어긋나는 동작을 드러내면 `notice-status.ts` · `orders.ts` 를 규칙에 맞게 고치고 그 사실을 SUMMARY 에 적는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && cd relay && npx vitest run order-store ws-order</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄 passed 수가 0</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run test && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS", 또는 relay 통과 수가 523(18-32 기준선) 미만</fails_when>
  </verify>
  <acceptance_criteria>
    - describe 「상태 단조성 (R3-WR-01)」 이 `<behavior>` 전 항목을 덮고 green 이다 — 49칸 표가 테스트 파일에 상수로 있다
    - relay 전량 green · 통과 수 ≥ 523 + 이 플랜 신규 케이스 수
    - `grep -n "R3-WR-01" relay/src/ws/order-handler.ts` 가 `recordUnmatched` 머리 주석의 줄을 보여 주고, `git diff "$PLAN_HEAD_BEFORE"..HEAD -- relay/src/ws/order-handler.ts` 는 주석 줄만 바뀌었다
  </acceptance_criteria>
  <done>어느 상태에서 어느 상태로 갈 수 있는지가 49칸으로 고정됐고, 막힌 갱신이 운영 로그에서 식별된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트웨이 → relay (OrderResp 51) | 통보 순서는 게이트웨이 타이밍이 정한다 — 늦게 온 확인 통보가 감사 기록을 바꿀 수 있다 |
| relay → Supabase `dma_orders` (서비스롤) | 감사 기록의 유일한 쓰기 경로. UPDATE 조건이 곧 기록 규칙이다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-135 | Repudiation | 늦은 M 이 체결된 정정 행 · 원주문 행을 「접수」 로 되돌림 | medium | mitigate | `runUpdate` 조건부 `status` 필터(`replaceableStatusesOf`) · ws ㊹ ㊺ RED → GREEN |
| T-18-136 | Tampering | 가드가 정상 전이를 막아 행이 requested · timeout 에 갇힘 | medium | mitigate | 49칸 전이표 회귀(손으로 쓴 기대값) · timeout → accepted 허용 · 같은 상태 재기록 허용 대조군 |
| T-18-137 | Information Disclosure | 새 warn 로그 | low | mitigate | 필드는 셀렉터 컬럼 · 목표 상태 · 통보 종류뿐 — 주문번호 · 계좌 원문 없음(T-16-45) 단언 |
| T-18-138 | Elevation of Privilege | 필터 추가 중 `order_no` 셀렉터 3축(사용자 · 당일)이 약해져 남의 행 갱신 | high | mitigate | 셀렉터 조립 지점 `runUpdate` 하나 유지 · 테넌트 경계 ⓷ 무수정 green · 재시도 쿼리 필터 단언 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 |
</threat_model>

## Artifacts this phase produces

- **relay:** `replaceableStatusesOf`(notice-status.ts — 상태 순위 · 종결 집합 화이트리스트) · `supabaseOrderSink` 조건부 `status` 필터 + `select("id")` + 막힘 warn 로그 + `{applied:false}` · `recordUnmatched` 주석 문단
- **테스트 인프라:** `relay/tests/helpers/fake-dma-orders.ts`(공용 가짜 PostgREST — `in` 필터 · update `select` · insert 기본 `status`)
- **테스트:** `ws-order.test.ts` ㊹ ㊺ · `order-store.test.ts` describe 「상태 단조성 (R3-WR-01)」(49칸 전이표 · 로그 · 카운터 · 재시도 · 과차단 대조군)

<verification>
- ㊹ ㊺ RED(수정 전) → GREEN · ㊸ · WR-03 (a)~(i) · ⓵~⓽ 무수정 green
- 49칸 전이표 green · 막힘 로그에 주문번호 · 계좌 없음 · `flushedNoop` 가산
- relay 전량 ≥ 523 green · typecheck 클린 · `supabase/` 무변경
</verification>

<success_criteria>
- 늦게 도착한 통보가 감사 행의 상태를 되돌릴 수 있는 쓰기 경로가 relay 에 없다
- 정상 전이는 전부 그대로 반영되고, 막힌 갱신은 로그 · 카운터로 보인다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-33-SUMMARY.md` when done
</output>
