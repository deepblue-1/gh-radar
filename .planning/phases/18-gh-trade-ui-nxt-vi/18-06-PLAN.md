---
phase: 18-gh-trade-ui-nxt-vi
plan: 06
type: execute
wave: 2
depends_on: ["18-02"]
files_modified:
  - webapp/src/components/trading/card/quote-grid-10.tsx
  - webapp/src/components/trading/card/card-header.tsx
  - webapp/src/components/trading/card/strategy-card.tsx
  - webapp/src/components/trading/limit-chaser-client.tsx
  - webapp/src/components/trading/__tests__/quote-grid-10.test.tsx
  - webapp/src/components/trading/__tests__/card-header.test.tsx
  - webapp/src/components/trading/__tests__/strategy-card.test.tsx
autonomous: true
requirements: [TRADE-09]

estimate:
  tokens: 76000
  raw_tokens: 76000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-10: 카드 헤더는 펼침·접힘 공통으로 캐럿 · 종목명 · 코드 · KRX|NXT 세그먼트 · 현재가 · 등락률 · LED 3칩(매수/매도/취소) · ⓘ · ✕ 를 그린다. **매수/매도/한방 스위치는 헤더에 없다** — 접힌 카드의 스위치 상태는 LED 로 읽는다 (TRADE-09)"
    - "D-10: 거래소 세그먼트는 **등록 전 카드(스위치 전부 OFF ∧ 서버 전략 없음)에서만** 활성이고, 등록 후에는 `disabled` + `aria-disabled=\"true\"` + `title` 「거래소는 전략 키의 일부라 등록 후에는 바꿀 수 없어요」 로 잠긴다 (TRADE-09)"
    - "D-11: 종목정보 10칸은 기존 `lc-quote-grid` 라벨 그대로(기준·시가·고가·저가·상한·하한·상승VI·거래·시총·발행1%)이고 **펼친 카드에만** 그려진다. 배치는 카드 폭 기준으로 §2.2b 밴드를 따른다 (TRADE-09)"
    - "D-28: `@container/lc` 선언이 페이지 본문에서 **카드 래퍼로 이동**한다 — 선언 위치만 바뀌고 `@min-[700px]/lc:`·`@min-[830px]/lc:`·`@min-[992px]/lc:` 유틸리티는 한 글자도 바뀌지 않는다. 컨테이너 이름을 신설하지 않는다(§2.2b 소비처가 둘이 되면 갈라진다) (TRADE-09)"
    - "D-26: 카드 헤더 한 줄 접힘 경계(카드 폭 760px)는 §2.2b 4밴드의 네 번째 경계가 **아니다** — 헤더 컴포넌트의 로컬 규칙이며 코드 주석이 「밴드 표와 무관」을 명시한다 (TRADE-09)"
    - "D-27: 카드는 서버 에코를 **자기 `key`(ISIN:계좌:거래소)로만** 필터한다. 작업대 상위에서 에코를 받아 카드에 분배하는 구조를 만들지 않는다 — 분배 로직이 상관의 두 번째 벌이 된다 (TRADE-09)"
    - "D-09: 펼침/접힘 토글은 `aria-expanded`·`aria-controls` 를 가진 헤더 버튼이고, 헤더 안의 세그먼트·LED·ⓘ·✕ 클릭은 전파를 막는다. 상태 변경은 재렌더이며 애니메이션이 없고, 토글 후 포커스는 누른 헤더 버튼으로 돌아온다 (TRADE-09)"
    - "E7 loading: 시세 미수신이면 현재가·등락률이 「—」, 서버 전략이 없으면 LED 「OFF」 3개다. 캐럿·세그먼트·ⓘ·✕ 는 즉시 활성 (TRADE-09)"
    - "E7 error: 헤더 자체의 오류 표면은 없다. ✕ 는 등록된 전략이면 기존 삭제 확인 다이얼로그를 거치고, 거부되면 카드를 유지하고 인라인 `role=\"status\"` 로 고지한다. 거래소 잠김 사유는 `title` 이 말한다 (TRADE-09)"
    - "E7 overflow: 카드 폭 760 미만에서 헤더가 2줄(`l1`/`l2`)로 접히고, 폰 밴드(카드 ≤699)에서는 종목명/코드가 2줄로 접힌다 (TRADE-09)"
    - "E7 long-text: 종목명은 1줄 ellipsis 이고 전체는 `title` 에 담긴다. 코드는 6자 mono 고정 (TRADE-09)"
    - "E8 empty: 시세 수신 전 10칸 값은 「—」이고 라벨 10개는 항상 그려진다 (TRADE-09)"
    - "E8 loading: 값 「—」 유지 — 스켈레톤·「불러오는 중」 위장 없음 (TRADE-09)"
    - "E8 populated: 기준 대비 up/down/flat 색 · 상한과 상승VI 는 `--up` 고정 · 하한은 `--down` 고정 · 거래/시총/발행1% 는 중립 (TRADE-09)"
    - "E8 partial: 10칸 중 일부 필드가 없으면(예: 상승VI 미제공) 그 칸만 「—」이고 배치·칸 수는 불변이다 (TRADE-09)"
  artifacts:
    - webapp/src/components/trading/card/strategy-card.tsx
    - webapp/src/components/trading/card/card-header.tsx
    - webapp/src/components/trading/card/quote-grid-10.tsx
  key_links:
    - "`strategy-card.tsx` 의 `@container/lc` 선언 ↔ `limit-chaser-form.tsx`·`orderbook-ladder.tsx` 의 `@min-[Npx]/lc:` 유틸리티 43개 — 선언이 카드로 오는 순간 그 유틸리티들이 카드 폭을 잰다"
    - "카드의 `key = strategyKey(isin, accountNo, exchange)` ↔ 서버 `limitChasers` 배열의 `find(c => c.key === key)` — 카드 N개에서 이 필터가 카드별로 유지돼야 한다"
    - "`latchLedStateOf`(latch-led.tsx) ↔ 헤더 LED 3칩 — Phase 17 규칙표를 다시 쓰지 않는다"
  prohibitions:
    - "MUST NOT 한 카드의 에코·더티·주문 상관이 다른 카드에 섞이게 한다 — 카드는 자기 `key` 로만 필터한다"
    - "MUST NOT LED 상태·전략 배지·더티 판정을 카드용으로 새로 쓴다 — `latchLedStateOf`·`strategyStatusOf`·`dirtyFieldsOf` 를 그대로 부른다"
    - "MUST NOT 카드 안에 뷰포트 브레이크포인트를 섞는다 — §2.2b 가 기록한 255px 역전이 되살아난다"
    - "MUST NOT 준비되지 않은 값을 스켈레톤·「불러오는 중」으로 위장한다 — 값이 없으면 「—」다"
---

<objective>
카드 1장의 **골격 세 조각**을 만든다 — 종목정보 10칸(`quote-grid-10.tsx`), 헤더(`card-header.tsx`), 그리고 이 둘을 담고 `@container/lc` 를 소유하는 카드 래퍼(`strategy-card.tsx`). 기존 `LimitChaserSurface` 가 이미 「한 전략」 단위이므로 이 작업의 본질은 **컴포넌트 승격과 컨테이너 선언 이동**이다.

Purpose: 카드 격자·본문·수동주문이 전부 이 골격 위에 얹힌다. 특히 `@container/lc` 선언이 카드로 옮겨지는 순간 기존 반응형 유틸리티 43개가 **한 글자도 안 바뀐 채** 카드 폭을 재기 시작한다 — 이것이 §2.2b 표를 복사하지 않고 두 표면을 지탱하는 방법이다.
Output: `components/trading/card/` 신설 3파일 + `limit-chaser-client.tsx` 의 해당 슬라이스 제거.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-PATTERNS.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `quote-grid-10.tsx` — 종목정보 10칸 추출</name>
  <files>webapp/src/components/trading/card/quote-grid-10.tsx, webapp/src/components/trading/limit-chaser-client.tsx, webapp/src/components/trading/__tests__/quote-grid-10.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/limit-chaser-client.tsx (`QuoteCell`·`priceText`·`priceTone` 와 `lc-quote-grid` 마크업 · 라벨 10개 원문 · `krx_close_price>0 → '종가'` 규율 주석)
    - webapp/src/lib/quote-format.ts 및 webapp/src/lib/__tests__/quote-format.test.ts (숫자·단위 축약 포맷 헬퍼 — 새로 만들지 않는다)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §카드 계약의 10칸 문단 · §Color 의 가격 방향 축
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html (10칸 배치 CSS `:444-445`)
  </read_first>
  <behavior>
    - 시세가 없으면 10칸 값이 전부 「—」이고 라벨 10개는 그려진다.
    - 일부 필드만 없으면 그 칸만 「—」이고 칸 수는 10 으로 유지된다.
    - 기준 대비 상승 값은 `--up`, 하락은 `--down`, 같으면 `--flat` 톤을 받는다.
    - 상한·상승VI 칸은 값과 무관하게 `--up` 고정, 하한 칸은 `--down` 고정.
    - 거래·시총·발행1% 칸은 중립 톤.
    - 기준 칸은 `krx_close_price > 0` 일 때 라벨이 「종가」로 바뀐다(Phase 17 규율 — 스냅샷 props 로 폴백하지 않는다).
  </behavior>
  <action>
`webapp/src/components/trading/card/quote-grid-10.tsx` 를 신설하고 `limit-chaser-client.tsx` 의 `QuoteCell`·`priceText`·`priceTone` 과 `lc-quote-grid` 마크업을 **그대로** 옮긴다. 라벨 10개 문자열(기준 · 시가 · 고가 · 저가 · 상한 · 하한 · 상승VI · 거래 · 시총 · 발행1%)과 색 규칙을 바꾸지 않는다. 숫자 포맷은 기존 `quote-format` 헬퍼를 그대로 쓴다 — 카드 전용 포맷 함수를 새로 만들지 않는다(같은 값이 카드·스트립·표에서 다른 문자열이 되면 안 된다).

서버 진실 취급 주석을 함께 가져온다 — 기준 칸의 「오늘 종가가 아니면 `0` 이다 / 스냅샷 props 로 폴백하지 않는다 / **벽시계로 판정하지 않는다**」 ★ 블록 전문. 이 주석이 이 컴포넌트가 왜 폴백을 안 하는지의 유일한 기록이다.

배치는 §2.2b 밴드를 **카드 폭 기준**으로 따른다 — 기존 `@min-[Npx]/lc:` 유틸리티를 그대로 쓰고 새 경계 숫자를 쓰지 않는다. `limit-chaser-client.tsx` 쪽 원본 정의와 마크업은 제거하고 새 컴포넌트를 import 해 쓴다(정의가 두 벌이 되지 않게).

테스트 `__tests__/quote-grid-10.test.tsx` 는 `<behavior>` 6케이스를 덮는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- quote-grid-10</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "lc-quote-grid" webapp/src/components/trading/card/quote-grid-10.tsx</automated>
    <fails_when>카운트가 0 — 기존 격자 클래스/라벨 세트를 그대로 옮기지 않았다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `webapp/src/components/trading/card/quote-grid-10.tsx` 가 라벨 10개를 전부 포함한다
    - 값 부재 시 「—」, 칸 수 10 유지가 단언된다
    - 상한·상승VI `--up` 고정 · 하한 `--down` 고정이 단언된다
    - `limit-chaser-client.tsx` 에 10칸 정의가 남아 있지 않다(정의 1벌)
    - 카드 전용 숫자 포맷 함수를 새로 만들지 않았다
  </acceptance_criteria>
  <done>종목정보 10칸이 카드·호가 탭 어디서든 같은 컴포넌트로 같은 문자열을 그린다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `card-header.tsx` — 헤더 한 줄/두 줄 · 거래소 세그먼트 잠김 · LED 3칩</name>
  <files>webapp/src/components/trading/card/card-header.tsx, webapp/src/components/trading/__tests__/card-header.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/limit-chaser-client.tsx (`StrategyStatus`·`strategyStatusOf`·`StatusBar`·`Dot` — 카드 헤더로 갈 부분과 작업대 상태줄로 갈 부분의 경계)
    - webapp/src/components/trading/latch-led.tsx (`latchLedStateOf` 규칙표 — 다시 쓰지 않는다)
    - webapp/src/components/trading/strategy-badge.tsx (배지 6종 · 거래소 태그 KRX 테두리형 / NXT 채움형)
    - webapp/src/components/ui/toggle-group.tsx (세그먼트 프리미티브)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §카드 계약 헤더 문단 · §Copywriting 카드 표(ⓘ/✕/세그먼트/LED aria-label 원문) · §접근성 계약 키보드 핸들링 표
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html (헤더 마크업 `:1062-1070` · 로컬 경계 `:382` · 종목명/현재가 타이포 `:386,395`)
  </read_first>
  <behavior>
    - 서버 전략이 없고 스위치가 전부 OFF 인 카드 → 거래소 세그먼트가 활성.
    - 서버 전략이 있는 카드 → 세그먼트 `disabled` + `aria-disabled="true"` + `title` 이 잠김 사유 문구.
    - 시세 미수신 → 현재가·등락률 「—」.
    - 서버 전략 없음 → LED 3칩이 전부 「OFF」.
    - 헤더 토글 버튼이 `aria-expanded` 를 갖고, 클릭하면 `onToggle` 이 1회 불린다.
    - 세그먼트·LED·ⓘ·✕ 를 클릭하면 `onToggle` 이 **불리지 않는다**(전파 차단).
    - ✕ 의 `aria-label` 이 「{종목명} 카드 닫기」.
    - `code` 가 없는 카드(돌파 유래 · relay lookup 실패)는 ⓘ 가 `disabled`.
    - 긴 종목명이 1줄 ellipsis 이고 `title` 에 전체가 담긴다.
    - 매수/매도/한방 스위치가 헤더 DOM 에 없다.
  </behavior>
  <action>
`webapp/src/components/trading/card/card-header.tsx` 를 신설한다. 구조는 목업 `:1062-1070` 이 정본이다 — `l1` 줄에 캐럿 ▶(펼치면 90° 회전, 150ms) · 종목명(15px/700, ellipsis) · 코드(11px mono) · KRX|NXT 세그먼트 · 우측 현재가+등락률, `l2` 줄에 LED 3칩 · ⓘ · ✕. 카드 폭 760px 이상에서 두 줄이 한 줄로 붙는다 — 이 경계는 **컴포넌트 로컬 컨테이너 규칙**이고 주석에 「§2.2b 밴드 표와 무관 — 헤더 한 줄 배치 전용의 로컬 경계다」를 명시한다. 폰 밴드(카드 ≤699)에서는 종목명/코드가 2줄로 접힌다(`line-height:1.15`, 코드 10px).

LED 3칩은 기존 `LatchLed` 를 그대로 쓴다 — `latchLedStateOf` 규칙표를 다시 쓰지 않는다. 거래소 태그·전략 배지는 `StrategyBadge` 를 쓴다. 상태 배지 파생은 기존 `strategyStatusOf` 를 import 해 쓰고 새 판정식을 만들지 않는다.

거래소 세그먼트는 `ToggleGroup type="single"` 로 만들고 `role="group"` + `aria-label="거래소"`, 선택 버튼 `aria-pressed="true"`. 잠김 조건은 「서버 전략이 있다」이고(= 등록됨), 잠기면 `disabled` + `aria-disabled="true"` + `title` 에 UI-SPEC §Copywriting 의 원문 잠김 사유를 넣는다. 등록된 전략의 거래소 변경은 이 phase 범위 밖이다(이연).

헤더 전체를 클릭 영역으로 두되 그 안의 세그먼트·LED·ⓘ·✕ 클릭은 `stopPropagation` 으로 전파를 막는다. 토글 버튼은 `aria-expanded`·`aria-controls` 를 갖고, 토글 후 포커스를 **누른 헤더 버튼으로** 되돌린다(접기로 카드가 스택으로 이동하면 포커스가 사라진다).

문구는 UI-SPEC §Copywriting 카드 표의 원문을 그대로 쓴다(ⓘ `title`/`aria-label`, ✕ `title`/`aria-label`, LED 라벨·값·`aria-label`). ⓘ 는 `code` 가 없으면 `disabled` 다(D-30 — relay lookup 실패 시 팝업을 열 수 없다).

**매수/매도/한방 스위치를 헤더에 두지 않는다**(D-12 — 스위치는 본문 각 그룹 제목줄 우측). 접힌 카드의 스위치 상태는 LED 로 읽는다.

테스트 `__tests__/card-header.test.tsx` 는 `<behavior>` 10케이스를 RTL 로 덮는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- card-header</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "latchLedStateOf\|LatchLed" webapp/src/components/trading/card/card-header.tsx</automated>
    <fails_when>카운트가 0 — 기존 LED 판정/컴포넌트를 쓰지 않고 새로 그렸다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "\b(sm|md|lg|xl):" webapp/src/components/trading/card/card-header.tsx</automated>
    <fails_when>non-zero exit — 카드 헤더에 뷰포트 브레이크포인트가 섞였다(D-28 위반)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 거래소 세그먼트 잠김 3속성(`disabled`·`aria-disabled`·`title`)이 등록된 카드에서 단언된다
    - 세그먼트/LED/ⓘ/✕ 클릭이 펼침 토글을 일으키지 않음이 단언된다
    - 헤더 DOM 에 매수/매도/한방 스위치가 없음이 단언된다
    - `code` 없는 카드에서 ⓘ 가 disabled 임이 단언된다
    - 헤더 파일에 뷰포트 브레이크포인트 클래스가 없다
    - 760 경계 주석이 「밴드 표와 무관」을 명시한다
  </acceptance_criteria>
  <done>카드 헤더가 접힌 상태에서도 전략 상태를 다 말하고, 등록 후 거래소를 바꿀 수 없다는 사실을 마우스·키보드·스크린리더 모두에게 말한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `strategy-card.tsx` — `LimitChaserSurface` 승격 + `@container/lc` 선언 이동</name>
  <files>webapp/src/components/trading/card/strategy-card.tsx, webapp/src/components/trading/limit-chaser-client.tsx, webapp/src/components/trading/__tests__/strategy-card.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/limit-chaser-client.tsx (`LimitChaserSurface` 전문 — relay 구독 · 에코 상관(`pendingRef`/`unacked`/`ackTimer`/`overwrittenRef`) · 자기 key `find` · 로그 · 더티 · 상수 `ECHO_BANNER_MS`/`ACK_TIMEOUT_MS` · `@container/lc` 선언 위치)
    - webapp/src/lib/limit-chaser.ts (`strategyKey`·`parseStrategyKey`(18-02 가 옮김)·`dirtyFieldsOf`·`DIRTY_COMPARED_FIELDS`)
    - webapp/src/lib/relay-provider.tsx (`useRelaySubscription` — 카드가 자기 키만 구독·해제)
    - webapp/src/lib/isin-labels.ts (`useIsinLabels` — 카드는 Map 자체가 아니라 `labels.get(isin)` 결과 문자열만 prop 으로 받는다)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md §Pattern 2 · §예 3 · §Pitfall 9 · §Pitfall 10
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-PATTERNS.md §`strategy-card.tsx`
  </read_first>
  <behavior>
    - 카드가 마운트되면 자기 `isin`·`exchange` 로 `subscribe` 가 1회 불리고, 언마운트에서 같은 키가 `unsubscribe` 된다.
    - 서버 `limitChasers` 배열에 다른 키의 전략이 여럿 있어도 카드는 **자기 key 의 것만** 읽는다.
    - 카드 A 의 에코가 카드 B 의 더티·pending 을 건드리지 않는다(두 카드 동시 렌더 테스트).
    - `open=false` 면 10칸과 본문이 렌더되지 않고 헤더만 남는다.
    - 카드 래퍼 요소가 `@container/lc` 클래스를 갖는다.
    - 카드 폭을 좁혀도 뷰포트 폭과 무관하게 내부 배치가 바뀐다(컨테이너 기준).
    - `useIsinLabels` 의 Map 이 새 인스턴스로 바뀌어도 이름이 같으면 카드가 재렌더되지 않는다.
  </behavior>
  <action>
`webapp/src/components/trading/card/strategy-card.tsx` 를 신설하고 `limit-chaser-client.tsx` 의 `LimitChaserSurface` 를 **컴포넌트로 승격**한다. `routeKey` prop 은 `initialKey` 가 되고, 계좌는 위(작업대 상태줄)에서 내려받는다. 함께 옮길 것: 상수 `ECHO_BANNER_MS`·`ACK_TIMEOUT_MS`, relay 구독 호출, 에코 상관 refs, 로그 수집, 더티 판정 연결.

**컨테이너 선언은 이동이다(복제 아님).** 현행 `@container/lc` 래퍼를 카드 `<article data-slot="strategy-card">` 로 옮긴다. 그러면 `limit-chaser-client.tsx`·`limit-chaser-form.tsx`·`orderbook-ladder.tsx` 의 `@min-[Npx]/lc:` 유틸리티 43개가 **한 글자도 안 바뀐 채** 카드 폭을 잰다(CSS 컨테이너는 가장 가까운 동명 조상을 찾는다). 새 컨테이너 이름을 만들지 않는다.

**에코 상관은 카드별로 유지한다.** 카드가 여럿이 되므로 `pendingRef`·`unacked`·`ackTimer`·`overwrittenRef` 를 인스턴스별로 두고, 서버 전략은 `limitChasers.find(c => c.key === key)` 로 **자기 것만** 고른다. 작업대 상위에서 에코를 받아 카드에 분배하는 구조를 만들지 않는다 — 분배 로직이 상관의 두 번째 벌이 되고, 그 순간 「다른 카드의 에코로 내 더티가 지워지는」 경로가 생긴다.

**재렌더 예산:** 카드는 `useIsinLabels()` 의 Map 을 직접 구독하지 않고 `labels.get(isin)` 결과(문자열)만 prop 으로 받는다 — 계좌 델타가 100ms 마다 오면 Map 이 매번 새로 만들어져 카드 N개가 전부 재렌더된다.

이 태스크에서 `strategy-card.tsx` 는 헤더 + 10칸까지만 조립한다. 본문(좌 호가 | 우 옵션 4그룹)은 18-10 이 `card-body.tsx` 로 채우므로, 여기서는 본문 자리에 prop 으로 받은 `children`(또는 명시적 `body` slot)을 두고 `open` 일 때만 렌더한다.

`limit-chaser-client.tsx` 에서 옮겨간 부분을 제거한다. 다만 **이 파일과 `/trading/limit-chaser/*` 라우트는 18-12 까지 동작해야 하므로**, 이 태스크에서는 파일을 지우지 않고 새 `StrategyCard` 를 쓰도록 재배선해 기존 화면이 계속 뜨게 한다(기존 e2e 가 18-13 까지 green 이어야 한다).

테스트 `__tests__/strategy-card.test.tsx` 는 `<behavior>` 7케이스를 RTL 로 덮는다. 특히 **두 카드를 동시에 렌더해 에코 격리**를 단언한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- strategy-card</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "@container/lc" webapp/src/components/trading/card/strategy-card.tsx</automated>
    <fails_when>카운트가 0 — 컨테이너 선언이 카드로 옮겨지지 않았다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -rc "@min-\[700px\]/lc:" webapp/src/components/trading/limit-chaser-form.tsx webapp/src/components/orderbook/orderbook-ladder.tsx</automated>
    <fails_when>어느 파일이든 카운트가 0 — 기존 반응형 유틸리티가 지워졌다(이동이어야 하는데 재작성했다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test</automated>
    <fails_when>non-zero exit, 또는 통과 수가 Phase 17 기준선 1008 보다 작다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `strategy-card.tsx` 의 루트 요소가 `@container/lc` 를 갖는다
    - `limit-chaser-form.tsx`·`orderbook-ladder.tsx` 의 `@min-[Npx]/lc:` 유틸리티 수가 이 phase 이전과 같다(이동이지 재작성이 아니다)
    - 두 카드 동시 렌더에서 에코 격리가 단언된다
    - 카드가 `limitChasers` 를 자기 key 로만 `find` 한다
    - 기존 `/trading/limit-chaser/*` 화면이 계속 동작한다(기존 단위 테스트 green)
    - webapp 테스트 전량 통과 수가 1008 이상
  </acceptance_criteria>
  <done>카드 1장이 독립적으로 구독하고 자기 에코만 읽으며, 카드 폭이 §2.2b 밴드를 재는 유일한 자가 된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay wss → 브라우저 (60/64 전략 에코 · 시세) | 카드 N개가 한 소켓을 공유한다. 프레임이 어느 카드 것인지 가르는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-25 | Tampering | 카드 간 에코 교차 오염 | critical | mitigate | 카드가 자기 `key` 로만 `find`. 상위 분배 구조 금지. 두 카드 동시 렌더 격리 테스트가 단언 |
| T-18-26 | Tampering | 다른 종목 호가로 주문 | critical | mitigate | 카드가 자기 `isin`/`exchange` 로만 구독하고 자기 키의 시세만 읽는다(T-15-40/T-16-02 승계) |
| T-18-27 | Tampering | 등록 후 거래소 변경 | high | mitigate | 세그먼트 잠김 3속성. 거래소는 전략 키의 일부라 바뀌면 다른 전략을 조작하게 된다 |
| T-18-28 | Spoofing | LED 오점등 | high | mitigate | `latchLedStateOf` 규칙표 한 곳만 사용. 새 규칙표 금지 |
| T-18-29 | Denial of Service | 카드 N개 재렌더 폭주 | medium | mitigate | `useIsinLabels` Map 을 카드가 구독하지 않고 문자열 prop 만 받는다 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 phase 는 새 패키지를 설치하지 않는다(RESEARCH §Package Legitimacy Audit 표 비어 있음) |
</threat_model>

## Artifacts this phase produces

새로 만드는 심볼·파일 (phase 전체 — 드리프트 검증에서 제외되어야 한다):

- **마이그레이션:** `supabase/migrations/20260921120000_dma_orders_modify_offhours.sql` · 컬럼 `dma_orders.piece_count` · `dma_orders.krx_session`
- **shared:** `RelayOrderModifyMsg` · `RelayKrxSession` · `OrderType` 의 `"M"` 멤버 · `RelayOrderNewMsg.pieceCount?` · `RelayOrderNewMsg.krxSession?` · `RelayRateCrossItem.name?` · `RelayRateCrossItem.code?`
- **relay:** `RelayOrderModifySchema` · `OrderInsertRow.pieceCount?` · `OrderInsertRow.krxSession?` · 돌파 항목 보강 private 메서드 · `relay/src/ws/__tests__/protocol.test.ts`
- **webapp lib:** `RelayOrderRequest.kind` 의 `"modify"` · `queued-window.ts`(`OrderButtonMode`·`ManualOrderAffordance`·`affordanceOf`) · `breakout-list.ts`(`kstDateKey`·`shouldRemoveBreakout`·`breakoutRowsFrom`·`BreakoutRow`·키 상수 4종·`REMOVE_MARGIN_PCT`·`ARM_GRACE_MS`·`HIGHLIGHT_MS`) · `use-breakout-quotes.ts`(`useBreakoutQuotes`·`MAX_BREAKOUT_SUBS`) · `alert-tone.ts`(`playBreakoutTone`·`isTonePlaybackBlocked`·`resumeToneContext`) · `limit-chaser.ts` 로 이동한 `parseStrategyKey`
- **webapp 컴포넌트:** `components/trading/workbench/` 의 `TradingWorkbench`·`WorkbenchStatusBar`·`ViSettingsRows`·`ViTriggerStrip`·`BreakoutStrip`·`StockAddBar`·`CardGrid`·`SharedPanels` · `components/trading/card/` 의 `StrategyCard`·`CardHeader`·`QuoteGrid10`·`CardBody`·`ManualOrderForm`·`StockInfoModal`
- **라우트:** `webapp/src/app/trading/page.tsx`
- **CSS 토큰:** `--new-bg` · `--new-bd` (globals.css §9 라이트/다크 양쪽)
- **e2e:** `webapp/e2e/specs/trading-workbench.spec.ts`

<verification>
- `pnpm --filter @gh-radar/webapp test` 전량 exit 0 (통과 수 1008 이상)
- `pnpm --filter @gh-radar/webapp run typecheck` exit 0
- `@min-[Npx]/lc:` 유틸리티 개수가 이 phase 이전과 동일(선언만 이동)
</verification>

<success_criteria>
- 카드 3조각이 존재하고 각각 테스트가 있다
- `@container/lc` 선언이 카드 래퍼 한 곳에만 있다
- 카드 간 에코·구독 격리가 기계적으로 증명된다
- 기존 상따 화면이 아직 동작한다(라우트 제거는 18-12 몫)
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-06-SUMMARY.md` when done
</output>