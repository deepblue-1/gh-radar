---
phase: 18-gh-trade-ui-nxt-vi
plan: 26
type: execute
wave: 14
depends_on: ["18-23"]
gap_closure: true
files_modified:
  - relay/src/hub/subscription-hub.ts
  - relay/src/ws/fanout.ts
  - relay/tests/fanout.test.ts
  - relay/tests/strategy-hub.test.ts
  - packages/shared/src/relay.ts
  - webapp/src/lib/use-relay-socket.ts
  - webapp/src/lib/__tests__/relay-socket.test.ts
  - webapp/src/components/trading/workbench/trading-workbench.tsx
  - webapp/src/components/trading/__tests__/trading-workbench.test.tsx
autonomous: true
requirements: [TRADE-09]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "deferred-items(18-22) · GC-IN-02: relay hub 가 사용자별로 「게이트웨이 64(LimitChaserList)를 받았는가」 를 기록한다. 인증 경로는 **알 때만** `lc.snap` 을 보낸다 — 모르면(콜드 세션 · 세션 교체 직후) 인증 직후 `lc.snap` 이 없고, 64 팬아웃이 그 연결의 첫 `lc.snap` 이 된다. 그래서 relay 가 내리는 `lc.snap` 은 언제나 확정 목록이다(빈 배열 = 「등록 전략 없음」 확정)"
    - "VI 설정(`getViTrigger` 3상태 · 모르면 안 보냄)과 같은 규율이 상따 목록에도 선다 — 세션 교체(`#clearCaches`)·`closeAll` 은 「받았음」 기록도 지운다. 사용자 간 기록이 섞이지 않는다(T-16-02)"
    - "D-02 · GC-IN-02: 작업대 `knowsRegistered` = 「이 연결에서 확정 `lc.snap` 을 받았는가」(`limitChaserSnapSeq > 0`) 하나다 — 목록이 비어 있는지는 보지 않는다. 등록 전략 0건 사용자의 오래된 포커스 키(①)와 전략을 모두 지운 뒤의 미스(②)는 보류되지 않고 버려진다"
    - "GC-IN-02 ③: 시퀀스 기준점은 세션이 `ready` 로 **전환**될 때(새 연결의 인증 ACK 포함) 0 으로 돌아간다 — 재연결·세션 재로그인 뒤 새 확정 `lc.snap` 이 오기 전의 미스는 옛 연결의 목록으로 버려지지 않고 보류된다. `ready → ready` 반복 프레임은 기준점을 건드리지 않는다"
    - "콜드 세션의 `?focus=K` 는 여전히 산다(D-02) — relay 가 64 전에 `lc.snap` 을 보내지 않으므로 첫 `lc.snap` 이 진짜 목록이고, 거기에 K 가 있으면 펼친다"
    - "protocol·shared 타입 모양은 바뀌지 않는다(주석만). `lc.snap` 은 relay↔webapp JSON 이고 gh-trade flatbuffers 스키마와 무관하다 — `sync-relay-schema.sh` 동기화 대상이 아니다(2026-09-22 planner 확인: gh-trade 저장소에 `lc.snap` 0건)"
  artifacts:
    - relay/src/hub/subscription-hub.ts
    - relay/src/ws/fanout.ts
    - webapp/src/lib/use-relay-socket.ts
    - webapp/src/components/trading/workbench/trading-workbench.tsx
  key_links:
    - "게이트웨이 64 → hub `#onLimitChaserList`(캐시 전량 교체 + 「받았음」 기록 + `lc.snap` 팬아웃) → 이미 붙은 연결 전부 / 새 연결은 인증 경로가 「받았음」 일 때만 캐시에서 `lc.snap`"
    - "브라우저: 인증 ACK(`state` ready 전환) → 리듀서 `limitChaserSnapSeq = 0` → 확정 `lc.snap` → +1 → 작업대 `knowsRegistered` → 포커스 보류/폐기"
    - "배포 순서 결합: webapp 의 새 `knowsRegistered`(빈 목록도 확정으로 읽음)는 relay 의 「받았음」 게이트가 배포된 뒤에만 안전하다 — 옛 relay 는 콜드 세션에서 빈 `lc.snap` 을 먼저 내리므로, webapp 만 먼저 나가면 콜드 세션 `?focus=` 가 진짜 목록 전에 버려진다(D-02 회귀)"
  prohibitions:
    - "MUST NOT `lc.snap` 에 새 필드(확정 플래그 등)를 더하거나 shared 타입 모양을 바꾼다 — 규율 변경(모르면 안 보냄)으로 닫는다"
    - "MUST NOT webapp 쪽에서 빈 목록을 계속 「모호」 로 다루는 우회를 남긴다 — 확정 여부는 relay 계약이 말한다"
    - "MUST NOT `?focus=` 마운트 1회 소비(D-02 · T-18-53)를 풀어 URL 변화마다 다시 읽는다"
    - "MUST NOT relay 배포 · git push 를 한다 — 이 플랜의 webapp 변경은 relay 배포 뒤에만 나가야 한다(18-32 가 순서를 기록한다)"
---

<objective>
18-22 가 이연한 relay 근본 수정(`#limitChaserKnown`)을 넣고, 그 계약 위에서 작업대 포커스 보류 판정(`knowsRegistered`)을 「이 연결에서 확정 64 를 받았는가」 하나로 줄인다 — GC-IN-02 의 세 틈을 한 슬라이스로 닫는다.

Purpose: relay 는 콜드 세션에서 게이트웨이 64 가 오기 전에 빈 hub 캐시를 `lc.snap []` 으로 내린다(「모름」 을 「전략 없음」 으로 말한다). 그래서 18-22 는 빈 목록을 모호하게 다뤘고, 그 대가로 ① 등록 전략 0건 사용자의 오래된 키가 무기한 보류되고 ② 전략을 모두 지운 뒤의 미스가 다시 보류되며 ③ 재연결 뒤 새 64 전의 미스가 옛 연결의 목록으로 버려진다(18-REVIEW-R2 GC-IN-02 · 18-VERIFICATION-R2 advisory · deferred-items.md). 오래된 키가 남으면 나중에 같은 키가 등록되는 순간 사용자 조작 없이 카드가 펼쳐진다(WR-07 원 결함).
Output: hub 「받았음」 기록 · 인증 경로 조건부 `lc.snap` · 리듀서 기준점 재설정 · `knowsRegistered` 단순화 · relay/webapp 회귀.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/deferred-items.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-22-SUMMARY.md

근거 인용(재확인 완료):
- `relay/src/ws/fanout.ts:594-599` — 인증 경로가 `lc.snap` 을 무조건 보낸다(「비어 있어도 1프레임」 주석). 바로 아래 VI 는 `getViTrigger` 가 `undefined` 면 보내지 않는다.
- `relay/src/hub/subscription-hub.ts:319` `#limitChasers` · `:595` `getLimitChasers` · `:1066-1077` `#onLimitChaserList`(64 전량 교체 + 팬아웃) · `:403-419` `attach`(세션 **객체** 교체 시에만 `#clearCaches`) · `:1356-1372` `#clearCaches` · `:696-710` `closeAll`. 세션 `ready` 마다 24/21/34 프리페치 → 64 가 온다(16-06 · strategy-hub ①).
- `relay/tests/fanout.test.ts:548-583` ⑭ — (B) 「아직 모른다」 연결에 `lc.snap []` 을 기대한다 = **결함을 진실로 잠근 단언**이다. 이 플랜에서 새 규율로 뒤집는다.
- `webapp/src/lib/use-relay-socket.ts:292-304` `limitChaserSnapSeq` 주석 · `:469-479` `state` 리듀서 · `:539-547` `lc.snap` 리듀서 · `:919-930` 인증 ACK 분기. `webapp/src/components/trading/workbench/trading-workbench.tsx:226-238` `knowsRegistered` · `:354` · `:402` 두 호출부.
- `webapp/src/components/trading/__tests__/trading-workbench.test.tsx:444-515` describe 「WR-07 …」 — 491 「콜드 세션 — 빈 스냅샷 뒤 진짜 64 에 K 가 있으면 펼친다」 는 옛 relay 우회를 잠근 케이스다. 새 계약(콜드 세션은 64 전에 `lc.snap` 없음 = `snapSeq 0`)으로 다시 쓴다.
- `lc.snap` 소비처: `strategy-status-card.tsx:319-335` `useSnapshotSeen` 은 `status === "ready"` 래치라 이 변경의 영향이 없다(콜드 세션의 짧은 빈 표시는 옛 빈 `lc.snap` 과 같은 모습). e2e `me.spec.ts:237` · `a11y.spec.ts:338` 은 「목록이 그려질 때」 를 기다리고, 스텁 게이트웨이는 24 에 64 로 자동 응답한다(`relay/tests/helpers/fake-gateway.ts:421`).
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] relay — 64 를 받았을 때만 인증 경로 `lc.snap` (RED: ⑭(B) 를 새 규율로 뒤집기)</name>
  <files>relay/src/hub/subscription-hub.ts, relay/src/ws/fanout.ts, relay/tests/fanout.test.ts, relay/tests/strategy-hub.test.ts, packages/shared/src/relay.ts</files>
  <read_first>
    - relay/src/hub/subscription-hub.ts (300-380 캐시 맵 선언과 3상태 주석 · 395-420 `attach` · 585-615 `getLimitChasers`·`getViTrigger` · 680-715 `stats`·`closeAll` · 1020-1080 60 에코·64 목록 · 1350-1390 `#clearCaches`)
    - relay/src/ws/fanout.ts (560-625 인증 경로 스냅샷 순서와 주석 · 980-1000 `getLimitChasers` 에코 조회 — 이 경로는 바꾸지 않는다)
    - relay/tests/fanout.test.ts (480-600 ⑬·⑭·⑭-2 — 하네스 `authed`·`open`·`framesOf`·`gateway.silenceLogin`·`respondLimitChaserList`)
    - relay/tests/strategy-hub.test.ts (131-300 · 439-482 ⑨ 세션 교체 · ⑩ 사용자 간 교차 — 허브 단위 하네스)
    - packages/shared/src/relay.ts (885-900 `RelayLimitChaserMsg` · `RelayLimitChaserSnapMsg` 주석)
  </read_first>
  <behavior>
    - ⑭(B) 다시 쓰기: 로그인이 끝나지 않아 64 를 받은 적 없는 사용자 B 의 새 탭 → `vi.list` 는 오지만 `lc.snap` 은 **0 프레임** (수정 전: `lc.snap []` 1 프레임 = RED)
    - ⑭(A) 유지: 64 가 빈 목록으로 온 사용자 → 새 탭은 `lc.snap []` 1 프레임(확정 「없음」)
    - ⑭-3 신설: 콜드 세션 — 탭이 64 전에 인증 → 인증 직후 `lc.snap` 없음 → 이후 게이트웨이 64(2건)가 오면 그 탭의 **첫** `lc.snap` 이 그 2건이다 (수정 전: 첫 `lc.snap` 이 빈 배열 = RED)
    - 허브 단위: 「받았음」 은 처음 false · 64 뒤 true · 60 에코만으로는 false · 세션 객체 교체 뒤 false · `closeAll` 뒤 false · 사용자 A 의 64 가 B 를 true 로 만들지 않는다
  </behavior>
  <action>
**RED 먼저.** `fanout.test.ts` ⑭(B) 의 `lc.snap` 단언을 새 규율(0 프레임)로 바꾸고 ⑭-3 을 더해 **수정 전 코드로** 돌려 RED 를 SUMMARY 에 적는다(⑭(B) 는 옛 단언이 결함을 잠그고 있었다는 사실도 함께 — 18-22 deferred 근거). `strategy-hub.test.ts` 에 허브 단위 케이스 「⑯ 64 를 받았는지 사용자별로 안다 — 60 에코·세션 교체·closeAll·사용자 교차」 를 더한다.

**hub (deferred-items 근본 수정 · GC-IN-02).** `SubscriptionHub` 에 사용자별 「64 수신」 집합(`#limitChaserKnown`, 키는 `userId`)을 두고, 공개 판정 `hasLimitChaserList(userId): boolean` 을 연다. `#onLimitChaserList` 가 캐시 교체와 **같은 자리에서** 기록하고(팬아웃 전), `#clearCaches(userId)` 와 `closeAll()` 이 지운다 — `#viTriggers` 3상태 주석과 같은 형식으로 「60 에코는 목록 전체를 말하지 않으므로 기록하지 않는다 · 모르면 `lc.snap` 을 지어내지 않는다」 를 적는다. `getLimitChasers` 는 그대로 둔다(에코 조회 `fanout.ts:995` 가 배열을 쓴다). `stats()` 에는 식별자 없는 개수 필드만 더해도 된다(선택 — 더하면 `/healthz` 소비처 grep 으로 영향 0 확인).

**fanout 인증 경로.** `lc.snap` 송신을 `hasLimitChaserList(userId)` 일 때만으로 바꾸고, 594-599 주석을 새 규율로 고친다 — 「`lc.snap` 은 hub 가 64 를 받았을 때만 캐시에서 내린다(빈 배열 = 확정 「없음」). 모르면 보내지 않고 곧 오는 64 팬아웃이 이 연결의 첫 `lc.snap` 이 된다 — VI 설정의 3상태 규율과 같다」. `vi.list` · `rate.cross.snap` 의 「비어 있어도 1프레임」 규율은 바꾸지 않는다(그 둘은 이 결함과 무관하다 — 바꾸지 않는 이유를 한 줄 남긴다).

**shared 주석.** `RelayLimitChaserSnapMsg` 머리 주석에 「relay 는 게이트웨이 64 를 받은 뒤에만 이 프레임을 내린다 — 받은 `lc.snap` 은 언제나 확정 목록이다(빈 배열 = 등록 전략 없음). 64 전에는 프레임이 없다」 를 더한다. 타입 모양은 바꾸지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay test -- fanout strategy-hub</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 passed 수가 0 — 콜드 탭이 빈 `lc.snap` 을 먼저 받으면 ⑭(B)·⑭-3 이 실패한다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run test && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests</automated>
    <fails_when>non-zero exit, 또는 relay 통과 수가 516 미만, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 수정 전 코드의 ⑭(B)·⑭-3 RED 원문이 있다
    - `grep -n "hasLimitChaserList" relay/src/hub/subscription-hub.ts relay/src/ws/fanout.ts` 가 두 파일 모두 1줄 이상
    - `#clearCaches` 와 `closeAll` 본문에서 「받았음」 집합을 지우는 줄이 각각 있다(`grep -n "#limitChaserKnown" relay/src/hub/subscription-hub.ts` 로 선언·기록·삭제 2곳을 읽는다)
    - 허브 단위 ⑯ green · relay 전량 green(≥ 516)
    - `packages/shared/src/relay.ts` 의 `RelayLimitChaserSnapMsg` 타입 선언 줄이 바뀌지 않았다(주석만 추가)
  </acceptance_criteria>
  <done>relay 가 내리는 `lc.snap` 은 이제 언제나 게이트웨이가 확정한 목록이다 — 「모름」 은 프레임 부재로만 표현된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: webapp — 연결별 확정 스냅샷 기준점 · `knowsRegistered = snapSeq > 0` (세 틈 회귀)</name>
  <files>webapp/src/lib/use-relay-socket.ts, webapp/src/lib/__tests__/relay-socket.test.ts, webapp/src/components/trading/workbench/trading-workbench.tsx, webapp/src/components/trading/__tests__/trading-workbench.test.tsx</files>
  <read_first>
    - webapp/src/lib/use-relay-socket.ts (285-305 `limitChaserSnapSeq` JSDoc · 440-480 리듀서 `reset`·`state` · 535-548 `lc.snap` · 835-990 연결 수명 — 인증 ACK 분기와 `local-status` 순서)
    - webapp/src/lib/__tests__/relay-socket.test.ts (1098-1140 describe 「limitChaserSnapSeq …」 — 프레임 주입·상태 읽기 하네스)
    - webapp/src/components/trading/workbench/trading-workbench.tsx (49-58 ⑥ 머리 주석 · 226-238 `knowsRegistered` · 338-408 유입 효과·사이드바 요청)
    - webapp/src/components/trading/__tests__/trading-workbench.test.tsx (401-515 사이드바 포커스 · WR-07 describe — 특히 491 콜드 세션 케이스)
    - relay/src/ws/fanout.ts (Task 1 결과 — 인증 ACK 뒤 조건부 `lc.snap` 순서)
  </read_first>
  <behavior>
    - 리듀서: `local-status connecting/reconnecting` → `state ready` → `limitChaserSnapSeq` 0 · 그 뒤 `lc.snap` → 1 · `state ready` 가 한 번 더 와도(이미 ready) 1 그대로 · `state reconnecting` → `state ready` 전환이면 다시 0
    - ① 등록 전략 0건 사용자: `snapSeq 1` · 빈 목록 · 사이드바 요청 K(없음) → 버려진다 → 나중에 K 가 60 으로 등록돼도 **접힌 채** (수정 전: 보류 → 펼쳐짐 = RED)
    - ② 목록이 있다가 모두 지워짐(`snapSeq 2` · 빈 목록) → 요청 K → 버려진다 (수정 전: 보류 = RED)
    - ③ 재연결: 기준점 0 · 옛 목록 [J] 남음 · 사이드바 요청 K → 보류 → 새 확정 `lc.snap [K]`(snapSeq 1) → K 카드 펼침 (옛 목록 기준으로 버리지 않는다 — 작업대 단은 수정 전에도 같을 수 있고, RED 는 리듀서 기준점 케이스가 낸다)
    - 콜드 세션(새 계약): `snapSeq 0` 동안 `?focus=K` 보류 → 첫 `lc.snap [K]` → 펼침
    - 기존 「스냅샷 전 요청 → 스냅샷에 K 있으면 펼친다」 · 「?focus= 뒤로가기 다시 덮지 않는다」 무수정 green
  </behavior>
  <action>
**리듀서 기준점 (GC-IN-02 ③).** `applyFrame` 의 `state` 분기에서 「이전 `status` 가 `ready` 가 아니고 이번 프레임이 `ready`」 일 때만 `limitChaserSnapSeq` 를 0 으로 되돌린다(순수 리듀서 안 — 인증 ACK 는 연결 직전 `local-status` 가 `connecting`/`reconnecting` 이므로 새 연결의 ACK 도 전환이다). 그러면 이 값은 「이번 `ready` 구간에서 적용한 확정 `lc.snap` 수」 가 된다. relay 는 세션이 `ready` 가 될 때마다 24 를 다시 보내 64 가 새로 오므로(strategy-hub ①), 전환 뒤에는 확정 `lc.snap` 이 반드시 한 번 더 온다 — 그 전의 미스는 보류가 맞다. `limitChaserSnapSeq` JSDoc 을 새 뜻(「이번 ready 구간에서 받은 확정 64 수 · 0 = 아직 못 받음 · relay 는 64 전에 lc.snap 을 보내지 않는다(18-26) · 빈 배열도 확정」)으로 다시 쓰고, 「소비처는 빈 목록을 모호하게 다룬다」 문단은 지운다. `lc.snap` 분기 주석도 맞춘다.

**작업대 (D-02 · GC-IN-02 ①②).** `knowsRegistered` 를 `snapSeq > 0` 하나로 줄인다 — 인자에서 목록을 뺀다. 머리 주석 ⑥ 의 ★ 문단과 함수 JSDoc 을 새 계약(relay 18-26: 64 전에는 `lc.snap` 없음 → 받은 스냅샷은 확정)으로 고치고, 콜드 세션의 빈 첫 스냅샷을 피하던 설명은 지운다. 두 호출부(유입 효과 · 사이드바 요청)가 같은 함수를 부르는 구조는 그대로다(판정의 유일 지점).

**테스트.** `relay-socket.test.ts` 기존 describe 에 기준점 3케이스(전환 시 0 · 반복 ready 불변 · reconnecting→ready 재설정)를 더한다. `trading-workbench.test.tsx` WR-07 describe 에 `<behavior>` ①②③ 을 더하고 **수정 전 코드로** ①② 의 RED 를 SUMMARY 에 적는다. 491 케이스는 옛 relay 우회(빈 첫 스냅샷)를 전제로 하므로 「콜드 세션 — relay 는 64 전에 lc.snap 을 보내지 않는다(snapSeq 0) → 첫 스냅샷에 K 가 있으면 펼친다」 로 다시 쓰고, 옛 단언이 결함 우회를 잠그고 있었다는 사실을 SUMMARY 에 남긴다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/webapp test -- trading-workbench relay-socket relay-provider</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test:e2e -- sidebar-tree trading-workbench me.spec a11y</automated>
    <fails_when>non-zero exit, 또는 출력에 "No tests found", 또는 "failed" 가 1 이상 — 콜드 세션 `?focus=` · 사이드바 포커스 왕복 · My page 전략 목록 · a11y 동기화 지점 중 하나가 새 relay 규율에서 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 수정 전 코드로 돌린 ①② RED 원문과 491 재작성 이유가 있다
    - `knowsRegistered` 의 매개변수가 하나(`snapSeq`)다 — `grep -n "function knowsRegistered" webapp/src/components/trading/workbench/trading-workbench.tsx` 로 읽은 시그니처
    - 리듀서 기준점 3케이스 · 작업대 ①②③ · 콜드 세션 케이스 green · 기존 포커스 케이스 무수정 green
    - e2e `sidebar-tree` · `trading-workbench` · `me` · `a11y` green
  </acceptance_criteria>
  <done>포커스 보류는 「이번 연결에서 확정 목록을 아직 못 받았다」 는 한 사실에만 산다 — 0건 사용자·빈 목록·재연결 모두 같은 규칙으로 판정된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay hub 캐시 → 브라우저 스냅샷 | 「모름」 을 「없음」 으로 내리면 브라우저가 거짓 확정 위에서 판정한다 |
| URL `?focus=` · 사이드바 이벤트 → 카드 펼침 | 신뢰할 수 없는 키 문자열 — `parseStrategyKey` 로만 해석(무변경) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-109 | Tampering | 오래된 포커스 키의 지연 발동 | low | mitigate | 확정 스냅샷 뒤의 미스는 버린다(`knowsRegistered = snapSeq > 0`) · relay 가 확정만 내린다(Task 1) · ①② 회귀 |
| T-18-110 | Information Disclosure | 사용자별 「받았음」 기록 교차 | medium | mitigate | 키가 `userId` · `#clearCaches(userId)` 가 그 사용자만 지운다 · 허브 단위 ⑯ 이 사용자 교차 0 을 고정 |
| T-18-111 | Denial of Service | 배포 순서 역전(webapp 먼저) | medium | mitigate | prohibitions(push·배포 금지) · key_links 에 결합 명시 · 18-32 가 VALIDATION Sign-Off 배포 순서에 기록 |
| T-18-112 | Denial of Service | 게이트웨이가 24 에 답하지 않을 때 | low | accept | `lc.snap` 이 오지 않아 포커스가 보류된 채 남는다(버리지 않는 쪽 — 사용자 조작 없는 오동작은 없다). VI 3상태와 같은 선택 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 |
</threat_model>

## Artifacts this phase produces

- **relay:** `SubscriptionHub.hasLimitChaserList(userId)` · 사용자별 64 수신 집합(세션 교체·closeAll 에서 삭제) · 인증 경로 조건부 `lc.snap`
- **shared:** `RelayLimitChaserSnapMsg` 주석(확정 목록 계약) — 타입 모양 무변경
- **webapp lib:** `limitChaserSnapSeq` 가 `ready` 전환마다 0 으로 돌아가는 리듀서 규칙
- **webapp 컴포넌트:** `knowsRegistered(snapSeq)` 단일 인자
- **테스트:** relay `fanout.test.ts` ⑭(B) 재작성 · ⑭-3 · `strategy-hub.test.ts` ⑯ / webapp `relay-socket.test.ts` 기준점 3 · `trading-workbench.test.tsx` ①②③ · 콜드 세션 재작성
- **deferred 해소:** `deferred-items.md` 18-22 항목의 근본 수정(해소 기록 추가는 18-32)

<verification>
- relay: 모르는 세션의 새 탭은 `lc.snap` 0 · 콜드 탭의 첫 `lc.snap` = 64
- webapp: 세 틈(0건 · 빈 목록 · 재연결) 회귀 green · 콜드 `?focus=` 유지
- e2e sidebar-tree · trading-workbench · me · a11y green
</verification>

<success_criteria>
- `lc.snap` = 확정 목록이라는 계약이 relay 에서 서고, 작업대는 그 계약 하나로 보류를 판정한다
- deferred-items 의 relay 콜드 세션 항목이 코드로 닫혔다(배포는 사람 몫)
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-26-SUMMARY.md` when done
</output>
