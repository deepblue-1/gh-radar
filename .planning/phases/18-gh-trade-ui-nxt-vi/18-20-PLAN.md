---
phase: 18-gh-trade-ui-nxt-vi
plan: 20
type: execute
wave: 10
depends_on: ["18-17"]
gap_closure: true
files_modified:
  - webapp/src/components/trading/workbench/card-grid.tsx
  - webapp/src/components/trading/__tests__/card-grid.test.tsx
  - webapp/src/components/trading/card/strategy-card.tsx
  - webapp/src/components/trading/__tests__/strategy-card.test.tsx
  - webapp/e2e/specs/trading-workbench.spec.ts
autonomous: true
requirements: [TRADE-09]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WR-02: 카드를 접고 펴도(스택 ↔ 격자 이동) 카드 컴포넌트가 **다시 마운트되지 않는다** — 미전송 더티 값 · 수동주문 「결과 모름」 잠금(중복 체결 방지) · 전송↔에코 상관(`pendingRef`/`unacked`) · 카드 로그가 유지된다. 격자 단 수(1/2/3)를 바꿔도 같다"
    - "D-09 레이아웃 무변경 — 펼친 카드 각 한 칸 → 접힌 카드는 스택 한 칸에 세로(gap 8px) · DOM 순서가 곧 포커스 순서 · 애니메이션 없음 · 스택 라벨 없음"
    - "D-11: 접힌 카드는 헤더만 **보인다**. 한 번이라도 펼친 카드의 본문은 접으면 숨김(`hidden`)으로 남아 상태를 지키고, 한 번도 펼친 적 없는 카드(등록 전략이 접힌 채 들어온 카드)는 본문을 만들지 않는다"
    - "D-12 · D-28: 접힌 더티 카드의 더티 액션 바(`document.body` 포털 · 종목명 문구)는 떠 있는 그대로다 — 미반영 값이 있다는 사실이 접기로 사라지지 않는다"
    - "UI-SPEC §접근성: 접기/펼치기 뒤 포커스는 누른 헤더 토글로 돌아온다(기존 `card-header` id 되찾기 그대로)"
  artifacts:
    - webapp/src/components/trading/workbench/card-grid.tsx
    - webapp/src/components/trading/card/strategy-card.tsx
  key_links:
    - "`CardGrid` 카드별 고정 호스트 노드(`createPortal` 대상) ↔ 칸/스택 자리표(ref 콜백이 노드를 옮겨 붙임) — React 트리 위치는 평평한 keyed 목록이라 부모가 바뀌지 않는다"
    - "`StrategyCard` `everOpened` ↔ `strategy-card-body` `hidden` — 본문 상태(LimitChaserForm 더티 · ManualOrderForm 잠금)가 접힘에서 산다"
  prohibitions:
    - "MUST NOT React 의 `Activity`(또는 `unstable_Activity`) 로 본문을 숨긴다 — Next 15.5 가 앱 라우터에 번들한 React(19.2.0-canary-0bdb9206)에는 그 export 가 없고 vitest 는 설치본 react 19.2.5 를 써서 **테스트만 통과하고 프로덕션에서 깨진다**"
    - "MUST NOT 새 패키지(react-reverse-portal 등)를 설치한다"
    - "MUST NOT 카드 폭을 JS 로 재거나 카드 안에 뷰포트 브레이크포인트를 들인다 — §2.2b 정본(globals.css) 규율"
    - "MUST NOT D-09 의 칸/스택 DOM 구조(`card-grid` > `card-cell` · `card-stack` > `card-cell`)를 바꾼다 — e2e 의 격자·스택 단언이 이 구조를 본다"
---

<objective>
18-REVIEW WR-02 를 닫는다. `CardGrid` 가 펼친 카드는 `card-cell` 직계, 접힌 카드는 `card-stack` 안에 그리므로 토글하면 **부모가 바뀌어** React 가 카드 서브트리를 다시 만든다 — 같은 `key` 여도 그렇다. 게다가 `StrategyCard` 는 접힌 카드의 본문을 아예 렌더하지 않는다(`{open && …}`). 그 결과 접기 한 번에 미전송 더티 값이 확인 없이 사라지고, `ManualOrderForm.blocked`(timeout 뒤 재주문을 막는 중복 체결 방지 잠금)가 풀리며, 에코 상관이 초기화된다. 작업대가 이미 이 재마운트를 우회하느라 로그 중복을 걸러내고 있다(`trading-workbench.tsx:407-411`).

설계: (1) **카드마다 고정 호스트 노드**를 두고 카드를 그 노드로 포털한다 — React 트리에서 카드는 평평한 keyed 목록에 있어 부모가 바뀌지 않고, 칸/스택 자리표의 ref 콜백이 그 노드를 옮겨 붙인다(D-09 DOM 구조 그대로). (2) `StrategyCard` 는 한 번 펼친 뒤로 본문을 **숨김으로 유지**한다. 대안(상태를 작업대로 끌어올리기)은 기각 — LimitChaserForm(1,526줄)의 내부 폼 상태·전송 잠금까지 끌어올려야 해 변경면이 훨씬 넓다.

Purpose: TRADE-09 작업대 카드가 접기/펴기로 실주문 안전장치를 잃지 않는다.
Output: 고정 호스트 격자 · 본문 유지 카드 · 회귀 테스트(단위 + e2e GC2).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-REVIEW.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-11-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] 카드 정체성 고정 — 카드별 호스트 노드 + 자리표 ref 콜백</name>
  <files>webapp/src/components/trading/workbench/card-grid.tsx, webapp/src/components/trading/__tests__/card-grid.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/workbench/card-grid.tsx (전문 161줄 — 머리 주석 ①~④, `renderOrderOf`, ✕ 뒤 포커스 레이아웃 효과 96-114, 칸/스택 렌더 135-160)
    - webapp/src/components/trading/__tests__/card-grid.test.tsx (전문 — 스텁 카드·`layout` 헬퍼·포커스 케이스)
    - webapp/src/components/trading/card/card-header.tsx (100-113 — 토글 뒤 `setTimeout` 으로 id 되찾기)
    - webapp/src/components/trading/workbench/trading-workbench.tsx (533-561 `renderCard` 사용처 · 407-411 재마운트 우회 주석)
  </read_first>
  <behavior>
    - 상태를 가진 스텁 카드(내부 `useState` 카운터 + 마운트 횟수 기록) 2장 · A 펼침 → 카운터를 3 으로 올림 → A 접음 → A 의 스텁 DOM 요소가 **같은 인스턴스**(`toBe`)이고 카운터 3 · 마운트 1회
    - A 를 다시 펼침 → 같은 인스턴스 · 카운터 3 · 마운트 1회
    - `cols` 1 → 2 → 3 재렌더 → 모든 카드 마운트 1회
    - 기존 레이아웃 케이스 무변경 green — [A][B][스택(3)] · 펼침 0 + 접힘 N · 스택 라벨 없음 · 스택 gap/flex-col · DOM 순서 재계산
    - ✕ 로 카드를 지우면 그 카드의 DOM 이 문서에서 사라지고 포커스가 다음 카드 헤더로 간다(기존 케이스 green)
  </behavior>
  <action>
`CardGrid` 를 「카드별 고정 호스트 노드 + 자리표」 구조로 바꾼다 (WR-02 · D-09 · D-28).

- 카드 키별 호스트 `div` 를 `useRef(new Map())` 에 한 번만 만든다(렌더 중 생성이지만 Map 조회로 멱등 · `typeof document` 가드 — SSR 에서는 만들지 않는다). 호스트에는 `style.display = "contents"` 를 직접 준다(Tailwind 스캔에 기대지 않는다 · 칸의 레이아웃·`@container/lc` 측정 대상인 카드 `article` 이 칸의 직접 레이아웃 자식처럼 동작한다).
- 격자 JSX 는 **지금과 같은 DOM 구조**(`card-grid` > 펼친 카드의 `card-cell` … > `card-stack` > 접힌 카드의 `card-cell`)를 그리되, 칸은 비어 있는 **자리표**다. 자리표마다 카드 키별로 memo 된 **안정 ref 콜백**을 달고, 콜백이 그 키의 호스트 노드를 자기 안에 붙인다(이미 자식이면 아무것도 하지 않는다). null 호출(자리표가 사라짐)에는 아무것도 하지 않는다 — 다음 자리표가 붙인다.
- 카드 내용은 격자 **뒤에** 오는 keyed 배열로 `createPortal(renderCard(c), host, key)` 를 그린다. 격자(자리표)를 포털 배열보다 **앞에** 두는 순서가 계약이다: 커밋 레이아웃 단계는 형제 순서대로 돌아서, 자리표 ref 가 카드 안쪽의 레이아웃 효과보다 먼저 노드를 제자리에 붙인다(주석으로 이 이유를 적는다).
- 사라진 키의 호스트는 레이아웃 효과에서 `remove()` 하고 Map 에서 지운다. ✕ 뒤 포커스 효과(96-114)는 그대로 둔다.
- 머리 주석 ①·③ 의 「재렌더/다시 마운트돼도」 문구를 「부모가 바뀌지 않아 마운트가 유지된다」로 고치고, 새 문단 ⑤ 에 WR-02 근거와 기각한 대안(상태 끌어올리기)을 한 문단으로 남긴다. 새 패키지·React 숨김 경계 컴포넌트는 쓰지 않는다(frontmatter prohibitions 참조).

테스트: `card-grid.test.tsx` 에 describe 「WR-02 — 접기/펴기·단 수 변경에 마운트 유지」 3케이스(`<behavior>` 1~3). 기존 케이스는 수정하지 않는다 — 스텁이 `querySelector` 로 칸 **안쪽**을 찾으므로 호스트 한 겹이 생겨도 그대로 통과해야 한다(통과하지 않으면 멈추고 원인을 SUMMARY 에 적는다).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- card-grid</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "Activity|react-reverse-portal" webapp/src/components/trading/workbench/card-grid.tsx</automated>
    <fails_when>non-zero exit — 금지된 숨김 경계 컴포넌트나 외부 패키지를 쓰고 있다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 「접고 펴도 같은 인스턴스 · 마운트 1회」 케이스가 green 이고, 수정 전 `card-grid.tsx` 에서 실패한다(RED 확인을 SUMMARY 에 한 줄)
    - `grep -c "createPortal" webapp/src/components/trading/workbench/card-grid.tsx` 가 1 이상
    - 기존 `card-grid.test.tsx` 케이스가 무수정 green
    - `CardGridProps`·`renderOrderOf` 의 공개 모양이 바뀌지 않았다(작업대 호출부 무수정으로 typecheck 통과)
  </acceptance_criteria>
  <done>격자에서 카드가 칸과 스택 사이를 오가도 React 는 그 카드를 다시 만들지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `StrategyCard` — 한 번 펼친 본문은 접어도 숨김으로 유지</name>
  <files>webapp/src/components/trading/card/strategy-card.tsx, webapp/src/components/trading/__tests__/strategy-card.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/card/strategy-card.tsx (566-705 — `StrategyCardProps`·`StrategyCardImpl`·본문 영역 `hidden={!open}` + `{open && …}` 685-694)
    - webapp/src/components/trading/__tests__/strategy-card.test.tsx (238-290 — 「open=false 면 헤더만」「open=true 면 … 순서로」 케이스와 `probeBody`)
    - webapp/src/components/trading/limit-chaser-form.tsx (1040-1060 · 1110-1120 — 더티 바 포털과 `dirtyHint` 종목명)
    - webapp/src/components/trading/card/manual-order-form.tsx (219-221 `blocked` · 433-437 timeout 잠금)
  </read_first>
  <behavior>
    - 처음부터 `open=false`(한 번도 펼친 적 없음) → `body` 호출 0 · 본문 영역 `hidden` · 자식 0 (기존 케이스 그대로)
    - `open=true` 로 렌더 → 상태 프로브 본문의 값 변경 → `open=false` 로 rerender → 본문 영역 `hidden` · 프로브 DOM 은 그대로 존재(같은 인스턴스) · `lc-quote-grid` 는 DOM 에 있으나 보이지 않음
    - 다시 `open=true` → 프로브 값이 유지된다
  </behavior>
  <action>
`StrategyCardImpl` 에 「한 번이라도 펼친 적 있는가」 상태를 두고(렌더 중 파생 갱신 — `open` 이 참이 되면 참으로, 다시 거짓이 되지 않는다), 본문 영역 안의 `QuoteGrid10`·`CardNotices`·`body` 를 **그 상태가 참이면** 그린다. 영역의 `hidden={!open}` 은 그대로라 접힌 카드는 헤더만 보인다(D-11). 한 번도 펼친 적 없는 카드는 지금처럼 본문을 만들지 않는다 — 등록 전략이 접힌 채 N장 들어와도 본문 비용이 없다.

⑤ 머리 주석과 685-690 의 「접힌 카드는 **헤더만** 그린다 — 10칸도 본문도 DOM 에 없다」 주석을 「접힌 카드는 헤더만 **보인다** — 한 번 펼친 본문은 숨김으로 남아 더티 값·「결과 모름」 잠금·에코 상관을 지킨다(WR-02). 한 번도 펼친 적 없는 카드는 본문을 만들지 않는다」로 고친다. 접힌 더티 카드의 더티 바(포털)가 계속 뜨는 것은 **의도**라고 한 줄 적는다 — 미반영 값이 접기로 사라지지 않는다(D-12 · D-28).

테스트: 기존 「open=false 면 헤더만 …」 케이스는 「한 번도 펼친 적 없으면」 으로 제목만 명확히 하고 단언은 그대로 둔다. 새 케이스 「WR-02 — 한 번 펼친 본문은 접어도 상태를 지킨다」 1개(`<behavior>` 2~3).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- strategy-card</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "Activity" webapp/src/components/trading/card/strategy-card.tsx</automated>
    <fails_when>non-zero exit — 금지된 React 숨김 경계 컴포넌트를 쓰고 있다</fails_when>
  </verify>
  <acceptance_criteria>
    - 「한 번 펼친 본문은 접어도 상태를 지킨다」 케이스가 green 이고 수정 전 코드에서 실패한다(RED 한 줄)
    - 「한 번도 펼친 적 없으면 본문 호출 0」 단언이 그대로 green
    - `strategy-card-flow.test.tsx` 가 무수정 green
  </acceptance_criteria>
  <done>펼쳤다 접은 카드는 보이지 않을 뿐 사용자가 넣은 값과 안전 잠금을 그대로 들고 있다.</done>
</task>

<task type="auto">
  <name>Task 3: e2e — D-11 단언을 「보이지 않음」으로 정정 + GC2 접기/펴기에 더티 값·더티 바 유지</name>
  <files>webapp/e2e/specs/trading-workbench.spec.ts</files>
  <read_first>
    - webapp/e2e/specs/trading-workbench.spec.ts (464-522 케이스 6 — 스택 안 본문 부재 단언 504-507 · `toggle` 헬퍼 489 · 731-756 케이스 12 더티 바 · 조회구 `dirtyBar`·`field`·`cardOf`)
    - webapp/src/components/trading/card/strategy-card.tsx (Task 2 결과)
  </read_first>
  <action>
케이스 6 의 「스택 안 카드는 헤더만 — 본문이 DOM 에 없다(D-11)」 단언(504-507)을 「스택 안 카드의 본문은 **보이지 않는다**(D-11 · WR-02 — 한 번 펼친 본문은 숨김으로 남는다)」로 바꾼다 — `card-stack` 안 `card-body` 의 `:visible` 개수 0 을 단언한다. 나머지 레이아웃 단언은 그대로다.

새 케이스 「GC2 카드를 접었다 펴도 미전송 값과 더티 바가 남는다 — 재마운트 없음 (WR-02 · D-09 · D-12)」: 종목 추가로 카드 1장 → 폼 필드 하나(예: `lc-buy-order-amount`)를 바꿔 더티 바 「1개 미반영」 + 종목명 확인 → 헤더 토글로 접기 → 카드가 스택으로 가고 더티 바가 **여전히 보이며** 종목명을 말한다 · 포커스가 그 카드의 헤더 토글에 있다 → 다시 펼치기 → 필드 값이 바꾼 값 그대로 · 더티 바 그대로. 끝으로 「되돌리기」로 정리해 다음 케이스에 상태를 남기지 않는다(직렬 모드). 이 케이스의 토글 조회는 기존 `toggle` 헬퍼 형식을 따른다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test:e2e -- trading-workbench</automated>
    <fails_when>non-zero exit, 또는 출력에 "No tests found", 또는 "failed" 가 1 이상 — 호스트 노드가 칸/스택 레이아웃이나 잘림 판정을 깨면 케이스 4·5·6·17·18 이 실패한다</fails_when>
  </verify>
  <acceptance_criteria>
    - GC2 가 접기 후 더티 바 가시 · 포커스 복귀 · 펼친 뒤 값 유지 세 지점을 단언하고 green
    - 케이스 6 이 `:visible` 기준으로 바뀌었고 green
    - 케이스 4·5·17·18(4밴드 잘림 0)·7(더티 바 겹침 0)이 무수정 green
  </acceptance_criteria>
  <done>실브라우저에서 카드를 접었다 펴도 사용자가 넣은 값이 사라지지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 조작(접기/펴기) → 실주문 안전장치 | 레이아웃 조작이 중복 체결 방지 잠금을 풀면 안 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-89 | Tampering | `ManualOrderForm.blocked` 해제 | high | mitigate | 재마운트 제거(Task 1) + 본문 유지(Task 2) — 「결과 모름」 잠금이 접기/펴기로 풀리지 않는다 |
| T-18-90 | Repudiation | 에코 상관 초기화 | medium | mitigate | 카드 훅 인스턴스가 유지돼 「내가 보낸 것」과 「다른 단말」 구분이 지켜진다 |
| T-18-91 | Denial of Service | 숨긴 본문 재렌더 비용 | low | accept | 한 번 펼친 카드만 본문을 유지 — 카드 훅은 이미 자기 시세를 구독하므로 구독 수는 늘지 않는다(T-18-29 예산 내) |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음(frontmatter 금지 명시) |
</threat_model>

## Artifacts this phase produces

- **webapp 컴포넌트:** `CardGrid` 카드별 고정 호스트 노드 · 자리표 안정 ref 콜백(내부) · `StrategyCardImpl` 「한 번 펼침」 상태(내부)
- **테스트:** `card-grid.test.tsx` describe 「WR-02 — 접기/펴기·단 수 변경에 마운트 유지」 · `strategy-card.test.tsx` 「WR-02 — 한 번 펼친 본문은 접어도 상태를 지킨다」 · e2e `GC2` · 케이스 6 단언 정정

<verification>
- 접기/펴기·단 수 변경에 카드 마운트 1회(단위)
- 실브라우저에서 접기/펴기에 더티 값·더티 바 유지(GC2)
- 4밴드 잘림·스택 레이아웃·더티 바 겹침 케이스 무수정 green
</verification>

<success_criteria>
- WR-02 의 세 증상(더티 소실 · 잠금 해제 · 에코 상관 초기화)이 구조적으로 사라졌다
- D-09 · D-11 의 보이는 레이아웃은 그대로다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-20-SUMMARY.md` when done
</output>
