---
phase: 18-gh-trade-ui-nxt-vi
plan: 29
type: execute
wave: 15
depends_on: ["18-24"]
gap_closure: true
files_modified:
  - supabase/migrations/20260922180000_dma_orders_price_check_null_safe.sql
autonomous: false
requirements: [TRADE-07]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "GC-CR-01 · D-21 · D-23: `[BLOCKING]` 원격 Supabase(`ivdbzxgaapbmrxreyuht`)의 `dma_orders_price_check` 정의가 18-24 의 null-safe 식이다 — 원격 덤프의 제약 줄에 `order_type` 의 `'N'` 갈래와 NULL 접기(`COALESCE` 또는 `IS NOT NULL`)가 있다"
    - "적용 **전에** 원격에서 새 CHECK 를 어길 행 수를 읽기 전용으로 셌고 그 값이 0 이다(가격 0 ∧ 정정 · 가격 0 ∧ 신규 ∧ 세션 NULL). 0 이 아니면 적용하지 않고 멈춰 보고한다 — 마이그레이션 안의 같은 트랜잭션 사전 확인은 두 번째 겹이다"
    - "원격 public 스키마 전후 덤프 diff 가 `dma_orders_price_check` 한 줄 교체 외에 0줄이다 — 정책 0개(default-deny)·`service_role` GRANT·RLS·다른 CHECK·컬럼이 푸시 전후 같다"
    - "원격 적용은 사람이 체크포인트에서 `apply` 를 고른 뒤에만 일어난다(18-18 과 같은 형태). `defer` 면 원격은 바뀌지 않고, 그 사실(DB 최후 방어선은 여전히 NULL 을 통과시킨다)이 SUMMARY 와 18-32 기록에 남는다"
  artifacts:
    - supabase/migrations/20260922180000_dma_orders_price_check_null_safe.sql
  key_links:
    - "원격 CHECK ↔ relay insert 선행 규율 — relay 는 감사 행 insert 가 성공해야 게이트웨이로 보낸다. 새 CHECK 가 막는 행을 만드는 코드 경로는 없다(18-24 근거) — 그래서 적용은 정상 주문을 막지 않고, 코드 층이 회귀하는 날에만 실주문 송신 **전에** 막는다(fail-closed)"
    - "배포 순서 DB → relay → 검증 → webapp push — 이 플랜은 DB 단계만 한다"
  prohibitions:
    - "MUST NOT 체크포인트 승인 전에 `supabase db push`(dry-run 제외)를 실행한다"
    - "MUST NOT 사전 확인 위반 행 수가 0 이 아닌데 푸시하거나, 그 행을 손으로 고친다 — 멈추고 보고한다(감사 행 재작성 금지)"
    - "MUST NOT 이 phase 밖의 미적용 마이그레이션이 `--dry-run` 목록에 섞였는데 푸시한다 — 멈추고 보고한다"
    - "MUST NOT 기존 Supabase 자격증명을 사용자에게 다시 묻는다 — 18-18 과 같이 저장소 링크(`supabase/.temp/project-ref`)와 CLI 로그인 세션, 저장소 env 파일을 먼저 쓴다"
    - "MUST NOT 로그·SUMMARY 에 키·비밀번호·행 원문(계좌번호 등)을 남긴다 — 개수와 원천 이름만"
---

<objective>
`[BLOCKING]` 18-24 의 `20260922180000_dma_orders_price_check_null_safe.sql` 을 **읽기 전용 사전 확인 → 사람 확인 → 적용 → 전후 덤프 diff** 순서로 실 원격 DB 에 반영한다(18-18 과 같은 절차 · 사전 확인이 앞에 붙는다).

Purpose: GC-CR-01 의 원격 몫. 파일과 로컬 회귀만으로는 원격의 최후 방어선이 여전히 NULL 을 통과시킨다 — `supabase migration list --linked` 는 「적용됨」 만 말하지 「의도대로 강제됨」 을 말하지 않는다(18-VERIFICATION-R2 오케스트레이터 주석). 이번에는 적용 결과를 제약 정의 원문으로 증명한다.
Output: 사전 확인 개수 원문 · 사람의 `apply`/`defer` 선택 · (apply 시) 원격 CHECK 재정의 + migration list · 덤프 diff 원문.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-18-PLAN.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-18-SUMMARY.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-24-SUMMARY.md

근거 인용(재확인 완료):
- 18-18 이 실제로 통과한 순서: `supabase db dump --linked -s public`(전) → `supabase migration list --linked` → `supabase db push --dry-run` → `supabase db push --yes` → `migration list` → 덤프(후) diff. 자격은 `supabase/.temp/project-ref`(= `ivdbzxgaapbmrxreyuht`) + CLI 저장 로그인.
- 18-18 적용 후 원격 원문: `CONSTRAINT "dma_orders_price_check" CHECK ((("price" > 0) OR (("price" = 0) AND (("order_type" = 'C'::"text") OR ("krx_session" = ANY (ARRAY['G2'::"text", 'G3'::"text"])))))),`
- 읽기 전용 개수 조회 원천: `workers/master-sync/.env`(또는 `webapp/.env.test.local`)의 `SUPABASE_URL` · `SUPABASE_SERVICE_ROLE_KEY` — PostgREST `HEAD` + `Prefer: count=exact` 로 행 본문 없이 개수만 받는다. `dma_orders` 는 RLS 정책 0개지만 service_role 은 RLS 를 우회하고 GRANT 를 가진다(18-18 덤프). URL 의 프로젝트 ref 가 `supabase/.temp/project-ref` 와 같은지 먼저 확인한다.
- `order_type` 은 N/M/C 만, `krx_session` 은 NULL/G2/G3 만 허용된다(20260921120000) — 그래서 새 CHECK 위반 = 가격 0 ∧ (정정 ∨ (신규 ∧ 세션 NULL)).
</context>

<tasks>

<task type="auto">
  <name>Task 1: 읽기 전용 사전 확인 — 위반 행 개수 · 전 덤프 · migration list · dry-run</name>
  <precondition>18-24 SUMMARY 가 로컬 회귀 GREEN(12/12)을 기록했고 `supabase/migrations/20260922180000_dma_orders_price_check_null_safe.sql` 이 커밋돼 있다 · 저장소가 원격에 링크돼 있다(`supabase/.temp/project-ref` 존재).</precondition>
  <files>supabase/migrations/20260922180000_dma_orders_price_check_null_safe.sql</files>
  <read_first>
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-18-SUMMARY.md (Task 2·3 — 실제로 통한 명령 순서와 출력 형태 · 덤프 줄 번호 840 부근의 CHECK 원문)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-24-SUMMARY.md (로컬 GREEN · 위반 행 실패 동작 원문)
    - supabase/migrations/20260922180000_dma_orders_price_check_null_safe.sql (사람에게 보여 줄 SQL 원문)
  </read_first>
  <action>
원격에 **쓰지 않는** 단계만 한다(GC-CR-01 · D-21 · D-23).
① **위반 행 개수(읽기 전용).** 저장소 env 파일에서 `SUPABASE_URL`·`SUPABASE_SERVICE_ROLE_KEY` 를 읽어(값은 출력·기록하지 않는다) PostgREST 에 `HEAD` 요청 4건을 보낸다 — `dma_orders` 에서 `price=eq.0&order_type=eq.M`(위반 a) · `price=eq.0&order_type=eq.N&krx_session=is.null`(위반 b) · 참고용 `price=eq.0&order_type=eq.C` · `price=eq.0` 전체. 각 응답의 `Content-Range` 총계를 기록한다. URL 호스트의 프로젝트 ref 가 `supabase/.temp/project-ref` 와 다르면 조회하지 않고 멈춘다. 위반 a+b 가 0 이 아니면 **여기서 멈추고** 개수만 보고한다(Task 2 의 선택지가 바뀐다).
② `supabase db dump --linked -s public` 으로 **푸시 전** 스냅숏을 저장소 밖 scratchpad 에 저장한다(커밋하지 않는다).
③ `supabase migration list --linked` — Local/Remote 가 어긋난 행이 `20260922180000` 하나뿐인지 본다.
④ `supabase db push --dry-run` — 적용 대상이 이 파일 하나뿐인지 본다. 다른 미적용 마이그레이션이 섞이면 멈추고 보고한다.
CLI 가 인증 오류로 막히면 그 자리에서 멈추고 동적 인증 게이트로 올린다(시도한 명령·응답 원문 포함, 값은 마스킹). 재시도로 두드리지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && supabase migration list --linked 2>&1 | grep -E "20260922180000" | grep -vcE "20260922180000 +\| +20260922180000"</automated>
    <fails_when>카운트가 0 — `20260922180000` 행이 목록에 없다(파일 미커밋·링크 실패) 또는 이미 Remote 에 적용돼 있다(예상과 다른 원격 상태 — 멈추고 보고), 또는 명령이 인증 오류를 낸다</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 개수 조회 4건의 필터와 총계(위반 a · 위반 b · 취소 0 · 가격 0 전체)가 있고 키·URL 값은 없다
    - `--dry-run` 출력 원문(적용 대상 1건 · `20260922180000_dma_orders_price_check_null_safe.sql`)이 SUMMARY 에 있다
    - 전 덤프가 저장소 밖 경로에 있고 `git status --short` 에 덤프 파일이 없다
  </acceptance_criteria>
  <done>원격을 바꾸지 않고, 적용해도 되는지 판단할 근거(위반 행 0 · 적용 대상 1건)가 원문으로 모였다.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: [원격 운영 DB] null-safe `dma_orders_price_check` 를 지금 적용할지 확인</name>
  <read_first>
    - supabase/migrations/20260922180000_dma_orders_price_check_null_safe.sql (사람에게 보여 줄 SQL 원문)
    - Task 1 결과(위반 행 개수 · dry-run 원문)
  </read_first>
  <decision>원격 Supabase(`ivdbzxgaapbmrxreyuht`) `dma_orders_price_check` 를 NULL 을 FALSE 로 접는 식(가격 0 = 취소 또는 G2/G3 신규만)으로 지금 바꿀 것인가</decision>
  <context>
GC-CR-01: 지금 원격 CHECK 는 `krx_session` 이 NULL 인 가격 0 신규·정정을 3값 논리 때문에 통과시킨다(18-24 로컬 회귀가 재현). 새 식은 그 행들을 거부하고, 취소(가격 0)와 G2/G3 신규(가격 0)는 그대로 받는다. 정책·권한·컬럼은 건드리지 않는다.

**사전 확인:** Task 1 의 위반 행 개수(a 정정 ∧ 가격 0 · b 신규 ∧ 가격 0 ∧ 세션 NULL)를 이 자리에 그대로 보인다. 0 이 아니면 `apply` 는 마이그레이션 안의 사전 확인 때문에 트랜잭션째 실패한다 — 그 경우 선택지는 `defer` 뿐이고, 그 행들을 어떻게 볼지는 별도 결정이다(감사 행을 손으로 고치지 않는다).

**되돌리기 비용(costly — 한쪽 문은 아니다):** CHECK 를 조이는 방향이라 되돌리려면 옛 식으로 되돌리는 새 마이그레이션 하나면 된다(기존 행은 옛 식도 만족). 비용은 운영 DB 변경 자체와, 적용 뒤 코드 층이 가격 0 신규·정정을 만들면 relay 가 게이트웨이 송신 **전에** 「주문 기록에 실패했습니다」로 끝낸다는 점이다 — 지금 코드에는 그런 경로가 없다(18-24 근거).
  </context>
  <options>
    <option id="apply">
      <name>지금 적용 (권장 — 위반 행 0 일 때)</name>
      <pros>감사 테이블 최후 방어선이 선언대로 선다 — 코드 층(zod·조립기)이 회귀해도 세션 없는 가격 0 신규·정정은 DB 가 막는다. 18-18 T-18-82 의 거짓 기록이 원격에서도 사실로 바뀐다</pros>
      <cons>운영 DB 변경. 되돌리려면 새 마이그레이션이 필요하다(쉽지만 한 번 더 배포 절차)</cons>
    </option>
    <option id="defer">
      <name>보류</name>
      <pros>원격 스키마 무변경</pros>
      <cons>원격 CHECK 는 계속 NULL 을 통과시킨다 — 실주문 안전은 relay zod·조립기 두 층에만 기댄다. GC-CR-01 은 「로컬 닫힘 · 원격 미반영」 으로 남는다</cons>
    </option>
  </options>
  <acceptance_criteria>
    - 사람이 `apply` 또는 `defer` 를 명시적으로 골랐고, 그 선택과 선택 시점의 위반 행 개수가 SUMMARY 에 있다
    - `defer` 면 Task 3 을 실행하지 않고 SUMMARY 에 「GC-CR-01 원격 미반영 — DB CHECK 는 여전히 NULL 을 통과시킨다」 를 남긴다
  </acceptance_criteria>
  <resume-signal>`apply` 또는 `defer` 를 입력</resume-signal>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] `supabase db push --yes` → 후 덤프 diff · 제약 원문 검증</name>
  <reversibility rating="costly">운영 DB 의 CHECK 를 조인다 — 되돌리기는 새 마이그레이션 하나로 되지만 운영 변경이고, 적용 뒤 막히는 행은 relay 가 송신 전에 끊는다. Task 2 에서 사람이 확인했다.</reversibility>
  <precondition>Task 2 선택이 `apply` 이고 Task 1 의 위반 행 a+b 가 0 이다.</precondition>
  <files>supabase/migrations/20260922180000_dma_orders_price_check_null_safe.sql</files>
  <read_first>
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-18-SUMMARY.md (Task 3 검증 형식 — 제약 원문 · 정책 0개 · GRANT 불변 · 전체 diff 요약)
    - Task 1 의 전 덤프 경로
  </read_first>
  <action>
18-18 이 실제로 통과한 순서를 따른다(GC-CR-01 · D-21 · D-23). ① `supabase db push --yes` — 출력에 「Applying migration 20260922180000_dma_orders_price_check_null_safe.sql…」 와 「Finished supabase db push.」 가 있어야 한다. 마이그레이션 안의 사전 확인이 `RAISE` 로 실패하면(그 사이 위반 행이 생김) 원격은 바뀌지 않았다 — 원문을 적고 멈춘다. ② `supabase migration list --linked` 로 Remote 열이 채워졌는지 본다. ③ **푸시 후** `supabase db dump --linked -s public` 을 떠 Task 1 의 전 덤프와 diff 한다(읽기 전용). 확인 항목: 제약 원문에 `'N'` 갈래와 NULL 접기(`COALESCE` 또는 `IS NOT NULL`)가 있고 `"price" > 0` 갈래와 취소 갈래가 그대로다 · `dma_orders` 대상 `CREATE POLICY` 전 0 / 후 0 · `ENABLE ROW LEVEL SECURITY`·`service_role` GRANT 전후 동일 · 전체 diff 가 그 CHECK 한 줄 교체뿐이다(이 파일은 COMMENT 를 달지 않는다). 하나라도 어긋나면 멈추고 보고한다 — 손으로 고치지 않는다.
`18-VALIDATION.md` 갱신은 이 플랜이 하지 않는다(18-32 가 R3 전체를 한 번에 기록한다).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && supabase migration list --linked 2>&1 | grep -cE "20260922180000 +\| +20260922180000"</automated>
    <fails_when>카운트가 0 — Remote 열이 비어 있다(푸시가 일어나지 않았거나 사전 확인으로 롤백됐다), 또는 명령이 non-zero(인증/링크 실패 — 인증 게이트로 올린다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && supabase db dump --linked -s public 2>/dev/null | grep "dma_orders_price_check" | grep -c "'N'"</automated>
    <fails_when>카운트가 0 — 원격 CHECK 에 신규 한정 갈래가 없다(반영 안 됨), 또는 dump 가 아무것도 출력하지 않아 0</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 `db push --yes` 출력 원문과 원격 `dma_orders_price_check` 정의 원문 1줄이 있다
    - SUMMARY 에 「정책 전 0 / 후 0 · RLS·GRANT 전후 동일 · 전체 diff = CHECK 1줄」 이 diff 원문과 함께 있다
    - 자격은 기존 원천(저장소 링크 · CLI 세션 · env 파일)으로 해결했고 사용자에게 재요청한 기록이 없다(원천 이름만)
  </acceptance_criteria>
  <done>원격 감사 테이블의 CHECK 가 3값 논리의 구멍 없이 선다는 것이 제약 정의 원문으로 증명됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 개발자 워크스테이션 → Supabase 운영 DB | 실 감사 테이블 제약을 바꾸고, 서비스 키로 개수를 읽는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-120 | Tampering | 의도치 않은 마이그레이션 동반 적용 | high | mitigate | `--dry-run` 적용 목록 1건 확인 · 섞이면 멈춤(Task 1 ④) |
| T-18-121 | Elevation of Privilege | RLS/GRANT 의도치 않은 변경 | high | mitigate | 전후 덤프 diff 로 정책 0 · GRANT·RLS 불변 확인(Task 3 ③) · 파일에 정책·권한 구문 0(18-24 게이트) |
| T-18-122 | Information Disclosure | 서비스 키 · 감사 행 원문 | high | mitigate | HEAD + count 만(행 본문 없음) · 키·URL 값 미기록 · 프로젝트 ref 대조 후에만 조회 |
| T-18-123 | Denial of Service | 기존 위반 행으로 적용 실패/정상 주문 차단 | medium | mitigate | 읽기 전용 사전 확인(Task 1 ①) + 마이그레이션 내부 사전 확인(같은 트랜잭션) · 코드 경로상 차단될 정상 행 0(18-24 근거) |
| T-18-124 | Repudiation | 운영 DB 변경 승인 | medium | mitigate | 사람 확인 체크포인트(Task 2) 뒤에만 푸시 · 선택과 근거 개수를 SUMMARY 에 기록 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 |
</threat_model>

## Artifacts this phase produces

- **원격 DB 상태:** `public.dma_orders` 의 `dma_orders_price_check` null-safe 재정의(apply 시)
- **증거:** SUMMARY 의 위반 행 개수 4건 · `--dry-run`·`db push`·`migration list --linked` 원문 · 전후 덤프 diff 요약(덤프 파일은 저장소 밖 — 커밋 안 함) · 사람의 선택 기록

<verification>
- 위반 행 0 확인 뒤에만 적용 · `migration list --linked` 가 `20260922180000` Remote 적용
- 원격 제약 원문에 신규 한정 갈래 + NULL 접기 · 정책·GRANT·RLS 불변
</verification>

<success_criteria>
- GC-CR-01 의 원격 몫이 「적용됨」 이 아니라 「의도대로 강제됨」 으로 증명됐다 — 또는 사람이 `defer` 를 골라 그 사실이 기록됐다
- 이 phase 밖 스키마는 한 줄도 바뀌지 않았다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-29-SUMMARY.md` when done
</output>
