---
phase: 18-gh-trade-ui-nxt-vi
plan: 11
type: execute
wave: 5
depends_on: ["18-05", "18-08", "18-09", "18-10"]
files_modified:
  - webapp/src/app/trading/page.tsx
  - webapp/src/components/trading/workbench/trading-workbench.tsx
  - webapp/src/components/trading/workbench/workbench-status-bar.tsx
  - webapp/src/components/trading/workbench/card-grid.tsx
  - webapp/src/components/trading/__tests__/trading-workbench.test.tsx
  - webapp/src/components/trading/__tests__/workbench-status-bar.test.tsx
  - webapp/src/components/trading/__tests__/card-grid.test.tsx
autonomous: true
requirements: [TRADE-09]

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01: `/trading` 한 페이지에서 탐색(돌파감지·VI 발동) → 추가 → 전략 설정 → 수동주문(신규·정정·취소) → 미체결/잔고/로그 확인이 전부 끝난다 (TRADE-09)"
    - "D-04: 페이지 순서가 위→아래로 제목줄 → 상태줄 → VI 설정 2줄 → VI 발동 스트립/표 → 돌파 스트립/표 → 종목 추가 → 카드 격자 → 공용 패널로 고정된다 (TRADE-09)"
    - "D-04: 상태줄에 DMA 상태 · 돌파 N(신규 M) · VI 발동 N(미확인 M) · 거래 종목 N · 「임계 20% · 재무장 −2%p」 · 77 구간 배지(모름이면 **배지 없음**) · 알림음 토글 · 반영 시각 · 1·2·3단 세그먼트가 있고, 폰 밴드에서는 세그먼트가 **DOM 에서 제거**된다(숨긴 컨트롤이 탭 체인에 남지 않게) (TRADE-09)"
    - "D-04: 단 수 선택은 `gh-radar:trading-cols` 로 기기별 저장되고, 폰 밴드에서는 저장값과 무관하게 항상 1단이다 (TRADE-09)"
    - "D-09: 격자 정렬은 펼친 카드가 먼저 각각 한 칸을 차지하고 접힌 카드는 **스택 한 칸**에 세로로 쌓인다. 스택 위 라벨이 없고 전환에 애니메이션이 없다(재렌더) (TRADE-09)"
    - "D-28: 페이지 축은 컨테이너 `wb` 가 재고, 카드 안은 `lc` 가 잰다. **뷰포트 분기를 신설하지 않는다**(앱 셸·사이드바는 기존 뷰포트 브레이크포인트 그대로) (TRADE-09)"
    - "D-02: `/trading?focus={전략키}` 로 들어오면 해당 카드가 펼쳐진 상태로 열린다. `focus` 는 **마운트 1회만 소비**하고 그 뒤에는 로컬 상태가 정본이다(뒤로가기로 카드가 다시 펼쳐지지 않는다) (TRADE-09)"
    - "D-27: DMA Ready 전에는 격자·상태줄이 아니라 기존 `DmaGate` 문구가 페이지를 대신한다. 「불러오는 중」으로 위장하지 않는다 (TRADE-09)"
    - "D-17: 알림음 토글은 상태줄에 있고 기본 꺼짐이며, 자동재생이 차단된 상태면 아이콘 라벨이 「클릭해 활성화」가 되고 `resume()` 은 그 클릭(사용자 제스처) 안에서만 불린다 (TRADE-09)"
    - "D-05: VI 마감알림 로컬 설정은 제거되지 않고 상태줄로 옮겨져 알림음 토글과 나란히 선다(「이 기기 전용 알림 2종」) (TRADE-09)"
    - "E1 loading: Ready 직후 첫 스냅샷 전에는 카운터 0 · 구간 배지 없음 · 「반영 —」이고 스켈레톤·「불러오는 중」 위장이 없다 (TRADE-09)"
    - "E1 error: relay 끊김/재연결은 DMA 필 하나가 말하고(● 색 + 라벨, 재시도 버튼 없음 — 자동 재연결), 주문·설정 오류는 상태줄이 아니라 해당 폼의 인라인 `role=\"status\"` 가 담당한다 (TRADE-09)"
    - "E6 empty: 카드 0개면 격자 자리에 「거래할 종목이 없어요」 + 「위 검색란에서 종목을 추가하거나, 돌파 목록의 종목을 눌러 시작하세요.」 를 그린다 (TRADE-09)"
    - "E6 loading: DmaGate 통과 전에는 격자를 그리지 않는다. Ready 직후 64 스냅샷 도착까지의 짧은 구간은 빈 문구를 그린다 — 스켈레톤 없음 (TRADE-09)"
    - "E6 error: 격자 자체의 로드 실패 경로가 없다(relay push). 카드별 등록/반영 실패는 그 카드의 더티 바 + 인라인 `role=\"status\"` 가 말한다 (TRADE-09)"
    - "E6 populated: 펼친 카드 먼저 각각 한 칸 → 접힌 카드는 스택 한 칸에 세로(gap 8px) · 1/2/3단은 page 컨테이너 + localStorage · 헤더 클릭으로 스택 이동(재렌더, 애니메이션 없음) (TRADE-09)"
    - "E6 partial: 시세 미수신 카드는 헤더 현재가·등락률과 10칸 값이 「—」이되 카드는 정상 렌더되고, 서버 전략이 없는 카드는 LED OFF 3개다 (TRADE-09)"
    - "TRADE-09 boundary: e2e `trading-workbench.spec.ts` 가 카드 컨테이너 폭 699/700 · 829/830 · 991/992 각 경계와 한 스텝 양옆에서 §2.2b 밴드 전환을 단언하고, 각 폭에서 `overflowX` 잘림 0 을 단언한다"
    - statement: "폰 밴드(page <700)에서 단 수 세그먼트가 DOM 에서 제거되고 나머지 상태줄 필이 2줄로 wrap 하며 어느 필도 잘리지 않는다 (E1 overflow)"
      verification: backstop
    - statement: "카드 폭 4밴드 × 격자 1/2/3단 × 폰/와이드 조합에서 잘림이 0 이다 (E6 overflow)"
      verification: backstop
    - statement: "펼침 0/1/2+ × 접힘 0/1/N 조합에서 스택이 정확히 한 칸을 차지하고 펼친 카드가 먼저 온다 (E6 zero-one-many)"
      verification: backstop
    - statement: "등락률·가격 표시가 카드·스트립·표·상태줄에서 같은 값에 같은 문자열을 만든다 — 표면마다 다른 반올림 규칙을 쓰지 않는다 (TRADE-09 precision)"
      verification: backstop
  artifacts:
    - webapp/src/app/trading/page.tsx
    - webapp/src/components/trading/workbench/trading-workbench.tsx
    - webapp/src/components/trading/workbench/workbench-status-bar.tsx
    - webapp/src/components/trading/workbench/card-grid.tsx
  key_links:
    - "`trading-workbench.tsx` 가 카드 집합 상태를 소유 ↔ 돌파 스트립·종목 추가란의 `onAddCard`/`onFocusCard` ↔ 카드 헤더의 ✕·캐럿 — 카드 생성·제거·펼침의 단일 소유자"
    - "상태줄 계좌 ↔ 신규 카드의 기본 계좌 · 공용 패널의 계좌 축 — 이미 등록된 카드는 자기 키의 계좌를 유지한다"
    - "`@container/wb` 선언 ↔ 격자 열 수 · 세그먼트 표시 · 공용 패널 sticky 전환"
  prohibitions:
    - "MUST NOT 작업대 상위에서 서버 에코를 받아 카드에 분배한다 — 분배 로직이 에코 상관의 두 번째 벌이 된다"
    - "MUST NOT 준비되지 않은 표면을 「불러오는 중」·스켈레톤으로 위장한다 — 준비 중이면 준비 중이라고 쓴다"
    - "MUST NOT 폰 밴드에서 단 수 세그먼트를 `display:none` 으로만 숨긴다 — 숨긴 컨트롤이 탭 체인에 남는다. 조건부 렌더로 DOM 에서 뺀다"
    - "MUST NOT 새 뷰포트 브레이크포인트를 만든다 — 페이지 축은 컨테이너 `wb` 가 잰다"
---

<objective>
`/trading` 페이지를 **조립**한다 — 라우트 엔트리, 카드 집합 상태를 소유하는 작업대 셸(`@container/wb`), 상태줄, 그리고 펼침 우선 + 접힘 스택 격자. 앞선 플랜들이 만든 블록(VI 2줄·VI 스트립·돌파 스트립·종목 추가란·카드·공용 패널)이 여기서 한 페이지가 된다.

Purpose: 사용자의 원 컨셉 — 「여러 메뉴 왔다갔다 안 하고 그 페이지에서 거래를 최대한 해결」 — 이 성립하는 지점이다. 카드 생성·제거·펼침의 **단일 소유자**를 두는 것이 이 플랜의 구조적 핵심이다.
Output: `app/trading/page.tsx` · `workbench/trading-workbench.tsx` · `workbench-status-bar.tsx` · `card-grid.tsx`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-PATTERNS.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `workbench-status-bar.tsx` — 카운터 · 구간 배지 · 알림 토글 2종 · 단 수 세그먼트</name>
  <files>webapp/src/components/trading/workbench/workbench-status-bar.tsx, webapp/src/components/trading/__tests__/workbench-status-bar.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/limit-chaser-client.tsx (`StatusBar`·`Dot` — 상태줄 원천. 카드 헤더로 간 부분과의 경계)
    - webapp/src/lib/queued-window.ts (18-04 — 구간 판정. 상태줄이 벽시계를 읽지 않는다)
    - webapp/src/lib/alert-tone.ts · webapp/src/lib/breakout-list.ts (18-04 — 토글 저장·차단 감지·단 수 저장)
    - webapp/src/lib/vi-alert.ts (마감알림 — 상태줄로 이관되는 기존 로컬 설정 · 권한 거부 사유 문구 경로)
    - webapp/src/components/ui/toggle-group.tsx
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §상태줄 문구표 전문(카운터·임계·반영·단 수 `aria-label`·구간 배지 5종) · §접근성 계약 키보드 표의 단 수 세그먼트 행
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html (상태줄 `:729-740` · 폰 세그먼트 숨김 CSS `:268`)
  </read_first>
  <behavior>
    - 첫 스냅샷 전 → 카운터 전부 0 · 구간 배지 없음 · 「반영 —」 · 스켈레톤 없음.
    - `queuedWindow === undefined` → 구간 배지가 DOM 에 없다.
    - `queuedWindow.open` → 배지 「예약구간 · 조각 최대 {maxPieces}」.
    - `g2Open` → 「시간외종가 G2 창」, `g3Open` → 「시간외종가 G3 창」, `preopenOpen` → 「장전 · 예약매수/매도」.
    - 신규 돌파 M=0 → 「신규」 필이 DOM 에 없다.
    - 단 수 세그먼트 클릭 → localStorage 에 저장되고 `onColsChange` 가 불린다.
    - 폰 밴드(page <700) → 단 수 세그먼트가 **DOM 에 없다**(조건부 렌더).
    - 알림음 토글 기본 꺼짐. 차단 상태면 라벨이 「클릭해 활성화」이고 클릭 시 `resumeToneContext` 가 그 제스처 안에서 불린다.
    - 마감알림 토글이 알림음 토글 옆에 있다.
    - DMA 상태 필에 재시도 버튼이 없다(자동 재연결).
  </behavior>
  <action>
`webapp/src/components/trading/workbench/workbench-status-bar.tsx` 를 신설하고 `limit-chaser-client.tsx` 의 `StatusBar`·`Dot` 중 **페이지 축 부분**을 옮긴다(LED 부분은 18-06 이 카드 헤더로 가져갔다 — 판정 함수는 하나로 유지한다).

구성과 문구는 UI-SPEC §상태줄 문구표 원문 그대로다 — DMA 「DMA 실시간」(● = `--led-armed`) · 「돌파 {N}」·「신규 {M}」·「VI 발동 {N}」·「미확인 {M}」·「거래 종목 {N}」 · 「임계 20% · 재무장 −2%p」 · 구간 배지 5종(모름이면 없음) · 「반영 {HH:MM:SS}」(mono) · 단 수 「1단」「2단」「3단」(그룹 `aria-label="카드 단 수"`).

**구간 배지는 `affordanceOf` 와 같은 원천을 쓴다** — 상태줄이 벽시계로 창을 판정하지 않는다. `queuedWindow === undefined` 면 배지를 **그리지 않는다**(「정규」로 위장하지 않는다).

**단 수 세그먼트는 폰 밴드에서 조건부 렌더로 DOM 에서 뺀다** — `display:none` 으로만 숨기면 숨긴 컨트롤이 탭 체인에 남는다(UI-SPEC §접근성 계약). 컨테이너 폭 판정을 JS 로 하지 않으려면 CSS 로 숨기되 `inert`/조건부 렌더 중 접근성 트리에서도 빠지는 방법을 쓴다 — HTML `hidden` 속성이 그 방법이다(globals.css 가 작성자 우선순위를 복원해 둠).

**알림 2종을 나란히 둔다**(UI-SPEC Open Questions Q-1 채택값) — 돌파 알림음 토글(`aria-label` 「돌파 알림음 켜기 (이 기기만)」, 기본 꺼짐, 차단 시 「클릭해 활성화」)과 VI 마감알림 토글(`vi-alert.ts` 의 기존 읽기/쓰기·권한 요청 경로를 그대로 쓴다 — 기능을 없애지 않는다).

계좌 필은 상태줄에 있고, 그 값이 **신규 카드의 기본 계좌**다. 이미 등록된 카드는 자기 키의 계좌를 유지하며 상태줄을 바꿔도 움직이지 않는다 — 그 사실을 `title` 로 말한다(UI-SPEC Q-3 채택값).

테스트 `__tests__/workbench-status-bar.test.tsx` 는 `<behavior>` 11케이스를 RTL 로 덮는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- workbench-status-bar</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "new Date\(|Date\.now\(\)" webapp/src/components/trading/workbench/workbench-status-bar.tsx</automated>
    <fails_when>non-zero exit — 상태줄이 벽시계로 구간을 판정하고 있다(D-22 위반). 「반영 시각」은 서버 값을 포맷만 하므로 현재 시각을 읽을 이유가 없다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "affordanceOf\|queuedWindow" webapp/src/components/trading/workbench/workbench-status-bar.tsx</automated>
    <fails_when>카운트가 0 — 구간 배지가 77 원천을 쓰지 않는다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `queuedWindow` 모름에서 배지가 DOM 에 없음이 단언된다
    - 구간 배지 5종 문구가 원문으로 단언된다
    - M=0 에서 「신규」 필이 접근성 트리에도 없음이 단언된다
    - 단 수 세그먼트가 폰 밴드에서 접근성 트리에서 빠짐이 단언된다
    - 알림음 차단 상태 라벨과 제스처 내 `resume` 호출이 단언된다
    - 마감알림 토글이 존재함이 단언된다(기능 제거 0)
    - 상태줄 파일에 현재 시각 호출이 없다
  </acceptance_criteria>
  <done>상태줄 하나가 DMA·돌파·VI·거래 종목·구간·알림·단 수를 전부 말하고, 모르는 것은 모른다고 말한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `card-grid.tsx` — 펼침 우선 + 접힘 스택 한 칸</name>
  <files>webapp/src/components/trading/workbench/card-grid.tsx, webapp/src/components/trading/__tests__/card-grid.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/card/strategy-card.tsx (18-06 — 카드 계약과 `open` prop)
    - webapp/src/lib/breakout-list.ts (18-04 — `readColsPref`/`writeColsPref`)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §레이아웃 계약 9번 · §접근성 계약 「포커스 순서 — 카드 격자」 전문
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html (격자 열 수 CSS `:370-373` · 스택 렌더 `:1089-1096` · 하단 「설계 메모」)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md §목업에서 추출한 수치 표
  </read_first>
  <behavior>
    - 펼친 카드 2장 + 접힌 카드 3장, 2단 → DOM 순서가 [펼친 A][펼친 B][스택(접힌 3장)] 이고 스택이 정확히 한 칸을 차지한다.
    - 펼친 0장 + 접힌 N장 → 스택 한 칸만 있다.
    - 펼친 N장 + 접힌 0장 → 스택 칸이 렌더되지 않는다.
    - 카드 0장 → 격자 대신 빈 문구 2줄이 그려진다.
    - 스택 위에 라벨이 없다.
    - 헤더 클릭으로 펼침/접힘이 바뀌면 DOM 순서가 재계산된다(전환 애니메이션 클래스 없음).
    - `cols` 1/2/3 이 page 컨테이너 클래스로 반영되고, 폰 밴드에서는 저장값과 무관하게 1단이다.
    - 스택 안 카드 사이 간격이 8px.
  </behavior>
  <action>
`webapp/src/components/trading/workbench/card-grid.tsx` 를 신설한다. 이 컴포넌트는 **정렬 규칙 한 가지**만 안다 — 펼친 카드는 각각 한 칸, 접힌 카드는 전부 **스택 한 칸**에 세로로. 렌더 순서는 펼친 카드들 → 스택이다(포커스 순서가 곧 DOM 순서다).

열 수는 `data-cols` 속성 + page 컨테이너 쿼리로 적용한다 — 목업 `:370-373` 이 정본이고, 폰 밴드에서는 열 수 지정이 무시되어 항상 1단이다. JS 로 폭을 재지 않는다.

접힌 카드가 0장이면 스택 칸 자체를 렌더하지 않는다. 카드가 0장이면 격자 대신 빈 문구(UI-SPEC §Copywriting 의 Empty state heading + body 원문)를 그린다.

스택 위에 라벨을 넣지 않는다(D-09). 전환 애니메이션이 없다 — 펼침/접힘은 재렌더다.

포커스 규율(UI-SPEC §접근성): 접기로 카드가 스택으로 이동하면 포커스가 사라지므로 **토글을 누른 헤더 버튼으로 포커스를 되돌린다**(카드 헤더가 `aria-expanded` 를 갖는다 — 18-06). ✕ 로 카드를 제거하면 포커스를 다음 카드 헤더로, 없으면 종목 추가 검색란으로 옮긴다.

테스트 `__tests__/card-grid.test.tsx` 는 `<behavior>` 8케이스를 RTL 로 덮고, 특히 **펼침 0/1/2+ × 접힘 0/1/N 조합**의 DOM 구조를 단언한다. 실제 폭 기반 단 수 전환은 jsdom 이 평가하지 않으므로 클래스/속성까지만 단언하고 18-13 Playwright 로 넘긴다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- card-grid</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "transition|animate-|getBoundingClientRect|offsetWidth" webapp/src/components/trading/workbench/card-grid.tsx</automated>
    <fails_when>non-zero exit — 격자에 전환 애니메이션이 있거나 JS 로 폭을 재고 있다(D-09/D-28 위반)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "\b(sm|md|lg|xl):" webapp/src/components/trading/workbench/card-grid.tsx</automated>
    <fails_when>non-zero exit — 격자에 뷰포트 브레이크포인트가 섞였다(페이지 축은 컨테이너 `wb` 가 잰다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 펼침 0/1/2+ × 접힘 0/1/N 아홉 조합 중 대표 케이스가 DOM 구조로 단언된다
    - 접힌 카드 0장에서 스택 칸이 렌더되지 않음이 단언된다
    - 카드 0장에서 빈 문구 2줄이 원문으로 단언된다
    - 스택 위 라벨이 없음이 단언된다
    - 격자 파일에 애니메이션·JS 폭 측정·뷰포트 브레이크포인트가 없다
  </acceptance_criteria>
  <done>4종목 중 1종목만 펼쳐졌을 때 그 카드만 세로로 길고 나머지 3종목이 한 칸에 다 들어온다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `trading-workbench.tsx` + `app/trading/page.tsx` — 셸 조립과 카드 집합 단일 소유</name>
  <reversibility rating="costly">D-01 — 상따 화면 1,641+1,526줄과 VI 화면 663+951줄이 이 한 페이지로 재구성된다. 되돌리려면 18-11~18-13 커밋 범위를 통째로 `git revert` 해 옛 라우트 3개와 그 클라이언트 컴포넌트·e2e 스펙을 함께 복원해야 한다 — 이 플랜만 되감으면 18-12 의 리다이렉트가 갈 곳 없는 경로를 가리킨다.</reversibility>
  <files>webapp/src/app/trading/page.tsx, webapp/src/components/trading/workbench/trading-workbench.tsx, webapp/src/components/trading/__tests__/trading-workbench.test.tsx</files>
  <read_first>
    - webapp/src/app/trading/vi/page.tsx (라우트 엔트리 + AppShell 조립 패턴 — 이것을 본뜬다. **이 파일은 18-12 가 리다이렉트로 바꾸므로 지금 내용을 복사해 둘 것**)
    - webapp/src/components/trading/limit-chaser-client.tsx (`LimitChaserClient` 라우트 엔트리 · `useLeaveWarning` — 작업대 1곳으로 옮긴다)
    - webapp/src/components/trading/dma-gate.tsx (게이트 문구 정본 — 페이지 1곳에서만 감싼다)
    - webapp/src/lib/limit-chaser.ts (`parseStrategyKey` — `?focus=` 파싱)
    - webapp/src/components/trading/workbench/*.tsx · card/*.tsx (18-05~18-10 이 만든 블록들의 prop 계약)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §레이아웃 계약 표 10행 전부 · §반응형 계약
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md §Pattern 6(`focus` 파라미터 규율) · §Pitfall 9 · §Pitfall 10 · §O-4
  </read_first>
  <behavior>
    - DMA Ready 전 → `DmaGate` 문구만 보이고 격자·스트립이 렌더되지 않는다.
    - Ready 후 → 섹션 10개가 UI-SPEC 순서대로 렌더된다.
    - 돌파 칩 클릭 → 카드가 1장 생기고 거래소 KRX · 스위치 전부 OFF · 펼친 상태로 시작한다.
    - 이미 카드가 있는 ISIN 을 다시 클릭 → 카드 수가 늘지 않고 그 카드가 펼쳐지며 스크롤된다.
    - 종목 추가란 「추가」 → 같은 규칙으로 카드가 생긴다.
    - 카드 ✕ → 그 카드만 사라지고 포커스가 다음 카드 헤더(없으면 검색란)로 간다.
    - `?focus={키}` 로 진입 → 그 키의 카드가 펼쳐진 상태로 열리고, 이후 URL 이 바뀌어도(뒤로가기) 카드 상태를 다시 덮지 않는다.
    - 더티 카드가 있는 상태에서 페이지를 떠나려 하면 이탈 경고가 뜬다(더티 카드 수 합산).
    - 작업대가 `limitChasers` 를 카드에 **분배하지 않는다** — 카드가 컨텍스트에서 직접 읽는다.
    - 페이지 본문 래퍼가 `@container/wb` 를 갖는다.
  </behavior>
  <action>
`webapp/src/components/trading/workbench/trading-workbench.tsx` 를 신설한다. 이 컴포넌트가 **카드 집합 상태의 단일 소유자**다 — `{ isin, name?, code?, exchange, open }` 목록과 `cols`, 선택된 미체결 행, 계좌를 갖는다. 돌파 스트립·종목 추가란의 `onAddCard`/`onFocusCard`, 카드 헤더의 ✕·캐럿이 전부 이 상태를 바꾼다.

**에코를 분배하지 않는다**(Pitfall 9). 작업대는 `limitChasers` 배열을 카드에 내려주지 않는다 — 카드가 `useRelay()` 에서 직접 읽고 자기 `key` 로 `find` 한다. 작업대가 가운데에 서는 순간 상관의 두 번째 벌이 생긴다.

**재렌더 예산**(Pitfall 10): `useIsinLabels()` Map 을 카드에 통째로 내리지 않고 `labels.get(isin)` 결과 문자열만 내린다.

본문 래퍼에 `@container/wb` 를 선언하고 섹션 10개를 UI-SPEC §레이아웃 계약 표 순서대로 조립한다 — 제목줄(「트레이딩」 + 계좌 필) → 상태줄 → VI 설정 2줄 → VI 발동 스트립/표 → 돌파 스트립/표 → 종목 추가 → 카드 격자 → 공용 패널. `DmaGate` 는 **페이지 1곳에서만** 감싼다(카드마다 감싸지 않는다).

`?focus=` 는 `useSearchParams()` 로 읽되 **마운트 1회만 소비**하고 그 뒤에는 로컬 상태가 정본이다 — 뒤로가기로 카드가 다시 펼쳐지지 않게. 값은 `parseStrategyKey` 로 푼다.

`useLeaveWarning` 을 `limit-chaser-client.tsx` 에서 옮겨와 **작업대 1곳**에서 쓴다(더티 카드 수 합산). 카드마다 걸지 않는다.

`webapp/src/app/trading/page.tsx` 를 신설한다. `trading/vi/page.tsx` 의 라우트 엔트리 + AppShell 조립 패턴을 본뜨고 `<TradingWorkbench/>` 를 렌더한다. **`vi/page.tsx` 는 18-12 가 리다이렉트로 바꾸므로, 그 전에 여기서 필요한 조립 패턴을 옮겨 둔다.**

테스트 `__tests__/trading-workbench.test.tsx` 는 `<behavior>` 10케이스를 RTL 로 덮는다. 특히 「작업대가 카드에 전략 배열을 내리지 않는다」를 prop 형태로 단언한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- trading-workbench</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "@container/wb" webapp/src/components/trading/workbench/trading-workbench.tsx</automated>
    <fails_when>카운트가 0 — 페이지 축 컨테이너가 선언되지 않아 격자 열 수·세그먼트 전환이 죽는다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "DmaGate" webapp/src/components/trading/workbench/trading-workbench.tsx</automated>
    <fails_when>카운트가 0 — 게이트 없이 격자가 그려진다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && test -f webapp/src/app/trading/page.tsx</automated>
    <fails_when>non-zero exit — 라우트 엔트리가 없다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test</automated>
    <fails_when>non-zero exit, 또는 통과 수가 Phase 17 기준선 1008 보다 작다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build &amp;&amp; pnpm --filter @gh-radar/relay run typecheck &amp;&amp; pnpm --filter @gh-radar/relay run typecheck:tests &amp;&amp; pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
    <human-check>`/trading` 을 dev 에서 열어 목업 `18-workbench-mockup.html` 과 섹션 순서·배치·문구를 전체적으로 대조한다(목업이 정본 — D-26). 1·2·3단 세그먼트를 바꿔 보고, 카드를 2~4장 만들어 펼침/접힘 스택 정렬을 눈으로 확인한다.</human-check>
  </verify>
  <acceptance_criteria>
    - 섹션 10개가 UI-SPEC 순서대로 렌더됨이 단언된다
    - 돌파 칩/검색 추가가 KRX · 스위치 OFF 카드를 만들고 중복 추가가 없음이 단언된다
    - `?focus=` 가 마운트 1회만 소비됨이 단언된다
    - 작업대가 카드에 `limitChasers` 를 prop 으로 내리지 않음이 단언된다
    - `DmaGate` 가 페이지 1곳에만 있다
    - 본문 래퍼가 `@container/wb` 를 갖는다
    - config `build_command` 전문이 exit 0
  </acceptance_criteria>
  <done>`/trading` 한 페이지에서 탐색·추가·설정·주문·확인이 전부 끝난다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL 쿼리(`?focus=`) → 카드 상태 | 외부에서 오는 유일한 페이지 입력 |
| relay wss → 브라우저 | 상태줄·스트립·격자·패널이 표시하는 모든 값 |
| 브라우저 → localStorage | 단 수 · 알림 토글 2종 (기기별) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-52 | Tampering | 작업대가 에코를 분배 | critical | mitigate | 작업대는 전략 배열을 내리지 않는다. 카드가 컨텍스트에서 직접 읽고 자기 key 로 `find` — 테스트가 prop 부재를 단언 |
| T-18-53 | Input Validation | `?focus=` 조작 | medium | mitigate | `parseStrategyKey` 로만 해석하고 형식이 어긋나면 무시한다. 카드를 자동으로 **등록**하지 않는다(펼침만) |
| T-18-54 | Spoofing | 게이트 통과 전 격자 렌더 | high | mitigate | `DmaGate` 가 페이지를 대신한다. Ready 전 카드가 그려지면 값 없는 주문 표면이 노출된다 |
| T-18-55 | Information Disclosure | 상태줄 계좌 표시 | low | accept | 계좌 선택은 사용자가 봐야 하는 값이다. 로그·알림에는 싣지 않는다 |
| T-18-56 | Denial of Service | 카드 N개 재렌더 | medium | mitigate | `useIsinLabels` Map 을 내리지 않고 문자열만 내린다. 격자는 JS 로 폭을 재지 않는다 |
| T-18-57 | Tampering | 숨긴 컨트롤이 탭 체인에 남음 | low | mitigate | 폰 밴드 단 수 세그먼트는 접근성 트리에서도 빠진다(HTML `hidden`/조건부 렌더) |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 phase 는 새 패키지를 설치하지 않는다(RESEARCH §Package Legitimacy Audit 표 비어 있음) |
</threat_model>

## Artifacts this phase produces

새로 만드는 심볼·파일 (phase 전체 — 드리프트 검증에서 제외되어야 한다):

- **마이그레이션:** `supabase/migrations/20260921120000_dma_orders_modify_offhours.sql` · 컬럼 `dma_orders.piece_count` · `dma_orders.krx_session`
- **shared:** `RelayOrderModifyMsg` · `RelayKrxSession` · `OrderType` 의 `"M"` 멤버 · `RelayOrderNewMsg.pieceCount?` · `RelayOrderNewMsg.krxSession?` · `RelayRateCrossItem.name?` · `RelayRateCrossItem.code?`
- **relay:** `RelayOrderModifySchema` · `OrderInsertRow.pieceCount?` · `OrderInsertRow.krxSession?` · 돌파 항목 보강 private 메서드 · `relay/src/ws/__tests__/protocol.test.ts`
- **webapp lib:** `RelayOrderRequest.kind` 의 `"modify"` · `queued-window.ts`(`OrderButtonMode`·`ManualOrderAffordance`·`affordanceOf`) · `breakout-list.ts`(`kstDateKey`·`shouldRemoveBreakout`·`breakoutRowsFrom`·`BreakoutRow`·키 상수 4종·`REMOVE_MARGIN_PCT`·`ARM_GRACE_MS`·`HIGHLIGHT_MS`) · `use-breakout-quotes.ts`(`useBreakoutQuotes`·`MAX_BREAKOUT_SUBS`) · `alert-tone.ts`(`playBreakoutTone`·`isTonePlaybackBlocked`·`resumeToneContext`) · `limit-chaser.ts` 로 이동한 `parseStrategyKey` · `limit-chaser-client.tsx` 에서 옮겨온 `useLeaveWarning`
- **webapp 컴포넌트:** `components/trading/workbench/` 의 `TradingWorkbench`·`WorkbenchStatusBar`·`ViSettingsRows`·`ViTriggerStrip`·`BreakoutStrip`·`StockAddBar`·`CardGrid`·`SharedPanels` · `components/trading/card/` 의 `StrategyCard`·`CardHeader`·`QuoteGrid10`·`CardBody`·`ManualOrderForm`(+`canModify`)·`StockInfoModal` · `LimitChaserForm` 의 `dirtyHint?` prop · `AccountPanel` 의 `onSelectUnfilled?`/`selectedOrderNo?` prop
- **라우트:** `webapp/src/app/trading/page.tsx`
- **CSS 토큰:** `--new-bg` · `--new-bd` (globals.css §9 라이트/다크 양쪽) · 컨테이너 이름 `wb`
- **e2e:** `webapp/e2e/specs/trading-workbench.spec.ts`

<verification>
- config `build_command` 전문이 exit 0
- `pnpm --filter @gh-radar/webapp test` 전량 exit 0 (통과 수 1008 이상)
- `/trading` 이 dev 에서 렌더되고 섹션 10개가 순서대로 보인다
</verification>

<success_criteria>
- `/trading` 한 페이지에서 탐색 → 추가 → 설정 → 주문 → 확인이 끝난다
- 카드 집합의 소유자가 하나이고 에코 분배가 없다
- 격자가 펼침 우선 + 접힘 스택으로 정렬된다
- 페이지 축이 컨테이너 `wb` 로만 측정된다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-11-SUMMARY.md` when done
</output>