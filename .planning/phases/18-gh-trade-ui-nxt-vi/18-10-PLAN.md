---
phase: 18-gh-trade-ui-nxt-vi
plan: 10
type: execute
wave: 4
depends_on: ["18-06", "18-07"]
files_modified:
  - webapp/src/components/trading/card/card-body.tsx
  - webapp/src/components/trading/limit-chaser-form.tsx
  - webapp/src/components/stock/stock-orderbook-section.tsx
  - webapp/src/components/trading/__tests__/card-body.test.tsx
  - webapp/src/components/stock/__tests__/stock-orderbook-section.test.tsx
autonomous: true
requirements: [TRADE-07, TRADE-09]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-12: 카드 본문은 **어느 밴드에서도 좌 호가(+최근 체결) | 우 옵션 세팅 4그룹**이다. 세로 스택이 없고 좌 pane 은 오른쪽 테두리를 갖는다 (TRADE-09)"
    - "D-12: 옵션 4그룹은 매수주문 · 한방체결 · 매도주문 · 매수취소이고 필드는 기존 `limit-chaser-form` 그대로다. 2열 배치에서 좌열 = 매수+한방, 우열 = 매도+취소다 (TRADE-09)"
    - "D-12: **「호가」「체결」「옵션 세팅」 같은 섹션 라벨을 넣지 않는다**. 매수/매도/한방 켜기 스위치는 각 그룹 제목줄 우측 끝(44×26)이고, 그룹 제목 옆 상태 보조문은 「감시 중」/「무장 · 대기」/「꺼짐」/「켜짐」/「발주 완료 · 무장 해제」 다 (TRADE-09)"
    - "D-28: §2.2b 4밴드가 **카드 컨테이너 폭** 기준으로 적용된다 — 기존 `@min-[Npx]/lc:` 유틸리티를 그대로 쓰고 새 경계 숫자를 도입하지 않는다. 2·3단 격자에서 카드 안 밀도가 폰 밴드로 떨어지는 것은 사용자가 확인한 의도된 결과다 (TRADE-09)"
    - "D-28: 더티 액션 바는 `react-dom` `createPortal` 로 `document.body` 에 띄운다 — 컨테이너의 layout containment 때문이며 포털을 걷으면 바가 카드 안으로 내려앉는다. 카드가 여럿이므로 **바 문구에 종목명을 쓴다** (TRADE-09)"
    - "D-24: 종목상세 호가 탭이 **카드 본문과 같은 컴포넌트**를 쓴다 — 상태줄(DMA · 계좌 · 거래소 KRX|NXT 세그먼트 · LED 3칩 · 구간 배지 · 반영 시각) → 종목정보 10칸 → 좌 호가(+체결) | 우 옵션 4그룹 + 적응형 수동주문 → 미체결/잔고 순서다 (TRADE-09)"
    - "D-24: 호가 탭이 카드와 다른 점은 셋뿐이다 — ① 거래소가 헤더가 아니라 상태줄에 있다 ② 수동주문 폼에 주문유형 콤보가 있다 ③ 미체결이 이 종목만이다. 호가 탭 본문도 같은 이름의 컨테이너를 선언한다 (TRADE-09)"
    - "D-19: 수동주문 진입은 카드와 호가 탭이 **같은 적응형 한 규칙**이다 — 컨테이너 <700 은 3탭, ≥700 은 「수동주문」 버튼 덮기(옵션 값 보존) (TRADE-07)"
    - "D-23: 호가 탭 수동주문 폼에만 주문유형 콤보가 붙는다 — 카드 본문에 넘기는 `variant` 가 그것을 가른다 (TRADE-07)"
    - "E9 empty: 구독 전/호가 없음이면 10단 행은 그리되 가격·잔량이 「—」이고 체결 테이프는 비운다(문구 없음) (TRADE-09)"
    - "E9 loading: 「—」 유지 — 스피너 없음 (TRADE-09)"
    - "E9 error: 구독 실패 시 「—」 유지 + 연결 상태는 상태줄 DMA 필이 말한다. **값 없는 가격 셀 클릭은 no-op** 이다(수동주문 가격을 채우지 않는다) (TRADE-09)"
    - "E9 populated: 카드 폭 830 이상은 3단(매도잔량 · 가격 · 매수잔량), 그 아래는 2단(가격 · 잔량) · 호가표 폭은 §2.2b 밴드값 · 체결 수량 색은 거래소 체결구분 기준 · `aria-label=\"호가 10단\"`/`\"체결 테이프\"` (TRADE-09)"
    - "E9 partial: 10단 중 빈 단(잔량 0/가격 없음)은 해당 행이 「—」이고 행 수는 10 으로 고정된다 (TRADE-09)"
    - "E9 overflow: 행 수 10 · 행 높이 고정 · 체결 테이프는 최근 N건 고정(폰 밴드 숨김) · 내부 스크롤 없음 (TRADE-09)"
    - "E10 empty: 등록 전 카드는 기존 `limit-chaser-form` 기본값(스위치 OFF · 보조문 「꺼짐」)이고 필드 공란이면 「수정」이 disabled 다 (TRADE-09)"
    - "E10 loading: 전송 중 더티 바가 「반영 중…」 + 두 버튼 disabled. 스위치 토글 후 에코까지 pending (TRADE-09)"
    - "E10 error: 거부/타임아웃은 주문 응답 3분기 문구 · 검증 실패는 기존 게이트 문구 · 더티 중 에코는 「다른 단말에서 변경됨」 `role=\"status\"` (TRADE-09)"
    - "E10 partial: 필드별 더티(`● ` 접두 + `--primary`) · 4그룹 중 일부만 켜짐은 정상(LED 로 표시) · 서버 스냅샷에 필드가 없으면 기본값 (TRADE-09)"
    - "E15 loading: 더티 바 전송 중 문구가 「반영 중…」이고 「되돌리기」「수정」이 disabled (TRADE-09)"
    - "E15 error: 거부/타임아웃이면 바가 유지되고 기존 `dirty-action-bar` `role=\"status\"` 문구가 뜨며 더티 값이 보존된다 (TRADE-09)"
    - "E15 long-text: 「{종목명} · {N}개 미반영」 의 종목명은 1줄 ellipsis 이고 안내 문장은 폰에서 2줄 wrap 허용이다 (TRADE-09)"
    - statement: "폰 밴드에서 더티 바(z-40)와 sticky 공용 패널(z-20)이 겹치지 않음을 두 `boundingBox()` 로 실브라우저에서 단언한다 — z-index 로 덮는 해법은 금지다 (E15 overflow)"
      verification: backstop
  artifacts:
    - webapp/src/components/trading/card/card-body.tsx
  key_links:
    - "`card-body.tsx` ↔ `strategy-card.tsx` 의 `@container/lc` 선언 — 본문의 모든 밴드 전환이 그 선언에 매달린다"
    - "`card-body.tsx` ↔ `stock-orderbook-section.tsx` — **같은 컴포넌트**가 두 표면을 그린다. 호가 탭 본문도 같은 이름의 컨테이너를 선언해야 밴드가 작동한다"
    - "`limit-chaser-form` 의 `DirtyActionBar` 포털 ↔ `hint` prop — 종목명 삽입은 prop 값 변경만으로 끝난다(공유 컴포넌트는 무수정)"
  prohibitions:
    - "MUST NOT 카드 안에 뷰포트 브레이크포인트를 섞는다 — §2.2b 가 기록한 255px 역전이 되살아난다"
    - "MUST NOT §2.2b 밴드 수치를 컴포넌트 주석·PLAN·UI-SPEC 에 복사한다 — globals.css 를 가리킨다"
    - "MUST NOT 더티 액션 바의 포털을 걷어낸다 — 바가 카드 안으로 내려앉아 화면 하단 고정이 깨진다"
    - "MUST NOT 호가 사다리를 카드 전용으로 새로 만든다 — `variant=\"chaser\"` 의 3트리 배타 조건과 색·바 정규화 규칙을 공유한다"
---

<objective>
카드 본문(좌 호가+체결 | 우 옵션 4그룹 + 적응형 수동주문)을 조립하고, **같은 컴포넌트로** 종목상세 호가주문 탭을 교체한다. 이로써 「카드 본문 = 호가 탭 본문」이라는 D-24 의 약속이 코드 층위에서 성립한다.

Purpose: 이 플랜이 §2.2b 4밴드의 측정 대상 이관을 완성한다 — 컨테이너 선언은 18-06 이 카드로 옮겼고, 여기서 호가 탭 본문에도 같은 이름을 선언하면 43개 유틸리티가 두 표면을 동시에 지탱한다. 표를 한 줄도 복사하지 않고.
Output: `card/card-body.tsx` 신설 · `limit-chaser-form.tsx` 의 더티 힌트 prop 화 · `stock-orderbook-section.tsx` 교체.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-PATTERNS.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-orderbook-tab-mockup.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `card-body.tsx` — 좌 호가+체결 | 우 옵션 4그룹 (밴드는 카드 폭 기준)</name>
  <files>webapp/src/components/trading/card/card-body.tsx, webapp/src/components/trading/__tests__/card-body.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/limit-chaser-client.tsx (본문 그리드 조립부 — 좌/우 pane 구성과 `@min-[Npx]/lc:` 사용처)
    - webapp/src/components/orderbook/orderbook-ladder.tsx (`variant="chaser"` · 3트리 배타 조건이 `@min-[830px]/lc:`·`@min-[700px]/lc:` 에 달려 있는 지점 · 색·바 정규화 규칙 · 「JS 는 폭을 재지 않는다」 규율)
    - webapp/src/components/orderbook/trade-tape.tsx (체결 테이프 · 폰 밴드 숨김)
    - webapp/src/components/trading/limit-chaser-form.tsx (4그룹 필드·스위치 위치·더티 판정·`DirtyActionBar` 포털 호출부 주석 전문)
    - webapp/src/components/trading/card/manual-order-form.tsx (18-07 — `variant` prop 과 적응형 진입 계약)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §카드 계약 본문 문단 · §호가·체결 문구 · §수동주문 진입 · §반응형 계약
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html (본문 마크업 `:1072-1082` · 본문 그리드 CSS `:448-450` · 폼 라벨칸 `:522` · 수동주문 적응형 `:581-590`)
  </read_first>
  <behavior>
    - 좌 pane 에 호가 10단이 있고 `aria-label="호가 10단"`, 우 pane 에 옵션 4그룹이 있다.
    - 「호가」「체결」「옵션 세팅」 이라는 섹션 라벨 텍스트가 DOM 에 없다.
    - 각 그룹 제목줄 우측에 스위치가 있고 `aria-label` 이 「매수주문 켜기」/「한방체결 켜기」/「매도주문 켜기」.
    - 그룹 제목 옆 보조문이 상태에 따라 5문구 중 하나다.
    - 시세 없음 → 10단 행 10개가 그려지되 값이 「—」, 체결 테이프는 비어 있다.
    - 값 없는 가격 셀 클릭 → 수동주문 가격 콜백이 불리지 않는다.
    - 값 있는 가격 셀 클릭 → 가격 콜백이 1회 불린다.
    - 본문 래퍼가 `@min-[700px]/lc:`·`@min-[830px]/lc:`·`@min-[992px]/lc:` 를 쓰고 뷰포트 브레이크포인트를 쓰지 않는다.
    - `variant="card"` 면 수동주문에 주문유형 콤보가 없고, `variant="orderbook"` 이면 있다.
    - 더티 상태에서 `DirtyActionBar` 가 `document.body` 에 포털되고 그 `hint` 문구에 종목명이 들어 있다.
  </behavior>
  <action>
`webapp/src/components/trading/card/card-body.tsx` 를 신설하고 `limit-chaser-client.tsx` 의 본문 그리드 조립을 **옮긴다**. 좌 pane 은 `OrderbookLadder variant="chaser"` + `TradeTape`(폰 밴드 숨김), 우 pane 은 `LimitChaserForm`(4그룹) + 적응형 `ManualOrderForm`. 좌 pane 에 `border-right: 1px solid var(--border)`.

**밴드 클래스는 기존 것을 그대로 쓴다.** 본문 그리드는 `@min-[700px]/lc:`·`@min-[830px]/lc:`·`@min-[992px]/lc:` 로 1열 → 2열(호가 폭 3단계)을 전환한다. 숫자는 §2.2b 정본에서 온 것이고 **주석에 표를 다시 적지 않는다** — 「밴드 수치의 정본은 `webapp/src/styles/globals.css` §2.2b」 한 줄만 남긴다. 뷰포트 브레이크포인트를 섞지 않는다.

**섹션 라벨을 넣지 않는다**(D-12). 「호가」「체결」「옵션 세팅」 같은 제목 텍스트가 없다 — 사용자가 「쓸데없는 라벨링, 공간만 차지」라고 명시적으로 뺀 것이다. 그룹 제목(「매수주문」「한방체결」「매도주문」「매수취소」)과 보조문은 남는다.

**적응형 수동주문(D-19):** 컨테이너 <700 이면 옵션 영역이 「매수 | 매도 | 수동」 3탭이 되고, ≥700 이면 옵션 우상단 「수동주문」 버튼이 폼으로 옵션을 덮는다. 덮는 동안 옵션 값은 보존된다(언마운트하지 않는다). 탭/덮기 전환 클래스는 기존 `limit-chaser-form.tsx` 의 pane 전환 패턴(`hidden @min-[700px]/lc:block` 계열)과 같은 문법을 쓴다.

**`variant` prop** 으로 카드용/호가탭용을 가른다 — `ManualOrderForm` 의 주문유형 콤보 유무가 이것으로 결정된다. 다른 차이는 상위(헤더 vs 상태줄의 거래소, 미체결 범위)가 담당하므로 본문 컴포넌트에는 들어오지 않는다.

**호가 사다리를 새로 만들지 않는다** — `variant="chaser"` 의 3트리 배타 조건(830 이상 3단 / 700–829 2단 / 700 미만)과 색·바 정규화 규칙을 공유한다. 값 없는 가격 셀 클릭은 no-op 이다.

테스트 `__tests__/card-body.test.tsx` 는 `<behavior>` 10케이스를 RTL 로 덮는다. 컨테이너 쿼리 자체는 jsdom 이 평가하지 않으므로 **클래스 존재**로 단언하고, 실제 전환은 18-13 Playwright 가 맡는다는 사실을 SUMMARY 에 적는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- card-body</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "\b(sm|md|lg|xl):" webapp/src/components/trading/card/card-body.tsx</automated>
    <fails_when>non-zero exit — 카드 본문에 뷰포트 브레이크포인트가 섞였다(D-28 위반)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "variant=\"chaser\"" webapp/src/components/trading/card/card-body.tsx</automated>
    <fails_when>카운트가 0 — 공유 호가 사다리를 쓰지 않고 새로 그렸다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -cE "@min-\[(700|830|992)px\]/lc:" webapp/src/components/trading/card/card-body.tsx</automated>
    <fails_when>카운트가 3 미만 — 세 밴드 경계 전부가 본문 그리드에 걸려 있지 않다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 섹션 라벨 3종이 DOM 에 없음이 단언된다
    - 그룹 제목줄 스위치 3개의 `aria-label` 이 단언된다
    - 값 없는 가격 셀 클릭 no-op 이 단언된다
    - `variant` 에 따른 주문유형 콤보 유무가 단언된다
    - 본문 파일에 뷰포트 브레이크포인트가 없고 세 밴드 클래스가 전부 있다
    - 주석에 §2.2b 밴드 수치 표가 복사돼 있지 않다(정본 경로만 가리킨다)
  </acceptance_criteria>
  <done>카드 본문이 어느 폭에서도 좌 호가 | 우 옵션이고, 밴드 전환이 카드 폭을 따른다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `limit-chaser-form.tsx` — 더티 힌트 prop 화 (카드마다 종목명)</name>
  <files>webapp/src/components/trading/limit-chaser-form.tsx, webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/limit-chaser-form.tsx (`DirtyActionBar` 포털 호출부와 그 위 ★ 주석 블록 전문 · 현행 고정 힌트 문구 · 탭/2열 전환 클래스 패턴)
    - webapp/src/components/trading/dirty-action-bar.tsx (`hint`·`className` prop 이 이미 열려 있다는 사실 · `fixed … z-40` · 「z-index 로 FAB 을 덮는 것은 해법이 아니다」 주석)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §카드 문구표의 더티 바 행 원문
  </read_first>
  <behavior>
    - `dirtyHint` prop 을 주면 더티 바 문구에 그 값이 들어간다.
    - prop 을 주지 않으면 기존 문구가 그대로다(기존 호출부 무영향).
    - 더티 바가 `document.body` 의 자손이고 폼 래퍼의 자손이 아니다.
    - SSR/마운트 전에는 포털을 만들지 않는다.
    - 전송 중에는 문구가 「반영 중…」이고 두 버튼이 disabled.
    - 거부/타임아웃 후에도 바가 남고 더티 값이 보존된다.
    - 종목명이 길면 1줄 ellipsis.
  </behavior>
  <action>
`limit-chaser-form.tsx` 의 `DirtyActionBar` 호출부에서 힌트 문구를 **prop 으로 받는다** — `dirtyHint?: string` 을 폼 props 에 더하고, 없으면 현행 고정 문구를 쓴다. 공유 컴포넌트 `dirty-action-bar.tsx` 는 **한 줄도 고치지 않는다**(`hint`·`className` 이 이미 열려 있다).

포털 규율 ★ 주석 블록은 **그대로 유지**하고 한 줄만 덧붙인다 — 「카드가 여럿이므로 바 문구에 종목명을 쓴다. 바가 어느 카드의 것인지 말하는 유일한 채널이고, 시각·스크린리더 양쪽에서 그렇다.」 포털을 걷지 않는다(걷으면 바가 뷰포트 하단이 아니라 본문 끝에 앉는다). SSR 가드(마운트된 뒤에만 포털)도 그대로다.

문구는 UI-SPEC §카드 문구표의 더티 바 행 원문을 쓴다 — 「{종목명} · {N}개 미반영」 + 안내 문장 + 버튼 2개 + 전송 중 「반영 중…」. 종목명은 1줄 ellipsis 이고 안내 문장은 폰에서 2줄 wrap 허용이다.

기존 테스트가 고정 문구를 단언하고 있으면 **기본값 경로로 그대로 통과**해야 한다 — 회귀 0 이 이 태스크의 성공 기준이다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- limit-chaser-form</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && git diff --stat -- webapp/src/components/trading/dirty-action-bar.tsx</automated>
    <fails_when>출력이 비어 있지 않다 — 공유 더티 바 컴포넌트를 고쳤다(prop 만으로 끝나야 한다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "createPortal" webapp/src/components/trading/limit-chaser-form.tsx</automated>
    <fails_when>카운트가 0 — 포털을 걷어냈다(바가 카드 안으로 내려앉는다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `dirtyHint` 를 주면 문구에 반영되고, 안 주면 기존 문구임이 단언된다
    - 더티 바가 `document.body` 자손임이 단언된다
    - `dirty-action-bar.tsx` 의 git diff 가 비어 있다
    - 기존 `limit-chaser-form` 테스트가 전부 통과한다(회귀 0)
    - 포털 ★ 주석 블록이 유지되고 카드 N개 관련 한 줄이 추가됐다
  </acceptance_criteria>
  <done>카드가 여럿이어도 더티 바가 어느 카드의 것인지 말한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `stock-orderbook-section.tsx` — 호가 탭 본문을 카드 본문으로 교체</name>
  <files>webapp/src/components/stock/stock-orderbook-section.tsx, webapp/src/components/stock/__tests__/stock-orderbook-section.test.tsx</files>
  <read_first>
    - webapp/src/components/stock/stock-orderbook-section.tsx (전문 — 현행 조립과 이 종목 한정 미체결 범위)
    - webapp/src/components/stock/stock-detail-tabs.tsx (호가주문 탭이 떠나면 언마운트되는 규율 주석)
    - webapp/src/app/stocks/[code]/page.tsx (넓은 컨테이너 규율 주석 `:23` 부근)
    - webapp/src/components/trading/card/card-body.tsx (Task 1 결과 — `variant` 계약)
    - webapp/src/components/orderbook/relay-status-bar.tsx (기존 상태줄 — 거래소 세그먼트가 붙을 자리)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §호가 탭 계약 · §수동주문 각주(호가 탭)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-orderbook-tab-mockup.html (전문 — 이 태스크의 레이아웃 정본)
  </read_first>
  <behavior>
    - 호가 탭 본문 래퍼가 카드와 **같은 이름의 컨테이너**를 선언한다.
    - 본문이 `CardBody variant="orderbook"` 을 쓴다(호가 사다리·옵션 4그룹을 이 파일이 다시 조립하지 않는다).
    - 상태줄에 거래소 KRX|NXT 세그먼트가 있고 카드 헤더에는 없던 자리다.
    - 상태줄에 DMA 상태 · 계좌 · LED 3칩 · 구간 배지 · 반영 시각이 있다.
    - 수동주문 폼에 주문유형 콤보가 있다.
    - 미체결 표가 이 종목만 보여준다.
    - 각주 문장이 정정 지원을 반영한 새 원문이다.
    - 탭을 떠나면 언마운트된다(실시간 호가 구독을 보이지 않는 탭에서 유지하지 않는다).
  </behavior>
  <action>
`webapp/src/components/stock/stock-orderbook-section.tsx` 의 본문을 `CardBody variant="orderbook"` 으로 교체한다. 이 파일이 호가 사다리·체결 테이프·옵션 4그룹을 직접 조립하던 부분을 걷어내고, **본문 래퍼에 카드와 같은 이름의 컨테이너를 선언**한다 — 그래야 `@min-[Npx]/lc:` 유틸리티가 호가 탭 본문 폭을 잰다.

상단 순서는 UI-SPEC §호가 탭 계약 그대로다 — 히어로(기존) → 갱신줄(기존) → 4탭 바(기존) → **상태줄** → 종목정보 10칸(`QuoteGrid10`) → 본문 → 미체결/잔고. 상태줄에는 DMA 상태 · 계좌 · **거래소 KRX|NXT 세그먼트** · LED 3칩 · 구간 배지 · 반영 시각이 들어간다(카드는 거래소가 헤더에 있지만 호가 탭은 상태줄이다 — 다른 점 셋 중 ①).

수동주문 각주는 기존 「정정 미지원」을 알리던 문장을 UI-SPEC §수동주문 표의 새 각주 원문으로 교체한다 — 정정이 열렸으므로 옛 문장은 거짓이다. 매수 비율 버튼이 없는 것은 기존 그대로다.

미체결은 **이 종목만** 보여준다(다른 점 셋 중 ③) — 작업대 공용 패널과 달리 종목 필터가 걸린다. 행 선택 → 수동주문 정정/취소 연결은 작업대와 같은 계약이다.

탭을 떠나면 언마운트되는 기존 규율을 유지한다.

레이아웃 정본은 `18-orderbook-tab-mockup.html` 이다(D-26) — 목업이 코드와 다르면 목업이 이긴다.

테스트 `__tests__/stock-orderbook-section.test.tsx` 는 `<behavior>` 8케이스를 덮는다. 기존 `orderbook.spec.ts`(e2e)가 깨지지 않게 주요 `data-*`/`aria-label` 훅을 유지하고, 불가피하게 바뀌는 셀렉터는 SUMMARY 에 목록으로 남겨 18-13 이 e2e 를 함께 고치게 한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- stock-orderbook-section</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "@container/lc" webapp/src/components/stock/stock-orderbook-section.tsx</automated>
    <fails_when>카운트가 0 — 호가 탭 본문이 컨테이너를 선언하지 않아 밴드 전환이 죽는다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "CardBody" webapp/src/components/stock/stock-orderbook-section.tsx</automated>
    <fails_when>카운트가 0 — 본문을 카드 본문 컴포넌트로 교체하지 않고 다시 조립했다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test</automated>
    <fails_when>non-zero exit, 또는 통과 수가 Phase 17 기준선 1008 보다 작다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build &amp;&amp; pnpm --filter @gh-radar/relay run typecheck &amp;&amp; pnpm --filter @gh-radar/relay run typecheck:tests &amp;&amp; pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 호가 탭 본문이 카드와 같은 컨테이너 이름을 선언한다
    - 본문이 `CardBody` 를 쓰고 호가 사다리·옵션 4그룹을 직접 조립하지 않는다
    - 상태줄에 거래소 세그먼트가 있고 수동주문에 주문유형 콤보가 있다
    - 미체결이 이 종목만임이 단언된다
    - 새 각주 원문이 단언된다
    - 바뀐 e2e 셀렉터 목록이 SUMMARY 에 있다
    - config `build_command` 전문이 exit 0
  </acceptance_criteria>
  <done>종목상세 호가 탭과 작업대 카드가 같은 컴포넌트·같은 밴드 규율로 그려진다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay wss → 브라우저 (호가·체결·전략 에코) | 본문이 표시하는 모든 값의 출처 |
| 사용자 클릭(호가 셀) → 수동주문 가격 | 가격이 폼으로 들어가는 경로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-47 | Tampering | 값 없는 호가 셀로 가격 채움 | high | mitigate | 값 없는 셀 클릭은 no-op. 테스트가 단언 |
| T-18-48 | Tampering | 다른 종목 호가로 주문 | critical | mitigate | 본문이 상위에서 받은 단일 `isin`/`exchange` 의 시세만 렌더하고, 수동주문 폼도 같은 값을 쓴다 |
| T-18-49 | Denial of Service | 더티 바 포털 제거 | medium | mitigate | `createPortal` 존재를 verify 가 grep. 포털을 걷으면 바가 카드 안으로 내려앉아 「수정」에 도달할 수 없다 |
| T-18-50 | Tampering | 호가 사다리 재구현 | medium | mitigate | `variant="chaser"` 공유. 3트리 배타 조건·색·바 정규화를 두 벌로 만들지 않는다 |
| T-18-51 | Information Disclosure | 호가 탭 미체결 범위 | low | mitigate | 이 종목만 필터. 다른 종목 주문번호가 이 화면에 노출되지 않는다 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 phase 는 새 패키지를 설치하지 않는다(RESEARCH §Package Legitimacy Audit 표 비어 있음) |
</threat_model>

## Artifacts this phase produces

새로 만드는 심볼·파일 (phase 전체 — 드리프트 검증에서 제외되어야 한다):

- **마이그레이션:** `supabase/migrations/20260921120000_dma_orders_modify_offhours.sql` · 컬럼 `dma_orders.piece_count` · `dma_orders.krx_session`
- **shared:** `RelayOrderModifyMsg` · `RelayKrxSession` · `OrderType` 의 `"M"` 멤버 · `RelayOrderNewMsg.pieceCount?` · `RelayOrderNewMsg.krxSession?` · `RelayRateCrossItem.name?` · `RelayRateCrossItem.code?`
- **relay:** `RelayOrderModifySchema` · `OrderInsertRow.pieceCount?` · `OrderInsertRow.krxSession?` · 돌파 항목 보강 private 메서드 · `relay/src/ws/__tests__/protocol.test.ts`
- **webapp lib:** `RelayOrderRequest.kind` 의 `"modify"` · `queued-window.ts`(`OrderButtonMode`·`ManualOrderAffordance`·`affordanceOf`) · `breakout-list.ts`(`kstDateKey`·`shouldRemoveBreakout`·`breakoutRowsFrom`·`BreakoutRow`·키 상수 4종·`REMOVE_MARGIN_PCT`·`ARM_GRACE_MS`·`HIGHLIGHT_MS`) · `use-breakout-quotes.ts`(`useBreakoutQuotes`·`MAX_BREAKOUT_SUBS`) · `alert-tone.ts`(`playBreakoutTone`·`isTonePlaybackBlocked`·`resumeToneContext`) · `limit-chaser.ts` 로 이동한 `parseStrategyKey`
- **webapp 컴포넌트:** `components/trading/workbench/` 의 `TradingWorkbench`·`WorkbenchStatusBar`·`ViSettingsRows`·`ViTriggerStrip`·`BreakoutStrip`·`StockAddBar`·`CardGrid`·`SharedPanels` · `components/trading/card/` 의 `StrategyCard`·`CardHeader`·`QuoteGrid10`·`CardBody`·`ManualOrderForm`(+`canModify`)·`StockInfoModal` · `LimitChaserForm` 의 `dirtyHint?` prop · `AccountPanel` 의 `onSelectUnfilled?`/`selectedOrderNo?` prop
- **라우트:** `webapp/src/app/trading/page.tsx`
- **CSS 토큰:** `--new-bg` · `--new-bd` (globals.css §9 라이트/다크 양쪽)
- **e2e:** `webapp/e2e/specs/trading-workbench.spec.ts`

<verification>
- config `build_command` 전문이 exit 0
- `pnpm --filter @gh-radar/webapp test` 전량 exit 0 (통과 수 1008 이상)
- `dirty-action-bar.tsx` 무수정(git diff 비어 있음)
- 카드 본문·호가 탭 본문에 뷰포트 브레이크포인트 0건
</verification>

<success_criteria>
- 카드 본문과 호가 탭 본문이 같은 컴포넌트다
- §2.2b 표를 한 줄도 복사하지 않고 두 표면이 같은 밴드를 쓴다
- 더티 바가 카드마다 종목명을 말하고 여전히 body 포털이다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-10-SUMMARY.md` when done
</output>