---
phase: 18-gh-trade-ui-nxt-vi
plan: 07
type: execute
wave: 3
depends_on: ["18-01", "18-04"]
files_modified:
  - webapp/src/components/trading/card/manual-order-form.tsx
  - webapp/src/components/orderbook/order-confirm-dialog.tsx
  - webapp/src/components/trading/__tests__/manual-order-form.test.tsx
  - webapp/src/components/orderbook/__tests__/order-confirm-dialog.test.tsx
autonomous: true
requirements: [TRADE-07]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-20: 수동주문 폼은 가격 · 수량 · (예약구간 ∧ KRX 일 때만) 조각 수 · 주문금액 · **「매수 · 매도 · 정정 · 취소」 4버튼 한 줄** 로 구성된다. 계좌 행 · 가격 ±버튼 · 비율(10/25/50/100%) 버튼 · 「호가 사다리를 누르면…」 안내는 **없다** (TRADE-07)"
    - "D-20: 기존 `order-panel` 의 오조작 방지 규율 중 **단일 제출 버튼 규율만** D-20 이 명시적으로 뒤집는다(gh-trade WinForms 종합주문창 동형). 확인 다이얼로그 필수 · 제출 후 즉시 재활성 금지 · `timeout` 은 실패가 아님 · LOCKED 색 규칙은 **한 글자도 완화되지 않는다** (TRADE-07)"
    - "D-21: 정정·취소 버튼은 미체결 행이 선택됐을 때만 활성이다. 선택되면 폼 위에 「원주문 {No}」 + 「{매수|매도} {가격} × {수량}」 칩이 뜨고 가격·수량이 원주문 값으로 채워진다. 선택이 없으면 두 버튼 `disabled` (TRADE-07)"
    - "D-21: 시간외종가 원주문(`board` G2/G3) · 예약 Q-ID 행(`queuedStatus` 비어 있지 않음) · 취소 보관 행(`pendingCancelSent`)은 정정이 잠긴다 — 서버가 어차피 거부하므로 UI 가 먼저 막는다. relay 는 이 판정을 하지 않는다 (TRADE-07)"
    - "D-22: 버튼 라벨·조각 입력 표시·확인 문구는 전부 `affordanceOf` 한 함수의 반환으로 결정되고, 폼이 벽시계를 읽거나 77 값으로 제출을 막지 않는다 (TRADE-07)"
    - "D-22: 예약구간(`open ∧ KRX`)에서만 조각 수 스테퍼가 보이고 초기값 5 · 상한은 서버 `maxPieces` 다. 그 밖에서는 조각 수를 **보내지 않는다**(부재 = 1) — 화면에 없는 값을 싣지 않는다 (TRADE-07)"
    - "D-23: 주문유형 콤보(지정가 | 시간외종가)는 **호가 탭 폼에만** 있다. 작업대 카드의 수동주문에는 주문유형이 없다(WinForms 상따창 동형). 콤보는 `KRX ∧ (g2Open ∨ g3Open)` 일 때만 시간외종가를 고를 수 있고, 창이 닫히면 지정가로 복귀한다 (TRADE-07)"
    - "D-23: 시간외종가 선택 시 가격 입력이 잠기고(값 「—」) 「참고 종가」가 표시되며 `price 0` + `krxSession` 이 송신되고 **정정 버튼이 비활성**이다. 버튼 라벨은 창과 무관하게 「매수/매도」(조각 1) (TRADE-07)"
    - "D-19: 폼 진입은 적응형 한 규칙이다 — 컨테이너 <700 이면 「매수 | 매도 | 수동」 3탭, ≥700 이면 옵션 우상단 「수동주문」 버튼으로 옵션 영역을 폼이 덮고 ✕ 로 닫으며 **옵션 값은 보존**된다 (TRADE-07)"
    - "D-27: 주문 응답 3분기 문구가 기존 원문 그대로다 — `accepted` 「주문이 접수됐어요 · 주문번호 {No}」 / `rejected` 「주문이 거부됐어요 · {message}」(파싱 금지·표시만) / `timeout` 「접수 응답이 늦어지고 있어요」 + 재확인 안내. 인라인 `role=\"status\"` 이며 토스트 라이브러리를 쓰지 않는다 (TRADE-07)"
    - "D-20: 호가 사다리 가격 셀 클릭은 폼의 가격을 채운다. 값이 없는 가격 셀 클릭은 no-op 다 (TRADE-07)"
    - "E11 empty: 빈 폼은 가격·수량 공란 · 원주문 미선택 → 「정정」「취소」 disabled · 「매수」「매도」는 활성이되 제출 시 기존 검증 문구가 뜬다 (TRADE-07)"
    - "E11 loading: 제출 → 응답까지 4버튼 전부 disabled(기존 pending 규율). 지연 시 `timeout` 분기 문구 (TRADE-07)"
    - "E11 error: `rejected`·`timeout`·검증 실패 문구가 전부 기존 원문이고 인라인 `role=\"status\"` 로 뜬다 (TRADE-07)"
    - "E11 partial: 원주문 선택 시 가격·수량이 채워지고 칩이 뜨며 해제해도 값은 유지된다. 조각 수는 `open ∧ KRX` 에서만 보인다. 시간외종가 선택 시 가격 잠금 「—」 + 「참고 종가」 (TRADE-07)"
    - "E11 long-text: 칩은 mono 고정폭 · 예약/시간외종가 안내 문장은 wrap · 폰 밴드에서 4버튼의 「예약」이 윗줄로 접힌다 (TRADE-07)"
    - "E12 loading: 확정 버튼 클릭 → 즉시 disabled · 다이얼로그 닫힘 · 이후 상태는 폼의 pending/응답 문구로 이어진다 (TRADE-07)"
    - "E12 overflow: 요약 dt/dd 는 세로 스택이고 폰에서 본문이 세로 스크롤되며 Radix Dialog focus trap 이 유지된다 (TRADE-07)"
    - "E12 long-text: 종목명은 wrap 허용(확인 화면이므로 ellipsis 없이 전체 노출) · 예약 줄·경고 박스 문장도 wrap (TRADE-07)"
  artifacts:
    - webapp/src/components/trading/card/manual-order-form.tsx
  key_links:
    - "`affordanceOf`(18-04) ↔ 버튼 라벨 · 조각 입력 표시 · 확인 문구 — 카드 폼과 호가 탭 폼이 같은 함수를 부른다"
    - "미체결 행 선택 ↔ 폼의 `orgOrderNo`/가격/수량 — 선택 해제 없이 정정·취소가 나갈 수 없다"
    - "`sendOrder({kind})` ↔ 18-01 의 `buildOrderFrame` 3분기 — 폼은 프레임을 직접 만들지 않는다"
  prohibitions:
    - "MUST NOT `timeout` 을 실패로 표시하거나 재시도를 유도하는 문구를 쓴다 — 이중 체결 위험이다. 「결과 모름」은 실패가 아니다"
    - "MUST NOT 확인 다이얼로그를 건너뛰는 제출 경로를 만든다 — 4버튼 전부 다이얼로그를 거치고 기본 포커스는 취소 버튼이다"
    - "MUST NOT 제출 후 응답 전에 버튼을 재활성한다 — 중복 제출 가드를 유지한다"
    - "MUST NOT `OrderResp.message` 문구를 파싱해 분기한다 — 표시만 한다"
---

<objective>
카드와 종목상세 호가 탭이 **함께 쓰는** 수동주문 폼을 만든다 — 가격 · 수량 · (예약구간) 조각 수 · 주문금액 · 「매수 · 매도 · 정정 · 취소」 4버튼, 미체결 행 선택으로 열리는 정정/취소, 호가 탭 전용 주문유형 콤보(시간외종가). 주문확인 다이얼로그는 정정·예약·시간외종가 요약 행을 받도록 넓힌다.

Purpose: 이 폼이 TRADE-07 의 UI 절반이다. 기존 `order-panel.tsx`(693줄)의 다이어트 재작성인데, **무엇이 빠지고 무엇이 절대 빠지지 않는가**가 계약이다 — 비율 버튼·가격 ±·계좌 행·안내 라벨은 빠지고, 확인 다이얼로그·중복 제출 가드·`timeout≠실패`·LOCKED 색 규칙은 그대로 남는다.
Output: `components/trading/card/manual-order-form.tsx` 신설 + `order-confirm-dialog.tsx` 확장.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-PATTERNS.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-orderbook-tab-mockup.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `manual-order-form.tsx` — 4버튼 폼 · 적응형 진입 · 77 라벨 연결</name>
  <files>webapp/src/components/trading/card/manual-order-form.tsx, webapp/src/components/trading/__tests__/manual-order-form.test.tsx</files>
  <read_first>
    - webapp/src/components/orderbook/order-panel.tsx (전문 — 파일 머리 5규율 주석 원문, 제출 pending 가드, 응답 3분기 문구(`:377`, `:611-642`), 검증 문구(`:175`), 각주 문장(`:581`), 비율/±/계좌 행 — **무엇을 빼는지** 확인용)
    - webapp/src/lib/queued-window.ts (18-04 의 `affordanceOf`·`ManualOrderAffordance`·예약 안내 문구 상수)
    - webapp/src/lib/relay-provider.tsx (`sendOrder`·`RelayOrderRequest` 3-kind — 18-01 확장분)
    - webapp/src/components/orderbook/order-confirm-dialog.tsx (현행 다이얼로그 계약)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §수동주문 문구표 · §77 매핑 표 · §시간외종가 문단 · §접근성 계약(포커스 순서 4·수동주문 닫기 Escape)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html (수동주문 적응형 `:571-619` · 4버튼 `:597` · 렌더 `:1017-1033`)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-orderbook-tab-mockup.html (수동주문 `:811-835` · 확인 `:863-875`)
  </read_first>
  <behavior>
    - 빈 폼: 「정정」「취소」 disabled, 「매수」「매도」 활성.
    - 가격 공란으로 「매수」 제출 → 다이얼로그 전에 검증 문구(기존 원문)가 인라인으로 뜨고 전송이 없다.
    - 유효한 값으로 「매수」 → 확인 다이얼로그가 열리고 기본 포커스가 취소 버튼.
    - 다이얼로그 확정 → `sendOrder` 1회 · 4버튼 즉시 disabled · 응답 전 재클릭해도 두 번째 전송이 없다.
    - 응답 `timeout` → 문구가 「접수 응답이 늦어지고 있어요」 + 재확인 안내이고, 재시도를 권하는 표현이 없다.
    - `affordanceOf` 가 `queued` 를 주면 버튼 라벨이 「예약매수/예약매도」이고 조각 스테퍼가 보인다(초기 5 · 상한 `maxPieces`).
    - 조각 스테퍼가 보이지 않는 상태에서 제출하면 요청에 조각 수가 실리지 않는다.
    - 컨테이너 <700 → 「매수 | 매도 | 수동」 3탭 진입. ≥700 → 「수동주문」 버튼이 옵션 영역을 덮고 ✕/Escape 로 닫히며 옵션 값이 보존된다.
    - 호가 사다리 가격 셀 클릭 콜백이 가격 입력을 채우고, 값 없는 셀은 no-op.
  </behavior>
  <action>
`webapp/src/components/trading/card/manual-order-form.tsx` 를 신설한다. `order-panel.tsx` 의 **다이어트 재작성**이다.

**파일 머리 주석에 무엇이 살고 무엇이 뒤집혔는지 적는다.** 살아남는 규율 — ① 색·위치·문구 3중 일치(매수 = `--up` 빨강 · 왼쪽 · 「매수」, 매도 = `--down` 파랑) ② 확인 다이얼로그 필수, 기본 포커스는 취소 ③ 제출 후 응답 전까지 비활성 + 「주문 전송 중…」 + 중복 제출 가드 ④ 결과는 셋이고 `status:"timeout"` 은 **실패가 아니다** ⑤ LOCKED 색 규칙(accent 파랑 토큰은 `--down` 과 값이 같으므로 이 파일에 등장하지 않는다). 뒤집힌 규율 — **단일 제출 버튼 규율**이 D-20 으로 뒤집혀 4버튼이 한 줄에 선다. 뒤집힌 이유(사용자 결정 D-20 · 정본 gh-trade WinForms 종합주문창)를 그 자리에 적는다. `catch` 로 예외를 삼키는 코드를 만들지 않는 것도 그대로다.

**제거 대상(D-20):** 계좌 행 · 가격 ± 버튼 · 비율(10/25/50/100%) 버튼 · 「호가 사다리를 누르면…」 안내 라벨. 매수 비율 버튼이 원래 없던 것은 기존 결정 그대로다.

**구성:** 라벨 「가격」(원) · 「수량」(주) · (조건부) 「조각 수」(「/ 최대 {max}」 + 스테퍼 `aria-label` 「조각 줄이기」/「조각 늘리기」) · 「주문금액」 파생값 · 4버튼 한 줄(`grid-template-columns: repeat(4, minmax(0,1fr))`). 폰 밴드에서 4버튼이 좁아지면 「예약」이 윗줄로 접힌다(10px, `line-height:1.15`).

**적응형 진입(D-19):** 컨테이너 `lc` 폭 <700 이면 옵션 영역이 「매수 | 매도 | 수동」 3탭이 되고 수동 탭이 이 폼이다. ≥700 이면 옵션 우상단 「수동주문」 버튼이 폼을 띄워 옵션 영역을 덮고, 헤더에 「수동주문」 + 「키 {ISIN}:{계좌}:{거래소}」 + ✕(`aria-label` 「수동주문 닫기」)가 붙는다. Escape 로도 닫히고 닫은 뒤 포커스는 「수동주문」 버튼으로 복귀한다. **옵션 값은 폼을 덮는 동안 보존된다**(언마운트하지 않거나 상태를 상위에 둔다).

**77 연결:** 버튼 라벨·조각 입력 표시·확인 문구는 전부 `affordanceOf(queuedWindow, exchange, orderType)` 반환으로 결정한다. 폼이 직접 창을 판정하지 않고, 이 값으로 제출을 막지도 않는다. 조각 수는 스테퍼가 **보일 때만** 요청에 싣는다.

**주문유형(D-23):** `variant` prop 으로 카드용/호가탭용을 가른다. 카드용에는 주문유형 콤보가 **없다**. 호가탭용에만 「지정가 | 시간외종가」 콤보가 있고, `affordanceOf(...).offHoursSelectable` 이 false 면 시간외종가 옵션이 `disabled` + `title`(UI-SPEC 원문)이며 이미 선택돼 있었다면 지정가로 되돌린다. 시간외종가일 때 가격 입력은 잠금(`--muted` 배경 · 값 「—」 · `aria-label` 「가격(시간외종가 · 잠김)」) + 「참고 종가 {가격}」 + 안내 문장 + 주문금액 「종가 확정 후」 이고, 요청에는 `price: 0` 과 `krxSession` 을 싣는다. 이 상태에서 **정정 버튼은 비활성**이다.

**각주(호가 탭):** 기존 `order-panel.tsx` 의 「정정 미지원」을 알리는 각주 문장을 UI-SPEC §수동주문 표의 새 각주 원문으로 **교체**한다 — 정정이 열렸으므로 옛 문장은 거짓이 된다.

**제출 경로:** `useRelayContext().sendOrder(req)` 만 쓴다 — 프레임을 직접 만들지 않고 `rid` 를 직접 생성하지 않는다(생성처가 하나여야 relay 중복 가드가 정상 주문을 막지 않는다). 응답 3분기 문구는 기존 원문을 그대로 쓰고 인라인 `role="status"` + `aria-live="polite"` 로 띄운다.

테스트 `__tests__/manual-order-form.test.tsx` 는 `<behavior>` 10케이스를 RTL 로 덮는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- manual-order-form</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "10%|25%|50%|100%" webapp/src/components/trading/card/manual-order-form.tsx</automated>
    <fails_when>non-zero exit — 비율 버튼이 다이어트 대상인데 남아 있다(D-20)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "affordanceOf" webapp/src/components/trading/card/manual-order-form.tsx</automated>
    <fails_when>카운트가 0 — 폼이 77 매핑 함수를 부르지 않고 자체 판정을 하고 있다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "new Date|Date\.now" webapp/src/components/trading/card/manual-order-form.tsx</automated>
    <fails_when>non-zero exit — 폼이 벽시계로 창을 판정하고 있다(D-22 위반)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 4버튼이 한 줄에 있고 미선택 상태에서 정정·취소가 disabled 임이 단언된다
    - 확인 다이얼로그 없이 전송되는 경로가 없음이 단언된다(제출 클릭 → 다이얼로그 → 확정 → 1회 전송)
    - 중복 제출 가드(응답 전 재클릭 무효)가 단언된다
    - `timeout` 문구에 재시도 권유 표현이 없음이 단언된다
    - 조각 스테퍼 비표시 상태에서 요청에 조각 수가 실리지 않음이 단언된다
    - 시간외종가 선택 시 `price: 0` + `krxSession` 송신과 정정 버튼 비활성이 단언된다
    - 카드 variant 에 주문유형 콤보가 없음이 단언된다
    - 파일에 비율 버튼 · 벽시계 호출이 없다
  </acceptance_criteria>
  <done>한 폼이 신규·정정·취소·예약·시간외종가 다섯 경우를 전부 내고, 어느 경우에도 확인 다이얼로그를 건너뛰지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 미체결 행 선택 → 정정/취소 활성 · 잠금 3조건</name>
  <files>webapp/src/components/trading/card/manual-order-form.tsx, webapp/src/components/trading/__tests__/manual-order-form.test.tsx</files>
  <read_first>
    - packages/shared/src/relay.ts (`RelayUnfilled` 전 필드 — `orderNo`·`orgOrderNo`·`side`·`price`·`orderQty`·`unfilledQty`·`queuedStatus`·`pendingStatus`·`board`·`pendingCancelSent`·`name?`·`code?`)
    - webapp/src/components/orderbook/account-panel.tsx (미체결 표 · 기존 Q-ID 가드와 `pendingCancelSent` 회색 처리)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md §「정정을 잠가야 하는 두 경우」 표
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §수동주문 선택 칩 문구 · §공용 패널 행 `title`
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-orderbook-tab-mockup.html (정정/취소 선택 상태 마크업)
  </read_first>
  <behavior>
    - 미체결 행을 선택하면 칩 「원주문 {주문No}」 + 「{매수|매도} {가격} × {수량}」 이 뜨고 가격·수량이 채워진다.
    - 칩의 ✕(`aria-label` 「선택 해제」)로 해제하면 칩이 사라지고 **값은 유지**된다.
    - 선택 없음 → 「정정」「취소」 disabled.
    - `board` 가 G2/G3 인 행 선택 → 「정정」 disabled(+ 사유 `title`), 「취소」는 활성.
    - `queuedStatus` 가 비어 있지 않은 행(Q-ID) 선택 → 「정정」 disabled, 「취소」 활성.
    - `pendingCancelSent` 가 true 인 행 → 선택 자체가 disabled.
    - `orderNo` 가 빈 행(접수 전) → 선택 disabled + `title` 사유.
    - 「취소」 클릭 → 확인 다이얼로그 제목 「미체결 주문을 취소할까요?」 + 경고 「취소 수량은 미체결 잔량 전부예요.」 + 확정 버튼 「취소 주문」.
    - 「정정」 클릭 → `sendOrder({kind:"modify", orgOrderNo, side, qty, price})` 1회.
    - 정정 요청의 `side` 가 원주문의 `side` 를 그대로 승계한다.
  </behavior>
  <action>
`manual-order-form.tsx` 에 원주문 선택 상태를 받는 prop(`selectedUnfilled?: RelayUnfilled | null` + `onClearSelection`)을 더한다. 선택이 들어오면 가격·수량을 원주문 값으로 채우고 폼 위에 선택 칩을 띄운다. 해제는 칩만 없애고 입력값은 남긴다.

**정정 잠금 판정을 한 함수로 뽑는다** — `canModify(row: Pick<RelayUnfilled, 'board' | 'queuedStatus' | 'pendingCancelSent' | 'orderNo'>): boolean`. 세 잠금 근거를 docstring 에 적는다: 시간외종가 원주문은 서버가 「취소 후 재등록」으로 거부한다 · 예약 Q-ID 행도 같은 이유로 거부된다 · 취소 보관 행은 이미 취소가 나갔다. **relay 는 이 판정을 하지 않는다** — UI 가 먼저 막고, 서버가 최종 판정이다. 버튼의 `disabled` 와 제출 가드가 **같은 함수**를 부른다.

「취소」는 미체결 잔량 전부를 보낸다 — `qty` 는 `unfilledQty` 이고 사용자가 수량을 줄여 보내는 경로를 만들지 않는다(기존 규율). 확인 다이얼로그 문구는 UI-SPEC §Copywriting 의 Destructive confirmation 원문 그대로다.

「정정」은 폼의 (바꾼) 가격·수량과 원주문의 `orgOrderNo`·`side` 를 실어 `sendOrder({kind:"modify"})` 로 보낸다. `side` 를 사용자가 고르게 하지 않는다 — 정정은 원주문 방향을 승계한다.

테스트 `__tests__/manual-order-form.test.tsx` 에 `<behavior>` 10케이스를 더한다. 특히 잠금 3조건 각각과 「취소 수량 = 미체결 잔량 전부」를 단언한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- manual-order-form</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "canModify" webapp/src/components/trading/card/manual-order-form.tsx</automated>
    <fails_when>카운트가 2 미만 — 판정 함수가 정의만 되고 버튼 `disabled` 와 제출 가드 양쪽에서 쓰이지 않는다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 정정 잠금 3조건(G2/G3 `board` · 비어 있지 않은 `queuedStatus` · `pendingCancelSent`)이 각각 단언된다
    - `orderNo` 빈 행이 선택 불가임이 단언된다
    - 취소 요청의 수량이 `unfilledQty` 임이 단언된다
    - 정정 요청의 `side` 가 원주문 값을 승계함이 단언된다
    - 선택 해제 후에도 입력값이 남음이 단언된다
  </acceptance_criteria>
  <done>미체결 한 행을 눌러 정정·취소를 낼 수 있고, 서버가 어차피 거부할 세 경우는 버튼이 먼저 막는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `order-confirm-dialog.tsx` — 정정 · 예약 · 시간외종가 요약 행 확장</name>
  <files>webapp/src/components/orderbook/order-confirm-dialog.tsx, webapp/src/components/orderbook/__tests__/order-confirm-dialog.test.tsx</files>
  <read_first>
    - webapp/src/components/orderbook/order-confirm-dialog.tsx (전문 — `Dialog` 골격(`:117-136`), 기본 포커스가 취소 버튼인 이유 주석(`:92`), 중복 제출 가드, 기존 요약 행 구성)
    - webapp/src/lib/queued-window.ts (예약 안내 문구 상수 2종)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §주문확인 다이얼로그 전체 문단(제목·확정 버튼·요약 행·주문유형 값·예약 줄·경고 박스 원문)
    - webapp/src/styles/globals.css (`--new-bg`/`--new-bd` — 예약 안내 줄 배경)
  </read_first>
  <behavior>
    - 제목이 주문 성격에 따라 「매수/매도/예약매수/예약매도/정정 주문을 넣을까요?」 로 바뀌고, 취소는 「미체결 주문을 취소할까요?」.
    - 정정이면 요약에 「원주문」 행이 추가된다.
    - 예약구간이면 요약에 「조각 수 {N} (서버 상한 {max})」 행과 `--new-bg` 예약 안내 줄이 붙는다.
    - 시간외종가면 「주문유형」 값이 「시간외종가 · 가격 0 (KRX 세션)」 이고 가격 값이 「참고 종가 {가격}원」.
    - 취소면 「취소 수량」 행 + 경고 「취소 수량은 미체결 잔량 전부예요.」.
    - 초기 포커스가 취소 버튼이고 Escape·배경 클릭으로 닫힌다.
    - 확정 버튼을 두 번 빠르게 눌러도 `onConfirm` 이 1회만 불린다.
    - `DialogTitle` 과 `DialogDescription` 이 항상 존재한다(a11y).
  </behavior>
  <action>
`order-confirm-dialog.tsx` 의 `detail` 계약을 넓혀 정정·예약·시간외종가를 표현할 수 있게 한다. 기존 골격(`Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle`/`DialogDescription`)과 **기본 포커스가 취소 버튼인 규율**(「실행 버튼에 포커스가 가면 Enter 한 번에 주문이 나간다」 주석 포함), 중복 제출 가드는 한 글자도 완화하지 않는다.

요약 행(dt/dd) 순서와 문구는 UI-SPEC §주문확인 다이얼로그 원문을 그대로 쓴다 — 종목 · 계좌 · 거래소 · (정정/취소면) 원주문 · 주문유형 · 가격 · 수량 · (예약구간이면) 조각 수 · 주문금액 / (취소면) 취소 수량. 주문유형 값 두 가지(「지정가 · 보통」 / 「시간외종가 · 가격 0 (KRX 세션)」)와 예약 줄·경고 박스 문구도 원문이다. 예약 줄은 `--new-bg`/`--new-bd` 박스, 경고 박스는 `--destructive` 8% 틴트 + `--destructive` 테두리다(UI-SPEC §Color 의 Destructive 허용 목록 3번).

종목명은 이 화면에서 **wrap 허용**이다(ellipsis 없이 전체 노출 — 확인 화면이므로). 요약 dt/dd 는 세로 스택이고 폰에서 본문이 세로 스크롤된다.

기존 호출부(`order-panel.tsx` 등)가 새 계약으로도 컴파일되도록 추가 필드는 전부 optional 로 둔다 — 이 phase 에서 기존 화면을 깨지 않는다.

테스트 `__tests__/order-confirm-dialog.test.tsx` 에 `<behavior>` 8케이스를 더한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- order-confirm-dialog</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "DialogDescription" webapp/src/components/orderbook/order-confirm-dialog.tsx</automated>
    <fails_when>카운트가 0 — a11y 필수 요소가 사라졌다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test</automated>
    <fails_when>non-zero exit, 또는 통과 수가 Phase 17 기준선 1008 보다 작다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 다섯 제목 변형과 취소 제목이 단언된다
    - 정정 「원주문」 행 · 예약 「조각 수」 행 + 안내 줄 · 시간외종가 주문유형/가격 값이 단언된다
    - 초기 포커스가 취소 버튼임이 단언된다
    - 확정 이중 클릭이 1회 호출로 끝남이 단언된다
    - 기존 호출부가 수정 없이 컴파일된다(추가 필드 전부 optional)
    - webapp 테스트 전량 통과 수가 1008 이상
  </acceptance_criteria>
  <done>다섯 주문 성격이 전부 확인 화면에서 정확히 요약되고, 실행 버튼에 포커스가 가는 경로가 여전히 없다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 입력 → 주문 요청 | 가격·수량·조각 수·원주문번호가 여기서 만들어진다 |
| 브라우저 → relay wss (`order.new`/`order.modify`/`order.cancel`) | 실계좌 발주 경로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-30 | Tampering | 4버튼 인접 오클릭 | high | mitigate | D-20 이 단일 제출 버튼 규율을 뒤집으므로, 확인 다이얼로그(기본 포커스 취소)가 **유일한** 오클릭 방어다. 다이얼로그를 건너뛰는 경로 0건을 테스트가 단언 |
| T-18-31 | Tampering | 중복 제출 → 이중 체결 | critical | mitigate | 제출 후 응답까지 4버튼 disabled + 다이얼로그 확정 1회 가드. `timeout` 문구가 재시도를 유도하지 않는다 |
| T-18-32 | Tampering | 잠겨야 할 정정이 나감 | high | mitigate | `canModify` 한 함수를 버튼 `disabled` 와 제출 가드가 함께 부른다. 3조건(G2/G3·Q-ID·취소 보관) 각각 테스트 |
| T-18-33 | Spoofing | `OrderResp.message` 기반 분기 | medium | mitigate | 표시 전용. 문구 비교로 상태를 판정하지 않는다 |
| T-18-34 | Tampering | 화면에 없는 조각 수 송신 | medium | mitigate | 스테퍼가 보일 때만 요청에 싣는다. 테스트가 미표시 상태 페이로드를 단언 |
| T-18-35 | Information Disclosure | 확인 다이얼로그 요약 | low | accept | 계좌번호·금액은 사용자가 확인해야 할 값이라 이 화면에 필요하다. 로그·알림에는 싣지 않는다 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 phase 는 새 패키지를 설치하지 않는다(RESEARCH §Package Legitimacy Audit 표 비어 있음) |
</threat_model>

## Artifacts this phase produces

새로 만드는 심볼·파일 (phase 전체 — 드리프트 검증에서 제외되어야 한다):

- **마이그레이션:** `supabase/migrations/20260921120000_dma_orders_modify_offhours.sql` · 컬럼 `dma_orders.piece_count` · `dma_orders.krx_session`
- **shared:** `RelayOrderModifyMsg` · `RelayKrxSession` · `OrderType` 의 `"M"` 멤버 · `RelayOrderNewMsg.pieceCount?` · `RelayOrderNewMsg.krxSession?` · `RelayRateCrossItem.name?` · `RelayRateCrossItem.code?`
- **relay:** `RelayOrderModifySchema` · `OrderInsertRow.pieceCount?` · `OrderInsertRow.krxSession?` · 돌파 항목 보강 private 메서드 · `relay/src/ws/__tests__/protocol.test.ts`
- **webapp lib:** `RelayOrderRequest.kind` 의 `"modify"` · `queued-window.ts`(`OrderButtonMode`·`ManualOrderAffordance`·`affordanceOf`) · `breakout-list.ts`(`kstDateKey`·`shouldRemoveBreakout`·`breakoutRowsFrom`·`BreakoutRow`·키 상수 4종·`REMOVE_MARGIN_PCT`·`ARM_GRACE_MS`·`HIGHLIGHT_MS`) · `use-breakout-quotes.ts`(`useBreakoutQuotes`·`MAX_BREAKOUT_SUBS`) · `alert-tone.ts`(`playBreakoutTone`·`isTonePlaybackBlocked`·`resumeToneContext`) · `limit-chaser.ts` 로 이동한 `parseStrategyKey`
- **webapp 컴포넌트:** `components/trading/workbench/` 의 `TradingWorkbench`·`WorkbenchStatusBar`·`ViSettingsRows`·`ViTriggerStrip`·`BreakoutStrip`·`StockAddBar`·`CardGrid`·`SharedPanels` · `components/trading/card/` 의 `StrategyCard`·`CardHeader`·`QuoteGrid10`·`CardBody`·`ManualOrderForm`(+`canModify`)·`StockInfoModal`
- **라우트:** `webapp/src/app/trading/page.tsx`
- **CSS 토큰:** `--new-bg` · `--new-bd` (globals.css §9 라이트/다크 양쪽)
- **e2e:** `webapp/e2e/specs/trading-workbench.spec.ts`

<verification>
- `pnpm --filter @gh-radar/webapp test` 전량 exit 0 (통과 수 1008 이상)
- `pnpm --filter @gh-radar/webapp run typecheck` exit 0
- 폼 파일에 비율 버튼·벽시계 호출 0건
</verification>

<success_criteria>
- 신규·정정·취소·예약·시간외종가 다섯 경우가 한 폼에서 난다
- 확인 다이얼로그를 건너뛰는 제출 경로가 없다
- 서버가 거부할 정정 3경우를 UI 가 먼저 막는다
- 77 판정이 `affordanceOf` 한 곳에서만 나온다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-07-SUMMARY.md` when done
</output>