---
phase: 18-gh-trade-ui-nxt-vi
plan: 28
type: execute
wave: 14
depends_on: ["18-23"]
gap_closure: true
files_modified:
  - webapp/src/components/trading/workbench/vi-settings-rows.tsx
  - webapp/src/components/trading/__tests__/vi-settings-rows.test.tsx
autonomous: true
requirements: [TRADE-08]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GC-WR-04 사용자 결정(2026-09-22 · 잠금): 등록된 VI 줄이 **중지(`run:false`)** 이고 등록 계좌 B 가 상태줄 계좌 A 와 **다를 때만** 줄에 명시 동작 「상태줄 계좌({A})로 옮겨 시작」 이 보인다. 누르면 시작 확인 다이얼로그가 열리고 요약 「계좌」 줄이 「{B} → {A}」 를 말한다. 확정하면 `vi.set` 이 `accountNo: A` · `run: true` · 그 줄의 거래소로 한 번 나간다"
    - "가동 중(`run:true`)에는 지금과 똑같이 등록 계좌 B 가 고정이다(CR-02 동작 보존) — 옮기기 동작은 DOM 에 없고, 「수정」·중지는 B 로 나가며, 고지 문구가 「옮기려면 먼저 중지하세요」 를 말한다. 어떤 경로로도 가동 중에 `accountNo: A` 인 `vi.set` 이 나가지 않는다"
    - "`vi.set` 의 계좌가 A 가 되는 것은 **옮기기 확인을 확정했을 때뿐**이다 — 중지 줄의 일반 스위치 「시작」·「수정」 은 여전히 B 로 나간다(18-15 CR-02 회귀 무수정 green)"
    - "확인 다이얼로그를 연 뒤 상태가 바뀌면(다른 단말이 시작해 `run:true` · 등록 계좌가 바뀜 · 상태줄 계좌가 바뀜) 확정은 아무것도 보내지 않고 다이얼로그 안에 사유를 말한다 — 요약에 보인 「B → A」 와 다른 계좌 이동이 나갈 수 없다"
    - "이 UI 에는 VI 삭제 기능이 없다 — 옮기기는 같은 거래소 슬롯의 `vi.set` 한 번(계좌 교체 + 시작)이며, 삭제·재등록 경로에 기대지 않는다"
  artifacts:
    - webapp/src/components/trading/workbench/vi-settings-rows.tsx
  key_links:
    - "`viMoveTargetOf(server, 상태줄 계좌)` → 옮기기 버튼 표시 · 다이얼로그 요약 `B → A` · 확정 직전 재판정 → `submit(true, A)` → `vi.set{accountNo:A, run:true, exchange}` → 서버 61 에코(`accountNo:A`) → `viRowAccountOf` 가 A 를 정본으로 → 고지·버튼 사라짐"
  prohibitions:
    - "MUST NOT 가동 중 줄에서 상태줄 계좌로 보내는 경로를 만든다(버튼 숨김 + `submit` 안의 가드 두 겹)"
    - "MUST NOT 옮기기를 「수정」 이나 일반 스위치 시작에 합친다 — 계좌 이동은 명시 동작 하나뿐이다"
    - "MUST NOT VI 삭제/재등록 경로를 만들거나 기대한다"
    - "MUST NOT 확인 다이얼로그의 기본 포커스·`showCloseButton={false}`·중복 제출 가드 규율을 완화한다(Phase 16 D-07 · T-16-10)"
---

<objective>
GC-WR-04 — CR-02 수정(18-15)이 막아 버린 「등록된 VI 를 다른 계좌로 옮기는 길」 을 **사용자 결정 그대로** 다시 연다: 중지 상태이고 상태줄 계좌가 다를 때만, 명시 동작 「상태줄 계좌({A})로 옮겨 시작」 과 「계좌 B → A」 확인 요약으로.

Purpose: 18-15 이후 서버 객체가 있으면(중지 상태 포함) 모든 `vi.set` 이 등록 계좌로 나가므로, 계좌 B 에 한 번 등록된 VI 는 이 화면에서 영원히 B 다. 실돈 오계좌를 막는 방향은 맞지만(가동 중 조용한 이전 금지 — CR-02), 사용자가 의도적으로 옮길 출구가 없다(18-REVIEW-R2 GC-WR-04). 사용자는 2026-09-22 「중지 상태에서만 · 명시 동작 · B → A 요약 · 가동 중은 지금처럼 고정」 을 골랐다.
Output: `viMoveTargetOf` 판정 · 옮기기 동작과 확인 경로 · 상태별 고지 문구 · 회귀.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-REVIEW-R2.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-15-SUMMARY.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md

근거 인용(재확인 완료):
- `webapp/src/components/trading/workbench/vi-settings-rows.tsx:118-138` `viRegisteredAccountText` · `viRowAccountOf` · `:309-317` 줄 계좌 정본 · `:401` `locked` · `:419-459` `submit(nextRun)` · `:462` `confirmKind` · `:521-538` 「수정」 버튼(줄 끝 · `data-slot="vi-row-fix"`) · `:551-555` 불일치 고지(`data-slot="vi-row-account"`) · `:562-589` `ViConfirmDialog` 호출 · `:701-827` `ViConfirmDialog`(요약 「계좌」 줄 759-762).
- `webapp/src/components/trading/__tests__/vi-settings-rows.test.tsx:527-620` describe 「CR-02 — 등록 전략의 계좌가 정본」 — 601 「서버 계좌 B(중지) · 상태줄 A → 시작 요약 「계좌」 가 B · 확정은 B · run:true」 는 **일반 스위치 시작**의 계약으로 그대로 산다.
- D-05: 계좌는 줄에 표시하지 않고 상태줄이 고른다 — 새 동작은 불일치일 때만 나타나는 예외 표면이다(18-15 고지와 같은 자리).
- e2e 픽스처는 계좌가 1개라(`E2E_ACCOUNT_NO`) 불일치 상태를 만들 수 없다 — 이 플랜의 회귀는 RTL 컴포넌트 테스트다. 2계좌 실기 확인은 UAT(18-VERIFICATION-R2 #7 확장)로 18-32 가 인계한다.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [tracer] 중지 · 계좌 다름 → 「상태줄 계좌({A})로 옮겨 시작」 → 요약 B → A → `vi.set{accountNo:A, run:true}`</name>
  <files>webapp/src/components/trading/workbench/vi-settings-rows.tsx, webapp/src/components/trading/__tests__/vi-settings-rows.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/workbench/vi-settings-rows.tsx (전문 837줄 — 특히 ⑨ 머리 주석 50-57 · 118-138 · 298-459 · 462-592 · 701-827)
    - webapp/src/components/trading/__tests__/vi-settings-rows.test.tsx (1-83 모킹 하네스 `sendMock`·`accountsMock`·`renderRows` · 337-370 확인 다이얼로그 · 527-620 CR-02 describe)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md (§Copywriting · §Color — VI 줄 문구·버튼 톤 · 「시작」 은 `--up` 채움, 테두리형 보조 버튼 관례)
  </read_first>
  <behavior>
    - `viMoveTargetOf`: 서버 KRX `{accountNo:B, run:false}` · 상태줄 A → `{from:B, to:A}` · `run:true` → null · 상태줄 = B → null · 미등록(null)/미조회(undefined) → null · 상태줄 '' → null · 서버 계좌 '' → null
    - 중지 B · 상태줄 A: 줄 아래 `data-slot="vi-row-move"` 버튼 텍스트 「상태줄 계좌(A)로 옮겨 시작」 → 클릭 → `vi-start-dialog` 가 열리고 기본 포커스는 「취소」, 요약 「계좌」 줄이 「B → A」(이름이 있으면 각 계좌 옆에) → 「시작」 → `sendMock` 1회 `{t:'vi.set', accountNo:A, exchange:'KRX', run:true, 금액·상승률 = 폼 값}`
    - 같은 조건에서 일반 스위치 → 시작 → 확정 → `accountNo:B`(18-15 601 케이스 그대로)
    - 가동 B · 상태줄 A: 옮기기 버튼 DOM 없음 · 「수정」 → `accountNo:B`
    - 중지 B · 상태줄 B: 옮기기 버튼 DOM 없음 · 고지 없음
  </behavior>
  <action>
**판정 (GC-WR-04 · D-05).** `viMoveTargetOf(server, statusAccountNo): { from: string; to: string } | null` 을 `viRowAccountOf` 옆에 export 한다 — 등록(객체 ∧ `accountNo` 공란 아님) ∧ `server.run === false` ∧ `statusAccountNo !== ''` ∧ 등록 계좌 ≠ 상태줄 계좌일 때만 `{from: 등록 계좌, to: 상태줄 계좌}`. JSDoc 에 사용자 결정(2026-09-22)과 두 규칙(가동 중에는 절대 옮기지 않는다 · 옮기기는 명시 동작 하나)을 적는다. 머리 주석 ⑨ 에 「중지 상태에서만 명시 동작으로 옮긴다(GC-WR-04)」 한 문단을 더한다.

**동작과 확인 경로.** `ViSettingsRow` 에 옮기기 확인 스냅샷 상태(`{from, to} | null`)를 두고:
- 버튼: `viMoveTargetOf` 가 null 이 아닐 때만 불일치 고지 줄(`vi-row-account`)과 **같은 블록**에 `data-slot="vi-row-move"` 테두리형 작은 버튼(높이 26px · 11px · 기존 「수정」 버튼과 같은 크기 축 · 채움 없음 — 채움 `--up` 은 확인 다이얼로그의 「시작」 하나다)으로 「상태줄 계좌({A})로 옮겨 시작」 을 그린다. `disabled` = `locked || submitting || blank || 금액 상한 초과`(일반 시작과 같은 조건). 좁은 폭에서 고지 문구와 버튼이 줄바꿈으로 잘림 없이 떨어지게 `flex-wrap` · `min-w-0` 규율을 따른다(lessons.md 「그리드/플렉스 자식 min-w-0」).
- 클릭: 그 순간의 `{from, to}` 를 스냅샷으로 잡고 `confirmKind = 'start'` 로 다이얼로그를 연다.
- `ViConfirmDialog`: 선택 prop `moveFrom?: { accountNo: string; name?: string }` 을 더한다 — 있으면 요약 「계좌」 줄이 「{from}(· 이름) → {to}(· 이름)」 이고 `DialogContent` 에 `data-move="true"`. 제목·경고·버튼·기본 포커스는 일반 시작과 같다. 옛 호출부는 prop 없이 그대로다.
- `submit(nextRun, accountOverride?)`: `accountOverride` 가 주어지면 송신 계좌로 쓰되, **그 순간 `server?.run === true` 면 `'blocked'`** 를 돌려준다(가동 중 이전 금지의 두 번째 겹). 기존 호출(`submit(run)`·일반 시작/중지)은 인자 없이 그대로 정본 계좌를 쓴다.
- 확정: 옮기기 스냅샷이 있으면 **지금 렌더의** `viMoveTargetOf(server, accountNo)` 를 다시 계산해 스냅샷과 `from`·`to` 가 모두 같을 때만 `submit(true, to)` 를 부르고, 다르면 보내지 않고 다이얼로그에 `VI_MOVE_STALE_TEXT = '계좌 상태가 바뀌었어요 — 다시 확인해 주세요'` 를 띄운다(`error` prop 경로 · GC-WR-06 규율대로 닫지 않는다). 다이얼로그가 닫히면 스냅샷을 비운다.
테스트는 `<behavior>` 전부를 CR-02 describe 옆 새 describe 「GC-WR-04 — 중지 상태에서만 상태줄 계좌로 옮겨 시작」 에 쓴다. 수정 전 코드에는 버튼이 없어 RED 다(SUMMARY 에 한 줄).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- vi-settings-rows</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `viMoveTargetOf` 7케이스 · 옮기기 왕복(버튼 → 요약 B → A → `accountNo:A · run:true`) · 일반 시작 B · 가동 중 버튼 없음 · 같은 계좌 버튼 없음 green
    - 18-15 CR-02 describe(527-620) 전 케이스가 무수정 green — `git diff "$PLAN_HEAD_BEFORE"..HEAD -- webapp/src/components/trading/__tests__/vi-settings-rows.test.tsx` 에서 그 describe 안의 삭제 줄 0(고지 문구 단언이 Task 2 에서 바뀌면 그 줄만 예외로 SUMMARY 에 적는다)
    - `grep -n "vi-row-move" webapp/src/components/trading/workbench/vi-settings-rows.tsx` 가 1줄 이상
  </acceptance_criteria>
  <done>중지된 VI 를 사용자가 명시적으로 상태줄 계좌로 옮겨 시작할 수 있고, 확인 요약이 옮겨 갈 두 계좌를 말한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 가동 중 이전 불가 · 확인 뒤 상태 변화 차단 · 상태별 고지 문구</name>
  <files>webapp/src/components/trading/workbench/vi-settings-rows.tsx, webapp/src/components/trading/__tests__/vi-settings-rows.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/workbench/vi-settings-rows.tsx (Task 1 결과 — `viRegisteredAccountText` · 옮기기 스냅샷 · `submit` 가드 · 확정 재판정)
    - webapp/src/components/trading/__tests__/vi-settings-rows.test.tsx (Task 1 describe · 572-590 고지 문구 케이스 · 320-336 에코 rerender 패턴)
  </read_first>
  <behavior>
    - 중지 B · 상태줄 A · 옮기기 다이얼로그 열림 → 다른 단말이 시작해 서버 에코 `run:true` 로 rerender → 「시작」 → `sendMock` 0회 · 다이얼로그 안 `vi-confirm-error` 「계좌 상태가 바뀌었어요 — 다시 확인해 주세요」 · 다이얼로그 열린 채
    - 같은 상황에서 상태줄 계좌가 C 로 바뀜 → 확정 → 0회 · 같은 문구 (요약에 보인 A 가 아닌 C 로 나가지 않는다)
    - 같은 상황에서 등록 계좌가 에코로 D 로 바뀜 → 확정 → 0회 · 같은 문구
    - 가동 B · 상태줄 A 고지: 「계좌 B … 에 등록된 VI 예요 — 가동 중에는 이 계좌로만 나가요 · 옮기려면 먼저 중지하세요」
    - 중지 B · 상태줄 A 고지: 「계좌 B … 에 등록된 VI 예요 — 수정·시작은 이 계좌로 나가요」 + 옮기기 버튼
    - 가동 중 어떤 조작(「수정」 · 중지 스위치)도 `accountNo:A` 를 내보내지 않는다 — 한 테스트에서 두 조작 뒤 `sendMock` 호출 전체의 `accountNo` 가 B 뿐
  </behavior>
  <action>
**고지 문구 (GC-WR-04 결정의 문구 갱신).** `viRegisteredAccountText(accountNo, name, run)` 로 세 번째 인자를 받아 두 갈래를 돌려준다 — 가동 중: 「계좌 {label} 에 등록된 VI 예요 — 가동 중에는 이 계좌로만 나가요 · 옮기려면 먼저 중지하세요」 / 중지: 「계좌 {label} 에 등록된 VI 예요 — 수정·시작은 이 계좌로 나가요」(옮기는 길은 바로 옆 버튼이 말한다 — 위치를 가리키는 말을 문구에 넣지 않는다, 줄바꿈 위치가 폭마다 다르다). 호출부는 `run` 을 넘긴다. 18-15 의 옛 문구(「수정·시작·중지는 이 계좌로 나가요」)를 단언하던 케이스는 새 두 문구로 고치고, 고친 사실을 SUMMARY 에 적는다(동작이 아니라 문구 계약이 사용자 결정으로 바뀐 것).

**확인 뒤 상태 변화 (T-18-118).** Task 1 의 확정 재판정이 `<behavior>` 세 경우(가동 전환 · 상태줄 변경 · 등록 계좌 변경)를 모두 막는지 테스트로 고정한다. 막히지 않는 경우가 있으면 재판정 조건을 스냅샷 `from`·`to` 와 지금 렌더의 `viMoveTargetOf` 전체 일치로 좁힌다. `submit` 의 가동 중 가드는 테스트에서 `accountOverride` 를 직접 줄 수 없으므로 「가동 전환 뒤 확정」 케이스가 그 가드까지 함께 지난다는 것을 SUMMARY 에 적는다.

**가동 중 계좌 불변 (CR-02 보존).** `<behavior>` 마지막 케이스로 가동 중 「수정」·중지가 B 로만 나감을 한 번에 단언한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- vi-settings-rows trading-workbench</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test:e2e -- trading-workbench -g "VI"</automated>
    <fails_when>non-zero exit, 또는 출력에 "No tests found", 또는 "failed" 가 1 이상 — 1계좌 e2e 의 VI 줄(시작/중지/수정 · 거래소 송신)이 새 코드에서 깨졌다</fails_when>
  </verify>
  <acceptance_criteria>
    - 확인 뒤 상태 변화 3케이스가 전송 0 + `vi-confirm-error` 문구 · 다이얼로그 유지로 green
    - 가동 중 두 조작의 `accountNo` 가 전부 B 인 케이스 green
    - `grep -n "옮기려면 먼저 중지하세요" webapp/src/components/trading/workbench/vi-settings-rows.tsx` 가 1줄 이상
    - `VI_MOVE_STALE_TEXT` 가 export 되고 확정 경로가 그것을 쓴다(`grep -n "VI_MOVE_STALE_TEXT"` 선언·사용 2줄 이상)
    - e2e `trading-workbench -g "VI"` green · webapp 전량은 18-32 가 본다
  </acceptance_criteria>
  <done>가동 중에는 어떤 경로로도 VI 계좌가 바뀌지 않고, 중지 상태의 명시적 이동은 사용자가 확인한 두 계좌 그대로만 일어난다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → relay `vi.set` | 계좌·거래소·금액·run 이 무인 매수 전략의 실돈 조건이 된다 |
| 다른 단말(WinForms·다른 탭) → 서버 61 에코 | 확인 다이얼로그가 열린 사이 전략 상태가 바뀔 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-117 | Elevation of Privilege | 가동 중 VI 계좌 조용한 이전 | high | mitigate | 옮기기 버튼은 `run:false` 에만(DOM 부재) · `submit` 가 `accountOverride` + `run:true` 면 `'blocked'` · 가동 중 두 조작 accountNo=B 회귀 |
| T-18-118 | Tampering | 확인 뒤 상태 변화로 다른 계좌 이동 | high | mitigate | 확정 직전 `viMoveTargetOf` 재판정 · 스냅샷 `from`/`to` 전체 일치 요구 · 3케이스 전송 0 |
| T-18-119 | Repudiation | 요약 계좌 ≠ 송신 계좌 | medium | mitigate | 요약 「B → A」 와 송신 `accountNo` 가 같은 스냅샷에서 나온다 · 왕복 테스트가 둘을 함께 단언 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 패키지 설치 없음 |
</threat_model>

## Artifacts this phase produces

- **webapp 컴포넌트:** `viMoveTargetOf`(export) · `VI_MOVE_STALE_TEXT`(export) · 「상태줄 계좌({A})로 옮겨 시작」 동작(`data-slot="vi-row-move"`) · `ViConfirmDialog` 선택 prop `moveFrom`(요약 「B → A」 · `data-move`) · `submit` 의 계좌 지정 + 가동 중 가드 · 상태별 `viRegisteredAccountText`
- **테스트:** `vi-settings-rows.test.tsx` describe 「GC-WR-04 — 중지 상태에서만 상태줄 계좌로 옮겨 시작」(판정 7 · 왕복 · 가동 중 부재 · 상태 변화 3 · 가동 중 계좌 불변)

<verification>
- 중지 ∧ 계좌 다름에서만 옮기기 동작 · 확정 = `vi.set{accountNo:A, run:true}` 1회
- 가동 중 A 송신 0(DOM + 가드) · 일반 시작/수정은 B · 확인 뒤 상태 변화 전송 0
- vi-settings-rows · trading-workbench 단위 · e2e VI green
</verification>

<success_criteria>
- 사용자 결정(GC-WR-04)이 문구·조건·요약까지 그대로 구현됐다
- CR-02 의 「가동 중 조용한 계좌 이전 없음」 이 한 글자도 약해지지 않았다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-28-SUMMARY.md` when done
</output>
