---
phase: 18-gh-trade-ui-nxt-vi
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/src/styles/globals.css
  - webapp/src/lib/limit-chaser.ts
  - webapp/src/lib/__tests__/limit-chaser.test.ts
  - webapp/src/components/trading/limit-chaser-client.tsx
autonomous: true
requirements: [TRADE-09]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-26: 목업이 도입한 토큰 `--new-bg`·`--new-bd` 가 globals.css §9 의 라이트(`:root`)와 다크(`.dark`) **양쪽**에 정의되고, 한쪽만 정의된 상태가 없다 (TRADE-09)"
    - "D-28: globals.css §2.2b 에 「이 표를 재는 컨테이너는 둘이다 — `lc`(카드 본문 = 호가 탭 본문) · `wb`(작업대 페이지 본문)」 문단이 3~4줄 추가되고, **밴드 수치 표는 다시 적히지 않는다**(표가 둘이 되면 갈라진다 — CLAUDE.md Conventions) (TRADE-09)"
    - "D-09: `parseStrategyKey` 가 `webapp/src/lib/limit-chaser.ts` 로 이동해 `strategyKey` 와 같은 파일에서 짝을 이루고, 기존 호출부는 import 경로만 바뀐 채 동작이 같다 (TRADE-09)"
    - "D-26: 목업이 쓰는 `--ctl-h` 는 목업 컨트롤 바 전용이라 프로덕션 토큰으로 승격되지 않는다 (TRADE-09)"
  artifacts:
    - webapp/src/styles/globals.css
    - webapp/src/lib/limit-chaser.ts
  key_links:
    - "globals.css §2.2b ↔ `@min-[700px]/lc:`·`@min-[830px]/lc:`·`@min-[992px]/lc:` 유틸리티 43개 — 이 phase 는 표가 아니라 **컨테이너 선언 위치**만 옮긴다"
    - "`--new-bg`/`--new-bd` ↔ 신규 돌파 30초 강조 · 미확인 VI 행 · 구간 배지 · 확인 다이얼로그 예약 줄 — 네 표면이 같은 두 토큰을 쓴다"
  prohibitions:
    - "MUST NOT §2.2b 밴드 수치 표를 globals.css 밖(UI-SPEC·PLAN·컴포넌트 주석)에 복사한다 — 표가 둘이 되는 순간 갈라진다"
    - "MUST NOT `--new-bg`/`--new-bd` 를 한쪽 테마에만 정의한다 — 반대 테마에서 「신규/미확인」 색 축이 조용히 사라진다"
---

<objective>
UI 조립이 올라탈 **디자인 시스템 바닥 두 줄**을 먼저 깐다 — 목업이 도입한 토큰 2건을 globals.css §9 로 승격하고, §2.2b 4밴드 표에 「이 표를 재는 컨테이너가 이제 둘이다」 문단을 더한다. 겸해서 전략 키 파싱 헬퍼를 조립 헬퍼 옆으로 옮겨 카드 컴포넌트가 페이지 셸을 import 하지 않게 한다.

Purpose: 뒤따르는 모든 UI 플랜(스트립·카드·격자·호가 탭)이 이 두 토큰과 이 컨테이너 규율을 전제로 쓴다. 나중에 하면 각 플랜이 제 나름의 색·제 나름의 브레이크포인트를 만든다.
Output: globals.css 토큰 2건 + §2.2b 문단 · `lib/limit-chaser.ts` 로 이동한 `parseStrategyKey`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md
</context>

<tasks>

<!-- planner-discipline-allow: --ctl-h -->

<task type="auto">
  <name>Task 1: 토큰 `--new-bg`·`--new-bd` 승격 + §2.2b 컨테이너 문단</name>
  <files>webapp/src/styles/globals.css</files>
  <read_first>
    - webapp/src/styles/globals.css (§9 `:root`/`.dark` 토큰 블록 · §2.2b 4밴드 주석 전문 · `--led-*` 주석의 「양쪽 테마 정의」 선례)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §Color 의 「토큰 추가 2건」 표 · §반응형 계약
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html (상단 `:91-93` 토큰 선언 원문 — 「이 목업 전용 신규 제안 토큰」)
  </read_first>
  <action>
globals.css §9 의 `:root` 블록에 `--new-bg: oklch(0.97 0.08 95);` 와 `--new-bd: oklch(0.86 0.14 92);` 를, `.dark` 블록에 `--new-bg: oklch(0.28 0.06 95);` 와 `--new-bd: oklch(0.45 0.10 92);` 를 더한다. 두 토큰 위에 한 줄 주석으로 **쓰이는 자리 네 곳**을 적는다 — 신규 돌파 30초 강조 행/칩(D-18) · 미확인 VI 행/칩(D-06) · 「신규 N」/「미확인 N」 필 · 예약구간·장전 구간 배지와 확인 다이얼로그 예약 안내 줄. 그리고 `--led-*` 주석과 같은 어조로 「라이트·다크 둘 다 정의한다 — 한쪽만 두면 반대 테마에서 이 축이 조용히 사라진다」를 남긴다. 목업의 `--ctl-h` 는 옮기지 않는다.

§2.2b 본문 **맨 아래**에 3~4줄 문단 하나를 덧붙인다: 이 표를 재는 컨테이너가 이제 둘이라는 사실(`lc` = 상따 카드 본문이자 종목상세 호가 탭 본문, 같은 컴포넌트 / `wb` = `/trading` 작업대 페이지 본문 — 격자 열 수·상태줄 세그먼트·스트립 표 열 접기·공용 패널 sticky 전환), 그리고 「밴드 수치는 위 표 그대로이고 측정 대상만 다르다」. **경계 숫자를 다시 적지 않는다.** 같은 문단에 카드 헤더 한 줄 접힘용 로컬 경계가 §2.2b 의 네 번째 경계가 아니라는 사실(카드 헤더 컴포넌트 로컬 규칙)을 한 줄로 못 박는다. 기존 ★ 포털 규율 문단은 그대로 두되 「카드가 여럿이라 바 문구에 종목명을 쓴다」를 한 줄 덧붙인다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && grep -c -- "--new-bg" webapp/src/styles/globals.css</automated>
    <fails_when>카운트가 2 미만(라이트·다크 중 한쪽이 빠졌다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c -- "--new-bd" webapp/src/styles/globals.css</automated>
    <fails_when>카운트가 2 미만</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -q -- "--ctl-h" webapp/src/styles/globals.css</automated>
    <fails_when>non-zero exit — 목업 전용 컨트롤 바 변수가 프로덕션 스타일시트로 새어 들어왔다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `webapp/src/styles/globals.css` 가 `--new-bg` 와 `--new-bd` 를 각각 정확히 2회(라이트 1 · 다크 1) 정의한다
    - §2.2b 문단이 문자열 `wb` 와 `lc` 를 모두 포함하고, 새 경계 숫자(760 등)를 밴드 표에 추가하지 않는다
    - `--ctl-h` 가 globals.css 에 없다
  </acceptance_criteria>
  <done>모든 후속 UI 플랜이 신규/미확인 강조 색과 컨테이너 규율을 한 곳에서 가져다 쓸 수 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `parseStrategyKey` 를 `lib/limit-chaser.ts` 로 이동</name>
  <files>webapp/src/lib/limit-chaser.ts, webapp/src/lib/__tests__/limit-chaser.test.ts, webapp/src/components/trading/limit-chaser-client.tsx</files>
  <read_first>
    - webapp/src/lib/limit-chaser.ts (`strategyKey`·`dirtyFieldsOf`·`DIRTY_COMPARED_FIELDS` — 이동 대상이 합류할 자리)
    - webapp/src/components/trading/limit-chaser-client.tsx (`parseStrategyKey` 현행 정의와 그 docstring)
    - webapp/src/lib/__tests__/limit-chaser.test.ts (기존 키 조립 테스트 문법)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md §`limit-chaser-client.tsx` 슬라이스 경계 표
  </read_first>
  <behavior>
    - `parseStrategyKey("KR7005930003:1234567801:KRX")` → `{ isin, accountNo, exchange }`.
    - 형식이 어긋난 문자열(구분자 부족·거래소 미지 값)은 기존 정의와 **같은** 실패 형태를 돌려준다 — 이동으로 동작이 바뀌지 않는다.
    - `strategyKey(parseStrategyKey(k))` 가 원래 키와 같다(왕복).
  </behavior>
  <action>
`parseStrategyKey` 를 `webapp/src/components/trading/limit-chaser-client.tsx` 에서 잘라 `webapp/src/lib/limit-chaser.ts` 의 `strategyKey` 바로 아래로 옮긴다. docstring 과 실패 처리 분기를 **한 글자도 바꾸지 않는다** — 이 태스크는 순수 이동이다. `limit-chaser-client.tsx` 는 이동한 심볼을 `@/lib/limit-chaser` 에서 import 하도록 고치고, 그 파일이 이 심볼을 재-export 하고 있었다면 재-export 도 제거해 정의가 한 곳만 남게 한다. 다른 호출부가 있으면 같은 import 로 바꾼다(`grep -rn "parseStrategyKey" webapp/src` 로 전수 확인).

`webapp/src/lib/__tests__/limit-chaser.test.ts` 에 왕복 케이스(조립 → 파싱 → 조립)와 형식 어긋남 케이스를 더한다. 12자 절단 규율은 `strategyKey` 쪽 기존 테스트가 이미 덮으므로 다시 쓰지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- limit-chaser</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "export function parseStrategyKey" webapp/src/lib/limit-chaser.ts</automated>
    <fails_when>카운트가 1 이 아니다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `webapp/src/lib/limit-chaser.ts` 에 `export function parseStrategyKey(` 가 정확히 1회 있다
    - `webapp/src/components/trading/limit-chaser-client.tsx` 가 이 심볼을 정의하지 않고 `@/lib/limit-chaser` 에서 가져온다
    - `pnpm --filter @gh-radar/webapp run typecheck` exit 0
    - `pnpm --filter @gh-radar/webapp test -- limit-chaser` exit 0
  </acceptance_criteria>
  <done>카드 컴포넌트가 페이지 셸 파일을 import 하지 않고도 전략 키를 파싱할 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 (이 플랜은 스타일 토큰과 순수 함수 이동만 건드린다) | 새 입력 경로·새 신뢰 경계가 생기지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-08 | Tampering | `parseStrategyKey` 이동 | medium | mitigate | 순수 이동 — 실패 분기를 바꾸지 않는다. 왕복 테스트가 전략 키 불일치(= 엉뚱한 전략 조작)를 막는다 |
| T-18-09 | Information Disclosure | globals.css 토큰 | low | accept | 색 토큰에는 비밀이 없다. 공개 번들에 들어가는 것이 정상이다 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 phase 는 새 패키지를 설치하지 않는다(RESEARCH §Package Legitimacy Audit 표 비어 있음). 설치 태스크 없음 → legitimacy 체크포인트 없음 |
</threat_model>

## Artifacts this phase produces

새로 만드는 심볼·파일 (phase 전체 — 드리프트 검증에서 제외되어야 한다):

- **마이그레이션:** `supabase/migrations/20260921120000_dma_orders_modify_offhours.sql` · 컬럼 `dma_orders.piece_count` · `dma_orders.krx_session`
- **shared:** `RelayOrderModifyMsg` · `RelayKrxSession` · `OrderType` 의 `"M"` 멤버 · `RelayOrderNewMsg.pieceCount?` · `RelayOrderNewMsg.krxSession?` · `RelayRateCrossItem.name?` · `RelayRateCrossItem.code?`
- **relay:** `RelayOrderModifySchema` · `OrderInsertRow.pieceCount?` · `OrderInsertRow.krxSession?` · 돌파 항목 보강 private 메서드 · `relay/src/ws/__tests__/protocol.test.ts`
- **webapp lib:** `RelayOrderRequest.kind` 의 `"modify"` · `queued-window.ts`(`ManualOrderAffordance`·`OrderButtonMode`·`affordanceOf`) · `breakout-list.ts`(`kstDateKey`·`shouldRemoveBreakout`·`breakoutRowsFrom`·`BREAKOUT_SOUNDED_KEY`·`BREAKOUT_DISMISSED_KEY`·`BREAKOUT_TONE_KEY`·`TRADING_COLS_KEY`·`REMOVE_MARGIN_PCT`·`ARM_GRACE_MS`·`HIGHLIGHT_MS`) · `use-breakout-quotes.ts`(`useBreakoutQuotes`·`MAX_BREAKOUT_SUBS`) · `alert-tone.ts`(`playBreakoutTone`·`isTonePlaybackBlocked`·`resumeToneContext`) · `limit-chaser.ts` 로 이동한 `parseStrategyKey`
- **webapp 컴포넌트:** `components/trading/workbench/` 의 `TradingWorkbench`·`WorkbenchStatusBar`·`ViSettingsRows`·`ViTriggerStrip`·`BreakoutStrip`·`StockAddBar`·`CardGrid`·`SharedPanels` · `components/trading/card/` 의 `StrategyCard`·`CardHeader`·`QuoteGrid10`·`CardBody`·`ManualOrderForm`·`StockInfoModal`
- **라우트:** `webapp/src/app/trading/page.tsx`
- **CSS 토큰:** `--new-bg` · `--new-bd` (globals.css §9 라이트/다크 양쪽)
- **e2e:** `webapp/e2e/specs/trading-workbench.spec.ts`

<verification>
- `pnpm --filter @gh-radar/webapp run typecheck` exit 0
- `pnpm --filter @gh-radar/webapp test -- limit-chaser` exit 0
- globals.css 의 §2.2b 밴드 수치 표는 이 플랜 전후로 **동일하다**(문단만 늘었다)
</verification>

<success_criteria>
- 신규/미확인 강조 색 축이 라이트·다크 양쪽에 존재한다
- §2.2b 가 「측정 대상이 둘」임을 말하되 숫자를 다시 적지 않는다
- 전략 키 조립·파싱이 한 파일에서 짝을 이룬다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-02-SUMMARY.md` when done
</output>
