---
phase: 18-gh-trade-ui-nxt-vi
plan: 14
type: execute
wave: 8
depends_on: ["18-13"]
gap_closure: true
files_modified:
  - webapp/src/lib/relay-provider.tsx
  - relay/src/ws/protocol.ts
  - relay/src/dma/envelope.ts
  - supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql
  - relay/tests/ws-order.test.ts
  - webapp/src/lib/__tests__/relay-provider.test.tsx
  - relay/src/ws/__tests__/protocol.test.ts
  - relay/src/dma/__tests__/envelope.test.ts
  - packages/shared/src/relay.ts
autonomous: true
requirements: [TRADE-07]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "시간외종가(G2/G3) 원주문의 취소가 웹의 4겹(webapp 번역기 · relay zod · 조립기 · DB CHECK) 을 전부 통과한다 — D-21 설계가 정정을 잠그고 유일한 조치로 「취소 후 재등록」을 안내하기 때문에, 이 취소 경로는 TRADE-07 의 필수 기능이다 (CR-01 · 이 플랜은 코드 3겹 + 마이그레이션 파일, 실 DB 반영은 18-18)"
    - "D-21 · 근본 원인: 취소 프레임의 `price` 는 원주문 가격의 **사본**이지 주문 조건이 아니다. 그래서 취소는 세 코드 층(webapp `buildOrderFrame` · relay `RelayOrderCancelSchema` · 조립기 `buildDirectOrderReq`)이 **같은 규칙 하나** — 「0 이상의 정수」 — 로만 가격을 본다. 신규 전용 조건식(`offHours`)을 취소에 재사용하지 않는다"
    - "D-23: 신규 주문의 가격 0 규칙은 그대로다 — `krxSession` G2/G3 일 때만 0 이 통과하고, 세션 없는 가격 0 신규·가격 0 정정은 네 층 어디서도 통과하지 못한다"
    - "음수·비정수 가격은 취소를 포함한 모든 종류에서 거부된다 — 취소 가격 −1 은 relay 가 스키마 위반으로 끊고 게이트웨이에 0바이트가 간다"
    - "취소 가격 0 요청은 게이트웨이로 `order_type \"C\"` · 원주문번호 · `price 0` 으로 나가고, 감사 행 insert 도 `price 0` · `orderType \"C\"` 로 기록된 뒤 취소확인(C) 통보로 `cancelled` 정산된다"
  artifacts:
    - webapp/src/lib/relay-provider.tsx
    - relay/src/ws/protocol.ts
    - relay/src/dma/envelope.ts
    - supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql
  key_links:
    - "`manual-order-form.tsx` 취소(`price: selected.price`) · `account-panel.tsx` 취소(`price: row.price`) → `buildOrderFrame` → `RelayOrderCancelSchema` → `order-handler` insert → `buildDirectOrderReq` — 호출부는 원주문 가격을 그대로 싣고, 네 층이 같은 규칙으로 받는다"
    - "마이그레이션 파일 ↔ 18-18 원격 반영 — 이 플랜은 파일만 만든다. 실 DB 에 없으면 relay 는 게이트웨이 송신 **전에** insert 실패로 「주문 기록에 실패했습니다」를 돌려준다(소켓은 끊기지 않는다 — 지금의 close(4400)보다 낫다)"
  prohibitions:
    - "MUST NOT 신규 주문의 가격 0 조건을 넓힌다 — 세션 없는 가격 0 지정가가 게이트웨이까지 가서 거부 왕복 5초를 태운다(D-23 · 조립기 주석의 논거)"
    - "MUST NOT 취소 가격을 0 으로 **바꿔 보낸다** — 호출부는 원주문 가격을 그대로 싣는다. 0 은 시간외종가 원주문의 실제 값일 때만 흐른다"
    - "MUST NOT 이 플랜에서 원격 DB 에 마이그레이션을 적용한다 — 적용은 18-18 의 한쪽 문(one-way) 체크포인트 뒤다"
    - "MUST NOT `RelayOrderCancelSchema` 를 `.strict()` 로 만든다 — 기존 smoke 프로브·구 클라이언트가 zod 기본 strip 에 기대고 있다(T-18-07)"
---

<objective>
CR-01 을 닫는 **수직 슬라이스(tracer)**. 시간외종가(G2/G3) 원주문 — `close_price_mode="zero"` 에서 가격 0 으로 접수된 미체결 — 의 취소가 webapp 번역기 · relay zod · 조립기 세 코드 층에서 막히던 것을, 「취소의 가격은 원주문 가격의 사본이다 → 0 이상의 정수면 된다」는 **하나의 규칙**으로 세 층을 함께 바꿔 연다. DB CHECK 는 같은 규칙의 새 마이그레이션 **파일**을 만들고(적용은 18-18), 층마다 경계 회귀 테스트를 박는다.

Purpose: D-21 은 시간외종가 원주문의 정정을 잠그고 「취소 후 재등록」을 유일한 조치로 안내한다(`MODIFY_LOCK_OFFHOURS_ORDER`). 그 취소가 막히면 사용자는 살아 있는 실주문을 웹에서 지울 수 없고, 가드를 우회해도 relay 가 close(4400) 로 소켓을 끊어 모든 구독·대기 주문이 함께 끊긴다(18-VERIFICATION truth #8 FAILED · 18-REVIEW CR-01).
Output: 세 코드 층의 취소 가격 규칙 정합 · 새 마이그레이션 파일 `20260922120000_dma_orders_cancel_price_zero.sql` · 층별 회귀 테스트.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-VERIFICATION.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-REVIEW.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-01-SUMMARY.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-03-SUMMARY.md

근거 인용(재확인 완료):
- `webapp/src/lib/relay-provider.tsx:170-173` — `offHours = req.kind === "new" && …` 로 취소는 양수만 통과.
- `relay/src/ws/protocol.ts:303-312` — `RelayOrderCancelSchema.price: z.number().int().positive()`.
- `relay/src/dma/envelope.ts:960` — `priceFloor = krxSession === null ? 1 : 0` (취소에는 세션이 없으니 1).
- 원격 DB 현행(18-03 덤프 원문): `CONSTRAINT "dma_orders_price_check" CHECK ((("price" > 0) OR (("price" = 0) AND ("krx_session" = ANY (ARRAY['G2','G3'])))))` — 제약 이름이 **명시 이름**으로 확정돼 있다.
- gh-trade `docs/features/preopen-offhours-order.md:41,69` — 「취소는 원주문 보드 승계로 종전 경로」·「정정·취소에 실린 `krx_session` 은 무시한다」. 그래서 취소 프레임에 세션을 실어 기존 CHECK 를 통과시키는 대안은 **기각**한다: 서버가 무시하는 값을 감사 행에 「세션 지정」으로 남기게 되고, 근본 원인(취소 가격을 신규 규칙으로 검증)은 그대로 남는다.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: [tracer] 가격 0 취소 한 경로 — 번역기 · zod · 조립기 · 마이그레이션 파일 · relay 왕복 테스트</name>
  <reversibility rating="reversible">코드 3층의 검증 조건 완화 — 되돌리면 이전 조건식으로 복귀할 뿐이고 데이터 이관이 없다. 원격 DB 반영(한쪽 문)은 18-18 이 따로 게이트한다.</reversibility>
  <files>webapp/src/lib/relay-provider.tsx, relay/src/ws/protocol.ts, relay/src/dma/envelope.ts, supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql, relay/tests/ws-order.test.ts</files>
  <read_first>
    - webapp/src/lib/relay-provider.tsx (전문 400줄 — `RelayOrderRequest` 86-111, `buildOrderFrame` 158-223)
    - relay/src/ws/protocol.ts (250-340 — `OrderSideSchema` · `RelayOrderNewSchema` · `RelayOrderCancelSchema` · `RelayOrderModifySchema` 와 그 머리 주석)
    - relay/src/dma/envelope.ts (893-1040 — `DirectOrderInput` · `toWireKrxSession` · `buildDirectOrderReq`, 특히 960-968 `priceFloor`)
    - supabase/migrations/20260921120000_dma_orders_modify_offhours.sql (머리 주석 형식 · 트랜잭션 · 「하지 않는 것」 문단 — 새 파일이 같은 형식을 따른다)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-03-SUMMARY.md (원격에 걸린 `dma_orders_price_check` 정의 원문 — 새 CHECK 는 이 이름을 교체한다)
    - relay/tests/ws-order.test.ts (232-420 — 하네스 `authed`·`orderReqsOf`·`orders.inserts`·케이스 ③ 취소·④ 스키마 절단 패턴, 1373-1480 정정 케이스)
  </read_first>
  <behavior>
    - 브라우저 모양 `order.cancel`(price 0 · 원주문번호 있음)을 relay 에 보내면 게이트웨이가 `DirectOrderReq` 1건을 받고 `orderType()==="C"` · `orgOrderNo()` 원문 · `price()===0` 이다
    - 같은 요청의 감사 insert 행이 `{ orderType: "C", price: 0 }` 이고, 이어 온 취소확인(C) 통보로 `order.result` 가 `status:"cancelled"` 로 정산된다
    - `order.cancel` price −1 은 스키마에서 끊긴다 — 게이트웨이로 0바이트(케이스 ④ 와 같은 단언 형태)
    - `order.modify` price 0 은 여전히 스키마에서 끊긴다 — 게이트웨이로 0바이트
  </behavior>
  <action>
**근본 원인 수정 — 취소의 가격 규칙을 세 층에서 같은 한 문장으로 만든다 (D-21 · D-23).** 신규 전용 조건(`offHours`)을 취소에 덧대는 식으로 조건을 쌓지 않는다.

① `webapp/src/lib/relay-provider.tsx` `buildOrderFrame`: 168-173 의 가격 검사를 **종류별 규칙**으로 바꾼다. 취소(`kind:"cancel"`)는 `Number.isInteger(req.price) && req.price >= 0` 이면 통과, 신규는 기존 그대로(양수 또는 `krxSession` G2/G3 ∧ 0), 정정은 양수만. 거부 문구는 기존 원문 「주문 가격을 확인해 주세요.」 그대로다. 옛 주석 「정정·취소에는 세션이 없으므로 여전히 양수만 통과한다」는 사실이 아니게 되므로 새 규칙과 그 근거(취소 가격 = 원주문 가격의 사본 · 게이트웨이가 취소에서 가격으로 판정하지 않음 · 시간외종가 `close_price_mode="zero"` 원주문은 가격 0 · 18-REVIEW CR-01)로 교체한다. `RelayOrderRequest.price` 필드 주석에 「취소는 원주문 가격 그대로 — 시간외종가 원주문이면 0」을 한 줄 더한다. 호출부(`manual-order-form.tsx`·`account-panel.tsx`)는 이미 원주문 가격을 싣고 있으므로 **건드리지 않는다**.

② `relay/src/ws/protocol.ts` `RelayOrderCancelSchema.price` 를 `z.number().int().nonnegative()` 로 바꾸고 머리 주석에 같은 근거를 적는다. 신규·정정 스키마는 바꾸지 않는다. `.strict()` 로 만들지 않는다.

③ `relay/src/dma/envelope.ts` `buildDirectOrderReq`: `priceFloor` 를 「`orderType === "C"` 이거나 `krxSession !== null` 이면 0, 아니면 1」로 바꾸고, 거부 문구를 세 갈래(지정가·시간외종가·취소)로 가른다 — 취소 문구는 「취소 주문가격은 0 이상의 정수여야 합니다」. 조립기가 「모든 호출 경로의 마지막 관문」이라는 규율(0 을 무조건 열지 않는다)을 주석에 유지한다. `DirectOrderInput.price` 주석도 취소 0 을 반영한다.

④ `supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql` 신설 — 한 트랜잭션에서 명시 이름 `dma_orders_price_check` 를 떨어뜨리고 같은 이름으로 다시 건다: `price > 0 OR (price = 0 AND (order_type = 'C' OR krx_session IN ('G2','G3')))`. 음수는 어느 경우에도 거부. 머리 주석은 20260921120000 파일 형식을 따른다(결정 근거 CR-01 · D-21 · D-23, 취소 가격은 원주문 사본이라는 근거, 「하지 않는 것」 — 접근 정책 추가 없음 · 권한 부여/회수 구문 재실행 없음 · 기존 행 수정 없음, 기존 행은 모두 새 CHECK 를 만족). **적용하지 않는다** — 원격 반영은 18-18.

⑤ `relay/tests/ws-order.test.ts` 에 새 케이스 두 개를 기존 번호 체계 뒤에 붙인다: 「가격 0 취소(시간외종가 원주문)가 orderType C · price 0 으로 기록·송신되고 취소확인으로 정산된다 (CR-01)」, 「취소 가격 −1 과 정정 가격 0 은 스키마에서 끊긴다 — 게이트웨이로 0바이트 (CR-01)」. 기존 ③·④·㉟ 의 하네스 호출 방식을 그대로 쓴다. 계약 변경은 없으므로 shared 빌드는 필수는 아니지만 verify 가 config `build_command` 의 첫 항을 먼저 돌린다(낡은 dist 함정 회피).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay test -- ws-order</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄의 passed 수가 0 — 가격 0 취소가 여전히 close(4400)로 끊기면 새 케이스가 실패한다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "order_type = 'C'" supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql</automated>
    <fails_when>카운트가 0, 또는 grep 이 exit 2(파일 없음) — 새 CHECK 에 취소 갈래가 없다</fails_when>
  </verify>
  <acceptance_criteria>
    - `relay/tests/ws-order.test.ts` 에 CR-01 케이스 2개가 있고 green 이다: 가격 0 취소의 게이트웨이 `price()===0` · `orderType()==="C"` · insert 행 `price:0` · `cancelled` 정산 / 취소 −1·정정 0 은 게이트웨이 0바이트
    - `relay/src/ws/protocol.ts` 의 `RelayOrderCancelSchema` 가 `nonnegative()` 이고 `RelayOrderNewSchema`·`RelayOrderModifySchema` 의 가격 규칙은 한 글자도 바뀌지 않았다 — `git diff <이 플랜 시작 전 HEAD>..HEAD -- relay/src/ws/protocol.ts` 의 변경 줄이 `RelayOrderCancelSchema` 블록과 그 머리 주석 안에만 있다
    - `buildDirectOrderReq` 의 가격 하한이 `orderType === "C"` 를 포함하고, 거부 문구가 세 갈래다
    - `buildOrderFrame` 이 취소 가격을 `Number.isInteger` ∧ `>= 0` 으로 판정하고, `offHours` 판정은 신규 가격 규칙에만 쓰인다
    - 새 마이그레이션 파일이 `BEGIN;`/`COMMIT;` 한 트랜잭션이고, 주석을 뺀 SQL 문에서 접근 정책·권한 구문이 0건이다 — `grep -v '^ *--' supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql | grep -ciE 'create policy|grant |revoke '` 가 0
  </acceptance_criteria>
  <done>브라우저 모양 가격 0 취소가 relay 를 지나 게이트웨이까지 `order_type "C"` · price 0 으로 나가고 감사 행에 같은 값으로 기록된다. DB CHECK 를 같은 규칙으로 여는 마이그레이션 파일이 준비됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 층별 경계 회귀 — webapp 번역기 · zod · 조립기 단위 테스트 + shared 계약 주석</name>
  <files>webapp/src/lib/__tests__/relay-provider.test.tsx, relay/src/ws/__tests__/protocol.test.ts, relay/src/dma/__tests__/envelope.test.ts, packages/shared/src/relay.ts</files>
  <read_first>
    - webapp/src/lib/__tests__/relay-provider.test.tsx (795-1100 — `sendOrder 번역` describe, 특히 ⑨-b · ⑨-h 의 단언 형태와 `sendOrderRef` 사용법)
    - relay/src/ws/__tests__/protocol.test.ts (전문 — `parseInbound — order.modify` · `krxSession · price 0` describe 패턴)
    - relay/src/dma/__tests__/envelope.test.ts (614-945 — `주문 조립·파싱 (D-21 / Pitfall 7·8)` describe 의 `buildDirectOrderReq` 단언 형태)
    - packages/shared/src/relay.ts (482-535 — `RelayOrderNewMsg` · `RelayKrxSession` · `RelayOrderCancelMsg` 주석)
    - webapp/src/lib/relay-provider.tsx · relay/src/ws/protocol.ts · relay/src/dma/envelope.ts (Task 1 결과)
  </read_first>
  <behavior>
    - webapp: 취소 price 0 → 와이어 `{t:"order.cancel", price:0, orgOrderNo}` 1프레임 / 취소 price −1·NaN·1.5 → 보내지 않고 `rejected` + 「주문 가격을 확인해 주세요.」 / 정정 price 0 → 보내지 않고 같은 문구 / 세션 없는 신규 price 0 → 기존 ⑨-h 그대로 거부
    - zod: `order.cancel` price 0 통과 · −1 과 1.5 실패 / `order.modify` price 0 실패
    - 조립기: `orderType:"C"` price 0 → 바이트 `price()===0` / `"C"` price −1 → `BAD_PRICE` / `"N"` price 0 세션 없음 → `BAD_PRICE` / `"M"` price 0 → `BAD_PRICE`
  </behavior>
  <action>
세 층 각각의 **경계 양쪽**을 단위 테스트로 박는다 — 한 층이라도 규칙이 갈라지면(예: webapp 은 열고 zod 는 닫음) 그 층의 테스트가 먼저 깨지게. 새 케이스 이름에 `CR-01` 을 붙여 회귀 추적이 가능하게 한다.

- `webapp/src/lib/__tests__/relay-provider.test.tsx`: ⑨-h 뒤에 「⑨-j 취소는 원주문 가격 그대로 — 0 이면 0 으로 싣고, 음수·비정수는 보내지 않는다 (CR-01)」와 「⑨-k 정정 가격 0 은 보내지 않는다 — 신규 전용 0 규칙이 정정으로 새지 않는다 (CR-01)」를 더한다. `sent` 배열·`sendOrderRef` 하네스를 그대로 쓴다.
- `relay/src/ws/__tests__/protocol.test.ts`: 새 describe 「parseInbound — order.cancel price 0 (CR-01)」.
- `relay/src/dma/__tests__/envelope.test.ts`: `주문 조립·파싱` describe 안에 취소 0 허용 · 취소 음수 거부 · 신규/정정 0 거부 4케이스.
- `packages/shared/src/relay.ts` `RelayOrderCancelMsg` 머리 주석에 「`price` 는 원주문 가격 그대로다 — 시간외종가(G2/G3) `close_price_mode="zero"` 원주문은 0 이다. 네 층(webapp 번역기 · relay zod · 조립기 · DB CHECK)이 취소에 한해 0 이상 정수를 받는다 (CR-01)」를 더한다. 타입 모양은 바꾸지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- relay-provider</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test -- protocol envelope</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 passed 수가 0</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run test</automated>
    <fails_when>non-zero exit, 또는 relay 통과 수가 500 미만(18-VERIFICATION 기준선)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "CR-01" webapp/src/lib/__tests__/relay-provider.test.tsx relay/src/ws/__tests__/protocol.test.ts relay/src/dma/__tests__/envelope.test.ts` 의 세 파일 카운트가 전부 1 이상이다
    - webapp 취소 −1·NaN·1.5 케이스가 `sent` 에 프레임이 추가되지 않았음을 단언한다(보내지 않음이 확실한 실패)
    - relay 전량 테스트가 green 이고 통과 수가 500 이상이다
    - `RelayOrderCancelMsg` 의 필드 목록(`t`·`rid`·`isin`·`exchange`·`orgOrderNo`·`qty`·`price`·`accountNo`)이 바뀌지 않았다
  </acceptance_criteria>
  <done>취소 가격 규칙이 세 코드 층에서 같은 문장으로 서 있고, 어느 층이 갈라져도 그 층의 단위 테스트가 먼저 깨진다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → relay wss | 취소 프레임의 `price` 가 신뢰할 수 없는 입력으로 넘어온다 |
| relay → 게이트웨이(실계좌) | 조립기가 마지막 관문 — 여기서 새는 값은 실주문 바이트가 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-66 | Tampering | `RelayOrderCancelSchema` 완화 | medium | mitigate | 0 만 연다(`nonnegative().int()`) — 음수·비정수는 여전히 zod 위반. 신규·정정 스키마는 무변경(Task 1 acceptance 가 diff 로 확인) |
| T-18-67 | Tampering | 조립기 `priceFloor` | high | mitigate | 0 허용은 `orderType === "C"` 또는 G2/G3 세션 두 갈래뿐. 세션 없는 신규 0·정정 0 의 `BAD_PRICE` 를 Task 2 가 단위 테스트로 고정 |
| T-18-68 | Denial of Service | 가격 0 취소의 close(4400) | high | mitigate | zod 가 0 을 받아 소켓이 끊기지 않는다 — 한 번의 취소 시도가 전 구독·대기 주문을 끊던 경로 제거 |
| T-18-69 | Repudiation | 감사 행 | medium | mitigate | 취소 행이 원주문 가격 그대로(0 포함) 기록된다. 원격 CHECK 가 열리기 전에는 relay 가 보내지 않고 「주문 기록에 실패」로 끝낸다(감사 없는 실주문 0) |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 플랜은 패키지를 설치하지 않는다 — legitimacy 체크포인트 없음 |
</threat_model>

## Artifacts this phase produces

- **마이그레이션 파일(미적용):** `supabase/migrations/20260922120000_dma_orders_cancel_price_zero.sql` — `dma_orders_price_check` 재정의(취소 갈래 추가)
- **relay:** `RelayOrderCancelSchema.price` 규칙 변경(`nonnegative`) · `buildDirectOrderReq` 취소 가격 하한 · 취소 거부 문구 「취소 주문가격은 0 이상의 정수여야 합니다」
- **webapp lib:** `buildOrderFrame` 종류별 가격 규칙
- **테스트:** relay `ws-order.test.ts` CR-01 2케이스 · `protocol.test.ts` 「order.cancel price 0 (CR-01)」 describe · `envelope.test.ts` 4케이스 · webapp `relay-provider.test.tsx` ⑨-j · ⑨-k

<verification>
- relay `ws-order` 새 케이스: 가격 0 취소가 게이트웨이·감사 행 양쪽에 price 0 · orderType C 로 나간다
- 세 코드 층 단위 테스트가 경계 양쪽(0 허용 · 음수/비정수 거부 · 신규/정정 0 거부)을 고정한다
- relay 전량 ≥ 500 green · relay/webapp typecheck 클린
</verification>

<success_criteria>
- 시간외종가 원주문 취소가 코드 3층을 통과한다(DB 층은 18-18 이 원격 반영)
- 신규·정정의 가격 규칙은 한 글자도 넓어지지 않았다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-14-SUMMARY.md` when done
</output>
