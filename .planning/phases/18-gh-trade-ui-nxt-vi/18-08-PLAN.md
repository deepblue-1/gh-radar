---
phase: 18-gh-trade-ui-nxt-vi
plan: 08
type: execute
wave: 3
depends_on: ["18-04", "18-06"]
files_modified:
  - webapp/src/components/trading/workbench/breakout-strip.tsx
  - webapp/src/components/trading/workbench/stock-add-bar.tsx
  - webapp/src/components/trading/limit-chaser-client.tsx
  - webapp/src/components/trading/__tests__/breakout-strip.test.tsx
  - webapp/src/components/trading/__tests__/stock-add-bar.test.tsx
autonomous: true
requirements: [TRADE-06]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-07: 돌파 스트립은 「돌파 N」 + 「신규 M」 필 + 칩 가로 스크롤 한 줄 + 「더보기」이고, 「더보기」로 열리는 표는 종목 · 현재가 · 등락률 · 임계 · 기준가 · 돌파시각 · 액션 7열이다. **칩과 행 어디에도 KRX/NXT 를 표시하지 않는다** (TRADE-06)"
    - "D-07: 칩/행을 클릭하면 바로 아래 격자에 카드가 생기고(거래소 KRX 로 시작 · 스위치 전부 OFF) 그 행은 「거래중」 표식으로 바뀐다. 이미 카드가 있는 종목은 새 카드를 만들지 않고 기존 카드를 펼치고 스크롤한다 (TRADE-06)"
    - "D-14: 목록은 relay 가 준 집합·순서를 **그대로** 쓴다 — 클라가 정렬·필터·상한을 다시 적용하지 않고 자동으로 카드를 열지 않는다. 78 빈 배열도 권위값이다 (TRADE-06)"
    - "D-18: 신규 행은 깜박임 없이 30초 `--new-bg` + 「신규」 배지 뒤 기본으로 돌아가고, 강조 타이머는 **목록 전체에 1개**(1초 tick, 강조 행이 있을 때만)다 (TRADE-06)"
    - "D-18: 행 ✕ 로 수동 삭제하면 그날 재돌파에도 다시 나오지 않는다(지운 종목 집합, 기기별·오늘). `aria-label` 은 「{종목명} 돌파 목록에서 지우기 (이 기기만 · 오늘)」 (TRADE-06)"
    - "D-17: 알림음은 종목당 하루 1회이고 **집합에 먼저 쓴 다음** 재생한다. 78 스냅샷으로 들어온 행은 무음·무강조이면서 「오늘 울린 종목」 집합에는 기록된다 (TRADE-06)"
    - "D-16: 이탈 삭제는 `shouldRemoveBreakout` 한 함수로만 판정하고, 구독하지 못한 행(현재가 모름)은 삭제하지 않고 76 의 마지막 가격을 그대로 보인다 (TRADE-06)"
    - "D-30: relay 가 `name`/`code` 를 못 채운 행은 ISIN 을 그대로 표시하고 그 행에서 만든 카드는 종목정보 팝업이 비활성이다 (TRADE-06)"
    - "D-08: 종목 추가란은 검색 입력 + 「추가」 버튼뿐이고 **거래소 토글이 없다**. 돌파 스트립 바로 아래 · 카드 격자 위에 놓인다 (TRADE-06)"
    - "D-08: 종목 검색은 Phase 16 상따 종목검색 동작을 그대로 승계한다 — 첫 항목 자동 활성화 · ↓/Enter 키보드 왕복 · `isin` 없는 종목(ETP 등)은 고를 수 없음 · 시장구분을 알 수 없는 종목도 같은 취급 (TRADE-06)"
    - "E4 empty: 행 0개면 라벨 「돌파 0」 유지 + 「아직 임계 20% 를 넘은 종목이 없어요」 · 「신규」 필 `hidden` (TRADE-06)"
    - "E4 loading: 78 스냅샷 전에도 빈 상태를 그린다 — 스피너·스켈레톤 없음. 인증 직후 78 이 오면 전량 교체 (TRADE-06)"
    - "E4 error: 목록은 relay push 전용이라 로드 실패 경로가 없다. 구독 실패·상한 초과 행은 이탈 판정을 하지 않고 76 의 마지막 가격을 보인다. relay 끊김은 상태줄 DMA 필이 말한다 (TRADE-06)"
    - "E4 populated: 칩 가로 스크롤 + 「더보기」 표 최신 위 · 신규 행 30초 `--new-bg` + 「신규」 배지(목록 전체 타이머 1개, 깜박임 없음) · 카드 있는 종목은 「거래중」 표식 · 78 스냅샷 행은 무음·무강조 (TRADE-06)"
    - "E4 partial: `name`/`code` 가 없으면 ISIN 을 그대로 표시하고 ⓘ 팝업 비활성. 시세 미구독 행은 76 의 가격·등락률 유지 (TRADE-06)"
    - "E4 overflow: 상한 200행(Phase 17 리듀서) · 칩 줄 가로 스크롤 · 표는 page 폰 밴드에서 목업의 열 접기(종목 셀 보조줄 「{HH:MM:SS} 돌파」)를 따른다 (TRADE-06)"
    - "E4 zero-one-many: 0 은 빈 문구 · 1+ 는 동일 문법 · 라벨 「돌파 {N}」·「신규 {M}」 은 숫자형이라 단/복수 어휘 분기가 없고, M=0 이면 「신규」 필이 `hidden` (TRADE-06)"
    - "E4 long-text: 종목명은 1줄 ellipsis 이고 칩 `title` 에 「{종목명} {코드} · 임계 {N}% · 기준가 {가격} · 돌파 {HH:MM:SS}」 전체가 담긴다 (TRADE-06)"
    - "E5 empty: 입력 공란이면 「추가」 disabled · 결과 목록 없음. 검색 결과 0건은 Phase 16 컴포넌트의 기존 빈 문구 그대로(신규 문구 없음) (TRADE-06)"
    - "E5 loading: 검색 중 상태는 Phase 16 종목검색의 기존 처리(디바운스 · 결과 갱신 · 첫 항목 자동 활성) 그대로 — 신규 로딩 표면 없음 (TRADE-06)"
    - "E5 error: 검색 API 실패는 Phase 16 컴포넌트 기존 처리 그대로. 이미 카드가 있는 종목을 「추가」하면 새 카드를 만들지 않고 기존 카드를 펼치고 스크롤한다 (TRADE-06)"
    - "E5 long-text: 결과 항목의 긴 종목명은 1줄 ellipsis 이고 입력값은 네이티브 가로 스크롤이다 (TRADE-06)"
  artifacts:
    - webapp/src/components/trading/workbench/breakout-strip.tsx
    - webapp/src/components/trading/workbench/stock-add-bar.tsx
  key_links:
    - "`breakout-strip.tsx` ↔ `breakout-list.ts`(18-04) — 표시·삭제·강조·집합 판정이 전부 그 모듈 함수에서 나온다"
    - "칩/행 클릭 ↔ 작업대의 카드 추가 콜백 — 스트립은 카드를 직접 만들지 않고 `onAddCard(isin, name?, code?)` 를 부른다"
    - "`stock-add-bar.tsx` ↔ `limit-chaser-client.tsx` 의 `StockSearchField`/`isPickable` — 승격이지 재구현이 아니다"
  prohibitions:
    - "MUST NOT 돌파 목록을 클라가 재정렬·재해석하거나 카드를 자동으로 열어 사용자가 요청하지 않은 거래 표면을 만든다"
    - "MUST NOT 구독하지 못해 현재가를 모르는 행을 이탈로 판정해 지운다"
    - "MUST NOT 알림 재생을 집합 기록보다 먼저 한다"
    - "MUST NOT 행마다 강조 타이머를 건다 — 목록은 최대 200행이다"
---

<objective>
`/trading` 의 **탐색 두 줄**을 만든다 — 돌파감지 스트립/표(칩 + 「더보기」 7열 표, 클릭으로 카드 추가)와 그 아래 종목 추가 검색란.

Purpose: Phase 17 이 76/78 파싱·캐시·리듀서를 끝냈지만 소비 화면이 **0개**다. 이 플랜이 TRADE-06 의 화면 절반이고, 18-04 가 만든 판정 함수들의 첫 소비처다.
Output: `components/trading/workbench/breakout-strip.tsx` · `stock-add-bar.tsx` 신설.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-PATTERNS.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `breakout-strip.tsx` — 칩 스트립 + 7열 표 + 「거래중」/「거래 추가」</name>
  <files>webapp/src/components/trading/workbench/breakout-strip.tsx, webapp/src/components/trading/__tests__/breakout-strip.test.tsx</files>
  <read_first>
    - webapp/src/lib/breakout-list.ts (18-04 — `breakoutRowsFrom`·`shouldRemoveBreakout`·집합 I/O·`HIGHLIGHT_MS`)
    - webapp/src/lib/relay-provider.tsx (`rateCrossItems` 리듀서 — 76 upsert · 78 전량 교체 · 서버 정렬 · 상한 200)
    - webapp/src/components/trading/vi-order-list.tsx (`useNow()` — 목록 전체 타이머 1개 선례)
    - webapp/src/components/trading/workbench/vi-trigger-strip.tsx (18-05 — 칩 + 「더보기」 표 조합의 동형 구조, 같은 문법을 쓴다)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §돌파감지 스트립·표 문구표(빈 상태·칩 `title`·✕ `aria-label`·액션 라벨) · §상호작용 「이탈 삭제」/「거래중 표식」/「78 스냅샷 행」
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html (마크업 `:745-746` · 렌더 `:859-884` · CSS `:282-300`, `:330-358`)
  </read_first>
  <behavior>
    - 행 0개 → 「돌파 0」 + 빈 문구, 「신규」 필이 DOM 에 없다.
    - 76 으로 새 행이 들어오면 30초 동안 `--new-bg` + 「신규」 배지, 30초 뒤 기본으로 돌아간다(1초 tick 타이머 1개).
    - 같은 ISIN 에 76 이 다시 오면 자리가 유지되고 현재가·등락률·임계·기준가만 갱신되며 돌파시각·돌파가는 첫 등재값을 유지한다.
    - 78 스냅샷 행은 강조 배경이 없고 「신규」 배지도 없다.
    - relay 배열 순서를 그대로 렌더한다(정렬·필터 호출 0회).
    - 칩/행 클릭 → `onAddCard(isin, name, code)` 1회. 이미 카드가 있는 ISIN 이면 `onFocusCard(isin)` 이 불리고 `onAddCard` 는 불리지 않는다.
    - 카드가 있는 종목의 행은 「거래중」(초록 ● · 점선 테두리 · `cursor: default`)이고 「거래 추가」 버튼이 없다.
    - 행 ✕ → `onDismiss(isin)` 1회, `aria-label` 이 UI-SPEC 원문.
    - `name`/`code` 가 없는 행은 ISIN 을 그대로 표시한다(ISIN 을 이름 자리에 넣어 「이름이 있다」고 위장하지 않는다 — 셀에 코드 자리가 비어 있음이 보인다).
    - 칩/행 어디에도 KRX/NXT 문자열이 없다.
    - 현재가를 모르는 행은 76 의 가격·등락률을 그대로 보이고 사라지지 않는다.
  </behavior>
  <action>
`webapp/src/components/trading/workbench/breakout-strip.tsx` 를 신설한다. 구조는 `vi-trigger-strip.tsx`(18-05)와 **같은 문법**이다 — 접힌 줄에 「돌파 {N}」 + 「신규 {M}」 필 + 칩 가로 스크롤 + 「더보기」, 펼친 표에 헤더줄(「돌파감지」 + 「돌파 {N} · 신규 {M} · 임계 20% · 최신 위」 + 「접기 ▴」)과 7열(종목 · 현재가 · 등락률 · 임계 · 기준가 · 돌파시각 · 액션).

**거래소를 표시하지 않는다**(D-07). 칩에도 행에도 KRX/NXT 태그가 없다 — 돌파 감지는 서버 정본상 KRX A3 에서만 발화하므로 표시할 정보가 아니다.

행 데이터는 `breakoutRowsFrom(rateCrossItems, { dismissed, cards, now })`(18-04)이 만든다. 이 컴포넌트는 **정렬·필터·상한을 다시 적용하지 않는다** — relay 리듀서가 이미 했다. 강조 만료는 `useNow()` 동형의 **목록 전체 1초 tick 타이머 1개**로 재렌더해 처리하고, 강조 행이 하나도 없으면 타이머를 멈춘다. 행마다 `setTimeout` 을 걸지 않는다(최대 200행).

**이탈 삭제**는 `shouldRemoveBreakout(row, quotes.get(relayQuoteKey(row.isin,"KRX"))?.price)` 로만 판정한다. 현재가가 `undefined` 면 그 행은 판정 대상이 아니다 — 76 의 마지막 가격을 그대로 보이고 지우지 않는다.

**알림음:** 새 76 행이 들어오고 그 ISIN 이 「오늘 울린 종목」에 없으면 ① `addSounded(isin)` 를 **먼저** 부르고 ② `playBreakoutTone()` 을 부른다. 순서가 계약이다. 78 스냅샷 유래 행(`silent`)은 재생하지 않고 집합 기록만 한다.

**액션 열:** 카드가 없으면 「거래 추가」 버튼(신규 행은 `--primary` 채움), 있으면 「거래중」 표식. 클릭 콜백은 둘로 나눈다 — `onAddCard(isin, name?, code?)` 와 `onFocusCard(isin)`. 이 컴포넌트는 카드를 직접 만들지 않는다.

문구는 UI-SPEC §돌파감지 스트립·표 원문 그대로다(빈 상태 · 칩 `title` 템플릿 · ✕ `aria-label` · 폰 밴드 보조줄 「{HH:MM:SS} 돌파」). 폰 밴드 열 접기는 목업이 정본이다(D-26).

테스트 `__tests__/breakout-strip.test.tsx` 는 `<behavior>` 12케이스를 RTL 로 덮는다. 타이머는 fake timers 로 30초 경계를 단언하고, 「집합 기록이 재생보다 먼저」를 호출 순서로 단언한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- breakout-strip</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "\.sort\(|\.reverse\(|\.slice\(0, *200" webapp/src/components/trading/workbench/breakout-strip.tsx</automated>
    <fails_when>non-zero exit — 컴포넌트가 서버 집합을 재정렬하거나 상한을 다시 적용하고 있다(D-14 위반)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "\"KRX\"|'KRX'|KRX<|>KRX|\"NXT\"|'NXT'" webapp/src/components/trading/workbench/breakout-strip.tsx</automated>
    <fails_when>non-zero exit — 칩/행에 거래소 문자열이 들어갔다(D-07 위반). 구독 거래소 상수가 필요하면 `use-breakout-quotes.ts` 안에 둔다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "shouldRemoveBreakout" webapp/src/components/trading/workbench/breakout-strip.tsx</automated>
    <fails_when>카운트가 0 — 이탈 판정을 컴포넌트가 다시 쓰고 있다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 30초 강조 만료가 fake timer 로 단언되고, 타이머 생성 횟수가 행 수와 무관하게 1 임이 단언된다
    - 78 스냅샷 행의 무음·무강조 + 집합 기록이 단언된다
    - 「집합 기록 → 재생」 호출 순서가 단언된다
    - 카드 있는 종목의 「거래중」 표식과 `onAddCard` 미호출이 단언된다
    - 현재가 모름 행이 삭제되지 않음이 단언된다
    - `name`/`code` 부재 행이 ISIN 을 그대로 보임이 단언된다
    - 컴포넌트 소스에 정렬·상한 재적용과 거래소 문자열이 없다
  </acceptance_criteria>
  <done>돌파 종목을 한 줄로 훑고 클릭 한 번으로 거래 대상에 올릴 수 있으며, 서버 집합을 한 번도 재해석하지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `stock-add-bar.tsx` — 종목검색 승격 (거래소 토글 없음)</name>
  <files>webapp/src/components/trading/workbench/stock-add-bar.tsx, webapp/src/components/trading/limit-chaser-client.tsx, webapp/src/components/trading/__tests__/stock-add-bar.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/limit-chaser-client.tsx (`StockSearchField`·`isPickable` 현행 정의와 존재 이유 주석 — `global-search.tsx` 를 쓰지 않는 이유, `isin` 없는 종목을 고를 수 없는 이유, 시장구분 미상 종목 취급)
    - webapp/src/lib/stock-api.ts (검색 API 계약 — `?q=` 로 코드/이름을 찾아 isin 을 반환)
    - webapp/src/components/ui/command.tsx (검색 프리미티브)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §레이아웃 계약 8번(placeholder 원문 · 「추가」 버튼 · 거래소 토글 없음)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html (종목 추가 `:748-751`)
  </read_first>
  <behavior>
    - 입력 공란 → 「추가」 disabled.
    - 검색 결과의 첫 항목이 자동 활성화된다.
    - ↓/Enter 키보드 왕복으로 항목을 고르고 확정할 수 있다.
    - `isin` 이 없는 종목은 고를 수 없다(활성화되지 않음).
    - 시장구분을 알 수 없는 종목도 `isin` 만 있으면 고를 수 있다.
    - 「추가」 → `onAdd(isin, name, code)` 1회.
    - 이미 카드가 있는 종목을 고르면 `onAdd` 대신 `onFocusCard(isin)` 이 불린다.
    - 거래소 토글이 DOM 에 없다.
    - 결과 항목의 긴 종목명이 1줄 ellipsis 다.
  </behavior>
  <action>
`limit-chaser-client.tsx` 안에 비공개로 있는 `StockSearchField` 와 `isPickable` 을 `webapp/src/components/trading/workbench/stock-add-bar.tsx` 로 **승격**한다(재구현이 아니라 이동). 존재 이유 주석을 함께 옮긴다 — `global-search.tsx` 를 쓰지 않는 이유(그쪽은 선택 시 종목 상세로 라우팅한다), `isin` 이 없는 종목(ETP 등)은 고를 수 없다는 규율, 시장구분을 알 수 없는 종목도 같은 취급이라는 규율. 첫 항목 자동 활성화와 ↓/Enter 키보드 왕복 동작을 유지한다(기존 e2e 케이스가 이것을 단언한다).

바 구성은 검색 입력(placeholder 「종목 추가 — 종목명 또는 코드」 + `⌘K` 힌트) + 「추가」 버튼(`.btn.pri`)뿐이다. **거래소 토글을 두지 않는다**(D-08) — 새 카드는 KRX 로 시작하고 등록 전에 카드 헤더 세그먼트에서 바꾼다.

이미 카드가 있는 종목을 고르면 새 카드를 만들지 않고 기존 카드를 펼치고 스크롤한다(돌파 스트립과 같은 규칙) — 콜백을 `onAdd`/`onFocusCard` 둘로 나눈다.

`limit-chaser-client.tsx` 는 승격된 컴포넌트를 import 해 쓰도록 고친다(정의 1벌 유지, 기존 화면 계속 동작).

테스트 `__tests__/stock-add-bar.test.tsx` 는 `<behavior>` 9케이스를 RTL 로 덮는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- stock-add-bar</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "isPickable" webapp/src/components/trading/workbench/stock-add-bar.tsx</automated>
    <fails_when>카운트가 0 — 선택 가능 판정을 승격하지 않고 다시 썼다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- limit-chaser-client</automated>
    <fails_when>non-zero exit, 또는 요약줄에 "Tests  0 passed" — 승격으로 기존 화면이 깨졌다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `stock-add-bar.tsx` 가 `isPickable` 판정을 포함하고 `limit-chaser-client.tsx` 에 중복 정의가 없다
    - 첫 항목 자동 활성화 · ↓/Enter 왕복이 단언된다
    - `isin` 없는 종목 선택 불가가 단언된다
    - 거래소 토글이 DOM 에 없음이 단언된다
    - 이미 카드가 있는 종목 선택 시 `onFocusCard` 가 불림이 단언된다
    - 기존 `limit-chaser-client` 테스트가 통과한다
  </acceptance_criteria>
  <done>돌파 목록에 없는 종목도 검색 한 번으로 거래 대상에 올릴 수 있다.</done>
</task>

<task type="auto">
  <name>Task 3: 두 표면의 상태 커버리지 마감 — 빈·로딩·부분·오버플로</name>
  <files>webapp/src/components/trading/__tests__/breakout-strip.test.tsx, webapp/src/components/trading/__tests__/stock-add-bar.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/workbench/breakout-strip.tsx (Task 1 결과)
    - webapp/src/components/trading/workbench/stock-add-bar.tsx (Task 2 결과)
    - webapp/src/components/trading/surface-placeholder.tsx (「불러오는 중」으로 위장하지 않는다 규율)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §UI Considerations 의 E4·E5 행 전부
  </read_first>
  <action>
UI-SPEC §UI Considerations 의 E4(empty/loading/error/populated/partial/overflow/zero-one-many/long-text 8행)와 E5(empty/loading/error/long-text 4행) 중 Task 1·2 에서 아직 단언되지 않은 상태를 테스트로 채운다.

특히 다음을 명시적으로 단언한다 — ① 78 스냅샷 도착 전 빈 상태에 스피너·스켈레톤이 **없다**(`surface-placeholder` 규율: 준비 중이면 준비 중이라고 쓴다) ② M=0 일 때 「신규」 필이 렌더되지 않는다(`hidden` 이 아니라 DOM 부재 또는 HTML `hidden` 속성 — 접근성 트리에서도 빠진다) ③ 200행에서 칩 줄이 가로 스크롤이고 표는 세로로 자연 확장한다 ④ 긴 종목명이 1줄 ellipsis + `title` 전문 ⑤ 「신규」가 색 **+ 배지 텍스트**로 말한다(색만으로 말하지 않는다) ⑥ 검색 결과 0건에 신규 문구를 만들지 않고 기존 컴포넌트 문구를 쓴다.

E4 overflow 의 폰 밴드 열 접기는 컨테이너 쿼리라 jsdom 에서 정확히 재현되지 않는다 — 단위 테스트는 **열 접기용 클래스/구조가 존재하는지**까지만 단언하고, 실제 잘림 0 단언은 18-13 의 Playwright 가 맡는다는 사실을 SUMMARY 에 적는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- breakout-strip</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test</automated>
    <fails_when>non-zero exit, 또는 통과 수가 Phase 17 기준선 1008 보다 작다</fails_when>
    <human-check>돌파 스트립과 종목 추가란을 dev 화면에서 열어 목업 `18-workbench-mockup.html` 의 해당 영역(범례·설계 메모 포함)과 배치·문구·색이 일치하는지 눈으로 대조한다(목업이 정본 — D-26).</human-check>
  </verify>
  <acceptance_criteria>
    - E4 8행 · E5 4행 중 단위 테스트로 표현 가능한 항목이 전부 단언된다
    - 「스피너·스켈레톤 없음」이 명시적으로 단언된다
    - 「신규」 필이 M=0 에서 접근성 트리에도 없음이 단언된다
    - 색 + 텍스트 이중 표현이 단언된다
    - 컨테이너 쿼리 의존 항목은 Playwright 로 넘긴다고 SUMMARY 에 기록된다
    - webapp 테스트 전량 통과 수가 1008 이상
  </acceptance_criteria>
  <done>두 표면의 모든 상태가 정직하게 그려지고, jsdom 이 못 보는 것만 실브라우저로 넘어간다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay wss → 브라우저 (76/78) | 돌파 집합이 여기서 들어온다. 클라는 재해석하지 않는다 |
| 브라우저 → localStorage | 「울린/지운 종목」 집합 (기기별) |
| 브라우저 → 검색 API (`/api/stocks/search`) | 종목 추가 경로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-36 | Tampering | 목록 재해석(정렬·상한·자동 열기) | high | mitigate | 컴포넌트에 정렬·상한 재적용 0건을 verify 가 grep 으로 단언. 카드 추가는 언제나 사용자 클릭에서만 |
| T-18-37 | Tampering | 모르는 값으로 행 삭제 | high | mitigate | `shouldRemoveBreakout` 이 `currentPrice === undefined` 에서 false. 테스트가 단언 |
| T-18-38 | Spoofing | ISIN 을 이름 자리에 넣기 | medium | mitigate | `name` 부재 시 ISIN 을 **코드/이름 구분이 보이는 형태로** 표시하고 ⓘ 를 비활성. 「이름이 있다」로 위장하지 않는다 |
| T-18-39 | Information Disclosure | localStorage 집합 | medium | mitigate | ISIN 만 저장(18-04 가 강제). 이 컴포넌트는 집합 I/O 를 직접 하지 않고 그 모듈을 부른다 |
| T-18-40 | Denial of Service | 200행 × 타이머 | medium | mitigate | 목록 전체 1초 tick 타이머 1개 · 강조 행 0 이면 정지 |
| T-18-41 | Elevation of Privilege | 고를 수 없는 종목 선택 | medium | mitigate | `isPickable` 승격(재구현 금지) — `isin` 없는 종목은 거래 대상이 될 수 없다 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 phase 는 새 패키지를 설치하지 않는다(RESEARCH §Package Legitimacy Audit 표 비어 있음) |
</threat_model>

## Artifacts this phase produces

새로 만드는 심볼·파일 (phase 전체 — 드리프트 검증에서 제외되어야 한다):

- **마이그레이션:** `supabase/migrations/20260921120000_dma_orders_modify_offhours.sql` · 컬럼 `dma_orders.piece_count` · `dma_orders.krx_session`
- **shared:** `RelayOrderModifyMsg` · `RelayKrxSession` · `OrderType` 의 `"M"` 멤버 · `RelayOrderNewMsg.pieceCount?` · `RelayOrderNewMsg.krxSession?` · `RelayRateCrossItem.name?` · `RelayRateCrossItem.code?`
- **relay:** `RelayOrderModifySchema` · `OrderInsertRow.pieceCount?` · `OrderInsertRow.krxSession?` · 돌파 항목 보강 private 메서드 · `relay/src/ws/__tests__/protocol.test.ts`
- **webapp lib:** `RelayOrderRequest.kind` 의 `"modify"` · `queued-window.ts`(`OrderButtonMode`·`ManualOrderAffordance`·`affordanceOf`) · `breakout-list.ts`(`kstDateKey`·`shouldRemoveBreakout`·`breakoutRowsFrom`·`BreakoutRow`·키 상수 4종·`REMOVE_MARGIN_PCT`·`ARM_GRACE_MS`·`HIGHLIGHT_MS`) · `use-breakout-quotes.ts`(`useBreakoutQuotes`·`MAX_BREAKOUT_SUBS`) · `alert-tone.ts`(`playBreakoutTone`·`isTonePlaybackBlocked`·`resumeToneContext`) · `limit-chaser.ts` 로 이동한 `parseStrategyKey`
- **webapp 컴포넌트:** `components/trading/workbench/` 의 `TradingWorkbench`·`WorkbenchStatusBar`·`ViSettingsRows`·`ViTriggerStrip`·`BreakoutStrip`·`StockAddBar`(+승격된 `StockSearchField`·`isPickable`)·`CardGrid`·`SharedPanels` · `components/trading/card/` 의 `StrategyCard`·`CardHeader`·`QuoteGrid10`·`CardBody`·`ManualOrderForm`(+`canModify`)·`StockInfoModal`
- **라우트:** `webapp/src/app/trading/page.tsx`
- **CSS 토큰:** `--new-bg` · `--new-bd` (globals.css §9 라이트/다크 양쪽)
- **e2e:** `webapp/e2e/specs/trading-workbench.spec.ts`

<verification>
- `pnpm --filter @gh-radar/webapp test` 전량 exit 0 (통과 수 1008 이상)
- `pnpm --filter @gh-radar/webapp run typecheck` exit 0
- 돌파 컴포넌트에 정렬·상한 재적용·거래소 문자열 0건
</verification>

<success_criteria>
- 돌파 스트립/표가 relay 집합을 한 번도 재해석하지 않고 그린다
- 알림음이 하루 1회·기록 우선 순서로 울린다
- 종목 추가란이 거래소 토글 없이 검색·추가만 한다
- 두 표면의 빈·로딩·부분 상태가 위장 없이 그려진다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-08-SUMMARY.md` when done
</output>