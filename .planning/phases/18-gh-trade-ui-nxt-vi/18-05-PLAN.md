---
phase: 18-gh-trade-ui-nxt-vi
plan: 05
type: execute
wave: 2
depends_on: ["18-02"]
files_modified:
  - webapp/src/components/trading/workbench/vi-settings-rows.tsx
  - webapp/src/components/trading/workbench/vi-trigger-strip.tsx
  - webapp/src/components/trading/vi-settings-card.tsx
  - webapp/src/components/trading/__tests__/vi-settings-rows.test.tsx
  - webapp/src/components/trading/__tests__/vi-trigger-strip.test.tsx
autonomous: true
requirements: [TRADE-08]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-05: VI 설정은 KRX 한 줄 · NXT 한 줄이고, 각 줄은 거래소 태그 · on/off 스위치(= `run`) · 상승률 % · 금액 만원 · 가동중이면 빨간 「가동중」 + 「서버 반영 {시각}」(아니면 「중지」) · 더티면 줄 끝 「수정」 으로 구성된다. 계좌·마감알림은 줄에 없다 (TRADE-08)"
    - "D-05: `vi.set` 은 **그 줄의 거래소**를 실어 보낸다. 고정 거래소 상수는 소비처 4곳(캡션·송신·요약행·주석)에서 전부 사라진다 — 한 곳이라도 남으면 NXT 줄이 KRX 전략을 덮는다 (TRADE-08)"
    - "D-05: 시작/중지 확인 다이얼로그(Phase 16 D-07)와 3초 ack 타임아웃 규율(「타이머는 잠금을 풀 뿐 아무것도 다시 보내지 않는다」)은 그대로 승계된다 (TRADE-08)"
    - "D-06: VI 발동 스트립은 접힌 줄에 「VI N · 미확인 M」 라벨 + 칩(종목명·발동가·전일대비·시각·상태 배지)을 그리고, 「더보기」로 열리는 표가 기존 VI 주문내역 화면을 **대체**한다 (TRADE-08)"
    - "D-06: 확인 체크는 **표에서만** 가능하고 활성 규칙은 기존 `isConfirmable` 을 그대로 부른다(접수 후 ∧ 주문번호 有 ∧ `!confirm_locked`). 체크 즉시 잠기고 즉시 `vi.confirm` 을 보내며 **더티가 아니다** (TRADE-08)"
    - "D-06: 미확인(`Accepted ∧ !confirmed`) 칩·행은 `--new-bg` 로 강조되고, 표는 양 거래소 한 목록·최신 위다 (TRADE-08)"
    - "D-27: VI 에코가 오면 baseline 을 덮고 더티를 지우며, 더티 중 에코면 값을 덮고 「다른 단말에서 변경됨」 `role=\"status\"` 를 인라인으로 띄운다 — 토스트 라이브러리를 쓰지 않는다 (TRADE-08)"
    - "TRADE-08 adjacency: KRX 줄과 NXT 줄은 상승률·금액이 완전히 같아도 **서로 다른 전략 슬롯**이며, 한 줄의 `vi.set`·더티·에코가 다른 줄의 상태를 건드리지 않는다"
    - "TRADE-08 empty: `viTriggers.KRX`/`viTriggers.NXT` 스냅샷이 없어도 두 줄은 항상 그려지고 스위치 OFF·「중지」·Phase 16 기본값으로 표시된다. `viOrders` 0건이면 스트립 라벨 「VI 0」 유지 + 「오늘 발동된 VI 주문이 없어요」"
    - "TRADE-08 ordering: VI 발동 표는 양 거래소를 한 목록에 최신 위로 그리고, 주문시각이 같은 두 행의 상대 순서는 relay 가 준 배열 순서를 **그대로** 유지한다(클라 재정렬 금지)"
    - "E2 empty: VI 스냅샷 전/전략 미설정이어도 KRX·NXT 두 줄을 항상 그린다 — 별도 빈 상태 문구 없음 (TRADE-08)"
    - "E2 loading: `vi.set` 전송 후 에코 전에는 줄 끝 「수정」이 「반영 중…」 + disabled 로 바뀐다. 스위치는 확인 다이얼로그 뒤 에코까지 pending 표시. 스피너 없음 (TRADE-08)"
    - "E2 error: 거부/타임아웃이면 줄 아래 인라인 `role=\"status\"` 로 기존 VI 화면 오류 문구를 그대로 띄우고 더티 값은 보존하며 `message` 는 표시만 한다 (TRADE-08)"
    - "E2 partial: 상승률·금액 중 하나만 바꿔도 줄 단위 더티(「수정」 1개, 바뀐 라벨만 `● `). 공란 입력은 「수정」 disabled. 서버 스냅샷에 한 거래소만 있으면 나머지 줄은 기본값·「중지」 (TRADE-08)"
    - "E3 empty: 칩 0개면 라벨 「VI 0」 유지 + 「오늘 발동된 VI 주문이 없어요」 · 「미확인」 필 `hidden` · 「더보기」 표도 같은 빈 문구 (TRADE-08)"
    - "E3 loading: 73 스냅샷 도착 전에도 빈 상태를 그린다 — 별도 fetch 가 없으므로 스피너·스켈레톤 없음 (TRADE-08)"
    - "E3 populated: 칩은 가로 스크롤 한 줄(줄바꿈 없음) · 표는 양 거래소 한 목록 최신 위 · 110초 진행바 + `{N}s` 숫자 · 미확인 칩/행 `--new-bg` (TRADE-08)"
    - "E3 partial: `Accepted ∧ filledQty>0` → 배지 「부분체결 {filled}/{qty}」. 주문번호 없는 행은 확인 체크 disabled + `title` 「접수 전(주문번호 없음)은 확인할 수 없어요」 + `aria-describedby` (TRADE-08)"
    - "E3 overflow: 칩 줄은 `overflow-x:auto` 가로 스크롤이고, 표는 page 폰 밴드에서 목업의 열 접기 규칙을 그대로 따른다(목업이 정본) · 표 캡션 문장은 wrap (TRADE-08)"
    - "E3 zero-one-many: 0 은 빈 문구 · 1+ 는 칩·행 동일 문법(단수 특별 취급 없음) · 라벨 「VI {N}」 은 숫자형이라 한국어 단/복수 어휘 분기가 없다 (TRADE-08)"
    - "E3 long-text: 종목명은 칩·셀에서 1줄 ellipsis 이고 전체 문자열은 `title` 에 담긴다. 상태 배지·숫자는 고정 어휘 (TRADE-08)"
    - statement: "확인 체크 전송(`ConfirmVIOrderReq 33`)이 거부/타임아웃되면 체크 잠금이 풀리고 행 인라인 `role=\"status\"` 가 기존 `vi-order-list` 문구로 고지한다 (E3 error, TRADE-08)"
      verification: backstop
  artifacts:
    - webapp/src/components/trading/workbench/vi-settings-rows.tsx
    - webapp/src/components/trading/workbench/vi-trigger-strip.tsx
  key_links:
    - "줄의 `exchange` prop ↔ `vi.set` 페이로드의 `exchange` — 이 연결이 끊기면 NXT 줄이 KRX 전략을 덮는다(Pitfall 8)"
    - "`vi-trigger-strip.tsx` ↔ 기존 `vi-order-list.tsx` 의 `isConfirmable` — 확인 활성 판정을 **다시 쓰지 않고** 그 함수를 부른다"
  prohibitions:
    - "MUST NOT `vi.set` 을 고정 거래소로 보내거나 고정 거래소 상수를 한 곳이라도 남긴다 — 한 줄이 다른 거래소 전략을 덮는다"
    - "MUST NOT 확인 체크 활성 판정을 새 조건식으로 다시 적는다 — 표시와 전송 가드가 같은 함수를 부르지 않으면 「비활성인데 눌리면 나가는 확인」이 뚫린다"
    - "MUST NOT 클라가 110/119초를 다시 재거나 VI 표를 재정렬한다 — 서버 계산값을 그대로 쓴다"
---

<objective>
`/trading` 상단의 VI 두 블록을 만든다 — 거래소별 **VI 설정 2줄**(KRX/NXT, on/off·상승률·금액만)과 **VI 발동 스트립/표**(칩 + 「더보기」 표, 확인 체크는 표에서만). 이 둘이 기존 `/trading/vi` 화면을 대체한다.

Purpose: Phase 17 이 `viTriggers{KRX,NXT}` 거래소 축을 이미 만들어 두었고, `RelayViSetMsg.exchange` 도 optional 로 열려 있다. 지금 막고 있는 것은 웹앱 쪽 **고정 거래소 상수 하나**뿐이다. 그 상수를 줄의 prop 으로 바꾸는 것이 TRADE-08 의 전부다.
Output: `components/trading/workbench/vi-settings-rows.tsx` · `vi-trigger-strip.tsx` 신설, `vi-settings-card.tsx` 의 고정 거래소 상수·캡션 제거.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-CONTEXT.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-PATTERNS.md
@.planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html
</context>

<tasks>

<!-- planner-discipline-allow: VI_EDIT_EXCHANGE -->

<task type="auto" tdd="true">
  <name>Task 1: `vi-settings-rows.tsx` — 거래소별 2줄 + 줄의 거래소를 실어 보내는 `vi.set`</name>
  <files>webapp/src/components/trading/workbench/vi-settings-rows.tsx, webapp/src/components/trading/vi-settings-card.tsx, webapp/src/components/trading/__tests__/vi-settings-rows.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/vi-settings-card.tsx (전문 — 특히 `VI_EDIT_EXCHANGE` 상수와 그 소비처 4곳(`:96` 정의 · `:105` 캡션 · `:376` `vi.set` 송신 · `:12` 주석), `VI_ACK_TIMEOUT_MS = 3_000` 규율, 시작/중지 확인 다이얼로그, `AlertSwitch` 마감알림)
    - packages/shared/src/relay.ts (`RelayViSetMsg` — `exchange` 가 이미 optional 이고 「미지정 = KRX」 주석)
    - webapp/src/lib/vi-alert.ts (`manwonToKrw`/`krwToManwon` — 만원↔원 변환은 여기 것만 쓴다)
    - webapp/src/lib/use-relay-socket.ts (`viTriggers` 리듀서와 `viAnyRunning`)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html (VI 설정 2줄 마크업 `:742` · 렌더 `:825-831` · CSS `:301-329`)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §상태줄·VI 설정 2줄 문구표 · §Color 의 Accent/Destructive 허용 목록
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-RESEARCH.md §Pitfall 8
  </read_first>
  <behavior>
    - KRX 줄의 「수정」을 누르면 페이로드의 `exchange` 가 `"KRX"`, NXT 줄에서는 `"NXT"` 로 나간다.
    - `viTriggers` 가 비어 있어도 두 줄이 그려지고 스위치 OFF · 「중지」 · 기본값이 보인다.
    - 스냅샷에 KRX 만 있으면 NXT 줄은 기본값·「중지」이고, KRX 줄만 서버 값을 보인다.
    - KRX 줄의 상승률을 바꿔도 NXT 줄은 더티가 되지 않는다(줄 독립).
    - 상승률만 바꿔도 줄 끝에 「수정」 1개가 뜨고 바뀐 라벨만 `● ` 접두를 갖는다.
    - 공란 입력이면 「수정」이 disabled.
    - 전송 후 에코 전에는 「수정」이 「반영 중…」 + disabled.
    - 거부/타임아웃이면 줄 아래 `role="status"` 문구가 뜨고 더티 값이 남는다.
    - 더티 중 서버 에코가 오면 값이 덮이고 「다른 단말에서 변경됨」 `role="status"` 가 뜬다.
    - 스위치 토글은 확인 다이얼로그를 거친다.
  </behavior>
  <action>
`webapp/src/components/trading/workbench/vi-settings-rows.tsx` 를 신설한다. `ViSettingsRows` 가 KRX·NXT 두 줄을 그리고, 줄 하나를 그리는 내부 컴포넌트가 `exchange: RelayExchange` 를 prop 으로 받는다. 줄 구성은 UI-SPEC §레이아웃 계약 3번 그대로 — 거래소 태그(KRX 테두리형 / NXT 채움형 accent) · on/off 스위치 · 「가동중」(`--up` 빨강) + 「서버 반영 {HH:MM:SS}」 또는 「중지」 · 라벨 「상승률」 `%` · 「금액」 `만원` · 더티 시 줄 끝 「수정」. **계좌 셀렉터와 마감알림 스위치는 줄에 두지 않는다**(계좌는 상태줄, 마감알림은 상태줄로 이관 — UI-SPEC Open Questions Q-1 채택값).

`vi-settings-card.tsx` 에서 고정 거래소 상수를 **정의와 소비처 4곳 모두** 제거한다 — 캡션 문자열, `vi.set` 송신부, 요약행, 파일 머리 주석. 송신부는 줄의 `exchange` prop 을 싣는다. 상수를 한 곳이라도 남기면 NXT 줄이 KRX 전략을 덮는다(Pitfall 8). 기존 파일에서 재사용할 것과 승계할 규율은 명시적으로 옮긴다 — 3초 ack 타임아웃과 「타이머는 잠금을 풀 뿐 **아무것도 다시 보내지 않는다**」 주석, 시작/중지 확인 다이얼로그(Phase 16 D-07), `formFromServer`/더티 판정, 만원↔원 변환(`manwonToKrw`/`krwToManwon` — 인라인 곱셈 금지, 「한 번 더 곱하면 1만 배 주문」). 마감알림(`AlertSwitch`)은 이 파일에서 제거하되 `vi-alert.ts` 의 함수들은 **지우지 않는다**(상태줄이 18-11 에서 가져다 쓴다).

`vi-settings-card.tsx` 자체는 이 태스크에서 **삭제하지 않는다** — 기존 `/trading/vi` 라우트가 18-12 까지 살아 있어야 하므로, 상수를 없애고 필요한 로직을 새 파일로 옮기는 데까지만 한다. 완전 제거는 18-13 이 한다.

스타일은 목업이 정본이다(D-26). 색은 UI-SPEC §Color 의 Accent/Destructive 허용 목록 밖으로 나가지 않는다 — 「가동중」 빨강은 `--up`(가격 방향 축)이지 `--destructive` 가 아니다.

테스트 `__tests__/vi-settings-rows.test.tsx` 는 `<behavior>` 10케이스를 RTL 로 덮는다. 특히 **두 줄의 `vi.set` 페이로드 `exchange` 가 각각 KRX/NXT 로 나가는 것**과 **줄 독립 더티**를 단언한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- vi-settings-rows</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found"(webapp test 는 `--passWithNoTests` 라 필터 오타가 exit 0 으로 통과한다), 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -rq "VI_EDIT_EXCHANGE" webapp/src</automated>
    <fails_when>non-zero exit — 고정 거래소 상수가 아직 남아 있다(소비처 하나만 남아도 NXT 줄이 KRX 를 덮는다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "\* *10000|\* *10_000" webapp/src/components/trading/workbench/vi-settings-rows.tsx</automated>
    <fails_when>non-zero exit — 만원↔원 변환을 인라인으로 하고 있다(`manwonToKrw`/`krwToManwon` 만 쓴다)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn "VI_EDIT_EXCHANGE" webapp/src` 가 0건이다
    - `vi-settings-rows.test.tsx` 가 KRX·NXT 각각의 `vi.set` 페이로드 `exchange` 를 단언한다
    - 줄 독립 더티(한 줄 편집이 다른 줄을 더티로 만들지 않음)가 단언된다
    - 스냅샷이 한 거래소만 있을 때 나머지 줄이 기본값·「중지」로 그려짐이 단언된다
    - 새 파일에 인라인 만원 곱셈이 없다
    - 계좌 셀렉터·마감알림 스위치가 줄 마크업에 없다
  </acceptance_criteria>
  <done>두 거래소의 VI 전략을 한 화면에서 각각 켜고 끄며 값을 바꿀 수 있고, 한 줄의 조작이 다른 줄을 건드리지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `vi-trigger-strip.tsx` — 칩 스트립 + 「더보기」 표(확인 체크는 표에서만)</name>
  <files>webapp/src/components/trading/workbench/vi-trigger-strip.tsx, webapp/src/components/trading/__tests__/vi-trigger-strip.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/vi-order-list.tsx (전문 — `isConfirmable`(:154) 와 그 docstring, 상태 6종 + `Accepted ∧ filledQty>0` 파생 부분체결, `confirm_locked`, 110/119초 진행바, `useNow()` 타이머 1개 선례)
    - webapp/src/components/trading/strategy-badge.tsx (배지 6종 · 거래소 태그)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-workbench-mockup.html (VI 발동 스트립 `:743` · 렌더 `:833-836` · 표 `:744`, `:837-855`)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §VI 발동 스트립·표(문구·열·aria-label·캡션 원문) · §접근성 계약의 VI 확인 체크 행
    - webapp/src/styles/globals.css (`--new-bg`/`--new-bd` — 18-02 가 더한 토큰)
  </read_first>
  <behavior>
    - `viOrders` 0건 → 라벨 「VI 0」 + 「오늘 발동된 VI 주문이 없어요」, 「미확인」 필이 DOM 에 없다.
    - 미확인 1건 → 라벨에 「미확인 1」 필이 뜨고 그 칩·행 배경이 `--new-bg`.
    - 「더보기」 클릭 → 표가 펼쳐지고 확인 체크박스가 표 안에만 존재한다(칩 영역에는 없다).
    - `orderNo` 가 빈 행 → 체크박스 disabled + `title` 「접수 전(주문번호 없음)은 확인할 수 없어요」 + `aria-describedby` 연결.
    - `confirmLocked` 행 → 체크박스 disabled + `title` 「서버가 잠근 확인(confirm_locked)」.
    - 체크 가능한 행을 체크 → 즉시 잠기고 `vi.confirm` 이 1회 나간다. 더티 바가 뜨지 않는다.
    - `Accepted ∧ filledQty>0` → 배지가 「부분체결 {filled}/{qty}」.
    - KRX·NXT 주문이 섞인 배열 → 표가 relay 배열 순서를 그대로 유지한다(클라 재정렬 0회).
    - 110초 진행바가 바 + `{N}s` 숫자를 함께 보인다(색만으로 말하지 않는다).
  </behavior>
  <action>
`webapp/src/components/trading/workbench/vi-trigger-strip.tsx` 를 신설한다. 접힌 상태는 「VI {N}」 + 「미확인 {M}」 필 + 칩 가로 스크롤 한 줄 + 「더보기」 버튼이고, 펼친 상태는 헤더줄(「VI 발동 주문」 + 「VI {N} · 미확인 {M} · 양 거래소 한 목록 · 최신 위」 + 「접기 ▴」) + 표다. 표 본문은 **기존 `ViOrderList` 를 그대로 쓴다** — 확인 체크 활성 판정(`isConfirmable`)·110/119초 진행바·`confirm_locked` 규칙을 다시 쓰지 않는다. `ViOrderList` 가 스트립 안에서 쓰이도록 필요한 최소 prop(거래소 열 표시·컨테이너 클래스)만 추가한다.

칩은 종목명 · 발동가 · 전일대비 · 시각 · 상태 배지(접수/체결/부분체결/취소/거부)로 구성하고, 미확인(`Accepted ∧ !confirmed`)이면 `--new-bg`/`--new-bd` 를 쓴다. 칩 줄은 `overflow-x:auto` 가로 스크롤이며 줄바꿈하지 않는다. 종목명은 1줄 ellipsis 이고 전체는 `title` 에 담는다.

문구는 UI-SPEC §VI 발동 스트립·표 표의 원문을 **그대로** 쓴다(빈 상태 「오늘 발동된 VI 주문이 없어요」, 체크박스 `aria-label` 「{종목명} 주문 확인 — 119초 미확인 취소 면제」, 비활성 `title` 2종, 표 캡션 전문). 새 문구를 짓지 않는다.

「더보기/접기」 토글 상태는 컴포넌트 로컬이고 localStorage 에 저장하지 않는다(D-15 가 정한 키 4개 외에 새 키를 만들지 않는다).

⚠️ 진행바 타이머는 `vi-order-list.tsx` 의 `useNow()` 한 개를 계속 쓴다 — 행마다 타이머를 만들지 않는다.

테스트 `__tests__/vi-trigger-strip.test.tsx` 는 `<behavior>` 9케이스를 RTL 로 덮는다. 「확인 체크가 칩 영역에 존재하지 않는다」와 「relay 배열 순서 보존」을 명시적으로 단언한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- vi-trigger-strip</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- vi-order-list</automated>
    <fails_when>non-zero exit, 또는 요약줄에 "Tests  0 passed" — 기존 확인 체크 규칙 회귀가 났다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ! grep -nE "\.sort\(|reverse\(\)" webapp/src/components/trading/workbench/vi-trigger-strip.tsx</automated>
    <fails_when>non-zero exit — 컴포넌트가 서버가 준 순서를 재정렬하고 있다</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -c "isConfirmable" webapp/src/components/trading/workbench/vi-trigger-strip.tsx webapp/src/components/trading/vi-order-list.tsx</automated>
    <fails_when>`vi-order-list.tsx` 카운트가 0(판정 함수가 사라졌다), 또는 grep 이 non-zero</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, 또는 출력에 "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 확인 체크박스가 표 안에만 렌더되고 칩 영역에는 없음이 테스트로 단언된다
    - 체크 즉시 `vi.confirm` 1회 전송 + 즉시 잠김 + 더티 바 미표시가 단언된다
    - 비활성 사유 `title` 2종과 `aria-describedby` 연결이 단언된다
    - 「부분체결 {filled}/{qty}」 배지가 단언된다
    - 컴포넌트가 배열을 재정렬하지 않는다
    - 기존 `vi-order-list` 테스트가 전부 통과한다(회귀 없음)
  </acceptance_criteria>
  <done>VI 발동 현황을 한 줄로 훑고 「더보기」로 확인까지 끝낼 수 있으며, 확인 규칙은 기존 판정 함수 한 곳에서만 나온다.</done>
</task>

<task type="auto">
  <name>Task 3: 두 블록 통합 확인 — 거래소 축 왕복과 빈/부분 상태</name>
  <files>webapp/src/components/trading/__tests__/vi-settings-rows.test.tsx, webapp/src/components/trading/__tests__/vi-trigger-strip.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/workbench/vi-settings-rows.tsx (Task 1 결과)
    - webapp/src/components/trading/workbench/vi-trigger-strip.tsx (Task 2 결과)
    - webapp/src/components/trading/__tests__/vi-settings-card.test.tsx (기존 VI 테스트가 무엇을 덮고 있었는지 — 대체 시 잃는 커버리지 확인)
    - .planning/phases/18-gh-trade-ui-nxt-vi/18-UI-SPEC.md §UI Considerations 의 E2·E3 행
  </read_first>
  <action>
기존 `vi-settings-card.test.tsx` 가 덮던 항목 중 **2줄 구조로 오면서 사라진 커버리지가 없는지** 대조하고, 빠진 것을 새 두 테스트 파일에 옮겨 담는다. 특히 확인해야 할 것: 주문금액 상한 가드(Phase 16 WR-07 가 zod·envelope·UI 세 층에 통일한 것), 시작/중지 확인 다이얼로그 취소 경로, 3초 ack 타임아웃이 **아무것도 재전송하지 않는다**는 단언.

그리고 UI-SPEC §UI Considerations 의 E2(empty/loading/error/partial)·E3(empty/loading/populated/partial/overflow/zero-one-many/long-text) 행 중 아직 단언되지 않은 상태를 채운다 — 특히 ① 73 스냅샷 전 빈 상태에 스피너·스켈레톤이 **없다**는 것 ② 긴 종목명이 1줄 ellipsis + `title` 이라는 것 ③ 「신규/미확인」이 색 **+ 배지 텍스트**로 말한다는 것(색만으로 말하지 않는다).

E3 error(확인 체크 전송 거부/타임아웃 시 잠금 해제 + 행 인라인 고지)는 **실 게이트웨이 왕복이 있어야 확실해지는 항목**이므로 mock 수준 단언까지만 하고, 그 한계를 SUMMARY 에 「backstop — 실기 UAT 로 확인」으로 기록한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test -- vi-</automated>
    <fails_when>non-zero exit, 또는 출력에 "No test files found", 또는 요약줄에 "Tests  0 passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp test</automated>
    <fails_when>non-zero exit, 또는 통과 수가 Phase 17 기준선 1008 보다 작다</fails_when>
    <human-check>VI 설정 2줄과 발동 스트립을 dev 화면에서 열어, 목업 `18-workbench-mockup.html` 의 VI 영역과 배치·문구·색이 일치하는지 눈으로 대조한다(목업이 정본 — D-26).</human-check>
  </verify>
  <acceptance_criteria>
    - 기존 `vi-settings-card.test.tsx` 가 덮던 항목 중 새 파일로 옮겨지지 않은 것이 SUMMARY 에 목록으로 없다(= 빠진 것 0)
    - 주문금액 상한 가드 · 확인 다이얼로그 취소 경로 · 3초 타임아웃 무재전송이 새 테스트에 있다
    - E2·E3 의 empty/loading/partial 상태가 「스피너 없음」까지 포함해 단언된다
    - `pnpm --filter @gh-radar/webapp test` 전량 통과 수가 1008 이상
  </acceptance_criteria>
  <done>거래소 축이 두 블록 전체에서 왕복하고, 기존 VI 화면이 갖고 있던 안전 단언을 하나도 잃지 않았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → relay wss (`vi.set` / `vi.confirm`) | 전략 설정과 확인이 여기서 나간다 |
| relay → 브라우저 (72/73 에코) | VI 주문 상태·`confirm_locked` 가 여기서 들어온다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-20 | Tampering | `vi.set` 거래소 축 | high | mitigate | 줄의 `exchange` prop 을 명시 송신. 고정 상수 0건을 verify 가 grep 으로 단언 |
| T-18-21 | Tampering | 확인 체크 오활성 | high | mitigate | 표시 `disabled` 와 전송 가드가 **같은** `isConfirmable` 을 부른다. 새 조건식 금지 |
| T-18-22 | Tampering | 만원↔원 변환 | high | mitigate | `manwonToKrw`/`krwToManwon` 만 사용. 인라인 곱셈 0건을 verify 가 단언(「한 번 더 곱하면 1만 배 주문」) |
| T-18-23 | Spoofing | `message` 표시 | medium | mitigate | 서버 문구는 표시만 하고 파싱해 분기하지 않는다 |
| T-18-24 | Denial of Service | 행마다 진행바 타이머 | low | mitigate | 기존 `useNow()` 타이머 1개를 계속 쓴다 |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 phase 는 새 패키지를 설치하지 않는다(RESEARCH §Package Legitimacy Audit 표 비어 있음) |
</threat_model>

## Artifacts this phase produces

새로 만드는 심볼·파일 (phase 전체 — 드리프트 검증에서 제외되어야 한다):

- **마이그레이션:** `supabase/migrations/20260921120000_dma_orders_modify_offhours.sql` · 컬럼 `dma_orders.piece_count` · `dma_orders.krx_session`
- **shared:** `RelayOrderModifyMsg` · `RelayKrxSession` · `OrderType` 의 `"M"` 멤버 · `RelayOrderNewMsg.pieceCount?` · `RelayOrderNewMsg.krxSession?` · `RelayRateCrossItem.name?` · `RelayRateCrossItem.code?`
- **relay:** `RelayOrderModifySchema` · `OrderInsertRow.pieceCount?` · `OrderInsertRow.krxSession?` · 돌파 항목 보강 private 메서드 · `relay/src/ws/__tests__/protocol.test.ts`
- **webapp lib:** `RelayOrderRequest.kind` 의 `"modify"` · `queued-window.ts`(`OrderButtonMode`·`ManualOrderAffordance`·`affordanceOf`) · `breakout-list.ts`(`kstDateKey`·`shouldRemoveBreakout`·`breakoutRowsFrom`·`BreakoutRow`·키 상수 4종·`REMOVE_MARGIN_PCT`·`ARM_GRACE_MS`·`HIGHLIGHT_MS`) · `use-breakout-quotes.ts`(`useBreakoutQuotes`·`MAX_BREAKOUT_SUBS`) · `alert-tone.ts`(`playBreakoutTone`·`isTonePlaybackBlocked`·`resumeToneContext`) · `limit-chaser.ts` 로 이동한 `parseStrategyKey`
- **webapp 컴포넌트:** `components/trading/workbench/` 의 `TradingWorkbench`·`WorkbenchStatusBar`·`ViSettingsRows`·`ViTriggerStrip`·`BreakoutStrip`·`StockAddBar`·`CardGrid`·`SharedPanels` · `components/trading/card/` 의 `StrategyCard`·`CardHeader`·`QuoteGrid10`·`CardBody`·`ManualOrderForm`·`StockInfoModal`
- **라우트:** `webapp/src/app/trading/page.tsx`
- **CSS 토큰:** `--new-bg` · `--new-bd` (globals.css §9 라이트/다크 양쪽)
- **e2e:** `webapp/e2e/specs/trading-workbench.spec.ts`

<verification>
- `pnpm --filter @gh-radar/webapp test` 전량 exit 0 (통과 수 1008 이상)
- `pnpm --filter @gh-radar/webapp run typecheck` exit 0
- `grep -rn "VI_EDIT_EXCHANGE" webapp/src` 0건
</verification>

<success_criteria>
- VI 설정이 거래소별 2줄이고 각 줄이 자기 거래소를 실어 보낸다
- VI 발동 스트립/표가 기존 VI 주문내역 화면의 확인 규칙을 한 글자도 완화하지 않은 채 대체한다
- 두 블록의 빈·로딩·부분 상태가 스피너 없이 정직하게 그려진다
</success_criteria>

<output>
Create `.planning/phases/18-gh-trade-ui-nxt-vi/18-05-SUMMARY.md` when done
</output>