---
phase: 21-gh-trade-mobile-app
plan: 18
type: execute
wave: 8
depends_on: ["21-16"]
files_modified:
  - webapp/src/components/providers/theme-provider.tsx
  - webapp/src/components/layout/__tests__/app-shell-chrome.test.tsx
  - webapp/e2e/specs/theme-default.spec.ts
  - mobile/ios/App/App/ThemeStore.swift
  - mobile/android/app/src/main/java/com/ghtrade/app/ThemeStore.kt
  - mobile/www/index.html
autonomous: true
gap_closure: true
requirements: [MOBILE-01]
coupling_justified:
  - "21-20: 21-20 이 iOS 사전 로드 자리값(GHTradeBridgeViewController.currentTheme · GHTradeTabBar 초기 팔레트)을 dark 로 바꾼다 — 둘 다 capacitorDidLoad 의 ThemeStore.load() 가 첫 렌더 전에 덮으므로 순서 무관"
  - "21-21: 21-21 이 Android 사전 로드 자리값(MainActivity.currentTheme · GhTradeTabBar 초기 팔레트)을 dark 로 바꾼다 — load() 의 applyTheme(ThemeStore.load(this)) 가 첫 프레임 전에 덮으므로 순서 무관"
  - "21-17: 두 플랜 모두 cap sync · assembleDebug 를 쓴다 — parallelization=false 순차 실행 · 빌드는 운영 sync 기준"

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "G-21-N1 · D-23a: 저장된 테마가 없는 첫 방문·첫 실행의 기본 테마는 다크다 — 브라우저(next-themes defaultTheme) · iOS(ThemeStore.load) · Android(ThemeStore.load) · 오프라인 폴백(?theme 미지정) 모두 같은 값"
    - "웹 첫 페인트(next-themes 인라인 스크립트가 붙이는 <html class=dark>)와 네이티브 첫 프레임(창·WebView 배경 #17171c · 상태바 밝은 아이콘 · 탭바 다크 팔레트)이 같다 — 앱 첫 실행에 흰 번쩍임이 없다(D-23 동기화 전제 유지)"
    - "저장값이 있는 사용자는 그대로다 — 웹 localStorage theme=light · iOS UserDefaults gh-trade.theme=light · Android SharedPreferences gh_trade/theme=light 는 계속 라이트"
    - "기본값은 저장되지 않는다 — localStorage theme 은 사용자가 토글할 때만 생긴다(나중에 기본값을 바꿔도 사용자 선택과 섞이지 않는다)"
    - "앱 첫 실행(저장값 없음) 웹은 네이티브에 theme {theme:'dark'} 를 보낸다 — 네이티브 저장값이 웹과 같은 다크로 시작한다"
  artifacts:
    - path: webapp/src/components/providers/theme-provider.tsx
      provides: "next-themes defaultTheme dark · enableSystem false"
      contains: "defaultTheme=\"dark\""
    - path: webapp/e2e/specs/theme-default.spec.ts
      provides: "저장값 없음 다크 · 저장값 유지 · 앱 첫 theme 메시지 · 오프라인 폴백 기본 다크 e2e"
      min_lines: 60
    - path: mobile/ios/App/App/ThemeStore.swift
      provides: "iOS 미저장 기본 .dark"
      contains: "?? .dark"
    - path: mobile/android/app/src/main/java/com/ghtrade/app/ThemeStore.kt
      provides: "Android 미저장 기본 dark"
      contains: "getString(KEY, \"dark\")"
    - path: mobile/www/index.html
      provides: "오프라인 폴백 — ?theme=light 가 아니면 다크"
      contains: "!== 'light'"
  key_links:
    - from: webapp/src/components/providers/theme-provider.tsx
      to: webapp/src/lib/native/native-bridge-provider.tsx
      via: "resolvedTheme(기본 dark) → postNative('theme', {theme}) → 네이티브 ThemeStore.save"
      pattern: "defaultTheme"
    - from: mobile/ios/App/App/ThemeStore.swift
      to: mobile/ios/App/App/SceneDelegate.swift
      via: "첫 프레임 창 배경 = GHTradePalette.of(ThemeStore.load()).bg"
      pattern: "ThemeStore\\.load"
  prohibitions:
    - "MUST NOT 사용자가 저장한 테마(웹 localStorage theme · iOS gh-trade.theme · Android gh_trade/theme)를 새 기본값으로 덮거나 무시한다"
    - "MUST NOT 웹·iOS·Android·오프라인 폴백 중 한 곳만 기본값을 바꿔 앱 첫 프레임과 웹 첫 페인트가 어긋나게 한다(라이트 → 다크 번쩍임)"
    - "MUST NOT 기본값을 저장소에 써서 사용자 선택처럼 보이게 만든다"
    - "MUST NOT 앱·웹 테마가 OS 다크모드를 따르게 한다(D-23 · enableSystem=false 유지)"
---

<objective>
G-21-N1: 저장된 테마가 없을 때의 기본 테마를 **다크**로 바꾼다 — 브라우저와 앱 공통(21-UAT.md G-21-N1 사용자 결정 2026-09-26 · CONTEXT D-23a 는 21-20 이 기록). 웹 `next-themes` 기본값 · iOS `ThemeStore` · Android `ThemeStore` · 오프라인 폴백 페이지 기본 경로를 **함께** 바꿔 네이티브 첫 프레임과 웹 첫 페인트를 맞춘다(D-23).

Purpose: 트레이더 대부분이 다크로 쓰는 시세 앱의 첫인상을 다크로 맞추되, 이미 고른 사람의 선택은 절대 건드리지 않는다.
Output: 기본값 4곳 · 단위 테스트 기대값 · e2e 신규 1(저장값 없음/있음 · 앱 첫 theme 메시지 · 오프라인 폴백).

범위 밖(사용자 결정 재개 금지): 스플래시(OS 런치 화면)는 D-22 대로 OS 외형을 따른다 — 앱 저장값을 읽을 수 없는 시점이다. 사전 로드 자리값(VC · Activity · 탭바 초기 팔레트)의 dark 전환은 탭바 파일을 소유한 21-20 · 21-21 이 한다.

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add · 커밋 직전 `git status -sb` · **push 금지**(21-24 체크포인트에서 사용자 결정 — 웹 기본값 변경은 push 즉시 모든 새 방문자에게 적용된다).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-UAT.md
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@webapp/src/components/providers/theme-provider.tsx
@webapp/src/components/layout/__tests__/app-shell-chrome.test.tsx
@webapp/src/lib/native/native-bridge-provider.tsx
@webapp/e2e/fixtures/native-app.ts
@webapp/e2e/specs/auth-guards.spec.ts
@mobile/ios/App/App/ThemeStore.swift
@mobile/android/app/src/main/java/com/ghtrade/app/ThemeStore.kt
@mobile/www/index.html

<interfaces>
```text
next-themes: storageKey 기본 'theme' (ThemeProvider 는 storageKey 를 넘기지 않는다) · 저장값이 있으면 항상 저장값 우선
NativeBridgeProvider: 마운트·변경 시 postNative('theme', { theme: resolvedTheme === 'dark' ? 'dark' : 'light' })
e2e fixtures/native-app.ts: installNativeApp(page, { platform?: 'ios'|'android' }) · nativeMessages(page) → { type, payload }[]
e2e 비로그인 컨텍스트 선례: auth-guards.spec.ts 파일 레벨 test.use({ storageState: { cookies: [], origins: [] } })
e2e 저장소 루트 경로 선례: fixtures/relay.ts path.resolve(__dirname, '../../..')
iOS: ThemeStore.key = "gh-trade.theme" · load() → GHTradeTheme · SceneDelegate 창 배경 · capacitorDidLoad applyTheme(ThemeStore.load())
Android: ThemeStore PREFS "gh_trade" / KEY "theme" · MainActivity.load() → applyTheme(ThemeStore.load(this))
오프라인 폴백: 네이티브가 항상 ?to=…&theme=<현재> 를 붙인다 — 기본 경로는 theme 이 없거나 모르는 값일 때만 탄다
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 웹 기본 다크(첫 페인트 · 저장값 우선 · 앱 첫 theme 메시지 e2e) + iOS ThemeStore 기본 .dark</name>
  <files>webapp/src/components/providers/theme-provider.tsx, webapp/src/components/layout/__tests__/app-shell-chrome.test.tsx, webapp/e2e/specs/theme-default.spec.ts, mobile/ios/App/App/ThemeStore.swift</files>
  <read_first>
    - webapp/src/components/providers/theme-provider.tsx (기본값 주석 ★ — 저장값 우선 설명)
    - webapp/src/components/layout/__tests__/app-shell-chrome.test.tsx (49~58행 defaultTheme 단언)
    - webapp/src/lib/native/native-bridge-provider.tsx (200행 theme 송신)
    - webapp/e2e/fixtures/native-app.ts · webapp/e2e/specs/auth-guards.spec.ts (비로그인 컨텍스트 선례)
    - mobile/ios/App/App/ThemeStore.swift · mobile/ios/App/App/SceneDelegate.swift (첫 프레임 창 배경)
  </read_first>
  <action>
    ① `theme-provider.tsx`: `NextThemesProvider` 의 defaultTheme 값을 `"dark"` 로 바꾼다(`attribute="class"` · `enableSystem={false}` · `disableTransitionOnChange` 유지). 머리 주석을 「Light/Dark 2 상태 · 기본값 dark(D-23a — iOS/Android ThemeStore 미저장 기본과 반드시 같아야 한다, 한 곳만 바꾸면 앱 첫 프레임과 웹 첫 페인트가 어긋난다)」로 고치고 ★ 저장값 우선 설명은 그대로 둔다.
    ② `app-shell-chrome.test.tsx`: 49행 테스트 이름을 「`defaultTheme="dark"` 로 next-themes 를 부른다」로, 58행 기대값을 `'dark'` 로. `enableSystem` 이 false 인지 단언이 없으면 같은 테스트에 `expect(props.enableSystem).toBe(false)` 를 더한다(D-23 OS 무시 계약).
    ③ `webapp/e2e/specs/theme-default.spec.ts`(신규): 파일 레벨 `test.use({ storageState: { cookies: [], origins: [] } })`(비로그인 · 저장값 없는 새 방문자). 머리 주석: 무엇을 증명하는가(D-23a · G-21-N1) · 깨지면 사용자가 겪는 일(새 사용자가 라이트로 시작 / 라이트를 고른 사용자가 다크로 바뀜). 테스트:
      - 「저장값 없음 → /login 첫 페인트부터 다크」: `page.addInitScript` 로 `DOMContentLoaded` 시점(once) `document.documentElement.className` 을 `window.__firstHtmlClass` 에 기록 → `page.goto('/login')` → `html` 이 `/(^|\s)dark(\s|$)/` 클래스 · `__firstHtmlClass` 에 `dark` 포함(하이드레이션 뒤 전환이 아니라 첫 페인트) · `localStorage.getItem('theme')` 이 `null`(기본값은 저장되지 않는다).
      - 「저장값 light 는 유지」: `addInitScript` 로 `localStorage.setItem('theme','light')` → `/login` → `html` 에 `dark` 클래스 없음 · `__firstHtmlClass` 에 `dark` 없음 · 저장값이 여전히 `light`.
      - 「앱 첫 실행 → theme {theme:'dark'} 송신」: `installNativeApp(page)` → `/login` → `expect.poll(() => nativeMessages(page))` 로 `type === 'theme'` 메시지가 나타나고 첫 theme 페이로드가 `{ theme: 'dark' }`.
    ④ `mobile/ios/App/App/ThemeStore.swift`: `load()` 의 미저장·모르는 값 폴백을 `.dark` 로. 주석 「저장값이 없거나 모르는 값이면 웹 기본과 같은 dark(D-23a)」. `key` · `save` 는 그대로.
    ⑤ 확인: vitest(②) · Playwright(③ — 설정의 webServer 가 :3100 을 재사용하거나 띄운다) · `native:build:ios`.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/layout/__tests__/app-shell-chrome.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/theme-default.spec.ts</automated>
    <fails_when>non-zero exit, or the summary contains "failed" or "skipped", or no summary line matches "[0-9]+ passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:ios</automated>
    <fails_when>non-zero exit, or output lacks "BUILD SUCCEEDED" or "SIM ENTITLEMENTS OK"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'defaultTheme="dark"' webapp/src/components/providers/theme-provider.tsx` ≥ 1 · `grep -c 'enableSystem={false}' webapp/src/components/providers/theme-provider.tsx` ≥ 1
    - 옛 기본값 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' webapp/src/components/providers/theme-provider.tsx | grep -c 'defaultTheme="light"')" -eq 0`
    - `grep -c "toBe('dark')" webapp/src/components/layout/__tests__/app-shell-chrome.test.tsx` ≥ 1
    - theme-default.spec.ts: `grep -c "origins: \[\]"` ≥ 1 · `grep -c "__firstHtmlClass"` ≥ 1 · `grep -c "installNativeApp"` ≥ 1 · `grep -cE "setItem\('theme', *'light'\)"` ≥ 1
    - `grep -c '?? .dark' mobile/ios/App/App/ThemeStore.swift` ≥ 1
    - iOS 옛 폴백 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/ios/App/App/ThemeStore.swift | grep -c '?? .light')" -eq 0`
    - verify 세 명령 통과 — theme-default.spec.ts 의 세 테스트가 목록 출력에 모두 ✓ 로 나온다(스킵 0)
  </acceptance_criteria>
  <done>새 방문자·앱 첫 실행은 첫 페인트부터 다크이고 네이티브에 dark 를 보낸다. 라이트를 저장한 사용자는 라이트다. iOS 미저장 기본도 다크다.</done>
</task>

<task type="auto">
  <name>Task 2: Android ThemeStore 기본 dark + 오프라인 폴백 기본 다크(파일 URL e2e)</name>
  <files>mobile/android/app/src/main/java/com/ghtrade/app/ThemeStore.kt, mobile/www/index.html, webapp/e2e/specs/theme-default.spec.ts</files>
  <read_first>
    - mobile/android/app/src/main/java/com/ghtrade/app/ThemeStore.kt (load 정규화 · 주석 8행)
    - mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt (load() 의 applyTheme(ThemeStore.load(this)) · applyTheme 정규화 — 이 플랜은 바꾸지 않는다)
    - mobile/www/index.html (7~12행 첫 페인트 스크립트 · :root / :root.dark 토큰 · 5초 탐침 스크립트)
    - webapp/e2e/specs/theme-default.spec.ts (Task 1)
  </read_first>
  <action>
    ① `ThemeStore.kt`: `load()` 의 SharedPreferences 기본 인자를 `"dark"` 로, 정규화를 「저장값이 정확히 `"light"` 일 때만 light, 그 외(미저장·모르는 값) dark」로 뒤집는다. KDoc 을 「값은 "dark" · "light" 둘뿐이고 그 외·미저장은 다크(웹 기본값 · D-23a)」로 고친다. `save()` 는 그대로(검증된 값만 들어온다).
    ② `mobile/www/index.html`: 첫 페인트 스크립트를 「`?theme` 이 `light` 가 아니면 `dark` 클래스를 붙인다」(`!== 'light'`)로 뒤집고 주석을 「기본 다크(D-23a) — 네이티브는 항상 현재 테마를 넘기며, 이 기본값은 값이 없거나 모를 때만 쓴다」로 고친다. CSS 토큰·탐침 스크립트·허용 출처(T-21-06)는 그대로.
    ③ `theme-default.spec.ts` 에 describe 「오프라인 폴백 기본 다크(D-19 · D-23a)」 추가: 저장소 루트 = `path.resolve(__dirname, '../../..')`, 파일 URL = `pathToFileURL(<루트>/mobile/www/index.html)`. 각 테스트 시작에 `page.context().setOffline(true)`(폴백의 도달 탐침이 페이지를 떠나지 않게). 케이스: 쿼리 없음 → `html` 에 `dark` · `?theme=light` → `dark` 없음 · `?theme=dark` → `dark` · `?theme=bogus` → `dark`.
    ④ 확인: Playwright 전체 spec(Task 1 의 3 + 오프라인 4) · `native:build:android`.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/theme-default.spec.ts</automated>
    <fails_when>non-zero exit, or the summary contains "failed" or "skipped", or no summary line matches "[0-9]+ passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:android</automated>
    <fails_when>non-zero exit, or output lacks "BUILD SUCCESSFUL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getString(KEY, "dark")' mobile/android/app/src/main/java/com/ghtrade/app/ThemeStore.kt` ≥ 1
    - Android 옛 기본 인자 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/android/app/src/main/java/com/ghtrade/app/ThemeStore.kt | grep -c 'getString(KEY, "light")')" -eq 0`
    - Android 옛 정규화 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/android/app/src/main/java/com/ghtrade/app/ThemeStore.kt | grep -c 'if (it == "dark")')" -eq 0`
    - `grep -c "!== 'light'" mobile/www/index.html` ≥ 1
    - 폴백 옛 분기 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*|<!--)' mobile/www/index.html | grep -c "get('theme') === 'dark'")" -eq 0`
    - `grep -c "pathToFileURL" webapp/e2e/specs/theme-default.spec.ts` ≥ 1 · `grep -c "setOffline(true)" webapp/e2e/specs/theme-default.spec.ts` ≥ 1
    - `git diff --quiet -- mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` exit 0 (21-21 소유 파일 미변경)
    - verify 두 명령 통과 — theme-default.spec.ts 의 일곱 테스트(Task 1 셋 + 오프라인 넷)가 목록 출력에 모두 ✓ 로 나온다(스킵 0)
  </acceptance_criteria>
  <done>Android 미저장 기본과 오프라인 폴백 기본도 다크다 — 네 곳의 기본값이 같아졌다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 저장 선택 → 테마 해석 | localStorage · UserDefaults · SharedPreferences 의 사용자 값이 기본값보다 우선해야 한다 |
| 웹 → 네이티브 theme 메시지 | 기존 T-21-02 계약(dark/light 두 값만) |
| 네이티브 → 오프라인 폴백 쿼리 | `?theme=` 은 네이티브가 넣는 값 — 모르는 값은 기본으로 떨어진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-49 | Tampering | 사용자 테마 선호 무결성 | medium | mitigate | 기본값만 바꾸고 저장값 읽기 경로는 그대로 · e2e 「저장값 light 는 유지」 · 기본값을 저장하지 않음 단언(Task 1 ③) |
| T-21-53 | Denial of Service | 첫 프레임 불일치 번쩍임 | low | mitigate | 웹·iOS·Android·폴백 기본값을 한 플랜에서 함께 변경 · e2e 첫 페인트 클래스 · 앱 첫 theme 메시지 단언 · 21-24 UAT 항목 2 |
| T-21-02 | Spoofing | theme 메시지 | low | mitigate | 기존 화이트리스트(dark/light) 무변경 — 이 플랜은 송신 경로를 바꾸지 않는다 |
| T-21-06 | Tampering | 오프라인 폴백 복귀 대상 | low | mitigate | 허용 출처 목록 무변경(Task 2 ②) |
</threat_model>

<verification>
- vitest app-shell-chrome · Playwright theme-default(7) · iOS/Android 빌드 통과
- 사람 확인(새로 설치한 앱 첫 화면 다크 · 흰 번쩍임 없음 · 라이트 저장 유지)은 21-24 UAT 항목 2
</verification>

<success_criteria>
- G-21-N1: 저장값이 없을 때 웹·앱이 같은 다크로 시작하고, 저장값이 있는 사용자는 변화가 없다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| Android `ExternalLinks.opensInAppBrowser(scheme, host, appHost)` · `ExternalLinksTest`(정본 26케이스) | Kotlin 순수 함수 · JUnit | 21-17 |
| Android `InAppBrowser.open(activity, uri, theme)` · `GhTradeWebViewClient.shouldOverrideUrlLoading` 가로채기 | Kotlin | 21-17 |
| `mobile/android/variables.gradle` `androidxBrowserVersion` · `app/build.gradle` androidx.browser 선언 (21-17 Task 1 선택 a 일 때만) | Gradle | 21-17 |
| `mobile/package.json` `native:test:android`(TabRoutesTest + ExternalLinksTest) · `native:check-external-links:ios` | 스크립트 | 21-17 · 21-22 |
| 웹 `ThemeProvider` 기본 다크 · iOS/Android `ThemeStore` 기본 다크 · `mobile/www/index.html` 기본 다크 | 설정 | 21-18 |
| e2e `webapp/e2e/specs/theme-default.spec.ts` (저장값 없음 다크 · 저장값 유지 · 앱 첫 theme 메시지 · 오프라인 폴백) | Playwright | 21-18 |
| 웹 `THEME_SWITCH_LABEL` · `ThemeSwitchIcon` (theme-toggle.tsx export) — 사이드바 ThemeToggle · AccountCard 공용 목적지 규칙 | 컴포넌트·상수 | 21-19 |
| 단위 `webapp/src/components/layout/__tests__/theme-toggle.test.tsx` | Vitest | 21-19 |
| iOS `GHTradeTabBar` 캡슐 C(라벨 없음 · 56×36 16% · 유리 · 두 겹 그림자 · 높이 60 · 페이드 86) · `GHTradePalette.glass` · `glassAlpha` | Swift | 21-20 |
| CSS `--native-tabbar-offset` = gap + 60 + 8 · `--native-body-reserve` 98 · native-shell e2e 수치(82 · 98 · 74) | 토큰 · Playwright | 21-20 |
| CONTEXT D-27b · D-23a · D-08b · D-28 | 결정 기록 | 21-20 |
| Android `GhTradeTabBar` 캡슐 C 근사(라벨 없음 · 불투명 유리 · elevation 12) · `GhTradePalette.glass` · 탭바 60dp · 페이드 86dp | Kotlin | 21-21 |
| iOS `ExternalLinks.opensInAppBrowser(scheme:host:appHost:)` · `isSameAppHost` · `NavigationDelegateProxy`(decidePolicyFor · WKUIDelegate createWebViewWith · SFSafariViewController) · `mobile/scripts/external-links-check.swift` · `check-external-links-ios.sh` | Swift · 스크립트 | 21-22 |
| 갭 클로징 전 게이트 기록 · push 전 UAT 환경 (21-23-SUMMARY) · UAT 재검증 결과 · push 결정과 이행 (21-24-SUMMARY) | 문서 | 21-23 · 21-24 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-18-SUMMARY.md` when done — 바꾼 기본값 4곳 · e2e 결과(테스트 이름별) · 빌드 결과를 적는다.
</output>
