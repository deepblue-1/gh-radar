---
phase: 21-gh-trade-mobile-app
plan: 11
type: execute
wave: 4
depends_on: ["21-10"]
files_modified:
  - mobile/ios/App/App/GHTradeBridgeViewController.swift
  - mobile/ios/App/App/NavigationDelegateProxy.swift
  - mobile/ios/App/App/ThemeStore.swift
  - mobile/ios/App/App/SceneDelegate.swift
  - mobile/ios/App/App/Info.plist
  - mobile/ios/App/App.xcodeproj/project.pbxproj
  - mobile/www/index.html
autonomous: true
requirements: [MOBILE-01]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-04 · D-17: iOS 당겨서 새로고침 = `UIRefreshControl`(WebView scrollView `bounces = true` 복원 — Capacitor 가 false 로 둔다) → `window.__ghTrade&&window.__ghTrade.refresh?window.__ghTrade.refresh():location.reload()` evaluate → 훅 완료와 무관하게 **1초 뒤** 스피너 종료 · 오버레이 열림·오프라인 페이지 동안 새로고침 비활성(Pitfall 7)"
    - "D-23: 웹 `theme {theme}` 메시지를 받으면 상태바 스타일(다크 → light content · 라이트 → dark content, `preferredStatusBarStyle` 오버라이드 — SystemBars 가 덮지 못하게) · `overrideUserInterfaceStyle` · WebView/스크롤/창 배경(라이트 `#ffffff` · 다크 `#17171c`) · 탭바·페이드 팔레트를 바꾸고 UserDefaults `gh-trade.theme` 에 저장한다 — 다음 실행 첫 프레임(SceneDelegate 창 배경 · capacitorDidLoad)부터 저장값 적용 · OS 다크모드는 보지 않는다"
    - "D-19: 첫 로드·이동 실패 중 **네트워크 오류 코드만**(NSURLErrorDomain −1009 · −1001 · −1003 · −1004 · −1005 · −1020, −999 취소 제외) 앱 내장 폴백 `capacitor://localhost/index.html?to=<원래 URL>&theme=<현재>` 를 띄운다 — Capacitor 오류 경로 설정은 쓰지 않고, 원래 내비게이션 델리게이트의 다른 콜백은 전달형 프록시로 그대로 전달된다"
    - "D-19: 폴백 페이지 = GH Trade 워드마크 · 「인터넷 연결을 확인해주세요」 · 「복구되면 자동으로 이동합니다」 · 「다시 시도」 · `online` 이벤트 + 5초 주기 실제 도달 탐침(no-cors fetch) 성공 시에만 복귀 · 복귀 대상은 `?to=` 의 출처가 허용 목록(`https://trade.jx1.io` · `http://localhost:3100`)일 때만 그 URL, 아니면 `https://trade.jx1.io/` (오픈 리다이렉트 방지) · `theme` 쿼리로 라이트/다크 · 탭바 숨김(D-12 ② — 호스트 규칙)"
    - "D-24: 폰 세로 고정 · iPad 4방향 — `supportedInterfaceOrientations` 를 idiom 으로 오버라이드(Pitfall 9) + Info.plist `UISupportedInterfaceOrientations` = Portrait · `~ipad` = 4방향 · `UIRequiresFullScreen` 없음 · `TARGETED_DEVICE_FAMILY = 1,2`"
    - "D-20: `CFBundleDisplayName` = `GH Trade` · 배포 타깃 iOS 15 이상(Claude's Discretion)"
  artifacts:
    - path: mobile/ios/App/App/NavigationDelegateProxy.swift
      provides: "Capacitor 델리게이트 전달 + 네트워크 오류 필터"
      contains: "forwardingTarget"
    - path: mobile/ios/App/App/ThemeStore.swift
      provides: "UserDefaults gh-trade.theme load/save"
      contains: "gh-trade.theme"
    - path: mobile/www/index.html
      provides: "오프라인 폴백(자동 복구 · safeTarget · 테마)"
      contains: "safeTarget"
  key_links:
    - from: mobile/ios/App/App/NavigationDelegateProxy.swift
      to: mobile/ios/App/App/GHTradeBridgeViewController.swift
      via: "owner?.showOffline(failedURL:)"
      pattern: "showOffline"
    - from: mobile/ios/App/App/GHTradeBridgeViewController.swift
      to: webapp/src/lib/native/native-bridge-provider.tsx
      via: "evaluateJavaScript(refresh 훅) · theme 메시지 수신"
      pattern: "__ghTrade.refresh"
---

<assumption_delta_decision>
- 검출기 `assumption-delta scan 21` → `detected: false`. 수동 판정: 두 번째 클라이언트 플랫폼 — 정본 명사 **웹앱**, 결정 **add-alongside**. 테마의 정본은 웹(next-themes)이고 네이티브는 따라가며 마지막 값만 저장한다(D-23 · 21-01 블록 참조).
</assumption_delta_decision>

<objective>
iOS 셸의 시스템 연동을 끝낸다: 당겨서 새로고침(D-04·D-17) · 테마 추종과 첫 프레임 저장값(D-23) · 네트워크 오류 전용 오프라인 폴백과 자동 복구(D-19) · 방향(D-24) · 표시명(D-20).

Purpose: 앱이 웹처럼 보이지 않게 하는 마지막 층 — 당겨서 갱신, 다크 전환 시 상태바·배경 깜빡임 없음, 비행기 모드에서 흰 화면 대신 복구 화면.
Output: VC 확장 + 델리게이트 프록시 + 테마 저장소 + Info.plist·pbxproj + 폴백 페이지 완성.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md
@.planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md
@mobile/ios/App/App/GHTradeBridgeViewController.swift
@mobile/ios/App/App/GHTradeTheme.swift
@mobile/www/index.html

iOS 빌드는 Bash timeout 600000. 새 Swift 파일은 21-01 SUMMARY 의 방식으로 Xcode 프로젝트에 등록한다.

<interfaces>
21-10 계약:

```swift
enum GHTradeTheme: String { case light, dark } · struct GHTradePalette { card, bg, primary, muted, line; static func of(_:) }
final class GHTradeTabBar: UIView { func apply(_ p: GHTradePalette); let fadeView: GHTradeFadeView }
// VC: var currentTheme · var overlayOpen · private(set) var isOfflinePage · func updateTabBarVisibility(animated:) · handleURL(_:)
```

Capacitor 8.5.2 사실(RESEARCH):

```text
CAPBridgeViewController: loadView() final · view == webView · scrollView.bounces = false(:301) · 기본 contentInsetAdjustmentBehavior .never
webView.navigationDelegate = Capacitor 의 WebViewDelegationHandler (bridge 가 강하게 보유) — 교체 대신 전달형 프록시
bridge.config.localURL = capacitor://localhost (Remote 모드에서도 에셋 핸들러가 public/ 을 서빙) · webDir index.html 없으면 fatalLoadError(:172)
SystemBars.load() 가 bridge.statusBarStyle = .default 로 덮는다 → preferredStatusBarStyle 오버라이드(Pitfall 13)
setScreenOrientationDefaults() 는 infoDictionary UISupportedInterfaceOrientations 만 읽는다 → ~ipad 병합 미확인 → idiom 오버라이드(Pitfall 9)
```

weekly-wine 원형: `CookieViewController.swift` L659-676 `setupPullToRefresh`/`handleRefresh` · L840-889 `PaymentNavigationDelegate`(responds/forwardingTarget) · `www/index.html` 61줄(online · 5초 · 다시 시도).

```swift
// 이 플랜이 새로 만드는 계약
enum ThemeStore { static let key = "gh-trade.theme"; static func load() -> GHTradeTheme; static func save(_ t: GHTradeTheme) }   // 기본 .light (웹 defaultTheme light)
final class NavigationDelegateProxy: NSObject, WKNavigationDelegate { init(original: WKNavigationDelegate?, owner: GHTradeBridgeViewController) }
// VC: func applyTheme(_ t: GHTradeTheme, animated: Bool) · func showOffline(failedURL: URL?)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 당겨서 새로고침(1초 고정) + 테마 추종·저장·첫 프레임 (D-04 · D-17 · D-23)</name>
  <files>mobile/ios/App/App/GHTradeBridgeViewController.swift, mobile/ios/App/App/ThemeStore.swift, mobile/ios/App/App/SceneDelegate.swift, mobile/ios/App/App.xcodeproj/project.pbxproj</files>
  <read_first>
    - mobile/ios/App/App/GHTradeBridgeViewController.swift (21-01·21-10 전체)
    - mobile/ios/App/App/SceneDelegate.swift (21-01)
    - mobile/ios/App/App/GHTradeTheme.swift (21-10 팔레트)
    - /Users/alex/repos/weekly-wine-app/ios/App/App/CookieViewController.swift (L655-676)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pattern 2 · Pitfall 7·13)
    - webapp/src/lib/native/native-bridge-provider.tsx (21-04 — theme 메시지 모양)
  </read_first>
  <action>
    ① `ThemeStore.swift`(`import Foundation`): `enum ThemeStore { static let key = "gh-trade.theme"; static func load() -> GHTradeTheme { GHTradeTheme(rawValue: UserDefaults.standard.string(forKey: key) ?? "") ?? .light }; static func save(_ t: GHTradeTheme) }` — 기본 light(웹 `defaultTheme="light"`). Xcode 등록.
    ② VC `capacitorDidLoad()` 맨 앞(super 직후)에서 `applyTheme(ThemeStore.load(), animated: false)` — 첫 프레임(D-23). `applyTheme(_ t:, animated:)`: `currentTheme = t` · `let p = GHTradePalette.of(t)` · `overrideUserInterfaceStyle = t == .dark ? .dark : .light`(스피너·키보드 색) · `view.backgroundColor`·`webView?.backgroundColor`·`webView?.scrollView.backgroundColor` = `p.bg` · `webView?.isOpaque = false` · `view.window?.backgroundColor = p.bg` · `tabBar.apply(p)` · 페이드 `apply(bg:)` · `setNeedsStatusBarAppearanceUpdate()` · `ThemeStore.save(t)`. `override var preferredStatusBarStyle: UIStatusBarStyle { currentTheme == .dark ? .lightContent : .darkContent }`(Pitfall 13). 메시지 핸들러 `theme`: `payload.theme` 이 `"dark"`/`"light"` 일 때만 `applyTheme(…, animated: true)`(0.2s 크로스페이드 허용). OS `traitCollection` 변화에 반응하는 코드는 두지 않는다(D-23 — 웹 `enableSystem=false`).
    ③ `SceneDelegate.swift`: 창 생성 직후 `window?.backgroundColor = GHTradePalette.of(ThemeStore.load()).bg`(흰/검 깜빡임 방지 — 루트 VC 교체 줄은 21-01 그대로).
    ④ 당겨서 새로고침(weekly-wine L659-676 이식, 핸들러만 교체): `setupPullToRefresh()` 를 `capacitorDidLoad()` 에서 호출 — `webView.scrollView.bounces = true`(★ Capacitor 기본 false 복원) · `let rc = UIRefreshControl()` · `rc.addTarget(self, action: #selector(handleRefresh(_:)), for: .valueChanged)` · `webView.scrollView.refreshControl = rc` 를 프로퍼티로 보관. `handleRefresh`: `webView?.evaluateJavaScript("window.__ghTrade&&window.__ghTrade.refresh?window.__ghTrade.refresh():location.reload()")` → `DispatchQueue.main.asyncAfter(deadline: .now() + 1.0) { sender.endRefreshing() }`(D-17 고정 1초 · 훅 Promise 를 기다리지 않는다). `updateTabBarVisibility` 안(또는 같은 시점)에서 `overlayOpen || isOfflinePage` 면 `webView.scrollView.refreshControl = nil`, 아니면 보관한 컨트롤을 다시 붙인다(Pitfall 7 — 키패드 시트 편집 중 당김 방지). 풀블리드라 스피너가 상태바 뒤에 그려지면 `viewDidLayoutSubviews` 에서 `rc.bounds = CGRect(x: rc.bounds.origin.x, y: -view.safeAreaInsets.top, width: rc.bounds.width, height: rc.bounds.height)` 로 안전영역 아래로 내린다(시각 확인은 21-16 UAT).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:ios</automated>
    <fails_when>non-zero exit, or the output lacks "BUILD SUCCEEDED"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:smoke:ios</automated>
    <fails_when>non-zero exit, or the output lacks "SMOKE OK ready platform=ios"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "scrollView.bounces = true" mobile/ios/App/App/GHTradeBridgeViewController.swift` 가 1줄
    - `grep -n "deadline: .now() + 1" mobile/ios/App/App/GHTradeBridgeViewController.swift` 가 1줄 이상 · `grep -n "__ghTrade.refresh" mobile/ios/App/App/GHTradeBridgeViewController.swift` 가 1줄
    - `grep -n "override var preferredStatusBarStyle" mobile/ios/App/App/GHTradeBridgeViewController.swift` 가 1줄 · `grep -n "overrideUserInterfaceStyle" mobile/ios/App/App/GHTradeBridgeViewController.swift` 가 1줄 이상
    - `grep -n '"gh-trade.theme"' mobile/ios/App/App/ThemeStore.swift` 가 1줄 · `grep -n "ThemeStore.load()" mobile/ios/App/App/SceneDelegate.swift` 가 1줄
    - `grep -cE "traitCollectionDidChange|registerForTraitChanges" mobile/ios/App/App/GHTradeBridgeViewController.swift` 가 0 (OS 다크모드 미추종)
  </acceptance_criteria>
  <done>당김 한 번에 웹 훅이 불리고 스피너가 1초에 닫히며, 웹 테마 전환이 상태바·배경·탭바에 즉시 반영되고 다음 실행 첫 프레임에 유지된다.</done>
</task>

<task type="auto">
  <name>Task 2: 오프라인 폴백(네트워크 오류 전용 프록시 + 자동 복구 페이지) · 방향 · Info.plist (D-19 · D-24 · D-20)</name>
  <files>mobile/ios/App/App/NavigationDelegateProxy.swift, mobile/ios/App/App/GHTradeBridgeViewController.swift, mobile/www/index.html, mobile/ios/App/App/Info.plist, mobile/ios/App/App.xcodeproj/project.pbxproj</files>
  <read_first>
    - /Users/alex/repos/weekly-wine-app/ios/App/App/CookieViewController.swift (L840-889 `PaymentNavigationDelegate`)
    - /Users/alex/repos/weekly-wine-app/www/index.html (61줄 원형)
    - /Users/alex/repos/weekly-wine-app/ios/App/App/Info.plist (L37-51 방향 키)
    - mobile/www/index.html (21-01 최소판) · mobile/ios/App/App/Info.plist (템플릿 — Scene manifest 유지)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pattern 5 · Code Examples 「iOS 전달형 네비게이션 프록시」 · Pitfall 2·9 · Assumptions A3)
  </read_first>
  <action>
    ① `NavigationDelegateProxy.swift`: RESEARCH Code Example 그대로의 전달형 프록시 — `weak var original: WKNavigationDelegate?` · `weak var owner: GHTradeBridgeViewController?` · 네트워크 오류 집합 `[NSURLErrorNotConnectedToInternet, NSURLErrorTimedOut, NSURLErrorCannotFindHost, NSURLErrorCannotConnectToHost, NSURLErrorNetworkConnectionLost, NSURLErrorDataNotAllowed]` · `didFailProvisionalNavigation`·`didFail` 만 구현(먼저 `original?.webView?(…)` 호출 → `NSURLErrorDomain` 이고 `NSURLErrorCancelled` 가 아니고 집합에 있을 때만 `owner?.showOffline(failedURL: e.userInfo[NSURLErrorFailingURLErrorKey] as? URL)`) · `responds(to:)`·`forwardingTarget(for:)` 로 나머지(정책 결정 포함)를 원본에 자동 전달 — 정책 결정 메서드를 직접 구현하지 않는다. Xcode 등록.
    ② VC: `capacitorDidLoad()` 에서 `navProxy = NavigationDelegateProxy(original: wv.navigationDelegate, owner: self)` · `wv.navigationDelegate = navProxy`(프록시를 강하게 보관하는 프로퍼티). `showOffline(failedURL:)`: 이미 오프라인 페이지면 무동작 · `to` = failedURL 이 있고 스킴이 http/https 면 그 절대 URL, 아니면 `bridge.config.serverURL` · `var c = URLComponents(url: bridge.config.localURL.appendingPathComponent("index.html"), resolvingAgainstBaseURL: false)` 에 `queryItems = [to, theme=currentTheme.rawValue]` → `webView?.load(URLRequest(url: c.url!))`. 호스트가 server 호스트가 아니므로 21-10 규칙으로 탭바·새로고침이 자동으로 숨는다(D-12 ②). Capacitor 설정의 오류 페이지 경로 키는 여전히 쓰지 않는다(Pitfall 2).
    ③ `mobile/www/index.html` 완성(weekly-wine 원형 + RESEARCH Pattern 5 보강): `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">` · 스크립트 상수 `ALLOWED_ORIGINS = ['https://trade.jx1.io', 'http://localhost:3100']` · `DEFAULT_TARGET = 'https://trade.jx1.io/'` · `function safeTarget(raw)` = `new URL(raw)` 가 성공하고 `origin` 이 허용 목록에 있으면 `href`, 아니면 `DEFAULT_TARGET`(오픈 리다이렉트 방지 T-21-06) · `theme` 쿼리 `dark` 면 `<html class="dark">`(배경 `#17171c` · 글자 흰색) 아니면 라이트(`#ffffff` · `#191f28`) · 본문: 「GH Trade」 워드마크(24/800) · 「인터넷 연결을 확인해주세요」 · 「복구되면 자동으로 이동합니다」 · 「다시 시도」 버튼(primary `#3182f6`/다크 `#3485fa`, 누르면 `tryReturn()`) · `tryReturn()` = `fetch(new URL('/icon.svg', target).href, { mode: 'no-cors', cache: 'no-store' })` 가 resolve 하면 `location.replace(target)`, reject 면 무동작(A3 — 「와이파이는 붙었는데 인터넷 없음」 왕복 방지) · `window.addEventListener('online', tryReturn)` · `setInterval(tryReturn, 5000)` · 동시 실행 가드 플래그. 외부 스크립트·폰트 로드 없음(오프라인에서 열려야 한다 — 시스템 폰트 스택).
    ④ 방향·표시(D-24·D-20): VC `override var supportedInterfaceOrientations: UIInterfaceOrientationMask { UIDevice.current.userInterfaceIdiom == .pad ? .all : .portrait }`(Pitfall 9). `Info.plist`: `CFBundleDisplayName` = `GH Trade` · `UISupportedInterfaceOrientations` = [`UIInterfaceOrientationPortrait`] · `UISupportedInterfaceOrientations~ipad` = Portrait·PortraitUpsideDown·LandscapeLeft·LandscapeRight · 전체 화면 강제 키는 두지 않는다(Split View 허용) · `UIViewControllerBasedStatusBarAppearance` = true(없으면 추가 — `preferredStatusBarStyle` 이 먹게) · Scene manifest(`UIApplicationSceneManifest`)는 템플릿 그대로. `project.pbxproj` 의 App 타깃 `TARGETED_DEVICE_FAMILY = "1,2"` · `IPHONEOS_DEPLOYMENT_TARGET` 이 15.0 미만이면 15.0(이상이면 템플릿 값 유지).
    ⑤ `plutil -lint` 로 Info.plist 형식을 확인하고 빌드·스모크를 돌린다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && plutil -lint mobile/ios/App/App/Info.plist && pnpm --filter @gh-radar/mobile run native:build:ios</automated>
    <fails_when>non-zero exit, or plutil output lacks "OK", or the build output lacks "BUILD SUCCEEDED"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && plutil -extract CFBundleDisplayName raw mobile/ios/App/App/Info.plist && plutil -extract "UISupportedInterfaceOrientations~ipad" json -o - mobile/ios/App/App/Info.plist</automated>
    <fails_when>the first line is not exactly "GH Trade", or the JSON array has fewer than 4 entries, or non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "forwardingTarget" mobile/ios/App/App/NavigationDelegateProxy.swift` · `grep -n "NSURLErrorCancelled" mobile/ios/App/App/NavigationDelegateProxy.swift` 가 각각 1줄 이상
    - `grep -c "decidePolicyFor" mobile/ios/App/App/NavigationDelegateProxy.swift` 가 0 (정책 결정은 원본에 자동 전달)
    - `grep -n "function safeTarget" mobile/www/index.html` · `grep -n "mode: 'no-cors'" mobile/www/index.html` · `grep -n "setInterval(tryReturn, 5000)" mobile/www/index.html` 가 각각 1줄
    - `grep -c "UIRequiresFullScreen" mobile/ios/App/App/Info.plist` 가 0 · `plutil -extract UISupportedInterfaceOrientations json -o - mobile/ios/App/App/Info.plist` 결과가 `["UIInterfaceOrientationPortrait"]`
    - `grep -n 'TARGETED_DEVICE_FAMILY = "1,2"' mobile/ios/App/App.xcodeproj/project.pbxproj` 가 1줄 이상
    - `grep -cE 'errorPath' mobile/capacitor.config.ts` 가 0 (여전히 미사용)
  </acceptance_criteria>
  <done>비행기 모드에서만 폴백이 뜨고(취소·404 에는 안 뜬다) 복구되면 허용된 원래 경로로 돌아가며, 폰은 세로·iPad 는 전방향이고 홈 화면 이름이 GH Trade 다(동작 확인은 21-16 UAT).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 폴백 페이지 `?to=` 쿼리 | 네이티브가 만든 값이지만 로컬 페이지 URL 을 조작하면 임의 출처로 보낼 수 있다 |
| 웹 `theme` 메시지 → 네이티브 저장 | 값이 UserDefaults 에 저장된다 |
| Capacitor 내비게이션 델리게이트 | 프록시가 원본 동작(앱 호스트 판정·외부 링크 처리)을 가리면 안 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-06 | Tampering(오픈 리다이렉트) | `www/index.html` `?to=` | high | mitigate | `safeTarget` 출처 허용 목록 · 기본값 운영 루트(Task 2 ③) |
| T-21-37 | Tampering | 프록시가 정책 결정 가림 → 외부 URL 을 앱 WebView 에서 열기 | high | mitigate | 정책 결정 미구현 → 원본 자동 전달(acceptance `decidePolicyFor` 0) · 실패 콜백도 원본 먼저 호출 |
| T-21-02 | Tampering | `theme` 페이로드 | low | mitigate | `"dark"`/`"light"` 두 값만 수용 · 저장 키 단일 |
| T-21-38 | Denial of Service | 오프라인 오탐(-999·404)으로 정상 화면 가림 | medium | mitigate | 네트워크 오류 코드 집합만 · 취소 제외 · HTTP 오류 무시(Pitfall 2) |
| T-21-SC | Tampering | 패키지 설치 | low | accept | 설치 없음 |
</threat_model>

<verification>
- `native:build:ios` · `native:smoke:ios` · plutil 검사 green
- 새로고침·테마·오프라인·회전 실동작은 21-16 UAT(시뮬레이터 네트워크 링크 컨디셔너·iPad 회전)
</verification>

<success_criteria>
- D-04·D-17·D-19·D-20·D-23·D-24 가 iOS 에서 구현됐다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/` 패키지 `@gh-radar/mobile` — scripts `native:sync` · `native:sync:dev` · `native:build:ios` · `native:build:android` · `native:smoke:ios` · `native:open:ios` · `native:open:android` · `native:assets` (21-01) · `native:smoke:android` (21-02) · `native:check-tab-routes:ios` (21-10) · `native:test:android` (21-12) · `native:verify-prod` (21-16) | 패키지·스크립트 | 21-01 · 21-02 · 21-10 · 21-12 · 21-16 |
| `mobile/capacitor.config.ts` — appId `com.ghtrade.app` · appName `GH Trade` · `CAP_SERVER_URL` · `SystemBars` (21-01) · `SocialLogin` (21-15) | 설정 | 21-01 · 21-15 |
| `mobile/tsconfig.json` · `mobile/.gitignore` | 설정 | 21-01 |
| `mobile/www/index.html` — 오프라인 폴백(최소 21-01 · `safeTarget`·5초 탐침·`online`·테마 21-11) | 정적 페이지 | 21-01 · 21-11 |
| `mobile/scripts/smoke-ios.sh` · `mobile/scripts/smoke-android.sh` — 「SMOKE OK ready platform=…」 | 스모크 | 21-01 · 21-02 |
| `mobile/scripts/check-tab-routes-ios.sh` · `mobile/scripts/tab-routes-check.swift` | Swift 경로표 검사 | 21-10 |
| `mobile/scripts/render-resources.mjs` · `mobile/resources/{icon.svg,icon-only.png,icon-foreground.png,icon-background.png,splash.png,splash-dark.png}` | 에셋 원본 | 21-14 |
| `mobile/scripts/verify-prod-config.mjs` · `mobile/README.md` | 운영 설정 검사·문서 | 21-16 |
| iOS `SceneDelegate.swift` (루트 = `GHTradeBridgeViewController()` · 창 배경 21-11) | Swift | 21-01 · 21-11 |
| iOS `GHTradeBridgeViewController` · `WeakScriptMessageHandler` · `NativeMessageType` | Swift | 21-01 · 21-10 · 21-11 |
| iOS `GHTradeTabBar` · `GHTradeTab` · `GHTabID` · `GHTradePalette` · `GHTradeTheme` · `TabRoutes.activeTab(forPath:)` · `TabRoutes.hidesTabBar(path:)` | Swift | 21-10 |
| iOS `NavigationDelegateProxy` · `ThemeStore` (UserDefaults `gh-trade.theme`) | Swift | 21-11 |
| Android `MainActivity` (`BridgeActivity`) · `GhTradeWebViewClient` (`BridgeWebViewClient` 상속) · `GhTradeBridge` (`@JavascriptInterface postMessage`) | Kotlin | 21-02 · 21-12 · 21-13 |
| Android `GhTradeTabBar` · `GhTradePalette` · `TabId` · `TabRoutes.activeTabFor` · `TabRoutes.hidesTabBar` · `TabRoutesTest` | Kotlin | 21-12 |
| Android `ThemeStore` (SharedPreferences `gh_trade`/`theme`) · `res/values/bools.xml` · `res/values-sw600dp/bools.xml` (`is_tablet`) | Kotlin·리소스 | 21-13 |
| Android `res/drawable/ic_tab_{home,home_fill,search,search_fill,trading,trading_fill,ai,ai_fill,me,me_fill}.xml` | 벡터 | 21-12 |
| 웹 `lib/native/native-detect.ts` — `NATIVE_DETECT_SCRIPT` · `isNativeApp` · `nativePlatform` | 유틸 | 21-01 |
| 웹 `lib/native/post-native.ts` — `postNative` · `NativeMsgType` | 유틸 | 21-04 |
| 웹 `lib/native/native-bridge-provider.tsx` — `NativeBridgeProvider` · `useNativeBridge` · `NativeRefreshFn` · `window.__ghTrade = { refresh, navigate, back }` | Provider | 21-04 |
| 웹 `lib/native/use-native-refresh.ts` — `useNativeRefresh` | 훅 | 21-04 |
| 웹 `lib/native/native-overlay-marker.tsx` — `NativeOverlayMarker` | 컴포넌트 | 21-04 |
| 웹 `lib/native/google-client-ids.ts` — `GOOGLE_WEB_CLIENT_ID` · `GOOGLE_IOS_CLIENT_ID` · `GOOGLE_ANDROID_CLIENT_ID` · `GOOGLE_IOS_URL_SCHEME` | 공개 식별자 상수 | 21-03 |
| 웹 `lib/native/native-google-login.ts` — `nativeGoogleSignIn` · `randomNonce` · `sha256Hex` · `classifyNativeLoginError` | 서비스 | 21-15 |
| 웹 `RelayConnectionState.probeNow` · `EMPTY_RELAY_VALUE.probeNow` | 훅 계약 | 21-07 |
| 웹 `lib/recent-search.ts` — `RECENT_SEARCH_KEY` · `RECENT_SEARCH_MAX` · `RecentSearchItem` · `readRecentSearches` · `pushRecentSearch` · `removeRecentSearch` · `clearRecentSearches` | 유틸 | 21-08 |
| 웹 `app/search/page.tsx` · `components/search/search-page-client.tsx` — `SearchPageClient` · 사이드바 `NAV_SEARCH_PAGE` | 페이지·상수 | 21-08 |
| 웹 `components/me/account-card.tsx` — `AccountCard` | 컴포넌트 | 21-09 |
| 웹 마크업 계약 `data-slot="app-aside"` · `data-slot="app-menu-button"` · `data-slot="stock-ai-button"` · `data-slot="shared-panels"` · `data-pinned` · `data-order-cta` · `data-slot="account-card"` | 마크업 | 21-05 · 21-06 · 21-09 |
| CSS `@custom-variant native` (21-04) · 앱 셸 규칙 (21-05) · `--app-safe-top` · `--app-safe-bottom` · `--app-safe-left` · `--app-safe-right` · `--native-tabbar-gap` · `--native-tabbar-offset` · `--native-body-reserve` (21-06) | 토큰·변형 | 21-04 · 21-05 · 21-06 |
| 웹 `app/icon.svg` (= 스케치 004 `#app-a`) | 파비콘 | 21-14 |
| 단위 테스트: `native-detect.test.ts` (21-01) · `post-native.test.ts` · `native-bridge-provider.test.tsx` · `native-overlay.test.tsx` (21-04) · `google-client-ids.test.ts` (21-03) · `relay-socket.test.ts` probeNow · `stock-native-refresh.test.tsx` (21-07) · `recent-search.test.ts` · `search-page-client.test.tsx` · `app-sidebar.test.tsx` 보강 (21-08) · `account-card.test.tsx` · `app-shell-chrome.test.tsx` 보강 (21-09) · `native-google-login.test.ts` · `login/__tests__/page.test.tsx` (21-15) · `TabRoutesTest.kt` (21-12) | 단위 | 각 플랜 |
| e2e: `fixtures/native-app.ts` — `installNativeApp` · `nativeMessages` (21-05) · `specs/native-shell.spec.ts` (21-05 · 21-06) · `specs/search-page.spec.ts` (21-08) · `specs/brand-account.spec.ts` (21-09) | Playwright | 각 플랜 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-11-SUMMARY.md` when done
</output>
