---
phase: 21-gh-trade-mobile-app
plan: 07
type: execute
wave: 3
depends_on: ["21-04"]
files_modified:
  - webapp/src/lib/use-relay-socket.ts
  - webapp/src/lib/relay-provider.tsx
  - webapp/src/lib/__tests__/relay-socket.test.ts
  - webapp/src/components/home/home-client.tsx
  - webapp/src/components/scanner/scanner-client.tsx
  - webapp/src/components/theme/themes-client.tsx
  - webapp/src/components/watchlist/watchlist-client.tsx
  - webapp/src/components/trading/workbench/trading-workbench.tsx
  - webapp/src/components/trading/me-client.tsx
  - webapp/src/components/stock/stock-detail-client.tsx
  - webapp/src/components/stock/stock-news-section.tsx
  - webapp/src/components/stock/stock-discussion-section.tsx
  - webapp/src/components/stock/__tests__/stock-native-refresh.test.tsx
autonomous: true
requirements: [MOBILE-01]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-16: `useRelayConnection` 이 `probeNow()` 를 노출하고 `RelayProvider`·`useRelayContext` 로 흐른다 — 백오프 대기·예산 소진이면 즉시 새 소켓, 인증된 OPEN 이면 `RELAY_RESUME_PROBE_MS`(5초) 탐침(무프레임이면 재연결), 연결 시도 중이면 무동작 · Provider 밖은 no-op · relay 프로토콜·서버 무변경"
    - "D-16: `/trading`(TradingWorkbench)·`/me`(MeClient)의 당겨서 새로고침 훅 = `probeNow` — 편집 중 수동주문 입력(컴포넌트 로컬 상태)은 유지된다"
    - "D-04: 홈(`useHomeQuery.refresh`)·상승률 상위(`usePolling.refresh`)·테마(`useThemesQuery.refresh`)·관심종목(`useWatchlistQuery.refresh`)이 `useNativeRefresh` 로 자기 재조회를 등록한다 — 웹 브라우저에서는 등록만 되고 호출 경로가 없다"
    - "D-18: 종목상세 당겨서 새로고침은 시세·차트·통계(`fetchStockDetail` 재조회 → `refreshSignal`)와 뉴스·토론 섹션의 **GET 캐시 재조회**만 부른다 — 외부 수집을 일으키는 POST `/news/refresh`·`/discussions/refresh` 호출 0 (크롤링 5원칙 3 · 공식 API 운영 기준 3)"
    - "D-17 연계: 웹 refresh 훅의 완료 시각과 무관하게 네이티브 스피너는 1초 고정(21-11·21-13) — 웹 훅은 Promise 를 돌려주지만 네이티브는 기다리지 않는다"
  artifacts:
    - path: webapp/src/lib/use-relay-socket.ts
      provides: "RelayConnectionState.probeNow"
      contains: "probeNow"
    - path: webapp/src/lib/relay-provider.tsx
      provides: "EMPTY_RELAY_VALUE.probeNow = NOOP"
      contains: "probeNow"
    - path: webapp/src/components/stock/__tests__/stock-native-refresh.test.tsx
      provides: "D-18 — refresh 가 POST refresh API 를 부르지 않음(MOBILE-01l)"
  key_links:
    - from: webapp/src/components/trading/workbench/trading-workbench.tsx
      to: webapp/src/lib/use-relay-socket.ts
      via: "useNativeRefresh(useRelayContext().probeNow) → onResume(true)"
      pattern: "useNativeRefresh\\("
    - from: webapp/src/components/stock/stock-news-section.tsx
      to: webapp/src/lib/native/use-native-refresh.ts
      via: "useNativeRefresh(load)  — load = fetchStockNews(GET)"
      pattern: "useNativeRefresh\\(load\\)"
---

<assumption_delta_decision>
- 검출기 `assumption-delta scan 21` → `detected: false`. 수동 판정: 두 번째 클라이언트 플랫폼 — 정본 명사 **웹앱**, 결정 **add-alongside**. 새로고침은 웹의 기존 재조회 함수를 등록만 하고 새 데이터 경로를 만들지 않는다(21-01 블록 참조).
</assumption_delta_decision>

<objective>
당겨서 새로고침의 「무엇을 다시 읽나」를 페이지마다 연결한다: relay 재탐침 `probeNow`(D-16, relay 무변경) · 목록 4화면 · 트레이딩/마이 · 종목상세(D-18 — GET 재조회만).

Purpose: 네이티브 제스처(21-11·21-13)가 `window.__ghTrade.refresh()` 를 부를 때 각 화면이 전체 reload 없이 자기 데이터만 새로 읽는다. 종목상세에서 사용자 제스처가 Naver·토론방 외부 호출을 일으키면 CLAUDE.md 운영 기준 위반이다.
Output: `probeNow` 계약 + 페이지 9곳 등록 + D-18 테스트.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md
@.planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md
@webapp/src/lib/native/use-native-refresh.ts

<interfaces>
기존 계약(코드에서 추출):

```ts
// webapp/src/lib/use-relay-socket.ts
export const RELAY_RESUME_PROBE_MS = 5_000;
export interface RelayConnectionState { …; send(msg): boolean; reconnect: () => void; subscribe…; unsubscribe…; sendOrder… }  // L470-500
export function useRelayConnection({ enabled }: UseRelayConnectionOptions): RelayConnectionState;          // L1099
// effect 지역 클로저(L1498-1541): restart() · stopProbe() · startProbe() · onResume(probe: boolean) {
//   if (disposed) return; if (exhausted || retryTimer !== null) { restart(); return; } if (probe) startProbe(); }
// 리스너 L1553-1561: visibilitychange · pageshow(persisted) · online → onResume(...)
// 반환 L1715-1730: const reconnect = useCallback(() => setReconnectNonce(v => v + 1), []); return useMemo<RelayConnectionState>(() => ({ … }), […])

// webapp/src/lib/relay-provider.tsx
const NOOP = () => {};  const EMPTY_RELAY_VALUE: RelayContextValue = { …, reconnect: NOOP, subscribe: NOOP, … }   // L328-377
const value = useMemo<RelayContextValue>(() => ({ ...connection, sendOrder, orderLocks }), [...]);                  // L450
export function useRelayContext(): RelayContextValue;

// 페이지 재조회 함수
home-client.tsx L39:        const { data, isLoading, isRefreshing, error, refresh } = useHomeQuery(…)
scanner-client.tsx L44:     const { data, error, refresh, isRefreshing, isInitialLoading } = usePolling(fetcher, {…})
themes-client.tsx L36-45:   const { …, refresh, … } = useThemesQuery()
watchlist-client.tsx L63:   … = useWatchlistQuery()   // 반환에 refresh: load 포함 (use-watchlist-query.ts L109-116)
trading-workbench.tsx L555: const relay = useRelayContext();
me-client.tsx L199:         const { accounts, accountStates, status } = useRelayContext();
stock-detail-client.tsx L71: const load = useCallback(async () => { … fetchStockDetail(code, signal) … setIsRefreshing … }, [code]);  // 차트는 refreshSignal={isRefreshing}
stock-news-section.tsx L50:  const load = useCallback(async () => { … fetchStockNews(stockCode, { days: 7, limit }, signal) … }, [stockCode]);  // 수동 버튼은 refreshStockNews(POST)
stock-discussion-section.tsx L77: const load = useCallback(async () => { … fetchStockDiscussions(stockCode, { hours, limit }, signal) … }, [stockCode]); // 수동 버튼은 refreshStockDiscussions(POST)
// webapp/src/lib/stock-api.ts: fetchStockDetail · fetchStockNews · refreshStockNews(POST …/news/refresh) · fetchStockDiscussions · refreshStockDiscussions(POST …/discussions/refresh)
// webapp/src/lib/native/use-native-refresh.ts (21-04): export function useNativeRefresh(fn: NativeRefreshFn | null | undefined): void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `probeNow()` — effect 지역 `onResume(true)` 를 공개 메서드로 (D-16, relay 무변경)</name>
  <files>webapp/src/lib/use-relay-socket.ts, webapp/src/lib/relay-provider.tsx, webapp/src/lib/__tests__/relay-socket.test.ts</files>
  <read_first>
    - webapp/src/lib/use-relay-socket.ts (L1-60 머리 주석 · L470-540 인터페이스 · L1240-1275 effect 지역 변수 · L1490-1570 복귀 경로 · L1700-1760 반환)
    - webapp/src/lib/relay-provider.tsx (L300-380 EMPTY · L440-470 value)
    - webapp/src/lib/__tests__/relay-socket.test.ts (L240-280 하네스 `render`·`connected`·`settle` · L2033-2360 복귀 describe R1~R11)
    - relay/src/ws/protocol.ts (L355-367 인바운드 스키마 — 「스냅샷 재요청」 프레임이 없음을 확인만)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pitfall 11)
  </read_first>
  <behavior>
    - P1: 예산 소진(`manual_required`) 상태에서 `probeNow()` → 새 소켓 1개가 즉시 열린다(`FakeWebSocket.instances` 증가, 백오프 대기 없이 attempt 0)
    - P2: 인증된 OPEN 소켓에서 `probeNow()` → `RELAY_RESUME_PROBE_MS − 1` 까지 새 소켓 0 · 그 사이 프레임이 오지 않으면 `+1ms` 에 새 소켓(옛 소켓 close) · 창 안에 새 프레임이 오면 새 소켓 0
    - P3: 연결 시도 중(인증 ACK 전)에는 `probeNow()` 가 아무 것도 하지 않는다 · 언마운트 뒤 `probeNow()` 호출은 소켓을 열지 않는다 · 같은 창 안 두 번 호출은 탐침 1개(중복 타이머 없음)
    - `useRelayContext()` 를 Provider 없이 부르면 `probeNow` 가 함수이고 throw 하지 않는다
  </behavior>
  <action>
    ① `RelayConnectionState` 에 `probeNow: () => void;` 를 `reconnect` 바로 뒤에 더한다 — JSDoc: D-16 · 「소켓이 죽었으면 재연결(초기 스냅샷 재수신), 살아 있으면 서버 푸시가 곧 최신이라 추가 요청 없음」 · relay 인바운드에 스냅샷 재요청 프레임이 없어 relay 는 무변경(Pitfall 11) · 사람 제스처(당겨서 새로고침)만 호출하므로 재시도 폭주가 아님(T-15-10). `RelaySocketState` 에는 넣지 않는다(종목 구독 표면 무변경).
    ② `useRelayConnection` 안에 `const probeNowRef = useRef<() => void>(NOOP_PROBE)`(모듈 상수 `NOOP_PROBE = () => {}`)를 두고, 연결 effect 안 `onResume` 정의 뒤에 `probeNowRef.current = () => onResume(true)`, 그 effect cleanup 첫 줄에 `probeNowRef.current = NOOP_PROBE` 를 둔다. 반환 `useMemo` 에 `probeNow` 를 넣되 값은 `const probeNow = useCallback(() => probeNowRef.current(), [])`(안정 참조 — `useNativeRefresh` 가 재등록하지 않게).
    ③ `relay-provider.tsx` 의 `EMPTY_RELAY_VALUE` 에 `probeNow: NOOP` 를 `reconnect: NOOP` 옆에 더한다(value 는 `...connection` 스프레드라 자동 전달).
    ④ `relay-socket.test.ts` 의 복귀 describe 끝에 `it('P1 …')`·`P2`·`P3` 을 먼저 쓴다(RED — `probeNow` 없음). 하네스(`render` · `connected` · `exhaust` · `FakeWebSocket` · 가짜 타이머)를 그대로 쓰고 케이스 이름에 `probeNow` 를 넣어 `-t probeNow` 로 걸러지게 한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/__tests__/relay-socket.test.ts -t probeNow</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or reports "0 passed" / all tests skipped (the -t filter matched nothing)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/__tests__/relay-socket.test.ts src/lib/__tests__/relay-provider.test.tsx</automated>
    <fails_when>non-zero exit, or the summary contains "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "probeNow: () => void;" webapp/src/lib/use-relay-socket.ts` 가 1줄 · `grep -n "probeNowRef.current = () => onResume(true)" webapp/src/lib/use-relay-socket.ts` 가 1줄
    - `grep -n "probeNow: NOOP" webapp/src/lib/relay-provider.tsx` 가 1줄
    - `git diff --quiet -- relay/` 가 exit 0 (relay 무변경 — 커밋 전 작업 트리 기준)
    - relay-socket 전체 스위트(R1~R11 포함)가 통과한다
  </acceptance_criteria>
  <done>relay 를 건드리지 않고 「지금 재탐침」이 공개 계약이 됐고, 세 갈래(소진/살아 있음/시도 중)가 테스트로 잠겼다.</done>
</task>

<task type="auto">
  <name>Task 2: 목록 4화면 + 트레이딩·마이 — `useNativeRefresh` 등록 (D-04 · D-16)</name>
  <files>webapp/src/components/home/home-client.tsx, webapp/src/components/scanner/scanner-client.tsx, webapp/src/components/theme/themes-client.tsx, webapp/src/components/watchlist/watchlist-client.tsx, webapp/src/components/trading/workbench/trading-workbench.tsx, webapp/src/components/trading/me-client.tsx</files>
  <read_first>
    - webapp/src/lib/native/use-native-refresh.ts (21-04)
    - webapp/src/components/home/home-client.tsx (L30-80) · webapp/src/components/scanner/scanner-client.tsx (L40-75) · webapp/src/components/theme/themes-client.tsx (L30-50) · webapp/src/components/watchlist/watchlist-client.tsx (L55-70)
    - webapp/src/components/trading/workbench/trading-workbench.tsx (L540-560) · webapp/src/components/trading/me-client.tsx (L195-205)
    - .planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md (§use-native-refresh 소비처 1·2·4)
  </read_first>
  <action>
    각 파일에서 이미 받아 둔 재조회 함수를 `useNativeRefresh(...)` 한 줄로 등록한다(import `@/lib/native/use-native-refresh`): home-client → `useNativeRefresh(refresh)` · scanner-client → `useNativeRefresh(refresh)` · themes-client → `useNativeRefresh(refresh)` · watchlist-client → `useWatchlistQuery()` 반환에서 `refresh` 를 꺼내(아직 안 꺼냈으면 구조분해에 추가) `useNativeRefresh(refresh)` · trading-workbench → `useNativeRefresh(relay.probeNow)` · me-client → `useRelayContext()` 구조분해에 `probeNow` 추가 후 `useNativeRefresh(probeNow)`. 훅 호출은 컴포넌트 최상위(조건부 반환보다 위)에 둔다 — 훅 순서 규칙. 각 줄 위에 한 줄 주석(`D-04` 또는 `D-16 — relay 재탐침, 편집 중 입력은 로컬 상태라 유지`). 다른 로직·렌더는 건드리지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/home src/components/theme src/components/watchlist src/components/trading/__tests__/trading-workbench.test.tsx src/components/trading/__tests__/me-client.test.tsx</automated>
    <fails_when>non-zero exit, or the summary contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 여섯 파일 각각에서 `grep -n "useNativeRefresh(" <파일>` 가 1줄
    - `grep -n "useNativeRefresh(relay.probeNow)" webapp/src/components/trading/workbench/trading-workbench.tsx` 가 1줄 · `grep -n "useNativeRefresh(probeNow)" webapp/src/components/trading/me-client.tsx` 가 1줄
  </acceptance_criteria>
  <done>탭 5개의 목적지 중 홈·검색 계열·트레이딩·마이가 당겨서 새로고침 때 자기 데이터만 다시 읽는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 종목상세 — 시세·차트·통계 + 뉴스·토론 GET 재조회만 등록, POST refresh 0 (D-18)</name>
  <files>webapp/src/components/stock/stock-detail-client.tsx, webapp/src/components/stock/stock-news-section.tsx, webapp/src/components/stock/stock-discussion-section.tsx, webapp/src/components/stock/__tests__/stock-native-refresh.test.tsx</files>
  <read_first>
    - webapp/src/components/stock/stock-detail-client.tsx (L57-100 `load` · L180-240 섹션 조립)
    - webapp/src/components/stock/stock-news-section.tsx (L1-40 머리 주석 · L39-120 `load` 와 버튼용 refresh 핸들러)
    - webapp/src/components/stock/stock-discussion-section.tsx (L1-40 · L64-140)
    - webapp/src/lib/stock-api.ts (L1-20 머리 · fetch/refresh 함수 시그니처)
    - webapp/src/components/stock/__tests__/stock-detail-client.test.tsx (모킹 방식 — 차트·호가 섹션 스텁)
    - CLAUDE.md §「Naver Search API Rate Limit」 · 「공식 API 운영 기준」 3 · 크롤링 5원칙 3
  </read_first>
  <behavior>
    - 앱 모드 + `NativeBridgeProvider` 안에 `StockNewsSection` + `StockDiscussionSection` 렌더 → 초기 `fetchStockNews` 1 · `fetchStockDiscussions` 1 → `window.__ghTrade.refresh()` → 각각 2 · `refreshStockNews` 0 · `refreshStockDiscussions` 0
    - 앱 모드 + `StockDetailClient`(차트·호가·테마칩·상한가·동조 섹션은 `vi.mock` 스텁) → `fetchStockDetail` 초기 1 → refresh → 2
    - 섹션 언마운트 뒤 `refresh()` → 그 섹션의 fetch 증가 0
    - 수동 「새로고침」 버튼 경로(`refreshStockNews`)는 그대로 존재한다(버튼 클릭 시 1회 — 기존 동작 회귀 없음)
  </behavior>
  <action>
    ① `stock-detail-client.tsx`: `useNativeRefresh(load)`(D-18 — 시세 재조회가 `isRefreshing` → 차트 `refreshSignal` 로 차트·통계까지 다시 읽힌다). 테마칩·상한가·동조 섹션은 등록하지 않는다(시세·차트·통계 범위 밖 · 마운트 시 읽은 캐시 유지).
    ② `stock-news-section.tsx`: 마운트 GET 용 `load` 를 `useNativeRefresh(load)` 로 등록한다. 버튼용 POST 핸들러는 등록하지 않는다. 머리 주석에 한 단락: D-18 — 사용자 제스처가 외부 수집(Naver 검색 API 호출)을 일으키면 「사용자 수 비례 외부 호출 금지」 위반이라 당겨서 새로고침은 서버 캐시 GET 만 다시 읽는다 · 수동 버튼(쿨다운·429 가드)만 POST.
    ③ `stock-discussion-section.tsx`: 같은 방식으로 GET `load` 만 등록 + 같은 근거 주석(토론방 = 크롤링 5원칙 3).
    ④ `__tests__/stock-native-refresh.test.tsx` 를 먼저 쓴다(RED): `vi.mock('@/lib/stock-api', …)` 로 다섯 함수를 스파이로 바꾸고, 앱 모드 헬퍼(`native-app` 클래스 + `webkit.messageHandlers.ghTrade`)와 `NativeBridgeProvider` 로 감싸 렌더한다. `next/navigation`·`next-themes`·무거운 하위 섹션은 `stock-detail-client.test.tsx` 방식대로 모킹. 파일 머리 「깨지면 사용자가 겪는 일」: ① 당김 한 번에 Naver API·프록시 크롤링이 사용자 수만큼 늘어 일 예산·법적 원칙을 깬다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/stock/__tests__/stock-native-refresh.test.tsx src/components/stock/__tests__/stock-detail-client.test.tsx</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or "stock-native-refresh.test.tsx" is absent from the output</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test</automated>
    <fails_when>non-zero exit, or the summary line contains "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "useNativeRefresh(load)" webapp/src/components/stock/stock-detail-client.tsx webapp/src/components/stock/stock-news-section.tsx webapp/src/components/stock/stock-discussion-section.tsx` 가 파일마다 1줄
    - `stock-native-refresh.test.tsx` 에 `refreshStockNews` 와 `refreshStockDiscussions` 호출 수 0 단언이 있다(`grep -n "refreshStockNews" …` · `grep -n "refreshStockDiscussions" …` 각 1줄 이상)
    - webapp 전체 단위 스위트 통과
  </acceptance_criteria>
  <done>종목상세 당겨서 새로고침이 서버 캐시 GET 만 다시 읽고 외부 수집을 일으키지 않음이 테스트로 잠겼다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 제스처 → 우리 API → 외부(Naver·토론방 프록시) | 당겨서 새로고침이 외부 호출로 번지면 사용자 수 비례 호출이 된다 |
| 사용자 제스처 → relay wss | 재탐침이 연결 폭주로 번지면 relay·게이트웨이 부담 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-12 | Denial of Service / 법적 | 종목상세 refresh → POST refresh API | high | mitigate | GET `load` 만 등록(Task 3 ①~③) · 호출 0 단언 테스트(MOBILE-01l) |
| T-21-13 | Denial of Service | `probeNow` 연타 → relay 재연결 폭주 | medium | mitigate | 사람 제스처 한정 · 네이티브 스피너 1초(D-17) · 탐침 중 중복 무동작(P3) · 살아 있는 소켓은 끊지 않음(P2) |
| T-21-SC | Tampering | 패키지 설치 | low | accept | 설치 없음 |
</threat_model>

<verification>
- probeNow P1~P3 + relay-socket 전체 · 페이지 단위 테스트 · stock-native-refresh · webapp 전체 스위트 · typecheck green
</verification>

<success_criteria>
- D-04·D-16·D-18 의 「무엇을 다시 읽나」가 화면마다 연결됐고 외부 호출 경로는 제스처로 열리지 않는다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/` 패키지 `@gh-radar/mobile` — scripts `native:sync` · `native:sync:dev` · `native:build:ios` · `native:build:android` · `native:smoke:ios` · `native:open:ios` · `native:open:android` · `native:assets` (21-01) · `native:smoke:android` (21-02) · `native:check-tab-routes:ios` (21-10) · `native:test:android` (21-12) · `native:verify-prod` (21-16) | 패키지·스크립트 | 21-01 · 21-02 · 21-10 · 21-12 · 21-16 |
| `mobile/capacitor.config.ts` — appId `com.ghtrade.app` · appName `GH Trade` · `CAP_SERVER_URL` · `SystemBars` (21-01) · `SocialLogin` (21-15) | 설정 | 21-01 · 21-15 |
| `mobile/tsconfig.json` · `mobile/.gitignore` | 설정 | 21-01 |
| `mobile/www/index.html` — 오프라인 폴백(최소 21-01 · `safeTarget`·5초 탐침·`online`·테마 21-11) | 정적 페이지 | 21-01 · 21-11 |
| `mobile/scripts/smoke-ios.sh` · `mobile/scripts/smoke-android.sh` — 「SMOKE OK ready platform=…」 | 스모크 | 21-01 · 21-02 |
| `mobile/scripts/check-tab-routes-ios.sh` · `mobile/scripts/tab-routes-check.swift` | Swift 경로표 검사 | 21-10 |
| `mobile/scripts/render-resources.mjs` · `mobile/resources/{icon.svg,icon-only.png,icon-foreground.png,icon-background.png,splash.png,splash-dark.png}` | 에셋 원본 | 21-14 |
| `mobile/scripts/verify-prod-config.mjs` · `mobile/README.md` | 운영 설정 검사·문서 | 21-16 |
| iOS `SceneDelegate.swift` (루트 = `GHTradeBridgeViewController()` · 창 배경 21-11) | Swift | 21-01 · 21-11 |
| iOS `GHTradeBridgeViewController` · `WeakScriptMessageHandler` · `NativeMessageType` | Swift | 21-01 · 21-10 · 21-11 |
| iOS `GHTradeTabBar` · `GHTradeTab` · `GHTabID` · `GHTradePalette` · `GHTradeTheme` · `TabRoutes.activeTab(forPath:)` · `TabRoutes.hidesTabBar(path:)` | Swift | 21-10 |
| iOS `NavigationDelegateProxy` · `ThemeStore` (UserDefaults `gh-trade.theme`) | Swift | 21-11 |
| Android `MainActivity` (`BridgeActivity`) · `GhTradeWebViewClient` (`BridgeWebViewClient` 상속) · `GhTradeBridge` (`@JavascriptInterface postMessage`) | Kotlin | 21-02 · 21-12 · 21-13 |
| Android `GhTradeTabBar` · `GhTradePalette` · `TabId` · `TabRoutes.activeTabFor` · `TabRoutes.hidesTabBar` · `TabRoutesTest` | Kotlin | 21-12 |
| Android `ThemeStore` (SharedPreferences `gh_trade`/`theme`) · `res/values/bools.xml` · `res/values-sw600dp/bools.xml` (`is_tablet`) | Kotlin·리소스 | 21-13 |
| Android `res/drawable/ic_tab_{home,home_fill,search,search_fill,trading,trading_fill,ai,ai_fill,me,me_fill}.xml` | 벡터 | 21-12 |
| 웹 `lib/native/native-detect.ts` — `NATIVE_DETECT_SCRIPT` · `isNativeApp` · `nativePlatform` | 유틸 | 21-01 |
| 웹 `lib/native/post-native.ts` — `postNative` · `NativeMsgType` | 유틸 | 21-04 |
| 웹 `lib/native/native-bridge-provider.tsx` — `NativeBridgeProvider` · `useNativeBridge` · `NativeRefreshFn` · `window.__ghTrade = { refresh, navigate, back }` | Provider | 21-04 |
| 웹 `lib/native/use-native-refresh.ts` — `useNativeRefresh` | 훅 | 21-04 |
| 웹 `lib/native/native-overlay-marker.tsx` — `NativeOverlayMarker` | 컴포넌트 | 21-04 |
| 웹 `lib/native/google-client-ids.ts` — `GOOGLE_WEB_CLIENT_ID` · `GOOGLE_IOS_CLIENT_ID` · `GOOGLE_ANDROID_CLIENT_ID` · `GOOGLE_IOS_URL_SCHEME` | 공개 식별자 상수 | 21-03 |
| 웹 `lib/native/native-google-login.ts` — `nativeGoogleSignIn` · `randomNonce` · `sha256Hex` · `classifyNativeLoginError` | 서비스 | 21-15 |
| 웹 `RelayConnectionState.probeNow` · `EMPTY_RELAY_VALUE.probeNow` | 훅 계약 | 21-07 |
| 웹 `lib/recent-search.ts` — `RECENT_SEARCH_KEY` · `RECENT_SEARCH_MAX` · `RecentSearchItem` · `readRecentSearches` · `pushRecentSearch` · `removeRecentSearch` · `clearRecentSearches` | 유틸 | 21-08 |
| 웹 `app/search/page.tsx` · `components/search/search-page-client.tsx` — `SearchPageClient` · 사이드바 `NAV_SEARCH_PAGE` | 페이지·상수 | 21-08 |
| 웹 `components/me/account-card.tsx` — `AccountCard` | 컴포넌트 | 21-09 |
| 웹 마크업 계약 `data-slot="app-aside"` · `data-slot="app-menu-button"` · `data-slot="stock-ai-button"` · `data-slot="shared-panels"` · `data-pinned` · `data-order-cta` · `data-slot="account-card"` | 마크업 | 21-05 · 21-06 · 21-09 |
| CSS `@custom-variant native` (21-04) · 앱 셸 규칙 (21-05) · `--app-safe-top` · `--app-safe-bottom` · `--app-safe-left` · `--app-safe-right` · `--native-tabbar-gap` · `--native-tabbar-offset` · `--native-body-reserve` (21-06) | 토큰·변형 | 21-04 · 21-05 · 21-06 |
| 웹 `app/icon.svg` (= 스케치 004 `#app-a`) | 파비콘 | 21-14 |
| 단위 테스트: `native-detect.test.ts` (21-01) · `post-native.test.ts` · `native-bridge-provider.test.tsx` · `native-overlay.test.tsx` (21-04) · `google-client-ids.test.ts` (21-03) · `relay-socket.test.ts` probeNow · `stock-native-refresh.test.tsx` (21-07) · `recent-search.test.ts` · `search-page-client.test.tsx` · `app-sidebar.test.tsx` 보강 (21-08) · `account-card.test.tsx` · `app-shell-chrome.test.tsx` 보강 (21-09) · `native-google-login.test.ts` · `login/__tests__/page.test.tsx` (21-15) · `TabRoutesTest.kt` (21-12) | 단위 | 각 플랜 |
| e2e: `fixtures/native-app.ts` — `installNativeApp` · `nativeMessages` (21-05) · `specs/native-shell.spec.ts` (21-05 · 21-06) · `specs/search-page.spec.ts` (21-08) · `specs/brand-account.spec.ts` (21-09) | Playwright | 각 플랜 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-07-SUMMARY.md` when done
</output>
