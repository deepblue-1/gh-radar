---
phase: 21-gh-trade-mobile-app
plan: 15
type: execute
wave: 6
depends_on: ["21-03", "21-09", "21-11", "21-13"]
files_modified:
  - webapp/src/lib/native/native-google-login.ts
  - webapp/src/lib/native/__tests__/native-google-login.test.ts
  - webapp/src/app/login/page.tsx
  - webapp/src/app/login/__tests__/page.test.tsx
  - mobile/package.json
  - pnpm-lock.yaml
  - mobile/capacitor.config.ts
  - mobile/ios/App/App/Info.plist
  - mobile/ios/App/CapApp-SPM/Package.swift
  - mobile/android/capacitor.settings.gradle
  - mobile/android/app/capacitor.build.gradle
  - mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
autonomous: true
requirements: [MOBILE-01]
user_setup:
  - service: google-cloud-oauth
    why: "21-03 에서 만든 iOS·Android OAuth 클라이언트와 Supabase Authorized Client IDs 가 선행 조건"

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-03: 앱(`html.native-app`)의 `/login` 「Google로 로그인」은 `signInWithOAuth`(WebView OAuth — Google 이 `disallowed_userAgent` 로 차단) 대신 네이티브 플러그인 `SocialLogin`(`@capgo/capacitor-social-login@8.5.11`)으로 id_token 을 받아 `supabase.auth.signInWithIdToken({ provider: 'google', token, nonce })` 로 세션을 만들고, 성공하면 `location.replace(safeNext)` 하드 내비 — 브라우저 경로는 종전 그대로"
    - "D-03 보안: nonce = `crypto.getRandomValues` 32바이트 hex(raw) · 플러그인에는 `SHA-256(raw)` hex, Supabase 에는 raw · `forcePrompt: true`(iOS 이전 로그인 복원이 nonce 없는 옛 토큰을 돌려주는 문제 — Pitfall 6) · UA 위장 없음 · id_token 을 로그에 남기지 않음"
    - "D-03: webapp 에 `@capacitor/core`·플러그인 npm 패키지를 추가하지 않는다 — 원격 페이지에 주입된 `window.Capacitor.nativePromise('SocialLogin', …)` 를 얇은 타입 래퍼로 호출한다(RESEARCH Alternatives)"
    - "D-03 · Claude's Discretion: 앱 로그인 실패 문구는 기존 맵을 재사용한다 — 사용자 취소 → `oauth_denied`, 그 외 → `auth_failed` · 진행 중에는 버튼 비활성 · 실패는 `[gh-radar]` 접두 console.error 로 남긴다 · `?next=` 는 기존 `safeNext` 가드(`/` 시작 · `//` 금지)"
    - "D-03 네이티브: `mobile/` 에 플러그인이 정확 버전으로 설치되고 `capacitor.config.ts` `plugins.SocialLogin.providers` = google 만 true · iOS Info.plist `CFBundleURLTypes` 에 역순 iOS 클라이언트 ID(`GOOGLE_IOS_URL_SCHEME`) · 두 플랫폼 빌드가 통과한다"
  artifacts:
    - path: webapp/src/lib/native/native-google-login.ts
      provides: "nativeGoogleSignIn · randomNonce · sha256Hex · classifyNativeLoginError"
      exports: ["nativeGoogleSignIn", "randomNonce", "sha256Hex", "classifyNativeLoginError"]
    - path: webapp/src/app/login/__tests__/page.test.tsx
      provides: "로그인 분기 단위 테스트(MOBILE-01f)"
    - path: mobile/capacitor.config.ts
      provides: "SocialLogin providers google"
      contains: "SocialLogin"
  key_links:
    - from: webapp/src/app/login/page.tsx
      to: webapp/src/lib/native/native-google-login.ts
      via: "isNativeApp() ? nativeGoogleSignIn(createClient()) : signInWithOAuth(...)"
      pattern: "nativeGoogleSignIn\\("
    - from: webapp/src/lib/native/native-google-login.ts
      to: webapp/src/lib/native/google-client-ids.ts
      via: "initialize { webClientId, iOSClientId, iOSServerClientId }"
      pattern: "GOOGLE_IOS_CLIENT_ID"
    - from: webapp/src/lib/native/native-google-login.ts
      to: mobile/capacitor.config.ts
      via: "window.Capacitor.nativePromise('SocialLogin', 'login', …) → 설치된 플러그인"
      pattern: "nativePromise"
---

<assumption_delta_decision>
- 검출기 `assumption-delta scan 21` → `detected: false`. 수동 판정: 두 번째 **인증 방식**(네이티브 id_token)이 더해지지만 세션 정본은 그대로 Supabase 쿠키 세션(`@supabase/ssr` 브라우저 클라이언트)이다 — 정본 명사 **Supabase 세션**, 결정 **add-alongside**(발급 경로만 곁에 붙고 middleware·`onAuthStateChange`·로그아웃은 무변경). promote 를 강제할 신호: 이메일/비번·Apple 등 제3 방식이 들어와 로그인 화면이 방식 선택 모델이 될 때(App Store 4.8 — RESEARCH Open Q8).
</assumption_delta_decision>

<objective>
D-03 네이티브 Google 로그인을 끝까지 잇는다: 웹 서비스 함수(nonce·해시·플러그인 호출·signInWithIdToken) → `/login` 앱 분기 → `mobile/` 플러그인 설치·설정·양 플랫폼 빌드.

Purpose: Google 이 WebView OAuth 를 막으므로 앱에서는 이 경로가 없으면 로그인 자체가 불가능하다. 세션은 브라우저 클라이언트가 쿠키로 쓰므로 middleware·기존 흐름이 그대로 이어진다.
Output: 서비스 + 페이지 분기 + 테스트 2 + 플러그인 설치·설정.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md
@.planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md
@.planning/phases/21-gh-trade-mobile-app/21-01-SUMMARY.md
@.planning/phases/21-gh-trade-mobile-app/21-03-SUMMARY.md
@webapp/src/app/login/page.tsx
@webapp/src/lib/native/google-client-ids.ts

<interfaces>
기존 계약(코드에서 추출):

```tsx
// webapp/src/app/login/page.tsx (21-09 이후)
const ERROR_MESSAGES: Record<string, string> = { auth_failed: "로그인 처리에 실패했습니다. 잠시 후 다시 시도해주세요.",
  oauth_denied: "Google 로그인을 취소하셨습니다. 계속하려면 다시 시도해주세요.", session_expired: "…", unknown: "…" };
const safeNext = rawNext && rawNext.startsWith("/") && !rawNext.startsWith("//") ? rawNext : "/";
const handleGoogleLogin = async () => { const supabase = createClient(); await supabase.auth.signInWithOAuth({ provider: "google",
  options: { redirectTo: `${window.location.origin}/auth/callback?next=${encodeURIComponent(safeNext)}`, queryParams: { prompt: "select_account" } } }); };
// <CardTitle>GH Trade에 로그인</CardTitle> · <Button onClick={handleGoogleLogin} aria-label="Google로 로그인">
// webapp/src/lib/supabase/client.ts: export function createClient() — @supabase/ssr createBrowserClient
// webapp/src/lib/native/native-detect.ts: export function isNativeApp(): boolean
// webapp/src/lib/native/google-client-ids.ts (21-03): GOOGLE_WEB_CLIENT_ID · GOOGLE_IOS_CLIENT_ID · GOOGLE_ANDROID_CLIENT_ID · GOOGLE_IOS_URL_SCHEME
// @supabase/auth-js 2.103: signInWithIdToken({ provider, token, nonce?, access_token? }) → { data, error }
```

`@capgo/capacitor-social-login@8.5.11`(RESEARCH Pattern 7 — 소스 확인): `initialize({ google: { webClientId, iOSClientId, iOSServerClientId, mode: 'online' } })` · `login({ provider: 'google', options: { nonce, forcePrompt } })` → `{ result: { idToken?: string|null, accessToken?: { token } | null } }`(결과는 `result.idToken` — `login.idToken` 아님) · JS 래퍼는 google 경로에서 인자를 그대로 넘긴다 → `window.Capacitor.nativePromise('SocialLogin', method, options)` 와 동치 · `capacitor:sync:before` 훅이 비활성 provider 를 스스로 정리한다 · postinstall 없음.

```ts
// 이 플랜이 새로 만드는 계약
export function randomNonce(bytes?: number): string;                 // 기본 32바이트 → 64 hex
export async function sha256Hex(s: string): Promise<string>;         // crypto.subtle — 보안 출처 전용(Pitfall 16)
export type NativeLoginErrorKey = 'oauth_denied' | 'auth_failed';
export function classifyNativeLoginError(err: unknown): NativeLoginErrorKey;
export async function nativeGoogleSignIn(supabase: SupabaseClient): Promise<{ error: Error | null }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `native-google-login.ts` — nonce(raw↔SHA-256) · nativePromise 래퍼 · signInWithIdToken (D-03)</name>
  <precondition>`webapp/src/lib/native/google-client-ids.ts` 가 있고 21-03 테스트가 통과한다</precondition>
  <files>webapp/src/lib/native/native-google-login.ts, webapp/src/lib/native/__tests__/native-google-login.test.ts</files>
  <read_first>
    - webapp/src/lib/native/google-client-ids.ts (21-03)
    - webapp/src/lib/supabase/client.ts (`createClient` 반환 타입)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pattern 7 전문 · Pitfall 6·16 · Assumptions A5·A14 · Security Domain V2/V6)
  </read_first>
  <behavior>
    - `randomNonce()` → 64자 `[0-9a-f]` · 두 번 호출 결과가 다르다
    - `sha256Hex('abc')` → `ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad`
    - `nativeGoogleSignIn(supabaseMock)`(가짜 `window.Capacitor.nativePromise`): 첫 호출 `('SocialLogin','initialize',{ google: { webClientId: GOOGLE_WEB_CLIENT_ID, iOSClientId: GOOGLE_IOS_CLIENT_ID, iOSServerClientId: GOOGLE_WEB_CLIENT_ID, mode: 'online' } })` · 둘째 `('SocialLogin','login',{ provider:'google', options:{ nonce: H, forcePrompt: true } })` · `signInWithIdToken` 인자 `{ provider:'google', token: <idToken>, nonce: R }` 이고 `sha256Hex(R) === H`
    - 플러그인 결과에 `accessToken.token` 이 있으면 `access_token` 도 넘기고, null 이면 넘기지 않는다
    - `result.idToken` 이 없으면 `Error('no_id_token')` 로 reject · `signInWithIdToken` 이 `{ error }` 를 돌려주면 그대로 `{ error }`
    - `classifyNativeLoginError`: 메시지/코드에 `cancel`(대소문자 무시) · `USER_CANCELLED` · `12501` · `-5` 가 있으면 `oauth_denied`, 그 외 `auth_failed`
    - `window.Capacitor` 에 `nativePromise` 가 없으면 `Error('native_unavailable')` 로 reject
  </behavior>
  <action>
    파일 머리 한국어 주석: D-03 · nonce 방향(플러그인=해시, Supabase=raw — auth-js 타입 주석 근거) · `forcePrompt` 이유(Pitfall 6) · 의존성 추가 없이 `nativePromise` 를 쓰는 이유 · 보안 출처 전용(LAN IP dev 에서는 동작하지 않는다 — Pitfall 16) · id_token 을 어떤 로그에도 남기지 않는다. 구현은 RESEARCH Pattern 7 그대로(`<interfaces>` 이름) — 단 로그인 옵션에 `scopes` 는 넣지 않는다(플러그인 기본 범위가 openid·email·profile 이고, 명시 scopes 는 Android 에서 추가 Activity 구현을 요구할 수 있다 — Task 3 에서 README 로 확인해 기록). 창 타입은 파일 안 지역 타입 캐스팅(전역 선언 확장 금지). 테스트를 먼저 쓴다(RED): jsdom 에 `crypto.subtle` 이 없으면 `vi.stubGlobal('crypto', (await import('node:crypto')).webcrypto)` · `window.Capacitor = { nativePromise: vi.fn(...) }` · `supabase = { auth: { signInWithIdToken: vi.fn() } }`.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/native/__tests__/native-google-login.test.ts</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or "native-google-login.test.ts" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "forcePrompt: true" webapp/src/lib/native/native-google-login.ts` 가 1줄 · `grep -n "crypto.subtle.digest('SHA-256'" webapp/src/lib/native/native-google-login.ts` 가 1줄 · `grep -n "getRandomValues" webapp/src/lib/native/native-google-login.ts` 가 1줄
    - `grep -n "signInWithIdToken" webapp/src/lib/native/native-google-login.ts` 가 1줄 이상
    - `grep -cE "console\.(log|info|debug)\(.*(idToken|token)" webapp/src/lib/native/native-google-login.ts` 가 0
    - `node -e "const p=require('./webapp/package.json');const d={...p.dependencies,...p.devDependencies};process.exit(Object.keys(d).some(k=>k.startsWith('@capacitor/')||k.startsWith('@capgo/'))?1:0)"` exit 0
  </acceptance_criteria>
  <done>네이티브 id_token → Supabase 세션 경로가 nonce 규칙·오류 분류와 함께 단위 테스트로 잠겼고 webapp 의존성은 늘지 않았다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `/login` 앱 분기 — 네이티브면 `nativeGoogleSignIn` → `location.replace(safeNext)`, 브라우저는 종전 OAuth (D-03)</name>
  <files>webapp/src/app/login/page.tsx, webapp/src/app/login/__tests__/page.test.tsx</files>
  <read_first>
    - webapp/src/app/login/page.tsx (전체 — ERROR_MESSAGES · safeNext · 버튼)
    - webapp/src/lib/auth-context.tsx (signOut 하드 리다이렉트 선례)
    - webapp/src/lib/native/native-google-login.ts (Task 1)
    - .planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md (§native-google-login + login/page 수정)
  </read_first>
  <behavior>
    - 브라우저(`isNativeApp` false): 클릭 → `signInWithOAuth` 1회, `redirectTo` 가 `/auth/callback?next=<safeNext 인코딩>` · `queryParams.prompt === 'select_account'` · `nativeGoogleSignIn` 0
    - 앱(true) + 성공: `nativeGoogleSignIn` 1회 · `signInWithOAuth` 0 · `window.location.replace('/me')`(`?next=/me`) 1회
    - 앱 + `?next=//evil.com` → `location.replace('/')`
    - 앱 + 취소(reject, 분류 `oauth_denied`) → `role="alert"` 에 「Google 로그인을 취소하셨습니다. 계속하려면 다시 시도해주세요.」 · `location.replace` 0
    - 앱 + `{ error }` 반환 → 「로그인 처리에 실패했습니다. 잠시 후 다시 시도해주세요.」
    - 진행 중 버튼 `disabled` · 두 번째 클릭이 두 번째 호출을 만들지 않는다
    - 제목 「GH Trade에 로그인」
  </behavior>
  <action>
    `login/page.tsx`: `useState` 로 `pending` · `nativeErrorKey` 를 두고 `handleGoogleLogin` 첫 줄에서 `if (pending) return;`. `isNativeApp()`(`@/lib/native/native-detect`)이 참이면 `setPending(true)` → `try { const { error } = await nativeGoogleSignIn(createClient()); if (error) { console.error('[gh-radar] native google login: supabase', error.message); setNativeErrorKey('auth_failed'); return; } window.location.replace(safeNext); } catch (err) { console.error('[gh-radar] native google login failed', err instanceof Error ? err.message : err); setNativeErrorKey(classifyNativeLoginError(err)); } finally { setPending(false); }` 후 return — 브라우저 경로(`signInWithOAuth`)는 그대로. 표시 오류 = `nativeErrorKey ?? errorKey` 를 기존 `ERROR_MESSAGES` 로(새 문구 없음). 버튼에 `disabled={pending}` · `aria-busy={pending}`. 머리 주석 목록에 D-03(Phase 21) 한 줄. 테스트를 먼저 쓴다(RED): `src/app/login/__tests__/page.test.tsx` — `vi.mock('next/navigation')`(`useSearchParams` 가변) · `vi.mock('@/lib/supabase/client')`(`signInWithOAuth` 스파이) · `vi.mock('@/lib/native/native-detect')` · `vi.mock('@/lib/native/native-google-login', async (orig) => ({ ...(await orig()), nativeGoogleSignIn: vi.fn() }))` · `window.location.replace` 는 `Object.defineProperty(window, 'location', …)` 로 스텁 후 원복.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/app/login/__tests__/page.test.tsx src/lib/native</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or "page.test.tsx" is absent from the output</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "isNativeApp()" webapp/src/app/login/page.tsx` 가 1줄 · `grep -n "nativeGoogleSignIn(createClient())" webapp/src/app/login/page.tsx` 가 1줄
    - `grep -n "window.location.replace(safeNext)" webapp/src/app/login/page.tsx` 가 1줄 · `grep -n "signInWithOAuth" webapp/src/app/login/page.tsx` 가 1줄 이상(브라우저 경로 유지)
    - `grep -n "disabled={pending}" webapp/src/app/login/page.tsx` 가 1줄
  </acceptance_criteria>
  <done>앱에서는 네이티브 로그인, 브라우저에서는 종전 OAuth 로 가는 분기가 오류 문구·오픈 리다이렉트 가드와 함께 잠겼다.</done>
</task>

<task type="auto">
  <name>Task 3: `mobile/` 플러그인 설치(8.5.11 exact) · `SocialLogin` 설정 · iOS URL scheme · Android 요건 확인 · 양 플랫폼 빌드 (D-03)</name>
  <precondition>21-01 Task 2 가 `@capgo/capacitor-social-login@8.5.11`(또는 지정 다운그레이드 버전)을 승인했다(21-01 SUMMARY)</precondition>
  <files>mobile/package.json, pnpm-lock.yaml, mobile/capacitor.config.ts, mobile/ios/App/App/Info.plist, mobile/ios/App/CapApp-SPM/Package.swift, mobile/android/capacitor.settings.gradle, mobile/android/app/capacitor.build.gradle, mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt</files>
  <read_first>
    - mobile/capacitor.config.ts (21-01)
    - mobile/ios/App/App/Info.plist (21-11 이후)
    - webapp/src/lib/native/google-client-ids.ts (`GOOGLE_IOS_URL_SCHEME` 값)
    - mobile/node_modules/@capgo/capacitor-social-login/README.md (설치 후 — 「Google」 iOS·Android 절 · Troubleshooting)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Standard Stack · Package Legitimacy Audit · Pattern 7 · Pitfall 17)
  </read_first>
  <action>
    ① `pnpm --filter @gh-radar/mobile add --save-exact @capgo/capacitor-social-login@8.5.11`(승인 버전). 이 플랜의 lockfile 변경은 이 한 번뿐. `@capacitor/app`·`@capacitor/splash-screen`·`@capacitor/status-bar` 는 여전히 설치하지 않는다.
    ② `capacitor.config.ts` `plugins` 에 `SocialLogin: { providers: { google: true, facebook: false, apple: false, twitter: false } }` 를 더한다(RESEARCH Pattern 9 · 노출 플러그인 최소 — T-21-15). 금지 키 규칙(21-01)은 그대로.
    ③ iOS: `Info.plist` 에 `CFBundleURLTypes` → 항목 1개(`CFBundleURLName` `google` · `CFBundleURLSchemes` = [`GOOGLE_IOS_URL_SCHEME` 값]) 를 더한다(capgo iOS 문서 — 역순 iOS 클라이언트 ID). README 가 요구하는 다른 iOS 항목이 있으면 그대로 따르고 SUMMARY 에 적는다(템플릿 AppDelegate/SceneDelegate 는 Capacitor 프록시로 URL 을 넘긴다 — 추가 코드가 필요하면 README 지시대로).
    ④ Android: README 「Google」 Android 절을 읽고, `mode: 'online'` · 명시 scopes 없음(Task 1) 구성에서 `MainActivity` 가 추가 인터페이스(예: 모디파이드 메인 액티비티 인터페이스)를 구현해야 하는지 확인한다 — 요구하면 README 대로 `MainActivity.kt` 에 구현하고, 요구하지 않으면 `MainActivity.kt` 는 건드리지 않는다. 결정과 근거를 SUMMARY 에 적는다.
    ⑤ `pnpm --filter @gh-radar/mobile exec cap sync`(플러그인의 `capacitor:sync:before` 훅이 비활성 provider 를 정리 · SPM `Package.swift`·`capacitor.settings.gradle`·`app/capacitor.build.gradle` 갱신 — 경로에 버전 해시가 들어가므로 생성 파일을 커밋, Pitfall 17) → iOS·Android 빌드. iOS 첫 빌드는 GoogleSignIn-iOS(≥9.0.0)·AppAuth·GTMSessionFetcher SPM 해석으로 수 분 — timeout 600000.
    ⑥ 실제 계정 선택·세션 생성은 시뮬레이터/에뮬레이터 UAT(21-16)에서 확인한다 — 이 태스크는 컴파일·설정까지.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:ios</automated>
    <fails_when>non-zero exit, or the output lacks "BUILD SUCCEEDED"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:android</automated>
    <fails_when>non-zero exit, or the output lacks "BUILD SUCCESSFUL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./mobile/package.json');process.exit(p.dependencies['@capgo/capacitor-social-login']==='8.5.11'?0:1)"` exit 0 (다운그레이드 승인 시 그 버전)
    - `grep -n "SocialLogin" mobile/capacitor.config.ts` 가 1줄 이상 · `grep -n "google: true" mobile/capacitor.config.ts` 가 1줄
    - `plutil -extract CFBundleURLTypes json -o - mobile/ios/App/App/Info.plist` 출력에 `com.googleusercontent.apps.` 가 있다
    - `grep -cE 'errorPath|allowNavigation|webContentsDebuggingEnabled|CapacitorHttp|CapacitorCookies' mobile/capacitor.config.ts` 가 0 (21-01 금지 키 유지)
    - `grep -n "capgo" mobile/ios/App/CapApp-SPM/Package.swift` 가 1줄 이상
  </acceptance_criteria>
  <done>네이티브 Google 로그인 플러그인이 두 앱에 들어가 설정·컴파일됐고, 웹 분기와 함께 시뮬레이터 UAT 만 남았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 네이티브 Google SDK → 웹(id_token) | 플러그인이 id_token 을 원격 페이지 JS 로 돌려준다 |
| 웹 → Supabase Auth(`signInWithIdToken`) | 서버가 서명·iss·aud·exp·nonce 를 검증하는 신뢰 경계 |
| 원격 페이지 JS → 네이티브 플러그인(`nativePromise`) | XSS 가 플러그인을 부를 수 있다 |
| npm → `mobile/` | 새 플러그인 1개 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-07 | Spoofing(재전송) | id_token replay | high | mitigate | raw nonce 32바이트 `getRandomValues` · 플러그인엔 SHA-256 · Supabase 가 검증 · 전역 Skip nonce off(21-03) |
| T-21-10 | Spoofing | iOS 이전 로그인 복원 토큰(nonce 없음) | high | mitigate | `forcePrompt: true`(Task 1 · acceptance grep) |
| T-21-08 | Spoofing | aud 혼동 | high | mitigate | Authorized Client IDs 에 우리 3개만(21-03) · 서버 검증 |
| T-21-11 | Tampering(오픈 리다이렉트) | 로그인 뒤 `location.replace` | medium | mitigate | 기존 `safeNext` 가드 재사용 · 테스트(`//evil.com` → `/`) |
| T-21-15 | Elevation of Privilege | XSS → 플러그인 호출 | medium | mitigate | 노출 플러그인 최소(SocialLogin google 만 · SystemBars) · 기존 XSS 방어 유지 · 플러그인은 사용자 계정 선택 UI 를 거친다 |
| T-21-42 | Information Disclosure | id_token·access_token 로그 유출 | medium | mitigate | 토큰을 console 에 남기지 않음(acceptance grep 0) · 오류는 메시지만 |
| T-21-SC | Tampering | `@capgo/capacitor-social-login@8.5.11` [SUS too-new] | high | mitigate | 21-01 Task 2 blocking-human 정당성 게이트 · `--save-exact` · postinstall 없음 · sync 훅은 자기 파일만 수정 |
</threat_model>

<verification>
- native-google-login · login page 단위 green · webapp typecheck · iOS/Android 빌드 green
- 실제 로그인(첫 로그인 · 재로그인 · 로그아웃 후 재로그인)은 21-16 UAT
</verification>

<success_criteria>
- D-03 이 웹·네이티브 양쪽에서 구현됐고, 브라우저 로그인은 회귀가 없다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/` 패키지 `@gh-radar/mobile` — scripts `native:sync` · `native:sync:dev` · `native:build:ios` · `native:build:android` · `native:smoke:ios` · `native:open:ios` · `native:open:android` · `native:assets` (21-01) · `native:smoke:android` (21-02) · `native:check-tab-routes:ios` (21-10) · `native:test:android` (21-12) · `native:verify-prod` (21-16) | 패키지·스크립트 | 21-01 · 21-02 · 21-10 · 21-12 · 21-16 |
| `mobile/capacitor.config.ts` — appId `com.ghtrade.app` · appName `GH Trade` · `CAP_SERVER_URL` · `SystemBars` (21-01) · `SocialLogin` (21-15) | 설정 | 21-01 · 21-15 |
| `mobile/tsconfig.json` · `mobile/.gitignore` | 설정 | 21-01 |
| `mobile/www/index.html` — 오프라인 폴백(최소 21-01 · `safeTarget`·5초 탐침·`online`·테마 21-11) | 정적 페이지 | 21-01 · 21-11 |
| `mobile/scripts/smoke-ios.sh` · `mobile/scripts/smoke-android.sh` — 「SMOKE OK ready platform=…」 | 스모크 | 21-01 · 21-02 |
| `mobile/scripts/check-tab-routes-ios.sh` · `mobile/scripts/tab-routes-check.swift` | Swift 경로표 검사 | 21-10 |
| `mobile/scripts/render-resources.mjs` · `mobile/resources/{icon.svg,icon-only.png,icon-foreground.png,icon-background.png,splash.png,splash-dark.png}` | 에셋 원본 | 21-14 |
| `mobile/scripts/verify-prod-config.mjs` · `mobile/README.md` | 운영 설정 검사·문서 | 21-16 |
| iOS `SceneDelegate.swift` (루트 = `GHTradeBridgeViewController()` · 창 배경 21-11) | Swift | 21-01 · 21-11 |
| iOS `GHTradeBridgeViewController` · `WeakScriptMessageHandler` · `NativeMessageType` | Swift | 21-01 · 21-10 · 21-11 |
| iOS `GHTradeTabBar` · `GHTradeTab` · `GHTabID` · `GHTradePalette` · `GHTradeTheme` · `TabRoutes.activeTab(forPath:)` · `TabRoutes.hidesTabBar(path:)` | Swift | 21-10 |
| iOS `NavigationDelegateProxy` · `ThemeStore` (UserDefaults `gh-trade.theme`) | Swift | 21-11 |
| Android `MainActivity` (`BridgeActivity`) · `GhTradeWebViewClient` (`BridgeWebViewClient` 상속) · `GhTradeBridge` (`@JavascriptInterface postMessage`) | Kotlin | 21-02 · 21-12 · 21-13 |
| Android `GhTradeTabBar` · `GhTradePalette` · `TabId` · `TabRoutes.activeTabFor` · `TabRoutes.hidesTabBar` · `TabRoutesTest` | Kotlin | 21-12 |
| Android `ThemeStore` (SharedPreferences `gh_trade`/`theme`) · `res/values/bools.xml` · `res/values-sw600dp/bools.xml` (`is_tablet`) | Kotlin·리소스 | 21-13 |
| Android `res/drawable/ic_tab_{home,home_fill,search,search_fill,trading,trading_fill,ai,ai_fill,me,me_fill}.xml` | 벡터 | 21-12 |
| 웹 `lib/native/native-detect.ts` — `NATIVE_DETECT_SCRIPT` · `isNativeApp` · `nativePlatform` | 유틸 | 21-01 |
| 웹 `lib/native/post-native.ts` — `postNative` · `NativeMsgType` | 유틸 | 21-04 |
| 웹 `lib/native/native-bridge-provider.tsx` — `NativeBridgeProvider` · `useNativeBridge` · `NativeRefreshFn` · `window.__ghTrade = { refresh, navigate, back }` | Provider | 21-04 |
| 웹 `lib/native/use-native-refresh.ts` — `useNativeRefresh` | 훅 | 21-04 |
| 웹 `lib/native/native-overlay-marker.tsx` — `NativeOverlayMarker` | 컴포넌트 | 21-04 |
| 웹 `lib/native/google-client-ids.ts` — `GOOGLE_WEB_CLIENT_ID` · `GOOGLE_IOS_CLIENT_ID` · `GOOGLE_ANDROID_CLIENT_ID` · `GOOGLE_IOS_URL_SCHEME` | 공개 식별자 상수 | 21-03 |
| 웹 `lib/native/native-google-login.ts` — `nativeGoogleSignIn` · `randomNonce` · `sha256Hex` · `classifyNativeLoginError` | 서비스 | 21-15 |
| 웹 `RelayConnectionState.probeNow` · `EMPTY_RELAY_VALUE.probeNow` | 훅 계약 | 21-07 |
| 웹 `lib/recent-search.ts` — `RECENT_SEARCH_KEY` · `RECENT_SEARCH_MAX` · `RecentSearchItem` · `readRecentSearches` · `pushRecentSearch` · `removeRecentSearch` · `clearRecentSearches` | 유틸 | 21-08 |
| 웹 `app/search/page.tsx` · `components/search/search-page-client.tsx` — `SearchPageClient` · 사이드바 `NAV_SEARCH_PAGE` | 페이지·상수 | 21-08 |
| 웹 `components/me/account-card.tsx` — `AccountCard` | 컴포넌트 | 21-09 |
| 웹 마크업 계약 `data-slot="app-aside"` · `data-slot="app-menu-button"` · `data-slot="stock-ai-button"` · `data-slot="shared-panels"` · `data-pinned` · `data-order-cta` · `data-slot="account-card"` | 마크업 | 21-05 · 21-06 · 21-09 |
| CSS `@custom-variant native` (21-04) · 앱 셸 규칙 (21-05) · `--app-safe-top` · `--app-safe-bottom` · `--app-safe-left` · `--app-safe-right` · `--native-tabbar-gap` · `--native-tabbar-offset` · `--native-body-reserve` (21-06) | 토큰·변형 | 21-04 · 21-05 · 21-06 |
| 웹 `app/icon.svg` (= 스케치 004 `#app-a`) | 파비콘 | 21-14 |
| 단위 테스트: `native-detect.test.ts` (21-01) · `post-native.test.ts` · `native-bridge-provider.test.tsx` · `native-overlay.test.tsx` (21-04) · `google-client-ids.test.ts` (21-03) · `relay-socket.test.ts` probeNow · `stock-native-refresh.test.tsx` (21-07) · `recent-search.test.ts` · `search-page-client.test.tsx` · `app-sidebar.test.tsx` 보강 (21-08) · `account-card.test.tsx` · `app-shell-chrome.test.tsx` 보강 (21-09) · `native-google-login.test.ts` · `login/__tests__/page.test.tsx` (21-15) · `TabRoutesTest.kt` (21-12) | 단위 | 각 플랜 |
| e2e: `fixtures/native-app.ts` — `installNativeApp` · `nativeMessages` (21-05) · `specs/native-shell.spec.ts` (21-05 · 21-06) · `specs/search-page.spec.ts` (21-08) · `specs/brand-account.spec.ts` (21-09) | Playwright | 각 플랜 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-15-SUMMARY.md` when done — Android 추가 요건 확인 결과와 iOS 추가 항목을 적는다.
</output>
