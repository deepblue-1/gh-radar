---
phase: 21-gh-trade-mobile-app
plan: 19
type: execute
wave: 8
depends_on: ["21-16"]
files_modified:
  - webapp/src/components/layout/theme-toggle.tsx
  - webapp/src/components/layout/__tests__/theme-toggle.test.tsx
  - webapp/src/components/layout/__tests__/app-sidebar.test.tsx
  - webapp/src/components/me/account-card.tsx
  - webapp/src/components/me/__tests__/account-card.test.tsx
  - webapp/e2e/specs/brand-account.spec.ts
autonomous: true
gap_closure: true
requirements: [MOBILE-01]
coupling_justified:
  - "21-20: 21-20 Task 3 이 이 플랜의 규칙을 CONTEXT D-08b 로 기록한다 — 문서 기록은 구현 순서와 무관"
  - "21-18: 21-18 이 기본 테마를 dark 로 바꾼다 — brand-account e2e 는 시작 테마를 읽어 양방향으로 토글하므로(wasDark 분기) 어느 순서로도 통과한다"

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "G-21-N2 · D-08b: 사이드바 ThemeToggle 과 /me 계정 카드 테마 버튼이 같은 규칙 — 아이콘 = 누르면 바뀔 테마(목적지): 다크일 때 Sun(해) · 라이트일 때 Moon(달)"
    - "두 버튼의 접근 이름(aria-label · 툴팁 title)은 행동 문구 — 다크일 때 「라이트 모드로 전환」 · 라이트일 때 「다크 모드로 전환」, 두 곳이 같은 문자열"
    - "규칙은 한 곳에 정의된다 — theme-toggle.tsx 가 export 하는 ThemeSwitchIcon · THEME_SWITCH_LABEL 을 AccountCard 가 가져다 쓴다(다시 갈라지지 않게)"
    - "데스크톱 /me 에서 사이드바 토글과 계정 카드 버튼의 아이콘(data-icon)과 접근 이름이 같다(e2e) · 누르면 그 테마로 바뀐다"
  artifacts:
    - path: webapp/src/components/layout/theme-toggle.tsx
      provides: "ThemeToggle(목적지 아이콘 · 행동 문구) · export ThemeSwitchIcon · THEME_SWITCH_LABEL"
      contains: "export const THEME_SWITCH_LABEL"
    - path: webapp/src/components/layout/__tests__/theme-toggle.test.tsx
      provides: "사이드바 토글 목적지 아이콘·행동 문구 단위 테스트"
      min_lines: 40
    - path: webapp/src/components/me/account-card.tsx
      provides: "계정 카드 테마 버튼 — 공용 규칙 사용"
      contains: "THEME_SWITCH_LABEL"
  key_links:
    - from: webapp/src/components/me/account-card.tsx
      to: webapp/src/components/layout/theme-toggle.tsx
      via: "import { ThemeSwitchIcon, THEME_SWITCH_LABEL } — 아이콘·접근 이름 단일 정의"
      pattern: "from ['\"]@/components/layout/theme-toggle['\"]"
    - from: webapp/e2e/specs/brand-account.spec.ts
      to: webapp/src/components/layout/theme-toggle.tsx
      via: "데스크톱 /me 사이드바 토글 ↔ 계정 카드 버튼 data-icon · 접근 이름 동등 단언"
      pattern: "data-icon"
  prohibitions:
    - "MUST NOT 같은 테마 상태에서 사이드바 토글과 계정 카드가 서로 다른 아이콘을 보인다"
    - "MUST NOT 아이콘은 목적지를 보이면서 접근 이름은 현재 상태를 말한다(스크린리더와 시각 표현 불일치)"
    - "MUST NOT 아이콘·문구 규칙을 두 컴포넌트에 따로 적어 다시 갈라질 여지를 남긴다"
---

<objective>
G-21-N2: 테마 아이콘 규칙을 하나로 통일한다 — 두 곳 모두 **누르면 바뀔 테마(목적지)**를 보인다(21-UAT.md G-21-N2 사용자 결정 2026-09-26 · CONTEXT D-08b 는 21-20 이 기록). 사이드바 `ThemeToggle` 을 계정 카드 규칙에 맞추고, 접근 이름은 두 곳 모두 행동 문구(「다크 모드로 전환」/「라이트 모드로 전환」)로 바꾼다. 규칙(아이콘 + 문구)은 `theme-toggle.tsx` 한 곳에서 export 해 계정 카드가 가져다 쓴다.

Purpose: 한 화면에 두 테마 버튼이 반대 아이콘을 보이던 혼란(UAT 2차 신규 요청)을 없애고, 아이콘이 행동을 예고하는 흔한 관례(macOS · GitHub · 문서 사이트)를 따른다.
Output: `theme-toggle.tsx`(규칙 export) · `account-card.tsx`(공용 규칙 사용) · 단위 테스트 신규 1 + 갱신 2 · e2e 갱신 1(데스크톱 동등성 추가).

기본 테마 변경(G-21-N1)은 21-18 이 한다 — 이 플랜의 테스트는 두 테마 상태를 모두 명시적으로 설정하므로 기본값과 무관하다.

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add · 커밋 직전 `git status -sb` · **push 금지**(21-24 체크포인트에서 사용자 결정).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-UAT.md
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@webapp/src/components/layout/theme-toggle.tsx
@webapp/src/components/me/account-card.tsx
@webapp/src/components/me/__tests__/account-card.test.tsx
@webapp/src/components/layout/__tests__/app-sidebar.test.tsx
@webapp/e2e/specs/brand-account.spec.ts

<interfaces>
```text
theme-toggle.tsx (현행): type ThemeValue = 'light' | 'dark' · LABELS(현재 상태 문구) · ThemeToggle({ className }) —
  mounted 가드 전에는 current = 'dark' · nextTheme = 반대 · Icon = 현재 상태(Sun/Moon) · aria-label·title = 현재 상태 문구
account-card.tsx (현행): current/nextTheme 같은 방식 · 다크면 <Sun data-icon="sun"> · 라이트면 <Moon data-icon="moon"> (이미 목적지 규칙)
  · 버튼 aria-label 은 상태·행동을 말하지 않는 일반 문구
사용처: app-sidebar.tsx 456행 <ThemeToggle className="size-9 shrink-0" /> · app-header.tsx 115행 <ThemeToggle /> (hideSidebar 셸)
app-sidebar.test.tsx ⑧ · ⑧-b: 토글을 옛 괄호 안내 문구 정규식으로 찾는다(731 · 753행)
brand-account.spec.ts: 계정 카드 버튼을 옛 일반 문구 exact 로 찾아 두 번 토글(wasDark 분기) · 앱 모드 390 뷰포트 안 단언
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 목적지 규칙을 theme-toggle.tsx 에 정의(ThemeSwitchIcon · THEME_SWITCH_LABEL export) + 사이드바 토글 적용 + 단위 테스트</name>
  <files>webapp/src/components/layout/theme-toggle.tsx, webapp/src/components/layout/__tests__/theme-toggle.test.tsx, webapp/src/components/layout/__tests__/app-sidebar.test.tsx</files>
  <read_first>
    - webapp/src/components/layout/theme-toggle.tsx (전체 — mounted 가드 · className 계약 · suppressHydrationWarning)
    - webapp/src/components/me/account-card.tsx (23 · 95~99행 목적지 규칙과 data-icon 관례)
    - webapp/src/components/me/__tests__/account-card.test.tsx (next-themes 모킹 모양)
    - webapp/src/components/layout/__tests__/app-sidebar.test.tsx (720~755행 ⑧ · ⑧-b)
  </read_first>
  <behavior>
    - resolvedTheme dark(또는 마운트 전) → 버튼 접근 이름·title 「라이트 모드로 전환」 · 아이콘 `[data-icon="sun"]` 1개 · `[data-icon="moon"]` 0개 · 클릭 → setTheme('light')
    - resolvedTheme light → 「다크 모드로 전환」 · `[data-icon="moon"]` · 클릭 → setTheme('dark')
    - className 을 넘기면 기본 44px 크기 클래스 대신 그 크기를 쓰되 버튼 기본 스타일은 유지(기존 계약)
    - ThemeSwitchIcon({ next: 'dark' }) → moon · ({ next: 'light' }) → sun · aria-hidden
  </behavior>
  <action>
    ① `theme-toggle.tsx`:
      - `export const THEME_SWITCH_LABEL: Record<ThemeValue, string>` = `{ dark: '다크 모드로 전환', light: '라이트 모드로 전환' }` — **키는 목적지 테마**. `ThemeValue` 타입도 export.
      - `export function ThemeSwitchIcon({ next, className }: { next: ThemeValue; className?: string })` — `next === 'dark'` 이면 lucide `Moon`(`data-icon="moon"`), 아니면 `Sun`(`data-icon="sun"`), 둘 다 `aria-hidden="true"`.
      - `ThemeToggle`: 옛 현재 상태 문구 맵을 지우고 아이콘을 `<ThemeSwitchIcon next={nextTheme} className="h-5 w-5" />` 로, `aria-label`·`title` 을 `THEME_SWITCH_LABEL[nextTheme]` 로 바꾼다. mounted 가드(마운트 전 current = dark) · className 계약 · `suppressHydrationWarning` 은 그대로.
      - 머리 주석: 「아이콘 = 누르면 바뀔 테마(목적지) · 접근 이름 = 행동 문구 — D-08b · 계정 카드와 같은 규칙, 정의는 여기 한 곳」.
    ② `__tests__/theme-toggle.test.tsx`(신규): account-card.test.tsx 와 같은 방식으로 `next-themes` 의 `useTheme` 를 모킹(`resolvedTheme` 변수 · `setTheme` vi.fn). `<behavior>` 네 줄을 각각 테스트로. 접근 이름은 `getByRole('button', { name: '라이트 모드로 전환' })` 처럼 정확 문자열로 찾는다. `className` 케이스는 `size-9` 를 넘겨 `size-9` 포함 · `inline-flex` 유지를 단언.
    ③ `app-sidebar.test.tsx` ⑧ · ⑧-b: 토글 탐색 정규식을 `/모드로 전환$/` 로 바꾼다(옛 괄호 안내 문구 정규식 제거). 나머지 단언(같은 줄 · shrink-0 · 비로그인에도 존재)은 그대로.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/layout/__tests__/theme-toggle.test.tsx src/components/layout/__tests__/app-sidebar.test.tsx src/components/layout/__tests__/app-shell-chrome.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or reports "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - theme-toggle.tsx: `grep -c "export const THEME_SWITCH_LABEL"` ≥ 1 · `grep -c "export function ThemeSwitchIcon"` ≥ 1 · `grep -c "다크 모드로 전환"` ≥ 1 · `grep -c "라이트 모드로 전환"` ≥ 1 · `grep -c 'data-icon="moon"'` ≥ 1 · `grep -c 'data-icon="sun"'` ≥ 1 · `grep -cF "THEME_SWITCH_LABEL[nextTheme]"` ≥ 1
    - 옛 상태 문구 제거(토글): `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' webapp/src/components/layout/theme-toggle.tsx | grep -c '클릭 시')" -eq 0`
    - 옛 정규식 제거(사이드바 테스트): `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' webapp/src/components/layout/__tests__/app-sidebar.test.tsx | grep -c '클릭 시')" -eq 0`
    - 사이드바 테스트가 새 정규식을 쓴다: `grep -cF '모드로 전환$/' webapp/src/components/layout/__tests__/app-sidebar.test.tsx` ≥ 1
    - verify 명령 통과(세 파일 모두 green — app-shell-chrome 은 hideSidebar 셸의 ThemeToggle 회귀 확인용)
  </acceptance_criteria>
  <done>사이드바 토글이 목적지 아이콘과 행동 문구를 보이고, 그 규칙이 export 된 한 곳에 정의돼 있다.</done>
</task>

<task type="auto">
  <name>Task 2: 계정 카드가 공용 규칙을 쓰게 + 단위·e2e 갱신(데스크톱 /me 사이드바 ↔ 카드 동등성)</name>
  <files>webapp/src/components/me/account-card.tsx, webapp/src/components/me/__tests__/account-card.test.tsx, webapp/e2e/specs/brand-account.spec.ts</files>
  <read_first>
    - webapp/src/components/me/account-card.tsx (전체 — 머리 주석 ③ · 88~100행 테마 버튼)
    - webapp/src/components/layout/theme-toggle.tsx (Task 1 export)
    - webapp/src/components/me/__tests__/account-card.test.tsx (98~118행)
    - webapp/e2e/specs/brand-account.spec.ts (전체 — 로그아웃은 누르지 않는 규칙 · 앱 모드 390 테스트)
  </read_first>
  <action>
    ① `account-card.tsx`: `import { ThemeSwitchIcon, THEME_SWITCH_LABEL } from '@/components/layout/theme-toggle'`. 테마 버튼의 `aria-label` 을 `THEME_SWITCH_LABEL[nextTheme]` 로, 같은 값을 `title` 로도 준다. 아이콘 분기를 `<ThemeSwitchIcon next={nextTheme} className="size-[18px]" />` 로 교체(시각 결과 동일 — 다크일 때 Sun, 라이트일 때 Moon). lucide `Sun`·`Moon` import 가 남지 않게 정리(LogOut 은 유지). 머리 주석 ③ 을 「아이콘·접근 이름 규칙은 ThemeToggle 과 같은 정의(D-08b)를 가져다 쓴다」로 고친다. 로그아웃 버튼·카드 치수(D-08a)는 그대로.
    ② `account-card.test.tsx` 「테마 버튼」 describe: 다크 케이스는 `getByRole('button', { name: '라이트 모드로 전환' })` + sun 1 · moon 0 + 클릭 → setTheme('light') · 라이트 케이스는 `{ name: '다크 모드로 전환' }` + moon + setTheme('dark'). describe 이름의 D-08a 를 D-08b 로. 파일 머리 주석의 아이콘 설명 줄도 새 규칙으로.
    ③ `brand-account.spec.ts`:
      - 기존 계정 카드 토글 테스트: 버튼 탐색을 `card.getByRole('button', { name: /모드로 전환$/ })` 로. 클릭 전 `wasDark` 에 따라 접근 이름이 「라이트 모드로 전환」(wasDark) 또는 「다크 모드로 전환」인지 단언하고, 두 번 토글 복구 흐름은 그대로. 머리 주석 2번 줄의 옛 일반 문구 표현을 「행동 문구 테마 버튼 두 번」으로.
      - 앱 모드 390 테스트: 뷰포트 안 단언의 버튼 탐색을 같은 정규식으로.
      - 신규 테스트 「데스크톱 1280 /me — 사이드바 토글과 계정 카드 테마 버튼이 같은 아이콘·같은 접근 이름(D-08b)」: 1280×800 → `/me` → `[data-slot="app-aside"]` 안 `/모드로 전환$/` 버튼과 계정 카드 안 같은 이름 버튼을 찾아 두 버튼의 accessible name 이 같고, 각 버튼 안 `svg[data-icon]` 의 `data-icon` 값이 같음을 단언 → 사이드바 버튼 클릭 → `html` dark 클래스가 뒤집히고 두 버튼의 `data-icon` 이 함께 바뀜 → 다시 클릭해 원상 복구. 로그아웃은 누르지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/me/__tests__/account-card.test.tsx src/components/layout/__tests__/theme-toggle.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/brand-account.spec.ts</automated>
    <fails_when>non-zero exit, or the summary contains "failed" or "skipped", or no summary line matches "[0-9]+ passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - account-card.tsx: `grep -cE "from ['\"]@/components/layout/theme-toggle['\"]"` ≥ 1 · `grep -cF "THEME_SWITCH_LABEL[nextTheme]"` ≥ 1 · `grep -c "ThemeSwitchIcon"` ≥ 1
    - 카드 옛 일반 문구 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' webapp/src/components/me/account-card.tsx | grep -c 'aria-label="테마 전환"')" -eq 0`
    - 카드 단위 테스트 옛 이름 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' webapp/src/components/me/__tests__/account-card.test.tsx | grep -c "name: '테마 전환'")" -eq 0`
    - 카드 단위 테스트가 행동 문구로 찾는다: `grep -c "라이트 모드로 전환" webapp/src/components/me/__tests__/account-card.test.tsx` ≥ 1
    - e2e 옛 이름 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' webapp/e2e/specs/brand-account.spec.ts | grep -c "'테마 전환'")" -eq 0`
    - e2e 데스크톱 동등성 테스트가 있다: `grep -c "app-aside" webapp/e2e/specs/brand-account.spec.ts` ≥ 1 · `grep -c "data-icon" webapp/e2e/specs/brand-account.spec.ts` ≥ 1
    - verify 세 명령 통과(brand-account 세 테스트 모두 ✓ — 로그아웃 클릭 없음)
  </acceptance_criteria>
  <done>계정 카드와 사이드바 토글이 같은 정의로 같은 아이콘·같은 접근 이름을 보이고, 데스크톱 /me 에서 그 동등성이 e2e 로 잠겼다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음) | 표시 전용 변경 — 사용자 입력 · 네트워크 · 저장 형식을 새로 다루지 않는다. 테마 저장은 기존 next-themes 경로 그대로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-50 | Spoofing | 테마 버튼 표시(아이콘·접근 이름 불일치로 사용자가 의도와 다른 전환) | low | mitigate | 규칙 단일 정의(ThemeSwitchIcon · THEME_SWITCH_LABEL) · 단위(양 테마) · e2e 데스크톱 동등성(Task 2 ③) |
| T-21-35 | Repudiation | 계정 카드 로그아웃 | low | accept | 이 플랜은 로그아웃 경로를 바꾸지 않는다(기존 useAuth().signOut) · e2e 는 로그아웃을 누르지 않는다 |
</threat_model>

<verification>
- vitest(theme-toggle · app-sidebar · app-shell-chrome · account-card) · Playwright brand-account · webapp typecheck 통과
- 사람 확인(데스크톱·앱에서 두 버튼 같은 아이콘 · 툴팁 행동 문구)은 21-24 UAT 항목 3
</verification>

<success_criteria>
- G-21-N2: 두 테마 컨트롤이 목적지 아이콘과 행동 문구라는 하나의 규칙을 한 곳의 정의로 따른다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| Android `ExternalLinks.opensInAppBrowser(scheme, host, appHost)` · `ExternalLinksTest`(정본 26케이스) | Kotlin 순수 함수 · JUnit | 21-17 |
| Android `InAppBrowser.open(activity, uri, theme)` · `GhTradeWebViewClient.shouldOverrideUrlLoading` 가로채기 | Kotlin | 21-17 |
| `mobile/android/variables.gradle` `androidxBrowserVersion` · `app/build.gradle` androidx.browser 선언 (21-17 Task 1 선택 a 일 때만) | Gradle | 21-17 |
| `mobile/package.json` `native:test:android`(TabRoutesTest + ExternalLinksTest) · `native:check-external-links:ios` | 스크립트 | 21-17 · 21-22 |
| 웹 `ThemeProvider` 기본 다크 · iOS/Android `ThemeStore` 기본 다크 · `mobile/www/index.html` 기본 다크 | 설정 | 21-18 |
| e2e `webapp/e2e/specs/theme-default.spec.ts` (저장값 없음 다크 · 저장값 유지 · 앱 첫 theme 메시지 · 오프라인 폴백) | Playwright | 21-18 |
| 웹 `THEME_SWITCH_LABEL` · `ThemeSwitchIcon` (theme-toggle.tsx export) — 사이드바 ThemeToggle · AccountCard 공용 목적지 규칙 | 컴포넌트·상수 | 21-19 |
| 단위 `webapp/src/components/layout/__tests__/theme-toggle.test.tsx` | Vitest | 21-19 |
| iOS `GHTradeTabBar` 캡슐 C(라벨 없음 · 56×36 16% · 유리 · 두 겹 그림자 · 높이 60 · 페이드 86) · `GHTradePalette.glass` · `glassAlpha` | Swift | 21-20 |
| CSS `--native-tabbar-offset` = gap + 60 + 8 · `--native-body-reserve` 98 · native-shell e2e 수치(82 · 98 · 74) | 토큰 · Playwright | 21-20 |
| CONTEXT D-27b · D-23a · D-08b · D-28 | 결정 기록 | 21-20 |
| Android `GhTradeTabBar` 캡슐 C 근사(라벨 없음 · 불투명 유리 · elevation 12) · `GhTradePalette.glass` · 탭바 60dp · 페이드 86dp | Kotlin | 21-21 |
| iOS `ExternalLinks.opensInAppBrowser(scheme:host:appHost:)` · `isSameAppHost` · `NavigationDelegateProxy`(decidePolicyFor · WKUIDelegate createWebViewWith · SFSafariViewController) · `mobile/scripts/external-links-check.swift` · `check-external-links-ios.sh` | Swift · 스크립트 | 21-22 |
| 갭 클로징 전 게이트 기록 · push 전 UAT 환경 (21-23-SUMMARY) · UAT 재검증 결과 · push 결정과 이행 (21-24-SUMMARY) | 문서 | 21-23 · 21-24 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-19-SUMMARY.md` when done — 바뀐 접근 이름 · 공용 export · 테스트 결과를 적는다.
</output>
