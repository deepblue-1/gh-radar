---
phase: 21-gh-trade-mobile-app
plan: 30
type: execute
wave: 13
depends_on: ["21-25"]
files_modified:
  - webapp/src/components/stock/news-view.ts
  - webapp/src/components/stock/news-full-list.tsx
  - webapp/src/components/stock/news-page-client.tsx
  - webapp/src/components/stock/stock-news-section.tsx
  - webapp/src/components/stock/stock-news-tab-panel.tsx
  - webapp/src/components/stock/stock-detail-client.tsx
  - webapp/src/components/stock/discussion-full-list.tsx
  - webapp/src/components/stock/discussion-page-client.tsx
  - webapp/src/components/stock/stock-discussion-section.tsx
  - webapp/src/components/stock/stock-detail-tabs.tsx
  - webapp/src/components/stock/__tests__/stock-detail-tabs.test.tsx
  - webapp/src/components/stock/__tests__/discussion-page-client.test.tsx
  - webapp/src/components/stock/__tests__/discussion-full-list.test.tsx
  - webapp/src/app/stocks/[code]/news/page.tsx
  - webapp/src/app/stocks/[code]/discussions/page.tsx
  - webapp/e2e/specs/news.spec.ts
  - webapp/e2e/specs/discussions.spec.ts
  - webapp/e2e/specs/discussion-filter.spec.ts
autonomous: true
gap_closure: true
requirements: [MOBILE-01]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G-21-R3-8 (종목상세 · D-29): 「뉴스·토론」 탭에서 「전체 뉴스 보기」「전체 토론 보기」를 누르면 페이지를 떠나지 않고 탭 안에서 전체목록으로 바뀌고 URL 은 `?tab=news&view=news|discussions`(pushState · 허용 목록)다"
    - "G-21-R3-8: 브라우저/Android 뒤로가기 · Esc · 화면 안 ← 가 요약으로 돌아오고 창 스크롤이 전체목록을 열기 전 위치로 복원된다 · 딥링크로 들어와 ← 를 누르면 `?tab=news` 로 바뀔 뿐 페이지를 떠나지 않는다 · 활성 뉴스·토론 탭 재클릭 = 요약"
    - "G-21-R3-8: 전체목록은 옛 전체 페이지와 같은 목록(뉴스 100 캡 · 토론 50 · 무한 스크롤 · 빈/오류 상태 · 토론 필터 비활성)을 보이고, 당겨서 새로고침은 GET 재조회만 한다(D-18)"
    - "G-21-R3-8: 옛 주소 `/stocks/{code}/news` · `/discussions` 는 탭 안 전체목록으로 리다이렉트된다(21-25 재확인 답)"
  artifacts:
    - path: webapp/src/components/stock/news-view.ts
      provides: "NEWS_VIEWS · toNewsView · useNewsView(showAll · back · 스크롤 복원) · exitNewsView"
      contains: "export function useNewsView"
    - path: webapp/src/components/stock/news-full-list.tsx
      provides: "NewsFullList({code, onBack?}) — 페이지·팝업 공용 목록"
      contains: "export function NewsFullList"
    - path: webapp/src/components/stock/discussion-full-list.tsx
      provides: "DiscussionFullList({code, onBack?}) — 필터 로컬 · 분류 정지 유지"
      contains: "export function DiscussionFullList"
    - path: webapp/src/components/stock/stock-news-tab-panel.tsx
      provides: "요약 ↔ 전체목록 전환 패널"
      contains: "StockNewsTabPanel"
  key_links:
    - from: webapp/src/components/stock/stock-news-section.tsx
      to: webapp/src/components/stock/news-view.ts
      via: "onShowAll → showAll('news') → pushState ?tab=news&view=news"
      pattern: "onShowAll"
    - from: webapp/src/components/stock/stock-detail-tabs.tsx
      to: webapp/src/components/stock/news-view.ts
      via: "활성 news 탭 재클릭 + view 있음 → exitNewsView"
      pattern: "exitNewsView"
    - from: webapp/src/app/stocks/[code]/news/page.tsx
      to: webapp/src/components/stock/stock-news-tab-panel.tsx
      via: "redirect ?tab=news&view=news"
      pattern: "view=news"
  prohibitions:
    - statement: "MUST NOT 탭 안 전체목록에서 POST 새로고침(/news/refresh)을 부른다 — 당김은 GET 재조회만(D-18)"
      status: unverified
      verification: "Task 1 acceptance — news-full-list 에 refresh 경로 grep 0"
    - statement: "MUST NOT 토론 전체목록 필터를 URL 에 쓴다(?tab · ?view 와 섞인다)"
      status: unverified
      verification: "Task 2 acceptance — discussion-full-list 에 라우터 사용 0"
---

<objective>
G-21-R3-8 의 종목상세 쪽 — 뉴스·토론 전체 보기를 **탭 안 전체목록**으로 바꾸고(D-29), 옛 전체 페이지를 리다이렉트로 바꾼다. 팝업 쪽(트레이딩 ⓘ 팝업 · 로컬 상태)은 이 플랜이 만드는 `NewsFullList` · `DiscussionFullList` · `onShowAll` 을 받아 21-33 이 붙인다(stock-info-modal.tsx 는 21-26 이 먼저 고친다 — 파일 순서 보장).

Purpose: 종목을 보다가 전체 뉴스·토론을 열어도 맥락(탭 · 스크롤)을 잃지 않고, iOS 처럼 네이티브 뒤로가기가 없는 셸에서도 화면 안 ← 로 돌아온다.
Output: 공용 전체목록 두 개 · useNewsView · 탭 패널 · 섹션 onShowAll · 탭 재클릭 규칙 · 리다이렉트 두 페이지 · 옛 페이지 클라이언트 삭제 · 테스트 갱신.

스크롤 주체는 창(window)이다 — AppShell `<main>` 은 높이 제한이 없어 스크롤하지 않는다(shared-panels.tsx ⑤-b 18-13 실측 · 플래너 코드 확인). 21-UAT 진단의 `main.scrollTop` 표기 대신 `window.scrollY` 를 쓴다.

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add(`git add -A` 금지) · 커밋 직전 `git status -sb` · **push 금지**(21-36).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-UAT.md
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@webapp/src/components/stock/stock-detail-tabs.tsx
@webapp/src/components/stock/stock-detail-client.tsx
@webapp/src/components/stock/stock-news-section.tsx
@webapp/src/components/stock/stock-discussion-section.tsx
@webapp/src/components/stock/news-page-client.tsx
@webapp/src/components/stock/discussion-page-client.tsx

<interfaces>
```text
StockDetailTabs: ?tab= 은 window.history.pushState(null,'',`?tab=${value}`) · 한 클릭 = 기록 1 가드(실시간 URL 비교 ≈107) · visited 패널 forceMount + data-[state=inactive]:hidden
stock-detail-client.tsx ≈232~237: news={<DetailBands><StockNewsSection stockCode/><StockDiscussionSection stockCode/></DetailBands>} (Suspense 안)
StockNewsSection ≈213~218 / StockDiscussionSection ≈269~274: 「전체 뉴스 보기 →」「전체 토론 보기 →」 = Link · 각자 useNativeRefresh(load = GET)
NewsPageClient: fetchStockDetail + fetchStockNews(days 7 · 100) · 무한 스크롤(IntersectionObserver · before 커서) · testid news-list · news-page-loading · news-page-empty · news-page-error · news-pagination-*
DiscussionPageClient: + useSearchParams/useRouter 필터(?filter) · CLASSIFY_PAUSED=true → 항상 all · Switch disabled · 50 · testid discussion-*
페이지: app/stocks/[code]/news/page.tsx · discussions/page.tsx ('use client' · use(params) · CODE_RE /^[A-Za-z0-9]{1,10}$/ · AppShell)
e2e: news.spec(≈52~130) · discussions.spec(≈81~280) · discussion-filter.spec(≈127~201)
Android 뒤로가기: 오버레이 없으면 WebView goBack(pushState 기록 pop → popstate → Next 가 검색 파라미터 갱신)
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — NewsFullList 추출 → 섹션 onShowAll → useNewsView(pushState · ← · Esc · 창 스크롤 복원) → 종목상세 패널 → e2e 왕복 (뉴스)</name>
  <files>webapp/src/components/stock/news-view.ts, webapp/src/components/stock/news-full-list.tsx, webapp/src/components/stock/news-page-client.tsx, webapp/src/components/stock/stock-news-section.tsx, webapp/src/components/stock/stock-news-tab-panel.tsx, webapp/src/components/stock/stock-detail-client.tsx, webapp/e2e/specs/news.spec.ts</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-R3-8 전체)
    - webapp/src/components/stock/news-page-client.tsx (전체)
    - webapp/src/components/stock/stock-news-section.tsx (전체)
    - webapp/src/components/stock/stock-detail-tabs.tsx (99~119 pushState 가드)
    - webapp/src/components/stock/stock-detail-client.tsx (192~240)
    - webapp/e2e/specs/news.spec.ts (50~130 — 목 데이터 · 전체 페이지 단언)
  </read_first>
  <action>
    per D-29: ① `news-full-list.tsx`(신규 'use client'): `export function NewsFullList({ code, onBack }: { code: string; onBack?: () => void })` — NewsPageClient 의 목록 부분(첫 페이지 · 무한 스크롤 · 로딩/빈/오류/페이지네이션 상태와 testid 전부)을 옮긴다(fetchStockDetail · notFound · 페이지 제목은 옮기지 않는다). 머리 줄: `onBack` 이 있으면 `<button type="button" aria-label="요약으로 돌아가기">` + lucide 화살표 + 제목 「최근 7일 뉴스」(h2), 없으면 제목만. `useNativeRefresh(load)` 로 GET 재조회만(D-18 주석).
    ② `news-page-client.tsx`: 헤더 + `<NewsFullList code={code} />` 로 줄인다(Task 3 가 지운다).
    ③ `news-view.ts`(신규): `NEWS_VIEWS = ['news','discussions'] as const` · `toNewsView(raw)`(허용 목록 밖 → null) · `useNewsView(code)` → `{ view, showAll(v), back() }` — `useSearchParams()` 로 `tab === 'news'` 일 때만 view 를 읽는다. `showAll(v)`: `window.scrollY` 를 코드별 모듈 상태에 저장 · 「우리가 쌓음」 표시 · `window.history.pushState(null, '', '?tab=news&view=' + v)` · 패널 머리로 스크롤. `back()`: 우리가 쌓았으면 `history.back()`, 아니면 `history.replaceState(null, '', '?tab=news')`. view 가 값 → null 로 바뀌는 effect 에서 저장 위치로 `window.scrollTo` 복원. `export function exitNewsView(code)`(탭 재클릭용 — back 과 같은 규칙). 머리 주석: D-29 · 뒤로가기 네 경로 · 스크롤 주체 = window.
    ④ `stock-news-section.tsx`: `onShowAll?: () => void` — 있으면 footer 를 같은 클래스의 `<button type="button">`, 없으면 기존 Link.
    ⑤ `stock-news-tab-panel.tsx`(신규): `StockNewsTabPanel({ code })` — view 가 있으면 요약(`DetailBands` 안 두 섹션)을 `hidden` 으로 숨긴 채 마운트 유지하고 view === 'news' 면 `<NewsFullList code onBack={back} />`. view 가 있을 때 document Escape(기본 동작이 막히지 않았고 열린 `[role="dialog"]` 가 없을 때) → `back()`.
    ⑥ `stock-detail-client.tsx`: news 패널을 `<StockNewsTabPanel code={stock.code} />` 로.
    ⑦ e2e(news.spec.ts) 「G-21-R3-8 탭 안 전체 뉴스」 하나: `?tab=news` → 창 스크롤 → 「전체 뉴스 보기」 → URL `view=news` · `news-list` 보임 → `page.goBack()` → 요약 · scrollY 복원(±2) → 다시 열고 「요약으로 돌아가기」 → `?tab=news` → 딥링크 `?tab=news&view=news` 로 새로 열어 ← → `?tab=news`(같은 경로).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/news.spec.ts && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or no summary line matches "[0-9]+ passed", or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function NewsFullList" webapp/src/components/stock/news-full-list.tsx` ≥ 1 · `grep -c "useNativeRefresh" webapp/src/components/stock/news-full-list.tsx` ≥ 1
    - `grep -c "export function useNewsView" webapp/src/components/stock/news-view.ts` ≥ 1 · `grep -c "history.pushState" webapp/src/components/stock/news-view.ts` ≥ 1 · `grep -c "window.scrollY" webapp/src/components/stock/news-view.ts` ≥ 1 · `grep -c "export function exitNewsView" webapp/src/components/stock/news-view.ts` ≥ 1
    - `grep -c "onShowAll" webapp/src/components/stock/stock-news-section.tsx` ≥ 1 · `grep -c "StockNewsTabPanel" webapp/src/components/stock/stock-detail-client.tsx` ≥ 1
    - POST 경로 없음: `test "$(grep -c 'refreshStockNews\|/news/refresh' webapp/src/components/stock/news-full-list.tsx)" -eq 0`
    - `grep -c "G-21-R3-8" webapp/e2e/specs/news.spec.ts` ≥ 1
    - verify 명령 통과
  </acceptance_criteria>
  <done>종목상세에서 전체 뉴스가 탭 안에 열리고, 뒤로가기 · ← 로 요약과 창 스크롤이 돌아온다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DiscussionFullList 추출(필터 로컬 · 분류 정지 유지) · 토론 onShowAll · 활성 뉴스·토론 탭 재클릭 = 요약</name>
  <files>webapp/src/components/stock/discussion-full-list.tsx, webapp/src/components/stock/discussion-page-client.tsx, webapp/src/components/stock/stock-discussion-section.tsx, webapp/src/components/stock/stock-news-tab-panel.tsx, webapp/src/components/stock/stock-detail-tabs.tsx, webapp/src/components/stock/__tests__/stock-detail-tabs.test.tsx, webapp/src/components/stock/__tests__/discussion-page-client.test.tsx, webapp/src/components/stock/__tests__/discussion-full-list.test.tsx</files>
  <read_first>
    - webapp/src/components/stock/discussion-page-client.tsx (전체)
    - webapp/src/components/stock/stock-discussion-section.tsx (255~281 footer)
    - webapp/src/components/stock/stock-news-tab-panel.tsx · news-view.ts (Task 1)
    - webapp/src/components/stock/stock-detail-tabs.tsx (99~119 handleValueChange)
    - webapp/src/components/stock/__tests__/stock-detail-tabs.test.tsx (pushState 스파이 · 재클릭 0회 테스트)
    - webapp/src/components/stock/__tests__/discussion-page-client.test.tsx (옮길 단언)
  </read_first>
  <behavior>
    - 활성 뉴스·토론 탭 재클릭 + URL 에 view 있음 → 요약 복귀(back 또는 replaceState 1회)
    - view 없는 활성 탭 재클릭 → pushState 0회(기존 테스트 유지)
    - DiscussionFullList: 필터 Switch disabled · 목록 all · 필터를 바꿔도 URL 불변 · onBack 이 있으면 「요약으로 돌아가기」 버튼
  </behavior>
  <action>
    ① `discussion-full-list.tsx`(신규): `DiscussionFullList({ code, onBack })` — DiscussionPageClient 의 필터 줄(Switch disabled · 「의미있는 토론만 보기」) · 목록 · 무한 스크롤 · 상태·testid 를 옮긴다. 필터는 **로컬 상태**로만(CLASSIFY_PAUSED 상수·주석 유지). 머리 줄은 NewsFullList 와 같은 규칙(「최근 7일 토론」). `useNativeRefresh(load)` = GET 만.
    ② `discussion-page-client.tsx`: 헤더 + `<DiscussionFullList code={code} />` 로 줄인다(Task 3 가 지운다). 단위 테스트를 `git mv` 로 `discussion-full-list.test.tsx` 로 옮겨 behavior 의 DiscussionFullList 단언으로 고친다.
    ③ `stock-discussion-section.tsx`: `onShowAll` — 뉴스 섹션과 같은 규칙. `stock-news-tab-panel.tsx`: `onShowAll={() => showAll('discussions')}` · view === 'discussions' 면 `<DiscussionFullList code onBack={back} />`.
    ④ `stock-detail-tabs.tsx` `handleValueChange`: 누른 탭이 이미 활성인 `news` 이고 실시간 URL 에 `view` 가 있으면 가드로 버리지 말고 `exitNewsView(code)`. 그 외 가드는 그대로. 단위 테스트에 behavior 두 케이스.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/stock/__tests__/stock-detail-tabs.test.tsx src/components/stock/__tests__/discussion-full-list.test.tsx src/components/stock/__tests__/stock-detail-client.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or reports "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function DiscussionFullList" webapp/src/components/stock/discussion-full-list.tsx` ≥ 1 · `grep -c "CLASSIFY_PAUSED" webapp/src/components/stock/discussion-full-list.tsx` ≥ 1
    - URL 쓰기 없음: `test "$(grep -c 'useRouter' webapp/src/components/stock/discussion-full-list.tsx)" -eq 0`
    - `grep -c "onShowAll" webapp/src/components/stock/stock-discussion-section.tsx` ≥ 1 · `grep -c "exitNewsView" webapp/src/components/stock/stock-detail-tabs.tsx` ≥ 1
    - `test -e webapp/src/components/stock/__tests__/discussion-full-list.test.tsx`
    - verify 명령 통과
  </acceptance_criteria>
  <done>토론도 탭 안 전체목록으로 열리고, 뉴스·토론 탭 재클릭이 요약으로 돌려보낸다.</done>
</task>

<task type="auto">
  <name>Task 3: 옛 전체 페이지 → 리다이렉트 · 페이지 클라이언트 삭제 · e2e 갱신(뉴스 · 토론 · 필터)</name>
  <files>webapp/src/app/stocks/[code]/news/page.tsx, webapp/src/app/stocks/[code]/discussions/page.tsx, webapp/src/components/stock/news-page-client.tsx, webapp/src/components/stock/discussion-page-client.tsx, webapp/e2e/specs/news.spec.ts, webapp/e2e/specs/discussions.spec.ts, webapp/e2e/specs/discussion-filter.spec.ts</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-25-SUMMARY.md (R3-8 리다이렉트 재확인 답 — 이견이면 그 답을 따른다)
    - webapp/src/app/stocks/[code]/news/page.tsx · discussions/page.tsx (CODE_RE · notFound)
    - webapp/e2e/specs/news.spec.ts (97~130) · discussions.spec.ts (81~280) · discussion-filter.spec.ts (127~201)
  </read_first>
  <action>
    ① 두 페이지를 서버 컴포넌트로: `params` await → `CODE_RE` 불일치면 `notFound()`, 맞으면 `redirect('/stocks/' + encodeURIComponent(code) + '?tab=news&view=news')`(토론은 `view=discussions` · 옛 `?filter` 는 버린다 — 분류 정지 중). 주석: D-29 북마크 호환. 21-25 답이 이견이면 그 답대로 하고 SUMMARY 에 적는다.
    ② `news-page-client.tsx` · `discussion-page-client.tsx` 를 `git rm`. 남은 import 0.
    ③ e2e: news.spec 의 전체 페이지 테스트를 「옛 URL → `?tab=news&view=news` 리다이렉트 · 탭 안 전체목록 100 캡 · ← = 요약」 으로, discussions.spec 의 전체 페이지 테스트는 리다이렉트 뒤 탭 안에서 같은 단언(50건 · 새로고침 버튼 없음 · 무한 스크롤 before 커서 · axe)으로 옮기고 「← back link navigates」 는 「← = 요약 복귀(페이지 이탈 없음)」 로, discussion-filter.spec 은 리다이렉트 뒤 토글 disabled · URL 불변 · 빈 배열 카피를 탭 안에서 단언한다. 새 스위트는 만들지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/news.spec.ts e2e/specs/discussions.spec.ts e2e/specs/discussion-filter.spec.ts e2e/specs/stock-detail-tabs.spec.ts</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or no summary line matches "[0-9]+ passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "redirect(" "webapp/src/app/stocks/[code]/news/page.tsx"` ≥ 1 · `grep -c "view=news" "webapp/src/app/stocks/[code]/news/page.tsx"` ≥ 1 · `grep -c "view=discussions" "webapp/src/app/stocks/[code]/discussions/page.tsx"` ≥ 1
    - 삭제: `test ! -e webapp/src/components/stock/news-page-client.tsx` · `test ! -e webapp/src/components/stock/discussion-page-client.tsx`
    - 남은 참조 0: `test "$(grep -rl 'NewsPageClient\|DiscussionPageClient' webapp/src webapp/e2e | wc -l | tr -d ' ')" -eq 0`
    - verify 두 명령 통과
  </acceptance_criteria>
  <done>옛 전체 페이지는 탭 안 전체목록으로 리다이렉트되고, 기존 테스트가 새 자리에서 같은 기능을 잠근다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `?view=` → 화면 상태 | 사용자·링크가 제어하는 값이 렌더 분기가 된다 |
| 옛 URL → 리다이렉트 | 경로 파라미터가 새 URL 로 조립된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-82 | Tampering | `?view=` 임의 값 | low | mitigate | toNewsView 허용 목록(news · discussions) 밖 → null = 요약(Task 1) |
| T-21-83 | Tampering | 리다이렉트 경로 조립 | low | mitigate | CODE_RE 검증 뒤 encodeURIComponent · 불일치 notFound(Task 3) |
| T-21-84 | Denial of Service | 전체목록 당김이 POST 새로고침 호출(쿼터) | medium | mitigate | GET 재조회만 · refresh 경로 grep 0(Task 1) |
</threat_model>

<verification>
- Playwright(news · discussions · discussion-filter · stock-detail-tabs) · vitest(stock-detail-tabs · discussion-full-list · stock-detail-client) · typecheck
- 사람 확인(iOS 화면 안 ← · Android 뒤로가기 · 스크롤 복원 체감)은 21-36 UAT
</verification>

<success_criteria>
- G-21-R3-8 종목상세 쪽이 탭 안 전체목록 · 네 가지 뒤로가기 · 리다이렉트로 닫히고, 팝업 쪽이 쓸 공용 컴포넌트가 준비됐다.
</success_criteria>

## Flagged assumptions

- **엣지 탐침 MOBILE-01 (unclassified · unresolved):** 경계는 view 허용 목록 · 딥링크 ← · 재클릭 두 분기로 명시했다. 나머지는 21-36 UAT.
- 요약 섹션은 숨긴 채 마운트를 유지해 돌아올 때 재요청·스켈레톤이 없다고 본다(탭 패널과 같은 keepMounted 규칙).

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| 스케치 `.planning/sketches/008-uat3-surfaces/`(index.html · README.md) · MANIFEST 008 행 | 목업 | 21-25 |
| CONTEXT D-12a · D-12b · D-25a · D-29 · D-30 · D-31 · D-32(R3-11 축소: 스크롤 복원 + 스켈레톤 없음 · 상태 초기화 허용) · WR-03 androidx.webkit 선언 답 | 결정 기록 | 21-25 |
| `PAD_CHIPS.만원` = 천만(+1,000) · 오천만(+5,000) · 1억(+10,000) · 지우기 · 20-CONTEXT D-17 포인터 | 상수 · 결정 | 21-26 |
| `SettingGroup` 제목 없는 그룹 상단 `pt-1` | 클래스 | 21-26 |
| 카드 헤더 ✕ `data-slot="card-close"` · 종목정보 팝업 ✕ = `size-8` + lucide `XIcon` + `after:-inset-1.5`(히트 44) · 팝업 고정 높이 · `scrollbar-gutter:stable` · 폰 safe-area 패딩 | 컴포넌트 | 21-26 |
| `webapp/src/lib/safe-path.ts` `isSafeInternalPath` · `recent-search.ts` `RECENT_CODE_RE` · `PopoverContent` 안 `NativeOverlayMarker` · `useDebouncedSearch` 반환 `resultsQuery` · `viewport.themeColor` `#17171c` + `ThemeColorSync` | 함수 · 훅 · 메타 | 21-27 |
| iOS 키보드 사유 즉시 숨김 · 재표시 90ms · `awaitingContent`(didStartProvisionalNavigation → 첫 route/1.5초) · 초기 숨김 · 브리지 출처 scheme·host·port 비교 · 오프라인 `dev=1` · 스모크 기기 지정(`IOS_DEVICE_UDID` · `ANDROID_SERIAL`) · mobile/README 명령표 | Swift · 스크립트 · 문서 | 21-28 |
| Android IME 애니메이션 콜백 즉시 숨김 · `awaitingContent`(onPageStarted) · `pullBlocked` 초기화 · 오프라인 뒤로가기(재현 시) · 오프라인 `dev=1` · 폴백 페이지 dev=1 일 때만 localhost · allowBackup=false + `res/xml/data_extraction_rules.xml` · 브리지 WebMessageListener(21-25 답 a) | Kotlin · XML · HTML | 21-29 |
| `NewsFullList` · `DiscussionFullList` · `useNewsView` · `StockNewsTabPanel` · 섹션 `onShowAll` · 종목상세 `?tab=news&view=news\|discussions` · 옛 URL 리다이렉트 · 팝업 `newsView` | 컴포넌트 · 훅 · 라우트 | 21-30 |
| `useTabRootScrollMemory`(`webapp/src/lib/tab-scroll-memory.ts` · AppShell 에서 호출) · 기존 `lib/query-cache` 재사용: 검색 허브 · 대화 목록 · 오늘 주문 | 훅 · 캐시 | 21-31 |
| 앱 공용 패널 숨김 CSS · 카드 더티 바 탭바 비킴(프로브) · `StrategyLogFeedProvider` · `useStrategyLogFeed` · `diffLimitChasers` · /me 전략 로그(`me-strategy-log`) | CSS · Provider · 컴포넌트 | 21-32 |
| 작업대 `?code=` 소비(카드 보장·펼침·포커스) · 카드 수동주문 주문유형(지정가/시간외종가) | 컴포넌트 | 21-33 |
| 종목상세 「트레이딩」 CTA(폰 바 + `detail-trading-button`) · 호가주문 탭 제거 · `?tab=orderbook` → `/trading?code=` · 호가 탭 전용 코드·`variant` 삭제 · orderbook.spec 선별 이전 | 컴포넌트 · 정리 · e2e | 21-34 |
| 게이트 기록 · `21-REVIEW-R2.md`(13건 대조) · UAT 환경(21-35-SUMMARY) · UAT 3차 재검증 · push 결정(21-36-SUMMARY) | 문서 | 21-35 · 21-36 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-30-SUMMARY.md` when done — 컴포넌트 · 훅 계약 · 리다이렉트 답 · 옮긴 테스트 목록 · e2e 결과.
</output>
