---
phase: 21-gh-trade-mobile-app
plan: 27
type: execute
wave: 12
depends_on: ["21-24"]
files_modified:
  - webapp/src/lib/safe-path.ts
  - webapp/src/lib/__tests__/safe-path.test.ts
  - webapp/src/app/login/page.tsx
  - webapp/src/app/login/__tests__/page.test.tsx
  - webapp/src/app/auth/callback/route.ts
  - webapp/src/lib/native/native-bridge-provider.tsx
  - webapp/src/lib/recent-search.ts
  - webapp/src/lib/__tests__/recent-search.test.ts
  - webapp/src/hooks/use-debounced-search.ts
  - webapp/src/components/search/search-page-client.tsx
  - webapp/src/components/search/__tests__/search-page-client.test.tsx
  - webapp/src/components/ui/popover.tsx
  - webapp/src/lib/native/__tests__/native-overlay.test.tsx
  - webapp/src/app/layout.tsx
  - webapp/src/components/providers/theme-provider.tsx
  - webapp/e2e/specs/theme-default.spec.ts
autonomous: true
gap_closure: true
requirements: [MOBILE-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G-21-CR · WR-01: 로그인 next · /auth/callback next · 네이티브 navigate 가 한 함수(isSafeInternalPath, lib/safe-path.ts)를 쓰고, `?next=/%5Cevil.com`(디코드 후 `/\\evil.com`) · 탭·개행 포함 경로 · `//evil.com` 은 `/` 로 떨어진다 — 정상 경로(`/` · `/me` · `/trading?focus=…` · `/stocks/005930?tab=news`)는 통과"
    - "G-21-CR · IN-07: 최근 검색 저장값의 종목 코드가 `/^[0-9A-Z]{6}$/` 이 아니면 읽기(sanitize)와 쓰기(push) 모두에서 버려진다 — `005930` · `0000J0` 은 통과"
    - "G-21-CR · WR-05: /search 에서 이전 검색어의 늦은 응답이 도착해도 새 검색어의 결과로 보이지 않는다 — 그 동안 「검색 중…」이고 「해당하는 종목이 없습니다」가 번쩍이지 않으며, Enter 는 이동·최근 검색 저장을 하지 않는다"
    - "G-21-CR · WR-04: Radix Popover 가 열려 있으면 앱 오버레이 계수에 잡혀 네이티브에 overlay {open:true} 가 가고, Android 뒤로가기(`__ghTrade.back()`)는 페이지 이동 대신 팝오버를 닫는다(D-26 ①) — 브라우저는 송신 0"
    - "G-21-CR · IN-06: theme-color 메타가 OS 다크모드가 아니라 앱 테마를 따른다 — 기본 `#17171c`, 라이트 저장값이면 `#ffffff`"
  artifacts:
    - path: webapp/src/lib/safe-path.ts
      provides: "isSafeInternalPath — 같은 출처 절대 경로 판정의 유일한 정의"
      contains: "export function isSafeInternalPath"
    - path: webapp/src/lib/recent-search.ts
      provides: "RECENT_CODE_RE 로 읽기·쓰기 필터"
      contains: "RECENT_CODE_RE"
    - path: webapp/src/hooks/use-debounced-search.ts
      provides: "결과가 어느 검색어의 것인지 resultsQuery 반환"
      contains: "resultsQuery"
    - path: webapp/src/components/ui/popover.tsx
      provides: "PopoverContent 첫 자식 NativeOverlayMarker"
      contains: "NativeOverlayMarker"
    - path: webapp/src/components/providers/theme-provider.tsx
      provides: "ThemeColorSync — resolvedTheme → meta theme-color"
      contains: "theme-color"
  key_links:
    - from: webapp/src/app/login/page.tsx
      to: webapp/src/lib/safe-path.ts
      via: "safeNext = isSafeInternalPath(rawNext) ? rawNext : '/'"
      pattern: "isSafeInternalPath"
    - from: webapp/src/lib/native/native-bridge-provider.tsx
      to: webapp/src/lib/safe-path.ts
      via: "navigate() 가 import 해서 쓴다(로컬 정의 삭제)"
      pattern: "from '@/lib/safe-path'"
    - from: webapp/src/components/search/search-page-client.tsx
      to: webapp/src/hooks/use-debounced-search.ts
      via: "pending = typing && trimmed !== resultsQuery"
      pattern: "resultsQuery"
    - from: webapp/src/components/ui/popover.tsx
      to: webapp/src/lib/native/native-bridge-provider.tsx
      via: "NativeOverlayMarker → acquireOverlay → overlay {open} · back() 합성 Escape"
      pattern: "NativeOverlayMarker"
  prohibitions:
    - statement: "MUST NOT 경로 가드를 두 곳 이상에 따로 정의한다 — login · callback · navigate 가 같은 함수를 import 한다"
      status: unverified
      verification: "Task 1 acceptance — 로컬 정의 0 · 세 파일 import ≥ 1"
    - statement: "MUST NOT 검색 결과를 요청한 검색어가 아닌 「응답 도착 시점의 입력값」 으로 판정한다"
      status: unverified
      verification: "Task 2 경쟁 응답 테스트"
    - statement: "MUST NOT 브라우저(html.native-app 없음)에서 팝오버 때문에 네이티브 메시지를 보낸다"
      status: unverified
      verification: "Task 3 브라우저 케이스 postMessage 0"
---

<objective>
G-21-CR 웹 쪽 다섯 건 — **WR-01**(로그인 next 역슬래시 오픈 리다이렉트) · **IN-07**(최근 검색 code 형식 미검증) · **WR-05**(/search 늦은 응답이 새 검색어 결과로 표시) · **WR-04**(Popover 가 오버레이 계수에서 빠져 Android 뒤로가기가 페이지를 떠남) · **IN-06**(theme-color 가 OS 다크모드를 따름). 21-REVIEW.md 수정안을 그대로 적용한다 — 새 추상화 없이 이미 있는 함수·마커를 옮기거나 붙인다.

Purpose: 같은 phase 가 navigate 에서 막은 우회를 로그인에서도 막고, 검색·뒤로가기·브라우저 크롬 색이 사용자가 본 것과 어긋나지 않는다.
Output: `lib/safe-path.ts`(navigate 에서 이동) · recent-search 검증 · 훅 `resultsQuery` · PopoverContent 마커 · theme-color 동기 · 테스트.

WR-04 설계 판단(기록): 오버레이 계수를 「back 대상」과 「탭바 숨김」 둘로 나누지 않는다 — 팝오버가 열린 동안 탭바가 숨는 것은 D-12 ③ 오버레이 규칙과 같은 판정이고, 나누면 계수 두 벌이 갈라질 위험만 생긴다.

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add(`git add -A` 금지 — 작업 트리에 다른 세션 변경이 있다) · 커밋 직전 `git status -sb` · **push 금지**(21-36).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-REVIEW.md
@webapp/src/lib/native/native-bridge-provider.tsx
@webapp/src/app/login/page.tsx
@webapp/src/app/auth/callback/route.ts
@webapp/src/lib/recent-search.ts
@webapp/src/hooks/use-debounced-search.ts
@webapp/src/components/search/search-page-client.tsx
@webapp/src/components/ui/popover.tsx
@webapp/src/components/ui/dialog.tsx
@webapp/src/app/layout.tsx
@webapp/src/components/providers/theme-provider.tsx

<interfaces>
```text
native-bridge-provider.tsx ≈81~86: function isSafeInternalPath(path: unknown): path is string — 문자열 · '/' 시작 · '//' 아님 · 역슬래시·C0·DEL 없음 (navigate 가 사용)
login/page.tsx ≈49~52: safeNext = rawNext && rawNext.startsWith('/') && !rawNext.startsWith('//') ? rawNext : '/' → 앱은 window.location.replace(safeNext)
auth/callback/route.ts ≈32~36: 같은 모양의 약한 가드 → NextResponse.redirect(`${origin}${safeNext}`)
recent-search.ts: sanitize(raw) ≈24~35 · pushRecentSearch(item) ≈71~80 · 파일 규율 ④ throw 금지
useDebouncedSearch(query, delayMs=300): { results, loading, error } — 진행 중 요청은 다음 타이머 발화 때 abort · GlobalSearch 도 소비(새 필드는 추가만)
search-page-client.tsx ≈79~93: 응답 도착 시점 입력을 기록하는 로컬 상태 + trimmedRef 이펙트 · pending 판정 · ≈137~141 handleSubmit
dialog.tsx ≈71: DialogPrimitive.Content 첫 자식 <NativeOverlayMarker /> — popover 도 같은 모양
layout.tsx ≈41~51 viewport.themeColor = 미디어 분기 두 값(light #ffffff · dark 옛 근사값)
theme-provider.tsx: NextThemesProvider attribute=class defaultTheme=dark enableSystem=false
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — isSafeInternalPath 공유 모듈 → 로그인 · /auth/callback · navigate 세 소비처 · 우회 표 테스트 (WR-01)</name>
  <files>webapp/src/lib/safe-path.ts, webapp/src/lib/__tests__/safe-path.test.ts, webapp/src/app/login/page.tsx, webapp/src/app/login/__tests__/page.test.tsx, webapp/src/app/auth/callback/route.ts, webapp/src/lib/native/native-bridge-provider.tsx</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-REVIEW.md (WR-01 전문)
    - webapp/src/lib/native/native-bridge-provider.tsx (머리 주석 · 80~86 로컬 정의 · 167~176 navigate)
    - webapp/src/app/login/page.tsx (40~100행)
    - webapp/src/app/auth/callback/route.ts (전체)
    - webapp/src/app/login/__tests__/page.test.tsx (40~120행 — location.replace mock · `//evil.com` 케이스 모양)
  </read_first>
  <action>
    ① 새 `webapp/src/lib/safe-path.ts`: `export function isSafeInternalPath(path: unknown): path is string` — native-bridge-provider 의 로컬 정의를 **그대로** 옮긴다. 머리 주석: 「같은 출처 절대 경로 판정의 유일한 정의 — login next(WR-01) · /auth/callback next · 네이티브 navigate 가 이 함수만 쓴다. URL 파서는 특수 스킴에서 역슬래시를 슬래시로 읽고 탭·개행을 지우므로 역슬래시·제어문자를 거부해야 한다」.
    ② `native-bridge-provider.tsx`: 로컬 정의를 지우고 `import { isSafeInternalPath } from '@/lib/safe-path'`.
    ③ `login/page.tsx` · `auth/callback/route.ts`: `safeNext` 를 `isSafeInternalPath(rawNext) ? rawNext : '/'` 로 바꾸고 가드 주석에 「WR-01 — lib/safe-path.ts 한 곳」 을 적는다. 브라우저 OAuth `redirectTo` 조립은 그대로.
    ④ 테스트: `safe-path.test.ts`(신규) 표 — 통과 `/` · `/me` · `/trading?focus=a%7Cb` · `/stocks/005930?tab=news&view=news` / 거부 `//evil.com` · 역슬래시 경로(`'/' + '\\' + 'evil.com'`) · 탭 문자 경로 · 개행 문자 경로 · `https://evil.com` · `evil.com` · `''` · `null` · `undefined` · `42`. `login/__tests__/page.test.tsx` 에 「앱 + ?next=/%5Cevil.com → location.replace("/")」 케이스 하나(기존 `//evil.com` 케이스 모양 재사용). callback 은 같은 함수 import 로 잠그고 별도 라우트 테스트는 만들지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/__tests__/safe-path.test.ts src/app/login/__tests__/page.test.tsx src/lib/native/__tests__/native-bridge-provider.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or reports "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function isSafeInternalPath" webapp/src/lib/safe-path.ts` ≥ 1
    - 세 소비처: `grep -c "isSafeInternalPath" webapp/src/app/login/page.tsx` ≥ 1 · `grep -c "isSafeInternalPath" webapp/src/app/auth/callback/route.ts` ≥ 1 · `grep -c "from '@/lib/safe-path'" webapp/src/lib/native/native-bridge-provider.tsx` ≥ 1
    - 로컬 정의 제거: `test "$(grep -c '^function isSafeInternalPath' webapp/src/lib/native/native-bridge-provider.tsx)" -eq 0`
    - `grep -c "%5Cevil" webapp/src/app/login/__tests__/page.test.tsx` ≥ 1
    - verify 명령 통과(native-bridge-provider 기존 navigate 테스트 포함)
  </acceptance_criteria>
  <done>로그인 · 콜백 · navigate 가 한 함수로 같은 출처 경로만 받고, 역슬래시·제어문자 우회는 `/` 로 떨어진다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 검색 정합 — 최근 검색 코드 형식(IN-07) · useDebouncedSearch resultsQuery 로 /search 판정(WR-05)</name>
  <files>webapp/src/lib/recent-search.ts, webapp/src/lib/__tests__/recent-search.test.ts, webapp/src/hooks/use-debounced-search.ts, webapp/src/components/search/search-page-client.tsx, webapp/src/components/search/__tests__/search-page-client.test.tsx</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-REVIEW.md (IN-07 · WR-05 경쟁 순서)
    - webapp/src/lib/recent-search.ts · webapp/src/lib/__tests__/recent-search.test.ts (전체)
    - webapp/src/hooks/use-debounced-search.ts (전체)
    - webapp/src/components/search/search-page-client.tsx (70~150행)
    - webapp/src/components/search/__tests__/search-page-client.test.tsx (fake timers · searchStocksMock · 로딩/결과 0 케이스)
  </read_first>
  <behavior>
    - 저장값 `[{code:'../me'},{code:'x?y'},{code:'005930'},{code:'0000J0'}]`(name 포함) → 읽으면 005930 · 0000J0 두 개만
    - `pushRecentSearch({code:'../me', name:'x'})` → 저장하지 않고 기존 목록을 그대로 반환(throw 없음)
    - 「삼」 요청 뒤 「삼성」 입력 · 타이머 발화 전 「삼」 응답(빈 배열) 도착 → 「검색 중…」 표시 · 결과 0 문구 없음 · Enter 해도 router.push 미호출 · 최근 검색 저장 없음 · 이어 「삼성」 결과 도착 → 결과 행 표시
  </behavior>
  <action>
    ① IN-07: `recent-search.ts` 에 `export const RECENT_CODE_RE = /^[0-9A-Z]{6}$/` 를 두고 `sanitize` 는 맞지 않는 code 면 `null`, `pushRecentSearch` 는 맞지 않으면 쓰지 않고 `readRecentSearches()` 를 반환한다. 머리 주석 ⑤ 에 「종목 코드는 6자리 대문자·숫자만 — `/stocks/${code}` 템플릿에 경로 조작 문자가 들어가면 안 된다(IN-07)」. 기존 테스트 픽스처 코드가 6자리 형식이 아니면 6자리로 바꾼다.
    ② WR-05: `useDebouncedSearch` 반환에 `resultsQuery: string`(현재 results/error 가 어느 trimmed 검색어의 것인지 · 초기 `''`)을 더한다 — 요청 시점 `trimmed` 를 클로저로 잡아 성공과 오류(AbortError 제외)에서 설정, 빈 입력 분기에서 `''`. `search-page-client.tsx` 는 응답 도착 시점 입력을 기록하던 로컬 상태 · ref · 이펙트를 지우고 `pending = typing && trimmed !== resultsQuery` 로 판정하며, `handleSubmit` 은 `typing && !showLoading && resultsQuery === trimmed` 일 때만 이동한다. 설명 주석을 새 판정으로 고친다.
    ③ behavior 세 줄을 각 테스트 파일에 추가한다(기존 케이스 통과 유지).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/__tests__/recent-search.test.ts src/components/search/__tests__/search-page-client.test.tsx src/components/search/__tests__/global-search.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const RECENT_CODE_RE" webapp/src/lib/recent-search.ts` ≥ 1 · `grep -n "RECENT_CODE_RE.test" webapp/src/lib/recent-search.ts` 가 두 줄(sanitize · push)
    - `grep -c "'../me'" webapp/src/lib/__tests__/recent-search.test.ts` ≥ 1
    - `grep -c "resultsQuery" webapp/src/hooks/use-debounced-search.ts` ≥ 1 · `grep -c "resultsQuery" webapp/src/components/search/search-page-client.tsx` ≥ 1
    - 옛 판정 제거: `test "$(grep -c 'trimmedRef' webapp/src/components/search/search-page-client.tsx)" -eq 0`
    - `grep -c "늦은" webapp/src/components/search/__tests__/search-page-client.test.tsx` ≥ 1
    - verify 명령 통과(GlobalSearch 회귀 없음)
  </acceptance_criteria>
  <done>최근 검색은 종목 코드 형식만 다루고, /search 는 결과가 속한 검색어로만 판정한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PopoverContent NativeOverlayMarker(WR-04) · theme-color = 앱 테마(IN-06)</name>
  <files>webapp/src/components/ui/popover.tsx, webapp/src/lib/native/__tests__/native-overlay.test.tsx, webapp/src/app/layout.tsx, webapp/src/components/providers/theme-provider.tsx, webapp/e2e/specs/theme-default.spec.ts</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-REVIEW.md (WR-04 · IN-06)
    - webapp/src/components/ui/popover.tsx · webapp/src/components/ui/dialog.tsx (55~85 — 마커 삽입 모양)
    - webapp/src/lib/native/__tests__/native-overlay.test.tsx (전체 — 앱/브라우저 헬퍼 · back() 케이스)
    - webapp/src/app/layout.tsx (17~51행) · webapp/src/components/providers/theme-provider.tsx (전체)
    - webapp/e2e/specs/theme-default.spec.ts (50~80행 — 새 컨텍스트 · 저장값 준비 모양)
  </read_first>
  <behavior>
    - 앱 모드에서 Popover 를 열면 overlay {open:true} 1회, 닫혀 언마운트되면 {open:false} 1회
    - 앱 모드 Popover 열림 중 `window.__ghTrade.back()` → true 이고 팝오버가 닫힌다
    - 브라우저 모드에서 Popover 를 열고 닫아도 postMessage 0
  </behavior>
  <action>
    ① WR-04: `PopoverContent` 가 `children` 을 구조 분해해 `PopoverPrimitive.Content` 안 첫 자식으로 `<NativeOverlayMarker />`, 그다음 `{children}` 을 렌더한다(dialog.tsx 와 같은 한 줄 주석 · WR-04). 나머지 props · 클래스 · Portal 그대로 · native-bridge-provider/marker 무수정. native-overlay.test.tsx 에 behavior 세 케이스(제어형 `open` · 기존 헬퍼 재사용).
    ② IN-06: `layout.tsx` `viewport.themeColor` 를 단일값 `'#17171c'`(D-23a 기본 다크)로 바꾸고 주석을 「theme-color = 앱 테마(OS 다크모드 아님 · IN-06). 클라이언트는 ThemeColorSync 가 갱신」으로 고친다. `theme-provider.tsx` 에 비공개 `ThemeColorSync`(상수 `THEME_COLOR = { dark: '#17171c', light: '#ffffff' }`)를 두고 `NextThemesProvider` 안에서 렌더 — `useTheme().resolvedTheme` 가 정해지면 `meta[name="theme-color"]` 의 content 를 설정(메타 없으면 아무것도 안 함 · null 렌더). e2e(theme-default.spec.ts) 「theme-color 메타 = 앱 테마(IN-06)」: 저장값 없는 새 컨텍스트 `/login` 에서 content `#17171c` 이고 `colorScheme: 'light'` 에뮬레이트에서도 같음 · localStorage `theme=light` 면 `#ffffff`.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/native/__tests__/native-overlay.test.tsx src/components/trading/lc/__tests__/setting-group.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/theme-default.spec.ts && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or no summary line matches "[0-9]+ passed", or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "NativeOverlayMarker" webapp/src/components/ui/popover.tsx` ≥ 2 (import + 렌더) · `grep -c "Popover" webapp/src/lib/native/__tests__/native-overlay.test.tsx` ≥ 1
    - `grep -c "themeColor: '#17171c'" webapp/src/app/layout.tsx` ≥ 1 · `test "$(grep -c 'prefers-color-scheme' webapp/src/app/layout.tsx)" -eq 0`
    - `grep -c "ThemeColorSync" webapp/src/components/providers/theme-provider.tsx` ≥ 2 · `grep -c "IN-06" webapp/e2e/specs/theme-default.spec.ts` ≥ 1
    - verify 두 명령 통과
  </acceptance_criteria>
  <done>앱에서 팝오버가 열린 동안 뒤로가기가 팝오버부터 닫고, 브라우저 크롬 색이 앱 테마를 따른다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `?next=` → 로그인 후 이동 | 링크 제공자가 제어하는 입력이 이동 대상이 된다 |
| localStorage → 최근 검색 링크 | 조작 가능한 저장값이 링크가 된다 |
| 검색 응답 → 이동·저장 | 다른 검색어의 결과로 모르는 종목 화면에 간다 |
| 웹 오버레이 상태 → 네이티브 뒤로가기 | 계수가 빠지면 편집 맥락을 버리고 페이지를 떠난다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-61 | Spoofing | 로그인 next 오픈 리다이렉트(WR-01) | high | mitigate | isSafeInternalPath 한 정의 · login/callback/navigate 공용 · 역슬래시·제어문자 거부 · 표 테스트(Task 1) |
| T-21-32 | Tampering | 최근 검색 code 경로 조작(IN-07) | low | mitigate | RECENT_CODE_RE 읽기·쓰기 필터(Task 2) |
| T-21-62 | Tampering | /search 경쟁 응답(WR-05) | medium | mitigate | 훅이 결과의 검색어 반환 · Enter 는 일치할 때만 · 경쟁 테스트(Task 2) |
| T-21-63 | Denial of Service | 팝오버 열림 중 뒤로가기 페이지 이탈(WR-04) | medium | mitigate | PopoverContent 마커 → back() 합성 Escape 로 팝오버만 닫음(Task 3) |
| T-21-64 | Information Disclosure | theme-color 메타 | low | accept | 공개 색 값뿐 — 정합 수정만(Task 3) |
</threat_model>

<verification>
- vitest(safe-path · login · native-bridge-provider · recent-search · search-page-client · global-search · native-overlay · setting-group) · Playwright theme-default · typecheck
- 사람 확인: Android 에서 상승률 상위 필터 팝오버 → 뒤로가기 = 팝오버만 닫힘(21-36 UAT)
</verification>

<success_criteria>
- WR-01 · IN-07 · WR-05 · WR-04 · IN-06 이 리뷰 수정안대로 닫히고 테스트로 잠겼다.
</success_criteria>

## Flagged assumptions

- **엣지 탐침 MOBILE-01 (unclassified · unresolved):** 경계는 경로 표(정상 4 · 우회 10) · 코드 표 · 경쟁 응답 한 순서로 명시했다. 나머지는 21-36 UAT.
- 종목 코드는 6자리 대문자·숫자라고 본다(`0000J0` 형식 포함). 최근 검색은 검색 결과(6자리)만 저장하므로 좁혀도 손실이 없다.

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| 스케치 `.planning/sketches/008-uat3-surfaces/`(index.html · README.md) · MANIFEST 008 행 | 목업 | 21-25 |
| CONTEXT D-12a · D-12b · D-25a · D-29 · D-30 · D-31 · D-32(R3-11 축소: 스크롤 복원 + 스켈레톤 없음 · 상태 초기화 허용) · WR-03 androidx.webkit 선언 답 | 결정 기록 | 21-25 |
| `PAD_CHIPS.만원` = 천만(+1,000) · 오천만(+5,000) · 1억(+10,000) · 지우기 · 20-CONTEXT D-17 포인터 | 상수 · 결정 | 21-26 |
| `SettingGroup` 제목 없는 그룹 상단 `pt-1` | 클래스 | 21-26 |
| 카드 헤더 ✕ `data-slot="card-close"` · 종목정보 팝업 ✕ = `size-8` + lucide `XIcon` + `after:-inset-1.5`(히트 44) · 팝업 고정 높이 · `scrollbar-gutter:stable` · 폰 safe-area 패딩 | 컴포넌트 | 21-26 |
| `webapp/src/lib/safe-path.ts` `isSafeInternalPath` · `recent-search.ts` `RECENT_CODE_RE` · `PopoverContent` 안 `NativeOverlayMarker` · `useDebouncedSearch` 반환 `resultsQuery` · `viewport.themeColor` `#17171c` + `ThemeColorSync` | 함수 · 훅 · 메타 | 21-27 |
| iOS 키보드 사유 즉시 숨김 · 재표시 90ms · `awaitingContent`(didStartProvisionalNavigation → 첫 route/1.5초) · 초기 숨김 · 브리지 출처 scheme·host·port 비교 · 오프라인 `dev=1` · 스모크 기기 지정(`IOS_DEVICE_UDID` · `ANDROID_SERIAL`) · mobile/README 명령표 | Swift · 스크립트 · 문서 | 21-28 |
| Android IME 애니메이션 콜백 즉시 숨김 · `awaitingContent`(onPageStarted) · `pullBlocked` 초기화 · 오프라인 뒤로가기(재현 시) · 오프라인 `dev=1` · 폴백 페이지 dev=1 일 때만 localhost · allowBackup=false + `res/xml/data_extraction_rules.xml` · 브리지 WebMessageListener(21-25 답 a) | Kotlin · XML · HTML | 21-29 |
| `NewsFullList` · `DiscussionFullList` · `useNewsView` · `StockNewsTabPanel` · 섹션 `onShowAll` · 종목상세 `?tab=news&view=news\|discussions` · 옛 URL 리다이렉트 · 팝업 `newsView` | 컴포넌트 · 훅 · 라우트 | 21-30 |
| `useTabRootScrollMemory`(`webapp/src/lib/tab-scroll-memory.ts` · AppShell 에서 호출) · 기존 `lib/query-cache` 재사용: 검색 허브 · 대화 목록 · 오늘 주문 | 훅 · 캐시 | 21-31 |
| 앱 공용 패널 숨김 CSS · 카드 더티 바 탭바 비킴(프로브) · `StrategyLogFeedProvider` · `useStrategyLogFeed` · `diffLimitChasers` · /me 전략 로그(`me-strategy-log`) | CSS · Provider · 컴포넌트 | 21-32 |
| 작업대 `?code=` 소비(카드 보장·펼침·포커스) · 카드 수동주문 주문유형(지정가/시간외종가) | 컴포넌트 | 21-33 |
| 종목상세 「트레이딩」 CTA(폰 바 + `detail-trading-button`) · 호가주문 탭 제거 · `?tab=orderbook` → `/trading?code=` · 호가 탭 전용 코드·`variant` 삭제 · orderbook.spec 선별 이전 | 컴포넌트 · 정리 · e2e | 21-34 |
| 게이트 기록 · `21-REVIEW-R2.md`(13건 대조) · UAT 환경(21-35-SUMMARY) · UAT 3차 재검증 · push 결정(21-36-SUMMARY) | 문서 | 21-35 · 21-36 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-27-SUMMARY.md` when done — 다섯 finding 별 변경 · 테스트 결과 · WR-04 계수 판단.
</output>
