---
phase: 21-gh-trade-mobile-app
plan: 20
type: execute
wave: 8
depends_on: ["21-16"]
files_modified:
  - mobile/ios/App/App/GHTradeTabBar.swift
  - mobile/ios/App/App/GHTradeTheme.swift
  - mobile/ios/App/App/GHTradeBridgeViewController.swift
  - webapp/src/styles/globals.css
  - webapp/e2e/specs/native-shell.spec.ts
  - .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
autonomous: true
gap_closure: true
requirements: [MOBILE-01]
coupling_justified:
  - "21-21: 같은 D-27b 상수를 Android 에 옮긴다 — 파일 겹침 없음 · 웹 여백 98 은 두 플랫폼 공통이라 어느 순서로도 결과가 같다(두 플랜 모두 21-24 UAT 전에 끝난다)"
  - "21-18: 21-18 이 iOS ThemeStore 기본을 dark 로 바꾸고 이 플랜은 사전 로드 자리값을 dark 로 바꾼다 — 자리값은 첫 렌더 전에 ThemeStore.load() 가 덮으므로 순서 무관"
  - "21-17 · 21-19: 이 플랜 Task 3 이 두 플랜의 결정(D-28 · D-08b)을 CONTEXT 에 기록한다 — 21-17 선택은 21-17-SUMMARY 가 있으면 인용, 없으면 「21-17 Task 1 결정」으로 적는다(문서 기록은 순서 무관)"

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G-21-1 · D-27b: iOS 탭바에 탭 이름 텍스트가 없고 아이콘이 세로 가운데에 있다 — VoiceOver 는 탭 이름(홈·검색·트레이딩·AI·마이)을 그대로 읽는다(accessibilityLabel 유지)"
    - "D-27b: iOS 탭바 = 스케치 007 C — 높이 60 · radius 30 · 테두리 없음 · 유리(ultra-thin 재질 + glass 틴트 다크 #2c2c35 62% / 라이트 #ffffff 72%) · 그림자 두 겹(0,4 r12 .14 + 0,1 r1 .08) · 활성 = 아이콘 뒤 캡슐 56×36 radius 18 primary 16% · 좌우 16 · 바닥 max(inset−14, 14) 불변 · iPad 폭 560 가운데 불변 · 하단 페이드 86(bg 80% @75%)"
    - "D-27b: 웹 --native-tabbar-offset = gap + 60 + 8 · --native-body-reserve = 98 — 앱에서 종목상세 「주문하기」 CTA 와 본문 마지막 콘텐츠가 60 탭바에 가리지 않는다(e2e: CTA bottom 82 · main 하단 98 · CTA 버튼 아래끝 ≤ 844−74)"
    - "브라우저(native-app 아님)는 종전 그대로 — CTA bottom 0 · 하단 20 · 예약 96 · main 하단 8"
    - "D-23a: iOS 사전 로드 자리값(VC currentTheme · 탭바 초기 팔레트)도 dark — 저장값 없는 첫 프레임과 같은 값"
    - "CONTEXT 에 D-27b · D-23a · D-08b · D-28 이 기록되고 D-27a · D-08a · D-23 원문은 역사로 남는다(대체 포인터만 추가)"
  artifacts:
    - path: mobile/ios/App/App/GHTradeTabBar.swift
      provides: "D-27b 캡슐 C 탭바 · 페이드"
      contains: "systemUltraThinMaterial"
    - path: mobile/ios/App/App/GHTradeTheme.swift
      provides: "GHTradePalette.glass · glassAlpha (다크 #2c2c35 0.62 / 라이트 #ffffff 0.72)"
      contains: "glassAlpha"
    - path: webapp/src/styles/globals.css
      provides: "--native-tabbar-offset = gap + 60 + 8 · --native-body-reserve 98"
      contains: "--native-body-reserve: 98px"
    - path: webapp/e2e/specs/native-shell.spec.ts
      provides: "탭바 여백 수치 단언(82 · 98 · 74)"
      contains: "'98px'"
    - path: .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
      provides: "D-27b · D-23a · D-08b · D-28"
      contains: "D-27b"
  key_links:
    - from: mobile/ios/App/App/GHTradeBridgeViewController.swift
      to: webapp/src/styles/globals.css
      via: "네이티브 탭바 높이 60 ↔ 웹 offset(gap + 60 + 8) · 본문 여백 98 — 한쪽만 바뀌면 CTA 가 가린다"
      pattern: "equalToConstant: 60"
    - from: webapp/e2e/specs/native-shell.spec.ts
      to: webapp/src/styles/globals.css
      via: "computed bottom 82px · paddingBottom 98px 단언"
      pattern: "82px"
    - from: mobile/ios/App/App/GHTradeTabBar.swift
      to: mobile/ios/App/App/GHTradeTheme.swift
      via: "tint = palette.glass.withAlphaComponent(palette.glassAlpha)"
      pattern: "glassAlpha"
  prohibitions:
    - "MUST NOT 시각 라벨을 지우면서 탭의 접근 이름(accessibilityLabel)까지 지운다"
    - "MUST NOT 네이티브 탭바 높이와 웹 --native-tabbar-offset · --native-body-reserve 를 따로 바꿔 주문 CTA 나 본문 끝이 탭바에 가리게 한다"
    - "MUST NOT 바닥 규칙 max(inset−14, 14) · 좌우 16 · iPad 폭 560 을 바꾼다"
    - "MUST NOT D-27a · D-08a · D-23 원문을 지우거나 고쳐 쓴다(대체 포인터만 덧붙인다)"
---

<objective>
G-21-1 (iOS + 웹 여백 + 결정 기록): 탭바 라벨을 없애고 스케치 007 **C · 캡슐 인디케이터 · 높이 60** 을 iOS 에 옮긴 뒤, 웹 본문 하단 여백과 하단 고정 바 기준선을 60 탭바에 맞춘다(21-UAT.md G-21-1 사용자 결정 2026-09-26 · 스케치 007 채택 — 목업 검토 게이트 통과, 새 목업 불필요). 갭 클로징 결정 네 건(D-27b · D-23a · D-08b · D-28)을 CONTEXT 에 기록한다 — CONTEXT 는 이 플랜 한 곳만 수정한다.

Purpose: 사용자가 기대한 라벨 없는 weekly-wine 계열 모양(테두리 없는 유리 · 캡슐 활성)을 iOS 네이티브로 내고, 탭바가 낮아진 만큼 웹 본문을 되돌려 콘텐츠 잠식을 줄인다.
Output: Swift 3파일 · CSS 토큰 2 · native-shell e2e 수치 · CONTEXT 결정 4.

**D-27b 상수 정본(스케치 007 index.html `.vc` · README Winner):** 높이 60 · radius 30(= 높이/2) · 좌우 16 · iPad 폭 560 가운데 · 바닥 max(inset−14, 14) · 테두리 없음 · 배경 glass-hi(다크 `rgba(44,44,53,.62)` = `#2c2c35` 62% · 라이트 `rgba(255,255,255,.72)`) + `backdrop-filter: blur(28px) saturate(1.8)` · 그림자 `0 4px 24px rgba(0,0,0,.14), 0 1px 2px rgba(0,0,0,.08)` · 아이콘 26 세로 가운데 · 활성 = 아이콘 뒤 캡슐 56×36 radius 18 `--primary` 16% + filled 아이콘 + primary 색 · 비활성 `--muted-fg` · 하단 페이드 높이 = 탭바 + 26 = 86, 투명 → `--bg` 80%(75% 지점) · 웹 본문 하단 여백 = 60 + 38 = 98. 색 토큰(bg · primary · muted · card)은 D-27a 그대로.
iOS 매핑: CSS blur 반경은 UIKit 에서 조절 불가 → `.systemUltraThinMaterial`(자체 틴트가 가장 옅어 glass 틴트가 스케치 알파를 떠맡고, 재질이 블러·채도를 준다) · CSS 그림자 blur 24 ≒ UIKit shadowRadius 12, blur 2 ≒ 1(기존 주석의 「CSS 16 ≒ 8」 환산과 같다).

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add · 커밋 직전 `git status -sb` · **push 금지**(21-24 체크포인트 — 웹 여백 98 은 새 네이티브 60 과 같이 나가야 한다).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-UAT.md
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/sketches/007-tab-bar-no-label/README.md
@.planning/sketches/007-tab-bar-no-label/index.html
@.planning/sketches/004-native-tab-bar/README.md
@mobile/ios/App/App/GHTradeTabBar.swift
@mobile/ios/App/App/GHTradeTheme.swift
@mobile/ios/App/App/GHTradeBridgeViewController.swift
@webapp/src/styles/globals.css
@webapp/e2e/specs/native-shell.spec.ts

<interfaces>
```text
GHTradeTabBar (21-10): final class GHTradeTabBar: UIView — pill(clipsToBounds) ⊃ blur · tint · stack(fillEqually) ⊃ GHTradeTabItem×5
  apply(_ p: GHTradePalette) · setActive(_ tab: GHTabID?) · fadeView: GHTradeFadeView · layoutSubviews 에서 shadowPath
GHTradeTabItem: UIControl — highlight(원) · icon(26, SF Symbol 21pt) · 텍스트 라벨 · accessibilityLabel = tab.title · traits .selected
GHTradeFadeView: CAGradientLayer locations/colors — apply(bg:)
GHTradePalette (GHTradeTheme.swift): card · bg · primary · muted · line — Android GhTradePalette(21-21)와 같은 값 유지
GHTradeBridgeViewController.setupTabBar (147~181행): fade 높이 상수 · tabBar 높이 상수 · 바닥 우선순위 제약 · 폭 ≤560
  applyTabBarTheme (211~215행) 주석이 블러 재질 이름을 적고 있다 · currentTheme 초기 자리값(44행)
globals.css §21 (617~647행): --native-tabbar-gap · --native-tabbar-offset · --native-body-reserve · 주석이 D-27a 수치를 적고 있다 (657 · 686행 주석도)
native-shell.spec.ts (178~272행): 머리 주석 수식 · 앱 390 홈 / 앱 390 종목상세 / 브라우저 390 종목상세 / 앱 1280 홈
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 라벨 없는 60 탭바(iOS) ↔ 웹 offset·본문 98 ↔ native-shell e2e 수치</name>
  <files>mobile/ios/App/App/GHTradeTabBar.swift, mobile/ios/App/App/GHTradeBridgeViewController.swift, webapp/src/styles/globals.css, webapp/e2e/specs/native-shell.spec.ts</files>
  <read_first>
    - mobile/ios/App/App/GHTradeTabBar.swift (전체 — 123~218행 GHTradeTabItem · 84~88행 shadowPath)
    - mobile/ios/App/App/GHTradeBridgeViewController.swift (145~181행 setupTabBar)
    - webapp/src/styles/globals.css (617~705행 §21)
    - webapp/e2e/specs/native-shell.spec.ts (178~272행)
    - .planning/sketches/007-tab-bar-no-label/index.html (`.vc` 규칙 · `--tbh` 60 · `.vc .body` · `.vc .fade`)
  </read_first>
  <action>
    ① `GHTradeTabBar.swift` — `GHTradeTabItem`: 탭 이름 텍스트 라벨 뷰를 통째로 지운다(프로퍼티 · 생성 · 제약 · 색 지정 · 눌림 알파). `accessibilityLabel = tab.title` · `isAccessibilityElement` · traits 는 그대로(D-27b · 접근 이름 유지). 아이콘 26×26 을 `centerXAnchor` + `centerYAnchor` 로 셀 정중앙에 둔다(윗변 상수 제거). 강조 뷰를 캡슐 56×36 · `layer.cornerRadius = 18` 로 바꾸고 역시 `centerXAnchor` + `centerYAnchor` 가운데. 눌림(`isHighlighted`)은 아이콘 알파만 0.55. 알약 모서리 반경과 `layoutSubviews` 의 그림자 경로 반경을 모두 30 으로(`cornerRadius = 30` · `cornerRadius: 30`). 헤더 주석·아이템 주석을 D-27b 수치로 고친다(라벨 없음 · 높이 60 · radius 30 · 캡슐 56×36).
    ② `GHTradeBridgeViewController.swift` setupTabBar: 탭바 높이 제약 상수를 60(`equalToConstant: 60`), 페이드 높이 상수를 86(`equalToConstant: 86` — 스케치 `--tbh + 26`)으로. 바닥 우선순위 제약(+14 · ≤ −14) · 폭(화면−32 · ≤560 · 가운데)은 그대로. 머리 주석의 D-27a 를 D-27b 로.
    ③ `globals.css` §21: `html.native-app` 의 `--native-tabbar-offset` 을 `calc(var(--native-tabbar-gap) + 60px + 8px)` 로, `--native-body-reserve` 를 `98px` 로. 블록 주석의 「탭바 기하의 정본」 문장을 D-27b(높이 60 · 바닥 max(inset − 14, 14) · 위 여유 8 · 본문 하단 여백 98 = 60 + 20 + 18)로, 657행 주석의 D-27a 를 D-27b 로, 686행 주석의 본문 몫 108 을 98 로 고친다. `--native-tabbar-gap` 식과 브라우저 0 값은 그대로.
    ④ `native-shell.spec.ts`: 머리 주석 수식을 offset = 14 + 60 + 8 = 82 · 본문 = 60 + 20 + 18 = 98 · 예약 76(불변)으로, 「★ 값이 바뀌면」 문장의 D-27a 를 D-27b 로. 단언: 앱 main paddingBottom 세 곳을 `'98px'` 로 · 앱 종목상세 CTA 바 bottom 을 `'82px'` 로 · 「탭바 윗변(화면 끝에서 14 + 60 = 74)」 주석과 `expect(box!.y + box!.height).toBeLessThanOrEqual(844 - 74)` 로. 테스트 제목 속 수치(92 · 108)도 82 · 98 로. 브라우저 테스트(0 · 20 · 96 · 8)는 그대로.
    ⑤ 손보는 표면(탭바) 안에서 아이콘 치우침·잘림·겹침이 보이면 바로 고치고 SUMMARY 에 한 줄로 보고한다.
    ⑥ 확인: Playwright native-shell 전체 · `native:build:ios` · (dev 서버 :3100 이 없으면 `PORT=3100 pnpm --filter @gh-radar/webapp run dev` 로 띄운 뒤) `native:smoke:ios`.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/native-shell.spec.ts</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or no summary line matches "[0-9]+ passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:ios</automated>
    <fails_when>non-zero exit, or output lacks "BUILD SUCCEEDED" or "SIM ENTITLEMENTS OK"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:smoke:ios</automated>
    <fails_when>non-zero exit, or output lacks "SMOKE OK ready platform=ios", or contains "SMOKE FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - 라벨 뷰 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/ios/App/App/GHTradeTabBar.swift | grep -c 'UILabel')" -eq 0`
    - 접근 이름 유지: `grep -c "accessibilityLabel = tab.title" mobile/ios/App/App/GHTradeTabBar.swift` ≥ 1
    - 가운데 정렬 · 캡슐: `grep -c "centerYAnchor" mobile/ios/App/App/GHTradeTabBar.swift` ≥ 1 · `grep -c "equalToConstant: 56" mobile/ios/App/App/GHTradeTabBar.swift` ≥ 1 · `grep -c "equalToConstant: 36" mobile/ios/App/App/GHTradeTabBar.swift` ≥ 1 · `grep -cE "cornerRadius[[:space:]]*=[[:space:]]*18" mobile/ios/App/App/GHTradeTabBar.swift` ≥ 1
    - 반경 30: `grep -cE "cornerRadius[[:space:]]*=[[:space:]]*30" mobile/ios/App/App/GHTradeTabBar.swift` ≥ 1 · `grep -c "cornerRadius: 30" mobile/ios/App/App/GHTradeTabBar.swift` ≥ 1
    - 옛 반경 제거(알약): `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/ios/App/App/GHTradeTabBar.swift | grep -cE 'cornerRadius[[:space:]]*=[[:space:]]*32')" -eq 0`
    - 옛 반경 제거(그림자 경로): `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/ios/App/App/GHTradeTabBar.swift | grep -c 'cornerRadius: 32')" -eq 0`
    - VC 높이: `grep -c "equalToConstant: 60" mobile/ios/App/App/GHTradeBridgeViewController.swift` ≥ 1 · `grep -c "equalToConstant: 86" mobile/ios/App/App/GHTradeBridgeViewController.swift` ≥ 1
    - VC 옛 탭바 높이 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/ios/App/App/GHTradeBridgeViewController.swift | grep -c 'equalToConstant: 70')" -eq 0`
    - VC 옛 페이드 높이 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/ios/App/App/GHTradeBridgeViewController.swift | grep -c 'equalToConstant: 120')" -eq 0`
    - CSS: `grep -c "+ 60px + 8px" webapp/src/styles/globals.css` ≥ 1 · `grep -c "native-body-reserve: 98px" webapp/src/styles/globals.css` ≥ 1 · `grep -c "D-27b" webapp/src/styles/globals.css` ≥ 1
    - CSS 옛 offset 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' webapp/src/styles/globals.css | grep -c '70px + 8px')" -eq 0`
    - CSS 옛 본문 여백 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' webapp/src/styles/globals.css | grep -c 'native-body-reserve: 108px')" -eq 0`
    - e2e 새 수치: `grep -c "'98px'" webapp/e2e/specs/native-shell.spec.ts` ≥ 1 · `grep -c "'82px'" webapp/e2e/specs/native-shell.spec.ts` ≥ 1 · `grep -c "844 - 74" webapp/e2e/specs/native-shell.spec.ts` ≥ 1
    - e2e 옛 본문 수치 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' webapp/e2e/specs/native-shell.spec.ts | grep -c "'108px'")" -eq 0`
    - e2e 옛 CTA 수치 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' webapp/e2e/specs/native-shell.spec.ts | grep -c "'92px'")" -eq 0`
    - verify 세 명령 통과
  </acceptance_criteria>
  <done>iOS 탭바가 라벨 없는 60 높이로 서고, 웹 본문·하단 고정 바가 같은 60 기준으로 비켜 서며, 그 수치가 e2e 로 잠겼다.</done>
</task>

<task type="auto">
  <name>Task 2: iOS 캡슐 C 시각 — 테두리 제거 · 유리(ultra-thin + glass 틴트) · 그림자 두 겹 · 캡슐 16% · 페이드 80% · 다크 자리값 · 대비 계산</name>
  <files>mobile/ios/App/App/GHTradeTabBar.swift, mobile/ios/App/App/GHTradeTheme.swift, mobile/ios/App/App/GHTradeBridgeViewController.swift</files>
  <read_first>
    - mobile/ios/App/App/GHTradeTabBar.swift (Task 1 결과 — setup · apply · configure · GHTradeFadeView)
    - mobile/ios/App/App/GHTradeTheme.swift (GHTradePalette 필드 · hex 헬퍼)
    - mobile/ios/App/App/GHTradeBridgeViewController.swift (44행 currentTheme 자리값 · 211~215행 applyTabBarTheme 주석)
    - .planning/sketches/007-tab-bar-no-label/index.html (`.t-dark`/`.t-light` 의 `--glass-hi` · `.vc .tb` · `.vc .tb .it .hl` · `.vc .fade`)
    - mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt (필드 이름을 같게 — 21-21 이 같은 이름으로 바꾼다)
  </read_first>
  <action>
    ① `GHTradeTheme.swift` `GHTradePalette`: 알약 테두리용 `line` 필드를 없애고 `glass: UIColor`(다크 `#2c2c35` · 라이트 `#ffffff`, 불투명 RGB)와 `glassAlpha: CGFloat`(다크 0.62 · 라이트 0.72 — 스케치 `--glass-hi` 알파)를 더한다. `card` · `bg` · `primary` · `muted` 는 그대로. 머리 주석의 정본을 「D-27a 색 토큰 + D-27b 유리」로, 「Android GhTradePalette 와 같은 값(glass RGB 동일 · Android 는 실블러가 없어 glassAlpha 대신 94% 근사 — A10)」으로 고친다.
    ② `GHTradeTabBar.swift` 본체:
      - 블러 재질을 `.systemUltraThinMaterial` 로 바꾸고 틴트 색을 `p.glass.withAlphaComponent(p.glassAlpha)` 로(`apply`).
      - 알약의 1px 헤어라인 테두리(폭·색 지정 두 줄)를 지운다.
      - 그림자 두 겹: 본체 레이어 = 그림자 불투명도 0.14 · 오프셋 (0, 4) · 반경 12(CSS blur 24 환산) · 경로 반경 30. 두 번째 접촉 그림자 = 알약 뒤에 넣은 투명 `CALayer` 프로퍼티 `contactShadow` — 불투명도 0.08 · 오프셋 (0, 1) · 반경 1 · 같은 반경 30 경로, `layoutSubviews` 에서 프레임·경로 갱신(암묵 애니메이션 끔).
      - 캡슐 강조 색 = `palette.primary.withAlphaComponent(0.16)`(`configure`).
      - 페이드(`GHTradeFadeView`): 위치 `[0, 0.75, 1]` · 알파 0 → 0.80 → 0.80(`withAlphaComponent(0.8)`), 같은 색 알파 보간 규칙 유지. 주석 「탭바 위 86pt · bg 80% (D-27b)」.
      - 초기 팔레트 자리값을 `.dark` 로(D-23a — 첫 `applyTabBarTheme` 전 잠깐의 값도 저장값 없는 기본과 같게).
    ③ `GHTradeBridgeViewController.swift`: `currentTheme` 초기 자리값을 `.dark` 로(D-23a). `applyTabBarTheme` 주석의 재질 이름을 ultra-thin 재질로 고친다.
    ④ 대비 계산(판정 근거를 SUMMARY 에): WCAG 상대 휘도 공식으로 node 한 줄 계산 — 유리 면 = glass 를 glassAlpha 로 bg 위에 합성한 색, 캡슐 면 = primary 16% 를 그 유리 면 위에 합성한 색. 다크·라이트 각각 (비활성 muted vs 유리 면) · (활성 primary vs 캡슐 면) 대비비를 적는다. 모두 3.0 이상(WCAG 1.4.11 비텍스트)이어야 하며, 미달이면 glassAlpha 를 스케치 범위 안에서 올리는 수정 후 재계산하고 SUMMARY 에 이유를 적는다.
    ⑤ 손보는 표면(탭바) 안의 시각 결함은 바로 고치고 한 줄 보고. 확인: `native:build:ios` · `native:smoke:ios`.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:ios</automated>
    <fails_when>non-zero exit, or output lacks "BUILD SUCCEEDED" or "SIM ENTITLEMENTS OK"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:smoke:ios</automated>
    <fails_when>non-zero exit, or output lacks "SMOKE OK ready platform=ios", or contains "SMOKE FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - 유리: `grep -c "systemUltraThinMaterial" mobile/ios/App/App/GHTradeTabBar.swift` ≥ 1 · `grep -c "glassAlpha" mobile/ios/App/App/GHTradeTabBar.swift` ≥ 1
    - 옛 재질 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/ios/App/App/GHTradeTabBar.swift | grep -c 'systemThinMaterial')" -eq 0`
    - 테두리 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/ios/App/App/GHTradeTabBar.swift | grep -c 'layer.border')" -eq 0`
    - 그림자 두 겹: `grep -n -E "shadow(Opacity|Radius|Offset)" mobile/ios/App/App/GHTradeTabBar.swift` 출력에 본체(불투명도 0.14 · 반경 12 · 오프셋 0,4)와 접촉 그림자(불투명도 0.08 · 반경 1 · 오프셋 0,1) 두 벌이 모두 보인다 · `grep -c "contactShadow" mobile/ios/App/App/GHTradeTabBar.swift` ≥ 1
    - 캡슐 16%: `grep -c "withAlphaComponent(0.16)" mobile/ios/App/App/GHTradeTabBar.swift` ≥ 1
    - 옛 강조 14% 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/ios/App/App/GHTradeTabBar.swift | grep -c 'withAlphaComponent(0.14)')" -eq 0`
    - 페이드 80%: `grep -c "0.75" mobile/ios/App/App/GHTradeTabBar.swift` ≥ 1 · `grep -c "withAlphaComponent(0.8" mobile/ios/App/App/GHTradeTabBar.swift` ≥ 1
    - 옛 페이드 92% 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/ios/App/App/GHTradeTabBar.swift | grep -c 'withAlphaComponent(0.92)')" -eq 0`
    - 다크 자리값(탭바): `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/ios/App/App/GHTradeTabBar.swift | grep -c 'GHTradePalette.of(.light)')" -eq 0`
    - 다크 자리값(VC): `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/ios/App/App/GHTradeBridgeViewController.swift | grep -c 'currentTheme: GHTradeTheme = .light')" -eq 0`
    - 팔레트: `grep -c "glassAlpha" mobile/ios/App/App/GHTradeTheme.swift` ≥ 1 · `grep -ci "0x2c2c35" mobile/ios/App/App/GHTradeTheme.swift` ≥ 1
    - 팔레트 line 필드 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/ios/App/App/GHTradeTheme.swift | grep -c 'let line')" -eq 0`
    - SUMMARY 에 다크·라이트 대비비 4개(비활성·활성 × 2)가 적혀 있고 모두 ≥ 3.0
    - verify 두 명령 통과
  </acceptance_criteria>
  <done>iOS 탭바가 스케치 007 C 의 테두리 없는 유리 캡슐 모양이고, 다크 기본에서도 아이콘 대비가 기준을 넘는다.</done>
</task>

<task type="auto">
  <name>Task 3: CONTEXT 에 갭 클로징 결정 4건 기록 — D-27b · D-23a · D-08b · D-28 (원문 보존 + 대체 포인터)</name>
  <files>.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md (전체 — `<decisions>` 의 D-08a(39행) · D-23(57행) · D-27a(65행) · 「### Claude's Discretion」 앞자리)
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-1 · N1 · N2 · N3 `decision` 줄 — 문구의 정본)
    - .planning/sketches/007-tab-bar-no-label/README.md (Winner)
    - .planning/phases/21-gh-trade-mobile-app/21-17-SUMMARY.md 가 있으면 Task 1 선택
  </read_first>
  <action>
    ① `<decisions>` 안 「### Claude's Discretion」 바로 앞에 새 소제목 「### 갭 클로징 결정 (UAT 2차 · 2026-09-26 사용자 확정)」을 넣고 네 항목을 적는다(각 항목 끝에 근거 = 21-UAT.md gap id):
      - **D-27b (G-21-1 · 스케치 007 C — D-27a 의 탭바 상수를 대체):** 라벨 없음(접근 이름은 iOS accessibilityLabel · Android contentDescription 유지) · 아이콘 26 세로 가운데 · 활성 = 아이콘 뒤 캡슐 56×36 radius 18 `--primary` 16% + filled 아이콘 + primary · 비활성 `--muted-fg` · 테두리 없음 · 유리 = glass-hi(다크 `#2c2c35` 62% · 라이트 `#ffffff` 72%) + 블러 28 + 채도 1.8(iOS = ultra-thin 재질 + glass 틴트 · Android = 실블러 없음 → glass RGB 94% 불투명 근사, A10 선례) · 그림자 `0 4px 24px rgba(0,0,0,.14)` + `0 1px 2px rgba(0,0,0,.08)`(iOS 두 겹 · Android elevation 12dp 근사) · 높이 60 · radius 30 · 좌우 16 · iPad 폭 560 가운데 · 바닥 max(inset−14, 14) 불변 · 하단 페이드 86(= 60 + 26, `--bg` 80% @75%) · 웹 `--native-tabbar-offset` = gap + 60 + 8 · 본문 하단 여백 98(= 60 + 20 + 18). D-27a 의 색 토큰 · 좌우 · 바닥 · iPad · 아이콘/스플래시는 유지. — Reversibility: reversible.
      - **D-23a (G-21-N1):** 저장된 테마가 없을 때 기본 = 다크, 웹·앱 공통 — 웹 next-themes 기본 dark(enableSystem=false 유지) · iOS/Android ThemeStore 미저장 dark · 오프라인 폴백은 `?theme=light` 가 아니면 다크 · 사전 로드 자리값도 dark. 저장값이 있으면 그대로(기본값은 저장하지 않는다). 스플래시(OS 런치 화면)는 D-22 대로 OS 외형을 따른다.
      - **D-08b (G-21-N2 — D-08a 의 테마 버튼 접근 이름을 대체):** 테마 버튼 아이콘 = 누르면 바뀔 테마(목적지: 다크일 때 Sun · 라이트일 때 Moon) — 사이드바 ThemeToggle 과 /me 계정 카드 공통 · 접근 이름 = 행동 문구 「다크 모드로 전환」/「라이트 모드로 전환」 · 정의는 `theme-toggle.tsx` 의 `ThemeSwitchIcon` · `THEME_SWITCH_LABEL` 한 곳.
      - **D-28 (G-21-N3):** 앱에서 서버 호스트(`server.url` 호스트, 운영 trade.jx1.io) 밖 http(s) 최상위 이동·새 창 요청 = 인앱 브라우저(iOS SFSafariViewController · Android Custom Tabs). mailto · tel · 앱 딥링크 등 비 http(s) 는 기존 Capacitor 기본(시스템). 같은 호스트는 WebView 유지(target=_blank 포함). 방식 = 네이티브 셸 가로채기(NavigationDelegateProxy · GhTradeWebViewClient) · 웹 코드 무수정 · npm 패키지 추가 없음 · `allowNavigation` 은 계속 비움 · 시스템으로 넘길 예외 호스트 없음(21-15 네이티브 로그인). Android Custom Tabs 구현 경로 = 21-17 Task 1 결정(SUMMARY 가 있으면 선택 a/b/c 를 적는다). — Reversibility: reversible.
    ② 원문은 고치지 않는다 — D-27a · D-08a · D-23 항목 **끝에** 한 줄씩 대체 포인터만 덧붙인다: D-27a 「→ 2026-09-26 D-27b 가 탭바 상수(높이 · radius · 배경 · 테두리 · 그림자 · 활성 표시 · 라벨 · 페이드 · 본문 여백)를 대체」 · D-08a 「→ D-08b 가 테마 버튼 아이콘 규칙·접근 이름을 대체」 · D-23 「→ D-23a: 미저장 기본 = 다크(웹·앱 공통)」. `<code_context>` 의 「기본 light」 줄 끝에도 「(→ D-23a 기본 dark)」.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && for id in D-27b D-23a D-08b D-28; do grep -q "\*\*${id} " .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md || { echo "MISSING ${id}"; exit 1; }; done && grep -q "높이 70 · radius 32" .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md && echo "CONTEXT DECISIONS OK"</automated>
    <fails_when>output contains "MISSING", or non-zero exit (D-27a original text removed), or "CONTEXT DECISIONS OK" is absent</fails_when>
  </verify>
  <acceptance_criteria>
    - 네 결정 머리(`**D-27b ` · `**D-23a ` · `**D-08b ` · `**D-28 `)가 모두 있다(verify 루프) — 각 머리는 `grep -n` 으로 한 줄씩만 보인다
    - `grep -c "갭 클로징 결정 (UAT 2차" .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md` ≥ 1
    - D-27a 원문 보존: `grep -c "높이 70 · radius 32" .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md` ≥ 1 · 대체 포인터 `grep -c "D-27b 가 탭바 상수" .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md` ≥ 1
    - D-08a 원문의 계정 카드 수치 보존: `grep -c "우측 40×40 radius 12" .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md` ≥ 1 · 포인터 `grep -c "D-08b 가 테마 버튼" .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md` ≥ 1
    - `grep -c "D-23a: 미저장 기본 = 다크" .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md` ≥ 1
    - D-28 이 예외 호스트 없음과 allowNavigation 비움을 적는다: `grep -c "계속 비움" .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md` ≥ 1 · `grep -c "예외 호스트 없음" .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md` ≥ 1
  </acceptance_criteria>
  <done>갭 클로징의 네 결정이 CONTEXT 에 정본으로 남고, 이전 결정 원문은 역사로 보존됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 네이티브 탭바 ↔ 웹 하단 고정 요소 | 두 층이 같은 기하 상수를 따로 가진다 — 어긋나면 주문 CTA 가 가린다 |
| 스크린리더 ↔ 탭바 | 시각 라벨 제거 뒤에도 탭 이름이 접근성 트리에 남아야 한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-30 | Denial of Service | 탭바가 종목상세 주문 CTA · /trading 하단 패널을 가림 | high | mitigate | 네이티브 60 ↔ offset(gap+60+8) · 본문 98 을 한 태스크에서 함께 변경 · e2e computed 82/98 · CTA 아래끝 ≤ 844−74(Task 1) · 21-24 UAT 항목 1 |
| T-21-51 | Denial of Service | 라벨 제거로 VoiceOver 가 탭을 구분 못 함 | medium | mitigate | accessibilityLabel = tab.title 유지 grep · traits .selected 유지(Task 1 acceptance) · 21-24 UAT 스크린리더 확인 |
| T-21-54 | Tampering | 탭바 상수 정본 이원화(D-27a vs 구현) | low | mitigate | D-27b 단일 정본 기록 + globals.css · e2e 주석이 D-27b 를 가리킴(Task 1 · Task 3) |
</threat_model>

<verification>
- Playwright native-shell 전체 · `native:build:ios` · `native:smoke:ios` 통과
- CONTEXT 네 결정 기록 확인
- 사람 확인(라벨 없음 · 캡슐 · 유리 · 그림자 · 60 높이 · iPad 560 · 다크 대비 · CTA 비가림)은 21-24 UAT 항목 1
</verification>

<success_criteria>
- G-21-1 iOS 측과 웹 여백이 D-27b 한 정본으로 맞춰졌고, 결정 네 건이 CONTEXT 에 남았다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| Android `ExternalLinks.opensInAppBrowser(scheme, host, appHost)` · `ExternalLinksTest`(정본 26케이스) | Kotlin 순수 함수 · JUnit | 21-17 |
| Android `InAppBrowser.open(activity, uri, theme)` · `GhTradeWebViewClient.shouldOverrideUrlLoading` 가로채기 | Kotlin | 21-17 |
| `mobile/android/variables.gradle` `androidxBrowserVersion` · `app/build.gradle` androidx.browser 선언 (21-17 Task 1 선택 a 일 때만) | Gradle | 21-17 |
| `mobile/package.json` `native:test:android`(TabRoutesTest + ExternalLinksTest) · `native:check-external-links:ios` | 스크립트 | 21-17 · 21-22 |
| 웹 `ThemeProvider` 기본 다크 · iOS/Android `ThemeStore` 기본 다크 · `mobile/www/index.html` 기본 다크 | 설정 | 21-18 |
| e2e `webapp/e2e/specs/theme-default.spec.ts` (저장값 없음 다크 · 저장값 유지 · 앱 첫 theme 메시지 · 오프라인 폴백) | Playwright | 21-18 |
| 웹 `THEME_SWITCH_LABEL` · `ThemeSwitchIcon` (theme-toggle.tsx export) — 사이드바 ThemeToggle · AccountCard 공용 목적지 규칙 | 컴포넌트·상수 | 21-19 |
| 단위 `webapp/src/components/layout/__tests__/theme-toggle.test.tsx` | Vitest | 21-19 |
| iOS `GHTradeTabBar` 캡슐 C(라벨 없음 · 56×36 16% · 유리 · 두 겹 그림자 · 높이 60 · 페이드 86) · `GHTradePalette.glass` · `glassAlpha` | Swift | 21-20 |
| CSS `--native-tabbar-offset` = gap + 60 + 8 · `--native-body-reserve` 98 · native-shell e2e 수치(82 · 98 · 74) | 토큰 · Playwright | 21-20 |
| CONTEXT D-27b · D-23a · D-08b · D-28 | 결정 기록 | 21-20 |
| Android `GhTradeTabBar` 캡슐 C 근사(라벨 없음 · 불투명 유리 · elevation 12) · `GhTradePalette.glass` · 탭바 60dp · 페이드 86dp | Kotlin | 21-21 |
| iOS `ExternalLinks.opensInAppBrowser(scheme:host:appHost:)` · `isSameAppHost` · `NavigationDelegateProxy`(decidePolicyFor · WKUIDelegate createWebViewWith · SFSafariViewController) · `mobile/scripts/external-links-check.swift` · `check-external-links-ios.sh` | Swift · 스크립트 | 21-22 |
| 갭 클로징 전 게이트 기록 · push 전 UAT 환경 (21-23-SUMMARY) · UAT 재검증 결과 · push 결정과 이행 (21-24-SUMMARY) | 문서 | 21-23 · 21-24 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-20-SUMMARY.md` when done — D-27b 상수 대응표(스케치 CSS → UIKit) · 대비비 계산 · e2e 결과 · 빌드/스모크 결과를 적는다.
</output>
