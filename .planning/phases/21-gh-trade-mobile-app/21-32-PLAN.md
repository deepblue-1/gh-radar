---
phase: 21-gh-trade-mobile-app
plan: 32
type: execute
wave: 13
depends_on: ["21-25", "21-27"]
files_modified:
  - webapp/src/styles/globals.css
  - webapp/src/components/trading/workbench/shared-panels.tsx
  - webapp/src/components/trading/card/strategy-card.tsx
  - webapp/e2e/specs/trading-workbench.spec.ts
  - webapp/src/lib/strategy-log-feed.tsx
  - webapp/src/lib/__tests__/strategy-log-feed.test.tsx
  - webapp/src/app/layout.tsx
  - webapp/src/components/trading/me-client.tsx
  - webapp/src/components/trading/strategy-status-card.tsx
  - webapp/src/components/trading/__tests__/me-client.test.tsx
autonomous: true
gap_closure: true
requirements: [MOBILE-01]
coupling_justified:
  - "21-27: 같은 layout.tsx 의 viewport.themeColor 를 앞 웨이브에서 고친다 — 이 플랜은 RelayProvider 안쪽에 공급자만 넣는다"
  - "21-25: /me 로그 배치는 스케치 008 ① 채택안(D-25a)이 정본 — Task 3 은 채택안 한 가지만 구현한다"

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G-21-R3-2 (D-25a): 앱(html.native-app)에서 /trading 하단 공용 패널(전 종목 로그 · 잔고 · 미체결)과 spacer 가 폰 · iPad 모두 보이지 않는다 — 브라우저는 폰·데스크톱 모두 종전 그대로 보인다"
    - "G-21-R3-2: 앱에서 카드 더티 바(저장/되돌리기)가 네이티브 탭바에 가리지 않고 탭바 몫(--native-tabbar-offset)만큼 위에 선다"
    - "G-21-R3-2: /me 에서 전 종목 전략 로그를 스케치 008 ① 채택안 배치로 보고, 각 줄에 종목명과 작업대와 같은 문장이 나온다(서버에 말한 것만 · 새 API 없음)"
    - "잃는 동작(잔고 행 → 카드 생성 · 카드 없는 종목 미체결 정정)은 앱에서 범위 밖으로 둔다(21-25 재확인 답)"
  artifacts:
    - path: webapp/src/styles/globals.css
      provides: "html.native-app 공용 패널 · spacer 숨김"
      contains: "shared-panels-spacer"
    - path: webapp/src/lib/strategy-log-feed.tsx
      provides: "diffLimitChasers · StrategyLogFeedProvider · useStrategyLogFeed"
      contains: "export function diffLimitChasers"
    - path: webapp/src/components/trading/me-client.tsx
      provides: "/me 전략 로그 배치(채택안)"
      contains: "D-25a"
  key_links:
    - from: webapp/src/app/layout.tsx
      to: webapp/src/lib/strategy-log-feed.tsx
      via: "RelayProvider 안쪽 StrategyLogFeedProvider — 앱 시작부터 쌓임"
      pattern: "StrategyLogFeedProvider"
    - from: webapp/src/lib/strategy-log-feed.tsx
      to: webapp/src/components/trading/strategy-log.tsx
      via: "strategyLogLine · serverMessageLogLine · strategiesDisabledLogLine 재사용(작업대와 같은 문장)"
      pattern: "strategyLogLine"
  prohibitions:
    - statement: "MUST NOT 브라우저(html.native-app 없음)의 /trading 공용 패널 동작을 바꾼다"
      status: unverified
      verification: "Task 1 e2e — 브라우저 390 · 1280 패널 보임(기존 단언)"
    - statement: "MUST NOT 로그 공급자가 서버를 호출하거나 새 저장소를 만든다 — relay 컨텍스트에서 파생만"
      status: unverified
      verification: "Task 2 acceptance — fetch 0"
---

<objective>
G-21-R3-2 — 앱에서 /trading 하단 공용 패널이 네이티브 탭바와 겹쳐 가리는 문제를 D-25a 대로 닫는다: 앱은 패널을 숨기고, 카드 더티 바는 탭바 위로 비키며, 전 종목 전략 로그는 /me(스케치 008 ① 채택안)로 옮긴다.

Purpose: 앱에서 트레이딩 화면 하단이 탭바와 겹치지 않고, 전 종목 전략 흐름은 마이 탭에서 본다.
Output: CSS 한 규칙 · 더티 바 비킴(프로브는 strategy-card 안 인라인) · 로그 공급자(앱 전역) · /me 배치 · 단위 테스트 · e2e(기존 스위트에 한 개).

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add(`git add -A` 금지 — 다른 세션이 같은 트레이딩 파일을 고칠 수 있다: 커밋 직전 `git status -sb` 로 대상 파일에 내 변경만 있는지 확인) · **push 금지**(21-36).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-UAT.md
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/sketches/008-uat3-surfaces/README.md
@webapp/src/styles/globals.css
@webapp/src/components/trading/workbench/shared-panels.tsx
@webapp/src/components/trading/card/strategy-card.tsx
@webapp/src/components/trading/strategy-log.tsx
@webapp/src/components/trading/me-client.tsx
@webapp/src/lib/relay-provider.tsx

<interfaces>
```text
globals.css §21(≈640~705): 652 대상 목록 주석 · [data-slot="shared-panels"][data-pinned="true"] padding-bottom(664 · 브라우저) · html.native-app [data-slot="shared-panels"] bottom 보정(694) · 핀 패딩 0(697) · --native-tabbar-offset = calc(gap + 60 + 8)
shared-panels.tsx: 루트 data-slot="shared-panels" · data-pinned · spacer data-slot="shared-panels-spacer" · --wb-bottom-inset(핀 상태 실측 높이 · 아니면 0) · 머리 주석 ⑤-c
strategy-card.tsx ≈742 usePinnedToViewportBottom(host, active): inset = --wb-bottom-inset · limit = innerHeight − inset · ≈800 사용 · ≈932 data-slot="card-dirty-host"
strategy-log.tsx(aba7954 이후): StrategyLogEntry(47) · strategyLogLine(220 — 시그니처는 파일을 읽고 따른다) · serverMessageLogLine(277) · strategiesDisabledLogLine(303) · marketCloseDisabledLogLine(314) · StrategyLog({entries, variant, emptyTitle, dense})(343)
relay 컨텍스트: limitChasers(crud D 제외 · key · isin · exchange) · messages(+receivedAt) · strategiesDisabled
작업대 합치기(trading-workbench.tsx): WeakSet seen · 같은 카드 직전 줄 같은 문장 중복 억제 · MAX_MERGED_LOG
layout.tsx 76~86: <RelayProvider> → <ChatProvider>
/me 순서(me-client ①): 제목 → AccountCard → MeStatusBar → StrategyStatusCard → 계좌 카드 N → TodayOrdersCard
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 앱 공용 패널·spacer 숨김(CSS) → 카드 더티 바 탭바 비킴(인라인 프로브) → e2e(앱 390·1024 숨김 · 브라우저 보임 · 바 위치)</name>
  <files>webapp/src/styles/globals.css, webapp/src/components/trading/workbench/shared-panels.tsx, webapp/src/components/trading/card/strategy-card.tsx, webapp/e2e/specs/trading-workbench.spec.ts</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-R3-2 root_cause · artifacts)
    - webapp/src/styles/globals.css (640~705 §21)
    - webapp/src/components/trading/workbench/shared-panels.tsx (머리 주석 ⑤ · 205~260 · spacer)
    - webapp/src/components/trading/card/strategy-card.tsx (735~805 · 895~935)
    - webapp/e2e/specs/trading-workbench.spec.ts (sharedPanels 헬퍼 · 편집 헬퍼 · relay 픽스처)
    - webapp/e2e/fixtures/native-app.ts
  </read_first>
  <action>
    per D-25a: ① `globals.css` §21 의 앱 전용 bottom 보정 규칙(694)과 핀 패딩 0 규칙(697)을 `html.native-app [data-slot="shared-panels"], html.native-app [data-slot="shared-panels-spacer"] { display: none; }` 한 규칙으로 바꾸고, 652 대상 목록 주석의 공용 패널 항목을 「앱에서는 숨김(D-25a)」 으로 고친다. 브라우저 규칙(664)은 그대로.
    ② `shared-panels.tsx` 머리 주석 ⑤-c 를 「앱은 패널·spacer 를 CSS 로 숨긴다(D-25a · G-21-R3-2) — 전 종목 로그는 /me」 로. 숨김 뒤 `--wb-bottom-inset` 이 0 이 아니면(측정이 display:none 을 못 따라가면) 앱일 때 0 을 쓰도록 이 파일에서 보정한다.
    ③ `strategy-card.tsx` `usePinnedToViewportBottom`: 탭바 몫을 읽는 숨은 프로브(문서에 한 번만 · `position:fixed; bottom: var(--native-tabbar-offset, 0px); height:0; visibility:hidden; pointer-events:none` · `data-slot="native-tabbar-probe"`)의 computed `bottom` px 를 이 파일 안 작은 함수로 읽어 `limit = innerHeight − inset − 탭바 몫` 으로 바꾼다(calc 변수는 getPropertyValue 로 px 를 못 읽는다 — 주석). 새 모듈은 만들지 않는다. 함수 주석에 「앱은 탭바 몫까지 비킨다(G-21-R3-2)」.
    ④ e2e(trading-workbench.spec.ts) 「G-21-R3-2 앱 공용 패널 숨김」 하나: installNativeApp + 폭 390 · 1024 → 패널 · spacer 안 보임 / 브라우저 390 · 1280 → 패널 보임 / 앱 390 에서 카드 하나를 더티로 만들고 카드 끝이 화면 아래로 가게 스크롤 → `[data-slot="card-dirty-host"]` 아래끝 ≤ `innerHeight − 82`(14 + 60 + 8 · 안전영역 0).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/trading-workbench.spec.ts -g "G-21-R3-2|공용 패널"</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or no summary line matches "[0-9]+ passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/__tests__/shared-panels.test.tsx src/components/trading/__tests__/strategy-card.test.tsx src/components/trading/__tests__/strategy-card-flow.test.tsx && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'html.native-app \[data-slot="shared-panels-spacer"\]' webapp/src/styles/globals.css` ≥ 1 · `grep -c "D-25a" webapp/src/styles/globals.css` ≥ 1
    - 앱 전용 핀 패딩 규칙 제거: `test "$(grep -c 'html.native-app \[data-slot="shared-panels"\]\[data-pinned' webapp/src/styles/globals.css)" -eq 0`
    - `grep -c "native-tabbar-probe" webapp/src/components/trading/card/strategy-card.tsx` ≥ 1
    - `grep -c "G-21-R3-2" webapp/e2e/specs/trading-workbench.spec.ts` ≥ 1
    - verify 두 명령 통과
  </acceptance_criteria>
  <done>앱에서 /trading 하단 패널이 사라지고 카드 더티 바가 탭바 위에 서며, 브라우저는 그대로다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 전 종목 전략 로그 공급자 — diffLimitChasers(순수) · messages · strategiesDisabled → StrategyLogFeedProvider(layout)</name>
  <files>webapp/src/lib/strategy-log-feed.tsx, webapp/src/lib/__tests__/strategy-log-feed.test.tsx, webapp/src/app/layout.tsx</files>
  <read_first>
    - webapp/src/components/trading/strategy-log.tsx (머리 주석 ①~ · 순수 함수들의 현재 시그니처 · StrategyLogEntry)
    - webapp/src/components/trading/workbench/trading-workbench.tsx (로그 합치기 · 중복 억제 · MAX_MERGED_LOG — `grep -n "MAX_MERGED_LOG"` 로 위치 확인)
    - webapp/src/lib/relay-provider.tsx (컨텍스트 값 · Provider 밖 기본값)
    - webapp/src/lib/isin-labels.ts · webapp/src/lib/limit-chaser.ts (exchangeLabeledName)
    - webapp/src/app/layout.tsx (60~90 Provider 순서)
  </read_first>
  <behavior>
    - diffLimitChasers(빈 Map, [A]) → A 의 등록 줄 1개
    - diffLimitChasers({A}, [A 동일]) → 0줄 · 런타임 카운터만 다른 에코도 0줄(strategy-log 의 기존 판정 재사용)
    - diffLimitChasers({A, B}, [A]) → B 의 삭제 줄 1개
    - 공급자: 같은 messages 항목은 한 번만 · strategiesDisabled null→값이면 일괄 비활성화 줄 1개 · 최신이 index 0 · 최대 200 줄
  </behavior>
  <action>
    ① `lib/strategy-log-feed.tsx`('use client'): `export function diffLimitChasers(prev: ReadonlyMap<string, RelayLimitChaser>, next: readonly RelayLimitChaser[])` → `{ key, isin, exchange, text }[]` — strategy-log.tsx 의 현재 순수 함수(전이 문장 · 런타임 전용 에코 판정)를 그대로 불러 쓴다(발주 여부 같은 이 화면이 모르는 맥락은 넘기지 않는다 — 주석). prev 에만 있는 키는 삭제 줄. `StrategyLogFeedProvider({ children })`: `useRelayContext()` 의 limitChasers 변화마다 ref 의 직전 Map 과 diff, messages 새 항목(WeakSet)마다 `serverMessageLogLine`, strategiesDisabled null→값마다 `strategiesDisabledLogLine()` 을 `StrategyLogEntry`(id `feed-log-N` · at = 로컬 HH:MM:SS 또는 receivedAt · level · who = `exchangeLabeledName(이름 ?? isin, exchange)`)로 앞에 쌓고 200 줄로 자른다. 같은 키 직전 줄과 같은 문장은 다시 쌓지 않는다. `export function useStrategyLogFeed()`(Provider 밖이면 빈 배열). 머리 주석: D-25a · 브라우저 메모리 전용(새로고침하면 빈다) · 서버에 말한 것만 · 새 API/스키마 없음.
    ② `app/layout.tsx`: `<RelayProvider>` 바로 안쪽(ChatProvider 바깥)에 `<StrategyLogFeedProvider>` — 앱 시작부터 쌓이도록(주석 한 줄).
    ③ 테스트: behavior 네 줄 — diff 는 순수 함수 직접, 공급자는 relay 컨텍스트 mock 으로 렌더.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/__tests__/strategy-log-feed.test.tsx src/components/trading/__tests__/strategy-log.test.tsx && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or reports "No test files found", or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function diffLimitChasers" webapp/src/lib/strategy-log-feed.tsx` ≥ 1 · `grep -c "export function useStrategyLogFeed" webapp/src/lib/strategy-log-feed.tsx` ≥ 1 · `grep -c "StrategyLogFeedProvider" webapp/src/app/layout.tsx` ≥ 2
    - 서버 호출 없음: `test "$(grep -c 'fetch(' webapp/src/lib/strategy-log-feed.tsx)" -eq 0`
    - verify 명령 통과
  </acceptance_criteria>
  <done>앱 전역 공급자가 전 종목 전략 전이를 작업대와 같은 문장으로 쌓는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /me 전 종목 전략 로그 — 스케치 008 ① 채택안 배치</name>
  <files>webapp/src/components/trading/me-client.tsx, webapp/src/components/trading/strategy-status-card.tsx, webapp/src/components/trading/__tests__/me-client.test.tsx</files>
  <read_first>
    - .planning/sketches/008-uat3-surfaces/README.md · index.html (① Winner · 수치 · 잃는 동작 답)
    - webapp/src/components/trading/me-client.tsx (① 세로 순서 계약 · 렌더)
    - webapp/src/components/trading/strategy-status-card.tsx (채택안이 B 일 때만)
    - webapp/src/components/trading/strategy-log.tsx (StrategyLog variant · dense · emptyTitle)
    - webapp/src/components/trading/__tests__/me-client.test.tsx (relay 컨텍스트 mock 모양)
  </read_first>
  <behavior>
    - 로그 공급자 값이 있으면 `[data-slot="me-strategy-log"]` 에 종목명(who)과 문장이 보인다
    - 값이 없으면 빈 문구 「아직 기록이 없어요」
    - DmaGate(미매핑) 분기에는 로그가 없다
  </behavior>
  <action>
    채택안(D-25a · 스케치 008 ①) **한 가지만** 구현한다(/me 는 웹·앱 공용 화면): A = `<StrategyStatusCard />` 바로 뒤 `<section data-slot="me-strategy-log">` 카드(제목 「전략 로그」 · `StrategyLog variant="embed"` entries=`useStrategyLogFeed()` · 채택 수치의 최대 높이 안 스크롤) / B = `strategy-status-card.tsx` 제목 줄 세그먼트 「현황 | 로그」(기본 현황) · 로그 본문에 data-slot / C = `<TodayOrdersCard />` 뒤 같은 카드. 수치(간격 · 최대 높이 · 제목 크기)는 README Winner 그대로. `me-client.tsx` 머리 주석 ① 세로 순서에 새 칸(「D-25a · G-21-R3-2」). 잃는 동작 ②③ 은 구현하지 않는다(21-25 답이 이견이면 그 답을 SUMMARY 에 인용하고 따른다). 테스트: behavior 세 줄.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/__tests__/me-client.test.tsx src/components/trading/__tests__/strategy-status-card.test.tsx && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `cat webapp/src/components/trading/me-client.tsx webapp/src/components/trading/strategy-status-card.tsx | grep -c 'data-slot="me-strategy-log"'` ≥ 1 · `cat webapp/src/components/trading/me-client.tsx webapp/src/components/trading/strategy-status-card.tsx | grep -c "useStrategyLogFeed"` ≥ 1
    - `grep -c "D-25a" webapp/src/components/trading/me-client.tsx` ≥ 1
    - SUMMARY 에 구현한 채택안(A/B/C)과 수치 대조가 있다
    - verify 명령 통과
  </acceptance_criteria>
  <done>/me 에서 전 종목 전략 로그를 채택된 배치로 본다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay 컨텍스트(전략 상태) → 로그 문장 | 서버가 말한 전이만 사용자에게 사실로 보여야 한다 |
| 앱 셸 → 웹 레이아웃 | 앱 전용 숨김이 브라우저로 새면 브라우저 사용자가 패널을 잃는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-88 | Repudiation | 로그가 발주 사실을 지어냄 | medium | mitigate | 작업대와 같은 순수 함수 재사용 · 이 화면이 모르는 맥락은 넘기지 않음(Task 2) |
| T-21-89 | Denial of Service | 브라우저 공용 패널 사라짐 | medium | mitigate | 규칙을 html.native-app 로 한정 · e2e 브라우저 보임 단언(Task 1) |
| T-21-90 | Tampering | 더티 바가 탭바에 가려 저장 불가 | medium | mitigate | 탭바 몫만큼 비킴 · e2e 위치 단언(Task 1) |
</threat_model>

<verification>
- Playwright trading-workbench `-g "G-21-R3-2|공용 패널"` · vitest(shared-panels · strategy-card · strategy-card-flow · strategy-log-feed · strategy-log · me-client · strategy-status-card) · typecheck
- 사람 확인(앱 /trading 하단 · 더티 바 · /me 로그 흐름)은 21-36 UAT
</verification>

<success_criteria>
- G-21-R3-2 가 D-25a 대로 닫혔다(앱 패널 숨김 · 더티 바 비킴 · /me 로그).
</success_criteria>

## Flagged assumptions

- **엣지 탐침 MOBILE-01 (unclassified · unresolved):** 경계는 앱/브라우저 × 390/1024/1280 · 200 줄 상한으로 명시했다. 나머지는 21-36 UAT.
- 로그는 앱 세션 메모리 전용이다 — 앱을 새로 열면 빈다(작업대 로그와 같은 성질). 영속 로그는 범위 밖.

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| 스케치 `.planning/sketches/008-uat3-surfaces/`(index.html · README.md) · MANIFEST 008 행 | 목업 | 21-25 |
| CONTEXT D-12a · D-12b · D-25a · D-29 · D-30 · D-31 · D-32(R3-11 축소: 스크롤 복원 + 스켈레톤 없음 · 상태 초기화 허용) · WR-03 androidx.webkit 선언 답 | 결정 기록 | 21-25 |
| `PAD_CHIPS.만원` = 천만(+1,000) · 오천만(+5,000) · 1억(+10,000) · 지우기 · 20-CONTEXT D-17 포인터 | 상수 · 결정 | 21-26 |
| `SettingGroup` 제목 없는 그룹 상단 `pt-1` | 클래스 | 21-26 |
| 카드 헤더 ✕ `data-slot="card-close"` · 종목정보 팝업 ✕ = `size-8` + lucide `XIcon` + `after:-inset-1.5`(히트 44) · 팝업 고정 높이 · `scrollbar-gutter:stable` · 폰 safe-area 패딩 | 컴포넌트 | 21-26 |
| `webapp/src/lib/safe-path.ts` `isSafeInternalPath` · `recent-search.ts` `RECENT_CODE_RE` · `PopoverContent` 안 `NativeOverlayMarker` · `useDebouncedSearch` 반환 `resultsQuery` · `viewport.themeColor` `#17171c` + `ThemeColorSync` | 함수 · 훅 · 메타 | 21-27 |
| iOS 키보드 사유 즉시 숨김 · 재표시 90ms · `awaitingContent`(didStartProvisionalNavigation → 첫 route/1.5초) · 초기 숨김 · 브리지 출처 scheme·host·port 비교 · 오프라인 `dev=1` · 스모크 기기 지정(`IOS_DEVICE_UDID` · `ANDROID_SERIAL`) · mobile/README 명령표 | Swift · 스크립트 · 문서 | 21-28 |
| Android IME 애니메이션 콜백 즉시 숨김 · `awaitingContent`(onPageStarted) · `pullBlocked` 초기화 · 오프라인 뒤로가기(재현 시) · 오프라인 `dev=1` · 폴백 페이지 dev=1 일 때만 localhost · allowBackup=false + `res/xml/data_extraction_rules.xml` · 브리지 WebMessageListener(21-25 답 a) | Kotlin · XML · HTML | 21-29 |
| `NewsFullList` · `DiscussionFullList` · `useNewsView` · `StockNewsTabPanel` · 섹션 `onShowAll` · 종목상세 `?tab=news&view=news\|discussions` · 옛 URL 리다이렉트 · 팝업 `newsView` | 컴포넌트 · 훅 · 라우트 | 21-30 |
| `useTabRootScrollMemory`(`webapp/src/lib/tab-scroll-memory.ts` · AppShell 에서 호출) · 기존 `lib/query-cache` 재사용: 검색 허브 · 대화 목록 · 오늘 주문 | 훅 · 캐시 | 21-31 |
| 앱 공용 패널 숨김 CSS · 카드 더티 바 탭바 비킴(프로브) · `StrategyLogFeedProvider` · `useStrategyLogFeed` · `diffLimitChasers` · /me 전략 로그(`me-strategy-log`) | CSS · Provider · 컴포넌트 | 21-32 |
| 작업대 `?code=` 소비(카드 보장·펼침·포커스) · 카드 수동주문 주문유형(지정가/시간외종가) | 컴포넌트 | 21-33 |
| 종목상세 「트레이딩」 CTA(폰 바 + `detail-trading-button`) · 호가주문 탭 제거 · `?tab=orderbook` → `/trading?code=` · 호가 탭 전용 코드·`variant` 삭제 · orderbook.spec 선별 이전 | 컴포넌트 · 정리 · e2e | 21-34 |
| 게이트 기록 · `21-REVIEW-R2.md`(13건 대조) · UAT 환경(21-35-SUMMARY) · UAT 3차 재검증 · push 결정(21-36-SUMMARY) | 문서 | 21-35 · 21-36 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-32-SUMMARY.md` when done — CSS 규칙 · 더티 바 수치 · 공급자 계약 · 채택안 구현 대조 · 테스트 결과.
</output>
