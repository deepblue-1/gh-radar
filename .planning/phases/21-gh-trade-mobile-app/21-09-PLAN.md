---
phase: 21-gh-trade-mobile-app
plan: 09
type: execute
wave: 5
depends_on: ["21-06", "21-07"]
files_modified:
  - webapp/src/app/layout.tsx
  - webapp/src/components/layout/app-header.tsx
  - webapp/src/app/login/page.tsx
  - webapp/src/app/design/page.tsx
  - webapp/src/app/design/_sections/layouts.tsx
  - webapp/src/components/layout/__tests__/app-shell-chrome.test.tsx
  - webapp/src/components/me/account-card.tsx
  - webapp/src/components/me/__tests__/account-card.test.tsx
  - webapp/src/components/trading/me-client.tsx
  - webapp/e2e/specs/brand-account.spec.ts
autonomous: true
requirements: [MOBILE-01]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-21: 노출 문자열만 GH Trade 로 바뀐다 — 문서 title `GH Trade` · 헤더 로고 텍스트 `GH Trade` · 로고 링크 `aria-label=\"GH Trade 홈\"` · 로그인 카드 제목 「GH Trade에 로그인」 · `/design` 카탈로그 2곳"
    - "D-21: `gh-radar:` localStorage 키 · `[gh-radar]` 콘솔 접두 · 패키지명 `@gh-radar/*` · 테스트 픽스처 `e2e@gh-radar.local` 은 그대로다"
    - "D-08 · D-08a: `/me` 에서 「My page」 제목 아래에 계정 카드 A(`data-slot=\"account-card\"` · `--card` radius 16 · padding 14/16 · 높이 72 · 아바타 44 `avatar_url` → 이니셜(`--raised-2` 바탕 17/700) · 이름 16/700 · 이메일 12.5 muted · 우측 40×40 radius 12 `--muted` 아이콘 버튼 2개)가 보이고, 카드 아래 16 여백 뒤 기존 순서(상태줄 → 전략 현황 → 계좌 카드)는 불변이다 — 웹·앱 공통, DMA 게이트 화면에서도 카드는 보인다"
    - "D-08a: 테마 버튼(`aria-label=\"테마 전환\"`)은 다크일 때 Sun · 라이트일 때 Moon 을 보이고 누르면 반대 테마로 바꾼다(하이드레이션 전 mounted 가드) · 로그아웃 버튼(`aria-label=\"로그아웃\"` · `--up` 색 LogOut)은 `useAuth().signOut`(하드 리다이렉트 `/login`)을 부른다"
    - "D-08: 사이드바 하단 `UserSection`·`ThemeToggle` 은 그대로 남는다"
  artifacts:
    - path: webapp/src/components/me/account-card.tsx
      provides: "AccountCard — /me 계정 카드 A"
      exports: ["AccountCard"]
    - path: webapp/e2e/specs/brand-account.spec.ts
      provides: "MOBILE-01i 브랜드 · MOBILE-01h 계정 카드 e2e"
  key_links:
    - from: webapp/src/components/trading/me-client.tsx
      to: webapp/src/components/me/account-card.tsx
      via: "<AccountCard /> — header 다음 · DmaGate 위"
      pattern: "<AccountCard />"
    - from: webapp/src/components/me/account-card.tsx
      to: webapp/src/lib/auth-context.tsx
      via: "useAuth() — user · displayName · signOut"
      pattern: "useAuth\\(\\)"
---

<assumption_delta_decision>
- 검출기 `assumption-delta scan 21` → `detected: false`. 수동 판정: 두 번째 클라이언트 플랫폼 — 정본 명사 **웹앱**, 결정 **add-alongside**. 브랜드는 표시명만 바꾸고 식별자(localStorage 키·패키지명)는 유지한다 — 식별 모델을 건드리지 않는다(21-01 블록 참조).
</assumption_delta_decision>

<objective>
두 가지 웹 기능을 끝낸다: (1) 브랜드 표시명 GH Trade(D-21 — 노출 문자열만), (2) `/me` 계정 카드 A(D-08·D-08a — 스케치 005 채택안). 둘 다 웹·앱 공통이며 push 즉시 브라우저 사용자에게도 보인다.

Purpose: 앱 「마이」 탭에서 로그아웃·테마 전환을 사이드바 드로어 없이 할 수 있게 하고, 앱과 웹의 이름을 맞춘다.
Output: 문자열 교체 6곳 + 단언 · AccountCard + 테스트 · MeClient 배치 · e2e.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md
@.planning/sketches/005-search-page-me-account/README.md
@webapp/src/components/layout/user-section.tsx
@webapp/src/components/layout/theme-toggle.tsx
@webapp/src/components/trading/me-client.tsx

<interfaces>
기존 계약(코드에서 추출):

```tsx
// 브랜드 문자열 위치 (grep 결과 — 이 6곳이 전부)
// app/layout.tsx:16            title: 'gh-radar',
// app/design/page.tsx:22       title: 'Design Catalog · gh-radar',
// app/design/page.tsx:41       Phase 3 · gh-radar 디자인 시스템. …
// app/design/_sections/layouts.tsx:14 · :43   <div …>gh-radar</div>
// app/login/page.tsx:67        <CardTitle className="mt-4 text-xl">gh-radar에 로그인</CardTitle>
// components/layout/app-header.tsx:27(주석) · :79 aria-label="gh-radar 홈" · :83 텍스트 gh-radar

// user-section.tsx L30-69 — 아바타 폴백 체인
const { user, displayName, signOut } = useAuth(); const [imgError, setImgError] = useState(false);
if (!user) return null;
const email = user.email ?? ""; const avatarUrl = user.user_metadata?.avatar_url as string | undefined;
const initial = (email[0] ?? "?").toUpperCase(); const name = displayName ?? "사용자";
const showImage = Boolean(avatarUrl) && !imgError;   // <img src alt="" onError={() => setImgError(true)} aria-hidden …/> (eslint-disable no-img-element 주석)

// theme-toggle.tsx — const { resolvedTheme, setTheme } = useTheme(); mounted 가드; current = mounted && resolvedTheme === 'light' ? 'light' : 'dark'

// me-client.tsx L197-215
export function MeClient() {
  const gateReason = useDmaGateReason(); const { accounts, accountStates, status /* + probeNow (21-07) */ } = useRelayContext();
  if (gateReason !== null) return <DmaGate reason={gateReason} surface="전략·잔고·미체결" />;
  return (<div data-slot="me-page" className="flex flex-col gap-[var(--s-3)]"><header …><h1>My page</h1><p>전략 현황 · 미체결 · 잔고</p></header> … 상태줄 → 전략 현황 → 계좌 카드 …</div>);
}
```

```tsx
// 이 플랜이 새로 만드는 계약
export function AccountCard(): JSX.Element | null;   // user 없으면 null · data-slot="account-card"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 브랜드 표시명 GH Trade — 6곳 교체 + 헤더 단언 (D-21)</name>
  <files>webapp/src/app/layout.tsx, webapp/src/components/layout/app-header.tsx, webapp/src/app/login/page.tsx, webapp/src/app/design/page.tsx, webapp/src/app/design/_sections/layouts.tsx, webapp/src/components/layout/__tests__/app-shell-chrome.test.tsx</files>
  <read_first>
    - webapp/src/app/layout.tsx (L15-18 metadata)
    - webapp/src/components/layout/app-header.tsx (L20-35 머리 주석 · L75-90 로고)
    - webapp/src/app/login/page.tsx (L60-72)
    - webapp/src/app/design/page.tsx (L20-45) · webapp/src/app/design/_sections/layouts.tsx (L10-45)
    - webapp/src/components/layout/__tests__/app-shell-chrome.test.tsx (파일 머리 ①② 목록 · 헤더 렌더 방식)
    - .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md (D-21 — 바꾸지 않는 것 목록)
  </read_first>
  <behavior>
    - `AppShell hideSidebar` 렌더 → `banner` 안에 텍스트 「GH Trade」 · 로고 링크 접근 이름 「GH Trade 홈」 · 「gh-radar」 텍스트 없음
  </behavior>
  <action>
    ① 여섯 곳을 교체한다: `layout.tsx` title `'GH Trade'` · `design/page.tsx` title `'Design Catalog · GH Trade'` 와 본문 「Phase 3 · GH Trade 디자인 시스템.」 · `design/_sections/layouts.tsx` 두 곳 `GH Trade` · `login/page.tsx` 「GH Trade에 로그인」 · `app-header.tsx` `aria-label="GH Trade 홈"` · 로고 텍스트 `GH Trade` · 머리 주석의 로고 설명.
    ② 바꾸지 않는다(D-21): `gh-radar:` 로 시작하는 localStorage 키 · `[gh-radar]` 로그 접두 · `@gh-radar/*` import · `e2e@gh-radar.local` · 패키지·디렉터리 이름. 교체 뒤 `grep -rn "gh-radar" webapp/src` 결과가 이 범주와 테스트 파일뿐인지 확인한다.
    ③ `app-shell-chrome.test.tsx` 에 describe ③ 「브랜드 표시명 GH Trade (D-21)」 케이스를 먼저 더한다(RED): 위 behavior. 파일 머리 목록에 ③ 한 줄 추가.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/layout/__tests__/app-shell-chrome.test.tsx</automated>
    <fails_when>non-zero exit, or the summary contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -rn "gh-radar" webapp/src --include='*.tsx' --include='*.ts' --exclude-dir=__tests__</automated>
    <fails_when>any output line contains a user-visible occurrence — i.e. a match that is not `gh-radar:` (storage key), `[gh-radar]` (log prefix), `@gh-radar/` (package import) or `gh-radar.local`</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "title: 'GH Trade'" webapp/src/app/layout.tsx` 가 1줄
    - `grep -n 'aria-label="GH Trade 홈"' webapp/src/components/layout/app-header.tsx` 가 1줄
    - `grep -n "GH Trade에 로그인" webapp/src/app/login/page.tsx` 가 1줄
    - `grep -n "GH Trade" webapp/src/app/design/_sections/layouts.tsx` 가 2줄 이상
  </acceptance_criteria>
  <done>사용자에게 보이는 이름이 전부 GH Trade 이고, 저장 키·로그 접두·패키지명은 그대로다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `/me` 계정 카드 A — `AccountCard` + MeClient 배치 (D-08 · D-08a)</name>
  <files>webapp/src/components/me/account-card.tsx, webapp/src/components/me/__tests__/account-card.test.tsx, webapp/src/components/trading/me-client.tsx</files>
  <read_first>
    - .planning/sketches/005-search-page-me-account/README.md (「결정」 `/me` 계정 카드 A 수치)
    - .planning/sketches/005-search-page-me-account/index.html (L116-122 `.acct-a` CSS · L302-312 마크업 — 「My page」 제목 다음에 카드)
    - webapp/src/components/layout/user-section.tsx (L30-110 아바타 폴백 · 로그아웃)
    - webapp/src/components/layout/theme-toggle.tsx (mounted 가드 · setTheme)
    - webapp/src/components/trading/me-client.tsx (L195-230 게이트·헤더·본문 순서)
    - webapp/src/components/layout/__tests__/app-sidebar.test.tsx (useAuth 모킹 방식)
  </read_first>
  <behavior>
    - user 없음 → 렌더 결과 null
    - `avatar_url` 있음 → `<img>` · 이미지 `error` 이벤트 → 이메일 첫 글자 대문자 이니셜로 바뀜 · `avatar_url` 없음 → 이니셜
    - 이름 = `displayName`(없으면 「사용자」) · 이메일 표시
    - `resolvedTheme='dark'` → 테마 버튼 안 아이콘이 Sun(`data-icon="sun"`) · 클릭 → `setTheme('light')` · `'light'` → Moon · 클릭 → `setTheme('dark')` · 두 버튼 `aria-label` 이 정확히 「테마 전환」·「로그아웃」
    - 로그아웃 클릭 → `signOut` 1회
    - 루트에 `data-slot="account-card"` · 카드 클래스에 `rounded-[16px]` · `min-h-[72px]`
  </behavior>
  <action>
    ① `webapp/src/components/me/account-card.tsx`('use client') `AccountCard`: `useAuth()` 로 user·displayName·signOut, UserSection 과 같은 폴백 체인(avatar_url → 이니셜, `imgError` 잠금, `<img>` eslint 예외 주석 동일). 테마는 `useTheme()` + mounted 가드(`current = mounted && resolvedTheme === 'light' ? 'light' : 'dark'` — ThemeToggle 과 같은 해석), 아이콘은 다크일 때 `Sun` · 라이트일 때 `Moon`(lucide 18 · `data-icon` 속성으로 테스트 식별). 마크업: `<section data-slot="account-card" aria-label="계정" className="flex min-h-[72px] items-center gap-3 rounded-[16px] bg-[var(--card)] px-4 py-3.5">` · 아바타 `size-11`(44) 원 — 이미지 또는 `bg-[var(--raised-2)] text-[17px] font-bold` 이니셜 · 텍스트 칸 `min-w-0 flex-col` (이름 `text-[16px] font-bold truncate` · 이메일 `text-[12.5px] text-[var(--muted-fg)] truncate`) · 우측 `ml-auto flex gap-1.5` 버튼 2개 `size-10 rounded-[12px] bg-[var(--muted)] grid place-items-center`(테마 `text-[var(--fg)]` `aria-label="테마 전환"` onClick `setTheme(next)` · 로그아웃 `text-[var(--up)]` `aria-label="로그아웃"` onClick `void signOut()`). 머리 주석: D-08·D-08a(스케치 005 A) · 사이드바 UserSection/ThemeToggle 은 그대로 둔다 · 토큰 대응(스케치 `--raised` = 웹 `--muted`).
    ② `me-client.tsx`: 정상 경로에서 `<header>` 바로 다음에 `<div className="mb-1"><AccountCard /></div>`(컨테이너 gap 12 + 4 = D-08a 카드 아래 16), 게이트 경로는 `return (<div className="flex flex-col gap-4"><AccountCard /><DmaGate … /></div>)` 로 감싸 DMA 미매핑 사용자도 로그아웃·테마 전환을 할 수 있게 한다. 상태줄·전략 현황·계좌 카드 순서와 나머지 로직(21-07 `useNativeRefresh(probeNow)` 포함)은 무변경.
    ③ `__tests__/account-card.test.tsx` 를 먼저 쓴다(RED): `vi.mock('@/lib/auth-context')`(가변 user/signOut 스파이) · `vi.mock('next-themes')`(가변 resolvedTheme · setTheme 스파이) 로 behavior 전부.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/me/__tests__/account-card.test.tsx src/components/trading/__tests__/me-client.test.tsx</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or "account-card.test.tsx" is absent from the output</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'data-slot="account-card"' webapp/src/components/me/account-card.tsx` 가 1줄
    - `grep -n 'aria-label="테마 전환"' webapp/src/components/me/account-card.tsx` 와 `grep -n 'aria-label="로그아웃"' webapp/src/components/me/account-card.tsx` 가 각각 1줄
    - `grep -n "<AccountCard />" webapp/src/components/trading/me-client.tsx` 가 2줄 이상(정상·게이트 경로)
    - `git diff --quiet -- webapp/src/components/layout/user-section.tsx webapp/src/components/layout/theme-toggle.tsx` exit 0 (사이드바 쪽 무변경 — 커밋 전 작업 트리 기준)
  </acceptance_criteria>
  <done>`/me` 상단에 계정 카드 A 가 서고 테마 전환·로그아웃이 카드에서 동작한다.</done>
</task>

<task type="auto">
  <name>Task 3: e2e `brand-account.spec.ts` — 제목·로고·키 유지 + 계정 카드 (MOBILE-01i · MOBILE-01h)</name>
  <files>webapp/e2e/specs/brand-account.spec.ts</files>
  <read_first>
    - webapp/e2e/specs/smoke.spec.ts · webapp/e2e/specs/me.spec.ts (L1-40 머리 관례)
    - webapp/e2e/fixtures/home.ts (`mockHomeApi`) · webapp/e2e/fixtures/native-app.ts (`installNativeApp`)
    - webapp/playwright.config.ts (storageState 로그인 사용자 = `E2E_TEST_EMAIL`)
  </read_first>
  <action>
    `webapp/e2e/specs/brand-account.spec.ts` 를 만든다. 케이스: ① `/`(`mockHomeApi`) — `toHaveTitle('GH Trade')` · 헤더 로고 텍스트 「GH Trade」 · 링크 이름 「GH Trade 홈」 · 페이지 로드 뒤 `Object.keys(localStorage)` 중 앱이 쓰는 키가 있으면 모두 `gh-radar:` 로 시작하고 `gh-trade:` 로 시작하는 키가 없다 ② `/me` — `[data-slot="account-card"]` 보임 · 테스트 사용자 이메일(`process.env.E2E_TEST_EMAIL`, 없으면 이메일 형식 텍스트) 포함 · 「테마 전환」 클릭 → `<html>` 의 `dark` 클래스가 토글된다(원상 복구까지 두 번 클릭) · **로그아웃 버튼은 누르지 않는다**(누르면 storageState 세션이 서버에서 폐기돼 이후 spec 이 전부 깨진다 — 주석으로 남긴다), 버튼 존재·접근 이름만 확인 ③ 앱 모드(`installNativeApp`) 390 폭 `/me` 에서 계정 카드가 보이고 사이드바 없이 접근된다. relay 는 띄우지 않는다(계정 카드는 DMA 게이트 화면에서도 보인다).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/brand-account.spec.ts</automated>
    <fails_when>non-zero exit, or output contains "failed" or "No tests found", or lacks "passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "toHaveTitle('GH Trade')" webapp/e2e/specs/brand-account.spec.ts` 가 1줄 · `grep -n "account-card" webapp/e2e/specs/brand-account.spec.ts` 가 1줄 이상
    - spec 이 로그아웃 버튼을 클릭하지 않는다(`grep -n "로그아웃" …` 결과가 존재 확인·주석뿐)
  </acceptance_criteria>
  <done>브랜드 표시와 키 불변, 계정 카드의 존재·테마 전환이 실브라우저에서 잠겼다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → Supabase Auth(로그아웃) | 기존 `signOut` 경로 재사용 |
| 외부 아바타 이미지 URL(Google) | `user_metadata.avatar_url` 을 `<img src>` 로 로드 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-21 | Denial of Service(데이터 유실) | 브랜드 변경이 localStorage 키를 바꿔 사용자 설정 유실 | medium | mitigate | 키·접두 불변(Task 1 ② · e2e ① 키 접두 단언) |
| T-21-34 | Information Disclosure | 아바타 외부 이미지 요청 | low | accept | 기존 UserSection 과 같은 경로(Google 프로필 이미지) · `alt=""` `aria-hidden` · 실패 시 이니셜 |
| T-21-35 | Repudiation | 실수 탭으로 로그아웃 | low | accept | 아이콘 버튼 40×40 · 명확한 접근 이름 · 하드 리다이렉트(기존 동작) — 스케치 005 사용자 채택안 |
| T-21-SC | Tampering | 패키지 설치 | low | accept | 설치 없음 |
</threat_model>

<verification>
- app-shell-chrome · account-card · me-client 단위 green · 브랜드 grep 범주 확인 · typecheck · brand-account.spec green
</verification>

<success_criteria>
- D-21·D-08·D-08a 가 웹·앱 공통으로 완성됐다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/` 패키지 `@gh-radar/mobile` — scripts `native:sync` · `native:sync:dev` · `native:build:ios` · `native:build:android` · `native:smoke:ios` · `native:open:ios` · `native:open:android` · `native:assets` (21-01) · `native:smoke:android` (21-02) · `native:check-tab-routes:ios` (21-10) · `native:test:android` (21-12) · `native:verify-prod` (21-16) | 패키지·스크립트 | 21-01 · 21-02 · 21-10 · 21-12 · 21-16 |
| `mobile/capacitor.config.ts` — appId `com.ghtrade.app` · appName `GH Trade` · `CAP_SERVER_URL` · `SystemBars` (21-01) · `SocialLogin` (21-15) | 설정 | 21-01 · 21-15 |
| `mobile/tsconfig.json` · `mobile/.gitignore` | 설정 | 21-01 |
| `mobile/www/index.html` — 오프라인 폴백(최소 21-01 · `safeTarget`·5초 탐침·`online`·테마 21-11) | 정적 페이지 | 21-01 · 21-11 |
| `mobile/scripts/smoke-ios.sh` · `mobile/scripts/smoke-android.sh` — 「SMOKE OK ready platform=…」 | 스모크 | 21-01 · 21-02 |
| `mobile/scripts/check-tab-routes-ios.sh` · `mobile/scripts/tab-routes-check.swift` | Swift 경로표 검사 | 21-10 |
| `mobile/scripts/render-resources.mjs` · `mobile/resources/{icon.svg,icon-only.png,icon-foreground.png,icon-background.png,splash.png,splash-dark.png}` | 에셋 원본 | 21-14 |
| `mobile/scripts/verify-prod-config.mjs` · `mobile/README.md` | 운영 설정 검사·문서 | 21-16 |
| iOS `SceneDelegate.swift` (루트 = `GHTradeBridgeViewController()` · 창 배경 21-11) | Swift | 21-01 · 21-11 |
| iOS `GHTradeBridgeViewController` · `WeakScriptMessageHandler` · `NativeMessageType` | Swift | 21-01 · 21-10 · 21-11 |
| iOS `GHTradeTabBar` · `GHTradeTab` · `GHTabID` · `GHTradePalette` · `GHTradeTheme` · `TabRoutes.activeTab(forPath:)` · `TabRoutes.hidesTabBar(path:)` | Swift | 21-10 |
| iOS `NavigationDelegateProxy` · `ThemeStore` (UserDefaults `gh-trade.theme`) | Swift | 21-11 |
| Android `MainActivity` (`BridgeActivity`) · `GhTradeWebViewClient` (`BridgeWebViewClient` 상속) · `GhTradeBridge` (`@JavascriptInterface postMessage`) | Kotlin | 21-02 · 21-12 · 21-13 |
| Android `GhTradeTabBar` · `GhTradePalette` · `TabId` · `TabRoutes.activeTabFor` · `TabRoutes.hidesTabBar` · `TabRoutesTest` | Kotlin | 21-12 |
| Android `ThemeStore` (SharedPreferences `gh_trade`/`theme`) · `res/values/bools.xml` · `res/values-sw600dp/bools.xml` (`is_tablet`) | Kotlin·리소스 | 21-13 |
| Android `res/drawable/ic_tab_{home,home_fill,search,search_fill,trading,trading_fill,ai,ai_fill,me,me_fill}.xml` | 벡터 | 21-12 |
| 웹 `lib/native/native-detect.ts` — `NATIVE_DETECT_SCRIPT` · `isNativeApp` · `nativePlatform` | 유틸 | 21-01 |
| 웹 `lib/native/post-native.ts` — `postNative` · `NativeMsgType` | 유틸 | 21-04 |
| 웹 `lib/native/native-bridge-provider.tsx` — `NativeBridgeProvider` · `useNativeBridge` · `NativeRefreshFn` · `window.__ghTrade = { refresh, navigate, back }` | Provider | 21-04 |
| 웹 `lib/native/use-native-refresh.ts` — `useNativeRefresh` | 훅 | 21-04 |
| 웹 `lib/native/native-overlay-marker.tsx` — `NativeOverlayMarker` | 컴포넌트 | 21-04 |
| 웹 `lib/native/google-client-ids.ts` — `GOOGLE_WEB_CLIENT_ID` · `GOOGLE_IOS_CLIENT_ID` · `GOOGLE_ANDROID_CLIENT_ID` · `GOOGLE_IOS_URL_SCHEME` | 공개 식별자 상수 | 21-03 |
| 웹 `lib/native/native-google-login.ts` — `nativeGoogleSignIn` · `randomNonce` · `sha256Hex` · `classifyNativeLoginError` | 서비스 | 21-15 |
| 웹 `RelayConnectionState.probeNow` · `EMPTY_RELAY_VALUE.probeNow` | 훅 계약 | 21-07 |
| 웹 `lib/recent-search.ts` — `RECENT_SEARCH_KEY` · `RECENT_SEARCH_MAX` · `RecentSearchItem` · `readRecentSearches` · `pushRecentSearch` · `removeRecentSearch` · `clearRecentSearches` | 유틸 | 21-08 |
| 웹 `app/search/page.tsx` · `components/search/search-page-client.tsx` — `SearchPageClient` · 사이드바 `NAV_SEARCH_PAGE` | 페이지·상수 | 21-08 |
| 웹 `components/me/account-card.tsx` — `AccountCard` | 컴포넌트 | 21-09 |
| 웹 마크업 계약 `data-slot="app-aside"` · `data-slot="app-menu-button"` · `data-slot="stock-ai-button"` · `data-slot="shared-panels"` · `data-pinned` · `data-order-cta` · `data-slot="account-card"` | 마크업 | 21-05 · 21-06 · 21-09 |
| CSS `@custom-variant native` (21-04) · 앱 셸 규칙 (21-05) · `--app-safe-top` · `--app-safe-bottom` · `--app-safe-left` · `--app-safe-right` · `--native-tabbar-gap` · `--native-tabbar-offset` · `--native-body-reserve` (21-06) | 토큰·변형 | 21-04 · 21-05 · 21-06 |
| 웹 `app/icon.svg` (= 스케치 004 `#app-a`) | 파비콘 | 21-14 |
| 단위 테스트: `native-detect.test.ts` (21-01) · `post-native.test.ts` · `native-bridge-provider.test.tsx` · `native-overlay.test.tsx` (21-04) · `google-client-ids.test.ts` (21-03) · `relay-socket.test.ts` probeNow · `stock-native-refresh.test.tsx` (21-07) · `recent-search.test.ts` · `search-page-client.test.tsx` · `app-sidebar.test.tsx` 보강 (21-08) · `account-card.test.tsx` · `app-shell-chrome.test.tsx` 보강 (21-09) · `native-google-login.test.ts` · `login/__tests__/page.test.tsx` (21-15) · `TabRoutesTest.kt` (21-12) | 단위 | 각 플랜 |
| e2e: `fixtures/native-app.ts` — `installNativeApp` · `nativeMessages` (21-05) · `specs/native-shell.spec.ts` (21-05 · 21-06) · `specs/search-page.spec.ts` (21-08) · `specs/brand-account.spec.ts` (21-09) | Playwright | 각 플랜 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-09-SUMMARY.md` when done
</output>
