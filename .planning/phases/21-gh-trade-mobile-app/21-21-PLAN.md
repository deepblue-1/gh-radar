---
phase: 21-gh-trade-mobile-app
plan: 21
type: execute
wave: 8
depends_on: ["21-16"]
files_modified:
  - mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt
  - mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt
  - mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
autonomous: true
gap_closure: true
requirements: [MOBILE-01]
coupling_justified:
  - "21-20: 같은 D-27b 상수의 iOS 판 — 파일 겹침 없음 · 웹 여백 98 은 21-20 이 바꾸고 이 플랜은 네이티브 60dp 만 맞춘다(둘 다 21-24 UAT 전에 끝난다)"
  - "21-17: 21-17 이 GhTradePalette.of(theme).bg 와 MainActivity.currentTheme getter 를 읽는다 — 이 플랜은 bg 를 유지하고 currentTheme 초기값만 바꾼다(순서 무관)"
  - "21-18: 21-18 이 ThemeStore 기본을 dark 로, 이 플랜이 사전 로드 자리값을 dark 로 — load() 의 applyTheme(ThemeStore.load(this)) 가 첫 프레임 전에 덮으므로 순서 무관"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "G-21-1 · D-27b: Android 탭바에 탭 이름 텍스트가 없고 아이콘이 셀 정중앙에 있다 — TalkBack 은 탭 이름(홈·검색·트레이딩·AI·마이)을 그대로 읽는다(contentDescription 유지)"
    - "D-27b: Android 탭바 = 스케치 007 C 근사 — 높이 60dp · radius 30dp · 테두리 없음 · 유리 = glass RGB(다크 #2c2c35 · 라이트 #ffffff) 94% 불투명(실블러 없음 — A10 선례) · 그림자 elevation 12dp · 활성 = 아이콘 뒤 캡슐 56×36dp radius 18dp primary 16% · 좌우 16dp · 폭 ≤560dp 가운데 · 바닥 max(내비 인셋 − 14, 14) 불변 · 하단 페이드 86dp(bg 80% @75%)"
    - "다크 기본(D-23a)에서 유리 면 위 비활성 아이콘 · 캡슐 위 활성 아이콘 대비비가 3.0 이상이다(계산값이 SUMMARY 에 있다)"
    - "D-23a: Android 사전 로드 자리값(MainActivity.currentTheme · 탭바 초기 팔레트)도 dark"
    - "경로표 · 숨김 · 탭 이동 · 뒤로가기 동작은 그대로다(TabRoutesTest 통과 · 스모크 ready)"
  artifacts:
    - path: mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt
      provides: "D-27b 캡슐 C 근사 탭바 · 페이드"
      contains: "setAlphaComponent(p.glass, 240)"
    - path: mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt
      provides: "glass 필드(다크 0xFF2C2C35 · 라이트 0xFFFFFFFF)"
      contains: "glass"
    - path: mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
      provides: "탭바 60dp · 페이드 86dp · 다크 자리값"
      contains: "dp(60f)"
  key_links:
    - from: mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
      to: webapp/src/styles/globals.css
      via: "네이티브 탭바 60dp ↔ 웹 --native-tabbar-offset(gap + 60 + 8) · 본문 98(21-20)"
      pattern: "dp\\(60f\\)"
    - from: mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt
      to: mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt
      via: "알약 색 = setAlphaComponent(p.glass, 240)"
      pattern: "p\\.glass"
  prohibitions:
    - "MUST NOT 시각 라벨을 지우면서 탭의 접근 이름(contentDescription)까지 지운다"
    - "MUST NOT 인셋 리스너를 탭바 컨테이너 밖(DecorView · 루트 · WebView)에 건다 — SystemBars 의 CSS 안전영역 주입을 덮는다(Pitfall 8)"
    - "MUST NOT 블러 없는 Android 에서 유리 면을 스케치 알파(62~72%)대로 비치게 해 뒤 글자가 아이콘과 겹쳐 읽히게 한다"
    - "MUST NOT 바닥 규칙 max(inset−14, 14) · 좌우 16dp · 폭 560dp 를 바꾼다"
---

<objective>
G-21-1 (Android): 탭바 라벨을 없애고 스케치 007 **C · 캡슐 인디케이터 · 높이 60** 을 Android 에 옮긴다(21-UAT.md G-21-1 사용자 결정 2026-09-26 · CONTEXT D-27b 는 21-20 이 기록). Android 에는 뒤 콘텐츠 실시간 블러의 표준 수단이 없으므로 유리 면은 기존 A10 선례대로 **glass RGB 94% 불투명**으로 근사하고, 다크 기본(D-23a)에서 아이콘 대비를 계산으로 확인한다.

Purpose: iOS(21-20)와 같은 라벨 없는 캡슐 탭바를 Android 에서도 내고, 웹 본문 여백 98(21-20)과 맞는 60dp 기하로 주문 CTA 가 가리지 않게 한다.
Output: Kotlin 3파일(탭바 · 팔레트 · Activity 기하/자리값).

**D-27b Android 매핑:** 높이 60dp · 알약 radius 30dp · 테두리 없음 · 알약 색 = glass(다크 `0xFF2C2C35` · 라이트 `0xFFFFFFFF`)에 알파 240(≈94% — 기존 A10 근사값과 같은 불투명도, 블러 없이 뒤 글자가 겹쳐 보이지 않게) · 그림자 = `elevation` 12dp(스케치 `0 4px 24px .14` 의 넓고 옅은 그림자 근사 — Android 그림자 알파는 테마 spotShadowAlpha 와 곱해지므로 색 알파로 흉내 내지 않고 높이로만 조절) · 캡슐 56×36dp radius 18dp · primary 알파 41(≈16%) · 아이콘 26dp 정중앙 · 페이드 86dp · bg 알파 204(80%) @ 0.75. 색 토큰(bg · primary · muted · card)은 D-27a 그대로.

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add · 커밋 직전 `git status -sb` · **push 금지**(21-24 체크포인트).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-UAT.md
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-12-SUMMARY.md
@.planning/sketches/007-tab-bar-no-label/README.md
@.planning/sketches/007-tab-bar-no-label/index.html
@mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt
@mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt
@mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt

<interfaces>
```text
GhTradeTabBar(context) : FrameLayout (21-12) — background = pillBackground(GradientDrawable) · elevation · outlineProvider BACKGROUND
  onMeasure 폭 ≤ 560dp · setActive(tab: TabId?) · apply(p: GhTradePalette) · fadeView(View, PaintDrawable 그라데이션)
  private class TabItem(context, tab) : FrameLayout — highlight(View, 원형 GradientDrawable) · icon(ImageView 26dp) · 텍스트 라벨 · contentDescription = tab.title
    configure(active, p) · setPressed(pressed) 로 아이콘·라벨 알파
GhTradePalette(card, bg, primary, muted, line) · of(theme): "dark" 만 다크
MainActivity.setupTabBar (259~308행): rootLayout.addView(fade, 높이 120dp) · addView(tabBar, 높이 70dp · 좌우 16 · 바닥 14) · 인셋 리스너(탭바만) · IME 숨김
  var currentTheme 초기 자리값(56행) · applyTheme(theme) — 시스템 바 · 배경 · 스피너 · tabBar.apply · 저장
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 라벨 없는 60dp 탭바(아이콘·캡슐 정중앙 · radius 30) + 페이드 86dp 기하</name>
  <files>mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt, mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt</files>
  <read_first>
    - mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt (전체 — 116~188행 TabItem)
    - mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt (259~308행 setupTabBar · 56행 자리값)
    - .planning/sketches/007-tab-bar-no-label/index.html (`.vc .tb` · `.vc .tb .it .hl` · `.vc .fade` · `--tbh` 60)
    - mobile/ios/App/App/GHTradeTabBar.swift (21-20 이 먼저 끝났다면 같은 기하의 iOS 판 — 없어도 이 플랜 상수로 충분)
  </read_first>
  <action>
    ① `GhTradeTabBar.kt` `TabItem`: 탭 이름 텍스트 라벨 뷰를 통째로 지운다(필드 · 생성 · addView · 색 지정 · 눌림 알파 · 관련 import). `contentDescription = tab.title` · `isClickable` · `isFocusable` · `isSelected` 갱신은 그대로(접근 이름 유지). 아이콘을 `LayoutParams(dp(26f), dp(26f), Gravity.CENTER)` 로 셀 정중앙에(위 여백 없음). 강조 뷰를 캡슐로: 모양은 사각형 `GradientDrawable` 에 `cornerRadius = dpF(18f)`, 크기 `LayoutParams(dp(56f), dp(36f), Gravity.CENTER)`(위 여백 없음). 눌림은 아이콘 알파만 0.55. 클래스·아이템 KDoc 을 D-27b 로(라벨 없음 · 캡슐 56×36 · 높이 60).
    ② 본체 알약 모서리 반경을 `dpF(30f)` 로.
    ③ `MainActivity.kt` setupTabBar: 페이드 높이를 `dp(86f)`, 탭바 높이를 `dp(60f)` 로. 좌우 16 · 바닥 14 초기값 · 인셋 리스너(탭바에만, max(nav − 14, 14)) · IME 숨김 · 초기 GONE 은 그대로. 주석의 D-27a 를 D-27b 로.
    ④ 손보는 표면(탭바) 안의 아이콘 치우침·잘림·겹침은 바로 고치고 SUMMARY 에 한 줄 보고.
    ⑤ 확인: `cap sync android` 후 `native:test:android`(경로표·링크표 회귀) · `native:build:android` · (dev 서버 :3100 이 없으면 `PORT=3100 pnpm --filter @gh-radar/webapp run dev`) `native:smoke:android`.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile exec cap sync android && pnpm --filter @gh-radar/mobile run native:test:android</automated>
    <fails_when>non-zero exit, or output lacks "BUILD SUCCESSFUL", or contains "FAILED"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:android</automated>
    <fails_when>non-zero exit, or output lacks "BUILD SUCCESSFUL", or contains "Unresolved reference"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:smoke:android</automated>
    <fails_when>non-zero exit, or output lacks "SMOKE OK ready platform=android", or contains "SMOKE FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - 라벨 뷰 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt | grep -c 'TextView')" -eq 0`
    - 위 여백 배치 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt | grep -c 'topMargin')" -eq 0`
    - 원형 강조 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt | grep -c 'OVAL')" -eq 0`
    - 옛 반경 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt | grep -c 'dpF(32f)')" -eq 0`
    - 접근 이름 유지: `grep -c "contentDescription = tab.title" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt` ≥ 1
    - 캡슐·정중앙: `grep -c "dp(56f)" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt` ≥ 1 · `grep -c "dp(36f)" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt` ≥ 1 · `grep -c "dpF(18f)" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt` ≥ 1 · `grep -c "Gravity.CENTER)" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt` ≥ 1 · `grep -c "dpF(30f)" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt` ≥ 1
    - Activity 기하: `grep -c "dp(60f)" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` ≥ 1 · `grep -c "dp(86f)" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` ≥ 1
    - 옛 탭바 높이 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt | grep -c 'dp(70f)')" -eq 0`
    - 옛 페이드 높이 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt | grep -c 'dp(120f)')" -eq 0`
    - 인셋 리스너는 여전히 탭바에만: `grep -c "setOnApplyWindowInsetsListener(tabBar)" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` ≥ 1
    - verify 세 명령 통과
  </acceptance_criteria>
  <done>Android 탭바가 라벨 없는 60dp 높이로 서고 아이콘·캡슐이 정중앙에 있으며, 경로·숨김·이동 동작은 회귀 없이 스모크를 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: Android 캡슐 C 시각 — 테두리 제거 · glass 94% 유리 근사 · elevation 12 · 캡슐 16% · 페이드 80% · 다크 자리값 · 대비 계산</name>
  <files>mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt, mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt, mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt</files>
  <read_first>
    - mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt (Task 1 결과 — apply · fadeDrawable · configure)
    - mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt (필드 · 주석)
    - mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt (56행 자리값 · applyTheme 이 p.card 를 스피너에 쓰는 곳)
    - .planning/sketches/007-tab-bar-no-label/index.html (`.t-dark`/`.t-light` 의 `--glass-hi`)
    - mobile/ios/App/App/GHTradeTheme.swift (21-20 이 먼저 끝났다면 같은 glass 이름·값)
  </read_first>
  <action>
    ① `GhTradePalette.kt`: 알약 테두리용 `line` 필드를 없애고 `glass: Int`(DARK `0xFF2C2C35` · LIGHT `0xFFFFFFFF`)를 더한다. `card`(스피너 배경에 계속 쓰임) · `bg` · `primary` · `muted` 는 그대로. KDoc 정본을 「D-27a 색 토큰 + D-27b 유리(glass RGB — iOS 와 같은 값 · Android 는 실블러가 없어 알파 240 근사, A10)」로.
    ② `GhTradeTabBar.kt` 본체 `apply`: 알약 색 = `ColorUtils.setAlphaComponent(p.glass, 240)`. 알약의 1px 외곽선 지정을 지운다(테두리 없음). `elevation = dpF(12f)`(넓고 옅은 그림자 근사, outlineProvider BACKGROUND 유지). 캡슐 강조 색 = `ColorUtils.setAlphaComponent(p.primary, 41)`(≈16%). 페이드: 위치 `0f, 0.75f, 1f` · bg 알파 204(80%)(같은 색 알파 보간 유지) · 주석 「탭바 위 86dp · bg 80% (D-27b)」. 초기 팔레트 자리값을 `GhTradePalette.of("dark")` 로(D-23a).
    ③ `MainActivity.kt`: `currentTheme` 초기 자리값을 `"dark"` 로(D-23a — 주석 「저장값 없는 기본과 같게, load() 가 곧 ThemeStore 값으로 덮는다」).
    ④ 대비 계산(SUMMARY 에): WCAG 상대 휘도 공식 node 한 줄 — 유리 면 = glass 를 240/255 로 bg 위에 합성, 캡슐 면 = primary 41/255 를 유리 면 위에 합성. 다크·라이트 각각 (비활성 muted vs 유리 면) · (활성 primary vs 캡슐 면) 대비비 4개. 모두 3.0 이상(WCAG 1.4.11 비텍스트)이어야 하고 미달이면 캡슐 알파를 16% 에 가깝게 유지하는 범위에서 조정 후 재계산 · 이유 기록.
    ⑤ 스모크 스크린샷(`$TMPDIR/gh-trade-android-smoke.png`)을 열어 로그인 화면에서 탭바가 숨는지(D-12) 확인한다 — 탭바 모양 자체의 사람 확인은 21-24 UAT 항목 1(로그인 뒤 화면).
    ⑥ 확인: `native:build:android` · `native:smoke:android`.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:android</automated>
    <fails_when>non-zero exit, or output lacks "BUILD SUCCESSFUL", or contains "Unresolved reference"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:smoke:android</automated>
    <fails_when>non-zero exit, or output lacks "SMOKE OK ready platform=android", or contains "SMOKE FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - 유리 근사: `grep -c "setAlphaComponent(p.glass, 240)" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt` ≥ 1
    - 테두리 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt | grep -c 'setStroke')" -eq 0`
    - 그림자·캡슐: `grep -c "dpF(12f)" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt` ≥ 1 · `grep -c "setAlphaComponent(p.primary, 41)" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt` ≥ 1
    - 옛 강조 14% 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt | grep -c 'setAlphaComponent(p.primary, 36)')" -eq 0`
    - 페이드 80%: `grep -c "0.75f" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt` ≥ 1 · `grep -c "setAlphaComponent(bg, 204)" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt` ≥ 1
    - 옛 페이드 92% 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt | grep -c 'setAlphaComponent(bg, 235)')" -eq 0`
    - 다크 자리값(탭바): `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt | grep -c 'GhTradePalette.of("light")')" -eq 0`
    - 다크 자리값(Activity): `grep -c 'currentTheme = "dark"' mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` ≥ 1
    - 팔레트: `grep -c "0xFF2C2C35" mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt` ≥ 1 · `grep -c "val glass" mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt` ≥ 1
    - 팔레트 line 필드 제거: `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt | grep -c 'val line')" -eq 0`
    - SUMMARY 에 다크·라이트 대비비 4개(비활성·활성 × 2)가 적혀 있고 모두 ≥ 3.0
    - verify 두 명령 통과
  </acceptance_criteria>
  <done>Android 탭바가 스케치 007 C 의 테두리 없는 캡슐 모양을 블러 없는 불투명 유리로 근사하고, 다크 기본에서 아이콘 대비가 기준을 넘는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 네이티브 탭바 ↔ 웹 하단 고정 요소 | 60dp 기하가 웹 offset·본문 98(21-20)과 어긋나면 주문 CTA 가 가린다 |
| TalkBack ↔ 탭바 | 시각 라벨 제거 뒤에도 탭 이름이 접근성 트리에 남아야 한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-30 | Denial of Service | 탭바가 종목상세 주문 CTA · /trading 하단 패널을 가림 | high | mitigate | 60dp ↔ 웹 gap+60+8 · 98(21-20 e2e 가 수치 잠금) · 바닥 규칙·인셋 리스너 불변(Task 1 acceptance) · 21-24 UAT 항목 1(Android 제스처/3버튼) |
| T-21-51 | Denial of Service | 라벨 제거로 TalkBack 이 탭을 구분 못 함 | medium | mitigate | contentDescription = tab.title 유지 grep · isSelected 유지(Task 1) · 21-24 UAT 스크린리더 확인 |
| T-21-39 | Tampering | 인셋 리스너 범위(SystemBars CSS 주입 보존) | medium | mitigate | 리스너는 탭바에만 — setOnApplyWindowInsetsListener(tabBar) 유지 grep(Task 1) |
</threat_model>

<verification>
- `native:test:android` · `native:build:android` · `native:smoke:android` 통과 · 대비 계산 기록
- 사람 확인(라벨 없음 · 캡슐 · 불투명 유리 근사 수용 · 다크 대비 · CTA 비가림 · 제스처/3버튼 바닥)은 21-24 UAT 항목 1
</verification>

<success_criteria>
- G-21-1 Android 측이 D-27b 상수(블러 근사 포함)로 맞춰졌고 기존 탭바 동작은 회귀하지 않았다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| Android `ExternalLinks.opensInAppBrowser(scheme, host, appHost)` · `ExternalLinksTest`(정본 26케이스) | Kotlin 순수 함수 · JUnit | 21-17 |
| Android `InAppBrowser.open(activity, uri, theme)` · `GhTradeWebViewClient.shouldOverrideUrlLoading` 가로채기 | Kotlin | 21-17 |
| `mobile/android/variables.gradle` `androidxBrowserVersion` · `app/build.gradle` androidx.browser 선언 (21-17 Task 1 선택 a 일 때만) | Gradle | 21-17 |
| `mobile/package.json` `native:test:android`(TabRoutesTest + ExternalLinksTest) · `native:check-external-links:ios` | 스크립트 | 21-17 · 21-22 |
| 웹 `ThemeProvider` 기본 다크 · iOS/Android `ThemeStore` 기본 다크 · `mobile/www/index.html` 기본 다크 | 설정 | 21-18 |
| e2e `webapp/e2e/specs/theme-default.spec.ts` (저장값 없음 다크 · 저장값 유지 · 앱 첫 theme 메시지 · 오프라인 폴백) | Playwright | 21-18 |
| 웹 `THEME_SWITCH_LABEL` · `ThemeSwitchIcon` (theme-toggle.tsx export) — 사이드바 ThemeToggle · AccountCard 공용 목적지 규칙 | 컴포넌트·상수 | 21-19 |
| 단위 `webapp/src/components/layout/__tests__/theme-toggle.test.tsx` | Vitest | 21-19 |
| iOS `GHTradeTabBar` 캡슐 C(라벨 없음 · 56×36 16% · 유리 · 두 겹 그림자 · 높이 60 · 페이드 86) · `GHTradePalette.glass` · `glassAlpha` | Swift | 21-20 |
| CSS `--native-tabbar-offset` = gap + 60 + 8 · `--native-body-reserve` 98 · native-shell e2e 수치(82 · 98 · 74) | 토큰 · Playwright | 21-20 |
| CONTEXT D-27b · D-23a · D-08b · D-28 | 결정 기록 | 21-20 |
| Android `GhTradeTabBar` 캡슐 C 근사(라벨 없음 · 불투명 유리 · elevation 12) · `GhTradePalette.glass` · 탭바 60dp · 페이드 86dp | Kotlin | 21-21 |
| iOS `ExternalLinks.opensInAppBrowser(scheme:host:appHost:)` · `isSameAppHost` · `NavigationDelegateProxy`(decidePolicyFor · WKUIDelegate createWebViewWith · SFSafariViewController) · `mobile/scripts/external-links-check.swift` · `check-external-links-ios.sh` | Swift · 스크립트 | 21-22 |
| 갭 클로징 전 게이트 기록 · push 전 UAT 환경 (21-23-SUMMARY) · UAT 재검증 결과 · push 결정과 이행 (21-24-SUMMARY) | 문서 | 21-23 · 21-24 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-21-SUMMARY.md` when done — D-27b 상수 대응표(스케치 CSS → Android) · 대비비 계산 · 테스트/빌드/스모크 결과를 적는다.
</output>
