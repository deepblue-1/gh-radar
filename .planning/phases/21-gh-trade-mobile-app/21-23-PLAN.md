---
phase: 21-gh-trade-mobile-app
plan: 23
type: execute
wave: 10
depends_on: ["21-17", "21-18", "21-19", "21-20", "21-21", "21-22"]
files_modified: []
autonomous: true
gap_closure: true
requirements: [MOBILE-01]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "갭 네 건(G-21-1 · G-21-N1 · G-21-N2 · G-21-N3)을 닫은 21-17~21-22 뒤 전 자동 게이트가 green — build_command · test_command · 루트 typecheck · Playwright 전체(알려진 범위 밖 3건만 제외) · iOS 경로표·링크표·빌드·스모크 · Android JUnit·빌드·스모크 · verify-prod(운영 설정) · 비밀 파일 미추적"
    - "push 전 UAT 환경이 준비돼 있다 — dev 웹 http://localhost:3100 · UAT 프록시 127.0.0.1:8080(운영 API 읽기) · 저장값 없는 dev 빌드 앱이 iPhone 17 · iPad Pro 11-inch (M5) · Android 에뮬레이터에 새로 설치돼 실행 중"
    - "UAT 준비는 추적 파일을 바꾸지 않는다(프록시는 scratchpad · 생성 설정은 gitignore) · relay(:8090)는 띄우지 않는다"
  artifacts:
    - path: .planning/phases/21-gh-trade-mobile-app/21-23-SUMMARY.md
      provides: "게이트 표 · UAT 환경 표(UDID · 설치 URL · 프록시 PID · dev 서버 기동 주체 · 21-17 선택)"
  key_links:
    - from: .planning/phases/21-gh-trade-mobile-app/21-23-SUMMARY.md
      to: .planning/phases/21-gh-trade-mobile-app/21-24-PLAN.md
      via: "UAT 환경 표를 21-24 체크포인트와 마무리가 그대로 쓴다(UDID · 프록시 PID · dev 서버 종료 책임)"
      pattern: "UAT 환경"
  prohibitions:
    - "MUST NOT UAT 중 relay(:8090)를 띄워 실계좌 주문 경로를 연다"
    - "MUST NOT env 파일(.env*)을 읽어 운영 API 주소를 얻는다 — gcloud 로 조회한다"
    - "MUST NOT UAT 프록시·dev 설정을 커밋한다"
    - "MUST NOT push 한다(push 여부는 21-24 체크포인트에서 사용자가 정한다)"
---

<objective>
Phase 21 갭 클로징 마감 1/2: 21-17~21-22 가 닫은 갭 네 건(G-21-1 탭바 · G-21-N1 기본 다크 · G-21-N2 테마 아이콘 · G-21-N3 인앱 브라우저)을 전 자동 게이트로 확인하고, **push 전** 사용자 UAT 환경(dev 웹 + UAT 프록시 + 저장값 없는 dev 빌드 앱 세 기기)을 준비한다. UAT 와 push 결정은 21-24 체크포인트가 받는다.

Purpose: 이 저장소에서 `git push` 는 곧 웹 프로덕션 배포다(STATE 교훈). 웹 여백 98 은 새 네이티브 탭바 60 과 같이 나가야 하고, 기본 다크·아이콘 규칙은 push 즉시 모든 브라우저 사용자에게 적용된다 — 그래서 자동 게이트 다음에 사람 눈으로 먼저 본다. 게이트(운영 sync 로 빌드·스모크)와 UAT 준비(dev sync 설치)는 생성 설정을 서로 다르게 굽기 때문에 태스크를 나눈다(게이트 검증이 UAT 설치본을 덮지 않게).
Output: 게이트 표 · UAT 환경 표(21-23-SUMMARY). 코드 변경 없음(게이트 실패 수정이 생기면 해당 갭 범위 안에서만, Deviations 로 기록).

검증 전용 플랜이라 새 기능 슬라이스(트레이서)가 없다 — 구현 플랜 21-17~21-22 가 각각 트레이서로 시작했다.
UAT 를 dev URL 로 하는 이유: push 결정을 UAT 뒤에 두었다(갭 플랜 지시). dev 에서 볼 수 없는 항목은 21-24 의 「push 후 재확인」 선택지로 실서버에서 마저 본다(21-16 UAT 2차와 같은 경로).

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add(절대 `git add -A` 금지 — 작업 트리에 다른 세션 변경이 있다) · 커밋 직전 `git status -sb` · **push 금지**.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-UAT.md
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-16-SUMMARY.md
@.planning/phases/21-gh-trade-mobile-app/21-17-SUMMARY.md
@.planning/phases/21-gh-trade-mobile-app/deferred-items.md
@.planning/sketches/007-tab-bar-no-label/README.md
@mobile/package.json
@mobile/README.md
@mobile/scripts/smoke-ios.sh
@mobile/scripts/smoke-android.sh

<interfaces>
```text
프로젝트 게이트(config.json):
  build_command = pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/webapp run typecheck
  test_command  = pnpm --filter @gh-radar/relay run test && pnpm --filter @gh-radar/webapp run test
알려진 범위 밖 e2e 3건(deferred-items.md · phase 21 이전부터): trading-workbench.spec.ts 「5. 격자 1/2/3단 …」 · 「P20-3 최악값 …」 · 「터치 기기 · iPhone 가로 폭 844 에서 종목 추가 입력이 16px 다」
mobile scripts: native:sync · native:sync:dev · native:build:ios · native:build:android · native:smoke:ios · native:smoke:android ·
  native:check-tab-routes:ios · native:check-external-links:ios(21-22) · native:test:android(TabRoutesTest + ExternalLinksTest, 21-17) · native:verify-prod
native:build:* 는 내부에서 cap sync 를 먼저 돈다 — CAP_SERVER_URL 이 환경에 있으면 dev URL 로 구워진다(capacitor.config.ts)
앱 번들/ APK 경로 = smoke-ios.sh 의 APP_PATH · smoke-android.sh 의 APK (빌드 산출물 — 커밋 대상 아님)
UAT 기기(21-16): iPhone 17 · iPad Pro 11-inch (M5) (iOS 27.0 런타임) · Android emulator-5554 (Medium_Phone_API_36.1)
운영 API: Cloud Run 서비스 gh-radar-server (asia-northeast3) · 헬스 GET /api/health · 배포 SA 는 GOOGLE_APPLICATION_CREDENTIALS + CLOUDSDK_CORE_PROJECT=gh-radar
Vercel: push → scripts/vercel-ignore-build.sh 가 직전 배포~HEAD 의 webapp 변경을 보고 빌드 · 운영 도메인 https://trade.jx1.io
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 갭 클로징 전 자동 게이트(운영 설정 기준)</name>
  <files>(코드 변경 없음 — 게이트 실패 수정은 해당 갭 범위 안에서만 Deviations 로)</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-16-SUMMARY.md (게이트 표 선례 · 시뮬레이터 런타임 두 개 · SwiftPM 키체인 우회)
    - .planning/phases/21-gh-trade-mobile-app/deferred-items.md (제외할 e2e 3건의 정확한 제목)
    - mobile/package.json (native:* 스크립트 — 21-17 · 21-22 가 넓힌 native:test:android · native:check-external-links:ios)
  </read_first>
  <action>
    ① 순서대로 돌리고 각 결과 요약(통과 수 등 실제 출력)을 SUMMARY 게이트 표에 적는다: build_command → test_command → 루트 `pnpm typecheck` → Playwright 전체(알려진 3건만 `--grep-invert` 로 제외) → iOS `native:check-tab-routes:ios` · `native:check-external-links:ios` · `native:build:ios` · `native:smoke:ios` → Android `cap sync android` · `native:test:android` · `native:build:android` · `native:smoke:android` → `native:verify-prod` · 비밀 파일 미추적.
    ② 스모크 전 webapp dev 서버가 `http://localhost:3100` 에 없으면 `nohup` 으로 `PORT=3100 pnpm --filter @gh-radar/webapp run dev` 를 띄우고(dev.sh 금지 — 다른 세션 포트를 죽인다) 「이 플랜이 띄움」과 PID 를 SUMMARY 에 적는다(21-24 가 종료 책임을 판단).
    ③ 실패하면 원인이 이번 갭 범위(21-17~21-22 가 바꾼 파일)면 고쳐 커밋하고 ①을 처음부터 다시, 범위 밖이면 SUMMARY 에 기록하고 멈춰 보고한다. 스모크 두 개가 끝나면 생성 설정은 운영 sync 로 복원돼 있다(verify-prod 가 증명).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/webapp run typecheck && pnpm typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay run test && pnpm --filter @gh-radar/webapp run test</automated>
    <fails_when>non-zero exit, or a vitest summary line contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test --grep-invert "격자 1/2/3단|P20-3 최악값|종목 추가 입력이 16px"</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or no summary line matches "[0-9]+ passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:check-tab-routes:ios && pnpm --filter @gh-radar/mobile run native:check-external-links:ios && pnpm --filter @gh-radar/mobile run native:build:ios && pnpm --filter @gh-radar/mobile run native:smoke:ios</automated>
    <fails_when>non-zero exit, or output lacks "TAB ROUTES OK", "EXTERNAL LINKS OK", "BUILD SUCCEEDED" or "SMOKE OK ready platform=ios", or contains "FAIL"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile exec cap sync android && pnpm --filter @gh-radar/mobile run native:test:android && pnpm --filter @gh-radar/mobile run native:build:android && pnpm --filter @gh-radar/mobile run native:smoke:android</automated>
    <fails_when>non-zero exit, or output lacks "BUILD SUCCESSFUL" or "SMOKE OK ready platform=android", or contains "FAILED"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:verify-prod && test -z "$(git ls-files -- 'mobile/*.jks' 'mobile/*.keystore' 'mobile/*.p12' 'mobile/*.mobileprovision')"</automated>
    <fails_when>non-zero exit, or output lacks "PROD CONFIG OK", or contains "FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - 여섯 게이트 명령이 모두 통과하고 SUMMARY 게이트 표에 각 결과 요약이 있다
    - Playwright 에서 제외한 것은 deferred-items.md 의 3건뿐이다(SUMMARY 에 제외 제목 명시)
    - `ST="$(git status -sb)" && printf '%s\n' "$ST"` 에 이 태스크가 만든 추적 파일 변경이 없다(게이트 수정 커밋이 있었다면 Deviations 에 기록)
  </acceptance_criteria>
  <done>갭 클로징 뒤 전 자동 게이트가 green 이고 생성 설정은 운영값이다.</done>
</task>

<task type="auto">
  <name>Task 2: push 전 UAT 환경 준비 — UAT 프록시 · dev 빌드 세 기기 새 설치(저장값 없음)</name>
  <files>(코드 변경 없음 — 프록시는 scratchpad, 생성 설정은 gitignore)</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-16-SUMMARY.md (UAT 1차 프록시 Deviation 2 · 실서버 UAT 준비 절차 · DeviceHub)
    - mobile/README.md (dev 절차 · adb reverse)
    - mobile/scripts/smoke-ios.sh · mobile/scripts/smoke-android.sh (앱 번들 APP_PATH / APK 경로)
    - .planning/phases/21-gh-trade-mobile-app/21-17-SUMMARY.md (Android 인앱 브라우저 선택 a/b/c)
  </read_first>
  <action>
    ① UAT 프록시(커밋하지 않는 scratchpad 스크립트 · 21-16 Deviation 2 와 같은 방식): 운영 API 주소는 `gcloud run services describe gh-radar-server --region asia-northeast3 --format 'value(status.url)'`(배포 SA 환경변수)로 얻는다 — env 파일을 읽지 않는다. `127.0.0.1:8080` 에서 받아 그 주소로 넘기며 요청 Origin 을 `https://trade.jx1.io` 로 바꾸고, 응답 ACAO 는 원래 요청 출처로 되돌린다. 로그는 메서드·경로·상태만. `nohup` 으로 띄워 실행기가 끝나도 살아 있게 하고 PID 를 적는다. relay(:8090)는 띄우지 않는다(UAT 중 실주문 경로 차단).
    ② dev 빌드: `CAP_SERVER_URL=http://localhost:3100` 을 붙여 `pnpm --filter @gh-radar/mobile run native:build:ios` · `native:build:android` 를 돌린다(내부 cap sync 가 dev URL 을 굽는다).
    ③ 새 설치(저장된 테마·로그인 없음 = N1 첫 실행 조건): iOS 27.0 런타임의 `iPhone 17` · `iPad Pro 11-inch (M5)` UDID 를 `xcrun simctl list devices available` 에서 찾아 둘 다 부팅 → 각각 `xcrun simctl uninstall <UDID> com.ghtrade.app` → smoke-ios.sh 의 APP_PATH 번들 `install` → `launch`. Android: 에뮬레이터 확보(smoke-android.sh 와 같은 AVD) → `adb uninstall com.ghtrade.app` → smoke-android.sh 의 APK `adb install` → `adb reverse tcp:3100 tcp:3100` · `adb reverse tcp:8080 tcp:8080` → `am start -n com.ghtrade.app/.MainActivity`. 시뮬레이터 창은 Xcode 27 의 DeviceHub 앱으로 연다.
    ④ SUMMARY 「UAT 환경」 표: 기기별 UDID/시리얼 · 각 iOS UDID 의 `xcrun simctl get_app_container <UDID> com.ghtrade.app` 성공 여부 · 설치 URL(dev) · 프록시 PID · dev 서버 기동 주체(Task 1 ②) · 21-17 선택(a/b/c).
  </action>
  <verify>
    <automated>curl -sf -o /dev/null http://localhost:3100/login && curl -sf -o /dev/null http://127.0.0.1:8080/api/health && echo "UAT ENDPOINTS OK"</automated>
    <fails_when>non-zero exit (dev server or proxy not answering), or "UAT ENDPOINTS OK" is absent</fails_when>
    <automated>PKGS="$(adb shell pm list packages com.ghtrade.app)" && printf '%s\n' "$PKGS" | grep -c "package:com.ghtrade.app"</automated>
    <fails_when>non-zero exit, or the count prints 0 (Android dev build not installed)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && OUT="$(pnpm --filter @gh-radar/mobile run native:verify-prod 2>&1 || true)" && printf '%s\n' "$OUT" | grep -c "FAIL"</automated>
    <fails_when>the count prints 0 — the generated config is still the production URL, so the UAT devices did not get the dev build</fails_when>
  </verify>
  <acceptance_criteria>
    - UAT 환경 표가 SUMMARY 에 있고 두 iOS UDID 모두 get_app_container 성공으로 적혀 있다
    - relay 를 띄우지 않았다: `lsof -iTCP:8090 -sTCP:LISTEN` 출력 0줄(다른 세션이 띄운 경우는 그 사실을 SUMMARY 에 적는다)
    - `ST="$(git status -sb)" && printf '%s\n' "$ST"` 에 이 태스크가 만든 추적 파일 변경이 없다
  </acceptance_criteria>
  <done>세 기기에 저장값 없는 dev 빌드 앱이 떠 있고 dev 웹·UAT 프록시가 응답한다 — 21-24 체크포인트가 바로 UAT 를 받을 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 빌드 설정 → 기기 설치본 | dev URL · cleartext 가 생성 설정에 남을 수 있다(21-24 가 복원) |
| UAT 프록시 → 운영 API | 로컬에서 운영 데이터를 읽는 임시 경로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Information Disclosure | dev 설정 운영 유출(Pitfall 15) | high | mitigate | 게이트 끝에 verify-prod(Task 1) · UAT 뒤 복원과 재검사는 21-24 Task 2 |
| T-21-14 | Information Disclosure | 키스토어·프로비저닝 파일 커밋 | high | mitigate | git ls-files 비밀 파일 0건(Task 1 verify) |
| T-21-55 | Information Disclosure | UAT 프록시 로그·노출 | medium | mitigate | 127.0.0.1 바인딩 · 로그는 메서드·경로·상태만 · 운영 주소는 gcloud 조회(env 파일 미열람) · 커밋하지 않음 · 종료는 21-24 |
| T-21-56 | Elevation of Privilege | UAT 중 실주문 경로 | medium | mitigate | relay(:8090) 미기동 확인(Task 2 acceptance) |
</threat_model>

<verification>
- Task 1 게이트 green(운영 설정 기준) · Task 2 UAT 환경 응답 · 추적 파일 무변경
</verification>

<success_criteria>
- 갭 네 건이 자동 게이트로 확인됐고, 21-24 가 곧바로 사용자 UAT 와 push 결정을 받을 수 있다.
</success_criteria>

## Flagged assumptions

- **엣지 탐침 MOBILE-01 (unclassified · unresolved):** 엣지 탐침이 MOBILE-01(여러 표면을 가진 셸 요구사항)을 분류하지 못했다. 이번 갭 범위의 경계는 각 플랜이 명시 테스트로 다룬다 — 테마 저장값 유무·모르는 값(21-18 e2e) · URL 스킴/호스트 26케이스(21-17 · 21-22) · 탭바 기하 수치(21-20 e2e) · 대비비 계산(21-20 · 21-21). 분류되지 않은 나머지 경계는 21-24 UAT 에서 사람이 검토한다.
- UAT 프록시는 21-16 과 같은 커밋하지 않는 임시 경로다 — 운영 Cloud Run 서비스 이름·리전이 바뀌었다면 `gcloud run services list` 로 찾는다(사용자에게 묻지 않는다).
- 실기기에 옛 빌드(탭바 70)가 설치돼 있다면 push 뒤 사용자가 재설치해야 여백이 맞는다(21-24 체크포인트에서 고지).

## Source coverage (갭 클로징 범위)

| SOURCE | ID | 항목 | Plan | Status |
|---|---|---|---|---|
| UAT | G-21-1 | 라벨 제거 · 스케치 007 C · 높이 60 · 웹 98 · e2e · D-27b | 21-20 · 21-21 · 21-23 · 21-24 | COVERED |
| UAT | G-21-13 | 웹 회귀 — 사용자 보고로 resolved | — | 제외(플랜 불필요) |
| UAT | G-21-N1 | 기본 다크 웹·앱 공통 · 저장값 유지 · 첫 프레임 동기 | 21-18 · 21-20(자리값·D-23a) · 21-21(자리값) · 21-24 | COVERED |
| UAT | G-21-N2 | 목적지 아이콘 · 행동 문구 aria-label · 테스트 | 21-19 · 21-20(D-08b) · 21-24 | COVERED |
| UAT | G-21-N3 | 호스트 밖 http(s) = 인앱 브라우저 · 네이티브 가로채기 · androidx.browser 재확인 게이트 · 예외 호스트 점검 | 21-17 · 21-22 · 21-20(D-28) · 21-24 | COVERED |
| REQ | MOBILE-01 | GH Trade 앱 셸 요구사항 | 21-17 ~ 21-24 | COVERED |
| CONTEXT | D-02 · D-27 · D-27a | 탭바 모양 — 목업 게이트(스케치 007 채택으로 충족) → D-27b | 21-20 · 21-21 | COVERED |
| CONTEXT | D-23 | 테마 동기 — 기본값만 D-23a 로 | 21-18 | COVERED |
| CONTEXT | D-08a | 계정 카드 — 테마 버튼 규칙만 D-08b 로 | 21-19 | COVERED |
| CONTEXT | D-12 · D-19 · D-25 | 숨김 · 오프라인 · safe-area — 기하 변경에 맞춰 회귀 확인 | 21-20 e2e · 21-18 폴백 · 21-24 UAT 5 | COVERED |
| RESEARCH | A10 · Pattern 5 · V14 | Android 블러 근사 · 전달형 프록시(T-21-37) · allowNavigation 비움 | 21-21 · 21-22 · 21-17/21-22/21-23/21-24(verify-prod) | COVERED |

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| Android `ExternalLinks.opensInAppBrowser(scheme, host, appHost)` · `ExternalLinksTest`(정본 26케이스) | Kotlin 순수 함수 · JUnit | 21-17 |
| Android `InAppBrowser.open(activity, uri, theme)` · `GhTradeWebViewClient.shouldOverrideUrlLoading` 가로채기 | Kotlin | 21-17 |
| `mobile/android/variables.gradle` `androidxBrowserVersion` · `app/build.gradle` androidx.browser 선언 (21-17 Task 1 선택 a 일 때만) | Gradle | 21-17 |
| `mobile/package.json` `native:test:android`(TabRoutesTest + ExternalLinksTest) · `native:check-external-links:ios` | 스크립트 | 21-17 · 21-22 |
| 웹 `ThemeProvider` 기본 다크 · iOS/Android `ThemeStore` 기본 다크 · `mobile/www/index.html` 기본 다크 | 설정 | 21-18 |
| e2e `webapp/e2e/specs/theme-default.spec.ts` (저장값 없음 다크 · 저장값 유지 · 앱 첫 theme 메시지 · 오프라인 폴백) | Playwright | 21-18 |
| 웹 `THEME_SWITCH_LABEL` · `ThemeSwitchIcon` (theme-toggle.tsx export) — 사이드바 ThemeToggle · AccountCard 공용 목적지 규칙 | 컴포넌트·상수 | 21-19 |
| 단위 `webapp/src/components/layout/__tests__/theme-toggle.test.tsx` | Vitest | 21-19 |
| iOS `GHTradeTabBar` 캡슐 C(라벨 없음 · 56×36 16% · 유리 · 두 겹 그림자 · 높이 60 · 페이드 86) · `GHTradePalette.glass` · `glassAlpha` | Swift | 21-20 |
| CSS `--native-tabbar-offset` = gap + 60 + 8 · `--native-body-reserve` 98 · native-shell e2e 수치(82 · 98 · 74) | 토큰 · Playwright | 21-20 |
| CONTEXT D-27b · D-23a · D-08b · D-28 | 결정 기록 | 21-20 |
| Android `GhTradeTabBar` 캡슐 C 근사(라벨 없음 · 불투명 유리 · elevation 12) · `GhTradePalette.glass` · 탭바 60dp · 페이드 86dp | Kotlin | 21-21 |
| iOS `ExternalLinks.opensInAppBrowser(scheme:host:appHost:)` · `isSameAppHost` · `NavigationDelegateProxy`(decidePolicyFor · WKUIDelegate createWebViewWith · SFSafariViewController) · `mobile/scripts/external-links-check.swift` · `check-external-links-ios.sh` | Swift · 스크립트 | 21-22 |
| 갭 클로징 전 게이트 기록 · push 전 UAT 환경 (21-23-SUMMARY) · UAT 재검증 결과 · push 결정과 이행 (21-24-SUMMARY) | 문서 | 21-23 · 21-24 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-23-SUMMARY.md` when done — 게이트 표 · UAT 환경 표(21-24 가 이어받는다).
</output>
