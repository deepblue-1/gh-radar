---
phase: 21-gh-trade-mobile-app
plan: 35
type: execute
wave: 16
depends_on: ["21-25", "21-26", "21-27", "21-28", "21-29", "21-30", "21-31", "21-32", "21-33", "21-34"]
files_modified:
  - .planning/phases/21-gh-trade-mobile-app/21-REVIEW-R2.md
autonomous: true
gap_closure: true
requirements: [MOBILE-01]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "UAT 3차 갭 12건(G-21-R3-1 ~ R3-11 · G-21-CR)을 닫은 21-25 ~ 21-34 뒤 전 자동 게이트가 green — 빌드 · 단위 테스트 · 루트 typecheck · 임시 worktree 프로덕션 빌드 · Playwright 전체(알려진 범위 밖 3건만 제외) · iOS 표·빌드·스모크 · Android JUnit·빌드·스모크 · verify-prod · 비밀 파일 미추적"
    - "G-21-CR: 21-REVIEW-R2.md 가 1차 발견 13건(WR-01 ~ WR-05 · IN-01 ~ IN-08) 각각의 처리(플랜 · 커밋 · 증거 명령과 결과) 또는 수정하지 않은 근거를 적는다 — 21-REVIEW.md 는 그대로다"
    - "push 전 UAT 환경이 준비돼 있다 — dev 웹 :3100 · UAT 프록시 127.0.0.1:8080(운영 API 읽기) · 저장값·로그인 없는 dev 빌드 앱이 iPhone 17 · iPad Pro 11-inch (M5) · Android 에뮬레이터에 새로 설치돼 실행 중 · relay(:8090)는 띄우지 않는다"
    - "이번 라운드에 백엔드(relay · server · supabase · workers · packages/shared) 변경이 없다 — 있으면 멈추고 보고한다(배포 순서가 relay 먼저로 바뀐다)"
  artifacts:
    - path: .planning/phases/21-gh-trade-mobile-app/21-REVIEW-R2.md
      provides: "1차 13건 처리 대조 표 · (execute-phase 코드 리뷰가 다시 돌면) 라운드 2 발견"
      contains: "WR-01"
  key_links:
    - from: .planning/phases/21-gh-trade-mobile-app/21-35-SUMMARY.md
      to: .planning/phases/21-gh-trade-mobile-app/21-36-PLAN.md
      via: "UAT 환경 표(UDID · 시리얼 · 프록시 PID · dev 서버 기동 주체) · 실서버 재확인 대상 표를 21-36 체크포인트가 쓴다"
      pattern: "UAT 환경"
    - from: .planning/phases/21-gh-trade-mobile-app/21-REVIEW-R2.md
      to: .planning/phases/21-gh-trade-mobile-app/21-REVIEW.md
      via: "1차 findings ID 13개를 그대로 행으로"
      pattern: "IN-08"
  prohibitions:
    - statement: "MUST NOT 21-REVIEW.md · 21-VERIFICATION.md · 21-UAT.md 의 기존 기록을 덮어쓴다 — 재리뷰·재검증은 -R2 파일로"
      status: unverified
      verification: "Task 2 verify git diff --quiet 세 파일"
    - statement: "MUST NOT UAT 준비 중 relay(:8090)를 띄워 실계좌 주문 경로를 연다"
      status: unverified
      verification: "Task 3 acceptance lsof :8090 LISTEN 0줄"
    - statement: "MUST NOT env 파일을 열어 운영 주소를 얻는다(gcloud 로 조회) · UAT 프록시·dev 설정을 커밋한다"
      status: unverified
      verification: "프록시는 scratchpad · 생성 설정 gitignore · git status 추적 변경 0"
    - statement: "MUST NOT push 한다(push 여부는 21-36 체크포인트)"
      status: unverified
      verification: "이 플랜에 push 명령 없음"
---

<objective>
UAT 3차 갭 클로징 마감 1/2 — 21-23 과 같은 **push 전 자동 게이트 + 리뷰 대조 기록 + UAT 환경 준비**를 한 플랜으로 가볍게 묶는다. UAT 와 push 결정은 21-36 이 받는다.

Purpose: 이 저장소에서 `git push` 는 곧 웹 프로덕션 배포다. 탭 전환 · 종목상세 구조 · 주문 표면 이동이 사용자 전원에게 바로 나가므로 자동 게이트 다음에 사람 눈으로 먼저 본다.
Output: 게이트 표 · 21-REVIEW-R2.md(13건 대조) · UAT 환경 표(21-35-SUMMARY). 코드 변경 없음(게이트 실패 수정이 생기면 해당 갭 범위 안에서만 · Deviations).

라운드 기준 커밋: `aba7954`(플래닝 시점 origin/master = HEAD). 트레이서 없음(검증 전용).

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add(`git add -A` 금지) · 커밋 직전 `git status -sb` · **push 금지**.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-REVIEW.md
@.planning/phases/21-gh-trade-mobile-app/21-23-PLAN.md
@.planning/phases/21-gh-trade-mobile-app/21-23-SUMMARY.md
@.planning/phases/21-gh-trade-mobile-app/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전 자동 게이트(운영 설정 기준) + 임시 worktree 프로덕션 빌드 + 백엔드 무변경 확인</name>
  <files>(코드 변경 없음 — 게이트 실패 수정은 해당 갭 범위 안에서만 Deviations 로)</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-23-SUMMARY.md (게이트 표 선례)
    - .planning/phases/21-gh-trade-mobile-app/deferred-items.md (제외 3건)
    - mobile/package.json · mobile/README.md (21-28 뒤 스모크 환경변수)
  </read_first>
  <action>
    ① 백엔드 무변경: `git log --oneline aba7954..HEAD -- relay/ server/ supabase/ workers/ packages/shared/` 가 비어 있어야 한다 — 비어 있지 않으면(다른 세션 커밋 포함) 멈추고 목록과 함께 보고한다.
    ② 순서대로 돌리고 실제 출력 요약을 SUMMARY 게이트 표에 적는다(verify 의 명령 그대로). 스모크 전 dev 서버(:3100)가 없으면 `nohup` 으로 `PORT=3100 pnpm --filter @gh-radar/webapp run dev` 를 띄우고(dev.sh 금지) 「이 플랜이 띄움」과 PID 를 적는다. 여러 기기면 `ANDROID_SERIAL` · `IOS_DEVICE_UDID` 를 지정한다.
    ③ 실패하면 원인이 이번 라운드(21-25 ~ 21-34) 변경이면 그 갭 범위 안에서 고쳐 커밋하고 ② 를 처음부터, 범위 밖이면 기록하고 멈춰 보고한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && test -z "$(git log --oneline aba7954..HEAD -- relay/ server/ supabase/ workers/ packages/shared/)" && echo "BACKEND UNCHANGED"</automated>
    <fails_when>non-zero exit, or "BACKEND UNCHANGED" is absent (a backend path changed — deploy order must become relay first)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/webapp run typecheck && pnpm typecheck && pnpm --filter @gh-radar/relay run test && pnpm --filter @gh-radar/webapp run test</automated>
    <fails_when>non-zero exit, or output contains "error TS", or a vitest summary line contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && W="$(mktemp -d)/wt" && git worktree add --detach "$W" HEAD >/dev/null 2>&1 && cp webapp/.env*.local "$W/webapp/" 2>/dev/null; ( cd "$W" && pnpm install --frozen-lockfile --prefer-offline >/dev/null 2>&1 && pnpm --filter @gh-radar/shared build >/dev/null && pnpm --filter @gh-radar/webapp run build ); RC=$?; git worktree remove --force "$W"; exit $RC</automated>
    <fails_when>non-zero exit, or output contains "Failed to compile", "Error occurred prerendering" or "Build error"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test --grep-invert "격자 1/2/3단|P20-3 최악값|종목 추가 입력이 16px"</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or no summary line matches "[0-9]+ passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:check-tab-routes:ios && pnpm --filter @gh-radar/mobile run native:check-external-links:ios && pnpm --filter @gh-radar/mobile run native:build:ios && pnpm --filter @gh-radar/mobile run native:smoke:ios</automated>
    <fails_when>non-zero exit, or output lacks "TAB ROUTES OK", "EXTERNAL LINKS OK", "BUILD SUCCEEDED" or "SMOKE OK ready platform=ios", or contains "FAIL"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile exec cap sync android && pnpm --filter @gh-radar/mobile run native:test:android && pnpm --filter @gh-radar/mobile run native:build:android && pnpm --filter @gh-radar/mobile run native:smoke:android</automated>
    <fails_when>non-zero exit, or output lacks "BUILD SUCCESSFUL" or "SMOKE OK ready platform=android", or contains "FAILED" or "SMOKE FAIL"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:verify-prod && test -z "$(git ls-files -- 'mobile/*.jks' 'mobile/*.keystore' 'mobile/*.p12' 'mobile/*.mobileprovision')"</automated>
    <fails_when>non-zero exit, or output lacks "PROD CONFIG OK", or contains "FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY 게이트 표에 verify 일곱 명령의 결과 요약이 있고 Playwright 제외는 deferred-items.md 3건뿐이다
    - `git status -sb` 에 이 태스크가 만든 추적 파일 변경이 없다(게이트 수정 커밋이 있었다면 Deviations 에 기록)
  </acceptance_criteria>
  <done>전 자동 게이트가 green 이고, 생성 설정은 운영값이며, 백엔드 배포가 필요 없다.</done>
</task>

<task type="auto">
  <name>Task 2: 21-REVIEW-R2.md — 1차 13건 처리 대조 표</name>
  <files>.planning/phases/21-gh-trade-mobile-app/21-REVIEW-R2.md</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-REVIEW.md (13건 정본 — 읽기만)
    - 21-25 · 21-27 · 21-28 · 21-29 · 21-34 SUMMARY (각 발견의 처리 · WR-03 답 · IN-03 재현 기록)
    - `git log --oneline aba7954..HEAD`
  </read_first>
  <action>
    새 파일 `21-REVIEW-R2.md`(frontmatter: phase · round 2 · reviewed 시각 · base aba7954 · head SHA · status). **21-REVIEW.md 는 읽기만 한다.** 「1차 발견 처리 대조」 표 — 행 13개(WR-01 ~ WR-05 · IN-01 ~ IN-08), 열 = 처리(수정 / 부분 수정 / 수정 안 함) · 플랜 · 커밋 SHA · 증거(실행한 명령과 결과 한 줄) · 비고. 매핑: WR-01 · IN-07 · WR-04 · WR-05 · IN-06 = 21-27 · WR-03 = 21-28(iOS) + 21-29(Android · 21-25 답 a/b 인용) · WR-02 · IN-01 · IN-03 = 21-29(IN-03 재현/불가 기록 인용) · IN-02 = 21-28 + 21-29 · IN-04 = 21-34 · IN-05 · IN-08 = 21-28. 「수정 안 함」 · 「부분 수정」 행은 근거 한 단락(예: WR-03 답 b 의 남는 위험 · IN-03 재현 불가 근거). 파일 머리에 규칙 한 줄: 「execute-phase 코드 리뷰가 이 라운드에 다시 돌면 그 출력은 이 파일의 『라운드 2 발견』 절(ID 는 R2- 접두)로 합치고 21-REVIEW.md 는 덮어쓰지 않는다」. 이 플랜에서 별도의 전면 재리뷰는 하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && R=.planning/phases/21-gh-trade-mobile-app/21-REVIEW-R2.md && for id in WR-01 WR-02 WR-03 WR-04 WR-05 IN-01 IN-02 IN-03 IN-04 IN-05 IN-06 IN-07 IN-08; do grep -q "| ${id} " "$R" || { echo "MISSING ${id}"; exit 1; }; done && git diff --quiet -- .planning/phases/21-gh-trade-mobile-app/21-REVIEW.md .planning/phases/21-gh-trade-mobile-app/21-VERIFICATION.md .planning/phases/21-gh-trade-mobile-app/21-UAT.md && echo "REVIEW R2 OK"</automated>
    <fails_when>output contains "MISSING", or "REVIEW R2 OK" is absent (a finding row is missing, or a round-1 artifact was modified)</fails_when>
  </verify>
  <acceptance_criteria>
    - 13개 행이 모두 있다(verify 루프) · 「수정 안 함」/「부분 수정」 행마다 근거 단락이 있다
    - `grep -c "R2-" .planning/phases/21-gh-trade-mobile-app/21-REVIEW-R2.md` ≥ 1 (머리 규칙 줄)
    - 21-REVIEW.md · 21-VERIFICATION.md · 21-UAT.md 무수정(verify)
  </acceptance_criteria>
  <done>리뷰 13건 각각의 처리 또는 근거가 새 파일에 남고, 1차 기록은 그대로다.</done>
</task>

<task type="auto">
  <name>Task 3: push 전 UAT 환경 — UAT 프록시 · dev 빌드 세 기기 새 설치(저장값·로그인 없음) · 실서버 재확인 대상 표</name>
  <files>(코드 변경 없음 — 프록시는 scratchpad, 생성 설정은 gitignore)</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-23-PLAN.md (Task 2 절차 — 그대로 따른다) · 21-23-SUMMARY.md (UAT 환경 표 형식)
    - mobile/scripts/smoke-ios.sh · smoke-android.sh (앱 번들 · APK 경로 · 기기 지정)
  </read_first>
  <action>
    ① 21-23 Task 2 와 같은 방식: 운영 API 주소는 `gcloud run services describe gh-radar-server --region asia-northeast3 --format 'value(status.url)'`(env 파일 미열람) · scratchpad 프록시 `127.0.0.1:8080`(Origin = https://trade.jx1.io · ACAO 되돌림 · 로그는 메서드·경로·상태) · `nohup` · PID 기록. relay(:8090)는 띄우지 않는다.
    ② dev 빌드: `CAP_SERVER_URL=http://localhost:3100` 로 `native:build:ios` · `native:build:android`.
    ③ 새 설치(저장값·로그인 없음 — R3-4 첫 로그인 · 기본 다크 조건): iPhone 17 · iPad Pro 11-inch (M5) UDID → 부팅 → `uninstall` → 설치 → `launch`. Android: `adb -s <시리얼> uninstall com.ghtrade.app` → 설치 → `adb reverse tcp:3100 tcp:3100` · `tcp:8080 tcp:8080` → `am start`.
    ④ SUMMARY 「UAT 환경」 표: 기기별 UDID/시리얼 · 설치 URL(dev) · 프록시 PID · dev 서버 기동 주체. 「실서버 재확인 대상」 표(relay 필요): G-21-R3-2(앱 /trading 하단 패널 숨김 · 더티 바 · /me 로그 실데이터) · R3-3 · R3-5 · R3-6 · R3-7(카드 필요) · R3-8 팝업 쪽(카드 ⓘ) · R3-9 카드 착지 · R3-10 카드 시간외종가(실주문 금지 — 선택·잠김·확인 다이얼로그까지) · R3-11 트레이딩 왕복 · WR-04 상따 설정 팝오버 뒤로가기.
  </action>
  <verify>
    <automated>curl -sf -o /dev/null http://localhost:3100/login && curl -sf -o /dev/null http://127.0.0.1:8080/api/health && echo "UAT ENDPOINTS OK"</automated>
    <fails_when>non-zero exit (dev server or proxy not answering), or "UAT ENDPOINTS OK" is absent</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && OUT="$(pnpm --filter @gh-radar/mobile run native:verify-prod 2>&1 || true)" && printf '%s\n' "$OUT" | grep -c "FAIL"</automated>
    <fails_when>the count prints 0 — the generated config is still the production URL, so the UAT devices did not get the dev build</fails_when>
  </verify>
  <acceptance_criteria>
    - UAT 환경 표와 실서버 재확인 대상 표가 SUMMARY 에 있다
    - relay 미기동: `lsof -iTCP:8090 -sTCP:LISTEN` 출력 0줄(다른 세션이 띄운 경우 그 사실을 기록)
    - `git status -sb` 에 이 태스크가 만든 추적 파일 변경이 없다
  </acceptance_criteria>
  <done>세 기기에 새 dev 빌드가 떠 있고 dev 웹 · 프록시가 응답한다 — 21-36 이 바로 UAT 와 push 결정을 받는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → 운영 API(UAT 프록시) | 운영 데이터를 읽는 로컬 프록시 |
| 로컬 → relay(실계좌) | UAT 중 relay 가 뜨면 실주문 경로가 열린다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-96 | Elevation of Privilege | UAT 중 relay 실주문 경로 | high | mitigate | relay 미기동 · lsof 확인(Task 3) · 트레이딩 실동작은 실서버 재확인 대상으로 분리 |
| T-21-97 | Information Disclosure | env 파일 · 프록시 설정 커밋 | medium | mitigate | gcloud 조회 · scratchpad 프록시 · 추적 변경 0 확인(Task 3) |
| T-21-98 | Repudiation | 1차 리뷰 기록 덮어쓰기 | low | mitigate | -R2 새 파일 · 세 파일 git diff --quiet(Task 2) |
</threat_model>

<verification>
- 게이트 일곱 명령 · REVIEW-R2 13행 · UAT 엔드포인트 응답 · dev 빌드 설치 확인
</verification>

<success_criteria>
- 21-36 체크포인트가 바로 UAT 와 push 결정을 받을 수 있다.
</success_criteria>

## Flagged assumptions

- **엣지 탐침 MOBILE-01 (unclassified · unresolved):** 이 플랜은 검증 전용이다 — 분류되지 않은 경계는 21-36 UAT 에서 사람이 본다.
- 기준 커밋 aba7954 이후 다른 세션이 백엔드 커밋을 넣으면 Task 1 이 멈춘다 — 그때는 relay 선배포 순서를 사용자에게 묻는다.

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| 스케치 `.planning/sketches/008-uat3-surfaces/`(index.html · README.md) · MANIFEST 008 행 | 목업 | 21-25 |
| CONTEXT D-12a · D-12b · D-25a · D-29 · D-30 · D-31 · D-32(R3-11 축소: 스크롤 복원 + 스켈레톤 없음 · 상태 초기화 허용) · WR-03 androidx.webkit 선언 답 | 결정 기록 | 21-25 |
| `PAD_CHIPS.만원` = 천만(+1,000) · 오천만(+5,000) · 1억(+10,000) · 지우기 · 20-CONTEXT D-17 포인터 | 상수 · 결정 | 21-26 |
| `SettingGroup` 제목 없는 그룹 상단 `pt-1` | 클래스 | 21-26 |
| 카드 헤더 ✕ `data-slot="card-close"` · 종목정보 팝업 ✕ = `size-8` + lucide `XIcon` + `after:-inset-1.5`(히트 44) · 팝업 고정 높이 · `scrollbar-gutter:stable` · 폰 safe-area 패딩 | 컴포넌트 | 21-26 |
| `webapp/src/lib/safe-path.ts` `isSafeInternalPath` · `recent-search.ts` `RECENT_CODE_RE` · `PopoverContent` 안 `NativeOverlayMarker` · `useDebouncedSearch` 반환 `resultsQuery` · `viewport.themeColor` `#17171c` + `ThemeColorSync` | 함수 · 훅 · 메타 | 21-27 |
| iOS 키보드 사유 즉시 숨김 · 재표시 90ms · `awaitingContent`(didStartProvisionalNavigation → 첫 route/1.5초) · 초기 숨김 · 브리지 출처 scheme·host·port 비교 · 오프라인 `dev=1` · 스모크 기기 지정(`IOS_DEVICE_UDID` · `ANDROID_SERIAL`) · mobile/README 명령표 | Swift · 스크립트 · 문서 | 21-28 |
| Android IME 애니메이션 콜백 즉시 숨김 · `awaitingContent`(onPageStarted) · `pullBlocked` 초기화 · 오프라인 뒤로가기(재현 시) · 오프라인 `dev=1` · 폴백 페이지 dev=1 일 때만 localhost · allowBackup=false + `res/xml/data_extraction_rules.xml` · 브리지 WebMessageListener(21-25 답 a) | Kotlin · XML · HTML | 21-29 |
| `NewsFullList` · `DiscussionFullList` · `useNewsView` · `StockNewsTabPanel` · 섹션 `onShowAll` · 종목상세 `?tab=news&view=news\|discussions` · 옛 URL 리다이렉트 · 팝업 `newsView` | 컴포넌트 · 훅 · 라우트 | 21-30 |
| `useTabRootScrollMemory`(`webapp/src/lib/tab-scroll-memory.ts` · AppShell 에서 호출) · 기존 `lib/query-cache` 재사용: 검색 허브 · 대화 목록 · 오늘 주문 | 훅 · 캐시 | 21-31 |
| 앱 공용 패널 숨김 CSS · 카드 더티 바 탭바 비킴(프로브) · `StrategyLogFeedProvider` · `useStrategyLogFeed` · `diffLimitChasers` · /me 전략 로그(`me-strategy-log`) | CSS · Provider · 컴포넌트 | 21-32 |
| 작업대 `?code=` 소비(카드 보장·펼침·포커스) · 카드 수동주문 주문유형(지정가/시간외종가) | 컴포넌트 | 21-33 |
| 종목상세 「트레이딩」 CTA(폰 바 + `detail-trading-button`) · 호가주문 탭 제거 · `?tab=orderbook` → `/trading?code=` · 호가 탭 전용 코드·`variant` 삭제 · orderbook.spec 선별 이전 | 컴포넌트 · 정리 · e2e | 21-34 |
| 게이트 기록 · `21-REVIEW-R2.md`(13건 대조) · UAT 환경(21-35-SUMMARY) · UAT 3차 재검증 · push 결정(21-36-SUMMARY) | 문서 | 21-35 · 21-36 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-35-SUMMARY.md` when done — 게이트 표 · REVIEW-R2 요약 · UAT 환경 표 · 실서버 재확인 대상 표.
</output>
