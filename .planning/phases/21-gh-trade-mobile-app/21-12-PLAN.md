---
phase: 21-gh-trade-mobile-app
plan: 12
type: execute
wave: 4
depends_on: ["21-02", "21-04", "21-10"]
files_modified:
  - mobile/android/app/src/main/java/com/ghtrade/app/TabRoutes.kt
  - mobile/android/app/src/test/java/com/ghtrade/app/TabRoutesTest.kt
  - mobile/package.json
  - mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt
  - mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt
  - mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
  - mobile/android/app/src/main/res/drawable/ic_tab_home.xml
  - mobile/android/app/src/main/res/drawable/ic_tab_home_fill.xml
  - mobile/android/app/src/main/res/drawable/ic_tab_search.xml
  - mobile/android/app/src/main/res/drawable/ic_tab_search_fill.xml
  - mobile/android/app/src/main/res/drawable/ic_tab_trading.xml
  - mobile/android/app/src/main/res/drawable/ic_tab_trading_fill.xml
  - mobile/android/app/src/main/res/drawable/ic_tab_ai.xml
  - mobile/android/app/src/main/res/drawable/ic_tab_ai_fill.xml
  - mobile/android/app/src/main/res/drawable/ic_tab_me.xml
  - mobile/android/app/src/main/res/drawable/ic_tab_me_fill.xml
autonomous: true
requirements: [MOBILE-01]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-27 · D-27a: 목업 게이트(스케치 004)를 통과한 채택안 수치만 Android 상수로 옮긴다 — 블러 근사(A10)는 21-16 UAT 에서 사용자 확인"
    - "D-14: Android 활성 탭 판정 `TabRoutes.activeTabFor` · 숨김 경로 `TabRoutes.hidesTabBar` 가 iOS `TabRoutes.swift`(21-10)와 같은 표를 쓰고 JUnit `TabRoutesTest` 가 표 전체를 단언한다"
    - "D-02 · D-27a: Android 탭바 = 알약(높이 70dp · radius 32dp · 좌우 16dp · 폭 min(560dp, 화면폭 − 32dp) 가운데 · 1px `--line` 테두리 · 그림자 elevation) — 실시간 블러 표준 수단이 없어 card 색 약 94% 불투명으로 근사(RESEARCH A10, 시각은 UAT) · 활성 = primary 14% 원 46dp + filled/굵은 아이콘 + primary 라벨 10sp · 비활성 muted · 하단 120dp 페이드(bg 92%)"
    - "D-27a(Android): 탭바 바닥 = 내비게이션 바 인셋 − 14dp, 하한 14dp — 인셋 리스너는 탭바 컨테이너에만 건다(DecorView·루트에 걸지 않는다 — SystemBars 리스너 보존, Pitfall 8)"
    - "D-15: 아이콘 = Material 계열 벡터 — outlined(비활성) ↔ filled 또는 굵은 획(활성) 쌍 `ic_tab_{home,search,trading,ai,me}(_fill)`"
    - "D-12: 숨김 = `/login`·`/auth/*` · 현재 URL 호스트 ≠ server.url 호스트(오프라인 로컬 페이지) · 웹 overlay 열림 · 키보드(IME) 표시 — 150ms 지연 후 200ms 페이드(+8dp 아래), 다시 보일 때 즉시 200ms"
    - "D-13 · D-06 · D-06a: 폭과 무관하게 표시 · 탭 탭 = `evaluateJavascript` 로 `window.__ghTrade.navigate(path)`(없으면 `location.assign`) · 오프라인 로컬 페이지에서는 server.url+경로 `loadUrl`"
  artifacts:
    - path: mobile/android/app/src/main/java/com/ghtrade/app/TabRoutes.kt
      provides: "TabId · TabRoutes.activeTabFor · TabRoutes.hidesTabBar · normalize"
      contains: "object TabRoutes"
    - path: mobile/android/app/src/test/java/com/ghtrade/app/TabRoutesTest.kt
      provides: "D-14 표 JUnit"
    - path: mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt
      provides: "알약 탭바 View · 페이드"
      contains: "class GhTradeTabBar"
    - path: mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt
      provides: "D-27a hex 팔레트"
      contains: "0xFF3485FA"
  key_links:
    - from: mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
      to: mobile/android/app/src/main/java/com/ghtrade/app/TabRoutes.kt
      via: "onUrlChanged / route 메시지 → TabRoutes.activeTabFor → tabBar.setActive"
      pattern: "TabRoutes\\.activeTabFor"
    - from: mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
      to: webapp/src/lib/native/native-bridge-provider.tsx
      via: "evaluateJavascript(\"window.__ghTrade&&window.__ghTrade.navigate?…\")"
      pattern: "__ghTrade"
---

<assumption_delta_decision>
- 검출기 `assumption-delta scan 21` → `detected: false`. 수동 판정: 두 번째 클라이언트 플랫폼 — 정본 명사 **웹앱**, 결정 **add-alongside**. Android 탭바도 웹 경로를 읽기만 하고 이동은 웹 훅에 맡긴다(21-01 블록 참조).
</assumption_delta_decision>

<objective>
Android 네이티브 하단 탭바(D-02·D-27a)를 `MainActivity` 에 얹고 활성 판정(D-14)·숨김 규칙(D-12·IME)·탭 이동(D-06a)을 웹 계약과 잇는다. 경로표는 JVM 단위 테스트로 잠근다.

Purpose: Android 사용자도 iOS 와 같은 5탭 내비를 쓴다. 인셋은 Capacitor SystemBars 와 싸우지 않도록 탭바 컨테이너에만 건다.
Output: Kotlin 4파일 + 테스트 1 + 벡터 10.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md
@.planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md
@.planning/sketches/004-native-tab-bar/README.md
@mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
@mobile/ios/App/App/TabRoutes.swift

Gradle 은 Bash timeout 600000. 빌드 전 `cap sync android` 가 생성 디렉터리(gitignore)를 만든다 — 새 클론이면 먼저 sync.

<interfaces>
21-02 · 21-10 계약:

```kotlin
class MainActivity : BridgeActivity() { fun serverHost(): String?; fun onUrlChanged(url: String); fun onNativeMessage(type: String, payload: JSONObject?) }
// GhTradeBridge → onNativeMessage(type ∈ ready|route|theme|overlay|pull, payload) — UI 스레드
```

```swift
// 21-10 TabRoutes.swift 표(그대로 옮긴다)
normalize: "" → "/" · 쿼리/프래그먼트 제거 · 루트 외 끝 슬래시 제거
activeTab: "/"→home · "/search","/scanner","/themes","/watchlist"→search · "/trading"→trading · "/chat"→ai · "/me"→me · 그 외 nil
hidesTabBar: == "/login" || hasPrefix("/login/") || == "/auth" || hasPrefix("/auth/")
GHTabID 순서 home, search, trading, ai, me · path "/","/search","/trading","/chat","/me" · title 홈·검색·트레이딩·AI·마이
```

weekly-wine 원형: `MainActivity.kt` L441-534 `setupTabBar`(CoordinatorLayout · `Gravity.BOTTOM` · 페이드 GradientDrawable · FrameLayout 70dp) · L857-930 `onUrlChanged`(숨김 Runnable 취소 · 애니메이션 경합) · `res/drawable/ic_tab_home.xml`(24dp 벡터 · stroke 1.8) · `ic_tab_sparkles.xml`.

D-27a hex: 다크 card `#202027` · bg `#17171c` · primary `#3485fa` · muted `#9e9ea4` · line `#12FFFFFF`(흰 7%) / 라이트 card `#ffffff` · bg `#ffffff` · primary `#3182f6` · muted `#6b7684` · line `#e5e8eb`.

```kotlin
// 이 플랜이 새로 만드는 계약(21-13 이 소비)
enum class TabId(val path: String, val title: String) { HOME("/", "홈"), SEARCH("/search", "검색"), TRADING("/trading", "트레이딩"), AI("/chat", "AI"), ME("/me", "마이") }
object TabRoutes { fun normalize(path: String): String; fun activeTabFor(path: String): TabId?; fun hidesTabBar(path: String): Boolean }
data class GhTradePalette(val card: Int, val bg: Int, val primary: Int, val muted: Int, val line: Int) { companion object { fun of(theme: String): GhTradePalette } }
class GhTradeTabBar(context: Context) : FrameLayout(context) { var onSelect: ((TabId) -> Unit)?; fun setActive(tab: TabId?); fun apply(p: GhTradePalette); val fadeView: View }
// MainActivity: var currentTheme = "light" · var overlayOpen · var isOfflinePage · var pullBlocked · var currentPath · fun updateTabBarVisibility() · fun selectTab(tab: TabId)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `TabRoutes.kt`(D-14 표) + JUnit `TabRoutesTest`</name>
  <files>mobile/android/app/src/main/java/com/ghtrade/app/TabRoutes.kt, mobile/android/app/src/test/java/com/ghtrade/app/TabRoutesTest.kt, mobile/package.json</files>
  <read_first>
    - mobile/ios/App/App/TabRoutes.swift · mobile/scripts/tab-routes-check.swift (21-10 — 같은 케이스 목록)
    - mobile/android/app/build.gradle (`testImplementation "junit:junit:$junitVersion"` 존재 확인 — 없으면 variables.gradle 값으로 추가)
  </read_first>
  <behavior>
    - 21-10 `tab-routes-check.swift` 의 케이스 전부(normalize 5 · activeTab 19 · hidesTabBar 8 · TabId 순서/경로/제목)가 같은 기대값으로 통과한다
  </behavior>
  <action>
    `TabRoutesTest.kt`(JUnit4 `@Test` · `assertEquals`)를 먼저 쓴다(RED). 그다음 `TabRoutes.kt`: `enum class TabId(val path: String, val title: String)` 과 `object TabRoutes` 를 `<interfaces>` 이름대로 — Android 의존 없는 순수 Kotlin(`Uri` 금지 — 문자열 연산만, JVM 단위 테스트에서 돌아야 한다). 머리 주석: D-14 정본 · iOS `TabRoutes.swift` 와 같은 표를 유지해야 한다(한쪽만 바꾸면 두 앱의 활성 탭이 갈린다). 템플릿 `ExampleUnitTest` 가 있으면 그대로 둔다. `mobile/package.json` scripts 에 `native:test:android` = `cd android && ./gradlew testDebugUnitTest --tests com.ghtrade.app.TabRoutesTest` 를 더한다(build/typecheck/test/lint 키 금지 유지 — 이름이 `native:` 접두라 루트 `pnpm -r` 가 집어가지 않는다).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile exec cap sync android && pnpm --filter @gh-radar/mobile run native:test:android</automated>
    <fails_when>non-zero exit, or output lacks "BUILD SUCCESSFUL", or contains "FAILED" or "No tests found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "object TabRoutes" mobile/android/app/src/main/java/com/ghtrade/app/TabRoutes.kt` 가 1줄 · `grep -c "import android" mobile/android/app/src/main/java/com/ghtrade/app/TabRoutes.kt` 가 0
    - `grep -n "native:test:android" mobile/package.json` 가 1줄
    - `grep -n '"/watchlist"' mobile/android/app/src/test/java/com/ghtrade/app/TabRoutesTest.kt` 가 1줄 이상 · `grep -n '"/stocks/005930"' mobile/android/app/src/test/java/com/ghtrade/app/TabRoutesTest.kt` 가 1줄 이상
  </acceptance_criteria>
  <done>Android 의 활성·숨김 경로 판정이 iOS 와 같은 표로 JVM 테스트에 잠겼다.</done>
</task>

<task type="auto">
  <name>Task 2: `GhTradePalette`(D-27a) + 벡터 아이콘 10개(D-15) + `GhTradeTabBar` 알약·페이드 뷰</name>
  <files>mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt, mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt, mobile/android/app/src/main/res/drawable/ic_tab_home.xml, mobile/android/app/src/main/res/drawable/ic_tab_home_fill.xml, mobile/android/app/src/main/res/drawable/ic_tab_search.xml, mobile/android/app/src/main/res/drawable/ic_tab_search_fill.xml, mobile/android/app/src/main/res/drawable/ic_tab_trading.xml, mobile/android/app/src/main/res/drawable/ic_tab_trading_fill.xml, mobile/android/app/src/main/res/drawable/ic_tab_ai.xml, mobile/android/app/src/main/res/drawable/ic_tab_ai_fill.xml, mobile/android/app/src/main/res/drawable/ic_tab_me.xml, mobile/android/app/src/main/res/drawable/ic_tab_me_fill.xml</files>
  <read_first>
    - .planning/sketches/004-native-tab-bar/README.md (「결정」) · .planning/sketches/004-native-tab-bar/index.html (L138-163)
    - /Users/alex/repos/weekly-wine-app/android/app/src/main/res/drawable/ic_tab_home.xml · ic_tab_home_fill.xml · ic_tab_search.xml · ic_tab_sparkles.xml · ic_tab_person.xml · ic_tab_person_fill.xml (24dp 벡터 형식 원형)
    - /Users/alex/repos/weekly-wine-app/android/app/src/main/java/kr/co/weeklywine/app/MainActivity.kt (L441-534 `setupTabBar`)
    - mobile/ios/App/App/GHTradeTabBar.swift (21-10 — 같은 수치·API)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pattern 3 「탭바」 · Assumptions A10)
  </read_first>
  <action>
    ① 벡터 10개(모두 `24dp` · viewport 24 · 색은 `#000000` — 코드에서 tint): `ic_tab_home`/`ic_tab_me` 는 weekly-wine `ic_tab_home(_fill)`·`ic_tab_person(_fill)` 모양을 옮기고, `ic_tab_search` 는 weekly-wine `ic_tab_search`, `ic_tab_ai` 는 weekly-wine `ic_tab_sparkles` 모양, `ic_tab_trading` 은 Material 「show_chart」 계열 상승 꺾은선(stroke 1.8 · round cap/join, 예 pathData `M3,17L9,11L13,15L21,7` + 화살촉 `M15,7H21V13`). `_fill` 변형: home·me 는 채운 모양, search·trading·ai 는 같은 모양에 stroke 2.4(활성 굵기 — iOS semibold 와 대응). weekly-wine 파일을 옮긴 경우 출처 주석 없이 형식만 맞춘다.
    ② `GhTradePalette.kt`: `data class GhTradePalette(card, bg, primary, muted, line: Int)` + `companion object { fun of(theme: String): GhTradePalette }` — `"dark"` 면 `0xFF202027.toInt()` · `0xFF17171C` · `0xFF3485FA` · `0xFF9E9EA4` · `0x12FFFFFF`, 그 외 라이트 `0xFFFFFFFF` · `0xFFFFFFFF` · `0xFF3182F6` · `0xFF6B7684` · `0xFFE5E8EB`. 주석: 정본 D-27a.
    ③ `GhTradeTabBar.kt`: `class GhTradeTabBar(context: Context) : FrameLayout(context)` — 자신 = 알약 컨테이너: `background = GradientDrawable().apply { cornerRadius = 32dp; setColor(ColorUtils.setAlphaComponent(card, 240)) /* ≈94% — A10 */; setStroke(1, line) }` · `elevation = 8dp` + `outlineProvider = ViewOutlineProvider.BACKGROUND`(그림자) · 안쪽 가로 `LinearLayout` 에 항목 5개(`TabId.values()` 순서, `layout_weight 1`): 항목 = `FrameLayout` 안 강조 원 `View` 46dp(`GradientDrawable` oval · primary alpha 36(≈14%) · top margin 7dp · 가로 가운데 · 활성일 때만 VISIBLE) · `ImageView` 26dp(top margin 12dp · 가운데) · `TextView`(아이콘 아래 4dp · 10sp · `Typeface.create("sans-serif-medium", Typeface.NORMAL)` · 가운데). 활성 = `_fill` drawable + primary tint(아이콘·라벨), 비활성 = 일반 drawable + muted. 항목 `contentDescription = title` · 클릭 → `onSelect?.invoke(tab)`. `val fadeView = View(context)` — `GradientDrawable(TOP_BOTTOM, [bg α0, bg α235(92%), bg α235])` · 클릭/포커스 불가. `setActive(tab: TabId?)`(null = 5개 비활성, D-14) · `apply(p)`(알약 색·테두리·아이콘/라벨/강조·페이드 색 갱신). dp 변환은 `resources.displayMetrics` 로.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:android</automated>
    <fails_when>non-zero exit, or the output lacks "BUILD SUCCESSFUL", or contains Kotlin "e: " errors or "AAPT: error"</fails_when>
  </verify>
  <acceptance_criteria>
    - 10개 drawable 파일이 모두 존재한다(`for f in home home_fill search search_fill trading trading_fill ai ai_fill me me_fill; do test -f mobile/android/app/src/main/res/drawable/ic_tab_$f.xml || exit 1; done` exit 0)
    - `grep -n "0xFF3485FA" mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt` · `grep -n "0xFF17171C" mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt` 가 각각 1줄
    - `grep -n "cornerRadius" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt` 가 1줄 이상 · `grep -n "fadeView" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeTabBar.kt` 가 1줄 이상
  </acceptance_criteria>
  <done>채택 수치의 Android 알약 탭바와 아이콘 쌍이 팔레트 교체 API 와 함께 컴파일된다.</done>
</task>

<task type="auto">
  <name>Task 3: MainActivity 배선 — 탭바 부착 · 인셋(탭바 컨테이너만) · IME · URL/route/overlay · 150ms 페이드 · 탭 → navigate (D-06a · D-12 · D-13 · D-14 · D-27a)</name>
  <files>mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt</files>
  <read_first>
    - mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt · GhTradeWebViewClient.kt · GhTradeBridge.kt (21-02)
    - /Users/alex/repos/weekly-wine-app/android/app/src/main/java/kr/co/weeklywine/app/MainActivity.kt (L90-120 · L441-534 · L857-930)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pattern 3 · Pitfall 8 · Anti-Patterns DecorView 리스너)
  </read_first>
  <action>
    ① 상태: `lateinit var tabBar: GhTradeTabBar` · `var currentTheme = "light"`(21-13 이 저장값으로) · `var currentPath = "/"` · `var isOfflinePage = false` · `var overlayOpen = false` · `var pullBlocked = false`(21-13 소비) · `private var keyboardVisible = false` · `private var hideRunnable: Runnable? = null`. `load()` 에서 21-02 코드 뒤에 `setupTabBar()`.
    ② `setupTabBar()`: `val root = bridge.webView.parent as ViewGroup`(템플릿 CoordinatorLayout — 21-13 이 WebView 를 SwipeRefreshLayout 으로 감싸기 **전에** 참조를 `rootLayout` 필드로 보관) · 페이드: `MATCH_PARENT × 120dp`, `gravity = BOTTOM` · 탭바: 높이 70dp · `gravity = BOTTOM or CENTER_HORIZONTAL` · 폭 = `min(560dp, 화면폭px − 32dp)`(`resources.configuration.screenWidthDp` 기준 — `onConfigurationChanged` 에서 다시 계산) · `tabBar.apply(GhTradePalette.of(currentTheme))` · `tabBar.onSelect = { selectTab(it) }`.
    ③ 인셋(Pitfall 8): `ViewCompat.setOnApplyWindowInsetsListener(tabBar) { v, insets -> val nav = insets.getInsets(WindowInsetsCompat.Type.navigationBars()).bottom; (v.layoutParams as MarginLayoutParams).bottomMargin = max(nav − 14dp, 14dp); v.requestLayout(); insets }` 와 `ViewCompat.requestApplyInsets(tabBar)` — **DecorView·루트·WebView 에는 인셋 리스너를 걸지 않는다**(SystemBars 의 DecorView 리스너 보존). 키보드: `rootLayout.viewTreeObserver.addOnGlobalLayoutListener { val ime = ViewCompat.getRootWindowInsets(rootLayout)?.isVisible(WindowInsetsCompat.Type.ime()) == true; if (ime != keyboardVisible) { keyboardVisible = ime; updateTabBarVisibility() } }`.
    ④ `onUrlChanged(url)`(21-02 의 로그 자리를 대체): `val uri = Uri.parse(url)` · `isOfflinePage = uri.host != serverHost()` · 아니면 `currentPath = TabRoutes.normalize(uri.path ?: "/")` · `tabBar.setActive(if (isOfflinePage) null else TabRoutes.activeTabFor(currentPath))` → `updateTabBarVisibility()`. `onNativeMessage`: `route` → `payload.optString("path")` 가 `/` 로 시작할 때만 `currentPath` 갱신 + 같은 처리(SPA 보강) · `overlay` → `overlayOpen = payload.optBoolean("open")` · `pull` → `pullBlocked = payload.optBoolean("blocked")` · `theme` → 이 플랜에서는 `currentTheme` 저장만(적용은 21-13).
    ⑤ `updateTabBarVisibility()`: `hide = TabRoutes.hidesTabBar(currentPath) || isOfflinePage || overlayOpen || keyboardVisible`. 숨김: 기존 `hideRunnable` 제거 → 새 Runnable 을 `handler.postDelayed(…, 150)` — 실행 시 여전히 숨겨야 하면 `tabBar.animate().alpha(0f).translationY(8dp).setDuration(200).withEndAction { visibility = GONE }` + 페이드 alpha 0. 보임: `hideRunnable` 제거 → `tabBar.animate().cancel()` → `VISIBLE` → `alpha(1f).translationY(0f).setDuration(200)`(weekly-wine L870-930 의 경합 방지 모양).
    ⑥ `selectTab(tab)`: `tabBar.setActive(tab)` → `isOfflinePage` 면 `bridge.webView.loadUrl(bridge.config.serverUrl.trimEnd('/') + tab.path)` · 아니면 `bridge.webView.evaluateJavascript("window.__ghTrade&&window.__ghTrade.navigate?window.__ghTrade.navigate('${tab.path}'):location.assign('${tab.path}')", null)` — 경로는 `TabId.path` 상수뿐.
    ⑦ 빌드 뒤 21-02 스모크를 다시 돌린다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:android</automated>
    <fails_when>non-zero exit, or the output lacks "BUILD SUCCESSFUL"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:smoke:android</automated>
    <fails_when>non-zero exit, or the output lacks "SMOKE OK ready platform=android"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "setOnApplyWindowInsetsListener(tabBar)" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 1줄 · `grep -cE "setOnApplyWindowInsetsListener\((window\.decorView|rootLayout|bridge\.webView|wv)\b" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 0
    - `grep -n "WindowInsetsCompat.Type.ime()" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 1줄 이상
    - `grep -n "TabRoutes.activeTabFor" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 1줄 이상 · `grep -n "postDelayed" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 1줄 이상
    - `grep -n "__ghTrade.navigate" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 1줄
  </acceptance_criteria>
  <done>Android 앱에 탭바가 서서 경로·오버레이·키보드에 따라 활성·숨김이 바뀌고, 탭 탭은 웹 클라 내비로 이동한다(시각·내비 모드별 여백은 21-16 UAT).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 웹 메시지(route/overlay/pull/theme) → 탭바 상태 | 표시 상태만 바뀐다 |
| 네이티브 → 웹 `evaluateJavascript` | 상수 경로만 조립 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-36 | Tampering | `evaluateJavascript` 문자열 조립 | medium | mitigate | `TabId.path` 상수만 사용(Task 3 ⑥) |
| T-21-03 | Elevation of Privilege | JS 인터페이스 | high | mitigate | 21-02 호스트 검사·화이트리스트 유지 · 이 플랜은 메서드를 늘리지 않는다 |
| T-21-39 | Denial of Service | DecorView 인셋 리스너 덮어쓰기로 SystemBars CSS 인셋·IME 패딩 소실 | medium | mitigate | 리스너는 탭바 컨테이너에만(acceptance grep 0) |
| T-21-SC | Tampering | 패키지 설치 | low | accept | 새 의존성 없음(androidx 기존 · Material 벡터는 로컬 XML) |
</threat_model>

<verification>
- TabRoutesTest green · `native:build:android` · `native:smoke:android` green
- 시각(알약·94% 근사·페이드)·제스처/3버튼 내비 여백·키보드 숨김은 21-16 UAT
</verification>

<success_criteria>
- D-02·D-06·D-06a·D-12·D-13·D-14·D-15·D-27a 가 Android 에서 구현됐다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/` 패키지 `@gh-radar/mobile` — scripts `native:sync` · `native:sync:dev` · `native:build:ios` · `native:build:android` · `native:smoke:ios` · `native:open:ios` · `native:open:android` · `native:assets` (21-01) · `native:smoke:android` (21-02) · `native:check-tab-routes:ios` (21-10) · `native:test:android` (21-12) · `native:verify-prod` (21-16) | 패키지·스크립트 | 21-01 · 21-02 · 21-10 · 21-12 · 21-16 |
| `mobile/capacitor.config.ts` — appId `com.ghtrade.app` · appName `GH Trade` · `CAP_SERVER_URL` · `SystemBars` (21-01) · `SocialLogin` (21-15) | 설정 | 21-01 · 21-15 |
| `mobile/tsconfig.json` · `mobile/.gitignore` | 설정 | 21-01 |
| `mobile/www/index.html` — 오프라인 폴백(최소 21-01 · `safeTarget`·5초 탐침·`online`·테마 21-11) | 정적 페이지 | 21-01 · 21-11 |
| `mobile/scripts/smoke-ios.sh` · `mobile/scripts/smoke-android.sh` — 「SMOKE OK ready platform=…」 | 스모크 | 21-01 · 21-02 |
| `mobile/scripts/check-tab-routes-ios.sh` · `mobile/scripts/tab-routes-check.swift` | Swift 경로표 검사 | 21-10 |
| `mobile/scripts/render-resources.mjs` · `mobile/resources/{icon.svg,icon-only.png,icon-foreground.png,icon-background.png,splash.png,splash-dark.png}` | 에셋 원본 | 21-14 |
| `mobile/scripts/verify-prod-config.mjs` · `mobile/README.md` | 운영 설정 검사·문서 | 21-16 |
| iOS `SceneDelegate.swift` (루트 = `GHTradeBridgeViewController()` · 창 배경 21-11) | Swift | 21-01 · 21-11 |
| iOS `GHTradeBridgeViewController` · `WeakScriptMessageHandler` · `NativeMessageType` | Swift | 21-01 · 21-10 · 21-11 |
| iOS `GHTradeTabBar` · `GHTradeTab` · `GHTabID` · `GHTradePalette` · `GHTradeTheme` · `TabRoutes.activeTab(forPath:)` · `TabRoutes.hidesTabBar(path:)` | Swift | 21-10 |
| iOS `NavigationDelegateProxy` · `ThemeStore` (UserDefaults `gh-trade.theme`) | Swift | 21-11 |
| Android `MainActivity` (`BridgeActivity`) · `GhTradeWebViewClient` (`BridgeWebViewClient` 상속) · `GhTradeBridge` (`@JavascriptInterface postMessage`) | Kotlin | 21-02 · 21-12 · 21-13 |
| Android `GhTradeTabBar` · `GhTradePalette` · `TabId` · `TabRoutes.activeTabFor` · `TabRoutes.hidesTabBar` · `TabRoutesTest` | Kotlin | 21-12 |
| Android `ThemeStore` (SharedPreferences `gh_trade`/`theme`) · `res/values/bools.xml` · `res/values-sw600dp/bools.xml` (`is_tablet`) | Kotlin·리소스 | 21-13 |
| Android `res/drawable/ic_tab_{home,home_fill,search,search_fill,trading,trading_fill,ai,ai_fill,me,me_fill}.xml` | 벡터 | 21-12 |
| 웹 `lib/native/native-detect.ts` — `NATIVE_DETECT_SCRIPT` · `isNativeApp` · `nativePlatform` | 유틸 | 21-01 |
| 웹 `lib/native/post-native.ts` — `postNative` · `NativeMsgType` | 유틸 | 21-04 |
| 웹 `lib/native/native-bridge-provider.tsx` — `NativeBridgeProvider` · `useNativeBridge` · `NativeRefreshFn` · `window.__ghTrade = { refresh, navigate, back }` | Provider | 21-04 |
| 웹 `lib/native/use-native-refresh.ts` — `useNativeRefresh` | 훅 | 21-04 |
| 웹 `lib/native/native-overlay-marker.tsx` — `NativeOverlayMarker` | 컴포넌트 | 21-04 |
| 웹 `lib/native/google-client-ids.ts` — `GOOGLE_WEB_CLIENT_ID` · `GOOGLE_IOS_CLIENT_ID` · `GOOGLE_ANDROID_CLIENT_ID` · `GOOGLE_IOS_URL_SCHEME` | 공개 식별자 상수 | 21-03 |
| 웹 `lib/native/native-google-login.ts` — `nativeGoogleSignIn` · `randomNonce` · `sha256Hex` · `classifyNativeLoginError` | 서비스 | 21-15 |
| 웹 `RelayConnectionState.probeNow` · `EMPTY_RELAY_VALUE.probeNow` | 훅 계약 | 21-07 |
| 웹 `lib/recent-search.ts` — `RECENT_SEARCH_KEY` · `RECENT_SEARCH_MAX` · `RecentSearchItem` · `readRecentSearches` · `pushRecentSearch` · `removeRecentSearch` · `clearRecentSearches` | 유틸 | 21-08 |
| 웹 `app/search/page.tsx` · `components/search/search-page-client.tsx` — `SearchPageClient` · 사이드바 `NAV_SEARCH_PAGE` | 페이지·상수 | 21-08 |
| 웹 `components/me/account-card.tsx` — `AccountCard` | 컴포넌트 | 21-09 |
| 웹 마크업 계약 `data-slot="app-aside"` · `data-slot="app-menu-button"` · `data-slot="stock-ai-button"` · `data-slot="shared-panels"` · `data-pinned` · `data-order-cta` · `data-slot="account-card"` | 마크업 | 21-05 · 21-06 · 21-09 |
| CSS `@custom-variant native` (21-04) · 앱 셸 규칙 (21-05) · `--app-safe-top` · `--app-safe-bottom` · `--app-safe-left` · `--app-safe-right` · `--native-tabbar-gap` · `--native-tabbar-offset` · `--native-body-reserve` (21-06) | 토큰·변형 | 21-04 · 21-05 · 21-06 |
| 웹 `app/icon.svg` (= 스케치 004 `#app-a`) | 파비콘 | 21-14 |
| 단위 테스트: `native-detect.test.ts` (21-01) · `post-native.test.ts` · `native-bridge-provider.test.tsx` · `native-overlay.test.tsx` (21-04) · `google-client-ids.test.ts` (21-03) · `relay-socket.test.ts` probeNow · `stock-native-refresh.test.tsx` (21-07) · `recent-search.test.ts` · `search-page-client.test.tsx` · `app-sidebar.test.tsx` 보강 (21-08) · `account-card.test.tsx` · `app-shell-chrome.test.tsx` 보강 (21-09) · `native-google-login.test.ts` · `login/__tests__/page.test.tsx` (21-15) · `TabRoutesTest.kt` (21-12) | 단위 | 각 플랜 |
| e2e: `fixtures/native-app.ts` — `installNativeApp` · `nativeMessages` (21-05) · `specs/native-shell.spec.ts` (21-05 · 21-06) · `specs/search-page.spec.ts` (21-08) · `specs/brand-account.spec.ts` (21-09) | Playwright | 각 플랜 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-12-SUMMARY.md` when done
</output>
