---
phase: 21-gh-trade-mobile-app
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - webapp/src/lib/native/google-client-ids.ts
  - webapp/src/lib/native/__tests__/google-client-ids.test.ts
autonomous: false
requirements: [MOBILE-01]
user_setup:
  - service: google-cloud-oauth
    why: "D-03 네이티브 Google Sign-In — iOS·Android OAuth 클라이언트는 웹 클라이언트와 같은 GCP 프로젝트(`gh-radar`, 번호 1023658565518)에 있어야 한다"
    dashboard_config:
      - task: "iOS OAuth client ID 생성(Bundle ID = 21-01 확정 ID)"
        location: "console.cloud.google.com → APIs & Services → Credentials (project gh-radar)"
      - task: "Android OAuth client ID 생성(Package = 21-01 확정 ID · debug SHA-1)"
        location: "console.cloud.google.com → APIs & Services → Credentials (project gh-radar)"
  - service: supabase
    why: "signInWithIdToken 이 iOS/Android id_token 의 aud 를 받아들이도록 Authorized Client IDs 등록"
    dashboard_config:
      - task: "Google provider 「Client IDs」 = 웹,iOS,Android (웹 먼저, 쉼표 구분)"
        location: "Supabase Dashboard → Project gh-radar → Authentication → Sign In / Providers → Google"

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-03 선행 조건: GCP 프로젝트 `gh-radar` 에 iOS(번들 ID = 21-01 확정값)·Android(패키지 = 같은 값 · debug SHA-1) OAuth 클라이언트가 만들어졌고, Supabase Google provider 의 Authorized Client IDs 에 웹·iOS·Android 클라이언트 ID 가 웹 먼저 순서로 등록됐다"
    - "D-03 보안: Supabase 의 전역 「Skip nonce checks」는 꺼진 채다 — nonce 검증을 유지한다(RESEARCH Pitfall 6)"
    - "D-03: 세 클라이언트 ID 와 iOS URL scheme(역순 클라이언트 ID)이 공개 식별자 상수로 `webapp/src/lib/native/google-client-ids.ts` 에 있고, 형식·역순 관계가 단위 테스트로 잠겼다 — Vercel env 추가 없음"
  artifacts:
    - path: webapp/src/lib/native/google-client-ids.ts
      provides: "GOOGLE_WEB_CLIENT_ID · GOOGLE_IOS_CLIENT_ID · GOOGLE_ANDROID_CLIENT_ID · GOOGLE_IOS_URL_SCHEME"
      exports: ["GOOGLE_WEB_CLIENT_ID", "GOOGLE_IOS_CLIENT_ID", "GOOGLE_ANDROID_CLIENT_ID", "GOOGLE_IOS_URL_SCHEME"]
    - path: webapp/src/lib/native/__tests__/google-client-ids.test.ts
      provides: "ID 형식 · URL scheme 역순 관계 단언"
  key_links:
    - from: webapp/src/lib/native/google-client-ids.ts
      to: webapp/src/lib/native/native-google-login.ts
      via: "SocialLogin initialize { webClientId, iOSClientId, iOSServerClientId } (21-15)"
      pattern: "GOOGLE_WEB_CLIENT_ID"
---

<assumption_delta_decision>
- 검출기 `assumption-delta scan 21` → `detected: false`. 수동 판정: 두 번째 클라이언트 플랫폼 — 정본 명사 **웹앱**, 결정 **add-alongside**. 로그인 정본(Supabase 세션 쿠키)은 그대로이고 네이티브는 id_token 발급자만 더한다(21-01 블록 참조).
</assumption_delta_decision>

<objective>
D-03 의 사람 선행 작업을 앞당겨 끝낸다: debug SHA-1 을 뽑아 주고(자동) → 사용자가 GCP·Supabase 콘솔에서 OAuth 클라이언트 2개와 Authorized Client IDs 를 등록(사람) → 돌려받은 공개 식별자를 코드 상수로 기록(자동).

Purpose: 21-15 네이티브 로그인은 이 값 없이 시작할 수 없다. 콘솔 작업은 코드와 무관하게 먼저 끝낼 수 있으므로 Wave 2 에 둔다.
Output: 콘솔 설정 완료 + `google-client-ids.ts` 상수·테스트.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md
@.planning/phases/21-gh-trade-mobile-app/21-01-SUMMARY.md
@webapp/SETUP.md

기존 웹 OAuth 클라이언트는 GCP 프로젝트 **`gh-radar`**(번호 1023658565518, 동의 화면 앱 이름 「gh-radar」)에 있다(오케스트레이터 확인). 저장소에는 `apps.googleusercontent.com` 흔적이 없다 — 웹 클라이언트 ID 는 콘솔에서 읽어 와야 한다(비밀 아님 · Client Secret 은 읽지도 옮기지도 않는다).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Android debug 키스토어 SHA-1 추출(콘솔 입력값 준비)</name>
  <files>(파일 변경 없음 — 출력만)</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (§Security Domain · Assumptions A14)
    - .planning/phases/21-gh-trade-mobile-app/21-01-SUMMARY.md (확정 번들 ID)
  </read_first>
  <action>
    `~/.android/debug.keystore` 가 있으면 `keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android` 를 실행해 `SHA1:` 줄 값을 뽑는다. 없으면 Android 표준 debug 키스토어를 만든다: `keytool -genkeypair -v -keystore ~/.android/debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"` 후 다시 조회한다(Gradle 이 만드는 것과 같은 값 — 이후 `assembleDebug` 서명과 일치). SHA-1 과 확정 번들 ID 를 Task 2 체크포인트 본문에 그대로 붙인다. 키스토어 파일을 저장소 안으로 복사하지 않는다. release 키 SHA-1 은 범위 밖(스토어 서명 — Deferred)이라 적어 둔다.
  </action>
  <verify>
    <automated>keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android</automated>
    <fails_when>non-zero exit, or the output has no line containing "SHA1:"</fails_when>
  </verify>
  <acceptance_criteria>
    - 위 명령 출력에 `SHA1:` 줄이 있고 그 값이 `([0-9A-F]{2}:){19}[0-9A-F]{2}` 형식이다
    - `git status --porcelain -- '*.keystore' '*.jks'` 출력이 비어 있다
  </acceptance_criteria>
  <done>콘솔에 넣을 debug SHA-1 이 준비됐다.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: GCP iOS·Android OAuth 클라이언트 생성 + Supabase Authorized Client IDs 등록</name>
  <read_first>
    - webapp/SETUP.md (§1.1 Supabase Google provider · §2 GCP Credentials 절차 — 같은 화면 경로)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pattern 7 · Pitfall 6 · Security Domain · A14)
  </read_first>
  <action>GCP 프로젝트 `gh-radar` 에 iOS·Android OAuth 클라이언트를 만들고, Supabase Google provider 의 Authorized Client IDs 에 웹·iOS·Android 클라이언트 ID 를 등록한 뒤, 세 ID 를 알려 준다.</action>
  <instructions>
    Claude 가 이미 한 일: debug SHA-1 추출(Task 1 출력 — 이 체크포인트 본문에 붙어 있다) · 번들 ID 확정(21-01 SUMMARY).
    사람만 할 수 있는 일(콘솔 로그인 필요):
    1. https://console.cloud.google.com/apis/credentials?project=gh-radar 를 연다. 「OAuth 2.0 Client IDs」 목록의 기존 **Web application** 클라이언트(Supabase 에 넣어 둔 것)의 **Client ID** 를 복사한다 → `web`. (교차 확인: Supabase Dashboard → Project gh-radar → Authentication → Sign In / Providers → Google → Client ID 칸과 같은 값)
    2. 같은 화면 「+ CREATE CREDENTIALS」 → 「OAuth client ID」 → Application type **iOS** → Name `GH Trade iOS` → Bundle ID `com.ghtrade.app`(21-01 확정값) → Team ID 는 알면 입력(선택) → CREATE → 표시되는 **Client ID** 복사 → `ios`. 같은 화면의 「iOS URL scheme」(`com.googleusercontent.apps.…`)도 확인만 한다.
    3. 「+ CREATE CREDENTIALS」 → 「OAuth client ID」 → Application type **Android** → Name `GH Trade Android (debug)` → Package name `com.ghtrade.app` → SHA-1 certificate fingerprint = Task 1 값 → CREATE → **Client ID** 복사 → `android`.
    4. Supabase Dashboard → Project gh-radar → Authentication → Sign In / Providers → **Google** → **Client IDs**(Authorized Client IDs) 칸에 `<web>,<ios>,<android>` 를 **웹 먼저**, 쉼표로, 공백 없이 입력 → Save. Client Secret 칸은 건드리지 않는다. **「Skip nonce checks」 토글은 켜지 않는다**(켜면 모든 클라이언트의 nonce 검증이 꺼진다 — Pitfall 6).
    5. (선택) GCP → APIs & Services → OAuth consent screen → App name 을 `gh-radar` → `GH Trade` 로 바꿀지 결정한다(네이티브 계정 선택 화면에 이 이름이 보인다). 바꾸지 않아도 이 phase 는 진행된다.
  </instructions>
  <verification>실행기가 세 값이 모두 `^[0-9]+-[a-z0-9]+\.apps\.googleusercontent\.com$` 형식이고 서로 다른지 확인한다. 실제 토큰 수락 여부는 21-16 UAT(시뮬레이터 네이티브 로그인)에서 드러난다 — aud 누락이면 Supabase 가 즉시 오류를 돌려준다.</verification>
  <acceptance_criteria>
    - 재개 신호에 `web=` · `ios=` · `android=` 세 값과 `consent=renamed|kept` 가 있다
    - 세 값이 위 정규식에 맞고 서로 다르다
  </acceptance_criteria>
  <resume-signal>`web=<ID> ios=<ID> android=<ID> consent=renamed|kept skip-nonce=off` 로 답한다</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 공개 식별자 상수 `google-client-ids.ts` + 형식·역순 관계 테스트</name>
  <precondition>Task 2 재개 신호의 세 ID 가 있다</precondition>
  <files>webapp/src/lib/native/google-client-ids.ts, webapp/src/lib/native/__tests__/google-client-ids.test.ts</files>
  <read_first>
    - webapp/src/lib/native/native-detect.ts (21-01 — 같은 디렉터리 머리 주석 관례)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pattern 7 `initialize` 인자 · Project Constraints 「Vercel env paste trailing newline」 — 상수로 두는 이유)
  </read_first>
  <behavior>
    - 세 상수가 모두 `^[0-9]+-[a-z0-9]+\.apps\.googleusercontent\.com$` 에 맞는다
    - `GOOGLE_IOS_URL_SCHEME` 은 `GOOGLE_IOS_CLIENT_ID` 의 `.` 구분 토큰을 뒤집어 이은 값(`com.googleusercontent.apps.<번호-해시>`)과 같다
    - 세 값이 서로 다르다 · 앞뒤 공백·개행이 없다
  </behavior>
  <action>
    `webapp/src/lib/native/google-client-ids.ts` 에 `export const GOOGLE_WEB_CLIENT_ID = '<web>'` · `export const GOOGLE_IOS_CLIENT_ID = '<ios>'` · `export const GOOGLE_ANDROID_CLIENT_ID = '<android>'` · `export const GOOGLE_IOS_URL_SCHEME = '<ios 역순>'` 를 둔다(문자열 리터럴 — 런타임 계산 금지, 21-15 가 Info.plist 에 같은 값을 적는다). 파일 머리 한국어 주석: 공개 식별자(비밀 아님) · GCP 프로젝트 `gh-radar` · Supabase Authorized Client IDs 에 같은 세 값 · Android 는 Credential Manager id_token 의 aud 가 웹 ID 라 코드에서 직접 쓰지 않지만 기록용으로 둔다(RESEARCH A14) · Vercel env 로 옮기지 않는 이유(개행 오염 사고 이력). 테스트 `google-client-ids.test.ts` 를 먼저 쓰고(RED — 상수 파일 없음) 상수를 채워 GREEN.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/native/__tests__/google-client-ids.test.ts</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or "google-client-ids.test.ts" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "apps.googleusercontent.com" webapp/src/lib/native/google-client-ids.ts` 가 3줄 이상
    - `grep -n "com.googleusercontent.apps." webapp/src/lib/native/google-client-ids.ts` 가 1줄
    - `grep -ci "secret" webapp/src/lib/native/google-client-ids.ts` 가 주석의 「비밀 아님」 설명 외에 값으로 쓰이지 않는다(`GOOGLE_.*SECRET` 상수 0)
  </acceptance_criteria>
  <done>네이티브 로그인에 필요한 공개 식별자가 형식 검증과 함께 코드에 기록됐고, 콘솔 쪽 선행 조건이 모두 끝났다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 네이티브 Google 로그인 → Supabase Auth | Supabase 가 id_token 의 서명·iss·aud·nonce 를 검증한다 — 허용 aud 목록이 이 플랜에서 정해진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-08 | Spoofing | Authorized Client IDs(aud 혼동) | high | mitigate | 우리 GCP 프로젝트의 웹·iOS·Android 클라이언트 3개만 등록(Task 2 ④) — 다른 앱 id_token 거부 |
| T-21-09 | Spoofing | Supabase 전역 nonce 검사 해제 | high | mitigate | 「Skip nonce checks」 off 유지를 재개 신호(`skip-nonce=off`)로 확인 |
| T-21-14 | Information Disclosure | 키스토어·Client Secret 유출 | medium | mitigate | 키스토어는 `~/.android` 에만 · Client Secret 은 읽지도 옮기지도 않음 · 상수는 공개 ID 뿐 |
| T-21-27 | Denial of Service | release 서명 SHA-1 미등록 | low | accept | 스토어 서명은 범위 밖(Deferred) — release 빌드 로그인은 이후 phase 에서 SHA-1 추가 |
</threat_model>

<verification>
- keytool 출력에 SHA1 · 재개 신호 3 ID · `google-client-ids.test.ts` green
</verification>

<success_criteria>
- D-03 의 콘솔 선행 조건(OAuth 클라이언트 2개 + Authorized Client IDs)이 끝났고 코드가 그 값을 안다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/` 패키지 `@gh-radar/mobile` — scripts `native:sync` · `native:sync:dev` · `native:build:ios` · `native:build:android` · `native:smoke:ios` · `native:open:ios` · `native:open:android` · `native:assets` (21-01) · `native:smoke:android` (21-02) · `native:check-tab-routes:ios` (21-10) · `native:test:android` (21-12) · `native:verify-prod` (21-16) | 패키지·스크립트 | 21-01 · 21-02 · 21-10 · 21-12 · 21-16 |
| `mobile/capacitor.config.ts` — appId `com.ghtrade.app` · appName `GH Trade` · `CAP_SERVER_URL` · `SystemBars` (21-01) · `SocialLogin` (21-15) | 설정 | 21-01 · 21-15 |
| `mobile/tsconfig.json` · `mobile/.gitignore` | 설정 | 21-01 |
| `mobile/www/index.html` — 오프라인 폴백(최소 21-01 · `safeTarget`·5초 탐침·`online`·테마 21-11) | 정적 페이지 | 21-01 · 21-11 |
| `mobile/scripts/smoke-ios.sh` · `mobile/scripts/smoke-android.sh` — 「SMOKE OK ready platform=…」 | 스모크 | 21-01 · 21-02 |
| `mobile/scripts/check-tab-routes-ios.sh` · `mobile/scripts/tab-routes-check.swift` | Swift 경로표 검사 | 21-10 |
| `mobile/scripts/render-resources.mjs` · `mobile/resources/{icon.svg,icon-only.png,icon-foreground.png,icon-background.png,splash.png,splash-dark.png}` | 에셋 원본 | 21-14 |
| `mobile/scripts/verify-prod-config.mjs` · `mobile/README.md` | 운영 설정 검사·문서 | 21-16 |
| iOS `SceneDelegate.swift` (루트 = `GHTradeBridgeViewController()` · 창 배경 21-11) | Swift | 21-01 · 21-11 |
| iOS `GHTradeBridgeViewController` · `WeakScriptMessageHandler` · `NativeMessageType` | Swift | 21-01 · 21-10 · 21-11 |
| iOS `GHTradeTabBar` · `GHTradeTab` · `GHTabID` · `GHTradePalette` · `GHTradeTheme` · `TabRoutes.activeTab(forPath:)` · `TabRoutes.hidesTabBar(path:)` | Swift | 21-10 |
| iOS `NavigationDelegateProxy` · `ThemeStore` (UserDefaults `gh-trade.theme`) | Swift | 21-11 |
| Android `MainActivity` (`BridgeActivity`) · `GhTradeWebViewClient` (`BridgeWebViewClient` 상속) · `GhTradeBridge` (`@JavascriptInterface postMessage`) | Kotlin | 21-02 · 21-12 · 21-13 |
| Android `GhTradeTabBar` · `GhTradePalette` · `TabId` · `TabRoutes.activeTabFor` · `TabRoutes.hidesTabBar` · `TabRoutesTest` | Kotlin | 21-12 |
| Android `ThemeStore` (SharedPreferences `gh_trade`/`theme`) · `res/values/bools.xml` · `res/values-sw600dp/bools.xml` (`is_tablet`) | Kotlin·리소스 | 21-13 |
| Android `res/drawable/ic_tab_{home,home_fill,search,search_fill,trading,trading_fill,ai,ai_fill,me,me_fill}.xml` | 벡터 | 21-12 |
| 웹 `lib/native/native-detect.ts` — `NATIVE_DETECT_SCRIPT` · `isNativeApp` · `nativePlatform` | 유틸 | 21-01 |
| 웹 `lib/native/post-native.ts` — `postNative` · `NativeMsgType` | 유틸 | 21-04 |
| 웹 `lib/native/native-bridge-provider.tsx` — `NativeBridgeProvider` · `useNativeBridge` · `NativeRefreshFn` · `window.__ghTrade = { refresh, navigate, back }` | Provider | 21-04 |
| 웹 `lib/native/use-native-refresh.ts` — `useNativeRefresh` | 훅 | 21-04 |
| 웹 `lib/native/native-overlay-marker.tsx` — `NativeOverlayMarker` | 컴포넌트 | 21-04 |
| 웹 `lib/native/google-client-ids.ts` — `GOOGLE_WEB_CLIENT_ID` · `GOOGLE_IOS_CLIENT_ID` · `GOOGLE_ANDROID_CLIENT_ID` · `GOOGLE_IOS_URL_SCHEME` | 공개 식별자 상수 | 21-03 |
| 웹 `lib/native/native-google-login.ts` — `nativeGoogleSignIn` · `randomNonce` · `sha256Hex` · `classifyNativeLoginError` | 서비스 | 21-15 |
| 웹 `RelayConnectionState.probeNow` · `EMPTY_RELAY_VALUE.probeNow` | 훅 계약 | 21-07 |
| 웹 `lib/recent-search.ts` — `RECENT_SEARCH_KEY` · `RECENT_SEARCH_MAX` · `RecentSearchItem` · `readRecentSearches` · `pushRecentSearch` · `removeRecentSearch` · `clearRecentSearches` | 유틸 | 21-08 |
| 웹 `app/search/page.tsx` · `components/search/search-page-client.tsx` — `SearchPageClient` · 사이드바 `NAV_SEARCH_PAGE` | 페이지·상수 | 21-08 |
| 웹 `components/me/account-card.tsx` — `AccountCard` | 컴포넌트 | 21-09 |
| 웹 마크업 계약 `data-slot="app-aside"` · `data-slot="app-menu-button"` · `data-slot="stock-ai-button"` · `data-slot="shared-panels"` · `data-pinned` · `data-order-cta` · `data-slot="account-card"` | 마크업 | 21-05 · 21-06 · 21-09 |
| CSS `@custom-variant native` (21-04) · 앱 셸 규칙 (21-05) · `--app-safe-top` · `--app-safe-bottom` · `--app-safe-left` · `--app-safe-right` · `--native-tabbar-gap` · `--native-tabbar-offset` · `--native-body-reserve` (21-06) | 토큰·변형 | 21-04 · 21-05 · 21-06 |
| 웹 `app/icon.svg` (= 스케치 004 `#app-a`) | 파비콘 | 21-14 |
| 단위 테스트: `native-detect.test.ts` (21-01) · `post-native.test.ts` · `native-bridge-provider.test.tsx` · `native-overlay.test.tsx` (21-04) · `google-client-ids.test.ts` (21-03) · `relay-socket.test.ts` probeNow · `stock-native-refresh.test.tsx` (21-07) · `recent-search.test.ts` · `search-page-client.test.tsx` · `app-sidebar.test.tsx` 보강 (21-08) · `account-card.test.tsx` · `app-shell-chrome.test.tsx` 보강 (21-09) · `native-google-login.test.ts` · `login/__tests__/page.test.tsx` (21-15) · `TabRoutesTest.kt` (21-12) | 단위 | 각 플랜 |
| e2e: `fixtures/native-app.ts` — `installNativeApp` · `nativeMessages` (21-05) · `specs/native-shell.spec.ts` (21-05 · 21-06) · `specs/search-page.spec.ts` (21-08) · `specs/brand-account.spec.ts` (21-09) | Playwright | 각 플랜 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-03-SUMMARY.md` when done — 세 클라이언트 ID(공개), 동의 화면 이름 변경 여부, Skip nonce off 확인을 적는다.
</output>
