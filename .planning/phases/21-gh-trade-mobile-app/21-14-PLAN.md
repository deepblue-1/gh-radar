---
phase: 21-gh-trade-mobile-app
plan: 14
type: execute
wave: 3
depends_on: ["21-01", "21-02"]
files_modified:
  - mobile/resources/icon.svg
  - mobile/scripts/render-resources.mjs
  - mobile/resources/icon-only.png
  - mobile/resources/icon-foreground.png
  - mobile/resources/icon-background.png
  - mobile/resources/splash.png
  - mobile/resources/splash-dark.png
  - mobile/ios/App/App/Assets.xcassets/AppIcon.appiconset/Contents.json
  - mobile/ios/App/App/Assets.xcassets/Splash.imageset/Contents.json
  - mobile/android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
  - mobile/android/app/src/main/res/values/ic_launcher_background.xml
  - mobile/android/app/src/main/res/values/splash_colors.xml
  - mobile/android/app/src/main/res/values-night/splash_colors.xml
  - mobile/android/app/src/main/res/values/styles.xml
  - webapp/src/app/icon.svg
autonomous: true
requirements: [MOBILE-01]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-22 · D-27a: 앱 아이콘 원본 = 스케치 004 `#app-a` 다크 레이더(바탕 `#17171c` · 흰 동심원 r312/176 stroke 42 · 스윕 512,512→790,352 · 중심 46 · `#f04452` 0→55% 부채꼴) 를 독립 SVG `mobile/resources/icon.svg` 로 추출하고, 같은 SVG 로 웹 파비콘 `webapp/src/app/icon.svg` 를 교체한다"
    - "D-22: 원본 PNG 5종을 Playwright(웹앱 devDependency — 새 설치 0)로 렌더한다 — `icon-only` 1024(원본 그대로) · `icon-foreground` 1024(투명 바탕 · 레이더 그룹 중심 기준 0.88배 — 어댑티브 안전영역, Pitfall 19) · `icon-background` 1024(`#17171c` 단색) · `splash` 2732(라이트 `#ffffff` + 둥근 아이콘 + 「GH Trade」 워드마크 800 · `#191f28`) · `splash-dark` 2732(다크 `#17171c` + 워드마크 흰색) · 워드마크 폰트 = 저장소 Pretendard"
    - "D-22: `@capacitor/assets@3.0.5` 를 **devDependency 로 넣지 않고** npx 일회 실행으로 iOS `AppIcon`·`Splash`(다크 변형 포함) · Android mipmap/어댑티브/스플래시를 생성한다 — Android 12+ 시스템 스플래시 배경은 라이트 `#ffffff` / 다크(OS 야간) `#17171c`"
    - "D-05: `mobile/` 의존성 목록에 `@capacitor/assets`·`sharp` 가 추가되지 않는다(Vercel 워크스페이스 설치 오염 방지)"
  artifacts:
    - path: mobile/resources/icon.svg
      provides: "아이콘 원본 SVG(#app-a)"
      contains: "#17171c"
    - path: mobile/scripts/render-resources.mjs
      provides: "SVG+워드마크 → PNG 5종(Playwright)"
    - path: webapp/src/app/icon.svg
      provides: "웹 파비콘 = 앱 아이콘"
      contains: "#f04452"
  key_links:
    - from: mobile/scripts/render-resources.mjs
      to: mobile/resources/icon.svg
      via: "readFile → page.setContent(인라인 SVG) → screenshot"
      pattern: "icon.svg"
    - from: mobile/package.json
      to: mobile/scripts/render-resources.mjs
      via: "native:assets = node scripts/render-resources.mjs && npx --yes @capacitor/assets@3.0.5 generate …"
      pattern: "render-resources"
---

<assumption_delta_decision>
- 검출기 `assumption-delta scan 21` → `detected: false`. 수동 판정: 두 번째 클라이언트 플랫폼 — 정본 명사 **웹앱**, 결정 **add-alongside**. 아이콘 원본 하나(`icon.svg`)가 웹 파비콘과 두 앱 아이콘을 함께 낳는다(21-01 블록 참조).
</assumption_delta_decision>

<objective>
D-22 아이콘·스플래시를 만든다: 채택 시안(스케치 004 아이콘 A)을 원본 SVG 로 추출 → Playwright 로 PNG 5종 렌더 → `@capacitor/assets` 일회 실행으로 양 플랫폼 배포 → 웹 파비콘 교체.

Purpose: 홈 화면·스플래시·브라우저 탭에서 GH Trade 가 같은 레이더 아이콘으로 보인다.
Output: 원본 SVG·렌더 스크립트·PNG 5종 · iOS/Android 생성 에셋 · 파비콘.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md
@.planning/sketches/004-native-tab-bar/README.md
@mobile/package.json

<interfaces>
스케치 004 `index.html` L216-225(원본 — 그대로 옮긴다):

```svg
<symbol id="app-a" viewBox="0 0 1024 1024">
  <rect width="1024" height="1024" fill="#17171c"/>
  <defs><linearGradient id="swA" x1="0" y1="1" x2="1" y2="0"><stop offset="0" stop-color="#f04452" stop-opacity="0"/><stop offset="1" stop-color="#f04452" stop-opacity=".55"/></linearGradient></defs>
  <path d="M512 512 L512 200 A312 312 0 0 1 782 356 Z" fill="url(#swA)"/>
  <g fill="none" stroke="#ffffff" stroke-width="42" stroke-linecap="round">
    <circle cx="512" cy="512" r="312"/><circle cx="512" cy="512" r="176"/>
    <path d="M512 512 L790 352"/>
  </g>
  <circle cx="512" cy="512" r="46" fill="#ffffff"/>
</symbol>
```

```text
mobile/package.json scripts.native:assets = "node scripts/render-resources.mjs && npx --yes @capacitor/assets@3.0.5 generate --ios --android --assetPath resources"  (21-01)
Playwright: webapp/node_modules/@playwright/test (^1.59.1) · chromium 캐시 ~/Library/Caches/ms-playwright/chromium-1217
폰트: webapp/public/fonts/PretendardVariable.woff2 (lib/fonts.ts)
@capacitor/assets Custom mode 파일명: icon-only.png(≥1024²) · icon-foreground.png · icon-background.png · splash.png · splash-dark.png(≥2732²) — resources/ 기본 탐색
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 원본 `icon.svg` 추출 + `render-resources.mjs`(Playwright) → PNG 5종 + 웹 파비콘 교체 (D-22)</name>
  <files>mobile/resources/icon.svg, mobile/scripts/render-resources.mjs, mobile/resources/icon-only.png, mobile/resources/icon-foreground.png, mobile/resources/icon-background.png, mobile/resources/splash.png, mobile/resources/splash-dark.png, webapp/src/app/icon.svg</files>
  <read_first>
    - .planning/sketches/004-native-tab-bar/index.html (L216-225 `#app-a` · 스플래시 시안 마크업)
    - .planning/sketches/004-native-tab-bar/README.md (「결정」 아이콘 A · 스플래시)
    - webapp/src/app/icon.svg (현 파비콘 — 교체 대상)
    - webapp/src/lib/fonts.ts (Pretendard 경로)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Code Examples 「아이콘·스플래시 생성」 · Pitfall 19 · Alternatives 「원본 PNG 렌더」)
  </read_first>
  <action>
    ① `mobile/resources/icon.svg`: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1024 1024">` 안에 `<interfaces>` 의 symbol 본문을 그대로(`defs` 는 맨 앞으로). `webapp/src/app/icon.svg` 를 같은 내용으로 교체(Next.js 가 파비콘으로 서빙 — 파일명·위치 무변경).
    ② `mobile/scripts/render-resources.mjs`(ESM): `createRequire(new URL('../../webapp/package.json', import.meta.url))` 로 `@playwright/test` 의 `chromium` 을 로드(새 설치 0 — RESEARCH). 입력 `resources/icon.svg`, 폰트 `webapp/public/fonts/PretendardVariable.woff2` 를 base64 `data:font/woff2` 로 `@font-face { font-family: 'Pretendard' }` 에 인라인(`setContent` 문서는 파일 URL 폰트를 못 읽는다). 렌더(각각 `page.setViewportSize` → `page.setContent(html)` → `document.fonts.ready` 대기 → `page.screenshot({ path, omitBackground })`): `icon-only.png` 1024(원본 SVG 전면) · `icon-foreground.png` 1024(`rect` 없는 레이더 그룹을 `transform="translate(512 512) scale(0.88) translate(-512 -512)"` · `omitBackground: true` 투명) · `icon-background.png` 1024(`#17171c` 단색) · `splash.png` 2732(`#ffffff` 바탕 가운데 세로 배치: 아이콘 320px 모서리 반경 22.37% 로 잘라 표시 + 간격 40px + 「GH Trade」 `font: 800 96px Pretendard` 색 `#191f28`) · `splash-dark.png` 2732(`#17171c` 바탕 · 워드마크 `#ffffff`). 출력 경로는 `mobile/resources/`. 끝에 파일별 크기를 한 줄씩 출력하고 `RENDER OK 5` 를 찍는다. 스크립트는 `mobile/` 에서 실행된다고 가정하되 경로는 `import.meta.url` 기준으로 푼다.
    ③ `node mobile/scripts/render-resources.mjs` 를 실행해 PNG 5종을 만든다(커밋 대상 — `@capacitor/assets` 입력 원본).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && node mobile/scripts/render-resources.mjs</automated>
    <fails_when>non-zero exit, or output lacks "RENDER OK 5"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && sips -g pixelWidth -g pixelHeight mobile/resources/icon-only.png mobile/resources/icon-foreground.png mobile/resources/icon-background.png mobile/resources/splash.png mobile/resources/splash-dark.png</automated>
    <fails_when>non-zero exit, or any icon file reports a pixelWidth other than 1024, or either splash file reports a pixelWidth other than 2732</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'viewBox="0 0 1024 1024"' mobile/resources/icon.svg` 와 `grep -n "#f04452" webapp/src/app/icon.svg` 가 각각 1줄 이상
    - `cmp -s mobile/resources/icon.svg webapp/src/app/icon.svg` exit 0 (같은 원본)
    - `grep -n "createRequire" mobile/scripts/render-resources.mjs` 가 1줄 · `grep -n "scale(0.88)" mobile/scripts/render-resources.mjs` 가 1줄
    - `node -e "const p=require('./mobile/package.json');const all={...p.dependencies,...p.devDependencies};process.exit(('@capacitor/assets' in all)||('sharp' in all)||('@playwright/test' in all)?1:0)"` exit 0
  </acceptance_criteria>
  <done>채택 아이콘의 원본 SVG·PNG 5종이 저장소에 있고 웹 파비콘이 같은 아이콘으로 바뀌었다.</done>
</task>

<task type="auto">
  <name>Task 2: `@capacitor/assets` 일회 생성(iOS·Android) + Android 12 스플래시 배경 + 빌드 확인 (D-22)</name>
  <files>mobile/ios/App/App/Assets.xcassets/AppIcon.appiconset/Contents.json, mobile/ios/App/App/Assets.xcassets/Splash.imageset/Contents.json, mobile/android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml, mobile/android/app/src/main/res/values/ic_launcher_background.xml, mobile/android/app/src/main/res/values/splash_colors.xml, mobile/android/app/src/main/res/values-night/splash_colors.xml, mobile/android/app/src/main/res/values/styles.xml</files>
  <read_first>
    - mobile/package.json (`native:assets`)
    - mobile/android/app/src/main/res/values/styles.xml (템플릿 `AppTheme.NoActionBarLaunch` — `Theme.SplashScreen` 부모)
    - mobile/ios/App/App/Base.lproj/LaunchScreen.storyboard (템플릿 — `Splash` 이미지 참조 확인만)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Standard Stack `@capacitor/assets` 비고 · Pattern 9 스플래시 플러그인 미설치)
  </read_first>
  <action>
    ① `cd mobile && npx --yes @capacitor/assets@3.0.5 generate --ios --android --assetPath resources --iconBackgroundColor '#17171c' --iconBackgroundColorDark '#17171c' --splashBackgroundColor '#ffffff' --splashBackgroundColorDark '#17171c'` 를 실행한다(21-01 Task 2 에서 `[OK]` 판정 — devDependency 추가 금지, D-05 · Vercel 설치 오염 방지). 생성물: iOS `Assets.xcassets/AppIcon.appiconset/*` · `Splash.imageset/*`(다크 appearance 포함) · Android `mipmap-*/ic_launcher*` · `mipmap-anydpi-v26/ic_launcher.xml` · `drawable*/splash.png` · `values/ic_launcher_background.xml`.
    ② Android 12+ 시스템 스플래시: `res/values/splash_colors.xml` = `<color name="gh_splash_background">#FFFFFF</color>`, `res/values-night/splash_colors.xml` = `#17171C`, `res/values/styles.xml` 의 `AppTheme.NoActionBarLaunch` 에 `<item name="windowSplashScreenBackground">@color/gh_splash_background</item>` 를 더한다(템플릿의 나머지 항목·`postSplashScreenTheme` 유지). `@capacitor/splash-screen` 플러그인은 설치하지 않는다(Pattern 9 — 시스템 스플래시 + `BridgeActivity` 테마 전환으로 충분).
    ③ `pnpm --filter @gh-radar/mobile run native:build:ios` 와 `native:build:android` 로 에셋 카탈로그·리소스가 컴파일되는지 확인한다. iOS 앱 아이콘에 알파가 남으면 빌드 경고가 뜬다 — `@capacitor/assets` 가 `icon-only` 를 평탄화하므로 경고가 없어야 한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:ios</automated>
    <fails_when>non-zero exit, or the output lacks "BUILD SUCCEEDED"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:android</automated>
    <fails_when>non-zero exit, or the output lacks "BUILD SUCCESSFUL", or contains "AAPT: error"</fails_when>
  </verify>
  <acceptance_criteria>
    - `ls mobile/ios/App/App/Assets.xcassets/AppIcon.appiconset/*.png` 가 1개 이상 · `ls mobile/ios/App/App/Assets.xcassets/Splash.imageset/*.png` 가 1개 이상
    - `test -f mobile/android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml` exit 0
    - `grep -n "windowSplashScreenBackground" mobile/android/app/src/main/res/values/styles.xml` 가 1줄 · `grep -in "#17171C" mobile/android/app/src/main/res/values-night/splash_colors.xml` 가 1줄
    - `grep -c "@capacitor/splash-screen" mobile/package.json` 가 0
  </acceptance_criteria>
  <done>두 플랫폼 빌드에 GH Trade 아이콘·스플래시(라이트/다크)가 들어갔다(홈 화면·설정 29px 가독성은 21-16 UAT).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npx 일회 실행 `@capacitor/assets@3.0.5` | 레지스트리 코드를 로컬에서 실행한다(워크스페이스 의존성에는 남지 않음) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-SC | Tampering | `npx @capacitor/assets@3.0.5` | medium | mitigate | 정확 버전 고정 · 감사표 `[OK]`(3.0.5 · 2024-03 · 489K/wk · ionic-team) · 21-01 Task 2 에서 확인 · devDependency 미추가(acceptance) |
| T-21-41 | Tampering | 렌더 스크립트가 원격 리소스 로드 | low | mitigate | `setContent` 인라인 SVG·base64 폰트만 — 네트워크 요청 없음 |
</threat_model>

<verification>
- `render-resources.mjs` RENDER OK 5 · sips 크기 · 두 플랫폼 빌드 green
</verification>

<success_criteria>
- D-22 가 끝났다 — 앱 아이콘·스플래시·웹 파비콘이 채택 시안 하나에서 나온다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/` 패키지 `@gh-radar/mobile` — scripts `native:sync` · `native:sync:dev` · `native:build:ios` · `native:build:android` · `native:smoke:ios` · `native:open:ios` · `native:open:android` · `native:assets` (21-01) · `native:smoke:android` (21-02) · `native:check-tab-routes:ios` (21-10) · `native:test:android` (21-12) · `native:verify-prod` (21-16) | 패키지·스크립트 | 21-01 · 21-02 · 21-10 · 21-12 · 21-16 |
| `mobile/capacitor.config.ts` — appId `com.ghtrade.app` · appName `GH Trade` · `CAP_SERVER_URL` · `SystemBars` (21-01) · `SocialLogin` (21-15) | 설정 | 21-01 · 21-15 |
| `mobile/tsconfig.json` · `mobile/.gitignore` | 설정 | 21-01 |
| `mobile/www/index.html` — 오프라인 폴백(최소 21-01 · `safeTarget`·5초 탐침·`online`·테마 21-11) | 정적 페이지 | 21-01 · 21-11 |
| `mobile/scripts/smoke-ios.sh` · `mobile/scripts/smoke-android.sh` — 「SMOKE OK ready platform=…」 | 스모크 | 21-01 · 21-02 |
| `mobile/scripts/check-tab-routes-ios.sh` · `mobile/scripts/tab-routes-check.swift` | Swift 경로표 검사 | 21-10 |
| `mobile/scripts/render-resources.mjs` · `mobile/resources/{icon.svg,icon-only.png,icon-foreground.png,icon-background.png,splash.png,splash-dark.png}` | 에셋 원본 | 21-14 |
| `mobile/scripts/verify-prod-config.mjs` · `mobile/README.md` | 운영 설정 검사·문서 | 21-16 |
| iOS `SceneDelegate.swift` (루트 = `GHTradeBridgeViewController()` · 창 배경 21-11) | Swift | 21-01 · 21-11 |
| iOS `GHTradeBridgeViewController` · `WeakScriptMessageHandler` · `NativeMessageType` | Swift | 21-01 · 21-10 · 21-11 |
| iOS `GHTradeTabBar` · `GHTradeTab` · `GHTabID` · `GHTradePalette` · `GHTradeTheme` · `TabRoutes.activeTab(forPath:)` · `TabRoutes.hidesTabBar(path:)` | Swift | 21-10 |
| iOS `NavigationDelegateProxy` · `ThemeStore` (UserDefaults `gh-trade.theme`) | Swift | 21-11 |
| Android `MainActivity` (`BridgeActivity`) · `GhTradeWebViewClient` (`BridgeWebViewClient` 상속) · `GhTradeBridge` (`@JavascriptInterface postMessage`) | Kotlin | 21-02 · 21-12 · 21-13 |
| Android `GhTradeTabBar` · `GhTradePalette` · `TabId` · `TabRoutes.activeTabFor` · `TabRoutes.hidesTabBar` · `TabRoutesTest` | Kotlin | 21-12 |
| Android `ThemeStore` (SharedPreferences `gh_trade`/`theme`) · `res/values/bools.xml` · `res/values-sw600dp/bools.xml` (`is_tablet`) | Kotlin·리소스 | 21-13 |
| Android `res/drawable/ic_tab_{home,home_fill,search,search_fill,trading,trading_fill,ai,ai_fill,me,me_fill}.xml` | 벡터 | 21-12 |
| 웹 `lib/native/native-detect.ts` — `NATIVE_DETECT_SCRIPT` · `isNativeApp` · `nativePlatform` | 유틸 | 21-01 |
| 웹 `lib/native/post-native.ts` — `postNative` · `NativeMsgType` | 유틸 | 21-04 |
| 웹 `lib/native/native-bridge-provider.tsx` — `NativeBridgeProvider` · `useNativeBridge` · `NativeRefreshFn` · `window.__ghTrade = { refresh, navigate, back }` | Provider | 21-04 |
| 웹 `lib/native/use-native-refresh.ts` — `useNativeRefresh` | 훅 | 21-04 |
| 웹 `lib/native/native-overlay-marker.tsx` — `NativeOverlayMarker` | 컴포넌트 | 21-04 |
| 웹 `lib/native/google-client-ids.ts` — `GOOGLE_WEB_CLIENT_ID` · `GOOGLE_IOS_CLIENT_ID` · `GOOGLE_ANDROID_CLIENT_ID` · `GOOGLE_IOS_URL_SCHEME` | 공개 식별자 상수 | 21-03 |
| 웹 `lib/native/native-google-login.ts` — `nativeGoogleSignIn` · `randomNonce` · `sha256Hex` · `classifyNativeLoginError` | 서비스 | 21-15 |
| 웹 `RelayConnectionState.probeNow` · `EMPTY_RELAY_VALUE.probeNow` | 훅 계약 | 21-07 |
| 웹 `lib/recent-search.ts` — `RECENT_SEARCH_KEY` · `RECENT_SEARCH_MAX` · `RecentSearchItem` · `readRecentSearches` · `pushRecentSearch` · `removeRecentSearch` · `clearRecentSearches` | 유틸 | 21-08 |
| 웹 `app/search/page.tsx` · `components/search/search-page-client.tsx` — `SearchPageClient` · 사이드바 `NAV_SEARCH_PAGE` | 페이지·상수 | 21-08 |
| 웹 `components/me/account-card.tsx` — `AccountCard` | 컴포넌트 | 21-09 |
| 웹 마크업 계약 `data-slot="app-aside"` · `data-slot="app-menu-button"` · `data-slot="stock-ai-button"` · `data-slot="shared-panels"` · `data-pinned` · `data-order-cta` · `data-slot="account-card"` | 마크업 | 21-05 · 21-06 · 21-09 |
| CSS `@custom-variant native` (21-04) · 앱 셸 규칙 (21-05) · `--app-safe-top` · `--app-safe-bottom` · `--app-safe-left` · `--app-safe-right` · `--native-tabbar-gap` · `--native-tabbar-offset` · `--native-body-reserve` (21-06) | 토큰·변형 | 21-04 · 21-05 · 21-06 |
| 웹 `app/icon.svg` (= 스케치 004 `#app-a`) | 파비콘 | 21-14 |
| 단위 테스트: `native-detect.test.ts` (21-01) · `post-native.test.ts` · `native-bridge-provider.test.tsx` · `native-overlay.test.tsx` (21-04) · `google-client-ids.test.ts` (21-03) · `relay-socket.test.ts` probeNow · `stock-native-refresh.test.tsx` (21-07) · `recent-search.test.ts` · `search-page-client.test.tsx` · `app-sidebar.test.tsx` 보강 (21-08) · `account-card.test.tsx` · `app-shell-chrome.test.tsx` 보강 (21-09) · `native-google-login.test.ts` · `login/__tests__/page.test.tsx` (21-15) · `TabRoutesTest.kt` (21-12) | 단위 | 각 플랜 |
| e2e: `fixtures/native-app.ts` — `installNativeApp` · `nativeMessages` (21-05) · `specs/native-shell.spec.ts` (21-05 · 21-06) · `specs/search-page.spec.ts` (21-08) · `specs/brand-account.spec.ts` (21-09) | Playwright | 각 플랜 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-14-SUMMARY.md` when done
</output>
