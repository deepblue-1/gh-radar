---
phase: 21-gh-trade-mobile-app
plan: 24
type: execute
wave: 11
depends_on: ["21-23"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [MOBILE-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "사용자가 iPhone 17 · iPad Pro 11-inch (M5) · Android 에뮬레이터 · 데스크톱 브라우저에서 UAT 항목 1~5(G-21-1 · N1 · N2 · N3 · 회귀)를 직접 확인하고, 결과와 push 여부를 체크포인트에서 정했다"
    - "push(= Vercel 웹 프로덕션 배포)는 사용자가 「Claude가 push」 계열을 고른 경우에만 실행되고, 그때는 운영 반영(CSS 본문 여백 98)을 확인한 뒤 세 기기에 운영 빌드를 다시 설치해 네이티브 탭바 60 과 웹 여백 98 이 맞는다"
    - "UAT 가 끝나면 생성 설정이 운영값으로 복원되고(native:verify-prod → PROD CONFIG OK) UAT 프록시·adb reverse 가 정리된다"
    - "재검증 결과는 21-24-SUMMARY 에 남고 21-UAT.md 의 기존 갭 기록은 덮어쓰지 않는다(다음 단계 /gsd-verify-work 21 이 이어받는다)"
  artifacts:
    - path: .planning/phases/21-gh-trade-mobile-app/21-24-SUMMARY.md
      provides: "UAT 항목×기기 결과 · 재개 신호 · push 여부·커밋 범위 · 운영 반영 확인 · 재설치 상태 · 다음 단계"
  key_links:
    - from: .planning/phases/21-gh-trade-mobile-app/21-24-SUMMARY.md
      to: .planning/phases/21-gh-trade-mobile-app/21-UAT.md
      via: "UAT 항목 1~4 가 갭 G-21-1 · N1 · N2 · N3 의 truth 를 그대로 확인 — 결과는 /gsd-verify-work 21 이 UAT 에 반영"
      pattern: "G-21-"
    - from: .planning/phases/21-gh-trade-mobile-app/21-24-PLAN.md
      to: .planning/phases/21-gh-trade-mobile-app/21-23-SUMMARY.md
      via: "UAT 환경 표(UDID · 프록시 PID · dev 서버 기동 주체)를 체크포인트 안내와 마무리 정리에 쓴다"
      pattern: "UAT 환경"
  prohibitions:
    - "MUST NOT 사용자의 명시 선택 없이 master 를 push 한다(= 웹 프로덕션 배포)"
    - "MUST NOT 다른 세션의 로컬 커밋이 섞인 상태로 push 한다 — push 직전 git status -sb · origin/master..HEAD 목록을 확인한다"
    - "MUST NOT UAT 뒤 dev URL · cleartext 를 생성 설정에 남긴다"
    - "MUST NOT 21-UAT.md 의 기존 갭 기록을 덮어쓴다"
    - "MUST NOT 운영 반영이 늦을 때 사용자 확인 없이 수동 prebuilt 배포를 실행한다"
---

<objective>
Phase 21 갭 클로징 마감 2/2: 21-23 이 준비한 push 전 UAT 환경(dev 웹 + UAT 프록시 + 저장값 없는 dev 빌드 앱 세 기기)에서 사용자가 갭 네 건과 회귀를 확인하고, **같은 체크포인트에서 push(= Vercel 웹 프로덕션 배포) 여부를 정한다**. 이어서 운영 설정을 복원하고, push 가 선택됐으면 운영 반영을 확인한 뒤 세 기기에 운영 빌드를 다시 설치한다.

Purpose: 웹 여백 98 은 새 네이티브 탭바 60 과 같이 나가야 하고(옛 70 앱은 재설치 전까지 본문 여백이 10pt 좁다), 기본 다크·아이콘 규칙은 push 즉시 모든 브라우저 사용자에게 적용된다 — 사람이 먼저 보고 사람이 배포를 정한다(STATE 교훈 「push = 배포」 · 사용자 규칙 「배포는 사용자 결정」). 서버·relay 변경은 없으므로 백엔드 배포 순서 제약은 없다.
Output: UAT 결과 · push 결정과 그 이행 기록(21-24-SUMMARY). 코드 변경 없음.

검증 전용 플랜이라 트레이서 태스크가 없다(구현 플랜 21-17~21-22 가 각각 트레이서로 시작했다).

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add(절대 `git add -A` 금지 — 작업 트리에 다른 세션 변경이 있다) · 커밋·push 직전 `git status -sb`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-UAT.md
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-23-SUMMARY.md
@.planning/phases/21-gh-trade-mobile-app/21-17-SUMMARY.md
@.planning/sketches/007-tab-bar-no-label/README.md
@mobile/package.json
@mobile/scripts/smoke-ios.sh
@mobile/scripts/smoke-android.sh
@scripts/vercel-ignore-build.sh

<interfaces>
```text
UAT 환경(21-23-SUMMARY 「UAT 환경」 표): iPhone 17 · iPad Pro 11-inch (M5) UDID · Android 시리얼 · 설치 URL(dev) · 프록시 PID · dev 서버 기동 주체 · 21-17 선택(a/b/c)
mobile scripts: native:sync · native:build:ios · native:build:android · native:verify-prod
앱 번들 / APK = smoke-ios.sh 의 APP_PATH · smoke-android.sh 의 APK (빌드 산출물)
Vercel: push → scripts/vercel-ignore-build.sh 가 직전 배포~HEAD 의 webapp 변경을 보고 빌드 · 운영 도메인 https://trade.jx1.io
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: UAT 재검증(iPhone 17 · iPad Pro 11 · Android 에뮬 · 브라우저) + push(= 웹 프로덕션 배포) 결정</name>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-1 · N1 · N2 · N3 의 truth 와 decision 줄)
    - .planning/phases/21-gh-trade-mobile-app/21-23-SUMMARY.md (게이트 표 · UAT 환경 표)
    - .planning/sketches/007-tab-bar-no-label/index.html (채택안 C · 높이 60 — 비교 기준)
  </read_first>
  <decision>갭 네 건 UAT 결과와 push(= Vercel 웹 프로덕션 배포) 여부</decision>
  <context>
    실행기는 체크포인트를 띄우기 전에 21-23 UAT 환경이 살아 있는지 확인한다: `curl -sf http://localhost:3100/login` · `curl -sf http://127.0.0.1:8080/api/health` · 세 기기에 앱 실행 중. 죽어 있으면 21-23 Task 2 절차로 다시 띄우고 알린다.
    준비된 것: dev 웹 `http://localhost:3100` · UAT 프록시(운영 API 읽기) · 저장값 없는 dev 빌드 앱(iPhone 17 · iPad Pro 11-inch (M5) · Android 에뮬레이터). 로그인은 각 기기에서 「Google로 로그인」 한 번(네이티브 로그인). 각 항목에 기기별 통과 여부를 적어 답한다.
    1. 탭바(G-21-1 · D-27b) — 세 기기 · 다크와 라이트 둘 다: 아이콘 아래 글자 없음 · 아이콘이 세로 가운데 · 활성 탭 = 아이콘 뒤 가로 캡슐(파란 16%) · 테두리 없음 · 유리 느낌(iOS 는 뒤가 흐리게 비침, Android 는 거의 불투명 근사 — 수용 가능한지) · 넓고 옅은 그림자 · 높이 60(스케치 007 C 와 나란히) · 바닥 20(인셋 없는 기기 14) · iPad 폭 560 가운데 · 다크(기본)에서 유리 면과 아이콘이 또렷한지 · VoiceOver/TalkBack 이 「홈·검색·트레이딩·AI·마이」를 읽는지 · 종목상세 「주문하기」 버튼과 /trading 폰 하단 패널이 탭바 위에 서서 눌리고, 목록 맨 끝 항목이 탭바에 가리지 않는지 · Android 제스처 내비와 3버튼 내비 둘 다.
    2. 기본 다크(G-21-N1 · D-23a): 새로 설치한 앱 첫 실행 — 스플래시 다음 첫 화면부터 다크(흰 번쩍임 없음 · 상태바 밝은 아이콘; 스플래시 자체는 D-22 대로 OS 외형을 따른다) · /me 에서 라이트로 바꾸고 앱 완전 종료 → 재실행 → 라이트 유지 · 데스크톱 브라우저 새 시크릿 창에서 `http://localhost:3100/login` 첫 화면 다크 · 라이트로 바꾸고 새로고침 → 라이트 유지.
    3. 테마 아이콘(G-21-N2 · D-08b): 데스크톱 `/me` — 사이드바 아래 토글과 계정 카드 테마 버튼이 같은 아이콘(다크일 때 해 · 라이트일 때 달) · 누르면 그 테마로 바뀜 · 마우스를 올리면 「라이트 모드로 전환」/「다크 모드로 전환」 · 앱 `/me` 계정 카드도 같은 규칙.
    4. 인앱 브라우저(G-21-N3 · D-28): 앱 홈 뉴스 링크 → iOS·iPad 는 앱 위 Safari 시트(「닫기」 버튼), Android 는 Custom Tab — 외부 Safari/Chrome 앱으로 전환되지 않음(21-17 선택이 c 였다면 Android 는 외부 브라우저가 기대값) · 닫으면 홈 그대로(스크롤 위치·탭바) · 종목상세 뉴스 · 종토방 원문 · AI 챗 인용 링크도 같음 · 같은 사이트 링크(종목·테마 이동)는 앱 안에서 이동 · Android 뒤로가기로 Custom Tab 이 닫히고 앱으로 돌아옴.
    5. 회귀 한 바퀴: 탭 이동(새로고침 없이) · 당겨서 새로고침 · 시트 열면 탭바 숨김 · 비행기 모드 → 오프라인 폴백 화면(현재 테마) → 복구 시 자동 복귀.
    push 영향: push = Vercel 웹 프로덕션 배포. 기본 다크 · 아이콘 규칙 · 본문 하단 여백 98 이 모든 브라우저 사용자에게 즉시 나간다. 이미 설치된 옛 네이티브(탭바 70) 앱은 다시 설치하기 전까지 본문 여백이 10pt 좁다 — Claude 가 push 하면 Task 2 가 세 시뮬레이터/에뮬레이터에 운영 빌드를 다시 설치한다(실기기가 있다면 사용자가 재설치). 서버·relay 변경은 없다.
  </context>
  <options>
    <option id="pass-claude-push">
      <name>전 항목 통과 · Claude가 push</name>
      <pros>Task 2 가 git status -sb · origin/master..HEAD 확인 → push → 운영 반영(CSS 98) 확인 → 운영 빌드 재설치까지 한 번에 끝낸다.</pros>
      <cons>push 즉시 웹 사용자에게 변경이 나간다.</cons>
    </option>
    <option id="pass-self-push">
      <name>전 항목 통과 · 직접 push(또는 보류)</name>
      <pros>배포 시점을 사용자가 고른다. Claude 는 push 하지 않고 운영 sync 복원과 push 뒤 재설치 명령만 남긴다.</pros>
      <cons>push 전까지 세 기기에는 dev 빌드가 남는다(운영 앱은 사용자가 push 뒤 재설치).</cons>
    </option>
    <option id="push-then-recheck">
      <name>dev 에서 못 본 항목이 있음 · Claude가 push 후 실서버에서 마저 확인</name>
      <pros>21-16 UAT 2차처럼 실서버 앱으로 남은 항목을 본다.</pros>
      <cons>확인 전 배포다 — 남은 항목은 /gsd-verify-work 21 에서 받는다.</cons>
    </option>
    <option id="issues">
      <name>실패 항목 있음 · push 하지 않음</name>
      <pros>배포 없이 갭으로 넘긴다.</pros>
      <cons>항목 번호 · 기기 · 설명을 함께 적어야 한다.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - 재개 신호가 `pass-claude-push` · `pass-self-push` · `push-then-recheck` · `issues: <항목 번호·기기·설명>` 중 하나다
    - 항목 1~5 의 기기별 결과가 21-24-SUMMARY 에 사용자 답 그대로 기록된다
  </acceptance_criteria>
  <resume-signal>`pass-claude-push` / `pass-self-push` / `push-then-recheck` / `issues: 1 Android — <설명>` 중 하나로 답한다</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 마무리 — UAT 정리 · 운영 sync 복원 · (선택 시) push → 운영 반영 확인 → 운영 빌드 재설치 · SUMMARY</name>
  <files>(코드 변경 없음)</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-23-SUMMARY.md (UAT 환경 표 — 프록시 PID · dev 서버 기동 주체 · UDID)
    - scripts/vercel-ignore-build.sh (webapp 변경이 있으면 docs 가 끝 커밋이어도 빌드된다)
    - mobile/scripts/smoke-ios.sh · mobile/scripts/smoke-android.sh (앱 번들 / APK 경로)
  </read_first>
  <action>
    ① UAT 정리: 21-23 이 띄운 UAT 프록시를 끈다(PID). dev 서버는 21-23 이 띄운 경우에만 끈다. `adb reverse --remove-all`.
    ② `pnpm --filter @gh-radar/mobile run native:sync` → `native:verify-prod` → `PROD CONFIG OK`.
    ③ 재개 신호가 `pass-claude-push` 또는 `push-then-recheck` 일 때만:
      - `git status -sb` 와 `git log --oneline origin/master..HEAD` 를 보고, 앞선 커밋이 전부 이번 갭 클로징(21-17~21-24 와 그 docs) 또는 사용자가 이미 아는 Phase 21 커밋인지 확인한다. 모르는 커밋(다른 세션의 로컬 커밋)이 섞여 있으면 **push 하지 않고** 목록과 함께 멈춰 보고한다. 작업 트리의 미커밋 변경(다른 세션 파일)은 건드리지 않는다.
      - `git push origin master`.
      - 운영 반영 확인(최대 10분 · 30초 간격): `https://trade.jx1.io/login` HTML 에서 `/_next/static/css/…css` 링크를 뽑아 받아, 그중 하나에 `--native-body-reserve:98px` 가 있으면 반영 완료. 10분이 지나도 없으면 `vercel inspect https://trade.jx1.io` 결과와 함께 보고하고 멈춘다(수동 prebuilt 배포는 사용자 확인 뒤에만).
      - 운영 빌드 재설치: `native:build:ios`(운영 sync) → 21-23 표의 두 iOS UDID 에 smoke-ios.sh 의 APP_PATH 번들 `install` · `launch` → `native:build:android` → APK `adb install -r` · `am start`. 세 기기 설치본이 운영 URL + 새 네이티브(탭바 60)임을 SUMMARY 에 적는다. `push-then-recheck` 면 사용자가 남은 항목을 실서버 앱에서 확인하도록 목록을 SUMMARY 끝에 적는다.
      - 재설치 뒤 `native:verify-prod` 를 한 번 더(`PROD CONFIG OK`).
    ④ `pass-self-push` 면 push 하지 않고, 사용자가 push 한 뒤 실행할 재설치 명령(`native:sync` → `native:build:ios` · `native:build:android` → 설치)을 SUMMARY 에 적는다.
    ⑤ `issues` 면 push 하지 않고, 실패 항목(번호 · 기기 · 설명 · 관련 갭 id)을 SUMMARY 「남은 갭」 표로 남긴다 — 다음 단계는 `/gsd-verify-work 21`. 21-UAT.md 는 수정하지 않는다.
    ⑥ SUMMARY: UAT 항목×기기 결과 · 재개 신호 · push 여부와 커밋 범위(이전·이후 SHA) · 운영 반영 확인 시각 · 재설치 상태 · 다음 단계.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:verify-prod</automated>
    <fails_when>non-zero exit, or output lacks "PROD CONFIG OK", or contains "FAIL"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && ST="$(git status -sb)" && printf '%s\n' "$ST" | head -1</automated>
    <fails_when>after a push choice, the first line still contains "ahead"; after a no-push choice, the command exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - `native:verify-prod` → `PROD CONFIG OK`(UAT 뒤 운영 설정 복원)
    - 21-23 이 띄운 프록시가 더 이상 `127.0.0.1:8080` 을 듣지 않는다(`lsof -iTCP:8080 -sTCP:LISTEN` 에 그 PID 없음) · `adb reverse --list` 출력 0줄
    - push 선택 시: `git status -sb` 첫 줄에 `ahead` 없음 · SUMMARY 에 push 한 커밋 범위 · 운영 CSS 에서 `--native-body-reserve:98px` 확인 시각 · 세 기기 운영 빌드 재설치 기록
    - push 미선택 시: `git log --oneline origin/master..HEAD` 가 남아 있고 SUMMARY 에 사용자가 push 뒤 실행할 재설치 명령이 있다
    - `git diff --quiet -- .planning/phases/21-gh-trade-mobile-app/21-UAT.md` exit 0 (UAT 기록 미수정)
  </acceptance_criteria>
  <done>UAT 결과와 사용자 결정대로 마무리됐다 — 운영 설정 복원 · (선택 시) 웹 배포와 운영 앱 재설치 · 기록 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `git push` → Vercel 프로덕션 | 웹 변경이 전 사용자에게 즉시 배포된다 |
| 로컬 빌드 설정 → 기기 설치본 | UAT 뒤 dev URL · cleartext 가 남을 수 있다 |
| 로컬 git → 원격 master | 같은 작업 트리를 쓰는 다른 세션의 로컬 커밋이 섞일 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-28 | Denial of Service | 검증 전 웹 배포(push) | high | mitigate | push 는 blocking-human 체크포인트 선택으로만(Task 1) · 운영 반영 확인 후 운영 빌드 재설치로 네이티브 60 ↔ 웹 98 정합(Task 2 ③) |
| T-21-52 | Tampering | 다른 세션 로컬 커밋이 섞인 push | high | mitigate | push 직전 git status -sb · origin/master..HEAD 목록 확인 · 모르는 커밋이면 중단(Task 2 ③) · git add -A 금지 |
| T-21-01 | Information Disclosure | dev 설정 운영 유출(Pitfall 15) | high | mitigate | UAT 뒤 native:sync → verify-prod · 재설치 뒤 재검사(Task 2 ② ③) |
| T-21-55 | Information Disclosure | UAT 프록시 잔존 | medium | mitigate | 프록시 종료 · adb reverse 정리 확인(Task 2 ① · acceptance) |
</threat_model>

<verification>
- Task 1 사용자 결정 · Task 2 운영 설정 복원(PROD CONFIG OK)과 결정 이행
</verification>

<success_criteria>
- 갭 네 건이 사용자 UAT 로 확인됐고, 웹 배포 여부가 사용자 결정으로 처리됐다. 실패 항목이 있으면 배포 없이 갭으로 넘어갔다.
</success_criteria>

## Flagged assumptions

- **엣지 탐침 MOBILE-01 (unclassified · unresolved):** 엣지 탐침이 MOBILE-01(여러 표면을 가진 셸 요구사항)을 분류하지 못했다. 이번 갭 범위의 경계는 각 플랜이 명시 테스트로 다뤘고(21-18 테마 저장값 유무 · 21-17/21-22 URL 26케이스 · 21-20 탭바 기하 수치 · 21-20/21-21 대비비), 분류되지 않은 나머지 경계는 이 플랜 Task 1 UAT 에서 사람이 검토한다.
- 실기기에 옛 빌드(탭바 70)가 설치돼 있다면 push 뒤 사용자가 재설치해야 여백이 맞는다(체크포인트 context 에 고지).
- UAT 프록시·dev 서버는 21-23 이 `nohup` 으로 띄운 별도 프로세스라 실행기 교체(체크포인트 재개) 뒤에도 살아 있다고 가정한다 — 죽어 있으면 Task 1 전에 다시 띄운다.

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| Android `ExternalLinks.opensInAppBrowser(scheme, host, appHost)` · `ExternalLinksTest`(정본 26케이스) | Kotlin 순수 함수 · JUnit | 21-17 |
| Android `InAppBrowser.open(activity, uri, theme)` · `GhTradeWebViewClient.shouldOverrideUrlLoading` 가로채기 | Kotlin | 21-17 |
| `mobile/android/variables.gradle` `androidxBrowserVersion` · `app/build.gradle` androidx.browser 선언 (21-17 Task 1 선택 a 일 때만) | Gradle | 21-17 |
| `mobile/package.json` `native:test:android`(TabRoutesTest + ExternalLinksTest) · `native:check-external-links:ios` | 스크립트 | 21-17 · 21-22 |
| 웹 `ThemeProvider` 기본 다크 · iOS/Android `ThemeStore` 기본 다크 · `mobile/www/index.html` 기본 다크 | 설정 | 21-18 |
| e2e `webapp/e2e/specs/theme-default.spec.ts` (저장값 없음 다크 · 저장값 유지 · 앱 첫 theme 메시지 · 오프라인 폴백) | Playwright | 21-18 |
| 웹 `THEME_SWITCH_LABEL` · `ThemeSwitchIcon` (theme-toggle.tsx export) — 사이드바 ThemeToggle · AccountCard 공용 목적지 규칙 | 컴포넌트·상수 | 21-19 |
| 단위 `webapp/src/components/layout/__tests__/theme-toggle.test.tsx` | Vitest | 21-19 |
| iOS `GHTradeTabBar` 캡슐 C(라벨 없음 · 56×36 16% · 유리 · 두 겹 그림자 · 높이 60 · 페이드 86) · `GHTradePalette.glass` · `glassAlpha` | Swift | 21-20 |
| CSS `--native-tabbar-offset` = gap + 60 + 8 · `--native-body-reserve` 98 · native-shell e2e 수치(82 · 98 · 74) | 토큰 · Playwright | 21-20 |
| CONTEXT D-27b · D-23a · D-08b · D-28 | 결정 기록 | 21-20 |
| Android `GhTradeTabBar` 캡슐 C 근사(라벨 없음 · 불투명 유리 · elevation 12) · `GhTradePalette.glass` · 탭바 60dp · 페이드 86dp | Kotlin | 21-21 |
| iOS `ExternalLinks.opensInAppBrowser(scheme:host:appHost:)` · `isSameAppHost` · `NavigationDelegateProxy`(decidePolicyFor · WKUIDelegate createWebViewWith · SFSafariViewController) · `mobile/scripts/external-links-check.swift` · `check-external-links-ios.sh` | Swift · 스크립트 | 21-22 |
| 갭 클로징 전 게이트 기록 · push 전 UAT 환경 (21-23-SUMMARY) · UAT 재검증 결과 · push 결정과 이행 (21-24-SUMMARY) | 문서 | 21-23 · 21-24 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-24-SUMMARY.md` when done — UAT 항목×기기 결과 · 재개 신호 · push/반영/재설치 기록 · 다음 단계(`/gsd-verify-work 21`).
</output>
