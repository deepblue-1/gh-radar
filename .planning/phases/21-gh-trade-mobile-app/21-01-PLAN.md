---
phase: 21-gh-trade-mobile-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
  - mobile/package.json
  - mobile/tsconfig.json
  - mobile/capacitor.config.ts
  - mobile/.gitignore
  - mobile/www/index.html
  - mobile/scripts/smoke-ios.sh
  - mobile/ios/App/App.xcodeproj/project.pbxproj
  - mobile/ios/App/App/SceneDelegate.swift
  - mobile/ios/App/App/GHTradeBridgeViewController.swift
  - mobile/ios/App/App/Base.lproj/Main.storyboard
  - mobile/ios/App/App/Info.plist
  - mobile/ios/App/CapApp-SPM/Package.swift
  - webapp/src/lib/native/native-detect.ts
  - webapp/src/lib/native/__tests__/native-detect.test.ts
  - webapp/src/app/layout.tsx
autonomous: false
requirements: [MOBILE-01]
user_setup:
  - service: apple-developer
    why: "D-20 번들 ID `com.ghtrade.app` 선점 여부 확인(one-way 결정 전)"
    dashboard_config:
      - task: "Identifiers 에 App ID(Explicit) `com.ghtrade.app` 등록 가능 여부 확인"
        location: "developer.apple.com → Certificates, IDs & Profiles → Identifiers → +"

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-20: appId(기본 `com.ghtrade.app`, 사용자가 대체값을 주면 그 값)가 `cap add ios` 전에 checkpoint:decision 으로 확인됐고, 같은 값이 `mobile/capacitor.config.ts` appId 와 iOS 번들 ID(`PRODUCT_BUNDLE_IDENTIFIER`)에 들어갔다 · 표시명 appName = `GH Trade`"
    - "D-01: `capacitor.config.ts` 의 server.url 기본값이 `https://trade.jx1.io` 이고 webDir `www` 에는 오프라인 폴백 `index.html` 만 있다 — static export·로컬 번들 없음. dev 는 `CAP_SERVER_URL` env 로만 바뀐다"
    - "D-05: `mobile/` 이 pnpm workspace 패키지 `@gh-radar/mobile` 이고 build·typecheck·test·lint 스크립트가 없어 루트 `pnpm -r run build|typecheck|lint` 가 집어가지 않는다 · `scripts/vercel-ignore-build.sh` 는 수정하지 않는다(`mobile/` 변경은 웹 배포를 유발하지 않는다)"
    - "D-11: `<head>` 인라인 스크립트가 `window.Capacitor.isNativePlatform()` 이 참일 때만 `<html>` 에 `native-app` 클래스와 `data-native-platform=\"ios|android\"` 를 붙이고 `ready` 메시지를 네이티브 채널로 보낸다 — 브라우저에서는 DOM 변화 0 · 송신 0"
    - "트레이서: iOS 시뮬레이터에서 루트 VC 가 `GHTradeBridgeViewController`(Pitfall 1·3 — SceneDelegate 코드 교체)인 GH Trade 셸이 웹(dev :3100)을 로드하고, 웹의 `ready` 가 `ghTrade` 메시지 채널로 도착해 통합 로그에 `ready platform=ios nativeApp=true` 가 찍힌다"
    - "Pitfall 15: iOS 스모크는 dev URL 로 sync 한 뒤 종료 시(성공·실패 무관) 운영 URL 로 다시 sync 한다"
    - "V14: `capacitor.config.ts` 에 내비게이션 허용 목록·오류 경로·웹 디버깅·네이티브 HTTP/쿠키 가로채기 키가 없다 — Capacitor 기본값(앱 호스트만 내비 허용 · 릴리스 디버깅 off)을 그대로 쓴다"
  artifacts:
    - path: mobile/package.json
      provides: "@gh-radar/mobile 패키지 · native:* 스크립트 · Capacitor 8.5.2 exact"
      contains: "@gh-radar/mobile"
    - path: mobile/capacitor.config.ts
      provides: "Remote-URL 셸 설정(appId · appName · server.url · SystemBars)"
      contains: "trade.jx1.io"
    - path: mobile/ios/App/App/GHTradeBridgeViewController.swift
      provides: "CAPBridgeViewController 서브클래스 · ghTrade 메시지 수신(ready 로그)"
      contains: "class GHTradeBridgeViewController"
    - path: mobile/ios/App/App/SceneDelegate.swift
      provides: "루트 VC = GHTradeBridgeViewController()"
      contains: "GHTradeBridgeViewController()"
    - path: webapp/src/lib/native/native-detect.ts
      provides: "NATIVE_DETECT_SCRIPT · isNativeApp · nativePlatform"
      exports: ["NATIVE_DETECT_SCRIPT", "isNativeApp", "nativePlatform"]
    - path: mobile/scripts/smoke-ios.sh
      provides: "dev sync → 빌드 → 시뮬 설치·실행 → ready 로그 확인 → 운영 sync 복원"
  key_links:
    - from: webapp/src/app/layout.tsx
      to: webapp/src/lib/native/native-detect.ts
      via: "<head><script dangerouslySetInnerHTML={{ __html: NATIVE_DETECT_SCRIPT }} /></head>"
      pattern: "NATIVE_DETECT_SCRIPT"
    - from: webapp/src/lib/native/native-detect.ts
      to: mobile/ios/App/App/GHTradeBridgeViewController.swift
      via: "window.webkit.messageHandlers.ghTrade.postMessage(JSON {type:'ready'})"
      pattern: "messageHandlers"
    - from: mobile/ios/App/App/SceneDelegate.swift
      to: mobile/ios/App/App/GHTradeBridgeViewController.swift
      via: "window?.rootViewController = GHTradeBridgeViewController()"
      pattern: "GHTradeBridgeViewController\\(\\)"
---

<assumption_delta_decision>
- 검출기: `gsd-tools query assumption-delta scan 21 --json` → `{"detected": false, "signals": []}` (2026-09-25 실행). 검출기는 발화하지 않았지만 이 phase 는 웹에 **두 번째 클라이언트 플랫폼(네이티브 셸)** 을 붙이므로 수동으로 검토해 기록한다.
- 정본 명사: **웹앱(`https://trade.jx1.io`, Next.js)** — 화면·라우트·세션(쿠키)·relay 계약의 유일한 정본. 네이티브 셸은 그 웹의 호스트(클라이언트)다.
- 결정: **add-alongside** — 웹의 식별/세션/라우팅 모델을 일반화(promote)하지 않고, 웹 쪽 `html.native-app` 분기 + `postNative`/`window.__ghTrade` 브리지로 앱 셸을 곁에 붙인다(D-01 Remote-URL 이 이 모양을 전제한다).
- 수용한 부채: 앱 전용 분기(네이티브 로그인 · 탭바 여백 · 오버레이 신호)가 웹 코드 안에 산다. promote 를 강제할 신호: 번들/오프라인 우선 방식 전환(D-01 costly), 또는 웹에 없는 앱 전용 기능(푸시·딥링크 — Deferred)이 들어올 때.
- 불변식 테스트: `webapp/e2e/specs/native-shell.spec.ts`(21-05·21-06)가 같은 라우트를 브라우저 모드와 앱 모드 양쪽에서 렌더해 셸 차이만 단언한다.
</assumption_delta_decision>

<objective>
**트레이서(Phase 21 첫 슬라이스).** 두 개의 게이트(D-20 번들 ID one-way 결정 · 패키지 정당성)를 통과한 뒤, 모노레포 `mobile/` 패키지 → Capacitor 8.5.2 Remote-URL 설정 → `cap add ios` → SceneDelegate 가 루트로 세우는 `GHTradeBridgeViewController` → WebView 가 웹(dev :3100)을 로드 → 웹 `<head>` 인라인 스크립트가 Capacitor 를 감지해 `html.native-app` 을 붙이고 `ready` 를 `ghTrade` 채널로 송신 → 네이티브가 통합 로그에 남기는 **한 줄 경로**를 끝까지 잇는다.

Purpose: 가장 위험한 가정 4개(Xcode 27 UIScene 필수 · SceneDelegate 코드 교체 · Remote-URL 원격 페이지에 native-bridge 주입 · 웹→네이티브 메시지 채널)를 첫 커밋에서 증명한다. 이 트레이서가 실패하면 탭바·새로고침·로그인은 전부 재설계 대상이다.
Output: `mobile/` 패키지 + iOS 프로젝트 + 웹 감지 스크립트 + 시뮬레이터 스모크 스크립트.

스키마 게이트: 이 phase 범위(ROADMAP·CONTEXT·RESEARCH)에서 `supabase/migrations` 등 스키마 파일이 검출되지 않았다 — DB push 태스크 없음.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md
@.planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md
@webapp/src/app/layout.tsx
@pnpm-workspace.yaml
@package.json

작업 루트는 메인 체크아웃 `/Users/alex/repos/gh-radar` 다(worktree 아님). 웹 단일 파일 테스트는 `pnpm --filter @gh-radar/webapp exec vitest --run <경로>` 로 돌린다. webapp dev 서버는 `PORT=3100 pnpm --filter @gh-radar/webapp run dev` 로 백그라운드 기동한다 — **`dev.sh` 는 쓰지 않는다**(다른 세션의 3100·8080·8090 포트를 kill 한다). iOS 빌드는 SPM 첫 해석에 수 분이 걸린다 — Bash timeout 600000 으로 돌린다.

<interfaces>
기존 계약(코드에서 추출):

```tsx
// webapp/src/app/layout.tsx — 현재 루트 구조(이 순서를 유지하고 <head> 만 더한다)
<html lang="ko" suppressHydrationWarning className={`${pretendard.variable} ${geistMono.variable}`}>
  <body>
    <ThemeProvider><AuthProvider><RelayProvider><ChatProvider>…</ChatProvider></RelayProvider></AuthProvider></ThemeProvider>
  </body>
</html>
// export const viewport: Viewport = { width:'device-width', initialScale:1, maximumScale:1, userScalable:false, themeColor:[…] }
```

```text
root package.json scripts: "typecheck": "pnpm --filter @gh-radar/shared build && pnpm -r run typecheck",
                           "build": "pnpm --filter @gh-radar/shared build && pnpm -r run build", "lint": "pnpm -r run lint"
pnpm-workspace.yaml packages: webapp · server · relay · workers/* · packages/*  (+ allowBuilds / onlyBuiltDependencies 두 키 동시 유지)
```

이 플랜이 새로 만드는 계약(후속 플랜이 이 이름으로 소비한다):

```ts
// webapp/src/lib/native/native-detect.ts
export const NATIVE_DETECT_SCRIPT: string;          // <head> 인라인 IIFE (의존성 0)
export function isNativeApp(): boolean;             // documentElement.classList.contains('native-app') · SSR false
export function nativePlatform(): 'ios' | 'android' | null; // data-native-platform
// 메시지 형식(모든 플랜 공통): JSON 문자열 {"type": NativeMsgType, "payload": …}
// NativeMsgType = 'ready' | 'route' | 'theme' | 'overlay' | 'pull'   (21-04 가 post-native.ts 로 타입 export)
// ready payload = { platform: 'ios'|'android', nativeApp: true, path: string }
```

```swift
// mobile/ios/App/App/GHTradeBridgeViewController.swift
final class GHTradeBridgeViewController: CAPBridgeViewController, WKScriptMessageHandler
enum NativeMessageType: String { case ready, route, theme, overlay, pull }
// 메시지 핸들러 이름 "ghTrade" · os Logger(subsystem: "com.ghtrade.app", category: "bridge")
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: D-20 번들 ID(one-way) 확정 — `com.ghtrade.app` 등록 가능 여부 확인</name>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md (D-20 · D-01 · D-05)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Open Questions 7 · Pitfall 1)
  </read_first>
  <decision>iOS 번들 ID = Android applicationId 를 무엇으로 고정할 것인가 (다음 태스크의 `cap add ios` 가 이 값을 Xcode 프로젝트에 굽는다)</decision>
  <context>
    D-20 은 `com.ghtrade.app` 을 정했지만 Apple 전역 번들 ID 선점 여부와 사용자 팀(weekly-wine 은 팀 `954QPCS3F5`)에서의 등록 가능 여부는 저장소 밖 사실이다. 되돌리는 비용: `cap add` 뒤에 바꾸면 iOS 프로젝트·Android 패키지 디렉터리(`com/ghtrade/app`)·GCP OAuth 클라이언트 2개(21-03)·Supabase Authorized Client IDs 를 모두 다시 만들어야 하고, 스토어 게시 뒤에는 **변경 불가**다.
    확인 방법(사용자): https://developer.apple.com/account/resources/identifiers/list → 「+」 → App IDs → App → Description `GH Trade` · Bundle ID **Explicit** `com.ghtrade.app` 입력 → Continue 시 「not available」 오류가 없으면 등록 가능(Register 까지 눌러 선점해 두는 것을 권장). 실기기 설치에 쓸 팀 ID 도 함께 알려 준다.
  </context>
  <options>
    <option id="confirm">
      <name>`com.ghtrade.app` 확정</name>
      <pros>CONTEXT D-20 그대로 · 후속 플랜의 문자열 수정 0</pros>
      <cons>Apple 등록을 미루면 나중에 선점될 위험이 남는다</cons>
    </option>
    <option id="alternate">
      <name>대체 ID 사용(예: `kr.jx1.ghtrade`)</name>
      <pros>선점·정책 충돌 회피</pros>
      <cons>Task 3 와 21-02 · 21-03 · 21-15 의 `com.ghtrade.app` 문자열을 전부 새 값으로 바꿔 실행해야 한다(실행기는 이 플랜 SUMMARY 에 확정값을 기록하고 후속 플랜은 그 값을 쓴다)</cons>
    </option>
  </options>
  <acceptance_criteria>
    - 재개 신호가 `confirm` 또는 `alternate: <reverse-DNS ID>` 형태이고, 확정 ID 와 (있으면) 팀 ID 가 이 플랜 SUMMARY 의 「결정」 절에 기록된다
  </acceptance_criteria>
  <resume-signal>`confirm 팀=<TEAM_ID>` 또는 `alternate: <번들ID> 팀=<TEAM_ID>` 로 답한다</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: 패키지 정당성 게이트 — Capacitor 8.5.2 · capgo social-login 8.5.11 ([SUS] too-new)</name>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (§Package Legitimacy Audit · §Standard Stack)
  </read_first>
  <what-built>
    아직 아무것도 설치하지 않았다. RESEARCH 감사표에서 `@capacitor/core` · `@capacitor/cli` · `@capacitor/ios` · `@capacitor/android` 8.5.2(게시 14일)와 `@capgo/capacitor-social-login` 8.5.11(게시 2일)이 `[SUS] too-new` 로 표시됐다. Task 3 가 앞의 네 개를, 21-15 가 capgo 를 설치한다. `@capacitor/assets` 3.0.5 는 `[OK]`(21-14 가 npx 일회 실행). 실행기는 이 체크포인트를 띄우기 전에 `npm view @capacitor/core@8.5.2 repository.url maintainers time --json` 과 같은 명령을 다섯 패키지에 돌려 출력(저장소 URL · 메인테이너 · 게시 시각)을 그대로 붙여 보인다.
  </what-built>
  <how-to-verify>
    1. https://www.npmjs.com/package/@capacitor/core 등 네 개 — 저장소가 `github.com/ionic-team/capacitor`, 게시자가 ionic 팀 계정, 8.5.2 게시일 2026-09-11 인지 본다.
    2. https://www.npmjs.com/package/@capgo/capacitor-social-login — 저장소 `github.com/Cap-go/capacitor-social-login`, 8.5.11 게시일 2026-09-23, 주간 다운로드 10만대인지 본다.
    3. 다섯 패키지 모두 `postinstall`/`install` 스크립트가 없다는 실행기 출력(`npm view <pkg>@<ver> scripts`)을 확인한다.
    4. 의심스러우면 버전을 내린다(예: Capacitor `latest-8.4` = 8.4.3 — 단 SceneDelegate 템플릿 재확인 필요, RESEARCH 감사표 비고).
  </how-to-verify>
  <acceptance_criteria>
    - 재개 신호가 `approved` 이거나 `downgrade: <pkg>@<ver>, …` 이고, 승인된 정확한 버전 목록이 SUMMARY 에 기록된다
    - 이 체크포인트는 auto-advance 로 넘기지 않는다(blocking-human)
  </acceptance_criteria>
  <resume-signal>`approved` 또는 `downgrade: @capacitor/core@8.4.3, …` 로 답한다</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 3 (tracer): `mobile/` → cap add ios → GHTradeBridgeViewController → 웹 감지 → `ready` 가 네이티브 로그에 도착 (end-to-end)</name>
  <reversibility rating="one-way">D-20 appId 가 `cap add ios` 로 Xcode 프로젝트에 구워진다 — 스토어 게시 뒤 번들 ID 변경 불가, 게시 전이라도 OAuth 클라이언트·패키지 디렉터리 재생성이 필요하다.</reversibility>
  <precondition>Task 1 확정 ID · Task 2 `approved` 가 이 플랜 SUMMARY 초안에 있다 · `xcrun simctl list devices available` 출력에 `iPhone 17` 이 있다(없으면 목록의 첫 iPhone 이름을 `DEVICE` env 로 쓴다)</precondition>
  <files>pnpm-workspace.yaml, pnpm-lock.yaml, mobile/package.json, mobile/tsconfig.json, mobile/capacitor.config.ts, mobile/.gitignore, mobile/www/index.html, mobile/scripts/smoke-ios.sh, mobile/ios/App/App.xcodeproj/project.pbxproj, mobile/ios/App/App/SceneDelegate.swift, mobile/ios/App/App/GHTradeBridgeViewController.swift, mobile/ios/App/App/Base.lproj/Main.storyboard, mobile/ios/App/App/Info.plist, mobile/ios/App/CapApp-SPM/Package.swift, webapp/src/lib/native/native-detect.ts, webapp/src/lib/native/__tests__/native-detect.test.ts, webapp/src/app/layout.tsx</files>
  <read_first>
    - webapp/src/app/layout.tsx (전체 — `<html suppressHydrationWarning>` · Provider 중첩 순서)
    - pnpm-workspace.yaml · package.json (루트 `pnpm -r` 스크립트 · allowBuilds 두 키)
    - scripts/vercel-ignore-build.sh (L22 diff 경로 — 수정하지 않음을 확인)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pattern 1 · Pattern 2 · Pattern 4 · Pattern 9 · Pitfall 1·3·15·16·17 · Validation Architecture iOS build command)
    - .planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md (`mobile/capacitor.config.ts` · `mobile/package.json` · `native-detect.ts` 절)
    - /Users/alex/repos/weekly-wine-app/capacitor.config.ts · /Users/alex/repos/weekly-wine-app/package.json (원형 — 그대로 복사하지 않는다)
    - /Users/alex/repos/weekly-wine-app/ios/App/App/CookieViewController.swift (L5 클래스 선언 · L75-90 `capacitorDidLoad` · L192-215 메시지 핸들러 모양)
    - /Users/alex/repos/weekly-wine-app/ios/.gitignore · /Users/alex/repos/weekly-wine-app/android/.gitignore (무시 목록 원형)
  </read_first>
  <behavior>
    - `native-detect.test.ts`: `window.Capacitor` 없음 → `<html>` 클래스·`data-native-platform` 변화 0, 어떤 postMessage 도 호출되지 않음
    - `Capacitor = { isNativePlatform: () => true, getPlatform: () => 'ios' }` + `webkit.messageHandlers.ghTrade.postMessage` 스파이 → 클래스 `native-app` · `data-native-platform="ios"` · postMessage 1회, 인자는 문자열이고 `JSON.parse` 결과가 `{ type: 'ready', payload: { platform: 'ios', nativeApp: true, path: '/' } }`
    - `getPlatform: () => 'android'` + `window.GhTradeBridge.postMessage` 스파이(웹킷 채널 없음) → GhTradeBridge 로 1회
    - `isNativePlatform: () => false` → 변화 0 · `isNativePlatform` 이 throw → 스크립트가 throw 하지 않음
    - `isNativeApp()` 은 클래스 유무를 그대로, `nativePlatform()` 은 속성값(없으면 null)을 돌려준다
  </behavior>
  <action>
    ① `pnpm-workspace.yaml` 의 `packages:` 끝(`- packages/*` 다음)에 `- mobile` 한 줄을 더한다. `allowBuilds`/`onlyBuiltDependencies` 는 설치가 `ERR_PNPM_IGNORED_BUILDS` 로 멈출 때만 두 키에 **같이** 더한다(파일 주석 규약).
    ② `mobile/package.json` 을 만든다: `name` `@gh-radar/mobile` · `version` `0.0.0` · `private` true · `description` 「GH Trade 네이티브 셸(Capacitor Remote-URL)」. scripts 는 이 이름만 둔다(D-05 — 루트 `pnpm -r run build|typecheck|lint` 가 집어가지 않도록 `build`·`typecheck`·`test`·`lint` 키 금지): `native:sync` = `cap sync` · `native:sync:dev` = `CAP_SERVER_URL=http://localhost:3100 cap sync` · `native:build:ios` = `cap sync ios && xcodebuild -project ios/App/App.xcodeproj -scheme App -configuration Debug -destination 'generic/platform=iOS Simulator' -derivedDataPath ios/DerivedData CODE_SIGNING_ALLOWED=NO build` · `native:build:android` = `cap sync android && cd android && ./gradlew assembleDebug` · `native:smoke:ios` = `bash scripts/smoke-ios.sh` · `native:open:ios` = `cap open ios` · `native:open:android` = `cap open android` · `native:assets` = `node scripts/render-resources.mjs && npx --yes @capacitor/assets@3.0.5 generate --ios --android --assetPath resources` (스크립트 파일은 21-14 가 만든다). 의존성은 Task 2 승인 버전으로 **정확 고정**: `pnpm --filter @gh-radar/mobile add --save-exact @capacitor/core@8.5.2 @capacitor/ios@8.5.2 @capacitor/android@8.5.2` 와 `pnpm --filter @gh-radar/mobile add -D --save-exact @capacitor/cli@8.5.2` 그리고 `pnpm --filter @gh-radar/mobile add -D typescript@^5.0.0`(CLI 가 `capacitor.config.ts` 를 읽으려면 pnpm 격리 레이아웃에서 이 패키지에 직접 있어야 한다). `@capacitor/app` · `@capacitor/splash-screen` · `@capacitor/status-bar` 는 설치하지 않는다(RESEARCH Pattern 9 · Pitfall 12). 이 플랜의 lockfile 변경은 이 한 번뿐이다.
    ③ `mobile/tsconfig.json`: `compilerOptions` = `target ES2022` · `module ESNext` · `moduleResolution Bundler` · `strict true` · `skipLibCheck true` · `noEmit true`, `include` = `["capacitor.config.ts"]`.
    ④ `mobile/capacitor.config.ts` (RESEARCH Pattern 9 — SocialLogin 은 21-15 가 더한다): `import type { CapacitorConfig } from '@capacitor/cli'` · `const serverUrl = process.env.CAP_SERVER_URL ?? 'https://trade.jx1.io'` · `appId: '<Task 1 확정값, 기본 com.ghtrade.app>'` · `appName: 'GH Trade'` · `webDir: 'www'` · `server: { url: serverUrl, cleartext: serverUrl.startsWith('http://') }` · `ios: { contentInset: 'never', allowsLinkPreview: false, scrollEnabled: true }` · `android: { allowMixedContent: false }` · `plugins: { SystemBars: { insetsHandling: 'css', initialViewportFitValueHint: 'cover' } }` · `export default config`. 다음 네 종류의 키는 **두지 않는다**(파일 주석에도 그 키 이름을 적지 않는다 — 검증 grep 이 주석까지 센다): 내비게이션 허용 목록(앱 호스트는 Capacitor 가 자동 허용, API·relay 는 내비가 아니라 fetch/WebSocket) · 오류 페이지 경로(D-19 — iOS -999·Android HTTP 4xx 오탐, 네이티브가 직접 처리) · 웹 콘텐츠 디버깅 플래그(릴리스 기본 off 유지) · 네이티브 HTTP/쿠키 가로채기 플러그인(`/chat` SSE 보호). 파일 머리 한국어 주석: D-01 Remote-URL · `CAP_SERVER_URL` 은 dev 전용이며 dev sync 뒤 기기 빌드 전에 반드시 `native:sync` 로 되돌린다(Pitfall 15).
    ⑤ `mobile/www/index.html` — iOS 는 webDir 에 `index.html` 이 없으면 로드 자체가 치명 오류다(RESEARCH Pattern 5). 이 태스크는 최소 오프라인 화면만: `lang="ko"` · viewport meta(`viewport-fit=cover`) · 가운데 「GH Trade」 워드마크(800) · 「인터넷 연결을 확인해주세요」 · 「복구되면 자동으로 이동합니다」 · 「다시 시도」 버튼(`location.href='https://trade.jx1.io/'`) · 라이트 `#ffffff` 바탕. 자동 복구·`?to=`·테마는 21-11 이 완성한다.
    ⑥ `pnpm --filter @gh-radar/mobile exec cap add ios` 를 실행한다. 생성 확인: `mobile/ios/App/App.xcodeproj` · `mobile/ios/App/App/SceneDelegate.swift` · `mobile/ios/App/CapApp-SPM/Package.swift` · `mobile/ios/.gitignore`. `mobile/.gitignore` 를 만든다: `node_modules/` · `.smoke/` · `ios/DerivedData/` · `ios/App/App/public/` · `ios/App/App/capacitor.config.json` · `ios/App/App/config.xml` · `ios/capacitor-cordova-ios-plugins/` · `ios/App/App.xcodeproj/xcuserdata/` · `ios/App/App.xcodeproj/project.xcworkspace/xcuserdata/` · `android/.gradle/` · `android/build/` · `android/app/build/` · `android/local.properties` · `android/capacitor-cordova-android-plugins/` · `android/app/src/main/assets/public/` · `android/app/src/main/assets/capacitor.config.json` · `android/app/src/main/assets/capacitor.plugins.json` · `android/app/src/main/res/xml/config.xml` · `*.jks` · `*.keystore`(서명은 범위 밖 — 키 파일 커밋 금지).
    ⑦ `mobile/ios/App/App/SceneDelegate.swift` 에서 템플릿이 코드로 만드는 기본 브리지 VC 생성식을 `GHTradeBridgeViewController()` 로 바꾼다(Pitfall 1·3 — storyboard 만 바꾸면 적용되지 않는다). `SceneDelegateProxy` 호출은 그대로 둔다. `Base.lproj/Main.storyboard` 의 `customClass` 도 `GHTradeBridgeViewController` · `customModule` `App` 으로 맞춘다(보조). `AppDelegate.swift` 는 템플릿 그대로.
    ⑧ `mobile/ios/App/App/GHTradeBridgeViewController.swift` 를 만든다: `import UIKit` · `import WebKit` · `import Capacitor` · `import os`. `final class GHTradeBridgeViewController: CAPBridgeViewController, WKScriptMessageHandler`. `private let log = Logger(subsystem: "com.ghtrade.app", category: "bridge")`. `enum NativeMessageType: String { case ready, route, theme, overlay, pull }`. `override func capacitorDidLoad()` → `super.capacitorDidLoad()` → `guard let wv = webView else { return }` → `wv.configuration.userContentController.add(WeakScriptMessageHandler(self), name: "ghTrade")`. 같은 파일에 `private final class WeakScriptMessageHandler: NSObject, WKScriptMessageHandler { weak var target: WKScriptMessageHandler? … }` 를 두어 userContentController 의 강한 참조 순환을 끊는다. `userContentController(_:didReceive:)`: `message.name == "ghTrade"` · `message.frameInfo.isMainFrame` · `message.body as? String` → `JSONSerialization` 로 `[String: Any]` → `type` 을 `NativeMessageType(rawValue:)` 로 화이트리스트(모르는 타입·파싱 실패는 무시, V5) · 보낸 문서 호스트(`message.frameInfo.request.url?.host`)가 `bridge?.config.serverURL.host` 와 같을 때만 처리(T-21-02). `ready` → `log.notice("ready platform=\(p, privacy: .public) nativeApp=\(flag, privacy: .public)")`, 나머지 타입은 `log.debug` 만(21-10·21-11 이 채운다). weekly-wine 의 `viewDidLayoutSubviews` 프레임 조작·쿠키 저장/복원은 가져오지 않는다(D-25 풀블리드 · RESEARCH Anti-Patterns).
    ⑨ Xcode 프로젝트 등록: `grep -n "PBXFileSystemSynchronizedRootGroup" mobile/ios/App/App.xcodeproj/project.pbxproj` 로 App 폴더가 동기화 그룹인지 먼저 본다. 동기화 그룹이면 파일을 두기만 하면 컴파일된다. 아니면 `SceneDelegate.swift` 항목을 본떠 `PBXFileReference` · `PBXBuildFile` · App 그룹 `children` · `PBXSourcesBuildPhase` `files` 에 `GHTradeBridgeViewController.swift` 를 새 24자리 대문자 16진 ID 로 등록한다. 이 규칙은 이후 플랜의 새 Swift 파일에도 똑같이 적용된다 — 어느 쪽이었는지 SUMMARY 에 적는다.
    ⑩ `webapp/src/lib/native/native-detect.ts`: `NATIVE_DETECT_SCRIPT` 는 의존성 없는 IIFE 문자열이다 — `try { var C = window.Capacitor; if (C && C.isNativePlatform && C.isNativePlatform()) { var d = document.documentElement; var p = C.getPlatform(); d.classList.add('native-app'); d.setAttribute('data-native-platform', p); var m = JSON.stringify({ type: 'ready', payload: { platform: p, nativeApp: true, path: location.pathname } }); var w = window.webkit; if (w && w.messageHandlers && w.messageHandlers.ghTrade) { w.messageHandlers.ghTrade.postMessage(m); } else if (window.GhTradeBridge) { window.GhTradeBridge.postMessage(m); } } } catch (e) {}`. 채널 우선순위(iOS → Android)는 21-04 `postNative` 와 같아야 한다는 주석을 단다(인라인이라 import 불가). `isNativeApp()` = `typeof document !== 'undefined' && document.documentElement.classList.contains('native-app')`, `nativePlatform()` = 속성값이 `ios`/`android` 일 때만 그 값, 아니면 null. 파일 머리 주석: D-11(웹이 스스로 감지 · weekly-wine bridge.js 방식 금지) · native-bridge 가 문서 시작 시점에 주입된다는 근거(RESEARCH Pattern 4).
    ⑪ `webapp/src/app/layout.tsx`: `<html …>` 바로 안, `<body>` 앞에 `<head><script id="gh-native-detect" dangerouslySetInnerHTML={{ __html: NATIVE_DETECT_SCRIPT }} /></head>` 를 더한다. 나머지(Provider 순서·metadata·viewport)는 이 태스크에서 건드리지 않는다(title 은 21-09, viewport-fit 은 21-06).
    ⑫ `webapp/src/lib/native/__tests__/native-detect.test.ts` 를 먼저 쓰고(RED) ⑩을 구현한다. 스크립트 실행은 `new Function(NATIVE_DETECT_SCRIPT)()`. 각 케이스 전후로 `document.documentElement` 의 클래스·속성과 `window.Capacitor`·`window.webkit`·`window.GhTradeBridge` 를 원복한다. 파일 머리에 「깨지면 사용자가 겪는 일」 번호 목록(PATTERNS §테스트 주석 관례).
    ⑬ `mobile/scripts/smoke-ios.sh`(bash · `set -euo pipefail`): (a) `curl -sf -o /dev/null http://localhost:3100/login` 실패 → 「webapp dev 서버(:3100)를 먼저 띄우세요」 출력 후 exit 2 (b) `trap` EXIT 으로 `pnpm exec cap sync ios` (운영 URL 복원, Pitfall 15) (c) `CAP_SERVER_URL=http://localhost:3100 pnpm exec cap sync ios` (d) `native:build:ios` 와 같은 `xcodebuild` 명령 (e) `DEVICE="${DEVICE:-iPhone 17}"` 부팅(이미 부팅이면 통과) → `xcrun simctl install booted ios/DerivedData/Build/Products/Debug-iphonesimulator/App.app` → 시작 시각 기록 → `xcrun simctl terminate booted <appId>` (실패 무시) → `xcrun simctl launch booted <appId>` (f) 최대 60초 동안 3초마다 `xcrun simctl spawn booted log show --style compact --start "<시작 시각>" --predicate 'subsystem == "com.ghtrade.app"'` 에서 `ready platform=ios nativeApp=true` 를 찾는다 (g) `xcrun simctl io booted screenshot "${TMPDIR:-/tmp}/gh-trade-ios-smoke.png"` (h) 찾으면 `SMOKE OK ready platform=ios` 출력 후 exit 0, 못 찾으면 마지막 로그 30줄과 `SMOKE FAIL` 출력 후 exit 1. 스크립트는 `mobile/` 에서 실행된다고 가정하고 첫 줄에서 `cd "$(dirname "$0")/.."`. 실행 권한(`chmod +x`)을 준다.
    ⑭ 실행: 3100 이 비어 있으면 `PORT=3100 pnpm --filter @gh-radar/webapp run dev` 를 백그라운드로 띄우고 `/login` 이 200/307 을 줄 때까지 기다린 뒤 `pnpm --filter @gh-radar/mobile run native:smoke:ios`. 로그에 `NSURLErrorDomain` `-1022`(ATS)가 보이면 `Info.plist` 에 `NSAppTransportSecurity` → `NSAllowsLocalNetworking` = true **만** 더한다(임의 로드 전면 허용 키 금지 — T-21-23). 필요 없었으면 Info.plist 는 템플릿 그대로 둔다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/native/__tests__/native-detect.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or "native-detect.test.ts" is absent from the output</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:ios</automated>
    <fails_when>non-zero exit, or the output lacks "BUILD SUCCEEDED"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:smoke:ios</automated>
    <fails_when>non-zero exit, or the output lacks "SMOKE OK ready platform=ios", or contains "SMOKE FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n '^  - mobile$' pnpm-workspace.yaml` 가 1줄
    - `node -e "const s=require('./mobile/package.json').scripts;process.exit(['build','typecheck','test','lint'].some(k=>k in s)?1:0)"` 가 exit 0 (repo 루트에서)
    - `node -e "const p=require('./mobile/package.json');process.exit(p.dependencies['@capacitor/core']==='8.5.2'&&p.devDependencies['@capacitor/cli']==='8.5.2'?0:1)"` 가 exit 0 (Task 2 가 downgrade 를 지시했으면 그 버전)
    - `grep -cE 'errorPath|allowNavigation|webContentsDebuggingEnabled|CapacitorHttp|CapacitorCookies' mobile/capacitor.config.ts` 가 0
    - `grep -n "https://trade.jx1.io" mobile/capacitor.config.ts` 와 `grep -n "appName: 'GH Trade'" mobile/capacitor.config.ts` 가 각각 1줄 이상
    - `grep -n "GHTradeBridgeViewController()" mobile/ios/App/App/SceneDelegate.swift` 가 1줄 · `grep -c "rootViewController = CAPBridgeViewController" mobile/ios/App/App/SceneDelegate.swift` 가 0
    - `grep -n 'name: "ghTrade"' mobile/ios/App/App/GHTradeBridgeViewController.swift` 가 1줄 이상
    - `grep -c "NSAllowsArbitraryLoads" mobile/ios/App/App/Info.plist` 가 0
    - `git check-ignore -q mobile/ios/App/App/capacitor.config.json && git check-ignore -q mobile/ios/App/App/public/index.html && git check-ignore -q mobile/ios/DerivedData/x` 가 exit 0
    - `grep -n "NATIVE_DETECT_SCRIPT" webapp/src/app/layout.tsx` 가 1줄 이상
    - `grep -n 'git diff --quiet "$PREV" "$CUR" -- webapp/ packages/shared/ pnpm-lock.yaml' scripts/vercel-ignore-build.sh` 가 1줄(D-05 — 무수정)
    - 네 개의 `<automated>` 명령이 모두 통과한다
  </acceptance_criteria>
  <done>iOS 시뮬레이터의 GH Trade 셸이 웹을 로드하고 웹이 스스로 `html.native-app` 을 붙여 `ready` 를 네이티브로 보낸다는 사실이 통합 로그로 증명됐고, 운영 URL 로 다시 sync 된 상태로 커밋됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 원격 웹 페이지 → 네이티브(`ghTrade` 메시지) | 운영 웹의 스크립트(또는 XSS)가 네이티브에 JSON 을 보낸다 — 네이티브는 신뢰하지 않고 파싱·화이트리스트한다 |
| 빌드 설정(dev ↔ 운영) | `CAP_SERVER_URL` 로 dev URL·cleartext 가 생성 설정에 구워진다 |
| npm 레지스트리 → `mobile/` 의존성 | 새 패키지 4개가 워크스페이스(=Vercel 설치)에 들어온다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Information Disclosure | dev `server.url`·cleartext 의 운영 빌드 유출(Pitfall 15) | high | mitigate | `native:sync:dev` 분리 · 스모크 스크립트 EXIT trap 이 운영 sync 복원(Task 3 ⑬b) · 최종 검사 `verify-prod-config.mjs`(21-16) |
| T-21-02 | Tampering / Elevation | `userContentController(_:didReceive:)` | high | mitigate | 메인 프레임 · 문자열 JSON · `NativeMessageType` 화이트리스트 · 문서 호스트 == server.url 호스트일 때만 처리 · UI 신호만 수행(Task 3 ⑧) |
| T-21-04 | Information Disclosure | 릴리스 WebView 디버깅 | medium | mitigate | 디버깅 키를 config 에 두지 않아 Capacitor 기본(릴리스 off) 유지 — acceptance grep 0 |
| T-21-05 | Spoofing | WebView 안 외부 호스트 내비 | medium | mitigate | 내비게이션 허용 목록 키 없음 → 앱 호스트 외 링크는 외부 브라우저(Capacitor 기본) |
| T-21-14 | Information Disclosure | 키스토어·서명 비밀 커밋 | high | mitigate | `mobile/.gitignore` 에 `*.jks` · `*.keystore` · 생성 설정 파일 · weekly-wine 의 기본 비밀번호 서명 설정은 이식하지 않음 |
| T-21-22 | Tampering | 네이티브 HTTP/쿠키 가로채기 | medium | mitigate | 해당 플러그인 키 없음(acceptance grep 0) — fetch/SSE 는 WebView 표준 경로 |
| T-21-23 | Tampering | ATS 전면 해제 | high | mitigate | 임의 로드 전면 허용 키 금지(acceptance grep 0) · 필요 시 `NSAllowsLocalNetworking` 만 |
| T-21-SC | Tampering | npm 설치(@capacitor/* 8.5.2) | high | mitigate | Task 2 blocking-human 정당성 게이트 · `--save-exact` 정확 고정 · postinstall 스크립트 없음 확인 |
</threat_model>

<verification>
- 웹: `native-detect.test.ts` green · webapp typecheck green
- iOS: `native:build:ios` BUILD SUCCEEDED · `native:smoke:ios` 「SMOKE OK ready platform=ios」
- 설정: capacitor.config.ts 금지 키 0 · Info.plist 전면 허용 키 0 · 생성 파일 gitignore
</verification>

<success_criteria>
- D-20 이 사람 확인을 거쳐 구워졌고, D-01·D-05·D-11 의 뼈대가 iOS 에서 끝까지 동작한다.
- 이후 플랜(탭바·새로고침·로그인)이 같은 채널(`ghTrade`)과 같은 감지 클래스(`native-app`) 위에 올라간다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/` 패키지 `@gh-radar/mobile` — scripts `native:sync` · `native:sync:dev` · `native:build:ios` · `native:build:android` · `native:smoke:ios` · `native:open:ios` · `native:open:android` · `native:assets` (21-01) · `native:smoke:android` (21-02) · `native:check-tab-routes:ios` (21-10) · `native:test:android` (21-12) · `native:verify-prod` (21-16) | 패키지·스크립트 | **21-01** · 21-02 · 21-10 · 21-12 · 21-16 |
| `mobile/capacitor.config.ts` — appId `com.ghtrade.app` · appName `GH Trade` · `CAP_SERVER_URL` · `SystemBars` (21-01) · `SocialLogin` (21-15) | 설정 | **21-01** · 21-15 |
| `mobile/tsconfig.json` · `mobile/.gitignore` | 설정 | **21-01** |
| `mobile/www/index.html` — 오프라인 폴백(최소 21-01 · `safeTarget`·5초 탐침·`online`·테마 21-11) | 정적 페이지 | **21-01** · 21-11 |
| `mobile/scripts/smoke-ios.sh` · `mobile/scripts/smoke-android.sh` — 「SMOKE OK ready platform=…」 | 스모크 | **21-01** · 21-02 |
| `mobile/scripts/check-tab-routes-ios.sh` · `mobile/scripts/tab-routes-check.swift` | Swift 경로표 검사 | 21-10 |
| `mobile/scripts/render-resources.mjs` · `mobile/resources/{icon.svg,icon-only.png,icon-foreground.png,icon-background.png,splash.png,splash-dark.png}` | 에셋 원본 | 21-14 |
| `mobile/scripts/verify-prod-config.mjs` · `mobile/README.md` | 운영 설정 검사·문서 | 21-16 |
| iOS `SceneDelegate.swift` (루트 = `GHTradeBridgeViewController()` · 창 배경 21-11) | Swift | **21-01** · 21-11 |
| iOS `GHTradeBridgeViewController` · `WeakScriptMessageHandler` · `NativeMessageType` | Swift | **21-01** · 21-10 · 21-11 |
| iOS `GHTradeTabBar` · `GHTradeTab` · `GHTabID` · `GHTradePalette` · `GHTradeTheme` · `TabRoutes.activeTab(forPath:)` · `TabRoutes.hidesTabBar(path:)` | Swift | 21-10 |
| iOS `NavigationDelegateProxy` · `ThemeStore` (UserDefaults `gh-trade.theme`) | Swift | 21-11 |
| Android `MainActivity` (`BridgeActivity`) · `GhTradeWebViewClient` (`BridgeWebViewClient` 상속) · `GhTradeBridge` (`@JavascriptInterface postMessage`) | Kotlin | 21-02 · 21-12 · 21-13 |
| Android `GhTradeTabBar` · `GhTradePalette` · `TabId` · `TabRoutes.activeTabFor` · `TabRoutes.hidesTabBar` · `TabRoutesTest` | Kotlin | 21-12 |
| Android `ThemeStore` (SharedPreferences `gh_trade`/`theme`) · `res/values/bools.xml` · `res/values-sw600dp/bools.xml` (`is_tablet`) | Kotlin·리소스 | 21-13 |
| Android `res/drawable/ic_tab_{home,home_fill,search,search_fill,trading,trading_fill,ai,ai_fill,me,me_fill}.xml` | 벡터 | 21-12 |
| 웹 `lib/native/native-detect.ts` — `NATIVE_DETECT_SCRIPT` · `isNativeApp` · `nativePlatform` | 유틸 | **21-01** |
| 웹 `lib/native/post-native.ts` — `postNative` · `NativeMsgType` | 유틸 | 21-04 |
| 웹 `lib/native/native-bridge-provider.tsx` — `NativeBridgeProvider` · `useNativeBridge` · `NativeRefreshFn` · `window.__ghTrade = { refresh, navigate, back }` | Provider | 21-04 |
| 웹 `lib/native/use-native-refresh.ts` — `useNativeRefresh` | 훅 | 21-04 |
| 웹 `lib/native/native-overlay-marker.tsx` — `NativeOverlayMarker` | 컴포넌트 | 21-04 |
| 웹 `lib/native/google-client-ids.ts` — `GOOGLE_WEB_CLIENT_ID` · `GOOGLE_IOS_CLIENT_ID` · `GOOGLE_ANDROID_CLIENT_ID` · `GOOGLE_IOS_URL_SCHEME` | 공개 식별자 상수 | 21-03 |
| 웹 `lib/native/native-google-login.ts` — `nativeGoogleSignIn` · `randomNonce` · `sha256Hex` · `classifyNativeLoginError` | 서비스 | 21-15 |
| 웹 `RelayConnectionState.probeNow` · `EMPTY_RELAY_VALUE.probeNow` | 훅 계약 | 21-07 |
| 웹 `lib/recent-search.ts` — `RECENT_SEARCH_KEY` · `RECENT_SEARCH_MAX` · `RecentSearchItem` · `readRecentSearches` · `pushRecentSearch` · `removeRecentSearch` · `clearRecentSearches` | 유틸 | 21-08 |
| 웹 `app/search/page.tsx` · `components/search/search-page-client.tsx` — `SearchPageClient` · 사이드바 `NAV_SEARCH_PAGE` | 페이지·상수 | 21-08 |
| 웹 `components/me/account-card.tsx` — `AccountCard` | 컴포넌트 | 21-09 |
| 웹 마크업 계약 `data-slot="app-aside"` · `data-slot="app-menu-button"` · `data-slot="stock-ai-button"` · `data-slot="shared-panels"` · `data-pinned` · `data-order-cta` · `data-slot="account-card"` | 마크업 | 21-05 · 21-06 · 21-09 |
| CSS `@custom-variant native` (21-04) · 앱 셸 규칙 (21-05) · `--app-safe-top` · `--app-safe-bottom` · `--app-safe-left` · `--app-safe-right` · `--native-tabbar-gap` · `--native-tabbar-offset` · `--native-body-reserve` (21-06) | 토큰·변형 | 21-04 · 21-05 · 21-06 |
| 웹 `app/icon.svg` (= 스케치 004 `#app-a`) | 파비콘 | 21-14 |
| 단위 테스트: `native-detect.test.ts` (21-01) · `post-native.test.ts` · `native-bridge-provider.test.tsx` · `native-overlay.test.tsx` (21-04) · `google-client-ids.test.ts` (21-03) · `relay-socket.test.ts` probeNow · `stock-native-refresh.test.tsx` (21-07) · `recent-search.test.ts` · `search-page-client.test.tsx` · `app-sidebar.test.tsx` 보강 (21-08) · `account-card.test.tsx` · `app-shell-chrome.test.tsx` 보강 (21-09) · `native-google-login.test.ts` · `login/__tests__/page.test.tsx` (21-15) · `TabRoutesTest.kt` (21-12) | 단위 | 각 플랜 |
| e2e: `fixtures/native-app.ts` — `installNativeApp` · `nativeMessages` (21-05) · `specs/native-shell.spec.ts` (21-05 · 21-06) · `specs/search-page.spec.ts` (21-08) · `specs/brand-account.spec.ts` (21-09) | Playwright | 각 플랜 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-01-SUMMARY.md` when done — 「결정」 절에 Task 1 확정 번들 ID·팀 ID, Task 2 승인 버전, pbxproj 등록 방식(동기화 그룹 여부), ATS 예외 추가 여부를 적는다.
</output>
