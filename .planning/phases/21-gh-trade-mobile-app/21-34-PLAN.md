---
phase: 21-gh-trade-mobile-app
plan: 34
type: execute
wave: 15
depends_on: ["21-25", "21-30", "21-33"]
files_modified:
  - webapp/src/components/stock/stock-detail-tabs.tsx
  - webapp/src/components/stock/stock-detail-client.tsx
  - webapp/src/components/stock/stock-hero.tsx
  - webapp/src/components/stock/__tests__/stock-detail-tabs.test.tsx
  - webapp/src/components/stock/__tests__/stock-detail-client.test.tsx
  - webapp/src/components/stock/__tests__/stock-native-refresh.test.tsx
  - webapp/e2e/specs/stock-detail-tabs.spec.ts
  - webapp/e2e/specs/native-shell.spec.ts
  - webapp/src/components/stock/stock-orderbook-section.tsx
  - webapp/src/components/stock/__tests__/stock-orderbook-section.test.tsx
  - webapp/src/components/stock/__tests__/orderbook.test.tsx
  - webapp/src/components/orderbook/relay-status-bar.tsx
  - webapp/src/components/orderbook/__tests__/relay-status-bar.test.tsx
  - webapp/src/components/orderbook/orderbook-skeleton.tsx
  - webapp/src/lib/exchange-choices.ts
  - webapp/e2e/specs/orderbook.spec.ts
  - webapp/e2e/specs/trading-workbench.spec.ts
  - webapp/src/components/trading/card/manual-order-form.tsx
  - webapp/src/components/trading/card/card-body.tsx
  - webapp/src/components/trading/workbench/trading-workbench.tsx
  - webapp/src/components/trading/__tests__/manual-order-form.test.tsx
  - webapp/src/components/trading/__tests__/card-body.test.tsx
autonomous: true
gap_closure: true
requirements: [MOBILE-01]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G-21-R3-9 (종목상세 쪽 · D-30): 종목상세의 「주문하기」가 모든 폭에서 「트레이딩」이 된다 — 폰은 하단 바(같은 data-slot · 같은 수치), 넓은 폭은 스케치 008 ② 채택안 자리의 버튼 — 누르면 `/trading?code={code}` 로 가서 그 종목 카드에 도착한다 · 매매 불가 종목(비 KOSPI/KOSDAQ · isin 없음)에는 버튼이 없다"
    - "G-21-R3-10 (D-31): 종목상세가 3탭(차트 · 종목정보 · 뉴스·토론)이 되고, `?tab=orderbook` 딥링크는 매매 가능하면 `/trading?code=` 로, 아니면 차트로 간다 · 종목상세 호가 탭 전용 코드(섹션 · 상태바 · 스켈레톤 · KRX 전용 제목 · 수동주문/CardBody variant · 호가 전용 각주)가 사라지고 공유 모듈은 남는다"
    - "orderbook.spec 의 고유 검증 다섯 가지(인증→구독→10단 · 재접속 배지 · 비로그인 wss 0 · 폭별 상태줄/시간외종가 배치 · 390 리플로우)는 작업대 기준으로 살아 있다"
    - "G-21-CR · IN-04: 종목상세 CTA 주석의 탭바 몫 수치가 정본 98(--native-body-reserve)과 같다"
  artifacts:
    - path: webapp/src/components/stock/stock-detail-tabs.tsx
      provides: "3탭 · 트레이딩 Link CTA · ?tab=orderbook 딥링크 처리"
      contains: "/trading?code="
    - path: webapp/e2e/specs/trading-workbench.spec.ts
      provides: "orderbook.spec 에서 옮긴 고유 검증(작업대 카드 기준)"
      contains: "G-21-R3-10"
  key_links:
    - from: webapp/src/components/stock/stock-detail-tabs.tsx
      to: webapp/src/components/trading/workbench/trading-workbench.tsx
      via: "Link /trading?code= → 21-33 착지(카드 보장·펼침·포커스)"
      pattern: "/trading\\?code="
    - from: webapp/src/components/stock/stock-detail-client.tsx
      to: webapp/src/components/trading/workbench/stock-add-bar.tsx
      via: "isPickable(stock) → tradable 프롭"
      pattern: "isPickable"
  prohibitions:
    - statement: "MUST NOT 공유 모듈(account-panel · order-panel · orderbook-ladder · trade-tape · order-confirm-dialog · quote-grid-10 · useStrategyCardState)을 지운다"
      status: unverified
      verification: "Task 2 acceptance — 파일 존재 검사"
    - statement: "MUST NOT orderbook.spec 을 지우면서 고유 검증을 대조 없이 버린다"
      status: unverified
      verification: "Task 3 SUMMARY 대조 표 · 이전 테스트 존재"
---

<objective>
R3-9 → R3-10 순서(D-31)의 뒤 절반 — 21-33 이 트레이딩 착지와 카드 주문유형을 만든 뒤, 종목상세를 바꾼다: **G-21-R3-9** 「주문하기」 → 「트레이딩」 링크(모든 폭) · **G-21-R3-10** 호가주문 탭 제거 · 딥링크 처리 · 탭 전용 코드와 e2e 정리 · **IN-04** 주석 수치.

Purpose: 종목상세는 정보 화면으로 남고 주문은 트레이딩 한 곳에서 한다. 탭이 사라져도 시간외종가 주문(카드 · 21-33)과 고유 테스트 검증은 남는다.
Output: CTA 링크 · 넓은 폭 버튼(채택안) · 3탭 · 삭제 여섯 파일 · variant 제거 · orderbook.spec 선별 이전.

과설계 금지: 이번 변경과 무관한 다른 파일의 옛 이름 언급 주석은 손대지 않는다. 매매 가능 판정은 stock-add-bar 의 `isPickable` 을 그대로 import 한다.

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add(`git add -A` 금지 · 삭제는 `git rm`) · 커밋 직전 `git status -sb` · **push 금지**(21-36).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-UAT.md
@.planning/phases/21-gh-trade-mobile-app/21-REVIEW.md
@.planning/sketches/008-uat3-surfaces/README.md
@webapp/src/components/stock/stock-detail-tabs.tsx
@webapp/src/components/stock/stock-detail-client.tsx
@webapp/src/components/stock/stock-hero.tsx
@webapp/src/components/trading/card/manual-order-form.tsx
@webapp/src/components/trading/card/card-body.tsx

<interfaces>
```text
StockDetailTabs({ code, chart, orderbook, info, news }) · TABS 4개 · keepMounted 호가 예외 · CTA 표시 = 활성 탭이 호가가 아닐 때 · data-order-cta 예약 · CTA 바 data-slot detail-order-cta-bar / 버튼 detail-order-cta(지금은 탭 전환 onClick) · ≈217 주석 탭바 몫 수치(IN-04 — 정본 98)
stock-detail-client.tsx: 호가창 섹션 orderbook 프롭 · stock: StockDetailResponse(isin · market) · 갱신시각 행 「새로고침」
stock-hero.tsx: 첫 줄(← · 이름 · 코드 · 시장 배지 · 관심 토글 · 앱 전용 「AI 분석」 ml-auto) · 가격 줄
stock-add-bar.tsx: export function isPickable(row) — KOSPI/KOSDAQ + isin
삭제 대상 사용처(플래너 grep): stock-orderbook-section(stock-detail-client · 두 단위 테스트) · relay-status-bar(자기 테스트) · orderbook-skeleton · exchange-choices KRX_ONLY_TITLE(stock-orderbook-section 만) — 그 밖 언급은 주석
manual-order-form / card-body: variant 'card' | 'orderbook' · data-variant · 호가 전용 각주 상수 · trading-workbench ≈1493 CardBody 호출에 variant 전달
테스트 영향: stock-detail-tabs.test · stock-detail-client.test(4탭) · stock-native-refresh.test(orderbook 목) · stock-detail-tabs.spec(4탭 · 호가 탭 경유) · native-shell.spec(CTA 수치 — data-slot 유지라 수치 불변) · orderbook.spec(≈900줄) · trading-workbench.spec GC6
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 「주문하기」 → 「트레이딩」 Link /trading?code= (폰 바 + 넓은 폭 채택안) · 매매 불가 숨김 → e2e(종목상세 → 트레이딩 카드) (G-21-R3-9)</name>
  <files>webapp/src/components/stock/stock-detail-tabs.tsx, webapp/src/components/stock/stock-detail-client.tsx, webapp/src/components/stock/stock-hero.tsx, webapp/src/components/stock/__tests__/stock-detail-tabs.test.tsx, webapp/e2e/specs/stock-detail-tabs.spec.ts, webapp/e2e/specs/native-shell.spec.ts</files>
  <read_first>
    - .planning/sketches/008-uat3-surfaces/README.md (② Winner — 넓은 폭 버튼 자리 · 수치)
    - webapp/src/components/stock/stock-detail-tabs.tsx (120~237 CTA · 예약 · 주석)
    - webapp/src/components/stock/stock-detail-client.tsx (150~240)
    - webapp/src/components/stock/stock-hero.tsx (전체)
    - webapp/src/components/stock/__tests__/stock-detail-tabs.test.tsx (CTA 테스트)
    - webapp/e2e/specs/native-shell.spec.ts (CTA 수치 테스트 — 이름만 바뀐다)
    - .planning/phases/21-gh-trade-mobile-app/21-33-SUMMARY.md (착지 동작)
  </read_first>
  <action>
    per D-30: ① `stock-detail-client.tsx`: `const tradable = isPickable(stock)`(stock-add-bar import) 를 `StockDetailTabs` 와 (채택안이 A·B 면) `StockHero` 에 넘긴다. 채택안 C 면 이 파일 갱신시각 행 「새로고침」 옆에 넓은 폭 버튼.
    ② `stock-detail-tabs.tsx`: 프롭 `tradable: boolean`. 폰 CTA 바는 tradable 일 때만(이 태스크에서는 호가 탭이 아직 있으므로 활성 탭 조건 유지 — Task 2 가 단순화), 버튼은 탭 전환 onClick 대신 `next/link` `<Link data-slot="detail-order-cta" href={'/trading?code=' + encodeURIComponent(code)}>트레이딩</Link>`(같은 클래스 · 56 · radius 16 · 17/600 · `--up`). 바 · 버튼 data-slot 유지(앱 규칙 · 챗 FAB · native-shell 수치가 그대로). 예약도 같은 조건. CTA 주석을 「트레이딩 링크(D-30) · 앱 예약 76 · 탭바 몫은 본문 98(--native-body-reserve · IN-04)」 로 다시 쓴다.
    ③ 넓은 폭 버튼(채택안 ②): `data-slot="detail-trading-button"` · `hidden md:inline-flex`(폰은 하단 바) · 같은 href · 「트레이딩」 · 채택안 수치 · tradable 일 때만.
    ④ 단위: CTA 테스트를 「tradable 이면 Link 「트레이딩」 href `/trading?code=005930`」 · 「tradable false 면 CTA · 예약 없음」 으로 바꾼다.
    ⑤ e2e: stock-detail-tabs.spec 「G-21-R3-9 트레이딩 버튼」 하나 — 390 폰 바 · 1280 넓은 폭 버튼 각각 클릭 → `/trading` 에 그 종목 카드가 펼쳐져 있다(로컬 relay 규약 describe) · 매매 불가 목(isin null) → 두 버튼 0. native-shell.spec 의 CTA 테스트 제목·주석의 「주문하기」 를 「트레이딩」으로(수치 불변).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/stock/__tests__/stock-detail-tabs.test.tsx src/components/stock/__tests__/stock-detail-client.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/stock-detail-tabs.spec.ts e2e/specs/native-shell.spec.ts</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or no summary line matches "[0-9]+ passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "/trading?code=" webapp/src/components/stock/stock-detail-tabs.tsx` ≥ 1 · `grep -c "트레이딩" webapp/src/components/stock/stock-detail-tabs.tsx` ≥ 1
    - CTA 가 탭 전환을 부르지 않는다: `test "$(grep -c "handleValueChange('orderbook')" webapp/src/components/stock/stock-detail-tabs.tsx)" -eq 0`
    - 넓은 폭 버튼: `cat webapp/src/components/stock/stock-hero.tsx webapp/src/components/stock/stock-detail-client.tsx | grep -c 'data-slot="detail-trading-button"'` ≥ 1
    - `grep -c "isPickable" webapp/src/components/stock/stock-detail-client.tsx` ≥ 1 · `grep -c "G-21-R3-9" webapp/e2e/specs/stock-detail-tabs.spec.ts` ≥ 1
    - IN-04: `grep -c "본문 98" webapp/src/components/stock/stock-detail-tabs.tsx` ≥ 1
    - verify 두 명령 통과
  </acceptance_criteria>
  <done>종목상세 어느 폭에서도 「트레이딩」 한 번으로 그 종목 카드에 도착하고, 매매 불가 종목에는 버튼이 없다.</done>
</task>

<task type="auto">
  <name>Task 2: 호가주문 탭 제거 · `?tab=orderbook` → /trading?code= (매매 불가 = 차트) · 탭 전용 파일 삭제 · 3탭 테스트 (G-21-R3-10)</name>
  <files>webapp/src/components/stock/stock-detail-tabs.tsx, webapp/src/components/stock/stock-detail-client.tsx, webapp/src/components/stock/__tests__/stock-detail-tabs.test.tsx, webapp/src/components/stock/__tests__/stock-detail-client.test.tsx, webapp/src/components/stock/__tests__/stock-native-refresh.test.tsx, webapp/e2e/specs/stock-detail-tabs.spec.ts, webapp/src/components/stock/stock-orderbook-section.tsx, webapp/src/components/stock/__tests__/stock-orderbook-section.test.tsx, webapp/src/components/stock/__tests__/orderbook.test.tsx, webapp/src/components/orderbook/relay-status-bar.tsx, webapp/src/components/orderbook/__tests__/relay-status-bar.test.tsx, webapp/src/components/orderbook/orderbook-skeleton.tsx, webapp/src/lib/exchange-choices.ts</files>
  <read_first>
    - webapp/src/components/stock/stock-detail-tabs.tsx (1~120 머리 주석 T1~T8 · TABS · keepMounted · 160~237)
    - webapp/src/components/stock/stock-detail-client.tsx (1~60 import · 207~222 orderbook 프롭)
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-R3-10 artifacts — 삭제·유지 목록)
    - webapp/src/lib/exchange-choices.ts
    - 세 단위 테스트 파일 · stock-detail-tabs.spec.ts
  </read_first>
  <action>
    per D-31: ① 지우기 전 `grep -rn` 으로 삭제 대상 여섯 파일과 `KRX_ONLY_TITLE` 의 사용처 표를 SUMMARY 에 적는다 — 삭제 대상 자신 · 그 테스트 · stock-detail-client 의 import 뿐이어야 한다(그 밖은 주석). 예상 밖 코드 사용처가 있으면 그 파일은 지우지 않고 보고한다.
    ② `stock-detail-tabs.tsx`: 탭을 차트 · 종목정보 · 뉴스토론 3개로, `orderbook` 프롭 · 패널 · keepMounted 예외 · 탭 전용 bleed 클래스를 지우고 CTA/예약 조건을 `tradable` 하나로. `?tab=orderbook` 딥링크: 한 번만 — tradable 이면 `router.replace('/trading?code=' + encodeURIComponent(code))`, 아니면 `window.history.replaceState(null, '', '?tab=chart')`(렌더는 허용 목록 밖 → 차트). 머리 주석의 호가 탭 항목(T6 · T8 예외)을 「D-31 호가주문 탭 제거 — 주문은 /trading」 으로.
    ③ `stock-detail-client.tsx`: 호가창 섹션 import 와 `orderbook` 프롭을 지우고 머리 주석 「4탭」 → 「3탭(D-31)」.
    ④ `git rm`: `stock/stock-orderbook-section.tsx` · `stock/__tests__/stock-orderbook-section.test.tsx` · `stock/__tests__/orderbook.test.tsx` · `orderbook/relay-status-bar.tsx` · `orderbook/__tests__/relay-status-bar.test.tsx` · `orderbook/orderbook-skeleton.tsx`. `exchange-choices.ts` 의 `KRX_ONLY_TITLE` 은 사용처가 0 이면 지운다. 공유 모듈은 건드리지 않는다.
    ⑤ 테스트: stock-detail-tabs.test — 호가 탭 언마운트 테스트 삭제 · 탭 3개 · 딥링크 두 경우 · pushState 테스트의 탭 이름 갱신. stock-detail-client.test — 3탭 · 호가 목 제거. stock-native-refresh.test — 호가 목 제거. stock-detail-tabs.spec — 3탭 · 호가 탭 테스트를 「`?tab=orderbook` 딥링크 → /trading?code=」 로 · 호가 탭 경유 단계를 뉴스/정보 탭으로.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/stock/__tests__/stock-detail-tabs.test.tsx src/components/stock/__tests__/stock-detail-client.test.tsx src/components/stock/__tests__/stock-native-refresh.test.tsx && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or output contains "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/stock-detail-tabs.spec.ts e2e/specs/stock-detail.spec.ts e2e/specs/native-shell.spec.ts</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or no summary line matches "[0-9]+ passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - 탭 정의에 호가 없음: `test "$(grep -c "v: 'orderbook'" webapp/src/components/stock/stock-detail-tabs.tsx)" -eq 0`
    - 딥링크 처리: `grep -c "router.replace" webapp/src/components/stock/stock-detail-tabs.tsx` ≥ 1
    - 섹션 사용 제거: `test "$(grep -c 'StockOrderbookSection' webapp/src/components/stock/stock-detail-client.tsx)" -eq 0`
    - 삭제: `test ! -e webapp/src/components/stock/stock-orderbook-section.tsx` · `test ! -e webapp/src/components/orderbook/relay-status-bar.tsx` · `test ! -e webapp/src/components/orderbook/orderbook-skeleton.tsx`
    - 코드 import 0: `test "$(grep -rln "components/stock/stock-orderbook-section'\|components/orderbook/relay-status-bar'\|components/orderbook/orderbook-skeleton'" webapp/src webapp/e2e | wc -l | tr -d ' ')" -eq 0`
    - 공유 모듈 유지: `test -e webapp/src/components/orderbook/account-panel.tsx` · `test -e webapp/src/components/orderbook/order-panel.tsx` · `test -e webapp/src/components/orderbook/orderbook-ladder.tsx` · `test -e webapp/src/components/trading/card/quote-grid-10.tsx`
    - IN-04 옛 수치 제거: `test "$(grep -c '본문 108' webapp/src/components/stock/stock-detail-tabs.tsx)" -eq 0`
    - verify 두 명령 통과
  </acceptance_criteria>
  <done>종목상세가 3탭이 되고, 옛 호가주문 딥링크는 트레이딩으로 이어지며, 탭 전용 코드가 사라졌다.</done>
</task>

<!-- planner-discipline-allow: variant -->
<!-- planner-discipline-allow: ORDERBOOK_FOOTNOTE -->
<!-- planner-discipline-allow: variant="card" -->
<task type="auto">
  <name>Task 3: orderbook.spec 선별 이전 · 삭제(GC6 재작성 포함) · 수동주문/CardBody variant 제거</name>
  <files>webapp/e2e/specs/orderbook.spec.ts, webapp/e2e/specs/trading-workbench.spec.ts, webapp/src/components/trading/card/manual-order-form.tsx, webapp/src/components/trading/card/card-body.tsx, webapp/src/components/trading/workbench/trading-workbench.tsx, webapp/src/components/trading/__tests__/manual-order-form.test.tsx, webapp/src/components/trading/__tests__/card-body.test.tsx</files>
  <read_first>
    - webapp/e2e/specs/orderbook.spec.ts (전체 — describe · 테스트 1~11 · P20-6 ①②③ · 시간외 G2 창 스텁)
    - webapp/e2e/specs/trading-workbench.spec.ts (카드 호가 · 재접속 · 상태줄 · 폰 리플로우 · GC6)
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-R3-10 tests — 이 파일에만 있는 검증 목록)
    - webapp/src/components/trading/card/manual-order-form.tsx · card-body.tsx (variant · 각주 · data-variant)
    - webapp/src/components/trading/workbench/trading-workbench.tsx (CardBody 호출 · grep "variant=")
    - webapp/src/components/trading/__tests__/manual-order-form.test.tsx · card-body.test.tsx (variant 케이스)
  </read_first>
  <action>
    ① 대조 표(SUMMARY): orderbook.spec 테스트마다 「작업대 스펙에 이미 있음(이름) / 이전 / 버림(종목상세 호가 탭 전용 UI)」. 고유 검증 다섯 가지 — 인증 → 구독 → 호가 10단(토큰이 URL 에 없음) · 회선 단절 → 재접속 배지 + 사다리 유지 · 비로그인 → 로그인 · relay wss 0 · 폭별 상태줄 1줄 / 시간외종가 배치(P20-6) · 390 미체결·잔고 리플로우 잘림 0 — 은 **반드시** 판정하고, 덮이지 않았으면 trading-workbench.spec 에 작업대 카드 버전(제목 「G-21-R3-10 이전 — …」)으로 옮긴다. 시간외종가는 21-33 카드 주문유형으로 G2 창 스텁 방식 그대로 한 테스트(선택 → 확인 다이얼로그 「시간외종가」 → 주문 프레임 세션 필드 · 스텁 게이트웨이라 실주문 없음).
    ② GC6 제목·본문의 종목상세 호가 탭 경유 부분을 작업대 안 다른 카드/다른 화면 왕복으로 다시 쓴다(잠금이 앱 수명이라는 검증 유지). `git rm webapp/e2e/specs/orderbook.spec.ts`.
    ③ variant 제거: `ManualOrderForm` · `CardBody` 의 variant 프롭 타입·구조분해·전달 · `data-variant` · 카드 조건 클래스(카드 값으로 고정) · 호가 전용 각주 상수와 렌더 블록 · 남은 호가 분기를 지운다. trading-workbench 의 CardBody 호출에서 variant 프롭을 뺀다. 두 파일 머리 주석의 「두 표면」 설명을 「카드 한 표면(D-31)」 으로. 테스트: 호가 variant 케이스 중 카드에서도 의미 있는 판정(주문유형 · 시간외 잠김 · 정정 불가 · 닫힌 창 차단)은 카드 기준으로 남기고(21-33 에 이미 있으면 중복 삭제), 호가 탭 전용 단언은 지운다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/__tests__/manual-order-form.test.tsx src/components/trading/__tests__/card-body.test.tsx src/components/trading/__tests__/strategy-card-flow.test.tsx && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or output contains "error TS"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && test ! -e webapp/e2e/specs/orderbook.spec.ts && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/trading-workbench.spec.ts --grep-invert "격자 1/2/3단|P20-3 최악값|종목 추가 입력이 16px"</automated>
    <fails_when>non-zero exit (spec file still present or a test failed), or the summary contains "failed", or no summary line matches "[0-9]+ passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `test ! -e webapp/e2e/specs/orderbook.spec.ts` · `test "$(grep -rl 'orderbook.spec' webapp/e2e | wc -l | tr -d ' ')" -eq 0`
    - 이전 테스트: `grep -c "G-21-R3-10" webapp/e2e/specs/trading-workbench.spec.ts` ≥ 1
    - GC6 재작성: `test "$(grep -c '호가 탭에서도' webapp/e2e/specs/trading-workbench.spec.ts)" -eq 0`
    - variant 제거(주석 제외): `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*|\{/\*)' webapp/src/components/trading/card/manual-order-form.tsx | grep -c "variant")" -eq 0` · 같은 검사 card-body.tsx
    - 각주 상수 제거(주석 제외): `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*|\{/\*)' webapp/src/components/trading/card/manual-order-form.tsx | grep -c 'ORDERBOOK_FOOTNOTE')" -eq 0`
    - 호출부(주석 제외): `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*|\{/\*)' webapp/src/components/trading/workbench/trading-workbench.tsx | grep -c 'variant="card"')" -eq 0`
    - SUMMARY 대조 표에 고유 검증 다섯 가지가 모두 판정돼 있다
    - verify 두 명령 통과(알려진 범위 밖 3건만 제외 — deferred-items.md)
  </acceptance_criteria>
  <done>종목상세 호가 탭 전용 e2e 와 variant 가 사라지고, 고유 검증과 주문 판정은 작업대 카드 기준으로 살아 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 종목상세 → 트레이딩(주문 표면) | 링크가 주문 가능한 카드로 사용자를 데려간다 |
| 코드 삭제 → 주문 경로 | 공유 모듈을 잘못 지우면 작업대 주문이 깨진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-93 | Tampering | 매매 불가 종목에 트레이딩 버튼 | medium | mitigate | isPickable 게이트 · 단위/e2e 매매 불가 0(Task 1) · 착지 쪽 이중 게이트(21-33) |
| T-21-94 | Denial of Service | 공유 주문 모듈 오삭제 | high | mitigate | 삭제 전 사용처 표 · 공유 모듈 존재 검사 · 작업대 e2e 전체(Task 2 · 3) |
| T-21-95 | Repudiation | 고유 e2e 검증 유실 | medium | mitigate | 대조 표 · 다섯 항목 필수 판정 · 이전 테스트(Task 3) |
</threat_model>

<verification>
- vitest(stock-detail-tabs · stock-detail-client · stock-native-refresh · manual-order-form · card-body · strategy-card-flow) · Playwright(stock-detail-tabs · stock-detail · native-shell · trading-workbench) · typecheck
- 사람 확인(폰·넓은 폭 트레이딩 버튼 · 3탭 · 카드 시간외종가)은 21-36 UAT
</verification>

<success_criteria>
- R3-9 · R3-10 종목상세 쪽과 IN-04 가 닫히고, 호가주문 탭 제거가 기능·검증 손실 없이 끝났다.
</success_criteria>

## Flagged assumptions

- **엣지 탐침 MOBILE-01 (unclassified · unresolved):** 경계는 매매 불가 두 조건 · 딥링크 두 분기 · 폭 390/1280 으로 명시했다. 나머지는 21-36 UAT.
- 다른 파일 주석의 옛 파일명 언급(me-client · quote-grid-10 · strategy-card · relay-provider · use-relay-socket)은 동작과 무관해 이번 범위에서 고치지 않는다.

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| 스케치 `.planning/sketches/008-uat3-surfaces/`(index.html · README.md) · MANIFEST 008 행 | 목업 | 21-25 |
| CONTEXT D-12a · D-12b · D-25a · D-29 · D-30 · D-31 · D-32(R3-11 축소: 스크롤 복원 + 스켈레톤 없음 · 상태 초기화 허용) · WR-03 androidx.webkit 선언 답 | 결정 기록 | 21-25 |
| `PAD_CHIPS.만원` = 천만(+1,000) · 오천만(+5,000) · 1억(+10,000) · 지우기 · 20-CONTEXT D-17 포인터 | 상수 · 결정 | 21-26 |
| `SettingGroup` 제목 없는 그룹 상단 `pt-1` | 클래스 | 21-26 |
| 카드 헤더 ✕ `data-slot="card-close"` · 종목정보 팝업 ✕ = `size-8` + lucide `XIcon` + `after:-inset-1.5`(히트 44) · 팝업 고정 높이 · `scrollbar-gutter:stable` · 폰 safe-area 패딩 | 컴포넌트 | 21-26 |
| `webapp/src/lib/safe-path.ts` `isSafeInternalPath` · `recent-search.ts` `RECENT_CODE_RE` · `PopoverContent` 안 `NativeOverlayMarker` · `useDebouncedSearch` 반환 `resultsQuery` · `viewport.themeColor` `#17171c` + `ThemeColorSync` | 함수 · 훅 · 메타 | 21-27 |
| iOS 키보드 사유 즉시 숨김 · 재표시 90ms · `awaitingContent`(didStartProvisionalNavigation → 첫 route/1.5초) · 초기 숨김 · 브리지 출처 scheme·host·port 비교 · 오프라인 `dev=1` · 스모크 기기 지정(`IOS_DEVICE_UDID` · `ANDROID_SERIAL`) · mobile/README 명령표 | Swift · 스크립트 · 문서 | 21-28 |
| Android IME 애니메이션 콜백 즉시 숨김 · `awaitingContent`(onPageStarted) · `pullBlocked` 초기화 · 오프라인 뒤로가기(재현 시) · 오프라인 `dev=1` · 폴백 페이지 dev=1 일 때만 localhost · allowBackup=false + `res/xml/data_extraction_rules.xml` · 브리지 WebMessageListener(21-25 답 a) | Kotlin · XML · HTML | 21-29 |
| `NewsFullList` · `DiscussionFullList` · `useNewsView` · `StockNewsTabPanel` · 섹션 `onShowAll` · 종목상세 `?tab=news&view=news\|discussions` · 옛 URL 리다이렉트 · 팝업 `newsView` | 컴포넌트 · 훅 · 라우트 | 21-30 |
| `useTabRootScrollMemory`(`webapp/src/lib/tab-scroll-memory.ts` · AppShell 에서 호출) · 기존 `lib/query-cache` 재사용: 검색 허브 · 대화 목록 · 오늘 주문 | 훅 · 캐시 | 21-31 |
| 앱 공용 패널 숨김 CSS · 카드 더티 바 탭바 비킴(프로브) · `StrategyLogFeedProvider` · `useStrategyLogFeed` · `diffLimitChasers` · /me 전략 로그(`me-strategy-log`) | CSS · Provider · 컴포넌트 | 21-32 |
| 작업대 `?code=` 소비(카드 보장·펼침·포커스) · 카드 수동주문 주문유형(지정가/시간외종가) | 컴포넌트 | 21-33 |
| 종목상세 「트레이딩」 CTA(폰 바 + `detail-trading-button`) · 호가주문 탭 제거 · `?tab=orderbook` → `/trading?code=` · 호가 탭 전용 코드·`variant` 삭제 · orderbook.spec 선별 이전 | 컴포넌트 · 정리 · e2e | 21-34 |
| 게이트 기록 · `21-REVIEW-R2.md`(13건 대조) · UAT 환경(21-35-SUMMARY) · UAT 3차 재검증 · push 결정(21-36-SUMMARY) | 문서 | 21-35 · 21-36 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-34-SUMMARY.md` when done — CTA 채택안 대조 · 삭제 전 사용처 표 · orderbook.spec 대조 표 · 테스트 결과.
</output>
