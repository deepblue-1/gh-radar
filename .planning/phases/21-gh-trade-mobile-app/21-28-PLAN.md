---
phase: 21-gh-trade-mobile-app
plan: 28
type: execute
wave: 12
depends_on: ["21-24"]
files_modified:
  - mobile/ios/App/App/GHTradeBridgeViewController.swift
  - mobile/ios/App/App/NavigationDelegateProxy.swift
  - mobile/scripts/smoke-ios.sh
  - mobile/scripts/smoke-android.sh
  - mobile/README.md
autonomous: true
gap_closure: true
requirements: [MOBILE-01]
coupling_justified:
  - "21-29: 같은 D-12a · D-12b 수치(키보드 즉시 · 재표시 90ms · 대기 1.5초 · 표시 280ms)를 Android 에 옮긴다 — 파일 겹침 없음 · 수치 정본은 21-CONTEXT D-12a/D-12b"
  - "21-29: IN-02 폴백 페이지(mobile/www/index.html)는 이 플랜이 iOS 에서 붙이는 dev=1 신호를 읽는다 — 21-29 가 뒤 웨이브"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G-21-R3-1 (iOS · D-12a): 입력 포커스로 키보드가 올라올 때 탭바가 150ms 대기 없이 즉시, 키보드 애니메이션 길이의 절반(0.08~0.2초)으로 같은 곡선에 맞춰 사라지고 아래로 내려가는 이동이 없다 · 키보드가 내려가면 90ms 뒤 다시 보인다(입력칸 이동의 연속 알림은 깜빡이지 않는다) · 키보드 끝 프레임이 화면 하단을 가리지 않으면(하드웨어·플로팅) 숨기지 않는다"
    - "G-21-R3-4 (iOS · D-12b): 콜드 스타트와 로그인 뒤 전체 문서 로드 동안 탭바가 숨어 있고, 새 문서의 첫 route 메시지(하이드레이션 뒤) 또는 1.5초에 280ms ease-out 으로 나타난다 — SPA 탭 이동은 영향 없다"
    - "G-21-CR · WR-03 (iOS 측): 브리지 메시지 출처 판정이 WKSecurityOrigin 의 스킴·호스트·포트 전체를 서버 URL 과 비교한다 — dev(`http://localhost:3100`)와 오프라인 페이지(`capacitor://localhost`)가 같은 출처로 판정되지 않는다"
    - "G-21-CR · IN-02 (iOS 신호): 오프라인 폴백 URL 에 서버 URL 이 루프백 http(dev 빌드)일 때만 `dev=1` 이 붙는다"
    - "G-21-CR · IN-08: 스모크가 기기를 명시한다 — iOS 는 IOS_DEVICE_UDID 또는 DEVICE 이름으로 UDID 를 구해 쓰고, Android 는 ANDROID_SERIAL(없으면 연결 기기가 하나일 때 그 기기)을 `adb -s` 로 넘기며 여러 대인데 지정이 없으면 설명과 함께 실패한다"
    - "G-21-CR · IN-05: mobile/README.md 명령표가 package.json 의 native:* 스크립트와 일치한다(native:test:android 의 실제 테스트 클래스 · native:check-external-links:ios 행 포함)"
  artifacts:
    - path: mobile/ios/App/App/GHTradeBridgeViewController.swift
      provides: "키보드 즉시 숨김 · 재표시 90ms · awaitingContent · 초기 숨김 · 출처 전체 비교 · 오프라인 dev=1"
      contains: "awaitingContent"
    - path: mobile/ios/App/App/NavigationDelegateProxy.swift
      provides: "didStartProvisionalNavigation → owner.beginDocumentLoad()"
      contains: "didStartProvisionalNavigation"
    - path: mobile/scripts/smoke-ios.sh
      provides: "IOS_DEVICE_UDID · DEVICE 이름 → UDID"
      contains: "IOS_DEVICE_UDID"
    - path: mobile/scripts/smoke-android.sh
      provides: "ANDROID_SERIAL · adb -s"
      contains: "ANDROID_SERIAL"
  key_links:
    - from: mobile/ios/App/App/NavigationDelegateProxy.swift
      to: mobile/ios/App/App/GHTradeBridgeViewController.swift
      via: "didStartProvisionalNavigation(원본 먼저) → owner.beginDocumentLoad() → 첫 route 에서 endAwaitingContent()"
      pattern: "beginDocumentLoad"
    - from: mobile/ios/App/App/GHTradeBridgeViewController.swift
      to: mobile/www/index.html
      via: "showOffline 의 dev=1 → 폴백 페이지 localhost 복귀 허용(21-29)"
      pattern: "\"dev\""
  prohibitions:
    - statement: "MUST NOT 웹 코드나 브리지 메시지 화이트리스트(ready route theme overlay pull)를 바꾼다 — 문서 로드 대기 해제는 기존 route 메시지를 쓴다"
      status: unverified
      verification: "Task 2 acceptance — 화이트리스트 줄 grep · 이 플랜 커밋의 webapp/ 파일 0"
    - statement: "MUST NOT 스모크가 기기가 여러 대일 때 아무 기기나 조용히 고른다"
      status: unverified
      verification: "Task 3 acceptance — 「기기가 여러 대」 실패 메시지 grep"
---

<objective>
iOS 셸의 UAT 3차 갭과 리뷰 finding — **G-21-R3-1**(키보드 위 탭바 잔상) · **G-21-R3-4**(로그인 전환 중 탭바 먼저 노출) · **WR-03 iOS 측**(출처 호스트만 비교) · **IN-02 iOS 신호**(오프라인 dev=1) — 에 스모크·문서 정리 두 건(**IN-08** 기기 지정 · **IN-05** README 명령표)을 묶는다. 21-UAT.md 진단 · 21-REVIEW.md 수정안 그대로, 새 타입·표 없이 VC 안에서 직접 고친다(과설계 금지).

Purpose: 입력할 때 탭바가 키보드 위에 걸리지 않고, 로그인 직후 빈 화면 위에 탭바가 먼저 뜨지 않으며, 브리지는 정확히 같은 출처만 받는다. 스모크가 UAT 기기 여러 대 환경에서 엉뚱한 기기에 설치하지 않는다.
Output: VC · NavigationDelegateProxy 수정 · 스모크 두 스크립트 · README 표.

스모크 전제: dev 서버 :3100 이 떠 있어야 한다 — 없으면 `PORT=3100 pnpm --filter @gh-radar/webapp run dev` 를 백그라운드로 띄운다(**dev.sh 금지** — 다른 세션 포트를 죽인다). relay(:8090)는 띄우지 않는다.

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add(`git add -A` 금지) · 커밋 직전 `git status -sb` · **push 금지**(21-36).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-UAT.md
@.planning/phases/21-gh-trade-mobile-app/21-REVIEW.md
@mobile/ios/App/App/GHTradeBridgeViewController.swift
@mobile/ios/App/App/NavigationDelegateProxy.swift
@mobile/scripts/smoke-ios.sh
@mobile/scripts/smoke-android.sh
@mobile/README.md

<interfaces>
```text
GHTradeBridgeViewController:
  상태 hideWork(38) · isOfflinePage(40~) · keyboardVisible(44) · navProxy(57)
  userContentController(106~): isMainFrame · 화이트리스트 case ready, route, theme, overlay, pull(29) · 호스트 비교(117~122) → .route = applyPath
  setupTabBar(154~) — 끝에서 updateTabBarVisibility(animated:false)(초기 보임)
  showOffline(failedURL:)(269~): capacitor://localhost/index.html?to=&theme= (URLComponents)
  handleURL(299~): 호스트 + 스킴 비교(303) · applyPath(313)
  observeKeyboard(321~) → keyboardWillShow(327) / keyboardWillHide(332) → keyboardVisible → updateTabBarVisibility(animated:true)
  shouldHideTabBar(339) · updateTabBarVisibility(343~): 숨김 = asyncAfter 0.15 + animate 0.2(alpha 0 · translate y 8) / 보임 = animate 0.2
NavigationDelegateProxy: original(weak) · owner(weak) · decidePolicyFor · createWebViewWith · didFailProvisional/didFail(원본 먼저) · 나머지 자동 전달
WKSecurityOrigin: protocol · host · port(기본 포트면 0)
package.json native:test:android = TabRoutesTest + ExternalLinksTest · native:check-external-links:ios 존재(README 표에 행 없음)
smoke-ios.sh: simctl install/terminate/launch/spawn log/io screenshot 가 부팅 기기 별칭을 쓴다(49~70)
smoke-android.sh: adb devices(41) · wait-for-device(46) · getprop(48) · reverse(58) · install(68) · logcat(69,80,98) · am(70~71) · screencap(88)
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 키보드 사유 즉시 숨김(짧은 페이드 · 이동 없음) · 재표시 90ms · 하단 가림 판정 → 빌드 · 스모크 (G-21-R3-1 · D-12a)</name>
  <files>mobile/ios/App/App/GHTradeBridgeViewController.swift</files>
  <read_first>
    - mobile/ios/App/App/GHTradeBridgeViewController.swift (34~60 상태 · 319~400 키보드·표시/숨김)
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-R3-1 root_cause · missing)
    - .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md (D-12 · 21-25 가 쓴 D-12a)
  </read_first>
  <action>
    per D-12a: ① 상태 `private var showWork: DispatchWorkItem?` · `private var hiddenByKeyboard = false` · `private var keyboardAnimation: (duration: Double, options: UIView.AnimationOptions) = (0.25, .curveEaseInOut)` 를 추가한다. ② `keyboardWillShow`: userInfo 의 `keyboardAnimationDurationUserInfoKey`(Double) · `keyboardAnimationCurveUserInfoKey`(UInt → `UIView.AnimationOptions(rawValue: curve << 16)`)를 저장하고, `keyboardFrameEndUserInfoKey` 를 `view.convert(_, from: nil)` 한 사각형이 `height > 0 && minY < view.bounds.maxY - 1` 일 때만 `keyboardVisible = true` → `updateTabBarVisibility(animated: true)`. `keyboardWillHide`: `keyboardVisible = false` → 같은 호출. ③ `updateTabBarVisibility(animated:)` 숨김 분기: 사유가 키보드(`keyboardVisible`)이고 animated 면 `hideWork`/`showWork` 취소 → 지연 없이 `UIView.animate(withDuration: min(0.2, max(0.08, keyboardAnimation.duration * 0.5)), delay: 0, options: [keyboardAnimation.options, .beginFromCurrentState])` 로 alpha 0(transform identity 유지 — 이동 없음) → 완료 시 여전히 숨김이면 isHidden · `hiddenByKeyboard = true`. 다른 사유는 기존 경로(0.15 대기 + 0.2 페이드) 그대로. 보임 분기: `hiddenByKeyboard` 였으면 `showWork` 로 0.09초 뒤 표시(그 사이 숨김이 오면 취소 — willHide→willShow 연속 깜빡임 방지), 아니면 즉시 · 표시 뒤 `hiddenByKeyboard = false`. 섹션 주석 「D-12a — 키보드 = 즉시 · 짧게 · 재표시 90ms」.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:ios && pnpm --filter @gh-radar/mobile run native:smoke:ios</automated>
    <fails_when>non-zero exit, or output lacks "BUILD SUCCEEDED", "SIM ENTITLEMENTS OK" or "SMOKE OK ready platform=ios"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "keyboardAnimationDurationUserInfoKey" mobile/ios/App/App/GHTradeBridgeViewController.swift` ≥ 1 · `grep -c "keyboardFrameEndUserInfoKey" mobile/ios/App/App/GHTradeBridgeViewController.swift` ≥ 1
    - `grep -c "showWork" mobile/ios/App/App/GHTradeBridgeViewController.swift` ≥ 2 · `grep -c "0.09" mobile/ios/App/App/GHTradeBridgeViewController.swift` ≥ 1 · `grep -c "D-12a" mobile/ios/App/App/GHTradeBridgeViewController.swift` ≥ 1
    - verify 명령 통과
  </acceptance_criteria>
  <done>iOS 탭바가 키보드 사유에서는 즉시 · 짧게 · 이동 없이 사라지고, 다시 나타날 때 디바운스된다(체감은 21-36 UAT).</done>
</task>

<task type="auto">
  <name>Task 2: 문서 로드 대기(D-12b · G-21-R3-4) · 브리지 출처 전체 비교(WR-03 iOS) · 오프라인 dev=1 (IN-02 iOS)</name>
  <files>mobile/ios/App/App/NavigationDelegateProxy.swift, mobile/ios/App/App/GHTradeBridgeViewController.swift</files>
  <read_first>
    - mobile/ios/App/App/NavigationDelegateProxy.swift (전체 — 원본 먼저 호출 규칙)
    - mobile/ios/App/App/GHTradeBridgeViewController.swift (Task 1 결과 · 106~150 메시지 처리 · 154~190 setupTabBar · 269~290 showOffline)
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-R3-4 root_cause · decision)
    - .planning/phases/21-gh-trade-mobile-app/21-REVIEW.md (WR-03 「iOS 쪽도 securityOrigin 의 protocol/host/port」 · IN-02)
    - webapp/src/lib/native/native-bridge-provider.tsx (route 는 마운트 effect 뒤 경로마다 1회 = 하이드레이션 뒤 신호)
  </read_first>
  <action>
    ① D-12b: `NavigationDelegateProxy` 에 `webView(_:didStartProvisionalNavigation:)` — **원본 먼저** 부르고 `owner?.beginDocumentLoad()`(같은 문서 안 pushState 는 이 콜백이 오지 않는다 — 주석). VC: `private var awaitingContent = true`(콜드 스타트도 대기) · `private var awaitingWork: DispatchWorkItem?` · `func beginDocumentLoad()`(true · 1.5초 뒤 `endAwaitingContent()` 예약 · `updateTabBarVisibility(animated: true)`) · `private func endAwaitingContent()`(대기 중일 때만 false · 예약 취소 · 갱신). `.route` 분기에서 `applyPath` **전에** `endAwaitingContent()`. `shouldHideTabBar` 에 `awaitingContent` 를 더한다. `setupTabBar` 끝에서 탭바·페이드를 `isHidden = true` · `alpha = 0` 으로 시작. 대기 해제로 보일 때는 0.28초 `.curveEaseOut`(다른 보임은 0.2 그대로). 웹 코드·화이트리스트는 바꾸지 않는다.
    ② WR-03 iOS: 메시지 처리의 요청 URL 호스트만 비교하던 검사(117~122행)를 `let o = message.frameInfo.securityOrigin` · `let s = bridge?.config.serverURL` 로 스킴(소문자) · 호스트(소문자) · 포트(`o.port` vs `s?.port ?? 0` — WKSecurityOrigin 은 기본 포트를 0 으로 준다) 전부 정확 일치 판정으로 바꾼다(하나라도 nil 이면 거부 · isMainFrame 유지 · 로그 「ignored … from foreign origin」). 별도 헬퍼 타입은 만들지 않는다.
    ③ IN-02 iOS: `showOffline` 에서 서버 URL 이 `scheme == "http"` 이고 호스트가 `localhost` 또는 `127.0.0.1` 이면 `URLQueryItem(name: "dev", value: "1")` 을 더한다(운영 https 에는 없음 · 주석 「IN-02 — 폴백 페이지는 dev=1 일 때만 localhost 복귀」).
    ④ SUMMARY 에 콜드 스타트 → 탭바 표시 시각을 한 번 실측해 적는다(참고값 · 판정은 UAT). 스모크 통과 = dev 출처 `http://localhost:3100` 의 ready 가 새 출처 판정을 통과한 것.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:ios && pnpm --filter @gh-radar/mobile run native:smoke:ios</automated>
    <fails_when>non-zero exit, or output lacks "BUILD SUCCEEDED", "SIM ENTITLEMENTS OK" or "SMOKE OK ready platform=ios"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "didStartProvisionalNavigation" mobile/ios/App/App/NavigationDelegateProxy.swift` ≥ 1 · `grep -c "beginDocumentLoad" mobile/ios/App/App/NavigationDelegateProxy.swift` ≥ 1
    - `grep -c "private var awaitingContent = true" mobile/ios/App/App/GHTradeBridgeViewController.swift` ≥ 1 · `grep -c "endAwaitingContent" mobile/ios/App/App/GHTradeBridgeViewController.swift` ≥ 2 · `grep -c "1.5" mobile/ios/App/App/GHTradeBridgeViewController.swift` ≥ 1
    - `grep -c "frameInfo.securityOrigin" mobile/ios/App/App/GHTradeBridgeViewController.swift` ≥ 1
    - 호스트만 비교하던 줄 제거: `test "$(grep -v -E '^[[:space:]]*//' mobile/ios/App/App/GHTradeBridgeViewController.swift | grep -c 'let senderHost')" -eq 0`
    - `grep -c 'name: "dev", value: "1"' mobile/ios/App/App/GHTradeBridgeViewController.swift` ≥ 1
    - 화이트리스트 무변경: `grep -c "case ready, route, theme, overlay, pull" mobile/ios/App/App/GHTradeBridgeViewController.swift` ≥ 1
    - 이 플랜 커밋이 웹을 건드리지 않는다: `L="$(git log --grep='(21-28)' --name-only --format=)" && test "$(printf '%s\n' "$L" | grep -c '^webapp/')" -eq 0`
    - verify 명령 통과
  </acceptance_criteria>
  <done>iOS 는 전체 문서 로드마다 탭바를 숨긴 채 기다렸다가 하이드레이션 뒤(또는 1.5초) 부드럽게 보이고, 브리지는 스킴·호스트·포트까지 같은 출처만 받으며, dev 빌드만 폴백에 dev 신호를 붙인다.</done>
</task>

<task type="auto">
  <name>Task 3: 스모크 기기 지정(IN-08) · README 명령표 = package.json native:* (IN-05)</name>
  <files>mobile/scripts/smoke-ios.sh, mobile/scripts/smoke-android.sh, mobile/README.md</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-REVIEW.md (IN-05 · IN-08)
    - mobile/scripts/smoke-ios.sh · mobile/scripts/smoke-android.sh (전체)
    - mobile/README.md (30~45 명령표) · mobile/package.json (scripts)
  </read_first>
  <action>
    ① IN-08 iOS: `UDID="${IOS_DEVICE_UDID:-}"` 가 비면 `xcrun simctl list devices available -j` 를 `node -e` 로 읽어 이름이 `DEVICE` 와 같은 기기 중 Booted 우선 · 없으면 첫 UDID(못 찾으면 「SMOKE FAIL — 시뮬레이터 "${DEVICE}" 없음」 exit 1). 이후 simctl 의 boot · bootstatus · install · terminate · launch · spawn log · io screenshot 기기 인자를 모두 `"${UDID}"` 로 바꾼다. 시작 시 「── 시뮬레이터: ${DEVICE} (${UDID})」 출력 · 머리 주석 환경변수에 `IOS_DEVICE_UDID`.
    ② IN-08 Android: 기기 확보 뒤 `SERIAL="${ANDROID_SERIAL:-}"` 가 비면 `adb devices` 의 `device` 줄이 1개면 그 시리얼, 2개 이상이면 「SMOKE FAIL — 기기가 여러 대다 · ANDROID_SERIAL 을 지정하세요(목록: …)」 exit 1. 이후 모든 adb 호출을 `ADB=(adb -s "${SERIAL}")` 배열로 부른다. 시작 시 시리얼 출력 · 머리 주석 환경변수에 `ANDROID_SERIAL`.
    ③ IN-05: README 명령표를 package.json `native:*` 와 1:1 로 — `native:test:android` 행 = `TabRoutesTest` · `ExternalLinksTest`, `native:check-external-links:ios` 행 신설(「iOS 인앱 브라우저 링크 판정표를 swiftc 로 검사. 성공 `EXTERNAL LINKS OK`」), 스모크 두 행에 환경변수(`DEVICE` · `IOS_DEVICE_UDID` · `AVD` · `ANDROID_SERIAL`)와 「여러 기기면 지정 필수」. 표 밖 문단은 바꾸지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && node -e "const s=Object.keys(require('./mobile/package.json').scripts).filter(k=>k.startsWith('native:'));const r=require('fs').readFileSync('mobile/README.md','utf8');const miss=s.filter(k=>!r.includes('\`'+k+'\`')&&!r.includes('\`'+k+'\` ·'));if(miss.length){console.log('MISSING '+miss.join(','));process.exit(1)}console.log('README TABLE OK '+s.length)" && bash -n mobile/scripts/smoke-ios.sh && bash -n mobile/scripts/smoke-android.sh</automated>
    <fails_when>non-zero exit, or output contains "MISSING", or "README TABLE OK" is absent, or bash -n reports a syntax error</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:smoke:ios && pnpm --filter @gh-radar/mobile run native:smoke:android</automated>
    <fails_when>non-zero exit, or output lacks "SMOKE OK ready platform=ios" or "SMOKE OK ready platform=android", or contains "SMOKE FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - iOS: `grep -c "IOS_DEVICE_UDID" mobile/scripts/smoke-ios.sh` ≥ 1 · `grep -c 'UDID}"' mobile/scripts/smoke-ios.sh` ≥ 3
    - iOS 부팅 별칭 제거(주석 제외): `test "$(grep -v -E '^[[:space:]]*#' mobile/scripts/smoke-ios.sh | grep -c 'simctl [a-z]* booted')" -eq 0`
    - Android: `grep -c "ANDROID_SERIAL" mobile/scripts/smoke-android.sh` ≥ 2 · `grep -c "adb -s" mobile/scripts/smoke-android.sh` ≥ 1 · `grep -c "기기가 여러 대" mobile/scripts/smoke-android.sh` ≥ 1
    - README: `grep -c "native:check-external-links:ios" mobile/README.md` ≥ 1 · `grep -c "ExternalLinksTest" mobile/README.md` ≥ 1 · `grep -c "ANDROID_SERIAL" mobile/README.md` ≥ 1
    - verify 두 명령 통과
  </acceptance_criteria>
  <done>스모크가 지정 기기에만 설치·실행하고 모호하면 멈추며, README 명령표가 실제 스크립트와 같다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView 문서 → 네이티브 브리지(WKScriptMessage) | 앱 서버가 아닌 문서가 탭바·테마·당김 메시지를 보낼 수 있다 |
| 오프라인 폴백 → 복귀 URL | 운영 번들이 dev 출처로 복귀하면 다른 로컬 서비스를 연다 |
| 스모크 → 기기 | 여러 기기 중 엉뚱한 기기에 dev 빌드를 설치 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-02 | Spoofing | iOS 브리지 출처 호스트만 비교(WR-03 iOS) | medium | mitigate | securityOrigin 스킴·호스트·포트 정확 일치 + isMainFrame(Task 2) · 스모크 ready 로 dev 출처 통과 확인 |
| T-21-06 | Tampering | 오프라인 폴백 dev 출처 복귀(IN-02) | low | mitigate | dev 빌드(루프백 http)에서만 dev=1(Task 2) · 페이지 쪽 허용 목록 분리는 21-29 |
| T-21-65 | Denial of Service | 탭바 숨김 상태 고착(awaitingContent 해제 누락) | medium | mitigate | 1.5초 타임아웃이 항상 해제 · 첫 route 에서 해제(Task 2) |
| T-21-66 | Tampering | 스모크가 다른 기기에 dev 빌드 설치(IN-08) | low | mitigate | UDID/시리얼 명시 · 여러 대면 실패(Task 3) |
</threat_model>

<verification>
- native:build:ios · native:smoke:ios(세 번 — 태스크마다) · native:smoke:android · README 표 대조 · bash -n
- 사람 확인(키보드 위 잔상 없음 · 로그인 직후 탭바 순서)은 21-36 UAT
</verification>

<success_criteria>
- G-21-R3-1 · R3-4 의 iOS 쪽과 WR-03 iOS · IN-02 신호 · IN-08 · IN-05 가 닫혔다.
</success_criteria>

## Flagged assumptions

- **엣지 탐침 MOBILE-01 (unclassified · unresolved):** 경계는 키보드 하단 가림 판정 · 대기 1.5초 상한 · 출처 세 요소로 명시했다. 나머지는 21-36 UAT.
- 키보드 애니메이션 곡선 값(7)은 공개 enum 밖이라 `rawValue << 16` 으로 옮긴다 — UIKit 관례. 체감은 UAT 에서 본다.

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| 스케치 `.planning/sketches/008-uat3-surfaces/`(index.html · README.md) · MANIFEST 008 행 | 목업 | 21-25 |
| CONTEXT D-12a · D-12b · D-25a · D-29 · D-30 · D-31 · D-32(R3-11 축소: 스크롤 복원 + 스켈레톤 없음 · 상태 초기화 허용) · WR-03 androidx.webkit 선언 답 | 결정 기록 | 21-25 |
| `PAD_CHIPS.만원` = 천만(+1,000) · 오천만(+5,000) · 1억(+10,000) · 지우기 · 20-CONTEXT D-17 포인터 | 상수 · 결정 | 21-26 |
| `SettingGroup` 제목 없는 그룹 상단 `pt-1` | 클래스 | 21-26 |
| 카드 헤더 ✕ `data-slot="card-close"` · 종목정보 팝업 ✕ = `size-8` + lucide `XIcon` + `after:-inset-1.5`(히트 44) · 팝업 고정 높이 · `scrollbar-gutter:stable` · 폰 safe-area 패딩 | 컴포넌트 | 21-26 |
| `webapp/src/lib/safe-path.ts` `isSafeInternalPath` · `recent-search.ts` `RECENT_CODE_RE` · `PopoverContent` 안 `NativeOverlayMarker` · `useDebouncedSearch` 반환 `resultsQuery` · `viewport.themeColor` `#17171c` + `ThemeColorSync` | 함수 · 훅 · 메타 | 21-27 |
| iOS 키보드 사유 즉시 숨김 · 재표시 90ms · `awaitingContent`(didStartProvisionalNavigation → 첫 route/1.5초) · 초기 숨김 · 브리지 출처 scheme·host·port 비교 · 오프라인 `dev=1` · 스모크 기기 지정(`IOS_DEVICE_UDID` · `ANDROID_SERIAL`) · mobile/README 명령표 | Swift · 스크립트 · 문서 | 21-28 |
| Android IME 애니메이션 콜백 즉시 숨김 · `awaitingContent`(onPageStarted) · `pullBlocked` 초기화 · 오프라인 뒤로가기(재현 시) · 오프라인 `dev=1` · 폴백 페이지 dev=1 일 때만 localhost · allowBackup=false + `res/xml/data_extraction_rules.xml` · 브리지 WebMessageListener(21-25 답 a) | Kotlin · XML · HTML | 21-29 |
| `NewsFullList` · `DiscussionFullList` · `useNewsView` · `StockNewsTabPanel` · 섹션 `onShowAll` · 종목상세 `?tab=news&view=news\|discussions` · 옛 URL 리다이렉트 · 팝업 `newsView` | 컴포넌트 · 훅 · 라우트 | 21-30 |
| `useTabRootScrollMemory`(`webapp/src/lib/tab-scroll-memory.ts` · AppShell 에서 호출) · 기존 `lib/query-cache` 재사용: 검색 허브 · 대화 목록 · 오늘 주문 | 훅 · 캐시 | 21-31 |
| 앱 공용 패널 숨김 CSS · 카드 더티 바 탭바 비킴(프로브) · `StrategyLogFeedProvider` · `useStrategyLogFeed` · `diffLimitChasers` · /me 전략 로그(`me-strategy-log`) | CSS · Provider · 컴포넌트 | 21-32 |
| 작업대 `?code=` 소비(카드 보장·펼침·포커스) · 카드 수동주문 주문유형(지정가/시간외종가) | 컴포넌트 | 21-33 |
| 종목상세 「트레이딩」 CTA(폰 바 + `detail-trading-button`) · 호가주문 탭 제거 · `?tab=orderbook` → `/trading?code=` · 호가 탭 전용 코드·`variant` 삭제 · orderbook.spec 선별 이전 | 컴포넌트 · 정리 · e2e | 21-34 |
| 게이트 기록 · `21-REVIEW-R2.md`(13건 대조) · UAT 환경(21-35-SUMMARY) · UAT 3차 재검증 · push 결정(21-36-SUMMARY) | 문서 | 21-35 · 21-36 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-28-SUMMARY.md` when done — 키보드 · 대기 수치 · 출처 판정 · dev=1 조건 · 콜드 스타트 실측 · 스모크 기기 · README 행 목록.
</output>
