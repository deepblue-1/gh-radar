---
phase: 21-gh-trade-mobile-app
plan: 17
type: execute
wave: 8
depends_on: ["21-16"]
files_modified:
  - mobile/android/variables.gradle
  - mobile/android/app/build.gradle
  - mobile/android/app/src/main/java/com/ghtrade/app/ExternalLinks.kt
  - mobile/android/app/src/main/java/com/ghtrade/app/InAppBrowser.kt
  - mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt
  - mobile/android/app/src/test/java/com/ghtrade/app/ExternalLinksTest.kt
  - mobile/package.json
autonomous: false
gap_closure: true
requirements: [MOBILE-01]
coupling_justified:
  - "21-21: 21-21 이 GhTradePalette(line → glass)와 MainActivity.currentTheme 초기값을 바꾸지만 이 플랜은 GhTradePalette.of(theme).bg 와 host.currentTheme getter 만 읽는다 — 어느 순서로 실행해도 컴파일·동작이 같다"
  - "21-18: 두 플랜 모두 cap sync · assembleDebug · 에뮬레이터를 쓴다 — 스모크가 끝나면 운영 sync 로 복원되고 parallelization=false 라 순차 실행된다"

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G-21-N3 (Android · D-28): 앱에서 서버 호스트 밖 http(s) 링크(홈·종목상세 뉴스 · 종토방 원문 · AI 챗 인용 등 target=_blank 앵커 포함)를 누르면 외부 브라우저 앱으로 전환되지 않고 Custom Tabs(앱 위 인앱 브라우저)로 열리며, 닫으면 앱이 그 화면 그대로다 — Task 1 선택이 a 또는 b 일 때"
    - "같은 호스트(server.url 호스트) 이동과 로컬 오프라인 페이지(https://localhost)는 WebView 안에 머문다 — 스모크에서 앱 호스트 문서가 실려 ready 가 온다"
    - "mailto:·tel:·앱 딥링크 등 비 http(s) 는 지금처럼 Capacitor 기본 경로(시스템 인텐트)로 간다 · 하위 프레임(iframe) 이동은 가로채지 않는다"
    - "분류 규칙은 순수 함수 ExternalLinks.opensInAppBrowser(scheme, host, appHost) 한 곳이고 JUnit 표(ExternalLinksTest 26케이스)가 잠근다 — iOS 21-22 가 같은 표를 복제한다"
    - "androidx.browser 사용 여부는 사용자 결정(Task 1)대로다 — 선택 a 면 앱 모듈 선언 버전이 capgo 가 이미 끌어오는 1.9.0 과 같아 APK 의존 집합이 바뀌지 않는다"
  artifacts:
    - path: mobile/android/app/src/main/java/com/ghtrade/app/ExternalLinks.kt
      provides: "호스트 밖 http(s) 판정 순수 함수(D-28)"
      contains: "fun opensInAppBrowser"
    - path: mobile/android/app/src/main/java/com/ghtrade/app/InAppBrowser.kt
      provides: "Custom Tabs 실행(테마 색 · 호스트만 로깅)"
      contains: "object InAppBrowser"
    - path: mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt
      provides: "shouldOverrideUrlLoading 가로채기(메인 프레임 · 호스트 밖 http(s) 만)"
      contains: "shouldOverrideUrlLoading"
    - path: mobile/android/app/src/test/java/com/ghtrade/app/ExternalLinksTest.kt
      provides: "iOS 와 공유하는 정본 판정 표 JUnit"
      contains: "class ExternalLinksTest"
  key_links:
    - from: mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt
      to: mobile/android/app/src/main/java/com/ghtrade/app/ExternalLinks.kt
      via: "shouldOverrideUrlLoading → ExternalLinks.opensInAppBrowser(request.url.scheme, request.url.host, host.serverHost())"
      pattern: "ExternalLinks\\.opensInAppBrowser"
    - from: mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt
      to: mobile/android/app/src/main/java/com/ghtrade/app/InAppBrowser.kt
      via: "InAppBrowser.open(host, request.url, host.currentTheme) 후 true"
      pattern: "InAppBrowser\\.open"
    - from: mobile/package.json
      to: mobile/android/app/src/test/java/com/ghtrade/app/ExternalLinksTest.kt
      via: "native:test:android --tests com.ghtrade.app.ExternalLinksTest"
      pattern: "ExternalLinksTest"
  prohibitions:
    - "MUST NOT 같은 호스트(앱 서버) 이동이나 로컬 오프라인 페이지를 WebView 밖(인앱 브라우저·시스템)으로 보낸다"
    - "MUST NOT 비 http(s) 스킴(javascript: · data: · file: · blob: · intent: · about:)을 Custom Tabs 로 넘기거나 intent: URI 를 Intent 로 파싱해 실행한다"
    - "MUST NOT mailto: · tel: · 앱 딥링크의 시스템 핸드오프를 깨뜨린다"
    - "MUST NOT 외부 URL 에 세션 쿠키·토큰·식별 쿼리를 덧붙이거나 전체 URL(경로·쿼리)을 로그에 남긴다 — 로그는 호스트만"
    - "MUST NOT 사용자 결정(Task 1) 없이 새 Gradle 의존성을 선언한다"
---

<objective>
G-21-N3 (Android): 앱 WebView 에서 **서버 호스트 밖 http(s) 최상위 이동**을 Custom Tabs(앱 위 인앱 브라우저)로 연다. 같은 호스트 · 로컬 오프라인 페이지 · 비 http(s)(mailto · tel · 딥링크)는 지금의 Capacitor 기본 경로 그대로 둔다. 웹 코드와 npm 패키지는 건드리지 않는다(21-UAT.md G-21-N3 사용자 결정 2026-09-26 · CONTEXT D-28 은 21-20 이 기록).

Purpose: 뉴스·종토방 원문을 읽고 닫으면 곧바로 앱의 같은 화면으로 돌아온다(외부 Chrome 앱 전환 없음). 분류 규칙을 순수 함수 하나로 두어 iOS(21-22)와 같은 표로 잠근다.
Output: Kotlin 3파일(ExternalLinks · InAppBrowser · WebViewClient 가로채기) · JUnit 표 1 · `native:test:android` 확장 · (선택 a 일 때) Gradle 선언 1줄 + 버전 변수.

**플래너 실측(2026-09-26, 재확인 대상):**
- `androidx.browser:browser:1.9.0` 은 앱 **런타임** 클래스패스에 이미 있다 — `:capgo-capacitor-social-login` 의 공통 `implementation 'androidx.browser:browser:1.9.0'` 이 APK 에 싣는다.
- 그러나 앱 **컴파일** 클래스패스에는 없다(`debugCompileClasspath` 0건). 라이브러리의 `implementation` 의존은 소비 모듈 컴파일에 노출되지 않는다 → 앱 코드가 `CustomTabsIntent` 를 쓰려면 앱 모듈 선언이 새로 필요하다. 그래서 Task 1 결정 게이트(사용자 지시 「방식은 권장안으로 진행하되 플랜에서 재확인」 · 패키지 정당성 게이트)를 둔다.
- Capacitor 8.5.2 Android 는 멀티 윈도우를 켜지 않는다(소스에 `setSupportMultipleWindows` 0건) → `target=_blank` · `window.open` 도 메인 프레임 이동으로 `shouldOverrideUrlLoading` 을 지난다. 새 창 전용 처리(`onCreateWindow`)는 필요 없다 — Task 2 가 다시 확인한다.
- 설치된 플러그인 중 `shouldOverrideLoad` 를 구현한 것은 없다(capgo social-login 0건) — 가로채기가 플러그인 판단을 빼앗지 않는다.
- 앱 로그인은 21-15 네이티브 Google 로그인이라 `/login` 앱 분기는 `signInWithOAuth` 전에 반환한다 → 시스템 브라우저로 꼭 넘겨야 하는 호스트(Supabase OAuth · Google 계정 도메인)는 남아 있지 않다. 예외 목록을 두지 않고 `server.allowNavigation` 도 계속 비운다(verify-prod 가 검사).

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add · 커밋 직전 `git status -sb` · **push 금지**(push = Vercel 웹 배포 — 21-24 체크포인트에서 사용자가 정한다).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-UAT.md
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-13-SUMMARY.md
@mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt
@mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
@mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt
@mobile/android/app/build.gradle
@mobile/android/variables.gradle
@mobile/package.json

<interfaces>
```text
MainActivity (21-12 · 21-13):
  fun serverHost(): String?            // server.url 호스트(없으면 appUrl 호스트) — 공개
  var currentTheme: String             // "dark" | "light" — 공개 getter
  companion object { const val TAG = "GHTrade" }
GhTradeWebViewClient(bridge: Bridge, private val host: MainActivity) : BridgeWebViewClient(bridge)
Capacitor BridgeWebViewClient.shouldOverrideUrlLoading(view, request) = bridge.launchIntent(request.url)
  launchIntent: 플러그인 shouldOverrideLoad → data/blob false → 앱 호스트+스킴 같으면 false(WebView 로드)
                → 그 외 Intent(ACTION_VIEW) startActivity 후 true (= 지금의 외부 브라우저 동작)
GhTradePalette.of(theme: String).bg : Int  // 다크 0xFF17171C · 라이트 0xFFFFFFFF
TabRoutesTest.kt(21-12) = JUnit4 표 테스트 모양의 선례
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Android Custom Tabs 구현 경로 결정 — androidx.browser 앱 모듈 선언 여부 (패키지 정당성 게이트)</name>
  <read_first>
    - mobile/android/app/build.gradle · mobile/android/variables.gradle (앱 의존성과 버전 변수 관례)
    - mobile/android/capacitor.settings.gradle (capgo social-login 모듈 경로 — 공통 의존에 androidx.browser 1.9.0)
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-N3 `decision` 줄)
  </read_first>
  <decision>앱 코드에서 Custom Tabs 를 여는 방법 — androidx.browser 를 앱 모듈에 직접 선언할지</decision>
  <context>
    실행기는 체크포인트를 띄우기 **전에** 두 명령을 다시 돌려 결과 줄을 메시지에 붙인다(플래너 실측의 재확인):
    ① `cd /Users/alex/repos/gh-radar/mobile/android && ./gradlew :app:dependencies --configuration debugRuntimeClasspath -q | grep -n 'androidx.browser'` — 기대: `androidx.browser:browser:1.9.0` 1줄(capgo 하위).
    ② 같은 명령에 `--configuration debugCompileClasspath` — 기대: 0줄.
    ②가 1줄 이상이면 앱이 이미 컴파일할 수 있는 상태이므로 선택 a 가 「새 선언 없음」으로 바뀐다 — 그 사실을 메시지에 적는다.
    사용자 결정(2026-09-26)은 「앱 안 브라우저 = Android Custom Tabs, 새 npm 패키지 없음」이다. Gradle 선언은 npm 이 아니지만 새 직접 의존 선언이므로 사람이 고른다. 권장은 a.
  </context>
  <options>
    <option id="a">
      <name>androidx.browser 1.9.0 을 앱 모듈에 선언 (권장)</name>
      <pros>공식 API(`CustomTabsIntent` · 다크/라이트 색 구성)를 타입 검사와 함께 쓴다. 좌표·버전이 capgo 가 이미 APK 에 싣는 것과 같아 APK 의존 집합이 바뀌지 않는다(Google Maven 의 AndroidX 공식 아티팩트). npm·lockfile 무변경.</pros>
      <cons>앱 모듈에 직접 선언 1줄 + 버전 변수 1줄이 생긴다. capgo 가 나중에 버전을 올리면 Gradle 이 높은 쪽을 고른다(무해하지만 추적 대상).</cons>
    </option>
    <option id="b">
      <name>선언 없이 Custom Tabs 프로토콜 인텐트</name>
      <pros>빌드 파일 무변경. Chrome 등 Custom Tabs 지원 브라우저에서 같은 인앱 탭이 열린다(세션 extra 를 실은 ACTION_VIEW).</pros>
      <cons>extra 키 문자열을 손으로 적는다(안정된 공개 프로토콜이지만 타입 검사 없음) — androidx.browser 1.9.0 상수와 대조해 SUMMARY 에 남겨야 한다.</cons>
    </option>
    <option id="c">
      <name>Android 만 시스템 브라우저(ACTION_VIEW) — 의존성 0</name>
      <pros>가장 단순하고 빌드 파일 무변경.</pros>
      <cons>G-21-N3 Android truth 를 충족하지 못한다(지금처럼 외부 Chrome 앱으로 전환). 사용자 결정의 축소이므로 SUMMARY Deviations 에 「사용자 선택」으로 적고 21-24 UAT 항목 4 의 Android 기대값을 「외부 브라우저」로 바꾼다.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - 재개 신호가 `a` · `b` · `c` 중 하나다
    - 체크포인트 메시지에 ①·② 명령의 실제 출력 줄(또는 0줄)이 붙어 있다
    - 선택 · 근거 · ①② 결과가 21-17-SUMMARY 「Decisions」 에 기록된다
  </acceptance_criteria>
  <resume-signal>`a` / `b` / `c` 중 하나로 답한다</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: 트레이서 — 호스트 밖 http(s) 탭 → ExternalLinks 판정 → GhTradeWebViewClient 가로채기 → Custom Tabs(Task 1 선택대로)</name>
  <reversibility rating="reversible">빌드 선언 1줄(선택 a) 또는 0줄 — 되돌리면 Capacitor 기본(시스템 브라우저)으로 돌아간다. 웹 · 저장 형식 · 공개 계약 변화 없음.</reversibility>
  <files>mobile/android/app/src/main/java/com/ghtrade/app/ExternalLinks.kt, mobile/android/app/src/main/java/com/ghtrade/app/InAppBrowser.kt, mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt, mobile/android/variables.gradle, mobile/android/app/build.gradle</files>
  <read_first>
    - mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt (super 먼저 부르는 규칙 · Pitfall 4)
    - mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt (`serverHost()` · `currentTheme` · `TAG` · `showOffline` 이 로컬 페이지를 `https://localhost` 로 싣는 이유)
    - mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt (`of(theme).bg`)
    - Capacitor Android `Bridge.java`(`launchIntent`) · `BridgeWebViewClient.java` — 위치: mobile/ 에서 `node -p "require('path').dirname(require.resolve('@capacitor/android/package.json'))"` 결과 뒤 `/capacitor/src/main/java/com/getcapacitor/`
    - 21-17-SUMMARY 초안의 Task 1 선택
  </read_first>
  <action>
    ① `ExternalLinks.kt`(신규, 순수 Kotlin — `java.util.Locale` 외 import 없음, JVM 단위 테스트 대상): `object ExternalLinks` 에 `fun opensInAppBrowser(scheme: String?, host: String?, appHost: String?): Boolean`. 판정 순서(하나라도 걸리면 false): scheme 을 `Locale.ROOT` 소문자로 바꿔 `http`·`https` 가 아니면 false(비 http(s) 는 전부 Capacitor 기본 경로에 맡긴다) → host 가 null·빈 문자열이면 false → appHost 가 null·빈 문자열이면 false(앱 호스트를 모르면 가로채지 않는다) → host 와 appHost 를 소문자로 **정확히 같은지**만 비교해 같으면 false(접미사·포함 비교 금지 — 위장 호스트는 외부로 본다, T-21-44) → host 가 루프백 집합 `localhost` · `127.0.0.1` · `::1` · `[::1]` 이면 false(로컬 에셋 서버 오프라인 폴백 · dev 서버) → 그 외 true. KDoc: D-28 · 「iOS 21-22 `ExternalLinks.swift` 와 같은 표를 유지한다 — 표를 고치면 두 파일을 함께」.
    ② `InAppBrowser.kt`(신규): `object InAppBrowser` 에 `fun open(activity: Activity, uri: Uri, theme: String)`. Task 1 선택별 본문:
      - 선택 a: `CustomTabsIntent.Builder()` 에 `setShowTitle(true)` · `setColorScheme(다크면 COLOR_SCHEME_DARK, 아니면 COLOR_SCHEME_LIGHT)` · `setDefaultColorSchemeParams(CustomTabColorSchemeParams.Builder().setToolbarColor(GhTradePalette.of(theme).bg).build())` → `build().launchUrl(activity, uri)`.
      - 선택 b: `Intent(Intent.ACTION_VIEW, uri)` 에 Bundle(`putBinder("android.support.customtabs.extra.SESSION", null)`)을 `putExtras` 하고 extra `android.support.customtabs.extra.TITLE_VISIBILITY` = 1 · `android.support.customtabs.extra.TOOLBAR_COLOR` = `GhTradePalette.of(theme).bg` · `androidx.browser.customtabs.extra.COLOR_SCHEME` = 다크 2 / 라이트 1 → `activity.startActivity`. 키 문자열과 정수값은 Gradle 캐시의 androidx.browser 1.9.0 AAR `CustomTabsIntent` 상수(`javap -constants`)와 대조해 SUMMARY 에 적는다 — 기억으로 적지 않는다.
      - 선택 c: `Intent(Intent.ACTION_VIEW, uri)` 로 시스템 브라우저. SUMMARY Deviations 에 사용자 선택으로 적는다.
      공통: 실행 직전 `Log.i(MainActivity.TAG, "in-app browser host=${uri.host}")` — 로그에는 **호스트만**(경로·쿼리·전체 URL 금지 — T-21-45). `ActivityNotFoundException` 은 잡아서 `Log.w` 로 호스트만 남기고 삼킨다(WebView 는 제자리). URL 에 쿼리·쿠키·헤더를 덧붙이지 않는다.
    ③ `GhTradeWebViewClient.kt`: `override fun shouldOverrideUrlLoading(view: WebView?, request: WebResourceRequest?): Boolean` — `request != null && request.isForMainFrame && ExternalLinks.opensInAppBrowser(request.url.scheme, request.url.host, host.serverHost())` 이면 `InAppBrowser.open(host, request.url, host.currentTheme)` 후 `true`. 그 외에는 `super.shouldOverrideUrlLoading(view, request)` 를 그대로 반환한다(같은 호스트 → WebView · 비 http(s) → Capacitor 시스템 인텐트 · data/blob → WebView). 하위 프레임은 가로채지 않는다. 옛 문자열 오버로드는 만들지 않는다. 클래스 KDoc 에 D-28 한 줄 추가. 멀티 윈도우 설정은 켜지 않는다(새 창 요청이 메인 프레임 이동으로 오는 전제를 유지).
    ④ 선택 a 에서만: `variables.gradle` 에 `androidxBrowserVersion = '1.9.0'`(capgo 가 끌어오는 버전과 같게) · `app/build.gradle` dependencies 에 `implementation "androidx.browser:browser:$androidxBrowserVersion"`. 선택 b · c 는 두 Gradle 파일을 건드리지 않는다. AndroidManifest 는 어느 선택에서도 바꾸지 않는다(서비스 바인딩을 하지 않으므로 패키지 가시성 선언 불필요).
    ⑤ 확인: `native:build:android`. webapp dev 서버가 `http://localhost:3100` 에 없으면 `PORT=3100 pnpm --filter @gh-radar/webapp run dev` 로 띄운다(dev.sh 쓰지 않음 — 다른 세션 포트를 죽인다). 그다음 `native:smoke:android` — 같은 호스트 문서가 WebView 에 실려 `ready` 가 도착하면 가로채기가 앱 호스트를 빼앗지 않았다는 증명이다(스모크는 끝나면 운영 sync 로 복원).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:android</automated>
    <fails_when>non-zero exit, or output lacks "BUILD SUCCESSFUL", or contains "Unresolved reference"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:smoke:android</automated>
    <fails_when>non-zero exit, or output lacks "SMOKE OK ready platform=android", or contains "SMOKE FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "fun opensInAppBrowser(scheme: String?, host: String?, appHost: String?): Boolean" mobile/android/app/src/main/java/com/ghtrade/app/ExternalLinks.kt` ≥ 1
    - `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*)' mobile/android/app/src/main/java/com/ghtrade/app/ExternalLinks.kt | grep -c '^import android')" -eq 0` (순수 Kotlin)
    - GhTradeWebViewClient.kt: `grep -c "request.isForMainFrame"` ≥ 1 · `grep -c "ExternalLinks.opensInAppBrowser"` ≥ 1 · `grep -c "InAppBrowser.open(host, request.url, host.currentTheme)"` ≥ 1 · `grep -c "super.shouldOverrideUrlLoading(view, request)"` ≥ 1
    - InAppBrowser.kt 의 모든 로그 호출이 호스트만 싣는다: `test "$(grep -cE 'Log\.[iwd]\(' mobile/android/app/src/main/java/com/ghtrade/app/InAppBrowser.kt)" -eq "$(grep -E 'Log\.[iwd]\(' mobile/android/app/src/main/java/com/ghtrade/app/InAppBrowser.kt | grep -c 'uri.host')"` · `grep -c "in-app browser host=" InAppBrowser.kt` ≥ 1 · `grep -c "ActivityNotFoundException" InAppBrowser.kt` ≥ 1
    - 선택 a: `grep -c "androidxBrowserVersion = '1.9.0'" mobile/android/variables.gradle` ≥ 1 · `grep -c 'androidx.browser:browser:$androidxBrowserVersion' mobile/android/app/build.gradle` ≥ 1 · `cd mobile/android && ./gradlew :app:dependencies --configuration debugCompileClasspath -q | grep -c 'androidx.browser:browser:1.9.0'` ≥ 1 · 같은 명령 `debugRuntimeClasspath` 출력에 `androidx.browser:browser:1.9.0` 이 있고 다른 버전으로의 승격 표기(`1.9.0 ->`)가 없다
    - 선택 b · c: `git diff --quiet -- mobile/android/app/build.gradle mobile/android/variables.gradle` 가 exit 0 · 선택 b 는 `grep -c "android.support.customtabs.extra.SESSION" InAppBrowser.kt` ≥ 1 이고 SUMMARY 에 AAR 상수 대조 결과가 있다
    - 멀티 윈도우 미사용 재확인: `grep -rl setSupportMultipleWindows "$(cd mobile && node -p "require('path').dirname(require.resolve('@capacitor/android/package.json'))")/capacitor/src" mobile/android/app/src` 출력 0줄
    - `git diff --quiet -- mobile/android/app/src/main/AndroidManifest.xml mobile/capacitor.config.ts` exit 0 (매니페스트 · allowNavigation 무변경)
    - verify 두 명령 통과
  </acceptance_criteria>
  <done>앱에서 호스트 밖 http(s) 메인 프레임 이동이 Task 1 선택대로 Custom Tabs(또는 선택 c 의 시스템 브라우저)로 가고, 같은 호스트 앱 문서는 WebView 에 그대로 실린다(스모크 ready).</done>
</task>

<task type="auto">
  <name>Task 3: 판정 표 JUnit(ExternalLinksTest) — iOS 21-22 와 공유하는 정본 26케이스 + native:test:android 확장</name>
  <files>mobile/android/app/src/test/java/com/ghtrade/app/ExternalLinksTest.kt, mobile/package.json</files>
  <read_first>
    - mobile/android/app/src/test/java/com/ghtrade/app/TabRoutesTest.kt (표 테스트 모양 · iOS 짝 주석 관례)
    - mobile/android/app/src/main/java/com/ghtrade/app/ExternalLinks.kt (Task 2)
    - mobile/package.json (`native:test:android` 현재 값 · build/typecheck/test/lint 키 금지 규칙 — 21-01)
  </read_first>
  <action>
    ① `ExternalLinksTest.kt`(신규): JUnit4 `class ExternalLinksTest`. 한 `@Test` 안에서 표(scheme, host, appHost → 기대값)를 돌며 `assertEquals("<번호> <scheme>://<host> app=<appHost>", 기대, ExternalLinks.opensInAppBrowser(...))`. KDoc: 「iOS `mobile/scripts/external-links-check.swift`(21-22)와 같은 순서·같은 기대값 — 표를 고치면 두 파일을 함께」 · D-28. 정본 케이스(appHost 는 따로 적지 않으면 `trade.jx1.io`):
      1 https · n.news.naver.com → true / 2 http · example.com → true / 3 HTTPS · Example.COM → true(대소문자) / 4 https · trade.jx1.io → false(같은 호스트) / 5 https · TRADE.JX1.IO → false / 6 https · www.trade.jx1.io → true(다른 호스트 = 사이트 밖) / 7 https · trade.jx1.io.evil.com → true(접미사 위장 = 외부) / 8 https · localhost → false(로컬 에셋 서버) / 9 http · localhost · appHost localhost → false(dev) / 10 http · 127.0.0.1 → false / 11 http · ::1 → false / 12 http · [::1] → false / 13 capacitor · localhost → false / 14 mailto · null → false / 15 tel · null → false / 16 javascript · null → false / 17 data · null → false / 18 file · "" → false / 19 blob · null → false / 20 intent · scan → false / 21 about · null → false / 22 kakaotalk · inappbrowse → false(앱 딥링크) / 23 https · "" → false / 24 https · null → false / 25 https · n.news.naver.com · appHost null → false / 26 scheme null · host null → false.
    ② `mobile/package.json` 의 `native:test:android` 를 `cd android && ./gradlew testDebugUnitTest --tests com.ghtrade.app.TabRoutesTest --tests com.ghtrade.app.ExternalLinksTest` 로 바꾼다. build/typecheck/test/lint 키는 추가하지 않는다(루트 `pnpm -r` 영향 방지).
    ③ 표가 하나라도 틀리면 ExternalLinks.kt 를 고친다(표가 정본). 결과(테스트 수·실패 0)를 SUMMARY 에 적는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile exec cap sync android && pnpm --filter @gh-radar/mobile run native:test:android</automated>
    <fails_when>non-zero exit (a --tests filter that matches no class also exits non-zero with "No tests found"), or output lacks "BUILD SUCCESSFUL", or contains "FAILED"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "class ExternalLinksTest" mobile/android/app/src/test/java/com/ghtrade/app/ExternalLinksTest.kt` ≥ 1
    - 경계 케이스가 표에 있다: `grep -c "trade.jx1.io.evil.com"` ≥ 1 · `grep -c "kakaotalk"` ≥ 1 · `grep -c '"\[::1\]"'` ≥ 1 · `grep -c '"intent"'` ≥ 1 (모두 ExternalLinksTest.kt)
    - `grep -c "ExternalLinksTest" mobile/package.json` ≥ 1 · `grep -c "TabRoutesTest" mobile/package.json` ≥ 1
    - `node -e "const s=require('./mobile/package.json').scripts;process.exit(['build','typecheck','test','lint'].some(k=>k in s)?1:0)"` exit 0
    - verify 명령 통과(TabRoutesTest 4 + ExternalLinksTest 통과, 실패 0)
  </acceptance_criteria>
  <done>호스트 밖 판정 규칙이 26케이스 표로 잠겼고, `native:test:android` 한 번으로 경로표와 링크 표가 함께 돈다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 웹 콘텐츠 → 네이티브 셸 내비 결정 | 뉴스·종토방·챗 인용의 링크 URL 은 제3자 데이터다(신뢰 불가 입력) |
| 네이티브 셸 → 인앱 브라우저 제공자 | Custom Tabs 는 브라우저 앱의 쿠키 저장소를 쓴다(WebView 세션과 분리) |
| 빌드 → Google Maven | 선택 a 의 androidx.browser 직접 선언 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-43 | Elevation of Privilege | ExternalLinks 스킴 판정 | high | mitigate | http/https 허용 목록만 true — javascript·data·file·blob·intent·about 은 false 로 Capacitor 기본 경로(Task 2 ①) · intent: URI 를 파싱해 실행하지 않음 · JUnit 케이스 13~22(Task 3) |
| T-21-44 | Spoofing | 앱 호스트 비교 | medium | mitigate | 소문자 정확 일치만 같은 호스트 — 접미사 위장(`trade.jx1.io.evil.com`)은 외부 → 인앱 브라우저가 실제 URL 을 보여 준다(케이스 7) |
| T-21-45 | Information Disclosure | InAppBrowser 로깅·URL 전달 | medium | mitigate | 로그는 호스트만(acceptance 의 로그 호출 대조) · URL 에 쿼리·쿠키·헤더를 덧붙이지 않음 · Custom Tabs 는 WebView 세션 쿠키를 공유하지 않는다 |
| T-21-46 | Tampering | target=_blank 역방향 탭내빙 | low | mitigate | WebView 안에 새 창을 만들지 않음(멀티 윈도우 미사용 확인) → opener 관계 없음 · 앵커의 `rel=noopener noreferrer`(T-13-11) 유지 |
| T-21-47 | Denial of Service | 연속 탭 | low | accept | 탭마다 Custom Tabs 액티비티 1개 · 뒤로가기로 닫힘 · 루프 경로 없음 |
| T-21-SC | Tampering | androidx.browser 선언(선택 a) | medium | mitigate | blocking-human 결정(Task 1) · 버전 1.9.0 = capgo 가 이미 APK 에 싣는 좌표와 동일 · 런타임 클래스패스 승격 없음 확인(Task 2 acceptance) · npm 무변경 |
</threat_model>

<verification>
- Task 1 선택이 SUMMARY 에 있고, 선택대로 구현됐다
- `native:build:android` · `native:smoke:android` · `native:test:android` 통과
- 사람 확인(호스트 밖 링크 → Custom Tabs, 닫으면 앱 복귀)은 21-24 UAT 항목 4 에서 한다
</verification>

<success_criteria>
- G-21-N3 Android 측이 사용자 결정(D-28)대로 구현됐고, 같은 호스트·비 http(s) 경로는 회귀 없이 기존 동작이다.
</success_criteria>

## Flagged assumptions

- 시스템 브라우저로 꼭 넘겨야 하는 호스트는 없다(21-15 네이티브 로그인). 배포 전 옛 웹을 실은 앱이 웹 OAuth 를 시작하는 경우(21-16 UAT 2차 초반)에는 이제 시스템 브라우저 대신 인앱 브라우저가 열린다 — 어느 쪽이든 WebView 에 세션이 생기지 않는 같은 실패이고, 앱 재실행으로 해소된다(UAT 기록과 동일).
- Custom Tabs 지원 브라우저가 없는 기기에서는 선택 a·b 도 기본 브라우저로 열린다(Custom Tabs 계약상 폴백).
- 엣지 탐침 MOBILE-01 행은 분류되지 않았다(unclassified · unresolved) — 이 플랜의 URL 경계는 Task 3 의 26케이스 표로 명시했고, 나머지는 21-24 UAT 에서 수동 검토한다.

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| Android `ExternalLinks.opensInAppBrowser(scheme, host, appHost)` · `ExternalLinksTest`(정본 26케이스) | Kotlin 순수 함수 · JUnit | 21-17 |
| Android `InAppBrowser.open(activity, uri, theme)` · `GhTradeWebViewClient.shouldOverrideUrlLoading` 가로채기 | Kotlin | 21-17 |
| `mobile/android/variables.gradle` `androidxBrowserVersion` · `app/build.gradle` androidx.browser 선언 (21-17 Task 1 선택 a 일 때만) | Gradle | 21-17 |
| `mobile/package.json` `native:test:android`(TabRoutesTest + ExternalLinksTest) · `native:check-external-links:ios` | 스크립트 | 21-17 · 21-22 |
| 웹 `ThemeProvider` 기본 다크 · iOS/Android `ThemeStore` 기본 다크 · `mobile/www/index.html` 기본 다크 | 설정 | 21-18 |
| e2e `webapp/e2e/specs/theme-default.spec.ts` (저장값 없음 다크 · 저장값 유지 · 앱 첫 theme 메시지 · 오프라인 폴백) | Playwright | 21-18 |
| 웹 `THEME_SWITCH_LABEL` · `ThemeSwitchIcon` (theme-toggle.tsx export) — 사이드바 ThemeToggle · AccountCard 공용 목적지 규칙 | 컴포넌트·상수 | 21-19 |
| 단위 `webapp/src/components/layout/__tests__/theme-toggle.test.tsx` | Vitest | 21-19 |
| iOS `GHTradeTabBar` 캡슐 C(라벨 없음 · 56×36 16% · 유리 · 두 겹 그림자 · 높이 60 · 페이드 86) · `GHTradePalette.glass` · `glassAlpha` | Swift | 21-20 |
| CSS `--native-tabbar-offset` = gap + 60 + 8 · `--native-body-reserve` 98 · native-shell e2e 수치(82 · 98 · 74) | 토큰 · Playwright | 21-20 |
| CONTEXT D-27b · D-23a · D-08b · D-28 | 결정 기록 | 21-20 |
| Android `GhTradeTabBar` 캡슐 C 근사(라벨 없음 · 불투명 유리 · elevation 12) · `GhTradePalette.glass` · 탭바 60dp · 페이드 86dp | Kotlin | 21-21 |
| iOS `ExternalLinks.opensInAppBrowser(scheme:host:appHost:)` · `isSameAppHost` · `NavigationDelegateProxy`(decidePolicyFor · WKUIDelegate createWebViewWith · SFSafariViewController) · `mobile/scripts/external-links-check.swift` · `check-external-links-ios.sh` | Swift · 스크립트 | 21-22 |
| 갭 클로징 전 게이트 기록 · push 전 UAT 환경 (21-23-SUMMARY) · UAT 재검증 결과 · push 결정과 이행 (21-24-SUMMARY) | 문서 | 21-23 · 21-24 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-17-SUMMARY.md` when done — Task 1 선택과 클래스패스 재실측 결과 · 선택 b 면 AAR 상수 대조 · 테스트 수 · 스모크 결과를 적는다.
</output>
