---
phase: 21-gh-trade-mobile-app
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - mobile/package.json
  - mobile/scripts/smoke-android.sh
  - mobile/android/build.gradle
  - mobile/android/variables.gradle
  - mobile/android/app/build.gradle
  - mobile/android/app/src/main/AndroidManifest.xml
  - mobile/android/app/src/main/res/values/strings.xml
  - mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
  - mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt
  - mobile/android/app/src/main/java/com/ghtrade/app/GhTradeBridge.kt
  - mobile/android/capacitor.settings.gradle
  - mobile/android/settings.gradle
autonomous: true
requirements: [MOBILE-01]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-02 · D-20: Android 프로젝트가 `mobile/android` 에 생성됐고 applicationId·namespace 가 21-01 확정 ID(기본 `com.ghtrade.app`) · 표시명 `strings.xml app_name` = `GH Trade` 다"
    - "D-02: `MainActivity` 는 Kotlin `BridgeActivity` 서브클래스이고 `load()` 오버라이드에서 `super.load()` 뒤에 붙는다 — 템플릿 Java `MainActivity` 는 남지 않는다"
    - "Pitfall 4: WebView 클라이언트는 `bridge.setWebViewClient(GhTradeWebViewClient(bridge, this))` 로 설치한 `BridgeWebViewClient` 상속본이다 — WebView 의 클라이언트를 통째로 새 인스턴스로 바꾸지 않고, 모든 오버라이드가 super 를 부른다"
    - "D-12 채널: `window.GhTradeBridge.postMessage(json)` 이 `@JavascriptInterface` 로 노출되고, 수신은 메인 스레드로 넘긴 뒤 현재 WebView URL 호스트가 server.url 호스트일 때만 · 타입 화이트리스트(`ready`·`route`·`theme`·`overlay`·`pull`)만 처리한다"
    - "D-11 확장 슬라이스: Android 에뮬레이터에서 GH Trade 셸이 웹(dev :3100)을 로드하고 웹의 `ready` 가 도착해 logcat 태그 `GHTrade` 에 `ready platform=android nativeApp=true` 가 찍힌다"
    - "Pitfall 15: Android 스모크도 종료 시 운영 URL 로 다시 sync 한다"
  artifacts:
    - path: mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
      provides: "BridgeActivity 서브클래스 · 클라이언트·JS 인터페이스 설치 · onUrlChanged"
      contains: "class MainActivity : BridgeActivity()"
    - path: mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt
      provides: "BridgeWebViewClient 상속 · URL 변경 전달"
      contains: "BridgeWebViewClient(bridge)"
    - path: mobile/android/app/src/main/java/com/ghtrade/app/GhTradeBridge.kt
      provides: "@JavascriptInterface postMessage · 호스트 검사 · 타입 화이트리스트"
      contains: "@JavascriptInterface"
    - path: mobile/scripts/smoke-android.sh
      provides: "에뮬레이터 dev 스모크 · 운영 sync 복원"
  key_links:
    - from: webapp/src/lib/native/native-detect.ts
      to: mobile/android/app/src/main/java/com/ghtrade/app/GhTradeBridge.kt
      via: "window.GhTradeBridge.postMessage(JSON {type:'ready'})"
      pattern: "GhTradeBridge"
    - from: mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
      to: mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt
      via: "bridge.setWebViewClient(GhTradeWebViewClient(bridge, this))"
      pattern: "setWebViewClient\\(GhTradeWebViewClient"
---

<assumption_delta_decision>
- 검출기 `assumption-delta scan 21` → `detected: false`. 수동 판정: 두 번째 클라이언트 플랫폼(네이티브 셸) 추가 — 정본 명사는 **웹앱(`https://trade.jx1.io`)**, 결정 **add-alongside**(근거·부채·promote 신호는 21-01 블록과 동일).
</assumption_delta_decision>

<objective>
Android 쪽 셸 슬라이스. 21-01 이 iOS 에서 증명한 경로(셸 → 웹 로드 → 웹 감지 → `ready` 도착)를 Android 에서 같은 모양으로 세운다: `cap add android` → Kotlin `MainActivity : BridgeActivity` → `BridgeWebViewClient` 상속본 설치 → `@JavascriptInterface` 채널 `GhTradeBridge` → 에뮬레이터 logcat 에 `ready` 한 줄.

Purpose: 이 슬라이스 뒤로 Android 사용자가 GH Trade 앱을 열어 웹을 볼 수 있다. weekly-wine 골격 중 Android 16 에서 깨지는 두 곳(WebView 클라이언트 통째 교체 · `onBackPressed`)을 처음부터 올바른 확장점으로 잡는다(뒤로가기 본체는 21-13).
Output: Android 프로젝트 + Kotlin 3파일 + 에뮬레이터 스모크 스크립트.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md
@.planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md
@.planning/phases/21-gh-trade-mobile-app/21-01-SUMMARY.md
@mobile/package.json
@mobile/capacitor.config.ts

21-01 SUMMARY 의 확정 번들 ID 를 쓴다(기본 `com.ghtrade.app` — 대체값이면 패키지 디렉터리 `java/<역DNS 경로>` 와 모든 문자열을 그 값으로). Gradle 첫 빌드는 수 분 — Bash timeout 600000. 에뮬레이터 AVD 는 `Medium_Phone_API_36.1`(API 36.1 = Android 16 동작).

<interfaces>
21-01 이 만든 계약:

```text
웹 → 네이티브 메시지: JSON 문자열 {"type": "ready"|"route"|"theme"|"overlay"|"pull", "payload": …}
ready payload = { platform: 'android', nativeApp: true, path: string }
Android 채널: window.GhTradeBridge.postMessage(json)   (iOS: webkit.messageHandlers.ghTrade)
mobile/package.json scripts: native:sync · native:sync:dev (CAP_SERVER_URL=http://localhost:3100) · native:build:android
```

Capacitor 8.5.2 확장점(RESEARCH Pattern 3 — 소스 확인):

```java
// com.getcapacitor.BridgeActivity: onCreate → setContentView(capacitor_bridge_layout_main) → load() → bridgeBuilder.create() (플러그인 load · loadUrl)
// com.getcapacitor.Bridge: public void setWebViewClient(BridgeWebViewClient client)  (Bridge.java:1468) · getWebView() · getConfig()
// com.getcapacitor.BridgeWebViewClient(Bridge bridge): shouldInterceptRequest(로컬 에셋 서버) · onPageCommitVisible · onReceivedError · onReceivedHttpError
```

이 플랜이 새로 만드는 계약(21-12 · 21-13 이 소비):

```kotlin
class MainActivity : BridgeActivity() { fun onUrlChanged(url: String); fun onNativeMessage(type: String, payload: JSONObject?) }
class GhTradeWebViewClient(bridge: Bridge, private val host: MainActivity) : BridgeWebViewClient(bridge)
class GhTradeBridge(private val host: MainActivity) { @JavascriptInterface fun postMessage(json: String) }
// logcat 태그 "GHTrade"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `cap add android` + Kotlin `MainActivity`·`GhTradeWebViewClient`·`GhTradeBridge` (BridgeWebViewClient 상속 · 호스트 검사)</name>
  <reversibility rating="costly">D-02 네이티브 탭바가 이 Activity 위에 얹힌다 — 확장점을 바꾸면 21-12·21-13 이 함께 다시 쓰인다(CONTEXT D-01·D-03 costly 와 같은 결합).</reversibility>
  <files>mobile/android/build.gradle, mobile/android/variables.gradle, mobile/android/app/build.gradle, mobile/android/app/src/main/AndroidManifest.xml, mobile/android/app/src/main/res/values/strings.xml, mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt, mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt, mobile/android/app/src/main/java/com/ghtrade/app/GhTradeBridge.kt, mobile/android/capacitor.settings.gradle, mobile/android/settings.gradle</files>
  <read_first>
    - mobile/capacitor.config.ts · mobile/package.json (21-01 산출)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pattern 3 · Pitfall 4·5·8 · Anti-Patterns · Assumptions A1)
    - .planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md (`MainActivity.kt` · `GhTradeWebViewClient.kt` 절)
    - /Users/alex/repos/weekly-wine-app/android/app/src/main/java/kr/co/weeklywine/app/MainActivity.kt (L90-120 `load()` · L338-352 `AndroidBridge` + `handler.post` · L725-745 반면교사 클라이언트 교체)
    - /Users/alex/repos/weekly-wine-app/android/build.gradle · /Users/alex/repos/weekly-wine-app/android/app/build.gradle (Kotlin 플러그인 적용 모양 · 서명 설정은 가져오지 않음)
  </read_first>
  <action>
    ① `pnpm --filter @gh-radar/mobile exec cap add android` 를 실행한다. 생성 확인: `mobile/android/app/build.gradle` 의 `applicationId` 와 `namespace` 가 확정 ID, `res/values/strings.xml` 의 `app_name`·`title_activity_main` 이 `GH Trade`(아니면 고친다), `variables.gradle` 의 `minSdkVersion` 24 · `compileSdkVersion`/`targetSdkVersion` 36(템플릿 값이 다르면 RESEARCH Claude's Discretion 기준 min 24 로 맞추고 나머지는 템플릿 값 유지).
    ② Kotlin: 루트 `mobile/android/build.gradle` 의 `buildscript.dependencies` 에 `classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:2.2.21'` 를 더하고 `mobile/android/app/build.gradle` 에 `apply plugin: 'kotlin-android'` 와 `kotlinOptions { jvmTarget = '21' }`(템플릿 `compileOptions` 의 Java 버전과 같은 값 — 다르면 그 값)을 더한다. `assembleDebug` 가 KGP 2.2.21 과 충돌하면 로컬 캐시에 있는 `1.9.22` 로 내리고 SUMMARY 에 적는다(RESEARCH A1). `kotlin-stdlib` 는 KGP 가 자동으로 넣으므로 따로 선언하지 않는다. weekly-wine 의 `signingConfigs.release`(기본 비밀번호 하드코딩)와 BlurView·google-services 의존성은 가져오지 않는다.
    ③ 템플릿 `app/src/main/java/<id 경로>/MainActivity.java` 를 지우고 같은 패키지에 `MainActivity.kt` 를 만든다: `class MainActivity : BridgeActivity()`. `private val handler = Handler(Looper.getMainLooper())`. `override fun load()` → `super.load()` → `val wv = bridge.webView ?: return` → `bridge.setWebViewClient(GhTradeWebViewClient(bridge, this))`(Pitfall 4 — WebView 에 새 클라이언트 인스턴스를 직접 대입하지 않는다) → `wv.addJavascriptInterface(GhTradeBridge(this), "GhTradeBridge")`. `fun serverHost(): String? = Uri.parse(bridge.config.serverUrl).host`(없으면 `bridge.appUrl` 호스트). `fun onUrlChanged(url: String)` 은 이 플랜에서 `Log.d("GHTrade", "url=$url")` 만(21-12 가 탭바 판정을 붙인다). `fun onNativeMessage(type: String, payload: JSONObject?)` 는 `ready` → `Log.i("GHTrade", "ready platform=${payload?.optString("platform")} nativeApp=${payload?.optBoolean("nativeApp")}")`, 그 외 타입 → `Log.d` 만. 뒤로가기는 이 플랜에서 건드리지 않는다(21-13 이 `onBackPressedDispatcher` 로 — 옛 `onBackPressed` 오버라이드 금지, Pitfall 5). `ViewCompat.setOnApplyWindowInsetsListener` 를 DecorView·루트에 걸지 않는다(Pitfall 8).
    ④ `GhTradeWebViewClient.kt`: `class GhTradeWebViewClient(bridge: Bridge, private val host: MainActivity) : BridgeWebViewClient(bridge)`. `onPageFinished(view, url)` → `super` 후 `host.onUrlChanged(url)` · `doUpdateVisitedHistory(view, url, isReload)` → `super` 후 `host.onUrlChanged(url)`. 로컬 에셋 요청 가로채기·HTTP 오류 콜백은 **오버라이드하지 않는다**(Pitfall 2·4 — 404 페이지는 웹 그대로 보이고 로컬 서버가 산다). `onReceivedError` 네트워크 오류 처리는 21-13 이 더한다.
    ⑤ `GhTradeBridge.kt`: `class GhTradeBridge(private val host: MainActivity)` · `@JavascriptInterface fun postMessage(json: String)` — JS 브리지 스레드에서 불리므로 곧바로 `host.runOnUiThread { … }` 로 넘긴다(RESEARCH Anti-Patterns). UI 스레드에서: `host.bridge.webView?.url` 의 호스트가 `host.serverHost()` 와 다르면 무시(T-21-03 — 오프라인 로컬 페이지·다른 출처 프레임 차단) → `JSONObject(json)` 파싱 실패 무시 → `type` 이 `setOf("ready","route","theme","overlay","pull")` 밖이면 무시 → `host.onNativeMessage(type, obj.optJSONObject("payload"))`. 다른 `@JavascriptInterface` 메서드는 두지 않는다(노출 표면 최소).
    ⑥ `AndroidManifest.xml` 은 템플릿 그대로(`configChanges` 에 `uiMode|orientation|screenSize` 가 있는지 확인만 — Pitfall 13). `android:screenOrientation` 은 넣지 않는다(21-13 이 코드로 분기, D-24).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:android</automated>
    <fails_when>non-zero exit, or the output lacks "BUILD SUCCESSFUL", or contains "e: " Kotlin compile errors</fails_when>
  </verify>
  <acceptance_criteria>
    - `test ! -e mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.java` 가 exit 0 (확정 ID 경로 기준)
    - `grep -n "class MainActivity : BridgeActivity()" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 1줄
    - `grep -n "setWebViewClient(GhTradeWebViewClient" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 1줄
    - `grep -cE 'webViewClient = |shouldInterceptRequest|onReceivedHttpError|override fun onBackPressed' mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt` 의 각 파일 값이 0
    - `grep -n "BridgeWebViewClient(bridge)" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt` 가 1줄
    - `grep -n "@JavascriptInterface" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeBridge.kt` 가 1줄 · `grep -n "runOnUiThread" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeBridge.kt` 가 1줄 이상
    - `grep -n 'applicationId "com.ghtrade.app"' mobile/android/app/build.gradle` 가 1줄(확정 ID 기준) · `grep -n "GH Trade" mobile/android/app/src/main/res/values/strings.xml` 가 1줄 이상
    - `grep -c "storePassword" mobile/android/app/build.gradle` 가 0
  </acceptance_criteria>
  <done>Android 프로젝트가 Kotlin Activity·상속 클라이언트·제한된 JS 인터페이스로 컴파일되고, WebView 클라이언트 교체·옛 뒤로가기·DecorView 인셋 리스너가 없다.</done>
</task>

<task type="auto">
  <name>Task 2: 에뮬레이터 스모크 — dev 웹 로드 → `ready` 가 logcat `GHTrade` 에 도착 → 운영 sync 복원</name>
  <precondition>`$ANDROID_HOME/emulator/emulator -list-avds` 출력에 AVD 가 1개 이상 있다(없으면 멈추고 보고)</precondition>
  <files>mobile/scripts/smoke-android.sh, mobile/package.json</files>
  <read_first>
    - mobile/scripts/smoke-ios.sh (21-01 — 같은 구조·출력 문구를 따른다)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Validation Architecture 시뮬레이터 스모크 · Pitfall 15·16 `adb reverse`)
  </read_first>
  <action>
    ① `mobile/scripts/smoke-android.sh`(bash · `set -euo pipefail` · 첫 줄 `cd "$(dirname "$0")/.."`): (a) `curl -sf -o /dev/null http://localhost:3100/login` 실패 → 「webapp dev 서버(:3100)를 먼저 띄우세요」 exit 2 (b) `trap` EXIT → `pnpm exec cap sync android`(운영 URL 복원, Pitfall 15) (c) 연결된 기기/에뮬레이터가 없으면(`adb devices` 에 `device` 상태 줄 없음) `"$ANDROID_HOME/emulator/emulator" -avd "${AVD:-Medium_Phone_API_36.1}" -no-snapshot-save -no-audio -no-boot-anim >/dev/null 2>&1 &` → `adb wait-for-device` → `adb shell getprop sys.boot_completed` 가 `1` 이 될 때까지 최대 180초 대기 (d) `adb reverse tcp:3100 tcp:3100` (에뮬레이터의 localhost → 호스트, Pitfall 16) (e) `CAP_SERVER_URL=http://localhost:3100 pnpm exec cap sync android` → `(cd android && ./gradlew assembleDebug)` (f) `adb install -r android/app/build/outputs/apk/debug/app-debug.apk` → `adb logcat -c` → `adb shell am force-stop <appId>` → `adb shell am start -n <appId>/.MainActivity` (g) 최대 90초 동안 3초마다 `adb logcat -d -s GHTrade:I` 에서 `ready platform=android nativeApp=true` 를 찾는다 (h) `adb exec-out screencap -p > "${TMPDIR:-/tmp}/gh-trade-android-smoke.png"` (i) 찾으면 `SMOKE OK ready platform=android` exit 0, 못 찾으면 `adb logcat -d -t 60` 끝부분과 `SMOKE FAIL` 출력 후 exit 1. `<appId>` 는 스크립트 상단 `APP_ID="${APP_ID:-com.ghtrade.app}"`(21-01 확정값을 기본값으로). 실행 권한을 준다.
    ② `mobile/package.json` scripts 에 `native:smoke:android` = `bash scripts/smoke-android.sh` 를 더한다(다른 키 무변경, build/typecheck/test/lint 키 금지 유지).
    ③ 3100 이 비어 있으면 `PORT=3100 pnpm --filter @gh-radar/webapp run dev` 를 백그라운드로 띄운 뒤 `pnpm --filter @gh-radar/mobile run native:smoke:android` 를 실행한다(`dev.sh` 금지 — 다른 세션 포트 kill).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:smoke:android</automated>
    <fails_when>non-zero exit, or the output lacks "SMOKE OK ready platform=android", or contains "SMOKE FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "native:smoke:android" mobile/package.json` 가 1줄
    - `grep -n "adb reverse tcp:3100 tcp:3100" mobile/scripts/smoke-android.sh` 가 1줄 · `grep -n "trap" mobile/scripts/smoke-android.sh` 가 1줄 이상
    - `test -x mobile/scripts/smoke-android.sh` 가 exit 0
    - 스모크 종료 뒤 `node -e "const c=require('./mobile/android/app/src/main/assets/capacitor.config.json');process.exit(c.server&&c.server.url==='https://trade.jx1.io'?0:1)"` 가 exit 0 (운영 복원 확인)
  </acceptance_criteria>
  <done>Android 에뮬레이터에서 GH Trade 셸이 웹을 로드하고 `ready` 가 네이티브에 도착함이 logcat 으로 증명됐으며, 생성 설정은 운영 URL 로 돌아와 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 원격/로컬 페이지 JS → `GhTradeBridge` | `addJavascriptInterface` 는 모든 프레임·출처에 객체를 노출한다 — 호출자를 신뢰하지 않는다 |
| dev ↔ 운영 빌드 설정 | cleartext·localhost 가 생성 매니페스트·설정에 구워진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03 | Elevation of Privilege | `GhTradeBridge.postMessage` | high | mitigate | 메서드 1개 · UI 스레드에서 현재 URL 호스트 == server.url 호스트 검사 · JSON 파싱 실패 무시 · 타입 화이트리스트 · UI 신호만(Task 1 ⑤) |
| T-21-02 | Tampering | 메시지 페이로드 | medium | mitigate | `optJSONObject`/`optString` 만 사용 · 알 수 없는 타입 드롭 |
| T-21-01 | Information Disclosure | dev sync(cleartext) 잔류 | high | mitigate | 스모크 EXIT trap 운영 sync + acceptance 에서 생성 설정 URL 재확인 |
| T-21-14 | Information Disclosure | 서명 비밀번호 하드코딩 | high | mitigate | release 서명 설정 미이식 · acceptance `storePassword` grep 0 |
| T-21-SC | Tampering | Kotlin Gradle 플러그인(Maven Central) | low | accept | JetBrains 공식 좌표 · 로컬 캐시 1.9.22 폴백 · npm 설치 없음 |
</threat_model>

<verification>
- `native:build:android` BUILD SUCCESSFUL
- `native:smoke:android` 「SMOKE OK ready platform=android」 · 생성 설정 운영 URL 복원
</verification>

<success_criteria>
- Android 셸이 iOS 와 같은 채널 규약(`{type,payload}` JSON)으로 웹 신호를 받는다 — 21-12 탭바·21-13 새로고침/뒤로가기가 이 위에 붙는다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/` 패키지 `@gh-radar/mobile` — scripts `native:sync` · `native:sync:dev` · `native:build:ios` · `native:build:android` · `native:smoke:ios` · `native:open:ios` · `native:open:android` · `native:assets` (21-01) · `native:smoke:android` (21-02) · `native:check-tab-routes:ios` (21-10) · `native:test:android` (21-12) · `native:verify-prod` (21-16) | 패키지·스크립트 | 21-01 · 21-02 · 21-10 · 21-12 · 21-16 |
| `mobile/capacitor.config.ts` — appId `com.ghtrade.app` · appName `GH Trade` · `CAP_SERVER_URL` · `SystemBars` (21-01) · `SocialLogin` (21-15) | 설정 | 21-01 · 21-15 |
| `mobile/tsconfig.json` · `mobile/.gitignore` | 설정 | 21-01 |
| `mobile/www/index.html` — 오프라인 폴백(최소 21-01 · `safeTarget`·5초 탐침·`online`·테마 21-11) | 정적 페이지 | 21-01 · 21-11 |
| `mobile/scripts/smoke-ios.sh` · `mobile/scripts/smoke-android.sh` — 「SMOKE OK ready platform=…」 | 스모크 | 21-01 · 21-02 |
| `mobile/scripts/check-tab-routes-ios.sh` · `mobile/scripts/tab-routes-check.swift` | Swift 경로표 검사 | 21-10 |
| `mobile/scripts/render-resources.mjs` · `mobile/resources/{icon.svg,icon-only.png,icon-foreground.png,icon-background.png,splash.png,splash-dark.png}` | 에셋 원본 | 21-14 |
| `mobile/scripts/verify-prod-config.mjs` · `mobile/README.md` | 운영 설정 검사·문서 | 21-16 |
| iOS `SceneDelegate.swift` (루트 = `GHTradeBridgeViewController()` · 창 배경 21-11) | Swift | 21-01 · 21-11 |
| iOS `GHTradeBridgeViewController` · `WeakScriptMessageHandler` · `NativeMessageType` | Swift | 21-01 · 21-10 · 21-11 |
| iOS `GHTradeTabBar` · `GHTradeTab` · `GHTabID` · `GHTradePalette` · `GHTradeTheme` · `TabRoutes.activeTab(forPath:)` · `TabRoutes.hidesTabBar(path:)` | Swift | 21-10 |
| iOS `NavigationDelegateProxy` · `ThemeStore` (UserDefaults `gh-trade.theme`) | Swift | 21-11 |
| Android `MainActivity` (`BridgeActivity`) · `GhTradeWebViewClient` (`BridgeWebViewClient` 상속) · `GhTradeBridge` (`@JavascriptInterface postMessage`) | Kotlin | 21-02 · 21-12 · 21-13 |
| Android `GhTradeTabBar` · `GhTradePalette` · `TabId` · `TabRoutes.activeTabFor` · `TabRoutes.hidesTabBar` · `TabRoutesTest` | Kotlin | 21-12 |
| Android `ThemeStore` (SharedPreferences `gh_trade`/`theme`) · `res/values/bools.xml` · `res/values-sw600dp/bools.xml` (`is_tablet`) | Kotlin·리소스 | 21-13 |
| Android `res/drawable/ic_tab_{home,home_fill,search,search_fill,trading,trading_fill,ai,ai_fill,me,me_fill}.xml` | 벡터 | 21-12 |
| 웹 `lib/native/native-detect.ts` — `NATIVE_DETECT_SCRIPT` · `isNativeApp` · `nativePlatform` | 유틸 | 21-01 |
| 웹 `lib/native/post-native.ts` — `postNative` · `NativeMsgType` | 유틸 | 21-04 |
| 웹 `lib/native/native-bridge-provider.tsx` — `NativeBridgeProvider` · `useNativeBridge` · `NativeRefreshFn` · `window.__ghTrade = { refresh, navigate, back }` | Provider | 21-04 |
| 웹 `lib/native/use-native-refresh.ts` — `useNativeRefresh` | 훅 | 21-04 |
| 웹 `lib/native/native-overlay-marker.tsx` — `NativeOverlayMarker` | 컴포넌트 | 21-04 |
| 웹 `lib/native/google-client-ids.ts` — `GOOGLE_WEB_CLIENT_ID` · `GOOGLE_IOS_CLIENT_ID` · `GOOGLE_ANDROID_CLIENT_ID` · `GOOGLE_IOS_URL_SCHEME` | 공개 식별자 상수 | 21-03 |
| 웹 `lib/native/native-google-login.ts` — `nativeGoogleSignIn` · `randomNonce` · `sha256Hex` · `classifyNativeLoginError` | 서비스 | 21-15 |
| 웹 `RelayConnectionState.probeNow` · `EMPTY_RELAY_VALUE.probeNow` | 훅 계약 | 21-07 |
| 웹 `lib/recent-search.ts` — `RECENT_SEARCH_KEY` · `RECENT_SEARCH_MAX` · `RecentSearchItem` · `readRecentSearches` · `pushRecentSearch` · `removeRecentSearch` · `clearRecentSearches` | 유틸 | 21-08 |
| 웹 `app/search/page.tsx` · `components/search/search-page-client.tsx` — `SearchPageClient` · 사이드바 `NAV_SEARCH_PAGE` | 페이지·상수 | 21-08 |
| 웹 `components/me/account-card.tsx` — `AccountCard` | 컴포넌트 | 21-09 |
| 웹 마크업 계약 `data-slot="app-aside"` · `data-slot="app-menu-button"` · `data-slot="stock-ai-button"` · `data-slot="shared-panels"` · `data-pinned` · `data-order-cta` · `data-slot="account-card"` | 마크업 | 21-05 · 21-06 · 21-09 |
| CSS `@custom-variant native` (21-04) · 앱 셸 규칙 (21-05) · `--app-safe-top` · `--app-safe-bottom` · `--app-safe-left` · `--app-safe-right` · `--native-tabbar-gap` · `--native-tabbar-offset` · `--native-body-reserve` (21-06) | 토큰·변형 | 21-04 · 21-05 · 21-06 |
| 웹 `app/icon.svg` (= 스케치 004 `#app-a`) | 파비콘 | 21-14 |
| 단위 테스트: `native-detect.test.ts` (21-01) · `post-native.test.ts` · `native-bridge-provider.test.tsx` · `native-overlay.test.tsx` (21-04) · `google-client-ids.test.ts` (21-03) · `relay-socket.test.ts` probeNow · `stock-native-refresh.test.tsx` (21-07) · `recent-search.test.ts` · `search-page-client.test.tsx` · `app-sidebar.test.tsx` 보강 (21-08) · `account-card.test.tsx` · `app-shell-chrome.test.tsx` 보강 (21-09) · `native-google-login.test.ts` · `login/__tests__/page.test.tsx` (21-15) · `TabRoutesTest.kt` (21-12) | 단위 | 각 플랜 |
| e2e: `fixtures/native-app.ts` — `installNativeApp` · `nativeMessages` (21-05) · `specs/native-shell.spec.ts` (21-05 · 21-06) · `specs/search-page.spec.ts` (21-08) · `specs/brand-account.spec.ts` (21-09) | Playwright | 각 플랜 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-02-SUMMARY.md` when done — 사용한 KGP 버전(2.2.21 또는 폴백)과 jvmTarget 을 적는다.
</output>
