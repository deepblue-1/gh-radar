---
phase: 21-gh-trade-mobile-app
plan: 05
type: execute
wave: 3
depends_on: ["21-04"]
files_modified:
  - webapp/src/components/layout/app-shell.tsx
  - webapp/src/components/layout/app-header.tsx
  - webapp/src/components/chat/chat-fab.tsx
  - webapp/src/components/stock/stock-hero.tsx
  - webapp/src/styles/globals.css
  - webapp/e2e/fixtures/native-app.ts
  - webapp/e2e/specs/native-shell.spec.ts
autonomous: true
requirements: [MOBILE-01]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-09: 앱(`html.native-app`)에서는 폭과 무관하게 고정 사이드바 `aside`(`data-slot=\"app-aside\"`)가 숨고 헤더 햄버거(`data-slot=\"app-menu-button\"`)가 `lg` 이상에서도 보여 드로어로만 사이드바를 연다 — 브라우저는 종전 그대로(≥lg aside 보임 · 햄버거 숨김)"
    - "D-13: 앱 셸에 폭 기준으로 탭바 존재를 가르는 웹 분기가 없다(탭바는 네이티브가 항상 표시) — iPad 가로 1280 에서도 사이드바가 숨는다"
    - "D-10: 앱에서 종목상세 `ChatFab` 이 보이지 않고, 종목상세 히어로 첫 줄의 「AI 분석」 버튼(`data-slot=\"stock-ai-button\"`)이 기존 `ChatSheet` 를 그 종목 컨텍스트로 연다 — 브라우저에서는 FAB 그대로 · 「AI 분석」 버튼 숨김"
    - "D-11 · D-12: e2e 픽스처 `installNativeApp` 이 가짜 `window.Capacitor` + 메시지 캡처 채널을 문서 시작 전에 주입해, 실제 인라인 감지 스크립트·Provider 가 앱 모드로 동작하고 `ready`·`route`·`overlay` 메시지가 `window.__nativeMsgs` 로 관측된다"
    - "D-06a: e2e 에서 `window.__ghTrade.navigate('/scanner')` 호출이 클라이언트 내비로 URL 을 바꾸고 `route {path:'/scanner'}` 를 남긴다"
  artifacts:
    - path: webapp/e2e/fixtures/native-app.ts
      provides: "installNativeApp(page,{platform}) · nativeMessages(page)"
      exports: ["installNativeApp", "nativeMessages"]
    - path: webapp/e2e/specs/native-shell.spec.ts
      provides: "앱 모드 셸 e2e (MOBILE-01j 1부) + 브라우저 모드 회귀"
    - path: webapp/src/components/stock/stock-hero.tsx
      provides: "「AI 분석」 버튼(앱 전용 표시)"
      contains: "stock-ai-button"
  key_links:
    - from: webapp/src/components/stock/stock-hero.tsx
      to: webapp/src/components/chat/chat-provider.tsx
      via: "useChat().openChat({ code, name })"
      pattern: "openChat\\("
    - from: webapp/src/styles/globals.css
      to: webapp/src/components/layout/app-shell.tsx
      via: "html.native-app [data-slot=\"app-aside\"] { display: none }"
      pattern: "app-aside"
---

<assumption_delta_decision>
- 검출기 `assumption-delta scan 21` → `detected: false`. 수동 판정: 두 번째 클라이언트 플랫폼 — 정본 명사 **웹앱**, 결정 **add-alongside**. 이 플랜의 `native-shell.spec.ts` 가 21-01 블록이 약속한 불변식 테스트(같은 라우트를 두 모드로 렌더해 셸 차이만 단언)의 첫 절반이다.
</assumption_delta_decision>

<objective>
앱 셸 분기 두 가지(D-09 사이드바·햄버거, D-10 ChatFab → 「AI 분석」 버튼)를 `html.native-app` 클래스 하나로 구현하고, 앱 모드를 브라우저에서 재현하는 Playwright 픽스처와 셸 e2e 를 만든다.

Purpose: 네이티브 탭바가 있는 앱에서 같은 목적지를 사이드바·FAB 로 두 번 보여 주지 않는다. 픽스처는 이후 플랜(21-06 safe-area, 21-08 검색)이 앱 모드를 브라우저에서 검증하는 공용 도구다.
Output: 셸 컴포넌트 4개 수정 + CSS 규칙 + 픽스처 + e2e.

§2.2b 준수: 분기는 셸 층(사이드바·햄버거·FAB)에만 두고 `/trading` 내부 컨테이너 쿼리 밴드에는 넣지 않는다.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md
@.planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md
@webapp/src/components/layout/app-shell.tsx
@webapp/src/components/layout/app-header.tsx
@webapp/src/components/chat/chat-fab.tsx
@webapp/src/components/stock/stock-hero.tsx

<interfaces>
기존 계약(코드에서 추출):

```tsx
// app-shell.tsx L63: <aside className="hidden w-60 shrink-0 … lg:sticky lg:top-14 lg:block lg:h-[calc(100dvh-3.5rem)] lg:self-start lg:overflow-y-auto">{sidebar}</aside>
// app-header.tsx L72: 햄버거 <button type="button" onClick={onMenuClick} aria-label="사이드바 열기" className="-ml-2 inline-flex h-11 w-11 … md:-ml-3 lg:hidden">
// chat-fab.tsx L114-122: <button ref={measureRef} type="button" data-slot="chat-fab" aria-label={label} onClick={handleClick} className="fixed right-6 bottom-6 z-40 flex h-14 …">
//   ★ measureRef 가 offsetWidth 를 CHAT_FAB_WIDTH_VAR 로 문서 루트에 싣는다(더티 바 우측 여백) — display:none 이면 0 이 실린다
// stock-hero.tsx L43-60: 첫 줄 <div className="flex flex-wrap items-center gap-2"> ← 버튼 · <h1>{stock.name}</h1> · 코드 · <Badge> · <WatchlistToggle …/>
// chat-provider.tsx: export function useChat(): { open; openChat(ctx?: StockContext): void; closeChat; stockContext; setStockContext }  (Provider 밖 NOOP 폴백)
//   export interface StockContext { code: string; name: string }
// globals.css: 레이어 밖 규칙은 Tailwind utilities 를 명시도와 무관하게 이긴다(L612-640 주석 · main:has(...) 선례)
```

```ts
// e2e 픽스처 관례: e2e/fixtures/home.ts `mockHomeApi(page, opts)` · mock-api.ts `mockStockApi(page, opts)` · chat.ts `mockChatApi(page, opts)`
// playwright baseURL http://localhost:3100 · chromium 프로젝트 = 로그인 storageState
```

이 플랜이 새로 만드는 계약:

```ts
// webapp/e2e/fixtures/native-app.ts
export type NativeTestPlatform = 'ios' | 'android';
export interface CapturedNativeMsg { type: string; payload?: unknown }
export async function installNativeApp(page: Page, opts?: { platform?: NativeTestPlatform }): Promise<void>; // addInitScript
export async function nativeMessages(page: Page): Promise<CapturedNativeMsg[]>;                         // window.__nativeMsgs
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 셸 분기 — 앱에서 aside 숨김·햄버거 상시(D-09) · ChatFab 숨김 + 히어로 「AI 분석」(D-10)</name>
  <files>webapp/src/components/layout/app-shell.tsx, webapp/src/components/layout/app-header.tsx, webapp/src/components/chat/chat-fab.tsx, webapp/src/components/stock/stock-hero.tsx, webapp/src/styles/globals.css</files>
  <read_first>
    - webapp/src/components/layout/app-shell.tsx (전체 — aside sticky 3종 묶음 주석 · 드로어)
    - webapp/src/components/layout/app-header.tsx (전체 — 햄버거 잉크 보정 주석은 건드리지 않는다)
    - webapp/src/components/chat/chat-fab.tsx (L55-125 — measureRef · 경로 게이트)
    - webapp/src/components/stock/stock-hero.tsx (L28-60)
    - webapp/src/components/chat/chat-provider.tsx (`useChat` · `StockContext`)
    - webapp/src/styles/globals.css (L5-6 변형 · L612-660 레이어 밖 규칙 구역)
    - webapp/src/components/layout/__tests__/app-shell-chrome.test.tsx (셸 램프 불변식 — 깨지면 안 된다)
    - .planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md (§셸 앱 분기)
  </read_first>
  <action>
    ① `app-shell.tsx`: `<aside …>` 에 `data-slot="app-aside"` 를 더한다(클래스 무변경). `main`·드로어·패딩 램프는 건드리지 않는다.
    ② `app-header.tsx`: 햄버거 `<button>` 에 `data-slot="app-menu-button"` 을 더한다(클래스·aria 무변경 — 로고 문자열은 21-09).
    ③ `globals.css` 레이어 밖 규칙 구역(`main:has([data-page-surface="plain"])` 근처)에 한 블록: `html.native-app [data-slot="app-aside"] { display: none; }` · `html.native-app [data-slot="app-menu-button"] { display: inline-flex; }`. 주석: D-09 — 앱은 네이티브 탭바가 목적지를 가지므로 iPad 가로에서도 고정 사이드바를 두지 않고 드로어만 쓴다 · Tailwind 변형 대신 레이어 밖 규칙을 쓰는 이유(`lg:block` 과 커스텀 변형의 출력 순서가 보장되지 않는다 — 레이어 밖 규칙은 utilities 를 항상 이긴다) · 뷰포트 브레이크포인트 층(§2.2b 앱 셸)이라 컨테이너 쿼리 밴드와 무관.
    ④ `chat-fab.tsx`: FAB `<button>` className 끝에 `native:hidden` 을 더한다(D-10 — 표시만 숨김, 경로 게이트·로그인 게이트·measureRef 무변경. 숨으면 offsetWidth 0 이 `CHAT_FAB_WIDTH_VAR` 로 실려 더티 바 우측 여백도 0 이 된다 — 주석으로 남긴다). 로그인 게이트 `Dialog` 는 그대로.
    ⑤ `stock-hero.tsx`: `useChat()` 에서 `openChat` 을 꺼내고, 첫 줄 `WatchlistToggle` 바로 뒤에 `<button type="button" data-slot="stock-ai-button" onClick={() => openChat({ code: stock.code, name: stock.name })} aria-label={\`AI 분석 — ${stock.name}\`} className="hidden native:inline-flex h-8 items-center gap-1 rounded-full bg-[var(--muted)] px-3 text-[13px] font-semibold text-[var(--fg)] focus-visible:ring-2 focus-visible:ring-[var(--ring)]">` + lucide `Sparkles`(size 14, `aria-hidden`) + 「AI 분석」 텍스트 를 둔다(D-10 — 헤더 액션 줄 · 기존 `ChatSheet` 를 연다 · 새 시트 없음). `hidden` 은 변형 없는 유틸이라 `native:inline-flex` 가 항상 뒤에 출력된다. 파일 머리 주석에 D-10 한 줄.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/layout/__tests__/app-shell-chrome.test.tsx src/components/layout/__tests__/app-sidebar.test.tsx src/components/chat/__tests__/chat-fab.test.tsx src/components/stock/__tests__/stock-hero.test.tsx</automated>
    <fails_when>non-zero exit, or the summary contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'data-slot="app-aside"' webapp/src/components/layout/app-shell.tsx` · `grep -n 'data-slot="app-menu-button"' webapp/src/components/layout/app-header.tsx` 가 각각 1줄
    - `grep -n 'html.native-app \[data-slot="app-aside"\]' webapp/src/styles/globals.css` 와 `grep -n 'html.native-app \[data-slot="app-menu-button"\]' webapp/src/styles/globals.css` 가 각각 1줄
    - `grep -n "native:hidden" webapp/src/components/chat/chat-fab.tsx` 가 1줄
    - `grep -n 'data-slot="stock-ai-button"' webapp/src/components/stock/stock-hero.tsx` 가 1줄 · `grep -n "native:inline-flex" webapp/src/components/stock/stock-hero.tsx` 가 1줄
    - `app-shell-chrome.test.tsx`(패딩 램프 불변식)가 수정 없이 통과한다
  </acceptance_criteria>
  <done>앱 모드에서만 사이드바가 드로어 전용이 되고 FAB 이 「AI 분석」 버튼으로 바뀌며, 브라우저 셸의 기존 단위 계약은 그대로다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: e2e 픽스처 `installNativeApp` + `native-shell.spec.ts` (앱 모드 셸 · 메시지 · 브라우저 회귀)</name>
  <files>webapp/e2e/fixtures/native-app.ts, webapp/e2e/specs/native-shell.spec.ts</files>
  <read_first>
    - webapp/e2e/specs/me.spec.ts (L1-40 import 관례 · 머리 「무엇을 증명하는가」 목록)
    - webapp/e2e/specs/home.spec.ts (L1-30 · L318-420 셸 불변식 케이스 — 뷰포트 설정·계산 스타일 측정 방식)
    - webapp/e2e/fixtures/home.ts (`mockHomeApi`) · webapp/e2e/fixtures/mock-api.ts (`mockStockApi` 옵션 · 종목 픽스처 코드) · webapp/e2e/fixtures/chat.ts (`mockChatApi`)
    - webapp/src/lib/native/native-detect.ts · webapp/src/lib/native/native-bridge-provider.tsx (21-01·21-04 — 메시지 모양)
    - webapp/playwright.config.ts (baseURL 3100 · setup/storageState · 단일 워커)
  </read_first>
  <behavior>
    - (앱·1280×800) `/` : `html` 에 클래스 `native-app` · `data-native-platform="ios"` · `[data-slot="app-aside"]` 숨김 · `[data-slot="app-menu-button"]` 보임 · 첫 캡처 메시지 `type === 'ready'` · `route {path:'/'}` 존재
    - (앱·1280) 햄버거 클릭 → 드로어(role=dialog) 보임 → 캡처에 `overlay {open:true}` → Escape → 드로어 사라짐 → `overlay {open:false}`
    - (앱·390×844) `/stocks/<mock 코드>` : `[data-slot="chat-fab"]` 보이지 않음 · `[data-slot="stock-ai-button"]` 보임 → 클릭 → 챗 시트(role=dialog) 보임 + `overlay {open:true}`
    - (앱) `page.evaluate(() => window.__ghTrade.navigate('/scanner'))` → `true` · URL `/scanner` · 캡처에 `route {path:'/scanner'}` · 전체 새로고침 없음(사전에 `window.__marker = 1` 을 심고 이동 뒤에도 남아 있다)
    - (브라우저·1280) `/` : `html` 에 `native-app` 없음 · aside 보임 · 햄버거 숨김 · `window.__ghTrade` undefined · (390) 종목상세에 chat-fab 보임 · 「AI 분석」 숨김
  </behavior>
  <action>
    ① `webapp/e2e/fixtures/native-app.ts`: `installNativeApp(page, { platform = 'ios' } = {})` 는 `page.addInitScript` 로 문서 시작 전에 `window.__nativeMsgs = []` · `window.Capacitor = { isNativePlatform: () => true, getPlatform: () => platform }` 를 심고, `platform === 'ios'` 면 `window.webkit = { messageHandlers: { ghTrade: { postMessage: (m) => window.__nativeMsgs.push(JSON.parse(m)) } } }`, `android` 면 `window.GhTradeBridge = { postMessage: (m) => window.__nativeMsgs.push(JSON.parse(m)) }` 를 심는다. `nativeMessages(page)` = `page.evaluate(() => window.__nativeMsgs ?? [])`. 파일 머리 주석: 실제 인라인 감지 스크립트(21-01)와 Provider(21-04)를 그대로 태우는 이유 · addInitScript 는 매 내비게이션 문서마다 다시 실행된다. 전역 타입은 파일 안 `declare global { interface Window { __nativeMsgs?: CapturedNativeMsg[] } }` 로 둔다(e2e tsconfig 범위).
    ② `webapp/e2e/specs/native-shell.spec.ts`: `test.describe('Phase 21 — 앱 모드 셸 (MOBILE-01j)')` 와 `test.describe('Phase 21 — 브라우저 모드 회귀')` 두 묶음으로 `<behavior>` 5개를 케이스로 쓴다. 데이터는 `mockHomeApi(page)` · `mockStockApi(page, …)`(픽스처가 제공하는 종목 코드 사용) · 챗 케이스는 `mockChatApi(page)`. 메시지 단언은 `expect.poll(() => nativeMessages(page))` 로 기다린다. relay 가 필요한 화면은 쓰지 않는다. 파일 머리에 「무엇을 증명하는가 / 깨지면 사용자가 겪는 일」 번호 목록(D-09 · D-10 · D-11 · D-12 · D-06a).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/native-shell.spec.ts</automated>
    <fails_when>non-zero exit, or output contains "failed" or "No tests found", or lacks "passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS" (tsconfig.e2e.json 포함)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function installNativeApp" webapp/e2e/fixtures/native-app.ts` 가 1줄 · `grep -n "addInitScript" webapp/e2e/fixtures/native-app.ts` 가 1줄 이상
    - `grep -n "stock-ai-button" webapp/e2e/specs/native-shell.spec.ts` 가 1줄 이상 · `grep -n "__ghTrade.navigate" webapp/e2e/specs/native-shell.spec.ts` 가 1줄 이상
    - native-shell.spec 이 앱 모드·브라우저 모드 두 describe 모두 통과한다
  </acceptance_criteria>
  <done>앱 모드 셸 분기와 웹→네이티브 메시지가 실브라우저에서 관측되고, 브라우저 모드 셸은 회귀 없이 종전 그대로임이 e2e 로 잠겼다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e 픽스처가 심는 가짜 Capacitor | 테스트 전용 — 프로덕션 번들에 포함되지 않는다(e2e/ 디렉터리) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-15 | Elevation of Privilege | 「AI 분석」 버튼이 권한 없는 챗 호출 | low | accept | 기존 `openChat` 경로 재사용 — 챗 API 는 서버가 JWT 로 검증(Phase 14 CHAT-01) · 새 권한 경로 없음 |
| T-21-29 | Spoofing | 웹 사용자가 `native-app` 클래스를 강제로 붙여 앱 분기 유발 | low | accept | 분기는 표시(사이드바·FAB)뿐이고 권한·데이터 경로가 없다 — 스스로의 화면만 바뀐다 |
| T-21-SC | Tampering | 패키지 설치 | low | accept | 설치 없음 |
</threat_model>

<verification>
- 셸 관련 단위 4파일 green · typecheck(e2e 포함) green · `native-shell.spec.ts` green
</verification>

<success_criteria>
- D-09·D-10·D-13 의 웹 쪽 분기가 클래스 하나로 동작하고, 앱 모드를 브라우저에서 재현하는 공용 픽스처가 생겼다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/` 패키지 `@gh-radar/mobile` — scripts `native:sync` · `native:sync:dev` · `native:build:ios` · `native:build:android` · `native:smoke:ios` · `native:open:ios` · `native:open:android` · `native:assets` (21-01) · `native:smoke:android` (21-02) · `native:check-tab-routes:ios` (21-10) · `native:test:android` (21-12) · `native:verify-prod` (21-16) | 패키지·스크립트 | 21-01 · 21-02 · 21-10 · 21-12 · 21-16 |
| `mobile/capacitor.config.ts` — appId `com.ghtrade.app` · appName `GH Trade` · `CAP_SERVER_URL` · `SystemBars` (21-01) · `SocialLogin` (21-15) | 설정 | 21-01 · 21-15 |
| `mobile/tsconfig.json` · `mobile/.gitignore` | 설정 | 21-01 |
| `mobile/www/index.html` — 오프라인 폴백(최소 21-01 · `safeTarget`·5초 탐침·`online`·테마 21-11) | 정적 페이지 | 21-01 · 21-11 |
| `mobile/scripts/smoke-ios.sh` · `mobile/scripts/smoke-android.sh` — 「SMOKE OK ready platform=…」 | 스모크 | 21-01 · 21-02 |
| `mobile/scripts/check-tab-routes-ios.sh` · `mobile/scripts/tab-routes-check.swift` | Swift 경로표 검사 | 21-10 |
| `mobile/scripts/render-resources.mjs` · `mobile/resources/{icon.svg,icon-only.png,icon-foreground.png,icon-background.png,splash.png,splash-dark.png}` | 에셋 원본 | 21-14 |
| `mobile/scripts/verify-prod-config.mjs` · `mobile/README.md` | 운영 설정 검사·문서 | 21-16 |
| iOS `SceneDelegate.swift` (루트 = `GHTradeBridgeViewController()` · 창 배경 21-11) | Swift | 21-01 · 21-11 |
| iOS `GHTradeBridgeViewController` · `WeakScriptMessageHandler` · `NativeMessageType` | Swift | 21-01 · 21-10 · 21-11 |
| iOS `GHTradeTabBar` · `GHTradeTab` · `GHTabID` · `GHTradePalette` · `GHTradeTheme` · `TabRoutes.activeTab(forPath:)` · `TabRoutes.hidesTabBar(path:)` | Swift | 21-10 |
| iOS `NavigationDelegateProxy` · `ThemeStore` (UserDefaults `gh-trade.theme`) | Swift | 21-11 |
| Android `MainActivity` (`BridgeActivity`) · `GhTradeWebViewClient` (`BridgeWebViewClient` 상속) · `GhTradeBridge` (`@JavascriptInterface postMessage`) | Kotlin | 21-02 · 21-12 · 21-13 |
| Android `GhTradeTabBar` · `GhTradePalette` · `TabId` · `TabRoutes.activeTabFor` · `TabRoutes.hidesTabBar` · `TabRoutesTest` | Kotlin | 21-12 |
| Android `ThemeStore` (SharedPreferences `gh_trade`/`theme`) · `res/values/bools.xml` · `res/values-sw600dp/bools.xml` (`is_tablet`) | Kotlin·리소스 | 21-13 |
| Android `res/drawable/ic_tab_{home,home_fill,search,search_fill,trading,trading_fill,ai,ai_fill,me,me_fill}.xml` | 벡터 | 21-12 |
| 웹 `lib/native/native-detect.ts` — `NATIVE_DETECT_SCRIPT` · `isNativeApp` · `nativePlatform` | 유틸 | 21-01 |
| 웹 `lib/native/post-native.ts` — `postNative` · `NativeMsgType` | 유틸 | 21-04 |
| 웹 `lib/native/native-bridge-provider.tsx` — `NativeBridgeProvider` · `useNativeBridge` · `NativeRefreshFn` · `window.__ghTrade = { refresh, navigate, back }` | Provider | 21-04 |
| 웹 `lib/native/use-native-refresh.ts` — `useNativeRefresh` | 훅 | 21-04 |
| 웹 `lib/native/native-overlay-marker.tsx` — `NativeOverlayMarker` | 컴포넌트 | 21-04 |
| 웹 `lib/native/google-client-ids.ts` — `GOOGLE_WEB_CLIENT_ID` · `GOOGLE_IOS_CLIENT_ID` · `GOOGLE_ANDROID_CLIENT_ID` · `GOOGLE_IOS_URL_SCHEME` | 공개 식별자 상수 | 21-03 |
| 웹 `lib/native/native-google-login.ts` — `nativeGoogleSignIn` · `randomNonce` · `sha256Hex` · `classifyNativeLoginError` | 서비스 | 21-15 |
| 웹 `RelayConnectionState.probeNow` · `EMPTY_RELAY_VALUE.probeNow` | 훅 계약 | 21-07 |
| 웹 `lib/recent-search.ts` — `RECENT_SEARCH_KEY` · `RECENT_SEARCH_MAX` · `RecentSearchItem` · `readRecentSearches` · `pushRecentSearch` · `removeRecentSearch` · `clearRecentSearches` | 유틸 | 21-08 |
| 웹 `app/search/page.tsx` · `components/search/search-page-client.tsx` — `SearchPageClient` · 사이드바 `NAV_SEARCH_PAGE` | 페이지·상수 | 21-08 |
| 웹 `components/me/account-card.tsx` — `AccountCard` | 컴포넌트 | 21-09 |
| 웹 마크업 계약 `data-slot="app-aside"` · `data-slot="app-menu-button"` · `data-slot="stock-ai-button"` · `data-slot="shared-panels"` · `data-pinned` · `data-order-cta` · `data-slot="account-card"` | 마크업 | 21-05 · 21-06 · 21-09 |
| CSS `@custom-variant native` (21-04) · 앱 셸 규칙 (21-05) · `--app-safe-top` · `--app-safe-bottom` · `--app-safe-left` · `--app-safe-right` · `--native-tabbar-gap` · `--native-tabbar-offset` · `--native-body-reserve` (21-06) | 토큰·변형 | 21-04 · 21-05 · 21-06 |
| 웹 `app/icon.svg` (= 스케치 004 `#app-a`) | 파비콘 | 21-14 |
| 단위 테스트: `native-detect.test.ts` (21-01) · `post-native.test.ts` · `native-bridge-provider.test.tsx` · `native-overlay.test.tsx` (21-04) · `google-client-ids.test.ts` (21-03) · `relay-socket.test.ts` probeNow · `stock-native-refresh.test.tsx` (21-07) · `recent-search.test.ts` · `search-page-client.test.tsx` · `app-sidebar.test.tsx` 보강 (21-08) · `account-card.test.tsx` · `app-shell-chrome.test.tsx` 보강 (21-09) · `native-google-login.test.ts` · `login/__tests__/page.test.tsx` (21-15) · `TabRoutesTest.kt` (21-12) | 단위 | 각 플랜 |
| e2e: `fixtures/native-app.ts` — `installNativeApp` · `nativeMessages` (21-05) · `specs/native-shell.spec.ts` (21-05 · 21-06) · `specs/search-page.spec.ts` (21-08) · `specs/brand-account.spec.ts` (21-09) | Playwright | 각 플랜 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-05-SUMMARY.md` when done
</output>
