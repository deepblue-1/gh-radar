---
phase: 21-gh-trade-mobile-app
plan: 29
type: execute
wave: 13
depends_on: ["21-25", "21-27", "21-28"]
files_modified:
  - mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
  - mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt
  - mobile/android/app/src/main/java/com/ghtrade/app/GhTradeBridge.kt
  - mobile/android/app/build.gradle
  - mobile/android/app/src/main/AndroidManifest.xml
  - mobile/android/app/src/main/res/xml/data_extraction_rules.xml
  - mobile/www/index.html
  - webapp/e2e/specs/theme-default.spec.ts
autonomous: true
gap_closure: true
requirements: [MOBILE-01]
coupling_justified:
  - "21-28: iOS 쪽 D-12a · D-12b 수치와 dev=1 신호 조건을 같은 값으로 옮긴다 — 파일 겹침 없음 · 21-28 이 앞 웨이브"
  - "21-27: theme-default.spec.ts 를 앞 웨이브에서 먼저 고친다(IN-06) — 이 플랜은 그 뒤에 폴백 케이스를 덧붙인다"

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G-21-R3-1 (Android · D-12a): IME 가 올라오기 시작하는 순간(애니메이션 onPrepare) 탭바가 즉시 짧게(키보드 애니메이션의 절반 · 80~200ms) 이동 없이 사라진다 — 키보드 위로 끌려 올라간 탭바가 보이는 프레임이 없다 · 키보드가 내려가면 90ms 뒤 다시 보인다"
    - "G-21-R3-4 (Android · D-12b): 콜드 스타트와 로그인 뒤 전체 문서 로드(onPageStarted) 동안 탭바가 숨어 있고 첫 route 메시지 또는 1.5초 뒤 280ms 감속으로 나타난다"
    - "G-21-CR · IN-01: ready 메시지에서 pullBlocked 가 false 로 초기화돼 새 문서의 첫 당김이 막히지 않는다"
    - "G-21-CR · WR-03: Android 브리지가 21-25 사용자 답대로 동작한다 — a: WebMessageListener(허용 출처 = 서버 출처 · 메인 프레임만)로 받고 미지원 WebView 는 최상위 출처 전체 비교 폴백 / b: 최상위 출처 스킴·호스트·포트 전체 비교(하위 프레임 판정 불가 — 부분 수정 근거 기록)"
    - "G-21-CR · WR-02: 설치된 APK 매니페스트가 allowBackup=false · fullBackupContent=false · dataExtractionRules(cloud-backup · device-transfer 모두 전 도메인 제외)를 갖는다"
    - "G-21-CR · IN-02: 오프라인 폴백 페이지는 복귀 대상으로 운영 출처만 허용하고 localhost:3100 은 `dev=1` 이 있을 때만 허용한다 · Android 도 dev 빌드에서만 dev=1 을 붙인다"
    - "G-21-CR · IN-03: 오프라인 폴백에서 뒤로가기 루프를 에뮬레이터로 재현해 보고, 재현되면 폴백 화면의 뒤로가기 = 앱 종료로 고친다(재현 안 되면 근거 기록)"
  artifacts:
    - path: mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
      provides: "IME 애니메이션 콜백 · awaitingContent · pullBlocked 초기화 · dev=1 · (재현 시) 폴백 뒤로가기 종료"
      contains: "setWindowInsetsAnimationCallback"
    - path: mobile/android/app/src/main/res/xml/data_extraction_rules.xml
      provides: "cloud-backup · device-transfer 전 도메인 제외"
      contains: "device-transfer"
    - path: mobile/www/index.html
      provides: "PROD_ORIGINS · dev=1 일 때만 DEV_ORIGINS · data-return-target"
      contains: "PROD_ORIGINS"
  key_links:
    - from: mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt
      to: mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
      via: "onPageStarted(super 먼저) → host.onDocumentStart() → 첫 route 에서 endAwaitingContent()"
      pattern: "onDocumentStart"
    - from: mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
      to: mobile/android/app/src/main/java/com/ghtrade/app/GhTradeBridge.kt
      via: "load() → GhTradeBridge(this).register(wv) (21-25 WR-03 답에 따른 채널)"
      pattern: "register\\(wv\\)"
  prohibitions:
    - statement: "MUST NOT 웹 송신 계약(window.GhTradeBridge.postMessage(json) · 메시지 화이트리스트)을 바꾼다"
      status: unverified
      verification: "이 플랜 커밋의 webapp/src 파일 0 · 스모크 ready 도착"
    - statement: "MUST NOT 21-25 답이 b 인데 androidx.webkit 을 선언한다"
      status: unverified
      verification: "Task 2 acceptance — 선택별 build.gradle diff 검사"
---

<objective>
Android 셸의 UAT 3차 갭과 리뷰 finding 전부 — **G-21-R3-1**(키보드 위 탭바) · **G-21-R3-4**(로그인 전환 중 탭바) · **IN-01**(pullBlocked 잔존) · **WR-03**(브리지 하위 프레임·출처) · **WR-02**(백업 켜짐) · **IN-02**(폴백 페이지 운영 번들 localhost 허용) · **IN-03**(오프라인 뒤로가기 루프). 21-28(iOS)과 같은 수치를 쓰되 새 정책 타입·표 파일 없이 MainActivity 안에서 직접 고친다(과설계 금지). WR-03 은 21-25 체크포인트의 사용자 답(a/b · 21-CONTEXT 「WR-03 Android 브리지 = 선택」 줄)대로 구현한다 — 멈춤 없음.

Purpose: 입력할 때와 로그인 직후 탭바가 어색하게 보이지 않고, 브리지·백업·폴백이 리뷰가 지적한 노출을 막는다.
Output: MainActivity · GhTradeWebViewClient · GhTradeBridge · (답 a) build.gradle · 매니페스트 + data_extraction_rules.xml · 폴백 페이지 + e2e 3케이스 · IN-03 재현 기록.

스모크 전제: dev 서버 :3100(없으면 `PORT=3100 pnpm --filter @gh-radar/webapp run dev` · dev.sh 금지) · relay 금지 · 에뮬레이터 하나(21-28 의 ANDROID_SERIAL 규칙).

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add(`git add -A` 금지) · 커밋 직전 `git status -sb` · **push 금지**(21-36).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-UAT.md
@.planning/phases/21-gh-trade-mobile-app/21-REVIEW.md
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
@mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt
@mobile/android/app/src/main/java/com/ghtrade/app/GhTradeBridge.kt
@mobile/android/app/src/main/AndroidManifest.xml
@mobile/www/index.html

<interfaces>
```text
MainActivity: 상태 overlayOpen · pullBlocked · keyboardVisible · hideRunnable · isOfflinePage
  load()(102~): wv.addJavascriptInterface(GhTradeBridge(this), "GhTradeBridge")(106) · serverHost()(133) · serverUri()(135)
  showOffline(failedUrl)(≈218~): https://localhost/index.html?to=<enc>&theme=<현재> · loadDataWithBaseURL
  handleBack(≈236) · navigateBack(≈248: canGoBack → goBack · 홈 아님 → 홈 · 홈 → finish)
  setupTabBar(≈262~): 탭바 인셋 리스너 · OnGlobalLayoutListener IME 판정 · 시작 GONE/alpha 0
  onNativeMessage(≈338~): ready(overlayOpen 해제) · route(applyPath) · pull(pullBlocked)
  updateTabBarVisibility(≈375~): 숨김 postDelayed 150 + 200(translationY 8dp) / 보임 200
GhTradeBridge: postMessage(json) @JavascriptInterface → runOnUiThread handle · handle 이 현재 WebView URL 호스트만 서버 호스트와 비교(25~26)
GhTradeWebViewClient: 모든 오버라이드는 super 먼저
androidx.core WindowInsetsAnimationCompat.Callback(DISPATCH_MODE_CONTINUE_ON_SUBTREE): onPrepare · onProgress · onEnd · typeMask · durationMillis
WebMessageListener(androidx.webkit): WebViewCompat.addWebMessageListener(wv, "GhTradeBridge", setOf(origin)) { view, message, sourceOrigin, isMainFrame, replyProxy -> } · WebViewFeature.WEB_MESSAGE_LISTENER — 주입 객체 GhTradeBridge.postMessage(string) 는 웹 계약과 같은 모양
variables.gradle: androidxWebkitVersion = '1.14.0'(capacitor-android implementation — 앱 컴파일 클래스패스에는 없음)
www/index.html(≈94~101): ALLOWED_ORIGINS = [운영, http://localhost:3100] · DEFAULT_TARGET · safeTarget
theme-default.spec.ts(≈94~): FALLBACK_URL = file URL of mobile/www/index.html
iOS 짝 수치(21-28): 키보드 숨김 = 애니메이션 × 0.5 [0.08,0.2]초 · 재표시 0.09초 · 대기 1.5초 · 대기 해제 표시 0.28초 감속
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — IME 애니메이션 콜백 즉시 숨김 · 재표시 90ms · 문서 로드 대기(onPageStarted → 첫 route/1.5초) · ready 에서 pullBlocked 초기화 → 빌드 · 스모크 (G-21-R3-1 · G-21-R3-4 · IN-01)</name>
  <files>mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt, mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt</files>
  <read_first>
    - mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt (28~85 상태 · 260~310 setupTabBar · 338~410 메시지 · 표시/숨김)
    - mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt (전체)
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-R3-1 · G-21-R3-4 root_cause)
    - .planning/phases/21-gh-trade-mobile-app/21-REVIEW.md (IN-01)
    - .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md (D-12a · D-12b)
  </read_first>
  <action>
    per D-12a · D-12b: ① 상태 `keyboardAnimMs = 250L` · `showRunnable: Runnable?` · `hiddenByKeyboard = false` · `awaitingContent = true`(콜드 스타트 첫 문서 대기) · `awaitingTimeout = Runnable { endAwaitingContent() }`. `shouldHideTabBar()` 에 `awaitingContent` 를 더한다. ② `setupTabBar` 에서 **탭바 뷰에만** `ViewCompat.setWindowInsetsAnimationCallback(tabBar, object : WindowInsetsAnimationCompat.Callback(DISPATCH_MODE_CONTINUE_ON_SUBTREE) { … })`: `onPrepare` — IME 애니메이션이고 루트 창 인셋에서 IME 가 아직 안 보이면(올라오는 방향) `keyboardAnimMs = animation.durationMillis` · `keyboardVisible = true` · `updateTabBarVisibility()`(레이아웃 전 즉시) · `onProgress` 는 insets 그대로 · `onEnd` — IME 면 실제 가시성으로 `keyboardVisible` 을 맞추고 갱신. 기존 OnGlobalLayoutListener 는 보정용으로 남긴다(주석). ③ `updateTabBarVisibility()` 숨김 분기: 사유가 키보드면 `hideRunnable`/`showRunnable` 제거 → 지연 없이 `(keyboardAnimMs / 2).coerceIn(80L, 200L)` ms 로 alpha 0 · translationY 0 → 끝에서 여전히 숨김이면 GONE · `hiddenByKeyboard = true`. 다른 사유는 기존 150 + 200 경로. 보임 분기: `hiddenByKeyboard` 였으면 90ms 뒤(`showRunnable` · 그 사이 숨김이 오면 제거), 대기 해제로 보이는 경우 280ms `DecelerateInterpolator`, 그 외 즉시 200ms. ④ `GhTradeWebViewClient.onPageStarted` — **super 먼저**, 이어 `host.onDocumentStart()`(KDoc: pushState 는 오지 않는다). MainActivity `fun onDocumentStart()`(대기 true · 1.5초 타임아웃 재예약 · 갱신) · `private fun endAwaitingContent()`(대기 중일 때만 false · 콜백 제거 · 갱신). `"route"` 분기에서 `applyPath` **전에** `endAwaitingContent()`. ⑤ IN-01: `"ready"` 분기에서 `pullBlocked = false` 도 설정(주석 한 줄). 웹 코드·화이트리스트 무변경. SUMMARY 에 콜드 스타트 → 탭바 표시 시각 한 번 실측(참고값).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:android && pnpm --filter @gh-radar/mobile run native:smoke:android</automated>
    <fails_when>non-zero exit, or output lacks "BUILD SUCCESSFUL" or "SMOKE OK ready platform=android", or contains "SMOKE FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - 콜백 대상은 탭바뿐: `grep -n "setWindowInsetsAnimationCallback" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 의 모든 줄이 `setWindowInsetsAnimationCallback(tabBar` 를 담는다
    - `grep -c "coerceIn(80L, 200L)" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` ≥ 1 · `grep -c "showRunnable" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` ≥ 2
    - `grep -c "super.onPageStarted" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt` ≥ 1 · `grep -c "host.onDocumentStart()" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt` ≥ 1
    - `grep -c "fun onDocumentStart" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` ≥ 1 · `grep -c "endAwaitingContent" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` ≥ 2 · `grep -c "1500L" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` ≥ 1
    - IN-01: `grep -n "pullBlocked = false" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 줄이 `"ready" ->` 줄과 `"route" ->` 줄 사이에 있다
    - 웹 무변경(이 플랜 커밋 기준): `L="$(git log --grep='(21-29)' --name-only --format=)" && test "$(printf '%s\n' "$L" | grep -c '^webapp/src/')" -eq 0`
    - verify 명령 통과
  </acceptance_criteria>
  <done>Android 탭바가 IME 가 올라오는 순간 즉시 사라지고, 전체 문서 로드 동안 숨었다가 첫 route 뒤 감속으로 나타나며, 새 문서의 당김이 막히지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: 브리지 채널(WR-03 · 21-25 답) · 백업 끄기(WR-02)</name>
  <reversibility rating="reversible">답 a 의 선언 1줄과 채널 교체 — 되돌리면 addJavascriptInterface 로 돌아간다. 웹 송신 계약 무변경. 백업 속성은 매니페스트 되돌림으로 복구.</reversibility>
  <files>mobile/android/app/src/main/java/com/ghtrade/app/GhTradeBridge.kt, mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt, mobile/android/app/build.gradle, mobile/android/app/src/main/AndroidManifest.xml, mobile/android/app/src/main/res/xml/data_extraction_rules.xml</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md (「WR-03 Android 브리지 = 선택」 줄 — 21-25 답)
    - .planning/phases/21-gh-trade-mobile-app/21-REVIEW.md (WR-03 · WR-02)
    - mobile/android/app/src/main/java/com/ghtrade/app/GhTradeBridge.kt (전체)
    - mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt (102~140 load · serverHost · serverUri)
    - mobile/android/app/build.gradle · mobile/android/variables.gradle
    - mobile/android/app/src/main/AndroidManifest.xml · mobile/android/app/src/main/res/xml/file_paths.xml (res/xml 관례)
  </read_first>
  <action>
    ① 출처 판정(답 a · b 공통): `GhTradeBridge` 에 비공개 `isAppOrigin(uri: Uri?): Boolean` — 서버 URI 와 스킴(소문자) · 호스트(소문자) · 포트(`Uri.port`, 없음 = -1) 전부 정확 일치(하나라도 null 이면 false). `handle` 의 호스트만 보던 검사를 최상위 URL 에 이 판정을 쓰는 것으로 바꾼다. 별도 파일·JUnit 은 만들지 않는다.
    ② 답 a: `app/build.gradle` dependencies 에 `implementation "androidx.webkit:webkit:$androidxWebkitVersion"`(주석 「WR-03 — capacitor-android 가 이미 싣는 1.14.0 과 같은 변수」). `GhTradeBridge.register(wv: WebView)`: `WebViewFeature.isFeatureSupported(WebViewFeature.WEB_MESSAGE_LISTENER)` 이고 서버 출처 문자열(`scheme://host` + 포트가 -1 이 아니면 `:port`, 인라인 조립)이 있으면 `WebViewCompat.addWebMessageListener(wv, "GhTradeBridge", setOf(origin)) { _, message, sourceOrigin, isMainFrame, _ -> … }` — `isMainFrame` 이 false 거나 `isAppOrigin(sourceOrigin)` 이 false 면 버리고, 아니면 `message.data` 로 기존 판정(JSON · 화이트리스트)을 부른다. 미지원이면 `addJavascriptInterface(this, "GhTradeBridge")` 폴백(① 의 전체 출처 비교). 채널을 `Log.i(TAG, "bridge channel=webmessage|jsinterface")` 로 한 번 남긴다. 답 b: 선언 없이 `register` 는 인터페이스 경로 + ① 만 — SUMMARY 에 「하위 프레임 판정 불가 · 부분 수정」 근거. MainActivity `load()` 의 등록 줄을 `GhTradeBridge(this).register(wv)` 로. 스모크 ready 가 안 오면(첫 문서 주입 시점을 놓친 경우) 등록을 Capacitor 플러그인 `load()`(첫 로드 전에 돈다)로 옮기고 그 이유를 SUMMARY 에 적는다.
    ③ WR-02: 매니페스트 `<application>` 의 `android:allowBackup` 을 `"false"` 로 바꾸고 `android:fullBackupContent="false"` · `android:dataExtractionRules="@xml/data_extraction_rules"` 를 더한다(위에 XML 주석 「WR-02 — WebView 쿠키 DB 에 주문 가능한 세션 · 자동 백업·기기 이전 모두 끔」). 새 `res/xml/data_extraction_rules.xml`: `<cloud-backup>` · `<device-transfer>` 각각에 `root` · `file` · `database` · `sharedpref` · `external` 다섯 도메인 `path="."` 제외(머리 주석: 테마 저장값도 백업 안 함 — 새 기기는 기본 다크).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:android && SDK="${ANDROID_HOME:-$HOME/Library/Android/sdk}" && AAPT2="$(ls "$SDK"/build-tools/*/aapt2 | sort -V | tail -1)" && M="$("$AAPT2" dump xmltree --file AndroidManifest.xml mobile/android/app/build/outputs/apk/debug/app-debug.apk)" && printf '%s\n' "$M" | grep -q 'allowBackup.*=false' && printf '%s\n' "$M" | grep -q 'fullBackupContent.*=false' && printf '%s\n' "$M" | grep -q 'dataExtractionRules' && echo "BACKUP OFF OK"</automated>
    <fails_when>non-zero exit, or "BACKUP OFF OK" is absent (an attribute is missing from the packaged manifest or aapt2 was not found)</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:smoke:android</automated>
    <fails_when>non-zero exit, or output lacks "SMOKE OK ready platform=android", or contains "SMOKE FAIL" or "Unresolved reference"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "GhTradeBridge(this).register(wv)" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` ≥ 1 · `grep -c "fun isAppOrigin" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeBridge.kt` ≥ 1
    - 답 a: `grep -c 'androidx.webkit:webkit:$androidxWebkitVersion' mobile/android/app/build.gradle` ≥ 1 · `grep -c "addWebMessageListener" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeBridge.kt` ≥ 1 · `grep -c "isMainFrame" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeBridge.kt` ≥ 1 · logcat 에 `bridge channel=webmessage` 가 SUMMARY 에 기록
    - 답 b: `L="$(git log --grep='(21-29)' --name-only --format=)" && test "$(printf '%s\n' "$L" | grep -c '^mobile/android/app/build.gradle$')" -eq 0`
    - `grep -c 'android:allowBackup="false"' mobile/android/app/src/main/AndroidManifest.xml` ≥ 1 · `grep -c '<device-transfer>' mobile/android/app/src/main/res/xml/data_extraction_rules.xml` ≥ 1 · `grep -c 'domain="root"' mobile/android/app/src/main/res/xml/data_extraction_rules.xml` ≥ 2
    - verify 두 명령 통과
  </acceptance_criteria>
  <done>Android 브리지가 사용자 답대로 출처(·프레임)를 가리고, 설치된 앱이 자동 백업·기기 이전에서 빠진다.</done>
</task>

<task type="auto">
  <name>Task 3: 오프라인 폴백 — dev=1 신호 · 페이지 허용 출처 분리 · e2e 3케이스(IN-02) · 뒤로가기 루프 재현 확인(IN-03)</name>
  <files>mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt, mobile/www/index.html, webapp/e2e/specs/theme-default.spec.ts</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-REVIEW.md (IN-02 · IN-03 재현 절차)
    - mobile/www/index.html (전체 — 인라인 스크립트 · T-21-06 주석)
    - mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt (showOffline · handleBack · navigateBack)
    - webapp/e2e/specs/theme-default.spec.ts (90~130행 — FALLBACK_URL · file URL 케이스 모양 · 21-27 이 더한 IN-06 케이스)
    - .planning/phases/21-gh-trade-mobile-app/21-13-SUMMARY.md (첫 실행 오프라인 → 뒤로가기 = 종료 기록)
  </read_first>
  <action>
    ① IN-02 Android 신호: `showOffline` URL 에 서버 URI 가 스킴 `http` 이고 호스트가 `localhost` 또는 `127.0.0.1` 일 때만 `&dev=1`.
    ② IN-02 페이지: `www/index.html` 허용 목록을 `PROD_ORIGINS = ['https://trade.jx1.io']` · `DEV_ORIGINS = ['http://localhost:3100']` 로 나누고 `new URLSearchParams(location.search).get('dev') === '1'` 일 때만 합친다. 계산된 복귀 대상을 `document.documentElement.setAttribute('data-return-target', target)` 로 남긴다. 주석 「T-21-06 · IN-02 — 운영 번들은 운영 출처만 · localhost 는 네이티브가 dev 빌드에서만 붙이는 dev=1 이 있을 때만」. 테마 · 문구 · 복귀 로직 그대로.
    ③ e2e(theme-default.spec.ts) 「오프라인 폴백 복귀 대상(IN-02)」 세 케이스: `?to=` localhost/me → `https://trade.jx1.io/` · 같은 URL + `&dev=1` → `http://localhost:3100/me` · `?to=` 운영 /trading → `https://trade.jx1.io/trading`.
    ④ IN-03 재현(dev 빌드 · 에뮬레이터): 홈이 뜬 뒤 `adb shell cmd connectivity airplane-mode enable` → `uiautomator dump` 로 탭바 「검색」 위치를 찾아 탭 → logcat `offline fallback for` 확인 → `KEYCODE_BACK` 3회(1초 간격) · 각 뒤 `offline fallback for` 누적 수와 앱이 앞에 남았는지 기록 → `airplane-mode disable`. 뒤로가기 뒤에도 앱이 남고 폴백이 다시 늘면 **재현** → `navigateBack` 첫 분기 `isOfflinePage -> finish()`(21-13 의 첫 실행 오프라인 동작과 같다) 후 같은 절차로 재확인. 재현 안 되면 코드 변경 없이 SUMMARY 「IN-03 재현 기록」 에 근거.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/theme-default.spec.ts</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or no summary line matches "[0-9]+ passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:android && pnpm --filter @gh-radar/mobile run native:smoke:android && AM="$(adb shell settings get global airplane_mode_on)" && test "$(printf '%s' "$AM" | tr -d '\r')" = "0" && echo "AIRPLANE OFF"</automated>
    <fails_when>non-zero exit, or output lacks "SMOKE OK ready platform=android" or "AIRPLANE OFF", or contains "SMOKE FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '&dev=1' mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` ≥ 1
    - `grep -c "PROD_ORIGINS" mobile/www/index.html` ≥ 1 · `grep -c "get('dev') === '1'" mobile/www/index.html` ≥ 1 · `grep -c "data-return-target" mobile/www/index.html` ≥ 1
    - `grep -c "IN-02" webapp/e2e/specs/theme-default.spec.ts` ≥ 1
    - SUMMARY 「IN-03 재현 기록」 에 단계 · 누적 수 · 판정 · (재현이면) `isOfflinePage -> finish()` 재확인 결과
    - verify 두 명령 통과
  </acceptance_criteria>
  <done>운영 폴백 페이지는 운영 호스트로만 돌아가고 dev 빌드만 localhost 로 돌아가며, IN-03 은 재현 결과대로 고쳐졌거나 근거가 남았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView 문서(메인·하위 프레임) → GhTradeBridge | 앱 서버가 아닌 문서·프레임이 네이티브 상태를 바꿀 수 있다 |
| 앱 데이터 → 클라우드 백업·기기 이전 | WebView 쿠키 DB 의 세션이 백업본에 실린다 |
| 오프라인 폴백 → 복귀 URL | 운영 번들이 dev 출처로 복귀 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03 | Spoofing | Android 브리지 하위 프레임·출처(WR-03) | medium | mitigate | 답 a: WebMessageListener 허용 출처 + isMainFrame · 폴백은 전체 출처 비교 / 답 b: 전체 출처 비교 + 부분 수정 근거(Task 2) |
| T-21-67 | Information Disclosure | 백업으로 세션 쿠키 유출(WR-02) | high | mitigate | allowBackup=false · fullBackupContent=false · dataExtractionRules 전 도메인 제외 · aapt2 확인(Task 2) |
| T-21-06 | Tampering | 폴백 페이지 dev 출처 복귀(IN-02) | low | mitigate | 운영/개발 목록 분리 · dev=1 은 dev 빌드만 · e2e 3케이스(Task 3) |
| T-21-65 | Denial of Service | 탭바 숨김 고착(awaitingContent) | medium | mitigate | 1.5초 타임아웃 항상 해제(Task 1) |
| T-21-SC | Tampering | androidx.webkit 직접 선언 | medium | mitigate | 21-25 사람 결정 · 이미 APK 에 있는 1.14.0 과 같은 변수 · 답 b 면 선언 없음 검사 |
</threat_model>

<verification>
- native:build:android · native:smoke:android(태스크마다) · aapt2 매니페스트 · Playwright theme-default · 비행기 모드 해제 확인
- 사람 확인(키보드 · 로그인 직후 탭바 · 폴백 뒤로가기)은 21-36 UAT
</verification>

<success_criteria>
- G-21-R3-1 · R3-4 Android 쪽과 IN-01 · WR-03 · WR-02 · IN-02 · IN-03 이 닫히거나 근거가 기록됐다.
</success_criteria>

## Flagged assumptions

- **엣지 탐침 MOBILE-01 (unclassified · unresolved):** 경계는 IME 방향 판정 · 대기 1.5초 · 출처 세 요소 · 폴백 3케이스로 명시했다. 나머지는 21-36 UAT.
- 오래된 WebView(WEB_MESSAGE_LISTENER 미지원)는 폴백 경로로 남는다 — 에뮬레이터에서 어느 채널인지 로그로 기록한다.

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| 스케치 `.planning/sketches/008-uat3-surfaces/`(index.html · README.md) · MANIFEST 008 행 | 목업 | 21-25 |
| CONTEXT D-12a · D-12b · D-25a · D-29 · D-30 · D-31 · D-32(R3-11 축소: 스크롤 복원 + 스켈레톤 없음 · 상태 초기화 허용) · WR-03 androidx.webkit 선언 답 | 결정 기록 | 21-25 |
| `PAD_CHIPS.만원` = 천만(+1,000) · 오천만(+5,000) · 1억(+10,000) · 지우기 · 20-CONTEXT D-17 포인터 | 상수 · 결정 | 21-26 |
| `SettingGroup` 제목 없는 그룹 상단 `pt-1` | 클래스 | 21-26 |
| 카드 헤더 ✕ `data-slot="card-close"` · 종목정보 팝업 ✕ = `size-8` + lucide `XIcon` + `after:-inset-1.5`(히트 44) · 팝업 고정 높이 · `scrollbar-gutter:stable` · 폰 safe-area 패딩 | 컴포넌트 | 21-26 |
| `webapp/src/lib/safe-path.ts` `isSafeInternalPath` · `recent-search.ts` `RECENT_CODE_RE` · `PopoverContent` 안 `NativeOverlayMarker` · `useDebouncedSearch` 반환 `resultsQuery` · `viewport.themeColor` `#17171c` + `ThemeColorSync` | 함수 · 훅 · 메타 | 21-27 |
| iOS 키보드 사유 즉시 숨김 · 재표시 90ms · `awaitingContent`(didStartProvisionalNavigation → 첫 route/1.5초) · 초기 숨김 · 브리지 출처 scheme·host·port 비교 · 오프라인 `dev=1` · 스모크 기기 지정(`IOS_DEVICE_UDID` · `ANDROID_SERIAL`) · mobile/README 명령표 | Swift · 스크립트 · 문서 | 21-28 |
| Android IME 애니메이션 콜백 즉시 숨김 · `awaitingContent`(onPageStarted) · `pullBlocked` 초기화 · 오프라인 뒤로가기(재현 시) · 오프라인 `dev=1` · 폴백 페이지 dev=1 일 때만 localhost · allowBackup=false + `res/xml/data_extraction_rules.xml` · 브리지 WebMessageListener(21-25 답 a) | Kotlin · XML · HTML | 21-29 |
| `NewsFullList` · `DiscussionFullList` · `useNewsView` · `StockNewsTabPanel` · 섹션 `onShowAll` · 종목상세 `?tab=news&view=news\|discussions` · 옛 URL 리다이렉트 · 팝업 `newsView` | 컴포넌트 · 훅 · 라우트 | 21-30 |
| `useTabRootScrollMemory`(`webapp/src/lib/tab-scroll-memory.ts` · AppShell 에서 호출) · 기존 `lib/query-cache` 재사용: 검색 허브 · 대화 목록 · 오늘 주문 | 훅 · 캐시 | 21-31 |
| 앱 공용 패널 숨김 CSS · 카드 더티 바 탭바 비킴(프로브) · `StrategyLogFeedProvider` · `useStrategyLogFeed` · `diffLimitChasers` · /me 전략 로그(`me-strategy-log`) | CSS · Provider · 컴포넌트 | 21-32 |
| 작업대 `?code=` 소비(카드 보장·펼침·포커스) · 카드 수동주문 주문유형(지정가/시간외종가) | 컴포넌트 | 21-33 |
| 종목상세 「트레이딩」 CTA(폰 바 + `detail-trading-button`) · 호가주문 탭 제거 · `?tab=orderbook` → `/trading?code=` · 호가 탭 전용 코드·`variant` 삭제 · orderbook.spec 선별 이전 | 컴포넌트 · 정리 · e2e | 21-34 |
| 게이트 기록 · `21-REVIEW-R2.md`(13건 대조) · UAT 환경(21-35-SUMMARY) · UAT 3차 재검증 · push 결정(21-36-SUMMARY) | 문서 | 21-35 · 21-36 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-29-SUMMARY.md` when done — IME · 대기 수치 · WR-03 답과 채널 로그 · aapt2 결과 · 폴백 e2e · IN-03 재현 기록.
</output>
