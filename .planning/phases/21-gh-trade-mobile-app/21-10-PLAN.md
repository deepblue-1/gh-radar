---
phase: 21-gh-trade-mobile-app
plan: 10
type: execute
wave: 3
depends_on: ["21-01", "21-04"]
files_modified:
  - mobile/ios/App/App/TabRoutes.swift
  - mobile/ios/App/App/GHTradeTheme.swift
  - mobile/ios/App/App/GHTradeTabBar.swift
  - mobile/ios/App/App/GHTradeBridgeViewController.swift
  - mobile/ios/App/App.xcodeproj/project.pbxproj
  - mobile/scripts/tab-routes-check.swift
  - mobile/scripts/check-tab-routes-ios.sh
  - mobile/package.json
autonomous: true
requirements: [MOBILE-01]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-27 · D-27a: 탭바 모양·아이콘의 HTML 목업 게이트는 스케치 004 로 통과했다(2026-09-25 채택 A 알약·A 다크 레이더) — 이 플랜은 채택안 수치·색만 네이티브 상수로 옮기고 새 시안을 만들지 않는다"
    - "D-14: 활성 탭 = 경로 정확 매칭만 — `/`→홈 · `/search`·`/scanner`·`/themes`·`/watchlist`→검색 · `/trading`(쿼리·끝 슬래시 무시)→트레이딩 · `/chat`→AI · `/me`→마이 · 그 외(`/stocks/*` · `/themes/*` · `/trading/vi` · `/chat/*` 등) 5탭 전부 비활성 — Foundation 전용 `TabRoutes` 로 구현되고 swiftc 검사 스크립트가 표 전체를 단언한다"
    - "D-02 · D-27a: iOS 탭바 = UIKit 알약 — 높이 70 · radius 32(연속 곡률) · 좌우 16 · 폭 최대 560 가운데(iPad) · 바닥 = 화면 끝에서 max(inset − 14, 14)(safeArea.bottom + 14 우선순위 제약 + 화면 끝 −14 이하 필수 제약) · 블러 + `--card` 82% 덮개 · 1px `--line` 테두리 · 그림자 offset (0,2) 18% · 활성 = `--primary` 14% 원 46 + filled/굵은 심볼 + primary 라벨 10pt semibold · 비활성 `--muted-fg` · 탭바 위로 120pt 하단 페이드(`--bg` 92%)"
    - "D-15: 아이콘 = SF Symbols — 홈 `house`/`house.fill` · 검색 `magnifyingglass` · 트레이딩 `chart.line.uptrend.xyaxis` · AI `sparkles` · 마이 `person.crop.circle`/`person.crop.circle.fill`(fill 변형이 없는 심볼은 활성 시 굵기 semibold) · 없는 이름은 assertionFailure + `circle` 폴백(Pitfall 20)"
    - "D-12: 탭바 숨김 = 경로 `/login`·`/auth/*` · WebView 호스트가 server.url 호스트가 아님(오프라인 로컬 페이지) · 웹 `overlay {open:true}` 동안 · 키보드 표시 중 — 숨김은 150ms 지연 후 0.2s 페이드(+8pt 아래로), 다시 보일 때 지연 없이 0.2s"
    - "D-13: 탭바는 폭·기기와 무관하게 표시된다(iPad 가로 포함) — 폭 기준 숨김 분기 없음"
    - "D-06 · D-06a: 탭 탭 = `window.__ghTrade.navigate(path)` 를 evaluate(훅 없으면 `location.assign(path)`) — 클라 내비라 relay 소켓 유지 · 같은 탭 재탭도 navigate(웹이 최상단 스크롤) · 오프라인 로컬 페이지에서는 server.url+경로를 WebView 로 직접 로드"
    - "D-14 보강: URL 은 `webView.observe(\\.url)` KVO 와 웹 `route {path}` 메시지 둘 다로 받는다(SPA pushState 대비)"
  artifacts:
    - path: mobile/ios/App/App/TabRoutes.swift
      provides: "GHTabID · TabRoutes.activeTab(forPath:) · TabRoutes.hidesTabBar(path:) · normalize"
      contains: "enum TabRoutes"
    - path: mobile/ios/App/App/GHTradeTabBar.swift
      provides: "GHTradeTabBar(UIView) · GHTradeTab · 페이드 뷰"
      contains: "final class GHTradeTabBar"
    - path: mobile/ios/App/App/GHTradeTheme.swift
      provides: "GHTradeTheme(.light/.dark) · GHTradePalette(D-27a hex)"
      contains: "struct GHTradePalette"
    - path: mobile/scripts/check-tab-routes-ios.sh
      provides: "swiftc 로 TabRoutes 표 검사 → TAB ROUTES OK"
  key_links:
    - from: mobile/ios/App/App/GHTradeBridgeViewController.swift
      to: mobile/ios/App/App/TabRoutes.swift
      via: "KVO url · route 메시지 → TabRoutes.activeTab(forPath:) → tabBar.setActive"
      pattern: "TabRoutes\\.activeTab"
    - from: mobile/ios/App/App/GHTradeBridgeViewController.swift
      to: webapp/src/lib/native/native-bridge-provider.tsx
      via: "evaluateJavaScript(\"window.__ghTrade&&window.__ghTrade.navigate?…\")"
      pattern: "__ghTrade"
---

<assumption_delta_decision>
- 검출기 `assumption-delta scan 21` → `detected: false`. 수동 판정: 두 번째 클라이언트 플랫폼 — 정본 명사 **웹앱**, 결정 **add-alongside**. 탭바는 웹 경로를 읽기만 하고(URL·route 메시지) 이동은 웹 훅에 맡긴다(21-01 블록 참조).
</assumption_delta_decision>

<objective>
iOS 네이티브 하단 플로팅 탭바(D-02 · D-27a 채택 수치)를 `GHTradeBridgeViewController` 위에 얹고, 활성 판정(D-14) · 숨김 규칙(D-12 · 키보드) · 탭 이동(D-06a)을 웹 계약(21-04)과 잇는다.

Purpose: 앱 사용자가 홈·검색·트레이딩·AI·마이를 한 번에 오간다. 활성 판정 표는 Foundation 전용 순수 함수로 두어 Xcode 테스트 타깃 없이도 swiftc 로 검사한다.
Output: Swift 4파일(경로표·팔레트·탭바·VC 배선) + 검사 스크립트.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md
@.planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md
@.planning/phases/21-gh-trade-mobile-app/21-01-SUMMARY.md
@.planning/sketches/004-native-tab-bar/README.md
@mobile/ios/App/App/GHTradeBridgeViewController.swift

21-01 SUMMARY 의 pbxproj 등록 방식(동기화 그룹 여부)을 따른다 — 동기화 그룹이 아니면 새 Swift 파일마다 `PBXFileReference`·`PBXBuildFile`·App 그룹 children·`PBXSourcesBuildPhase` 를 등록한다. iOS 빌드는 Bash timeout 600000.

<interfaces>
21-01 · 21-04 계약:

```text
웹 → 네이티브(ghTrade 채널, JSON 문자열): {type:'route', payload:{path}} · {type:'overlay', payload:{open}} · {type:'theme', payload:{theme:'dark'|'light'}} · {type:'ready', …} · {type:'pull', …}
네이티브 → 웹: window.__ghTrade = { refresh(): Promise<void>; navigate(path: string): boolean; back(): boolean }  (앱에서만 · 하이드레이션 뒤)
GHTradeBridgeViewController: final class …: CAPBridgeViewController, WKScriptMessageHandler · NativeMessageType · log(Logger subsystem com.ghtrade.app)
bridge?.config.serverURL (URL) · bridge?.config.localURL (capacitor://localhost)
```

스케치 004 CSS(탭바 A · 수치 정본):

```css
.tabbar.va { left:16px; right:16px; bottom:calc(34px - 14px); height:70px; border-radius:32px;
  background: color-mix(in srgb, var(--card) 82%, transparent); backdrop-filter: blur(18px);
  box-shadow: 0 2px 16px rgba(0,0,0,.18), 0 0 0 1px var(--line) inset; }
.device.ipad .tabbar.va { left:50%; width:560px; transform:translateX(-50%); }
.tabbar .item { flex:1; flex-direction:column; align-items:center; gap:4px; }  .tabbar.va .item { justify-content:flex-start; padding-top:12px; }
.tabbar .item svg { width:26px; height:26px; }  .tabbar .item .lb { font-size:10px; font-weight:600; letter-spacing:-.01em; }
.tabbar.va .item .hl { width:46px; height:46px; border-radius:23px; background: color-mix(in srgb, var(--primary) 14%, transparent); top:7px; }
.fade.va { height:120px; background: linear-gradient(to bottom, transparent, color-mix(in srgb, var(--bg) 92%, transparent) 70%); }
.device.overlay .tabbar { opacity:0; transform: translateY(8px); }  /* transition .2s */
```

D-27a hex: 다크 card `#202027` · bg `#17171c` · primary `#3485fa` · muted `#9e9ea4` · line rgba(255,255,255,.07) / 라이트 card `#ffffff` · bg `#ffffff` · primary `#3182f6` · muted `#6b7684` · line `#e5e8eb`.

이 플랜이 새로 만드는 계약(21-11 이 소비):

```swift
enum GHTabID: String, CaseIterable { case home, search, trading, ai, me; var path: String; var title: String }
enum TabRoutes { static func normalize(_ path: String) -> String; static func activeTab(forPath path: String) -> GHTabID?; static func hidesTabBar(path: String) -> Bool }
enum GHTradeTheme: String { case light, dark }
struct GHTradePalette { let card, bg, primary, muted, line: UIColor; static func of(_ theme: GHTradeTheme) -> GHTradePalette }
final class GHTradeTabBar: UIView { var onSelect: ((GHTabID) -> Void)?; func setActive(_ tab: GHTabID?); func apply(_ palette: GHTradePalette); let fadeView: UIView }
// VC: var currentTheme: GHTradeTheme · var overlayOpen: Bool · func updateTabBarVisibility(animated:) · func selectTab(_:)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `TabRoutes.swift`(D-14 경로표 · Foundation 전용) + swiftc 검사 스크립트</name>
  <files>mobile/ios/App/App/TabRoutes.swift, mobile/scripts/tab-routes-check.swift, mobile/scripts/check-tab-routes-ios.sh, mobile/package.json, mobile/ios/App/App.xcodeproj/project.pbxproj</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md (D-06 · D-12 ① · D-14)
    - .planning/phases/21-gh-trade-mobile-app/21-01-SUMMARY.md (pbxproj 등록 방식)
    - mobile/package.json (scripts — build/typecheck/test/lint 키 금지 유지)
  </read_first>
  <behavior>
    - `normalize`: `""`→`/` · `/trading?focus=X`→`/trading` · `/scanner/`→`/scanner` · `/a#b`→`/a` · `/`→`/`
    - `activeTab`: `/`·`""`→home · `/search`·`/scanner`·`/scanner/`·`/themes`·`/watchlist`→search · `/trading`·`/trading?focus=X`·`/trading/`→trading · `/chat`→ai · `/me`→me · `/stocks/005930`·`/themes/abc`·`/trading/vi`·`/chat/123`·`/me/x`·`/searching`·`/login`·`/auth/callback`→nil
    - `hidesTabBar`: `/login`·`/login/x`·`/auth`·`/auth/callback`→true · `/`·`/me`·`/loginx`·`/author`→false
    - `GHTabID.allCases` 순서 = home, search, trading, ai, me · `path` = `/`,`/search`,`/trading`,`/chat`,`/me` · `title` = 홈, 검색, 트레이딩, AI, 마이
  </behavior>
  <action>
    ① `mobile/scripts/tab-routes-check.swift` 를 먼저 쓴다(RED — TabRoutes 없음): 최상위 코드로 behavior 의 모든 케이스를 `(입력, 기대)` 배열로 두고 실패마다 `print("FAIL …")` 후 실패 수가 0 이 아니면 `exit(1)`, 전부 통과면 `print("TAB ROUTES OK \(n)")`.
    ② `mobile/scripts/check-tab-routes-ios.sh`(bash · `set -euo pipefail` · `cd "$(dirname "$0")/.."`): 임시 디렉터리에 `tab-routes-check.swift` 를 `main.swift` 로 복사 → `xcrun swiftc -o "$TMP/check" ios/App/App/TabRoutes.swift "$TMP/main.swift"` → 실행. `mobile/package.json` scripts 에 `native:check-tab-routes:ios` = `bash scripts/check-tab-routes-ios.sh` 추가. 실행 권한.
    ③ `mobile/ios/App/App/TabRoutes.swift`(`import Foundation` 만 — UIKit 금지, 스크립트가 단독 컴파일한다): `enum GHTabID: String, CaseIterable { case home, search, trading, ai, me }` + `var path: String` · `var title: String`(홈·검색·트레이딩·AI·마이 — D-06 순서 고정). `enum TabRoutes` 의 `normalize`(쿼리·프래그먼트 제거 · 루트 외 끝 슬래시 제거 · 빈 문자열 `/`), `activeTab(forPath:)`(normalize 뒤 **정확 일치** 표 — D-14), `hidesTabBar(path:)`(normalize 뒤 `== "/login"` · `hasPrefix("/login/")` · `== "/auth"` · `hasPrefix("/auth/")` — D-12 ①). 머리 주석: D-14 정본 · Android `TabRoutes.kt`(21-12)와 같은 표를 유지해야 한다.
    ④ `TabRoutes.swift` 를 Xcode 프로젝트에 등록한다(21-01 방식).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:check-tab-routes:ios</automated>
    <fails_when>non-zero exit, or output contains "FAIL", or lacks "TAB ROUTES OK"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "import UIKit" mobile/ios/App/App/TabRoutes.swift` 가 0 · `grep -n "static func activeTab(forPath" mobile/ios/App/App/TabRoutes.swift` 가 1줄
    - `grep -n "native:check-tab-routes:ios" mobile/package.json` 가 1줄
    - `grep -n "TabRoutes.swift" mobile/ios/App/App.xcodeproj/project.pbxproj` 가 1줄 이상(동기화 그룹이면 이 항목 대신 SUMMARY 기록으로 갈음)
  </acceptance_criteria>
  <done>D-14 활성 판정·D-12 경로 숨김이 순수 함수로 표 전체가 검사된다.</done>
</task>

<task type="auto">
  <name>Task 2: `GHTradeTheme`·`GHTradePalette`(D-27a hex) + `GHTradeTabBar` 알약 뷰 + 하단 페이드 (D-02 · D-15 · D-27a)</name>
  <files>mobile/ios/App/App/GHTradeTheme.swift, mobile/ios/App/App/GHTradeTabBar.swift, mobile/ios/App/App.xcodeproj/project.pbxproj</files>
  <read_first>
    - .planning/sketches/004-native-tab-bar/README.md (「결정」 전체)
    - .planning/sketches/004-native-tab-bar/index.html (L138-163 탭바 A CSS)
    - /Users/alex/repos/weekly-wine-app/ios/App/App/CookieViewController.swift (L279-330 `setupTabBar` 구조 · L435 `tabTapped` · L784-830 `GradientBlurView`)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pattern 2 「탭바 상수」 · Pitfall 20)
  </read_first>
  <action>
    ① `GHTradeTheme.swift`(`import UIKit`): `enum GHTradeTheme: String { case light, dark }` · `struct GHTradePalette { let card, bg, primary, muted, line: UIColor; static func of(_ t: GHTradeTheme) -> GHTradePalette }` — 값은 D-27a hex 그대로(다크 `#202027` · `#17171c` · `#3485fa` · `#9e9ea4` · white 7% / 라이트 `#ffffff` · `#ffffff` · `#3182f6` · `#6b7684` · `#e5e8eb`), hex → `UIColor` 헬퍼 `UIColor(hex: UInt32, alpha: CGFloat = 1)` 를 같은 파일 fileprivate 확장으로. 주석: 정본 = CONTEXT D-27a · 웹 토큰 `toss-dark.css`/`toss-light.css`.
    ② `GHTradeTabBar.swift`(`import UIKit`): `final class GHTradeTabBar: UIView` — 자신은 투명 컨테이너로 그림자(`shadowColor` black · `shadowOpacity 0.18` · `shadowOffset (0,2)` · `shadowRadius 8` — CSS blur 16 ≒ UIKit 8 · `clipsToBounds=false`)를 지고, 안쪽 `pill`(cornerRadius 32 · `cornerCurve .continuous` · clipsToBounds) 에 `UIVisualEffectView(effect: UIBlurEffect(style: .systemThinMaterial))` 전면 + `tint` 뷰(card 색 alpha 0.82) + `pill.layer.borderWidth = 1 / UIScreen.main.scale`·`borderColor = line` 을 둔다(UIKit 블러 반경은 조절 불가 — 「블러 18」 근사, RESEARCH Pattern 2). 항목 5개는 `GHTabID.allCases` 순서로 가로 `UIStackView`(`.fillEqually`)에 `GHTradeTabItem: UIControl`: 강조 원 46×46(cornerRadius 23 · top 7 · centerX · primary alpha 0.14 · 활성일 때만 보임) · 아이콘 `UIImageView` 26×26(top 12 · centerX · `contentMode .scaleAspectFit`) · 라벨(아이콘 아래 4 · `UIFont.systemFont(ofSize: 10, weight: .semibold)` · 가운데). 심볼 표(D-15): home `house`/`house.fill` · search `magnifyingglass`/같은 이름 · trading `chart.line.uptrend.xyaxis`/같은 이름 · ai `sparkles`/같은 이름 · me `person.crop.circle`/`person.crop.circle.fill`; 설정 `UIImage.SymbolConfiguration(pointSize: 21, weight: 활성 ? .semibold : .regular)`; `UIImage(systemName:)` 가 nil 이면 `assertionFailure` + `circle` 폴백(Pitfall 20). 색: 활성 = primary(아이콘 tint·라벨), 비활성 = muted. 접근성: 항목 `accessibilityLabel = title` · `accessibilityTraits = [.button] + (활성 ? .selected : [])`. 공개 API: `var onSelect: ((GHTabID) -> Void)?` · `func setActive(_ tab: GHTabID?)`(nil = 5개 전부 비활성, D-14) · `func apply(_ p: GHTradePalette)`(tint·테두리·강조·아이콘 색 갱신) · `let fadeView: GHTradeFadeView` — `GHTradeFadeView: UIView`(`isUserInteractionEnabled=false`, `CAGradientLayer` 색 [bg α0, bg α0.92, bg α0.92] · locations [0, 0.7, 1] · `layoutSubviews` 에서 frame 갱신 · `apply(bg:)`).
    ③ 두 파일을 Xcode 프로젝트에 등록(21-01 방식). 이 태스크는 VC 에 붙이지 않는다(Task 3) — 빌드만 통과시킨다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:ios</automated>
    <fails_when>non-zero exit, or the output lacks "BUILD SUCCEEDED"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "0x3485fa\|3485FA\|3485fa" mobile/ios/App/App/GHTradeTheme.swift` · `grep -in "0x17171c\|17171c" mobile/ios/App/App/GHTradeTheme.swift` 가 각각 1줄 이상
    - `grep -n "cornerRadius = 32" mobile/ios/App/App/GHTradeTabBar.swift` 가 1줄 이상 · `grep -n "\.systemThinMaterial" mobile/ios/App/App/GHTradeTabBar.swift` 가 1줄
    - `grep -n '"house.fill"' mobile/ios/App/App/GHTradeTabBar.swift` · `grep -n '"person.crop.circle.fill"' mobile/ios/App/App/GHTradeTabBar.swift` 가 각각 1줄
    - `grep -n "assertionFailure" mobile/ios/App/App/GHTradeTabBar.swift` 가 1줄 이상
  </acceptance_criteria>
  <done>채택 수치대로 그려진 알약 탭바·페이드 뷰가 팔레트 교체 API 와 함께 컴파일된다.</done>
</task>

<task type="auto">
  <name>Task 3: VC 배선 — 제약(바닥 max(inset−14,14) · 폭 ≤560) · URL KVO + route/overlay · 키보드 · 150ms 페이드 · 탭 → navigate 훅 (D-06a · D-12 · D-13 · D-14)</name>
  <files>mobile/ios/App/App/GHTradeBridgeViewController.swift</files>
  <read_first>
    - mobile/ios/App/App/GHTradeBridgeViewController.swift (21-01 — 메시지 핸들러 구조)
    - /Users/alex/repos/weekly-wine-app/ios/App/App/CookieViewController.swift (L20-30 상태 필드 · L192-215 `filterState` 숨김 애니메이션 · L279-330 · L548-655 `observeURL` + `removeAllAnimations` 경합 방지)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pattern 2 · Pattern 6 · Pitfall 8-2 키보드)
    - webapp/src/lib/native/native-bridge-provider.tsx (21-04 — navigate 검증 규칙)
  </read_first>
  <action>
    ① 상태: `private let tabBar = GHTradeTabBar()` · `private var urlObservation: NSKeyValueObservation?` · `private var hideWork: DispatchWorkItem?` · `private(set) var currentPath = "/"` · `private(set) var isOfflinePage = false` · `var overlayOpen = false` · `private var keyboardVisible = false` · `var currentTheme: GHTradeTheme = .light`(21-11 이 저장값으로 바꾼다). `capacitorDidLoad()` 끝에 `setupTabBar()` · `observeURL()` · `observeKeyboard()` 를 더한다(21-01 메시지 핸들러 등록 뒤).
    ② `setupTabBar()`: `view.addSubview(tabBar.fadeView)` 후 `view.addSubview(tabBar)`(view == webView — weekly-wine 과 같음; weekly-wine 의 webView frame 조작은 가져오지 않는다, D-25 풀블리드). Auto Layout — 페이드: leading/trailing/bottom = view, height 120. 탭바: height 70 · `centerXAnchor == view.centerX` · `widthAnchor ≤ 560`(필수) · `widthAnchor == view.width − 32`(priority 750) · `leading ≥ view.leading + 16` · `trailing ≤ view.trailing − 16` · `bottomAnchor == view.safeAreaLayoutGuide.bottomAnchor + 14`(priority 750) · `bottomAnchor ≤ view.bottomAnchor − 14`(필수) — 인셋 34 기기에서 화면 끝 20, 인셋 0 기기에서 14(D-27a). 폭 기준 숨김 분기를 두지 않는다(D-13). `tabBar.apply(GHTradePalette.of(currentTheme))` · 페이드 `apply(bg:)` · `tabBar.onSelect = { [weak self] in self?.selectTab($0) }`.
    ③ `observeURL()`: `urlObservation = webView?.observe(\.url, options: [.new]) { [weak self] wv, _ in DispatchQueue.main.async { self?.handleURL(wv.url) } }`. `handleURL(_ url: URL?)`: 호스트가 `bridge?.config.serverURL.host` 와 다르면 `isOfflinePage = true`, 같으면 `isOfflinePage = false` · `currentPath = TabRoutes.normalize(url.path)` · `tabBar.setActive(TabRoutes.activeTab(forPath: currentPath))` → `updateTabBarVisibility(animated: true)`. 메시지 핸들러의 `route` 는 `payload.path`(문자열 · `/` 로 시작할 때만)로 같은 처리(SPA 보강 — D-14), `overlay` 는 `overlayOpen = payload.open == true` 후 `updateTabBarVisibility(animated: true)`.
    ④ `observeKeyboard()`: `UIResponder.keyboardWillShowNotification` → `keyboardVisible = true` · `keyboardWillHideNotification` → `false`, 둘 다 `updateTabBarVisibility(animated: true)`(Claude's Discretion — 키보드 중 숨김).
    ⑤ `updateTabBarVisibility(animated:)`: `shouldHide = TabRoutes.hidesTabBar(path: currentPath) || isOfflinePage || overlayOpen || keyboardVisible`. 숨김: 기존 `hideWork` 취소 → 새 `DispatchWorkItem` 을 150ms 뒤 실행(D-12) — 실행 시 여전히 숨겨야 할 때만 `UIView.animate(withDuration: 0.2)` 로 탭바·페이드 `alpha = 0` · 탭바 `transform = translationX 0, y 8` → 완료 시 `isHidden = true`. 보임: `hideWork` 취소 → `layer.removeAllAnimations()`(경합 방지 — weekly-wine L600-630) → `isHidden = false` → 0.2s 로 `alpha = 1` · `transform = .identity`. `animated == false` 면 즉시 적용.
    ⑥ `selectTab(_ tab: GHTabID)`: 즉시 `tabBar.setActive(tab)`(반응성) → `isOfflinePage` 면 `webView?.load(URLRequest(url: serverURL 에 tab.path 를 붙인 URL))`(루트는 serverURL 그대로) · 아니면 `webView?.evaluateJavaScript("window.__ghTrade&&window.__ghTrade.navigate?window.__ghTrade.navigate('\(tab.path)'):location.assign('\(tab.path)')")` — 경로는 `GHTabID.path` 상수뿐(사용자 입력 없음 → 스크립트 주입 불가).
    ⑦ 빌드 후 스모크(21-01)를 다시 돌려 회귀가 없음을 본다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:ios</automated>
    <fails_when>non-zero exit, or the output lacks "BUILD SUCCEEDED"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:smoke:ios</automated>
    <fails_when>non-zero exit, or the output lacks "SMOKE OK ready platform=ios"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "safeAreaLayoutGuide.bottomAnchor, constant: 14" mobile/ios/App/App/GHTradeBridgeViewController.swift` 가 1줄 · `grep -n "lessThanOrEqualTo: view.bottomAnchor, constant: -14" mobile/ios/App/App/GHTradeBridgeViewController.swift` 가 1줄
    - `grep -n "lessThanOrEqualToConstant: 560" mobile/ios/App/App/GHTradeBridgeViewController.swift` 가 1줄
    - `grep -n "observe(\\\\.url" mobile/ios/App/App/GHTradeBridgeViewController.swift` 가 1줄 · `grep -n "keyboardWillShowNotification" mobile/ios/App/App/GHTradeBridgeViewController.swift` 가 1줄
    - `grep -n "0.15\|milliseconds(150)" mobile/ios/App/App/GHTradeBridgeViewController.swift` 가 1줄 이상 · `grep -n "withDuration: 0.2" mobile/ios/App/App/GHTradeBridgeViewController.swift` 가 1줄 이상
    - `grep -n "__ghTrade.navigate" mobile/ios/App/App/GHTradeBridgeViewController.swift` 가 1줄
    - `grep -cE "wv\.frame|webView\?\.frame =" mobile/ios/App/App/GHTradeBridgeViewController.swift` 가 0 (풀블리드 — WebView 프레임 조작 없음)
  </acceptance_criteria>
  <done>iOS 앱에 채택 수치의 탭바가 서서 경로에 따라 활성·숨김이 바뀌고, 탭 탭은 웹 클라 내비로 이동한다(시각 확인은 21-16 UAT).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 웹 `route`/`overlay` 메시지 → 탭바 상태 | 웹(또는 XSS)이 탭바를 숨기거나 활성 표시를 바꿀 수 있다 — 표시만, 이동·권한 없음 |
| 네이티브 → 웹 `evaluateJavaScript` | 네이티브가 문자열 스크립트를 실행한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-36 | Tampering | `evaluateJavaScript` 문자열 조립 | medium | mitigate | 경로는 `GHTabID.path` 상수뿐 — 외부 입력을 스크립트에 넣지 않는다(Task 3 ⑥) |
| T-21-02 | Tampering | `route` 페이로드 | low | mitigate | 문자열·`/` 시작만 수용 → `TabRoutes.normalize` · 표시 상태만 바뀜 |
| T-21-18 | Denial of Service | 오버레이 신호 고착으로 탭바 영구 숨김 | low | mitigate | 웹 참조계수(21-04) · URL 변경 시 재평가 · 오프라인/로그인 규칙과 독립 |
| T-21-SC | Tampering | 패키지 설치 | low | accept | 설치 없음(SF Symbols·UIKit 시스템 프레임워크) |
</threat_model>

<verification>
- `native:check-tab-routes:ios` TAB ROUTES OK · `native:build:ios` BUILD SUCCEEDED · `native:smoke:ios` SMOKE OK
- 시각(D-27a 수치·다크/라이트·iPad 560·페이드)·활성/숨김 시나리오는 21-16 UAT 로 확인
</verification>

<success_criteria>
- D-02·D-06·D-06a·D-12·D-13·D-14·D-15·D-27a 가 iOS 에서 구현됐다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/` 패키지 `@gh-radar/mobile` — scripts `native:sync` · `native:sync:dev` · `native:build:ios` · `native:build:android` · `native:smoke:ios` · `native:open:ios` · `native:open:android` · `native:assets` (21-01) · `native:smoke:android` (21-02) · `native:check-tab-routes:ios` (21-10) · `native:test:android` (21-12) · `native:verify-prod` (21-16) | 패키지·스크립트 | 21-01 · 21-02 · 21-10 · 21-12 · 21-16 |
| `mobile/capacitor.config.ts` — appId `com.ghtrade.app` · appName `GH Trade` · `CAP_SERVER_URL` · `SystemBars` (21-01) · `SocialLogin` (21-15) | 설정 | 21-01 · 21-15 |
| `mobile/tsconfig.json` · `mobile/.gitignore` | 설정 | 21-01 |
| `mobile/www/index.html` — 오프라인 폴백(최소 21-01 · `safeTarget`·5초 탐침·`online`·테마 21-11) | 정적 페이지 | 21-01 · 21-11 |
| `mobile/scripts/smoke-ios.sh` · `mobile/scripts/smoke-android.sh` — 「SMOKE OK ready platform=…」 | 스모크 | 21-01 · 21-02 |
| `mobile/scripts/check-tab-routes-ios.sh` · `mobile/scripts/tab-routes-check.swift` | Swift 경로표 검사 | 21-10 |
| `mobile/scripts/render-resources.mjs` · `mobile/resources/{icon.svg,icon-only.png,icon-foreground.png,icon-background.png,splash.png,splash-dark.png}` | 에셋 원본 | 21-14 |
| `mobile/scripts/verify-prod-config.mjs` · `mobile/README.md` | 운영 설정 검사·문서 | 21-16 |
| iOS `SceneDelegate.swift` (루트 = `GHTradeBridgeViewController()` · 창 배경 21-11) | Swift | 21-01 · 21-11 |
| iOS `GHTradeBridgeViewController` · `WeakScriptMessageHandler` · `NativeMessageType` | Swift | 21-01 · 21-10 · 21-11 |
| iOS `GHTradeTabBar` · `GHTradeTab` · `GHTabID` · `GHTradePalette` · `GHTradeTheme` · `TabRoutes.activeTab(forPath:)` · `TabRoutes.hidesTabBar(path:)` | Swift | 21-10 |
| iOS `NavigationDelegateProxy` · `ThemeStore` (UserDefaults `gh-trade.theme`) | Swift | 21-11 |
| Android `MainActivity` (`BridgeActivity`) · `GhTradeWebViewClient` (`BridgeWebViewClient` 상속) · `GhTradeBridge` (`@JavascriptInterface postMessage`) | Kotlin | 21-02 · 21-12 · 21-13 |
| Android `GhTradeTabBar` · `GhTradePalette` · `TabId` · `TabRoutes.activeTabFor` · `TabRoutes.hidesTabBar` · `TabRoutesTest` | Kotlin | 21-12 |
| Android `ThemeStore` (SharedPreferences `gh_trade`/`theme`) · `res/values/bools.xml` · `res/values-sw600dp/bools.xml` (`is_tablet`) | Kotlin·리소스 | 21-13 |
| Android `res/drawable/ic_tab_{home,home_fill,search,search_fill,trading,trading_fill,ai,ai_fill,me,me_fill}.xml` | 벡터 | 21-12 |
| 웹 `lib/native/native-detect.ts` — `NATIVE_DETECT_SCRIPT` · `isNativeApp` · `nativePlatform` | 유틸 | 21-01 |
| 웹 `lib/native/post-native.ts` — `postNative` · `NativeMsgType` | 유틸 | 21-04 |
| 웹 `lib/native/native-bridge-provider.tsx` — `NativeBridgeProvider` · `useNativeBridge` · `NativeRefreshFn` · `window.__ghTrade = { refresh, navigate, back }` | Provider | 21-04 |
| 웹 `lib/native/use-native-refresh.ts` — `useNativeRefresh` | 훅 | 21-04 |
| 웹 `lib/native/native-overlay-marker.tsx` — `NativeOverlayMarker` | 컴포넌트 | 21-04 |
| 웹 `lib/native/google-client-ids.ts` — `GOOGLE_WEB_CLIENT_ID` · `GOOGLE_IOS_CLIENT_ID` · `GOOGLE_ANDROID_CLIENT_ID` · `GOOGLE_IOS_URL_SCHEME` | 공개 식별자 상수 | 21-03 |
| 웹 `lib/native/native-google-login.ts` — `nativeGoogleSignIn` · `randomNonce` · `sha256Hex` · `classifyNativeLoginError` | 서비스 | 21-15 |
| 웹 `RelayConnectionState.probeNow` · `EMPTY_RELAY_VALUE.probeNow` | 훅 계약 | 21-07 |
| 웹 `lib/recent-search.ts` — `RECENT_SEARCH_KEY` · `RECENT_SEARCH_MAX` · `RecentSearchItem` · `readRecentSearches` · `pushRecentSearch` · `removeRecentSearch` · `clearRecentSearches` | 유틸 | 21-08 |
| 웹 `app/search/page.tsx` · `components/search/search-page-client.tsx` — `SearchPageClient` · 사이드바 `NAV_SEARCH_PAGE` | 페이지·상수 | 21-08 |
| 웹 `components/me/account-card.tsx` — `AccountCard` | 컴포넌트 | 21-09 |
| 웹 마크업 계약 `data-slot="app-aside"` · `data-slot="app-menu-button"` · `data-slot="stock-ai-button"` · `data-slot="shared-panels"` · `data-pinned` · `data-order-cta` · `data-slot="account-card"` | 마크업 | 21-05 · 21-06 · 21-09 |
| CSS `@custom-variant native` (21-04) · 앱 셸 규칙 (21-05) · `--app-safe-top` · `--app-safe-bottom` · `--app-safe-left` · `--app-safe-right` · `--native-tabbar-gap` · `--native-tabbar-offset` · `--native-body-reserve` (21-06) | 토큰·변형 | 21-04 · 21-05 · 21-06 |
| 웹 `app/icon.svg` (= 스케치 004 `#app-a`) | 파비콘 | 21-14 |
| 단위 테스트: `native-detect.test.ts` (21-01) · `post-native.test.ts` · `native-bridge-provider.test.tsx` · `native-overlay.test.tsx` (21-04) · `google-client-ids.test.ts` (21-03) · `relay-socket.test.ts` probeNow · `stock-native-refresh.test.tsx` (21-07) · `recent-search.test.ts` · `search-page-client.test.tsx` · `app-sidebar.test.tsx` 보강 (21-08) · `account-card.test.tsx` · `app-shell-chrome.test.tsx` 보강 (21-09) · `native-google-login.test.ts` · `login/__tests__/page.test.tsx` (21-15) · `TabRoutesTest.kt` (21-12) | 단위 | 각 플랜 |
| e2e: `fixtures/native-app.ts` — `installNativeApp` · `nativeMessages` (21-05) · `specs/native-shell.spec.ts` (21-05 · 21-06) · `specs/search-page.spec.ts` (21-08) · `specs/brand-account.spec.ts` (21-09) | Playwright | 각 플랜 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-10-SUMMARY.md` when done
</output>
