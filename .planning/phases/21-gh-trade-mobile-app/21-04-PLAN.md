---
phase: 21-gh-trade-mobile-app
plan: 04
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - webapp/src/lib/native/post-native.ts
  - webapp/src/lib/native/native-bridge-provider.tsx
  - webapp/src/lib/native/use-native-refresh.ts
  - webapp/src/lib/native/native-overlay-marker.tsx
  - webapp/src/lib/native/__tests__/post-native.test.ts
  - webapp/src/lib/native/__tests__/native-bridge-provider.test.tsx
  - webapp/src/lib/native/__tests__/native-overlay.test.tsx
  - webapp/src/app/layout.tsx
  - webapp/src/styles/globals.css
  - webapp/src/components/ui/sheet.tsx
  - webapp/src/components/ui/dialog.tsx
  - webapp/src/components/trading/lc/number-pad-sheet.tsx
autonomous: true
requirements: [MOBILE-01]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-12: 웹은 `postNative(type, payload)` 하나로만 네이티브에 보낸다 — iOS `webkit.messageHandlers.ghTrade` 가 있으면 그쪽, 없으면 Android `window.GhTradeBridge`, 둘 다 없으면 `false`(보내지 않음) · 페이로드는 `{type,payload}` JSON 문자열"
    - "D-04 · D-06a: 앱(`html.native-app`)에서만 `window.__ghTrade = { refresh, navigate, back }` 이 설치되고 Provider 언마운트 시 지워진다 — 브라우저에서는 `window.__ghTrade` 가 없고 네이티브 송신 0"
    - "D-04: `refresh()` 는 등록된 훅을 **전부** 부르고(Promise.allSettled — 한 훅 실패가 다른 훅을 막지 않고 `[gh-radar]` 접두 console.error 로 드러난다), 등록된 훅이 없으면 `location.reload()` 한다 · 언마운트된 페이지의 훅은 불리지 않는다"
    - "D-06 · D-06a: `navigate(path)` 는 `/` 로 시작하고 `//` 로 시작하지 않는 경로만 `router.push` 하고, 현재 경로와 같으면 스크롤 최상단으로 올린다 — 그 외 입력은 `false`"
    - "D-12 · D-26: Sheet·Dialog(주문 확인·⌘K 포함)·NumberPadSheet Content 안의 `NativeOverlayMarker` 가 열림 참조계수를 올리고, 계수 0→1 에서 `overlay {open:true}`, 1→0 에서 `overlay {open:false}` 를 한 번씩만 보낸다 · `back()` 은 오버레이가 열려 있으면 가장 위 레이어만 닫고(합성 Escape) `true`, 아니면 `false`"
    - "D-14 · D-23: 경로가 바뀔 때마다 `route {path}` 를, 마운트 후와 `resolvedTheme` 변경 때 `theme {theme:'dark'|'light'}` 를 한 번씩 보낸다(하이드레이션 전 값 금지)"
    - "Claude's Discretion(내부 스크롤 당김): Android 앱에서 터치 시작 지점의 가장 가까운 내부 스크롤 조상이 위로 스크롤돼 있으면 `pull {blocked:true}`, 아니면 `{blocked:false}` 를 값이 바뀔 때만 보낸다 · 앱에서는 내부 스크롤 컨테이너가 문서로 overscroll 을 넘기지 않는다"
  artifacts:
    - path: webapp/src/lib/native/post-native.ts
      provides: "postNative · NativeMsgType"
      exports: ["postNative", "NativeMsgType"]
    - path: webapp/src/lib/native/native-bridge-provider.tsx
      provides: "NativeBridgeProvider · useNativeBridge · window.__ghTrade"
      exports: ["NativeBridgeProvider", "useNativeBridge", "NativeRefreshFn"]
    - path: webapp/src/lib/native/use-native-refresh.ts
      provides: "useNativeRefresh(fn) — 마운트 등록 · 언마운트 해제"
      exports: ["useNativeRefresh"]
    - path: webapp/src/lib/native/native-overlay-marker.tsx
      provides: "NativeOverlayMarker — Content 마운트 = 오버레이 열림"
      exports: ["NativeOverlayMarker"]
  key_links:
    - from: webapp/src/app/layout.tsx
      to: webapp/src/lib/native/native-bridge-provider.tsx
      via: "<ThemeProvider><NativeBridgeProvider>…</NativeBridgeProvider></ThemeProvider>"
      pattern: "<NativeBridgeProvider>"
    - from: webapp/src/components/ui/sheet.tsx
      to: webapp/src/lib/native/native-overlay-marker.tsx
      via: "SheetPrimitive.Content 첫 자식 <NativeOverlayMarker />"
      pattern: "<NativeOverlayMarker />"
    - from: webapp/src/lib/native/native-bridge-provider.tsx
      to: webapp/src/lib/native/post-native.ts
      via: "postNative('route'|'theme'|'overlay'|'pull', …)"
      pattern: "postNative\\("
---

<assumption_delta_decision>
- 검출기 `assumption-delta scan 21` → `detected: false`. 수동 판정: 두 번째 클라이언트 플랫폼 — 정본 명사 **웹앱**, 결정 **add-alongside**. 이 플랜의 Provider 가 그 「곁에 붙는」 접점 전부를 한 곳에 모은다(21-01 블록 참조).
</assumption_delta_decision>

<objective>
웹 쪽 앱 분기의 단일 접점 `NativeBridgeProvider` 를 만든다: 채널 래퍼 `postNative` · `window.__ghTrade`(refresh·navigate·back) · refresh 레지스트리 · 오버레이 참조계수 · route/theme/pull 메시지. 오버레이 원천(Sheet·Dialog·NumberPadSheet)에 마커를 심는다.

Purpose: 네이티브 탭바(21-10·21-12)·당겨서 새로고침(21-11·21-13)·뒤로가기(21-13)·페이지별 새로고침 연결(21-07·21-08)이 모두 이 계약 하나를 소비한다. 앱이 아니면 전부 no-op 이라 브라우저 사용자에게 동작 변화가 없다.
Output: `lib/native/` 4파일 + 테스트 3파일 + layout 마운트 + 마커 3곳 + CSS 변형.

레지스트리 의미(Claude's Discretion 기록): RESEARCH 는 「스택 · 마지막 등록 우선」을 제안했지만 **등록된 훅 전부 호출**로 정한다 — 종목상세는 시세(StockDetailClient)·뉴스·토론 섹션이 각자 자기 GET 재조회를 등록해야 D-18(외부 호출 경로 금지)을 섹션 경계를 넘지 않고 지킬 수 있다. App Router 에서 동시에 마운트되는 페이지는 하나라 「중첩 페이지」 충돌이 없다.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md
@.planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md
@webapp/src/app/layout.tsx
@webapp/src/lib/native/native-detect.ts
@webapp/src/lib/auth-context.tsx
@webapp/src/components/ui/sheet.tsx
@webapp/src/components/ui/dialog.tsx

<interfaces>
기존 계약(코드에서 추출):

```tsx
// webapp/src/lib/native/native-detect.ts (21-01)
export const NATIVE_DETECT_SCRIPT: string; export function isNativeApp(): boolean; export function nativePlatform(): 'ios'|'android'|null;

// webapp/src/lib/auth-context.tsx — Provider 밖 no-op 폴백 관례
const EMPTY: AuthState = { user: null, displayName: null, isLoading: false, signOut: NOOP_SIGN_OUT };
const AuthContext = createContext<AuthState | null>(null);
export function useAuth(): AuthState { return useContext(AuthContext) ?? EMPTY; }  // (모양)

// webapp/src/components/layout/theme-toggle.tsx — mounted 가드
const { resolvedTheme, setTheme } = useTheme(); const [mounted, setMounted] = useState(false); useEffect(() => setMounted(true), []);

// webapp/src/components/ui/sheet.tsx L55-80: <SheetPortal><SheetOverlay /><SheetPrimitive.Content data-slot="sheet-content" …>{children}…
// webapp/src/components/ui/dialog.tsx L58-69: <DialogPortal><DialogOverlay /><DialogPrimitive.Content data-slot="dialog-content" …>{children}…
// webapp/src/components/trading/lc/number-pad-sheet.tsx L210-241: <Dialog.Portal> … <Dialog.Content …>  (radix-ui Dialog 직접 조립 · body 포털)
// globals.css L5: @custom-variant dark (&:where(.dark, .dark *));
// Radix DismissableLayer 는 document 에 capture keydown 리스너를 달고 Escape 에서 가장 위 레이어만 닫는다(@radix-ui/react-use-escape-keydown)
```

이 플랜이 새로 만드는 계약:

```ts
// post-native.ts
export type NativeMsgType = 'ready' | 'route' | 'theme' | 'overlay' | 'pull';
export function postNative(type: NativeMsgType, payload?: unknown): boolean;
// 페이로드: route {path: string} · theme {theme: 'dark'|'light'} · overlay {open: boolean} · pull {blocked: boolean} · ready (21-01)

// native-bridge-provider.tsx ('use client')
export type NativeRefreshFn = () => unknown | Promise<unknown>;
export interface NativeBridgeValue {
  isNative: boolean;
  registerRefresh: (fn: NativeRefreshFn) => () => void;   // 반환 = 해제
  acquireOverlay: () => () => void;                        // 반환 = 해제(중복 해제 무시)
}
export function NativeBridgeProvider({ children }: { children: ReactNode }): JSX.Element;
export function useNativeBridge(): NativeBridgeValue;     // Provider 밖 = no-op 값
// window.__ghTrade (앱에서만): { refresh(): Promise<void>; navigate(path: string): boolean; back(): boolean }

// use-native-refresh.ts
export function useNativeRefresh(fn: NativeRefreshFn | null | undefined): void;

// native-overlay-marker.tsx
export function NativeOverlayMarker(): null;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `postNative` · `NativeBridgeProvider`(window.__ghTrade · refresh 레지스트리 · route/theme/pull) · `useNativeRefresh` + layout 마운트</name>
  <files>webapp/src/lib/native/post-native.ts, webapp/src/lib/native/native-bridge-provider.tsx, webapp/src/lib/native/use-native-refresh.ts, webapp/src/lib/native/__tests__/post-native.test.ts, webapp/src/lib/native/__tests__/native-bridge-provider.test.tsx, webapp/src/app/layout.tsx</files>
  <read_first>
    - webapp/src/lib/native/native-detect.ts (21-01 — 채널 우선순위와 메시지 형식)
    - webapp/src/lib/auth-context.tsx (L13-32 EMPTY 폴백 · Provider 모양)
    - webapp/src/lib/relay-provider.tsx (L360-377 EMPTY no-op · L450-468 `useMemo` value + React 19 `<Context value>`)
    - webapp/src/components/layout/theme-toggle.tsx (mounted 가드)
    - webapp/src/app/layout.tsx (Provider 중첩 — ThemeProvider 안쪽에 둔다)
    - webapp/src/lib/__tests__/relay-provider.test.tsx (L1-40 vi.mock 패턴 · 파일 머리 「깨지면 사용자가 겪는 일」 목록)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pattern 4 · Pattern 6 · Pitfall 7 · Architectural Responsibility Map)
  </read_first>
  <behavior>
    - post-native: 두 채널 다 있음 → iOS 만 1회 · Android 만 있음 → Android 1회 · 둘 다 없음 → `false`, 호출 0 · 인자는 `JSON.stringify({type, payload})` 문자열 · `window` 없는 환경(SSR 가드)에서 `false`
    - Provider(브라우저 = `native-app` 클래스 없음): 마운트 후 `window.__ghTrade === undefined` · 경로·테마가 바뀌어도 postMessage 0
    - Provider(앱 = 클래스 + `webkit.messageHandlers.ghTrade.postMessage` 스파이): `window.__ghTrade.refresh/navigate/back` 이 함수 · 언마운트 후 `undefined`
    - refresh: 훅 A·B 등록 → `refresh()` 1회에 A·B 각 1회 · A 가 reject 해도 B 호출 + `console.error` 에 `[gh-radar]` 포함 · B 를 가진 컴포넌트 언마운트 후 `refresh()` → A 만 · 등록 0 → `window.location.reload` 1회(`Object.defineProperty` 로 스텁)
    - navigate: `'/search'` → `router.push('/search')` + `true` · 현재 경로(`usePathname` = `'/scanner'`)와 같은 `'/scanner'` → push 0, `window.scrollTo` 가 `top: 0` 으로 1회 + `true` · `'//evil.com'`·`'https://x'`·`''`·숫자 → `false`, push 0
    - route: 마운트 직후 `route {path:'/'}` 1회 · pathname 을 `'/me'` 로 바꿔 rerender → `route {path:'/me'}` 1회 추가 · 같은 값 rerender → 추가 0
    - theme: mounted 뒤 `theme {theme:'light'}` 1회 · `resolvedTheme` 을 `'dark'` 로 → `theme {theme:'dark'}` 1회 추가
    - pull(Android 앱, `data-native-platform="android"`): `overflow-y:auto` 이고 `scrollTop=40` 인 div 안 요소에서 `touchstart` → `pull {blocked:true}` · 같은 곳 다시 → 추가 0 · 문서 본문(스크롤 조상 없음)에서 `touchstart` → `pull {blocked:false}` · iOS 앱에서는 `pull` 송신 0
  </behavior>
  <action>
    ① `post-native.ts`: `export type NativeMsgType = 'ready' | 'route' | 'theme' | 'overlay' | 'pull'`. 창 타입은 파일 안 지역 타입 `type NativeWindow = Window & { webkit?: { messageHandlers?: { ghTrade?: { postMessage(m: string): void } } }; GhTradeBridge?: { postMessage(m: string): void } }` 로 캐스팅한다(전역 `declare global` 로 `webkit` 을 늘리지 않는다). `postNative(type, payload)` 는 RESEARCH Pattern 4 그대로: SSR 가드 → JSON 문자열 → iOS 채널 우선 → Android → `false`. 송신 중 예외는 잡아 `console.error('[gh-radar] postNative', type, err)` 후 `false`(PC-7 무로그 금지). 파일 머리 주석: D-12 채널 · 21-01 인라인 스크립트와 같은 우선순위.
    ② `native-bridge-provider.tsx`('use client'): `NativeBridgeContext = createContext<NativeBridgeValue | null>(null)` + Provider 밖 `EMPTY_NATIVE_BRIDGE`(`isNative:false`, 등록/획득은 해제 no-op 을 돌려준다). 상태: `const [isNative, setIsNative] = useState(false)` — 마운트 effect 에서 `setIsNative(isNativeApp())`(하이드레이션 일치 — 인라인 스크립트가 이미 클래스를 붙였다). 레지스트리 `handlersRef = useRef(new Set<NativeRefreshFn>())` · 오버레이 `overlayCountRef = useRef(0)`. `registerRefresh(fn)` → Set 추가, 반환 = 삭제. `acquireOverlay()` → 계수 +1, 0→1 이고 앱이면 `postNative('overlay', { open: true })`; 반환 해제 함수는 한 번만 동작(지역 `released` 플래그) → 계수 −1, 1→0 이고 앱이면 `postNative('overlay', { open: false })`. `window.__ghTrade` 설치 effect(의존: `isNative`): 앱이면 `{ refresh, navigate, back }` 를 설치하고 cleanup 에서 `delete window.__ghTrade`. `refresh` = 핸들러 배열이 비면 `window.location.reload()` 후 resolve, 아니면 `Promise.allSettled(handlers.map(h => Promise.resolve().then(h)))` → rejected 마다 `console.error('[gh-radar] native refresh 실패', reason)`. `navigate(path)` = `typeof path === 'string' && path.startsWith('/') && !path.startsWith('//')` 가 아니면 `false`; `path` 의 경로부(쿼리 앞)가 현재 `usePathname()` 과 같으면 `window.scrollTo({ top: 0, behavior: 'smooth' })`, 다르면 `router.push(path)`(Next 가 최상단으로 이동) 후 `true`(D-06 재탭 = 최상단). `back()` = 계수 > 0 이면 `document.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape', bubbles: true, cancelable: true }))` 후 `true`, 아니면 `false`(Radix 가 가장 위 레이어만 닫는다 — RESEARCH Pattern 4). 최신 pathname/router 는 ref 로 읽어 `window.__ghTrade` 를 경로마다 다시 설치하지 않는다. route effect(의존: `isNative`, `pathname`): 앱이면 `postNative('route', { path: pathname })`. theme effect: `const { resolvedTheme } = useTheme()` + mounted 가드, 앱이고 mounted 면 `postNative('theme', { theme: resolvedTheme === 'dark' ? 'dark' : 'light' })`(의존: `isNative`, `mounted`, `resolvedTheme`). pull effect(의존: `isNative`): 앱이고 `nativePlatform() === 'android'` 일 때만 `document.addEventListener('touchstart', onTouch, { capture: true, passive: true })` — 대상에서 `document.body` 까지 올라가며 `getComputedStyle(el).overflowY` 가 `auto|scroll` 이고 `el.scrollHeight > el.clientHeight` 인 첫 조상을 찾아 `blocked = el.scrollTop > 0`(없으면 false), 직전 값과 다를 때만 `postNative('pull', { blocked })`. value 는 `useMemo`, 렌더는 React 19 `<NativeBridgeContext value={value}>`. `useNativeBridge()` = `useContext(...) ?? EMPTY_NATIVE_BRIDGE`. 파일 머리 한국어 주석: 책임 5가지(RESEARCH Pattern 4) · 레지스트리 「전부 호출」 결정 근거(이 플랜 objective) · 앱이 아니면 전부 no-op.
    ③ `use-native-refresh.ts`('use client'): `useNativeRefresh(fn)` — `fnRef` 에 최신 fn 을 담고, 마운트 effect 에서 `registerRefresh(() => fnRef.current?.())` 1회 등록·언마운트 해제(fn identity 가 바뀌어도 재등록하지 않는다). fn 이 null/undefined 면 호출 시 아무것도 안 한다.
    ④ `webapp/src/app/layout.tsx`: `ThemeProvider` 바로 안쪽에서 `AuthProvider` 를 `<NativeBridgeProvider>` 로 감싼다(`useTheme` 필요 · Relay/Chat 보다 바깥). `<head>` 스크립트·metadata·viewport 는 건드리지 않는다.
    ⑤ 테스트를 먼저 쓴다(RED): `post-native.test.ts` · `native-bridge-provider.test.tsx`. `vi.mock('next/navigation', …)` 로 `usePathname`(가변 변수)·`useRouter().push` 스파이, `vi.mock('next-themes', …)` 로 `useTheme` 가변 `resolvedTheme`. 앱 모드 설정/원복 헬퍼로 `document.documentElement` 클래스·`data-native-platform`·`window.webkit`·`window.GhTradeBridge` 를 매 케이스 정리한다. 송신 단언은 **실제 postMessage 인자 배열을 JSON 파싱한 결과**로 한다(relay-provider 테스트의 「실제 송신 프레임 배열」 원칙). 파일 머리에 「깨지면 사용자가 겪는 일」 번호 목록.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/native/__tests__/post-native.test.ts src/lib/native/__tests__/native-bridge-provider.test.tsx src/lib/native/__tests__/native-detect.test.ts</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or "native-bridge-provider.test.tsx" is absent from the output</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function postNative" webapp/src/lib/native/post-native.ts` 가 1줄 · `grep -n "'ready' | 'route' | 'theme' | 'overlay' | 'pull'" webapp/src/lib/native/post-native.ts` 가 1줄
    - `grep -n "__ghTrade" webapp/src/lib/native/native-bridge-provider.tsx` 가 1줄 이상 · `grep -n "allSettled" webapp/src/lib/native/native-bridge-provider.tsx` 가 1줄 이상
    - `grep -n "<NativeBridgeProvider>" webapp/src/app/layout.tsx` 가 1줄
    - `grep -n "export function useNativeRefresh" webapp/src/lib/native/use-native-refresh.ts` 가 1줄
    - 두 `<automated>` 가 통과한다
  </acceptance_criteria>
  <done>앱에서만 켜지는 브리지(채널·훅·레지스트리·route/theme/pull 신호)가 동작하고, 브라우저에서는 전역·송신이 0 임이 테스트로 잠겼다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 오버레이 마커 — Sheet·Dialog·NumberPadSheet Content + `back()` 이 가장 위 레이어만 닫음 · CSS `native` 변형</name>
  <files>webapp/src/lib/native/native-overlay-marker.tsx, webapp/src/lib/native/__tests__/native-overlay.test.tsx, webapp/src/components/ui/sheet.tsx, webapp/src/components/ui/dialog.tsx, webapp/src/components/trading/lc/number-pad-sheet.tsx, webapp/src/styles/globals.css</files>
  <read_first>
    - webapp/src/components/ui/sheet.tsx (L55-107 `SheetContent`)
    - webapp/src/components/ui/dialog.tsx (L50-85 `DialogContent`)
    - webapp/src/components/trading/lc/number-pad-sheet.tsx (L1-40 머리 주석 · L205-245 `Dialog.Portal`/`Dialog.Content`)
    - webapp/src/components/layout/app-shell.tsx (L117-160 — 부모가 `open` 을 직접 바꾸는 드로어: `onOpenChange` 훅킹이 열림을 놓치는 예)
    - webapp/src/styles/globals.css (L1-8 `@custom-variant dark`)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pattern 8 · Pattern 4 `back()` · Pitfall 7)
  </read_first>
  <behavior>
    - 앱 모드 + Provider 안에서 `Sheet`(ui/sheet) 하나를 `open` prop 으로 열기 → `overlay {open:true}` 1회 · 닫기(퇴장 애니메이션 뒤 Content 언마운트) → `overlay {open:false}` 1회
    - Sheet A 열린 채 Dialog B 열기 → 추가 송신 0 · B 닫기 → 0 · A 닫기 → `overlay {open:false}` 1회(0↔1 전이에서만)
    - A·B 가 열린 상태에서 `window.__ghTrade.back()` → `true` 이고 B 의 `onOpenChange(false)` 만 불린다(A 는 열린 채) · 아무것도 안 열림 → `back()` = `false`
    - 브라우저 모드(클래스 없음)에서 Sheet 열고 닫기 → postMessage 0 (마커는 렌더되지만 송신 없음)
    - 부모가 `open` 을 true 로 직접 바꾼 Sheet(`onOpenChange` 가 불리지 않는 경로)도 `overlay {open:true}` 를 보낸다
  </behavior>
  <action>
    ① `native-overlay-marker.tsx`('use client'): `export function NativeOverlayMarker(): null` — `const { acquireOverlay } = useNativeBridge(); useEffect(() => acquireOverlay(), [acquireOverlay]); return null;`. 머리 주석: Content 는 열린 동안(+퇴장 애니메이션)만 마운트되므로 마운트 수명이 곧 열림 수명이다 · `onOpenChange` 훅킹 금지 이유(부모가 `open` 을 직접 바꾸는 AppShell 드로어는 콜백이 안 불린다 — RESEARCH Pattern 8) · 브라우저에서는 Provider 가 송신하지 않는다.
    ② `ui/sheet.tsx` 의 `SheetPrimitive.Content` 첫 자식, `ui/dialog.tsx` 의 `DialogPrimitive.Content` 첫 자식, `number-pad-sheet.tsx` 의 `Dialog.Content` 첫 자식에 `<NativeOverlayMarker />` 를 넣는다(각 1곳 — Popover·Tooltip 은 오버레이 아님). 다른 클래스·구조는 이 태스크에서 건드리지 않는다(safe-area 는 21-06).
    ③ `globals.css`: L5 `@custom-variant dark …` 바로 아래에 `@custom-variant native (&:where(.native-app, .native-app *));` 를 더한다(Tailwind v4 — `native:hidden` 등). 그리고 파일의 레이어 밖 규칙 구역(현재 `main:has([data-page-surface="plain"])` 근처)에 `html.native-app body * { overscroll-behavior-y: contain; }` 를 더하고 주석: 앱에서 내부 스크롤(호가 사다리·카드 목록)이 문서로 스크롤 체이닝돼 네이티브 당겨서 새로고침이 켜지는 것을 막는다(RESEARCH Pitfall 7 · iOS WebKit 체이닝) · `html`/`body` 자신에는 걸지 않아 문서 당김(UIRefreshControl)은 산다.
    ④ `native-overlay.test.tsx` 를 먼저 쓴다(RED): Provider + 실제 `ui/sheet`·`ui/dialog` 렌더, 앱 모드 헬퍼(Task 1 과 같은 모양 — 공용 헬퍼가 있으면 재사용), 퇴장 애니메이션 대기는 `waitFor` 로 Content 사라짐을 확인한 뒤 송신 배열을 본다. `next/navigation`·`next-themes` 는 Task 1 테스트와 같은 방식으로 모킹.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/native/__tests__/native-overlay.test.tsx src/components/trading/lc/__tests__/number-pad-sheet.test.tsx src/components/search/__tests__/global-search.test.tsx</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or "native-overlay.test.tsx" is absent from the output</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run test</automated>
    <fails_when>non-zero exit, or the summary line contains "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "<NativeOverlayMarker />" webapp/src/components/ui/sheet.tsx webapp/src/components/ui/dialog.tsx webapp/src/components/trading/lc/number-pad-sheet.tsx` 가 파일마다 1줄
    - `grep -n "@custom-variant native (&:where(.native-app, .native-app \*));" webapp/src/styles/globals.css` 가 1줄
    - `grep -n "overscroll-behavior-y: contain" webapp/src/styles/globals.css` 가 1줄 이상
    - `grep -c "onOpenChange" webapp/src/lib/native/native-overlay-marker.tsx` 가 주석 밖 코드에서 0 (`grep -v '^\s*\*\|^\s*//' webapp/src/lib/native/native-overlay-marker.tsx | grep -c onOpenChange` 가 0)
    - webapp 전체 단위 스위트가 통과한다(기존 Sheet/Dialog 소비처 회귀 없음)
  </acceptance_criteria>
  <done>모든 오버레이 원천이 열림/닫힘을 한 참조계수로 네이티브에 알리고, 네이티브 뒤로가기가 가장 위 레이어만 닫을 수 있으며, 앱에서 내부 스크롤이 문서 당김으로 번지지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 네이티브 → 웹(`evaluateJavaScript`/`evaluateJavascript` 로 `window.__ghTrade.*` 호출) | 네이티브는 상수 경로만 넘긴다 — 웹은 입력을 다시 검증한다 |
| 웹 → 네이티브(`postNative`) | 페이로드는 신호뿐(경로·테마·불리언) — 개인정보·토큰을 싣지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-16 | Tampering | `window.__ghTrade.navigate(path)` | medium | mitigate | `/` 시작 · `//` 금지 · 문자열만 — 외부 URL·프로토콜 상대 경로로 `router.push` 불가(Task 1 ②) |
| T-21-17 | Tampering | `back()` 합성 Escape 가 주문 확인을 닫음 | low | accept | Escape = 취소 경로(주문 전송 없음) · Radix 가 가장 위 레이어만 닫는다 |
| T-21-18 | Denial of Service | 오버레이 계수 누수로 탭바 영구 숨김 | medium | mitigate | 마운트 수명 기반 계수 · 해제 함수 1회 가드 · 0↔1 전이 테스트(Task 2 behavior) |
| T-21-02 | Information Disclosure | 네이티브 채널로 민감정보 송신 | low | mitigate | 메시지 5종 페이로드가 경로·테마·불리언뿐(post-native 타입으로 고정) |
| T-21-SC | Tampering | 패키지 설치 | low | accept | 이 플랜은 설치 없음 |
</threat_model>

<verification>
- `lib/native` 단위 테스트 4파일 green · webapp 전체 단위 스위트 green · typecheck green
</verification>

<success_criteria>
- D-04·D-06a·D-12·D-14·D-23·D-26 의 웹 측 계약이 한 Provider 로 모였고, 앱이 아닐 때 브라우저 동작 변화가 0 이다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/` 패키지 `@gh-radar/mobile` — scripts `native:sync` · `native:sync:dev` · `native:build:ios` · `native:build:android` · `native:smoke:ios` · `native:open:ios` · `native:open:android` · `native:assets` (21-01) · `native:smoke:android` (21-02) · `native:check-tab-routes:ios` (21-10) · `native:test:android` (21-12) · `native:verify-prod` (21-16) | 패키지·스크립트 | 21-01 · 21-02 · 21-10 · 21-12 · 21-16 |
| `mobile/capacitor.config.ts` — appId `com.ghtrade.app` · appName `GH Trade` · `CAP_SERVER_URL` · `SystemBars` (21-01) · `SocialLogin` (21-15) | 설정 | 21-01 · 21-15 |
| `mobile/tsconfig.json` · `mobile/.gitignore` | 설정 | 21-01 |
| `mobile/www/index.html` — 오프라인 폴백(최소 21-01 · `safeTarget`·5초 탐침·`online`·테마 21-11) | 정적 페이지 | 21-01 · 21-11 |
| `mobile/scripts/smoke-ios.sh` · `mobile/scripts/smoke-android.sh` — 「SMOKE OK ready platform=…」 | 스모크 | 21-01 · 21-02 |
| `mobile/scripts/check-tab-routes-ios.sh` · `mobile/scripts/tab-routes-check.swift` | Swift 경로표 검사 | 21-10 |
| `mobile/scripts/render-resources.mjs` · `mobile/resources/{icon.svg,icon-only.png,icon-foreground.png,icon-background.png,splash.png,splash-dark.png}` | 에셋 원본 | 21-14 |
| `mobile/scripts/verify-prod-config.mjs` · `mobile/README.md` | 운영 설정 검사·문서 | 21-16 |
| iOS `SceneDelegate.swift` (루트 = `GHTradeBridgeViewController()` · 창 배경 21-11) | Swift | 21-01 · 21-11 |
| iOS `GHTradeBridgeViewController` · `WeakScriptMessageHandler` · `NativeMessageType` | Swift | 21-01 · 21-10 · 21-11 |
| iOS `GHTradeTabBar` · `GHTradeTab` · `GHTabID` · `GHTradePalette` · `GHTradeTheme` · `TabRoutes.activeTab(forPath:)` · `TabRoutes.hidesTabBar(path:)` | Swift | 21-10 |
| iOS `NavigationDelegateProxy` · `ThemeStore` (UserDefaults `gh-trade.theme`) | Swift | 21-11 |
| Android `MainActivity` (`BridgeActivity`) · `GhTradeWebViewClient` (`BridgeWebViewClient` 상속) · `GhTradeBridge` (`@JavascriptInterface postMessage`) | Kotlin | 21-02 · 21-12 · 21-13 |
| Android `GhTradeTabBar` · `GhTradePalette` · `TabId` · `TabRoutes.activeTabFor` · `TabRoutes.hidesTabBar` · `TabRoutesTest` | Kotlin | 21-12 |
| Android `ThemeStore` (SharedPreferences `gh_trade`/`theme`) · `res/values/bools.xml` · `res/values-sw600dp/bools.xml` (`is_tablet`) | Kotlin·리소스 | 21-13 |
| Android `res/drawable/ic_tab_{home,home_fill,search,search_fill,trading,trading_fill,ai,ai_fill,me,me_fill}.xml` | 벡터 | 21-12 |
| 웹 `lib/native/native-detect.ts` — `NATIVE_DETECT_SCRIPT` · `isNativeApp` · `nativePlatform` | 유틸 | 21-01 |
| 웹 `lib/native/post-native.ts` — `postNative` · `NativeMsgType` | 유틸 | 21-04 |
| 웹 `lib/native/native-bridge-provider.tsx` — `NativeBridgeProvider` · `useNativeBridge` · `NativeRefreshFn` · `window.__ghTrade = { refresh, navigate, back }` | Provider | 21-04 |
| 웹 `lib/native/use-native-refresh.ts` — `useNativeRefresh` | 훅 | 21-04 |
| 웹 `lib/native/native-overlay-marker.tsx` — `NativeOverlayMarker` | 컴포넌트 | 21-04 |
| 웹 `lib/native/google-client-ids.ts` — `GOOGLE_WEB_CLIENT_ID` · `GOOGLE_IOS_CLIENT_ID` · `GOOGLE_ANDROID_CLIENT_ID` · `GOOGLE_IOS_URL_SCHEME` | 공개 식별자 상수 | 21-03 |
| 웹 `lib/native/native-google-login.ts` — `nativeGoogleSignIn` · `randomNonce` · `sha256Hex` · `classifyNativeLoginError` | 서비스 | 21-15 |
| 웹 `RelayConnectionState.probeNow` · `EMPTY_RELAY_VALUE.probeNow` | 훅 계약 | 21-07 |
| 웹 `lib/recent-search.ts` — `RECENT_SEARCH_KEY` · `RECENT_SEARCH_MAX` · `RecentSearchItem` · `readRecentSearches` · `pushRecentSearch` · `removeRecentSearch` · `clearRecentSearches` | 유틸 | 21-08 |
| 웹 `app/search/page.tsx` · `components/search/search-page-client.tsx` — `SearchPageClient` · 사이드바 `NAV_SEARCH_PAGE` | 페이지·상수 | 21-08 |
| 웹 `components/me/account-card.tsx` — `AccountCard` | 컴포넌트 | 21-09 |
| 웹 마크업 계약 `data-slot="app-aside"` · `data-slot="app-menu-button"` · `data-slot="stock-ai-button"` · `data-slot="shared-panels"` · `data-pinned` · `data-order-cta` · `data-slot="account-card"` | 마크업 | 21-05 · 21-06 · 21-09 |
| CSS `@custom-variant native` (21-04) · 앱 셸 규칙 (21-05) · `--app-safe-top` · `--app-safe-bottom` · `--app-safe-left` · `--app-safe-right` · `--native-tabbar-gap` · `--native-tabbar-offset` · `--native-body-reserve` (21-06) | 토큰·변형 | 21-04 · 21-05 · 21-06 |
| 웹 `app/icon.svg` (= 스케치 004 `#app-a`) | 파비콘 | 21-14 |
| 단위 테스트: `native-detect.test.ts` (21-01) · `post-native.test.ts` · `native-bridge-provider.test.tsx` · `native-overlay.test.tsx` (21-04) · `google-client-ids.test.ts` (21-03) · `relay-socket.test.ts` probeNow · `stock-native-refresh.test.tsx` (21-07) · `recent-search.test.ts` · `search-page-client.test.tsx` · `app-sidebar.test.tsx` 보강 (21-08) · `account-card.test.tsx` · `app-shell-chrome.test.tsx` 보강 (21-09) · `native-google-login.test.ts` · `login/__tests__/page.test.tsx` (21-15) · `TabRoutesTest.kt` (21-12) | 단위 | 각 플랜 |
| e2e: `fixtures/native-app.ts` — `installNativeApp` · `nativeMessages` (21-05) · `specs/native-shell.spec.ts` (21-05 · 21-06) · `specs/search-page.spec.ts` (21-08) · `specs/brand-account.spec.ts` (21-09) | Playwright | 각 플랜 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-04-SUMMARY.md` when done
</output>
