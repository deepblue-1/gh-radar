---
phase: 21-gh-trade-mobile-app
plan: 25
type: execute
wave: 12
depends_on: ["21-24"]
files_modified:
  - .planning/sketches/008-uat3-surfaces/index.html
  - .planning/sketches/008-uat3-surfaces/README.md
  - .planning/sketches/MANIFEST.md
  - .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
autonomous: false
gap_closure: true
requirements: [MOBILE-01]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G-21-R3-2 · G-21-R3-9 · G-21-R3-10: 구현 전에 standalone HTML 목업(sketch 008 — globals.css 실제 토큰 인라인 · 변형 A/B/C · 다크/라이트)을 사용자가 열어 보고 세 표면(/me 전 종목 전략 로그 배치 · 종목상세 넓은 폭 「트레이딩」 버튼 배치 · 트레이딩 카드 수동주문 주문유형 배치)의 채택안을 답했다"
    - "같은 체크포인트에서 재확인 세 건을 받았다 — G-21-R3-2 「잃는 동작 ②③ 은 앱에서 범위 밖」 · G-21-R3-8 「옛 전체 페이지 URL 은 리다이렉트로 유지」 · G-21-CR WR-03 「androidx.webkit 1.14.0 을 앱 모듈에 선언해 WebMessageListener 로 고칠지」"
    - "채택안과 UAT 3차 결정이 21-CONTEXT.md 에 D-12a · D-12b · D-25a · D-29 · D-30 · D-31 · D-32 로 기록되고, D-32 는 G-21-R3-11 truth 축소(탭 루트별 스크롤 복원 + 스켈레톤 없이 이전 내용 → 조용한 갱신 · 재마운트와 입력·펼침 초기화 허용 · keep-alive 기각)를 적는다 — 이전 결정 원문(D-06 · D-12 · D-25)은 대체 포인터만 붙은 채 남는다"
    - "사용자 답 전에는 스케치·CONTEXT 커밋이 없고, 이 플랜은 웹·네이티브 소스를 한 줄도 바꾸지 않는다"
  artifacts:
    - path: .planning/sketches/008-uat3-surfaces/index.html
      provides: "세 표면 × 변형 A/B/C × 다크/라이트 목업 (globals.css 토큰 인라인)"
      contains: "--bg"
    - path: .planning/sketches/008-uat3-surfaces/README.md
      provides: "디자인 질문 · 변형 설명 · Winner(사용자 답)"
      contains: "Winner"
    - path: .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
      provides: "UAT 3차 결정 D-12a · D-12b · D-25a · D-29 · D-30 · D-31 · D-32"
      contains: "D-32"
  key_links:
    - from: .planning/sketches/008-uat3-surfaces/README.md
      to: .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
      via: "Winner ①②③ → D-25a(/me 로그 배치) · D-30(넓은 폭 트레이딩 버튼) · D-31(카드 주문유형 배치)"
      pattern: "스케치 008"
    - from: .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
      to: .planning/phases/21-gh-trade-mobile-app/21-UAT.md
      via: "각 결정 끝의 근거 = G-21-R3-* gap id · D-32 는 UAT R3-11 decision 줄(2026-09-26 수정)과 같은 문구"
      pattern: "G-21-R3-"
  prohibitions:
    - statement: "MUST NOT 사용자가 목업에 답하기 전에 R3-2 /me 전략 로그 · R3-9 넓은 폭 「트레이딩」 버튼 · R3-10 카드 주문유형 표면을 구현하거나, 스케치·CONTEXT 를 커밋하거나, push 한다(목업을 연 것은 검토가 아니다)"
      status: unverified
      verification: "Task 1 의 webapp/·mobile/ 상태 스냅숏 비교(시작 = 끝) · Task 3 만 커밋 · push 명령 없음"
    - statement: "MUST NOT D-06 · D-12 · D-25 원문을 지우거나 고쳐 쓴다 — 끝에 대체 포인터만 덧붙인다"
      status: unverified
      verification: "Task 3 verify 의 원문 문자열 grep"
    - statement: "MUST NOT 목업에 추측한 색·간격을 쓴다 — 값은 webapp/src/styles/globals.css 의 :root · .dark 에서 그대로 옮긴다"
      status: unverified
      verification: "Task 1 acceptance 의 토큰 문자열 대조"
---

<objective>
UAT 3차 갭 클로징 1단계 — **목업 게이트 · 재확인 · 결정 기록**. 새 화면 요소 세 곳(G-21-R3-2 /me 전 종목 전략 로그 · G-21-R3-9 종목상세 넓은 폭 「트레이딩」 버튼 · G-21-R3-10 트레이딩 카드 수동주문 주문유형)을 standalone HTML 목업(sketch 008)으로 먼저 보여 주고, 같은 체크포인트에서 재확인 세 건(R3-2 잃는 동작 · R3-8 리다이렉트 · WR-03 androidx.webkit 선언)을 받은 뒤, UAT 3차 결정 전부를 CONTEXT 에 박제한다.

Purpose: 프로젝트 관례(UI 는 HTML 목업 먼저 · 검토 게이트 전 구현/커밋 금지)를 지키고, 사람 멈춤을 한 번으로 모은다(WR-03 결정을 따로 멈추지 않는다). 뒤 플랜(21-29 · 21-30 · 21-32 · 21-33 · 21-34)은 여기 답을 정본으로 쓴다.
Output: `.planning/sketches/008-uat3-surfaces/` · MANIFEST 008 행 · CONTEXT 결정 7건. 코드 변경 없음.

목업 게이트 플랜이라 트레이서 태스크가 없다(구현 플랜 21-26 ~ 21-34 가 각각 트레이서로 시작한다). R3-5 · R3-6 · R3-7 은 손보는 표면 안 시각 결함이라 목업 없이 고친다(사용자 결정 · 21-26).

G-21-R3-11 은 같은 날 사용자가 단순안으로 바꿨다(21-UAT.md decision 줄 수정 · 「과설계 금지」): keep-alive 없이 탭 루트별 스크롤 복원 + 기존 query-cache 로 스켈레톤 없는 재방문 — 입력·펼침 초기화 허용. D-32 는 그 축소를 기록한다(21-31 이 구현).

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add(`git add -A` 금지 — 작업 트리에 다른 세션 변경이 있다) · 커밋 직전 `git status -sb` · **사용자 답(Task 2) 전 커밋 금지 · push 금지**(push = Vercel 웹 프로덕션 배포, 마지막 21-36 체크포인트에서만).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-UAT.md
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-REVIEW.md
@.planning/sketches/MANIFEST.md
@.planning/sketches/007-tab-bar-no-label/README.md
@webapp/src/styles/globals.css
@webapp/src/components/trading/me-client.tsx
@webapp/src/components/trading/strategy-log.tsx
@webapp/src/components/stock/stock-hero.tsx
@webapp/src/components/stock/stock-detail-client.tsx
@webapp/src/components/trading/card/manual-order-form.tsx

<interfaces>
```text
/me 세로 순서(me-client.tsx ①): 제목 → AccountCard(mb-1) → MeStatusBar → StrategyStatusCard → 계좌 카드 N → TodayOrdersCard
StrategyLog(strategy-log.tsx): variant 'card' | 'embed' · entries {id, at 'HH:MM:SS', text, level?, who?} · 빈 문구 「아직 기록이 없어요」
종목상세 공통 영역: StockHero 첫 줄(← · 종목명 · 코드 칩 · 시장 배지 · 관심 토글 · 앱 전용 「AI 분석」 ml-auto) → 가격 줄(30/700) → 갱신시각 행(「새로고침」 outline sm, stock-detail-client)
폰 하단 CTA(stock-detail-tabs.tsx): data-slot detail-order-cta-bar · 버튼 56 · radius 16 · 17/600 · bg --up · md:hidden
수동주문 행 순서(manual-order-form.tsx): 1 가격 → (조각) → 4 주문유형(호가 탭 전용 44 행 「지정가 ›」 + 투명 select · 937~970) → 5 주문금액 → 4버튼 · 시간외 판정 affordanceOf(queuedWindow, exchange, orderType).offHoursSelectable
WR-03(21-REVIEW): androidx.webkit 1.14.0 은 :capacitor-android 의 implementation 이라 APK 런타임에는 있고 앱 컴파일 클래스패스에는 없다(플래너 실측) — WebMessageListener 로 고치려면 앱 모듈 선언 1줄(variables.gradle 의 androidxWebkitVersion 공유)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 스케치 008 — 세 표면 × 변형 A/B/C × 다크/라이트 standalone 목업 작성 · open (커밋 금지)</name>
  <files>.planning/sketches/008-uat3-surfaces/index.html, .planning/sketches/008-uat3-surfaces/README.md</files>
  <read_first>
    - webapp/src/styles/globals.css (`:root` · `.dark` 토큰 블록 — 목업에 인라인할 값의 정본)
    - .planning/sketches/007-tab-bar-no-label/index.html (형식 · 다크/라이트 토글 관례)
    - webapp/src/components/trading/me-client.tsx · webapp/src/components/trading/strategy-log.tsx
    - webapp/src/components/stock/stock-hero.tsx · webapp/src/components/stock/stock-detail-client.tsx
    - webapp/src/components/trading/card/manual-order-form.tsx (930~975 주문유형 행 · 44px 행 규칙)
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-R3-2 · R3-9 · R3-10 `missing` · `decision`)
  </read_first>
  <action>
    시작하자마자 `git status --porcelain -- webapp/ mobile/ > "${TMPDIR:-/tmp}/gh-radar-21-25-pre.txt"` 로 상태 스냅숏을 남긴다(작업 트리에 다른 세션의 미커밋 변경이 있을 수 있다 — 건드리지 않고 비교만). 외부 리소스 없는 한 파일 `.planning/sketches/008-uat3-surfaces/index.html` 을 만든다(시스템 폰트 · 이미지·네트워크 URL 0). `<style>` 맨 위에 globals.css `:root` · `.dark` 토큰 중 쓰는 것(`--bg` · `--surface` · `--card` · `--muted` · `--fg` · `--muted-fg` · `--border-subtle` · `--primary` · `--up` · `--down` · radius 토큰)을 값 그대로 옮기고 상단 토글로 `.dark` 를 붙였다 뗀다(기본 다크 — D-23a). 세 섹션:
    ① /me 전 종목 전략 로그 배치(폰 390 한 장 · 로그 예시 5줄, 종목명 포함): A = 전략 현황 카드 바로 아래 독립 카드 「전략 로그」 · B = 전략 현황 카드 안 「현황 | 로그」 전환 · C = 오늘 주문 아래 맨 끝 카드.
    ② 종목상세 넓은 폭(1024) 「트레이딩」 버튼 배치(브라우저판 · 앱판(「AI 분석」 있음)): A = 히어로 첫 줄 끝 · B = 가격 줄 끝 · C = 갱신시각 행 「새로고침」 옆. 폰 하단 바는 라벨만 「트레이딩」 인 참고 한 장.
    ③ 트레이딩 카드 수동주문 주문유형(카드 폭 344): A = 호가 탭과 같은 44px 행 「주문유형 지정가 ›」(주문금액 위) · B = 가격 칸 위 세그먼트 「지정가 | 시간외종가」 · C = 가격 라벨 옆 칩. 시간외 창 열림/닫힘(비활성 + 안내) 두 상태.
    각 변형 제목에 A/B/C 와 한 줄 장단점. README 는 sketch 007 형식(frontmatter sketch/name/question/winner/tags · Design Question · How to View · Variants)으로 쓰고 `winner: "pending"`. `open .planning/sketches/008-uat3-surfaces/index.html` 로 연다. **커밋하지 않는다.**
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && F=.planning/sketches/008-uat3-surfaces/index.html && test -f "$F" && test -f .planning/sketches/008-uat3-surfaces/README.md && N="$(grep -c -E 'https?://' "$F" || true)" && test "$N" -eq 0 && grep -q -- '--bg' "$F" && grep -q -- '--up' "$F" && git status --porcelain -- webapp/ mobile/ > "${TMPDIR:-/tmp}/gh-radar-21-25-post.txt" && cmp -s "${TMPDIR:-/tmp}/gh-radar-21-25-pre.txt" "${TMPDIR:-/tmp}/gh-radar-21-25-post.txt" && echo "SKETCH 008 OK"</automated>
    <fails_when>non-zero exit, or "SKETCH 008 OK" is absent (file missing, a network URL present, a token missing, or webapp/mobile status differs from the snapshot taken at task start)</fails_when>
  </verify>
  <acceptance_criteria>
    - 세 섹션과 변형 A · B · C 가 있다: `grep -c -E '변형 [ABC]|data-variant="[abc]"' .planning/sketches/008-uat3-surfaces/index.html` ≥ 9
    - 다크/라이트 전환: `grep -c '\.dark' .planning/sketches/008-uat3-surfaces/index.html` ≥ 1
    - README frontmatter 에 `winner: "pending"` · `sketch: 008`
    - webapp/·mobile/ 상태가 시작 스냅숏과 같다(verify 의 diff — 다른 세션의 미커밋 변경은 그대로 두고 비교만) · 이 태스크 커밋 없음
  </acceptance_criteria>
  <done>세 표면의 변형이 실제 토큰 색으로 다크/라이트 모두 그려진 목업이 열려 있고, 아무것도 커밋되지 않았다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: 목업 검토 + 재확인 3건(한 번에 질문 하나씩)</name>
  <read_first>
    - .planning/sketches/008-uat3-surfaces/README.md
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-R3-2 missing 4번째 줄 · G-21-R3-8 decision 줄)
    - .planning/phases/21-gh-trade-mobile-app/21-REVIEW.md (WR-03)
  </read_first>
  <action>
    목업이 열려 있는지 확인하고(닫혔으면 다시 연다) 아래 여섯 질문을 **다이얼로그 하나에 질문 하나씩** 순서대로 묻는다(설명·비교는 질문 본문과 선택지 설명에). WR-03 질문 전에 `cd /Users/alex/repos/gh-radar/mobile/android && ./gradlew :app:dependencies --configuration debugRuntimeClasspath -q | grep -n 'androidx.webkit'`(기대 1줄 · capacitor-android 하위)와 같은 명령 `debugCompileClasspath`(기대 0줄)를 돌려 결과 줄을 질문에 붙인다. 답 전에는 아무것도 커밋하지 않는다. 「수정: …」 이면 Task 1 로 돌아가 고치고 다시 연다.
  </action>
  <what-built>sketch 008 목업(`open .planning/sketches/008-uat3-surfaces/index.html`) — ① /me 로그 배치 · ② 넓은 폭 「트레이딩」 버튼 · ③ 카드 주문유형, 모두 A/B/C × 다크/라이트.</what-built>
  <how-to-verify>
    1. ① /me 전략 로그 — A(전략 현황 아래 독립 카드) / B(전략 현황 카드 안 「현황 | 로그」) / C(맨 끝 카드)? (앱에서 /trading 하단 패널이 사라지는 대신 여기서 본다 — G-21-R3-2)
    2. ② 넓은 폭 「트레이딩」 버튼 — A(히어로 첫 줄 끝) / B(가격 줄 끝) / C(갱신시각 행)? (폰은 하단 바 라벨만 바뀐다 — G-21-R3-9)
    3. ③ 카드 주문유형 — A(44px 행) / B(가격 위 세그먼트) / C(가격 라벨 옆 칩)? (호가주문 탭을 없애면 시간외종가 신규 주문은 여기서만 된다 — G-21-R3-10)
    4. 재확인 — 앱에서 /trading 하단 패널을 숨기면 ② 잔고 행 → 그 종목 카드 생성·포커스 ③ 카드 없는 종목의 미체결 정정 진입이 앱에서 없어진다. 범위 밖으로 두는 데 동의? (동의 / 이견: 내용)
    5. 재확인 — 옛 주소 `/stocks/{code}/news` · `/discussions` 는 탭 안 전체목록(`?tab=news&view=…`)으로 리다이렉트해 북마크를 살린다(G-21-R3-8 기본값). 동의? (동의 / 이견: 내용)
    6. 재확인(WR-03) — Android 브리지가 하위 프레임 호출을 막으려면 `androidx.webkit` 1.14.0 을 앱 모듈에 한 줄 선언해야 한다(이미 Capacitor 가 APK 에 싣는 같은 좌표·버전 — APK 의존 집합 불변). a = 선언하고 WebMessageListener(허용 출처 · 메인 프레임만)로 고친다(권장) / b = 선언 없이 최상위 문서 출처 전체 비교만 강화(하위 프레임 판정 불가 — 부분 수정으로 근거 기록).
  </how-to-verify>
  <resume-signal>`①A ②B ③A 잃는동작:동의 리다이렉트:동의 webkit:a` 형식(이견이면 `이견: <내용>`) 또는 `수정: <목업 수정 요청>`</resume-signal>
  <acceptance_criteria>
    - 재개 신호에 ①②③ 각각 A · B · C 중 하나 · 재확인 두 답 · `webkit:a` 또는 `webkit:b` 가 있다(또는 `수정:` 으로 Task 1 반복)
    - 답 원문과 클래스패스 두 명령 결과가 21-25-SUMMARY 「Decisions」 에 그대로 기록된다
  </acceptance_criteria>
</task>

<!-- planner-region-allow: winner: "pending" -->
<task type="auto">
  <name>Task 3: 채택안 박제 — README Winner · MANIFEST 008 행 · CONTEXT UAT 3차 결정 7건(원문 보존 + 대체 포인터) · 커밋</name>
  <files>.planning/sketches/008-uat3-surfaces/README.md, .planning/sketches/MANIFEST.md, .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md (전체 — UAT 2차 소제목 뒤 · D-06(30행) · D-12(42행) · D-25(59행) 끝)
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (R3-1 · R3-2 · R3-4 · R3-8 · R3-9 · R3-10 · R3-11 `decision` 줄 — R3-11 은 2026-09-26 수정본)
    - Task 2 재개 신호
  </read_first>
  <action>
    ① README `winner:` 를 사용자 답(예 `"①A ②B ③A"`)으로, 「## Winner」 에 세 채택안의 위치·수치와 재확인 답을 적는다. MANIFEST 표에 008 행(`uat3-surfaces` · 질문 · Winner · 태그 `mobile, me, stock-detail, trading, gap-closure`).
    ② CONTEXT `<decisions>` 의 UAT 2차 소제목 뒤, 「### Claude's Discretion」 바로 앞에 「### 갭 클로징 결정 (UAT 3차 · 2026-09-26 사용자 확정)」 과 아래 7항목(각 끝에 「근거 = 21-UAT.md G-21-R3-N」):
      - **D-12a (G-21-R3-1 — D-12 의 키보드 숨김 타이밍 대체):** 키보드 사유 숨김 = 150ms 대기 없이 즉시 · 키보드보다 짧은 페이드(iOS = userInfo 애니메이션 시간의 절반 · 같은 곡선, Android = IME 애니메이션 onPrepare 에서 즉시) · 아래로 내려가는 이동 없음 · 재표시 90ms 디바운스 · iOS 는 키보드 끝 프레임이 화면 하단을 가릴 때만 키보드로 본다. 오버레이·경로·오프라인 숨김은 D-12 그대로.
      - **D-12b (G-21-R3-4):** 전체 문서 로드 시작(iOS didStartProvisionalNavigation · Android onPageStarted) = 탭바 대기 → 그 문서의 첫 `route` 메시지(하이드레이션 뒤 · 웹 계약 무변경) 또는 1.5초 뒤 280ms ease-out 표시 · SPA 이동 무관 · iOS 도 숨긴 채 시작.
      - **D-25a (G-21-R3-2 — D-25 의 /trading 하단 공용 패널 항목 대체):** 앱(`html.native-app`)에서 /trading 공용 패널 + spacer 숨김(폰 · iPad · 브라우저 불변) · 카드 더티 바는 `--native-tabbar-offset` 까지 비킨다 · 전 종목 전략 로그는 /me(스케치 008 ① 채택안) · 잃는 동작 답 기록.
      - **D-29 (G-21-R3-8):** 뉴스·토론 전체 보기 = 탭 안 전체목록 — 종목상세 `?tab=news&view=news|discussions`(pushState · 허용 목록) · 트레이딩 ⓘ 팝업은 로컬 상태(URL 불변) · 뒤로가기(Android · 브라우저 · Esc · 화면 안 ←) = 요약 · 옛 URL 처리 = 사용자 답 · 당겨서 새로고침 = GET 재조회만(D-18).
      - **D-30 (G-21-R3-9):** 「주문하기」 → 「트레이딩」 = `/trading?code={code}` · 모든 폭(폰 하단 바 + 넓은 폭 = 스케치 008 ② 채택안) · 매매 불가(비 KOSPI/KOSDAQ · isin 없음) 숨김 · 트레이딩은 그 종목 카드를 보장·펼침·포커스.
      - **D-31 (G-21-R3-10):** 종목상세 호가주문 탭 제거 · `?tab=orderbook` → `/trading?code=`(매매 불가면 차트) · 시간외종가 신규 주문 = 카드 수동주문 주문유형(스케치 008 ③ 채택안 · affordanceOf 그대로) · 공유 모듈 유지 · 21-33 → 21-34 순서로 CTA 가 갈 곳 없는 중간 상태를 만들지 않는다.
      - **D-32 (G-21-R3-11 — D-06 의 탭 이동에 규칙 추가 · 2026-09-26 단순안):** 탭 루트(`/` · `/search` · `/trading` · `/chat` · `/me`) 사이를 오가면 탭 루트마다 **스크롤 위치를 복원**하고, 마지막으로 받은 내용을 **스켈레톤 없이 바로** 보여 준 뒤 조용히 갱신한다(기존 `lib/query-cache` 재사용 · 새 데이터 라이브러리 없음). 화면 재마운트와 입력 · 펼침 상태 초기화는 허용한다. 같은 탭 재탭 = 맨 위(D-06a) 유지. keep-alive(React Activity · 화면 보존)와 탭별 웹뷰는 기각(사용자: 과설계 금지). truth 는 이 범위로 좁힌다(21-UAT.md R3-11 decision 줄). — Reversibility: reversible.
    ③ 원문 보존 + 포인터 한 줄씩: D-06 끝 「→ D-32 탭 루트 스크롤 복원 · 스켈레톤 없는 재방문(재탭 = 맨 위 유지)」 · D-12 끝 「→ D-12a 키보드 숨김 즉시 · D-12b 문서 로드 중 대기」 · D-25 끝 「→ D-25a 앱 /trading 하단 공용 패널 숨김 · 종목상세 CTA 는 D-30 「트레이딩」」.
    ④ WR-03 답(a/b)은 D 항목이 아니라 CONTEXT 끝 「### 갭 클로징 결정 (UAT 3차)」 소제목 마지막 줄에 「WR-03 Android 브리지 = 선택 a|b(21-25 사용자 답)」 로 한 줄 적는다(21-29 가 읽는다).
    ⑤ 커밋(사용자 답 뒤): `git status -sb` → 세 파일 + index.html 만 add → 「docs(21): UAT 3차 목업 008 채택안과 결정 D-12a·D-12b·D-25a·D-29~D-32 기록」. push 하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && C=.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md && for id in D-12a D-12b D-25a D-29 D-30 D-31 D-32; do grep -q "\*\*${id} " "$C" || { echo "MISSING ${id}"; exit 1; }; done && grep -q "갭 클로징 결정 (UAT 3차" "$C" && grep -q "탭 5개 = \*\*홈" "$C" && grep -q "숨김 = ① URL 이" "$C" && grep -q "safe-area 는 웹이 책임진다" "$C" && grep -q "D-32 탭 루트 스크롤 복원" "$C" && grep -q "WR-03 Android 브리지 = 선택" "$C" && grep -q "008" .planning/sketches/MANIFEST.md && echo "UAT3 DECISIONS OK"</automated>
    <fails_when>output contains "MISSING", or "UAT3 DECISIONS OK" is absent (a decision head, the D-06/D-12/D-25 original text, a pointer, the WR-03 line, or the MANIFEST row is missing)</fails_when>
  </verify>
  <acceptance_criteria>
    - 일곱 결정 머리가 모두 있다(verify 루프) — 각 `grep -n "^- \*\*<ID> "` 한 줄
    - D-32 가 축소 범위를 적는다: `grep -c "스켈레톤 없이 바로" .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md` ≥ 1 · `grep -c "keep-alive(React Activity · 화면 보존)와 탭별 웹뷰는 기각" .planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md` ≥ 1
    - 원문 보존: D-06 · D-12 · D-25 원문 문자열(verify 의 세 grep)이 남아 있다 · 포인터 세 개(`D-12a 키보드 숨김 즉시` · `D-25a 앱 /trading 하단 공용 패널 숨김` · `D-32 탭 루트 스크롤 복원`) 각 ≥ 1
    - README `winner:` 가 pending 이 아니다: `sed -n '1,8p' .planning/sketches/008-uat3-surfaces/README.md | grep -c 'winner: "pending"'` 결과 0
    - `git log -1 --name-only` 가 스케치·MANIFEST·CONTEXT 파일만 담고 push 없음
  </acceptance_criteria>
  <done>채택안 · 재확인 답 · UAT 3차 결정이 정본(CONTEXT · 스케치 README)에 남았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 목업 → 구현 | 채택안이 정본이 된다 — 검토 전 구현·커밋이 섞이면 사용자가 보지 않은 UI 가 배포 경로에 들어간다 |
| 로컬 git → 원격 master | push = 웹 프로덕션 배포 · 같은 작업 트리에 다른 세션 변경 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-57 | Tampering | 검토 전 UI 구현/커밋 | medium | mitigate | blocking-human 체크포인트(Task 2) · Task 1 webapp/·mobile/ 상태 스냅숏 비교 · 커밋은 Task 3 에서만 |
| T-21-58 | Tampering | 결정 정본 이원화(UAT decision 줄 vs CONTEXT) | low | mitigate | D-ID 끝 gap id 근거 · D-32 = UAT R3-11 수정 decision 과 같은 문구 · 원문 보존 + 포인터 |
| T-21-SC | Tampering | androidx.webkit 직접 선언(WR-03) | medium | mitigate | 사람 결정(질문 6) · 좌표·버전 = 이미 APK 에 있는 1.14.0 · 클래스패스 실측 첨부 |
| T-21-52 | Tampering | 다른 세션 변경이 섞인 커밋 | high | mitigate | 파일 지정 add · 커밋 직전 git status -sb · push 금지 |
</threat_model>

<verification>
- 목업 파일 · 사용자 답(6) · CONTEXT 결정 7건 + WR-03 줄 · MANIFEST 행 · 커밋 1개(push 없음)
</verification>

<success_criteria>
- 목업 게이트가 필요한 세 표면이 채택안을 갖고, 재확인 세 건과 UAT 3차 결정(R3-11 축소 포함)이 CONTEXT 에서 인용 가능하다.
</success_criteria>

<assumption_delta_decision>
detector: detected=true · signal=chosen · term="custom" · snippet="androidx.browser 선언 결정 체크포인트 → 호스트 밖 http(s) = Custom Tabs(GhTradeWebViewClient 가로채기)"
outcome: no-change
rationale: 「Custom」 은 Android 제품명 「Custom Tabs」 이고 그 결정(D-28 · 21-17 선택 a)은 이미 resolved 된 G-21-N3 이다. 이번 라운드는 그 경로를 바꾸지 않는다.
separately-surfaced: 이번 라운드의 실제 결정 변경은 G-21-R3-11 이다 — 사용자가 2026-09-26 keep-alive 를 버리고 「스크롤 복원 + 스켈레톤 없는 재방문 · 상태 초기화 허용」 단순안을 골랐다(과설계 금지). 21-UAT.md R3-11 decision 줄에 반영했고 D-32 가 기록한다. 메커니즘 스파이크·결정 체크포인트는 두지 않는다.
</assumption_delta_decision>

## Flagged assumptions

- **엣지 탐침 MOBILE-01 (unclassified · unresolved):** 엣지 탐침이 MOBILE-01(여러 표면을 가진 셸 요구사항)을 분류하지 못했다. 이 라운드의 경계는 각 구현 플랜의 명시 테스트로 다루고, 분류되지 않은 나머지는 21-36 UAT 에서 사람이 본다.
- 목업은 토큰을 옮긴 정적 그림이다 — 구현 모양의 미세 차이는 21-36 UAT 가 본다.

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| 스케치 `.planning/sketches/008-uat3-surfaces/`(index.html · README.md) · MANIFEST 008 행 | 목업 | 21-25 |
| CONTEXT D-12a · D-12b · D-25a · D-29 · D-30 · D-31 · D-32(R3-11 축소: 스크롤 복원 + 스켈레톤 없음 · 상태 초기화 허용) · WR-03 androidx.webkit 선언 답 | 결정 기록 | 21-25 |
| `PAD_CHIPS.만원` = 천만(+1,000) · 오천만(+5,000) · 1억(+10,000) · 지우기 · 20-CONTEXT D-17 포인터 | 상수 · 결정 | 21-26 |
| `SettingGroup` 제목 없는 그룹 상단 `pt-1` | 클래스 | 21-26 |
| 카드 헤더 ✕ `data-slot="card-close"` · 종목정보 팝업 ✕ = `size-8` + lucide `XIcon` + `after:-inset-1.5`(히트 44) · 팝업 고정 높이 · `scrollbar-gutter:stable` · 폰 safe-area 패딩 | 컴포넌트 | 21-26 |
| `webapp/src/lib/safe-path.ts` `isSafeInternalPath` · `recent-search.ts` `RECENT_CODE_RE` · `PopoverContent` 안 `NativeOverlayMarker` · `useDebouncedSearch` 반환 `resultsQuery` · `viewport.themeColor` `#17171c` + `ThemeColorSync` | 함수 · 훅 · 메타 | 21-27 |
| iOS 키보드 사유 즉시 숨김 · 재표시 90ms · `awaitingContent`(didStartProvisionalNavigation → 첫 route/1.5초) · 초기 숨김 · 브리지 출처 scheme·host·port 비교 · 오프라인 `dev=1` · 스모크 기기 지정(`IOS_DEVICE_UDID` · `ANDROID_SERIAL`) · mobile/README 명령표 | Swift · 스크립트 · 문서 | 21-28 |
| Android IME 애니메이션 콜백 즉시 숨김 · `awaitingContent`(onPageStarted) · `pullBlocked` 초기화 · 오프라인 뒤로가기(재현 시) · 오프라인 `dev=1` · 폴백 페이지 dev=1 일 때만 localhost · allowBackup=false + `res/xml/data_extraction_rules.xml` · 브리지 WebMessageListener(21-25 답 a) | Kotlin · XML · HTML | 21-29 |
| `NewsFullList` · `DiscussionFullList` · `useNewsView` · `StockNewsTabPanel` · 섹션 `onShowAll` · 종목상세 `?tab=news&view=news\|discussions` · 옛 URL 리다이렉트 · 팝업 `newsView` | 컴포넌트 · 훅 · 라우트 | 21-30 |
| `useTabRootScrollMemory`(`webapp/src/lib/tab-scroll-memory.ts` · AppShell 에서 호출) · 기존 `lib/query-cache` 재사용: 검색 허브 · 대화 목록 · 오늘 주문 | 훅 · 캐시 | 21-31 |
| 앱 공용 패널 숨김 CSS · 카드 더티 바 탭바 비킴(프로브) · `StrategyLogFeedProvider` · `useStrategyLogFeed` · `diffLimitChasers` · /me 전략 로그(`me-strategy-log`) | CSS · Provider · 컴포넌트 | 21-32 |
| 작업대 `?code=` 소비(카드 보장·펼침·포커스) · 카드 수동주문 주문유형(지정가/시간외종가) | 컴포넌트 | 21-33 |
| 종목상세 「트레이딩」 CTA(폰 바 + `detail-trading-button`) · 호가주문 탭 제거 · `?tab=orderbook` → `/trading?code=` · 호가 탭 전용 코드·`variant` 삭제 · orderbook.spec 선별 이전 | 컴포넌트 · 정리 · e2e | 21-34 |
| 게이트 기록 · `21-REVIEW-R2.md`(13건 대조) · UAT 환경(21-35-SUMMARY) · UAT 3차 재검증 · push 결정(21-36-SUMMARY) | 문서 | 21-35 · 21-36 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-25-SUMMARY.md` when done — 목업 변형 요약 · 사용자 답 원문(6) · 클래스패스 실측 · CONTEXT 결정 목록 · 커밋 SHA.
</output>
