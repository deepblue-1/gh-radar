---
phase: 21-gh-trade-mobile-app
plan: 13
type: execute
wave: 5
depends_on: ["21-12", "21-11"]
files_modified:
  - mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
  - mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt
  - mobile/android/app/src/main/java/com/ghtrade/app/ThemeStore.kt
  - mobile/android/app/src/main/res/values/bools.xml
  - mobile/android/app/src/main/res/values-sw600dp/bools.xml
  - mobile/android/app/build.gradle
  - mobile/android/variables.gradle
autonomous: true
requirements: [MOBILE-01]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-04 · D-17: Android 당겨서 새로고침 = WebView 를 `SwipeRefreshLayout`(androidx 1.2.0)으로 감싸고 제스처 → `evaluateJavascript(refresh 훅 ‖ location.reload())` → 훅과 무관하게 1초 뒤 `isRefreshing = false` · 문서가 위로 스크롤돼 있거나 웹이 `pull {blocked:true}` 를 보냈거나 오버레이 열림·오프라인 페이지면 당김 비활성(Pitfall 7) · 스피너는 상태바 아래(`setProgressViewOffset`)"
    - "D-26: 뒤로가기 = `onBackPressedDispatcher.addCallback`(Android 16 에서 옛 오버라이드는 불리지 않는다 — Pitfall 5) — ① 웹 오버레이 열림이면 `window.__ghTrade.back()` 호출(가장 위 시트만 닫힘) ② `canGoBack` 이면 `goBack` ③ 홈(`/`)이 아니면 홈으로 navigate ④ 홈이면 앱 종료"
    - "D-23: 웹 `theme` 메시지 → 상태바·내비게이션바 아이콘 명암(`WindowInsetsControllerCompat` — 창 상태바 색 직접 지정 금지) · DecorView·WebView 배경(라이트 `#ffffff` · 다크 `#17171c`) · 탭바·새로고침 스피너 팔레트 · SharedPreferences `gh_trade`/`theme` 저장 → 다음 실행 `load()` 첫 프레임부터 적용 · `onConfigurationChanged` 에서 super 뒤 재적용(SystemBars 가 OS 다크모드로 되돌리는 것 방지 — Pitfall 13)"
    - "D-24: 폰 `portrait` · 태블릿(sw600dp) `fullSensor` — `res/values(-sw600dp)/bools.xml` `is_tablet` 로 `onCreate` 에서 `requestedOrientation` 분기"
    - "D-19: 메인 프레임 네트워크 오류(ERROR_HOST_LOOKUP · ERROR_CONNECT · ERROR_IO · ERROR_TIMEOUT)에서만 `https://localhost/index.html?to=<원래 URL>&theme=<현재>` 폴백 — HTTP 4xx/5xx(Next 404 포함)는 웹 그대로 · 로컬 요청 가로채기 오버라이드 없음(Pitfall 2·4)"
    - "앱 백그라운드 전환(`onPause`) 시 `CookieManager.flush()` 로 Supabase 세션 쿠키를 디스크에 남긴다"
  artifacts:
    - path: mobile/android/app/src/main/java/com/ghtrade/app/ThemeStore.kt
      provides: "SharedPreferences gh_trade/theme load/save"
      contains: "gh_trade"
    - path: mobile/android/app/src/main/res/values-sw600dp/bools.xml
      provides: "is_tablet = true"
      contains: "is_tablet"
  key_links:
    - from: mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
      to: webapp/src/lib/native/native-bridge-provider.tsx
      via: "evaluateJavascript(__ghTrade.refresh / __ghTrade.back)"
      pattern: "__ghTrade\\.(refresh|back)"
    - from: mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt
      to: mobile/www/index.html
      via: "loadUrl(https://localhost/index.html?to=…&theme=…)"
      pattern: "localhost/index.html"
---

<assumption_delta_decision>
- 검출기 `assumption-delta scan 21` → `detected: false`. 수동 판정: 두 번째 클라이언트 플랫폼 — 정본 명사 **웹앱**, 결정 **add-alongside**. 새로고침·뒤로가기의 「무엇을 할지」는 웹 훅이 정하고 네이티브는 제스처·시간만 가진다(21-01 블록 참조).
</assumption_delta_decision>

<objective>
Android 셸의 시스템 연동을 끝낸다: 당겨서 새로고침(D-04·D-17) · Android 16 대응 뒤로가기 정책(D-26) · 테마 추종·저장(D-23) · 방향(D-24) · 네트워크 오류 전용 오프라인 폴백(D-19, 폴백 페이지는 21-11 완성본 공유).

Purpose: weekly-wine 골격 중 Android 16 에서 깨지는 부분(뒤로가기)을 올바른 API 로, Capacitor 가 이미 하는 일(인셋·로컬 서버)은 건드리지 않고 붙인다.
Output: MainActivity·WebViewClient 확장 + ThemeStore + bools 리소스 + swiperefreshlayout 의존성.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md
@.planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md
@mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt
@mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt
@mobile/www/index.html

Gradle 은 Bash timeout 600000.

<interfaces>
21-02 · 21-12 계약:

```kotlin
class MainActivity : BridgeActivity() {
  lateinit var tabBar: GhTradeTabBar; var currentTheme = "light"; var currentPath = "/"; var isOfflinePage = false
  var overlayOpen = false; var pullBlocked = false; lateinit var rootLayout: ViewGroup
  fun serverHost(): String?; fun onUrlChanged(url: String); fun onNativeMessage(type: String, payload: JSONObject?)
  fun updateTabBarVisibility(); fun selectTab(tab: TabId)
}
class GhTradeWebViewClient(bridge: Bridge, private val host: MainActivity) : BridgeWebViewClient(bridge)   // onPageFinished · doUpdateVisitedHistory 만
data class GhTradePalette(card, bg, primary, muted, line) { companion object { fun of(theme: String) } }
// 21-11 www/index.html: ?to=<URL>&theme=dark|light · safeTarget 허용 출처 · 5초 탐침
```

Capacitor 8.5.2 사실(RESEARCH Pattern 3 · Pitfall 8·13): `SystemBars.handleOnConfigurationChanged` 가 회전·uiMode 변경마다 DEFAULT(OS 다크) 스타일과 DecorView 배경을 다시 쓴다 · 매니페스트 `configChanges` 에 `uiMode|orientation|screenSize` 가 있어 액티비티는 재생성되지 않는다 · 로컬 에셋 서버 = `https://localhost` · 프로그램 `loadUrl` 은 `shouldOverrideUrlLoading` 을 거치지 않는다.

weekly-wine 원형: `MainActivity.kt` L182-209 `setupPullToRefresh`(부모에서 떼어 SwipeRefreshLayout 에 같은 index 재삽입 · `setOnChildScrollUpCallback` · `postDelayed({ isRefreshing = false }, 1000)`) · L122-140 옛 뒤로가기 정책(구조만 참고 — 옛 오버라이드 API 는 쓰지 않는다).

```kotlin
// 이 플랜이 새로 만드는 계약
object ThemeStore { fun load(ctx: Context): String; fun save(ctx: Context, theme: String) }  // prefs "gh_trade" · key "theme" · 기본 "light"
// MainActivity: fun applyTheme(theme: String) · fun showOffline(failedUrl: String) · private fun handleBack()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SwipeRefreshLayout 당겨서 새로고침(1초 고정 · 비활성 조건) + `onBackPressedDispatcher` 뒤로가기 정책 (D-04 · D-17 · D-26)</name>
  <files>mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt, mobile/android/app/build.gradle, mobile/android/variables.gradle</files>
  <read_first>
    - mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt (21-02·21-12 전체)
    - /Users/alex/repos/weekly-wine-app/android/app/src/main/java/kr/co/weeklywine/app/MainActivity.kt (L122-140 · L182-209)
    - mobile/android/variables.gradle · mobile/android/app/build.gradle (의존성 블록)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pattern 3 SwipeRefreshLayout · Pitfall 5·7 · Assumptions A13)
    - webapp/src/lib/native/native-bridge-provider.tsx (21-04 — `back()` 반환 규칙 · `pull` 신호)
  </read_first>
  <action>
    ① 의존성: `variables.gradle` 에 `androidxSwipeRefreshLayoutVersion = '1.2.0'` · `app/build.gradle` 에 `implementation "androidx.swiperefreshlayout:swiperefreshlayout:$androidxSwipeRefreshLayoutVersion"`(Google Maven 공식 좌표 · RESEARCH Standard Stack).
    ② `wrapInSwipeRefresh(wv)`(weekly-wine L186-209 이식 · 21-12 의 `rootLayout` 참조를 잡은 **뒤** `load()` 에서 호출): WebView 를 부모에서 떼어 같은 `layoutParams`·index 로 `SwipeRefreshLayout` 에 넣는다 · `setOnChildScrollUpCallback { _, _ -> wv.canScrollVertically(-1) || pullBlocked || overlayOpen || isOfflinePage }`(A13 — `pull` 신호가 늦게 오면 내부 스크롤에서 가끔 당겨질 수 있어 웹 `overscroll-behavior` 가 보완 · UAT) · `setOnRefreshListener { wv.evaluateJavascript("window.__ghTrade&&window.__ghTrade.refresh?window.__ghTrade.refresh():location.reload()", null); handler.postDelayed({ swipe.isRefreshing = false }, 1000) }`(D-17 — 훅 결과를 기다리지 않는다) · 스피너 위치는 상태바 인셋 아래: `ViewCompat.getRootWindowInsets(rootLayout)` 의 `statusBars().top` 을 `top` 으로 `setProgressViewOffset(false, top, top + 64dp)` — 인셋 리스너를 새로 걸지 않고 `rootLayout.post { … }` 로 한 번 읽는다(Pitfall 8). 스피너 색은 `applyTheme`(Task 2)이 정한다. `swipeRefresh` 를 필드로 보관.
    ③ 뒤로가기(D-26 · Pitfall 5): `onCreate` 에서 `super.onCreate` 뒤 `onBackPressedDispatcher.addCallback(this, object : OnBackPressedCallback(true) { override fun handleOnBackPressed() = handleBack() })`. `handleBack()`: (a) `overlayOpen` 이면 `bridge.webView.evaluateJavascript("window.__ghTrade&&window.__ghTrade.back?window.__ghTrade.back():false", null)` 후 return (b) `bridge.webView.canGoBack()` 면 `goBack()` (c) 오프라인 페이지가 아니고 `currentPath != "/"` 면 `selectTab(TabId.HOME)` (d) 그 외 `finish()`. 옛 뒤로가기 오버라이드 메서드는 두지 않는다. `@capacitor/app` 은 설치돼 있지 않아 경합 콜백이 없다(Pitfall 12).
    ④ `onPause()` 오버라이드: `super.onPause()` 뒤 `CookieManager.getInstance().flush()`(weekly-wine 쿠키 저장/복원 로직은 가져오지 않는다 — RESEARCH Anti-Patterns).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:android</automated>
    <fails_when>non-zero exit, or the output lacks "BUILD SUCCESSFUL"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "swiperefreshlayout:swiperefreshlayout" mobile/android/app/build.gradle` 가 1줄 · `grep -n "androidxSwipeRefreshLayoutVersion = '1.2.0'" mobile/android/variables.gradle` 가 1줄
    - `grep -n "postDelayed({ swipe.isRefreshing = false }, 1000)\|isRefreshing = false }, 1000" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 1줄 이상
    - `grep -n "setOnChildScrollUpCallback" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 1줄 · 같은 줄/블록에 `pullBlocked` 와 `overlayOpen` 이 쓰인다(`grep -n "pullBlocked" …` 1줄 이상)
    - `grep -n "onBackPressedDispatcher.addCallback" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 1줄 · `grep -c "override fun onBackPressed()" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 0
    - `grep -n "__ghTrade.back" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 1줄 · `grep -n "CookieManager.getInstance().flush()" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 1줄
  </acceptance_criteria>
  <done>당김 한 번에 웹 훅이 불리고 1초에 스피너가 닫히며, 뒤로가기가 시트 → 이전 화면 → 홈 → 종료 순으로 동작하도록 Android 16 호환 API 로 배선됐다.</done>
</task>

<task type="auto">
  <name>Task 2: 테마 추종·저장·재적용(D-23) · 방향 bools(D-24) · 네트워크 오류 전용 오프라인 폴백(D-19)</name>
  <files>mobile/android/app/src/main/java/com/ghtrade/app/ThemeStore.kt, mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt, mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt, mobile/android/app/src/main/res/values/bools.xml, mobile/android/app/src/main/res/values-sw600dp/bools.xml</files>
  <read_first>
    - mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt (21-02)
    - mobile/android/app/src/main/java/com/ghtrade/app/GhTradePalette.kt (21-12)
    - mobile/www/index.html (21-11 — 쿼리 계약 `to` · `theme`)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pattern 3 `onReceivedError` · Code Examples 「Android 방향 분기 리소스」 · Pitfall 2·4·8·13 · Assumptions A2)
  </read_first>
  <action>
    ① `ThemeStore.kt`: `object ThemeStore { private const val PREFS = "gh_trade"; private const val KEY = "theme"; fun load(ctx: Context): String = ctx.getSharedPreferences(PREFS, MODE_PRIVATE).getString(KEY, "light").let { if (it == "dark") "dark" else "light" }; fun save(ctx: Context, theme: String) }`.
    ② `applyTheme(theme)`: `currentTheme = theme` · `val p = GhTradePalette.of(theme)` · `WindowInsetsControllerCompat(window, window.decorView).apply { isAppearanceLightStatusBars = theme == "light"; isAppearanceLightNavigationBars = theme == "light" }` · `window.decorView.setBackgroundColor(p.bg)` · `bridge.webView.setBackgroundColor(p.bg)` · `swipeRefresh.setColorSchemeColors(p.primary)` · `swipeRefresh.setProgressBackgroundColorSchemeColor(p.card)` · `tabBar.apply(p)` · `ThemeStore.save(this, theme)`. 창의 상태바 색을 직접 지정하는 API 는 쓰지 않는다(엣지투엣지·SystemBars 충돌). `load()` 에서 탭바·새로고침 준비 뒤 `applyTheme(ThemeStore.load(this))`(첫 프레임). `onNativeMessage` 의 `theme` → `payload.optString("theme")` 이 `dark`/`light` 면 `applyTheme`. `override fun onConfigurationChanged(newConfig)` → `super.onConfigurationChanged(newConfig)` **뒤에** `applyTheme(currentTheme)` 와 탭바 폭 재계산(Pitfall 13 · 21-12 폭 규칙). OS 다크모드(`uiMode`)를 테마 판단에 쓰지 않는다(D-23).
    ③ 방향(D-24): `res/values/bools.xml` = `<bool name="is_tablet">false</bool>`, `res/values-sw600dp/bools.xml` = `true`. `onCreate` 에서 `super.onCreate` **전에** `requestedOrientation = if (resources.getBoolean(R.bool.is_tablet)) ActivityInfo.SCREEN_ORIENTATION_FULL_SENSOR else ActivityInfo.SCREEN_ORIENTATION_PORTRAIT`. 매니페스트 `screenOrientation` 은 넣지 않는다(enum 이라 리소스 분기 불가).
    ④ 오프라인(D-19): `GhTradeWebViewClient.onReceivedError(view, request, error)` 오버라이드 — `super.onReceivedError(…)` 먼저 → `request.isForMainFrame` 이고 `error.errorCode in setOf(ERROR_HOST_LOOKUP, ERROR_CONNECT, ERROR_IO, ERROR_TIMEOUT)`(A2) 일 때만 `host.showOffline(request.url.toString())`. HTTP 오류 콜백과 로컬 요청 가로채기는 여전히 오버라이드하지 않는다. `MainActivity.showOffline(failedUrl)`: 이미 오프라인 페이지면 무동작 · `bridge.webView.loadUrl("https://localhost/index.html?to=" + URLEncoder.encode(failedUrl, "UTF-8") + "&theme=" + currentTheme)` — 호스트가 server 가 아니므로 21-12 규칙으로 탭바·새로고침이 숨는다(D-12 ②). 복귀는 폴백 페이지의 `safeTarget` 이 정한다(21-11).
    ⑤ 빌드 → TabRoutesTest → 스모크 순으로 확인한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:build:android && pnpm --filter @gh-radar/mobile run native:test:android</automated>
    <fails_when>non-zero exit, or output lacks "BUILD SUCCESSFUL", or contains "FAILED"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/mobile run native:smoke:android</automated>
    <fails_when>non-zero exit, or the output lacks "SMOKE OK ready platform=android"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n '"gh_trade"' mobile/android/app/src/main/java/com/ghtrade/app/ThemeStore.kt` 가 1줄
    - `grep -n "isAppearanceLightStatusBars" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 1줄 이상 · `grep -c "statusBarColor" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 0
    - `grep -n "super.onConfigurationChanged" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 1줄
    - `grep -n "SCREEN_ORIENTATION_FULL_SENSOR" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 1줄 · `grep -n "true" mobile/android/app/src/main/res/values-sw600dp/bools.xml` 가 1줄
    - `grep -n "isForMainFrame" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt` 가 1줄 · `grep -cE "onReceivedHttpError|shouldInterceptRequest" mobile/android/app/src/main/java/com/ghtrade/app/GhTradeWebViewClient.kt` 가 0
    - `grep -n "https://localhost/index.html?to=" mobile/android/app/src/main/java/com/ghtrade/app/MainActivity.kt` 가 1줄
  </acceptance_criteria>
  <done>Android 가 웹 테마를 따라 상태바·배경·탭바를 바꾸고 재실행에도 유지하며, 폰은 세로·태블릿은 전방향, 네트워크 오류에서만 폴백으로 간다(실동작은 21-16 UAT).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 네이티브 → 웹 `evaluateJavascript`(refresh/back) | 상수 스크립트만 |
| 오프라인 폴백 URL 조립 | 실패한 원래 URL 을 쿼리로 넘긴다 |
| androidx Maven 의존성 | 새 라이브러리 1개 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-06 | Tampering(오픈 리다이렉트) | `?to=` 인코딩·복귀 | high | mitigate | `URLEncoder.encode` 로 조립 · 복귀 판정은 폴백 페이지 `safeTarget` 허용 출처(21-11) |
| T-21-38 | Denial of Service | 오프라인 오탐(HTTP 4xx/5xx) | medium | mitigate | 메인 프레임 + 네트워크 오류 코드 4종만 · HTTP 오류 콜백 미오버라이드(acceptance grep 0) |
| T-21-40 | Denial of Service | 뒤로가기가 앱을 즉시 종료(Android 16) | medium | mitigate | `onBackPressedDispatcher` 콜백 · 옛 오버라이드 0(acceptance) |
| T-21-13 | Denial of Service | 당김 연타 → 과도 재조회 | low | mitigate | 1초 고정 스피너 · 웹 훅은 GET 재조회뿐(21-07 D-18) |
| T-21-SC | Tampering | `androidx.swiperefreshlayout:1.2.0` | low | accept | Google Maven 공식 AndroidX 좌표 · npm 설치 없음 |
</threat_model>

<verification>
- `native:build:android` · TabRoutesTest · `native:smoke:android` green
- 당김·뒤로가기(제스처/3버튼)·테마·회전·비행기 모드는 21-16 UAT
</verification>

<success_criteria>
- D-04·D-17·D-19·D-23·D-24·D-26 이 Android 에서 구현됐다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/` 패키지 `@gh-radar/mobile` — scripts `native:sync` · `native:sync:dev` · `native:build:ios` · `native:build:android` · `native:smoke:ios` · `native:open:ios` · `native:open:android` · `native:assets` (21-01) · `native:smoke:android` (21-02) · `native:check-tab-routes:ios` (21-10) · `native:test:android` (21-12) · `native:verify-prod` (21-16) | 패키지·스크립트 | 21-01 · 21-02 · 21-10 · 21-12 · 21-16 |
| `mobile/capacitor.config.ts` — appId `com.ghtrade.app` · appName `GH Trade` · `CAP_SERVER_URL` · `SystemBars` (21-01) · `SocialLogin` (21-15) | 설정 | 21-01 · 21-15 |
| `mobile/tsconfig.json` · `mobile/.gitignore` | 설정 | 21-01 |
| `mobile/www/index.html` — 오프라인 폴백(최소 21-01 · `safeTarget`·5초 탐침·`online`·테마 21-11) | 정적 페이지 | 21-01 · 21-11 |
| `mobile/scripts/smoke-ios.sh` · `mobile/scripts/smoke-android.sh` — 「SMOKE OK ready platform=…」 | 스모크 | 21-01 · 21-02 |
| `mobile/scripts/check-tab-routes-ios.sh` · `mobile/scripts/tab-routes-check.swift` | Swift 경로표 검사 | 21-10 |
| `mobile/scripts/render-resources.mjs` · `mobile/resources/{icon.svg,icon-only.png,icon-foreground.png,icon-background.png,splash.png,splash-dark.png}` | 에셋 원본 | 21-14 |
| `mobile/scripts/verify-prod-config.mjs` · `mobile/README.md` | 운영 설정 검사·문서 | 21-16 |
| iOS `SceneDelegate.swift` (루트 = `GHTradeBridgeViewController()` · 창 배경 21-11) | Swift | 21-01 · 21-11 |
| iOS `GHTradeBridgeViewController` · `WeakScriptMessageHandler` · `NativeMessageType` | Swift | 21-01 · 21-10 · 21-11 |
| iOS `GHTradeTabBar` · `GHTradeTab` · `GHTabID` · `GHTradePalette` · `GHTradeTheme` · `TabRoutes.activeTab(forPath:)` · `TabRoutes.hidesTabBar(path:)` | Swift | 21-10 |
| iOS `NavigationDelegateProxy` · `ThemeStore` (UserDefaults `gh-trade.theme`) | Swift | 21-11 |
| Android `MainActivity` (`BridgeActivity`) · `GhTradeWebViewClient` (`BridgeWebViewClient` 상속) · `GhTradeBridge` (`@JavascriptInterface postMessage`) | Kotlin | 21-02 · 21-12 · 21-13 |
| Android `GhTradeTabBar` · `GhTradePalette` · `TabId` · `TabRoutes.activeTabFor` · `TabRoutes.hidesTabBar` · `TabRoutesTest` | Kotlin | 21-12 |
| Android `ThemeStore` (SharedPreferences `gh_trade`/`theme`) · `res/values/bools.xml` · `res/values-sw600dp/bools.xml` (`is_tablet`) | Kotlin·리소스 | 21-13 |
| Android `res/drawable/ic_tab_{home,home_fill,search,search_fill,trading,trading_fill,ai,ai_fill,me,me_fill}.xml` | 벡터 | 21-12 |
| 웹 `lib/native/native-detect.ts` — `NATIVE_DETECT_SCRIPT` · `isNativeApp` · `nativePlatform` | 유틸 | 21-01 |
| 웹 `lib/native/post-native.ts` — `postNative` · `NativeMsgType` | 유틸 | 21-04 |
| 웹 `lib/native/native-bridge-provider.tsx` — `NativeBridgeProvider` · `useNativeBridge` · `NativeRefreshFn` · `window.__ghTrade = { refresh, navigate, back }` | Provider | 21-04 |
| 웹 `lib/native/use-native-refresh.ts` — `useNativeRefresh` | 훅 | 21-04 |
| 웹 `lib/native/native-overlay-marker.tsx` — `NativeOverlayMarker` | 컴포넌트 | 21-04 |
| 웹 `lib/native/google-client-ids.ts` — `GOOGLE_WEB_CLIENT_ID` · `GOOGLE_IOS_CLIENT_ID` · `GOOGLE_ANDROID_CLIENT_ID` · `GOOGLE_IOS_URL_SCHEME` | 공개 식별자 상수 | 21-03 |
| 웹 `lib/native/native-google-login.ts` — `nativeGoogleSignIn` · `randomNonce` · `sha256Hex` · `classifyNativeLoginError` | 서비스 | 21-15 |
| 웹 `RelayConnectionState.probeNow` · `EMPTY_RELAY_VALUE.probeNow` | 훅 계약 | 21-07 |
| 웹 `lib/recent-search.ts` — `RECENT_SEARCH_KEY` · `RECENT_SEARCH_MAX` · `RecentSearchItem` · `readRecentSearches` · `pushRecentSearch` · `removeRecentSearch` · `clearRecentSearches` | 유틸 | 21-08 |
| 웹 `app/search/page.tsx` · `components/search/search-page-client.tsx` — `SearchPageClient` · 사이드바 `NAV_SEARCH_PAGE` | 페이지·상수 | 21-08 |
| 웹 `components/me/account-card.tsx` — `AccountCard` | 컴포넌트 | 21-09 |
| 웹 마크업 계약 `data-slot="app-aside"` · `data-slot="app-menu-button"` · `data-slot="stock-ai-button"` · `data-slot="shared-panels"` · `data-pinned` · `data-order-cta` · `data-slot="account-card"` | 마크업 | 21-05 · 21-06 · 21-09 |
| CSS `@custom-variant native` (21-04) · 앱 셸 규칙 (21-05) · `--app-safe-top` · `--app-safe-bottom` · `--app-safe-left` · `--app-safe-right` · `--native-tabbar-gap` · `--native-tabbar-offset` · `--native-body-reserve` (21-06) | 토큰·변형 | 21-04 · 21-05 · 21-06 |
| 웹 `app/icon.svg` (= 스케치 004 `#app-a`) | 파비콘 | 21-14 |
| 단위 테스트: `native-detect.test.ts` (21-01) · `post-native.test.ts` · `native-bridge-provider.test.tsx` · `native-overlay.test.tsx` (21-04) · `google-client-ids.test.ts` (21-03) · `relay-socket.test.ts` probeNow · `stock-native-refresh.test.tsx` (21-07) · `recent-search.test.ts` · `search-page-client.test.tsx` · `app-sidebar.test.tsx` 보강 (21-08) · `account-card.test.tsx` · `app-shell-chrome.test.tsx` 보강 (21-09) · `native-google-login.test.ts` · `login/__tests__/page.test.tsx` (21-15) · `TabRoutesTest.kt` (21-12) | 단위 | 각 플랜 |
| e2e: `fixtures/native-app.ts` — `installNativeApp` · `nativeMessages` (21-05) · `specs/native-shell.spec.ts` (21-05 · 21-06) · `specs/search-page.spec.ts` (21-08) · `specs/brand-account.spec.ts` (21-09) | Playwright | 각 플랜 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-13-SUMMARY.md` when done
</output>
