---
phase: 21-gh-trade-mobile-app
plan: 08
type: execute
wave: 4
depends_on: ["21-04", "21-05"]
files_modified:
  - webapp/src/lib/recent-search.ts
  - webapp/src/lib/__tests__/recent-search.test.ts
  - webapp/src/components/search/search-page-client.tsx
  - webapp/src/components/search/__tests__/search-page-client.test.tsx
  - webapp/src/app/search/page.tsx
  - webapp/src/components/layout/app-sidebar.tsx
  - webapp/src/components/layout/__tests__/app-sidebar.test.tsx
  - webapp/e2e/specs/search-page.spec.ts
autonomous: true
requirements: [MOBILE-01]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-07 · D-07a: 새 페이지 `/search` = B 「탐색 허브」 — 제목 「검색」 22/700 → 검색 입력(높이 48 · radius 14 · `--muted` 면 · placeholder 「종목명 또는 코드」 · 입력 중 우측 ✕ 지우기) → 진입 타일 3열(상승률 상위 `/scanner` · 테마 `/themes` · 관심종목 `/watchlist`, 실데이터 보조 문구 「25%↑ N종목」·「오늘 N개」·「N종목」) → 「최근 검색」 → 「지금 상승률 상위」 5행 + 「더보기 ›」(`/scanner`)"
    - "D-07a: 입력 중(공백 제외 1자 이상)에는 타일·최근 검색·미리보기가 숨고 결과 카드(52 행 · 종목명 · 코드 · 시장 배지 · 현재가+등락률)만 보인다 — 결과는 기존 `useDebouncedSearch`(300ms) · 선택 시 `/stocks/{code}` 이동 + 최근 검색 저장"
    - "D-07a: 최근 검색은 localStorage `gh-radar:recent-search`(D-21 접두 유지) 최대 10개 · 최신 앞 · 같은 코드 중복 없음 · 개별 ✕ 삭제 · 「지우기」 전체 삭제 · 비어 있으면 섹션 숨김 · 저장 실패가 화면을 막지 않는다"
    - "D-07a: 「지금 상승률 상위」·타일 카운트는 마운트 시 기존 scanner API(`fetchScannerStocks`, rate_desc)·테마 API 를 1회씩 읽는다 — 자동 폴링 없음 · 관심종목 수는 `useWatchlistSet().count`"
    - "D-04: `/search` 가 `useNativeRefresh` 로 미리보기·카운트 재조회를 등록한다"
    - "D-07 · D-07a: 웹 사이드바 홈 바로 아래에 「검색」(`/search`) 링크가 추가되고 활성 시 기존 선택 토큰(`--nav-on-bg`/`--nav-on-fg`)을 쓴다 — 「종목검색」 그룹 3항목은 그대로(사이드바 재편은 Deferred) · 데스크톱 본문 max-width 900"
  artifacts:
    - path: webapp/src/lib/recent-search.ts
      provides: "최근 검색 localStorage 유틸"
      exports: ["RECENT_SEARCH_KEY", "RECENT_SEARCH_MAX", "RecentSearchItem", "readRecentSearches", "pushRecentSearch", "removeRecentSearch", "clearRecentSearches"]
    - path: webapp/src/components/search/search-page-client.tsx
      provides: "SearchPageClient — /search B 탐색 허브"
      exports: ["SearchPageClient"]
    - path: webapp/src/app/search/page.tsx
      provides: "/search 라우트(AppShell + AppSidebar)"
    - path: webapp/e2e/specs/search-page.spec.ts
      provides: "MOBILE-01g e2e"
  key_links:
    - from: webapp/src/components/search/search-page-client.tsx
      to: webapp/src/hooks/use-debounced-search.ts
      via: "useDebouncedSearch(query, 300)"
      pattern: "useDebouncedSearch\\("
    - from: webapp/src/components/search/search-page-client.tsx
      to: webapp/src/lib/recent-search.ts
      via: "pushRecentSearch({ code, name, market }) on select"
      pattern: "pushRecentSearch\\("
    - from: webapp/src/components/layout/app-sidebar.tsx
      to: webapp/src/app/search/page.tsx
      via: "NAV_SEARCH_PAGE = { href: '/search', label: '검색', icon: Search }"
      pattern: "NAV_SEARCH_PAGE"
---

<assumption_delta_decision>
- 검출기 `assumption-delta scan 21` → `detected: false`. 수동 판정: 두 번째 클라이언트 플랫폼 — 정본 명사 **웹앱**, 결정 **add-alongside**. `/search` 는 앱 탭의 목적지이자 웹 기능이다 — 앱 전용 분기 없이 같은 페이지를 두 클라이언트가 쓴다(21-01 블록 참조).
</assumption_delta_decision>

<objective>
앱 「검색」 탭의 목적지이자 웹 신규 페이지인 `/search`(스케치 005 B 채택안)를 만든다: 최근 검색 유틸 → 페이지 클라이언트(입력·타일·최근·미리보기·결과 카드) → 라우트 + 사이드바 링크 → e2e.

Purpose: D-06 의 5탭 중 「검색」이 갈 곳이 생긴다. 목업 게이트는 스케치 005 로 통과했다(D-07a — 수치 정본은 스케치 README 「결정」).
Output: 유틸 1 · 컴포넌트 1 · 라우트 1 · 사이드바 링크 · 단위 3 · e2e 1.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md
@.planning/sketches/005-search-page-me-account/README.md
@webapp/src/components/search/global-search.tsx
@webapp/src/hooks/use-debounced-search.ts
@webapp/src/lib/breakout-list.ts
@webapp/src/app/watchlist/page.tsx

<interfaces>
기존 계약(코드에서 추출):

```ts
// webapp/src/hooks/use-debounced-search.ts
export function useDebouncedSearch(query: string, delayMs = 300): { results: Stock[]; loading: boolean; error: Error | undefined };
// webapp/src/lib/stock-api.ts
export function searchStocks(q: string, signal: AbortSignal): Promise<StockDetailResponse[]>;   // Stock: code · name · market · price · changeRate …
// webapp/src/lib/scanner-api.ts
export async function fetchScannerStocks(state: ScannerState, signal: AbortSignal): Promise<{ stocks: StockWithProximity[]; lastUpdatedAt: string | null }>; // sort=rate_desc · limit 100
// webapp/src/lib/scanner-query.ts
export const DEFAULT_SCANNER_STATE: ScannerState;   // { market: 'ALL' }
// webapp/src/lib/theme-api.ts
export function fetchSystemThemes(): Promise<ThemeWithStats[]>;
// webapp/src/hooks/use-watchlist-set.tsx
export function useWatchlistSet(): { set: Set<string>; count: number; isAtLimit; optimisticAdd; optimisticRemove; refresh };
// webapp/src/lib/breakout-list.ts L35·L70-95 — `BREAKOUT_TONE_KEY = "gh-radar:breakout-tone"` · typeof window 가드 + try/catch 「저장 실패가 화면 동작을 막지 않는다」 + 파싱 후 타입 필터
// webapp/src/components/layout/app-sidebar.tsx
interface NavLeaf { href: string; label: string; icon: NavIcon }
const NAV_HOME: NavLeaf = { href: "/", label: "홈", icon: Home };            // L86 · lucide `Search` 는 이미 import 됨
const NAV_SEARCH_GROUP: NavLeaf[] = [ {"/scanner","상승률 상위",TrendingUp}, {"/themes","테마",Layers}, {"/watchlist","관심종목",Star} ];
// 렌더 L377-381: <li><NavLink item={NAV_HOME} active={isActive(NAV_HOME.href)} /></li> 다음 <GroupHeading label="종목검색" icon={Search} />
// global-search.tsx 문구: 「검색 중…」 · 「검색에 실패했습니다. 잠시 후 다시 시도해 주세요.」 · `"{query}" 에 해당하는 종목이 없습니다`
// 토큰: --card · --muted(= 스케치의 --raised) · --raised-2 · --muted-fg · --faint(= 스케치의 --faint-fg) · --up · --down · --up-bg · --down-bg · --primary
```

```ts
// 이 플랜이 새로 만드는 계약
export const RECENT_SEARCH_KEY = 'gh-radar:recent-search';
export const RECENT_SEARCH_MAX = 10;
export interface RecentSearchItem { code: string; name: string; market?: string; at: number }
export function readRecentSearches(): RecentSearchItem[];
export function pushRecentSearch(item: Omit<RecentSearchItem, 'at'>): RecentSearchItem[];
export function removeRecentSearch(code: string): RecentSearchItem[];
export function clearRecentSearches(): void;
export function SearchPageClient(): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 최근 검색 유틸 `recent-search.ts` (localStorage `gh-radar:recent-search` · 최대 10)</name>
  <files>webapp/src/lib/recent-search.ts, webapp/src/lib/__tests__/recent-search.test.ts</files>
  <read_first>
    - webapp/src/lib/breakout-list.ts (L30-40 키 규약 · L70-95 읽기/쓰기 가드)
    - webapp/src/lib/__tests__/breakout-list.test.ts (localStorage 테스트 방식)
    - .planning/sketches/005-search-page-me-account/README.md (「결정」 최근 검색 절)
  </read_first>
  <behavior>
    - 빈 저장소 → `readRecentSearches()` = `[]` · JSON 이 아니거나 배열이 아니면 `[]` · 원소 중 `code`/`name` 이 문자열이 아닌 것은 버린다
    - `pushRecentSearch({code:'005930',name:'삼성전자',market:'KOSPI'})` → 첫 원소가 그 종목, `at` 은 `Date.now()`
    - 같은 코드를 다시 push → 중복 없이 맨 앞으로 이동
    - 11개 push → 10개만 남고 가장 오래된 것이 빠진다
    - `removeRecentSearch('005930')` → 그 항목만 제거 · `clearRecentSearches()` → 키 삭제
    - `localStorage.setItem` 이 throw 해도 push 는 throw 하지 않고 메모리 결과를 돌려준다
  </behavior>
  <action>
    `webapp/src/lib/recent-search.ts` 를 `<interfaces>` 이름대로 만든다. 모든 함수는 `typeof window === 'undefined'` 가드와 try/catch(「저장 실패가 화면 동작을 막지 않는다」 — breakout-list 관례). 저장 형식은 `RecentSearchItem[]` JSON. 머리 주석: D-07a · 키 접두 `gh-radar:` 는 D-21 브랜드 변경에도 유지 · 종목 코드·이름만 저장(개인정보 아님). 테스트를 먼저 쓴다(RED → GREEN).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/__tests__/recent-search.test.ts</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or "recent-search.test.ts" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "export const RECENT_SEARCH_KEY = 'gh-radar:recent-search'" webapp/src/lib/recent-search.ts` 가 1줄 · `grep -n "export const RECENT_SEARCH_MAX = 10" webapp/src/lib/recent-search.ts` 가 1줄
  </acceptance_criteria>
  <done>최근 검색이 규약 키로 안전하게 저장·정리된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `SearchPageClient` + `/search` 라우트 + 사이드바 「검색」 링크 (D-07 · D-07a · D-04)</name>
  <files>webapp/src/components/search/search-page-client.tsx, webapp/src/components/search/__tests__/search-page-client.test.tsx, webapp/src/app/search/page.tsx, webapp/src/components/layout/app-sidebar.tsx, webapp/src/components/layout/__tests__/app-sidebar.test.tsx</files>
  <read_first>
    - .planning/sketches/005-search-page-me-account/README.md (「결정」 `/search` B 수치 전부)
    - .planning/sketches/005-search-page-me-account/index.html (B 변형 마크업 — 타일·최근·미리보기·결과 카드)
    - webapp/src/components/search/global-search.tsx (L33-100 상태 분기·문구)
    - webapp/src/components/search/__tests__/global-search.test.tsx (searchStocks 모킹 방식)
    - webapp/src/app/watchlist/page.tsx (라우트 조립 원형)
    - webapp/src/components/layout/app-sidebar.tsx (L80-100 NAV 상수 · L163-185 NavLink · L375-392 렌더)
    - webapp/src/components/layout/__tests__/app-sidebar.test.tsx (L272-340 트리 케이스 · 활성 토큰 케이스 L314)
    - webapp/src/lib/native/use-native-refresh.ts (21-04)
  </read_first>
  <behavior>
    - 초기(입력 빈칸): 제목 「검색」 · `placeholder="종목명 또는 코드"` 입력 · 타일 3개 링크 href `/scanner`·`/themes`·`/watchlist` · scanner 목 6종목(등락률 31·28·26·24·20·18) → 첫 타일 보조 「25%↑ 3종목」 · 테마 목 4개 → 「오늘 4개」 · 관심종목 count 7 → 「7종목」 · 미리보기 5행(순위 1~5) · 「더보기 ›」 href `/scanner`
    - 최근 검색이 비어 있으면 섹션 없음 · 2개 저장돼 있으면 두 행 + 「지우기」 · ✕ → 그 행 제거 · 「지우기」 → 섹션 사라짐
    - 「삼성」 입력 → ✕ 지우기 버튼 보임 · 타일·최근·미리보기 사라짐 · 디바운스 뒤 결과 행(이름·코드·시장 배지·현재가·등락률) · 결과 클릭 → `router.push('/stocks/005930')` + `readRecentSearches()[0].code === '005930'` · ✕ 클릭 → 입력 비고 허브 복귀
    - 로딩 「검색 중…」 · 오류 「검색에 실패했습니다. 잠시 후 다시 시도해 주세요.」 · 결과 0 `"xyz" 에 해당하는 종목이 없습니다`
    - scanner fetch 가 실패해도 페이지는 렌더되고 미리보기 자리에 조용한 안내(예: 「상승률 상위를 불러오지 못했어요」)만 뜬다 · 타일 보조는 「—」
    - 사이드바: 「홈」 다음 항목이 「검색」 링크(`/search`), `/search` 에서 `aria-current="page"` · 「종목검색」 그룹 3항목 유지
  </behavior>
  <action>
    ① `search-page-client.tsx`('use client') `SearchPageClient`: 루트 `mx-auto flex w-full max-w-[900px] flex-col gap-4`. 상태 `query`(controlled), `useDebouncedSearch(query, 300)`, `recent`(마운트 후 `readRecentSearches()` — 하이드레이션 일치), `preview`(scanner 결과 top 5 + `surgeCount` = `changeRate >= 25` 개수), `themeCount`, `useWatchlistSet().count`. 허브 데이터는 `loadHub` = `useCallback` 으로 `fetchScannerStocks(DEFAULT_SCANNER_STATE, controller.signal)` 과 `fetchSystemThemes()` 를 `Promise.allSettled` 로 1회 읽고 각각 실패는 그 칸만 「—」/안내(자동 폴링·타이머 없음), 언마운트 시 abort. `useNativeRefresh(loadHub)`(D-04). 입력: `<input type="search" inputMode="search" enterKeyHint="search" aria-label="종목 검색" placeholder="종목명 또는 코드">` 높이 48 · radius 14 · `bg-[var(--muted)]` · 좌 검색 아이콘 · 입력 중 우측 18 원형 ✕(`aria-label="검색어 지우기"`). `trimmed.length > 0` 이면 결과 카드만: 행 높이 52 · 종목명 15/600 · 코드 12.5 `--muted-fg` mono · 시장 배지 11/700 `bg-[var(--muted)]` · 우측 현재가 14/600(천 단위) + 등락률(`>0` `--up` · `<0` `--down` · 부호·소수 2자리). 문구·분기(로딩·오류·빈 결과)는 GlobalSearch 와 같은 문자열. 결과 선택 → `pushRecentSearch({ code, name, market })` → `router.push(\`/stocks/${code}\`)`. 입력 빈칸이면: 타일 3열(`grid grid-cols-3 gap-2`, 카드 `bg-[var(--card)] rounded-[16px] px-3 py-3.5`, 아이콘 36 radius 12 틴트 면 — 상승률 `bg-[var(--up-bg)] text-[var(--up)]` TrendingUp · 테마 `bg-[var(--down-bg)] text-[var(--primary)]` Layers · 관심종목 `bg-[rgba(250,204,21,.16)] text-[#eab308]` Star, 제목 14/700, 보조 12 `--muted-fg`) → 「최근 검색」 섹션(제목 15/600 muted + 우측 「지우기」 `text-[var(--primary)] text-[13px] font-semibold` 버튼, 카드 안 44 행: 시계 아이콘 16 `--faint` · 종목명 · 행 우측 ✕ `aria-label="{이름} 최근 검색에서 삭제"`, 행 클릭 → `/stocks/{code}`) → 「지금 상승률 상위」 섹션(제목 + 「더보기 ›」 `<Link href="/scanner">`, 44 행: 순위 13 muted · 종목명 14.5/600 · 우측 등락률 600 색, 행 → `/stocks/{code}`). 링크는 `next/link`. 레이아웃 유틸은 뷰포트 브레이크포인트만(셸 층 — §2.2b 컨테이너 밴드 무관).
    ② `webapp/src/app/search/page.tsx`: watchlist 페이지와 같은 모양 — `'use client'` · `<AppShell sidebar={<AppSidebar />}><SearchPageClient /></AppShell>`. 머리 주석: D-07 · middleware 기본 차단 규칙이 자동 적용(공개 경로 아님).
    ③ `app-sidebar.tsx`: `const NAV_SEARCH_PAGE: NavLeaf = { href: "/search", label: "검색", icon: Search };` 를 `NAV_HOME` 아래에 두고, 렌더에서 홈 `<li>` 바로 다음에 `<li><NavLink item={NAV_SEARCH_PAGE} active={isActive(NAV_SEARCH_PAGE.href)} /></li>` 를 넣는다. 그룹 3항목·순서·숨김 규칙 무변경.
    ④ 테스트를 먼저 쓴다(RED): `search-page-client.test.tsx`(`vi.mock('@/lib/stock-api')` `searchStocks` · `vi.mock('@/lib/scanner-api')` · `vi.mock('@/lib/theme-api')` · `vi.mock('@/hooks/use-watchlist-set')` · `next/navigation` 스파이 · 디바운스는 가짜 타이머) 로 `<behavior>` 전부 · `app-sidebar.test.tsx` 에 케이스 2개(「홈」 다음이 「검색」 · `/search` 활성 + 기존 활성 토큰 클래스).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/search src/components/layout/__tests__/app-sidebar.test.tsx src/lib/__tests__/recent-search.test.ts</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or "search-page-client.test.tsx" is absent from the output</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'placeholder="종목명 또는 코드"' webapp/src/components/search/search-page-client.tsx` 가 1줄
    - `grep -n "useNativeRefresh(loadHub)" webapp/src/components/search/search-page-client.tsx` 가 1줄
    - `grep -n "max-w-\[900px\]" webapp/src/components/search/search-page-client.tsx` 가 1줄
    - `grep -cE "setInterval|usePolling" webapp/src/components/search/search-page-client.tsx` 가 0 (자동 폴링 없음)
    - `grep -n 'NAV_SEARCH_PAGE: NavLeaf = { href: "/search", label: "검색"' webapp/src/components/layout/app-sidebar.tsx` 가 1줄
    - `test -f webapp/src/app/search/page.tsx` exit 0
  </acceptance_criteria>
  <done>`/search` 가 B 탐색 허브로 서고, 입력 중 결과·최근 검색·실데이터 카운트·미리보기가 동작하며, 사이드바에 「검색」이 있다.</done>
</task>

<task type="auto">
  <name>Task 3: e2e `search-page.spec.ts` — 허브 → 검색 → 이동 → 최근 검색 (MOBILE-01g)</name>
  <files>webapp/e2e/specs/search-page.spec.ts</files>
  <read_first>
    - webapp/e2e/specs/search.spec.ts (⌘K 검색 e2e — 파일명 충돌 금지 · 목 사용 방식)
    - webapp/e2e/fixtures/mock-api.ts (`mockStockApi` 검색·상세·scanner 라우트)
    - webapp/e2e/fixtures/themes.ts (`mockThemesApi` 목록 옵션)
    - webapp/e2e/fixtures/native-app.ts (21-05 — 앱 모드 케이스 1개에 사용)
  </read_first>
  <action>
    `webapp/e2e/specs/search-page.spec.ts` 를 만든다(머리 「무엇을 증명하는가」 목록). 준비: `mockStockApi(page, …)` 를 먼저 등록한 뒤 `page.route('**/api/scanner*', …)` 로 등락률 31·28·26·24·20·18 인 6종목을 돌려주는 목을 **나중에** 등록(Playwright 는 나중 등록 라우트가 우선) · `mockThemesApi(page, { list: <4개> })`. 케이스: ① `/search` 초기 — 제목 「검색」 · 타일 3개 href · 첫 타일 「25%↑ 3종목」 · 미리보기 5행 · 「더보기 ›」 href `/scanner` ② 입력 「삼성」 → 타일 숨김 · 결과 행 표시 → 클릭 → URL `/stocks/005930`(목 종목 코드) → `page.evaluate(() => localStorage.getItem('gh-radar:recent-search'))` 에 그 코드 포함 → `/search` 재방문 → 「최근 검색」 행 표시 → 「지우기」 → 섹션 사라짐 ③ 1280 폭 사이드바 「검색」 링크 `aria-current="page"` · 본문 폭 ≤ 900 ④ 앱 모드(`installNativeApp`) 390 폭에서 같은 페이지가 렌더되고 캡처 메시지에 `route {path:'/search'}` 가 있다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/search-page.spec.ts</automated>
    <fails_when>non-zero exit, or output contains "failed" or "No tests found", or lacks "passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "gh-radar:recent-search" webapp/e2e/specs/search-page.spec.ts` · `grep -n "installNativeApp" webapp/e2e/specs/search-page.spec.ts` 가 각각 1줄 이상
    - 기존 `search.spec.ts` 가 수정되지 않았다
  </acceptance_criteria>
  <done>`/search` 의 허브·검색·최근 검색·사이드바 링크·앱 모드 렌더가 실브라우저에서 잠겼다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 우리 API(`/api/stocks/search` · `/api/scanner` · `/api/themes`) | 기존 인증·검증 경로 재사용 — 새 엔드포인트 없음 |
| localStorage | 같은 출처 스크립트가 읽고 쓴다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-19 | Information Disclosure | 최근 검색 localStorage | low | accept | 종목 코드·이름만(개인정보·토큰 없음) · 기기 로컬 · 「지우기」 제공 |
| T-21-32 | Tampering | 조작된 localStorage 값 | low | mitigate | 파싱 후 타입 필터 · 문자열만 렌더(React 이스케이프) · 이동 경로는 `/stocks/${code}` 템플릿(코드는 문자열 필드) |
| T-21-33 | Denial of Service | `/search` 방문마다 scanner·테마 조회 | low | mitigate | 마운트 1회 + 제스처 재조회만 · 폴링 없음(acceptance grep) · 둘 다 우리 서버 캐시(외부 API 아님) |
| T-21-SC | Tampering | 패키지 설치 | low | accept | 설치 없음 |
</threat_model>

<verification>
- recent-search · search-page-client · app-sidebar 단위 green · typecheck green · search-page.spec green
</verification>

<success_criteria>
- D-07·D-07a 가 웹·앱 공통 페이지로 완성됐고 앱 「검색」 탭의 목적지가 생겼다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/` 패키지 `@gh-radar/mobile` — scripts `native:sync` · `native:sync:dev` · `native:build:ios` · `native:build:android` · `native:smoke:ios` · `native:open:ios` · `native:open:android` · `native:assets` (21-01) · `native:smoke:android` (21-02) · `native:check-tab-routes:ios` (21-10) · `native:test:android` (21-12) · `native:verify-prod` (21-16) | 패키지·스크립트 | 21-01 · 21-02 · 21-10 · 21-12 · 21-16 |
| `mobile/capacitor.config.ts` — appId `com.ghtrade.app` · appName `GH Trade` · `CAP_SERVER_URL` · `SystemBars` (21-01) · `SocialLogin` (21-15) | 설정 | 21-01 · 21-15 |
| `mobile/tsconfig.json` · `mobile/.gitignore` | 설정 | 21-01 |
| `mobile/www/index.html` — 오프라인 폴백(최소 21-01 · `safeTarget`·5초 탐침·`online`·테마 21-11) | 정적 페이지 | 21-01 · 21-11 |
| `mobile/scripts/smoke-ios.sh` · `mobile/scripts/smoke-android.sh` — 「SMOKE OK ready platform=…」 | 스모크 | 21-01 · 21-02 |
| `mobile/scripts/check-tab-routes-ios.sh` · `mobile/scripts/tab-routes-check.swift` | Swift 경로표 검사 | 21-10 |
| `mobile/scripts/render-resources.mjs` · `mobile/resources/{icon.svg,icon-only.png,icon-foreground.png,icon-background.png,splash.png,splash-dark.png}` | 에셋 원본 | 21-14 |
| `mobile/scripts/verify-prod-config.mjs` · `mobile/README.md` | 운영 설정 검사·문서 | 21-16 |
| iOS `SceneDelegate.swift` (루트 = `GHTradeBridgeViewController()` · 창 배경 21-11) | Swift | 21-01 · 21-11 |
| iOS `GHTradeBridgeViewController` · `WeakScriptMessageHandler` · `NativeMessageType` | Swift | 21-01 · 21-10 · 21-11 |
| iOS `GHTradeTabBar` · `GHTradeTab` · `GHTabID` · `GHTradePalette` · `GHTradeTheme` · `TabRoutes.activeTab(forPath:)` · `TabRoutes.hidesTabBar(path:)` | Swift | 21-10 |
| iOS `NavigationDelegateProxy` · `ThemeStore` (UserDefaults `gh-trade.theme`) | Swift | 21-11 |
| Android `MainActivity` (`BridgeActivity`) · `GhTradeWebViewClient` (`BridgeWebViewClient` 상속) · `GhTradeBridge` (`@JavascriptInterface postMessage`) | Kotlin | 21-02 · 21-12 · 21-13 |
| Android `GhTradeTabBar` · `GhTradePalette` · `TabId` · `TabRoutes.activeTabFor` · `TabRoutes.hidesTabBar` · `TabRoutesTest` | Kotlin | 21-12 |
| Android `ThemeStore` (SharedPreferences `gh_trade`/`theme`) · `res/values/bools.xml` · `res/values-sw600dp/bools.xml` (`is_tablet`) | Kotlin·리소스 | 21-13 |
| Android `res/drawable/ic_tab_{home,home_fill,search,search_fill,trading,trading_fill,ai,ai_fill,me,me_fill}.xml` | 벡터 | 21-12 |
| 웹 `lib/native/native-detect.ts` — `NATIVE_DETECT_SCRIPT` · `isNativeApp` · `nativePlatform` | 유틸 | 21-01 |
| 웹 `lib/native/post-native.ts` — `postNative` · `NativeMsgType` | 유틸 | 21-04 |
| 웹 `lib/native/native-bridge-provider.tsx` — `NativeBridgeProvider` · `useNativeBridge` · `NativeRefreshFn` · `window.__ghTrade = { refresh, navigate, back }` | Provider | 21-04 |
| 웹 `lib/native/use-native-refresh.ts` — `useNativeRefresh` | 훅 | 21-04 |
| 웹 `lib/native/native-overlay-marker.tsx` — `NativeOverlayMarker` | 컴포넌트 | 21-04 |
| 웹 `lib/native/google-client-ids.ts` — `GOOGLE_WEB_CLIENT_ID` · `GOOGLE_IOS_CLIENT_ID` · `GOOGLE_ANDROID_CLIENT_ID` · `GOOGLE_IOS_URL_SCHEME` | 공개 식별자 상수 | 21-03 |
| 웹 `lib/native/native-google-login.ts` — `nativeGoogleSignIn` · `randomNonce` · `sha256Hex` · `classifyNativeLoginError` | 서비스 | 21-15 |
| 웹 `RelayConnectionState.probeNow` · `EMPTY_RELAY_VALUE.probeNow` | 훅 계약 | 21-07 |
| 웹 `lib/recent-search.ts` — `RECENT_SEARCH_KEY` · `RECENT_SEARCH_MAX` · `RecentSearchItem` · `readRecentSearches` · `pushRecentSearch` · `removeRecentSearch` · `clearRecentSearches` | 유틸 | 21-08 |
| 웹 `app/search/page.tsx` · `components/search/search-page-client.tsx` — `SearchPageClient` · 사이드바 `NAV_SEARCH_PAGE` | 페이지·상수 | 21-08 |
| 웹 `components/me/account-card.tsx` — `AccountCard` | 컴포넌트 | 21-09 |
| 웹 마크업 계약 `data-slot="app-aside"` · `data-slot="app-menu-button"` · `data-slot="stock-ai-button"` · `data-slot="shared-panels"` · `data-pinned` · `data-order-cta` · `data-slot="account-card"` | 마크업 | 21-05 · 21-06 · 21-09 |
| CSS `@custom-variant native` (21-04) · 앱 셸 규칙 (21-05) · `--app-safe-top` · `--app-safe-bottom` · `--app-safe-left` · `--app-safe-right` · `--native-tabbar-gap` · `--native-tabbar-offset` · `--native-body-reserve` (21-06) | 토큰·변형 | 21-04 · 21-05 · 21-06 |
| 웹 `app/icon.svg` (= 스케치 004 `#app-a`) | 파비콘 | 21-14 |
| 단위 테스트: `native-detect.test.ts` (21-01) · `post-native.test.ts` · `native-bridge-provider.test.tsx` · `native-overlay.test.tsx` (21-04) · `google-client-ids.test.ts` (21-03) · `relay-socket.test.ts` probeNow · `stock-native-refresh.test.tsx` (21-07) · `recent-search.test.ts` · `search-page-client.test.tsx` · `app-sidebar.test.tsx` 보강 (21-08) · `account-card.test.tsx` · `app-shell-chrome.test.tsx` 보강 (21-09) · `native-google-login.test.ts` · `login/__tests__/page.test.tsx` (21-15) · `TabRoutesTest.kt` (21-12) | 단위 | 각 플랜 |
| e2e: `fixtures/native-app.ts` — `installNativeApp` · `nativeMessages` (21-05) · `specs/native-shell.spec.ts` (21-05 · 21-06) · `specs/search-page.spec.ts` (21-08) · `specs/brand-account.spec.ts` (21-09) | Playwright | 각 플랜 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-08-SUMMARY.md` when done
</output>
