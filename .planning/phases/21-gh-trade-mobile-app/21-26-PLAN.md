---
phase: 21-gh-trade-mobile-app
plan: 26
type: execute
wave: 12
depends_on: ["21-24"]
files_modified:
  - webapp/src/lib/numpad.ts
  - webapp/src/lib/__tests__/numpad.test.ts
  - webapp/src/components/trading/lc/__tests__/number-pad-sheet.test.tsx
  - .planning/phases/20-toss-order-ticket/20-CONTEXT.md
  - webapp/src/components/trading/lc/setting-group.tsx
  - webapp/src/components/trading/lc/__tests__/setting-group.test.tsx
  - webapp/src/components/trading/card/card-header.tsx
  - webapp/src/components/trading/__tests__/card-header.test.tsx
  - webapp/src/components/trading/card/stock-info-modal.tsx
  - webapp/src/components/trading/__tests__/stock-info-modal.test.tsx
autonomous: true
gap_closure: true
requirements: [MOBILE-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G-21-R3-3: 주문금액(만원 단위) 키패드 칩이 「천만 · 오천만 · 1억 · 지우기」이고, 각각 현재 값에 1,000 · 5,000 · 10,000(만원)을 더한다(예: 100 에 천만 → 1100) — 9자리 상한은 그대로"
    - "G-21-R3-5: 제목 없는 가격 섹션 카드(매수가격·주문금액 / 매도 가격)의 상단 패딩이 pt-1 이라 위 여백이 아래와 대칭이다 — 제목 있는 그룹은 pt-2.5 그대로"
    - "G-21-R3-6: 트레이딩 종목카드 닫기(✕)가 32×32 상자 + 16px 아이콘 + 44×44 히트 영역이고, 헤더 줄 높이 · 760 이상 order-last · 전파 차단 · 접근 이름 「{종목명} 카드 닫기」 · title 「카드 제거」가 그대로다"
    - "G-21-R3-7: 뷰포트 700 이상에서 종목정보 팝업 높이가 탭과 로딩 상태에 관계없이 고정(min(720px, 100dvh − 48px − 안전영역 위·아래))이고 본문 스크롤바 자리가 예약된다 · ✕ 는 20 아이콘 · 32 상자 · 히트 44 · 폰 전체화면(700 미만)에서 헤더 위 · 본문 아래에 --app-safe-top/bottom 패딩이 붙어 노치·홈 인디케이터에 가리지 않는다(브라우저는 0)"
  artifacts:
    - path: webapp/src/lib/numpad.ts
      provides: "만원 칩 천만/오천만/1억/지우기"
      contains: "오천만"
    - path: webapp/src/components/trading/lc/setting-group.tsx
      provides: "제목 없는 그룹 pt-1"
      contains: "pt-1"
    - path: webapp/src/components/trading/card/card-header.tsx
      provides: "✕ size-8 + XIcon + after:-inset-1.5"
      contains: "card-close"
    - path: webapp/src/components/trading/card/stock-info-modal.tsx
      provides: "고정 높이 · scrollbar-gutter · safe-area 패딩 · XIcon ✕ 44"
      contains: "scrollbar-gutter:stable"
  key_links:
    - from: webapp/src/lib/numpad.ts
      to: webapp/src/components/trading/lc/number-pad-sheet.tsx
      via: "PAD_CHIPS['만원'] → 시트 칩 줄 렌더 → applyPadChip add"
      pattern: "PAD_CHIPS"
    - from: webapp/src/components/trading/card/stock-info-modal.tsx
      to: webapp/src/styles/globals.css
      via: "--app-safe-top · --app-safe-bottom (§21 — Android 주입 우선 · iOS env() 폴백 · 브라우저 0)"
      pattern: "--app-safe-top"
  prohibitions:
    - statement: "MUST NOT 만원 칩의 단위를 바꾼다 — 칩 값은 만원 단위 정수(1000 = 1,000만원)로 버퍼에 더해지고, 와이어(원) 변환 경로는 손대지 않는다"
      status: unverified
      verification: "Task 1 단위 테스트 「100 + 천만 = 1100」 · lc-fields/limit-chaser 변환 파일 git diff 없음"
    - statement: "MUST NOT ✕ 를 키우면서 카드 닫기 확인 다이얼로그 경로나 헤더 클릭 전파 차단을 없앤다"
      status: unverified
      verification: "card-header.test 「전파 차단」 단언 통과"
    - statement: "MUST NOT 팝업을 카드 컨테이너 안에 렌더한다(layout containment 로 fixed 가 갇힌다 — body 포털 유지)"
      status: unverified
      verification: "stock-info-modal.test 기존 포털 단언 통과"
---

<objective>
UAT 3차 트레이딩 시각 결함 네 건 — G-21-R3-3(주문금액 키패드 칩 천만/오천만/1억) · G-21-R3-5(가격 섹션 카드 위 여백 중첩) · G-21-R3-6(카드 ✕ 크기·히트) · G-21-R3-7(종목정보 팝업 크기 고정 · ✕ · 앱 safe-area). 모두 21-UAT.md 진단의 권장 수정을 그대로 적용한다(R3-3 사용자 확정 · R3-5/6/7 은 손보는 표면 안 시각 결함 — 목업 없이 구현 후 UAT 확인).

Purpose: 큰 금액을 한 번에 올리고, 가격 카드가 다른 카드와 같은 리듬으로 서며, 카드·팝업 ✕ 가 손가락으로 눌리고, 팝업이 탭마다 출렁이거나 노치에 가리지 않는다.
Output: numpad 칩 · SettingGroup 패딩 분기 · 카드 ✕ · 팝업 크기/✕/safe-area · 단위 테스트 · 20-CONTEXT D-17 포인터. 팝업 안 전체목록(G-21-R3-8 팝업 쪽)은 21-30 이 같은 파일을 이어 받는다(웨이브 13).

검증은 단위 테스트(클래스·동작)로 잠그고 실제 치수·체감은 21-36 UAT 에서 본다(새 e2e 를 만들지 않는다 — 과설계 금지).

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add(`git add -A` 금지 — 작업 트리에 다른 세션 변경이 있다) · 커밋 직전 `git status -sb` · **push 금지**(21-36 체크포인트).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-UAT.md
@webapp/src/lib/numpad.ts
@webapp/src/components/trading/lc/setting-group.tsx
@webapp/src/components/trading/card/card-header.tsx
@webapp/src/components/trading/card/stock-info-modal.tsx

<interfaces>
```text
numpad.ts: type PadChip = { label; ariaLabel?; op: {kind:'add', n} | {kind:'set', n} | {kind:'clear'} | ... }
  PAD_CHIPS: Record<PadUnit, readonly PadChip[]> · 만원 줄 ≈91행 · add(n) 헬퍼는 라벨을 「+{n}」 로 만든다 → 새 칩은 객체를 직접 적는다
  PAD_MAX_DIGITS = 9 · applyPadChip(state, chip, ctx)
SettingGroup(setting-group.tsx ≈274~): section className 「min-w-0 rounded-[16px] bg-[var(--group-bg)] px-2.5 pt-2.5 pb-1」 · spec.title 있을 때만 헤더
card-header.tsx ≈275~287: ✕ — h-[26px] px-2 text-[11px] · 글리프 ✕ · title 「카드 제거」 · aria-label 「{name} 카드 닫기」 · e.stopPropagation() · @min-[760px]/lc:order-last
stock-info-modal.tsx: DialogContent className(≈125~130: 폰 inset-0 h-dvh · ≥700 h-auto max-h-[calc(100dvh-48px)] w-[min(960px,calc(100%-32px))]) · DialogHeader(≈132 px-3.5 py-2.5) · ✕(≈143~152 h-8 w-8 글리프 · closeRef · aria-label 닫기) · 본문 data-slot stock-info-modal-body(≈189 min-h-0 flex-1 overflow-y-auto px-3.5 py-3)
참고: lucide XIcon(ui/dialog.tsx 가 이미 import) · 모달 닫기 h-8 · Button sm = h-8
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — 만원 칩 천만/오천만/1억 → applyPadChip 더하기 → 키패드 시트 칩 줄 (G-21-R3-3)</name>
  <files>webapp/src/lib/numpad.ts, webapp/src/lib/__tests__/numpad.test.ts, webapp/src/components/trading/lc/__tests__/number-pad-sheet.test.tsx, .planning/phases/20-toss-order-ticket/20-CONTEXT.md</files>
  <read_first>
    - webapp/src/lib/numpad.ts (70~110행 — CLEAR · add · PAD_CHIPS · applyPadChip 상한)
    - webapp/src/lib/__tests__/numpad.test.ts (85~160행 — 라벨 표 · 만원 더하기 · 상한 케이스)
    - webapp/src/components/trading/lc/__tests__/number-pad-sheet.test.tsx (칩 줄 렌더 단언 모양)
    - .planning/phases/20-toss-order-ticket/20-CONTEXT.md (D-17 줄)
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-R3-3)
  </read_first>
  <action>
    ① `PAD_CHIPS` 만원 줄을 객체 네 개로 바꾼다(per G-21-R3-3 사용자 결정): `{ label: '천만', ariaLabel: '1,000만원 더하기', op: { kind: 'add', n: 1_000 } }` · `{ label: '오천만', ariaLabel: '5,000만원 더하기', op: { kind: 'add', n: 5_000 } }` · `{ label: '1억', ariaLabel: '1억원 더하기', op: { kind: 'add', n: 10_000 } }` · `CLEAR`. 더하기 동작과 9자리 상한은 그대로. 줄 위 주석 한 줄 「만원 = 천만 · 오천만 · 1억(값은 만원 단위 · G-21-R3-3)」.
    ② `numpad.test.ts`: 만원 라벨 기대값을 `["천만", "오천만", "1억", "지우기"]` 로, 더하기 케이스를 「천만 on 100 → 1100」 · 「1억 on 0 → 10000」 · 「오천만 on 999999000 → 999999999(상한)」로 바꾸고 aria 이름 세 개를 단언한다.
    ③ `number-pad-sheet.test.tsx`: 만원 시트 칩 버튼 이름 네 개(「1,000만원 더하기」 · 「5,000만원 더하기」 · 「1억원 더하기」 · 「전부 지우기」)와 값 100 에서 「1,000만원 더하기」 → 표시 1,100 케이스 하나(기존 렌더 헬퍼 재사용).
    ④ `20-CONTEXT.md` D-17 줄 **끝에** 포인터만 덧붙인다: 「→ 2026-09-26 (21-UAT G-21-R3-3) 금액(만원) 칩 = 천만(+1,000) · 오천만(+5,000) · 1억(+10,000) · 지우기 — 더하기 유지」. 원문은 고치지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/__tests__/numpad.test.ts src/components/trading/lc/__tests__/number-pad-sheet.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or reports "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "label: '천만'" webapp/src/lib/numpad.ts` ≥ 1 · `grep -c "label: '오천만'" webapp/src/lib/numpad.ts` ≥ 1 · `grep -c "label: '1억'" webapp/src/lib/numpad.ts` ≥ 1
    - `grep -c "n: 10_000 }" webapp/src/lib/numpad.ts` ≥ 1 · `grep -c "1100" webapp/src/lib/__tests__/numpad.test.ts` ≥ 1
    - 20-CONTEXT 원문 보존 + 포인터: `grep -c "금액(만원): +10 · +50 · +100" .planning/phases/20-toss-order-ticket/20-CONTEXT.md` ≥ 1 · `grep -c "G-21-R3-3" .planning/phases/20-toss-order-ticket/20-CONTEXT.md` ≥ 1
    - verify 명령 통과
  </acceptance_criteria>
  <done>만원 키패드에서 천만/오천만/1억이 만원 단위 1,000/5,000/10,000 을 더하고 스크린리더 이름을 갖는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 가격 섹션 pt-1 (G-21-R3-5) · 카드 ✕ size-8 + XIcon + 히트 44 (G-21-R3-6)</name>
  <files>webapp/src/components/trading/lc/setting-group.tsx, webapp/src/components/trading/lc/__tests__/setting-group.test.tsx, webapp/src/components/trading/card/card-header.tsx, webapp/src/components/trading/__tests__/card-header.test.tsx</files>
  <read_first>
    - webapp/src/components/trading/lc/setting-group.tsx (255~300행 — SettingGroup · 머리 주석)
    - webapp/src/components/trading/lc/__tests__/setting-group.test.tsx (165~195행 — 패딩 단언 · 가격 섹션 케이스)
    - webapp/src/components/trading/card/card-header.tsx (머리 주석 · 270~290 ✕ 버튼)
    - webapp/src/components/trading/__tests__/card-header.test.tsx (175~300 — ✕ 이름 · title · 순서 · 전파 차단)
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-R3-5 · G-21-R3-6 root_cause · missing)
  </read_first>
  <behavior>
    - 제목 있는 그룹: section 클래스에 pt-2.5 · px-2.5 · pb-1 (기존 단언 유지)
    - 제목 없는 가격 그룹(buy-price · sell-price): section 클래스 토큰에 pt-1 이 있고 pt-2.5 는 없다
    - ✕ 는 data-slot="card-close" 버튼 · aria-hidden svg 하나 · textContent 빈 문자열 · 클래스에 size-8 · after:-inset-1.5 · @min-[760px]/lc:order-last
    - ✕ 접근 이름 「{종목명} 카드 닫기」 · title 「카드 제거」 · 클릭 시 onToggle 미호출(전파 차단) 유지
  </behavior>
  <action>
    ① `SettingGroup` section `className` 을 `cn('min-w-0 rounded-[16px] bg-[var(--group-bg)] px-2.5 pb-1', spec.title ? 'pt-2.5' : 'pt-1')` 로(per G-21-R3-5 진단 — 제목 없는 그룹은 첫 행 44px items-center 가 위 여백을 이미 가진다). 머리 주석 한 줄 「상단 패딩은 제목 유무로 가른다(G-21-R3-5)」. 테스트: 가격 섹션 케이스에 `className.split(/\s+/)` 가 `pt-1` 포함 · `pt-2.5` 미포함 단언.
    ② 카드 ✕ (per G-21-R3-6 진단): `data-slot="card-close"` · 클래스 `relative inline-flex size-8 -my-[3px] flex-none items-center justify-center rounded-[var(--r)] text-[var(--muted-fg)] hover:bg-[var(--muted)] hover:text-[var(--fg)] after:absolute after:-inset-1.5 after:content-[''] @min-[760px]/lc:order-last` · 자식 = lucide `XIcon`(`aria-hidden="true"` · `className="size-4"`, 텍스트 글리프 제거). `-my-[3px]` 로 헤더 줄 높이 26 유지 · 히트 32 + 12 = 44. `type` · `title` · `aria-label` · `e.stopPropagation()` · `onClose()` 그대로. 머리 주석의 ✕ 설명을 「32 상자 · 16 아이콘 · 히트 44(G-21-R3-6)」 로. 테스트: behavior 의 ✕ 두 줄 단언 추가(기존 단언 통과 유지).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/lc/__tests__/setting-group.test.tsx src/components/trading/__tests__/card-header.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "spec.title ? 'pt-2.5' : 'pt-1'" webapp/src/components/trading/lc/setting-group.tsx` ≥ 1 · `grep -c "'pt-1'" webapp/src/components/trading/lc/__tests__/setting-group.test.tsx` ≥ 1
    - `grep -c 'data-slot="card-close"' webapp/src/components/trading/card/card-header.tsx` ≥ 1 · `grep -c "after:-inset-1.5" webapp/src/components/trading/card/card-header.tsx` ≥ 1 · `grep -c "XIcon" webapp/src/components/trading/card/card-header.tsx` ≥ 1
    - 옛 작은 상자 제거(주석 제외): `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*|\{/\*)' webapp/src/components/trading/card/card-header.tsx | grep -c 'h-\[26px\] flex-none items-center rounded')" -eq 0`
    - `grep -c 'title="카드 제거"' webapp/src/components/trading/card/card-header.tsx` ≥ 1 · `grep -c "stopPropagation" webapp/src/components/trading/card/card-header.tsx` ≥ 1
    - verify 명령 통과
  </acceptance_criteria>
  <done>가격 카드 위 여백이 대칭이고, 카드 ✕ 가 32 상자 · 44 히트로 눌리며 헤더 줄은 그대로다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 종목정보 팝업 — 고정 높이 · scrollbar-gutter · 폰 safe-area 패딩 · ✕ XIcon 44 (G-21-R3-7)</name>
  <files>webapp/src/components/trading/card/stock-info-modal.tsx, webapp/src/components/trading/__tests__/stock-info-modal.test.tsx</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-R3-7 root_cause · missing)
    - webapp/src/components/trading/card/stock-info-modal.tsx (전체 — 머리 주석 ⑤ · DialogContent · DialogHeader · ✕ · 본문)
    - webapp/src/components/trading/__tests__/stock-info-modal.test.tsx (이름 · 포커스 · 포털 단언)
    - webapp/src/styles/globals.css (§21 --app-safe-top · --app-safe-bottom 정의)
  </read_first>
  <behavior>
    - DialogContent 클래스에 `min-[700px]:h-[min(720px,calc(100dvh-48px-var(--app-safe-top)-var(--app-safe-bottom)))]` 가 있고 `min-[700px]:h-auto` 가 없다
    - 본문(stock-info-modal-body) 클래스에 `[scrollbar-gutter:stable]` · `pb-[calc(12px+var(--app-safe-bottom))]` · `min-[700px]:pb-3`
    - 헤더 클래스에 `pt-[calc(10px+var(--app-safe-top))]` · `min-[700px]:pt-2.5`
    - ✕ 는 svg 아이콘 · textContent 빈 문자열 · size-8 · after:-inset-1.5 · aria-label 「닫기」 · 첫 포커스 유지
  </behavior>
  <action>
    per G-21-R3-7 진단: ① ≥700 의 `min-[700px]:h-auto` · `min-[700px]:max-h-[calc(100dvh-48px)]` 를 `min-[700px]:h-[min(720px,calc(100dvh-48px-var(--app-safe-top)-var(--app-safe-bottom)))]` 하나로(폭 규칙 그대로). 본문에 `[scrollbar-gutter:stable]`. ② 폰 전체화면(<700): DialogHeader 세로 패딩을 `pt-[calc(10px+var(--app-safe-top))] pb-2.5 min-[700px]:pt-2.5`, 본문 아래 패딩을 `pb-[calc(12px+var(--app-safe-bottom))] min-[700px]:pb-3` — 브라우저 안전영역 0 이면 종전과 같다. ③ ✕: 글리프를 lucide `XIcon`(`aria-hidden="true"` · `className="size-5"`)으로, 버튼 클래스 `relative inline-flex size-8 flex-none items-center justify-center rounded-[var(--r)] text-[var(--muted-fg)] hover:bg-[var(--muted)] hover:text-[var(--fg)] after:absolute after:-inset-1.5 after:content-['']`. `ref={closeRef}` · `aria-label="닫기"` · `DialogClose asChild` 그대로. ④ 머리 주석 ⑤ 를 「폰 전체화면(안전영역 패딩) · ≥700 고정 높이 · 본문 스크롤 · scrollbar-gutter 로 폭 고정(G-21-R3-7)」 로. ⑤ 단위 테스트에 behavior 네 줄을 단언한다(기존 테스트 통과 유지). 손보는 표면 안에서 다른 줄바꿈·잘림이 보이면 고치고 SUMMARY 에 한 줄 보고.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/__tests__/stock-info-modal.test.tsx && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "min-\[700px\]:h-\[min(720px" webapp/src/components/trading/card/stock-info-modal.tsx` ≥ 1 · `grep -c "scrollbar-gutter:stable" webapp/src/components/trading/card/stock-info-modal.tsx` ≥ 1 · `grep -c "var(--app-safe-top)" webapp/src/components/trading/card/stock-info-modal.tsx` ≥ 1
    - 가변 높이 제거(주석 제외): `test "$(grep -v -E '^[[:space:]]*(//|\*|/\*|\{/\*)' webapp/src/components/trading/card/stock-info-modal.tsx | grep -c 'min-\[700px\]:h-auto')" -eq 0`
    - `grep -c "XIcon" webapp/src/components/trading/card/stock-info-modal.tsx` ≥ 1 · `grep -c "after:-inset-1.5" webapp/src/components/trading/card/stock-info-modal.tsx` ≥ 1
    - verify 명령 통과
  </acceptance_criteria>
  <done>팝업이 탭과 무관하게 같은 크기로 서고, ✕ 가 크게 눌리며, 앱 폰에서 노치·홈 인디케이터에 가리지 않는다(체감은 21-36 UAT).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 키패드 칩 → 주문 금액 버퍼 → relay 와이어(원) | 만원 버퍼 값이 원으로 바뀌어 실주문 금액이 된다 |
| 카드·팝업 ✕ → 닫기 | 히트 영역이 커지면 오터치가 늘 수 있다 · 가려지면 탈출 불가 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-59 | Tampering | 만원 칩 값(단위 착오 = 금액 1만 배) | high | mitigate | 칩 값은 만원 정수(1000/5000/10000)로 버퍼에 더함 · 단위 테스트 100 + 천만 = 1100 · 9자리 상한 테스트 · 원 변환 경로 무변경(Task 1) |
| T-21-60 | Denial of Service | 카드 ✕ 히트 44 오터치로 카드 닫힘 | low | mitigate | 등록 카드 · 판정 불가 카드는 기존 확인 다이얼로그 · 헤더 전파 차단 유지(Task 2 테스트) |
| T-21-80 | Denial of Service | 팝업 ✕ 가 노치 밑에 가려 탈출 불가 | medium | mitigate | 헤더 pt = 10 + --app-safe-top(Task 3) · Esc · 배경 클릭 경로 유지 |
</threat_model>

<verification>
- vitest 세 묶음(numpad · number-pad-sheet / setting-group · card-header / stock-info-modal) · typecheck
- 사람 확인(칩 모양 · 가격 카드 여백 · ✕ 크기 · 팝업 세 탭 높이 · 앱 노치)은 21-36 UAT
</verification>

<success_criteria>
- G-21-R3-3 · R3-5 · R3-6 · R3-7 이 진단의 권장 수정대로 구현되고 테스트로 잠겼다.
</success_criteria>

## Flagged assumptions

- **엣지 탐침 MOBILE-01 (unclassified · unresolved):** 경계는 칩 상한(999,999,999) · 제목 유무 두 분기 · 팝업 700 경계로 명시했다. 나머지는 21-36 UAT.
- jsdom 은 레이아웃이 없어 치수는 클래스로 잠근다 — 실제 여백 대칭 · 32/44 · 세 탭 높이 동일은 UAT 에서 본다. 720 상한은 진단 제안값이다.

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| 스케치 `.planning/sketches/008-uat3-surfaces/`(index.html · README.md) · MANIFEST 008 행 | 목업 | 21-25 |
| CONTEXT D-12a · D-12b · D-25a · D-29 · D-30 · D-31 · D-32(R3-11 축소: 스크롤 복원 + 스켈레톤 없음 · 상태 초기화 허용) · WR-03 androidx.webkit 선언 답 | 결정 기록 | 21-25 |
| `PAD_CHIPS.만원` = 천만(+1,000) · 오천만(+5,000) · 1억(+10,000) · 지우기 · 20-CONTEXT D-17 포인터 | 상수 · 결정 | 21-26 |
| `SettingGroup` 제목 없는 그룹 상단 `pt-1` | 클래스 | 21-26 |
| 카드 헤더 ✕ `data-slot="card-close"` · 종목정보 팝업 ✕ = `size-8` + lucide `XIcon` + `after:-inset-1.5`(히트 44) · 팝업 고정 높이 · `scrollbar-gutter:stable` · 폰 safe-area 패딩 | 컴포넌트 | 21-26 |
| `webapp/src/lib/safe-path.ts` `isSafeInternalPath` · `recent-search.ts` `RECENT_CODE_RE` · `PopoverContent` 안 `NativeOverlayMarker` · `useDebouncedSearch` 반환 `resultsQuery` · `viewport.themeColor` `#17171c` + `ThemeColorSync` | 함수 · 훅 · 메타 | 21-27 |
| iOS 키보드 사유 즉시 숨김 · 재표시 90ms · `awaitingContent`(didStartProvisionalNavigation → 첫 route/1.5초) · 초기 숨김 · 브리지 출처 scheme·host·port 비교 · 오프라인 `dev=1` · 스모크 기기 지정(`IOS_DEVICE_UDID` · `ANDROID_SERIAL`) · mobile/README 명령표 | Swift · 스크립트 · 문서 | 21-28 |
| Android IME 애니메이션 콜백 즉시 숨김 · `awaitingContent`(onPageStarted) · `pullBlocked` 초기화 · 오프라인 뒤로가기(재현 시) · 오프라인 `dev=1` · 폴백 페이지 dev=1 일 때만 localhost · allowBackup=false + `res/xml/data_extraction_rules.xml` · 브리지 WebMessageListener(21-25 답 a) | Kotlin · XML · HTML | 21-29 |
| `NewsFullList` · `DiscussionFullList` · `useNewsView` · `StockNewsTabPanel` · 섹션 `onShowAll` · 종목상세 `?tab=news&view=news\|discussions` · 옛 URL 리다이렉트 · 팝업 `newsView` | 컴포넌트 · 훅 · 라우트 | 21-30 |
| `useTabRootScrollMemory`(`webapp/src/lib/tab-scroll-memory.ts` · AppShell 에서 호출) · 기존 `lib/query-cache` 재사용: 검색 허브 · 대화 목록 · 오늘 주문 | 훅 · 캐시 | 21-31 |
| 앱 공용 패널 숨김 CSS · 카드 더티 바 탭바 비킴(프로브) · `StrategyLogFeedProvider` · `useStrategyLogFeed` · `diffLimitChasers` · /me 전략 로그(`me-strategy-log`) | CSS · Provider · 컴포넌트 | 21-32 |
| 작업대 `?code=` 소비(카드 보장·펼침·포커스) · 카드 수동주문 주문유형(지정가/시간외종가) | 컴포넌트 | 21-33 |
| 종목상세 「트레이딩」 CTA(폰 바 + `detail-trading-button`) · 호가주문 탭 제거 · `?tab=orderbook` → `/trading?code=` · 호가 탭 전용 코드·`variant` 삭제 · orderbook.spec 선별 이전 | 컴포넌트 · 정리 · e2e | 21-34 |
| 게이트 기록 · `21-REVIEW-R2.md`(13건 대조) · UAT 환경(21-35-SUMMARY) · UAT 3차 재검증 · push 결정(21-36-SUMMARY) | 문서 | 21-35 · 21-36 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-26-SUMMARY.md` when done — 칩 표 · 패딩 분기 · ✕ 치수 · 팝업 크기 식 · 테스트 결과.
</output>
