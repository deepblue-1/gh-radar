---
phase: 21-gh-trade-mobile-app
plan: 31
type: execute
wave: 13
depends_on: ["21-27"]
files_modified:
  - webapp/src/lib/tab-scroll-memory.ts
  - webapp/src/lib/__tests__/tab-scroll-memory.test.tsx
  - webapp/src/components/layout/app-shell.tsx
  - webapp/e2e/specs/home.spec.ts
  - webapp/src/components/search/search-page-client.tsx
  - webapp/src/components/search/__tests__/search-page-client.test.tsx
  - webapp/src/components/chat/conversation-list.tsx
  - webapp/src/components/chat/__tests__/conversation-list.test.tsx
  - webapp/src/components/trading/today-orders-card.tsx
  - webapp/src/components/trading/__tests__/today-orders-card.test.tsx
autonomous: true
gap_closure: true
requirements: [MOBILE-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "G-21-R3-11 (D-32 · 2026-09-26 축소): 탭 루트(`/` · `/search` · `/trading` · `/chat` · `/me`)에서 스크롤한 뒤 다른 탭에 갔다 돌아오면 그 탭 루트의 창 스크롤 위치가 복원된다"
    - "G-21-R3-11 (D-32): 돌아온 탭 루트는 마지막으로 받은 내용을 스켈레톤·로딩 문구 없이 바로 보여 주고 뒤에서 조용히 갱신한다 — 홈(기존 캐시) · 검색 허브(상승률 상위 미리보기 · 테마 수) · AI 대화 목록 · 마이 오늘 주문"
    - "검색 파라미터를 달고 들어온 탭 루트(`/trading?code=` 등)는 복원하지 않는다 — 목적지 스크롤(카드 reveal)이 이긴다"
    - "같은 탭 재탭 = 맨 위(D-06a)는 그대로다 · 탭 루트가 아닌 화면(종목상세 등)은 이 규칙의 영향을 받지 않는다 · 입력·펼침 상태는 재마운트로 초기화돼도 된다(사용자 수용)"
  artifacts:
    - path: webapp/src/lib/tab-scroll-memory.ts
      provides: "TAB_ROOTS · useTabRootScrollMemory() — 루트별 window.scrollY 기록·복원"
      contains: "export function useTabRootScrollMemory"
    - path: webapp/src/components/layout/app-shell.tsx
      provides: "모든 탭 루트 페이지가 쓰는 셸에서 훅 호출"
      contains: "useTabRootScrollMemory"
  key_links:
    - from: webapp/src/components/layout/app-shell.tsx
      to: webapp/src/lib/tab-scroll-memory.ts
      via: "useTabRootScrollMemory() — usePathname 이 탭 루트일 때만 동작"
      pattern: "useTabRootScrollMemory"
    - from: webapp/src/components/search/search-page-client.tsx
      to: webapp/src/lib/query-cache.ts
      via: "허브 슬롯 초기값 = readQueryCache · loadHub 성공 시 writeQueryCache"
      pattern: "readQueryCache"
  prohibitions:
    - statement: "MUST NOT 새 데이터 라이브러리(react-query · swr 등)나 화면 보존(React Activity · keep-alive · 숨긴 DOM 유지)을 도입한다 — 기존 lib/query-cache 재사용(D-32)"
      status: unverified
      verification: "Task 2 acceptance — package.json dependencies diff 없음 · Activity 심볼 grep 0"
    - statement: "MUST NOT 사용자별 캐시 값을 로그인 사용자 전환 뒤에 보여 준다"
      status: unverified
      verification: "기존 AuthProvider 의 clearQueryCache(사용자 id 변경) 경로 재사용 — 새 캐시 키는 전부 query-cache 저장소"
---

<objective>
G-21-R3-11 — 2026-09-26 사용자 단순안(D-32): 탭 루트를 오갈 때 **창 스크롤 위치 복원 + 마지막으로 받은 내용을 스켈레톤 없이 바로 보여 준 뒤 조용히 갱신**. 화면 재마운트와 입력·펼침 초기화는 허용한다. keep-alive · 스파이크 · 결정 체크포인트는 없다(사용자: 과설계 금지).

Purpose: 앱 탭바로 홈 ↔ 검색 ↔ 트레이딩을 오가도 보던 자리와 내용이 그대로 보여 탭 전환이 가볍다.
Output: `lib/tab-scroll-memory.ts`(AppShell 에서 호출) · 기존 `lib/query-cache` 로 검색 허브 · 대화 목록 · 오늘 주문 시드 · 단위 테스트 · e2e 한 개(home.spec).

플래너 코드 확인(요청대로 스파이크 없이): 스크롤 주체는 **창(window)** 이다 — AppShell 바깥 div 가 `flex min-h-dvh flex-col` 이고 `<main className="min-w-0 flex-1 overflow-auto …">` 는 높이 제한이 없어 스스로 스크롤하지 않는다(shared-panels.tsx ⑤-b 18-13 실측과 같다). 같은 탭 재탭은 native-bridge-provider `navigate()` 의 `window.scrollTo({top:0, behavior:'smooth'})` 가 이미 처리한다. 다섯 탭 루트 페이지(`app/page.tsx` · `search` · `trading` · `chat` · `me`)는 모두 `<AppShell>` 을 쓴다. 홈은 `use-home-query` 가 이미 query-cache 를 쓰고, 트레이딩은 앱 수준 RelayProvider 상태를 읽어 재마운트해도 내용이 남는다.

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add(`git add -A` 금지) · 커밋 직전 `git status -sb` · **push 금지**(21-36).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-UAT.md
@webapp/src/lib/query-cache.ts
@webapp/src/components/layout/app-shell.tsx
@webapp/src/lib/native/native-bridge-provider.tsx
@webapp/src/components/search/search-page-client.tsx
@webapp/src/components/chat/conversation-list.tsx
@webapp/src/components/trading/today-orders-card.tsx

<interfaces>
```text
query-cache.ts: QUERY_CACHE_MAX_AGE_MS = 5분 · readQueryCache<T>(key, maxAgeMs?) · writeQueryCache<T>(key, data) · clearQueryCache() (AuthProvider 가 사용자 id 변경 시 호출 — auth-context.tsx:67)
  소비처 관례: use-themes-query.ts:74 `const [cached] = useState(() => readQueryCache<T>(KEY))` → 초기 상태 · 성공 시 writeQueryCache
native-bridge-provider navigate(path): 같은 경로면 window.scrollTo({top:0, behavior:'smooth'}) · 아니면 router.push(path)
search-page-client.tsx ≈101~124: Slot<T> = loading | ok | error · scanner/themeCount 초기 {status:'loading'} · loadHub(Promise.allSettled) · settle(prev, result) · useNativeRefresh(loadHub)
conversation-list.tsx ≈61~100: conversations 초기 [] · stockFilter(ALL) · useEffect([stockFilter, refreshKey]) → listConversations(code) → updatedAt 역정렬 · ALL 이면 stockOptions 갱신
today-orders-card.tsx ≈181~205: restored: JournalOrderRow[] | null (null = 로딩 → 「오늘 주문을 불러오는 중이에요…」) · load() = fetchTodayOrders · 날짜 = kstDateIso()
e2e: fixtures/native-app.ts installNativeApp · window.__ghTrade.navigate(path) = 실제 탭 탭 경로 · home.spec 목 API 준비 모양
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — useTabRootScrollMemory(루트별 window.scrollY 기록·복원) → AppShell 호출 → 단위 테스트 · e2e(홈 스크롤 → 검색 → 홈 = 위치 복원 · 스켈레톤 없음 · 재탭 = 맨 위)</name>
  <files>webapp/src/lib/tab-scroll-memory.ts, webapp/src/lib/__tests__/tab-scroll-memory.test.tsx, webapp/src/components/layout/app-shell.tsx, webapp/e2e/specs/home.spec.ts</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-R3-11 — decision 줄 2026-09-26 수정본)
    - webapp/src/components/layout/app-shell.tsx (전체 — 'use client' · 머리 주석)
    - webapp/src/lib/native/native-bridge-provider.tsx (160~180 navigate)
    - webapp/e2e/fixtures/native-app.ts · webapp/e2e/specs/home.spec.ts (목 API 준비 · 첫 테스트 모양)
    - webapp/src/components/home/home-skeleton.tsx (스켈레톤 testid/클래스 — e2e 부재 단언용)
  </read_first>
  <behavior>
    - pathname `/` 에서 scrollY 600 으로 scroll 이벤트 → 언마운트 → pathname `/` 로 다시 마운트 → 다음 프레임에 window.scrollTo(0, 600)
    - pathname `/search` 의 기록은 `/` 에 섞이지 않는다(루트별 따로)
    - 탭 루트가 아닌 pathname(`/stocks/005930`)에서는 기록도 복원도 하지 않는다
    - 저장값 없음 또는 0 이면 scrollTo 를 부르지 않는다(Next 기본 동작 그대로)
    - 탭 루트에 검색 파라미터를 달고 들어오면(`/trading?code=…` · `?focus=…` 같은 목적지 착지) 복원하지 않는다
  </behavior>
  <action>
    per D-32: ① `webapp/src/lib/tab-scroll-memory.ts`('use client' 훅 모듈): `export const TAB_ROOTS = ['/', '/search', '/trading', '/chat', '/me'] as const` · 모듈 수준 `Map<string, number>` · `export function useTabRootScrollMemory(): void` — `usePathname()` 이 TAB_ROOTS 에 없으면 아무것도 하지 않는다. 있으면 effect 에서 (a) 저장값 y 를 먼저 읽고 (b) passive `scroll` 리스너를 달아 `window.location.pathname === root` 일 때만 `window.scrollY` 를 기록한다(다른 경로로 넘어가는 동안의 스크롤 · 클램프는 버린다) (c) y > 0 이고 `window.location.search` 가 비어 있으면(검색 파라미터 착지 = 목적지가 따로 있다 — 21-33 `?code=` reveal · `?focus=`) `requestAnimationFrame` 으로 `window.scrollTo(0, y)` 를 시도하고, 문서 높이가 모자라면(`scrollHeight - innerHeight < y`) 최대 30 프레임까지 다시 시도한 뒤 가능한 만큼만 내린다 · 정리 시 리스너와 대기 프레임을 해제한다. 머리 주석: 「D-32 — 탭 루트 스크롤 복원(창이 스크롤 주체 · main 은 높이 제한 없음). 재탭 = 맨 위는 navigate() 가 처리(D-06a). keep-alive 아님 — 화면은 재마운트되고 내용은 query-cache 가 바로 채운다」.
    ② `app-shell.tsx`: 컴포넌트 첫머리에서 `useTabRootScrollMemory()` 를 부른다(모든 탭 루트 페이지가 AppShell 을 쓴다 · 비 루트 페이지는 no-op). 머리 주석 목록에 한 줄.
    ③ 단위 테스트(`tab-scroll-memory.test.tsx`): `next/navigation` `usePathname` 을 mock 하고 `window.scrollTo` 스파이 · `scrollY` 정의 · rAF 는 fake timers 로 진행해 behavior 다섯 케이스를 단언한다.
    ④ e2e(home.spec.ts) 「G-21-R3-11 탭 루트 스크롤 복원」 하나: 390×844 · installNativeApp · 홈 목 데이터로 `/` → 콘텐츠가 뜬 뒤 `window.scrollTo(0, 600)` → `__ghTrade.navigate('/search')` → 검색 화면 확인 → `__ghTrade.navigate('/')` → `window.scrollY` 가 600±4 로 수렴(expect.poll) · 홈 스켈레톤이 보이지 않는다 → `__ghTrade.navigate('/')`(재탭) → scrollY 가 0 으로 수렴.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/lib/__tests__/tab-scroll-memory.test.tsx src/components/layout/__tests__/app-shell-chrome.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or reports "No test files found"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/home.spec.ts -g "G-21-R3-11" && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or no summary line matches "[0-9]+ passed" (a -g filter that matches nothing prints "No tests found"), or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function useTabRootScrollMemory" webapp/src/lib/tab-scroll-memory.ts` ≥ 1 · `grep -c "export const TAB_ROOTS" webapp/src/lib/tab-scroll-memory.ts` ≥ 1 · `grep -c "window.location.pathname" webapp/src/lib/tab-scroll-memory.ts` ≥ 1
    - `grep -c "useTabRootScrollMemory()" webapp/src/components/layout/app-shell.tsx` ≥ 1
    - `grep -c "G-21-R3-11" webapp/e2e/specs/home.spec.ts` ≥ 1
    - verify 두 명령 통과
  </acceptance_criteria>
  <done>탭 루트를 떠났다 돌아오면 보던 스크롤 위치로 돌아오고, 같은 탭 재탭은 여전히 맨 위로 간다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 스켈레톤 없는 재방문 — 검색 허브 · 대화 목록 · 오늘 주문을 기존 query-cache 로 시드하고 조용히 갱신 (D-32)</name>
  <files>webapp/src/components/search/search-page-client.tsx, webapp/src/components/search/__tests__/search-page-client.test.tsx, webapp/src/components/chat/conversation-list.tsx, webapp/src/components/chat/__tests__/conversation-list.test.tsx, webapp/src/components/trading/today-orders-card.tsx, webapp/src/components/trading/__tests__/today-orders-card.test.tsx</files>
  <read_first>
    - webapp/src/lib/query-cache.ts · webapp/src/hooks/use-themes-query.ts (70~110 — 시드·쓰기 관례)
    - webapp/src/components/search/search-page-client.tsx (100~125 허브 · 21-27 결과)
    - webapp/src/components/chat/conversation-list.tsx (55~110)
    - webapp/src/components/trading/today-orders-card.tsx (178~210 · 310~325 로딩 문구)
    - 세 컴포넌트의 테스트 파일(모듈 mock · 렌더 헬퍼 모양)
  </read_first>
  <behavior>
    - 검색 허브: 첫 마운트에서 loadHub 가 성공하면 캐시에 쓰고, 언마운트 뒤 다시 마운트하면 첫 렌더부터 미리보기 행 · 테마 수가 보이며 로딩 스켈레톤이 없다(요청은 다시 나간다)
    - 대화 목록: 필터별 키로 캐시 · 재마운트 첫 렌더에 이전 목록이 보이고 요청 뒤 교체된다
    - 오늘 주문: 오늘 날짜 키로 캐시 · 재마운트 첫 렌더에 이전 행이 보이고 「오늘 주문을 불러오는 중이에요…」 가 없다 · 날짜가 다르면 캐시를 쓰지 않는다
    - 캐시가 없거나 5분이 지났으면 종전 로딩 표시 그대로
  </behavior>
  <action>
    per D-32(기존 `lib/query-cache` 재사용 · 새 라이브러리 없음 · use-themes-query 관례):
    ① `search-page-client.tsx`: 키 `search:hub` 에 `{ scanner: ScannerPreview; themeCount: number }` 를 저장한다 — `useState(() => readQueryCache(...))` 로 두 슬롯 초기값을 `{status:'ok', value}` 로 시드하고(없으면 loading), `loadHub` 가 둘 다 fulfilled 일 때 `writeQueryCache`. 에러·부분 실패 처리(settle)는 그대로.
    ② `conversation-list.tsx`: 키 `chat:conversations:${stockFilter}` — 초기 `conversations` 를 ALL 키 캐시로 시드하고, 필터가 바뀌면 그 키 캐시가 있으면 먼저 보여 준다. 정렬된 결과를 쓴다. ALL 이면 `stockOptions` 도 캐시 값에서 시드.
    ③ `today-orders-card.tsx`: 키 `me:today-orders:${kstDateIso()}` — `restored` 초기값을 캐시로 시드(없으면 null), `load` 성공 시 쓴다(실패 시 쓰지 않는다 · 기존 failed 처리 그대로).
    ④ 세 테스트 파일에 behavior 를 단언한다(테스트 사이 `clearQueryCache()` 로 격리). 새 의존성 · 화면 보존 코드를 넣지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/search/__tests__/search-page-client.test.tsx src/components/chat/__tests__/conversation-list.test.tsx src/components/trading/__tests__/today-orders-card.test.tsx && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "readQueryCache" webapp/src/components/search/search-page-client.tsx` ≥ 1 · `grep -c "writeQueryCache" webapp/src/components/search/search-page-client.tsx` ≥ 1
    - `grep -c "chat:conversations:" webapp/src/components/chat/conversation-list.tsx` ≥ 1 · `grep -c "me:today-orders:" webapp/src/components/trading/today-orders-card.tsx` ≥ 1
    - 새 의존성 없음(이 플랜 커밋 기준): `L="$(git log --grep='(21-31)' --name-only --format=)" && test "$(printf '%s\n' "$L" | grep -c 'package.json$\|pnpm-lock.yaml$')" -eq 0`
    - 화면 보존 없음: `test "$(grep -rl 'react.activity\|unstable_Activity' webapp/src | wc -l | tr -d ' ')" -eq 0`
    - verify 명령 통과
  </acceptance_criteria>
  <done>검색 허브 · 대화 목록 · 오늘 주문이 돌아올 때 스켈레톤 없이 이전 내용으로 바로 서고 뒤에서 갱신된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 메모리 캐시 → 화면 | 이전 응답(사용자별 대화 · 주문)이 다음 렌더에 그대로 보인다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-85 | Information Disclosure | 사용자 전환 뒤 이전 사용자 대화·주문 표시 | medium | mitigate | 같은 query-cache 저장소 → AuthProvider 가 사용자 id 변경 시 clearQueryCache(기존) · 5분 만료 |
| T-21-86 | Tampering | 낡은 주문 목록이 오늘 것처럼 보임 | low | mitigate | 키에 KST 날짜 · 조용한 재요청이 곧 교체(Task 2) |
| T-21-87 | Denial of Service | 복원 루프가 스크롤을 붙잡음 | low | mitigate | 최대 30 프레임 · 정리 시 해제(Task 1) |
</threat_model>

<verification>
- vitest(tab-scroll-memory · app-shell-chrome · search-page-client · conversation-list · today-orders-card) · Playwright home.spec `-g "G-21-R3-11"` · typecheck
- 사람 확인(iOS/Android 탭바로 다섯 탭을 오가며 위치·내용 유지 체감)은 21-36 UAT
</verification>

<success_criteria>
- G-21-R3-11 이 D-32 축소 truth(스크롤 복원 + 스켈레톤 없는 재방문 · 재탭 = 맨 위)대로 닫혔다.
</success_criteria>

## Flagged assumptions

- **엣지 탐침 MOBILE-01 (unclassified · unresolved):** 경계는 루트 목록 · 비 루트 no-op · 30 프레임 상한으로 명시했다. 나머지는 21-36 UAT.
- Next App Router 는 이동 때 URL 을 먼저 바꾸고 스크롤을 맨 위로 옮긴다고 본다 — 리스너가 `location.pathname` 을 확인하므로 순서가 달라도 이전 루트 값이 0 으로 덮이지 않는다. e2e 가 실제 순서를 잠근다.

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| 스케치 `.planning/sketches/008-uat3-surfaces/`(index.html · README.md) · MANIFEST 008 행 | 목업 | 21-25 |
| CONTEXT D-12a · D-12b · D-25a · D-29 · D-30 · D-31 · D-32(R3-11 축소: 스크롤 복원 + 스켈레톤 없음 · 상태 초기화 허용) · WR-03 androidx.webkit 선언 답 | 결정 기록 | 21-25 |
| `PAD_CHIPS.만원` = 천만(+1,000) · 오천만(+5,000) · 1억(+10,000) · 지우기 · 20-CONTEXT D-17 포인터 | 상수 · 결정 | 21-26 |
| `SettingGroup` 제목 없는 그룹 상단 `pt-1` | 클래스 | 21-26 |
| 카드 헤더 ✕ `data-slot="card-close"` · 종목정보 팝업 ✕ = `size-8` + lucide `XIcon` + `after:-inset-1.5`(히트 44) · 팝업 고정 높이 · `scrollbar-gutter:stable` · 폰 safe-area 패딩 | 컴포넌트 | 21-26 |
| `webapp/src/lib/safe-path.ts` `isSafeInternalPath` · `recent-search.ts` `RECENT_CODE_RE` · `PopoverContent` 안 `NativeOverlayMarker` · `useDebouncedSearch` 반환 `resultsQuery` · `viewport.themeColor` `#17171c` + `ThemeColorSync` | 함수 · 훅 · 메타 | 21-27 |
| iOS 키보드 사유 즉시 숨김 · 재표시 90ms · `awaitingContent`(didStartProvisionalNavigation → 첫 route/1.5초) · 초기 숨김 · 브리지 출처 scheme·host·port 비교 · 오프라인 `dev=1` · 스모크 기기 지정(`IOS_DEVICE_UDID` · `ANDROID_SERIAL`) · mobile/README 명령표 | Swift · 스크립트 · 문서 | 21-28 |
| Android IME 애니메이션 콜백 즉시 숨김 · `awaitingContent`(onPageStarted) · `pullBlocked` 초기화 · 오프라인 뒤로가기(재현 시) · 오프라인 `dev=1` · 폴백 페이지 dev=1 일 때만 localhost · allowBackup=false + `res/xml/data_extraction_rules.xml` · 브리지 WebMessageListener(21-25 답 a) | Kotlin · XML · HTML | 21-29 |
| `NewsFullList` · `DiscussionFullList` · `useNewsView` · `StockNewsTabPanel` · 섹션 `onShowAll` · 종목상세 `?tab=news&view=news\|discussions` · 옛 URL 리다이렉트 · 팝업 `newsView` | 컴포넌트 · 훅 · 라우트 | 21-30 |
| `useTabRootScrollMemory`(`webapp/src/lib/tab-scroll-memory.ts` · AppShell 에서 호출) · 기존 `lib/query-cache` 재사용: 검색 허브 · 대화 목록 · 오늘 주문 | 훅 · 캐시 | 21-31 |
| 앱 공용 패널 숨김 CSS · 카드 더티 바 탭바 비킴(프로브) · `StrategyLogFeedProvider` · `useStrategyLogFeed` · `diffLimitChasers` · /me 전략 로그(`me-strategy-log`) | CSS · Provider · 컴포넌트 | 21-32 |
| 작업대 `?code=` 소비(카드 보장·펼침·포커스) · 카드 수동주문 주문유형(지정가/시간외종가) | 컴포넌트 | 21-33 |
| 종목상세 「트레이딩」 CTA(폰 바 + `detail-trading-button`) · 호가주문 탭 제거 · `?tab=orderbook` → `/trading?code=` · 호가 탭 전용 코드·`variant` 삭제 · orderbook.spec 선별 이전 | 컴포넌트 · 정리 · e2e | 21-34 |
| 게이트 기록 · `21-REVIEW-R2.md`(13건 대조) · UAT 환경(21-35-SUMMARY) · UAT 3차 재검증 · push 결정(21-36-SUMMARY) | 문서 | 21-35 · 21-36 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-31-SUMMARY.md` when done — 훅 동작 · 캐시 키 표 · 테스트 · e2e 결과.
</output>
