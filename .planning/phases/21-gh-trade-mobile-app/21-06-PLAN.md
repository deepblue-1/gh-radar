---
phase: 21-gh-trade-mobile-app
plan: 06
type: execute
wave: 4
depends_on: ["21-05"]
files_modified:
  - webapp/src/app/layout.tsx
  - webapp/src/styles/globals.css
  - webapp/src/components/layout/app-header.tsx
  - webapp/src/components/layout/app-shell.tsx
  - webapp/src/components/trading/card/strategy-card.tsx
  - webapp/src/components/trading/workbench/shared-panels.tsx
  - webapp/src/components/stock/stock-detail-tabs.tsx
  - webapp/src/components/trading/workbench/alert-toasts.tsx
  - webapp/src/components/chat/chat-fab.tsx
  - webapp/src/components/trading/lc/number-pad-sheet.tsx
  - webapp/e2e/specs/native-shell.spec.ts
autonomous: true
requirements: [MOBILE-01]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-25: `viewport` 가 `viewportFit: 'cover'` 를 가진다(웹·앱 공통 — Claude's Discretion) · safe-area 값은 `--app-safe-{top,bottom,left,right}` = `var(--safe-area-inset-*, env(safe-area-inset-*, 0px))`(Android SystemBars 주입 우선, iOS·Safari env 폴백) 하나로만 읽는다"
    - "D-25: 헤더는 `h-14` 높이를 유지한 채 상단 safe-area 만큼 위 여백이 붙고(상태바 뒤까지 헤더 블러가 이어지는 풀블리드), 셸 좌우는 safe-area 만큼 들어온다 — 크롬(안전영역 0)에서 헤더·본문 패딩 램프 8/16/24 와 셸 불변식(home.spec)이 그대로다"
    - "D-25 · D-27a: 앱(`html.native-app`)에서 `--native-tabbar-gap` = max(safe-bottom − 14, 14) · `--native-tabbar-offset` = gap + 70 + 8 · 본문 `main` 하단 여백 = 108px(D-27a 폰·iPad 공통) — 브라우저에서는 offset 0 · 본문 여백 종전"
    - "D-25(리서치 반영): 앱에서 하단 고정 바 전부(더티 바 · /trading 폰 하단 패널 · 종목상세 「주문하기」 CTA · ≥700 알림 토스트)가 `--native-tabbar-offset` 만큼 탭바 위로 올라가고 자기 safe-area 하단 패딩을 버린다 · FAB 없는 앱에서 더티 바 우측 여백 0"
    - "D-25: 브라우저에서도 하단 고정 요소(더티 바 · 폰 하단 패널 · CTA · 챗 FAB · 알림 토스트 · 바텀/사이드 시트 · 키패드 시트)가 `--app-safe-bottom`(폰 알림 토스트는 `--app-safe-top`)만큼 비켜 선다"
    - "§2.2b: safe-area 분기는 셸·고정 요소 층에만 있고 `/trading` 컨테이너 쿼리 밴드 규칙에는 들어가지 않는다"
  artifacts:
    - path: webapp/src/styles/globals.css
      provides: "--app-safe-* · --native-tabbar-gap · --native-tabbar-offset · --native-body-reserve · 앱/웹 하단 고정 요소 규칙"
      contains: "--native-tabbar-offset"
    - path: webapp/src/app/layout.tsx
      provides: "viewportFit: 'cover'"
      contains: "viewportFit: 'cover'"
    - path: webapp/e2e/specs/native-shell.spec.ts
      provides: "MOBILE-01j 2부 — 탭바 여백·CTA 위치 계산 스타일 단언"
  key_links:
    - from: webapp/src/components/trading/workbench/shared-panels.tsx
      to: webapp/src/styles/globals.css
      via: "reserveStyle bottom = calc(var(--native-tabbar-offset, 0px) + reserve px)"
      pattern: "native-tabbar-offset"
    - from: webapp/src/components/stock/stock-detail-tabs.tsx
      to: webapp/src/styles/globals.css
      via: "data-order-cta 속성 → html.native-app [data-order-cta=\"true\"] 예약 76px"
      pattern: "data-order-cta"
---

<assumption_delta_decision>
- 검출기 `assumption-delta scan 21` → `detected: false`. 수동 판정: 두 번째 클라이언트 플랫폼 — 정본 명사 **웹앱**, 결정 **add-alongside**. safe-area·탭바 여백은 CSS 변수 하나(`--native-tabbar-offset`)로 곁에 붙고 브라우저에서는 0 으로 떨어진다(21-01 블록 참조).
</assumption_delta_decision>

<objective>
D-25 를 끝낸다: `viewport-fit=cover` + safe-area 변수 체계 + 앱 전용 탭바 여백(`--native-tabbar-offset`, 본문 108) + 하단 고정 요소 전부의 이동. 브라우저 셸 불변식과 §2.2b 밴드는 그대로 둔다.

Purpose: 네이티브 탭바(높이 70 · 바닥 20)가 웹의 「주문하기」 CTA·/trading 하단 패널·더티 바를 가리면 앱에서 주문을 못 한다(RESEARCH Pitfall 10). 풀블리드(D-25)라 헤더도 상태바 밑으로 들어가야 한다.
Output: CSS 토큰·규칙 한 구역 + 셸·고정 요소 컴포넌트의 최소 수정 + e2e 단언.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-CONTEXT.md
@.planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md
@.planning/phases/21-gh-trade-mobile-app/21-PATTERNS.md
@.planning/sketches/004-native-tab-bar/README.md
@webapp/src/styles/globals.css
@webapp/src/components/layout/app-header.tsx
@webapp/src/components/layout/app-shell.tsx

<interfaces>
기존 계약(코드에서 추출):

```text
layout.tsx viewport = { width:'device-width', initialScale:1, maximumScale:1, userScalable:false, themeColor:[…] }   (주석 quick-260925-ptw 유지)
app-header.tsx L46 header: "sticky top-0 z-10 flex h-14 items-center gap-3 border-b border-transparent bg-[color-mix(in_oklch,var(--bg)_88%,transparent)] px-2 backdrop-blur-md md:px-4 lg:px-6"
  ★ app-shell-chrome.test.tsx 가 header className 에 `h-14` · `px-2` · `md:px-4` · `lg:px-6` 가 있고 맨몸 `px-6` 이 없음을 단언한다 — 유지
app-shell.tsx L57 wrapper: "flex min-h-dvh flex-col bg-[var(--bg)] text-[var(--fg)]" · L63 aside: "… lg:sticky lg:top-14 lg:block lg:h-[calc(100dvh-3.5rem)] …" · L81 main: "min-w-0 flex-1 overflow-auto bg-[var(--surface)] p-2 md:p-4 lg:p-6"
strategy-card.tsx L777: "scroll-mt-16"  (헤더 56 밑에 카드 머리가 묻히지 않게)
dirty-action-bar.tsx L103-110: data-slot="dirty-action-bar" · "fixed inset-x-0 bottom-0 z-40 …" · "py-[var(--s-2)] px-[var(--s-4)]" · FAB 표면에서는 CHAT_FAB_CLEARANCE_CLASS = right-[calc(var(--chat-fab-w,0px)_+_32px)]
shared-panels.tsx L226-252: reserveStyle = reserve===null ? undefined : pinned ? { bottom: reserve } : { bottom: reserve, marginBottom: reserve } · data-testid="shared-panels" · pinned → "fixed inset-x-0 bottom-0 z-20 …" / 아니면 "sticky bottom-0 z-20 …" + @min-[700px]/wb:static
stock-detail-tabs.tsx L131 Tabs 루트: cn('flex-col gap-0', showOrderCta && 'max-md:pb-[calc(76px+max(20px,env(safe-area-inset-bottom)))]') · L217-219 CTA 바: data-slot="detail-order-cta-bar" "fixed inset-x-0 bottom-0 z-30 … pt-2.5 pb-[max(20px,env(safe-area-inset-bottom))] md:hidden"
alert-toasts.tsx L58: "pointer-events-none fixed right-3 bottom-3 z-50 … max-[699px]:top-3 max-[699px]:right-3 max-[699px]:bottom-auto max-[699px]:left-3 max-[699px]:w-auto"
chat-fab.tsx L118: "fixed right-6 bottom-6 z-40 … native:hidden"(21-05)
number-pad-sheet.tsx L241: "fixed inset-x-0 mx-auto bottom-[max(10px,env(safe-area-inset-bottom))] z-50"
ui/sheet.tsx SheetContent: data-slot="sheet-content" data-side={side} · 사이드 시트 "inset-y-0 … h-full" · 바텀 "inset-x-0 bottom-0 h-auto border-t"
globals.css L646-652: @media (max-width: 767.98px) { body:has([data-slot="detail-order-cta-bar"]) [data-slot="chat-fab"] { bottom: calc(70px + max(20px, env(safe-area-inset-bottom, 0px))); } }
```

탭바 기하(D-27a · 스케치 004 결정): 높이 70 · 바닥 = 화면 끝에서 max(inset − 14, 14) · 본문 하단 여백 108(= 70 + 20 + 18).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 토대 — `viewport-fit=cover` · `--app-safe-*` / `--native-tabbar-*` 변수 · 앱 본문 108 · 헤더 상단·셸 좌우 safe-area</name>
  <files>webapp/src/app/layout.tsx, webapp/src/styles/globals.css, webapp/src/components/layout/app-header.tsx, webapp/src/components/layout/app-shell.tsx, webapp/src/components/trading/card/strategy-card.tsx</files>
  <read_first>
    - webapp/src/app/layout.tsx (viewport 주석 quick-260925-ptw)
    - webapp/src/styles/globals.css (L1-10 · `:root` 토큰 블록 위치 · L612-660 레이어 밖 규칙 · §2.2b 주석)
    - webapp/src/components/layout/app-header.tsx (L36-60 header 주석 — 램프·잉크 보정)
    - webapp/src/components/layout/app-shell.tsx (L25-40 sticky 3종 묶음 주석 · L57-81)
    - webapp/src/components/layout/__tests__/app-shell-chrome.test.tsx (L64-120 — `h-14` · 램프 단언)
    - webapp/src/components/trading/card/strategy-card.tsx (L770-780 `scroll-mt-16`)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pitfall 8·10·14 · Pattern 9 SystemBars `insetsHandling:'css'`)
    - .planning/sketches/004-native-tab-bar/README.md (「결정」 — 바닥 20 · 본문 108)
  </read_first>
  <action>
    ① `layout.tsx` viewport 에 `viewportFit: 'cover'` 를 더하고 기존 주석 아래에 한 단락: D-25 풀블리드 · 웹·앱 공통(Claude's Discretion — 앱 전용 런타임 meta 교체 기각) · 가로 노치(Pitfall 14)는 셸 좌우 safe-area 패딩이 막는다 · 안전영역 값은 `--app-safe-*` 로만 읽는다.
    ② `globals.css` 에 레이어 밖 새 구역 「§21 safe-area · 네이티브 탭바 여백(D-25 · D-27a)」을 `main:has([data-page-surface="plain"])` 규칙 앞에 만든다. 내용: `:root { --app-safe-top: var(--safe-area-inset-top, env(safe-area-inset-top, 0px)); --app-safe-bottom: var(--safe-area-inset-bottom, env(safe-area-inset-bottom, 0px)); --app-safe-left: var(--safe-area-inset-left, env(safe-area-inset-left, 0px)); --app-safe-right: var(--safe-area-inset-right, env(safe-area-inset-right, 0px)); --native-tabbar-offset: 0px; --native-body-reserve: 0px; }` · `html.native-app { --native-tabbar-gap: max(calc(var(--app-safe-bottom) - 14px), 14px); --native-tabbar-offset: calc(var(--native-tabbar-gap) + 70px + 8px); --native-body-reserve: 108px; }` · `html.native-app main { padding-bottom: var(--native-body-reserve); }`. 주석: `--safe-area-inset-*` 는 Capacitor 8.5 SystemBars 가 Android 에서 `<html>` 인라인으로 주입하는 이름이라 **우리가 정의하지 않는다**(정의하면 주입값을 가린다) · 탭바 기하의 정본은 CONTEXT D-27a(바닥 = 화면 끝에서 max(inset−14, 14) · 높이 70 · 여유 8 · 본문 108) · 뷰포트/셸 층 규칙이라 §2.2b 컨테이너 밴드와 무관.
    ③ `app-header.tsx` header className 에 `box-content pt-[var(--app-safe-top)]` 를 더한다 — `h-14`(콘텐츠 56)와 램프 `px-2 md:px-4 lg:px-6` 는 그대로 두고, content-box 라 전체 높이 = 56 + 상단 안전영역이 된다(단위 테스트의 `h-14` 계약 유지). 주석 한 줄(D-25 · 크롬에서는 0).
    ④ `app-shell.tsx`: 바깥 래퍼 div 에 `pl-[var(--app-safe-left)] pr-[var(--app-safe-right)]` 를 더한다(헤더·본문 양쪽이 같은 양만큼 들어와 셸 불변식 「헤더 좌우 패딩 == 본문 좌우 패딩」이 유지된다 · 크롬 0). aside 의 `lg:top-14` → `lg:top-[calc(3.5rem+var(--app-safe-top))]`, `lg:h-[calc(100dvh-3.5rem)]` → `lg:h-[calc(100dvh-3.5rem-var(--app-safe-top))]`. `main` 패딩 램프·`min-w-0`·`overflow-auto` 는 건드리지 않는다(sticky 3종 묶음 주석 유지).
    ⑤ `strategy-card.tsx` 의 `scroll-mt-16` → `scroll-mt-[calc(4rem+var(--app-safe-top))]`(앱 풀블리드에서 헤더가 56 + 상태바라 카드 머리가 묻히지 않게 — 기존 주석에 D-25 한 줄 추가).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/layout/__tests__/app-shell-chrome.test.tsx src/components/layout/__tests__/app-sidebar.test.tsx</automated>
    <fails_when>non-zero exit, or the summary contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "viewportFit: 'cover'" webapp/src/app/layout.tsx` 가 1줄
    - `grep -n -- "--app-safe-bottom: var(--safe-area-inset-bottom, env(safe-area-inset-bottom, 0px));" webapp/src/styles/globals.css` 가 1줄
    - `grep -n -- "--native-tabbar-offset: calc(var(--native-tabbar-gap) + 70px + 8px);" webapp/src/styles/globals.css` 가 1줄 · `grep -n -- "--native-body-reserve: 108px;" webapp/src/styles/globals.css` 가 1줄
    - `grep -cE '^\s*--safe-area-inset-(top|bottom|left|right)\s*:' webapp/src/styles/globals.css` 가 0 (주입 변수를 정의하지 않는다)
    - `grep -n "box-content pt-\[var(--app-safe-top)\]" webapp/src/components/layout/app-header.tsx` 가 1줄 · 같은 파일에 `h-14` 가 남아 있다
    - `grep -n "pl-\[var(--app-safe-left)\] pr-\[var(--app-safe-right)\]" webapp/src/components/layout/app-shell.tsx` 가 1줄
    - `app-shell-chrome.test.tsx` 가 수정 없이 통과한다
  </acceptance_criteria>
  <done>safe-area·탭바 변수 체계가 한 구역에 있고, 헤더·셸이 풀블리드 안전영역을 따르며, 브라우저 셸 단위 계약은 그대로다.</done>
</task>

<task type="auto">
  <name>Task 2: 하단 고정 요소 — 앱은 탭바 위로(`--native-tabbar-offset`), 브라우저는 safe-area 만큼 비켜 섬</name>
  <files>webapp/src/styles/globals.css, webapp/src/components/trading/workbench/shared-panels.tsx, webapp/src/components/stock/stock-detail-tabs.tsx, webapp/src/components/trading/workbench/alert-toasts.tsx, webapp/src/components/chat/chat-fab.tsx, webapp/src/components/trading/lc/number-pad-sheet.tsx</files>
  <read_first>
    - webapp/src/components/trading/dirty-action-bar.tsx (L1-60 파일 머리 ⑥ · L98-120 — 이 파일은 수정하지 않고 globals.css 규칙으로만 움직인다)
    - webapp/src/components/trading/workbench/shared-panels.tsx (L20-60 머리 주석 · L130-165 `useDirtyBarReserve` · L220-260 루트)
    - webapp/src/components/stock/stock-detail-tabs.tsx (L115-135 · L200-230 CTA 주석의 식 3개)
    - webapp/src/components/trading/workbench/alert-toasts.tsx (L50-65)
    - webapp/src/components/chat/chat-fab.tsx (L114-122) · webapp/src/components/chat/fab-clearance.ts (클리어런스 = FAB 폭 + 32)
    - webapp/src/components/trading/lc/number-pad-sheet.tsx (L236-245)
    - webapp/src/components/ui/sheet.tsx (L55-90 — 수정하지 않는다, `data-side` 속성만 쓴다)
    - webapp/src/styles/globals.css (L640-655 CTA 가 있을 때 FAB 들어올림 규칙)
    - .planning/phases/21-gh-trade-mobile-app/21-RESEARCH.md (Pitfall 10 목록)
  </read_first>
  <action>
    ① `globals.css` §21 구역에 이어서(주석에 대상 목록과 근거 Pitfall 10): 브라우저·앱 공통 — `[data-slot="dirty-action-bar"] { padding-bottom: max(var(--s-2), var(--app-safe-bottom)); }` · `[data-slot="shared-panels"][data-pinned="true"] { padding-bottom: var(--app-safe-bottom); }` · 사이드·바텀 시트: `[data-slot="sheet-content"][data-side="left"], [data-slot="sheet-content"][data-side="right"] { border-top: var(--app-safe-top) solid transparent; border-bottom: var(--app-safe-bottom) solid transparent; }` 와 `[data-slot="sheet-content"][data-side="bottom"] { border-bottom: var(--app-safe-bottom) solid transparent; }`(투명 테두리 — 배경이 border-box 까지 칠해져 시트 면이 상태바·홈 인디케이터 뒤까지 이어지고 소비처 패딩 클래스는 건드리지 않는다). 앱 전용 — `html.native-app [data-slot="dirty-action-bar"] { bottom: var(--native-tabbar-offset); right: 0; padding-bottom: var(--s-2); }`(앱엔 FAB 이 없으니 FAB 클리어런스 우측 여백 0) · `html.native-app [data-slot="detail-order-cta-bar"] { bottom: var(--native-tabbar-offset); padding-bottom: 10px; }` · `@media (max-width: 767.98px) { html.native-app [data-order-cta="true"] { padding-bottom: 76px; } }`(CTA 바 높이 10+56+10 — 탭바 몫은 본문 108 이 따로 진다) · `html.native-app [data-slot="shared-panels"] { bottom: var(--native-tabbar-offset); }`(fixed·sticky 모두 · ≥700 static 은 무영향) · `html.native-app [data-slot="shared-panels"][data-pinned="true"] { padding-bottom: 0; }` · `@media (min-width: 700px) { html.native-app [data-slot="alert-toasts"] { bottom: calc(var(--native-tabbar-offset) + 12px); } }`(폰 밴드는 상단 배치라 제외 — 넣으면 top·bottom 이 동시에 걸려 토스트가 늘어난다). 기존 FAB 들어올림 규칙의 `env(safe-area-inset-bottom, 0px)` → `var(--app-safe-bottom)`.
    ② `shared-panels.tsx`: 루트에 `data-slot="shared-panels"` 와 `data-pinned={pinned ? 'true' : undefined}` 를 더한다(`data-testid` 유지). `reserveStyle` 의 `bottom` 을 `` `calc(var(--native-tabbar-offset, 0px) + ${reserve}px)` `` 로 바꾼다(인라인 스타일이 CSS 규칙을 이기므로 앱에서는 탭바 위 + 더티 바 높이, 브라우저는 종전 값). `marginBottom` 은 그대로. 머리 주석에 D-25 한 단락.
    ③ `stock-detail-tabs.tsx`: Tabs 루트에 `data-order-cta={showOrderCta ? 'true' : undefined}` 를 더하고 예약 클래스의 `env(safe-area-inset-bottom)` → `var(--app-safe-bottom)`. CTA 바의 `pb-[max(20px,env(safe-area-inset-bottom))]` → `pb-[max(20px,var(--app-safe-bottom))]`. CTA 주석의 「식 3개」 단락에 앱 분기(바 bottom = `--native-tabbar-offset` · 바 하단 10 · 예약 76)를 한 줄 더한다.
    ④ `alert-toasts.tsx`: `bottom-3` → `bottom-[calc(12px+var(--app-safe-bottom))]`, `max-[699px]:top-3` → `max-[699px]:top-[calc(12px+var(--app-safe-top))]`(나머지 클래스 무변경 · 루트에 이미 `data-slot="alert-toasts"` 있음).
    ⑤ `chat-fab.tsx`: `bottom-6` → `bottom-[max(24px,var(--app-safe-bottom))]`(`native:hidden` 유지).
    ⑥ `number-pad-sheet.tsx`: `bottom-[max(10px,env(safe-area-inset-bottom))]` → `bottom-[max(10px,var(--app-safe-bottom))]`(Android WebView<140 은 env 가 0 — 주입 변수 우선).
    ⑦ 코드 전체에서 `env(safe-area-inset-` 직접 사용이 `globals.css` 의 `--app-safe-*` 정의 4줄 외에 남지 않게 한다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/stock/__tests__/stock-detail-tabs.test.tsx src/components/chat/__tests__/chat-fab.test.tsx src/components/trading/lc/__tests__/number-pad-sheet.test.tsx src/components/trading/__tests__/shared-panels.test.tsx src/components/trading/__tests__/alert-toasts.test.tsx</automated>
    <fails_when>non-zero exit, or the summary contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && grep -rn "env(safe-area-inset-" webapp/src --include='*.tsx' --include='*.ts' --include='*.css'</automated>
    <fails_when>any output line is from a file other than webapp/src/styles/globals.css, or globals.css shows more than the 4 `--app-safe-*` definition lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'html.native-app \[data-slot="detail-order-cta-bar"\]' webapp/src/styles/globals.css` · `grep -n 'html.native-app \[data-slot="dirty-action-bar"\]' webapp/src/styles/globals.css` · `grep -n 'html.native-app \[data-slot="shared-panels"\]' webapp/src/styles/globals.css` 가 각각 1줄 이상
    - `grep -n 'data-order-cta=' webapp/src/components/stock/stock-detail-tabs.tsx` 가 1줄 · `grep -n 'data-slot="shared-panels"' webapp/src/components/trading/workbench/shared-panels.tsx` 가 1줄
    - `grep -n "native-tabbar-offset, 0px" webapp/src/components/trading/workbench/shared-panels.tsx` 가 1줄
    - `grep -n "bottom-\[max(24px,var(--app-safe-bottom))\]" webapp/src/components/chat/chat-fab.tsx` 가 1줄
    - 두 `<automated>` 가 통과한다
  </acceptance_criteria>
  <done>앱에서 모든 하단 고정 바가 탭바 위에 서고, 브라우저에서도 노치·홈 인디케이터를 비켜 서며, safe-area 원천은 변수 네 개뿐이다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: e2e — 앱 모드 계산 스타일(본문 108 · CTA 92 · 예약 76) + 브라우저 셸 불변식 회귀</name>
  <files>webapp/e2e/specs/native-shell.spec.ts</files>
  <read_first>
    - webapp/e2e/specs/native-shell.spec.ts (21-05 — 기존 describe 두 개와 픽스처 사용 방식)
    - webapp/e2e/fixtures/native-app.ts (`installNativeApp`)
    - webapp/e2e/specs/home.spec.ts (L318-420 「셸 불변식」 두 케이스 — 수정하지 않고 회귀로 돌린다)
    - webapp/e2e/fixtures/mock-api.ts (종목상세 목 — CTA 가 보이는 기본 탭 조건)
  </read_first>
  <behavior>
    - 두 모드 공통: `meta[name="viewport"]` content 에 `viewport-fit=cover` 포함 · 헤더 `getBoundingClientRect().height` = 56(크롬 안전영역 0)
    - 앱·390×844 `/` : `getComputedStyle(main).paddingBottom === '108px'`
    - 앱·390 `/stocks/<코드>` : `[data-slot="detail-order-cta-bar"]` 계산 `bottom === '92px'`(크롬 env 0 → gap 14 + 70 + 8) · `padding-bottom === '10px'` · `[data-order-cta="true"]` 계산 `padding-bottom === '76px'`
    - 브라우저·390 같은 화면: CTA `bottom === '0px'` · `padding-bottom === '20px'` · 예약 `padding-bottom === '96px'` · `main` paddingBottom `'8px'`
    - 앱·1280 `/` : `main` paddingBottom `'108px'`(iPad 공통값)
  </behavior>
  <action>
    `native-shell.spec.ts` 에 `test.describe('Phase 21 — safe-area · 탭바 여백 (D-25 · D-27a)')` 를 더하고 `<behavior>` 를 케이스로 쓴다(픽스처 `installNativeApp`, 데이터 `mockHomeApi`·`mockStockApi`). 수치 근거(식)를 케이스 제목 또는 주석에 적는다 — 값이 바뀌면 D-27a 와 globals.css §21 을 같이 고쳐야 한다는 경고 포함. 이어서 `home.spec.ts -g "셸 불변식"` 을 회귀로 돌린다(MOBILE-01k — 파일 수정 없음).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/native-shell.spec.ts</automated>
    <fails_when>non-zero exit, or output contains "failed" or "No tests found", or lacks "passed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/home.spec.ts -g "셸 불변식"</automated>
    <fails_when>non-zero exit, or output contains "failed" or "No tests found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "92px" webapp/e2e/specs/native-shell.spec.ts` · `grep -n "108px" webapp/e2e/specs/native-shell.spec.ts` · `grep -n "viewport-fit=cover" webapp/e2e/specs/native-shell.spec.ts` 가 각각 1줄 이상
    - `home.spec.ts` 가 이 플랜에서 수정되지 않았다(`git diff --quiet -- webapp/e2e/specs/home.spec.ts` exit 0, 커밋 전 작업 트리 기준)
    - 두 `<automated>` 가 통과한다
  </acceptance_criteria>
  <done>탭바 여백 식이 실브라우저 계산 스타일로 잠겼고 브라우저 셸 불변식은 수정 없이 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음(표현 층) | 이 플랜은 CSS·마크업 속성만 바꾼다 — 데이터·권한 경로 변화 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-30 | Denial of Service(자기 자산) | 탭바가 주문 CTA·더티 바를 가려 주문·수정 불가 | high | mitigate | 앱에서 하단 고정 바 전부 `--native-tabbar-offset` 위로(Task 2 ①) · e2e 계산 스타일 단언(Task 3) · 21-16 UAT 에서 실기 확인 |
| T-21-31 | Tampering(UI 오조작) | 가로 노치 아래로 버튼이 들어가 오터치 | low | mitigate | 셸 좌우 safe-area 패딩(Task 1 ④) |
| T-21-SC | Tampering | 패키지 설치 | low | accept | 설치 없음 |
</threat_model>

<verification>
- 셸 단위 테스트 · 고정 요소 관련 단위 테스트 green · `env(safe-area-inset-` 직접 사용 정리 · native-shell.spec green · home.spec 셸 불변식 2 passed
</verification>

<success_criteria>
- D-25 가 웹·앱 공통으로 적용되고, 앱에서는 탭바(D-27a 기하)와 겹치는 웹 요소가 0 이다.
</success_criteria>

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| `mobile/` 패키지 `@gh-radar/mobile` — scripts `native:sync` · `native:sync:dev` · `native:build:ios` · `native:build:android` · `native:smoke:ios` · `native:open:ios` · `native:open:android` · `native:assets` (21-01) · `native:smoke:android` (21-02) · `native:check-tab-routes:ios` (21-10) · `native:test:android` (21-12) · `native:verify-prod` (21-16) | 패키지·스크립트 | 21-01 · 21-02 · 21-10 · 21-12 · 21-16 |
| `mobile/capacitor.config.ts` — appId `com.ghtrade.app` · appName `GH Trade` · `CAP_SERVER_URL` · `SystemBars` (21-01) · `SocialLogin` (21-15) | 설정 | 21-01 · 21-15 |
| `mobile/tsconfig.json` · `mobile/.gitignore` | 설정 | 21-01 |
| `mobile/www/index.html` — 오프라인 폴백(최소 21-01 · `safeTarget`·5초 탐침·`online`·테마 21-11) | 정적 페이지 | 21-01 · 21-11 |
| `mobile/scripts/smoke-ios.sh` · `mobile/scripts/smoke-android.sh` — 「SMOKE OK ready platform=…」 | 스모크 | 21-01 · 21-02 |
| `mobile/scripts/check-tab-routes-ios.sh` · `mobile/scripts/tab-routes-check.swift` | Swift 경로표 검사 | 21-10 |
| `mobile/scripts/render-resources.mjs` · `mobile/resources/{icon.svg,icon-only.png,icon-foreground.png,icon-background.png,splash.png,splash-dark.png}` | 에셋 원본 | 21-14 |
| `mobile/scripts/verify-prod-config.mjs` · `mobile/README.md` | 운영 설정 검사·문서 | 21-16 |
| iOS `SceneDelegate.swift` (루트 = `GHTradeBridgeViewController()` · 창 배경 21-11) | Swift | 21-01 · 21-11 |
| iOS `GHTradeBridgeViewController` · `WeakScriptMessageHandler` · `NativeMessageType` | Swift | 21-01 · 21-10 · 21-11 |
| iOS `GHTradeTabBar` · `GHTradeTab` · `GHTabID` · `GHTradePalette` · `GHTradeTheme` · `TabRoutes.activeTab(forPath:)` · `TabRoutes.hidesTabBar(path:)` | Swift | 21-10 |
| iOS `NavigationDelegateProxy` · `ThemeStore` (UserDefaults `gh-trade.theme`) | Swift | 21-11 |
| Android `MainActivity` (`BridgeActivity`) · `GhTradeWebViewClient` (`BridgeWebViewClient` 상속) · `GhTradeBridge` (`@JavascriptInterface postMessage`) | Kotlin | 21-02 · 21-12 · 21-13 |
| Android `GhTradeTabBar` · `GhTradePalette` · `TabId` · `TabRoutes.activeTabFor` · `TabRoutes.hidesTabBar` · `TabRoutesTest` | Kotlin | 21-12 |
| Android `ThemeStore` (SharedPreferences `gh_trade`/`theme`) · `res/values/bools.xml` · `res/values-sw600dp/bools.xml` (`is_tablet`) | Kotlin·리소스 | 21-13 |
| Android `res/drawable/ic_tab_{home,home_fill,search,search_fill,trading,trading_fill,ai,ai_fill,me,me_fill}.xml` | 벡터 | 21-12 |
| 웹 `lib/native/native-detect.ts` — `NATIVE_DETECT_SCRIPT` · `isNativeApp` · `nativePlatform` | 유틸 | 21-01 |
| 웹 `lib/native/post-native.ts` — `postNative` · `NativeMsgType` | 유틸 | 21-04 |
| 웹 `lib/native/native-bridge-provider.tsx` — `NativeBridgeProvider` · `useNativeBridge` · `NativeRefreshFn` · `window.__ghTrade = { refresh, navigate, back }` | Provider | 21-04 |
| 웹 `lib/native/use-native-refresh.ts` — `useNativeRefresh` | 훅 | 21-04 |
| 웹 `lib/native/native-overlay-marker.tsx` — `NativeOverlayMarker` | 컴포넌트 | 21-04 |
| 웹 `lib/native/google-client-ids.ts` — `GOOGLE_WEB_CLIENT_ID` · `GOOGLE_IOS_CLIENT_ID` · `GOOGLE_ANDROID_CLIENT_ID` · `GOOGLE_IOS_URL_SCHEME` | 공개 식별자 상수 | 21-03 |
| 웹 `lib/native/native-google-login.ts` — `nativeGoogleSignIn` · `randomNonce` · `sha256Hex` · `classifyNativeLoginError` | 서비스 | 21-15 |
| 웹 `RelayConnectionState.probeNow` · `EMPTY_RELAY_VALUE.probeNow` | 훅 계약 | 21-07 |
| 웹 `lib/recent-search.ts` — `RECENT_SEARCH_KEY` · `RECENT_SEARCH_MAX` · `RecentSearchItem` · `readRecentSearches` · `pushRecentSearch` · `removeRecentSearch` · `clearRecentSearches` | 유틸 | 21-08 |
| 웹 `app/search/page.tsx` · `components/search/search-page-client.tsx` — `SearchPageClient` · 사이드바 `NAV_SEARCH_PAGE` | 페이지·상수 | 21-08 |
| 웹 `components/me/account-card.tsx` — `AccountCard` | 컴포넌트 | 21-09 |
| 웹 마크업 계약 `data-slot="app-aside"` · `data-slot="app-menu-button"` · `data-slot="stock-ai-button"` · `data-slot="shared-panels"` · `data-pinned` · `data-order-cta` · `data-slot="account-card"` | 마크업 | 21-05 · 21-06 · 21-09 |
| CSS `@custom-variant native` (21-04) · 앱 셸 규칙 (21-05) · `--app-safe-top` · `--app-safe-bottom` · `--app-safe-left` · `--app-safe-right` · `--native-tabbar-gap` · `--native-tabbar-offset` · `--native-body-reserve` (21-06) | 토큰·변형 | 21-04 · 21-05 · 21-06 |
| 웹 `app/icon.svg` (= 스케치 004 `#app-a`) | 파비콘 | 21-14 |
| 단위 테스트: `native-detect.test.ts` (21-01) · `post-native.test.ts` · `native-bridge-provider.test.tsx` · `native-overlay.test.tsx` (21-04) · `google-client-ids.test.ts` (21-03) · `relay-socket.test.ts` probeNow · `stock-native-refresh.test.tsx` (21-07) · `recent-search.test.ts` · `search-page-client.test.tsx` · `app-sidebar.test.tsx` 보강 (21-08) · `account-card.test.tsx` · `app-shell-chrome.test.tsx` 보강 (21-09) · `native-google-login.test.ts` · `login/__tests__/page.test.tsx` (21-15) · `TabRoutesTest.kt` (21-12) | 단위 | 각 플랜 |
| e2e: `fixtures/native-app.ts` — `installNativeApp` · `nativeMessages` (21-05) · `specs/native-shell.spec.ts` (21-05 · 21-06) · `specs/search-page.spec.ts` (21-08) · `specs/brand-account.spec.ts` (21-09) | Playwright | 각 플랜 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-06-SUMMARY.md` when done
</output>
