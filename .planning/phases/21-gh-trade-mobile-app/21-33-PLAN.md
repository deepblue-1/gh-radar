---
phase: 21-gh-trade-mobile-app
plan: 33
type: execute
wave: 14
depends_on: ["21-25", "21-26", "21-30", "21-32"]
files_modified:
  - webapp/src/components/trading/workbench/trading-workbench.tsx
  - webapp/src/components/trading/__tests__/trading-workbench.test.tsx
  - webapp/e2e/specs/trading-workbench.spec.ts
  - webapp/src/components/trading/card/manual-order-form.tsx
  - webapp/src/components/trading/__tests__/manual-order-form.test.tsx
  - webapp/src/components/trading/card/stock-info-modal.tsx
  - webapp/src/components/trading/__tests__/stock-info-modal.test.tsx
autonomous: true
gap_closure: true
requirements: [MOBILE-01]
coupling_justified:
  - "21-34: 종목상세 「트레이딩」 CTA 와 호가주문 탭 제거는 이 플랜이 만드는 착지(`?code=`)와 카드 주문유형이 먼저 있어야 한다 — D-31 순서(R3-9 → R3-10) 그대로 21-34 가 다음 웨이브"
  - "21-30: 팝업 안 전체목록은 21-30 의 NewsFullList · DiscussionFullList · 섹션 onShowAll 을 쓴다"

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G-21-R3-9 (트레이딩 쪽 · D-30): `/trading?code={code}` 로 들어오면 그 종목 카드가 없으면 만들고, 있으면 펼치고, 그 카드로 스크롤·포커스한다 — 같은 URL 을 다시 열어도 카드가 늘지 않고, 형식 오류·매매 불가(비 KOSPI/KOSDAQ · isin 없음) 코드는 카드 없이 로그만 남으며, 어떤 경우에도 전략 등록·주문을 보내지 않는다"
    - "G-21-R3-10 (트레이딩 쪽 · D-31): 트레이딩 카드 수동주문에 주문유형(지정가/시간외종가)이 스케치 008 ③ 채택안 배치로 있고, 시간외종가 선택 가능 판정 · 창 닫힘 복귀 · 닫힌 창 전송 차단 · 가격 잠김은 호가 탭과 같은 규칙(affordanceOf)이다"
    - "G-21-R3-8 (팝업 · D-29): 트레이딩 ⓘ 팝업 「뉴스·토론」 탭에서 전체 보기가 팝업 안 전체목록으로 바뀌고(/trading URL 불변), Esc · Android 뒤로가기 · 화면 안 ← 가 요약으로, 요약에서 한 번 더 Esc 면 팝업이 닫힌다"
  artifacts:
    - path: webapp/src/components/trading/workbench/trading-workbench.tsx
      provides: "?code= 소비 — fetchStockDetail → isPickable → ensureIsinCard(reveal) · URL 정리"
      contains: "ensureIsinCard(detail.isin"
    - path: webapp/src/components/trading/card/manual-order-form.tsx
      provides: "카드 주문유형(채택안)"
      contains: "G-21-R3-10"
    - path: webapp/src/components/trading/card/stock-info-modal.tsx
      provides: "newsView 로컬 상태 · onEscapeKeyDown 두 단계"
      contains: "newsView"
  key_links:
    - from: webapp/src/components/trading/workbench/trading-workbench.tsx
      to: webapp/src/components/trading/workbench/stock-add-bar.tsx
      via: "isPickable(detail) — 매매 가능 판정 한 곳"
      pattern: "isPickable"
    - from: webapp/src/components/trading/card/stock-info-modal.tsx
      to: webapp/src/components/stock/news-full-list.tsx
      via: "newsView === 'news' → <NewsFullList code onBack>"
      pattern: "NewsFullList"
  prohibitions:
    - statement: "MUST NOT `?code=` 착지가 전략 등록·주문·정정 프레임을 보낸다"
      status: unverified
      verification: "Task 1 e2e — relay 로 나간 전략/주문 프레임 0"
    - statement: "MUST NOT 팝업 안 전체목록을 위해 /trading URL 을 바꾼다(작업대 useSearchParams 재렌더)"
      status: unverified
      verification: "Task 3 acceptance — stock-info-modal 에 history/router 호출 0"
---

<objective>
R3-9 → R3-10 순서(D-31)의 앞 절반 — 트레이딩 쪽을 먼저 만든다: **G-21-R3-9** `/trading?code=` 착지(카드 보장·펼침·포커스) · **G-21-R3-10** 카드 수동주문 주문유형(시간외종가가 호가주문 탭 없이도 가능). 같은 트레이딩 표면이라 **G-21-R3-8 팝업 쪽**(ⓘ 팝업 안 전체목록)도 여기서 붙인다. 종목상세 CTA 교체·호가주문 탭 제거는 21-34 가 이 착지·주문유형이 생긴 뒤에 한다(갈 곳 없는 중간 상태 금지).

Purpose: 종목상세에서 「트레이딩」을 누르면 곧바로 그 종목 카드로 가고, 시간외종가 신규 주문이 카드에서 가능해지며, 팝업에서 뉴스·토론 전체를 떠나지 않고 본다.
Output: 작업대 `?code=` 소비 · 수동주문 주문유형(채택안) · 팝업 newsView · 단위 테스트 · e2e 한 개(착지).

과설계 금지: `?code=` 는 작업대 안 effect 하나(처리한 코드 ref · 정규식 · isPickable 재사용)로 처리하고 새 훅·모듈을 만들지 않는다. 팝업 전체목록은 단위 테스트로 잠근다.

커밋 규율: 한글 메시지 · **Co-Authored-By 줄을 넣지 않는다** · 파일 지정 add(`git add -A` 금지) · 커밋 직전 `git status -sb` · **push 금지**(21-36).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-gh-trade-mobile-app/21-UAT.md
@.planning/sketches/008-uat3-surfaces/README.md
@webapp/src/components/trading/workbench/trading-workbench.tsx
@webapp/src/components/trading/workbench/stock-add-bar.tsx
@webapp/src/components/trading/card/manual-order-form.tsx
@webapp/src/components/trading/card/stock-info-modal.tsx

<interfaces>
```text
trading-workbench.tsx: useSearchParams(129 import · 613) · ⑥ `?focus=` 마운트 1회 소비(612~620 · 머리 주석 69) · layoutRestored(631 — 복원 전 카드 추가는 복원이 덮는다 → 복원 뒤에 처리) · ensureIsinCard(isin, name, code, reveal)(767~ · 있으면 펼침 · 없으면 추가 · setScrollTarget) · 잔고 행 → ensureIsinCard(…, true)(787)
stock-add-bar.tsx: ORDERABLE_MARKETS ['KOSPI','KOSDAQ'](71) · export function isPickable(row): row is StockDetailResponse & {isin: string}(78)
fetchStockDetail(code, signal) (lib/stock-api) → StockDetailResponse { code, name, market, isin | null, … }
manual-order-form.tsx: variant 'card' | 'orderbook' · orderTypeState(378) · orderType 파생(405 — 호가 variant 에서만 state) · affordanceOf → aff.offHoursSelectable · 창 닫힘 복귀 이펙트(457~460) · 가격 잠김 상자 · 4 주문유형 행(937~ · 호가 variant 전용) · 호가 전용 각주(1058~ · 21-34 정리)
stock-info-modal.tsx(21-26 이후): StockInfoTabs · 뉴스 패널(StockNewsSection · StockDiscussionSection) · 본문 data-slot stock-info-modal-body
21-30 공용: NewsFullList({code, onBack?}) · DiscussionFullList({code, onBack?}) · 섹션 onShowAll?: () => void
Android back(): overlay 열림이면 document 에 합성 Escape → Radix 가장 위 레이어 onEscapeKeyDown
21-31: 검색 파라미터를 단 탭 루트 진입은 스크롤 복원을 건너뛴다 — `?code=` reveal 이 이긴다
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 트레이서 — /trading?code= → 작업대 effect(정규식 · 처리 ref) → fetchStockDetail → isPickable → ensureIsinCard(reveal) → URL 정리 → e2e (G-21-R3-9)</name>
  <files>webapp/src/components/trading/workbench/trading-workbench.tsx, webapp/src/components/trading/__tests__/trading-workbench.test.tsx, webapp/e2e/specs/trading-workbench.spec.ts</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-R3-9 root_cause · artifacts)
    - webapp/src/components/trading/workbench/trading-workbench.tsx (60~80 머리 주석 ⑥ · 605~660 focus · layoutRestored · 730~790 ensureIsinCard)
    - webapp/src/components/trading/workbench/stock-add-bar.tsx (60~90 isPickable)
    - webapp/src/components/trading/__tests__/trading-workbench.test.tsx (focus 소비 테스트 모양 · fetch mock)
    - webapp/e2e/specs/trading-workbench.spec.ts (로컬 relay 픽스처 · 종목 목 · 송신 프레임 캡처 방식)
  </read_first>
  <action>
    per D-30: `trading-workbench.tsx` 에 effect 하나 — `const code = searchParams?.get('code') ?? null` 이 있고 `layoutRestored` 가 true 이며 처리 ref(`handledCodeRef`)와 다를 때만: ref 에 기록 → `/^[0-9A-Z]{6}$/` 불일치면 `console.warn('[gh-radar] ?code= 형식 아님 — 무시')` → 맞으면 `fetchStockDetail(code, signal)` → `isPickable(detail)`(stock-add-bar 에서 import — 판정 한 곳) 이면 `ensureIsinCard(detail.isin, detail.name, detail.code, true)`, 아니면 `console.warn('[gh-radar] ?code= 매매 불가 종목 — 카드 없음', code)` · 실패는 `console.error` 후 카드 없음 · 끝나면 `window.history.replaceState(null, '', '/trading')` 로 `code` 를 지운다(focus 등 다른 파라미터가 있으면 그것만 남긴다) · 정리 시 abort. 전략 등록·주문은 보내지 않는다. 머리 주석 ⑥ 아래 ⑥-b 「`?code=` — 종목상세 「트레이딩」 착지(D-30) · 복원 뒤 1회 · 카드 보장 + reveal · 송신 0」.
    단위(trading-workbench.test.tsx): `?code=005930` → fetchStockDetail 1회 → 카드 추가 · 펼침 · reveal 대상 / 같은 코드 카드가 있으면 추가 없이 펼침 / KONEX → 카드 없음 / `abc` → fetch 0.
    e2e(trading-workbench.spec.ts) 「G-21-R3-9 /trading?code=」 하나: 로컬 relay 규약 · 종목 상세 목 → `/trading?code=005930` → 그 종목 카드가 펼쳐지고 헤더 토글에 포커스 · URL 에 `code` 없음 · relay 로 나간 전략/주문 프레임 0 → 같은 URL 다시 → 카드 수 그대로.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/__tests__/trading-workbench.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed"</fails_when>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/trading-workbench.spec.ts -g "G-21-R3-9"</automated>
    <fails_when>non-zero exit, or the summary contains "failed", or no summary line matches "[0-9]+ passed" (a -g filter that matches nothing prints "No tests found")</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "ensureIsinCard(detail.isin" webapp/src/components/trading/workbench/trading-workbench.tsx` ≥ 1 · `grep -c "isPickable" webapp/src/components/trading/workbench/trading-workbench.tsx` ≥ 1 · `grep -c "handledCodeRef" webapp/src/components/trading/workbench/trading-workbench.tsx` ≥ 2
    - 종목상세 무변경(이 플랜 커밋 기준): `L="$(git log --grep='(21-33)' --name-only --format=)" && test "$(printf '%s\n' "$L" | grep -c '^webapp/src/components/stock/')" -eq 0`
    - `grep -c "G-21-R3-9" webapp/e2e/specs/trading-workbench.spec.ts` ≥ 1
    - verify 두 명령 통과
  </acceptance_criteria>
  <done>`/trading?code=` 가 그 종목 카드를 보장·펼침·포커스하고, 형식 오류·매매 불가는 로그만 남기며 아무것도 보내지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 카드 수동주문 주문유형(지정가/시간외종가) — 스케치 008 ③ 채택안 · affordance 그대로 (G-21-R3-10 트레이딩 쪽)</name>
  <files>webapp/src/components/trading/card/manual-order-form.tsx, webapp/src/components/trading/__tests__/manual-order-form.test.tsx</files>
  <read_first>
    - .planning/sketches/008-uat3-surfaces/README.md · index.html (③ Winner · 수치 · 마크업)
    - webapp/src/components/trading/card/manual-order-form.tsx (1~30 머리 주석 · 120~160 문구 상수 · 370~470 affordance · 800~860 가격/잠김 상자 · 930~975 주문유형 행)
    - webapp/src/components/trading/__tests__/manual-order-form.test.tsx (350~420 주문유형 케이스 · orderbook variant 케이스)
  </read_first>
  <behavior>
    - card variant 에도 주문유형 컨트롤이 있고 기본 지정가
    - KRX + 시간외 창 열림 → 시간외종가 선택 가능 → 선택하면 가격 칸 잠김 · 전송 페이로드 orderType offhours + session
    - NXT 또는 창 닫힘 → 시간외종가 비활성 + 비활성 사유 title
    - 시간외종가 선택 중 창이 닫히면 지정가로 복귀 · 닫힌 창으로는 전송 차단
  </behavior>
  <action>
    per D-31: `orderType` 파생(405행)을 variant 와 무관하게 `orderTypeState` 로 바꾸고, 주문유형 컨트롤을 스케치 008 ③ **채택안 배치** 한 가지로 카드에서도 렌더한다 — A = 기존 44px 행(937~)의 호가 variant 조건을 없애고 그대로 / B = 가격 위 세그먼트 「지정가 | 시간외종가」(`data-slot="mo-type"` · 카드 헤더 거래소 세그먼트와 같은 컴포넌트 · 비활성 항목 title) / C = 가격 라벨 옆 칩 + 네이티브 select. 선택 가능 판정(`aff.offHoursSelectable`) · 창 닫힘 복귀 이펙트 · 닫힌 창 전송 차단 · 가격 잠김 상자 · 구간 배지는 그대로 쓴다. 호가 전용 각주는 이 플랜에서 두고 21-34 가 정리한다. 머리 주석 첫 단락을 「카드에도 주문유형(D-31 · G-21-R3-10 — 호가주문 탭 제거 뒤 시간외종가 신규 주문의 유일한 표면)」 으로 고친다. 테스트: 「카드 variant 에는 주문유형 콤보가 없다」 케이스를 behavior 네 케이스로 바꾸고 기존 orderbook variant 케이스는 그대로 통과(21-34 가 옮긴다).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/__tests__/manual-order-form.test.tsx src/components/trading/__tests__/card-body.test.tsx && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - 카드도 주문유형 상태를 쓴다: `test "$(grep -c "variant === 'orderbook' ? orderTypeState" webapp/src/components/trading/card/manual-order-form.tsx)" -eq 0`
    - `grep -c "G-21-R3-10" webapp/src/components/trading/card/manual-order-form.tsx` ≥ 1
    - SUMMARY 에 구현한 채택안(A/B/C)과 수치 대조가 있다
    - verify 명령 통과
  </acceptance_criteria>
  <done>트레이딩 카드에서 시간외종가 신규 주문을 같은 판정과 방어로 낼 수 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 팝업 안 전체목록 — newsView 로컬 상태 · ← · Esc/Android 뒤로가기 두 단계 · URL 불변 (G-21-R3-8 팝업)</name>
  <files>webapp/src/components/trading/card/stock-info-modal.tsx, webapp/src/components/trading/__tests__/stock-info-modal.test.tsx</files>
  <read_first>
    - .planning/phases/21-gh-trade-mobile-app/21-UAT.md (G-21-R3-8 — 팝업은 로컬 state · /trading 에 pushState 금지)
    - webapp/src/components/trading/card/stock-info-modal.tsx (21-26 이후 전체)
    - webapp/src/components/stock/news-full-list.tsx · discussion-full-list.tsx · stock-news-section.tsx · stock-discussion-section.tsx (21-30 — onBack · onShowAll)
    - webapp/src/components/trading/__tests__/stock-info-modal.test.tsx (섹션 mock 모양)
  </read_first>
  <behavior>
    - 뉴스·토론 탭에서 「전체 뉴스 보기」 → NewsFullList 가 보이고 요약 섹션은 없다
    - Escape → 요약으로 돌아오고 팝업은 열려 있다 · 한 번 더 Escape → 팝업 닫힘
    - 전체목록의 「요약으로 돌아가기」 → 요약 · 「전체 토론 보기」 도 같은 규칙
    - 어느 단계에서도 window.history 가 바뀌지 않는다
  </behavior>
  <action>
    per D-29: `StockInfoModal` 에 `newsView: 'news' | 'discussions' | null` 상태(팝업이 닫히면 언마운트 → 다음 열림은 요약) · 뉴스 패널은 null 이면 두 섹션에 `onShowAll`, 아니면 해당 전체목록(`onBack` = null 로). 전체목록으로 갈 때 본문(`stock-info-modal-body`) scrollTop 을 ref 에 저장하고 돌아오면 복원. `DialogContent` 에 `onEscapeKeyDown` — newsView 가 있으면 `preventDefault()` 후 null(Android 뒤로가기 합성 Escape 도 같은 경로). URL 은 건드리지 않는다(머리 주석 한 줄: 「/trading URL 불변 — 작업대 useSearchParams 재렌더 방지」). 테스트: behavior 네 줄(섹션 mock 이 onShowAll 버튼을 그리게 · 전체목록 fetch mock · history 스파이).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/components/trading/__tests__/stock-info-modal.test.tsx && pnpm --filter @gh-radar/webapp run typecheck</automated>
    <fails_when>non-zero exit, or the vitest summary contains "failed", or output contains "error TS"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "newsView" webapp/src/components/trading/card/stock-info-modal.tsx` ≥ 3 · `grep -c "onEscapeKeyDown" webapp/src/components/trading/card/stock-info-modal.tsx` ≥ 1 · `grep -c "NewsFullList" webapp/src/components/trading/card/stock-info-modal.tsx` ≥ 1 · `grep -c "DiscussionFullList" webapp/src/components/trading/card/stock-info-modal.tsx` ≥ 1
    - URL 무변경: `test "$(grep -c 'history.pushState\|history.replaceState\|router.push' webapp/src/components/trading/card/stock-info-modal.tsx)" -eq 0`
    - verify 명령 통과
  </acceptance_criteria>
  <done>트레이딩 팝업에서 전체 뉴스·토론을 팝업 안에서 보고, 뒤로가기가 요약 → 닫힘 순으로 돌아간다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `?code=` → 작업대 카드 생성 | 링크가 제어하는 값이 카드(주문 표면)를 만든다 |
| 카드 주문유형 → relay 주문 프레임 | 시간외종가 세션 · 가격 규칙이 실주문 필드가 된다 |
| Android 뒤로가기 → 팝업 상태 | 합성 Escape 한 번이 한 단계만 되돌려야 한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-91 | Tampering | `?code=` 로 임의 카드 생성·자동 송신 | high | mitigate | 6자리 정규식 · isPickable(KOSPI/KOSDAQ + isin) · 송신 0 e2e(Task 1) |
| T-21-92 | Tampering | 카드 시간외종가 오전송(닫힌 창) | high | mitigate | affordanceOf 그대로 · 창 닫힘 복귀 · 전송 차단 단위 테스트(Task 2) |
| T-21-81 | Tampering | 팝업 전체목록이 /trading URL 변경 | low | mitigate | 로컬 상태만 · URL API 0 grep(Task 3) |
</threat_model>

<verification>
- vitest(trading-workbench · manual-order-form · card-body · stock-info-modal) · Playwright trading-workbench `-g "G-21-R3-9"` · typecheck
- 사람 확인(착지 포커스 · 카드 주문유형 배치 · 팝업 뒤로가기)은 21-36 UAT
</verification>

<success_criteria>
- R3-9 · R3-10 의 트레이딩 쪽과 R3-8 팝업 쪽이 닫혀, 21-34 가 CTA 교체·탭 제거를 안전하게 할 수 있다.
</success_criteria>

## Flagged assumptions

- **엣지 탐침 MOBILE-01 (unclassified · unresolved):** 경계는 코드 정규식 · 매매 불가 두 조건 · Esc 두 단계로 명시했다. 나머지는 21-36 UAT.
- `?code=` 처리 뒤 replaceState 로 파라미터를 지워도 Next 의 useSearchParams 재렌더는 처리 ref 로 막는다고 본다 — 단위 테스트가 한 번만 처리됨을 잠근다.

## Artifacts this phase produces

| 산출물 | 종류 | 만드는 플랜 |
|---|---|---|
| 스케치 `.planning/sketches/008-uat3-surfaces/`(index.html · README.md) · MANIFEST 008 행 | 목업 | 21-25 |
| CONTEXT D-12a · D-12b · D-25a · D-29 · D-30 · D-31 · D-32(R3-11 축소: 스크롤 복원 + 스켈레톤 없음 · 상태 초기화 허용) · WR-03 androidx.webkit 선언 답 | 결정 기록 | 21-25 |
| `PAD_CHIPS.만원` = 천만(+1,000) · 오천만(+5,000) · 1억(+10,000) · 지우기 · 20-CONTEXT D-17 포인터 | 상수 · 결정 | 21-26 |
| `SettingGroup` 제목 없는 그룹 상단 `pt-1` | 클래스 | 21-26 |
| 카드 헤더 ✕ `data-slot="card-close"` · 종목정보 팝업 ✕ = `size-8` + lucide `XIcon` + `after:-inset-1.5`(히트 44) · 팝업 고정 높이 · `scrollbar-gutter:stable` · 폰 safe-area 패딩 | 컴포넌트 | 21-26 |
| `webapp/src/lib/safe-path.ts` `isSafeInternalPath` · `recent-search.ts` `RECENT_CODE_RE` · `PopoverContent` 안 `NativeOverlayMarker` · `useDebouncedSearch` 반환 `resultsQuery` · `viewport.themeColor` `#17171c` + `ThemeColorSync` | 함수 · 훅 · 메타 | 21-27 |
| iOS 키보드 사유 즉시 숨김 · 재표시 90ms · `awaitingContent`(didStartProvisionalNavigation → 첫 route/1.5초) · 초기 숨김 · 브리지 출처 scheme·host·port 비교 · 오프라인 `dev=1` · 스모크 기기 지정(`IOS_DEVICE_UDID` · `ANDROID_SERIAL`) · mobile/README 명령표 | Swift · 스크립트 · 문서 | 21-28 |
| Android IME 애니메이션 콜백 즉시 숨김 · `awaitingContent`(onPageStarted) · `pullBlocked` 초기화 · 오프라인 뒤로가기(재현 시) · 오프라인 `dev=1` · 폴백 페이지 dev=1 일 때만 localhost · allowBackup=false + `res/xml/data_extraction_rules.xml` · 브리지 WebMessageListener(21-25 답 a) | Kotlin · XML · HTML | 21-29 |
| `NewsFullList` · `DiscussionFullList` · `useNewsView` · `StockNewsTabPanel` · 섹션 `onShowAll` · 종목상세 `?tab=news&view=news\|discussions` · 옛 URL 리다이렉트 · 팝업 `newsView` | 컴포넌트 · 훅 · 라우트 | 21-30 |
| `useTabRootScrollMemory`(`webapp/src/lib/tab-scroll-memory.ts` · AppShell 에서 호출) · 기존 `lib/query-cache` 재사용: 검색 허브 · 대화 목록 · 오늘 주문 | 훅 · 캐시 | 21-31 |
| 앱 공용 패널 숨김 CSS · 카드 더티 바 탭바 비킴(프로브) · `StrategyLogFeedProvider` · `useStrategyLogFeed` · `diffLimitChasers` · /me 전략 로그(`me-strategy-log`) | CSS · Provider · 컴포넌트 | 21-32 |
| 작업대 `?code=` 소비(카드 보장·펼침·포커스) · 카드 수동주문 주문유형(지정가/시간외종가) | 컴포넌트 | 21-33 |
| 종목상세 「트레이딩」 CTA(폰 바 + `detail-trading-button`) · 호가주문 탭 제거 · `?tab=orderbook` → `/trading?code=` · 호가 탭 전용 코드·`variant` 삭제 · orderbook.spec 선별 이전 | 컴포넌트 · 정리 · e2e | 21-34 |
| 게이트 기록 · `21-REVIEW-R2.md`(13건 대조) · UAT 환경(21-35-SUMMARY) · UAT 3차 재검증 · push 결정(21-36-SUMMARY) | 문서 | 21-35 · 21-36 |

<output>
Create `.planning/phases/21-gh-trade-mobile-app/21-33-SUMMARY.md` when done — 착지 흐름 · 주문유형 채택안 대조 · 팝업 두 단계 · 테스트 결과.
</output>
