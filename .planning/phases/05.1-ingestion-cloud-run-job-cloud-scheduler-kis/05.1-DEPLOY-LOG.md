# Phase 05.1 — Cloud Run Job + Scheduler 배포 로그

> 본 문서는 Wave 0(05.1-01-PLAN)에서 생성된 **템플릿**입니다.
> Wave 5(05.1-05-PLAN) 실배포 시 TBD 필드를 실값으로 채웁니다.

- **배포 일시 (UTC):** TBD
- **GCP 프로젝트:** `gh-radar`
- **리전:** `asia-northeast3`
- **Cloud Run Job:** `gh-radar-ingestion`
- **이미지:** `IMAGE=asia-northeast3-docker.pkg.dev/gh-radar/gh-radar/ingestion:<SHA>`
- **Scheduler:** `gh-radar-ingestion-scheduler` (cron `* 9-15 * * 1-5` KST)

## Service Accounts

| Role | Email | 부여 권한 |
|------|-------|-----------|
| Job runtime | `gh-radar-ingestion-sa@gh-radar.iam.gserviceaccount.com` | `roles/secretmanager.secretAccessor` × 4 secrets |
| Scheduler invoker | `gh-radar-scheduler-sa@gh-radar.iam.gserviceaccount.com` | `roles/run.invoker` on Job `gh-radar-ingestion` (리소스 단위) |

## Secrets

| Secret ID | ENV Var | 생성/재사용 |
|-----------|---------|------------|
| `gh-radar-kis-app-key` | `KIS_APP_KEY` | 신규 |
| `gh-radar-kis-app-secret` | `KIS_APP_SECRET` | 신규 |
| `gh-radar-kis-account-number` | `KIS_ACCOUNT_NUMBER` | 신규 |
| `gh-radar-supabase-service-role` | `SUPABASE_SERVICE_ROLE_KEY` | Phase 2 재사용 |

## 평문 ENV

| Key | Value |
|-----|-------|
| `SUPABASE_URL` | TBD |
| `KIS_BASE_URL` | `https://openapi.koreainvestment.com:9443` |
| `LOG_LEVEL` | `info` |
| `APP_VERSION` | `<GIT_SHA>` |

## Job 구성

| 항목 | 값 |
|------|-----|
| CPU | 1 |
| Memory | 512Mi |
| task-timeout | 120s |
| max-retries | 0 |
| parallelism | 1 |
| tasks | 1 |

## Alert policy

- **displayName:** `gh-radar-ingestion-failure`
- **Policy ID:** TBD (`gcloud alpha monitoring policies list ...`)
- **Notification channel:** email TBD (`gcloud beta monitoring channels list ...`)
- **조건:** `metric.labels.result="failed" AND completed_execution_count > 0` (5분 윈도우)

## INV 검증 결과 (smoke-ingestion.sh)

| INV | 내용 | 결과 |
|-----|------|------|
| INV-1 | `jobs execute --wait` exit 0 | TBD |
| INV-2 | logs: `cycle complete` OR `non-trading day detected, exiting` | TBD |
| INV-3 | logs: `cycle failed`/`EGW00201` 0건 | TBD |
| INV-4 | Supabase `stocks.max(updated_at)` < 120s OR 휴장일 로그 | TBD |
| INV-5 | Scheduler `state=ENABLED` | TBD |

## Manual 검증 (DEPLOY 후 관찰)

| 항목 | 관찰 시점 | 결과 |
|------|----------|------|
| 첫 거래일 월요일 09:00 KST 토큰 재발급 | 배포 후 첫 월요일 | TBD |
| 이메일 알림 실제 수신 (강제 실패 테스트) | 배포 직후 | TBD |
| Cloud Logging secret 평문 노출 0건 (T-05.1-04) | 배포 후 1일 | TBD |
| execution overlap 발생 여부 (Pitfall 1) | 첫 주 | TBD |

## 배포 중 조우한 이슈와 해결

(실배포 시 기록)

## 운영 참고

- **재배포:** `bash scripts/deploy-ingestion.sh`
- **Scheduler pause:** `gcloud scheduler jobs pause gh-radar-ingestion-scheduler --location=asia-northeast3`
- **Scheduler resume:** `gcloud scheduler jobs resume gh-radar-ingestion-scheduler --location=asia-northeast3`
- **로그:** `gcloud logging read 'resource.type="cloud_run_job" AND resource.labels.job_name="gh-radar-ingestion"' --freshness=1h`
- **콘솔:** https://console.cloud.google.com/run/jobs/details/asia-northeast3/gh-radar-ingestion?project=gh-radar
