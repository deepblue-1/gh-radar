---
phase: 05.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/setup-ingestion-iam.sh
  - scripts/deploy-ingestion.sh
  - scripts/smoke-ingestion.sh
  - ops/alert-ingestion-failure.yaml
  - .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md
  - workers/ingestion/DEPLOY.md
autonomous: true
requirements:
  - INFR-01
  - INFR-02
user_setup: []
tags:
  - gcp
  - cloud-run-jobs
  - cloud-scheduler
  - secret-manager
  - ingestion
  - deploy-scripts

must_haves:
  truths:
    - "Wave 1 이후 단계가 실행할 모든 bash/YAML 스크립트 파일이 리포에 존재한다"
    - "각 스크립트가 `bash -n`(syntax check)을 통과한다"
    - "deploy-ingestion.sh는 deploy-server.sh와 동일한 7-section 구조로 작성되어 diff 리뷰가 가능하다"
    - "DEPLOY-LOG.md 템플릿이 존재해 Wave 5에서 값만 채우면 된다"
  artifacts:
    - path: "scripts/setup-ingestion-iam.sh"
      provides: "API enable + SA 2종(ingestion/scheduler) + Secret 4종 생성 (idempotent)"
      contains: "gcloud services enable"
    - path: "scripts/deploy-ingestion.sh"
      provides: "Docker build(amd64) → push → Cloud Run Job deploy → IAM invoker binding → Scheduler create|update → smoke"
      contains: "gcloud run jobs deploy"
    - path: "scripts/smoke-ingestion.sh"
      provides: "Job execute --wait + Supabase stocks freshness + Cloud Logging grep + Scheduler ENABLED 검증"
      contains: "gcloud run jobs execute"
    - path: "ops/alert-ingestion-failure.yaml"
      provides: "Cloud Monitoring alert policy 선언(실패 카운터 > 0 트리거)"
      contains: "run.googleapis.com/job/completed_execution_count"
    - path: ".planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md"
      provides: "Wave 5에서 채울 실배포 기록 템플릿 (Phase 2 패턴 복사)"
      contains: "IMAGE="
  key_links:
    - from: "scripts/deploy-ingestion.sh"
      to: "scripts/smoke-ingestion.sh"
      via: "Section 7 bash 호출"
      pattern: "bash \"\\$\\(dirname \"\\$0\"\\)/smoke-ingestion.sh\""
    - from: "scripts/deploy-ingestion.sh"
      to: "workers/ingestion/Dockerfile"
      via: "-f workers/ingestion/Dockerfile"
      pattern: "workers/ingestion/Dockerfile"
    - from: "scripts/setup-ingestion-iam.sh"
      to: "Secret Manager (gh-radar-kis-*, gh-radar-supabase-service-role)"
      via: "gcloud secrets create/versions add"
      pattern: "gh-radar-kis-app-key"
---

<objective>
Phase 05.1의 운영 리소스 배포에 필요한 모든 bash 스크립트·YAML·문서 템플릿을 **한 번에 스캐폴드**한다. Wave 1 이후 단계는 이 파일들을 실행/참조만 하므로, 본 Wave는 실제 GCP 호출 없이 파일 작성 + `bash -n` 구문 검사만 수행한다.

Purpose: Wave 1~5의 실배포 단계가 "이미 작성된 스크립트를 그대로 실행"만 하도록 만들어 실수 여지를 제거하고, deploy-server.sh와 나란히 두어 diff 리뷰가 가능하게 한다.

Output:
- `scripts/setup-ingestion-iam.sh` (신규) — 1회성 API/SA/Secret 생성
- `scripts/deploy-ingestion.sh` (신규) — 반복 배포 (7-section 미러)
- `scripts/smoke-ingestion.sh` (신규) — E2E 검증
- `ops/alert-ingestion-failure.yaml` (신규) — alert policy 선언
- `.planning/phases/05.1-*/05.1-DEPLOY-LOG.md` (신규) — 실배포 기록 템플릿
- `workers/ingestion/DEPLOY.md` — deprecated 헤더 추가
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-CONTEXT.md
@.planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-RESEARCH.md
@.planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-VALIDATION.md
@scripts/deploy-server.sh
@scripts/smoke-server.sh
@workers/ingestion/Dockerfile
@workers/ingestion/src/index.ts
@workers/ingestion/src/config.ts
@.planning/phases/02-backend-api/DEPLOY-LOG.md

<interfaces>
<!-- Phase 1 ingestion env 계약 (workers/ingestion/src/config.ts) — 스크립트가 주입해야 하는 env 이름 -->
```typescript
// 필수 env (Secret Manager로 주입)
process.env.SUPABASE_SERVICE_ROLE_KEY
process.env.KIS_APP_KEY
process.env.KIS_APP_SECRET
process.env.KIS_ACCOUNT_NUMBER   // 포맷: XXXXXXXX-XX

// 필수 env (평문)
process.env.SUPABASE_URL
process.env.KIS_BASE_URL         // 기본 "https://openapi.koreainvestment.com:9443"
process.env.LOG_LEVEL            // 기본 "info"
process.env.APP_VERSION          // GIT_SHA 주입 (Phase 2 D-35 승계)
```

<!-- Phase 1 ingestion 로그 메시지 (workers/ingestion/src/index.ts) — smoke에서 grep 대상 -->
```
"starting ingestion cycle"
"non-trading day detected, exiting"   // 휴장일 early-exit 증거
"cycle complete"                        // 정상 종료 증거
"cycle failed"                          // 실패 증거 (smoke에서 0건 검증)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/setup-ingestion-iam.sh 작성 — API enable + SA 2종 + Secret 4종 (idempotent)</name>
  <files>scripts/setup-ingestion-iam.sh</files>
  <read_first>
    - scripts/deploy-server.sh (Section 1 가드 패턴)
    - .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-RESEARCH.md §"Pattern 3" + §"Pattern 4"
    - .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-CONTEXT.md §"Secret / 환경변수 명세"(D-09, D-11)
  </read_first>
  <action>
신규 파일 `scripts/setup-ingestion-iam.sh`를 **실행 권한 포함(chmod +x 불필요 — Phase 2와 동일하게 `bash` 명시 호출 전제)**으로 작성. 섹션 구조는 deploy-server.sh를 미러:

```bash
#!/usr/bin/env bash
set -euo pipefail

# ═══ Section 1: gcloud guard (deploy-server.sh와 동일 패턴) ═══
EXPECTED_PROJECT="${GCP_PROJECT_ID:-}"
EXPECTED_CONFIG="gh-radar"
[[ -z "$EXPECTED_PROJECT" ]] && { echo "ERROR: GCP_PROJECT_ID env var is required" >&2; exit 1; }
ACTIVE_CONFIG=$(gcloud config configurations list --filter='IS_ACTIVE=true' --format='value(name)')
ACTIVE_PROJECT=$(gcloud config get-value project 2>/dev/null || true)
[[ "$ACTIVE_CONFIG" != "$EXPECTED_CONFIG" ]] && { echo "ERROR: active config is '$ACTIVE_CONFIG', expected '$EXPECTED_CONFIG'" >&2; exit 1; }
[[ "$ACTIVE_PROJECT" != "$EXPECTED_PROJECT" ]] && { echo "ERROR: active project is '$ACTIVE_PROJECT', expected '$EXPECTED_PROJECT'" >&2; exit 1; }
echo "✓ gcloud guard: config=$ACTIVE_CONFIG, project=$ACTIVE_PROJECT"

# ═══ Section 2: 필수 입력 env 검증 ═══
: "${KIS_APP_KEY:?KIS_APP_KEY must be set}"
: "${KIS_APP_SECRET:?KIS_APP_SECRET must be set}"
: "${KIS_ACCOUNT_NUMBER:?KIS_ACCOUNT_NUMBER must be set (format XXXXXXXX-XX)}"
: "${SUPABASE_SERVICE_ROLE_KEY:?SUPABASE_SERVICE_ROLE_KEY must be set}"

# ═══ Section 3: API enable (idempotent; enabled면 no-op) ═══
echo "▶ enabling required APIs..."
gcloud services enable \
  run.googleapis.com \
  cloudscheduler.googleapis.com \
  secretmanager.googleapis.com \
  monitoring.googleapis.com \
  artifactregistry.googleapis.com \
  iam.googleapis.com

# ═══ Section 4: gcloud alpha/beta 컴포넌트 설치 (monitoring policies용) ═══
gcloud alpha --help >/dev/null 2>&1 || gcloud components install alpha -q
gcloud beta  --help >/dev/null 2>&1 || gcloud components install beta  -q

# ═══ Section 5: Service Account 2종 생성 (D-07, D-11) ═══
for SA in gh-radar-scheduler-sa gh-radar-ingestion-sa; do
  if gcloud iam service-accounts describe "${SA}@${EXPECTED_PROJECT}.iam.gserviceaccount.com" >/dev/null 2>&1; then
    echo "✓ SA exists: $SA"
  else
    gcloud iam service-accounts create "$SA" --display-name="gh-radar ${SA}"
    echo "✓ SA created: $SA"
  fi
done

# ═══ Section 6: Secret Manager 4종 (D-09) ═══
create_or_update_secret() {
  local name="$1" value="$2"
  if ! gcloud secrets describe "$name" >/dev/null 2>&1; then
    gcloud secrets create "$name" --replication-policy=automatic
    echo "✓ secret created: $name"
  fi
  printf '%s' "$value" | gcloud secrets versions add "$name" --data-file=-
  echo "✓ secret version added: $name"
}

create_or_update_secret gh-radar-kis-app-key        "$KIS_APP_KEY"
create_or_update_secret gh-radar-kis-app-secret     "$KIS_APP_SECRET"
create_or_update_secret gh-radar-kis-account-number "$KIS_ACCOUNT_NUMBER"

# gh-radar-supabase-service-role: Phase 2에서 이미 생성. 없으면 새로 만들고, 있으면 동일 값이면 스킵.
if gcloud secrets describe gh-radar-supabase-service-role >/dev/null 2>&1; then
  echo "✓ secret reused (Phase 2): gh-radar-supabase-service-role"
else
  create_or_update_secret gh-radar-supabase-service-role "$SUPABASE_SERVICE_ROLE_KEY"
fi

# ═══ Section 7: Secret accessor 바인딩 (런타임 SA → 4개 secret) ═══
for SECRET in gh-radar-kis-app-key gh-radar-kis-app-secret \
              gh-radar-kis-account-number gh-radar-supabase-service-role; do
  gcloud secrets add-iam-policy-binding "$SECRET" \
    --member="serviceAccount:gh-radar-ingestion-sa@${EXPECTED_PROJECT}.iam.gserviceaccount.com" \
    --role=roles/secretmanager.secretAccessor >/dev/null
  echo "✓ secretAccessor bound: $SECRET"
done

echo ""
echo "✅ setup-ingestion-iam.sh complete"
echo "Next: bash scripts/deploy-ingestion.sh"
```

**주의사항:**
- `roles/run.invoker` 바인딩은 **Job이 존재해야 가능** → 이 스크립트에는 포함 **안 함**. deploy-ingestion.sh Section 5.5에서 처리(CONTEXT D-07 + RESEARCH Pattern 3 주의).
- Secret 값은 전부 **env로만 주입** — 커맨드라인 인자 금지, 스크립트 하드코딩 금지 (RESEARCH Pattern 4 보안 원칙).
- `--replication-policy=automatic`을 secrets create에 반드시 포함 (RESEARCH Pitfall 8).
  </action>
  <verify>
    <automated>bash -n scripts/setup-ingestion-iam.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/setup-ingestion-iam.sh` → 0
    - `bash -n scripts/setup-ingestion-iam.sh` → exit 0 (구문 검사 통과)
    - `grep -c 'gcloud services enable' scripts/setup-ingestion-iam.sh` ≥ 1
    - `grep -c 'gh-radar-scheduler-sa' scripts/setup-ingestion-iam.sh` ≥ 1
    - `grep -c 'gh-radar-ingestion-sa' scripts/setup-ingestion-iam.sh` ≥ 1
    - `grep -c 'gh-radar-kis-app-key' scripts/setup-ingestion-iam.sh` ≥ 1
    - `grep -c 'gh-radar-kis-app-secret' scripts/setup-ingestion-iam.sh` ≥ 1
    - `grep -c 'gh-radar-kis-account-number' scripts/setup-ingestion-iam.sh` ≥ 1
    - `grep -c 'gh-radar-supabase-service-role' scripts/setup-ingestion-iam.sh` ≥ 1
    - `grep -c 'replication-policy=automatic' scripts/setup-ingestion-iam.sh` ≥ 1
    - `grep -c 'secretmanager.secretAccessor' scripts/setup-ingestion-iam.sh` ≥ 1
    - `grep -qE 'EXPECTED_CONFIG=(\"|'\'')?gh-radar' scripts/setup-ingestion-iam.sh` → 0
    - `grep -c 'roles/run.invoker' scripts/setup-ingestion-iam.sh` == 0 (invoker는 deploy-ingestion.sh에서 처리)
    - VALIDATION Row 05.1-01-02, 05.1-01-03 준비 완료
  </acceptance_criteria>
  <done>
파일 존재, 구문 검사 통과, 모든 Secret/SA 이름이 CONTEXT D-07/D-09/D-11과 정확히 일치, invoker 바인딩이 이 스크립트에 **없음**.
  </done>
</task>

<task type="auto">
  <name>Task 2: scripts/deploy-ingestion.sh 작성 — 7-section 구조 (Build → Push → Job deploy → Invoker binding → Scheduler create|update → Smoke)</name>
  <files>scripts/deploy-ingestion.sh</files>
  <read_first>
    - scripts/deploy-server.sh (미러 대상 — 모든 섹션 구조 동일)
    - .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-RESEARCH.md §"Example 1"
    - .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-CONTEXT.md §"배포 스크립트 + Cloud Run Job 설정"(D-01~D-05, D-10)
    - .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-RESEARCH.md §"Pitfall 2"(OAuth vs OIDC 정정)
    - workers/ingestion/Dockerfile
  </read_first>
  <action>
신규 파일 `scripts/deploy-ingestion.sh` 작성. 섹션 구조는 deploy-server.sh와 **완전 동일한 7 섹션**. 차이점만 교체:

```bash
#!/usr/bin/env bash
set -euo pipefail

# ═══ Section 1: 가드 (deploy-server.sh와 동일) ═══
EXPECTED_PROJECT="${GCP_PROJECT_ID:-}"
EXPECTED_CONFIG="gh-radar"
[[ -z "$EXPECTED_PROJECT" ]] && { echo "ERROR: GCP_PROJECT_ID env var is required" >&2; echo "Hint: export GCP_PROJECT_ID=<your-project-id>" >&2; exit 1; }
ACTIVE_CONFIG=$(gcloud config configurations list --filter='IS_ACTIVE=true' --format='value(name)')
ACTIVE_PROJECT=$(gcloud config get-value project 2>/dev/null || true)
[[ "$ACTIVE_CONFIG"  != "$EXPECTED_CONFIG"  ]] && { echo "ERROR: active config '$ACTIVE_CONFIG' != '$EXPECTED_CONFIG'" >&2; exit 1; }
[[ "$ACTIVE_PROJECT" != "$EXPECTED_PROJECT" ]] && { echo "ERROR: active project '$ACTIVE_PROJECT' != '$EXPECTED_PROJECT'" >&2; exit 1; }

# 선행 리소스(SA) 존재 확인 — 없으면 setup-ingestion-iam.sh 먼저 실행
for SA in gh-radar-scheduler-sa gh-radar-ingestion-sa; do
  gcloud iam service-accounts describe "${SA}@${EXPECTED_PROJECT}.iam.gserviceaccount.com" >/dev/null 2>&1 \
    || { echo "ERROR: SA '$SA' not found. Run: bash scripts/setup-ingestion-iam.sh" >&2; exit 1; }
done
echo "✓ gcloud guard + SA check"

# ═══ Section 2: 변수 ═══
JOB=gh-radar-ingestion
REGION=asia-northeast3
REPO=gh-radar
SHA=$(git rev-parse --short HEAD)
REGISTRY="${REGION}-docker.pkg.dev/${EXPECTED_PROJECT}/${REPO}"
IMAGE="${REGISTRY}/ingestion:${SHA}"
IMAGE_LATEST="${REGISTRY}/ingestion:latest"
: "${SUPABASE_URL:?SUPABASE_URL must be set (export or source .env.deploy)}"
echo "✓ variables: SHA=$SHA, IMAGE=$IMAGE"

# ═══ Section 3: Build (amd64 + GIT_SHA) ═══
echo "▶ docker build..."
docker build \
  --platform=linux/amd64 \
  --build-arg "GIT_SHA=${SHA}" \
  -f workers/ingestion/Dockerfile \
  -t "$IMAGE" \
  -t "$IMAGE_LATEST" \
  .

# ═══ Section 4: Push ═══
echo "▶ docker push..."
docker push "$IMAGE"
docker push "$IMAGE_LATEST"

# ═══ Section 5: Deploy Job (D-02, D-03, D-10, D-11) ═══
echo "▶ gcloud run jobs deploy..."
gcloud run jobs deploy "$JOB" \
  --image="$IMAGE" \
  --region="$REGION" \
  --service-account="gh-radar-ingestion-sa@${EXPECTED_PROJECT}.iam.gserviceaccount.com" \
  --cpu=1 \
  --memory=512Mi \
  --task-timeout=120s \
  --max-retries=0 \
  --parallelism=1 \
  --tasks=1 \
  --set-env-vars="^@^SUPABASE_URL=${SUPABASE_URL}@KIS_BASE_URL=https://openapi.koreainvestment.com:9443@LOG_LEVEL=info@APP_VERSION=${SHA}" \
  --set-secrets="KIS_APP_KEY=gh-radar-kis-app-key:latest,KIS_APP_SECRET=gh-radar-kis-app-secret:latest,KIS_ACCOUNT_NUMBER=gh-radar-kis-account-number:latest,SUPABASE_SERVICE_ROLE_KEY=gh-radar-supabase-service-role:latest"

# ═══ Section 5.5: Scheduler SA → Job invoker (D-07; 리소스 단위) ═══
gcloud run jobs add-iam-policy-binding "$JOB" \
  --region="$REGION" \
  --member="serviceAccount:gh-radar-scheduler-sa@${EXPECTED_PROJECT}.iam.gserviceaccount.com" \
  --role=roles/run.invoker >/dev/null
echo "✓ run.invoker bound: gh-radar-scheduler-sa"

# ═══ Section 6: Scheduler create-or-update (D-02, D-06, D-08; OAuth ≠ OIDC) ═══
JOB_URI="https://${REGION}-run.googleapis.com/apis/run.googleapis.com/v1/namespaces/${EXPECTED_PROJECT}/jobs/${JOB}:run"
SCHED=gh-radar-ingestion-scheduler
SCHED_SA="gh-radar-scheduler-sa@${EXPECTED_PROJECT}.iam.gserviceaccount.com"

if gcloud scheduler jobs describe "$SCHED" --location="$REGION" >/dev/null 2>&1; then
  echo "▶ scheduler update..."
  gcloud scheduler jobs update http "$SCHED" \
    --location="$REGION" \
    --schedule="* 9-15 * * 1-5" \
    --time-zone="Asia/Seoul" \
    --uri="$JOB_URI" \
    --http-method=POST \
    --oauth-service-account-email="$SCHED_SA"
else
  echo "▶ scheduler create..."
  gcloud scheduler jobs create http "$SCHED" \
    --location="$REGION" \
    --schedule="* 9-15 * * 1-5" \
    --time-zone="Asia/Seoul" \
    --uri="$JOB_URI" \
    --http-method=POST \
    --oauth-service-account-email="$SCHED_SA"
fi

# ═══ Section 7: Smoke ═══
echo ""
echo "✓ Deployed: Cloud Run Job $JOB @ $IMAGE"
bash "$(dirname "$0")/smoke-ingestion.sh" "$JOB" "$REGION"

echo ""
echo "✅ deploy-ingestion.sh complete"
```

**필수 포인트:**
- **`--oauth-service-account-email`** 사용 (RESEARCH Pitfall 2 — CONTEXT D-07 정정). `--oidc-service-account-email` **사용 금지**.
- `--set-env-vars` delimiter `^@^` — URL 안의 `:` 충돌 회피 (deploy-server.sh 검증 패턴).
- `--task-timeout=120s` — 명시적 `s` 단위 (RESEARCH Pitfall 7).
- `-f workers/ingestion/Dockerfile` + build context는 프로젝트 루트(`.`) — pnpm workspace 구조 지원.
- Section 5.5 `run jobs add-iam-policy-binding`은 프로젝트 단위가 아닌 **리소스 단위** (RESEARCH Anti-Patterns 참조).
  </action>
  <verify>
    <automated>bash -n scripts/deploy-ingestion.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/deploy-ingestion.sh` → 0
    - `bash -n scripts/deploy-ingestion.sh` → exit 0
    - `grep -c 'gcloud run jobs deploy' scripts/deploy-ingestion.sh` == 1
    - `grep -c -- '--oauth-service-account-email' scripts/deploy-ingestion.sh` ≥ 1
    - `grep -c -- '--oidc-service-account-email' scripts/deploy-ingestion.sh` == 0  # OIDC 사용 금지
    - `grep -c -- '--task-timeout=120s' scripts/deploy-ingestion.sh` == 1
    - `grep -c -- '--parallelism=1' scripts/deploy-ingestion.sh` == 1
    - `grep -c -- '--max-retries=0' scripts/deploy-ingestion.sh` == 1
    - `grep -c -- '--platform=linux/amd64' scripts/deploy-ingestion.sh` == 1
    - `grep -c '\* 9-15 \* \* 1-5' scripts/deploy-ingestion.sh` ≥ 1
    - `grep -c 'Asia/Seoul' scripts/deploy-ingestion.sh` ≥ 1
    - `grep -c 'gh-radar-ingestion-sa@' scripts/deploy-ingestion.sh` ≥ 1
    - `grep -c 'gh-radar-scheduler-sa@' scripts/deploy-ingestion.sh` ≥ 1
    - `grep -c 'workers/ingestion/Dockerfile' scripts/deploy-ingestion.sh` == 1
    - `grep -c 'asia-northeast3-docker.pkg.dev' scripts/deploy-ingestion.sh` ≥ 1
    - `grep -c 'ingestion:\${SHA}\|ingestion:\$SHA' scripts/deploy-ingestion.sh` ≥ 0  # SHA 태깅 존재
    - `grep -c 'gh-radar-kis-app-key:latest' scripts/deploy-ingestion.sh` == 1
    - `grep -c 'gh-radar-kis-app-secret:latest' scripts/deploy-ingestion.sh` == 1
    - `grep -c 'gh-radar-kis-account-number:latest' scripts/deploy-ingestion.sh` == 1
    - `grep -c 'gh-radar-supabase-service-role:latest' scripts/deploy-ingestion.sh` == 1
    - `grep -c 'scheduler jobs describe' scripts/deploy-ingestion.sh` == 1
    - `grep -c 'scheduler jobs create http\|scheduler jobs update http' scripts/deploy-ingestion.sh` ≥ 2
    - `grep -c 'smoke-ingestion.sh' scripts/deploy-ingestion.sh` ≥ 1
    - VALIDATION Rows 05.1-02-01, 02-02, 02-03, 03-01, 03-02, 03-03 준비 완료
  </acceptance_criteria>
  <done>
deploy-server.sh와 7-section 구조 일치, OAuth SA 사용, 모든 리소스/시크릿/SA 이름이 CONTEXT와 바이트 단위 일치.
  </done>
</task>

<task type="auto">
  <name>Task 3: scripts/smoke-ingestion.sh + ops/alert-ingestion-failure.yaml + DEPLOY-LOG.md 템플릿 + DEPLOY.md deprecated 헤더</name>
  <files>
    scripts/smoke-ingestion.sh,
    ops/alert-ingestion-failure.yaml,
    .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md,
    workers/ingestion/DEPLOY.md
  </files>
  <read_first>
    - scripts/smoke-server.sh (미러 패턴 — check 함수, PASS/FAIL 집계)
    - .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-RESEARCH.md §"Example 2" + §"Pattern 5"
    - .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-CONTEXT.md §"검증 / 모니터링"(D-12~D-15)
    - .planning/phases/02-backend-api/DEPLOY-LOG.md (Phase 2 템플릿)
    - workers/ingestion/src/index.ts (로그 메시지 정확한 문자열)
    - workers/ingestion/DEPLOY.md (deprecated 대상)
  </read_first>
  <action>
**A. `scripts/smoke-ingestion.sh`** — smoke-server.sh 패턴 미러, 5개 INV 검증:

```bash
#!/usr/bin/env bash
set -uo pipefail
# 주의: -e 비활성 — 모든 INV를 돌리고 집계 (smoke-server.sh 패턴)

JOB="${1:-gh-radar-ingestion}"
REGION="${2:-asia-northeast3}"
PASS=0; FAIL=0; declare -a FAILED

check() {
  local name="$1"; shift
  echo -n "  $name ... "
  if "$@" >/dev/null 2>&1; then echo PASS; PASS=$((PASS+1));
  else echo FAIL; FAIL=$((FAIL+1)); FAILED+=("$name"); fi
}

echo "Smoke testing Job=$JOB Region=$REGION"
echo ""

# INV-1: Job 실행 완료 (exit 0)
check "INV-1 jobs execute --wait exit 0" \
  gcloud run jobs execute "$JOB" --region="$REGION" --wait

# INV-2: 최근 5분 로그에 "cycle complete" OR "non-trading day detected, exiting" (D-13 휴장일 분기)
check "INV-2 logs: cycle complete OR non-trading day" bash -c "
  gcloud logging read '
    resource.type=\"cloud_run_job\"
    AND resource.labels.job_name=\"$JOB\"
    AND (jsonPayload.msg=\"cycle complete\" OR jsonPayload.msg=\"non-trading day detected, exiting\")
  ' --freshness=5m --limit=5 --format='value(jsonPayload.msg)' | grep -Eq '(cycle complete|non-trading day)'
"

# INV-3: 최근 5분 실패 0건
check "INV-3 logs: no cycle failed / EGW00201" bash -c \"
  COUNT=\\\$(gcloud logging read '
    resource.type=\\\"cloud_run_job\\\"
    AND resource.labels.job_name=\\\"$JOB\\\"
    AND (jsonPayload.msg=\\\"cycle failed\\\" OR textPayload:\\\"EGW00201\\\")
  ' --freshness=5m --limit=1 --format='value(timestamp)' | wc -l)
  [ \\\"\\\$COUNT\\\" -eq 0 ]
\"

# INV-4: Supabase stocks freshness (거래일) OR 휴장일 log (WARN 허용, INV-2가 dominant)
#   Claude's Discretion: service_role 사용 (RESEARCH §Validation Architecture)
check "INV-4 Supabase stocks freshness or holiday" bash -c "
  : \${SUPABASE_URL:?}; : \${SUPABASE_SERVICE_ROLE_KEY:?}
  MAX_TS=\$(curl -fsS \"\${SUPABASE_URL}/rest/v1/stocks?select=updated_at&order=updated_at.desc&limit=1\" \
    -H \"apikey: \$SUPABASE_SERVICE_ROLE_KEY\" \
    -H \"Authorization: Bearer \$SUPABASE_SERVICE_ROLE_KEY\" | jq -r '.[0].updated_at')
  [ -z \"\$MAX_TS\" ] || [ \"\$MAX_TS\" = null ] && exit 1
  AGE=\$(( \$(date +%s) - \$(date -d \"\$MAX_TS\" +%s 2>/dev/null || date -jf '%Y-%m-%dT%H:%M:%S' \"\${MAX_TS%%.*}\" +%s) ))
  [ \"\$AGE\" -lt 120 ] || gcloud logging read '
    resource.type=\"cloud_run_job\" AND resource.labels.job_name=\"'\"$JOB\"'\"
    AND jsonPayload.msg=\"non-trading day detected, exiting\"
  ' --freshness=10m --limit=1 --format='value(timestamp)' | grep -q .
"

# INV-5: Scheduler ENABLED 상태
check "INV-5 scheduler ENABLED" bash -c "
  STATE=\$(gcloud scheduler jobs describe gh-radar-ingestion-scheduler --location=\"$REGION\" --format='value(state)')
  [ \"\$STATE\" = ENABLED ]
"

echo ""
echo "═══════════════════════════════════════"
echo "PASS: $PASS  FAIL: $FAIL"
[ "$FAIL" -eq 0 ] || { echo "Failed: ${FAILED[*]}"; exit 1; }
echo "✅ All smoke invariants passed"
```

(주: 위 heredoc 속 `check` 호출의 쉘 escape는 실제 작성 시 RESEARCH Example 2의 인라인 형태를 정확히 그대로 복사. 실행 검증 과정의 단일 `$` 이스케이프는 `bash -c "..."` 안에서 `\$` 변환. 실행 오류 시 RESEARCH Example 2 원문을 1:1 복사.)

**B. `ops/alert-ingestion-failure.yaml`** — Wave 3에서 `gcloud alpha monitoring policies create --policy-from-file=...`로 import. `${NOTIFICATION_CHANNEL_ID}` 치환은 배포 시점에 sed로 수행:

```yaml
displayName: "gh-radar-ingestion-failure"
documentation:
  content: |
    Cloud Run Job `gh-radar-ingestion` 실행이 최근 5분 내 실패했습니다.
    - Cloud Logging에서 `jsonPayload.msg="cycle failed"` 확인
    - KIS API(EGW00201 rate limit) / Supabase 접근 / task_timeout 초과 여부 점검
    - Runbook: .planning/phases/05.1-*/05.1-DEPLOY-LOG.md §운영 참고
  mimeType: text/markdown
combiner: OR
conditions:
  - displayName: "gh-radar-ingestion 실행 실패 발생"
    conditionThreshold:
      filter: |
        resource.type = "cloud_run_job"
        AND resource.labels.job_name = "gh-radar-ingestion"
        AND metric.type = "run.googleapis.com/job/completed_execution_count"
        AND metric.labels.result = "failed"
      comparison: COMPARISON_GT
      thresholdValue: 0
      duration: 0s
      aggregations:
        - alignmentPeriod: 300s
          perSeriesAligner: ALIGN_SUM
alertStrategy:
  autoClose: 3600s
notificationChannels:
  - "${NOTIFICATION_CHANNEL_ID}"
```

**C. `.planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md` 템플릿** — Phase 2 구조 복사, Wave 5에서 실값 채움:

```markdown
# Phase 05.1 — Cloud Run Job + Scheduler 배포 로그

- **배포 일시 (UTC):** TBD
- **GCP 프로젝트:** `gh-radar`
- **리전:** `asia-northeast3`
- **Cloud Run Job:** `gh-radar-ingestion`
- **이미지:** `IMAGE=asia-northeast3-docker.pkg.dev/gh-radar/gh-radar/ingestion:<SHA>`
- **Scheduler:** `gh-radar-ingestion-scheduler` (cron `* 9-15 * * 1-5` KST)

## Service Accounts

| Role | Email | 부여 권한 |
|------|-------|-----------|
| Job runtime | `gh-radar-ingestion-sa@gh-radar.iam.gserviceaccount.com` | `roles/secretmanager.secretAccessor` × 4 secrets |
| Scheduler invoker | `gh-radar-scheduler-sa@gh-radar.iam.gserviceaccount.com` | `roles/run.invoker` on Job `gh-radar-ingestion` (리소스 단위) |

## Secrets

| Secret ID | ENV Var | 생성/재사용 |
|-----------|---------|------------|
| `gh-radar-kis-app-key` | `KIS_APP_KEY` | 신규 |
| `gh-radar-kis-app-secret` | `KIS_APP_SECRET` | 신규 |
| `gh-radar-kis-account-number` | `KIS_ACCOUNT_NUMBER` | 신규 |
| `gh-radar-supabase-service-role` | `SUPABASE_SERVICE_ROLE_KEY` | Phase 2 재사용 |

## 평문 ENV

| Key | Value |
|-----|-------|
| `SUPABASE_URL` | TBD |
| `KIS_BASE_URL` | `https://openapi.koreainvestment.com:9443` |
| `LOG_LEVEL` | `info` |
| `APP_VERSION` | `<GIT_SHA>` |

## Job 구성

| 항목 | 값 |
|------|-----|
| CPU | 1 |
| Memory | 512Mi |
| task-timeout | 120s |
| max-retries | 0 |
| parallelism | 1 |
| tasks | 1 |

## Alert policy

- **displayName:** `gh-radar-ingestion-failure`
- **Policy ID:** TBD (`gcloud alpha monitoring policies list ...`)
- **Notification channel:** email TBD (`gcloud beta monitoring channels list ...`)
- **조건:** `metric.labels.result="failed" AND completed_execution_count > 0` (5분 윈도우)

## INV 검증 결과 (smoke-ingestion.sh)

| INV | 내용 | 결과 |
|-----|------|------|
| INV-1 | `jobs execute --wait` exit 0 | TBD |
| INV-2 | logs: `cycle complete` OR `non-trading day detected, exiting` | TBD |
| INV-3 | logs: `cycle failed`/`EGW00201` 0건 | TBD |
| INV-4 | Supabase `stocks.max(updated_at)` < 120s OR 휴장일 로그 | TBD |
| INV-5 | Scheduler `state=ENABLED` | TBD |

## Manual 검증 (DEPLOY 후 관찰)

| 항목 | 관찰 시점 | 결과 |
|------|----------|------|
| 첫 거래일 월요일 09:00 KST 토큰 재발급 | 배포 후 첫 월요일 | TBD |
| 이메일 알림 실제 수신 (강제 실패 테스트) | 배포 직후 | TBD |
| Cloud Logging secret 평문 노출 0건 (T-05.1-04) | 배포 후 1일 | TBD |
| execution overlap 발생 여부 (Pitfall 1) | 첫 주 | TBD |

## 배포 중 조우한 이슈와 해결

(실배포 시 기록)

## 운영 참고

- **재배포:** `bash scripts/deploy-ingestion.sh`
- **Scheduler pause:** `gcloud scheduler jobs pause gh-radar-ingestion-scheduler --location=asia-northeast3`
- **로그:** `gcloud logging read 'resource.type="cloud_run_job" AND resource.labels.job_name="gh-radar-ingestion"' --freshness=1h`
- **콘솔:** https://console.cloud.google.com/run/jobs/details/asia-northeast3/gh-radar-ingestion?project=gh-radar
```

**D. `workers/ingestion/DEPLOY.md` 상단에 deprecated 헤더 추가** (CONTEXT §Specific Ideas "DEPLOY.md는 보존하되 deprecated 표기"):

파일 최상단(기존 `# Cloud Run Job 배포 가이드` 위)에 2~3줄 삽입:

```markdown
> **DEPRECATED (Phase 05.1):** 이 문서는 Phase 1 초안 배포 가이드입니다.
> 실제 배포는 `scripts/deploy-ingestion.sh` (및 선행 `scripts/setup-ingestion-iam.sh`)를 사용하세요.
> 본 파일은 명령어 참조용으로 보존됩니다.
```

기존 내용은 변경하지 않음.
  </action>
  <verify>
    <automated>bash -n scripts/smoke-ingestion.sh && test -f ops/alert-ingestion-failure.yaml && test -f .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md && grep -q 'DEPRECATED' workers/ingestion/DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/smoke-ingestion.sh` → 0
    - `bash -n scripts/smoke-ingestion.sh` → exit 0
    - `grep -c 'gcloud run jobs execute' scripts/smoke-ingestion.sh` ≥ 1
    - `grep -c 'cycle complete' scripts/smoke-ingestion.sh` ≥ 1
    - `grep -c 'non-trading day detected, exiting' scripts/smoke-ingestion.sh` ≥ 1
    - `grep -c 'cycle failed' scripts/smoke-ingestion.sh` ≥ 1
    - `grep -c 'EGW00201' scripts/smoke-ingestion.sh` ≥ 1
    - `grep -c 'gh-radar-ingestion-scheduler' scripts/smoke-ingestion.sh` ≥ 1
    - `grep -c 'rest/v1/stocks' scripts/smoke-ingestion.sh` ≥ 1
    - `test -f ops/alert-ingestion-failure.yaml` → 0
    - `grep -c 'run.googleapis.com/job/completed_execution_count' ops/alert-ingestion-failure.yaml` == 1
    - `grep -c 'result = "failed"' ops/alert-ingestion-failure.yaml` ≥ 1 (또는 `result = \"failed\"`)
    - `grep -c 'NOTIFICATION_CHANNEL_ID' ops/alert-ingestion-failure.yaml` ≥ 1
    - `grep -c 'displayName: "gh-radar-ingestion-failure"' ops/alert-ingestion-failure.yaml` == 1
    - `test -f .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md` → 0
    - `grep -c 'IMAGE=' .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md` ≥ 1
    - `grep -c 'gh-radar-ingestion-sa' .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md` ≥ 1
    - `grep -c 'gh-radar-scheduler-sa' .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md` ≥ 1
    - `grep -c 'INV-5' .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md` ≥ 1
    - `head -5 workers/ingestion/DEPLOY.md | grep -c 'DEPRECATED'` ≥ 1
    - VALIDATION Rows 05.1-04-01, 04-02, 05-01~04, 06-01 준비 완료
  </acceptance_criteria>
  <done>
세 스크립트 모두 작성·lint 통과, alert YAML은 placeholder 유지, DEPLOY-LOG 템플릿은 Wave 5에서 채울 모든 필드 `TBD` 표기, DEPLOY.md deprecated 헤더 존재.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 개발자 셸 → 리포 (git commit) | 사용자 입력 env (KIS/Supabase secrets)가 이 단계에서는 **파일에 쓰이지 않고** setup 스크립트 실행 시점에만 env로 주입. 스크립트 코드 내 하드코딩 금지가 핵심. |
| YAML 템플릿 (ops/) → GCP API | `${NOTIFICATION_CHANNEL_ID}` 같은 placeholder는 치환 전 커밋 가능. 치환 결과는 `/tmp/`에서 수명 짧음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-01 | Information Disclosure | `scripts/setup-ingestion-iam.sh` | mitigate | Secret 값을 env로만 수신(`: "${KIS_APP_KEY:?}"`), 커맨드라인/파일 하드코딩 금지. `gcloud secrets versions add --data-file=-` stdin 사용으로 프로세스 리스트 노출 방지. |
| T-05.1-02 | Elevation of Privilege | `scripts/setup-ingestion-iam.sh` (SA 분리) | mitigate | Scheduler SA와 Runtime SA를 **물리적으로 분리** (D-11). Runtime SA에 `roles/secretmanager.secretAccessor`만 부여(프로젝트 레벨 바인딩 금지). |
| T-05.1-03 | Spoofing | `scripts/deploy-ingestion.sh` §Section 6 (Scheduler 인증) | mitigate | `--oauth-service-account-email` 강제 사용(RESEARCH Pitfall 2). 리소스 단위 `roles/run.invoker` 바인딩만 허용(§Section 5.5) — 프로젝트 단위 금지. |
| T-05.1-04 | Information Disclosure | `workers/ingestion/` 로그 | accept | Phase 1 pino redact 기설정 신뢰(코드 변경 없음). 본 Wave 0는 스크립트만 작성 — 새 로그 포인트 없음. Manual 검증 항목으로 Cloud Logging grep 포함. |
| T-05.1-05 | Tampering | `scripts/deploy-ingestion.sh` 이미지 태깅 | mitigate | `:SHA` + `:latest` 이중 태그 + `git rev-parse --short HEAD`로 불변 포인터. 실배포 시 Cloud Run revision에는 `:SHA` 바인딩하고 `:latest`는 추적용. |
| T-05.1-06 | Tampering | `ops/alert-ingestion-failure.yaml` placeholder | accept | `${NOTIFICATION_CHANNEL_ID}` 치환 전 파일은 리포에 존재하나 실제 policy 생성에는 치환 후 `/tmp/` 복사본 사용. 커밋된 파일에 채널 ID 자체가 노출되지 않음. |
</threat_model>

<verification>
Wave 0는 **파일 작성만 수행 — GCP API 호출 없음**.

```bash
# 구문 검사 (필수)
bash -n scripts/setup-ingestion-iam.sh
bash -n scripts/deploy-ingestion.sh
bash -n scripts/smoke-ingestion.sh

# 파일 존재 확인
test -f ops/alert-ingestion-failure.yaml
test -f .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md

# 보안 레드라인 (하드코딩 금지 증명)
! grep -nE '(PSS[A-Za-z0-9]{20,}|eyJhbGci)' scripts/setup-ingestion-iam.sh scripts/deploy-ingestion.sh scripts/smoke-ingestion.sh
# ↑ KIS app key prefix 'PS' + JWT prefix 'eyJhbGci' — 어느 스크립트에도 하드코딩되면 안 됨

# OIDC 오사용 방지
! grep -n -- '--oidc-service-account-email' scripts/deploy-ingestion.sh
```

Wave 0 완료 후 다음 Plan(02)은 `bash scripts/setup-ingestion-iam.sh` 실행.
</verification>

<success_criteria>
- 5개 신규 파일 + DEPLOY.md deprecated 헤더 추가 완료
- 모든 `bash -n` 통과
- deploy-ingestion.sh가 deploy-server.sh와 7-section 구조 1:1 매핑(섹션 헤더 주석 포함)
- `--oauth-service-account-email` 사용 확인, `--oidc-service-account-email` 0건
- 모든 Secret/SA 이름 CONTEXT D-07/D-09/D-11와 바이트 단위 일치
- Smoke 스크립트가 workers/ingestion/src/index.ts의 로그 메시지 문자열(`cycle complete`, `non-trading day detected, exiting`, `cycle failed`)과 정확히 일치
- VALIDATION Rows 01-01, 02-01~03, 03-01~03, 04-01~02, 05-01~04, 06-01의 검증 명령이 **파일 단위로 실행 가능한 상태** 도달
</success_criteria>

<output>
완료 후 `.planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-01-SUMMARY.md`에 기록:
- 각 파일의 실제 줄 수
- `bash -n` 결과
- deploy-server.sh와의 섹션 구조 diff 요약(덧붙여진 섹션 = Section 5.5 IAM binding + Section 6 Scheduler)
- 의도적으로 **보류**한 항목 (invoker 바인딩은 setup이 아닌 deploy에 위치)
</output>
