# Plan 05.1-04 Summary — Wave 3: Alert Policy + Email Notification Channel

**Executed:** 2026-04-14
**Status:** COMPLETE
**Executor:** human-action (Alex)

## 실행 내역

이메일 채널 생성(`alex@jx1.io`) → `ops/alert-ingestion-failure.yaml` placeholder 치환 → `gcloud alpha monitoring policies create` 실행.

## Acceptance Criteria 검증

| # | 기준 | 결과 |
|---|------|------|
| V1 | Email notification channel 존재 | ✅ `projects/gh-radar/notificationChannels/14409521670382124894` |
| V2 | Policy `gh-radar-ingestion-failure` ENABLED | ✅ `True` |
| V3 | Policy filter = Cloud Run Job failed executions | ✅ `metric.type=run.googleapis.com/job/completed_execution_count` + `metric.labels.result="failed"` |
| V4 | 채널 연결됨 | ✅ policy `notificationChannels` 배열에 channel resource name 포함 |

## 생성 리소스

```
Notification channel (email):
  projects/gh-radar/notificationChannels/14409521670382124894
  - displayName: "gh-radar ops email"
  - email: alex@jx1.io
  - enabled: true (verification 메일 클릭 필요 — 사용자 inbox 확인 예정)

Alert policy:
  projects/gh-radar/alertPolicies/8385793339456322031
  - displayName: gh-radar-ingestion-failure
  - condition: run.googleapis.com/job/completed_execution_count with result=failed > 0 over 5m
  - combiner: OR
  - alertStrategy.autoClose: 3600s
  - notificationChannels: [email channel 위]
```

## 미완료 작업 (후속)

- **Email verification**: 사용자가 `alex@jx1.io` inbox에서 "Verify notification channel" 링크 클릭 필요. 클릭 전까지는 channel이 unverified 상태라 알림이 실제로 전달되지 않을 수 있음. Wave 5 DEPLOY-LOG 기록 시 수동 검증 체크리스트에 포함.

## 다음 단계

Wave 4/5 smoke 재확인은 Wave 2에서 이미 자동 실행되어 PASS. Wave 5(문서 갱신)로 진행.
