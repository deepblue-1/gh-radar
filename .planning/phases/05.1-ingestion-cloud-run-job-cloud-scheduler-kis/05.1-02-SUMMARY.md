# Plan 05.1-02 Summary — Wave 1: setup-ingestion-iam.sh 실행

**Executed:** 2026-04-14
**Status:** COMPLETE
**Executor:** human-action (Alex)

## 실행 내역

환경: gcloud config=gh-radar, project=gh-radar, account=alex@jx1.io. 자격증명은 `workers/ingestion/.env`에서 주입(평문 로그/프로세스리스트 노출 없음).

명령:
```
set -a; source workers/ingestion/.env; set +a
export GCP_PROJECT_ID=gh-radar
bash scripts/setup-ingestion-iam.sh
```

## Acceptance Criteria 검증

| # | 기준 | 결과 |
|---|------|------|
| 1 | 6개 API enabled | ✅ `gcloud services enable ...` idempotent 성공 |
| 2 | gcloud alpha/beta 설치 | ✅ Section 4 통과 (Modules updated) |
| 3 | SA 2종 존재 | ✅ `gh-radar-scheduler-sa`, `gh-radar-ingestion-sa` 생성 확인 |
| 4 | Secret 4종 :latest | ✅ kis-app-key/app-secret/account-number 신규 v1, supabase-service-role Phase 2 재사용 |
| 5 | Runtime SA accessor 바인딩 | ✅ 4/4 secret에 `roles/secretmanager.secretAccessor` 부여 (`gh-radar-ingestion-sa`) |
| 6 | Secret 값 평문 노출 없음 | ✅ stdin(`--data-file=-`) 주입, 쉘 히스토리에는 `source` 명령만 기록 |

## 생성 리소스

```
gcloud iam service-accounts:
  - gh-radar-scheduler-sa@gh-radar.iam.gserviceaccount.com
  - gh-radar-ingestion-sa@gh-radar.iam.gserviceaccount.com

gcloud secrets (4):
  - gh-radar-kis-app-key         (v1, created)
  - gh-radar-kis-app-secret      (v1, created)
  - gh-radar-kis-account-number  (v1, created)
  - gh-radar-supabase-service-role (reused from Phase 2)

IAM bindings (4):
  secretmanager.secretAccessor → gh-radar-ingestion-sa@... on each secret above
```

## Deviations

없음. Plan 명세 그대로 실행.

## 다음 단계

Wave 2: `bash scripts/deploy-ingestion.sh` — Docker build + push + Cloud Run Job deploy + Scheduler + smoke.
