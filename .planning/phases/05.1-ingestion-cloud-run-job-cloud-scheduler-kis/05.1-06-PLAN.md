---
phase: 05.1
plan: 06
type: execute
wave: 5
depends_on: [05.1-01, 05.1-02, 05.1-03, 05.1-04, 05.1-05]
files_modified:
  - .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - INFR-01
  - INFR-02

tags:
  - docs
  - deploy-log
  - state
  - roadmap

must_haves:
  truths:
    - "DEPLOY-LOG.md의 모든 TBD 필드가 실제 값으로 치환됨 (배포 시각, SHA, 이미지 URI, SA/Secret/Policy ID, INV 결과)"
    - "STATE.md가 Phase 05.1 완료로 갱신됨 (progress counter, current focus)"
    - "ROADMAP.md의 Phase 05.1 항목이 `[x]` + Plans 리스트 + Success Criteria 검증 결과로 업데이트됨"
    - "REQUIREMENTS.md traceability 테이블에서 INFR-01, INFR-02 상태가 업데이트됨 (deploy 완료 반영)"
  artifacts:
    - path: ".planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md"
      provides: "실배포 기록 (SHA, 이미지, SA, Secret, Policy, INV 결과)"
      contains: "IMAGE=asia-northeast3-docker.pkg.dev"
    - path: ".planning/STATE.md"
      provides: "Phase 05.1 completion 반영"
      contains: "Phase 05.1"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 05.1 [x] 및 Success Criteria 검증"
      contains: "Phase 05.1"
  key_links:
    - from: "DEPLOY-LOG.md"
      to: "Plan 02/03/04/05 SUMMARY"
      via: "실배포 값 전이"
      pattern: "IMAGE|CHANNEL|POLICY"
---

<objective>
Plan 02~05에서 수집한 실배포 값을 `.planning/phases/05.1-*/05.1-DEPLOY-LOG.md`에 기록하고, STATE/ROADMAP/REQUIREMENTS를 Phase 05.1 완료로 갱신한다. 본 Plan은 문서 작업만 수행하므로 autonomous=true.

Purpose: Phase 05.1의 공식 완료 문서화. 다음 페이즈(6 이후) 작업자가 현재 운영 구성을 참조할 수 있게 한다.

Output:
- DEPLOY-LOG.md (완전히 채워진 상태)
- STATE.md 갱신
- ROADMAP.md 갱신
- REQUIREMENTS.md traceability 갱신
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md
@.planning/phases/02-backend-api/DEPLOY-LOG.md

<interfaces>
<!-- Plan 02~05 SUMMARY에서 전이할 값 -->
Plan 02 SUMMARY: (API enabled, SA 2종, Secret 4종 — 결과만)
Plan 03 SUMMARY: SHA, IMAGE URI, Job config snapshot, Scheduler URI, deploy timestamp
Plan 04 SUMMARY: CHANNEL_NAME, POLICY_NAME, verification timestamp
Plan 05 SUMMARY: smoke 결과 표, 거래일/휴장일 분기, 이메일 알림 수신 시각, overlap 관찰
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 05.1-DEPLOY-LOG.md의 TBD 필드를 실값으로 치환</name>
  <files>.planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md</files>
  <read_first>
    - .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-02-SUMMARY.md
    - .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-03-SUMMARY.md
    - .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-04-SUMMARY.md
    - .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-05-SUMMARY.md
    - .planning/phases/02-backend-api/DEPLOY-LOG.md (형식 참조)
  </read_first>
  <action>
`.planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md`의 **모든 `TBD` 토큰**을 실값으로 치환. Plan 02~05 SUMMARY에서 아래 값을 끌어옴:

**배포 헤더 (Plan 03 SUMMARY):**
- 배포 일시 (UTC + KST) — ISO-8601
- GCP 프로젝트 — `gh-radar`
- 리전 — `asia-northeast3`
- Cloud Run Job — `gh-radar-ingestion`
- 이미지 — `asia-northeast3-docker.pkg.dev/gh-radar/gh-radar/ingestion:<SHA>` (실제 SHA)
- Scheduler — `gh-radar-ingestion-scheduler` (cron, tz 재확인)

**Service Accounts (Plan 02 SUMMARY):**
- Job runtime SA: `gh-radar-ingestion-sa@gh-radar.iam.gserviceaccount.com` — secretAccessor × 4
- Scheduler invoker SA: `gh-radar-scheduler-sa@gh-radar.iam.gserviceaccount.com` — run.invoker on Job (리소스 단위)

**Secrets (Plan 02 SUMMARY):**
- 4개 Secret ID 테이블 — 생성/재사용 컬럼 채움

**평문 ENV (Plan 03 SUMMARY):**
- SUPABASE_URL, KIS_BASE_URL, LOG_LEVEL, APP_VERSION(=SHA) 실값

**Job 구성 (Plan 03):** cpu=1, memory=512Mi, task-timeout=120s, max-retries=0, parallelism=1, tasks=1 — 그대로 유지.

**Alert policy (Plan 04 SUMMARY):**
- displayName: `gh-radar-ingestion-failure`
- Policy ID: 실값 (`projects/gh-radar/alertPolicies/<ID>`)
- Notification channel: 실값 (`projects/gh-radar/notificationChannels/<ID>`)
- 조건 그대로 유지

**INV 검증 (Plan 05 SUMMARY):**
- INV-1~5 각 결과 `✅ PASS` + 증거(로그 타임스탬프 또는 updated_at 값)

**Manual 검증 (Plan 05 SUMMARY):**
- 거래일/휴장일 분기 결과
- 이메일 알림 수신 시각
- Cloud Logging secret 평문 노출 확인 결과
- execution overlap 관찰 결과 (Pitfall 1)

**배포 중 조우한 이슈와 해결:** Plan 02~05에서 발생한 실제 이슈(있으면) 또는 "특이 이슈 없음" 기록.

**운영 참고:** 섹션은 템플릿 그대로 유지, 필요 시 재배포 명령 + logging 쿼리 예시 추가.

재쓰기 후 `grep -c 'TBD' .planning/phases/05.1-*/05.1-DEPLOY-LOG.md`가 0이 되어야 한다.
  </action>
  <verify>
    <automated>grep -c 'TBD' .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md | awk '$1==0{exit 0}$1!=0{exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TBD' .planning/phases/05.1-*/05.1-DEPLOY-LOG.md` == 0
    - `grep -cE 'IMAGE=.*ingestion:[0-9a-f]{7,}' .planning/phases/05.1-*/05.1-DEPLOY-LOG.md` ≥ 1
    - `grep -c 'gh-radar-ingestion-sa@gh-radar' .planning/phases/05.1-*/05.1-DEPLOY-LOG.md` ≥ 1
    - `grep -c 'gh-radar-scheduler-sa@gh-radar' .planning/phases/05.1-*/05.1-DEPLOY-LOG.md` ≥ 1
    - `grep -cE 'alertPolicies/[0-9]+' .planning/phases/05.1-*/05.1-DEPLOY-LOG.md` ≥ 1
    - `grep -cE 'notificationChannels/[0-9]+' .planning/phases/05.1-*/05.1-DEPLOY-LOG.md` ≥ 1
    - `grep -c 'INV-1' .planning/phases/05.1-*/05.1-DEPLOY-LOG.md` ≥ 1
    - `grep -c 'INV-5' .planning/phases/05.1-*/05.1-DEPLOY-LOG.md` ≥ 1
    - `grep -cE '(PASS|FAIL)' .planning/phases/05.1-*/05.1-DEPLOY-LOG.md` ≥ 5   # 각 INV 결과
    - VALIDATION Row 05.1-06-01 ✅ green
  </acceptance_criteria>
  <done>
DEPLOY-LOG.md 전 필드 치환 완료, TBD 0건, Phase 2 DEPLOY-LOG.md 형식과 일관성 유지.
  </done>
</task>

<task type="auto">
  <name>Task 2: STATE.md + ROADMAP.md + REQUIREMENTS.md 갱신 — Phase 05.1 완료 반영</name>
  <files>
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/STATE.md (현재 상태)
    - .planning/ROADMAP.md §"Phase 05.1" (placeholder 치환 필요)
    - .planning/REQUIREMENTS.md §"Traceability"
    - .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md (완성된 로그)
  </read_first>
  <action>
**A. `.planning/STATE.md` 갱신:**

frontmatter:
- `status: verifying` → `verifying` 유지(최종 verify는 다음 단계), 단 `stopped_at: "Phase 05.1 complete — awaiting /gsd-verify-work"`
- `last_activity: 2026-04-14` → 실제 완료 날짜
- `progress.completed_phases: 2` → `3` (1, 2, 05.1까지 완료로 집계; Phase 3/4는 이미 complete이므로 기존 counter 방식 유지. 기존 구조에서 completed_phases=2이 틀려 보이나 현재 STATE.md 로직 따름 — 실질적으로 **Phase 05.1 추가 완료를 반영**해 1 증가)
- `progress.total_phases: 10` → `10` 유지 (05.1 insertion 포함)
- `progress.completed_plans` → 현재 값 + 6 (본 페이즈 plan 수)
- `progress.percent` → 재계산

본문:
- "Current Position" 블록 업데이트:
  - `Phase: 04 ...` → `Phase: 05.1 (ingestion-cloud-run-job-cloud-scheduler-kis) — ✅ COMPLETE`
  - `Plan: 1 of 1 ✅` → `Plan: 6 of 6 ✅`
  - `Plans completed: 9 ...` → `15 (기존 9 + Phase 05.1: 6)`
  - `Last activity`: 오늘 날짜
- 새 섹션 추가: "Phase 05.1 Success Criteria 검증" (아래 표 형식)

```markdown
## Phase 05.1 Success Criteria 검증

| # | 기준 | 상태 | 증거 |
|---|---|---|---|
| 1 | Cloud Run Job `gh-radar-ingestion` 배포됨 (config 맞음) | ✅ | Plan 03 V1~V4 · DEPLOY-LOG |
| 2 | Scheduler `gh-radar-ingestion-scheduler` ENABLED + OAuth SA | ✅ | Plan 03 V6~V8 |
| 3 | Secret 4종 + IAM 분리 | ✅ | Plan 02 V3~V5 |
| 4 | Alert policy + 이메일 채널 | ✅ | Plan 04 V1~V4 |
| 5 | smoke 5/5 PASS (cycle complete/휴장일/실패 0건) | ✅ | Plan 05 |
| 6 | 이메일 알림 실수신 (Manual) | ✅ | Plan 05 Manual |
```

- "Roadmap Evolution" 블록에 추가: `- Phase 05.1 완료 2026-04-XX: Cloud Run Job + Scheduler 가동, 평일 09:00~15:59 KST 매분 KIS→Supabase 폴링 활성화`

**B. `.planning/ROADMAP.md` §"Phase 05.1" 갱신:**

현재:
```
### Phase 05.1: Ingestion 운영 배포 ... (INSERTED)
**Goal:** [Urgent work - to be planned]
**Requirements**: TBD
**Depends on:** Phase 5
**Plans:** 0 plans

Plans:
- [ ] TBD (run /gsd-plan-phase 05.1 to break down)
```

다음으로 교체:
```
### Phase 05.1: Ingestion 운영 배포 — Cloud Run Job + Cloud Scheduler로 KIS 데이터 자동 수집 활성화 (INSERTED)

**Goal:** Phase 1에서 완성된 `workers/ingestion/` 코드를 Cloud Run Job + Cloud Scheduler로 실제 GCP에 배포하여 평일 장 시간(09:00~15:59 KST) 매 분 KIS API → Supabase stocks 자동 폴링을 활성화한다. 코드 변경 없이 운영 인프라만 구성.
**Requirements**: INFR-01, INFR-02 (production 활성화)
**Depends on:** Phase 5
**Success Criteria** (what must be TRUE):
  1. Cloud Run Job `gh-radar-ingestion` (cpu=1, memory=512Mi, task-timeout=120s, max-retries=0, parallelism=1)이 런타임 SA `gh-radar-ingestion-sa`로 4개 secret을 주입받아 배포되어 있다
  2. Cloud Scheduler `gh-radar-ingestion-scheduler`가 cron `* 9-15 * * 1-5` (Asia/Seoul) + OAuth SA `gh-radar-scheduler-sa`로 Job `:run` API를 호출하며 ENABLED 상태다
  3. `scripts/smoke-ingestion.sh`가 `jobs execute --wait` + Cloud Logging(`cycle complete` 또는 `non-trading day detected, exiting`) + Supabase `stocks.max(updated_at) < 120s` + Scheduler ENABLED 5개 INV를 모두 PASS한다
  4. Cloud Monitoring alert policy `gh-radar-ingestion-failure`가 이메일 채널로 연결되어 실패 시 사용자에게 자동 알림된다
  5. Secret Manager에 KIS/Supabase credential 4종이 등록되어 있으며 리포·이미지·로그에 평문으로 남지 않는다
**Plans:** 6 plans
- [x] 05.1-01-PLAN.md — Wave 0 스크립트/YAML/DEPLOY-LOG 템플릿 스캐폴드
- [x] 05.1-02-PLAN.md — Wave 1 setup-ingestion-iam.sh 실행 (API + SA 2종 + Secret 4종)
- [x] 05.1-03-PLAN.md — Wave 2 deploy-ingestion.sh 실행 (Build → Push → Job + Invoker + Scheduler)
- [x] 05.1-04-PLAN.md — Wave 3 Alert policy + 이메일 notification channel
- [x] 05.1-05-PLAN.md — Wave 4 smoke-ingestion.sh E2E + 이메일 알림 Manual
- [x] 05.1-06-PLAN.md — Wave 5 DEPLOY-LOG/STATE/ROADMAP 갱신
```

Progress 테이블에 Phase 05.1 행 추가 / 또는 기존 행이 있으면 Plans Complete를 `6/6`, Status `✅ 완료`, Completed 날짜 기록.

**C. `.planning/REQUIREMENTS.md` §"Traceability" 갱신:**

INFR-01, INFR-02 행의 Status 컬럼을 현재(`Pending`)에서 `Complete (Phase 05.1 deploy)`로 변경. Phase 컬럼은 `Phase 1 + Phase 05.1`로 확장(code=Phase 1, production deploy=Phase 05.1).

Coverage 요약은 변경하지 않음(이미 23/23 mapped).

**D. 체크리스트 업데이트:** §"v1 Requirements > Infrastructure"의 `- [ ] **INFR-01**...`, `- [ ] **INFR-02**...`를 `- [x]`로 변경.
  </action>
  <verify>
    <automated>test -f .planning/STATE.md && test -f .planning/ROADMAP.md && test -f .planning/REQUIREMENTS.md && grep -q 'Phase 05.1 Success Criteria' .planning/STATE.md && grep -q '05.1-06-PLAN.md' .planning/ROADMAP.md && grep -qE '^- \[x\] \*\*INFR-01\*\*' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Phase 05.1 Success Criteria 검증' .planning/STATE.md` == 1
    - `grep -c 'Phase 05.1: Ingestion 운영 배포' .planning/ROADMAP.md` ≥ 1
    - `grep -c '05.1-01-PLAN.md' .planning/ROADMAP.md` == 1
    - `grep -c '05.1-06-PLAN.md' .planning/ROADMAP.md` == 1
    - `grep -c 'TBD (run /gsd-plan-phase 05.1' .planning/ROADMAP.md` == 0  # placeholder 제거
    - `grep -c '\[Urgent work - to be planned\]' .planning/ROADMAP.md` == 0
    - `grep -cE '^- \[x\] \*\*INFR-01\*\*' .planning/REQUIREMENTS.md` == 1
    - `grep -cE '^- \[x\] \*\*INFR-02\*\*' .planning/REQUIREMENTS.md` == 1
    - `grep -c 'Complete (Phase 05.1' .planning/REQUIREMENTS.md` ≥ 2
    - STATE.md frontmatter `last_activity`가 오늘 날짜
  </acceptance_criteria>
  <done>
3개 문서 갱신 완료, Phase 05.1 완료가 traceability/progress 전반에 반영됨.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

문서 갱신만 수행 — 외부 trust boundary 없음.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-16 | Information Disclosure | DEPLOY-LOG.md에 민감값 유입 위험 | mitigate | secret 값(KIS_APP_KEY 등)은 **기록 금지** — Secret ID(`gh-radar-kis-app-key`)만 기록. 이메일 주소는 policy ID 참조로 간접 기록(선택적으로 `<사용자 이메일>`로 마스킹). SUPABASE_URL은 public endpoint이므로 기록 OK. |
| T-05.1-17 | Tampering | ROADMAP/STATE 무결성 | accept | Git commit으로 버전 관리. 실수 revert 가능. |
</threat_model>

<verification>
```bash
# 문서 정합성
test -f .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md
! grep -q 'TBD' .planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-DEPLOY-LOG.md

# STATE/ROADMAP/REQUIREMENTS 일관성
grep -q 'Phase 05.1' .planning/STATE.md
grep -q '05.1-06-PLAN.md' .planning/ROADMAP.md
grep -qE '^- \[x\] \*\*INFR-01\*\*' .planning/REQUIREMENTS.md

# 보안 레드라인 (DEPLOY-LOG에 KIS/Supabase 값 노출 금지)
! grep -nE '(PSS[A-Za-z0-9]{30,}|eyJhbGci[A-Za-z0-9_.-]{30,})' .planning/phases/05.1-*/05.1-DEPLOY-LOG.md
```
</verification>

<success_criteria>
- DEPLOY-LOG 완성 (TBD 0건)
- STATE/ROADMAP/REQUIREMENTS 반영
- 보안 레드라인 통과 (credential 평문 0건)
- Phase 05.1 [x] 체크 확립
</success_criteria>

<output>
완료 후 `.planning/phases/05.1-ingestion-cloud-run-job-cloud-scheduler-kis/05.1-06-SUMMARY.md`에 기록:
- 갱신된 문서 3개 diff 요약
- DEPLOY-LOG 최종 크기 / TBD 잔존 확인
- Phase 05.1 완료 timestamp
- 다음 단계 제안: `/gsd-verify-work 05.1`
</output>
