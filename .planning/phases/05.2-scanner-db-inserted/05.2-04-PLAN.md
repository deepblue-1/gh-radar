---
phase: 05.2
plan: 04
type: execute
wave: 3
depends_on: ["05.2-03"]
files_modified:
  - webapp/src/lib/api.ts
  - webapp/src/lib/scanner-api.ts
  - webapp/src/lib/scanner-api.test.ts
  - webapp/src/lib/format.ts
  - webapp/src/lib/format.test.ts
  - webapp/src/components/ui/number.tsx
  - webapp/src/hooks/use-polling.ts
  - webapp/src/components/scanner/scanner-client.tsx
  - webapp/src/components/scanner/scanner-table.tsx
  - webapp/src/components/scanner/scanner-card-list.tsx
  - webapp/src/components/scanner/scanner-skeleton.tsx
autonomous: true
requirements: [SCAN-04, SCAN-08]
must_haves:
  truths:
    - "Scanner 페이지가 '거래량' 대신 '거래대금' 라벨을 표시한다 (table 헤더 + skeleton 헤더 + mobile card)"
    - "각 stock의 거래대금이 formatTradeAmount 규칙(0→'-', <1조→'N,NNN억', ≥1조→'N.N조')으로 렌더된다"
    - "fetchScannerStocks 가 { stocks, lastUpdatedAt } 반환 (헤더에서 추출)"
    - "'최근 갱신 HH:MM:SS KST' 표시가 서버 X-Last-Updated-At 헤더 기준이며 Date.now() 를 사용하지 않는다"
    - "헤더 부재(0건) 시 lastUpdatedAt 는 undefined 로 전파되어 UI에 시각 표시가 사라진다"
  artifacts:
    - path: "webapp/src/lib/format.ts"
      provides: "formatTradeAmount(n) 단일 진리원 포맷터"
      exports: ["formatTradeAmount"]
    - path: "webapp/src/lib/format.test.ts"
      provides: "formatTradeAmount 엣지케이스 단위 테스트"
      contains: "formatTradeAmount"
    - path: "webapp/src/lib/scanner-api.test.ts"
      provides: "fetchScannerStocks 헤더 추출 테스트 (mock fetch)"
      contains: "X-Last-Updated-At"
    - path: "webapp/src/lib/scanner-api.ts"
      provides: "raw fetch 기반 fetcher, { stocks, lastUpdatedAt } 반환"
      contains: "X-Last-Updated-At"
    - path: "webapp/src/hooks/use-polling.ts"
      provides: "Date.now() 제거 — fetcher 결과에 위임"
      contains: "usePolling"
    - path: "webapp/src/components/scanner/scanner-client.tsx"
      provides: "data.stocks 분해 + data.lastUpdatedAt → epoch ms 변환"
      contains: "lastUpdatedAt"
    - path: "webapp/src/components/scanner/scanner-table.tsx"
      provides: "거래대금 헤더 + stock.tradeAmount 렌더"
      contains: "거래대금"
    - path: "webapp/src/components/scanner/scanner-card-list.tsx"
      provides: "거래대금 렌더 (mobile)"
      contains: "tradeAmount"
    - path: "webapp/src/components/scanner/scanner-skeleton.tsx"
      provides: "'거래대금' 헤더 라벨"
      contains: "거래대금"
  key_links:
    - from: "fetchScannerStocks"
      to: "response.headers.get('X-Last-Updated-At')"
      via: "raw fetch"
      pattern: "X-Last-Updated-At"
    - from: "scanner-client.tsx"
      to: "ScannerFilters lastUpdatedAt prop (epoch ms | undefined)"
      via: "new Date(iso).getTime() 변환"
      pattern: "new Date\\(.*lastUpdatedAt.*\\)\\.getTime"
    - from: "scanner-table.tsx / scanner-card-list.tsx"
      to: "stock.tradeAmount"
      via: "<Number format='trade-amount' /> 또는 formatTradeAmount util"
      pattern: "tradeAmount"
---

<objective>
Scanner UI가 (1) 거래량 대신 거래대금을 단일 진리원 포맷터로 표시하고 (2) "최근 갱신" 시각을 서버 응답 헤더 `X-Last-Updated-At` 기준으로 표시하도록 webapp 전면 수정.

Purpose: D-14 ~ D-19 잠긴 결정 구현. SCAN-04/SCAN-08 사용자 가시 가치 완성.
Output: 11개 파일 (신규 3개 + 수정 8개), vitest PASS, 빌드 green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-scanner-db-inserted/05.2-CONTEXT.md
@.planning/phases/05.2-scanner-db-inserted/05.2-RESEARCH.md
@.planning/phases/05.2-scanner-db-inserted/05.2-VALIDATION.md
@webapp/src/lib/api.ts
@webapp/src/lib/scanner-api.ts
@webapp/src/hooks/use-polling.ts
@webapp/src/components/ui/number.tsx
@webapp/src/components/scanner/scanner-client.tsx
@webapp/src/components/scanner/scanner-filters.tsx
@webapp/src/components/scanner/scanner-table.tsx
@webapp/src/components/scanner/scanner-card-list.tsx
@webapp/src/components/scanner/scanner-skeleton.tsx

<interfaces>
<!-- formatTradeAmount 스펙 (D-15) -->
```typescript
// webapp/src/lib/format.ts
/**
 * 거래대금(KRW) 포맷터. D-15 규칙.
 * - 0 또는 null/undefined → "-"
 * - < 1조(1e12) → "N,NNN억" (억 미만 버림, 천단위 콤마)
 * - ≥ 1조 → "N.N조" (소수점 1자리)
 */
export function formatTradeAmount(value: number | null | undefined): string;
```

<!-- fetchScannerStocks 신규 시그니처 (D-17 / RESEARCH Finding 4) -->
```typescript
export function fetchScannerStocks(
  state: ScannerState,
  signal: AbortSignal,
): Promise<{ stocks: StockWithProximity[]; lastUpdatedAt: string | null }>;
```

<!-- api.ts 에서 resolveBaseUrl export 필요 (RESEARCH Finding 4 인용) -->
```typescript
// 현재 내부 함수 → export 로 승격
export function resolveBaseUrl(): string { /* 기존 로직 그대로 */ }
```

<!-- ScannerFiltersProps.lastUpdatedAt 계약 (변경 없음 — epoch ms | undefined) -->
```typescript
// webapp/src/components/scanner/scanner-filters.tsx
export interface ScannerFiltersProps {
  // ...
  lastUpdatedAt?: number;  // epoch ms, 유지
  // ...
}
```

<!-- scanner-client.tsx 연결부 -->
```typescript
const { data, error, refresh, isRefreshing, isInitialLoading } = usePolling(fetcher, ...);
// data: { stocks, lastUpdatedAt: string | null } | undefined
const stocks = data?.stocks ?? [];
const iso = data?.lastUpdatedAt ?? null;
const lastUpdatedAtMs = iso ? new Date(iso).getTime() : undefined;
const lastUpdatedAtSafe = Number.isFinite(lastUpdatedAtMs) ? lastUpdatedAtMs : undefined;
// → ScannerFilters lastUpdatedAt={lastUpdatedAtSafe}
// → ScannerTable stocks={stocks}, ScannerCardList stocks={stocks}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: formatTradeAmount util + <Number format='trade-amount'/> + vitest</name>
  <read_first>
    - webapp/src/components/ui/number.tsx (formatByType switch 구조, marketCap 케이스)
    - .planning/phases/05.2-scanner-db-inserted/05.2-CONTEXT.md D-15, D-16
    - .planning/phases/05.2-scanner-db-inserted/05.2-RESEARCH.md §"6. 거래대금 Formatter 설계" (엣지케이스 표)
    - .planning/phases/05.2-scanner-db-inserted/05.2-VALIDATION.md 05.2-04-01
  </read_first>
  <files>webapp/src/lib/format.ts, webapp/src/lib/format.test.ts, webapp/src/components/ui/number.tsx</files>
  <behavior>
    formatTradeAmount 엣지케이스 (RESEARCH §6 표 인용):
    - 0 → "-"
    - null → "-"
    - undefined → "-"
    - 1e8 (1억) → "1억"
    - 1234 * 1e8 (1234억) → "1,234억"
    - 9999 * 1e8 (9999억) → "9,999억"
    - 1e12 (정확히 1조) → "1.0조"
    - 2.3e12 (2.3조) → "2.3조"
    - 1234567890 (12.3억대) → "12억" (억 미만 버림)
  </behavior>
  <action>
    **Step 1 — `webapp/src/lib/format.ts` 신규 파일:**

    D-16 "단일 진리원" 원칙 — util 분리 방식 채택 (vitest 테스트 용이성 + `<Number>` 와 직접 import 양쪽 사용처 지원).

    ```typescript
    /**
     * Scanner 포맷 유틸 — 단일 진리원 (Phase 05.2 D-15/D-16).
     */

    /**
     * 거래대금(KRW) 포맷터.
     * - 0 또는 null/undefined → "-"
     * - < 1조 (1e12) → "N,NNN억" (억 미만 버림, 천단위 콤마)
     * - ≥ 1조 → "N.N조" (소수점 1자리)
     */
    export function formatTradeAmount(
      value: number | null | undefined,
    ): string {
      if (value === null || value === undefined) return '-';
      if (!Number.isFinite(value) || value === 0) return '-';
      const trillion = 1e12;
      const uk = 1e8;
      if (value >= trillion) {
        return `${(value / trillion).toFixed(1)}조`;
      }
      return `${new Intl.NumberFormat('ko-KR').format(Math.floor(value / uk))}억`;
    }
    ```

    **Step 2 — `webapp/src/lib/format.test.ts` 신규 파일:**

    vitest 엣지케이스 9개 전부 커버:

    ```typescript
    import { describe, it, expect } from 'vitest';
    import { formatTradeAmount } from './format';

    describe('formatTradeAmount', () => {
      it('0 → "-"', () => expect(formatTradeAmount(0)).toBe('-'));
      it('null → "-"', () => expect(formatTradeAmount(null)).toBe('-'));
      it('undefined → "-"', () => expect(formatTradeAmount(undefined)).toBe('-'));
      it('1e8 (1억) → "1억"', () => expect(formatTradeAmount(1e8)).toBe('1억'));
      it('1234e8 (1234억) → "1,234억"', () =>
        expect(formatTradeAmount(1234e8)).toBe('1,234억'));
      it('9999e8 (9999억) → "9,999억"', () =>
        expect(formatTradeAmount(9999e8)).toBe('9,999억'));
      it('1e12 (1조) → "1.0조"', () =>
        expect(formatTradeAmount(1e12)).toBe('1.0조'));
      it('2.3e12 (2.3조) → "2.3조"', () =>
        expect(formatTradeAmount(2.3e12)).toBe('2.3조'));
      it('1,234,567,890 (12.3억대) → "12억" (억 미만 버림)', () =>
        expect(formatTradeAmount(1_234_567_890)).toBe('12억'));
    });
    ```

    **Step 3 — `webapp/src/components/ui/number.tsx` 수정:**

    (a) `NumberFormat` 유니온에 `'trade-amount'` 추가:
    ```typescript
    export type NumberFormat =
      | 'price'
      | 'percent'
      | 'volume'
      | 'marketCap'
      | 'trade-amount'  // ← 추가
      | 'plain';
    ```

    (b) `formatByType` 함수 상단에 import:
    ```typescript
    import { formatTradeAmount } from '@/lib/format';
    ```

    (c) `switch(format)` 내 `case 'marketCap':` 와 `case 'plain'` 사이에:
    ```typescript
    case 'trade-amount':
      return formatTradeAmount(value);
    ```

    **주의:**
    - `<Number value={0} format="trade-amount" />` 는 `-` 렌더하는데, `formatByType` 직전의 showSign 분기에서 `value > 0` 만 접두사 붙이므로 `-` 출력에 `+` 가 붙지 않음. 안전.
    - 기존 `volume` 케이스는 **삭제하지 않음** (다른 페이지에서 쓸 수 있음 — 현재 scanner 만 사용하지만 future-proof).
  </action>
  <acceptance_criteria>
    VALIDATION.md 05.2-04-01 충족:
    - `test -f webapp/src/lib/format.ts`
    - `test -f webapp/src/lib/format.test.ts`
    - `grep -q "formatTradeAmount" webapp/src/lib/format.ts`
    - `grep -q "'trade-amount'" webapp/src/components/ui/number.tsx`
    - `cd webapp && npx vitest run src/lib/format.test.ts` → 9/9 PASS.
  </acceptance_criteria>
  <verify>
    <automated>cd webapp && npx vitest run src/lib/format.test.ts</automated>
  </verify>
  <done>
    format.ts/format.test.ts 신규 생성, number.tsx에 `trade-amount` 케이스 추가, vitest 9/9 PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: api.ts resolveBaseUrl export + scanner-api.ts raw fetch 전환 + X-Last-Updated-At 헤더 추출 + vitest</name>
  <read_first>
    - webapp/src/lib/api.ts (resolveBaseUrl 내부 함수, apiFetch 에러 처리 패턴)
    - webapp/src/lib/scanner-api.ts (fetchScannerStocks 기존 시그니처)
    - .planning/phases/05.2-scanner-db-inserted/05.2-CONTEXT.md D-17
    - .planning/phases/05.2-scanner-db-inserted/05.2-RESEARCH.md §"4. apiFetch 헤더 접근 — 시그니처 변경 전략" (권장 패턴 코드 블록)
    - .planning/phases/05.2-scanner-db-inserted/05.2-VALIDATION.md 05.2-04-03
  </read_first>
  <files>webapp/src/lib/api.ts, webapp/src/lib/scanner-api.ts, webapp/src/lib/scanner-api.test.ts</files>
  <behavior>
    - fetchScannerStocks 가 `{ stocks, lastUpdatedAt: string | null }` 반환.
    - 응답 헤더에 `X-Last-Updated-At: 2026-04-14T10:01:33.796Z` 있으면 `lastUpdatedAt === '2026-04-14T10:01:33.796Z'`.
    - 응답 헤더 없으면 `lastUpdatedAt === null`.
    - 4xx/5xx 응답 시 `ApiClientError` throw (기존 apiFetch 와 동일 에러 계약).
    - 8초 타임아웃 유지 (AbortSignal.timeout 또는 AbortController).
  </behavior>
  <action>
    **Step 1 — `webapp/src/lib/api.ts` 수정:**

    `function resolveBaseUrl()` 선언을 `export function resolveBaseUrl()` 로 승격. 로직은 **변경 금지**. 이 함수의 다른 call site(`apiFetch` 내부)도 그대로 유지.

    **Step 2 — `webapp/src/lib/scanner-api.ts` 전면 교체:**

    기존 `apiFetch<StockWithProximity[]>` 호출을 raw fetch 로 교체해 `Response.headers` 에 접근. RESEARCH Finding 4 권장 패턴 인용:

    ```typescript
    import type { Stock, ApiErrorBody } from '@gh-radar/shared';
    import { ApiClientError, resolveBaseUrl } from './api';
    import type { ScannerState } from './scanner-query';

    export const SCANNER_LIMIT = 100;
    const DEFAULT_TIMEOUT_MS = 8_000;

    export type StockWithProximity = Stock & { upperLimitProximity: number };

    export interface ScannerFetchResult {
      stocks: StockWithProximity[];
      lastUpdatedAt: string | null;
    }

    export async function fetchScannerStocks(
      state: ScannerState,
      signal: AbortSignal,
    ): Promise<ScannerFetchResult> {
      const params = new URLSearchParams({
        sort: 'rate_desc',
        minRate: String(state.min),
        market: state.market,
        limit: String(SCANNER_LIMIT),
      });
      const base = resolveBaseUrl();
      let url: string;
      try {
        url = new URL(`/api/scanner?${params.toString()}`, base).toString();
      } catch (cause) {
        throw new ApiClientError({
          code: 'INVALID_BASE_URL',
          message: `API base URL 이 올바르지 않습니다 (값: "${base}")`,
          status: 0,
          cause,
        });
      }

      // 외부 signal + 8s 타임아웃 합성
      const controller = new AbortController();
      const onExternalAbort = () => controller.abort(signal.reason);
      if (signal.aborted) controller.abort(signal.reason);
      else signal.addEventListener('abort', onExternalAbort, { once: true });
      const timeoutId = setTimeout(
        () => controller.abort(new DOMException('Timeout', 'TimeoutError')),
        DEFAULT_TIMEOUT_MS,
      );

      let response: Response;
      try {
        response = await fetch(url, {
          cache: 'no-store',
          headers: { accept: 'application/json' },
          signal: controller.signal,
        });
      } catch (cause) {
        if (controller.signal.aborted && (cause as Error)?.name !== 'AbortError') {
          throw cause;
        }
        const isTimeout =
          (cause as Error)?.name === 'TimeoutError' ||
          (cause as Error)?.name === 'AbortError';
        throw new ApiClientError({
          code: isTimeout ? 'TIMEOUT' : 'NETWORK_ERROR',
          message: isTimeout ? '요청이 시간 초과되었습니다' : '네트워크 요청에 실패했습니다',
          status: 0,
          cause,
        });
      } finally {
        clearTimeout(timeoutId);
        signal.removeEventListener('abort', onExternalAbort);
      }

      const requestId = response.headers.get('x-request-id') ?? undefined;
      if (!response.ok) {
        let code = `HTTP_${response.status}`;
        let message = response.statusText || '요청이 실패했습니다';
        try {
          const body = (await response.json()) as Partial<ApiErrorBody> | undefined;
          if (body?.error?.code) code = body.error.code;
          if (body?.error?.message) message = body.error.message;
        } catch { /* noop */ }
        throw new ApiClientError({ code, message, status: response.status, requestId });
      }

      const stocks = (await response.json()) as StockWithProximity[];
      const lastUpdatedAt = response.headers.get('X-Last-Updated-At');
      return { stocks, lastUpdatedAt };
    }
    ```

    **주의 (P-5):**
    - 타임아웃 8초 + 외부 signal 합성 로직 반드시 포함 — 누락 시 네트워크 hang 가능.
    - `response.headers.get('X-Last-Updated-At')` 은 브라우저가 CORS exposed 에 헤더가 포함돼 있어야만 값을 반환 — Plan 03에서 `exposedHeaders` 설정 완료한 전제.
    - 기존 스케일 주석 (`changeRate` 정수 %) 은 파일 상단에 유지 또는 축약 — 구현 로직에는 영향 없음.

    **Step 3 — `webapp/src/lib/scanner-api.test.ts` 신규 파일:**

    `vi.stubGlobal('fetch', ...)` 또는 `vi.spyOn(globalThis, 'fetch')` 로 Response mock 후 3 케이스:

    ```typescript
    import { describe, it, expect, vi, afterEach } from 'vitest';
    import { fetchScannerStocks } from './scanner-api';
    import { ApiClientError } from './api';
    import type { ScannerState } from './scanner-query';

    const STATE: ScannerState = { min: 25, market: 'ALL' };

    afterEach(() => {
      vi.restoreAllMocks();
    });

    describe('fetchScannerStocks', () => {
      it('X-Last-Updated-At 헤더를 lastUpdatedAt 으로 반환', async () => {
        const iso = '2026-04-14T10:01:33.796Z';
        vi.stubGlobal(
          'fetch',
          vi.fn(async () =>
            new Response(JSON.stringify([]), {
              status: 200,
              headers: {
                'Content-Type': 'application/json',
                'X-Last-Updated-At': iso,
              },
            }),
          ),
        );
        const result = await fetchScannerStocks(STATE, new AbortController().signal);
        expect(result.lastUpdatedAt).toBe(iso);
        expect(result.stocks).toEqual([]);
      });

      it('헤더 부재 시 lastUpdatedAt === null', async () => {
        vi.stubGlobal(
          'fetch',
          vi.fn(async () =>
            new Response(JSON.stringify([]), {
              status: 200,
              headers: { 'Content-Type': 'application/json' },
            }),
          ),
        );
        const result = await fetchScannerStocks(STATE, new AbortController().signal);
        expect(result.lastUpdatedAt).toBeNull();
      });

      it('4xx 응답 시 ApiClientError throw', async () => {
        vi.stubGlobal(
          'fetch',
          vi.fn(async () =>
            new Response(JSON.stringify({ error: { code: 'INVALID_QUERY_PARAM', message: 'bad' } }), {
              status: 400,
              headers: { 'Content-Type': 'application/json' },
            }),
          ),
        );
        await expect(
          fetchScannerStocks(STATE, new AbortController().signal),
        ).rejects.toBeInstanceOf(ApiClientError);
      });
    });
    ```

    NEXT_PUBLIC_API_BASE_URL 환경변수 미설정 시 `http://localhost:8080` fallback 발생 — test가 fetch mock을 잡으므로 실제 요청 안 나감.
  </action>
  <acceptance_criteria>
    VALIDATION.md 05.2-04-03 충족:
    - `grep -q "export function resolveBaseUrl" webapp/src/lib/api.ts`
    - `grep -q "X-Last-Updated-At" webapp/src/lib/scanner-api.ts`
    - `grep -q "lastUpdatedAt" webapp/src/lib/scanner-api.ts`
    - `test -f webapp/src/lib/scanner-api.test.ts`
    - `cd webapp && npx vitest run src/lib/scanner-api.test.ts` → 3/3 PASS.
  </acceptance_criteria>
  <verify>
    <automated>cd webapp && npx vitest run src/lib/scanner-api.test.ts</automated>
  </verify>
  <done>
    api.ts의 resolveBaseUrl export됨. scanner-api.ts 가 raw fetch + 헤더 추출 패턴으로 교체됨. scanner-api.test.ts 3 case PASS.
  </done>
</task>

<task type="auto">
  <name>Task 3: use-polling.ts Date.now() 제거 + scanner-client + scanner-table/card-list/skeleton 거래대금 전환</name>
  <read_first>
    - webapp/src/hooks/use-polling.ts (setLastUpdatedAt(Date.now()) 위치 L62)
    - webapp/src/components/scanner/scanner-client.tsx (data/lastUpdatedAt 사용 위치)
    - webapp/src/components/scanner/scanner-filters.tsx (lastUpdatedAt prop 계약 — 변경 금지)
    - webapp/src/components/scanner/scanner-table.tsx (`stock.volume` + '거래량' 헤더 위치)
    - webapp/src/components/scanner/scanner-card-list.tsx (`stock.volume` 렌더 위치)
    - webapp/src/components/scanner/scanner-skeleton.tsx ('거래량' 라벨 위치 L16)
    - webapp/src/hooks/use-polling.test.ts (있다면 — Date.now() assert 영향 확인)
    - .planning/phases/05.2-scanner-db-inserted/05.2-CONTEXT.md D-14, D-17, D-18, D-19
    - .planning/phases/05.2-scanner-db-inserted/05.2-RESEARCH.md §"5. usePolling<T> 제네릭 확장 영향 분석" (권장 패턴)
    - .planning/phases/05.2-scanner-db-inserted/05.2-VALIDATION.md 05.2-04-02, 05.2-04-04
  </read_first>
  <files>webapp/src/hooks/use-polling.ts, webapp/src/components/scanner/scanner-client.tsx, webapp/src/components/scanner/scanner-table.tsx, webapp/src/components/scanner/scanner-card-list.tsx, webapp/src/components/scanner/scanner-skeleton.tsx</files>
  <action>
    **Step 1 — `webapp/src/hooks/use-polling.ts` 수정:**

    L62의 `setLastUpdatedAt(Date.now());` 라인 한 줄을 **완전 제거**. `lastUpdatedAt` state 자체와 `UsePollingResult.lastUpdatedAt: number | undefined` 타입은 **유지** (하위 컴포넌트 타입 계약 안정성).

    결과: `lastUpdatedAt` 은 항상 `undefined` 상태가 된다. (scanner-client.tsx 에서 `data.lastUpdatedAt` 을 별도로 취하므로 훅의 값을 구독하지 않음.)

    기존 test `use-polling.test.ts` 가 `lastUpdatedAt` Date.now() assert 를 포함하면 해당 assert 제거 또는 undefined assert 로 교체. (파일 존재 시 `grep "lastUpdatedAt" webapp/src/hooks/use-polling.test.ts` 결과 확인 후 수정.)

    **Step 2 — `webapp/src/components/scanner/scanner-client.tsx` 수정:**

    (a) 구조분해에서 `lastUpdatedAt` 제거 (훅에서 더 이상 사용 안 함):
    ```typescript
    const { data, error, refresh, isRefreshing, isInitialLoading } =
      usePolling(fetcher, { intervalMs: POLL_INTERVAL_MS, key });
    ```

    (b) `data` 타입이 `{ stocks, lastUpdatedAt } | undefined` 로 변경됨. `hasData`/`isEmpty` 등 data 기반 분기도 `data?.stocks` 로 조정:
    ```typescript
    const stocks = data?.stocks ?? [];
    const iso = data?.lastUpdatedAt ?? null;
    const lastUpdatedAtMs = iso ? new Date(iso).getTime() : NaN;
    const lastUpdatedAt = Number.isFinite(lastUpdatedAtMs) ? lastUpdatedAtMs : undefined;

    const hasData = data !== undefined && stocks.length > 0;
    const isEmpty = data !== undefined && stocks.length === 0 && !error;
    ```

    (c) `ScannerTable`/`ScannerCardList` 에 `stocks={stocks}` 전달 (기존 `data ?? []` 를 `stocks` 로 교체).

    (d) `ScannerFilters lastUpdatedAt={lastUpdatedAt}` — epoch ms(number|undefined). 기존 `ScannerFiltersProps.lastUpdatedAt?: number` 계약 불변이라 filters.tsx 수정 불필요.

    **Step 3 — `webapp/src/components/scanner/scanner-table.tsx` 수정:**

    (a) 헤더 라벨 L42: `<span>거래량</span>` → `<span>거래대금</span>`

    (b) 렌더 L81-83:
    ```typescript
    <span className="mono text-right text-[var(--fg)]">
      <Number value={stock.tradeAmount} format="trade-amount" />
    </span>
    ```

    **주의:**
    - `stock.volume` 참조는 이 파일에서 완전히 제거한다 (D-14 "완전 교체").
    - 컬럼 width(`GRID_COLS`) 는 유지 — 동일 100px 우측정렬 기존 디자인 그대로.

    **Step 4 — `webapp/src/components/scanner/scanner-card-list.tsx` 수정:**

    L59-61:
    ```typescript
    <span className="mono text-[var(--muted-fg)]">
      <Number value={stock.tradeAmount} format="trade-amount" />
    </span>
    ```

    `stock.volume` 참조 제거.

    **Step 5 — `webapp/src/components/scanner/scanner-skeleton.tsx` 수정:**

    L16 배열의 `'거래량'` 을 `'거래대금'` 으로 교체:
    ```typescript
    {['종목명', '코드', '마켓', '현재가', '등락률', '거래대금'].map(...)}
    ```

    **Step 6 — 전체 빌드 확인:**
    ```bash
    cd webapp && npx tsc --noEmit
    cd webapp && npx vitest run
    ```
    둘 다 통과해야 함. 타입 에러 나면 `stock.volume` 잔존 참조 또는 `data` 구조 미전환 부분 찾아 수정.

    **Step 7 — grep 기반 잔존 확인:**
    ```bash
    grep -rn "stock.volume" webapp/src/components/scanner/
    # 예상: 0 줄
    grep -rn "거래량" webapp/src/components/scanner/
    # 예상: 0 줄
    ```
    이 두 grep이 0줄 리턴해야 Task 완료.
  </action>
  <acceptance_criteria>
    VALIDATION.md 05.2-04-02, 05.2-04-04 충족:
    - `grep -rl "stock.volume" webapp/src/components/scanner/` → 0 파일 (05.2-04-02)
    - `grep -rl "거래량" webapp/src/components/scanner/` → 0 파일
    - `! grep -q "Date.now()" webapp/src/hooks/use-polling.ts` (05.2-04-04 — Date.now() 완전 제거)
    - `grep -q "stock.tradeAmount" webapp/src/components/scanner/scanner-table.tsx`
    - `grep -q "stock.tradeAmount" webapp/src/components/scanner/scanner-card-list.tsx`
    - `grep -q "거래대금" webapp/src/components/scanner/scanner-skeleton.tsx`
    - `grep -q "거래대금" webapp/src/components/scanner/scanner-table.tsx`
    - `grep -q "new Date(.*lastUpdatedAt" webapp/src/components/scanner/scanner-client.tsx` (iso → ms 변환 로직)
    - `cd webapp && npx tsc --noEmit` PASS
    - `cd webapp && npx vitest run` ALL PASS
  </acceptance_criteria>
  <verify>
    <automated>cd webapp && npx tsc --noEmit && npx vitest run && test $(grep -rl "stock.volume" webapp/src/components/scanner/ | wc -l) -eq 0 && test $(grep -rl "거래량" webapp/src/components/scanner/ | wc -l) -eq 0 && ! grep -q "Date.now()" webapp/src/hooks/use-polling.ts</automated>
  </verify>
  <done>
    use-polling.ts에 Date.now() 흔적 없음. scanner-client가 서버 헤더 iso → ms 변환해 filters에 전달. 3개 scanner 뷰 컴포넌트 모두 tradeAmount 렌더. 라벨 전부 "거래대금". webapp vitest + tsc 통과.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → webapp (CORS) | X-Last-Updated-At 헤더는 Plan 03에서 exposedHeaders에 명시. |
| webapp → 사용자 (렌더) | formatTradeAmount 의 `Number.isFinite` 가드로 NaN/Infinity 방어. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-04-01 | Tampering | 서버가 조작된 X-Last-Updated-At 반환 (예: 미래 시각) | accept | 동일 오리진 Cloud Run 서버를 신뢰. ISO 파싱 실패 시 `new Date()` 가 Invalid Date → `getTime() === NaN` → `Number.isFinite` 가드로 undefined fallback. UI가 "갱신 시각: -" 표시. |
| T-05.2-04-02 | Denial of Service | 8초 타임아웃 없이 hang | mitigate | scanner-api.ts 내 AbortController + setTimeout(8000) 합성 패턴 명시. |
| T-05.2-04-03 | Information Disclosure | raw fetch 로그/에러에 URL 노출 | accept | ApiClientError cause 에 URL 직접 포함 안 함. DevTools에서는 어차피 노출되므로 추가 방어 불필요. |
| T-05.2-04-04 | Tampering | URL injection via ScannerState | mitigate | 이미 Phase 5 T-5-01 scanner-query.ts `clamp`/`whitelist` 로 방어. 서버 zod 2차 검증. |
</threat_model>

<verification>
- `cd webapp && npx vitest run` 전체 (format.test.ts + scanner-api.test.ts + 기존 테스트 포함) PASS.
- `cd webapp && npx tsc --noEmit` 에러 없음.
- VALIDATION 05.2-04-01 / 02 / 03 / 04 네 행 모두 충족.
- 수동 로컬 smoke (선택): `cd webapp && pnpm dev` + DevTools Network Response Headers 에 `X-Last-Updated-At` 확인, Scanner 테이블 "거래대금" 헤더 렌더 확인.
</verification>

<success_criteria>
1. Scanner page의 desktop table / mobile card / skeleton 모든 헤더가 "거래대금".
2. `stock.volume` 참조가 `webapp/src/components/scanner/` 디렉토리에서 완전히 삭제됨.
3. `webapp/src/hooks/use-polling.ts` 에서 `Date.now()` 호출이 0회.
4. `fetchScannerStocks` 가 `{ stocks, lastUpdatedAt }` 반환 + vitest mock 헤더 테스트 PASS.
5. `scanner-client.tsx` 가 iso 문자열 → epoch ms 변환해 `ScannerFilters.lastUpdatedAt` 로 전달.
6. webapp 전체 vitest + tsc 빌드 PASS.
</success_criteria>

<output>
완료 후 `.planning/phases/05.2-scanner-db-inserted/05.2-04-SUMMARY.md` 작성.
</output>
