---
phase: 05.2
plan: 05
type: execute
wave: 4
depends_on: ["05.2-01", "05.2-02", "05.2-03", "05.2-04"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [SCAN-04, SCAN-08]
user_setup:
  - service: supabase
    why: "stocks 테이블 trade_amount 컬럼 마이그레이션 반영"
    dashboard_config:
      - task: "supabase CLI 로그인 + 프로젝트 링크 확인"
        location: "로컬 shell — `supabase login` + `supabase link` 상태 점검"
  - service: gcp-cloud-run
    why: "Express 서버 재배포 (trade_amount 응답 + X-Last-Updated-At 헤더 + CORS exposedHeaders)"
    dashboard_config:
      - task: "gcloud CLI 인증 및 프로젝트 설정 확인"
        location: "로컬 shell — `gcloud auth list` + `gcloud config list project`"
  - service: gcp-cloud-run-job
    why: "Ingestion Worker 이미지 재빌드/배포 — trade_amount upsert 활성화"
    dashboard_config:
      - task: "deploy-ingestion.sh 실행 (Phase 05.1 스크립트)"
        location: "로컬 shell — 이미지 빌드 + Job 갱신"
  - service: vercel
    why: "Webapp 재배포 (거래대금 렌더 + 서버 헤더 기반 갱신시각)"
    dashboard_config:
      - task: "main 브랜치 push로 자동 트리거 또는 `vercel --prod`"
        location: "로컬 shell 또는 Vercel Dashboard"
must_haves:
  truths:
    - "Supabase stocks 테이블에 trade_amount 컬럼이 실제 존재한다"
    - "Production Cloud Run Job이 매 cycle trade_amount 값을 DB에 upsert한다"
    - "Production /api/scanner 응답에 tradeAmount 필드 + X-Last-Updated-At 헤더가 포함된다"
    - "Production webapp /scanner 페이지가 '거래대금' 컬럼을 올바른 포맷으로 렌더한다"
    - "Production webapp 의 '최근 갱신' 시각이 서버 헤더 기준이며 장 외 시간에 정지한다"
    - "REQUIREMENTS.md 에 SCAN-08 신규 등록 + SCAN-04 각주 추가 + Traceability 테이블 갱신"
    - "ROADMAP.md Phase 05.2 체크리스트에 5개 plan 모두 [x] 마킹됨"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "SCAN-08 신규 + SCAN-04 거래대금 각주 + Phase 05.2 Traceability 행"
      contains: "SCAN-08"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 05.2 Plans 섹션 5개 [x] + 완료일"
      contains: "05.2-05-PLAN.md"
  key_links:
    - from: "Cloud Run Job (ingestion production)"
      to: "Supabase stocks.trade_amount"
      via: "upsert.ts rows 매핑 (Plan 02)"
      pattern: "trade_amount"
    - from: "webapp Vercel production"
      to: "Cloud Run server X-Last-Updated-At"
      via: "CORS exposedHeaders + raw fetch (Plan 03/04)"
      pattern: "X-Last-Updated-At"
---

<objective>
Wave 0-3에서 만든 코드 변경을 production에 반영하고 문서(REQUIREMENTS/ROADMAP)를 갱신한다. **이 플랜은 checkpoint 기반 비자동 플랜** — 사용자 환경변수/인증/GCP 권한이 필요하며, 배포 순서(RESEARCH §7)를 엄수해야 한다.

배포 순서 고정:
1. **Supabase** db push (마이그레이션)
2. **Cloud Run Server** 재배포 (deploy-server.sh)
3. **Cloud Run Job (Ingestion)** 재배포 (deploy-ingestion.sh)
4. **Vercel Webapp** 재배포

Purpose: D-20/D-21 문서 갱신 + Wave 0-3 변경사항을 production에 반영해 SCAN-04/SCAN-08 완결.
Output: 4개 배포 완료 + 2개 문서 파일 갱신 + UAT 검증.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-scanner-db-inserted/05.2-CONTEXT.md
@.planning/phases/05.2-scanner-db-inserted/05.2-RESEARCH.md
@.planning/phases/05.2-scanner-db-inserted/05.2-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@scripts/deploy-server.sh
@scripts/deploy-ingestion.sh

<interfaces>
<!-- REQUIREMENTS.md 갱신 스펙 (D-20, D-21) -->
### Scanner 섹션 (§"### Scanner") 수정:
- SCAN-04 라인 뒤에 각주: `(Phase 05.2에서 "거래량" → "거래대금"으로 재해석)`
- SCAN-07 뒤에 신규 한 줄 추가:
  `- [x] **SCAN-08**: 갱신시각 서버 DB 기준 표시 (Phase 05.2)`

### Traceability 테이블 (§"## Traceability") 추가:
| SCAN-08 | Phase 5.2 | Complete |
| (SCAN-04 행은 "Phase 5 / Phase 5.2" 로 갱신, Status 유지 Complete)

### Coverage 카운트 업데이트:
- v1 requirements: 23 → 24 total
- Mapped to phases: 23 → 24

<!-- ROADMAP.md 갱신 스펙 (§"Phase 05.2") -->
- `**Plans:** 0 plans` → `**Plans:** 5 plans`
- `Plans:` 아래 placeholder 한 줄을 다음 5줄로 교체:
  ```
  - [x] 05.2-01-PLAN.md — DB migration (trade_amount 컬럼)
  - [x] 05.2-02-PLAN.md — 공용 타입 + ingestion map/upsert + vitest
  - [x] 05.2-03-PLAN.md — server API (COLS + X-Last-Updated-At + CORS exposedHeaders)
  - [x] 05.2-04-PLAN.md — webapp (formatTradeAmount + raw fetch + UI 교체)
  - [x] 05.2-05-PLAN.md — Supabase/Cloud Run/Vercel 배포 + 문서 갱신
  ```
- Phase 체크리스트 아이콘 `[ ]` → `[x]` 전환 (ROADMAP L114 전후 `### Phase 05.2:` 섹션 제목의 체크박스는 없지만 Overview 섹션이 있다면 반영).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Supabase 마이그레이션 적용 (supabase db push)</name>
  <what-to-run>
    사용자 로컬 shell에서 Supabase 링크된 상태 확인 후:
    ```bash
    supabase db push
    ```
    실행 전 `supabase projects list` 로 올바른 프로젝트에 링크되어 있는지 확인.
  </what-to-run>
  <how-to-verify>
    1. `supabase db push` 출력에 `20260414110000_add_trade_amount.sql` 마이그레이션 적용 로그가 보인다.
    2. Supabase Dashboard > Table Editor > stocks 테이블 컬럼 목록에 `trade_amount` (bigint, default 0, NOT NULL) 노출.
    3. 또는 CLI: `supabase db diff` → "No schema differences" 또는 빈 출력.
    4. 기존 row 중 하나 SELECT 시 `trade_amount=0` 확인:
       ```sql
       SELECT code, volume, trade_amount FROM stocks LIMIT 3;
       ```
  </how-to-verify>
  <resume-signal>"supabase pushed" 또는 마이그레이션 로그 붙여넣기</resume-signal>
  <why-manual>`supabase` CLI 는 로컬 환경의 access token + project link 에 의존하며 Claude 권한 범위 밖.</why-manual>
  <action>
    사용자가 수행. Claude는 로그/결과를 보고 다음 태스크로 넘어간다.
  </action>
  <verify>
    <automated>echo "manual verification required"</automated>
  </verify>
  <done>
    Supabase stocks 테이블에 trade_amount 컬럼이 존재한다 (사용자 확인).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Cloud Run Server 재배포 (deploy-server.sh)</name>
  <what-to-run>
    사용자 로컬 shell에서:
    ```bash
    gcloud auth list    # 활성 계정 확인
    gcloud config list project    # 프로젝트 확인
    bash scripts/deploy-server.sh
    ```
  </what-to-run>
  <how-to-verify>
    1. 스크립트 종료 코드 0.
    2. 스크립트 후반부에 Cloud Run revision URL 출력.
    3. smoke:
       ```bash
       SERVER_URL="https://gh-radar-server-1023658565518.asia-northeast3.run.app"
       curl -sS -D - "$SERVER_URL/api/scanner?limit=3&sort=rate_desc" -o /tmp/scanner.json
       # 응답 헤더에 "X-Last-Updated-At: 2026-..." 확인
       jq '.[0] | {code, volume, tradeAmount}' /tmp/scanner.json
       # tradeAmount 필드 노출 확인 (값은 Wave 2 ingestion 재배포 전이라 0일 수 있음)
       ```
    4. CORS 검증:
       ```bash
       curl -sS -D - -H "Origin: https://gh-radar-webapp.vercel.app" \
         "$SERVER_URL/api/scanner?limit=1" -o /dev/null | grep -i "access-control-expose-headers"
       # 응답에 "access-control-expose-headers: X-Last-Updated-At,X-Request-Id" 포함
       ```
  </how-to-verify>
  <resume-signal>"server deployed" + curl 헤더 결과 붙여넣기</resume-signal>
  <why-manual>`gcloud` CLI + service account key + GCP project permissions 필요.</why-manual>
  <action>
    사용자가 수행. smoke curl 결과로 X-Last-Updated-At 헤더 + tradeAmount 필드 존재 확인.
  </action>
  <verify>
    <automated>echo "manual verification required"</automated>
  </verify>
  <done>
    Production /api/scanner 응답에 tradeAmount 필드 + X-Last-Updated-At 헤더 + access-control-expose-headers 동봉.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Cloud Run Job (Ingestion) 재배포 (deploy-ingestion.sh)</name>
  <what-to-run>
    사용자 로컬 shell에서:
    ```bash
    bash scripts/deploy-ingestion.sh
    ```
    Phase 05.1의 배포 스크립트 재실행. 이미지 tag는 새 git SHA로 자동 갱신.
  </what-to-run>
  <how-to-verify>
    1. 스크립트 종료 코드 0. 새 이미지 tag 출력.
    2. Cloud Scheduler 다음 트리거(매 분) 또는 수동 `gcloud run jobs execute gh-radar-ingestion --region asia-northeast3` 로 즉시 실행.
    3. Supabase에서 2-3분 후 SELECT:
       ```sql
       SELECT code, volume, trade_amount, updated_at
       FROM stocks ORDER BY updated_at DESC LIMIT 5;
       ```
       `trade_amount > 0` 인 row 최소 1개 존재 확인 (장 시간 내 기준).
    4. 장 외 시간이라면 trade_amount 는 0 유지 — 이 경우 `bsop_date` 기반 휴장 감지가 동작 중임을 로그로 확인.
  </how-to-verify>
  <resume-signal>"ingestion deployed" + 최신 SELECT 결과 붙여넣기</resume-signal>
  <why-manual>Cloud Run Job 배포는 GCP 인증 필요. Supabase SELECT도 서비스 키 필요.</why-manual>
  <action>사용자가 수행.</action>
  <verify>
    <automated>echo "manual verification required"</automated>
  </verify>
  <done>
    Ingestion Job 재배포 완료, 다음 cycle부터 trade_amount 값이 DB에 upsert.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Vercel Webapp 재배포</name>
  <what-to-run>
    옵션 A (자동): main push 로 Vercel auto-deploy 트리거:
    ```bash
    git push origin master
    ```
    옵션 B (수동 CLI): `cd webapp && vercel --prod`
  </what-to-run>
  <how-to-verify>
    1. Vercel Dashboard 에서 최신 deployment "Ready" 상태.
    2. 브라우저에서 https://gh-radar-webapp.vercel.app/scanner 접속.
    3. DevTools Network 탭:
       - `/api/scanner` 요청 Response Headers에 `X-Last-Updated-At: 2026-...` 보임.
       - `Access-Control-Expose-Headers` 에 `X-Last-Updated-At` 포함.
    4. 테이블 헤더에 "거래대금" 라벨.
    5. 각 row 우측에 `N,NNN억` 또는 `N.N조` 포맷의 값 (또는 0인 row는 `-`).
    6. 필터 바 우측 "최근 갱신 HH:MM:SS KST" 표시 — 장 외 시간이면 서버 마지막 upsert 시각 그대로 유지.
    7. 새로고침(F5 또는 refresh 버튼) 시 시각이 서버 응답에 따라 갱신 또는 유지.
  </how-to-verify>
  <resume-signal>"webapp deployed" + Network 탭 스크린샷 또는 헤더 붙여넣기</resume-signal>
  <why-manual>Vercel 배포는 GitHub/Vercel 인증 필요. UI 육안 검증은 실제 브라우저에서만 가능.</why-manual>
  <action>사용자가 수행. Task 1-3가 전부 성공 후에만 실행 (역방향 배포 방지 P-1).</action>
  <verify>
    <automated>echo "manual verification required"</automated>
  </verify>
  <done>
    Production webapp 에서 VALIDATION 05.2-05-02 E2E 3가지 확인 사항(거래대금 렌더, X-Last-Updated-At 헤더, 서버 기준 갱신시각) 모두 통과.
  </done>
</task>

<task type="auto">
  <name>Task 5: REQUIREMENTS.md / ROADMAP.md 갱신</name>
  <read_first>
    - .planning/REQUIREMENTS.md (Scanner 섹션 + Traceability 테이블 + Coverage 카운트)
    - .planning/ROADMAP.md §"Phase 05.2" (Plans placeholder)
    - .planning/phases/05.2-scanner-db-inserted/05.2-CONTEXT.md D-20, D-21
    - .planning/phases/05.2-scanner-db-inserted/05.2-VALIDATION.md 05.2-05-01
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
    **Step 1 — `.planning/REQUIREMENTS.md` 수정:**

    (a) `### Scanner` 섹션에서 SCAN-04 라인을 각주 추가 형태로 교체:
    ```markdown
    - [x] **SCAN-04**: 종목별 현재가 + 등락률 + 거래량 표시 *(Phase 05.2에서 "거래량" → "거래대금"으로 재해석 — D-21)*
    ```

    (b) SCAN-07 바로 아래에 SCAN-08 신규 라인 추가:
    ```markdown
    - [x] **SCAN-08**: 갱신시각 서버 DB 기준 표시 (Phase 05.2) — `X-Last-Updated-At` 응답 헤더
    ```

    (c) `## Traceability` 테이블에 SCAN-04 행 Phase를 `Phase 5, 5.2` 로 업데이트:
    ```markdown
    | SCAN-04 | Phase 5, 5.2 | Complete |
    ```
    그리고 SCAN-07 행 아래에 SCAN-08 행 추가:
    ```markdown
    | SCAN-08 | Phase 5.2 | Complete |
    ```

    (d) `**Coverage:**` 섹션 카운트 증가:
    ```markdown
    - v1 requirements: 24 total
    - Mapped to phases: 24
    - Unmapped: 0 ✓
    ```

    (e) 하단 `*Last updated:*` 라인 업데이트:
    ```markdown
    *Last updated: 2026-04-14 — Phase 05.2 SCAN-08 신규 + SCAN-04 거래대금 재해석*
    ```

    **Step 2 — `.planning/ROADMAP.md` 수정:**

    `### Phase 05.2:` 섹션 (L114 전후):
    - `**Plans:** 0 plans` → `**Plans:** 5/5 plans executed ✅`
    - `Plans:` 아래 placeholder 한 줄 (`- [ ] TBD ...`) 삭제 후 다음 5줄로 교체:
      ```markdown
      Plans:
      - [x] 05.2-01-PLAN.md — DB migration (trade_amount 컬럼)
      - [x] 05.2-02-PLAN.md — 공용 타입 + ingestion map/upsert + vitest
      - [x] 05.2-03-PLAN.md — server API (COLS + X-Last-Updated-At + CORS exposedHeaders)
      - [x] 05.2-04-PLAN.md — webapp (formatTradeAmount + raw fetch + UI 교체)
      - [x] 05.2-05-PLAN.md — Supabase/Cloud Run/Vercel 배포 + 문서 갱신
      ```
    - 섹션 말미에 `**Completed:** 2026-04-14` 라인 추가.

    **주의:**
    - Phase 체크리스트 상단 Overview 섹션(L19 `- [ ] **Phase 5:**`)은 Phase 5 이미 `[x]` 이고 05.2는 별도 인라인 섹션. 최종 섹션 Progress 테이블 업데이트는 planner 생략 (해당 테이블이 오래돼 있고 정합성이 깨져 있음).
  </action>
  <acceptance_criteria>
    VALIDATION.md 05.2-05-01 충족:
    - `grep -q "SCAN-08" .planning/REQUIREMENTS.md`
    - `grep -q "거래대금" .planning/REQUIREMENTS.md`
    - `grep -q "SCAN-08" .planning/ROADMAP.md` (Plans 섹션이나 설명 중 어디서든)
    - `grep -c "05.2-0[1-5]-PLAN.md" .planning/ROADMAP.md` ≥ 5
    - `grep -q "Completed:.*2026-04-14" .planning/ROADMAP.md`
    - `grep -q "v1 requirements: 24 total" .planning/REQUIREMENTS.md`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "SCAN-08" .planning/REQUIREMENTS.md && grep -q "거래대금" .planning/REQUIREMENTS.md && grep -c "05.2-0[1-5]-PLAN.md" .planning/ROADMAP.md | awk '{exit ($1<5)}' && grep -q "v1 requirements: 24 total" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    REQUIREMENTS.md / ROADMAP.md 모두 Phase 05.2 완료 상태 반영. Coverage 24/24.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → Supabase (CLI) | service_role 권한 migration 실행 |
| 로컬 → GCP (gcloud) | Cloud Run deploy + Job update |
| 로컬 → Vercel | main push 또는 vercel CLI 배포 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-05-01 | Elevation of Privilege | 배포 순서 오류 (webapp 먼저) → production UX 깨짐 | mitigate | Task 1→2→3→4 체크포인트 순차 강제. P-1 pitfall 문서화. 각 Task resume-signal 필수 대기. |
| T-05.2-05-02 | Denial of Service | Cloud Run deploy 실패로 server 다운 | mitigate | min-instances=1 유지 + deploy-server.sh 에 기존 revision rollback 로직 (Phase 2 D-36) 의존. 실패 시 이전 revision 자동 serving. |
| T-05.2-05-03 | Information Disclosure | 문서 갱신 시 시크릿 실수 커밋 | accept | REQUIREMENTS.md/ROADMAP.md 편집은 시크릿 입력 없음. `.env` 파일 미수정. |
</threat_model>

<verification>
- VALIDATION 05.2-05-01 (auto grep): Task 5 verify 명령으로 확인.
- VALIDATION 05.2-05-02 (manual E2E): Task 4 (Vercel 배포) 의 how-to-verify 3가지 항목 사용자 확인.
- 모든 checkpoint resume-signal 수신 기록.
</verification>

<success_criteria>
1. Supabase stocks 테이블에 trade_amount 컬럼 존재 (CLI 또는 Dashboard 확인).
2. Production server 에서 `curl /api/scanner` 응답에 tradeAmount + X-Last-Updated-At + exposed-headers 확인.
3. Production ingestion이 다음 cycle 부터 trade_amount > 0 로 upsert (장 시간 내).
4. Production webapp /scanner 페이지가 거래대금 렌더 + 서버 기준 갱신시각 표시.
5. `.planning/REQUIREMENTS.md` 에 SCAN-08 등록, SCAN-04 각주, Coverage 24/24.
6. `.planning/ROADMAP.md` Phase 05.2 섹션에 5 plan 모두 [x] + Completed 날짜.
</success_criteria>

<output>
완료 후 `.planning/phases/05.2-scanner-db-inserted/05.2-05-SUMMARY.md` 작성 — 이미지 tag, Cloud Run revision, Vercel deployment URL, curl smoke 헤더 로그 포함.
</output>
