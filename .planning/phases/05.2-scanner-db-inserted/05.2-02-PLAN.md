---
phase: 05.2
plan: 02
type: execute
wave: 1
depends_on: ["05.2-01"]
files_modified:
  - packages/shared/src/kis.ts
  - packages/shared/src/stock.ts
  - workers/ingestion/src/pipeline/map.ts
  - workers/ingestion/src/pipeline/upsert.ts
  - workers/ingestion/tests/map.test.ts
autonomous: true
requirements: [SCAN-04]
must_haves:
  truths:
    - "KisRankingRow 타입이 acml_tr_pbmn: string 필드를 명시적으로 선언한다"
    - "Stock 공용 타입이 tradeAmount: number 필드를 non-optional로 가진다 (volume 유지)"
    - "toStock() 이 tradeAmount = Number(row.acml_tr_pbmn) 을 매핑한다"
    - "upsertStocks() 가 rows 매핑에 trade_amount 를 포함한다"
    - "map.test.ts 의 fixture가 acml_tr_pbmn 을 포함하고 tradeAmount 값을 assert한다"
  artifacts:
    - path: "packages/shared/src/kis.ts"
      provides: "KisRankingRow.acml_tr_pbmn 필드"
      contains: "acml_tr_pbmn"
    - path: "packages/shared/src/stock.ts"
      provides: "Stock.tradeAmount 필드"
      contains: "tradeAmount"
    - path: "workers/ingestion/src/pipeline/map.ts"
      provides: "tradeAmount 매핑 라인"
      contains: "tradeAmount"
    - path: "workers/ingestion/src/pipeline/upsert.ts"
      provides: "trade_amount DB 컬럼 upsert"
      contains: "trade_amount"
    - path: "workers/ingestion/tests/map.test.ts"
      provides: "tradeAmount assert"
      contains: "tradeAmount"
  key_links:
    - from: "toStock() in map.ts"
      to: "Stock.tradeAmount"
      via: "Number(row.acml_tr_pbmn) 변환"
      pattern: "tradeAmount:\\s*Number\\(row\\.acml_tr_pbmn\\)"
    - from: "upsertStocks() rows 매핑"
      to: "stocks.trade_amount DB 컬럼"
      via: "snake_case 변환"
      pattern: "trade_amount:\\s*s\\.tradeAmount"
---

<objective>
공유 타입(`packages/shared`)과 ingestion 파이프라인을 확장해 KIS `acml_tr_pbmn` (누적 거래대금)을 Supabase `stocks.trade_amount` 컬럼에 upsert한다. 타입 → map → upsert → 테스트 순서로 수정하고, 기존 `volume` 경로는 **절대** 건드리지 않는다(D-01, D-08).

Purpose: D-04 ~ D-08 잠긴 결정을 반영해 이후 Wave에서 server/webapp이 참조할 공용 타입과 ingestion 기록 경로를 확립한다.
Output: 5개 파일 수정, vitest map.test.ts green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-scanner-db-inserted/05.2-CONTEXT.md
@.planning/phases/05.2-scanner-db-inserted/05.2-RESEARCH.md
@.planning/phases/05.2-scanner-db-inserted/05.2-VALIDATION.md
@packages/shared/src/kis.ts
@packages/shared/src/stock.ts
@workers/ingestion/src/pipeline/map.ts
@workers/ingestion/src/pipeline/upsert.ts
@workers/ingestion/tests/map.test.ts

<interfaces>
<!-- 현재 KisRankingRow (packages/shared/src/kis.ts) -->
```typescript
export type KisRankingRow = {
  hts_kor_isnm: string;
  mksc_shrn_iscd: string;
  stck_prpr: string;
  prdy_vrss: string;
  prdy_ctrt: string;
  acml_vol: string;  // ← 여기 바로 아래에 acml_tr_pbmn 추가
  stck_oprc: string;
  // ...
  [key: string]: string;  // 인덱스 시그니처 이미 존재
};
```

<!-- 현재 Stock (packages/shared/src/stock.ts) -->
```typescript
export type Stock = {
  code: string;
  name: string;
  market: Market;
  price: number;
  changeAmount: number;
  changeRate: number;
  volume: number;       // ← 유지 (D-08)
  open: number;
  // ...
  updatedAt: string;
};
```

<!-- 현재 toStock (workers/ingestion/src/pipeline/map.ts) -->
- `volume: Number(row.acml_vol)` 라인 바로 아래에 `tradeAmount: Number(row.acml_tr_pbmn)` 추가

<!-- 현재 upsertStocks rows 매핑 (workers/ingestion/src/pipeline/upsert.ts L16-31) -->
- `volume: s.volume,` 라인 바로 아래에 `trade_amount: s.tradeAmount,` 추가

<!-- 기존 map.test.ts fixture -->
- `sampleRankingRow`에 `acml_tr_pbmn: "<값>"` 필드 추가
- 두 `it()` 블록 모두에서 `expect(stock.tradeAmount).toBe(<숫자>)` assert 추가
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KisRankingRow에 acml_tr_pbmn, Stock에 tradeAmount 필드 추가 + 테스트 확장</name>
  <read_first>
    - packages/shared/src/kis.ts (기존 필드 순서 확인)
    - packages/shared/src/stock.ts (Stock 타입 현재 shape)
    - workers/ingestion/tests/map.test.ts (fixture 구조, KisRankingRow 속성)
    - .planning/phases/05.2-scanner-db-inserted/05.2-CONTEXT.md D-04, D-06, D-07, D-08
    - .planning/phases/05.2-scanner-db-inserted/05.2-RESEARCH.md §"2. KIS acml_tr_pbmn 필드 스키마"
  </read_first>
  <files>packages/shared/src/kis.ts, packages/shared/src/stock.ts, workers/ingestion/tests/map.test.ts</files>
  <behavior>
    - Test 1 (순위 + 시세 데이터): sampleRankingRow 에 `acml_tr_pbmn: "759264850"` fixture 값 추가 → `expect(stock.tradeAmount).toBe(759264850)`.
    - Test 2 (fallback 시): 동일 fixture 재사용 → `expect(stock.tradeAmount).toBe(759264850)` (fallback 분기에서도 거래대금은 ranking에서 옴).
    - TypeScript 빌드: `Stock.tradeAmount: number` non-optional이므로 map.ts가 수정되지 않으면 빌드 실패 — Task 2에서 해소.
  </behavior>
  <action>
    **Step 1 — `packages/shared/src/kis.ts` 수정:**
    `acml_vol: string;` 라인 바로 **아래**에 `acml_tr_pbmn: string;` 한 줄 추가. 순서는 KIS 문서의 필드 근접성(거래량 → 거래대금) 기준 유지.

    **Step 2 — `packages/shared/src/stock.ts` 수정:**
    `Stock` 타입에서 `volume: number;` 라인 바로 **아래**에 `tradeAmount: number;` 한 줄 추가. `volume` 필드는 **유지**(D-08). non-optional — `?` 사용 금지.

    **Step 3 — `workers/ingestion/tests/map.test.ts` 수정:**
    - `sampleRankingRow` 객체의 `acml_vol: "655925",` 라인 바로 아래에 `acml_tr_pbmn: "759264850",` 추가(= 759,264,850 원 ≈ 7.5억, 실측 가능한 현실적 값).
    - 첫 `it()` 내부, `expect(stock.volume).toBe(655925);` 바로 아래에 `expect(stock.tradeAmount).toBe(759264850);` 추가.
    - 두 번째 `it()` 내부, fallback 분기에도 `expect(stock.tradeAmount).toBe(759264850);` 추가 (ranking row에서 직접 오므로 priceData 유무와 무관하게 동일).

    **이 시점에서 `map.ts` 는 아직 수정 안 함 → 타입 에러 + 테스트 실패(RED)**. Task 2에서 GREEN 전환.

    **주의:**
    - `KisRankingRow` 인덱스 시그니처 `[key: string]: string` 이 이미 있어 타입 전파는 런타임 안전하지만, D-04 명시적 선언 원칙 준수.
    - `Stock.tradeAmount` 추가 시 server/webapp 빌드도 깨질 수 있음 → Wave 2~3에서 해소됨. 이 플랜 범위 내 `workers/ingestion` 의 빌드만 유효해야 함.
  </action>
  <acceptance_criteria>
    RED 단계 검증:
    - `grep -q 'acml_tr_pbmn: string' packages/shared/src/kis.ts` (1개 매칭)
    - `grep -q 'tradeAmount: number' packages/shared/src/stock.ts` (1개 매칭)
    - `grep -qE 'acml_tr_pbmn.*"?759264850"?' workers/ingestion/tests/map.test.ts`
    - `grep -c 'expect(stock.tradeAmount).toBe(759264850)' workers/ingestion/tests/map.test.ts` = 2
    - `cd workers/ingestion && npx vitest run tests/map.test.ts` → **FAIL** (map.ts 미수정 상태 → tradeAmount undefined).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'acml_tr_pbmn: string' packages/shared/src/kis.ts && grep -q 'tradeAmount: number' packages/shared/src/stock.ts && grep -qE 'acml_tr_pbmn.*759264850' workers/ingestion/tests/map.test.ts</automated>
  </verify>
  <done>
    3개 파일 수정 완료. vitest 실행 시 RED(map.ts 미수정) 상태 — Task 2 GREEN 선행.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: map.ts tradeAmount 매핑 + upsert.ts trade_amount 컬럼 + vitest GREEN</name>
  <read_first>
    - workers/ingestion/src/pipeline/map.ts (toStock 반환 객체 구조)
    - workers/ingestion/src/pipeline/upsert.ts (rows 매핑 snake_case 변환 패턴)
    - Task 1 결과 (kis.ts/stock.ts/map.test.ts)
    - .planning/phases/05.2-scanner-db-inserted/05.2-CONTEXT.md D-05
    - .planning/phases/05.2-scanner-db-inserted/05.2-RESEARCH.md §"7. 배포 순서 Pitfall" P-3, P-4 (COLS/upsert 매핑 누락 주의)
  </read_first>
  <files>workers/ingestion/src/pipeline/map.ts, workers/ingestion/src/pipeline/upsert.ts</files>
  <behavior>
    - Test 1: `stock.tradeAmount === 759264850` (Number("759264850") 변환 검증).
    - Test 2 (fallback): priceData 없을 때도 `stock.tradeAmount === 759264850` (ranking row에서 직접 옴).
    - Edge: `Number("")` 는 `0` — 장 외 시간 등 빈 문자열 입력 안전.
  </behavior>
  <action>
    **Step 1 — `workers/ingestion/src/pipeline/map.ts` 수정:**
    `toStock()` 의 반환 객체에서 `volume: Number(row.acml_vol),` 라인 바로 **아래**에 한 줄 추가:

    ```typescript
    tradeAmount: Number(row.acml_tr_pbmn),
    ```

    기존 필드 순서/위치 건드리지 말고 **volume 바로 아래** 고정.

    **Step 2 — `workers/ingestion/src/pipeline/upsert.ts` 수정:**
    rows 매핑(L16-31)에서 `volume: s.volume,` 라인 바로 **아래**에 한 줄 추가:

    ```typescript
    trade_amount: s.tradeAmount,
    ```

    **주의:**
    - snake_case 변환 규칙 준수 (`s.tradeAmount` → `trade_amount`).
    - P-3 pitfall: 이 라인 누락 시 DB에 DEFAULT 0만 기록됨. 반드시 포함.

    **Step 3 — vitest 실행:**
    `cd workers/ingestion && npx vitest run tests/map.test.ts` → **GREEN**. 모든 assert 통과.

    **주의(빌드):**
    - 이 시점에서 `server/` 와 `webapp/` 은 여전히 `Stock.tradeAmount` 를 참조하지 않으므로 TypeScript 빌드가 깨질 수 있음. `Stock`이 non-optional이라 `rowToStock()` 에서 tradeAmount 누락 시 `StockWithProximity` 리턴 타입 충돌. 이는 Wave 2 (Plan 03) 에서 해소된다. **이 플랜은 workers/ingestion 빌드만 green 요구** — server/webapp 빌드는 Wave 2 완료 후 검증.
  </action>
  <acceptance_criteria>
    GREEN 단계 검증 (VALIDATION.md 05.2-02-01, 05.2-02-02):
    - `grep -q 'tradeAmount: Number(row.acml_tr_pbmn)' workers/ingestion/src/pipeline/map.ts`
    - `grep -q 'trade_amount: s.tradeAmount' workers/ingestion/src/pipeline/upsert.ts`
    - `grep -q 'acml_tr_pbmn' packages/shared/src/kis.ts && grep -q 'trade_amount' workers/ingestion/src/pipeline/map.ts` (05.2-02-02 grep 행 — map.ts에 `trade_amount` 문자열이 upsert.ts로 이동했으므로 실제는 upsert.ts에서 grep)
    - `cd workers/ingestion && npx vitest run tests/map.test.ts` → PASS (tradeAmount 관련 assert 2개 포함).
    - `cd workers/ingestion && pnpm build` (있다면) 또는 `cd workers/ingestion && npx tsc --noEmit` → 타입 에러 없음.
  </acceptance_criteria>
  <verify>
    <automated>cd workers/ingestion && npx vitest run tests/map.test.ts</automated>
  </verify>
  <done>
    map.ts 에 `tradeAmount: Number(row.acml_tr_pbmn)` 추가됨. upsert.ts 에 `trade_amount: s.tradeAmount` 추가됨. vitest map.test.ts 2/2 PASS.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KIS API → ingestion | 외부 JSON 문자열 → Number() 변환. 악의 입력보다는 형식 오류 관심사. |
| ingestion → Supabase | service_role 키로 upsert, RLS 우회. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-02-01 | Tampering | `Number(row.acml_tr_pbmn)` 비숫자 입력 (예: "abc") | accept | `Number("abc") === NaN` → Supabase bigint column으로 upsert 시 에러 raise. 기존 `volume` 경로도 동일 약점이며 Phase 1에서 accept 결정. KIS 응답은 신뢰 API. |
| T-05.2-02-02 | Information Disclosure | tradeAmount non-optional 타입 누출로 server/webapp 빌드 파손 | mitigate | Wave 순서 보장 — Plan 03/04가 각각 server/webapp 소비 경로를 동시에 맞춰 non-optional 계약 유지. CI/배포 전 전체 workspace 빌드 확인(Wave 4). |
</threat_model>

<verification>
- `cd workers/ingestion && npx vitest run` (VALIDATION 05.2-02-01)
- `grep -q "acml_tr_pbmn" packages/shared/src/kis.ts && grep -q "trade_amount" packages/shared/src/stock.ts && grep -q "tradeAmount" workers/ingestion/src/pipeline/map.ts` — stock.ts는 camelCase `tradeAmount`. VALIDATION 05.2-02-02 행은 `trade_amount` 를 stock.ts에서 찾지만 실제로는 camelCase 규약. 실제 grep 실행 시 `tradeAmount|trade_amount` 둘 중 하나라도 존재하면 통과.
- `grep -q 'tradeAmount' packages/shared/src/stock.ts && grep -q 'tradeAmount' workers/ingestion/src/pipeline/map.ts` — planner가 VALIDATION.md 문구와 실제 파일 컨벤션 간 불일치를 주석 처리. executor는 camelCase grep 기준으로 통과 확인.
</verification>

<success_criteria>
1. `KisRankingRow` 에 `acml_tr_pbmn: string` 명시적 필드 존재.
2. `Stock` 에 `tradeAmount: number` non-optional 존재. `volume: number` 유지.
3. `toStock()` 반환 객체에 `tradeAmount: Number(row.acml_tr_pbmn)` 존재.
4. `upsertStocks()` rows 매핑에 `trade_amount: s.tradeAmount` 존재.
5. `cd workers/ingestion && npx vitest run tests/map.test.ts` 전부 PASS (기존 2 test + tradeAmount assert 2개).
</success_criteria>

<output>
완료 후 `.planning/phases/05.2-scanner-db-inserted/05.2-02-SUMMARY.md` 작성.
</output>
