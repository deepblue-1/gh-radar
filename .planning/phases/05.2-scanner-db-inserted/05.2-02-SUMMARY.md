---
phase: 05.2
plan: 02
subsystem: shared-types-ingestion
tags: [shared, ingestion, kis, supabase, tdd]
requires:
  - "supabase/migrations/20260414110000_add_trade_amount.sql (Plan 01)"
provides:
  - "KisRankingRow.acml_tr_pbmn 필드 (shared)"
  - "Stock.tradeAmount: number non-optional (shared)"
  - "toStock() 의 tradeAmount 매핑"
  - "upsertStocks() 의 trade_amount 컬럼 기록"
affects:
  - "server/webapp 빌드 (Stock.tradeAmount non-optional → Wave 2/3 에서 후속 수정)"
tech_stack_added: []
patterns:
  - "TDD RED → GREEN 두 commit 분리"
  - "shared 타입 → ingestion 매핑 → upsert 컬럼 일관 전파"
  - "Number() 캐스팅으로 KIS 문자열 → JS number"
key_files_created: []
key_files_modified:
  - "packages/shared/src/kis.ts"
  - "packages/shared/src/stock.ts"
  - "workers/ingestion/src/pipeline/map.ts"
  - "workers/ingestion/src/pipeline/upsert.ts"
  - "workers/ingestion/tests/map.test.ts"
decisions:
  - "D-04~D-08 잠금 결정 그대로 반영 (volume 유지, tradeAmount non-optional, snake_case DB 컬럼명)"
metrics:
  duration_minutes: 3
  tasks_completed: "2/2"
  completed_at: "2026-04-14"
---

# Phase 05.2 Plan 02: Shared Types + Ingestion 거래대금 파이프라인 Summary

KIS `acml_tr_pbmn`(누적 거래대금)을 ingestion 파이프라인에서 캡처해 Supabase `stocks.trade_amount` 컬럼으로 upsert하는 전체 경로를 구축. 공유 타입(`KisRankingRow.acml_tr_pbmn`, `Stock.tradeAmount`)을 먼저 확장해 단일 진리원을 확립했다.

## What Was Built

| Layer | File | Change |
|-------|------|--------|
| Shared types | `packages/shared/src/kis.ts` | `acml_tr_pbmn: string` 필드 명시 |
| Shared types | `packages/shared/src/stock.ts` | `tradeAmount: number` non-optional 추가, `volume` 유지 |
| Ingestion mapper | `workers/ingestion/src/pipeline/map.ts` | `toStock()` 에 `tradeAmount: Number(row.acml_tr_pbmn)` |
| Ingestion upsert | `workers/ingestion/src/pipeline/upsert.ts` | rows 매핑에 `trade_amount: s.tradeAmount` |
| Ingestion tests | `workers/ingestion/tests/map.test.ts` | fixture `acml_tr_pbmn: "759264850"` + 두 it() 에 assert |

## Decisions Made

- D-04 / D-05 / D-07: 타입과 매핑 일관 전파 — KIS 응답 → shared Stock → DB row 변환에서 거래대금이 누락되지 않도록 5개 파일 동시 수정.
- D-08: `volume` 컬럼/필드는 유지 — 지금은 미사용이지만 미래 확장 여지를 보존.
- TDD: Task 1(타입+테스트만)을 RED commit으로, Task 2(map.ts/upsert.ts 구현)를 GREEN commit으로 분리해 변경 의도를 git history 에서 명확하게 추적.

## Deviations from Plan

- **VALIDATION 05.2-02-02 grep 표현 보정 (사전 noted):** plan 본문에 명시된 대로 `stock.ts` 는 camelCase `tradeAmount` 사용 — VALIDATION.md 의 `grep "trade_amount" stock.ts` 표현은 부정확하므로 plan의 명시적 컨벤션(`tradeAmount`)을 기준으로 검증 통과.
- **shared 패키지 빌드 트리거:** `workers/ingestion` 의 `npx tsc --noEmit` 가 stale `dist/index.d.ts` 를 보고 있어 한 번 `pnpm build` (packages/shared) 실행 후 통과 확인. 코드 변경 아니므로 commit에 미반영.

## Verification

- `grep -q 'acml_tr_pbmn: string' packages/shared/src/kis.ts` → PASS
- `grep -q 'tradeAmount: number' packages/shared/src/stock.ts` → PASS
- `grep -qE 'acml_tr_pbmn.*759264850' workers/ingestion/tests/map.test.ts` → PASS
- `grep -c 'expect(stock.tradeAmount).toBe(759264850)' workers/ingestion/tests/map.test.ts` → 2
- `grep -q 'tradeAmount: Number(row.acml_tr_pbmn)' workers/ingestion/src/pipeline/map.ts` → PASS
- `grep -q 'trade_amount: s.tradeAmount' workers/ingestion/src/pipeline/upsert.ts` → PASS
- `cd workers/ingestion && npx vitest run tests/map.test.ts` → 2/2 PASS
- `cd workers/ingestion && npx tsc --noEmit` → no errors

VALIDATION 05.2-02-01, 05.2-02-02 충족.

## Commits

- `0765012` test(05.2-02): KisRankingRow.acml_tr_pbmn / Stock.tradeAmount 타입 + map 테스트 추가 (RED)
- `64ece2c` feat(05.2-02): toStock 에 tradeAmount 매핑 + upsertStocks 에 trade_amount 컬럼 기록 (GREEN)

## Self-Check: PASSED

- All modified files exist — FOUND
- Both commits exist — FOUND
- vitest GREEN — PASS
