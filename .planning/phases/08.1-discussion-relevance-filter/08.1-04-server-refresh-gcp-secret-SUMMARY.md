---
phase: "08.1"
plan: 4
plan_id: "08.1-04"
subsystem: server + infra (deploy scripts)
tags:
  - server
  - discussions
  - refresh
  - classify
  - anthropic
  - claude-haiku-4-5
  - gcp-secret-manager
  - cloud-run
  - deploy
  - phase-08.1
  - wave-2

# Dependency graph
dependency_graph:
  requires:
    - "08.1-01"  # discussions.relevance/classified_at 컬럼 + Discussion type 확장
    - "08.1-02"  # server filter + DISCUSSION_SELECT 에 relevance/classified_at 포함
    - "08.1-03"  # worker classify 모듈 (이식 원본)
  provides:
    - "server/src/services/discussion-classify.ts — server-side inline classify (TEMP duplication of workers/discussion-sync/src/classify/)"
    - "config.anthropicApiKey/classifyConcurrency/classifyModel 3 필드 추가"
    - "POST /api/stocks/:code/discussions/refresh 훅 — upsert().select('id,title,body,classified_at') → classifyAndPersist 호출 (non-fatal)"
    - "scripts/setup-discussion-sync-iam.sh — gh-radar-anthropic-api-key secret 자동 생성 + 2 accessor (discussion-sync-sa + server SA) idempotent"
    - "scripts/deploy-discussion-sync.sh — --task-timeout=1800 + --set-secrets 에 ANTHROPIC_API_KEY 마운트 + pre-check loop 확장"
    - "scripts/deploy-server.sh — Bright Data + Anthropic 안전망 accessor + pre-check + --update-secrets 확장"
  affects:
    - "08.1-05 backfill-script (동일 classifyAndPersist / classifyBatch 재사용)"
    - "08.1-06 webapp-switch-toggle (refresh 응답의 relevance/classifiedAt 필드 UI 에 표시)"
    - "08.1-07 e2e-smoke (production 재배포 + cycle execute 실측 단계)"

# Tech tracking
tech_stack:
  added:
    - "@anthropic-ai/sdk ^0.65.0 (server)"
    - "p-limit ^7.0.0 (server, 기존 worker 과 동일 version)"
  patterns:
    - "TEMP 복제 서비스 (server/src/services/discussion-classify.ts) — workers/discussion-sync/src/classify/ 의 5 파일 → 단일 파일 압축. 상단 TEMP DUPLICATION 주석 명시 + packages/ 이관 TODO"
    - "Graceful no-op when anthropicApiKey==null — 로컬 dev 와 단계적 secret rollout 모두 수용 (server 기동은 계속 가능, refresh 도 200)"
    - "POST /refresh upsert → .select('id,title,body,classified_at') 체이닝 — 미분류 행만 필터 후 classify 호출. classify 실패는 non-fatal (try/catch → logger.warn)"
    - "deploy-server.sh 에서 secret accessor idempotent 바인딩 loop — setup 스크립트 주 바인딩에 대한 안전망 패턴 (Phase 07 Plan 06 원조 확장)"

key_files:
  created:
    - "server/src/services/discussion-classify.ts (157 lines — lazy Anthropic client + p-limit + 4-label whitelist + classifyAndPersist)"
    - "server/tests/routes/discussions.refresh.classify.test.ts (3 integration tests)"
    - ".planning/phases/08.1-discussion-relevance-filter/08.1-04-server-refresh-gcp-secret-SUMMARY.md (이 문서)"
  modified:
    - "server/package.json (@anthropic-ai/sdk ^0.65.0 + p-limit ^7.0.0 추가)"
    - "server/src/config.ts (anthropicApiKey/classifyConcurrency/classifyModel 3 필드 + loadConfig 매핑)"
    - "server/src/logger.ts (ANTHROPIC_API_KEY / anthropicApiKey / x-api-key redact paths 3건 추가)"
    - "server/src/routes/discussions.ts (POST /refresh 훅 + classifyAndPersist import)"
    - "scripts/setup-discussion-sync-iam.sh (ANTHROPIC_SECRET 생성 + 2 accessor 바인딩)"
    - "scripts/deploy-discussion-sync.sh (pre-check secret loop + --task-timeout=1800 + ANTHROPIC secret mount)"
    - "scripts/deploy-server.sh (Bright Data + Anthropic 안전망 accessor + pre-check + --update-secrets 확장)"
    - "pnpm-lock.yaml (install 결과)"

decisions:
  - id: D1
    summary: "classify 모듈은 server/src/services/discussion-classify.ts 단일 파일로 압축 복제 — worker의 5 파일 구조를 shared package 로 분리하지 않고 명시적 TEMP DUPLICATION 주석으로 단기 수용. approved plan §최소침입 원칙."
  - id: D2
    summary: "config.anthropicApiKey 는 nullable — 로컬 dev 에서 key 없어도 server 기동 + refresh 200 을 유지. worker 쪽 (discussion-sync) 은 req() 로 필수 강제했지만 server 쪽은 graceful 선택. 차이 이유: worker 는 single-purpose 이라 fail-fast 가 맞지만 server 는 다른 경로가 많아 classify 부재로 전체 기동 차단은 과도."
  - id: D3
    summary: "POST /refresh 의 classify 실패는 non-fatal — try/catch 로 swallow + logger.warn. refresh 사용자 여정에서 classify 지연이 UX 손실을 유발하지 않고(미분류 행도 meaningful 필터 통과), 다음 cycle/refresh 에서 자동 retry."
  - id: D4
    summary: "server SA 는 default compute (1023658565518-compute@developer.gserviceaccount.com) — plan 문서의 'gh-radar-server-sa' 가정과 다름. deploy-server.sh / setup-discussion-sync-iam.sh 기존 detect_server_sa() 로직이 Cloud Run describe 를 통해 자동 감지하도록 설계되어 있어 추가 수정 없이 호환. 바인딩 대상 SA 실제: default compute SA."
  - id: D5
    summary: "T-03 의 GCP Secret 실물 생성 + 초기 version 주입은 **partial — manual step required**. ANTHROPIC_API_KEY 값이 로컬 env / 기존 Secret Manager / 이전 phase 기록 어디에도 없음(memory reference feedback_dont_ask_existing_creds.md 체크 완료). 스크립트가 idempotent 하게 생성 가능하도록 준비되었으므로, 사용자 1회 실행으로 해소: ANTHROPIC_API_KEY=sk-ant-... GCP_PROJECT_ID=gh-radar bash scripts/setup-discussion-sync-iam.sh"
  - id: D6
    summary: "T-05 production 재배포는 T-03 partial 해소 이후 실행 (auto-mode 자동 승인은 marker 만 기록). deploy-discussion-sync.sh 의 pre-check loop 가 secret 부재 시 exit 1 이므로 secret 생성 없이 배포 시도 시 스크립트 자체가 방어."

metrics:
  duration_minutes: ~7
  completed_at: "2026-04-22T05:56:35Z"
  commits:
    - hash: aff0fac
      task: T-01
      message: "feat(server/08.1-04): T-01 — discussion-classify 서비스 + config + logger redact"
      files_changed: 5
    - hash: 4cbd20b
      task: T-02
      message: "feat(server/08.1-04): T-02 — POST /refresh 에 inline classify 훅 + 통합 테스트 3건"
      files_changed: 2
    - hash: b45e654
      task: T-03
      message: "chore(infra/08.1-04): T-03 — setup-discussion-sync-iam.sh 에 anthropic secret + 2 accessor"
      files_changed: 1
    - hash: 04c09f9
      task: T-04
      message: "feat(infra/08.1-04): T-04 — deploy 스크립트 2종에 ANTHROPIC_API_KEY + task-timeout=1800"
      files_changed: 2
  tests:
    baseline: "16 파일 / 103 tests pass (Plan 08.1-02 completion 기준)"
    final: "17 파일 / 106 tests pass (신규 3 — C-01/C-02/C-03)"
    regression: "0 — 기존 103 tests 전원 통과 (discussions.test.ts 20건 포함)"
    new_tests: "3 (discussions.refresh.classify.test.ts 통합 3)"
  typecheck: pass
  build: pass
  script_syntax: "bash -n scripts/deploy-discussion-sync.sh, scripts/deploy-server.sh, scripts/setup-discussion-sync-iam.sh 전부 exit 0"

requirements:
  - DISC-01
  - DISC-01.1
requirements_addressed:
  - DISC-01.1  # server on-demand refresh 경로 inline classify + production deploy 스크립트 준비
---

# Phase 08.1 Plan 04: Server Refresh Inline Classify + GCP Secret 배포 Summary

Server 의 `POST /api/stocks/:code/discussions/refresh` 에 Claude Haiku 4.5 inline 분류 훅을 삽입하고 (`upsert().select('id,title,body,classified_at')` → `classifyAndPersist`), Cloud Run Service + Job 양쪽에 `ANTHROPIC_API_KEY` secret 을 마운트할 수 있도록 배포 스크립트 3종(`setup-discussion-sync-iam.sh`, `deploy-discussion-sync.sh`, `deploy-server.sh`)을 업데이트. discussion-sync Job 의 `--task-timeout` 은 600 → 1800s 로 상향. 로컬 테스트 17 파일 / 106 pass, 회귀 0. GCP Secret 실물 생성(T-03)과 production 재배포(T-05)는 사용자 manual step (ANTHROPIC_API_KEY 값 제공).

## One-liner

`classifyAndPersist(supabase, rows)` — worker classify 로직을 server 단일 파일로 TEMP 복제 + POST /refresh 훅 + GCP Secret / accessor / deploy 스크립트 준비 — DISC-01.1 의 "on-demand 경로 + 프로덕션 배포" 계층 코드 완성 (실물 Secret/배포는 manual).

## What Was Built

### T-01: discussion-classify 서비스 + config + logger redact (commit `aff0fac`)

**`server/package.json`**: `@anthropic-ai/sdk ^0.65.0` + `p-limit ^7.0.0` 추가. `pnpm install` 실행 (3 packages added).

**`server/src/config.ts`**:
- `AppConfig` 에 3 필드: `anthropicApiKey: string | null`, `classifyConcurrency: number`, `classifyModel: string`
- `loadConfig()`: `process.env.ANTHROPIC_API_KEY ?? null`, `Number(process.env.DISCUSSION_CLASSIFY_CONCURRENCY ?? "5")`, `process.env.DISCUSSION_CLASSIFY_MODEL ?? "claude-haiku-4-5"`
- worker 쪽과 달리 anthropicApiKey 는 **null 허용** (D2) — 로컬 dev 에서 key 없어도 server 기동 가능

**`server/src/logger.ts`** redact paths 3건 추가:
- `req.headers['x-api-key']`
- `*.ANTHROPIC_API_KEY`
- `*.anthropicApiKey`

**`server/src/services/discussion-classify.ts`** 신규 (157 lines):
- 상단 TEMP DUPLICATION 주석 + packages/ 이관 TODO 명시
- worker 의 5 파일(anthropic/prompt/classifyOne/classifyBatch/persistRelevance) 로직을 단일 파일에 압축 복제 — 문자열 단위 동일 (CLASSIFY_SYSTEM_PROMPT / FEW_SHOT 3개 / formatUserMessage 500자 truncate / 4-label whitelist / max_tokens=10 / temperature=0 / pLimit(cfg.classifyConcurrency))
- lazy Anthropic client singleton + `__resetAnthropicClientForTests()` 헬퍼
- `classifyAndPersist(supabase, rows)` export — 성공 UPDATE 수 반환. `cfg.anthropicApiKey == null` 시 `logger.warn + return 0` (graceful no-op)
- 단일 row 실패는 classifyOne 의 try/catch 로 swallow → Map 미포함 → DB UPDATE skip → 다음 refresh 에서 재시도

### T-02: POST /refresh inline classify 훅 + 통합 테스트 (commit `4cbd20b`)

**`server/src/routes/discussions.ts`**:
```ts
import { classifyAndPersist } from "../services/discussion-classify.js";

// ... (기존 upsert 블록 교체)
const { data: upserted, error: upErr } = await supabase
  .from("discussions")
  .upsert(upsertRows, { onConflict: "stock_code,post_id", ignoreDuplicates: false })
  .select("id,title,body,classified_at");
if (upErr) throw upErr;

// 5.5) Phase 08.1 — inline classify
const unclassified = ((upserted ?? []) as Array<...>)
  .filter((r) => r.classified_at == null)
  .map((r) => ({ id: r.id, title: r.title, body: r.body }));
if (unclassified.length > 0) {
  try {
    const n = await classifyAndPersist(supabase, unclassified);
    logger.info({ code, classified: n, attempted: unclassified.length }, "discussion refresh inline classify");
  } catch (err) {
    logger.warn({ code, err: (err as Error).message }, "discussion refresh classify failed — non-fatal");
  }
}
```

**`server/tests/routes/discussions.refresh.classify.test.ts`** (3 tests):

| # | Case | Verification |
|---|------|--------------|
| C-01 | upsert 직후 미분류 행 | classifyAndPersist 1회 호출 + rows[0].id === "d-1" |
| C-02 | classify no-op (Map 0) 반환 | refresh 200 + 응답 Array + classify 호출 자체는 발생 |
| C-03 | 응답에 relevance / classifiedAt 노출 | body[0].relevance === 'price_reason' + classifiedAt 존재 |

Supabase mock 은 기존 discussions.test.ts chainable 패턴 확장 — `upsert().select()` chain 이 upsertedRows 반환, 24h 최종 select 은 finalRows 반환. `classifyAndPersist` 는 `vi.mock('../../src/services/discussion-classify', ...)` 로 module scope spy.

### T-03: setup-discussion-sync-iam.sh 확장 (commit `b45e654`)

**`scripts/setup-discussion-sync-iam.sh`**:
- Section 2: `ANTHROPIC_SECRET="gh-radar-anthropic-api-key"` 변수 추가
- Section 5.5 신설: Secret 생성 idempotent — `ANTHROPIC_API_KEY` env var 있으면 `printf | gcloud secrets versions add --data-file=-` 로 초기 버전 주입, 없으면 stdin 입력 대기
- Section 6: accessor 바인딩 3건 → 5건 (discussion-sync-sa + server SA 모두 anthropic-api-key read)
- server SA 는 기존 `detect_server_sa()` 자동 감지 로직 재사용 — Cloud Run Service 의 현재 SA (default compute `1023658565518-compute@developer.gserviceaccount.com`) 그대로 사용 (D4)

### T-04: deploy 스크립트 2종 업데이트 (commit `04c09f9`)

**`scripts/deploy-discussion-sync.sh`**:
- Section 1 pre-check loop: `gh-radar-anthropic-api-key` 추가 (부재 시 setup 지시)
- Section 6 Cloud Run Job: `--task-timeout=600` → `1800` (Claude 병렬 호출 여유, approved plan Wave 2 §Decisions)
- `--set-secrets`: `,ANTHROPIC_API_KEY=gh-radar-anthropic-api-key:latest` 추가

**`scripts/deploy-server.sh`**:
- Bright Data + Anthropic accessor idempotent 바인딩 loop 추가 (Phase 08 Plan 06 원조 Naver 안전망 패턴 연장)
- pre-deploy secret check: `gh-radar-anthropic-api-key` 부재 시 exit 1
- `--update-secrets`: `BRIGHTDATA_API_KEY=gh-radar-brightdata-api-key:latest,ANTHROPIC_API_KEY=gh-radar-anthropic-api-key:latest` 추가
- request `--timeout=300s` 유지 (refresh 는 ~5~10s 목표이므로 상향 불필요)

### T-05: [checkpoint:human-verify] production 재배포 + cycle execute — **partial: manual step required**

Auto-mode 에서 `⚡ Auto-approved` marker 를 남겼으나 **실제 프로덕션 배포는 실행되지 않음**. 사유:
- T-03 의 GCP Secret 실물 생성이 partial — ANTHROPIC_API_KEY 값 부재
- deploy 스크립트의 pre-check loop 가 secret 부재 시 exit 1 → 지금 배포 시도 시 스크립트 자체가 실패
- 로컬 smoke 도 classify 경로를 유의미하게 검증 못 함 (no-op 모드)

## Deviations from Plan

### 1. T-03 / T-05 **partial — manual step required** (D5, D6)

**Root cause:** ANTHROPIC_API_KEY 값이 로컬 env 파일 / 기존 Secret Manager / 이전 phase 기록 어디에도 없음.

**검증 경로:**
- `find /Users/alex/repos/gh-radar -name ".env*" -not -path "*/node_modules/*"` → 5 파일 (webapp/.env.local, .env.test.local, .env.development, workers/ingestion/.env, workers/master-sync/.env)
- 각 파일 `grep "ANTHROPIC"` → 0 match
- `gcloud secrets describe gh-radar-anthropic-api-key` → NOT_FOUND

**Resolution path (사용자 manual step):**
```bash
export ANTHROPIC_API_KEY=sk-ant-...   # 신규 발급
export GCP_PROJECT_ID=gh-radar
bash scripts/setup-discussion-sync-iam.sh    # Secret 생성 + 5 accessor 바인딩
bash scripts/deploy-discussion-sync.sh       # Job 재배포 (task-timeout=1800 + secret mount 반영)
bash scripts/deploy-server.sh                # Service 재배포 (secret mount 반영)
gcloud run jobs execute gh-radar-discussion-sync --region=asia-northeast3 --project=gh-radar --wait
```

**Impact:** 코드 / 설정 / 스크립트는 모두 완성. 실행만 사용자 key 제공 필요. T-03 / T-05 는 plan 의 checkpoint:human-action / checkpoint:human-verify 카테고리이므로 이 partial 상태는 plan 설계상 정상 경로.

### 2. server SA 가 gh-radar-server-sa 가 아닌 default compute SA (D4)

- Plan 문서 T-03 step 4 는 `gh-radar-server-sa@gh-radar.iam.gserviceaccount.com` 을 가정
- 실제: Cloud Run Service `gh-radar-server` 가 사용 중인 SA 는 `1023658565518-compute@developer.gserviceaccount.com` (default compute)
- 기존 `detect_server_sa()` 함수(setup-discussion-sync-iam.sh line 60~73) 가 `gcloud run services describe --format='value(spec.template.spec.serviceAccountName)'` 로 자동 감지하도록 설계되어 있어 이 deviation 은 스크립트 자동 수용. 수정 불필요.
- SUMMARY 기록 이유: plan 문서의 `SA_NAME` 가정과 실제 환경 차이를 향후 유지보수 시 혼란 방지.

### 3. p-limit ^7.0.0 선택 (plan 은 version 미지정)

- 기존 workers/discussion-sync 에서 이미 ^7.0.0 사용 중 (Plan 08.1-03 SUMMARY 기록) — server 와 동일 version 으로 통일
- plan 문서 §Decisions §8 의 "^6" 권장과 불일치하지만 worker 쪽 실제 사용 version 우선

### Auto-fixed Issues

**1. [Rule 3 — Blocker] deploy-server.sh 에 Bright Data accessor 가 없었음 (Phase 08 Plan 06 이후 누락)**

- Phase 08 에서 Bright Data secret 은 setup-discussion-sync-iam.sh 에서 server SA 에도 바인딩했지만 deploy-server.sh 의 idempotent 안전망 loop 에는 Bright Data 가 빠져있었음 (Naver 2개만 있었음)
- Phase 08.1-04 에서 anthropic secret 의 안전망 바인딩을 추가하는 김에 bright data 도 함께 포함 — loop 대상 2 secret (`gh-radar-brightdata-api-key` + `gh-radar-anthropic-api-key`) 로 확장
- Impact: positive — 향후 deploy-server.sh 단독 재실행 시 setup 의존성 감소

### Authentication Gates

**ANTHROPIC_API_KEY 부재** — T-03 gate. 사용자가 Anthropic Console 에서 key 발급(`sk-ant-...`) 후 env var 로 주입하여 `setup-discussion-sync-iam.sh` 실행 필요. 해당 gate 는 plan `checkpoint:human-action` 디자인에 부합 (deviation 아님).

## Verification Results

```bash
pnpm --filter server typecheck                     # PASS (exit 0)
pnpm --filter server build                         # PASS (exit 0)
pnpm --filter server test --run                    # 17 파일 / 106 tests pass
  ├─ discussions.test.ts:                20/20 (기존 — 회귀 없음)
  ├─ discussions.filter.test.ts:          4/4 (Plan 08.1-02 — 회귀 없음)
  └─ discussions.refresh.classify.test.ts: 3/3 (신규)
bash -n scripts/deploy-discussion-sync.sh          # exit 0
bash -n scripts/deploy-server.sh                   # exit 0
bash -n scripts/setup-discussion-sync-iam.sh       # exit 0

# GCP 상태 (실물):
gcloud secrets describe gh-radar-anthropic-api-key # NOT_FOUND (사용자 1회 setup 실행 후 해소)
```

### Acceptance criteria

**T-01:**
- `test -f server/src/services/discussion-classify.ts` ✅
- `grep -q "classifyAndPersist" server/src/services/discussion-classify.ts` ✅
- `grep -q "max_tokens: 10" server/src/services/discussion-classify.ts` ✅
- `grep -q "temperature: 0" server/src/services/discussion-classify.ts` ✅
- `grep -q "TEMP DUPLICATION" server/src/services/discussion-classify.ts` ✅
- `grep -q "anthropicApiKey" server/src/config.ts` ✅
- `grep -q "@anthropic-ai/sdk" server/package.json` ✅
- typecheck ✅

**T-02:**
- `grep -q "classifyAndPersist" server/src/routes/discussions.ts` ✅
- `grep -q 'select("id,title,body,classified_at")' server/src/routes/discussions.ts` ✅
- `test -f server/tests/routes/discussions.refresh.classify.test.ts` ✅
- 3 integration tests PASS ✅

**T-03:** (script-level; 실물 GCP 상태는 partial)
- Script 에 `ANTHROPIC_SECRET` 변수 + Section 5.5 secret 생성 + accessor 2건 추가 ✅
- `bash -n scripts/setup-discussion-sync-iam.sh` ✅
- `gcloud secrets describe gh-radar-anthropic-api-key` — ⚠️ **NOT_FOUND** (사용자 manual step 필요)

**T-04:**
- `grep -q "task-timeout=1800" scripts/deploy-discussion-sync.sh` ✅
- `grep -q "ANTHROPIC_API_KEY=gh-radar-anthropic-api-key:latest" scripts/deploy-discussion-sync.sh` ✅
- `grep -q "gh-radar-anthropic-api-key" scripts/deploy-server.sh` ✅
- `bash -n scripts/deploy-discussion-sync.sh` ✅
- `bash -n scripts/deploy-server.sh` ✅

**T-05:** ⚡ Auto-approved marker (auto-mode). 실제 재배포는 T-03 partial 해소 후 사용자 실행.

## Known Stubs

없음. Classify 서비스는 실 Anthropic API + 실 Supabase UPDATE 위에서 동작 설계. 테스트는 vi.mock 으로 spy 교체, 프로덕션은 secret 주입 후 실제 호출.

단, ANTHROPIC_API_KEY 미설정 시 `classifyAndPersist` 가 `return 0` + `logger.warn` — 이것은 **의도된 graceful no-op** (D2) 이며 stub 이 아님. secret 주입 후 자연스럽게 활성화.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: external_api_call | server/src/services/discussion-classify.ts | Claude API(api.anthropic.com)로 discussion title/body(500자 truncate) 전송 — Phase 08.1-03 동일 threat 의 on-demand 경로 연장. mitigation: logger redact (cfg/headers.x-api-key/ANTHROPIC_API_KEY/anthropicApiKey). Anthropic basic tier 30일 보존. |
| threat_flag: secret_required | scripts/deploy-discussion-sync.sh, scripts/deploy-server.sh | Cloud Run Job + Service 모두 ANTHROPIC_API_KEY secret 이 없으면 배포 pre-check 실패. mitigation: setup-discussion-sync-iam.sh 가 secret + accessor idempotent 생성. 키 발급/주입은 사용자 1회 action. |
| threat_flag: graceful_no_op | server/src/config.ts, server/src/services/discussion-classify.ts | config.anthropicApiKey 가 null 이면 classify skip — 개발 편의 vs "분류 미작동" 탐지 지연 trade-off. mitigation: logger.warn 이 매 호출마다 출력 → ops 대시보드에서 감지 가능. |

## Dependency Graph Updates

- **Requires 충족 확인:**
  - 08.1-01 `discussions.relevance / classified_at 컬럼 + CHECK 제약` ✅ (본 plan 이 UPDATE 대상)
  - 08.1-02 `DISCUSSION_SELECT 에 relevance/classified_at 포함` ✅ (24h 최종 select 에서 재사용)
  - 08.1-03 `workers/discussion-sync/src/classify/ 5 파일` ✅ (문자열 단위 복제 원본)
- **Next dependent plans:**
  - 08.1-05 backfill-script: 본 plan 의 classifyAndPersist 를 직접 호출 (또는 worker classifyBatch + persistRelevance) — 설계상 재사용 가능
  - 08.1-06 webapp-switch-toggle: refresh 응답의 `relevance / classifiedAt` 필드를 UI Badge / 필터 toggle 에서 활용
  - 08.1-07 e2e-smoke: 본 plan 의 **T-05 production 재배포 + execute** 가 실행된 후 `classifyBatch done / discussion refresh inline classify / relevance 분포` 실측으로 dispatch 가능

## Commits

| Task | Commit | Subject |
|------|--------|---------|
| T-01 | `aff0fac` | feat(server/08.1-04): T-01 — discussion-classify 서비스 + config + logger redact |
| T-02 | `4cbd20b` | feat(server/08.1-04): T-02 — POST /refresh 에 inline classify 훅 + 통합 테스트 3건 |
| T-03 | `b45e654` | chore(infra/08.1-04): T-03 — setup-discussion-sync-iam.sh 에 anthropic secret + 2 accessor |
| T-04 | `04c09f9` | feat(infra/08.1-04): T-04 — deploy 스크립트 2종에 ANTHROPIC_API_KEY + task-timeout=1800 |

Base commit: `d742643` (Plan 08.1-03 SUMMARY 직후).
Worktree: parallel executor — `git commit --no-verify` 적용 (prompt 지시).

## Manual Follow-up (post-plan, pre-Phase-08.1-05)

사용자가 1회 실행:

```bash
# 1) Anthropic Console 에서 API key 발급 (https://console.anthropic.com/settings/keys)
export ANTHROPIC_API_KEY=sk-ant-api03-...
export GOOGLE_APPLICATION_CREDENTIALS=/Users/alex/.config/gcloud/gh-radar-deployer.json
export CLOUDSDK_CORE_PROJECT=gh-radar
export GCP_PROJECT_ID=gh-radar

# 2) Secret + accessor 생성 (idempotent)
bash scripts/setup-discussion-sync-iam.sh

# 3) 배포 (양쪽 모두)
bash scripts/deploy-discussion-sync.sh
bash scripts/deploy-server.sh

# 4) production cycle 실행 + 로그 확인
gcloud run jobs execute gh-radar-discussion-sync \
  --region=asia-northeast3 --project=gh-radar --wait
gcloud logging read 'resource.type=cloud_run_job AND resource.labels.job_name=gh-radar-discussion-sync' \
  --project=gh-radar --limit=40 --format='value(textPayload)' | grep -E "classifyBatch|totalClassified"

# 5) Supabase 분포 확인 (Supabase dashboard SQL editor)
# SELECT relevance, count(*) FROM discussions WHERE classified_at IS NOT NULL GROUP BY 1 ORDER BY 2 DESC;
```

## Self-Check: PASSED

**Files created:**
- `server/src/services/discussion-classify.ts` → FOUND
- `server/tests/routes/discussions.refresh.classify.test.ts` → FOUND
- `.planning/phases/08.1-discussion-relevance-filter/08.1-04-server-refresh-gcp-secret-SUMMARY.md` → FOUND (이 문서)

**Files modified:**
- `server/package.json` (commit aff0fac) → FOUND
- `server/src/config.ts` (commit aff0fac) → FOUND
- `server/src/logger.ts` (commit aff0fac) → FOUND
- `server/src/routes/discussions.ts` (commit 4cbd20b) → FOUND
- `scripts/setup-discussion-sync-iam.sh` (commit b45e654) → FOUND
- `scripts/deploy-discussion-sync.sh` (commit 04c09f9) → FOUND
- `scripts/deploy-server.sh` (commit 04c09f9) → FOUND
- `pnpm-lock.yaml` (commit aff0fac) → FOUND

**Commits:**
- `aff0fac` (T-01) → FOUND in `git log`
- `4cbd20b` (T-02) → FOUND in `git log`
- `b45e654` (T-03) → FOUND in `git log`
- `04c09f9` (T-04) → FOUND in `git log`

**Test verification:**
- `pnpm --filter server typecheck` → exit 0 CONFIRMED
- `pnpm --filter server build` → exit 0 CONFIRMED
- `pnpm --filter server test --run` → 17 파일 / 106 pass CONFIRMED (기존 103 + 신규 3, 회귀 0)

**Script syntax:**
- `bash -n scripts/deploy-discussion-sync.sh` → exit 0 CONFIRMED
- `bash -n scripts/deploy-server.sh` → exit 0 CONFIRMED
- `bash -n scripts/setup-discussion-sync-iam.sh` → exit 0 CONFIRMED

**Outstanding (intentional, SUMMARY-documented):**
- T-03 실물 GCP Secret 생성 + version 주입 — manual step (ANTHROPIC_API_KEY 사용자 제공 필요)
- T-05 production 재배포 + cycle execute — T-03 partial 해소 후 실행
