---
phase: "08.1"
plan: 3
plan_id: "08.1-03"
subsystem: workers/discussion-sync/classify
tags:
  - workers
  - discussion-sync
  - classify
  - anthropic
  - claude-haiku-4-5
  - p-limit
  - vitest
  - phase-08.1
  - wave-2

# Dependency graph
dependency_graph:
  requires:
    - "08.1-01"  # discussions.relevance TEXT + classified_at TIMESTAMPTZ 컬럼 + CHECK 제약
  provides:
    - "workers/discussion-sync/src/classify/ 모듈 5파일 (anthropic/prompt/classifyOne/classifyBatch/persistRelevance)"
    - "DiscussionSyncConfig 에 anthropicApiKey / classifyConcurrency / classifyModel 3 필드 확장"
    - "upsertDiscussions 반환 shape: { upserted, unclassifiedRows: {id,title,body}[] }"
    - "runDiscussionSyncCycle 내 classify hook — upsert 직후 미분류 행만 Claude Haiku 로 inline 분류"
    - "cycle summary 로그에 totalClassified 필드"
  affects:
    - "08.1-04 server-refresh-gcp-secret (GCP Secret Manager 에 ANTHROPIC_API_KEY 주입 필요)"
    - "08.1-05 backfill-script (동일 classifyBatch + persistRelevance 모듈 재사용)"
    - "08.1-07 e2e-smoke (운영 cycle 1회 실행 시 relevance/classified_at 값 실측)"

# Tech tracking
tech_stack:
  added:
    - "@anthropic-ai/sdk ^0.65.0"
  patterns:
    - "Anthropic SDK 싱글톤 lazy 초기화 + __resetAnthropicClientForTests 헬퍼 — 테스트 환경에서 env 설정 후 호출 순서 자유"
    - "Claude Haiku 4.5 sync messages.create — max_tokens=10 / temperature=0 / system + few-shot 3개 + user (approved plan §Decisions §1, §2, §7)"
    - "p-limit(cfg.classifyConcurrency=5) + Promise.allSettled — 한 row 실패가 배치 전체 실패를 유발하지 않음"
    - "실패 row(unknown label / API error) 는 Map 에 미포함 → classified_at 미업데이트 → 다음 cycle 재시도 (자동 retry, 별도 state 테이블 불필요)"
    - "vi.hoisted — vi.mock factory 에서 테스트 상태 공유 (vi.mock 은 hoisted 이므로 최상단 const 접근 불가)"

key_files:
  created:
    - "workers/discussion-sync/src/classify/anthropic.ts (22 lines, lazy SDK 싱글톤 + test reset)"
    - "workers/discussion-sync/src/classify/prompt.ts (44 lines, CLASSIFY_SYSTEM_PROMPT + 3 few-shot + formatUserMessage 500자 truncate)"
    - "workers/discussion-sync/src/classify/classifyOne.ts (41 lines, messages.create + 4-label 매치 + null fallback)"
    - "workers/discussion-sync/src/classify/classifyBatch.ts (31 lines, p-limit + allSettled → Map<id, Label>)"
    - "workers/discussion-sync/src/classify/persistRelevance.ts (27 lines, update({relevance, classified_at: now()}) 단건 loop)"
    - "workers/discussion-sync/src/classify/__tests__/classifyOne.test.ts (4 tests — 정상/trim+lowercase/unknown/throw)"
    - "workers/discussion-sync/src/classify/__tests__/classifyBatch.test.ts (3 tests — 3 rows Map/실패 1건/p-limit 동시성 검증)"
    - "workers/discussion-sync/tests/index.test.ts (1 통합 test — cycle 전체 mock + update 스파이)"
  modified:
    - "workers/discussion-sync/package.json (@anthropic-ai/sdk ^0.65.0 추가)"
    - "workers/discussion-sync/src/config.ts (anthropicApiKey/classifyConcurrency/classifyModel 3 필드 + req() / env override)"
    - "workers/discussion-sync/src/logger.ts (cfg.anthropicApiKey / headers.x-api-key / *.ANTHROPIC_API_KEY / *.anthropicApiKey redact paths)"
    - "workers/discussion-sync/src/pipeline/upsert.ts (UpsertResult interface + select 확장 + unclassifiedRows 추출)"
    - "workers/discussion-sync/src/index.ts (classify import + classifyBatch/persistRelevance hook + totalClassified + 로그)"
    - "workers/discussion-sync/tests/pipeline/upsert.test.ts (신규 select shape 반영 + unclassifiedRows 2 신규 케이스)"

decisions:
  - id: D1
    summary: "classify 모듈 파일 위치는 plan 제안과 동일(workers/discussion-sync/src/classify/) — 기존 pipeline/proxy/scraper 와 대칭. test 위치도 plan 제안(src/classify/__tests__/) 그대로 — 기존 tests/ 폴더와 분리하여 모듈 locality 확보 (08.1-02 와 달리 plan 제안 경로가 합리적)"
  - id: D2
    summary: "Anthropic client 은 lazy 싱글톤으로 구현 — import side-effect 제거하여 vitest 의 module scope mock 과 충돌 회피. __resetAnthropicClientForTests 는 테스트 전용 export 로 명명 규약에 의해 프로덕션 호출 차단"
  - id: D3
    summary: "classifyOne 의 try/catch 는 모든 예외를 null 로 swallow — 단일 row 실패가 배치 전체를 중단시키지 않도록. 단점: silent failure 이지만 classifyBatch log 의 total/classified 차이로 탐지 가능, 추가로 실패 row 는 classified_at NULL 유지 → 다음 cycle 에서 자동 retry (영구 실패 없음)"
  - id: D4
    summary: "persistRelevance 는 단건 UPDATE 루프 — bulk CASE WHEN 을 Supabase JS SDK 로 구현하면 raw SQL 우회 필요(쿼리 문자열 concat, RLS 우회 위험). 배치 크기가 cycle 당 최대 수십~수백 row 이므로 네트워크 RTT 누적 영향은 허용 범위 (1 row ≈ 50ms × 100 = 5s)"
  - id: D5
    summary: "Anthropic API key 는 loadConfig 에서 req() 로 required — 값 없으면 throw. 이는 Phase 08.1 이후 discussion-sync 배포에 ANTHROPIC_API_KEY 가 필수임을 코드 수준에서 강제(08.1-04 GCP Secret Manager 주입 의무화). classify 비활성화 모드는 별도 지원하지 않음(Phase 08.1 전체 설계는 classify 가 always-on 전제)"
  - id: D6
    summary: "tests/index.test.ts 에서 vi.hoisted 패턴 채택 — vi.mock 은 파일 top 으로 자동 hoist 되어 factory 내부의 외부 상수 접근 불가. vi.hoisted 로 mock 공유 상태(mockCreate/updateCalls/infoCalls)를 hoist 하면 factory 에서 접근 가능 + 테스트 본문에서도 동일 참조 사용"
  - id: D7
    summary: "upsert.test.ts 의 기존 mock row 에서 url 필드 제거 — DiscussionRow 스키마는 Phase 08-02 에서 이미 url 컬럼 제거됨(commit 4717052 fix(08-02)). 기존 테스트는 TS strict 가 아닌 재현형 리터럴이라 우연히 통과했던 것. 이번 select 확장 수정 기회에 동시 정리"

metrics:
  duration_minutes: ~9
  completed_at: "2026-04-22T05:44:48Z"
  commits:
    - hash: c9e3d8a
      task: T-01
      message: "T-01 — @anthropic-ai/sdk dep + config(anthropicApiKey/classifyConcurrency/classifyModel) + logger redact"
      files_changed: 4
    - hash: 70e811a
      task: T-02
      message: "T-02 — classify 모듈 5파일 (anthropic/prompt/classifyOne/classifyBatch/persistRelevance)"
      files_changed: 5
    - hash: eefcdde
      task: T-03
      message: "T-03 — classify 단위 테스트 7개 (classifyOne 4 + classifyBatch 3)"
      files_changed: 2
    - hash: 3c01a31
      task: T-04
      message: "T-04 — upsert.ts 확장 + cycle classify 훅 + 통합 테스트"
      files_changed: 4
  tests:
    baseline: "11 파일 / 65 tests pass + 3 todo (T-04 시작 전)"
    final: "14 파일 / 74 tests pass + 3 todo (9 신규)"
    regression: "0 — 기존 65 tests 전원 통과 (upsert.test.ts 가 shape 변경으로 재작성되었으나 동일 5 assertion → 오히려 2 신규 unclassifiedRows 케이스 추가)"
    new_tests: "9 (classifyOne 4 + classifyBatch 3 + upsert unclassifiedRows 2 → 사실상 9, index.test.ts 통합 1 포함 10이지만 upsert 의 2개는 기존 구조에서 추가된 것)"
  typecheck: pass
  build: pass

requirements:
  - DISC-01
  - DISC-01.1
requirements_addressed:
  - DISC-01.1  # 핵심 엔진: inline Claude Haiku 분류 → relevance/classified_at 저장
---

# Phase 08.1 Plan 03: Classify Module Summary

`workers/discussion-sync` 에 Claude Haiku 4.5 inline 분류 엔진을 도입 — `src/classify/` 5 파일(anthropic/prompt/classifyOne/classifyBatch/persistRelevance) + cycle 훅(upsert 직후 미분류 행만 Claude 호출 → `discussions.relevance/classified_at` UPDATE) + 테스트 8개(단위 7 + 통합 1). approved plan 의 §Decisions §1(claude-haiku-4-5), §2(max_tokens=10/temperature=0), §7(4-category prompt), §8(p-limit(5)) literal 이식.

## One-liner

`classifyBatch(rows, log)` — p-limit(5) 동시성으로 Claude Haiku 4.5 sync API 를 호출해 4-카테고리(price_reason|theme|news_info|noise) 중 하나로 분류, 실패한 row 는 `classified_at` NULL 유지로 다음 cycle 자동 retry — DISC-01.1 의 "수집 직후 인라인 분류 엔진" 완성.

## What Was Built

### T-01: dependency + config + logger redact (commit `c9e3d8a`)

**`workers/discussion-sync/package.json`**: `@anthropic-ai/sdk ^0.65.0` 추가 (p-limit 은 기존 ^7.0.0 유지 — approved plan 의 ^6 권장보다 최신, 호환성 OK).

**`src/config.ts`**:

- `DiscussionSyncConfig` 에 3 필드: `anthropicApiKey` / `classifyConcurrency` / `classifyModel`.
- `loadConfig()`:
  - `anthropicApiKey: req("ANTHROPIC_API_KEY")` — required. 미설정 시 Phase 08 cycle 자체가 throw.
  - `classifyConcurrency: Number(process.env.DISCUSSION_SYNC_CLASSIFY_CONCURRENCY ?? "5")` — default 5.
  - `classifyModel: process.env.DISCUSSION_SYNC_CLASSIFY_MODEL ?? "claude-haiku-4-5"` — §Decisions §1 고정.

**`src/logger.ts`**: redact paths 4개 추가 — `cfg.anthropicApiKey`, `headers.x-api-key`, `*.ANTHROPIC_API_KEY`, `*.anthropicApiKey`. 기존 Bright Data / Supabase redact 유지.

`pnpm install` 로 SDK local node_modules 반영 완료 (workspace 루트 lockfile 업데이트).

### T-02: classify 모듈 5파일 (commit `70e811a`)

**`src/classify/anthropic.ts`** (22 lines):

```ts
let _client: Anthropic | null = null;
export function getAnthropicClient(): Anthropic { /* lazy + required key check */ }
export function __resetAnthropicClientForTests(): void { _client = null; }
```

**`src/classify/prompt.ts`** (44 lines):

- `CLASSIFY_SYSTEM_PROMPT`: 4 카테고리 정의(price_reason/theme/news_info/noise) + "출력은 라벨 단어 하나만".
- `FEW_SHOT`: 3개 — 실적 서프라이즈(news_info) / 2차전지 로테이션(theme) / "ㅋㅋㅋ 또 떨어지네"(noise).
- `formatUserMessage(title, body)`: `제목: ${title}\n본문: ${body?.slice(0,500) ?? ""}` — 500자 truncate.
- `buildFewShotMessages()`: user/assistant 쌍 6개 배열.

**`src/classify/classifyOne.ts`** (41 lines):

```ts
export type Label = "price_reason" | "theme" | "news_info" | "noise";
export async function classifyOne(row: {id, title, body}): Promise<Label | null> {
  // messages.create({ model: cfg.classifyModel, max_tokens: 10, temperature: 0,
  //                    system: CLASSIFY_SYSTEM_PROMPT,
  //                    messages: [...buildFewShotMessages(), user] })
  // → text.trim().toLowerCase() 매치 실패 OR try/catch throw → null 반환
}
```

**`src/classify/classifyBatch.ts`** (31 lines):

```ts
const limit = pLimit(cfg.classifyConcurrency);
const results = new Map<string, Label>();
await Promise.allSettled(rows.map((r) => limit(async () => {
  const label = await classifyOne(r);
  if (label) results.set(r.id, label);
})));
log.info({ total: rows.length, classified: results.size }, "classifyBatch done");
```

**`src/classify/persistRelevance.ts`** (27 lines):

```ts
for (const [id, label] of labels) {
  const { error } = await supabase.from("discussions")
    .update({ relevance: label, classified_at: now }).eq("id", id);
  if (!error) updated++;
}
return updated; // 성공 UPDATE 수
```

### T-03: classify 단위 테스트 7개 (commit `eefcdde`)

**`src/classify/__tests__/classifyOne.test.ts`** (4 tests):

| # | Case | Verification |
|---|------|--------------|
| 1 | 정상 `"price_reason"` | `'price_reason'` 반환 + max_tokens=10 / temperature=0 / model=claude-haiku-4-5 호출 인자 |
| 2 | `"  NOISE  "` | trim+lowercase → `'noise'` |
| 3 | unknown `"happy"` | null (화이트리스트 매치 실패) |
| 4 | API throw | null (try/catch → 다음 cycle 재시도) |

**`src/classify/__tests__/classifyBatch.test.ts`** (3 tests):

| # | Case | Verification |
|---|------|--------------|
| 1 | 3 rows (price_reason/theme/noise) | Map size 3, 각 id 매핑 정확 |
| 2 | 3 rows 중 1개 unknown | Map size 2 (실패 row 미포함) |
| 3 | 10 rows + mockCreate 에 10ms 딜레이 | inflight counter max ≤ 5 (p-limit 동작 실증) |

Anthropic SDK 는 `vi.mock('@anthropic-ai/sdk', () => ({ default: vi.fn().mockImplementation(...) }))` 로 module scope 교체. 각 테스트 `beforeEach` 에서 `__resetAnthropicClientForTests()` 로 싱글톤 리셋.

### T-04: upsert + cycle 훅 + 통합 테스트 (commit `3c01a31`)

**`src/pipeline/upsert.ts`**:

```ts
export interface UpsertResult {
  upserted: number;
  unclassifiedRows: Array<{ id: string; title: string; body: string | null }>;
}

export async function upsertDiscussions(supabase, rows): Promise<UpsertResult> {
  // .select("id") → .select("id,title,body,relevance,classified_at")
  const unclassifiedRows = all.filter((r) => r.classified_at == null)
    .map((r) => ({ id: r.id, title: r.title, body: r.body }));
  return { upserted: all.length, unclassifiedRows };
}
```

**`src/index.ts`** — cycle 내부:

```ts
const { upserted, unclassifiedRows } = await upsertDiscussions(supabase, rows);
totalUpserted += upserted;
let classifiedInBatch = 0;
if (unclassifiedRows.length > 0) {
  const labels = await classifyBatch(unclassifiedRows, log);
  classifiedInBatch = await persistRelevance(supabase, labels);
  totalClassified += classifiedInBatch;
}
log.info({ code, mode, requests, upserted, unclassified: unclassifiedRows.length,
           classified: classifiedInBatch }, "per-stock done");
```

Cycle summary 로그에 `totalClassified` 필드 추가.

**`tests/index.test.ts`** 통합 1 테스트 — `vi.hoisted` 패턴:

- Anthropic SDK (price_reason 고정) / Supabase update 스파이 / logger 캡처 / collectDiscussions / loadTargets / apiUsage / retention / proxy client 전부 mock.
- 1회 cycle 실행 → Anthropic 1회 호출 / `update({relevance: "price_reason", classified_at: <ISO>}).eq("id", "row-1")` / summary.totalClassified=1 / per-stock.classified=1 전수 검증.

## Deviations from Plan

### 자동 수정 없음 (plan literal 이식)

approved plan §Decisions §1, §2, §7, §8 를 전부 그대로 구현. max_tokens=10, temperature=0, model=claude-haiku-4-5, p-limit(5), 4-category 화이트리스트, few-shot 3개, 500자 truncate 모두 literal.

### 부가 정리 (Rule 1/2)

**1. `tests/pipeline/upsert.test.ts` 의 DiscussionRow mock 에서 `url` 필드 제거** (Rule 1 — 기존 테스트 스키마 불일치)

- Phase 08-02 에서 commit `4717052 fix(08-02): worker DiscussionRow 에서 url 컬럼 제거 — DB 스키마 불일치 해소` 로 `DiscussionRow` 에서 url 필드가 제거됐지만, upsert.test.ts 의 `mkRow` 는 여전히 url 을 포함하고 있었음. 기존 TypeScript strict 가 객체 리터럴만 검사하고 함수 반환형 체크를 우회하는 케이스라 우연히 빌드만 통과하던 상태.
- 이번 select shape 확장으로 동 테스트를 재작성하는 과정에서 정리 — url 필드 제거 + 새 shape 반영.
- Impact: positive — 스키마 일치, TS strict 하 향후 리팩터링 안전.

**2. `__resetAnthropicClientForTests` 헬퍼 추가** (Rule 2 — missing critical for test isolation)

- plan 에는 명시되지 않았으나 싱글톤 lazy 초기화와 `vi.mock` 의 조합에서 테스트 간 격리가 중요. `_client = null` 리셋 없이는 첫 테스트가 set 한 SDK 인스턴스가 두 번째 테스트까지 유지되어 mock 교체가 반영 안 됨. 테스트 전용 export 로 명명해 프로덕션 호출 방지.
- Impact: neutral-positive — 프로덕션 코드에 테스트 용 함수 존재하지만 명명 규약으로 안전.

### 보완 설명

**`upsert.test.ts` 의 기존 `mkRow` url 필드 이슈**: 이는 repository 선행 PR 의 타입 불일치가 잠복해 있던 것이고, 이번 plan 이 직접 원인은 아님. select shape 변경으로 test data 도 업데이트가 필요했고 그 기회에 정리. approved plan 의 `files_modified` 에 `upsert.test.ts` 가 명시되지 않았으나 타입 정합성 확보를 위해 불가피.

### Auto-fixed Issues

없음 — plan 실행 중 발견된 신규 bug / missing-critical / blocker 없음.

### Authentication Gates

없음 — `ANTHROPIC_API_KEY` 는 config 레벨 required 이지만 테스트는 env var 을 set 하고 `vi.mock` 로 SDK 자체를 교체하므로 실제 API 호출 없음. 프로덕션 배포(08.1-04)에서 GCP Secret Manager 로 주입될 예정.

## Verification Results

```bash
pnpm --filter @gh-radar/discussion-sync typecheck   # PASS (exit 0)
pnpm --filter @gh-radar/discussion-sync build       # PASS (exit 0)
pnpm --filter @gh-radar/discussion-sync test        # 14 파일 / 74 tests pass + 3 todo
```

### 테스트 breakdown

| 영역 | 파일 | tests |
|---|---|---|
| classify/__tests__/classifyOne | 4 | ✅ |
| classify/__tests__/classifyBatch | 3 | ✅ |
| tests/index.test.ts (신규 통합) | 1 | ✅ |
| tests/pipeline/upsert.test.ts (재작성) | 5 | ✅ |
| 기존 나머지 (apiUsage/logger/retention/pipeline.test/collectDiscussions/map/targets/parseDiscussionsJson/fetchDiscussions/proxy.client) | 61 | ✅ |
| 합계 | 74 | ✅ (+3 todo 유지) |

### Acceptance criteria

- T-01: `grep -q '"@anthropic-ai/sdk"' package.json` ✅ / `anthropicApiKey: string` config ✅ / `classifyConcurrency` ✅ / `classifyModel.*claude-haiku-4-5` ✅ / `cfg.anthropicApiKey` logger ✅ / typecheck ✅
- T-02: 5 파일 전부 생성 ✅ / `max_tokens: 10` ✅ / `temperature: 0` ✅ / `pLimit(cfg.classifyConcurrency)` ✅ / `price_reason` + `noise` prompt 존재 ✅ / typecheck ✅
- T-03: 2 테스트 파일 ✅ / classify 테스트 7 passed ✅
- T-04: `unclassifiedRows` upsert ✅ / `select("id,title,body,relevance,classified_at")` ✅ / index.ts 에 `classifyBatch` + `persistRelevance` + `totalClassified` ✅ / 전체 test pass ✅ / build pass ✅

## Known Stubs

없음. 이 plan 의 모든 변경은 실 DB(08.1-01 컬럼) + 실 Claude API(08.1-04 에서 Secret 주입 후 프로덕션 배포) 위에서 동작 설계.

ANTHROPIC_API_KEY 가 없는 로컬 환경에서는 `runDiscussionSyncCycle()` 진입 시 config req() 에서 throw — 이는 의도된 fail-fast 동작(§Decisions §5). 테스트에서는 env var 을 set + SDK mock 교체로 우회.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: external_api_call | workers/discussion-sync/src/classify/classifyOne.ts | Claude API (`api.anthropic.com`) 로 discussion title/body(최대 500자) 전송. PII 유출 리스크는 낮음 (공개 토론방 글) 이나 Anthropic data retention 정책 준수 필요. mitigation: 현재 basic tier(30일 보존). Phase 08.1-04 배포 시 GCP Secret Manager 경로 정립 후 재검토 |
| threat_flag: secret_required | workers/discussion-sync/src/config.ts | ANTHROPIC_API_KEY 는 required config → 배포 환경에서 누락 시 Cloud Run Job fail-fast. 08.1-04 에서 Secret Manager mount 필수. mitigation: 계획됨(08.1-04 plan) |

## Dependency Graph Updates

- **Requires 충족 확인:** 08.1-01 SUMMARY 의 `provides` 에 `discussions.relevance/classified_at 컬럼 + CHECK 제약` 명시, 본 plan 의 persistRelevance 가 직접 의존. commits c55f1d1/b4848c7/8490c03/T-03 (DB push) 검증 완료.
- **Next dependent plans:**
  - 08.1-04 (server-refresh-gcp-secret): 이 classify 모듈을 production Cloud Run Job 환경에서 실행하기 위해 Secret Manager 에 ANTHROPIC_API_KEY 주입 + deploy 스크립트 env 확장 + 서버 discussion refresh 경로에 inline 분류 hook 추가.
  - 08.1-05 (backfill-script): 기존 15,463 row 일회성 분류 — classifyBatch + persistRelevance 그대로 재사용.
  - 08.1-07 (e2e-smoke): cycle 1회 실행 후 `SELECT relevance, count(*) FROM discussions GROUP BY 1` 실측 → 분포 sanity check.

## Commits

| Task | Commit | Subject |
|---|---|---|
| T-01 | `c9e3d8a` | `feat(discussion-sync/08.1-03): T-01 — @anthropic-ai/sdk dep + config(anthropicApiKey/classifyConcurrency/classifyModel) + logger redact` |
| T-02 | `70e811a` | `feat(discussion-sync/08.1-03): T-02 — classify 모듈 5파일 (anthropic/prompt/classifyOne/classifyBatch/persistRelevance)` |
| T-03 | `eefcdde` | `test(discussion-sync/08.1-03): T-03 — classify 단위 테스트 7개 (classifyOne 4 + classifyBatch 3)` |
| T-04 | `3c01a31` | `feat(discussion-sync/08.1-03): T-04 — upsert.ts 확장 + cycle classify 훅 + 통합 테스트` |

Base commit: `04cfe4a` (Plan 08.1-02 SUMMARY 직후).  
Worktree: parallel executor — `git commit --no-verify` 적용 (prompt 지시).

## Self-Check: PASSED

**Files created:**

- `workers/discussion-sync/src/classify/anthropic.ts` → FOUND
- `workers/discussion-sync/src/classify/prompt.ts` → FOUND
- `workers/discussion-sync/src/classify/classifyOne.ts` → FOUND
- `workers/discussion-sync/src/classify/classifyBatch.ts` → FOUND
- `workers/discussion-sync/src/classify/persistRelevance.ts` → FOUND
- `workers/discussion-sync/src/classify/__tests__/classifyOne.test.ts` → FOUND
- `workers/discussion-sync/src/classify/__tests__/classifyBatch.test.ts` → FOUND
- `workers/discussion-sync/tests/index.test.ts` → FOUND

**Files modified:**

- `workers/discussion-sync/package.json` (commit c9e3d8a)
- `workers/discussion-sync/src/config.ts` (commit c9e3d8a)
- `workers/discussion-sync/src/logger.ts` (commit c9e3d8a)
- `workers/discussion-sync/src/pipeline/upsert.ts` (commit 3c01a31)
- `workers/discussion-sync/src/index.ts` (commit 3c01a31)
- `workers/discussion-sync/tests/pipeline/upsert.test.ts` (commit 3c01a31)

**Commits:**

- `c9e3d8a` (T-01) → FOUND in `git log`
- `70e811a` (T-02) → FOUND in `git log`
- `eefcdde` (T-03) → FOUND in `git log`
- `3c01a31` (T-04) → FOUND in `git log`

**Test verification:**

- `pnpm --filter @gh-radar/discussion-sync typecheck` → exit 0 CONFIRMED
- `pnpm --filter @gh-radar/discussion-sync build` → exit 0 CONFIRMED
- `pnpm --filter @gh-radar/discussion-sync test` → 14 파일 / 74 pass + 3 todo CONFIRMED (baseline 11 파일 / 65 pass, 회귀 0)
