---
status: partial
phase: 08.1-discussion-relevance-filter
source: [08.1-VERIFICATION.md]
started: 2026-04-22T07:15:00Z
updated: 2026-04-22T07:15:00Z
---

## Current Test

[awaiting manual production enablement]

## Tests

### 1. ANTHROPIC_API_KEY 발급 + GCP Secret 주입
expected: `gcloud secrets describe gh-radar-anthropic-api-key --project=gh-radar` exit 0 + 2 SA (`gh-radar-discussion-sync-sa`, `gh-radar-server-sa`) accessor grant 존재
result: [pending]
command:
```
export ANTHROPIC_API_KEY=sk-ant-...
export GCP_PROJECT_ID=gh-radar
bash scripts/setup-discussion-sync-iam.sh
```

### 2. Cloud Run Service + Job 재배포
expected: `gh-radar-discussion-sync` Job + `gh-radar-server` Service 배포 revision 에 ANTHROPIC_API_KEY secret mount + task-timeout=1800 적용
result: [pending]
command:
```
bash scripts/deploy-discussion-sync.sh
bash scripts/deploy-server.sh
```

### 3. 15,463 rows production backfill (~$23, ~50분)
expected: `SELECT count(*) FROM discussions WHERE classified_at IS NULL` → 200 미만 (rate-limit 재시도 후), 4-카테고리 분포 확보
result: [pending]
command:
```
pnpm --filter @gh-radar/discussion-sync build
pnpm --filter @gh-radar/discussion-sync backfill:dev
```

### 4. Production Vercel UI smoke 6/6
expected: 08.1-07 PLAN T-03 의 D 섹션 6 항목 모두 PASS (토글 기본 ON, OFF→`?filter=all`, SSR, 무한 스크롤 filter 유지)
result: [pending]
url: https://gh-radar-webapp.vercel.app/stocks/005930/discussions

### 5. Playwright 로컬 실행
expected: 4 시나리오 PASS
result: [pending]
command:
```
pnpm --filter webapp exec playwright install chromium
pnpm --filter webapp exec playwright test discussion-filter.spec.ts
```

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps

_제기된 gap 없음. 전체 코드/config 레벨 must_haves 11/11 verified. 5 항목은 auto-mode 범위 밖의 manual production enablement._
