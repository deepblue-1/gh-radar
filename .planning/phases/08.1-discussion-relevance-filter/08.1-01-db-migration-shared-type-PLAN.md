---
phase: "08.1"
plan_id: "08.1-01"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260422120000_discussion_relevance.sql
  - packages/shared/src/discussion.ts
  - packages/shared/src/__tests__/discussion.test.ts
autonomous: true
parallel_safe: true
requirements:
  - DISC-01
  - DISC-01.1
requirements_addressed:
  - DISC-01
  - DISC-01.1
must_haves:
  truths:
    - "discussions.relevance TEXT 컬럼과 classified_at TIMESTAMPTZ 컬럼이 Supabase production DB 에 존재한다"
    - "relevance 값은 price_reason / theme / news_info / noise 4개 또는 NULL 만 허용된다 (CHECK 제약)"
    - "미분류 행 조회용 partial index 와 meaningful 조회용 partial index 가 존재한다"
    - "packages/shared 의 Discussion 타입에 relevance / classifiedAt 필드가 추가되어 worker/server/webapp 3개 워크스페이스에서 타입 전파된다"
  artifacts:
    - path: "supabase/migrations/20260422120000_discussion_relevance.sql"
      provides: "relevance + classified_at 2컬럼 + 2 partial index + 체크 제약"
      contains: "ADD COLUMN relevance"
    - path: "packages/shared/src/discussion.ts"
      provides: "Discussion 타입 확장 (relevance union, classifiedAt string|null)"
      exports: ["Discussion"]
  key_links:
    - from: "supabase/migrations/20260422120000_discussion_relevance.sql"
      to: "discussions 테이블"
      via: "ALTER TABLE + CREATE INDEX"
      pattern: "ALTER TABLE discussions"
    - from: "packages/shared/src/discussion.ts"
      to: "server/src/mappers/discussions.ts + workers/discussion-sync/src/classify + webapp"
      via: "import { Discussion } from '@gh-radar/shared'"
      pattern: "relevance.*price_reason"
---

<objective>
discussions 테이블에 `relevance` + `classified_at` 두 컬럼을 추가하고 4-카테고리 체크 제약과 2개 partial index 를 도입한다. 동시에 `packages/shared/src/discussion.ts` 의 Discussion 타입을 확장해 모든 하위 워크스페이스(server/webapp/workers)에 의미성 분류 필드를 전파한다.
</objective>

<background>
Phase 08.1 의 모든 후속 plan 은 이 컬럼/타입 확장 위에서 동작한다. 08.1-02(server filter)·08.1-03(classify 모듈)·08.1-05(backfill)·08.1-06(webapp Switch) 모두 이 plan 의 결과에 의존하므로 Wave 1 에 단독 배치되어 병렬 실행(08.1-02 와 함께)을 가능하게 한다. RLS(`anon_read_discussions`) 는 미변경 — anon 이 새 컬럼을 자동 SELECT 가능 (민감 데이터 아님).

approved plan `/Users/alex/.claude/plans/08-1-breezy-pine.md` §Approach Wave 1 Plan 08.1-01 를 literal 하게 이식.
</background>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-discussion-board/08-CONTEXT.md
@packages/shared/src/discussion.ts
@supabase/migrations/20260413120000_init_tables.sql
@supabase/migrations/20260413120100_rls_policies.sql
</context>

<tasks>

<task id="T-01" type="auto">
  <name>Task 1: migration 파일 생성 — ALTER TABLE + CHECK + 2 partial index</name>
  <files>supabase/migrations/20260422120000_discussion_relevance.sql</files>
  <read_first>
    - /Users/alex/repos/gh-radar/supabase/migrations/20260417120200_news_description.sql
    - /Users/alex/repos/gh-radar/supabase/migrations/20260413120000_init_tables.sql
    - /Users/alex/repos/gh-radar/supabase/migrations/20260413120100_rls_policies.sql
  </read_first>
  <action>
신규 파일 `supabase/migrations/20260422120000_discussion_relevance.sql` 을 생성한다. 파일 내용은 approved plan 블록을 **그대로** 사용 (네이밍/오타 수정 금지):

```sql
ALTER TABLE discussions
  ADD COLUMN relevance TEXT,
  ADD COLUMN classified_at TIMESTAMPTZ;
ALTER TABLE discussions
  ADD CONSTRAINT discussions_relevance_check
  CHECK (relevance IS NULL OR relevance IN ('price_reason','theme','news_info','noise'));
CREATE INDEX idx_discussions_unclassified
  ON discussions (stock_code, posted_at DESC)
  WHERE classified_at IS NULL;
CREATE INDEX idx_discussions_meaningful
  ON discussions (stock_code, posted_at DESC)
  WHERE relevance IS NULL OR relevance <> 'noise';
```

- 상단에 주석 블록으로 Phase 08.1 / DISC-01.1 / approved plan 인용 표기.
- RLS 변경 금지 (기존 `anon_read_discussions` policy 가 새 컬럼을 자동 SELECT).
- 마이그레이션 적용은 **이 task 수행 범위가 아님** — T-03 에서 `supabase db push` 실행.
  </action>
  <acceptance_criteria>
    - `test -f supabase/migrations/20260422120000_discussion_relevance.sql` 성공
    - `grep -q "ADD COLUMN relevance TEXT" supabase/migrations/20260422120000_discussion_relevance.sql`
    - `grep -q "ADD COLUMN classified_at TIMESTAMPTZ" supabase/migrations/20260422120000_discussion_relevance.sql`
    - `grep -q "discussions_relevance_check" supabase/migrations/20260422120000_discussion_relevance.sql`
    - `grep -q "idx_discussions_unclassified" supabase/migrations/20260422120000_discussion_relevance.sql`
    - `grep -q "idx_discussions_meaningful" supabase/migrations/20260422120000_discussion_relevance.sql`
    - `grep -q "price_reason" supabase/migrations/20260422120000_discussion_relevance.sql && grep -q "noise" supabase/migrations/20260422120000_discussion_relevance.sql`
  </acceptance_criteria>
  <done>
    파일 존재 + 체크 제약·인덱스 정의 완료. 아직 DB 적용 안 됨.
  </done>
</task>

<task id="T-02" type="auto">
  <name>Task 2: packages/shared Discussion 타입 확장 + 유닛 테스트</name>
  <files>
    packages/shared/src/discussion.ts
    packages/shared/src/__tests__/discussion.test.ts
  </files>
  <read_first>
    - /Users/alex/repos/gh-radar/packages/shared/src/discussion.ts
    - /Users/alex/repos/gh-radar/packages/shared/src/index.ts
  </read_first>
  <action>
1. `packages/shared/src/discussion.ts` 의 `Discussion` 타입에 두 필드를 추가한다. 기존 필드(id, stockCode, postId, title, body, author, postedAt, scrapedAt, url) 는 순서·nullability 모두 유지. 추가 필드 (주석 포함):

```ts
  /**
   * Phase 08.1 — Claude Haiku 4.5 로 분류된 의미성 라벨.
   *  - 'price_reason'  : 가격 움직임 이유·차트·수급 언급
   *  - 'theme'         : 테마·업종·정책 언급
   *  - 'news_info'     : 뉴스 인용·공시·실적 사실
   *  - 'noise'         : 욕설·감탄사·뇌피셜·광고·단순 반응
   *  - null            : 아직 분류 전 (수집 직후 ~ 분류 완료 윈도) 또는 Claude 호출 실패
   */
  relevance: 'price_reason' | 'theme' | 'news_info' | 'noise' | null;
  /** Phase 08.1 — 분류가 실제로 완료된 시각. null = 미분류. */
  classifiedAt: string | null;
```

파일 상단 주석 블록도 `Phase 08.1 — relevance / classifiedAt 확장` 추가.

2. `packages/shared/src/__tests__/discussion.test.ts` 를 생성해 타입 구조를 런타임에서 검증한다. 예:

```ts
import { describe, it, expect } from 'vitest';
import type { Discussion } from '../discussion.js';

describe('Discussion type (Phase 08.1)', () => {
  it('relevance/classifiedAt 필드를 허용해야 한다', () => {
    const d: Discussion = {
      id: 'x', stockCode: '005930', postId: '1', title: 't',
      body: null, author: null, postedAt: '2026-04-21T00:00:00+09:00',
      scrapedAt: '2026-04-21T00:01:00+09:00',
      url: 'https://stock.naver.com/domestic/stock/005930/discussion/1?chip=all',
      relevance: 'price_reason',
      classifiedAt: '2026-04-21T00:02:00+09:00',
    };
    expect(d.relevance).toBe('price_reason');
    expect(d.classifiedAt).not.toBeNull();
  });
  it('relevance=null / classifiedAt=null 도 유효하다', () => {
    const d: Discussion = {
      id: 'y', stockCode: '005930', postId: '2', title: 't2',
      body: null, author: null, postedAt: '2026-04-21T00:00:00+09:00',
      scrapedAt: '2026-04-21T00:01:00+09:00',
      url: 'https://stock.naver.com/domestic/stock/005930/discussion/2?chip=all',
      relevance: null,
      classifiedAt: null,
    };
    expect(d.relevance).toBeNull();
  });
});
```

3. shared workspace 이미 vitest 를 사용하면 재사용, 없으면 간단히 추가 (`vitest.config.ts` 없으면 pnpm --filter @gh-radar/shared test 가 default 로 작동). 기존 테스트 infra 가 있으면 해당 경로 규약 따르기.
  </action>
  <acceptance_criteria>
    - `grep -q "relevance:" packages/shared/src/discussion.ts`
    - `grep -q "classifiedAt:" packages/shared/src/discussion.ts`
    - `grep -q "'price_reason' | 'theme' | 'news_info' | 'noise'" packages/shared/src/discussion.ts`
    - `test -f packages/shared/src/__tests__/discussion.test.ts`
    - `pnpm --filter @gh-radar/shared build` exit 0 (타입 전파 확인)
    - `pnpm --filter @gh-radar/shared test` exit 0 (2 테스트 PASS, 이미 테스트 infra 존재 가정)
  </acceptance_criteria>
  <done>
    Discussion 타입 확장 + 유닛 테스트 PASS + shared build green.
  </done>
</task>

<task id="T-03" type="checkpoint:human-action">
  <name>Task 3: Supabase production 에 마이그레이션 적용 (supabase db push)</name>
  <files>-</files>
  <read_first>
    - /Users/alex/repos/gh-radar/supabase/migrations/20260422120000_discussion_relevance.sql
  </read_first>
  <action>
`supabase db push` 는 프로덕션 DB 에 DDL 을 적용하는 [BLOCKING] 수동 단계다. 실행 전 현재 DB 상태 확인 + 링크된 프로젝트 확인 필요.

실행:
```bash
pnpm supabase db push
```

성공 기준: exit 0 + output 에 `20260422120000_discussion_relevance.sql` 적용 로그.

실행 후 검증 SQL (Supabase SQL editor 또는 psql):
```sql
-- 컬럼 2개 존재 확인
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_name = 'discussions'
  AND column_name IN ('relevance','classified_at');
-- 행 2건 (relevance TEXT, classified_at timestamptz) 반환해야 함

-- 체크 제약 시험 (INSERT 거부돼야 함)
INSERT INTO discussions (stock_code, post_id, title, relevance, posted_at, scraped_at)
VALUES ('TEST00','t1','테스트','bad_label', now(), now());
-- → new row for relation "discussions" violates check constraint "discussions_relevance_check"
```
테스트 INSERT 성공해버리면 체크 제약이 미적용된 것이므로 즉시 중단 + migration 재검토.
  </action>
  <acceptance_criteria>
    - `pnpm supabase db push` exit 0
    - `information_schema.columns` 쿼리에서 `relevance` (TEXT) + `classified_at` (timestamp with time zone) 반환
    - 잘못된 relevance 값 INSERT 시 CHECK 제약 위반 에러 발생 (사용자가 직접 SQL editor 에서 확인)
  </acceptance_criteria>
  <done>
    Supabase production 에 컬럼 + 제약 + 인덱스 반영. 사용자가 `✅ migration applied` 확인.
  </done>
</task>

</tasks>

<verification>
```bash
# 1) 파일 존재 + 내용
test -f supabase/migrations/20260422120000_discussion_relevance.sql
grep -q "idx_discussions_meaningful" supabase/migrations/20260422120000_discussion_relevance.sql

# 2) 타입 + 테스트
pnpm --filter @gh-radar/shared build
pnpm --filter @gh-radar/shared test

# 3) DB 적용 (T-03)
pnpm supabase db push
# Supabase SQL editor 에서 information_schema.columns 쿼리로 컬럼 2개 확인
```
</verification>

<success_criteria>
- Migration SQL 파일 존재 + 구성 요소 6개 (2 ADD COLUMN, 1 CHECK, 2 INDEX, 0 RLS 변경)
- packages/shared build + test PASS, Discussion 타입에 relevance / classifiedAt 노출
- Supabase production DB 에 컬럼 + 제약 + 인덱스 반영 (T-03 완료 시)
- DISC-01.1 의 "의미성 분류 결과 저장소" 기반 성립
</success_criteria>

<commit_hint>
commit message: `feat(discussions): Phase 08.1 Plan 01 — relevance/classified_at 컬럼 + shared 타입 확장 (DISC-01.1 기반)`
</commit_hint>

<output>
After completion, summarize T-01/T-02/T-03 결과(파일 경로, DB 컬럼 쿼리 결과, test pass 수)를 Plan 07 의 SUMMARY 작성 근거로 `.planning/phases/08.1-discussion-relevance-filter/08.1-01-SUMMARY.md` 에 기록.
</output>
