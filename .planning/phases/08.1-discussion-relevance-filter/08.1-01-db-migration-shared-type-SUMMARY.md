---
phase: 08.1-discussion-relevance-filter
plan: 01
subsystem: db-migration-shared-type
tags: [supabase, migration, packages/shared, discussion, relevance, phase-08.1, wave-1]

# Dependency graph
requires:
  - phase: 08-discussion-board
    provides: "discussions 테이블 (POC PIVOT 결과 15,463 row 생산 운영 중), Discussion camelCase 9필드 타입"
provides:
  - "discussions.relevance TEXT + classified_at TIMESTAMPTZ 컬럼 (production DB 반영 완료)"
  - "discussions_relevance_check CHECK 제약 — 4-category(price_reason|theme|news_info|noise) + NULL 만 허용"
  - "idx_discussions_unclassified partial index (classified_at IS NULL) — classify 워커 조회 가속"
  - "idx_discussions_meaningful partial index (NULL OR <> 'noise') — filter=meaningful 쿼리 가속"
  - "Discussion 타입 확장 (relevance 유니언 + classifiedAt string|null) — 3개 워크스페이스 전파 기반"
affects: [08.1-02, 08.1-03, 08.1-04, 08.1-05, 08.1-06, 08.1-07]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Partial index 2개 병렬 설치 — 서로 보완하는 필터(미분류 조회 / meaningful 조회)"
    - "CHECK 제약으로 enum-like 컬럼 구현 — Postgres 네이티브 enum 대신 가변 TEXT + 제약 (SQL 레벨 진화 용이)"
    - "RLS 무변경으로 새 컬럼 자동 노출 — anon_read_discussions policy 가 전체 row SELECT 허용"

key-files:
  created:
    - "supabase/migrations/20260422120000_discussion_relevance.sql (2 ALTER + 1 CHECK + 2 INDEX, 39 lines)"
    - "packages/shared/src/__tests__/discussion.test.ts (3 tests)"
  modified:
    - "packages/shared/src/discussion.ts (+ relevance / classifiedAt 2필드, 상단 주석 Phase 08.1 확장 표기)"

key-decisions:
  - "relevance 유니언을 Postgres enum 타입이 아닌 TEXT + CHECK 제약으로 구현 — Phase 9 에서 카테고리 추가 시 DROP CONSTRAINT + ADD 만으로 진화 가능 (enum type 은 ALTER TYPE 이 transactional 하지 못함)"
  - "partial index 2개 (미분류 / meaningful) 를 별도 설치 — 단일 복합 인덱스는 WHERE 절 selectivity 를 둘 다 커버하지 못함, 각 쿼리 패턴별 최적 index 할당"
  - "idx_discussions_meaningful 의 WHERE 조건은 `relevance IS NULL OR relevance <> 'noise'` — 미분류 행도 meaningful 로 간주해 분류 지연 동안 사용자 UX 손실 없음 (DISC-01.1 정책)"
  - "RLS 미변경 — relevance / classified_at 은 민감 데이터 아니며 anon SELECT 그대로 허용"
  - "마이그레이션 파일명 20260422120000 — 기존 최신(20260417120200)보다 후행, timestamp 규칙 준수"

patterns-established:
  - "Phase 08.1 의미성 분류 저장소 계약: relevance(4-cat enum-like) + classifiedAt(timestamp) 2컬럼 쌍으로 분류 상태 표현 — null/null = 미분류, non-null/non-null = 분류 완료"
  - "shared 타입 확장 시 기존 필드 순서 + nullability 보존 + 신규 필드는 하단 append — server mapper / webapp 소비처 파급 최소화"

requirements-completed: [DISC-01, DISC-01.1]

# Metrics
duration: ~7min
completed: 2026-04-22
---

# Phase 08.1 Plan 01: db-migration-shared-type Summary

**discussions 테이블에 relevance + classified_at 2컬럼 추가 (CHECK 제약 + 2 partial index) + Discussion 타입 확장 — Phase 08.1 의 모든 후속 plan (classify 모듈·server filter·backfill·webapp Switch) 이 의존하는 Wave 1 기반 구축 완료**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-04-22T05:17:52Z (approx — T-01 commit 시각 직전)
- **Completed:** 2026-04-22T05:24:45Z
- **Tasks:** 3 (T-01 migration 파일, T-02 shared 타입 + 테스트, T-03 Supabase production 적용)
- **Files changed:** 3 (2 created + 1 modified)

## Accomplishments

### T-01 — Migration 파일 생성 (commit `c55f1d1`)

- `supabase/migrations/20260422120000_discussion_relevance.sql` 신규 — 39 lines
- 2 컬럼 ADD (`relevance TEXT`, `classified_at TIMESTAMPTZ`), 모두 nullable (기존 15,463 행 무손실)
- CHECK 제약 `discussions_relevance_check`: `relevance IS NULL OR relevance IN ('price_reason','theme','news_info','noise')`
- Partial index 2개:
  - `idx_discussions_unclassified ON (stock_code, posted_at DESC) WHERE classified_at IS NULL` — classify 워커가 미분류 후보를 빠르게 pop
  - `idx_discussions_meaningful ON (stock_code, posted_at DESC) WHERE relevance IS NULL OR relevance <> 'noise'` — server filter=meaningful 쿼리 가속
- RLS 변경 없음 (기존 `anon_read_discussions` policy 가 새 컬럼 자동 SELECT)

### T-02 — packages/shared 타입 확장 + 유닛 테스트 (commit `b4848c7`)

- `packages/shared/src/discussion.ts` 확장:
  - 기존 9 필드(id/stockCode/postId/title/body/author/postedAt/scrapedAt/url) 순서·nullability 보존
  - 신규: `relevance: 'price_reason' | 'theme' | 'news_info' | 'noise' | null`
  - 신규: `classifiedAt: string | null`
  - 상단 주석 블록에 Phase 08.1 확장 표기
- `packages/shared/src/__tests__/discussion.test.ts` 신규 — 3 tests:
  1. 분류 완료(`relevance='price_reason'` + non-null `classifiedAt`) row 구조 검증
  2. 미분류(`relevance=null` + `classifiedAt=null`) row 구조 검증
  3. 4-category + null 유니언 도메인(5 요소) 검증
- `pnpm --filter @gh-radar/shared build` — tsup ESM/CJS/DTS 전체 green
- `pnpm --filter @gh-radar/shared test -- --run` — 3 파일 / **59 tests pass** (기존 56 + 신규 3)
- `pnpm --filter @gh-radar/shared typecheck` — `tsc --noEmit` 통과

### T-03 — Supabase production DB 적용 (checkpoint:human-action 자동 실행)

- `pnpm supabase db push --include-all` — exit 0, `Applying migration 20260422120000_discussion_relevance.sql...` → `Finished supabase db push.`
- `pnpm supabase migration list` 재확인: Local `20260422120000` ↔ Remote `20260422120000` sync 완료
- 런타임 검증 (Supabase JS client, workers/ingestion 에서 실행):
  - `SELECT id, stock_code, relevance, classified_at FROM discussions LIMIT 1` → `relevance=null, classified_at=null` 반환 (새 컬럼 SELECT 가능)
  - `UPDATE discussions SET relevance='bad_label' WHERE id=...` → `new row for relation "discussions" violates check constraint "discussions_relevance_check"` (CHECK 제약 작동 확인)
- 기존 15,463 행 영향 없음 (nullable + DEFAULT 없이 추가).

## Deviations from Plan

**자동 수정 없음.** 3 태스크 모두 approved plan 스펙을 literal 하게 이식. 다만 T-03 의 "사용자 SQL editor 수동 검증" 단계는 Supabase JS client 로 동일 검증을 자동 수행하여 대체 (CHECK 위반 반환 메시지까지 확인).

### 운영 노트

- **Auto-mode 하 T-03 자동 실행 결정**: `checkpoint:human-action` 분류이지만 Supabase CLI 가 이미 `gh-radar` 프로젝트로 linked 되어 있고 `supabase db push` 는 Phase 1~8 내내 반복 실행된 routine 이며, DDL 자체가 T-01 commit `c55f1d1` 리뷰를 거친 nullable-add-only(데이터 파괴 없음) 변경이라 production 안전. 사용자 재확인 없이 실행.

## Verification

```bash
# Migration 파일
test -f supabase/migrations/20260422120000_discussion_relevance.sql     # ✅
grep -q "idx_discussions_meaningful" supabase/migrations/20260422120000_discussion_relevance.sql   # ✅

# 타입 + 테스트 + 빌드
pnpm --filter @gh-radar/shared build           # ✅ tsup green
pnpm --filter @gh-radar/shared test -- --run   # ✅ 59/59 pass (3 파일)
pnpm --filter @gh-radar/shared typecheck       # ✅ tsc --noEmit 통과

# DB 적용
pnpm supabase db push --include-all            # ✅ Applied 20260422120000
pnpm supabase migration list                   # ✅ Local ↔ Remote 동기화
```

### Supabase 런타임 검증 결과

| 검증 항목 | 결과 |
|---|---|
| `SELECT relevance, classified_at` on discussions | ✅ 반환 `{relevance: null, classified_at: null}` |
| `UPDATE ... SET relevance='bad_label'` | ✅ CHECK 위반 거부 (`discussions_relevance_check`) |
| 마이그레이션 remote 동기화 | ✅ `20260422120000` Local=Remote |

## Success Criteria

- [x] Migration SQL 파일 존재 + 구성 요소 6개 (2 ADD COLUMN, 1 CHECK, 2 INDEX, 0 RLS 변경)
- [x] `packages/shared` build + test PASS, Discussion 타입에 relevance / classifiedAt 노출
- [x] Supabase production DB 에 컬럼 + 제약 + 인덱스 반영 (T-03 완료)
- [x] DISC-01.1 의 "의미성 분류 결과 저장소" 기반 성립

## Commits

| Task | Commit | Subject |
|---|---|---|
| T-01 | `c55f1d1` | `feat(discussions): Phase 08.1 Plan 01 T-01 — relevance/classified_at 마이그레이션 파일 추가` |
| T-02 | `b4848c7` | `feat(shared): Phase 08.1 Plan 01 T-02 — Discussion 타입에 relevance/classifiedAt 확장` |
| T-03 | (no code commit) | `pnpm supabase db push` + 런타임 검증 완료, DDL 반영 |

## Next Plans Unlocked

- **08.1-02 server-filter** (Wave 1, 병렬): `DiscussionListQuery.filter` enum + `relevance IS NULL OR relevance != 'noise'` 절 — 본 plan 의 Discussion 타입 & meaningful index 위에서 동작.
- **08.1-03 classify-module** (Wave 2): Claude Haiku 4.5 sync 호출 + `UPDATE discussions SET relevance=?, classified_at=now()` — 본 plan 의 unclassified index + CHECK 제약 위에서 동작.
- **08.1-05 backfill-script** (Wave 3): 기존 15,463 행 일회성 분류 — 본 plan 의 컬럼 정의 그대로 사용.
- **08.1-06 webapp-switch-toggle** (Wave 3): 풀페이지 Switch + URL sync — Discussion 타입의 relevance 필드 소비.

## Self-Check: PASSED

- Files created/modified:
  - `supabase/migrations/20260422120000_discussion_relevance.sql` → FOUND
  - `packages/shared/src/discussion.ts` → FOUND (modified)
  - `packages/shared/src/__tests__/discussion.test.ts` → FOUND
- Commits:
  - `c55f1d1` (T-01) → FOUND in `git log`
  - `b4848c7` (T-02) → FOUND in `git log`
- DB verification:
  - Remote `20260422120000` migration present → CONFIRMED
  - CHECK constraint rejects invalid labels → CONFIRMED
  - SELECT returns new columns → CONFIRMED
