---
phase: "08.1"
plan_id: "08.1-05"
plan: 5
type: execute
wave: 3
depends_on:
  - "08.1-01"
  - "08.1-03"
files_modified:
  - workers/discussion-sync/src/backfill.ts
  - workers/discussion-sync/package.json
  - workers/discussion-sync/src/__tests__/backfill.test.ts
autonomous: false
parallel_safe: true
requirements:
  - DISC-01
  - DISC-01.1
requirements_addressed:
  - DISC-01.1
must_haves:
  truths:
    - "workers/discussion-sync/src/backfill.ts 를 실행하면 classified_at IS NULL 인 기존 행(15k+)을 chunk 단위로 classifyBatch + persistRelevance 한다"
    - "MAX_BACKFILL_ROWS env 로 1회 실행 한도를 제한할 수 있다 (기본 20000)"
    - "500행 단위로 progress 로그 출력 + Ctrl-C graceful shutdown"
    - "backfill 완료 후 classified_at IS NULL 인 행이 0 또는 API 실패 소량만 남는다"
  artifacts:
    - path: "workers/discussion-sync/src/backfill.ts"
      provides: "일회성 백필 스크립트 엔트리 포인트 (node dist/backfill.js / tsx src/backfill.ts)"
    - path: "workers/discussion-sync/package.json"
      provides: "backfill / backfill:dev npm scripts"
  key_links:
    - from: "workers/discussion-sync/src/backfill.ts"
      to: "classify/classifyBatch + classify/persistRelevance (08.1-03)"
      via: "동일 모듈 재사용 — 로직 복제 금지"
      pattern: "classifyBatch|persistRelevance"
---

<objective>
15,463+ 기존 discussions 행(classified_at IS NULL) 을 일회성 백필 스크립트로 4-카테고리 분류한다. 08.1-03 의 classify 모듈을 재사용해 로직 중복 없이 chunk(10,000행) × 반복 루프로 처리한다. 안전장치(MAX_BACKFILL_ROWS, progress log, Ctrl-C handler) 를 포함.
</objective>

<background>
Wave 3. 08.1-03 의 classify 모듈이 완성된 후에만 의미 있음. 08.1-06 (webapp) 과 병렬 실행 가능 (다른 파일군). 예상 비용 ~$23 일회성 — 사용자 승인 필요하므로 `autonomous: false`. 실행 후 production cycle(08.1-04 이후) 이 신규 수집분만 처리하게 되어 비용이 안정화된다.

approved plan `/Users/alex/.claude/plans/08-1-breezy-pine.md` §Approach Wave 3 Plan 08.1-05 literal 이식.
</background>

<context>
@.planning/phases/08-discussion-board/08-CONTEXT.md
@workers/discussion-sync/src/classify/classifyBatch.ts
@workers/discussion-sync/src/classify/persistRelevance.ts
@workers/discussion-sync/src/config.ts
@workers/discussion-sync/src/logger.ts
@workers/discussion-sync/src/services/supabase.ts
@workers/discussion-sync/package.json
</context>

<tasks>

<task id="T-01" type="auto">
  <name>Task 1: workers/discussion-sync/src/backfill.ts 엔트리 + Ctrl-C handler</name>
  <files>workers/discussion-sync/src/backfill.ts</files>
  <read_first>
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/index.ts
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/classify/classifyBatch.ts
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/classify/persistRelevance.ts
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/services/supabase.ts
  </read_first>
  <action>
신규 파일 `workers/discussion-sync/src/backfill.ts` 작성.

```ts
/**
 * Phase 08.1 Plan 05 — 일회성 backfill 스크립트.
 *
 * 목적: 기존 15k+ discussions (classified_at IS NULL) 을 chunk 단위로 분류.
 *
 * 사용:
 *   pnpm --filter @gh-radar/discussion-sync backfill:dev       # 로컬 tsx
 *   pnpm --filter @gh-radar/discussion-sync build
 *   pnpm --filter @gh-radar/discussion-sync backfill           # node dist/backfill.js
 *
 * 환경변수:
 *   ANTHROPIC_API_KEY, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY (필수)
 *   MAX_BACKFILL_ROWS (기본 20000)
 *   BACKFILL_CHUNK_SIZE (기본 100) — chunk per classifyBatch
 *   BACKFILL_SELECT_PAGE (기본 10000) — 단일 SELECT 한도
 *
 * 종료 조건:
 *   - unclassified 잔여 0 → complete
 *   - 누적 처리 >= MAX_BACKFILL_ROWS → complete (재실행으로 이어가기)
 *   - SIGINT/SIGTERM → graceful shutdown (현재 chunk 완료 후 종료)
 */
import "dotenv/config";
import { loadConfig } from "./config.js";
import { createLogger } from "./logger.js";
import { createSupabaseClient } from "./services/supabase.js";
import { classifyBatch } from "./classify/classifyBatch.js";
import { persistRelevance } from "./classify/persistRelevance.js";

const MAX_BACKFILL_ROWS = Number(process.env.MAX_BACKFILL_ROWS ?? "20000");
const CHUNK_SIZE = Number(process.env.BACKFILL_CHUNK_SIZE ?? "100");
const SELECT_PAGE = Number(process.env.BACKFILL_SELECT_PAGE ?? "10000");
const PROGRESS_EVERY = 500;

async function main() {
  const cfg = loadConfig();
  const log = createLogger(cfg.logLevel).child({ app: "discussion-sync-backfill", version: cfg.appVersion });
  const supabase = createSupabaseClient(cfg.supabaseUrl, cfg.supabaseServiceRoleKey);

  let shuttingDown = false;
  const onSignal = () => {
    if (shuttingDown) return;
    shuttingDown = true;
    log.warn({}, "shutdown signal — finishing current chunk then exit");
  };
  process.on("SIGINT", onSignal);
  process.on("SIGTERM", onSignal);

  let processed = 0;
  let classified = 0;
  let failed = 0;
  const startedAt = Date.now();

  while (!shuttingDown && processed < MAX_BACKFILL_ROWS) {
    const { data, error } = await supabase
      .from("discussions")
      .select("id,title,body")
      .is("classified_at", null)
      .order("posted_at", { ascending: false })
      .limit(SELECT_PAGE);
    if (error) {
      log.error({ err: error.message }, "backfill select failed");
      process.exit(1);
    }
    const page = (data ?? []) as Array<{ id: string; title: string; body: string | null }>;
    if (page.length === 0) {
      log.info({ processed, classified, failed }, "no more unclassified rows — complete");
      break;
    }

    // chunk 분할 처리
    for (let i = 0; i < page.length; i += CHUNK_SIZE) {
      if (shuttingDown) break;
      if (processed >= MAX_BACKFILL_ROWS) break;
      const chunk = page.slice(i, i + CHUNK_SIZE);
      const labels = await classifyBatch(chunk, log);
      const updated = await persistRelevance(supabase, labels);
      classified += updated;
      failed += chunk.length - updated;
      processed += chunk.length;
      if (processed % PROGRESS_EVERY < CHUNK_SIZE) {
        const ratePerMin = processed / ((Date.now() - startedAt) / 60000);
        log.info(
          { processed, classified, failed, ratePerMin: Math.round(ratePerMin) },
          "backfill progress",
        );
      }
    }
  }

  log.info(
    { processed, classified, failed, elapsedSec: Math.round((Date.now() - startedAt) / 1000) },
    "backfill complete",
  );
  process.exit(0);
}

if (process.argv[1] && (process.argv[1].endsWith("backfill.js") || process.argv[1].endsWith("backfill.ts"))) {
  main().catch((err) => {
    // eslint-disable-next-line no-console
    console.error("[backfill] fatal", err);
    process.exit(1);
  });
}
```

핵심 포인트:
- 08.1-03 의 classifyBatch / persistRelevance 를 **재사용** — 로직 중복 금지.
- chunk 루프 안에서 shuttingDown 체크 → SIGINT/SIGTERM 시 현재 chunk 완료 후 graceful exit.
- MAX_BACKFILL_ROWS 초과 시 조기 종료 — 재실행으로 이어가기 가능 (`classified_at IS NULL` 이 여전히 있는 행만 pickup).
- rate 계산은 log 메시지에 포함해 ETA 추정 쉬움.
  </action>
  <acceptance_criteria>
    - `test -f workers/discussion-sync/src/backfill.ts`
    - `grep -q "classifyBatch" workers/discussion-sync/src/backfill.ts`
    - `grep -q "persistRelevance" workers/discussion-sync/src/backfill.ts`
    - `grep -q "is(\"classified_at\", null)" workers/discussion-sync/src/backfill.ts`
    - `grep -q "MAX_BACKFILL_ROWS" workers/discussion-sync/src/backfill.ts`
    - `grep -q "SIGINT" workers/discussion-sync/src/backfill.ts`
    - `pnpm --filter @gh-radar/discussion-sync typecheck` exit 0
    - `pnpm --filter @gh-radar/discussion-sync build` exit 0 → `dist/backfill.js` 생성 확인
  </acceptance_criteria>
  <done>
    backfill.ts 엔트리 포인트 존재 + build → dist/backfill.js 생성.
  </done>
</task>

<task id="T-02" type="auto">
  <name>Task 2: package.json scripts 추가 (backfill, backfill:dev)</name>
  <files>workers/discussion-sync/package.json</files>
  <read_first>
    - /Users/alex/repos/gh-radar/workers/discussion-sync/package.json
  </read_first>
  <action>
`workers/discussion-sync/package.json` 의 `scripts` 필드에 2개 추가:
```json
"backfill": "node dist/backfill.js",
"backfill:dev": "tsx -r dotenv/config src/backfill.ts"
```

기존 스크립트(`dev`, `build`, `typecheck`, `test`) 는 그대로 유지. 순서는 build 다음 행에 자연스럽게 배치.
  </action>
  <acceptance_criteria>
    - `grep -q '"backfill": "node dist/backfill.js"' workers/discussion-sync/package.json`
    - `grep -q '"backfill:dev": "tsx -r dotenv/config src/backfill.ts"' workers/discussion-sync/package.json`
    - `pnpm --filter @gh-radar/discussion-sync run backfill --help 2>/dev/null || true` — script 자체는 tx 없이 실행되면 연결 에러 정도 (파일 해석 성공 = 최소 요건).
  </acceptance_criteria>
  <done>
    scripts 에 backfill + backfill:dev 노출.
  </done>
</task>

<task id="T-03" type="auto" tdd="true">
  <name>Task 3: 단위 테스트 — chunk 루프 / MAX_BACKFILL_ROWS / shutdown</name>
  <files>workers/discussion-sync/src/__tests__/backfill.test.ts</files>
  <read_first>
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/backfill.ts
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/classify/classifyBatch.ts
  </read_first>
  <behavior>
    - Test 1: 250 rows 을 CHUNK_SIZE=100 으로 처리 → classifyBatch 가 3회 호출 (100 + 100 + 50)
    - Test 2: MAX_BACKFILL_ROWS=150 설정 시 processed=150 에서 중단 → classifyBatch 호출 2회 + mid-chunk 중단 처리
    - Test 3: SIGINT 시뮬레이션 → 현재 chunk 완료 후 exit, 다음 chunk 호출 안 됨
  </behavior>
  <action>
backfill 로직 중 `main()` 이 process.exit 하므로 테스트하기 어려움 → backfill.ts 내부 로직을 `export async function runBackfill(options)` 로 추출 후 main() 이 그것을 호출하는 구조로 리팩터링하거나, 테스트를 **scripted integration** 으로 (예: child_process spawn) 작성.

간편 접근: `runBackfill({ maxRows, chunkSize, supabase, classifyFn, persistFn, log })` 로 DI 버전을 export. main() 이 production config 로 runBackfill 호출. 테스트는 mock supabase + mock classifyFn(항상 `Map()` 반환) + in-memory counter 로 호출 횟수 검증.

```ts
// backfill.ts 안에 추가:
export async function runBackfill(opts: {
  supabase: SupabaseClient;
  log: pino.Logger;
  maxRows: number;
  chunkSize: number;
  selectPage: number;
  classify: typeof classifyBatch;
  persist: typeof persistRelevance;
  shouldStop?: () => boolean;
}): Promise<{ processed: number; classified: number; failed: number }> {
  // ... 기존 루프 로직을 여기로 이동
}
```

테스트:
```ts
import { runBackfill } from '../backfill.js';
// mock supabase that returns 250 rows on first call, 0 on second
// mock classify that returns labels for all rows
// expect classifyFn calls = 3
```
  </action>
  <acceptance_criteria>
    - `test -f workers/discussion-sync/src/__tests__/backfill.test.ts`
    - `pnpm --filter @gh-radar/discussion-sync test -- backfill` exit 0 (3 PASS)
    - `grep -q "runBackfill" workers/discussion-sync/src/backfill.ts`
  </acceptance_criteria>
  <done>
    3 단위 테스트 PASS + runBackfill export 로 DI 가능.
  </done>
</task>

<task id="T-04" type="checkpoint:human-action">
  <name>Task 4: [BLOCKING] 일회성 backfill 실행 (예상 비용 ~$23, 소요 ~50분)</name>
  <files>-</files>
  <read_first>
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/backfill.ts
    - /Users/alex/repos/gh-radar/workers/discussion-sync/package.json
  </read_first>
  <action>
**⚠️ 비용 사전 승인 필요 (approved plan §Approach Wave 3 08.1-05):**
- 예상 비용: 15,463 rows × 입력 ~1500 토큰 × Haiku 4.5 ($1/M input) ≈ $23
- 예상 소요: 15k × 1s (p-limit 5) ≈ 50분
- Anthropic Tier 1 기본 50 RPM — p-limit(5) × 60초 = 300 RPM 이론 최대. 실제로는 Haiku 응답 속도(평균 500ms)로 100~200 RPM 수준 → rate limit 히트 가능. 필요 시 `DISCUSSION_SYNC_CLASSIFY_CONCURRENCY=3` 로 낮춤.

**사용자 확인 요청:**
"Plan 08.1-05 backfill 을 실행하면 15,463 rows × ~$23 비용 + 50분 소요됩니다. 진행하려면 `go` 라고 응답해주세요. 중간 rate limit 발생 시 Ctrl-C 로 graceful 중단 가능합니다."

**실행 (사용자 승인 후):**
```bash
cd /Users/alex/repos/gh-radar
pnpm --filter @gh-radar/discussion-sync build  # dist/backfill.js 갱신
# env 는 workers/discussion-sync/.env 에 ANTHROPIC_API_KEY + SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY
pnpm --filter @gh-radar/discussion-sync backfill:dev 2>&1 | tee /tmp/backfill-08.1-05.log
```

**중간 모니터링 (별도 터미널):**
```bash
tail -f /tmp/backfill-08.1-05.log
# 별도 SQL 셸:
# SELECT count(*) FROM discussions WHERE classified_at IS NULL;
# → 감소 추이 확인
```

**완료 후 검증:**
```sql
SELECT relevance, count(*) FROM discussions GROUP BY relevance ORDER BY 2 DESC;
-- 기대: price_reason/theme/news_info/noise 4 라벨 분포 + NULL 잔량 0 (또는 API 실패 소량 <100)

SELECT count(*) FROM discussions WHERE classified_at IS NULL;
-- 기대: < 100 (완전 0 이 아니면 재실행으로 정리)
```

**실패/중단 시 재실행:**
스크립트는 idempotent — `classified_at IS NULL` 만 pickup 하므로 처음부터 안전하게 다시 실행 가능.
  </action>
  <acceptance_criteria>
    - 사용자가 `go` 또는 명시적 승인 응답
    - `/tmp/backfill-08.1-05.log` 마지막 라인에 `"backfill complete"` 및 `classified > 10000`
    - 완료 후 SQL `SELECT count(*) FROM discussions WHERE classified_at IS NULL` < 200
    - SQL `SELECT relevance, count(*) GROUP BY` 에 최소 3 라벨(noise 확실 + 다른 라벨 2개 이상) 존재
  </acceptance_criteria>
  <done>
    15k+ 행 분류 완료 + 분포 스냅샷 확보. 사용자가 결과 확인 + 비용 실측 보고.
  </done>
</task>

</tasks>

<verification>
```bash
pnpm --filter @gh-radar/discussion-sync typecheck
pnpm --filter @gh-radar/discussion-sync test
pnpm --filter @gh-radar/discussion-sync build
# T-04 완료 후:
# SELECT relevance, count(*) FROM discussions GROUP BY relevance ORDER BY 2 DESC;
# → noise 비율이 50% 내외이면 정상, 10% 미만이면 프롬프트 튜닝 재검토 (Phase 10+)
```
</verification>

<success_criteria>
- backfill.ts + runBackfill export + package scripts 2개
- 3 단위 테스트 PASS
- production backfill 실행 완료 + 분포 스냅샷 획득
- 비용 실측치 기록 (SUMMARY 반영)
- DISC-01.1 의 "기존 15k 행 의미성 분류" 완수
</success_criteria>

<commit_hint>
commit message: `feat(discussion-sync): Phase 08.1 Plan 05 — backfill 스크립트 + 15k 기존 행 일회성 분류 (DISC-01.1 backfill)`
</commit_hint>

<output>
T-01~T-04 결과 + backfill.log 마지막 50줄 + SQL 분포 스냅샷 + 비용 실측치 → SUMMARY 재료.
</output>
