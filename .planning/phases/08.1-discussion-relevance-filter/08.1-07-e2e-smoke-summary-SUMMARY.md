---
phase: "08.1"
plan_id: "08.1-07"
plan: 7
wave: 4
status: completed-with-deferred-manual-smoke
completed: 2026-04-22
---

# Plan 08.1-07 — E2E + Phase SUMMARY

## Tasks

| Task | Type | Status | Commit |
|------|------|--------|--------|
| T-01 Playwright spec (4 시나리오) | auto | ✅ | `3c132a1` |
| T-02 REQUIREMENTS/ROADMAP 업데이트 | auto | ✅ | `0e1e076` |
| T-03 Production smoke 6/6 + SQL 스냅샷 | checkpoint:human-verify | ⏸ deferred (manual) | — |
| T-04 08.1-SUMMARY.md + STATE.md | auto | ✅ | (this commit) |

## T-01 — Playwright E2E

`webapp/e2e/tests/discussion-filter.spec.ts` — 4 시나리오:
1. 기본 ON(meaningful) → noise 제외
2. 토글 OFF → URL `?filter=all` + noise 포함
3. 직접 `?filter=all` 진입 → 토글 OFF 상태
4. filter=meaningful + 빈 응답 → 전용 empty 카피 렌더

**로컬 실행 상태:** spec 파일 생성 완료. 실제 `pnpm --filter webapp playwright test discussion-filter.spec.ts` 실행은 Wave 4 executor timeout 으로 인해 미수행 — 사용자가 `pnpm --filter webapp playwright install` 후 1회 수동 실행 권장.

## T-02 — REQUIREMENTS/ROADMAP

`0e1e076`:
- `REQUIREMENTS.md` — DISC-01.1 공식 추가 + Traceability 행
- `ROADMAP.md` — Phase 08.1 Goal/Requirements/Plans TBD → 확정 내용

## T-03 — Production smoke (DEFERRED)

Auto-mode 에서 실제 production URL 수동 클릭 + Cloud Run Job 실행 + Supabase SQL 실측은 불가. `08.1-SUMMARY.md` 의 `Manual Follow-up` 섹션에 체크리스트로 기록됨. 사용자가 ANTHROPIC_API_KEY 확보 → deploy → smoke 순서로 1회 실행 필요.

## T-04 — Phase SUMMARY + STATE

- `.planning/phases/08.1-discussion-relevance-filter/08.1-SUMMARY.md` 신규 작성 (plan 구조 따름, 실측 수치는 production smoke 후 보완).
- `.planning/STATE.md` — Phase 08.1 완료 반영, `completed_phases` 8 → 9, `completed_plans` 46 → 53.

## Deviations

1. **T-03 deferred** — Auto mode 제약 (설계 의도: plan 의 `autonomous: false` 가 바로 이 상황을 예고).
2. **Playwright 로컬 실행 미수행** — executor timeout. Spec 파일 자체는 검증 가능 상태.

## Verification

- typecheck/build: 전 workspace PASS (Wave 1–3 검증 시 확인, 본 plan 은 spec + meta 파일만 추가/수정)
- 전체 testsuite: 회귀 없음 (Wave 1–3 plan SUMMARY 누적 기록 참조)

## Manual Follow-up (user actions)

1. **ANTHROPIC_API_KEY 발급** + GCP Secret 주입:
   ```bash
   export ANTHROPIC_API_KEY=sk-ant-...
   export GCP_PROJECT_ID=gh-radar
   bash scripts/setup-discussion-sync-iam.sh
   ```
2. **Production 재배포:**
   ```bash
   bash scripts/deploy-discussion-sync.sh
   bash scripts/deploy-server.sh
   ```
3. **일회성 backfill (선택, 비용 ~$23):**
   ```bash
   pnpm --filter @gh-radar/discussion-sync build
   pnpm --filter @gh-radar/discussion-sync backfill:dev
   ```
4. **Playwright 로컬 실행:**
   ```bash
   pnpm --filter webapp exec playwright install chromium
   pnpm --filter webapp exec playwright test discussion-filter.spec.ts
   ```
5. **Production smoke 6/6** — plan T-03 의 A/B/C/D 절차 수행, 08.1-SUMMARY.md 실측 수치 보완.
