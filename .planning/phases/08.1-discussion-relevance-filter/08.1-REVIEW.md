---
phase: 08.1-discussion-relevance-filter
reviewed: 2026-04-22T06:59:50Z
depth: quick
commits:
  base: 184a65b
  head: 9d220ce
  range: 184a65b..9d220ce
files_reviewed: 19
files_reviewed_list:
  - supabase/migrations/20260422120000_discussion_relevance.sql
  - packages/shared/src/discussion.ts
  - server/src/services/discussion-classify.ts
  - server/src/routes/discussions.ts
  - server/src/schemas/discussions.ts
  - server/src/mappers/discussions.ts
  - server/src/config.ts
  - server/src/logger.ts
  - workers/discussion-sync/src/classify/anthropic.ts
  - workers/discussion-sync/src/classify/prompt.ts
  - workers/discussion-sync/src/classify/classifyOne.ts
  - workers/discussion-sync/src/classify/classifyBatch.ts
  - workers/discussion-sync/src/classify/persistRelevance.ts
  - workers/discussion-sync/src/backfill.ts
  - workers/discussion-sync/src/pipeline/upsert.ts
  - workers/discussion-sync/src/index.ts
  - workers/discussion-sync/src/config.ts
  - workers/discussion-sync/src/logger.ts
  - webapp/src/components/stock/discussion-page-client.tsx
  - webapp/src/components/ui/switch.tsx
  - webapp/src/lib/stock-api.ts
  - scripts/deploy-discussion-sync.sh
  - scripts/deploy-server.sh
  - scripts/setup-discussion-sync-iam.sh
findings:
  critical: 0
  warning: 0
  info: 2
  total: 2
status: clean
---

# Phase 08.1: Code Review Report

**Reviewed:** 2026-04-22T06:59:50Z
**Depth:** quick (pattern-matching only)
**Range:** `184a65b..9d220ce` (commits c55f1d1..9d220ce on top of base 184a65b)
**Files Reviewed:** 24 (변경/생성 소스 + 3 deploy 스크립트)
**Status:** clean (고신뢰도 Critical / Warning 없음 — Info 2건만)

## Summary

Phase 08.1 의 변경 범위 — Supabase migration, shared 타입 확장, server-side inline classify + filter, worker classify 모듈 5파일, 15k 행 backfill 스크립트, webapp Switch 토글, deploy 스크립트 secret mount — 를 quick depth 로 스캔. 리뷰 범위에서 지정한 6개 고신뢰도 영역 (secret 누출, classify 에러처리, CHECK 제약 회귀, filter injection, concurrency 초과, deploy secret 정합성) 전부 통과.

고신뢰도 Critical / Warning 은 없음. 문서 주석 오타 1건 + 비핵심 TEMP DUPLICATION 자가기록 1건만 Info 수준으로 기록.

### 고신뢰도 영역 통과 근거

| 영역 | 파일 | 판정 근거 |
|------|------|----------|
| Secret 누출 | server/src/logger.ts, workers/discussion-sync/src/logger.ts, 소스 전체 | worker 는 `cfg.anthropicApiKey` / `headers.x-api-key` / `*.ANTHROPIC_API_KEY` / `*.anthropicApiKey` 4경로 redact. server 는 `*.ANTHROPIC_API_KEY` / `*.anthropicApiKey` redact. 소스 어디에도 `console.log`/하드코드 key 문자열 없음 (테스트 파일의 `"test-anth"` 는 명백한 fake). |
| Classify 에러 처리 | workers/.../classify/*.ts, server/src/services/discussion-classify.ts | `classifyOne` 의 try/catch 가 모든 예외를 null 로 swallow → Map 미포함 → `classified_at` NULL 유지 → 다음 cycle 자동 retry. 배치는 `Promise.allSettled` 로 한 행 실패가 전체 중단 차단. Server `POST /refresh` 훅도 try/catch + `logger.warn` non-fatal. |
| Migration CHECK 회귀 | supabase/migrations/20260422120000_discussion_relevance.sql | 2 컬럼 모두 nullable + DEFAULT 없이 ADD → 기존 15,463 행 무손실. CHECK 는 `relevance IS NULL OR relevance IN (...)` 로 NULL 명시 허용. SUMMARY 의 production DB 적용 로그 확인됨. |
| Filter 파라미터 인젝션 | server/src/schemas/discussions.ts, server/src/routes/discussions.ts:272-274 | Zod `z.enum(['all', 'meaningful'])` 가 이외 값 전부 400 reject. `filter === 'meaningful'` 엄격 비교 후 Supabase PostgREST `.or('relevance.is.null,relevance.neq.noise')` 인자는 하드코드 문자열 — 사용자 입력 concat 없음. T-08 SQLi 보호 유지. |
| classifyBatch concurrency 초과 | workers/.../classify/classifyBatch.ts, server/src/services/discussion-classify.ts | `pLimit(cfg.classifyConcurrency)` default 5. Anthropic Tier 1 RPM 내 안전 범위 (approved plan §8). env override 경로는 있으나 deploy 스크립트 어디에서도 재정의 안 함. 프로덕션은 default 5 고정. |
| Deploy secret mount 정합성 | scripts/deploy-discussion-sync.sh:63-68/148, scripts/deploy-server.sh:76-91/130, scripts/setup-discussion-sync-iam.sh:83/146-164/191-195 | setup 스크립트: `gh-radar-anthropic-api-key` 생성 idempotent + discussion-sync-SA / server-SA 양쪽 accessor 바인딩. deploy-discussion-sync.sh: pre-check secret 존재 + `--set-secrets` 에 `ANTHROPIC_API_KEY=gh-radar-anthropic-api-key:latest` 명시. deploy-server.sh: 안전망 accessor loop + pre-check + `--update-secrets` 동일. 3파일 전부 `bash -n` 통과 확인(SUMMARY 기록). |

## Info

### IN-01: mapper 파일 주석의 migration 파일명 오타

**File:** `server/src/mappers/discussions.ts:8-9`
**Issue:** DiscussionRow 타입 상단 JSDoc 이 참조하는 migration 파일명이 실제 파일과 불일치.

```ts
 * Schema (`supabase/migrations/20260413120000_init_tables.sql:58-71`
 *       + `supabase/migrations/20260422000000_discussions_relevance.sql`):
```

실제 파일은 `supabase/migrations/20260422120000_discussion_relevance.sql` (timestamp 가 `20260422000000` 이 아닌 `20260422120000`, 파일명은 `discussion_relevance` 단수형).

**Impact:** 런타임 영향 없음. 향후 유지보수 시 grep/navigation 혼란.

**Fix:**
```ts
 * Schema (`supabase/migrations/20260413120000_init_tables.sql:58-71`
 *       + `supabase/migrations/20260422120000_discussion_relevance.sql`):
```

---

### IN-02: Server/Worker classify 로직 TEMP DUPLICATION (self-documented)

**File:** `server/src/services/discussion-classify.ts:8-13` (상단 주석 자가 기록됨) + `workers/discussion-sync/src/classify/{anthropic,prompt,classifyOne,classifyBatch,persistRelevance}.ts`
**Issue:** CLASSIFY_SYSTEM_PROMPT / FEW_SHOT / max_tokens=10 / temperature=0 / model / 4-label whitelist / 500자 truncate / p-limit 패턴이 server 단일 파일과 worker 5 파일에 중복 존재. 이미 소스 주석에 `⚠️ TEMP DUPLICATION` 명시 + phase SUMMARY §TODOs 에 `packages/discussion-classify` 이관 기록됨 → 의도된 기술 부채.

**Impact:** 단일 파일 drift 시 server / worker 분류 결과가 달라질 위험. 현재는 문자열 동일. v1 범위 내에서 수용 가능하나 다음 plan 에서 이관 권장.

**Fix:** Phase 9 이후 `packages/discussion-classify` 패키지 생성 → server / worker 양쪽에서 import. (approved plan §Deferred 에 이미 등록됨 — 추가 조치 불요, 추적 목적 기록.)

---

_Reviewed: 2026-04-22T06:59:50Z_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: quick_
