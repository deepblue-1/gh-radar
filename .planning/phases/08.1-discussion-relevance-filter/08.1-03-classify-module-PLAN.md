---
phase: "08.1"
plan_id: "08.1-03"
plan: 3
type: execute
wave: 2
depends_on:
  - "08.1-01"
files_modified:
  - workers/discussion-sync/package.json
  - workers/discussion-sync/src/config.ts
  - workers/discussion-sync/src/logger.ts
  - workers/discussion-sync/src/classify/anthropic.ts
  - workers/discussion-sync/src/classify/prompt.ts
  - workers/discussion-sync/src/classify/classifyOne.ts
  - workers/discussion-sync/src/classify/classifyBatch.ts
  - workers/discussion-sync/src/classify/persistRelevance.ts
  - workers/discussion-sync/src/classify/__tests__/classifyOne.test.ts
  - workers/discussion-sync/src/classify/__tests__/classifyBatch.test.ts
  - workers/discussion-sync/src/pipeline/upsert.ts
  - workers/discussion-sync/src/index.ts
  - workers/discussion-sync/tests/index.test.ts
autonomous: true
parallel_safe: false
requirements:
  - DISC-01
  - DISC-01.1
requirements_addressed:
  - DISC-01.1
must_haves:
  truths:
    - "discussion-sync cycle 은 upsert 직후 classified_at IS NULL 인 신규 행만 pickup 해서 Claude Haiku 4.5 Sync API 로 분류한다"
    - "분류된 행은 relevance(4 라벨) + classified_at=now() 로 DB 에 반영된다"
    - "Claude API 호출 실패 또는 라벨 매치 실패 시 해당 행은 classified_at 을 업데이트하지 않아 다음 cycle 에서 재시도된다"
    - "Claude API 동시 호출 수는 p-limit(5) 로 제한된다"
  artifacts:
    - path: "workers/discussion-sync/src/classify/anthropic.ts"
      provides: "lazy Anthropic SDK 싱글톤 (ANTHROPIC_API_KEY 없으면 throw)"
    - path: "workers/discussion-sync/src/classify/prompt.ts"
      provides: "고정 system prompt + 4 카테고리 정의 + few-shot 3개"
    - path: "workers/discussion-sync/src/classify/classifyOne.ts"
      provides: "{id,title,body} → 'price_reason'|'theme'|'news_info'|'noise'|null"
    - path: "workers/discussion-sync/src/classify/classifyBatch.ts"
      provides: "rows[] → Map<id,label> with p-limit(5)"
    - path: "workers/discussion-sync/src/classify/persistRelevance.ts"
      provides: "Map<id,label> → Supabase UPDATE"
  key_links:
    - from: "workers/discussion-sync/src/pipeline/upsert.ts"
      to: "select('id,title,body,relevance,classified_at') return"
      via: "ignoreDuplicates:false + select 확장"
      pattern: "select.*id.*title.*body"
    - from: "workers/discussion-sync/src/index.ts runDiscussionSyncCycle"
      to: "classify/classifyBatch + persistRelevance"
      via: "upsert 결과에서 classified_at IS NULL 인 행만 전달"
      pattern: "classifyBatch.*persistRelevance"
---

<objective>
`workers/discussion-sync` 에 inline 분류 모듈 (`src/classify/`) 을 도입하고 기존 cycle `runDiscussionSyncCycle` 내 upsert 직후 분류 훅을 삽입한다. Claude Haiku 4.5 Sync API, `p-limit(5)`, `temperature=0`, `max_tokens=10`, 4-카테고리 라벨 정확 매치 정책을 approved plan 그대로 구현한다.
</objective>

<background>
Wave 2. 08.1-01 의 DB 컬럼이 적용된 후 동작한다. 08.1-04(server refresh) 는 이 plan 의 classify 모듈을 import 하므로 08.1-03 → 08.1-04 순차 실행 권장. 08.1-05(backfill) 도 동일 모듈을 재사용. 따라서 이 plan 은 Phase 08.1 의 실질적 핵심 엔진이다.

approved plan `/Users/alex/.claude/plans/08-1-breezy-pine.md` §Approach Wave 2 Plan 08.1-03 를 literal 이식. Decisions §1, §2, §7, §8 을 그대로 준수.
</background>

<context>
@.planning/phases/08-discussion-board/08-CONTEXT.md
@workers/discussion-sync/src/index.ts
@workers/discussion-sync/src/pipeline/upsert.ts
@workers/discussion-sync/src/pipeline/map.ts
@workers/discussion-sync/src/pipeline/collectDiscussions.ts
@workers/discussion-sync/src/config.ts
@workers/discussion-sync/src/logger.ts
@workers/discussion-sync/package.json
@packages/shared/src/discussion.ts
</context>

<interfaces>
From workers/discussion-sync/src/pipeline/upsert.ts (기존):
```typescript
export async function upsertDiscussions(
  supabase: SupabaseClient,
  rows: DiscussionRow[],
): Promise<{ upserted: number }>;
```
→ 확장:
```typescript
export interface UpsertResult {
  upserted: number;
  unclassifiedRows: Array<{ id: string; title: string; body: string | null }>;
}
export async function upsertDiscussions(
  supabase: SupabaseClient,
  rows: DiscussionRow[],
): Promise<UpsertResult>;
```

From workers/discussion-sync/src/index.ts line 99:
```typescript
const { upserted } = await upsertDiscussions(supabase, rows);
totalUpserted += upserted;
```
→ 확장:
```typescript
const { upserted, unclassifiedRows } = await upsertDiscussions(supabase, rows);
totalUpserted += upserted;
if (unclassifiedRows.length > 0) {
  const labels = await classifyBatch(unclassifiedRows, log);
  const persisted = await persistRelevance(supabase, labels);
  totalClassified += persisted;
}
```

New module surface (workers/discussion-sync/src/classify/):
```typescript
// anthropic.ts
export function getAnthropicClient(): Anthropic;

// prompt.ts
export const CLASSIFY_SYSTEM_PROMPT: string;
export function formatUserMessage(title: string, body: string | null): string;

// classifyOne.ts
export type Label = 'price_reason' | 'theme' | 'news_info' | 'noise';
export async function classifyOne(row: { id: string; title: string; body: string | null }): Promise<Label | null>;

// classifyBatch.ts
export async function classifyBatch(
  rows: Array<{ id: string; title: string; body: string | null }>,
  log: pino.Logger,
): Promise<Map<string, Label>>;

// persistRelevance.ts
export async function persistRelevance(
  supabase: SupabaseClient,
  labels: Map<string, Label>,
): Promise<number>; // 성공 UPDATE 수
```
</interfaces>

<tasks>

<task id="T-01" type="auto">
  <name>Task 1: dependencies + config + logger redact 추가</name>
  <files>
    workers/discussion-sync/package.json
    workers/discussion-sync/src/config.ts
    workers/discussion-sync/src/logger.ts
  </files>
  <read_first>
    - /Users/alex/repos/gh-radar/workers/discussion-sync/package.json
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/config.ts
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/logger.ts
  </read_first>
  <action>
1. `workers/discussion-sync/package.json` dependencies 에 추가:
   ```json
   "@anthropic-ai/sdk": "^0.65.0"
   ```
   NOTE: `p-limit` 은 이미 `^7.0.0` 로 존재 (기존 cycle 용) — approved plan 의 `^6` 권장값보다 최신이므로 그대로 유지.

2. `workers/discussion-sync/src/config.ts` 의 `DiscussionSyncConfig` 인터페이스 + `loadConfig()` 에 `anthropicApiKey: string` + `classifyConcurrency: number` (기본 5) 필드 추가:
   ```ts
   anthropicApiKey: req("ANTHROPIC_API_KEY"),
   classifyConcurrency: Number(process.env.DISCUSSION_SYNC_CLASSIFY_CONCURRENCY ?? "5"),
   classifyModel: process.env.DISCUSSION_SYNC_CLASSIFY_MODEL ?? "claude-haiku-4-5",
   ```
   ※ approved plan §Decisions §1 "claude-haiku-4-5" 고정. env override 는 엔지니어링 안전망(테스트용).

3. `workers/discussion-sync/src/logger.ts` 의 `redact.paths` 배열에 추가:
   ```ts
   "cfg.anthropicApiKey",
   "*.ANTHROPIC_API_KEY",
   "*.anthropicApiKey",
   "headers.x-api-key",
   ```
   기존 Bright Data / Supabase redact 유지.

4. `pnpm --filter @gh-radar/discussion-sync install` 로 SDK 설치.
  </action>
  <acceptance_criteria>
    - `grep -q '"@anthropic-ai/sdk"' workers/discussion-sync/package.json`
    - `grep -q "anthropicApiKey: string" workers/discussion-sync/src/config.ts`
    - `grep -q "classifyConcurrency" workers/discussion-sync/src/config.ts`
    - `grep -q 'classifyModel.*claude-haiku-4-5' workers/discussion-sync/src/config.ts`
    - `grep -q "cfg.anthropicApiKey" workers/discussion-sync/src/logger.ts`
    - `pnpm --filter @gh-radar/discussion-sync typecheck` exit 0
  </acceptance_criteria>
  <done>
    dependency/config/logger 셋업 완료. classify 모듈이 Anthropic SDK, cfg.anthropicApiKey, cfg.classifyConcurrency, cfg.classifyModel 사용 가능.
  </done>
</task>

<task id="T-02" type="auto">
  <name>Task 2: classify 모듈 5파일 — anthropic / prompt / classifyOne / classifyBatch / persistRelevance</name>
  <files>
    workers/discussion-sync/src/classify/anthropic.ts
    workers/discussion-sync/src/classify/prompt.ts
    workers/discussion-sync/src/classify/classifyOne.ts
    workers/discussion-sync/src/classify/classifyBatch.ts
    workers/discussion-sync/src/classify/persistRelevance.ts
  </files>
  <read_first>
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/config.ts
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/pipeline/map.ts
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/services/supabase.ts
  </read_first>
  <action>

**`classify/anthropic.ts`** — lazy 싱글톤:
```ts
import Anthropic from "@anthropic-ai/sdk";
import { loadConfig } from "../config.js";

let _client: Anthropic | null = null;
export function getAnthropicClient(): Anthropic {
  if (_client) return _client;
  const cfg = loadConfig();
  if (!cfg.anthropicApiKey) throw new Error("ANTHROPIC_API_KEY missing");
  _client = new Anthropic({ apiKey: cfg.anthropicApiKey });
  return _client;
}
```

**`classify/prompt.ts`** — approved plan §Approach Wave 2 08.1-03 의 prompt.ts 섹션을 **정확히 복사** (재작성 금지):
```ts
/**
 * Phase 08.1 — Claude Haiku 4.5 분류 프롬프트.
 *
 * approved plan D7:
 *  - 고정 system prompt
 *  - 4 카테고리 정의 + few-shot 3개
 *  - input ≤ 1500 토큰 가정
 *  - max_tokens=10 (라벨만)
 *  - temperature=0
 */
export const CLASSIFY_SYSTEM_PROMPT = `한국 주식 종목토론방 글을 다음 4개 라벨 중 하나로 분류해라. price_reason(가격 움직임 이유·차트·수급), theme(테마·업종·정책 언급), news_info(뉴스 인용·공시·실적 사실), noise(욕설·감탄사·뇌피셜·광고·단순 반응). 출력은 라벨 단어 하나만.`;

const FEW_SHOT: Array<{ title: string; body: string; label: string }> = [
  {
    title: "실적 서프라이즈 — 영업이익 35% 증가",
    body: "금일 공시된 1분기 실적, 컨센 대비 35% 초과 달성. 매출도 전년 대비 12% 증가.",
    label: "news_info",
  },
  {
    title: "2차전지 섹터 로테이션 지속",
    body: "정책 수혜 테마가 돌면서 관련 업종으로 자금 유입. LG엔솔/포스코퓨처엠 동반 강세.",
    label: "theme",
  },
  {
    title: "ㅋㅋㅋ 또 떨어지네",
    body: "",
    label: "noise",
  },
];

/** User 메시지 포맷터 — body 500자 truncate. */
export function formatUserMessage(title: string, body: string | null): string {
  const b = (body ?? "").slice(0, 500);
  return `제목: ${title}\n본문: ${b}`;
}

/** few-shot 을 대화 형식으로 전개 — messages.create 의 messages[] 앞에 prepend. */
export function buildFewShotMessages(): Array<{ role: "user" | "assistant"; content: string }> {
  const out: Array<{ role: "user" | "assistant"; content: string }> = [];
  for (const fs of FEW_SHOT) {
    out.push({ role: "user", content: formatUserMessage(fs.title, fs.body) });
    out.push({ role: "assistant", content: fs.label });
  }
  return out;
}
```

**`classify/classifyOne.ts`**:
```ts
import { getAnthropicClient } from "./anthropic.js";
import { CLASSIFY_SYSTEM_PROMPT, formatUserMessage, buildFewShotMessages } from "./prompt.js";
import { loadConfig } from "../config.js";

export type Label = "price_reason" | "theme" | "news_info" | "noise";
const LABELS: ReadonlySet<Label> = new Set(["price_reason","theme","news_info","noise"]);

export async function classifyOne(row: {
  id: string; title: string; body: string | null;
}): Promise<Label | null> {
  const cfg = loadConfig();
  const client = getAnthropicClient();
  try {
    const res = await client.messages.create({
      model: cfg.classifyModel,
      max_tokens: 10,
      temperature: 0,
      system: CLASSIFY_SYSTEM_PROMPT,
      messages: [
        ...buildFewShotMessages(),
        { role: "user", content: formatUserMessage(row.title, row.body) },
      ],
    });
    const first = res.content.find((c) => c.type === "text");
    const text = first && first.type === "text" ? first.text.trim().toLowerCase() : "";
    if (LABELS.has(text as Label)) return text as Label;
    return null;
  } catch {
    return null; // 실패 시 skip — classified_at 미업데이트 → 다음 cycle 재시도
  }
}
```

**`classify/classifyBatch.ts`**:
```ts
import pLimit from "p-limit";
import type pino from "pino";
import { classifyOne, type Label } from "./classifyOne.js";
import { loadConfig } from "../config.js";

export async function classifyBatch(
  rows: Array<{ id: string; title: string; body: string | null }>,
  log: pino.Logger,
): Promise<Map<string, Label>> {
  const cfg = loadConfig();
  const limit = pLimit(cfg.classifyConcurrency); // approved plan §8 — p-limit(5)
  const results = new Map<string, Label>();
  await Promise.allSettled(
    rows.map((r) =>
      limit(async () => {
        const label = await classifyOne(r);
        if (label) results.set(r.id, label);
      }),
    ),
  );
  log.info({ total: rows.length, classified: results.size }, "classifyBatch done");
  return results;
}
```

**`classify/persistRelevance.ts`**:
```ts
import type { SupabaseClient } from "@supabase/supabase-js";
import type { Label } from "./classifyOne.js";

export async function persistRelevance(
  supabase: SupabaseClient,
  labels: Map<string, Label>,
): Promise<number> {
  if (labels.size === 0) return 0;
  const now = new Date().toISOString();
  let updated = 0;
  // 단건 UPDATE 루프 — bulk CASE WHEN 은 Supabase JS SDK 지원 미약.
  for (const [id, label] of labels) {
    const { error } = await supabase
      .from("discussions")
      .update({ relevance: label, classified_at: now })
      .eq("id", id);
    if (!error) updated++;
  }
  return updated;
}
```
  </action>
  <acceptance_criteria>
    - `test -f workers/discussion-sync/src/classify/anthropic.ts`
    - `test -f workers/discussion-sync/src/classify/prompt.ts`
    - `test -f workers/discussion-sync/src/classify/classifyOne.ts`
    - `test -f workers/discussion-sync/src/classify/classifyBatch.ts`
    - `test -f workers/discussion-sync/src/classify/persistRelevance.ts`
    - `grep -q "max_tokens: 10" workers/discussion-sync/src/classify/classifyOne.ts`
    - `grep -q "temperature: 0" workers/discussion-sync/src/classify/classifyOne.ts`
    - `grep -q "pLimit(cfg.classifyConcurrency)" workers/discussion-sync/src/classify/classifyBatch.ts`
    - `grep -q "price_reason" workers/discussion-sync/src/classify/prompt.ts && grep -q "noise" workers/discussion-sync/src/classify/prompt.ts`
    - `pnpm --filter @gh-radar/discussion-sync typecheck` exit 0
  </acceptance_criteria>
  <done>
    5 파일 존재, 프롬프트 상수 4 카테고리 모두 포함, max_tokens=10 / temperature=0 고정, p-limit 으로 동시성 제어.
  </done>
</task>

<task id="T-03" type="auto" tdd="true">
  <name>Task 3: classify 단위 테스트 2개 — classifyOne / classifyBatch</name>
  <files>
    workers/discussion-sync/src/classify/__tests__/classifyOne.test.ts
    workers/discussion-sync/src/classify/__tests__/classifyBatch.test.ts
  </files>
  <read_first>
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/classify/classifyOne.ts
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/classify/classifyBatch.ts
    - /Users/alex/repos/gh-radar/workers/discussion-sync/tests
    - /Users/alex/repos/gh-radar/workers/discussion-sync/vitest.config.ts
  </read_first>
  <behavior>
    - classifyOne test 1: Anthropic mock 이 `"price_reason"` 텍스트 반환 → `'price_reason'` 리턴
    - classifyOne test 2: Anthropic mock 이 `"  NOISE  "` 반환 → trim/lowercase 후 `'noise'` 리턴
    - classifyOne test 3: Anthropic mock 이 `"unknown_label"` 반환 → `null` 리턴 (라벨 매치 실패)
    - classifyOne test 4: Anthropic mock 이 예외 throw → `null` 리턴 (try/catch)
    - classifyBatch test 1: 3 rows 입력, mock 이 각각 price_reason/theme/noise 반환 → Map size 3
    - classifyBatch test 2: 3 rows 입력, 1 개는 null (실패) → Map size 2
    - classifyBatch test 3: 10 rows 동시 호출 시 p-limit(5) 가 5 concurrency 이하로 제한 (counter spy)
  </behavior>
  <action>
`vi.mock('@anthropic-ai/sdk')` 로 Anthropic SDK 를 mock. `getAnthropicClient` 의 반환값을 `{ messages: { create: vi.fn() } }` 로 대체. `loadConfig` 의 `anthropicApiKey/classifyConcurrency/classifyModel` 은 환경변수 set 또는 vi.mock 으로 override.

classifyOne.test.ts 샘플:
```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';

vi.mock('@anthropic-ai/sdk', () => {
  const mockCreate = vi.fn();
  return {
    default: vi.fn().mockImplementation(() => ({ messages: { create: mockCreate } })),
    __mockCreate: mockCreate,
  };
});

import { classifyOne } from '../classifyOne.js';
import Anthropic from '@anthropic-ai/sdk';

beforeEach(() => {
  process.env.ANTHROPIC_API_KEY = 'test-key';
  process.env.SUPABASE_URL = 'http://localhost';
  process.env.SUPABASE_SERVICE_ROLE_KEY = 'sr';
  process.env.BRIGHTDATA_API_KEY = 'bd';
  (Anthropic as unknown as { __mockCreate: ReturnType<typeof vi.fn> }).__mockCreate.mockReset();
});

describe('classifyOne', () => {
  it('price_reason 매치', async () => {
    const mockCreate = (Anthropic as any).__mockCreate;
    mockCreate.mockResolvedValueOnce({ content: [{ type: 'text', text: 'price_reason' }] });
    const out = await classifyOne({ id: '1', title: 't', body: 'b' });
    expect(out).toBe('price_reason');
  });
  // ... test 2/3/4 동일 패턴
});
```

classifyBatch.test.ts 는 mock Anthropic + 가짜 pino logger(`pino({ level: 'silent' })`) + 10개 row 에 대해 동시성 카운터로 p-limit 검증.

NOTE: 기존 workers/discussion-sync/vitest.config.ts 와 tests/ 폴더 규약을 따르기. 기존 패턴이 `tests/` 폴더를 쓰는지 `src/**/__tests__` 를 쓰는지 확인 후 맞추기.
  </action>
  <acceptance_criteria>
    - `test -f workers/discussion-sync/src/classify/__tests__/classifyOne.test.ts`
    - `test -f workers/discussion-sync/src/classify/__tests__/classifyBatch.test.ts`
    - `pnpm --filter @gh-radar/discussion-sync test -- classify` exit 0
    - 테스트 출력에 `classifyOne.*4 passed` + `classifyBatch.*3 passed` (또는 total ≥ 7)
  </acceptance_criteria>
  <done>
    7 테스트 PASS. Anthropic SDK 가 mocked — 네트워크 호출 없음.
  </done>
</task>

<task id="T-04" type="auto">
  <name>Task 4: upsert.ts 확장 + index.ts cycle 에 classify 훅 삽입 + 통합 테스트</name>
  <files>
    workers/discussion-sync/src/pipeline/upsert.ts
    workers/discussion-sync/src/index.ts
    workers/discussion-sync/tests/index.test.ts
  </files>
  <read_first>
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/pipeline/upsert.ts
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/index.ts
  </read_first>
  <action>
1. **`workers/discussion-sync/src/pipeline/upsert.ts`** — return shape 확장:
   ```ts
   export interface UpsertResult {
     upserted: number;
     unclassifiedRows: Array<{ id: string; title: string; body: string | null }>;
   }

   export async function upsertDiscussions(
     supabase: SupabaseClient,
     rows: DiscussionRow[],
   ): Promise<UpsertResult> {
     if (rows.length === 0) return { upserted: 0, unclassifiedRows: [] };
     const { data, error } = await supabase
       .from("discussions")
       .upsert(rows, { onConflict: "stock_code,post_id", ignoreDuplicates: false })
       .select("id,title,body,relevance,classified_at");
     if (error) throw error;
     const all = (data ?? []) as Array<{ id: string; title: string; body: string | null; relevance: string | null; classified_at: string | null }>;
     const unclassifiedRows = all
       .filter((r) => r.classified_at == null)
       .map((r) => ({ id: r.id, title: r.title, body: r.body }));
     return { upserted: all.length, unclassifiedRows };
   }
   ```
   타이틀이 select 결과에 없으면 이전 `.select("id")` 를 위와 같이 확장 — Supabase `.upsert().select(...)` 가 insert+update 된 row 의 지정 컬럼 반환.

2. **`workers/discussion-sync/src/index.ts`** — cycle 에 훅 삽입:
   - 파일 상단 import 추가:
     ```ts
     import { classifyBatch } from "./classify/classifyBatch.js";
     import { persistRelevance } from "./classify/persistRelevance.js";
     ```
   - `totalUpserted` 옆에 `let totalClassified = 0;` 선언.
   - 기존 `const { upserted } = await upsertDiscussions(...)` 블록을 다음으로 교체:
     ```ts
     const { upserted, unclassifiedRows } = await upsertDiscussions(supabase, rows);
     totalUpserted += upserted;
     if (unclassifiedRows.length > 0) {
       const labels = await classifyBatch(unclassifiedRows, log);
       const persisted = await persistRelevance(supabase, labels);
       totalClassified += persisted;
     }
     log.info({ code: t.code, mode, requests, upserted, classified: unclassifiedRows.length ? unclassifiedRows.length : 0 }, "per-stock done");
     ```
   - cycle 종료 `log.info(...)` summary object 에 `totalClassified` 추가.

3. **`workers/discussion-sync/tests/index.test.ts`** — 통합 테스트 1개:
   - mock Anthropic (label=price_reason 고정) + mock Bright Data fetch(기존 패턴) + mock Supabase 로 cycle 1회 실행.
   - 검증: `discussions.upsert` 호출됨, `discussions.update` 호출됨 (relevance='price_reason', classified_at 세팅), `log.info` 에 `totalClassified ≥ 1`.
   - 기존 tests/index.test.ts 가 있으면 해당 파일에 describe 블록 `'Phase 08.1 classify hook'` 을 append.
  </action>
  <acceptance_criteria>
    - `grep -q "unclassifiedRows" workers/discussion-sync/src/pipeline/upsert.ts`
    - `grep -q 'select("id,title,body,relevance,classified_at")' workers/discussion-sync/src/pipeline/upsert.ts`
    - `grep -q "classifyBatch" workers/discussion-sync/src/index.ts`
    - `grep -q "persistRelevance" workers/discussion-sync/src/index.ts`
    - `grep -q "totalClassified" workers/discussion-sync/src/index.ts`
    - `pnpm --filter @gh-radar/discussion-sync test` exit 0 (모든 기존 테스트 + 신규 통합 PASS)
    - `pnpm --filter @gh-radar/discussion-sync build` exit 0
  </acceptance_criteria>
  <done>
    cycle 이 upsert 직후 신규/미분류 행만 분류 → update. 기존 classified_at 이 있는 행은 skip (비용 통제 §2).
  </done>
</task>

</tasks>

<verification>
```bash
pnpm --filter @gh-radar/discussion-sync typecheck
pnpm --filter @gh-radar/discussion-sync test
pnpm --filter @gh-radar/discussion-sync build

# 로컬 smoke (ANTHROPIC_API_KEY + Supabase creds 있을 때):
# ANTHROPIC_API_KEY=... pnpm --filter @gh-radar/discussion-sync dev
# → 로그에 "classifyBatch done { total: X, classified: Y }" 및 cycle 종료 summary 에 totalClassified
# → Supabase SQL: SELECT relevance, count(*) FROM discussions GROUP BY 1;
```
</verification>

<success_criteria>
- classify/ 5 파일 + 2 테스트 + index.ts cycle 통합
- Claude Haiku 4.5 Sync API + max_tokens=10 + temperature=0 + p-limit(5) literal 준수
- 실패한 분류는 classified_at 미업데이트 → 다음 cycle 재시도
- `pnpm --filter @gh-radar/discussion-sync test` 전체 PASS
- DISC-01.1 의 "수집 직후 인라인 분류 엔진" 핵심 구현
</success_criteria>

<commit_hint>
commit message: `feat(discussion-sync): Phase 08.1 Plan 03 — Claude Haiku inline classify 모듈 + cycle 훅 (DISC-01.1 엔진)`
</commit_hint>

<output>
완료 후 T-01~T-04 결과(신규 파일 수, test 수, 모듈 호출 그래프) 요약 — SUMMARY 재료.
</output>
