---
phase: "08.1"
plan_id: "08.1-06"
plan: 6
type: execute
wave: 3
depends_on:
  - "08.1-01"
  - "08.1-02"
files_modified:
  - webapp/src/components/ui/switch.tsx
  - webapp/src/lib/stock-api.ts
  - webapp/src/components/stock/discussion-page-client.tsx
  - webapp/src/components/stock/__tests__/discussion-page-client.test.tsx
  - webapp/e2e/fixtures/mock-api.ts
autonomous: true
parallel_safe: true
requirements:
  - DISC-01
  - DISC-01.1
requirements_addressed:
  - DISC-01.1
must_haves:
  truths:
    - "/stocks/[code]/discussions 페이지 상단에 shadcn Switch 토글이 렌더링된다 (label: 의미있는 토론만 보기)"
    - "초기 상태는 meaningful (URL 미존재 시 기본 ON)"
    - "토글 on/off 시 URL 이 ?filter=meaningful 또는 ?filter=all 로 변경되고 리스트가 재fetch 된다"
    - "filter 상태가 loadMore (무한 스크롤 페이지네이션) 에도 일관되게 전달된다"
    - "meaningful 인데 결과 0건 시 '의미있는 토론이 아직 없어요. 토글을 꺼서 전체 글을 볼 수 있어요.' 카피가 표시된다"
    - "상세 페이지 Card (stock-discussion-section.tsx) 는 수정되지 않는다 (approved plan §Decisions §5)"
  artifacts:
    - path: "webapp/src/components/ui/switch.tsx"
      provides: "shadcn Switch (Radix UI)"
    - path: "webapp/src/lib/stock-api.ts"
      provides: "FetchDiscussionsOpts.filter + query serializer 확장"
    - path: "webapp/src/components/stock/discussion-page-client.tsx"
      provides: "URL sync + Switch UI + 빈 상태 카피 분기"
  key_links:
    - from: "webapp/src/components/stock/discussion-page-client.tsx"
      to: "useSearchParams / router.replace"
      via: "next/navigation"
      pattern: "useSearchParams|router.replace"
    - from: "webapp/src/lib/stock-api.ts fetchStockDiscussions"
      to: "server /api/stocks/:code/discussions?filter=..."
      via: "URLSearchParams"
      pattern: "params.set\\('filter'"
---

<objective>
풀페이지 `/stocks/[code]/discussions` 에 shadcn Switch 토글을 추가해 "의미있는 토론만 보기 (기본 ON)" 필터를 도입한다. URL 이 `?filter=meaningful` / `?filter=all` 로 sync 되고, 리스트/무한 스크롤 모두 filter 를 일관 전달한다. 상세 Card (`stock-discussion-section.tsx`, top-5/24h) 는 수정하지 않는다.
</objective>

<background>
Wave 3. 08.1-02 의 server filter 와 08.1-01 의 타입 확장이 있어야 의미가 있다. 08.1-05(backfill) 와 병렬 가능 (파일군 무관). 이 plan 은 사용자-대면 최종 UX — 토글 기본 ON = meaningful.

approved plan `/Users/alex/.claude/plans/08-1-breezy-pine.md` §Approach Wave 3 Plan 08.1-06 + §Decisions §5/§6 literal 이식.
</background>

<context>
@.planning/phases/08-discussion-board/08-CONTEXT.md
@webapp/src/components/stock/discussion-page-client.tsx
@webapp/src/lib/stock-api.ts
@webapp/src/lib/api.ts
@packages/shared/src/discussion.ts
@webapp/src/app/stocks/[code]/discussions/page.tsx
</context>

<interfaces>
From webapp/src/lib/stock-api.ts:83-116 (기존):
```typescript
export interface FetchDiscussionsOpts {
  hours?: number;
  days?: number;
  limit?: number;
  before?: string;
}
```
→ 확장:
```typescript
export interface FetchDiscussionsOpts {
  hours?: number;
  days?: number;
  limit?: number;
  before?: string;
  /** Phase 08.1 — 의미있는 토론 필터. 서버 schema 'all' | 'meaningful'. */
  filter?: 'all' | 'meaningful';
}
```

From webapp/src/components/stock/discussion-page-client.tsx (기존):
```typescript
export function DiscussionPageClient({ code }: { code: string }) {
  // useState: stock, discussions, isLoading, error, notFoundFlag, hasMore, isFetchingMore, paginationError
  // useEffect: load on mount, IntersectionObserver for loadMore
}
```
→ 확장:
```typescript
// 추가 상태: filter ('all' | 'meaningful')
// useSearchParams + useRouter 로 URL sync
// onCheckedChange → router.replace + setFilter + list reset + load
```
</interfaces>

<tasks>

<task id="T-01" type="auto">
  <name>Task 1: shadcn Switch 설치 + stock-api.ts 확장</name>
  <files>
    webapp/src/components/ui/switch.tsx
    webapp/src/lib/stock-api.ts
  </files>
  <read_first>
    - /Users/alex/repos/gh-radar/webapp/src/lib/stock-api.ts
    - /Users/alex/repos/gh-radar/webapp/components.json
    - /Users/alex/repos/gh-radar/webapp/package.json
  </read_first>
  <action>
1. **shadcn Switch 설치:**
   ```bash
   pnpm --filter webapp dlx shadcn@latest add switch
   ```
   이 명령은 `webapp/src/components/ui/switch.tsx` 생성 + `@radix-ui/react-switch` 의존성 추가.

2. **`webapp/src/lib/stock-api.ts`** — `FetchDiscussionsOpts` 와 `fetchStockDiscussions` 확장:
   - interface 에 `filter?: 'all' | 'meaningful';` 추가 (주석 포함).
   - `fetchStockDiscussions` body 의 `params` 직렬화에 추가 (limit 세팅 뒤):
     ```ts
     if (opts.filter) params.set('filter', opts.filter);
     ```
   - 기존 `hours/days/limit/before` 로직 유지. filter 는 optional — undefined 시 서버가 default 'all' 반환.
  </action>
  <acceptance_criteria>
    - `test -f webapp/src/components/ui/switch.tsx`
    - `grep -q "@radix-ui/react-switch" webapp/package.json`
    - `grep -q "filter?: 'all' | 'meaningful'" webapp/src/lib/stock-api.ts`
    - `grep -q "params.set('filter', opts.filter)" webapp/src/lib/stock-api.ts`
    - `pnpm --filter webapp typecheck` exit 0
  </acceptance_criteria>
  <done>
    Switch 컴포넌트 파일 존재 + fetchStockDiscussions 가 filter 쿼리를 전송한다.
  </done>
</task>

<task id="T-02" type="auto">
  <name>Task 2: discussion-page-client.tsx — Switch UI + URL sync + 빈 상태 카피</name>
  <files>webapp/src/components/stock/discussion-page-client.tsx</files>
  <read_first>
    - /Users/alex/repos/gh-radar/webapp/src/components/stock/discussion-page-client.tsx
    - /Users/alex/repos/gh-radar/webapp/src/components/ui/switch.tsx
  </read_first>
  <action>
`webapp/src/components/stock/discussion-page-client.tsx` 수정:

1. **import 추가:**
   ```tsx
   import { useSearchParams, useRouter } from 'next/navigation';
   import { Switch } from '@/components/ui/switch';
   ```

2. **컴포넌트 시그니처 유지** (props 변경 없음).

3. **초기 filter 파생 (URL 기반):**
   ```tsx
   const searchParams = useSearchParams();
   const router = useRouter();
   const filterFromUrl: 'all' | 'meaningful' =
     searchParams.get('filter') === 'all' ? 'all' : 'meaningful'; // 기본 meaningful (approved plan §6)
   const [filter, setFilter] = useState<'all' | 'meaningful'>(filterFromUrl);
   ```

4. **load 함수에 filter 파라미터 반영:**
   ```tsx
   // 기존 fetchStockDiscussions(code, { days: 7, limit: PAGE_SIZE }, signal) →
   fetchStockDiscussions(code, { days: 7, limit: PAGE_SIZE, filter }, controller.signal),
   ```

5. **loadMore 에도 filter 전달:**
   ```tsx
   const next = await fetchStockDiscussions(
     code,
     { days: 7, limit: PAGE_SIZE, before: cursor, filter },
     controller.signal,
   );
   ```

6. **onCheckedChange 핸들러:**
   ```tsx
   const onToggleFilter = useCallback((checked: boolean) => {
     const nextFilter: 'all' | 'meaningful' = checked ? 'meaningful' : 'all';
     setFilter(nextFilter);
     const params = new URLSearchParams(searchParams.toString());
     params.set('filter', nextFilter);
     router.replace(`?${params.toString()}`, { scroll: false });
     // list 초기화 — load() 가 discussions=null 로 리셋하고 첫 페이지 재fetch
     setDiscussions(null);
     setHasMore(true);
     setPaginationError(null);
     inFlightCursorRef.current = undefined;
   }, [searchParams, router]);
   ```

7. **useEffect 의존성 수정** — `load` 가 filter 의존하도록:
   - `load` 의 `useCallback` deps 에 `filter` 추가.
   - 첫 mount 뿐만 아니라 filter 변경 시에도 `useEffect(() => { void load(); }, [load])` 로 재실행됨.

8. **Switch UI 삽입** — h1 아래 flex row, back-nav 와 분리된 자리:
   ```tsx
   <header className="flex items-center gap-3">
     <Link href={`/stocks/${encodeURIComponent(code)}`} aria-label="종목 상세로 돌아가기" ...>←</Link>
     <h1 ...>{headingName} — 최근 7일 토론</h1>
   </header>

   <div className="flex items-center justify-between gap-3" data-testid="discussion-filter-toggle">
     <label htmlFor="discussion-meaningful-toggle" className="text-[length:var(--t-sm)] text-[var(--muted-fg)]">
       의미있는 토론만 보기
     </label>
     <Switch
       id="discussion-meaningful-toggle"
       checked={filter === 'meaningful'}
       onCheckedChange={onToggleFilter}
       aria-label="의미있는 토론만 보기"
     />
   </div>
   ```

9. **빈 상태 카피 분기:**
   - 기존 `discussions.length === 0` 섹션을 filter 별로 분기:
     ```tsx
     : discussions && discussions.length === 0 ? (
       <section role="status" data-testid="discussion-page-empty" ...>
         <h2>표시할 토론 글이 없어요</h2>
         <p>
           {filter === 'meaningful'
             ? '의미있는 토론이 아직 없어요. 토글을 꺼서 전체 글을 볼 수 있어요.'
             : '최근 7일 내 수집된 토론 글이 없습니다. 종목 상세에서 새로고침을 실행해주세요.'}
         </p>
       </section>
     )
     ```

**반드시 지키기:**
- 상세 Card (`stock-discussion-section.tsx`) 는 이 task 에서 **절대 수정 금지** (approved plan §5).
- URL 기본값: `?filter` 미존재 시 UI 는 meaningful 표시지만 URL 은 업데이트하지 않음 (초기 하이드레이션 시 라우터 미변경 — `router.replace` 는 onCheckedChange 에서만 호출).
- SSR 하이드레이션 안전성: `useSearchParams` 는 client component 에서만 — 이 파일이 이미 `'use client'` 이므로 문제 없음.
  </action>
  <acceptance_criteria>
    - `grep -q "useSearchParams" webapp/src/components/stock/discussion-page-client.tsx`
    - `grep -q "useRouter" webapp/src/components/stock/discussion-page-client.tsx`
    - `grep -q "import { Switch }" webapp/src/components/stock/discussion-page-client.tsx`
    - `grep -q "의미있는 토론만 보기" webapp/src/components/stock/discussion-page-client.tsx`
    - `grep -q "의미있는 토론이 아직 없어요. 토글을 꺼서 전체 글을 볼 수 있어요" webapp/src/components/stock/discussion-page-client.tsx`
    - `grep -q "filter === 'meaningful'" webapp/src/components/stock/discussion-page-client.tsx`
    - `grep -q "filter === 'meaningful' ? 'meaningful' : 'all'" webapp/src/components/stock/discussion-page-client.tsx || grep -q "checked ? 'meaningful' : 'all'" webapp/src/components/stock/discussion-page-client.tsx`
    - `grep -q "router.replace" webapp/src/components/stock/discussion-page-client.tsx`
    - **상세 Card 미변경 검증:** `git diff --stat HEAD -- webapp/src/components/stock/stock-discussion-section.tsx` → 빈 출력 (수정 없음)
    - `pnpm --filter webapp typecheck` exit 0
    - `pnpm --filter webapp build` exit 0
  </acceptance_criteria>
  <done>
    Switch + URL sync + 빈 상태 카피 분기 완료. 상세 Card 무변경.
  </done>
</task>

<task id="T-03" type="auto" tdd="true">
  <name>Task 3: vitest 컴포넌트 테스트 — URL sync / 빈 상태 / filter→fetch 전달</name>
  <files>webapp/src/components/stock/__tests__/discussion-page-client.test.tsx</files>
  <read_first>
    - /Users/alex/repos/gh-radar/webapp/src/components/stock/discussion-page-client.tsx
    - /Users/alex/repos/gh-radar/webapp/src/components/stock/__tests__ (존재 확인 — 없으면 생성)
  </read_first>
  <behavior>
    - Test 1: 초기 URL `?` (filter 미존재) → Switch checked=true (meaningful), fetchStockDiscussions 호출 시 `{ filter: 'meaningful' }` 전달
    - Test 2: 초기 URL `?filter=all` → Switch checked=false, fetch 시 `{ filter: 'all' }` 전달
    - Test 3: Switch 클릭(ON→OFF) → router.replace('?filter=all') 호출 + fetch 재호출 with `{ filter: 'all' }`
    - Test 4: filter=meaningful 로 빈 배열 반환 시 `"의미있는 토론이 아직 없어요. 토글을 꺼서 전체 글을 볼 수 있어요."` 텍스트 렌더
    - Test 5: filter=all 로 빈 배열 반환 시 기존 카피(`최근 7일 내 수집된 토론 글이 없습니다...`) 렌더
  </behavior>
  <action>
기존 webapp vitest + React Testing Library 패턴을 따라 테스트 생성. `next/navigation` 은 vi.mock 으로 `useSearchParams` / `useRouter` override. `fetchStockDiscussions` + `fetchStockDetail` 는 `vi.mock('@/lib/stock-api', ...)` 로 spy.

```tsx
import { render, screen, waitFor, act, fireEvent } from '@testing-library/react';
import { describe, it, expect, vi, beforeEach } from 'vitest';

const mockReplace = vi.fn();
const mockSearchParams = new URLSearchParams();

vi.mock('next/navigation', () => ({
  useSearchParams: () => mockSearchParams,
  useRouter: () => ({ replace: mockReplace, push: vi.fn() }),
  notFound: vi.fn(),
}));

vi.mock('@/lib/stock-api', () => ({
  fetchStockDetail: vi.fn().mockResolvedValue({ code: '005930', name: '삼성전자', price: 70000 }),
  fetchStockDiscussions: vi.fn().mockResolvedValue([]),
}));

import { DiscussionPageClient } from '../discussion-page-client';
import { fetchStockDiscussions } from '@/lib/stock-api';

beforeEach(() => {
  mockReplace.mockClear();
  (fetchStockDiscussions as any).mockClear();
  mockSearchParams.delete('filter');
});

describe('DiscussionPageClient Phase 08.1 filter toggle', () => {
  it('초기 URL 미지정 → meaningful 로 fetch', async () => {
    render(<DiscussionPageClient code="005930" />);
    await waitFor(() => expect(fetchStockDiscussions).toHaveBeenCalled());
    const opts = (fetchStockDiscussions as any).mock.calls[0][1];
    expect(opts.filter).toBe('meaningful');
  });

  // 나머지 테스트도 동일 패턴
});
```
  </action>
  <acceptance_criteria>
    - `test -f webapp/src/components/stock/__tests__/discussion-page-client.test.tsx`
    - `pnpm --filter webapp test -- discussion-page-client` exit 0 (5 PASS)
  </acceptance_criteria>
  <done>
    5 컴포넌트 테스트 PASS. URL sync + 빈 상태 분기 + filter 파라미터 전달 검증.
  </done>
</task>

<task id="T-04" type="auto">
  <name>Task 4: Playwright mock 확장 (filter 쿼리 수용)</name>
  <files>webapp/e2e/fixtures/mock-api.ts</files>
  <read_first>
    - /Users/alex/repos/gh-radar/webapp/e2e/fixtures/mock-api.ts
  </read_first>
  <action>
기존 `mockDiscussionsApi` 핸들러(Phase 08 에서 생성)가 URL 의 `filter` 쿼리를 이해하도록 확장. 구현 예:

```ts
// mock-api.ts 안의 discussion route handler 수정
export async function mockDiscussionsApi(page: Page, code: string, rows: Discussion[]) {
  await page.route(`**/api/stocks/${code}/discussions?**`, (route) => {
    const url = new URL(route.request().url());
    const filter = url.searchParams.get('filter') ?? 'all';
    let body = rows;
    if (filter === 'meaningful') {
      body = rows.filter((r) => r.relevance !== 'noise'); // relevance null 도 pass
    }
    route.fulfill({ status: 200, contentType: 'application/json', body: JSON.stringify(body) });
  });
}
```

NOTE: 기존 mock 의 정확한 signature 에 맞춰 최소 패치만 하기 (파라미터 시그니처 변경 금지).
  </action>
  <acceptance_criteria>
    - `grep -q "searchParams.get('filter')" webapp/e2e/fixtures/mock-api.ts`
    - `grep -q "relevance !== 'noise'" webapp/e2e/fixtures/mock-api.ts`
    - `pnpm --filter webapp typecheck` exit 0
  </acceptance_criteria>
  <done>
    Playwright mock 이 filter 를 이해. E2E spec (08.1-07) 이 토글 동작을 검증 가능.
  </done>
</task>

</tasks>

<verification>
```bash
pnpm --filter webapp typecheck
pnpm --filter webapp test
pnpm --filter webapp build

# 로컬 smoke (PORT=3100, memory: feedback_check_dev_sh_first.md):
# bash dev.sh  # webapp + server 동시
# open http://localhost:3100/stocks/005930/discussions
# → 토글 초기 ON 상태, URL ?filter= 없음 (default)
# → 토글 클릭 → URL 에 ?filter=all 추가, 리스트 재fetch
# → 다시 클릭 → ?filter=meaningful, 재fetch
# → 직접 주소창 /stocks/005930/discussions?filter=all 입력 → 토글 OFF 상태로 로드
```
</verification>

<success_criteria>
- Switch 컴포넌트 설치 + stock-api.ts 확장
- URL sync (initial read from useSearchParams + update via router.replace)
- load/loadMore 모두 filter 전달
- 빈 상태 카피 2종 (meaningful vs all)
- 5 vitest 테스트 PASS
- 상세 Card (`stock-discussion-section.tsx`) **변경 없음**
- Playwright mock 이 filter 쿼리 수용
- DISC-01.1 의 "사용자-대면 필터 UX" 완수
</success_criteria>

<commit_hint>
commit message: `feat(webapp): Phase 08.1 Plan 06 — discussions 풀페이지 Switch 토글 + URL sync + 빈 상태 카피 (DISC-01.1 UI)`
</commit_hint>

<output>
T-01~T-04 결과 + 로컬 smoke 스크린샷(또는 기능 체크리스트) → SUMMARY 재료.
</output>
