---
phase: 08.1-discussion-relevance-filter
verified: 2026-04-22T16:05:00Z
status: human_needed
score: 11/11 code-level must-haves verified (5 manual follow-ups pending)
overrides_applied: 0
gaps: []
deferred:
  - truth: "AI 토론방 요약 + 긍/부정/중립 센티먼트 분석"
    addressed_in: "Phase 9"
    evidence: "Phase 9: AI Summarization — Requirements: NEWS-02, DISC-02 (ROADMAP.md:284-287)"
  - truth: "상세 페이지 Card(top-5 / 24h) 에 의미성 필터 토글 적용"
    addressed_in: "Phase 10+"
    evidence: "08.1-SUMMARY.md §Deferred — '상세 Card top-5 에 토글 적용 → Phase 10+'"
  - truth: "packages/discussion-classify 로 worker+server 공유 로직 이관"
    addressed_in: "Phase 9+"
    evidence: "08.1-SUMMARY.md §Deferred — 'packages/discussion-classify shared 이관 (현재 server/worker 에 TEMP DUPLICATION)'"
human_verification:
  - test: "Anthropic API key 주입 및 GCP Secret `gh-radar-anthropic-api-key` version 등록"
    expected: "`gcloud secrets versions list gh-radar-anthropic-api-key --project=gh-radar` 가 최소 1개 ENABLED version 반환. 2개 SA (gh-radar-discussion-sync-sa, gh-radar-server-sa) 가 secretAccessor 바인딩 보유."
    why_human: "Auto-mode 는 실 API key 를 생성/주입할 수 없음. setup-discussion-sync-iam.sh 가 idempotent 하게 secret placeholder 를 생성하고 accessor grant 를 설정하지만 실 값(sk-ant-...) 주입 단계는 `ANTHROPIC_API_KEY` env 를 보유한 사용자만 수행 가능."
  - test: "Cloud Run Service (gh-radar-server) + Cloud Run Job (gh-radar-discussion-sync) 재배포로 ANTHROPIC_API_KEY 환경변수 반영"
    expected: "`gcloud run services describe gh-radar-server --region=asia-northeast3 --format='value(spec.template.spec.containers[0].env)' | grep ANTHROPIC` 출력에 secret ref 포함. `gcloud run jobs describe gh-radar-discussion-sync --region=asia-northeast3 --format='value(spec.template.spec.taskTimeout)'` 가 `1800s` 반환."
    why_human: "Auto-mode 가 production Cloud Run 재배포 (GCP API 호출, 이미지 빌드) 를 실행하지 않음. deploy 스크립트는 commit 되어 있으나 사용자가 `bash scripts/deploy-discussion-sync.sh && bash scripts/deploy-server.sh` 실행 필요."
  - test: "15,463 rows production backfill 실행 및 분포 스냅샷 획득"
    expected: "`pnpm --filter @gh-radar/discussion-sync backfill:dev` 로그 마지막 라인에 `backfill complete`, `classified > 10000`. `SELECT relevance, count(*) FROM discussions GROUP BY relevance` 결과 4 라벨 분포 획득 + NULL 잔량 < 200."
    why_human: "Auto-mode 는 ~$23 비용 소비와 ~50분 장시간 실행을 사용자 승인 없이 수행하면 안 됨. backfill.ts 는 idempotent 하므로 중단/재개 가능."
  - test: "Production Vercel UI smoke 6/6 확인"
    expected: "https://gh-radar-webapp.vercel.app/stocks/005930/discussions 에서: (1) 토글 기본 ON (2) 토글 클릭 시 URL `?filter=all` 반영 (3) `?filter=meaningful` 으로 직접 진입 시 Switch ON 상태 (4) 의미없는 토론 노출 제거 확인 (5) 무한 스크롤 시 filter 유지 (6) SSR hydration mismatch 없음."
    why_human: "UI 시각 검증, 사용자 인터랙션 (클릭·URL 타이핑), SSR 동작 확인이 필요해 브라우저 세션 실 사용자 경험으로만 판정 가능."
  - test: "Playwright E2E 로컬 실행 (discussion-filter.spec.ts 4 시나리오)"
    expected: "`pnpm --filter webapp exec playwright install chromium` 후 `pnpm --filter webapp exec playwright test discussion-filter.spec.ts` exit 0, 4/4 PASS."
    why_human: "Auto-mode 환경에 Playwright browser 바이너리 미설치. spec 파일은 commit 되어 있으나 로컬 실행은 browser install 필요."
---

# Phase 08.1: 종목토론 의미성 AI 분류 + 웹앱 필터 토글 검증 보고서

**Phase Goal:** 종목토론 글을 Claude AI(Haiku 4.5) 로 4-카테고리 분류(`price_reason` / `theme` / `news_info` / `noise`) 하고, 웹앱 풀페이지에 "의미있는 토론만 보기" 토글(기본 ON)을 추가해 트레이더가 노이즈 없이 핵심 토론을 빠르게 파악하도록 한다.
**Verified:** 2026-04-22T16:05:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (코드 레벨)

| #   | Truth                                                                 | Status     | Evidence                                                                                          |
| --- | --------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------- |
| 1   | discussions.relevance / classified_at 컬럼 + CHECK 제약 + 2 partial index | ✓ VERIFIED | `supabase/migrations/20260422120000_discussion_relevance.sql` (ADD COLUMN 2개 + `discussions_relevance_check` + `idx_discussions_unclassified` + `idx_discussions_meaningful`) |
| 2   | packages/shared Discussion 타입에 relevance(4-label union) + classifiedAt 전파 | ✓ VERIFIED | `packages/shared/src/discussion.ts:40-42` — 'price_reason' \| 'theme' \| 'news_info' \| 'noise' \| null + classifiedAt: string \| null. shared build PASS. |
| 3   | worker cycle 이 upsert 직후 Claude Haiku 4.5 Sync API 로 신규 행 inline 분류 (p-limit 5, temperature 0, max_tokens 10) | ✓ VERIFIED | `workers/discussion-sync/src/classify/` 5파일 (anthropic/prompt/classifyOne/classifyBatch/persistRelevance). `classifyOne.ts`: `max_tokens: 10`, `temperature: 0`. `classifyBatch.ts`: `pLimit(cfg.classifyConcurrency)`. index.ts L115-117: `classifyBatch → persistRelevance` 훅 + totalClassified 로그. |
| 4   | server GET `/api/stocks/:code/discussions?filter=meaningful` 분기 (`relevance IS NULL OR relevance != 'noise'`) | ✓ VERIFIED | `server/src/schemas/discussions.ts:50` — `filter: z.enum(['all','meaningful'])`. `server/src/routes/discussions.ts:272-273` — `if (filter === 'meaningful') q = q.or('relevance.is.null,relevance.neq.noise')`. |
| 5   | server POST `/refresh` 에도 classify 훅 + graceful no-op when ANTHROPIC_API_KEY 미존재 | ✓ VERIFIED | `server/src/services/discussion-classify.ts` (TEMP DUPLICATION 명시). `routes/discussions.ts:17 + L420 + L435` — `classifyAndPersist` import + upsert().select 확장 + non-fatal try/catch. |
| 6   | webapp 풀페이지 `/stocks/[code]/discussions` 에 shadcn Switch 토글 (기본 ON=meaningful) + URL sync | ✓ VERIFIED | `webapp/src/components/ui/switch.tsx` 존재. `discussion-page-client.tsx:5,10,51-52,171,231-237,258` — useSearchParams/useRouter, Switch 렌더, router.replace, 빈 상태 분기 카피. |
| 7   | Webapp fetchStockDiscussions 이 filter 쿼리 전송 | ✓ VERIFIED | `webapp/src/lib/stock-api.ts:99` — `filter?: 'all' \| 'meaningful'`. L120 — `if (opts.filter) params.set('filter', opts.filter)`. |
| 8   | 상세 Card (`stock-discussion-section.tsx`) 미변경 (approved plan §Decisions §5) | ✓ VERIFIED | git diff 결과 stock-discussion-section.tsx 는 phase range (184a65b..HEAD) 에 나타나지 않음 — code review report 19 files_reviewed_list 에도 부재. |
| 9   | 기존 15k 행 backfill 스크립트 (MAX_BACKFILL_ROWS + SIGINT graceful + 단위 테스트) | ✓ VERIFIED | `workers/discussion-sync/src/backfill.ts` — runBackfill DI export, SIGINT handler, MAX_BACKFILL_ROWS default 20000. `package.json:8-9` — backfill/backfill:dev scripts. `backfill.test.ts` 3 tests PASS. |
| 10  | Playwright E2E spec (4 시나리오) | ✓ VERIFIED | `webapp/e2e/specs/discussion-filter.spec.ts` (PLAN 에는 `tests/` 경로 명시되었으나 webapp 프로젝트의 실제 폴더 규약은 `specs/` 임). 4 describe/test — 기본 ON, 토글 OFF URL=all, 직접 ?filter=all 진입, 빈 배열 카피. |
| 11  | deploy 스크립트 ANTHROPIC_API_KEY secret mount + task-timeout=1800 + setup IAM | ✓ VERIFIED | `scripts/deploy-discussion-sync.sh:63` (secret pre-check), L143 (task-timeout=1800), L148 (--set-secrets 에 ANTHROPIC_API_KEY). `scripts/deploy-server.sh:76,86,130` 동일 패턴. `scripts/setup-discussion-sync-iam.sh:83,146-164` ANTHROPIC_SECRET 생성 + 2 SA accessor grant. bash -n 3개 모두 통과. |

**Score:** 11/11 code-level truths verified

### Deferred Items (후속 phase)

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | AI 토론방 요약 + 긍/부정/중립 센티먼트 분석 (DISC-02) | Phase 9 | ROADMAP.md §Phase 9 Requirements: NEWS-02, DISC-02 |
| 2 | 상세 페이지 Card(top-5 / 24h) 에 의미성 필터 토글 적용 | Phase 10+ | 08.1-SUMMARY.md §Deferred |
| 3 | packages/discussion-classify 로 worker+server 공유 로직 이관 | Phase 9+ (TODO) | 08.1-SUMMARY.md §TODOs + 소스 주석 `⚠️ TEMP DUPLICATION` |
| 4 | 분류 프롬프트 튜닝/재학습 (v2) | 운영 관찰 후 | 08.1-SUMMARY.md §Deferred |
| 5 | 분류 실패 (NULL 잔여) 전용 재시도 정책 | 운영 관찰 후 | 08.1-SUMMARY.md §Deferred (현재는 매 cycle 자동 pickup) |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/20260422120000_discussion_relevance.sql` | relevance + classified_at + CHECK + 2 partial index | ✓ VERIFIED | 2028 bytes, ALTER TABLE + CHECK 제약 + idx_discussions_unclassified + idx_discussions_meaningful 모두 포함 |
| `packages/shared/src/discussion.ts` | Discussion type 확장 | ✓ VERIFIED | relevance union + classifiedAt, tsup build green |
| `server/src/schemas/discussions.ts` | filter enum | ✓ VERIFIED | `z.enum(['all','meaningful']).optional()` + transform default 'all' |
| `server/src/routes/discussions.ts` | filter=meaningful 분기 + relevance select | ✓ VERIFIED | DISCUSSION_SELECT 에 relevance,classified_at. `.or('relevance.is.null,relevance.neq.noise')` 체이닝. |
| `server/src/mappers/discussions.ts` | toDiscussion camelCase 매핑 | ✓ VERIFIED | L54-55 relevance cast + classifiedAt |
| `server/src/services/discussion-classify.ts` | server-side classifyAndPersist (TEMP DUPLICATION) | ✓ VERIFIED | 5220 bytes, max_tokens=10/temperature=0/ANTHROPIC redact + graceful no-op |
| `workers/discussion-sync/src/classify/{5 files}` | anthropic/prompt/classifyOne/classifyBatch/persistRelevance | ✓ VERIFIED | 5파일 모두 존재, 7 unit tests PASS |
| `workers/discussion-sync/src/backfill.ts` | runBackfill DI + SIGINT handler | ✓ VERIFIED | 6104 bytes, runBackfill export, 3 unit tests PASS |
| `workers/discussion-sync/src/index.ts` | cycle 에 classify 훅 삽입 | ✓ VERIFIED | L115-117 classifyBatch → persistRelevance, L169 totalClassified 로그 |
| `workers/discussion-sync/src/pipeline/upsert.ts` | UpsertResult.unclassifiedRows | ✓ VERIFIED | select("id,title,body,relevance,classified_at") + filter(r.classified_at == null) |
| `webapp/src/components/ui/switch.tsx` | shadcn Switch | ✓ VERIFIED | radix-ui Switch wrap |
| `webapp/src/lib/stock-api.ts` | FetchDiscussionsOpts.filter | ✓ VERIFIED | filter?: 'all' \| 'meaningful' + params.set('filter',...) |
| `webapp/src/components/stock/discussion-page-client.tsx` | Switch + URL sync + 빈 상태 카피 | ✓ VERIFIED | useSearchParams/useRouter + Switch + '의미있는 토론이 아직 없어요...' 카피 |
| `webapp/e2e/specs/discussion-filter.spec.ts` | Playwright 4 시나리오 | ✓ VERIFIED | 4 test cases — 기본 ON/OFF toggle/직접 URL/빈 배열 |
| `webapp/e2e/fixtures/discussions.ts` | mock 이 filter 쿼리 수용 | ✓ VERIFIED | searchParams.get('filter') + relevance !== 'noise' 분기 (mock-api.ts:10 주석 참조) |
| `scripts/deploy-discussion-sync.sh` | ANTHROPIC_API_KEY secret + task-timeout=1800 | ✓ VERIFIED | --set-secrets + task-timeout=1800 + pre-check |
| `scripts/deploy-server.sh` | ANTHROPIC_API_KEY secret | ✓ VERIFIED | --update-secrets 에 ANTHROPIC_API_KEY 포함 |
| `scripts/setup-discussion-sync-iam.sh` | Secret 생성 + 2 SA accessor | ✓ VERIFIED | ANTHROPIC_SECRET 변수 + accessor loop (2개 SA) |
| `.planning/REQUIREMENTS.md` | DISC-01.1 + Traceability 행 | ✓ VERIFIED | L43 DISC-01.1 entry + L117 Traceability 행 + v1 27 total |
| `.planning/ROADMAP.md` | Phase 08.1 Goal/Requirements/Plans | ✓ VERIFIED | L258-261 Phase 08.1 + 8/7 plans complete 마킹 |
| `server/tests/routes/discussions.filter.test.ts` | 4 integration tests | ✓ VERIFIED | 파일 존재 + server test suite 106 PASS 포함 |
| `server/tests/routes/discussions.refresh.classify.test.ts` | 3 integration tests | ✓ VERIFIED | 파일 존재 + server test suite 106 PASS 포함 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| server GET handler | Supabase `.or('relevance.is.null,relevance.neq.noise')` | filter==='meaningful' 분기 | ✓ WIRED | routes/discussions.ts:272-273 grep 확인 |
| server mapper toDiscussion | packages/shared Discussion | relevance/classifiedAt camelCase | ✓ WIRED | mappers/discussions.ts:54-55 |
| worker upsert pipeline | classify/classifyBatch + persistRelevance | unclassifiedRows → cycle 훅 | ✓ WIRED | index.ts:16-17 import, L115-117 호출 |
| backfill.ts | classifyBatch + persistRelevance | runBackfill 내부 호출 | ✓ WIRED | backfill.ts grep — 둘 다 import 후 루프 내 호출 |
| discussion-page-client | Next.js useSearchParams/useRouter | URL sync | ✓ WIRED | L5 import, L51-52 hook, L171 router.replace |
| fetchStockDiscussions | /api/stocks/:code/discussions?filter=... | URLSearchParams | ✓ WIRED | stock-api.ts:120 params.set('filter', opts.filter) |
| deploy scripts | GCP Secret `gh-radar-anthropic-api-key:latest` | --set-secrets / --update-secrets | ✓ WIRED | 3개 스크립트 grep 통과, bash -n 정상 |
| setup-iam script | 2 SA accessor bindings | gcloud secrets add-iam-policy-binding loop | ✓ WIRED | L146-164 (ANTHROPIC secret + accessor grant) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| shared 패키지 Discussion 타입 build | `pnpm --filter @gh-radar/shared build` | DTS 7.50 KB build success | ✓ PASS |
| discussion-sync 전체 테스트 | `pnpm --filter @gh-radar/discussion-sync test` | 15 test files / 77 passed / 3 todo | ✓ PASS |
| server 전체 테스트 | `pnpm --filter server test` | 17 test files / 106 passed | ✓ PASS |
| webapp 전체 테스트 | `pnpm --filter webapp test` | 22 test files / 143 passed / 1 skipped | ✓ PASS |
| deploy script 문법 | `bash -n scripts/deploy-*.sh scripts/setup-*.sh` | 3개 모두 exit 0 | ✓ PASS |
| classify 단위 테스트 (7건) | `pnpm --filter @gh-radar/discussion-sync test -- classify` | classifyOne 4 + classifyBatch 3 passed | ✓ PASS |
| backfill 단위 테스트 | `pnpm --filter @gh-radar/discussion-sync test -- backfill` | 3 passed | ✓ PASS |
| webapp discussion-page-client 테스트 | `pnpm --filter webapp test -- discussion-page-client` | 5 passed | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| DISC-01 (enhancement) | 08.1-01 ~ 08.1-07 | 네이버 종목토론 글 목록 표시 (의미성 필터 토글 추가로 강화) | ✓ SATISFIED | REQUIREMENTS.md:42 [x] Phase 8 complete. Phase 08.1 에서 추가된 토글이 기존 풀페이지 UX 를 강화. |
| DISC-01.1 (new) | 08.1-01 ~ 08.1-07 | 종목토론 의미성 AI 분류 (4-category) + 필터링 토글 | ✓ SATISFIED (code-level) | REQUIREMENTS.md:43 DISC-01.1 entry + Traceability 행. 11 code-level truths 모두 VERIFIED. production backfill + 배포는 manual follow-up 필요. |

### Anti-Patterns Found

Code review (08.1-REVIEW.md) 는 `status: clean` 으로 Critical/Warning 0건. Info 2건만:
- IN-01: `server/src/mappers/discussions.ts:8-9` 주석의 migration 파일명 오타 (`20260422000000_discussions_relevance.sql` → 실제 `20260422120000_discussion_relevance.sql`). 런타임 영향 없음.
- IN-02: server/worker classify 로직 TEMP DUPLICATION (자가 기록 + SUMMARY §TODOs 에 이관 추적). 의도된 기술 부채.

### Human Verification Required

다음 5개 항목은 auto-mode 제약으로 인해 코드 레벨까지만 완료됨. production 활성화를 위해 사용자 수동 액션 필요:

#### 1. ANTHROPIC_API_KEY 발급 + GCP Secret version 주입

**Test:** Anthropic 콘솔에서 API key 발급 후, `export ANTHROPIC_API_KEY=sk-ant-...` 로 환경변수 설정 후 `bash scripts/setup-discussion-sync-iam.sh` 실행.
**Expected:** `gcloud secrets versions list gh-radar-anthropic-api-key --project=gh-radar` 가 1개 이상 ENABLED version 반환. `gcloud secrets get-iam-policy gh-radar-anthropic-api-key` 에 2개 SA (gh-radar-discussion-sync-sa, gh-radar-server-sa) accessor 바인딩.
**Why human:** auto-mode 는 실 API key 값을 생성/주입할 수 없음.

#### 2. Cloud Run Service + Job 재배포

**Test:** `bash scripts/deploy-discussion-sync.sh && bash scripts/deploy-server.sh`.
**Expected:**
- `gcloud run jobs describe gh-radar-discussion-sync --region=asia-northeast3 --project=gh-radar --format='value(spec.template.spec.taskTimeout)'` → `1800s`
- `gcloud run jobs describe` 에서 env 에 `ANTHROPIC_API_KEY` secret ref 포함
- `gcloud run services describe gh-radar-server --region=asia-northeast3` 도 동일 secret ref 포함

**Why human:** auto-mode 가 production Cloud Run 재배포 (이미지 빌드 + rollout) 를 실행하지 않음.

#### 3. 15,463 rows production backfill 실행

**Test:**
```bash
pnpm --filter @gh-radar/discussion-sync build
pnpm --filter @gh-radar/discussion-sync backfill:dev 2>&1 | tee /tmp/backfill-08.1-05.log
```
**Expected:** 로그 마지막 라인에 `backfill complete`, `classified > 10000`. Supabase SQL `SELECT relevance, count(*) FROM discussions GROUP BY relevance` → 4 라벨 분포 + NULL 잔량 < 200.
**Why human:** ~$23 API 비용 + ~50분 장시간 실행. 사용자 승인 없이 auto-mode 가 실행하면 안 됨. 스크립트는 idempotent (중단/재개 가능).

#### 4. Production Vercel UI smoke 6/6

**Test:** https://gh-radar-webapp.vercel.app/stocks/005930/discussions 방문 후 6 항목 클릭 검증.
**Expected:** 토글 기본 ON / 클릭 시 URL `?filter=all` / 직접 `?filter=meaningful` 진입 시 Switch ON / noise 제거 / 무한 스크롤 filter 유지 / SSR hydration 문제 없음.
**Why human:** UI 시각 검증 + 사용자 인터랙션 + SSR 동작은 실 브라우저에서만 판정 가능.

#### 5. Playwright 로컬 실행

**Test:**
```bash
pnpm --filter webapp exec playwright install chromium
pnpm --filter webapp exec playwright test discussion-filter.spec.ts
```
**Expected:** exit 0, 4/4 PASS.
**Why human:** auto-mode 환경에 Playwright browser 바이너리 미설치. spec 파일은 commit 되어 있음.

### Gaps Summary

코드 레벨 gap 은 **없음**. 11개 must_haves (migration, shared type, worker cycle, server filter + refresh, webapp Switch + URL sync + 빈 상태 카피, backfill, deploy scripts, REQUIREMENTS/ROADMAP 갱신, E2E spec) 모두 파일 존재 + 내용 패턴 일치 + 단위/통합 테스트 PASS (discussion-sync 77, server 106, webapp 143, shared build green).

Production 활성화 5 항목은 중요 context 에 명시된 auto-mode 제약 (secret 값 주입, Cloud Run 재배포, ~$23 비용 backfill, UI smoke, Playwright browser install) 에 해당하며, 코드/config 레벨로는 준비 완료이므로 `human_needed` 로 분류.

Code review (`08.1-REVIEW.md`) 는 `status: clean` (Critical/Warning 0건). TEMP DUPLICATION 은 자가 기록되고 Phase 9+ 로 deferred.

---

_Verified: 2026-04-22T16:05:00Z_
_Verifier: Claude (gsd-verifier)_
