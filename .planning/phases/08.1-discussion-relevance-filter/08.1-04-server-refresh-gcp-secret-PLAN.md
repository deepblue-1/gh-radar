---
phase: "08.1"
plan_id: "08.1-04"
plan: 4
type: execute
wave: 2
depends_on:
  - "08.1-01"
  - "08.1-02"
  - "08.1-03"
files_modified:
  - server/src/services/discussion-classify.ts
  - server/src/routes/discussions.ts
  - server/src/config.ts
  - server/src/__tests__/discussions.refresh.classify.test.ts
  - scripts/deploy-discussion-sync.sh
  - scripts/setup-discussion-sync-iam.sh
  - scripts/deploy-server.sh
autonomous: false
parallel_safe: false
requirements:
  - DISC-01
  - DISC-01.1
requirements_addressed:
  - DISC-01.1
must_haves:
  truths:
    - "POST /api/stocks/:code/discussions/refresh 는 Bright Data 스크래핑 + upsert 직후 신규 행을 Claude Haiku 로 분류하고 DB 에 relevance / classified_at 을 즉시 반영한다"
    - "refresh 응답은 relevance 필드가 채워진 Discussion[] 을 반환한다 (신규 행 한정, 기존 캐시 행은 이미 분류되어 있음)"
    - "Cloud Run Job (gh-radar-discussion-sync) 과 Cloud Run Service (gh-radar-server) 둘 다 ANTHROPIC_API_KEY 시크릿을 마운트한다"
    - "Cloud Run Job task-timeout 이 1800s 로 상향되어 Claude 병렬 호출 여유 확보"
  artifacts:
    - path: "server/src/services/discussion-classify.ts"
      provides: "worker classify/ 로직의 서버 쪽 1회 복제 (향후 shared package 이관 TODO)"
    - path: "scripts/deploy-discussion-sync.sh"
      provides: "discussion-sync Job --set-secrets 에 ANTHROPIC_API_KEY 추가 + --task-timeout=1800"
    - path: "scripts/deploy-server.sh"
      provides: "server Cloud Run --set-secrets 에 ANTHROPIC_API_KEY 추가"
  key_links:
    - from: "server/src/routes/discussions.ts POST /refresh 핸들러"
      to: "server/src/services/discussion-classify.ts"
      via: "scraped 신규 행을 inline 분류"
      pattern: "classifyAndPersist"
    - from: "scripts/deploy-*.sh"
      to: "GCP Secret Manager secret gh-radar-anthropic-api-key"
      via: "--set-secrets ANTHROPIC_API_KEY=gh-radar-anthropic-api-key:latest"
      pattern: "gh-radar-anthropic-api-key:latest"
---

<objective>
Server 의 POST `/api/stocks/:code/discussions/refresh` 엔드포인트도 worker 와 동일하게 on-demand 수집 직후 inline 분류를 수행하도록 확장한다. Claude SDK 호출 로직은 `server/src/services/discussion-classify.ts` 에 1회 복제(향후 shared package 이관 TODO). GCP 배포 측면에서 신규 secret `gh-radar-anthropic-api-key` 를 Secret Manager 에 등록하고 discussion-sync Job + server Cloud Run Service 둘 다 마운트 + SA accessor grant + task-timeout 1800s 상향을 적용한다.
</objective>

<background>
Wave 2 두 번째 plan. 08.1-03 의 classify 모듈을 server 쪽에 복제해 on-demand 경로에서도 동일 인라인 분류를 보장한다 (approved plan §Approach Wave 2 08.1-04). 공유 package 분리는 최소침입 원칙에 의해 **지연** — 복제본에 TODO 주석 명시. `gcloud secrets create` 및 SA accessor grant 는 수동 단계가 필수(GCP 콘솔 권한 + `.env.local` 의 ANTHROPIC_API_KEY 먼저 확인, memory reference feedback_dont_ask_existing_creds.md) 이므로 `autonomous: false`.

approved plan `/Users/alex/.claude/plans/08-1-breezy-pine.md` §Approach Wave 2 Plan 08.1-04 literal 이식.
</background>

<context>
@.planning/phases/08-discussion-board/08-CONTEXT.md
@server/src/routes/discussions.ts
@server/src/config.ts
@scripts/deploy-discussion-sync.sh
@workers/discussion-sync/src/classify/anthropic.ts
@workers/discussion-sync/src/classify/prompt.ts
@workers/discussion-sync/src/classify/classifyOne.ts
@workers/discussion-sync/src/classify/classifyBatch.ts
@workers/discussion-sync/src/classify/persistRelevance.ts
</context>

<tasks>

<task id="T-01" type="auto">
  <name>Task 1: server-side discussion-classify 서비스 1회 복제 + 환경변수 추가</name>
  <files>
    server/src/services/discussion-classify.ts
    server/src/config.ts
    server/package.json
  </files>
  <read_first>
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/classify/anthropic.ts
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/classify/prompt.ts
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/classify/classifyOne.ts
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/classify/classifyBatch.ts
    - /Users/alex/repos/gh-radar/workers/discussion-sync/src/classify/persistRelevance.ts
    - /Users/alex/repos/gh-radar/server/src/config.ts
    - /Users/alex/repos/gh-radar/server/package.json
  </read_first>
  <action>
1. `server/package.json` dependencies 에 `"@anthropic-ai/sdk": "^0.65.0"` + `"p-limit": "^7.0.0"` (이미 있으면 skip) 추가. `pnpm install` 실행.

2. `server/src/config.ts` 에 `anthropicApiKey: string | null` + `classifyConcurrency: number` + `classifyModel: string` 필드 추가 (없으면 null — refresh 경로에서 분류 skip). 환경변수 매핑:
   ```ts
   anthropicApiKey: process.env.ANTHROPIC_API_KEY ?? null,
   classifyConcurrency: Number(process.env.DISCUSSION_CLASSIFY_CONCURRENCY ?? "5"),
   classifyModel: process.env.DISCUSSION_CLASSIFY_MODEL ?? "claude-haiku-4-5",
   ```

3. `server/src/services/discussion-classify.ts` 신규 파일 — workers/discussion-sync/src/classify/ 의 5 파일 로직을 단일 파일에 압축 복제. 파일 상단에 TODO:
   ```ts
   /**
    * Phase 08.1 — server-side inline classify service.
    *
    * ⚠️ TEMP DUPLICATION: 이 로직은 workers/discussion-sync/src/classify/ 와 동일.
    * 향후 packages/discussion-classify 로 이관 예정 (approved plan Plan 08.1-04 §최소침입 방식).
    * 수정 시 worker 쪽과 drift 되지 않도록 주의.
    *
    * export: classifyAndPersist(supabase, rows) — {id,title,body}[] → Map<id,label>
    *  → 동일한 고정 system prompt + few-shot + max_tokens=10 + temperature=0 + p-limit(5)
    *  → Supabase UPDATE discussions SET relevance=$1, classified_at=now() WHERE id=$2
    */
   ```

   export 두 개:
   ```ts
   export type Label = 'price_reason' | 'theme' | 'news_info' | 'noise';

   /** 분류 + DB UPDATE 를 한 번에 — 성공 건수 반환. 실패하면 해당 id 의 relevance 미업데이트. */
   export async function classifyAndPersist(
     supabase: SupabaseClient,
     rows: Array<{ id: string; title: string; body: string | null }>,
   ): Promise<number>;
   ```

   CLASSIFY_SYSTEM_PROMPT / few-shot 내용은 workers 쪽과 **문자열 단위로 동일**. (drift 방지 — 최소 Minimal 복제.)

4. ANTHROPIC_API_KEY 가 없을 때 graceful no-op:
   - `config.anthropicApiKey` 가 null 이면 `classifyAndPersist` 가 즉시 return 0 + `logger.warn({}, "anthropic key missing — classify skipped")`.
   - 이렇게 하면 로컬 dev 환경에서 ANTHROPIC_API_KEY 없어도 refresh 엔드포인트가 500 터지지 않음.
  </action>
  <acceptance_criteria>
    - `test -f server/src/services/discussion-classify.ts`
    - `grep -q "classifyAndPersist" server/src/services/discussion-classify.ts`
    - `grep -q "max_tokens: 10" server/src/services/discussion-classify.ts`
    - `grep -q "temperature: 0" server/src/services/discussion-classify.ts`
    - `grep -q "TEMP DUPLICATION" server/src/services/discussion-classify.ts`
    - `grep -q "anthropicApiKey" server/src/config.ts`
    - `grep -q "@anthropic-ai/sdk" server/package.json`
    - `pnpm --filter server typecheck` exit 0
  </acceptance_criteria>
  <done>
    서버에 classify 서비스 생성. anthropicApiKey 없을 때 graceful skip.
  </done>
</task>

<task id="T-02" type="auto" tdd="true">
  <name>Task 2: POST /refresh 핸들러에 inline 분류 훅 삽입 + 통합 테스트</name>
  <files>
    server/src/routes/discussions.ts
    server/src/__tests__/discussions.refresh.classify.test.ts
  </files>
  <read_first>
    - /Users/alex/repos/gh-radar/server/src/routes/discussions.ts
    - /Users/alex/repos/gh-radar/server/src/services/discussion-classify.ts
  </read_first>
  <behavior>
    - Test 1: POST /refresh 가 success path 에서 upsert 직후 `classifyAndPersist` 호출 (spy)
    - Test 2: ANTHROPIC_API_KEY 없을 때 refresh 는 여전히 200 반환, classify 는 no-op
    - Test 3: refresh 응답의 Discussion[] 중 신규 행이 relevance=(mock 라벨) 로 반환됨 (classify 후 다시 24h top 5 select 하므로)
  </behavior>
  <action>
1. `server/src/routes/discussions.ts` 의 POST /refresh 핸들러에서 upsert 직후(line 414 근처) + 24h select(line 417) 사이에 훅 삽입:
   ```ts
   // 5.5) Phase 08.1 — inline classify: 방금 upsert 된 신규 행만 분류.
   // upsert().select('id,title,body,relevance,classified_at') 를 사용하도록 변경 필요:
   const { data: upserted, error: upErr } = await supabase
     .from("discussions")
     .upsert(upsertRows, { onConflict: "stock_code,post_id", ignoreDuplicates: false })
     .select("id,title,body,classified_at");
   if (upErr) throw upErr;
   const unclassified = ((upserted ?? []) as Array<{id:string;title:string;body:string|null;classified_at:string|null}>)
     .filter((r) => r.classified_at == null)
     .map((r) => ({ id: r.id, title: r.title, body: r.body }));
   if (unclassified.length > 0) {
     try {
       const n = await classifyAndPersist(supabase, unclassified);
       logger.info({ code, classified: n, attempted: unclassified.length }, "discussion refresh inline classify");
     } catch (e) {
       logger.warn({ code, err: (e as Error).message }, "discussion refresh classify failed — non-fatal");
     }
   }
   ```
   기존 line 406~414 의 `if (upsertRows.length > 0) { ... upsert ... }` 블록을 위 select 확장으로 교체. unclassified 가 빈 배열이면 classify 호출 skip. classify 실패는 non-fatal — refresh 응답은 그대로 성공 반환.

2. 파일 상단 import 추가: `import { classifyAndPersist } from "../services/discussion-classify.js";`

3. `server/src/__tests__/discussions.refresh.classify.test.ts` — 통합 테스트 3개 작성. `classifyAndPersist` 는 `vi.mock('../services/discussion-classify', ...)` 로 spy. Supabase mock 은 기존 refresh 테스트 패턴 복제 (Bright Data client mock 포함).

예:
```ts
import { describe, it, expect, vi } from 'vitest';
vi.mock('../services/discussion-classify', () => ({
  classifyAndPersist: vi.fn().mockResolvedValue(2),
}));
// ... createApp + supertest + Bright Data mock 하여 POST /refresh 호출
// classifyAndPersist 가 호출됐는지 spy 검증
```
  </action>
  <acceptance_criteria>
    - `grep -q "classifyAndPersist" server/src/routes/discussions.ts`
    - `grep -q 'select("id,title,body,classified_at")' server/src/routes/discussions.ts`
    - `test -f server/src/__tests__/discussions.refresh.classify.test.ts`
    - `pnpm --filter server test -- discussions.refresh.classify` exit 0 (3 PASS)
  </acceptance_criteria>
  <done>
    refresh 핸들러가 upsert 직후 분류를 비동기로 수행 (실패는 non-fatal). 3 통합 테스트 PASS.
  </done>
</task>

<task id="T-03" type="checkpoint:human-action">
  <name>Task 3: GCP Secret `gh-radar-anthropic-api-key` 생성 + SA accessor grant</name>
  <files>-</files>
  <read_first>
    - /Users/alex/.claude/projects/-Users-alex-repos-gh-radar/memory/MEMORY.md
    - /Users/alex/repos/gh-radar/scripts/setup-discussion-sync-iam.sh
  </read_first>
  <action>
**[BLOCKING HUMAN]** — 이미 credentials 가 어디에 있는지 먼저 확인 (memory: feedback_dont_ask_existing_creds.md):
1. `grep -r ANTHROPIC_API_KEY /Users/alex/repos/gh-radar/*.env* /Users/alex/repos/gh-radar/**/.env* 2>/dev/null | head` — 로컬 `.env` 에 이미 있으면 그 값을 재사용.
2. `gcloud secrets list --project=gh-radar | grep anthropic` — 이미 존재하면 step 3 skip.

3. Secret 생성 (없을 때만):
   ```bash
   export CLOUDSDK_CORE_PROJECT=gh-radar
   gcloud secrets create gh-radar-anthropic-api-key --replication-policy="automatic"
   printf "%s" "$ANTHROPIC_API_KEY" | gcloud secrets versions add gh-radar-anthropic-api-key --data-file=-
   ```

4. SA accessor grant (2개 SA — discussion-sync + server):
   ```bash
   for SA in gh-radar-discussion-sync-sa gh-radar-server-sa; do
     gcloud secrets add-iam-policy-binding gh-radar-anthropic-api-key \
       --member="serviceAccount:${SA}@gh-radar.iam.gserviceaccount.com" \
       --role="roles/secretmanager.secretAccessor"
   done
   ```
   ※ server SA 이름은 scripts/setup-server-iam.sh 에 정의된 실제 이름으로 치환 (대부분 `gh-radar-server-sa`).

5. (옵션) `scripts/setup-discussion-sync-iam.sh` 파일도 업데이트 — Secret 생성 + accessor grant 2건을 idempotent 하게 추가. 향후 setup 재실행 시 일관성 유지.

**검증:**
```bash
gcloud secrets describe gh-radar-anthropic-api-key
gcloud secrets get-iam-policy gh-radar-anthropic-api-key --format=json | jq '.bindings[] | select(.role=="roles/secretmanager.secretAccessor") | .members'
# 출력에 gh-radar-discussion-sync-sa@... 및 gh-radar-server-sa@... 포함 필수
```
  </action>
  <acceptance_criteria>
    - `gcloud secrets describe gh-radar-anthropic-api-key --project=gh-radar` exit 0
    - accessor 바인딩에 discussion-sync-sa + server-sa 2개 모두 존재
    - 사용자가 "secret + accessor 생성 확인" 메시지 제공
  </acceptance_criteria>
  <done>
    GCP Secret Manager 에 ANTHROPIC_API_KEY 저장 + 2 SA accessor 부여 완료. deploy 스크립트가 `--set-secrets` 참조 가능 상태.
  </done>
</task>

<task id="T-04" type="auto">
  <name>Task 4: deploy 스크립트 2종 업데이트 — discussion-sync Job + server Service</name>
  <files>
    scripts/deploy-discussion-sync.sh
    scripts/deploy-server.sh
  </files>
  <read_first>
    - /Users/alex/repos/gh-radar/scripts/deploy-discussion-sync.sh
    - /Users/alex/repos/gh-radar/scripts/deploy-server.sh
  </read_first>
  <action>
1. **`scripts/deploy-discussion-sync.sh`** 수정:
   - line 143 `--task-timeout=600` → `--task-timeout=1800` (Claude 병렬 호출 여유, approved plan Wave 2 08.1-03).
   - line 148 `--set-secrets="SUPABASE_SERVICE_ROLE_KEY=gh-radar-supabase-service-role:latest,BRIGHTDATA_API_KEY=gh-radar-brightdata-api-key:latest"` 를 다음으로 교체:
     ```
     --set-secrets="SUPABASE_SERVICE_ROLE_KEY=gh-radar-supabase-service-role:latest,BRIGHTDATA_API_KEY=gh-radar-brightdata-api-key:latest,ANTHROPIC_API_KEY=gh-radar-anthropic-api-key:latest"
     ```
   - Section 1 가드의 secret check loop 에 `gh-radar-anthropic-api-key` 추가:
     ```bash
     for SECRET in gh-radar-supabase-service-role gh-radar-brightdata-api-key gh-radar-anthropic-api-key; do
     ```

2. **`scripts/deploy-server.sh`** 수정:
   - `--set-secrets=...` 에 동일하게 `ANTHROPIC_API_KEY=gh-radar-anthropic-api-key:latest` 추가 (기존 secret 목록 유지).
   - secret pre-check loop 에도 추가.
   - server 쪽은 Cloud Run **Service** 이므로 `--task-timeout` 대신 `--timeout` (request) 은 변경 불필요 — refresh 경로는 Bright Data + Claude 총 ~5~10초 내 완료 목표.
  </action>
  <acceptance_criteria>
    - `grep -q "task-timeout=1800" scripts/deploy-discussion-sync.sh`
    - `grep -q "ANTHROPIC_API_KEY=gh-radar-anthropic-api-key:latest" scripts/deploy-discussion-sync.sh`
    - `grep -q "gh-radar-anthropic-api-key" scripts/deploy-server.sh`
    - `bash -n scripts/deploy-discussion-sync.sh` exit 0 (syntax check)
    - `bash -n scripts/deploy-server.sh` exit 0
  </acceptance_criteria>
  <done>
    deploy 스크립트 2종이 ANTHROPIC_API_KEY secret 을 마운트하고 discussion-sync 의 task-timeout 이 1800s 로 상향됨.
  </done>
</task>

<task id="T-05" type="checkpoint:human-verify">
  <name>Task 5: [BLOCKING] production 재배포 + gcloud run jobs execute 실측</name>
  <what-built>
    - Server Cloud Run Service 재배포 (ANTHROPIC_API_KEY 마운트)
    - Cloud Run Job `gh-radar-discussion-sync` 재배포 (ANTHROPIC_API_KEY 마운트 + task-timeout=1800)
    - 로컬 `POST /api/stocks/005930/discussions/refresh` 동작 확인
  </what-built>
  <how-to-verify>
1. **로컬 smoke:**
   ```bash
   export ANTHROPIC_API_KEY=...  # .env 에 이미 있다면 pnpm --filter server dev 가 읽음
   pnpm --filter server dev &  # 또는 기존 dev.sh 기준 PORT 사용
   curl -X POST http://localhost:3001/api/stocks/005930/discussions/refresh | jq '.[0] | {postId, title, relevance, classifiedAt}'
   # 기대: relevance 가 price_reason/theme/news_info/noise 중 하나 (또는 null 단 mock 없을 때), classifiedAt 세팅
   ```

2. **server Cloud Run Service 재배포:**
   ```bash
   bash scripts/deploy-server.sh
   ```

3. **discussion-sync Job 재배포:**
   ```bash
   bash scripts/deploy-discussion-sync.sh
   ```

4. **production 1회 실행:**
   ```bash
   gcloud run jobs execute gh-radar-discussion-sync \
     --region=asia-northeast3 --project=gh-radar --wait
   gcloud logging read 'resource.type=cloud_run_job AND resource.labels.job_name=gh-radar-discussion-sync' \
     --project=gh-radar --limit=40 --format='value(textPayload)' | grep -E "classifyBatch|totalClassified"
   ```
   기대 로그: `classifyBatch done { total: X, classified: Y }` 및 cycle 종료 summary `totalClassified > 0`.

5. **Supabase 분포 확인:**
   ```sql
   SELECT relevance, count(*) FROM discussions
     WHERE classified_at IS NOT NULL
     GROUP BY relevance ORDER BY 2 DESC;
   ```
   4 라벨 중 최소 3개 이상 표시되어야 함.

**실패 분기:**
- `403 secret access denied` → T-03 의 accessor grant 누락 → `gcloud secrets add-iam-policy-binding ...` 재실행.
- `task timeout exceeded` → task-timeout 상향이 반영되지 않음 → `gcloud run jobs describe` 로 확인.
- classify=0 → ANTHROPIC_API_KEY 환경변수 주입 누락 → `gcloud run jobs describe | grep ANTHROPIC` 로 확인.
  </how-to-verify>
  <resume-signal>"deploy + execute 확인 완료" 라고 응답하거나 문제 상세 보고</resume-signal>
</task>

</tasks>

<verification>
```bash
pnpm --filter server test
pnpm --filter server build
bash -n scripts/deploy-discussion-sync.sh
bash -n scripts/deploy-server.sh
# T-05 완료 후:
# gcloud run jobs describe gh-radar-discussion-sync --region=asia-northeast3 --project=gh-radar --format='value(spec.template.spec.taskTimeout)' → "1800s"
# gcloud run jobs describe gh-radar-discussion-sync --region=asia-northeast3 --project=gh-radar --format='value(spec.template.spec.template.spec.containers[0].env)' | grep ANTHROPIC
```
</verification>

<success_criteria>
- server/src/services/discussion-classify.ts 생성 + refresh 핸들러 훅 삽입 + 3 테스트 PASS
- GCP Secret `gh-radar-anthropic-api-key` 생성 + 2 SA accessor grant
- deploy-discussion-sync.sh (task-timeout=1800, ANTHROPIC_API_KEY mount), deploy-server.sh (ANTHROPIC_API_KEY mount)
- production Cloud Run Job 실행 시 classify 로그 + DB 분포 4 라벨 확인
- DISC-01.1 의 "on-demand 경로 + 프로덕션 배포" 완료
</success_criteria>

<commit_hint>
commit message: `feat(server,infra): Phase 08.1 Plan 04 — server refresh inline classify + GCP Secret 배포 (DISC-01.1 on-demand 경로)`
</commit_hint>

<output>
T-01~T-05 결과 + gcloud describe 출력(env/task-timeout/secret) + DB 분포 스냅샷을 SUMMARY 재료로.
</output>
