---
phase: quick-260905-u9b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "ROADMAP.md 상단 `## Phases` 체크리스트에 Phase 12·13·14(완료)·15(진행중) 항목이 있다"
    - "`## Progress` 의 Execution Order 가 1 → … → 15 까지 이어진다"
    - "Progress 표에 Phase 15 행(4/20, In Progress)이 있다"
    - "Phase 12~15 상세 섹션이 `## Phase Details` 안(= `## Progress` 앞)에 위치한다"
    - "15-02·15-06 이 [x] 이고 Phase 15 집계가 4/20 이다 (SUMMARY 존재 기준)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 1~15 정합 로드맵 (목록·순서·표·상세 위치·Phase 15 진행도)"
  key_links: []
---

<objective>
ROADMAP.md 가 두 곳(상단 체크리스트/Progress 표 vs 파일 끝 append 된 상세 섹션)에서 따로 관리되다 어긋난 것을 한 번에 복구한다.
Phase 12~15 는 `## Progress` 뒤에 append 돼 상단 목록·실행 순서에서 빠졌고, Progress 표는 14 까지만, Phase 15 plan 체크는 15-02·15-06 이 누락돼 있다.
</objective>

<context>
- 원인: Phase 12~15 등록 시 `### Phase N` 상세가 파일 끝에 append 됨(`## Progress` 뒤). 상단 목록·Execution Order 는 미갱신. Progress 표는 phase 완료 시마다 갱신돼 14 까지 있음.
- 진행도 근거: `.planning/phases/15-*/` 에 15-01·15-02·15-06·15-11 SUMMARY 존재 → 4/20. (태스크 착수 시점엔 3/20 이었으나 실행 중 15-06 이 완료됨 — 커밋 1cd1a4e)
- 동시 실행 주의: 다른 세션의 executor worktree(`agent-ae587a846afd9079e`, locked) 가 Phase 15 를 병렬 실행 중. worktree merge 흐름은 ROADMAP.md 를 main 백업으로 되돌리므로 이 태스크는 **main tree 에서 직접 편집** (worktree 격리 금지). Phase 15 plan 줄 텍스트는 바꾸지 않아(체크박스만) 다른 세션의 Edit 앵커가 깨지지 않는다.
- STATE.md 의 phase 진행 수치(Plan 1 of 20, 88/102)는 execute-phase 오케스트레이터 소유 → 이 태스크 범위 밖.
</context>

<tasks>

<task type="auto">
  <name>Task 1: ROADMAP.md 정합성 복구 (단일 스크립트, 앵커 1회 매치 강제)</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    node 스크립트로 아래 6개 편집을 원자적으로 적용. 각 문자열 앵커는 정확히 1회 매치해야 하며 아니면 throw.
    1. 상단 체크리스트: Phase 11 줄 뒤에 Phase 12·13·14 `[x]`(완료일 포함) + Phase 15 `[ ]`(in progress, 4/20) 4줄 삽입.
    2. Execution Order: `… → 11` → `… → 11 → 12 → 13 → 14 → 15`.
    3. Progress 표: 14 행 뒤 `| 15. DMA 중계 서버(relay) | 4/20 | In Progress | — |` 삽입.
    4. `- [ ] 15-02-PLAN.md` → `[x]`, `- [ ] 15-06-PLAN.md` → `[x]`.
    5. `**Plans:** 2/20 plans executed` → `4/20`.
    6. `### Phase 12:` 부터 파일 끝까지 블록을 잘라 `## Progress` 직전에 삽입. 삼중 개행은 이중으로 정리.
  </action>
  <verify>
    grep 으로 상단 12~15 항목·Execution Order·Progress 15행·15-02/15-06 [x]·4/20 확인; `### Phase 12~15` 줄번호 < `## Progress` 줄번호; 줄 수 = 619 + 5 = 624.
  </verify>
  <done>위 verify 전부 통과 + ROADMAP.md 단독 커밋.</done>
</task>

</tasks>

<verification>
- `grep -n '^## \|^### Phase 1[2-5]' .planning/ROADMAP.md` 에서 Phase 12~15 가 `## Phase Details` 와 `## Progress` 사이.
- `git diff --stat` 이 ROADMAP.md 1개 파일만.
</verification>

<success_criteria>
- must_haves.truths 5개 전부 참.
</success_criteria>

<output>
`.planning/quick/260905-u9b-roadmap-md-phases-execution-order-progre/260905-u9b-SUMMARY.md`
</output>
