---
phase: quick-260923-ge2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - relay/src/generated/StockDMA.fbs
  - relay/src/generated/stock-dma/subscribe-quote-req.ts
  - relay/src/dma/envelope.ts
  - relay/src/hub/subscription-hub.ts
  - relay/src/ws/protocol.ts
  - relay/src/ws/fanout.ts
  - relay/tests/hub.test.ts
  - relay/tests/fanout.test.ts
  - relay/tests/protocol.test.ts
  - relay/tests/helpers/ws-client.ts
  - packages/shared/src/relay.ts
  - webapp/src/lib/use-relay-socket.ts
  - webapp/src/lib/use-breakout-quotes.ts
  - webapp/src/lib/__tests__/relay-socket.test.ts
  - webapp/src/lib/__tests__/use-breakout-quotes.test.tsx
autonomous: true
requirements: [GE2-1, GE2-2, GE2-3]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "relay 가 게이트웨이로 보내는 SubscribeQuoteReq(29) 에 level 바이트가 실린다. 어떤 종목에 PRICE 소비자만 있으면 0→1 전이는 28 → 29(level=1) 두 프레임이고 32 는 나가지 않는다. FULL 소비자가 하나라도 있으면 종전대로 28 → 29(level=0) → 32 다 (회신 §할 일 2·4 · GE2-1)"
    - "같은 (user,isin,ex) 에서 PRICE→FULL 승격은 28 → 29(0) → 32 를 다시 보내고, FULL→PRICE 강등은 29(level=1) 한 건만 보내며, 마지막 소비자 이탈은 29(subscribe=false) 한 건이다. resubscribeAll 은 실효 level 로 되건다. 참조계수 합계가 승격·강등 도중 0 을 지나지 않는다 (GE2-1)"
    - "브라우저 sub 프레임은 선택 필드 lv(full|price · 기본 full) 를 가진다. 같은 소켓·같은 키의 재 sub 은 같은 level 이면 종전처럼 무시되고 다른 level 이면 그 소켓의 level 을 갱신한다(참조계수는 늘지 않는다). unsub 은 level 무관 (GE2-2)"
    - "소켓이 price 로 잡은 키에는 tape 프레임(200ms 증분 배치 · 구독 직후 링버퍼 스냅샷)이 가지 않는다 — 게이트웨이 가동본이 level 을 모르는 구버전이라 71 이 흘러와도, 같은 키를 다른 탭이 FULL 로 봐도 마찬가지다. q 프레임은 호가 배열까지 그대로 간다. price→full 승격 시 그 소켓은 링버퍼 tape 스냅샷을 받는다 (GE2-2)"
    - "webapp useRelayContext().subscribe/unsubscribe 는 세 번째 인자 level(기본 'full') 을 받고 키당 {full, price} 두 카운트를 센다. 실효 level(full ≥1 이면 full, 아니면 price) 이 바뀌면 같은 키로 sub 을 다시 보내고, flushSubscriptions(인증 ACK · 재접속) 도 실효 level 로 보낸다. full 프레임은 종전과 같은 모양({t,isin,ex} — lv 없음) 이라 기존 호출부·기존 테스트가 무변경으로 통과한다 (GE2-3)"
    - "useBreakoutQuotes 만 'price' 로 구독·해제한다. 카드(FULL) 와 돌파 칩(PRICE) 이 같은 종목을 보면 와이어는 full 1벌이고, 카드가 닫히면 price 로 강등 재송신된다 (회신 §할 일 5 · GE2-3)"
    - "relay vitest 전량 · webapp vitest 전량 · shared build 후 relay/webapp typecheck · Playwright trading-workbench+orderbook 이 통과한다. 생성 코드 2파일은 경로 지정으로만 커밋되고 다른 세션의 미커밋 파일은 건드리지 않았다. SUMMARY 에 「관찰 사항」(69/71 중복 제거 유무) 과 「배포 후 확인 절차」 가 있다 (회신 §할 일 1·6)"
  artifacts:
    - path: "relay/src/dma/envelope.ts"
      provides: "QUOTE_LEVEL {FULL:0, PRICE:1} 상수 · buildSubscribeQuoteReq 5번째 인자 level(기본 FULL)"
      contains: "QUOTE_LEVEL"
    - path: "relay/src/hub/subscription-hub.ts"
      provides: "키당 {full, price} 참조계수 · 실효 level 산출 · subscribe/unsubscribe(level) 전이 규칙 · #sendSubscribe(level) · resubscribeAll 실효 level · subscriptionLevel() 진단"
      contains: "subscriptionLevel"
    - path: "relay/src/ws/protocol.ts"
      provides: "RelaySubSchema.lv (z.enum full|price, optional)"
      contains: "lv:"
    - path: "relay/src/ws/fanout.ts"
      provides: "Conn.keys 값에 lv · sub 재수신 level 갱신 · price 키 tape 캐시 생략 · #deliver 의 price 키 tape 차단 · close 의 level 별 unsubscribe"
      contains: "lv"
    - path: "packages/shared/src/relay.ts"
      provides: "RelaySubLevel = 'full' | 'price' · RelaySubMsg.lv?"
      contains: "RelaySubLevel"
    - path: "webapp/src/lib/use-relay-socket.ts"
      provides: "subscribe/unsubscribe(isin, exchange, level='full') · subRefs {full, price} · wireSubs {lv} · flushSubscriptions 실효 level 재송신"
      contains: "RelaySubLevel"
    - path: "webapp/src/lib/use-breakout-quotes.ts"
      provides: "돌파 칩 구독을 price level 로"
      contains: "price"
  key_links:
    - from: "webapp/src/lib/use-breakout-quotes.ts"
      to: "webapp/src/lib/use-relay-socket.ts (subscribe 3번째 인자)"
      via: "subscribe(isin, 'KRX', 'price') → subRefs.price += 1 → flushSubscriptions → {t:'sub', isin, ex, lv:'price'}"
      pattern: "lv"
    - from: "relay/src/ws/fanout.ts (sub 처리)"
      to: "relay/src/hub/subscription-hub.ts subscribe(userId, isin, ex, level)"
      via: "msg.lv ?? 'full' → conn.keys {lv} → hub.subscribe(level) / 재 sub 은 hub.subscribe(new) 뒤 hub.unsubscribe(old)"
      pattern: "hub.subscribe"
    - from: "relay/src/hub/subscription-hub.ts #sendSubscribe"
      to: "relay/src/dma/envelope.ts buildSubscribeQuoteReq(isin, exchange, true, level)"
      via: "실효 level → QUOTE_LEVEL 바이트 → 29 프레임 vtable 슬롯 10"
      pattern: "buildSubscribeQuoteReq"
    - from: "relay/src/hub/subscription-hub.ts #flush (tape 팬아웃)"
      to: "relay/src/ws/fanout.ts #deliver"
      via: "msg.t === 'tape' ∧ conn.keys.get(keyOf(msg.i, msg.x))?.lv === 'price' → 그 소켓 건너뜀"
      pattern: "tape"
---

<objective>
가벼운 시세 구독(`SubscribeQuoteReq.level` = PRICE) 을 gh-radar 전 구간(게이트웨이 계약 → relay 업스트림 → relay 브라우저 표면 → webapp) 에 적용한다. `/trading` CPU 1단계(quick-260923-elb) 의 남은 과제 **2b** 다 — 돌파 칩 최대 40종목이 지금은 호가창과 똑같은 FULL 구독이라 10호가 59 · 체결 71 · 거래원 75 가 종목마다 전부 흘러온다. 칩이 읽는 것은 현재가·등락률뿐이다.

**계약 정본은 `tasks/gh-trade-price-only-quote-subscription-reply.md`(gh-trade 회신, quick-260923-exo) 다.** 요청서(`...-prompt.md`) 와 어긋나면 회신이 이긴다 — PRICE 59 는 호가 배열을 비우지 않고, 구독 직후 서버 스냅샷은 (원래부터) 없으며 28/58 이 담당한다. 회신 「gh-radar 가 할 일」 1~6 이 범위이고 이 plan 의 requirement id 로 묶는다:
- **GE2-1** (회신 1·2·3·4): 생성 코드 2파일 커밋 · `buildSubscribeQuoteReq` level 인자(지금 relay 는 TS2554 로 컴파일이 깨져 있다 — `envelope.ts:399` 4-인자 호출) · 수기 사본 3곳 확인(msg-type 은 MsgType 무변경) · hub 참조계수를 full/price 로 나눠 실효 level 로 29 송신 + 전이 규칙 + `resubscribeAll`.
- **GE2-2** (회신 4 의 브라우저 측 귀결): `sub` 프레임 `lv?: "full"|"price"` · 소켓별 level · 같은 소켓 재 sub 은 level 갱신 · **price 소켓에 tape 를 흘리지 않는 것을 relay 가 자체 보장**(게이트웨이 가동본 bd1c541d 는 level 을 몰라 level=1 에도 FULL 로 보낸다).
- **GE2-3** (회신 5): webapp 참조계수 level 합성 + `useBreakoutQuotes` price 전환. 회신 6(실서버 확인) 은 가동본이 구버전이라 지금 불가 → SUMMARY 「배포 후 확인 절차」 로 남긴다.

**분해 형태(--no-tracer 근거):** 28/29/32 경로는 이미 프로덕션에서 도는 구조이고 이번 변경은 그 위에 필드 하나와 참조계수 분기를 얹는 것이다 — 얇은 관통 슬라이스가 새로 알려 줄 아키텍처 정보가 없다. 대신 계층별 3 태스크로 나누고 **각 태스크 끝에서 컴파일·테스트가 초록인 상태로 커밋**한다(오케스트레이터 지시). 순서는 업스트림 → 브라우저 표면 → webapp 이라 배포 순서(relay 먼저)와도 같다.

Purpose: 돌파 40종목의 59 를 체결 때만(종목당 ≤5Hz), 71·75 는 0 으로 — 게이트웨이→relay·relay→브라우저 두 구간 모두에서 프레임 개수를 근본적으로 줄인다. 호가창 카드가 열린 종목은 FULL 그대로여야 한다.
Output: 코드 커밋 3건(Task 별 · 한글 · 명시 경로 · push 없음 · Co-Authored-By 없음) + SUMMARY(관찰 사항 · 배포 후 확인 절차).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@tasks/gh-trade-price-only-quote-subscription-reply.md
@tasks/gh-trade-price-only-quote-subscription-prompt.md
@.planning/quick/260923-elb-cpu-1/260923-elb-SUMMARY.md

@relay/src/generated/StockDMA.fbs
@relay/src/generated/stock-dma/subscribe-quote-req.ts
@relay/src/dma/envelope.ts
@relay/src/hub/subscription-hub.ts
@relay/src/ws/protocol.ts
@relay/src/ws/fanout.ts
@packages/shared/src/relay.ts
@webapp/src/lib/use-relay-socket.ts
@webapp/src/lib/use-breakout-quotes.ts

<interfaces>
<!-- 실행자가 다시 탐색하지 않도록 옮겨 둔다. 줄 번호는 HEAD 62d8a77 기준이다. 작업 트리 상태(플랜 시점 실측): `relay/src/generated/` 2파일만 M, 그 외 미추적 = .planning/quick 3 디렉터리 + tasks/*.md 2건(다른 세션 소유 — 건드리지 않는다). -->

작업 트리 / 게이트 명령
- 생성 코드: `relay/src/generated/StockDMA.fbs` SYNC MARKER `server-repo-commit: 228ea1f5` · `synced-date: 2026-09-23`. `SubscribeQuoteReq` 주석 블록 L829-838 + `level: ubyte = 0` L843. `subscribe-quote-req.ts`: `level(): number`(슬롯 10, 기본 0) L44 · `addLevel` L65 · `createSubscribeQuoteReq(builder, isinOffset, exchangeOffset, subscribe, level)` L74 — **5 인자**.
- 현재 `pnpm --filter @gh-radar/relay run typecheck` → `src/dma/envelope.ts(399,33): error TS2554: Expected 5 arguments, but got 4.` (플랜 시점 실측).
- 패키지 이름: `@gh-radar/shared` · `@gh-radar/relay` · `@gh-radar/webapp`. shared 는 `dist` 로 소비된다 — **계약을 바꾼 뒤 `pnpm --filter @gh-radar/shared build` 를 먼저** 돌려야 relay/webapp typecheck 가 새 타입을 본다(STATE.md 16-41 함정).
- relay: `run typecheck`(tsc) · `run typecheck:tests`(tsconfig.tests.json) · `run test`(vitest run) · 파일 단위 `exec vitest run tests/hub.test.ts`. webapp: `run typecheck`(src + e2e) · `run test`(vitest --run) · `exec vitest --run <경로>` · `exec playwright test trading-workbench orderbook`(실 relay 8090 + 스텁 게이트웨이 · dev 3100 — 두 포트가 비어 있어야 한다, elb SUMMARY).
- git 은 **레포 루트에서만** 친다(`webapp/` 안에 중첩 `.git` 이 있다 — elb Deviation 5). `git add -A` 금지 · 커밋 직전 `git status --short` 로 stage 대상 재확인(동시 세션 커밋 경합 메모).

relay/src/dma/envelope.ts
- `buildGetQuoteReq(isin, exchange)` L382 · `buildSubscribeQuoteReq(isin, exchange, subscribe)` L393-410(`createSubscribeQuoteReq` 4-인자 호출 L399) · `buildGetTradeTapeReq(isin, exchange, count)` L413. flatbuffers `addFieldInt8(slot, value, default)` 는 값이 기본값과 같으면 필드를 싣지 않는다 → level 0 프레임은 종전과 바이트가 같다.

relay/src/hub/subscription-hub.ts
- 헤더 결정 근거 D-33 L11-13(0→1: 28→29→32 · 1→0: 29 false · 체결은 별도 구독 없음) · D-35 L14-16. `HubSession {userId, isReady, send(Uint8Array), on(frame|ready)}` L128-137.
- `subKey(userId, isin, exchange)` L201 · `userPrefix` L206 · `#refs = new Map<string, number>()` L324 · `#tapes` L328 · `#pending`/`#flushTimers` L330-332.
- `subscribe(userId, isin, exchange)` L482-495(next>1 → "[HUB] 이미 구독 중" info · 0→1 → `#sendSubscribe`) · `unsubscribe` L498-525(없는 참조 warn "참조계수 없는 해제 — 무시" · next>0 info "탭 1개 해제 — 구독 유지" · 0 이면 `#refs.delete` → 세션 ready 검사 → `buildSubscribeQuoteReq(isin, exchange, false)`) · `resubscribeAll(userId)` L539-550(`#refs.keys()` 순회 + `#splitKey`) · `getSnapshot` L713 · `getTape` L718(링버퍼 복사본) · `refCount()` L820(진단·테스트) · `stats().subscriptionCount = #refs.size` L828 · `closeAll` L841 · `#onTape` L1417-1440(69 는 링 교체 · 71 은 이어붙임 · **시각·순번 중복 제거 없음** — 플랜 시점 grep 실측) · `#armFlush`/`#flush` L1442-1465(`{t:"tape", i, x, snap, e}` 팬아웃) · `#sendSubscribe(userId, isin, exchange)` L1471-1491(세션 없음 warn · ready 이전 info · 28 → 29(true) → 32 · info "신규 구독 — 스냅샷+구독+체결 요청 송신") · `#onReady` L1493(resubscribeAll 은 여기 한 곳) · `#countKeys` L1560 · `#splitKey` L1571.

relay/src/ws/protocol.ts
- `ExchangeSchema = z.enum(["KRX","NXT"])` L45 · `RelaySubSchema {t:"sub", isin, ex}` L56-60 · `RelayUnsubSchema` L62-66 · `RelayInboundSchema = z.discriminatedUnion("t", [...])` L344 · `parseInbound(raw): RelayInbound | null` L368(safeParse 실패 → null → 호출부 close 4400). zod 기본 strip — `.strict()` 금지 관례.

relay/src/ws/fanout.ts
- 헤더 연결 수명 규칙 7 L22(`sub → 참조계수 +1, 스냅샷 캐시 즉시 전송`) · 8 L23. `Conn.keys: Map<string, {isin; ex}>` L278 · `keyOf(isin, ex)` = `${isin}|${ex}` L315 · `this.#hub.on("fanout", (e) => this.#deliver(e.userId, e.msg))` L375.
- `sub` 처리 L813-832: `conn.keys.has(key)` → info "[WS] 이 소켓의 중복 구독 — 무시" return · `conn.keys.set` · `hub.subscribe` · `getSnapshot` → `#send` · `getTape` 길이>0 → `#send({t:"tape", i, x, snap:true, e})`. `unsub` L834-841.
- `#onClose` L1202-1206: `for ({isin, ex} of conn.keys.values()) hub.unsubscribe(...)`. `#deliver(userId, msg)` L1295-1299: 그 사용자의 **모든** 소켓에 `#send`(키 보유 여부를 보지 않는다 — 브라우저가 키로 거른다, use-relay-socket 규율 7). `#send(conn, msg)` L1306(백프레셔).

packages/shared/src/relay.ts
- `RelaySubMsg = { t:"sub"; isin; ex: RelayExchange }` L397 · `RelayUnsubMsg` L400 · `RelayInbound` 유니온 L571 · `RelayTape {t:"tape"; i; x; snap; e}` L708 · `RelayServerMsg.lv: string` L867 은 **다른 뜻**(54 통지 등급) — 이름 충돌 없음(다른 타입).

webapp/src/lib/use-relay-socket.ts
- 헤더 규율 3 L21-23(`Map<key, count>` 0→1 sub · 1→0 unsub) · 컨텍스트 타입 `subscribe/unsubscribe: (isin, exchange) => void` L369-371 · `subRefsRef: Map<key, {isin, ex, count}>` L933 · `wireSubsRef: Map<key, {isin, ex}>` L937 · `flushSubscriptions` L959-972(`entry.count < 1 || wireSubsRef.has(key)` continue · `ws.send({t:"sub", isin, ex})`) · cleanup 의 unsub 루프 L1205-1207 · `subscribe` L1229-1240 · `unsubscribe` L1242-1258 · 반환 useMemo deps L1344. `relayQuoteKey(isin, exchange)` 가 키.
- `relay-provider.tsx`: `RelayContextValue = Omit<RelayConnectionState, "sendOrder"> & {...}` L141 → subscribe 타입은 use-relay-socket 이 정본. `EMPTY_RELAY_VALUE.subscribe = NOOP` L354(3-인자에도 대입 가능). `useRelaySubscription` L486-496 은 `subscribe(isin, exchange)` 2-인자 — **무변경**.

webapp/src/lib/use-breakout-quotes.ts (229줄)
- 헤더 ①~⑤ L1-32 · `BREAKOUT_EXCHANGE = "KRX"` L59 · `useRelayContext()` 의 `{subscribe, unsubscribe, quotes}` L145 · 구독 diff effect L160-175(`unsubscribe(isin, BREAKOUT_EXCHANGE)` L165 · `subscribe(isin, BREAKOUT_EXCHANGE)` L171) · 언마운트 전량 해제 L178-184(L181).

테스트 하네스
- `relay/tests/hub.test.ts`: `SentReq {msgType, isin, exchange, subscribe, count}` L46-52 · `decodeReq` L54-89(29 는 `env.subscribeQuoteReq()` 되읽기 L69-77) · `FakeSession` L92-123(`sent: SentReq[]` · `isReady` 가변 · `pushFrame` · `emitReady` · `subscribeReqs()`) · ① L149(`session.sent.map(msgType)` 가 `[28,29,32]` · `refCount` 2) · ② L166 · ⑤ L209(재구독 3프레임 세트) · ⑪ L310(ready 이전) · ⑫ L323(참조계수 없는 해제) · `buildTradeTapeFrame({snapshot, entries})` from helpers/frames · `fanout` 이벤트 수집 배열(⑥ L226 참조).
- `relay/tests/fanout.test.ts`: `gatewayMsgTypes`(게이트웨이가 받은 msgType 누적) L220 · `startHarness` L223 · `open()`/`authed(token)` L261-278 · fake timers(setTimeout 등) L281 · ⑥ 탭 2개 공유 L367 · ⑨ close 시 키만 해제 L442 · ⑩ 스키마 위반 close 4400 L466(`ws.sendRaw`) · ⑪ 캐시 즉시 응답 L476. `FakeGateway.pushQuote(sock, {snapshot, lastPrice})` · `pushTape(sock, {isin?, exchange?, snapshot?, entries?})`(helpers/fake-gateway.ts L135-137) · `gateway.sockets[0]`. `USER_A`/`SAMPLE_ISIN` 상수.
- `relay/tests/helpers/ws-client.ts`: `sendSub(isin, ex)` L68 · `sendUnsub` L73 · `sendRaw(payload)` L78.
- `relay/tests/protocol.test.ts`: `parseInbound(JSON.stringify({t:"sub", isin: ISIN, ex:"KRX"}))?.t` L285.
- `webapp/src/lib/__tests__/relay-socket.test.ts`: `FakeWebSocket` L67(`sent` · `parsedSent()` L127 · `accept()` · `push(frame)` · `FakeWebSocket.last()`) · `render()`/`connected(hook)`/`settle()` · 「구독 참조계수」 describe L416-467: ⑥ `toEqual({t:'sub', isin, ex:'KRX'})` **정확 일치** · ⑥-a ACK 이전 보류 · ⑥-b unauthorized · 재접속 describe L469(L518 에 subscribe 후 재접속 재구독 패턴) · cleanup describe L585.
- `webapp/src/lib/__tests__/use-breakout-quotes.test.tsx`: `mockRelay = {...EMPTY_RELAY_VALUE, subscribe, unsubscribe}`(vi.fn) L42-48 · `toHaveBeenCalledWith(A, "KRX")` 단언 L65-66 · L87 · L89 · L96-97 · L191-192(vitest 는 인자 개수까지 본다 → 3번째 인자가 생기면 **갱신 필수**) · `every((c) => c[1] === "KRX")` L131 · **소스 규율 테스트** L135-142: 훅 소스 전체(주석 포함)에 `.send(` 와 `t:\s*["']sub["']` 가 없어야 한다.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: relay 업스트림 level — 생성 코드 2파일 커밋 · envelope level 인자 · hub full/price 참조계수와 전이 규칙 · resubscribeAll 실효 level (GE2-1 · 회신 §할 일 1~4)</name>
  <files>relay/src/generated/StockDMA.fbs, relay/src/generated/stock-dma/subscribe-quote-req.ts, relay/src/dma/envelope.ts, relay/src/hub/subscription-hub.ts, packages/shared/src/relay.ts, relay/tests/hub.test.ts</files>
  <behavior>
    hub.test.ts 의 `SentReq` 에 `level: number | null` 을 더하고 `decodeReq` 의 29 갈래에서 `req?.level() ?? null` 로 되읽는다(다른 갈래는 null). 새 describe 「SubscribeQuoteReq level (quick-260923-ge2)」 — 케이스 L1~L8. 구현 전 RED 를 한 번 관측한다(L1·L3·L4·L6 은 시그니처가 없어 컴파일부터 빨갛다 — 그것으로 충분하다).
    - L1 PRICE 0→1: `hub.subscribe("user-1", SAMPLE_ISIN, "KRX", "price")` → `session.sent.map(msgType)` 이 정확히 `[GetQuoteReq, SubscribeQuoteReq]` 이고 29 는 `{subscribe:true, level:1}`. GetTradeTapeReq 0건. `hub.subscriptionLevel("user-1", SAMPLE_ISIN, "KRX")` = "price".
    - L2 FULL 기본값: `hub.subscribe("user-1", SAMPLE_ISIN, "KRX")` → `[28, 29, 32]` 이고 29 의 `level` 은 0. `subscriptionLevel` = "full". (① 은 무수정 통과 — 이 케이스는 level 바이트를 명시 단언한다.)
    - L3 승격: price 구독 뒤 `sent.length = 0` → `subscribe(..., "full")` → 정확히 `[28, 29(level 0), 32]` 가 **다시** 나간다. `refCount` 2 · `subscriptionLevel` "full".
    - L4 강등과 해제: full 구독 뒤 price 구독 → 추가 프레임 0(여전히 `[28,29,32]` 3건). `unsubscribe(..., "full")` → 정확히 1건 추가 = 29 `{subscribe:true, level:1}`. `refCount` 1 · `subscriptionLevel` "price". `unsubscribe(..., "price")` → 29 `{subscribe:false}` 1건 · `refCount` 0 · `subscriptionLevel` undefined.
    - L5 price 이탈은 무프레임: full+price 뒤 `unsubscribe(..., "price")` → 프레임 추가 0 · "full" 유지.
    - L6 resubscribeAll 실효 level: `SAMPLE_ISIN|KRX` price · `OTHER_ISIN|NXT` full → `sent.length = 0` → `session.emitReady()` → SAMPLE 은 28 → 29(level 1) 이고 32 없음, OTHER 는 28 → 29(level 0) → 32. GetTradeTapeReq 총 1건.
    - L7 level 불일치 해제 무시: full 만 있는 키에 `unsubscribe(..., "price")` → 프레임 0 · `refCount` 1 그대로 (⑫ 와 같은 warn 경로).
    - L8 Ready 이전 승격·강등은 프레임을 내지 않고 ready 가 실효 level 로 복원한다: `isReady=false` 세션에 price → full 승격, 이어 `unsubscribe("full")` 강등 → `sent` 0건 · `subscriptionLevel` "price" → `emitReady()` → `[28, 29(level 1)]`.
    - 기존 ①~⑭ 는 **무수정 통과**해야 한다(`refCount` 는 합계를 돌려준다).
  </behavior>
  <action>
    **0. 트리 확인.** `git status --short` 로 `relay/src/generated/` 2파일만 M 이고 나머지는 미추적임을 확인한다. `git diff -- relay/src/generated/StockDMA.fbs` 에서 SYNC MARKER 가 `228ea1f5 / 2026-09-23` 인지, `SubscribeQuoteReq` 에 `level: ubyte = 0` 이 append 됐는지 본다. 이 2파일은 **내용을 고치지 않는다**(gh-trade `sync-relay-schema.sh` 산출물). `relay/src/dma/msg-type.ts` 는 MsgType 변경이 없어 손대지 않는다 — `grep -n "SubscribeQuoteReq: 29" relay/src/dma/msg-type.ts` 로 확인만 하고 SUMMARY 에 「수기 사본 3곳 중 msg-type 무변경 확인」 한 줄을 남긴다(회신 §할 일 3).

    **1. packages/shared/src/relay.ts — level 어휘.** 인바운드 구역(`RelayAuthMsg` 근처) 에 `export type RelaySubLevel = "full" | "price"` 를 추가한다. JSDoc: 「구독 수준. full = 59+71+75 종전 그대로 / price = 59 만(가격 섹션 갱신 때만 · 키당 ≥200ms · 호가 배열은 그대로 실린다 — 개수 문제이지 크기 문제가 아니다). 정본은 gh-trade 회신 `tasks/gh-trade-price-only-quote-subscription-reply.md`(quick-260923-exo) · fbs `SubscribeQuoteReq.level` 주석.」 `RelaySubMsg` 의 `lv` 필드는 **Task 2** 에서 넣는다(이 태스크는 타입 어휘만). 이후 `pnpm --filter @gh-radar/shared build` 를 돌린다.

    **2. relay/src/dma/envelope.ts — level 바이트.** `buildSubscribeQuoteReq` 위에 `export const QUOTE_LEVEL = { FULL: 0, PRICE: 1 } as const` 와 `export type QuoteLevelByte = (typeof QUOTE_LEVEL)[keyof typeof QUOTE_LEVEL]` 을 둔다. 시그니처를 `buildSubscribeQuoteReq(isin, exchange, subscribe, level: QuoteLevelByte = QUOTE_LEVEL.FULL)` 로 넓히고 `createSubscribeQuoteReq` 5번째 인자로 `level` 을 넘긴다. JSDoc 에 적을 것: level 은 vtable 슬롯 10(append-only · 기존 4/6/8 불변) · 기본 0 이라 FULL 프레임은 종전과 바이트가 같다(flatbuffers 가 기본값 필드를 싣지 않는다) · 해제(`subscribe:false`) 는 level 무관이라 호출부가 level 을 넘기지 않는다 · 서버는 1 만 PRICE 로 보고 그 밖 값은 FULL 로 접는다.

    **3. relay/src/hub/subscription-hub.ts — 참조계수 분리.**
    - `import type { RelaySubLevel } from "@gh-radar/shared"`(기존 shared import 목록에 추가) · envelope 에서 `QUOTE_LEVEL` 도 import.
    - 모듈 상단 헬퍼: `type SubRefs = { full: number; price: number }` · `totalRefs(r)` · `effectiveLevel(r): RelaySubLevel` = `r.full > 0 ? "full" : "price"` · `levelByte(level: RelaySubLevel): QuoteLevelByte` = `"price"` 면 PRICE, **그 밖은 전부 FULL**(모르는 값을 더 가벼운 경로로 열화시키지 않는다 — 서버 규칙과 동형). `#refs` 를 `Map<string, SubRefs>` 로 바꾼다.
    - `subscribe(userId, isin, exchange, level: RelaySubLevel = "full")`: refs 를 얻거나 `{full:0, price:0}` 으로 만들고, `prevTotal`·`prevLevel`(prevTotal>0 일 때만) 을 기억한 뒤 `refs[level] += 1`. prevTotal 0 → `#sendSubscribe(userId, isin, exchange, effectiveLevel(refs))`. prevLevel "price" 이고 새 실효가 "full" → info 「[HUB] PRICE→FULL 승격 — 스냅샷+구독+체결 재송신」 후 `#sendSubscribe(..., "full")`(회신 규칙 2: 서버 스냅샷이 없으므로 0→1 과 같은 순서를 다시 보낸다 · 서버는 같은 키 재구독을 level 덮어쓰기로 처리하고 상한을 두 번 세지 않는다). 그 밖 → 기존 「이미 구독 중」 info(로그 필드에 `level`·`refs` 추가).
    - `unsubscribe(userId, isin, exchange, level: RelaySubLevel = "full")`: refs 가 없거나 `refs[level] <= 0` → 기존 warn 「참조계수 없는 해제 — 무시」(로그에 `level` 추가) return. `prevLevel = effectiveLevel(refs)`; `refs[level] -= 1`. 합계 > 0 이면: 새 실효가 prevLevel 과 다르면(= FULL→PRICE) 세션 ready 검사 후 `session.send(buildSubscribeQuoteReq(isin, exchange, true, QUOTE_LEVEL.PRICE))` **1건** + info 「[HUB] FULL→PRICE 강등 — 29(level=1) 1건」(ready 이전이면 info 만 — ready 의 resubscribeAll 이 실효 level 로 되건다); 같으면 기존 「탭 1개 해제 — 구독 유지」. 합계 0 → 기존 경로 그대로(`#refs.delete` → 29 false).
    - `#sendSubscribe(userId, isin, exchange, level: RelaySubLevel)`: 세션 없음·ready 이전 가드는 그대로. `send(28)` → `send(buildSubscribeQuoteReq(isin, exchange, true, levelByte(level)))` → **level 이 "full" 일 때만** `send(32)`(price 는 71 이 오지 않으므로 69 스냅샷도 요청하지 않는다). 로그에 `level` 필드를 넣고 메시지를 「신규 구독 — 스냅샷+구독(+체결) 요청 송신」 처럼 level 을 드러내게 한다. JSDoc 「0→1 전이의 3프레임」 을 「FULL 3프레임 / PRICE 2프레임」 으로 고친다.
    - `resubscribeAll`: `#refs.entries()` 를 순회해 `#sendSubscribe(userId, parts.isin, parts.exchange, effectiveLevel(refs))`.
    - `refCount()` 는 **합계**를 돌려준다(기존 테스트·fanout.test 의 `refCount === 2` 유지). 진단용 `subscriptionLevel(userId, isin, exchange): RelaySubLevel | undefined` 를 `refCount` 옆에 추가한다(없으면 undefined). `stats().subscriptionCount = #refs.size`·`#countKeys`·`closeAll` 은 그대로.
    - 헤더 D-33 블록 바로 아래에 level 규칙을 잇는다(이 주석이 계약 문서 역할이다 — 오케스트레이터 지시): 참조계수는 키당 {full, price} · 업스트림 level = full ≥1 ? FULL : PRICE · 0→1 PRICE = 28→29(1)(32 없음) · 0→1 FULL = 28→29(0)→32 · PRICE→FULL 승격 = 28→29(0)→32 재송신 · FULL→PRICE 강등 = 29(1) 1건 · 마지막 이탈 = 29(false) · resubscribeAll 은 실효 level · 모르는 level 은 FULL 로 접는다 · 75 거래원도 FULL 전용 · 출처 = gh-trade 회신(quick-260923-exo) 와 fbs 주석 L829-838.
    - **부수 발견 확인(고치지 않는다 — 범위 고정):** `#onTape`(L1417 근처) 를 읽고 69 스냅샷과 뒤따르는 71 사이에 시각·순번 기준 중복 제거가 있는지 확인한다. 플랜 시점 grep 으로는 없다. 없으면 코드를 건드리지 말고 SUMMARY 「관찰 사항」 에 남긴다: 회신 「부수 발견」(테이프 미전송 카운터가 구독 전에도 늘어 첫 71 에 구독 전 최대 100건이 실려 69 와 겹칠 수 있다) · 현재 hub 는 그대로 이어붙인다 · 후속 과제 제안(webapp `tapeRowKeys` 의 entryKey `t|p|q|cv` 와 같은 식별로 링·pending 병합 시 중복 제거).

    **4. 테스트** — behavior 대로. `OTHER_ISIN`·`SAMPLE_ISIN`·`FakeSession`·`TAPE_RING_SIZE` 등 기존 것을 쓴다.

    **5. 게이트 후 커밋 1.** 순서: `pnpm --filter @gh-radar/shared build` → relay `typecheck` · `typecheck:tests` → `exec vitest run tests/hub.test.ts` → 전체 `run test`. 커밋 직전 `git status --short` 재확인 후 **명시 경로 6개만** stage: `relay/src/generated/StockDMA.fbs relay/src/generated/stock-dma/subscribe-quote-req.ts relay/src/dma/envelope.ts relay/src/hub/subscription-hub.ts packages/shared/src/relay.ts relay/tests/hub.test.ts`. 메시지(한글): `feat(quick-260923-ge2): SubscribeQuoteReq level 동기화(gh-trade 228ea1f5) — relay 구독 참조계수를 full/price 로 나눠 실효 level 로 29 송신·승격/강등 재송신`. Co-Authored-By 없음 · push 없음.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/relay exec vitest run tests/hub.test.ts && grep -q 'server-repo-commit: 228ea1f5' relay/src/generated/StockDMA.fbs && grep -q 'QUOTE_LEVEL' relay/src/dma/envelope.ts && grep -q 'subscriptionLevel' relay/src/hub/subscription-hub.ts && grep -q 'RelaySubLevel' packages/shared/src/relay.ts && git diff --quiet -- relay/src/generated/ && git diff --quiet HEAD -- relay/src/generated/</automated>
  </verify>
  <done>relay typecheck 가 TS2554 없이 exit 0 이다. hub.test L1~L8 GREEN(구현 전 RED 관측) · 기존 ①~⑭ 무수정 통과. 생성 코드 2파일이 커밋돼 `git diff HEAD -- relay/src/generated/` 가 비어 있고 `.planning/quick`·`tasks/*.md` 미추적 파일은 그대로 미추적이다. 한글 커밋 1건(명시 경로 · push 없음).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: relay 브라우저 표면 — sub 프레임 lv(full|price) · 소켓별 level 과 재 sub level 갱신 · price 소켓 tape 차단(증분·구독 직후 캐시) · shared 계약 (GE2-2)</name>
  <files>packages/shared/src/relay.ts, relay/src/ws/protocol.ts, relay/src/ws/fanout.ts, relay/tests/helpers/ws-client.ts, relay/tests/fanout.test.ts, relay/tests/protocol.test.ts</files>
  <behavior>
    protocol.test.ts(기존 L285 옆): `parseInbound` 가 `{t:"sub", isin, ex:"KRX", lv:"price"}` → `lv === "price"` · lv 생략 → `lv` undefined(기본 full 은 fanout 이 접는다) · `lv:"turbo"` → null.
    ws-client.ts `sendSub(isin, ex, lv?)` — lv 가 있을 때만 필드를 싣는다(기존 호출 무변경).
    fanout.test.ts 새 describe 「가격 전용 구독 lv (quick-260923-ge2)」 — 이 파일은 setTimeout 이 fake 라 hub 의 200ms 배치는 `vi.advanceTimersByTime(TAPE_BATCH_MS)` 로 닫는다. tape 부재 판정은 「tape 를 push → 배치 창 진행 → 그 뒤 q 를 push → q 도착을 기다린 뒤 inbox 에 tape 가 없음」 순서로 한다(소켓 전송은 순서 보존이라 q 가 왔는데 tape 가 없으면 tape 는 오지 않은 것이다).
    - F1 price 단독: `sendSub(SAMPLE_ISIN, "KRX", "price")` → `h.hub.subscriptionLevel(USER_A, ...)` "price" · `gatewayMsgTypes` 에 28·29 는 있고 **32 는 0건**. `gateway.pushTape(sock, {snapshot:false, entries:[{tradeTime:"090000000001"}]})` → 배치 진행 → `pushQuote` → q 수신 → inbox 에 `t === "tape"` 0건 · q 는 `toMatchObject({i, x:"KRX", p})`(호가 배열 제거 없음 — `ap`/`bp` 키 존재).
    - F2 같은 사용자 A(full)·B(price): A 먼저 sub, B 는 price → `refCount` 2 · level "full" · 29 는 1건(실효 불변 → 재송신 없음). tape push → 배치 진행 → q push → A inbox 에 tape 1건 · B inbox 에 tape 0건 · 둘 다 q 있음.
    - F3 같은 소켓 승격: B 를 price 로 sub(29 1건 · 32 0건) → tape push + 배치 진행(B 는 못 받지만 hub 링버퍼에는 쌓인다) → B 가 **같은 키를 `lv:"full"` 로 재 sub** → `refCount` 는 **1 그대로** · level "full" · gatewayMsgTypes 에 28 이 2건 · 29 가 2건 · 32 가 1건(승격 재송신) · B inbox 에 `{t:"tape", snap:true}` 1건(링버퍼 캐시).
    - F4 같은 소켓 강등: full → 같은 키 `lv:"price"` 재 sub → level "price" · refCount 1 · 29 가 2건 · 32 는 1건에서 늘지 않는다.
    - F5 같은 소켓 같은 level 재 sub → 무시(refCount 1 · 29 1건) — 기존 L817 동작의 회귀 잠금.
    - F6 price 소켓 close → `refCount` 0 이 되고 29 가 2건(구독 + 해제)이 된다(⑨ 와 동형 · level 별 unsubscribe 경로 확인).
    - F7 `sendRaw({t:"sub", isin: SAMPLE_ISIN, ex:"KRX", lv:"turbo"})` → close 4400 · refCount 0 (⑩ 동형).
    - 기존 ①~㉔ 무수정 통과(특히 ⑥·⑨·⑪ — lv 없는 sub 은 full).
  </behavior>
  <action>
    **1. packages/shared/src/relay.ts.** `RelaySubMsg` 를 `{ t: "sub"; isin: string; ex: RelayExchange; lv?: RelaySubLevel }` 로 넓힌다. JSDoc: 기본 full · 같은 소켓·같은 키 재 sub 은 level 갱신(같으면 무시 · 참조계수 불변) · price 로 잡은 키에는 tape 프레임이 오지 않고 q 는 호가 배열까지 그대로 온다 · 옛 relay 는 zod strip 으로 `lv` 를 버려 full 로 동작한다(배포 순서 안전). `RelayUnsubMsg` 는 무변경, JSDoc 에 「level 무관 — 그 소켓이 잡은 키를 놓는다」 한 줄. 이후 `pnpm --filter @gh-radar/shared build`.

    **2. relay/src/ws/protocol.ts.** `ExchangeSchema` 옆에 `const SubLevelSchema = z.enum(["full", "price"])` 를 두고 `RelaySubSchema` 에 `lv: SubLevelSchema.optional()` 을 더한다. 주석: 열거 밖 문자열은 `ex` 와 같은 규율로 **스키마 위반(close 4400)** 이다 — 「모르는 값은 FULL 로 접는다」 는 hub 의 업스트림 바이트 규칙이고, 브라우저 표면에서는 유일한 클라이언트(webapp) 가 어휘를 공유하므로 관대함이 곧 표면이다(헤더 규칙 2 와 같은 태도). `.strict()` 는 쓰지 않는다(관례).

    **3. relay/src/ws/fanout.ts.**
    - `import type { RelaySubLevel } from "@gh-radar/shared"`(기존 shared import 에 추가). `Conn.keys` 값 타입을 `{ isin; ex; lv: RelaySubLevel }` 로.
    - `sub` 처리(L813~): `const lv = msg.lv ?? "full"` 을 분기 **안에서** 계산한다(Pitfall 14 — `keyOf` 와 같은 자리). `held = conn.keys.get(key)`: (a) `held` 있고 `held.lv === lv` → 기존 「중복 구독 — 무시」 그대로. (b) `held` 있고 level 다름 → `const prev = held.lv; held.lv = lv;` 다음 **순서로** `this.#hub.subscribe(userId, isin, ex, lv)` 뒤 `this.#hub.unsubscribe(userId, isin, ex, prev)` 를 부른다 — 이 순서라야 합계가 0 을 지나지 않는다(반대면 29(false) 뒤 재구독이 나간다). 승격(`lv === "full"`)이면 이 소켓은 tape 를 받은 적이 없으므로 `getTape` 캐시가 있으면 `{t:"tape", snap:true, ...}` 를 그 소켓에 보낸다(q 스냅샷은 다시 보내지 않는다 — 이미 q 스트림을 받고 있다). info 「[WS] 같은 소켓 재구독 — level 갱신」 에 `prev`·`lv`. (c) 신규 → `conn.keys.set(key, {isin, ex, lv})` · `hub.subscribe(userId, isin, ex, lv)` · q 스냅샷 캐시는 종전대로 · **tape 캐시는 `lv === "full"` 일 때만** 보낸다.
    - `#onClose`(L1202~): 구조분해에 `lv` 를 더해 `hub.unsubscribe(userId, isin, ex, lv)`.
    - `#deliver(userId, msg)`: 루프 안에서 `msg.t === "tape"` 이고 `conn.keys.get(keyOf(msg.i, msg.x))?.lv === "price"` 이면 그 소켓을 건너뛴다. 주석: 게이트웨이 가동본이 level 을 모르는 구버전이거나 같은 키를 다른 탭이 FULL 로 봐서 71 이 흘러와도 price 소켓에는 tape 가 가지 않는다 — relay 자체 보장. **그 소켓이 잡지 않은 키**의 tape 는 종전대로 간다(브라우저가 키로 거른다 — use-relay-socket 규율 7 · 이 plan 은 그 동작을 바꾸지 않는다). q 는 손대지 않는다. 사용자 격리(userId 단위 소켓 집합) 는 그대로다(T-15-02).
    - 헤더 규칙 7 을 갱신: 「sub(lv full|price · 기본 full) → 참조계수 +1(level 별) · q 스냅샷 캐시 즉시 · tape 캐시는 full 만 · 같은 소켓 재 sub 은 level 갱신(같으면 무시) · price 키의 tape 는 #deliver 가 그 소켓에서 걸러낸다」. 정본 출처(gh-trade 회신 quick-260923-exo · hub 헤더 D-33 level 블록) 를 한 줄로 가리킨다.

    **4. 테스트** — behavior 대로. `TAPE_BATCH_MS` 는 hub 모듈 export 를 import 한다. F1~F7 은 `h.hub.subscriptionLevel`·`refCount`·`gatewayMsgTypes`·`gateway.pushTape`/`pushQuote`·inbox 만으로 단언한다(게이트웨이가 받은 level 바이트는 Task 1 의 hub.test 가 잠갔다 — fanout.test 는 msgType 종류·개수까지).

    **5. 게이트 후 커밋 2.** `pnpm --filter @gh-radar/shared build` → relay `typecheck`·`typecheck:tests` → `run test`(전량 · 기준선 534 + Task 1 신규 8 + 이번 신규). 명시 경로 6개만 stage: `packages/shared/src/relay.ts relay/src/ws/protocol.ts relay/src/ws/fanout.ts relay/tests/helpers/ws-client.ts relay/tests/fanout.test.ts relay/tests/protocol.test.ts`. 메시지: `feat(quick-260923-ge2): 브라우저 sub 프레임에 lv(full|price) — 소켓별 level 로 재구독을 갱신하고 price 소켓에는 tape 를 흘리지 않는다`. Co-Authored-By 없음 · push 없음.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/relay run test && grep -q 'lv?: RelaySubLevel' packages/shared/src/relay.ts && grep -q 'SubLevelSchema' relay/src/ws/protocol.ts && grep -q 'subscriptionLevel' relay/tests/fanout.test.ts</automated>
  </verify>
  <done>relay vitest 전량 GREEN(F1~F7 · protocol 3건 신규 · 기존 ①~㉔ 무수정). price 소켓 inbox 에 tape 가 0건이고 full 소켓에는 있다(F2). 승격 재 sub 이 refCount 를 늘리지 않고 28·29·32 재송신 + 캐시 tape 스냅샷을 준다(F3). 한글 커밋 1건(명시 경로 · push 없음).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: webapp 참조계수 level 합성·실효 level 재송신 · useBreakoutQuotes price 전환 · 전량 게이트(relay+webapp vitest · typecheck · Playwright) (GE2-3 · 회신 §할 일 5·6)</name>
  <files>webapp/src/lib/use-relay-socket.ts, webapp/src/lib/use-breakout-quotes.ts, webapp/src/lib/__tests__/relay-socket.test.ts, webapp/src/lib/__tests__/use-breakout-quotes.test.tsx</files>
  <behavior>
    relay-socket.test.ts 새 describe 「구독 level (quick-260923-ge2)」 — `parsedSent()` 로 프레임을 정확 일치 단언한다.
    - W1 `subscribe(ISIN_A, 'KRX', 'price')` → 마지막 프레임 `toEqual({t:'sub', isin: ISIN_A, ex:'KRX', lv:'price'})`.
    - W2 승격: price 뒤 `subscribe(ISIN_A, 'KRX')`(full) → sub 프레임이 **2건**이고 두 번째는 `{t:'sub', isin, ex:'KRX'}`(lv 없음 — full 은 종전 모양). unsub 0건.
    - W3 강등·해제: full 뒤 price → sub 프레임 1건 그대로. `unsubscribe(ISIN_A, 'KRX')`(full) → 새 프레임 `{... lv:'price'}` 1건 · unsub 0건. `unsubscribe(ISIN_A, 'KRX', 'price')` → `{t:'unsub', isin, ex:'KRX'}`.
    - W4 ACK 이전 price 구독은 보류되다 ACK 에 `lv:'price'` 로 나간다(⑥-a 동형).
    - W5 재접속 재구독도 실효 level: price 만 잡은 상태로 서버가 1006 으로 닫고 백오프 뒤 새 소켓이 ACK 되면 재구독 프레임에 `lv:'price'`(재접속 describe L469~ 의 기존 패턴을 그대로 따른다 — 타이머 진행 방식 포함).
    - W6 level 불일치 해제 무시: full 만 있는데 `unsubscribe(ISIN_A, 'KRX', 'price')` → 프레임 추가 0 · 이어 `unsubscribe(ISIN_A, 'KRX')` → unsub 1건(카운트가 새지 않았다).
    - 기존 ⑥·⑥-a·⑥-b · 재접속 · cleanup describe 는 **무수정 통과**(full 프레임 모양 불변).
    use-breakout-quotes.test.tsx: 구독 diff·상한 케이스의 `toHaveBeenCalledWith(X, "KRX")` 를 `toHaveBeenCalledWith(X, "KRX", "price")` 로 갱신한다(L65-66 · L87 · L89 · L96-97 · L191-192 — vitest 는 인자 개수까지 대조하므로 이것은 회귀가 아니라 **계약 변경(3번째 인자)** 에 따른 갱신이고 SUMMARY 「갱신한 기존 테스트와 이유」 에 적는다). 상한 케이스(L131) 에 `subscribe.mock.calls.every((c) => c[2] === "price")` 를 추가한다. 헤더 ① 문장에 「price level」 을 더한다. 소스 규율 테스트(L135-142) 는 무수정이며 계속 통과해야 한다.
  </behavior>
  <action>
    **1. webapp/src/lib/use-relay-socket.ts.**
    - shared import 에 `type RelaySubLevel` 추가.
    - 컨텍스트 타입(L369-371): `subscribe: (isin, exchange, level?: RelaySubLevel) => void` · `unsubscribe` 동형. JSDoc: 기본 "full"(기존 호출부 무변경) · 키당 {full, price} 두 카운트 · 실효 level = full ≥1 ? full : price · 실효 level 이 바뀌면 같은 키로 sub 재송신(relay 가 같은 소켓 재구독을 level 갱신으로 처리한다 — fanout 규칙 7) · `lv` 는 price 일 때만 싣는다(full 프레임은 종전과 같은 모양 → 옛 relay 에도 안전).
    - `subRefsRef` 값을 `{ isin; ex; full: number; price: number }` 로, `wireSubsRef` 값을 `{ isin; ex; lv: RelaySubLevel }` 로 바꾼다. 모듈 헬퍼: `effectiveSubLevel(entry)` · `subFrameOf(entry)`(price 면 `{t:"sub", isin, ex, lv:"price"}`, 아니면 `{t:"sub", isin, ex}`).
    - `flushSubscriptions`: 합계 < 1 이면 continue. `lv = effectiveSubLevel(entry)`; `wire = wireSubsRef.get(key)`; `wire !== undefined && wire.lv === lv` 면 continue; 아니면 `subFrameOf` 송신 후 `wireSubsRef.set(key, {isin, ex, lv})`. JSDoc 에 「level 변경 재송신도 이 한 함수를 탄다 — 0→1 · 재접속 · 승격/강등 세 경로를 따로 두면 한쪽만 고쳐진다」 를 잇는다.
    - `subscribe(isin, exchange, level = "full")`: entry 를 얻거나 `{isin, ex, full:0, price:0}` 으로 만들고 `entry[level] += 1` → `flushSubscriptions()`.
    - `unsubscribe(isin, exchange, level = "full")`: entry 없거나 `entry[level] <= 0` → return(hub 의 「참조계수 없는 해제 — 무시」 와 동형 · 기존 `if (!entry) return` 태도 유지). `entry[level] -= 1`. 합계 > 0 이면 `flushSubscriptions()`(강등이면 lv:"price" 재송신 · 아니면 wire 일치로 무동작) 후 return. 합계 0 → 기존 경로(`delete` · wire 제거 · `unsub` 송신).
    - cleanup 의 unsub 루프(L1205-1207) 는 `wire.isin/ex` 만 쓰므로 그대로. 헤더 규율 3 에 「카운트는 level 별 {full, price} 이고 와이어 level 은 그 합성(full 우선)이다 — 실효 level 이 바뀌면 같은 키로 sub 을 다시 보낸다(quick-260923-ge2)」 를 더한다.

    **2. webapp/src/lib/use-breakout-quotes.ts.** `BREAKOUT_EXCHANGE` 옆에 `const BREAKOUT_SUB_LEVEL: RelaySubLevel = "price"`(shared 타입 import). 구독 diff effect(L165·L171) 와 언마운트 해제(L181) 세 호출에 3번째 인자로 넘긴다. 헤더에 ⑥ 을 추가: 「가격 전용 구독(quick-260923-ge2 · 2b). 칩은 현재가·등락률만 읽으므로 price 로 잡는다 — 게이트웨이는 59 를 가격 섹션 갱신 때만(≤5Hz) 보내고 71·75 는 보내지 않는다(PRICE 59 에도 호가 배열은 실려 오지만 읽지 않는다). 카드(FULL) 와 같은 종목이면 relay·webapp 참조계수가 full 로 합성하고, 카드가 닫히면 price 로 강등된다. relay 는 게이트웨이 가동본이 구버전이어도 price 소켓에 tape 를 흘리지 않는다.」 **주의:** 이 파일은 소스 규율 테스트가 주석까지 정규식으로 훑는다 — 소켓 send 호출 모양과 sub 프레임 리터럴 모양(`t` 키에 sub 문자열) 을 **주석에도** 쓰지 말고 「sub 프레임」 처럼 산문으로 적는다.

    **3. 테스트** — behavior 대로. 구현 전 W1~W3 RED 를 관측한다(3-인자 시그니처가 없어 typecheck 부터 빨갛다 — 그것으로 충분).

    **4. 전량 게이트.** 순서: `pnpm --filter @gh-radar/shared build` → `pnpm --filter @gh-radar/relay run typecheck` · `typecheck:tests` → `pnpm --filter @gh-radar/webapp run typecheck` → `pnpm --filter @gh-radar/relay run test` → `pnpm --filter @gh-radar/webapp run test`(기준선 1510/1 skip + 신규) → 3100·8090 이 비어 있는지 확인 후 `pnpm --filter @gh-radar/webapp exec playwright test trading-workbench orderbook`(실 relay + 스텁 게이트웨이 · e2e 는 lv 를 검사하지 않으므로 무수정 통과가 기대값 — 실패하면 원인을 SUMMARY 에 적고 e2e 수정은 web-first 단언 교체까지만 허용). 끝난 뒤 두 포트가 비어 있음을 확인한다.

    **5. 커밋 3 + SUMMARY 재료.** 명시 경로 4개만 stage: `webapp/src/lib/use-relay-socket.ts webapp/src/lib/use-breakout-quotes.ts webapp/src/lib/__tests__/relay-socket.test.ts webapp/src/lib/__tests__/use-breakout-quotes.test.tsx`. 메시지: `feat(quick-260923-ge2): webapp 구독 참조계수를 full/price 로 합성해 실효 level 로 sub 을 보내고 돌파 칩은 가격 전용 구독으로 전환`. Co-Authored-By 없음 · push 없음. SUMMARY 에 반드시 넣을 절: ① 커밋 3건 표 ② 「관찰 사항」(Task 1 의 69/71 중복 제거 확인 결과 · msg-type 무변경 확인) ③ 「갱신한 기존 테스트와 이유」(use-breakout-quotes 3번째 인자) ④ 「배포 후 확인 절차」 — 게이트웨이 가동본(bd1c541d) 이 level 을 몰라 지금은 실서버 PRICE 동작을 볼 수 없다; 배포 순서는 relay 먼저(옛 webapp 은 lv 를 안 보내 full · 무해) → 검증 → webapp push(push = Vercel 배포); 확인 항목: relay 로그에서 `level` 필드가 붙은 「신규 구독」·「PRICE→FULL 승격」·「FULL→PRICE 강등」 라인 grep, 브라우저 devtools WS 프레임 필터로 돌파 ISIN 의 송신 `"lv":"price"` 와 수신 `"t":"tape"` 부재, 게이트웨이 재기동 뒤에는 돌파 ISIN 의 `q` 가 체결 때만(종목당 ≤5Hz) 오고 호가창 카드가 열린 종목은 tape 가 계속 오는지, 게이트웨이→relay 구간은 gh-trade 세션 로그(회신 §할 일 6) ⑤ 배포·push 는 범위 밖(사용자 결정).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/relay run test && pnpm --filter @gh-radar/webapp run test && grep -q 'RelaySubLevel' webapp/src/lib/use-relay-socket.ts && grep -q 'BREAKOUT_SUB_LEVEL' webapp/src/lib/use-breakout-quotes.ts && pnpm --filter @gh-radar/webapp exec playwright test trading-workbench orderbook</automated>
  </verify>
  <done>W1~W6 GREEN(구현 전 RED 관측) · 기존 relay-socket ⑥·⑥-a·⑥-b·재접속·cleanup 무수정 통과 · use-breakout-quotes 는 3번째 인자 갱신만으로 통과(소스 규율 테스트 무수정 통과). relay·webapp vitest 전량 · shared build 후 3개 typecheck · Playwright trading-workbench+orderbook 0 failed. 한글 커밋 3건이 `git log` 에 있고 `git status --short` 에 남은 M 은 없다(미추적은 다른 세션 파일만). SUMMARY 에 관찰 사항·갱신 테스트 사유·배포 후 확인 절차가 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → relay wss | 인증된 사용자의 `sub` 프레임에 새 필드 `lv` 가 들어온다(비신뢰 입력) |
| relay → 게이트웨이 TCP | relay 가 만든 29 프레임에 level 바이트가 실린다(신뢰 경로 · relay 가 유일한 송신자) |
| relay → 브라우저 (팬아웃) | tape 프레임을 소켓 level 에 따라 걸러낸다 — 사용자 격리와 무관한 필터 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ge2-01 | Tampering | `protocol.ts` RelaySubSchema.lv | low | mitigate | `z.enum(["full","price"]).optional()` — 열거 밖 값은 safeParse 실패 → 기존 close(4400) 경로(F7 · protocol 테스트). hub 의 `levelByte` 는 모르는 값을 FULL 로 접어 업스트림에 1 외의 값이 나가지 않는다 |
| T-ge2-02 | Denial of Service | `fanout.ts` 같은 소켓 level 토글 → 승격마다 28/29/32 3프레임 | low | mitigate | 기존 인바운드 토큰 버킷(T-16-06 `rateTokens`) 이 소켓당 프레임 수를 제한한다. 승격 재송신은 회신 규칙 2 가 요구하는 계약이라 줄이지 않는다. 같은 level 재 sub 은 무시(F5) 라 프레임 0 |
| T-ge2-03 | Information Disclosure | `fanout.ts #deliver` tape 필터 | low | accept | 필터는 `#users.get(userId)` 소켓 집합 **안에서** 건너뛰기만 한다 — 타 사용자에게 가는 경로는 생기지 않는다(T-15-02 유지 · ⑦·㉒ 무수정 통과가 증거) |
| T-ge2-04 | Repudiation | hub 전이 로그 | low | mitigate | 승격·강등·불일치 해제 전부 `level` 필드를 실어 info/warn 으로 남긴다(S-5) — 배포 후 확인 절차의 grep 키워드 |
| T-ge2-SC | Tampering | npm 설치 | low | accept | 신규 패키지 0 — 설치 작업이 없어 패키지 정당성 게이트 해당 없음 |
</threat_model>

<verification>
- Task 별 `<verify>` 명령이 전부 exit 0. 마지막 태스크의 전량 게이트: `pnpm --filter @gh-radar/shared build` → relay/webapp typecheck 3종 → relay vitest(기준선 534 + 신규) → webapp vitest(기준선 1510/1 skip + 신규) → Playwright trading-workbench+orderbook.
- `git log --oneline -3` 에 `quick-260923-ge2` 커밋 3건 · `git status --short` 에 M 없음 · 미추적은 `.planning/quick/*` 3 디렉터리와 `tasks/*.md` 2건만(플랜 시점과 동일).
- `git show --stat HEAD~2` 가 생성 코드 2파일을 포함하고 `.planning`·`tasks` 를 포함하지 않는다.
- SUMMARY: 커밋 표 · 관찰 사항(69/71 중복 제거 유무 · msg-type 무변경) · 갱신 테스트 사유 · 배포 후 확인 절차 · Playwright 결과.
</verification>

<success_criteria>
- relay 가 컴파일되고(TS2554 해소) 생성 코드 2파일이 경로 지정 커밋으로 들어갔다.
- hub: PRICE 단독 0→1 = 28→29(1) · FULL 0→1 = 28→29(0)→32 · 승격 = 3프레임 재송신 · 강등 = 29(1) 1건 · 마지막 이탈 = 29(false) · resubscribeAll 실효 level — 전부 hub.test 로 잠겼다.
- 브라우저 `sub` 에 `lv` 가 있고, price 소켓은 tape 를 받지 않으며(증분·캐시 모두), 같은 소켓 재 sub 은 level 갱신이다 — fanout.test 로 잠겼다.
- webapp `subscribe/unsubscribe(isin, exchange, level='full')` 가 level 별 카운트를 합성해 실효 level 로 sub 을(재)송신하고, `useBreakoutQuotes` 만 price 다. 기존 full 프레임 모양·기존 호출부·기존 테스트(use-breakout-quotes 3번째 인자 제외)는 무변경이다.
- 전량 게이트 초록 · 커밋 3건(한글 · 명시 경로 · push 없음 · Co-Authored-By 없음) · SUMMARY 에 배포 후 확인 절차.
</success_criteria>

<output>
Create `.planning/quick/260923-ge2-subscribequotereq-level-price-gh-trade-2/260923-ge2-SUMMARY.md` when done
</output>
