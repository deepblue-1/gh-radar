---
phase: quick-260925-0pf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/src/styles/__tests__/tds-tokens.test.ts
  - webapp/src/styles/globals.css
  - webapp/src/lib/chart-colors.ts
  - webapp/src/lib/__tests__/chart-colors.test.ts
  - webapp/src/components/stock/detail-bands.tsx
  - webapp/src/app/chat/page.tsx
  - webapp/src/components/ui/card.tsx
  - webapp/src/components/ui/sheet.tsx
  - webapp/src/components/ui/table.tsx
  - webapp/src/components/ui/separator.tsx
  - webapp/src/components/chat/citation.tsx
  - webapp/src/components/chat/composer.tsx
  - webapp/src/components/chat/conversation-list.tsx
  - webapp/src/components/layout/app-sidebar.tsx
  - webapp/src/components/orderbook/orderbook-ladder.tsx
  - webapp/src/components/orderbook/trade-tape.tsx
  - webapp/src/components/stock/stock-detail-tabs.tsx
  - webapp/src/components/stock/stock-discussion-section.tsx
  - webapp/src/components/stock/discussion-page-client.tsx
  - webapp/src/components/stock/stock-news-section.tsx
  - webapp/src/components/stock/stock-limit-up-section.tsx
  - webapp/src/components/stock/stock-stats-grid.tsx
  - webapp/src/components/stock/stock-hero.tsx
  - webapp/src/components/scanner/scanner-table.tsx
  - webapp/src/components/scanner/scanner-skeleton.tsx
  - webapp/src/components/trading/limit-chaser-form.tsx
  - webapp/src/components/trading/vi-order-list.tsx
  - webapp/src/components/trading/strategy-status-card.tsx
  - webapp/src/components/trading/card/stock-info-modal.tsx
  - webapp/src/components/trading/card/card-body.tsx
  - webapp/src/components/trading/card/manual-order-form.tsx
  - webapp/src/components/trading/workbench/vi-trigger-strip.tsx
  - webapp/src/components/trading/workbench/shared-panels.tsx
  - webapp/src/components/trading/workbench/breakout-strip.tsx
  - webapp/src/components/trading/workbench/vi-settings-rows.tsx
  - webapp/src/components/watchlist/watchlist-skeleton.tsx
  - webapp/src/components/watchlist/watchlist-table.tsx
  - webapp/src/components/trading/__tests__/card-body.test.tsx
  - webapp/src/components/stock/__tests__/stock-detail-tabs.test.tsx
autonomous: true
requirements: [0PF-A, 0PF-B, 0PF-C, 0PF-D]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "(0PF-A · U-01) globals.css `:root`·`.dark` 의 테마 색 토큰이 @toss/tds-colors@0.1.0 공식값(또는 그 색의 rgba 틴트)이다 — 다크 띠 #101013 · 다크 faint grey400 #62626d · 다크 raised-2 grey200 #3c3c47 · 다크 seg-on/pill-on grey300 #4d4d59 · 다크 red #f04251(destructive·up 및 up-bg·bid-bar 틴트 rgb 240,66,81)"
    - "(0PF-A) 라이트 보조 텍스트 `--muted-fg` = grey600 #6b7684, `--flat` 도 같은 값(vj1 짝 유지). 주 텍스트 `--fg-2` grey800 #333d4b · 가장 진한 `--fg` grey900 #191f28 은 그대로"
    - "(0PF-B) 행 구분선·입력 hairline 은 TDS hairlineBorder(`--border-subtle` = 라이트 #e5e8eb · 다크 #3c3c47)로 그려지고, 면 외곽선(4면 `border`)은 한 곳도 새로 보이지 않는다 — 무테 유지"
    - "(0PF-A) 차트 팔레트의 up/down 이 테마별 globals.css `--up`/`--down` 과 같은 hex 이고, 축 글자 = grey500 · grid = grey100(두 테마 대칭), 모든 키 hex"
    - "(0PF-C) 종목상세 탭 17px/600(t5) · 스탯 라벨 15px/400 보조색(t6) · 히어로 종목명 17px/600 · 폰 「주문하기」 CTA 56px/radius 16/17px(xlarge) + 좌우 20 · 하단 max(20px, safe-area) · 챗 FAB 은 CTA 위 · 마지막 콘텐츠가 CTA 에 안 가려짐"
    - "(0PF-C · 0PF-D) 수동주문 주문 버튼 48px/radius 14(TDS large) — 글자 13px·px-px·whitespace-normal 불변(`lc` 가로 증가 0)"
    - "(0PF-D) §2.2b 블록 · 밴드 경계 700/830/992 · wb 680 · 3단 400 하한 · `--lw` 76/104 · 더티 바 포털 · container-type 위치 불변, `lc` 안 글자 크기·굵기·자간·가로 패딩·gap·열 폭 증가 0"
    - "typecheck · 전체 vitest · next build 통과"
  artifacts:
    - path: "webapp/src/styles/__tests__/tds-tokens.test.ts"
      provides: "TDS 토큰 값 고정 + 차트↔CSS up/down 동기 + 행 구분선 hairline 가드"
    - path: "webapp/src/styles/globals.css"
      provides: "TDS 공식값 토큰(:root·.dark 대칭) · FAB 들어올림 규칙"
    - path: "webapp/src/lib/chart-colors.ts"
      provides: "lightweight-charts hex 팔레트(TDS 공식값)"
  key_links:
    - from: "webapp/src/lib/chart-colors.ts"
      to: "webapp/src/styles/globals.css"
      via: "up/down hex == 테마별 --up/--down (tds-tokens.test.ts 교차 단언)"
      pattern: "f04251"
    - from: "webapp/src/components/stock/stock-detail-tabs.tsx (CTA 바 높이)"
      to: "webapp/src/styles/globals.css (chat-fab bottom)"
      via: "바 = 10 + 56 + max(20, safe) → FAB = 70 + max(20, safe), Tabs 예약 = 76 + max(20, safe)"
      pattern: "max\\(20px"
    - from: "행 구분선 소비처(border-t/b/l/r · divide)"
      to: "--border-subtle"
      via: "가드 테스트가 방향 테두리 + var(--border) 조합을 0 으로 고정"
      pattern: "border-\\[var\\(--border-subtle\\)\\]"
---

<objective>
theme/toss-b 브랜치의 토스 B 테마 값을 **공식 TDS(토스 디자인 시스템)** 값으로 맞춘다 — 색(@toss/tds-colors 앱 팔레트) · 텍스트 역할 · 타입 스케일 · 버튼 크기. 서체는 Pretendard 유지.

사용자 결정(RESEARCH 「결정」): **U-01** 기준 팔레트 = 공식 `@toss/tds-colors@0.1.0` 앱 팔레트(토스증권 웹 tw 팔레트 아님). **U-02** 서체 = Pretendard 유지(TPS 미사용).
범위 = RESEARCH 「보정 대상」 목록 + 그 직접 귀결(같은 비공식값을 쓰는 형제 토큰). 구조 변경 0 — vj1 이 만든 토큰 구조 위에서 **값만** 교정한다(tracer 생략: 아키텍처 입증됨, 새 경로 0).

Purpose: 실험 테마가 「토스처럼 보이는 추정값」이 아니라 공식 값 위에 서게 해서, 채택 여부를 공식 기준으로 판단할 수 있게 한다.
Output: TDS 토큰 값·차트 팔레트·hairline 구분선·타입/버튼 크기 교정 + 가드 테스트 1개(신규) + SUMMARY(제안 커밋 메시지 포함, **커밋·push 안 함**).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260925-0pf-tds-theme-toss-b/260925-0pf-RESEARCH.md
@.planning/quick/260924-vj1-b-theme-toss-b/260924-vj1-SUMMARY.md
@webapp/src/styles/globals.css
@webapp/src/lib/chart-colors.ts
@webapp/src/lib/__tests__/chart-colors.test.ts

## 작업 위치 · 커밋 규칙 (필독)
- 작업 트리 = git worktree `/Users/alex/repos/gh-radar/.claude/worktrees/toss-b`(브랜치 `theme/toss-b`, HEAD f8280a4). 모든 경로는 이 worktree 기준. **메인 트리 `/Users/alex/repos/gh-radar` 는 절대 건드리지 않는다.**
- **git commit 금지 · push 금지.** 사용자 전역 규칙(커밋 메시지를 먼저 보여주고 승인 후 커밋)에 따라 모든 변경은 미커밋으로 남기고, SUMMARY 에 한글 제안 커밋 메시지(HEAD 대비 working tree 전체 1커밋, Co-Authored-By 없음)를 적는다.
- 미추적 `webapp/e2e/specs/zz-theme-gallery.spec.ts` 는 오케스트레이터 것 — 건드리지 않는다.
- 전제 확인됨(계획 시점 실측): `packages/shared/dist` · `relay/node_modules` · `webapp/node_modules` 존재. dev 서버 불필요.

## 계획 시점 실측 (HEAD f8280a4 · 이 플랜의 범위 근거)
- 비공식 값: 다크 `--raised-2`·`--seg-on-bg`·`--pill-on-bg` = #454552, 다크 `--band` #0f0f12, 다크 `--faint` #6b6b73, 다크 `--destructive`·`--up` #f04452(라이트 값), 다크 `--up-bg`·`--bid-bar` 틴트 rgb(240,68,82), 다크 `--border-subtle` 흰 4%, 차트 다크 grid #26262c.
- 텍스트 역할 비대칭: 라이트 `--muted-fg`·`--flat` = grey500 #8b95a1 인데 다크는 grey600 #9e9ea4. 차트 축 글자도 라이트 grey500 · 다크 grey600.
- `--faint` 소비처 19곳 전부 `border-[var(--faint)]` / `bg-[var(--faint)]`(점선 플레이스홀더·OFF LED 윤곽·스탯 범위 밴드) — 글자색 0 → 역할은 3차가 아니라 **비활성(grey400)**. 라이트 #b0b8c1 이 이미 grey400.
- `--border-subtle` 소비처 27곳 **전부** 방향 구분선(`border-t`/`border-b`/`divide-*`/`[&>tr+tr>*]:border-t`) — 이미 「행 구분선」 토큰이다(ui/table.tsx 주석 「row divider … (hairline)」).
- `var(--border)` 소비처(컴포넌트·app, `__tests__`·`app/design` 제외): 4면 `border` 외곽선 54줄(면 — 값 유지 대상), 방향 구분선 45줄(아래 T2 목록), 기타 32줄(색 혼합·트랙·팝오버 외곽 등).
- 크기: 종목상세 탭 트리거 16px, CTA 54px / 16.5px / 바 `px-4` · 하단 12px+safe, Tabs 루트 예약 84px, FAB `calc(80px + safe)`, 스탯 라벨 13.5px(2곳)·값 16px/500, 히어로 종목명 18px, 수동주문 주문 버튼 52px/radius 14/13px.
- 이 값들을 단언하는 기존 테스트: `chart-colors.test.ts`(라이트·다크 up 동일 단언 — 이번에 깨짐), `card-body.test.tsx:219`(좌 pane `border-[var(--border)]` 단언 — 이번에 깨짐). 탭·CTA·버튼 크기·스탯 라벨·히어로 이름 크기를 단언하는 테스트 없음.

## TDS 공식 값 (RESEARCH 발췌 · 이 플랜이 쓰는 것만)
- 라이트 grey: 100 #f2f4f6 · 200 #e5e8eb · 400 #b0b8c1 · 500 #8b95a1 · 600 #6b7684 · 800 #333d4b · 900 #191f28. red500 #f04452 · blue500 #3182f6.
- 다크 grey: 100 #2c2c35 · 200 #3c3c47 · 300 #4d4d59 · 400 #62626d · 500 #7e7e87 · 600 #9e9ea4 · 800 #e4e4e5. red500 #f04251 · blue500 #3485fa. greyBackground #101013.
- hairlineBorder: 라이트 #e5e8eb · 다크 #3c3c47.
- 타입: t5 17/25.5 · st10 16/24 · t6 15/22.5 · t7 13 · t4 20 · t1 30. 본문 line-height 1.5 를 상속하므로 `text-[17px]`→25.5, `text-[15px]`→22.5 로 TDS line-height 와 자동 일치.
- 버튼: xlarge 56/r16/t5 17 · large 48/r14/t5 17. BottomCTA = xlarge · 좌우 20 · 하단 20/safe-area.

## 불변 (건드리면 오발주·잘림 회귀)
- globals.css §2.2b 주석 블록 전체(「§2.2b 상따 화면 반응형」 ~ 「§2.3 글로벌 베이스라인」 줄까지) 한 글자도 바꾸지 않는다 — 계획 시점 sed 범위 sha256 지문 `c7eb5ddd…ea254f` 를 T3 verify 가 대조한다(git 없이). §2.3 머리 줄도 범위에 들어가므로 그 줄도 건드리지 않는다.
- `card-body.tsx` 의 `@min-[700px]/lc:grid-cols-[260px_minmax(0,1fr)]` · `@min-[830px]/lc:grid-cols-[400px_minmax(0,1fr)]` · `@min-[992px]/lc:grid-cols-[460px_minmax(0,1fr)]`, `limit-chaser-form.tsx` 의 `[--lw:76px]` · `@min-[992px]/lc:[--lw:104px]` · `createPortal`(더티 바 포털 — 실제 위치는 이 파일), `strategy-card.tsx` `LC_CONTAINER_CLASS`, container-type 위치.
- **`lc` 카드 안**(호가 사다리 · 종목정보 10칸 · 매수/매도/수동 폼 · 카드 헤더): 글자 크기·굵기·letter-spacing·가로 패딩·gap·열 폭·`--lw` 를 **키우지 않는다**. 허용 = 색 · radius · 세로 높이. TDS 크기가 `lc` 안 글자를 키우게 되면 현재 크기를 유지하고 SUMMARY 에 적는다.
- 면(카드·박스·상태줄·팝오버·시트 외곽)은 무테 유지 — `--border` 값은 바꾸지 않는다(4면 외곽선 54줄이 기대는 값). hairline 은 행 구분선·입력 hairline 에만.
- 차트에 들어가는 색은 hex/rgb 만(lightweight-charts 가 oklch 거부).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDS 공식 토큰 값(라이트·다크) + 차트 팔레트 + 토큰 고정 테스트</name>
  <files>webapp/src/styles/__tests__/tds-tokens.test.ts, webapp/src/styles/globals.css, webapp/src/lib/chart-colors.ts, webapp/src/lib/__tests__/chart-colors.test.ts, webapp/src/components/stock/detail-bands.tsx</files>
  <behavior>
    - tds-tokens.test.ts 「TDS 토큰 값」: globals.css 를 `readFileSync(path.resolve(__dirname, '../globals.css'))` 로 읽어 첫 `:root {` 블록과 첫 `.dark {` 블록에서 `--이름: 값;` 을 파싱한다(대소문자 무시 비교).
    - 라이트 기대값: --bg #ffffff · --surface #f2f4f6 · --band #f2f4f6 · --fg #191f28 · --fg-2 #333d4b · --muted #f2f4f6 · --muted-fg #6b7684 · --flat #6b7684 · --faint #b0b8c1 · --raised-2 #e5e8eb · --border-subtle #e5e8eb · --card #ffffff · --popover #ffffff · --primary #3182f6 · --up #f04452 · --destructive #f04452 · --down #3182f6 · --up-bg #ffeeee · --down-bg #e8f3ff · --seg-on-bg #ffffff · --pill-on-bg #191f28 · --side-bg #f9fafb.
    - 다크 기대값: --bg #17171c · --surface #17171c · --band #101013 · --fg #ffffff · --fg-2 #e4e4e5 · --muted #2c2c35 · --muted-fg #9e9ea4 · --flat #9e9ea4 · --faint #62626d · --raised-2 #3c3c47 · --border-subtle #3c3c47 · --card #202027 · --popover #2c2c35 · --primary #3485fa · --up #f04251 · --destructive #f04251 · --down #3485fa · --seg-on-bg #4d4d59 · --pill-on-bg #4d4d59 · --side-bg #101013.
    - 다크 `--up-bg` 와 `--bid-bar` 가 `rgba(240, 66, 81,` 로 시작(공백 허용 정규식).
    - 테마 색 토큰 집합(bg surface band fg fg-2 muted muted-fg faint raised-2 border border-subtle input ring card card-fg popover popover-fg primary primary-fg secondary secondary-fg accent accent-fg destructive destructive-fg up down flat up-bg down-bg seg-on-bg seg-on-fg seg-on-shadow pill-on-bg pill-on-fg ask-bar bid-bar side-bg led-latent led-armed new-bg new-bd)이 두 블록 모두에 정의되고, 그 값에 `oklch` 가 없다.
    - 교차 단언: `getChartPalette('light').up/down` == 라이트 --up/--down, `getChartPalette('dark').up/down` == 다크 --up/--down.
    - chart-colors.test.ts: 공식값 고정 — light {up #f04452, down #3182f6, text #8b95a1, grid #f2f4f6}, dark {up #f04251, down #3485fa, text #7e7e87, grid #2c2c35}. 분기 테스트는 up·down·text·grid **네 키 모두** 테마별로 다르다로 바꾼다(기존 「up 동일」 단언과 그 주석 제거). hex 형식·bg 투명·한국식 색(up 적색·down 청색) 테스트는 유지.
  </behavior>
  <action>
(0PF-A · U-01) RED 먼저: 위 behavior 대로 `webapp/src/styles/__tests__/tds-tokens.test.ts` 를 새로 만들고(디렉터리 신규 — vitest include `src/**/*.test.{ts,tsx}` 에 이미 포함, 소스 읽기 패턴은 `latch-led.test.tsx` 와 동일), `chart-colors.test.ts` 를 고친 뒤 실행해 실패를 확인한다.

GREEN — `globals.css` 값만 바꾼다(키 추가·삭제 0, `--border` 값 유지):
- `:root`: `--muted-fg` 와 `--flat` → #6b7684(grey600 보조 텍스트. `--flat` 은 vj1 이 `--muted-fg` 와 같은 값으로 짝지은 토큰이라 함께 옮긴다 — 다크는 이미 둘 다 grey600). `--border-subtle` → #e5e8eb(TDS hairlineBorder).
- `.dark`: `--band` → #101013(greyBackground). `--faint` → #62626d(grey400 — 소비처가 전부 테두리/채움이라 「비활성」 역할, 라이트 #b0b8c1 과 대칭). `--raised-2` → #3c3c47(grey200 — 라이트 raised-2 #e5e8eb 와 대칭). `--border-subtle` → #3c3c47(hairlineBorder). `--destructive`·`--up` → #f04251. `--up-bg` → rgba(240, 66, 81, 0.16), `--bid-bar` → rgba(240, 66, 81, 0.28). `--seg-on-bg`·`--pill-on-bg` → #4d4d59(grey300).
- 다크 선택 면 grey300 근거(재량 · RESEARCH 「대비 확인」 지시): 트랙 `--muted` grey100 #2c2c35 대비 명도비가 grey200 1.27 : 1 · 현재 비공식값 1.47 : 1 · grey300 1.66 : 1 이다. 세그먼트 선택(감시대상 매도잔량/매수잔량 등)은 설정 오판 = 오발주로 이어지므로 현재보다 약해지면 안 된다 → grey300. 덤으로 hover(`--raised-2` grey200)와 선택(grey300)이 다시 구분된다(지금은 둘 다 같은 값).
- 주석: §9 머리 주석의 B 단락을 TDS 기준으로 고쳐 쓴다 — 출처 `@toss/tds-colors@0.1.0`, 텍스트 역할(주 grey800 `--fg-2` · 가장 진한 grey900 `--fg` · 보조 grey600 `--muted-fg` · 비활성 grey400 `--faint`), 명도 단계(다크 bg #17171c → card #202027 → raised #2c2c35 → raised-2 #3c3c47), **`--border-subtle` = TDS hairlineBorder = 행 구분선·입력 hairline 전용**, **`--border` = 면 외곽선 잔재 — 무테 유지를 위해 거의 안 보이는 값을 의도적으로 유지(4면 외곽선 소비처가 기대는 값이라 hairline 으로 올리면 모든 면에 테두리가 생긴다)**. `.dark` 의 B 주석과 `--seg-on-*` 설명도 새 값·근거로. §2.2b 블록은 건드리지 않는다.

`chart-colors.ts`: dark `up` #f04251, `text` #7e7e87(grey500 3차 — 축 라벨. 라이트 #8b95a1 도 grey500 이라 두 테마 같은 역할), `grid` #2c2c35(grey100 — 라이트 #f2f4f6 도 grey100). light 는 값 불변. 파일 머리의 「상승색은 두 테마 모두 같다」 문단을 「TDS 공식값 · 테마별 red500(라이트 #f04452 / 다크 #f04251) · 축 글자 grey500 · grid grey100 · up/down 은 globals.css 와 같은 hex(tds-tokens.test.ts 가 교차 단언)」로 교체.

`detail-bands.tsx`: 머리 주석의 다크 띠 색을 #101013 으로(주석만).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp run test src/styles/__tests__/tds-tokens.test.ts src/lib/__tests__/chart-colors.test.ts && grep -q 'tds-colors' webapp/src/styles/globals.css && grep -q '#101013' webapp/src/components/stock/detail-bands.tsx</automated>
  </verify>
  <done>두 테스트 파일이 RED → GREEN(실패 확인 후 통과). globals.css 두 블록이 위 기대값과 일치, 키 집합 불변, §2.2b 무변경. 차트 팔레트가 TDS hex 로 고정되고 up/down 이 CSS 토큰과 교차 검증된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 행 구분선을 hairline(--border-subtle)으로 — 가드 테스트 + 45줄 스윕 + 특수 3곳</name>
  <files>webapp/src/styles/__tests__/tds-tokens.test.ts, webapp/src/app/chat/page.tsx, webapp/src/components/ui/card.tsx, webapp/src/components/ui/sheet.tsx, webapp/src/components/ui/table.tsx, webapp/src/components/ui/separator.tsx, webapp/src/components/chat/citation.tsx, webapp/src/components/chat/composer.tsx, webapp/src/components/chat/conversation-list.tsx, webapp/src/components/layout/app-sidebar.tsx, webapp/src/components/orderbook/orderbook-ladder.tsx, webapp/src/components/orderbook/trade-tape.tsx, webapp/src/components/stock/stock-detail-tabs.tsx, webapp/src/components/stock/stock-discussion-section.tsx, webapp/src/components/stock/discussion-page-client.tsx, webapp/src/components/stock/stock-news-section.tsx, webapp/src/components/stock/stock-limit-up-section.tsx, webapp/src/components/scanner/scanner-table.tsx, webapp/src/components/scanner/scanner-skeleton.tsx, webapp/src/components/trading/limit-chaser-form.tsx, webapp/src/components/trading/vi-order-list.tsx, webapp/src/components/trading/strategy-status-card.tsx, webapp/src/components/trading/card/stock-info-modal.tsx, webapp/src/components/trading/card/card-body.tsx, webapp/src/components/trading/card/manual-order-form.tsx, webapp/src/components/trading/workbench/vi-trigger-strip.tsx, webapp/src/components/trading/workbench/shared-panels.tsx, webapp/src/components/trading/workbench/breakout-strip.tsx, webapp/src/components/trading/workbench/vi-settings-rows.tsx, webapp/src/components/watchlist/watchlist-skeleton.tsx, webapp/src/components/watchlist/watchlist-table.tsx, webapp/src/components/trading/__tests__/card-body.test.tsx</files>
  <behavior>
    - tds-tokens.test.ts 에 describe 「행 구분선은 hairline(--border-subtle)」 추가: `src/components` 와 `src/app` 의 `.tsx` 를 재귀로 읽되 `__tests__` 디렉터리와 `src/app/design` 은 제외한다.
    - 각 줄을 공백·따옴표·백틱으로 토큰화하고, 토큰마다 대괄호 깊이 0 의 마지막 콜론까지를 변형 접두로 떼어 낸다(`[&>*]:border-t` → `border-t`, `md:border-b` → `border-b`).
    - 위반 = 그 줄에 색 토큰 `border-[var(--border)]` 또는 `divide-[var(--border)]` 가 있고, 방향 토큰(정규식 `^border-[tblrxy](-[1-9][0-9]*|-\[[0-9.]+px\])?$` — `-0` 은 제외 — 또는 `divide-` 로 시작하는 토큰)이 있으며, 4면 토큰 `border` 는 **없는** 줄. 기대: 위반 목록 = 빈 배열(실패 시 파일:줄 목록을 메시지로 출력).
    - 스윕 전에는 이 테스트가 45줄을 보고하며 실패해야 한다(RED).
  </behavior>
  <action>
(0PF-B) RED: 위 가드를 추가하고 실행해 정확히 아래 45줄이 보고되는지 대조한다(목록과 다르면 멈추고 차이를 SUMMARY 에 기록한 뒤 규칙 기준으로 판단).

스윕 규칙: 보고된 각 줄에서 색 토큰의 `var(--border)` 만 `var(--border-subtle)` 로 바꾼다(변형 접두·방향 토큰·다른 클래스는 그대로). 색만 바뀌므로 기하 변화 0 — `lc` 안 줄(사다리 · card-body · limit-chaser-form · manual-order-form)도 가로 폭 불변.

계획 시점(HEAD f8280a4) 대상 45줄 — T1 은 이 파일들을 건드리지 않으므로 줄 번호가 그대로 맞다:
- app/chat/page.tsx:250
- components/ui: card.tsx:98(CardFooter) · sheet.tsx:117, 130(헤더·푸터 구분선) · table.tsx:47
- components/chat: citation.tsx:40(인용 왼쪽 3px 막대) · composer.tsx:48 · conversation-list.tsx:119
- components/layout/app-sidebar.tsx:160(하위 목록 hairline)
- components/orderbook/orderbook-ladder.tsx: 476, 482, 488, 499, 504, 508, 520, 851, 957, 960, 961, 1175, 1257 (13줄)
- components/orderbook/trade-tape.tsx:509
- components/stock: stock-detail-tabs.tsx:142(sticky 탭 바 기준선) · stock-discussion-section.tsx:260 · discussion-page-client.tsx:282 · stock-news-section.tsx:203
- components/scanner: scanner-table.tsx:77 · scanner-skeleton.tsx:33
- components/trading: limit-chaser-form.tsx:1234, 1304 · vi-order-list.tsx:609 · strategy-status-card.tsx:473, 492
- components/trading/card: stock-info-modal.tsx:132, 179 · card-body.tsx:309, 315
- components/trading/workbench: vi-trigger-strip.tsx:160, 168 · shared-panels.tsx:256 · breakout-strip.tsx:353 · vi-settings-rows.tsx:525
- components/watchlist: watchlist-skeleton.tsx:36 · watchlist-table.tsx:81

가드가 못 잡는 구분선 3곳도 hairline 으로(같은 치환):
- `ui/separator.tsx` 의 `shrink-0 bg-[var(--border)]` → 배경색을 `--border-subtle` 로(Separator = 구분선 프리미티브). 파일 머리 주석의 background 설명도 같이.
- `trading/card/manual-order-form.tsx` 잠김 입력 분기의 테두리 → `--border-subtle`(vj1 의도 「잠김 = 투명 + 헤어라인」 = TDS 입력 hairline). 편집 가능 분기의 `--input` 은 그대로.
- `stock/stock-limit-up-section.tsx` KPI 격자(`gap-px ... bg-[var(--border)]`)의 배경 → `--border-subtle`(gap-px 격자선 = 구분선).

바꾸지 않는 것(면·컨트롤·트랙 — 무테/현상 유지): 4면 `border` 외곽선 54줄 전부, `ui/sheet.tsx:75`(시트 가장자리 = 면), `ui/tooltip.tsx` 화살표 stroke, `layout/user-section.tsx` 팝오버 외곽, `orderbook/order-panel.tsx:442`·`account-panel.tsx:642`·`relay-status-bar.tsx:43-44` 컨트롤 외곽, `color-mix(… var(--border))` 혼합 전부(hover·dirty·destructive), `home-header.tsx` 범위 슬라이더 트랙, `alert-toasts.tsx` 진행 바 트랙, `vi-settings-rows.tsx:774` 토글 꺼짐 트랙, `theme-detail-client.tsx` 가운뎃점 글자색, `src/app/design/**` 카탈로그.

테스트 갱신: `card-body.test.tsx` 의 좌 pane 테두리 색 단언(현재 `--border`)을 `border-[var(--border-subtle)]` 로 옮긴다(`border-r` 단언과 두 pane 형제 단언은 그대로 — 동작 단언 약화 금지).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp run test src/styles/__tests__/tds-tokens.test.ts src/components/trading/__tests__/card-body.test.tsx && grep -q 'bg-\[var(--border-subtle)\]' webapp/src/components/ui/separator.tsx && grep -q 'border-\[var(--border-subtle)\] bg-transparent' webapp/src/components/trading/card/manual-order-form.tsx && grep -q 'gap-px.*bg-\[var(--border-subtle)\]' webapp/src/components/stock/stock-limit-up-section.tsx && grep -q '@min-\[830px\]/lc:grid-cols-\[400px_minmax(0,1fr)\]' webapp/src/components/trading/card/card-body.tsx</automated>
  </verify>
  <done>가드가 RED(45줄) → GREEN(0줄). 특수 3곳 hairline. 4면 면 외곽선은 한 줄도 바뀌지 않음(`var(--border)` 를 쓰는 4면 `border` 줄 수가 스윕 전후 동일 — SUMMARY 에 전후 개수 기록). card-body 테스트 통과.</done>
</task>

<task type="auto">
  <name>Task 3: TDS 타입 스케일 · 버튼 크기(탭·스탯·히어로·폰 CTA·주문 버튼) + 전체 게이트</name>
  <files>webapp/src/components/stock/stock-detail-tabs.tsx, webapp/src/styles/globals.css, webapp/src/components/stock/stock-stats-grid.tsx, webapp/src/components/stock/stock-hero.tsx, webapp/src/components/trading/card/manual-order-form.tsx, webapp/src/components/stock/__tests__/stock-detail-tabs.test.tsx</files>
  <action>
(0PF-C) 모두 `lc` 밖 표면이다 — 단, 주문 버튼(0PF-D)만 `lc` 안이라 높이만 바꾼다.

1) `stock-detail-tabs.tsx`
- 탭 트리거 글자 16 → `text-[17px]`(t5 17/600 — 굵기 `font-semibold`·높이 50·`px-3`·밑줄 채널 불변). 폰 360 에서 4라벨(차트·호가주문·종목정보·뉴스토론) 합 ≈ 14자 × 17 + 패딩 96 ≈ 334px < 컨테이너 344 이고 `overflow-x-auto` 도 남아 있다.
- 폰 CTA 버튼(TDS BottomCTA = xlarge): `h-[56px]` · `rounded-[16px]`(유지) · `text-[17px]` · `font-semibold`(유지). 바 래퍼: 좌우 `px-5`(20), 하단 `pb-[max(20px,env(safe-area-inset-bottom))]`(「하단 20/safe-area」를 max 로 해석 — 노치 없는 폰 20, 노치 폰은 safe-area 만큼), 위 `pt-2.5`·그라디언트·`fixed`·`md:hidden`·`data-slot` 불변.
- 바 높이가 10 + 56 + max(20, safe) 가 되므로 Tabs 루트의 CTA 예약 여백을 `max-md:pb-[calc(76px+max(20px,env(safe-area-inset-bottom)))]`(바 + 10) 로 바꾼다(기존 고정 84 는 노치 폰에서 safe-area 만큼 가려졌다 — 같은 식으로 함께 교정).
- B CTA 주석에 TDS BottomCTA 근거(xlarge 56/r16/t5 · 좌우 20 · 하단 20/safe-area)와 위 높이 식을 적는다.

2) `globals.css` 챗 FAB 들어올림 규칙(§3.9 끝 `body:has([data-slot="detail-order-cta-bar"]) [data-slot="chat-fab"]`): `bottom: calc(70px + max(20px, env(safe-area-inset-bottom, 0px)))` — CTA 바 윗변(66 + max(20, safe)) 위 4px, 기존 간격 유지. 주석을 같은 식으로. §2.2b 블록은 건드리지 않는다.

3) `stock-stats-grid.tsx`: 시가·저가·고가 / 거래량·거래대금·시가총액 두 줄의 라벨 크기 13.5 → `text-[15px]`(t6 15/400, 보조색 `--muted-fg` 유지). 값은 `text-[16px] font-medium` 그대로(st10 16/500 선택 — RESEARCH 가 st10 16 또는 t5 17 을 허용, 3열 격자 값 줄의 가로 증가를 피하려고 16). 폰 3열 칸 ≈ 104px 에 4자 라벨 15px ≈ 60px 이라 여유.

4) `stock-hero.tsx`: 종목명 `h1` 크기 18 → `text-[17px]`(스케일 밖 값을 t5 로 스냅 — 줄어드는 방향, 굵기 600 유지). 가격 30/700 · 등락 15/500 · 코드 칩 12 는 이미 TDS(t1 · t6 · st12)라 불변.

5) `manual-order-form.tsx` 주문 버튼(`OrderButton` 류 — `rounded-[14px] border px-px text-[13px]` 가 있는 클래스 줄): 높이 52 → `h-[48px]`(TDS large 48 · radius 14 는 이미 일치). **글자 13px · `px-px` · `leading-[1.15]` · `whitespace-normal` · `@min-[700px]/lc:` 변형 전부 불변** — TDS large 글자(t5 17)는 `lc` 가로 증가 0 규칙으로 미적용(SUMMARY 에 기록). 폰에서 「예약매수」 2줄 접힘 = 13 × 1.15 × 2 ≈ 30px < 48 이라 안 잘린다. 근처 주석에 52 가 적혀 있으면 48(TDS large)로 고친다.

6) 테스트: `stock-detail-tabs.test.tsx` 의 CTA Test 7 에 버튼 className 이 `h-[56px]` 와 `rounded-[16px]` 를 포함한다는 단언, 탭 트리거 하나가 `text-[17px]` 를 포함한다는 단언을 추가(기존 전환·pushState·scroll 단언은 그대로).

7) 전체 게이트: `pnpm --filter @gh-radar/webapp run typecheck` → `run test`(전체) → `run build`. 빌드 뒤 컴파일 CSS 에서 새 임의값 두 개(`padding-bottom:max(20px,env(safe-area-inset-bottom))` · `padding-bottom:calc(76px + max(…))`)가 생성됐는지 확인한다(아래 verify). 실패 테스트는 이 플랜이 바꾼 값을 단언하던 것만 의미를 옮겨 고친다 — 동작 단언을 지우거나 약화하지 않는다.

8) SUMMARY(`.planning/quick/260925-0pf-tds-theme-toss-b/260925-0pf-SUMMARY.md`)에 반드시: (i) 게이트 결과(테스트 파일 수·통과 수·기준선 대비 증감), (ii) 재량 결정 — 다크 선택 면 grey300 명도비 근거 · `--faint` 비활성 역할 · `--flat` 짝 · 차트 축 grey500/grid grey100 · hairline = 기존 `--border-subtle` 재사용(새 토큰 0)과 `--border` 유지 이유 · 값 16 유지 · 종목명 17 스냅 · 주문 버튼 48 과 글자 13 유지 · BottomCTA 하단 max 해석, (iii) `lc` 안에서 적용하지 않은 TDS 수치 목록, (iv) 오케스트레이터 스크린샷 점검 목록 — 다크/라이트 × 종목상세 4탭(폰 360·390 탭 바 한 줄, CTA 56·FAB 비겹침·마지막 콘텐츠 비가림) · 작업대 3단 호가표 컨테이너 400(가격 잘림 0, 새 hairline 행 구분선) · 본문 700 폼(주문금액·라벨 잘림 0) · 폰 수동주문 48px 버튼 2줄 접힘 · 다크 세그먼트/알약 선택 식별 · 면에 테두리가 새로 생기지 않았는지(홈·챗·테마·뉴스·토론), (v) **제안 한글 커밋 메시지 1개**(HEAD 대비 working tree 전체 · Co-Authored-By 없음 · 예: `feat(webapp): 토스 B 테마를 공식 TDS 값으로 정렬 — 팔레트·텍스트 역할·hairline 구분선·타입 스케일·CTA/주문 버튼 크기`)와 변경 파일 목록. **커밋·push 하지 않는다.**
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp run test && pnpm --filter @gh-radar/webapp run build && grep -q 'h-\[56px\]' webapp/src/components/stock/stock-detail-tabs.tsx && grep -q 'text-\[17px\]' webapp/src/components/stock/stock-detail-tabs.tsx && grep -q 'text-\[15px\]' webapp/src/components/stock/stock-stats-grid.tsx && grep -q 'h-\[48px\]' webapp/src/components/trading/card/manual-order-form.tsx && grep -q 'calc(70px + max(20px' webapp/src/styles/globals.css && grep -rqE 'padding-bottom: ?max\(20px, ?env\(safe-area-inset-bottom\)\)' webapp/.next/static/css && grep -rqE 'padding-bottom: ?calc\(76px ?\+ ?max\(20px, ?env\(safe-area-inset-bottom\)\)\)' webapp/.next/static/css && grep -q '\[--lw:76px\]' webapp/src/components/trading/limit-chaser-form.tsx && grep -q '@min-\[992px\]/lc:\[--lw:104px\]' webapp/src/components/trading/limit-chaser-form.tsx && grep -q 'createPortal' webapp/src/components/trading/limit-chaser-form.tsx && sed -n '/§2.2b 상따 화면 반응형/,/§2.3 글로벌 베이스라인/p' webapp/src/styles/globals.css | shasum -a 256 | grep -q '^c7eb5ddde37f19180a0595602fbb4a8130dd3e72c06a33344662e35300ea254f'</automated>
  </verify>
  <done>typecheck 0 · 전체 vitest 통과(기준선 1681 + 신규) · build 성공 · 컴파일 CSS 에 새 CTA 여백 두 식 존재 · §2.2b 블록 diff 0 · `--lw`·포털·card-body 열 폭 그대로. SUMMARY 에 게이트·재량·스크린샷 목록·제안 커밋 메시지. 커밋 0 · push 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 화면 표시 → 트레이더 판단 | 새 네트워크·입력·저장 경로 0(순수 시각 값 교정). 위험은 표시 무결성(잘림·선택 상태 오판·가림)이 오발주로 이어지는 것뿐 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0pf-01 | Tampering(표시 무결성 → 오발주) | `lc` 카드(사다리·폼·수동주문 버튼) | high | mitigate | `lc` 안은 색·세로 높이만 변경(구분선 색 · 주문 버튼 48), 글자·가로 패딩·열 폭·`--lw` 불변. T3 verify 가 `--lw` 76/104 · 포털 · card-body 400 열 · §2.2b diff 0 을 게이트. 스크린샷 목록에 컨테이너 400/700/폰 360·390 |
| T-0pf-02 | Tampering(선택 상태 오판) | 다크 세그먼트·알약 선택 면 | medium | mitigate | 공식 후보 중 트랙 대비 명도비가 현재(1.47)보다 높은 grey300(1.66)을 선택, hover(grey200)와 선택 분리. tds-tokens.test.ts 가 값 고정 |
| T-0pf-03 | Denial of Service(조작 불가 — 가림) | 폰 CTA 바 · 챗 FAB · 마지막 콘텐츠 | medium | mitigate | 바 높이 식(10 + 56 + max(20, safe))에서 FAB(70 + max)·Tabs 예약(76 + max)을 유도, 컴파일 CSS 에 두 식이 생성됐는지 T3 verify 에서 확인 |
| T-0pf-04 | Tampering(무테 회귀) | `var(--border)` 소비처 스윕 | low | mitigate | 스윕 대상을 「방향 토큰 + 4면 토큰 없음」 규칙으로 한정하는 가드 테스트, 4면 외곽선 줄 수 전후 동일 기록, `--border` 값 불변 |
</threat_model>

<verification>
- T1: tds-tokens.test.ts(토큰 값·교차 단언) + chart-colors.test.ts RED → GREEN.
- T2: 행 구분선 가드 RED(45) → GREEN(0), 특수 3곳 grep, card-body 테스트.
- T3: typecheck · 전체 vitest · next build · 컴파일 CSS 임의값 2식 · 불변식 grep · §2.2b diff 0.
- 커밋 0 · push 0 (`git log -1` 이 여전히 f8280a4).
</verification>

<success_criteria>
- 두 테마의 모든 테마 색 토큰이 TDS 공식 팔레트 값(또는 그 rgba 틴트)이고 `:root`·`.dark` 집합이 대칭이다.
- 행 구분선은 TDS hairline 으로 보이고, 면에는 새 테두리가 생기지 않는다.
- 종목상세 탭·스탯 라벨·히어로 이름·폰 CTA·주문 버튼이 TDS 타입 스케일/버튼 크기를 따른다(`lc` 가로 증가 0 예외는 SUMMARY 에 명시).
- 차트 팔레트가 CSS 토큰과 같은 공식 hex 로 고정되고 테스트가 그 동기를 지킨다.
- typecheck · 전체 테스트 · build 통과, 미커밋 상태 + 제안 커밋 메시지.
</success_criteria>

<output>
Create `.planning/quick/260925-0pf-tds-theme-toss-b/260925-0pf-SUMMARY.md` when done — 게이트 결과, 재량 결정, `lc` 미적용 TDS 수치, 스크린샷 점검 목록, 제안 한글 커밋 메시지(커밋·push 하지 않음)를 반드시 포함.
</output>
