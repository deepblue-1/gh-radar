---
phase: quick-260925-o1s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/deploy-relay.sh
  - ops/alert-relay-down.yaml
  - docs/relay-operations.md
  - .planning/quick/260925-o1s-relay-alert-doc-size/260925-o1s-verify.sh
autonomous: true
requirements: [O1S-DOC, O1S-MEASURE, O1S-ALERTONLY, O1S-APPLY]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`ops/alert-relay-down.yaml` 의 documentation.content(YAML 파싱 후 문자열의 UTF-8 바이트)가 9,500 이하다 — 원래 10,834, 모의 교체 실측 9,039. 측정값을 SUMMARY 에 적는다 (O1S-DOC · O1S-MEASURE)"
    - "정책의 문서 밖 필드(조건 2개 · 임계 0.9 · 싱가포르 필터 · combiner AND · enabled · notificationChannels 자리표시자 · alertStrategy)는 기준 커밋 1978d9b 와 구조적으로 같고, YAML 텍스트에서 `  mimeType:` 줄부터 파일 끝까지는 바이트 단위로 같다 (O1S-DOC)"
    - "알림 문서 8번은 4줄 이하 요약이고 `docs/relay-operations.md 「관찰자 기록 연결 (Phase 19)」 → 「상태별 대응」` 링크를 담는다. 범위 좁히기 표(journal 3행 포함)와 healthz `curl` 명령은 그대로 남는다 (O1S-DOC)"
    - "옛 8번의 사실(로그 tail -50 · `[JOURNAL]`/`[journal]` 구분 · 아침 확인 · 호가·주문 무영향 · 해시 대조 · 관찰자 로그인(5) 구버전 · 30초 간격 · lagSeq · db_error SQL 2개 · 유실 없음 · seqRegressions 비-503)이 전부 `docs/relay-operations.md` 「관찰자 기록 연결 (Phase 19)」 안에 있다 (O1S-DOC)"
    - "`deploy-relay.sh --alert-only` 는 gcloud 가드 → 알림 정책 단계만 돈다: SUPABASE_URL 불필요, `compute ssh`·docker·uptime·secrets 호출 0. 기본(전체) 경로는 기준 커밋 스크립트와 gcloud/docker 호출 기록·출력이 같다 — stub 하네스로 증명 (O1S-ALERTONLY)"
    - "라이브 정책 `gh-radar-relay-down` 의 documentation.content 가 로컬 파싱 결과와 바이트 단위로 같고 「상태별 대응」을 담는다. 적용 전/후 describe 에서 conditions(자동 부여 name 제외)·combiner·enabled·notificationChannels·alertStrategy·displayName 이 같다 (O1S-APPLY)"
    - "executor 는 git 커밋·push 를 하지 않고, relay/server/webapp 배포도 하지 않으며, Phase 19 산출물(19-*.md · ROADMAP · STATE 의 Phase 19 부분)을 건드리지 않는다"
  artifacts:
    - path: "scripts/deploy-relay.sh"
      provides: "`--alert-only` 모드 + 알림 정책 단계를 함수 `apply_alert_policy` 로 추출(Section 6 과 alert-only 공용)"
      contains: "apply_alert_policy"
    - path: "ops/alert-relay-down.yaml"
      provides: "8번 4줄 요약 + 운영 문서 링크, documentation ≤ 9,500 바이트"
      contains: "「상태별 대응」"
    - path: "docs/relay-operations.md"
      provides: "「관찰자 기록 연결 (Phase 19)」에 알림 문서 크기 규칙 · --alert-only · 「상태별 대응」 상세 절차"
      contains: "상태별 대응 (알림 8번 상세)"
    - path: ".planning/quick/260925-o1s-relay-alert-doc-size/260925-o1s-verify.sh"
      provides: "하위명령 stub · size · precheck · live-diff — 실패 시 exit 1"
  key_links:
    - from: "scripts/deploy-relay.sh Section 6 (전체 경로)"
      to: "apply_alert_policy"
      via: "인라인 블록을 함수 호출 한 줄로 대체 — 채널 치환(sed) → RESOLVED_YAML → list 후 update-or-create 로직은 한 벌만 존재"
    - from: "scripts/deploy-relay.sh `--alert-only` 분기"
      to: "apply_alert_policy"
      via: "Section 2 상수 직후(SHA · SUPABASE_URL 가드 · DMA_HOST SSH 조회보다 앞)에서 호출 후 exit 0"
    - from: "ops/alert-relay-down.yaml 8번 요약"
      to: "docs/relay-operations.md `## 관찰자 기록 연결 (Phase 19)` · `**상태별 대응 (알림 8번 상세).**`"
      via: "링크 문자열의 절 이름이 실제 헤딩/굵은 제목과 글자 그대로 일치"
---

<objective>
relay 알림 정책 `gh-radar-relay-down` 의 documentation 이 GCP 상한(10,240 바이트)을 넘어(10,834) `deploy-relay.sh` 마지막 단계가 exit 1 로 끝나는 문제를 고친다.

1. 알림 문서 8번(관찰자 기록 연결 이상, 약 2,463 바이트)의 상세 절차를 `docs/relay-operations.md` 「관찰자 기록 연결 (Phase 19)」로 옮겨 합치고, 알림 문서에는 4줄 요약 + 링크만 남겨 9,500 바이트 이하로 만든다. 범위 좁히기 표 · healthz 명령 등 1차 대응 명령은 알림에 남긴다. 정책 조건 · 임계값(0.9 · 싱가포르 AND) · 알림 채널은 건드리지 않는다.
2. relay 재배포 없이 정책만 갱신하는 `deploy-relay.sh --alert-only` 를 추가한다(전체 경로 동작 불변).
3. `--alert-only` 로 라이브 정책을 갱신하고, 적용 전/후 describe 를 비교해 문서만 바뀌었음을 증명한다.

Purpose: 앞으로 모든 relay 배포가 알림 단계에서 exit 1 로 끝나는 상태를 없애고, 관찰자 장애 대응 절차를 잃지 않는다.
Output: 수정된 3개 파일 + 검증 스크립트 1개 + SUMMARY(측정값 · 라이브 비교 결과). **커밋은 하지 않는다** — 오케스트레이터가 사용자 확인 후 한 번에 커밋한다.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@ops/alert-relay-down.yaml
@scripts/deploy-relay.sh
@docs/relay-operations.md

## 계획 시점 실측 (2026-09-25)

- 기준 커밋 `1978d9b` — 이 3개 파일(`scripts/deploy-relay.sh` · `ops/alert-relay-down.yaml` · `docs/relay-operations.md`)은 작업 트리와 차이 0 (계획 시 `git diff --quiet 1978d9b -- …` 확인).
- 크기 측정 방법(GCP 가 받는 문자열 그대로): `ruby -ryaml -e 'print YAML.load_file(ARGV[0])["documentation"]["content"].bytesize' ops/alert-relay-down.yaml` → 현재 **10834** (19-12 의 GCP 오류 값과 일치). python3 에는 PyYAML 이 없다 — YAML 파싱은 시스템 ruby(2.6, Psych)로, JSON 비교는 python3 로 한다.
- 절별 바이트: 머리+범위 좁히기 표 1,826 · 1번 812 · 2번 966 · 3번 523 · 4번 494 · 5번 295 · 6번 220 · 7번 235 · **8번 2,463** · 임계값과 오탐 831 · 싱가포르 AND 1,452 · 설계 메모 608.
- 8번(YAML 75~94행)을 아래 「확정 문안 A」로 바꾼 모의 파일 실측 = **9,039 바이트**(조건 2개 파싱 정상). 다른 절은 옮길 필요 없다. 특히 「싱가포르 AND 조건」은 `infra/relay/README.md` 254행이 근거 문서로 가리키므로 옮기지 않는다.
- 라이브 정책: `projects/gh-radar/alertPolicies/7995724305267722560`(`infra/relay/README.md` 254행). 라이브 문서는 8번이 없는 옛 판이다(19-12 갱신 실패로 그대로 유효).
- gcloud: 활성 config `gh-radar` · 계정 `gh-radar-deployer@gh-radar.iam.gserviceaccount.com` — `deploy-relay.sh` Section 1 가드를 통과한다.
- `deploy-relay.sh` 최상위 요구 사항: `GCP_PROJECT_ID`(가드 — alert-only 에도 필요) · `SUPABASE_URL`(`:?` 가드, 97행 — alert-only 에는 무관) · `read_live_dma_host` 의 IAP SSH(117행 부근 — alert-only 에는 무관하고 느림). `DMA_HOST` · `LOG_LEVEL` 은 선택. 알림 단계는 453~480행, `ALERT_POLICY` 상수는 89행, `RELAY_SA` 90행, `SHA=` 92행.
- 기준 커밋 이후 조건 부분은 `2e3a869`(2026-09-17 싱가포르 AND) 이후 바뀐 적이 없다 — 로컬 조건 = 라이브 조건이어야 정상.
- 작업 트리에 이 quick 과 무관한 변경(`.planning/state.json` · `.planning/milestone.lock` · `.planning/sketches/`)이 있다. 건드리지 않는다.

## 확정 문안 A — `ops/alert-relay-down.yaml` 8번 교체본

8번 전체(현재 75행 `    8. **관찰자 기록 연결(Phase 19) 이상**` 부터 94행 `         docs/relay-operations.md 「관찰자 기록 연결」 참조.` 까지)를 아래 4줄로 바꾼다. 들여쓰기는 그대로(첫 줄 4칸, 이어지는 줄 7칸). 95행의 빈 줄과 96행 `검증:` 줄은 그대로 둔다.

```
    8. **관찰자 기록 연결(Phase 19) 이상** — healthz `journal.state` 가 live 가 아니다. 주문 기록만 멈추고 호가·주문은 영향 없다.
       장중(평일 08:00~20:00 KST)에만 503(`rejected` 즉시 · 나머지 180초 뒤). 장 밖에서는 200 이고 본문 `journal` 에만 드러난다.
       `rejected` 는 관찰자 비밀 불일치다 — 값을 맞춘 뒤 relay 재배포. 로그: `sudo docker logs gh-radar-relay 2>&1 | grep -E '\[JOURNAL\]|\[journal\]' | tail -50`.
       상태별 대응(connecting·replaying·db_error SQL·seq 역행) 상세: docs/relay-operations.md 「관찰자 기록 연결 (Phase 19)」 → 「상태별 대응」.
```

## 확정 문안 B — `docs/relay-operations.md` 「관찰자 기록 연결 (Phase 19)」 합치기

현재 43~45행의 `- **알림.** …` 불릿(3줄) 전체를 아래 블록으로 바꾼다. 앞뒤 불릿(「로그인 거부」 · 「전환 순서」 · 「롤백」 등)은 그대로 둔다. 기존 절에 이미 있는 사실(커서·epoch · 유실 0 근거 · resync · seq 역행 · 로그인 거부 정지 · 전환 순서 · 롤백)은 다시 쓰지 않고 「위 「…」」로 가리킨다.

```
- **알림.** 장중(평일 08:00~20:00 KST · 휴장일 제외)에 `rejected` 는 즉시, 그 밖의 비-live 는 180초 뒤 `/healthz` 503 → 기존 uptime
  `gh-radar-relay-healthz` · 정책 `gh-radar-relay-down`(≈ 3분 + 5분 창). 장 밖에서는 200 이고 본문 `journal` 에만 드러난다 — 밤사이
  끊김은 알림이 아니라 아침 확인 대상이다. 끊겨도 호가·주문 자체는 영향이 없고, 주문 기록만 DB 에 들어오지 않는다.
  relay 로그는 Cloud Logging 에 없다 — `sudo docker logs gh-radar-relay 2>&1 | grep -E '\[JOURNAL\]|\[journal\]' | tail -50`.
  `[JOURNAL]` 은 관찰자(연결·로그인·상태 전이), `[journal]` 은 기록기(적용 RPC·커서·재동기화)다.
- **알림 문서는 요약만 둔다.** `ops/alert-relay-down.yaml` 의 documentation 은 GCP 상한이 **10,240 바이트**다. 넘으면
  `deploy-relay.sh` 가 컨테이너를 새로 띄운 뒤 정책 갱신 단계에서 exit 1 로 끝난다(19-12 에서 10,834 바이트로 실제 발생).
  그래서 8번은 4줄 요약 + 이 절 링크만 두고 상세는 아래 「상태별 대응」에 쓴다. 문서를 늘릴 땐 9,500 바이트 이하를 지킨다
  (측정: `ruby -ryaml -e 'print YAML.load_file("ops/alert-relay-down.yaml")["documentation"]["content"].bytesize'`).
  문서만 고쳤으면 relay 재배포 없이 `GCP_PROJECT_ID=gh-radar NOTIFICATION_CHANNEL_ID=<채널 ID> bash scripts/deploy-relay.sh --alert-only`
  로 정책만 갱신한다(빌드·VM 배포·uptime check 를 건너뛴다).
- **상태별 대응 (알림 8번 상세).** healthz 본문 `journal.state` 기준. `vpn:false` 가 같이 보이면 알림 문서 2번(VPN)이 먼저다.
  - `rejected` — 게이트웨이가 관찰자 로그인을 거부했다. relay 재시작 전에는 다시 시도하지 않는다(위 「로그인 거부」). 두 비밀
    (Secret Manager `gh-radar-dma-observer-secret` · 게이트웨이 `config/observer.toml`)이 같은지 **해시로** 대조하고(값을 화면에
    찍지 말 것 — `infra/relay/README.md` §Secret 4종 값 주입), 맞춘 뒤 relay 를 재배포(또는 컨테이너 재시작)한다.
  - `connecting` · `logging_in` · `replaying` 이 오래간다 — 게이트웨이가 관찰자 로그인(5)을 모르는 구버전이면 로그인 응답
    타임아웃이 반복된다. 게이트웨이 재시작 중이면 스스로 붙는다(재접속 상한 없음 · 30초 간격). `replaying` 이 길면 `lagSeq` 가
    줄고 있는지 본다.
  - `db_error` — 관찰자는 붙었지만 적용 RPC(`dma_journal_apply`)가 연속 실패한다. Supabase SQL 편집기에서
    `select * from dma_journal_cursor;` 로 커서(`last_seq` · `updated_at`)가 멈췄는지,
    `select seq, apply_error from dma_journal_events where apply_error is not null order by seq desc limit 20;` 로 투영 실패가
    쌓였는지 본다. 적용 RPC 가 실패하는 동안 커서는 전진하지 않으므로 **유실은 없다** — 복구되면 같은 배치부터 다시 적용된다.
  - `journal.seqRegressions` > 0 — **503 이 아니다**(표시 신호). 위 「seq 역행 신호」.
```

## 검증 스크립트 계약 — `.planning/quick/260925-o1s-relay-alert-doc-size/260925-o1s-verify.sh`

`#!/usr/bin/env bash` + `set -euo pipefail`, 시작 시 `cd "$(git rev-parse --show-toplevel)"`. 하위명령 4개, 실패는 원인 한 줄 + exit 1, 통과는 `<NAME> PASS …` 한 줄. `BASE_REF="${BASE_REF:-1978d9b}"`. YAML 은 ruby(`-ryaml -rjson`), JSON 은 python3. 임시 파일은 `mktemp -d` + `trap` 정리. Task 1 이 `stub` 을, Task 2 가 `size` 를, Task 3 이 `precheck` · `live-diff` 를 채운다.

- **`stub`** — PATH 맨 앞에 가짜 `gcloud` · `docker` 를 둔 임시 디렉터리를 만들어 실제 GCP·docker 에 절대 닿지 않게 한다. 가짜들은 인자 전체를 `$STUB_LOG` 에 한 줄씩 적는다. 가짜 `gcloud` 응답: `config configurations list` → `gh-radar` · `config get-value project` → `gh-radar` · `compute ssh` 인자에 `docker inspect` 가 있으면 `10.41.1.120`, 그 밖의 `compute ssh` → 0 · `compute instances describe` → `RUNNING` · `secrets describe` → 0 · `secrets versions list` → `1` · `secrets get-iam-policy` → `serviceAccount:gh-radar-relay-sa@gh-radar.iam.gserviceaccount.com` · `compute firewall-rules list` → 탭 구분 4행(`relay-allow-https`·`relay-allow-iap-ssh`·`relay-allow-internal-order`·`relay-allow-wireguard`, 둘째 열 `radar-gw`) · `auth configure-docker` → 0 · `monitoring uptime list-configs` → 가짜 uptime 이름 · `monitoring uptime update` → 0 · `alpha monitoring policies list` → `STUB_NO_POLICY=1` 이면 빈 출력, 아니면 `projects/gh-radar/alertPolicies/7995724305267722560` · `alpha monitoring policies update|create` → `--policy-from-file=` 값의 파일을 `$STUB_OUT/resolved.yaml` 로 복사 후 0 · 그 밖의 모든 호출 → stderr `unexpected gcloud: …` + exit 97. 가짜 `docker` 는 기록만 하고 0. 시나리오(각각 별도 로그 파일):
  - S1 전체 경로 회귀: `git show $BASE_REF:scripts/deploy-relay.sh` 를 임시 파일로 뽑아 기준판과 새 판을 같은 env(`GCP_PROJECT_ID=gh-radar` · `SUPABASE_URL=https://stub.supabase.co` · `NOTIFICATION_CHANNEL_ID=123`, `DMA_HOST` 미설정)로 각각 인자 없이 실행. 둘 다 exit 0, 호출 로그(`--policy-from-file=<경로>` 를 고정 문자열로 정규화)가 같고, stdout+stderr 합본이 같다. 새 판 로그에서 `monitoring uptime update` 가 `alpha monitoring policies update` 보다 앞에 있다. 두 판의 captured `resolved.yaml` 이 `cmp` 로 같다.
  - S2 `--alert-only`: `SUPABASE_URL` 을 **unset**(env -u), `GCP_PROJECT_ID=gh-radar` · `NOTIFICATION_CHANNEL_ID=123`. exit 0, 로그에 `alpha monitoring policies list` 와 `alpha monitoring policies update` 가 있고 `compute ssh` · `compute instances` · `secrets` · `firewall-rules` · `monitoring uptime` 은 0건, docker 로그는 비어 있다. captured `resolved.yaml` 에 `projects/gh-radar/notificationChannels/123` 이 있고 `${NOTIFICATION_CHANNEL_ID}` 는 없으며 S1 의 것과 `cmp` 로 같다. 그 파일의 documentation 바이트를 출력한다(정보용).
  - S2b 생성 갈래: S2 와 같되 `STUB_NO_POLICY=1` → 로그에 `alpha monitoring policies create` 가 있다.
  - S3 `--alert-only` + `NOTIFICATION_CHANNEL_ID` 미설정 → exit ≠ 0, 로그에 `policies update`/`create` 0건.
  - S4 알 수 없는 인자 `--bogus` → exit 1, stderr 에 `usage:` 와 `--alert-only`.
  - S5 기본 모드 + `SUPABASE_URL` unset → exit ≠ 0, stderr 에 `SUPABASE_URL must be set`, 로그에 `compute ssh` 0건(가드 순서 불변).
  통과 시 `STUB PASS`.
- **`size`** — (1) ops 파일 documentation 바이트 ≤ 9500(값 출력) (2) 기준판(`git show $BASE_REF:ops/alert-relay-down.yaml`)과 새 판을 ruby 로 파싱해 `documentation.content` 만 지운 뒤 구조 동등 (3) 두 판에서 `^  mimeType:` 줄부터 끝까지 바이트 동일 (4) 파싱된 content 에서 `8. ` 로 시작하는 줄부터 이어지는 비공백 줄 수 ≤ 4 (5) ops 파일(주석 줄 `^\s*#` 제외)에 `「상태별 대응」` · `docs/relay-operations.md 「관찰자 기록 연결 (Phase 19)」` · `"state":"rejected"` · `"state":"db_error"` · `disconnectedSec` · `curl -s -w` 가 있고, `lagSeq` · `apply_error is not null` · `관찰자 로그인(5)` 은 0건 (6) `docs/relay-operations.md` 에 고정 문자열 13개(`| tail -50` · `은 관찰자(연결·로그인·상태 전이)` · `아침 확인 대상` · `호가·주문 자체는 영향이 없` · `**해시로** 대조` · `관찰자 로그인(5)` · `30초 간격` · `lagSeq` · `apply_error is not null order by seq desc limit 20` · `**유실은 없다**` · `상태별 대응 (알림 8번 상세)` · `--alert-only` · `10,240 바이트`)가 각각 1회 이상, 헤딩 `## 관찰자 기록 연결 (Phase 19)` 가 정확히 1회. 통과 시 `SIZE PASS bytes=<N>`.
- **`precheck <before.json>`** — 로컬 YAML(채널 자리표시자를 `projects/gh-radar/notificationChannels/14409521670382124894` 로 치환)의 문서 밖 필드가 라이브에 **부분집합으로 일치**하는지 본다: displayName · combiner · enabled · alertStrategy.autoClose · notificationChannels 가 같고, 조건은 displayName 으로 짝지어 로컬 `conditionThreshold` 의 모든 키가 라이브에 같은 값으로 있다(`filter` 는 공백을 한 칸으로 접어 비교, 숫자는 float 비교). 라이브에만 있는 키는 출력만 한다. 불일치면 `PRECHECK STOP — 적용하면 문서 밖 설정이 바뀐다` + exit 1. 통과 시 `PRECHECK PASS`.
- **`live-diff <before.json> <after.json>`** — (1) 투영 P = {displayName, combiner, enabled, notificationChannels, alertStrategy, severity, userLabels, conditions(각 원소에서 `name` 제거, displayName 순 정렬)} 이 before/after 에서 같다(다르면 두 투영을 정렬된 JSON 으로 출력) (2) after 의 documentation.content 가 ruby 로 파싱한 로컬 content 와 바이트 동일(두 파일로 써서 `cmp`) (3) after 문서에 `「상태별 대응」` 포함, before 문서와 다름 (4) after 문서 바이트 ≤ 9500. 통과 시 `LIVE PASS before_doc=<B> after_doc=<A>`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: `deploy-relay.sh --alert-only` 끝단까지 — 알림 정책 단계를 함수로 추출하고 stub 하네스로 두 경로를 증명</name>
  <files>scripts/deploy-relay.sh, .planning/quick/260925-o1s-relay-alert-doc-size/260925-o1s-verify.sh</files>
  <read_first>scripts/deploy-relay.sh (1~100행 인자 처리·가드·상수, 440~507행 Section 6·최종 요약)</read_first>
  <precondition>`git diff --quiet 1978d9b -- scripts/deploy-relay.sh ops/alert-relay-down.yaml docs/relay-operations.md` 가 0 이다(다른 세션이 이 파일들을 바꿨으면 중단하고 보고).</precondition>
  <action>
O1S-ALERTONLY. 전체 경로의 동작은 한 글자도 바꾸지 않는 것이 원칙이다 — 기존 로직을 옮기기만 하고 개선(trap 추가 · 메시지 수정 등)은 하지 않는다.

1. 헤더 사용법 주석(「사용법:」 블록, `--rollback` 줄 아래)에 한 줄 추가: `GCP_PROJECT_ID=gh-radar NOTIFICATION_CHANNEL_ID=<채널> bash scripts/deploy-relay.sh --alert-only` — 알림 정책만 갱신(빌드·VM 배포·uptime check 건너뜀, SUPABASE_URL 불필요). 문서(ops/alert-relay-down.yaml)만 고쳤을 때 쓴다는 한 줄 설명을 붙인다.
2. 인자 `case` 에 `--alert-only) MODE=alert-only ;;` 를 추가하고, `*)` 의 usage 문구를 `usage: bash scripts/deploy-relay.sh [--rollback <tag> | --alert-only]` 로 바꾼다.
3. Section 2 상수 블록에서 `ALERT_POLICY=` 바로 아래에 `ALERT_FILE="ops/alert-relay-down.yaml"` 을 둔다(Section 6 의 같은 대입은 제거 — 한 곳에만).
4. `RELAY_SA=` 줄 다음, `SHA=$(git rev-parse …)` 보다 앞에 함수 `apply_alert_policy()` 를 정의한다. 본문은 현재 Section 6 의 알림 블록(NOTIFICATION_CHANNEL_ID 선택 주석 · `if [[ -f "$ALERT_FILE" && -z … ]]` 경고 갈래 · `elif` 의 채널 정규화 → `mktemp` → `sed` 치환 → `policies list` → update-or-create → `rm -f` → `✓ Alert policy ready`)을 **그대로 옮긴 것**이다. 함수 안에서만 쓰는 `CHANNEL_RESOURCE` · `RESOLVED_YAML` · `EXISTING_POLICY` 는 `local` 로 선언한다. 함수 위에 「Section 6(전체 배포)과 --alert-only 가 같은 한 벌을 쓴다 — 두 벌로 적으면 한쪽만 고쳐진다」는 취지의 주석을 단다(기존 `read_live_dma_host` 주석과 같은 어조).
5. 함수 정의 바로 다음에 alert-only 분기를 둔다: `MODE` 가 `alert-only` 이면 (a) `ALERT_FILE` 이 없으면 「저장소 루트에서 실행하세요」 에러 + exit 1 (b) `NOTIFICATION_CHANNEL_ID` 가 비었으면 「--alert-only 는 NOTIFICATION_CHANNEL_ID 가 필요합니다」 에러 + exit 1 — 전체 경로와 달리 여기서는 이것이 유일한 목적이라 건너뛰지 않고 실패한다 (c) `apply_alert_policy` 호출 (d) 「relay 컨테이너 · uptime check 는 건드리지 않았습니다」 한 줄 + exit 0. 이 위치는 `SUPABASE_URL` `:?` 가드와 `read_live_dma_host`(IAP SSH)보다 앞이므로 alert-only 는 둘 다 요구·실행하지 않는다. gcloud 가드(Section 1)는 그대로 통과해야 한다.
6. Section 6 에서 uptime 블록 뒤의 옮겨 간 알림 블록 자리를 `apply_alert_policy` 호출 한 줄로 바꾼다. Section 6 머리 주석과 uptime 블록은 그대로.
7. 검증 스크립트를 만들고 `chmod +x` 한다 — 이 태스크에서는 `<context>` 「검증 스크립트 계약」의 `stub` 하위명령(S1~S5 + S2b)만 구현한다. 나머지 하위명령은 이후 태스크가 채운다(지금은 호출 시 「미구현」 + exit 2). 가짜 gcloud 의 catch-all 이 exit 97 로 막으므로 하네스가 실제 GCP 를 부를 수 없다는 점을 스크립트 머리 주석에 적는다.

git 커밋을 하지 않는다(사용자 규칙 — 오케스트레이터가 사용자 확인 후 커밋).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash -n scripts/deploy-relay.sh && grep -c 'apply_alert_policy' scripts/deploy-relay.sh && bash .planning/quick/260925-o1s-relay-alert-doc-size/260925-o1s-verify.sh stub</automated>
  </verify>
  <done>`bash -n` 통과 · `apply_alert_policy` 가 정의 1 + 호출 2(Section 6 · alert-only) · `ALERT_FILE=` 대입이 파일에 1회 · `verify.sh stub` 이 `STUB PASS` 로 끝난다(S1 에서 기준판과 새 판의 호출 로그·출력 동일, S2 에서 SUPABASE_URL 없이 정책 update 만 호출).</done>
</task>

<task type="auto">
  <name>Task 2: 알림 문서 8번을 운영 문서로 옮겨 documentation 을 9,500 바이트 이하로 — 문서 밖 필드 불변 증명</name>
  <files>ops/alert-relay-down.yaml, docs/relay-operations.md, .planning/quick/260925-o1s-relay-alert-doc-size/260925-o1s-verify.sh</files>
  <read_first>ops/alert-relay-down.yaml (10~25행 범위 좁히기 표, 75~96행 8번과 검증 줄), docs/relay-operations.md (16~52행 「관찰자 기록 연결 (Phase 19)」)</read_first>
  <action>
O1S-DOC · O1S-MEASURE. 사용자 결정: 8번 상세는 `docs/relay-operations.md` 「관찰자 기록 연결 (Phase 19)」로 옮겨 합치고 알림에는 2~4줄 요약 + 문서 경로만, 1차 대응 명령(범위 좁히기 표 · healthz `curl`)은 알림에 유지, 정책 조건·임계값(0.9 · 싱가포르 AND)·알림 채널은 불변 — documentation 만.

1. `ops/alert-relay-down.yaml`: 8번 전체를 `<context>` 「확정 문안 A」 4줄로 바꾼다. 범위 좁히기 표(journal 3행 포함) · 1~7번 · 「검증:」 줄 · 「임계값과 오탐」 · 「싱가포르 AND 조건」 · 「설계 메모」 · `mimeType` 이하 전부는 손대지 않는다. 문안 A 는 모의 교체로 9,039 바이트가 나온 문안이다 — 글자를 늘리지 않는다.
2. `docs/relay-operations.md`: 「관찰자 기록 연결 (Phase 19)」의 `- **알림.**` 불릿을 `<context>` 「확정 문안 B」 블록으로 바꾼다(알림 불릿 개정 + 「알림 문서는 요약만 둔다」 + 「상태별 대응 (알림 8번 상세)」). 파일 제목 · 출처 줄 · Phase 17 사실 목록 · 다른 불릿은 그대로. 옛 8번의 사실이 하나도 빠지지 않았는지 기준판 8번(`git show 1978d9b:ops/alert-relay-down.yaml` 75~94행)과 한 줄씩 대조한다 — 빠진 사실이 있으면 문안 B 의 해당 하위 불릿에 같은 문체로 보탠다(알림 쪽이 아니라 운영 문서 쪽에).
3. 검증 스크립트에 `<context>` 계약의 `size` 하위명령을 구현한다. 부정 grep 대상 문자열은 ops 파일의 주석 줄을 뺀 본문에서만 센다.
4. 측정값 두 개를 SUMMARY 용으로 남겨 둔다: 변경 전 10834(기준판) · 변경 후 `size` 가 출력한 N. 가능하면 교차 확인으로 `stub` 의 S2 가 출력한 resolved.yaml 문서 바이트도 같은 N 인지 본다.

9,500 을 넘으면 다른 절을 옮기지 말고 멈춰서 원인을 보고한다(문안 A 는 이미 여유 461 바이트로 실측됐다). git 커밋을 하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash .planning/quick/260925-o1s-relay-alert-doc-size/260925-o1s-verify.sh size && bash .planning/quick/260925-o1s-relay-alert-doc-size/260925-o1s-verify.sh stub && git diff --stat 1978d9b -- ops/alert-relay-down.yaml docs/relay-operations.md</automated>
  </verify>
  <done>`SIZE PASS bytes=N`(N ≤ 9500, 예상 9039) · `STUB PASS` 유지 · ops YAML 의 문서 밖 구조와 `mimeType` 이하 바이트가 기준판과 동일 · 8번 4줄 이하 + 운영 문서 링크 · `size` (6)의 고정 문자열 13개(옛 8번 사실 11개 + 크기 규칙 · `--alert-only`)가 운영 문서에 존재.</done>
</task>

<task type="auto">
  <name>Task 3: `--alert-only` 로 라이브 정책 갱신 — 적용 전/후 describe 비교로 문서만 바뀌었음을 증명</name>
  <files>.planning/quick/260925-o1s-relay-alert-doc-size/260925-o1s-verify.sh</files>
  <read_first>.planning/quick/260925-o1s-relay-alert-doc-size/260925-o1s-verify.sh (Task 1·2 에서 만든 하위명령)</read_first>
  <precondition>gcloud 활성 config 가 `gh-radar` 이고 `~/.config/gcloud/gh-radar-deployer.json` 이 존재한다(`gcloud config configurations list` · `ls` 로 확인). Task 2 의 `size` · `stub` 가 통과한 상태다.</precondition>
  <reversibility rating="reversible">정책 문서만 바뀐다. 되돌리려면 before 스냅샷의 옛 문서로 다시 update 하면 되고, update 가 실패하면 GCP 는 옛 정책을 그대로 유지한다.</reversibility>
  <action>
O1S-APPLY. relay · server · webapp 배포는 하지 않는다(`--alert-only` 는 빌드·SSH·uptime 을 부르지 않는다 — Task 1 S2 가 증명). 스냅샷은 executor 세션의 scratchpad 디렉터리(없으면 `mktemp -d`)에 둔다 — 이하 `$S`.

1. 검증 스크립트에 `<context>` 계약의 `precheck` · `live-diff` 하위명령을 구현한다.
2. 정책 이름 확정: `gcloud alpha monitoring policies list --filter='displayName=gh-radar-relay-down' --format='value(name)'` 가 정확히 1줄이어야 한다(0 또는 2줄 이상이면 중단·보고 — 스크립트는 `head -1` 이라 엉뚱한 정책을 고칠 수 있다). `infra/relay/README.md` 254행 값(`projects/gh-radar/alertPolicies/7995724305267722560`)과 다르면 SUMMARY 에 적는다(README 는 고치지 않는다).
3. before 스냅샷: `gcloud alpha monitoring policies describe "$POLICY" --format=json` 을 `$S/o1s-before.json` 으로.
4. `verify.sh precheck "$S/o1s-before.json"` — STOP 이면 적용하지 말고 불일치 내용을 그대로 보고한다(이 경우 update 가 조건·임계·채널을 바꾸게 되므로 사용자 결정 사항).
5. 적용: 저장소 루트에서 env `GCP_PROJECT_ID=gh-radar` · `NOTIFICATION_CHANNEL_ID=14409521670382124894`(비밀 아님) · `GOOGLE_APPLICATION_CREDENTIALS="$HOME/.config/gcloud/gh-radar-deployer.json"` · `CLOUDSDK_CORE_PROJECT=gh-radar` 로 `bash scripts/deploy-relay.sh --alert-only` 를 1회 실행한다. `SUPABASE_URL` 은 넣지 않는다(불필요함 자체가 검증 대상). 기대 출력: 가드 ✓ → `▶ updating alert policy` → `✓ Alert policy ready` → exit 0. 실패하면 재시도로 두드리지 말고 오류 전문을 SUMMARY 에 적고 멈춘다. gcloud 네트워크 호출이 샌드박스에 막히면 샌드박스 해제로 같은 명령을 다시 실행한다.
6. after 스냅샷: 같은 describe 를 `$S/o1s-after.json` 으로.
7. `verify.sh live-diff "$S/o1s-before.json" "$S/o1s-after.json"`.
8. `bash -n scripts/deploy-relay.sh` 를 마지막으로 한 번 더 돌린다.
9. SUMMARY(`.planning/quick/260925-o1s-relay-alert-doc-size/260925-o1s-SUMMARY.md`)에 적는다: 크기 측정 방법(ruby YAML 파싱 → documentation.content UTF-8 바이트)과 값(변경 전 10,834 → 변경 후 N, 라이브 before/after 문서 바이트), 옮긴 내용과 위치, `--alert-only` 설계(함수 추출 · 분기 위치 · 요구 env), stub 하네스 결과(S1~S5), 정책 이름, precheck 결과, live-diff 결과(투영 동일 · 문서 동일), `--alert-only` 실행 출력 요약, 커밋하지 않았다는 사실. 스냅샷 JSON 파일 자체는 커밋 대상에 넣지 않는다.

git 커밋·push 를 하지 않는다. STATE.md · ROADMAP.md · Phase 19 산출물을 수정하지 않는다(quick 기록 갱신은 오케스트레이터 몫).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && bash -n scripts/deploy-relay.sh && bash .planning/quick/260925-o1s-relay-alert-doc-size/260925-o1s-verify.sh live-diff "$S/o1s-before.json" "$S/o1s-after.json"</automated>
  </verify>
  <done>`--alert-only` 실행 exit 0 · `PRECHECK PASS` · `LIVE PASS`(before/after 투영 동일, 라이브 문서 = 로컬 content 바이트 동일, 「상태별 대응」 포함, ≤ 9,500) · SUMMARY 에 측정값·비교 결과 기록 · 작업 트리에 커밋 없음(`git log -1 --format=%h` 가 여전히 시작 시 HEAD).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 개발기 → GCP Cloud Monitoring API | deployer SA(owner)로 알림 정책을 덮어쓴다. `--policy-from-file` 은 정책 전체 치환이라 문서 외 필드도 바뀔 수 있다 |
| 저장소 문서 → 운영자 | 알림 본문과 운영 문서가 장애 대응 절차의 정본이다. 비밀 값이 섞이면 안 된다 |
| 검증 하네스 → 로컬 셸 | 가짜 gcloud/docker 로 배포 스크립트를 실행한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-o1s-01 | Tampering | `--alert-only` → `policies update --policy-from-file` | high | mitigate | Task 3 `precheck` 이 적용 전에 로컬 문서 밖 필드 ⊆ 라이브를 확인하고, 불일치면 적용하지 않는다. 적용 후 `live-diff` 가 conditions·combiner·enabled·channels·alertStrategy 투영 동일을 증명 |
| T-o1s-02 | Denial of Service | 알림 정책(장애 감지) | high | mitigate | update 는 원자적이라 실패해도 옛 정책이 유지된다. 재시도로 두드리지 않는다. `live-diff` 가 `enabled` 불변을 확인하고, `policies list` 결과가 정확히 1개일 때만 적용(엉뚱한 정책 덮어쓰기 방지) |
| T-o1s-03 | Tampering | `deploy-relay.sh` 전체 경로 리팩터 | medium | mitigate | stub S1 이 기준 커밋 스크립트와 새 스크립트의 gcloud/docker 호출 로그·출력이 같음을 증명. 로직은 함수로 옮기기만 한다 |
| T-o1s-04 | Elevation of Privilege | stub 하네스가 실제 GCP·docker 를 부르는 사고 | medium | mitigate | 가짜 `gcloud`·`docker` 를 PATH 맨 앞에 두고, 알려지지 않은 gcloud 호출은 exit 97 로 실패시킨다 |
| T-o1s-05 | Information Disclosure | 알림 문서·운영 문서 | low | mitigate | 문서에는 비밀 이름·파일 경로·해시 대조 절차만 적고 값은 적지 않는다(확정 문안 A·B). 채널 ID 는 비밀이 아니지만 운영 문서에는 `<채널 ID>` 자리표시자로 둔다 |
| T-o1s-06 | Repudiation | 커밋 | low | accept | executor 는 커밋하지 않는다. 오케스트레이터가 메시지를 보여 주고 사용자 확인 후 한 번에 커밋(한글 · Co-Authored-By 없음) |
</threat_model>

<verification>
- `bash -n scripts/deploy-relay.sh` 통과.
- `verify.sh stub` → `STUB PASS`, `verify.sh size` → `SIZE PASS bytes=N`(N ≤ 9500).
- 라이브: `PRECHECK PASS` → `--alert-only` exit 0 → `LIVE PASS`.
- `git status --short` 에 이 quick 의 변경은 `scripts/deploy-relay.sh` · `ops/alert-relay-down.yaml` · `docs/relay-operations.md` · quick 디렉터리 파일(PLAN · SUMMARY · verify.sh)뿐이고, 커밋은 0.
</verification>

<success_criteria>
- documentation.content 10,834 → ≤ 9,500 바이트(GCP 상한 10,240 아래), 측정값이 SUMMARY 에 있다.
- 옛 8번의 절차가 운영 문서 「관찰자 기록 연결 (Phase 19)」 → 「상태별 대응」에 빠짐없이 있고, 알림에는 4줄 요약 + 링크 + 로그 명령, 범위 좁히기 표와 healthz 명령은 그대로.
- `deploy-relay.sh --alert-only` 가 SUPABASE_URL 없이 알림 정책만 갱신하고, 전체 경로는 기준판과 동작이 같다.
- 라이브 정책 문서가 새 판으로 바뀌었고 조건·임계(0.9 · 싱가포르 AND)·채널은 적용 전과 같다.
- 커밋·배포 0, Phase 19 산출물 변경 0.
</success_criteria>

<output>
Create `.planning/quick/260925-o1s-relay-alert-doc-size/260925-o1s-SUMMARY.md` when done.

오케스트레이터 참고(커밋은 사용자 확인 후, executor 는 하지 않음): 대상 파일은 `scripts/deploy-relay.sh` · `ops/alert-relay-down.yaml` · `docs/relay-operations.md` · `.planning/quick/260925-o1s-relay-alert-doc-size/`(PLAN · SUMMARY · verify.sh) + 오케스트레이터가 갱신하는 quick 기록. 메시지 초안: `fix(relay): 알림 정책 documentation 10,834→<N> 바이트로 축소(8번 상세를 운영 문서로) · deploy-relay.sh --alert-only 추가 · 라이브 정책 갱신` — 한글, Co-Authored-By 없음.
</output>
