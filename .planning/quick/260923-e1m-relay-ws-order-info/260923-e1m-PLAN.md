---
phase: quick-260923-e1m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - relay/src/store/orders.ts
  - relay/src/ws/order-handler.ts
  - relay/tests/helpers/fake-dma-orders.ts
  - relay/tests/order-store.test.ts
  - relay/tests/ws-order.test.ts
autonomous: true
requirements: [A1, B1, B3, S1]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "(S1) 대기에 붙지 않은 체결 E 통보 여러 조각이 오면 dma_orders.filled_qty 가 조각 합으로 누적된다. 누적이 행 qty 미만이면 status 는 partially_filled, qty 이상이면 filled 다. 조각들이 큐에 한꺼번에 쌓였다가 한 번에 비워져도 결과가 같다"
    - "(S1) 누적은 PostgREST 조건부 UPDATE(filled_qty·status 를 비교하는 compare-and-swap)로 한다. 읽은 뒤 다른 갱신이 끼어들면 다시 읽고 재시도한다(최대 ORDER_FILL_CAS_MAX_ATTEMPTS). 새 마이그레이션·Supabase RPC 는 없다. 그래서 배포는 relay 단독이다"
    - "(S1) 이미 종결된 행(cancelled·rejected)에 늦은 E 가 오면 filled_qty 만 누적된다. status·notice_type·message 는 바뀌지 않는다. 대기에 붙은 첫 통보(finish)의 절대값 기록은 그대로다"
    - "(B1) 자동주문(limit_chaser·vi)의 취소확인 C(orderNo=취소 전문의 새 번호, orgOrderNo=원주문)는 원주문 행을 cancelled 로 갱신한다. 원주문 행의 order_no·origin 은 덮지 않고, 새 행을 만들지 않는다. price 0 이 ERROR 를 만들지 않는다"
    - "(B1) 자동주문의 정정·취소 거부 R(rc 804 등, orgOrderNo 또는 requestKind Cancel/Modify)은 원주문 상태를 바꾸지 않고 새 행도 만들지 않는다. info 1줄만 남긴다"
    - "(B1) 원주문 행을 못 찾으면 warn 1줄만 남기고 insert 하지 않는다. 조회 자체가 실패하면 safePgError 로 error 를 남기고, C 이면 원주문번호 셀렉터로 cancelled 열화 갱신을 보낸다(S-5)"
    - "(B1) orgOrderNo 가 비어 있고 orderNo 가 곧 원주문번호인 거래소 자동취소 C 는 orderNo 로 찾은 행을 cancelled 로 갱신한다(09-22 VI 3405000021 경로 보존)"
    - "(B3) 취소성 통보는 어떤 경로로도 autoInsertRow 에서 새 행이 되지 않는다. 신규 접수 A 의 자동주문 insert(⑫ ①-c ㉗ ㉘)는 그대로다. 낡은 가격 CHECK 주석은 원격 CHECK(20260922120000 + 20260922180000) 기준으로 고쳐져 있다"
    - "(A1) relay 가 낸 주문과 무관한 수동 통보(같은 게이트웨이 세션을 공유하는 WinForms 등)는 ERROR 가 아니라 info 1줄로 남는다. 문구는 「같은 세션의 다른 클라이언트(WinForms 등) 주문, relay 가 낸 주문 아님」 취지다. 갱신·insert 는 0건이다"
    - "(A1) relay 가 낸 주문인데 행에 못 붙은 통보는 기존 ERROR 「대기·행 어디에도 붙지 않는 수동 통보」를 유지한다. 대상은 좁히기 실패로 후보가 있었던 경우, 연결 종료로 대기가 걷힌 경우, 5초 timeout 뒤 늦게 온 통보, relay 가 정산한 주문번호·원주문번호를 가리키는 통보다. 기존 ⑨ ㉑ ㉖ 은 수정 없이 통과한다"
    - "새로 생긴 로그 줄 어디에도 계좌번호가 없다(T-16-45). PostgREST 오류는 전부 safePgError 를 지난다. relay 테스트 스위트 전체·typecheck·typecheck:tests 가 green 이다"
  artifacts:
    - path: "relay/src/store/orders.ts"
      provides: "OrderUpdate.filledQtyDelta · OrderFillSink · supabaseOrderFillSink(CAS 누적 + 상태 파생) · OrderSinks.addFill · OrderStore 큐의 fillDelta 갈래"
      exports: ["supabaseOrderFillSink", "ORDER_FILL_CAS_MAX_ATTEMPTS", "OrderFillSink"]
    - path: "relay/src/ws/order-handler.ts"
      provides: "patchOf 의 E 조각 → filledQtyDelta · 취소성 통보 원주문 라우팅(settleOriginal) · autoInsertRow 취소성 가드 · 수동 미부착 통보의 relay 관련성 판정(relay 흔적)"
      exports: ["isCancelLikeNotice"]
    - path: "relay/tests/helpers/fake-dma-orders.ts"
      provides: "select 컬럼 투영 · DB 기본값 거울(status 'requested' · filled_qty 0) · insert 행의 qty/filled_qty · onSelect 훅(CAS 경합 재현)"
    - path: "relay/tests/order-store.test.ts"
      provides: "S1 누적·상태 파생·CAS 경합·종결 행·계약 위반 회귀"
    - path: "relay/tests/ws-order.test.ts"
      provides: "S1 e2e 누적 · B1/B3 C·R804·미발견·자동취소 예외·조회 실패 · A1 WinForms info / relay 관련 ERROR 회귀"
  key_links:
    - from: "relay/src/ws/order-handler.ts"
      to: "relay/src/store/orders.ts"
      via: "patchOf 가 E 조각을 filledQtyDelta 로 싣고 → enqueueUpdate → #drain 이 addFill sink 로 보낸다"
      pattern: "filledQtyDelta"
    - from: "relay/src/ws/order-handler.ts"
      to: "relay/src/store/orders.ts"
      via: "settleOriginal 이 findIdByOrderNo(userId, 원주문번호) 로 찾은 행을 orderRowId 셀렉터 + status cancelled 로 갱신한다"
      pattern: "settleOriginal"
    - from: "relay/src/ws/order-handler.ts (hub order listener)"
      to: "relay/src/ws/order-handler.ts (recordUnmatched)"
      via: "후보 존재 여부(candidates.length > 0)와 relay 흔적(relayTrail)으로 ERROR/info 를 가른다"
      pattern: "isRelayRelated"
---

<objective>
relay WS-order 통보 기록 경로의 결함 4건을 한 번에 고친다. 사용자는 A1·B1·B3·S1 을 함께 하기로 정했다. 진단 원문은 `.planning/debug/relay-ws-order-unmatched-notice.md` 의 Evidence 와 Resolution 이다.

- **S1**: 대기에 붙지 않은 E 통보가 `filled_qty` 를 체결 조각 수량으로 **덮어쓴다** (`store/orders.ts:949-951` · `statusOf(E, null)=undefined`). 그래서 전량 체결된 LC 059·070 이 accepted · 700/408 주로 남았다. 이것을 누적으로 바꾸고 행 qty 로 filled / partially_filled 를 파생한다.
- **B1**: 자동주문의 취소확인 C·정정/취소 거부 R 은 orderNo 에 취소 전문 자신의 새 번호를 싣는다(gh-trade `KBBroker.cpp:1660-1661`). 그런데 `ensureRow` 는 orderNo 로만 찾고 insert 로 빠진다. 이것을 원주문번호(orgOrderNo) 축으로 원주문 행을 찾아 C 면 cancelled 로 바꾸게 한다. R 은 원주문 상태를 유지한다.
- **B3**: 취소성 통보로는 새 행을 만들지 않는다. 낡은 가격 CHECK 주석(`autoInsertRow` :660-661)을 원격 CHECK 기준으로 고친다. 신규 접수 A 경로는 그대로 둔다.
- **A1**: relay 는 D-17 철회 이후 WinForms 와 한 게이트웨이 세션을 공유한다. 그 팬아웃으로 들어오는 WinForms 수동 주문 통보를 ERROR 가 아니라 info 로 남긴다. relay 가 낸 주문인데 못 붙은 경우만 ERROR 를 유지한다.

**S1 설계 판단 (RPC 없음).** 먼저 조사했다. 대기에 붙은 경로(`finish`)에도 누적 방식은 없다. 그 경로는 첫 통보 1건만 받아 신선한 행(filled_qty 0)에 절대값을 쓸 뿐이다. 코드베이스에 있는 증분 패턴은 `incr_api_usage` RPC(`20260417120000_api_usage.sql`) 하나다. 그래도 RPC 를 쓰지 않고 **PostgREST 조건부 UPDATE 에 의한 compare-and-swap** 을 택한다. 근거는 셋이다.
1. 18-33(R3-WR-01)이 이미 같은 판단을 했다. 「relay 필터로 UPDATE 한 문장 안에서 원자적으로. 마이그레이션·트리거가 필요 없고, 원격 반영 사람 게이트가 없다」(`store/orders.ts:329-340`).
2. relay 는 `dma_orders` 의 유일한 writer 다(D-03 — server 는 읽기만 한다). 그리고 `#drain` 은 항목을 **직렬로** await 한다. 그래서 relay 내부에는 동시 쓰기가 없다. CAS 는 그 전제가 깨질 때를 위한 원자성 보증이다.
3. 마이그레이션이 없으면 배포 순서 제약(마이그레이션 → relay)과 원격 적용 게이트가 사라진다.

비용은 조각당 왕복 2회(select + update)다. 큐는 비동기라 통보 수신 경로(D-32)를 막지 않는다.

**B1 라우팅.** `recordUnmatched` 에서 수동 분기를 지난 뒤, `ensureRow` 앞에서 `isCancelLikeNotice` 로 가른다. 취소성이면 `settleOriginal` 로 보낸다.
- C: `normalizeOrderNo(orgOrderNo)` 가 비어 있지 않으면 원주문번호로 찾는다. 비어 있으면(거래소 자동취소 예외) orderNo 로 찾는다. 찾은 원주문 행을 `{orderRowId, status:"cancelled", noticeType, resultCode, message}` 로 갱신한다. orderNo·origin·filledQty 는 싣지 않는다.
- R: 조회도 갱신도 하지 않는다. info 1줄만 남긴다.
- 어느 쪽도 insert 하지 않는다(B3).

**하지 않는 것 (범위 밖 — SUMMARY 「관측된 잔여」에 적는다):**
- 수동 경로의 원주문 상태 미갱신은 고치지 않는다. relay 가 낸 수동 취소가 pending 으로 정산될 때 원주문 행(수동·자동 모두)이 cancelled 로 바뀌지 않는 문제다(진단 조사 6의 09-10 사례). 사용자는 B1 을 자동주문 recordUnmatched 경로로 정했다.
- 기존 오염 데이터를 보정하지 않는다(LC 059/070 filled_qty, accepted 로 고착된 취소 원주문들). 코드 수정만 한다.
- finish 정산 직후 200ms 플러시 전에 온 E 가 orderNo 조회를 놓치는 경합은 고치지 않는다. A1 의 relay 흔적 덕분에 ERROR 로는 드러난다.
- 교보 브로커 경로(orgOrderNo 미기입)는 그대로다.
- 정정확인 M 의 자동 insert 동작도 바꾸지 않는다.

Purpose: 자동주문 취소가 생긴 날마다 원주문이 accepted 로 영구 고착되는 결함, 체결 추적이 마지막 조각으로 덮이는 결함, 정상인 공유 세션 WinForms 주문마다 ERROR 가 찍히는 소음. 이 셋을 코드와 회귀 테스트로 닫는다.
Output: `relay/src/store/orders.ts` · `relay/src/ws/order-handler.ts` 수정, 공용 가짜 `fake-dma-orders.ts` 확장, `order-store.test.ts`·`ws-order.test.ts` 회귀.

**실행 제약 (반드시 지킨다):**
- executor 는 **git commit·push 를 하지 않는다.** 변경만 남긴다. 사용자 CLAUDE.md 에 따라 오케스트레이터가 사용자 확인 뒤 커밋한다.
- 다른 세션이 작업 중인 파일은 **읽기·수정·stage 금지**다. `webapp/src/components/layout/*`, `webapp/src/components/trading/vi-order-list.tsx`, `webapp/src/components/trading/exchange-tag.tsx`, `webapp/e2e/specs/*`, `.planning/quick/260923-cqj*`, `.planning/quick/260923-dmb*` 가 그것이다.
- **장중(08:00~20:00 KST)에는 relay 를 재시작·배포하지 않는다.** 이 quick 은 코드와 테스트만 한다. 원격 DB 접근도 없다(마이그레이션 없음).
- 로그에 계좌번호를 싣지 않는다(T-16-45). PostgREST 오류는 `safePgError` 를 지난다.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/debug/relay-ws-order-unmatched-notice.md
@relay/src/ws/order-handler.ts
@relay/src/store/orders.ts
@relay/src/order/notice-status.ts
@relay/tests/helpers/fake-dma-orders.ts

<interfaces>
<!-- 실행자가 코드베이스를 다시 훑지 않도록 핵심 계약을 여기 모았다. -->

order/notice-status.ts (변경 없음 — import 해서 쓴다):
- statusOf(notice, requestedQty: number|null): DmaOrderStatus|undefined  — E + null 이면 undefined
- filledQtyOf(notice): number|undefined  — noticeType "E" 일 때만 notice.quantity(=체결 조각)
- replaceableStatusesOf(next): readonly DmaOrderStatus[]  — 상태 단조성의 유일 판정. 순위 requested/timeout 0 < accepted 1 < partially_filled 2 < filled/cancelled/rejected 3(종결, 종결끼리 이동 금지)

store/orders.ts (현재):
- OrderUpdate { orderRowId?, orderNo?, status?, resultCode?, noticeType?, message?, filledQty?, origin?, userId? }
- OrderSelector = {column:"id", value} | {column:"order_no", value, userId}
- OrderUpdateSink = (sel, patch: OrderRowPatch) => Promise<void | {applied:boolean}>
- OrderSinks = { update, insert, findIdByOrderNo }  — supabaseOrderSinks(supabase) 가 부팅 결선(index.ts:120)
- supabaseOrderSink: status 가 있으면 .in("status", replaceableStatusesOf(next)) + .select("id") 로 막힘 판정
- supabaseOrderLookupSink: .select("id").eq("user_id").eq("order_no").gte/lt(created_at, kstDayRangeUtc()).order(created_at desc).limit(1)
- OrderStore: enqueueUpdate(동기 O(1)) → #queue → #drain 이 항목을 **직렬로** await, 실패 1회 재큐잉 후 드롭(#dropped). stats(): {queued, flushed, flushedNoop, retried, dropped, inserted, insertConverged}
- rowPatchOf(update): camel→snake 유일 경계. filledQty → filled_qty(음수 0)
- kstDayRangeUtc(now?): {from, to}  — KST 당일 반열린 구간(ISO)

ws/order-handler.ts (현재):
- hub "order" 리스너(:389-447): 사용자 연결의 pending 에서 ISIN 후보 → narrowPending → 정산 or `void recordUnmatched(userId, notice)`
- patchOf(notice) :450-460 → {orderNo, status: statusOf(notice,null), resultCode, noticeType, message, filledQty: filledQtyOf(notice), origin}
- recordUnmatched :493-560 — manual 분기(:496-541, 행 없으면 logger.error "대기·행 어디에도 붙지 않는 수동 통보 — stdout 이 유일한 기록이다") → 자동은 ensureRow
- ensureRow :572-609 (inflight Map 키 `${userId}|${notice.orderNo}`) → insertOnly :624-647 → autoInsertRow :657-711
- finish (handle 안 :995-1038): notice null → timeout, 아니면 절대값 filledQty 로 행 정산
- closeConn :1095-1114 — state.pending 의 타이머를 끊고 비운다
- normalizeOrderNo(raw) :1166 — trim + 선행 0 제거, 값 없음은 ""
- ParsedOrderResp 필드: orderNo, noticeType, resultCode, message, isin, side, sideTrusted, price, quantity, orgOrderNo, exchange, board, requestKind("New"/"Modify"/"Cancel"/""), requester, origin, originKind("manual"|"limit_chaser"|"vi")

tests:
- ws-order.test.ts mkOrderStore(opts {existingId, insertFails, insertGate}) — updates/inserts/lookups 기록 스텁
- ws-order.test.ts restartWithRealStore(seed: FakeRow[]) — 진짜 OrderStore(supabaseOrderSinks(fakeDmaOrders(seed))) 하네스. start() 없이 flushNow() 로 비운다(가짜 타이머)
- gateway.pushOrderResp(gatewaySocket(), {noticeType, orderNo, orgOrderNo, origin:"Manual"|"LimitChaser"|"VITrigger", quantity, price, resultCode, requestKind, isin}) — 기본 isin SAMPLE_ISIN, origin "Manual"
- order-store.test.ts recordingSinks() (:60-94) · ⓻-b(:660-667) 가 OrderSinks 객체를 손으로 조립한다 → addFill 추가 필요
- 기준선: `pnpm --filter @gh-radar/relay test` = 22 files · 564 tests green (2026-09-23 10:14 확인)
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer · S1): 대기 밖 E 조각 → filled_qty 누적 + 상태 파생 — handler → OrderStore → CAS fill sink → 가짜 행까지 한 줄로 관통</name>
  <files>relay/src/store/orders.ts, relay/src/ws/order-handler.ts, relay/tests/helpers/fake-dma-orders.ts, relay/tests/order-store.test.ts, relay/tests/ws-order.test.ts</files>
  <behavior>
    - order-store: 행 qty 10 · filled_qty 0 · accepted 에 조각 3·4 를 큐에 넣고 flushNow 하면 filled_qty 7 · partially_filled 가 된다. 조각 3 을 더하면 filled_qty 10 · filled 가 된다
    - order-store: CAS 경합을 재현한다. 첫 select 직후 onSelect 훅이 행 filled_qty 를 5 로 바꾸면 첫 UPDATE 는 0행이다. sink 가 다시 읽어 5+3=8 로 반영하고, select 가 2회 이상이며 dropped 는 0 이다
    - order-store: 종결 행(cancelled, notice_type C)에 조각 2 를 넣으면 filled_qty 만 +2 된다. status 는 cancelled, notice_type 은 C 그대로다
    - order-store: 한 갱신에 filledQty 와 filledQtyDelta 가 함께 실리면 드롭된다(dropped +1, error 로그). rowPatchOf 는 filledQtyDelta 로 컬럼을 만들지 않는다
    - order-store: 갱신 전용 OrderStore(sink 함수 하나로 생성)에 delta 항목이 들어오면 조용히 사라지지 않는다. 재시도 후 dropped 가 오르고 error 가 남는다
    - ws-order e2e(진짜 store): 시드 LC 행(order_no 3405000059, qty 10, accepted)에 origin LimitChaser E 3·4 를 연달아 밀어 넣고 flushNow 하면 filled_qty 7 · partially_filled 다. E 3 을 더하면 filled_qty 10 · filled 이고, 행 수는 그대로다(insert 0)
    - 기존 ⑬ 은 기대값을 `filledQtyDelta: 7` 로 바꾼다(filledQty 는 undefined). ㊸ ㊹ ㊺ 의 finish 절대값 경로는 수정 없이 통과한다
  </behavior>
  <action>
**계약 (store/orders.ts), per S1:**
1. `OrderUpdate` 에 `filledQtyDelta?: number` 를 더한다. docstring 에 네 가지를 적는다. 체결 조각 수량이고, 행의 filled_qty 에 **더한다**(S1). 대기 밖 통보 전용이다. 절대값 `filledQty`(finish 첫 통보 전용)와 함께 싣는 것은 계약 위반이다.
2. `export type OrderFillSink = (sel: OrderSelector, delta: number, patch: OrderRowPatch) => Promise<void | OrderUpdateResult>` 를 정의한다. `OrderSinks` 에 **필수** 필드 `addFill: OrderFillSink` 를 더한다. `supabaseOrderSinks` 가 `addFill: supabaseOrderFillSink(supabase)` 를 싣는다. index.ts 는 수정하지 않는다(이미 supabaseOrderSinks 를 쓴다).
3. `export const ORDER_FILL_CAS_MAX_ATTEMPTS = 3` 를 둔다.
4. `export function supabaseOrderFillSink(supabase): OrderFillSink` 를 만든다. 알고리즘:
   - 각 시도에서 먼저 대상 행을 읽는다. 컬럼은 `id, qty, filled_qty, status` 이다. 셀렉터가 `id` 면 `.eq("id")` 로 읽는다. `order_no` 면 supabaseOrderLookupSink 와 **같은 3축**(user_id·order_no·당일 gte/lt)에 created_at 내림차순·limit 1 을 건다. 읽기 조립은 이 sink 안의 작은 내부 함수 한 곳에만 둔다.
   - select 오류는 `safePgError` 로 error 로그를 남기고 throw 한다(큐 재시도 규율).
   - 행이 없으면 warn 1줄을 남긴다(column 만 싣는다). 그리고 `{applied:false}` 를 돌려준다.
   - 계산은 이렇다. cur = filled_qty(없으면 0). next = cur + delta. derived = next ≥ qty 이면 "filled", 아니면 "partially_filled". advance = `replaceableStatusesOf(derived)` 가 현재 status 를 포함하는가.
   - advance 가 참이면, 들어온 patch 에서 `order_no` 키를 뺀 나머지에 `filled_qty: next, status: derived` 를 얹어 쓴다. order_no 를 빼는 이유가 있다. 이 행은 바로 그 번호로 찾았거나 id 로 찾았다. 주문번호를 채우는 것은 finish 의 몫이다. 그리고 빼면 23505 경로가 생기지 않는다.
   - advance 가 거짓이면(종결 행) `{ filled_qty: next, updated_at }` 만 쓴다. 늦은 통보가 종결 행의 notice_type·message 를 덮지 않게 한다. 이것은 18-33 의 「막힌 갱신은 통째로」 규율과 같은 방향이다.
   - next > qty 면 warn 1줄을 남긴다. 싣는 것은 qty·filledQty 숫자와 column 뿐이다. 문구는 「누적 체결수량이 주문수량을 넘었다 — 값을 자르지 않는다(중복 통보 의심)」 취지다. 값을 자르지 않는다(지어내지 않는다).
   - UPDATE 는 `.eq("id", 읽은 id).eq("filled_qty", cur).eq("status", 읽은 status).select("id")` 로 보낸다. 오류가 나면 safePgError 로 error 를 남기고 throw 한다. 결과가 1행이면 끝낸다(반환 생략 = applied). 0행이면 CAS 에서 진 것이다. 다음 시도로 가서 다시 읽는다.
   - 시도를 다 쓰면 error 로그를 남긴다(attempts·column 만 싣는다). 문구는 「체결 누적 CAS 경합 상한 — 큐 재시도로 넘긴다」 취지다. 그리고 Error 를 throw 한다.
   - 로그에는 계좌번호·주문번호 원문을 싣지 않는다(T-16-45). 이 sink 의 docstring 에 근거 셋을 적는다. 직렬 drain, relay 단독 writer(D-03), 18-33 이 relay 조건부 UPDATE 를 택한 이유다. 그리고 Supabase RPC 대신 CAS 를 택했다는 한 문단을 둔다.
5. `OrderStore` 를 고친다.
   - `#addFill: OrderFillSink | null` 을 둔다. OrderSinks 로 만들면 addFill 을, sink 함수 하나로 만들면 null 을 넣는다.
   - `QueueItem` 에 `fillDelta?: number` 를 더한다.
   - `enqueueUpdate` 에 규칙 셋을 더한다. (a) filledQty 와 filledQtyDelta 가 둘 다 있으면 `#dropped += 1`, error 로그(계약 위반) 후 return 한다. (b) delta 가 정수가 아니거나 1 미만이면 warn 을 남기고 delta 를 없는 것으로 친다. (c) 「갱신할 필드가 없는 요청」 판정은 patch 에 `updated_at` 만 남았고 **그리고** delta 도 없을 때만이다. push 할 때 `fillDelta` 를 싣는다.
   - `#drain` 은 fillDelta 가 있으면 addFill 로, 없으면 기존 sink 로 보낸다. addFill 이 null 이면 「fill sink 미결선」 Error 를 throw 한다. 기존 재시도·드롭 규율을 그대로 타게 하기 위해서다(갱신 전용 store 가 조용히 삼키지 않는다).
   - 카운터(flushed / flushedNoop / dropped)는 기존 규칙 그대로다.
6. `rowPatchOf` 는 filledQtyDelta 를 컬럼으로 만들지 않는다. 현재 코드가 이미 그렇다. 확인만 하고 docstring 에 한 줄 적는다.

**handler (ws/order-handler.ts), per S1:**
- `patchOf` 의 `filledQty: filledQtyOf(notice)` 를 `filledQtyDelta: filledQtyOf(notice)` 로 바꾼다. docstring 에 적는다. 대기 밖 E 의 quantity 는 조각이다. 누적과 filled/partially_filled 파생은 store 의 fill sink 가 행 qty 로 한다. 옛 덮어쓰기 결함은 진단 조사 6(LC 059/070)이다.
- `finish` 는 수정하지 않는다. 절대값 filledQty 를 쓰고 statusOf(notice, msg.qty) 를 쓴다. finish 에 한 줄 주석을 더한다. 첫 통보는 방금 insert 한 신선한 행(filled_qty 0)에 닿으므로 절대값이 곧 누적값이다. 이후 조각은 recordUnmatched 가 delta 로 보낸다.

**가짜 (tests/helpers/fake-dma-orders.ts):** 기존 동작은 전부 유지한다.
- `from().select(cols)` 가 받은 컬럼 문자열을 FakeQuery 에 기록한다(`columns`). select 결과는 요청 컬럼만 투영한다. 그래서 `select("id")` 는 지금처럼 `{id}` 만 돌려준다.
- DB 기본값 거울 한 곳을 둔다. status 는 'requested', filled_qty 는 0 이다. 투영과 `eq` 비교가 행에 그 컬럼이 없을 때 이 기본값을 읽는다. `in` 필터의 기존 status 기본값 처리도 이 한 곳을 쓰게 합친다.
- insert 가 만드는 행에 `qty`(values 에 숫자로 있으면)와 `filled_qty: 0` 을 더한다(DB DEFAULT 거울).
- `opts.onSelect?: (q: FakeQuery, nth: number) => void` 훅을 더한다. select 쿼리의 matched 를 계산한 직후, 결과를 돌려주기 전에 부른다. CAS 경합 재현용이다.

**테스트:**
- order-store.test.ts: recordingSinks() 와 ⓻-b 의 OrderSinks 조립에 addFill 을 더한다. recordingSinks 는 기록 스텁을, ⓻-b 는 `supabaseOrderFillSink(supabase)` 를 쓴다. 그리고 behavior 의 order-store 5건을 새 describe 「S1 — 체결 조각 누적 (quick-260923-e1m)」 로 추가한다.
- ws-order.test.ts: ⑬ 기대값을 `filledQtyDelta: 7` 로 바꾼다. restartWithRealStore(seed) 를 쓰는 e2e 1건을 새 describe 「quick-260923-e1m — 통보 기록 경로」 첫 케이스 「S1-①」 로 추가한다. 시드 행은 user USER_A, order_no "3405000059", created_at 은 당일(kstDayRangeUtc().from + 1h), status accepted, qty 10, filled_qty 0, origin limit_chaser 다. E 두 개를 연달아 밀어 넣고, `store.stats().queued >= 2` 를 기다린 뒤 flushNow 한다(두 조각이 한 큐에 동시에 있는 상황). 그다음 세 번째 E 를 넣는다. 새 describe 는 기존 `describe("wss 주문 경로 (D-02)")` **안의 하위 describe** 로 둔다. beforeEach 하네스·restartWithRealStore 를 재사용하기 위해서다.
- 순서: 먼저 RED 를 확인한다(새 케이스가 덮어쓰기로 실패). 그다음 구현하고 GREEN 을 확인한다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test tests/order-store.test.ts tests/ws-order.test.ts && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && test "$(grep -v '^\s*//' relay/src/ws/order-handler.ts | grep -c 'filledQtyDelta')" -ge 1 && test "$(grep -c 'supabaseOrderFillSink' relay/src/store/orders.ts)" -ge 2 && MIG="$(git status --porcelain -- supabase/migrations)" && test -z "$MIG" && test "$(grep -c '\.rpc(' relay/src/store/orders.ts)" -eq 0</automated>
  </verify>
  <done>
대기 밖 E 조각이 filled_qty 에 누적되고, 행 qty 로 partially_filled/filled 가 파생된다. CAS 경합에서는 다시 읽어 올바른 합이 된다. 종결 행은 filled_qty 만 누적된다. 계약 위반과 갱신 전용 store 의 delta 는 드롭으로 드러난다. order-store 와 ws-order 스위트, typecheck 2종이 green 이다. 마이그레이션·RPC 는 0 이다.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (B1 · B3): 자동주문 취소성 통보를 원주문번호로 원주문 행에 붙인다 — C 는 cancelled, R 은 상태 유지, 어느 쪽도 insert 없음</name>
  <files>relay/src/ws/order-handler.ts, relay/tests/ws-order.test.ts</files>
  <behavior>
    - B1-①: LC 자동 C. orderNo 3405000076, orgOrderNo 3405000070, price 0, quantity 23228, origin LimitChaser 이고, 원주문 3405000070 → row-lc-070 이 있다. updates 에 {orderRowId:"row-lc-070", status:"cancelled", noticeType:"C"} 1건이 있다. 그 갱신에 orderNo·origin·filledQty·filledQtyDelta 가 없다. inserts 0, lookups 는 [{USER_A, "3405000070"}] 이다. error 로그에 「가격이 0 이하」가 없다
    - B1-②: VI 자동 C(origin VITrigger)도 같은 규칙으로 원주문 행이 cancelled 가 된다
    - B1-③: 자동 R. noticeType R, resultCode 804, orderNo 새 번호, orgOrderNo 원주문, requestKind Cancel, origin LimitChaser 다. updates 0, inserts 0, lookups 0 이다. info 에 「원주문 상태 유지」 취지 1줄이 있고 계좌번호는 없다
    - B1-④: 원주문 행이 없으면 updates 0, inserts 0 이다. warn 에 「원주문 행을 찾지 못했다」 취지가 있고, error 에 「가격이 0 이하」가 없다
    - B1-⑤: 거래소 자동취소 예외. C 이고 orderNo 3405000021 이 원주문, orgOrderNo 는 공백 10자, origin VITrigger 다. lookups 는 [{USER_A, "3405000021"}] 이고 그 행이 cancelled 로 갱신된다
    - B1-⑥: 조회가 throw 하면 updates 에 {orderNo:"3405000070", userId:USER_A, status:"cancelled"}(orderRowId 없음) 1건이 있다. error 로그는 safePgError 를 지나고 계좌번호가 없다
    - B1-⑦(진짜 store): 시드 LC 행(order_no 3405000070, accepted, qty 10)에 C 를 보내고 flushNow 한다. 행 status 는 cancelled, order_no 는 여전히 3405000070 이고, 행 수는 그대로다
    - isCancelLikeNotice 순수 단위 테스트. C 는 true 다. R 은 orgOrderNo 가 있거나 requestKind 가 Cancel/Modify 이면 true 다. R 이 orgOrderNo 도 없고 requestKind 가 New/"" 이면 false 다. A·E·M 은 false 다
    - 기존 ⑫ ①-c ㉗ ㉘(신규 접수 A 자동 insert · 접수 전 거부 R insert)는 수정 없이 통과한다
  </behavior>
  <action>
**order-handler.ts, per B1:**
1. `normalizeOrderNo` 근처(순수 함수 구역)에 `export function isCancelLikeNotice(n: ParsedOrderResp): boolean` 를 둔다. 규칙은 이렇다. noticeType 이 "C" 이면 참이다. noticeType 이 "R" 이면서 `normalizeOrderNo(n.orgOrderNo) !== ""` 이거나 requestKind 가 "Cancel"·"Modify" 이면 참이다. 나머지는 거짓이다. 정정확인 "M" 은 이 quick 의 범위가 아니라 제외한다.
   docstring 에 네 가지를 적는다. gh-trade `KBBroker.cpp:1660-1661`(orderNo = 취소 전문 자신의 번호, orgOrderNo = 원주문), `Server.cpp:392-394`, 진단 조사 5, 그리고 접수 전 거부 R(orderNo "" · org 없음 · New)은 신규 주문의 거부라 기존 insert 경로에 남는다는 점이다.
2. 내부 함수 `orderNoLookupKeys(raw)` 를 둔다. 반환값은 [raw.trim(), normalizeOrderNo(raw).padStart(10, "0")] 에서 중복과 빈 값을 뺀 배열이다. 정규화 결과가 "" 이면 빈 배열이다. 근거는 와이어 주문번호가 10자리 0 좌패딩 고정폭이라는 것이다(normalizeOrderNo docstring). 저장된 order_no 도 와이어 원문이므로 첫 키로 보통 끝난다.
3. `recordUnmatched` 에서 수동 분기가 끝난 **뒤**, `ensureRow` 호출 **앞**에 분기를 넣는다. `isCancelLikeNotice(notice)` 이면 `await settleOriginal(userId, notice)` 후 return 한다. ensureRow·insertOnly 로는 절대 내려가지 않는다(B3).
4. `settleOriginal(userId, notice)` 를 createOrderHandler 안에 둔다(deps·inflight 접근이 필요하다).
   - (a) noticeType "R" 이면 조회도 갱신도 하지 않는다. 원주문 상태를 유지한다. `logger.info({origin, orderNo, noticeType, resultCode, requestKind}, …)` 로 「자동주문 정정·취소 거부 — 원주문 상태 유지, 새 행을 만들지 않는다」 취지를 남기고 return 한다.
   - (b) C 는 이렇게 처리한다.
     - 대상 번호를 고른다. normalizeOrderNo(orgOrderNo) 가 비어 있지 않으면 orgOrderNo 다. 비어 있으면 거래소 자동취소 예외로 보고 orderNo 다(진단 조사 6 — 09-22 VI 3405000021). 키가 0개면 warn(「원주문을 가리킬 번호가 없다 — 새 행을 만들지 않는다」) 후 return 한다.
     - 각 키에 대해 inflight 에 `${userId}|${key}` 가 진행 중이면 await 한다. 결과가 row 면 그 id 를 쓴다. 접수 A 직후 취소가 insert 왕복과 겹치는 경주 방어다.
     - 없으면 키를 차례로 `deps.orderStore.findIdByOrderNo(userId, key)` 로 찾는다. 첫 non-null 을 쓴다.
     - 조회가 throw 하면 이렇게 한다. `logger.error({ pgError: safePgError(err), origin, orderNo, noticeType }, …)` 로 「자동주문 취소확인 — 원주문 조회 실패, 원주문번호 셀렉터로 열화 갱신」 취지를 남긴다. 그리고 `enqueueUpdate({ orderNo: 첫 키, userId, status: "cancelled", noticeType, resultCode, message })` 를 보내고 return 한다(S-5 — 조회가 죽었다고 통보를 버리지 않는다).
     - 행이 없으면 `logger.warn({origin, orderNo, noticeType, resultCode}, …)` 로 「자동주문 취소확인 — 원주문 행을 찾지 못했다. 새 행을 만들지 않는다」 취지를 남기고 return 한다.
     - 찾았으면 `enqueueUpdate({ orderRowId: id, status: "cancelled", noticeType, resultCode, message })` 를 보내고, `logger.info` 로 「자동주문 취소확인 — 원주문 행을 취소로 갱신」 1줄을 남긴다.
   - **orderNo·origin·filledQty·filledQtyDelta 는 싣지 않는다.** 이유를 주석에 적는다. orderNo 는 취소 전문의 새 번호라 원주문 행의 order_no 를 덮는다. origin 은 원주문 행의 값이 정본이다. filled 된 원주문을 cancelled 로 되돌리는 것은 store 의 상태 단조성 필터가 막는다.
   - 로그에 계좌번호는 없다. 51 통보에는 애초에 계좌가 실리지 않는다.
5. B3 방어선을 `autoInsertRow` 맨 앞에 둔다. `isCancelLikeNotice(notice)` 이면 `logger.error(ctx, …)` 로 「취소성 통보가 insert 경로에 왔다 — 새 행을 만들지 않는다 (B3 라우팅 누락)」 취지를 남기고 null 을 돌려준다.
   **낡은 가격 CHECK 주석(:660-661)을 고친다.** qty 는 CHECK > 0 이다. price 는 원격 CHECK(20260922120000 + 20260922180000, 18-29 SUMMARY 원격 적용 확인)가 0 을 취소 행(order_type C)과 G2/G3 세션 신규에만 허용한다. 이 경로는 취소성 통보를 insert 하지 않고(B3) 자동주문 행은 세션을 싣지 않으므로, 여기 온 price ≤ 0 은 파손 프레임이다. 그래서 멈추고 사유를 남긴다.
   `orderType: notice.noticeType === "C" ? "C" : "N"` 은 위 가드 때문에 C 가 도달할 수 없다. `"N"` 으로 줄이고 그 이유를 한 줄 적는다.
6. 파일 머리 T-16-07 문단과 recordUnmatched docstring 에 한 줄씩 더한다. 「취소성 통보(C·정정/취소 거부 R)는 새 행이 아니라 원주문 행의 갱신이다 (quick-260923-e1m B1/B3)」.

**테스트 (ws-order.test.ts):**
- `mkOrderStore` opts 에 두 가지를 더한다. `existingIds?: Record<string, string>` 는 주문번호별 조회 결과다. 조회는 `existingIds[orderNo] ?? existingId ?? null` 이다. `lookupFails?: boolean` 이면 findIdByOrderNo 가 throw 한다.
- Task 1 의 하위 describe 「quick-260923-e1m — 통보 기록 경로」에 behavior 의 B1-①~⑦ 을 추가한다. 스텁 opts 가 다른 케이스는 기존 `restartHarness()` 패턴(orders 교체 후 재기동)을 쓴다. ⑦ 은 restartWithRealStore 를 쓴다. isCancelLikeNotice 순수 단위는 파일 끝 narrowPending 구역 옆에 새 describe 로 둔다(mkNotice 재사용).
- 먼저 RED 를 확인한다(B1-① 이 insert 경로·가격 0 error 로 실패). 그다음 GREEN 을 확인한다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test tests/ws-order.test.ts && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && test "$(grep -c 'settleOriginal' relay/src/ws/order-handler.ts)" -ge 2 && test "$(grep -c 'export function isCancelLikeNotice' relay/src/ws/order-handler.ts)" -eq 1 && test "$(grep -c '20260922180000' relay/src/ws/order-handler.ts)" -ge 1 && test "$(grep -c 'price` 는 CHECK > 0' relay/src/ws/order-handler.ts)" -eq 0</automated>
  </verify>
  <done>
자동 C 는 원주문 행을 cancelled 로 갱신한다(order_no·origin 불변, insert 0). 자동 R 은 원주문 상태를 유지한다(조회·갱신·insert 0). 미발견은 warn 만 남기고, 조회 실패는 열화 갱신과 safePgError error 를 남긴다. 거래소 자동취소 예외는 보존된다. autoInsertRow 에 취소성 가드가 있고, 가격 CHECK 주석은 원격 기준으로 고쳐졌다. 신규 접수 A 경로 회귀는 0 이다.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (A1): 공유 세션 WinForms 수동 통보는 info, relay 가 낸 주문인데 못 붙은 통보만 ERROR — 관련성 판정은 후보 존재 + relay 흔적</name>
  <files>relay/src/ws/order-handler.ts, relay/tests/ws-order.test.ts</files>
  <behavior>
    - A1-①: relay 가 주문을 한 건도 보내지 않은 상태에서 origin Manual 인 WinForms 통보 3건을 밀어 넣는다(A 3405000065 · E 3405000057 · C 3405000066 price 0, 스텁 조회 null). info 에 「다른 클라이언트」·「relay 가 낸 주문」 문구가 3회 있다. error 에 「붙지 않는 수동 통보」가 없다. updates 0, inserts 0, lookups 3 이고, info·error 어디에도 계좌번호가 없다
    - A1-②: relay 가 낸 수동 주문이 접수 A 로 정산된 뒤(finish), 같은 orderNo 의 E 가 조회 null 로 도착하면 ERROR 「붙지 않는 수동 통보」가 유지된다(finish 직후 플러시 전 경합 = relay 자기 주문). 같은 테스트에서 그 주문을 원주문번호로 가리키는 수동 C(새 orderNo, orgOrderNo = relay 주문번호, 조회 null)도 ERROR 다
    - 기존 ⑨(timeout 뒤 늦은 통보) · ㉑(좁히기 실패) · ㉖(연결 종료 후 도착)은 한 줄도 고치지 않고 ERROR 로 통과한다
  </behavior>
  <action>
**order-handler.ts, per A1:**
1. createOrderHandler 클로저에 「relay 흔적」 상태를 둔다. `relayTrail: Map<string /*userId*/, { day: string; isins: Set<string>; orderNos: Set<string> }>` 다.
   - `day` 는 `kstDayRangeUtc().from` 이다. `store/orders.js` 에서 값으로 import 한다(순환 없음 — orders.ts 는 handler 를 import 하지 않는다).
   - 내부 함수 `trailOf(userId)` 는 없으면 만들고, day 가 오늘과 다르면 두 Set 을 비워 오늘로 재설정한다.
   - `markTrailIsin(userId, isin)` 와 `markTrailOrderNo(userId, orderNo)` 를 둔다. 후자는 `normalizeOrderNo` 결과가 ""면 무시하고, 아니면 정규화값을 저장한다.
   - docstring 에 적을 것: 이 흔적은 「relay 가 오늘 이 사용자 대신 낸 수동 주문 중 행에 주문번호가 붙지 못했을 수 있는 것」의 종목과 「relay 가 정산으로 알게 된 주문번호」다. 사용자당 1항목이고 KST 날짜가 바뀌면 비운다. 크기는 하루 relay 주문 수로 묶인다(T-16-33 — 무한 성장 없음). relay 재시작 시 사라진다. 재시작은 20:00 이후뿐이라 당일 흔적 손실은 수용 범위다.
2. 흔적을 찍는 자리는 셋이다.
   - `finish(null)`(timeout)에서 `markTrailIsin(userId, msg.isin)` 를 부른다.
   - `finish(notice)` 에서 `markTrailOrderNo(userId, notice.orderNo)` 를 부른다.
   - `closeConn` 에서 `state.pending` 을 비우기 **전에** 각 항목의 isin 으로 `markTrailIsin(state.userId, p.isin)` 을 부른다.
3. `isRelayRelated(userId, notice, hadCandidates)` 는 아래 중 하나면 참이다.
   - hadCandidates
   - 흔적 isins 에 notice.isin 이 있다
   - 흔적 orderNos 에 normalizeOrderNo(notice.orderNo) 가 있다
   - normalizeOrderNo(notice.orgOrderNo) 가 비어 있지 않고 흔적 orderNos 에 있다
   docstring 에 오판 방향을 적는다. 같은 날 relay 주문이 걸렸던 종목의 WinForms 주문은 ERROR 로 남는다(보수적 — 신호를 잃는 쪽보다 낫다).
4. hub 리스너의 `void recordUnmatched(userId, notice)` 를 `void recordUnmatched(userId, notice, candidates.length > 0)` 로 바꾼다. `recordUnmatched` 시그니처에 `hadCandidates: boolean` 을 더한다.
5. 수동 분기에서 행이 없을 때(existingId === null) 로그를 가른다. 기존 필드 세트 {orderNo, noticeType, resultCode, isin, quantity, price} 는 유지하고 계좌 필드는 없다.
   - `isRelayRelated` 가 참이면 **기존 `logger.error` 와 문구를 그대로 둔다**(⑨ ㉑ ㉖ 가 그 문구를 잠근다).
   - 거짓이면 `logger.info` 로 남긴다. 문구는 「[WS-order] 같은 세션의 다른 클라이언트(WinForms 등) 주문 통보 — relay 가 낸 주문이 아니다 (공유 게이트웨이 세션 팬아웃 · D-17 철회), 기록 대상 아님」 이다.
   - 두 경우 모두 갱신·insert 는 없다(기존 규율).
6. recordUnmatched docstring 의 「수동 주문도 이 분기를 탄다」 목록에 3번을 더한다. 「3. **공유 세션 팬아웃** — relay 는 D-17 철회로 WinForms 와 같은 DMA user_id 세션을 쓴다. 게이트웨이는 51 통보를 세션의 모든 연결로 보낸다(gh-trade Server.cpp:515-538 → Gateway.cpp:1093-1112). 그래서 relay 가 내지 않은 WinForms 주문 통보가 온다. 붙을 행이 없는 것이 정상이라 info 다(진단 Resolution A)」. 1·2 번과 흔적 기반 경합만 ERROR 라는 판정 규칙도 한 문단으로 적는다.

**테스트 (ws-order.test.ts):** 하위 describe 「quick-260923-e1m — 통보 기록 경로」에 A1-①, A1-② 를 추가한다. logger info/error/warn 은 spyOn 으로 본다(㉖ 패턴). ⑨ ㉑ ㉖ 은 수정하지 않는다. 먼저 RED(A1-① 이 error 로 실패)를 확인하고, 그다음 GREEN 을 확인한다. 마지막으로 relay 전체 스위트를 돌린다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/relay test && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && test "$(grep -c 'isRelayRelated' relay/src/ws/order-handler.ts)" -ge 2 && test "$(grep -c '다른 클라이언트' relay/src/ws/order-handler.ts)" -ge 1 && test "$(grep -c '붙지 않는 수동 통보' relay/src/ws/order-handler.ts)" -ge 1 && CHANGED="$(git status --porcelain -- relay)" && test "$(printf '%s\n' "$CHANGED" | grep -c .)" -le 5</automated>
  </verify>
  <done>
relay 와 무관한 공유 세션 수동 통보는 info 1줄(갱신·insert 0)로 남는다. relay 관련(후보·연결 종료·timeout·정산 번호) 미부착 통보는 기존 ERROR 를 유지한다. ⑨ ㉑ ㉖ 은 무수정으로 통과한다. relay 전체 스위트(기준선 564 + 신규)·typecheck·typecheck:tests 가 green 이고, relay 변경 파일은 계획한 5개뿐이다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트웨이 51 통보 → relay | 공유 세션 팬아웃으로 relay 가 내지 않은 주문(WinForms)의 통보가 섞여 들어온다. orderNo·orgOrderNo·quantity 는 와이어 원문이다 |
| relay → Supabase dma_orders (service role) | relay 가 유일한 writer 다(D-03). 잘못된 셀렉터는 남의 행·어제 행 쓰기가 된다(T-16-14) |
| relay stdout → Cloud Logging/docker 로그 | 감사 사본(D-24). 계좌번호와 PostgREST details 유출 경로다(T-16-45) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-e1m-01 | Information Disclosure | 새 로그 줄(A1 info · B1 info/warn/error · fill sink warn/error) | high | mitigate | 계좌 필드를 싣지 않는다(51 에 계좌 없음). fill sink 로그는 column·숫자만 싣는다. PostgREST 오류는 전부 safePgError 를 지난다. 테스트가 SAMPLE_ACCOUNT_NO 부재를 단언한다(A1-① · B1-③ · B1-⑥) |
| T-e1m-02 | Tampering | settleOriginal 이 원주문 행을 갱신 | high | mitigate | 조회는 findIdByOrderNo(userId, 번호)의 3축(사용자·당일)이다. 갱신은 orderRowId(PK) 셀렉터로 한다. patch 에서 orderNo·origin 을 빼서 취소 번호가 원주문 order_no 를 덮지 못하게 한다. filled → cancelled 역행은 상태 단조성 필터가 막는다. 열화 경로는 order_no 셀렉터가 userId 를 필수로 요구한다(selectorOf) |
| T-e1m-03 | Tampering | filled_qty 누적(lost update / 과대 누적) | medium | mitigate | CAS UPDATE(eq filled_qty·status) + 재조회 재시도(ORDER_FILL_CAS_MAX_ATTEMPTS)를 쓴다. drain 은 직렬이다. qty 초과는 자르지 않고 warn 으로 드러낸다. 51 통보 재생 경로는 없다(조사 확인) |
| T-e1m-04 | Repudiation | 취소성 통보가 행으로 남지 않음 | medium | mitigate | 원주문 행의 status·notice_type·message 에 C 가 반영된다. 미발견은 warn, R 은 info 로 남는다. Hub 감사 사본(stdout)은 그대로다. 조회 실패는 열화 갱신 + error 다(S-5) |
| T-e1m-05 | Repudiation | A1 등급 하향이 relay 실패를 가림 | medium | mitigate | 후보 존재·연결 종료·timeout·정산 번호 흔적이 있으면 ERROR 를 유지한다(⑨ ㉑ ㉖ + A1-② 가 잠근다). 오판 방향은 보수적(ERROR 쪽)이다 |
| T-e1m-06 | Denial of Service | relayTrail 메모리 | low | mitigate | 사용자당 1항목이고 KST 일 단위로 비운다. 크기는 하루 relay 주문 수로 묶인다(T-16-33) |
| T-e1m-07 | Denial of Service | fill 조각당 왕복 2회로 큐 적체 | low | accept | 큐는 비동기라 수신 경로(D-32)를 막지 않는다. 조각 90건 버스트도 수 초 안에 비워진다. 감사 기록이지 사용자 즉시 표시 경로가 아니다 |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/relay test` 가 전체 green 이다(기준선 22 files · 564 tests + 신규). ⑨ ⑫ ⑬(기대값만 delta 로 갱신) ①-c ㉑ ㉖ ㉗ ㉘ ㊸ ㊹ ㊺ 이 통과한다.
- `pnpm --filter @gh-radar/relay run typecheck` 와 `pnpm --filter @gh-radar/relay run typecheck:tests` 가 오류 0 이다.
- 회귀 4종이 존재하고 통과한다. (1) A1-①: 공유 세션 수동 통보 → info, ERROR 아님. (2) B1-①/②/⑦: 자동 C → 원주문 행 cancelled, insert 없음. (3) B1-③: R804 → 원주문 상태 유지, insert 없음. (4) S1-① 과 order-store S1: 대기 밖 E 여러 조각 → filled_qty 누적, qty 도달 시 filled.
- `git status --porcelain -- supabase/migrations` 가 비어 있다(마이그레이션 없음, 미추적 새 파일 포함).
- `git status --porcelain -- relay` 는 계획된 5개 파일만 나온다. webapp·다른 quick 디렉터리는 손대지 않았다.
</verification>

<success_criteria>
- 공유 세션 WinForms 통보가 더 이상 ERROR 를 만들지 않는다. relay 자기 주문의 미부착만 ERROR 다.
- 자동주문 취소가 원주문 행을 cancelled 로 닫는다. 가격 0 ERROR 와 취소성 insert 는 0 이다.
- 대기 밖 체결이 누적되어 전량 체결 행이 filled 가 된다.
- 코드·테스트만 바뀌었다. 커밋·배포·원격 DB 변경은 0 이다.
</success_criteria>

<output>
`.planning/quick/260923-e1m-relay-ws-order-info/260923-e1m-SUMMARY.md` 를 만든다. 반드시 들어갈 절:
- **S1 설계 판단**: RPC 대신 CAS 를 택한 근거 3가지와 조사 결과(finish 에 누적 방식 없음 · incr_api_usage 가 유일한 증분 패턴).
- **배포 단계 (20:00 KST 이후에만)**:
  1. 마이그레이션 없음 → DB 단계 없음.
  2. relay 를 기존 `deploy-relay.sh` 로 배포한다(장중 08:00~20:00 재시작 금지).
  3. `/healthz` 와 `smoke-relay.sh` 로 검증한다.
  4. 그다음 push 한다. 이 저장소에서 push 는 곧 webapp 배포이고, 이번 변경에는 webapp diff 가 없다. 다른 세션의 webapp 미커밋 변경은 이 커밋에 섞지 않는다.
  5. 배포 다음 장중에 docker logs 를 확인한다. 「다른 클라이언트」 info 가 나오는지, 「붙지 않는 수동 통보」 ERROR 가 relay 주문이 없는 창에서 0 인지, 「가격이 0 이하」 가 0 인지 본다. dma_orders 에서는 자동 원주문의 cancelled 전이와 filled_qty 누적을 확인한다.
- **관측된 잔여 (범위 밖)**:
  - 수동 취소 pending 정산 시 원주문 행 미갱신
  - 기존 오염 행(LC 059/070 · accepted 고착 원주문) 미보정
  - finish 직후 200ms 플러시 전 E 조회 누락 경합(이제 ERROR 로 드러남)
  - 교보 경로
- 테스트 수 전후, 새 테스트 목록, 변경 파일 5개.
</output>
