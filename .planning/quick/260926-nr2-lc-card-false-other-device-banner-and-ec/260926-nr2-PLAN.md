---
phase: quick-260926-nr2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/relay.ts
  - packages/shared/src/index.ts
  - webapp/src/components/trading/strategy-log.tsx
  - webapp/src/components/trading/card/strategy-card.tsx
  - webapp/src/components/trading/__tests__/strategy-log.test.tsx
  - webapp/src/components/trading/__tests__/strategy-card-flow.test.tsx
  - webapp/src/lib/limit-chaser.ts
  - webapp/src/lib/use-relay-socket.ts
  - webapp/src/lib/relay-provider.tsx
  - webapp/src/components/trading/strategy-status-card.tsx
  - webapp/src/lib/__tests__/relay-socket.test.ts
  - webapp/src/lib/__tests__/limit-chaser.test.ts
autonomous: true
requirements: [NR2-1, NR2-2, NR2-3, NR2-4, NR2-5, NR2-6, NR2-7]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "(NR2-1) 카드 헤더 래치 LED 를 눌러(lc.arm) 돌아온 에코는 「다른 단말에서 변경됐어요」 배너를 세우지 않고 래치 전이 로그 1줄만 남긴다. lc.arm 전송이 성공하면 3초 안에 답(그 키의 60 에코 또는 거부 통지)이 없을 때 「미반영 · 서버 응답을 기다리고 있어요」가 서고, 거부 통지가 오면 미반영이 거둬지며 거부 원문이 카드 경보·로그에 선다. send 가 false 면 추적도 재전송도 없다"
    - "(NR2-2) 서버 이중 에코(직답 + 300ms 플러시 동일 사본)처럼 내용이 직전과 같은 에코는 배너·로그·발주 배지를 전혀 바꾸지 않는다"
    - "(NR2-3) 서버 런타임 푸시(래치 자동 ON · 발주로 게이트 소진 · 게이트 복원 · 체결 수량/기준선 갱신)는 배너를 세우지 않는다. 발주로 매수 게이트가 소진된 에코는 여전히 로그 「매수 발주 — 무장 해제」와 배지 「발주 완료 · 무장 해제」를 낸다"
    - "(NR2-4) 이 브라우저에서 「전부 정지」를 누르면 매수 무장 카드마다 로그가 「매수 무장 해제」(「매수 발주」 아님)이고 「발주 완료 · 무장 해제」 배지와 배너가 없다. 65 수신 로그는 「전부 정지가 반영됐어요 · 서버가 모든 전략을 비활성화했어요」이고 「장 마감 규칙」이라고 말하지 않는다. 15:40 KRX 자동 해제 에코도 발주로 읽지 않고 「서버가 KRX 전략을 자동 비활성화했어요 (장 마감 규칙)」 1줄을 남긴다"
    - "(NR2-5) 재접속 lc.snap 이 같은 내용의 새 객체로 목록을 갈아도 어느 카드에도 배너·로그가 생기지 않는다"
    - "(NR2-6) sellOrderQty · sellQtyTrackBaseline · cancelQtyTrackBaseline(와 relay 파생 name/code)만 바뀐 에코는 「서버 반영 완료」를 로그하지 않는다. S→C 전용 필드 목록은 packages/shared 한 곳에서만 정의된다"
    - "(NR2-7) 다른 단말이 사용자 설정 값(예: buyWatchQty)을 바꾼 에코는 종전대로 6초 배너(role=status) + 로그 1줄을 낸다 — 기존 flow ⑦·⑦b, strategy-card B카드 테스트, e2e 13 의미가 그대로다"
  artifacts:
    - path: "packages/shared/src/relay.ts"
      provides: "LIMIT_CHASER_SERVER_COUNTER_FIELDS · LIMIT_CHASER_SERVER_LATCH_FIELDS · LIMIT_CHASER_SERVER_ONLY_FIELDS 단일 정의, RelayLimitChaserInput 이 그 const 에서 파생"
      contains: "LIMIT_CHASER_SERVER_ONLY_FIELDS"
    - path: "webapp/src/components/trading/strategy-log.tsx"
      provides: "limitChaserValuesChanged · isRuntimeOnlyEcho · 교정된 strategiesDisabledLogLine · marketCloseDisabledLogLine"
      contains: "isRuntimeOnlyEcho"
    - path: "webapp/src/components/trading/card/strategy-card.tsx"
      provides: "「무엇이 바뀌었나」 기준 에코 분류 · 원인 인지 hadOrder · lc.arm 3초 추적"
      contains: "limitChaserDisableEchoes"
    - path: "webapp/src/lib/limit-chaser.ts"
      provides: "isLimitChaserArmRejection · isMarketCloseReleaseNotice · marketCloseReleaseKeysOf · limitChaserGateDisarmed (판정 유일 지점)"
      contains: "isLimitChaserArmRejection"
    - path: "webapp/src/lib/use-relay-socket.ts"
      provides: "limitChaserDisableEchoes 귀속 · 전부 정지 in-flight 창(65/수명/8초 경계) · STRATEGIES_DISABLE_ACK_TIMEOUT_MS"
      contains: "STRATEGIES_DISABLE_ACK_TIMEOUT_MS"
  key_links:
    - from: "webapp/src/components/trading/strategy-log.tsx"
      to: "packages/shared/src/relay.ts"
      via: "LIMIT_CHASER_SERVER_ONLY_FIELDS / LIMIT_CHASER_SERVER_COUNTER_FIELDS import (skip 목록 재나열 금지)"
      pattern: "LIMIT_CHASER_SERVER_(ONLY|COUNTER)_FIELDS"
    - from: "webapp/src/components/trading/card/strategy-card.tsx"
      to: "webapp/src/components/trading/strategy-log.tsx"
      via: "isRuntimeOnlyEcho(prev, server) 무동작 분기 + limitChaserValuesChanged 배너 조건"
      pattern: "isRuntimeOnlyEcho|limitChaserValuesChanged"
    - from: "webapp/src/lib/use-relay-socket.ts send"
      to: "relayReducer strategies-disable-sent / strategies.disabled"
      via: "msg.t === strategies.disable 전송 성공 시 in-flight 창 열기, 65·수명·타임아웃이 닫기"
      pattern: "strategies-disable-sent"
    - from: "webapp/src/components/trading/card/strategy-card.tsx"
      to: "useRelayContext().limitChaserDisableEchoes"
      via: "disable.echo === server 동일성 비교로 hadOrder=false"
      pattern: "limitChaserDisableEchoes"
    - from: "webapp/src/components/trading/strategy-status-card.tsx"
      to: "webapp/src/lib/use-relay-socket.ts"
      via: "STRATEGIES_DISABLE_ACK_TIMEOUT_MS 공유(65 유실 판정 시각 일치)"
      pattern: "STRATEGIES_DISABLE_ACK_TIMEOUT_MS"
---

<objective>
상따 카드가 「다른 단말에서 변경됐어요」를 오배너하고, 같은 뿌리(에코 원인 오분류)로 「매수 발주」·「발주 완료」·「서버 반영 완료」·「장 마감 규칙」을 잘못 말하는 결함을 한 번에 고친다.

뿌리: 카드 에코 이펙트가 **누가 보냈나**(`pendingRef` 비었음)와 **객체 동일성**(`prev === server`)만으로 에코를 분류한다. 그러나 60 에코의 상당수는 다른 단말이 아니라 (1) 내 lc.arm, (2) 서버 이중 에코, (3) 서버 런타임 푸시, (4) 내 전부 정지 / 15:40 서버 해제, (5) 재접속 lc.snap 이고, (6) `valuesChanged` 는 S→C 런타임 필드를 값 변경으로 센다.

요구 ID (이 quick 의 범위 정의 — 오케스트레이터 근본원인 1~6 에서 도출):
- NR2-1 내 lc.arm 에코 오배너 제거 + lc.arm 3초 미반영 추적 + 거부 인식·표시
- NR2-2 내용 동일 에코(서버 이중 에코) 무동작
- NR2-3 서버 런타임 푸시 무배너 (발주 소진 로그·배지는 유지)
- NR2-4 이 브라우저의 전부 정지 + 15:40 KRX 자동 해제 에코를 발주로 읽지 않음 + 65 로그 문구 교정
- NR2-5 재접속 lc.snap 동일 내용 무배너
- NR2-6 값 변경 판정이 S→C 전용 런타임 필드(+ relay 파생 name/code)를 무시 — shared 단일 정의
- NR2-7 다른 단말의 사용자 값 변경은 여전히 배너 (회귀 가드)

Purpose: 트레이더가 카드 고지를 믿을 수 있어야 한다. 「발주 완료」는 나가지 않은 주문을 미체결에서 찾게 만들고(Pitfall 10), 매 푸시마다 서는 「다른 단말」 배너는 진짜 다른 단말 변경 신호를 묻는다.
Output: 위 files_modified 의 코드·테스트 변경이 **working tree 에 남은 상태**(커밋 없음).

## 오케스트레이터 전제 정정 (gh-trade 서버 코드 읽기 전용 확인 결과 — 확정 사실로 쓴다)

- **65(`DisableStrategiesResp`, relay `strategies.disabled`)는 15:40 과 무관하다.** 송신자는 `gh-trade/server/src/net/Gateway.cpp` `ProcessDisableStrategies`(~L4472-4583) 하나이고, 순서 계약은 ① 상태 변경 → ② 저장 → ③ 키별 60 에코(`SendToSession` — 세션 전 연결) → ④ 65 집계를 **요청 연결에만 맨 마지막**에 보낸다. relay 는 그것을 사용자 전 소켓에 팬아웃한다(`relay/src/hub/subscription-hub.ts` ~L1034). 즉 65 는 「(이 사용자의) 전부 정지 완료」 신호이고, 지금 카드가 65 에 찍는 「서버가 모든 전략을 자동 비활성화했어요 (장 마감 규칙)」은 **전부 정지를 장 마감으로 오표시**한다.
- **15:40 은 `Server::DisableKrxLimitChasers`(`app/Server.cpp` ~L2256-2310)다.** `DisableLimitChasers('K')`(cfg 매수·매도·취소 게이트 off, 게이트가 이미 전부 꺼진 전략은 건너뜀) → 전략마다 `MarkEchoDirty`(60 에코는 **다음 300ms `FlushLimitChaserEchoes` 틱**에 세션 전 연결로) → 저장 → `SendServerMessageToSession(INFO, ctx.source="System", kind=Purge, isin/계좌 없음, 본문 "15:40 KRX 상따 매수·매도·취소 N건 해제 — …")` 를 **동기로 즉시**. 따라서 브라우저에는 **통지가 에코보다 먼저** 도착한다. 65 는 없다. NXT 는 애프터마켓(20:00)이라 해제되지 않는다.
- 다른 Purge 통지(`PurgeAllStrategies` — 06:00·20:00 K·20:05)는 `BroadcastServerMessage` 라 source 가 없고(src ""), 삭제라 60 에코를 내지 않는다.
- **lc.arm 거부**: `Gateway::ProcessArm{Sell,Cancel,Buy}Latch`(~L3458-3680)는 성공·멱등이면 즉답 60 에코(`ReplyToConn`) + 점등 갈래는 `MarkEchoDirty` 플러시 사본, 실패면 `SendServerMessageToConn(conn, "WARN", 사유)` 를 **기본 ServerMessageContext** 로 보낸다 → `src:"System"`, `i:""`, `a:""`, `kind:""`. relay 단계 거부는 `{t:"msg", lv:"ERROR", src:"Relay"}`(`relay/src/ws/fanout.ts` `rejectFrame` ~L331). 현 `isLimitChaserServerMessage` 는 이 둘을 **카드에 그리지 않는다**, `isLimitChaserSetRejection` 도 인식하지 않는다.
- **매수 발주**(`LimitChaser::SubmitBuyOrder` ~L2267): 매수 게이트만 소진하고 취소 게이트는 재무장·취소 래치/기준선은 리셋한다.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@webapp/src/components/trading/card/strategy-card.tsx
@webapp/src/components/trading/strategy-log.tsx
@webapp/src/lib/limit-chaser.ts
@packages/shared/src/relay.ts

<!-- 실행 전 필수 규칙 (사용자 전역 CLAUDE.md · 동시 세션) -->
- **git add / commit / push 금지.** 모든 변경은 working tree 에 남긴다. 커밋 메시지는 사용자가 확인한 뒤 따로 만든다.
- 다른 세션이 이 저장소에서 동시 작업 중이다. `.planning/phases/21-*`, `.planning/state.json`, `.planning/milestone.lock`, `tasks/lessons.md` 는 **읽지도 고치지도 스테이징하지도 않는다.**
- `/Users/alex/repos/gh-trade` 는 읽기 전용 참고다 — 수정 금지.
- 주석 스타일: 주변과 같은 밀도의 한국어 주석 + ★ 표식. 새 의존성 없음. 최소 영향.
- `@gh-radar/shared` 는 webapp 이 **dist** 로 해석한다(`packages/shared/package.json` exports → dist, dist 는 git 미추적). shared 를 고친 뒤에는 webapp typecheck/vitest 전에 반드시 `pnpm --filter @gh-radar/shared build`.

<interfaces>
<!-- 실행자가 코드베이스를 다시 탐색하지 않도록 뽑아 둔 계약 -->

strategy-card.tsx (useStrategyCardState):
- refs/state: `pendingRef: RelayLimitChaserInput|null`, `ackTimer`, `unacked`, `answerSeq`, `overwrittenRef`, `banner`/`bannerTimer`, `appliedAt`, `fired`, `prevServerRef`.
- `handleSent(cfg)` — lc.set 경로가 `use-lc-field-commit.ts` onSent 로 부른다(시그니처 유지).
- `acceptAnswer()` — 타이머 끄고 unacked=false, answerSeq+1. 「서버가 답했다」의 유일 함수.
- 키 일치 `lastLimitChaserEcho` 이펙트(~L385) → acceptAnswer. 에코 이펙트(~L391-446) → 삭제/전이 로그/fired/배너.
- 메시지 이펙트(~L469-496): `isLimitChaserServerMessage` 로 표시 몫 거름 → `serverMessageLogLine` → level error 만 `setLastError` → `isLimitChaserSetRejection` 이면 acceptAnswer.
- 65 이펙트(~L500-511): `strategiesDisabled` 변화 시 `strategiesDisabledLogLine()` 로그.
- `handleArm(kind)`(~L545): clickable 재확인 후 `send({t:"lc.arm", key: ledServer.key, latch: kind})` — 추적 없음.

strategy-log.tsx:
- private `valuesChanged(prev, next)`(~L143) skip = buyEnabled, sellEnabled, sellEntryLatched, cancelQtyEnabled, cancelTradeEnabled, cancelEntryLatched, buyEntryLatched, crud, key.
- `strategyLogLine(prev, next, {hadOrder})`, `TRANSITION_TEXT` (buyArmed '매수 무장', buyFired '매수 발주 — 무장 해제', buyDisarmed '매수 무장 해제', buyLatched '매수 진입 래치 ON — 잔량 항 판정 시작', sellLatched '매도 진입 래치 ON — 감시 시작', valuesApplied '서버 반영 완료'), private `cancelArmedOf`.
- `strategiesDisabledLogLine()` → 현재 '서버가 모든 전략을 자동 비활성화했어요 (장 마감 규칙)'.

use-relay-socket.ts:
- `RelayData`/`INITIAL_DATA`(~L562-620), `RelayAction` = local-status | frames | stale | reset(~L627-652), `relayReducer`, `applyFrame`(case "state" ~L668, "msg" ~L725, "lc" ~L732, "lc.snap" ~L747, "strategies.disabled" ~L810), `upsertLimitChaser`(~L922), `send`(~L1684, ws.send 후 true), 연결 수명 이펙트 teardown(~L1581), 반환 useMemo(~L1741). `RelayConnectionState` 인터페이스 문서(~L370-470).
- `RelayServerMsg` = {t:"msg", lv, m, i, a, src, kind}.

relay-provider.tsx: `RelayContextValue = Omit<RelayConnectionState,"sendOrder"> & {...}`, `EMPTY_RELAY_VALUE`(~L328) — 모든 테스트 목이 이것을 spread 한다.

strategy-status-card.tsx: local `const DISABLE_ACK_TIMEOUT_MS = 8_000`(~L93), 65 백스톱 이펙트(~L380-390), `send({t:"strategies.disable"})`(~L399). 이미 `@/lib/use-relay-socket` 에서 import 한다.

packages/shared/src/index.ts: relay 값 export 블록(`MAX_VI_ORDER_AMOUNT_KRW` 가 있는 `export { … } from "./relay"`, ~L91-96)과 타입 export 블록(`RelayLimitChaserInput` 포함)이 **명시 나열**이다 — 새 const/타입을 거기에 더해야 보인다.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): 「무엇이 바뀌었나」로 에코 분류 — shared 단일 정의 → 로그 판정 → 카드 에코 이펙트 → 화면</name>
  <files>packages/shared/src/relay.ts, packages/shared/src/index.ts, webapp/src/components/trading/strategy-log.tsx, webapp/src/components/trading/card/strategy-card.tsx, webapp/src/components/trading/__tests__/strategy-log.test.tsx, webapp/src/components/trading/__tests__/strategy-card-flow.test.tsx</files>
  <behavior>
    - strategyLogLine(prev, {...prev, sellOrderQty 변경}) / sellQtyTrackBaseline / cancelQtyTrackBaseline / name / code 만 바뀜 → null (「서버 반영 완료」 없음)
    - limitChaserValuesChanged: 위 필드만 바뀌면 false, buyWatchQty 가 바뀌면 true, 게이트·래치만 바뀌면 false
    - isRuntimeOnlyEcho: 동일 내용(새 객체 포함)·카운터 3종만·name/code 만 다름 → true / 래치·게이트·사용자 값이 다름 → false
    - LIMIT_CHASER_SERVER_ONLY_FIELDS 는 정확히 6개이고 COUNTER(3) ∪ LATCH(3) 와 같다
    - 카드: 내 lc.arm 에코(래치 ON) → 배너 없음 + 래치 로그 1줄
    - 카드: 내 스위치 전송 → 에코 → 같은 내용 새 객체 한 번 더(이중 에코) → 배너 없음 + 로그 줄 수 불변
    - 카드: 보내지 않은 래치 자동 ON / 게이트 false→true / 체결·기준선 갱신 → 배너 없음 (래치·무장은 전이 로그, 카운터는 로그 없음)
    - 카드: 보내지 않은 매수 무장 true→false → 「매수 발주 — 무장 해제」 로그 + 「발주 완료 · 무장 해제」 표시 + 배너 없음
    - 카드: 같은 내용 lc.snap(새 객체) → 배너·로그 없음
    - 카드: 보내지 않은 buyWatchQty 변경 → 종전대로 배너 + 로그 (기존 ⑦·⑦b 무수정 통과)
    - strategiesDisabledLogLine() 은 전부 정지 문구, marketCloseDisabledLogLine() 은 15:40 KRX 문구
  </behavior>
  <action>
**(1) shared 단일 정의 (NR2-6).** `packages/shared/src/relay.ts` 에서 `RelayLimitChaser` 타입 바로 뒤에 세 readonly 튜플을 export 한다: `LIMIT_CHASER_SERVER_COUNTER_FIELDS`(sellOrderQty · sellQtyTrackBaseline · cancelQtyTrackBaseline), `LIMIT_CHASER_SERVER_LATCH_FIELDS`(sellEntryLatched · cancelEntryLatched · buyEntryLatched), `LIMIT_CHASER_SERVER_ONLY_FIELDS`(두 튜플을 펼쳐 합친 6개). 각각 `as const` 로 선언하고 `satisfies readonly (keyof RelayLimitChaser)[]` 로 키 오타를 컴파일 타임에 막는다. 타입 `LimitChaserServerOnlyField = (typeof LIMIT_CHASER_SERVER_ONLY_FIELDS)[number]` 도 export. `RelayLimitChaserInput` 의 Omit 합집합을 `LimitChaserServerOnlyField | "key" | "market" | "name" | "code"` 로 바꿔 여섯 이름이 이 파일에서 **const 한 곳에만** 적히게 한다(타입 결과는 종전과 동일해야 한다). 기존 「⚠️ S→C 전용 6필드」 문서 주석은 유지하되 정본이 이 const 라고 가리키고, 카운터(서버 런타임 값 — 체결·래칫이 바꾼다)와 래치(사용자에게 보이는 전이)가 왜 갈리는지 한 줄씩 적는다. `packages/shared/src/index.ts` 의 relay 값 export 블록에 세 const 를, 타입 export 블록에 `LimitChaserServerOnlyField` 를 더한다. 그다음 `pnpm --filter @gh-radar/shared build`.

**(2) 로그 판정 (NR2-6 · NR2-2 · NR2-5 · NR2-4 문구).** `webapp/src/components/trading/strategy-log.tsx`:
- private `valuesChanged` 를 export `limitChaserValuesChanged(prev, next)` 로 바꾸고(내부 호출부도 교체), skip 집합을 모듈 스코프 Set 하나로 만든다: 게이트 4종(buyEnabled · sellEnabled · cancelQtyEnabled · cancelTradeEnabled) ∪ shared `LIMIT_CHASER_SERVER_ONLY_FIELDS` 펼침 ∪ crud · key · name · code. 이름을 다시 나열하지 않는다(shared const 를 import). 기존 래치 ★ 주석(T-17-39)은 살리고, ★ 카운터 3종은 서버 런타임(체결 → sellOrderQty, 호가 래칫 → 기준선)이라 사용자 설정 반영이 아니다, ★ name/code 는 relay 파생 표시값이다 를 더한다.
- export `isRuntimeOnlyEcho(prev, next)`: 두 객체 키 합집합에서 `LIMIT_CHASER_SERVER_COUNTER_FIELDS` 와 name · code 를 뺀 나머지가 전부 `Object.is` 로 같으면 true(완전 동일도 true). ★ 래치는 이 집합에 **넣지 않는다** — 래치 ON/해제는 사용자에게 보이는 전이 로그다. ★ 이 함수가 「내용상 새로 말할 것이 없는 에코」의 유일 판정이다.
- `strategiesDisabledLogLine()` 반환을 정확히 `전부 정지가 반영됐어요 · 서버가 모든 전략을 비활성화했어요` 로 바꾸고 문서에 「65 = gh-trade `ProcessDisableStrategies` 집계 응답(전부 정지)만의 신호, 15:40 과 무관」을 적는다(위 objective 의 정정 근거). 새 export `marketCloseDisabledLogLine()` 은 정확히 `서버가 KRX 전략을 자동 비활성화했어요 (장 마감 규칙)` 를 반환한다(16-UI-SPEC §15:40 문구를 `DisableKrxLimitChasers` 의 'K' 필터 사실에 맞춰 KRX 로 좁힘 — Task 3 가 소비). 파일 상단 ① 의 「15:40 전략 일괄 비활성화(65)」 서술도 사실대로 고친다.

**(3) 카드 에코 이펙트 (NR2-1 배너 · NR2-2 · NR2-3 · NR2-5 · NR2-7).** `strategy-card.tsx` 에코 이펙트(~L391-446)에서 삭제 갈래와 `prev === server` 동일성 조기 반환은 그대로 두고, 그 **다음**에:
- `prev !== null && isRuntimeOnlyEcho(prev, server)` 면 `prevServerRef.current = server`, `overwrittenRef.current = 0` 만 하고 return. 로그·배너·fired·appliedAt 을 건드리지 않고, `acceptAnswer` 도 부르지 않으며(응답 채널은 키 일치 `lastLimitChaserEcho` 이펙트 — 같은 내용의 lc.snap 을 답으로 세면 재접속만으로 미반영이 거둬진다는 use-relay-socket `lastLimitChaserEcho` 문서의 규율과 같다), **`pendingRef` 도 건드리지 않는다.** ★ 주석으로 근거를 남긴다: (i) 내용이 같으면 누구에게 귀속할 전이가 없다. (ii) gh-trade 는 lc.set/lc.arm 에 즉답 에코를 주고 `MarkEchoDirty` 가 300ms 플러시 동일 사본을 또 민다 — 여기서 pendingRef 를 소비하면 사본이 내 진짜 에코보다 먼저 온 순간 내 변경이 「다른 단말」로, 내 매수 끄기가 「발주」로 읽힌다. (iii) 남는 비용: 서버가 내 lc.set 을 prev 와 같은 값으로 정규화해 답하면 pendingRef 가 다음 비런타임 에코까지 남아 그 한 번의 다른 단말 배너를 삼키고 hadOrder 를 내 마지막 요청에서 읽는다 — (ii) 의 오귀속보다 싸다. (iv) 게이트/래치 런타임 푸시가 내 에코보다 먼저 오면 여전히 pendingRef 를 소비한다(기존 경합, 이제 더 좁다).
- 배너 조건을 `sent === null && prev !== null && limitChaserValuesChanged(prev, server)` 로 바꾼다. 기존 「다른 단말 변경」 주석을 「누가 보냈나가 아니라 무엇이 바뀌었나」로 다시 쓴다: 게이트·래치·런타임 카운터는 서버가 스스로 양방향으로 뒤집는다(에코 xxxEnabled = cfg && armed — OnQuote 자동 래치, SubmitBuy/Sell/Cancel 게이트 소진, Restore*Gate 복원, OnExecution 체결, lc.arm 답, 전부 정지) → 사용자 설정 값이 내 요청 없이 바뀐 경우만 다른 단말이다. 전이 로그 줄(매수 무장·래치 ON 등)은 종전대로 남는다.
- hadOrder 계산·fired 래치·삭제 갈래는 이 태스크에서 바꾸지 않는다(원인 인지는 Task 3).

**(4) 테스트.** `strategy-log.test.tsx` 에 behavior 의 순수 함수 케이스를 더하고 기존 ⑫ 를 새 65 문구로 고친 뒤 15:40 문구 케이스를 더한다. `strategy-card-flow.test.tsx` 에 describe `에코 분류 — 무엇이 바뀌었나 (quick-260926-nr2)` 를 더해 behavior 의 카드 케이스를 단언한다: 래치 문장은 `TRANSITION_TEXT` 를 strategy-log 에서 import 해 쓰고(문구 재타이핑 금지), lc.arm 케이스는 `echo({ sellEnabled: true })` 로 시작해 `led('sell')` 클릭 후 `echo({ sellEnabled: true, sellEntryLatched: true })` 를 `limitChasers` 와 `lastLimitChaserEcho` 로 내려 준다. 로그 줄 수 불변은 `logRows().length` 전후 비교. 기존 ⑬ 은 기대 문구만 새 65 문구로 바꾼다. 기존 ⑦ · ⑦b 와 `strategy-card.test.tsx` 의 B 카드 배너 테스트는 **수정 없이** 통과해야 한다(NR2-7 회귀 가드).
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/webapp run test src/components/trading/__tests__/strategy-log.test.tsx src/components/trading/__tests__/strategy-card-flow.test.tsx src/components/trading/__tests__/strategy-card.test.tsx</automated>
  </verify>
  <done>shared 가 세 const 와 `LimitChaserServerOnlyField` 를 export 하고 `RelayLimitChaserInput` 이 그것에서 파생된다. strategy-log 의 skip 목록이 shared const 를 import 한다. 카드는 내용 동일·카운터만 다른 에코에 무동작이고, 배너는 사용자 값 변경일 때만 선다. 세 테스트 파일과 relay typecheck 가 통과한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: relay 연결 층 — 비활성화 원인 귀속 (이 브라우저의 전부 정지 65 창 · 15:40 KRX 해제 통지)</name>
  <files>webapp/src/lib/limit-chaser.ts, webapp/src/lib/use-relay-socket.ts, webapp/src/lib/relay-provider.tsx, webapp/src/components/trading/strategy-status-card.tsx, webapp/src/lib/__tests__/relay-socket.test.ts</files>
  <behavior>
    - 연결 후 send({t:"strategies.disable"}) → 그 뒤 온 lc 에코는 limitChaserDisableEchoes.get(key) = {echo: 그 프레임 item(=lastLimitChaserEcho 와 같은 객체), cause: "killSwitch"}
    - strategies.disabled(65) 수신 후 같은 키 에코 → 항목 삭제(귀속 없음)
    - send 후 STRATEGIES_DISABLE_ACK_TIMEOUT_MS 경과 → 이후 에코 귀속 없음
    - 소켓이 열리기 전 send → false, 이후 에코 귀속 없음
    - send 후 연결이 끊겨 재접속 → 새 연결의 에코 귀속 없음
    - lc.snap(KRX 키 매수 무장 + NXT 키 매수 무장) → msg{lv INFO, src System, kind Purge, i "", a ""} → KRX 키 게이트 off 에코 = cause "marketClose" / NXT 키 매수 off 에코 = 귀속 없음 / 그 KRX 키 다음 에코 = 항목 삭제
    - Purge 통지 뒤 그 KRX 키의 첫 에코가 게이트를 하나도 끄지 않으면 귀속 없음(대기 키만 소비)
    - lc.snap 은 귀속 맵을 비운다
    - strategy-status-card 의 65 백스톱이 종전과 같은 8초로 동작한다
  </behavior>
  <action>
**(1) 판정 유일 지점 (limit-chaser.ts).** 기존 메시지 판정 함수들 옆에 순수 함수 셋을 더한다 — 서버 계약 결합은 여기 한 곳의 주석이 소유한다(objective 의 gh-trade 확인 사실을 파일·함수명으로 인용):
- `isMarketCloseReleaseNotice(msg: { src: string; kind: string })` → `src === 'System' && kind === 'Purge'`. 동등 비교만, 본문 `m` 은 읽지 않는다(이 파일의 규율). 문서: 현재 이 조합의 유일 송신자는 `Server::DisableKrxLimitChasers` 이고 PurgeAllStrategies 의 Purge 는 source 가 없어(src "") 걸리지 않으며 그쪽은 삭제라 60 에코도 없다.
- `limitChaserGateDisarmed(prev, next)` → buyEnabled 또는 sellEnabled 또는 취소 무장(cancelQtyEnabled || cancelTradeEnabled) 중 하나라도 true→false.
- `marketCloseReleaseKeysOf(list: readonly RelayLimitChaser[])` → exchange 가 'KRX' 이고 에코상 게이트(buyEnabled · sellEnabled · cancelQtyEnabled · cancelTradeEnabled) 중 하나라도 켜진 전략의 key 집합. 문서: 에코 게이트 = cfg && armed 이므로 에코상 켜짐 ⇒ cfg 켜짐 ⇒ `DisableLimitChasers('K')` 가 해제·`MarkEchoDirty` 로 반드시 에코한다. NXT 는 애프터마켓이 20:00 까지라 제외된다.

**(2) 귀속 상태 (use-relay-socket.ts, NR2-4).**
- `strategy-status-card.tsx` 의 65 대기 상한을 이 파일로 옮겨 `export const STRATEGIES_DISABLE_ACK_TIMEOUT_MS = 8_000` 로 두고 기존 문서 주석을 함께 옮긴다. export 타입 `LimitChaserDisableCause = "killSwitch" | "marketClose"`, `LimitChaserDisableEcho = { echo: RelayLimitChaser; cause: LimitChaserDisableCause }`.
- `RelayConnectionState` 와 `RelayData` 에 `limitChaserDisableEchoes: ReadonlyMap<string, LimitChaserDisableEcho>` 를 더하고 문서화한다: 「이 60 에코는 발주가 아니라 비활성화의 결과다」 — 키 → 그 에코 객체. 소비자는 **에코 객체 동일성**으로 대조한다(나중 lc.snap 객체는 절대 맞지 않는다). ⚠️ 다른 단말(같은 사용자의 다른 탭·기기 포함)의 전부 정지는 여기 표현되지 않는다 — 에코가 65 보다 먼저 오므로 그 탭은 원인을 알 시점이 없다(Pitfall 10 한계). `RelayData` 내부 전용(반환하지 않음) 필드 `killSwitchInFlight: boolean`, `marketCloseReleaseKeys: ReadonlySet<string>`. `INITIAL_DATA` 는 모듈 상수 빈 Map / false / 빈 Set.
- `RelayAction` 에 `{ type: "strategies-disable-sent" }`, `{ type: "strategies-disable-expired" }` 를 더한다. sent → killSwitchInFlight true. expired → false(이미 false 면 같은 state 참조 반환).
- `send`: `ws.send` 한 뒤 `msg.t === "strategies.disable"` 이면 sent 를 dispatch 하고, 이전 만료 타이머 ref 를 지운 뒤 `STRATEGIES_DISABLE_ACK_TIMEOUT_MS` 뒤 expired 를 dispatch 하는 타이머를 건다. 소켓 미연결로 false 를 돌려주는 갈래에서는 아무것도 dispatch 하지 않는다. 연결 수명 이펙트 teardown(~L1581)에서 이 타이머를 지운다. ★ 시간 경계 근거 주석: 65 는 서버 핸들러의 **마지막** 프레임이고 키별 에코가 그 앞에 같은 연결로 온다(ProcessDisableStrategies 순서 계약). 살아 있는 소켓에서 65 가 끝내 안 오는 경우는 relay 가 게이트웨이 전에 거부했거나(그러면 전부 정지 에코도 없다) 프레임 유실뿐이고, 그 판정 시각은 상태 카드가 「65 유실」을 선언하는 시각과 같아야 하므로 같은 상수를 쓴다. 벽시계 창이 아니라 65·수명이 1차 경계이고 이 타이머는 백스톱이다.
- `applyFrame`:
  - `"lc"`: 기존 upsert·`lastLimitChaserEcho` 갱신을 유지하면서 원인을 정한다 — `killSwitchInFlight` 면 "killSwitch", 아니면 `marketCloseReleaseKeys` 에 그 키가 있고 `limitChaserGateDisarmed(기존 목록의 같은 키 항목, frame.item)` 이면 "marketClose"(기존 항목이 없으면 원인 없음). 그 키는 원인과 무관하게 `marketCloseReleaseKeys` 에서 뺀다(첫 에코가 소비 — 가지고 있을 때만 새 Set). 원인이 있으면 맵에 key → `{ echo: frame.item, cause }` 를 넣은 새 Map, 원인이 없는데 맵에 키가 있으면 뺀 새 Map, 그 밖에는 **같은 참조**(카드 재렌더 예산).
  - `"lc.snap"`: 귀속 맵을 빈 상수로, `marketCloseReleaseKeys` 를 빈 Set 으로(스냅샷은 복원이지 원인이 아니고 객체 정체성이 전부 새것이다).
  - `"strategies.disabled"`: 기존 저장에 더해 killSwitchInFlight false.
  - `"msg"`: 기존 prepend 에 더해 `isMarketCloseReleaseNotice(frame)` 면 `marketCloseReleaseKeys = marketCloseReleaseKeysOf(state.limitChasers)`.
  - 수명: `"state"` 프레임의 s 가 "ready" 가 아니면, 그리고 `local-status` 액션이면 killSwitchInFlight false · `marketCloseReleaseKeys` 빈 Set(요청 도중 끊긴 연결의 에코는 새 연결에 lc.snap 으로 돌아온다). `reset` 은 이미 INITIAL_DATA.
- 반환 useMemo 에 `limitChaserDisableEchoes` 를 더한다.

**(3) relay-provider.tsx.** `EMPTY_RELAY_VALUE` 에 빈 ReadonlyMap 모듈 상수로 `limitChaserDisableEchoes` 를 더하고 한 줄 주석(Provider 밖에는 소켓도 비활성화 요청도 없다).

**(4) strategy-status-card.tsx.** 로컬 `DISABLE_ACK_TIMEOUT_MS` 를 지우고 `@/lib/use-relay-socket` 에서 `STRATEGIES_DISABLE_ACK_TIMEOUT_MS` 를 import 해 백스톱 이펙트에 쓴다. 동작·문구 불변.

**(5) relay-socket.test.ts.** describe `상따 비활성화 원인 귀속 (quick-260926-nr2)` 로 behavior 전 항목을 기존 하네스(`render()` · `connected(hook)` · `ws.push` · `hook.result.current.send`)로 단언한다. 15:40 통지 프레임의 m 은 실제 서버 문구 모양을 쓰되 판정은 src·kind 로만 되는지 확인한다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp run test src/lib/__tests__/relay-socket.test.ts src/components/trading/__tests__/strategy-status-card.test.tsx && pnpm --filter @gh-radar/webapp run typecheck</automated>
  </verify>
  <done>relay 컨텍스트가 `limitChaserDisableEchoes` 를 내보내고, 이 브라우저의 전부 정지 창(65 · 연결 수명 · 8초 백스톱으로 닫힘)과 15:40 KRX 해제 통지(키별 첫 에코로 소비)가 에코 객체 단위로 귀속된다. 상태 카드와 소켓 훅이 같은 65 상한 상수를 쓴다. relay-socket · strategy-status-card 테스트와 webapp typecheck 가 통과한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 카드 소비 — 원인 인지 hadOrder · lc.arm 3초 추적과 거부 인식</name>
  <files>webapp/src/components/trading/card/strategy-card.tsx, webapp/src/lib/limit-chaser.ts, webapp/src/lib/__tests__/limit-chaser.test.ts, webapp/src/components/trading/__tests__/strategy-card-flow.test.tsx</files>
  <behavior>
    - isLimitChaserArmRejection: {lv WARN, src System, i "", a "", kind ""} → true / System INFO → false / System WARN + kind Purge → false / {lv ERROR, src Relay} (i·a 가 빈 값 또는 이 전략 축) → true / LimitChaser WARN 같은 isin → true, 다른 isin → false / SetLimitChaser → false / isin 또는 accountNo 빈 값 → false
    - isMarketCloseReleaseNotice · marketCloseReleaseKeysOf(KRX·게이트 켜짐만) · limitChaserGateDisarmed 단위 케이스
    - 카드: 전부 정지 귀속 에코(buy·sell true→false) → 「매수 무장 해제」, 「매수 발주」 없음, 「발주 완료 · 무장 해제」 없음, 배너 없음
    - 카드: 15:40 귀속 에코 → 위와 같음 + 「서버가 KRX 전략을 자동 비활성화했어요 (장 마감 규칙)」 로그 1줄
    - 카드: 귀속 맵의 echo 가 지금 server 와 다른 객체면 귀속 무시 → 「매수 발주 — 무장 해제」
    - 카드: LED 클릭(send true) → 3초 무응답 → 「미반영」, 30초 뒤에도 send 1회
    - 카드: LED 클릭 → 3초 안에 그 키 에코(lastLimitChaserEcho) → 미반영 없음
    - 카드: LED 클릭 → System WARN 거부 통지 → 미반영 없음 + card-server-error 에 원문 + 로그 1줄
    - 카드: arm 이 없을 때 온 System WARN 은 카드에 서지 않는다(표시 몫 불변)
    - 카드: LED 클릭인데 send false → 3초 뒤에도 미반영 없음, send 1회(재전송 없음)
  </behavior>
  <action>
**(1) lc.arm 답 판정 (limit-chaser.ts, NR2-1).** 판정 유일 지점에 `isLimitChaserArmRejection(msg: { src; i; a; lv; kind }, isin, accountNo)` 를 더한다. isin 이나 accountNo 가 빈 값이면 false, lv 는 WARN 또는 ERROR 만. 인정하는 모양 셋: (a) 게이트웨이 36/37/38 거부 — src 'System' 이고 i · a · kind 가 모두 빈 값(`ProcessArm*Latch` 가 기본 ServerMessageContext 로 보낸다); (b) relay 게이트웨이 전 거부 — src 'Relay'(fanout `RELAY_MSG_SOURCE`)이고 i 는 빈 값 또는 isin, a 는 빈 값 또는 accountNo — **구현 전에 `relay/src/ws/fanout.ts` 의 lc.arm 갈래(`#strategySession` · `#armLatchAccount` · `#accountAllowed` · `#onStrategySendFailed`)가 실제로 싣는 i/a 를 읽고, 항상 계좌를 싣는다면 그만큼 좁힌다**; (c) src 'LimitChaser' 이고 i === isin(D-20 설계 경로 — 서버가 나중에 맥락을 붙여도 산다). 본문 m 은 읽지 않는다. 문서: 이 함수는 「이 통지가 **내가 보낸 lc.arm 의 답**인가」만 묻고 호출자가 arm 을 보내 놓은 창 안에서만 의미가 있다 — 게이트웨이가 arm 거부에 키를 싣지 않으므로 상관은 in-flight 창으로 한다. 실패 모드: 창 안에 무관한 같은 모양 통지가 오면 미반영이 일찍 거둬질 뿐(= 이번 변경 전 동작: 추적 없음)이고, 거짓 「미반영」은 만들지 않는다. 같은 파일 `isLimitChaserServerMessage` 문서의 「`LimitChaser` 가 lc.arm 실패 사유의 유일한 경로」 문장을 사실(현 gh-trade 는 src System 으로 보낸다 → 이 새 함수)에 맞게 고치되 그 함수의 로직은 바꾸지 않는다.

**(2) 카드 (strategy-card.tsx).**
- `handleSent` 의 타이머 부분을 `startAckWait`(useCallback: unacked false, 기존 타이머 지우고 `ACK_TIMEOUT_MS` 뒤 unacked true)로 뽑고, `handleSent` = `pendingRef.current = cfg` + `startAckWait()`. 시그니처 불변.
- `armInFlightRef = useRef(false)` 를 두고 키 변경 이펙트에서 false 로 되돌린다. `handleArm`: `send(...)` 가 true 일 때만 `armInFlightRef.current = true` + `startAckWait()`. `pendingRef` 는 **건드리지 않는다**(arm 은 lc.set 이 아니다 — hadOrder 가 그것을 매수 끄기로 읽으면 안 된다). false 면 아무것도 하지 않는다(추적·재전송 없음 — T-16-10 · T-17-40). handleArm 문서 ★ 에 「3초 표시만, 재전송 없음」을 더한다.
- 키 일치 `lastLimitChaserEcho` 이펙트에서 `acceptAnswer()` 앞에 `armInFlightRef.current = false`.
- 메시지 이펙트: 새 메시지마다 `armAnswer = armInFlightRef.current && isLimitChaserArmRejection(msg, isin, accountNo)` 를 먼저 구한다. `armAnswer` 도 아니고 `isLimitChaserServerMessage` 도 아니면 건너뛴다(표시 몫 불변). 로그는 종전대로 `serverMessageLogLine`. `setLastError` 는 level 이 error 이거나 `armAnswer` 일 때 — 사용자 클릭에 대한 거절은 클릭한 자리(카드 경보)에 보여야 한다(PC-7 무로그 fail-safe 금지). `armAnswer` 면 `armInFlightRef.current = false` + `acceptAnswer()`. ERROR 의 `isLimitChaserSetRejection` → acceptAnswer 는 그대로.
- 원인 인지 hadOrder (NR2-4): `useRelayContext()` 에서 `limitChaserDisableEchoes` 를 받는다. 에코 이펙트의 비런타임 갈래에서 `disable = limitChaserDisableEchoes.get(key)`, `cause = disable !== undefined && disable.echo === server ? disable.cause : null`. hadOrder = cause 가 있으면 false, 없으면 종전(`sent === null ? true : sent.buyEnabled === true`). cause 가 "marketClose" 면 전이 로그 뒤에 `marketCloseDisabledLogLine()` 1줄을 더 남긴다. fired 래치는 이 hadOrder 를 그대로 쓴다(귀속 에코는 fired 를 세우지 않는다). 이펙트 deps 에 `limitChaserDisableEchoes` 를 더한다(동일성 조기 반환이 재실행을 무해하게 한다). Pitfall 10 주석을 다시 쓴다: 발주가 아닌 해제로 **알 수 있는** 것은 내 lc.set(buyEnabled false) · 이 브라우저의 전부 정지(65 경계 창) · 서버 15:40 KRX 해제(통지 경계)뿐이고, 다른 단말의 전부 정지는 에코만으로 발주와 구별되지 않는다(65 가 에코 뒤에 오므로 늦다) — 알려진 한계.
- 65 이펙트는 그대로 `strategiesDisabledLogLine()`(Task 1 에서 전부 정지 문구로 교정됨)을 남긴다.

**(3) 테스트.** `webapp/src/lib/__tests__/limit-chaser.test.ts` 에 behavior 의 순수 함수 케이스(Task 2 의 세 함수 포함). `strategy-card-flow.test.tsx` 에 describe `발주 판정 원인 · lc.arm 답 (quick-260926-nr2)` 로 behavior 의 카드 케이스를 단언한다 — 귀속은 `setRelay({ limitChasers: [e1], lastLimitChaserEcho: e1, limitChaserDisableEchoes: new Map([[KEY, { echo: e1, cause: 'killSwitch' }]]) })` 처럼 컨텍스트로 내려 주고, 거부 통지는 기존 `msg()` 헬퍼로 `messages` 에 넣는다. 로그 문장·15:40 문장은 strategy-log 의 export(TRANSITION_TEXT · marketCloseDisabledLogLine)를 import 해 단언한다. 기존 ⑲-8(연타·send false 무재전송)과 ⑧ · ㉑ 계열은 수정 없이 통과해야 한다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp run test src/lib/__tests__/limit-chaser.test.ts src/components/trading/__tests__/strategy-card-flow.test.tsx src/components/trading/__tests__/strategy-card.test.tsx src/components/stock/__tests__/stock-orderbook-section.test.tsx && pnpm --filter @gh-radar/webapp run typecheck</automated>
  </verify>
  <done>귀속된 비활성화 에코는 발주로 읽히지 않고(로그 「매수 무장 해제」·배지 없음), 15:40 해제는 원인 1줄을 남긴다. lc.arm 은 3초 미반영 추적을 받고 그 키 에코 또는 인식된 거부로 거둬지며 거부 원문이 카드에 선다. send false 는 추적·재전송이 없다. 지정 테스트와 webapp typecheck 가 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay → browser (wss 프레임) | 60 에코 · 64 스냅샷 · 65 집계 · 54 통지가 카드 판정의 입력이다. 브라우저는 서버 사실을 재계산하지 않고 원인만 귀속한다 |
| 사용자 클릭 → send (lc.arm · strategies.disable) | 실계좌 자동매매 상태를 바꾸는 요청. 재전송은 곧 두 번째 요청이다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-nr2-01 | Repudiation (표시 무결성) | strategy-card 에코 이펙트 hadOrder | high | mitigate | 이 브라우저의 전부 정지·15:40 해제 에코를 relay 귀속 맵(에코 객체 동일성)으로 발주에서 제외 — 나가지 않은 주문을 「발주 완료」로 말하지 않는다(Pitfall 10). 다른 단말 전부 정지는 구별 불가를 주석으로 명시 |
| T-nr2-02 | Tampering / DoS | handleArm · 3초 타이머 | high | mitigate | 타이머는 표시만 한다. send false 면 추적도 재전송도 없다(T-16-10 · T-17-40). 테스트가 30초 뒤 send 1회를 단언 |
| T-nr2-03 | Information Disclosure | use-relay-socket send 로그 | medium | mitigate | 새 dispatch·타이머 경로는 콘솔 로그를 더하지 않는다. 기존 미연결 로그는 msg.t 만 싣는 규율 유지(T-16-18) |
| T-nr2-04 | Spoofing (상관 오인) | isLimitChaserArmRejection | low | accept | 게이트웨이가 arm 거부에 키를 싣지 않아 in-flight 창 상관이다. 창 안의 무관한 System WARN 이 미반영을 일찍 거두거나 다른 카드 경보에 서는 것이 최악이며, 이는 변경 전(추적 없음)보다 나빠지지 않는다 |
| T-nr2-05 | DoS (상태 고착) | killSwitchInFlight / marketCloseReleaseKeys | medium | mitigate | 65 · 연결 수명(local-status · 비 ready state · reset) · STRATEGIES_DISABLE_ACK_TIMEOUT_MS 백스톱으로 창을 닫고, 15:40 대기 키는 키별 첫 에코로 소비·lc.snap 으로 비운다. 고착 시 실패 방향은 발주 과소표시(보수 방향) |
| T-nr2-06 | Tampering (드리프트) | S→C 전용 필드 목록 | low | mitigate | 목록을 shared const 한 곳에 두고 RelayLimitChaserInput 과 로그 skip 이 모두 파생 — 목록 두 벌 금지 |

패키지 설치 없음 — 공급망(SC) 항목 해당 없음.
</threat_model>

<verification>
1. 전체 빌드 게이트(프로젝트 build_command): `pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/webapp run typecheck`
2. 전체 테스트(프로젝트 test_command): `pnpm --filter @gh-radar/relay run test && pnpm --filter @gh-radar/webapp run test` — 기존 테스트 전부 통과(배너 문구를 바꾼 테스트는 ⑬ · strategy-log ⑫ 두 곳뿐이어야 한다).
3. e2e `webapp/e2e/specs/trading-workbench.spec.ts` test 13 은 실행하지 않는다(dev 서버 + 목 relay 필요). 의미 유효성만 확인한다: 그 테스트의 에코는 사용자 값 `buyWatchQty` 를 바꾸므로 새 기준(limitChaserValuesChanged)에서도 배너 + 로그 1줄이 선다. SUMMARY 에 「e2e 미실행 · 의미 유효」로 적는다.
4. `git status --short` 로 변경이 files_modified 목록(과 shared dist 비추적 산출물)뿐이고 아무것도 스테이징·커밋되지 않았음을 확인한다. `.planning/phases/21-*` · `.planning/state.json` · `tasks/lessons.md` 는 이 작업 전과 같은 상태여야 한다.
</verification>

<success_criteria>
- NR2-1 ~ NR2-7 의 must_haves.truths 가 각각 최소 1개의 자동 테스트로 단언된다.
- 오배너 원인 6가지(lc.arm · 이중 에코 · 런타임 푸시 · 전부 정지/15:40 · lc.snap · valuesChanged)가 모두 닫히고, 다른 단말의 사용자 값 변경 배너는 유지된다.
- 65 로그가 더 이상 「장 마감 규칙」을 말하지 않고, 15:40 해제는 KRX 문구로 원인이 남는다.
- 빌드 게이트와 relay · webapp 전체 테스트가 통과한다.
- 커밋·푸시 없음. 동시 세션 파일 무접촉.
</success_criteria>

<output>
Create `.planning/quick/260926-nr2-lc-card-false-other-device-banner-and-ec/260926-nr2-SUMMARY.md` when done (e2e 미실행 사실, 오케스트레이터 전제 정정(65 ≠ 15:40), 남은 알려진 한계 — 다른 단말 전부 정지·lc.snap 으로 관측된 해제는 여전히 발주로 읽힘, 런타임 게이트 푸시가 내 에코보다 먼저 오는 경합 — 를 포함). 커밋하지 않는다.
</output>
