---
phase: quick-260915-h3p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/news-sync/src/pipeline/cadence.ts
  - workers/news-sync/src/pipeline/targets.ts
  - workers/news-sync/src/config.ts
  - workers/news-sync/src/index.ts
  - workers/news-sync/tests/cadence.test.ts
  - workers/news-sync/tests/config.test.ts
  - workers/news-sync/src/naver/searchNews.ts
  - workers/news-sync/src/apiUsage.ts
  - workers/news-sync/src/pipeline/classify.ts
  - workers/news-sync/tests/searchNews.test.ts
  - workers/news-sync/tests/collectStockNews.test.ts
  - workers/news-sync/tests/classify.test.ts
  - workers/news-sync/tests/apiUsage.test.ts
  - workers/news-sync/tests/schedule.test.ts
  - scripts/deploy-news-sync.sh
  - scripts/smoke-news-sync.sh
  - CLAUDE.md
  - .planning/research/STACK.md
autonomous: true
requirements: [H3P-A, H3P-B, H3P-C, H3P-D]

estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "평일 KST 08:00~20:02 에 시작한 실행은 hot 등급(top_movers rank<=30 ∪ 모든 watchlist) 전부 + rest 등급(rank>30 또는 rank null) 중 버킷 floor(KST 자정 이후 분/3)%3 에 해당하는 종목만 호출한다. 그 외 시각과 토·일은 전체 대상(~104)을 호출한다."
    - "rest 종목 하나는 연속된 3개 슬롯에서 정확히 1번 수집된다(9분 주기). watchlist 종목은 rank 와 무관하게 hot 이며 rest 로 중복 호출되지 않는다."
    - "스케줄러 시작이 몇 초~2분 59초 늦어도 같은 3분 슬롯(같은 버킷)으로 매핑된다."
    - "네이버 일일 한도 소진 판정(429 본문 errorCode 010 또는 'Query limit exceeded' — 또는 5종목 연속 재시도 후 429) 시 run 을 즉시 중단하고 error 로그를 남긴다. 같은 KST 날짜에 두 번째 판정이 나오면 자정까지 이후 run 은 네이버 호출 0 으로 skip 한다."
    - "news-sync 일일 예산 가드 기본값과 배포값이 18,750(공식 25,000 의 75%)이다."
    - "라이브 news-sync 스케줄러는 market · market-close · offhours · weekend 4개뿐이고, 1주를 분 단위로 펼쳤을 때 같은 분에 두 개가 발화하는 경우가 0이다. morning · morning-10h · intraday 는 존재하지 않는다."
    - "CLAUDE.md 와 그 원본 .planning/research/STACK.md 가 스크래핑 5원칙과 공식 API(Naver Search API) 운영 기준을 분리해 적고 있다."
  artifacts:
    - path: "workers/news-sync/src/pipeline/cadence.ts"
      provides: "resolveRunMode · kstMinuteOfDay · restBucketFor · stockBucket · selectRunTargets · planRun (순수 함수)"
      contains: "export function planRun"
    - path: "workers/news-sync/tests/cadence.test.ts"
      provides: "버킷/슬롯/모드/대상 구성 단위 테스트"
    - path: "workers/news-sync/tests/schedule.test.ts"
      provides: "deploy 스크립트 cron 파싱 → 1주 분 단위 전개 → 중복 0 · 발화 수 · 모드 정합 단언"
      contains: "NEWS_SCHEDULERS"
    - path: "scripts/deploy-news-sync.sh"
      provides: "4개 스케줄러 update-or-create + 폐기 3개 삭제 + NEWS_SYNC_DAILY_BUDGET=18750"
      contains: "OBSOLETE_NEWS_SCHEDULERS"
  key_links:
    - from: "workers/news-sync/src/index.ts"
      to: "workers/news-sync/src/pipeline/cadence.ts"
      via: "run 시작 시 now 1회 캡처 → planRun(now, cfg.newsSyncMode, records)"
      pattern: "planRun\\("
    - from: "workers/news-sync/tests/schedule.test.ts"
      to: "scripts/deploy-news-sync.sh"
      via: "NEWS_SCHEDULERS 배열을 읽어 cron 전개 + 각 발화 분의 resolveRunMode 결과 대조"
      pattern: "deploy-news-sync\\.sh"
    - from: "workers/news-sync/src/naver/searchNews.ts"
      to: "workers/news-sync/src/index.ts"
      via: "429 본문 → NaverBudgetExhaustedError → classifyPerStockError → stopAll + recordQuotaStrike"
      pattern: "recordQuotaStrike"
---

<objective>
news-sync 의 네이버 검색 API 수집 주기를 재설계한다(사용자 승인 설계 1~6 을 그대로 구현).

1. 평일 장시간(08:00~20:00 KST)은 3분 단일 잡. 매 실행이 hot 등급(급등 상위 30 ∪ 관심종목) 전부와 rest 등급의 3조 순환 버킷 하나를 수집한다(설계 1).
2. 장외는 평일 밤 · 주말 스케줄러로 분리하고 전체 대상을 수집한다. 장시간 잡과 같은 분에 발화하지 않는다(설계 2).
3. 모드는 워커가 KST 시각으로 스스로 판정하고 env 로 강제할 수 있다(설계 3).
4. 네이버 일일 한도가 실제로 소진되면 그 run 을 즉시 중단하고, 같은 날 두 번째 판정부터는 자정까지 호출하지 않는다. 계속 두드리지 않는다(설계 4).
5. 예산 가드를 18,750 으로 낮춘다(설계 5).
6. CLAUDE.md 운영 원칙을 스크래핑과 공식 API 로 나눈다(설계 6).

Purpose: 회당 호출을 ~112 → ~60 으로 줄이면서 급등·관심 종목 뉴스 신선도를 3분으로 끌어올리고, 오늘 관측된 동분 중복 발화 429 와 "한도 소진 시 계속 두드림" 결함을 구조적으로 없앤다.
Output: cadence 모듈 + 한도 소진 중단 로직 + 재편된 스케줄러(라이브 배포·관측 완료) + 갱신된 CLAUDE.md/STACK.md.

KRX 평일 휴장일은 평일로 취급한다(범위 밖 — SUMMARY 에 명시). server `NAVER_DAILY_BUDGET`(24500) 은 변경하지 않는다(SUMMARY 에 명시).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/quick/260915-boq-morning-theme-news-coverage/260915-boq-SUMMARY.md

@workers/news-sync/src/index.ts
@workers/news-sync/src/config.ts
@workers/news-sync/src/pipeline/targets.ts
@workers/news-sync/src/pipeline/classify.ts
@workers/news-sync/src/naver/searchNews.ts
@workers/news-sync/src/naver/collectStockNews.ts
@workers/news-sync/src/apiUsage.ts
@scripts/deploy-news-sync.sh
@scripts/smoke-news-sync.sh

## 계획 시점 실측 (2026-09-15, 재조사 불필요)

- 라이브 news-sync 스케줄러 4개 (asia-northeast3, Asia/Seoul): morning `*/3 8-9 * * 1-5`, morning-10h `0-30/3,45 10 * * 1-5`, intraday `*/15 11-15 * * 1-5`, offhours `0 */2 * * *` — 전부 ENABLED.
- `top_movers.rank` 은 nullable int (migration 20260415120000, `idx_top_movers_rank ... NULLS LAST`). 최신 scan 은 rank 1..100.
- `api_usage` 는 PK(service, usage_date), service 는 자유 text(CHECK 없음), RLS on + 정책 0 = service_role 전용. `incr_api_usage(p_service, p_date, p_amount)` 는 이미 service_role grant. 별도 service 라벨을 마커로 쓰는 선례가 있다(theme-sync contentHash) → 마이그레이션 불필요.
- 네이버 공식 오류 코드 문서(openapi guide errorcode, 2026-09-15 조회)는 "429 = 오픈 API 호출 시 일 허용량 초과" 만 적고 errorCode 값은 없다. JSON 오류 필드명은 `errorMessage` · `errorCode`. 커뮤니티에서 알려진 형태 [ASSUMED]: 일일 한도 = errorCode `010` / "Query limit exceeded", 초당 제한 = errorCode `012` / "Rate limit exceeded". 오늘 관측된 동분 중복 429 는 당일 회복됐으므로 초당 제한 429 도 실재한다 → 본문으로 구분 + 원문 본문 로깅이 필수.
- CLAUDE.md 의 스택 블록은 `<!-- GSD:stack-start source:research/STACK.md -->` 관리 블록이다. 그러나 현재 "Legal posture" 와 "Operational 5 rules" 는 CLAUDE.md 에만 손으로 들어가 있고(b691b15) STACK.md 의 해당 절(166~170행)은 옛 문구("사용자 요청 시에만 스크랩 · 5~10분 캐시", "상세 페이지 진입 시에만 뉴스 호출")다. 재생성 시 산문 단락은 CLAUDE.md 에서 빠지는 것으로 보인다(STACK.md 의 Key Constraints 산문이 CLAUDE.md 에는 제목만 남아 있음). 생성기 코드는 로컬에서 찾지 못했다.
- estimate-calibration: factor 1, sample_count 0, confidence low.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 장시간 등급 대상 선택 — hot(rank≤30 ∪ 관심종목) + rest 3조 순환, KST 모드 판정 end-to-end (설계 1·3·5)</name>
  <files>workers/news-sync/src/pipeline/cadence.ts, workers/news-sync/src/pipeline/targets.ts, workers/news-sync/src/config.ts, workers/news-sync/src/index.ts, workers/news-sync/tests/cadence.test.ts, workers/news-sync/tests/config.test.ts</files>
  <behavior>
    tests/cadence.test.ts. 시각은 전부 UTC Date 로 만들고 헬퍼 kst(y,m,d,h,mi,s) = new Date(Date.UTC(y,m-1,d,h-9,mi,s)) 를 쓴다. 호스트 TZ 에 의존하면 안 된다.
    - resolveRunMode(now,"auto") 경계 (2026-09-15 화요일): 07:59 → full, 08:00 → tiered, 19:59 → tiered, 20:00 → tiered, 20:02:59 → tiered, 20:03 → full. 토 2026-09-19 10:00 → full. 일 2026-09-20 12:00 → full. 월 2026-09-14 00:00 → full. UTC 입력 2026-09-14T23:00:00Z(= 화 08:00 KST) → tiered.
    - resolveRunMode override: 토요일에 "tiered" 를 주면 tiered, 화 10:00 에 "full" 을 주면 full.
    - restBucketFor 슬롯: 08:00 → 1, 08:03 → 2, 08:06 → 0 (연속 슬롯이 +1 mod 3 으로 회전). 지연 흡수: 08:03:00 · 08:03:40 · 08:05:59 은 모두 같은 버킷이고 08:06:00 에서 바뀐다. 20:00:00 과 20:02:30 → 1. 19:57 → 0.
    - stockBucket: 같은 코드는 항상 같은 값. 구현 후 계산한 실제 값으로 대표 코드 3개("005930", "000660", "035720")를 고정해, 해시가 바뀌면 조용히 재배치되지 않게 잠근다. 연속 6자리 코드 "000100".."000999"(900개)에서 버킷별 개수가 모두 270~330.
    - selectRunTargets 구성: rank 1..100 인 records + watched 4개(rank 5 인 종목 1개, rank 70 인 종목 1개, top_movers 에 없는 종목 2개).
      - hot = rank≤30(30개) ∪ watched → 중복 제거 시 33개.
      - rank 70 인 watched 종목은 hot 에만 있고, 어떤 버킷에서도 rest 로 다시 선택되지 않는다.
      - rank null 인 종목은 rest 로 취급한다.
      - 버킷 0·1·2 의 rest 선택 3세트는 서로 겹치지 않고, 합집합이 rest 전체(69개)와 같다.
      - 모든 선택 결과에서 code 는 유일하다.
      - full 모드는 records 전체를 반환한다.
    - buildTargetRecords: movers 에 같은 code 가 두 번 있으면 작은 rank 를 쓴다. watch 에 있고 movers 에 없으면 rank null + watched true. masters 에 없는 code 는 제외한다(FK 사전 차단 유지).
    - planRun(now, override, records): 화 08:03 이면 { mode: tiered, restBucket: 2, selected: hot ∪ 버킷 2, counts: { total, hot, restTotal, restSelected } }. 토요일이면 { mode: full, restBucket: null, selected: 전체 }.
    - tests/config.test.ts: NEWS_SYNC_MODE 미지정 → "auto". "full" → "full". "bogus" → loadConfig 가 /NEWS_SYNC_MODE/ 로 throw. naverDailyBudget 기본값 테스트의 기대값을 24500 → 18750 으로 바꾼다(설계 5).
  </behavior>
  <action>
    먼저 테스트를 작성하고 실패(RED)를 확인한 뒤 구현한다. 커밋은 GREEN 과 한 번에 한다(직전 quick 관행).

    (a) 새 파일 src/pipeline/cadence.ts — 순수 함수만 둔다. I/O 금지. 시각 계산은 +9h 후 getUTC* 로만 한다(@gh-radar/shared kstDateIso 와 같은 방식, 컨테이너 TZ=UTC 가정).
    - export 상수: MARKET_WINDOW_START_MIN=480, MARKET_WINDOW_END_MIN=1202, HOT_RANK_MAX=30, REST_BUCKET_COUNT=3, SLOT_MINUTES=3.
    - END=1202(20:02 포함)인 이유를 주석으로 남긴다: 20:00 스케줄 실행이 늦게 시작해도 tiered 로 판정되게 하는 여유이고, intraday-sync 가 20:02 까지 top_movers 를 갱신한다.
    - export type RunMode = "tiered" | "full", RunModeSetting = "auto" | RunMode.
    - kstMinuteOfDay(now): KST 자정 이후 정수 분.
    - kstDayOfWeek(now): 0=일 … 6=토.
    - resolveRunMode(now, setting): tiered/full 이 주어지면 그대로 쓴다. auto 는 월~금이고 START ≤ 분 ≤ END 이면 tiered, 아니면 full.
    - restBucketFor(now) = floor(kstMinuteOfDay/SLOT_MINUTES) % REST_BUCKET_COUNT.
      주석에 적는다: 스케줄러는 예정 시각보다 일찍 발화하지 않는다. now 는 run 시작 시 1회 캡처한다. 분 단위 floor 이므로 최대 2분 59초 늦은 시작도 예정 슬롯에 매핑된다. 연속 3분 슬롯은 버킷을 1,2,0 순으로 회전시키므로 rest 종목은 정확히 9분마다 수집된다. 상태를 저장하지 않는다(stateless).
    - stockBucket(code): 코드 문자열에 FNV-1a 32bit(Math.imul, 오프셋 0x811c9dc5, prime 0x01000193)를 적용하고, murmur3 fmix32 finalizer 로 비트를 섞은 뒤(>>>0 유지) % REST_BUCKET_COUNT. 상위 종목 구성이 매 실행 바뀌어도 종목별 버킷이 안정적이도록 인덱스가 아니라 코드 해시를 쓴다.
    - export interface TargetRecord { code; name; rank: number | null; watched: boolean }.
    - selectRunTargets(records, mode, restBucket): full 이면 records 그대로. tiered 면 hot = watched 이거나 (rank !== null 이고 rank ≤ HOT_RANK_MAX). rest 는 hot 이 아니면서 stockBucket === restBucket 인 것. 결과는 hot 을 앞에 둔 순서이고 code 기준으로 한 번만 담는다(방어적 Set). counts(total, hot, restTotal, restSelected)를 함께 반환한다.
    - planRun(now, setting, records): mode, restBucket(tiered 만, full 은 null), selected, counts 를 한 번에 반환한다 — index.ts 가 호출하는 단일 진입점.

    (b) src/pipeline/targets.ts
    - 순수 함수 buildTargetRecords(movers: {code, rank}[], watchCodes: string[], masters: {code, name}[]): TargetRecord[] 를 export 한다(규칙은 behavior 참조).
    - loadTargets 는 I/O 만 남긴다. top_movers 조회를 select("code, rank") 로 바꾸고, masters 조회 후 buildTargetRecords 결과를 반환한다(반환 타입 TargetRecord[]).
    - NewsTarget 을 쓰는 곳은 typecheck 로 전부 찾아 TargetRecord 로 맞춘다.
    - 헤더 주석에 rank 사용 이유(hot 등급)를 추가한다.
    - buildTargetRecords 테스트는 tests/cadence.test.ts 에 함께 둔다.

    (c) src/config.ts
    - NewsSyncConfig 에 newsSyncMode: RunModeSetting 을 추가한다. env NEWS_SYNC_MODE, 기본 "auto". auto/tiered/full 이 아니면 "invalid NEWS_SYNC_MODE: <값>" 으로 throw.
    - naverDailyBudget 기본값을 "24500" → "18750" 으로 바꾼다(설계 5: 공식 25,000 의 75%).

    (d) src/index.ts wiring
    - runNewsSyncCycle 첫 줄 근처에서 const now = new Date() 를 1회 캡처한다. kstDateString(now), firstCutoffIso(now 기준 7일)도 이 now 를 쓴다(Date.now() 직접 읽기 제거).
    - loadTargets 다음에 planRun(now, cfg.newsSyncMode, records) 를 호출한다.
    - 로그 "news-sync targets loaded" 를 { mode, restBucket, ...counts, selected: selected.length } 를 담은 "news-sync targets selected" 로 교체한다.
    - 사전 예산 체크, lastSeenMap 코드 목록, per-stock 루프는 모두 selected 기준으로 돈다.
    - "news-sync cycle complete" 로그에 mode, restBucket, targets(selected 수) 필드를 추가한다. pages(=성공 호출 수)는 기존대로 둔다 — Task 3 라이브 검증이 이 필드로 회당 호출 수와 버킷 회전을 읽는다.
    - 파일 상단 Flow 주석을 갱신한다.

    모드 선택 방식(설계 3 — Claude 재량): 스케줄러가 모드를 넘기는 대신 KST 시각 auto 판정 + NEWS_SYNC_MODE override 를 쓴다.
    - 근거: 스케줄러 4개가 하나의 Cloud Run Job 을 본문 없는 v1 jobs:run URI 로 호출한다. 스케줄러별로 모드를 넘기려면 스케줄러마다 containerOverrides JSON 본문을 관리해야 하고, 본문이 잘못 들어간 스케줄러는 조용히 틀린 모드로 돈다.
    - auto 판정은 Job 을 상태 없이 두면서 Task 3 의 schedule.test 가 cron 과 모드를 교차 검증할 수 있게 한다.
    - 수동 실행은 gcloud run jobs execute 의 --update-env-vars NEWS_SYNC_MODE=full|tiered 로 강제한다.
  </action>
  <verify>
    <automated>pnpm --dir workers/news-sync run typecheck && pnpm --dir workers/news-sync run test && pnpm --dir workers/news-sync run build</automated>
  </verify>
  <done>
    - cadence.test.ts 의 모든 경계·회전·지연·구성 케이스가 통과하고, 기존 테스트도 전부 통과한다. typecheck 와 build 가 exit 0.
    - index.ts 가 planRun 한 번으로 모드·버킷·대상을 정하고, 두 로그(targets selected, cycle complete)에 mode/restBucket/targets 가 찍힌다.
    - 커밋 1개: "feat(news-sync): 장시간 급등 상위 30·관심종목 매회 + 나머지 3조 순환 수집 — KST 모드 자동 판정·예산 18,750". Co-Authored-By 없음.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 네이버 일일 한도 소진 시 run 즉시 중단 + 같은 날 2회째 판정부터 자정까지 skip (설계 4)</name>
  <files>workers/news-sync/src/naver/searchNews.ts, workers/news-sync/src/apiUsage.ts, workers/news-sync/src/pipeline/classify.ts, workers/news-sync/src/index.ts, workers/news-sync/tests/searchNews.test.ts, workers/news-sync/tests/collectStockNews.test.ts, workers/news-sync/tests/classify.test.ts, workers/news-sync/tests/apiUsage.test.ts</files>
  <behavior>
    - searchNews (429 매핑):
      - 본문 { errorCode: "010", errorMessage: "Query limit exceeded" } → NaverBudgetExhaustedError, 인스턴스에 errorCode "010" 이 보존된다.
      - 본문 { errorMessage: "query LIMIT exceeded" }(코드 없음) → NaverBudgetExhaustedError.
      - JSON 문자열 본문 '{"errorCode":"010","errorMessage":"Query limit exceeded"}' → NaverBudgetExhaustedError.
      - 본문 { errorCode: "012", errorMessage: "Rate limit exceeded" } → NaverRateLimitError(errorCode "012" 보존).
      - 본문 없음 → NaverRateLimitError. 기존 "BudgetExhausted 로는 throw 되지 않음" 회귀는 '본문 없음/미확인 코드' 조건으로 이름만 명확히 하고 유지한다.
    - collectStockNews: 첫 페이지 429 + 본문 010 → NaverBudgetExhaustedError 로 reject 되고, client.get 호출은 정확히 1회(재시도 없음 = 두드리지 않음).
    - classify:
      - Consecutive429Tracker(threshold 5): recordRateLimitFailure 4번까지 false, 5번째 true. 중간에 recordSuccess 가 끼면 0 으로 리셋.
      - CONSECUTIVE_429_STOCKS_FOR_EXHAUSTION === 5.
      - 기존 classifyPerStockError 분류 테스트는 그대로 통과한다.
    - apiUsage:
      - recordQuotaStrike(supabase, "2026-09-15") → rpc("incr_api_usage", { p_service: "naver_search_news_quota_strike", p_date: "2026-09-15", p_amount: 1 }).
      - readQuotaStrikes 는 service eq "naver_search_news_quota_strike" 로 조회한다.
      - 기존 checkBudget/incrementUsage 호출은 service 기본값 "naver_search_news" 로 동작이 그대로다.
      - QUOTA_STRIKES_TO_STOP_DAY === 2.
  </behavior>
  <action>
    먼저 테스트를 작성하고 RED 를 확인한다. 기존 테스트 파일의 mock 패턴(axiosErr, mkSeq, supabase rpc mock)을 읽고 따른다.

    (a) src/naver/searchNews.ts
    - NaverRateLimitError 와 NaverBudgetExhaustedError 가 선택적 detail { errorCode?, errorMessage? } 를 받아 readonly 필드로 보존하게 한다.
      - 인자 없는 기존 생성자 호출도 호환된다.
      - message 에 코드가 있으면 덧붙인다(예: Naver daily query quota exhausted (HTTP 429 errorCode=010 ...)).
    - export 순수 함수 두 개를 추가한다.
      - extractNaverError(data): 객체면 그대로 필드를 읽고, 문자열이면 JSON.parse 를 시도하며 실패하면 errorMessage=원문(최대 200자). 항상 {errorCode?, errorMessage?} 를 반환한다.
      - isDailyQuotaExhausted(detail): errorCode === "010" 또는 errorMessage 에 대소문자 무시 "query limit exceeded" 가 들어 있으면 true.
    - 429 분기: detail 을 추출해 isDailyQuotaExhausted 면 NaverBudgetExhaustedError(detail), 아니면 NaverRateLimitError(detail).
    - 주석에 근거를 남긴다: 공식 errorcode 문서는 429 를 "일 허용량 초과" 로만 적고 코드값이 없다. 010/"Query limit exceeded" 와 012/"Rate limit exceeded" 는 커뮤니티 관측 형태[ASSUMED]다. 초당 제한 429 도 실재한다(2026-09-15 동분 중복 발화). 미확인 본문은 초당 제한으로 취급하고 index 휴리스틱이 보완한다.
    - collectStockNews.ts 는 수정하지 않는다. 재시도는 NaverRateLimitError 에만 적용되므로 소진 에러는 이미 즉시 전파된다 — 테스트로 잠근다.

    (b) src/apiUsage.ts
    - checkBudget 와 incrementUsage 에 마지막 선택 인자 service(기본 SERVICE)를 추가한다.
    - export 상수: QUOTA_STRIKE_SERVICE = "naver_search_news_quota_strike", QUOTA_STRIKES_TO_STOP_DAY = 2.
    - readQuotaStrikes(supabase, dateKst) = checkBudget(…, QUOTA_STRIKE_SERVICE). recordQuotaStrike(supabase, dateKst) = incrementUsage(…, 1, QUOTA_STRIKE_SERVICE).
    - 주석에 적는다: api_usage 의 KST 날짜 키이므로 자정에 자연 리셋된다. 마이그레이션이 필요 없다(theme-sync 마커 선례). 호출 카운터 naver_search_news 값은 오염시키지 않는다.

    (c) src/pipeline/classify.ts
    - export CONSECUTIVE_429_STOCKS_FOR_EXHAUSTION = 5 와 class Consecutive429Tracker(threshold) { recordRateLimitFailure(): boolean; recordSuccess(): void } 를 추가한다.
    - 주석: 5 > 동시성 3 이므로 동시에 떠 있던 종목들이 한 번의 초당 버스트로 함께 실패해도 혼자서는 발동하지 않는다.
    - classifyPerStockError 는 그대로 둔다.

    (d) src/index.ts
    - 사전 차단: loadTargets 전에 readQuotaStrikes(오늘 KST) ≥ QUOTA_STRIKES_TO_STOP_DAY 이면 log.warn({ strikes, dateKst }, "Naver daily quota exhausted earlier today — skipping run until KST midnight") 후 return 한다(네이버 호출 0).
    - run 내부 상태: tracker = new Consecutive429Tracker(CONSECUTIVE_429_STOCKS_FOR_EXHAUSTION), quotaAbort: null | "explicit" | "suspected".
    - per-stock 성공(upsert 후)마다 tracker.recordSuccess().
    - catch 분기:
      - kind budget-exhausted → stopAll. 아직 quotaAbort 가 없을 때만 quotaAbort="explicit" 으로 두고 recordQuotaStrike 를 1회 호출한 뒤, log.error({ code, errorCode, errorMessage, strikesToday }, "Naver daily quota exhausted — aborting run (no retries); second strike today stops news-sync until KST midnight").
      - kind rate-limit → log.warn 에 errorCode/errorMessage 를 포함한다(원문 본문을 남겨 첫 실제 발생에서 [ASSUMED] 형태를 확인하기 위함). tracker.recordRateLimitFailure() 가 true 면 stopAll + quotaAbort="suspected" + recordQuotaStrike 1회 + log.error({ consecutive: 5, strikesToday }, "suspected Naver daily quota exhaustion: 5 consecutive stocks 429 after retries — aborting run").
      - auth 는 기존 stopAll 을 유지한다.
      - other 는 tracker 를 건드리지 않는다.
    - recordQuotaStrike 가 실패하면 log.error 로 남기고 중단은 그대로 진행한다(throw 하지 않음).
    - 한 run 에서 strike 는 최대 1회만 기록한다. 동시에 떠 있던 종목들이 같은 에러를 여러 번 던지므로 이 가드가 필요하다.
    - 자체 예산 가드 무로그 결함도 고친다: onPage 에서 used > cfg.naverDailyBudget 이 되는 최초 1회에 log.error({ used, budget }, "news-sync daily budget guard reached — aborting run") 을 남긴다. 현재는 조용히 stopAll 만 켠다.
    - cycle complete 로그에 quotaAbort 필드를 추가한다.
    - 설계 판단(Claude 재량) 주석: 한 run 의 증거는 그 run 만 중단하고, 같은 날 두 번째 증거에서 당일 중단한다. 오판(초당 버스트를 소진으로 오인)이 하루 뉴스 전체를 끄는 비용이 매우 크다. 반면 실제 소진 시 추가 비용은 다음 1회 run 의 거절된 호출 몇 건뿐이라 "두드리지 않음" 을 만족한다.
  </action>
  <verify>
    <automated>pnpm --dir workers/news-sync run typecheck && pnpm --dir workers/news-sync run test</automated>
  </verify>
  <done>
    - behavior 의 모든 케이스와 기존 테스트가 통과하고 typecheck 가 exit 0.
    - src/naver/searchNews.ts 에서 NaverBudgetExhaustedError 가 실제로 throw 된다(grep -v '^\s*\*' src/naver/searchNews.ts | grep -c 'new NaverBudgetExhaustedError' ≥ 1).
    - index.ts 에 readQuotaStrikes 사전 차단과 recordQuotaStrike 기록이 모두 있다.
    - 커밋 1개: "fix(news-sync): 네이버 일일 한도 소진 시 즉시 중단·같은 날 두 번째 판정부터 자정까지 수집 중단 — 429 본문 구분·연속 429 휴리스틱". Co-Authored-By 없음.
  </done>
</task>

<task type="auto">
  <name>Task 3: 스케줄러 재편(장시간 3분 단일 + 장외 분리, 중복 0 검증) · CLAUDE.md/STACK.md 운영 기준 분리 · 배포와 라이브 관측 (설계 1·2·5·6)</name>
  <files>scripts/deploy-news-sync.sh, scripts/smoke-news-sync.sh, workers/news-sync/tests/schedule.test.ts, CLAUDE.md, .planning/research/STACK.md</files>
  <precondition>$HOME/.config/gcloud/gh-radar-deployer.json 이 존재하고, 활성 gcloud configuration 이 gh-radar 이며(deploy 스크립트 Section 1 가드), docker 데몬이 응답한다(docker info exit 0).</precondition>
  <action>
    (a) scripts/deploy-news-sync.sh
    - 헤더 리소스 목록과 Section 8 주석을 새 구성으로 바꾼다.
      - 설계 요약: 평일 장시간 3분 단일 잡(hot 매회 + rest 3조 순환), 장외 전체 대상.
      - 예산 추정: 장시간 241회 × ~60 ≈ 14.5K. 평일 밤 5회·아침 backfill 을 더해 ≈15.5K/일(공식 25K 의 ~62%). 가드 18,750.
      - 동분 중복 0 은 workers/news-sync/tests/schedule.test.ts 가 이 배열을 직접 읽어 단언한다.
      - 옛 boq 주석(아침장 3분 분리 · 알고 남긴 offhours 중복)은 이번 재편으로 해소됐다고 한 줄로 대체한다.
    - NEWS_SCHEDULERS 배열을 정확히 4항목으로 바꾼다. 적용 순서는 아래 배열 순서이고, 새 장시간 잡을 먼저 만들고 offhours 를 좁혀 적용 중 공백과 겹침을 최소화한다.
      - "gh-radar-news-sync-market|*/3 8-19 * * 1-5"
      - "gh-radar-news-sync-offhours|0 0-6/2,22 * * 1-5"
      - "gh-radar-news-sync-weekend|0 */2 * * 0,6"
      - "gh-radar-news-sync-market-close|0 20 * * 1-5"
    - 기존 update-or-create 루프는 그대로 쓴다.
    - 루프 뒤에 declare -a OBSOLETE_NEWS_SCHEDULERS=(gh-radar-news-sync-morning gh-radar-news-sync-morning-10h gh-radar-news-sync-intraday) 와 idempotent 삭제 루프를 추가한다.
      - describe 가 성공하면 gcloud scheduler jobs delete "$S" --location --project --quiet 후 "▶ scheduler delete: $S".
      - 실패하면 "✓ already absent: $S".
    - Section 6 --set-env-vars 의 NEWS_SYNC_DAILY_BUDGET 값을 18750 으로 바꾼다. 같은 줄의 NAVER_DAILY_BUDGET=24500 은 건드리지 않고, "worker 는 NEWS_SYNC_DAILY_BUDGET 만 읽는다" 는 주석 한 줄만 추가한다. NEWS_SYNC_MODE 는 설정하지 않는다(기본 auto).
    - Section 9 echo 의 스케줄러 목록을 갱신한다.
    - gcloud 가 "0-6/2,22" 식을 거부하면(boq 는 "0-30/3,45" 수용을 확인했다) 동치인 "0 0,2,4,6,22 * * 1-5" 로 바꾸고, smoke 기대값과 SUMMARY 에 같은 식을 기록한다.

    (b) scripts/smoke-news-sync.sh
    - INV-3 계열을 새 구성으로 교체한다.
      - INV-3a market 스케줄 일치.
      - INV-3b market-close 일치.
      - INV-3c offhours(새 식) 일치.
      - INV-3d weekend 일치.
      - INV-3e 4개 모두 ENABLED.
      - INV-3f 폐기 3개가 각각 describe 실패(존재하지 않음).
    - 기존 check 헬퍼 패턴을 따른다.

    (c) 새 파일 workers/news-sync/tests/schedule.test.ts
    - path.resolve(__dirname, "../../../scripts/deploy-news-sync.sh") 를 읽는다.
    - "declare -a NEWS_SCHEDULERS=(" 부터 닫는 ")" 까지 블록에서 "이름|cron" 항목을, OBSOLETE_NEWS_SCHEDULERS 블록에서 이름 목록을 파싱한다.
    - 테스트 파일 안에 최소 unix-cron 전개기를 둔다.
      - 필드: 분 0-59, 시 0-23, 일 1-31, 월 1-12, 요일 0-6(7→0).
      - 토큰: *, */n, a, a-b, a-b/n, a/n, 콤마 목록. 그 밖의 토큰은 throw(조용히 잘못 전개하지 않음).
      - 일과 요일이 둘 다 제한된 식도 throw — 이번 식들은 해당하지 않는다.
      - Cloud Scheduler 는 unix-cron 이고 요일 0=일요일이며, 시각은 Asia/Seoul 벽시계 기준이다.
    - 2026-09-14(월)~2026-09-20(일) KST 1주 10,080분을 전개해 단언한다.
      - ① 활성 이름 집합 = {market, offhours, weekend, market-close}. 폐기 3개와 교집합 0.
      - ② 어떤 분에도 발화 스케줄러 2개 이상 = 0건. 실패 시 겹친 분과 이름을 메시지에 출력한다.
      - ③ 주간 발화 수: market 1200, market-close 5, offhours 25, weekend 24.
      - ④ 발화 분마다 kst 헬퍼로 만든 Date 에 resolveRunMode(…, "auto") 를 적용: market/market-close → "tiered", offhours/weekend → "full" (cron 과 워커 모드 판정의 교차 검증).
      - ⑤ 평일마다 480~1200분 중 3의 배수인 분이 전부 발화하고, 그 날의 tiered 발화 순서대로 restBucketFor 가 매번 (직전+1)%3 이다.
      - ⑥ 주 안에서 연속 발화 간격의 최댓값 ≤ 120분.

    (d) CLAUDE.md — GSD stack 관리 블록 안에서만 수정한다. Conventions 절(GSD:conventions 마커 사이)은 한 글자도 바꾸지 않는다.
    - "Operational 5 rules" 1~5 항목과 Legal posture 문단은 그대로 둔다.
    - 5원칙 바로 아래 인용 블록(5원칙을 Naver Search API 와 기타 한국 데이터 소스까지 넓히던 문장)을 새 인용 블록으로 교체한다. 새 블록의 내용: 이 5원칙은 스크래핑 source(Naver 종목토론방 및 향후 스크래핑 source)에 적용한다. 공식 쿼터가 공개된 API(Naver Search API 등)는 아래 "공식 API 운영 기준" 을 따른다. 새 source 는 먼저 둘 중 어느 쪽인지 분류하고 해당 기준을 만족하는 운영 설계를 점검할 것.
    - 비어 있는 "### Naver Search API Rate Limit" 제목 아래를 채운다. 모두 목록 형식으로 쓴다.
      - 공식 한도: 앱(Client ID)당 25,000 호출/일. 본 프로젝트 카운터는 api_usage service naver_search_news, KST 날짜 키. display 최대 100, start 최대 1000.
      - 소비처: workers/news-sync(주 소비). server POST /api/stocks/:code/news/refresh 가 같은 Client ID·같은 카운터를 공유한다(스로틀된 수동 새로고침).
      - 예산: news-sync NEWS_SYNC_DAILY_BUDGET 18,750(75%). server NAVER_DAILY_BUDGET 24,500 은 별도 가드.
      - 수집 주기: 평일 08:00~20:00 KST 3분 단일 잡 — 급등 상위 30 + 관심종목 매회, 나머지 top_movers 3조 순환(종목당 9분). 평일 00·02·04·06·22시, 주말 2시간마다 전체 대상. 예상 ≈15.5K/일(~62%).
      - **공식 API 운영 기준** 번호 목록 5개:
        1. 설계 예산은 공식 일일 한도의 75% 이하.
        2. 한도 안에서 서버측 주기 배치 폴링 허용(장중 포함).
        3. 호출량은 사용자 수와 독립 — 사용자 트리거 O(N) 호출 금지(예외: 기존 스로틀된 수동 새로고침).
        4. 일일 한도 소진 신호 시 당일 중단, 재시도로 두드리지 않음. news-sync 는 판정 run 을 즉시 중단하고, 같은 KST 날짜 두 번째 판정부터 자정까지 skip.
        5. 출처 표기와 원문 링크를 항상 노출.
    - Sources 목록에 공식 오류 코드 문서 링크 한 줄을 추가한다: https://developers.naver.com/docs/common/openapiguide/errorcode.md

    (e) .planning/research/STACK.md — CLAUDE.md 가 재생성돼도 (d) 가 사라지지 않도록 원본에도 반영한다.
    - "## Key Constraints and Warnings" 아래 "### Naver 종목토론방 Scraping Risk" 와 "### Naver Search API Rate Limit" 의 옛 산문(on-demand 스크랩 · 5~10분 캐시 · 상세 진입 시에만 뉴스 호출)을 CLAUDE.md 블록과 같은 내용으로 교체한다: Legal posture, 5원칙, 분리 인용 블록, Rate Limit 목록과 공식 API 운영 기준.
    - 생성기 코드를 찾지 못했고, 산문 단락은 CLAUDE.md 로 옮겨질 때 빠지는 것으로 보인다. 그래서 핵심 문장은 굵은 글씨로 시작하는 목록 항목이나 번호 목록으로 쓴다.
    - generate-claude-md 는 실행하지 않는다. 손으로 쓴 Conventions 등 다른 블록을 덮어쓸 수 있다.

    (f) 커밋(테스트 통과 후, 배포 전)
    - (a)(b)(c): "chore(news-sync): 스케줄러를 장시간 3분 단일 잡·평일 밤·주말로 재편 — 동분 중복 발화 제거·예산 18,750"
    - (d)(e): "docs: 네이버 검색 API 는 공식 API 운영 기준으로 분리 — 스크래핑 5원칙과 구분·한도 수치 명시"
    - 둘 다 Co-Authored-By 없음, push 금지.

    (g) 배포 — 커밋 후 HEAD 기준. 하나의 Bash 호출에서 순서대로 실행하고, 오래 걸리므로 background 실행을 권장한다.
    - GOOGLE_APPLICATION_CREDENTIALS=$HOME/.config/gcloud/gh-radar-deployer.json, CLOUDSDK_CORE_PROJECT=gh-radar, GCP_PROJECT_ID=gh-radar 를 export 한다.
    - SUPABASE_URL 은 gcloud run jobs describe gh-radar-news-sync --region asia-northeast3 --format=json 의 컨테이너 env 에서 jq 로 뽑아 셸 변수로만 둔다.
    - smoke INV-5/6 용 SUPABASE_SERVICE_ROLE_KEY 는 gcloud secrets versions access latest --secret=gh-radar-supabase-service-role 결과를 셸 변수로만 export 한다.
    - 비밀 값은 echo 하지 않고 .env 파일도 읽지 않는다.
    - bash scripts/deploy-news-sync.sh 를 실행한다. 스크립트 끝의 smoke(INV-1 이 Job 1회 추가 실행)까지 PASS 여야 한다.
    - 실행 전 KST 시각이 정각·15·30·45분 직전이면 1분 기다려, 새 market 잡 생성과 옛 intraday 삭제 사이의 수 초 겹침이 발화 분에 걸리지 않게 한다. 한 번 겹쳐도 upsert ignoreDuplicates + atomic 카운터로 무결성은 안전하다.

    (h) 라이브 관측 — SUMMARY 에 표로 기록한다.
    - ① gcloud scheduler jobs list --location=asia-northeast3 --format='table(name.basename(),schedule,state)' 에서 news-sync 행이 정확히 4개(의도한 식, ENABLED)이고, 폐기 3개는 describe 가 NOT_FOUND.
    - ② 배포 이후 예약 시각(3의 배수 분)에 생성된 연속 실행을 최소 3건 확보한다. gcloud run jobs executions list --job=gh-radar-news-sync --region=asia-northeast3 --limit=8 을 폴링하고, foreground sleep 대신 background/until 루프를 쓴다. 전부 succeeded 여야 한다.
    - ③ gcloud logging read 로 jsonPayload.msg="news-sync cycle complete" 최근 로그의 version, mode, restBucket, targets, pages, budgetBefore, budgetAfter, errors, quotaAbort 를 표로 뽑는다.
      - version = HEAD 짧은 SHA, mode=tiered.
      - restBucket 이 연속 실행에서 +1 mod 3 으로 회전한다.
      - pages 55~65 가 정상 범위다. 배포 직후 첫 1~2회는 rest 종목 backlog 로 더 클 수 있으니 그대로 기록하고 이후 수렴을 확인한다.
      - errors 0, quotaAbort null.
    - ④ 배포 이후 news-sync severity>=ERROR 로그 0건.
    - ⑤ api_usage 정상성: 최신 budgetAfter < 18,750 이고, (budgetAfter + 남은 장시간 회차 × 관측 평균 pages)가 18,750 미만인지 계산해 기록한다.
    - 배포 완료 시각이 19:50 KST 이후라 예약 tiered 실행을 3건 못 보면: 20:00 실행(restBucket 1)을 관측한다. 그 뒤 3분 이상 간격으로 gcloud run jobs execute gh-radar-news-sync --region asia-northeast3 --update-env-vars NEWS_SYNC_MODE=tiered --wait 를 2회 실행해 버킷 회전과 pages 를 확인하고, "예약 tiered 연속 3건 미관측(다음 평일 08시에 확인)" 을 정직하게 기록한다. 22:00 offhours 전체 실행(mode=full)은 관측되면 기록, 아니면 미관측으로 남긴다.
  </action>
  <verify>
    <automated>bash -n scripts/deploy-news-sync.sh && bash -n scripts/smoke-news-sync.sh && pnpm --dir workers/news-sync run typecheck && pnpm --dir workers/news-sync run test && grep -q '공식 API 운영 기준' CLAUDE.md && grep -q '공식 API 운영 기준' .planning/research/STACK.md && git show 3ce5f81275abf76ebff1770835dd360b96f689ce:CLAUDE.md > "${TMPDIR:-/tmp}/h3p-claude-base.md" && sed -n '/GSD:conventions-start/,/GSD:conventions-end/p' "${TMPDIR:-/tmp}/h3p-claude-base.md" > "${TMPDIR:-/tmp}/h3p-conv-base.txt" && sed -n '/GSD:conventions-start/,/GSD:conventions-end/p' CLAUDE.md > "${TMPDIR:-/tmp}/h3p-conv-now.txt" && test -s "${TMPDIR:-/tmp}/h3p-conv-now.txt" && diff "${TMPDIR:-/tmp}/h3p-conv-base.txt" "${TMPDIR:-/tmp}/h3p-conv-now.txt"</automated>
    <automated>GOOGLE_APPLICATION_CREDENTIALS=$HOME/.config/gcloud/gh-radar-deployer.json CLOUDSDK_CORE_PROJECT=gh-radar gcloud scheduler jobs list --location=asia-northeast3 --format='value(name.basename(),schedule,state)' | grep news-sync</automated>
  </verify>
  <done>
    - schedule.test.ts ①~⑥ 과 전체 news-sync 테스트가 통과한다(동분 중복 0 · 주간 발화 1200/5/25/24 · 모드 정합).
    - CLAUDE.md 와 STACK.md 둘 다 "공식 API 운영 기준" 과 18,750 · 25,000 수치를 담고 있고, 5원칙을 Naver Search API 로 넓히던 문장은 두 파일 모두에서 사라졌다(grep -c 'Naver Search API · 향후' CLAUDE.md = 0). CLAUDE.md Conventions 절 diff 는 0줄이다.
    - deploy-news-sync.sh 가 smoke 까지 "✅ deploy-news-sync.sh complete" 로 끝났다.
    - 라이브 스케줄러는 정확히 4개이고 폐기 3개는 없다.
    - 배포 SHA 로 연속 3건(또는 기록된 대체 관측)이 succeeded, mode=tiered, 버킷 회전, pages ~55-65, ERROR 0, 예산 여유 계산이 SUMMARY 에 있다.
    - 커밋 2개, Co-Authored-By 없음, push 없음.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| news-sync → Naver Search API | 공식 쿼터(25K/일)가 걸린 외부 API. 429 응답 본문은 신뢰할 수 없는 외부 입력 |
| news-sync → Supabase (service_role) | api_usage 카운터·strike 마커 쓰기, top_movers/watchlists/stocks 읽기 |
| 운영자 셸 → GCP (deployer SA) | 배포 시 SUPABASE_URL · service role 키를 셸 변수로 다룸 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-h3p-01 | D (DoS, 자기유발) | index.ts per-stock 루프 · 스케줄러 | high | mitigate | 한도 소진 판정 시 stopAll(Task 2), 같은 날 2 strike 에 자정까지 사전 skip, 예산 가드 18,750(75%), schedule.test 로 동분 중복 발화 0 단언(Task 3) |
| T-h3p-02 | D (오판으로 당일 뉴스 중단) | classify Consecutive429Tracker · strike latch | medium | mitigate | 휴리스틱 임계 5 > 동시성 3. 한 run 의 증거는 그 run 만 중단하고 두 번째 증거에서만 당일 중단. 모든 429 에 errorCode/errorMessage 원문을 로깅해 [ASSUMED] 코드 형태를 첫 발생에서 확인 |
| T-h3p-03 | T (Tampering) | api_usage strike 행 | low | accept | RLS on + 정책 0 = service_role 전용(migration 20260417120000). incr_api_usage 는 service_role grant 만 있음. 새 grant·RPC 없음 |
| T-h3p-04 | I (Info disclosure) | deploy/smoke 셸 · 워커 로그 | medium | mitigate | 비밀은 셸 변수로만 export, echo 금지, .env 미열람. 워커 로그에는 네이버 errorCode/errorMessage 만 담고 요청 헤더는 담지 않음(기존 logger redact 유지) |
| T-h3p-05 | T (설정 drift) | deploy-news-sync.sh ↔ smoke ↔ 워커 모드 판정 | medium | mitigate | schedule.test 가 deploy 배열을 직접 파싱해 cron 발화 분마다 resolveRunMode 를 대조. smoke INV-3a~f 가 라이브 스케줄러 식과 폐기 잡 부재를 확인 |

패키지 설치 없음 — T-h3p-SC 해당 없음.
</threat_model>

<verification>
- pnpm --dir workers/news-sync run typecheck && pnpm --dir workers/news-sync run test (cadence · schedule · searchNews · collectStockNews · classify · apiUsage · config 포함 전체) exit 0.
- pnpm --dir workers/news-sync run build exit 0.
- bash -n 두 스크립트 통과.
- 라이브: 스케줄러 4개만 존재 · 폐기 3개 NOT_FOUND · 배포 SHA 로 연속 tiered 실행 succeeded · restBucket 회전 · pages ~55-65 · ERROR 0 · budgetAfter 여유.
- git log: 커밋 4개(feat · fix · chore · docs), 한글 메시지, Co-Authored-By 0건, push 없음.
</verification>

<success_criteria>
- 평일 장시간 회당 네이버 호출이 ~112 → ~60 으로 줄고, hot 종목은 3분, rest 종목은 9분 신선도를 갖는다.
- 어떤 분에도 news-sync 가 두 번 발화하지 않는다(테스트 단언 + 라이브 스케줄러 목록).
- 일일 한도 실제 소진 시 수집이 스스로 멈추고, 에러 로그가 남는다.
- 일일 예산 가드가 공식 한도의 75% 인 18,750 이다.
- CLAUDE.md 와 원본 STACK.md 가 스크래핑 5원칙과 공식 API 운영 기준을 분리해 적는다.
- SUMMARY 에 명시할 범위 밖·미변경 항목: KRX 평일 휴장일은 평일 취급, server NAVER_DAILY_BUDGET 은 미변경, 429 본문 코드 형태는 [ASSUMED] 이며 첫 실제 발생 로그로 확인할 것.
</success_criteria>

<output>
Create `.planning/quick/260915-h3p-news-sync-tiered-cadence/260915-h3p-SUMMARY.md` when done
</output>
