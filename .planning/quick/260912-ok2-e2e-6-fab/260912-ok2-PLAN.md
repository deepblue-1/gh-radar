---
phase: quick-260912-ok2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/src/components/trading/limit-chaser-client.tsx
  - webapp/src/components/trading/__tests__/limit-chaser-client.test.tsx
  - webapp/e2e/specs/trading-limit-chaser.spec.ts
  - webapp/src/components/trading/limit-chaser-form.tsx
  - webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx
  - webapp/src/components/trading/dirty-action-bar.tsx
  - webapp/e2e/specs/a11y.spec.ts
  - webapp/e2e/specs/chat.spec.ts
  - webapp/e2e/specs/orderbook.spec.ts
  - webapp/e2e/specs/sidebar-tree.spec.ts
  - webapp/e2e/specs/trading-vi.spec.ts
  - .planning/WINDOWS.md
autonomous: true
requirements: [TRADE-01, CHAT-01]
quick_id: 260912-ok2

estimate:
  tokens: 210000
  raw_tokens: 210000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "① 이번에 건드린 e2e 6개 스펙이 **실제 브라우저 실행**에서 전부 초록이고, 전체 스위트에 새로 깨진 것이 0이다"
    - "① 단언 **삭제 0건**(chat 비로그인 시나리오 한 건의 조건부 예외 제외) — 낡은 단언은 현재 계약으로 다시 쓰였고, 260911-w5h 가 화면에서 뺀 주문번호·출처 태그를 e2e 를 통과시키려고 되돌리지 않았다"
    - "② 검색이 열리면 입력에 포커스가 있어 바로 타이핑되고, Esc 로 종목명 표시로 복귀하며, 결과 항목 클릭이 blur 취소에 잡아먹히지 않는다"
    - "③ `DirtyActionBar` 에 FAB 회피용 우측 여백이 없고(렌더된 className 으로 증명), 왜 없어졌는지가 파일에 남아 다음 사람이 되돌리지 않는다"
    - "④ 거래소 콤보 높이가 **두 상태 모두**에서 오른쪽 컨트롤(종목명 트리거 / 검색 입력)과 같고, 상태 전환에서 위아래로 튀지 않으며 `appearance-none` 이 없다"
    - "⑤ `CheckRow` 라벨색이 `Row` 와 같은 `--muted-fg` 이고, 체크박스와 라벨의 어긋남은 **원인이 확인된 뒤** 고쳐졌으며(매직 오프셋 금지), 더티 표현(`● ` + `--primary` + `font-semibold`)은 그대로다"
    - "⑥ 실측 3건(뷰포트 716 세그먼트 잘림 0 · 검색 중 종목정보 10칸 유지 · Esc 복귀)이 e2e 단언으로 박제됐고, 잘림 판정은 기존 잎 요소 `getBoundingClientRect` 방식을 그대로 쓴다"
    - "⑦ WINDOWS 의 실측으로 닫힌 4건(세그먼트 폭 · 카드 패딩 잘림 여유 · 검색 입력 잔여 점프 · 호가 초기 스크롤)이 닫혔고, 「Playwright 미실행」 3건은 **실제 실행 결과**로 갱신됐다"
    - "회귀 0 — webapp 유닛이 63 files / 801 passed / 1 skipped 아래로 내려가지 않고, lint warning 이 정확히 3 · error 0 이다"
  artifacts:
    - webapp/src/components/trading/limit-chaser-client.tsx
    - webapp/src/components/trading/limit-chaser-form.tsx
    - webapp/src/components/trading/dirty-action-bar.tsx
    - webapp/e2e/specs/trading-limit-chaser.spec.ts
    - webapp/e2e/specs/a11y.spec.ts
    - webapp/e2e/specs/chat.spec.ts
    - .planning/WINDOWS.md
    - .planning/quick/260912-ok2-e2e-6-fab/260912-ok2-SUMMARY.md
  key_links:
    - "`StockSearchField` 입력 포커스 ↔ 래퍼 `onKeyDown`(Esc) · `onBlur`(취소) — 포커스가 없으면 그 두 핸들러는 **한 번도 실행된 적 없는 죽은 코드**다. 포커스를 주는 순간 비로소 살아나고, 그때 `relatedTarget` + `onMouseDown` 2중 장치가 처음 시험대에 오른다"
    - "`globals.css` 의 `[hidden]{display:none!important}` ↔ pane 의 `hidden @min-[700px]/lc:block` 클래스 ↔ a11y 단언 — 단언을 맞추려고 `hidden` **속성**을 되돌리면 컴팩트 이상에서 두 pane 을 나란히 세울 수 없다"
    - "거래소 콤보 높이 ↔ 종목명 트리거 높이 ↔ 검색 입력 `h-9` — 셋 중 둘만 맞추면 나머지 한 상태에서 콤보가 튄다"
    - "사다리 트리 3벌(1단·2단·3단) DOM 공존 ↔ `toHaveCount` 단언 — `toHaveCount` 는 가시성을 보지 않는다. 한 곳만 고치면 같은 함정이 옆 단언에 그대로 남는다"
    - "chat e2e 비로그인 시나리오 ↔ `chat-fab.test.tsx` Test 1(D-01) — D-01 커버리지는 **둘 중 최소 한 곳**에 반드시 살아 있어야 한다"
---

<objective>
브라우저 실측으로 드러난 것을 닫는다. 이 저장소의 e2e 는 **그냥 돌아간다** — 그동안
「Playwright 미실행」으로 넘겨 온 것이 틀렸다는 것이 이번 세션의 실측으로 확인됐다.

Purpose: 실패 6건(선재 3 · 오늘 3)을 현재 계약으로 다시 쓰고, 그 실측이 드러낸 실제 결함
(검색 입력 무포커스 → Esc·blur 복귀가 애초에 동작한 적 없음)을 고치고, 사용자 지시 3건
(FAB 여백 제거 · 콤보 높이 정렬 · 체크박스 행 정렬/색)을 반영한 뒤, **e2e 를 실제로 돌려
초록을 확인한다.**

Output: 소스 3파일 + 유닛 2파일 + e2e 6스펙 + WINDOWS.md, 그리고 실제 Playwright 실행 결과.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/WINDOWS.md

@webapp/src/styles/globals.css
@webapp/src/components/trading/limit-chaser-client.tsx
@webapp/src/components/trading/limit-chaser-form.tsx
@webapp/src/components/trading/dirty-action-bar.tsx
@webapp/src/components/chat/__tests__/chat-fab.test.tsx
</context>

<ground_truth>
계획 단계에서 저장소를 직접 읽어 확인한 사실이다. 추정이 아니다 — 실행자는 이것을
출발점으로 쓰되, **고치기 전에 브라우저에서 다시 본다.**

| 사실 | 위치 |
|---|---|
| pane 숨김은 클래스다 (`hidden @min-[700px]/lc:block`) | `limit-chaser-form.tsx:1009-1016` |
| `[hidden]{display:none!important}` 가 작성자 규칙으로 살아 있다 | `globals.css:243` |
| a11y 스펙은 아직 `hidden` **속성**을 기대한다 | `a11y.spec.ts:415,421,422` |
| 같은 계약을 limit-chaser 스펙은 **이미 가시성으로** 옮겼다(260912-k2x) | `trading-limit-chaser.spec.ts:313-318` |
| 모바일 미체결 카드 문구는 `미체결 {잔량} / {주문량}주` 이고 **주문번호·출처 태그가 없다** | `account-panel.tsx:562-578` |
| 주문번호는 취소 버튼 `aria-label`(`주문번호 {no} 취소`)에만 남아 있다 | `account-panel.tsx` · `orderbook.spec.ts:389` |
| orderbook 의 낡은 단언 | `orderbook.spec.ts:391` `toContainText('30/50')` |
| vi 의 낡은 단언 | `trading-vi.spec.ts:685` `toContainText('0000135801')` |
| compact 테이프가 1단·2단 트리 **둘 다**에 있다 | `orderbook-ladder.tsx:1011,1149` |
| 「잘림 0」 판정 방식(잎 요소 `getBoundingClientRect().right` 비교) | `trading-limit-chaser.spec.ts:470-510` · `orderbook.spec.ts:369-386` |
| `StockSearchField` 의 Esc·blur 는 래퍼 `<div>` 에 있고 입력에 자동 포커스가 없다 | `limit-chaser-client.tsx:1066-1090` |
| 검색 입력 `h-9` + 결과 목록 `top-10` 은 한 쌍이다 | `limit-chaser-client.tsx:1098` |
| 거래소 콤보 현재 클래스(좁은 폭 `text-[10px] px-1 py-0.5`, ≥992 `h-7 text-[14px]`) | `limit-chaser-client.tsx:479` |
| `CheckRow` 라벨은 `text-[var(--fg)]`, `Row` 라벨은 `text-[var(--muted-fg)]` | `limit-chaser-form.tsx:1287-1290` vs `1473-1476` |
| 체크박스는 `size-[17px] flex-none`, 라벨은 `text-[13px] truncate`, 묶음은 `flex items-center gap-[3px]` | `limit-chaser-form.tsx:1462-1478` |
| 세그먼트는 `role="group" aria-label="감시 대상"`, 높이 `h-[38px]` | `limit-chaser-form.tsx:676-700` |
| 종목정보 10칸은 `[data-slot="lc-quote-grid"]` 의 **직계 자식 10개**(`QuoteCell`, 자체 slot 없음) | `limit-chaser-client.tsx:596-680`, `792` |
| D-01 유닛 커버리지 | `chat-fab.test.tsx` Test 1 |
| 미인증 리다이렉트를 이미 잠근 경로: `/scanner` `/watchlist` `/` `/trading/limit-chaser` (**`/stocks/{code}` 는 없다**) | `auth-guards.spec.ts:33-83` |
| Playwright 프로젝트는 `setup`(auth.setup.ts) → `chromium`(storageState) 2단이다 | `playwright.config.ts:112-128` |
| `webapp/.playwright/auth.json` 이 존재한다 | 2026-09-12 17:32 생성 |
| WINDOWS 열린 8건 중 5·6·7·8 이 실측으로 닫힌 것, 1·3·4 가 「Playwright 미실행」 | `.planning/WINDOWS.md` |
</ground_truth>

<tasks>

<task type="tracer">
  <name>Task 1: 헤더 한 줄을 컴포넌트 → e2e → 실제 브라우저까지 꿴다 (② ④ + ①의 limit-chaser 2건 + ⑥ 3건)</name>

  <precondition>
    `pnpm -C webapp test:e2e` 가 `:3100` dev 서버를 자동 기동하거나 이미 뜬 것을 재사용한다
    (`reuseExistingServer`). `webapp/.playwright/auth.json` 이 있어야 로그인 상태 스펙이 돈다 —
    없거나 만료면 `setup` 프로젝트를 함께 돌려 재생성한다. 둘 중 하나라도 성립하지 않으면 멈추고
    보고하라.
  </precondition>

  <files>
    webapp/src/components/trading/limit-chaser-client.tsx,
    webapp/src/components/trading/__tests__/limit-chaser-client.test.tsx,
    webapp/e2e/specs/trading-limit-chaser.spec.ts
  </files>

  <read_first>
    `limit-chaser-client.tsx` 461~560(헤더 카드 한 줄) · 596~680(종목정보 10칸) · 1015~1160
    (`StockSearchField`), `trading-limit-chaser.spec.ts` 전체(특히 40~60 헬퍼 · 160~175 ·
    300~320 · 440~510).
  </read_first>

  <action>
    이 태스크는 **헤더 한 줄**이라는 하나의 표면을 프로덕션 컴포넌트 → 유닛 → e2e 단언 →
    실제 브라우저 실행까지 한 번에 꿴다. 여기서 초록을 못 보면 남은 두 태스크는 의미가 없으므로
    가장 먼저 온다.

    ⓐ **② 검색 입력 자동 포커스.** 검색이 열릴 때 입력에 포커스를 준다. `autoFocus` 속성이든
      `ref` + `useEffect` 든 수단은 고르되, 고른 이유를 그 자리에 적어라.
      ★ **첫 진입(`isin === ''`)에서도 포커스가 가는 것이 맞는지 판단하라.** 페이지를 여는 순간
        모바일 키보드가 뜨는 동작이 될 수 있다. 「사용자가 종목명을 눌러 연 경우」에만 포커스하는
        편이 안전하다고 판단하면 그렇게 하고 **근거를 코드 주석과 SUMMARY 에 남겨라.** 어느 쪽을
        골랐든 그 선택이 유닛 단언으로 잠겨야 한다.
      ★ 포커스가 생기면 래퍼의 `onBlur` 취소 경로가 **비로소 실제로 동작한다.** 결과 목록 클릭이
        blur 에 잡아먹히지 않는지(`relatedTarget` 포함 판정 + `<ul>` `onMouseDown` 기본동작 차단,
        두 장치)를 **e2e 로 확인하라** — 이 경로는 지금까지 포커스가 없어서 한 번도 실행된 적이
        없다. 유닛(jsdom)만으로 통과했다고 적지 마라.

    ⓑ **④ 거래소 콤보 높이 정렬.** 먼저 브라우저에서 세 컨트롤(거래소 `<select>` · 종목명 트리거
      `[data-slot="lc-stock-trigger"]` · 검색 입력 `[aria-label="종목 검색"]`)의 **실측 높이를
      재라**(폰 390 과 데스크톱 1180 두 폭). 그 숫자를 SUMMARY 에 적고, 그 위에서 고쳐라.
      목표 상태: 종목을 고른 상태에서는 콤보 높이 = 트리거 높이, 검색 중에는 콤보 높이 = 입력
      높이이며, **두 상태 사이에서 콤보의 높이가 변하지 않는다**(±1px). 세 컨트롤 높이를 밴드별로
      한 값에 모으는 것이 가장 단순한 해법이지만 수단은 실측 뒤에 정하라.
      ★ `appearance-none` 을 넣지 마라 — 네이티브 캐럿과 OS 선택 UI 를 잃는다. 유닛이 이 금지를
        렌더된 `className` 으로 잠근다(주석이 아니라 클래스 문자열을 본다).
      ★ 데스크톱 글자 크기(≥992 의 14px)는 직전 quick 이 이미 키운 값이다 — **줄이지 마라.**
        이번 목적은 높이 정렬이다.
      ★ 검색 입력 `h-9` 를 바꾸면 결과 목록 `top-10` 이 짝이라는 사실을 기억하라. 한쪽만 고치면
        목록이 입력에서 떠서 마우스가 그 틈을 지나는 순간 닫힌 것처럼 보인다.

    ⓒ **①의 `trading-limit-chaser.spec.ts:459` 수리 + `toHaveCount` 전수 점검.**
      1단·2단·3단 트리가 DOM 에 공존하고 한쪽만 `display:none` 이라 compact 테이프가 2개로
      세어진다. 개수 단언을 **가시성 기준**으로 옮겨라(`:visible` 등). 그리고 **같은 파일의 다른
      `toHaveCount` 단언을 전수 점검하라** — 같은 셀렉터가 숨은 트리에도 매칭되는지 하나씩 확인하고,
      점검 결과(바꾼 것 / 안 바꿔도 되는 것과 그 이유)를 SUMMARY 에 표로 남겨라. 「하나 고쳤다」로
      끝내면 옆 단언에 같은 함정이 그대로 남는다.

    ⓓ **⑥ 실측 3건 박제.** 이 스펙에 단언으로 남긴다:
      · 뷰포트 **716**(본문 700 = 컴팩트 최소)에서 세그먼트(`role="group"` `aria-label="감시 대상"`)
        가 잘리지 않는다.
      · 검색을 열어도 종목정보가 **10칸 유지**된다(`[data-slot="lc-quote-grid"]` 직계 자식 10개).
      · 검색을 열고 **Esc → 종목명 트리거로 복귀**한다(`[data-slot="lc-stock-trigger"]` 재등장).
      ★ 잘림 판정은 **이미 있는 방식**(잎 요소 `getBoundingClientRect().right` 를 컨테이너 오른쪽
        끝과 비교, 1px 반올림 여유)을 그대로 쓴다. 새 진단 방식을 만들지 마라. 파일 안에서 그
        블록을 지역 헬퍼로 뽑는 것은 허용하되, 기존 호출부의 동작이 한 글자도 달라지면 안 된다.
      ★ Esc 단언은 ⓐ 가 고쳐지기 전에는 **실패하는 것이 정상**이다. 순서상 ⓐ 를 먼저 고치고 단언을
        붙이든, 단언을 먼저 붙여 빨간 것을 보고 고치든 실행자가 고르되, **빨간 것을 본 사실을
        SUMMARY 에 적어라** — 보지 않았다면 그 단언이 무엇을 잠그는지 아무도 모른다.

    ⓔ **유닛 갱신.** `limit-chaser-client.test.tsx` 에 (1) 포커스 정책(어느 상태에서 포커스가
      가는가)과 (2) 거래소 `<select>` 의 렌더된 `className` 이 `appearance-none` 을 담지 않는다는
      단언을 넣는다. 기존 케이스는 **한 줄도 지우지 마라** — 포커스가 생기면서 깨지는 케이스가 있으면
      새 계약으로 다시 쓰고, 무엇을 왜 다시 썼는지 적어라.
  </action>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar &amp;&amp; pnpm -C webapp test src/components/trading/__tests__/limit-chaser-client.test.tsx &amp;&amp; pnpm -C webapp typecheck &amp;&amp; pnpm -C webapp test:e2e 'auth.setup|trading-limit-chaser'</automated>
  </verify>

  <done>
    검색이 열리면 입력에 포커스가 있고, Esc 가 실제 브라우저에서 종목명 트리거를 되돌리며, 결과 항목
    클릭이 blur 에 잡아먹히지 않는다. 거래소 콤보가 두 상태 모두에서 오른쪽 컨트롤과 같은 높이로 서고
    상태 전환에 튀지 않으며 `appearance-none` 이 없다. `trading-limit-chaser.spec.ts` 가 **실제
    실행에서 전부 초록**이고, 그 파일의 `toHaveCount` 전수 점검 결과가 SUMMARY 에 표로 남았다.
    ⑥ 단언 3건이 들어갔고 잘림 판정은 기존 방식 그대로다. 단언 삭제 0건.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 폼·액션바 두 지시와 a11y 계약 (③ ⑤ + ①의 a11y 1건)</name>

  <files>
    webapp/src/components/trading/limit-chaser-form.tsx,
    webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx,
    webapp/src/components/trading/dirty-action-bar.tsx,
    webapp/e2e/specs/a11y.spec.ts
  </files>

  <read_first>
    `dirty-action-bar.tsx` 전체(특히 파일 상단 ⑥ 주석과 `:88-89`), `limit-chaser-form.tsx`
    1265~1300(`Row`) · 1424~1500(`CheckRow`) · 960~1020(탭/pane), `a11y.spec.ts` 395~455,
    `globals.css` 230~245(`[hidden]` 블록).
  </read_first>

  <behavior>
    - `CheckRow` 비더티 라벨의 className 이 `Row` 비더티 라벨과 **같은 색 토큰**을 갖는다
    - `CheckRow` 더티 라벨은 여전히 `font-semibold` + `--primary` + 앞머리 `● ` 를 갖는다
    - `DirtyActionBar` 의 렌더된 className 에 오른쪽 여백 예약(`pr-[…]`)이 없다
    - 더티 0 이면 바가 DOM 에 없다(기존 계약 불변) · `role="status"` · `aria-live="polite"` 불변
  </behavior>

  <action>
    ⓐ **③ FAB 회피 여백 제거 (사용자 명시 지시).** `dirty-action-bar.tsx` 의 오른쪽 여백 유틸리티와
      그 산식 주석(`128 = 24 + 83 + 21`)을 걷어낸다. FAB 은 `/stocks/{code}` 로 좁혀졌고 이 바의
      소비처 둘(`limit-chaser-form` · `vi-settings-card`)은 모두 `/trading/*` 이라 **겹칠 수 없다.**
      ★ 파일 상단 ⑥ 주석을 **지우지 말고 다시 써라** — 「왜 있었는지」와 「왜 없어졌는지」가 같이
        남아야 다음 사람이 「FAB 과 겹친다」며 되돌리지 않는다. 공용 컴포넌트에 종목상세류 소비처가
        새로 붙으면 무엇을 다시 판단해야 하는지도 함께 적어라.
      ★ 회귀 잠금을 **렌더된 className 단언**으로 만든다(`limit-chaser-form.test.tsx` 의 기존
        `actionBar()` 헬퍼 재사용). 소스 텍스트 grep 이 아니라 클래스 문자열을 보는 이유: 주석에
        옛 값을 언급하는 순간 grep 게이트가 스스로 무효가 된다.

    ⓑ **⑤ 체크박스 행 — 색.** `CheckRow` 라벨의 비더티 색을 `Row` 와 같게 맞춘다. 더티일 때의
      `--primary` + `font-semibold` + `● ` 접두는 **그대로 둔다**(더티 표현은 비색 채널과 함께
      가는 규율이다, WCAG 1.4.1).

    ⓒ **⑤ 체크박스 행 — 정렬.** 체크박스(17px)와 13px 라벨의 광학 중심이 어긋난다.
      ★ **원인을 먼저 확인하라.** 브라우저에서 체크박스와 라벨 텍스트의 실제 박스를 재고, 어긋남이
        어디서 오는지 고르라 — 후보: 브라우저 기본 `<input type=checkbox>` 마진(Tailwind preflight
        가 이미 0으로 만들었는지 확인), 라벨 line-height 로 생긴 line box 와 17px 박스의 차,
        폰트의 광학 중심과 기하 중심 차, 홀수 px(17) 로 생기는 반픽셀. **추측으로 `mt-[1px]` 같은
        매직 오프셋을 박지 마라.** 확인한 원인을 코드 주석과 SUMMARY 에 적고, 그 원인을 고쳐라.
      ★ 어긋남이 실측상 1px 미만이라 「고칠 것이 없다」가 결론이면 **그렇게 적어라** — 없는 결함을
        고쳤다고 쓰지 말고, 실측 숫자를 근거로 남겨라. 그 경우에도 ⓑ(색)는 그대로 수행한다.

    ⓓ **①의 `a11y.spec.ts:415` 수리.** pane 숨김은 이제 **클래스**다. `hidden` **속성**으로는
      표현할 수 없다 — `globals.css` 의 `[hidden]{display:none!important}` 가
      `@min-[700px]/lc:block` 을 이겨서 컴팩트 이상에서 두 pane 을 나란히 못 세운다.
      단언을 **효과 기준**으로 바꿔라: 비활성 pane 이 보이지 않고(`toBeHidden()`), 활성 pane 이
      보이며(`toBeVisible()`), 탭을 바꾸면 둘이 뒤집힌다. 기존 `tabbablesIn(...) === []` 과
      `toBeGreaterThan(0)` 은 **그대로 둔다.**
      ★ **느슨해지는 것이 아니다** — `display:none` 도 접근성 트리에서 빠지므로 같은 성질이다.
        그 사실을 단언 옆 주석에 적어라(`trading-limit-chaser.spec.ts` 313~318 이 같은 말을 이미
        하고 있으니 표현을 맞춰라).
      ★ 셀렉터가 strict mode 에서 한 요소로 해석되는지 확인하라 — 폼이 여러 벌 렌더되면
        `toBeHidden()` 이 개수 단언과 다르게 실패한다.
  </action>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar &amp;&amp; pnpm -C webapp test src/components/trading/__tests__/limit-chaser-form.test.tsx src/components/trading/__tests__/vi-settings-card.test.tsx src/components/trading/__tests__/limit-chaser-client.test.tsx &amp;&amp; pnpm -C webapp typecheck &amp;&amp; pnpm -C webapp test:e2e 'auth.setup|a11y'</automated>
  </verify>

  <done>
    `DirtyActionBar` 의 렌더된 className 에 오른쪽 여백 예약이 없고 그 사실이 유닛으로 잠겼으며,
    제거 사유가 파일에 남았다. `CheckRow` 비더티 라벨색이 `Row` 와 같고 더티 표현 3종은 불변이다.
    체크박스 정렬은 **확인된 원인**에 따라 고쳐졌거나, 실측 근거와 함께 「고칠 것 없음」으로 적혔다.
    `a11y.spec.ts` 가 실제 실행에서 초록이고 pane 단언이 효과 기준이며 탭 순서 단언은 그대로다.
    단언 삭제 0건.
  </done>
</task>

<task type="auto">
  <name>Task 3: 남은 선재·오늘 실패 4건 + 전량 실행 + WINDOWS 정리 (①의 나머지 · ⑦)</name>

  <files>
    webapp/e2e/specs/orderbook.spec.ts,
    webapp/e2e/specs/trading-vi.spec.ts,
    webapp/e2e/specs/sidebar-tree.spec.ts,
    webapp/e2e/specs/chat.spec.ts,
    .planning/WINDOWS.md
  </files>

  <read_first>
    `orderbook.spec.ts` 310~395, `trading-vi.spec.ts` 615~700, `sidebar-tree.spec.ts` 60~150,
    `chat.spec.ts` 1~70, `auth-guards.spec.ts` 30~85, `chat-fab.test.tsx` Test 1,
    `account-panel.tsx` 530~580.
  </read_first>

  <action>
    ⓐ **선재 2건 — 문구·필드가 바뀐 자리.** 지금 **화면이 정본**이다. 단언을 현재 계약으로 다시
      쓰되 **삭제하지 마라.**
      · `orderbook.spec.ts:391` — 카드 문구는 `미체결 {잔량} / {주문량}주` 다. 의도(「30 중 50」이
        보인다)를 유지한 채 현재 문구로 다시 써라.
      · `trading-vi.spec.ts:685` — 모바일 카드에는 주문번호가 없다(260911-w5h, **사용자 승인
        사항**). 주문번호를 다시 그리게 만드는 **역주행 금지.** 그 단언의 의도는 「거래소 필터가
        남긴 한 행이 **그 주문**이다」이므로, 지금 그 주문을 식별하는 살아 있는 채널(취소 버튼
        `aria-label` 의 `주문번호 {no} 취소`, 또는 그 행만의 값)로 증거를 옮겨라.
      ★ 두 곳 모두 「무엇을 잠그던 단언인가 → 그 증거를 어디로 옮겼는가」를 단언 옆 주석에 적어라.

    ⓑ **선재 1건 — `sidebar-tree.spec.ts:111` 은 원인이 미확정이다.**
      **먼저 원인을 확인하라**(직전 커밋과의 비교, 실제 DOM 관찰, 어느 요소가 없는지).
      코드가 틀렸으면 **코드를**, 단언이 낡았으면 **단언을** 고쳐라. 둘 중 어느 쪽이었는지와 그
      판단 근거를 SUMMARY 에 적어라. 원인이 프로덕션 코드면 그 소스 파일(예:
      `webapp/src/components/layout/app-sidebar.tsx` 와 그 유닛)도 이 태스크가 소유한다 —
      단, Task 1·2 가 소유한 4개 파일은 **건드리지 마라.** 그 파일이 원인이면 멈추고 보고하라.

    ⓒ **오늘 1건 — `chat.spec.ts:40` 은 구조적으로 도달 불가다.** 앱 전체가 로그인 게이트라
      (260911-tuk) 비로그인 사용자가 FAB 을 볼 수 있는 페이지가 **존재하지 않는다.**
      두 갈래 중 하나를 고르되 **권장은 다시 쓰기**다:
      · (권장) 「비로그인은 `/stocks/{code}` 에서 `/login` 으로 리다이렉트된다」는 **현재의 진짜
        계약**으로 다시 쓴다. `auth-guards.spec.ts` 는 `/scanner` `/watchlist` `/`
        `/trading/limit-chaser` 를 잠그고 있지만 **`/stocks/{code}` 는 아무도 잠그지 않았다** —
        다시 쓰면 죽은 시나리오가 새 커버리지가 된다. ★ 리다이렉트가 실제로 일어나는지 **먼저
        관측하고** 관측한 대로 써라(next 파라미터 인코딩 포함).
      · (대안) 도달 불가 사유를 적고 제거한다.
      ★ 어느 쪽이든 **D-01 커버리지가 어디에 살아 있는지 SUMMARY 에 명시하라**
        (`chat-fab.test.tsx` 「Test 1 — 비로그인 클릭 시 로그인 필요 상태 표시 + openChat 미호출」).
        커버리지를 잃는 삭제는 금지다.

    ⓓ **★ 전량 실행 — 이것이 이 과제의 핵심이다.** `pnpm -C webapp test:e2e` 를 **전부** 돌려라.
      목표: 이번에 건드린 6개 스펙 전부 초록 + 전체 스위트에서 **새로 깨진 것 0**.
      실측 결과(통과/실패/스킵 수)를 SUMMARY 에 **그대로** 적어라. 기준선은 66 passed / 6 failed /
      4 skipped 다. 남는 실패가 있으면 **숨기지 말고** 원인과 함께 보고하고 WINDOWS 에 등재하라.
      ★ 없는 검증을 했다고 적지 마라.
      ★ **SUMMARY 에 e2e 숫자를 적을 때 그 값이 실제로 파싱된 것인지 확인하라. 못 읽었으면 숫자를
        지어내지 말고 「파싱 실패」라고 적어라.** 게이트가 `GATE_PARSE_FAIL` 을 내면 그것이 곧
        「읽지 못했다」이며 「실패 0」이 아니다 — 둘을 섞어 적는 순간 이 과제의 목적(미검증을 실측으로
        닫는다)이 정확히 뒤집힌다. 판정의 1차 신호는 숫자가 아니라 **Playwright 의 exit code** 다.

    ⓔ **⑦ WINDOWS 정리.** `node ~/.claude/gsd-core/bin/gsd-tools.cjs windows fixed <id>` /
      `waive <id> "<reason>"` 로 갱신한다.
      · **닫는다(실측으로 종결):** 5(세그먼트 버튼 폭 — 폰 62 / 컴팩트최소 56 / 792 75 / 와이드 69,
        잘림 0), 6(카드 틴트 패딩 16px 잘림 여유 — 네 폭 전부 잘림 0), 8(검색 입력 잔여 점프 —
        헤더 카드 +4px 로 측정됨), 7(호가 초기 스크롤 중앙정렬 — **사용자가 직접 확인해 정상**).
      · **실제 실행 결과로 갱신:** 1·3·4(「Playwright 미실행」). 이번에 돌렸으므로 **무조건 삭제가
        아니라** 무엇을 돌렸고 무엇이 남았는지로 갱신한다 — 초록이면 닫고, 남은 실패가 있으면 그
        내용으로 다시 등재하라.
      · **2(밴드 경계 700/830/992)는 임의로 닫지 마라.** 이번 ⑥ 단언은 **700 경계만** 덮는다.
        실제로 덮은 범위를 근거로 「좁혀 다시 등재」하거나 그대로 두고, 그 판단을 SUMMARY 에 적어라.
      · jsdom·이번 실행으로 증명되지 않은 것이 남으면 `windows append` 로 새로 등재하라.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/alex/repos/gh-radar || exit 1; OUT="${TMPDIR:-/tmp}/ok2"; mkdir -p "$OUT" || exit 1; pnpm -C webapp test 2&gt;&amp;1 | tee "$OUT/test.out" || exit 1; pnpm typecheck || exit 1; pnpm lint 2&gt;&amp;1 | tee "$OUT/lint.out" || exit 1; pnpm build || exit 1; pnpm -C webapp test:e2e 2&gt;&amp;1 | tee "$OUT/e2e.out"; E2E_RC=$?; TF=$(grep -oE 'Test Files +[0-9]+ passed' "$OUT/test.out" | grep -oE '[0-9]+'); TP=$(grep -oE 'Tests +[0-9]+ passed' "$OUT/test.out" | grep -oE '[0-9]+'); W=$(grep -c 'Warning:' "$OUT/lint.out"); E=$(grep -c 'Error:' "$OUT/lint.out"); EP=$(grep -oE '^ *[0-9]+ passed' "$OUT/e2e.out" | tail -1 | grep -oE '[0-9]+'); EF=$(grep -oE '^ *[0-9]+ failed' "$OUT/e2e.out" | tail -1 | grep -oE '[0-9]+'); if [ -z "$TF" ] || [ -z "$TP" ]; then echo "GATE_PARSE_FAIL: vitest 요약을 읽지 못했다 — $OUT/test.out 을 직접 보라"; exit 1; fi; if [ -z "$W" ] || [ -z "$E" ]; then echo "GATE_PARSE_FAIL: lint 출력을 읽지 못했다 — $OUT/lint.out 을 직접 보라"; exit 1; fi; if [ -z "$EP" ]; then echo "GATE_PARSE_FAIL: e2e 요약의 'N passed' 줄을 읽지 못했다 — 이것은 실패 0 이 아니라 판독 불가다. $OUT/e2e.out 을 직접 보고 SUMMARY 에 숫자를 지어내지 마라"; exit 1; fi; if [ -z "$EF" ]; then EF=0; fi; echo "unit files=$TF passed=$TP | lint warn=$W err=$E | e2e rc=$E2E_RC passed=$EP failed=$EF"; if [ "$E2E_RC" -ne 0 ]; then echo "GATE_FAIL: playwright exit=$E2E_RC — 실패가 남아 있다"; exit 1; fi; if [ "$TF" -ge 63 ] &amp;&amp; [ "$TP" -ge 801 ] &amp;&amp; [ "$W" -eq 3 ] &amp;&amp; [ "$E" -eq 0 ] &amp;&amp; [ "$EF" -eq 0 ] &amp;&amp; [ "$EP" -ge 71 ]; then echo GATE_OK; else echo "GATE_FAIL: 기준선 미달"; exit 1; fi; node -e 'const s=require("child_process").execSync("node "+process.env.HOME+"/.claude/gsd-core/bin/gsd-tools.cjs windows status",{encoding:"utf8"});const j=JSON.parse(s);const closed=[5,6,7,8].every(id=&gt;{const e=j.ledger.entries.find(x=&gt;x.id===id);return e&amp;&amp;e.status!=="open";});if(!closed)throw new Error("WINDOWS 5/6/7/8 still open");if(j.ledger.open_count&gt;4)throw new Error("open_count="+j.ledger.open_count);console.log("windows open_count="+j.ledger.open_count)'</automated>
  </verify>

  <done>
    `pnpm -C webapp test` exit 0 이고 Test Files ≥ 63 · Tests passed ≥ 801 · skipped 1 이다(감소 0).
    `pnpm typecheck` exit 0 · `pnpm lint` Error 0 / Warning 정확히 3 · `pnpm build` exit 0.
    **`pnpm -C webapp test:e2e` 가 실제로 돌아 failed 0 · passed ≥ 71** 이고, 이번에 건드린 6개
    스펙이 전부 초록이며, 실측 숫자가 SUMMARY 에 그대로 적혔다. WINDOWS 5·6·7·8 이 닫혔고
    1·3·4 는 실제 실행 결과로 갱신됐으며 2 의 처리 판단이 기록됐다. 단언 삭제는 chat 비로그인
    시나리오 1건의 조건부 예외 외에 0건이고, D-01 커버리지 소재가 SUMMARY 에 명시됐다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 DOM → 사용자 | 상따는 실계좌 발주 설정 화면이다. 잘못 읽힌 숫자·눌리지 않는 컨트롤이 곧 오발주다 |
| 키보드/스크린리더 → 화면 | 포커스가 어디에도 없으면 그 화면은 키보드 사용자에게 조작 불가다 |
| e2e 단언 → 사람의 믿음 | 통과하는 단언은 「참이다」라는 선언이다. 가시성을 보지 않는 단언은 거짓을 참이라 말한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ok2-01 | Tampering (계약 역주행) | `orderbook.spec.ts` · `trading-vi.spec.ts` | high | mitigate | e2e 를 통과시키려 화면을 되돌리는 것이 가장 큰 위험이다. 주문번호·출처 태그 제거는 **사용자 승인 사항**이므로 Task 3 ⓐ 는 코드가 아니라 단언만 옮기고, 옮긴 증거 채널을 주석에 적는다 |
| T-ok2-02 | Denial of Service (기능 상실) | `StockSearchField` 포커스 | high | mitigate | 포커스가 생기면 `onBlur` 취소가 **처음으로 실제 동작**한다 — 결과 클릭이 blur 에 먹히면 종목을 영영 못 고른다. Task 1 이 그 경로를 유닛이 아니라 **실제 브라우저**로 확인한다 |
| T-ok2-03 | Information Disclosure (역: 정보 소실) | `a11y.spec.ts` pane 단언 | high | mitigate | 단언을 맞추려 `hidden` 속성을 되돌리면 `[hidden]{display:none!important}` 가 컴팩트 이상에서 두 pane 을 죽인다. 효과 기준(`toBeHidden`)으로 옮기고 탭 순서 단언은 그대로 둔다 |
| T-ok2-04 | Spoofing (거짓 초록) | `toHaveCount` 단언 전반 | high | mitigate | 트리 3벌 공존으로 `toHaveCount` 가 숨은 DOM 을 센다. 한 곳이 아니라 **파일 전수 점검**을 요구하고 결과를 표로 남긴다 |
| T-ok2-05 | Repudiation (커버리지 소실) | `chat.spec.ts` 비로그인 시나리오 | medium | mitigate | 도달 불가 시나리오 제거가 D-01 커버리지 상실로 이어지지 않도록, 다시 쓰기를 권장하고 어느 쪽이든 커버리지 소재를 SUMMARY 에 명시하게 한다 |
| T-ok2-06 | Elevation of Privilege (UI 오조작) | 모바일 자동 포커스 | medium | mitigate | 첫 진입에서 무조건 포커스하면 모바일에서 페이지를 여는 순간 키보드가 뜬다. Task 1 이 「트리거로 연 경우에만」을 명시적 판단 항목으로 두고 근거를 남기게 한다 |
| T-ok2-07 | Denial of Service (CTA 상실) | `dirty-action-bar.tsx` 여백 제거 | low | accept | 오늘 두 소비처는 FAB 과 공존하지 않는다(사용자 명시 지시). 새 소비처가 붙을 때 다시 판단해야 한다는 사실을 파일 주석으로 남기는 것이 수용 조건이다 |
| T-ok2-08 | Tampering (시각 회귀) | `CheckRow` 정렬 수정 | low | mitigate | 매직 오프셋은 다음 폰트·크기 변경에서 조용히 틀어진다. 원인 확인을 선행 조건으로 못박고, 원인 불명이면 고치지 말고 실측을 적게 한다 |
| T-ok2-SC | Tampering | npm/pip/cargo installs | n/a | accept | **패키지 설치 0건.** 기존 의존성(Playwright · vitest · Tailwind)만 쓴다 — 새 공급망 표면이 없다 |
</threat_model>

<verification>
게이트는 Task 3 마지막에 한 번 돈다. 기준선은 저장소에서 실제로 측정한 값이다.

| 명령 | 기준선 | 허용 |
|---|---|---|
| `pnpm -C webapp test` | 63 files / 801 passed / 1 skipped | 감소 0 (`files ≥ 63`, `passed ≥ 801`) |
| `pnpm typecheck` | exit 0 | exit 0 (shared build 선행 포함) |
| `pnpm lint` | Error 0 / Warning 3 | Error 0 · Warning **정확히 3** |
| `pnpm build` | exit 0 | exit 0 |
| `pnpm -C webapp test:e2e` | 66 passed / **6 failed** / 4 skipped | **failed 0** · passed ≥ 71 |

★ **「못 읽었다」와 「0이다」를 구분한다.** 판정의 1차 신호는 숫자가 아니라 **Playwright 의
exit code** 다(`E2E_RC`) — 실패가 하나라도 있으면 0 이 아니다. 숫자 파싱은 **보고용**이고,
요약의 `N passed` 줄을 읽지 못하면 기본값으로 때우지 않고 `GATE_PARSE_FAIL` 로 **시끄럽게**
멈춘다. `failed` 줄은 실패 0 일 때 리포터가 아예 출력하지 않으므로, 그 부재를 0 으로 읽는 것은
**`passed` 줄이 읽혔고 `E2E_RC` 가 0 일 때만** 정당하다 — 그 두 조건을 먼저 통과시킨 뒤에만
`EF=0` 으로 둔다. 네 가지 출력(초록 · 실패 있음 · 요약 판독 불가 · 빈 출력)에 대해 이 판정을
계획 단계에서 실제로 돌려 확인했다.
  · 이 저장소는 이 패턴에 이미 한 번 데였다 — **무로그 fail-safe 금지**(LLM 워커 catch 가 실패를
    삼켜 「간헐적 문제」로 위장됐던 건). 게이트가 조용히 0 을 넣으면 이 과제의 목적(미검증을
    실측으로 닫는다)이 정확히 뒤집힌다.
  · 같은 이유로 `vitest`·`lint` 요약도 빈 문자열이면 기본값 없이 `GATE_PARSE_FAIL` 로 멈춘다.
    플랜 전체에 `|| echo 0` · `|| true` 형태의 조용한 기본값은 **0건**이다.

★ e2e passed 하한을 71 로 둔 이유: chat 비로그인 시나리오를 (대안) 갈래로 제거하면 총 케이스가
하나 줄어든다. 다시 쓰기를 택하면 72 이상이 된다. **어느 쪽이든 failed 는 0 이어야 한다.**

★ `pnpm typecheck` 는 루트 스크립트가 `@gh-radar/shared` 를 먼저 빌드하므로 낡은 `dist` 함정
(16-41 이 데인 것)을 자동으로 피한다 — `pnpm -C webapp typecheck` 만 돌리고 끝내지 마라.
</verification>

<success_criteria>
- ① ~ ⑦ 일곱 항목이 전부 구현됐고, 어느 것도 「v1」·「임시」·「나중에」로 축소되지 않았다.
- **e2e 를 실제로 돌렸고** 그 숫자가 SUMMARY 에 실제 출력에서 옮겨 적혔다.
- 기존 단언 삭제 0건(chat 1건 조건부 예외). 깨진 계약은 전부 새 계약으로 다시 쓰였다.
- 화면을 되돌린 곳이 0곳이다 — 주문번호·출처 태그·`hidden` 속성 어느 것도 e2e 를 위해 부활하지 않았다.
- `sidebar-tree` 는 「코드가 틀렸다 / 단언이 낡았다」 중 어느 쪽이었는지가 근거와 함께 적혔다.
- `CheckRow` 정렬은 **확인된 원인**에 따라 고쳐졌고 매직 오프셋이 없다.
- WINDOWS 5·6·7·8 이 닫혔고 1·3·4 가 실제 결과로 갱신됐으며 2 의 처리 판단이 기록됐다.
- 전량 게이트 5종 통과.
</success_criteria>

<output>
Create `.planning/quick/260912-ok2-e2e-6-fab/260912-ok2-SUMMARY.md` when done.

SUMMARY 에 반드시 포함:
- **e2e 실측 숫자 원문** (passed / failed / skipped, 실행 명령, 남은 실패가 있으면 스펙·라인·원인)
- 6개 스펙 각각 「무엇이 왜 깨져 있었고 → 무엇으로 다시 썼는가」 한 줄씩
- `sidebar-tree` 의 원인 판정 (코드였는가 단언이었는가 · 근거)
- `chat` 갈래 선택과 **D-01 커버리지가 어디에 살아 있는지** 명시
- `trading-limit-chaser.spec.ts` `toHaveCount` **전수 점검 표** (바꾼 것 / 안 바꾼 것과 이유)
- ② 포커스 정책 선택(첫 진입 포함 여부)과 그 근거 · Esc 단언이 고치기 전 **빨간 것을 본 사실**
- ④ 세 컨트롤(콤보 · 트리거 · 검색 입력) 높이 **실측값** (폰 390 · 데스크톱 1180)
- ⑤ 체크박스 어긋남의 **확인된 원인**과 실측 숫자 (또는 「고칠 것 없음」의 실측 근거)
- ③ 여백 제거 후 회귀 잠금이 어디에 있는지(유닛 케이스 이름)
- WINDOWS 처리 전체 목록 (닫음 / 갱신 / 새로 등재)
- **하지 않은 검증을 했다고 적지 말 것**
</output>
