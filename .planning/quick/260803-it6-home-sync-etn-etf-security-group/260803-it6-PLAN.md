---
phase: quick-260803-it6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/home-sync/src/pipeline/loadSurges.ts
  - workers/home-sync/src/pipeline/loadSurges.test.ts
autonomous: false
requirements: [HOME-01]

must_haves:
  truths:
    - "security_group 이 'ETF' 또는 'ETN' 인 종목은 홈 급등 집합에서 제외된다"
    - "security_group 이 '주권' 으로 오분류된 ETN(예 570127 한투 인버스2X코스피200선물 ETN)도 종목명 패턴으로 제외된다"
    - "종목명에 인버스/레버리지가 포함된 상품은 제외된다"
    - "제외는 surgeMax slice **이전**에 일어나 제외된 자리를 후순위 일반 종목이 채운다"
    - "stocks 마스터에 없는 코드는 기존대로 이름=코드로 통과한다(이름 패턴만 적용)"
    - "종목명에 '스팩' 이 들어간 종목은 이번 범위가 아니므로 계속 통과한다"
    - "기존 home-sync 테스트 115개가 회귀 없이 통과한다"
  artifacts:
    - path: "workers/home-sync/src/pipeline/loadSurges.ts"
      provides: "isExcludedProduct 순수 판별 함수 + 마스터 해석→필터→정렬→slice 재배치"
      contains: "isExcludedProduct"
    - path: "workers/home-sync/src/pipeline/loadSurges.test.ts"
      provides: "ETF/ETN group 제외, 오분류 ETN 이름 fallback, 인버스/레버리지, slice 전 필터 순서, 스팩 통과 회귀"
      contains: "570127"
  key_links:
    - from: "workers/home-sync/src/pipeline/loadSurges.ts"
      to: "stocks 마스터 select"
      via: "security_group 컬럼 추가 조회"
      pattern: "code,name,market,security_group"
    - from: "loadSurges 파이프라인"
      to: "surgeMax slice"
      via: "filter 를 slice 앞에 배치"
      pattern: "\\.filter\\([\\s\\S]*isExcludedProduct[\\s\\S]*\\.slice\\(0, cfg\\.surgeMax\\)"
---

<objective>
홈 급등 스캔(home-sync)에서 ETN/ETF/레버리지·인버스 상품을 전면 제외한다.

배경(2026-07-20 실사례): 홈에 "인버스ETN" 테마로 KODEX/TIGER 인버스 ETF + 각사 인버스 ETN
12개가 노출됐다. 지수 하락에 연동된 파생상품이 "오늘의 급등 테마"로 잡히면 트레이더에게
아무 의미가 없다.

Purpose: 홈 급등 테마의 신호 품질 확보 — 급등 슬롯을 실제 개별 종목 재료로만 채운다.
Output: loadSurges.ts 이중 필터 + 파이프라인 순서 재배치, 테스트, 프로덕션 배포+smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@workers/home-sync/src/pipeline/loadSurges.ts
@workers/home-sync/src/pipeline/loadSurges.test.ts
@workers/home-sync/src/index.ts

<interfaces>
<!-- 실행자가 직접 사용할 핵심 계약. 코드 탐색 불필요. -->

**실측 데이터 (Supabase stocks 마스터, 확인 완료 — 재조회 불필요):**
- `security_group` 분포: 주권 871, ETF 84, ETN 15, 부동산투자회사 24, 투자회사 2 등. ELW 는 현재 0건.
- **오분류 실사례**: `570127` "한투 인버스2X코스피200선물 ETN" 이 `security_group='주권'`,
  `security_type='보통주'` 로 잘못 적재됨 → group 필터만으로는 누락. **이름 패턴 fallback 필수.**

**선례 (workers/theme-sync/src/pipeline/upsertThemes.ts, 읽기전용 — 구조만 참고):**
```typescript
export function isThemeEligible(row: {
  name: string | null; security_group: string | null; kosdaq_segment: string | null;
}): boolean {
  if (row.security_group && ETP_SECURITY_GROUPS.has(row.security_group)) return false;
  if (row.kosdaq_segment?.startsWith("SPAC")) return false;
  if (row.name?.includes("스팩")) return false;
  return true;
}
```
→ 이번 plan 은 **스팩 로직을 복제하지 않는다** (별도 결정 필요, 범위 밖).

**현재 loadSurges 파이프라인 (변경 대상 순서):**
```
stock_quotes(change_rate>=threshold, updated_at>=오늘KST자정)
  → sort desc → slice(surgeMax)      ← 여기서 이미 잘림
  → stocks 마스터 code→name 해석 (청크 IN, QUOTE_CHUNK=200, select "code,name,market")
  → news_articles 48h 창 + 종목별 2단정렬 top-K
```
**문제:** slice 이후에 필터하면 제외분만큼 급등 슬롯이 비어버린다.

**변경 후 순서 (승인된 설계):**
```
stock_quotes(threshold 통과 전체, 정렬/slice 없음)
  → stocks 마스터 해석 (select 에 security_group 추가)
  → isExcludedProduct 필터
  → sort change_rate desc → slice(surgeMax)
  → news_articles (기존 그대로)
```
threshold(기본 15~20%) 통과분만 마스터 해석하므로 청크 IN 비용 미미.

**테스트 mock 헬퍼 (workers/home-sync/tests/helpers/supabase-mock.ts, 기존 사용법 그대로):**
```typescript
setQuotes(sb.from("stock_quotes"), [{ data: [...], error: null }]);  // 파일 상단 로컬 헬퍼
sb.from("stocks").in.mockResolvedValue({ data: [...], error: null });
sb.from("news_articles").order.mockResolvedValue({ data: [], error: null });
```
`stocks` mock 은 `.in` resolve 라 select 컬럼 변경의 영향을 받지 않는다 → 기존 테스트 무회귀.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: isExcludedProduct 이중 필터 + slice 이전 배치</name>
  <files>workers/home-sync/src/pipeline/loadSurges.ts, workers/home-sync/src/pipeline/loadSurges.test.ts</files>
  <behavior>
    `export function isExcludedProduct(name, securityGroup): boolean` — 순수 함수, 2개 인자 모두
    `string | null | undefined` 허용:
    - securityGroup 이 (trim + toUpperCase 후) 'ETF' 또는 'ETN' → true.
    - 그 외에는 name 에 `/ETN|ETF|인버스|레버리지/i` 매치 → true.
    - name/securityGroup 둘 다 없으면 false (마스터 미해석 코드는 통과).
    - '스팩' 은 패턴에 **없다** — "삼성스팩8호" 는 false 를 반환해야 한다(범위 밖, 회귀 가드).
    - 'KODEX'/'TIGER' 등 브랜드명도 패턴에 없다 — group 필터가 커버하므로 일반 기업명 오탐을 피한다.

    loadSurges 동작:
    - stocks 마스터 select 가 `"code,name,market,security_group"` 으로 확장되고, security_group 이
      code 별 맵으로 보관된다.
    - 마스터 해석 대상이 threshold 통과 **전체** 코드다 (slice 이전).
    - 제외 필터 → change_rate desc 정렬 → surgeMax slice 순서로 적용된다.
    - 필터 후 0건이면 `[]` 반환 (뉴스 쿼리 미실행).
    - retry-on-empty / 신선도 컷오프 / 뉴스 48h 창 / 2단 정렬은 전부 기존 동작 유지.

    loadSurges.test.ts 신규 케이스:
    1. `isExcludedProduct` 단위: ('삼성전자','주권')=false, ('KODEX 200','ETF')=true,
       ('아무거나','etn' 소문자)=true, ('한투 인버스2X코스피200선물 ETN','주권')=true,
       ('KODEX 레버리지','주권')=true, ('삼성스팩8호','주권')=false, (undefined,undefined)=false.
    2. security_group='ETF' 종목이 결과에서 빠진다.
    3. **오분류 fixture**: code `570127` / name "한투 인버스2X코스피200선물 ETN" /
       security_group `'주권'` → 결과에서 빠진다 (이름 패턴 fallback 증명).
    4. 종목명에 '인버스'/'레버리지' 포함 + group='주권' → 빠진다.
    5. **slice 전 필터 순서**: surgeMax=2, quotes 4건(등락률 desc 로 1·2위가 ETF/ETN, 3·4위가
       일반 종목) → 결과가 3·4위 일반 종목 2건 (빈 슬롯 없음). 이 테스트가 이번 변경의 핵심.
    6. 마스터 미해석 코드(stocks mock 이 빈 배열) → 기존대로 name=code 로 통과.
    7. stocks select 인자에 'security_group' 이 포함됐는지 assert
       (`sb.from("stocks").select.mock.calls[0][0]`).
    8. 스팩 회귀: name '삼성스팩8호' / group '주권' → 통과(제외 안 됨).
  </behavior>
  <action>
    loadSurges.ts 상단(NEWS_CANDIDATES_PER_STOCK 근처)에 상수 + 판별 함수 추가:
    ```typescript
    /** ETP 증권그룹 — KRX SECUGRP_NM. 실측 stocks 마스터에 ELW 0건이라 ETF/ETN 만 (승인 설계). */
    const EXCLUDED_SECURITY_GROUPS = new Set(["ETF", "ETN"]);
    /**
     * 종목명 fallback 패턴 — security_group 오분류 대비(570127 "한투 인버스2X코스피200선물 ETN"
     * 이 '주권'/'보통주' 로 적재된 실사례). KODEX/TIGER 등 브랜드명은 일반 기업명 오탐 위험 대비
     * 실익이 낮아 미포함(그 상품들은 group 필터가 커버). '스팩' 은 이번 범위 아님 — 넣지 말 것.
     */
    const EXCLUDED_NAME_PATTERN = /ETN|ETF|인버스|레버리지/i;

    export function isExcludedProduct(
      name: string | null | undefined,
      securityGroup: string | null | undefined,
    ): boolean {
      if (securityGroup && EXCLUDED_SECURITY_GROUPS.has(securityGroup.trim().toUpperCase())) {
        return true;
      }
      return name ? EXCLUDED_NAME_PATTERN.test(name) : false;
    }
    ```

    파이프라인 재배치 (현행 122~143 라인 구간):
    - `quoteRows` → `const passing = quoteRows.map(r => ({ code: r.code, changeRate: Number(r.change_rate) }))`
      로만 변환(정렬/slice 제거). `if (passing.length === 0) return [];`
    - 마스터 해석 루프의 순회 대상을 `passing.map(s => s.code)` 로 바꾸고, select 를
      `"code,name,market,security_group"` 으로 확장. 응답 행 타입을
      `{ code: string; name: string; security_group?: string | null }` 로 받아
      `nameByCode` 와 신규 `groupByCode: Map<string, string | null>` 두 맵을 채운다.
    - 그 다음 필터+정렬+slice:
      ```typescript
      const surges = passing
        .filter((s) => !isExcludedProduct(nameByCode.get(s.code), groupByCode.get(s.code)))
        .sort((a, b) => b.changeRate - a.changeRate)
        .slice(0, cfg.surgeMax);
      if (surges.length === 0) return [];
      const codes = surges.map((s) => s.code);
      ```
    - 이후 뉴스 로드 블록(3, 3b)과 최종 return 은 **무변경** (`codes` 를 그대로 사용).
    - 파일 상단 JSDoc 흐름 설명(1~19라인)의 1·2번 항목을 새 순서로 갱신하고, "필터가 slice 보다
      먼저여야 제외분 슬롯을 후순위 종목이 채운다" 이유를 1줄로 명시.

    index.ts / config.ts / 프롬프트는 변경하지 않는다 (loadSurges 반환값만 줄어들 뿐,
    content hash·carry·클러스터링 경로는 그대로 동작).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/workers/home-sync && pnpm test && pnpm typecheck</automated>
  </verify>
  <done>home-sync 전체 스위트 green — 기존 115개 회귀 0 + 신규 케이스 통과, typecheck exit 0. slice 전 필터 순서 테스트(케이스 5)와 오분류 ETN fixture(케이스 3)가 실제로 assert 된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 배포 + smoke + 홈 육안 확인 (체크포인트)</name>
  <action>오케스트레이터가 아래 how-to-verify 절차를 실행한 뒤 사용자 승인을 받는다. 코드 변경 없음.</action>
  <what-built>
    home-sync loadSurges 에 ETN/ETF/레버리지·인버스 이중 필터(security_group + 종목명 패턴) 적용 +
    surgeMax slice 이전으로 필터 배치(제외분 슬롯을 후순위 일반 종목이 채움).
    코드/테스트 완료. 프로덕션 배포 + smoke 는 오케스트레이터가 실행.
  </what-built>
  <how-to-verify>
    오케스트레이터 실행 절차:
    1. 빌드: `cd /Users/alex/repos/gh-radar/workers/home-sync && pnpm build`
    2. 배포: `bash /Users/alex/repos/gh-radar/scripts/deploy-home-sync.sh`
       (env: GCP_PROJECT_ID + SUPABASE_URL 필수 — MEMORY reference_deploy_worker_env)
    3. smoke: `bash /Users/alex/repos/gh-radar/scripts/smoke-home-sync.sh` — INV-1~6 전부 PASS.
    4. Job 1회 실행 후 오늘 최신 home_theme_snapshots payload 확인:
       - themes/singles 어디에도 ETF/ETN/인버스/레버리지 종목명이 없다.
       - "인버스ETN" 류 테마가 사라졌다.
       - stock_count 가 0 으로 붕괴하지 않았다(필터 후에도 일반 급등 종목이 남는다).
       - 장 마감/급등 없는 시간대라 비어 있으면, 최근 급등 있던 trade_date 스냅샷과 비교.
    5. 프로덕션 홈(https://gh-radar-webapp.vercel.app/) 육안 확인.
    커밋은 CLAUDE.md 규칙(한글 메시지, 사용자 확인 후, Co-Authored-By 금지) 준수.
  </how-to-verify>
  <resume-signal>배포+smoke PASS 및 홈에서 ETF/ETN 소멸 확인 후 "approved" 또는 이슈 보고</resume-signal>
</task>

</tasks>

<verification>
- `cd workers/home-sync && pnpm test && pnpm typecheck` 통과 (기존 115개 회귀 0).
- isExcludedProduct 단위 테스트: ETF/ETN group, 소문자 etn, 오분류 570127, 레버리지, 스팩 통과, null 통과.
- slice 전 필터 순서 테스트: surgeMax=2 에 상위 2건이 ETP 일 때 결과가 후순위 일반 2건.
- stocks select 인자에 security_group 포함 assert.
- 배포 후 smoke-home-sync.sh 전 항목 PASS + 스냅샷 payload 에 ETF/ETN 부재.
</verification>

<success_criteria>
1. 홈 급등 스냅샷에 security_group='ETF'/'ETN' 종목이 0건.
2. security_group 오분류 ETN(570127 형태)도 종목명 패턴으로 0건.
3. 제외로 인해 급등 슬롯이 비지 않는다 — surgeMax 만큼 일반 종목으로 채워진다.
4. 스팩은 계속 통과한다(범위 밖 결정 보존).
5. home-sync 테스트 스위트 green, 프로덕션 smoke 전 항목 PASS.
</success_criteria>

<output>
완료 후 `.planning/quick/260803-it6-home-sync-etn-etf-security-group/260803-it6-SUMMARY.md` 생성.
</output>
