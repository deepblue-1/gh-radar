---
phase: quick-260923-p3k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/src/components/trading/workbench/trading-workbench.tsx
  - webapp/src/components/trading/__tests__/trading-workbench.test.tsx
  - webapp/e2e/specs/trading-workbench.spec.ts
autonomous: true
requirements: [P3K-A, P3K-B]

must_haves:
  truths:
    - "카드를 접으면 접힘 스택의 맨 끝으로, 펼치면 펼친 카드들의 맨 끝으로 간다 — 「가장 최근에 상태가 바뀐 카드가 그 무리의 끝」 (P3K-A)"
    - "새로 열리는 카드(종목 추가 · 돌파 칩 · 미체결 행 선택 · `?focus=`/사이드바 포커스 · 거래소 토글 충돌로 다른 카드가 열릴 때)도 펼친 카드 맨 끝이다. 새 카드 append 와 등록 전략 자동 카드(`open:false` append)는 이미 끝이라 그대로다 (P3K-A)"
    - "이미 같은 상태인 카드(펼친 카드를 다시 포커스 · 없는 id)는 배열 참조가 그대로라 순서도 재렌더도 바뀌지 않는다 (P3K-A)"
    - "`renderOrderOf`(card-grid ①)는 손대지 않는다 — 무리 안 순서 = 작업대 카드 배열 순서, 순서 원천은 배열 하나다. 포커스 · `scrollIntoView` 규율(card-grid ③ · card-header ③)도 그대로다 (P3K-A)"
    - "저장 배치(quick-260923-lyt)는 배열 순서를 그대로 저장·복원하므로 새 순서가 다른 메뉴에 갔다 와도 유지된다 — 기존 L1 테스트가 그대로 초록 (P3K-A)"
    - "게이트: `pnpm --filter @gh-radar/webapp run typecheck` 0 · `pnpm --filter @gh-radar/webapp run test` 전량 초록 · Playwright `trading-workbench` spec 0 fail · `pnpm-lock.yaml` 변경 0 (P3K-B)"
  artifacts:
    - path: "webapp/src/components/trading/workbench/trading-workbench.tsx"
      provides: "`export function withCardOpen(prev, id, open)` 순수 함수 + `open` 을 바꾸는 7개 지점의 단일 경로"
      contains: "export function withCardOpen"
    - path: "webapp/src/components/trading/__tests__/trading-workbench.test.tsx"
      provides: "`withCardOpen` 순수 함수 describe + 작업대 순서 describe(toggle · addCard/focusCard · selectUnfilled)"
      contains: "withCardOpen"
    - path: "webapp/e2e/specs/trading-workbench.spec.ts"
      provides: "케이스 6 에 「방금 바뀐 카드가 무리의 끝」 정체성 단언(펼친 칸 순서 · 스택 안 순서)"
      contains: "quick-260923-p3k"
  key_links:
    - from: "trading-workbench.tsx `toggleCard` · `focusCard` · `addCard`(기존 카드 분기) · `selectUnfilled`(existing 분기) · `changeExchange`(clash 분기) · `withFocusedCard`(기존 키 분기) · `fillAccountCards`(펼침 승계)"
      to: "`withCardOpen`"
      via: "`setCards((prev) => withCardOpen(prev, id, open))` — map 으로 `open` 만 바꾸던 식 전부 교체"
    - from: "카드 배열 순서"
      to: "card-grid.tsx `renderOrderOf` → DOM 순서 → `writeTradingLayout` 저장 순서"
      via: "배열이 유일한 순서 원천 (변경 없음)"
---

<objective>
작업대 카드 순서 규칙 — **접으면 접힘 스택 맨 끝, 펼치면 펼친 카드 맨 끝, 새로 열리는 카드도 펼친 카드 맨 끝**. 즉 「가장 최근에 상태가 바뀐 카드가 그 무리의 끝」.

Purpose: 지금은 무리 안 순서가 카드가 처음 생긴 순서로 고정돼, 방금 펼친 카드가 펼친 무리의 **중간**에 끼거나 방금 접은 카드가 스택 **중간**에 박힌다. 사용자가 방금 만진 카드를 무리의 끝에서 예측 가능하게 찾도록 한다(사용자 요청 2026-09-23).

Output: `trading-workbench.tsx` 순수 함수 `withCardOpen` + `open` 을 바꾸는 모든 지점의 단일 경로화 · vitest(순수 함수 + 작업대 순서) · e2e 케이스 6 정체성 단언. `card-grid.tsx` · `renderOrderOf` · 포커스/스크롤 규율 · `trading-layout.ts` 는 **손대지 않는다**.

설계는 오케스트레이터가 잠갔다(`<design_decisions_locked>`). 대안 검토 · `renderOrderOf` 정렬 로직 변경 · 저장 포맷 변경 · 애니메이션 · 새 의존성은 이 quick 밖이다.

**MUTABLE-SCOPE — 순차 실행 전제:** 계획 시점 HEAD `00100f0`. 같은 저장소에서 quick-260923-onn 이 `trading-workbench.tsx`(`WorkbenchCardItem` prop 배선 · `renderCard`) · `trading-workbench.test.tsx` · e2e spec(케이스 10/11 · GC2/GC3 LED 단언)을 먼저 편집·커밋한다. 이 플랜은 **그 뒤**에 돈다 — 아래는 줄 번호가 아니라 **함수 이름**으로 지점을 가리키며, onn 이 손대지 않는 함수들(`withFocusedCard` · `fillAccountCards` · `focusCard` · `addCard` · `toggleCard` · `changeExchange` · `selectUnfilled` 의 업데이터)만 바꾼다. 실행 전 `git status -sb` 로 working tree 가 깨끗한지(onn 커밋 완료) 확인하고, 아니면 멈춘다.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

**앵커 지도 (플래너가 읽었다 — 파일 전체를 다시 읽지 말고 함수 이름으로 Grep 해 그 구간만 연다).**

webapp/src/components/trading/workbench/card-grid.tsx (253줄 · **수정 없음**)
- 머리 주석 ① 「렌더 순서는 펼친 카드들 → 스택이고, 같은 무리 안에서는 받은 순서를 지킨다. DOM 순서가 곧 포커스 순서다」 — 그대로 참이다. ③ 포커스 규율 · ⑤ 고정 호스트 노드(포털) 그대로.
- `export function renderOrderOf(cards)` → `{ open, folded }` 를 입력 순서대로 나눈다. **그대로 둔다.**

webapp/src/components/trading/workbench/trading-workbench.tsx (1124줄)
- 머리 주석 ② 「이 컴포넌트가 카드 집합의 단일 소유자」 — 여기에 순서 규칙 한 단락을 더한다.
- `interface WorkbenchCard { id, isin, accountNo, exchange, open, name?, code? }` · `function keyOf(c)`.
- 순수 함수 구간(모두 `WorkbenchSurface` 위, export 된 것은 테스트가 직접 부른다): `withFocusedCard(prev, hit, newId)`(비export · 기존 키 카드면 `prev.map` 으로 펼침, 없으면 append) → `cardForUnfilled` → `holdingQuotePrice` → `fillAccountCards(cards, accountNo)`(끝에서 `openKeys` 에 든 접힌 카드를 `kept.map` 으로 펼친다) → `restoreSavedCards`(open 변경 없음 — 그대로).
- `WorkbenchSurface` 안 콜백: 등록 전략 효과(`fresh.forEach` 로 `open:false` append → 그대로 · 끝에 `withFocusedCard` 호출) · `useTradingFocusRequest` 콜백(`withFocusedCard`) · `focusCard(isin)`(첫 카드 `prev.map` 펼침) · `addCard(isin, name?, code?)`(기존 카드면 `prev.map` 펼침, 없으면 append) · `toggleCard(id)`(`prev.map` 으로 반전) · `changeExchange(id, exchange)`(clash 분기 `prev.map` 으로 clash 카드 펼침 — 두 번째 `setCards` 는 exchange 만 바꾸므로 그대로) · `selectUnfilled(row)`(`cardForUnfilled` existing 이면 `prev.map` 펼침, new 면 append).
- 저장 효과 `writeTradingLayout(userId, { cards, seen })` — `cards` 배열 순서 그대로 저장. 복원 `restoreSavedCards` 는 저장 순서 그대로 앞에 세운다. **별도 처리 불필요.**

webapp/src/components/trading/__tests__/trading-workbench.test.tsx (1765줄)
- 스텁: `StubCard`(`data-slot="strategy-card"` · `data-key` · `data-open`, 토글 버튼 `id=strategy-card-{cardId}-toggle` + `aria-expanded`, 「{label} 카드 닫기」 · 「{label} NXT」 버튼), `StockAddBar` 스텁(「추가」 = KR7005930003 삼성전자 · 있으면 `onFocusCard`), `BreakoutStrip` 은 실물(칩 `[data-slot="breakout-chip"]` · `rc()` 기본 = 씨젠 KR7096530001 · 카드 있으면 `onFocusCard`).
- 헬퍼: `ACCOUNT` · `slot(name)` · `cardsInDom()`(DOM 순서 = 렌더 순서) · `toggleOf(isin)` · `lc(isin, over)` · `rc(over)` · `relay(over)` · `mockRelay` · `render(<TradingWorkbench />)`.
- import 블록 `import { cardForUnfilled, fillAccountCards, holdingQuotePrice, restoreSavedCards, … } from '../workbench/trading-workbench'` — 여기에 `withCardOpen` 을 더한다.
- 기존 순서 단언: describe 「배치 기억 (quick-260923-lyt)」 L1 은 조작 **뒤**의 `byKey()` 를 찍어 리마운트 후와 비교한다 — 새 규칙에서도 그대로 초록이어야 한다(저장·복원이 배열 순서를 지킨다는 증거). WR-05 describe 는 등록 자동 카드 순서(append)만 본다 — 불변. `fillAccountCards` describe 는 결과가 1~2장이라 순서 영향 없음.

webapp/src/components/trading/__tests__/card-grid.test.tsx (**수정 없음**) — `Harness` 가 자기 state 로 `prev.map` 토글해 `renderOrderOf` 입력 순서 유지 계약을 잠근다. 작업대 규칙과 무관하다.

webapp/e2e/specs/trading-workbench.spec.ts (1891줄)
- 헬퍼 `grid(page)` · `cards(page)` · `cardSelector(isin)` = `[data-slot="strategy-card"][data-key^="{isin}:"]` · `cardOf` · `toggleOf(page, isin)` · `pickCols`.
- 케이스 `'6. 펼침 0/1/2+ × 접힘 0/1/N …'` — 시드 `[E2E_ISIN, E2E_LONG_NAME_ISIN, 'KR7086520004']` 3장 접힘. `layout()` 은 칸을 `open`/`stack(n)` 로만 찍어 카드 정체성을 보지 않는다. `const cells = grid(page).locator(':scope > *')` 가 이미 선언돼 있다. 순서: LONG 펼침 → E2E 펼침(`['open','open','stack(1)']`) → 086520 펼침 → LONG 접기(`['open','open','stack(1)']`) → 본문 숨김 단언으로 끝.
- 케이스는 직렬이며 페이지를 공유한다(GC2 「다음 케이스(직렬)에 더티 상태를 남기지 않는다」) — 케이스 6 의 끝 상태 모양(펼침 2 · 접힘 1)을 바꾸지 않는다.

게이트 명령 근거(이전 quick 이 통과시킨 원문): `pnpm --filter @gh-radar/webapp run typecheck` · `pnpm --filter @gh-radar/webapp run test`(config `test_command` 의 webapp 몫) · `cd webapp && pnpm exec playwright test trading-workbench`(260923-onn 플랜 L181 · relay 는 spec 픽스처가 8090 에 스스로 띄운다 · dev 서버 3100 은 `webServer` 가 재사용/기동).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `withCardOpen` 순수 함수 + `open` 을 바꾸는 7개 지점 단일 경로화 + vitest</name>
  <files>webapp/src/components/trading/workbench/trading-workbench.tsx, webapp/src/components/trading/__tests__/trading-workbench.test.tsx</files>
  <read_first>webapp/src/components/trading/workbench/trading-workbench.tsx (머리 주석 ② · `withFocusedCard` · `fillAccountCards` · `focusCard` · `addCard` · `toggleCard` · `changeExchange` · `selectUnfilled` 구간만 Grep 으로), webapp/src/components/trading/__tests__/trading-workbench.test.tsx:60-260 (스텁·헬퍼) · describe 「배치 기억 (quick-260923-lyt)」 · describe 「fillAccountCards」, webapp/src/components/trading/workbench/card-grid.tsx:102-111 (`renderOrderOf`)</read_first>
  <behavior>
    - 순수 함수(새 describe 「withCardOpen — 카드 순서 규칙 (quick-260923-p3k · 순수 함수)」, `renderOrderOf` 는 `'../workbench/card-grid'` 에서 import):
      - 대상이 이미 같은 상태(펼친 카드에 `open:true`) → 반환이 `toBe(prev)` (참조 동일)
      - 없는 id → `toBe(prev)`
      - 접기: `[A open, B open, C folded]` 에 `withCardOpen(prev, 'A', false)` → id 순서 `['B','C','A']` · A 만 `open:false` · `renderOrderOf` 결과 `open=[B]` · `folded=[C, A]` · 입력 `prev` 는 그대로(불변)
      - 펼치기: `[A folded, B folded, C open]` 에 `withCardOpen(prev, 'A', true)` → `['B','C','A']` · `renderOrderOf` `open=[C, A]` · `folded=[B]`
    - 작업대(새 describe 「TradingWorkbench — 카드 순서: 가장 최근에 바뀐 카드가 무리의 끝 (quick-260923-p3k)」, `keys = () => cardsInDom().map((c) => [c.getAttribute('data-key'), c.getAttribute('data-open')])`):
      - 토글: `limitChasers: [lc(X), lc(Y), lc(Z)]` 3장 접힘 → `toggleOf(X)` · `toggleOf(Y)` 펼침 → DOM `[X true, Y true, Z false]` → `toggleOf(X)` 접기 → DOM `[Y true, Z false, X false]`(X 가 스택 **맨 끝**) → `toggleOf(Z)` 펼침 → DOM `[Y true, Z true, X false]`(Z 가 펼친 무리 **맨 끝**)
      - 새로 열리는 카드(돌파 칩 = `rc()` 씨젠 S): `limitChasers: [lc(X), lc(Y)]`, `rateCrossItems: [rc()]` → 칩 클릭(추가 · 펼침 · 끝) → `toggleOf(S)` 접기 → `toggleOf(Y)` 펼침 → `toggleOf(X)` 펼침 → DOM `[Y true, X true, S false]` → 칩 다시 클릭(「거래중」 → `focusCard`) → DOM `[Y true, X true, S true]`(S 가 펼친 무리 맨 끝 — 옛 규칙이면 `[S, Y, X]`) → 칩 한 번 더(이미 펼침 · 참조 유지) → 순서 불변 `[Y, X, S]`
      - 미체결 행 선택(existing 분기): 기존 describe 「TradingWorkbench — 미체결 행 선택 (D-21)」 의 픽스처(`account`/`accountStates` 미체결 행 · 공용 패널 실물 행 클릭)를 그대로 써서, 정확 일치 카드가 **접혀 있고 다른 펼친 카드가 뒤에 있는** 상태에서 행을 누르면 그 카드가 펼친 무리의 **맨 끝**으로 오는지 `keys()` 로 단언(케이스 「WR-04 — 정확 일치 카드가 있으면 카드가 늘지 않고 그 카드가 펼쳐진다」 옆에 케이스 1개 추가). 픽스처를 새로 만들지 않는다.
    - 기존 테스트: describe 「배치 기억」 L1 · 「fillAccountCards」 · WR-05 · 「카드 추가」 는 **수정 없이** 초록이어야 한다. 새 규칙 때문에 깨지는 입력-순서 단언이 있으면 새 규칙으로 갱신하고 SUMMARY 에 케이스명을 적는다.
  </behavior>
  <action>
1. `trading-workbench.tsx` — `withFocusedCard` **바로 위**에 순수 함수를 추가한다(export · 테스트가 직접 부른다):
   `export function withCardOpen(prev: WorkbenchCard[], id: string, open: boolean): WorkbenchCard[]` — `findIndex` 로 대상을 찾아 없거나 `open` 이 이미 같으면 `prev` 를 **그대로 반환**(참조 유지 · React 베일아웃 · 재렌더 없음), 다르면 그 카드를 뺀 나머지 순서를 지킨 새 배열 끝에 `{ ...card, open }` 을 붙여 반환한다. 입력을 변형하지 않는다. JSDoc: 「카드 순서 규칙(quick-260923-p3k) — 접으면 접힘 스택 맨 끝, 펼치면 펼친 카드 맨 끝. `open` 을 바꾸는 모든 경로가 이 함수 하나를 지난다. 무리 안 순서는 배열 순서다(card-grid ①) — `renderOrderOf` 는 손대지 않는다.」
2. `open` 을 바꾸던 7개 지점을 전부 `withCardOpen` 으로 교체한다 — 스프레드로 `open` 만 갱신하던 `prev.map(...)` 식은 파일에 남기지 않는다:
   - `withFocusedCard`: 기존 키 분기 — `prev.find((x) => keyOf(x) === hit.key)` 로 카드를 잡아 `withCardOpen(prev, cur.id, true)`; 없으면 기존 append 그대로.
   - `fillAccountCards`: 끝의 `openKeys` 반영 — `let next = kept;` 로 시작해 `kept` 를 순회하며 `openKeys.has(keyOf(c)) && !c.open` 인 카드마다 `next = withCardOpen(next, c.id, true)` (펼침을 잇는 등록 카드도 펼친 무리 끝 — 「가장 최근에 바뀐 카드」 규칙 동일). `openKeys.size === 0` 이면 `kept` 그대로. 반환 모양 `{ next, dropped }` 불변.
   - `focusCard`: 첫 카드를 찾아 `withCardOpen(prev, first.id, true)`; 없으면 `prev`.
   - `addCard`: 기존 카드 분기 → `withCardOpen(prev, first.id, true)`; 없으면 기존 append 그대로(이미 끝).
   - `toggleCard`: `prev.find` 로 현재 `open` 을 읽어 `withCardOpen(prev, id, !cur.open)`; 없으면 `prev`.
   - `changeExchange`: clash 분기 → `withCardOpen(prev, clash.id, true)`. 두 번째 `setCards`(exchange 교체)는 그대로.
   - `selectUnfilled`: `target.kind === "existing"` → `withCardOpen(prev, target.id, true)`; new 는 기존 append 그대로.
   - 등록 전략 자동 카드(`fresh.forEach` 의 `open:false` append) · `restoreSavedCards` · `removeCard` 는 손대지 않는다.
3. 머리 주석 ② 에 한 단락을 더한다: 「★ 카드 순서(quick-260923-p3k) — 배열 순서가 곧 무리 안 순서다(card-grid ①). `open` 을 바꾸는 모든 경로는 `withCardOpen` 하나를 지나 그 카드를 배열 **맨 끝**으로 옮긴다: 접으면 접힘 스택 맨 끝, 펼치면(토글 · 종목 추가/돌파 칩 · 미체결 선택 · `?focus=`/사이드바 · 거래소 충돌) 펼친 카드 맨 끝. 새 카드와 등록 전략 자동 카드는 append 라 이미 끝이다. 이미 같은 상태면 참조 그대로(순서 · 재렌더 불변). 배치 저장(quick-260923-lyt)은 이 배열 순서를 그대로 저장·복원한다.」 포커스·스크롤(card-grid ③ · `scrollTarget` 레이아웃 효과)은 건드리지 않는다 — 토글 뒤 포커스는 같은 id 의 헤더 버튼으로 돌아오고 스크롤 대상은 여전히 `cards.find` 로 푼다.
4. `trading-workbench.test.tsx` — import 블록에 `withCardOpen` 을 더하고 `renderOrderOf` 를 `'../workbench/card-grid'` 에서 import 한다. `<behavior>` 의 두 describe 와 미체결 케이스 1개를 **RED 먼저**(함수가 없으니 컴파일 실패 = RED) → 1~3 구현 → GREEN. 기존 케이스는 무수정 초록이 목표다.
5. 게이트: typecheck + 이 테스트 파일 + card-grid 테스트. 커밋(한글 · `feat(quick-260923-p3k): 작업대 카드 순서 — 접으면 접힘 스택 끝 · 펼치면 펼친 카드 끝 (withCardOpen 단일 경로)` · Co-Authored-By 금지 · `git add -A` 금지 — 두 파일만 지정 · `.planning` 미커밋 · push 금지).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp test src/components/trading/__tests__/trading-workbench.test.tsx src/components/trading/__tests__/card-grid.test.tsx && test "$(grep -c 'export function withCardOpen' webapp/src/components/trading/workbench/trading-workbench.tsx)" -eq 1 && test "$(grep -c 'withCardOpen(' webapp/src/components/trading/workbench/trading-workbench.tsx)" -ge 8 && test "$(grep -c 'open: !c.open' webapp/src/components/trading/workbench/trading-workbench.tsx)" -eq 0 && test "$(grep -c '{ ...x, open: true }' webapp/src/components/trading/workbench/trading-workbench.tsx)" -eq 0 && test "$(grep -c '{ ...c, open: true }' webapp/src/components/trading/workbench/trading-workbench.tsx)" -eq 0 && test "$(grep -c 'withCardOpen' webapp/src/components/trading/__tests__/trading-workbench.test.tsx)" -ge 5 && git diff --quiet HEAD -- webapp/src/components/trading/workbench/card-grid.tsx webapp/src/lib/trading-layout.ts pnpm-lock.yaml</automated>
  </verify>
  <done>`withCardOpen` 이 export 된 순수 함수로 존재하고 `open` 을 바꾸는 7개 지점(`withFocusedCard` · `fillAccountCards` · `focusCard` · `addCard` · `toggleCard` · `changeExchange` · `selectUnfilled`)이 전부 그것을 부른다(호출 8회 이상 · 스프레드 `open` 갱신 식 0). 순수 함수 4케이스 + 작업대 순서 3케이스(토글 · 돌파 칩 추가/포커스 · 미체결 existing)가 초록이고 기존 lyt L1 · fillAccountCards · WR-05 · 카드 추가 케이스가 무수정 초록. `card-grid.tsx` · `trading-layout.ts` · lockfile diff 0. typecheck 0. 코드 커밋 1건.</done>
</task>

<task type="auto">
  <name>Task 2: e2e 케이스 6 정체성 단언 + 전체 게이트</name>
  <files>webapp/e2e/specs/trading-workbench.spec.ts</files>
  <read_first>webapp/e2e/specs/trading-workbench.spec.ts:100-120 (헬퍼 `grid` · `cards` · `cardSelector` · `toggleOf`) · 케이스 `'6. 펼침 0/1/2+ × 접힘 0/1/N'` 본문 전체</read_first>
  <action>
1. 케이스 6 안, 「펼침 2 · 접힘 1」 단언(`toggle(E2E_ISIN)` 뒤 `['open','open','stack(1)']`) **직후**에 정체성 단언 2줄을 더한다 — 이미 선언된 `cells` 를 재사용: `cells.nth(0).locator(cardSelector(E2E_LONG_NAME_ISIN))` 가 1개, `cells.nth(1).locator(cardSelector(E2E_ISIN))` 가 1개(방금 펼친 카드가 펼친 무리 **맨 끝** — 옛 규칙이면 시드 순서대로 E2E 가 앞이었다). 주석에 `quick-260923-p3k` 를 적는다.
2. 케이스 6 **끝**(마지막 본문 숨김 단언 뒤)에 스택 안 순서 단언을 더한다: `toggle(E2E_ISIN)` 클릭 → `layout()` 이 `['open','stack(2)']` → `grid(page).locator('[data-slot="card-stack"] [data-slot="strategy-card"]')` 의 `nth(0)` 가 `data-key` `^{E2E_LONG_NAME_ISIN}:` · `nth(1)` 이 `^{E2E_ISIN}:` (방금 접은 카드가 스택 **맨 끝** · `toHaveAttribute('data-key', new RegExp(...))`). 그다음 `toggle(E2E_ISIN)` 을 다시 클릭해 `['open','open','stack(1)']` 로 되돌린다 — 직렬 공유 페이지의 끝 상태 모양(펼침 2 · 접힘 1)을 이전과 같게 남긴다.
3. 게이트: `pnpm --filter @gh-radar/webapp run typecheck`(e2e tsconfig 포함) · `pnpm --filter @gh-radar/webapp run test` 전량 · `cd webapp && pnpm exec playwright test trading-workbench`(0 fail). Playwright 가 환경(relay 8090 픽스처 · dev 3100) 문제로 돌지 못하면 원인 로그를 SUMMARY 에 적고 멈춘다 — 통과로 적지 않는다.
4. 커밋(한글 · `feat(quick-260923-p3k): e2e 케이스 6 — 방금 바뀐 카드가 무리의 끝 단언` · 이 파일만 지정 · Co-Authored-By 금지 · `.planning` 미커밋 · push 금지).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp run test && test "$(grep -c 'quick-260923-p3k' webapp/e2e/specs/trading-workbench.spec.ts)" -ge 1 && cd webapp && pnpm exec playwright test trading-workbench</automated>
  </verify>
  <done>케이스 6 이 펼친 칸 순서(LONG → E2E)와 스택 안 순서(LONG → E2E)로 새 규칙을 실브라우저에서 잠그고 끝 상태 모양은 이전과 같다. typecheck 0 · webapp vitest 전량 초록 · Playwright `trading-workbench` spec 0 fail. 코드 커밋 1건, push 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → 작업대 상태 | 저장 배치(`readTradingLayout`)는 이미 try/catch · 모양 검증을 거쳐 읽힌다(quick-260923-lyt). 이번 변경은 배열 순서만 바꾸고 포맷·파서는 건드리지 않는다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-p3k-01 | Tampering | `withCardOpen` / 저장 배치 순서 | low | accept | 순수 함수 · 서버 송신 0 · 화면 순서만. 조작된 localStorage 는 기존 lyt 파서가 거른다. |
| T-p3k-02 | Denial of Service | `withCardOpen` 참조 유지 | low | mitigate | 같은 상태면 `prev` 참조 반환 → `setCards` 베일아웃 · 저장 효과 재실행 없음(무한 루프 방지). 순수 함수 테스트 「참조 동일」 2케이스가 잠근다. |
| T-p3k-SC | Tampering | 패키지 설치 | low | accept | 설치 0 · `pnpm-lock.yaml` 변경 0 (Task 1 verify 가 diff 0 확인). |
</threat_model>

<verification>
- Task 1: typecheck 0 · `trading-workbench.test` + `card-grid.test` 초록 · `withCardOpen` 호출 ≥ 8 · 스프레드 `open` 갱신 식 0 · `card-grid.tsx` · `trading-layout.ts` · lockfile diff 0.
- Task 2: webapp vitest 전량 · Playwright `trading-workbench` spec 0 fail.
- 수동(선택): `/trading` 에서 카드 3장 펼친 뒤 가운데 카드를 접으면 스택 맨 끝, 스택 가운데 카드를 펼치면 펼친 카드 맨 끝, 돌파 칩으로 접힌 카드를 열면 펼친 카드 맨 끝. 다른 메뉴 갔다 와도 순서 유지.
</verification>

<success_criteria>
- 「가장 최근에 상태가 바뀐 카드가 그 무리의 끝」 이 `withCardOpen` 한 경로로 구현되고, `open` 을 바꾸는 7개 지점이 전부 그것을 쓴다.
- `renderOrderOf` · card-grid 포커스/스크롤 규율 · 저장 포맷 불변. 저장·복원은 새 순서를 그대로 지킨다(기존 L1 무수정 초록).
- vitest(순수 함수 4 + 작업대 3) · e2e 케이스 6 정체성 단언이 규칙을 잠근다. 게이트 전부 초록. 커밋 2건(한글 · Co-Authored-By 없음), push 없음.
</success_criteria>

<output>
Create `.planning/quick/260923-p3k-wb-card-order/260923-p3k-SUMMARY.md` when done — 바뀐 지점 7개 · 새 테스트 케이스명 · 갱신한 기존 케이스(있으면) · 게이트 결과 원문(typecheck · vitest 수 · Playwright passed/failed 수) · 커밋 해시.
</output>
