---
phase: quick-260720-in0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/home-sync/src/pipeline/loadThemeHints.ts
  - workers/home-sync/src/pipeline/loadThemeHints.test.ts
  - workers/home-sync/src/ai/prompt.ts
  - workers/home-sync/src/ai/prompt.test.ts
  - workers/home-sync/src/ai/clusterSurges.ts
  - workers/home-sync/src/ai/clusterSurges.test.ts
  - workers/home-sync/src/index.ts
  - workers/home-sync/src/index.test.ts
autonomous: false
requirements: [QUICK-260720-in0]
user_setup: []

must_haves:
  truths:
    - "급등 종목 2개 이상이 공유하는 네이버 테마가 Claude 클러스터링 프롬프트에 '참고 테마 분류' 섹션으로 전달된다"
    - "뉴스 근거가 없어도 같은 참고 테마에 속한 2+ 급등 종목을 그 테마로 묶을 수 있다 (사료 3종목 케이스)"
    - "단독 소속 테마(1종목)는 프롬프트에서 제외되어 토큰/노이즈가 늘지 않는다"
    - "기존 65개 테스트가 회귀 없이 통과한다 (themeHints 기본값 빈 Map 하위호환)"
  artifacts:
    - path: "workers/home-sync/src/pipeline/loadThemeHints.ts"
      provides: "theme_stocks→themes 로드 + 2+ 공유 필터 → Map<themeName, string[]>"
      exports: ["loadThemeHints"]
    - path: "workers/home-sync/src/ai/prompt.ts"
      provides: "formatClusterMessage(surges, themeHints) 참고 테마 섹션 + 시스템 규칙 + few-shot"
      contains: "참고 테마 분류"
  key_links:
    - from: "workers/home-sync/src/index.ts"
      to: "loadThemeHints"
      via: "loadSurges 후 힌트 로드 → cluster(surges, cfg, themeHints)"
      pattern: "loadThemeHints"
    - from: "workers/home-sync/src/ai/clusterSurges.ts"
      to: "formatClusterMessage"
      via: "themeHints 전달"
      pattern: "formatClusterMessage\\(surges, themeHints"
---

<objective>
home-sync 클러스터링에 네이버 테마 멤버십을 힌트로 추가해, 뉴스 텍스트 신호가 없어도 동일 테마 동반 급등 종목을 묶는다.

문제: 2026-07-20 장중 미래생명자원(218150)·고려산업(002140)·한탑(002680)이 곡물/사료 재료로 동반 급등했으나, 클러스터링이 100% 뉴스 텍스트 기반(D-04 anti-hallucination)이라 세 종목을 잇는 텍스트가 없어 전부 singles 처리됨. 해결 데이터는 이미 존재 — theme-sync 가 수집한 네이버 테마 `사료` = 정확히 이 3종목 (themes/theme_stocks). 추가 크롤링/API 불필요.

Purpose: anti-hallucination 원칙(뉴스 우선, 사실 지어내기 금지)을 유지하면서 기존 큐레이션 테마 멤버십을 클러스터링 힌트로만 활용.
Output: 신규 loadThemeHints 로더 + 프롬프트 참고 섹션 + 오케스트레이션 배선 + 테스트.

설계 원본: @.planning/quick/260720-in0-home-sync-singles/approved-plan.md (재설계 금지, 그대로 분해)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260720-in0-home-sync-singles/approved-plan.md
@workers/home-sync/src/pipeline/loadSurges.ts
@workers/home-sync/src/ai/prompt.ts
@workers/home-sync/src/ai/clusterSurges.ts
@workers/home-sync/src/index.ts

<interfaces>
<!-- 실행자가 그대로 사용할 계약. 코드베이스 탐색 불필요. -->

Surge 타입 (loadSurges.ts) — 변경 금지, themeHints 는 별도 인자:
```typescript
export interface Surge {
  code: string;
  name: string;
  changeRate: number;
  news: NewsRow[];
}
```

theme_stocks 스키마 (theme-sync/pipeline/upsertThemes.ts 확인):
  - 컬럼: theme_id (uuid), stock_code (text), source, effective_from, effective_to
  - **활성 멤버십 = effective_to IS NULL** (retired 는 effective_to 채워짐)

themes 스키마:
  - 컬럼: id (uuid), name (text), norm_key, is_system (bool), hidden (bool)
  - **표시 대상 = hidden = false**

QUOTE_CHUNK 청크 IN 패턴 (loadSurges.ts, 재사용):
```typescript
const QUOTE_CHUNK = 200;
for (let i = 0; i < codes.length; i += QUOTE_CHUNK) {
  const chunk = codes.slice(i, i + QUOTE_CHUNK);
  const { data, error } = await supabase.from("...").select("...").in("col", chunk);
  if (error) throw error;
  // ...
}
```

formatClusterMessage 현재 시그니처 (prompt.ts) — themeHints 인자 추가 대상:
```typescript
export function formatClusterMessage(surges: Surge[]): {
  message: string;
  indexedNews: Array<{ title: string; url: string; source: string }>;
}
```

clusterSurges 현재 시그니처 (clusterSurges.ts) — themeHints 인자 추가 대상:
```typescript
export async function clusterSurges(
  surges: Surge[],
  cfg: HomeSyncConfig,
): Promise<ClusterResult>
```

HomeSyncDeps.cluster (index.ts) — 시그니처 확장 대상:
```typescript
cluster?: (surges: Surge[], cfg: HomeSyncConfig) => Promise<ClusterResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 테마 힌트 로더 loadThemeHints</name>
  <files>workers/home-sync/src/pipeline/loadThemeHints.ts, workers/home-sync/src/pipeline/loadThemeHints.test.ts</files>
  <behavior>
    - 급등 코드 3개 중 002140·002680·218150 이 theme_stocks 에서 theme_id X('사료') 를 공유 → Map 에 {"사료": ["002140","002680","218150"]} 포함.
    - 어떤 테마에 급등 종목이 1개만 속하면(정치인 테마 등) 해당 테마는 Map 에서 제외 (2+ 공유 필터).
    - effective_to 가 채워진(retired) 멤버십은 무시 (활성만).
    - hidden=true 테마는 제외.
    - codes 200개 초과 시 QUOTE_CHUNK 로 나눠 조회 (청크 경계 넘어 같은 테마 종목이 합산되는지 검증).
    - 빈 codes → 빈 Map (Supabase 호출 0). theme_stocks 결과 0행 → 빈 Map.
    - Supabase error → throw (조용한 실패 금지).
  </behavior>
  <action>
    신규 `workers/home-sync/src/pipeline/loadThemeHints.ts` 생성. export async function `loadThemeHints(supabase: SupabaseClient, codes: string[]): Promise<Map<string, string[]>>`.

    구현:
    1. codes 비었으면 즉시 빈 Map 반환.
    2. QUOTE_CHUNK(=200, loadSurges.ts 값과 동일하게 로컬 상수 선언) 청크로 `theme_stocks` 조회: `.select("theme_id,stock_code").in("stock_code", chunk).is("effective_to", null)`. error 시 throw. 결과를 `Map<theme_id, Set<stock_code>>` 로 누적 (청크 경계 넘어 종목 합산). stock_code 는 반드시 chunk 집합(Set)에 포함된 것만 반영(mock/응답 방어, loadSurges 3) 단계 패턴 계승).
    3. **2+ 공유 필터**: value Set 크기 < 2 인 theme_id 제거.
    4. 남은 theme_id 들을 QUOTE_CHUNK 청크로 `themes` 조회: `.select("id,name,hidden").in("id", chunk)`. error 시 throw. hidden=true 는 제외. id→name 매핑.
    5. `Map<themeName, string[]>` 구성 — name 있는 테마만, 종목 코드 배열은 stock_code 오름차순 정렬(결정적 출력, 스냅샷 테스트 안정). 동일 name 충돌 시(드묾) 코드 병합.
    6. 파일 상단에 목적 주석: "급등 종목 2+ 가 공유하는 네이버 테마만 클러스터링 힌트로 로드. Surge 불변, 별도 인자. anti-hallucination 유지 — Claude 에 힌트로만 제공."

    테스트 `loadThemeHints.test.ts`: loadSurges.test.ts 의 Supabase mock 패턴 계승(`.from().select().in().is()` 체이닝 mockResolvedValue). behavior 6개 케이스 커버. 실제 Claude/네트워크 호출 없음.
  </action>
  <verify>
    <automated>pnpm -C workers/home-sync test -- loadThemeHints</automated>
  </verify>
  <done>loadThemeHints 신규 테스트 통과, 2+ 공유 필터·청크·effective_to·hidden·빈 입력·error throw 모두 검증.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 프롬프트 참고 섹션 + 배선 (prompt → clusterSurges → index)</name>
  <files>workers/home-sync/src/ai/prompt.ts, workers/home-sync/src/ai/prompt.test.ts, workers/home-sync/src/ai/clusterSurges.ts, workers/home-sync/src/ai/clusterSurges.test.ts, workers/home-sync/src/index.ts, workers/home-sync/src/index.test.ts</files>
  <behavior>
    - formatClusterMessage(surges, themeHints) 가 2+ 종목 공유 테마를 "참고 테마 분류" 섹션으로 유저 메시지 끝에 추가. themeHints 빈 Map → 섹션 미출력(기존 출력과 동일, 하위호환).
    - 섹션 포맷: "참고 테마 분류 (네이버, 2개 이상 급등 종목이 공유하는 것만):" 헤더 + "- 사료: 002140 고려산업, 002680 한탑, 218150 미래생명자원" 라인(종목명은 surges 에서 해석, 미해석 시 코드만).
    - indexedNews 출력은 불변 (뉴스 인덱스 계약 영향 없음).
    - 시스템 프롬프트에 규칙 2줄 추가(뉴스 부족해도 참고 테마 2+ 묶기 허용 + 뉴스 서사 충돌 시 뉴스 우선), 기존 규칙 유지.
    - few-shot 1개 추가: 뉴스 없는 2종목 + 참고 분류 "사료" → 사료 테마로 묶는 예시.
    - clusterSurges(surges, cfg, themeHints?) — themeHints 기본값 빈 Map, formatClusterMessage 로 전달. content hash 계산(computeContentHash)은 불변.
    - runHomeSyncCycle: loadSurges 후 loadThemeHints 호출 → cluster(surges, cfg, themeHints) 전달. HomeSyncDeps.cluster 시그니처에 themeHints 추가.
  </behavior>
  <action>
    **prompt.ts**:
    - `formatClusterMessage(surges: Surge[], themeHints: Map<string, string[]> = new Map())` 로 시그니처 확장(기본값 빈 Map — 하위호환). 반환 타입 불변.
    - 함수 끝에서 themeHints.size>0 이면 참고 섹션 문자열 구성: 헤더 "참고 테마 분류 (네이버, 2개 이상 급등 종목이 공유하는 것만):" + 각 [name, codes] 를 "- {name}: {code} {종목명}, ..." 라인으로. 종목명은 surges 에서 code→name Map 만들어 해석, 미존재 시 코드만. message 끝에 "\n\n" + 섹션 append. themeHints 비면 append 안 함(기존 message 그대로).
    - `CLUSTER_SYSTEM_PROMPT` 규칙 목록에 approved-plan §2 문구 그대로 2줄 추가: (a) "뉴스 근거가 부족해도, 참고 테마 분류에서 같은 테마에 속한 급등 종목 2개 이상은 그 테마로 묶을 수 있다. 이 경우 테마명은 참고 분류의 이름을 사용하고, reason 에는 뉴스 근거가 없으면 '동일 테마 소속 동반 급등'임을 밝히며 사실을 지어내지 않는다. newsRefs 는 실제 있는 인덱스만." (b) "참고 분류가 뉴스 서사와 충돌하면 뉴스를 우선한다." 기존 규칙·JSON 출력 예시는 유지.
    - `CLUSTER_FEW_SHOT` 에 항목 1개 추가(곡물사료 재현): user 에 급등 2종목 + "뉴스: (없음)" + 참고 테마 분류 "사료" 섹션, assistant 에 사료 테마로 묶고 reason="동일 테마 소속 동반 급등" newsRefs=[] 인 JSON. buildClusterFewShot 은 CLUSTER_FEW_SHOT 순회라 자동 반영.

    **clusterSurges.ts**:
    - `clusterSurges(surges, cfg, themeHints: Map<string, string[]> = new Map())` 로 확장. `formatClusterMessage(surges, themeHints)` 호출로 변경. 나머지 파이프라인(demote/resolve/reassign/sort) 불변. computeContentHash 는 index.ts 소관이며 surges 만 입력 → 불변 확인.

    **index.ts**:
    - `HomeSyncDeps.cluster` 시그니처를 `(surges: Surge[], cfg: HomeSyncConfig, themeHints: Map<string, string[]>) => Promise<ClusterResult>` 로 확장.
    - loadThemeHints import. surges 로드 후(hash-miss 4b 분기 진입 전 또는 진입 시) `const themeHints = surges.length > 0 ? await loadThemeHints(supabase, surges.map(s => s.code)) : new Map<string,string[]>();` — surges 0 이면 호출 0(Supabase 절약). 4b 분기의 `cluster(surges, cfg)` → `cluster(surges, cfg, themeHints)`.
    - content hash 계산·hash-match carry 분기는 불변(테마 멤버십은 일 배치라 사실상 정적, hash 에 미포함 — approved-plan §3).

    **테스트** (기존 mock 패턴 계승, 회귀 0):
    - prompt.test.ts: themeHints 있는 경우 섹션 포맷·2+ 필터 이미 로더 책임이므로 여기선 "전달된 Map 그대로 섹션 출력" + 빈 Map 시 미출력(기존 message 동일) 검증. indexedNews 불변 확인.
    - clusterSurges.test.ts: themeHints 인자가 formatClusterMessage 로 전달되는 경로(spy 또는 message 문자열에 섹션 포함) + 기본값 빈 Map 하위호환.
    - index.test.ts: cluster mock 이 themeHints 3번째 인자를 받는지 + loadThemeHints 를 surges>0 시 호출/surges=0 시 미호출(Supabase mock) 검증. 기존 carry/hash-skip 테스트 회귀 없음.
  </action>
  <verify>
    <automated>pnpm -C workers/home-sync test && pnpm -C workers/home-sync typecheck</automated>
  </verify>
  <done>신규 + 기존 전체 테스트 통과(회귀 0), typecheck exit 0. 사료 few-shot·참고 섹션·배선 경로 검증됨.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    테마 힌트 로더 + 프롬프트 참고 섹션 + home-sync 배선 완료. 로컬 테스트·typecheck green.
    배포는 오케스트레이터가 사용자 GCP 인증으로 실행 (deploy-home-sync.sh, 기존 스케줄 유지).
  </what-built>
  <how-to-verify>
    1. 배포 실행 (사용자 인증): `bash workers/home-sync/deploy-home-sync.sh` (기존 스케줄 `*/5 9-15 * * 1-5` 유지 확인).
    2. 스모크: `bash scripts/smoke-home-sync.sh` — 통과 확인 (스케줄 하드코딩 기대값이 있으면 함께 점검, lessons.md 교훈).
    3. 다음 거래일 장중 스냅샷에서 곡물/사료 동반 급등 시 3종목이 하나의 테마로 묶이는지 `/api/home` 또는 홈(/) 확인. (장 마감 후에는 stock_quotes 신선도 필터로 급등 0건일 수 있음 — Task 2 fixture 단위 검증이 1차 증거, 프로덕션은 다음 거래일 관찰.)
    4. Claude 호출 횟수·비용 회귀 없음 확인 (호출 횟수 불변, 입력 토큰만 참고 섹션 라인 수만큼 소폭 증가).
  </how-to-verify>
  <resume-signal>"approved" 또는 이슈 설명</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm -C workers/home-sync test` — 신규(loadThemeHints, prompt 섹션, clusterSurges 전달, index 배선) + 기존 65개 전부 통과.
2. `pnpm -C workers/home-sync typecheck` — exit 0.
3. 배포 후 `bash scripts/smoke-home-sync.sh` 통과.
4. 다음 거래일 장중 동일 테마 동반 급등 케이스에서 singles 대신 테마 묶임 확인.
</verification>

<success_criteria>
- 급등 종목 2+ 가 공유하는 네이버 테마가 Claude 프롬프트에 참고 섹션으로 전달된다.
- 뉴스 공백 시에도 사료 3종목 케이스가 하나의 테마로 묶인다 (few-shot + 규칙으로 유도).
- 단독 소속 테마는 제외되어 노이즈/토큰 최소화.
- anti-hallucination 유지 (뉴스 우선, 근거 없으면 '동일 테마 소속 동반 급등' 명시, 사실 지어내기 금지).
- 기존 테스트 회귀 0, content hash·Claude 호출 횟수 불변.
</success_criteria>

<output>
After completion, create `.planning/quick/260720-in0-home-sync-singles/260720-in0-SUMMARY.md`
</output>
