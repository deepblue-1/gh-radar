---
phase: quick-260923-pq2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/relay.ts
  - packages/shared/src/index.ts
  - relay/src/dma/envelope.ts
  - relay/src/store/gateway-symbols.ts
  - relay/src/ws/fanout.ts
  - relay/src/index.ts
  - relay/tests/gateway-symbols.test.ts
  - relay/tests/fanout.test.ts
  - webapp/src/lib/use-relay-socket.ts
  - webapp/src/lib/relay-provider.tsx
  - webapp/src/lib/exchange-choices.ts
  - webapp/src/lib/__tests__/exchange-choices.test.ts
  - webapp/src/lib/__tests__/relay-socket.test.ts
  - webapp/src/components/trading/card/card-header.tsx
  - webapp/src/components/trading/card/strategy-card.tsx
  - webapp/src/components/stock/stock-orderbook-section.tsx
  - webapp/src/components/trading/__tests__/card-header.test.tsx
  - webapp/src/components/trading/__tests__/strategy-card.test.tsx
  - webapp/src/components/stock/__tests__/stock-orderbook-section.test.tsx
autonomous: true
requirements: [PQ2-A, PQ2-B, PQ2-C]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "relay 가 게이트웨이 57 종목마스터의 `nxt_tradable` 을 버리지 않고 ISIN 집합으로 보관하며, 적재 전에는 `null`(모름)이다 — Supabase `stocks` 에는 NXT 정보가 없으므로 57 이 유일한 원천 (PQ2-A · A-1 · A-2)"
    - "인증 직후 스냅샷 묶음 끝에 `{t:\"nxt.snap\", isins:[…]}` 1프레임이 **집합이 적재돼 있을 때만** 나가고(적재 전엔 0프레임 — `lc.snap` 게이트와 같은 「모름은 안 보낸다」), 일일 재적재(`updated`)가 끝나면 인증된 모든 연결에 다시 나간다 (PQ2-A · A-3)"
    - "`SymbolInfo` 에는 플래그가 들어가지 않는다 — Supabase 원천 행에 값이 없어 `false` 로 지어내게 되므로(T-16-05). `relay/src/store/symbols.ts` 는 diff 0 (PQ2-A · A-1)"
    - "웹앱 상태 `nxtTradable` 은 초기 `null`(모름)이고 `nxt.snap` 마다 새 Set 으로 통째 교체된다. 프레임이 안 오는 구 relay·e2e 스텁 게이트웨이에서는 `null` 그대로라 KRX|NXT 둘 다 그린다 — 하위호환 (PQ2-B · B-1)"
    - "카드 헤더와 종목상세 호가 상태줄의 거래소 세그먼트가 같은 순수 함수 `exchangeChoicesOf(isin, exchange, nxtTradable)` 로 선택지를 뽑는다: 모름 → 둘 다 · 집합에 있음 → 둘 다 · 집합에 없음 → 「KRX」 라벨 하나(같은 자리·같은 높이 · 토글 아님) · 단 현재 거래소가 이미 NXT 면 둘 다(되돌아갈 수 있게) (PQ2-B · B-2 · B-3)"
    - "`isNxtEmpty` 휴리스틱(D3) · VI 설정 2줄의 KRX/NXT · `exchangeLocked` 계약 · 카드 거래소 자동 전환 없음 — 전부 그대로 (PQ2-B)"
    - "게이트: config `build_command` 4단 0 에러 · `test_command`(relay + webapp) 전량 초록 · Playwright `trading-workbench` spec 0 fail(기존 세그먼트 단언은 57 없는 스텁 게이트웨이라 둘 다 그려야 한다 — 회귀 확인) · `pnpm-lock.yaml` · `supabase/` · `relay/src/generated/` diff 0 (PQ2-C)"
    - "커밋 2건(relay+shared 1 · webapp 1 — 배포 단위 분리) · 한글 · Co-Authored-By 없음 · `.planning` 미커밋 · push 없음. SUMMARY 에 배포 순서(relay 먼저 → 검증 → webapp) 명기 (PQ2-C)"
  artifacts:
    - path: "packages/shared/src/relay.ts"
      provides: "`RelayNxtSnapMsg = { t: \"nxt.snap\"; isins: string[] }` + `RelayOutbound` 유니온 원소"
      contains: "RelayNxtSnapMsg"
    - path: "relay/src/store/gateway-symbols.ts"
      provides: "`nxtTradableIsins(): readonly string[] | null` · `stats().nxtTradableCount` · 일일 재적재 시 통째 교체"
      contains: "nxtTradableIsins"
    - path: "relay/src/ws/fanout.ts"
      provides: "`WsFanoutDeps.nxtTradable?: NxtTradableFeed` · 인증 직후 조건부 1프레임 · `updated` 재전송 · `closeAll` 에서 리스너 해제"
      contains: "nxt.snap"
    - path: "webapp/src/lib/exchange-choices.ts"
      provides: "`exchangeChoicesOf` 순수 함수 · `EXCHANGE_CHOICES_ALL` · `KRX_ONLY_TITLE`"
      contains: "export function exchangeChoicesOf"
    - path: "webapp/src/lib/use-relay-socket.ts"
      provides: "`RelayConnectionState.nxtTradable: ReadonlySet<string> | null` · `case \"nxt.snap\"` 리듀스"
      contains: "nxt.snap"
    - path: "webapp/src/components/trading/card/card-header.tsx"
      provides: "`exchangeChoices` prop · 단일 선택지 라벨 변형(`data-single=\"true\"`)"
      contains: "data-single"
    - path: "webapp/src/components/stock/stock-orderbook-section.tsx"
      provides: "상태줄 세그먼트에 같은 순수 함수 적용 · 단일 라벨 변형"
      contains: "exchangeChoicesOf"
  key_links:
    - from: "relay/src/dma/envelope.ts `parseSymbolMasterFrame` 루프"
      to: "`GatewaySymbolRow.nxtTradable`"
      via: "`item.nxtTradable()` (생성 코드 `symbol-master-item.ts` 접근자)"
    - from: "relay/src/store/gateway-symbols.ts `onFrame` 원자 교체"
      to: "`WsFanout` (`nxtTradable` dep)"
      via: "`emit(\"updated\")` → fanout 이 `nxtTradableIsins()` 를 읽어 인증된 모든 연결에 `nxt.snap`"
    - from: "relay/src/index.ts `new WsFanout({...})`"
      to: "`gatewaySymbols`"
      via: "`nxtTradable: gatewaySymbols` 결선 — 빠지면 프레임이 영원히 안 나가고 웹앱은 둘 다 그린다(조용한 퇴행)"
    - from: "webapp `useRelayContext().nxtTradable`"
      to: "`CardHeader.exchangeChoices` · `OrderbookStatusBar.exchangeChoices`"
      via: "`strategy-card.tsx` · `stock-orderbook-section.tsx` 가 각각 `exchangeChoicesOf(isin, exchange, nxtTradable)` 로 계산해 내린다"
---

<!-- planner-discipline-allow: quick-260923-pq2 -->
<!-- 위 리터럴은 커밋 태그다. Task 1·2·3 의 음성 grep 은 소스가 아니라 `git log --format=%s -- <금지 경로>` 출력에서 이 태그를 찾는다(= 이 quick 의 커밋이 금지 경로를 건드리지 않았다는 증거). 커밋 메시지·주석에는 반드시 들어가야 하므로 allowlist 한다. -->

<objective>
NXT 에 상장되지 않은 종목은 카드 거래소 세그먼트(그리고 종목상세 호가 상태줄)에서 NXT 를 숨긴다. relay 가 게이트웨이 종목마스터(57)의 `nxt_tradable` 을 보존해 ISIN 집합을 `nxt.snap` 프레임으로 내리고, 웹앱은 그 집합으로 선택지를 뽑되 **모르면(프레임 없음) 둘 다** 그린다.

Purpose: 사용자 요청(2026-09-23) 「NXT 에 상장 안 된 종목은 굳이 나올 필요 없다」. 지금은 57 에 플래그가 실려 오는데 relay 파싱이 버리고, Supabase `stocks` 에는 NXT 정보가 없어 웹앱이 판정할 근거가 없다. 검색으로 추가한 카드(REST `searchStocks`)도 있어 항목별 보강(name/code 선례)으로는 부족하므로 **집합 한 번 내리기**가 가장 작다.

Output: shared 계약 `RelayNxtSnapMsg` · relay 57 파싱 플래그 보존 + 스토어 집합(`null` = 모름) + fanout 조건부 스냅샷/재적재 재전송 · 웹앱 리듀서 `nxtTradable` + 순수 함수 `exchangeChoicesOf` + 두 표면(카드 헤더 · 호가 상태줄) 단일 라벨 변형 · vitest(relay 2 · webapp 5 파일) · 게이트 + Playwright 회귀. 설계는 오케스트레이터가 잠갔다(`<design_decisions_locked>` A-1~A-4 · B-1~B-4) — 대안 검토 · Supabase 스키마/마스터 변경 · 서버(gh-trade) 변경 · `SymbolInfo` 플래그 삽입 · 카드 거래소 자동 전환 · VI 설정 2줄 변경은 이 quick 밖이다.

**MUTABLE-SCOPE — 순차 실행 전제:** 계획 시점 HEAD `6ae9f3e`(p3k 완료). 같은 저장소에서 quick-260923-pgu(알림/VI 탭) · quick-260923-pgv(거래소 토글 D-10 — `exchangeLocked` 를 없앨 수 있다)가 **먼저** 돈다. 이 플랜은 그 뒤에 실행한다. 아래는 줄 번호가 아니라 **함수·타입 이름**으로 지점을 가리키고, `exchangeLocked` prop 의 존재·부재에 **의존하지 않는다**(있으면 그대로 두고, 없으면 없는 대로). 실행 전 `git status -sb` 로 working tree 가 깨끗한지 확인하고 아니면 멈춘다.

**배포 주의(SUMMARY 에 반드시 명기):** relay 미배포 변경이 이미 쌓여 있다(R2~R4 + quick 여러 건). 이 quick 은 그 위에 얹힌다. 웹앱은 프레임이 안 오면 둘 다 그리는 하위호환이므로 **relay 먼저 → 검증 → webapp** 순서면 안전하다. push 는 하지 않는다(사용자 결정 — 이 저장소에서 push 는 곧 webapp 프로덕션 배포다).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

**앵커 지도 (플래너가 읽었다 — 파일 전체를 다시 읽지 말고 이름으로 Grep 해 그 구간만 연다).**

relay/src/generated/StockDMA.fbs:930 `nxt_tradable: bool` — 「NXT 거래가능 여부. 서버가 NXT A0 수신으로 판정한다 (D-12)」. 생성 접근자 `relay/src/generated/stock-dma/symbol-master-item.ts` `nxtTradable(): boolean`. **생성 코드 수정 없음.**

relay/src/dma/envelope.ts
- `export type GatewaySymbolRow = { isin; code; name; market: OrderMarket | null }` — 바로 위 주석 「표시·주문에 필요한 최소 필드만 담는다(sec_group_id·nxt_tradable 제외)」.
- `export function parseSymbolMasterFrame(env)` — 원소 루프 끝 `rows.push({ isin, code, name, market: fromWireMasterMarketType(item.marketType() ?? "") })`.

relay/src/store/gateway-symbols.ts (EventEmitter · `SymbolLookup`)
- 머리 주석 「하지 않는 것: 57 을 브라우저로 흘리지 않는다(공개 마스터 — relay 이름 해석에만 쓴다)」 — 이번에 예외가 생긴다(NXT ISIN 집합만).
- `type Inflight = { userId; startedAt; nextSeq; totalItems; rawCount; skipped; frames; rows: Map<string, SymbolInfo> }`.
- 필드 `#byIsin = new Map<string, SymbolInfo>()` · `#loadedDayKey` · `#loadedAt` · `#inflight`.
- `lookup(isin)` · `stats(): { symbolCount; loadedAt; dayKey; inflight }` (테스트는 `toMatchObject` 로만 본다).
- `onFrame(userId, frame)` — 원소 루프 `for (const row of frame.rows) inflight.rows.set(row.isin, { code, name, market, source: "gateway" })` → `is_last` 검증 → 원자 교체 `this.#byIsin = inflight.rows` → `this.emit("updated", { count })`.
- `#tryRequest` 가 `this.#inflight = { …, rows: new Map() }` 를 세운다.

relay/src/store/symbols.ts — `SymbolInfo = { code; name; market; source?: "gateway" }` · `SymbolMap.lookup` = `#byIsin.get ?? #fallback?.lookup`. **수정 없음.**

relay/src/ws/fanout.ts
- `export type WsFanoutDeps = { server?; supabase; sessions; hub; credKey; path?; authTimeoutMs?; heartbeatMs?; backpressureLimitBytes?; orderStore?; symbols?; orderTimeoutMs? }`.
- `type Conn` · `type UserEntry = { session; conns: Set<Conn>; onState }` · 필드 `#conns`(미인증 포함) · `#users: Map<string, UserEntry>`(인증+자격증명 있는 사용자만 — 「여기 없는 사용자에게는 아무것도 가지 않는다 (T-15-02)」).
- `constructor(deps)` — 끝에서 `this.#hub.on("fanout", …)` · `#orders` 조립 · `#heartbeat` 설정.
- `async #onFirstMessage(conn, msg)` — 인증 성공 갈래 끝: `session.stateFrame()` → `acct` 전량 → `lc.snap`(`hasLimitChaserList` 게이트) → `vi` 거래소별(3상태) → `vi.list`(비어도 1프레임) → `rate.cross.snap`(비어도 1프레임) → `queued.window`(모르면 안 보냄). 여기 **맨 끝**에 `nxt.snap` 을 붙인다.
- `#deliver(userId, msg)` — 사용자 소켓 집합에만. `#send(conn, msg)` — 백프레셔 + `encode`.
- `async closeAll(code?, reason, graceMs)` — `clearInterval(this.#heartbeat)` · `#server?.removeListener("upgrade", …)` · … · `this.#orders?.close()`. `close()` = `closeAll()`.

relay/src/hub/subscription-hub.ts `export interface HubSymbolMasterFeed { onSessionReady; onFrame }` — 구조적 타입 선례. fanout 도 같은 식으로 **최소 표면 타입**을 둔다(`GatewaySymbolMaster` 를 import 하지 않는다).

relay/src/index.ts — `const gatewaySymbols = new GatewaySymbolMaster({ pickSession })` · `gatewaySymbols.on("updated", () => hub.refreshNames())` · `const fanout = new WsFanout({ supabase, sessions: sessionManager, hub, credKey, orderStore, symbols })`.

packages/shared/src/relay.ts — `export type RelayRateCrossSnapMsg = { t: "rate.cross.snap"; items: RelayRateCrossItem[] }` · `export type RelayQueuedWindowMsg` · `export type RelayOutbound = | RelayStateMsg | … | RelayRateCrossSnapMsg | RelayQueuedWindowMsg`. packages/shared/src/index.ts — 명시 export 목록에 `RelayRateCrossSnapMsg, RelayQueuedWindowMsg,` 가 있는 `} from "./relay"` 블록. `relay/src/ws/protocol.ts` 의 아웃바운드는 `encode(msg)`(JSON.stringify + bigint 가드)뿐 — zod 없음, **수정 없음**.

relay/tests/helpers/frames.ts — `FakeSymbolMasterItem = { isin?; code?; name?; marketType?; secGroupId?; nxtTradable? }` · `buildSymbolMasterFrame({ items, seq, totalItems, isLast })` 가 이미 `addNxtTradable(b, item.nxtTradable ?? false)` 를 싣는다(빌더 수정 없음).
relay/tests/gateway-symbols.test.ts — 헬퍼 `isinOf(n)` · `codeOf(n)` · `mkItems(n, offset)` · `parse57(payload)` · `class FakeSession`(`emitReady` · `pushFrame`) · `T0` · `DAY_MS` · 케이스 ①~⑫(④ 「원소 가드」 가 `parse57` 행 단언 + `gw.lookup(...).toEqual({ code, name, market, source })` 선례).
relay/tests/fanout.test.ts — `startHarness(overrides: Partial<WsFanoutDeps>)`(선례 `startHarness({ backpressureLimitBytes: -1 })`) · `open()` · `authed(token)` · `framesOf(inbox, t)` · `flushIo` · `waitFor` · 케이스 ⑭-2(「rate.cross.snap 은 비어 있어도 1프레임 — queued.window 는 모르면 안 온다」 — 새 탭 `open()` + `sendAuth("token-a")` + `waitFor(vi.list 1건)` + `flushIo(20)` 패턴). 인증 프레임 순서를 배열 정확일치로 단언하는 케이스는 없다.
relay/tests/helpers/fake-gateway.ts — 27(`GetSymbolMasterReq`)에 응답하지 않는다 → e2e 로컬 relay 는 57 을 못 받아 집합이 `null` → `nxt.snap` 0프레임.

webapp/src/lib/use-relay-socket.ts
- `export interface RelayConnectionState { … rateCrossItems; rateCrossSnapSeq; limitChaserSnapSeq; queuedWindow: RelayQueuedWindowMsg | undefined; send; reconnect; subscribe; unsubscribe; sendOrder }`.
- `type RelayData = { … queuedWindow }` · `const INITIAL_DATA: RelayData = { …, queuedWindow: undefined }`.
- `function relayReducer` — `"reset"` → `INITIAL_DATA`(로그아웃·비활성화). 재접속(`local-status` reconnecting)은 데이터를 지우지 않는다 — `rateCrossItems` 도 같다.
- `function applyFrame(state, frame, at)` — `case "rate.cross.snap"` · `case "queued.window"` · `case "strategies.disabled"` · `default`(알 수 없는 `t` 무시 T-15-41).
- `useRelayConnection` 끝 `useMemo(() => ({ status, …, queuedWindow: data.queuedWindow, send, … }))`.
- `parseFrame` 은 `t` 가 문자열이면 통과 — 화이트리스트 없음.

webapp/src/lib/relay-provider.tsx — `RelayContextValue = Omit<RelayConnectionState, "sendOrder"> & { sendOrder; orderLocks }`(새 필드 자동 포함) · `EMPTY_RELAY_VALUE` 객체(주석 달린 필드 나열 — `queuedWindow: undefined,` 다음에 추가) · `RelayProvider` 의 `value = { ...connection, sendOrder, orderLocks }`.

webapp/src/components/trading/card/card-header.tsx
- 머리 주석 ①~④ · `const EXCHANGES: readonly RelayExchange[] = ["KRX", "NXT"]` · `export interface CardHeaderProps { name; nameTitle?; code; exchange; onExchangeChange; exchangeLocked; price; changeRate; ledServer; onArm; open; onToggle; toggleId; controlsId; onInfo?; onClose; unfilledCount?; holdingQty? }` · `function stop(event)`.
- 세그먼트: `<span onClick={stop} className="flex flex-none"><ToggleGroup type="single" value={exchange} … aria-label="거래소" data-slot="card-exchange-segment" className="h-5 gap-0 overflow-hidden rounded-[var(--r-sm)] border border-[var(--border)]">{EXCHANGES.map((ex) => <ToggleGroupItem … className="h-5 min-w-0 rounded-none bg-[var(--card)] px-1.5 text-[10px] font-bold tracking-[0.02em] text-[var(--muted-fg)] not-first:border-l not-first:border-[var(--border)] data-[state=on]:bg-[var(--accent)] data-[state=on]:text-[var(--accent-fg)]">{ex}</ToggleGroupItem>)}</ToggleGroup></span>`.

webapp/src/components/trading/card/strategy-card.tsx — `export function StrategyCard(props)` 안 `const { accountStates, status } = useRelayContext();`(④ 카드 계좌 슬라이스 주석 아래) · `<CardHeader name={displayName} … exchange={exchange} onExchangeChange={handleExchange} … />`. 훅 `useStrategyCardState` 는 건드리지 않는다.

webapp/src/components/stock/stock-orderbook-section.tsx
- `const EXCHANGES: readonly RelayExchange[] = ['KRX', 'NXT'];`(모듈 상수) · `const { queuedWindow } = useRelayContext();` · `const isNxtEmpty = …`(D3 · **그대로**) · `<OrderbookStatusBar status … exchange={exchange} onExchangeChange={handleExchangeChange} card={card} windowBadge={…} onReconnect={…} />`.
- `function OrderbookStatusBar({ status, statusLabel, accounts, accountNo, onAccountChange, exchange, onExchangeChange, card, windowBadge, onReconnect })` — `<ToggleGroup type="single" value={exchange} … disabled={status !== 'ready'} aria-label="거래소" data-slot="orderbook-exchange-segment" className="h-5 gap-0 overflow-hidden rounded-[var(--r-sm)] border border-[var(--border)]">{EXCHANGES.map(…)}</ToggleGroup>`(항목 클래스는 카드 헤더와 동일).

webapp 테스트
- `webapp/src/lib/__tests__/relay-socket.test.ts` — fake `WebSocket` 로 프레임 주입(`renderHook(useRelayConnection)`), describe 「rate.cross 순서 …」(`t: 'rate.cross.snap'` 주입 선례) · 「정리 (cleanup)」(`enabled` false 재렌더 = reset 선례). ⚠️ 경로: `src/**/*.test.{ts,tsx}` 만 실행된다.
- `webapp/src/lib/__tests__/` 에 순수 함수 테스트를 두면 된다(`format.test.ts` 처럼 `src/lib/` 직속도 가능).
- `webapp/src/components/trading/__tests__/card-header.test.tsx` — `renderHeader(over: Partial<CardHeaderProps>)` · `exchangeGroup = () => screen.getByRole('group', { name: '거래소' })` · `exchangeButtons = () => within(exchangeGroup()).getAllByRole('radio')` · 첫 케이스가 `const [krx, nxt] = exchangeButtons()` 로 둘 다를 잠근다(무수정 초록 목표).
- `webapp/src/components/trading/__tests__/strategy-card.test.tsx` — `relay(over)` = `{ ...EMPTY_RELAY_VALUE, status:'ready', accounts, subscribe, unsubscribe, send, ...over }` · `baseProps`(exchange 'KRX' · isin 은 케이스가 준다) · `RelayContext.Provider value={relay(…)}` 로 실물 `StrategyCard` 를 그린다.
- `webapp/src/components/stock/__tests__/stock-orderbook-section.test.tsx` — `vi.mock('@/lib/relay-provider')` 로 `useRelayContext: () => ctx` · `beforeEach` 의 `ctx = { ...EMPTY_RELAY_VALUE, status:'ready', … }` · `statusBar()` · 케이스 ③(`within(seg).getByRole('radio', { name: 'NXT' })`) · ③-c.
- `webapp/e2e/specs/trading-workbench.spec.ts` 케이스 3 `card.locator('[data-slot="card-exchange-segment"]').getByRole('radio', { name: 'KRX' })` — 스텁 게이트웨이에 57 이 없으니 둘 다 그려야 통과한다(회귀 확인만 · spec 수정 없음).

게이트 명령 원문(config): `build_command` = `pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/webapp run typecheck` · `test_command` = `pnpm --filter @gh-radar/relay run test && pnpm --filter @gh-radar/webapp run test`. Playwright: `cd webapp && pnpm exec playwright test trading-workbench`(relay 는 spec 픽스처가 8090 에 스스로 띄운다 · dev 서버 3100 은 `webServer` 가 재사용/기동 — 260923-p3k 플랜과 동일).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: relay + shared — 57 `nxt_tradable` 보존 → ISIN 집합(`null` = 모름) → `nxt.snap` 조건부 스냅샷·재적재 재전송</name>
  <files>packages/shared/src/relay.ts, packages/shared/src/index.ts, relay/src/dma/envelope.ts, relay/src/store/gateway-symbols.ts, relay/src/ws/fanout.ts, relay/src/index.ts, relay/tests/gateway-symbols.test.ts, relay/tests/fanout.test.ts</files>
  <read_first>relay/src/dma/envelope.ts (`GatewaySymbolRow` · `parseSymbolMasterFrame` 원소 루프 — Grep), relay/src/store/gateway-symbols.ts (머리 주석 「하지 않는 것」 · `Inflight` · 필드 · `stats` · `onFrame` 원자 교체 · `#tryRequest` 의 inflight 생성), relay/src/ws/fanout.ts (`WsFanoutDeps` · `constructor` 끝 · `#onFirstMessage` 인증 성공 갈래 끝 `queued.window` 송신 · `#users` · `closeAll`), relay/src/index.ts (`new WsFanout({` 블록), packages/shared/src/relay.ts (`RelayRateCrossSnapMsg` · `RelayOutbound`), packages/shared/src/index.ts (`RelayQueuedWindowMsg,` 가 든 export 블록), relay/tests/gateway-symbols.test.ts (헬퍼 구간 + 케이스 ④ · ② 패턴), relay/tests/fanout.test.ts (`startHarness` · `authed` · `open` · `framesOf` · 케이스 ⑭-2)</read_first>
  <behavior>
    - relay/tests/gateway-symbols.test.ts — 새 케이스 「⑬ nxt_tradable 보존 (quick-260923-pq2) — 파싱 행 플래그 · 스토어 ISIN 집합 · 적재 전 null · 재적재 시 통째 교체 · SymbolInfo 에는 없음」:
      - 원소 5건(`mkItems(5)` 에 `nxtTradable: true` 를 2건(예: 101 · 103)만 얹는다) → `parse57` 행의 `nxtTradable` 이 그 2건만 `true`, 나머지 `false`.
      - `new GatewaySymbolMaster({ now })` 적재 전: `nxtTradableIsins()` 가 `null` · `stats().nxtTradableCount` 가 `0`.
      - Ready → 프레임 적재 후: `nxtTradableIsins()` 가 `[isinOf(101), isinOf(103)]`(57 원순서) · `stats().nxtTradableCount` 2 · `lookup(isinOf(101))` 이 `toEqual({ code, name, market: "K", source: "gateway" })` — 플래그 키가 **없다**(A-1).
      - `t += DAY_MS` 로 다음 master-day 로 넘긴 뒤 Ready → 다른 집합(예: 104 만 `true`)으로 재적재 → `nxtTradableIsins()` 가 `[isinOf(104)]` 로 **통째 교체**(101·103 없음) · `updated` 이벤트가 두 번째도 1회.
      - 빈 마스터 실패(⑦ 선례)나 seq-gap 실패 뒤에도 기존 집합이 유지된다(실패 갈래는 집합을 건드리지 않는다) — 1단언.
    - relay/tests/fanout.test.ts — 새 케이스 「⑭-4 nxt.snap — 적재 전엔 0프레임, 적재 뒤 인증하면 스냅샷 묶음 끝에 1프레임, updated 시 인증된 모든 연결에 재전송, 미인증 소켓엔 없음, close 가 리스너를 뗀다 (quick-260923-pq2)」:
      - 스텁 `class StubNxtFeed extends EventEmitter { isins: readonly string[] | null = null; nxtTradableIsins() { return this.isins; } }` 를 `startHarness({ nxtTradable: feed })` 로 주입한 **별도 하네스**(⑦ 백프레셔 케이스의 `strict` 선례 · 케이스 끝에서 close). 기본 `h` 는 건드리지 않는다.
      - (a) `feed.isins === null` 인 채 `authed("token-a")` → `flushIo(20)` → `framesOf(inbox, "nxt.snap")` 길이 0(`rate.cross.snap` 은 1 — 다른 규율임을 같은 케이스에서 대조).
      - (b) `feed.isins = [SAMPLE_ISIN]` → 새 탭 `open()` + `sendAuth("token-a")` + `waitFor(vi.list 1건)` + `flushIo(20)` → `nxt.snap` 정확히 1프레임 `{ t: "nxt.snap", isins: [SAMPLE_ISIN] }` 이고 inbox 안 인덱스가 `rate.cross.snap` 보다 **뒤**다.
      - (c) 미인증 소켓 1개를 `open()` 만 해 둔 채 `feed.isins = [SAMPLE_ISIN, OTHER_ISIN]; feed.emit("updated", { count: 2 })` → `flushIo(20)` → (a)의 탭과 (b)의 탭 **둘 다** 새 `nxt.snap`(isins 2건)을 1프레임씩 더 받고, 미인증 소켓 inbox 에는 `nxt.snap` 0.
      - (d) 하네스 `close()` 뒤 `feed.listenerCount("updated")` 가 0.
    - 기존 케이스 ①~⑫ · ⑭-2 · ⑭-3 은 **무수정 초록**(dep 미주입 하네스에서는 프레임이 나가지 않으므로 드롭 0 게이트도 그대로).
  </behavior>
  <action>
1. **shared 계약(A-3).** `packages/shared/src/relay.ts` 의 `RelayQueuedWindowMsg` 정의 **뒤**에 `export type RelayNxtSnapMsg = { t: "nxt.snap"; isins: string[] }` 를 추가한다. JSDoc: 「NXT 거래가능 종목 ISIN 집합 스냅샷(quick-260923-pq2). 원천은 게이트웨이 종목마스터 57 의 `nxt_tradable`(서버가 NXT A0 수신으로 판정 · fbs D-12) 하나다 — Supabase `stocks` 에는 NXT 정보가 없다. relay 는 집합이 **적재돼 있을 때만** 인증 직후 1프레임을 내리고(적재 전엔 보내지 않는다 — 브라우저는 「모름」으로 둘 다 그린다), 일일 재적재가 끝나면 인증된 모든 연결에 다시 내린다. 전량 교체 의미다(병합 아님). 사용자 데이터가 없는 공개 마스터 파생값이다.」 `RelayOutbound` 유니온 끝(`RelayQueuedWindowMsg` 뒤)에 `| RelayNxtSnapMsg` 를 더한다. `packages/shared/src/index.ts` 의 같은 export 블록 `RelayQueuedWindowMsg,` 다음 줄에 `RelayNxtSnapMsg,` 를 더한다.
2. **57 파싱 보존(A-1).** `relay/src/dma/envelope.ts` `GatewaySymbolRow` 에 `nxtTradable: boolean` 을 더하고 위 주석을 「표시·주문에 필요한 최소 필드 + NXT 거래가능 플래그(quick-260923-pq2 — 웹앱 세그먼트 판정의 유일한 원천). `sec_group_id` 는 여전히 제외」로 고친다. `parseSymbolMasterFrame` 의 `rows.push({...})` 에 `nxtTradable: item.nxtTradable()` 을 더한다(생성 접근자 — 기본값 `false`, 형식 가드 불필요).
3. **스토어 집합(A-2).** `relay/src/store/gateway-symbols.ts`: `Inflight` 에 `nxt: string[]` 을 더하고 `#tryRequest` 의 inflight 생성에 `nxt: []` 를 넣는다. 필드 `#nxtTradableIsins: readonly string[] | null = null` 을 `#byIsin` 옆에 둔다(JSDoc: 「NXT 거래가능 ISIN 집합. **적재 전엔 `null`(모름)** — `false` 로 지어내지 않는다(T-16-05). `#byIsin` 과 같은 적재 단위로 원자 교체된다」). `onFrame` 원소 루프에서 `if (row.nxtTradable) inflight.nxt.push(row.isin)`(57 원순서 유지 · `SymbolInfo` 에는 넣지 **않는다**). 원자 교체 지점(`this.#byIsin = inflight.rows` 직후)에 `this.#nxtTradableIsins = inflight.nxt`(한 번 만든 배열 참조를 그대로 보관 — 조회마다 복사하지 않는다). 실패 갈래(`#fail`)는 집합을 건드리지 않는다(기존 맵 유지 규율과 동일). 조회 `nxtTradableIsins(): readonly string[] | null { return this.#nxtTradableIsins; }`. `stats()` 반환 타입에 `nxtTradableCount: number` 를 더하고 `this.#nxtTradableIsins?.length ?? 0`(식별자 없음). 머리 주석 「57 을 브라우저로 흘리지 않는다」 항목을 「57 의 이름·코드는 브라우저로 흘리지 않는다. 예외는 NXT 거래가능 ISIN 집합(`nxt.snap`, quick-260923-pq2) 하나 — fanout 이 `nxtTradableIsins()` 와 `updated` 로 읽는다」로 고치고, 적재 로그(`[SYM-GW] 게이트웨이 종목마스터 적재`)의 필드에 `nxtTradableCount` 를 더한다.
4. **fanout(A-3).** `relay/src/ws/fanout.ts`: `WsFanoutDeps` 위에 최소 표면 타입 `export type NxtTradableFeed = { nxtTradableIsins(): readonly string[] | null; on(event: "updated", listener: () => void): unknown; off(event: "updated", listener: () => void): unknown }` 을 두고(`HubSymbolMasterFeed` 선례 — `GatewaySymbolMaster` 가 구조적으로 만족하고 테스트는 스텁을 넣는다), `WsFanoutDeps.nxtTradable?: NxtTradableFeed` 를 더한다(JSDoc: 「NXT 거래가능 ISIN 집합 원천(quick-260923-pq2). 없으면 `nxt.snap` 을 내리지 않고 브라우저는 둘 다 그린다」). 필드 `readonly #nxtTradable: NxtTradableFeed | null` 과 `readonly #onNxtUpdated: () => void` 를 두고 constructor 끝에서 `this.#nxtTradable = deps.nxtTradable ?? null; this.#onNxtUpdated = () => this.#broadcastNxtSnap(); this.#nxtTradable?.on("updated", this.#onNxtUpdated)`. `closeAll` 의 `clearInterval(this.#heartbeat)` 옆에서 `this.#nxtTradable?.off("updated", this.#onNxtUpdated)`.
   - `#nxtSnapFrame(): RelayNxtSnapMsg | null` — dep 없음 또는 `nxtTradableIsins()` 가 `null` 이면 `null`, 아니면 `{ t: "nxt.snap", isins: [...isins] }`(전송용 복사 1회).
   - `#onFirstMessage` 인증 성공 갈래의 `queued.window` 송신 **바로 뒤**에 `const nxtSnap = this.#nxtSnapFrame(); if (nxtSnap !== null) this.#send(conn, nxtSnap);` 를 붙이고 주석을 단다: 「⚠️ `nxt.snap` 은 `lc.snap` 과 같다 — 집합이 **적재돼 있을 때만** 보낸다. 적재 전(콜드 부팅 · 57 미수신)의 「모름」을 빈 배열로 내리면 브라우저가 모든 종목에서 NXT 를 지우는 거짓 확정이 된다. `vi.list`·`rate.cross.snap` 의 「비어도 보낸다」와 **다르다** — 저쪽 빈 배열은 확정 정보다. 적재가 뒤에 끝나면 `updated` 재전송이 이 연결의 첫 `nxt.snap` 이 된다.」
   - `#broadcastNxtSnap()` — 프레임을 **한 번** 만들어 `for (const entry of this.#users.values()) for (const conn of entry.conns) this.#send(conn, frame)`. `null` 이면 아무것도 하지 않는다. 주석: 「전 사용자 순회는 여기뿐이다 — 내리는 값이 사용자 데이터가 아니라 공개 마스터 파생 집합이라 T-15-02(사용자 격리)와 무관하다. `#users` 만 돌므로 미인증·자격증명 미등록 연결에는 가지 않는다. 크기 ≈ NXT 종목 수백~천 개 × 12자 ≈ 10~15KB, 연결당 하루 1회 — 백프레셔 규율은 `#send` 가 쥔다.」
5. **결선.** `relay/src/index.ts` `new WsFanout({ … symbols, })` 에 `nxtTradable: gatewaySymbols,` 를 더하고 한 줄 주석 「NXT 거래가능 집합 → `nxt.snap`(quick-260923-pq2). 빠지면 프레임이 안 나가고 웹앱은 둘 다 그린다(조용한 퇴행) — 결선 grep 게이트가 잡는다」.
6. **TDD.** `<behavior>` 의 두 케이스를 먼저 쓴다(타입이 없으니 `typecheck:tests` 실패 = RED) → 1~5 구현 → GREEN. 기존 케이스는 무수정 초록.
7. 게이트: shared build · relay typecheck · typecheck:tests · relay test 전량. 커밋 **정확히 1건**(한글 · `feat(quick-260923-pq2): relay 57 nxt_tradable 보존 → nxt.snap 프레임 (shared 계약 · 스토어 ISIN 집합 · 인증 직후 조건부 스냅샷 · 재적재 재전송)` · 파일 8개만 지정 · Co-Authored-By 금지 · `git add -A` 금지 · `.planning` 미커밋 · push 금지).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/relay run test && test "$(grep -c 'RelayNxtSnapMsg' packages/shared/src/relay.ts)" -ge 2 && test "$(grep -c 'RelayNxtSnapMsg' packages/shared/src/index.ts)" -ge 1 && test "$(grep -c 'nxtTradable: item.nxtTradable()' relay/src/dma/envelope.ts)" -ge 1 && test "$(grep -c 'nxtTradableIsins()' relay/src/store/gateway-symbols.ts)" -ge 1 && test "$(grep -v '^ *[/*]' relay/src/ws/fanout.ts | grep -c '"nxt.snap"')" -ge 1 && test "$(grep -c 'nxtTradable: gatewaySymbols' relay/src/index.ts)" -ge 1 && test "$(grep -c 'nxt.snap' relay/tests/fanout.test.ts)" -ge 3 && test "$(grep -c 'nxtTradableIsins' relay/tests/gateway-symbols.test.ts)" -ge 3 && git diff --quiet -- relay/src/store/symbols.ts relay/src/generated relay/src/ws/protocol.ts pnpm-lock.yaml && git diff --cached --quiet -- relay/src/store/symbols.ts relay/src/generated relay/src/ws/protocol.ts pnpm-lock.yaml && L=$(git log --format=%s -- relay/src/store/symbols.ts relay/src/generated relay/src/ws/protocol.ts pnpm-lock.yaml) && test "$(printf '%s\n' "$L" | grep -c 'quick-260923-pq2')" -eq 0</automated>
  </verify>
  <done>57 파싱 행에 `nxtTradable` 이 보존되고, `GatewaySymbolMaster` 가 ISIN 집합을 `null`(적재 전)/배열(적재 후 · 재적재 시 통째 교체)로 노출하며 `SymbolInfo` 에는 들어가지 않는다(`symbols.ts` diff 0). `WsFanout` 이 인증 직후 스냅샷 묶음 끝에 집합이 있을 때만 `nxt.snap` 1프레임을 보내고 `updated` 에 인증된 모든 연결로 재전송하며 `closeAll` 이 리스너를 뗀다. `index.ts` 결선 1줄. shared 에 `RelayNxtSnapMsg` 가 유니온·index export 에 있다. gateway-symbols ⑬ · fanout ⑭-4 초록, 기존 케이스 무수정 초록. 생성 코드·protocol·lockfile diff 0. 커밋 1건, push 없음.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: webapp — `nxtTradable` 리듀서 · 순수 함수 `exchangeChoicesOf` · 카드 헤더 + 호가 상태줄 단일 라벨 변형</name>
  <files>webapp/src/lib/use-relay-socket.ts, webapp/src/lib/relay-provider.tsx, webapp/src/lib/exchange-choices.ts, webapp/src/lib/__tests__/exchange-choices.test.ts, webapp/src/lib/__tests__/relay-socket.test.ts, webapp/src/components/trading/card/card-header.tsx, webapp/src/components/trading/card/strategy-card.tsx, webapp/src/components/stock/stock-orderbook-section.tsx, webapp/src/components/trading/__tests__/card-header.test.tsx, webapp/src/components/trading/__tests__/strategy-card.test.tsx, webapp/src/components/stock/__tests__/stock-orderbook-section.test.tsx</files>
  <read_first>webapp/src/lib/use-relay-socket.ts (`RelayConnectionState` 의 `queuedWindow` 필드 · `RelayData` · `INITIAL_DATA` · `applyFrame` 의 `case "queued.window"` · `useMemo` 반환 객체 — Grep), webapp/src/lib/relay-provider.tsx (`EMPTY_RELAY_VALUE` 의 `queuedWindow: undefined,`), webapp/src/components/trading/card/card-header.tsx (머리 주석 ② · `EXCHANGES` · `CardHeaderProps` · 세그먼트 JSX), webapp/src/components/trading/card/strategy-card.tsx (`StrategyCard` 안 `useRelayContext()` 구조분해 · `<CardHeader` JSX), webapp/src/components/stock/stock-orderbook-section.tsx (`EXCHANGES` · `useRelayContext()` · `isNxtEmpty` · `<OrderbookStatusBar` 호출 · `function OrderbookStatusBar` 세그먼트 JSX), webapp/src/lib/__tests__/relay-socket.test.ts (「rate.cross 순서」 · 「정리 (cleanup)」 describe), webapp/src/components/trading/__tests__/card-header.test.tsx (`renderHeader` · `exchangeGroup` · 첫 두 케이스), webapp/src/components/trading/__tests__/strategy-card.test.tsx (`relay()` · `baseProps` · 첫 케이스), webapp/src/components/stock/__tests__/stock-orderbook-section.test.tsx (`beforeEach` ctx · 케이스 ③)</read_first>
  <behavior>
    - `webapp/src/lib/__tests__/exchange-choices.test.ts`(새 파일 · describe 「exchangeChoicesOf — 거래소 선택지 (quick-260923-pq2)」):
      - 모름(`null` · `undefined`) → `['KRX','NXT']` 이고 반환 참조가 `EXCHANGE_CHOICES_ALL` 과 `toBe` 로 같다(안정 참조).
      - 집합에 이 ISIN 있음 → 둘 다. 집합에 없음 + 현재 `'KRX'` → `['KRX']`(`toBe(EXCHANGE_CHOICES_KRX_ONLY)`). 집합에 없음 + 현재 `'NXT'` → 둘 다(되돌아갈 수 있게 · B-2 예외). 빈 ISIN(`''`) → 둘 다. 빈 집합(`new Set()`) + KRX → `['KRX']`(빈 집합은 「NXT 거래 종목 없음」의 확정이지 모름이 아니다).
    - `relay-socket.test.ts` 새 describe 「nxt.snap — NXT 거래가능 집합 (quick-260923-pq2)」: 초기 `result.current.nxtTradable` 이 `null` → `{t:'nxt.snap', isins:[A,B]}` 주입 → `ReadonlySet` 이고 A·B 포함 → 두 번째 `{isins:[C]}` → C 만 있고 A 없음(통째 교체) → 재접속 경로(「재접속 규율」 describe 의 close 후 재연결 선례)에서는 값이 남아 있고(다른 스냅샷과 동일 · relay 가 재인증마다 다시 내린다) → `enabled:false` 재렌더(「정리」 선례 = `reset`) 뒤 `null`.
    - `card-header.test.tsx` 새 describe 「CardHeader — 거래소 선택지 (quick-260923-pq2)」: ① `exchangeChoices` 미지정 → radio 2개(기존 첫 케이스와 같은 모양 — 명시 1케이스) · ② `exchangeChoices: ['KRX','NXT']` → radio 2개 · ③ `exchangeChoices: ['KRX']` → `screen.queryByRole('group', { name: '거래소' })` 가 `null` · `document.querySelector('[data-slot="card-exchange-segment"][data-single="true"]')` 의 텍스트가 `'KRX'` · `title` 이 `KRX_ONLY_TITLE` · 클래스에 `h-5` 포함(같은 높이) · 그 요소 클릭 시 `onToggle` · `onExchangeChange` 둘 다 미호출(전파 차단 유지 ③).
    - `strategy-card.test.tsx` 새 케이스 「quick-260923-pq2 — 컨텍스트 nxtTradable 집합에 이 카드 ISIN 이 없으면 헤더 세그먼트가 KRX 라벨 하나, 있으면/모르면 둘 다」: `relay({ nxtTradable: new Set(['KR7000000000']) })` + `isin` 카드 → 카드 안에 `role="radio"` 0 · `[data-single="true"]` 1 · `relay({ nxtTradable: new Set([isin]) })` → radio 2 · `relay()`(EMPTY = null) → radio 2.
    - `stock-orderbook-section.test.tsx` 새 케이스 「③-d quick-260923-pq2 — nxtTradable 집합에 이 종목이 없으면 상태줄 세그먼트가 KRX 라벨 하나(NXT radio 없음), 있으면 둘 다」: `ctx.nxtTradable = new Set(['KR7000000000'])` → `within(statusBar()).queryByRole('radio', { name: 'NXT' })` null · `[data-slot="orderbook-exchange-segment"][data-single="true"]` 텍스트 'KRX' · `ctx.nxtTradable = new Set([ISIN])` → NXT radio 있음. 기존 ③ · ③-c(EMPTY = null)는 무수정 초록.
  </behavior>
  <action>
1. **리듀서(B-1).** `webapp/src/lib/use-relay-socket.ts`: `RelayConnectionState` 의 `queuedWindow` 필드 뒤에 `nxtTradable: ReadonlySet<string> | null` 을 더한다(JSDoc: 「NXT 거래가능 ISIN 집합 — **2상태**. `null` = relay 가 `nxt.snap` 을 아직 안 줬다(연결 전 · 구 relay · relay 가 57 을 못 받음) → 화면은 KRX|NXT 둘 다 그린다. Set = 확정 집합(빈 Set 도 확정). 프레임마다 **통째 교체**다. 원천은 게이트웨이 57 `nxt_tradable` 하나(quick-260923-pq2).」). `RelayData` 에 같은 필드, `INITIAL_DATA` 에 `nxtTradable: null`(주석 「아직 모른다 — 빈 Set 으로 위장하면 모든 종목의 NXT 가 사라진다」). `applyFrame` 에 `case "nxt.snap": return { ...state, nxtTradable: new Set(frame.isins) };`(주석 「전량 교체 · 재접속에서는 지우지 않는다 — relay 가 재인증마다 다시 내린다(`rateCrossItems` 와 같은 규율). `reset` 만 `INITIAL_DATA` 로 되돌린다」). `useMemo` 반환 객체에 `nxtTradable: data.nxtTradable`. `webapp/src/lib/relay-provider.tsx` `EMPTY_RELAY_VALUE` 의 `queuedWindow: undefined,` 다음에 `nxtTradable: null,`(주석 「Provider 밖에는 소켓이 없다 — 모름」). `RelayContextValue` 는 `Omit<RelayConnectionState, …>` 라 자동 포함.
2. **순수 함수(B-2).** 새 파일 `webapp/src/lib/exchange-choices.ts`: `import type { RelayExchange } from '@gh-radar/shared'` · `export const EXCHANGE_CHOICES_ALL: readonly RelayExchange[] = ['KRX', 'NXT']` · `export const EXCHANGE_CHOICES_KRX_ONLY: readonly RelayExchange[] = ['KRX']` · `export const KRX_ONLY_TITLE = 'NXT 에서 거래되지 않는 종목이에요'` · `export function exchangeChoicesOf(isin: string, exchange: RelayExchange, nxtTradable: ReadonlySet<string> | null | undefined): readonly RelayExchange[]` — `nxtTradable == null`(모름 · 스텁이 키를 안 준 `undefined` 포함) → ALL · `isin === ''` → ALL · `exchange === 'NXT'` → ALL(이미 NXT 인 카드 — 저장 배치 복원 등 — 는 되돌아갈 수 있어야 한다) · `nxtTradable.has(isin)` → ALL · 그 외 → KRX_ONLY. **상수 참조를 그대로 반환**한다(호출부 memo · 비교가 참조로 성립). 파일 머리 주석: 두 표면(카드 헤더 · 호가 상태줄)이 이 함수 하나를 쓴다 · 숨김만 하고 거래소를 자동 전환하지 않는다 · `isNxtEmpty`(D3) 휴리스틱은 플래그를 모를 때의 안전망으로 그대로 둔다.
3. **카드 헤더(B-2).** `webapp/src/components/trading/card/card-header.tsx`: 모듈 상수 `EXCHANGES` 를 지우고 `EXCHANGE_CHOICES_ALL` · `KRX_ONLY_TITLE` 을 `@/lib/exchange-choices` 에서 import. `CardHeaderProps` 에 `exchangeChoices?: readonly RelayExchange[]`(JSDoc: 「거래소 선택지 — `exchangeChoicesOf` 결과. 미지정 = 모름 = 둘 다. 길이 1 이면 토글이 아니라 라벨 하나를 그린다(quick-260923-pq2)」) · 기본값 `EXCHANGE_CHOICES_ALL`. 세그먼트 JSX: 기존 `<span onClick={stop} className="flex flex-none">` 래퍼는 유지하고 그 안을 분기 — `exchangeChoices.length === 1` 이면 `ToggleGroup` 대신 `<span data-slot="card-exchange-segment" data-single="true" title={KRX_ONLY_TITLE} className="inline-flex h-5 items-center overflow-hidden rounded-[var(--r-sm)] border border-[var(--border)]"><span className="flex h-5 items-center bg-[var(--card)] px-1.5 text-[10px] font-bold tracking-[0.02em] text-[var(--muted-fg)]">{exchangeChoices[0]}</span></span>`(라벨 톤 = 항목 off 색 · 같은 높이·패딩·글자라 레이아웃 불변 · role/aria-label 없음 — 상호작용 없는 라벨). 아니면 기존 `ToggleGroup` 그대로에 `{exchangeChoices.map((ex) => …)}`. `exchangeLocked` 관련 코드는 **있으면 손대지 않고 없으면 만들지 않는다**(pgv 소관). 머리 주석에 ②-b 한 단락: 「NXT 미거래 종목(quick-260923-pq2)은 세그먼트가 「KRX」 라벨 하나다 — 판정은 `exchangeChoicesOf`(호출부 `strategy-card`)이고 헤더는 받은 선택지만 그린다. 모르면 둘 다. 현재 거래소가 NXT 면 둘 다(순수 함수 예외).」
4. **카드 배선(B-4).** `strategy-card.tsx` `StrategyCard` 의 `const { accountStates, status } = useRelayContext();` 에 `nxtTradable` 을 더하고 `const exchangeChoices = exchangeChoicesOf(isin, exchange, nxtTradable);`(순수 함수가 안정 참조를 돌려주므로 `useMemo` 불필요) → `<CardHeader … exchangeChoices={exchangeChoices} />`. import `exchangeChoicesOf` from `@/lib/exchange-choices`.
5. **호가 상태줄(B-3).** `stock-orderbook-section.tsx`: 모듈 상수 `EXCHANGES` 를 지우고 `exchangeChoicesOf` · `KRX_ONLY_TITLE` 을 import. `const { queuedWindow, nxtTradable } = useRelayContext();` · `const exchangeChoices = exchangeChoicesOf(subscriptionIsin ?? '', exchange, nxtTradable);` → `<OrderbookStatusBar … exchangeChoices={exchangeChoices} />`. `OrderbookStatusBar` props 에 `exchangeChoices: readonly RelayExchange[]` 를 더하고 세그먼트를 카드 헤더와 같은 분기로 — 길이 1 이면 `<span data-slot="orderbook-exchange-segment" data-single="true" title={KRX_ONLY_TITLE} className=…(3 과 동일 클래스)>…</span>`, 아니면 기존 `ToggleGroup`(`disabled={status !== 'ready'}` 그대로)에 `{exchangeChoices.map(…)}`. `isNxtEmpty` · `TabNotices nxtEmpty` · `handleExchangeChange` · 종목 전환 리셋은 **그대로**. 세그먼트 주석에 「NXT 미거래 종목은 라벨 하나(quick-260923-pq2 · 카드 헤더와 같은 순수 함수) · `isNxtEmpty` 는 플래그를 모를 때의 안전망으로 유지」 한 줄.
6. **TDD.** `<behavior>` 의 다섯 테스트를 먼저 쓴다(모듈·필드가 없으니 typecheck 실패 = RED) → 1~5 구현 → GREEN. 기존 card-header 첫 두 케이스 · strategy-card 전 케이스 · orderbook ③/③-c · relay-socket 전 케이스는 **무수정 초록**.
7. 게이트: webapp typecheck + 아래 테스트 파일 5개. 커밋 **정확히 1건**(한글 · `feat(quick-260923-pq2): 카드·호가 거래소 세그먼트 — NXT 미거래 종목은 KRX 라벨 하나 (nxt.snap 리듀서 · exchangeChoicesOf · 모르면 둘 다)` · 파일 11개만 지정 · Co-Authored-By 금지 · `git add -A` 금지 · `.planning` 미커밋 · push 금지).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp test src/lib/__tests__/exchange-choices.test.ts src/lib/__tests__/relay-socket.test.ts src/components/trading/__tests__/card-header.test.tsx src/components/trading/__tests__/strategy-card.test.tsx src/components/stock/__tests__/stock-orderbook-section.test.tsx && test "$(grep -c 'export function exchangeChoicesOf' webapp/src/lib/exchange-choices.ts)" -ge 1 && test "$(grep -v '^ *[/*]' webapp/src/lib/use-relay-socket.ts | grep -c 'case "nxt.snap"')" -ge 1 && test "$(grep -c 'nxtTradable: null' webapp/src/lib/relay-provider.tsx)" -ge 1 && test "$(grep -c 'exchangeChoicesOf(' webapp/src/components/trading/card/strategy-card.tsx)" -ge 1 && test "$(grep -c 'exchangeChoicesOf(' webapp/src/components/stock/stock-orderbook-section.tsx)" -ge 1 && test "$(grep -c 'data-single' webapp/src/components/trading/card/card-header.tsx)" -ge 1 && test "$(grep -c 'data-single' webapp/src/components/stock/stock-orderbook-section.tsx)" -ge 1 && test "$(grep -v '^ *[/*]' webapp/src/components/trading/card/card-header.tsx | grep -c 'const EXCHANGES')" -eq 0 && test "$(grep -v '^ *[/*]' webapp/src/components/stock/stock-orderbook-section.tsx | grep -c 'const EXCHANGES')" -eq 0 && git diff --quiet -- pnpm-lock.yaml supabase webapp/src/components/trading/workbench/vi-settings-rows.tsx webapp/e2e && git diff --cached --quiet -- pnpm-lock.yaml supabase webapp/src/components/trading/workbench/vi-settings-rows.tsx webapp/e2e && L=$(git log --format=%s -- pnpm-lock.yaml supabase webapp/src/components/trading/workbench/vi-settings-rows.tsx webapp/e2e) && test "$(printf '%s\n' "$L" | grep -c 'quick-260923-pq2')" -eq 0</automated>
  </verify>
  <done>`nxtTradable`(초기 `null` · `nxt.snap` 통째 교체 · `reset` 만 초기화)이 소켓 계약과 `EMPTY_RELAY_VALUE` 에 있다. `exchangeChoicesOf` 가 6케이스로 잠기고 안정 참조를 돌려준다. 카드 헤더와 호가 상태줄이 같은 함수 결과로 세그먼트를 그리며 길이 1 이면 `data-single="true"` 라벨(같은 높이 · title · role 없음)이고, 모르면·포함이면·현재 NXT 면 둘 다다. `isNxtEmpty` · VI 설정 2줄 · e2e spec · lockfile diff 0. 새 테스트 5파일 초록 · 기존 케이스 무수정 초록 · typecheck 0. 커밋 1건, push 없음.</done>
</task>

<task type="auto">
  <name>Task 3: 전체 게이트(build 4단 · test 2단 · Playwright 회귀) + 배포 순서 SUMMARY</name>
  <files>.planning/quick/260923-pq2-wb-nxt-hide-nxt-nxt-relay-57-nxt-tradabl/260923-pq2-SUMMARY.md</files>
  <read_first>.planning/config.json (`workflow.build_command` · `workflow.test_command` 원문), webapp/e2e/specs/trading-workbench.spec.ts 케이스 3 의 `card-exchange-segment` radio 단언(Grep `card-exchange-segment` — 스텁 게이트웨이는 57 이 없어 둘 다 그려야 통과)</read_first>
  <action>
1. config 원문 그대로 `build_command` 4단 → `test_command` 2단을 돌린다(둘 다 0 에러 · 전량 초록). 이어 `cd webapp && pnpm exec playwright test trading-workbench`(0 fail — 케이스 3 의 KRX radio 단언이 「프레임 없음 → 둘 다」 하위호환의 실브라우저 증거다). Playwright 가 환경(relay 8090 픽스처 · dev 3100) 문제로 돌지 못하면 원인 로그를 SUMMARY 에 적고 멈춘다 — 통과로 적지 않는다.
2. `git log --format='%h %s' | grep 'quick-260923-pq2'` 로 이 quick 의 `feat` 커밋이 정확히 2건(Task 1 relay+shared · Task 2 webapp)인지 확인하고(상대 앵커 `HEAD~N` 은 쓰지 않는다 — 병렬 세션의 커밋이 끼어들 수 있다), `git status -sb` 에 `.planning` 외 미커밋 변경이 없는지 본다. 금지 영역은 세 겹으로 확인한다: 작업트리 `git diff --quiet -- <경로>` · 스테이지 `git diff --cached --quiet -- <경로>` · 이 quick 태그가 붙은 커밋이 그 경로를 건드린 적 없음(`git log --format=%s -- <경로>` 에 `quick-260923-pq2` 0건). 경로 = `relay/src/store/symbols.ts` · `relay/src/generated` · `pnpm-lock.yaml` · `supabase`.
3. SUMMARY 작성 — 반드시 담을 것: (a) **배포 순서**: 「relay 미배포 변경(R2 18-14·17·19 + R3 18-25·18-26 + R4 18-33 + quick cqj·e1m·ge2·pq2 …) 위에 얹힘. 순서 = relay 먼저(`deploy-relay.sh` · 사용자 실행) → `/healthz` · `smoke-relay.sh` 검증 → webapp push(= Vercel 배포). 웹앱은 `nxt.snap` 이 안 오면 둘 다 그리는 하위호환이라 relay 가 먼저면 안전하고, **역순(webapp 먼저)도 화면은 깨지지 않는다**(둘 다 표시 유지) — 다만 기능이 켜지지 않을 뿐. push 는 하지 않았다(사용자 결정)」. (b) 바뀐 지점 목록(파일·함수·타입 이름). (c) 새 테스트 케이스명 7개(relay ⑬ · ⑭-4 · webapp exchange-choices · nxt.snap describe · card-header describe · strategy-card 1 · orderbook ③-d). (d) 게이트 결과 원문(typecheck 4단 · relay test 수 · webapp test 수 · Playwright passed/failed/skipped 수). (e) 커밋 해시 2건. (f) 스펙-코드 불일치가 있었으면 항목별로.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/relay run test && pnpm --filter @gh-radar/webapp run test && git diff --quiet -- relay/src/store/symbols.ts relay/src/generated pnpm-lock.yaml supabase && git diff --cached --quiet -- relay/src/store/symbols.ts relay/src/generated pnpm-lock.yaml supabase && L=$(git log --format=%s -- relay/src/store/symbols.ts relay/src/generated pnpm-lock.yaml supabase) && test "$(printf '%s\n' "$L" | grep -c 'quick-260923-pq2')" -eq 0 && C=$(git log --format=%s) && test "$(printf '%s\n' "$C" | grep -c 'feat(quick-260923-pq2)')" -ge 2 && cd webapp && pnpm exec playwright test trading-workbench</automated>
  </verify>
  <done>build 4단 0 에러 · relay·webapp vitest 전량 초록 · Playwright `trading-workbench` 0 fail(케이스 3 KRX radio 단언 = 하위호환 증거). 커밋 2건(relay+shared · webapp) · 금지 영역 diff 0 · push 없음. SUMMARY 에 배포 순서(relay 먼저 → 검증 → webapp)와 게이트 원문·테스트 케이스명·커밋 해시가 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트웨이 57 → relay | 신뢰하는 사내 게이트웨이의 마스터 프레임. `nxt_tradable` 은 생성 접근자 bool — 형식 가드 불필요. 기존 원소 가드(ISIN·코드·이름)는 그대로 통과해야 행이 된다. |
| relay → 브라우저 (`nxt.snap`) | 공개 마스터 파생 ISIN 집합. 사용자 데이터 없음. `#users`(인증 + 자격증명 등록) 연결에만 간다. |
| 브라우저 상태 → 화면 | `null`(모름) 과 Set(확정)을 가른다 — 모름을 빈 집합으로 위장하면 모든 종목의 NXT 가 사라진다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-pq2-01 | Information Disclosure | `WsFanout.#broadcastNxtSnap` 전 사용자 순회 | low | accept | 내리는 값이 공개 마스터 파생 ISIN 집합(사용자 데이터 0). `#users` 만 순회해 미인증·미등록 연결엔 가지 않는다(⑭-4 (c) 단언). |
| T-pq2-02 | Denial of Service | `nxt.snap` 크기(≈10~15KB) × 연결 수 | low | mitigate | 연결당 인증 1회 + 일일 재적재 1회뿐. 프레임을 1회 만들어 재사용. 전송은 `#send` 백프레셔(임계 초과 드롭 · 연속 3회 종료) 안에서만. |
| T-pq2-03 | Tampering | 「모름」을 확정으로 위장 | medium | mitigate | relay: 집합 `null` 이면 프레임을 보내지 않는다(적재 전 빈 배열 금지 — ⑭-4 (a)). webapp: 초기 `null` · `exchangeChoicesOf` 는 `null`/`undefined` 를 둘 다로 푼다(테스트 「모름」 케이스). 실패 갈래는 기존 집합 유지(⑬). |
| T-pq2-04 | Tampering | `SymbolInfo` 에 `false` 지어내기(T-16-05) | medium | mitigate | 플래그를 `SymbolInfo` 에 넣지 않는다 — ⑬ 의 `lookup(...).toEqual({code,name,market,source})` 정확일치 + `symbols.ts` diff 0 게이트. |
| T-pq2-05 | Denial of Service | 카드가 NXT 에 갇힘 | low | mitigate | 현재 거래소가 NXT 면 집합에 없어도 둘 다 그린다(순수 함수 예외 · 테스트). 자동 전환은 하지 않는다(숨김만). |
| T-pq2-06 | Repudiation | 조용한 결선 누락 | low | mitigate | `index.ts` 의 `nxtTradable: gatewaySymbols` grep 게이트(Task 1 verify). 누락 시 프레임이 안 나가도 화면은 둘 다 그려 사용자 피해는 없지만 기능이 켜지지 않는다. |
| T-pq2-SC | Tampering | 패키지 설치 | low | accept | 설치 0 · `pnpm-lock.yaml` 변경 0 (Task 1·2·3 verify 가 diff 0 확인). |
</threat_model>

<verification>
- Task 1: shared build · relay typecheck · typecheck:tests · relay test 전량 · grep 게이트(계약 · 파싱 · 스토어 · fanout · 결선 · 테스트) · `symbols.ts` · 생성 코드 · protocol · lockfile diff 0.
- Task 2: webapp typecheck · 테스트 5파일 · grep 게이트(순수 함수 · 리듀서 case · EMPTY · 두 표면 배선 · `data-single` · 모듈 상수 `EXCHANGES` 제거) · VI 설정 · e2e · lockfile diff 0.
- Task 3: config `build_command` 4단 + `test_command` 2단 · Playwright `trading-workbench` 0 fail · 커밋 2건 · 금지 영역 diff 0.
- 수동(선택 · relay 배포 뒤): `/trading` 에 NXT 미거래 종목 카드를 추가하면 세그먼트가 「KRX」 라벨 하나, NXT 거래 종목은 KRX|NXT. 종목상세 호가 탭도 동일. relay 재기동 직후(57 적재 전) 열린 탭은 둘 다였다가 적재 뒤 자동으로 갱신된다(`updated` 재전송).
</verification>

<success_criteria>
- relay 가 57 의 `nxt_tradable` 을 ISIN 집합으로 보존하고(`null` = 모름), 인증 직후 집합이 있을 때만 `nxt.snap` 1프레임 + 일일 재적재 재전송. `SymbolInfo` 무변경.
- 웹앱이 `nxtTradable` 2상태로 두 표면의 세그먼트 선택지를 순수 함수 하나로 뽑는다 — 모름·포함·현재 NXT 는 둘 다, 미포함은 KRX 라벨 하나(레이아웃 불변). 자동 전환 없음. `isNxtEmpty` 유지.
- 프레임이 없으면(구 relay · e2e 스텁) 둘 다 — Playwright 케이스 3 이 그 증거. 게이트 전부 초록. 커밋 2건(relay+shared · webapp · 한글 · Co-Authored-By 없음), push 없음. SUMMARY 에 배포 순서.
</success_criteria>

<output>
Create `.planning/quick/260923-pq2-wb-nxt-hide-nxt-nxt-relay-57-nxt-tradabl/260923-pq2-SUMMARY.md` when done — Task 3 의 (a)~(f)를 그대로: 배포 순서(relay 먼저 → 검증 → webapp · push 안 함) · 바뀐 지점(파일·함수·타입) · 새 테스트 케이스명 · 게이트 결과 원문 · 커밋 해시 2건 · 스펙-코드 불일치.
</output>
