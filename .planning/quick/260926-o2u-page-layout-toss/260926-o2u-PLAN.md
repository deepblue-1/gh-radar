---
phase: quick-260926-o2u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/src/components/layout/page-layout.ts
  - webapp/src/components/layout/page-header.tsx
  - webapp/src/components/layout/__tests__/page-header.test.tsx
  - webapp/src/app/scanner/page.tsx
  - webapp/src/components/scanner/scanner-client.tsx
  - webapp/e2e/specs/search-page.spec.ts
  - webapp/src/components/layout/app-sidebar.tsx
  - webapp/src/components/layout/__tests__/app-sidebar.test.tsx
  - webapp/e2e/specs/sidebar-tree.spec.ts
  - webapp/e2e/specs/auth-session.spec.ts
  - webapp/src/components/theme/themes-client.tsx
  - webapp/src/components/theme/theme-rank-row.tsx
  - webapp/src/components/theme/themes-skeleton.tsx
  - webapp/src/components/theme/__tests__/themes-client.test.tsx
  - webapp/src/components/watchlist/watchlist-client.tsx
  - webapp/src/app/themes/page.tsx
  - webapp/src/app/watchlist/page.tsx
  - webapp/src/app/page.tsx
  - webapp/src/components/home/home-client.tsx
  - webapp/src/components/home/home-header.tsx
  - webapp/src/components/home/__tests__/home-client.test.tsx
  - webapp/e2e/specs/home.spec.ts
  - webapp/src/components/trading/me-client.tsx
  - webapp/e2e/specs/me.spec.ts
  - webapp/src/components/search/search-page-client.tsx
  - webapp/src/app/error.tsx
  - webapp/src/app/not-found.tsx
  - webapp/src/components/trading/surface-placeholder.tsx
autonomous: true
requirements: [O2U-D1, O2U-D2, O2U-D3, O2U-D4, O2U-D5, O2U-VERIFY]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "사이드바에 「종목검색」 소제목과 그 하위 3링크(상승률 상위·테마·관심종목)가 DOM 에 없다. 트리 링크 순서는 홈 → 검색 → (트레이딩 그룹 · My page, 기존 표시 조건 그대로) → AI 애널리스트다 (O2U-D1 · D1)"
    - "`/search` · `/scanner` · `/themes` · `/themes/{id}` · `/watchlist` 에서 사이드바 「검색」 링크가 aria-current=page + 선택 토큰으로 켜진다. `/scanner/detail` · `/` · `/stocks/*` 에서는 켜지지 않고, 그 밖의 활성 규칙(samePath 정확 일치)은 그대로다 (O2U-D1 · D1)"
    - "`/scanner` · `/themes` · `/watchlist` 의 제목 왼쪽에 「뒤로가기」 버튼(ChevronLeft 24px · 히트 36×40 · 왼쪽 -8px · aria-label 뒤로가기)이 있고, 누르면 history.length>1 이면 router.back(), 아니면 router.push('/search') 한다. `/` · `/me` · `/search` 에는 뒤로가기가 없다 (O2U-D2 · D2)"
    - "6개 페이지(/, /me, /scanner, /themes, /watchlist, /search)의 h1 이 모두 공용 PageHeader 로 22px/700/-0.02em 이고 제목 줄 최소 높이 44px 이다. 기존 설명 문구·갱신 시각·날짜 네비는 PageHeader 의 설명/오른쪽 영역에 보존된다. PageHeader 루트는 `<header>` 요소가 아니다(home.spec 헤더 잉크 불변식 보호) (O2U-D2 · D4)"
    - "6개 페이지 본문이 한 상수 PAGE_WRAP(`mx-auto flex w-full max-w-[900px] flex-col gap-4`)으로 900 폭 가운데 정렬된다. globals.css · app-shell.tsx · app/layout.tsx 는 diff 0 이다 (O2U-D3 · D3)"
    - "홈 「주도 테마」「개별 급등」과 테마 페이지 소제목이 15px/600 muted 제목 + 13px/600 faint 평문 개수(알약 없음) + 기존 오른쪽 액션을 쓰고, 제목 행과 내용 사이가 8px 이다. 테마 순위 목록은 카드 한 장(rounded 16 · card 면 · overflow-hidden) 안에 행을 묶고 행 사이 hairline 이며, 순위·테마명·출처·종목수·막대·평균값은 그대로다. 홈 ThemeCard/SoloCard 내부와 1열 쌓기, 상승률 상위·관심종목의 표/카드 리스트 내부는 그대로다 (O2U-D4 · D4)"
    - "/me 는 900 폭에서 계좌 카드 2열(칸당 표 영역 약 419px < 미체결 439·잔고 444 최소폭)이 가로 스크롤을 만들므로 AccountPanel 에 stack 을 넘겨 ≥1280 에서도 미체결 위 · 잔고 아래로 쌓고, 두 표 모두 가로 스크롤 없이 들어간다 (O2U-D3 파생 · 표면 내 시각 결함 수정)"
    - "미정의 크기 토큰으로 16px 로 떨어지던 h1(/scanner · /me · error · not-found · surface-placeholder)이 22px 이 되고, webapp/src 에 그 토큰 참조가 0건이다. globals.css 에 새 토큰을 추가하지 않는다 (O2U-D5 · D5)"
    - "검증 결과가 사실대로 기록된다 — typecheck · eslint(변경 파일) · vitest(관련 + 전체) 통과, Playwright 는 실행한 spec 의 통과/실패와 미실행 spec 의 이유(로컬 relay 8090 등)를 SUMMARY 에 적는다. 커밋은 한글 · 공동 저자 트레일러 없음 · 내 파일만 명시 add · push 없음 (O2U-VERIFY)"
  artifacts:
    - path: "webapp/src/components/layout/page-layout.ts"
      provides: "지시문 없는 순수 상수 모듈 — PAGE_WRAP · CARD · SECTION_TITLE · SECTION_COUNT · ROW_DIVIDER (서버 컴포넌트 page.tsx 도 문자열로 import)"
      contains: "export const PAGE_WRAP"
    - path: "webapp/src/components/layout/page-header.tsx"
      provides: "'use client' PageHeader({ title, back?, actions?, description?, className? }) + 내부 BackButton(useRouter 는 back 일 때만 마운트되는 BackButton 안에서만 호출)"
      contains: 'data-slot="page-header"'
    - path: "webapp/src/components/layout/__tests__/page-header.test.tsx"
      provides: "h1 규격 · back 유무 · back 동작(history 분기) · actions/description · 루트가 header 요소 아님"
      contains: "뒤로가기"
    - path: "webapp/src/components/layout/app-sidebar.tsx"
      provides: "종목검색 그룹 제거 + isSearchHubPath(허브 하위에서 「검색」 활성)"
      contains: "isSearchHubPath"
  key_links:
    - from: "webapp/src/app/page.tsx · webapp/src/app/scanner/page.tsx (서버 컴포넌트)"
      to: "webapp/src/components/layout/page-layout.ts PAGE_WRAP"
      via: "지시문 없는 .ts 모듈이라 서버 컴포넌트가 실제 문자열을 받는다 — 'use client' 모듈의 비컴포넌트 export 는 서버에서 client reference 로 바뀌어 className 이 깨진다"
    - from: "PageHeader back"
      to: "next/navigation useRouter"
      via: "BackButton 안에서만 useRouter 호출 — back 없는 페이지(HomeClient vitest 는 next/navigation 을 목하지 않는다)는 라우터 컨텍스트 없이 렌더된다"
    - from: "AppSidebar 「검색」 NavLink active"
      to: "isSearchHubPath(pathname)"
      via: "samePath('/search'|'/scanner'|'/themes'|'/watchlist') || pathname.startsWith('/themes/')"
    - from: "ThemeRankRow 행 포커스"
      to: "globals.css *:focus-visible (--focus-outline-offset 변수)"
      via: "목록 카드가 overflow-hidden 이라 바깥 링이 잘리므로 행 Link 에 [--focus-outline-offset:-2px] 로 링을 안쪽에 그린다(globals.css 무수정)"
    - from: "MeClient 계좌 카드"
      to: "webapp/src/components/orderbook/account-panel.tsx stack prop"
      via: "900 폭 컨테이너에서 2열이 표를 잘라 스크롤시키는 것을 호출부가 알려 주는 기존 장치(account-panel.tsx L222-229 주석) — account-panel.tsx 자체는 무수정"
---

<objective>
사용자가 채택한 목업(`mockup.html`, 본문 폭 C=900)대로 웹 페이지 레이아웃을 토스 문법으로 통일한다.

1. 사이드바 「종목검색」 그룹(상승률 상위 · 테마 · 관심종목)을 걷어내고, 세 페이지는 `/search` 허브 타일로만 들어가며 그 페이지들에서는 사이드바 「검색」이 켜진다(앱 검색 탭과 같은 경험) — D1.
2. 공용 PageHeader(22px 제목 · 44px 줄 · 선택 뒤로가기 · 오른쪽 보조 · 설명)를 만들어 6개 페이지에 쓰고, 허브 하위 3페이지에는 뒤로가기를 단다 — D2.
3. 홈 · My page · 상승률 상위 · 테마 · 관심종목 · 검색 본문을 한 상수 PAGE_WRAP(900 · 가운데)로 맞춘다 — D3.
4. 섹션 제목/개수 문법과 테마 순위 「카드 한 장 + hairline」 목록을 적용한다 — D4.
5. 미정의 크기 토큰으로 16px 로 떨어지던 h1 들을 22px 로 고친다 — D5.

트레이딩(`/trading*`) · 종목상세(`/stocks/*`) · `/themes/[id]` · `/chat` 은 범위 밖이다.

Purpose: 사이드바 트리와 앱 탭 구조를 일치시키고, 어느 화면에서도 같은 폭·같은 제목 줄·같은 섹션 문법으로 읽히게 한다(검색 페이지가 기준).
Output: 공용 레이아웃 모듈 2개 + 단위 테스트 1개, 6개 페이지 적용, 사이드바 정리, 관련 단위·e2e 테스트 갱신, 코드 커밋 3건(태스크당 1건).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260926-o2u-page-layout-toss/260926-o2u-CONTEXT.md
@.planning/quick/260926-o2u-page-layout-toss/mockup.html
@CLAUDE.md
@tasks/lessons.md

<interfaces>
<!-- 실행자가 코드베이스를 다시 탐색하지 않도록 계획 단계(2026-09-26)에 실측해 둔 사실. 경로는 모두 webapp/ 기준. -->

① 검색 페이지(기준) — `src/components/search/search-page-client.tsx`
   - L68-70 로컬 상수: `CARD = 'rounded-[16px] bg-[var(--card)]'` · `SECTION_TITLE = 'text-[15px] font-semibold text-[var(--muted-fg)]'` · `ROW_DIVIDER = '[&+&]:border-t [&+&]:border-[var(--border-subtle)]'`
   - L154 루트 `mx-auto flex w-full max-w-[900px] flex-col gap-4` · L155 h1 `mt-1.5 text-[22px] font-bold tracking-[-0.02em] text-[var(--fg)]` · L157 form `-mt-1`
   - 섹션 머리 패턴 L270 `<div className="mt-0.5 flex items-center justify-between">` + h2 SECTION_TITLE + 오른쪽 13px/600 primary 액션.

② 뒤로가기 동작 원형 — `src/components/stock/stock-hero.tsx` L35-43: `typeof window !== 'undefined' && window.history.length > 1 ? router.back() : router.push(...)`. 이번 fallback 은 `/search` 만.

③ 사이드바 — `src/components/layout/app-sidebar.tsx`
   - L6-15 lucide import(Home, Layers, MessageSquare, Search, Star, TrendingUp, User, Zap). Layers·Star·TrendingUp 은 L95-100 그룹 상수에서만 쓰인다. Search 는 L92 NAV_SEARCH_PAGE 도 쓴다.
   - L95-100 그룹 상수, L392 비링크 소제목 호출, L393-401 하위 목록. L202-238 GroupHeading — `item` 이 없을 때의 `<li>` 전용 분기(L232-237)는 L392 만 쓴다. L405 트레이딩 호출은 `item` 을 준다.
   - L148 `samePath` · L372 `isActive` · L389 「검색」 NavLink.
   - 문서 주석 중 갱신 대상: L39-42(트리), L44-50(①), L52-56(② 정확 일치), L88-92(NAV_SEARCH_PAGE 주석 「그룹 3항목은 그대로」), L197-198(GroupHeading 글꼴 주석).
   - 셸 `app-shell.tsx` 의 drawer 자동 닫힘은 조상 중 `A` 또는 `BUTTON[data-nav-item]` 에서 닫는다 — 이번에 **건드리지 않는다**.

④ 사이드바 테스트
   - `src/components/layout/__tests__/app-sidebar.test.tsx`: L11 머리 주석 ①, L273-286(소제목 ① 테스트), L313-327(260925-gy6 — idle 예시로 상승률 상위 링크 사용), L329-344(21-08 D-07 — topItems[2] 소제목 · topItems[3] 그룹 href 3개), L358-368(`/scanner` 라벨 테스트 · 「구 라벨 문자열을 이 파일에 적지 않는다」 규율 주석), L379-390(③-a 공개 항목에 상승률 상위), L698-705(`/scanner/detail` 정확 일치 테스트). `mockPathname` 변수로 경로 주입, `setupReady()` = 로그인+ready.
   - `e2e/specs/sidebar-tree.spec.ts`: L71-86 TREE_LINKS 8개(홈·검색·상승률 상위·테마·관심종목·트레이딩·My page·AI 애널리스트) + 주석 L75, L162-168 `TREE_LINKS.slice(0, 6)` / `slice(6)`, L276 공개 항목 단언, L335-369 케이스 4(L349-361 소제목 클릭으로 drawer 가 안 닫힘 단언), L371-387 케이스 5(사이드바 상승률 상위 링크 → /scanner). 이 파일은 `withLocalRelay()`(ws 8090 고정)가 필요하다.
   - `e2e/specs/auth-session.spec.ts` L36-40: `/scanner` 에서 사이드바 상승률 상위·관심종목 링크가 보인다는 단언(범위가 page 전체라 본문 링크와도 섞일 수 있음).

⑤ 페이지 현황
   - `src/app/page.tsx`(서버 컴포넌트) L24 래퍼 → Suspense(HomeSkeleton) → HomeClient. L11 주석이 「상승률 상위는 사이드바 2번째 메뉴」라고 적고 있다.
   - `src/components/home/home-header.tsx` L133-170: 루트 `flex flex-col gap-[var(--s-3)]` → 제목행(`flex flex-wrap items-baseline justify-between gap-3`, h1 L136 = --t-h2 24/800 + 날짜 네비 div L139-169) → 시점 슬라이더 행(L173-257, 그대로 둔다).
   - `src/components/home/home-client.tsx` L95 루트 `flex flex-col gap-[var(--s-4)]`, L112-126 「주도 테마」 머리(h2 --t-h4 18/700 + mono 알약 개수 + CopyTextButton ml-auto) → L127-129 ThemeCard 들, L136-150 「개별 급등」 머리 → L151-153 SoloCard 들. 두 머리 모두 `mt-[var(--s-2)]`.
   - `src/app/me/page.tsx` 는 MeClient 만 감싼다(**수정하지 않는다** — 21-35 가 편집 예정). `src/components/trading/me-client.tsx` L206-215 DMA 게이트 분기 루트 `flex flex-col gap-4`, L218 본 루트 `data-slot="me-page"` `flex flex-col gap-[var(--s-3)]`, L219-226 header/h1/설명 「전략 현황 · 미체결 · 잔고」, L251-268 계좌 카드 `<section data-slot="me-account-card">` 안 `<AccountPanel selectedAccountNo accountName account status />`. 파일 머리 주석 ④(L28-33 부근)가 「≥1280px 2열(477/477)」을 서술한다.
   - `src/components/orderbook/account-panel.tsx` L222-229 `stack?: boolean` — 「컨테이너 폭이 좁아 2열이 표를 자르면 호출부가 stack 을 넘긴다」는 기존 장치. L671-676 2열은 `min-[1280px]:grid-cols-2` 뷰포트 규칙, 각 섹션 `px-[var(--s-3)]`. 현재 stack 호출부 0곳. **이 파일은 수정하지 않는다**(종목상세와 공유).
   - `src/app/scanner/page.tsx`(서버) L22 래퍼 → Suspense(ScannerSkeleton) → ScannerClient. L16 주석이 사이드바 상승률 상위/관심종목 nav 를 서술한다. `src/components/scanner/scanner-client.tsx` L105-135 프래그먼트, L107-114 header(h1 「상승률 상위」 + 설명 「상한가 근접 종목(등락률 10% 이상)을 실시간으로 추적합니다. 마켓을 선택해 리스트를 확인하세요.」). 갱신 시각·새로고침은 ScannerFilters 안에 있다(그대로 둔다).
   - `src/app/themes/page.tsx`('use client') L12 주석 「사이드바의 "테마" 링크가 aria-current="page" 로 active」. `src/components/theme/themes-client.tsx` L91 루트 `<section aria-label="테마" className="flex flex-col gap-6">`, L93-114 header(h1 --t-h2 + 설명 「지금 뜨는 테마 랭킹 — {SORT_LABEL}」 + 오른쪽 갱신 점·「최근 갱신 16:00 KST」), L117-150 「⭐ 내 테마」(h2 + ＋ 테마 만들기 버튼 · 목록 ul gap-2), L153-216 「시스템 테마 랭킹」(h2 + 「↓ {SORT_LABEL}」 캡션 · 목록 ul gap-2 · 출처 푸터).
   - `src/components/theme/theme-rank-row.tsx` L52-59 Link className 에 행마다 `rounded-[var(--r)] border border-[var(--border)] bg-[var(--card)] px-[var(--s-4)] py-[var(--s-3)]` + hover 테두리. ThemeRankRow 소비처는 themes-client 뿐. `themes-skeleton.tsx` L13-27 행마다 border+bg+rounded 카드 8개.
   - `src/app/watchlist/page.tsx`('use client') L14 주석 「사이드바의 "관심종목" 링크가 aria-current="page"」. `src/components/watchlist/watchlist-client.tsx` L70 루트 `<section aria-label="관심종목" className="flex flex-col gap-4">`, L71-91 header(h1 --t-h3 20/600 + POLLING_DESC 설명 + 오른쪽 갱신 점·「최근 갱신 {fmtTime} KST」).

⑥ 테스트가 기대는 것
   - `src/components/theme/__tests__/themes-client.test.tsx`: `next/navigation` 을 `useRouter: () => ({ push })` 로 목한다(back 없음 → 렌더에는 지장 없음). `screen.getByRole('list')` 가 **단일 목록**을 가정하는 케이스가 있다(내 테마 비었을 때) — 새 목록/리스트 역할을 추가하지 말 것. `within(items[0]).getByText('1')` 은 목록 안으로 좁혀져 있다.
   - `src/components/home/__tests__/home-client.test.tsx`: `next/navigation` 을 목하지 않는다 → PageHeader 는 back 이 없을 때 useRouter 를 부르면 안 된다.
   - `src/components/search/__tests__/search-page-client.test.tsx` L130·L208: `getByRole('heading', { level: 1, name: '검색' })`.
   - `e2e/specs/search-page.spec.ts` L125-141: 1280 에서 h1 의 **부모** 박스 폭 ≤ 900 && > 600 (PageHeader 제목 줄이 부모가 돼도 성립해야 한다). L55-57 `prepare()` 가 scanner·themes·stock API 를 목한다. 파일은 로컬 relay 불필요.
   - `e2e/specs/home.spec.ts` L372-437 잉크 불변식: `document.querySelectorAll('header svg')` 의 보이는 svg 가 정확히 2개여야 한다 — **본문에 `<header>` 요소를 새로 만들고 그 안에 아이콘(날짜 네비 셰브론)을 넣으면 깨진다.** 그래서 PageHeader 루트는 `div` 다. `mockHomeApi`, `HOME_POPULATED` 는 `../fixtures/home` 에서 export.
   - `e2e/specs/me.spec.ts` L671-722 케이스 7(1280, 2열 단언 L688-690 · `computedMinWidth` 단언 L700-701 · 잘림 0 · 표 스크롤 컨테이너 안쪽), L36-44 머리 주석 ④, L372 `computedMinWidth` 헬퍼(케이스 7 전용). me.spec 은 로컬 relay 가 필요하다. L665 `html` 에 `data-slot="me-page"` 포함 단언 — 루트 data-slot 유지.

⑦ 전역 포커스 — `src/styles/globals.css` L450 `*:focus-visible { outline: var(--focus-outline, 2px solid var(--ring)); outline-offset: var(--focus-outline-offset, 3px); box-shadow: var(--focus-shadow, …) }`. 버튼·링크는 이 이중 링이 유일한 포커스 표시다(걷지 말 것). 변수로 오프셋만 조정할 수 있다.

⑧ D5 대상 — 정의되지 않은 크기 토큰 `--t-2xl` 을 `text-[length:var(--t-2xl)]` 로 쓰는 h1 이 정확히 5곳: `src/app/error.tsx` L30 · `src/app/not-found.tsx` L17 · `src/components/scanner/scanner-client.tsx` L108 · `src/components/trading/me-client.tsx` L220 · `src/components/trading/surface-placeholder.tsx` L26. 치환값은 `text-[22px]`.

⑨ 명령(이전 quick 에서 통과한 형태 그대로)
   - `pnpm --filter @gh-radar/webapp run typecheck` (tsc + e2e tsconfig)
   - `pnpm --filter @gh-radar/webapp exec vitest --run <경로…>` · 전체 `pnpm --filter @gh-radar/webapp run test`
   - `pnpm --filter @gh-radar/webapp exec eslint <변경 파일…>`
   - `pnpm --filter @gh-radar/webapp exec playwright test <spec…> --project=chromium` — webServer 는 `PORT=3100 pnpm dev`, reuseExistingServer. 계획 시점 :3100 에 다른 세션의 dev 서버(PID 97752)가 떠 있다 — **끄거나 재시작하지 않는다**(turbopack HMR 이 변경을 반영한다).
</interfaces>
</context>

<constraints>
- 수정 금지: `webapp/src/styles/globals.css` · `webapp/src/components/layout/app-shell.tsx` · `webapp/src/app/layout.tsx` · `webapp/src/app/me/page.tsx` · `webapp/src/app/search/page.tsx` · `webapp/src/components/orderbook/account-panel.tsx` · 트레이딩 작업대(`components/trading/workbench/*`, `components/trading/card/*`, `components/trading/lc/*`) · `stocks/*` · `chat/*` · `themes/[id]`. globals.css 에 토큰 추가 금지(D5).
- 메인 트리에서 순차 실행(worktree 금지). 다른 세션의 미커밋 변경(.planning/*, tasks/lessons.md, packages/shared/src/*, webapp/src/lib/{limit-chaser,relay-provider,use-relay-socket}.ts(x), trading strategy-* 파일, phase 21 PLAN 초안)이 트리에 있다 — **건드리지도 stage 하지도 않는다**. 커밋 직전 `git status -sb` 를 다시 보고, `git add <내 파일 경로…>` 로 이 플랜의 files_modified 중 실제로 바꾼 파일만 명시 추가한다(`git add -A` · `git add .` 금지). 태스크마다 커밋 1건, 메시지 한글, 공동 저자 트레일러 없음. **push 금지**(이 저장소에서 push = 프로덕션 배포).
- typecheck/vitest 실패가 내 파일 밖(다른 세션 미커밋 WIP)에서 나면 고치지 말고 파일·에러 요지를 SUMMARY 에 「범위 밖 선재」로 적는다. 내 파일이 원인이면 고친다.
- e2e 는 실행한 것만 통과라고 쓴다. 로컬 relay(8090)·Supabase 시드 등 환경 때문에 못 돌린 spec 은 「미실행 + 이유」로 적는다.
- 사용자 대면 문구·주석·SUMMARY 는 한글.
- 셸 명령은 저장소 루트(`/Users/alex/repos/gh-radar`)에서 실행한다 — grep·git 경로는 `webapp/…` 기준이고, `pnpm --filter @gh-radar/webapp exec …` 는 패키지 디렉터리에서 돌아 `src/…`·`e2e/…` 상대 경로를 쓴다.
</constraints>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 공용 페이지 문법(PAGE_WRAP · PageHeader) 도입 — /search 타일 → /scanner → 뒤로가기 한 경로 끝까지</name>
  <files>webapp/src/components/layout/page-layout.ts, webapp/src/components/layout/page-header.tsx, webapp/src/components/layout/__tests__/page-header.test.tsx, webapp/src/app/scanner/page.tsx, webapp/src/components/scanner/scanner-client.tsx, webapp/e2e/specs/search-page.spec.ts</files>
  <read_first>
    - webapp/src/components/search/search-page-client.tsx L60-160 (기준 상수·루트·h1 — interfaces ①)
    - webapp/src/components/stock/stock-hero.tsx L30-60 (뒤로가기 원형 — interfaces ②)
    - webapp/src/components/scanner/scanner-client.tsx 전체, webapp/src/app/scanner/page.tsx 전체
    - webapp/e2e/specs/search-page.spec.ts 전체
    - .planning/quick/260926-o2u-page-layout-toss/mockup.html L87-98 (`.ph` · `.back` · `.st` 수치)
  </read_first>
  <behavior>
    - page-header 테스트 1: `<PageHeader title="상승률 상위" />` → h1(level 1, 이름 「상승률 상위」) className 에 `text-[22px]`·`font-bold`·`tracking-[-0.02em]`, h1 의 부모(제목 줄)에 `min-h-11`. 「뒤로가기」 버튼 없음. 이때 `useRouter` 목이 한 번도 호출되지 않는다(vi.fn 으로 확인).
    - 테스트 2: 루트는 `data-slot="page-header"` 이고 렌더 결과(container) 안에 `header` 요소가 0개다(home.spec 잉크 불변식 보호 — interfaces ⑥).
    - 테스트 3: `back` + history.length 2(`vi.spyOn(window.history, 'length', 'get').mockReturnValue(2)`) → 「뒤로가기」 클릭 → router.back 1회, router.push 0회.
    - 테스트 4: `back` + history.length 1 → 클릭 → router.push('/search') 1회, router.back 0회.
    - 테스트 5: 뒤로가기 버튼 접근 이름 「뒤로가기」, type=button, 아이콘 svg 는 aria-hidden.
    - 테스트 6: `actions`(예: 「15:31 갱신」 span)와 `description`(예: 「설명 문구」)이 렌더되고, 설명 p 의 className 에 `text-[var(--muted-fg)]`, actions 컨테이너 className 에 `ml-auto`.
  </behavior>
  <action>
    per D3 · D2 — 먼저 테스트(page-header.test.tsx)를 behavior 대로 작성해 실패를 확인한 뒤 구현한다. `next/navigation` 은 파일 상단에서 `useRouter: vi.fn(() => ({ back, push }))` 꼴로 목하고, 각 테스트 전에 back/push/useRouter 를 초기화한다.

    (1) `page-layout.ts` 신설 — **'use client' 지시문을 달지 않는다.** 서버 컴포넌트(app/page.tsx · app/scanner/page.tsx)가 import 해서 실제 문자열을 받아야 하기 때문이다('use client' 모듈의 비컴포넌트 export 는 서버에서 client reference 로 바뀐다 — 파일 머리 주석에 이 이유를 한 줄로 적는다). export 5개: `PAGE_WRAP = 'mx-auto flex w-full max-w-[900px] flex-col gap-4'`(D3, 검색 루트와 동일), `CARD`, `SECTION_TITLE`, `ROW_DIVIDER`(interfaces ① 의 값과 바이트 동일), `SECTION_COUNT = 'text-[13px] font-semibold tabular-nums text-[var(--faint)]'`(D4). 각 상수에 한 줄 주석(용도 · 적용 범위: 트레이딩·종목상세·채팅은 쓰지 않는다).

    (2) `page-header.tsx` 신설('use client') — export `PageHeader({ title: string; back?: boolean; actions?: ReactNode; description?: ReactNode; className?: string })`.
       - 루트는 `div`(header 요소 금지 — 이유를 주석에: home.spec 잉크 불변식이 `header svg` 를 센다), `data-slot="page-header"`, className `cn('flex flex-col gap-1.5', className)`.
       - 제목 줄 `div`: `mt-0.5 flex min-h-11 flex-wrap items-center gap-x-1 gap-y-2`(목업 `.ph` = 최소 44 · margin-top 2 · gap 4, 좁은 폭에서 오른쪽 영역이 다음 줄로 접히게 wrap).
       - back 이면 제목 앞에 내부 `BackButton` 을 렌더. h1 className `text-[22px] font-bold tracking-[-0.02em] text-[var(--fg)]`(D2 · 검색 h1 과 같은 규격, margin 없음).
       - actions 가 있으면 `div` className `ml-auto flex items-center gap-2 text-[13px] text-[var(--muted-fg)]` 안에 넣는다.
       - description 이 있으면 제목 줄 아래 `p` className `text-[length:var(--t-sm)] text-[var(--muted-fg)]`(D2).
       - `BackButton`(비공개 함수 컴포넌트): 이 컴포넌트 **안에서만** `useRouter()` 를 부른다(back 없는 페이지는 라우터 컨텍스트 없이 렌더돼야 한다 — interfaces ⑥ HomeClient 테스트). `<button type="button" aria-label="뒤로가기">`, className `-ml-2 flex h-10 w-9 shrink-0 items-center justify-center rounded-[10px] text-[var(--fg)] transition-colors hover:bg-[color-mix(in_oklch,var(--muted)_60%,transparent)]`, 자식 lucide `ChevronLeft` `className="size-6"` `aria-hidden="true"`. 포커스 표시는 전역 `*:focus-visible` 이중 링에 맡긴다(outline 을 걷는 유틸을 붙이지 않는다 — interfaces ⑦). onClick = interfaces ② 패턴, fallback 은 파일 상수 `'/search'` 하나(D2 — 사용자 입력·쿼리에서 오지 않는 고정 경로).
       - 파일 머리 주석: D2 요약(뒤로가기 표시 페이지 = 상승률 상위·테마·관심종목, 탭 루트 = 홈·My page·검색은 없음)과 quick-260926-o2u 출처.

    (3) `/scanner` 적용 — `app/scanner/page.tsx` 래퍼 div 의 className 을 `PAGE_WRAP` 로 바꾸고(import `@/components/layout/page-layout`), L16 주석을 「진입은 `/search` 허브 타일 · 사이드바는 「검색」이 켜진다(quick-260926-o2u D1)」 취지로 고친다. `scanner-client.tsx` 의 L107-114 header 블록을 `<PageHeader title="상승률 상위" back description="(기존 설명 문구 그대로)" />` 로 바꾼다 — 이로써 그 파일의 미정의 크기 토큰 참조도 사라진다(D5). ScannerFilters·표·카드 리스트는 그대로(D4).

    (4) e2e — `search-page.spec.ts` describe 안에 케이스 추가: 「1280 — 타일 → /scanner: 제목 22px · 본문 ≤ 900 · 뒤로가기 → /search (quick-260926-o2u D2·D3)」. 1280×800 → `/search` → 바로가기 nav 첫 타일 클릭 → URL `/scanner$` → main 의 h1 「상승률 상위」 보임 · `getComputedStyle(h1).fontSize === '22px'` → main 의 `[data-slot="page-header"]` 박스 폭 ≤ 900 이고 > 600 → main 의 「뒤로가기」 버튼 클릭 → URL `/search$`. (직접 진입 fallback 은 e2e 로 재지 않는다 — Playwright 새 탭은 about:blank 이 history 에 남아 length 가 2 다. fallback 은 단위 테스트 4 가 잠근다.)

    커밋: `feat(quick-260926-o2u): 공용 페이지 헤더·본문 폭 900 도입 — 상승률 상위에 뒤로가기`.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp exec vitest --run src/components/layout/__tests__/page-header.test.tsx && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/search-page.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - page-header.test.tsx 6케이스 이상 통과(먼저 실패를 본 뒤 통과).
    - 지시문 줄 검사 `grep -cE "^['\"]use client['\"]" webapp/src/components/layout/page-layout.ts` 가 0(주석 속 언급은 줄 머리가 따옴표가 아니라 세지 않는다) 이고 `grep -c "export const PAGE_WRAP" webapp/src/components/layout/page-layout.ts` 가 1.
    - `grep -c "PAGE_WRAP" webapp/src/app/scanner/page.tsx` ≥ 1, `grep -c "PageHeader" webapp/src/components/scanner/scanner-client.tsx` ≥ 1.
    - search-page.spec.ts 신규 케이스 포함 전 케이스 통과(환경상 못 돌리면 이유 기록).
  </acceptance_criteria>
  <done>/search 타일로 들어간 /scanner 가 900 폭 · 22px 제목 · 뒤로가기(→ /search)로 동작하고, 공용 모듈 2개가 단위 테스트로 잠겨 커밋됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 사이드바 「종목검색」 제거 + 허브 하위에서 「검색」 활성 · 테마/관심종목에 헤더·폭·목록 문법</name>
  <files>webapp/src/components/layout/app-sidebar.tsx, webapp/src/components/layout/__tests__/app-sidebar.test.tsx, webapp/e2e/specs/sidebar-tree.spec.ts, webapp/e2e/specs/auth-session.spec.ts, webapp/e2e/specs/search-page.spec.ts, webapp/src/components/theme/themes-client.tsx, webapp/src/components/theme/theme-rank-row.tsx, webapp/src/components/theme/themes-skeleton.tsx, webapp/src/components/theme/__tests__/themes-client.test.tsx, webapp/src/components/watchlist/watchlist-client.tsx, webapp/src/app/themes/page.tsx, webapp/src/app/watchlist/page.tsx</files>
  <read_first>
    - webapp/src/components/layout/app-sidebar.tsx 전체 (interfaces ③)
    - webapp/src/components/layout/__tests__/app-sidebar.test.tsx L1-60 · L260-400 · L690-710 (interfaces ④)
    - webapp/e2e/specs/sidebar-tree.spec.ts L60-90 · L150-175 · L255-388
    - webapp/e2e/specs/auth-session.spec.ts L30-55
    - webapp/src/components/theme/themes-client.tsx · theme-rank-row.tsx · themes-skeleton.tsx 전체, themes-client.test.tsx L1-60 · L180-320
    - webapp/src/components/watchlist/watchlist-client.tsx 전체
    - Task 1 이 만든 webapp/src/components/layout/page-layout.ts · page-header.tsx
  </read_first>
  <behavior>
    - 사이드바: 로그인+ready 에서 트리에 「종목검색」 텍스트가 없고 `/scanner`·`/themes`·`/watchlist` href 링크가 0개. 최상위 li 순서 = 홈 · 검색 · 트레이딩(링크) · …
    - 사이드바: mockPathname 이 `/search`·`/scanner`·`/themes`·`/themes/abc-123`·`/watchlist` 이면 「검색」 링크 aria-current=page + `bg-[var(--nav-on-bg)]`; `/`·`/scanner/detail`·`/stocks/005930`·`/chat` 이면 aria-current 없음.
    - 사이드바: 비로그인에서도 홈·검색·AI 애널리스트 링크가 보인다(③-a 공개 항목 갱신).
    - 테마: 시스템 랭킹 목록(ul)이 className 에 `rounded-[16px]`·`bg-[var(--card)]`·`overflow-hidden` 을 갖고, 각 li 에 `[&+&]:border-t` 가 있으며, 행 Link className 에 `border-[var(--border)]` 가 없다.
    - 테마: 「시스템 테마 랭킹」 h2 className 에 `text-[15px]`, 표시 개수(평문 span, `text-[var(--faint)]`)가 목록 길이와 같다. 「뒤로가기」 버튼이 있다.
  </behavior>
  <action>
    per D1 — 사이드바(먼저 app-sidebar.test.tsx 를 아래대로 고쳐 실패를 본 뒤 구현):
    - app-sidebar.tsx: 그룹 상수(L95-100)와 L392-401 렌더 블록을 지우고, 그 상수에서만 쓰던 lucide 아이콘 3개 import 를 뺀다(Search 는 남는다). GroupHeading 의 비링크 `<li>` 분기는 호출부가 사라지므로 제거하고 `item` 을 필수로 바꾼다. 새 함수 `isSearchHubPath(pathname: string): boolean` = `samePath` 로 `/search`·`/scanner`·`/themes`·`/watchlist` 중 하나와 같거나 `pathname.startsWith('/themes/')` 이면 true(D1 — `/scanner/detail` 같은 다른 하위 경로는 켜지 않는다). 「검색」 NavLink 의 active 를 `isSearchHubPath(pathname)` 로 바꾼다. 나머지 `isActive`(samePath) 규칙·트레이딩 그룹·조건부 숨김·data-nav-item 은 그대로.
    - 문서 주석 갱신(interfaces ③ 목록): 트리 서술을 「홈 · 검색 · [트레이딩 …] · My page · AI 애널리스트」로, ① 을 「그룹 제목은 이제 트레이딩(링크)뿐 — 비링크 소제목을 다시 두게 되면 버튼으로 만들지 말 것(셸 drawer 자동 닫힘이 BUTTON[data-nav-item]·A 에서 닫는다)」 취지로, ② 에 「예외: 검색 허브 하위(상승률 상위·테마·관심종목 + 테마 상세)에서는 「검색」이 켜진다 — 앱 탭바가 검색 탭을 켜는 것과 같다(quick-260926-o2u D1)」를 덧붙이고, NAV_SEARCH_PAGE 주석의 「그룹 3항목은 그대로」 문장과 GroupHeading 글꼴 주석의 그룹명 언급을 현재 트리에 맞게 고친다. 사라진 그룹명 문자열을 주석에 새로 남기지 않아도 된다.
    - app-sidebar.test.tsx: 머리 주석 ① 을 「허브 하위 3링크 없음 · 「검색」 활성 규칙」으로 교체. L273-286 소제목 테스트 → 「그룹이 없다: 그룹명 텍스트 없음 · 세 href 링크 0개」. L313-327 의 idle 예시 링크를 「홈」으로. L329-344 → 「홈 다음 검색, 그다음 트레이딩 제목 링크」(topItems[2] 안에 이름 「트레이딩」 링크). L358-368 `/scanner` 라벨 테스트 → behavior 의 활성 표(`it.each` 가능). 같은 자리의 「구 라벨 문자열을 이 파일에 적지 않는다」 규율은 계속 지킨다. L379-390 ③-a 공개 항목 단언을 「검색」으로. L698-705 → `/scanner/detail` 에서 「검색」·「홈」 모두 aria-current 없음.
    - sidebar-tree.spec.ts: TREE_LINKS 를 5개(홈·검색·트레이딩·My page·AI 애널리스트)로, 주석 L71-78 갱신, 케이스 1 의 `slice(0, 6)`/`slice(6)` 을 `slice(0, 3)`/`slice(3)` 으로. 케이스 2 L276 공개 항목을 「검색」으로. 케이스 4 는 소제목 클릭 단계(L353-361)를 지우고 제목을 「drawer 가 같은 트리를 쓰고, 3단 링크를 누르면 닫힌다」로, 주석에 「비링크 소제목이 트리에서 사라져 그 대조 대상이 없다」를 적는다. 케이스 5 는 「5. 허브 하위 페이지(/scanner)에서 「검색」이 켜지고 그룹 링크는 없다 (quick-260926-o2u D1)」로 교체: `/scanner` 이동 → desktopNav 의 「검색」(exact) aria-current=page → 그룹명 텍스트 0개 → 이름 「상승률 상위」·「테마」·「관심종목」(exact) 링크 0개 → main 의 h1 「상승률 상위」 보임.
    - auth-session.spec.ts L36-40: 「AppSidebar 주 메뉴: 검색 링크 노출 · /scanner 에서 활성, 허브 하위 링크 없음」으로 바꾸고 범위를 `page.getByRole('navigation', { name: '주 메뉴' }).first()` 로 좁힌다(본문 링크와 섞이지 않게) — 「검색」(exact) 보임 + aria-current=page, 이름에 「상승률 상위」·「관심종목」 포함 링크 0개.
    - search-page.spec.ts 에 케이스 추가: 「1280 — 허브 하위(/scanner · /themes · /watchlist)에서 사이드바 「검색」 활성 · 뒤로가기 · 본문 ≤ 900 (quick-260926-o2u D1·D2·D3)」 — 경로마다 goto → 주 메뉴 nav(first) 의 「검색」(exact) aria-current=page · 이름 「상승률 상위」·「테마」·「관심종목」(exact) 링크 0개 → main 의 「뒤로가기」 버튼 보임 → main `[data-slot="page-header"]` 박스 폭 ≤ 900. (watchlist 데이터 응답 여부와 무관하게 헤더는 렌더된다.)

    per D2 · D3 · D4 — 테마:
    - themes-client.tsx: 루트 section 은 aria-label 유지, className 을 `PAGE_WRAP` 로. header 블록(L93-114)을 `<PageHeader title="테마" back description={지금 뜨는 테마 랭킹 — {SORT_LABEL}} actions={갱신 점 span + 「최근 갱신 16:00 KST」 mono span} />` 로 — 점·문구 클래스는 기존 그대로 옮기고 바깥 div 의 글자 클래스는 PageHeader actions 기본값에 맡긴다. 두 소제목 블록을 `<section aria-labelledby=… className="flex flex-col gap-2">`(D4 — 제목 행과 내용 사이 8px)로 바꾸고, 머리 행 = `flex items-center gap-1.5` 안에 h2(`SECTION_TITLE`, id 부여, 텍스트 「⭐ 내 테마」·「시스템 테마 랭킹」 그대로) + 목록이 보일 때만 개수 span(`SECTION_COUNT` — 내 테마 = myThemes.length, 시스템 = rankedSystemThemes.length) + 기존 오른쪽 요소(＋ 테마 만들기 버튼 / 「↓ {SORT_LABEL}」 캡션)에 `ml-auto`. 두 목록 ul 을 `cn(CARD, 'm-0 list-none overflow-hidden p-0', 기존 isRefreshing 불투명도)` 로, 각 li 에 `ROW_DIVIDER`. 로그인 유도·빈 상태·오류·출처 푸터·ThemeEditDialog 는 그대로.
    - theme-rank-row.tsx: Link className 에서 행 카드 꾸밈(rounded·border·bg·hover 테두리 줄)을 빼고, 패딩은 유지, `transition-colors hover:bg-[color-mix(in_oklch,var(--muted)_60%,transparent)] [--focus-outline-offset:-2px]` 를 더한다(목록 카드가 overflow-hidden 이라 전역 바깥 링이 잘린다 → 오프셋 변수로 링을 행 안쪽에 그림 · globals.css 무수정, interfaces ⑦). 그리드·순위·테마명·출처·종목수·막대·평균값 마크업은 그대로(D4). 머리 주석에 「카드 한 장 안의 행(quick-260926-o2u D4)」 한 줄.
    - themes-skeleton.tsx: 8행을 `cn(CARD, 'skeleton-list overflow-hidden')` 한 장 안에 두고 행마다 `ROW_DIVIDER` + `px-4 py-3`(행별 border·bg·rounded 제거) — 로딩→로드 전환에서 모양이 튀지 않게.
    - themes-client.test.tsx: behavior 의 테마 케이스 2개를 추가한다(기존 케이스 수정 없이 통과해야 한다 — 새 list 역할을 만들지 않았는지 이것으로 확인).
    - app/themes/page.tsx L12 주석을 「진입 = /search 허브 타일, 사이드바는 「검색」이 켜진다(quick-260926-o2u D1)」 취지로.

    per D2 · D3 — 관심종목:
    - watchlist-client.tsx: 루트 section 은 aria-label 유지, className 을 `PAGE_WRAP` 로. header(L71-91)를 `<PageHeader title="관심종목" back description={POLLING_DESC} actions={갱신 점 + 「최근 갱신 {fmtTime} KST」} />` 로. 표·카드 리스트·상태 분기는 그대로(D4).
    - app/watchlist/page.tsx L14 주석을 themes 와 같은 취지로.

    커밋: `feat(quick-260926-o2u): 사이드바 종목검색 그룹 제거 · 테마·관심종목 헤더·폭·목록 통일`.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp exec vitest --run src/components/layout/__tests__/app-sidebar.test.tsx src/components/theme/__tests__/themes-client.test.tsx src/components/layout/__tests__/page-header.test.tsx && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/search-page.spec.ts e2e/specs/auth-session.spec.ts e2e/specs/themes.spec.ts e2e/specs/sidebar-tree.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - app-sidebar.test.tsx · themes-client.test.tsx 전 케이스 통과(기존 themes 케이스는 무수정).
    - `grep -c "isSearchHubPath" webapp/src/components/layout/app-sidebar.tsx` ≥ 2(정의 + 사용).
    - `grep -c "PAGE_WRAP" webapp/src/components/theme/themes-client.tsx webapp/src/components/watchlist/watchlist-client.tsx` 가 파일마다 ≥ 1.
    - search-page · auth-session · themes spec 통과. sidebar-tree 는 로컬 relay(8090)가 뜨면 통과, 못 뜨면 「미실행 + 이유」 기록.
  </acceptance_criteria>
  <done>사이드바가 홈·검색·(트레이딩)·AI 애널리스트 트리이고 허브 하위 페이지에서 「검색」이 켜지며, /themes·/watchlist 가 900 폭 · 뒤로가기 헤더 · 카드 한 장 순위 목록으로 렌더되고 커밋됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 탭 루트(홈 · My page · 검색)에 같은 헤더·폭·섹션 문법 + 미정의 크기 토큰 제거</name>
  <files>webapp/src/app/page.tsx, webapp/src/components/home/home-client.tsx, webapp/src/components/home/home-header.tsx, webapp/src/components/home/__tests__/home-client.test.tsx, webapp/e2e/specs/home.spec.ts, webapp/src/components/trading/me-client.tsx, webapp/e2e/specs/me.spec.ts, webapp/src/components/search/search-page-client.tsx, webapp/src/app/error.tsx, webapp/src/app/not-found.tsx, webapp/src/components/trading/surface-placeholder.tsx</files>
  <read_first>
    - webapp/src/app/page.tsx 전체, webapp/src/components/home/home-header.tsx L125-175, home-client.tsx L90-165, home-client.test.tsx L1-60 · L235-260
    - webapp/e2e/specs/home.spec.ts L20-60 · L300-440 (잉크 불변식 — interfaces ⑥)
    - webapp/src/components/trading/me-client.tsx L1-60 · L195-280, webapp/src/components/orderbook/account-panel.tsx L215-235 · L665-700 (읽기만)
    - webapp/e2e/specs/me.spec.ts L20-60 · L360-380 · L660-725
    - webapp/src/components/search/search-page-client.tsx L1-160
    - webapp/src/app/error.tsx · not-found.tsx · webapp/src/components/trading/surface-placeholder.tsx (interfaces ⑧ 의 줄)
  </read_first>
  <behavior>
    - 홈: 「주도 테마」·「개별 급등」 h2 className 에 `text-[15px]`·`text-[var(--muted-fg)]`, 바로 뒤 개수 span 이 테마/개별 수를 평문으로 보여 주고 className 에 `text-[var(--faint)]` 가 있으며 `rounded-full` 이 없다. 「주도 테마 전체 복사」 버튼은 그대로 동작(기존 케이스 무수정 통과).
    - 홈: h1 「오늘의 급등 테마」 className 에 `text-[22px]`, 「이전 날짜」·「다음 날짜」·「오늘」 버튼과 시점 슬라이더가 그대로 있고 「뒤로가기」 버튼은 없다.
  </behavior>
  <action>
    per D3 · D2 · D4 — 홈(먼저 home-client.test.tsx 에 behavior 케이스 2개를 추가해 실패를 본다):
    - app/page.tsx: 래퍼 div className 을 `PAGE_WRAP`(import `@/components/layout/page-layout` — 서버 컴포넌트라 지시문 없는 모듈에서 가져온다)로. L11 주석의 「사이드바 2번째 메뉴」 서술을 「/search 허브 타일로 진입」으로 고친다.
    - home-header.tsx: 제목행(L135-170)을 `<PageHeader title="오늘의 급등 테마" actions={기존 날짜 네비 div 그대로} />` 로 바꾼다 — 날짜 네비의 버튼·라벨·disabled 규칙·aria-label 은 한 글자도 바꾸지 않는다(D4 범위 밖). 뒤로가기 없음(탭 루트, D2). 루트 `flex flex-col gap-[var(--s-3)]` 와 시점 슬라이더 행은 그대로. 머리 주석의 「--t-h2 24/800」 서술을 「PageHeader 22/700(quick-260926-o2u D2)」로.
    - home-client.tsx: 두 섹션(L110-131, L134-155)을 각각 `<section aria-labelledby="home-themes-title"|"home-singles-title" className="flex flex-col gap-2">`(D4 8px)로 감싸고, 머리 행 = `flex items-center gap-1.5` 안에 h2(id, `SECTION_TITLE`) + 개수 span(`SECTION_COUNT`, mono 알약 제거) + 기존 CopyTextButton(ml-auto 유지). 머리의 `mt-[var(--s-2)]` 는 뺀다. 카드들은 머리 아래 `div className="flex flex-col gap-[var(--s-4)]"` 로 묶어 기존 카드 간격 16 과 1열 쌓기를 유지한다(D4 — ThemeCard/SoloCard 내부 무수정, 2열 그리드 금지). 루트·빈 상태·오류 카드는 그대로.
    - home.spec.ts 에 케이스 추가: 「1280 — 홈 제목 22px · 뒤로가기 없음 · 본문 ≤ 900 (quick-260926-o2u D2·D3)」 — `mockHomeApi(page, { response: HOME_POPULATED })` → `/` → h1 「오늘의 급등 테마」 fontSize 22px → main 「뒤로가기」 버튼 0개 → main `[data-slot="page-header"]` 폭 ≤ 900 && > 600. 기존 잉크 불변식 케이스는 **수정하지 않고** 통과해야 한다(PageHeader 가 header 요소가 아님을 실측으로 확인하는 자리).

    per D3 · D2 · D5 — My page(me-client.tsx 만, me/page.tsx 무수정):
    - DMA 게이트 분기 루트 className 을 `PAGE_WRAP` 로(헤더는 새로 달지 않는다 — 기존 화면 구성 유지).
    - 본 루트는 `data-slot="me-page"` 유지, className 을 `cn(PAGE_WRAP, 'gap-[var(--s-3)]')` 로(D-08a 간격 12 + mb-1 규율 유지 — twMerge 가 gap 을 덮는다).
    - header 블록(L219-226)을 `<PageHeader title="My page" description="전략 현황 · 미체결 · 잔고" />` 로 — 미정의 크기 토큰 참조가 사라진다(D5). 뒤로가기 없음(탭 루트).
    - 계좌 카드의 `<AccountPanel … />` 에 `stack` 을 넘긴다. 이유(주석으로 남김): 900 폭 카드에서 ≥1280 2열이면 칸마다 표 영역이 약 419px 로 미체결 439 · 잔고 444 최소폭보다 좁아 두 표가 가로 스크롤된다 — account-panel 의 `stack` 은 바로 이 경우(좁은 컨테이너)를 호출부가 알려 주는 기존 장치다. 파일 머리 주석 ④ 의 「≥1280 2열(477/477)」 서술을 「My page 는 900 폭이라 stack(세로) — quick-260926-o2u」로 고친다. account-panel.tsx 는 수정하지 않는다.
    - me.spec.ts 케이스 7 을 새 배치로 갱신: 제목 「7. 데스크톱 1280 — 계좌 카드 안 미체결 위 · 잔고 아래(stack)이고 표가 가로 스크롤 없이 들어간다 (quick-260926-o2u)」, 2열 단언(L688-690)을 「holdings 의 y ≥ unfilled 의 y + height − 1 · 두 섹션 x 차이 ≤ 1」로, `computedMinWidth` 단언 2줄을 「각 섹션 첫 `[data-slot="table-container"]` 의 scrollWidth ≤ clientWidth + 1」로 바꾼다. 잘림 0 · 스크롤 컨테이너 안쪽 단언은 유지. `computedMinWidth` 헬퍼가 더는 쓰이지 않으면 헬퍼와 그 주석을 지운다(미사용 선언으로 typecheck/eslint 가 막히지 않게). 머리 주석 ④ 의 2열 서술도 stack 기준으로 고친다.

    per D3 · D2 — 검색(search-page-client.tsx, 최소 diff — 21-28 이 같은 파일을 편집 예정):
    - 로컬 상수 3개(L68-70)를 지우고 `@/components/layout/page-layout` 의 `CARD`·`SECTION_TITLE`·`ROW_DIVIDER` 를 import(값 동일이라 시각 변화 0). 루트 className 을 `PAGE_WRAP` 로. h1(L155)을 `<PageHeader title="검색" />` 로(뒤로가기 없음 — 탭 루트). form 의 `-mt-1` 은 그대로 둔다. 나머지 로직·마크업 무수정.

    per D5 — 나머지 3곳(error.tsx · not-found.tsx · surface-placeholder.tsx)은 interfaces ⑧ 의 줄에서 그 크기 클래스만 `text-[22px]` 로 한 줄 치환한다. 주석에 옛 토큰 이름을 남기지 않는다. globals.css 에 토큰을 추가하지 않는다.

    마무리: 전체 vitest, typecheck, 이 플랜에서 바꾼 파일 전부에 eslint 를 돌리고, 가능하면 :3100 에서 6개 페이지를 1280·390 × 라이트·다크로 스크린샷(scratchpad)해 목업(`mockup.html`)의 제목 줄·폭·섹션 머리와 대조한다(줄바꿈·잘림·겹침이 보이면 이 태스크 안에서 고친다).

    커밋: `feat(quick-260926-o2u): 홈·My page·검색 헤더·폭 통일 · 미정의 크기 토큰 제거`.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/webapp exec vitest --run src/components/home src/components/search src/components/layout src/components/theme && pnpm --filter @gh-radar/webapp run test && pnpm --filter @gh-radar/webapp run typecheck && test "$(grep -rn -- '--t-2xl' webapp/src | wc -l | tr -d ' ')" = "0" && git diff --quiet HEAD -- webapp/src/styles/globals.css webapp/src/components/layout/app-shell.tsx webapp/src/app/layout.tsx webapp/src/app/me/page.tsx webapp/src/components/orderbook/account-panel.tsx && pnpm --filter @gh-radar/webapp exec playwright test e2e/specs/home.spec.ts e2e/specs/search-page.spec.ts e2e/specs/me.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - home-client.test.tsx 신규 2케이스 포함 통과, 전체 webapp vitest 통과(범위 밖 선재 실패는 기록).
    - `grep -c "PAGE_WRAP"` 가 webapp/src/app/page.tsx · webapp/src/components/trading/me-client.tsx · webapp/src/components/search/search-page-client.tsx 에서 각각 ≥ 1, `grep -c "stack" webapp/src/components/trading/me-client.tsx` ≥ 1.
    - webapp/src 전체에서 미정의 크기 토큰 참조 0건(verify 의 grep), 금지 파일 5개 diff 0.
    - home.spec(잉크 불변식 무수정 포함) · search-page.spec 통과. me.spec 은 로컬 relay 가 뜨면 통과, 못 뜨면 「미실행 + 이유」 기록.
    - eslint(변경 파일) 0 errors.
  </acceptance_criteria>
  <done>홈·My page·검색이 상승률 상위·테마·관심종목과 같은 900 폭 · 22px 제목 줄 · 섹션 문법을 쓰고, /me 계좌 표가 가로 스크롤 없이 쌓이며, 16px 로 떨어지던 h1 이 모두 22px 이 되어 커밋됐다(push 없음).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 history → 뒤로가기 | 사용자 탐색 이력만 쓴다. fallback 경로는 코드 상수 `/search` — URL 쿼리·사용자 입력이 섞이지 않는다 |
| 사이드바 표시 ↔ 라우트 접근 | 메뉴에서 빠지는 것은 권한이 아니다. /scanner · /themes · /watchlist 의 인증은 기존 middleware 가 그대로 막는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-o2u-01 | Tampering | PageHeader BackButton fallback | low | mitigate | `router.push` 인자는 파일 상수 `'/search'` 하나 — 쿼리·props 로 경로를 받지 않아 오픈 리다이렉트 표면이 없다. page-header.test 케이스 4 가 push('/search') 를 잠근다 |
| T-o2u-02 | Elevation of Privilege | 사이드바 그룹 제거 · 「검색」 활성 규칙 | low | accept | 표시 규칙 변경일 뿐 인가와 무관(기존 주석 ④ 「UI 숨김은 권한이 아니다」 유지). /watchlist 는 middleware 가 미인증을 /login 으로 돌린다(변경 없음) |
| T-o2u-03 | Information Disclosure | /me 계좌 카드 stack 배치 | low | accept | 배치만 바뀌고 표시 데이터·마스킹 규칙(계좌번호 전체 표시 D2)은 그대로. relay 조회 경로 변경 없음 |
</threat_model>

<verification>
- Task 별 `<verify>` 명령이 모두 통과(또는 환경상 미실행 사유가 기록)된다.
- `git diff --stat HEAD~3..HEAD` 가 이 플랜의 files_modified 안의 파일만 보여 준다(다른 세션 파일 0).
- `git log -3 --format='%s%n%b'` 에 공동 저자 트레일러가 없고 메시지가 한글이다. `git status -sb` 가 여전히 origin 대비 ahead 이며 push 하지 않았다.
- 권장: :3100 스크린샷(6페이지 × 1280/390 × 라이트/다크)을 목업과 대조한 결과를 SUMMARY 에 한 줄로.
</verification>

<success_criteria>
- D1: 사이드바에 종목검색 그룹이 없고, 허브 하위 페이지에서 「검색」이 켜진다(단위 + e2e).
- D2: 6페이지가 공용 PageHeader(22px · 44px 줄)를 쓰고, 상승률 상위·테마·관심종목만 뒤로가기가 있으며 동작(back / fallback /search)이 단위 테스트로 잠겼다.
- D3: 6페이지가 PAGE_WRAP 한 상수로 900 폭 가운데 정렬, 셸·globals·layout 무수정.
- D4: 홈·테마 섹션 머리 문법, 테마 순위 카드 한 장 + hairline, 홈 카드 내부·1열 유지, 표/카드 리스트 내부 유지.
- D5: 미정의 크기 토큰 참조 0건, 해당 h1 22px.
- /me 계좌 표가 900 폭에서 가로 스크롤 없이 쌓인다(파생 수정 — SUMMARY 에 이유 한 줄).
</success_criteria>

<output>
Create `.planning/quick/260926-o2u-page-layout-toss/260926-o2u-SUMMARY.md` when done — 태스크별 커밋 해시, 검증 명령·결과 표(실행/미실행 구분), /me stack 파생 결정 한 줄, 범위 밖 선재 실패(있으면), 후속 메모(account-panel.tsx `stack` 주석의 「기본 = My page 2열」 서술이 이제 낡았다 — 파일 소유 범위 밖이라 이번엔 미수정).
</output>
