---
phase: quick-260923-m23
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260923120000_dma_orders_modified.sql
  - packages/shared/src/relay.ts
  - relay/src/order/notice-status.ts
  - webapp/src/lib/orders-api.ts
  - webapp/src/lib/__tests__/orders-api.test.ts
  - relay/src/store/orders.ts
  - relay/tests/helpers/fake-dma-orders.ts
  - relay/tests/order-store.test.ts
  - supabase/tests/dma_orders_modified.test.sql
  - scripts/verify-dma-orders-price-check.sh
  - relay/src/ws/order-handler.ts
  - relay/tests/ws-order.test.ts
autonomous: true
requirements: [M23-A, M23-B, M23-C, M23-D, M23-E, M23-F]

estimate:
  tokens: 200000
  raw_tokens: 200000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "사고 재현: 원주문 매도 18,249 중 7,432 체결(hub 원주문 unfilled 10,817) 상태에서 정정 요청 13,827 → 정정확인 M(quantity 13,827) 이 오면 새 정정 행 qty 는 10,817 이고, 이후 체결 10,817 이 쌓이면 그 행은 filled 다. 원주문 행은 modified_qty 10,817 · status 'modified' 다 (M23-A · M23-C · M23-F)"
    - "일부정정: 원주문 A 100 → M 30 이면 새 행 qty 30, 원주문 modified_qty 30 · 여전히 accepted 이고, 이후 원주문 체결 70 이 오면 원주문은 filled 다 (M23-B · M23-C)"
    - "전량 정정 + 선체결: A 100 → E 40 → M(130) 이면 새 행 qty 60, 원주문 filled_qty 40 + modified_qty 60 · 'modified' 다 (M23-B · M23-C)"
    - "순서 역전: 체결 E(Modify) 가 정정 대기를 먼저 정산해 새 행이 partially_filled 가 된 뒤 늦은 M 이 오면, M 은 CAS 수량 경로로 qty 를 이동 수량으로 내리고 filled 로 재판정한다 (M23-B · M23-C)"
    - "이동 수량(movedQty)은 hub 66/67 상태에서 순수함수로 계산되고, 67 먼저 도착(새 행이 hub 에 있음)과 51 먼저 도착(원주문 잔량만 있음) 두 순서에서 같은 값이다. 둘 다 없으면 통보 수량 폴백 + warn 1줄(계좌번호 없음) (M23-A)"
    - "원주문 이동 반영은 M 통보 1건당 정확히 1회다 — 통보 수신 리스너 한 지점에서만 일어나고, 같은 M 주문번호가 두 번 와도 사용자·당일 기준 두 번째는 건너뛴다. CAS clamp(남은 잔량 이하)가 2선이다 (M23-C)"
    - "자동주문(상따 등) 정정확인 M 으로 만드는 새 행은 order_type 'M' · qty = movedQty 다 (M23-C)"
    - "기존 동작 불변: 체결 조각 누적(S1-a~h) · 상태 단조성(㊹ ㊺ · R3-WR-01) · e1m 기록 경로 테스트가 단언 수정 없이 초록이다 (M23-B · M23-F)"
    - "dma_orders 에 modified_qty integer NOT NULL DEFAULT 0 CHECK(>=0) 컬럼과 status 8종 CHECK 가 추가 전용 마이그레이션 1개로 들어가고, 로컬 일회용 컨테이너 pgTAP 회귀가 초록이다. 원격 적용은 하지 않는다 (M23-D)"
    - "웹앱 오늘 주문 목록에서 status 'modified' 행은 라이브 A/E 프레임이 있어도 「정정」(tone muted)으로 보인다 — 기존 단어·톤 재사용, 새 시각 요소 없음 (M23-E)"
  artifacts:
    - path: "supabase/migrations/20260923120000_dma_orders_modified.sql"
      provides: "modified_qty 컬럼 + status CHECK 8종 교체(pg_constraint 조회로 기존 status CHECK 제거 후 dma_orders_status_check 명시 이름으로 재생성)"
      contains: "modified_qty"
    - path: "supabase/tests/dma_orders_modified.test.sql"
      provides: "pgTAP 회귀 — 컬럼 존재·NOT NULL·기본값 0 · status 8종 허용 · 미지 status/음수 modified_qty 거부 · status CHECK 1개"
      contains: "modified_qty"
    - path: "packages/shared/src/relay.ts"
      provides: "DmaOrderStatus 에 'modified' 추가"
      contains: "\"modified\""
    - path: "relay/src/order/notice-status.ts"
      provides: "STATUS_RANK modified 3 · TERMINAL 에 modified"
      contains: "modified: 3"
    - path: "relay/src/store/orders.ts"
      provides: "OrderQtyChange(fillDelta·qtyCap·modifiedDelta) · OrderUpdate.qtyCap/modifiedQtyDelta · 수량 변경 일반 CAS(supabaseOrderFillSink) · 큐 항목 qtyChange 라우팅"
      contains: "modified_qty"
    - path: "relay/src/ws/order-handler.ts"
      provides: "export modifyMovedQty 순수함수 · OrderNoticeSource.getAccountStates · 리스너 단일 지점 원주문 이동(recordModifyMove) · finish/recordUnmatched/ensureRow/autoInsertRow 의 M qtyCap · 자동 M insert order_type M"
      contains: "export function modifyMovedQty"
    - path: "webapp/src/lib/orders-api.ts"
      provides: "STATUS_LABELS.modified { 정정, muted } · orderDisplayStatus 에서 status 'modified' 우선"
      contains: "modified: { label: \"정정\", tone: \"muted\" }"
  key_links:
    - from: "relay/src/ws/order-handler.ts 리스너(deps.hub.on(\"order\"))"
      to: "modifyMovedQty(deps.hub.getAccountStates(userId), notice)"
      via: "M 통보 수신 틱에 동기 1회 계산 → settle(notice, movedQty) · recordUnmatched(…, movedQty) · recordModifyMove(…, movedQty) 에 같은 값 전달"
      pattern: "getAccountStates"
    - from: "order-handler enqueueUpdate({ qtyCap | modifiedQtyDelta })"
      to: "OrderStore.enqueueUpdate → QueueItem.qtyChange → #applyFill → supabaseOrderFillSink"
      via: "수량 입력이 하나라도 있으면 update sink 가 아니라 CAS sink 로 간다"
      pattern: "qtyChange"
    - from: "supabaseOrderFillSink 읽기 컬럼 modified_qty"
      to: "supabase/migrations/20260923120000_dma_orders_modified.sql"
      via: "relay 새 코드는 modified_qty 를 읽는다 — 마이그레이션 원격 적용이 relay 배포보다 반드시 먼저"
      pattern: "modified_qty"
    - from: "DmaOrderStatus 'modified'"
      to: "notice-status STATUS_RANK · webapp STATUS_LABELS"
      via: "Record<DmaOrderStatus, …> 컴파일 강제"
      pattern: "modified"
---

<objective>
gh-trade 정정확인 수량 캡(gh-trade a940b25f · a70f6ab7)에 relay `dma_orders` 감사 기록을 맞춘다. 정정확인(51, noticeType "M")의 `quantity` 는 **요청 수량 에코**이고 거래소는 min(요청, 원주문 잔량) 만 옮긴다. 지금 relay 는 (결함 ①) 새 정정 행 qty 를 에코로 적어 영원히 partially_filled 로 남기고, (결함 ②) 원주문 행을 전혀 건드리지 않아 전량 정정이면 원주문이 accepted/partially_filled 로 영구 잔류한다.

Purpose: 트레이더가 오늘 주문 목록·감사 기록에서 정정된 주문의 진짜 수량과 종결 상태를 본다 — 2026-09-23 매도 정정 사고(18,249 중 7,432 체결 → 13,827 정정 요청 → 10,817 이동)가 기록상 정확히 재현된다.

Output: 마이그레이션 1개(+pgTAP 회귀) · shared 상태 1종 추가 · relay store 의 수량 변경 일반 CAS 경로 · order-handler 의 M 통보 처리(새 행 qty 캡 + 원주문 이동 기록) · 웹앱 표시 1줄.

설계는 오케스트레이터가 잠갔다(Design decisions A~F). 이 플랜은 그것을 그대로 구현한다 — 재논의하지 않는다.

**분해 방식 메모 (tracer-first 면제 근거):** 오케스트레이터가 태스크 형태(T1 스키마·타입·표시 → T2 store CAS → T3 handler 적용)를 지정했고, 아키텍처는 잠긴 설계로 이미 확정돼 얇은 수직 슬라이스가 새 정보를 주지 않는다. 대신 각 태스크 커밋 시점마다 저장소 전체가 컴파일·테스트 초록이도록 경계를 잡았다(T1 이 `Record<DmaOrderStatus,…>` 소비처를 같은 커밋에서 채운다).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

**선행 독서를 줄이기 위한 앵커 지도 (플래너가 이미 읽었다 — 파일 전체를 다시 읽지 말고 이 구간만 연다).** 줄 번호는 2026-09-23 HEAD 기준이다.

relay/src/ws/order-handler.ts (1677줄)
- L111-120 `OrderNoticeSource` 인터페이스(on · getLimitChasers · getViTrigger) — 여기에 `getAccountStates` 추가.
- L169-213 `PendingOrder` 타입, L212 `settle: (notice) => void`.
- L342-378 `relayTrail` · `trailOf` · `markTrailOrderNo` — 사용자·KST 당일 스코프 Set 들(day 바뀌면 비움).
- L451-509 `deps.hub.on("order", …)` 리스너 — 후보 수집 → `narrowPending` → hit 면 `settle(notice)` 후 return, 아니면 `void recordUnmatched(...).catch(logger.error{err, orderNo})`.
- L519-529 `patchOf(notice)` — `{orderNo, status: statusOf(notice,null), resultCode, noticeType, message, filledQtyDelta: filledQtyOf(notice), origin}`.
- L576-663 `recordUnmatched(userId, notice, hadCandidates)` — 수동 분기 L583-637(findIdByOrderNo(notice.orderNo) 실패 → `{...patch, userId}` 열화 L601, 기존 행 → `{...patch, orderRowId}` L609, 없음 → isRelayRelated error/info), 취소성 → `settleOriginal` L642-645, 자동 `ensureRow` → row L652 / lookup-failed L660.
- L682-754 `settleOriginal` — 원주문 조회부 L698-739(orderNoLookupKeys → inflight 재사용 → findIdByOrderNo 순회 → catch 에서 safePgError 로그 + `orderNo: keys[0], userId` 열화 갱신).
- L766-803 `ensureRow`, L818-841 `insertOnly`, L851-915 `autoInsertRow`(L865 수량 가드 · L905 orderType 하드코딩 · L907 `qty: notice.quantity`).
- L1093-1114 수동 insert(정정은 `orderType: kind` "M", `qty: msg.qty`), L1199-1249 `finish`(M 이면 statusOf → "accepted", `filledQty: filledQtyOf(notice)` 절대값 · orderNo 채움), L1255-1275 `entry`(settle: finish).
- L1382 `normalizeOrderNo`, L1389-1408 `isCancelLikeNotice`(docstring L1395-1396 「정정확인 M 의 자동 insert 동작은 이 quick 의 범위가 아니라 그대로 둔다」— 이번에 갱신), L1417-1422 `orderNoLookupKeys`(비export), L1544 `narrowPending`. 순수함수 구역 머리 L1339-1341.

relay/src/store/orders.ts (1172줄)
- L144-184 `OrderUpdate`(L154-158 filledQty 절대값 · L159-171 filledQtyDelta 규율 주석).
- L259-264 `OrderInsertRow.status` 주석 「CHECK 는 7종」.
- L300-311 `OrderFillSink(sel, delta: number, patch)`, L314-320 `OrderSinks.addFill`.
- L384-~420 `supabaseOrderSink`(L415 `status IN replaceableStatusesOf(next)` — 막히면 통째로 무동작 · `{applied:false}`).
- L657-787 `supabaseOrderFillSink` — readTarget L696-722(cols "id, qty, filled_qty, status"), 계산 L735-761, CAS UPDATE L763-777(eq id · eq filled_qty · status 있으면 eq status), 상한 L781-785.
- L800-806 `QueueItem.fillDelta`, L918-966 `enqueueUpdate`(L931-938 계약 위반 드롭 · L939-946 조각 검증 warn · L949-953 「갱신할 필드가 없는 요청」), L1060-1096 `#drain`(L1067-1070 라우팅), L1098-1104 `#applyFill`, L1157-1171 `rowPatchOf`.

relay/src/order/notice-status.ts — L50-52 statusOf("M") → "accepted", L72-89 STATUS_RANK(주석 「7종」), L91-92 TERMINAL, L109-113 replaceableStatusesOf.

packages/shared/src/relay.ts — L763-~800 `RelayUnfilled{orderNo, orgOrderNo, isin, side, price, orderQty, filledQty, unfilledQty, …}`, L816-835 `RelayAccountState{t:"acct", a(계좌번호), snap, hold, unf, rm, st}`, L1174-1182 `DmaOrderStatus` 7종.
relay/src/hub/subscription-hub.ts L713 `getAccountStates(userId): RelayAccountState[]` (사용자 prefix 스캔, 동기).

webapp/src/lib/orders-api.ts — L101-110 NOTICE_LABELS(A/E/C/R), L112-122 STATUS_LABELS(7종 Record), L130-133 `orderDisplayStatus`(라이브 nt 가 이김). 테스트 webapp/src/lib/__tests__/orders-api.test.ts L192-229 `describe('orderDisplayStatus')` · 헬퍼 `view(base, live)` · `frame({nt})`.

테스트 하네스
- relay/tests/helpers/fake-dma-orders.ts L53 `DB_DEFAULTS = { status: "requested", filled_qty: 0 }`(DB 기본값 거울 — 투영·eq 가 이 값을 읽는다), eq 필터는 컬럼 불문 실제 적용(L147-150).
- relay/tests/order-store.test.ts L63-102 `recordingSinks`(L96-99 addFill 스텁이 `filled_qty_delta: delta` 로 기록), L1206-1406 `describe("S1 — 체결 조각 누적")`(직접 호출 L1289 · L1350 · L1365 가 두 번째 인자로 숫자 3).
- relay/tests/ws-order.test.ts L120-164 `mkOrderStore`(inserts · updates · lookups · existingIds), L227-240 `orderModify`, L758 직접 호출 hub 스텁 `{ on, getLimitChasers, getViTrigger }`, L1166 `vi.spyOn(hub, "getLimitChasers")` 패턴, L1693-1705 `restartWithRealStore(seed)`, L1708-1732 `modifySettledByFill`, L1735-1756 `pushLateModifyConfirm`, L1758 ㊹ · L1779 ㊺, L1811 e1m describe(L1817 `autoRow`), L2125 `mkNotice`, L2149 isCancelLikeNotice describe. 하네스 게이트웨이는 계좌 상태(66/67)를 기본으로 보내지 않는다 → `hub.getAccountStates` 는 `[]` 이다(필요하면 `vi.spyOn(hub, "getAccountStates").mockReturnValue([...])`). `buildOrderRespFrame` 의 quantity 기본값은 10.

마이그레이션·DB 회귀 선례
- supabase/migrations/20260921120000_dma_orders_modify_offhours.sql — 헤더(결정 근거·함정·하지 않는 것) 스타일, 인라인 CHECK 이름을 상수로 가정하지 않고 `pg_constraint` + `pg_get_constraintdef(oid) ~ '\m컬럼\M'` DO 블록으로 떨어뜨린 뒤 명시 이름으로 재생성(L43-62).
- 원래 status CHECK 는 20260905120200_dma_orders.sql L62 의 인라인 컬럼 CHECK(7종).
- scripts/verify-dma-orders-price-check.sh — 일회용 로컬 컨테이너(public.ecr.aws/supabase/postgres:17.6.1.104, 로컬 이미지 존재 확인됨)에 마이그레이션 재생 후 `TEST_FILE`(L29 하드코딩) 실행. supabase/tests/dma_orders_price_check.test.sql 이 pgTAP 형식 선례(BEGIN · pgtap 확장 · auth.users 고정 uuid 1행 · plan(N) · throws_ok '23514' · ROLLBACK).

**작업 트리 주의:** 시작 시점에 다른 세션의 미커밋 변경 `relay/src/generated/StockDMA.fbs` · `relay/src/generated/stock-dma/viorder-item.ts` 가 있다. 이 플랜의 것이 아니다 — **스테이징하지 않는다.** 커밋은 항상 파일을 명시해 `git add <files>` 하고, 커밋 직전 `git status --short` · `git log -1 --oneline` 으로 다른 세션 커밋/편집이 끼어들지 않았는지 재확인한다(메모리: 동시 세션 커밋 경합).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 스키마·공유 타입·표시 계약 — modified_qty 컬럼 + status 'modified' (D · E)</name>
  <files>supabase/migrations/20260923120000_dma_orders_modified.sql, packages/shared/src/relay.ts, relay/src/order/notice-status.ts, webapp/src/lib/orders-api.ts, webapp/src/lib/__tests__/orders-api.test.ts</files>
  <read_first>
    - supabase/migrations/20260921120000_dma_orders_modify_offhours.sql (헤더 스타일 + DO 블록 L43-62)
    - supabase/migrations/20260905120200_dma_orders.sql L48-70 (원 status CHECK)
    - relay/src/order/notice-status.ts L40-113
    - webapp/src/lib/orders-api.ts L95-134, webapp/src/lib/__tests__/orders-api.test.ts L192-229
  </read_first>
  <behavior>
    - orderDisplayStatus(view({status:'modified'}, frame({nt:'A'}))) → { label: '정정', tone: 'muted' } (라이브 A 보다 우선)
    - orderDisplayStatus(view({status:'modified'}, frame({nt:'E'}))).label === '정정'
    - orderDisplayStatus(view({status:'modified'}, null)).label === '정정'
    - 기존 orderDisplayStatus 4개 케이스 무수정 초록 (status 가 modified 가 아니면 라이브가 이긴다)
    - relay replaceableStatusesOf('accepted') 에 'modified' 가 없고, replaceableStatusesOf('modified') 는 requested·timeout·accepted·partially_filled·modified 다 (relay 전체 테스트 무수정 초록으로 확인)
  </behavior>
  <action>
    (1) 마이그레이션 `supabase/migrations/20260923120000_dma_orders_modified.sql` 새로 작성 (per D). 20260921120000 의 헤더 형식을 따른다 — 「결정 근거」(gh-trade a940b25f·a70f6ab7 정정확인 수량 캡, 정정 이동분을 원주문 행에 기록해야 filled_qty+modified_qty=qty 로 종결을 판정할 수 있다는 것, 'modified' = 원주문 잔량이 정정으로 새 주문번호에 옮겨가 마지막 이동으로 닫힌 행), 「함정」(인라인 CHECK 이름은 Postgres 자동명이라 상수로 가정하지 않는다 — `pg_constraint` 에서 `contype='c'` 이고 정의문이 정규식 단어 경계 `\mstatus\M` 에 걸리는 CHECK 를 찾아 DROP 한 뒤 `dma_orders_status_check` 명시 이름으로 재생성; `krx_session`·`notice_type` 같은 다른 컬럼 CHECK 가 걸리지 않음을 단어 경계가 보장), 「하지 않는 것」(POLICY·GRANT 재실행 없음, 기존 행 수정 없음 — 새 컬럼은 DEFAULT 0 이 사실과 맞다, 원격 적용은 이 quick 이 하지 않는다 — 사람 게이트), 「호환」(추가 전용이라 구 relay 는 새 컬럼·새 상태를 쓰지 않아 그대로 돈다; 반대로 새 relay 는 modified_qty 를 읽으므로 이 파일이 relay 배포보다 먼저 원격 적용돼야 한다). 본문은 BEGIN → DO 블록(status CHECK 조회 DROP, 떨어뜨린 개수가 0 이면 RAISE EXCEPTION 으로 멈춘다 — 조용히 CHECK 두 개가 공존하는 상태를 만들지 않는다) → `ADD CONSTRAINT dma_orders_status_check CHECK (status IN ('requested','accepted','rejected','filled','partially_filled','cancelled','timeout','modified'))` → `ADD COLUMN modified_qty integer NOT NULL DEFAULT 0 CONSTRAINT dma_orders_modified_qty_check CHECK (modified_qty >= 0)` → 두 컬럼 COMMENT ON COLUMN(한국어, 의미·쓰는 주체 relay·파생 규칙 filled_qty+modified_qty>=qty) → COMMIT. `supabase db push` 등 원격 명령은 실행하지 않는다.
    (2) `packages/shared/src/relay.ts` L1174-1182 `DmaOrderStatus` 에 `| "modified"` 추가하고 JSDoc 에 의미(원주문 행 전용 종결 상태 — 이동분으로 닫힘, 새 정정 행은 쓰지 않는다, relay store 가 수량으로 파생) 1~3줄.
    (3) `relay/src/order/notice-status.ts`: STATUS_RANK 에 `modified: 3`, TERMINAL 집합에 "modified" 추가. 주석의 「7종」을 「8종」으로, 순위 설명 줄에 종결 3 목록에 modified 추가. statusOf 는 바꾸지 않는다(M 통보 자체는 새 행의 「접수」다 — 'modified' 는 store 가 수량으로만 파생한다는 한 줄을 statusOf 의 "M" case 주석에 덧붙인다).
    (4) `webapp/src/lib/orders-api.ts`: STATUS_LABELS 에 `modified: { label: "정정", tone: "muted" }` (per E — 기존 단어·톤 재사용, 새 시각 요소 없음이라 목업 게이트 대상 아님: 기존 StatusTag 의 라벨 문자열 1종 추가). 주석 「7종」→「8종」. `orderDisplayStatus` 는 복원 행 status 가 'modified' 면 라이브 프레임보다 먼저 그 라벨을 돌려준다 — 근거 주석: 원주문 행의 최신 라이브 프레임은 정정 전의 A/E 라 그대로 두면 「접수」·「체결」로 남는다(M 통보는 새 주문번호로 오므로 원주문 행의 라이브를 갱신하지 않는다). 다른 status 의 동작은 불변.
    (5) `webapp/src/lib/__tests__/orders-api.test.ts` 의 orderDisplayStatus describe 에 <behavior> 3케이스를 추가(기존 케이스 무수정). 먼저 테스트를 추가해 RED 확인 → 구현 → GREEN.
    (6) `pnpm --filter @gh-radar/shared build` 후 relay·server·webapp typecheck 로 `Record<DmaOrderStatus,…>` 소비처가 전부 채워졌는지 확인한다(server `services/dma-orders.ts` 는 타입만 참조 — 수정 불필요 예상, 컴파일이 가리키면 채운다). server `GET /api/orders` select 에 modified_qty 는 추가하지 않는다(per E).
    커밋: `fix(quick-260923-m23): dma_orders 정정 이동 수량(modified_qty)·'modified' 상태 추가 — 마이그레이션 · shared 상태 · relay 순위 · 웹앱 「정정」 표시` (한글, Co-Authored-By 없음, push 없음).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/server run typecheck && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp test src/lib/__tests__/orders-api.test.ts && pnpm --filter @gh-radar/relay test && test "$(grep -c 'modified_qty' supabase/migrations/20260923120000_dma_orders_modified.sql)" -ge 2 && test "$(grep -c "'modified'" supabase/migrations/20260923120000_dma_orders_modified.sql)" -ge 1 && test "$(grep -c '"modified"' packages/shared/src/relay.ts)" -ge 1 && test "$(grep -c 'modified: 3' relay/src/order/notice-status.ts)" -eq 1 && test "$(grep -c 'modified: { label: "정정", tone: "muted" }' webapp/src/lib/orders-api.ts)" -eq 1 && MIGMOD="$(git diff --name-only HEAD -- supabase/migrations)" && test -z "$MIGMOD"</automated>
  </verify>
  <done>마이그레이션 파일이 존재하고(원격 미적용), DmaOrderStatus 8종 · STATUS_RANK/TERMINAL 에 modified · 웹앱 「정정」 라벨과 우선 규칙이 들어가 shared build + relay/server/webapp typecheck 초록, 웹앱 orders-api 테스트(신규 3 포함) 초록, relay 전체 테스트 무수정 초록. 원자 커밋 1개(생성물 relay/src/generated 미포함).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: store 수량 변경 일반 CAS 경로 — qtyCap · modifiedDelta · 'modified' 파생 + DB 회귀 (B · D)</name>
  <files>relay/src/store/orders.ts, relay/tests/helpers/fake-dma-orders.ts, relay/tests/order-store.test.ts, supabase/tests/dma_orders_modified.test.sql, scripts/verify-dma-orders-price-check.sh</files>
  <read_first>
    - relay/src/store/orders.ts L144-184, L259-264, L300-320, L384-420, L657-806, L918-966, L1060-1104, L1157-1171 (앵커 지도 참조 — 파일 전체를 읽지 않는다)
    - relay/tests/helpers/fake-dma-orders.ts L40-110
    - relay/tests/order-store.test.ts L63-102, L1206-1406
    - scripts/verify-dma-orders-price-check.sh 전체(140줄), supabase/tests/dma_orders_price_check.test.sql L1-30
  </read_first>
  <behavior>
    (fill sink 단위 — 진짜 supabaseOrderFillSink + fakeDmaOrders, 새 describe 「M23 — 정정 수량 CAS」)
    - qtyCap 만: 행 qty 13827 · filled 0 · accepted 에 qtyCap 10817 → qty 10817, status accepted 그대로, modified_qty 0
    - qtyCap 이 행 qty 이상(예 qty 10 에 qtyCap 20)이고 다른 수량 입력 없음 → 쓰기 없음 · `{applied:false}` (올리지 않는다)
    - 늦은 캡 재판정: qty 130 · filled 60 · partially_filled 에 `{qtyCap:60, status:'accepted', noticeType:'M'}` → qty 60 · status filled
    - 수량 불변 + 패치 status 가 되돌림: qty 10 · filled 10 · filled 행에 `{qtyCap:10, status:'accepted', noticeType:'M'}` → 쓰기 없음 · notice_type 불변 · `{applied:false}` (㊹ 와 같은 결과)
    - 전량 이동: qty 100 · filled 40 · partially_filled 에 modifiedDelta 60 → modified_qty 60 · status 'modified'
    - 일부 이동 후 체결: qty 100 · filled 0 · accepted 에 modifiedDelta 30 → modified_qty 30 · accepted 유지; 이어 fillDelta 70 → filled_qty 70 · status 'filled'
    - clamp: qty 100 · filled 40 · modified 60 · 'modified' 에 modifiedDelta 10 → 쓰기 없음 · `{applied:false}` · warn 1줄, 필드 키는 정확히 column · modifiedDelta · moved (주문번호·계좌 없음)
    - 종결 보호: 'modified' 행에 fillDelta 5 → filled_qty 만 +5, status 'modified' 유지
    - CAS UPDATE 필터에 id · filled_qty · status · qty · modified_qty 가 모두 걸린다
    (큐 계약 — recordingSinks)
    - `{orderRowId, qtyCap: 5}` 와 `{orderRowId, modifiedQtyDelta: 5}` 는 update sink 가 아니라 addFill 로 간다
    - filledQty 절대값 + (filledQtyDelta | qtyCap | modifiedQtyDelta) 동시 → 드롭 + error 1줄
    - qtyCap 0 · -1 · 1.5 → warn 후 없는 것으로 취급, 남은 게 updated_at 뿐이면 「갱신할 필드가 없는 요청」 무시
    - rowPatchOf 는 qtyCap · modifiedQtyDelta 로 컬럼을 만들지 않는다
    (기존) S1-a~h · 상태 단조성 describe · 23505 · 오류 유출 describe 는 단언 수정 없이 초록
  </behavior>
  <action>
    per B. 이름은 기존 것을 유지해 변경면을 줄인다(`OrderFillSink` · `addFill` · `supabaseOrderFillSink` · `ORDER_FILL_CAS_MAX_ATTEMPTS` 그대로) — docstring 만 「체결 조각 누적」에서 「수량 변경 CAS(체결 조각 · 수량 캡 · 정정 이동)」로 넓힌다.
    (1) 타입: `export type OrderQtyChange = { fillDelta?: number; qtyCap?: number; modifiedDelta?: number }` 신설. `OrderFillSink` 두 번째 인자를 `change: OrderQtyChange` 로 바꾼다. `OrderUpdate` 에 `qtyCap?: number`(행 qty 를 이 값 이하로 **내리기만** — 정정확인의 이동 수량, 절대 올리지 않음) 와 `modifiedQtyDelta?: number`(원주문 행의 정정 이동 조각 — modified_qty 에 더함) 를 filledQtyDelta 와 같은 주석 밀도로 추가. `QueueItem.fillDelta` 를 `qtyChange?: OrderQtyChange` 로 바꾼다. `OrderInsertRow.status` 주석 「7종」→「8종」.
    (2) `enqueueUpdate`: 계약 위반 드롭을 「filledQty(절대값)와 CAS 수량 입력(filledQtyDelta · qtyCap · modifiedQtyDelta) 중 하나라도 함께」로 넓힌다(문구에 그 이름들을 적는다; error 1줄 · dropped+1). 세 입력 각각 1 이상의 정수가 아니면 warn(`{column, <필드명>: 값}` — 기존 filledQtyDelta warn 필드 형태 그대로) 후 없는 것으로 친다. 유효한 입력이 하나라도 있으면 큐 항목에 `qtyChange` 를 싣고(필드명 매핑 filledQtyDelta→fillDelta, qtyCap→qtyCap, modifiedQtyDelta→modifiedDelta), 「갱신할 필드가 없는 요청」 판정은 patch 가 updated_at 뿐이고 qtyChange 도 없을 때만. `#drain` 라우팅은 `qtyChange` 유무로. `#applyFill` 시그니처도 change 로. `rowPatchOf` 는 새 두 필드로 컬럼을 만들지 않는다는 문장을 기존 filledQtyDelta 문단에 합친다.
    (3) `supabaseOrderFillSink` 계산(잠긴 설계 B 그대로): readTarget 컬럼에 modified_qty 추가. cur=filled_qty(수 아니면 0) · curMod=modified_qty(수 아니면 0) · qty=행 qty(수 아니면 null) · status. nextFilled = cur + (fillDelta ?? 0). qty 가 null/≤0 이면 캡·이동 계산 불가 → 수량 변화는 fill 만. qty 가 있으면 newQty = qtyCap 있으면 min(qty, qtyCap) 아니면 qty; room = max(0, newQty − nextFilled − curMod); moved = modifiedDelta 있으면 min(modifiedDelta, room) 아니면 0; moved < modifiedDelta 면 warn `{column, modifiedDelta, moved}` 「정정 이동 수량을 남은 잔량으로 잘랐다 — 중복 통보 의심」; nextMod = curMod + moved. 기존 「누적 체결수량이 주문수량을 넘었다」 warn 은 조건(nextFilled > newQty)·필드(column · qty(=newQty) · filledQty) 그대로.
       수량 변화 여부 = fill > 0 또는 newQty ≠ qty 또는 moved > 0.
       · 수량 변화 없음: 일반 update sink 와 **같은 판정**을 한다 — 패치 status 가 있으면 `replaceableStatusesOf(패치 status)` 에 현재 status 가 들 때만 패치(order_no 제외)를 쓰고, 아니면 통째로 무동작 `{applied:false}`(18-33 「막힌 갱신은 통째로」). 패치 status 가 없고 updated_at 외 필드도 없으면 `{applied:false}`. 이것이 ㊹(늦은 M 이 filled 행의 notice_type 을 덮지 않음)와 순수 캡 no-op 을 지킨다.
       · 수량 변화 있음: 파생 derived = (nextFilled + nextMod ≥ newQty) ? (moved > 0 ? 'modified' : 'filled') : (nextFilled > 0 ? 'partially_filled' : 없음). 목표 = derived ?? 패치 status ?? 없음. 목표가 없으면 진행, 있으면 `replaceableStatusesOf(목표).includes(현재 status)` 일 때만 진행. 진행이면 패치(order_no 제외) + 수량 컬럼 + (derived 있으면 status: derived), 막히면 수량 컬럼만. 수량 컬럼 = filled_qty: nextFilled · updated_at · (newQty ≠ qty 면 qty: newQty) · (moved > 0 이면 modified_qty: nextMod).
       CAS 조건: eq id · eq filled_qty cur · (status 있으면 eq status) · (qty 있으면 eq qty) · eq modified_qty curMod. 0행이면 다시 읽기(기존 상한·throw 규율 그대로). 로그는 column·숫자뿐, PostgREST 오류는 safePgError.
       docstring 에 이 규칙과 근거(수량 캡은 CAS 로 가야 하는 이유: E(Modify) 가 먼저 정산한 뒤 늦은 M 이 일반 update 로 오면 단조성 필터에 걸려 qty 까지 버려진다 — GC-WR-01 / 18-25) 그리고 「이 컬럼을 읽으므로 마이그레이션 20260923120000 원격 적용이 relay 배포보다 먼저」를 적는다(기존 「마이그레이션이 없으면 배포 순서 제약도 없다」 문장은 이제 틀리므로 고친다).
    (4) `relay/tests/helpers/fake-dma-orders.ts` DB_DEFAULTS 에 `modified_qty: 0` 추가(DB 기본값 거울) + 머리 주석 ⑤ 갱신.
    (5) `relay/tests/order-store.test.ts`: 기존 직접 호출 3곳(L1289 · L1350 · L1365)의 두 번째 인자 `3` 을 `{ fillDelta: 3 }` 로, recordingSinks addFill 스텁을 `(sel, change, patch)` 로 받아 `filled_qty_delta: change.fillDelta` 와 새 입력(`qty_cap` · `modified_qty_delta`)을 기록하게 바꾼다 — **호출 형태만 바꾸고 기존 단언은 한 글자도 바꾸지 않는다.** <behavior> 의 새 케이스들을 새 describe 로 추가(먼저 RED 확인 → 구현 → GREEN).
    (6) `scripts/verify-dma-orders-price-check.sh` 에 `--test <path>` 옵션 추가(기본값 = 기존 dma_orders_price_check.test.sql, 기존 호출 동작 불변, 파일 존재 검사, `-h` 도움말 줄 갱신; 헤더에 「--test 로 다른 dma_orders 회귀도 돌린다」 한 줄). 안전 경계(원격 접촉 0 · 이미지 pull 0 · trap 정리)는 그대로.
    (7) `supabase/tests/dma_orders_modified.test.sql` 새 pgTAP 회귀(per D, price_check 테스트 형식 그대로 — BEGIN · pgtap 확장 · auth.users 고정 uuid 1행(새 uuid) · plan(N) · ROLLBACK): has_column modified_qty · col_not_null · col_default_is 0 · 기존 7종 + 'modified' 8종 status 행 insert lives_ok · 미지 status 'bogus' throws_ok '23514' · modified_qty -1 throws_ok '23514' · status 를 참조하는 CHECK 제약이 정확히 1개(`pg_constraint` 조회 is(...,1)) · 그 이름이 dma_orders_status_check. 단언 설명에 값 튜플을 적는다.
    Docker 데몬이 안 떠 있어 pgTAP 러너가 실패하면 우회하지 말고 멈춰 보고한다.
    커밋: `fix(quick-260923-m23): 수량 변경 일반 CAS 경로 — 정정 수량 캡(qtyCap)·원주문 이동(modifiedDelta)·'modified' 파생 · dma_orders_modified pgTAP 회귀` (한글, Co-Authored-By 없음, push 없음).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test tests/order-store.test.ts tests/ws-order.test.ts && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && bash scripts/verify-dma-orders-price-check.sh --test supabase/tests/dma_orders_modified.test.sql && bash scripts/verify-dma-orders-price-check.sh && test "$(grep -c 'modified_qty' relay/src/store/orders.ts)" -ge 3 && test "$(grep -c 'qtyChange' relay/src/store/orders.ts)" -ge 3 && test "$(grep -c 'export type OrderQtyChange' relay/src/store/orders.ts)" -eq 1 && test "$(grep -c 'modified_qty' relay/tests/helpers/fake-dma-orders.ts)" -ge 1 && MIGMOD="$(git diff --name-only HEAD -- supabase/migrations)" && test -z "$MIGMOD"</automated>
  </verify>
  <done>store 가 fillDelta·qtyCap·modifiedDelta 를 한 CAS 경로로 처리하고 'modified'/'filled' 를 수량으로 파생한다. 새 fill sink·큐 계약 테스트 초록, 기존 order-store·ws-order 테스트 단언 무수정 초록, 두 pgTAP 회귀(신규 + 기존 price check) PASS, relay typecheck(본체·테스트) 초록. 원자 커밋 1개.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: order-handler M 통보 적용 — movedQty 순수함수 · 새 행 qty 캡 · 원주문 이동 1회 · 자동 M insert (A · C · F)</name>
  <files>relay/src/ws/order-handler.ts, relay/tests/ws-order.test.ts</files>
  <read_first>
    - relay/src/ws/order-handler.ts 앵커 지도의 구간만: L111-120, L169-213, L342-378, L451-529, L576-915, L1199-1275, L1339-1422
    - relay/tests/ws-order.test.ts L120-164, L227-240, L745-765, L1158-1180, L1680-1830, L2120-2175
    - relay/src/store/orders.ts 의 Task 2 결과 중 OrderUpdate 새 필드 주석만
  </read_first>
  <behavior>
    (순수함수 `modifyMovedQty(states, notice)` — 새 describe)
    - 67 먼저: states 의 unf 에 새 주문번호 Y 행(orderQty 10817)이 있으면 → { movedQty: 10817, source: "new" }
    - 51 먼저: Y 없고 원주문 X 행(unfilledQty 10817)이 있으면, notice.quantity 13827 → { movedQty: 10817, source: "org" } — 위와 같은 값
    - 둘 다 없음 → { movedQty: 13827, source: "fallback" }
    - noticeType 이 M 이 아니면 null · 주문번호 빈 값이면 null · 정규화한 주문번호 == 정규화한 원주문번호면 null
    - 주문번호 표기 차이(선행 0 유무)는 normalizeOrderNo 로 같게 본다 · ISIN 이 다른 unf 행은 매칭하지 않는다 · 계좌가 여럿이어도 전 계좌 unf 를 본다
    (하네스 — 진짜 store: restartWithRealStore + vi.spyOn(hub, "getAccountStates"))
    - 사고 재현: 원주문 X 행(매도 · qty 18249 · filled 7432 · partially_filled) seed, hub 원주문 X unfilled 10817 → order.modify(side S · qty 13827 · orgOrderNo X) → M(orderNo Y · orgOrderNo X · Modify · quantity 13827) → flush → 정정 행 qty 10817 · accepted · order_no Y; 원주문 modified_qty 10817 · 'modified' → 대기 밖 E(Y · 10817) → flush → 정정 행 filled_qty 10817 · filled
    - 일부정정: X qty 100 · accepted, hub X unfilled 100 → 정정 30 → M(quantity 30) → 정정 행 qty 30; X modified_qty 30 · accepted → E(X · 70) → X filled
    - 전량 정정 + 선체결: X qty 100, E(X · 40) 먼저 → hub X unfilled 60 → 정정 130 → M(quantity 130) → 정정 행 qty 60; X filled_qty 40 · modified_qty 60 · 'modified'
    - 순서 역전: 정정 130(X 잔량 60) → E(Modify · Y · 60) 이 대기를 정산(정정 행 partially_filled · filled 60) → 늦은 M(Y · X · 130) → 정정 행 qty 60 · filled
    - 이중 적용 방지: 같은 M 을 두 번 밀어도 원주문 이동 갱신(modifiedQtyDelta)은 1건만 큐에 들어가고 두 번째는 info 1줄
    (하네스 — 스텁 store mkOrderStore)
    - 자동주문 M: origin LimitChaser 의 M(Y · X · quantity 130), hub X unfilled 60, getLimitChasers 로 계좌 제공 → inserts[0] 이 { orderType: "M", qty: 60, orgOrderNo: X, orderNo: Y }
    - 원주문 행이 없으면(WinForms 원주문) 이동 갱신 없음 · 새 행 없음 · info 1줄 · 로그에 계좌번호 없음
    (기존) ㉟ ㊹ ㊺ · e1m describe · narrowPending · isCancelLikeNotice 테스트 단언 무수정 초록
  </behavior>
  <action>
    per A · C. 먼저 <behavior> 테스트를 추가해 RED 를 확인한 뒤 구현한다.
    (1) 인터페이스: `OrderNoticeSource` 에 `getAccountStates(userId: string): RelayAccountState[]` 추가(주석: M 통보 이동 수량의 정본 — hub 66/67 누적 상태, 동기). `@gh-radar/shared` import 에 RelayAccountState 추가. ws-order.test.ts L758 직접 호출 hub 스텁에 `getAccountStates: () => []` 추가(타입 맞춤, 그 테스트 단언 무수정).
    (2) 순수함수(per A): 파일 하단 「통보 매칭 (순수 함수)」 구역에 `export type ModifyMove = { movedQty: number; source: "new" | "org" | "fallback" }` 와 `export function modifyMovedQty(states: readonly RelayAccountState[], notice: ParsedOrderResp): ModifyMove | null` 를 둔다. 규칙: M 아님 → null; own = normalizeOrderNo(orderNo), org = normalizeOrderNo(orgOrderNo); own 이 "" 거나 own === org 면 null(정정 대상 번호를 식별할 수 없다 — 캡·이동 모두 하지 않는다, ㊺ 같은 에코 방어); 전 계좌 unf 중 isin 이 같고 정규화 orderNo 가 own 인 행이 있고 orderQty ≥ 1 → 그 orderQty("new" — 서버가 캡한 값, 67 먼저); 아니고 org ≠ "" 이며 같은 isin·정규화 orderNo 가 org 인 행이 있으면 min(notice.quantity, 그 행 unfilledQty) 가 1 이상일 때 그 값("org" — 서버와 같은 계산, 51 먼저); 아니면 notice.quantity ≥ 1 이면 notice.quantity("fallback"), 아니면 null. docstring 에 gh-trade 서버 규칙(새 행 수량 = min(정정확인 orderQty, 원주문 잔량) · 일부정정은 원주문 행이 잔량 감소로 살아 있음 · 전량은 원주문번호 removed) 과 「새 행 먼저 보는 이유: 67 이 먼저 왔으면 원주문 잔량은 이미 줄어 있어 min 이 틀린다」를 적는다.
    (3) 리스너 단일 지점(per C.3): `deps.hub.on("order")` 리스너 맨 앞에서 M 통보면 `modifyMovedQty(deps.hub.getAccountStates(userId), notice)` 를 **동기 1회** 계산한다(try/catch — 예외면 logger.error 에 orderNo·noticeType 만 싣고 null 로 진행, GC-WR-01 규율: 통보 1건의 파손이 프로세스를 내리지 않게). source 가 "fallback" 이면 warn 1줄(orderNo · orgOrderNo · noticeType · quantity — 계좌번호 금지, T-16-45; hub 상태의 계좌 필드 `a` 를 로그에 싣지 않는다). 결과가 있고 원주문번호가 있으면 `void recordModifyMove(userId, notice, movedQty).catch(...)`(recordUnmatched 와 같은 최후 그물 문구 규율). 그 뒤 기존 흐름: hit 이면 `hit.entry.settle(notice, movedQty)`, 아니면 `recordUnmatched(userId, notice, candidates.length > 0, movedQty)`. 이 한 곳에서 계산한 같은 값이 세 경로로 간다 — 비동기 조회 뒤에 hub 를 다시 읽지 않는다(그 사이 67 이 오면 값이 달라진다는 근거 주석).
    (4) `recordModifyMove(userId, notice, movedQty)`(per C.2 · C.3): relayTrail 항목에 `modifyMoves: Set<string>`(정규화한 M 주문번호, KST 당일 스코프 — trailOf 가 날짜 바뀌면 함께 비운다)를 추가하고 이미 있으면 info 1줄 후 반환(「같은 정정확인이 두 번 왔다 — 원주문 이동은 1회만」), 없으면 먼저 등록한 뒤 진행. 원주문 조회는 `settleOriginal` L698-739 의 조회부(orderNoLookupKeys → inflight 재사용 → findIdByOrderNo 순회)를 private 헬퍼(예: `findOriginalRowId(userId, keys)` — 결과 `{kind:"row",id} | {kind:"none"} | {kind:"lookup-failed", err}`)로 추출해 settleOriginal 과 공유한다. 오류 로그 문구는 각 호출자에 남겨 settleOriginal 의 기존 로그·동작을 한 글자도 바꾸지 않는다(e1m 테스트가 잠근다). row → `enqueueUpdate({ orderRowId, modifiedQtyDelta: movedQty })`(통보 필드 notice_type·message·orderNo 는 싣지 않는다 — M 은 새 주문의 통보이고 원주문 행의 notice 필드는 그 행 자신의 것이다) + info 1줄; none → info 1줄(「원주문 행 없음 — WinForms 등 relay 밖 원주문, 새 행을 만들지 않는다」); lookup-failed → error(safePgError) + `{ orderNo: keys[0], userId, modifiedQtyDelta: movedQty }` 열화 갱신(S-5). 로그 필드는 orderNo · orgOrderNo · noticeType · movedQty 정도, 계좌번호 없음.
    (5) 새 행 qty 캡(per C.1):
       · `PendingOrder.settle` 을 `(notice, movedQty?: number) => void` 로 넓히고 `finish` 가 받는다. finish 의 기존 enqueue 는 그대로 두고, notice 가 M 이고 movedQty 가 있으면 **별도 항목** `enqueueUpdate({ orderRowId, qtyCap: movedQty })` 를 이어서 넣는다(근거 주석: 기존 항목은 order_no 를 채워야 해서 일반 sink 로 가야 하고, CAS sink 는 order_no 를 싣지 않는다; 큐는 직렬이라 순서가 보장된다). order.result 프레임은 불변.
       · `recordUnmatched(userId, notice, hadCandidates, movedQty?)`: movedQty 가 있을 때만 `qtyCap: movedQty` 를 수동 분기의 기존 행 갱신(L609)과 열화 갱신(L601), 자동 분기의 row(L652)·lookup-failed(L660) 갱신 객체에 함께 싣는다. 나머지 판정·로그 문구 불변.
       · `ensureRow`/`insertOnly`/`autoInsertRow` 에 movedQty 를 전달한다. `autoInsertRow` 는 M 통보면 orderType "M", qty 는 movedQty(없으면 notice.quantity), 수량 가드는 그 실제 qty 로 검사한다. 자동 insert 행의 orgOrderNo 는 기존대로. 주석의 「이 경로의 행은 언제나 신규다」와 `isCancelLikeNotice` docstring 의 「정정확인 M 의 자동 insert 동작은 이 quick 의 범위가 아니라」 문장을 현재 동작(m23: order_type M · qty = 이동 수량)으로 고친다. 「7종」 주석이 있으면 「8종」으로.
    (6) 이중 적용 점검 결론을 리스너 주석에 남긴다: 한 통보는 settle 과 recordUnmatched 중 **정확히 하나**만 탄다(hit 이면 return) — 그래서 새 행 캡은 각 경로에 두고, 원주문 이동만 리스너 한 곳 + modifyMoves Set 으로 1회를 보장, CAS clamp 가 2선.
    (7) 테스트는 <behavior> 대로 ws-order.test.ts 에 추가: 순수함수 describe(mkNotice 재사용, RelayAccountState 픽스처는 `{t:"acct", a:"ACCT-TEST", snap:true, hold:[], unf:[...], rm:[], st:""}` 형태 — 필요한 RelayUnfilled 필드 채움) + 하네스 describe(restartWithRealStore 로 seed 행을 넣고 FakeRow 에 qty·filled_qty·status·order_no·user_id·created_at(당일) 를 채운다; 정정 행 찾기는 `fake.rows.find(r => r.order_no === Y)`; 통보 뒤 조회 완료를 waitFor 로 기다린 뒤 flushIo · store.flushNow — pushLateModifyConfirm 패턴). 로그 스파이는 필요한 곳만 mock 한다. 기존 테스트 단언은 바꾸지 않는다 — 기존 테스트가 깨지면 구현을 고친다.
    커밋: `fix(quick-260923-m23): 정정확인 M 반영 — hub 66/67 기준 이동 수량으로 정정 행 qty 캡 · 원주문 행 modified_qty 기록(1회) · 자동 정정 행 order_type M` (한글, Co-Authored-By 없음, push 없음).
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/relay test && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && test "$(grep -c 'export function modifyMovedQty' relay/src/ws/order-handler.ts)" -eq 1 && test "$(grep -c 'getAccountStates' relay/src/ws/order-handler.ts)" -ge 2 && test "$(grep -c 'modifiedQtyDelta' relay/src/ws/order-handler.ts)" -ge 2 && test "$(grep -c 'qtyCap' relay/src/ws/order-handler.ts)" -ge 4 && test "$(grep -c 'modifyMoves' relay/src/ws/order-handler.ts)" -ge 2 && test "$(grep -c 'modifyMovedQty' relay/tests/ws-order.test.ts)" -ge 3</automated>
  </verify>
  <done>M 통보가 hub 기준 이동 수량으로 새 정정 행 qty 를 캡하고 원주문 행에 modified_qty 를 1회 기록하며, 자동 M 은 order_type M · qty 이동 수량으로 insert 된다. 사고 재현·일부정정·선체결·순서 역전·이중 적용·자동 M 테스트 초록, 기존 relay 테스트 전부 단언 무수정 초록, typecheck(본체·테스트) 초록. 원자 커밋 1개.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트웨이 51/66/67 → relay | 정정확인 quantity 는 요청 에코(신뢰 불가 값), 66/67 은 서버 캡이 반영된 정본. 중복·순서 역전 가능 |
| relay → Supabase dma_orders (service role) | relay 단독 writer. 조건부 UPDATE(CAS)로 원자성 확보 |
| relay 로그(stdout/Cloud Logging) | 감사 두 번째 사본. 계좌번호 금지(T-16-45) |
| 마이그레이션 원격 적용 ↔ relay 배포 | 새 relay 가 새 컬럼을 읽는다 — 순서가 뒤집히면 쓰기 경로가 죽는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-m23-01 | Information disclosure | order-handler 신규 로그(폴백 warn · 이동 info · 중복 info) · fill sink clamp warn | medium | mitigate | 로그 필드를 orderNo · orgOrderNo · noticeType · 수량 숫자로 한정, hub 상태의 계좌 필드 `a` 미로깅, PostgREST 오류는 safePgError. clamp warn 필드 키를 테스트로 잠근다(column · modifiedDelta · moved) |
| T-m23-02 | Tampering | 원주문 modified_qty (중복 M 통보) | high | mitigate | 리스너 단일 지점 + 사용자·당일 modifyMoves Set 으로 M 주문번호당 1회, CAS clamp min(modifiedDelta, 남은 잔량) 2선, 이중 적용 테스트 |
| T-m23-03 | Elevation of privilege | 원주문 행 조회·열화 셀렉터 | high | mitigate | findIdByOrderNo(userId, …) 사용자·주문번호·당일 3축 그대로, 열화 갱신은 userId 없는 order_no 셀렉터를 selectorOf 가 드롭(gap 1) |
| T-m23-04 | Tampering | qtyCap 으로 행 qty 상향·0 | medium | mitigate | newQty = min(행 qty, qtyCap) 로 내리기만, qtyCap 1 미만·비정수는 enqueueUpdate 가 warn 후 무시(테스트) |
| T-m23-05 | Denial of service | relay 가 마이그레이션 전 배포되면 readTarget 의 modified_qty select 실패 → CAS throw → 재시도 후 체결 갱신 드롭 | high | mitigate | 배포 순서 사람 게이트(마이그레이션 원격 적용 → relay → push)를 PLAN·SUMMARY·마이그레이션 헤더·fill sink docstring 에 명시. 이 quick 은 원격 적용·배포·push 를 하지 않는다 |
| T-m23-06 | Repudiation | WinForms 원주문(행 없음)의 정정 | low | accept | 새 행을 만들지 않고 info 1줄 — relay stdout 이 감사 두 번째 사본(D-24). relay 가 낸 주문이 아니므로 기록 대상 아님(e1m A1 과 같은 판단) |
| T-m23-07 | Tampering | 51 이 hub 상태보다 먼저·나중 도착 | medium | mitigate | 새 행(orderQty) 우선 → 원주문 min(요청, 잔량) → 폴백 순서의 순수함수 + 두 순서 동일값 테스트, 계산은 수신 틱에 동기 1회 |
</threat_model>

<verification>
전체 게이트(Task 3 커밋 후, 저장소 루트):
- `pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/relay run typecheck && pnpm --filter @gh-radar/relay run typecheck:tests && pnpm --filter @gh-radar/server run typecheck && pnpm --filter @gh-radar/webapp run typecheck`
- `pnpm --filter @gh-radar/relay test` (기존 534 + 신규 전부 초록, 기존 단언 수정 0)
- `pnpm --filter @gh-radar/webapp test src/lib/__tests__/orders-api.test.ts`
- `bash scripts/verify-dma-orders-price-check.sh --test supabase/tests/dma_orders_modified.test.sql` 와 `bash scripts/verify-dma-orders-price-check.sh` 둘 다 `# RESULT: PASS`
- `git log --oneline -3` 이 `fix(quick-260923-m23):` 커밋 3개이고, `git show --stat HEAD~2..HEAD` 에 relay/src/generated 경로가 없다. Co-Authored-By 트레일러 없음. push 하지 않았다(`git status -sb` 가 origin 대비 ahead 3).
- 원격 DB 접촉 0: `supabase db push`·원격 psql 을 실행하지 않았다.

**배포 순서 메모 (사람 게이트 — 이 quick 은 실행하지 않는다):**
1. 마이그레이션 `20260923120000_dma_orders_modified.sql` 원격 적용(사전: 원격 status 값이 7종 밖인 행 0건 확인, 적용 후 CHECK 1개·컬럼 기본값 확인).
2. relay 배포 — 새 코드가 `modified_qty` 를 읽으므로 **반드시 1 뒤**. relay 에는 이전부터 미배포로 쌓인 변경(STATE: R2~R4 · e1m 등)이 함께 나간다.
3. 검증 후 push(= webapp 프로덕션 배포, server 포함). webapp 「정정」 라벨은 1 이전에는 나타날 행이 없어 무해하지만 순서는 1 → 2 → 3 을 지킨다. 백엔드가 막히면 push 하지 않는다.
</verification>

<success_criteria>
- 결함 ① 해소: M 통보 뒤 새 정정 행 qty = 이동 수량(서버 캡과 동일), 이후 전량 체결 시 filled — 51/67 도착 순서·E(Modify) 선정산 순서 모두.
- 결함 ② 해소: 원주문 행에 modified_qty 가 M 1건당 1회 기록되고, 체결+이동 = 주문수량이면 이동으로 닫힌 경우 'modified', 체결로 닫힌 경우 'filled'.
- 자동 M insert 가 order_type M · qty 이동 수량.
- 스키마: 추가 전용 마이그레이션 1개 + pgTAP 회귀 PASS, 원격 미적용.
- 표시: 'modified' 행은 「정정」(muted), 라이브 A/E 보다 우선.
- 기존 테스트 단언 수정 0, 관련 workspace typecheck 초록, 태스크별 한글 원자 커밋 3개, push 없음.

Source coverage audit:
| Source | Item | Plan 위치 |
|--------|------|-----------|
| CONTEXT A | hub 66/67 정본 · getAccountStates · normalizeOrderNo · 새행→원주문→폴백 · 순수함수 테스트 | Task 3 (1)(2)(3)(7) |
| CONTEXT B | CAS 일반화 입력·계산·상태 파생·단조성·CAS 조건·큐 계약 | Task 2 (1)-(3)(5) |
| CONTEXT C.1 | finish · recordUnmatched 수동/자동 · ensureRow · autoInsertRow M | Task 3 (5) |
| CONTEXT C.2 | 원주문 이동 · 행 없음 info · 조회 실패 열화 | Task 3 (4) |
| CONTEXT C.3 | 이중 적용 방지 단일 지점 + clamp 2선 | Task 3 (3)(4)(6) · Task 2 (3) |
| CONTEXT D | 마이그레이션 · 제약 이름 조회 · 헤더 관례 · 원격 미적용 · pgTAP | Task 1 (1) · Task 2 (6)(7) |
| CONTEXT E | DmaOrderStatus · STATUS_RANK/TERMINAL · STATUS_LABELS · orderDisplayStatus · server select 무변경 | Task 1 (2)-(6) |
| CONTEXT F | 사고 재현 · 일부정정 · 선체결 · 순서 · hub 두 순서 · 자동 M · fill sink CAS · 실행 명령 | Task 2 (5) · Task 3 (7) · verification |
| Constraints | 한글 주석 · 계좌번호 금지 · safePgError · 원자 커밋 · Co-Authored-By 없음 · push 없음 · 배포 순서 메모 | 전 태스크 · threat_model · verification |
</success_criteria>

<output>
Create `.planning/quick/260923-m23-dma-orders/260923-m23-SUMMARY.md` when done — 커밋 3개 해시, 테스트 수(relay 전/후), pgTAP 결과, 배포 순서 사람 게이트(마이그레이션 → relay → push)와 「원격 미적용 · relay 미배포 · push 안 함」 상태를 명시.
</output>
