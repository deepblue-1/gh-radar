---
phase: quick-260924-vj1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/src/styles/globals.css
  - webapp/src/lib/chart-colors.ts
  - webapp/src/lib/__tests__/chart-colors.test.ts
  - webapp/src/components/ui/card.tsx
  - webapp/src/components/ui/button.tsx
  - webapp/src/components/ui/badge.tsx
  - webapp/src/components/ui/input.tsx
  - webapp/src/components/ui/textarea.tsx
  - webapp/src/components/ui/input-group.tsx
  - webapp/src/components/ui/checkbox.tsx
  - webapp/src/components/ui/toggle.tsx
  - webapp/src/components/ui/tabs.tsx
  - webapp/src/components/layout/app-shell.tsx
  - webapp/src/components/layout/app-header.tsx
  - webapp/src/components/layout/app-sidebar.tsx
  - webapp/src/components/layout/user-section.tsx
  - webapp/src/app/login/page.tsx
  - webapp/src/components/home/solo-card.tsx
  - webapp/src/components/home/theme-card.tsx
  - webapp/src/components/scanner/scanner-table.tsx
  - webapp/src/components/scanner/scanner-skeleton.tsx
  - webapp/src/components/watchlist/watchlist-table.tsx
  - webapp/src/components/watchlist/watchlist-skeleton.tsx
  - webapp/src/components/theme/theme-chips.tsx
  - webapp/src/components/theme/theme-edit-dialog.tsx
  - webapp/src/components/chat/message-assistant.tsx
  - webapp/src/components/chat/conversation-list.tsx
  - webapp/src/components/chat/chat-fab.tsx
  - webapp/src/components/search/search-trigger.tsx
  - webapp/src/components/stock/detail-bands.tsx
  - webapp/src/components/stock/stock-detail-client.tsx
  - webapp/src/components/stock/stock-detail-tabs.tsx
  - webapp/src/components/stock/stock-hero.tsx
  - webapp/src/components/stock/stock-daily-chart-section.tsx
  - webapp/src/components/stock/stock-daily-chart.tsx
  - webapp/src/components/stock/stock-stats-grid.tsx
  - webapp/src/components/stock/stock-limit-up-section.tsx
  - webapp/src/components/stock/stock-comovement-section.tsx
  - webapp/src/components/stock/stock-news-section.tsx
  - webapp/src/components/stock/stock-discussion-section.tsx
  - webapp/src/components/stock/stock-orderbook-section.tsx
  - webapp/src/components/orderbook/orderbook-ladder.tsx
  - webapp/src/components/orderbook/account-panel.tsx
  - webapp/src/components/orderbook/order-panel.tsx
  - webapp/src/components/orderbook/relay-status-bar.tsx
  - webapp/src/components/trading/limit-chaser-form.tsx
  - webapp/src/components/trading/latch-led.tsx
  - webapp/src/components/trading/exchange-tag.tsx
  - webapp/src/components/trading/dirty-action-bar.tsx
  - webapp/src/components/trading/vi-order-list.tsx
  - webapp/src/components/trading/today-orders-card.tsx
  - webapp/src/components/trading/strategy-status-card.tsx
  - webapp/src/components/trading/dma-gate.tsx
  - webapp/src/components/trading/card/strategy-card.tsx
  - webapp/src/components/trading/card/card-header.tsx
  - webapp/src/components/trading/card/card-tabs.tsx
  - webapp/src/components/trading/card/quote-grid-10.tsx
  - webapp/src/components/trading/card/manual-order-form.tsx
  - webapp/src/components/trading/card/stock-info-modal.tsx
  - webapp/src/components/trading/workbench/workbench-status-bar.tsx
  - webapp/src/components/trading/workbench/vi-trigger-strip.tsx
  - webapp/src/components/trading/workbench/breakout-strip.tsx
  - webapp/src/components/trading/workbench/stock-add-bar.tsx
  - webapp/src/components/trading/workbench/shared-panels.tsx
  - webapp/src/components/trading/workbench/card-grid.tsx
  - webapp/src/components/trading/workbench/vi-settings-rows.tsx
  - webapp/src/components/stock/__tests__/stock-hero.test.tsx
  - webapp/src/components/orderbook/__tests__/orderbook-ladder-chaser.test.tsx
  - webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx
autonomous: true
requirements: [VJ1-A, VJ1-B, VJ1-C, VJ1-D]

estimate:
  tokens: 320000
  raw_tokens: 320000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "다크·라이트 두 테마 모두 B 팔레트로 칠해진다 — 다크 바탕 #17171c · 카드 #202027 · raised #2c2c35, 라이트 본문면 #f2f4f6 위 흰 카드(종목상세 비-호가 탭만 흰 바탕 + #f2f4f6 띠). 상승 #f04452 · 하락 #3485fa(다크)/#3182f6(라이트). `:root` 와 `.dark` 가 같은 토큰 집합을 빠짐없이 정의한다 (VJ1-A)"
    - "카드·패널 면은 테두리 대신 명도 단계로 갈린다 — `--border` 는 B 헤어라인(다크 rgba(255,255,255,.06) · 라이트 #f2f4f6)이 되어 면 외곽선은 사실상 사라지고 행 구분선만 가는 선으로 남는다. radius 는 카드 20 · 박스 16 · 컨트롤 12 · 알약 999 (VJ1-A)"
    - "숫자(`.mono`)는 Geist Mono 가 아니라 Pretendard + tabular-nums 로 그려지고 자릿수 정렬은 유지된다 (VJ1-A)"
    - "종목상세: 히어로 가격 30px/700 · 코드 raised 칩 · 16px semibold 탭 바(2px fg 밑줄 + 옅은 1px 기준선) · 차트/종목정보/뉴스토론 섹션은 풀폭 평면 블록 + 12px 띠 · 라이트 호가주문 탭은 #f2f4f6 면 위 흰 카드 (VJ1-B)"
    - "폰(<768) 종목상세 하단에 고정 빨강 「주문하기」 CTA(54px · radius 16)가 있고 누르면 호가주문 탭으로 전환되며, 호가주문 탭에서는 사라진다. AI 챗 FAB 와 겹치지 않는다 (VJ1-B)"
    - "트레이딩: 폰 1단 사다리 행 44px(박스 440 = 44 × 10 쌍 유지) · 잔량 막대는 둥근 틴트 박스(`--ask-bar`/`--bid-bar`) · 세그먼트는 알약 트랙 + 선택 면 · 카드/상태줄/스트립은 무테 면 (VJ1-C)"
    - "§2.2b 밴드 경계(본문 700·830·992 · wb 680) · 3단 호가표 400px 하한 · `--lw` 76px(≥992 104px) · 더티 액션 바 포털 · `container-type` 위치가 그대로이고, `lc` 카드 안에서는 가로 폭을 키우는 변경(글자 크기·굵기·가로 패딩·열 폭·gap)이 0 이다 (VJ1-D)"
    - "게이트: `pnpm --filter @gh-radar/webapp run typecheck` 0 · `pnpm --filter @gh-radar/webapp run test` 전량 초록 · 차트 팔레트는 hex/rgba 만 (VJ1-D)"
  artifacts:
    - path: "webapp/src/styles/globals.css"
      provides: "B 토큰(라이트 `:root` + `.dark`) · `.mono` → `--font-num` · `.card-shadow` none · `.tbl-wrap` B 표 · 종목상세 띠/평면화 규칙 · 라이트 종목상세 흰 본문(`main:has`) · 폰 FAB 들어올림"
      contains: "--surface"
    - path: "webapp/src/components/stock/detail-bands.tsx"
      provides: "`DetailBands` — 자식 섹션마다 `data-detail-band` 풀폭 블록으로 감싸고 12px `--band` 틈으로 가른다"
      contains: "data-detail-band"
    - path: "webapp/src/components/stock/stock-detail-tabs.tsx"
      provides: "B 탭 바 + 폰 주문하기 CTA(`detail-order-cta-bar`) + 라이트 호가주문 회색 면"
      contains: "detail-order-cta"
    - path: "webapp/src/lib/chart-colors.ts"
      provides: "B hex 차트 팔레트 (lightweight-charts oklch 거부 회피)"
      contains: "#f04452"
    - path: "webapp/src/components/orderbook/orderbook-ladder.tsx"
      provides: "폰 44px 행 · 440 박스 · 토큰 막대"
      contains: "h-[440px]"
  key_links:
    - from: "app-shell.tsx `main`"
      to: "globals.css `--surface` + `main:has([data-page-surface=\"plain\"])`"
      via: "라이트 본문면은 #f2f4f6, 종목상세 루트가 `data-page-surface=\"plain\"` 이면 흰 `--bg`"
    - from: "stock-detail-tabs.tsx CTA 버튼"
      to: "`handleValueChange('orderbook')`"
      via: "기존 탭 전환 경로(pushState + scrollIntoView) 재사용 — 새 내비게이션 경로 0"
    - from: "globals.css `[data-detail-band]`"
      to: "섹션 루트 평면화 + 안쪽 `--card` → `--muted` 재정의"
      via: "레이어 밖 규칙이 Tailwind utilities 레이어를 이긴다"
    - from: "stock-daily-chart.tsx / chat mini-chart"
      to: "chart-colors.ts `getChartPalette`"
      via: "hex 팔레트 주입 — CSS 변수를 JS 로 읽지 않는다"
---

<objective>
토스증권 테마 스케치 001 의 채택안 **B(토스 풀 스킨)** 를 실제 webapp 전역에 **다크·라이트 둘 다** 입힌다. 사용자가 모든 화면(홈·스캐너·테마·관심종목·종목상세 4탭·트레이딩·마이·챗)을 여러 폭에서 보고 판단할 수 있게 하는 실험 브랜치(`theme/toss-b`) 작업이다 — 마음에 안 들면 브랜치째 버린다.

전략: **토큰 + 공용 프리미티브를 먼저 바꿔 모든 화면이 B 를 상속**하게 하고(T1), B 가 구조를 요구하는 두 곳 — 종목상세(T2)와 트레이딩/호가 표면(T3) — 만 컴포넌트를 직접 고친다.

Purpose: 급등 종목을 빠르게 포착하는 트레이더 화면의 인상을 토스증권 수준으로 끌어올리되, 오발주로 직결되는 트레이딩 밀도·잘림 불변식(§2.2b)은 한 픽셀도 건드리지 않는다.
Output: globals.css B 토큰 블록, 프리미티브/셸 리스킨, 종목상세 띠 구조 + 폰 CTA, 트레이딩 표면 리스킨, 갱신된 테스트.

Tracer-first 면제(= `--no-tracer` 상당): 아키텍처가 이미 검증된 앱의 순수 시각 리스킨이라 증명할 수직 슬라이스가 없다. T1 이 끝나는 순간 모든 화면이 이미 B 로 보이므로 T1 자체가 전 계층(토큰 → 프리미티브 → 셸 → 모든 페이지)을 관통한다.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/sketches/001-toss-dark-theme/README.md
@.planning/sketches/001-toss-dark-theme/index.html
@webapp/src/styles/globals.css

## 타이포 교정 (사용자 피드백 2026-09-24 — 우선 적용, 아래 본문과 충돌 시 이 절이 이긴다)
사용자: 「토스 스크린샷 봐도 숫자나 글자 폰트가 그렇게 굵고 크지 않다」. 스크린샷 실측(390pt 기준) 결과 토스는 크기·굵기를 절제하고 위계를 **색 대비(흰 fg vs 회색 muted-fg)** 로 만든다. 스케치의 B 는 이를 과장했었다. 규칙:
- 히어로 가격 30px / 700 (800 금지). 종목명 18px / 600. 등락 15px / 500.
- 섹션 제목 20px / 700 (유지). 탭 16px / 600, 비활성은 회색.
- 값(시세 격자·KPI 제외 일반 숫자) 16px / 500, 라벨 13.5~14px 회색 / 400. KPI 큰 숫자 20px / 700 (extrabold 금지).
- 입력 값 600, 주문 버튼·CTA 600. 사다리 가격 500, 최근 체결가 행만 700.
- 전역: `font-extrabold`(800) 은 이 플랜에서 새로 쓰지 않는다. 기존 코드의 800 도 손대는 표면에서 만나면 700 이하로 낮춘다(`lc` 가로 증가 0 규칙과 동일 방향 — 굵기를 낮추는 건 폭을 줄이므로 안전).

## 작업 위치 · 커밋 규칙 (필독)
- 작업 트리는 git worktree `/Users/alex/repos/gh-radar/.claude/worktrees/toss-b`(브랜치 `theme/toss-b`) 이다. 모든 경로는 이 worktree 기준. **메인 트리 `/Users/alex/repos/gh-radar` 는 다른 세션이 쓰는 중 — 절대 건드리지 않는다.**
- 커밋은 `theme/toss-b` 에만, 태스크당 1커밋, 메시지 한글, `Co-Authored-By` 넣지 않는다(사용자 전역 규칙). **push 금지** — 이 저장소에서 push 는 곧 프로덕션 배포이고, 이 브랜치도 push 하지 않는다.
- `.env*` 가 없으므로 dev 서버 + 백엔드 데이터 확인은 하지 않는다. 스크린샷 검증은 오케스트레이터가 이후에 한다. 게이트는 typecheck + vitest + grep.
- `packages/shared/dist` 가 이 worktree 에 없다 → 첫 typecheck 전에 `pnpm --filter @gh-radar/shared build` 1회.

## 설계 계약 — 스케치 `index.html` 이 정본
`.v-b` 토큰 블록(다크) · `.light.v-b` 블록(라이트) · `.v-b ...` 구조 규칙 · `.light.v-b.pg-trading` / `.light.v-b [data-p="ob"]` 규칙을 먼저 끝까지 읽는다. 아래 표는 그것을 프로덕션 토큰 이름으로 옮긴 매핑이다.

<interfaces>
### B 토큰 매핑 (globals.css — `:root` = 라이트, `.dark` = 다크, 둘 다 전부 정의)

| 토큰 | 다크 | 라이트 | 비고 |
|---|---|---|---|
| `--bg` | #17171c | #ffffff | 페이지·헤더 바탕 |
| `--surface` (신규) | #17171c | #f2f4f6 | AppShell `main` 본문면. 라이트는 회색 면 + 흰 카드(B 작업대 규칙을 전 화면에) |
| `--band` (신규) | #0f0f12 | #f2f4f6 | 종목상세 섹션 사이 12px 띠 |
| `--card` / `--card-fg` | #202027 / #ffffff | #ffffff / #191f28 | |
| `--popover` / `--popover-fg` | #2c2c35 / #ffffff | #ffffff / #191f28 | |
| `--fg` | #ffffff | #191f28 | |
| `--fg-2` (신규) | #e4e4e5 | #333d4b | |
| `--muted-fg` | #9e9ea4 | #8b95a1 | |
| `--faint` (신규) | #6b6b73 | #b0b8c1 | 점선 플레이스홀더·OFF LED 윤곽 |
| `--muted` (= B raised) | #2c2c35 | #f2f4f6 | 입력·세그먼트 트랙·칩·안쪽 박스 |
| `--raised-2` (신규) | #454552 | #e5e8eb | hover·2단 raised |
| `--secondary` / `--secondary-fg` | #2c2c35 / #e4e4e5 | #f2f4f6 / #333d4b | |
| `--border` | rgba(255,255,255,.06) | #f2f4f6 | = B `--line`. 면 외곽선은 사실상 소거, 구분선만 남음 |
| `--border-subtle` | rgba(255,255,255,.04) | #f2f4f6 | |
| `--input` | transparent | transparent | 입력 테두리 소거(포커스·dirty·invalid 테두리 채널은 그대로 살아 있음) |
| `--ring` / `--primary` | #3485fa | #3182f6 | `--primary-fg` #ffffff 양쪽 |
| `--accent` / `--accent-fg` | rgba(52,133,250,.16) / #8ab8ff | #e8f3ff / #1b64da | 의미 유지 = 「primary 틴트」(챗 버블·AI 배지·행 선택 등) |
| `--destructive` / `--destructive-fg` | #f04452 / #ffffff | #f04452 / #ffffff | |
| `--up` / `--down` / `--flat` | #f04452 / #3485fa / #9e9ea4 | #f04452 / #3182f6 / #8b95a1 | 한국식 상승=빨강·하락=파랑 |
| `--up-bg` / `--down-bg` | rgba(240,68,82,.16) / rgba(52,133,250,.16) | #ffeeee / #e8f3ff | |
| `--led-latent` / `--led-armed` | #ffb13d / #2fca73 | #ff9f1a / #03b26c | 래치 LED 축 — 기존 주석 유지 |
| `--new-bg` / `--new-bd` | #3a3014 / #6d5a1d | #fff6d9 / #ffd257 | 신규/미확인 축 — 기존 주석 유지 |
| `--seg-on-bg` / `--seg-on-fg` (신규) | #454552 / #ffffff | #ffffff / #191f28 | 세그먼트 선택 면 |
| `--seg-on-shadow` (신규) | 0 0 #0000 | 0 1px 3px rgba(0,0,0,.1) | 다크는 `none` 대신 `0 0 #0000`(Tailwind shadow 합성에서 무효값이 되지 않게) |
| `--pill-on-bg` / `--pill-on-fg` (신규) | #454552 / #ffffff | #191f28 / #ffffff | 작은 알약 탭·xs 거래소 세그먼트 선택 |
| `--ask-bar` / `--bid-bar` (신규) | rgba(52,133,250,.28) / rgba(240,68,82,.28) | rgba(49,130,246,.16) / rgba(240,68,82,.16) | 호가 잔량 막대 |
| `--side-bg` (신규) | #101013 | #f9fafb | 사이드바·드로어 바탕 |

테마 공통(`:root` 에만): `--r-sm` 6px · `--r` 12px(컨트롤) · `--r-md` 16px(박스·상태줄·표) · `--r-lg` 20px(카드) · `--radius` 0.75rem · `--font-num: var(--font-sans)`.

### 불변 (건드리면 오발주·잘림 회귀)
- globals.css §2.2b 주석 블록과 그 수치 전부, `card-body.tsx` 의 `@min-[700px]/lc:grid-cols-[260px_minmax(0,1fr)]` · `@min-[830px]/lc:grid-cols-[400px_minmax(0,1fr)]` · `@min-[992px]/lc:grid-cols-[460px_minmax(0,1fr)]`, `limit-chaser-form.tsx` 의 `[--lw:76px]` · `@min-[992px]/lc:[--lw:104px]`, `strategy-card.tsx` `LC_CONTAINER_CLASS`, `card-grid.tsx` `COLS_CLASS` · `trading-workbench.tsx` `WB_SINGLE_COLUMN_BELOW`(680), `dirty-action-bar.tsx` 포털, `app-shell.tsx` main 패딩 램프 `p-2 md:p-4 lg:p-6` 와 헤더 `px-2 md:px-4 lg:px-6`.
- **`lc` 카드 안(호가 사다리 · 종목정보 10칸 · 매수/매도/수동 폼 · 카드 헤더) 규칙:** 가로 폭을 먹는 변경 0 — 글자 크기·굵기·letter-spacing·가로 패딩·gap·열 폭·`--lw` 를 키우지 않는다. 허용: 색·radius·면·세로 높이(행/컨트롤 높이·세로 패딩). B 의 굵은 숫자·큰 입력 글자는 `lc` 밖(히어로·종목상세 섹션·작업대 크롬)에만 쓴다. 근거: 본문 700 은 「주문금액이 처음 안 잘리는 폭」이라 여유가 0 이고, 3단 호가표 400 도 실측 하한이다 — 잘린 라벨은 오발주다.
- Pretendard 전환은 가로 폭 면에서 안전하다: tnum 숫자는 Geist Mono 와 거의 같은 폭이고 쉼표·마침표·콜론은 훨씬 좁다 — 같은 문자열이 같거나 좁게 그려진다. 굵기를 올리지 않는 한 새 잘림은 생기지 않는다.
- 면 테두리는 **지우지 말고 색만 투명(`border-transparent`)** 으로 — 1px 기하를 보존해 `lc`/`wb` 컨테이너 폭과 밴드 전환 지점이 한 픽셀도 움직이지 않게 한다.
- 레이어: globals.css 의 레이어 밖 규칙은 Tailwind `utilities` 레이어를 **명시도와 무관하게 이긴다**(기존 `.tbl-wrap` 주석과 같은 원리). `.mono` 같은 레이어 밖 규칙에 `font-weight` 를 넣으면 `font-semibold` 유틸과 상속 굵기를 덮어쓴다 — 넣지 않는다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: B 토큰(라이트·다크) + 공용 프리미티브 + 앱 셸 전역 리스킨</name>
  <files>webapp/src/styles/globals.css, webapp/src/lib/chart-colors.ts, webapp/src/lib/__tests__/chart-colors.test.ts, webapp/src/components/ui/card.tsx, webapp/src/components/ui/button.tsx, webapp/src/components/ui/badge.tsx, webapp/src/components/ui/input.tsx, webapp/src/components/ui/textarea.tsx, webapp/src/components/ui/input-group.tsx, webapp/src/components/ui/checkbox.tsx, webapp/src/components/ui/toggle.tsx, webapp/src/components/ui/tabs.tsx, webapp/src/components/layout/app-shell.tsx, webapp/src/components/layout/app-header.tsx, webapp/src/components/layout/app-sidebar.tsx, webapp/src/components/layout/user-section.tsx, webapp/src/app/login/page.tsx, webapp/src/components/home/solo-card.tsx, webapp/src/components/home/theme-card.tsx, webapp/src/components/scanner/scanner-table.tsx, webapp/src/components/scanner/scanner-skeleton.tsx, webapp/src/components/watchlist/watchlist-table.tsx, webapp/src/components/watchlist/watchlist-skeleton.tsx, webapp/src/components/theme/theme-chips.tsx, webapp/src/components/theme/theme-edit-dialog.tsx, webapp/src/components/chat/message-assistant.tsx, webapp/src/components/chat/conversation-list.tsx, webapp/src/components/search/search-trigger.tsx</files>
  <action>
(VJ1-A) 먼저 스케치 index.html 의 `.v-b` · `.light.v-b` · `.v-b ...` 규칙을 끝까지 읽고, 아래를 순서대로 적용한다.

1) globals.css 토큰. `:root`(라이트)와 `.dark` 에 위 「B 토큰 매핑」 표의 값을 그대로 넣는다. 기존 토큰은 값만 교체하고(oklch → hex/rgba), 신규 토큰(`--surface` `--band` `--fg-2` `--faint` `--raised-2` `--seg-on-bg` `--seg-on-fg` `--seg-on-shadow` `--pill-on-bg` `--pill-on-fg` `--ask-bar` `--bid-bar` `--side-bg`)은 **두 블록 모두**에 추가한다 — 한쪽만 두면 반대 테마에서 조용히 사라진다(기존 `--led-*`/`--new-*` 주석이 경고하는 바로 그 함정). `--led-*` · `--new-*` 의 기존 의미 주석은 보존하고 값만 바꾼다. radius(`--r-sm` 6 · `--r` 12 · `--r-md` 16 · `--r-lg` 20 · `--radius` 0.75rem)와 `--font-num: var(--font-sans)` 는 `:root` 한 곳. shadcn alias(`--background` 등)와 `@theme inline` 매핑은 그대로 둔다. 파일 상단 §9 주석에 「B(토스 풀 스킨, sketch 001) — `--border` = B 헤어라인, 면 구분은 명도 단계」 한 단락을 덧붙인다. §2.2b 블록은 **한 글자도 바꾸지 않는다**.

2) globals.css 공용 규칙. `.mono` 는 font-family 를 `var(--font-num)` 로, `font-variant-numeric: tabular-nums`, `font-feature-settings: 'tnum' 1` 만 남기고 Geist Mono 전용 보정(`slashed-zero`, `'ss01'`, `transform: translateY(0.5px)`)을 걷는다 — font-weight 는 넣지 않는다(interfaces 「레이어」 참고). `.slider-val` 도 `--font-num`. `.card-shadow` 와 `.dark .card-shadow` 는 둘 다 `box-shadow: none`(B `--card-shadow: none`) — 클래스는 소비처 호환을 위해 남긴다. `.tbl-wrap` 은 B 표: 테두리색 transparent(1px 기하 유지) · radius `var(--r-md)` · `overflow-x: auto` 유지(기존 ★ 주석 보존), thead th 배경 transparent · 아래 테두리 0, 행 사이 구분선 0(B `.v-b .tbl td { border-top: 0 }`), 행 hover 는 `color-mix(in oklch, var(--fg) 3%, transparent)`.

3) 차트 팔레트 `lib/chart-colors.ts` — hex 만(lightweight-charts 가 oklch 를 거부, 사용자 메모리 규칙). light: up #f04452 · down #3182f6 · text #8b95a1 · grid #f2f4f6. dark: up #f04452 · down #3485fa · text #9e9ea4 · grid #26262c. bg 는 투명 그대로. B 는 두 테마 상승색이 같으므로 `chart-colors.test.ts` 의 「다크/라이트 분기가 실제로 다른 hex」 단언을 down·text·grid 가 다르다는 단언으로 고치고(상승색 동일은 B 의도라고 주석), 한국식 색 단언은 라이트·다크 둘 다 돌게 넓힌다.

4) 프리미티브. `card.tsx`: 테두리 색 `border-transparent`(기하 유지), radius 는 `--r-lg`(→20) 그대로, `default` variant 의 `card-shadow` 는 유지해도 무해(none) — 주석을 B 기준으로 갱신. `button.tsx`: `outline`·`secondary` 는 `bg-[var(--muted)]` + `border-transparent` + hover `bg-[var(--raised-2)]`(B `.btn` ghost = raised 채움), 크기(h-8/9/10)는 **바꾸지 않는다**(앱 전역 레이아웃 흔들림 방지). `badge.tsx`: `outline`·`secondary` → `bg-[var(--muted)]` + `border-transparent`(글자는 각각 `--muted-fg`/`--secondary-fg`) — B `.pill`. `input.tsx`: `bg-[var(--bg)]` → `bg-[var(--muted)]`; `--input` 이 transparent 가 되어 테두리가 안 보이지만 `data-focus-ring="seamless"` ↔ `focus-visible:border-[var(--ring)]` 쌍은 **그대로 둔다**(링만 걷고 테두리 변화가 없으면 WCAG 2.4.7 위반 — 파일 주석의 경고). `textarea.tsx` · `input-group.tsx`: 채움 `bg-[var(--muted)]`. `checkbox.tsx`: 미체크 윤곽이 `--input` 에 기대고 있어 투명화로 사라지므로 `border-[1.5px] border-[var(--muted-fg)]` + `rounded-[5px]` 로(B `.chk`). `toggle.tsx`: `outline` variant 를 `border-transparent bg-[var(--muted)]` 로, 선택(`data-[state=on]` · `aria-pressed`)은 `--seg-on-bg`/`--seg-on-fg` + `shadow-[var(--seg-on-shadow)]` — toggle-group 의 컨테이너 패딩은 추가하지 않는다(xs 세그먼트 소비처가 `lc` 카드 헤더 안이라 가로 폭 증가 금지). `tabs.tsx`: `default` variant 목록은 `bg-muted` 트랙 유지, 활성 트리거를 `bg-background`/`dark:bg-input/30`/`dark:border-input` 대신 `--seg-on-bg`/`--seg-on-fg`/`--seg-on-shadow` 로. `line` variant 는 손대지 않는다.

5) 앱 셸. `app-shell.tsx`: `main` 에 `bg-[var(--surface)]` 추가(패딩 램프·`min-w-0`·`overflow-auto` 불변), 데스크톱 `aside` 는 `border-transparent bg-[var(--side-bg)]`, 모바일 `SheetContent` 도 `bg-[var(--side-bg)]`. `app-header.tsx`: 아래 테두리 색 transparent(B `--hdr-bd: 0`), px 램프 불변. `app-sidebar.tsx`: `LINK_ACTIVE` → `bg-[var(--muted)] text-[var(--fg)] font-semibold`(B 사이드바 `a.on` = raised), 서브리스트 헤어라인은 유지. `user-section.tsx`: hover 채움 `bg-[var(--bg)]` → `bg-[var(--muted)]`. `app/login/page.tsx`: 래퍼 `bg-[var(--bg)]` → `bg-[var(--surface)]`(라이트에서 흰 위 흰 카드가 사라지는 것 방지).

6) 전역 스윕(stock/trading/orderbook 폴더는 T2·T3 소유라 **제외**). (a) `grep -rn 'border-dashed border-\[var(--border)\]' webapp/src --include='*.tsx'` — 이 점선 플레이스홀더는 **전 폴더** 대상으로 `border-[var(--faint)]` 로 바꾼다(기계적 치환, 헤어라인이 된 `--border` 로는 점선이 안 보인다). (b) `grep -rn 'border border-\[var(--border)\]' webapp/src/components/{home,scanner,watchlist,theme,chat,search,layout,ui} webapp/src/app --include='*.tsx'` 중 채움 클래스가 없는 요소를 하나씩 보고: 칩·버튼류 → `bg-[var(--muted)] border-transparent`, 페이지 면 위에 바로 놓인 표·목록·카드 → `bg-[var(--card)] border-transparent`, 순수 구분선 → 그대로. (c) 같은 폴더들에서 `bg-[var(--bg)]` 를 컨트롤 채움으로 쓰는 곳(conversation-list 입력 · search-trigger kbd) → `bg-[var(--muted)]`. (d) 같은 폴더들에서 `bg-[var(--muted)]` 면이 카드 밖 페이지 면에 바로 놓여 라이트(#f2f4f6 위 #f2f4f6)에서 사라지는 곳이 있으면 `bg-[var(--card)]`. (e) 챗 composer 가 카드 밖 페이지 면에 바로 놓였다면 입력 채움을 `bg-[var(--card)]` 로. home `solo-card`/`theme-card` 의 테두리 색은 transparent 로(그림자는 `.card-shadow` none 으로 이미 소거).

7) 이 태스크가 바꾼 클래스를 단언하던 단위 테스트가 깨지면 새 클래스로 갱신한다(단언 삭제 금지 — 의미를 새 토큰으로 옮긴다). 커밋: `feat(260924-vj1): 토스 B 테마 토큰·프리미티브·앱 셸 전역 적용`(한글 본문, Co-Authored-By 없음). push 금지.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/shared build && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp exec vitest run --passWithNoTests src/lib src/components/ui src/components/layout src/components/home src/components/scanner src/components/theme src/components/watchlist src/components/chat src/components/search && test "$(grep -c -- '--surface:' webapp/src/styles/globals.css)" -ge 2 && test "$(grep -c -- '--seg-on-bg:' webapp/src/styles/globals.css)" -ge 2 && test "$(grep -c -- '--pill-on-bg:' webapp/src/styles/globals.css)" -ge 2 && test "$(grep -c -- '--ask-bar:' webapp/src/styles/globals.css)" -ge 2 && test "$(grep -c -- '--led-armed:' webapp/src/styles/globals.css)" -ge 2 && test "$(grep -c -- '--new-bd:' webapp/src/styles/globals.css)" -ge 2 && grep -q 'font-family: var(--font-num)' webapp/src/styles/globals.css && grep -q '본문 700px' webapp/src/styles/globals.css && grep -q '400px 도 실측 하한' webapp/src/styles/globals.css && grep -q 'bg-\[var(--surface)\]' webapp/src/components/layout/app-shell.tsx && grep -q 'p-2 md:p-4 lg:p-6' webapp/src/components/layout/app-shell.tsx</automated>
  </verify>
  <done>
- `:root` 와 `.dark` 가 표의 B 토큰을 둘 다 정의하고 §2.2b 블록은 무변경이다.
- `.mono` 는 Pretendard(`--font-num`) + tnum, `.card-shadow` 는 none, `.tbl-wrap` 은 B 표.
- Card/Button/Badge/Input/Textarea/InputGroup/Checkbox/Toggle/Tabs 가 B 면(raised 채움 · 무테 · 12/16/20 radius · 세그먼트 선택 면)으로 그려지고 입력 포커스 테두리 채널은 살아 있다.
- 라이트 본문면은 #f2f4f6(`--surface`) + 흰 카드, 사이드바 `--side-bg`, 헤더 무테.
- 차트 팔레트 hex 테스트 초록, typecheck 0, 대상 vitest 초록, 커밋 1개(push 없음).
  </done>
</task>

<task type="auto">
  <name>Task 2: 종목상세 B 구조 — 히어로 · 16px 탭 바 · 풀폭 섹션 + 12px 띠 · 라이트 호가주문 회색 면 · 폰 주문하기 CTA</name>
  <files>webapp/src/styles/globals.css, webapp/src/components/stock/detail-bands.tsx, webapp/src/components/stock/stock-detail-client.tsx, webapp/src/components/stock/stock-detail-tabs.tsx, webapp/src/components/stock/stock-hero.tsx, webapp/src/components/stock/stock-daily-chart-section.tsx, webapp/src/components/stock/stock-daily-chart.tsx, webapp/src/components/stock/stock-stats-grid.tsx, webapp/src/components/stock/stock-limit-up-section.tsx, webapp/src/components/stock/stock-comovement-section.tsx, webapp/src/components/stock/stock-news-section.tsx, webapp/src/components/stock/stock-discussion-section.tsx, webapp/src/components/chat/chat-fab.tsx, webapp/src/components/stock/__tests__/stock-hero.test.tsx</files>
  <action>
(VJ1-B) 스케치의 `.hero` · `.detail-tabs` · `.v-b .detail-stack > .card` · `.cta-bar` · `.light.v-b [data-p="ob"] > .stack` 규칙이 목표다.

1) 흰 본문(라이트). `stock-detail-client.tsx` 의 정상 렌더 루트 div 에 `data-page-surface="plain"` 을 달고, globals.css 에 레이어 밖 규칙 `main:has([data-page-surface="plain"]) { background: var(--bg); }` 를 추가한다(주석: 종목상세 비-호가 탭은 B 라이트 = 흰 바탕 + 회색 띠, 나머지 화면은 `--surface` 회색 면 + 흰 카드). 다크는 `--surface` = `--bg` 라 변화 없음. 루트의 `space-y-6` 는 B 밀도로 줄인다: 히어로 위아래 `pt-3.5 pb-1.5`, 갱신시각 행 `pb-2.5`, 그 아래 탭은 간격 0. 새로고침 `Button` 은 `size="sm"`(outline 은 T1 에서 이미 raised 채움). 에러 문구 줄은 유지.

2) `DetailBands` 신규(`components/stock/detail-bands.tsx`). 자식 섹션 각각을 `<div data-detail-band>` 로 감싸는 작은 컴포넌트다(`React.Children.map`, null 자식은 건너뜀). 바깥 컨테이너는 `data-detail-bands` + `flex flex-col gap-3 bg-[var(--band)]` + 가로 bleed `-mx-2 md:-mx-4 lg:-mx-6`(app-shell main 패딩 램프와 **같은 값** — 한쪽만 고치면 좌우로 삐져나간다), 각 띠는 `bg-[var(--bg)] px-2 md:px-4 lg:px-6 py-6 lg:py-7`. 즉 섹션은 풀폭 평면 블록이고 flex gap 12px 이 `--band` 색 띠가 된다(다크 #0f0f12 · 라이트 #f2f4f6). 폭은 명시하지 않는다(`w-full` 을 주면 음수 마진이 폭을 못 늘린다). globals.css 에 레이어 밖 규칙 3개를 한 주석 블록(「종목상세 B 띠」)으로 추가: `[data-detail-band]:empty { display: none; }`(섹션이 null 을 렌더하면 띠도 틈도 안 생김 — flex gap 은 display:none 항목에 생기지 않는다), `[data-detail-band] { --card: var(--muted); }`(띠 안 「카드」 면은 B `.box` = raised 박스가 된다 — 라이트에서 흰 위 흰 박스가 사라지는 것 방지), `[data-detail-band] > * { background: transparent; border-color: transparent; border-radius: 0; box-shadow: none; padding: 0; }`(섹션 루트 카드 평면화 — 레이어 밖이라 섹션의 `p-6`/`bg-[var(--card)]` 유틸을 이긴다). 주석에 이 원리를 적는다.

3) 탭 연결 `stock-detail-tabs.tsx`. (a) `info` 패널은 기존 `space-y-8` 래퍼 대신 `DetailBands` 로 네 섹션(StatsGrid · ThemeChips · LimitUp · Comovement)을, `news` 는 `space-y-6` 대신 `DetailBands` 로 두 섹션을, `chart` 는 `DetailBands` 로 차트 섹션 하나를 감싼다 — 이 조립은 `stock-detail-client.tsx` 에서 한다(패널 내용은 상위가 주입하는 기존 계약 T7 유지). (b) `NARROW_PANEL` 의 `pt-[var(--s-5)]` → `pt-0`(B `--panel-pt: 0`). (c) 호가주문 `TabsContent`: `w-full pt-[var(--s-5)]` → `w-auto` + B 라이트 회색 면 `bg-[var(--surface)]` + bleed `-mx-2 px-2 md:-mx-4 md:px-4 lg:-mx-6 lg:px-6` + 아래 `-mb-2 md:-mb-4 lg:-mb-6` + `pt-3 pb-5`. 음수 마진과 같은 패딩이 상쇄되어 **콘텐츠 박스 폭(= `@container/lc` 폭)은 그대로**다 — `w-full` 을 남기면 박스가 거터×2 만큼 좁아져 밴드 전환 지점이 움직이므로 반드시 뺀다. T6 주석을 이 사실로 갱신. (d) sticky 탭 바 래퍼 bleed 를 `-mx-2 px-2 md:-mx-4 md:px-4 lg:-mx-6 lg:px-6` 로(기존엔 md 단계가 빠져 768~1023 에서 바가 8px 덜 퍼졌다 — 손대는 표면의 시각 결함이라 함께 고친다), 아래 테두리는 `--border`(B 옅은 1px 기준선) 유지, 바탕 `--bg`. (e) 트리거: `h-[50px] px-3 text-[16px] font-semibold`, 비활성 `--muted-fg`, 활성 `--fg` + 2px fg 밑줄(기존 `border-b-2` 채널 유지). 폰 390 에서 4라벨 합 ≈ 334px 로 한 줄에 들어가며 `overflow-x-auto` 도 그대로 있다. (f) 폰 CTA: `active !== 'orderbook'` 일 때만 `<div data-slot="detail-order-cta-bar">` 를 렌더 — `fixed inset-x-0 bottom-0 z-30 md:hidden px-4 pt-2.5 pb-[calc(12px+env(safe-area-inset-bottom))]` + 위로 투명→`--bg` 그라디언트, 안에 `type="button"` 「주문하기」 버튼(`data-slot="detail-order-cta"`, `h-[54px] w-full rounded-[16px] bg-[var(--up)] text-white text-[16.5px] font-semibold`) — onClick 은 **기존 `handleValueChange('orderbook')`** 을 부른다(한 클릭 = 기록 1개 가드 · pushState · scrollIntoView 재사용, 새 경로 0). CTA 가 보일 때 Tabs 루트에 `max-md:pb-[84px]` 를 줘 마지막 콘텐츠가 가려지지 않게 한다. `position: fixed` 이지만 조상에 `container-type` 이 없는 자리(탭 셸 루트)라 컨테이닝 블록 함정에 걸리지 않는다 — 주석으로 명시. 뷰포트 `md` 분기는 앱 셸 층이라 CLAUDE.md 규칙(상따 본문 안에서만 컨테이너 쿼리)과 충돌하지 않는다.

4) 챗 FAB 겹침. `chat-fab.tsx` 버튼에 `data-slot="chat-fab"` 만 추가하고, globals.css 에 `@media (max-width: 767.98px) { body:has([data-slot="detail-order-cta-bar"]) [data-slot="chat-fab"] { bottom: calc(80px + env(safe-area-inset-bottom, 0px)); } }` 를 둔다(CTA 가 있을 때만 FAB 가 CTA 위로 올라가고, 호가주문 탭에선 CTA 가 사라져 제자리).

5) 히어로 `stock-hero.tsx`(B `.hero`): 섹션 `space-y-6` → `space-y-1.5`, 종목명 `text-[18px] font-semibold tracking-[-0.02em]`, 코드 span 은 raised 칩(`rounded-[6px] bg-[var(--muted)] px-[7px] py-[2px] text-[12px] font-semibold text-[var(--muted-fg)]` + `mono` 유지), 시장 `Badge variant="outline"`(T1 에서 raised 알약). 가격 `text-[30px] font-bold tracking-[-0.02em] leading-tight`(폰·데스크톱 동일), 등락액·등락률 `text-[15px] font-medium`. 문구·데이터는 바꾸지 않는다. `stock-hero.test.tsx` 의 가격 크기 클래스 단언을 새 클래스로 갱신.

6) 섹션별 B 손질. `stock-daily-chart-section.tsx`: 제목 `text-[20px] font-bold tracking-[-0.02em]`, 「장중」 칩 `rounded-full`, 일봉/기간 두 세그먼트를 알약 트랙으로 — 그룹 `inline-flex gap-0.5 rounded-[var(--r)] bg-[var(--muted)] p-[3px]`(테두리 제거), 항목 `h-8 rounded-[calc(var(--r)-3px)] px-3 text-[13px] font-semibold`, 선택 `bg-[var(--seg-on-bg)] text-[var(--seg-on-fg)] shadow-[var(--seg-on-shadow)]`, 비선택 `text-[var(--muted-fg)] hover:text-[var(--fg)]`(`role="tab"`·`aria-selected`·testid 유지). 차트 래퍼의 `-mx-5` bleed 와 `bg-[var(--card)]` 를 걷어 `mt-3` + `bg-transparent` 로(B `.v-b .chart-wrap { margin: 12px 0 0 }` — 띠 안에선 `--card` 가 raised 로 재정의되므로 카드색 칠을 남기면 차트 바탕이 회색 박스가 된다). `stock-daily-chart.tsx` 의 `bg-[var(--card)]` 도 `bg-transparent`(모달에선 부모 카드색이 그대로 비친다). `stock-stats-grid.tsx`(B `.spec` · `.kv-grid`): 스펙트럼 트랙을 그라디언트 14px → `h-[5px] rounded-full bg-[var(--muted)]`, 당일 범위 밴드 `bg-[var(--faint)]` 불투명, 현재가 점 `size-3 rounded-full bg-[var(--muted-fg)]` 테두리 없이 `shadow-[0_0_0_4px_color-mix(in_oklch,var(--fg)_8%,transparent)]`, 현재가 태그는 배경 알약 대신 `text-[var(--fg-2)]` 굵은 텍스트(엣지 앵커링 로직 유지), 시가·저가·고가 / 거래량·대금·시총 두 줄의 `border-t` 구분선 제거 · 값 `text-[16px] font-medium`, 라벨 13.5px `--muted-fg`. 폴백 격자의 `Card` 들은 띠 안에서 raised 박스가 된다(추가 수정 불필요). `stock-limit-up-section.tsx` · `stock-comovement-section.tsx` · `stock-news-section.tsx` · `stock-discussion-section.tsx`: 섹션 최상위 제목만 `text-[20px] font-bold tracking-[-0.02em]` 로, 안쪽 KPI/행 박스는 띠 재정의로 raised 가 되므로 테두리 색만 transparent 로(격자선 트릭 `gap-px bg-[var(--border)]` 은 유지), 테두리만 있고 채움 없는 칩·버튼은 `bg-[var(--muted)] border-transparent`, 「더 보기」 버튼은 `rounded-[var(--r)]` 그대로 두되 테두리 transparent. 섹션 로딩/에러 루트도 같은 평면화 규칙을 받으므로 따로 손대지 않는다.

7) stock 폴더 단위 테스트를 돌려 이 태스크가 바꾼 클래스 단언만 갱신한다. 커밋: `feat(260924-vj1): 종목상세 토스 B 구조 — 풀폭 섹션·띠·히어로·폰 주문하기 CTA`. push 금지.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp exec vitest run --passWithNoTests src/components/stock src/components/chat && test -f webapp/src/components/stock/detail-bands.tsx && test "$(grep -c 'data-detail-band' webapp/src/styles/globals.css)" -ge 3 && grep -q 'main:has(\[data-page-surface="plain"\])' webapp/src/styles/globals.css && grep -q 'data-page-surface="plain"' webapp/src/components/stock/stock-detail-client.tsx && grep -q 'detail-order-cta' webapp/src/components/stock/stock-detail-tabs.tsx && grep -q "handleValueChange('orderbook')" webapp/src/components/stock/stock-detail-tabs.tsx && grep -q 'md:-mx-4' webapp/src/components/stock/stock-detail-tabs.tsx && grep -q 'data-slot="chat-fab"' webapp/src/components/chat/chat-fab.tsx && grep -q 'seg-on-bg' webapp/src/components/stock/stock-daily-chart-section.tsx && grep -q 'text-\[30px\]' webapp/src/components/stock/stock-hero.tsx</automated>
  </verify>
  <done>
- 차트·종목정보·뉴스토론 탭은 섹션마다 풀폭 평면 블록 + 12px `--band` 띠로 갈리고(빈 섹션은 띠도 없음), 띠 안 안쪽 박스는 raised.
- 라이트: 종목상세 비-호가 탭 흰 바탕, 호가주문 탭은 #f2f4f6 풀폭 면 위 흰 카드 — `lc` 컨테이너 콘텐츠 폭은 이전과 동일.
- 히어로 30px/700 · 코드 칩 · 16px semibold 탭 바(md 램프 bleed 포함).
- 폰 CTA 「주문하기」가 호가주문 탭으로 전환하고 그 탭에선 사라지며 FAB 는 CTA 위로 올라간다.
- typecheck 0, stock·chat vitest 초록, 커밋 1개(push 없음).
  </done>
</task>

<task type="auto">
  <name>Task 3: 트레이딩·호가 표면 B 리스킨(사다리 · 폼 · 전략 카드 · 상태줄 · 스트립 · 공용 패널) + 전체 게이트</name>
  <files>webapp/src/components/orderbook/orderbook-ladder.tsx, webapp/src/components/orderbook/account-panel.tsx, webapp/src/components/orderbook/order-panel.tsx, webapp/src/components/orderbook/relay-status-bar.tsx, webapp/src/components/stock/stock-orderbook-section.tsx, webapp/src/components/trading/limit-chaser-form.tsx, webapp/src/components/trading/latch-led.tsx, webapp/src/components/trading/exchange-tag.tsx, webapp/src/components/trading/dirty-action-bar.tsx, webapp/src/components/trading/vi-order-list.tsx, webapp/src/components/trading/today-orders-card.tsx, webapp/src/components/trading/strategy-status-card.tsx, webapp/src/components/trading/dma-gate.tsx, webapp/src/components/trading/card/strategy-card.tsx, webapp/src/components/trading/card/card-header.tsx, webapp/src/components/trading/card/card-tabs.tsx, webapp/src/components/trading/card/quote-grid-10.tsx, webapp/src/components/trading/card/manual-order-form.tsx, webapp/src/components/trading/card/stock-info-modal.tsx, webapp/src/components/trading/workbench/workbench-status-bar.tsx, webapp/src/components/trading/workbench/vi-trigger-strip.tsx, webapp/src/components/trading/workbench/breakout-strip.tsx, webapp/src/components/trading/workbench/stock-add-bar.tsx, webapp/src/components/trading/workbench/shared-panels.tsx, webapp/src/components/trading/workbench/card-grid.tsx, webapp/src/components/trading/workbench/vi-settings-rows.tsx, webapp/src/components/orderbook/__tests__/orderbook-ladder-chaser.test.tsx, webapp/src/components/trading/__tests__/limit-chaser-form.test.tsx</files>
  <action>
(VJ1-C, VJ1-D) 스케치의 `.v-b .lr` · `.l3` · `.status-bar` · `.strip` · `.xchip` · `.scard` · `.side-tabs` · `.fcol` · `.btn4` · `.pills` · `.seg.xs` 규칙이 목표다. interfaces 의 **`lc` 카드 안 규칙(가로 폭 증가 0)** 과 불변 목록을 모든 편집에 적용한다. 라이트 작업대는 T1 의 `--surface`(#f2f4f6) 위라 카드는 흰 `--card` 로 이미 떠 보인다 — 페이지 면에 바로 놓인 요소가 `--muted`(라이트 #f2f4f6)로 칠해져 있으면 사라지므로 `--card` 로 올리고, 카드 위에 놓인 칩/컨트롤이 `--card`/`--bg` 로 칠해져 있으면 안 보이므로 `--muted` 로 내린다 — 이것이 이 태스크 색 편집의 단일 규칙이다.

1) 사다리 `orderbook-ladder.tsx`. (a) 폰 1단 트리(`ladder-row-mobile`): 행 `h-[34px]` → `h-[44px]`, 박스 `ladder-scroll` `h-[340px]` → `h-[440px]` — 「10행 높이 박스 안에서 20행 스크롤」 확정 규칙의 쌍(44 × 10)을 유지하고 두 ★ 주석의 숫자를 함께 고친다. 행 radius `rounded-[10px]`, 잔량 막대 `top-1 bottom-1 rounded-[8px]`, 최근 체결가 행(`isLast`)에 `bg-[var(--muted)]` 추가(B `.v-b .lr.cur` = raised 면 — 굵기 채널은 유지). 글자 크기(13 · 10 · 9px)·가로 패딩·gap 은 **그대로**(42% 열에서 7자리 가격이 `truncate` 에 걸리면 오발주). (b) 2단(`twoRow` · `ladder-scroll-two`)과 3단 표(`data-tree="three"`)는 행 높이 24px → 32px(`h-6` → `h-8`, 사다리 행 셀만 — 체결 테이프 등 다른 h-6 는 제외)로 올리고 2단 박스 `h-[240px]` → `h-[320px]`(32 × 10 쌍, 주석 갱신). 3단은 고정 박스가 없어 행만 커진다. 데스크톱 밀도 절충(폰은 B 44 그대로)이므로 행 높이를 파일 상단 상수 하나로 모아 사용자가 44 로 바꾸기 쉽게 하고, 그 상수 옆에 「sketch B = 44 · 데스크톱 밀도 절충 32」라고 적는다. 3단 400px 표의 글자 크기·굵기·마커 슬롯·열 폭은 불변. (c) 모든 잔량 막대 색(StandardLadder · ChaserLadder 전 트리)의 `color-mix(... var(--down) 16% ...)` / `color-mix(... var(--up) 16% ...)` 를 `bg-[var(--ask-bar)]` / `bg-[var(--bid-bar)]` 로, 막대 radius 2~3px → 6px(24/32px 행) · 8px(44px 행). 상한가 행 틴트와 매도1/매수1 경계선은 유지.

2) 폼 `limit-chaser-form.tsx`. (a) 폰 매수/매도 탭(B `.side-tabs`): `h-[42px]`, 비활성 `border-transparent bg-[var(--muted)] text-[var(--muted-fg)]`, 활성은 방향색 대신 `border-transparent bg-[var(--seg-on-bg)] text-[var(--seg-on-fg)] shadow-[var(--seg-on-shadow)]`(B 는 중립 선택 면 — 라벨 「매수/매도」 텍스트가 방향을 말한다). (b) 값 입력 래퍼(`h-[38px] ... bg-[var(--bg)] px-1.5`)와 같은 높이의 세그먼트 그룹·행 `min-h-[38px]` 들: 높이 38 → 46px(B `--input-h`), 채움 `bg-[var(--muted)]`, **가로 패딩 `px-1.5` 불변**. 테두리 채널(평소 `--input` = 투명 · dirty `--primary` · focus-within `--ring`)은 그대로라 dirty/포커스 표시가 산다. 세그먼트 그룹(감시대상 등)은 트랙 `bg-[var(--muted)]` + 선택 항목 `--seg-on-*`, 트랙 **안쪽 패딩은 넣지 않는다**(항목 가로 폭이 줄어 「매도잔량」이 잘린다). (c) 방향 카드(`side` buy/sell): B `--buy-tint`/`--sell-tint` = 카드색이므로 5% 방향 틴트 배경과 폰 bleed 그림자(`shadow-[0_0_0_8px_…]`)·`clip-path` 를 걷고 `bg-[var(--card-base)]` 하나로 — 체크박스 방향색 `[--lc-accent]` 는 유지. ≥992 카드의 radius 는 `--r-md`(16), 테두리 색 transparent. **`[--lw:76px]` · `@min-[992px]/lc:[--lw:104px]` · 모든 `grid-cols-[var(--lw)_…]` · 가로 gap · `@min-[700px]/lc:px-2` 는 불변.** `limit-chaser-form.test.tsx` 의 `h-[38px]` 등 바뀐 클래스 단언을 갱신.

3) 수동주문 `manual-order-form.tsx`: 입력 `bg-[var(--bg)]` → `bg-[var(--muted)]`(잠김 상태는 `bg-transparent` + `--border` 헤어라인으로 편집 가능 필드와 구분 유지), 입력 높이 h-8 → h-[46px](세로만), 주문유형 세그먼트는 트랙+`--seg-on-*`, 매수/매도/정정/취소 버튼 `h-9` → `h-[52px]` · `rounded-[14px]` · `font-semibold`(B `.btn4`) — `px-px` · `text-[13px]` · `whitespace-normal` 는 불변.

4) 전략 카드·헤더·탭. `strategy-card.tsx` article: 테두리 색 transparent(기하 유지), 열림 그림자 제거(B `--card-shadow-open: none`), dirty 테두리 `color-mix(… var(--primary) 55% …)` 는 **유지**(바가 어느 카드 것인지 말하는 채널). `card-header.tsx`: 헤더 세로 패딩 `py-2` → `py-3`(가로 `px-2.5` 불변 — 헤더 한 줄 접힘 로컬 경계 보호), xs 거래소 `ToggleGroup`(B `.seg.xs`): 그룹 `rounded-full border-transparent bg-[var(--muted)]`, 항목 `rounded-full bg-transparent`, 선택 `data-[state=on]:bg-[var(--pill-on-bg)] data-[state=on]:text-[var(--pill-on-fg)]`, 항목 사이 `not-first:border-l` 제거(폭이 줄 뿐 늘지 않음). 같은 xs 세그먼트 패턴을 `stock-orderbook-section.tsx` 와 `workbench-status-bar.tsx` 에도 똑같이. `card-tabs.tsx` `CARD_TAB_TRIGGER` · `shared-panels.tsx` 탭 트리거 · `stock-info-modal.tsx` 탭 트리거(B `.pills`): `rounded-full`, 활성 `--accent` → `--pill-on-bg`/`--pill-on-fg`(dark: 접두 변형도 같이). `exchange-tag.tsx` 활성도 `--pill-on-*`. `quote-grid-10.tsx`: 칸 구분 `border-t` 제거(B `.v-b .qg > div { border-top: 0 }`) — 10칸 한 줄 배치·글자·gap 불변.

5) 상태줄·스트립·패널(B `.status-bar` `--status-bg` = 카드). `workbench-status-bar.tsx` 와 `stock-orderbook-section.tsx` 상태줄: `bg-[var(--muted)]` + 테두리 → `bg-[var(--card)] border-transparent rounded-[var(--r-md)]` + 세로 `py-2.5`(가로는 작업대 폭에서 `px-4` 허용 — flex-wrap 이라 잘리지 않고 줄바꿈된다; `lc` 안의 종목상세 상태줄은 가로 패딩 불변), 안쪽 칩·알림음 버튼·계좌 select 의 `bg-[var(--card)]` → `bg-[var(--muted)] border-transparent`. `relay-status-bar.tsx` 도 같은 규칙. `latch-led.tsx` 칩(B `.led`): `border-transparent bg-[var(--muted)]`, OFF 상태는 `--faint` 윤곽(기존 점선이면 점선 유지) — 이 파일은 `latch-led.test.tsx` 가 소스를 읽어 단언하므로 그 테스트를 먼저 보고 맞춘다. `vi-trigger-strip.tsx` · `breakout-strip.tsx`: 스트립 컨테이너는 카드(테두리 transparent), xchip `bg-[var(--bg)]` → `bg-[var(--muted)] border-transparent rounded-full`, 점선(거래중) 칩은 `border-dashed border-[var(--faint)] bg-transparent`, 「더보기」 버튼 `bg-[var(--muted)] border-transparent`, 표 머리 셀 `bg-[var(--muted)]` → transparent, 신규 행 「추가」 버튼의 primary 채움은 유지(`breakout-strip.test` 단언). `stock-add-bar.tsx`: 입력은 페이지 면 위라 `bg-[var(--card)] border-transparent`(포커스 테두리 채널 유지), 자동완성 목록 `bg-[var(--popover)]`. `shared-panels.tsx` 루트 테두리 transparent. `card-grid.tsx` 빈 칸 점선은 T1 이 이미 `--faint` 로. `vi-settings-rows.tsx` · `account-panel.tsx`(select) · `order-panel.tsx`(input) 의 `bg-[var(--bg)]` 컨트롤 → `bg-[var(--muted)]`; `account-panel` 의 행 선택 `--accent` 는 유지(테스트 단언 · primary 틴트 의미). `dirty-action-bar.tsx`: 바 면 `bg-[var(--bg)]` → `bg-[var(--popover)]` + `rounded-[var(--r-md)]`, **포털·문구·위치 로직 불변**. `vi-order-list` · `today-orders-card` · `strategy-status-card` · `dma-gate` 의 채움 없는 테두리 칩/버튼은 `bg-[var(--muted)] border-transparent`, 순수 구분선은 그대로.

6) 전체 게이트. `pnpm --filter @gh-radar/webapp run test` 전량을 돌려 이 플랜이 바꾼 클래스를 단언하던 테스트(예: 사다리 ⑫ `h-[340px]`·`h-[34px]` → 440/44, 2단 `h-[240px]` → 320, 폼 `h-[38px]` → 46, 탭/칩 `--accent` → `--pill-on-*`)만 의미를 옮겨 갱신한다 — 동작 단언을 지우거나 약화하지 않는다. 가능하면 `pnpm --filter @gh-radar/webapp run build` 도 시도하되 env 부재로만 실패하면 SUMMARY 에 사유를 적고 넘어간다. SUMMARY 에 (i) 오케스트레이터 스크린샷 점검 목록 — 3단 호가표 컨테이너 400 에서 가격 잘림 0, 본문 700 에서 주문금액 잘림 0, 폰 360/390 사다리·폼, 라이트/다크 × 종목상세 4탭 × 작업대, CTA·FAB 겹침 — 과 (ii) 재량 결정(라이트 전 화면 회색 면 + 흰 카드, 데스크톱 사다리 32px 절충, B 수치 중 `lc` 가로 증가분 미적용, 방향 틴트 제거)을 적는다. 커밋: `feat(260924-vj1): 트레이딩·호가 표면 토스 B 리스킨 + 테스트 갱신`. push 금지.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar/.claude/worktrees/toss-b && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp run test && grep -q 'h-\[440px\]' webapp/src/components/orderbook/orderbook-ladder.tsx && grep -q 'h-\[44px\]' webapp/src/components/orderbook/orderbook-ladder.tsx && test "$(grep -c 'var(--ask-bar)' webapp/src/components/orderbook/orderbook-ladder.tsx)" -ge 3 && test "$(grep -c 'var(--bid-bar)' webapp/src/components/orderbook/orderbook-ladder.tsx)" -ge 3 && grep -q '@min-\[700px\]/lc:grid-cols-\[260px_minmax(0,1fr)\]' webapp/src/components/trading/card/card-body.tsx && grep -q '@min-\[830px\]/lc:grid-cols-\[400px_minmax(0,1fr)\]' webapp/src/components/trading/card/card-body.tsx && grep -q '@min-\[992px\]/lc:grid-cols-\[460px_minmax(0,1fr)\]' webapp/src/components/trading/card/card-body.tsx && grep -q '\[--lw:76px\]' webapp/src/components/trading/limit-chaser-form.tsx && grep -q '@min-\[992px\]/lc:\[--lw:104px\]' webapp/src/components/trading/limit-chaser-form.tsx && grep -q 'createPortal' webapp/src/components/trading/dirty-action-bar.tsx && grep -q 'pill-on-bg' webapp/src/components/trading/card/card-tabs.tsx && grep -q 'pill-on-bg' webapp/src/components/trading/workbench/shared-panels.tsx && grep -q 'seg-on-bg' webapp/src/components/trading/limit-chaser-form.tsx</automated>
  </verify>
  <done>
- 폰 사다리 44px 행 / 440 박스, 2단 32 / 320, 3단 32px 행, 잔량 막대는 `--ask-bar`/`--bid-bar` 둥근 박스, 최근 체결가 행은 raised 면.
- 폼·수동주문: 입력 46px raised(가로 패딩 불변), 알약 세그먼트, 52px 주문 버튼, 방향 틴트 제거, `--lw` 76/104 불변.
- 전략 카드·스트립·상태줄·공용 패널은 무테 카드 면 + raised 칩, 알약 탭은 `--pill-on-*`, 더티 바 포털 불변.
- §2.2b 밴드 클래스 · 400/460/260 열 · `--lw` · 포털 grep 게이트 통과, typecheck 0, webapp 전체 vitest 초록, 커밋 1개(push 없음), SUMMARY 에 스크린샷 점검 목록과 재량 결정 기록.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음(신규) | 순수 클라이언트 CSS/마크업 리스킨. 새 입력·네트워크·저장 경로 0. CTA 는 기존 탭 전환 핸들러만 호출한다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-vj1-01 | Tampering(표시 무결성 → 오발주) | 호가 사다리 가격·폼 라벨/값(`lc` 카드) | high | mitigate | `lc` 안 가로 증가 0 규칙(글자 크기·굵기·가로 패딩·gap·열 폭·`--lw` 불변), 면 테두리는 투명화로 1px 기하 보존, card-body 열 폭·`--lw` grep 게이트, 오케스트레이터 스크린샷 점검 목록(컨테이너 400·700·폰 360/390) |
| T-vj1-02 | Denial of Service(조작 불가) | 폰 고정 「주문하기」 CTA · 챗 FAB · 더티 액션 바 | medium | mitigate | CTA 는 호가주문 탭에서 언마운트(더티 바와 동시 노출 없음), CTA 존재 시 FAB 를 `:has` 규칙으로 CTA 위로, 본문 하단 `max-md:pb-[84px]`, 더티 바 포털 불변 grep 게이트 |
| T-vj1-03 | Tampering(상태 오인) | 입력 dirty/포커스 표시 · 전략 카드 dirty 테두리 | medium | mitigate | `--input` 투명화 후에도 dirty `--primary` · focus `--ring` 테두리 채널과 `data-focus-ring="seamless"` 쌍 유지, 카드 dirty 테두리 color-mix 유지 |
| T-vj1-04 | Information Disclosure | 차트 팔레트 | low | accept | 색 상수만 교체 — 데이터 노출 경로 무관. oklch 주입 금지는 기존 chart-colors 테스트가 강제 |
</threat_model>

<verification>
- 태스크별 `<verify>` 전부 통과 + 마지막에 `pnpm --filter @gh-radar/webapp run typecheck` 0 · `pnpm --filter @gh-radar/webapp run test` 전량 초록.
- `git log --oneline master..theme/toss-b` 에 이 플랜 커밋 3개, `git status` 깨끗, 원격 push 0(`git status -sb` 에 ahead 표시만 있고 push 하지 않음).
- 메인 트리 `/Users/alex/repos/gh-radar` 무변경(이 worktree 밖 파일을 쓰지 않음).
</verification>

<success_criteria>
- 모든 화면이 다크·라이트 모두 B 팔레트·무테 면·큰 radius·Pretendard 숫자로 렌더되고, 종목상세는 풀폭 섹션 + 띠 · 30px 히어로 · 16px 탭 · 폰 CTA 를 갖는다.
- 트레이딩 밀도 불변식(§2.2b 밴드 · 400 하한 · `--lw` 76 · 포털 · 컨테이너 위치)과 `lc` 가로 증가 0 규칙이 지켜진다.
- typecheck/test 초록, `theme/toss-b` 에만 커밋, push 없음.
</success_criteria>

<output>
Create `.planning/quick/260924-vj1-b-theme-toss-b/260924-vj1-SUMMARY.md` when done — 스크린샷 점검 목록과 재량 결정(라이트 전 화면 회색 면 · 데스크톱 사다리 32px 절충 · `lc` 가로 증가분 미적용 · 방향 틴트 제거 · `--accent` 의미 유지)을 반드시 포함.
</output>
