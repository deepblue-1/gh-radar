---
phase: quick-260820-fh2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/intraday-sync/src/marketWindow.ts
  - workers/intraday-sync/src/kiwoom/fetchRanking.ts
  - workers/intraday-sync/src/pipeline/eodClose.ts
  - workers/intraday-sync/src/index.ts
  - workers/intraday-sync/tests/marketWindow.test.ts
  - workers/intraday-sync/tests/eodClose.test.ts
  - workers/intraday-sync/tests/fetchRanking.test.ts
  - workers/intraday-sync/tests/runCycle.test.ts
  - workers/candle-sync/src/pipeline/recentDates.ts
  - workers/candle-sync/src/config.ts
  - workers/candle-sync/src/modes/recover.ts
  - workers/candle-sync/tests/recentDates.test.ts
  - workers/candle-sync/tests/runRecover.test.ts
autonomous: false
requirements: [D-fh2-01, D-fh2-02, D-fh2-03, D-fh2-04, D-fh2-05]

must_haves:
  truths:
    - "intraday-sync 는 09:00~15:30 KST 밖에서는 stock_daily_ohlcv 에 쓰지 않는다 (NXT 프리/애프터마켓 가격이 일봉을 오염시키지 않음)"
    - "intraday-sync 는 09:00~15:30 밖에서도 stock_quotes 를 계속 갱신한다 (NXT 실시간 현재가 표시 유지)"
    - "15:35~15:55 KST 사이클이 ka10027 stex_tp=1(KRX 전용) 으로 당일 공식 종가를 stock_daily_ohlcv 에 upsert 한다"
    - "candle-sync recover 는 결측 판정과 무관하게 최근 2영업일을 KRX bydd_trd 로 무조건 재적재한다"
    - "2026-05-21~2026-08-19 오염 종가가 KRX 공식값으로 정정된다 (000660 2026-08-19 close = 1500000)"
  artifacts:
    - path: "workers/intraday-sync/src/marketWindow.ts"
      provides: "KST 정규장/EOD 창 판정 순수 함수"
      exports: ["kstMinutesOfDay", "isDailyWriteWindow", "isEodClosePass"]
    - path: "workers/intraday-sync/src/pipeline/eodClose.ts"
      provides: "ka10027 stex_tp=1 sweep → intraday_upsert_close EOD 종가 패스"
      exports: ["runEodClosePass"]
    - path: "workers/candle-sync/src/pipeline/recentDates.ts"
      provides: "DB distinct date 기준 최근 N 영업일 조회"
      exports: ["fetchRecentTradingDates"]
  key_links:
    - from: "workers/intraday-sync/src/index.ts"
      to: "isDailyWriteWindow"
      via: "intradayUpsertClose / intradayUpsertOhlc 호출 게이트"
      pattern: "isDailyWriteWindow"
    - from: "workers/intraday-sync/src/pipeline/eodClose.ts"
      to: "fetchKa10027(..., stexTp=\"1\")"
      via: "KRX 전용 거래소 구분 전달"
      pattern: "stexTp"
    - from: "workers/candle-sync/src/modes/recover.ts"
      to: "fetchRecentTradingDates"
      via: "강제 재적재 일자 병합"
      pattern: "fetchRecentTradingDates"
---

<objective>
intraday-sync 가 KRX 정규장 밖(프리마켓 08:00~08:59, 15:31~15:59)의 NXT 가격을 `stock_daily_ohlcv`
종가로 덮어써 온 오염을 근본 차단하고, KRX 공식 종가로 당일 일봉을 확정하는 EOD 패스를 추가한다.
또한 candle-sync recover 의 overlay 가 구조적으로 한 번도 작동하지 않던 결함(결측 판정이 영영
발동 안 함)을 최근 2영업일 무조건 재적재로 보완한 뒤, 2026-05-21~2026-08-19 오염 구간을 백필한다.

Purpose: 일봉 종가가 KRX 공식 종가와 불일치(62거래일 중 36일)해 차트·등락률·스캐너 시그널이
         전부 틀린 값 위에 서 있다. 8/19 SK하이닉스는 close 1,606,000 > high 1,559,000 으로
         자기모순 상태다.
Output: intraday-sync 일봉 쓰기 창 제한 + EOD KRX 종가 패스, candle-sync recover 강제 재적재,
        배포 및 백필 실행, 정정 검증.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@workers/intraday-sync/src/index.ts
@workers/intraday-sync/src/kiwoom/fetchRanking.ts
@workers/intraday-sync/src/pipeline/upsertClose.ts
@workers/intraday-sync/src/pipeline/map.ts
@workers/intraday-sync/tests/runCycle.test.ts
@workers/candle-sync/src/modes/recover.ts
@workers/candle-sync/src/pipeline/missingDates.ts
@workers/candle-sync/src/config.ts
@workers/candle-sync/tests/runRecover.test.ts
@scripts/deploy-intraday-sync.sh
@scripts/deploy-candle-sync.sh

<interfaces>
<!-- 실행자가 코드베이스를 다시 탐색하지 않도록 필요한 계약을 여기에 박아둔다. -->

workers/intraday-sync/src/kiwoom/fetchRanking.ts (현재 시그니처 — Task 1 에서 5번째 인자 추가):
  export async function fetchKa10027(
    client: AxiosInstance, token: string, sortTp = "1", hardCap = 5000,
  ): Promise<KiwoomKa10027Row[]>
  body 내부에 `stex_tp: "3"` 하드코딩 (line 48). "1"=KRX, "2"=NXT, "3"=통합.

workers/intraday-sync/src/pipeline/map.ts:
  export function ka10027RowToCloseUpdate(row: KiwoomKa10027Row, dateIso: string): IntradayCloseUpdate
  // IntradayCloseUpdate = { code, date, name, price, changeAmount, changeRate, volume, tradeAmount }

workers/intraday-sync/src/pipeline/upsertClose.ts:
  export async function intradayUpsertClose(
    supabase: SupabaseClient, updates: IntradayCloseUpdate[],
  ): Promise<{ count: number }>
  // RPC intraday_upsert_close — 기존 row 는 UPDATE, 없으면 INSERT. idempotent.

workers/intraday-sync/src/index.ts 내부 기존 헬퍼:
  function todayIsoKst(now?: Date): string      // "YYYY-MM-DD"
  function kstHour(now: Date): number           // 0..23
  runIntradayCycle(): Promise<{ step1Count: number; step2Count: number; failed: number }>
  // ↑ 반환 shape 변경 금지 — 기존 runCycle.test.ts 가 toEqual 로 3키 전량 비교한다.

workers/candle-sync/src/config.ts:
  export type Config = { ...; recoverLookback: number; recoverThreshold: number; recoverMaxCalls: number; ... }
  function parseNumberEnv(raw: string | undefined, fallback: number): number

workers/candle-sync/src/pipeline/missingDates.ts:
  export async function findMissingDates(
    supabase: SupabaseClient, opts: { lookback: number; threshold: number; maxCalls: number },
  ): Promise<string[]>   // ISO "YYYY-MM-DD" descending
  // 최근 distinct date 조회 패턴(line 48~78): .from("stock_daily_ohlcv").select("date")
  //   .gte("date", sinceIso).order("date", { ascending: false }) → 클라이언트측 dedupe

workers/candle-sync/src/pipeline/upsert.ts:
  export async function upsertOhlcv(supabase, rows: StockDailyOhlcv[]): Promise<{ count: number }>
  // onConflict "code,date" DO UPDATE — open/high/low/close/volume/trade_amount/change_amount/change_rate 전량 갱신
</interfaces>

<market_structure>
KRX 정규장 09:00~15:30 (15:20~15:30 동시호가로 공식 종가 확정).
15:40~16:00 시간외 종가매매 = 종가 그대로 체결(무해). **16:00 부터 시간외 단일가(±10%) 개시 → 이후 cur_prc 오염.**
NXT: 프리마켓 08:00~08:50, 메인 09:00~15:20, 애프터마켓 15:30~20:00.
→ EOD 공식 종가 안전 창 = 15:35~15:55.
intraday-sync Scheduler cron = `* 8-15 * * 1-5` (KST) — 15:35~15:55 사이클이 이미 존재하므로 스케줄 변경 불필요.
</market_structure>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: intraday-sync 일봉 쓰기 창 제한(09:00~15:30) + EOD KRX 전용 종가 패스</name>
  <files>
    workers/intraday-sync/src/marketWindow.ts,
    workers/intraday-sync/src/kiwoom/fetchRanking.ts,
    workers/intraday-sync/src/pipeline/eodClose.ts,
    workers/intraday-sync/src/index.ts,
    workers/intraday-sync/tests/marketWindow.test.ts,
    workers/intraday-sync/tests/eodClose.test.ts,
    workers/intraday-sync/tests/fetchRanking.test.ts,
    workers/intraday-sync/tests/runCycle.test.ts
  </files>
  <behavior>
    marketWindow.ts (순수 함수 — 테스트 선행):
    - kstMinutesOfDay(new Date("2026-08-20T00:00:00Z")) === 540  (09:00 KST)
    - isDailyWriteWindow: 08:59 → false, 09:00 → true, 12:00 → true, 15:30 → true, 15:31 → false, 15:59 → false
    - isEodClosePass: 15:35/15:40/15:45/15:50/15:55 → true; 15:34/15:36/15:56/16:00/14:35 → false
    fetchKa10027:
    - stexTp 인자 미지정 → 요청 body.stex_tp === "3" (기존 동작 보존)
    - stexTp="1" 전달 → 요청 body.stex_tp === "1"
    runEodClosePass:
    - fetchKa10027 을 sort_tp "1" 과 "3" 두 번, 각각 stexTp="1" 로 호출
    - 병합 결과를 code 기준 dedupe 후 intradayUpsertClose 만 호출 (upsertQuotesStep1/rebuildTopMovers 미호출)
    - 병합 0 row → warn + { count: 0 }, intradayUpsertClose 미호출
    runIntradayCycle:
    - 10:00 KST → intradayUpsertClose + intradayUpsertOhlc 호출됨 (기존 동작)
    - 08:30 KST → intradayUpsertClose/intradayUpsertOhlc 미호출, upsertQuotesStep1/upsertQuotesStep2 는 호출됨
    - 15:45 KST → STEP1/STEP2 일봉 쓰기 미호출, quotes 는 호출, runEodClosePass 호출됨
    - 15:45 이외(예: 15:31) → runEodClosePass 미호출
    - 반환 shape 는 { step1Count, step2Count, failed } 3키 유지 (일봉 skip 시 step1Count=0)
  </behavior>
  <action>
    D-fh2-01 / D-fh2-02 구현. 기존 워커 관례대로 **실패 테스트 선행 커밋 → 구현 커밋** 2단으로 진행하고,
    주석은 한글 + 결정 태그(D-fh2-xx) 스타일을 따른다.

    (1) `workers/intraday-sync/src/marketWindow.ts` 신규 — 순수 함수 3개 export.
    `kstMinutesOfDay(now)` 는 index.ts 의 기존 KST 변환 관례(`now.getTime() + 9*3600*1000` 후 getUTC*)를
    그대로 써서 0..1439 분을 반환한다. `isDailyWriteWindow(now)` 는 540 이상 930 이하(양끝 포함).
    `isEodClosePass(now)` 는 시각이 15시이고 분이 {35,40,45,50,55} 중 하나일 때 true.
    5분 간격을 쓰는 이유(단일 사이클 실패 대비 5회 기회 확보 + API 호출 절약)를 주석에 남긴다.
    15:30 을 포함하는 이유와 16:00 시간외 단일가 개시가 상한인 이유도 주석에 명시한다.

    (2) `fetchRanking.ts` — `fetchKa10027(client, token, sortTp = "1", hardCap = 5000, stexTp = "3")` 로
    5번째 인자를 추가하고 body 의 하드코딩 `stex_tp: "3"` 을 `stex_tp: stexTp` 로 교체한다.
    기본값 "3" 유지로 기존 STEP1 호출부는 무변경. 주석에 "1"=KRX 전용이 EOD 공식 종가 확보 경로임을 적는다.

    (3) `workers/intraday-sync/src/pipeline/eodClose.ts` 신규 — `runEodClosePass(deps)` export.
    deps 는 supabase, kiwoom(AxiosInstance), accessToken, dateIso, hardCap, log 를 받는다.
    withRetry 로 감싼 fetchKa10027(sortTp "1", stexTp "1") 과 fetchKa10027(sortTp "3", stexTp "1") 결과를
    concat, `ka10027RowToCloseUpdate` 로 매핑(개별 실패는 카운트만 하고 skip — STEP1 관례 동일),
    Map by code 로 dedupe 후 `intradayUpsertClose` 만 호출한다.
    **stock_quotes 와 top_movers 는 절대 건드리지 않는다** — 15:35 이후 표시용 현재가는 NXT 애프터마켓
    값을 유지해야 하므로(D-fh2-01) EOD 패스가 덮어쓰면 회귀다.
    병합 0 row 면 warn 로그 후 { count: 0 } 반환.
    volume 은 KRX 전용 거래량이 들어가는데 이는 공식 일봉 관례와 일치하므로 의도된 동작임을 주석에 남긴다.

    (4) `index.ts` 배선 — `dateIso` 계산 직후 `const dailyWrite = isDailyWriteWindow(now);`
    `const eodPass = isEodClosePass(now);` 를 구하고 cycle 시작 로그에 두 플래그를 포함한다.
    STEP1 DB 쓰기 구간: `intradayUpsertClose` 호출을 `dailyWrite` 로 게이트하고 skip 시 step1Count=0 +
    info 로그("정규장 밖 — 일봉 쓰기 skip, quotes 만 갱신"). `upsertQuotesStep1` 과 `rebuildTopMovers` 는
    게이트하지 않는다(둘 다 표시 계층). STEP2 구간: `intradayUpsertOhlc` 를 동일하게 게이트하고
    `upsertQuotesStep2` 는 유지. STEP2 완료 후 `eodPass` 가 true 면 `runEodClosePass` 를 호출하고
    결과 count 를 로그에 남긴다(반환 객체 shape 는 변경 금지).
    EOD 패스 실패가 cycle 전체를 죽이지 않도록 try/catch 로 감싸 error 로그 후 계속 진행한다 —
    다음 5분 뒤 사이클이 재시도한다(idempotent).

    (5) 기존 `tests/runCycle.test.ts` 의 `vi.setSystemTime` 은 이미 10:00 KST(정규장)로 고정돼 있으므로
    기존 케이스는 그대로 통과해야 한다. 08:30 / 15:45 / 15:31 케이스를 신규 describe 로 추가한다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/intraday-sync test</automated>
  </verify>
  <done>
    marketWindow/eodClose/fetchRanking/runCycle 테스트 전부 green.
    `grep -v '^\s*//' workers/intraday-sync/src/kiwoom/fetchRanking.ts | grep -c 'stex_tp: "3"'` === 0
    (하드코딩 제거 확인).
    runCycle 테스트에서 08:30·15:45 사이클이 intradayUpsertClose 를 호출하지 않고,
    15:45 사이클만 runEodClosePass 를 호출함이 단언된다.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: candle-sync recover — 최근 2영업일 무조건 KRX 재적재</name>
  <files>
    workers/candle-sync/src/pipeline/recentDates.ts,
    workers/candle-sync/src/config.ts,
    workers/candle-sync/src/modes/recover.ts,
    workers/candle-sync/tests/recentDates.test.ts,
    workers/candle-sync/tests/runRecover.test.ts
  </files>
  <behavior>
    fetchRecentTradingDates(supabase, 2):
    - stock_daily_ohlcv 의 최근 distinct date 를 내림차순으로 최대 2개 반환
    - 조회 결과 없음 → [] 반환 (throw 아님)
    runRecover:
    - findMissingDates 가 [] 라도 forced 2일자에 대해 fetchBydd + upsertOhlcv 실행 → datesProcessed=2
    - forced 와 missing 이 겹치면 해당 일자는 1회만 fetch (중복 호출 없음)
    - forced 일자도 KRX 0 row / OHLV=0 비율 >50% 이면 skip (기존 가드 동일 적용)
    - forced + missing 합계가 recoverMaxCalls 를 넘으면 forced 를 먼저 보존한 채 상한 적용
    - forced 조회 자체가 실패해도 missing 경로는 계속 동작 (fail-open)
  </behavior>
  <action>
    D-fh2-03 구현. 실패 테스트 선행 커밋 → 구현 커밋 2단.

    (1) `workers/candle-sync/src/pipeline/recentDates.ts` 신규 — `fetchRecentTradingDates(supabase, n)`.
    missingDates.ts line 48~78 의 distinct date 조회 패턴을 그대로 재사용한다
    (오늘 기준 20일 전부터 `.gte("date", sinceIso).order("date", { ascending: false })` 후 클라이언트측
    Set dedupe → 앞에서 n 개). error 는 로그 후 throw (호출부가 fail-open 책임).

    (2) `config.ts` — `recoverForceRecentDays: parseNumberEnv(process.env.RECOVER_FORCE_RECENT_DAYS, 2)`
    를 Config 타입과 loadConfig 에 추가. 주석: "결측 판정과 무관하게 무조건 재적재할 최근 영업일 수.
    intraday-sync 가 매일 전 종목 row 를 만들어 결측 판정(row count < 활성×0.9)이 영영 발동하지 않는
    구조적 결함 보완 (D-fh2-03, 2026-08-20)". 0 이면 강제 재적재 비활성(킬 스위치)임을 명시.

    (3) `recover.ts` — findMissingDates 호출 전에 forced 일자를 구한다.
    `recoverForceRecentDays > 0` 일 때만 `fetchRecentTradingDates(supabase, n)` 를 try/catch 로 호출하고,
    실패 시 warn 로그 후 빈 배열로 진행(fail-open — 강제 재적재 실패가 기존 recover 를 죽이면 안 됨).
    `targetDates` = forced 를 앞에 두고 missingDates 를 이어붙인 뒤 Set 으로 중복 제거,
    마지막에 `slice(0, config.recoverMaxCalls)` 적용. forced 가 앞이므로 상한에 잘리지 않는다.
    빈 배열이면 기존 "no missing dates detected" 조기 반환 경로를 그대로 사용한다.
    시작 로그에 `{ forcedDates, missingDates, targetDates }` 를 남겨 어느 경로로 적재됐는지 사후 판별
    가능하게 한다. 루프 본문(0 row skip / >50% OHLV=0 skip / bootstrap / map / upsert / per-date 격리)은
    기존 그대로 재사용 — forced 일자에도 동일 가드가 걸린다.

    (4) 기존 `tests/runRecover.test.ts` 는 `../src/pipeline/recentDates` 를 mock 하지 않으면 실제 모듈이
    빈 supabase stub 을 호출해 깨진다. 파일 상단에 mock 을 추가하고 beforeEach 에서 기본값
    `mockRecent.mockResolvedValue([])` 로 리셋해 기존 4개 케이스의 기대값을 보존한다.
    loadConfig mock 에 `recoverForceRecentDays: 2` 를 추가한다.
  </action>
  <verify>
    <automated>pnpm --filter @gh-radar/candle-sync test</automated>
  </verify>
  <done>
    recentDates/runRecover 테스트 green. 기존 runRecover 4개 케이스 미회귀.
    "결측 0 + forced 2일자 → datesProcessed=2" 케이스가 통과한다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 배포 + 2026-05-21~2026-08-19 백필 + 정정 검증</name>
  <files>(코드 변경 없음 — 기존 scripts/deploy-intraday-sync.sh, scripts/deploy-candle-sync.sh 실행)</files>
  <action>
    D-fh2-04 / D-fh2-05 실행. 코드 변경 없이 배포 → 백필 → 검증만 수행한다.
    아래 &lt;what-built&gt; 의 명령 블록을 순서대로 실행하고, &lt;how-to-verify&gt; 항목을 확인한 뒤
    결과를 사용자에게 제시하고 승인 대기한다. 백필은 되돌리기 어려운 프로덕션 쓰기이므로
    사용자 승인 없이 완료 처리하지 않는다.
  </action>
  <what-built>
    Task 1·2 를 프로덕션에 배포하고, 오염 구간을 KRX 공식 일봉으로 백필한 뒤 정정 결과를 확인한다.
    백필은 기존 backfill 모드가 BACKFILL_FROM/BACKFILL_TO 를 이미 지원하므로 코드 변경 없이 실행만 한다
    (D-fh2-04). upsertOhlcv 가 onConflict(code,date) DO UPDATE 로 open/high/low/close/volume/
    trade_amount/change_amount/change_rate 전량을 덮어쓰므로 오염 컬럼이 모두 정정된다.

    실행 순서 (반드시 이 순서 — intraday-sync 를 먼저 고쳐야 백필 직후 재오염되지 않는다):

    ```
    export GOOGLE_APPLICATION_CREDENTIALS="$HOME/.config/gcloud/gh-radar-deployer.json"
    export CLOUDSDK_CORE_PROJECT=gh-radar
    export GCP_PROJECT_ID=gh-radar
    export SUPABASE_URL="$(grep '^SUPABASE_URL=' workers/master-sync/.env | cut -d= -f2-)"

    bash scripts/deploy-intraday-sync.sh
    bash scripts/deploy-candle-sync.sh

    gcloud run jobs execute gh-radar-candle-sync-backfill \
      --region=asia-northeast3 --wait \
      --update-env-vars=MODE=backfill,BACKFILL_FROM=2026-05-21,BACKFILL_TO=2026-08-19
    ```

    `gcloud run jobs execute --update-env-vars` 가 거부되면 fallback:
    `gcloud run jobs update gh-radar-candle-sync-backfill --region=asia-northeast3
     --update-env-vars=BACKFILL_FROM=2026-05-21,BACKFILL_TO=2026-08-19` → execute → 완료 후 동일 명령의
    `--remove-env-vars=BACKFILL_FROM,BACKFILL_TO` 로 원복.

    백필 완료 후 검증 쿼리 (Supabase REST, service role key 는 workers/master-sync/.env 에서 로드):
    ```
    SRK="$(grep '^SUPABASE_SERVICE_ROLE_KEY=' workers/master-sync/.env | cut -d= -f2-)"
    curl -s "$SUPABASE_URL/rest/v1/stock_daily_ohlcv?code=eq.000660&date=eq.2026-08-19&select=date,open,high,low,close,volume" \
      -H "apikey: $SRK" -H "Authorization: Bearer $SRK"
    ```
  </what-built>
  <how-to-verify>
    1. `bash scripts/deploy-intraday-sync.sh` 가 성공하고 Scheduler cron 이 `* 8-15 * * 1-5` 로 유지되는지
       (출력 마지막 줄) 확인.
    2. `bash scripts/deploy-candle-sync.sh` 가 Job 3개 + Scheduler 2개를 갱신하고 종료했는지 확인.
    3. 백필 Job 실행 로그에서 `runBackfill complete` 의 `daysProcessed` 가 약 62(거래일 수), `daysFailed`
       가 0 인지 확인. `MIN_EXPECTED violation` 또는 `KRX 401` 로 중단되지 않았어야 한다.
    4. 위 curl 결과 000660 / 2026-08-19 의 **close === 1500000** 이고 **close <= high** 인지 확인.
       (백필 전 값은 close 1,606,000 / high 1,559,000 이었다.)
    5. 005930 의 임의 오염 일자 1건을 같은 방식으로 조회해 close <= high 가 성립하는지 교차 확인.
    6. 다음 거래일 15:35~15:55 사이클 로그에서 `runEodClosePass` 가 실행돼 count > 0 을 남기고,
       15:31~15:34 및 16:00 이후 사이클(스케줄상 15:59 까지)에서는 일봉 쓰기 skip 로그가 뜨는지
       확인 (이 항목은 배포 당일 장 마감 후 사후 확인 — 즉시 검증 대상 아님).
  </how-to-verify>
  <verify>
    <automated>curl -s "$SUPABASE_URL/rest/v1/stock_daily_ohlcv?code=eq.000660&amp;date=eq.2026-08-19&amp;select=close,high" -H "apikey: $SRK" -H "Authorization: Bearer $SRK" | grep -q '"close":1500000'</automated>
    <human-check>배포 로그 + 백필 daysProcessed/daysFailed + 정정 종가를 사람이 확인</human-check>
  </verify>
  <done>
    intraday-sync·candle-sync 프로덕션 배포 완료(스케줄 유지), 백필 daysFailed=0,
    000660 2026-08-19 close=1500000 이고 close &lt;= high, 사용자 승인 획득.
  </done>
  <resume-signal>배포·백필·검증 결과를 확인했으면 "approved" 또는 발견한 문제를 알려주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 키움 OpenAPI → intraday-sync | 외부 응답이 일봉 DB 로 직행. 거래소 구분(stex_tp)에 따라 의미가 달라지는 신뢰 불가 입력 |
| KRX OpenAPI → candle-sync | 공식 인증키 배치 호출. stale/부분 응답 가능 |
| Cloud Run Job env override → 프로덕션 DB | 백필 범위 지정 실수 시 광범위 덮어쓰기 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-fh2-01 | Tampering | 키움 ka10027 stex_tp=3 응답 → stock_daily_ohlcv | mitigate | `isDailyWriteWindow` 로 09:00~15:30 밖 일봉 쓰기 차단 (Task 1) |
| T-fh2-02 | Tampering | EOD 패스가 16:00 이후 시간외 단일가를 종가로 기록 | mitigate | `isEodClosePass` 상한을 15:55 로 고정, 16:00 이후 실행 불가 |
| T-fh2-03 | Tampering | KRX stale 응답(전 종목 OHLV=0)이 정상 row 덮어씀 | mitigate | recover 기존 `>50% OHLV=0 skip` 가드를 forced 재적재에도 동일 적용 (Task 2) |
| T-fh2-04 | Denial of Service | EOD 패스 추가 호출로 키움 rate limit 초과 | mitigate | 5분 간격 5회로 제한 + 기존 `acquireKiwoomRateToken` 버킷 공유 |
| T-fh2-05 | Tampering | 백필 범위 오지정으로 정상 구간 덮어씀 | accept | `--wait` 로 관측하며 실행, 범위는 blocking 체크포인트에서 사람이 확인. upsert 는 KRX 공식값이라 정상 구간에 덮어써도 무해 |
| T-fh2-SC | Tampering | npm/pip/cargo installs | n/a | 신규 패키지 설치 없음 — 기존 의존성만 사용 |
</threat_model>

<verification>
- `pnpm --filter @gh-radar/intraday-sync test` green
- `pnpm --filter @gh-radar/candle-sync test` green
- `pnpm -r typecheck` (또는 워크스페이스 빌드) green
- 프로덕션: 000660 / 2026-08-19 close === 1500000, close <= high
</verification>

<success_criteria>
1. 정규장(09:00~15:30) 밖 사이클이 `stock_daily_ohlcv` 를 쓰지 않고 `stock_quotes` 만 갱신한다.
2. 15:35~15:55 사이클이 ka10027 stex_tp="1" 로 KRX 공식 종가를 일봉에 upsert 한다.
3. candle-sync recover 가 결측 판정 없이도 최근 2영업일을 KRX bydd_trd 로 재적재한다.
4. 2026-05-21~2026-08-19 백필이 완료되고 000660 2026-08-19 종가가 1,500,000 으로 정정된다.
5. 두 워커가 프로덕션에 배포되고 기존 스케줄이 유지된다.
</success_criteria>

<output>
Create `.planning/quick/260820-fh2-intraday-sync-nxt-15-30-krx-eod-recover/260820-fh2-SUMMARY.md` when done
</output>
