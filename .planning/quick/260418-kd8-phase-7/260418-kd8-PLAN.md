---
phase: quick-260418-kd8
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - QUICK-260418-kd8 — `/stocks/[code]/news` 풀페이지 무한 스크롤 페이지네이션 (Phase 8 토론방 1:1 미러)
files_modified:
  - server/src/schemas/news.ts
  - server/src/routes/news.ts
  - server/tests/routes/news.test.ts
  - webapp/src/lib/stock-api.ts
  - webapp/src/lib/__tests__/stock-api.test.ts
  - webapp/src/components/stock/news-page-client.tsx

must_haves:
  truths:
    - "사용자가 `/stocks/[code]/news` 에서 첫 100건을 보고 스크롤 하단 진입 시 다음 100건이 자동 append 된다"
    - "다음 페이지 응답이 PAGE_SIZE 미만이면 '최근 7일 뉴스를 모두 불러왔어요' 안내가 노출된다"
    - "pagination 도중 에러가 발생해도 기존 list 는 유지되고 sentinel 영역에 inline 에러 + 다시 시도 버튼이 노출된다 (stale-but-visible)"
    - "기존 Phase 7 news 의 14개 테스트는 변경 없이 그대로 통과한다 (회귀 0)"
  artifacts:
    - path: "server/src/schemas/news.ts"
      provides: "NewsListQuery 에 before(z.string().datetime({offset:true}).optional()) 필드 추가"
      contains: "before"
    - path: "server/src/routes/news.ts"
      provides: "GET 핸들러에서 before 가 있으면 .lt('published_at', before) 적용"
      contains: ".lt(\"published_at\""
    - path: "server/tests/routes/news.test.ts"
      provides: "V-news-cursor-a/b/c 테스트 — before 적용/미적용/invalid"
      contains: "before"
    - path: "webapp/src/lib/stock-api.ts"
      provides: "FetchNewsOpts 에 before?: string 추가 + URLSearchParams 전송"
      contains: "before"
    - path: "webapp/src/components/stock/news-page-client.tsx"
      provides: "IntersectionObserver + loadMore + sentinel + paginationError 상태 — DiscussionPageClient 1:1 미러"
      contains: "IntersectionObserver"
  key_links:
    - from: "webapp/src/components/stock/news-page-client.tsx"
      to: "/api/stocks/:code/news?before=<lastPublishedAt>"
      via: "fetchStockNews 의 before 옵션"
      pattern: "before:\\s*cursor"
    - from: "server/src/routes/news.ts GET"
      to: "supabase news_articles query"
      via: ".lt('published_at', before)"
      pattern: "\\.lt\\(\"published_at\""
---

<objective>
Phase 7 뉴스 풀페이지(`/stocks/[code]/news`)에 Phase 8 토론방과 동일한 IntersectionObserver 기반 무한 스크롤
페이지네이션을 적용한다. 새 디자인 결정은 일절 없다 — `discussion-page-client.tsx` / `discussions` 라우트 /
`DiscussionListQuery` 의 패턴을 1:1 로 미러링하며, news 측의 timestamp 필드 이름(`published_at` / `publishedAt`)
과 PAGE_SIZE(100, 기존 hard cap 그대로) 만 치환한다.

Purpose:
  - Phase 7 news 풀페이지의 100건 하드캡 너머의 글을 무한 스크롤로 탐색 가능하게 함.
  - Phase 8 에서 검증된 keyset pagination(`before=<lastPublishedAt>`) 구조를 재사용하여 신규 인덱스 / OFFSET 회피.
  - 동일 코드 패턴 유지로 향후 유지보수 일원화.

Output:
  - 서버 스키마/라우트/테스트 + webapp API wrapper/컴포넌트/테스트 6개 파일 수정.
  - Atomic 3 commit (한글, Co-Authored-By 없음).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/alex/repos/gh-radar/CLAUDE.md
@/Users/alex/repos/gh-radar/.planning/STATE.md

# Phase 8 reference (1:1 mirror source — 읽기 전용 참고)
@/Users/alex/repos/gh-radar/server/src/schemas/discussions.ts
@/Users/alex/repos/gh-radar/server/src/routes/discussions.ts
@/Users/alex/repos/gh-radar/server/tests/routes/discussions.test.ts
@/Users/alex/repos/gh-radar/webapp/src/lib/stock-api.ts
@/Users/alex/repos/gh-radar/webapp/src/components/stock/discussion-page-client.tsx

# Phase 7 news 측 (수정 대상)
@/Users/alex/repos/gh-radar/server/src/schemas/news.ts
@/Users/alex/repos/gh-radar/server/src/routes/news.ts
@/Users/alex/repos/gh-radar/server/tests/routes/news.test.ts
@/Users/alex/repos/gh-radar/webapp/src/lib/__tests__/stock-api.test.ts
@/Users/alex/repos/gh-radar/webapp/src/components/stock/news-page-client.tsx
@/Users/alex/repos/gh-radar/packages/shared/src/news.ts

<interfaces>
<!-- 핵심 계약 — 코드베이스 탐색 불필요. 그대로 사용. -->

NewsArticle (packages/shared/src/news.ts) — camelCase, timestamp 는 `publishedAt` (ISO 8601):
```typescript
export type NewsArticle = {
  id: string;
  stockCode: string;
  title: string;
  description?: string | null;
  source: string | null;
  url: string;
  publishedAt: string;     // ← cursor 로 사용
  contentHash: string | null;
  summaryId: string | null;
  createdAt: string;
};
```

DB column 이름은 snake_case `published_at` (server route .order/.lt 에 그대로 사용).

Phase 8 NewsListQuery 와 동일 의미를 갖는 cursor 필드 정의:
```typescript
before: z.string().datetime({ offset: true }).optional()
```
NewsListQuery 는 현재 `.transform()` 가 각 필드에 개별 적용된 단순 z.object 이므로
`before` 필드를 같은 패턴으로 추가하면 `queryParsed.data.before` 로 그대로 접근 가능.
(DiscussionListQuery 의 second-stage `.transform()` 래퍼 흉내는 불필요 — news 는 windowMs 합성이 없음)

PAGE_SIZE 정책 (1:1 미러 차이점):
  - DiscussionPageClient: PAGE_SIZE = 50 (서버 hard cap)
  - NewsPageClient (이번 작업): PAGE_SIZE = 100 (서버 hard cap, 기존 fetchStockNews 기본값과 일치)

기존 fetchStockNews 호출부 (NewsPageClient L45):
```typescript
fetchStockNews(code, { days: 7, limit: 100 }, controller.signal)
```
→ 그대로 유지. before 옵션만 loadMore 호출에서 추가.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: server — NewsListQuery before 필드 + GET 핸들러 .lt 적용 + 3개 테스트 추가</name>
  <files>
    server/src/schemas/news.ts,
    server/src/routes/news.ts,
    server/tests/routes/news.test.ts
  </files>

  <behavior>
    - V-news-cursor-a: `?before=<ISO>` 전달 시 200 + supabase chain 의 `lt` 가 호출됨 (`from('news_articles')` 호출 검증).
    - V-news-cursor-b: before 미지정 시 기존 동작 유지 (200, 기존 14개 테스트 회귀 0).
    - V-news-cursor-c: `?before=not-iso` → 400 INVALID_QUERY_PARAM (z.string().datetime() reject).
    - 기존 14개 news 테스트는 변경 없이 그대로 통과 (clamp / 400 / 404 / 429 / 503 / CORS).
  </behavior>

  <action>
    1) `server/src/schemas/news.ts` — NewsListQuery 의 z.object 에 `before` 필드를 추가:
       ```typescript
       before: z
         .string()
         .datetime({ offset: true })
         .optional(),
       ```
       기존 `days` / `limit` 의 `.transform()` clamp 로직은 손대지 말 것 (회귀 차단).
       파일 상단 docstring 에 한 줄 추가:
       "before: 무한 스크롤 cursor — ISO 8601 timestamp. 응답에서 `published_at < before` 인 글만 반환."

    2) `server/src/routes/news.ts` GET 핸들러 — DiscussionListQuery + discussions GET 의 패턴을 그대로 미러:
       - 구조분해에 `before` 추가: `const { days, limit, before } = queryParsed.data;`
       - 기존 single-statement `.from('news_articles').select(...).eq(...).gte(...).order(...).limit(limit)` 를
         **반드시** `let q = supabase.from(...).select(...)...limit(limit);` 로 분리하고
         바로 다음 줄에 `if (before) q = q.lt('published_at', before);` 추가한 뒤
         `const { data, error } = await q;` 로 await.
       - POST `/refresh` 핸들러 내부의 별도 `from('news_articles')` 호출 (cooldown probe / 갱신 후 list)
         은 손대지 말 것. before 는 GET 만의 옵션.

    3) `server/tests/routes/news.test.ts` — 기존 chainable mock 의 `chain` 객체에 `chain.lt` 추가:
       ```typescript
       chain.lt = vi.fn().mockReturnValue(chain);
       ```
       (기존 chain.gte 바로 다음 줄에 삽입. 기존 14개 테스트는 lt 를 호출하지 않으므로 회귀 0.)

       `describe("GET /api/stocks/:code/news ...")` 블록 끝에 3개 `it()` 추가
       (Phase 8 V-08a/b/c 와 동일 구조 — 'news_articles' / 'published_at' 으로 치환):

       ```typescript
       it("V-news-cursor-a infinite scroll cursor `before=<ISO>` → SQL applies published_at < before", async () => {
         const supabase = makeSupabase({
           "stocks.single": { code: "005930" },
           "news_articles.list": [snakeNewsRow(0)],
         });
         const app = createApp({
           supabase: supabase as unknown as Parameters<typeof createApp>[0]["supabase"],
         });
         const before = "2026-04-17T05:30:00.000Z";
         const res = await request(app).get(
           `/api/stocks/005930/news?days=7&limit=100&before=${encodeURIComponent(before)}`,
         );
         expect(res.status).toBe(200);
         expect(res.body).toHaveLength(1);
         const newsCalls = (supabase.from as ReturnType<typeof vi.fn>).mock.calls.filter(
           (c) => c[0] === "news_articles",
         );
         expect(newsCalls.length).toBeGreaterThan(0);
       });

       it("V-news-cursor-b before 미지정 시 lt 미호출 — 첫 페이지 동작 회귀 없음", async () => {
         const supabase = makeSupabase({
           "stocks.single": { code: "005930" },
           "news_articles.list": [snakeNewsRow(0)],
         });
         const app = createApp({
           supabase: supabase as unknown as Parameters<typeof createApp>[0]["supabase"],
         });
         const res = await request(app).get(
           "/api/stocks/005930/news?days=7&limit=100",
         );
         expect(res.status).toBe(200);
       });

       it("V-news-cursor-c invalid before (non-ISO) → 400 INVALID_QUERY_PARAM", async () => {
         const supabase = makeSupabase({ "stocks.single": { code: "005930" } });
         const app = createApp({
           supabase: supabase as unknown as Parameters<typeof createApp>[0]["supabase"],
         });
         const res = await request(app).get(
           "/api/stocks/005930/news?days=7&before=not-iso",
         );
         expect(res.status).toBe(400);
         expect(res.body?.error?.code).toBe("INVALID_QUERY_PARAM");
       });
       ```

    4) commit (한글, Co-Authored-By 금지):
       `feat(server/news): /api/stocks/:code/news 에 before cursor 추가 — 무한 스크롤 페이지네이션 (Phase 8 1:1 미러)`
  </action>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar/server && pnpm vitest run tests/routes/news.test.ts</automated>
    추가 검증: 기존 14개 + 신규 3개 = 17개 모두 PASS.
    추가로 `pnpm tsc --noEmit` 으로 NewsListQuery 변경의 타입 회귀 0 확인.
  </verify>

  <done>
    - server/src/schemas/news.ts NewsListQuery 에 before 옵션 필드 존재.
    - server/src/routes/news.ts GET 가 before 있으면 `.lt('published_at', before)` 적용.
    - tests/routes/news.test.ts 에 V-news-cursor-a/b/c 추가, 17/17 PASS.
    - tsc --noEmit 통과.
    - 1개 atomic commit (한글, Co-Authored-By 없음).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: webapp lib — fetchStockNews 에 before 옵션 + 단위 테스트</name>
  <files>
    webapp/src/lib/stock-api.ts,
    webapp/src/lib/__tests__/stock-api.test.ts
  </files>

  <behavior>
    - hours/days/limit 만 전달 시 URL 에 `before=` 미포함 (기존 회귀 0).
    - before 옵션 → URL 에 `before=<encoded ISO>` 포함.
    - days/limit 와 before 동시 전달 시 모두 URL 에 포함.
  </behavior>

  <action>
    1) `webapp/src/lib/stock-api.ts` — `FetchNewsOpts` 인터페이스 확장 (DiscussionsOpts 의 before 주석 그대로 미러):
       ```typescript
       export interface FetchNewsOpts {
         /** 조회 기간 (일). 기본 7. */
         days?: number;
         /** 최대 반환 건수. 기본 100. */
         limit?: number;
         /**
          * 무한 스크롤 cursor — ISO 8601 timestamp.
          * 서버는 `published_at < before` 인 기사만 반환 → 마지막 페이지의 마지막 기사 publishedAt 을
          * 다음 호출에 전달하면 자연스러운 키셋 페이지네이션이 됨.
          */
         before?: string;
       }
       ```

       `fetchStockNews` 본문 수정 — 현재 두 줄로 set 하는 URLSearchParams 를 그대로 두고 한 줄 추가:
       ```typescript
       const params = new URLSearchParams({
         days: String(opts.days ?? 7),
         limit: String(opts.limit ?? 100),
       });
       if (opts.before) params.set('before', opts.before);
       return apiFetch<NewsArticle[]>(
         `/api/stocks/${encodeURIComponent(code)}/news?${params.toString()}`,
         { signal },
       );
       ```
       파일 상단 JSDoc 의 `fetchStockNews` 한 줄 옆에 "(before cursor 옵션 지원, 무한 스크롤)" 추가.

    2) `webapp/src/lib/__tests__/stock-api.test.ts` — top of file 에서 import 에 `fetchStockNews` 추가:
       ```typescript
       import { searchStocks, fetchStockDetail, fetchStockDiscussions, fetchStockNews } from '../stock-api';
       ```

       fetchStockDiscussions describe 블록 직전 또는 직후에 새 describe 추가
       (fetchStockDiscussions 의 3개 케이스를 1:1 미러):

       ```typescript
       describe('fetchStockNews (260418-kd8 infinite scroll cursor)', () => {
         beforeEach(() => mockFetch.mockReset().mockResolvedValue([]));

         it('days/limit 만 전달 시 before 미포함', async () => {
           const signal = new AbortController().signal;
           await fetchStockNews('005930', { days: 7, limit: 100 }, signal);
           const url = mockFetch.mock.calls[0]![0] as string;
           expect(url).toContain('days=7');
           expect(url).toContain('limit=100');
           expect(url).not.toContain('before=');
         });

         it('before 옵션 → URL 에 before=<encoded ISO> 포함', async () => {
           const signal = new AbortController().signal;
           const cursor = '2026-04-17T05:32:00.000Z';
           await fetchStockNews(
             '005930',
             { days: 7, limit: 100, before: cursor },
             signal,
           );
           const url = mockFetch.mock.calls[0]![0] as string;
           expect(url).toContain(`before=${encodeURIComponent(cursor)}`);
           expect(url).toContain('days=7');
           expect(url).toContain('limit=100');
         });

         it('opts 비어있을 때 days=7, limit=100 default 사용', async () => {
           const signal = new AbortController().signal;
           await fetchStockNews('005930', {}, signal);
           const url = mockFetch.mock.calls[0]![0] as string;
           expect(url).toContain('days=7');
           expect(url).toContain('limit=100');
         });
       });
       ```

    3) commit (한글, Co-Authored-By 금지):
       `feat(webapp/lib): fetchStockNews 에 before cursor 옵션 추가 — 무한 스크롤 페이지네이션`
  </action>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && pnpm vitest run src/lib/__tests__/stock-api.test.ts</automated>
    기존 5개 테스트 + 신규 3개 = 8개 모두 PASS.
    추가로 `pnpm tsc --noEmit` 으로 FetchNewsOpts 변경의 타입 회귀 0 확인.
  </verify>

  <done>
    - FetchNewsOpts 에 before?: string 추가됨.
    - fetchStockNews 가 before 옵션 시 URL 에 포함.
    - 신규 3개 unit 테스트 PASS.
    - tsc --noEmit 통과.
    - 1개 atomic commit (한글, Co-Authored-By 없음).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: webapp — NewsPageClient 무한 스크롤 (DiscussionPageClient 1:1 미러)</name>
  <files>webapp/src/components/stock/news-page-client.tsx</files>

  <behavior>
    - 첫 mount 시 fetchStockNews({ days:7, limit:100 }) 호출 (기존 동작 그대로).
    - 첫 페이지 응답 < PAGE_SIZE → hasMore=false.
    - 첫 페이지 응답이 PAGE_SIZE 이상이면 sentinel 이 viewport 진입 시 자동으로 다음 페이지 fetch
      (`fetchStockNews({ days:7, limit:PAGE_SIZE, before: lastPublishedAt })`) → 결과 append.
    - `id` 기준 dedup (동일 publishedAt 경계에서 두 페이지에 걸칠 가능성 안전망).
    - pagination 에러 → 기존 list 유지 + sentinel 영역에 inline 에러 + "다시 시도" 버튼.
    - hasMore=false 일 때 sentinel 영역에 "최근 7일 뉴스를 모두 불러왔어요" 안내.
    - 기존 404 → notFound() / 초기 로드 에러 → 고정 copy + 다시 시도 버튼 동작 그대로 유지.
  </behavior>

  <action>
    `webapp/src/components/stock/news-page-client.tsx` 를 `discussion-page-client.tsx` 와 1:1 미러
    구조로 재작성한다. 도메인 치환표:

    | discussion 측              | news 측 (이번 작업)         |
    |---------------------------|--------------------------|
    | Discussion / Discussion[]  | NewsArticle / NewsArticle[] |
    | fetchStockDiscussions      | fetchStockNews            |
    | { days: 7, limit: 50 }     | { days: 7, limit: 100 }   |
    | PAGE_SIZE = 50             | PAGE_SIZE = 100           |
    | discussions / setDiscussions | articles / setArticles  |
    | last.postedAt              | last.publishedAt          |
    | seen = postId 기준           | seen = id 기준 (NewsArticle 에는 id 만 unique)         |
    | DiscussionItem variant="full" | NewsItem variant="full" (기존 그대로) |
    | DiscussionListSkeleton variant="full" | NewsListSkeleton (기존 그대로) |
    | "최근 7일 토론을 모두 불러왔어요" | "최근 7일 뉴스를 모두 불러왔어요" |
    | "추가 글을 불러오지 못했어요"   | "추가 뉴스를 불러오지 못했어요" |
    | data-testid="discussion-pagination-sentinel" | data-testid="news-pagination-sentinel" |
    | data-testid="discussion-pagination-loading" | data-testid="news-pagination-loading" |
    | data-testid="discussion-pagination-error"   | data-testid="news-pagination-error"   |
    | data-testid="discussion-pagination-end"     | data-testid="news-pagination-end"     |
    | "표시할 토론 글이 없어요" / discussion-page-empty | (기존 "표시할 뉴스가 없어요" / news-page-empty 유지) |

    구체 미러 사항:
    1) imports: useCallback, useEffect, useRef, useState 모두 사용. `Discussion` 대신 `NewsArticle`.
       `DiscussionItem`/`DiscussionListSkeleton` 대신 기존 `NewsItem`/`NewsListSkeleton` 유지.

    2) state — 기존 stock/articles/isLoading/error/notFoundFlag/controllerRef 에 추가:
       ```typescript
       const [hasMore, setHasMore] = useState(true);
       const [isFetchingMore, setIsFetchingMore] = useState(false);
       const [paginationError, setPaginationError] = useState<Error | null>(null);
       const sentinelRef = useRef<HTMLDivElement | null>(null);
       const inFlightCursorRef = useRef<string | undefined>(undefined);
       ```

    3) `const PAGE_SIZE = 100;` 모듈 상수 (DiscussionPageClient 와 같은 위치).

    4) `load` 콜백 — discussion-page-client L49-83 그대로 미러:
       - 진입 시 `setHasMore(true); setPaginationError(null); inFlightCursorRef.current = undefined;`
       - fetchStockNews 는 `{ days: 7, limit: PAGE_SIZE }` 전달.
       - 성공 시 `if (newsData.length < PAGE_SIZE) setHasMore(false);` 추가.

    5) `loadMore` 콜백 — discussion-page-client L85-117 그대로 미러:
       - `if (!articles || articles.length === 0) return;`
       - `const last = articles[articles.length - 1];`
       - `const cursor = last.publishedAt;` ← postedAt → publishedAt 치환.
       - dedup: `const seen = new Set(prev.map((a) => a.id));` ← postId → id 치환.
       - fetchStockNews 호출에 `before: cursor` 포함.

    6) IntersectionObserver useEffect — discussion-page-client L125-142 그대로 미러
       (의존성 배열을 `[articles, hasMore, loadMore]` 로 치환).

    7) JSX:
       - 기존 header / 초기 에러 카드 / loading skeleton / empty state / 리스트 컨테이너 모두 유지.
       - 기존 `<ul>...{articles.map((a)=>...)}</ul>` 바로 아래에 sentinel + 상태 표시 div 추가.
         (discussion-page-client L221-246 의 sentinel 블록 그대로 미러.
          단 testid 와 카피는 위 치환표대로.)

    8) load 콜백 의존성 배열은 `[code]` 그대로. setHasMore/setPaginationError/inFlightCursorRef 는
       useState/useRef setter 라 의존성 불필요 (discussion-page-client 와 동일).

    유지 대상 (변경 금지):
       - h1 카피 "{stock.name} — 최근 7일 뉴스".
       - 새로고침 버튼 없음 (UX 결정 그대로).
       - 404 → notFound().
       - data-testid="news-page-loading" / "news-page-empty" / "news-page-error" / "news-list".
       - NewsItem variant="full" 호출 시그니처.

    9) commit (한글, Co-Authored-By 금지):
       `feat(webapp/news): NewsPageClient 무한 스크롤 적용 — IntersectionObserver + before cursor (Phase 8 토론방 1:1 미러)`
  </action>

  <verify>
    <automated>cd /Users/alex/repos/gh-radar/webapp && pnpm tsc --noEmit && pnpm lint --filter . 2>&1 | tail -20</automated>

    수동 (autonomous 가능, 사용자 개입 불필요): `webapp/src/components/stock/news-page-client.tsx` 를
    `discussion-page-client.tsx` 와 diff 떠서 구조가 1:1 (도메인 치환만 차이) 인지 self-check.
    체크리스트:
    - [ ] PAGE_SIZE 상수 정의됨 (= 100)
    - [ ] hasMore / isFetchingMore / paginationError / sentinelRef / inFlightCursorRef 5개 추가됨
    - [ ] load 콜백 진입 시 hasMore=true 리셋 + 응답 PAGE_SIZE 미만 시 hasMore=false
    - [ ] loadMore 콜백에서 last.publishedAt 을 cursor 로 사용 + id 기준 dedup
    - [ ] IntersectionObserver useEffect (rootMargin: '200px')
    - [ ] sentinel div + 3개 상태 카피 (loading / error+retry / end) 노출

    추가: webapp dev server (PORT=3100, dev.sh 기준) 가 이미 실행 중이라면
    브라우저에서 `/stocks/005930/news` 접속 시 무한 스크롤 동작 확인 가능 (수동, optional).
  </verify>

  <done>
    - news-page-client.tsx 가 discussion-page-client.tsx 와 1:1 구조 (도메인 치환만 차이).
    - tsc --noEmit / lint PASS.
    - 1개 atomic commit (한글, Co-Authored-By 없음).
    - 3 commit 모두 push 완료 (CLAUDE.md 규칙: commit 후 push).
  </done>
</task>

</tasks>

<verification>
**Phase 7 회귀 검증 (필수):**
- `cd server && pnpm vitest run tests/routes/news.test.ts` → 17/17 PASS (기존 14 + 신규 3).
- `cd webapp && pnpm vitest run src/lib/__tests__/stock-api.test.ts` → 8/8 PASS (기존 5 + 신규 3).
- `cd server && pnpm tsc --noEmit` → 0 error.
- `cd webapp && pnpm tsc --noEmit && pnpm lint` → 0 error / 0 warning.

**Phase 8 회귀 0 확인:**
- discussion 라우트/컴포넌트 파일은 read-only 참고만 — 수정 0.
- discussions 테스트 는 미실행이 안전 (변경 없음).
</verification>

<success_criteria>
- 서버 GET `/api/stocks/:code/news?days=7&limit=100&before=<ISO>` 가 published_at < before 결과만 반환.
- 잘못된 before (non-ISO) → 400 INVALID_QUERY_PARAM.
- before 미지정 시 기존 응답 동작 회귀 0 (V-13/V-15/V-16 통과).
- webapp `/stocks/[code]/news` 풀페이지 스크롤 하단 진입 시 자동으로 다음 100건 append.
- 마지막 페이지에서 "최근 7일 뉴스를 모두 불러왔어요" 안내.
- 페이지네이션 에러 시 기존 list 유지 + inline 에러 + 다시 시도 버튼.
- 3개 atomic 한글 commit, Co-Authored-By 없음, push 완료.
</success_criteria>

<output>
이 작업은 quick task 라 SUMMARY.md 자동 생성 대상이 아님 — 작업 결과는 commit log 와 위 success_criteria 로
기록 종료. 필요 시 사용자가 STATE.md Pending Todos 또는 RETROSPECTIVE.md 에 한 줄 추가 가능.
</output>
