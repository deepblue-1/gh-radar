---
phase: quick-260926-d76
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/src/app/login/page.tsx
  - webapp/src/app/login/__tests__/page.test.tsx
autonomous: true
requirements: [D76-LAYOUT, D76-CTA, D76-STATE, D76-LOGIC, D76-VERIFY]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "/login 에 카드 · 제목 · 설명 문구가 없다. 화면은 앱 아이콘 A(76×76 · radius 22%) + 워드마크 h1 「GH Trade」(26px/800 · letter-spacing −0.03em)가 세로·가로 가운데(gap 22, md 이상 24)에 서고, 그 밖의 글자는 버튼 라벨과 오류 알림뿐이다 (D76-LAYOUT)"
    - "폰(<768px): Google 버튼이 좌우 20 · 바닥 44 + --app-safe-bottom 위치에 풀폭으로 선다 — 390×844 에서 left 20 · width 350 · bottom 800, --safe-area-inset-bottom 34px 주입 시 bottom 766. md(≥768) 이상: 워드마크 아래 360 폭 가로 가운데 · 바닥 여백 200 — 1280×800 에서 left 460 · width 360 · bottom 600 (D76-LAYOUT)"
    - "버튼은 반전 CTA — 높이 56 · radius 16 · 16px/700. 라이트 배경 rgb(25, 31, 40) · 글자 rgb(255, 255, 255), 다크는 그 반대. hover 는 배경색 불변 + 필터만(라이트 brightness(1.25) · 다크 brightness(0.94)), active scale .985 (D76-CTA)"
    - "진행 중(pending): G 아이콘·라벨 대신 20px 스피너 1개 + 70% 불투명 · disabled · aria-busy=true, 접근 이름 「Google로 로그인」 은 aria-label 로 유지된다 (D76-STATE)"
    - "오류: ?error= 4종 · 네이티브 실패 문구가 버튼 바로 위(간격 12) radius 12 알림에 --up-bg 배경 / --up 글자 13.5px/500 으로 뜨고, role=alert · aria-live=polite · ERROR_MESSAGES 4종 문구는 바이트 그대로다 (D76-STATE)"
    - "로그인 로직 불변 — 브라우저 OAuth(redirectTo · prompt=select_account) · 앱 nativeGoogleSignIn → location.replace(safeNext) · //evil.com 차단 · 앱 오류 우선 · 재클릭 방지. login vitest 10건(기존 8건 무수정 + 교체 1 + 신규 1) 통과 (D76-LOGIC)"
    - "코드 커밋은 `feat(quick-260926-d76): …` 1건 — 한글 · 공동 저자 트레일러 없음 · 정확히 2개 경로 · push 없음. :3100 dev 서버(PID 97397)는 끄거나 재시작하지 않는다. e2e 는 통과/미실행(+이유)이 SUMMARY 에 사실대로 적힌다 (D76-VERIFY)"
  artifacts:
    - path: "webapp/src/app/login/page.tsx"
      provides: "sketch 006 A 「비움」 로그인 화면 — 앱 아이콘 + 워드마크 가운데, 폰 하단(safe-area) / md+ 360 폭 반전 Google CTA, 오류 알림 · 진행 중 스피너"
      contains: 'data-slot="login-wordmark"'
    - path: "webapp/src/app/login/__tests__/page.test.tsx"
      provides: "워드마크 단독(제목·설명 부재) 케이스 + 진행 중 스피너·접근 이름 유지 케이스 — 기존 로직 8건 무수정"
      contains: "login-spinner"
  key_links:
    - from: "page.tsx 앱 아이콘 img"
      to: "webapp/src/app/icon.svg (Next 메타데이터 라우트 /icon.svg)"
      via: "src=/icon.svg — dev(:3100)·운영(Vercel) 모두 200 image/svg+xml 확인, middleware matcher 가 .svg 를 제외해 비로그인에도 열린다"
    - from: "page.tsx 하단 영역 padding-bottom"
      to: "webapp/src/styles/globals.css §21 --app-safe-bottom"
      via: "calc(44px + var(--app-safe-bottom)) — 앱은 Capacitor 주입값(--safe-area-inset-bottom), 브라우저는 폴백"
    - from: "page.tsx Button className"
      to: "webapp/src/components/ui/button.tsx buttonVariants + cn(twMerge)"
      via: "className 이 default variant 의 bg · hover:bg · text · rounded · h · disabled:opacity 를 twMerge 로 덮는다 — Task 2 계산 스타일 점검이 증명"
---

<objective>
사용자가 sketch 006 에서 채택한 A안 「비움」을 `/login` 에 구현한다. 카드 · 제목 · 설명 문구를 걷어내고, 화면을 앱 아이콘 A + 워드마크 「GH Trade」 + Google 버튼 하나로 세운다. 버튼은 반전 CTA(라이트 짙은 바탕 / 다크 흰 바탕)이고, 폰에서는 하단(safe-area 포함)에, md 이상에서는 워드마크 아래 360 폭으로 선다.

앱(Phase 21)은 Remote-URL 셸이라 운영 웹 `/login` 을 그대로 렌더한다 — 웹 한 번 고치면 앱도 같은 화면이다. 네이티브 id_token 로그인 · 브라우저 OAuth · safeNext 가드 등 **로직은 한 줄도 바꾸지 않고 시각만** 바꾼다.

Purpose: 문구 없이 브랜드(아이콘 · 이름)와 버튼 하나로 「무슨 앱인지」를 전달하는 토스식 로그인 화면(sketch 006 결정 2026-09-26).
Output: `page.tsx` 시각 교체, `page.test.tsx` 케이스 1건 교체 + 1건 추가, 검증 결과, 코드 커밋 1건.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/sketches/006-login-screen/README.md
@webapp/src/app/login/page.tsx
@webapp/src/app/login/__tests__/page.test.tsx
@webapp/src/components/ui/button.tsx

<interfaces>
<!-- 실행자가 코드베이스를 다시 탐색하지 않도록 계획 단계에서 확인해 둔 사실. -->

채택 시각 계약 — sketch 006 README 「결정」 절 + index.html 변형 A:
- 가운데(`.la .mid`): flex-1 · 세로·가로 가운데 · gap 22(데스크톱 24) · padding-top = 상단 safe-area.
- 아이콘(`.mark`): 76×76 · border-radius 22% · overflow hidden. 워드마크(`.wordmark`): 26px · 800 · letter-spacing −.03em · line-height 1.
- 하단(`.la .foot`): padding 0 20px 44px. 데스크톱(`.desk .la .foot`): width 360 · margin 0 auto · padding 0 0 200px.
- 버튼(`.gbtn`): flex 가운데 · gap 10 · width 100% · height 56 · radius 16 · 16px/700 · letter-spacing −.01em · transition transform/filter/opacity · 다크 #ffffff/#191f28 · 라이트 #191f28/#ffffff · hover 다크 brightness(.94) / 라이트 brightness(1.25) · active scale(.985) · G 아이콘 20px.
- 진행 중(`.busy`): 버튼 opacity .7 · 포인터 차단 · G·라벨 숨김 · 스피너 20px(border 2.5px currentColor · 오른쪽 투명 · .8s linear 회전).
- 알림(`.alert`): flex · gap 8 · padding 12px 14px · radius 12 · bg --up-bg · color --up · 13.5px/500 · line-height 1.4 · margin-bottom 12 · 경고 아이콘 16px.

코드베이스 사실(계획 단계 확인):
- 앱 아이콘 A 원본 = `webapp/src/app/icon.svg`(sketch 004 `#app-a` 심볼과 path 동일). Next 메타데이터 라우트로 `/icon.svg` 에 서빙 — dev :3100 과 운영 Vercel 모두 200 image/svg+xml. `webapp/src/middleware.ts` matcher 가 `.svg` 를 제외하므로 비로그인에도 열린다. 다른 브랜드 마크 컴포넌트는 없다.
- `<img>` 선례: `webapp/src/components/me/account-card.tsx` 64행 — `// eslint-disable-next-line @next/next/no-img-element` + 사유 주석.
- 토큰(globals.css): 라이트 `--bg #ffffff` · `--fg #191f28` · `--up #f04452` · `--up-bg #ffeeee` / 다크 `--bg #17171c` · `--fg #ffffff` · `--up #f04251` · `--up-bg rgba(240, 66, 81, 0.16)`. 반경 `--r 12px` · `--r-md 16px`. 이 프로젝트의 `rounded-xl`/`rounded-2xl` 은 `--radius` 배수라 12/16 을 보장하지 않는다 → 임의값 `rounded-[var(--r)]` · `rounded-[var(--r-md)]` 를 쓴다.
- dark 변형: `@custom-variant dark (&:where(.dark, .dark *))`. next-themes `attribute="class"` · `defaultTheme="light"` · `enableSystem={false}` · 저장 키 기본값 `theme`.
- safe-area(§21): `--app-safe-top/bottom/left/right` 만 읽는다. `/login` 은 네이티브 탭바가 숨는 경로(`mobile/ios/App/App/TabRoutes.swift` · Android `TabRoutes.kt` — D-12 ①)라 탭바 몫이 없다. 루트 layout 은 children 을 main 으로 감싸지 않는다.
- `Button`(`components/ui/button.tsx`): base 에 `rounded-[var(--r)] font-medium gap-2 transition-[background,border-color,opacity] duration-[120ms] disabled:pointer-events-none disabled:opacity-50 active:opacity-90`, default variant 에 `bg-[var(--primary)] text-[var(--primary-fg)] hover:bg-[color-mix(in_oklch,var(--primary)_88%,black)]`, size lg 에 `h-10 px-[18px] text-[var(--t-base)]`. `data-slot="button"` 을 스스로 붙이고 props 를 뒤에 펼친다. className 은 `cn()`(tailwind-merge) 으로 합쳐진다.
- lucide-react 1.x 설치됨 · `CircleAlert` export 확인.
- `min-h-dvh` 선례: `components/layout/center-shell.tsx` · `app-shell.tsx`.
- 현재 SSR HTML(`curl :3100/login`)에 로그인 본문이 그대로 렌더된다(Suspense 폴백 아님) — SSR 마커 검사가 유효하다.
- 접지된 명령: `pnpm --filter @gh-radar/webapp exec vitest --run src/app/login`(현재 9 passed) · `pnpm --filter @gh-radar/webapp run typecheck`(현재 통과) · `pnpm --filter @gh-radar/webapp exec eslint src/app/login`(현재 0).
- e2e: `playwright.config.ts` baseURL :3100 · `reuseExistingServer: !CI` · `chromium` 프로젝트가 `setup`(auth.setup.ts — NEXT_PUBLIC_SUPABASE_URL · NEXT_PUBLIC_SUPABASE_ANON_KEY · E2E_TEST_PASSWORD 필요)에 의존. `auth-guards.spec.ts` 는 파일 레벨 `test.use({ storageState: { cookies: [], origins: [] } })` 라 로그인 세션이 필요 없다. 로그인 화면 단언은 에러 문구 텍스트 · `getByRole("button", { name: /Google.*로그인|Google로 로그인/ })` 뿐(제목·설명 단언 없음).
</interfaces>
</context>

<constraints>
운영 제약 — 반드시 지킨다:
- 이 working tree 에서 `:3100` Next dev 서버(PID 97397)가 다른 세션의 UAT 용으로 돌고 있다. 끄거나 재시작 금지. 파일 저장 시 HMR 로 즉시 반영되는 것은 의도된 상태.
- `git push` 금지. 커밋은 `git add <명시 경로>` 만(`git add -A`/`-u` 금지) — 다른 세션이 남긴 `.planning/state.json`, `tasks/lessons.md`, `.planning/milestone.lock` 을 절대 스테이징하지 않는다.
- 커밋 메시지는 한글, `feat(quick-260926-d76): …` 형식, Co-Authored-By 넣지 않기.
- worktree 격리 없이 main tree 에서 순차 실행.
- 범위: `webapp/src/app/login/page.tsx` · `webapp/src/app/login/__tests__/page.test.tsx` 두 파일만. native 코드(mobile/) · middleware · auth callback · 다른 페이지 · `button.tsx` · `globals.css` 수정 금지.
</constraints>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: /login 시각 교체 한 경로 — 테스트 교체·추가(RED) → page.tsx A안(GREEN) → dev 서버 SSR 까지 확인</name>
  <files>webapp/src/app/login/__tests__/page.test.tsx, webapp/src/app/login/page.tsx</files>
  <behavior>
    - (교체 — 69행 케이스) `it('워드마크 「GH Trade」 만 있고 설명 문구는 없다')`: `screen.getByText('GH Trade')` 존재 · `screen.getByRole('heading', { level: 1, name: 'GH Trade' })` 존재 · `screen.queryByText(/관심종목을 저장/)` 가 null · `screen.queryByText('GH Trade에 로그인')` 가 null.
    - (신규 — 파일 끝 describe 안) `it('진행 중에는 G 아이콘·라벨 대신 스피너 — 접근 이름 「Google로 로그인」 은 유지')`: 앱 모드(`nativeDetect.mockReturnValue(true)`) + 끝나지 않는 `nativeSignIn`(`new Promise(() => {})`). 클릭 전: `button()` 의 textContent 에 「Google로 로그인」 포함 · `button().querySelector('[data-slot="login-spinner"]')` null. `fireEvent.click(button())` 후: `button()`(이름으로 조회)이 여전히 찾히고 · `button()` 이 텍스트 「Google로 로그인」 을 포함하지 않으며(`not.toHaveTextContent`) · `button().querySelector('svg')` null · `button().querySelectorAll('[data-slot="login-spinner"]')` 길이 1.
    - 나머지 8건(브라우저 OAuth · 앱 성공 · //evil.com · 취소 · 플러그인 실패 · Supabase error · 오류 우선순위 · 진행 중 disabled/aria-busy)은 한 글자도 고치지 않고 통과한다.
  </behavior>
  <action>
**RED — 테스트 먼저.** `page.test.tsx` 에서 69~72행 제목 케이스를 behavior 의 교체 케이스로 바꾸고, describe 끝에 behavior 의 신규 케이스를 추가한다. 파일 상단 JSDoc 목록 끝에 한 줄 — 「워드마크만 · 진행 중 스피너(접근 이름 유지) → 화면 개편(sketch 006 A · quick-260926-d76)이 로그인 수단을 가리지 않는다」 취지. 기존 8건 · 목(mock) 설정 · `button()` 헬퍼는 건드리지 않는다. vitest 를 돌려 교체·신규 2건만 실패하는 것을 확인한다(RED 확인은 커밋하지 않는다 — 코드 커밋은 Task 2 의 1건).

**GREEN — page.tsx 시각만 교체.** diff 는 import 블록 · 파일 헤더 주석 · `LoginForm` 의 return JSX 에만 생겨야 한다.
1. import: shadcn 카드 컴포넌트 4종 import 문을 통째로 지운다. `import { CircleAlert } from "lucide-react";` 를 추가한다. `Button` import 유지.
2. 헤더 주석: 「Card 중앙 정렬 레이아웃」 줄을 「sketch 006 A 「비움」(quick-260926-d76) — 카드 없음 · 앱 아이콘 + 워드마크 가운데 · 폰 하단 반전 CTA(safe-area 변수) / md 이상 워드마크 아래 360 폭」 으로 바꾼다. 옛 제목·설명 문자열은 주석에도 남기지 않는다. 나머지 주석 줄(D-07 · D-14 · D-15 · D-03 등)은 그대로.
3. 루트: `div` 유지(시맨틱 main 요소로 바꾸면 앱 셸 전역 규칙이 본문 하단 108 여백을 붙인다). 클래스 `flex min-h-dvh flex-col bg-[var(--bg)] pl-[var(--app-safe-left)] pr-[var(--app-safe-right)] text-[var(--fg)]`. 배경은 A안대로 페이지 바탕 `--bg`(종전 `--surface` 아님). `min-h-dvh` 는 명세의 min-h-screen 과 같은 풀블리드 의도지만, 폰 사파리에서 100vh 는 하단 고정 버튼을 툴바 뒤로 밀어 넣으므로 dvh 로 둔다(center-shell · app-shell 과 같은 선택). 좌우 safe 패딩은 AppShell 루트와 같은 규약(가로 모드 노치).
4. 가운데 영역 `div`: `flex flex-1 flex-col items-center justify-center gap-[22px] pt-[var(--app-safe-top)] md:gap-6`.
   - 앱 아이콘: `img` — `src="/icon.svg"` · `alt=""` · `aria-hidden="true"` · `width={76}` · `height={76}` · `draggable={false}` · `data-slot="login-mark"` · className `size-[76px] select-none rounded-[22%]`. 바로 위에 account-card.tsx 선례대로 `// eslint-disable-next-line @next/next/no-img-element` 와 한 줄 사유(SVG 는 next/image 최적화 대상이 아니며, 파비콘과 같은 앱 아이콘 A 원본 app/icon.svg 메타데이터 라우트를 그대로 재사용). 인라인 SVG 로 path 를 복제하지 않는다 — 마크 원본은 app/icon.svg 하나.
   - 워드마크: `h1` — `data-slot="login-wordmark"` · className `text-[26px] font-extrabold leading-none tracking-[-0.03em]` · 텍스트 「GH Trade」 하나(다른 글자 금지 — 테스트가 정확히 일치로 찾는다).
5. 하단 영역 `div` — `data-slot="login-foot"` · className `px-5 pb-[calc(44px+var(--app-safe-bottom))] md:mx-auto md:w-[360px] md:px-0 md:pb-[200px]`. safe-area 는 `--app-safe-bottom` 변수로만 읽는다(CSS 환경 함수 직접 호출 금지). 탭바 오프셋 변수도 쓰지 않는다 — `/login` 은 네이티브 탭바가 숨는 경로(D-12 ①)다.
   - 알림(`errorMessage` 있을 때만, 버튼 앞): 기존 `role="alert"` · `aria-live="polite"` 유지, className `mb-3 flex items-center gap-2 rounded-[var(--r)] bg-[var(--up-bg)] px-3.5 py-3 text-[13.5px] font-medium leading-[1.4] text-[var(--up)]`(종전 가운데 정렬 · destructive 틴트는 버린다 — `--up`/`--up-bg` 는 라이트·다크 모두 정의돼 대체 불필요). 내용 = `CircleAlert`(`aria-hidden="true"` · className `size-4 shrink-0`) + `{errorMessage}`. 명세의 rounded-xl 대신 `rounded-[var(--r)]`(=12px)인 이유는 interfaces 참조.
   - 버튼: 기존 `Button` 과 props(`onClick={handleGoogleLogin}` · `disabled={pending}` · `aria-busy={pending}` · `aria-label="Google로 로그인"` · `size="lg"`) 유지. className 을 다음으로 교체: `h-14 w-full gap-2.5 rounded-[var(--r-md)] px-5 text-[16px] font-bold tracking-[-0.01em] bg-[#191f28] text-white hover:bg-[#191f28] hover:brightness-125 dark:bg-white dark:text-[#191f28] dark:hover:bg-white dark:hover:brightness-[.94] active:scale-[.985] active:opacity-100 disabled:opacity-70 transition duration-150`. 근거: (a) default variant 의 color-mix hover 배경을 twMerge 로 지우려면 hover 배경을 같은 색으로 명시해야 하고 다크 hover 도 따로 둔다. (b) `text-[var(--t-base)]` 같은 var 임의값은 tailwind-merge 가 색 그룹으로 볼 수 있어 글자 크기를 `text-[16px]` 로 못박는다. (c) Tailwind v4 의 scale 유틸은 transform 이 아닌 개별 scale 속성이라, 기본 `transition`(scale · filter · opacity 포함)을 쓴다. 색은 sketch 결정의 hex 그대로(반전 CTA).
   - 버튼 내용: `pending` 이면 스피너 `span` 하나 — `data-slot="login-spinner"` · `aria-hidden="true"` · className `size-5 animate-spin rounded-full border-[2.5px] border-current border-r-transparent [animation-duration:800ms]`. 아니면 기존 Google G `svg`(path 4개 · `aria-hidden` 그대로, className 을 `size-5` 로 — 종전 `mr-2` 제거, 간격은 gap-2.5) + 텍스트 「Google로 로그인」.
6. 불변(diff 0): `ERROR_MESSAGES` 4종 · `safeNext` 가드 · `shownErrorKey` 우선순위 · `handleGoogleLogin` 전체(`isNativeApp` 분기 · `nativeGoogleSignIn(createClient())` · `console.error` 두 문구 · `classifyNativeLoginError` · `window.location.replace(safeNext)` · `signInWithOAuth` 옵션) · `Suspense` 래핑 · default export.
7. 저장하면 :3100 dev 서버가 HMR 로 반영한다 — 서버를 띄우거나 끄지 않는다. verify 의 curl 이 연결 실패(서버 부재)하면 서버를 띄우지 말고, vitest·소스 게이트만 통과시킨 뒤 SUMMARY 에 「SSR 확인 미실행(dev 서버 부재)」 을 적는다.

vitest 를 다시 돌려 10건 통과(GREEN)를 확인한다. 정리할 것이 있으면 로직 손대지 않는 범위에서만 REFACTOR.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp exec vitest --run src/app/login && F=webapp/src/app/login/page.tsx && CODE="$(grep -vE '^[[:space:]]*(//|\*|/\*|\{/\*)' "$F")" && ! printf '%s\n' "$CODE" | grep -qF 'components/ui/card' && ! printf '%s\n' "$CODE" | grep -qF 'GH Trade에 로그인' && ! printf '%s\n' "$CODE" | grep -qF '관심종목을 저장' && ! printf '%s\n' "$CODE" | grep -qF 'env(' && ! printf '%s\n' "$CODE" | grep -qF 'native-tabbar-offset' && ! printf '%s\n' "$CODE" | grep -qF '<main' && printf '%s\n' "$CODE" | grep -qF 'var(--app-safe-bottom)' && printf '%s\n' "$CODE" | grep -qF 'src="/icon.svg"' && HTML="$(curl -s --max-time 60 http://localhost:3100/login)" && printf '%s' "$HTML" | grep -qF 'data-slot="login-wordmark"' && ! printf '%s' "$HTML" | grep -qF '관심종목을 저장'</automated>
  </verify>
  <done>login vitest 10 passed(교체 1 · 신규 1 · 기존 8 무수정). page.tsx 코드(주석 제외)에 카드 import · 옛 제목 · 옛 설명 · CSS 환경 함수 직접 호출 · 탭바 오프셋 변수 · main 요소가 없고, `var(--app-safe-bottom)` 와 `src="/icon.svg"` 가 있다. :3100 SSR HTML 에 `data-slot="login-wordmark"` 가 있고 옛 설명 문구가 없다(서버 부재 시 그 사실을 SUMMARY 에 기록). 로직 영역 diff 0.</done>
</task>

<task type="auto">
  <name>Task 2: 통합 검증(타입 · lint · e2e · 계산 스타일/스크린샷) → 코드 커밋 1건</name>
  <files>webapp/src/app/login/page.tsx (점검에서 어긋남이 나올 때만 className 수정)</files>
  <action>
1. 정적 게이트: `pnpm --filter @gh-radar/webapp run typecheck` 와 `pnpm --filter @gh-radar/webapp exec eslint src/app/login` 을 돌린다(계획 시점 둘 다 통과). 실패가 두 파일 안이면 고친다.
2. e2e: `cd /Users/alex/repos/gh-radar/webapp && pnpm exec playwright test e2e/specs/auth-guards.spec.ts e2e/specs/brand-account.spec.ts` — `reuseExistingServer` 로 이미 도는 :3100 을 재사용한다(서버를 새로 띄우거나 죽이지 않는다). `setup` 프로젝트가 환경변수(E2E_TEST_PASSWORD 등)·시딩 문제로 실패하면: 로그인 화면 단언은 쿠키 없는 context 라 `pnpm exec playwright test e2e/specs/auth-guards.spec.ts --project=chromium --no-deps` 로 auth-guards 만 다시 돌리고, brand-account 는 「미실행 + 이유(에러 한 줄)」 로 SUMMARY 에 적는다. 돌지 않은 것을 통과했다고 절대 쓰지 않는다.
3. 계산 스타일 · 스크린샷 점검(커밋하지 않는 임시 CJS 스크립트를 실행자 세션 scratchpad 에 두고 `NODE_PATH=/Users/alex/repos/gh-radar/webapp/node_modules node <스크립트>` 로 실행 — `@playwright/test` 의 `chromium` 사용, 쿠키 없는 새 context, 테마는 `context.addInitScript` 로 localStorage `theme` 을 `light`/`dark` 로 선주입). 버튼은 `getByRole('button', { name: 'Google로 로그인' })`, 알림은 `getByRole('alert')`, 아이콘·워드마크는 `data-slot` 으로 잡고 `getBoundingClientRect` + `getComputedStyle` 로 단언한다:
   - 폰 390×844 라이트·다크 `/login`: 아이콘 76×76 · naturalWidth > 0(실제로 로드) · 워드마크 font-size 26px · font-weight 800 · 버튼 left 20 · width 350 · bottom 800 · height 56 · border-radius 16px · font-size 16px · font-weight 700 · 배경/글자 라이트 rgb(25, 31, 40)/rgb(255, 255, 255), 다크 rgb(255, 255, 255)/rgb(25, 31, 40).
   - 폰 + `document.documentElement.style.setProperty('--safe-area-inset-bottom', '34px')` 주입 후: 버튼 bottom 766(safe 변수 배선 증명).
   - 폰 `/login?error=auth_failed` 라이트·다크: 알림 bottom + 12 = 버튼 top · 알림 border-radius 12px · 라이트 bg rgb(255, 238, 238) · color rgb(240, 68, 82), 다크 bg rgba(240, 66, 81, 0.16) · color rgb(240, 66, 81) · 버튼 bottom 은 여전히 800.
   - 데스크톱 1280×800 라이트·다크: 버튼 width 360 · left 460 · bottom 600. `hover()` 후 배경색 불변 + computed filter 에 라이트 `brightness(1.25)` / 다크 `brightness(0.94)`.
   - 스크린샷 6장(폰 라·다 · 폰 오류 라·다 · 데스크톱 라·다)을 scratchpad 에 저장하고 Read 로 열어 줄바꿈 · 잘림 · 겹침을 눈으로 본다. 손보는 표면 안의 시각 결함은 묻지 말고 page.tsx 에서 바로 고친 뒤 1·3 을 다시 돌리고 SUMMARY 에 한 줄 남긴다.
   - 어긋나면 원인(대개 button.tsx variant 클래스와 twMerge · 캐스케이드 순서)을 page.tsx className 에서 고친다. button.tsx · globals.css 는 건드리지 않는다. 진행 중 스피너 모양은 브라우저에서 재현하지 않는다(클릭이 실제 OAuth 로 나간다) — vitest 신규 케이스가 구조를 증명한다.
4. 커밋: `git status -sb` 로 다른 세션 변경(`.planning/state.json` · `tasks/lessons.md` · `.planning/milestone.lock` · `.planning/sketches/…`)이 그대로 미스테이징인지 확인 → `git add webapp/src/app/login/page.tsx webapp/src/app/login/__tests__/page.test.tsx` → `git diff --cached --name-only` 가 정확히 그 두 줄인지 확인 → `git commit`. 제목 「feat(quick-260926-d76): 로그인 화면 sketch 006 A안 — 카드·문구 제거, 앱 아이콘+워드마크, 반전 Google CTA(safe-area)」, 본문 2~4줄 한글(카드·제목·설명 제거 / 폰 하단·md 360 배치 / 로그인 로직 불변 / 테스트 교체 1·추가 1). 공동 저자 트레일러 없음. push 하지 않는다.
5. SUMMARY 에 기록: vitest 결과 · typecheck/eslint · e2e(통과 수 또는 미실행 + 이유) · 계산 스타일 단언 표(기대 vs 실측) · 스크린샷 경로 · SSR 확인 결과 · 커밋 해시. 명세와 다르게 택한 두 가지(min-h-dvh · rounded-[var(--r)]/[var(--r-md)])와 이유를 한 줄씩.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --filter @gh-radar/webapp run typecheck && pnpm --filter @gh-radar/webapp exec eslint src/app/login && pnpm --filter @gh-radar/webapp exec vitest --run src/app/login && NAMES="$(git show --name-only --format= HEAD)" && test "$(printf '%s\n' "$NAMES" | LC_ALL=C sort | tr '\n' ' ')" = "webapp/src/app/login/__tests__/page.test.tsx webapp/src/app/login/page.tsx " && SUBJ="$(git log -1 --format=%s)" && printf '%s\n' "$SUBJ" | grep -q '^feat(quick-260926-d76): ' && BODY="$(git log -1 --format=%B)" && ! printf '%s\n' "$BODY" | grep -qi 'co-authored-by' && STAGED="$(git diff --cached --name-only)" && test -z "$STAGED"</automated>
  </verify>
  <done>typecheck · eslint · login vitest 통과. 계산 스타일 단언(폰 라·다 · safe 34 · 오류 라·다 · 데스크톱 라·다 · hover)이 전부 기대값이고 스크린샷에 줄바꿈·잘림·겹침이 없다. e2e 는 통과 수 또는 「미실행 + 이유」가 SUMMARY 에 사실대로 있다. HEAD 가 정확히 2개 경로만 담은 `feat(quick-260926-d76): …` 한글 커밋이고 공동 저자 트레일러가 없으며, 다른 세션 파일은 스테이징되지 않았고 push 하지 않았다. :3100 dev 서버는 계속 떠 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query → /login | `?error=` · `?next=` 는 누구나 조작할 수 있는 값 — 이번 변경은 이 값의 처리(맵 조회 · safeNext 가드)를 바꾸지 않고 표시 위치·모양만 바꾼다 |
| /login → Google OAuth · 네이티브 SocialLogin | 로그인 트리거 경로 — 로직 diff 0, 버튼 속성(disabled · aria-busy · aria-label) 유지 |
| 브라우저 → /icon.svg | 동일 출처 정적 메타데이터 라우트 — 인증 불필요한 공개 자산 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-d76-01 | Tampering | `?next=` → `safeNext` → `location.replace` / OAuth redirectTo | high | mitigate | safeNext 가드(`/` 시작 · `//` 차단) 코드 diff 0 — 기존 vitest 「앱 + ?next=//evil.com → location.replace("/")」 무수정 통과 + e2e auth-guards 「/login?next=//attacker.com 은 safeNext 로 치환」 |
| T-d76-02 | Information Disclosure | `?error=` 알림 | low | mitigate | 알림은 `ERROR_MESSAGES` 맵 값만 렌더하고 알 수 없는 키는 unknown 폴백 — 원시 쿼리 값을 화면에 넣지 않는다(맵·우선순위 diff 0, 오류 케이스 vitest 4건 무수정) |
| T-d76-03 | Denial of Service | 진행 중 재탭(계정 선택 시트 중복 · nonce 엇갈림) | medium | mitigate | `disabled={pending}` · `aria-busy` · 조기 return 유지, 스피너 교체 후에도 접근 이름 유지 — vitest 기존 진행 중 케이스 + 신규 스피너 케이스 |
| T-d76-04 | Spoofing | 앱 아이콘 img `/icon.svg` | low | accept | 동일 출처 정적 파일(app/icon.svg)이고 외부 URL·사용자 입력이 섞이지 않는다. `<img>` 로 SVG 를 그리면 스크립트가 실행되지 않는다 |
</threat_model>

<verification>
전체(실행자 마지막 단계):
- `pnpm --filter @gh-radar/webapp exec vitest --run src/app/login` — 10 passed
- `pnpm --filter @gh-radar/webapp run typecheck` · `pnpm --filter @gh-radar/webapp exec eslint src/app/login` — 통과
- `cd webapp && pnpm exec playwright test e2e/specs/auth-guards.spec.ts e2e/specs/brand-account.spec.ts` — 통과 또는 「미실행 + 이유」(대체 실행 `--project=chromium --no-deps` 결과 포함)
- 계산 스타일 점검 — 폰 390 라·다 / safe 34 / 오류 라·다 / 데스크톱 1280 라·다 / hover 단언 전부 기대값, 스크린샷 6장 육안 확인
- `git show --stat HEAD` — 2개 경로 · 한글 · 공동 저자 없음 · push 없음 · 다른 세션 파일 미스테이징
</verification>

<success_criteria>
- 사용자가 `/login` 에서 카드·문구 없이 앱 아이콘 + 「GH Trade」 + Google 버튼 하나만 본다 — 폰은 하단(safe-area 위), 데스크톱은 워드마크 아래 360 폭.
- 버튼이 라이트/다크에서 반전 CTA 색으로 보이고, 오류 알림과 진행 중 스피너가 sketch 006 A 대로 나온다.
- 로그인 동작(브라우저 OAuth · 앱 네이티브 로그인 · 오픈 리다이렉트 방어 · 오류 문구)은 전과 똑같다 — 기존 테스트 8건 무수정 통과.
- 코드 커밋 1건(2 경로)만 남고, dev 서버와 다른 세션의 작업은 건드리지 않았다.
</success_criteria>

<output>
Create `.planning/quick/260926-d76-sketch-006-a-card-google-cta-safe-area/260926-d76-SUMMARY.md` when done
</output>
