---
phase: quick-260926-d76
plan: 01
status: complete
subsystem: webapp/auth-ui
tags: [login, sketch-006, safe-area, mobile, toss]
requires: [webapp/src/app/icon.svg, globals.css §21 --app-safe-*]
provides: [sketch 006 A「비움」 /login 화면]
affects: [webapp /login, 앱(Remote-URL 셸) 로그인 화면]
tech-stack:
  added: []
  patterns: [Button className 으로 default variant twMerge 덮기, safe-area 는 --app-safe-* 변수만]
key-files:
  created: []
  modified:
    - webapp/src/app/login/page.tsx
    - webapp/src/app/login/__tests__/page.test.tsx
decisions:
  - "루트 min-h-dvh (명세 min-h-screen 대신) — 폰 사파리 100vh 는 하단 고정 버튼을 툴바 뒤로 민다(center-shell·app-shell 과 같은 선택)"
  - "반경은 rounded-[var(--r)]=12 / rounded-[var(--r-md)]=16 (명세 rounded-xl/2xl 대신) — 이 프로젝트의 rounded-xl/2xl 은 --radius 배수라 12/16 을 보장하지 않는다"
metrics:
  duration: "~12분"
  completed: 2026-09-26
requirements: [D76-LAYOUT, D76-CTA, D76-STATE, D76-LOGIC, D76-VERIFY]
plan_head_before: 1cebbd2d58d4df0c2c07fff78337befa79010443
actuals:
  tokens: 9000
  tasks: 2
  commits: 1
---

# Quick 260926-d76 Plan 01: 로그인 화면 sketch 006 A「비움」 Summary

카드·제목·설명을 걷어낸 `/login` — 앱 아이콘 A(76 · radius 22%, `/icon.svg` 재사용) + 워드마크 h1「GH Trade」 가운데, 반전 Google CTA(56 · radius 16 · 16/700)를 폰 하단(`calc(44px + var(--app-safe-bottom))`) / md 이상 워드마크 아래 360 폭에 배치. 오류는 버튼 위 `--up-bg`/`--up` 12px 알림, 진행 중은 20px 스피너 + 70%. 로그인 로직 diff 0.

## 커밋

| Task | 커밋 | 내용 |
|------|------|------|
| 1+2 | `eda719a` | feat(quick-260926-d76): 로그인 화면 sketch 006 A안 — 카드·문구 제거, 앱 아이콘+워드마크, 반전 Google CTA(safe-area) — 정확히 2개 경로, 한글, 공동 저자 트레일러 없음, push 없음 |

(계획대로 Task 1 RED/GREEN 은 커밋하지 않고 Task 2 에서 코드 커밋 1건으로 묶음.)

## 검증 결과 (실제 관측값)

| 게이트 | 명령 | 결과 |
|--------|------|------|
| RED | `pnpm --filter @gh-radar/webapp exec vitest --run src/app/login` (구현 전) | 2 failed / 8 passed — 교체·신규 2건만 실패 (의도) |
| GREEN | 같은 명령 (구현 후, 커밋 후 재실행) | **10 passed (10)** |
| typecheck | `pnpm --filter @gh-radar/webapp run typecheck` | exit 0 (`tsc --noEmit && tsc -p tsconfig.e2e.json`) |
| eslint | `pnpm --filter @gh-radar/webapp exec eslint src/app/login` | exit 0, 출력 없음 |
| 소스 게이트 | Task 1 verify 의 grep 체인 | 통과 (카드 import · 옛 제목/설명 · `env(` · 탭바 오프셋 · `<main` 없음, `var(--app-safe-bottom)` · `src="/icon.svg"` 있음) |
| SSR | `curl -s http://localhost:3100/login` | `data-slot="login-wordmark"` 1건, 「관심종목을 저장」 0건. `/icon.svg` → 200 image/svg+xml |
| e2e | `cd webapp && pnpm exec playwright test e2e/specs/auth-guards.spec.ts e2e/specs/brand-account.spec.ts` | **16 passed** (setup 1 + auth-guards 12 + brand-account 3). 기존 :3100 서버 재사용, `--no-deps` 대체 실행 불필요 |
| 계산 스타일 | scratchpad `check.cjs` (Playwright chromium, 쿠키 없는 context, localStorage `theme` 선주입) | **66/66 OK, FAIL=0** |
| Task 2 verify | 플랜의 automated 체인 전체 | `TASK2_VERIFY_OK` |

:3100 dev 서버(PID 97397)는 끄거나 재시작하지 않았고 검증 후에도 살아 있음.

### 계산 스타일 단언 (기대 vs 실측 — 라이트·다크 모두 동일하게 일치)

| 상황 | 항목 | 기대 | 실측 |
|------|------|------|------|
| 폰 390×844 | 아이콘 | 76×76 · radius 22% · naturalWidth>0 | 76×76 · 22% · 로드됨 |
| 폰 | 워드마크 | 26px / 800 / −0.78px(−0.03em) | 26px / 800 / −0.78px |
| 폰 | 아이콘↔워드마크 gap | 22 | 22 |
| 폰 | 버튼 left · width · bottom · height | 20 · 350 · 800 · 56 | 20 · 350 · 800 · 56 |
| 폰 | 버튼 radius · font | 16px · 16px/700 | 16px · 16px/700 |
| 폰 라이트 | 버튼 bg / color | rgb(25, 31, 40) / rgb(255, 255, 255) | 일치 |
| 폰 다크 | 버튼 bg / color | rgb(255, 255, 255) / rgb(25, 31, 40) | 일치 |
| 폰 + `--safe-area-inset-bottom: 34px` | 버튼 bottom | 766 | 766 |
| 폰 `?error=auth_failed` | 알림 bottom+12 = 버튼 top | 744 | 744 |
| 폰 오류 | 알림 radius · font · 버튼 bottom | 12px · 13.5px/500 · 800 | 일치 (알림 높이 42.89, 한 줄) |
| 폰 오류 라이트 | 알림 bg / color | rgb(255, 238, 238) / rgb(240, 68, 82) | 일치 |
| 폰 오류 다크 | 알림 bg / color | rgba(240, 66, 81, 0.16) / rgb(240, 66, 81) | 일치 |
| 데스크톱 1280×800 | 버튼 width · left · bottom | 360 · 460 · 600 | 360 · 460 · 600 |
| 데스크톱 | 아이콘↔워드마크 gap (md) | 24 | 24 |
| 데스크톱 hover 라이트 | 배경 불변 · filter | rgb(25, 31, 40) · brightness(1.25) | 일치 |
| 데스크톱 hover 다크 | 배경 불변 · filter | rgb(255, 255, 255) · brightness(0.94) | 일치 |

진행 중 스피너는 브라우저에서 재현하지 않음(클릭이 실제 OAuth 로 나감) — vitest 신규 케이스가 구조(스피너 1개 · svg 0 · 라벨 텍스트 없음 · 접근 이름 유지)를 증명.

### 스크린샷 (육안 확인 — 줄바꿈 · 잘림 · 겹침 없음)

scratchpad `/private/tmp/claude-501/-Users-alex-repos-gh-radar/ecb44b57-f213-40b8-9fb3-fcaab94fd11d/scratchpad/` 의 `phone-light.png` · `phone-dark.png` · `phone-error-light.png` · `phone-error-dark.png` · `desktop-light.png` · `desktop-dark.png`.

- 좌하단 「I」 원형 버튼은 Next dev 인디케이터(dev 전용 오버레이)로 운영 빌드에는 없다.
- 관찰(결함 아님, 수정 안 함): 다크에서 앱 아이콘의 `#17171c` 사각 바탕이 페이지 `--bg`(#17171c)와 같아 squircle 윤곽이 녹고 레이더 선만 보인다. 채택 스케치 006 도 같은 심볼·같은 다크 바탕으로 테두리 없이 그렸으므로 스케치 그대로의 결과다. 윤곽을 원하면 별도 취향 결정(예: 다크에서 1px 링).

## Deviations from Plan

없음 — 플랜대로 실행. 참고 사항 2가지:
- 계산 스타일 스크립트에서 `getByRole('alert')` 가 Next route announcer(`#__next-route-announcer__`, role=alert)까지 잡아 strict 위반 → 스크립트 셀렉터를 `[role="alert"][aria-live="polite"]` 로 좁힘(검증 도구 수정, 제품 코드 무관).
- 명세와 다르게 택한 두 가지(플랜 지시대로): `min-h-dvh`(폰 사파리 100vh 가 하단 버튼을 툴바 뒤로 밀기 때문) · `rounded-[var(--r)]`/`rounded-[var(--r-md)]`(이 프로젝트 rounded-xl/2xl 은 --radius 배수라 12/16 비보장).

## 범위 준수

- 수정 파일은 `webapp/src/app/login/page.tsx` · `webapp/src/app/login/__tests__/page.test.tsx` 2개뿐. mobile/ · middleware · auth callback · button.tsx · globals.css 무수정.
- 작업 중 working tree 에 다른 세션의 `mobile/ios/App/App.xcodeproj/project.pbxproj` 변경이 새로 나타났으나 스테이징하지 않음. `.planning/state.json` · `tasks/lessons.md` · `.planning/milestone.lock` · `.planning/sketches/**` 도 미스테이징.
- 로직 영역(ERROR_MESSAGES · safeNext · shownErrorKey · handleGoogleLogin · Suspense · default export) diff 0 — 기존 vitest 8건 무수정 통과, e2e open-redirect 2건 통과.

## Known Stubs

없음.

## Self-Check: PASSED

- FOUND: webapp/src/app/login/page.tsx (`data-slot="login-wordmark"` 포함)
- FOUND: webapp/src/app/login/__tests__/page.test.tsx (`login-spinner` 포함)
- FOUND: commit eda719a (`git rev-list --count 1cebbd2..HEAD` = 1)
