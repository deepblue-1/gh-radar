---
phase: quick-260608-g0k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - DOC-LEGAL-FIX
  - DOC-OPS-PRINCIPLES

must_haves:
  truths:
    - "CLAUDE.md 안에서 2021도1533 판결을 '형사 책임 성립' 근거로 인용한 부분이 사라진다 (정확한 사실관계로 교체)"
    - "2021도1533 (형사 무죄) 와 2017다224395 (민사 DB권 침해 인정) 가 명확히 구분되어 기술된다"
    - "한국 크롤링 운영 5원칙 (배치 캡 / 24h 캐싱+해시 / on-demand 금지 / 429·403 backoff / 출처+부분 캐싱) 이 CLAUDE.md 에 적힌다"
    - "정정·추가는 모두 'Naver 종목토론방 Scraping Risk' 섹션 또는 그 주변 (GSD:stack 마커 내부) 에 둔다"
    - "다른 문단·코드·구조는 변경되지 않는다 (영향 범위 = 법적 진술 1줄 + 비어있던 Risk 섹션 본문 추가)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "정정된 한국 크롤링 법적 사실관계 + 운영 5원칙"
      contains: "2017다224395"
  key_links:
    - from: "CLAUDE.md Discussion board bullet"
      to: "CLAUDE.md '### Naver 종목토론방 Scraping Risk' 섹션"
      via: "동일 법적 주제, 같은 파일 내부 cross-reference (사실관계 → 운영 원칙)"
      pattern: "2017다224395.*?민사"
---

<objective>
CLAUDE.md 의 한국 크롤링 법적 진술을 사실관계에 맞게 정정하고, 한국 크롤링 일반에 적용할 운영 5원칙을 명문화한다.

Purpose:
- 현재 CLAUDE.md 의 "대법원 2021도1533 = 형사 책임 성립" 진술은 사실과 정반대 (해당 판결은 무죄 확정). 향후 본 문서를 컨텍스트로 받는 Claude 가 잘못된 전제로 의사결정 (e.g. "크롤링을 더 줄여야 한다" 가 아닌 "다른 법적 근거로 줄여야 한다") 하지 않도록 한다.
- 진짜 리스크는 형사가 아닌 민사 DB권 침해 (2017다224395 잡코리아 v. 사람인) 이며, 이 리스크를 실효적으로 낮추는 5가지 운영 패턴을 코드 변경 가능한 형태로 박아둔다 (배치 캡 / 캐싱 / on-demand 금지 / backoff / 부분 캐싱).

Output:
- CLAUDE.md 1개 파일 수정 (코드 변경 없음)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# 정정 대상 — CLAUDE.md line 69 (Discussion board 섹션 안의 한 줄)
# 잘못된 원문:
#   - 대법원 2022. 5. 12. 선고 2021도1533 판결 established that violating terms of service during scraping can constitute criminal liability; keep scraping minimal and for personal/informational use
#
# 정확한 사실관계:
#   - 대법원 2021도1533 (여기어때 v. 야놀자, 형사, 2022.5.12 확정): 정보통신망법 / 컴퓨터등장애업무방해 / 저작권법 3개 쟁점 모두 무죄.
#     판시: API 에 명시적 접근 보호조치 (인증 등) 가 없고, 복제가 DB 전체의 '상당한 부분' 이 아니면 형사 책임을 면한다.
#   - 대법원 2017다224395 (사람인 v. 잡코리아, 민사): DB제작자 권리 침해 인정 → 거액 손배 명령.
#   → 즉 형사 리스크는 낮으나 **민사 DB권 침해 리스크가 실존**. 보수적 운영의 근거는 '형사 처벌 회피' 가 아닌 '민사 DB권 침해 회피' 이다.

# 추가할 운영 5원칙 (한국 크롤링 일반에 적용, '### Naver 종목토론방 Scraping Risk' 빈 섹션에 본문으로 삽입):
#   1. 일 1~2회 배치 캡 (장중 폴링 금지)
#   2. 24h 캐싱 + 콘텐츠 해시 변경 감지 시에만 갱신
#   3. 사용자 클릭 시 on-demand fetch 금지 (서버측 배치만)
#   4. HTTP 429/403 감지 시 즉시 24h backoff
#   5. 출처 표기 + 부분 캐싱 (전체 DB 덤프 보관 금지)

# 구조적 제약:
#   - CLAUDE.md 는 GSD 가 관리하는 마커 구조 (`<!-- GSD:stack-start source:research/STACK.md -->` ... `<!-- GSD:stack-end -->`).
#   - 정정 대상 (line 69) 과 빈 섹션 (line 118) 모두 GSD:stack 마커 내부 → 후속 sync 시 덮어쓰여질 가능성.
#   - 사용자 요구는 즉시 적용이므로 일단 CLAUDE.md 에 직접 반영. 마커 구조 자체는 건드리지 않음 (재생성 원본 추적은 별도 이슈).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discussion board bullet 의 잘못된 형사 책임 진술 → 사실관계 정정 (민사 DB권 분리 명시)</name>
  <files>CLAUDE.md</files>
  <action>
CLAUDE.md line 69 (Discussion board bullet 중 "대법원 2022. 5. 12. 선고 2021도1533 판결 established that violating terms of service during scraping can constitute criminal liability; keep scraping minimal and for personal/informational use" 1줄) 을 Edit 도구로 아래 2줄로 교체.

교체 후 텍스트 (영문 원문 보존 + 한글 부연 — 원 라인 톤과 동일하게 bullet 한 줄 + 보조 한 줄):
```
- Korean Supreme Court 2022. 5. 12. 선고 2021도1533 (여기어때 v. 야놀자, criminal) acquitted scraping under 정보통신망법, 컴퓨터등장애업무방해, 저작권법 — criminal liability from ToS violation alone is **not** established when no explicit access protection (auth) is bypassed and copying is not of a "substantial part" of the DB.
- However, 대법원 2017다224395 (잡코리아 v. 사람인, civil) **did** uphold DB-producer rights infringement and ordered substantial damages — the real, live risk for Naver scraping is **civil**, not criminal. Keep scraping minimal, cached, attributed, and avoid full-DB equivalence to stay outside the civil DB-right zone.
```

주의:
- 한 줄을 두 줄로 늘리면서 인덴트 (`- `) 동일하게 유지.
- 영문 원문 톤 보존 (해당 섹션 전체가 영문 + 한자/한글 인용 혼용 스타일).
- 주변 bullet (그 위 robots.txt 줄, 그 아래 Architecture decision 줄) 은 손대지 않음.
- "criminal liability" 라는 단어는 의도적으로 부정문으로 유지 (검색 가능성, 향후 LLM 컨텍스트에서 토큰 일치 회피용).
  </action>
  <verify>
    <automated>grep -n "2017다224395" CLAUDE.md &amp;&amp; grep -n "2021도1533" CLAUDE.md &amp;&amp; ! grep -n "can constitute criminal liability" CLAUDE.md</automated>
  </verify>
  <done>
- CLAUDE.md 에 "2017다224395" 가 정확히 1회 이상 등장한다.
- CLAUDE.md 에 "2021도1533" 은 여전히 등장하나, 그 문맥이 "acquitted" / "is not established" 로 무죄 사실을 명시한다.
- CLAUDE.md 안에 "can constitute criminal liability" 문자열이 더 이상 존재하지 않는다.
- diff 는 line 69 부근 +2 / -1 정도, 다른 라인 변동 없음.
  </done>
</task>

<task type="auto">
  <name>Task 2: 'Naver 종목토론방 Scraping Risk' 빈 섹션에 한국 크롤링 운영 5원칙 본문 추가</name>
  <files>CLAUDE.md</files>
  <action>
CLAUDE.md line 118 의 `### Naver 종목토론방 Scraping Risk` heading 은 현재 본문이 비어있고 바로 `### Naver Search API Rate Limit` 가 이어진다. Edit 도구로 해당 heading 바로 아래에 아래 본문을 삽입.

삽입할 본문 (heading 은 기존 그대로 두고, heading 다음 줄에 빈 줄 1개 + 본문 추가):

```
**Legal posture (정정 요약).** 본 프로젝트의 보수적 운영 근거는 형사 처벌 회피가 아니라 **민사 DB제작자 권리 침해 (대법원 2017다224395) 회피** 이다. 대법원 2021도1533 (형사) 은 무죄 확정이며 ToS 위반만으로 형사 책임은 성립하지 않는다. 단, 민사 DB권 침해는 "전체의 상당한 부분" 복제 / 반복적 체계적 수집 / 원본 서비스의 시장가치 잠식 시 인정될 수 있다.

**Operational 5 rules — 모든 한국 크롤링 (Naver 종목토론방 포함) 에 적용.**

1. **일 1~2회 배치 캡.** 장중 실시간 폴링 금지. 배치 잡 (Cloud Run Job + Scheduler) 만 허용. 사용자 트래픽에 비례하는 호출량은 "체계적 수집" 으로 해석될 위험이 있으므로 사용자 수와 분리된 고정 배치만 사용.

2. **24h 캐싱 + 콘텐츠 해시 변경 감지 시에만 갱신.** Supabase 에 fetch 결과를 저장하고 24시간 TTL. 재방문 시 원본 페이지의 콘텐츠 해시 (또는 last-modified, etag) 가 바뀐 경우에만 갱신 호출. 동일 콘텐츠 재크롤링은 비용 / 법적 양면에서 순손실.

3. **사용자 클릭 시 on-demand fetch 금지.** 종목 상세 페이지 진입이 백엔드 원본 fetch 를 트리거하면 안 됨. 서버측 배치가 미리 채워둔 캐시만 읽음. 사용자 수가 N 일 때 호출량이 O(N) 이 되는 패턴 전면 금지. (= "서버측 배치만, on-demand 금지" 원칙)

4. **HTTP 429 / 403 감지 시 즉시 24h backoff.** 429 (rate-limit) 또는 403 (access denied) 응답이 한 번이라도 관측되면 해당 source 전체에 대해 24시간 동안 새 호출을 차단하고 알림을 띄움. 자동 재시도 / 지수 backoff 으로 두드리지 않음 — 차단 신호는 명시 차단으로 해석.

5. **출처 표기 + 부분 캐싱 (전체 DB 덤프 보관 금지).** 캐시 / 요약 / 표시에 원본 URL + 출처명을 항상 함께 노출. DB 에는 표시·요약에 필요한 최소 필드만 저장 (전체 본문·전체 게시판 덤프 불가). 핵심 가치는 "AI 요약 + 컨텍스트" 이지 "원본 데이터의 재배포" 가 아님 — 민사 DB권 침해의 핵심 요건인 "상당한 부분 복제" 를 구조적으로 회피.

> 이 5원칙은 Naver 종목토론방뿐 아니라 Naver Search API · 향후 추가될 다른 한국 데이터 소스에도 동일 적용. 새로운 source 추가 시 본 5원칙을 만족하는 운영 설계를 먼저 점검할 것.
```

주의:
- heading `### Naver 종목토론방 Scraping Risk` 는 그대로 유지, 그 다음 줄부터 위 본문 삽입.
- 다음 heading `### Naver Search API Rate Limit` 사이에 빈 줄 1개 유지.
- 마크다운 들여쓰기 / 글머리 기호 (1. 2. ...) 깨지지 않도록 유의.
- 본문은 한글 (사용자와의 합의 사항) + 법령·판결 인용은 원문 한자/숫자 보존.
  </action>
  <verify>
    <automated>grep -n "Operational 5 rules" CLAUDE.md &amp;&amp; grep -c "^[0-9]\. \*\*" CLAUDE.md | awk '$1+0 >= 5 {exit 0} {exit 1}' &amp;&amp; grep -n "Legal posture" CLAUDE.md &amp;&amp; grep -n "전체 DB 덤프 보관 금지" CLAUDE.md</automated>
  </verify>
  <done>
- CLAUDE.md 의 `### Naver 종목토론방 Scraping Risk` 다음 줄부터 "Legal posture" 단락이 시작된다.
- 같은 섹션 안에 "Operational 5 rules" 라는 단어와 함께 1~5번 글머리 (`1. **...**`, `2. **...**`, ...) 가 모두 존재한다.
- "전체 DB 덤프 보관 금지", "24h backoff", "on-demand fetch 금지", "일 1~2회 배치 캡", "콘텐츠 해시" 5개 키워드가 모두 검색된다.
- diff 는 line 118 부근에 본문 추가만 있고, 다른 heading / 다른 섹션 본문은 변동 없음.
- 마크다운 렌더링 시 5개 번호 글머리가 동일 리스트로 묶인다 (글머리 사이 빈 줄 2개 이상 들어가지 않음 — 1줄 빈줄로 단락 구분).
  </done>
</task>

</tasks>

<verification>
전체 plan 종료 시점에 한 번에 확인:

```bash
# 1. 잘못된 진술이 사라졌는가
! grep -n "can constitute criminal liability" CLAUDE.md

# 2. 정정된 사실관계가 추가되었는가
grep -n "2017다224395" CLAUDE.md
grep -n "acquitted" CLAUDE.md

# 3. 5원칙이 모두 들어갔는가
grep -n "Operational 5 rules" CLAUDE.md
grep -n "일 1~2회 배치 캡" CLAUDE.md
grep -n "콘텐츠 해시" CLAUDE.md
grep -n "on-demand fetch 금지" CLAUDE.md
grep -n "24h backoff" CLAUDE.md
grep -n "전체 DB 덤프 보관 금지" CLAUDE.md

# 4. 영향 범위 — CLAUDE.md 만 변경, 코드 변경 없음
git diff --stat
# 기대: CLAUDE.md | <small N> +-, 다른 파일 0
```

수동 확인 1건:
- CLAUDE.md 의 GSD 마커 (`<!-- GSD:stack-start -->` / `<!-- GSD:stack-end -->`) 두 줄이 그대로 보존되는지 육안 점검.
</verification>

<success_criteria>
- [ ] CLAUDE.md 에 "2021도1533 → 형사 책임 성립" 진술이 더 이상 없다.
- [ ] CLAUDE.md 에 2021도1533 (형사 무죄) 와 2017다224395 (민사 DB권 침해 인정) 가 구분되어 명시된다.
- [ ] CLAUDE.md 의 `### Naver 종목토론방 Scraping Risk` 섹션에 5원칙 본문이 존재한다 (배치 캡 / 캐싱·해시 / on-demand 금지 / 429·403 backoff / 부분 캐싱).
- [ ] 5원칙은 한국 크롤링 일반에 적용된다는 일반화 문장이 포함된다.
- [ ] diff 는 CLAUDE.md 만 변경하고 코드 / 다른 문서는 0 변경.
- [ ] GSD 마커 구조 (`<!-- GSD:stack-start ... -->` ... `<!-- GSD:stack-end -->`) 가 보존된다.
</success_criteria>

<output>
After completion:
- Commit message (한글, Co-Authored-By 절대 금지):
  ```
  docs(CLAUDE.md): 한국 크롤링 법적 진술 정정 + 운영 5원칙 추가

  - 2021도1533 (형사 무죄) 와 2017다224395 (민사 DB권 침해) 분리 명시
  - 진짜 리스크는 형사 처벌이 아닌 민사 DB제작자 권리 침해
  - 한국 크롤링 운영 5원칙: 배치 캡 / 24h 캐싱+해시 / on-demand 금지 /
    429·403 즉시 backoff / 출처+부분 캐싱
  ```
- 커밋 전에 커밋 메시지 사용자 확인 → 확인 후 커밋 + push.
- Summary 파일: `.planning/quick/260608-g0k-claude-md-5/260608-g0k-SUMMARY.md` (quick task 관례 따라 생성).
</output>
