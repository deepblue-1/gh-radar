---
phase: quick-260915-il4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/news-sync/src/naver/collectStockNews.ts
  - workers/news-sync/src/index.ts
  - workers/news-sync/src/apiUsage.ts
  - workers/news-sync/tests/cycle.test.ts
  - workers/news-sync/tests/collectStockNews.test.ts
  - workers/news-sync/tests/apiUsage.test.ts
  - CLAUDE.md
  - .planning/research/STACK.md
autonomous: true
requirements: [IL4-A, IL4-B, IL4-C, IL4-D, IL4-E]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "같은 KST 날짜에 한도 소진 strike 가 2 이상 쌓여 있어도 news-sync run 은 skip 되지 않고 대상을 로드해 네이버를 호출한다."
    - "오늘 strike 가 1 이상이면 run 은 선택된 첫 대상 1종목을 다른 종목보다 먼저, 단독으로(동시성 없이), 429 재시도 없이 수집한다. 그 탐침이 429(소진 본문이든 일반 rate-limit 본문이든)를 받으면 네이버 호출은 그 1건뿐이고 run 은 즉시 중단되며 strike 는 run 당 1회만 기록된다."
    - "탐침이 성공하거나 429 가 아닌 실패(400 · 5xx · 네트워크)로 끝나면 info 로그 'quota probe passed — resuming' 을 남기고 나머지 대상을 평소 동시성과 429 backoff 재시도로 수집한다."
    - "오늘 strike 가 0 이면 탐침 경로를 타지 않으며 기존 동작(동시성 수집 · 429 backoff 재시도 · 연속 5종목 429 휴리스틱 · 예산 가드 · 401 stopAll)이 그대로다."
    - "CLAUDE.md 와 .planning/research/STACK.md 의 '공식 API 운영 기준' 4항이 한 글자도 다르지 않은 같은 문장으로, 판정 run 중단 + 이후 run 단독 탐침 1건 재확인 + 통과 시 자동 재개를 적는다."
    - "배포된 gh-radar-news-sync 잡 이미지 태그가 이번 작업의 커밋 SHA 이고, 배포 후 예약 실행 2건 이상이 succeeded 이며, news-sync 스케줄러는 h3p 의 4개(market · market-close · offhours · weekend) 그대로다."
  artifacts:
    - path: "workers/news-sync/src/index.ts"
      provides: "당일 skip 제거 · strike≥1 시 첫 대상 단독 탐침 · 탐침 429 즉시 중단 · strikesToday/quotaProbe 로그 필드"
      contains: "quota probe passed — resuming"
    - path: "workers/news-sync/src/naver/collectStockNews.ts"
      provides: "CollectOpts.retryOnRateLimit (기본 true, false 면 429 단일 시도)"
      contains: "retryOnRateLimit"
    - path: "workers/news-sync/tests/cycle.test.ts"
      provides: "runNewsSyncCycle 을 모듈 mock 으로 끝까지 돌리는 탐침 경로 테스트"
      contains: "runNewsSyncCycle"
    - path: "CLAUDE.md"
      provides: "공식 API 운영 기준 4항 — 단독 탐침 재개"
      contains: "단독 탐침 호출 1건"
    - path: ".planning/research/STACK.md"
      provides: "CLAUDE.md 와 동일한 4항"
      contains: "단독 탐침 호출 1건"
  key_links:
    - from: "workers/news-sync/src/index.ts"
      to: "workers/news-sync/src/naver/collectStockNews.ts"
      via: "processTarget(첫 대상, probe=true) → collectStockNews(opts.retryOnRateLimit=false) → fetchPageWithRateLimitBackoff 가 빈 지연 배열로 1회만 시도"
      pattern: "retryOnRateLimit"
    - from: "workers/news-sync/src/index.ts"
      to: "workers/news-sync/src/apiUsage.ts"
      via: "run 시작 readQuotaStrikes → strikesToday≥1 이면 탐침 · 탐침 429 → strikeOnce(\"probe\"|\"explicit\") → recordQuotaStrike 1회"
      pattern: "strikeOnce\\(\"probe\"\\)"
    - from: "workers/news-sync/tests/cycle.test.ts"
      to: "workers/news-sync/src/index.ts"
      via: "config · logger · supabase · naver client · targets · lastSeen · upsert · retention · apiUsage 를 vi.mock 하고 runNewsSyncCycle 을 직접 호출"
      pattern: "runNewsSyncCycle\\("
---

<objective>
news-sync 에서 네이버 일일 한도 소진 판정(strike)이 같은 KST 날짜에 2회 쌓이면 자정까지 수집을 통째로 끄던 동작(quick-260915-h3p, 커밋 deebf82)을 제거한다. 대신 판정한 run 만 즉시 중단하고, 이후 run 은 첫 대상 1종목을 재시도 없이 단독으로 먼저 수집하는 탐침으로 한도 회복을 확인한 뒤 통과하면 자동 재개한다.

Purpose: 두 판정 경로(429 본문 [ASSUMED] 코드 매핑, 연속 5종목 429 휴리스틱)는 둘 다 오판할 수 있다(초당 버스트 · 네이버 장애 · 코드 매핑 오류). 오판 2회가 하루 뉴스 수집 전체와 홈 주도 테마 품질을 떨어뜨리는 반면, 실제 소진은 설계상 거의 불가능하다(설계 ≈15.5K/일 < 워커 예산 18,750 < 공식 25,000). 탐침 방식은 실제 소진이 계속될 때 run 당 거절 호출 1건(3분에 1건)만 쓰고, 오판이면 다음 run 에 바로 복구한다.

Output: index.ts 탐침 흐름 + collectStockNews 재시도 끄기 옵션 + runNewsSyncCycle 사이클 테스트 + CLAUDE.md/STACK.md 4항 갱신 + news-sync 라이브 재배포와 예약 실행 관측.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/quick/260915-h3p-news-sync-tiered-cadence/260915-h3p-SUMMARY.md
@workers/news-sync/src/index.ts
@workers/news-sync/src/naver/collectStockNews.ts
@workers/news-sync/src/apiUsage.ts
@workers/news-sync/tests/collectStockNews.test.ts
@workers/news-sync/tests/apiUsage.test.ts

## 계획 시점 라이브 관측 (2026-09-15 13:26 KST, 범위 근거)
- HEAD `f5ac8ec`, 작업 트리 clean. h3p 이후 news-sync · CLAUDE.md · STACK.md · deploy 스크립트를 건드린 커밋 없음.
- 라이브 잡 이미지 `news-sync:993c6a7`. env 이름: SUPABASE_URL · NAVER_BASE_URL · NAVER_DAILY_BUDGET · NEWS_SYNC_DAILY_BUDGET · NEWS_SYNC_CONCURRENCY · LOG_LEVEL · APP_VERSION (plain), SUPABASE_SERVICE_ROLE_KEY · NAVER_CLIENT_ID · NAVER_CLIENT_SECRET (secret).
- 스케줄러 4개 ENABLED: market `*/3 8-19 * * 1-5` · market-close `0 20 * * 1-5` · offhours `0 0-6/2,22 * * 1-5` · weekend `0 */2 * * 0,6`.
- 예약 실행 13:15 · 13:18 · 13:21 · 13:24 KST 전부 succeeded. 오늘(KST 00:00 이후) news-sync 로그 중 "quota" 포함 0건 → strike 0.
- 로그 엔트리 형태: `severity` 없음, `jsonPayload.msg`(메시지) · `jsonPayload.level`(pino 숫자, warn=40 · error=50) · `jsonPayload.version`(=APP_VERSION=SHA), 라벨 `run.googleapis.com/execution_name`.
- deploy-news-sync.sh 에 알림 정책 단계 없음(NOTIFICATION_CHANNEL_ID 불필요). docker build 는 로컬 작업 트리의 `packages/shared/` · `workers/news-sync/` 를 COPY 한다.
- runNewsSyncCycle 을 직접 부르는 테스트는 현재 0개 — 탐침 흐름 검증용 사이클 테스트를 새로 만든다.

## 코드 사실 (계획자가 읽은 값)
- index.ts:71-79 가 당일 skip 블록, :203 이 소진 error 로그의 자정 중단 문구, :36 · :54-57 이 그 설계 주석.
- apiUsage.ts:22-23 이 당일 중단 임계 상수(값 2)와 그 JSDoc. 소비처는 index.ts 와 apiUsage.test.ts:9,15 뿐.
- collectStockNews.ts:21-45 fetchPageWithRateLimitBackoff 는 지연 배열 [250, 500] 을 하드코딩하고 NaverRateLimitError 만 재시도한다. searchNews 는 429 를 절대 재시도하지 않고, 상태 없음(네트워크)·5xx 만 1초 후 1회 재시도한다.
- cadence.planRun 은 setting "full" 이면 입력 순서 그대로 전체를 selected 로 돌려준다(시계 무관) — 테스트 결정성에 사용.
</context>

## Source coverage audit

| Source | Item | Plan coverage |
|--------|------|---------------|
| GOAL | 한도 소진 오판으로 하루 수집이 꺼지지 않게, 판정 run 만 중단 + 단독 탐침 자동 복구 | Task 1 (코드) · Task 2 (라이브) |
| CONTEXT locked 1 | 당일 skip 제거, 판정 run 즉시 중단 · run 당 strike 1회 · error 로그 유지 | Task 1 (IL4-A) |
| CONTEXT locked 2 | strike≥1 이면 첫 대상 단독 탐침, 재시도 없음, 성공→재개 로그, 429→중단+strike, 비429 실패→계속, 예산/401 불변 | Task 1 (IL4-B) |
| CONTEXT locked 3 | 죽은 상수·주석·로그 문구 정리 | Task 1 (IL4-C) |
| CONTEXT locked 4 | CLAUDE.md 4항 재작성 + STACK.md 동일 미러, 다른 절 불변 | Task 2 (IL4-D) |
| Verification | vitest·typecheck·build, 지정 테스트 6종, 배포·스케줄러 불변·예약 실행 2건+ 관측, strike 0 이라 라이브 탐침 미발동 명시 | Task 1 verify · Task 2 (IL4-E) |

RESEARCH.md 없음(quick). Deferred 항목 없음.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 당일 중단 제거 + 첫 대상 단독 탐침 — collectStockNews 재시도 끄기 옵션부터 runNewsSyncCycle 사이클 테스트까지</name>
  <files>workers/news-sync/src/naver/collectStockNews.ts, workers/news-sync/src/index.ts, workers/news-sync/src/apiUsage.ts, workers/news-sync/tests/cycle.test.ts, workers/news-sync/tests/collectStockNews.test.ts, workers/news-sync/tests/apiUsage.test.ts</files>
  <behavior>
    tests/cycle.test.ts (신규, runNewsSyncCycle 을 직접 호출):
    - C1 strike 0: 대상 3개 모두 네이버 호출 1회씩(get 3회) · upsertNews 3회 · recordQuotaStrike 0회 · 메시지에 "quota probe" 가 들어간 로그 0건 · cycle complete 로그 strikesToday 0, quotaProbe null.
    - C2 strike 1, 전부 성공: get 3회 · upsertNews 3회 · 첫 get 호출의 params.query 가 첫 대상 이름 · 이벤트 순서상 첫 대상의 응답 완료가 두 번째·세 번째 대상의 호출 시작보다 앞섬(단독) · info "quota probe passed — resuming" 정확히 1회 · cycle complete quotaProbe "passed", quotaAbort null.
    - C3 strike 1, 탐침 429 소진 본문(errorCode "010", errorMessage "Query limit exceeded"): get 정확히 1회 · recordQuotaStrike 정확히 1회 · upsertNews 0회 · cycle complete quotaAbort "explicit", quotaProbe "rate-limited", skipped 2.
    - C4 strike 1, 탐침 429 일반 본문 — it.each 두 가지({errorCode "012", errorMessage "Rate limit exceeded"} 와 빈 객체 {}): get 정확히 1회(backoff 재시도 없음) · recordQuotaStrike 정확히 1회 · upsertNews 0회 · cycle complete quotaAbort "probe", quotaProbe "rate-limited", skipped 2, errors 1.
    - C5 strike 1, 탐침 비429 실패 — it.each 두 가지(HTTP 400, response 없는 네트워크 에러): run 계속 · 나머지 2개 대상 upsertNews 2회 · recordQuotaStrike 0회 · info "quota probe passed — resuming" 1회 · cycle complete errors 1, quotaProbe "passed". 네트워크 케이스는 searchNews 의 1초 재시도로 첫 대상 get 2회(총 4회) — 테스트 timeout 10초.
    - C6 strike 2 와 strike 7 (it.each): loadTargets 1회 호출 · get 3회 · info "quota probe passed — resuming" 1회 — 사전 skip 없음.
    - C7 회귀 잠금 strike 0, 두 번째 대상이 일반 429 를 3연속: 그 대상 get 3회(250ms·500ms backoff 유지) · recordQuotaStrike 0회 · 나머지 대상 upsertNews 2회.
    tests/collectStockNews.test.ts (추가 1건):
    - retryOnRateLimit false + 일반 429 1회 → NaverRateLimitError 로 reject, client.get 1회. 기존 "429 3회 연속 → get 3회" 케이스는 옵션 미지정 기본값으로 그대로 통과.
    tests/apiUsage.test.ts:
    - 상수 테스트에서 당일 중단 임계 상수 import 와 단언 1줄을 제거하고 QUOTA_STRIKE_SERVICE 단언은 유지.
  </behavior>
  <action>
    범위: CONTEXT locked 1·2·3 (IL4-A · IL4-B · IL4-C). 먼저 behavior 의 테스트를 작성해 실패를 확인한 뒤 구현한다.

    (1) collectStockNews.ts — 재시도 끄기 옵션 (IL4-B 탐침 단일 시도).
    - CollectOpts 에 선택 필드 retryOnRateLimit?: boolean 를 추가한다. JSDoc 은 "기본 true. false 면 429(NaverRateLimitError)를 재시도하지 않고 첫 응답에서 propagate — 한도 탐침용(quick-260915-il4). 이 run 의 탐침 대상 모든 페이지에 적용된다" 로 적는다.
    - 하드코딩된 지연 배열을 모듈 상수 RATE_LIMIT_RETRY_DELAYS_MS = [250, 500] 로 올리고, fetchPageWithRateLimitBackoff 가 지연 배열을 인자로 받게 한다. collectStockNews 는 opts.retryOnRateLimit === false 면 빈 배열을, 그 외에는 상수를 넘긴다. 기존 루프는 빈 배열이면 1회 시도 후 throw 하므로 루프 본문은 바꾸지 않는다. 함수 JSDoc 의 "최대 2회" 설명에 옵션으로 끌 수 있다는 한 줄을 더한다.
    - onPage · 페이지네이션 · 종료 조건은 한 줄도 바꾸지 않는다.

    (2) index.ts — 당일 skip 제거와 탐침 흐름.
    - import 에서 당일 중단 임계 상수를 뺀다. readQuotaStrikes · recordQuotaStrike 는 유지한다.
    - :71-79 블록을 const strikesToday = await readQuotaStrikes(supabase, dateKst) 한 줄로 바꾸고 조기 return 을 없앤다(IL4-A). readQuotaStrikes 가 throw 하면 지금처럼 fatal 로 전파한다(동작 불변).
    - "news-sync targets selected" info 로그 필드에 strikesToday 를 더한다.
    - quotaAbort 타입을 null | "explicit" | "suspected" | "probe" 로 넓힌다. 새 변수 quotaProbe: null | "passed" | "rate-limited" 를 null 로 둔다. strikeOnce 의 kind 인자 타입도 같이 넓힌다.
    - limit 콜백 본문 전체를 지역 async 함수 processTarget(t: TargetRecord, probe: boolean) 로 옮긴다(TargetRecord 는 ./pipeline/cadence.js 에서 type import). 본문은 그대로 두되 두 곳만 바꾼다. 첫째, collectStockNews 호출 opts 에 retryOnRateLimit: !probe 를 넣는다. 둘째, catch 의 첫 분기로 "probe 이고 분류가 budget-exhausted 또는 rate-limit" 을 추가한다. 이 분기는 stopAll = true, errors++, strikeOnce(분류가 budget-exhausted 면 "explicit", 아니면 "probe") 를 수행하고, first 일 때 log.warn 을 남긴다. 필드는 code · errorCode · errorMessage(원문 — [ASSUMED] 코드 형태 확인용) · quotaAbort · strikesToday(strikeOnce 반환값), 메시지는 "quota probe got HTTP 429 — aborting run (no retries); next run probes again" 이다. 이 분기는 Consecutive429Tracker 를 건드리지 않는다. warn 인 이유: 같은 날 판정 run 이 이미 error 를 남겼고, 실제 소진이 이어지면 이후 3분마다 반복될 예상된 거절이라 ERROR 로 쌓지 않는다(재량 판단).
    - probe 가 아닌 기존 분기(budget-exhausted → stopAll + strikeOnce("explicit") + error, rate-limit → tracker 휴리스틱, auth → stopAll, other → skip)는 로직을 바꾸지 않는다. 단 소진 error 로그 메시지(:203)의 뒷절, 즉 같은 날 두 번째 판정 시 자정까지 멈춘다고 약속하는 부분을 "next runs re-check with a single probe call" 로 교체해 메시지 전체를 "Naver daily quota exhausted — aborting run (no retries); next runs re-check with a single probe call" 로 만든다(IL4-C).
    - lastSeenMap 로드 뒤, pLimit 수집 전에 탐침 단계를 넣는다. strikesToday >= 1 이고 selected 가 비어 있지 않으면 다음 순서로 진행한다.
      · log.info({ strikesToday, probeCode: selected[0].code }, "Naver quota strike recorded earlier today — probing with first target alone") 를 남긴다.
      · await processTarget(selected[0], true) 로 동시성 없이 단독 실행한다.
      · quotaAbort 가 null 이 아니면 quotaProbe = "rate-limited". 아니면 quotaProbe = "passed" 로 두고, stopAll 이 false 일 때만 log.info({ strikesToday, probeCode }, "quota probe passed — resuming") 를 남긴다(401·예산 가드로 멈춘 경우 그 자체 로그로 충분).
      · 이어지는 pLimit 수집 대상은 selected 의 두 번째부터다. strike 0 이면 탐침 없이 selected 전체를 processTarget(t, false) 로 수집한다.
      · 탐침이 중단시킨 경우에도 나머지 대상을 똑같이 pLimit 에 넣는다 — processTarget 첫 줄의 stopAll 검사가 skipped 를 세므로 별도 분기를 만들지 않는다.
    - "news-sync cycle complete" 로그 필드에 strikesToday 와 quotaProbe 를 더한다.
    - 파일 헤더 Flow 주석을 고친다. 1b 는 "readQuotaStrikes(오늘 KST) — 1 이상이면 5단계 전에 첫 선택 대상을 재시도 없이 단독 탐침 (quick-260915-il4)" 으로 적는다. 5단계 설명에는 "탐침 429(본문 무관) → stopAll + strike 1회" 를 더한다. "한도 소진 설계 판단" 단락은 다음 내용으로 교체한다. 한 run 의 증거는 그 run 만 중단한다. 이후 run 은 첫 대상 1종목을 재시도 없이 먼저 수집해 회복을 확인하고, 429 가 아니면 나머지를 평소대로 수집한다. 실제 소진이 계속되면 run 당 거절 호출 1건만 쓴다. 당일 중단은 오판(초당 버스트 · 네이버 장애 · [ASSUMED] 코드 매핑) 2회가 하루 뉴스를 끄는 비용이 커서 quick-260915-il4 에서 제거했다.

    (3) apiUsage.ts — 당일 중단 임계 상수와 그 JSDoc(:22-23)을 삭제한다. QUOTA_STRIKE_SERVICE 의 주석 블록은 유지하되, strike 가 이제 관측용 마커이자 다음 run 탐침 트리거라는 한 줄을 더한다. readQuotaStrikes · recordQuotaStrike 시그니처는 불변.

    (4) tests/cycle.test.ts 신규 — index.ts 를 끝까지 돌리는 결정적 하네스.
    - vi.mock 대상(기존 테스트처럼 확장자 없는 상대 경로 사용): ../src/config, ../src/logger, ../src/services/supabase, ../src/naver/client, ../src/pipeline/targets, ../src/pipeline/lastSeen, ../src/pipeline/upsert, ../src/retention, ../src/apiUsage.
      · config: loadConfig 가 NewsSyncConfig fixture 를 반환한다(tests/naver.test.ts 의 mkCfg 모양, newsSyncMode "full", newsSyncConcurrency 3, naverDailyBudget 18750, naverBaseUrl https). mock 이 안 먹으면 실제 loadConfig 가 "missing env: SUPABASE_URL" 로 실패하므로 그 경우 경로를 .js 확장자 형태로 바꾼다.
      · logger: createLogger 가 { child: () => rec } 를 반환하고, rec 는 info · warn · error · debug 가 vi.fn 이며 child 는 자기 자신을 돌려준다.
      · services/supabase: 빈 객체.
      · naver/client: createNaverClient 가 테스트별로 설정되는 fake { get } 을 반환한다. get(url, { params }) 는 params.query(대상 이름)별 동작 큐에서 하나를 꺼내 수행한다: ok = 10ms 대기 후 { data: { items: [] } } 로 resolve, 429/400 = { response: { status, data: body } } 를 가진 Error 로 reject, net = response 없는 Error 로 reject. 호출 시작과 완료를 이벤트 배열에 "이름:start" · "이름:end" 로 기록한다.
      · targets: loadTargets 가 대상 3개를 반환한다(code 000001/000002/000003, 이름 알파/베타/감마, rank 1/2/3, watched false).
      · lastSeen: 빈 Map. upsert: { inserted: 0 }. retention: 0.
      · apiUsage: importOriginal 로 kstDateString 등은 실제 구현을 유지하고, readQuotaStrikes(테스트별 값) · recordQuotaStrike(resolve 1) · checkBudget(resolve 0) · incrementUsage(resolve 1) 만 vi.fn 으로 덮는다.
    - beforeEach 에서 vi.clearAllMocks 로 호출 기록을 비운다. cycle complete 요약은 rec.info 호출 중 두 번째 인자가 "news-sync cycle complete" 인 호출의 첫 인자로 읽는다.
    - behavior 의 C1~C7 을 구현한다. 사라진 skip 로그의 옛 문구를 테스트 문자열로 쓰지 않는다 — 부재는 행동(loadTargets · get 호출 수)으로 단언한다. 테스트 이름은 한글로 적는다.

    (5) tests/collectStockNews.test.ts 에 behavior 의 1건을 기존 "429 backoff retry" describe 안 mkSeq 헬퍼로 추가한다. tests/apiUsage.test.ts 는 behavior 대로 import 와 단언 1줄씩만 지운다.

    커밋(검증 통과 후, 명시 경로만 stage — 다른 세션이 동시에 커밋할 수 있다): 위 6개 파일만 git add. 메시지는 "fix(news-sync): 한도 소진 판정 후 당일 수집 중단 제거 — 판정 run 만 중단·다음 run 은 첫 종목 단독 탐침(재시도 없음)으로 자동 복구". Co-Authored-By 트레일러 넣지 않음. push 하지 않음.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && pnpm --dir workers/news-sync run typecheck && pnpm --dir workers/news-sync run test && pnpm --dir workers/news-sync exec vitest run tests/cycle.test.ts tests/collectStockNews.test.ts tests/apiUsage.test.ts && pnpm --dir workers/news-sync run build && ! grep -rn 'QUOTA_STRIKES_TO_STOP_DAY\|KST midnight\|second strike' workers/news-sync/src workers/news-sync/tests && grep -q 'quota probe passed — resuming' workers/news-sync/src/index.ts && grep -q 'retryOnRateLimit: !probe' workers/news-sync/src/index.ts</automated>
  </verify>
  <done>
    typecheck · build exit 0. vitest 전체 통과이며 기준선 128 passed · 3 todo(h3p) 대비 cycle.test 의 C1~C7(it.each 전개 포함) 과 collectStockNews 1건이 늘고 실패 0. 음성 grep 0건(당일 중단 상수 · 자정 skip 로그 · 두 번째 strike 문구가 src/tests 에서 사라짐). strike≥1 run 은 첫 대상 단독 탐침 후 재개하고, 탐침 429 는 네이버 호출 1건으로 run 을 끝낸다는 것을 C3·C4 가 get 호출 수 1 로 잠근다. 커밋 1건(fix(news-sync), 6개 파일, 한글, 트레일러 없음), push 없음.
  </done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md·STACK.md 4항 동일 갱신 → news-sync 재배포 → 스케줄러 불변 확인 · 예약 실행 2건 이상 관측</name>
  <files>CLAUDE.md, .planning/research/STACK.md</files>
  <precondition>Task 1 커밋이 HEAD 에 있고, $HOME/.config/gcloud/gh-radar-deployer.json 이 존재하며 gcloud 활성 configuration 이 gh-radar 다(deploy 스크립트 가드). 가드가 실패하면 configuration 을 새로 만들지 말고 중단 후 보고한다.</precondition>
  <reversibility rating="reversible">배포는 이전 이미지로 되돌릴 수 있다: gcloud run jobs update gh-radar-news-sync --region=asia-northeast3 --image=asia-northeast3-docker.pkg.dev/gh-radar/gh-radar/news-sync:993c6a7. 스케줄러는 스크립트가 같은 4개를 idempotent update 한다.</reversibility>
  <action>
    범위: CONTEXT locked 4 (IL4-D) + Verification 의 배포·관측 (IL4-E).

    (1) 문서 (IL4-D). CLAUDE.md:149 와 .planning/research/STACK.md:196 의 "공식 API 운영 기준" 4항 한 줄을 각각 Edit 로 교체한다. 두 파일에 들어갈 새 줄은 정확히 다음 문장이다(앞의 "4. " 포함, 굵은 글씨 표기 그대로):
    4. **일일 한도 소진 신호 시 해당 run 즉시 중단, 재시도로 두드리지 않음.** news-sync 는 이후 run 을 단독 탐침 호출 1건으로만 재확인하고, 통과하면 자동 재개한다 (오판으로 하루 수집이 꺼지는 것 방지).
    다른 줄 · 다른 절(스크래핑 5원칙 · Naver Search API Rate Limit 목록 · Conventions · Sources)은 건드리지 않는다. Write 금지(Edit 만). generate-claude-md 류 재생성 도구를 실행하지 않는다. 두 파일만 git add 해서 커밋한다. 메시지는 "docs: 공식 API 운영 기준 4항을 판정 run 중단 + 단독 탐침 재개로 갱신 — CLAUDE.md·STACK.md 동일"이고, Co-Authored-By 트레일러와 push 는 없다.

    (2) 배포 전 점검 (IL4-E).
    - 셸에 GOOGLE_APPLICATION_CREDENTIALS=$HOME/.config/gcloud/gh-radar-deployer.json, CLOUDSDK_CORE_PROJECT=gh-radar, GCP_PROJECT_ID=gh-radar 를 export 한다.
    - SUPABASE_URL 은 라이브 잡에서 읽는다. gcloud run jobs describe gh-radar-news-sync --region=asia-northeast3 --format=json 출력에서 컨테이너 env 중 name 이 SUPABASE_URL 인 value 를 python3 로 뽑아 셸 변수로만 export 한다. 값은 echo 하지 않고 "https:// 로 시작하는가"와 길이만 확인한다. .env 파일은 읽지 않는다. 비밀(서비스 롤 키 · 네이버 키)은 스크립트가 Secret Manager 에서 주입하므로 셸에서 다루지 않는다.
    - docker build 가 로컬 트리를 COPY 하므로 git status --short -- workers/news-sync packages/shared 가 비어 있어야 한다(비어 있지 않으면 중단·보고). DEPLOY_SHA=$(git rev-parse --short HEAD) 와 KST 시각을 기록한다.
    - 스케줄러 스냅샷: gcloud scheduler jobs list --location=asia-northeast3 --format='value(name.basename(),schedule,state,timeZone,httpTarget.uri)' 에서 news-sync 행만 LC_ALL=C sort 해 세션 스크래치 디렉터리의 sched-before.txt 에 저장한다.

    (3) 배포. 저장소 루트에서 bash scripts/deploy-news-sync.sh 를 실행한다(수 분 소요 — 긴 timeout 또는 백그라운드 + 완료 대기). 끝 줄 "deploy-news-sync.sh complete" 와 smoke FAIL 0 을 확인한다. 스크립트가 끝에 부르는 smoke 의 INV-1 은 수동 실행 1건을 만든다 — 이것은 예약 실행 관측에 세지 않는다.

    (4) 배포 후 확인.
    - 같은 명령으로 sched-after.txt 를 만들고 diff sched-before.txt sched-after.txt 가 빈 출력인지 본다. 폐기 3개(morning · morning-10h · intraday)는 "already absent" 여야 한다.
    - gcloud run jobs describe 의 컨테이너 image 태그가 DEPLOY_SHA 인지 확인한다.

    (5) 예약 실행 관측. foreground sleep 대신 Monitor/until-loop 로 폴링한다.
    - gcloud run jobs executions list --job=gh-radar-news-sync --region=asia-northeast3 --limit=8 --format='value(metadata.name,metadata.creationTimestamp,status.succeededCount,status.failedCount)' 에서 다음 조건을 모두 만족하는 실행을 고른다: 배포 스크립트 종료 이후 생성, smoke INV-1 실행이 아님, 생성 시각이 3의 배수 분의 0~15초.
    - 그중 succeeded 가 2건 이상 될 때까지 기다린다. 장시간이면 3분마다 1건이므로 보통 6~9분이면 된다.
    - 각 실행에 대해 로그 필터 resource.type="cloud_run_job" AND resource.labels.job_name="gh-radar-news-sync" AND labels."run.googleapis.com/execution_name"="<이름>" 로 jsonPayload.msg 가 "news-sync targets selected" 와 "news-sync cycle complete" 인 엔트리를 읽는다. 확인 항목: version == DEPLOY_SHA, mode(tiered 기대), strikesToday == 0, quotaProbe == null, quotaAbort == null, targets · pages · inserted · errors · budgetBefore → budgetAfter 기록.
    - 배포 이후 news-sync 의 jsonPayload.level>=40(warn 이상) 로그 건수도 센다.
    - 배포 종료가 19:57 KST 이후라 장시간 예약이 남지 않으면 20:00 market-close 와 22:00 offhours 를 관측 대상으로 삼고, 세션 안에 못 보면 미관측으로 정직하게 기록한다.

    (6) SUMMARY 에 명시할 사실:
    - 오늘 strike 0 이므로 라이브 탐침 경로는 발동하지 않았고, 탐침 동작은 cycle.test.ts 로만 검증됐다.
    - 429 본문 코드 형태는 여전히 [ASSUMED] 이며, 탐침 warn 로그가 원문 errorCode/errorMessage 를 남긴다.
    - 스케줄러 diff 0줄.
    - 관측 실행 표.
  </action>
  <verify>
    <automated>cd /Users/alex/repos/gh-radar && L='4. **일일 한도 소진 신호 시 해당 run 즉시 중단, 재시도로 두드리지 않음.** news-sync 는 이후 run 을 단독 탐침 호출 1건으로만 재확인하고, 통과하면 자동 재개한다 (오판으로 하루 수집이 꺼지는 것 방지).' && grep -Fxq "$L" CLAUDE.md && grep -Fxq "$L" .planning/research/STACK.md && ! grep -n '자정까지 skip' CLAUDE.md .planning/research/STACK.md && D1="$(git diff --numstat f5ac8ec -- CLAUDE.md)" && D2="$(git diff --numstat f5ac8ec -- .planning/research/STACK.md)" && test "$(printf '%s\n' "$D1" | cut -f1,2)" = "$(printf '1\t1')" && test "$(printf '%s\n' "$D2" | cut -f1,2)" = "$(printf '1\t1')" && diff <(GOOGLE_APPLICATION_CREDENTIALS=$HOME/.config/gcloud/gh-radar-deployer.json CLOUDSDK_CORE_PROJECT=gh-radar gcloud scheduler jobs list --location=asia-northeast3 --format='value(name.basename(),schedule,state)' | grep news-sync | LC_ALL=C sort) <(printf '%s\t%s\t%s\n' gh-radar-news-sync-market '*/3 8-19 * * 1-5' ENABLED gh-radar-news-sync-market-close '0 20 * * 1-5' ENABLED gh-radar-news-sync-offhours '0 0-6/2,22 * * 1-5' ENABLED gh-radar-news-sync-weekend '0 */2 * * 0,6' ENABLED | LC_ALL=C sort)</automated>
  </verify>
  <done>
    두 문서의 4항이 같은 새 문장이고 각 파일 diff 는 f5ac8ec 대비 +1/−1 줄뿐이다. docs 커밋 1건(한글, 트레일러 없음), push 없음. deploy-news-sync.sh exit 0 · smoke FAIL 0 · 라이브 잡 이미지 태그 = DEPLOY_SHA. 스케줄러 before/after diff 빈 출력이고 4개가 기대 식·ENABLED 그대로다. 배포 후 예약 실행 2건 이상 succeeded 이며, 그 cycle complete 로그의 version = DEPLOY_SHA · strikesToday 0 · quotaProbe null 을 SUMMARY 표로 남긴다. 라이브 탐침 미발동 사유(strike 0)를 명시한다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| news-sync → Naver Search API | 외부 쿼터 API. 429 응답 본문(errorCode/errorMessage)은 비신뢰 외부 입력이며 로그로 흘러간다 |
| news-sync → Supabase api_usage | service_role 로 strike 마커를 읽고 쓴다. 그 값이 이제 run 흐름(탐침 여부)을 결정한다 |
| 운영자 셸 → Cloud Run / Scheduler | 배포 시 SUPABASE_URL 을 라이브 env 에서 읽고 비밀은 Secret Manager 참조로만 주입 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-il4-01 | Denial of Service | index.ts 당일 skip (자가 차단) | high | mitigate | 당일 skip 블록 제거. strike≥1 이면 첫 대상 단독 탐침만 하고 429 가 아니면 즉시 재개 — C2 · C5 · C6 테스트가 잠근다 |
| T-il4-02 | Denial of Service | 실제 소진 중 네이버 반복 호출 (약관·차단 위험) | medium | mitigate | 탐침은 collectStockNews retryOnRateLimit=false 로 단일 시도. 탐침 429 면 네이버 호출 1건 후 stopAll, 나머지 대상은 skipped — C3 · C4 가 get 호출 수 1 로 잠근다. 3분 주기이므로 소진 지속 시 run 당 최대 1건. 자체 예산 가드(18,750)와 사전 checkBudget 은 불변 |
| T-il4-03 | Tampering | api_usage strike 마커 | low | accept | service_role 전용 RLS 와 기존 incr_api_usage grant 범위 안(h3p T-h3p-03 과 동일). 마커가 오염돼도 최악은 run 당 탐침 1종목 순차 선행이며 수집이 꺼지지는 않는다 |
| T-il4-04 | Information Disclosure | 탐침 warn 로그 · 배포 셸 | medium | mitigate | 로그에는 기존과 같은 errorCode 와 200자 절단 errorMessage 만 남기고 pino redact 경로는 불변. 배포 셸은 SUPABASE_URL 을 echo 하지 않고(형식·길이만 확인) .env 를 읽지 않으며, 비밀은 --set-secrets 참조로만 전달한다 |
| T-il4-05 | Repudiation | 탐침 결과 추적 | low | mitigate | targets selected 로그에 strikesToday, cycle complete 로그에 strikesToday · quotaProbe · quotaAbort 를 남겨 run 단위로 탐침 발동·결과를 재구성할 수 있게 한다 |
</threat_model>

<verification>
- 로컬: pnpm --dir workers/news-sync run typecheck / run test / run build 모두 exit 0, cycle.test.ts C1~C7 통과, 음성 grep 0건.
- 행동: strike 0 → 탐침 없음, strike≥1 → 첫 대상 단독 선행(이벤트 순서), 탐침 429(소진·일반 본문) → get 1회 · strike 1회 · 재시도 0, 탐침 비429 실패 → 계속, strike≥2 → skip 없음, 비탐침 429 backoff 유지.
- 문서: CLAUDE.md · STACK.md 4항 동일 문장, 각 +1/−1 줄.
- 라이브: 이미지 태그 = DEPLOY_SHA, smoke FAIL 0, 스케줄러 before/after diff 0 과 기대 4행 일치, 배포 후 예약 실행 2건 이상 succeeded (version = DEPLOY_SHA, strikesToday 0, quotaProbe null).
</verification>

<success_criteria>
- 한도 소진 오판이 2회 쌓여도 news-sync 가 다음 run 에 수집을 재개한다(당일 중단 경로가 코드에 없음).
- 실제 소진이 이어지는 동안 run 당 네이버 호출은 탐침 1건으로 제한된다.
- 운영 문서 두 곳이 새 동작과 일치한다.
- 새 이미지가 라이브에서 예약 실행 2건 이상을 정상 처리했고 스케줄러는 변하지 않았다.
- 커밋 2건(fix(news-sync) · docs), 한글 메시지, Co-Authored-By 없음, push 없음, 명시 경로만 stage.
</success_criteria>

<output>
Create `.planning/quick/260915-il4-news-sync-quota-probe-recovery/260915-il4-SUMMARY.md` when done
</output>
