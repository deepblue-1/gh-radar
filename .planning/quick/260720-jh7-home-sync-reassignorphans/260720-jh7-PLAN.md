---
phase: quick-260720-jh7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/home-sync/src/ai/roundup.ts
  - workers/home-sync/src/ai/roundup.test.ts
  - workers/home-sync/src/ai/prompt.ts
  - workers/home-sync/src/ai/prompt.test.ts
  - workers/home-sync/src/ai/clusterSurges.ts
  - workers/home-sync/src/ai/clusterSurges.test.ts
autonomous: false
requirements: [QUICK-260720-JH7]

must_haves:
  truths:
    - "라운드업(시황/거래상위/마감) 기사가 여러 급등 종목명을 나열해도 테마 병합 신호로 쓰이지 않는다"
    - "고려산업 실사례([서울데이터랩] 기사)가 라운드업으로 판정되어 사료 테마 힌트가 무력화되지 않는다"
    - "Claude 프롬프트가 라운드업 기사를 테마 묶음 근거로 삼지 말라는 규칙 + 라운드업 라벨을 본다"
    - "기존 82개 테스트 회귀 0"
  artifacts:
    - path: "workers/home-sync/src/ai/roundup.ts"
      provides: "isRoundupNews 데이터 기반 라운드업 판정 헬퍼"
      contains: "export function isRoundupNews"
    - path: "workers/home-sync/src/ai/roundup.test.ts"
      provides: "경계(2/3) + 실사례 fixture 테스트"
  key_links:
    - from: "workers/home-sync/src/ai/prompt.ts"
      to: "workers/home-sync/src/ai/roundup.ts"
      via: "formatClusterMessage 뉴스 라인 [라운드업] 라벨"
      pattern: "isRoundupNews"
    - from: "workers/home-sync/src/ai/clusterSurges.ts"
      to: "workers/home-sync/src/ai/roundup.ts"
      via: "reassignOrphans 후보 수집 시 라운드업 news 제외"
      pattern: "isRoundupNews"
---

<objective>
home-sync 라운드업(시황) 기사가 급등 종목 여러 개를 한 기사에 나열할 때 발생하는 두 가지 오염을 가드한다:
(a) Claude 가 무관 종목들을 한 테마로 오클러스터, (b) reassignOrphans 가 "라운드업 제목에 종목명 등장"을 정밀 병합 신호로 오해해 무관 종목을 흡수.

2026-07-20 실사례: "[서울데이터랩] 코스피 거래상위…" 라운드업 기사가 고려산업+형지+흥아해운+SK이터닉스 등을 나열 → 고려산업이 엉뚱한 테마로 오흡수되어 사료 테마 힌트(고려산업+미래생명자원)가 무력화됨.

Purpose: 데이터 기반 휴리스틱(급등 집합 종목명 3개 이상 등장 = 라운드업)으로 라운드업 기사를 식별하고, Claude 프롬프트와 reassignOrphans 양쪽에서 병합 신호에서 배제한다.
Output: isRoundupNews 헬퍼 + 프롬프트 규칙/라벨 + reassignOrphans 라운드업 제외 + 회귀 없는 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@workers/home-sync/src/ai/prompt.ts
@workers/home-sync/src/ai/clusterSurges.ts
@workers/home-sync/src/pipeline/loadSurges.ts

<interfaces>
<!-- 실행자가 알아야 할 계약. 코드베이스 탐색 불필요. -->

loadSurges.ts:
```typescript
export interface NewsRow {
  id: string; stock_code: string; title: string; url: string;
  source: string | null; published_at: string;
  description: string | null;  // Naver 스니펫 (HTML 포함 가능, nullable)
}
export interface Surge { code: string; name: string; changeRate: number; news: NewsRow[]; }
// 기존 nameMatches 패턴 계승: verbatim 부분문자열 판정 (n.title.includes(name) || n.description?.includes(name))
```

@gh-radar/shared:
```typescript
export interface HomeNewsRef { title: string; url: string; source: string; }  // description 없음(title만)
```

clusterSurges.ts (현재 시그니처 — 변경 최소화):
```typescript
export interface ResolvedTheme {
  name: string; reason: string | null; stockCodes: string[]; news: HomeNewsRef[];
}
export function reassignOrphans(
  themes: ResolvedTheme[],
  singles: RawSingle[],  // { stockCode, reason, newsRefs }
  surgeByCode: Map<string, { name: string; changeRate: number }>,  // ← 모든 급등 종목명이 여기 있음
): { themes: ResolvedTheme[]; singles: RawSingle[] }
// 후보 테마 수집: t.reason.includes(name) || t.news.some(n => n.title.includes(name))
```

prompt.ts formatClusterMessage:
```typescript
export function formatClusterMessage(surges: Surge[], themeHints?: Map<string,string[]>): {
  message: string;
  indexedNews: Array<{ title: string; url: string; source: string }>;
}
// 뉴스 라인 현재 형식: `[${idx}] ${s.code} ${n.title} — ${snippet}` 또는 `[${idx}] ${s.code} ${n.title}`
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: isRoundupNews 데이터 기반 라운드업 판정 헬퍼 + 테스트</name>
  <files>workers/home-sync/src/ai/roundup.ts, workers/home-sync/src/ai/roundup.test.ts</files>
  <behavior>
    - 경계: 급등 종목명 2개만 등장 → false, 3개 등장 → true (minDistinct 기본 3).
    - 실사례 fixture: title="[서울데이터랩] 코스피 거래상위 고려산업·형지·흥아해운·SK이터닉스…" + surgeNames 에 고려산업/형지/흥아해운/SK이터닉스 포함 → true.
    - description 기반: title 에 1개 + description 에 2개(총 3 distinct) → true (title+description 합산).
    - 같은 종목명이 title·description 중복 등장 → distinct 1로만 카운트(중복 부풀림 방지).
    - surgeNames 빈 Iterable → 항상 false. HomeNewsRef(description 없음)도 안전 처리(title만).
  </behavior>
  <action>
새 파일 `workers/home-sync/src/ai/roundup.ts` 생성. 프로젝트 특정 import 없음(순수 함수, 순환의존 회피 — prompt.ts·clusterSurges.ts 양쪽이 import).

`export function isRoundupNews(news: { title: string; description?: string | null }, surgeNames: Iterable<string>, minDistinct = 3): boolean` 구현:
- 판정 텍스트 = `news.title + " " + (news.description ?? "")`. (HomeNewsRef 처럼 description 없으면 title 만.)
- surgeNames 중 판정 텍스트에 **verbatim 부분문자열**로 등장하는 **distinct** 종목명 수를 센다 (기존 loadSurges.nameMatches 패턴 계승 = `text.includes(name)`). 같은 name 은 1회만 카운트.
- 빈 name("") 은 스킵(모든 텍스트에 매칭되는 오판 방지).
- distinct count >= minDistinct 이면 true.

**결정(자기 자신 종목명 포함): 전체 distinct 3개 이상** 채택(총합 기준, 소속 종목 제외 안 함). 근거: 실사례([서울데이터랩])는 4개 distinct 로 여유 있게 라운드업 판정되고, 개별 특징주 기사는 소속 종목 외 다른 급등 종목명을 2개 이상 나열하는 일이 드물어 오판 위험 낮음. 단순·데이터 기반 유지.

키워드 리스트("시황"/"거래상위" 등) 사용 금지 — 순수 데이터 휴리스틱.

`roundup.test.ts` 에 위 behavior 5케이스 작성.
  </action>
  <verify>
    <automated>cd workers/home-sync && npx vitest run src/ai/roundup.test.ts</automated>
  </verify>
  <done>isRoundupNews 가 경계(2 false/3 true) + 실사례 fixture + description 합산 + distinct dedup + 빈 names 케이스를 모두 통과.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 프롬프트 규칙/라벨 + reassignOrphans 라운드업 제외 + 테스트</name>
  <files>workers/home-sync/src/ai/prompt.ts, workers/home-sync/src/ai/prompt.test.ts, workers/home-sync/src/ai/clusterSurges.ts, workers/home-sync/src/ai/clusterSurges.test.ts</files>
  <behavior>
    - prompt.ts: CLUSTER_SYSTEM_PROMPT 에 라운드업 규칙 문자열이 존재("시황"/"거래상위"/"라운드업"/"특징주" 등 키워드 + "테마 묶음의 근거로 삼지 말" 취지).
    - formatClusterMessage: 라운드업 판정 뉴스 라인은 `[라운드업]` prefix 를 포함, 비라운드업 라인은 미포함. indexedNews 계약(순서/개수)은 불변.
    - reassignOrphans: 테마 news 가 라운드업 기사뿐이면 종목명 매칭 신호로 쓰이지 않아 오흡수 안 됨. 비라운드업 news 제목 매칭 또는 reason 매칭은 기존대로 병합.
    - 고려산업 실사례 재현: 라운드업 news 제목에 "고려산업" 이 등장해도(다른 급등 종목명 3+ 동반) reassignOrphans 가 그 테마로 병합하지 않음.
  </behavior>
  <action>
**prompt.ts:**
1. 상단에 `import { isRoundupNews } from "./roundup";` 추가.
2. `CLUSTER_SYSTEM_PROMPT` 규칙 목록에 한 줄 추가: 여러 종목을 나열하는 시황/거래상위/마감 라운드업 기사(뉴스 라인에 `[라운드업]` 표기됨)는 테마 묶음의 근거로 삼지 말 것. 특징주 기사와 참고 테마 분류를 우선. (기존 규칙 톤·형식 유지, JSON 출력 예시 계약 불변.)
3. `formatClusterMessage` 내부: 뉴스 라인 생성 전 급등 종목명 집합 `const surgeNames = surges.map((s) => s.name);` 준비. 각 news `n` 에 대해 `const roundup = isRoundupNews(n, surgeNames);` 계산(n 은 NewsRow → title+description 사용). 라운드업이면 라인 형식에 `[라운드업] ` prefix 삽입: `[${idx}] [라운드업] ${s.code} ${n.title}...`. indexedNews.push 는 기존과 동일(verbatim, 라벨 미포함) — 계약 불변.

**clusterSurges.ts:**
1. 상단에 `import { isRoundupNews } from "./roundup";` 추가.
2. `reassignOrphans` 는 **시그니처 변경 없음**(하위호환) — surgeByCode 에 이미 모든 급등 종목명이 있음. 함수 시작부에 `const surgeNames = [...surgeByCode.values()].map((v) => v.name);` 1회 계산.
3. 후보 테마 수집(candidates filter)의 `inNews` 조건을 라운드업 제외로 변경: `const inNews = t.news.some((n) => !isRoundupNews(n, surgeNames) && n.title.includes(name));`. `inReason`(reason 매칭)은 그대로 유지 — 라운드업 제외는 news 제목 동반 나열 신호에만 적용.
   (ResolvedTheme.news 는 HomeNewsRef 로 description 없음 → isRoundupNews 가 title 만으로 판정, 정상.)

**테스트:**
- prompt.test.ts: (a) CLUSTER_SYSTEM_PROMPT 에 라운드업 규칙 키워드 존재, (b) formatClusterMessage 가 라운드업 뉴스(3+ 종목명 나열)에 `[라운드업]` 라벨 부여 + 일반 뉴스엔 미부여, (c) indexedNews 개수/순서 불변.
- clusterSurges.test.ts: 고려산업 오흡수 방지 fixture 추가 — 어떤 테마의 news 제목이 라운드업("코스피 거래상위 고려산업·형지·흥아해운·SK이터닉스…", 급등 종목명 3+ 나열)이고 single=고려산업 일 때, reassignOrphans 가 그 테마로 병합하지 **않고** singles 에 고려산업이 남는지 검증. 기존 금호건설 병합(비라운드업 단일 종목명 등장) 케이스는 여전히 병합되는지 회귀 확인.
  </action>
  <verify>
    <automated>cd workers/home-sync && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>전체 테스트 통과(기존 82 + 신규 회귀 0), typecheck exit 0. 라운드업 라벨/규칙/제외 신규 테스트 green. 고려산업 실사례 fixture 가 오흡수 방지를 증명하고 금호건설 정상 병합은 유지.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 배포 + smoke (오케스트레이터 실행)</name>
  <what-built>home-sync 라운드업 가드 (isRoundupNews 헬퍼 + 프롬프트 규칙/라벨 + reassignOrphans 라운드업 제외). 전체 테스트 82+신규 green, typecheck 통과.</what-built>
  <how-to-verify>
오케스트레이터가 실행:
1. home-sync 이미지 빌드 + Cloud Run Job `gh-radar-home-sync` 재배포 (기존 deploy 스크립트 사용).
2. Job 수동 execute → 로그에서 클러스터 결과 확인: 라운드업 기사에 `[라운드업]` 라벨 출력, 고려산업류 종목이 무관 테마로 오흡수되지 않음.
3. smoke-home-sync 스크립트 재실행 → 기대 결과 PASS.
4. (선택) 급등 있는 슬롯이면 `/api/home` 스냅샷에서 사료 테마 힌트가 라운드업에 무력화되지 않았는지 스팟체크.

주의(lessons): 주기/기대값 하드코딩된 smoke 스크립트가 있으면 이번 변경과 무관하더라도 실패 표기 여부 확인.
  </how-to-verify>
  <resume-signal>배포+smoke PASS 확인 후 "approved" 또는 이슈 기술</resume-signal>
</task>

</tasks>

<verification>
- `cd workers/home-sync && npx vitest run` → 전체 통과 (기존 82 + 신규, 회귀 0).
- `npx tsc --noEmit` → exit 0.
- isRoundupNews 경계(2/3) + 실사례 fixture 검증.
- reassignOrphans 가 라운드업 news 제목의 동반 나열을 병합 신호에서 제외 (고려산업 fixture), 비라운드업 단일 종목명 등장은 계속 병합 (금호건설 회귀).
</verification>

<success_criteria>
- 라운드업 기사(급등 종목명 3+ 나열)가 데이터 기반으로 식별된다 (키워드 리스트 아님).
- Claude 프롬프트가 라운드업 규칙 + `[라운드업]` 라벨을 받아 오클러스터 압력이 줄어든다.
- reassignOrphans 가 라운드업 제목 동반 나열을 정밀 신호로 오해하지 않아 고려산업류 오흡수가 방지된다.
- 기존 82개 테스트 회귀 0.
</success_criteria>

<output>
After completion, create `.planning/quick/260720-jh7-home-sync-reassignorphans/260720-jh7-SUMMARY.md`
</output>
