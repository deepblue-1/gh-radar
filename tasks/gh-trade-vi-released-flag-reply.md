# gh-trade 회신 — VI 해제됨 플래그 (2026-09-23)

요청서: `gh-trade-vi-released-flag-request.md`. 아래는 gh-trade 회신 원문이다.

- 필드: `vi_released: bool`, vtable 슬롯 36. 72·73 모두에 실리고, 옛 서버에서는 false 로 읽힌다.
- Pending 항목: 해제되면 `vi_released=true` 로 73 푸시된다. 체결·취소·거부로 끝난 항목도 마찬가지다.
- 목록에 남는가: 해제 뒤에도 서버 목록에 남고 플래그만 켜진다. 클라에서 숨긴다. 서버가 항목을 지우는 건 마감 퍼지(15:40 KRX, 20:00·06:00)와 기동 시 날짜 가드뿐이다.
- 재기동 보정 경로: 서버를 다시 켤 때 해제예정 + 60초가 지난 항목을 해제로 보정한다. 이 경로는 73 으로 푸시하지 않고, 로그인할 때 받는 72 스냅샷으로 전달된다.
- 알려진 한계: VI 도중에 서버를 재기동하면, 그때 이미 끝나 있던 항목은 그 VI 의 해제를 놓칠 수 있다. 다음 재기동 보정이나 20:00 퍼지 전까지 false 로 남는다.
- 실서버 배포 커밋: 아직 없다.
- gh-radar `relay/src/generated/` 두 파일(StockDMA.fbs · viorder-item.ts)은 gh-trade 가 갱신해 두었다(커밋은 gh-radar 몫).
